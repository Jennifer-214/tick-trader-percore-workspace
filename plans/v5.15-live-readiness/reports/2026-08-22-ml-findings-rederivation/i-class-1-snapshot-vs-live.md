# Scan 1 re-derivation — snapshot-vs-live over the training-panel worker surface (at HEAD 273cd4c)

**Replaces:** the never-persisted `i-class-1-snapshot-vs-live.md` of 2026-08-21 (digest: `plans/v5.15-live-readiness/reports/2026-08-21-training-artifact-surface-scan/README.md`).
**Scope:** every `TrainingPanelState` field a worker thread consumes, SNAPPED vs LIVE; Class-13 snap-block completeness; the `ui_training_side` consumer trace; AR-20 on the leaf-4b `hp_override` chain; one targeted look each for the unresolved ids S1-F11 and S3-F11.
**Method + pin:** the orchestrator's leaf-5 edit landed in `Backtest/BacktestPanels.hpp` and `Backtest/BacktestEngine.hpp` **mid-scan** (working tree grew +50/+126 lines; `LabelBatchTarget` / `Backtest_ComputeLabelsBatch` appearing — in-flight work per the directive, not findings). All analysis and **every `file:line` below is pinned to HEAD `273cd4c`** via `git show HEAD:` extracts; anchors were cross-verified (e.g. `TrainingPanelState` at `Backtest/BacktestPanels.hpp:2968`, `mh_run_one_horizon_fv` at `:4467`). Reads were re-confirmed HEAD-consistent after the drift was detected. Grep roots covered: `Backtest/ CoreFrameworks/ GUI/ ML_Headers/ tests/ tools/ Strategies/ DataStream/ FixedPoint/` (named explicitly per Landmine 19). I did not trust the digest, the plan's SHIPPED table, or code comments — each claim below was re-read at HEAD; three plan/comment claims did **not** survive (marked ⚠ inline).
*(Orchestrator note at persist time: leaf 5 has since LANDED as engine `f317c2d`; NEW-1/NEW-4/NEW-5/NEW-6 below were FIXED same-day — see the punch-list in the E.1.2.D plan; S3-F11's single-target disposition is subsumed by f317c2d's wrapper NaN-prefill.)*

Files referenced by bare line numbers: **P** = `/home/caramel/code/FoxML_Trader_v2/Backtest/BacktestPanels.hpp`, **E** = `/home/caramel/code/FoxML_Trader_v2/Backtest/BacktestEngine.hpp`, **L** = `/home/caramel/code/FoxML_Trader_v2/Backtest/LabelFunctions.hpp`, all at 273cd4c.

---

## 1. S1-F1..F10 verdicts at HEAD (the digest rows, re-verified in code)

| Id | Verdict at HEAD | Evidence |
|---|---|---|
| S1-F1 (8 hyperparams read live; snaps unread) | **FIXED** — with one surviving axis (NEW-1) | Worker captures all 8 snaps P:4972-4986; booster consumes `snap_hp` P:4647; RFV P:4691+4697; summary reports the snapshot P:4756-4758; parallel jobs carry it P:4851, P:5083 |
| S1-F2 (`label_type` live at FV worker) | **FIXED for FV + WF; NOT applied to the HP sweep** (NEW-4) | FV click sources from `run_control->run_config.label_type` P:7469; WF click P:6912; HP-sweep click still snaps the combo: `hp_args->snap_label_type = state->label_type` P:7290 |
| S1-F3 (stamp records defaults) | **FIXED for 7 of 8 params** | Stamp uses `hp_override` when present E:1426-1443. Residuals: `n_estimators` (NEW-1), `tree_method` stamp split-brain — stamp reads `config_used.xgb_tree_method` E:1443 while training uses the panel's (KNOWN, homed E.1.2.D leaf 14; verified real at HEAD) |
| S1-F4 (stale CSV arrays when inputs hidden) | **PARTLY FIXED; hidden-parser shape STILL OPEN** | Side flip clears the kind CSV P:5287-5288 (commit `1cfa658` ✓). But the render gate P:5356-5357 hides inputs **and parsers** for `label_type ∉ {WIN_LOSS, BARRIER, VOL_BARRIER, PVS}`; a **Label-Type combo change does NOT clear** (P:5340-5344 retargets only `model_path`), so stale `ui_label_kind_per_horizon[]` / `ui_tp/sl_per_horizon[]` keep driving the MH click snaps (P:6387-6401): pick PVS, type Label-Kind CSV, switch combo to Will-Peak → horizons still train the stale CSV kinds while the combo displays Will Peak. Buy side has no side-gate to intercept (`Training_SideLabelGate` returns 2 for side≠1, L:584-585) |
| S1-F5 (no cross-panel exclusion; realloc UAF) | **PARTLY FIXED; a second unhomed direction remains** (NEW-5) | Collect gates now consult `Training_AnyWorkerRunning` P:5538-5539, P:5624-5627 (leaf 6 ✓). Residual A (homed, verified): `GUI_Panel_RunControl` `can_run = data->selected_count > 0` only, P:714-716. **Residual B (NOT in the plan): every train-side gate omits `!run_control->running`** — P:6073 (`can_train`), P:6303 (`mh_can_train`), P:6884 (`can_wf`), P:7262-7265 (`can_hp`), P:7412-7415 (`can_fv`) |
| S1-F6 (WF worker had no snap block) | **FIXED** | `WalkForwardWorkerArgs` snap block P:3421-3426; capture-before-free P:3459-3465; populated at click P:6906-6913 |
| S1-F7 (side flip clobbers typed `model_path`) | **STILL OPEN** | Side flip P:5289-5290 and label-type change P:5343 both `snprintf` over `state->model_path`; a hand-typed RFV target path is silently replaced, re-pointing what the next Run-Full-Validation click stamps |
| S1-F8 (MH completion `status_msg` has no live reader) | **STILL OPEN** | MH worker writes the completion line P:5205-5208 (and error lines P:5004, P:5011); the only renderer is gated `if (state->model_trained)` P:6534-6536; the MH path sets `model_trained=false` at click (P:6138) and never true (only the dead `train_model_worker_fn` sets it, P:3931/P:4075). Class-44 shape |
| S1-F9 (dead `snap_model_path` / `snap_label_type`) | **STILL OPEN** | Captured P:4924-4938; consumed only as `(void)label_type` P:5213 and `(void)model_path` P:5217 in the `#ifndef USE_XGBOOST` branch — zero production reads. The Class-13 fossil |
| S1-F10 (unsynchronised `mh_horizon_status[8][128]`) | **STILL OPEN** | Written from N parallel worker threads P:4494, 4529, 4618, 4683, 4709-4734, P:5077/5089/5141; read every GUI frame P:6521-6522; plain `char` arrays, no volatile/atomic/fence. Display-only tearing; same family: mid-run `memset(&state->mh_horizon_fv[h])` P:5030 under a rendering table |

---

## 2. The SNAPPED-vs-LIVE table at HEAD (ask 1)

Legend: **SNAP-CLICK** = read once on the GUI thread at click into worker args (the correct pattern). **LIVE-BY-DESIGN** = deliberately live (cancel/progress/status). **LIVE-SHARED** = read off shared state from the worker; stable only while the exclusion matrix holds.

| Worker (spawn site) | SNAP-CLICK | LIVE-BY-DESIGN | LIVE (residual risk) |
|---|---|---|---|
| `walkforward_worker_fn` P:3452 (spawn P:6914) | wf quartet, `label_type` (from `run_config`), `snap_hp` — P:6906-6913 | `&state->wf_progress`, `&state->wf_cancel` P:3470 | `data = &run_control->results` — realloc hazard only via NEW-5 |
| `fullvalidation_worker_fn` P:3667 (spawn P:7479) | model_path, secret, label fwd/tp/sl, `label_type` (from `run_config` P:7469), wf quartet, gap, fraction, `snap_hp` P:7478 | `fv_progress`/`fv_cancel` P:3785 | `state->model_path` re-read at P:3816 — **deliberate** (diagnostic prints the *current* value to contrast with the click-time one); `data->config_used.auto_stamp_on_held_out` P:3732 + `data->sample_count` P:3715 — stable absent NEW-5 |
| `hp_sweep_worker_fn` P:3539 (spawn P:7301) | ranges, num_params, wf quartet — P:7294-7300; **`label_type` snapped from the COMBO not `run_config`** P:7290 (NEW-4) | `hp_progress`/`hp_total`/`hp_cancel` P:3564-3565 | `data->config_used.xgb_train_nthread` (sweep parallelism) E:2878 |
| `train_multi_horizon_worker_fn` P:4918 (spawns P:6229 single-N=1, P:6440 multi) | ALL of `MultiHorizonWorkerArgs` P:4392-4431 captured pre-free P:4924-4998, incl. the 8 hyperparams → `snap_hp` P:4972-4986; both click handlers populate every field (P:6141-6266, P:6345-6477 — field-by-field verified complete) | `state->mh_cancel` P:5073, P:5171; progress/status writes | `results->sample_count` P:5010, `config_used.multi_horizon_max_threads` P:5047, `xgb_train_nthread` P:5169; serial mode **mutates + restores the shared `run_control->run_config`** P:5191, P:5203 — all stable only absent NEW-5 / the Run-Control residual |
| `mh_per_horizon_parallel_worker` P:4872 → `mh_run_one_horizon_fv` P:4467 | everything by parameter; `snap_hp` on the job P:4851 because "the parallel worker cannot reach the outer scope" | `state->mh_cancel` P:4665, P:4695, P:4728; per-horizon status/progress/complete writes | `results->config_used.xgb_eval_nthread` P:4648 (isolated copy in parallel; shared in serial) |
| `collect_multi_horizon_worker_fn` P:433 (spawn P:5686) | horizons/tp/sl P:436-442 | `rc->cancel_flag` P:460 | `rc->run_config.label_type` P:488, P:509 — written only by the gated collect clicks; stable |
| `backtest_worker_fn` P:356 (spawns P:564 via `RunControl_Start`, P:5617 collect) | run_config populated pre-`running=1` | cancel/progress | — |
| `train_model_worker_fn` P:3883 | — | — | **DEAD: zero `pthread_create` sites at HEAD** (see NEW-3); its snapshot-at-worker-ENTRY pattern P:3894-3911 is the old racy shape, now unreachable |

**Operator mid-run edit impact, post-fix:** editing hyperparams / WF params / label combo / TP-SL CSVs / side / secret **mid-run changes nothing for an in-flight run** on any live spawn path — the remaining live reads are cancel/progress (intended), one deliberate diagnostic, and shared-`results` reads that only go wrong when a *second* worker is allowed to start (NEW-5). That is a genuine, verified improvement over the 2026-08-21 state.

**`ui_training_side` full consumer trace (charter item):** P:3151 (field) → side-flip handler P:5260-5291 (retargets label to WILL_PEAK, clears kind CSV, **clobbers model_path** = S1-F7) → tier gate `Training_SideLabelGate` P:5307-5311 (aggregates over the CSV set; L:583-592) → collect gates via `side_gate` P:5538, P:5628 → train gate P:6073-6074 → role preview P:6038 → click-time snaps P:6209, P:6410 (`snap_training_side`) → worker stack P:4997 → job P:5116 → `mh_run_one_horizon_fv(training_side)` P:5192/P:4895 → `Training_ResolveRole` P:4542 → role file + stamp `req_role` P:4576-4600. Save Run reads it click-time at P:6606. **No worker reads `ui_training_side` directly at HEAD** — the side surface is fully snapped. Clean.

---

## 3. S1-F11 — the recovery candidate (ask 2)

**S1-F11-recovered (candidate) = NEW-1: `n_estimators` is snapped, threaded, stamped — and both validation trainers ignore it for a hardcoded 200.** — **HIGH**

- The shipped-model booster loops `for (it = 0; it < hp.n_estimators; ++it)` P:4664 — the operator's value (350 for `twins`).
- The WF fold trainer: `int n_rounds = 200;` E:1974, loop E:1984 ("no early stopping yet — full n_rounds always", E:1973).
- The held-out trainer: `int n_rounds = 200;` E:2457, loop E:2461.
- `XGBHyperparams_Apply` cannot carry it: "n_estimators is NOT a booster param — it's the iteration loop count… Caller reads hp.n_estimators" (`Backtest/XGBHyperparams.hpp:136-137`) — and these two callers don't.
- The stamp records `hp.n_estimators` E:1436; the summary records it P:4758.

**Consequence:** with panel `n_estimators=350`, the shipped model has 350 trees and the stamp+summary say 350 — but the WF metric and the held-out figure (the number that gates auto-stamp via `gap_threshold`) describe **200-tree models nobody ships**. This is leaf 4's own defect class ("the held-out figure… described a model nobody trained and nobody shipped") surviving on one of the eight axes, and it is invisible to every shipped oracle: `XGBHyperparams_Defaults().n_estimators == 200` (`XGBHyperparams.hpp:62` — whose comment "matches v5.9.5g WF/HeldOut hardcoded n_rounds" *documents* the coupling), so at defaults the hardcode is bytewise silent, C.3k (a fallback-constant pin) stays green, and the dogfood stamp-grep passes because the stamp reads the snapshot. ⚠ The plan's leaf-4 claim "four configurations collapse to one" is therefore **not fully true at HEAD**.
Why it is the best S1-F11 fit: it is exactly scan 1's charter object — a `TrainingPanelState` field, its click-to-consume path, failing at the consumer — and it is the Class-13 sub-shape one step deeper than the one the catalog just gained: *snap block complete, snap threaded, consumer substitutes a constant*. Identity with the lost row is unprovable; treat "S1-F11" as re-derived content, not a restored citation.
**Disposition:** replace both `n_rounds = 200` with `hp.n_estimators` (hp is already in scope at both sites); fold into leaf 14 or ship as a leaf-4c one-liner pair; add the missing oracle — a test that passes a non-default `n_estimators` via `hp_override` and asserts iteration count (observable via `tm_progress`-style counting or booster tree dump in the XGB TU; at minimum extend C.3k's comment to stop implying coverage).

---

## 4. AR-20 on the leaf-4b chain (ask 3)

Callee chain: `Backtest_RunFullValidation` (E:1302, `hp_override` default nullptr E:1316) → `Backtest_RunWalkForward` (E:1660/1668) + `HeldOutSplit_TrainEval` (E:1176-1181, E:2304-2309). All callers, all roots:

| Caller | Passes | Verdict |
|---|---|---|
| `fullvalidation_worker_fn` → RFV P:3782-3787 | `&snap_hp` | wired ✓ |
| `mh_run_one_horizon_fv` → RFV P:4691-4697 | `&snap_hp` | wired ✓ |
| RFV → WF internally E:1364 | forwards `hp_override` | ✓ |
| RFV → HeldOut internally E:1374 | forwards `hp_override` | ✓ |
| `walkforward_worker_fn` → WF P:3467-3472 | `&snap_hp` | wired ✓ |
| `Backtest_RunHyperparamTrainSweep` → WF E:2905-2909 | 10 args, no hp → nullptr | **BY DESIGN — holds** (varies via `cfg_override` per cell) |
| tests (`tests/controller_test.cpp` — 7 RFV cells, 4 HeldOut cells) | default nullptr | pins pre-override behavior; acceptable |

**No unwired production entry point remains for the seven `Apply`-carried params.** The surviving gap is on the **consumer** side (NEW-1), which AR-20's caller-enumeration proxy structurally cannot see — worth adding a consumer-side clause to the close-ritual candidate ("for a threaded parameter, also grep the callee body for every field of the parameter actually being consumed").

**The whitelist claim (P:2752-2766, the plan cites "~2765") — CHECKED, and it is ⚠ partially wrong at HEAD.** The six advertised XGB sweep keys (also in the Training-panel tooltip P:7231-7233): `xgb_subsample` / `xgb_colsample_bytree` / `xgb_min_child_weight` / `xgb_seed` propagate via the `if (!hp_override)` block E:1904-1908 ✓; `xgb_eval_nthread` is always read from `eff_cfg` E:1915-1916 ✓; **`xgb_train_nthread` is consumed NOWHERE in the WF path** — only by the stamp (E:1443) and by the sweep's own worker count, which reads the *base* cfg, not the cell (E:2878). So the plan's "no swept key is silently dropped" is false for one key: an `xgb_train_nthread` sweep produces N metric-identical cells with no diagnostic (and the block also propagates `tree_method` E:1909-1912, which the "exactly" omits). The nullptr-by-design note itself **holds**; the leaf-14 note's set-equality claim needs the correction. Related: NEW-7 below.

---

## 5. Class-13 dead-snap sub-shape remaining at HEAD (ask 4)

Field-by-field over every worker-args struct (population site vs consumer):

- `MultiHorizonWorkerArgs.snap_model_path` + `.snap_label_type` — populated at both clicks (P:6146-6153/P:6221, P:6355-6362/P:6363), captured (P:4924-4938), **read by nothing on the production path** (`(void)` casts P:5213/P:5217). = **S1-F9, still open.**
- `MultiHorizonParallelJob.snap_auto_stamp_enabled` / the worker's `snap_auto_stamp_enabled` — carried, then deliberately `(void)`-cast with a dated reason P:4584 ("kept in args for back-compat; not gating now" — v5.11.47 always-stamp). Documented tombstone, not a defect; candidate for deletion at the next struct touch.
- `WalkForwardWorkerArgs`, `FullValidationWorkerArgs`, `HyperparamSweepWorkerArgs`, `CollectMultiHorizonWorkerArgs` — every field has both a populate site and a pre-free consumer. **Clean.**
- The extreme of the sub-shape: `TrainModelWorkerArgs` + `train_model_worker_fn` (NEW-3) — the whole worker is unreferenced.

---

## 6. New findings (missed by the fix wave, or newly measured)

**NEW-1 — HIGH** — see § 3 (the S1-F11 candidate).

**NEW-2 — MED-HIGH — the shipped model trains on a different SAMPLE SET than the one validation measures (neutral-filter divergence).** The WF path pre-compacts binary labels to non-neutral (`label != 0.5`) E:1716-1728, E:1760; held-out filters identically E:2346-2352 (`filter_neutrals = LabelType_IsBinary`, plus NaN-drop for multiclass). The shipped-model trainer in `mh_run_one_horizon_fv` filters **only** NaN/Inf — P:4622-4633 — so every 0.5 label stays in the training set with `binary:logistic`. For BARRIER (~97% neutrals per E:1717) and VOL_BARRIER (real 0.5 mass from its not-enough-history/zero-vol returns, L:221/228/241), the deployed model is trained with a dominant 0.5 target mass pulling outputs toward 0.5 while WF/HO measure neutral-free models. The adjacent comment P:4612-4615 acknowledges only the row-range difference ("closely enough"), not this. Same measure-one-ship-another class as leaf 4, on the sample axis. WIN_LOSS and PVS are unaffected (no 0.5s / NaN-dropped everywhere). *Disposition:* mirror the `filter_neutrals` rule in the P:4622-4639 copy loop (3 lines), or promote the shared filter into a helper all three trainers call (the leaf-14 value-mapper's sibling).

**NEW-3 — MED-LOW — `train_model_worker_fn` (~470 lines) + `TrainModelWorkerArgs` are dead at HEAD.** Zero `pthread_create` references in any searched root (only spawns: P:564, 2849, 5138, 5580, 5686, 6229, 6440, 6914, 7301, 7479; E:3125 is the sweep thunk). Orphaned since v5.11.44 routed Train Model through the MH worker (P:6116-6120). It is compiled-in dead capital-adjacent code (H21 discipline: remove, don't leave), and it *diverges* from the live path (it filters `== 0.5f` P:3938 — the very rule NEW-2 says the live path lost; it also snapshots at worker entry rather than click). Its existence also keeps S1-F8's `model_trained` flag semantics alive. *Disposition:* delete worker + args struct + the stale cross-references (P:3004, P:6089); the deletion makes S1-F8's fix (re-gate P:6536 on `mh_complete` or render unconditionally-when-nonempty) unavoidable rather than optional.

**NEW-4 — LOW-MED — the HP sweep still snapshots `label_type` from the combo, not from the labels' producer.** P:7290 `hp_args->snap_label_type = state->label_type` vs the leaf-7 resolver-SSoT choice applied at P:7469 (FV) and P:6912 (WF). Cross-click reachable, no race: collect under PVS, flip the combo, Run Hyperparam Sweep → cells train a binary objective on 3-class-valued labels. Partially loud (XGBoost rejects out-of-range logistic targets → fold WARN, E-side `UpdateOneIter` logging), **silent when both kinds are binary** (WIN_LOSS↔BARRIER: barrier's 0.5s get neutral-filtered and the sweep ranks cells against mislabeled semantics). No stamp is written by the sweep, so blast radius = sweep-metric integrity. *Disposition:* one-line source change to `run_control->run_config.label_type`, same as leaf 7.

**NEW-5 — MED-HIGH — train-during-collect: the reverse direction of S1-F5 is open and UNHOMED.** Leaf 6 blocked collect-during-train (P:5538-5539, P:5624-5627) and homes Run Control's `can_run` (P:714-716, verified) — but no train-side gate checks `run_control->running`: P:6073 (`can_train`), P:6303, P:6884 (`can_wf`), P:7262-7265 (`can_hp`), P:7412-7415 (`can_fv`). `sample_count` grows incrementally during a collect (appended per slow-path cycle; `BacktestResults_EnsureCapacity` reallocs and **moves** `feature_matrix`/`labels`, E:424-446), so seconds into a collect the train buttons enable and a click puts a trainer's `memcpy` loop (P:4634-4636) and label writes (E:902-931) under a concurrently realloc'ing/moving buffer — the same latent UAF, opposite initiator. Additionally, any plain Run Backtest calls `BacktestResults_Reset` at entry (`Backtest/BacktestSharded.hpp:124`), memset-zeroing `sample_count` and `config_used` under a running trainer — data-pull rather than UAF, same matrix cell as the homed residual. *Disposition:* add `&& !run_control->running` to the five gates (5 one-liners) now; the homed Run-Control residual still needs the predicate plumbed into `GUI_Panel_RunControl` (it lacks `state`), which is the same edit the plan already owns.

**NEW-6 — MED — `mh_run_one_horizon_fv` selects its WF display/summary metric with the wrong `label_kind` discriminant.** `FullValidationResults.label_kind` is raw `num_classes` — "0=binary, 1=regression, ≥2=multiclass" (E:1098; set E:1685 from `LabelType_NumClasses`). Every other site tests `== 1` for regression (P:1704, P:1778, P:6779, P:6935, P:7505; E:2908) — but the MH path tests `== 2`: P:4702, P:4705 (status metric pick) and P:4767 (`// regression` in the summary writer). No `FOREACH_TARGET` row has `num_classes == 2` (L:82-97: values 0,0,1,4,0,0,0,3,1,1,1), so the `== 2` branch is unreachable and every **regression** horizon (FORWARD_PNL=2, CS_*) falls to the accuracy branch: status prints `WF=0.000` (WF leaves `mean_val_accuracy` at 0 for regression, E:2250) and `summary.txt` records `val_accuracy: 0.00` instead of `val_correlation`/`val_mse` — PastRuns then displays the summary value as truth. Likely mechanism: `LABEL_FORWARD_PNL == 2` (the label *type*) confused with kind, or `SamplesSnapshot`'s third, collapsed encoding (P:297 maps multiclass→2) — three different `label_kind` meanings coexist on this surface. `git log -L` dates the lines to `b74496f` (2026-05-07, v5.11.41.C) — a **missed** pre-existing bug adjacent to the f99e102 edit region, not newly introduced. The held-out side is coincidentally harmless (`he.metric` already holds correlation for regression, E:2499-2505). Current classification workflows (`twins` = PVS, kind 3) are unaffected. *Disposition:* flip both sites to `== 1` (matching E:1098) or better, route through `LabelType_IsRegression` (L:490-501 exists precisely to end this); leaf-13/14 adjacency.

**NEW-7 — LOW-MED — the sweep silently no-ops on an unrecognized key.** `ConfigField_Set` returns 0 for an unknown key (E:2565-2629); the sweep's `run_cell` discards the return (E:2896-2898). The Training-panel key field is free-typed (P:7241); a typo'd key → every cell trains the base cfg, all metrics identical, `best_idx = 0`, no diagnostic. Class 52 (swallow-and-coerce) + Class 51 (vacuously-comparable cells) at the sweep surface, and the concrete instance of the § 4 whitelist correction (`xgb_train_nthread` is a recognized-but-inert axis). *Disposition:* check the return at the two call sites, fail the sweep loudly pre-loop (validate both keys once before spawning cells).

---

## 7. Label-pass sweep (S3 rows at HEAD + the S3-F11 targeted look)

Verified against the digest's S3 rows (all still open unless noted): **S3-F3** NaN counters accumulate — `results->stats.nan_labels_total++` E:915-931 with no per-pass reset across the `P = 1+N+N·S` invocations ✓ open (the in-flight leaf-5 `LabelBatchTarget` carries per-target counters — in-flight, not assessed). **S3-F5** `Backtest/PhaseTimers.hpp:25,33,87` still claims "Backtest is single-threaded" while parallel MH jobs and sweep workers `+=` the global non-atomically ✓ open. **S3-F7** `Label_VolBarrier`'s 4th param is `vol_window` (L:213-217) and the label pass passes `extra = fwd` (the horizon) E:900-901 — a horizon sweep sweeps the vol lookback ✓ open. **S3-F8** `LabelFunctions.hpp` uses `NAN` (L:366+) with only `<stdint.h>` included (L:29) ✓ open. **S3-F9** the P:4502-4503 comment cites "BacktestEngine.hpp:904" for the leaf pick; at HEAD the pick loop is E:786-794 (E:904 is the `extra` selection) — still stale ✓ (moot once leaf 5 rewrites the call). **S3-F10** literal `8` state arrays (P:3133-3136, P:5029) vs `HORIZON_LIST_MAX` — latent-only, `HORIZON_LIST_MAX == 8` (`CoreFrameworks/ControllerConfig.hpp:1266`) ✓ open.

**S3-F11-recovered (candidate): a mid-walk abort leaves the PREVIOUS pass's labels in place, and the trainer trains on them silently.** `Backtest_ComputeLabelsFromSamples` returns `void`; on a sort refusal it frees and returns partway (E:828-833, E:845-849, E:875-879), and on an unopenable file at the count pass it `continue`s with `file_counts[f]=0` (E:751-755) — corrupting every later sample's file-offset mapping. Because the MH flow calls this function *per horizon over the same `labels[]` buffer*, an aborted pass leaves horizon *h* training on horizon *h−1*'s (or collect-time) labels — `n_valid` stays ≥ 50, nothing downstream notices, and the stamp records horizon *h*'s parameters over another horizon's labels. Evidence this is the lost row: the in-flight leaf-5 batch function's NaN-prefill contract names it verbatim ("on a mid-walk abort… unwritten slots read as invalid-label instead of… training on garbage/stale vectors") — a mitigation is being built for a finding no surviving digest row states. *Disposition at the pre-existing surface:* NaN-prefill `labels[]` at entry of the single-target path too (or propagate an rc) — cheap and independent of leaf 5's landing. *(Orchestrator note: DONE — f99ac-era wrapper now routes through the batch body, which NaN-prefills `results->labels` and returns an rc; landed in `f317c2d`.)*

Also noted (LOW, adjacent): unknown `label_type` falls back to `Label_WinLoss` silently E:794 (Class-52 shape; parse gates make it near-unreachable today).

---

## 8. Where the paired a-class should push (refute spots)

1. **NEW-1's severity** — is the 200-round hardcode *defensible* as a validation-cost bound rather than a bug? The refute needs to beat: the stamp/summary *say* `n_estimators=350` while the gating metrics measured 200 (an artifact-vs-truth lie, not a cost knob), and leaf 4's own contract text at P:4687-4690. Also check whether `XGBHyperparams_Apply`'s params make round-count comparisons second-order for hist trees (they don't at 200→350 with lr 0.05, but measure if contested).
2. **NEW-2's materiality** — quantify: with BARRIER's ~97% neutral mass, does keeping 0.5s actually move the shipped model's decision boundary, or does XGBoost's `scale_pos_weight` (P:4662 path applies no weights in the mh trainer — note it *also* skips the class-balance weighting the WF/HO/dead-worker paths apply, a second sub-divergence worth confirming) wash it out? My reading says the mh final trainer applies **neither** neutral-filtering **nor** `scale_pos_weight`/multiclass weights (P:4655-4663 sets only objective/num_class) — if that compound reading survives refutation, NEW-2 upgrades to HIGH.
3. **NEW-5's reachability** — confirm `sample_count` is written incrementally during a collect (I verified the append architecture via E:424-446 + the sorted-append comment E:713-715 and the Backtest CLAUDE.md; the a-class should pin the exact `on_slow_path` increment site in `CoreFrameworks/BacktestSharded.hpp` / driver).
4. **NEW-6's blast radius** — whether any operator-real flow trains regression horizons through the MH path today (the Label-Kind CSV makes it 3 keystrokes; but if regression is never used, MED → LOW).
5. **The S1-F11 identity claim** — both candidates (§ 3, § 7) are *content* re-derivations; if the orchestrator has any surviving fragment (terminal scrollback, `plan_checks/` notes) they should be checked against these before the ids are re-anchored in `owning_findings`.
6. **My "clean" verdicts** — the strongest refutation surface is § 2's LIVE-SHARED column: I judged the shared-`results` reads "stable absent NEW-5"; an a-class should hunt for a *fourth* initiator I did not model (e.g. the Optimizer panel's `optimizer_worker_fn` P:2849 — it runs full backtests over `state->results` of the *Optimizer* panel's own RunControlState; I verified it uses a separate state struct, but the sharing topology between panels deserves one adversarial pass).

**Verification-tool note:** the mechanical toolchain (conformance analyzer, size guard, calls_graph) targets the engine hot/sharded path; none covers this ImGui-TU surface (`SHARDED_FILES` excludes BacktestPanels), so this scan is grep+read-grounded against git-pinned extracts — `DOCS/CODE_MAP.md` regen was skipped deliberately because the working tree was mid-edit and the generator would have indexed drifted lines. Suite/build were not run (read-only scan; the working tree is mid-edit and not mine to build).
