---
type: handoff
status: active
ship_tag: .E.0.10
plan_type: live-readiness
sprint_end_goal: make the codebase more maintainable for future development (v5.X professionalization); live-readiness gated behind the whole E-series
ship_end_goal: CLOSE .E.0.10 — A6 capital guard (EGRESS LANDED, INGRESS next) + guard-matrix-no-HOLE + Phase-D (blanket live-gate boot-refuse) + ship close. #9 cohort RESOLVED (guard + reclaim-design, retirement homed). Gate-substrate direction RATIFIED (D-219).
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-14-E.0.10-net1-done-10-built-stepB-next-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
required_reading: [the register § "A6 PRE-CODING GATE" (the egress-LANDED + ingress-REMAINING design + the SHALT-vs-degrade OPERATOR DECISION) + § "GATE-SUBSTRATE ARCHITECTURE" (the 3-I→3-A cascade that reshaped A6/A13/#9); decision-log D-219 (the ratified gate-substrate direction + #9 retire-via-reclaim + the retirement design); TD-171 § "🔶 A6 PROGRESS" + TD-197 (#9 resolution, the reclaim-not-freeze RESOLVED bullet); RBP Class 48 (the prevented A13 zero-threshold→Momentum bug); the retirement-spec Rule 2 "Worked refinement" (NAME-is-the-identifier / non-persisted-BIT-reclaims); DOCS/LANDMINES.md Landmine 10 (the two-repo commit split — engine code engine-side, tests/ workspace-side)]
engine_head: 7d6a941 (A6 EGRESS chokepoint committed — Strategies/StrategyParameters.hpp + StrategyInterface.hpp ONLY; the INGRESS half is the next engine code, in CoreModelZoo.hpp + StrategyParameters.hpp)
workspace_head: at-or-after the close commit (the char-test + the full session capture + this handoff/MASTER refresh; all pushed to origin/main — self-referential close commits sit on top)
coding_status: mid-coding-checkpoint — A6 EGRESS landed (3612/0, hot-path CLEAN); INGRESS half is the next code, behind a fresh 3-I→3-A swarm
defers: none
deletion_scope: none
---

# Handoff — `.E.0.10` A6 EGRESS LANDED; NEXT = a fresh 3-I→3-A swarm (verify the egress, then build the ingress)

## TL;DR (the 60-second pickup)
- **A6 EGRESS chokepoint LANDED + COMMITTED (engine `7d6a941`).** `GateParameters_FinalizeEmit(out, strategy_halt_reason)` (`StrategyParameters.hpp:279`) **range-validates** `tp_pct`/`sl_pct` into `[0, 10000.0]` — catches NEGATIVE (SL-inversion) AND the NaN/+Inf→`+sat` a sign-only clamp would MISS (`+sat` confirmed `FixedPointN.hpp:1897`) → `GATE_FLAG_BUY_BLOCKED` + clamp + **`SHALT_BAD_PCT`** (new append-only `FOREACH_SHALT` row, set UNCONDITIONALLY = OVERRIDES first-wins so a benign earlier SHALT can't hide the corruption). ONE call in `Strategy_BuildParameters` post-switch (`:1786`, before the `tp_pct_b` derive) → **M7-STRUCTURAL: every dispatched strategy flows through it, a 7th auto-inherits** (the "foundation for the rest of E" shape; `.E.1` extends these emit sites and inherits the guard free). Verify: suite **3612/0** (+12 non-vacuous char-tests: negative / NaN-laundered-+sat / valid-passthrough / first-wins-override) + **hot-path CLEAN** (`calls_graph_diff`). The whole A6 design was reshaped by a 2-I→2-A pre-coding gate (register § "A6 PRE-CODING GATE").
- **🔴 NEXT ACTION (operator-directed): re-run a FRESH 3-I→3-A swarm** — (a) **VERIFY the egress first-half landed clean** (re-establish context after a marathon session; confirm `7d6a941` is sound + no regression), THEN (b) **design + build the A6 INGRESS half** (see "The A6 INGRESS design" below).
- **#9 cohort RESOLVED** — the cfg-flag-orphan CLASS is **structurally closed** by the standing `scan_class_44_cfg_orphan.py --strict` guard (`check_session_docs.sh` HARD 10). Per-flag DESIGN resolved (operator-corrected: tombstone the **NAME**, RECLAIM the non-persisted **BIT in place** — NOT freeze, NOT widen; codified retirement-spec Rule 2 + memory). Mechanical retirement HOMED to TD-197 (batches with the legacy-`PortfolioController` deletion, TD-002). Task #3 closed.
- **Gate-substrate direction RATIFIED (D-219)** — consolidate within the EXISTING `GateParameters` substrate (NOT a new `FOREACH_GATE` registry = over-engineering; hot-swap is seqlock-provided, NOT a justification; **taker-output-shape ONLY** — maker/microstructure is a future-`.E` frontier, captured). A13's naive port (publish `bg_price_threshold=0`) was a live **Momentum-fires-every-tick** bug → RBP **Class 48**.

## What landed this session (the full arc — all captured; doc-CI SWEEP CLEAN; independent close-review 10/10 VERIFIED)
| deliverable | home | engine? |
|---|---|---|
| **A6 EGRESS chokepoint** — `GateParameters_FinalizeEmit` + `SHALT_BAD_PCT` + char-test | engine `7d6a941` (Strategies/) + `tests/controller_test.cpp` | ✅ committed |
| **Gate-substrate 3-I→3-A cascade** (substrate-exists / hot-swap-false / no-new-registry / taker-only) | register § "GATE-SUBSTRATE ARCHITECTURE" + **D-219** | — |
| **A6 2-I→2-A pre-coding gate** (range-not-sign / override-first-wins / NaN-+sat / silent-swap) + the SHALT-vs-degrade OPERATOR DECISION | register § "A6 PRE-CODING GATE" + TD-171 | — |
| **RBP Class 48** (control-intent-as-sentinel-value; the prevented A13 bug) | `DOCS/recurring-bug-patterns/class-48-*.md` + RBP index | — |
| **Retirement reclaim-not-freeze refinement** (NAME-is-identifier / non-persisted-BIT-reclaims) | `dead-code-and-identifier-retirement-discipline.md` Rule 2 + memory `feedback_tombstone_the_name_reclaim_the_nonpersisted_bit` | — |
| **`feedback_dont_generalize_substrate_before_input_space_known`** | MEMORY.md (Tier-1) | — |
| **Orphan-checker → STANDING-CI** | `check_session_docs.sh` HARD 10 + `DOCS/TOOLS.md` | — |
| **2 `_future` docs** (book-aware fill model + microstructure alpha · warm-start history-replay state-priming) | `plans/_future/2026-06-14-*` | — |
| **#9 dispositions + retirement design** | TD-197 (RESOLVED) + D-219 | — |

## 🔴 NEXT: the 3-I→3-A swarm (verify egress clean → design+build the ingress)
Operator-directed: **re-run a fresh 3-I→3-A** (the methodology that's been earning its keep all session — the A-class caught 3 over-claims, each orchestrator-code-read confirmed).
- **First (verify):** confirm the egress landed clean — `git -C /home/caramel/code/FoxML_Trader_v2 show --stat 7d6a941` (= 2 files), `./build.sh test` (3612/0), `tools/calls_graph_diff.sh verify` (hot-path CLEAN), and re-read `GateParameters_FinalizeEmit` (`StrategyParameters.hpp:279`) + the call site (`:1786`) against the design in the register § "A6 PRE-CODING GATE". Re-establish the session arc from the register + D-219.
- **Then (design+build the ingress):** the I-class designs the ingress mechanics, the A-class refutes (NaN/Inf reachability, per-arm vs whole-model granularity, the corrupt-vs-missing branch, the SHALT-the-node + retrain-warn surfacing). Build it (engine + char-test + build-verify), per the heavier posture.

## The A6 INGRESS design (the remaining capital-path work — operator-DECIDED, gate-validated)
Two sub-parts; ground each at code (the gate already traced them):
1. **Refuse a corrupt barrier AT LOAD** — at `CoreModelZoo_TryLoadRole` (`ML_Headers/CoreModelZoo.hpp:346-347`, the ONLY on-disk `label_*_pct` entry, grep-verified): detect `!isfinite(sr.label_tp_pct) || !isfinite(sr.label_sl_pct) || <0` → REFUSE the load (`Model_Free`+`Model_Init`+`return 0` + a non-throttled WARN), mirroring the EXISTING always-refuse horizon-mismatch precedent (`:447-457`; NOT strict-gated — `held_out_gate_strict` defaults 0). This is **per-arm-drop**: drops a poisoned ensemble arm, refuses a single-zoo model. **`< 0` alone MISSES NaN/+Inf** (they round-trip the stamp via `%.6g`/`parse_double_fast` → poison the WHOLE blend; one NaN arm corrupts `blend_tp_d`/`blend_sl_d` at `StrategyParameters.hpp:1155-1158`, which has NO isnan guard) → use `!isfinite || <0`. **Add a blend isnan-guard** at `:1155-1158` as the in-engine backstop (sister to the `pred_raw` guard at `:1225`).
2. **SHALT-the-node + retrain-warning (the OPERATOR DECISION, 2026-06-14):** on a CORRUPT model the core REFUSES to trade + surfaces a STICKY "model corrupt — RETRAIN" alert (NOT the existing 60s-throttled log). This is **distinct from the existing MISSING-model → SimpleDip degrade** (`StrategyParameters.hpp:794-796`) — a corrupt artifact is more alarming than an absent one, and the existing `MODEL_LOAD_FAILED` → silent SimpleDip swap is the wrong response. → a **NEW CoreState `MODEL_CORRUPT` flag** (distinct from `MASK_CORE_STATE_MODEL_LOAD_FAILED`), branch the fallback at `:794-796`: corrupt → SHALT-the-node (gate-zero entries) + the retrain alert; missing → the existing SimpleDip degrade. (SHALT-the-node = the strategy emits `BUY_BLOCKED` every cycle for that core; reuses the egress veto path. Add a `FOREACH_FAILURE_MODE` bit for GUI/health forensics.)

**Why the EGRESS alone isn't enough (the ingress is load-bearing):** the egress clamp silently floors a corrupt value at emit-time → would let a corrupt/tampered model LOAD and TRADE (with neutered barriers) while emitting no operator signal. The ingress is the layer that DETECTS + REFUSES + surfaces the corruption (the register decided two-layer: `:333`).

## Also remaining for the `.E.0.10` close (after A6)
- **A6 ship-close** (version bump + char-tests + asan/ubsan + commit/tag).
- **guard-matrix-no-HOLE** (Phase-C step D — the foundation-solid STOP-condition: every invariant `.E.1` touches is an enforced row; now measures the COMPLETE guard set incl. the A6 chokepoint).
- **🔴 Phase-D — the blanket live-gate boot-refuse (DECIDED, just unimplemented; D-77/F-2 + D-168):** add a REFUSE in `LiveReadiness_Verify` (8 existing checks, NONE on the torn-read class / `.E`-incomplete) via the NEW-1 `ControllerConfig_IsLiveCapital` predicate; tombstone-removed at `.E`/v5.16 (H21). Then `.E.0.10` ship close (version bump `.E.0.9`→`.E.0.10` + tag + postmortem + push).
- (off-`.E.0.10`: the cfg-flag retirements [TD-197, batch with TD-002], the gate-substrate consolidation [D-219, future-`.E`], the warmup-replay state-priming [`_future`, future-`.E`], the maker/book-aware frontier [`_future`], the A5 fill-side guard [TD-171, `.E.1` adapter].)

## TaskList state at handoff write (recreate verbatim via TaskCreate)
| # | status | subject |
|---|---|---|
| 1 | completed | Net-1 characterization net — 6 chars, suite 3600/0 |
| 2 | completed | Phase-C step A — #10 Class-44 cfg-flag-orphan detector BUILT + wired |
| 3 | completed | Phase-C step B (#9) — RESOLVED (class guarded, reclaim-design codified, retirement homed TD-197/TD-002) |
| 4 | **in_progress** | Phase-C step C — A6 chokepoint guard (TECH_DEBT-171): EGRESS landed (`7d6a941`); INGRESS next (fresh 3-I→3-A) |
| 5 | pending | Phase-C step D — guard-matrix-no-HOLE check |
| 6 | pending | Phase-D — blanket live-gate boot-refuse (LiveReadiness_Verify via NEW-1 predicate) + `.E.0.10` ship close |

## Arming for the next action (the A6 INGRESS — the surface is the ML model-load + the strategy fallback)
- **Route to:** `/hft-audit` (the gate-emit/slow-path domain) + `/ml-audit` (the model-load/stamp path) + `/decision-check` IF any sub-shape needs a call (the SHALT-vs-degrade is already DECIDED). Run the 3-I→3-A swarm (M8-armed; arm each agent with the register § "A6 PRE-CODING GATE" + D-219 + the file:line anchors; keep the A-class independent on the verdict).
- **Run (not just read):** `./build.sh test` (after any engine edit) · `tools/run_all_tests.sh --full` (asan/ubsan at ship-close) · `tools/calls_graph_diff.sh verify` (hot-path-untouched) · `tools/check_session_docs.sh` · regen `./tools/gen_code_map.sh`.
- **Read first:** `DOCS/STRATEGY_AND_CODING_RULES.md` (hot/slow/ML invariants) + `DOCS/CLAUDE_ML_INVARIANTS.md` (the model-load/stamp surface). Invariants in play: **H4** (Money; the label_*_pct check is on the raw stamp double, pre-Money — validation-only) · **H9** (stamp wire-format read-only) · **H21** (the new `MODEL_CORRUPT` CoreState flag + `SHALT_BAD_PCT` are append-only) · **H7/H8** (the ingress is boot/load + slow-path; hot path UNTOUCHED).
- **LANDMINE 10:** the engine code (CoreModelZoo + StrategyParameters) commits ENGINE-side; the char-test (`tests/`) commits WORKSPACE-side — cross-reference the SHAs. `git add tests/` from the engine FAILS ("beyond a symbolic link").

## Git state at handoff
- **Engine: `7d6a941`** (`feat/v5.15-live-readiness`) — the A6 EGRESS chokepoint (2 files: StrategyParameters.hpp + StrategyInterface.hpp). Clean tree. Pre-commit hooks GREEN (Check L P&L-SSoT / Check H identifier-retirement / Check K struct-layout).
- **Workspace: synced this close** (`main`) — the char-test + the full session capture (register, D-219, TD-171/197, Class 48, the retirement-spec, the 2 _future docs, check_session_docs HARD 10, TOOLS.md) + this handoff + MASTER refresh. All pushed.
- **Suite: 3612/0.** doc-CI SWEEP CLEAN. Independent close-review 10/10 VERIFIED + COHERENT.

## Critical pickup-time reads (load these first)
1. **register** `plan_checks/E.0.10-finding-disposition-register.md` § "A6 PRE-CODING GATE" (the egress-landed + ingress design + the SHALT-vs-degrade decision) + § "GATE-SUBSTRATE ARCHITECTURE" (the cascade).
2. **decision-log** `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` **D-219** (the ratified direction).
3. **TD-171** § "🔶 A6 PROGRESS" + **TD-197** (the #9 resolution) in `DOCS/tech-debt/open.md`.
4. **RBP Class 48** `DOCS/recurring-bug-patterns/class-48-sentinel-value-control-signal-inverts-by-direction.md` + the retirement-spec `DESIGN_SPECS/meta-disciplines/dead-code-and-identifier-retirement-discipline.md` Rule 2.
5. **DOCS/LANDMINES.md** Landmine 10 (two-repo commit split).
6. The CODE: `StrategyParameters.hpp:279` (`GateParameters_FinalizeEmit`) + `:1786` (call site) + `:794-796` (the fallback to branch) + `:1155-1158` (the blend to isnan-guard); `CoreModelZoo.hpp:346-347` (the ingress) + `:447-457` (the refuse precedent); `StrategyInterface.hpp:264` (`SHALT_BAD_PCT`).

**Pickup:** `/accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-14-E.0.10-A6-egress-landed-ingress-next-handoff.md` (one-command arm + verify).
