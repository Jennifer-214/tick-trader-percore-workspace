---
type: handoff
status: active
ship_tag: .E.0.10
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-12-E.0.10-A25-decided-A25-impl-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
required_reading: [register (§Routing map + A1–A31 + the /decision-check verdict §), decision-log D-204..D-210, per-node-position-ownership-model.md, definition-of-done-and-armed-scout-verification.md (M8), PARITY-040, DOCS/STRATEGY_AND_CODING_RULES.md, cfg-scope-discipline.md, CLAUDE.md H22, CODE_MAP (regen at pickup)]
engine_head: 8352b5e (A25+A28 + the deep-check fixes F1/F3/F7; on feat/v5.15-live-readiness; clean)
workspace_head: at or after the .E.0.10 deep-check close-out sync (this handoff + the M8 codification + PARITY-040 + D-210 refresh it — verify "at or after")
coding_status: fix-ship in flight — A9/A19/A1/A3/A8/D-190/A25/A28 + the F1/F3/F7 deep-check fixes LANDED; A24 is the next CODE step (fix-approach decision pending /decision-check)
deletion_scope: none
---

# .E.0.10 handoff — A25+A28 + deep-check (F1) landed, A24 next (2026-06-12 close)

> **Why this handoff:** A25 (per-fill `original_tp` SSoT) + A28 (Position reset) landed (`0e1f6d2`). An operator-prompted **armed deep-check** on the *already-closed* ship then caught **F1** — a real AUTO-core capital bug — plus close-out drift; all fixed + committed (`8352b5e`, suite **3386/0**, asan/ubsan **3246/0**). The terminating discipline (**M8** — Definition-of-Done + armed scout-first subagents) is codified so this doesn't recur. The next CODE step is still **A24**. Nothing is lost.

## State AT CLOSE (close-session verified — 2026-06-12)
- Engine HEAD `8352b5e` (A25+A28 + F1/F3/F7), branch `feat/v5.15-live-readiness`, working tree clean. Suite **3386/0**; **asan/ubsan 3246/0** (`run_all_tests.sh --full`). Version.hpp pinned at `5.15.5.F.4d.1.E.0.9` (NOT bumped — version-scheme rework deferred).
- Workspace HEAD **at or after** the close-session sync (this handoff + M8 spec + PARITY-040 + D-210 + § 11.5 registry + the `/decision-check` Stage 2.5 M8-arming). Predecessor workspace commits `ff6a64b` (the codification + fixes) + `e64176c` (D-210 STATUS→COMPLETE).
- **`/close-session` ran in full at this close:** mechanical sweep `check_session_docs.sh` **SWEEP CLEAN** (all HARD); the **independent deliverable-completeness review returned CLEAN** — all 16 session deliverables VERIFIED on disk (substantive, coherent, fully propagated to every home, surgical/no clobbered neighbors, no fabrications, privacy boundary intact; the F2-false claim independently re-grounded against the shared-producer code). Meta-harvest: quiet (mature apparatus — the M8 lesson became a first-class discipline; no new catalog rows; one watch-item = registry-ID earmark collision, catalog if it recurs).
- 1 advisory only: 5 KNOWN unmarked capital-test `// ADV-REFUTE` blocks (pre-existing backlog at TECH_DEBT-164; the F1/A28 tests are outside the tool's balance/realized/fees scope).

## TaskList state at handoff write (recreate verbatim)
| ID | Status | Subject |
|---|---|---|
| #1 | completed | A9 — slippage at fill chokepoint |
| #2 | completed | A25 #5 decision (= option b) |
| #3 | completed | A25 — per-fill original_tp SSoT (+A28; A18 reverted) |
| #4 | pending (NEXT) | A24 — cfg-mutation single-source (H22) |
| #5 | pending | A16 — partial-fill state-gate |
| #6 | pending | F-059 — exit golden-master (AFTER A24/A25) |
| #7 | pending | A6 — chokepoint gate-emit validity guard |
| #8 | completed | TD-186 harvest (spec v1.3 + RBP Class 45) |
| #9 | pending | H22 spec (per-node-purity-scale-invariance.md, at A24 close) |
| #10 | completed | F1 (HIGH) — AUTO trail-anchor (resolved_strategy_id) |
| #11 | completed | A28 char-test (H1) |
| #12 | completed | register + MASTER flipped to LANDED (H2/H3) |
| #13 | completed | #18 torn-read enrollment (H4) |
| #14 | completed | F2 — VERIFIED FALSE (backtest shares the live producer) |
| #15 | completed | doc cluster — PARITY-040 / Class-44 / spec-metadata / D-208 promises |

## Deep-check close (this session — the M8 dogfound it)
The armed deep-check (2 scout-first agents: code-correctness + discipline, mechanical-tool-armed) on the already-"closed" A25/A28 ship found + this session fixed:
- **F1 (HIGH, capital — engine `8352b5e`):** the A25 producer resolved per-fill `tp_pct` from the CONFIGURED `strategy_id` (AUTO → `ResolvePerFillTpPct` default → global `take_profit_pct`) not `resolved_strategy_id` → an AUTO core with a per-strategy override armed `original_tp` (trail anchor) ≠ `live_tp` (exit) — the exact A25 bug, reintroduced for AUTO, AND inconsistent with A1's resolved restore. The char-test missed it (hand-builds `cmd.tp_pct`, bypassing the producer). Fixed → `state.cores[slot].resolved_strategy_id` + an AUTO regression test.
- **F3 (LOW, `8352b5e`):** `Portfolio_AddPosition` (legacy) routed through the `Position_Reset` SSoT (was subset-zeroing 5 of 9 fields; A19/A28 class).
- **F7 (LOW, `8352b5e`):** corrected the exit-bandit counterfactual comment (`original_tp` is the FILL-priced anchor post-A25, not the "intended TP target").
- **F2 — VERIFIED FALSE (code-read):** "backtest never sets `cmd.tp_pct`" was refuted — the sharded backtest shares the live `Async.hpp:909` producer + `handle_buy_fill` fill-priced path (`event_log_mode==1` "same fill+drain" per `BacktestSharded.hpp:162-189`, for train-serve parity) → backtest `original_tp` is fill-priced IDENTICAL to live. Recorded inside **PARITY-040**.
- **PARITY-040 (NEW):** the real divergence is #5 (event-log replay reconstructs `original_tp` from `e.tp` expected-priced ≠ live fill-priced) — LOW-MED, secondary replay path only; decided D-208 option (b) = document (this entry) + F-059 freeze-flag; structural fix rides the TECH_DEBT-126 version rework.
- **Trackers/docs:** A28 char-test added (H1); register + MASTER + routing-map flipped to LANDED (H2/H3); `Position::{original_tp,original_sl}` enrolled in A22's torn-read site-list (H4); Class-44 count reconciled `2`→`4` (M3); spec-metadata fixed (M4).
- **M8 codified** (the loop terminator): `meta-disciplines/definition-of-done-and-armed-scout-verification.md` + memory `feedback_define_done_and_arm_scout_subagents` + D-210 + DESIGN_PHILOSOPHY § 11.5 row + `/decision-check` Stage 2.5 arming + the adversarial-methodology cross-ref. **Mechanical backstop candidate: `check_close_out_completeness.py` (homed, not built).**

## THE NEXT ACTION — A24 (cfg-mutation single-source, H22; HIGH)

**What A24 fixes (register A24):** `RebuildOneCore` mutates `volume_multiplier`/`entry_offset_pct`/`spacing_multiplier` on the FLAT `resolved_cfg`, but every production consumer reads the boot-frozen per-core shadow `cores[slot]` or strategy-owned `live_*` → the mutations NEVER reach a live consumer. Session vol-mult, the **D10 P&L-regression filter-tighten safety loop (default-ON)**, spike relaxation — all silently inert. Regression from the per-core migration (`49649b8`). The literal H22 CI-check candidate.
> **Line-ref note (re-grounded at close 2026-06-12):** the register/A24 line numbers (`ControllerEventLoop.hpp:2360/2426/2978` mutate · `:2700/:2998` read) have **drifted ~20 lines** — the real mutation sites are now ~`:2337/:2403/:2410/:2960`. **Anchor on the SYMBOLS, not the line numbers** — grep `resolved_cfg.{volume_multiplier,entry_offset_pct,spacing_multiplier}\s*=` for the mutations and the `cores[slot]` / `live_*` reads for the consumers, and regen CODE_MAP at pickup (per nav-infra discipline). The finding itself is unchanged + real.

**FIX-APPROACH DECISION — run `/decision-check` FIRST** (the binding adversarial-default + the operator's "run investigative/adversarial checks"):
- (a) mutate `resolved_cfg.cores[slot]` directly (single-source the per-core view), OR
- (b) sync the mutated override-eligible fields to `cores[slot]` before the consumer.
The register leans (a). **Run `/decision-check` ROUTING the agents to the surface-matched skills + ARMED per M8** (both landed this session): A24 = cfg-flow/H22 → `/trace-deps` + `/dod-audit` + `cfg-scope-discipline.md` (+ Class 25/26/27), with the agents handed the reference docs + the mechanical toolchain (`check_per_core_registry_integrity.py`) + CODE_MAP + scout-first. Consult Caramel before coding.

**Co-deliverable — the H22 spec (task #9):** author `DESIGN_SPECS/data-disciplines/per-node-purity-scale-invariance.md` — CLAUDE.md's **H22 row DANGLING-REFERENCES it** (the file does not exist; D-193 task #21 owed; the ownership-spec's body cross-ref + register also point at it). A24 is its canonical worked example. Author at A24 close, then **re-add it to `per-node-position-ownership-model.md`'s `sister_specs:`** (dropped this session to clear the broken ref — M4).

**Close per the M8 DoD** (now binding): code+producer-verified · 3-lens char-test per fix (`characterization-test-discipline.md`) + `// ADV-REFUTE` · `run_all_tests.sh --full` · trackers flipped · PARITY entry for any divergence · promises honored · obligations homed · docs indexed · meta codified · `check_session_docs.sh` SWEEP CLEAN. Verify with an ARMED scout-first independent adversarial pass (resolve agent code-fact disagreements by READING the code — AR-11).

## Then the rest of the fix-ship (in order)
A16 (partial-fill state-gate — gate HandleFill + slot-free on `ORDER_FILLED` not `event_log_mode`; fold A17) → F-059 (exit-chain golden-master, AFTER A24/A25; freeze-flags the #5 replay divergence per PARITY-040 + A9/A10) → A6 (chokepoint gate-emit tp/sl-validity guard). Each = fix + 3-lens char-test + `// ADV-REFUTE` + run_all_tests + surface-matched ARMED adversarial verify (M8).

## GOING-FORWARD REFINEMENTS (landed this session — APPLY them)
1. **`/decision-check` Stage 2.5 — surface-matched skill routing** (MANDATORY): route the investigative + adversarial agents to the DOMAIN skill matching the decision's MATERIAL (money→`/accounting-audit`; hot-path→`/hft-audit`; cfg/registry→`/trace-deps`+`/dod-audit`+`cfg-scope-discipline`; ML→`/ml-audit`; train-serve→`/parity-check`).
2. **M8 — armed scout-first subagents + the Definition-of-Done contract** (`definition-of-done-and-armed-scout-verification.md`): every verify subagent boots BLIND → arm its prompt with reference-docs + the mechanical toolchain to RUN + nav-infra + the domain skill, and tell it to SCOUT before executing; adversarial-independence on the VERDICT only. A close TERMINATES against the enumerated DoD in ONE armed pass — not trickle-discovery. Apply both firing A24's checks + every subsequent fix's close.

## Critical pickup-time reads (nav-infra, first-class)
- The disposition **register** — A1–A31 routing map + the **A24 row** + the `/decision-check` verdict § + the deep-check A16+ / F1-H4 rows.
- **decision-log D-204..D-210** (A25 carry, #5, the front-load verdict, the position-ownership design + A18 revert, **D-210 = the deep-check + F1 + M8**).
- **`per-node-position-ownership-model.md`** (the venue-net/`.E.1` design) + **`definition-of-done-and-armed-scout-verification.md`** (M8 — how to close a fix completely) + **PARITY-040** (the #5 replay divergence + the F2-false record).
- **CODE_MAP** — regen `./tools/gen_code_map.sh` at pickup.
- Path-discipline: `DOCS/STRATEGY_AND_CODING_RULES.md` + `cfg-scope-discipline.md` (A24 touches per-core cfg) + CLAUDE.md H22.

## Paste this into a fresh session

```
I'm picking up .E.0.10 (the net-1 correctness fix-ship) for v5.15-live-readiness.

Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-12-E.0.10-A25-A28-landed-A24-next-handoff.md

This is a fresh context — do NOT trust prior-session memory; verify against current code.

After PICKUP-READY: the next fix is A24 (cfg-mutation single-source, H22 — RebuildOneCore mutates
the flat resolved_cfg but consumers read the boot-frozen cores[slot]; the default-ON D10 safety loop
is dead). Run /decision-check on the fix-approach (mutate cores[slot] directly vs sync-to-cores[slot]),
ROUTING the agents to the surface-matched skills (/trace-deps + /dod-audit + cfg-scope-discipline per
Stage 2.5) and ARMING them per M8 (reference docs + mechanical toolchain + CODE_MAP + scout-first).
Consult Caramel on findings before coding. Co-deliver the H22 spec per-node-purity-scale-invariance.md
(CLAUDE.md H22 dangling-references it) + re-add it to the ownership spec's sister_specs. Close per the
M8 Definition-of-Done. Then A16 -> F-059 -> A6.
```

## Quick links
- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- Decision log: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-204..**D-210**)
- Register: `plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md`
- Position-model design: `DESIGN_SPECS/data-disciplines/per-node-position-ownership-model.md`
- **M8 close-out discipline: `DESIGN_SPECS/meta-disciplines/definition-of-done-and-armed-scout-verification.md`**
- **PARITY-040** (the #5 replay divergence + F2-false): `DOCS/PARITY_ISSUES.md`
- Predecessor handoff: `plans/v5.15-live-readiness/handoffs/2026-06-12-E.0.10-A25-decided-A25-impl-handoff.md` (superseded)
```
