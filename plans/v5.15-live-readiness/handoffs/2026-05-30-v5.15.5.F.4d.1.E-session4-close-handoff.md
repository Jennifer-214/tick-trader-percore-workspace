---
type: handoff
template: _TEMPLATE-e-ship-planning-handoff.md (7-section + SEAM per D-72)
ship_tag: NEXT = the numeric-foundation unification ship (decimal money + unified FixedPoint<RADIX,FRAC>) — gated behind .E.0.1 (the net)
predecessor: 2026-05-30-v5.15.5.F.4d.1.E.0.1-session3-close-handoff.md (picked up this session; D-97 was its headline-next, now DECIDED)
successor: v5.15.5.F.4d.1.E.1 (Foundation Core→Node rename — gated behind .E.0.1 + the numeric-foundation ship + Net-1)
engine_head: 6fd09af (Version still 5.15.5.F.4d.1.E.0.5 — NO engine SHIP this session; 2 META/tooling commits: bdaa289 CI tools + 6fd09af pre-commit Check E)
workspace_head: 4202101 (+ this handoff's push)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (D-90→D-115 — this session added D-99→D-115)
foundation_doc: plans/v5.15-live-readiness/subplans/2026-05-30-v5.15.5.F.4d.1.E-money-numeric-core-foundation.md (the D-97 deliverable + the unification ship's plan body)
rollback_anchor: v5.15.5.F.4d.1.E.0.5 @ 0b841b3 (GPG-signed; last shipped)
---

# Session-4 close handoff — HEADLINE: D-97 DECIDED (decimal money + unified `FixedPoint<RADIX,FRAC>`); NEXT = the numeric-foundation unification ship

**Created:** 2026-05-30 (`/close-session`). **Branch:** `feat/v5.15-live-readiness`. **Pickup:** `/accept-handoff <this doc>`.

This session picked up the Session-3 handoff (whose headline-next was "draft the D-97 decimal-vs-binary decision doc"), **TOOK that decision (decimal)**, deepened it into a unified-radix-core architecture, wrote + audit-verified the foundation doc, and — on operator direction — **completed the D-112 skill↔CI wiring** + hardened the doc-CI net. **No engine code shipped** (META/tooling + planning only). Independent Stage-5.5 review = **COMPLETE** (all 15 deliverables VERIFIED on disk).

## 1. Headline next + end goal — the numeric-foundation unification ship
**The D-97 decision is MADE: money = DECIMAL fixed-point**, implemented as **ONE unified `FixedPoint<RADIX,FRAC>`** (binary `<2,64>` = features/ML/stats; decimal `<10,8>` = money), which **absorbs the parallel `FixedPoint64.hpp`** and **reuses the `.E.0.1`-certified binary op bodies** (rewrite the FRAME + write decimal fresh; don't re-debug the certified bodies). The foundation doc (`subplans/2026-05-30-...E-money-numeric-core-foundation.md`) is the decision-record AND the unification ship's plan body — B-Plus EXIT 0, all open items settled. **End goal:** close the binary-money-imprecision + parallel-numeric-type + silent-domain-cast + absent-quantization classes via the unified core, so `.E.1` lands on the FINAL numeric shape (core touched once). **CONSULT before coding** (money core; HIGH-RISK; its own `/precoding-audit-gate`).

## 2. Git state (pickup verify)
Engine **`6fd09af`** (== origin; Version still `5.15.5.F.4d.1.E.0.5` — NO ship; 2 tooling commits this session). Workspace **`4202101`** (== origin + this handoff push). Branch `feat/v5.15-live-readiness`. Both repos in-sync with origin. (Engine working tree has 18 pre-existing untracked `DOCS/*.md` — the `.E.2` doc-suite landing out-of-band; NOT this session, not blocking.)

## 3. The core decisions this session (decision-log D-99→D-115 authoritative)
- **D-97 (taken): money = DECIMAL** — exact-by-construction + source-exact; binary's `~1e-19` is a magnitude argument that doesn't answer exactness/source-fidelity.
- **D-99: unified `FixedPoint<RADIX,FRAC>`** — NOT a parallel `DecimalFPN` (Class-21/SSoT trap; 40+ ops radix-agnostic); absorbs FixedPoint64; reuse-certified-bodies (NOT from-scratch).
- **D-100: golden-EPOCH invariant + determinism≠correctness** — within an epoch byte-repro is absolute; binary→decimal is a deliberate epoch transition (regen golden + retrain); a wrong-but-deterministic impl reproduces forever, so the regen boundary needs a ONE-TIME correctness gate (decimal-exactness differential + hand-computed-fee reference + round-trip).
- **D-101: sequencing** — numeric core reaches FINAL shape BEFORE the `.E.1` rename: D-97 decision → `.E.0.1` certifies CURRENT binary core (the NET) → numeric-foundation ship UNDER the net → `.E.1` rename.
- **D-102: producer double-roundtrip** (latent precision bug; `Run.hpp:1374`→`Async.hpp:179-180` re-derives FPN from a double) → TECH_DEBT-149, homed to the ship.
- **D-103: the boundary is a PAIR of type-invisible casts** (feature↔money, all FPN-vs-FPN today → compile silently) → **O-1 strong-type the boundary** so the compiler enumerates them.
- **D-104: single global `10^8` scale** (Binance ≤8dp; exact for every symbol) + decimal fills the missing tick/lot quantization.
- **D-105: ONE canonical rounding mode applied uniformly incl. replay** — the one hard determinism requirement.
- **D-106: venue-as-SSoT** ("let the venue decide") — defer numeric semantics to the exchange; per-venue in `FOREACH_EXCHANGE`; `static_assert(storage≥venue_precision)`. NEW memory.
- **D-107/108/109 (open items SETTLED):** O-1 = YES strong-type the boundary; O-2 = the ship SUBSUMES `.E.0.3`; R-1 = Binance fee = venue-reported (LIVE books `executionReport` commission; paper/backtest round-up-at-precision).
- **D-110: completeness sweep** found the forgotten `ShardedSnapshotPersist.hpp` recovery surface (raw-fwrite money fields + the `Run.hpp:653` usdt_recovered double→FPN boundary) → in the ship's blast radius. (Includes a self-correction: my first D-110 write fabricated the file/HMAC specifics; corrected from disk.)
- **D-111/112/113/114/115: the CI/skill infrastructure** (see §6).

## 4. Cross-ship invariants this must preserve
H1-H20. The numeric core change is major-version-scale (FixedPointN core + accounting + stamp wire-format + snapshot/recovery + tests). It runs UNDER the `.E.0.1` net (binary instantiation byte-identical to the locked golden = red build; money paths regen goldens w/ the D-100 one-time correctness gate). Hot path UNTOUCHED at per-tick cost (500ns path does money COMPARES, not mul/div — audit-confirmed). One-time stamp/model retrain accepted (backwards-compat not a concern pre-adoption).

## 5. Required reading (pickup)
Decision-log **D-97→D-115** (esp. D-99/D-100/D-101 — the architecture, golden-epoch, sequencing) + the **foundation doc** (`subplans/2026-05-30-...E-money-numeric-core-foundation.md` — the ship's plan body; § Blast radius, § Open decisions [all settled], § Acceptance) + the **`.E.0.1` v0.3 plan** (the net that gates the ship; its D-97 flags) + the **`.E.0.3` STUB** (SUPERSEDED-banner — folded into the unification ship per D-108). Baseline CLAUDE.md / CLAUDE.local.md / MEMORY.md auto-load. NEW memories: `feedback_defer_to_source_authority_for_external_semantics` (D-106) + `feedback_run_doc_ci_tools_first_never_hand_verify` (the session's meta-lesson).

## 6. The CI/skill infrastructure landed this session (D-111→D-115; the "avoid errors going forward" work)
- **`tools/check_session_docs.sh`** (NEW) — one-command doc/plan CI aggregator (bidirectional+index memories + B-Plus + capture-audit-mechanical + forward-promise + meta-registry). The single answer to "are the docs clean?".
- **`tools/check_capture_audit.py`** (NEW) — mechanizes capture-audit Checks 1/4/8 (index-sync / decision-sentinels / skill→CLAUDE.md linkage); negative-tested (catches orphans).
- **All 6 doc/audit skills wired** to the aggregator (readiness Step-0.5 / handoff Stage 1.8 / precoding-audit-gate Stage 2.5 / accept-handoff Stage 5 / capture-audit / close-session). Mechanical→tool; judgment→agent.
- **pre-commit hook Check E** (NEW) — fires bidirectional-memories + capture-audit-mechanical on memory/skill/doc commits (closed the commit-time gap; installed == canonical; proven to block an orphan).
- **All 5 tools machine-portable** (0 hardcoded `/home/caramel` paths) → run on any clone/PC.
- **check_meta_registry.py regex fix** (TD-150 false-positive closed).
- **3-layer net:** skill-time (aggregator wired) + commit-time (hook A-E) + on-demand (aggregator). SSoT: all layers call the same tools. Catches MECHANICAL drift; judgment still needs agent + operator.

## 7. After pickup — the backlog (keys off the numeric-foundation ship + `.E.0.1`)
- **Numeric-foundation unification ship (Task #11):** its own HIGH-RISK `/precoding-audit-gate` + `/blindspot-scan` + new-fn design-audit (the `FixedPoint<RADIX,FRAC>` + decimal-Mul + decimal-parse designs); fix-design F-A (verify LIVE books the reported commission vs recomputes — `OrderManager.hpp:1144/1189`); R-1 research (Binance fee-rounding precise convention).
- **`.E.0.1` (Task #2):** the net — gates the unification ship; D-97-UNAFFECTED parts (FP-det F-056/57/58 + locale-class close + F-076/StampT) proceed; money-parse now targets decimal.
- **Task #4 (codification):** Classes 37+ + D-94 H20-extension + D-95 audit-auto-fire + the D-100 golden-epoch DESIGN_SPEC + D-106 venue-SSoT spec.
- **Task #10 / D-98:** backlog→`.E`-home structural mapping (93 TECH_DEBT + 5 PARITY + 141 findings).

## Verification gate
This close ran `/close-session`: Stage 2.0 aggregator SWEEP CLEAN (exit 0) · Stage 2.1 forward-promise (HIGH=0; 22 pre-existing MED/LOW) · Stage 4.5 harvest (AR-1 recurrence bump) · **Stage 5.5 independent review = COMPLETE (15/15 VERIFIED on disk)** · both repos pushed in-sync. Stage 5 `/readiness` skipped (no in-flight plan-body coding this session — meta/tooling + planning only).

## SEAM (rolling-window per D-72)
**Outbound — this session delivered:** the D-97 decision (decimal) + the unified-radix-core architecture (D-99..D-110) + the audit-verified foundation doc/ship-plan + the complete D-112 skill↔CI wiring + the hardened 3-layer doc-CI net + the AR-1 conclusion-before-verification meta-lesson (codified + structurally netted).
**Inbound — next session MUST verify (/accept-handoff does most):** decision-log D-99→D-115 present; foundation doc substantive + B-Plus clean; the 2 new memories indexed + bidirectional; the 6 skills + hook Check E wired; TD-149 open / TD-150 closed; aggregator SWEEP CLEAN. Then: the numeric-foundation ship is the headline (gated behind `.E.0.1`).

## TaskList (recreate on pickup)
| # | Task | Status |
|---|---|---|
| #1 | money-numeric-core foundation design doc (D-97/99/100/101) | completed |
| #12 | D-112 skill↔CI wiring (all 6 skills + aggregator + hook Check E) | completed |
| #2 | `.E.0.1` Net-2 FP+replay determinism net (v0.3) | in_progress — gates the ship; D-97-unaffected parts proceed |
| #11 | **numeric-foundation unification ship** (`FixedPoint<RADIX,FRAC>`) | pending — **HEADLINE NEXT** (after `.E.0.1`); HIGH-RISK, own gate |
| #3 | codify Classes 37+ + skill auto-fire + golden-epoch/venue-SSoT specs (D-94/95/100/106) | pending |
| #4 | D-98 backlog→`.E`-home structural mapping | pending |
| #5 | `.E.0.3` deterministic-IO + tt:: parse family | folded into #11 (D-108) |
| #6 | `.E.1` Foundation Core→Node rename | pending (blocked: `.E.0.1` + #11 + Net-1; may re-sequence per D-101) |
| #7 | fixed-cost SWAR ingestion parse (`FPN_FromString`) | pending (.E.1 sub-item; parse-target decimal per D-97) |
| #8 | forward-vision: concern-tags across CODE | pending (future) |

## Pickup — first action
`/accept-handoff <this doc>`. Then: with `.E.0.1` as the gating net, the **numeric-foundation unification ship** is the headline — fire its HIGH-RISK `/precoding-audit-gate` against the foundation doc (the ship's plan body) + `/blindspot-scan` + the new-fn design-audit, then consult before coding. The decision is made; the ship's design is written + audit-verified; what's left is its own pre-coding gate → coding under the net.

**End of Session-4 close handoff.**
