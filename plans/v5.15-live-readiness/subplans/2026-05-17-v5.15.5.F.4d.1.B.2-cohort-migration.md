# v5.15.5.F.4d.1.B.2 — Cohort migration — plan body skeleton

**Branch:** `feat/v5.15-live-readiness`
**Predecessor:** `v5.15.5.F.4d.1.B.1` (framework consolidation; not yet shipped at this skeleton's write time)
**Pre-tag rollback anchor:** `pre-v5.15.5.F.4d.1.B.2` (create at Step 0 after `.B.1` ships)
**Successor:** `v5.15.5.F.4d.1.B.3` (legacy empty-out)
**Sub-master:** `subplans/2026-05-16-v5.15.5.F.4d.1-thread-a-framework-full.md` v1.3
**Status:** **SKELETON v1.0 (2026-05-17)** — full plan body draft deferred until `.B.1` ships + pre-coding audit gate fires for `.B.2`

---

## Why this ship exists

`.B.1` builds the framework (canonical FOREACH_CFG_GATE sparse sidecar + 3 derived-filter consumer macros + 2 NEW DESIGN_SPECs Stage 3 first reference). At `.B.1` close, walker iterates 0 rows (no cohort fields flag STAMP_BOUND_CFG_DERIVED bit yet). This sub-ship populates the cohort.

This sub-ship exercises the framework via cohort migration. After `.B.2` close, the walker iterates 24-row cohort + 5 bitmap-resident bool fields; framework consumer macros produce real cfg→inf wiring + drift checks + stamp body emit.

---

## Scope (`.B.2` cohort migration; ~6-8h focused)

### IN scope

1. **24-row FOREACH_STAMP_BOUND_CFG cohort migration → STAMP_BOUND_CFG_DERIVED metadata bit:**
   - 22 clean migrations (bit-add to existing source row metadata_flags column)
   - 1 NEW row addition (`gap_acceptable_threshold` — see CRIT-5 reframing: existing manual field at ControllerConfig.hpp:889/:1729/:2554 → DELETE manual + ADD source row with STAMP_BOUND_CFG_DERIVED bit; verify Backtest usage compiles)
   - 2 pre-canonical parity gaps (`ml_buy_threshold` at CfgFieldRegistry.hpp:524 + `bandit_blend_ratio` at :528) — ADD STAMP_BOUND bit + then add STAMP_BOUND_CFG_DERIVED bit
   - 4 retroactive `.A.7` cohort migrations (`ml_tp_pct` :525 + `ml_sl_pct` :526 + `barrier_blend_mode` :637 + `per_horizon_barrier_blend` via ML_CFG_FLAG sig migration)
   - DELETE corresponding manual POST_CFG entries in `StampBoundModelConstRegistry.hpp`

2. **FOREACH_ML_CFG_FLAG 5→6 arg sig migration:**
   - Add `metadata_flags` column to 12-row registry
   - 5 STAMP_BOUND-eligible bitmap rows get STAMP_BOUND_CFG_DERIVED bit
   - 7 other rows get `0` (no metadata)
   - Migrate `X_GEN_ML_CFG_BIT` + `X_GEN_ML_CFG_MASK` X-macro consumer signatures
   - Preserve existing `static_assert(ML_CFG_COUNT <= 16)` at `MlCfgFlagRegistry.hpp:76-77`

3. **FOREACH_CFG_GATE sparse sidecar population at `.B.1` framework:**
   - Add per-cohort gate_when expressions for fields requiring non-default gate (Bandit/Thompson cohort → BANDIT_ENABLED; Soft-risk → risk_degradation_curve != 0; Ridge → RIDGE_WITHIN/ACROSS; Composite confidence → COMPOSITE_CONFIDENCE_ENABLED)
   - Winsor cohort gates default (cfg parse-time validation enforces bounds)

4. **Winsor parse-time validation (~5 LOC):**
   - Leverage existing `WARN_ON_CLAMP` infrastructure at `tt::cfg_parse_field<T>` for individual bounds
   - Add cross-field `low < high` invariant check at parser post-finalize
   - Drop original v1.2 sidecar's `low <= 0.0 || high >= 1.0` predicate (rejects DEFAULT cfg per accounting-audit CRIT-2)

5. **Migrate 4 ModelInference.hpp walker site mechanisms + StampHelper.hpp:156 + CoreModelZoo.hpp:243:**
   - `:1199 / :1643` — struct-gen migrations (auto-decl `T name; uint8_t has_##name;` via framework consumer)
   - `:1401` — parser migration (read stamp bytes → set inf.* via framework consumer)
   - `:1788` — canonical-body emit migration (write cfg → stamp bytes via STAMP_CFG_POPULATE_FROM_DERIVED)
   - `StampHelper.hpp:156` — `STAMP_CFG_AUTOPOPULATE(inf, cfg)` → `STAMP_CFG_POPULATE_FROM_DERIVED(buf, cap, cfg)` adjusted for sig
   - `CoreModelZoo.hpp:243` — drift loop migration → `DRIFT_CHECK_AUTOPOPULATE`

6. **CRIT-6 canonical body byte order decision and implementation:**
   - Operator decision required: (a) accept reorder + bump stamp_format_version OR (b) walker enrichment to preserve declaration order
   - Implement per chosen option

7. **Updated tests:**
   - ~25-30 new tests in `controller_test.cpp` exercising non-empty cohort walk
   - HMAC chain byte preservation tests via `wire_format_invariants.hpp` helper (non-empty body case)
   - Cohort-specific drift gate tests per Bandit/Thompson, Ridge, etc.

### NOT IN scope (deferred to `.B.3`)

- DELETE `MemHeaders/CfgDerivedInferenceCfgRegistry.hpp`
- DELETE `FOREACH_STAMP_BOUND_CFG` (legacy registry empty-out)
- 14 `controller_test.cpp` count-assertion migrations (FOREACH_STAMP_BOUND_CFG_COUNT references)
- CI Checks 9-12 (cohort eligibility verification)
- v5.14 stamp fixture regression test
- DESIGN_SPECS spec-body cleanup (HIGH-E from synthesis)

---

## Steps (outline; full draft at `.B.2` pre-coding gate)

- Step 0 — pre-coding rollback anchor + Path γ-class re-verification (post-`.B.1`-ship state)
- Step 1 — 24-row cohort bit-add at FOREACH_PER_CORE_CFG_FIELD + FOREACH_GLOBAL_CFG_FIELD
- Step 2 — gap_acceptable_threshold migration (DELETE manual + ADD source row)
- Step 3 — 2 pre-canonical parity gaps + 4 retroactive `.A.7` cohort migrations
- Step 4 — FOREACH_ML_CFG_FLAG 5→6 sig migration + consumer macro updates
- Step 5 — Populate FOREACH_CFG_GATE sparse sidecar per cohort
- Step 6 — Winsor parse-time validation
- Step 7 — Migrate 4 ModelInference walker site mechanisms + StampHelper.hpp:156 + CoreModelZoo.hpp:243
- Step 8 — CRIT-6 canonical body byte order decision + implementation
- Step 9 — Update tests + Add cohort-specific drift gate tests + HMAC byte preservation
- Step 10 — Build verify + ship close

---

## Pre-coding triggers (REQUIRED before promoting SKELETON → ACTIVE)

1. `.B.1` ships clean (3206+ tests GREEN; framework infrastructure verified empty-walk)
2. `/precoding-audit-gate` fires on this plan body (5 audits parallel; possibly +accounting-audit since cohort migration affects HMAC chain)
3. CRIT-6 canonical body byte order decision resolved with operator
4. Full plan body drafted with concrete Step 0-10 detail
5. Sidecar with concrete code samples (`2026-05-17-v5.15.5.F.4d.1.B.2-cohort-migration-examples.md`)
6. Pre-coding audit synthesis + triage per per-sub-ship cycle discipline

---

## Cross-references

- Predecessor `.B.1` plan body: `2026-05-17-v5.15.5.F.4d.1.B.1-framework-consolidation.md`
- Sub-master v1.3: `subplans/2026-05-16-v5.15.5.F.4d.1-thread-a-framework-full.md`
- Audit synthesis: `plan_checks/2026-05-17-v5.15.5.F.4d.1.B-audit-synthesis.md`
- Original `.B` plan body (SUPERSEDED): `subplans/2026-05-16-v5.15.5.F.4d.1.B-migration-consumer.md`

---

**End of `.B.2` plan body skeleton v1.0.** Full draft deferred until after `.B.1` ships.
