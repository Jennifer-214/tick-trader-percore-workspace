---
type: handoff
date: 2026-06-10
status: active
ship_tag: E.0.10
plan_type: live-readiness
ship_context: .E.0.10 Net-1 (pre-.E.1 characterization safety net) — MID-SHIP checkpoint
coding_status: mid-coding-checkpoint
predecessor_handoff: 2026-06-10-ship-b-shipped-next-e02-handoff.md (superseded)
plan: plans/v5.15-live-readiness/subplans/2026-06-10-v5.15.5.F.4d.1.E.0.10-net1-correctness-net.md
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
deletion_scope: none
---

# Handoff — `.E.0.10` Net-1: D-190 fixed + the cross-thread torn-read class surfaced (`.E.1` is now a capital-safety gate)

## State at handoff write
- **Branch:** `feat/v5.15-live-readiness`
- **Engine HEAD:** `5e65933` (clean) · tests **3343/0** (`./build.sh test`)
- **Workspace HEAD:** `ff7acca` (clean) — **all pushed** to `origin/main`
- **Tag anchor:** `v5.15.5.F.4d.1.E.0.9` (Ship B — decimal money — SHIPPED)
- **The in-flight ship `.E.0.10`** is the **Net-1 characterization safety net** that freezes current behavior BEFORE the `.E.1` Core→Node rename can silently break it. It is partly done (see below) — this is a mid-ship checkpoint, NOT a ship close.

## What THIS session did (all committed; engine c1a10d2/d0a6bb1/5e65933 + workspace 7f318c5..416abd3)

**Two real bugs found + fixed:**
1. **D-190 (capital bug, FIXED — engine `c1a10d2`):** P&L gross was open-coded in 3 sites with 2 formulas — `round((exit−entry)×qty)` (1-mul) vs `round(exit×qty)−round(entry×qty)` (2-mul) DIVERGE by 1 ULP on ~25% of fills under decimal half-even. PREEXISTING (invisible under the old FPN repr); the Ship-B decimal flip ACTIVATED it. Fix = `Money_FillGross` 1-mul SSoT in `Portfolio.hpp`; all 5 price-diff gross sites route through it. Triple-guarded (regression test + pre-commit Check G `tools/check_money_gross_single_source.py` + docs). Independent review confirmed complete (the one un-routed site `EventLoopAggregates.hpp:131` is display-only + 1-mul, not a defect).
2. **persist-8 (LOW, FIXED — engine `d0a6bb1`):** paper-reset left the `ExecutionCore.active` hot mirror un-cleared → zombie-active window. Fix = `cores[c].active = 0` in the reset loop (`Async.hpp:726`). LOW (paper-mode + the phantom sell is guarded at `OrderManager.hpp:1185`).

**🔴 THE BIG FIND — cross-thread torn-read CLASS (systemic; drives `.E.1`'s severity):** an adversarial 3-agent sweep found the OMS money cluster has **NO seqlock/atomic anywhere** — every non-drainer thread reads raw 16B `Money` (2 machine words → tears under concurrent drainer writes). **9 sites; 3 are LIVE capital-control** — worst is the **reconciler that pushes a balance CORRECTION to live capital off a torn read** (Knight-Capital-shaped), plus the global + per-core MTM kill switches. It's a DELIBERATE, design-time-documented accepted-race (in-code 2026-04-09 + decision-log F-2/D-74); the structural fix (aggregator-as-single-writer + seqlock-published money snapshot) **already lives in the `.E.1` plan** (D-54). This session REFRAMED it: F-2 called it a "free bonus" → it is **LOAD-BEARING for capital safety**, so **`.E.1` is now a HARD LIVE-ENABLE GATE** (live trading BLOCKED until the torn-read class + `conc-5` land). Folded into the `.E.1` plan (Bug-Classes `:68` + acceptance `:280`) + decision-log `F-2-AMEND`.

**Scope discipline (an over-lump I made, caught by operator pushback + a 3-agent refute → AR-6):** the torn-read class is `persist-dod-1` ⊇ the kill-switch read ONLY. `persist-8` (stale-flag) and `conc-5` (SPSC-ring race) are **DISTINCT roots** — a seqlock fixes neither. Do NOT re-collapse them.

**Codifications (4):** NEW DESIGN_SPECS `adversarial-multi-agent-audit-methodology.md` + `cross-thread-multiword-read-consistency-discipline.md`; NEW memory `feedback_fold_findings_into_destination_plan` (reactive cross-plan finding-fold process); meta-anti-pattern **AR-6** (false-cluster: shared-epoch/theme ≠ shared-root).

## What's LEFT (the close-out plan — register § "📍 SESSION STATE")

| # | Item | Notes |
|---|---|---|
| 1 | **Live-enable HARD gate** | ✅ decision + reframe folded into `.E.1` plan + F-2-AMEND. (Structural boot-refuse is a `.E.1` task.) |
| 2 | **Deeper concurrency audit** | ✅ the 3-agent sweep (9 sites). |
| 3 | **Anti-pattern + CI check → `RECURRING_BUG_PATTERNS`** | ⏳ OWED. Content is in `cross-thread-multiword-read-consistency-discipline.md` § "Anti-pattern + CI check" (two shapes + a pre-specified mechanical check: flag >8B cross-thread-written field read outside a seqlock). |
| 4 | **`.E.1` structural fix** (aggregator rework) | FUTURE SHIP. **⚠️ The `.E.1` plan is RED / pre-audit-gate** — a prior `/plan-dive` (`plan_checks/E.0-audit-reports/E.1-dive-synthesis.md`, verdict RED) found the gate's OWN mechanisms broken: the D-54 aggregator depends on nonexistent `FPN_Atomic*` + still codes the SUPERSEDED pull-model + a stale "FPN 24B" sizing; `conc-5`'s `submit_queue` is MPSC-vs-SPSC contradictory (`SPSCRing.hpp:12` = no MPSC primitive). The plan MUST be amended + re-audited (`/precoding-audit-gate`) before it can close the live-enable gate — do NOT treat it as "implement the already-designed fix." |
| 5 | **`/adversarial-audit` skill** | ⏳ OWED. So "check X" auto-fires the 3-agent refute (extends TECH_DEBT-164 + `feedback_auto_route_input_to_matching_skill`). This session's over-lump is the failure mode it prevents. |
| 6 | **Remaining Net-1 characterization tests** | ⏳ OWED — `oms-ts-1` (fee-blind ±$15), `hpg-bc-1` (F-059 golden-master), `wfa-1` (warm-restart restores positions exit-armed — CONFIRM, may be closed). |
| 7 | **`.E.1` plan B-Plus reconciliation** | ⏳ OWED — the `.E.1` plan has 10 PRE-EXISTING illustrative template fragments (lines ~362–925) that trip B-Plus (false-positives). Add exemption markers or compilable context at the `.E.1` plan review. |
| — | **`conc-5` (CRITICAL)** | ⏳ runtime tsan multi-producer stress on a disposable clone — the hard gate for `.E.1`. Distinct root (SPSC ring → single-producer/MPSC fix). |
| — | **TECH_DEBT-163 (always-loaded budget)** | ⚠️ NOW PRESSING — MEMORY.md 5 B free, CLAUDE.local.md 20 B free. The next memory/rule addition FAILS the byte-cap until the durable restructure (split/move-to-on-demand). Do this BEFORE the next codification. |
| — | **Phase-1 backlog re-triage** | TaskList #7/#8/#11 — 93 TECH_DEBT + 5 PARITY disposition; fpmem granular ×8; `.E.1`-home mapping. |

## TaskList state at handoff write (recreate verbatim)

| ID | Status | Subject |
|---|---|---|
| #7 | in_progress | fpmem cluster re-triage → register |
| #8 | in_progress | Re-triage E.1 bucket (30) + PERSIST subset of PRE-PAPER-TEST |
| #9 | completed | D-110 snapshot money-exact round-trip probe |
| #10 | pending | Verify conc-5 (CRITICAL) on disposable clone |
| #11 | pending | Backlog→.E.1-home mapping + TECH_DEBT/PARITY disposition |

(Recreate via TaskCreate. Note: the close-out items #3/#5/#6/#7-plan/TECH_DEBT-163 above are NOT yet TaskList entries — add them if you pick up Net-1 completion.)

## Critical pickup-time reads
- This handoff (whole) + the **register** (`plan_checks/E.0.10-finding-disposition-register.md` — the SSoT; read § "📍 SESSION STATE + CLOSE-OUT PLAN" first, then the persist-8/persist-dod-1/cluster entries)
- The `.E.1` plan (`subplans/2026-05-28-...E.1-foundation.md`) — esp. Bug-Classes `:68` + acceptance `:280` (the folded torn-read class + gate) + decision-log `F-2-AMEND`
- NEW specs: `DESIGN_SPECS/concurrency-patterns/cross-thread-multiword-read-consistency-discipline.md` (the torn-read discipline + the 9 sites + the anti-pattern owed) + `DESIGN_SPECS/audit-methodologies/adversarial-multi-agent-audit-methodology.md`
- **`.E.1` readiness:** `plan_checks/E.0-audit-reports/E.1-dive-synthesis.md` (the RED verdict — read BEFORE assuming the gate is "designed") + `E-guard-coverage-matrix.md`
- Baseline: `CLAUDE.md` + `CLAUDE.local.md` + `MEMORY.md`

## Pickup
Run **`/accept-handoff /home/caramel/code/tick-trader-percore-workspace/plans/v5.15-live-readiness/handoffs/2026-06-10-E.0.10-net1-concurrency-checkpoint-handoff.md`** — it loads the cited reads, verifies git state, runs the mechanical floor + `/readiness`, recreates the TaskList, and reports the concrete next action.

**Suggested next action:** either (a) finish Net-1 — the 3 remaining characterization tests (`oms-ts-1`/`hpg-bc-1`/`wfa-1`) + `conc-5` tsan — or (b) clear the OWED codifications (the `RECURRING_BUG_PATTERNS` anti-pattern + CI check, the `/adversarial-audit` skill) — but **do TECH_DEBT-163 (budget restructure) first if any always-loaded doc needs touching.** The `.E.1` Foundation ship itself is the next major ship after Net-1 closes, and it's now gated as capital-safety-load-bearing, not just cleanup.

## Operator collaboration (carry forward)
Address Caramel as Caramel/she/her · no AskUserQuestion modals (inline options) · consult before coding on audit findings · **checks default to ADVERSARIAL** (FIND/REFUTE + multiple independent cross-checking agents) per `feedback_adversarial_framing_default_for_checks` · correctness-first, never-skip-thoroughness, rich-context-preservation is the standing default · commit/push only when asked (workspace is auto-synced at close; the public engine repo's session commits are LOCAL — push only on request).
