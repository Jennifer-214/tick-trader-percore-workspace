---
type: subplan-scaffold
ship_tag: v5.15.5.F.4d.1.E.1.6
anchor: pre-v5.15.5.F.4d.1.E.1.6
parent: 2026-05-28-v5.15.5.F.4d.1.E.1-foundation.md
predecessor: 2026-06-15-v5.15.5.F.4d.1.E.1.5-per-cluster-purity.md
successor: (none — .E.1 close → .E.2)
status: scaffold
risk: MED
hot_path: untouched (additive framework; new threads/registries, no per-tick path edit)
spine: FRAMEWORK (no spine) — the LAST .E.1 leaf
decisions_honored: [D-3, D-58, D-22, D-32, D-33, D-15, D-28, H15, H18, H19, H13, H14]
td_closed: [TD-135, TD-162]
guard_rows: [§4a-H15, §4a-H18, §4a-H19, §4a-H13-adapter-dispatch, §4a-H14-mode-MBS]
source: plan_checks/2026-06-15-E.1-decomp-A-adjudication.md §2 (E.1.6)
---

# E.1.6 — FOREACH_EXCHANGE machinery + FOREACH_SUBACCOUNT + 4-state per-node mode + dev/prod thread topology

> SCAFFOLD (not a coding body). Meaningful enough to reason + sequence + seed the dive body; the full body is re-grounded against HEAD by a C-class pass when this leaf becomes the dive (per `plan-hierarchy-and-sub-master-decomposition.md` §3). All file:line are HEAD `572c978` and WILL drift — re-verify at dive. **This is the LAST `.E.1` leaf; its OUTBOUND seam is `.E.1` close → `.E.2`.**

## Scope

**FRAMEWORK (no D-214 spine). The additive multi-exchange substrate — the framework rides the COMPLETE, PURE per-cluster trading core that E.1.0–E.1.5 delivered; it never re-opens coherence, fill-math, or purity.** Four deliverables under one rollback anchor (a break here is additive-framework: a registry mis-wire is a compile error under the H15/H19 CI, or a Binance-row regression — a rollback drops ONLY multi-exchange, leaving a fully-working single-exchange engine):

1. **`FOREACH_EXCHANGE` meta-registry + the per-cluster adapter/WS/producer MACHINERY.** Generalize the EXISTING single-exchange `tt::ExchangeAdapter<F>` fn-pointer interface (`CoreFrameworks/ExchangeAdapter.hpp:84` — already in `namespace tt`, already C-polymorphism, header comment already says "future phases can add Coinbase, Kraken, etc.") into a `FOREACH_EXCHANGE`-driven registry; Binance the FIRST canonical row. Wire the per-cluster producer/adapter/WS thread MACHINERY into the `ClusterState` CONTAINER shape E.1.2 already landed (the SHAPE was early per resolution #3a; this ship adds the machinery). GENERIC-framework focus (D-58) — the row + adapter contract must be exchange-agnostic, NOT Alpaca/IBKR-specific (those are `.E.6`/`.E.7`, 1-row additions).
2. **`FOREACH_SUBACCOUNT_<EXCHANGE>` per-exchange meta-registry.** Sub-account credentials + rate-budget + listenKey topology, sparse rows indexed by (cluster, subaccount). Binance the first canonical (the `NodeState.binding.subaccount_id` E.1.2 landed indexes into this).
3. **4-state per-node mode enum, MBS_* 2-bit (H14).** FORMALIZE + relocate the EXISTING global 3-value `trading_mode` (`TRADING_MODE_PAPER=0/LIVE=1/SHADOW=2`, ControllerConfig.hpp:59-61, H21-immutable) into a per-NODE 4-state `backtest|paper|live|shadow` field on `NodeState.hot.mode` (the field E.1.2 landed as `uint8_t mode`). MBS_* SHIFT_*/MASK_* accessors over the 2-bit slot — NO C++ bitfield (H14).
4. **dev-vs-production thread-topology cfg.** A NEW `topology.mode = dev|production` cfg field: `dev` = OS-scheduled (the existing best-effort `EngineSharded_PinThread` at Run.hpp:188 becomes a no-op/soft path; laptop functional); `production` = strict isolcpus/nohz_full/pinning (today isolcpus is only a printed hint at Run.hpp:331). Formalizes the dev/prod split D-33 calls for.

## Spine / layer

FRAMEWORK (no spine) — the LAST cut. Additive framework on a complete trading core: INBOUND is the per-cluster-PURE container (E.1.5) on the coherent (E.1.3) + conservation-correct (E.1.4) fill path, on the FINAL frozen `NodeState`/`ClusterState` layout (E.1.2), under E.1.0's standing CI net. The framework EXTENDS; it does not re-touch any capital path. Safest last cut (a rollback leaves a working single-exchange engine).

## TD-closed (one line each)

- **TD-135** — Class-11 hardcoded `regime_names[]` sibling-arrays (GUI: `SettingsPanel.hpp:1517`, `StrategyQualityPanel.hpp:300`; the `BanditLearning.hpp:455` param is the CONSUMER, not the bypass) → registry-fit against the EXISTING `FOREACH_REGIME` (MetaRegistry.hpp:73). **VERIFY via `/registry-fit-audit`** at ship close — confirm the names flow from `FOREACH_REGIME`, not a parallel array (the FOREACH_EXCHANGE work makes the registry-fit-audit the natural gate to also sweep `regime_names`).
- **TD-162** — warm-restart fee-recompute. **VERIFY/own-if-it-lands-here.** OPEN HOMING QUESTION: the adjudication §2 routes TD-162 to E.1.6 as "verify subsumed," but warm-restart fee-recompute is a FILL/reconcile concern (E.1.4 owns A20 warm-tail replay + venue-exact commission). Likely E.1.4's natural surface; E.1.6 may only inherit a "verify it landed" cross-link. Resolve at E.1.4 dive (see Known folds + the synthesis flag).
- **TD-203 STAYS v5.16 — do NOT close here.** It is the Phase-D blanket live-gate removal (`LiveReadiness_Verify` refuses live until `.E`; H21 tombstone), explicitly post-live-readiness per D-222 + the chain in the decision log :1449.

## Locked decisions

- **D-3** — `FOREACH_EXCHANGE` day-one (the multi-exchange substrate is a `.E.1` deliverable, not deferred to the first second-exchange ship).
- **D-58** — GENERIC-framework focus. The registry + adapter contract are exchange-agnostic; NOT Alpaca/IBKR-specific. Adding the (N+1)th exchange = 1 `FOREACH_EXCHANGE` row + the adapter's fn-pointer specialization.
- **D-22 / D-32** — the 4-state per-node mode (backtest|paper|live|shadow) folded to `.E.1` (the formalization + per-node relocation lands here, the last leaf).
- **D-33** — dev-vs-production thread topology (`topology.mode = dev|production`).
- **D-15 / D-28** — cluster/node/deployment hierarchy: the MACHINERY lands LATE (here); the SHAPE/`ClusterState` container landed EARLY (E.1.2, resolution #3a). This ship is the "machinery" half of that split.
- **H15** — both registries enrolled in `FOREACH_REGISTRY` (MetaRegistry.hpp). **H18** — any per-exchange customization via a sidecar `FOREACH_EXCHANGE_OVERRIDE`, never a parallel wide-variant registry (Class 21). **H19** — topology: each new registry declares LEVEL + a valid PARENT. **H13** — adapter dispatch stays fn-pointer-table C-polymorphism (the EXISTING `tt::ExchangeAdapter<F>` model), never `reinterpret_cast`. **H14** — the mode field is MBS_* 2-bit, no bitfield. **H21** — the mode enum values PAPER=0/LIVE=1/SHADOW=2 are append-only/immutable (a persisted/wire identifier); a new `backtest` value gets the NEXT free slot, NEVER a renumber.

## §4 guard-rows this leaf fills

Per guard-matrix §4a:
- **§4a H15** — `FOREACH_EXCHANGE` + `FOREACH_SUBACCOUNT_<EXCHANGE>` enrolled in `FOREACH_REGISTRY` (the `test_meta_registry_coverage` CI Check RED-builds a registry added without enrollment).
- **§4a H18** — the sidecar-override row for any per-exchange custom semantics (sparse `FOREACH_EXCHANGE_OVERRIDE` indexed by the parent's FIELD_IDX).
- **§4a H19** — `test_meta_registry_topology`: both new registries declare LEVEL (1, direct) + PARENT (`FOREACH_REGISTRY`); the per-subaccount registry may sit at LEVEL 2 under `FOREACH_EXCHANGE` — resolve the level at dive against the enforced numbering.
- **§4a H13 adapter-dispatch** — the adapter generalization stays `tt::` fn-pointer-table (CI/audit candidate: no `reinterpret_cast` punning introduced in the dispatch).
- **§4a H14 mode-MBS** — the no-bitfield grep-CI (E.1.0 armed) catches a careless `mode : 2` bitfield; the mode field uses MBS_* SHIFT_*/MASK_* constants.

## INBOUND seam (needs from E.1.5 — and the foundations beneath it)

- From **E.1.5**: **capital authority is per-cluster-PURE** + the A34 concentration governor + the (N+1)th-cluster H22 witness passing. The framework wires into this PURE per-cluster container — it rides the purity, NEVER re-opening it (E.1.5's OUTBOUND, verbatim). One-cluster = one-exchange is now the authority model the FOREACH_EXCHANGE machinery populates.
- From **E.1.2** (transitively, the load-bearing layout): the `ClusterState` CONTAINER shape + `NodeState.binding{cluster_id, subaccount_id, client_order_id_prefix}` + the flat `per_cluster[]` aggregate slots + the `NodeState.hot.mode` `uint8_t` field — all PRESENT + FINAL. This ship adds MACHINERY into a frozen container; it does NOT re-touch the layout (no snapshot VERSION bump here — bytes are frozen at E.1.2).
- From **E.1.3**: the per-cluster producer/adapter/WS thread MACHINERY this ship wires absorbs into the per-cluster coherence model (the aggregator is the sole kill-flag writer; the adapter/WS threads are per-cluster-isolated — a Binance WS drop never touches another cluster).
- From **E.1.0**: the standing CI net (H15/H19/H14/H13 grep-CI + the meta-registry coverage/topology checks) RED-builds a mis-enrolled registry, a bitfield mode, or a punning dispatch.

## OUTBOUND seam (= `.E.1` CLOSE → `.E.2`)

**The multi-exchange substrate is established + the per-node mode + the dev/prod topology are live.** Specifically:
- **Adding the (N+1)th exchange = 1 `FOREACH_EXCHANGE` row + the adapter's fn-pointer specialization** — the operational H22/H15 test. `.E.6` (Alpaca) + `.E.7` (IBKR) are 1-row-in-registry additions on this substrate.
- The 4-state per-node mode is live (each node runs backtest|paper|live|shadow independently; the aggregator tags P&L by mode).
- `topology.mode = dev|production` selects OS-scheduled (laptop) vs strict-pinned (server).
- The DAG's `.E.1`→`.E.2..8` edges are satisfied; `.E.1` (the foundation sub-sprint) CLOSES. `.E.2` (headless + per-node configs + ML CLI) picks up on the complete, multi-exchange-ready, per-cluster-pure substrate.

> This leaf has NO successor inside `.E.1`. Its OUTBOUND IS the sub-sprint close. The sub-master (`.E.1-foundation.md`) + the DAG update to mark `.E.1` shipped when this lands.

## Design-shape (code-grounded; HEAD 572c978, WILL drift — re-verify at dive)

**The adapter generalization (EXTEND, do NOT parallel).** The canonical sister is `tt::ExchangeAdapter<F>` (`CoreFrameworks/ExchangeAdapter.hpp:84`) — a `struct` of fn-pointers (`submit_market_buy`/`submit_market_sell`/`get_balances`/`query_order`/`shutdown` + opaque `void* ctx`), embedded BY VALUE in `OrderManagerState` (`OrderManager.hpp:472`, COLD cluster anchor with an alignment static_assert at :746). Today it is single-exchange (Binance, via `BinanceOrderAPI` at `DataStream/BinanceOrderAPI.hpp:91`). The FOREACH_EXCHANGE work: a `FOREACH_EXCHANGE(X)` row (enum value, name, supports_subaccounts, rate_per_min, market_hours_kind) that auto-generates the `ExchangeEnum` + per-exchange metadata accessors; the adapter STAYS the fn-pointer table (H13 — C-polymorphism per CLAUDE.md conventions; the per-exchange `_Get` populates the fn-pointers). **NOTE: the v0.1 sub-master sketch (`.E.1-foundation.md:438-536`) proposed `tt::submit_order<ExchangeT>` TEMPLATE-specialization dispatch — that is a DIFFERENT, parallel dispatch model + would be a Class-21/H13 mismatch against the landed fn-pointer table. Use the fn-pointer-table generalization, NOT template specialization. RE-GROUND the sketch.**

**FOREACH_EXCHANGE / FOREACH_SUBACCOUNT enrollment.** Both are NEW (0-hit confirmed: no `FOREACH_EXCHANGE`/`FOREACH_SUBACCOUNT`/`EXCHANGE_BINANCE` symbols today). Enroll in `FOREACH_REGISTRY` (MetaRegistry.hpp:35) with the real row shape `X(registry_name, LEVEL, PARENT_NAME, description)` — Binance the first canonical row. (v0.1 sketch `.E.1-foundation.md:538-559` for FOREACH_SUBACCOUNT — RE-GROUND the credentials/rate-budget topology against the landed `binding`.)

**The 4-state mode (FORMALIZE the existing enum).** The EXISTING values: `constexpr uint8_t TRADING_MODE_PAPER=0/LIVE=1/SHADOW=2` (`ControllerConfig.hpp:59-61`; SHADOW is RESERVED/behaves-as-PAPER, append-only per H21). `backtest` is NOT in this enum today (backtest is `engine_mode`, separate). This ship: add a `backtest` value at the NEXT free slot (3 — H21 append-only, NEVER renumber PAPER/LIVE/SHADOW), relocate the authority from global `cfg.trading_mode` (read at e.g. ControllerConfig.hpp:2046 `ControllerConfig_IsLiveCapital` — but that relocation to `cluster.trading_mode` is E.1.5's surface, NOT re-done here) to the per-NODE `NodeState.hot.mode` (the `uint8_t mode` E.1.2 landed), encoded MBS_* 2-bit (canonical sister: the Order.hpp:274 MBS_* bandit-context accessors, multi-bit-state-encoding-pattern.md). The parser already maps "paper"/"live"/"shadow" strings (ControllerConfig.hpp:2836-2838) — extend for per-node "backtest" + the per-node cfg surface.

**The dev/prod topology cfg.** NEW `topology.mode = dev|production` (0-hit today). `dev` softens `EngineSharded_PinThread` (Run.hpp:188 `pthread_setaffinity_np` — best-effort already, WARN-on-fail) to a no-op; `production` enforces strict pinning + documents the isolcpus/nohz_full/`chrt -f 90` operator requirement (today only a printed hint at Run.hpp:331). A new global cfg field (1 row in `FOREACH_GLOBAL_CFG_FIELD`).

## Rollback anchor

`pre-v5.15.5.F.4d.1.E.1.6` (create at the dive's Phase A.0). **A break here is additive-framework** — a registry mis-wire is a compile error under H15/H19 CI, or a Binance-row regression. A `git bisect` lands on this anchor; a rollback drops ONLY multi-exchange + the per-node mode + the topology cfg, leaving a fully-working single-exchange engine (E.1.0–E.1.5 intact). The safest last cut.

## Known folds / findings it owns

- **TD-162 homing (OPEN — resolve at E.1.4 dive, do NOT silently absorb here).** Warm-restart fee-recompute reads as a FILL/reconcile concern (E.1.4's A20 warm-tail replay + venue-exact-commission surface). The adjudication §2 routes it to E.1.6 as "verify subsumed," but its NATURAL owner is E.1.4. Likely resolution: E.1.4 owns the fix; E.1.6 carries only a "verify it landed" cross-link. Flag for the operator (synthesis) + the E.1.4 dive. Homed-visible, NOT dropped (it is on the task list).
- **TD-135 verify (NOT assume).** `regime_names[]` is hardcoded at 3 GUI sites + `BanditLearning.hpp` takes it as a CONSUMER param. The EXISTING `FOREACH_REGIME` registry (MetaRegistry.hpp:73) is the fit target. RUN `/registry-fit-audit` at ship close — confirm the names flow from the registry, not a parallel array. Do not close on adjacency.
- **GENERIC-not-Alpaca/IBKR (D-58 discipline).** Resist baking Alpaca/IBKR specifics into the FOREACH_EXCHANGE row shape or the adapter contract. The row + contract must be exchange-agnostic so `.E.6`/`.E.7` are 1-row additions. If a field is only meaningful for one venue (e.g. `market_hours_kind` for equities), it belongs in the GENERIC row with an ALWAYS default, or in a sidecar override (H18) — never a parallel registry.
- **fn-pointer-table, NOT template-specialization (H13).** The v0.1 sketch's `tt::submit_order<ExchangeT>` is the wrong model against the landed `tt::ExchangeAdapter<F>` fn-pointer struct. Extend the fn-pointer table. (Stated in Design-shape; repeated as a fold because it is the single most likely scaffold→dive mis-step.)
- **H21 mode-enum immutability.** PAPER=0/LIVE=1/SHADOW=2 are persisted/wire identifiers — append `backtest` at the next slot, NEVER renumber. SHADOW stays RESERVED-but-reserved (do not value-reuse even though it behaves-as-PAPER).
- **No snapshot VERSION bump here.** The layout is FINAL at E.1.2; this ship adds threads + registries + a mode-field SEMANTIC (the `uint8_t mode` byte already exists in the frozen layout) + cfg fields. If the per-node mode or topology lands a NEW persisted byte, that is a layout change → it should have been in E.1.2's frozen struct; re-check at dive that `NodeState.hot.mode` + any topology persistence already exist in the frozen layout, else escalate (a late layout change re-opens the snapshot epoch — avoid).
- **`.E.1` close bookkeeping.** This leaf shipping closes the `.E.1` sub-sprint: flip the sub-master's register rows, fill the last guard-matrix rows (H15/H18/H19/H13/H14), update the DAG to mark `.E.1` shipped, and confirm the `.E.1`→`.E.2` edge is the next pickup.

## Shape-match note (do NOT re-schedule landed work)

`tt::ExchangeAdapter<F>` EXISTS (`ExchangeAdapter.hpp:84`) — this ship GENERALIZES it (FOREACH_EXCHANGE registry + Binance as the first canonical row), it does NOT build the adapter interface from scratch. The 3-value `trading_mode` enum EXISTS (`ControllerConfig.hpp:59-61`, H21-immutable) — this ship FORMALIZES it to 4-state + relocates it per-NODE, it does NOT invent the mode concept. Thread pinning EXISTS (`Run.hpp:188`) — this ship adds the `dev|production` SELECTOR over it, it does NOT build pinning. The `ClusterState` CONTAINER + `binding` + `per_cluster[]` + `NodeState.hot.mode` byte LANDED at E.1.2 — this ship wires MACHINERY into them, it does NOT re-lay the container. NEW here: the two registries, the 4th mode value + MBS_* per-node encoding, the `topology.mode` cfg field, the per-cluster producer/adapter/WS thread wiring.
