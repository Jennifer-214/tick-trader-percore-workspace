---
type: handoff
status: active   # THE one live handoff. Supersedes 2026-07-17-E.1.2.A-completeness-gate-phaseC-open-handoff.md (Phase-C actionable sweep COMPLETE). ≤1-active enforced by check_handoff_active_singleton.py.
ship_tag: E.1.2.A
plan_type: refactor   # in-code documentation infrastructure — maintainability deliverable, NOT a capital-path change
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: the locked [SCHEMA]_[v1.0] tag system rolled out to EVERY unit + the CI COMPLETENESS gate standing → then the north-star core-utils (C4 DERIVED-write / foxtag update) + the plugin V1 pass → the custom-IDE V1 (in-code-doc-system-north-star.md, D-358)
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-07-17-E.1.2.A-completeness-gate-phaseC-open-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md, DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md, DESIGN_SPECS/framework-patterns/doc-intelligence-toolchain-architecture.md, DESIGN_SPECS/meta-disciplines/calibration-corpus-non-vacuity-discipline.md]
deletion_scope: none   # comments-only + lossless-gated everywhere; MARK-never-delete ([DEPRECATED])
coding_status: phaseC-sweep-COMPLETE   # all 6 actionable directories converted + committed; only the 3 RegimeDetector WIP reds remain (off-limits). NEXT = north-star core-utils + plugin.
---

# E.1.2.A — Phase-C ACTIONABLE SWEEP COMPLETE (baseline 47→3); next = north-star core-utils + plugin

**Created:** 2026-07-18
**Target:** the north-star toolchain (Layer 2 core-utils: C4 DERIVED-write / RC-A / `foxtag update`) + the plugin V1 UX (Layer 3, operator's session) → the custom-IDE V1. The Phase-C conversion sweep is DONE.
**Branch:** feat/v5.15-live-readiness
**Plan:** `plans/v5.15-live-readiness/subplans/2026-07-05-E.1.2.A-comment-system-and-doc-consolidation.md` (§ COMPLETENESS-GATE + § SCOPE-IN) + the north-star `DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md`
**Decision log:** `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-359..D-363)
**Sprint MASTER:** `plans/v5.15-live-readiness/MASTER.md`

**Engine state at handoff write:**
- HEAD: `c36e96b` (Backtest conversion — Phase-C sweep DONE). The 5 conversion commits: `27036dc` ML_Headers · `eccb032` MemHeaders · `33dd595` DataStream+GUI · `d718a4d` CoreFrameworks · `c36e96b` Backtest.
- Version.hpp: `5.15.5.F.4d.1.E.1.1` · Suite baseline `3702/0` (verify `./build.sh test`) · gui build ok.
- Working tree: `M Strategies/RegimeDetector.hpp` (CARAMEL'S WIP — DO NOT TOUCH / DO NOT git-add; its `:543`/`:627` reds are the accepted-known sweep reds + its `:241`/`:545`/`:575` are the 3 remaining completeness findings) + untracked scratch (`build_clangd/`, `resume`, `tmpo…`). Otherwise clean.

**Workspace state at handoff write:**
- HEAD `5dc1830` (baseline 12→3). Session commits: `a29758c`/`eeecdff` (D-363 + toolchain-core specs) · `bce0b6e` (function-local tool exemption) · `f4ab500`/`3937dfa`/`5dc1830` (per-chunk baseline+index regens).
- Untracked: `note` (a stray root file — NOT ours; leave it).

---

## What landed this session (2026-07-18) — INDEPENDENT-REVIEW ALL-CLEAR

**The Phase-C ACTIONABLE SWEEP is COMPLETE.** Every directory converted to `[SCHEMA]_[v1.0]`; baseline **47 → 3** (the only remaining findings are the 3 RegimeDetector reds, Caramel's WIP, off-limits).

| Directory | Findings | Commit |
|---|---|---|
| ML_Headers | 13 | `27036dc` |
| MemHeaders | 4 (+ 5 function-local exemptions) | `eccb032` |
| DataStream + GUI | 4 (incl. PerNodeSnap 277-line nested block, D-340) | `33dd595` |
| CoreFrameworks (capital core) | 9 (+ calls_graph_diff CLEAN; 2 nested blocks) | `d718a4d` |
| Backtest | 9 | `c36e96b` |

- **Every chunk cleared the same bar:** per-file `lossless.py` + validator + completeness + the **comment-stripped HEAD-vs-working 0-code-change proof** + `./build.sh test` 3702/0 + doc-CI green (only the accepted RegimeDetector reds). CoreFrameworks additionally got `calls_graph_diff` (hot path provably untouched); Backtest got the gui build. **Zero engine code changed anywhere** — all comment blocks.
- **First `[DEPRECATED]` application:** the 5 ConfidenceScore `*_LegacyV1` wire-readers (verified live-but-legacy via the `ShadowLoadLegacyV1` path @ `PortfolioController.hpp:2332`; D-360).
- **TOOL REFINEMENT — function-local struct exemption** (`bce0b6e`): a struct declared inside a function body's braces is a stack impl-detail, not a navigable unit → EXEMPT in `check_conversion_completeness.py` (the plugin-aligned/extensible/schema-coherent call, Caramel's directive). Distinguished from a namespace-scope struct merely sited under a mega `[FUNCTION]` block (brace-depth 0 → still flagged). Ships with a selftest fixture (DemoLocal6 exempt / DemoLumped6 still flagged — calibration-corpus discipline). Cleaned 5 pre-existing false-positive RAII/staging structs.
- **DESIGN_SPECS captured** (Caramel: "capture the toolchain-core designs"): NEW `framework-patterns/doc-intelligence-toolchain-architecture.md` (one-parser-N-consumers D-337 + grammar-derived + migration-contract D-349) + NEW `meta-disciplines/calibration-corpus-non-vacuity-discipline.md` (D-362) + the D-363 emitter-coverage amendment to `mechanical-verification-of-derived-code-facts.md`. Closed the grammar-derived / migration-contract / parity-gated-cutover zero-coverage gaps.
- **D-363 (C4 scope-in)** — decision log + plan SCOPE-IN + task #7; `tools/CLAUDE.md` currency fix (GCN → synthetic DemoLumped6).

---

## THE remaining work (the finish line) — north-star pivot

The conversion was actually *phase 5* of the north-star (`in-code-doc-system-north-star.md`), done early because the schema locked + the checkers were good enough. What remains splits cleanly.

### 1. Phase-C tail — homed, dependency-gated (NOT dropped)
- **Task #6 — HARD-zero flip + going-forward rule.** Waits for **RegimeDetector** (Caramel's WIP) to convert — it's the only gap between baseline-3 and a *true* zero. The gate already blocks regressions via the baseline meanwhile. When RegimeDetector lands: flip the completeness CI to strict HARD-zero + codify the always-loaded "every new unit → a `[SCHEMA]` block" rule.
- **Task #7 — C4 DERIVED-write.** MECHANISM PINNED this session (read `check_cache_layout.py`): `--fix` fills 0 because `refresh_derived` (line ~151) only **rewrites** existing `[SIZE]`/`[ALIGN]`/`[CACHE_LINES]`/`[STRADDLE]` tag-lines — it does NOT add them, and our blocks carry an empty `[DERIVED]`. **This IS the north-star's toolchain increment 2c** (`foxtag update`, the D-327 STRUCT writer). See § NEXT ACTION.

### 2. North-star core-utils (Phase 4 — the shared fact-producer; the receiver's lane)
Per the D-349 migration contract, the DERIVED-write goes in the **Python authoritative layer first** (NOT `foxtag update` directly — that's the later parity-gated cutover). Increment-1 scope (in task #7):
- **(a)** extend `check_cache_layout.py`'s `refresh_derived` to **insert** the filled layout tags into a covered struct's *empty* `[DERIVED]` (with a calibration selftest);
- **(b)** coverage — a dedicated all-headers probe TU + the **RC-A** template-instantiation anchor (main.cpp under-covers; templates need instantiation at real F — the `[INSTANTIATION]` axis);
- **(c)** gate the corpus-wide blast radius (a newly-surfaced cross-thread straddle = a genuine H6 finding, triage not suppress);
- **(d)** then the `foxtag update` cutover behind `parity_check.sh`.
- Adjacent: **RC-B** (regenerate the full compile-DB — GUI-only + stale today, 11% of headers fail standalone) + the generalized drift-gate.

### 3. North-star plugin (Phase 5 — the nvim UX; OPERATOR'S SESSION per § north-star 6 / D-353)
cursor-tracking by enclosing unit · HUD-vs-PANEL split (accumulate, not replace) · the polymorphic unit CARD · context-gated actions · pop-out doc-viewer (`[REFERENCE]` float) · tag-enriched/filterable trees · dual-panel compare · registry-row browser. **Split:** the receiver builds the fact-producers (§2), Caramel builds the nvim UX, meeting at the `tagadapter` contract (build the producer once → CI gate + plugin + doc-viewer inherit it — the anti-Class-18 linchpin).

---

## NEXT ACTION (armed)

**Start increment-1 step-1: the `refresh_derived` insert + selftest** (§2a) — small, self-contained, verifiable, and it unblocks trustworthy DERIVED facts in the plugin (RC-E: never green-on-vacuous). Then step-2 (coverage / probe TU).

- **Files:** `tools/check_conversion_completeness.py` is NOT it — the DERIVED-writer is `tools/check_cache_layout.py` (`refresh_derived`, ~line 134-162; the emitter is `run_emitter` via foxtag/nvim). Read it first.
- **Specs:** `in-code-documentation-schema.md` § the per-`[TYPE]` `[DERIVED]` axes (STRUCT → SIZE/ALIGN/CACHE_LINES/STRADDLE) + § "DERIVED — WRITTEN vs LIVE-PREVIEW" (D-327) · `mechanical-verification-of-derived-code-facts.md` § Coverage-boundedness (D-363) · the north-star § 5 (RC-A…E).
- **Discipline:** the calibration-corpus (`calibration-corpus-non-vacuity-discipline.md`) — the tool change ships WITH a golden-broken/golden-complete selftest. D-349 migration contract — Python authoritative until parity-gated.
- **Invariants:** engine H1-H22 do NOT govern the dev-plane (`tools/CLAUDE.md`); the cache-layout gate enforces H6 (cross-thread straddle) / H12 (padding) / H21 (never reorder a serialized struct — the `--fix` writer touches ONLY DERIVED comment tags, never fields).

---

## HARD rules (unchanged — do NOT trip)
1. **NEVER touch or git-add `Strategies/RegimeDetector.hpp`** — Caramel's WIP (its reds are accepted-known + its 3 completeness findings are hers to convert).
2. **Code bytes NEVER change** — comments-only, `lossless.py`-gated (comment-stripped diff == git HEAD). Verify with the comment-stripped HEAD-vs-working proof before every commit.
3. **MARK, never delete** — a rotted/legacy unit gets `[DEPRECATED]`/`[MARKED_FOR_DELETION]`, never a code deletion.
4. **Function-local structs are tool-EXEMPT** — don't hand-block a struct declared inside a function body.

---

## Capture-completeness (this session — INDEPENDENT-REVIEW ALL-CLEAR, nothing lost)
- **Decisions → LOGGED:** D-363 (C4 scope-in) — decision log (paired sentinels) + plan SCOPE-IN + task #7. No new decisions beyond D-363.
- **Specs → indexed:** 2 NEW DESIGN_SPECS (toolchain-architecture + calibration-corpus) + the mechanical-verification amendment — all enrolled in README/TAG_INDEX; bidirectional sisters via mechanical-verification. (One-way sisters from in-code-documentation-schema → toolchain-arch homed for the /metadata-audit cadence — task #9.)
- **Tool → committed:** the function-local exemption in `check_conversion_completeness.py` + selftest (`bce0b6e`).
- **Tasks:** #5 COMPLETED (the 44 actionable units). #6 (HARD-zero, pending RegimeDetector) · #7 (C4, scoped w/ the mechanism) · #8 (foxtag cutover + plugin, D-358, Caramel's) · #9 (toolchain-spec remainder + bidirectional sister sweep) all pending.
- **Meta-error harvest:** none (only minor communication one-offs — over-framing a template application as a decision; a sloppy "change the code" phrasing that read scarier than the reality. Not recurring shapes; anti-ceremony → no catalog row).
- **Independent review:** ALL-CLEAR (9/9 deliverables VERIFIED on disk; 30/30 changed files confirmed comments-only; voice preserved verbatim; 0 files added to the public tree).

---

## Paste this prompt into a fresh Claude Code session

```
type: handoff-pickup

I'm picking up E.1.2.A — the Phase-C actionable sweep is COMPLETE (6 directories converted + committed, baseline 47→3, only the 3 RegimeDetector WIP reds remain). Next = the north-star toolchain core-utils (C4 DERIVED-write) then the plugin.

Run: /accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-18-E.1.2.A-phaseC-sweep-complete-next-northstar-handoff.md

NEXT ACTION: increment-1 step-1 — extend tools/check_cache_layout.py's refresh_derived to INSERT the filled [SIZE]/[ALIGN]/[CACHE_LINES]/[STRADDLE] tags into a covered struct's EMPTY [DERIVED] (today it only rewrites existing lines — the reason --fix fills 0), shipped WITH a calibration selftest. Python-authoritative per D-349; foxtag update is the later parity-gated cutover. Then step-2 = coverage (probe TU + RC-A instantiation anchor).

HARD rules: (1) NEVER touch/git-add Strategies/RegimeDetector.hpp — Caramel's WIP. (2) Code bytes NEVER change — comments-only, lossless-gated. (3) MARK never delete ([DEPRECATED]). (4) Function-local structs are tool-EXEMPT.

The plugin UX (cursor-track / HUD-PANEL / unit CARD / doc-viewer) is Caramel's session (north-star §6, D-353) — you build the fact-producers, meet at the tagadapter contract. Do NOT trust prior-session memory — verify against current code.
```

## Quick links
- North-star (the target IDE): `DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md` (§5 toolchain RC-A…E · §6 plugin UX · §8 gap-to-current)
- The DERIVED-writer to extend: `tools/check_cache_layout.py` (`refresh_derived`)
- Toolchain orientation: `tools/CLAUDE.md` · Schema SSoT: `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md`
- The completeness gate: `tools/check_conversion_completeness.py` (`--selftest` · `--baseline tools/lib/completeness_baseline.txt`)
- Plan (the whole phase): `plans/v5.15-live-readiness/subplans/2026-07-05-E.1.2.A-comment-system-and-doc-consolidation.md`
