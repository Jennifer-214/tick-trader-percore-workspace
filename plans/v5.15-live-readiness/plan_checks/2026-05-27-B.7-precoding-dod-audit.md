---
type: audit-report
audit_type: dod-audit
audit_skills: [/dod-audit]
target_plan: plans/v5.15-live-readiness/subplans/2026-05-27-v5.15.5.F.4d.1.B.7-bugfix-cleanup-umbrella-close.md
ship_tag: v5.15.5.F.4d.1.B.7
severity: yellow
verdict: YELLOW
findings_count: 9
established: 2026-05-27
---

# /dod-audit report — v5.15.5.F.4d.1.B.7 doc-side amendments + C1 close-out scoping

## Audit scope

3-agent pre-coding audit gate fire (Option C). Audit scoped per orchestrator prompt to:

1. **E1 doc-tag-vocabulary amendment** — ~12 CONCERN + ~7 SURFACE tags + `ci-tools` → `ci-tooling` rename
2. **B17 Stage 2 → Stage 3 promotion** — `implementation-layer-blindspot-taxonomy.md` B17 pillar (Class 34)
3. **C1 close-out CLAUDE.md scoping** — file-size discipline scoping amendments
4. **TECH_DEBT close-out scope** — 5 entries → `wontfix-per-ai-workflow`
5. **`tools/calls_graph_diff.sh` post-.B.6 update** — SHARDED_FILES list extension

**Method:** Walked DESIGN_SPECS catalog dynamically against plan body amendments; cross-referenced each amendment to canonical pattern body; verified bidirectional sister-link discipline; checked Stage-promotion criteria per `pattern-codification-lifecycle.md`; cross-checked plan amendment list against /metadata-audit synthesis recommendations.

**Verdict: YELLOW** — 9 findings; 2 BLOCKING (clarification needed before Phase B coding); 4 amendment-shape clarifications; 3 advisory. None CRITICAL. Plan body is broadly sound; clarifications keep amendments mechanically clean.

---

## Per-amendment verdict summary

| Amendment | Verdict | Notes |
|---|---|---|
| E1 doc-tag-vocabulary | YELLOW | Plan says "RENAME `ci-tools` → `ci-tooling`" but vocab ALREADY uses `ci-tooling`. Actual fix is a single-consumer alignment, not a vocab rename. + 2 axis-confusion fixes (concern vs surface tag misuse) need explicit per-tag rationale. |
| B17 Stage 2→3 promotion | GREEN | Promotion criteria met per `pattern-codification-lifecycle.md` Stage 3 (first canonical landed); Class 34 reciprocity verified both directions; Worked Examples accurate. |
| C1 CLAUDE.md scoping | YELLOW | Scoping language preserves test 5K rule explicitly (good); but C1 RATIONALE only lives in plan body + scoping prose. Needs durable codification (DESIGN_SPEC section + memory rule) to survive plan archival. |
| TECH_DEBT close-outs | GREEN | Per-entry-specific rationale satisfied (-117 = `done-incidentally`; -029/-114/-116/-118 = `wontfix-per-ai-workflow`). Move to `closed.md` per ledger discipline. |
| `calls_graph_diff.sh` fix | YELLOW | Fix shape needs explicit decision: extend SHARDED_FILES list with 4 sub-files (mechanical) OR keep INDEX shim + use compile-time include expansion (more robust to future sub-splits). BLOCKING decision before Phase C.4. |

---

## Detailed findings

### F1 — [BLOCKING] E1 `ci-tools` → `ci-tooling` framing is a CONSUMER alignment, not a vocab RENAME (HIGH)

**Plan body (line 194, 254-255):** "RENAME `ci-tools` → `ci-tooling`"

**Actual state:**
- `doc-tag-vocabulary.md` SURFACE axis (line 75) already lists `ci-tooling` — NO `ci-tools` row exists.
- `ci-tooling` is in production use at 3 sites (`audit-report-format.md`, `runtime-toggleable-bench-gate-pattern.md`, `meta-registry-pattern-for-codebase-registry-discipline.md`).
- `ci-tools` is in use at EXACTLY 1 site: `meta-disciplines/structural-enforcement-when-memory-insufficient.md` surface line.

**Discrepancy:** Plan body language implies vocab has `ci-tools` (to be renamed). Actual fix is to UPDATE the single consumer to use the existing canonical `ci-tooling` term. No vocab amendment is needed for THIS axis — the consumer file just needs `surface: [doc-pipeline, ci-tools, plan-discipline]` → `surface: [doc-pipeline, ci-tooling, plan-discipline]`.

**Sister to:** `categorical-triggers-in-always-loaded-docs.md` — vocab discipline says single canonical term per surface; tracker drift is the failure mode.

**Recommended amendment:** Plan body § E1 should reframe "RENAME `ci-tools` → `ci-tooling`" to "ALIGN `structural-enforcement-when-memory-insufficient.md` surface tag `ci-tools` → existing canonical `ci-tooling`". The /metadata-audit L2 row 1 (line 142) correctly states "RENAME to existing `ci-tooling`" — plan body mis-summarized this. This impacts the operator-visible amendment list at § DESIGN_SPECS landed/amended (line 194).

**Severity:** HIGH (clarification BLOCKING for amendment correctness — operator would otherwise add a non-existent `ci-tools` row to vocab).

---

### F2 — [BLOCKING] `calls_graph_diff.sh` fix shape needs explicit decision (HIGH)

**Plan body (line 280, 357-359):** "update SHARDED_FILES list (or similar) to include `CoreFrameworks/EngineSharded/*.hpp`"

**Actual state per inspection of `tools/calls_graph_diff.sh`:**
- `SHARDED_FILES` list at line 26 contains `CoreFrameworks/EngineSharded.hpp` (now the 5-code-line INDEX shim post-`.B.6`).
- Tool uses `grep -hE` on raw file text — does NOT follow `#include` transitively.
- Post-`.B.6` impact: `extract_calls "CoreFrameworks/EngineSharded.hpp"` returns ~0 matches (only the 4 `#include` lines remain). The 1,956 code-LOC across Boot/SlowPath/Async/Run sub-files is INVISIBLE to the orphan-detection scope.
- Class 1 (orphan-after-architectural-migration) detection scope is BROKEN — any strategy function called only from inside `EngineSharded/*` sub-files would falsely register as orphan candidate.

**Two fix options (operator decision needed before Phase C.4):**

| Option | Description | Trade-off |
|---|---|---|
| **(a) Glob expand SHARDED_FILES** | Replace literal `EngineSharded.hpp` with shell glob `CoreFrameworks/EngineSharded/*.hpp` (5 sub-files). Mechanically simplest; matches scope intent. | Future EngineSharded subfolder additions auto-included (good); if SHIM file later contains real symbols, those are missed (low risk). |
| **(b) Add INDEX awareness** | Detect INDEX shims via `splits_into:` frontmatter (or `#pragma once`-only-then-includes signature) + auto-expand to sub-files. Sister to file-size-split-discipline canonical pattern. | More general (future-proof for additional subfolder splits at TECH_DEBT-117/-118 if those land); higher complexity now; needs a sister `tools/` helper or convention. |

**Recommended:** (a) at this ship for mechanical simplicity matching MEDIUM ship risk profile; queue (b) as TECH_DEBT entry if future subfolder splits land (per `file-size-split-discipline.md` v1.3 frozen subfolder pattern). Plan body Phase C.4 (line 357) should commit to (a) explicitly.

**Sister pattern:** None canonical yet — first "INDEX shim → tool consumer adaptation" surface. Codification candidate IF tool-side adaptation becomes a recurring class (currently 1 instance; not yet a pattern).

**Severity:** HIGH (BLOCKING for Phase C.4 — fix shape ambiguous; Class 1 detection scope is currently broken and the ship doesn't fix it without commitment).

---

### F3 — [YELLOW] C1 close-out rationale lacks durable codification (MEDIUM)

**Plan body claim (line 273):** "DISCIPLINE APPLIES SELECTIVELY in AI-driven solo workflow per 2026-05-27 rethink — see CLAUDE.local.md going-forward rule for current scoping"

**Issue:** C1 rationale lives in 4 places — TECH_DEBT close-out rationale strings + CLAUDE.md scoping prose + CLAUDE.local.md going-forward rule amendment + `file-size-split-discipline.md` v1.4 new "AI-driven workflow scoping" section. All 4 carry partial-context phrasing.

**Per `pattern-codification-lifecycle.md` Stage 5 + `categorical-triggers-in-always-loaded-docs.md`:** A categorical scoping decision (when discipline applies vs not) IS a recurring class — future disciplines may need similar AI-workflow scoping. C1 is the first instance.

**Missing:**
1. The `file-size-split-discipline.md` v1.4 new section should be FULL standalone rationale (3-5 paragraphs) per DESIGN_SPEC structural completeness — NOT just "see going-forward rule".
2. Sister discipline candidate: NEW DESIGN_SPEC `ai-workflow-discipline-scoping.md` OR section in existing `categorical-triggers-in-always-loaded-docs.md` covering "when canonical disciplines get rescoped under AI-driven workflows" — codification of the C1 SHAPE (not just instance). Operator triage required for full Stage 2 DRAFT-level codification (NEW DESIGN_SPEC) vs simpler section-addition. Per `feedback_proactive_novel_alternative_consideration` Stage 2 promotion gate (1st instance only), defer NEW DESIGN_SPEC creation until 2nd instance surfaces (similar discipline rescoping). This ship can capture C1 rationale in `file-size-split-discipline.md` v1.4 alone — sufficient for 1st-instance preservation.

**Recommended amendment:** Plan body § DESIGN_SPECS landed/amended (line 195) should explicitly require `file-size-split-discipline.md` v1.4 NEW section "AI-driven workflow scoping" be SELF-CONTAINED (3-5 paragraph rationale, NOT pointer to going-forward rule), with frozen-state note for subfolder pattern. CLAUDE.local.md amendment then cites the DESIGN_SPEC section.

**Severity:** MEDIUM (rationale-loss risk if plan archives; durable codification location ambiguous).

---

### F4 — [YELLOW] B17 Stage 3 promotion needs `first_canonical_application` frontmatter field (MEDIUM)

**Plan body claim (line 259):** "edit `implementation-layer-blindspot-taxonomy.md` B17 pillar frontmatter `stage: 2-draft` → `stage: 3-first-canonical`; add `first_canonical_application: v5.15.5.F.4d.1.B.6` field"

**Actual state of `implementation-layer-blindspot-taxonomy.md` frontmatter:**
- Top-level frontmatter has `stage: 3-first-canonical` (set 2026-05-18 for the parent taxonomy itself).
- B17 lives as a PILLAR entry under `pillars:` YAML list (line 14): `- B17: forward-decl-namespace-shadow (Stage 2 DRAFT 1st-instance at v5.15.5.F.4d.1.B.6 Phase B.3 SlowPath.hpp + Phase B.2 Async.hpp; sister to Class 34)`.

**Issue:** Pillar Stage progression is encoded in pillar STRING DESCRIPTION (not a structured per-pillar frontmatter field). The frontmatter `stage:` field is for the TAXONOMY-LEVEL stage, not per-pillar. Plan body amendment instruction "stage: 2-draft → stage: 3-first-canonical" is ambiguous — does it mean:
- (a) Update the string description inside `pillars:` list entry?
- (b) Add a NEW per-pillar structured field `b17_stage: 3-first-canonical`?
- (c) Add B17 Stage 3 promotion to body text Stage progression section (§ Codification lifecycle, line 433)?

**Per `pattern-codification-lifecycle.md` Stage 3 criteria** (line 91-97): Stage 3 = first canonical reference site lands. B17 has 2 canonical applications same ship (SlowPath.hpp + Async.hpp) — exceeds Stage 3 (which is single first-canonical); could even argue Stage 4 (cohort applications proving reusability across surfaces). Recommend: amend pillar string description (option a) to `B17: forward-decl-namespace-shadow (Stage 3 FIRST CANONICAL at v5.15.5.F.4d.1.B.6 Phase B; 2 same-ship canonical applications: SlowPath.hpp steady_clock + Async.hpp CandleAccumulator; sister to Class 34)`.

**Sister verification (cross-reference reciprocity):**
- B17 pillar (line 346-366) references Class 34 sister memory + sister codification — GOOD.
- `class-34-forward-decl-namespace-shadow.md` (line 127) references taxonomy B17 — GOOD.
- Reciprocity VERIFIED both directions.

**Recommended amendment:** Plan body § E2 (line 259) should clarify amendment as "edit `implementation-layer-blindspot-taxonomy.md` `pillars:` list line 14 — update B17 description from `(Stage 2 DRAFT ...)` to `(Stage 3 FIRST CANONICAL at v5.15.5.F.4d.1.B.6 Phase B; 2 same-ship canonical applications)`; update body text § Codification lifecycle table if applicable". No new frontmatter field needed.

**Severity:** MEDIUM (amendment shape unclear; could surface as drift if applied differently than intended).

---

### F5 — [YELLOW] B17 Stage 4 cohort promotion eligibility — borderline (LOW)

**Per `pattern-codification-lifecycle.md` Stage 4 criteria** (line 102-110): Stage 4 = cohort applications proving reusability across surfaces. B17 has 2 same-ship instances at the same surface category (header extraction); both same Phase B work.

**Question:** Does "same Phase B work" count as cohort (multiple surfaces same week) or as "single canonical first reference" (Stage 3 only)?

**Per `class-34-forward-decl-namespace-shadow.md`:** `recurrence_count: 2`. Class file documents 2 instances. By Class 34 documentation discipline, Stage 4 cohort criterion is borderline-met.

**Recommended:** KEEP B17 at Stage 3 first canonical for this ship; revisit at next surface (next subfolder split per file-size-split-discipline.md frozen pattern). Promotion-by-staleness is the conservative call per `feedback_audit_canonical_sister_before_new_infra` 2-instance threshold (Stage 3 fits "2 instances same ship" cleanly; Stage 4 fits "2+ instances DIFFERENT ships at DIFFERENT surface categories"). Plan body decision align with conservative posture (Stage 3 only).

**Severity:** LOW (advisory; no blocker).

---

### F6 — [YELLOW] Plan body axis-confusion fixes need per-tag explicit rationale (MEDIUM)

**Plan body (line 198):** "4 axis-confusion fixes per /metadata-audit (HIGH-2): cpp17-inline-variable...md (`data-discipline` → CONCERN axis); cfg-field-categorization-discipline.md (`cfg-flow` → SURFACE; `h17-h14`/`dod` → `data-oriented-design`)"

**Per /metadata-audit H1 (line 49-55):**
- `data-discipline` is a DOC TYPE used as CONCERN tag → axis confusion. /metadata-audit recommends DELETE from tags (line 122).
- `cfg-flow` is a SURFACE tag used as CONCERN tag → axis confusion. /metadata-audit recommends RENAME to correct axis (line 121).
- `dod` → rename to `data-oriented-design` (already in vocab; line 125).
- `h17-h14` → DELETE (invariant codes not tag-axis appropriate; line 126).

**Issue:** Plan body summarizes the 4 fixes as one bullet without per-tag verdict. The fixes are NOT symmetric:
- `data-discipline` = DELETE from tags (axis confusion; no replacement)
- `cfg-flow` = MOVE from CONCERN to SURFACE axis
- `dod` = RENAME to `data-oriented-design`
- `h17-h14` = DELETE (invariant codes not vocabulary-axis appropriate)

**Recommended:** Plan body § DESIGN_SPECS landed/amended (line 198) should split into 4 per-tag bullets with verb (DELETE / RENAME / MOVE-AXIS) + rationale (axis-confusion / vocab-canonical / invariant-code-misuse). Mechanical-amendment clarity prevents drift.

**Severity:** MEDIUM (ambiguous amendment shape across 4 distinct fixes).

---

### F7 — [GREEN] TECH_DEBT close-out per-entry rationale satisfied

**Plan body § E.1 (line 383):** Each entry gets specific rationale per close-kind:
- TECH_DEBT-029 / -114 / -116 / -118: `wontfix-per-ai-workflow` (C1 rationale; AI-driven solo workflow rationale; test 5K rule retained explicitly).
- TECH_DEBT-117: `done-incidentally` (RECURRING_BUG_PATTERNS.md already split at .B.6 prep; different rationale; correct).
- TECH_DEBT-131: AMEND (scope 7 → 5 ACTIONABLE after Phase A.3 classification per `feedback_archived_changelog_preservation_discipline`); then CLOSE.

**Per `ledger-entry-templates.md` close-out discipline:** Each close-out must cite (1) close-kind (`done` / `wontfix` / `done-incidentally` / `dropped`), (2) per-entry rationale (not boilerplate), (3) move to `closed.md`. All 5 satisfy.

**Sister patterns considered:**
- `feedback_archived_changelog_preservation_discipline` — TECH_DEBT-131 amendment correctly preserves ARCHIVED + CLOSED-historical refs (16 of 21 sites left alone). Sister discipline applied correctly.
- `feedback_operator_facing_doc_cohort_at_cfg_deletion` — TECH_DEBT-131 retroactive scope expansion is the cohort-discipline application; correct.

**Verdict:** GREEN; no amendment needed.

---

### F8 — [YELLOW] Doc-tag-vocabulary new-tag set spans 3 codification cohorts (LOW)

**Plan body new tags (line 192-194):**
- CONCERN (~12): `ssot`, `cpp17`, `header-only`, `shared-state`, `linker-deduplication`, `mirror-prevention`, `code-loc-counting`, `inline-variable`, `forward-decl-shadow`, `block-scope-statics`, `subfolder-pattern`, `wontfix-rationale`
- SURFACE (~7): `doc-pipeline`, `plan-pipeline`, `handoff-pipeline`, `header-split`, `helper-extraction`, `session-pickup`, `skill-pipeline`

**Cohort analysis:**
- **`.B.4` codification cohort (already landed but vocab lagged):** `ssot`, `mirror-prevention`, `linker-deduplication` (probably also `cpp17`, `inline-variable`) — already in production use in `.B.6` new DESIGN_SPECS.
- **`.B.6` codification cohort:** `forward-decl-shadow`, `block-scope-statics`, `subfolder-pattern`, `header-split`, `header-only`, `shared-state`, `code-loc-counting` — surfaced from B17/B18 pillars + sister DESIGN_SPECS.
- **`.B.7` PROPOSED cohort:** `wontfix-rationale` — NEW concept; first instance at C1 close-out (no canonical application yet beyond `.B.7` itself).

**Per `doc-tag-vocabulary.md` § Adding new tags (line 173-181):** "First doc to introduce a tag is fine — usefulness emerges over time." NO min-count gatekeeping. So technically all 19 tags are eligible to ADD now.

**But per `categorical-triggers-in-always-loaded-docs.md` discipline:** Tags should describe RECURRING CATEGORIES. `wontfix-rationale` is fine if it lands in `.B.7` close-out usage + future close-outs reuse; if `.B.7` is the only use forever, it's a singleton candidate per `/metadata-audit` L1 (line 114-136).

**Recommended:** ACCEPT all 19 additions per vocabulary discipline (no gatekeeping); revisit at next quarterly `/metadata-audit` for singleton-consolidation. Note `wontfix-rationale` is the only single-application proposal; document its use in the vocab row explicitly to make sister applications discoverable.

**Severity:** LOW (advisory; vocab discipline already permits this).

---

### F9 — [YELLOW] Bidirectional sister-link sweep scope (4 confirmed; full sweep deferred) (LOW)

**Plan body (line 199):** "Bidirectional sister-link sweep (4 confirmed + ~10-20 likely): add back-refs in cache-line-discipline.md, structural-fix-preferred-decision-framework.md, canonical-sister-extension-discipline.md, pattern-codification-lifecycle.md, implementation-layer-blindspot-taxonomy.md, file-size-split-discipline.md"

**Per /metadata-audit M1 (line 94-104):** 4 confirmed asymmetries; full sweep needs `--bidirectional` flag with permission grant. Plan body acknowledges this in NOT-IN-scope (line 297): "Bidirectional sister-link full sweep (operator perm needed for tools/check_doc_metadata.py --bidirectional) → Inline-fix the 4 confirmed; full sweep on operator re-run with permission grant".

**Issue:** The 4 confirmed asymmetries are real concrete amendments; the 10-20 likely ones are estimates. Plan body Phase D.7 (line 374-375) commits to "4 confirmed sites"; full sweep deferred is appropriate.

**Concern:** None — defer is justified per `feedback_no_defer_for_effort` exception (perm-grant blocker, not effort-avoidance).

**Recommended:** ACCEPT defer; explicitly cite the 4 specific asymmetries (per /metadata-audit M1 table) in plan body Phase D.7 step instructions so amendment is mechanically unambiguous. Currently plan lists 6 sites (line 199) but /metadata-audit M1 lists 4 asymmetries (line 98-102) + an additional taxonomy↔cpp17 back-ref (line 104). Cross-check that plan's 6 sites equal /metadata-audit's 4+1=5 confirmed sites + 1 extra.

**Severity:** LOW (advisory; scope alignment with audit findings).

---

## Cross-pattern verification — DESIGN_SPECS discipline applied

| Discipline | Verdict | Notes |
|---|---|---|
| `pattern-codification-lifecycle.md` Stage 3 criteria | GREEN | B17 promotion meets Stage 3 first canonical (2 same-ship applications); promotion language correctly applied per F4 amendment recommendation |
| `categorical-triggers-in-always-loaded-docs.md` | YELLOW | C1 scoping language preserves test 5K rule explicitly; F3 flags durable-codification gap |
| `doc-tag-vocabulary.md` § Adding new tags | GREEN | No min-count gatekeeping; 19 additions eligible |
| `doc-frontmatter-convention.md` § Greppability | YELLOW | F6 flags multi-tag axis-confusion fixes that need per-tag rationale; plan summarizes 4 fixes as 1 bullet |
| `canonical-sister-extension-discipline.md` | GREEN | TECH_DEBT-131 retroactive scope expansion correctly applies sister-discipline (`feedback_operator_facing_doc_cohort_at_cfg_deletion`); CI tool extension at Phase B.4 is sister-extension (`canonical-sister-extension-discipline.md`) not new-tool |
| `file-size-split-discipline.md` v1.3 → v1.4 amendment shape | YELLOW | F3 flags v1.4 "AI-driven workflow scoping" section needs FULL self-contained rationale (3-5 paragraphs), not pointer to going-forward rule |
| `feedback_archived_changelog_preservation_discipline` | GREEN | TECH_DEBT-131 Phase A.3 classification correctly preserves 16 ARCHIVED + CLOSED-historical sites |

---

## Blocking findings before Phase B unlock

| # | Finding | Resolution required |
|---|---|---|
| F1 | E1 `ci-tools` framing | Operator clarifies: amendment is single-consumer ALIGN to `ci-tooling`, NOT vocab RENAME. Update plan body § DESIGN_SPECS landed/amended language to match. |
| F2 | `calls_graph_diff.sh` fix shape | Operator decides (a) glob expand vs (b) INDEX awareness. Plan body Phase C.4 step instruction explicit per choice. |

**Non-blocking (informational):**
- F3 (C1 rationale codification) — recommend amend before C1 close-out work starts
- F4 (B17 promotion mechanism) — recommend clarify amendment shape (pillar string vs frontmatter field)
- F5 (B17 Stage 4 borderline) — recommend stay at Stage 3
- F6 (axis-confusion fixes) — recommend per-tag rationale bullets
- F8 (`wontfix-rationale` singleton) — accept per vocab discipline; revisit at next /metadata-audit
- F9 (bidirectional sister-link scope) — verify plan's 6 sites equal /metadata-audit's 5 confirmed

---

## DOD pattern application — missed applications

No DESIGN_SPECS pattern was missed in the plan body amendments. All amendments correctly apply:
- `pattern-codification-lifecycle.md` (Stage 3 for B17; deferral for Stage 4)
- `canonical-sister-extension-discipline.md` (Check 7 extension as sister to existing tool, NOT new tool)
- `feedback_operator_facing_doc_cohort_at_cfg_deletion` (TECH_DEBT-131 retroactive scope)
- `feedback_archived_changelog_preservation_discipline` (TECH_DEBT-131 Phase A.3 classification)
- `feedback_audit_canonical_sister_before_new_infra` (Check 7 extension over new tool)
- `file-size-split-discipline.md` (C1 scoping amendment preserves subfolder Stage 3 frozen status)

---

## Recommendations (operator triage)

**Must-fix before Phase B coding (BLOCKING):**

1. **F1** — Reframe E1 `ci-tools` amendment as single-consumer alignment (one file edit), NOT vocab rename. Update plan body line 194 + 254-255 wording.
2. **F2** — Decide `calls_graph_diff.sh` fix shape (option a glob OR option b INDEX awareness). Update plan body Phase C.4 (line 357-359) with explicit instruction.

**Recommended amendments (NON-BLOCKING but high-value):**

3. **F3** — Amend plan body § DESIGN_SPECS landed/amended (line 195) to require `file-size-split-discipline.md` v1.4 "AI-driven workflow scoping" section be FULL self-contained rationale (3-5 paragraphs) preserving C1 institutional knowledge durably; CLAUDE.local.md going-forward rule cites DESIGN_SPEC section.
4. **F4** — Clarify B17 promotion amendment shape (line 259): edit `pillars:` list string description; no new frontmatter field needed.
5. **F6** — Split 4 axis-confusion fixes (line 198) into per-tag bullets with verb + rationale.

**Advisory (defer or accept):**

6. **F5** — KEEP B17 at Stage 3 first canonical for this ship.
7. **F8** — ACCEPT all 19 vocab additions; revisit `wontfix-rationale` singleton at next /metadata-audit.
8. **F9** — Verify plan body 6-site sister-link sweep list equals /metadata-audit M1 5 confirmed sites.

---

## Verification + sanity-check status

- **Read DESIGN_SPECS catalog dynamically:** `doc-tag-vocabulary.md` v1.0, `implementation-layer-blindspot-taxonomy.md` v1.3, `file-size-split-discipline.md` v1.3, `pattern-codification-lifecycle.md` v1.0
- **Read sister DESIGN_SPECS:** `categorical-triggers-in-always-loaded-docs.md`, `class-34-forward-decl-namespace-shadow.md`
- **Read audit reports:** `/metadata-audit` 2026-05-27 (YELLOW DRIFT-36)
- **Inspected `tools/calls_graph_diff.sh`:** SHARDED_FILES at line 26 confirmed monolithic EngineSharded.hpp scope
- **Cross-referenced ci-tools/ci-tooling usage:** `rg "ci-tools"` returned 3 files (vocab + plan + 1 consumer); `rg "ci-tooling"` returned canonical 3-site usage
- **Verified reciprocity:** B17 ↔ Class 34 cross-references reciprocate both directions
- **Verified Stage promotion criteria:** Per `pattern-codification-lifecycle.md` Stage 3 (first canonical) for B17; Stage 4 (cohort) borderline-met but conservative posture recommended
- **NOT verified this pass:** Full bidirectional sister-link sweep (per /metadata-audit defer); per-row file-size-split-discipline.md v1.4 section content (not yet drafted)

---

**End of /dod-audit report 2026-05-27.** Verdict YELLOW; 2 BLOCKING findings (F1 + F2) require operator clarification before Phase B coding starts; 4 NON-BLOCKING amendment-shape recommendations improve mechanical precision; 3 advisory items defer or accept. Plan body is broadly sound — DOD discipline correctly applied across all amendments; gaps are clarity/codification-durability, not pattern misapplication.
