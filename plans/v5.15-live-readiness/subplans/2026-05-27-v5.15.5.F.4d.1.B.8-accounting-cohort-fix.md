---
type: subplan
ship_tag: v5.15.5.F.4d.1.B.8
ship_kind: mixed (hotfix + structural enforcement + catalog amendment + anti-pattern codification + skill amendment)
status: DRAFT v1.2 — amended 2026-05-27 PM post cycle-2 inflection check (/readiness + /blindspot-scan re-fire on v1.1)
predecessor: v5.15.5.F.4d.1.B.7 (Class 26 ×2 hotfix + cleanup + C1 close-out SHIPPED 2026-05-27)
successor: v5.15.5.F.4d.1.C (per-core override emission CLI; 2nd canonical of framework-driven-cli-binary-pattern; QUEUED)
parent_umbrella: n/a — standalone post-ship hotfix (sister-bug surface closure for the `ea08210` mechanical migration cohort)
sprint: v5.15-live-readiness
established: 2026-05-27
last_amended: 2026-05-27 (cycle 2 inflection — v1.1 → v1.2 closes 4 recursive sister-cohort findings from /blindspot-scan re-fire)
risk: HIGH (silent realized-P&L drift in production accounting paths; per-core fee-floor miscalibration affects ratchet-SL + breakeven-on-profit + Strategy ratchet helper shared across MR/Momentum/EmaCross/MLStrategy + ControllerEventLoop call + GUI diag display)
audit_tier: HIGH-RISK  # 5-agent /precoding-audit-gate per /readiness Check 34 — P&L drift criticality + multi-file fix + new CI check + Class catalog amendment + comprehensive cohort enumeration + NEW Phase H anti-pattern codification + skill amendments
effort_estimate: ~5.5-7.5h focused single-substantive-path (Phase A ~45 min + Phase B ~30 min + Phase C ~60 min + Phase D ~30 min + Phase E ~30 min + Phase F ~30 min + Phase G ~60 min + Phase H ~95-125 min per v1.1 cycle-2 calibration)
hot_path: UNTOUCHED (all 4 HIGH sites slow-path EventLoop_* + Strategy ratchet helper + GUI diag; MED-1 site is TUI snapshot publication; LOW-1 site is DrainerConstants init; NOT BG_Evaluate/SG_Evaluate/ExecutionCore_Tick; verified via tools/calls_graph_diff.sh post-fix)
audit_synthesis:
  - plan_checks/2026-05-27-B.8-precoding-parity-check.md (GREEN-with-discoveries — 0 BLOCKING / 1 HIGH-NEW LEGACY-EXEMPT / 1 MED / 2 LOW / 2 INFO verified)
  - plan_checks/2026-05-27-B.8-precoding-trace-deps.md (YELLOW — 0 BLOCKING / 2 HIGH-NEW / 3 MED / 2 LOW; HIGH-3 caller count error 3→5 callers)
  - plan_checks/2026-05-27-B.8-precoding-dod-audit.md (GREEN — 0 findings + 2 OBS; all H4/H6/H7/H12/H15/H20 PRESERVED)
  - plan_checks/2026-05-27-B.8-precoding-readiness.md (YELLOW — 0 BLOCKING / 5 MED / 3 LOW; M7 spec drift Check 9 retroactive + Check 10)
  - plan_checks/2026-05-27-B.8-precoding-blindspot-scan.md (YELLOW — 0 BLOCKING / 4 HIGH / 3 MED / 2 LOW; Phase A scope gap + Check 10 3-layer detection + Class catalog cohort gap + DOCUMENTED-RISK forward-promise gap)
  - plan_checks/2026-05-27-B.8-precoding-readiness-v1.1.md (GREEN READY-TO-CODE — all v1.0 5 MED closed + 1 of 3 LOW; 2 LOW carry; 0 NEW; 18 code blocks B-Plus verified 0 fabrications)
  - plan_checks/2026-05-27-B.8-precoding-blindspot-scan-v1.1.md (YELLOW — 4 NEW recursive sister-cohort findings from Phase H expansion; B19 Option C fold subsumes need for new pillar; cycle 2 SMALLER+DIFFERENT-SHAPE = NOT spiral; v1.2 closes all 4)
sister_specs:
  - meta-disciplines/structural-enforcement-when-memory-insufficient.md (M7; Check 10 = next canonical application; sister to Check 9 paired-access detection; v1.2 → v1.3 at this ship — adds Check 9 retroactive + Check 10)
  - meta-disciplines/canonical-sister-extension-discipline.md (v1.0 → v1.1 at this ship — adds 2nd CI-tooling-surface application: Check 10 sister to Check 9; complements existing REGISTRY-surface applications Path γ + Path γ #2)
  - meta-disciplines/sister-cohort-amendment-completeness-discipline.md (NEW Stage 2 DRAFT at this ship — codifies sister-catalog + sister-ledger + sister-skill amendment cohort discipline when amending any Class N catalog)
  - framework-patterns/cfg-field-categorization-discipline.md (Stage 3 first canonical at .B.4; fee_rate_taker is PER_CORE_FLAT_SYNC_PARAMETER; consumer-side gap discipline)
  - refactor-patterns/decision-time-data-binding-pattern.md (Class 27 closure sister; per-core consumer reads per-core slot)
  - refactor-patterns/cfg-scope-discipline.md (per-core fields read via core_id index, never as scalar)
  - meta-disciplines/structural-fix-preferred-decision-framework.md (parent meta-rule; Class 26 recurrence 13→17 at .B.8 close)
tags: [hotfix, structural-enforcement, accounting, pnl-drift, per-core-indexing, class-26-closure, m7-canonical, fee-floor, anti-pattern-codification, skill-amendment]
surface: [slow-path, oms-drainer, fee-floor, per-core-indexing, ci-tooling, gui-diag, strategy-ratchet, display, doc-pipeline, skill-pipeline]
---

# v5.15.5.F.4d.1.B.8 — accounting cohort fix + Stage 6 Check 10 escalation + Phase H anti-pattern codification + skill amendments — plan body

**Branch:** `feat/v5.15-live-readiness`
**Pre-tag rollback anchor:** `pre-v5.15.5.F.4d.1.B.8` (create at Step 0 of coding)
**Status:** **READY-FOR-ACTIVE-CODING (v1.2; 2026-05-27 PM)** — cycle 3 /blindspot-scan inflection check GREEN; CONVERGED; fixpoint reached; iteration trajectory 9 → 4 → 0 findings (steep convergence per `feedback_iteration_spiral_signals_audit_meta_gap`). Plan body promoted DRAFT v1.2 → ACTIVE post operator approval + cycle-3 GREEN verdict. Phase A starts post Step-0 ritual (pre-tag rollback anchor + /sync-workspace).

## Amendment log (v1.0 → v1.1 → v1.2)

Per `feedback_session_decision_log_discipline` — amendment cycle 1 (v1.0 → v1.1) folded 13 punch-list items from 5-agent audit synthesis + added NEW Phase H per Caramel directive. Amendment cycle 2 (v1.1 → v1.2) folds 4 recursive sister-cohort findings from /blindspot-scan v1.1 re-fire (predicted by re-fire prompt — Phase H scope expansion triggered sister-cohort recursion). Single-cycle inline log shape per discipline; no separate decision-log file needed yet (cycle 3+ trigger).

### v1.0 → v1.1 amendments (5-agent precoding-audit-gate synthesis):

| Source | ID | Amendment | Section affected |
|---|---|---|---|
| /trace-deps | HIGH-3 | Strategy_WriteRatchetSL caller count corrected 3→5 (MLStrategy.hpp:314 + ControllerEventLoop.hpp:2530 missed) | Why this ship exists / Phase A / Phase E |
| /trace-deps + /parity-check (convergent) | EmaCross | EmaCross.hpp:143-144 explicit LEGACY-EXEMPT classification | Phase A |
| /trace-deps | MED-3 | Check 10 whitelist refined to `{c, core, core_id, slot}` primary + anti-confusion note (`idx`/`pos` NOT per-core; `i` heuristic boot-time only) | Phase C |
| /trace-deps | LOW-3 | Check 10 aliasing handling for `resolved_cfg.X` stack-local copy; Phase C step 7 sanity-verify reverts HIGH-4 too | Phase C |
| /parity-check | MED | ShardedSnapshot.hpp:139 KEEP-AS-GLOBAL extension (sister to existing 328-331) | Phase B |
| /parity-check | LOW | Decision A novel-alt enhancement — cite CoreFrameworks/EngineSharded/SlowPath.hpp:63-68 as sister-canonical precedent | Design space |
| /dod-audit | OBS-1 | DrainerConstants.hpp file header comment block ("Total 24B" → "16B") post-LOW-1 | Phase B |
| /readiness | MED-1 | Create M6 CSV sidecar `plan_checks/2026-05-27-B.8-check10-body-content-enumeration.csv` for Check 10 scan fn args | Phase C |
| /readiness | MED-2 | M7 spec `canonical_applications:` add BOTH Check 9 (retroactive — `.B.7` doc drift) AND Check 10 (5th + 6th entries) | Phase D |
| /readiness | MED-5 | MeanReversion `Strategy_WriteRatchetSL` actual call site verification at Phase A.5 | Phase A |
| /blindspot-scan | HIGH-1 | Phase A enumeration scope expansion — broader symbol union + lambda/template/macro extraction passes + explicit LEGACY-EXEMPT classification | Phase A |
| /blindspot-scan | HIGH-2 | Check 10 detection 3-layer design (fn-param + per-core-for-loop + lambda-body + member-fn `this->core_id` + indirect per-core via state pointer) | Phase C |
| /blindspot-scan | HIGH-3 | Class catalog amendment cohort — NEW Phase D Steps D.4 (Class 27 sister) + D.5 (Class 25 parent) + D.6 (Class 18 ancestor) | Phase D |
| /blindspot-scan | MED-4 | `canonical-sister-extension-discipline.md` v1.0 → v1.1 with Check 10 as 2nd CI-tooling-surface application | Phase D |
| /blindspot-scan | MED-6 | DOCUMENTED-RISK PARITY entry template inline + close `.B.7` forward-promise gap (Class 26 catalog line 98 promised but no entry exists) | Phase G |
| /blindspot-scan | LOW | DrainerConstants byte-equivalence grep verify before LOW-1 lands; B14 leaves-first ordering inline | Phase B |
| Caramel directive | NEW Phase H.1 | Codify NEW anti-pattern `sister-cohort-amendment-completeness` (memory + Stage 2 DRAFT DESIGN_SPEC + MEMORY.md + CLAUDE.local.md) | NEW Phase H |
| Caramel directive | NEW Phase H.2 | Codify NEW anti-pattern `forward-promise-auto-write-verification` (memory + Stage 2 DRAFT) + extend `/capture-audit` Check 11 (forward-promise verification at next-ship-time) | NEW Phase H |
| Caramel directive | NEW Phase H.3 | Amend `/accounting-audit` SKILL.md — category 2 extended with explicit UNINDEXED-GLOBAL Class 26 sub-shape detection | NEW Phase H |
| Caramel directive | NEW Phase H.4 | Update CLAUDE.local.md going-forward rules + CLAUDE.md cross-refs for NEW disciplines + skills | NEW Phase H |

Net v1.1: v1.0 was sound but missing sister-cohort completeness coverage. v1.1 closes all 13 audit findings + folds Caramel's anti-pattern codification + skill amendment directives so NO open items accumulate as tech debt.

### v1.1 → v1.2 amendments (cycle-2 inflection check; /readiness GREEN + /blindspot-scan YELLOW recursive findings):

| Source | ID | Amendment | Section affected |
|---|---|---|---|
| /blindspot-scan v1.1 | H-RECURSIVE-1 | Add `/dod-audit` SKILL.md to Phase H.2 sister-skill amendment cohort (4 per-core surface citations need Class 26 sub-shape A/B distinction); `/bug-check` auto-handled (reads RECURRING_BUG_PATTERNS dynamically); `/ml-audit` + `/registry-fit-audit` + `/hft-audit` NOT candidates (no Class 26 refs) | Phase H.2 |
| /blindspot-scan v1.1 | M-RECURSIVE-1 | Verification gate: add "Check 11 dogfood — fire NEW Check 11 against `.B.8`'s own forward-promises before ship close" (codifying ship dogfoods the codified discipline; recursive verification that .B.8's NEW Stage 2 DRAFT promotion + Check 11 implementation forward-promises are themselves tracked) | Verification gate + Phase G Step G.7 |
| /blindspot-scan v1.1 | M-EFFORT | Effort estimate calibration: `~5-7h` → `~5.5-7.5h` total; Phase H per-step refined (H.1 20→25-30 min; H.2 30→45-60 min); H.3 + H.4 on-target unchanged | Frontmatter effort_estimate + Phase H step estimates |
| /blindspot-scan v1.1 | L-PILLAR-B14 | Phase B LOW-1 step ordering swap: step 4 (`static_assert`) ↔ step 5 (`_pad[7]`); `static_assert` depends on `_pad` outcome → pure leaves-first ordering | Phase B Step B.6 LOW-1 sub-step list |
| /blindspot-scan v1.1 | B19 Option C | FOLD recursive shape into NEW DESIGN_SPEC `sister-cohort-amendment-completeness-discipline.md` body — covers when amending sister-cohort artifacts ITSELF triggers further sister-cohort enumeration (e.g., amending a Class catalog → sister catalogs need amendment → THOSE may need sister catalogs amended; recursion termination criterion = fixpoint reached when amended cohort has no NEW sister cross-refs). Subsumes need for NEW pillar B19 in implementation-layer-blindspot-taxonomy.md per canonical-sister-extension-discipline (extend spec rather than build parallel pillar) | Phase H.2 NEW DESIGN_SPEC body content |

Net v1.2: v1.1 was sound on plan body completeness (/readiness GREEN) but Phase H scope expansion triggered recursive sister-cohort drift (predicted). v1.2 closes all 4 NEW findings via micro-amendments (~25 min total scope addition). Inflection check at cycle 3 (/blindspot-scan re-fire) expected GREEN by construction since B19 Option C codifies the META.

---

## Why this ship exists

The `.B.7` ship hotfixed Class 26 ×2 instances at `CoreFrameworks/EngineSharded/Async.hpp:814+853` where `ea08210` mechanical migration substituted wrong index variable (`cfg.cores[i]` where `i` was inner ring-pop counter; should have been `cfg.cores[slot]`). Stage 6 structural enforcement via NEW Check 9 (paired-access mismatch detector) closes the WRONG-INDEX sub-shape mechanically.

Post-`.B.7` `/accounting-audit` standalone codebase sweep (recommended next step per `.B.7` handoff Step 2) surfaced **4 NEW HIGH-severity Class 26 instances in accounting fee-floor compute paths**. Same `ea08210` migration cohort; **different sub-shape**:

- `.B.7` closed sub-shape A = WRONG-INDEX paired-access: `cfg.core_overrides[X]` + `cfg.cores[Y]` with X != Y (Check 9 catches mechanically)
- `.B.8` closes sub-shape B = **UNINDEXED-GLOBAL**: `cfg.fee_rate_taker` (no index at all) read at per-core consumer site where `cfg.cores[core_id].fee_rate_taker` is authoritative (Check 9 does NOT catch — no array indexing present to mismatch; Check 10 NEW catches mechanically)

**Findings (full audit report at `plan_checks/2026-05-27-post-B.7-accounting-audit.md`):**

| ID | Severity | File | Lines | Symptom | Effect |
|---|---|---|---|---|---|
| HIGH-1 | HIGH | `CoreFrameworks/ControllerEventLoop.hpp` | 3605-3606 | `EventLoop_TrailingSLRatchetOneCore` reads `cfg.fee_rate_taker` (GLOBAL) while `core_id` is in scope (line 3585) | Trailing SL ratchet fee-floor cap silently flattens to global → ratchet pushes SL too close to entry on high-fee cores → SG-fired exits at near-breakeven gross become NET-NEGATIVE → silent realized-P&L drift per core |
| HIGH-2 | HIGH | `CoreFrameworks/ControllerEventLoop.hpp` | 3670-3671 | `EventLoop_BreakevenOnProfitOneCore` same shape (sister block to HIGH-1) | Net-profit threshold (2× fee_rate_taker) + fee_floor cap (3× fee_rate_taker) silently flattens to global → same family P&L drift |
| HIGH-3 | HIGH | `Strategies/StrategyLifecycle.hpp` | 272-273 | `Strategy_WriteRatchetSL` shared helper reads `cfg->fee_rate_taker` (GLOBAL) while `slot` in scope (line 280 uses `state->cores[slot]` correctly) | Affects **5 callers** (MR/Momentum/EmaCross sharded ratchet + MLStrategy.hpp:314 + ControllerEventLoop.hpp:2530) — fee-floor cap silently flattens per-core. **CORRECTED v1.1: audit cited 3 callers; /trace-deps verified 5 callers.** |
| HIGH-4 | HIGH | `CoreFrameworks/ControllerEventLoop.hpp` | 3042-3043 | `EventLoop_RebuildOneCore` GUI diag capture reads `resolved_cfg.fee_rate_taker` (GLOBAL post-ResolveForCore; `fee_rate_taker` NOT in PER_CORE_OVERRIDE_FIELDS macro) | Display↔execution divergence at fee-floor diag panel — operator sees one fee floor in GUI while execution computes another via `Strategy_TpFloor` at line 3061 (which correctly uses `&resolved_cfg.cores[slot]`) |
| MED-1 | MED | `CoreFrameworks/ShardedSnapshot.hpp` | 249-250 + 328-331 + **NEW 139** | TUI net_pnl + Settings panel display reads global fee_rate while `core_id_for_pos` in scope; **NEW v1.1: line 139 `snap->fee_rate_pct = FPN_ToDouble(cfg->fee_rate)` is engine-wide headline display OUTSIDE per-position loop (per /parity-check MED)** | TUI net_pnl column wrong for cores with differing fee_rates; Settings display shows GLOBAL not per-core; line 139 KEEP-AS-GLOBAL per Settings panel semantic (sister to 328-331) |
| LOW-1 | LOW | `MemHeaders/DrainerConstants.hpp` | 65, 100 + **NEW header comment 5-45** | `fee_rate_taker_d` field WRITTEN at boot but UNREAD post-`.F.4c.3` WIP2d-1.B.1 cache deletion (vestigial Class 27 sub-instance) + **NEW v1.1: file header comment block "Total 24B" → "16B" post-deletion** | No runtime hazard (UNREAD); dead state preserves stale Class 27 anti-pattern in struct; comment block currency drift |

**Why this didn't surface at `.F.4c.3` WIP2d-1.B.1 migration:** the audit pass focused on OMS scalar field deletion (`oms->fee_rate_*` → `o->pre_resolved.fee_rate`). Slow-path fee-floor compute paths were not in the audit cohort. PARITY-032 closure at `.B.4` D1-B added the `EventLoop_BreakevenOnProfitOneCore` call (HIGH-2 surface); both fns pre-date `.F.4c.3` migration but the fee_rate reads inside were not updated when global → per-core flow landed.

**Why memory codification was insufficient (recurrence at codified discipline surface):**
- `feedback_structural_fix_for_recurring_class` (Class 26 MANDATORY structural fix) codified at .B.4 v1.7.6 → DID NOT prevent UNINDEXED-GLOBAL sub-shape introduction (different sub-shape than WRONG-INDEX paired-access)
- Check 9 mechanical detection at .B.7 → COULDN'T detect UNINDEXED-GLOBAL sub-shape (no `cfg.core_overrides[X]` + `cfg.cores[Y]` paired pattern to flag)
- `feedback_cfg_field_categorization_at_registry_add_time` + `feedback_categorize_by_consumer_pattern_not_field_name` codified at .B.4 v1.7.6 → APPLIES at registry-add-time, not at consumer-add-time; doesn't catch consumer-side reads of legacy global form

Per M7 (`structural-enforcement-when-memory-insufficient.md`): recurrence at codified surface = Stage 6 escalation trigger. Check 10 is the structural-enforcement application closing the UNINDEXED-GLOBAL sub-shape mechanically.

**Caller-context confirmation (per audit + /trace-deps verification 2026-05-27):**
- HIGH-1 caller: `EngineCommon.hpp:734` per-core slow-path body with `c` iterator as `core_id` arg → CONFIRMED per-core call frame
- HIGH-2 caller: `EngineCommon.hpp:718` per-core slow-path body with `c` iterator → CONFIRMED per-core
- HIGH-3 callers (CORRECTED 3→5): `Strategies/private/EmaCross.hpp:216` (EmaCross_ExitAdjustSharded) + `Strategies/Momentum.hpp:393` + `Strategies/MeanReversion.hpp` (forward-decl at :561; **actual call site MUST be verified at Phase A.5** — not located in /trace-deps scan) + `Strategies/MLStrategy.hpp:314` (missed by audit) + `CoreFrameworks/ControllerEventLoop.hpp:2530` (missed by audit) — all 5 confirmed per-core via slot parameter
- HIGH-4 outer loop: `CoreFrameworks/ControllerEventLoop.hpp:2316` `resolved_cfg = ControllerConfig_ResolveForCore(*config, slot)` per-core dispatch → CONFIRMED per-core

**Effect-masking nuance:** all 4 HIGH bugs are SILENT when per-core fee_rate_taker values are uniform across cores (operator default). Surfaces only when operator sets per-core fee_rate_taker differently (e.g., `core_2_fee_rate_taker=0.0015` for high-fee tier testing). However: any operator using per-core fee_rate_taker calibration sweeps over `partial_exit_pct` / `tp2_mult` / SL settings may have produced tainted results (operators tuned against silently-miscalibrated behavior). DOCUMENTED-RISK PARITY entry advisory at Step G.6 inline (does NOT defer to `.F` per /blindspot-scan MED-6 finding that `.B.7` forward-promise was never actually written).

**v1.1 scope expansion rationale (per Caramel directive 2026-05-27 PM):** "lets fold all the findings in, and see if we have any existing design specs that can address these if needed, and then save any anti patterns, and maybe update some skills to check against known anti patterns as well... i dont wanna leave open items that just accumulate tech debt." This expands `.B.8` scope to include NEW Phase H — anti-pattern codification + skill amendments — so the disciplines that closed THIS bug class are installed for future prevention. Per `feedback_no_defer_for_effort` + `feedback_motivated_collaborator_for_caramel`.

---

## Ship type + end goal + acceptance criteria

**Plan type:** `mixed` (hotfix + structural enforcement + catalog amendment + anti-pattern codification + skill amendment)

**Ship end goal:** Close 4 NEW HIGH UNINDEXED-GLOBAL Class 26 instances (fee_rate_taker accounting cohort from `ea08210` mechanical migration) via consumer-side substitution + close MED display drift + open+close LOW dead-state TECH_DEBT + land Stage 6 Check 10 (M7 next canonical structural enforcement; sister to Check 9 paired-access detection) + amend Class 26 catalog with NEW UNINDEXED-GLOBAL sub-shape (recurrence_count 13→17) + add regression tests asserting per-core fee_rate_taker divergence + amend sister Class 27/25/18 catalogs with cross-refs + codify NEW anti-patterns (sister-cohort amendment completeness + forward-promise auto-write verification) + extend `/accounting-audit` + `/capture-audit` skills with new detection / verification.

**How this contributes to sprint MASTER goal:** v5.15 sprint goal = paper-test session entry with structurally-sound codebase. `.B.8` removes 4 silent realized-P&L drift bugs in production accounting paths + locks structural enforcement against the UNINDEXED-GLOBAL Class 26 sub-shape mechanically + installs NEW disciplines that prevent future sister-cohort-amendment + forward-promise-auto-write drift. Sprint advances toward `.C` framework consolidation with known-clean baseline + extended discipline coverage. Without `.B.8`, paper-test session would surface silent P&L bugs (or worse, mask them if operator uses uniform per-core fee_rate_taker) → silent live-trading P&L drift becomes possible.

**Acceptance criteria:**

Universal:
- Hot path UNTOUCHED (`tools/calls_graph_diff.sh verify` GREEN; all fix sites are slow-path / drainer-cycle / GUI / display; NOT BG_Evaluate/SG_Evaluate/ExecutionCore_Tick)
- 5 binaries clean (test + gui + suite + tsan + asan)
- Tests GREEN: 3223 / 0 controller_test baseline preserved + ~16-32 NEW regression tests asserting per-core fee_rate_taker divergence (4 slots × 2 fields × 4 consumer paths)
- CI checks PASS: existing Check 1-9 + NEW Check 10 (M7 6th canonical) verification (sanity-verified by revert-detect-reapply test for HIGH-1 + HIGH-4 aliased reads)
- Wire-format replay determinism: N/A (no cfg parser changes; no stamp body changes)

`hotfix`-specific:
- **Bug reproducer GREEN:** Test demonstrates per-core fee_rate_taker correctness with differing values (e.g., core 0 = 0.0008, core 1 = 0.0010, core 2 = 0.0012, core 3 = 0.0015) — verify ratchet SL fee-floor cap + breakeven-on-profit threshold + Strategy_WriteRatchetSL helper + GUI diag display all read the CORRECT per-core value at each
- **Regression test added:** New test section `tests/controller_test.cpp` "v5.15.5.F.4d.1.B.8 Class 26: UNINDEXED-GLOBAL accounting cohort closure" with 4 slots × 2 fields × 4 consumer sites = ~16-32 assertions
- **Sister-bug-class checked:** Phase A comprehensive cohort enumeration verifies NO other UNINDEXED-GLOBAL per-core-field reads at per-core consumer sites beyond the 4 HIGH findings (slippage_pct / fee_rate_maker / per-core-overridden fields all swept; LEGACY-EXEMPT sites at RegimeDetector.hpp:760/789/808 + EmaCross.hpp:143-144 explicitly classified)

`refactor`-specific:
- **CLOSED bug classes:** Class 26 ×4 NEW UNINDEXED-GLOBAL instances + UNINDEXED-GLOBAL sub-shape catalog amendment; `/bug-check class_26` CLEAN post-fix (existing + NEW sub-shape covered)
- **CLOSED TECH_DEBT entries:**
  - TECH_DEBT-138 NEW close (LOW-1 dead DrainerConstants.fee_rate_taker_d field; open+close same ship)
- **NEW TECH_DEBT entries opened:** none (all findings closed at this ship; comprehensive cohort enumeration verifies no deferred sister-shapes; NEW disciplines from Phase H prevent future drift)
- **NEW DESIGN_SPECs landed (Stage 2 DRAFT):**
  - `DESIGN_SPECS/meta-disciplines/sister-cohort-amendment-completeness-discipline.md` (NEW Stage 2 DRAFT; codifies sister-catalog + sister-ledger + sister-skill amendment cohort discipline)
- **AMENDED DESIGN_SPECS:**
  - `structural-enforcement-when-memory-insufficient.md` v1.2 → v1.3 — add Check 9 retroactive + Check 10 to `canonical_applications:` list (5th + 6th entries; corrects `.B.7` doc drift)
  - `canonical-sister-extension-discipline.md` v1.0 → v1.1 — add Check 10 as 2nd CI-tooling-surface application (sister to Check 9 at .B.7); existing v1.0 examples are at REGISTRY surface (Path γ + Path γ #2) — v1.1 extends to CI-TOOLING surface
  - `cfg-field-categorization-discipline.md` — add note that consumer-side discipline check at registry add-time should verify EXISTING consumer sites for newly-per-core-migrated fields are updated to use per-core slot (preventive analog to Check 10 commit-time enforcement)
- **Class catalog amendments:**
  - `class-26-global-consumer-reading-per-core-field.md` recurrence 13→17 + NEW Sub-shape B section + 4 NEW Worked Examples + Check 10 cross-ref
  - `class-27-single-value-cache-flattens-per-instance.md` — add Class 26 sub-shape B sister cross-ref note (Class 27 line 38 already cross-refs Class 26 generally; amend with sub-shape B distinction)
  - `class-25-scope-erosion-per-core-consumer.md` — add Class 26 sub-shape B sister cross-ref (Class 25 is parent at consumer-discipline layer per Class 26 catalog Sister classes line 103)
  - `class-18-mirror-plans-missing-data-flow.md` — add Class 26 sub-shape B + Check 10 cross-ref (Class 18 is parent family per Class 26 catalog Sister classes line 105)

`admin`:
- Check 10 implementation lands in `tools/check_per_core_registry_integrity.py` with refined whitelist `{c, core, core_id, slot}` primary + heuristic `i` for boot-time + anti-confusion documentation on `idx`/`pos`
- Per-core-migrated field registry derivable from FOREACH_PER_CORE_CFG_FIELD (single source of truth per H15)
- Check 10 detection covers 3 layers: fn-param scope + per-core-for-loop enclosing scope + lambda-body capture (with member-fn `this->core_id` + indirect per-core via state pointer)
- Check 10 sanity-verified via revert-detect-reapply for HIGH-1 AND HIGH-4 (covers direct-field-access AND stack-local aliased-via-resolved_cfg)
- M6 CSV sidecar at `plan_checks/2026-05-27-B.8-check10-body-content-enumeration.csv` documents Check 10 scan fn args explicitly (per `feedback_enumerate_helper_signature_args_before_extract`)

`anti-pattern codification` (NEW Phase H):
- NEW memory file `feedback_sister_cohort_amendment_completeness.md` (Stage 2 DRAFT)
- NEW memory file `feedback_forward_promise_auto_write_verification.md` (Stage 2 DRAFT)
- NEW DESIGN_SPEC `sister-cohort-amendment-completeness-discipline.md` (Stage 2 DRAFT)
- MEMORY.md index updated with 2 NEW entries
- CLAUDE.local.md going-forward rules updated with 2 NEW entries

`skill amendment` (NEW Phase H):
- `/accounting-audit` SKILL.md category 2 extended with explicit UNINDEXED-GLOBAL Class 26 sub-shape detection sub-bullets
- `/capture-audit` SKILL.md NEW Check 11 "Forward-promised auto-write at prior ship close verification"

---

## Design space + future-oriented choices

### Decision A — Fix shape (consumer-side substitution vs architectural amend)

| Option | Description | Robustness | Latency | Design alignment | Future-easier | Verdict |
|---|---|---|---|---|---|---|
| **(a) Consumer-side substitution + Check 10 extension** | Change `cfg.fee_rate_taker` → `cfg.cores[core_id].fee_rate_taker` at each consumer site; extend `tools/check_per_core_registry_integrity.py` with Check 10 to flag UNINDEXED-GLOBAL per-core-field reads at per-core consumer sites | Closes 4 instances + prevents UNINDEXED-GLOBAL sub-shape recurrence structurally via Check 10 | UNTOUCHED (all sites slow-path / drainer / GUI) | ✓ M7 Stage 6 escalation; sister-extension of existing Check 9 per `canonical-sister-extension-discipline.md` v1.1; aligns with `cfg-scope-discipline.md` (per-core fields read via core_id index); **sister-canonical precedent: `CoreFrameworks/EngineSharded/SlowPath.hpp:63-68` comment block documents `.F.4c.3 WIP2d-1.B.1` closure of Class 27 fn-local cache variant at SAME surface family — proven correction pattern** (NEW v1.1 per /parity-check LOW) | YES — Check 10 catches N future instances at commit-time | **CHOSEN** |
| (b) Add `fee_rate_taker` to PER_CORE_OVERRIDE_FIELDS macro + ResolveForCore merges per-core value into resolved_cfg | Architectural variant: change resolved_cfg semantics so `resolved_cfg.fee_rate_taker` carries per-core value after ResolveForCore; consumers don't need `cores[slot]` indexing | Closes 4 instances via different mechanism; doesn't prevent NEW UNINDEXED-GLOBAL recurrence at fields not added to PER_CORE_OVERRIDE_FIELDS | UNTOUCHED | ✗ doesn't address sub-shape recurrence prevention; expands PER_CORE_OVERRIDE_FIELDS surface (currently only fee_floor_mult per ControllerConfig.hpp:119) requires per-field analysis; consumer-side reads of `cfg.fee_rate_taker` outside ResolveForCore context still broken | PARTIAL — fixes ONLY the 4 instances; doesn't generalize | rejected — narrower fix surface vs option (a); doesn't address M7 escalation criterion |
| (c) Wrap `cfg.cores[slot].X` access in `Cfg_PerCoreField(cfg, slot, FIELD_X)` helper to make scope explicit | Refactor all per-core cfg access through helper fn | Closes class architecturally via mandatory helper invocation | UNTOUCHED (helper inlines to direct access) | ✓ structural but heavy-handed for 4-site fix; surface impact ~hundreds of call sites across codebase | YES but high cost (multi-day refactor) | rejected — proportionate-response discipline; Check 10 catches same class at much lower cost; matches `feedback_proportionate_response_to_audit_findings` |

**Auto-pick rationale:** (a) aligns with M7 lifecycle (Check 10 = next canonical after Check 9) + `canonical-sister-extension-discipline.md` v1.1 (extend existing tool, not new tool; sister-canonical CoreFrameworks/EngineSharded/SlowPath.hpp:63-68 closure pattern at same surface family) + proportionate-response (mechanical fix proportionate to bug instance count; structural enforcement proportionate to recurrence risk). Sister-canonical: Check 10 is sister to Check 9 (same file, same surface family, different detection signature). Future-easier multiplier: each new UNINDEXED-GLOBAL instance caught at commit-time instead of audit-time.

**Alternative reconsidered if/when:** if Check 10 surfaces false positives at high rate (>1 per quarter) OR if NEW per-core-field migrations land where consumer-side discipline keeps drifting, revisit (b) PER_CORE_OVERRIDE_FIELDS expansion or (c) helper-fn wrap.

**Novel alternative considered per `feedback_proactive_novel_alternative_consideration`:**
| Novel option | Description | Verdict |
|---|---|---|
| Compile-time `static_assert(false, "use cores[core_id].X")` via tagged-type `PerCoreField<T>` wrapper on `ControllerConfig::fee_rate_taker` | Replace `FPN<F> fee_rate_taker` with `PerCoreField<FPN<F>> fee_rate_taker` — implicit conversion to FPN<F> DELETED; only `cfg.fee_rate_taker.get(core_id)` compiles | rejected — compile-time enforcement attractive but requires touching every legitimate global consumer (boot-time / display "Settings panel" view / single_core legacy PortfolioController) with disambiguation; Check 10 catches at lower cost with similar prevention guarantee |

### Decision B — Ship sequence (standalone .B.8 vs fold into .C)

| Option | Description | Verdict |
|---|---|---|
| **(a) Standalone .B.8 post-ship hotfix** | Separate `.B.8` ship; clean sprint accounting; same shape as `.B.7` (Class 26 sister-bug cohort fix) | **CHOSEN** per operator directive + sister-shape consistency with `.B.7` |
| (b) Fold into `.C` per-core override emission CLI | Apply during `.C` planning; bundle with CLI work | rejected — `.C` is different surface (per-core override emission CLI is framework consolidation work); folding would scope-creep `.C` + delay P&L-drift fix |
| (c) Defer to `.F` professionalization audit sweep | Bundle with comprehensive audit ship | rejected per `feedback_no_defer_for_effort` — silent P&L drift is critical; deferring lets bug survive longer for no upside |

### Decision C — Phase A scope (audit's 4 HIGH only vs comprehensive cohort enumeration)

| Option | Description | Verdict |
|---|---|---|
| **(a) Comprehensive cohort enumeration before scope lock** | Phase A: enumerate ALL per-core-migrated fields × ALL per-core consumer sites; verify ZERO additional UNINDEXED-GLOBAL sites beyond audit's 4 HIGH; explicit LEGACY-EXEMPT classification for RegimeDetector.hpp:760/789/808 + EmaCross.hpp:143-144 (per convergent /trace-deps + /parity-check findings) | **CHOSEN** per `feedback_enumerate_consumers_before_registry_row_deletion` analog + operator directive "correctness over speed" |
| (b) Trust audit's 4 HIGH; skip enumeration | Apply only the 4 audit-flagged fixes | rejected — audit's WIDE scope was thorough but couldn't enumerate ALL per-core fields exhaustively; Phase A gives mechanical verification + LEGACY classification before scope locks |

### Decision D — Regression test depth (4 assertions vs comprehensive matrix)

| Option | Description | Verdict |
|---|---|---|
| **(a) Comprehensive regression matrix (~16-32 assertions)** | 4 slots × 2 fields (fee_rate_taker primary + fee_rate fallback) × 4 consumer sites; verify each consumer reads correct per-core value at each | **CHOSEN** — matches `.B.7` regression test depth precedent; HIGH-RISK tier warrants comprehensive coverage |
| (b) Minimal regression (4 assertions; one per HIGH site) | Single assertion per fix site | rejected — too thin for HIGH-risk P&L drift surface |

### Decision E — Anti-pattern codification timing (NEW v1.1)

Per Caramel directive 2026-05-27 PM "save any anti-patterns".

| Option | Description | Verdict |
|---|---|---|
| **(a) Codify NEW anti-patterns NOW at .B.8** | Codify sister-cohort-amendment-completeness + forward-promise-auto-write-verification as Stage 2 DRAFT memory + DESIGN_SPEC at this ship; install in MEMORY.md + CLAUDE.local.md | **CHOSEN** per operator directive + `feedback_no_defer_for_effort` |
| (b) Defer codification to a future cleanup ship | Note in postmortem; codify later | rejected — operator directive + per `feedback_structural_fix_for_recurring_class` (this IS the recurrence trigger; codify at the recurrence event) + the 2 anti-patterns are evidenced TWICE (Class 26 catalog forward-promise gap + Phase D sister-cohort gap surfaced by /blindspot-scan) so Stage 2 DRAFT threshold met now |

**Auto-pick rationale:** operator directive aligns with codify-at-recurrence-event discipline; both anti-patterns have 2+ evidence instances (Stage 2 DRAFT threshold per `pattern-codification-lifecycle.md`); deferring would let the anti-pattern accumulate silently.

### Decision F — Skill amendment timing (NEW v1.1)

Per Caramel directive 2026-05-27 PM "update some skills to check against known anti patterns".

| Option | Description | Verdict |
|---|---|---|
| **(a) Amend `/accounting-audit` + `/capture-audit` NOW at .B.8** | `/accounting-audit` category 2 extended with explicit UNINDEXED-GLOBAL sub-shape detection; `/capture-audit` NEW Check 11 for forward-promise verification | **CHOSEN** per operator directive |
| (b) Open TECH_DEBT for skill amendments | Defer skill amendments to future ship | rejected — operator directive + skill amendments are CO-LOCATED with anti-pattern codification (same ship that codifies the discipline should also encode the discipline into the skill that enforces it) |
| (c) Only amend `/accounting-audit`; defer `/capture-audit` Check 11 | Partial scope | rejected — both anti-patterns have skill-implementable detection; doing one without the other leaves drift surface |

**Auto-pick rationale:** Phase H folds skill amendments as the structural-enforcement layer of the anti-patterns codified at H.1. Same-ship codification + enforcement is the canonical M7 application shape (sister to Check 9 + class-26 catalog amendment at .B.7).

---

## Canonical sister patterns considered

Per `/readiness` Check 29 + `feedback_audit_canonical_sister_before_new_infra`.

| Sister | Verdict | Rationale |
|---|---|---|
| `tools/check_per_core_registry_integrity.py` Check 9 (NEW at .B.7; M7 4th canonical paired-access mismatch detector per .B.7 Class 26 catalog claim BUT NOT yet in M7 spec canonical_applications list — DOC DRIFT TO CORRECT at Phase D Step D.2) | **EXTEND (sister Check 10)** | Same tool, same surface family (per-core cfg integrity), different detection signature (UNINDEXED-GLOBAL vs PAIRED-ACCESS-MISMATCH). 3-question test all YES |
| `tools/check_per_core_registry_integrity.py` Check 7 (Class 27 subsystem-state cfg-mirror scan; existing) | **NO-FOLD** | Different detection target (subsystem state struct fields vs consumer-side reads); same tool but different scan logic |
| `tools/check_per_core_registry_integrity.py` Check 8 (cfg field categorization scaffold; pending sister-ship impl) | **PARALLEL SISTER** | Same tool, different category enforcement; Check 8 at REGISTRY-ADD-time; Check 10 at CONSUMER-ADD-time. Distinct discipline layers; both M7 family |
| `CoreFrameworks/EngineSharded/SlowPath.hpp:63-68` Class 27 fn-local cache closure comment block (`.F.4c.3 WIP2d-1.B.1`) | **CITE as sister-canonical precedent** (NEW v1.1 per /parity-check LOW) | Same surface family (per-core consumer reading per-core field); proven correction pattern for Class 27 fn-local variant; cite in Decision A novel-alt section + Phase D catalog amendment |
| `feedback_cfg_field_categorization_at_registry_add_time` (codified at .B.4 v1.7.6) | **APPLY (consumer-side analog)** | Discipline applies at registry-add-time; Check 10 enforces consumer-side analog. Sister to /readiness Check 44 (plan-time enforcement at registry add); Check 10 is commit-time enforcement at consumer add |
| `canonical-sister-extension-discipline.md` v1.0 | **EXTEND (NEW v1.1 — Check 10 as 2nd CI-tooling-surface application)** | Existing v1.0 examples at REGISTRY surface (Path γ + Path γ #2 + sister-registry parity); v1.1 adds 2nd CI-tooling-surface application after Check 9 first at .B.7; sister to existing examples at amendment Phase D Step D.7 |
| `decision-time-data-binding-pattern.md` (Class 27 closure pattern; pre-resolve per-core values onto in-flight objects) | **NO-APPLY direct** (per /dod-audit OBS-3) | Different closure mechanism — decision-time binding eliminates the cfg read entirely (Order object carries fee_rate). Applicable conceptually but the 4 HIGH sites aren't fill-event-bound (they're slow-path strategy adapts); decision-time binding shape doesn't fit. Direct per-core indexing is the right shape |
| `feedback_audit_canonical_sister_before_new_infra` (parent meta-rule) | **APPLY** | Verified Check 10 extends existing Check 9 (not new tool) — discipline satisfied |
| Pre-`.B.7` Class 26 fix (sister cohort precedent) | **EXTEND-SISTER-SHAPE** | `.B.7` fixed WRONG-INDEX sub-shape; `.B.8` fixes UNINDEXED-GLOBAL sub-shape. Same Class 26 family, sister sub-shape application. Catalog amendment captures both sub-shapes structurally |
| `structural-fix-preferred-decision-framework.md` (parent meta-rule) | **APPLY** | Class 26 recurrence at .B.8 = 17 (13 + 4 NEW); MANDATORY structural fix policy applies. Check 10 IS the structural fix |
| `pattern-codification-lifecycle.md` Stage 2 DRAFT threshold (NEW for Phase H anti-patterns) | **APPLY** | Sister-cohort-amendment-completeness has 2 instances (Phase D /blindspot-scan finding + Class 27 catalog sister cross-ref needed); forward-promise-auto-write has 2 instances (`.B.7` Class 26 forward-promise + `.B.7` Stage 6 promotion candidate forward-promise). Both meet Stage 2 DRAFT threshold |

**Verified by:** Sister inspection completed during plan-draft + 5-agent /precoding-audit-gate synthesis (this section); /merge-scan + /anti-spaghetti results in 5-agent audit synthesis reports.

---

## Bug classes this ship closes (structural impact)

| Class | Title | Closure mechanism |
|---|---|---|
| **Class 26** UNINDEXED-GLOBAL sub-shape | Global consumer reading per-core field (with no index at all) | 4 consumer-site substitutions `cfg.fee_rate_taker` → `cfg.cores[core_id].fee_rate_taker` + Check 10 extension to mechanically catch UNINDEXED-GLOBAL per-core-field reads at per-core consumer sites (Stage 6 M7 next canonical; sister to Check 9 PAIRED-ACCESS-MISMATCH detection from .B.7) |
| **Class 27** (vestigial sub-instance) | Single-value cache flattens per-instance (dead state from prior cache deletion) | LOW-1 fix: delete `DrainerConstants.fee_rate_taker_d` field + update `DrainerConstants_Init` + adjust `static_assert(sizeof(DrainerConstants) == 24, ...)` + trailing `_pad` + file header comment block update |
| **NEW Stage 2 DRAFT — sister-cohort-amendment-completeness** | Amending one Class catalog without parallel sister-catalog amendments creates sister-cohort drift (caught by /blindspot-scan HIGH-3 at this ship) | Codify as memory + Stage 2 DRAFT DESIGN_SPEC; apply at Phase D Steps D.4/D.5/D.6 for Class 27/25/18 cross-ref amendments |
| **NEW Stage 2 DRAFT — forward-promise-auto-write-verification** | Ship close promises an auto-write (PARITY entry / TECH_DEBT / postmortem note) that may not actually land; next-ship verification needed (caught by /blindspot-scan MED-6 at this ship — `.B.7` Class 26 catalog promised DOCUMENTED-RISK PARITY entry that doesn't exist) | Codify as memory + Stage 2 DRAFT; extend `/capture-audit` Check 11 for next-ship-time verification |

**Audit verification:** `/bug-check class_26 module:CoreFrameworks` CLEAN post-fix (existing + NEW sub-shape covered); `/bug-check class_27 module:MemHeaders` CLEAN post-LOW-1 fix.

**False-positive surface (per M3 discipline):**

Check 10 detection signature (3-layer per /blindspot-scan HIGH-2): (1) function takes per-core context (parameter name in whitelist `{c, core, core_id, slot}` primary + `i` heuristic boot-time only) OR (2) enclosing per-core for-loop scope OR (3) lambda body capturing per-core context. AND reads `cfg.X` / `cfg->X` / `resolved_cfg.X` (aliased per-core var per /trace-deps LOW-3) UNINDEXED where X is in per-core field migration list.

**Legitimate UNINDEXED-GLOBAL reads at per-core consumer sites (NOT Class 26; whitelist Check 10):**
- Boot-time reads (cores haven't been initialized yet; cfg is canonical scalar source) — Check 10 scope detection should exclude boot-time fns (fn name contains `Boot` / `Init`)
- Pre-resolved decision-time reads (Order has fee_rate captured at submit; reading order.fee_rate is correct) — Check 10 doesn't fire (read is on order, not cfg)
- Genuinely-global cfg fields (engine_arch, log_level, paper_test_mode) — Check 10 doesn't fire (field not in per-core migration list)
- Display "Settings panel" view (operator-facing global cfg display) — Check 10 may fire; document exemption inline OR add Section D exemption to MANUAL_FIELDS_INVENTORY.md
- Legacy single_core PortfolioController.hpp reads — Check 10 may fire; legacy code path; document exemption (4 LEGACY sites at `RegimeDetector.hpp:760/789/808` + `EmaCross.hpp:143-144` explicitly classified in Phase A)
- `idx` symbol — per-POSITION (active_bitmap ctz) NOT per-core; document anti-confusion at whitelist
- `pos` / `worker` symbols — NOT per-core; document anti-confusion

---

## DESIGN_SPECs landed/amended

### NEW DESIGN_SPECs at ship close (Stage 2 DRAFT)

- **NEW** `DESIGN_SPECS/meta-disciplines/sister-cohort-amendment-completeness-discipline.md` (Stage 2 DRAFT) — codifies the discipline that when amending one Class catalog (or any sister-cohort artifact: DESIGN_SPEC, memory, ledger), parallel sister-catalog amendments are required. Sister to canonical-sister-extension-discipline.md (which is at PRE-CODING / CREATION layer); this discipline is at AMENDMENT layer. Bug-class evidence: Class 27/25/18 sister-cross-ref gap at Phase D / Class 27 catalog line 38 already documents sister cross-ref to Class 26 generally but not sub-shape distinction. Stage 2 DRAFT; promotes to Stage 3 at 2nd canonical application

### Stage promotions at this ship

- `canonical-sister-extension-discipline.md` v1.0 → v1.1 — add Check 10 as 2nd CI-tooling-surface application (sister to Check 9 first at .B.7); v1.0 examples at REGISTRY surface (Path γ + Path γ #2); v1.1 extends to CI-TOOLING surface

### Existing DESIGN_SPEC amendments

- `DESIGN_SPECS/meta-disciplines/structural-enforcement-when-memory-insufficient.md` v1.2 → v1.3:
  - Add Check 9 to `canonical_applications:` list (5th entry — retroactive fix for `.B.7` doc drift)
  - Add Check 10 to `canonical_applications:` list (6th entry)
  - Add Worked Examples section entry "Stage 6 promotion: Class 26 WRONG-INDEX sub-shape → Check 9" + "Stage 6 promotion: Class 26 UNINDEXED-GLOBAL sub-shape → Check 10" mirroring existing entries
  - Increment `version: 1.2` → `1.3`; `last_amended: 2026-05-27`
- `DESIGN_SPECS/framework-patterns/cfg-field-categorization-discipline.md`:
  - Add note that consumer-side discipline check at registry-add-time should include sister verification that EXISTING consumer sites for a newly-per-core-migrated field are updated to use per-core slot (preventive analog to Check 10 commit-time enforcement)

### Class catalog amendments

- **Class 26** (`class-26-global-consumer-reading-per-core-field.md`):
  - Frontmatter `recurrence_count: 13` → `recurrence_count: 17`
  - NEW "## Sub-shapes" section — document TWO sub-shapes:
    - Sub-shape A: WRONG-INDEX paired-access (`.B.7` close; Check 9 catches)
    - Sub-shape B: UNINDEXED-GLOBAL at per-core consumer site (`.B.8` close; Check 10 catches)
  - NEW Worked Examples entries for 4 NEW instances
  - Cross-references update: add Check 10 reference; add `decision-time-data-binding-pattern.md § UNINDEXED-GLOBAL sub-shape` (if applicable)
- **Class 27** (`class-27-single-value-cache-flattens-per-instance.md`):
  - Amend existing sister-cross-ref at line 38 to distinguish Class 26 sub-shapes A vs B (currently general)
  - Add note: "Class 26 sub-shape B (UNINDEXED-GLOBAL) is the consumer-side analog of Class 27 — both fail to thread per-core context through but at different layers (Class 27 at cache structure / Class 26-B at consumer read)"
  - Sister cross-ref to Check 10 added
- **Class 25** (`class-25-scope-erosion-per-core-consumer.md`):
  - Add Class 26 sub-shape B cross-ref (Class 25 is parent at consumer-discipline layer per Class 26 catalog Sister classes line 103)
  - Add note that Class 26 sub-shape B is a specific instance of Class 25 scope erosion at the per-core-consumer surface
- **Class 18** (`class-18-mirror-plans-missing-data-flow.md`):
  - Add Class 26 sub-shape B + Check 10 cross-ref (Class 18 is parent family per Class 26 catalog Sister classes line 105)
  - Note: "Class 26 sub-shape B is one instance shape of 'mirror missing per-instance dimension' — the cfg has per-instance dimension but consumer-side read is mirror-incomplete"

---

## Scope

### IN scope

**Phase A — Comprehensive cohort enumeration (~45 min):**
- Enumerate ALL per-core fields via FOREACH_PER_CORE_CFG_FIELD (~88 fields per Check 1 PASS output)
- Identify per-core fields with potential UNINDEXED-GLOBAL hazard (those with sister GLOBAL form in ControllerConfig<F>; fee_rate / fee_rate_taker / fee_rate_maker / slippage_pct primary candidates)
- Enumerate per-core consumer sites using BROADER detection (per /blindspot-scan HIGH-1):
  - Function-parameter scope: fn takes parameter named `c` / `core` / `core_id` / `slot`
  - Per-core for-loop enclosing scope: `for (int <whitelist-symbol> = 0; ...)` patterns
  - Lambda body scope: `[capture-list]() { ... }` with per-core capture
  - Member function: `this->core_id` / `this->slot` access
  - Indirect per-core through state pointer: `per_core_state->core_id` accessed in fn body
- Cross-grep: for each (per-core-field, per-core-consumer-fn) pair, look for unindexed `cfg.X` / `cfg->X` / `resolved_cfg.X` read in fn body
- Classify each finding:
  - REAL Class 26 UNINDEXED-GLOBAL (fix at Phase B; expand scope if found beyond audit's 4 HIGH)
  - DISPLAY (Settings panel intentional global; document with comment + consider Section D exemption)
  - BOOT-TIME (whitelist; not Class 26)
  - **LEGACY (NEW v1.1 — explicit classification per /trace-deps + /parity-check convergent finding):**
    - `RegimeDetector.hpp:760/789/808` — legacy single_core path; not Class 26 per audit Cat 8 verdict
    - `EmaCross.hpp:143-144` — legacy `EmaCross_ExitAdjust` single_core path; caller is `PortfolioController.hpp:906`; not Class 26 per audit Cat 8 verdict
  - FALSE-POSITIVE (legitimate scalar reads; document why)
- Cite `StrategyParameters.hpp:1762` as sister-canonical "what good looks like" for Phase D catalog amendment narrative (per /trace-deps recommendation)
- Output: `plan_checks/2026-05-27-B.8-cohort-enumeration.csv` (one row per finding with classification)
- **NEW Phase A.5 (per /readiness MED-5 + /trace-deps HIGH-3):** verify MeanReversion actual `Strategy_WriteRatchetSL` call site (plan cites `:561 forward-decl` but actual call site not located in /trace-deps scan; verify before Phase B starts)
- Surface comprehensive cohort to Caramel + lock scope BEFORE Phase B coding starts

**Phase B — Fixes (~30 min):**

HIGH-1: `CoreFrameworks/ControllerEventLoop.hpp:3605-3606`
```cpp
// BEFORE:
double fee_taker_d = FPN_ToDouble(cfg.fee_rate_taker);
if (fee_taker_d <= 0.0) fee_taker_d = FPN_ToDouble(cfg.fee_rate);

// AFTER:
double fee_taker_d = FPN_ToDouble(cfg.cores[core_id].fee_rate_taker);
if (fee_taker_d <= 0.0) fee_taker_d = FPN_ToDouble(cfg.cores[core_id].fee_rate);
```

HIGH-2: `CoreFrameworks/ControllerEventLoop.hpp:3670-3671` — identical substitution shape with `core_id` in scope.

HIGH-3: `Strategies/StrategyLifecycle.hpp:272-273`
```cpp
// BEFORE:
FPN<F> fee_taker = !FPN_IsZero(cfg->fee_rate_taker)
    ? cfg->fee_rate_taker : cfg->fee_rate;

// AFTER:
FPN<F> fee_taker = !FPN_IsZero(cfg->cores[slot].fee_rate_taker)
    ? cfg->cores[slot].fee_rate_taker : cfg->cores[slot].fee_rate;
```

HIGH-4: `CoreFrameworks/ControllerEventLoop.hpp:3042-3043`
```cpp
// BEFORE:
FPN<F> fee_taker = !FPN_IsZero(resolved_cfg.fee_rate_taker)
    ? resolved_cfg.fee_rate_taker : resolved_cfg.fee_rate;

// AFTER (per audit recommendation):
FPN<F> fee_taker = !FPN_IsZero(resolved_cfg.cores[slot].fee_rate_taker)
    ? resolved_cfg.cores[slot].fee_rate_taker : resolved_cfg.cores[slot].fee_rate;
```

MED-1 line 249-250: `CoreFrameworks/ShardedSnapshot.hpp:249-250`
```cpp
// BEFORE:
double fee_r = FPN_ToDouble(cfg->fee_rate);
ps->net_pnl = ps->gross_pnl - (fee_r * 200.0);

// AFTER:
double fee_r = FPN_ToDouble(cfg->cores[core_id_for_pos].fee_rate_taker);
if (fee_r <= 0.0) fee_r = FPN_ToDouble(cfg->cores[core_id_for_pos].fee_rate);
ps->net_pnl = ps->gross_pnl - (fee_r * 200.0);
```

MED-1 lines 328-331 + **NEW line 139 (per /parity-check MED)**: KEEP-AS-GLOBAL per operator-facing Settings panel semantics (Settings panel shows GLOBAL defaults; per-core deviations surfaced via per_core_count panel at line 339). Document as intentional global with inline comment at line 139 + lines 328-331:
```cpp
// KEEP-AS-GLOBAL: Settings panel operator-facing display shows global defaults;
// per-core deviations surfaced via per_core_count panel at line 339.
```

LOW-1: `MemHeaders/DrainerConstants.hpp:65,100` (PLUS NEW v1.1 header comment block update + B14 leaves-first ordering):

**B14 leaves-first deletion ordering** (per /blindspot-scan LOW + /dod-audit OBS-1; AMENDED v1.2 per /blindspot-scan v1.1 L-PILLAR-B14 — step 4↔5 swap because `static_assert` depends on `_pad` outcome → pure leaves-first ordering):
1. First: grep verify ZERO consumers of `dc.fee_rate_taker_d` codebase-wide (audit already confirmed; re-verify before delete)
2. Then: delete `double fee_rate_taker_d;` field at line 65
3. Then: delete `dc.fee_rate_taker_d = FPN_ToDouble(cfg.fee_rate_taker);` assignment at line 100
4. Then: adjust trailing `_pad[7]` per new sizeof requirements **(was step 5 in v1.1; SWAPPED v1.2 — `_pad` must be sized BEFORE static_assert validates)**
5. Then: update `static_assert(sizeof(DrainerConstants) == 24, ...)` to new size (likely 24 → 16 after removing 8B field; verify via sizeof check) **(was step 4 in v1.1; SWAPPED v1.2 — validates AFTER `_pad` adjustment)**
6. Then: update comment at line 84-85 to remove stale "cfg field is boot-set immutable" claim
7. **NEW v1.1 (per /dod-audit OBS-1):** update file header comment block (lines 5-45) "Total 24B" wording → "Total 16B" post-deletion
8. Then: B-Plus regression sanity — verify build clean + grep verify NO new consumers crept in
- TECH_DEBT-138 OPEN + CLOSE same ship

**Phase C — Check 10 implementation (~60 min):**

Extend `tools/check_per_core_registry_integrity.py` with 3-layer detection (per /blindspot-scan HIGH-2):

1. Add `CHECK_10_SCAN_FILES` list (mirror Check 9 structure; same files + `Strategies/StrategyLifecycle.hpp` + `Strategies/private/EmaCross.hpp` + `Strategies/Momentum.hpp` + `Strategies/MeanReversion.hpp` + `Strategies/MLStrategy.hpp` + `ShardedSnapshot.hpp`)

2. **Refined whitelist (per /trace-deps MED-3):**
```python
CHECK_10_PER_CORE_CONSUMER_SYMBOLS_PRIMARY = {'c', 'core', 'core_id', 'slot'}  # always per-core
CHECK_10_PER_CORE_CONSUMER_SYMBOLS_HEURISTIC = {'i'}  # heuristic; boot-time loop-iterator only
CHECK_10_NOT_PER_CORE_SYMBOLS = {'idx', 'pos', 'worker'}  # anti-confusion documentation; NOT per-core (idx = per-POSITION ctz; pos = per-position; worker = per-thread)
```

3. Add `PER_CORE_FIELD_LIST` derivation from FOREACH_PER_CORE_CFG_FIELD (reuse existing `parse_foreach_per_core_cfg_field` from Check 1; intersect with fields that have GLOBAL sister in ControllerConfig<F>)

4. Add `scan_check_10_violations(text, file_rel_path)` with 3-layer detection (per /blindspot-scan HIGH-2):
   - **Layer 1 — Function-parameter scope:** find fn defs taking `const ControllerConfig<F>&` / `const ControllerConfig<F>*` parameter AND taking per-core symbol parameter (from whitelist primary OR heuristic)
   - **Layer 2 — Per-core for-loop enclosing scope:** within scanned files, find `for (int <whitelist-symbol> = 0; ...)` patterns; scope = body of for-loop
   - **Layer 3 — Lambda body scope:** find `[capture-list]() { ... }` with per-core capture (`[core_id]` / `[slot]` / etc.); scope = lambda body
   - **Member fn `this->core_id`:** detect class member-fn that accesses `this->core_id` or `this->slot`; scope = whole method body
   - **Indirect per-core via state pointer:** detect `per_core_state->core_id` or `state->cores[core_id]` access in fn body; scope = enclosing fn body
   - Within each per-core scope, scan for `cfg.X` / `cfg->X` UNINDEXED reads where X ∈ PER_CORE_FIELD_LIST
   - **Aliased detection (per /trace-deps LOW-3):** also scan for `resolved_cfg.X` / `<aliased-var>.X` UNINDEXED where aliased var is stack-local copy of ControllerConfig<F>
   - Flag each as violation: (file, line_num, fn_name OR scope_type, field_name)
   - Exclude legitimate reads:
     - Boot-time fns (heuristic: fn name contains `Boot` / `Init`)
     - Section D exemption list (NEW addition to MANUAL_FIELDS_INVENTORY.md; default exemptions = Settings panel KEEP-AS-GLOBAL sites + LEGACY single_core PortfolioController paths)

5. Add `main()` integration (mirror Check 9 block pattern; ~10 lines)

6. Add fixture test in scan logic that catches pre-fix `ControllerEventLoop.hpp:3605` pattern (regression-prevention for the tool itself)

7. **Sanity verification (per /trace-deps LOW-3 — extend to HIGH-4 aliased case):**
   - Revert HIGH-1 fix (change `cfg.cores[core_id].fee_rate_taker` back to `cfg.fee_rate_taker` at line 3605); run tool → Check 10 FAIL at exact line ref `ControllerEventLoop.hpp:3605` flagged; re-apply
   - **NEW v1.1:** Revert HIGH-4 fix (aliased case via `resolved_cfg.fee_rate_taker`); run tool → Check 10 FAIL at exact line ref `ControllerEventLoop.hpp:3042` flagged; re-apply
   - Document in postmortem per `.B.7` Check 9 precedent

**M6 CSV sidecar (per /readiness MED-1):**
- Create `plan_checks/2026-05-27-B.8-check10-body-content-enumeration.csv` documenting Check 10 scan fn args explicitly:
  - Columns: `arg_name`, `type`, `purpose`, `derivation_source` (e.g., parsed from where), `default_or_fallback`
  - Rows: text, file_rel_path, CHECK_10_SCAN_FILES, CHECK_10_PER_CORE_CONSUMER_SYMBOLS_PRIMARY, etc.
- Sister to Check 9 inline-reading precedent acceptable per M6 escape clause if CSV is created OR rationale documented

**Phase D — Class 26 catalog amendment + DESIGN_SPEC amendments + sister catalog cohort (~30 min):**

**Step D.1:** Edit `DOCS/recurring-bug-patterns/class-26-global-consumer-reading-per-core-field.md`:
- Frontmatter `recurrence_count: 13` → `17`
- Add NEW "## Sub-shapes" section documenting Sub-shape A (WRONG-INDEX paired-access; Check 9) + Sub-shape B (UNINDEXED-GLOBAL at per-core consumer site; Check 10)
- Add 4 NEW Worked Examples entries per Class catalog amendment section above
- Update Cross-references to include Check 10
- Clarify detection signature with both sub-shapes + their mechanical detection

**Step D.2:** Edit `DESIGN_SPECS/meta-disciplines/structural-enforcement-when-memory-insufficient.md`:
- Add Check 9 to `canonical_applications:` frontmatter list (5th entry — retroactive fix per /readiness MED-2 finding that .B.7 ship close never added it)
- Add Check 10 to `canonical_applications:` frontmatter list (6th entry)
- Add Worked Examples section entries:
  - "Stage 6 promotion: Class 26 sub-shape A WRONG-INDEX → Check 9 in check_per_core_registry_integrity.py" (retroactive for .B.7)
  - "Stage 6 promotion: Class 26 sub-shape B UNINDEXED-GLOBAL → Check 10 in check_per_core_registry_integrity.py"
- Increment `version: 1.2` → `1.3`; `last_amended: 2026-05-27`

**Step D.3:** Edit `DESIGN_SPECS/framework-patterns/cfg-field-categorization-discipline.md`:
- Add note that consumer-side discipline check at registry-add-time should include sister verification that EXISTING consumer sites for a newly-per-core-migrated field are updated to use per-core slot (preventive analog to Check 10 commit-time enforcement)

**Step D.4 (NEW v1.1 per /blindspot-scan HIGH-3):** Edit `DOCS/recurring-bug-patterns/class-27-single-value-cache-flattens-per-instance.md`:
- Amend existing sister-cross-ref at line 38 to distinguish Class 26 sub-shapes A vs B (currently general)
- Add note: "Class 26 sub-shape B (UNINDEXED-GLOBAL) is the consumer-side analog of Class 27 — both fail to thread per-core context through but at different layers (Class 27 at cache structure; Class 26 sub-shape B at consumer read site)"
- Sister cross-ref to Check 10 added

**Step D.5 (NEW v1.1 per /blindspot-scan HIGH-3):** Edit `DOCS/recurring-bug-patterns/class-25-scope-erosion-per-core-consumer.md`:
- Add Class 26 sub-shape B cross-ref (Class 25 is parent at consumer-discipline layer)
- Add note that Class 26 sub-shape B is a specific instance of Class 25 scope erosion at per-core-consumer surface

**Step D.6 (NEW v1.1 per /blindspot-scan HIGH-3):** Edit `DOCS/recurring-bug-patterns/class-18-mirror-plans-missing-data-flow.md`:
- Add Class 26 sub-shape B + Check 10 cross-ref (Class 18 is parent family)
- Note: "Class 26 sub-shape B is one instance shape of 'mirror missing per-instance dimension' — the cfg has per-instance dimension but consumer-side read is mirror-incomplete"

**Step D.7 (NEW v1.1 per /blindspot-scan MED-4):** Edit `DESIGN_SPECS/meta-disciplines/canonical-sister-extension-discipline.md`:
- Version 1.0 → 1.1
- Add Check 10 as 2nd CI-tooling-surface application example (sister to existing REGISTRY-surface examples Path γ + Path γ #2)
- Note: "Sister-extension discipline applies at SPATIAL (parallel infrastructure) + TEMPORAL (sister sig migration) + CROSS-TOOL (bash scripts) + CI-TOOLING (Check 9 paired-access + Check 10 UNINDEXED-GLOBAL) axes — Check 10 first canonical at .B.8 demonstrates the CI-TOOLING-axis discipline application"

**Phase E — Regression tests (~30 min):**

**Step E.1:** Write new test section in `tests/controller_test.cpp` (verify split file location at Phase A):
- Section title: "v5.15.5.F.4d.1.B.8 Class 26: UNINDEXED-GLOBAL accounting cohort closure"
- Setup: 4 cores with differing per-core `fee_rate_taker` values (e.g., 0.0008 / 0.0010 / 0.0012 / 0.0015)
- For each of HIGH-1/2/3/4 consumer site:
  - For each core_id 0..3:
    - Trigger consumer with state populated for that core
    - Capture the fee_rate value the consumer reads
    - Assert captured value == cfg.cores[core_id].fee_rate_taker (NOT global)
- For LOW-1: `static_assert(sizeof(DrainerConstants) == <new-size>, ...)` regression test

**Step E.2 (per /blindspot-scan MED-5 awareness):** Note that controller_test.cpp is at ~26K lines (5.2× over test 5K threshold per `.B.7` C1 close-out — TECH_DEBT-127 stays open as test-reliability surface). Phase E new tests SHOULD consider helper extraction (per `feedback_enumerate_helper_signature_args_before_extract` M6) if test setup is repetitive; defer full controller_test.cpp split per TECH_DEBT-127 deferral rationale.

**Step E.3:** Build verify (`./build.sh test`); run controller_test; verify 3223 baseline + ~16-32 NEW tests = ~3239-3255 / 0 failed.

**Phase F — Build verify + latency check (~30 min):**

**Step F.1:** Full `./build.sh all` — 5 binaries clean (test + gui + suite + tsan + asan).

**Step F.2:** Run all tests: controller_test ~3239-3255 / 0 + depth_recorder_test 17/0 + parity_harness baseline.

**Step F.3:** `tools/calls_graph_diff.sh verify` — hot path symbols unchanged.

**Step F.4:** `/latency-track` on Phase B fix sites — verify no slow-path / drainer-cycle / strategy-ratchet perf regression; HOT_PATH_CHANGELOG entry if applicable (likely NONE — field substitution is zero-latency-impact).

**Step F.5:** `tools/check_per_core_registry_integrity.py` — verify all 10 checks PASS (Check 1-9 existing + NEW Check 10 CLEAN post-fix; Phase C step 7 sanity verification documents revert-detect-reapply for BOTH HIGH-1 direct + HIGH-4 aliased cases).

**Step F.6:** `/bug-check class_26 module:CoreFrameworks` — CLEAN expected (existing + NEW sub-shape covered).

**Step F.7:** `/bug-check class_27 module:MemHeaders` — CLEAN expected (LOW-1 vestigial fix).

**Phase G — Ship close (~60 min):**

**Step G.1:** Version.hpp bump `5.15.5.F.4d.1.B.7` → `5.15.5.F.4d.1.B.8` with substantive comment block citing UNINDEXED-GLOBAL Class 26 sub-shape closure + Check 10 M7 next-canonical structural enforcement + Phase H anti-pattern codification + skill amendments + 4 HIGH + 1 MED + 1 LOW closures + ~16-32 NEW regression tests.

**Step G.2:** CHANGELOG.md `.B.8` row in slim format per post-2026-05-03 convention; match `.B.7` voice; emoji 🔧 for fix-led ship. **NEW v1.1 (per /readiness LOW-2):** verify label convention matches `.B.7` precedent. Key items: 4 HIGH Class 26 UNINDEXED-GLOBAL accounting cohort closure + Stage 6 Check 10 (M7 next canonical) + 1 MED display fix + LOW-1 TECH_DEBT-138 close + Class 26 catalog amendment (recurrence 13→17 + NEW sub-shape) + Class 27/25/18 sister-cohort amendments + canonical-sister-extension-discipline.md v1.0→v1.1 + structural-enforcement-when-memory-insufficient.md v1.2→v1.3 + NEW Stage 2 DRAFT sister-cohort-amendment-completeness-discipline + NEW Stage 2 DRAFT forward-promise-auto-write memory + /accounting-audit + /capture-audit skill amendments.

**Step G.3:** Commit + GPG-signed tag `v5.15.5.F.4d.1.B.8`.

**Step G.4:** Postmortem at `plans/v5.15-live-readiness/postmortems/2026-05-27-v5.15.5.F.4d.1.B.8-postmortem.md`. **NEW v1.1 (per /readiness LOW-3):** verify postmortem convention matches `.B.7` precedent (frontmatter / structure / cross-refs / lessons captured / what went well / what went poorly). **OBS-2 positive note (per /blindspot-scan v1.2 cycle 3):** explicitly cite Phase G.7 Check 11 dogfood as FIRST CANONICAL APPLICATION of Check 11 — sister-canonical to `.B.7` Check 9 first-canonical-via-revert-detect-reapply pattern; same M7 discipline-installation shape (codify discipline + dogfood verify in same ship); document in "What went well" section.

**Step G.5:** CLAUDE.local.md sprint state row update:
- Most recent ship → `v5.15.5.F.4d.1.B.8`
- In-flight ship → NONE
- Queued pipeline → `.C` per-core override emission CLI (#2 in TaskList chain) → `.D` CI tool consolidation → `.E` per-core drainer 3-sub-ship → `.F` audit sweep → close
- Add NEW going-forward rule entries per Phase H.4

**Step G.6 — DOCUMENTED-RISK PARITY entry (NEW v1.1 per /blindspot-scan MED-6 — close `.B.7` forward-promise gap):**
- File: `tick-trader-percore-workspace/DOCS/PARITY_ISSUES.md`
- NEW entry: PARITY-NNN `fee_rate_taker` historical calibration tainted-results advisory
- Status: OPEN; severity: DOCUMENTED-RISK
- Inline template:
  ```yaml
  id: PARITY-NNN
  title: fee_rate_taker historical calibration tainted-results advisory (per-core UNINDEXED-GLOBAL Class 26 sub-shape closure at .B.8)
  status: open
  severity: documented-risk
  surface_tags: [accounting, per-core-indexing, fee-floor, slow-path, historical-calibration]
  ```
  Body documents: operators using per-core fee_rate_taker calibration sweeps over `partial_exit_pct` / `tp2_mult` / SL settings PRIOR to `.B.8` may have produced tainted results (operators tuned against silently-miscalibrated behavior). Forward-looking work fine post-.B.8 fix; historical calibration values may warrant re-validation. Closure trigger: operator re-validation at next paper-test cycle OR operator decision that historical calibrations are acceptable
- **ALSO closes `.B.7` forward-promise gap:** retroactively writes the DOCUMENTED-RISK PARITY entry that `.B.7` Class 26 catalog line 98 promised for `partial_exit_pct` / `tp2_mult` (which is the same fee_rate_taker calibration concern from a sister angle). Plan body Step G.6 NOTES that this entry is BOTH `.B.7` forward-promise closure AND `.B.8` new advisory.

**Step G.7:** /capture-audit --deep verify CLEAN (no decision-capture drift). **NEW v1.1:** also runs NEW Check 11 (forward-promise auto-write verification — added at Phase H.2 below) which cross-checks current ship + prior `.B.7` ship close promises against actual ledger state. **NEW v1.2 (per /blindspot-scan v1.1 M-RECURSIVE-1 dogfood requirement):** Check 11 must ALSO fire against `.B.8`'s OWN forward-promises (Stage 2 → 3 promotion at 2nd canonical for `sister-cohort-amendment-completeness-discipline` + `forward-promise-auto-write-verification` disciplines + Check 11 self-verification) — verify each `.B.8` forward-promise tracked at expected ledger location OR documented as "deferred to next-ship-time per discipline". Ship codifying the discipline dogfoods the codified discipline.

**Step G.8:** /sync-workspace (off-machine backup of plans + DESIGN_SPECS + skills + memory).

**Step G.9:** /handoff to next session (`.C` planning).

**Step G.10:** Push only on Caramel authorization (`git push origin feat/v5.15-live-readiness` + `git push origin v5.15.5.F.4d.1.B.8`).

### Phase H — NEW anti-pattern codification + skill amendments (~75 min)

**Per Caramel directive 2026-05-27 PM: "save any anti-patterns, and maybe update some skills to check against known anti-patterns".** This phase folds NEW disciplines INTO `.B.8` ship close so disciplines that closed THIS bug class are installed for future prevention.

**Step H.1 — Anti-pattern memory codification (~25-30 min per v1.2 effort calibration):**

Create NEW memory files:

a. `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/feedback_sister_cohort_amendment_completeness.md` (Stage 2 DRAFT)
   - Frontmatter: name, description, type: feedback
   - Body: when amending one Class catalog OR DESIGN_SPEC OR ledger entry, sister-cohort artifacts that cross-ref it require parallel amendments
   - **Why:** /blindspot-scan caught HIGH-3 finding at .B.8 — plan amended Class 26 only; Class 27/25/18 sister cross-refs needed parallel amendments. Recurring pattern: sister-cohort artifacts drift silently when one amends without sister enumeration
   - **How to apply:** at any catalog/DESIGN_SPEC/ledger amendment, enumerate sister artifacts (cross-refs in the amended artifact + reverse cross-refs from sister artifacts) BEFORE locking scope. Apply parallel amendments in same ship
   - Worked examples: .B.8 Phase D Steps D.4/D.5/D.6 Class 27/25/18 amendments

b. `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/feedback_forward_promise_auto_write_verification.md` (Stage 2 DRAFT)
   - Frontmatter: name, description, type: feedback
   - Body: ship-close promises an auto-write (PARITY entry / TECH_DEBT / catalog amendment / Stage promotion); next-ship-time verification needed
   - **Why:** /blindspot-scan caught MED-6 finding at .B.8 — `.B.7` Class 26 catalog line 98 promised DOCUMENTED-RISK PARITY entry; never written. Forward-promise drift is silent and accumulates
   - **How to apply:** at next-ship-time, grep prior ship's catalog amendments + postmortems for "forward advisory" / "DOCUMENTED-RISK entry at .X close" / "Stage 6 escalation candidate" + verify each landed at actual ledger location
   - Worked examples: .B.8 Phase G Step G.6 retroactively closes .B.7 forward-promise + .B.8 promises (verified at Phase G.7)

Update `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md` index — add 2 NEW entries with 1-line summaries.

**Step H.2 — NEW DESIGN_SPEC + skill amendments (~45-60 min per v1.2 effort calibration):**

a. Create NEW `DESIGN_SPECS/meta-disciplines/sister-cohort-amendment-completeness-discipline.md` (Stage 2 DRAFT):
   - Frontmatter: type: meta-discipline, stage: 2-draft, version: 1.0, established: 2026-05-27, tags, surface, sister_specs, applies_at_skills
   - Body: problem statement + the discipline (amendment-cohort enumeration) + worked examples (.B.8 Class 27/25/18 cohort) + trade-offs + when to apply + when to skip + cross-references
   - **OBS-1 polish (per /blindspot-scan v1.2 cycle 3 — authoring-time reminder):** include explicit `## Anti-pattern` subsection in the spec body documenting what the discipline PREVENTS (sister-cohort drift; silent amendment-incompleteness; cross-ref staleness); standard DESIGN_SPEC body convention per sister specs
   - Sister to `canonical-sister-extension-discipline.md` (which is at PRE-CODING / CREATION layer); this discipline is at AMENDMENT layer
   - Lifecycle: Stage 2 DRAFT (single canonical instance at .B.8); promotes to Stage 3 at 2nd canonical application
   - **NEW v1.2 per /blindspot-scan v1.1 B19 Option C fold (subsumes need for new pillar B19 in implementation-layer-blindspot-taxonomy.md):** body section "## Recursive sister-cohort enumeration" — when amending sister-cohort artifacts ITSELF triggers further sister-cohort enumeration (worked example: .B.8 Phase H amending /accounting-audit + /capture-audit triggered sister-skill cohort enumeration → /dod-audit added to cohort via v1.2 amendment; this recursion itself = canonical META pattern). Recursion termination criterion = FIXPOINT REACHED when amended cohort has no NEW sister cross-refs at next enumeration pass. Worked example: .B.8 v1.1 → v1.2 closed recursion (cycle 3 /blindspot-scan re-fire verifies fixpoint).

b. Amend `claude-skills/accounting-audit/SKILL.md`:
   - Section "10-category checklist" category 2 (Per-core / per-instance fee_rate + commission indexing):
     - Add explicit sub-bullets for Class 26 sub-shape A (WRONG-INDEX paired-access; Check 9 catches mechanically) + sub-shape B (UNINDEXED-GLOBAL at per-core consumer site; Check 10 catches mechanically)
     - Add note: "When firing this audit, distinguish sub-shape A vs B in findings; cite Check 9 / Check 10 respectively for mechanical detection"
   - Section "Output format" amend: per-finding format add `class_subshape:` field for Class 26 findings
   - Section "Cross-references" add Check 10 reference

c. Amend `claude-skills/capture-audit/SKILL.md`:
   - Add NEW "### Check 11: Forward-promised auto-write at prior ship close verification" section
   - Body: scan prior `N` ships' (default: last 3 ships) catalog amendments + postmortems for forward-promise sentinels ("forward advisory" / "DOCUMENTED-RISK entry at .X close" / "Stage 6 escalation candidate at .X" / "auto-write at ship close: ...")
   - For each promised auto-write, verify it landed at the expected ledger location (PARITY_ISSUES.md / TECH_DEBT.md / DESIGN_SPECS / MEMORY.md / etc.)
   - Output: list of UNFULFILLED forward-promises with prior ship reference + suggested closure path
   - Invocation: included in `--deep` mode (default for pre-handoff); not in `--quick`
   - Cross-references to `feedback_forward_promise_auto_write_verification.md` memory

d. **NEW v1.2 per /blindspot-scan v1.1 H-RECURSIVE-1:** Amend `claude-skills/dod-audit/SKILL.md`:
   - Per /blindspot-scan v1.1 finding: /dod-audit SKILL.md has 4 per-core surface citations that would benefit from Class 26 sub-shape A/B distinction; missed in v1.1 cohort enumeration
   - Sister-skill amendment cohort completeness — `/dod-audit` is sister to `/accounting-audit` + `/capture-audit` for per-core cfg discipline enforcement
   - Add Class 26 sub-shape A/B distinction to the 4 cited per-core surface citations (per audit's identification — exact line refs to be confirmed at Phase H.2 execution-time via grep)
   - Cross-reference Check 9 + Check 10 mechanical detection
   - `/bug-check` auto-handled via RECURRING_BUG_PATTERNS dynamic read (no skill amendment needed; verified by /blindspot-scan v1.1)
   - `/ml-audit` + `/registry-fit-audit` + `/hft-audit` NOT candidates (no Class 26 references per /blindspot-scan v1.1 sweep)

**Step H.3 — Existing DESIGN_SPEC amendment for canonical-sister-extension (~15 min):**

Edit `DESIGN_SPECS/meta-disciplines/canonical-sister-extension-discipline.md`:
- Version 1.0 → 1.1
- Add NEW sub-section "CI-tooling-surface axis (NEW 2026-05-27 v5.15.5.F.4d.1.B.8)":
  - Existing examples are at SPATIAL (Path γ + Path γ #2) + TEMPORAL (sister-registry sig migration cohort) + CROSS-TOOL (bash scripts → framework-driven CLI binary) axes
  - NEW: CI-tooling-surface axis — when extending a CI check tool, audit for sister extensions to existing tools rather than building new tools
  - First canonical: Check 9 (paired-access mismatch detector) at `.B.7` extended `tools/check_per_core_registry_integrity.py` rather than building new tool
  - Second canonical: Check 10 (UNINDEXED-GLOBAL detector) at `.B.8` extends same tool with sister detection signature
- Add Worked Examples section entries for Check 9 + Check 10 CI-tooling-surface application

**Step H.4 — CLAUDE.local.md + CLAUDE.md amendments (~10 min):**

a. Edit `CLAUDE.local.md` § Going-forward rules:
   - Add NEW entry "**Sister-cohort amendment completeness when amending Class catalog / DESIGN_SPEC / ledger** (2026-05-27)" — `feedback_sister_cohort_amendment_completeness.md`; `DESIGN_SPECS/meta-disciplines/sister-cohort-amendment-completeness-discipline.md`. Trigger: any Class catalog / DESIGN_SPEC / ledger amendment → enumerate sister-cohort artifacts via cross-refs + reverse cross-refs; apply parallel amendments same ship. `/capture-audit` Check 9 (memory→DESIGN_SPECS sister) catches drift
   - Add NEW entry "**Forward-promise auto-write verification at next-ship time** (2026-05-27)" — `feedback_forward_promise_auto_write_verification.md`; `/capture-audit` Check 11. Trigger: at ship-close + /handoff; verify prior ship forward-promises (PARITY entries / TECH_DEBT / catalog amendments) landed at expected ledger location; surface UNFULFILLED for retroactive closure

b. Edit `CLAUDE.md` if any disciplines warrant CLAUDE.md item promotion (likely NOT yet — both new disciplines are Stage 2 DRAFT; promote to CLAUDE.md after Stage 5+ per `pattern-codification-lifecycle.md`)

c. Update `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md` index with 2 NEW entries (already done at H.1)

### NOT IN scope (explicit deferral)

| Item | Defers to | Rationale |
|---|---|---|
| MED-1 lines 328-331 + line 139 Settings panel display (KEEP-AS-GLOBAL) | n/a — intentional design (operator-facing global Settings view) | Add inline comment documenting intent; not Class 26 |
| Per-core fee_rate_taker fields added to PER_CORE_OVERRIDE_FIELDS macro (architectural variant per Decision A option b) | Deferred — Check 10 prevention is sufficient; only revisit if false-positive rate or NEW per-core-field migrations warrant | Per Decision A; proportionate response |
| `Cfg_PerCoreField` helper-fn wrap (Decision A option c) | Deferred indefinitely | Per Decision A; proportionate response; multi-day refactor not warranted |
| `slippage_pct` per-core migration (if Phase A surfaces UNINDEXED-GLOBAL slippage_pct sites) | Either fold into `.B.8` (if cohort tight) OR open TECH_DEBT for `.C` | Phase A finding-driven; operator triage at scope-lock |
| Legacy single_core PortfolioController.hpp:592,1244,1601,1848 + EmaCross.hpp:143-144 + RegimeDetector.hpp:760/789/808 global reads (legacy architecture; not Class 26 per /trace-deps + /parity-check LEGACY-EXEMPT classification) | n/a — legacy single_core has no per-core dimension; global is correct there | Not Class 26 per audit Cat 8 LEGACY-KEEP verdict; explicit Section D exemption candidates |
| Section D exemption list addition to MANUAL_FIELDS_INVENTORY.md | Defer to `.C` or absorb at Phase C if cohort warrants | Operator triage at Phase A scope-lock |
| controller_test.cpp 26K-line split (TECH_DEBT-127 surface) | Per `.B.7` C1 close-out — stays open as test-reliability surface | Phase E new tests use helper extraction per M6 to manage growth |
| Stage 3 promotion of sister-cohort-amendment-completeness-discipline.md | Defer to 2nd canonical application | Stage 2 DRAFT threshold met at .B.8; Stage 3 needs 2nd canonical sister-cohort amendment instance |
| Stage 3 promotion of forward-promise-auto-write-verification | Defer to 2nd canonical OR `/capture-audit` Check 11 catches drift in production | Stage 2 DRAFT threshold met; Stage 3 needs 2nd canonical |

---

## Verification gate

| Gate | Pass criteria | Verification |
|---|---|---|
| Build clean | `./build.sh all` GREEN (5 binaries) | mechanical |
| Tests pass | ~3239-3255 / 0 controller + 17/0 depth_recorder + parity_harness baseline | mechanical |
| Hot path UNTOUCHED | `tools/calls_graph_diff.sh verify` GREEN | mechanical |
| Bug fixed | NEW regression test section "v5.15.5.F.4d.1.B.8 Class 26: UNINDEXED-GLOBAL accounting cohort closure" PASSES (~16-32 assertions) | mechanical |
| Class 26 CLEAN | `/bug-check class_26 module:CoreFrameworks` returns CLEAN | audit |
| Class 27 CLEAN | `/bug-check class_27 module:MemHeaders` returns CLEAN | audit |
| CI Check 10 works | Tool flags re-introduced UNINDEXED-GLOBAL on revert sanity test for HIGH-1 AND HIGH-4 aliased cases; CLEAN post-fix | mechanical |
| Phase A enumeration complete | `plan_checks/2026-05-27-B.8-cohort-enumeration.csv` exists + classifies every finding (incl. LEGACY-EXEMPT explicit) + operator-triaged scope | manual review |
| Phase A.5 MeanReversion caller verified | Actual `Strategy_WriteRatchetSL` call site located in MeanReversion.hpp | manual review |
| /latency-track GREEN | No slow-path / drainer / strategy-ratchet perf regression; HOT_PATH_CHANGELOG entry if any | audit |
| TECH_DEBT-138 closed | Dead `DrainerConstants.fee_rate_taker_d` field deleted; entry in `closed.md` | bookkeeping |
| Class 26 catalog amended | recurrence_count 17; NEW sub-shape section; 4 NEW Worked Examples | manual review |
| Class 27/25/18 sister cross-refs amended | Each catalog references Class 26 sub-shape B + Check 10 | manual review |
| M7 spec amended | Check 9 (retroactive) + Check 10 in canonical_applications list; Worked Examples entries added; version 1.3 | manual review |
| canonical-sister-extension-discipline.md amended | Version 1.1; CI-tooling-surface axis section added | manual review |
| DOCUMENTED-RISK PARITY entry | PARITY-NNN entry opened in PARITY_ISSUES.md for fee_rate_taker historical calibration; retroactively closes `.B.7` forward-promise | bookkeeping |
| NEW Stage 2 DRAFT memories | `feedback_sister_cohort_amendment_completeness.md` + `feedback_forward_promise_auto_write_verification.md` exist + indexed in MEMORY.md | mechanical |
| NEW Stage 2 DRAFT DESIGN_SPEC | `sister-cohort-amendment-completeness-discipline.md` exists at `DESIGN_SPECS/meta-disciplines/` | mechanical |
| /accounting-audit amended | Category 2 has explicit sub-shape A/B sub-bullets + Check 9/Check 10 cross-refs | manual review |
| /capture-audit amended | NEW Check 11 section present; included in `--deep` mode | manual review + run /capture-audit --deep to verify Check 11 invokes |
| CLAUDE.local.md going-forward rules | 2 NEW entries added | manual review |
| M6 CSV sidecar | `plan_checks/2026-05-27-B.8-check10-body-content-enumeration.csv` exists OR M6 deferral documented | mechanical |
| /dod-audit SKILL.md amended (v1.2 NEW per /blindspot-scan v1.1 H-RECURSIVE-1) | 4 per-core surface citations have Class 26 sub-shape A/B distinction + Check 9/Check 10 cross-refs | manual review |
| Check 11 dogfood (v1.2 NEW per /blindspot-scan v1.1 M-RECURSIVE-1) | After Phase H.2c lands Check 11, fire Check 11 against `.B.8`'s OWN forward-promises (Stage 2 → 3 promotion at 2nd canonical for sister-cohort-amendment + forward-promise-auto-write disciplines + Check 11 self-verification) — verify each promise tracked at expected ledger location OR documented as "deferred to next-ship-time per discipline" | mechanical (Check 11 invocation) |
| sister-cohort-amendment-completeness-discipline.md recursive shape (v1.2 NEW per /blindspot-scan v1.1 B19 Option C) | NEW DESIGN_SPEC body includes "Recursive sister-cohort enumeration" section + fixpoint termination criterion + .B.8 v1.1→v1.2 worked example | manual review |

---

## TECH_DEBT auto-write at ship close

| Entry | Action |
|---|---|
| TECH_DEBT-138 NEW | OPEN + CLOSE same ship (LOW-1: dead `DrainerConstants.fee_rate_taker_d` field deleted) |
| TECH_DEBT-NNN PARITY (DOCUMENTED-RISK) | OPEN at Step G.6 — `fee_rate_taker` historical calibration tainted-results advisory; **ALSO closes `.B.7` forward-promise gap** retroactively |

NO other NEW TECH_DEBT entries — all findings closed at this ship per Caramel directive "no defer; no accumulating tech debt". Phase H folds anti-pattern codifications + skill amendments INTO this ship rather than deferring.

If Phase A surfaces additional scope beyond audit's 4 HIGH:
- Each finding either folded into Phase B (if cohort tight) OR opened as TECH_DEBT entry with `.C` or future-ship trigger (operator triage)

---

## Pre-coding triggers (REQUIRED before promoting DRAFT → ACTIVE coding)

Per `feedback_tiered_audit_discipline_per_plan_scope` HIGH-RISK tier:

1. Plan body draft v1.2 complete (THIS DOCUMENT)
2. ✅ `/precoding-audit-gate` 5-agent HIGH-RISK fire COMPLETED 2026-05-27 (cycle 1) — audit synthesis at 5 plan_checks files cited above:
   - `/parity-check` GREEN-with-discoveries
   - `/trace-deps` YELLOW (5 amendments folded into v1.1)
   - `/dod-audit` GREEN (2 OBS folded)
   - `/readiness` YELLOW (5 MED amendments folded)
   - `/blindspot-scan` YELLOW (4 HIGH amendments folded → NEW Phase H)
3. ✅ Audit synthesis written + triaged with operator per `feedback_consult_on_audit_findings` (operator approved v1.1 fold-all per 2026-05-27 PM directive)
4. ✅ v1.0 → v1.1 amendment cycle complete (single cycle; no spiral signal per `feedback_iteration_spiral_signals_audit_meta_gap`)
5. ✅ `/readiness` + `/blindspot-scan` cycle 2 re-fire COMPLETED 2026-05-27 PM — `/readiness` GREEN (all v1.0 5 MED closed + 0 NEW); `/blindspot-scan` YELLOW (4 recursive sister-cohort findings predicted by re-fire prompt; cycle 2 SMALLER+DIFFERENT-SHAPE = NOT spiral per discipline)
6. ✅ v1.1 → v1.2 amendment cycle complete (4 micro-amendments folded per Caramel directive "no defer; no accumulating tech debt")
7. **PENDING:** `/blindspot-scan` cycle 3 inflection check re-fire on v1.2 to verify recursion termination (B19 Option C fold subsumes recursive shape into NEW DESIGN_SPEC body; expected GREEN by construction since META is codified). If GREEN: promote DRAFT v1.2 → ACTIVE Phase A coding. If YELLOW: cycle 3 + persistent findings = true META-gap signal warranting different approach (escalate per `feedback_iteration_spiral_signals_audit_meta_gap`)
8. Pre-tag rollback anchor `pre-v5.15.5.F.4d.1.B.8` created (Step 0 of coding)
9. CI checks PASS at HEAD (`check_meta_registry.py` + `check_per_core_registry_integrity.py` 9 existing checks; Check 10 sanity-verified at Phase C step 7 BEFORE ship close)
10. `/sync-workspace` (off-machine backup before substantive rework — plans + DESIGN_SPECS + skills + memory)
11. Phase A cohort enumeration CSV reviewed + scope locked by operator

---

## Cross-references

- Predecessor ship: `subplans/2026-05-27-v5.15.5.F.4d.1.B.7-bugfix-cleanup-umbrella-close.md` (SHIPPED 2026-05-27)
- Predecessor postmortem: `postmortems/2026-05-27-v5.15.5.F.4d.1.B.7-postmortem.md`
- Predecessor handoff: `handoffs/2026-05-27-v5.15.5.F.4d.1.B.7-post-ship-handoff.md`
- Audit informing this plan: `plan_checks/2026-05-27-post-B.7-accounting-audit.md` (full audit report; YELLOW verdict; 4 HIGH + 1 MED + 1 LOW; comprehensive sister-cohort framing)
- 5-agent precoding-audit-gate synthesis reports (per audit_synthesis frontmatter list above)
- Successor: `subplans/<date>-v5.15.5.F.4d.1.C-per-core-override-emission-cli.md` (QUEUED; #2 in TaskList chain)
- Sister scaffolds (forward pipeline): `.E` per-core drainer 3-sub-ship (`subplans/2026-05-27-v5.15.5.F.4d.1.E-per-core-drainer-architecture-SCAFFOLD.md`) + `.F` audit sweep (`subplans/2026-05-27-v5.15.5.F.4d.1.F-professionalization-audit-sweep-SCAFFOLD.md`)
- DESIGN_SPECS amended/created at ship close:
  - `meta-disciplines/structural-enforcement-when-memory-insufficient.md` v1.2 → v1.3 (Check 9 retroactive + Check 10)
  - `meta-disciplines/canonical-sister-extension-discipline.md` v1.0 → v1.1 (CI-tooling-surface axis)
  - **NEW** `meta-disciplines/sister-cohort-amendment-completeness-discipline.md` (Stage 2 DRAFT)
  - `framework-patterns/cfg-field-categorization-discipline.md` (consumer-side analog note)
- DESIGN_SPECS sister-pattern (NO amendment but APPLIED):
  - `refactor-patterns/decision-time-data-binding-pattern.md` (Class 27 closure sister; per-core consumer reads per-core slot)
  - `refactor-patterns/cfg-scope-discipline.md` (per-core fields read via core_id index)
  - `meta-disciplines/structural-fix-preferred-decision-framework.md` (Class 26 recurrence 17 = MANDATORY structural fix)
  - `meta-disciplines/pattern-codification-lifecycle.md` (Stage 2 DRAFT threshold for NEW disciplines)
- Class catalog amendments at ship close:
  - `DOCS/recurring-bug-patterns/class-26-global-consumer-reading-per-core-field.md` (recurrence 13→17; NEW sub-shape; 4 NEW Worked Examples; Check 10 cross-refs)
  - `DOCS/recurring-bug-patterns/class-27-single-value-cache-flattens-per-instance.md` (sister cross-ref to Class 26 sub-shape B + Check 10)
  - `DOCS/recurring-bug-patterns/class-25-scope-erosion-per-core-consumer.md` (Class 26 sub-shape B + Check 10 cross-ref)
  - `DOCS/recurring-bug-patterns/class-18-mirror-plans-missing-data-flow.md` (Class 26 sub-shape B + Check 10 cross-ref)
- NEW memory files at ship close:
  - `feedback_sister_cohort_amendment_completeness.md` (Stage 2 DRAFT)
  - `feedback_forward_promise_auto_write_verification.md` (Stage 2 DRAFT)
- Memory files (auto-loaded; relevant disciplines applied):
  - `feedback_motivated_collaborator_for_caramel` (silent P&L drift = best-software path applies; quality bar)
  - `feedback_no_defer_for_effort` (4 HIGH fixes + Phase H NEW codifications not deferred)
  - `feedback_structural_fix_for_recurring_class` (Class 26 recurrence 17 = MANDATORY; Check 10 is the structural fix)
  - `feedback_structural_enforcement_when_memory_insufficient` (M7; Check 10 is next canonical application after Check 9)
  - `feedback_audit_canonical_sister_before_new_infra` (Check 10 extends Check 9 existing tool; not new tool)
  - `feedback_consult_on_audit_findings` (Phase A scope-lock + audit synthesis triage with operator)
  - `feedback_proportionate_response_to_audit_findings` (Decision A option (a) over (b)+(c); proportionate response to instance count + Phase H fold-not-defer)
  - `feedback_plan_right_not_fast` (v1.1 amendment cycle comprehensive before Phase B coding)
  - `feedback_tiered_audit_discipline_per_plan_scope` (HIGH-RISK tier per P&L criticality + multi-file + new CI check + Phase H scope)
  - `feedback_session_decision_log_discipline` (amendment log inline; single amendment cycle = no separate decision log file needed yet)
  - `feedback_enumerate_consumers_before_registry_row_deletion` (consumer-side analog; comprehensive enumeration before scope lock)
  - `feedback_categorize_by_consumer_pattern_not_field_name` (Phase A classification uses consumer-pattern verification, not field-name heuristic)
  - `feedback_proactive_novel_alternative_consideration` (Decision A novel-alt PerCoreField<T> wrapper considered + rejected with rationale)
- Ledger entries:
  - TECH_DEBT-138 NEW (OPEN + CLOSE same ship; LOW-1 dead DrainerConstants field)
  - PARITY-NNN NEW (DOCUMENTED-RISK fee_rate_taker historical calibration advisory; retroactively closes `.B.7` forward-promise)
- Tools touched:
  - `tools/check_per_core_registry_integrity.py` — Check 10 NEW (M7 next canonical; sister to Check 9)
- Skills amended:
  - `claude-skills/accounting-audit/SKILL.md` — Category 2 explicit Class 26 sub-shape A/B detection
  - `claude-skills/capture-audit/SKILL.md` — NEW Check 11 forward-promise verification
- CI checks at sprint cadence (verify all PASS at ship close):
  - `tools/check_meta_registry.py` (3 checks)
  - `tools/check_per_core_registry_integrity.py` (10 checks; Check 10 NEW)
  - `tools/check_plan_body_symbol_existence.py` (B-Plus pre-commit hook)

---

**End of plan body v1.1 (2026-05-27 PM).** Single amendment cycle from v1.0 — all 13 audit findings folded + 4 NEW Phase H items added per Caramel directive. Awaits optional /readiness re-run + Caramel review before Phase A coding starts. Phase A comprehensive cohort enumeration may surface additional scope; operator triage at Phase A scope-lock.
