---
type: finding-disposition-register
ship: v5.15.5.F.4d.1.E.0.10 (Net-1)
status: IN-PROGRESS (Phase-1 re-triage; grows as findings are code-verified)
source_corpus: plan_checks/E.0-audit-reports/pre-implementation-findings/ (141 findings) + DOCS/tech-debt/open.md (93) + DOCS/PARITY_ISSUES.md (5 open)
disposition_legend: "CLOSED-by-<ship> | OPEN | NEW (post-Ship-B) | SUPERSEDED | PENDING-RETRIAGE | MOOT-UNREACHABLE (real-in-code but the trigger is structurally unreachable + a larger failure is already in play if it ever fires → pin the guarantee, don't patch; REQUIRES code-demonstrated unreachability, NOT a vibe — feedback_moot_unreachable_disposition)"
verification_rule: "disposition flips ONLY on a code grep/READ-of-context — never a token-count, never an assumption (the fpmem lesson: USE_NATIVE_128 showed 8 refs but was inert)."
dogfoods: feedback_tag_disposition_at_fix_time (WH-7) + feedback_consult_indexes_before_full_reads
---

# `.E.0.10` finding-disposition register

Live disposition of the `.E.0` backlog (141 findings + 93 TECH_DEBT + 5 PARITY), re-triaged against the **post-Ship-B** engine. Every item carries a status flipped at verification; the set stays queryable instead of reconstructed.

## 📍 SESSION STATE + CLOSE-OUT PLAN (2026-06-10 — end of the big `.E.0.10` session)

**WHERE WE ARE:** `.E.0.10` (Net-1) in progress. SHIPPED this session (all committed + pushed — engine `5e65933`, workspace `71d96b3`): **D-190** (P&L gross SSoT — the real capital bug) + triple-guard + codification; **4 Net-1 characterization tests** (oms-ts-2 / tsa-live-2 / D-110 / rsf-ts-1, suite 3343/0); **persist-8 fix** (LOW); **2 NEW DESIGN_SPECS** (adversarial-multi-agent-audit-methodology + cross-thread-multiword-read-consistency); the persist-dod-1 re-exam + the corrected concurrency picture.

**THE BIG OPEN ITEM — cross-thread torn-read class:** SYSTEMIC (9 sites; 3 LIVE capital-control incl. the reconciler that *corrects capital on a torn read* = Knight-shaped). **DELIBERATE accepted-race** (2026-04-09), structural fix SLATED for `.E.1` (aggregator-single-writer + seqlock-published money snapshot; F-2/D-74). It is NOT "left open" — it's TRACKED + PLANNED + (proposed) GATED.

**🚧 LIVE-ENABLE HARD GATE (proposed — needs a decision-log entry + ideally a boot-time refuse):** **live trading MUST be BLOCKED until the torn-read class + `conc-5` are closed** (lands `.E.1`). The capital-critical races (kill-switch, reconciler) are pre-production NOW; the gate guarantees they cannot reach live before the fix. This is what makes the `.E.1` deferral SAFE rather than open.

**🔑 OPERATOR ARCHITECTURE INSIGHT:** the torn-read fix (published snapshot) + the decoupled monitoring plane + headless/SSH viewing are **ONE pattern** — the GUI already reads the safe published snapshot; the `.E.1` aggregator-published-snapshot is the foundation for all three. → fold into `plans/_future/2026-05-12-decoupling-endgoal-roadmap.md`.

**CLOSE-OUT PLAN (the in-depth work — deserves a FOCUSED phase, fresh context):**
1. **Live-enable HARD gate** (safety; decision-log + boot-refuse).
2. **Deeper concurrency audit** — the 9 sites + a full sweep, as `.E.1` prep (a focused adversarial multi-agent pass).
3. **Anti-pattern + CI check** → `RECURRING_BUG_PATTERNS` (catches future instances; content in the new spec).
4. **`.E.1` structural fix** (aggregator rework — closes the class).
5. **Adversarial-audit SKILL** (so "check" auto-fires the methodology — addresses the over-lump failure mode directly; extends TECH_DEBT-164 + `feedback_auto_route_input_to_matching_skill`).
6. **Remaining Net-1** characterization tests (oms-ts-1 / hpg-bc-1 / wfa-1) — in-session-able.
7. **`.E.1` plan code-sample B-Plus reconciliation** (surfaced 2026-06-10 by the F-2 fold) — the `.E.1` plan carries **10 PRE-EXISTING** illustrative template-code fragments (lines ~362–925; use `F`/`RING_SIZE` without standalone context) that trip the B-Plus symbol check now that the plan is a "session plan." NOT fabrications — a B-Plus false-positive class (context-dependent illustrations). My F-2 fold (prose, lines 68/280) + this register are CLEAN. Reconcile at the `.E.1` plan review: add B-Plus exemption markers OR wrap each fragment in compilable template context. Tracked, NOT blocking (workspace commits have no B-Plus pre-commit gate).

## 📍 2026-06-13 PHASE-4 CLOSE-OUT — the 4 durable-now capital fixes DONE + the 2 engine findings + the live-gate

**The 4 durable-now capital fixes LANDED + committed + pushed:** **A17** (REST `order_complete` from venue status; D-215) · **A21** (reconciler cash-leg Money/H4 + detect-only `ProcessReconcile`; D-216) · **A20** (boot seed-don't-replay; persist REVERSED to `.E.1` — paper never consumes the watermark; D-216) · **NEW-1** (split-brain collapse → `ControllerConfig_IsLiveCapital`; legacy LIVE hard-refused; the use_real_money/trading_mode conflict HARD-REFUSES boot per **D-218**; RBP **Class 47**; D-217/D-218). Each: full fan-out → fix → anti-vacuous char-test → 5-way verify → `.E.1` fold.

**The 2 engine findings (2026-06-10 adversarial audit) re-dispositioned:**
- `oms-ts-2` per-core vs OMS P&L rounding-divergence → **CLOSED-by-D-190** (the gross-formula SSoT `Money_FillGross` unified the two derivations; confirm at pickup by code-read).
- `tsa-live-2` boot-reconcile same-core fill collapse → **cold-boot-DEFUSED by A20** (seed-don't-replay → no replay on cold boot → no collapse); the warm-tail collapse rides `.E.1` venue-net-reconcile.

**🔴 The live-enable HARD gate (this register's §SESSION-STATE #1 / line 21 "proposed") — VERIFIED MISSING (2026-06-13).** `LiveReadiness_Verify` has 8 REFUSE checks (secret/mlockall/cores/models/age/feature-hash/label-hash/stamps-hmac) — **NONE blocks on the torn-read class / conc-5**. So sharded live (`trading_mode=live` + valid secrets/models) RUNS with the Knight-shaped reconciler-corrects-on-a-torn-read race live. → `.E.0.10` Phase-D, CRITICAL.

**`.E.0.10` (Net-1) is NOT closed** — the characterization net (F-059 keystone + oms-ts-1/wfa-1/F-090/F-093/F-046/F-018 + PERSIST) + the structural closes (#9/#10/A6) + the live-gate remain. Pickup: `handoffs/2026-06-13-E.0.10-net1-closeout-and-live-gate-handoff.md` (Phases A0-D).

## Re-triage progress
| batch | status |
|---|---|
| fpmem cluster (core 3 + layout) | ✅ VERIFIED CLOSED (2026-06-10) |
| fpmem cluster (granular ×8) | ⏳ PENDING |
| E.1 bucket (30, incl. `conc-5` CRIT) | 🔄 IN-PROGRESS — HIGH targets verified; MED/LOW + 3 detail-reads pending |
| PERSIST subset of PRE-PAPER-TEST | ⏳ PENDING |
| E.2 / E.3 / E.5 / E.6 (routed to own ships) | ⏳ PENDING (disposition only) |
| BACKLOG-STANDALONE (24) + 93 TECH_DEBT + 5 PARITY | ⏳ PENDING |

## fpmem cluster — VERIFIED (code-read, not token-count)

| finding | sev | disposition | evidence |
|---|---|---|---|
| `fpmem-1` FPN_Sqrt<64> native-path IEEE round-trip voids deterministic NR sqrt | HIGH | **CLOSED-by-.E.0.9-P5** | native-forwarding block REMOVED; `FixedPointN.hpp:1246` "the flag is inert"; ONE FP path (certified 16B bodies) — no native branch to round through `double` |
| `fpmem-2` strict-aliasing/misaligned `__uint128_t` pun in `_to_fp64`/`_from_fp64` (accounting UB) | HIGH | **CLOSED-by-.E.0.9-P5** | FP64 conversion block REMOVED (`FixedPointN.hpp:490`); `FixedPoint64.hpp` deleted; 5 residual refs are historical comments/tombstones, not live code |
| `fpmem-3` FPN native path untested — tests omit `USE_NATIVE_128` (tested≠shipped) | HIGH | **CLOSED-by-.E.0.9-P5** | native-FP pin REMOVED from `controller_test` (`CMakeLists:221/233/244`); one FP path → nothing to diverge |
| `fpmem` layout static_assert (sizeof==16) | LOW | **CLOSED** | `static_assert(sizeof(FPN_Binary<64>)==16)` `FixedPointN.hpp:44` + Money `:102` present |
| `fpmem` granular ×8 (DivNoAssert-sat / FPN_Negate-untested / Mul-AddSat-sat / ToDouble-dead-work / IsValidFinite-recompute / FromString-overflow / partial-native-spec / `FP64_AddSat`-dead) | mixed | **PENDING-RETRIAGE** | `FP64`-prefixed → likely CLOSED (FP64 gone); `FPN_`-op ones may apply to the new `FPN_Binary`/`Money` op surface → per-finding grep owed |

**Method note (the fpmem lesson, harvest candidate):** grep-COUNT misleads in BOTH directions. `USE_NATIVE_128` = 8 refs but INERT (build-fingerprint string + provenance comments). Disposition requires reading what each ref IS, never its presence/absence. Sharpens `feedback_run_doc_ci_tools_first_never_hand_verify` → *verify-by-context, not by-count.*

## E.1 + characterization targets — re-triage (verified by context, 2026-06-10)

**Pattern: structural/concurrency findings were CLOSED by later ships; the TEST-GAP findings ("no test for X") are still OPEN — and they ARE Net-1's deliverable** (nobody wrote them; that is the point of Net-1).

| finding | sev | disposition | evidence (code-read) |
|---|---|---|---|
| `conc-bc-1` paper-reset no quiescence handshake | HIGH | **CLOSED** | park handshake present — `paper_reset_in_progress` → slow-paths park (`Run.hpp:1670`; `Async.hpp:564` "slow-paths park (yield)"; producer clears on reset complete) |
| `hpg-bc-1` parity oracle validates `SG_Evaluate` stub | HIGH | **SUPERSEDED → Net-1** | **CORRECTED 2026-06-11 (NOT "gone"):** `LegacyReferenceDriver` is a live EXPERIMENT-ONLY header (`CoreFrameworks/LegacyReferenceDriver.hpp`, old `FPN_Binary`, NOT in `CMakeLists`/`build.sh` — included only by `experiments/per_core_sharding/test_migration_head_to_head.cpp`); the `StrategyParameters.hpp:671` comment references it. Byte-identity for the real per-fill TP/SL exit path is false-by-construction → fix = F-059 golden-master, a genuinely net-new Net-1 deliverable |
| `tsa-live-2` `Reconcile_ApplyMissedFills` asserts count+watermark, not balance | HIGH | **✅ DONE (Net-1 #2)** | extended Test 3 (`controller_test:20131+`): after 3 replayed buy fills, `Portfolio_CountActive(&oms.portfolio) >= 1` → the portfolio side-effect is now asserted. **build+suite GREEN 3286/0.** Golden-master finding: event-log append is a no-op in this fixture (buffer unallocated) → the in-struct portfolio bitmap is the right evidence (documented in the test). |
| `rsf-ts-1` `Regime_Classify` no direct classification test | HIGH | **✅ DONE (Net-1)** | added 12 checks (`controller_test:~19678`) for classification + direction/strength/hysteresis (the cold-start gate was the only prior coverage) |
| `oms-ts-2` `sum(core_realized)==oms->realized_pnl` never reconciled | HIGH | **✅ DONE (Net-1 #3)** | added to the mode-1 post-exit test (`controller_test:7810+`): `Money_Eq(cores[0].core_realized, oms.realized_pnl)` + `Money_Eq(cores[0].core_fees, oms.total_fees)`; **build+suite GREEN 3288/0** — the two independent net-P&L derivations RECONCILE exactly (no divergence). |
| `persist-8` paper-reset omits `ExecutionCore_Init` → zombie active | HIGH | **✅ interim FIXED (`d0a6bb1`); robust rides conc-5** | confirmed REAL (hot mirror persisted, un-cleared) → LOW (paper-mode + guarded at `OrderManager.hpp:1185`); interim fix clears `cores[c].active=0` in the reset (`Async.hpp:726`); full hot-path quiesce pairs with conc-5 |
| `persist-dod-1` torn un-sync snapshot save | HIGH | **REAL (re-examined) → rides `.E.1` aggregator** | the "likely-closed" verified the ARCHIVE save; the torn risk is the PERIODIC save (`Async.hpp:431`, paper-mode, producer thread reads live OMS) — confirmed by the code's own KNOWN-RACE comment. 1 of the 9 torn-read-class sites; closed by the `.E.1` aggregator. LOW-MED (paper). |
| `wfa-1` warm-restart restores positions exit-disarmed | HIGH | **LIKELY-CLOSED** | restore ARMS the gates — `ShardedSnapshotPersist.hpp:54` "stay armed-but-inactive", `:681` "hot-path SG/TP/SL gates armed" |
| `D-110` snapshot money-exact round-trip | HIGH | **✅ DONE (Net-1 #1)** | 7 money assertions TIGHTENED `fabs(ToDouble-V)<1e-6` → `Money_Eq(x, MQ(V))` (`controller_test:5816-5860`); **build + suite GREEN 3285/0** → decimal recovery round-trips money EXACTLY (probe came back CLEAR — no representation bug) |

## MED-tier OMS/money re-triage — closing the false-floor (2026-06-11)

The readiness pass flagged a false-floor risk: money surfaces the `.E.1` rename TOUCHES that the headline 8-target list didn't characterize. Re-triaged the MED-tier OMS/money findings against the deduped corpus (`CANONICAL-FINDINGS.md`), paste-not-summarize. Scope rule: rename-touched-invariant = IN; changes-by-design / future-path / separate-fix = OUT-with-reason. (Disposition = SCOPING; each IN-one's CURRENT behavior is code-verified at test-write per this register's `verification_rule`.)

| finding (orig id) | sev | what | Net-1 scope |
|---|---|---|---|
| `F-090` + `F-093` | MED | executionReport + `Reconcile_ParseMyTrades` parsers assert `is_maker`/`order_complete`/`is_buyer` but NEVER the parsed price/qty/commission (the parser's core job) | **IN** — the "parser byte-equiv" deliverable; characterize the FULL parsed output (price/qty/commission), not just the flags |
| `F-046` (`oms-ts-4`) | MED | `EventLoop_FlattenAll` tested only push-count, 1 position, partials-OFF; partials-on logical_core shift + drain-to-close + **balance** untested | **IN** — rename touches FlattenAll + the partials slot model; characterize balance-after-flatten + the partials-on path |
| `F-018` (`oms-ts-5`) | MED | partial-EXIT money path never end-to-end; asymmetric-leg tests bypass `handle_sell_fill` + hand-seed sibling arrays/win-bitmap | **IN (money only)** — the per-leg win/loss aggregation (`FPN_GreaterThan(net,0)`) + `last_exit_fill_price`/`fee` writes are invariant → characterize vs the real `handle_sell_fill`→`DrainPostFill` consumer. The slot-MAPPING is `.E.1` changes-by-design — do NOT freeze it |
| `F-044` | MED | `Order_BindPreResolved` fixes `pre_resolved.fee_rate` at SUBMIT with `is_maker=0`; real `is_maker` set at FILL, no re-bind → maker fills booked at TAKER rate but counted in the maker bucket | **OUT (future maker path)** — latent; live is taker-only/paper. Fix rides `.E.1`/`.E.3` fee work (composes D-123/D-173). Freezing the buggy current behavior would be wrong |
| `F-045` (`oms-ts-3`) | MED | maker-side of `OrderManager_AccountMakerTakerFee` untested; `total_fees==maker+taker` asserted only on the legacy path | **OUT (future maker path)** — sister to F-044; characterize when the maker path goes live (`.E.5`/`.E.6`) |
| `F-096` | MED→**CORRECTNESS** | partial-exit per-leg ENTRY qty split in `double` (**H4 violation** on the sizing path) + leg-A+leg-B qty does not conserve `intended_qty` | **SEPARATE FIX, not characterize** — an H4 `double`-on-sizing violation is a bug to FIX, not a behavior to freeze. **OWED: PARITY/TECH_DEBT entry** + fix rides `.E.1` partial-exit rework |

**Net:** false-floor closed. The IN money surfaces (parser full-output F-090/093, FlattenAll balance F-046, partial-exit money F-018) join `oms-ts-1`/`hpg-bc-1`/`wfa-1` as Phase-2 characterization targets. The OUT ones each carry an explicit reason (future-path / changes-by-design / separate-fix) — none silently dropped. **F-096's H4 `double`-on-sizing is the one real correctness finding** surfaced here → ledger it.

## Adversarial audit of the 3 money-core characterizations (2026-06-10, 3 independent agents)

Operator-directed adversarial audit (completeness / vacuousness / frozen-bug lenses, none fed my findings). **It CORRECTED the "3/6 done green" assessment — 2 of 3 had real issues + surfaced 2 ENGINE findings a self-audit would have confirmed green.**

| target | verdict | finding |
|---|---|---|
| **oms-ts-2** | ❌ **FROZE A FALSE INVARIANT + ENGINE finding** | the two P&L derivations ROUND DIFFERENTLY: `oms.realized_pnl` uses `Money_Mul(Money_Sub(exit,entry),qty)` (1 rounded mul, `OrderManager.hpp:1209-1216`); `core_realized` uses `Money_Sub(Money_Mul(exit,qty),Money_Mul(entry,qty))` (2 rounded muls, `ControllerEventLoop.hpp:1528-1540`). Agent compiled both + swept 20M realistic triples → **25.01% diverge by 1 ULP (1e-8), and it ACCUMULATES** (`core_realized` never re-synced). My `Money_Eq` passed ONLY on the test's rounding-clean inputs (60000/61200/0.01) → enshrines a contract that fails ~25% on live data + masks a latent accounting discrepancy. **ENGINE FINDING:** two P&L truths drift by rounding — what Ship-B exact-money was meant to eliminate. FIX: unify the gross (both share ONE `Money_Mul(Money_Sub(exit,entry),qty)`) → reconciliation legitimately exact; OR tolerance-band the test + ledger the divergence. |
| **tsa-live-2** | ⚠️ **WEAK + masks a recovery question** | `Portfolio_CountActive >= 1` is invariant to how many fills applied: all 3 boot-replayed buys route to core 0 (empty order_bitmap → fallback, `Reconcile.hpp:236-245`) → `Portfolio_OpenSlot` OVERWRITES slot 0 ×3 (`Portfolio.hpp:347-372`) → exactly 1 position, **2 of 3 fills' qty/price silently dropped**. `>=1` passes even if only 1 fill applied. Silent on no-balance-debit (buys net-on-close). FIX: `== 1` + assert surviving Position qty/price (last-write-wins) + document the collapse; **investigate whether dropping 2 replayed fills is intended or a recovery defect.** |
| **D-110** | ✅ **assertions CORRECT, coverage INCOMPLETE** | the 6 OMS + 1 core money fields are correctly money-exact + non-vacuous (fresh `r2` baseline rules out no-op-load false-pass — verified). BUT **8 per-core money fields** (allocated_balance, core_fees, core_open_notional, core_gross_wins/losses, last_entry_price, core_peak_balance, core_dd_pct) + **7 Position<F> money fields** are SAVED-but-UNASSERTED (`ShardedSnapshotPersist.hpp:187-213` + `:169`; Test 2 never opens a position); pnl_feeder double-tolerance + a Money/FPN_Binary sizeof type-pun. FIX: extend Test 2 — assert all per-core money-exact + open a Position with distinct values + assert each. |

**2 ENGINE findings to ledger** (operator disposition needed): (1) per-core vs OMS P&L derivation rounding divergence; (2) boot-reconcile same-core fill collapse / data-loss.

**Meta (harvest candidate):** a GREEN characterization test masked a real engine bug — oms-ts-2 froze a false invariant that holds only on clean inputs. Self-audit confirms; adversarial refutes. Validates adversarial-audit-for-capital.

## Process-error retro — why "3/6 done green" was WRONG (2026-06-10)

**Root cause: treated SUITE-GREEN as VERIFIED + SELF-ATTESTED my own Phase-2 work** — both things the corpus already forbids:
- A green test proves the assertion-as-written holds on the input-as-written — NOT that it's the right invariant (oms-ts-2), complete (D-110: 1/9 per-core fields), or non-vacuous (tsa-live-2: `>=1`). = **AR-4 verification-by-proxy at the test-WRITING layer.**
- Didn't enumerate/generalize: "reconciles exactly" from ONE clean input (**AR-1**); tightened seen-fields vs the full persist registry set (paste-tool-output). Both are codified memories I failed to apply.
- Self-attested: declared own work done on green; no independent adversarial pass (which I have at `/close-session` 5.5 + `/precoding-audit-gate` 3.5) until operator forced it.
- **DEEPER:** no TRIGGER fired the disciplines on "I wrote a test + it's green." Recurrence-despite-memory = **M7**.

**Codification queue (from this retro):**
1. memory `feedback_passing_test_is_not_verification` (green ≠ verified; adversarial-verify own characterization work before "done").
2. **M7 structural:** the adversarial 3-lens pass (complete / non-vacuous / not-frozen-bug) FIRES on characterization/test work before "done" — the missing trigger.
3. DESIGN_SPEC `characterization-test-discipline` (the 3 lenses).
4. meta-anti-pattern-index harvest: green-test-as-done + adversarial-beats-self.
5. PARITY: per-core vs OMS P&L derivation rounding divergence.
6. **BLOCKER:** MEMORY.md at byte-cap (WH-6) → compress before landing new memories.

**More-issues sweep owed:** P&L divergence likely a CLASS (other paired per-core-vs-aggregate money derivations); re-check the fpmem "CLOSED-by-context" calls with the same skepticism; run concurrency/replay lenses.

**ENGINE root-cause TRACED → D-105 (foreseen-but-incompletely-solved):**
- The 1-mul/2-mul split is **PREEXISTING** — present in the FPN era (`.E.0.6`: DrainPostFill `:1532` 2-mul vs mode-0 `:880` 1-mul vs Portfolio_CloseSlot 1-mul). Invisible at FPN's 2⁻⁶⁴ granularity (~1e-19); the P2b flip (838bf09) mechanically carried it into decimal where half-even at 1e-8 = a real 1-ULP divergence. **The flip ACTIVATED a latent bug, didn't write it.**
- **D-105 (2026-05-30) IDENTIFIED THIS COHORT** ("decimal-sensitive cohort that must move together: handle_sell_fill + EventLoop_OnEvent exit + Portfolio_CloseSlot gross + fee muls + replay") AND the risk ("live≠replay if not uniform"). BUT the solution was **uniform rounding MODE (half-even), not single-source FORMULA.** Same mode ≠ same value when one path is 1-mul and another 2-mul. AND D-105 judged the existing inconsistency "correctness-WIN, not a present bug (0 CRITICAL)" because "entry/exit/replay all truncate the same way → balances reconcile" — TRUE under FPN truncation, FALSE once decimal half-even amplifies the formula gap. AND the cohort list (`:1959-1967` + `:862-890`) appears to have **missed the DrainPostFill mode-1 path (`:1528-1540`)** — the 2-mul outlier.
- **Net:** foreseen (cohort + risk), but solution-incomplete (mode-uniformity ≠ formula-SSoT) + cohort-incomplete + a "not-a-present-bug" judgment that didn't survive the representation change.

**Codification this opens:** amend **D-105** (add formula-SSoT requirement + the cohort gap); extend **AP4** (rounding-mode class → also "single-source the COMPUTATION, not just the mode, for any money value derived ≥2 ways"); NEW **landmine** (parallel money derivations agree under truncation/binary, diverge under decimal — the epoch activates latent divergence); NEW **meta-anti-pattern** (judged "not a present bug" under the OLD representation without checking it survives the NEW one); PARITY entry; **codebase-wide sweep** for parallel/dual money derivations.

**✅ FIX LANDED (D-190, 2026-06-10):** NEW `Money_FillGross(entry, mark, qty)` (1-mul SSoT, `Portfolio.hpp:397`) — ALL 5 price-diff gross sites route through it (realized: `Portfolio_CloseSlot` + `ControllerEventLoop:879/1537`; unrealized: `Portfolio_ComputePnL:443` + MTM kill-switch `:2896`). The `:1536` 2-mul→1-mul is the only behavior change; unrealized folds are behavior-neutral (already 1-mul). **3288/0** + clean compile. **ADVERSARIALLY VERIFIED:** structural grep proved completeness — and CAUGHT 2 sites (`:443`/`:2896`) the initial enumeration missed → folded (the completeness lens working). Memories `feedback_passing_test_is_not_verification` + `feedback_single_source_the_computation_not_just_the_mode` codified; D-190 decision-log amendment landed. STILL OWED: backtest-golden regen check (D-105-flagged; unit suite unaffected — clean inputs); AP4 class extension + characterization-test DESIGN_SPEC + M7 "adversarial-pass-fires-on-test-writing" trigger (deeper process codification); the other Net-1 tests (tsa-live-2 strengthen, D-110 extend); codebase sweep for sibling parallel money derivations.

**✅ "DON'T REINTRODUCE" NET LANDED (3 layers, 2026-06-10):**
1. **Behavioral** — D-190 regression test in `controller_test` (now 3290/0): pins the 1-mul/2-mul divergence as REAL on divergent inputs (verified non-vacuous) + guards `Money_FillGross` against reverting to 2-mul.
2. **Structural** — `tools/check_money_gross_single_source.py` wired into **pre-commit Check G** (scoped to money-path accounting headers): catches a re-introduced open-coded 2-mul gross at commit. Teeth-proofed via `..._selftest.sh` — and the self-test CAUGHT a Landmine-5 bug in the guard itself (`.resolve()` followed the symlinked `tools/` → wrong root; fixed to `.absolute()`). Both enrolled in TOOLS.md.
3. **Institutional** — D-190 + PARITY-038 + Landmine 8 + 2 memories + the `Money_FillGross` doc-comment.
Doc sweep CLEAN (tools-inventory + budget + metadata all green).

**✅ ADVERSARIAL SIBLING-DIVERGENCE SWEEP (3 independent agents, 2026-06-10):** hunted the whole money path for OTHER D-190-class (parallel-derivation) divergences. **VERDICT: clean for steady-state** — gross/net/total_fee/exit_fee(market)/open_notional-decrement/balance/realized_pnl/win-loss-bucketing/boot-capital-split all reconcile across the live + replay + backtest paths. The agents CROSS-CHECKED each other: Agent 1 rated a warm-restart fee-replay HIGH (claimed core_realized vs realized_pnl diverge by the full fee); Agent 3 REFUTED it by reading the callers (both replay folds pass `Money_Zero()` rate → both gross-of-fee → they AGREE; `ControllerEventLoop.hpp:951-952` + `OmsFieldRegistry.hpp:741-743`). Two items surfaced, both pre-existing/known:
  - **NEW (low/latent) — track for `.E.1` fee work:** the warm-restart replay folds (`Portfolio_FromEventLog`, `EventLoopState_ReconstructPerCoreFromEventLog`) IGNORE the stored booked fee `e.fee` and recompute `notional×rate`. Harmless today (rate=0, documented gross-of-fee boot replay) but a latent trap — they should CONSUME `e.fee` (re-single-source the fee exactly as D-190 single-sourced the gross). Composes with D-123/D-173 fee booking; natural home = `.E.1` adapter/fee rework.
  - **KNOWN (D-123, deferred to `.E.1`/`.E.3`):** live venue-ingest is still a `double` pipeline (`OrderResult` carries `double`; `money_from_double_payload` bridge; REST avg-price = `cum_quote/exec_qty` double-division diverges from WS `"L"` exact). D-123 already DECIDED the fix (parse venue strings → decimal, carry decimal on `OrderResult`, book reported commission source-exact). Live is pre-validation. No new action — verified tracked.

**Codified the operator preference (2026-06-10):** memory `feedback_adversarial_framing_default_for_checks` — audit/check/review skills default to ADVERSARIAL framing (FIND/REFUTE, not confirm) + prefer multiple independent agents that cross-check. (MEMORY.md hit its byte-cap a 5th time this session — a real WH-6 structural problem; the always-loaded index needs a context-aware-loading split, not more trimming.)

**✅ NET-1 CONTINUATION (2026-06-10) — the 2 adversarial-audit findings on the prior 3 tests CLOSED:**
- **tsa-live-2 STRENGTHENED** (`controller_test:20156+`): `>=1` → `==1` + qty 0.001 + surviving entry 50050 (array-order last-write-wins) — non-vacuous; pins the released-order→cores[0] collapse. Cross-linked TECH_DEBT-072 BOTH ways (the collapse is a sibling aspect of the documented fee-fallback; not a new defect — a per-core one-position-model limitation).
- **D-110 EXTENDED** (`controller_test:5749+`): all 9 persisted per-core money fields (was just `core_realized`) + 7 `Position<F>` money fields (Test 2 now opens a position) assert money-exact across save→recover. Suite **3331/0**.
- **Deferred debt → TECH_DEBT** (per `feedback_opportunistic_tech_debt_closure` — subsume-vs-adjacent): TECH_DEBT-162 (replay folds ignore `e.fee`), -163 (MEMORY.md WH-6 byte-cap), -164 (D-190 process-codification residue). Golden-regen verified NOT-owed (kernel-only golden, D-190 doesn't touch it); double-ingest D-123-tracked.
- **`rsf-ts-1` ✅ DONE** (`controller_test:19678+`): Regime_Classify direct classification — 12 non-vacuous checks across TRENDING (strong) / MILD_TREND / TRENDING_DOWN / VOLATILE / RANGING + the hysteresis LAG (proposed flips on cycle 1, current commits on cycle == threshold). Thresholds set explicitly. Suite **3343/0**. (Was: only the cold-start gate covered.)
- **`persist-8` ⚠️ DEFECT CANDIDATE SCOPED (not just a test gap; 2026-06-10):** the paper-reset (`Async.hpp:689-729`) wipes the portfolio (`OMS_RESET_AUTOPOPULATE`→`Portfolio_Init`) + ctx (`CORE_CTX_RESET_AUTOPOPULATE`) but does NOT clear the `ExecutionCore` hot mirror (`active`/`live_tp`/`live_sl` — it's a persisting pointer, not in either reset). `paper_reset_in_progress` is checked at EXACTLY ONE site — the SLOW-path loop (`Run.hpp:1670`); the HOT path never parks on reset. The slow-path DOES re-sync `active` from `portfolio.active_bitmap` (`ControllerEventLoop.hpp:2721`) but cadence-gated (waits `slow_path_interval` ticks after un-park). → **ZOMBIE-ACTIVE WINDOW:** reset-while-position-open leaves `ExecutionCore.active=1` + stale TP/SL; the un-parked hot path can evaluate (and potentially fire a phantom TP/SL/sell) on the wiped portfolio before the slow path re-arms. **VERIFY-FOR-RESOLUTION (race-sensitive; pair with conc-5):** (a) does the producer feed ticks in-window → exploitable; (b) the phantom-sell harm-trace (corrupts the fresh-session balance?); (c) fix = clear `active`/`live_tp`/`live_sl` IN the reset (or park the hot path during reset). Adversarially verified the trace (one-site park check + the 2721 re-sync) — not over-claimed. Like oms-ts-2→D-190, the characterization surfaced a real bug.
- **`persist-8` ✅ RESOLVED + SEVERITY CORRECTED → LOW (2026-06-10; operator adversarial pushback "are you sure it's major?"):** re-examination de-rated it — (1) paper-reset is PAPER-MODE ONLY (`Async.hpp:561` `!use_real_money`) → ZERO real capital; (2) the phantom sell is GUARDED — `HandleFill` checks `active_bitmap` before processing (`OrderManager.hpp:1185`) → phantom = no-op, no corruption. The zombie flag is real but defended downstream → hygiene, LOW. I had OVER-RATED it (the Agent-1 trap, on my OWN finding — the human ran the adversarial lens on me; now a worked example in `adversarial-multi-agent-audit-methodology.md`). **FIX LANDED (d0a6bb1):** `cores[c].active = 0` in the reset loop (reset already holds the `cores[]` array; single-byte hardware-atomic; once active=0 the hot path skips the exit eval so stale `live_tp`/`live_sl` are never read). Structurally correct + 3343/0; the behavioral integration test + full hot-path quiesce (the robust version) pair with conc-5. **COMPLETENESS-SWEPT:** `ExecutionCore` is the ONLY pointer-target in the reset registry (`CoreCtxInitRegistry.hpp:94`) — single instance, no siblings of this shape. **META-LESSON (`/close-session` HARVEST candidate — NOT hand-curated, per the meta-anti-pattern-index population rule):** *structural-pattern false-completeness* — a registry/SSoT/walker creates a false sense of TOTAL coverage; the thing OUTSIDE its reach is the gap (D-190: gross open-coded outside the `Money_FillGross` SSoT; persist-8: hot mirror outside the reset registry). 2 instances → an AR-family candidate; harvest at close.
- **`persist-dod-1` RE-EXAMINED → REAL (NOT closed; persist-8's incomplete-quiesce finding led here, 2026-06-10):** the register's "likely-closed" verified the ARCHIVE save (`Async.hpp:605`, inside the paper-reset quiesce) — but the torn-save risk is the **PERIODIC** save (`Async.hpp:431`, "~every 1024 slow-path cycles, paper-mode `!live_trading`"), which runs on the PRODUCER thread and reads the live OMS state DIRECTLY (`ShardedSnapshot_Save` takes `const EventLoopState*` — no copy/seqlock) while the DRAINER writes balance/portfolio → torn save. CONFIRMED by the code's OWN `KNOWN RACE` comment (`Async.hpp:434-438`, audit 2026-04-09): the producer reads `oms->balance` torn vs the drainer. LOW-MED (paper-mode + low-prob + recovery already approximate). The comment's "FPN_Binary<64> 64 words" is STALE post-Ship-B (Money = 16B/2 words) but the torn-read survives for any >8B value.
- **🔑 CORRECTED via adversarial 3-agent refute (2026-06-10) — the "cluster of 4" was OVER-LUMPED (operator caught it: "are you making stuff up?"):** an earlier note here claimed persist-8 + persist-dod-1 + kill-switch + conc-5 share ONE root. **WRONG — I verified 3 and ASSERTED conc-5 in.** Real grouping: **(A) torn cross-thread reads of 16B `Money`** = `persist-dod-1` (full-state periodic save) ⊇ the kill-switch `balance` read (subset) — ONE root, seqlock/consistent-copy fix. **(B) `persist-8`** = standalone (stale-flag / missing-reset; single byte, NO tear; interim-fixed `d0a6bb1`). **(C) `conc-5`** = standalone (SPSC ring pushed by 2 producer threads → lost/dup order; fix = single-producer-by-construction / MPSC, a seqlock does NOTHING for it). They share a remediation EPOCH (`.E.1` topology rework) + a THEME (the sharded topology added a 2nd writer / un-parked thread the original SPSC/single-writer discipline never covered, and the in-code comments lag the thread model) — NOT one root mechanism.
- **WIDESPREAD (agent sweep) — class (A) is SYSTEMIC, 9 sites:** the OMS money cluster (`balance`/`realized_pnl`/`total_fees`/`portfolio`/per-core P&L) has **NO seqlock + NO atomic anywhere**; every non-drainer reader reads raw 16B `Money`. The GUI consumer is the ONE site done right (reads the seqlock-published `TUISnapshot`). **3 are LIVE capital-control paths** (worst-first): the **LIVE reconciler** reads `balance` torn then *pushes a balance CORRECTION to live capital* (Knight-Capital-shaped self-healing-on-torn-read); the **global kill switch** + the **per-core MTM kill switch** read torn balance/P&L → false-or-missed trips on the capital guard. Plus a GUI drag-TP/SL **write**-hazard (16B exit price). **DELIBERATE accepted-race** (in-code `Async.hpp:434` audit 2026-04-09 + decision-log **F-2**/**D-74** + `E.1-findings` `conc-1`/`conc-5`/`conc-6`/`conc-dod-1`); the structural fix (aggregator-as-single-writer + seqlock-published account snapshot) is SLATED for `.E.1` ("free bonus" per F-2). **MISSING: the reader-side DESIGN_SPEC** — existing specs cover only the WRITER/false-sharing side (slow→hot cfg seqlock, alignas(64) flags). NEW spec owed (writing this session) + a `RECURRING_BUG_PATTERNS` class + a CI check (flag any >8B cross-thread-written field read/assigned outside a seqlock); **site remediation rides `.E.1` as the topology rework, NOT piecemeal now.** Comment fix landed (the triple-stale "64 words").
- **STILL OPEN in Net-1:** `hpg-bc-1` (F-059 golden-master), `wfa-1` (confirm — likely closed); `conc-5` CRITICAL runtime-confirm; the Phase-1 backlog re-triage (93 TECH_DEBT + 5 PARITY); batch adversarial-verify the new tests at ship close. **RESOLVED this session:** `oms-ts-1` (fee-EXACT characterization landed 2026-06-11 — `controller_test.cpp:8311`; exact `Money_Eq` total_fees==2.412 / realized_pnl==9.588 / balance==before+9.588; hand-derived from booking code, GREEN cross-checks the reading; suite 3347/0), `persist-8` (interim FIXED `d0a6bb1`; robust rides conc-5), `persist-dod-1` (REAL torn periodic-save; rides the `.E.1` aggregator), `rsf-ts-1`/`tsa-live-2`/`oms-ts-2`/D-110 (tests landed).

## CRITICALs (must runtime-confirm on a disposable clone — corpus hard-constraint #3)
- `conc-5` (E.1) — submit_queue multi-producer race. **PENDING** — `.E.1` CHANGES-BY-DESIGN claim is "LIKELY/verify"; confirm via `tsan` + drainer/slow stress. NOT closed; the rename must handle it.
- `live-bc-1` (E.6) — Binance global/US endpoint split. **PENDING** — routed to `.E.6` (out of Net-1 scope); disposition only.

## Net-1 characterization targets (the PERSIST kept-surfaces, by finding ID)
`oms-ts-1` (fee-blind ±$15 check) · `oms-ts-2` (core/oms reconciliation) · `persist-dod-1` (torn snapshot) · `wfa-1` (warm-restart exit-disarmed) · `tsa-live-2` (recovery count-not-balance) · `persist-8` (paper-reset zombie active) · `hpg-bc-1` (stub-oracle / F-059) · `rsf-ts-1` (Regime_Classify untested). → each becomes a characterization test in Phase 2; disposition (already-tested? buggy?) verified in Phase 1.

## 🔎 Net-1 adversarial money-surface bug-hunt (2026-06-11 — 5 independent surface-blind agents)

> **⚙️ A1 FIX LANDED (2026-06-11):** single-sourced `ResolvePerFillTpPct/SlPct` (`StrategyParameters.hpp`) — the entry dispatcher (SimpleDip/MR/EmaCross/Momentum) + the restore site now resolve the per-NODE override, not the global. Characterization test GREEN (`controller_test.cpp`: restore → 60720/59400 override, NOT 60480/59640 global), suite **3368/0** (+MR cohort variant). **✅ CLOSED 2026-06-11** — 3-agent independent refute (vacuity/value/completeness) all SOUND/CORRECT (helper byte-identical to every pre-fix entry inline → no regression; SID round-trips before recompute; leg-B converges); folded the MR-SID variant + the EmaCross stateless H22-exemption. TECH_DEBT-168 + PARITY-039 closed. ML restore-recompute stays GLOBAL (blend unreproducible at restore — a separate tracked sibling, out of A1 scope).

The plan's stated "one place the fan-out earns its tokens" (money-surface completeness). 5 independent agents on exit-pricing / partial-exit / parser / FlattenAll / sibling-divergence. **3 NEW HIGH bugs verified against code (refute-condition sealed by the orchestrator before disposition — adversarial-default + AR-8); convergent confirmations of F-096 + D-123; 3 clean refutes.** Validated the thesis: the fan-out found 3 NEW HIGH bugs no single audit had surfaced.

### NEW findings (disposition + .E-home)

| id | finding | sev | site (sealed) | disposition | home |
|---|---|---|---|---|---|
| **A1** | Warm-restart recomputes `live_tp/live_sl` from GLOBAL `take_profit_pct`, ignoring the per-strategy override (`simpledip/mr/emacross_tp_pct`) → a restored position exits at a DIFFERENT TP/SL than while live (SimpleDip/MR/EmaCross when override set; Momentum/ML unaffected). Leg-B same. | HIGH | `ShardedSnapshotPersist.hpp:653` vs `StrategyParameters.hpp:327`; sealed: `ResolveForCore@1383` does NOT fold the override; restore comment `:644` states the violated intent | VERIFIED bug — char-now (correct-value, paired w/ fix to avoid enshrining) + FIX = single-source `ResolvePerFillTpSlPct(core,strategy,cfg)` for entry+restore | TECH_DEBT-168 · PARITY-039 · **✅ CLOSED in `.E.0.10` 2026-06-11** (was triaged → `.E.1`; closed-now per the bidirectional rule — the fix is durable, not subsumed; see the A1 banner above) |
| **A2** | Parser drops partial-fill qty — venue `"z"` (cumulative) never parsed; `filled_qty` overwritten not accumulated; slot freed on 1st fill → later partials dropped → position sized at ONE leg | HIGH | `BinanceUserData.hpp:339` + `OrderManager.hpp:1394,1440`; sealed: `rg '"z"'`=0 | char-current + FIX | TECH_DEBT-169 · **fix → D-123 venue-ingest (`.E.1`/`.E.3`; SWAR-parse notes)** |
| **A3** | FlattenAll half-flattens under partials — ignores `OMS_PushSubmit` return → queue-full leaves leg B OPEN on an emergency path + dirty `partner_pending_bitmap` mis-buckets next trade's W/L | HIGH | `ControllerEventLoop.hpp:3441-3443`; sealed: `submitted++` unconditional (verified: `OMS_PushSubmit:1069` returns false on SPSC-full) | **SPLIT (two-foundations).** Durable-now LANDED `.E.0.10`: A3-core honest count (`submitted += pushed`) + loud `SHORTFALL` log + A8 leg-gate `(slot&1)&partial_on` (`ControllerEventLoop.hpp` FlattenAll; build 3368/0). Char pins ONLY the STABLE happy-path (N legs → N submits w/ room). **Failure-path NOT pinned** — `.E.1` reworks the per-core SPSC topology, so freezing the queue-full half-flatten = a worthless golden that breaks at the rework. | TECH_DEBT-170 · honest-count contract + **retry + failure-path char → `.E.1`** (folded into the `.E.1` plan §TL;DR #2 submit-path rework, 2026-06-11) |
| A4 | BNB/non-USDT commission DROPPED (computed fee substituted); reconcile-replay path drops the parsed commission entirely (HandleFill called w/o commission args) | MED→HIGH-on-BNB | `OrderManager.hpp:1309-1318` + `Reconcile.hpp:546` | char-current + FIX rides D-123 | TECH_DEBT-169 (parser cluster) · D-123 |
| A5 | WS fill side never cross-checked vs venue `"S"` | ~~MED~~ → **dormant-tripwire (3-agent refute 2026-06-11)** | `OrderManager.hpp:1330` dispatch; match `:1359-1363` | **Intra-process slip UNREACHABLE** (fill→order match is full 64-bit exact-id equality + bitmap + dedup — A7 shape). Real residual = venue-side / future-LIMIT mismatch (dormant-assumption tripwire, sister to the taker-bound fee guard `:1131-1145`), not a live Knight bug. **`"S"` is NOT parsed + `OrderResult` has NO side field** → NOT "~5 lines"; needs a parser extract + a NEW POD field on the SPSC-ring struct. Chokepoint = `OrderManager_ProcessFillCommand` (1 site; WS+REST). | TECH_DEBT-171 · **fold into the D-123/`.E.1` `OrderResult` decimal touch** — add the side field in the SAME struct touch (do-it-once; NOT subsumed, but shares D-123's struct-touch + the A2/A4 parser surface) |
| A6 | ML barrier-blend `tp_pct/sl_pct` unclamped → negative `sl_pct` inverts SL above entry (instant fire) | ~~MED~~ → **LOW (3-agent refute 2026-06-11)** | `StrategyParameters.hpp:1611-1614` (line shifted post-A1) | **Unreachable from any IN-ENGINE computation** — cfg pcts walker-clamped ≥0, Ridge weights clip non-neg + renormalize, bandit weights softmax. Only inflow to a negative = an unvalidated negative `label_sl_pct` in an on-disk model stamp (`CoreModelZoo.hpp:347` raw float copy, no floor). Root = **unvalidated stamp INGRESS + unclamped EGRESS**, not "unlucky weights". | TECH_DEBT-171 · **durable-now, NOT subsumed** (`.E.1` SoA/topology doesn't touch blend math). Fix = clamp ≥0 + gate-zero/`SHALT` (NOT an `assert` — don't abort a node on bad fixture data) at the **gate-emit chokepoint (all 6 strategy emits, not ML-only)** + a **stamp-load floor** on `label_*_pct`. Two-layer: ingest-validate + egress-invariant |
| A7 | FlattenAll at `price≤0` → `Money_Zero` fill → notional wipeout (**sim-only**) | ~~MED~~ → **MOOT-UNREACHABLE** | `ControllerEventLoop.hpp:3426` | **NOT FIXED — phantom (reachability disproven 2026-06-11, code-read).** Backtest: gap=0 by construction → flatten can't fire (`ShardedBacktestDriver.hpp:320-333`); gate `ws_dead_time_flatten_enabled` OFF-by-default (live-deployment only); pre-warmup guarded; `current_price` = last-known/tick price (>0). Harmful case needs sim + gate-manually-on + an already-broken price source (a bigger bug than the wipeout). → CHARACTERIZE the guarantee (Net-1), don't patch. `feedback_moot_unreachable_disposition` | TECH_DEBT-170 (A7 downgraded; A3 stays open) |
| A8 | FlattenAll hardcoded `leg=0` for odd slots (benign — no `Order_GetLeg` consumer — future-proof) | LOW | `ControllerEventLoop.hpp` FlattenAll | **LANDED `.E.0.10`** (`5b02cce`): `leg = (uint8_t)((slot & 1) & partial_on)` — gated on partials. NB: corrects the bare `(slot&1)` first proposed, which mislabels odd STANDALONE slots as leg B when partials are OFF. | TECH_DEBT-170 (cluster) |
| **A9** | **Paper-mode slippage silently DEAD on the production sharded path** — `pre_resolved.slippage_pct` bound (`Order.hpp:363`) but READ at ZERO sites; `EventLoop_OnEvent` called w/o `cores` (`Async.hpp:840`) + returns before the slip body in mode-1 (`ControllerEventLoop.hpp:1908`); the slip block mutates a DISCARDED local `event` copy while the OMS submit books the raw `event.price` (`Async.hpp:887`→`OrderManager.hpp:995`). Paper + BACKTEST book at the raw trigger-crossing price → P&L systematically OPTIMISTIC (zero exec-cost model). Root: commit `0119551` wired the FEE half of the pre_resolved migration, orphaned the SLIPPAGE half. | **HIGH** | bound `Order.hpp:363` + 0 reads (grep-sealed, self-verified); `Async.hpp:840` (no `cores`) + `ControllerEventLoop.hpp:1908` (mode-1 return) + `OrderManager.hpp:995` (raw fill) | **VERIFIED (2 independent agents CONVERGED + self-re-read). char-current SLIP-LESS** (golden-master: freeze today's behavior + mark regen-on-fix) **+ FIX**. Durable-now candidate (`.E.1` renames the path, doesn't redo the slip-apply) → **OPERATOR-TRIAGE: fix-now (`.E.0.10`) vs own ship.** Fix = consume `pre_resolved.slippage_pct` at the fill chokepoint (`OrderManager_HandleFill` / paper `Submit` branch; entry +slip / exit −slip; paper-gated) + delete the dead `OnEvent` slip block (one slip SSoT, closes the bound-but-unread mirror). | NEW · register-homed pending triage (TECH_DEBT-NNN at disposition) · **REFUTES this register's own line-195 "books at tick−slip" framing** |
| **A11** | **`.F.4d` bandit reward-attribution feature SILENTLY DEAD (producer-orphan, Sub-shape inverse of A9).** `MBS_OrderSetBanditContext` (`Order.hpp:294`) is DEFINED but CALLED NOWHERE — `Order_BindPreResolved` documents the bind (`:118-120`) but never invokes it → the `flags_packed` bandit bits (`bandit_active_state`/`bandit_regime`/`bandit_chosen_arm`, bits 17-25) effective-write = zero-init. The LIVE consumer `real_on_exit_calibration` (`OrderManager.hpp:697-721`) reads all three → every calib row logs regime/arm = 0 + emits `bandits[0]`'s probabilities regardless of the trade's real regime/arm. | **HIGH** (ML/calib fidelity) | setter `Order.hpp:294` (0 callers, grep-sealed); reader `OrderManager.hpp:697-721` | **VERIFIED (adversarial sweep). char-current (logs zeros) + FIX = call the setter at the bind seam** (carry the bandit context into `SubmitCommand`/`Order_BindPreResolved`, decision-time-binding-consistent). Pairs with A12 — same `.F.4d Step 7` cohort. | NEW · register-homed · own ML-fix ship (TECH_DEBT-NNN) |
| **A12** | **`bandit_reward_bps[]` NEVER WRITTEN (producer-orphan).** `OrderManagerState::bandit_reward_bps[]` (`OrderManager.hpp:425`) comment claims "Written at HandleFill SELL" (`:419`) but NO value-write exists — only the `FOREACH_OMS_PER_SLOT_FIELD` zero-init (`OmsFieldRegistry.hpp:342`). The LIVE reader `real_on_exit_calibration` (`OrderManager.hpp:703`) → every calib row logs `reward_bps_attributed = 0.0`. | **MED-HIGH** (ML/calib fidelity) | decl `:425`, read `:703`, init `OmsFieldRegistry.hpp:342`; 0 value-writes (grep-sealed) | **VERIFIED. char-current (logs 0.0) + FIX = implement the documented HandleFill-SELL writer.** A11+A12 = the `.F.4d` bandit reward-attribution consumer shipped, BOTH producers orphaned → the offline reward dataset is all-zeros (bandit training/eval reads garbage). | NEW · register-homed · own ML-fix ship (same cohort as A11) |
| **A13** | **No-trade-band fee-breakeven gate absent on the LIVE sharded path (dead-path consumer, Class 40+2).** `MASK_GATE_CFG_NO_TRADE_BAND_ENABLED` (suppress entries with signal < `fee_rate×no_trade_band_mult`, default mult 3.0) is read ONLY in legacy `PortfolioController.hpp:852` + display-only `EngineTUI.hpp:1641`. No sharded `Strategy_BuildParameters`/`BG_Evaluate` reader → operators enabling it get NO effect, sub-breakeven entries fire, AND the GUI shows a "no-trade-band blocked" indicator that never corresponds to a real gate (Class-2 display↔execution lie). | **MED** (default OFF) | legacy `PortfolioController.hpp:852`; GUI `EngineTUI.hpp:1641`; no sharded reader (grep-sealed) | **char-current (inert) + DECISION: wire into `Strategy_BuildParameters` (zero `bg_price_threshold`+SHALT when signal<band) OR tombstone the flag+GUI field (H21).** | NEW · register-homed · own ship |
| **A14** | **Legacy vol-sizing flag inert on the sharded path (config foot-gun, Class 21/26).** `MASK_RISK_CFG_VOL_SIZING_ENABLED` ("legacy ATR" vol-sizing) read ONLY in legacy `PortfolioController.hpp:1197`. The sharded path HAS vol-scaling but under a DIFFERENT flag (`MASK_ML_CFG_FOXML_VOL_SCALING_ENABLED`, `StrategyParameters.hpp:1878`) → operators flipping the legacy flag get no effect. Two near-synonym flags, one inert. | **LOW-MED** (default OFF; functional equiv exists under a different name) | legacy `PortfolioController.hpp:1197`; sharded `StrategyParameters.hpp:1878` | **DECISION: tombstone/alias the legacy flag to the foxml one OR document the supersession. Cohort-audit candidate (2 sibling vol-scaling flags).** | NEW · register-homed · cohort cleanup |
| **A15** | **`cores=nullptr` zero-stub landmine in the live `EventLoop_OnEvent` caller (Class 26 — latent).** The live drainer (`Async.hpp:840`) + `DrainEvents` (`:2042`) call `EventLoop_OnEvent` with NO `cores` arg → `effective_cores[...]` falls back to `NULL_PER_CORE_CFG_STUB_ARRAY` (all-zeros, `ControllerEventLoop.hpp:1858`). MOOT today (mode-1 returns at `:1908` before the body), but if A9 is "fixed" by reviving the `OnEvent` body — or `OnEvent` is ever reached in mode-0 from the live caller — every per-core fee/slippage reads ZERO. | **LOW now / HIGH-if-reactivated** | `Async.hpp:840` (no cores); stub `ControllerEventLoop.hpp:1858`; mode-1 return `:1908` | **DO NOT patch in isolation — fix A9 via `OrderManager_Submit`'s pre_resolved Order (NOT by reviving the null-stub `OnEvent` body); if `OnEvent` is ever made live, the caller MUST pass `&cfg.cores[0]`.** | NEW · register-homed · fold into the A9 fix |
| **A10** | **Dual-arm TP collapsed — flat `tp_pct` overwrites the strategy's stddev-scaled TP.** `ExecutionCore.hpp:542-545`: a non-zero `tp_pct` → `live_tp = fill×(1+tp_pct)` (flat), DISCARDING `sg_take_profit_price` (Momentum's `entry + σ×tp_mult`). REACHABLE on DEFAULT cfg (Momentum `tp_pct`=`take_profit_pct`=3% non-zero AND `momentum_tp_mult`=3.0 → stddev TP ≠ flat). The param pack carries BOTH arms; the hot path's strict IF/ELSE collapses to flat-only. Corroborated by `ShardedSnapshotPersist.hpp:635-642` (a prior postmortem chose flat-canonical) + a 2026-04-30 user incident. | **MED** | `ExecutionCore.hpp:542-545`; inflow `StrategyParameters.hpp:321-322,539-560`; defaults `CfgFieldRegistry.hpp:469,526` | **DESIGN-DECISION (operator-owned) — NOT a determinism bug.** char-current AS-IS (freeze the flat overwrite + flag) — do NOT freeze-corrected (a baked-in dual-arm = a reimplemented oracle). Resolution options (operator picks): (a) **TRUE second leg** (flat→leg A, stddev→leg B partial-exit pair = the original dual-arm intent — "take some at flat, let the rest run to σ"); (b) `Money_Min(flat,stddev)` (caps upside but defeats let-it-run); (c) per-strategy precedence flip (Momentum `tp_pct`=0 → use stddev). Intersects A6 (#3 chokepoint guard) + H22 (#5). | NEW · register-homed pending triage · own design ship |

### 🎯 A9/A10 — operator design intent + verification (2026-06-12)
**A10 dual-arm (operator intent):** the IDEAL was an *adaptive* TP — at the first calculated TP, read momentum direction {rising / stable / falling}: take at the spike if it's a local peak (then drops), HOLD to maximize if it keeps rising, hold-if-stable (unless projected to fall). The **dual-leg (2 positions/buy)** was the simpler settle she landed on — it improved WR → KEEP it. Code reconciliation (verified 2026-06-12): (1) flat TP fires = the "take at first peak" arm ✓; (2) trailing-ratchet PLUMBING exists (`ExecutionCore.hpp:427` `Money_Max(tp, ratchet_tp)`, default-off, driven by `Regime_AdjustPositionsSharded` + comment-flagged "future strategy trailing") = the *infrastructure* for "hold if rising" but NOT confirmed wired for Momentum (needs an inflow trace); (3) the Momentum **stddev** target = the discarded volatility-scaled "let-run ceiling" (A10); (4) the dual-leg = 2 FLAT legs (`tp_pct` + `tp2_mult` scale-out), not one-flat-one-stddev. **Resolution: KEEP the dual-leg; A10 = decide the stddev arm's fate (remove-vestigial OR wire-as-let-run-ceiling); the full adaptive-peak = a future strategy-enhancement ship.** F-059 characterizes current (flat + ratchet-plumbing + dual-leg) as-is + flags A10.
**A9 slippage — CONFIRMED REGRESSION (operator: "it used to work properly"):** corroborates the git root cause (`0119551` wired fee, orphaned slippage in the same pre_resolved migration). Not a design choice → a fix. Durable-now candidate.
**A10 REFINED (adversarial sibling sweep, 2026-06-12 — Agent "computed-but-discarded"):** the discard MECHANISM is SYSTEMIC across all 4 pct-strategies (entry-fill `tp_pct`/`sl_pct` override discards `sg_*_price` when pct≠0 = the default state, `take_profit_pct=3.00`/`stop_loss_pct=1.50`), governed by the **`sg_*` (absolute) vs `tp_pct`/`sl_pct` (fractional) DUAL-REPRESENTATION** with documented precedence (`GateParameters.hpp:91-105`: zero-pct → use `sg_*`). But it's **BENIGN for SimpleDip/MR/EmaCross** (S3 — same-pct value at a stale entry price; the per-fill recompute off the real fill is the intended phase-14 loss-bias fix) and only nullifies a **DISTINCT signal for Momentum** (S1 — the stddev geometry). KEY NUANCE: the Momentum stddev **TRAIL survives via the RATCHET channel** (`Momentum_ExitAdjustSharded`→`Strategy_WriteRatchetSL`→`ratchet_sl`, consumed `ExecutionCore.hpp:446-447`) — so the "let-it-run" arm is ALIVE; only the stddev **entry-TP** is discarded → this is why A10 is MED, not HIGH. **NEW corollary S2 (LOW):** the MOM `momentum_min_tp_margin_pct` quality filter (`StrategyParameters.hpp:1699-1701`) inspects the flat `tp_pct`, not the stddev TP (inert by default, margin=0). **Fix (singular, closes S1+S2):** convert Momentum's stddev geometry to a per-fill fraction at build time (`tp_pct = stddev×mult / entry`) so the ONE per-fill path is honest for every strategy → retires `sg_*` as the dual-representation SSoT-violation it's become. (S5 leg-B `tp_pct_b=tp_pct×tp2_mult` + S6 regime/ratchet = CLEAN; the stddev reaches positions via the ratchet, not the dead `sg_*` entry value.)

### 🔬 Deep adversarial audit (2026-06-12) — NEW findings A16+ ("really get a deep pass over core trading code"; 6-agent sweep, hunting NEW beyond A1–A15)

| id | finding | sev | site | disposition |
|---|---|---|---|---|
| **A16** | **Mode-1 books a PARTIAL fill as COMPLETE + frees the order slot in ONE step → remaining qty DROPPED.** `OrderManager_ProcessFillCommand` gates `HandleFill` + the unconditional slot-free on `event_log_mode==1`, NOT on `ORDER_FILLED` — a real partial fill (`order_complete==0`→`ORDER_PARTIAL`) books at the partial qty then frees the slot; the next fill for the remaining qty finds the bitmap clear→`slot=-1`→dropped. **DISTINCT from A2** (A2 = parser doesn't accumulate `"z"`; A16 = the OMS book-on-PARTIAL + unconditional-free, survives the parser fix). | **HIGH** (LIVE only — real venue partials; paper/backtest emit 1 synthetic fill) | `OrderManager.hpp:1407-1418` + `:1440` | char-current + FIX = gate `HandleFill`+slot-free on `Order_GetState==ORDER_FILLED`; `ORDER_PARTIAL` → keep slot open + accumulate. Partial-fill cluster (A2/A4) but the OMS-side gate is **durable-now**. NEW. |
| **A17** | Paper mode-1 synthetic fill never sets `order_complete=1` (`:993-998`) → every paper order is `ORDER_PARTIAL`; `total_filled` skipped yet booking runs + slot freed; mode-0 paper DOES bump `total_filled` → the two paper modes disagree. `total_filled` = display/diag only (no capital path). | LOW (observability; the pre-live paper path mislabels) | `OrderManager.hpp:993-998` | FIX = set `order_complete=1` in the paper synthetic fill (paper fills are atomic). |
| **A18** | `handle_buy_fill` has NO already-active-slot guard — `handle_sell_fill` defends the SELL-on-closed race (`:1185`) but BUY does not → a cross-cycle async re-entry can `Portfolio_OpenSlot`-overwrite a still-open position, leaking `entry_fee` + `core_open_notional` + desyncing the mask accounting. | LOW-latent (LIVE only — async exit/new-entry overlap; paper synchronous → can't occur) | `OrderManager.hpp:1159-1176` vs `:1185` | FIX = add the symmetric active-slot guard in `handle_buy_fill` (mirror `:1185`). **→ ATTEMPTED + REVERTED `.E.0.10` (D-209):** the unconditional guard flips the boot-reconcile collapse last-write→first-write (broke `tsa-live-2`; collides w/ TECH_DEBT-072). Re-homed → TECH_DEBT-188 + `data-disciplines/per-node-position-ownership-model.md` (scope to the LIVE async path, or subsumed by venue-net reconcile @`.E.1`). |
| **A19** | **`ratchet_tp` NEVER cleared between trades (H22 violation) — a stale TP-ratchet from a prior trade silently raises/disables the next trade's TP.** `effective_tp = Money_Max(tp, ratchet_tp)` (`ExecutionCore.hpp:427/447/458`); `ratchet_tp` written max-only (`StrategyLifecycle.hpp:311`, AUTO regime-transition) + zeroed only at Init. Its sibling `ratchet_sl` IS cleared every inactive-slot slow-cycle (`ControllerEventLoop.hpp:2725-2727`); `ratchet_tp` is conspicuously ABSENT from that block (the `:2717-2720` comment states the clear-intent but only does SL). On a `STRATEGY_AUTO` core, a regime transition in-position sets `ratchet_tp`, it persists across later trades → their TP fires too late or never. A trade's exit becomes a function of a PREVIOUS trade's history (H22 break). Bounded to one run (not persisted). Secondary: GUI `is_trailing` lies (Class-2, agrees-with-broken). | **MED** | clear-omission `ControllerEventLoop.hpp:2725-2727`; consumed `ExecutionCore.hpp:427`; writer `StrategyLifecycle.hpp:304-316` | **DURABLE CLOSE-NOW (one-liner): add `pending_params.ratchet_tp = Money_Zero()` to the inactive-slot clear block, restoring SL/TP symmetry.** `.E.1` only renames it. NEW. |
| **A20** | **Reconcile watermark `last_seen_trade_id` is `SKIP_PERSIST` → restart re-applies historical fills (capital corruption).** It's the ONLY guard against `Reconcile_ApplyMissedFills` replaying an already-applied trade (`Reconcile.hpp:218,222`), sole writer `:277`, registered `SKIP_PERSIST` (`OmsFieldRegistry.hpp:300`) + seeded 0 every boot + NO WS-path writer (the "bump on every WS fill" design is unimplemented). On a LIVE `AUTO_SYNC` boot, `GetMyTrades(fromId=0)` returns the last 100 SETTLED trades (`Run.hpp:1014`→`BinanceOrderAPI.hpp:808`) → replays ALL → double-books P&L/fees + phantom positions (replayed BUYs always open a slot). Live does NOT load the snapshot (`Run.hpp:982`) → reconcile IS the live recovery, so the watermark reset is load-bearing. | **HIGH** (LIVE, non-default — needs `reconcile_mode=auto_sync`, which survives the LIVE WARN→STRICT flip; AUTO_SYNC is the trusted crash-recovery path) | `OmsFieldRegistry.hpp:300` (SKIP_PERSIST); guard `Reconcile.hpp:218,222`; `Run.hpp:1014,1101` | char + FIX = seed the watermark to the latest `trade_id` BEFORE the first replay (or persist it to a live recovery sidecar) + bound the `GetMyTrades` window by `startTime`. Distinct from `tsa-live-2`. **Gates the live-enable decision.** NEW. |
| **A21** | **Live reconciler `expected_usdt` ignores open-position notional → clobbers `oms->balance` → FALSE kill-switch trip + permanent P&L double-count (capital-safety-critical).** `oms->balance` is a SYNTHETIC P&L tracker (`starting + Σrealized`), not a cash wallet — a BUY opens the slot but never debits entry notional. `ReconciliationLoop_Pass` (`:129-130`) computes `expected = oms_balance − inflight_buy_notional`, but a FILLED-open position's notional is in NEITHER term while the real venue USDT IS reduced by it → `drift ≈ −notional(open)` ≫ 1¢ tol → `OrderManager_ProcessReconcile:1456` overwrites `oms->balance = venue_usdt`. → (1) the global kill-switch reads this field → FALSE min-balance / drawdown trip mid-position; (2) `handle_sell_fill` then `+= net` on top → permanent P&L corruption; (3) `ks_peak` re-baseline skew. | **HIGH/CRIT-on-live** (unconditional once live — reconciler starts whenever `live_trading`, 30s cadence, no enable flag; fires on the NORMAL state of holding a position >30s) | formula `ReconciliationLoop.hpp:129-130`; clobber `OrderManager.hpp:1456`; KS reads `ControllerEventLoop.hpp:3252-3266` | char + FIX = `expected_usdt −= Σ(open-filled-position entry×qty)` (walk `active_bitmap`); do NOT overwrite `oms->balance` while `active_bitmap != 0`. **DISTINCT from the torn-read class (wrong even with an atomic read); the `.E.1` seqlock does NOT fix it. Hard live-enable gate.** NEW. |

| **A22** | **2nd torn-read SURFACE — `CoreContext` slow↔drainer mirrors (the known 9-site class is OMS-money-cluster ONLY).** `intended_qty` (16B Money = the ORDER QUANTITY) written by SLOW (`ControllerEventLoop.hpp:3091`), read by DRAINER bare (`Async.hpp:827`) → torn `__int128` → wrong qty to the venue. `intended_tp`/`intended_sl` same crossing (`Async.hpp:853/:885`). The `.E.1` OMS-aggregator fix does NOT cover these (different struct/thread-pair/fields). Siblings (LOW): torn `last_entry_price` (spacing), unsync `pnl_feeder` (adaptive), lost-update `last_entry_tick`. | **HIGH** (qty) / MED (tp/sl) — live sharded, slow+drainer free-running on separate pinned CPUs | write `ControllerEventLoop.hpp:3091`; read `Async.hpp:827/853/885` | FIX = publish the intent tuple via a seqlock (fold `intended_*` into the `pending_params`/`GateParameters` seqlock pack). ENROLL `CoreContext::{intended_*,allocated_balance,last_entry_price,pnl_feeder}` **+ `Position::{original_tp,original_sl}` (NEW slow↔drainer site post-A25 #H4 deep-check 2026-06-12 — writer = drainer `handle_buy_fill`, readers = 4 `*_ExitAdjustSharded` on slow, bare 16B Money; A25 widened this)** as torn-read sites + the CI check (`sizeof>8 && !atomic && cross-thread-written && read outside a seqlock`). Expands #10. NEW. |
| **A23** | Torn `allocated_balance` (16B Money sizing input) — written by PRODUCER on cfg hot-reload (`Async.hpp:359`), read by SLOW for sizing (`ControllerEventLoop.hpp:2701`), bare → a hot-reload during a sizing read tears the per-core capital → wrong size. | MED (rare — GUI hot-reload only) | write `Async.hpp:359`; read `ControllerEventLoop.hpp:2701` | FIX = seqlock OR move the write to the reader's thread (producer sets a realloc-pending atomic; slow applies its own write). Same CoreContext surface as A22. NEW. |
| **A24** | **Session-of-day + D10-adaptive + spike-spacing cfg mutations are DEAD on the production path (Class 44 + H22; the adaptive SAFETY loop is INERT).** `RebuildOneCore` mutates `volume_multiplier`/`entry_offset_pct`/`spacing_multiplier` on the FLAT `resolved_cfg` every rebuild (`ControllerEventLoop.hpp:2360/2426/2978`), but every production consumer reads the BOOT-FROZEN per-core shadow `cores[slot]` (`:2700`/`:2998`) or strategy-owned `live_*` (seeded once at boot) → none reach a live consumer. Session vol-mult (non-1.0 18/24h), the D10 P&L-regression filter-tighten safety loop (default-ON), spike relaxation — all silently inert. **Regression** from the per-core migration (`49649b8` switched reads to `cores[slot]` without reconciling the flat mutations). | **HIGH** (wrong gate — SimpleDip vol-gate mis-set most of the day; the losing-streak safety loop dead) | mutate `ControllerEventLoop.hpp:2360/2426/2978`; read `:2700/:2998` + `StrategyParameters.hpp:365/381/245` | FIX (H22) = mutate `resolved_cfg.cores[slot]` directly (single-source the per-core view) OR sync the override-eligible mutated fields to `cores[slot]` before the consumer. The literal H22 CI-check candidate. NEW. **→ `/decision-check` 2026-06-12 (D-211): option (c)** — slice-as-sole-storage (a's mechanics) + repoint the `:2974` GUI display-read (live display↔exec inversion, A32) + extend Check-10 CI guard; flat-field DELETION homed (TECH_DEBT-191, gated on PortfolioController retirement); third-storage `live_*` gap (D6/D10 don't reach MR/Momentum, A33) homed (TECH_DEBT-189). **Class 44-B (recurrence 4→5).** Plumbing-now (`.E.0.10`); POLICY redesign → `.E.M` stub. Confirmed by 3 code-reads (resolver flat-only `ControllerConfig.hpp:1385-1417`; consumer reads slice `:2677`; bug real). **✅ LANDED `.E.0.10` (engine `f2ef5d6` slice-redirect + A32 display repoint; char-test `3388/0` ADV-REFUTE 7/7; `check_per_core_registry_integrity.py` Check-11 guard built+self-tested). 5-way verified: adversarial 7/7 SOUND · DOD/HFT clean · calls_graph_diff clean (hot path untouched) · asan/ubsan 3246/0. D10 re-activation paper-test = the one deferred behavior-validation (alpha measurement, not a code gate).** |
| **A25** | Trailing-SL ACTIVATION gates on the absolute `original_tp` (expected-entry-priced) while the exit fires from per-fill `live_tp` — all 4 sharded strategies (`Momentum.hpp:373`/`MeanReversion.hpp:565`/`MLStrategy.hpp:312`/`EmaCross.hpp:216`). Under slippage they diverge → the trail arms at the wrong price. Ratchet plumbing is fine; only the trigger threshold is stale-priced. Class 44 / absolute-vs-per-fill family (sister to A1/A10). | MED (mistimes the trail start; not an accounting error) | set-site `Portfolio.hpp:354`; read-sites above | FIX (**DECIDED** D-204/D-205) = the SET-SITE fix: bind per-fill `tp_pct` via `Order_BindPreResolved`+`ResolvePerFillTpPct` (Order 256→272, in-flight SPSC-only) → set `original_tp = fill×(1+tp_pct)` at `handle_buy_fill`/`Portfolio_OpenSlot` → fixes all 4 trail readers + the bandit. Premise CONFIRMED by code-read (the sharded `*_ExitAdjustSharded` DO read `original_tp`, dispatched `ControllerEventLoop.hpp:2716`; a narrow-bandit reframe was raised + RETRACTED). Carry LOCKED. **→ `/decision-check` 2026-06-12 (§ verdict below): land-NOW CONFIRMED, front-load refuted; #5 → option (b); co-land A18+A28.** |

**Deep-audit LOW/advisory (A26/A27 — captured at close-out completeness check; were in the synthesis, not the register):**
- **A26** (LOW-MED) — the GLOBAL account kill-switch is realized-P&L-only (reads `oms->balance`, which moves only on realized exits) → blind to deep *unrealized* drawdown on an open position. **Substantially DEFENDED:** the per-core MTM kill-switch (`ControllerEventLoop.hpp:2868`) DOES value unrealized + is default-ON (`enable_mtm_kill_switch=1`). Gap = only the account-AGGREGATE unrealized view + the operator-mental-model mismatch ("halt if wallet < $X" actually = "realized-adjusted-starting < $X"). Fix: document the realized-only semantics + optionally fold an aggregate-MTM term into the global trip. → `.E.1` / cleanup.
- **A27** (LOW) — `std::this_thread::sleep_for(100ms)` in the reconciler poll loop (`ReconciliationLoop.hpp:183`) vs H3 "NO `sleep_for` anywhere". It's a background REST-poller (not hot/slow path) — the established orchestration-layer exception (`Run.hpp` uses `std::thread`/`sleep_for` throughout boot). Fix: document the orchestration-layer H3 exception OR convert to a condition-free timed wait. → cleanup. (NEW-4/5/6 — `last_entry_price`/`pnl_feeder`/`last_entry_tick` — captured under A22/A23 as CoreContext siblings.)

**A28–A31 + A25-impl-notes — surfaced by the 2026-06-12 A25 deep-harvest (exhaustive 2-critic pass, code-cited; the "get EVERYTHING" close-check):**
- **A28** (latent → durable-now; sister A19) — `Portfolio_Init`/`Portfolio_ClearPositions`/`Portfolio_AddPositionWithExits` zero a SUBSET of `Position` fields → `original_tp`/`original_sl`/`pair_index`/`entry_timestamp_us` survive a slot reuse (stale trail-anchor + mis-paired partial legs; the **A19 bug class one field over** — A19 was `ratchet_tp`-never-cleared, this is `original_*`-never-cleared). Live entry (`Portfolio_OpenSlot:354`) is safe; reachable via Init/Clear-then-reuse + the legacy `AddPositionWithExits` (which relies on a follow-up patch). FIX = full-struct reset in `Portfolio_Init`/`ClearPositions` (`Portfolio.hpp:216-225`/`414-423`) + set `original_*` inside `Portfolio_AddPositionWithExits` (`:257-271`). → **`.E.0.10` durable-now** (fold into the A25 `Portfolio` touch). TECH_DEBT-182.
- **A29** (REAL → gates-live) — GUI-drag on the sharded path writes `pos->take_profit_price`/`stop_loss_price` (`Async.hpp:246-247`) but the hot exit gate fires on `core->live_tp`/`live_sl` (`ExecutionCore.hpp:337/427`) → **a dragged stop/TP has NO execution effect on the live sharded engine** (Class-2 display↔execution lie; the register's prior GUI-drag note was the torn-WRITE only, NOT "functionally inert"). Capital-relevant. FIX = the drag handler must also write `live_tp`/`live_sl` (+ decide whether a drag re-anchors `original_tp`/disarms the ratchet). → **`.E.1`** (gates live). TECH_DEBT-184.
- **A30** (display) — `is_trailing = (take_profit_price != original_tp)` (`EngineTUI.hpp:273`/`:1556`) is permanently FALSE on the sharded path (the sharded ratchet moves `ratchet_sl`/`live_tp`, never `pos->take_profit_price`) → the trail/HOLDING badge never lights even while trailing; + the two derivations use `FPN_Equal` vs `Money_Eq` on a `Money` (Class-41 encoding-blind). FIX = the explicit riding-flag (folds into the D-206 redesign cascade-risk #3) + SSoT the derivation on `Money_Eq`. → fold into D-206 / cleanup.
- **A31** (latent; H21 dead-code candidate) — the non-sharded `*_ExitAdjust` family (`Momentum.hpp:249`/`MeanReversion.hpp:413`/`MLStrategy.hpp:219`/`EmaCross.hpp:109`) + the `PortfolioController.hpp:1372-1396` `original_tp` back-door are LIVE only via the deprecated `single_core` `PortfolioController` (compiled+runnable from `main.cpp`, boot-warned). A25's set-site fix does NOT reach them (they read/write `Position` directly) → a latent single_core post-A25 inconsistency; MR's variant also uses `FPN_*` on `Money` (Class-41). → `/dead-code-trace` + TECH_DEBT-185 (retire if single_core is truly dead).
- **A25-impl notes (RESOLVE before A25 lands):** **(#5 REAL/parity)** the event-log REPLAY reconstructs `original_tp` from the expected-entry `e.tp` (`OrderEventLog.hpp:732` ← `:1337`) → post-A25 **live ≠ replay** (the **reconstruct-path Class**, meta-lesson #1 / TECH_DEBT-186) → DECIDE: store the fill-priced TP on `OrderEvent` (a persisted-struct field-add → H21 event-log VERSION bump + tombstone) OR document `original_tp` as non-byte-reproducible across replay + have F-059 freeze-flag it. **→ RESOLVED 2026-06-12 (`/decision-check`) = option (b)** (document + F-059 freeze-flag; NO `OrderEvent` format bump now — couples to the deferred version rework TD-126). Corrected reachability via the AR-11 code-read: a production caller DOES exist (`OMS_INIT_AUTOPOPULATE` L4 `OmsFieldRegistry.hpp:741-744` writes `_fold.portfolio` — reconstructed `original_tp` from `e.tp` — into the live OMS portfolio, `event_log_mode==1`-gated), but `original_tp` IS persisted in the binary snapshot (`ShardedSnapshot.hpp:248`, the PRIMARY recovery) + live uses Reconcile not replay (`Run.hpp:974`) → LOW-MED, confined to the secondary event-log-replay path. **(#8)** A25 flips Momentum's `original_tp` from stddev-geometry to flat-`tp_pct` (`ResolvePerFillTpPct` returns the flat pct for Momentum per A10) → moves Momentum's trail-ARMING threshold, distinct from the slippage correction (cross-ref A10; F-059 flag). **(#15)** A25 cleans the exit-bandit counterfactual reward (`ControllerEventLoop.hpp:1750`) → SHIFTS the learned exit policy over time (intended; but DORMANT until A11/A12 fix the orphaned bandit producers — register §A11/A12). **(#19)** A25 widens A18's unguarded-`Portfolio_OpenSlot`-overwrite blast radius (now clobbers `original_tp`) → **land A18 with/before A25**. **(#6/#7)** mode-0 `EventLoop_OnEvent` OpenSlot + the single_core `PortfolioController` back-door are A25 NON-targets (document the intentional semantics split). **(#18)** post-A25 `Position::{original_tp,original_sl}` is a NEW slow↔drainer torn-read site (writer = drainer `handle_buy_fill`; readers = the 4 `*_ExitAdjustSharded` on the slow thread, bare 16B) → **enroll in A22's** torn-read site-list + CI check.

### 🧭 `/decision-check` verdict (2026-06-12) — A25 land-NOW; front-load REFUTED; #5 → option (b)

Operator-run `/decision-check` (TD-178) on *"front-load 3 class-closing codifications BEFORE A25 vs land A25 now."* **Investigative + INDEPENDENT-adversarial halves both REFUTED the front-load**; the orchestrator AR-11 code-read corrected a shared agent blind spot. **🔴 RED on front-load → code A25 now, codify opportunistically after.**

- **Planning-treadmill self-catch:** "author 3 specs + a version rework first" was the exact `feedback_planning_treadmill_bias_to_execution` anti-pattern (codified THIS ship) — turning the locked/audited A25 (D-204/D-205 `decided`) into meta-work. The next move is execution.
- **#5 RESOLVED → option (b)** (see the A25-impl-notes #5 line above). Corrected reachability (the code-read caught the agents' shared "no caller" error): a caller DOES exist (`OMS_INIT_AUTOPOPULATE` L4) but the binary snapshot is primary (`ShardedSnapshot.hpp:248`) + live uses Reconcile (`Run.hpp:974`) → LOW-MED; the structural single-source (option a) couples to the deferred `OrderEvent`/version rework → correctly NOT now.
- **Front-load items → real homes:** **TD-186** = EXTEND `decision-time-data-binding-pattern.md` (Stage-3 sister already carries the "downstream reads canonical, never recompute" principle) + add an RBP Class, **at A25 fix-close** (A1+A25 then real shipped instances — can't codify-from-an-uncoded-instance). **H22 spec** = author at **A24 fix-close** (A24 = canonical instance); the CI-check rides `.E.1`. **TD-126** (version rework) → own `v5.16` micro-sprint (paper-test-entry trigger; HMAC-stamp-bound; `RELEASE_VERSION_STRING` already `0.3`). **TD-180** (sizeof⇒version-bump guard) → rides **D-206**'s `Position` 128→192B bump (its named consumer; A25 grows only in-flight `Order`).
- **Impl wrinkle (not a carry change):** `ResolvePerFillTpPct` is NOT in scope in `Order.hpp`/`OrderManager.hpp` (include-light) → resolve in the dispatcher where A1 already calls it + carry onto `SubmitCommand`→Order (NOT literally inside `Order_BindPreResolved`). `strategy_id` set @`OrderManager.hpp:969`.
- **Confirmed sequence:** A25 (+A18 +A28/TD-182; #5=option b) → A24 (+H22 spec) → A16 (+A17) → F-059 (freeze-flags #5 + A9/A10) → A6.

### 🧭 `/decision-check` verdict (2026-06-12, D-211) — A24 → option (c); + the D6/D10/spike redesign capture

Operator-run `/decision-check` (armed M8, routed cfg/registry per Stage 2.5) on A24's fix-approach → opened a full design session. **Full reasoning: D-211.** **Fleshed-out redesign directions: `subplans/2026-06-12-v5.15.5.F.4d.1.E.M-adaptive-entry-redesigns.md`.** Verdict 🟡 YELLOW on bare (a); (b) REFUTED; **complete fix = option (c)** (see the A24 row above for the (c) breakdown). Two gate agents + orchestrator AR-11 code-reads; the fan-out found 3 findings beyond the register.

**AR-11 resolved by code-read:** MR/Momentum read `state->live_vol_mult` (THIRD storage, `StrategyParameters.hpp:434/510`), NOT the slice → A24 reaches **SimpleDip/EmaCross/ML only** (adversarial agent correct; investigative wrong). D6 session-mult reaches MR/Momentum via neither path.

| id | finding | sev | disposition |
|---|---|---|---|
| **A32** | display↔exec inversion — GUI spacing diag (`ControllerEventLoop.hpp:2974`) reads FLAT `spacing_cfg.spacing_multiplier` (sees the spike relaxation) while the gate `Strategy_SpacingOk` (`:2980`) reads the slice (doesn't) → the operator's diagnostic advertises a spacing floor the engine isn't enforcing | HIGH | **FOLDED into A24** (same field-split; fixed in (c) step 2 — repoint `:2974` to the slice) |
| **A33** | third-storage `state->live_*` — D6 session-mult + RebuildOneCore-D10 don't reach MR/Momentum (they read `live_*`, adapted by their OWN loop `MeanReversion.hpp:175-180`/`Momentum.hpp:121-125`, persisted `Portfolio.hpp:575`); two competing adaptation ARCHITECTURES (cfg-mutation vs strategy-state `live_*`) | MED | **TECH_DEBT-189** → `.E.M` convergence (probably onto `live_*`); A24 must NOT false-close as "live for all cores" |
| **A34** | cluster-aggregate exposure ungoverned — N nodes long the same symbol = N× concentration; per-node purity makes each node correct, the cluster aggregate is owned by NOTHING (one-position-per-NODE, `per-node-position-ownership-model.md`) | MED | **TECH_DEBT-190** → `.E.1`+ (the aggregator already publishing the account snapshot is the home) |

**Redesign routing (the POLICY pass, distinct from A24's plumbing):** **D6** baseline-free rolling volume normalization (KEEP; relative-ratio or z-score, no hardcoded baseline; → `RegimeSignals`/`FlowFeatures`) — NOT bandit-gated, earliest → `.E.M` · **D10** keep + paper-test the re-activation now (rides A24), learned ver → `.E.M` (A11/A12-gated) · **spike** bandit-learned per-strategy spacing → `.E.M` (A11/A12-gated; co-design w/ D-206) · cluster-aggregate → `.E.1`+. All fleshed out in the `.E.M` stub. **Bandit pieces HARD-GATED on A11/A12** (the reward producers are orphaned → the bandit reads all-zeros → can't learn until fixed).

### 🔭 Round-2 Class-44 orphan sweep (2026-06-13, D-211 sequence) — 3 armed lanes; class BOUNDED + 3 NEW flag-orphans

Operator-directed Round-2: 3 independent M8-armed adversarial agents swept the whole Class-44 surface (cfg/flag orphans · producer-orphans · in-flight-bind orphans), each blind to the others. **Result: the producer-orphan + in-flight-bind lanes are CLEAN beyond the known cohort — the class is BOUNDED** (A9 fix confirmed-complete, A25 confirmed-consumed, A10/A11/A12 confirmed + already homed, ZERO new producer-orphans, all `pre_resolved.*`/`Order`/`SubmitCommand`/`GateParameters`/`original_*` proven-consumed). The cfg-flag lane surfaced 3 NEW orphans (each VERIFIED by orchestrator code-read per AR-11 / ground-before-record):

| id | finding | sev | disposition |
|---|---|---|---|
| **A35** | `MASK_GATE_CFG_GATE_EMA_ENABLED` — operator-settable + GUI-rendered (`SettingsPanel.hpp:1088`) but read ONLY at dead `PortfolioController.hpp:779`; the sharded path sets EMA alpha unconditionally (`Run.hpp:878-881`) and EmaCross reads `state->prev_ema` gated only on `!FPN_IsZero(prev_ema)` (`StrategyParameters.hpp:653`), NEVER the flag → the flag toggles nothing on sharded (EMA-gating effectively always-on for EmaCross; "use EMA vs rolling-avg" un-selectable; other strategies never consult it). Class 44 Sub-A + a Class-2 display facet. | MED (default OFF) | NEW → register-homed; **cohort with A13/A14** (operator-settable flag, no sharded reader); decision = wire into `Strategy_BuildParameters` OR tombstone the flag + `gate_ema_alpha` GUI field (H21) |
| **A36** | `MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PARTIAL` — **DEFAULT ON** (`ControllerConfig.hpp:1622` `=1`), "move SL to entry after TP1 hit (partial-exit ratchet)" — read ONLY at dead `PortfolioController.hpp:682`; NO sharded reader (sibling `BREAKEVEN_ON_PROFIT` IS sharded-wired) → on the sharded path the default-ON breakeven-after-partial RISK feature is ABSENT (a partial-exit position never ratchets SL to breakeven → can give back the TP1 gain). | **MED-HIGH** (default ON; risk; partial-exit-gated) | NEW → register-homed; **intersects A16** (partial-fill state-gate) + F-059 (exit golden-master); decision = wire into the sharded partial-exit exit-chain OR tombstone |
| **A37** | `MASK_OPS_CFG_SESSION_FILTER_ENABLED` — the session vol-mult is applied UNCONDITIONALLY (`ControllerEventLoop.hpp:2313+`, branchless lookup, NO flag gate) → the enable-flag gates NOTHING. Compounds A24 (the session mechanism ALSO writes the flat field no consumer reads). Session phase defaults are non-1.0 (Asian 1.5 / US 0.8 / Overnight 1.3) → NOT inert-by-default; killed by the A24 storage gap. | LOW-MED | **FOLDED into A24** (the session facet); the flag itself is a sub-orphan — tombstone-or-wire with A24 / the D6 redesign |

**Dead-code sightings (Class 40 / H21, NOT Class 44 — homed TECH_DEBT-192):** `EngineSharded_ForceCloseOnShutdown` (`ShardedLiveSafety.hpp:214`) calls the STALE 9-positional `OrderManager_Submit` (only the `SubmitCommand` form exists now, `:905`) + pre-Ship-A `FPN_Binary<F>` money types → compiles ONLY because it is an uninstantiated template; the call site was REMOVED (`Run.hpp:2259-2276`, to fix a shutdown-hang) → **force-close-on-shutdown is currently DISABLED** (a `.E.1` live-enable question — does live want force-close on shutdown?) + a latent compile-break if ever re-wired. Plus 4 Class-40 dead setters: `Order_SetRetryCount` (dead pair — reader also dead), `EventLoopState_SetIntendedParams` (redundant; self-flagged "phase 05 replaces it"), `ConfidenceScorer_MarkPredict` (dead wrapper), `last_predicted_exit_thompson_arm` (write+read absent scaffolding). `MASK_BANDIT_SHADOW_LEARNING` (a slow-path gate bit computed every rebuild, ZERO readers) is a LOW Class-44-Sub-A within the gate registry — tombstone or wire.

**Meta (reinforces the Class-44 detector — the foundational guard):** NONE of the existing tools (`scan_class_27_full.py`, `check_per_core_registry_integrity.py`) catch this class — A35/A36/A13/A14 + A11/A12 would ALL have been auto-flagged by the "write-with-no-live-read / read-with-no-live-write" detector that Class 44's `closure_mechanism` proposes. This sweep IS the evidence for building it (the H22 Check-10 extension is the cfg-flag slice; the codebase-wide field-level detector is the full version). Tooling gap noted: `check_oms_per_slot_registry_integrity.py` (named in the agent brief) does NOT exist — only `check_per_core_registry_integrity.py`.

### 🗺️ Routing map — deep-audit cohort → E homes (2026-06-12, D-201; "amend them to each plan they're associated with so E delivers polish")

| finding | E-home / fix-grouping | disposition |
|---|---|---|
| **A9** slippage dead | **`.E.0.10` durable-now fix-ship** | ✅ **LANDED** — slip consumed at the `OrderManager_Submit` synthetic-fill chokepoint (one production SSoT; entry +slip/exit −slip, paper-gated by construction, branchless H20); dead `EventLoop_OnEvent` slip block DELETED (closes A15); char-test migrated (60060/60939, 3-lens); suite 3371/0; 2-agent adversarial refute CLEAN (sign/live-gating/no-double-slip/backtest-coverage/OnEvent-deletion all verified). ✅ RESOLVED per **D-203**: conservative non-zero default `slippage_pct` (0.05%) folded into `ControllerConfig` (engine `2b38bd6`) → sim pessimistic by DEFAULT, not opt-in. Codified `adversarial-pessimistic-simulation-discipline.md` (A9 first canonical). |
| **A16** partial book-and-free (OMS gate) | **`.E.0.10` fix-ship** (gate) + D-123/`.E.1` (parser A2/A4) | durable-now gate |
| **A19** ratchet_tp clear | **`.E.0.10` fix-ship** | ✅ **LANDED** (engine `f90aa66` + char-test 4/4; suite 3372/0; `run_all_tests.sh --fast` green) |
| **A24** dead cfg mutations | **`.E.0.10` fix-ship** (H22 single-source) | ✅ **LANDED** (engine `f2ef5d6` + char-test `3388/0` + Check-11 guard; 5-way verified; D10 paper-test deferred) |
| **A25** trail stale-TP trigger | **`.E.0.10` fix-ship** (per-fill SSoT) | ✅ **LANDED** `0e1f6d2` (3378/0; carry resolved at the Async submit site → `Order::pre_resolved` → `handle_buy_fill`; A18 reverted→D-209/TD-188). ⚠️ **F1 follow-up (deep-check 2026-06-12):** the producer resolved tp_pct from the CONFIGURED `strategy_id` (AUTO→global) not `resolved_strategy_id` → fixed (`Async.hpp` resolved_strategy_id) + AUTO test (task #10) |
| **A20** reconcile watermark | **`.E.1` live-enable gate** | gates live |
| **A21** reconciler balance formula | **`.E.1` live-enable gate** | gates live (KS corruption) |
| **A22/A23** CoreContext torn-read | **`.E.1`** (aggregator/concurrency rework; expands torn-read #10) | gates live |
| **A15** cores=nullptr zero-stub | fold into the A9 fix | latent |
| **A11/A12** bandit · **A10** dual-arm | **`.E.M` ML ship** (TECH_DEBT-174) | subsumed / design-decision |
| **A2/A4** partial qty/commission · **A5** venue side | **D-123** (+ `.E.0.10` data-loss stop) | subsumed (decimal) |
| **A13** no-trade-band · **A14** legacy vol-flag | `.E.1` wire OR cleanup (tombstone) | decision |
| **A26** account-KS realized-only · **A27** reconciler `sleep_for` (H3) | `.E.1` / cleanup | TECH_DEBT-183 / -187 |
| **A28** Init/Clear reset-subset (A19 class) | **`.E.0.10`** durable-now | ✅ **LANDED** `0e1f6d2` (`Position_Reset` SSoT + char-test `3386/0`); TECH_DEBT-182 CLOSED. NB: `Portfolio_AddPosition` legacy still subset-zeroes (F3, test-only) |
| **A29** GUI-drag inert on sharded exit | **`.E.1`** (gates live) | TECH_DEBT-184 |
| **A30** `is_trailing` perma-false (display) | fold → D-206 / cleanup | — |
| **A31** single_core `*_ExitAdjust` dead-code | cleanup (H21) | TECH_DEBT-185 |
| **reconstruct-path** Class+spec (meta-lesson #1; A1+A25 canonical) | `.E` discipline / `/bug-check` | TECH_DEBT-186 |

**The `.E.0.10` durable-now fix-ship (the immediate CODING work):** A19 (ratchet_tp one-liner) → A9 (slippage consume + delete the dead `OnEvent` block) → A25 (trail per-fill SSoT) → A24 (cfg-mutation single-source) → A16 (partial-fill state-gate). Each = fix + characterization test (per `characterization-test-discipline.md`) + `calls_graph_diff` + independent `// ADV-REFUTE`. **A9 sequenced early** — it gates trusting paper-test results as live-readiness evidence. Live-enable set (A20/A21/A22/A23) → `.E.1`; ML set → `.E.M`; decimal set → D-123.

**TECH_DEBT routed to E:** 159 (FPN re-pack + regression-check) → `.E.1`; 173 (branch-scanner) → `.E.1`; 174 (bandit) → `.E.M`; 175 (struct-tracker) + 176 (unified test-runner) + **179/180/181** (cascade-tooling dogfood gaps — `--composition` transitive walk / `sizeof⇒version-bump` guard [M7] / `--help`+stale `Portfolio.hpp:97-116` comment) → `.E` discipline-ships. → E-done = polished.

### ⚖️ Disposition correction (2026-06-11 — operator-caught over-defer; rule re-applied)
Initial homes deferred ALL fixes to `.E.1`/D-123 on ADJACENCY ("the rework touches that surface"). Operator caught it (no-defer + `.E`-purpose = close debt / patch failures). Re-applied the subsumption-not-adjacency test (`feedback_opportunistic_tech_debt_closure`, amended BIDIRECTIONAL this session — folding a finding TRACKS it; it does NOT defer the fix):
- **CLOSE NOW in `.E.0.10` (durable — `.E.1` would only RENAME these, not redo):** A1 · A3 · A5 · A6 · A7 · A8. Each = fix + characterization test + binding 3-agent adversarial refute + `// ADV-REFUTE`. (`.E.0.10` accordingly expands from test/tooling-only → +capital-hardening.)
- **DEFER to D-123 (genuinely SUBSUMED — pre-existing decimal-OrderResult design):** A2/A4 decimal-exactness — but a durable data-loss STOP lands now (accumulate qty + parse `"z"`; pass reconcile commission) so nothing bleeds pre-D-123.
TECH_DEBT-168/170/171 → `closed.md` as each fix lands; 169 splits (stop-now / decimal-rides-D-123).

### Confirmed-existing (precisely located) + known-homed
- **F-096** — partial-leg qty split in `double` — PRECISELY located at `Async.hpp:827-837` (`Money→double→llround→Money`; `legA+legB ≠ intended` by ≤1e-8); diverges from the EXACT single_core `PortfolioController.hpp:1344`. Already TECH_DEBT-167 / `.E.1` Decision K — **this hunt validates the ledger entry's site + magnitude.**
- **D-123** — WS price/qty/commission via `double`; REST avg = `cum_quote/exec_qty` division ≠ WS `"L"` exact. KNOWN, `.E.1`/`.E.3`. A2/A4 are NEW members of this class.
- **F-044/045** — maker booked at taker rate (bucket self-consistent; `total_fees` wrong amount). KNOWN, LIMIT-gated, TECH_DEBT-154.

### Refuted clean (adversarial-default cutting BOTH ways)
- **`FPN_GreaterThan`-on-`Money`** (this register's OWN prior §MED-tier suspicion) — **REFUTED**: all live win/loss sites use the correct `Money_Gt`; the literal `FPN_GreaterThan(net,0)` does NOT exist in code — it survived only as a STALE COMMENT at `ControllerEventLoop.hpp:1977` (the register transcribed the rotted comment, not the line). **Comment fixed 2026-06-11.** Lesson: a register disposition sourced from a COMMENT, not the executable line, is an AR-3-shape (doc-accepted-as-verification).
- **`.v`-direct compares** in `PositionExitGate` (`Portfolio.hpp:483`) — **REFUTED**: bit-identical to `Money_Ge` (`FixedPointN.hpp:1857` = `a.v>=b.v`); deprecated single_core path.
- per-leg gross via `Money_FillGross` / entry-exit notional round-trip / `last_exit_*` per-slot arrays — **REFUTED clean** (no D-190 sibling; no leg-B clobber) → become positive-lock characterizations.

### Characterization gaps confirmed (the false-floor — Phase-2 targets, sharpened)
- **F-059** exit chain — **SHARPENED + CORRECTED by the 2026-06-12 adversarial panel** (the prior "books at `tick−slip`" framing was REFUTED — an AR-3 stale-assumption, same shape as the `FPN_GreaterThan` comment-transcribe above): **(A9 HIGH)** slippage is silently DEAD on the production path (`pre_resolved.slippage_pct` bound-but-unread → books the RAW tick price; freeze slip-LESS + regen-on-fix) + **(A10 MED)** the flat-`tp_pct` branch (`ExecutionCore.hpp:542-545`) overwrites/discards Momentum's stddev TP = a collapsed dual-arm (reachable on default cfg). F-059 freezes CURRENT behavior (slip-less + flat-overwrite) and FLAGS A9/A10 (do not freeze-corrected). → task #7.
- **F-018** partial-exit per-leg gross w/ REAL entry (the only test seeds `entry=0,qty=1` → degenerate identity, `:8526`). → task #8.
- **F-046** FlattenAll P&L (0 tests reference `EventLoop_FlattenAll`). → task #8.
- **MTM** unrealized VALUE never asserted (only the `KILL_TRIPPED` boolean, `:5629`). → fold into MED-tier.

### 🧭 Meta-lessons (harvest candidates — codify at /close-session per the population rule)
1. **NEW anti-pattern class candidate — "reconstruct-path-reads-a-DIFFERENT-source-field":** a restore/reconstruct/replay path that re-derives a value the FORWARD path computes, but reads a different source field, so the two silently diverge (A1: restore reads global `take_profit_pct`; entry reads the per-strategy override). Distinct from D-190 (same field, different FORMULA) and Class 18 (parallel emit/parse mirror). Sibling to AR-7 (structural-pattern false-completeness). **Structural fix = single-source the derivation BOTH paths call.** → candidate new Class in RECURRING_BUG_PATTERNS + a `forward-vs-reconstruct-single-source` DESIGN_SPEC.
2. **vacuous-test-masks-bug (recurrence):** the restore test HID A1 because its fixture used a cfg where global==override → the divergence was invisible (same shape as oms-ts-2's clean-input invariant). Concrete new rule for the **characterization-test-discipline** spec (TECH_DEBT-164 part 2): *a characterization fixture MUST vary the fields whose divergence the assertion could mask.*
3. **comment-as-disposition-source (AR-3):** the `FPN_GreaterThan` false-positive came from trusting a code COMMENT over the executable line. Reinforces verify-by-context-not-comment.
4. **Thesis validated:** money-surface completeness fan-out earns its tokens (3 NEW HIGH bugs). The plan's "exactly one dimension, 3–5 agents" bet was correct.

### 🌊 Cascade-trace findings (2026-06-13) — A16 fill-path "headwater" trace (3 armed agents + orchestrator code-read on the HIGH)

Fired after the A16 `/decision-check` (D-212) at operator direction ("this is what the stream flows from… many downstream consumers"; "live is gated behind finishing E in totality"). 3 M8-armed agents: downstream-consumer graph + debt-cohort enumeration + adversarial stress-test of the now-half + the `.E.1` design. Disposition column = **PROPOSED** (operator rules at **decision-log D-213**; nothing executed).

- **NEW-1 (HIGH, pre-existing) — live-safety gate field-decoupling (Knight-shaped).** `LiveReadiness_Verify` enforces its REFUSE suite ONLY under `live=(trading_mode==TRADING_MODE_LIVE)` (`LiveReadiness.hpp:230,241,256`), but real orders are authorized by `use_real_money` (`Run.hpp:577,493`; OMS `MASK_OMS_STATE_LIVE_TRADING` ← `live_trading=use_real_money`). `NormalizeForMode` only acts `if(trading_mode==LIVE)` (`ControllerConfig.hpp:2009`) — NO field couples them (**orchestrator-verified**). ⇒ a `use_real_money=1` + `trading_mode≠LIVE` config places real orders while the 8 pre-flight REFUSE checks (HMAC-verified stamps / feature+label hash drift / model age / secret-nonempty / mlockall) return PASS un-enforced. (`use_real_money=1` DOES still refuse on missing secrets.cfg `:581` + hardcoded-strategy-in-live `:493-507` — so not "unguarded," but the centralized SUITE is bypassed.) The safety control is keyed off a different flag than the one that arms capital (Knight-Capital class). A16's proposed LiveReadiness pin would INHERIT this. **PROPOSED: durable-now / pre-`.E.1` — boot consistency guard `use_real_money=1 ⇒ trading_mode==LIVE` (cleanest; retroactively arms ALL `trading_mode`-keyed gates + the strict-flips). Independent of the fill rework.** Owes a register-row + TECH_DEBT promotion at disposition.
- **NEW-2 (HIGH, live-only, was UNHOMED) — SELL-partial whole-slot close.** `handle_sell_fill:1234` → `Portfolio_CloseSlot(&oms->portfolio, pslot, fill_price)`; signature (`Portfolio.hpp:413`) takes NO `fill_qty` → a partial SELL closes the ENTIRE slot qty + books gross/P&L on the full position while the venue holds the remainder. Exit-side twin of A16; survives both the A2 parser fix AND the A16 book/free gate. **PROPOSED: `.E.1` Set-α** (subsumed by the per-node net-position model alongside A18 / TECH_DEBT-072/-188; the A16 gate-on-`ORDER_FILLED` covers it interim). Cross-link A16.
- **NEW-3 (MED-latent, live-only) — no reaper for a stuck `ORDER_PARTIAL` slot.** `Reconcile_AutoCancelStale` (`Reconcile.hpp:343`) cancels by venue open-order list, not by local PARTIAL-state age; no periodic in-flight timeout. A slot that goes PARTIAL and never completes (cancel / IOC-remainder / venue silence) leaks → `order_bitmap` (uint16, 16 slots) exhaustion → new orders dropped. Bounded by no-persist. **PROPOSED: `.E.1` submit-path/back-pressure/lifecycle (item-54 surface).**
- **RECLASS A20 + A21 — NOT `.E.1`-aggregator-subsumed (Set-β independent live-enable gates, durable-now candidates).** Subsumption test: the `.E.1` aggregator/per-node rework would only RENAME these, not fix them. **A21** (`ReconciliationLoop.hpp:129-130`) — `expected_usdt=balance−inflight_buy_notional` excludes FILLED-open-position notional from BOTH terms → `ProcessReconcile` clobbers `oms->balance` mid-position → false KS trip + permanent P&L double-count; **wrong even with an atomic read** (a FORMULA bug the seqlock/aggregator does NOT touch). **A20** (`last_seen_trade_id` SKIP_PERSIST, `OmsFieldRegistry.hpp:300`) — restart re-applies historical fills on AUTO_SYNC (double-book). **PROPOSED: pull OUT of "rides `.E.1`" → durable-now or a dedicated pre-`.E.1` reconciler ship** (A21 = subtract open notional + don't clobber while `active_bitmap!=0`; A20 = seed/persist watermark + bound the `GetMyTrades` window). Subsumption-vs-adjacency: NOT subsumed ⇒ don't defer-on-adjacency.
- **`.E.1`-PLAN-COMPLETENESS gaps (named-not-designed; capture so `.E.1` inherits design, not a forward-promise).** Adversarial TARGET-2 (grep-confirmed vs the `.E.1` foundation plan body): (a) the intra-session partial-accumulate `HandleFill` (weighted-avg re-entry + keep-slot-open + `order_complete` gate) is ASSERTED-folded with ZERO design content — DISTINCT from the ownership spec's BOOT-recovery venue-net reconcile (conflated in D-212); (b) **A22** (`CoreContext::intended_{qty,tp,sl}` + post-A25 `Position::{original_tp,original_sl}`) is a torn-read sibling on a DIFFERENT struct/thread-pair → a SEPARATE seqlock pack the single money-aggregator does not reach; (c) **A29** (GUI-drag→`live_*` write path); (d) **A34** (per-cluster concentration cap — named, no rule); (e) the `FillEvent` struct is USED in the aggregator illustration but never DEFINED; (f) **H21 seam** — if A16-accumulate / A2/A4 cumulative-qty must PERSIST per-fill data on `OrderEvent` (already `OMSEL02`) → OMSEL02→03 version-bump + tombstone, undecided. **PROPOSED: fold each as an explicit `.E.1`-foundation-plan design requirement.**
- **A17 RE-GRADE — verified safe + narrow (both agents).** Forcing `order_complete=1` on the paper synthetic fill (`OrderManager.hpp:996-1021`) + REST adapter perturbs NOTHING capital — only flips paper/backtest `total_filled` under-count→correct (display/diag). NO impact to OrderEventLog replay (`OEVT_FULL_FILL` unconditional; replay branches on order_type only), calibration, bandit, reconciler, or backtest parity. **Caveat: apply ONLY to a genuine REST FILL, not the ACK-only path (`fill_qty==0 → ORDER_ACKNOWLEDGED`, `:1413`).** Severity (LOW/observability) stays accurate for the COUNTER, but A17 is a HARD co-requisite of A16 (once the free gates on terminal, paper never frees without it).
- **CORRECTION — `Order.hpp:179-180` comment-rot is NOT Class-38.** Class 38 = the determinism/locale finding (2026-05-31). The "running total / weighted across partials" comments are a documentation-lie (Class-44 theme) — partials are never accumulated (`filled_qty` overwritten `:1419`). Fix at A2/A16 landing, or correct-to-actual now.

### 🧪 Keystone validation outcome (2026-06-13, 4-agent pass → decision-log D-214)

The proposed single keystone ("apply SSoT+H22+publish to the accounting spine") is **OVER-LUMPED — REJECTED as the `.E.1` organizing root** (corroborated: adversarial + cluster/HFT agents + the codebase's own over-lump caution in `cross-thread-multiword-read-consistency-discipline.md`). KERNEL holds (coherence+purity = publish+H22, the hot-path discipline; COMPOSES at Cluster/Deployment per agent C). **CORRECTED → THREE orthogonal spines, triaged per-finding by subsumption-not-adjacency:** (1) **fill-completeness / protocol-temporal** (A16, sell-partial, A2, A4, A17, A18, NEW-3 — the fill path's real root; "treat-a-partial-as-complete", wrong even with one owner; **UNDER-codified → needs a spec**) · (2) **cross-thread coherence** (torn-read class, A22; specced) · (3) **per-node/per-cluster purity H22** (A1/A24/A34; specced). **Independent (no spine):** A20 (persist) / A21 (formula) / NEW-1 (mode-flag) → durable-now. Unification lives at the META level (a meta-anti-pattern reasoning shape + "the accounting subsystem never got the hot-path discipline"), NOT one mechanism.
- **NEW sub-findings:** **F2 (verified — the `.E.1` plan body types money as `FPN<F>` at `:385-389`/`:637-650`/`:662-663`/`:681-682`; stale pre-Ship-B; H4 if coded — must be `Money`; the `:1910` addendum flags FPN SIZE not the FPN→`Money` TYPE)** · **F1 (24B/split-slot-atomic premise — already self-flagged STALE at `:1910`; 16B `__int128` → native atomic)** · **A34 needs a per-cluster-PURE concentration governor** (the one genuine cluster-composition hole; predicted by the purity spine) · **latency-codification gap** (effective-latency = whole tick→decision pipeline + the 3 contracts: critical-path / reaction / variance).
- **Cluster composition: COMPOSES-WITH-GAPS** (read-path = derivation hierarchy; write-path = kill-flag-mirror inversion; A34 = the hole). **D-212 latency claims VERIFIED** (hot path `grep -c`=0). **Branchless ACHIEVABLE** (`g_fill_dispatch` Class-28 canonical).
- **Codification = three-spines (NOT one keystone) — PENDING operator bless** (full plan in D-214 STATUS).
