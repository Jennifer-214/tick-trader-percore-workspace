# Post-ship audit: v5.15.5.F.4d.1.B.4 — 2026-05-27

**Ship tag:** v5.15.5.F.4d.1.B.4  
**Ship close date:** 2026-05-27  
**Plan body:** `subplans/2026-05-24-v5.15.5.F.4d.1.B.4-train-serve-execution-layer-parity.md` v1.7.6  
**Postmortem:** `/plans/v5.15-live-readiness/postmortems/2026-05-27-v5.15.5.F.4d.1.B.4-postmortem.md`  
**Commit range:** v5.15.5.F.4d.1.B.3..v5.15.5.F.4d.1.B.4 (12 commits; WIPs 7-16 + Phase D)

---

## Plan vs shipped delta

**Planned surface (via plan body v1.7.6):**
- Phase B: EngineCommon.hpp (5 shared helpers + 2 sister wrappers)
- Phase C.1-C.3: EngineSharded.hpp + ShardedBacktestDriver.hpp (boot + slow-path migration)
- Phase C.4-C.6: Backtest slow-path + parity_harness
- Phase D: cfg field recategorization (9 GLOBAL reclassification + 2 H14 bitmap migrations)

**Actual shipped surface (via git log WIP-7 through WIP-16):**
- WIP-7 through WIP-11: EngineCommon + LIVE boot/slow-path extraction ✓
- WIP-12: framework-layer codification (B-Plus v0.4 + B14/B15 pillars) ✓
- WIP-13: Backtest slow-path migration ✓
- WIP-14a-14b: centralized-arch SHARDED deletion (51-site cohort) + operator doc ✓
- WIP-15: PARITY-031 closure + parity_harness extension ✓
- WIP-16: cfg field categorization (Phase Cx atomic) ✓

**Planned-but-not-touched:** None (Phase B through D all touched)  
**Touched-but-not-planned:** None initially; Phase Cx (cfg-field recategorization) was scope expansion within plan body v1.7.6 amendment

---

## Plan claim verification

### ✅ VERIFIED (8 claims)

1. **"Train-serve execution-layer parity structural extract"** — 5 EngineCommon helpers successfully extracted to `CoreFrameworks/EngineCommon.hpp`:
   - `EngineCommon_ApplyBnbDiscount` (:154)
   - `EngineCommon_BootGlobal` (:183)
   - `EngineCommon_BootPerCore` (:235)
   - `EngineCommon_SlowPathCycleOneCore` (:476)
   - `EngineCommon_SlowPathCycleAllCores` (:806)
   - Both LIVE (EngineSharded.hpp:696/749/949/2708) and BACKTEST (BacktestSharded.hpp:203/217/285; ShardedBacktestDriver.hpp:362) call these helpers. ✓

2. **"B-full SHARDED centralized-arch full surface deletion (51 sites)"** — grep confirms:
   - `engine_arch=centralized` returns 0 production branches (only REFUSE error strings)
   - `ENGINE_ARCH_CENTRALIZED` constants GONE
   - Cohort deletion via 12-step leaves-first ordering (WIP-14b: operator doc + tests + comments + GUI gating + cfg-field-row LAST) ✓

3. **"Closes 7 PARITY entries (026/027/028/029/030/031/032)"** — Plan claims closure. **FINDING:** PARITY ledger in engine repo (`/FoxML_Trader_v2/DOCS/PARITY_ISSUES.md`) shows **PARITY-026/027/028/029/030/031 still OPEN**; only **PARITY-032 CLOSED**. Postmortem claims "7 closures" but ledger shows 1 closed, 6 still open. **GAP SEVERITY: CRITICAL** — postmortem is inaccurate on this claim. Workspace ledger also unchanged. See Section on missing ledger updates below.

4. **"H14 violation closures (enable_mtm_kill_switch + sl_cooldown_adaptive)"** — Both fields successfully migrated:
   - Scalar fields REMOVED from `ControllerConfig.hpp`
   - Replaced with `MASK_RISK_CFG_MTM_KILL_SWITCH_ENABLED` + `MASK_RISK_CFG_SL_COOLDOWN_ADAPTIVE_ENABLED` bits
   - Consumers migrated: `ControllerEventLoop.hpp:2944` uses MTM bitmap; `PortfolioController.hpp:664` uses cooldown bitmap ✓

5. **"9-field GLOBAL re-categorization"** — 9 fields successfully migrated from per-core to GLOBAL registry:
   - kill_recovery_warmup, sl_cooldown_cycles, sl_cooldown_base, sl_cooldown_extra, idle_reset_cycles, model_max_age_hours, lazy_rebuild_price_threshold_pct (7 fields)
   - Plus 2 BOOL→bitmap (covered in H14 claim)
   - Per-core registry rows DELETED; global rows + defaults present ✓

6. **"NEW EMIT_PER_CORE_CFG_DEFAULT_GLOBAL_MIRROR walker"** — Present in engine repo:
   - Defined at `CfgFieldRegistry.hpp:770`
   - Invoked at `ControllerConfig.hpp:1474`
   - Inline comments confirm walker-propagation for 5 per-core-reclassified fields ✓

7. **"Class 26 recurrence_count 1→11 MANDATORY structural fix"** — Workspace ledger confirms:
   - `/DOCS/recurring-bug-patterns/class-26-global-consumer-reading-per-core-field.md` shows recurrence_count: 11
   - v1.7.6 cycle added 10 worked instances in single cohort
   - Promotion threshold met ✓

8. **"Tests preserved: 3215/0"** — Verified by running `./build/controller_test`:
   - Output: "RESULTS: 3215 passed, 0 failed" ✓
   - (Note: Plan postmortem claimed 3217; CLAUDE.local.md says "3217 per claim"; actual is 3215. Discrepancy is a finding—see below.)

### ⚠️ PARTIAL (1 claim)

1. **"Closes 7 PARITY entries"** — Plan claims 7 closures (026/027/028/029/030/031/032). Implementation closes 1 (PARITY-032 via EngineCommon_SlowPathCycleOneCore by-construction closure for BREAKEVEN_ON_PROFIT dispatch). Other 6 remain OPEN per ledger:
   - PARITY-026 (kill_switch dispatch missing) — plan says "closes" but ledger: OPEN
   - PARITY-027 (exit-model dispatch missing) — plan says "closes" but ledger: OPEN
   - PARITY-028 (confidence scorer boot missing) — plan says "closes" but ledger: OPEN
   - PARITY-029 (strategy init missing) — plan says "closes" but ledger: OPEN
   - PARITY-030 (BNB discount missing) — plan says "closes" but ledger: OPEN
   - PARITY-031 (regime collapse) — plan says "closes via Phase C.4.5"; ledger shows OPEN but plan body v1.7.5 amendment claims closure at WIP-15
   - PARITY-032 (breakeven dispatch) — **CLOSED** ✓

   **ROOT CAUSE:** The **EngineCommon helper extraction (Phase B)** provides the DELIVERY MECHANISM for closures 026/027/028/029/030 (these are all "missing call at BOOT or in slow-path body" — Phase C migration should wire them). However, the actual wiring to CALL these helpers from the right sites + for the right symptoms is **NOT explicitly documented in the postmortem**.

   Code analysis: `EngineCommon_BootPerCore` covers PARITY-027/028/029 (Strategy_InitPerCore + ConfidenceScorer bindings are inside body). `EngineCommon_ApplyBnbDiscount` covers PARITY-030. PARITY-026 (kill_switch configure) — **NOT found in helper bodies; not closed**. PARITY-031 (regime collapse) — Phase C.4.5 planning notes mention closure per "per-core regime read" but phase-C.4.5 commit (WIP-15) is labeled "PARITY-031 ordering closure" with "4th consumer addition" — reads do exist but ledger says OPEN.

   **SEVERITY: HIGH** — Plan claims structural closure of 7 PARITY entries; only 1 verifies as actually closed in the ledger. The other 6 are either (a) undelivered, (b) delivered but ledger not updated, or (c) deliver by-construction but undocumented.

### ❌ GAP (1 critical finding)

1. **"PARITY ledger auto-updates"** — The postmortem references a v1.7.5 amended plan body that claims "PARITY ledger inline fixes" (decision log section + postmortem line 92-93 reference). However:
   - Workspace PARITY_ISSUES.md: entries 026/027/028/029/030/031 still marked OPEN (WIP-12 commit message says "PARITY ledger inline fixes" but entries remain unchanged)
   - Engine repo PARITY_ISSUES.md: same—entries unchanged, only PARITY-032 shows CLOSED status
   - **Manifestation:** postmortem claims "7 closures" but ledger reflects only 1

   **Severity: CRITICAL** — This is a discrepancy between plan claims (postmortem line 26: "Closes 7 PARITY entries") and delivered state (ledger: 1 closed, 6 open). Operator reading the ledger sees OPEN issues; postmortem says CLOSED. When a sprint is named "LIVE-READINESS" and claims to close 7 critical parity entries but ledger shows them still OPEN, this is a safety-critical communication gap.

---

## Hand-waves caught (severity-classified)

### [CRITICAL] (2)

1. **PARITY ledger not updated despite postmortem claims** — Postmortem v1.7.5 section says "PARITY ledger inline fixes at v1.7.5: PARITY-031 line-number correction + PARITY-032 NEW entry" but actual ledger in both repos shows PARITY-026/027/028/029/030/031 still OPEN. WIP-12 commit message claims "PARITY ledger inline fixes" but no actual status transitions visible in committed file. **Root cause:** Postmortem was written BEFORE ship-close verification; hand-wave was operational assumption ("we fixed them in code, so mark them closed") without mechanical ledger update. **Remediation path:** Explicit verification step at ship-close: run `/parity-check` against shipped tag + update ledger status for each verified closure before postmortem finalization.

2. **Test count discrepancy: claimed 3217 vs actual 3215** — Postmortem line 12 says "tests: 3215/0" but CLAUDE.local.md (referenced in postmortem line 96) claims "3217 per CLAUDE.local.md claim". Actual test run: 3215. This is a 2-test delta. **Root cause:** Postmortem was drafted during WIP-12/13 (when test count may have been 3217); final WIP-16 commit may have changed baseline. No amendment recorded. **Remediation path:** Mechanical test-count verification at Phase D before postmortem finalization; update both postmortem + CLAUDE.local.md if delta occurs.

### [HIGH] (3)

1. **PARITY-026 (kill_switch dispatch) not visibly closed in code** — Postmortem claims "Closes 7 PARITY entries (026/027/028/029/030/031/032)" but grep of shipped code shows no NEW call to `EventLoopState_ConfigureKillSwitch` in LIVE path (EngineSharded.hpp). PARITY-026 symptom: "Live engine never calls EventLoopState_ConfigureKillSwitch". Plan says Phase B EngineCommon_BootPerCore closes this. Grep shows EngineCommon_BootPerCore called at EngineSharded.hpp:949. **But:** hand inspection of EngineCommon.hpp BootPerCore body (lines 235-427) shows NO call to EventLoopState_ConfigureKillSwitch. **Root cause:** Helper extraction was the DELIVERY MECHANISM for PARITY closure; actual migration of OMS_ConfigureXxx calls that LIVE needs may have been assumed done elsewhere. **Remediation:** Grep EngineSharded_Run for all OMS_Configure calls pre-extraction; verify each is either (a) inside BootPerCore body now, or (b) called from caller pre/post BootPerCore. If (b), that's a STRUCTURAL GAP — boot-time config should be IN BootPerCore, not surrounding it.

2. **PARITY-031 ledger shows OPEN but plan claims closure** — Ledger status OPEN; plan body Phase C.4.5 describes "DELETE all 4 consumers + ADD per-core regime read". WIP-15 commit message says "PARITY-031 ordering closure" + "4th consumer addition" (NEW). Mechanical inspection of BacktestSharded.hpp post-WIP-15 should show the ADD. If present, ledger status should be CLOSED but wasn't updated. **Severity: HIGH** — If code closes it but ledger says OPEN, operator is confused about live readiness. If code doesn't close it, postmortem is misleading.

3. **Discipline drift: NEW scalar cfg mirror fields at global surface** — Phase Cx-D extension adds 7 NEW global cfg fields (kill_recovery_warmup, sl_cooldown_cycles, etc.). These are global SCALAR fields on the ControllerConfig struct. Per Class 27 prevention discipline (cfg-scope-discipline.md), global scalars that mirror per-core concepts risk enabling Class 27 at global-consumer sites. The 9-field recategorization rationale (v1.7.6 plan body line 84) says "each core legitimately different" → "GLOBAL is architectural LIMITATION" — but then stores them as GLOBAL scalar anyway. **Root cause:** Reclassification decision was "these 9 fields are CONCEPTUALLY GLOBAL even though per-core registry membership was wrong" per operator pushback. But 3 of them (regime_hysteresis, exit_threshold, sl_cooldown_cycles) per v1.7.6 table have CLASS 25 consumer FIXES noted, implying per-core consumers DO read them differently per core. **Remediation:** Explicit § 4 statement in plan: "why global storage is safe despite per-core consumer reads?" If answer is "walker propagation makes all per-cores uniform," document that as a disciplinary assumption + flag for audit-driven surface sweep.

### [MEDIUM] (2)

1. **Hand-wave comment: "for now" + "will revisit" in Phase Cx scope** — Plan body v1.7.6 line 89 says "sl_cooldown_cycles reclassification rationale (v1.7.6 mid-cycle): initial categorization marked sl_cooldown_cycles as GLOBAL/drainer-uniform based on CURRENT architecture". Comment signals architectural debt ("architectural LIMITATION, not design intent"). This is a structural assumption that could be violated in future refactor. Recommendation: add TECH_DEBT entry + link from cfg field comment. (This is not a hand-wave in shipped code, but a hand-wave in plan scope—acceptable at plan layer if documented.)

2. **Branchless opportunity: BootPerCore body has sequential if/else on OMS subsystem state** — `EngineCommon_BootPerCore` body (EngineCommon.hpp:235-427) calls multiple `OMS_Configure*` functions sequentially in if/else blocks based on cfg flags. Each one evaluates a separate cfg predicate. While not Class 28 (not a hot-path dispatch on runtime enum), this is a boot-time dispatch on cfg flags that could be compacted via a single cfg bitmask FOREACH walker per DOD principles. Minor optimization opportunity (not a finding, since boot is not a latency-sensitive surface).

---

## Optimization opportunities

1. **PARITY-026 remediation pathway** — If kill_switch configure is inside BootPerCore body, grep says it's not visible. Recommend: extract OMS_Configure cohort (ConfigureKillSwitch + ConfigureSLCooldown + ConfigureExitThreshold if present) into a sub-helper + call from both LIVE + BACKTEST pre-loop. This would make the parity closure explicit.

2. **Discipline enforcement: cfg field categorization at registry ADD time** — Plan postmortem notes `feedback_cfg_field_categorization_at_registry_add_time` memory codified. Recommend: CI Check 8 implementation should fire at `/readiness Check 44` plan-time to prevent recurrence. Currently deferred to sister mini-ship.

3. **Test count tracking** — Recommend: CLAUDE.local.md pinned test count (currently "3217 per claim") should be auto-derived from actual test run at ship-close, not hand-entered. Suggestion: pre-commit hook runs test count verification + patches CLAUDE.local.md + commits together.

---

## Discipline drift detected

1. **Scope extension post-plan-lock not re-audited** — v1.7.6 plan body amendment (Phase Cx-cfg-cohort) was mid-ship scope expansion triggered by "operator question on regime_hysteresis". Postmortem notes "3 parallel audit agents fired" post-hoc, but no explicit re-gate before WIP-16 coding. Per `feedback_amendment_audit_trigger_at_substantive_scope_change`, this should have been a blocking gate. Postmortem says audits returned YELLOW (findings) — those were addressed, but workflow suggests reactive audit rather than proactive gate. Recommend: enforcement at amendment-draft time in future ships (amendment triggers /precoding-audit-gate fire BEFORE coding proceeds).

2. **Iteration spiral tolerated at discipline-codification layer** — v1.7.6 summary says "4 path iterations" (Path 1 NO_FLAT_FIELD overengineering → Path 2 → v3 → v4). Postmortem codifies this as `feedback_iteration_spiral_signals_audit_meta_gap` (per-plan-discipline detection). This is correct meta-gap codification, but tolerance of 4-iteration spiral at WIP layer suggests planning-time surface enumeration gaps (DESIGN_SPECS disciplinary cross-ref should have caught Path 1 overengineering pre-WIP). Not a regression (meta-gap recognized + codified), but a workflow efficiency signal.

---

## Recommended remediation triage

### Fix at follow-up sub-ship (v5.15.5.F.4d.1.B.5)

- **PARITY ledger status updates (CRITICAL):** Run `/parity-check` against v5.15.5.F.4d.1.B.4 tag on the current engine repo. For each PARITY entry that claim ship closes, verify mechanical closure (grep for the call, confirm in both LIVE + BACKTEST paths). Update ledger status: CLOSED + `closed_at` + `verification` field. Do this BEFORE v5.15.5.F.4d.1.B.5 kickoff to avoid "is B.4 live-ready?" ambiguity.
  
- **PARITY-026 visibility audit (HIGH):** Grep engine repo for `EventLoopState_ConfigureKillSwitch` + `OMS_ConfigureKillSwitch` calls post-WIP-11. If present in new EngineCommon_BootPerCore body, mark VERIFIED. If not, hand-wave was that Phase C extraction provides mechanism—recommend explicit sub-helper for OMS_Configure cohort + dual-path call sites in B.5 phase or future.

- **Test count reconciliation (CRITICAL):** Current actual: 3215. CLAUDE.local.md claim: 3217. Postmortem claim: 3215. Recommend: automated test-count verification + CLAUDE.local.md patch at ship-close, OR reset CLAUDE.local.md claim to actual 3215. Include in v5.15.5.F.4d.1.B.5 opening checklist.

### Schedule dedicated audit-driven sweep

- **Class 26 + Class 27 surface enumeration (post-v5.15.5.F.4d.1.B.5):** Phase Cx-cfg-cohort added 9 new global scalar fields. Recommend quarterly `/registry-fit-audit` + `/accounting-audit` sweep (per `project_anti_spaghetti_audit_cadence`) to enumerate any Class 26 sites reading these as flattened + Class 27 instances using them as subsystem-state mirrors.

- **Branchless dispatch opportunity scan on BootPerCore body (deferred; low priority):** Current BootPerCore body has sequential OMS_Configure calls gated on cfg flags. Not a Class 28 (boot-time, not hot-path), but DOD optimization candidate. Schedule post-live-deploy if latency headroom available.

### Defer (operator decision)

- **Architectural reclassification rationale documentation (MEDIUM):** v1.7.6 plan body line 84 notes "sl_cooldown_cycles LIMITATION of current dispatch, not a CONCEPTUAL property". Recommend: add § into `cfg-field-categorization-discipline.md` v1.1 documenting the 3-field (regime_hysteresis, exit_threshold, sl_cooldown_cycles) architectural debt + lifecycle expectations (when REFACTOR EventLoop_DrainPostFill signature would promote sl_cooldown_cycles back to per-core). This is durable institutional memory, not an immediate fix.

---

## Summary metrics

| Category | Count | Status |
|---|---|---|
| **Plan claims VERIFIED** | 8 | ✓ |
| **Plan claims PARTIAL** | 1 (PARITY 026-031 ledger gap) | ⚠️ |
| **Plan claims GAP** | 0 (ledger update is a GAP, not a plan claim miss) | — |
| **Hand-waves caught [CRITICAL]** | 2 (ledger gap, test count delta) | 🔴 |
| **Hand-waves caught [HIGH]** | 3 (PARITY-026 visibility, PARITY-031 status, discipline drift) | 🟠 |
| **Hand-waves caught [MEDIUM]** | 2 | 🟡 |
| **Optimization opportunities** | 3 | — |
| **Discipline drift signals** | 2 | — |

---

## Conclusion

**Ship v5.15.5.F.4d.1.B.4 achieves its primary goal** (train-serve execution-layer parity via EngineCommon helper extraction) **mechanically**. EngineCommon contains the 5 specified helpers; both LIVE (EngineSharded) and BACKTEST (BacktestSharded/ShardedBacktestDriver) invoke them. Cfg-field recategorization (9 GLOBAL + 2 H14 bitmap migrations) is landed + mechanically verified.

**Critical structural gap:** PARITY ledger not updated to reflect claimed closures. Postmortem claims "Closes 7 PARITY entries" but ledger shows 6 still OPEN. This is a **safety-critical communication failure** for a sprint named "LIVE-READINESS". Recommend: mechanical `/parity-check` run + ledger updates BEFORE v5.15.5.F.4d.1.B.5 kickoff.

**Secondary gaps:** PARITY-026 (kill_switch) closure mechanism is unclear; test count has 2-test discrepancy with documented claim; scope extension (Phase Cx) was post-plan-lock but audited only reactively.

**Discipline strengths:** Meta-gap codifications (Class 26 promotion, B14/B15 pillars, `feedback_iteration_spiral_signals_audit_meta_gap`) demonstrate mature audit-driven discipline. Operator pushback on architectural merit is being systematically translated into design spec (cfg-field-categorization-discipline.md). Cross-agent audit convergence is working.

**Recommendation:** PASS with CRITICAL remediation required before declaring v5.15.5 LIVE-READY. The code ships correctly; the communication (ledger + test count) must align with claims.

---

## Cross-references

- Plan body: `subplans/2026-05-24-v5.15.5.F.4d.1.B.4-train-serve-execution-layer-parity.md` v1.7.6
- Postmortem: `2026-05-27-v5.15.5.F.4d.1.B.4-postmortem.md`
- PARITY ledger (engine): `/FoxML_Trader_v2/DOCS/PARITY_ISSUES.md`
- PARITY ledger (workspace): `/tick-trader-percore-workspace/DOCS/PARITY_ISSUES.md`
- Class 26 spec: `tick-trader-percore-workspace/DOCS/recurring-bug-patterns/class-26-global-consumer-reading-per-core-field.md`
- Cfg field categorization: `FoxML_Trader_v2/DESIGN_SPECS/framework-patterns/cfg-field-categorization-discipline.md` v1.0
