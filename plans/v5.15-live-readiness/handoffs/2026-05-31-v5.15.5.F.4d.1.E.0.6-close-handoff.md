---
type: handoff
ship_tag: v5.15.5.F.4d.1.E.0.6 (SHIPPED — GPG-tagged + pushed)
semantic_name: .E.0.1 — the FP+replay+locale determinism NET (Net-2 of D-73)
predecessor: 2026-05-31-v5.15.5.F.4d.1.E.0.1-determinism-net-session-close-handoff.md (the pickup this session started from)
successor: numeric-foundation unification ship (#11; gated behind this net, now UNBLOCKED) → .E.1 (Core→Node rename)
engine_head: 3f415a0 (committed + pushed to origin feat/v5.15-live-readiness; tag v5.15.5.F.4d.1.E.0.6 pushed)
workspace_head: pushed at this close via /sync-workspace
branch: feat/v5.15-live-readiness
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (D-90..D-115; NO new D's this session — execution + ship-close)
postmortem: plans/v5.15-live-readiness/postmortems/2026-05-31-v5.15.5.F.4d.1.E.0.6-postmortem.md
rollback_anchor: v5.15.5.F.4d.1.E.0.5 @ the prior GPG-signed tag
---

# Session close — .E.0.1 determinism net SHIPPED as tag E.0.6. Next = #11 numeric-foundation.

**Created 2026-05-31.** Branch `feat/v5.15-live-readiness`. **Pickup: `/accept-handoff <this doc>`.**

## 1. State — .E.0.1 (the determinism NET) is SHIPPED and verified. .E.0 foundation phase is DONE.

`v5.15.5.F.4d.1.E.0.6` is committed (`3f415a0`), GPG-tagged ("Good signature from Caramel"), and pushed to origin. The semantic name is `.E.0.1` (the determinism net); the tag is monotonic `E.0.6` per D-88 (prior tag E.0.5 = semantic .E.0.2). It gates **#11** (decimal numeric core) + **.E.1** (Core→Node rename), both now unblocked.

**What shipped (commit `3f415a0`, 10 files):**
- **FP-determinism** F-056/57/58 + **replay** F-054/55 + recorder `to_chars` (the engine clusters landed in the prior WIP commits 2c8830a..74bd77b; this commit is the close).
- **F-076** zero-init `ControllerConfig` cfg-fingerprint ctor (PARITY-035, **CLOSED**). The struct is PADDED → the ctor is the guard (a `has_unique_object_representations` static_assert applies only to padding-free StampT).
- recorder/replay locale loop (PARITY-036, **CLOSED**).
- **Locale-determinism class close**: Classes 37/38/39 + `DESIGN_SPECS/data-disciplines/locale-determinism-discipline.md`.
- **Standing determinism net**: `tools/check_determinism.sh` → pre-commit Check F + `tools/check_determinism_selftest.sh` (the net guards the net — proves each gate catches its injected regression).
- **3 silently-broken CI guards CLOSED this session** (the session's theme): (1) the dead pre-commit hook [prior, 74bd77b]; (2) the **detached-stdin HANG class** (`exec </dev/null` on the hook + det scripts + a self-test guard + LANDMINES Landmine 2 — closes the 74bd77b comment-not-a-guard gap); (3) **`calls_graph_diff.sh`** (hardcoded `/home/caramel/code/tick-trader-percore` path → `$REPO_ROOT` + `EngineCommon.hpp` added to `SHARDED_FILES` → false `Regime_Init` orphan → rc=1 across prior ships; now rc=0 CLEAN).

## 2. The ship-close incident (READ — it shaped the close + the next-session guards)

A thoroughness pass (operator-driven "are we sure nothing's broken") caught a **BROKEN BUILD that the predecessor handoff AND a `/readiness` audit had both reported "green."** The "green" was **stale build cache**: a stray, **agent-authored** broken `static_assert` (incomplete-type on instantiation + wrong-premise on a padded struct) + a stray misplaced doc had been **staged by an over-stepping read-only `/readiness` Explore subagent** (Bash write-escape). Discarded; clean rebuild from zero = **3241/0**. An **independent adversarial verifier** then caught the `calls_graph_diff` bug + my own `cmd | tail` pipe-masking error (I'd masked the tool's real rc=1). Harvested as **meta-anti-pattern AR-4** (verification-by-proxy — its dominant recurrence). The lesson: **clean-build-from-zero + independent adversarial verification; never trust GREEN-on-clean or a proxy exit.**

## 3. Verification receipts (clean-from-zero, not cache)
controller_test **3241/0** (clean rebuild) · depth_recorder_test **17/0** · `build.sh all` from zero rc=0 (engine, engine_gui, foxml_suite+XGBoost, compare_scalers, parity_harness) · `check_determinism.sh` GREEN · `check_determinism_selftest.sh` GREEN · `calls_graph_diff verify` rc=0 CLEAN · independent adversarial verifier confirmed all of the above (it's what caught the calls_graph_diff bug). Hot path UNTOUCHED (by inspection — no hot-path files in the diff — AND by the now-fixed tool).

## 4. Deferred follow-ups (durably tracked — TECH_DEBT-151 + TaskList; NONE evaporate)
- **#8** standing CI orphan-guard (dead-code prevention) — **`.E.1`-prep** (the rename is a dead-code-generating event; the guard must exist first). Promote `calls_graph_diff.sh` to a pre-commit Check H; verify-by-injection.
- **#9** guard: flag un-attributed working-tree mutations (the ROOT of this incident — a read-only subagent wrote+staged junk). Snapshot+compare `git status` after a read-only/audit subagent; ship-close check for staged files outside the declared set.
- **#10** hang-class remainder: extend `exec </dev/null` to the other `tools/*.sh`; codify the agent `< /dev/null`-on-detached-bash habit.
- **#2** codify tail: `/post-ship-audit` AR-4 wiring + locale-spec sister-reciprocation.
- **D-98** backlog→`.E`-home sweep · task-#4 codification + skill-auto-fire (D-94/D-95).

## 5. HEADLINE NEXT — #11 numeric-foundation unification ship (HIGH-RISK)
D-97 DECIDED: money = **DECIMAL** via ONE unified `FixedPoint<RADIX,FRAC>` (binary `<2,64>` features / decimal `<10,8>` money; absorbs `FixedPoint64`; reuses `.E.0.1`-certified binary bodies; D-99..D-110). Foundation design doc WRITTEN + audit-clean (`subplans/2026-05-30-...E-money-numeric-core-foundation.md`; B-Plus EXIT 0). Sequenced **after `.E.0.1` (done → unblocked), before `.E.1`** (D-101). Its own `/precoding-audit-gate` + `/blindspot-scan` + new-fn design-audit (D-93). Subsumes `.E.0.3` (D-108). Blast radius incl. ShardedSnapshotPersist + OrphanRecovery double→FPN boundary (D-110).

## Required reading (pickup)
This handoff + the `.E.0.6` postmortem + the `.E.0.1` plan body (now `status: SHIPPED`) + the #11 foundation design doc + decision-log D-90..D-115. Baseline CLAUDE.md/local/MEMORY auto-load (sprint-state updated to E.0.6 this close).

## TaskList (recreate)
#3 #11 numeric-foundation (pending; HEADLINE NEXT; gated behind .E.0.1 = now unblocked) · #2 codify tail · #4 D-98 backlog map · #5 .E.1 rename · #6 SWAR parse · #7 concern-tags · #8 orphan-guard (.E.1-prep) · #9 tree-mutation guard · #10 hang-class remainder. (#1 .E.0.1 ship = COMPLETED this session.)

## First action
`/accept-handoff <this doc>` → then begin **#11 numeric-foundation unification** (its own `/precoding-audit-gate` + `/blindspot-scan` first, per D-93/D-99) on the now-locked determinism-net foundation.

**End of E.0.6 close handoff.**
