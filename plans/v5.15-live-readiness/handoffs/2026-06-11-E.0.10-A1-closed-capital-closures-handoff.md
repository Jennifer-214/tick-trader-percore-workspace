---
type: handoff
status: active
ship_tag: .E.0.10
plan_type: live-readiness
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness)
ship_end_goal: a pre-.E.1 correctness/characterization net + the capital bugs the net surfaced, closed where durable
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-11-E.0.10-net1-adversarial-default-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, register (bug-hunt §), plan-body, sprint-MASTER]
engine_head: b89b60d
workspace_head: a9c3bb8 (pre-sync; this close pushes the session docs)
deletion_scope: none
coding_status: A1 CLOSED (fix+test+refute); A3 scoped (retry fork); A5/A6/A7/A8 + A2/A4 + conc-5 pending
---

# .E.0.10 handoff — A1 closed + the capital-closure queue (2026-06-11 close-out)

**Target ship:** `.E.0.10` — Net-1 correctness net + the capital bugs it surfaced (pre-`.E.1` regression safety).
**Branch:** `feat/v5.15-live-readiness` · **Engine HEAD:** `b89b60d` (A1 fix committed; clean tree) · **Tests: 3368/0.**
**Register (live disposition SSoT):** `plan_checks/E.0.10-finding-disposition-register.md` § "🔎 Net-1 adversarial money-surface bug-hunt".

> **Compaction note:** this session ran very long. Treat every claim here as a HINT; verify against current code (`feedback_compaction_degrades_treat_handoffs_as_hints`). This close ran a 3-agent independent completeness review (Stage 5.5) — its findings were folded; see "Close verification" below.

## What landed this session (the arc)

1. **5-agent adversarial money-surface bug-hunt → 14 findings, 3 NEW HIGH capital bugs verified.** The Net-1 thesis (characterize + adversarially refute → find latent capital bugs) paid off. Findings A1–A8 + confirmed F-096 (precise site `Async.hpp:827`) + D-123 members. ALL documented in the register bug-hunt § + TECH_DEBT-168→171 + PARITY-039 + folded into `.E.1`/SWAR-parse plans.
2. **Over-defer rule fixed (the codification trigger):** I deferred 8 fixes to `.E.1`/D-123 on plan-ADJACENCY; operator caught it ×2. Fix: `feedback_opportunistic_tech_debt_closure` made **BIDIRECTIONAL** (governs deferring a NEW fix; durable-fix-the-plan-only-renames ≠ subsumed → close NOW) + `feedback_fold_findings_into_destination_plan` "folding TRACKS, does NOT defer the fix" clarifier. Harvested to meta-anti-pattern-index PL-4.
3. **H22 — Scale-invariance / shard independence** added to the Hard-Invariants table (per-node/-cluster logic = pure fn of node-local inputs; "(N+1)th node/cluster = ZERO change to existing logic"; generalizes Class 26/27/torn-read + A1). DESIGN_SPEC + CI-check still owed (task #21).
4. **A1 CLOSED** (the first capital bug fully run to ground): single-source `ResolvePerFillTpPct/SlPct` helper (`StrategyParameters.hpp`) called by BOTH the entry dispatcher (SimpleDip/MR/EmaCross/Momentum) AND restore (`ShardedSnapshotPersist.hpp`) — restore was reading the GLOBAL `take_profit_pct`, dropping the per-node override. SimpleDip+MR cohort char-test (3368/0); **3-agent independent refute SOUND/CORRECT**; EmaCross stateless branch documented as an H22 exemption. TECH_DEBT-168 closed · PARITY-039 fixed. Engine commit `b89b60d`.
5. **Module-scoped CLAUDE.md rollout (3/7):** DataStream/ Backtest/ GUI/ landed (workspace files + engine symlinks); Strategies/ ML_Headers/ MemHeaders/ FixedPoint/ drafted in-agent (BLOCKED on the sandbox; need relaying in — task #20).
6. **ADHD memory amendment** — "it's not about earning the coding, it's readiness-gated" (her correction).

## The OPEN QUEUE (next-session work, in rough order)

- **A3 — FlattenAll half-flatten (the next capital closure; SCOPED + has a design fork).** `EventLoop_FlattenAll` (`ControllerEventLoop.hpp:3413`) discards the `OMS_PushExitForSlot` bool (`submitted++` unconditional `:3445`) → under partials a full submit_queue leaves leg B OPEN on the emergency path. Split:
  - **Durable, close-now:** A8 `leg=(slot&1)` (`:3444`, behavior-preserving); A7 refuse-on-`price≤0` (`:3426`, vs the `Money_Zero` wipeout); A3-core honest `submitted += pushed` + log the shortfall.
  - **THE FORK — the retry mechanism:** the caller CAS-sets `flatten_pending=1`, fires once, auto-clears only at the recovery-window deadline (`EventLoop_TryClearRecovery` `:3567`) → the current model BLOCKS re-fire within the window, so a queue-full push isn't retried. A real retry needs new plumbing. **AND `.E.1` reworks the drainer/queue model (drainer→per-node absorption)** → the retry's right shape may be genuinely `.E.1`-SUBSUMED (the merit defer, NOT adjacency). **VERIFY against the `.E.1` drainer plan, don't assume.** Lean: close the durable 3 now (+ a characterization test pinning the half-flatten), then verify-or-build the retry. TECH_DEBT-170.
- **A5 / A6 — durable guards (close-now):** A5 WS fill side cross-check vs venue `"S"` (Knight-shaped, ~5 lines; `OrderManager.hpp:1330`); A6 ML barrier-blend `tp_pct/sl_pct` sign/range clamp + `live_sl<entry<live_tp` assert (`StrategyParameters.hpp:1366`). TECH_DEBT-171. Each = fix + char-test + 3-agent refute + `// ADV-REFUTE`.
- **A2 / A4 — parser venue-ingest (data-loss STOP now; decimal-exactness rides D-123):** A2 partial-fill qty drop (`"z"` never parsed); A4 BNB/reconcile commission drop. The full decimal fix is genuinely D-123-subsumed; the data-loss STOP (accumulate qty; pass reconcile commission) is durable-now. TECH_DEBT-169.
- **conc-5 — CRITICAL .E.1 live-enable gate:** submit_queue multi-producer race; verify via tsan on a disposable clone (task #4).
- **Codification follow-ons:** H22 DESIGN_SPEC `per-node-purity-scale-invariance.md` + CI-check (task #21); the NEW anti-pattern class "reconstruct-path-reads-a-DIFFERENT-source-field" (A1's class) → RECURRING_BUG_PATTERNS + DESIGN_SPEC (task #10/#12); the `characterization-test-discipline` DESIGN_SPEC (TECH_DEBT-164 part 2, task #12).
- **task #13 — `.E.1` B-Plus reconciliation (NOW ACTIVELY TRIPS):** folding A1/A3/A2-A4 into the `.E.1` plan made it session-modified, so `check_session_docs.sh` HARD-fails on its 10 pre-existing illustrative `(NEW)` template fragments (`NodeState.hpp`/`ExchangeRegistry.hpp`, lines ~366/438/.../1424) + 1 stale citation (`EngineSharded/Run.hpp` missing the `CoreFrameworks/` prefix). VERIFIED these are the known false-positive (none from this session's folds). Until reconciled (fence the fragments as illustrative), a clean sweep needs `SKIP_PLAN_BODY_CHECK=1` (justified). **Priority** — it now trips every close.
- **The 4 remaining tag files** (Strategies/ML_Headers/MemHeaders/FixedPoint module CLAUDE.md) — drafted, blocked-on-sandbox; relay the agent drafts into the workspace dirs + symlink (task #20).

## ➕ Carried forward from the prior Net-1 handoff — STILL OWED (the A-series did NOT absorb these)

The bug-hunt + A1 dominated this session, but the prior handoff's Net-1 queue is **not** all closed — these remain owed and are DISTINCT deliverables (do not let the A-series mask them):
- **hpg-bc-1 / F-059 — the golden-master oracle (was the prior handoff's NEXT action).** Freeze the REAL per-fill `tp_pct`/`sl_pct` → `live_tp`/`live_sl` (`ExecutionCore.hpp:543`) → SG-trigger → exit-P&L chain the experiment's stub-oracle never covered. A1 FOUND a bug in that chain (restore divergence) but did NOT write the golden-master — **F-059 is still owed.** `// ADV-REFUTE` binding. (task #7)
- **MED-tier CHARACTERIZATION** (the regression tests, distinct from the A-bug FIXES): F-090/093 (parser full price/qty/commission — A2/A4 are the BUGS here; the characterization test is separate), F-046 (FlattenAll partials-on balance — A3 is the bug; characterize the balance), F-018 (partial-exit MONEY win/loss-agg invariant; NOT the slot-mapping = `.E.1` changes-by-design). (task #8)
- **wfa-1** (warm-restart exit-armed) — LIKELY-CLOSED, confirm. (folded into task #7/#18 surface)
- **Phase-1 backlog re-triage** (93 open TECH_DEBT + 5 PARITY → `.E.1`-home mapping) — IN-PROGRESS; register is the live SSoT. fpmem granular ×8 + the E.1-bucket MED/LOW + the PRE-PAPER-TEST PERSIST subset still pending. (tasks #1/#2/#5)
- **5-block capital-refute marker backlog** (the sweep's standing advisory): D-110, oms-ts-2, the core_fees tests carry no `// ADV-REFUTE` (self-attested when written). Add markers as each gets an independent refute. Shrinking per close-the-class.
- **TECH_DEBT-165 — the torn-read anti-pattern class + CI check** (the 9-site cross-thread 16B-`Money` read class; flag any >8B cross-thread-written field read outside a seqlock) → RECURRING_BUG_PATTERNS. **DISTINCT** from the A1 "reconstruct-reads-a-different-field" class. Site remediation rides the `.E.1` aggregator. (task #10)

## TaskList (recreate verbatim via TaskCreate at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | in_progress | fpmem granular ×8 re-triage → register |
| #2 | in_progress | Re-triage E.1 bucket (MED/LOW + 3 detail-reads) + PRE-PAPER-TEST PERSIST subset |
| #3 | completed | D-110 snapshot money-exact round-trip probe |
| #4 | pending | Verify conc-5 (CRITICAL) on disposable clone — tsan (`.E.1` hard live-enable gate) |
| #5 | pending | Backlog→`.E.1`-home mapping (93 TECH_DEBT + 5 PARITY) |
| #6 | completed | Net-1 characterization tests (oms-ts-1/1b/2, tsa-live-2, D-110, rsf-ts-1) |
| #7 | pending | **hpg-bc-1 / F-059 golden-master oracle** (the real per-fill exit chain) |
| #8 | pending | MED-tier characterization (F-090/093 parser, F-046 FlattenAll, F-018 partial-exit money) |
| #9 | completed | TECH_DEBT-163 context-aware loading |
| #10 | pending | Codify torn-read class + CI check → RECURRING_BUG_PATTERNS (TECH_DEBT-165) |
| #11 | completed | TECH_DEBT-164 part 3 — adversarial-default wired |
| #12 | pending | TECH_DEBT-164 parts 1+2 (AP4 extension + characterization-test-discipline DESIGN_SPEC) |
| #13 | pending | `.E.1` plan B-Plus reconciliation (NOW actively trips check_session_docs) |
| #14 | completed | Nav-infra wiring into /accept-handoff + /handoff |
| #15 | completed | F-096 ledgered as TECH_DEBT-167 |
| #16 | completed | **A1** — snapshot-restore TP/SL per-node override (CLOSED, refute SOUND, 3368/0) |
| #17 | pending | **A2** — parser partial-fill qty drop (data-loss stop now; decimal rides D-123) |
| #18 | pending | **A3** — FlattenAll half-flatten (durable-3 now; retry → verify `.E.1`-subsumption) |
| #19 | completed | Ledger + register the 8 bug-hunt findings (TECH_DEBT-168→171 + PARITY-039) |
| #20 | in_progress | Module-scoped CLAUDE.md rollout (3/7; Strategies/ML_Headers/MemHeaders/FixedPoint pending) |
| #21 | pending | **H22** codification — per-node-purity-scale-invariance DESIGN_SPEC + CI-check (invariant placed) |
| #22 | pending | Capital closures **A5/A6/A7/A8** (durable guards) |
| #23 | pending | Codify the /handoff + close-review prior-queue carry-forward check (meta-lesson: this handoff initially dropped the prior Net-1 queue; operator caught it) |

*(NEW classes owed beyond #10: the A1 "reconstruct-path-reads-a-different-source-field" anti-pattern → RECURRING_BUG_PATTERNS + a `forward-vs-reconstruct-single-source` DESIGN_SPEC; folds with #12.)*

## Close verification (this close ran an in-depth adversarial pass)

- **Mechanical sweep:** all HARD checks pass EXCEPT the known `.E.1` B-Plus FP (task #13); advisory = 5-block capital-marker backlog (known).
- **3-agent independent completeness review:** A1 CLEANLY CLOSED ✅ · findings+codifications VERIFIED ✅ · coherence/privacy/surgical sound (zero blockers). Folded their findings: the meta-harvest (PL-4 over-defer bump), the MASTER banner staleness (now reflects A1/3368/0), the register A1 table-row (→ CLOSED), and a CLAUDE.md H20/H22 typo.

## Git state
- **Engine:** `b89b60d` on `feat/v5.15-live-readiness` (A1 fix committed; clean tree). Pre-commit guards passed.
- **Workspace:** synced at this close (register + ledgers + memories + CLAUDE.md + tests + 3 tag dirs + this handoff).
- **TECH_DEBT-168** flagged CLOSED in `open.md`; physical move to `closed.md` deferred to ship-close (its own note).

## ⚠️ Drift items + landmines (carried forward + new — DO NOT lose)

1. **`check_session_docs.sh` RED on the `.E.1` B-Plus** (task #13) — NOW actively trips (folding A1/A3/A2-A4 made the `.E.1` plan session-modified). Known pre-existing false-positive: 10 illustrative `(NEW)` fragments (`NodeState.hpp`/`ExchangeRegistry.hpp`) + 1 stale citation (`EngineSharded/Run.hpp` missing the `CoreFrameworks/` prefix). Clean sweep needs `SKIP_PLAN_BODY_CHECK=1` until reconciled (fence the fragments).
2. **Engine-vs-workspace `tech-debt/` blindspot (Landmine 9):** the engine repo does NOT symlink `DOCS/tech-debt/` (only individual `DOCS/*.md` are per-file symlinks). The REAL ledger is `tick-trader-percore-workspace/DOCS/tech-debt/{open,closed}.md`. From the engine path it looks "missing" → a prior session opened a duplicate TECH_DEBT. **Always ledger via the workspace `DOCS/tech-debt/open.md` path.**
3. **Module CLAUDE.md symlinks are LOCAL-only:** the 3 new engine-side symlinks (`DataStream`/`Backtest`/`GUI`/CLAUDE.md → workspace) are gitignored in the engine (not committed) — they exist on this machine but need recreating on a fresh clone. The committed **workspace** `*/CLAUDE.md` files are the source of truth.
4. **Binding adversarial-default** — capital/judgment verification is INDEPENDENT-refute BY DEFAULT; `// ADV-SELF` is opt-out-with-reason. Do NOT self-attest "verified" on capital work.
5. **TECH_DEBT-168 physical move** to `closed.md` is deferred to ship-close (flagged `CLOSED` in `open.md` for now).
6. **Decision-log trail:** this session's big decisions are now tracked as **D-191 (capital-closure scope expansion / the money-hunt) · D-192 (A1 single-source fix) · D-193 (H22 invariant) · D-194 (over-defer rule → bidirectional)** in `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`, each with `<!-- D/C/F -->` + `<!-- STATUS -->` sentinels.

## Pickup
```
/accept-handoff /home/caramel/code/tick-trader-percore-workspace/plans/v5.15-live-readiness/handoffs/2026-06-11-E.0.10-A1-closed-capital-closures-handoff.md
```
**Immediate next action:** A3 (FlattenAll). Close the durable 3 (A8 leg-index, A7 price-guard, A3 honest-count) + a characterization test pinning the half-flatten; then VERIFY whether the retry mechanism is genuinely `.E.1`-subsumed (read the `.E.1` drainer plan) before building it. **Capital fixes are adversarial-refute BY DEFAULT** (binding) — fix → independent 3-agent refute → fold → `// ADV-REFUTE` marker.

## Operator collaboration (every-turn)
Address Caramel as Caramel / she / her. No AskUserQuestion modals — inline options. Evaluate on robustness + latency + design, not time. After pre-coding checks, consult before coding. Adversarial/independent-refute is the BINDING default for verification. Defer needs SUBSUMPTION (the plan's design redoes the fix), never ADJACENCY (the plan touches the surface) — `feedback_opportunistic_tech_debt_closure` (bidirectional). Coding is READINESS-gated, not earned (`user_adhd_deferred_reward_discipline`).
