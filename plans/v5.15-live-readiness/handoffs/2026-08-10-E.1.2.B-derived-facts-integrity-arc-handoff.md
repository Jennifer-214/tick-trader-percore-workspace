---
type: handoff
status: active   # THE one live handoff. Supersedes 2026-08-09-E.1.2.B-0.2-g3-humanization-cpp-policy-handoff.md (the D-413/414/415 derived-facts integrity arc replaced its world). ≤1-active enforced by check_handoff_active_singleton.py.
ship_tag: E.1.2.B
plan_type: mixed
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: every toolchain surface a thin consumer of the ONE core, 0.x→1.0.0 — TIMING REVISED by D-415 (C++ conversion DEFERS to v1; contracts + validity teeth are what accrue)
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-08-09-E.1.2.B-0.2-g3-humanization-cpp-policy-handoff.md
supersedes: 2026-08-09-E.1.2.B-0.2-g3-humanization-cpp-policy-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [doc-disciplines/in-code-documentation-schema.md, meta-disciplines/calibration-corpus-non-vacuity-discipline.md, audit-methodologies/differential-to-absolute-gate-contract-widening.md]
coding_status: "**FIFTH boundary (2026-08-12, 60-min session — MASTER UPDATE 27): OPERATOR ROUTING — PLUGIN FINISHES BEFORE ENGINE.** The E.1.2 resume re-queues BEHIND the plugin arc (pickup had started its punch-list — 3 frontmatter edits REVERTED clean; the c-class report stands ready, key anchors re-verified at `a71b893`, its `Portfolio.hpp:488/:901` partner cites do NOT verify). **TD-258 CLOSED both halves** (plugin `9d6c98b`+`3566412`: `toolio_kinds.lua` registry + `:checkhealth` parity + `assert_consumed` LOAD-BEARING at both decode seams + suite tooth 43/43; floor: `check_toolio_kind_parity.py` HARD in `check_session_docs.sh`, 6-teeth; entry moved open→closed; TOOLS.md + FEATURE_LOOKUP enrolled). **0.5 OPENED (same session): TD-257 substrate = `compile_command.py`** (ONE command source; §2 terminal half LIVE; `compile_command/1` envelope, exempt-tier'd; 7 teeth; the TD-258 gate FIRED live on it — producer-first proven). **BUILD HALF LANDED (2026-08-13, engine `2654ad4`): `./build.sh asm` 1:1 sidecars (objdump-from-shipped-binary — only honest form under `-flto`) + `CMAKE_EXPORT_COMPILE_COMMANDS=ON` (real dbs; `-DNDEBUG` divergence found live). CARDS viewer-half LANDED (plugin: `asmshipped.lua` `<leader>ds` — shipped-asm card, honest states, newest-first sweep; explorer relabeled; suite 44/44; live-smoked both ways on the Notify_Send case). SYNC LANDED (engine `dc6affc` + plugin `730cee1`: -g on 4 targets · sidecars auto-emit every build · objdump -l · `<leader>ds` bidirectional cursor-sync + inlined-from attribution + shipped insn counts; rungs roadmap = ideas §12; D-419 logs the decisions). DOGFOOD ROUND same sitting (plugin `644aa45`): painter (theme-linked builtin groups) · NAME-POSITION matcher (the FPN_Binary param-list false-match, regression-pinned) · struct guard (per-FUNCTION view says so) · card rnu/wrap off. SIXTH boundary (2026-08-14): sync band → whisper tint (`50a1549`) → LspReferenceText LINK per operator ("the variable-hover look", `702626c`) · **TD-257 latency half LANDED + operator-BLESSED** (ws `a328418`: flags derived from the shipping db — pinned list had drifted 4 count-affecting axes; wrong-flags tooth 82-vs-30; budgets re-blessed w/ _provenance; hot-path HOLDS; Check N green) · **FOLLOW SURFACE** (plugin `df66414`: `<leader>ds` toggles the ONE live card; follows the enclosing fn across files on idle; `<CR>` jumps markers incl. INTO inlined-from files; `r` re-resolves; 39 teeth). **TD-257 CLOSED (2026-08-14)** — the symlink question resolved as the BEST-OF-BOTH SPLIT (operator): the editor keeps its liveness db via the root symlink; every FACT consumer resolves SHIPPING-FIRST (`build/`'s db) — plugin `flags_for` + `compile_command.py` default + the hard-derived latency checker; entry moved open→closed with the 4-leg disposition. RIDERS landed: in-editor BLESS rows (menu → `:terminal`, a real pty — D-394 control intact) · `./build.sh test` chains the conformance REPORT (non-fatal; gate stays pre-commit) · follow/'for'-bug fix chain (tagged→treesitter→keyword-guarded-cword; plugin `3db363f`) · wheat-mix sync band w/ `sync_alpha`/`sync` dials. SEVENTH boundary (2026-08-14 late): BUDGET+SIMD chips on the shipped-asm card (basis-honest 'ratchet N probe-basis ✓/⚠'; plugin `ce60547`) · the OUTPUT LOG `<leader>dn` (notify ring, no :Noice digging; `62382e5`) · peach sync band (`3f0b745`) · **RC-F REGISTER-FIT CARD SHIPPED** `<leader>dR` (ws `f16acb7`: `--json` register_fit/1 envelope + `--struct`; plugin `c0ecbbe`: flagged-first render, both-costs header, H14 footer; the TD-258 gate FIRED pre-consumer AGAIN — second live proof; parity 6 kinds/4 consumed; suite 46/46). RUNGS LANDED (plugin `5c3a092`): ▲ branch-class marks (asmdiff classifier reused; data-dep/cond/cmov header chip) + CALL-FOLLOW (`<CR>` on a call retargets to the callee, `b` back-stack — the shipped-code browser complete). --INLINES RUNG LANDED (plugin `bdac9e3`, sidecar-format-free: one awk pass + the shared name-position rule; INLINED-AWAY lists callers navigably, `I` = where-else-inlined, live-proven Position_Reset→Portfolio_Init ×3). NEXT: the 0.5 tail (registry [ROW]/[COLUMN] cards · R5 · .toolbus) → plugin arc CLOSES → THEN E.1.2. NEXT: TD-257 CLOSE leaf (probe cutover + provenance + wrong-flags tooth + the insn-budget join) · 0.5 remainder (RC-F card · registry cards · R5 · .toolbus) → THEN E.1.2.** Earlier state follows: **SESSION CLOSED 2026-08-11 (MASTER UPDATE 26)** — close ritual: final sweep CLEAN · captures paid at FOUR boundaries · operator bless committed at close (`8921bef`, golden 888) · no regenerated handoff (this doc was kept continuously current; regeneration at context ceiling adds risk, not fidelity — the declared-delta pattern). **`0.2` IS COMPLETE (D-418)** — all seven deliverables (b)(c)(e)(f)(g)(d)(a) closed. THIS SESSION: the D-413/414/415 arc (4 leaves) → (g)-4 D-416 → (g)-5 D-417 (resolve_cited_path lift) → (d) update_toolchain.py (first run: 40 cite auto-repairs / 35 live docs) + (a) DOCS/TOOLCHAIN_CONTRACTS.md. Golden blessed 885 (+D-417/D-418 pending next cadence). NEXT threads (re-ground at the 2026-08-10 PICKUP session's second boundary — earlier: close-out verified · Notify.hpp stray d5a6881 · AR-8 PAID · asm/build.sh 1:1 logged ideas §2; THEN: **AR-8 holes 7–10 CLOSED ws `6df91fa`** [OR-fold arming · shrink-only ENFORCED+pawl · suite-row HARD · exempt tier — register dispositions flipped] · **0.4 TOOL half LANDED ws `a9a512e`** [`citable_ids --resolve` → `cited_path/1` envelope; selftest T6 + live tri-state smoke] · scratch-file stance codified [memory + D-413 evidence record + build_clangd gitignore]; all pushed): FOURTH boundary (overnight, operator dogfooding live — the tag-native feature wave, plugin `..80dba5d`): dashboard rewired onto the TAG CORPUS (repo-wide [SIZE]/[STRADDLE] facts — the origin-TU false-results bug dead; dir→file→list grouping, per-group folds, `g` grouped⇄ranked-25 toggle) · **R3 SHIPPED** (unitindex.lua — tree entries resolve to enclosing unit + [TAG] list) · tag-FILTERABLE trees (`/` + the `T` no-typing PICKER) · browse-by-[TAG] (`<leader>dt`) · TAG ADD (`m→Add [TAG]`, ✎, vocab-derived) · health rg/python/resolver checks · `.toolbus/` first-consumer SHIFTED to 0.5 (ideas §11 v.5). FEATURE_LOOKUP carries all of it. 0.4 remainder = infrastructure only (.toolbus@0.5 · TD-258 · verdicts@0.7). THIRD boundary (same night, operator dogfooding live): **docview SHIPPED + the PLUGIN-PARITY ARC — ALL SIX PHASES, one evening** (operator-directed fleet I-1/I-2/I-3 → SSoT register `plan_checks/2026-08-10-plugin-parity-fleet-register.md` + verbatim reports under `reports/2026-08-10-plugin-parity/`; plugin `30f48f7..f0b1149`: one registry/runner/renderer · the operator's LAYER-STACK rule · dm-EQUAL · notes-sweep removed · vocab-validated gating [all 5 grammar tables decoded] · chooser clamp/nowrap · curated `docs_refs` lens · keymap registry with derived help · menu-seam teeth; suite 40/40; [REFERENCE] corpus mined 550+132 ids engine `4371df3`+`a71b893`; cosmetic tail enumerated in the register). Register-pressure checker captured ideas §11.5 (0.5, dep TD-257). Agent-report persistence live (SUBAGENT_ARMING §6.5 + `reports/`). NEXT: ① the register's small tail (README key-block derive · ui.lua extraction · notify voice · checkhealth) · ② contract-stale 55 INFO · ③ tasks #5/#6 backlog (#6 half 1 done `9ad5cc9`) · ④ **E.1.2 (engine leaf) — the c-class currency verdict is IN** (`reports/2026-08-11-e12-currency/`): the plan is ONE SUPERSESSION LAYER BEHIND — Steps 1/2a/2b/2c SHIPPED 2026-07-04 (engine `2c11922`+`84d73e0`+`8648e27`; D-294..D-305) before the A/B campaign was inserted; the pickup's FIRST act = the report's 11-item punch-list (amendment-9 banner · mark AM-8 + Phase-F-③ delivered · D-233 swap · fix the PORTFOLIO 7→8-vs-BLK-2 contradiction · re-anchor ~30 cites · fresh `pre-E.1.2-resume` tag), THEN resume at the D-305 tail (`FOREACH_NODE_PERSIST_FIELD` first — grep-zero today). INBOUND straddles all VERIFIED live (+ NEW: `swap_model_path_requested[16]` joins the writer-group); ship close retires EXACTLY the 4 engine baseline keys (never the 9 suite ones). Amendment 5 EXECUTED at receipt (E.1.3 td_closed strip)"
---

# E.1.2.B — the DERIVED-facts integrity arc (D-413/414/415) — leaves 1–3 shipped · leaf 4 partial

**Written 2026-08-10 under operator laptop-time pressure. The DISPOSITION SSoT for this arc is
`plan_checks/2026-08-10-D414-toolchain-sweep-finding-register.md`** — read IT, not prose recall.
Decisions: **D-413** (mismatch diagnosis + C1–C4) · **D-414** (4-I+A sweep, compounding order) ·
**D-415** (C++ DEFERRED to v1; foxtag demoted frozen-kept; parity gate PARKED, `FOXTAG_REARM=1`
runs it; re-arms at v1 as acceptance).

## State at write (all pushed, remote-verified)

| Repo | HEAD | Last commits |
|---|---|---|
| Engine `feat/v5.15-live-readiness` | `fefdac1` | leaf-3 arming + exemptions; leaf-2 [STRADDLE] re-ground = `1cdc9a1` (the member list IS that commit's diffstat — `git show --stat 1cdc9a1`) |
| Workspace `main` | `5afeb31` +this close | leaves 1/1b/2/3: rc-honest client, tri-state gate/writer, Check-T park, strict-new HARD + baseline, D-137 wrapper |
| Plugin `feat/hud-robustness` | `fb8e3f3` | tri-state detector (`82aacce`) + F11 err-refusal |

## What the arc built (one paragraph)

Producer-agreement proved ≠ validity (both layout backends agreed NotifyState had no straddlers;
both wrong — the operator's HUD screenshot caught it). Now: the detector is TRI-STATE (resolved
hits always report; unresolved delta-BOUNDED → proven-clean or NAMED unverified), the written
`[STRADDLE]` fact can never print `none` for an unverifiable record, 13 cross-thread structs are
newly ARMED with 20 curated field exemptions, and the H6 gate is **HARD `--strict-new`** over a
13-key SHRINK-ONLY grandfather baseline — a NEW cross-thread straddler in either policed TU (main +
suite, BOTH HARD since the AR-8 hole-9 close; shrink-only ENFORCED, orphan-pawled; `--isolate`
rides no standing row — the stated residual) REDs the commit. Class 57
(emit-boundary honesty flattening) codifies the whole family; the pipe-swallow sub-shape got an
M7 PreToolUse hook (`tools/hooks/block_pipe_rc_read.sh`, teeth-proven, wired in settings).

## Debt-closure status (operator asked explicitly — nothing homed-in-name-only)

- Baseline's 4 engine rows → **FOLDED into the E.1.2 plan body** (§ INBOUND findings; TUISharedState ×3 + NodeContext).
- Suite trio (TrainingPanelState ×9) → **TECH_DEBT-269** (realign at the foxml_suite refactor).
- Pipe-swallow ×3 recurrence → **Class 57 sub-shape D + the hook** (M7 structural close).
- torn-cfg publication seam → E.0.10 register §.E.1-DECOMPOSITION → **E.1.3 candidate** (pre-live only; blanket block gates it).

## TaskList (recreate at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | ✅ **(g) THREAD COMPLETE** (steps 1–5; D-416 + D-417, 2026-08-10) | Step 4: reciprocal-supersession HARD + contract-stale ADVISORY (triage queue open: 4 WARN fired-triggers TD-119×2/120/126 + 58 INFO re-homes, operator-paced). Step 5: `resolve_cited_path()` lifted into citable_ids.py (tri-state; B-Plus 15-teeth oracle green; live 5-RENAMED reproduction). **NEXT = (d) the D-374 update-orchestrator, with (a) completing alongside** — (g)'s RENAMED payloads + the lifted resolver are its inputs |
| #2 | pending | OPERATOR TTY: golden bless — additions now ≈+6 (D-412/413/414/415 + Class 57 + TD-269; re-derive via `bless.py --console`) |
| #3/#4 | pending | (d) update-orchestrator · (a) contracts (LOAD-BEARING under D-415 — contracts are what make v1 simple; pull-forward stands PROPOSED in D-414) |
| #5/#6 | pending | Backlog TD-254(c)/-246/-259 · ledger id-line format split + stragglers (-140/-163/-175a/-204/-250 per c-class) |
| NEW | ✅ DONE (2026-08-10, engine `ef74caf` + ws `05f14fc`) | **Leaf 4 CLOSED**: fabricated/drifted `[CONSUMERS]` corrected by verified curation · **A2 gate live** (`--callgraph-check` ADVISORY + `--callgraph-selftest` HARD teeth; 55 tokens GREEN) · the phantom's ORIGIN closed (template placeholder leak → angle-form placeholders + templates kept under the gate) · README premise corrected. Register carries the arc scorecard |

## Capture-completeness (judgment ledger, Check-N form)

| Check | Verdict |
|---|---|
| Check 2 plan-body frontmatter | ✅ intact (plan bodies touched only by the E.1.2 §INBOUND fold; frontmatter unchanged) |
| Check 3 decision artifacts | ✅ D-413/414/415 minted, paired sentinels + nav rows + range D-97..D-415 (capture-audit Check 4 PASS, mechanical) |
| Check 5 handoff currency | ✅ THIS doc (supersedes 2026-08-09; singleton flipped; MASTER UPDATE 24 points here) |
| Check 6 M7 escalation | ✅ ONE owed and PAID — pipe-swallow ×3 recurrence → `tools/hooks/block_pipe_rc_read.sh` (teeth-proven RED/GREEN, wired) |
| Check 7 spec promotion | ✅ none eligible (Class 57 enters at first-canonical with its founding census) |
| Check 9 memory→spec | ✅ no new memories minted (decisions live in the log; the pattern lives in Class 57) |
| Check 10 going-forward rules | ✅ none new (the compounding-order rule is logged in D-414 as this backlog's standing order, not a collaboration rule) |
| Check 12 cascade | ✅ D-415 cascade complete: demotion (client/rebuild/gate) + Check-T park + TOOLS.md/README banners + register |
| **AR-8 independent review** | ⚠️ **PARTIAL — DECLARED** (silence ≠ verdict): leaf-1 fixes WERE adversarially reviewed (the consolidated a-class pass); leaves 2/3 + this doc are teeth/gate-verified (mechanical) but NOT independently reviewed — operator laptop-time pressure. Compensating control = `/accept-handoff` Stage 4–6 re-derivation + the register's re-derive fences; queue an a-class pass over leaves 2/3 diffs at next session if desired. **→ PAID 2026-08-10 (pickup session): the a-class pass RAN** — leaves STAND, ZERO false exemption reasons; 2 partial refutes (orient-tier last-line-wins ALIVE, MED-HIGH — OMS/ExecutionCore armed by line order · SHRINK-ONLY unenforced, MED) + a HARD-row scope over-claim (MED doc-truth). Register § "AR-8 COMPENSATOR PAID" carries holes 7–10 + the named one-line-scale fixes |

## WILL BITE (read before touching anything)

- `$?` after a pipeline → the NEW hook BLOCKS it (capture rc directly / PIPESTATUS). rg `-rln` → blocked (older sister hook).
- Check T is PARKED (`FOXTAG_REARM=1` to run); foxtag = frozen-kept, layout `auto` = Lua, inventory = Python-first (`FOXTAG_INVENTORY=1` opt-in).
- The strict-new baseline is SHRINK-ONLY (`tools/lib/cache_layout_baseline.txt`); re-bless only at a triage that CLOSES rows.
- Engine `--isolate` runs: clang rc=1 is STANDING dialect noise (tolerate-with-note is the calibrated design — see the register's F3 recalibration).
- The 2026-08-09 handoff's (g)-4 design section is still the binding spec for task #1 (this doc doesn't restate it).

## Re-derive at pickup

```
./tools/check_session_docs.sh                     # the full floor (incl. the NEW strict-new HARD row)
python3 tools/check_cache_layout.py --selftest    # 20 legs
bash tools/check_cache_layout_selftest.sh         # D-137 wrapper
python3 tools/check_capture_audit.py --check 14   # id corpus (bless deltas listed)
git -C tools/plugins/fox-symdeps.nvim log --oneline -3
rg -n "D/C/F: D-41[345]" plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
```
