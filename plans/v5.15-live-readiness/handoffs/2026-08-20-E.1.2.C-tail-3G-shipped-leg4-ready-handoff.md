---
type: handoff
status: active   # THE one live handoff (supersede-on-write; ≤1-active enforced by check_handoff_active_singleton.py)
ship_tag: v5.15.5.F.4d.1.E.1.2.C
plan_type: coding
supersedes: 2026-08-20-E.1.2.C-legs-0-2-3-shipped-handoff.md
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-08-20-E.1.2.C-legs-0-2-3-shipped-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
decision_ref: D-429
audit_tier: HIGH
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: the train→serve→learn ML loop verified end-to-end (E.1.2.C program, D-428); then the rest of E
coding_status: "E.1.2.C tail session SHIPPED (engine window 29a9a3a..f4bbafe + ws d16bce9..ee00034, all pushed): the deferred 3-role tables (C.3g/C.3h), the GUI polish batch, 3G-i section grouping, 3G-ii bundle picker (loader-shared matcher + resolution preview), the EnsembleHotSwap dead-code burial, PARITY-043/-044 CLOSED at the ledger, register currency pass, TD-288 minted, Landmine 21. The PREDECESSOR queue is fully DONE; the one open agent-side item is the session-minted #16/A-12 flag (NEXT ACTIONS 3). NEXT = leg 4 empirical (OPERATOR trains; runbook in the plan/protocol) → D6/D7 forks → the #16/A-12 flag → /ship close."
---

# E.1.2.C tail — 3G shipped, ledgers flipped, dead code buried; leg 4 is the only thing left

## State at write — ANCHORS, not counts

| Repo | HEAD anchor | Window this session | Remote |
|---|---|---|---|
| Engine `feat/v5.15-live-readiness` | `f4bbafe` | `29a9a3a..f4bbafe` | pushed |
| Workspace `main` | at-or-after `ee00034` + this close's commits | `d16bce9..HEAD` | pushed at this close |

Engine untracked operator files unchanged (`scripts/*.sh` edits + 4 scratch files — keep). Both corpus goldens + citable-ids operator-blessed this session (D-394 TTY ritual, twice).

## Re-derive at pickup (every number from these, none from prose)

```bash
./tools/check_session_docs.sh                      # expect SWEEP CLEAN
./build.sh test && ./build/controller_test         # expect BUILD 0 + suite green (3850/0 at write — REDERIVE)
./tools/check_determinism.sh                       # expect GREEN
python3 tools/check_identifier_retirement.py       # expect GREEN (93 at write)
./tools/calls_graph_diff.sh                        # expect CLEAN
git log 29a9a3a..HEAD --oneline                    # the engine session commits, each self-describing
git -C ../tick-trader-percore-workspace log d16bce9..HEAD --oneline
```

## The session in one paragraph

Picked up the legs-0/2/3 handoff via `/accept-handoff` (all claims re-derived green — the operator's "short close-out" held). Then the whole remaining agent-side queue was executed and shipped: the deferred pure-fn tables, the operator-reported GUI polish batch, 3G-i, 3G-ii on an explicit mid-session greenlight, the close-out bookkeeping (PARITY flips + TD-288 + FEATURE_LOOKUP entries), a register currency pass, and the EnsembleHotSwap dead-code burial. The operator's "are you sure it functions like that?" pushback on the bundle-resolution story triggered a full at-HEAD re-verification (confirmed, with cites — see D-429). Decisions logged at **D-429**; D-428's STATUS amended to current truth.

## What landed (commit per leaf; messages are the detail SSoT)

Engine (`29a9a3a..f4bbafe`): `409cda7` Training_ResolveRole→LabelFunctions.hpp move · `8fb8095` GUI polish (results-table horizon snapshot + unit-first TP/SL tooltips + 3-class collect-mh labels + the exit-predictor boot count = the leg-4 Stage-1 oracle) · `09399b2` 3G-i section grouping (SettingsSectionIndex.hpp; ImGui-free; one header per canonical section) · `f2a5059` tag blocks · `bed04f1` 3G-ii bundle picker (ModelBundleScan.hpp + `Model_ParseHorizonSibling` EXTRACTED from the loader — one matcher for boot + picker) · `753fbed` EnsembleHotSwap.hpp DELETED (H21; zero production callers, proven) · `f4bbafe` layout-tag refresh.

Workspace (`d16bce9..ee00034` + this close): `e8acc06` C.3g/C.3h tables · `03f03c8` baseline shrink · `744246a` 3G-i pins · `cb0492a` **PARITY-043 + PARITY-044 CLOSED with mechanism lines + PARITY-042 surface-update (still open) + TECH_DEBT-288 minted (the A-4 defaulted-tail cohort, 9 members named) + 4 FEATURE_LOOKUP entries** · `c4735fe`/`ee00034` operator blesses · `4f97cbf` 3G-ii pins · `18a21cd` Landmine 21 + picker FL entry · `dce4b9f` register currency dispositions + register-#13 doc-drift fixes · `19b89a3` legacy test-cell deletion (justified; Free-completeness cell survives) · `ceb22e1` index regen.

**Operator can NOW:** everything from the predecessor handoff, PLUS: pick a model family from the Settings dropdown as ONE `[ensemble · Nh · buy+exit]` entry (writes the base path; resolution preview + Verify-stamps button), and see grouped Settings sections (one header each, curated order).

## 🔴 NEXT ACTIONS (ordered)

1. **Leg 4 empirical — OPERATOR-DRIVEN, everything is ready.** Stage 1: collect once → train buy → flip side=Exit (same run_name, ≥2 horizons) → select the family in the picker → boot paper with `use_exit_model=1 exit_bandit_enabled=1`. **Pass = boot line `ensemble active (..., K exit predictors)` with K≥2.** Stage 2: `SHALT_EXIT_PREDICTED` fires → `exit_bandit_state.json` lands with `pulls>0` + non-uniform weights → restart reloads. Delete state JSONs between fresh arms (they transfer BY DESIGN). Stage 3: the A/B matrix per `plans/v5.15-live-readiness/reports/2026-08-20-ml-verification-program/bandit-loop-state.md` § "What the verification program must run" (both arms `exit_blender_mode=0`, fixed seeds). Protocol: `plans/v5.15-live-readiness/reports/2026-08-20-ml-verification-program/e2e-verification-protocol.md` (its §2a false-drift ignore-list is MOSTLY MOOT post-leg-2 — drift lines are REAL now; its Shape-A `pred:` expectations are achievable post-R1).
2. **Open forks (operator decisions):** **D6** (scaler: restore emit in the mh path vs retire the apparatus — couples with deleting the DEAD `train_model_worker_fn` + Save Run block; a `/decision-check` candidate) · **D7** (exit-bandit observability: PerNodeSnap mirror vs accept JSON forensics for this program — decide before/during Stage 2 if GUI visibility is wanted).
3. **The #16/A-12 FLAG (register dispositions block):** the leg-3 mixed-output gate item — assert the blend loop accepts a binary WILL_PEAK arm beside 3-class PVS in `exit_predictor[]` — was NOT found landed in the leg-3 commits. Verify or land before program close.
4. **`/ship` close when the operator calls the program done:** version bump (Version.hpp still `.E.1.1` — deliberate, per D-429 (7)) + CHANGELOG row + tag + `/post-ship-audit` + the register-#12 stale-comment totality check.
5. **Then the rest of E** per the E.1.2 close-gate + queue.

## Arming for the next action (M8)

- **Read first:** D-429 (the session's decision record) + the plan body's `§ Register dispositions` block + this handoff.
- **Skills:** `/decision-check` for D6 when it opens · `/ml-audit` if leg-4 findings reshape anything · `/post-ship-audit` + `/ship` at close.
- **Tools to RUN:** the re-derive block above; `check_cache_layout.py --tu foxml_suite.cpp --fix` after ANY GUI-state struct change (bit us twice this session; the gate is strict-new on BOTH TUs).
- **Anti-patterns in play:** Class 51 (pins drive REAL fns — the reason the legacy cells died rather than survived their subject) · Class 18 (the shared-matcher extraction is the pre-empt; do not mint parallel matchers) · AR-19 (trace to every reader — the ModelValidation CALLERS list had drifted to 3-of-4 wrong) · Class 57 ($?-after-pipeline; hook-enforced, fired twice this session).
- **Invariants:** H21 (two burns live: `exit_signal_model_dir` + the EnsembleHotSwap deletion) · H1 (fixed arrays in the new GUI-plane structs) · H4 · H22.
- **WILL BITE:** `tests/`+`tools/` are workspace DIR symlinks but `DOCS/` is per-FILE symlinks — engine-side `DOCS/tech-debt/...` paths DO NOT EXIST (Landmine 21; use workspace absolute paths) · the tests TU only compiles from the build dir (g++ realpath-resolves the symlink; Landmine 21) · a NEW/DELETED engine header = corpus-pin drift = an operator TTY bless (D-394; deliberate friction) · ImGui render fns: no blocking I/O (scan/verify are click-gated — keep it that way).

## TaskList (recreate at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | pending (operator) | Leg 4 Stage 1: train buy+exit family → picker → boot; pass = K≥2 exit predictors on the boot line |
| #2 | pending (operator) | Leg 4 Stage 2 loop-closure (paper) → Stage 3 A/B matrix |
| #3 | pending (operator fork) | D6 scaler restore-vs-retire (+ dead train_model_worker_fn/Save-Run deletion, coupled) |
| #4 | pending (operator fork) | D7 exit-bandit observability (PerNodeSnap mirror vs JSON forensics) |
| #5 | pending | #16/A-12 flag: land-or-verify the mixed-output blend gate item |
| #6 | pending | /ship close: version bump + CHANGELOG + tag + /post-ship-audit + register-#12 totality check |

## Capture-completeness (Stage 6.5.2 — the EIGHT judgment checks, explicit)

- **Check 2 (plan frontmatter):** VERIFIED — the E.1.2.C plan carries `audit_tier: HIGH` + `decision_log:` + `sister_specs:`; no new plans this session.
- **Check 3 (decision-log artifact):** VERIFIED — D-429 written at the expected path; D-428 STATUS amended in place.
- **Check 5 (handoff currency):** THIS document supersedes `2026-08-20-E.1.2.C-legs-0-2-3-shipped-handoff.md` (flipped `superseded` at this close); its PENDING list is fully discharged except the items re-listed under NEXT ACTIONS above (leg 4, D6/D7, /ship-close items) — none stale, all deliberate carries.
- **Check 6 (M7 escalation candidates):** NONE new — the session's tool friction (rg `-r` typo, `$?`-pipeline) was caught by EXISTING hooks (the enforcement working, not a gap); no recurring-despite-memory class surfaced.
- **Check 7 (DESIGN_SPECS promotion):** NONE — no spec's stage moved this session; no new spec warranted (3G's patterns are single-consumer GUI-plane; codify only if a second consumer appears).
- **Check 9 (memory→spec sisters):** NONE this session — zero new memory files, zero new skills; captures went to D-429, the ledgers (TD-288, PARITY flips), FEATURE_LOOKUP, LANDMINES, and the plan register block.
- **Check 10 (going-forward rules):** NONE new — no operator correction rose to a durable rule (session directives were ephemeral logistics: hold-push, keep-working-an-hour).
- **Check 12 (amendment cascade):** VERIFIED — D-428's STATUS amendment cascaded here + MASTER UPDATE 35; the register dispositions cascaded to PARITY_ISSUES (flips) + FEATURE_LOOKUP (fixes) + CLAUDE_ML_INVARIANTS (contract table) in `dce4b9f`/`cb0492a`; sweep CLEAN as the LAST pre-sync act (the mid-close half-flip the AR-8 review caught — `superseded_by:` written as a comment, not the key — was FIXED before this shipped).
- **Close-out enforcer (`check_close_out_completeness.py --since d16bce9`):** its owed surfaces were WRITTEN at this close, not waived — Class-51 + Class-58 known-instance rows (the EnsembleHotSwap cells + the two false include-comments), the retirement-discipline spec's Application paragraph, and the workspace-TRACKED `DOCS/CODE_MAP.md` mirror re-synced to the `f4bbafe` regen (the `1a4ec47`-precedent step; the engine-side copy is the ONE regular file among the DOCS symlinks — Landmine 21 records the exception). Two MED advisories PREDATE the session (memory frontmatter canonicalization + backup drift ×3, mtimes ≤ Aug 19) — `/sync-workspace`'s migrate step canonicalizes them at this push.

**Volatile-count discipline:** all quantities above are anchored to SHAs/IDs; suite count appears only inside the re-derive fence. VOLATILE-OK: the "3850/0 at write" annotation inside the fence is the sanctioned form.

## Independent-review record (Stage 5.5 + 6.5.4)

- Deliverable-completeness review (independent, no-stake): **PARTIAL → fixed-at-close.** 7/8 items VERIFIED (no clobbers, no privacy leaks, no unlogged decisions); the one gap — the workspace-TRACKED CODE_MAP mirror two sessions stale, still listing the deleted fn — was FIXED before the sync commit. <!-- REVIEW-VERDICT: PARTIAL, gap fixed -->
- Adversarial handoff review (a-class, default-REFUTED, AR-8): **REFUTED-WITH-FINDINGS → all 8 triaged + fixed at close.** HIGH F1 (supersession half-flip: `superseded_by:` key missing — the sweep's reciprocal gate REDDED) fixed + full sweep re-run as the last act; F2 (this section originally showed only green) fixed above; F3 (a third false EnsembleHotSwap comment at `Run.hpp:~1876`, missed by the burial's own sweep) fixed engine-side; F4 (the plan's `status:` line still read "pre-coding gate IN FLIGHT") fixed; F5 (D-429 (8) under-listed the surfaced set by one) amended; F6 (the CODE_MAP regular-file exception) recorded in Landmine 21; F7 (reports paths lacked the sprint prefix) fixed; F8 (coding_status over-claim) fixed. Its "Survives" list independently re-measured the suite (3850/0), the windows, the matcher's two consumers, the boot oracle, and the ledger flips. <!-- AR8-VERDICT: REFUTED-WITH-FINDINGS, all fixed -->

## memories/skills → index

NONE this session — no new memory files, no new skills (verified, not assumed: zero writes under `~/.claude/.../memory/` this session; the capture surface was the decision log + ledgers + FEATURE_LOOKUP + LANDMINES + the plan register).
