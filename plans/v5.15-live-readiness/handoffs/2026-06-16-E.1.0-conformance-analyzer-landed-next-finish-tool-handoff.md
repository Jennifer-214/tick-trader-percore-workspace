---
type: handoff
status: superseded   # SUPERSEDED 2026-06-16 (s4) by 2026-06-16-E.1.0-conformance-analyzer-FINISHED-next-shipclose-codifications-handoff.md — its "finish the tool (4 items)" is DONE (full foundation: 7-kernel manifest + source-allow/libm exemptions + transitive scan + CI-wire + baseline + 5-pass verify). Same work-line (supersede, not defer).
ship_tag: ".E.1.0 — H8 RESHAPED: a static latency-path CONFORMANCE ANALYZER (instruction-budget, not wall-clock-ns). First cut LANDED + dogfooded + wired. NEXT = finish the tool (4 items) → 2 DESIGN_SPECs → V-class → ship E.1.0."
plan_type: live-readiness
sprint_end_goal: make the codebase maintainable + live-ready (v5.X professionalization); live gated behind the whole E-series
ship_end_goal: ".E.1.0 stands up the §4d guard NET. H14/H10 ENFORCED (prior sessions). This session: a hardened I→A re-pass RESHAPED H8 from a runtime latency bench → a STATIC ASM instruction-budget conformance analyzer (deterministic, trackable, zero engine behavior) — first cut landed/dogfooded/wired. REMAINING: finish the tool (slow-path callee-symbol + grandfathered baseline + CI-wire + non-float teeth) → the 2 DESIGN_SPECs + Class-51 → baseline → V-class → ship."
predecessor_handoff: handoffs/2026-06-16-E.1.0-foundation-hardened-next-H8-bench-handoff.md (SUPERSEDED — its "build the H8 bench" is DONE+RESHAPED per the hardened I→A re-pass)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (latest D-236; this session added D-232..236)
plan_body: plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.0-guard-hole-closure.md (read § "HARDENED I→A RE-PASS (2026-06-16 s3)" + the RESOLVED banner — that's the CURRENT H8 shape; the earlier FULL CODING BODY's H8/Phase-D framing is SUPERSEDED-for-H8, see currency note below)
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
dependency_graph: plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, the E.1.0 plan body § "HARDENED I→A RE-PASS (s3)", decision log D-232→236, tools/check_latency_path_conformance.py (the deliverable), the .E dependency-graph DAG, regen CODE_MAP at pickup]
engine_head: 226a652 (the RollingStats size-pins — `fix(.E.1.0)`; from a PRIOR session; NOT pushed; working tree CLEAN). This session added NO engine commits — the conformance analyzer lives in tools/ (gitignored-in-place, workspace-backed). Predecessor baseline = .E.0.10 tag 572c978.
workspace_head: at-or-after the /sync-workspace at this session's close (9 doc/plan edits + the 2 new tool files: check_latency_path_conformance.py + _selftest.sh)
coding_status: mid-coding-checkpoint — the H8 conformance-analyzer FIRST CUT landed (apparatus, in tools/); the tool has 4 finish-items + 2 DESIGN_SPECs before E.1.0 ships
---

# Handoff — `.E.1.0` conformance-analyzer first cut LANDED → next = finish the tool

## TL;DR (the 60-second pickup)

- **H8 got RESHAPED this session.** A hardened 3-I→3-A re-pass (operator-directed; task #14) on the converged "A⁺" H8-bench design **found 3 things the prior 3:3 missed** — substrate vacuity, a missing counter, a degrade-mask gap — and (via operator steering) reshaped H8 from a **runtime latency bench** into a **STATIC ASM instruction-budget CONFORMANCE ANALYZER**. Instruction-count budgets replace wall-clock-ns: deterministic, diffable, no run, no box-pinning, **zero engine behavior** (so E.1.0 stays a true enforcement-scaffold). Decisions: **D-232** (the re-pass) · **D-233** (static-tool fork-resolution) · **D-234** (the conformance-analyzer extension) · **D-235** (zero-data-dependent-branch E-goal) · **D-236** (curation/reduction → the optimization leaf, NOT E.1.0).
- **The tool's first cut LANDED + dogfooded + is WIRED.** `tools/check_latency_path_conformance.py` (sister to `check_struct_size_budget.py`) disassembles the PRODUCTION hot/slow paths + gates instruction-count + **branch-classification {loop / rare-cold / data-dependent-warm = the H7/H20 meter}** + no-float(H4)/div/malloc/indirect/spill, and **asserts its own non-vacuity** (Class-51 self-defense — it proved itself by refusing RollingStats's optimized-to-2-instr wrapper). Verified by an independent review (CLEAN). Wired into TOOLS.md + `/hft-audit` + `/dod-audit` + CoreFrameworks/CLAUDE.md + `latency-path-discipline.md`.
- **NEXT = finish the tool** (3 assembly items — the riskiest part, always-inline-template disassembly, is PROVEN): (1) slow-path callee-symbol follow; (2) set the grandfathered-25 baseline (`--update-budgets`); (3) wire to pre-commit/CI. *(Item 4 — the per-detector teeth + variant-completeness — LANDED this session; the teeth caught + fixed 2 vacuity bugs incl. a Class-51 hole in the tool's own forbidden-call detector. See Finish-items.)* Then the 2 DESIGN_SPECs + Class-51, baseline (#4), V-class (#5), ship.

## The ideal way to start

1. **`/accept-handoff`** (resolves here). Regen CODE_MAP; load baseline + nav-infra + the decision log D-232→236 + the plan body § "HARDENED I→A RE-PASS (s3)".
2. **RUN the tool to see where it stands:** `cd /home/caramel/code/FoxML_Trader_v2 && python3 tools/check_latency_path_conformance.py 2>&1 | head -40` — you'll see the ExecutionCore_Tick breakdown (instr=2097, branches loop=22/rare-cold=15/data-dependent=25, H4 float=0) + the RollingStats NON-VACUITY FAIL (the intended teeth — that's finish-item #1). `--selftest` confirms the H4 teeth pass.
3. **Read the CURRENT H8 shape:** plan body § "HARDENED I→A RE-PASS (2026-06-16 s3)" + the `⮑ RESOLVED (D-233)` banner + D-234/235/236. **Ignore the plan body's earlier FULL CODING BODY Phase-D "latency ratchet" framing for H8** — it's the pre-reshape approach (see currency note).
4. **Finish the tool** (the 4 finish-items below), then the codifications.

## What landed this session

- **The hardened I→A re-pass (D-232)** — 3-I→3-A, M8-armed, every claim AR-11 orchestrator-verified. It RE-CONFIRMED the core mechanics but RE-OPENED: 🔴 substrate (the single-threaded backtest is vacuous AND blind to false-sharing; the threaded synthetic run isn't CI-reproducible) → operator chose the static-instruction axis; 🟡 the `ml_inference_ran` counter doesn't exist + the degrade mask misses `CFG_DRIFT_STRICT_REFUSED` (both → the optimization leaf, D-236).
- **The fork resolution (D-233/234):** H8 = a static ASM **conformance analyzer** (not a runtime bench). FORK B dissolved (zero engine behavior). The branch-count check **subsumes the H7/H20 grep** (task #2 — ASM branches are ground truth where the grep was undecidable).
- **The tool** — `tools/check_latency_path_conformance.py` + `_selftest.sh`. Built, dogfooded (the dogfood CAUGHT a real AVX/FMA float-detector hole — the selftest teeth fired; fixed), independently reviewed CLEAN.
- **The E-goal (D-235):** drive data-dependent-warm branches → 0 (H20/H11); the tool's classifier is the progress METER. **Scope (D-236):** the curation (which branches are warmup-100%-predicted + why) + the reduction (bandit-touching) = the **optimization leaf**, NOT E.1.0. E.1.0 grandfathers the current 25 as a no-new-regression baseline.
- **Wiring:** TOOLS.md (both rows) · `/hft-audit` (codegen step) · `/dod-audit` (H11/branchless/derived-fact) · CoreFrameworks/CLAUDE.md tools-slice · `latency-path-discipline.md` (`-S`-inspection checklist). `/latency-track` was already a `sister_skill` of `/hft-audit` + indexed (no edit needed).

## TaskList state at handoff write (recreate verbatim at pickup)

| # | Task | Status | Note |
|---|---|---|---|
| 1 | **H8 static latency-path CONFORMANCE ANALYZER (hot+slow)** | in_progress | first cut LANDED; 4 finish-items below |
| 2 | H7/H20 branchless gate | pending | **SUBSUMED by #1's branch-count** (keep a source grep only as an optional locator) |
| 3 | grep-CI aggregator at ≥2 gates | pending | blocked by #2 |
| 4 | baseline capture → HOT_PATH_CHANGELOG | pending | RESHAPED: the baseline IS the tool's budget sidecar (`--update-budgets`), not a wall-clock entry; add a 1-line HOT_PATH_CHANGELOG pointer |
| 5 | V-class verify | pending | 5 binaries + 3635/0 + calls_graph CLEAN + check_determinism (the engine is untouched; verify the tool runs) |
| 6 | ship-close codifications | pending | the 2 DESIGN_SPECs (new conformance pattern + amend mechanical-verification) + the seam-folds |
| 7 | TECH_DEBT-204 — .githooks not mirrored by /sync-workspace | pending (deferrable apparatus) | symlink engine .githooks → workspace |
| 8 | cascade-check (H8 across E.1.1/E.1.2) | ✅ done | |
| 9 | slow-path optimization homing | ✅ done (CORRECTED) | own .E leaf (compute+cache), NOT E.1.2 |
| 10 | size+cache guard tool | ✅ done | check_struct_size_budget.py (⚠️ still manual/unwired — see "tool usage" gap) |
| 11 | umbrella sweep finish | pending | the other ~15-20 stale size comments + the 6 preventive pins + **wire check_struct_size_budget.py to pre-commit** |
| 12 | subagent registry + custom agent types | pending | |
| 13 | codify Class-51 (first canonical) + cascade-bridge | pending | the analyzer's non-vacuity self-defense is now Class-51's 5th worked instance |
| 14 | RE-RUN the I→A sweep with hardened tools/agents | ✅ done | it PAID OFF (found the 3 the prior 3:3 missed) |
| 15 | **[HIGH] trade-branch → branchless (H20)** | pending | LOCKED on principle; behind the H8 meter that verifies it |
| 16 | harden close-session capture process | ✅ done | |

## Finish-items for the tool (the immediate next work; all assembly — the hard part is proven)

1. **Slow-path callee-symbol follow.** `RollingStats_Push` is `inline` (not `always_inline`) → it stays out-of-line, so the noinline probe-wrapper inlines to ~2 instrs → the non-vacuity guard (correctly) FAILS it. Fix: when the target fn isn't inlined into the wrapper, find + analyze its OWN out-of-line symbol (objdump the callee symbol, not the wrapper). The hot path (always-inline) analyzes fine via the wrapper.
2. **Set the grandfathered-25 baseline** via `--update-budgets` (writes `tools/lib/latency_path_budgets.json`) → the ratchet becomes "no NEW instructions/branches beyond today." The HARD invariant checks (float/div/malloc/indirect) gate NOW regardless.
3. **Wire to pre-commit/CI** (a new Check letter; chain A–G + M/L taken — pick the next free) + add to `run_all_tests.sh`. **Note (the "are we using all the tools" gap):** the tool is report-only/manual today; so is `check_struct_size_budget.py` (built last session, never wired — owed by #11). Both should get wired — a manual tool is a hole, not a guard.
4. **✅ DONE (this session) — teeth + variant-completeness** (then re-verified, see note). All 6 detector teeth fire (`--selftest`: float/div/forbidden-call/indirect/branch/non-vacuity); the teeth caught + fixed the **forbidden-call-blind-to-external-syms** Class-51 hole (the analyzer's own forbidden-call detector couldn't see `call malloc` in an unlinked `.o` — fixed via `objdump -r` + reloc-parsing; the **headline Class-51 worked example** for #6/#13). `spill` = labeled heuristic-advisory.

   **⚠️ Post-#4 INDEPENDENT RE-VERIFY (the operator's repeated "are you sure" → spawned a no-stake reviewer; model-bounded-self-check escalation) caught + FIXED 2 *correctness* bugs:** (a) the indirect detector I'd added for `jmp *` tail-calls **false-flagged GCC `switch` jump-tables** (they dispatch via an *unindexed* `jmp *%rax` — index in a prior `movslq`) → **reverted to `call *` only** (excl. indexed fn-ptr-table calls); empirically proven (switch→indirect=0, `call *`→indirect=1). (b) `--update-budgets` **silently dropped a skipped manifest row** from the baseline → now WARNS (incomplete-baseline guard). + `FORBIDDEN_CALL` += `_Znwj/_Znaj`. **3 NEW homed finish-items** from the re-verify: the **4 remaining D-233 manifest rows** (`BG_Evaluate`/`SG_Evaluate` inlined into `ExecutionCore_Tick` = covered transitively; `EventLoop_RebuildOneCore`/`Regime_Classify` need probes) · indirect-**tail-call** (`jmp *`) detection via a look-back · the `--selftest` exit-code hardening.

   **⟹ remaining tool finish = items 1–3 above (slow-path callee-symbol + grandfathered baseline + CI-wire) + the 3 re-verify finish-items.**

## Plan-body currency note (read this before trusting the plan body top-to-bottom)

The E.1.0 plan body's **FULL CODING BODY (Phase A.0–E)** describes the ORIGINAL approach (grep-CI + a `LATENCY_BENCH` ratchet). Its **H14/H10 parts LANDED** (real history — those gates shipped). Its **H8 part (Phase D — the latency ratchet) is SUPERSEDED** by the conformance-analyzer (D-234) + the "3:3 GATE VERDICT" + the "HARDENED I→A RE-PASS (s3)" sections. So: **read the s3 sections + D-234/235/236 as the current H8 shape; the Phase-D latency-ratchet framing is pre-reshape.** (Not blanket-superseded — only the H8 framing reshaped.)

## Arming for the next action

For **finishing the tool** — the surface is a CI apparatus over the hot/slow ASM, so arm it:
- **Route to:** `/hft-audit` + `/dod-audit` (they now point AT the tool — use them to sanity the checks) + the tool's own dogfood (run it, read the breakdown). It's apparatus → **build + dogfood-iterate + teeth-proof, NOT a 3:3** (`feedback_process_weight_by_surface_blast_radius`).
- **Run (not just read):** `python3 tools/check_latency_path_conformance.py` (+ `--selftest`, `--update-budgets`, `--asm`); `tools/calls_graph_diff.sh`; `tools/run_all_tests.sh` (V-class); regen `./tools/gen_code_map.sh`.
- **Read first:** `latency-path-discipline.md` (the parent doc — its `-S` checklist is what the tool mechanizes) + `STRATEGY_AND_CODING_RULES.md` §4/§5/H4/H7 + the tool's own docstring. Invariants in play: **H4, H7/H20, H11, H2, H8** (the meter).
- **Sister tool to mirror:** `check_struct_size_budget.py` (same manifest + compile-probe + `--selftest` shape; the 2nd instance of the derived-fact-budget framework).

## Capture-completeness (this session — nothing lost; independently reviewed CLEAN)

- **Decisions → log:** D-232 (hardened I→A re-pass) · D-233 (static-tool fork-resolution) · D-234 (conformance-analyzer extension) · D-235 (zero-data-dependent-branch goal) · D-236 (curation/reduction scope → optimization leaf). All paired sentinels; verified by the independent deliverable review + `check_session_docs.sh` (decision-log-completeness ✅).
- **Findings → ledger:** NO new TECH_DEBT/PARITY this session — all findings homed to the decision log (D-232..236) + the E.1.0 plan body (the HARDENED I→A RE-PASS section) + the E.1.2 scaffold (the conditional INBOUND fold). The "2 budget tools unwired" gap → finish-item #3 + #11 (homed). The optimization leaf (curation + reduction + bandit overlap A11/A12/TD-174) → D-235/D-236 (homed; scaffolds when its turn comes).
- **Memories/skills → indexed:** NO new memories (the patterns home to the pending DESIGN_SPECs, not memories). The tool ENROLLED in TOOLS.md (both rows) + WIRED into `/hft-audit` + `/dod-audit` + CoreFrameworks/CLAUDE.md + `latency-path-discipline.md`. The 2 DESIGN_SPECs (new conformance pattern + the mechanical-verification amendment) + Class-51 = ship-close codifications (#6/#13, tracked — per pattern-codification-lifecycle, NOT front-loaded mid-build).
- **Verified:** `check_session_docs.sh` SWEEP CLEAN (all 12 HARD + 4 ADV) · independent deliverable-completeness review CLEAN (all 8 items VERIFIED; cross-coherence confirmed — every doc-claimed check has real code; the tool runs + teeth pass) · the tool's non-vacuity guard proved itself on RollingStats.

## Nav-infra (first-class pickup reads)

- **CODE_MAP** (regen `./tools/gen_code_map.sh` at pickup) — the anti-fabrication file:line ground truth.
- **The .E dependency-graph DAG** (`subplans/2026-05-28-…E-dependency-graph.md`) — E.1.0 → E.1.1 → E.1.2 …; surface-ownership.
- **The finding-disposition register** (`plan_checks/E.0.10-finding-disposition-register.md`).
- **TOOLS.md** — the tool inventory (the analyzer is enrolled at the size-tool sisters).
- **Per-function impact:** `/dependency-chain-trace` + `/trace-deps` before editing a hot/slow fn.

## Operator collaboration reminders

- Address **Caramel as Caramel / she / her**. No AskUserQuestion modals — inline options.
- Evaluate on **robustness + latency + design philosophy, NOT time**.
- **Consult-before-coding** for any engine behavior change (the tool itself is apparatus — build + dogfood; but the #15 trade-branch fix + any hot-path edit ride the audit gate).
- The tool is APPARATUS → **build + dogfood-iterate**, not a heavy pre-gate (`feedback_process_weight_by_surface_blast_radius`).
- **Invoke composable skills, don't hand-execute** (`feedback_invoke_composable_skills_dont_hand_execute` — codified this sprint as #16).
