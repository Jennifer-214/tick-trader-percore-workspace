---
status: superseded
ship: E.1.2.A
descriptor: tag-layer-built-pilot-converted
engine_head: d4812de
workspace_head: 50ece69
plugin_head: e06e5d6
branch: feat/v5.15-live-readiness
plan: plans/v5.15-live-readiness/subplans/2026-07-05-E.1.2.A-comment-system-and-doc-consolidation.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
date: 2026-07-06
supersedes: 2026-07-05-E.1.2.A-schema-locked-validator-built-handoff.md
audit_tier: LOW
superseded_by: 2026-07-06-tag-system-full-design-locked-schema-v2-core-handoff.md   # back-ref added 2026-08-10 at the (g)-4 reciprocal-supersession contract adoption (metadata repair only; body untouched)
---

# E.1.2.A handoff — tag-layer BUILT · pilot CONVERTED · format APPROVED · go-forward = mechanical

## TL;DR — where we are

**The DESIGN phase of E.1.2.A is DONE.** The in-code documentation tag-system is designed, proven on a real file, codified, and operator-**APPROVED**. The `fox-symdeps.nvim` plugin tag-layer is BUILT + committed. What remains is **rote mechanical conversion** (P3) + the ecosystem build (tools · docs · doc-viewer) — the operator's framing: *".A is the bounded standardization subplan; the hard part (design) is done, the rest is mechanical."*

**This is a clean bank point after an enormous milestone session.** Nothing is half-done or at risk — everything below is committed.

## State (all committed + pushed)

| Repo | HEAD | Branch | Content |
|---|---|---|---|
| engine | `d4812de` | feat/v5.15-live-readiness | pilot conversion (RegimeDetector.hpp 6 units + StrategyInterface FOREACH_STRATEGY); `5fd3b58` = operator's ExecutionCore build-time-sizing note |
| workspace | `50ece69` (+ this close's D-330/end-goal/MASTER commit) | main | schema · decision-log · plan · SUBAGENT_ARMING · tools |
| plugin (fox-symdeps.nvim) | `e06e5d6` | feat/hud-robustness | the tag-layer (5 new modules + wirings) |
| Linux_Theme | `5627385` | fix/chrony-fallback-nvim-0.12 | nvim template: auto-install the tag adapter |

## What landed this session

**Tooling (workspace, committed):** `check_code_tag_blocks.py` `[REFERENCE]`-resolution (frozen WORKSPACE-rooted resolvers) · **cache-layout CI gate `check_cache_layout.py` (D-320)** — detect/gate/refresh(`--fix`)/trace, context-aware via `[THREAD]`, NEVER auto-align · `emit_record_layout.lua` (headless struct-layout emitter, reuses the plugin's recordlayout SSoT) · advisory-wired into `check_session_docs.sh`.

**Plugin tag-layer (fox-symdeps.nvim `e06e5d6` — the `tagadapter.install` seam LIT):** `tag_grammar_adapter` (parse/format_derived/verify) · `tagwriter` (write-in-place, MERGES — keeps cache-gate layout, appends call-graph) · `tagcontext` (resolve the unit from the enclosing block — cursor ANYWHERE) · `actions`+`menu` (the `<leader>dm` / HUD-`m` **context action palette** — keyboard-first, hjkl nav, tag-`[TYPE]`-filtered, peach selection bar) · `:FoxSymdepsDerived[!]` (preview / write+autosave). Config auto-installs it (live `~/.config/nvim/init.lua` + Linux_Theme template).

**Schema (proven on the pilot + committed):** **D-322** vocab-reconcile (18 rows) · **D-326** code-local comments STAY in `[CODE]` (struct fields · fn step-comments · sub-group headers; body WRAPPED not rewritten; **fixed 2 leaked "comments moved OUT" framings**) · **D-327** `[DERIVED]` WRITTEN-stable vs LIVE-PREVIEW-volatile · **D-328** tag-`[TYPE]` action palette · **D-329** `[SECTION]_[label]` **bar-wrapped** within-fn markers (`----` bars = load-bearing plugin begin/end → within-fn TOC) · **D-330** the go-forward → **custom-IDE end state** (now the plan's stated End goal).

**PILOT: `RegimeDetector.hpp` (engine `d4812de`) — 6 units converted, operator-APPROVED:** RegimeSignals · CumDeltaState · CumDelta_Init · Regime_ComputeSignals (8 bar-`[SECTION]` phases) · RegimeState · Regime_Init. Code **BYTE-IDENTICAL** (comments-only — independently verified: empty comment-stripped diff), validates clean, `--fix`'d struct layouts.

## NEXT ACTION (start here)

**1. Finish `RegimeDetector.hpp`** — the remaining units are mechanical wraps (same pattern, byte-identical, `--fix` the structs, menu-fill fn call-graph on demand):
`REGIME STATE PERSISTENCE DELEGATE` (the `FOREACH_REGIME_PERSIST_FIELD` registry + AUTOPOPULATE macros + count-lock → a `[REGISTRY]` block) · `CumDelta_Push` · `TickRateState` + `TickRate_Init/Push/CurrentZ` · `Regime_Classify` (big fn → `[SECTION]` its phases) · `Regime_ToStrategy` (trivial) · `Regime_AdjustPositions` (big fn).
Then validate clean + `git diff` byte-identical check + commit.

**2-4. The go-forward workstreams (D-330):** full P3 codebase conversion (me, unit-by-unit, no subagents until the wrap proves fully rote) → **tools-alongside** (build the MISSING code-tag index; run cache-gate + DERIVED generators over the corpus; CI-gate) → **plugin + workspace docs aligned** (close the D-325 propagation backlog) → the **pop-out doc-viewer capstone** (`[REFERENCE]` already resolves → the plugin floats the referenced doc alongside the code = the custom IDE).

## Critical pickup-time reads
- `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md` — the format SSoT (D-326/327/329 + the `[SECTION]` bar-wrap + the fn/struct/registry templates)
- decision-log `v5.15.5.F.4d.1.E-architecture-v2.md` — **D-318..D-330** (this session's arc)
- the umbrella plan (frontmatter above) — § End goal (the custom-IDE vision) + § P2/P3
- `Strategies/RegimeDetector.hpp` @ `d4812de` — the reference pilot (copy the pattern)
- `DOCS/SUBAGENT_ARMING.md` § 2.6 — the tag-grammar an agent must honor

## Deferred (homed, NOT this ship)
- **TD-232** — ExecutionCore 68KB / L1d hot-working-set residency + a `[HOT_WORKING_SET]` tag axis
- **TD-233** (HIGH) — node core-topology / 3rd mediator core / producer-drainer scaling
- **TD-234** (HIGH) — adversarial burst-overload resilience (DEFENSIVE framing only; quote-stuffing offensively = market manipulation, out of scope)
- **B-Plus basename false-positive** — recurring; worth a tool-pass to teach it subdir resolution (it flags every decision-log edit; advisory, never blocks)
- `[REFERENCE]_[AUDIT]` — existence-unchecked until an audit index exists

## Capture-completeness + TaskList (close verification)

**Decisions captured:** D-318..D-330 (decision-log) — D-320 cache-gate · D-321 toolchain-split · D-322 vocab (18 rows) · D-323/324 architecture threads · D-325 propagation · D-326 code-local-stays · D-327 DERIVED written/preview · D-328 action palette · D-329 `[SECTION]` bars · D-330 custom-IDE go-forward.
**Ledgers:** TECH_DEBT-231..234 (`DOCS/tech-debt/open.md`). **Memories:** none new this session (all captures were decisions/schema). **Codifications:** schema (D-326/327/329) · SUBAGENT_ARMING § 2.6 · the plan End goal.
**Independent close-out review:** VERIFIED 5/5 (byte-identical pilot; the "comments moved OUT" contradiction fully resolved; no stubs).
**TaskList snapshot** (DESIGN phase DONE; go-forward = mechanical P3 + ecosystem — see NEXT ACTION):

| # | status | task |
|---|---|---|
| 1 | in_progress | E.1.2.A umbrella — in-code documentation system + workspace consolidation |
| 2 | in_progress | P1 — grammar CI validator (built; increments continue) |
| 3 | pending | P1 — scaffold generator + golden fixture (golden done; generator pending) |
| 4 | in_progress | P2 — pilot conversion (6 units done; 7 mechanical wraps remaining) |
| 5 | in_progress | #14 — unified fact-grammar propagation (code-tag index MISSING) |
| 6 | in_progress | [REFERENCE]-resolution → first conversion → completeness detector |
| 7 | completed | Cache-layout CI gate (D-320) |
**PENDING (next session):** finish `RegimeDetector.hpp` → full P3 conversion → tools-alongside (build the code-tag index) → plugin+docs alignment → the pop-out doc-viewer capstone.

## Pickup command
```
/accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-06-E.1.2.A-tag-layer-built-pilot-converted-handoff.md
```
