---
type: subplan-scaffold
ship_tag: v5.15.5.F.4d.1.E.1.2
fable_fit: GATE-ONLY - run the 5-agent /precoding-audit-gate on Fable (parallel-sub-agent orchestration is its other peak); the relayout CODE itself is settled-design + Opus-fine - do NOT spend the window on the mechanics. (2026-07-02 Fable-window triage)
ship_kind: data-relayout (SoA + cluster-container shape) + snapshot VERSION bump
parent: 2026-05-28-v5.15.5.F.4d.1.E.1-foundation.md  # sub-master (umbrella)
predecessor: 2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md
successor: 2026-06-15-v5.15.5.F.4d.1.E.1.3-aggregator-coherence.md  # SPINE-2 (capital cluster) — NOT in this scaffold batch
status: reformalized-coding-plan (pre-audit-gate; re-grounded HEAD b10e778 2026-07-02; supersedes the 2026-06-15 scaffold body — retained below as design-context + audit trail)
audit_tier: HIGH  # determinism-gated wire/layout freeze on capital-persistence + a major dead-serializer deletion → 5-agent /precoding-audit-gate + /blindspot-scan B14
plan_version: v1.0-reformalized (2026-07-02)
audit_reports_informing:
  - plan_checks/2026-06-30-E.1.2-per-field-declaration-table.md
  - plan_checks/2026-06-30-E-umbrella-hotpath-forward-audit-synthesis.md
established: 2026-06-15
spine: FOUNDATION (no spine) — the LAST foundation leaf; the struct goes FINAL here
risk: HIGH  # bytes change → snapshot VERSION bump + determinism re-cert; the layout every spine ship reads is frozen HERE (re-touch is expensive)
hot_path: TOUCHED (SoA relayout of Position/Portfolio + new alignas(64) NodeState clusters; H10 first real SoA AVX kernel; E.1.0's determinism gate guards byte-identity)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
dependency_graph: plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md
guard_matrix: plans/v5.15-live-readiness/E-guard-coverage-matrix.md  # §4a H6/H10/H12 + H21
rollback_anchor: pre-v5.15.5.F.4d.1.E.1.2
sister_specs:
  - meta-disciplines/plan-hierarchy-and-sub-master-decomposition.md
  - data-disciplines/per-node-position-ownership-model.md  # owner_node_id (D-209 venue-net foundation) + the K=1 slot==node model + Future-expansion sub-pool
  - data-disciplines/cache-line-discipline.md  # alignas(64) cluster-by-access-pattern (H6)
  - meta-disciplines/struct-change-cascade-impact-tooling.md  # D-202 — the cascade check FIRST, mandatory for layout change
  - wire-format-patterns/wire-format-byte-preservation-discipline.md  # snapshot bytes + H12 explicit padding
  - meta-disciplines/dead-code-and-identifier-retirement-discipline.md  # H21 — snapshot VERSION tombstone (persisted-wire identifier)
---

# E.1.2 — NodeState SoA/`Money` relayout + ClusterState CONTAINER shape + snapshot VERSION bump

**Status:** REFORMALIZED coding plan (2026-07-02, re-grounded at engine HEAD `b10e778`). Supersedes the 2026-06-15 SCAFFOLD body (retained below from "## Scope" onward as the design-context layer; the old "## Design-shape (…572c978…)" section is marked SUPERSEDED). The LAST FOUNDATION leaf — **the struct goes FINAL here; every capital-cluster spine ship (E.1.3+) reads/writes this frozen layout, no re-touch.** Open it ONCE, correctly (avionics-grade).

> **⏩ REFORMALIZE banner (2026-07-02, Opus session — accept-handoff pickup of `handoffs/2026-07-01-E.1.2-reformalize-handoff.md`).** Built from the per-field declaration table (`plan_checks/2026-06-30-E.1.2-per-field-declaration-table.md`) + the CONVERGENT AMENDED FREEZE PLAN (`plan_checks/2026-06-30-E-umbrella-hotpath-forward-audit-synthesis.md` §CONVERGENT + §A-CLASS ADVERSARIAL VERDICTS) + decisions D-283/D-288/D-289 + the H22 / decision-time-binding codification (D-287). Cascade tools re-run at HEAD: `gen_code_map.sh --composition "Position<64>"` = **0 transitive containers** (the 192B freeze is CONTAINED); `--byte-context` = the 2 size-pins (`Portfolio.hpp:117/143`) + the sole live blob dump (`ShardedSnapshotPersist.hpp:169`). All `file:line` below are HEAD `b10e778` (= E.1.1 ship `0ee227a` + the benign `.clang-format` commit; code byte-identical to `0ee227a`). RE-VERIFY any decision-log-sourced ledger/test line (flagged inline) at the dive per `feedback_compaction_degrades`.

## Ship end goal + acceptance criteria

**Plan type:** refactor (determinism-gated data relayout + wire VERSION bump + dead-code deletion).

**Ship end goal (1 sentence):** Open `Position`/`NodeState` to their FINAL successor layout in ONE determinism epoch — freeze `Position` at **192B** (reserve the D-206 16B `peak` + `owner_node_id`, both PERSIST), registry-drive its sole live serializer off `FOREACH_POSITION_FIELD`, compose-sub-registries the NodeContext persist-serializer, delete the two dead Position serializers, and Money-ize the residual `double` money seam — so every downstream `.E` spine reads a frozen, byte-guarded, `Money`-typed layout with zero re-touch.

**How this contributes to the sprint MASTER goal:** the successor-foundation leaf (#6) of the `.E.1` foundation arc — the layout freeze the E.1.3 aggregator / E.1.4 fill / E.1.5 purity / E.1.6 multi-exchange all build on; live-readiness cannot proceed on an unfrozen capital-state layout.

**Acceptance criteria (verifiable at ship close):**
- **Determinism (the gate):** golden-master round-trip byte-compare of the LIVE snapshot (`ShardedSnapshot_Save`→`_Load`) under BOTH `±USE_NATIVE_128` — bytewise-identical (NOT a sizeof-only assert; a byte-reorder keeps `sizeof==192` but must FAIL this). Fixtures regenerated (D-131 epoch-free).
- **Wire/H21:** `PORTFOLIO_SNAPSHOT_VERSION` 7→8 + `SHARDED_SNAPSHOT_VERSION` 10→11 bumped + tombstoned in the golden ledger (`check_identifier_retirement.py --update`); `CONTROLLER_SNAPSHOT_VERSION`=14 DELETED (dead-path retirement, not a bump).
- **Size-pins (Check-K):** `check_struct_alignment.py` GREEN with the Position pin at 192B (`Portfolio.hpp:117` → `==192`; `:143` persist-relationship holds); pins parameterized by `sizeof(Money)` (pin the relationship, not the literal `192`).
- **Hot path:** `calls_graph_diff.sh` GREEN (the SoA relayout is hot-path-INERT — the gate reads `ExecutionCore<F>`, never `Portfolio.positions[]`); `check_latency_path_conformance.py` no p99 regression vs the E.1.0 baseline (SoA should IMPROVE it — D-55 locality).
- **H4:** the F-096 leg-split (`Async.hpp:842`) carries NO `double` on the money path (`Money_Mul`/`Money_Sub`, conservation by construction); `check_money_gross_single_source.py` GREEN.
- **Serializer:** the sole live Position serializer + the NodeContext persist-serializer are registry-driven (no hand field-list can silently drop a field → closes the D-110 silent-zero-on-restore risk + TECH_DEBT-196 field-count-lock).
- **Deletion:** `Portfolio_Save/Load` + `PortfolioController_*Snapshot` gone; the `"TICK"` refuse-magic (`ShardedSnapshot_Load:329-333`) PRESERVED; ledger + tool SOURCES rows in lockstep (one commit each).
- **Tests GREEN:** suite baseline maintained; the `controller_test.cpp` 4-version snapshot assert (≈`:26819` — re-verify) updated for the SHARDED bump.
- **5 binaries clean** at sprint cadence.

## Design-shape — RE-GROUNDED (HEAD `b10e778`, 2026-07-02)  [SUPERSEDES the scaffold's `572c978` Design-shape below]

**`Position<F>` today (`CoreFrameworks/Portfolio.hpp:52`):** `alignas(64)`, **128B = 2 cache lines exact**, auto-generated from `FOREACH_POSITION_FIELD` (real home = **`MemHeaders/PositionFieldRegistry.hpp:49-59`** — the scaffold + per-field table's `CoreFrameworks/PositionFieldRegistry.hpp` path is STALE). Row shape `X(name, type, init, persist_kind, doc)`; 9 PERSIST fields @ offsets 0/16/32/48/64/80/96 (7×`Money`: take_profit_price, stop_loss_price, quantity, entry_price, entry_fee, original_tp, original_sl) + 112 (`entry_timestamp_us` u64) + 120 (`pair_index` i8) + `_pad_pos[7]`@121. Size-pins: `Portfolio.hpp:117` (`sizeof==128`), `:122-130` (offsets), `:141` (`POSITION_PERSIST_BYTES==128`), `:143` (`sizeof − PERSIST == 0`). NOTE: Position was ALREADY 192B pre-Ship-A (24B Money); Ship-A shrank it to 128B — E.1.2 grows it BACK to 192B (3 cache lines) via a reserved 16B `peak` + `owner_node_id`, NOT fatter fields.

**`Position<F>` FINAL → 192B (3 cache lines):** append via `FOREACH_POSITION_FIELD` rows (1 row each auto-flows to the struct AND the registry-driven serializer):
- `X(peak, Money, Money_Zero(), PERSIST, "slow-tracked trailing MFE peak → feeds hot ratchet; D-206")` — 16B (per A-2's REFUTAL of the floor-channel: D-206's learned/ATR give-back needs the real persisted peak).
- `X(owner_node_id, uint16_t, 0, PERSIST, "cold: boot-reconcile attribution (cluster<<8)|node; D-209")` — 2B.
- riding-flag(s) for the D-206 give-back + the reserved 16-bit fill-record owner (A-1 (B): the `order_id>>60` 4-bit attribution is incompatible with the 16-bit cap). 128→144 (peak), then owner + flags + fill-owner + pad → 192; exact sub-layout of the reserved ~48B = a Phase-B call; all named + `=0`-init + PERSIST (A-2: `_pad_pos` repurposing is NOT "free" — needs the version bump + explicit zero-init).

**The serializers at HEAD (currency-corrected — the scaffold under-cites):**
- SOLE LIVE Position serializer = the raw blob dump `ShardedSnapshotPersist.hpp:169` (`fwrite(portfolio.positions, sizeof(Position<F>), 16, f)`) + its load mate. AoS→SoA re-derive MUST round-trip EVERY field (a dropped SoA field = D-110 silent-zero-on-restore).
- NodeContext per-node hand-`fwrite` loop = **`ShardedSnapshotPersist.hpp:172-261` save / `:436-499` load** (NOT the scaffold's `:176-233` — that drops `:234-260`). Becomes the COMPOSE-SUB-REGISTRIES driver.
- DEAD (delete, D-289): `Portfolio_Save`/`_Load` (`Portfolio.hpp:547/586`) + `PortfolioController_SaveSnapshot`/`_LoadSnapshot` (`PortfolioController.hpp:2029/2104`).

**Versions at HEAD (stale comments — THREE layers to sweep):** actual `#define PORTFOLIO_SNAPSHOT_VERSION 7` (`Portfolio.hpp:534`) but inline comments say **6** (`Portfolio.hpp:45/68/82/95/132`) AND **5** (`PositionFieldRegistry.hpp:14/21/42`); `SHARDED_SNAPSHOT_VERSION 10u` (`ShardedSnapshotPersist.hpp:94`); `CONTROLLER_SNAPSHOT_VERSION 14` (`PortfolioController.hpp:2025`, delete). Also stale: `ConfidenceScore.hpp:1005/1050/1065` "=11/12" comments; `ShardedSnapshotPersist.hpp:9-11/48/59` legacy-v11 refuse comments; the `Portfolio.hpp:102-114` old-24B-192B layout block. Sweep ALL at the bump.

**F-096 leg-split STILL `double` (`CoreFrameworks/EngineSharded/Async.hpp:823/842/896` — the handoff's "add the `EngineSharded/`" path fix):** `Money→double→split→Money` (`double full_qty = Money_ToDouble(intended_qty)`@842; back via `money_from_double_payload`@896). H4 violation (TD-167). Fix in the relayout: `legA=Money_Mul(intended, frac); legB=Money_Sub(intended, legA)` — conservation by construction, no `double`.

## The coding plan (phased — the CONVERGENT AMENDED FREEZE PLAN, ordered)

**Phase A.0 — rollback anchor + cascade re-run.** Tag `pre-v5.15.5.F.4d.1.E.1.2`. Re-run the cascade (`gen_code_map.sh --composition/--byte-context "Position<64>"` + `check_struct_alignment.py` baseline GREEN) immediately before touching layout, to re-confirm 0 transitive containers + the size-pin set.

**Phase B — open `Position` ONCE → 192B.** Append the `peak` + `owner_node_id` + riding-flag/fill-owner rows to `FOREACH_POSITION_FIELD` (`MemHeaders/PositionFieldRegistry.hpp`); update the size-pins (`Portfolio.hpp:117` 128→192; offset asserts for the new fields; `:141/:143` persist-relationship); parameterize the pins by `sizeof(Money)`. Zero-init every new field at every OpenSlot/Add/bypass site. Sweep the stale version comments (5/6 → the real value at the Phase-G bump).

**Phase C — registry-drive the serializers (A-1 + A-3 converge; drop-proof gather/scatter).**
- Position: drive its sole live serializer (`ShardedSnapshotPersist.hpp:169` blob) off `FOREACH_POSITION_FIELD` — field-by-field gather/scatter under AoS→SoA, no hand-literals (the `+7` stride / the `16` count / the field-list all auto-flow; a missed SoA field = D-110).
- NodeContext: convert the `:172-261`/`:436-499` hand-loop to a COMPOSE-SUB-REGISTRIES driver — a thin loop delegating to `regime_state` + `pnl_feeder` + `confidence` (already `FOREACH_CONFIDENCE_PERSIST_FIELD`) sub-walkers. NOT a flat `FOREACH_NODE_PERSIST_FIELD` (OMS is flat `{DIRECT,BIT}`; NodeContext needs nested/array/delegate). Do NOT introduce a `SERIALIZER_GENERATED` meta-registry flag (grep-zero; adding it triggers the TD-057 schema cascade) — keep it Stage-2. This structurally closes TECH_DEBT-196 (field-count-lock) + makes **TECH_DEBT-227** (`partner_pending_pnl`) a natural persist row if it joins the walked set — DECIDE its inclusion here vs E.1.4 at this phase.
- Mark derived fields (`node_dd_pct`@`:213`; every `ShardedSnapshot` per_node double `:490-496`) recompute-on-load, NOT raw-persist rows (Fight #2). Drop `drawdown_max` (0 consumers) + `drawdown_current` (derivable).

**Phase D — delete the two dead serializers (D-289).** ONE commit each (or the retirement guard `sys.exit`-crashes): delete the `#define` + dead fns + the ledger row (`identifier_ledger.txt:55/57` — re-verify) + the tool SOURCES row (`check_identifier_retirement.py:57-59` — re-verify) + run `--update`. PRESERVE the `"TICK"` refuse-magic (`ShardedSnapshot_Load:329-333`, the one live consumer). VERIFY via `/trace-deps` that deleting `PortfolioController_*Snapshot` doesn't orphan a still-live ConfidenceScore serializer leg (`ConfidenceScore.hpp:1065` wire is shared — confirm the LIVE ShardedSnapshot confidence sub-walker survives).

**Phase E — Money-ize the F-096 leg (TD-167)** + a `static_assert`/runtime `legA.qty + legB.qty == intended_qty` conservation assert.

**Phase F — reserve the cross-E slots (reserve, don't solve).** `NodeState`/`ClusterState` skeletons (`alignas(64)` clusters: hot / slow_account [**ALL `Money`, never `FPN<F>`** — the silent-encoding-epoch trap, synthesis frozen-item 1] / drainer_state / binding / kill_flags); the 5 missing wire-locked `Money` fields (`node_gross_wins/losses`, `last_entry_price`, `node_dd_pct`, `pnl_feeder.price_samples` — A-3); the `intended_*` OWN seqlock pack (A22); the 16-bit fill-record owner; RESERVE-not-delete `NodeState.hot.mode` (D-284 — E.1.5/E.1.6 need it) + keep `cfg.trading_mode` the STAMP_BOUND SSoT. Define the cap-symbol SSoT in `Limits.hpp` (`NUM_EXCHANGES`/`MAX_NODES_PER_CLUSTER`/`MAX_CLUSTERS` ≤256 each; `Limits.hpp` has only `MAX_EXECUTION_NODES=16`/`MAX_PORTFOLIO_POSITIONS=16` today); `owner_node_id = (cluster<<8)|node`; decouple `cluster_id` from `ExchangeEnum` (H22). The ③ `ControllerConfig` fingerprint-safety fold (D-247/D-256): include the 4B gate-summary in the size-pinned final layout + ADD the missing `static_assert(sizeof(ControllerConfig)==N)` + the guaranteed-0-on-clean-load assert at every `Fingerprint_Compute` site.

**Phase G — version bumps + determinism guard + ship.** Bump PORTFOLIO 7→8 / SHARDED 10→11 (+ tombstone + `--update`); the golden-master `±USE_NATIVE_128` round-trip byte-compare (update `controller_test.cpp` 4-version assert on the SHARDED bump); regen fixtures; `check_session_docs.sh` CLEAN; then (the gate already ran pre-Phase-A) → consult Caramel → `/ship`.

## Test-change enumeration (per `feedback_test_change_enumeration_per_plan_body`)
- **(a) MODIFIED:** the `controller_test.cpp` 4-version snapshot assert (SHARDED 10→11 / PORTFOLIO 7→8); every Position offset/size `static_assert` (128→192); the snapshot round-trip fixtures (regenerated at the epoch bump).
- **(b) BROKEN-REPLACED:** any test asserting `sizeof(Position)==128` or the old `PORTFOLIO_SNAPSHOT_VERSION`/`CONTROLLER_SNAPSHOT_VERSION`; tests exercising `Portfolio_Save/Load` or `PortfolioController_*Snapshot` (deleted with the serializers — verify none are load-bearing BEFORE deleting).
- **(c) NEW:** the golden-master `±USE_NATIVE_128` snapshot round-trip byte-compare (the determinism gate — the acceptance criterion); a `peak`/`owner_node_id` persist round-trip test (D-110: a repr/layout change silently recovering a slightly-wrong value is the nightmare case); the F-096 leg conservation assert; a registry-driven-serializer "no field silently dropped" test.

## Currency corrections applied (the scaffold body was STALE — re-grounded HEAD `b10e778`)
- Registry home: `MemHeaders/PositionFieldRegistry.hpp` (NOT `CoreFrameworks/…`).
- Position freeze = **192B** (reserve 16B peak) — the synthesis's early "leans floor @128B" is SUPERSEDED by its own A-class verdict + D-283.
- NodeContext serializer = `ShardedSnapshotPersist.hpp:172-261`/`:436-499` (NOT `:176-233`).
- F-096 leg STILL `double` at `Async.hpp:842` (NOT "already Money-ized").
- Versions: PORTFOLIO `#define`=7 (comments stale-say 6 AND 5); SHARDED=10; CONTROLLER=14 (delete).
- The old "## Design-shape (…572c978…)" section below is SUPERSEDED by the RE-GROUNDED section above (retained for audit trail).

---

*Below: the original 2026-06-15 scaffold body (design-context layer — Scope / seams / locked decisions / known folds stay authoritative; the old Design-shape is SUPERSEDED by the RE-GROUNDED section above).*

## Scope
Three layout deliverables, all bytes-only on FROZEN names (E.1.1 already renamed):
1. **AoS→SoA relayout** of `Portfolio`/`Position` (D-55) — the money fields are ALREADY `Money` (`FixedPoint<10,8>`, 16B) so this is LAYOUT-only, NO type churn (D-101 numeric-final already satisfied by Ship-A/B); AVX-512-friendly SoA even at small N. Add `owner_node_id` to `Position` (D-209 venue-net reconcile foundation; attribution for boot-reconcile so N fills don't collapse to node 0).
2. **The NEW `NodeState` struct** — `alignas(64)` clusters by access pattern (hot reads / slow-account / drainer-state / binding / kill-flags), the per-node home E.1.3+ relocates state into. H12 explicit-padding + size-pin static_asserts.
3. **The `ClusterState` CONTAINER shape** — the struct SKELETON + `NodeState.binding{cluster_id, subaccount_id, client_order_id_prefix}` + the flat `per_cluster[]` aggregate slot array (D-225 resolution #3a: the container SHAPE is FOUNDATIONAL data-layout, lands EARLY with the relayout; the adapter/WS/registry MACHINERY is E.1.6, late). The hierarchy nodes live in (D-15/D-28).

Then: bump `SHARDED_SNAPSHOT_VERSION` + `PORTFOLIO_SNAPSHOT_VERSION` (bytes change → H21 tombstone the old version + regen fixtures, D-131 epoch-free = cheap) + a double-split conservation assert. NO consumer-logic rework (that's the spine ships).

## Spine / layer
FOUNDATION (no D-214 spine) — but the FINAL one. Layout-only → a determinism/size-pin break bisects HERE, orthogonal-and-ordered vs E.1.1 (E.1.1 = NAMES, E.1.2 = BYTES, separate anchors per D-225 resolution #2).

## TD-closed
- **TD-196** (snapshot field-count-lock): the SoA relayout + VERSION bump SUPERSEDES the old count-lock with the size-pinned `static_assert(sizeof==N)` + the bumped VERSION (the E.1.3 seqlock-published snapshot later supersedes it further — A-adjudication §4(a) routes the *full* TD-196 close to E.1.3, but the field-count-lock mechanism is replaced HERE by the relayout's size-pins).
- None other directly; this leaf LANDS the foundations the spines close debt against: `owner_node_id` (D-209), SoA (D-55), the cluster-container shape (D-15/D-28).

## Locked decisions honored
- **D-55** (Portfolio/Position SoA + AVX-512): AoS→SoA, layout-only (money already `Money`).
- **D-99 / D-104 / D-107 / D-125 / D-176** (the unified decimal `Money` core, Ship-A/B DONE): USE `Money` — the SoA carries `Money` fields, no `double` on the money path. D-101 numeric-final-before-rename SATISFIED → SoA is pure relayout, no type churn.
- **D-209** (per-node ownership + venue-net foundation): add explicit `owner_node_id` on `Position`; coordinate with D-55 (SoA) + D-206 (peak field) so Position storage opens ONCE. Sub-pool mechanism DEFERRED (future-expansion, build-when-scaling) — only the `owner_node_id` foundation lands here.
- **D-15 / D-28** (cluster/node/deployment hierarchy is FOUNDATIONAL data layout): the `ClusterState` container SHAPE + `binding.cluster_id` + `per_cluster[]` land here (resolution #3a), the machinery follows in E.1.6.
- **D-131** (epoch-free): the snapshot VERSION bump + fixture regen is CHEAP (no live models; regen test fixtures, no migration) — do NOT over-engineer snapshot back-compat (softens the H21 finding to its discipline-only form).
- **H10** (SIMD scalar fallback byte-identical) · **H12** (explicit padding) · **H21** (snapshot VERSION append-only/tombstone).

## §4 guard-rows it fills
Per guard-matrix §4a:
- **H10**: the FIRST real SoA AVX kernel with a bytewise-identical scalar fallback — VALIDATED against E.1.0's determinism gate (`±USE_NATIVE_128` byte-compare). E.1.0 armed the gate; this is its first non-trivial consumer (A-adjudication §4b "H10 re-validated in E.1.2's first SoA AVX kernel").
- **H12**: explicit `int<N>_t _padding<N> = 0` static_asserts on the new NodeState/ClusterState clusters (byte-equiv / snapshot context).
- **H6**: `alignas(64)` cross-thread on the seqlock-bound clusters (hot / slow_account / drainer_state / kill_flags get dedicated cache lines).
- **H21**: the snapshot VERSION bump + tombstone (`check_identifier_retirement.py --update` against the golden ledger) + `check_struct_alignment.py` size-pins (Check K — a silent layout change becomes a compile error, then the VERSION bump).

## INBOUND seam (needs from predecessor — E.1.1)
E.1.1 delivered `NodeContext`/`NodeState` BY NAME (the rename is done — this leaf relayouts a tree whose names are already final) + the rename-cascade tool (reused here for the SoA layout-impact enumeration). E.1.0's determinism gate is live (the byte-identity check this leaf's H10 SoA kernel must pass) + the H8 latency ratchet (the SoA relayout must not regress the hot-path p99 past the E.1.0 baseline; SoA + AVX should IMPROVE it — D-55 cache-locality + bandwidth).

## OUTBOUND seam (invariant the successor relies on — E.1.3, the capital cluster)
SoA + `owner_node_id` + the `ClusterState` skeleton + `binding.cluster_id` + `per_cluster[]` all PRESENT; snapshot VERSION bumped + tombstoned; size-pins green; the determinism gate green `±USE_NATIVE_128`; the new `NodeState` clusters (`slow_account` / `drainer_state` / `kill_flags` / `binding`) present + `alignas(64)`. **The struct is FINAL** — E.1.3 relocates state INTO these frozen clusters (the aggregator writes `slow_account`/`kill_flags`, the absorbed drainer owns `drainer_state`, the NEW-1 relocation targets `cluster.trading_mode`), it never re-touches the layout.

**CROSS-CLUSTER OUTBOUND (foundation cluster → capital cluster E.1.3):** *a frozen+guarded hot path on a FINAL `NodeState`/`ClusterState` layout with `Money` accounting fields + the cluster container + snapshot version bumped.* This is the precondition the entire capital superstructure (E.1.3 coherence / E.1.4 fill / E.1.5 purity) rests on.

## Design-shape (SUPERSEDED — scaffold-time, HEAD `572c978`; retained for audit trail. Current layout + the 192B freeze target live in "## Design-shape — RE-GROUNDED (HEAD b10e778)" above)
- **Current `Position`** (`CoreFrameworks/Portfolio.hpp:52`): `template <unsigned F> struct alignas(64) Position` — auto-generated from `FOREACH_POSITION_FIELD` (PositionFieldRegistry.hpp): 7 `Money` fields (take_profit_price/stop_loss_price/quantity/entry_price/entry_fee/original_tp/original_sl @ 16B each, offsets 0/16/32/48/64/80/96) + `entry_timestamp_us` (u64 @112) + `pair_index` (i8 @120) + `_pad_pos[7]` → **128B size-pinned (= 2 cache lines exact)**. Money already 16B (Ship-A flipped 24→16). SoA = pivot the `positions[N]` array-of-struct into struct-of-arrays per field (the AVX-512 walk; `owner_node_id` joins as a new per-position field via `FOREACH_POSITION_FIELD` — 1 registry row, auto-flows).
- **Current `Portfolio`** (`Portfolio.hpp:151`): `template <unsigned F> struct Portfolio` = `active_bitmap` (u16, hot, cache-line-0) + `_pad0`/`_pad1` + `Position<F> positions[MAX_PORTFOLIO_POSITIONS]` (AoS; static_assert `MAX_PORTFOLIO_POSITIONS <= 16` for the u16 bitmap). SoA relayout pivots `positions[]`.
- **NEW `NodeState`** (0-hit symbol — free; replaces parts of `CoreContext`-post-rename = `NodeContext`): `alignas(64)` clusters — `hot` (seqlock params + adapter handle + symbol + mode), `slow_account` (realized_pnl/open_notional/capital/drawdown — aggregator reads via seqlock), `drainer_state` (the SPSC rings + order table + post-fill bookkeeping — E.1.3 relocates the absorbed drainer here), `binding` (cluster_id/subaccount_id/client_order_id_prefix), `kill_flags` (per_node/per_cluster_mirror/global_mirror atomics). Size-pin + offsetof static_asserts (the v0.1 sketch at the sub-master `.E.1-foundation.md:368-432` is the SHAPE reference — but RE-GROUND it; its `FPN<F>` money fields must be `Money`, and `MAX_CORES` is a phantom → `MAX_EXECUTION_NODES`).
- **NEW `ClusterState` CONTAINER skeleton** (0-hit — free): the struct shell + `nodes[MAX_NODES_PER_CLUSTER]` + `per_cluster[]` aggregate slots + the boot-time-immutable identity fields. SHAPE only — the producer/adapter/WS thread handles + the adapter dispatch are E.1.6 (resolution #3a: shape early, machinery late). (v0.1 sketch at `.E.1-foundation.md:572-614` — RE-GROUND.)
- **Snapshot VERSIONs (RE-DERIVED, scaffold-time):** `SHARDED_SNAPSHOT_VERSION = 10u` (`CoreFrameworks/ShardedSnapshotPersist.hpp:94`, "Ship-B DECIMAL epoch") → bump to **11**; `PORTFOLIO_SNAPSHOT_VERSION = 7` (`CoreFrameworks/Portfolio.hpp:534`, "Ship-B DECIMAL epoch") → bump to **8**. (NOTE: the A-adjudication's "6→7" was stale — PORTFOLIO is ALREADY 7; the real bump is 7→8 / 10→11.) Both gate `fread` version-reject (`ShardedSnapshotPersist.hpp:341`, `Portfolio.hpp:602`) → old snapshots auto-rejected at the epoch boundary (D-100/D-131; regen fixtures). H21 tombstone the old versions in the golden ledger.
- **Cascade tool (MANDATORY FIRST — D-202):** per CoreFrameworks/CLAUDE.md surface rule, a core-struct layout change runs the cascade check FIRST: `tools/gen_code_map.sh --composition Position` (transitive containers) + `--byte-context Position` (wire/persist/memcmp sites) + `check_struct_alignment.py` Check K (size-pin coverage). The `.E.0.10` 16B flip cascaded `Money`→`OrderPreResolved`→`Order`/`Position` + persist sites — too big to hold in head. Reuse E.1.1's rename-cascade piece.
- **F-096 conservation assert**: the double-split partial-exit path — `static_assert`/runtime `legA.qty + legB.qty == intended_qty` (the F-096 H4 sizing fix surfaced `.E.0.10`; the leg-split now uses `Money` not `double` — verify the SoA relayout carries it).

## Rollback anchor
`pre-v5.15.5.F.4d.1.E.1.2` (create at the dive's Phase A.0). Bisectable: layout-only → a determinism/size-pin/conservation break bisects here, orthogonal to E.1.1 (NAME) — the BYTES anchor.

## Known folds / findings it owns
- **The snapshot VERSION bump is the H21 event** (the persisted-wire identifier — distinct from E.1.1's cfg-KEY clean-break which is operator-text). Tombstone v10/v7 in the golden ledger, bump to v11/v8, regen all snapshot fixtures (D-131 free). **Operator migration impact** (SECTION): old warm-restart snapshots are version-rejected → the operator's in-flight recovery state from a pre-`.E.1.2` run won't load (acceptable: epoch boundary, no live models; a fresh paper session re-warms). Recovery MUST round-trip money EXACTLY through the new SoA layout + be tested (D-110: a repr/layout change silently recovering a slightly-wrong balance is the nightmare case).
- **`owner_node_id` lands but its CONSUMER is E.1.3** (the venue-net reconcile that uses it to attribute boot fills per-node, fixing the collapse-to-node-0 at `Reconcile.hpp:245`). This leaf adds the FIELD; E.1.3's reconcile rework reads it. Don't close D-209's collapse here — only the foundation field.
- **`third storage` convergence note (TECH_DEBT-189, D-211)**: the SoA pass opens the strategy-state structs (`live_vol_mult` etc.) — the operator-flagged convergence of the three "live volume multiplier" storages is a `.E.1`/`.E.M` design decision that "touches the strategy-state structs `.E.1`'s SoA pass opens anyway." FLAG for the dive: decide whether to converge here or home to `.E.M` (don't silently expand scope — fold the decision, not necessarily the work).
- **`MAX_NODES_PER_CLUSTER` vs `MAX_EXECUTION_NODES`**: this leaf introduces the per-cluster node cap (`MAX_NODES_PER_CLUSTER`) — distinct from E.1.1's deployment-wide `MAX_EXECUTION_NODES`. Keep them separate constants (the cluster cap is a sub-count). Resolve naming with E.1.1's C-class flag.
- **H10 SoA kernel is the determinism gate's first real test** — if the AVX-512 SoA portfolio walk and its scalar fallback diverge by even one byte under `±USE_NATIVE_128`, E.1.0's gate RED-builds here. This is the gate earning its keep (guard-matrix §4a:110).
- **INBOUND from E.1.0's H8 hardened re-pass (CONDITIONAL — pending D-232 / FORK-A ratification):** E.1.0's H8 ships MINIMAL (the bench + per-core p99 emit + the absolute/REBUILD checks). The hardened I→A (D-232) split these to E.1.2 as its **first REAL latency consumer** — fold at the E.1.0 ship-close once FORK A/B is ratified: (1) the **`ml_inference_ran` counter** (covering single-zoo + ensemble) + the **ML-worst-case vacuity gate** (the strat=ML label is vacuous — gate on degrade FLAGS incl. `CFG_DRIFT_STRICT_REFUSED`, registry-sourced); (2) the **10%-margin latency RATCHET-down** (needs E.1.2's re-baselined floor — E.1.1 is names-only, can't move p99); (3) the **floor CAPTURE** itself. This relayout is where the latency gate genuinely bites (AVX bytes + cache change), so the worst-case-ML + ratchet earn their keep HERE, not at E.1.0. (Deep tail p999/p9999 → `.E.2`; the ROLLING compute optimization → its own `.E` leaf per D-229/#9.)
- **[③ re-gate fold 2026-06-22 · D-247 · F-B/F-I] — when this leaf FINALIZES `ControllerConfig`'s layout, it owns the config-compiler's fingerprint-safety.** ③ (E.1.1) seeds `cfg_load_fault_flags` in `ControllerConfig`'s cold boot-flags region (next to `live_capital_cfg_conflict@:576`). `ControllerConfig` is SHA-256-fingerprinted RAW into the model lineage (`ControllerConfig.hpp:347` → `Fingerprint.hpp:180`) — the new field is a DETERMINISM surface. This leaf MUST: (a) include the 4-byte gate-summary in the size-pinned final layout; (b) **ADD the `static_assert(sizeof(ControllerConfig)==N)` it currently LACKS** (F-I — Check-K gap: a fingerprinted struct with no size-pin → a silent perturbation isn't caught today); (c) **[CORRECTED 2026-06-23 · D-256 · forward-seam FS-1]** ensure the fault field is **guaranteed-0-on-clean-load — NOT "excluded"**: there is NO field-exclusion mechanism (`Fingerprint_Compute` is a `void*`+sizeof byte-hasher; `ControllerConfig.hpp:1328` admits it), so the `assert(config_used.cfg_load_fault_flags==0)` at every `Fingerprint_Compute` call site IS the guarantee. (This is the D2 guard-strengthen surface — the `sizeof` size-pin ALONE is vacuous for a size-PRESERVING relocation, e.g. ③ item-2's equal-size field migrate keeps `sizeof==53056` while offsets shift → the assert, not the pin, is what catches a poisoned-cfg fingerprint.) The bitmap GROWS across the E-series (E.1.5 folds `live_capital_cfg_conflict` in + adds CLUSTER_PURITY; E.1.6 adds EXCHANGE_INVALID) — the guaranteed-0 (assert-at-every-`Fingerprint_Compute`) discipline is structural, applies to the whole field forever. The readout error/warning LIST stays stack-local in `ControllerConfig_Load` (NOT a struct member — else KB of transient diagnostics ride the fingerprint + the hot-reload bulk-copy + the GUI mirror). Re-gate SSoT: `plan_checks/2026-06-22-E.1.1-config-compiler-adversarial-regate.md`.
