---
type: audit-report
audit: /bug-check
scope: plan body + landed code targeted re-fire
target: v5.15.5.F.4d.1.B.4 v1.7.3
plan_body: plans/v5.15-live-readiness/subplans/2026-05-24-v5.15.5.F.4d.1.B.4-train-serve-execution-layer-parity.md
head: c2595d6 (WIP-checkpoint 4)
established: 2026-05-26
verdict: YELLOW — v1.7.3 CRITICAL closures all PASS + ZERO new Class instances introduced by amendment cycle BUT 4 NEW Class 14 fabrications surfaced inside the v1.7.3 amendment itself (3 new struct-field/path fabrications at Step C.4 BACKTEST caller code sample + prose; 1 amendment self-inconsistency residue across 3 lines)
recommendation: AMEND v1.7.3 to v1.7.4 before Phase B Step B.3 body coding unlock — fix 4 NEW Class 14 instances + 3 internal-inconsistency residue + verify no further /bug-check spiral
---

# /bug-check report — v5.15.5.F.4d.1.B.4 v1.7.3 (2026-05-26)

## Stage 0 — DESIGN_PHILOSOPHY preload

Loaded:
- § 11 (Process discipline) — for Class 14 (plan-time symbol verification)
- § 7 (Maintenance gradient — Structural-fix preferred) — for Class 18 (mirror-state preservation)
- § 3 (Hard Invariants — H13 type-trait dispatch) — for Class 23 (typed/opaque-pointer review)
- § 1.5 (Framework discipline meta-principle) — for Class 24/27/28 (cfg surface + per-instance binding + branchless dispatch closures)
- § 11.5 (Meta-discipline registry — M5/M6) — for M6 META-discipline codification verification

## Step 1 — Format-invariant validation

CLEAN — `DOCS/RECURRING_BUG_PATTERNS.md` (INDEX) + 32 sub-class spec files (per `DOCS/recurring-bug-patterns/class-*.md`) all well-formed with `surface_tags:`, `severity:`, `closure_mechanism:`, `**Detection:**` blocks (some delegate to /trace-deps + /ml-audit per registry-driven discipline).

NOTE: Class 26 referenced in Class 27 body as sister concept but no own canonical entry. Classes 31 + 32 cited from CLAUDE.local.md sprint state but not yet codified into own files. NOT a v1.7.3-introduced doc-debt finding (pre-existing per INDEX § "Note on numbering").

## Step 2-4 — Targeted re-verify per task spec

### Verdict summary

| Class | Verdict | Notes |
|---|---|---|
| Class 14 (fabricated symbols) | **NEW-4** | v1.7.3 N-2 + N-3 + N-6 + self-catch all CLOSED ✓ BUT amendment cycle introduced 4 NEW Class 14 instances at Step C.4 (see below) |
| Class 18 (mirror state/code) | KNOWN-7 | N-4 fix landed correctly ✓ PARITY-026 closure intent PRESERVED |
| Class 21 (parallel descriptors) | CLEAN | BootPerCore drops unused `oms` ✓ SlowPathCycleOneCore 9-arg sister-canonical fit ✓ |
| Class 23 (type-erased reinterpret_cast) | CLEAN | `oms.ezoo_refs[c] = (void*)ezoo_ptr` is safe typed→opaque conversion (void* field declared at OrderManager.hpp:624) |
| Class 24 (capability-cfg surface mismatch) | CLEAN | All 4 cohort closures structurally intact |
| Class 25 (scope-erosion per-core) | CLEAN | All `state.cores[c].*` reads correctly scoped to per-core variable |
| Class 27 (scalar cfg-mirror cache) | CLEAN | EngineCommon.hpp: zero static fn-local caches; const cfg reference signature enforces decision-time-data-binding |
| Class 28 (branchless hand-wave) | CLEAN | D1-B IS sister-instance per H20 § "genuine binary predicate" exemption (verified vs canonical wrappers at ControllerEventLoop.hpp:2344 + :3558); N-3 RATIONALE REFRAME at line 749 honest about it being NEW code applying slow-path-gate cache pattern (not cosmetic-extension) |
| Class 1-13 | CLEAN | Not in scope of touched surfaces |
| Class 15-17 | CLEAN | No signature drift / naming drift / adjacent-struct-grep gaps |
| Class 19-20 | CLEAN | No hardcoded instance names / bitmap overflow (FOREACH_SLOW_PATH_GATE bit budget = 10 + 1 = 11/16 safe) |
| Class 22 | CLEAN | All cfg gating via FOREACH_SLOW_PATH_GATE + AUTOPOPULATE_ENGINE_WIDE registry-driven |
| Class 26/29/30/31/32 | N/A | Not touched by this ship |

## Detail — v1.7.3 amendment closures verification

### CRITICAL N-2 (AUTOPOPULATE macro arg signature) — PASS

**Verified macro signature at HEAD:** `CoreFrameworks/SlowPathGateRegistry.hpp:236-243`
```cpp
#define SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE(state, _cfg_arg)
    do {
        ...
        (state).flags = _new_flags;  // ← writes to (state).flags directly
        ...
    } while (0)
```

**Canonical caller sites verified:**
- `CoreFrameworks/ControllerEventLoop.hpp:2335` — `SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE(state->global_gate_state, *config);`
- `CoreFrameworks/ControllerEventLoop.hpp:3555` — `SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE(state->global_gate_state, cfg);`

**Plan body amendment at line 750 cites:** `SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE(state.global_gate_state, cfg)` — CORRECT (helper takes `EventLoopState<F>&` reference vs caller-site pointer; `state.global_gate_state` correctly accesses GlobalGateState field by reference rather than `state->global_gate_state` pointer chase).

**Historical wrong-cite preservation:** Lines 5, 55, 750, 1070 retain `(state, cfg)` framing but ALL labeled "was incorrectly" / "incorrectly cited" — correctly preserved as amendment-history per `feedback_proactive_novel_alternative_consideration` (4-pillar self-audit Pillar 4).

VERDICT: CRITICAL N-2 CLOSURE LANDED CORRECTLY.

### CRITICAL N-3 (D1-B rationale reframe) — PASS

**Plan body line 749** correctly reframes: "v1.7.2 incorrectly framed this as 'matches existing wrapper at :3799 cosmetic-extension'; /bug-check verified — wrapper at :3799 reads `cfg.lifecycle_cfg_flags` DIRECTLY, NOT via slow-path-gate cache. D1-B is **legitimately NEW code applying the slow-path-gate cache pattern to breakeven for the first time**."

**However see FINDING C-3 below** — line 752 still cites pre-N-3 "matches existing wrapper pattern" framing (residual inconsistency within v1.7.3 amendment cycle).

VERDICT: N-3 PARTIAL LANDING (semantic correctness intact at line 749; line 752 stale residue not swept).

### CRITICAL N-4 (Step C.4 H1 over-broad deletion REVERT) — PASS

**Verified producer-thread sites at HEAD:**
- `EngineSharded.hpp:1615` — `EventLoop_UpdateEmaPriceAllCores` fires on PRODUCER THREAD (NOT in lambda :3044-3311)
- `EngineSharded.hpp:1817-1821` — ema_price replication path on PRODUCER THREAD
- `EngineSharded.hpp:1886` — `EventLoop_KillSwitchEvaluate` fires on PRODUCER THREAD (NOT in lambda :3044-3311)

**Verified BACKTEST sites STAY per N-4:**
- `ShardedBacktestDriver.hpp:354` — `EventLoop_UpdateEmaPriceAllCores(drv->state, *drv->ema_price);` — STAYS
- `ShardedBacktestDriver.hpp:367` — `EventLoop_KillSwitchEvaluate(drv->state);` — STAYS

**Plan body amendments at lines 992 + 995** correctly label both as "KEEP (v1.7.3 N-4 REVERTED DELETION)" with explicit PARITY-026 preservation rationale.

VERDICT: CRITICAL N-4 CLOSURE LANDED CORRECTLY. Without this fix, v1.7.2 would have silently DROPPED kill_switch + ema_price replication from backtest → REVERSED PARITY-026 closure intent.

### CRITICAL N-6 (9-arg SlowPathCycle signatures with BookSnapshot sister-canonical reuse) — PASS

**Verified at landed code `CoreFrameworks/EngineCommon.hpp`:**
- `:103` — NEW include `"../DataStream/BinanceDepth.hpp"` for BookSnapshot<F> sister-canonical reuse ✓
- `:104` — NEW include `"SlowPathGateRegistry.hpp"` for SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE macro ✓
- `:470-479` — `EngineCommon_SlowPathCycleOneCore` 9-arg signature: `(cfg, c, state, oms, price, volume, ts_us, now_tick, depth)` ✓
- `:490-498` — `EngineCommon_SlowPathCycleAllCores` 8-arg signature (drops `c`): `(cfg, state, oms, price, volume, ts_us, now_tick, depth)` ✓

**Verified all NEW v1.7.3 cited symbols exist at HEAD:**
- `BookSnapshot<F>` — `DataStream/BinanceDepth.hpp:29-41` ✓ (NO `enabled` field — v1.7.3 self-catch correctly removed fabricated construct)
- `BookSnapshot_Init` — `DataStream/BinanceDepth.hpp:43-59` ✓
- `last_volume.load()` — `EngineSharded.hpp:1347` (atomic field) + usage at `:1537`, `:1819-1821`, `:3069` ✓
- `ticks_produced.load()` — `EngineSharded.hpp:1340` (atomic field) + usage at `:1535`, `:1874`, `:2467`, `:2800` ✓
- `g_depth_shared.active_idx` — `EngineSharded.hpp:835` (DepthSharedState) + ACQUIRE pattern at `:1817`, `:3053` ✓
- `MASK_GATE_CFG_DEPTH_ENABLED` — `GateCfgFlagRegistry.hpp:58/76/94` ✓
- `cfg.gate_cfg_flags` — `ControllerConfig.hpp:568` ✓
- `ts_us` (now_tick distinct) — caller pattern matches `:1930-1931` + `:2800` (separate atomics) ✓
- `tick_index` (BACKTEST) — driver-side counter; verify at Phase C implementation per plan body comment ✓

VERDICT: CRITICAL N-6 CLOSURE LANDED CORRECTLY. Skeleton declarations + includes ALL match spec.

### NEW Class 14 instances introduced at v1.7.3 — 4 CRITICAL

The v1.7.3 amendment cycle attempted to close 4 CRITICALs from v1.7.2 — but in adding the Step C.4 BACKTEST caller code sample, introduced 4 NEW Class 14 instances. Each is a fabricated symbol/path that doesn't exist at HEAD `c2595d6`:

#### NEW-1 (CRITICAL) — `Backtest/DepthReplayState.hpp` path fabrication

**Plan body cites:**
- Line 971 (inside Step C.4 code sample comment): "field-mapping verified at Phase C implementation time per Backtest/DepthReplayState.hpp"
- Line 988 (prose-body): "BACKTEST DepthReplayState struct in `Backtest/DepthReplayState.hpp`"

**Actual location at HEAD:** `DataStream/DepthReplayState.hpp` (verified via `find /home/caramel/code/FoxML_Trader_v2 -name "DepthReplay*"`)

**Severity:** CRITICAL — pre-coding plan-time fabrication; would have caused Phase A Step A.6.5.b header-cycle trial compile FAIL when EngineCommon.hpp transitively pulls in `#include "Backtest/DepthReplayState.hpp"` per v1.7.3 HIGH-2 header-cycle scope expansion. **Sister to v1.6 cycle 14a/14b/14c fabrications that triggered v1.7 D3 Stage 6 CI tool promotion.** Meta-recurrence of v1.6 class.

**Fix:** rewrite path cites to `DataStream/DepthReplayState.hpp` (2 sites: lines 971 + 988).

#### NEW-2 (CRITICAL) — `drv->depth_enabled` struct-member fabrication

**Plan body cites:**
- Line 974 (Step C.4 code sample): `if (drv && drv->depth_enabled) {`

**Actual at HEAD:** `ShardedBacktestDriver_State<F>` struct exposes NO `depth_enabled` field. Only synthesized at `ShardedBacktestDriver.hpp:351` as ternary `(drv->book_imbalance || drv->current_spread) ? 1 : 0`. No bool member.

**Severity:** CRITICAL — would COMPILE FAIL at Phase C body coding ("no member named 'depth_enabled' in 'ShardedBacktestDriver_State<F>'"). Class 14 anti-shape (plan calls a struct field that doesn't exist).

**Fix options:**
- Replace with the ternary synthesis pattern actually used at `:351`: `if (drv && (drv->book_imbalance && drv->current_spread && drv->current_mid_price))`
- OR introduce a `depth_enabled` field on ShardedBacktestDriver_State (out-of-scope; Phase 0 sub-tag pattern per Class 14 prevention)
- OR pull the depth-enable check directly from cfg: `if (drv && BITMAP_IS_SET(cfg.gate_cfg_flags, MASK_GATE_CFG_DEPTH_ENABLED))`

#### NEW-3 (CRITICAL) — `drv->current_book_imbalance` struct-member fabrication

**Plan body cites:**
- Line 975 (Step C.4 code sample): `depth.imbalance = drv->current_book_imbalance;`
- Line 988 (prose-body): "field-mapping `drv->current_book_imbalance` / `drv->current_spread` / `drv->current_mid_price`"

**Actual at HEAD:** `ShardedBacktestDriver_State<F>` exposes `book_imbalance` (NOT `current_book_imbalance`) at `:102` as `const FPN<F>* book_imbalance;` (pointer; nullable). No `current_` prefix.

**Severity:** CRITICAL — would COMPILE FAIL at Phase C body coding. Sister to NEW-2 (same struct-member discovery surface).

**Fix:** Replace `drv->current_book_imbalance` → `*drv->book_imbalance` (note: pointer deref needed; existing siblings `current_spread` + `current_mid_price` are ALSO pointer fields at `:119-120`).

#### NEW-4 (CRITICAL — sister to NEW-3) — pointer vs value field access

**Plan body cites:**
- Line 976 (Step C.4 code sample): `depth.spread = drv->current_spread;`
- Line 977 (Step C.4 code sample): `depth.mid_price = drv->current_mid_price;`

**Actual at HEAD:** `current_spread` + `current_mid_price` ARE actual field names at `ShardedBacktestDriver.hpp:119-120` — BUT they are `const FPN<F>*` POINTERS (not FPN values).

**Severity:** CRITICAL — would COMPILE FAIL ("cannot convert const FPN<F>* to FPN<F>"). Class 14 field-shape misalignment.

**Fix:** Use pointer deref pattern matching existing `:349-350`: `depth.spread = *drv->current_spread; depth.mid_price = *drv->current_mid_price;` (with null-check sister to existing `:307`).

### Combined fix recommendation for NEW-1 through NEW-4

The 4 NEW findings cluster into ONE Step C.4 BACKTEST code sample. Recommended consolidated rewrite (per `feedback_iteration_spiral_signals_audit_meta_gap` — comprehensive consumer-enumeration rather than piecemeal):

```cpp
// Depth snapshot (BACKTEST: driver re-exposes pointers from DepthReplayState
// for slow-path consumption per DataStream/DepthReplayState.hpp:46-66 + canonical
// pointer-deref pattern at ShardedBacktestDriver.hpp:307 + :349-350):
BookSnapshot<BACKTEST_FP> depth = BookSnapshot_Init<BACKTEST_FP>();
if (drv && drv->book_imbalance && drv->current_spread && drv->current_mid_price) {
    depth.imbalance = *drv->book_imbalance;
    depth.spread    = *drv->current_spread;
    depth.mid_price = *drv->current_mid_price;
    // (other BookSnapshot fields bids[]/asks[]/top_imbalance/update_count/
    //  last_update_id/timestamp_us — helper body doesn't read these at lambda
    //  body :3050-3058 scope; leave at default-init via BookSnapshot_Init)
}
```

Plus prose-body footer rewrite at line 988:
- Replace `Backtest/DepthReplayState.hpp` → `DataStream/DepthReplayState.hpp` (2 sites: lines 971 + 988)
- Replace `drv->current_book_imbalance` → `*drv->book_imbalance` (single site at line 988)
- Replace "field-mapping `drv->current_book_imbalance` / `drv->current_spread` / `drv->current_mid_price` matches LIVE BookSnapshot.imbalance/spread/mid_price" → "field-mapping `*drv->book_imbalance` (pointer to FPN) → BookSnapshot.imbalance + `*drv->current_spread` → spread + `*drv->current_mid_price` → mid_price; null-guards per sister pattern at ShardedBacktestDriver.hpp:307 + :349-350"

### Self-inconsistency residue (3 sites) — MED

In addition to the 4 NEW Class 14 fabrications, v1.7.3 amendment cycle left 3 internal-inconsistency residue lines that didn't get swept during the amendment:

#### C-2 (MED) — Step B.3 body span line range stale

- Line 742 (v1.7.3 N-5 amendment) says: `:3044-3311` (CORRECTED)
- Line 745 (Step B.3 body bullet) says: `:3036-3312` (STALE pre-v1.7.3 value)

Same plan body section; one line corrects, next line ignores own correction. Inconsistency catches future-reader. **Fix:** Update line 745 to `:3044-3311` to match line 742.

#### C-3 (MED) — D1-B rationale residue

- Line 749 (v1.7.3 N-3 RATIONALE REFRAME) says: "wrapper at :3799 reads cfg.lifecycle_cfg_flags DIRECTLY, NOT via slow-path-gate cache. D1-B is LEGITIMATELY NEW code applying the slow-path-gate cache pattern to breakeven for the first time"
- Line 752 (Step B.3 body bullet) says: "Non-regressive: matches existing wrapper pattern at `ControllerEventLoop.hpp:3799` in production since v5.14.9.B.0 (2026-05-10)"

Lines 749-750 reframe; lines 752 retains pre-N-3 framing. **Fix:** Rewrite line 752 to: "Non-regressive: cached predicate evaluates to same value as direct cfg read at slow-path-budget cost; sister wrapper at `ControllerEventLoop.hpp:3799` reads cfg directly (pre-v5.15.5.F.4d.1.B.4 pattern) — D1-B applies slow-path-gate cache discipline for first time per pattern fit"

#### C-4 (MED) — Rejected mask name residue

- Line 751 (v1.7.1 naming convention preserved): "`MASK_BREAKEVEN_ON_PROFIT` (slow-path-gate auto-generated symbol)... B-HIGH-1's proposed `MASK_GATE_BREAKEVEN_ON_PROFIT` rename reconsidered as over-correction"
- Line 753 (Step B.3 body bullet) cites: "cached `MASK_GATE_BREAKEVEN_ON_PROFIT` bit = 0 → branchless BITMAP_IS_SET returns 0"

Line 751 affirms canonical name `MASK_BREAKEVEN_ON_PROFIT`; line 753 cites rejected name `MASK_GATE_BREAKEVEN_ON_PROFIT`. **Fix:** Rewrite line 753: "cached `MASK_BREAKEVEN_ON_PROFIT` bit = 0 → branchless BITMAP_IS_SET returns 0"

### Class 14 recurrence_count update

Per v1.7.3 Step D.10.5 (line 1133): "Class 14 recurrence_count 5→8 (v1.7.3 added N-2 macro arg + N-3 rationale framing + N-6 body-args)".

**Actual count after this re-fire's findings:** 5 (prior) + 3 (v1.7.3 N-2/N-3/N-6 closures) + 4 (NEW-1/NEW-2/NEW-3/NEW-4 fabrications introduced AT v1.7.3 amendment cycle itself) = **12 recurrences**.

Note: NEW-1 through NEW-4 are technically still pre-coding plan-time catches (audit catching pre-coding), so the "zero production occurrences" Class 14 closure mechanism is still intact. But the META-recurrence pattern (v1.6 14a/14b/14c → v1.7.3 NEW-1/NEW-2/NEW-3/NEW-4 same body-content-enumeration surface) ALIGNS WITH the M6 META-discipline `feedback_enumerate_helper_signature_args_before_extract` that v1.7.3 itself codifies. The amendment cycle introduced the very fabrications its own NEW META-discipline targets.

**Recommendation:** Update Class 14 amendment at Phase D Step D.10.5 from "recurrence_count 5→8" to "recurrence_count 5→12" + add per-instance entries documenting NEW-1 through NEW-4 + cite v1.7.3 amendment cycle as the discovery event.

## Class 18 (mirror state/code) — KNOWN-7

N-4 fix landed correctly per detail above. Step D.10.5 amendment of recurrence_count 6→7 verified correct (N-4 mirror catch). No other Class 18 instances introduced by v1.7.3 amendment cycle.

## Class 27 (scalar cfg-mirror cache) — CLEAN

Verified at landed code `CoreFrameworks/EngineCommon.hpp`:
- `EngineCommon_BootPerCore` body (`:233-427`) — zero static fn-local cfg caches; const cfg reference signature enforces per-instance data flow per decision-time-data-binding-pattern
- `EngineCommon_SlowPathCycleOneCore` declaration (`:470-479`) — const cfg reference; per-instance scalar args (`price, volume, ts_us, now_tick`) per caller per O2 bytewise-identical math discipline
- `EngineCommon_SlowPathCycleAllCores` declaration (`:490-498`) — same shape; thin fan wrapper

No subsystem state created that mirrors per-instance cfg as scalar.

## Class 28 (branchless hand-wave) — CLEAN

D1-B BITMAP_IS_SET on cached gate bit IS sister-instance per H20 § "genuine binary predicate" exemption:
- Canonical wrappers at ControllerEventLoop.hpp:2344 (MASK_LAZY_REBUILD_ACTIVE), :3558 (MASK_WS_FLATTEN_ACTIVE) — same shape verified ✓
- N-3 RATIONALE REFRAME at plan body line 749 honestly identifies it as NEW code applying slow-path-gate cache pattern (not cosmetic-extension) — framing-honesty discipline preserved
- D1-C fn_ptr deferral rationale (TECH_DEBT-125) architecturally sound — OneCore signature divergence makes uniform fn_ptr premature

NOT a new Class 28 instance.

## NEW META-discipline (M6) verification

Per task spec section 9:

| Mechanism | Verdict |
|---|---|
| NEW memory at `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/feedback_enumerate_helper_signature_args_before_extract.md` | LANDED ✓ (7197 bytes; established 2026-05-26 00:13) |
| MEMORY.md index update | LANDED ✓ (line 55; uses categorical pattern triggers per `feedback_categorical_triggers_over_hardcoded_refs`) |
| Plan body Phase D Step D.10.5 codification mechanism enumeration | LANDED ✓ (line 1127-1134 enumerates 6 mechanisms: memory + MEMORY.md + /readiness Check 33 + DESIGN_SPEC + CI tool extension + Class recurrence_count amendments) |
| /readiness Check 33 amendment | QUEUED for Phase D ship close (per plan body :1130) |
| DESIGN_SPEC candidate at `DESIGN_SPECS/meta-disciplines/body-content-enumeration-at-plan-time-discipline.md` | QUEUED Stage 2 DRAFT (per plan body :1131) |
| CI tool extension at `tools/check_plan_body_symbol_existence.py` per-callee parameter-list grep | QUEUED for Phase D (per plan body :1132) |
| Class recurrence_count amendments | QUEUED for Phase D (per plan body :1133; needs update from 5→8 to 5→12 per this re-fire's NEW-1/NEW-2/NEW-3/NEW-4 catches) |

M6 codification mechanism well-formed; ship-close items correctly queued at Phase D.

## NEW tiered audit discipline candidate verification (Phase D)

Per task spec section 10:

Plan body Step D.10.5 expanded scope includes:
- M6 META-discipline codification (verified above)
- /readiness Check 33 amendment (verified queued)
- DESIGN_SPEC sister candidate for `.B.5-.B.11` umbrella scope (per plan body :1134 — "pre-draft source structure enumeration discipline")

**Tiered audit discipline as standalone codification NOT separately enumerated** in Step D.10.5 — but the SHAPE is captured via the sister memory candidate at line 1134 ("sub-file boundaries + function counts + macro existence verified vs grep before plan body lock") which is the umbrella-scope generalization of M6.

Recommendation: Phase D Step D.10.5 should explicitly add tiered-audit-discipline sister memory as its own codification artifact (separate from sister memory candidate for `.B.5-.B.11`) — current entry conflates the two. Track as MED amendment for v1.7.4 if other amendments fire.

## Inflection check per `feedback_iteration_spiral_signals_audit_meta_gap`

Per task spec inflection criteria:

| Criterion | Result |
|---|---|
| ZERO new Class instances = inflection REACHED | NO — 4 NEW Class 14 instances surfaced (NEW-1/NEW-2/NEW-3/NEW-4) |
| 1-2 mechanical/cosmetic findings = mechanical-only follow-ups OK | NO — 4 findings are CRITICAL substantive (compile-fail surface) |
| 3+ NEW substantive findings = iteration spiral risk (escalate to operator) | YES — 4 substantive findings cluster at single code sample (Step C.4 BACKTEST caller) |

**However, recognize the META-pattern:**
- v1.6 cycle: 3 fabrications caught (14a + 14b + 14c)
- v1.7.3 cycle: 4 fabrications caught (NEW-1 through NEW-4)
- Same surface: Step C.4 / body-content-enumeration / external struct field reads

This is NOT iteration spiral of the kind that `feedback_iteration_spiral_signals_audit_meta_gap` targets (where amendments find smaller-and-smaller findings across cycles). This IS the SAME MARGINAL FINDING SHAPE recurring at v1.6 → v1.7.3 — which the v1.7.3 M6 META-discipline codification ITSELF targets via body-content-enumeration discipline + CI tool extension at Phase D D.6.

**The v1.7.3 amendment fix introduces fabrications of the very class the v1.7.3 amendment cycle codifies a META-discipline against.** Meta-recurrence catch confirms M6 codification timing is correct — Phase D ship-close mechanisms (CI tool extension + /readiness Check 33) would catch NEW-1 through NEW-4 at amendment-time going forward.

**Operator escalation framing:** Cluster of 4 fabrications at single sample IS audit working (no production occurrences; pre-coding catch). But the v1.7.3 author committed the same fabrication class the v1.7.3 amendment cycle was codifying a discipline against → operator decision warranted on (a) whether v1.7.4 amendment cycle is needed to fix NEW-1 through NEW-4 BEFORE Phase B Step B.3 unlocks, OR (b) whether to land M6 CI tool + Check 33 FIRST (per Phase D Step D.6 / D.10.5) so v1.7.4 amendment gets caught by the new tooling, OR (c) fold the BACKTEST caller rewrite into Phase C coding-time with explicit pre-coding `rg` enumeration discipline.

## Phase B Step B.3 unlock recommendation

**AMEND.**

v1.7.3 CRITICAL closures (N-2 / N-3 / N-4 / N-6) all PASS — substantive amendment cycle correctly resolved the 4 CRITICALs surfaced by 5-parallel-agent audit at v1.7.2.

But the v1.7.3 amendment cycle itself introduced 4 NEW CRITICAL Class 14 fabrications (NEW-1 / NEW-2 / NEW-3 / NEW-4) inside the Step C.4 BACKTEST caller code sample + prose. All 4 would cause compile-fail at Phase B Step B.3 OR Phase C body coding.

Plus 3 self-inconsistency residue lines (C-2 / C-3 / C-4) inside the same v1.7.3 amendment cycle that didn't get swept during the amendment.

**Recommended sequence:**
1. v1.7.4 amendment cycle: fix NEW-1/NEW-2/NEW-3/NEW-4 (Step C.4 BACKTEST caller rewrite per "Combined fix recommendation" above) + sweep C-2/C-3/C-4 self-inconsistency residue
2. Targeted re-fire `/bug-check` on v1.7.4 spec to verify inflection (expected: ZERO new Class instances; only mechanical-only follow-ups if any)
3. Then Phase B Step B.3 body coding unlocks

**Alternative sequence (operator decision):**
- Land M6 Phase D D.6 CI tool + /readiness Check 33 FIRST (per Phase D Step D.10.5 amendment) → v1.7.4 amendment naturally gets caught by new tooling
- Or fold BACKTEST caller rewrite into Phase C coding-time per `feedback_categorical_triggers_over_hardcoded_refs` discipline (mark Step C.4 code sample as TBD-at-coding-time + rely on `rg`-enumeration pre-coding pattern)

Note: the iteration spiral concern is REAL but the v1.7.3 M6 codification ITSELF is the structural fix for it. The amendment cycle's META-recurrence catch (this re-fire) validates that the M6 codification timing was correct. Per `feedback_iteration_spiral_signals_audit_meta_gap`: "STOP individual-finding-chase; codify META-gap immediately; apply comprehensive sweep with new discipline; verify inflection" — v1.7.3 codified META-gap; this re-fire surfaces the comprehensive sweep findings; verifying inflection should happen at v1.7.4 OR Phase B Step B.3 trial compile.

**Cross-methodology validation:** /bug-check at v1.7.3 caught 4 NEW CRITICAL fabrications that the v1.7.3 amendment cycle itself missed → 5-agent parallel audit + cross-methodology validation continues to be structurally MORE complete than any single agent. Sister to `feedback_iteration_spiral_signals_audit_meta_gap` (audit methodology gap signal).

## Auto-write contracts triggered

Per CLAUDE.local.md Auto-write contracts § "Parity findings" + § "Deferred items":

- **NEW PARITY-NNN candidate:** none (Class 14 fabrications are plan-time + pre-coding; do not warrant PARITY ledger entry per plan-time scope filter)
- **NEW TECH_DEBT-NNN candidate:** consider TECH_DEBT entry for "v1.7.3 amendment cycle introduced 4 Class 14 fabrications + 3 self-inconsistency residues at Step C.4; M6 META-discipline codification timing validated by this re-fire" — operator decision whether to ledger at v1.7.4 amendment OR fold into Phase D Step D.10.5 postmortem section
- **DOCS/RECURRING_BUG_PATTERNS.md Class 14 amendment:** Phase D ship close should bump recurrence_count from 5→12 (not 5→8 as currently spec'd at plan body :1133); add per-instance entries for NEW-1 through NEW-4 + cite "v1.7.3 amendment cycle 2026-05-26 — meta-recurrence catch validating M6 META-discipline codification timing"

## Cross-references

- v1.7.3 amendment summary: plan body lines 5, 19, 33, 37-89
- M6 META-discipline memory: `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/feedback_enumerate_helper_signature_args_before_extract.md`
- M6 codification mechanism enumeration: plan body lines 1127-1134
- v1.7.2 → v1.7.3 audit synthesis sources cited at plan body lines 43-47
- Landed code: `CoreFrameworks/EngineCommon.hpp` (500 LOC; HEAD `c2595d6`)
- Sister memory candidates queued at Phase D Step D.10.5 for `.B.5-.B.11` umbrella scope
- Sister anti-pattern: Class 14 per `DOCS/recurring-bug-patterns/class-14-plan-calls-non-existent-function.md` (5 instances pre-v1.7.3)
- Sister meta-recurrence pattern: v1.6 14a/14b/14c → v1.7.3 NEW-1/NEW-2/NEW-3/NEW-4 (same body-content-enumeration surface)

---

**End of report.** Returned for operator review per consult-before-coding discipline; never auto-proceeds.
