---
type: subplan-scaffold
ship_tag: v5.15.5.F.4d.1.E.1.1
ship_kind: mechanical-rename + cfg-key clean-break
parent: 2026-05-28-v5.15.5.F.4d.1.E.1-foundation.md  # sub-master (umbrella)
predecessor: 2026-06-15-v5.15.5.F.4d.1.E.1.0-guard-hole-closure.md
successor: 2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md
status: in-flight (mid-dive; full body expanded — §"Gate synthesis" + §"State-audit refinements" are the dive SSoT. single_core deletion COMPLETE: Phase 1+2+3 landed 3eadb53/7eacb80/2baf0f6/b9ce419 [Phase 3 = engine_mode delete + parity_harness retire; TD-140 closed, TD-185→E.1.3]; ① the TD-175 cascade tool BUILT + completeness-validated [TD-175a CLOSED, D-240; freeze=3645]; ② the Core→Node mechanical rename LANDED [engine 1da1c1c + workspace c356e03; 3635/0 + determinism + conformance + calls_graph + Check-H all GREEN; both pre-commit hooks clean]; NEXT = ③ the cfg cluster [HARD-REFUSE + C-2 guard + operator cfg migration] → ⑤ V-class sanitizers — see §"Execution update (2026-06-21)")
established: 2026-06-15
spine: FOUNDATION (no spine)
risk: MED  # massive mechanical surface, but pure identifier substitution on FROZEN logic under E.1.0's net; the cfg clean-break is the only behavior change
audit_tier: HIGH  # deletion-class (single_core removal) + cfg clean-break; received 7-agent /precoding-audit-gate + 8-agent state-audit (added 2026-06-20 — was missing; flagged by /capture-audit Check 2)
hot_path: TOUCHED (names only — bytes/logic unchanged; E.1.0's H7/H8 gates prove no perf/branch regression)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
dependency_graph: plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md
guard_matrix: plans/v5.15-live-readiness/E-guard-coverage-matrix.md
rollback_anchor: pre-v5.15.5.F.4d.1.E.1.1
sister_specs:
  - meta-disciplines/plan-hierarchy-and-sub-master-decomposition.md
  - meta-disciplines/dead-code-and-identifier-retirement-discipline.md  # H21 NAME-vs-bit; cfg-key clean-break (Rule 2 reclaim-not-freeze)
  - meta-disciplines/per-node-purity-scale-invariance.md  # the Core→Node rename IS the terminology bridge for H22
  - data-disciplines/per-node-position-ownership-model.md  # NodeContext/owner_node_id terminology forward-ref
---

# E.1.1 — Core→Node mechanical rename + cfg-key CLEAN-BREAK

**Status:** SCAFFOLD (2026-06-15). Full coding body expanded by a C-class re-ground pass when this becomes the in-flight dive (the grep COUNTS below WILL drift by then — RE-DERIVE at dive). Second leaf of the FOUNDATION cluster.

## Scope
The mechanical `Core → Node` identifier substitution across the codebase on the FROZEN hot path (names only — bytes + logic unchanged; runs UNDER E.1.0's net which proves no perf/branch/determinism regression), PLUS the cfg-key CLEAN-BREAK (D-219/D-223: rename `core_N_*` → `node_N_*` keys AND make the parser HARD-REFUSE an unrecognized legacy key — loud failure, NOT a tombstone/alias — closing the Knight silent-revert-to-default class structurally; operator migrates her own `engine.cfg`), PLUS the `single_core` LIVE deletion (D-61). The snapshot VERSION is NOT touched here (E.1.2 owns it — this leaf is identifiers only; H21 protects persisted-wire VERSIONs, which the rename never reaches).

## Spine / layer
FOUNDATION (no spine). Pure identifier substitution → bisectable as "a rename typo, never a layout race or aggregator change" (A-adjudication §2 E.1.1). One rollback anchor per concern (the rename) — orthogonal to E.1.2's relayout anchor (D-225 resolution #2: name-only vs bytes-only are different concerns with different failure modes → 2 anchors).

## TD-closed
- **TD-197** (cfg-flag-orphan retirement): the HARD-REFUSE parser SUBSUMES the orphan-key class — an unrecognized flag now fails loud instead of silently reverting to default. The reclaim-not-freeze retirement (D-219) batches the actual cfg-flag retirements here, co-landing with the legacy-`PortfolioController` reader removal.
- **TD-002** (PARTIAL — the `single_core` sub-deletion): D-61 `single_core` LIVE full removal lands here. NOTE per A-adjudication §4(a): TD-002 is the ONE non-atomic TD — its `single_core` deletion lands in E.1.1; its ControllerEventLoop *central drainer* removal is the substantive close in E.1.3. Cross-linked, not double-counted.
- **TD-175** (build-and-use the struct-cascade-impact tool): `tools/check_struct_alignment.py` ALREADY EXISTS (+ `tools/test_check_struct_alignment.py`); this leaf BUILDS the rename-cascade-enumeration piece on top of it (the consumer-site enumeration that de-risks the ~hundreds-of-sites edit) + USES it on the rename, then E.1.2 reuses it for the SoA relayout.
- **TD-129 / TD-135** (rename-adjacent stale naming): VERIFY-subsumed at ship close — TD-135 `regime_names[]` hardcoded sibling-array confirmed via `/registry-fit-audit` (A-adjudication §4(a)).

## Locked decisions honored
- **D-27** (Core→Node hard rename, Option α): all `state.cores[i]`→`state.nodes[i]`, `MAX_CORES`→`MAX_NODES`, `FOREACH_PER_CORE_CFG_FIELD`→`FOREACH_PER_NODE_CFG_FIELD`, `CoreContext`→`NodeContext`, per-core dirs→per-node dirs. *(NOTE: the REAL symbol is `MAX_EXECUTION_CORES`, not the decision-log shorthand `MAX_CORES` — see Design-shape; rename target `MAX_EXECUTION_NODES`.)*
- **D-61** (dead-code deletion folded into `.E.1`): `single_core` LIVE removed.
- **D-219 / D-223** (cfg-key CLEAN-BREAK + hard-refuse, NOT alias): the `core_N_*` keys are operator-config TEXT (name-keyed, parsed from `engine.cfg`), NOT wire/HMAC-signed → clean-break + HARD-REFUSE the unrecognized key, operator migrates. This REFINES trace-deps' "extend FOREACH_LEGACY_PREFIXED_KEY" (alias) → clean-break+refuse. The v0.1 `core_N_*`-alias tombstone slip is explicitly NOT reintroduced (A-adjudication §3 REFUTE — verified clean).
- **D-131** (epoch-free): no snapshot/stamp break here (identifiers only); the discipline cost is ~0 regardless.
- **H21**: the snapshot VERSION + persisted-wire identifiers are NOT touched (E.1.2 owns the VERSION bump); the cfg-KEY clean-break is correct precisely because keys are operator-text not wire (D-223 distinction: persisted-wire identifier → tombstone; operator-config text → clean-break).

## §4 guard-rows it fills
None NEW. RUNS UNDER E.1.0's net — its value to the matrix is PROVING the net catches a careless-rename hot-path regression (H7/H8/H10/H20 gates stay green across a names-only edit, or RED-flag a slip). No new guard rows; the determinism gate (`±USE_NATIVE_128` byte-compare) must stay byte-identical across the rename (names don't change bytes).

## INBOUND seam (needs from predecessor — E.1.0)
E.1.0's STANDING CI green + armed: the H7/H14/H20 grep-CI + the H10 determinism gate + the H8 latency ratchet, all live, so the rename's correctness is mechanically gated (a names-only edit cannot regress perf/branch/determinism without RED-building). The baseline p99 (captured at E.1.0) is the reference the rename must not exceed.

## OUTBOUND seam (invariant the successor relies on — E.1.2)
A fully-renamed tree on FROZEN layout: NO `Core*`/`MAX_EXECUTION_CORES`/`CoreContext`/`state.cores`/`core_id`/`per_core_`/`FOREACH_PER_CORE_CFG_FIELD` residue (the RE-DERIVED grep returns empty); the parser HARD-REFUSES legacy `core_*` keys; `single_core` gone; controller_test green + byte-behavior-neutral (the determinism golden byte-identical to E.1.0); the rename-cascade tool exists for E.1.2 to reuse on the SoA relayout. **E.1.2 relayouts a tree whose names are already final** — it touches BYTES on FROZEN names, the orthogonal anchor.

## Design-shape — RE-GROUNDED (C-1, 2026-06-17, HEAD `90b2daf` [.E.1.0])

Verbatim greps; **collision PASS** — all six target names 0-hit today → clean substitution holds. Counts drifted UP vs the scaffold (under-scoped) → the cascade tool runs the WHOLE source tree, not the union of these lists.

**★ PRESERVE-list (do NOT rename — the executor needs explicit exclusions; rename-ship-methodology "verify the EXECUTOR against the substring matrix"):**
- **`ExecutionCore`/`ExecutionCore_Tick`/`ExecutionCore_*`** — the hot-path execution ENGINE (a node OWNS one), NOT the node/shard concept; renaming breaks the standing conformance gate (its kernel manifest names `ExecutionCore_Tick`). `ExecutionCore.hpp` does NOT rename.
- **`CoreFrameworks`** — the directory/namespace in every `#include`/build file (out of scope).
- **`MULTICORE_TUI`** + generic "CPU core" comments.

**Rename surface (RE-DERIVED):**
- `MAX_EXECUTION_CORES` → `MAX_EXECUTION_NODES`: **16 files** (list unchanged). `MAX_CORES` = 0 (phantom). NAMING DECIDED: keep verbose `MAX_EXECUTION_NODES` (deployment-wide cap; avoids E.1.2's `MAX_NODES_PER_CLUSTER`).
- `num_execution_cores` → `num_execution_nodes`: a `cores` token (own pattern); ALSO a cfg-KEY → another clean-break/HARD-REFUSE key (operator migrates).
- `CoreContext` → `NodeContext`: `:251`; **4** static_asserts @ `:555/:559/:566/:576` (scaffold's `:555-566` missed `:576`); `CoreContextDisplayMeta` @ `:651` + `_Init` @ `:691`. HAZARD: `CoreContext` ⊂ `CoreContextDisplayMeta` → anchored single-pass ONLY (non-idempotency → `NodeNodeContext`).
- `.cores[` → `state.nodes[i]`: **22 files** (unchanged).
- `core_id` → `node_id`: **389 hits** (was 351; ~35 files, broader than any list). `core_idx` → `node_idx`.
- `per_core_*` → `per_node_*`: **231 hits** (was 217). Families w/ own anchored patterns: `PerCoreOverrides`/`PER_CORE_OVERRIDE_*`/`PerCoreFieldDef`/`per_core_fields`/`FOREACH_MANUAL_PER_CORE_FIELD`/`MAX_GUI_CORES`.
- `FOREACH_PER_CORE_CFG_FIELD` → `FOREACH_PER_NODE_CFG_FIELD`: **7 files** (unchanged). H15 enrollment @ `MetaRegistry.hpp:40` (name + desc rename). CI check `check_per_core_registry_integrity.py` → `check_per_node_registry_integrity.py` (+ ALL refs: hook, `check_session_docs.sh`, `CoreFrameworks/CLAUDE.md`, Version.hpp).
- `CoreCtx*`/`CORE_CTX_*`, `CORE_STATE_FLAG`/`MASK_CORE_STATE_*`/`FOREACH_CORE_STATE_FLAG`, `CoreModelZoo`/`CoreLatencyStats` → `Node*`.
- COLLISION PASS: `FOREACH_PER_NODE_CFG_FIELD`/`MAX_EXECUTION_NODES`/`NodeContext`/`node_id`/`per_node_`/`.nodes[` = ALL 0 hits.

**① FILE-BASENAME RENAMES (DECIDED IN-SCOPE; separate enumerated sub-phase):** 6 `Core*`→`Node*` files: `ML_Headers/CoreModelZoo.hpp`(13 includers), `MemHeaders/{CoreCtxInitRegistry(2),CoreCtxSummaryFieldRegistry(1),CoreStateFlagRegistry(3),PerCoreStateFlagsRegistry(4)}.hpp`, `CoreFrameworks/CoreLatencyStats.hpp`(4) = **~27 `#include` sites** + include-GUARD macro rename for `#ifndef`-guarded files (`CORE_MODEL_ZOO_HPP`→…; `CoreLatencyStats` is `#pragma once`, no macro). NO build/tool path refs (clean). Missed include/guard = RED build (compile-caught). **The `#include`-cascade enumeration is a TOOTH in the TD-175 cascade tool** (operator: "add it to the tool we check the other stuff with").

**cfg-parser CLEAN-BREAK — TWO parsers:**
- **(A) `ControllerConfig.hpp`**: 8 `strncmp(key,"core_",5)` branches `:2878/:2886/:2897/:2905/:2918/:2931/:2941` + catch-all `:2968`. **★ `:2968` is NOT the silent-swallow — it OPENS the ~130-line per-core OVERRIDE block (`:2968-3099`: `PER_CORE_OVERRIDE_FIELDS` + bitmap-domain walker + `horizon_list`/`ensemble_blend_mode`/`disabled_horizons`/`feature_mask`), each `continue`. Refusing AT `:2968` deletes every override.** HARD-REFUSE goes at the **outer `:2968`-block fall-through (between `:3098` and `:3099`)** — both legs land there → covers unknown-suffix AND bad-index — PLUS each of the 8 recognized-suffix branches refuses on out-of-range `idx` (today they silently `continue`). [CORRECTED by gate C-1: `~:3097` is inside the valid-index block, and a bad index never reaches it — see §Gate-synthesis.] Offset stays 5. No alias branch (D-223 honored).
- **(B) `GUI/SettingsPanel.hpp:922`**: a SECOND `strncmp(p,"core_",5)` parser (own `core_idx`, bound `MAX_GUI_CORES`, sub-branches `:941-959` + `per_core_fields[]` walk) reading the SAME keys → rename + HARD-REFUSE in LOCKSTEP or the GUI silently diverges.

**WRITE-side lockstep (round-trip):** GUI cfg-WRITERS `snprintf(…,"core_%d_…")` @ `SettingsPanel.hpp:1309/1334/1350/1387/1400/1415/1597/1664` — rename or the GUI saves `core_` keys the renamed parser then refuses.

**Persisted/emitted NAME surfaces (operator-facing; H21-SAFE — binary snapshot is INDEX-keyed):** trade-log filenames `…_core_%d_order_history.csv` (`ShardedTradeLog.hpp:100`), `core_%d.csv` (`Async.hpp:670`), JSON key `"core_id"` (`NodeCtxSummaryFieldRegistry.hpp:206`). Old logs keep old names → migration note.

**TD-175 cascade tool**: reuse `tools/check_struct_alignment.py` for layout-impact; build the WHOLE-tree rename-consumer-enumeration + the ① `#include`-cascade tooth (per `feedback_enumerate_consumers_before_registry_row_deletion`) for the 389+231+22-file + 6-file-rename surface.

---

## Design-shape (SUPERSEDED — scaffold-time counts vs `572c978`; retained for audit trail; see RE-GROUNDED above)
**Real rename symbols + RE-DERIVED surface (per `feedback_paste_tool_output_dont_summarize` — pasted verbatim from the scaffold-time grep; STALE by the dive):**
- `MAX_EXECUTION_CORES` → `MAX_EXECUTION_NODES`: **16 files** — `Backtest/BacktestSharded.hpp`, `CoreFrameworks/{CfgFieldRegistry,ControllerConfig,ControllerEventLoop,EngineCommon,OrderManager,Reconcile,ShardedSnapshotPersist,ShardedTradeLog}.hpp`, `CoreFrameworks/EngineSharded/{Async,Run}.hpp`, `DataStream/EngineTUI.hpp`, `Limits.hpp`, `MemHeaders/OmsFieldRegistry.hpp`, `Strategies/StrategyLifecycle.hpp`, `Version.hpp`. (`MAX_CORES` = **0 hits** — the decision-log shorthand is a PHANTOM; the real symbol is `MAX_EXECUTION_CORES`.)
- `CoreContext` → `NodeContext`: `struct alignas(64) CoreContext` at **`CoreFrameworks/ControllerEventLoop.hpp:251`** (the per-core slot context; layout-invariant static_asserts at :555-566); `struct CoreContextDisplayMeta` → `NodeContextDisplayMeta` at **`:651`** (display-only meta). NO `CoreContext.hpp` file — both inline in `ControllerEventLoop.hpp`.
- `state.cores[i]` / `.cores[` → `state.nodes[i]`: **22 files** (`Backtest/{BacktestEngine,BacktestSharded}.hpp`, `CoreFrameworks/{CfgFieldRegistry,ControllerConfig,ControllerEventLoop,EngineCommon,HotSwap,LiveReadiness,OrderManager,PaperResetArchive,PortfolioController,Reconcile,ShardedSnapshotPersist}.hpp`, `CoreFrameworks/EngineSharded/{Async,Run,SlowPath}.hpp`, `GUI/SettingsPanel.hpp`, `MemHeaders/{CoreCtxInitRegistry,CoreCtxSummaryFieldRegistry,OmsFieldRegistry}.hpp`, `ML_Headers/RollingTurnover.hpp`, `Version.hpp`).
- `core_id` → `node_id`: **351 hits**. `per_core_*` → `per_node_*`: **217 hits**. (Bulk mechanical; the cascade tool enumerates the consumer sites.)
- `FOREACH_PER_CORE_CFG_FIELD` → `FOREACH_PER_NODE_CFG_FIELD`: **7 files** (`CoreFrameworks/{CfgFieldRegistry,ControllerConfig,MetaRegistry}.hpp`, `GUI/SettingsPanel.hpp`, `MemHeaders/CfgGateRegistry.hpp`, `ML_Headers/StampHelper.hpp`, `Version.hpp`). MetaRegistry enrollment (H15) renames with it — verify the meta-registry row + the CI check (`check_per_core_registry_integrity.py` → `check_per_node_registry_integrity.py`).
- `single_core` (D-61 delete): **16 files** (`Backtest/BacktestEngine.hpp`, `CoreFrameworks/{CfgFieldRegistry,ControllerConfig,ControllerEventLoop,EngineSharded,LegacyReferenceDriver,OrderManager,PortfolioController,ShardedLiveSafety,ShardedSnapshot,ShardedTradeLog}.hpp`, `GUI/{DashboardPanels,SettingsPanel}.hpp`, `main.cpp`, `Strategies/StrategyParameters.hpp`, `Version.hpp`).
- **Collision check**: `FOREACH_PER_NODE_CFG_FIELD` / `MAX_EXECUTION_NODES` / `NodeContext` = **0 hits** today → no target-name collision (clean substitution).

**cfg parser CLEAN-BREAK surface** (the HARD-REFUSE edit): the **8** `strncmp(key, "core_", 5) == 0` branches at `CoreFrameworks/ControllerConfig.hpp:2878-2968` — `_time_exit_ticks` (:2878), `_risk_pct` (:2886), `_max_drawdown_pct` (:2897), `_model_path` (:2905), `_symbol` (:2918), `_model_dir` (:2931), `_strategy` (:2941), and the **catch-all `if (strncmp(key,"core_",5)==0)` at :2968**. Rename the recognized branches to `node_`; turn the :2968 catch-all (currently the silent-swallow) into the HARD-REFUSE (loud error + abort-load) for any unrecognized `core_*` OR `node_*` key. (Each branch validates `idx >= 0 && idx < 16` via `atoi(key+5)` — the index parse offset stays 5 since `core_`/`node_` are both 5 chars; verify at dive.)

**TD-175 cascade tool**: `tools/check_struct_alignment.py` (+ test) EXISTS — reuse for the layout-impact half; build the rename-consumer-enumeration piece (one comprehensive grep across all access patterns + file types per `feedback_enumerate_consumers_before_registry_row_deletion`) as the de-risk for the 351+217+22-file mechanical surface.

## Rollback anchor
`pre-v5.15.5.F.4d.1.E.1.1` (create at the dive's Phase A.0). Bisectable: pure identifier substitution on FROZEN logic → a test break is a rename typo (or the deliberate cfg clean-break), never a layout race or aggregator change.

## Known folds / findings it owns
- **The cfg clean-break IS a behavior change** (the one in this otherwise-mechanical leaf): an operator's existing `engine.cfg` with `core_N_*` keys will HARD-REFUSE after this lands → **Operator migration impact** (mandatory SECTION per `feedback_backwards_compat_not_default_concern`): the operator renames her `core_N_*` keys to `node_N_*`. Loud-fail-by-design (closes the Knight silent-revert class) — surface this in FEATURE_LOOKUP + the ship's migration note. No SOFT-compat alias (D-219/D-223 clean-break is the decided posture).
- **MetaRegistry rename ripple** (H15): `FOREACH_PER_CORE_CFG_FIELD`'s meta-registry enrollment + the integrity CI check rename together — a sister-cohort amendment (don't rename the registry and leave the check pointing at the old name).
- **GUI per-core display strings** (`GUI/SettingsPanel.hpp`, `DashboardPanels.hpp`): the operator-facing "Core N" labels → "Node N" — display↔execution terminology must move together (no half-renamed UI).
- **Naming RESOLVED** (C-1): keep `MAX_EXECUTION_NODES` (deployment-wide cap; avoids E.1.2's `MAX_NODES_PER_CLUSTER`).
- **① File-basename renames DECIDED IN-SCOPE** (separate sub-phase; ~27 `#include` + guard-macro cascade folded into the TD-175 tool — see RE-GROUNDED Design-shape).
- **② `single_core` CLEAN DELETE — NO tombstone** (DECIDED; `engine_mode` verified NOT persisted/wire → H21 doesn't force a tombstone; operator anti-bricked-bits preference). **Phase 1+2+3 LANDED — single_core deletion COMPLETE** (`3eadb53` orphan + `7eacb80` legacy-engine cut + `2baf0f6` include-prune + **`b9ce419` Phase 3**): the legacy single-threaded engine + dispatch + the NEW-1 single_core refuse are GONE from `main.cpp`; Phase 3 deleted the `engine_mode` field via its `CfgFieldRegistry.hpp:393` registry row (H17 — auto-cascaded the X-macro struct field + both `ENGINE_MODE_*` enum constants + the parser branch + the 2 hot-reload readers) + retired `parity_harness` (CMake target + source + cohort). **TD-140 CLOSED**; **TD-185→E.1.3** (coupling verified — see below). All gates green (build 3635/0 · fresh asan/ubsan 3635/0 · conformance + determinism golden byte-identical + calls_graph clean · pre-commit M/H/K green, H21: engine_mode confirmed non-persisted). ~18 stale `engine_mode` COMMENTS across 12 files remain (gate-on-CODE-residue per the 8-agent finding; CODE clean; the earlier "~15" undercounted per the 2026-06-21 independent review) → swept WITH the rename. KEEP: predicate `ControllerConfig_IsLiveCapital` (**7** surviving sharded callers — the 10 single_core sites died with main.cpp) + `PortfolioController` the STRUCT (deeper central-drainer removal = E.1.3, the non-atomic TD-002 half). Leaves-first deletion ORDER = the gate's B14 pass.
- **③ gate-scope `_PER_CORE` → `_PER_NODE` (DECIDED — rename for consistency; "each node uses 2 cores" = a real semantic diff).** + **CORRECTED:** the old "benign micro-note" was WRONG — the global-key refuse is NOT optional (`engine_mode=single_core`→silent sharded-LIVE; `num_execution_cores=8`→silent revert-to-4). Both detailed in §Gate-synthesis below (the SSoT for the dive scope).

---

## Gate synthesis — amendments + corrected dive scope (2026-06-17 · /precoding-audit-gate · RED→amended)

7-agent deletion-class gate (4 investigative + 2 adversarial + completeness-critic). **Verdict RED (major amendment, NOT re-design):** every decision holds (PRESERVE-boundary + determinism byte-identical + H21 all CONFIRMED), but the enumeration was systematically under-scoped, the cfg-clean-break has a concrete operator-brick, and the de-risk tooling can't see the guards it must update. **This section is the SSoT for the dive scope**; the C-1 RE-GROUND above is what the gate built on.

### 🔴 CRITICAL
- **C-1 · HARD-REFUSE location + completeness.** Refuse at the **outer `:2968`-block fall-through (`:3098`-`:3099`)** — both unknown-suffix AND bad-index land there — PLUS each of the 8 recognized-suffix branches refuses on `idx` out-of-range (today silent `continue`). `~:3097` (inside the valid-index block) misses bad-index. (I-3 + A-2 + orchestrator code-read.)
- **C-2 · round-trip operator-brick (GUI↔engine key-mirror drift).** GUI writes `core_N_confidence_freshness_tau` (`SettingsPanel.hpp:669/1664`) for an override the engine DELETED (`ControllerConfig.hpp:172`) → after the HARD-REFUSE the GUI bricks its own cfg. Root: GUI `per_core_fields[]` and engine `PER_CORE_OVERRIDE_FIELDS` are two hand-mirrors that already drifted. **DISPOSITION (in E.1.1):** delete the stale GUI row + add `tools/check_gui_engine_cfg_key_parity.py` (GUI-writer-key ⊆ engine-parser-key), teeth-proofed on this drift → HARD-REFUSE safe-by-construction. Full mirror single-source = later ship (**homed: TECH_DEBT-208**, the structural single-source; the E.1.1 guard makes deferring safe).

### 🟠 HIGH
- **§HIGH-globals — global-key refuse scoped IN.** Deleted/renamed global keys (`engine_mode`, `num_execution_cores`) HARD-REFUSE loud, else "closes the Knight class structurally" is false (silent sharded-LIVE / silent node-count revert).
- **tools/ cohort vacuously-green (Class-51); the cascade tool + OUTBOUND done-grep are source-only → WIDEN both to scan `tools/`+`build.sh`+`.githooks/`.** Same-commit rename + teeth RED→GREEN: `scan_class_27_full.py:272` (the Class-27/H22 scanner — silent-pass if missed), `check_identifier_retirement.py` (**commit-blocker** — Check H freezes `CORE_STATE_FLAG_*`; co-migrate ledger+SOURCES via `--update`), `check_storage_t_coverage.py`, `check_field_name_uniqueness.py`, `scan_class_44_cfg_orphan.py`, `build.sh:268/271` (`[ -f ]` silent-skip if the file renames but build.sh doesn't), `check_meta_registry.py:28`, `tools_verification_baseline.txt`, `check_doc_rename_classification.py` (its KEEP_TOKENS deferred code-symbols TO E.1.1 — co-migrate). The integrity check's INTERNAL body hardcodes the old names (coupled rename, not a `git mv`).
- **② re-scope (delete-set + 3-phase order).** Missing live sites: `Async.hpp:325` (sharded reload), `PortfolioController._HotReload:1968-81` (kept struct), `parity_harness.cpp` (live target), `CfgFieldRegistry.hpp:393` (the `engine_mode` REGISTRY ROW — H17; "delete the field" = delete the row + decrement any field-count assert), `controller_test.cpp:1624`. IsLiveCapital corrected: **7 surviving callers / 10 die-in-block** (not "~20, only :303"). PortfolioController KEEP-set ⊃ tests/backtest (also EngineTUI/MetricsLog/ShardedSnapshotPersist) — do NOT trim its fns; do NOT collateral-delete `num_execution_cores`. **3-phase leaves-first:** (1) orphan `LegacyReferenceDriver.hpp`; (2) legacy `main.cpp:~286→end` + dispatch-rewrite-to-unconditional-sharded + NEW-1 refuse (atomic); (3) `engine_mode` field/registry-row/enum/parser + the 2 surviving readers + parity_harness disposition + the tool cohort (atomic).
- **H15 = 10 meta-rows, not 1.** `MetaRegistry.hpp` `:40-43,:80-81,:85-87` + the 5 child `PARENT_NAME` refs (`:46-50`, H19 topology) + `FOREACH_PER_CORE_FIELD_TYPE`/`FOREACH_CORE_CTX_SUMMABLE_FIELD`. `MANUAL_FIELDS_INVENTORY.md` Section A is Check-4-bound (the 11 `core_*` field names rename or RED).
- **parity_harness disposition** (its single_core↔sharded diff is deleted): retire OR repoint to sharded-vs-sharded.

### 🟡 MED/LOW
RE-DERIVE counts via the tool (the done-grep is itself incomplete — 7 patterns vs ~600 `core_<stat>`) · PRESERVE-list ADD `FoxML_Core` (sister-project name in comments) + the `experiments/per_core_sharding/` path · `MLStatusPanel.hpp` 4 runtime `"core %d:"` strings (not compile-caught) · the tracked `engine_sharded.cfg` fixture HARD-REFUSES its own renamed parser (rename in-commit) · sibling state-flag registries (`CORE_STATE_FLAG_*`/`core_state_flags` vs bare `STATE_FLAG_*`/`state_flags`) — anchor each independently · the stale `:2619` "unknown-key error at parse" comment is FALSE today (rewrite, don't propagate).

### Test-change enumeration (per `feedback_test_change_enumeration_per_plan_body`)
MODIFIED `controller_test.cpp:1624` (drop `engine_mode` from the HAS_SIDE_EFFECT aggregate) · BROKEN/REPLACED `parity_harness.cpp` (disposition above) · NEW: rename identity rides the determinism golden (byte-identical) + the `check_gui_engine_cfg_key_parity.py` teeth.

### Node / CPU / Cluster terminology SSoT (the E.1.1 deliverable that "supports the future plans")
- **Model (verified):** **Cluster {producer-CPU 0 · drainer-CPU N+1 · N nodes} > Node (= hot+slow thread pair across 2 physical cores; hot=`NodeHotCpu(id)`, slow=smart-pinned) > CPU core (hardware).** A node owns 2 CPUs; `node_id ≠ cpu_id`; `num_execution_cores` is a misnomer = node count.
- **Disambiguation RULE (→ PRESERVE-list):** `core_*`/`Core*` shard ids → **node**; "core" = **CPU hardware** (pinning/affinity/SMT/`cpu_id` + the "core pinning"/"physical core"/"single-thread-per-core" comments) → **stays core**.
- **DELIVERABLE (E.1.1):** sharpen the glossary (`DESIGN_PHILOSOPHY §15`) + `per-node-purity-scale-invariance.md` from the imprecise "a node = a pinned CPU" → the 3-level hierarchy above. SSoT every later `.E` plan keys off.
- **Helper commit LANDED `2b8bd6c` (2026-06-17):** `EngineSharded_{NodeHotCpu,DrainerCpu,NodeSlowCpuBase}` + topology doc block in `Run.hpp` (behavior-preserving; build 3635/0; standalone, pre-rename, outside the names-only bisect).

### OUTBOUND seam additions (forward-promises)
→ **E.1.2:** make the node→CPU mapping legible *in node-state* (store hot/slow CPU if it earns its place; the symmetric `NodeSlowCpu`) — composes with the SoA relayout (design-once). → **`.E.4` NUMA / `.E.2` config:** operator-facing `node_N_hot_cpu` config (a feature; build when the cluster/NUMA input space is known).

### Path forward
**State re-verified 2026-06-20** — an 8-agent adversarial mid-dive sweep (build/sanitizers · git-forensics · Phase-2-cut · single_core-completeness · rename-surface · CI-guards · cfg-clean-break · doc-currency) replaced the planned light re-verify. **Tree fully green** (controller_test 3635/0 · fresh asan+ubsan 3635/0 · determinism golden byte-identical · conformance clean · calls_graph CLEAN); no show-stopper, no missed live-read, no foreign collision, no third operator-brick; IsLiveCapital 7-survive/10-die confirmed twice (independently). Refinements folded below. → code **3-phase leaves-first** → **V-class** verify (build + char-tests + sanitizers + calls_graph + determinism golden byte-identical + the new parity guard + **the `node_id` 3-hit allowlist**) before commit.

### State-audit refinements (2026-06-20 · 8-agent mid-dive sweep · HEAD `3eadb53`)
Corrections + additions that **SUPERSEDE** the specific claims they name above. No claim below changes the dive's SHAPE — they sharpen the later phases + fix two would-waste-a-cycle errors.

**Phase 2 (`main.cpp` untangle) — editor caveats the compiler will NOT surface:**
- The dispatch rewrite (`:272-285` → unconditional sharded) MUST preserve the bench/non-bench inner branch (`:279-283`, `oms_bench_enabled`) — do not collapse to a single call.
- The "cut → compiler flags orphans → green" loop will NOT flag (a) the now-dead `#include`s (`:21-55`: PoolAllocator/TradeLog/MetricsLog/TickRecorder/BinanceOrderAPI/EngineTUI/PortfolioController/Licensing/CandleAccumulator/GuiThread/BinanceDepth) — manual prune; nor (b) the dispatch-`return`-vs-fallthrough decision at the surviving `:1339 return 0;`. Precise cut: delete `:287-1337` + `engine_force_close_all` (`:59-120` incl. its banner), rewrite `:272-285`, KEEP `:266-270`, leaving `:1339 return 0; :1340 }`. Zero ambiguous symbols; all teardown co-deleted (no double-free/leak); no `#ifdef` straddle (all GUI/TUI/LATENCY conditionals live entirely inside the doomed block).

**Phase 3 (`engine_mode` delete) — 2 corrections to the §HIGH ② re-scope:**
- ✗ "decrement any field-count `static_assert`" is a **NON-EDIT** — `engine_mode` is in `FOREACH_GLOBAL_CFG_FIELD`; both sides of the size-assert (`CfgFieldRegistry.hpp:1053`) auto-derive from the X-macro, so deleting the registry row `:393` shrinks them in lockstep AND auto-deletes the `cfg.engine_mode` struct field → the compiler cascades every `.engine_mode` site. Don't hunt for a number to decrement.
- parity_harness: **retire, don't repoint** — `engine_mode` is already written-but-never-read (Backtest_Run unconditionally calls BacktestSharded_Run today), so "repoint to sharded-vs-sharded" is a no-op tautology.
- ADD un-named site `ControllerConfig.hpp:1860` (`cfg.engine_mode = ENGINE_MODE_SHARDED;` in `ControllerConfig_Default()` + its `:1853-1859` comment) — build-caught, but enumerate it.
- ADD `tools/scan_class_44_cfg_orphan.py` to the §HIGH tools-cohort (its DEAD_FILES rationale references single_core).
- **Stale `engine_mode` comments (NOT compiler-caught; clean so the "no residue" grep is honest)** [added 2026-06-20 self-verify]: `ControllerConfig.hpp:38-53` (the `ENGINE_MODE_*` doc block) + `:961`/`:968` (trading_mode "distinct from engine_mode") + `:1856` + `:2860`/`:2862` · `Async.hpp:315` (boot-only-fields list) · `PortfolioController.hpp:1965`. **LEAVE** the `single_core`-PortfolioController comments (`PortfolioController.hpp:357/409/632/1477` + `ControllerConfig.hpp:1361`) — they describe the SURVIVING struct's deprecated paths, removed in E.1.3, NOT Phase 3. (NOTE: the stale-comment set is BROADER than this list — ~15 `engine_mode` dispatch/doc comments across EngineSharded/Run/Backtest/GUI/DataStream per the 8-agent accuracy-refuter [2026-06-21 independent review: actual = 18 across 12 files — the rename-sweep target]; gate Phase 3 on CODE residue, not a raw `rg engine_mode`.)
- **TD-140** (`engine_mode` cfg field vestigial) [added 2026-06-20 one-more-sweep; ledger annotated — the cohort-sister miss the sweep caught: TD-185 got annotated, TD-140 didn't]: THIS is the Phase-3 **ROOT EDIT** — delete the `engine_mode` registry row (`CfgFieldRegistry.hpp:393`, H17; auto-cascades the struct field + enums/parser/readers) → **closes at Phase 3**. Its 2026-05-28 "deleted at `.E.0.1`" premise is corrected to `7eacb80` (Phase 2). Cohort-sister of TD-185 per `feedback_sister_cohort_amendment_completeness`.
- **TD-185** (`*_ExitAdjust` cohort + `original_tp` back-door) [added 2026-06-20 self-verify; ledger annotated]: now UNREACHABLE post-Phase-2 (main.cpp single_core entry gone at `7eacb80`) but still compiled. **RESOLVED at Phase 3 (`b9ce419`) → DEFER to E.1.3:** the coupling check ran — every `*_ExitAdjust` + `Regime_AdjustPositions` caller lives in `PortfolioController`'s central-drainer (`:881/:893/:901/:1716/:1743/:1941`), so the cohort does NOT excise standalone-clean → CLOSE at E.1.3 with the central-drainer/TD-002 removal (ledger disposition updated 2026-06-21).

**Rename phase — refinements to the RE-GROUNDED design-shape + PRESERVE-list:**
- **`node_id` collision-check is STALE:** the 3 `node_id` hits (`Run.hpp:202/213`) are this plan's OWN helper commit `2b8bd6c` (`NodeHotCpu(int node_id)`) — the intended target namespace, NOT a foreign collision. The "0 hits" claim + the OUTBOUND "residue grep returns empty" must allow these 3 → **V-class needs a 3-hit allowlist** or it misfires post-rename.
- **`experiments/per_core_sharding/` MUST be excluded** from the cascade tool — it's a real on-disk dir with its own `core_id`/`per_core_` tokens + a live comment at `ExecutionCore.hpp:312`; the gate's "tool runs the WHOLE source tree" instruction directly contradicts the PRESERVE-list there.
- **Elevate `FoxML_Core` to the PRIMARY PRESERVE-list** (7 comment hits; a naive `Core`→`Node` sweep makes `FoxML_Node`).
- ADD `EngineTUI.hpp:1331/1340` ("controller core" / "resetting core 0 … core 3") to the per-site human-classification set (prose-ambiguous: shard-node vs CPU-hardware).

**cfg clean-break — correction + cross-link (C-1/C-2 both re-confirmed exact):**
- `engine_sharded.cfg` is **NOT tracked** (gitignored) — treat as operator-local on-disk migration, NOT an in-commit fixture edit (the §MED "tracked engine_sharded.cfg fixture" line is wrong).
- **C-2 ↔ PARITY-006:** the `confidence_freshness_tau` GUI key (the C-2 brick) is the same key PARITY-006's freshness_tau fix renames — coordinate the C-2 stale-GUI-row delete with PARITY-006 when the cfg phase lands.
- C-1 confirmed exact: clean fall-through at `ControllerConfig.hpp:3099` (refuse between `:3098`/`:3099`); `~:3097` confirmed wrong. C-2 is the SOLE brick (adversarial third-brick hunt empty). Both global-key refuse gaps (`engine_mode`/`num_execution_cores`) confirmed real.

**Standing-gate coverage caveat:** "rides under .E.1.0's gates" holds for ENGINE-SOURCE renames (a slip RED-builds), but `tools/` / `build.sh` / `.githooks/` renames commit **GREEN** — no cascade tool / done-grep is wired yet. The §HIGH "build the TD-175 cascade tool + widen scan to `tools/`" is therefore **load-bearing, not optional**.

---

## Execution update (2026-06-21) — ① the cascade tool is BUILT + completeness-validated; the consumer model + the rename sequence

*(Execution-state capture, not a scope/architecture shift — D-239 [the rename design] + D-240 [the tool refinement, gated by a `/decision-check` I→A] already passed their gates; no `/precoding-audit-gate` re-fire needed. This section makes the dive's current state legible: ① is done, ② the rename is next.)*

### ① DONE — `tools/cascade.py` (TD-175a) + the R2 standing fix
- **`cascade.py rename`** — on-demand **ENUMERATE-ONLY** worklist across engine source **+ the compiler-blind apparatus** (`tools/`/`build.sh`/`.githooks/`) + the `#include`-cascade + the expected-residual allowlist. **`cascade_selftest.sh`** = non-vacuity teeth (per-family positive controls + PRESERVE/`experiments/` false-pos guards). Reserved `cascade struct` → E.1.2.
- **R2 standing net** = a 1-line `check_tools_inventory.py` `build.sh`-scan extension (+ case-3 teeth) — closes the `build.sh:271` `[ -f tools/X.py ]` silent-skip permanently. (NOT a new `cascade audit` subcommand — convention: standing guards stay `check_`-prefixed; the broader apparatus→dead-`FOREACH` guard = noted-future, ≈80% redundant with `check_meta_registry`.)
- **D-240 gate refinement** (operator-ratified via `/decision-check`): **R1** `rename`=enumerate-only, NO code `--apply` (the compiler is the code-token oracle per rename-ship-methodology Phase 4; a code `--apply` only adds a Class-36 silent-corruption surface) · **R2** above.
- **Completeness-validated:** a post-build **a-class audit** ("ensure the tool is finding all places") caught a Class-33 under-count (hand-list + START-anchor missed `CoreSlowState` / the ~50 `core_<stat>` field family / `g_per_core_*` / embedded `*_core_id` / the `*_cores` family / `EventLoopAggregates.hpp` / guard macros — **2118→3645 sites**) → fixed via the **family-regex sub-word-boundary matcher** (allows a preceding `_` to catch embedded compounds; blocks alnum to spare `score`/`record`) + per-family selftest controls + a mechanical **freeze ⊇ raw-`rg` cross-check** (0 real misses; the 33 residuals all PRESERVE-only `CoreFrameworks` files — closes the AR-8 self-attestation). The **Phase-2 enumeration freeze** is frozen: `plan_checks/2026-06-21-E.1.1-rename-cascade-enumeration-freeze.md` (3645 sites). Spec: `DESIGN_SPECS/refactor-patterns/rename-cascade-enumeration-tooling.md` (v1.2). **TD-175a CLOSED**; TD-175 remainder (AST `struct` cascade) → E.1.2.
- **Rollback anchors:** engine tag `pre-v5.15.5.F.4d.1.E.1.1` @ `b9ce419` (the rename anchor) · workspace `9cffbb4` (build) + `42e462e` (completeness fix).

### Consumer-completeness model — BOTH worlds addressed (the operator's "are all consumers updated?")
- **World 1 — code consumers** (callers, field reads, `#include`s, template uses): the **compiler IS the completeness oracle** — a half-renamed code tree won't compile, so "all code consumers" is guaranteed by red-build-until-green, not the tool. *Residual* (uninstantiated template members / `#if 0` blocks the compiler skips) → caught by the **determinism golden** (byte-identical output proves zero behavior drift) + the V-class.
- **World 2 — invisible consumers** (`tools/` regexes, `build.sh`, `.githooks/`, `identifier_ledger.txt`, comments, format strings): **NO compiler** → enumerated by the cascade freeze (validated `freeze ⊇ reality`), co-migrated in the **same commit**, each guard teeth-proofed RED→GREEN, with the R2 `build.sh`-scan as the standing net.
- **Cohorts that must move atomically** (miss one → the set silently breaks): **H15** (`FOREACH_PER_CORE_CFG_FIELD` + its ~10 `MetaRegistry.hpp` rows + the integrity-check rename) · **H21** (`CORE_STATE_FLAG_*` + `identifier_ledger.txt`, co-migrate via `--update`) · the **GUI-writer ↔ engine-parser** cfg-key pair (the C-2 brick → ④'s guard). **One external consumer:** the operator's `engine.cfg` `core_N_*` keys → the deliberate clean-break + migration.

### ② NEXT — the rename execution sequence (the compounding order)
The map is done + trustworthy; the rename itself is next. Order chosen so each step compounds + never holds an unprotected intermediate state:
1. **ONE atomic mechanical commit: code + apparatus (the TOOL-REGEX cohort) + the `#include`-cascade together** — not split. Single green checkpoint where the **compiler proves World-1** completeness AND the **teeth prove World-2** completeness; no red-build window; bisectable as "a rename typo, never a logic change." (Mechanism: scripted per-family substitution matching the cascade families [longest-first, PRESERVE-aware, overlap-resolved], compile-gated red→green per rename-ship-methodology Phase 4 — `clang-rename` is the Class-36-preferred alternative if the tooling is available.)
2. **The cfg cluster — guard FIRST, then the break:** land the **C-2 parity guard** (`check_gui_engine_cfg_key_parity.py`) + delete the stale `SettingsPanel.hpp:669` `confidence_freshness_tau` row → THEN the cfg-key rename + the **HARD-REFUSE** (RE-DERIVE the line — the `:2968`/`:3099` cites are PRE-Phase-3) + the **global-key refuse** (`engine_mode=`/`num_execution_cores=`) + the GUI `SettingsPanel.hpp:922` parser lockstep. Guard-before-behavior = no brick window.
3. **V-class verify** (the behavior-neutrality proof): build + char-tests + fresh asan/ubsan + `calls_graph` + **determinism golden byte-IDENTICAL** + the new parity guard + the **`node_id` 3-hit allowlist** (`Run.hpp:202/213`, the `2b8bd6c` helper).
4. **Ship-close** (lowest risk, last): the doc sweep is **TWO distinct cohorts, a different tool each** —
   - **(4a) per-core CONCEPTUAL reframe** via `check_doc_rename_classification.py` (its `DEFAULT_TOKENS` = `per-core` + `single_core` ONLY — the vision-language reframe).
   - **(4b) the CODE-SYMBOL doc-citation cohort** (⚠️ the ALL-N scout executor-gap catch, 2026-06-21): dead engine symbols cited as CURRENT across **~38 live DESIGN_SPECS + reference DOCS** (`DOCS/CLAUDE_INVARIANTS.md`, `CLAUDE_ML_INVARIANTS.md` [live HOW-TO recipes — "add field to `CoreModelZoo` struct" :431/435/461/495], `ML_TEST_RECIPES.md`, `OPERATOR_DEPLOYMENT.md`) **+ ~40 operational skill refs** — `CoreModelZoo`/`CoreContext`/`CoreSlowState`/`PerCoreSnap`/`per_core_*`/`FOREACH_PER_CORE_*`/`.cores[`/`MAX_EXECUTION_CORES`. **`check_doc_rename_classification.py` DELIBERATELY EXCLUDES code symbols** (its docstring defers them "WITH the code at .E.1" = HERE) → it STRUCTURALLY CANNOT sweep this cohort; **use `cascade.py rename --docs`** (its family-regex + `CoreModelZoo→NodeModelZoo` map DO see them) to ENUMERATE → classify current-vs-historical → mutate the CURRENT ones. **PRESERVE (historical worked-examples — terminology-evolution-bridge, do NOT rewrite):** `trace-deps:187-188`, `check-29:19/72`, `parity-check:470/667/674`, `merge-scan:157`. **Already done at close (2026-06-21):** the FUNCTIONAL `check-17:12` trigger keyword `CoreModelZoo`→`NodeModelZoo` + `:19` `PerCoreSnap`→`PerNodeSnap` (the readiness check was INERT for the renamed model-load path).
   - **(4c) the rest:** FEATURE_LOOKUP (revise 4 stale lines + add 2) + the ~18 stale `engine_mode` comment sweep + the ~535 CPU-vs-shard "cores" comment-prose human-classification + the TD-140 physical move open.md→closed.md.

### ② LANDED (2026-06-21) — engine `1da1c1c` + workspace `c356e03`
The mechanical rename is committed + green: **3635/0** · determinism net byte-identical · H8 conformance clean · calls_graph clean · Check-H ledger GREEN (46/46, values preserved) · per-node integrity GREEN · both pre-commit hooks clean. Content-sub 3633 sites + 6 engine git-mv + the tool git-mv + ledger `--update` + tests/ + the iterative compiler-fixes.

**The World-1 oracle did its job — the compiler/gates caught every cascade-matcher gap (the red→green loop), proving the "compiler-for-code-consumers" model.** Known matcher limitations (the freeze is the World-2 planning aid; the compiler is the World-1 completeness guarantor — these are honest gaps backstopped by the build, NOT silent):
- the `.cores[` token matched only dot-access → missed the `cores[` decl + `->cores[` arrow + bare `cores` array/param (the compiler caught all).
- `core_[a-z]` missed `core_` + regex-metachar in apparatus (`core_\w+`, `"core_"`, `core_<digit>`) → the integrity/parser/feature-mask gates caught them.
- the scan scope omitted `tests/` → the `controller_test` build caught it.
- prose-ambiguous bare `cores`/`core_<N>` comments deliberately left for the human-classification sweep (ship-close).
*(A cascade-matcher hardening — broaden `.cores[`→`\bcores\[`, add `tests/` to scope, the apparatus regex-metachar class — is a low-value future cleanup: the compiler + gates are the proven backstop, so the leak is caught-not-silent. Homed as a noted cascade follow-up, not a blocker.)*

**NEXT = ③ the cfg cluster** (the parser already RECOGNIZES `node_`; ③ adds the HARD-REFUSE loud-fail at the `ControllerConfig.hpp:~2929` outer-block fall-through + the global-key refuse `engine_mode=`/`num_execution_cores=` + the C-2 GUI parity guard + delete the stale `SettingsPanel.hpp:669` row + operator migrates `backtest.cfg`/`engine_sharded.cfg` core_→node_) **→ ⑤ V-class** (asan/ubsan — the only remaining V-class leg; build/determinism/conformance/calls_graph already green).

---

## ③ adversarial re-gate (2026-06-22) — framework GREEN, 3 HIGH, scope decision OPEN (D-247)

The TARGETED adversarial re-gate the prior handoff owed (5 a-class refuters, one per config-compiler delta) RAN 2026-06-22. **SSoT: `plan_checks/2026-06-22-E.1.1-config-compiler-adversarial-regate.md`; decision: D-247 (STATUS OPEN).** All 5 PARTIAL-REFUTED — the FRAMEWORK holds (collect-all · flag-based · never-abort-in-Load · compile-gates-every-fresh-start · the uint32 gate · COMPLETE caller enumeration), but 3 HIGH (all orchestrator-code-verified at HEAD `1da1c1c`) change ③'s scope:
- **HIGH capital (F-A):** the per-node hand-rolled parser (`:2850`, `strstr`+no-clamp+flags-discarded) silently mishandles capital VALUES (`risk_pct=999`→999% alloc via `Run.hpp:924`; `stop_loss_pct=banana`→SL off — the founding case) AND typo'd KEYS (`strstr` under-fire→writes slot 0). ③'s key-match (value-validation deferred to E.2 per D-243) closes silent-wrong-KEY, NOT silent-wrong-VALUE. D-242's "unparseable→ERROR" prose overstated ③.
- **HIGH determinism (F-B):** `cfg_load_fault_flags` in `ControllerConfig` enters the model SHA-256 fingerprint (`ControllerConfig.hpp:347`→`Fingerprint.hpp:180`) — "not wire" is false. → fingerprint-exclude/guarantee-0 + stack-local readout + golden-regen; folds into E.1.2 struct-finalize (+ the MISSING ControllerConfig size-pin, F-I).
- **HIGH design-consistency (F-C):** `config_error_log` file = Class-44 path-orphan contradicting D-243 → ③ terminal/stderr-only, defer the file to E.2.
- **MED:** readout ERROR-priority (F-D); the DEAD EngineTUI hot-reload edit-site → drop from the cadence list + delete the `TUI_HandleInput`/`PortfolioController_HotReload` orphan pair (F-E, verified 0-callers); Async copy-order trap + shared `cfg_compile_ok()` helper (F-F/F-G); List-C substring-colliding-typo test (F-H).

**Scope decision (D-247, OPEN — operator):** ③ is the cfg-validation FOUNDATION the capital spines (E.1.3/4/5) + framework (E.1.6) + headless-config (E.2, where ③ is the named hierarchical-config-validation-pattern SEED) build on. **B** = key/index-only (defer matcher + value-validation to E.2; founding motivation unmet, spines build on unvalidated inputs). **A-narrow (recommended)** = exact-suffix matcher fix (fold the 7 `strstr` branches into the `:2929` exact-match dispatch) + 4-field capital value-validation now (the non-capital sweep stays E.2). **A-broad** = full value-sweep now (overscope). The implementation fixes (F-B/F-C/F-D/F-E/F-F/F-G/F-H) apply under either. If A-narrow → a FRESH I→A on the corrected (parser+registry) shape before coding. Forward-seams folded into E.1.2/E.1.5/E.1.6/E.2 (2026-06-22).

**RESOLVED → D-248 (2026-06-22):** the full I→A cascade + an operator-directed forward-compat sweep LOCKED the corrected design — **asymmetric caps** (loss-side ≤100% / `take_profit_pct` ≤~1000%, reusing `tt::barrier_is_corrupt`; NO MARGIN [[project_no_margin_trading]]), **per-field bounds** (not a uniform constant), **scope ~12** (the 4 core + the 8 per-strategy tp/sl overrides), hand-rolled inline ERROR-block sentinel-aware, convert-all-7-strstr, atof→parse_double_fast. The D-206 SL/TP ratchet redesign does NOT conflict (downstream of the cfg seed). The Σ≤100% no-margin allocation-sum → TECH_DEBT-211 → E.5. SSoT: `plan_checks/2026-06-22-E.1.1-config-compiler-IA-cascade-synthesis.md` + D-248. Operator-directed: codify the anti-pattern/spec slate + RE-RUN audit subagents on this corrected shape before coding.

**→ D-253 FINAL (2026-06-22) — the codeable design.** The re-run cycles LANDED: re-audit (3 idioms) → deep-pass (the macro-fold target was DEAD; the live chokepoint is the registry walker `tt::cfg_parse_field`) → D-250-verify (→D-252) → D-252 verify cycle (→**D-253**). CONVERGED on **walker-chokepoint + a metadata-bit dispatch** (reuse `cfg_compute_mask`/`FOREACH_METADATA_BIT` — honors the `.F.4b` lock). Cycle corrections + operator decisions: **TWO side-tagged bits** `CAPITAL_BOUND_LOSS`/`CAPITAL_BOUND_GAIN` (the asymmetry; both-active = compile error); **seam split** (walker DETECTS+FLAGS; post-loop `cfg_compile_ok()` POLICY — the walker is void + `trading_mode` isn't resolved at parse); **D2 ALWAYS-ABORT** on any capital fault (all modes); **D1 hand-roll idiom-2 NOW** (the dedicated `node_N_risk_pct`/`max_drawdown_pct` legacy-array branches bypass the walker = H22 hole); restore `parse_double_fast_checked`; per-tagged-row not name-heuristic; **D3 cross-node Σ → the CLUSTER (cluster-aggregator) / E.5**. Foundation cycle-verified: SUPPORTS the rest of E (extensions NAMED+HOMED — E.1.6 `ENUM_STRICT`, D-206 runtime loss-floor). Residual TDs: 212 (vacuous-clamp) / 213 (model_path) / 214 (drift-gate) / 215 (dead-macro). Canonical decisions: **D-249..D-253** in the decision-log. **NEXT: CODE ③ (build-gated micro-commits); V-class gate. Full anti-pattern/spec codification at ③ ship-close.**
