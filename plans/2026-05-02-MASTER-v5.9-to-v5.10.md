# Master Plan — v5.9 sprint exit + v5.10 epic

**Date:** 2026-05-02
**Branch:** `feat/v5.9-ml-hardening` (current); v5.10 starts from `experiment/per-core-sharding` post-merge
**Source audits:**
- `/tmp/v5.10-audit.md` (codebase audit, 21 items, 2026-05-02)
- `DOCS/v5.10-design-notes.md` (12 ideas)
- v5.9 GUI gap audit (4 deferred items)
- 2026-05-02 paper-test feedback from operator

---

## Current State

**Live operator state — refreshed at sprint pre-flight.**

| Field | Value |
|---|---|
| Branch | `feat/v5.9-ml-hardening` |
| HEAD commit | `cbad8fb` (will update as ships land) |
| Last shipped tag | `v5.9.5j` (Train Model auto-stamp; #8 + #9 deferred to v5.10 polish) |
| Tags total in v5.9 | 27 (v5.9.0 → v5.9.5j) |
| Tests passing | 1326/0 (post-v5.9.5j; #8 + #9 tests deferred) |
| Hot path status | UNTOUCHED across all 27 v5.9 ships |
| Active sprint | **A complete — ready for merge to `experiment/per-core-sharding`** |
| Next ship | **MERGE v5.9** then v5.10.0a (parallel grid search + multi-horizon) |
| Sprint A progress | 3/3 — v5.9.5h + v5.9.5i + v5.9.5j shipped 2026-05-02 |
| Sprint B progress | 0/5 (a-e drafted, not started) |
| Sprint C progress | 0/N (placeholder; v5.11) |
| Plan-check verdict | GREEN (last run 2026-05-02) |
| Parity-check verdict | GREEN — foundation clean across 11 sections (2026-05-02 inline audit) |
| Build state | CLEAN — `./build.sh test gui suite` (1289/0 tests, 0 new warnings) |
| In-flight model training | None (paused per operator decision 2026-05-02) |
| Workspace synced | `7d89afc` |

**Blockers:** None.

**Open design gates:**
- v5.10.0b FPN sin/cos approach (lock before B.1) — see plan §"Risk register"

**Pre-flight checklist (run before starting v5.9.5h):**
- [ ] `./build.sh test gui suite` clean (all 3 targets)
- [ ] 1289/0 tests pass
- [ ] `/parity-check` on v5.9.5g foundation — GREEN
- [ ] Each sub-plan has a `<plan>-PREFLIGHT.md` sidecar with operator
      validation criteria + watch-points
- [ ] Master plan Current State updated to reflect pre-flight pass

**Update protocol:** After each ship, edit this section:
- Bump HEAD commit to the new tag's SHA
- Bump Last shipped tag
- Bump test count
- Bump Sprint progress (e.g., "1/3 — v5.9.5h shipped 2026-05-XX")
- Update "Next ship"
- Re-verify Hot path UNTOUCHED claim
- Push master plan update with the ship's commit (master is part of
  the ship)

If ANY pre-flight checkbox is unchecked, do NOT start the next ship.
Fix the foundation first.

## Executive summary

**State today (v5.9.5g, HEAD cbad8fb):** v5.9 ML Hardening sprint
shipped 24 tags, 1289/0 tests, all 27 V5_9_AUDIT findings closed.
Operator paper-testing surfaced 5 follow-up items + the v5.10
design-notes queue accumulated 12 ideas. This master plan organizes
all 21 candidate items into shippable units.

**Top finding from audit:** Many items thought to be "v5.10" are
actually 30min–2h ships because the infrastructure already exists.
Specifically:

- **#15 boot WARN capture: ALREADY DONE** (foxml_suite redirects engine
  stderr to logging/foxml_suite.log + Engine Log panel tails it)
- **#19 Past Runs stamp_ok column: 30 min** (just adds column;
  PastRun.stamp_verify_state already exists)
- **#16 ML Status recorded cfg row: 1-2h** (panel exists; data already
  populated by v5.9.5d Verify Stamp)
- **#1 parallel grid search: 30 min** AFTER hyperparams become cfg fields
  (Backtest_RunSweep already does serial grid search)
- **#6 auto-stamp Train Model: 1-2h** (Full Validation has full wiring;
  Train Model just needs to call same `stamp_write_for_model`)

**The "biggest" remaining items:**
- **#21 hyperparam ownership: 2-3 days** (no cfg fields exist; biggest item)
- **#11 FPN-end-to-end: 2-3 days** (refactor + retrain)
- **#13 hot model swap: 2-3 days** (RCU pattern + position handling)

## Phasing

| Sprint | Ships | Duration | Theme |
|---|---|---|---|
| **Sprint A** (v5.9 wrap-up) | v5.9.5h, v5.9.5i, v5.9.5j | 3-4 sessions | Stamp ↔ runtime observability + UX polish |
| **MERGE v5.9** to `experiment/per-core-sharding` | — | 30 min | Sprint exit |
| **Sprint B** (v5.10 core) | v5.10.0a–e | 5-7 sessions | Big infra: hyperparams power-user features, model lifecycle |
| **Sprint C** (v5.11+ deferred) | — | TBD | Nice-to-haves not blocking anything |

**Critical-path order (must ship before dependents):**
- **v5.9.5h #21** (hyperparams) → unblocks **v5.10.0a #1** (grid search), **#4** (multi-horizon)
- **v5.9.5j design decision #17** (Option A: WF-only) → unblocks **v5.9.5j #6** (auto-stamp Train Model)

## Sprint A — v5.9 wrap-up (3 ships)

Each sub-plan in its own doc. Detail level: full implementation breakdown.

### [v5.9.5h] Full stamp ↔ runtime observability foundation

**Sub-plan:** `2026-05-02-v5.9.5h-hyperparam-ownership.md` (existing,
to be updated post-readiness)

**Scope:** XGBoost hyperparameter ownership (#21) + build flag
fingerprinting (#10) + Past Runs stamp_ok column (#19)

**Effort:** ~10h (one ~full session)

**Items:**
- **#21** XGBHyperparams struct (~1h) + 5 cfg fields (~1h) + foxml_suite Training panel UI (~1.5h) + engine_gui Settings panel rows (~15min) + stamp body extension (~1.5h) + Verify Stamp display (~30min) + load-time WARN (~30min) + bash CLI parity (~30min) + tests (~45min) + docs (~30min) — **~8h**
- **#10** build flag fingerprinting — Surface G stamp body extension. Same shape as hyperparam stamp binding. Constexpr `BUILD_FLAGS_HASH()` over `{USE_NATIVE_128, USE_XGBOOST, optimization level}`; embed in stamp; load-time WARN. **~2h**
- **#19** Past Runs stamp_ok column — PastRun.stamp_verify_state already exists. Add table column rendering green/yellow/red. **~30min**

**Files touched:** ~15
**Tests:** +15-20 EXTENSIBILITY tests (1289 → ~1310)
**Hot path:** UNTOUCHED

### [v5.9.5i] Operator audit views

**Sub-plan:** `2026-05-02-v5.9.5i-cfg-enforcement-stamps-panel.md` (NEW)

**Scope:** Inference cfg load-time enforcement (#12) + ML Status
recorded cfg row (#16) + Stamps Inspection panel (#14)

**Effort:** ~7-9h

**Items:**
- **#12** Load-time enforcement of stamped inference cfg (~4-6h):
  - Cfg field `acknowledge_inference_cfg_drift` (suppression flag, default 0)
  - In `CoreModelZoo._TryLoadRole`: post-`verify_model_stamp`, walk the parsed `inference_cfg_*` fields. Per-field comparison to runtime cfg. Tier 1 fields (freshness_tau, confidence_threshold_scale, barrier_gate_enabled): REFUSE in strict mode / WARN otherwise. Tier 2 (confidence_hard_block_threshold, bandit_blend_ratio, fee_rate_*): WARN regardless.
  - Per-field tests
  - Documentation update in `DOCS/CLAUDE_ML_INVARIANTS.md`
- **#16** ML Status panel — recorded cfg row (~1-2h):
  - In `GUI/MLStatusPanel.hpp`, add expandable "Stamp recorded cfg ▾" section
  - Pull data from `ModelHandle` (already has stamp-derived fields per v5.9.4a; extend with v5.9.5h's hyperparam fields too)
  - Render runtime-vs-stamp drift indicators (green if matches, yellow if differs)
  - Lives in engine_gui (live operator view; foxml_suite spawns engine_gui)
- **#14** Stamps Inspection panel (~2-3h):
  - New stateful panel registered via FOREACH_PANEL X-macro (v5.8.4b)
  - Walks `models/*/`, parses every stamp, renders sortable table
  - Reuses `verify_model_stamp` helper + `ModelStampResult` struct
  - Filters: stamp_ok / FAIL / missing
  - Click-row: expands to full Stamp details (reuses v5.9.5d tree)

**Files touched:** ~10
**Tests:** +10-15 (1310 → ~1325)
**Hot path:** UNTOUCHED

### [v5.9.5j] Sprint exit polish

**Sub-plan:** `2026-05-02-v5.9.5j-multiclass-autostamp-snapshots.md` (NEW)

**Scope:** Per-class accuracy display (#8) + ConfidenceScorer
snapshot tests (#9) + Train Model auto-stamp (#6, with #17 design
locked to Option A: WF-only)

**Effort:** ~5-7h

**Items:**
- **#8** Per-class accuracy breakdown display (~2-3h):
  - Extend `WalkForwardFoldResult` struct with per-class arrays
  - Compute per-class TP/FP in `Backtest_RunWalkForward` post-fold
  - Display in foxml_suite Training panel WF results table when `label_kind == 2` (multiclass)
  - Tooltip explains: "class N: train=X% val=Y% (baseline=Z%)"
- **#9** ConfidenceScorer extended snapshot tests (~1-2h):
  - Push N>4 predictions through ConfidenceScorer
  - Record IC + RMSE at each step
  - Snapshot file at `tests/snapshots/confidence_scorer.expected`
  - Test compares actual vs expected; fails on formula drift
- **#6** Train Model auto-stamp with #17 Option A (~1-2h):
  - **Design decision #17:** Train Model auto-stamps using **WF mean accuracy only** (no held-out). Faster path; stamp's `held_out_metric` is sentinel/zero. Engine load-time strict mode treats Train-Model-stamped models as "warn-only" not refuse (since gap can't be computed). Operators wanting full validation use Run Full Validation button (existing path).
  - Wire in `train_model_worker_fn` post-Persist:
    - Read `auto_stamp_path` + `auto_stamp_secret` from worker args (parallel to Full Validation's flow)
    - Build `StampInferenceCfgInputs` from cfg + label_type + hyperparams (same as v5.9.5h's RFV path)
    - Call `stamp_write_for_model` with WF-mean-acc as `wf_mean_val`, sentinel 0 as `held_out_metric`, sentinel `gap_threshold=0.0`
    - Add `auto_stamp_attempted` + `auto_stamp_ok` + `auto_stamp_path_written` to TrainingPanelState
    - Display result in Training panel below status_msg
  - Engine load-time gracefully accepts Train-Model-stamped models (no refuse) — they're explicitly less rigorous

**Files touched:** ~6
**Tests:** +10-12 (1325 → ~1337)
**Hot path:** UNTOUCHED

### [MERGE v5.9] → `experiment/per-core-sharding`

After v5.9.5j ships + operator paper-test validation:

```bash
git checkout experiment/per-core-sharding
git merge --no-ff feat/v5.9-ml-hardening -m "merge v5.9 ML hardening sprint"
git push origin experiment/per-core-sharding
```

**Sprint final:** ~27-28 tags (v5.9.0 through v5.9.5j), 1030 → ~1337
tests, all 27 V5_9_AUDIT findings closed + 14 v5.10 candidate items
shipped.

## Sprint B — v5.10 core (5 ships)

Bigger ships. Each its own session.

### [v5.10.0a] Grid search wire + multi-horizon

**Sub-plan:** `2026-05-03-v5.10.0a-grid-search-multihorizon.md` (NEW, draft after merge)

**Scope:** #1 parallel grid search (wire to hyperparams + cross-core) + #4 multi-horizon training

**Effort:** ~1-2 days

**Items:**
- **#1.a** Wire OptimizerPanel to sweep XGBoost hyperparams (~30min):
  - After v5.9.5h, hyperparams are cfg fields. OptimizerRange already supports any cfg field via `ConfigField_Set`. Just expose hyperparams in the OptimizerPanel UI's parameter selector.
- **#1.b** Cross-core parallel training (~1-2 days):
  - 4 cores train models with different sweep parameter combinations in parallel
  - Per-core feature_matrix slicing (already exists in BacktestSharded)
  - Per-core model save with sweep-index suffix
  - Aggregation: pick best by metric across all cores
  - Triple-split discipline: separate train/val/test (test = locked, only used for final selection)
- **#4** Multi-horizon (~5-7h, after #1):
  - Cfg field `horizon_list=100,500,1000,5000,20000` (CSV)
  - Training loop iterates horizons, trains N models in parallel via #1's machinery
  - Inference: load all N models, return highest-confidence prediction
  - Stamp body: `grid_member_count` + per-member SHA list

### [v5.10.0b] FPN-end-to-end slow path

**Sub-plan:** `2026-05-04-v5.10.0b-fpn-end-to-end.md` (NEW)

**Scope:** #11 FPN-end-to-end refactor for cross-build determinism

**Effort:** 2-3 days (3 stages)

**Stages:**
- **B.1** FPN versions of RollingStats primitives (mean, variance, slope) — 1 day
- **B.2** RegimeSignals refactor + replay-determinism test — 1 day
- **B.3** Retrain reference models + `MODEL_FORMAT_VERSION` bump for refusal-on-load of pre-v5.10 stamps — 4-6h

**Risk:** High (~500 LOC refactor; all models need retraining; potential precision drift). Pre-tag carefully; could rollback per stage.

### [v5.10.0c] Hot model swap

**Sub-plan:** `2026-05-05-v5.10.0c-hot-model-swap.md` (NEW)

**Scope:** #13 hot model swap (engine reloads on cfg change without restart)

**Effort:** 2-3 days

**Items:**
- TUISharedState extension: `swap_model_path_requested[MAX_CORES]` + `pending_model_path[MAX_CORES][256]` (RCU pattern, mirrors strategy hot-swap)
- engine_gui Settings panel: "Apply (live)" button next to Model Dir Combo
- Engine consumer: tick loop reads atomic, calls `CoreModelZoo_TryLoadRole` on new path; old model retired after position close
- Position handling decision: keep open positions running on old model OR force-close on swap (cfg-driven)
- Tests: integration test for swap mid-backtest

### [v5.10.0d] FOREACH_TARGET label registry

**Sub-plan:** `2026-05-06-v5.10.0d-foreach-target.md` (NEW)

**Scope:** #2 X-macro retrofit for labels

**Effort:** 3-4h

**Items:**
- `FOREACH_TARGET(X)` X-macro replacing `label_table[]`
- `LABEL_REGISTRY_HASH()` constexpr (FNV-1a, mirrors FEATURE_REGISTRY_HASH)
- Stamp body: `label_registry_hash` field
- Load-time check refuses stamps with mismatched hash (same shape as feature_registry_hash refuse)
- Tests: round-trip + mismatch refuse

### [v5.10.0e] Drift detection / model retirement

**Sub-plan:** `2026-05-07-v5.10.0e-drift-detection.md` (NEW)

**Scope:** #3 runtime IC monitoring

**Effort:** 4-5h

**Items:**
- 3 cfg fields: `confidence_ic_floor` (default 0.02), `confidence_ic_floor_window` (24h), `auto_kill_on_drift` (0/1)
- ConfidenceScorer extended with rolling 24h IC history
- Boot wiring: emit CRITICAL log on threshold breach; optionally trip kill_switch
- ML Status panel row: "Drift: IC=0.018 below floor 0.02 for 5h" (red)
- Tests: drift fires WARN/kill; healthy IC has no false positives

## Sprint C — v5.11 deferred (nice-to-have)

Items not blocking anything; ship when time/operator-need permits:

- **#5** Feature mask cfg per-core (3-4h)
- **#7** Scaler comparison tool (2h)
- **#18** RFV scaler binding (after design decision)
- New items surfaced during Sprint A/B test sessions

## Risk register

| Risk | Mitigation |
|---|---|
| v5.9.5h #21 scope creep (hyperparam tunability touches many surfaces) | Phased shipping; if any phase regresses, revert to pre-v5.9.5h |
| v5.10.0b FPN refactor introduces precision drift | Pre-tag per stage; abandon if epsilon-tolerance fails |
| v5.10.0c hot swap thread-safety | Mirror existing strategy hot-swap RCU pattern; lots of integration tests |
| Sprint A → operator validation gates merge | Each sub-letter is independently testable; partial sprint success is acceptable |
| Test count balloons hard to maintain | Already at +259 tests for v5.9; v5.10 adds another +30-50; review pruning at v5.11 |

## Operator-validation gates

Each ship has explicit operator-validation steps:

- **v5.9.5h:** Train a model with default hyperparams → stamp records them → Verify Stamp surfaces them in tree
- **v5.9.5i:** Cfg-mismatch boot WARN fires; Stamps Inspection panel renders all models
- **v5.9.5j:** Per-class accuracy displays for multiclass label run; Train Model auto-stamps
- **v5.10.0a:** Sweep `xgb_subsample` from 0.5 to 1.0; best model emerges with metrics displayed
- **v5.10.0b:** Replay-determinism test passes bytewise across all 3 build configs
- **v5.10.0c:** Swap a model live, verify new model fires predictions while old positions exit naturally

## Updates this plan triggers

After each ship:
- Update `DOCS/v5.10-design-notes.md` — mark shipped items with version tag
- Update `DOCS/CHANGELOG.md` — v5.9.x row reflects final tag count + tests
- Update `DOCS/V5_9_ML_HARDENING_AUDIT.md` — remains CLOSED but reference the v5.9.5h-j ships
- Re-generate `DOCS/CODE_MAP.md` if new top-level functions added
- Push branch + tag per ship (per memory feedback "push per phase")

## Cross-references

- Audit: `/tmp/v5.10-audit.md`
- Design notes: `DOCS/v5.10-design-notes.md`
- v5.9 sprint changelog: `DOCS/changelogs/2026-05-02-v5.9-ml-hardening.md`
- /parity-check skill: `.claude/skills/parity-check/SKILL.md`
- /readiness skill: `.claude/skills/readiness/SKILL.md`

---

## Integration Matrix (cross-plan cohesion verification)

This section is the **parity-check for the plans themselves**: same
discipline as `/parity-check` for code, applied to planning
documents. When a sub-plan is updated, this matrix flags
inconsistencies before they become merge conflicts or load-bearing
silent breakage.

### Files touched per plan

| File | v5.9.5h | v5.9.5i | v5.9.5j | v5.10.0a | v5.10.0b | v5.10.0c | v5.10.0d | v5.10.0e |
|---|---|---|---|---|---|---|---|---|
| `Backtest/XGBHyperparams.hpp` (NEW) | ✓ create | — | — | read | — | — | — | — |
| `Backtest/BacktestPanels.hpp` | ✓ Train UI + scaler row | — | ✓ per-class display + auto-stamp UI | ✓ Optimizer UI | — | — | — | — |
| `Backtest/BacktestEngine.hpp` | ✓ training paths use struct | — | ✓ per-class compute | ✓ sweep parallel | — | — | — | — |
| `CoreFrameworks/ControllerConfig.hpp` | ✓ +5 hyperparam cfg | ✓ +1 ack flag | — | ✓ +1 horizon_list | — | — | — | ✓ +3 drift cfg |
| `ML_Headers/ModelInference.hpp` | ✓ +8 stamp fields + has_xgb_hyperparams + +1 build_flags | ✓ +1 ack handling | ✓ gap=0 sentinel | — | ✓ format_version 6 | — | ✓ +1 label_registry_hash | — |
| `ML_Headers/CoreModelZoo.hpp` | ✓ load-WARN for hyperparams + build_flags | ✓ load-WARN for inference cfg | ✓ training-only stamp accept | — | — | ✓ swap consumer | ✓ label_hash refuse | ✓ drift kill |
| `GUI/SettingsPanel.hpp` | ✓ +5 hyperparam field_defs + ML Hyperparams section | — | — | — | — | ✓ Apply (live) button | — | — |
| `GUI/MLStatusPanel.hpp` | — | ✓ recorded cfg row | — | — | — | — | — | ✓ drift row |
| `GUI/StampsInspectorPanel.hpp` (NEW) | — | ✓ create | — | — | — | — | — | — |
| `tools/stamp_model.sh` | ✓ +9 hyperparam flags + build_flags | — | — | — | ✓ format_version |  | ✓ +label_hash | — |
| `tests/controller_test.cpp` | ✓ +15-20 | ✓ +10-15 | ✓ +10-12 | ✓ +10 | ✓ +8 | ✓ tests | ✓ +6 | ✓ +6 |
| `Strategies/RegimeDetector.hpp` | — | — | — | — | ✓ FPN refactor | — | — | — |
| `ML_Headers/RollingStats.hpp` | — | — | — | — | ✓ FPN ops | — | — | — |
| `Backtest/LabelFunctions.hpp` | — | — | — | — | — | — | ✓ FOREACH_TARGET | — |
| `ML_Headers/ConfidenceScore.hpp` | — | — | ✓ snapshot tests | — | — | — | — | ✓ rolling IC |

### Stamp body field additions (canonical-order critical)

HMAC verification requires byte-exact canonical body order.
Multiple plans extending the stamp body must coordinate field
position. **Rule:** new fields appended ONLY at end of canonical
body. Order locked once shipped.

| Position | Field | Added by | Status |
|---|---|---|---|
| (existing) | model_format_version, sha256, trained_on, wf_mean_val, held_out_metric, gap, gap_threshold | pre-v5.9 | locked |
| 8 | feature_registry_hash | v5.8.6 | locked |
| 9 | engine_version | v5.8.6 | locked |
| 10 | stamp_format_version | v5.9.0 | locked |
| 11 | inference_cfg_* (5 fields) | v5.9.2b | locked |
| 12 | inference_cfg_bandit_blend_ratio | v5.9.2b | locked |
| 13 | inference_cfg_fee_rate_maker/taker | v5.9.2b | locked |
| 14 | training_poll_interval | v5.9.4a | locked |
| 15 | feature_scaler_present + scaler_sha256 | v5.9.3a | locked |
| 16 | model_num_outputs | v5.9.4a | locked |
| **17** | **xgb_hyperparams (8 fields)** | **v5.9.5h** | TBD |
| **18** | **build_flags_hash** | **v5.9.5h** | TBD |
| **19** | **label_registry_hash** | **v5.10.0d** | TBD |
| **20** | **grid_member_count + members** | **v5.10.0a (multi-horizon)** | TBD |

**Cohesion check:** v5.9.5h adds positions 17 + 18 in ONE ship. If
the canonical body emit order differs between bash CLI and
in-process, HMAC verification breaks immediately (caught by v5.8.8
round-trip test). Plans must agree on relative order.

### Cfg field additions

Avoid name collisions across plans:

| Cfg field | Added by | Type | Default |
|---|---|---|---|
| `xgb_subsample` | v5.9.5h | float | 0.8 |
| `xgb_colsample_bytree` | v5.9.5h | float | 0.8 |
| `xgb_min_child_weight` | v5.9.5h | int | 5 |
| `xgb_seed` | v5.9.5h | int | 42 |
| `xgb_tree_method` | v5.9.5h | string | "hist" |
| `acknowledge_inference_cfg_drift` | v5.9.5i | int | 0 |
| `horizon_list` | v5.10.0a | string (CSV ints) | "" |
| `confidence_ic_floor` | v5.10.0e | float | 0.02 |
| `confidence_ic_floor_window` | v5.10.0e | int (seconds) | 86400 |
| `auto_kill_on_drift` | v5.10.0e | int | 0 |

**Cohesion check:** All `acknowledge_*` flags reuse the
v5.9.4 suppression-pattern. If ANY plan adds a NEW pattern (e.g.,
`suppress_X` or `enable_X_check`), flag for refactor.

### Architectural invariants (every ship must preserve)

| Invariant | Verification |
|---|---|
| Hot path UNTOUCHED | `tools/calls_graph_diff.sh` per ship; flag if BG_/SG_Evaluate or ExecutionCore_Tick changes |
| `MODEL_FORMAT_VERSION` only bumps deliberately | Currently 5 (v5.9 used Surface G `has_*` forward-compat). Bumps to 6 in v5.10.0b (FPN-e2e). NOT in v5.9.x. |
| `SHARDED_SNAPSHOT_VERSION` only bumps when struct changes | Currently 6 (v5.4.4). No v5.9-v5.10.0e ship bumps it. |
| `FEATURE_REGISTRY_HASH` flips ONLY on FOREACH_FEATURE structural changes | No plan touches FOREACH_FEATURE; hash stable across v5.9-v5.10.0e. |
| `LABEL_REGISTRY_HASH` flips ONLY on FOREACH_TARGET structural changes | New in v5.10.0d. Stable thereafter. |
| Train-serve parity: features computed identically backtest + live | All ML changes preserve this; v5.10.0b's FPN refactor explicitly verifies via replay-determinism test. |
| Stamp body forward-compat: legacy stamps load with `has_*=0` | Every Surface G extension preserves this; tests verify per ship. |
| 3-tier strict mode (refuse / warn-with-surface / silent-forbidden) | New strict-mode behaviors must follow v5.9.3a's pattern. |

### Test count progression

| Ship | New tests | Cumulative |
|---|---|---|
| v5.9.5g (current) | — | 1289 |
| v5.9.5h | +15-20 | ~1305 |
| v5.9.5i | +10-15 | ~1320 |
| v5.9.5j | +10-12 | ~1332 |
| v5.10.0a | +10-14 | ~1346 |
| v5.10.0b | +8-12 | ~1358 |
| v5.10.0c | (TBD, integration tests) | ~1366 |
| v5.10.0d | +6-8 | ~1374 |
| v5.10.0e | +6-8 | ~1382 |

**Cohesion check:** Test counts are claims; verify per ship via
`./build.sh test`. Update master plan if drift > 10% from claim.

### Cross-plan dependency edges (Mermaid-style)

```
v5.9.5h (#21 hyperparams)
   ├──> v5.9.5i (#16 ML Status row needs hyperparam fields on ModelHandle)
   ├──> v5.9.5j (#6 auto-stamp uses XGBHyperparams struct)
   ├──> v5.10.0a (#1 grid search sweeps hyperparam cfg fields)
   └──> v5.10.0a (#4 multi-horizon uses hyperparam machinery)

v5.9.5i (#12 cfg enforcement)
   └──> v5.10.0c (#13 hot model swap; load-time WARN logic reused)

v5.9.5j (#6 auto-stamp + #17 design)
   └──> (no downstream dependents)

v5.10.0a (#1 grid search)
   └──> v5.10.0e (#3 drift detection; reuses sweep machinery for retrain)

v5.10.0b (FPN-e2e + MODEL_FORMAT_VERSION 6)
   └──> ALL future stamps need format=6+ (one-way migration)

v5.10.0d (FOREACH_TARGET)
   └──> v5.10.0a multi-horizon may want label-version-aware stamps

v5.10.0e (drift detection)
   └──> requires v5.10.0c (hot swap) for "auto-retire on drift" workflow
```

### Cross-plan conflict check (run before each ship)

When opening a plan to start coding, run this checklist:

- [ ] Files this plan touches — does any concurrent plan touch
      same file? If yes, sequence them or merge their scopes.
- [ ] Cfg fields this plan adds — do any conflict with existing
      cfg names (engine.cfg.example) or earlier-plan claims?
- [ ] Stamp body fields this plan adds — does the canonical-order
      position in the matrix above match the actual ship order?
- [ ] Test count claim — does it sum cleanly with existing total?
- [ ] Architectural invariants — has any prior ship landed work
      that invalidates this plan's assumptions?
- [ ] Branch state — is this plan still building on the right
      predecessor (e.g., v5.10.0a presumes v5.9 is merged to
      `experiment/per-core-sharding`)?

If any check flags YES, **update the master plan first** before
coding. The master is the source of truth.

### Master plan update protocol

When ANY sub-plan changes scope or shipping order:
1. Edit the affected sub-plan file.
2. Update this master's Integration Matrix to reflect the change.
3. Re-verify the cross-plan conflict checklist.
4. If a downstream plan is affected, edit it too (cascade).
5. Commit master + all touched sub-plans together.

**Smell test:** if a sub-plan's effort estimate doubles, or its
files-touched list grows by > 50%, the plan needs a re-readiness pass
before coding begins.

### Sub-plan review SLA

Per the v5.4 architectural-sprint discipline (CLAUDE_REVIEW.md
Check 11), every sub-plan must pass:

- `/readiness <plan-path>` — clean GREEN before coding
- `/parity-check` — narrow scope to the plan's surfaces (e.g.,
  `/parity-check stamp` for v5.9.5h, `/parity-check features` for
  v5.10.0b)
- Operator validation gate — explicit test steps after coding,
  before tag

Skip any of these and the master plan is silently lying about
deliverable readiness.
