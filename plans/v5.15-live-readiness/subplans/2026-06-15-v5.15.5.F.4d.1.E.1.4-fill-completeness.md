---
type: subplan-scaffold
ship_tag: v5.15.5.F.4d.1.E.1.4
anchor: pre-v5.15.5.F.4d.1.E.1.4
parent: 2026-05-28-v5.15.5.F.4d.1.E.1-foundation.md
predecessor: 2026-06-15-v5.15.5.F.4d.1.E.1.3-aggregator-coherence.md
successor: 2026-06-15-v5.15.5.F.4d.1.E.1.5-per-cluster-purity.md
status: scaffold
risk: HIGH
hot_path: true
spine: SPINE-1 (fill-completeness)
decisions_honored: [D-209, D-212, D-213, D-214, D-215, D-216]
td_closed: [TD-169, TD-202, TD-195]
guard_rows: [§4c-conservation-invariants, §4c-fill-path-normalization, ks_peak-source-purity, §4a-H1, §4a-H3, §4a-H9, §4a-H12]
source: plan_checks/2026-06-15-E.1-decomp-A-adjudication.md §2 (E.1.4) + the RESIDUAL-HOLE contract §3
---

# E.1.4 — fill-path completeness + single-owner-`disk_file` event-log

> SCAFFOLD (not a coding body). Meaningful enough to reason + sequence + seed the dive body; the full body is re-grounded against HEAD by a C-class pass when this leaf becomes the dive (per `plan-hierarchy-and-sub-master-decomposition.md` §3). All file:line are HEAD `572c978` and WILL drift — re-verify at dive.

## Scope

**SPINE-1 (fill-completeness). The capital-accounting completion ship — it rewrites ONLY the fill LEAF MATH inside E.1.3's already-proven plumbing.** Six sub-concerns under one rollback anchor (they share one inbound = E.1.3's coherent published snapshot, and one outbound = conservation holds; you cannot assert "fills half-work" at an intermediate seam, so they do not pre-split — a runtime-sizing call if it proves oversized, homed below as a note NOT a defer):

1. **`FillEvent<F>` definition** — the normalized fill POD: H1 (no heap), H12 (explicit `_padding` default-init), `Money` money fields, persists → H9 (wire-byte preservation). A 0-hit NEW symbol (the A-class code-read confirmed).
2. **The REAL `handle_*_fill` LEAF MATH** (rewritten inside E.1.3's frozen plumbing):
   - **A16 intra-session partial-accumulate** — weighted-avg re-entry + keep-slot-open, `order_complete`-gated.
   - **Class-46 SELL-partial fix** — `handle_sell_fill` (`OrderManager.hpp:1214`) currently calls `Portfolio_CloseSlot(&oms->portfolio, pslot, fill_price)` at `:1243` ignoring `fill_qty` → whole-closes a partial. Fix: close BY `fill_qty`. Closes RBP Class 46.
3. **A20 warm-tail replay** — WS-fill watermark + persist (REVERSED into `.E.1` per D-216) + venue-exact commission through reconcile + `GetMyTrades` pagination.
4. **A21 venue-net qty leg** — Σ venue-asset-qty reconcile + ledger-booked correction; NEVER the raw `balance = exchange_usdt` clobber (`Reconcile.hpp:608`) + `ks_peak` source-purity. (The DETECT-ONLY `ProcessReconcile` landed `.E.0.10` per D-216; this ship completes the venue-net reconstruction leg.)
5. **Single-owner-`disk_file` event-log rework** (TD-202, class-level — beyond the `.E.0.10` instance):
   - The writer thread OWNS open/close/rotate via a polled atomic; **Reset/Init SIGNAL it, never touch the `FILE*` cross-thread** (closes the 3-way Reset race + the FIX-2 create→publish TOCTOU).
   - `LoadFromDisk` body-integrity gate — wire the unused OMSEL02 `reserved` checksum word.
   - `OrderEvent` H21 bump IF per-fill data persists.
6. **TD-195 warm-restart exit-disarm** — verify subsumed / own (the disarm window from the wfa-1 finding).

## Spine

SPINE-1 of the three D-214 spines (purity / coherence / **fill**). SPINE-1 is downstream of SPINE-2 (coherence, E.1.3) by a HARD data dependency (the reconciler torn-read leg + A21 read E.1.3's coherent published snapshot, not racily off the drainer) and upstream of SPINE-3 (purity, E.1.5 governs on the fill-fed aggregate). D-212/213/214: this is the SPINE-1 reference application of the fill-completeness DESIGN_SPEC.

## TD-closed (one line each)

- **TD-169** — venue fill-parser data-loss / venue-exact commission (A20/A21).
- **TD-202** — class-level single-owner-`disk_file` (beyond the `.E.0.10` quiesce-first instance already landed).
- **TD-195** — warm-restart exit-disarm — verify subsumed (the disarm-window fix the wfa-1 witness inverts on).
- Closes RBP **Class 46** (the partial-fill / SELL-whole-close class).

## Locked decisions

- **D-209** — venue-net reconstruction (reconstruct each node's net position from venue truth, NOT fill-replay); sub-pool deferred.
- **D-212 / D-213 / D-214** — the fill real-handler folds to `.E.1` (asserted-not-designed at `.E.0.10`); SPINE-1 is the reference app; the INERT interim composes as E.1.3's stub.
- **D-215** — A20 (seed-don't-replay shape) + A21 (asset-agnostic STRUCTURE-not-exact cash-leg reconcile).
- **D-216** — A20/A21 legs; A20-persist REVERSED into `.E.1` (paper never consumes the watermark → it was vacuous as a do-now).

## §4 guard-rows this leaf fills

- **§4c conservation invariants** — `balance == start + Σrealized` holds through fill + reconcile.
- **§4c fill-path normalization** — Class 46 ENFORCED.
- **`ks_peak` source-purity** — CI / audit candidate (the peak derives from the ledger, not a clobbered exchange value).
- **§4a H1** — `FillEvent<F>` no-heap POD.
- **§4a H3** — the single-owner event-log uses a polled atomic, NOT `std::mutex`/`condition_variable` (the Reset/Init signal path).
- **§4a H9** — `FillEvent` + `OrderEvent` persist → wire-byte preservation; locale-pinned at emit; H21 bump if per-fill data persists.
- **§4a H12** — explicit `_padding` default-init on `FillEvent<F>` (byte-equivalence context).

## INBOUND seam (the RESIDUAL-HOLE contract — EXPLICIT, this is the DESTINATION side)

> **THE E.1.3 → E.1.4 RESIDUAL-HOLE CONTRACT (per the A-class adjudication §3 — stated identically on E.1.3's OUTBOUND).**

This ship INHERITS from E.1.3: **the complete, tsan-clean fill-consumption PLUMBING (ring-drain + snapshot-publish) + the INERT leaf reached inside `OrderManager_HandleFill` (`OrderManager.hpp:1450`).** The plumbing is FROZEN.

**E.1.4's inbound check (the contract clause this ship owns):**

- The `:1450` seam IS reached (E.1.3's test asserts "a fill COMMAND drains, dispatches, and hits the leaf" WITHOUT asserting math) **and the plumbing is FROZEN.**
- **This ship touches ONLY the leaf MATH inside `OrderManager_HandleFill`:1450** — the `handle_*_fill` bodies + `FillEvent`. It does **NOT** touch the drain shell (`OrderManager_Tick`, the phased-drain buckets, the single-producer ring, the published-snapshot ledger — all E.1.3's, all frozen).
- It also inherits E.1.3's coherent published snapshot (the reconciler torn-read leg + A21 read the snapshot, not the racing `oms->balance`) + `owner_node_id` (E.1.2, for fill attribution).

> If E.1.3 dropped one of the 4 M6 drainer responsibilities, a conservation break here would trace to a *plumbing* gap upstream — that is why E.1.3's M6 enumeration completeness is the contract's de-risk clause. **E.1.4 assumes the plumbing is sound and bisects ONLY the leaf math.**

## OUTBOUND seam (to E.1.5)

The **complete, conservation-correct fill path.** Specifically: partials accumulate (char-tests); SELL-partials reduce by `fill_qty` (not whole-close); warm-tail reachable + venue-exact-fee + idempotent; reconcile is ledger-booked + replay-reproducible (no raw-clobber); event-log single-owner (tsan + recovery test); conservation holds (`balance == start + Σrealized`). **The capital-accounting path is COMPLETE** — E.1.5 then governs (A34) on the fill-fed per-cluster aggregate.

## Design-shape (code-grounded; HEAD 572c978, WILL drift — re-verify at dive)

**The fill leaf (the ONLY math edit).** `OrderManager_HandleFill` (`OrderManager.hpp:1450`) dispatches via `g_fill_dispatch[]` (`:1304`) to `handle_sell_fill` (`:1214`) / `handle_buy_fill`. `handle_sell_fill` WRITES `oms->balance` (`:1249`) / `oms->realized_pnl` (`:1250`) / `oms->ks_peak_balance` (`:1252`). The Class-46 bug: `Portfolio_CloseSlot(&oms->portfolio, pslot, fill_price)` (`:1243`) ignores `fill_qty`. A16 adds weighted-avg re-entry + keep-slot-open. The INERT correct-form interim is at `:1508` (E.1.3's stub) — this ship makes it live.

**`FillEvent<F>` (NEW POD).** H1 (no heap) + H12 (`int<N>_t _padding<N> = 0;`) + `Money` money fields. Persists → H9.

**The event-log (single-owner rework).** `OrderEventLog` struct (`OrderEventLog.hpp:156`); `disk_file` (`:167`); `writer_thread_active` atomic (`:193` — the `.E.0.10` TD-202/Class-50 quiesce-first instance already in place). Rework: `OrderEventLog_Init` (`:209`) + `OrderEventLog_Reset` (`:509`) SIGNAL the writer (polled atomic), never touch `disk_file`. `OrderEventLog_LoadFromDisk` (`:551`) gains the body-integrity gate wiring the OMSEL02 `reserved` checksum word (`OrderEventLogFileHeader.reserved`, `:146` — currently `// future: checksum`).

**The reconcile leg.** `Reconcile.hpp` — the raw clobber surface is `r.exchange_usdt` (`:608`); the venue-net leg reconstructs Σ venue-asset-qty + books a ledger correction. `ProcessReconcile` is DETECT-ONLY (landed `.E.0.10`, D-216); this ship completes the booked-correction reconstruction + `GetMyTrades` pagination + `ks_peak` source-purity.

## Rollback anchor

`pre-v5.15.5.F.4d.1.E.1.4`. **A break here is a CONSERVATION-INVARIANT or fill-accounting break** — coherence (E.1.3) + rename (E.1.1) are upstream-CLOSED, so the break is unambiguously fill-math, isolated from the aggregator-race surface. `git bisect` lands on this anchor. (Per the contract: if a break instead traces upstream to a plumbing gap, that is an E.1.3 M6-enumeration failure surfaced here — the contract's de-risk clause is what keeps the seam clean.)

## Known folds it owns

- **The 6-sub-concern note (NOT a defer — a runtime-sizing home per cut-line principle 7):** if E.1.4 proves oversized at runtime, the natural further-split is `{FillEvent + A16 + Class-46}` | `{A20 + A21 reconcile}` | `{event-log}`. Homed HERE as a note. Do NOT pre-split (they share one inbound = the coherent snapshot, one outbound = conservation; "fills half-work" is not assertable at an intermediate seam).
- **A4 / TECH_DEBT-169 freeze-flag** — F-093 commission is parsed but DROPPED by `ApplyMissedFills`; the venue-exact-fee leg closes the drop (freeze the parse, then complete the booking).
- **Class 45 freeze-FLAG (NOT a fix here)** — `Position.original_tp` live `fill×(1+tp_pct)` ≠ replay `e.tp=intended_tp`; `original_sl` does NOT diverge. Disclaim, do not "fix" (A25 operator design-decision).
- **TD-195 disarm window** — the `cached_params.flags` warm-restart disarm (wfa-1 witness inverts on the fix); verify subsumed by the fill/reconcile rework or own it explicitly.

## Shape-match note (do NOT re-schedule landed work)

A20/A21/A17/NEW-1 already LANDED `.E.0.10` as PREDICATES / legs (D-215/216/217). This ship builds on the A20/A21 *predicates* (the legs, not the predicate) + the DETECT-ONLY `ProcessReconcile`. The fill-handler is assert-only / INERT at `.E.0.10` (`OrderManager.hpp:1508`) — the REAL handler folds HERE. The single-owner-`disk_file` quiesce-first Init landed `.E.0.10` (TD-202 instance, RBP Class 50); this ship does the CLASS-level rework (Reset/Init never touch `FILE*`; the LoadFromDisk integrity gate). Do NOT touch E.1.3's frozen plumbing.
