# /dod-audit re-audit report — v5.14.11 AMENDED — 2026-05-11

Surface: amended plan `plans/v5.14-foxml-port-and-maker/subplans/2026-05-08-v5.14.11-online-corr-update.md` + workspace `DESIGN_SPECS/` + new invariants (CLAUDE.md item 25, CLAUDE.local.md going-forward) + TECH_DEBT-032.

Mode: plan-mode re-audit; first-pass dod-audit findings cross-checked for resolution; new findings sought across expanded scope.

---

## Catalog ingested

21 patterns in `DESIGN_SPECS/` (verified by `ls *.md | grep -v README`); README catalog count claim "21 patterns" MATCHES filesystem.

Two genuinely NEW docs:
- `sliding-window-online-statistics-pattern.md` (16,509 bytes; first-app annotated v5.14.11.A)
- `avx512-byte-determinism-pattern.md` (17,275 bytes; first-app v5.11.7 / second-app v5.14.11.B)

One EXTENDED doc:
- `cfg-flag-eligibility-criteria.md` — appended "Cohort audit when new field has siblings (v5.14.11+)" section (~60 lines) at line 258-318.

New "Math kernel patterns" category in README (line 106-110); 4 new quick-discovery entries (lines 130-133). Catalog accuracy: MATCH.

---

## Summary table

| Pattern | Verdict in amended plan |
|---|---|
| x-macro-registry-with-presence-dispatch | MATCH (FOREACH_ML_CFG_FLAG + FOREACH_STAMP_BOUND_CFG used canonically) |
| autopopulate-pattern-for-production-caller-class | MATCH (STAMP_CFG_AUTOPOPULATE flows untouched; verified at StampBoundCfgRegistry.hpp:206) |
| pre-post-cfg-registry-split | N/A (not triggered by ridge_* cohort) |
| registry-tuple-as-single-source-of-truth | MATCH (4 FOREACH_ML_CFG_FLAG entries auto-flow to parser+GUI+override+stamp-binding) |
| heterogeneous-registry-pattern | MATCH (ML domain pre-existing; 4 new entries; uint16_t headroom OK) |
| cfg-flag-eligibility-criteria | MATCH (cohort audit triggered + applied to 4-field ridge cohort) |
| slow-path-gate-registry-pattern | MATCH (C3: MASK_RIDGE_ONLINE_CORR_ACTIVE PER_CORE scope; 9→10 entries, 6 bits headroom) |
| structural-fix-preferred-decision-framework | MATCH (sliding-window-by-design eliminates PARITY-018 class; C1 helper extraction eliminates Class 18 mirror) |
| bitmap-flag-api | MATCH (BITMAP_IS_SET + BITMAP_ANY usage at dispatch sites) |
| per-bit-per-core-override-pattern | MATCH (4 new entries inherit ML-domain per-core override automatically) |
| wire-format-byte-preservation-discipline | MATCH (3 emit_source flips DIRECT_FIELD→BITMAP_BIT with `BITMAP_IS_SET(...) ? 1 : 0` ternary normalization; preserves HMAC chain) |
| avx512-byte-determinism-pattern (NEW) | MATCH (3 sites enumerated + 7 rules + SHA-256 lock test; v5.11.7 reference pattern) |
| sliding-window-online-statistics-pattern (NEW) | MATCH (sum-of-squares form + bounded-input stability argument; first application) |
| postloadsetup-registry-pattern | N/A (D5 resolved: RidgeWeights_Init memset covers zero-init for appended fields) |
| audit-driven-pre-coding-gate | MATCH (re-audit pattern follows v5.14.10 precedent) |

**Pattern verdict count:** 13 MATCH / 0 PARTIAL / 2 N/A / 0 MISSED.

---

## Status update on previous findings

### HIGH-1 (slow-path-gate predicate missing) — RESOLVED

Amended plan Step 6 adds `MASK_RIDGE_ONLINE_CORR_ACTIVE` PER_CORE scope predicate to FOREACH_SLOW_PATH_GATE. Sister to existing `MASK_RIDGE_WITHIN_ACTIVE` (line 84) and `MASK_THOMPSON_ACTIVE`. Headroom verified: registry currently has 9 entries in uint16_t → 7 bits free; new entry brings to 10, 6 bits remaining.

### MEDIUM-2 (helper extraction for dual call sites) — RESOLVED

Amended plan Step 4 adds `RidgeBlender_BuildHistoryFromRing<F>` + `RidgeBlender_OnlineCycleStep<F>`. Both buy-side (StrategyParameters.hpp:996) and exit-side (:1195) call sites reduce to single `RidgeBlender_OnlineCycleStep<F>(...)` invocation. Class 18 mirror prevention by construction; matches v5.14.2.E.1 PostLoadSetup precedent. Call-site line numbers verified at grep (line 996 buy / line 1195 exit / `RidgeBlender_Compute` follow-ups at 1010 + 1213).

### Tension A (1k-cycle reset code smell) — RESOLVED

Caramel adopted (C) sliding-window Welford with sum-of-squares form. PARITY-018 eliminated by construction (no periodic reset → no reset-rebuild-bug class). Stability argument bounded by inputs × K=64 → ε × 10^-14 << PARITY 1e-9. Pattern documented in `sliding-window-online-statistics-pattern.md` with first-application annotation.

### Tension B (cfg-flag eligibility framework vs precedent) — RESOLVED

Caramel adopted (2) cohort migration: 4 ridge_* fields → MASK_ML_CFG_*. Eliminates intra-family inconsistency. Cohort-audit discipline codified in `cfg-flag-eligibility-criteria.md` new section + CLAUDE.local.md going-forward rule. Closes the eligibility-framework-vs-precedent gap by elevating cohort audit to first-class step.

---

## New findings

### LOW-1 — CLAUDE.md item 25 phrasing vs DESIGN_SPEC content: MATCH

Item 25 (CLAUDE.md:208) cites "7 rules + SHA-256 lock test template" — verified against avx512-byte-determinism-pattern.md (Rule 1 no `_reduce_*`; Rule 2 `_mm512_div_pd` not mul-by-reciprocal; Rule 3 `_mm512_fmadd_pd` matches gcc fusion; Rule 4 vector compute + scalar reduce; Rule 5 compile-time gate; Rule 6 SHA-256 snapshot lock test; Rule 7 subnormal preservation). 7 rules accounted for; lock test template at section "## The pattern (rules) — Rule 6". Item 25 application list "v5.11.7 (first); v5.14.11.B (second)" matches DESIGN_SPEC's reference-implementations section.

Item 25 phrasing is terser than items 19-24 — intentional per Caramel framing (TECH_DEBT-032 demonstrates the cleaner direction). No phrasing-vs-DESIGN_SPEC drift.

### LOW-2 — TECH_DEBT-032 two-layer framing: PRESERVED

TECH_DEBT-032 entry (TECH_DEBT.md:644) captures both layers correctly: Layer 1 trim items 19-24 (~6-12h mechanical refactor; each item becomes 3-5 sentences) + Layer 2 handoff-skill-managed context loading (~4-8h architectural change). Triggers + cost + cross-refs all present. Caramel framing 2026-05-11 quote preserved. Reciprocal-discipline cross-ref to "codify design principles" going-forward rule captured at line 657.

### LOW-3 — Sub-tag scope sanity check: REASONABLE

- .A (~350 LOC): RidgeOnlineState embed + sum-of-squares math + UpdateOnline + FinalizeCorrFromSums + BuildCorr refactor + helper extraction + drop dead last_compute_us + Welford-equivalence tests + SHA-256 cfg=0/cfg=1 baselines. Math-kernel-shape-change ship.
- .B (~250 LOC): AVX-512 for 3 sites + per-site annotations + SHA-256 scalar↔AVX-512 lock tests. SIMD-only ship (no math shape change; preserves bytewise to scalar baseline established in .A).
- .C (~150 LOC): Decision 4 cohort migration (4 FOREACH_ML_CFG_FLAG entries) + slow-path-gate predicate edits (2 migrate + 1 new) + 3 stamp-binding emit_source flips + branchless multi-flag dispatch + HOT_PATH_CHANGELOG + CHANGELOG + Version.hpp bump. Cohort migration ship.

Total ~750 LOC across 3 sub-tags. LOC estimates reasonable per category sizes; .A is biggest (math refactor + helper extraction + tests; ~350 LOC is plausible). Sub-tag dependencies hold: .A produces scalar reference path → .B mirrors it in AVX-512 → .C wires the cfg cohort. Dependency order MATCHES execution order. GREEN.

### LOW-4 — Cholesky AVX-512 (D) follows v5.11.7 discipline: VERIFIED

Plan Step 7 cites all 4 byte-determinism rules at the canonical Cholesky site:
- Rule 4 (vector mul + scalar reduce): `__m512d prod = _mm512_mul_pd(li, lj); ... double prod_arr[8]; _mm512_mask_storeu_pd(prod_arr, mask, prod); for (int k = 0; k < j; ++k) s -= prod_arr[k];` — preserves L-to-R order
- Rule 5 (compile-time gate): `#if defined(__AVX512F__) ... #else ... #endif` with explicit scalar fallback
- Rule 2 implied via `_mm512_div_pd` for any reciprocal-shape operation (not shown in snippet but called out in trade-off summary)
- Pattern application notes the 3 sites: diagonal `L[i][i]` (inner k-loop), forward solve `L y = μ`, back solve `L^T w = y`

All 3 sites get SHA-256 lock test per Rule 6 (PARITY-017 closure in test additions section).

### LOW-5 — Cohort migration Y3 dispatch byte-equivalence: VERIFIED

Plan Step 6 shows 3 FOREACH_STAMP_BOUND_CFG entries flipping from `DIRECT_FIELD` to `BITMAP_BIT` emit_source. Each uses `BITMAP_IS_SET(cfg.ml_cfg_flags, MASK_ML_CFG_X) ? 1 : 0` ternary normalization for byte-equivalence with prior `int 0|1` direct values. Pattern matches v5.14.9.F.2/F.5 + v5.14.10 Surprise 6 precedent. HMAC chain preservation discipline matches `wire-format-byte-preservation-discipline.md`.

Caveat: ensure the `emit_when` predicates inside the 3 entries (which currently read `cfg.ridge_within_horizon || cfg.ridge_across_horizons` direct truthiness at lines 103/105/107/109/111/138) get updated to `BITMAP_ANY(cfg.ml_cfg_flags, MASK_ML_CFG_RIDGE_WITHIN_HORIZON | MASK_ML_CFG_RIDGE_ACROSS_HORIZONS)` form — plan Step 6 shows this is included. MATCH.

### LOW-6 — Per-core override for ridge_* cohort: AUTO-FLOWS

4 new FOREACH_ML_CFG_FLAG entries inherit ML-domain per-core override capability automatically via PER_CORE_OVERRIDE_BITMAP_DOMAINS (ML domain pre-deployed). No new pattern-application gap introduced by scope expansion. Per-core override for cohort ridge_* is the same shape as per-core override for `confidence_enabled` (existing). MATCH.

### NO new pattern gaps from scope expansion

Expanded scope (refactored BuildCorr + AVX-512 Cholesky + cohort migration) introduces ZERO new pattern gaps. All net-new pattern applications already cataloged.

---

## Recommendations

### Address now (folded into amended plan)

All HIGH-1 / MEDIUM-2 / Tension A / Tension B resolved in plan amendment. No further folds needed pre-coding.

### Address during current ship

- Confirm `RidgeWeights_Init` memset extends to new appended online-state fields (D5 resolution; verify mechanical at code-time).
- Verify `BITMAP_ANY` predicate construction in 3 emit_when slots of FOREACH_STAMP_BOUND_CFG ridge_within_horizon / ridge_across_horizons / exit_blender_mode entries matches `BITMAP_IS_SET(...) ? 1 : 0` ternary semantics at codegen-time.

### Defer with TECH_DEBT entry

None new from this re-audit. TECH_DEBT-032 already captured pre-audit (CLAUDE.md context-management cleanup); v5.14.11 ships first, cleanup follows.

### CLEAN — no action

All 13 MATCH pattern applications represent correct application of the canonical shape. No drift, no half-applications.

---

## Verdict: GREEN

Amended plan resolves all 4 prior findings (HIGH-1 / MEDIUM-2 / Tension A / Tension B), introduces 0 new pattern-application gaps, and applies 2 net-new DESIGN_SPECS canonically (sliding-window-online-statistics + avx512-byte-determinism). All new invariants (CLAUDE.md item 25, CLAUDE.local.md cohort-audit rule) consistent with DESIGN_SPECS content. TECH_DEBT-032 correctly captures two-layer cleanup framing.

**Recommendation: .A kickoff approved.** Plan is structurally complete; no further amendments needed before coding starts.
