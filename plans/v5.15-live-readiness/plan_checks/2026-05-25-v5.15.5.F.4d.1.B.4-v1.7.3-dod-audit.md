---
type: plan-check
audit_kind: dod-audit-v1.7.3
target_ship: v5.15.5.F.4d.1.B.4 (v1.7.2 plan body + landed B.0/B.1/B.2 EngineCommon.hpp)
audit_date: 2026-05-25
auditor: claude (Opus 4.7 1M-context subagent)
predecessor_audits:
  - 2026-05-25-v5.15.5.F.4d.1.B.4-dod-audit-RE-AUDIT.md (v1.2 baseline; GREEN)
  - 2026-05-25-v5.15.5.F.4d.1.B.4-v1.6-precoding-audit-gate.md (v1.6)
  - 2026-05-25-v5.15.5.F.4d.1.B.4-v1.6-blindspot-scan.md (v1.6)
  - 2026-05-25-v5.15.5.F.4d.1.B.4-v1.7-blindspot-scan.md (v1.7)
  - 2026-05-25-v5.15.5.F.4d.1.B.4-v1.7-parity-check.md (v1.7)
  - 2026-05-25-v5.15.5.F.4d.1.B.4-v1.7-bug-check.md (v1.7)
  - 2026-05-25-v5.15.5.F.4d.1.B.4-v1.7.1-parity-check.md (v1.7.1; GREEN-WITH-3-MINOR-DRIFT)
verdict: YELLOW-WITH-1-CRITICAL
critical_count: 1
high_count: 3
medium_count: 5
low_count: 4
catalog_walked: 80+ DESIGN_SPECS across 11 subdirs (audit-methodologies/, concurrency-patterns/, data-disciplines/, doc-disciplines/, feature-patterns/, framework-patterns/, ledger-templates/, meta-disciplines/, plan-templates/, refactor-patterns/, wire-format-patterns/)
---

# /dod-audit — .B.4 v1.7.2 — comprehensive DESIGN_SPECS catalog walk

**Scope:** plan_path=`subplans/2026-05-24-v5.15.5.F.4d.1.B.4-train-serve-execution-layer-parity.md` v1.7.2 + landed code `CoreFrameworks/EngineCommon.hpp` (B.0 ApplyBnbDiscount + B.1 BootGlobal + B.2 BootPerCore bodies landed at WIP-checkpoint commit `09204b3` per plan body) + slow-path lambda source `CoreFrameworks/EngineSharded.hpp:3044-3311` (the B.3 SlowPathCycleOneCore extract target).

**Focus (per operator):** comprehensive DESIGN_SPECS catalog walk to catch the body-args enumeration gap class (third instance: v1.5 fabricated symbol → v1.7 scope/state mismatch → v1.7.3 body-args under-enumeration).

**Verdict: YELLOW-WITH-1-CRITICAL.** Catalog walk converged with operator framing: there IS a NEW substantive gap at v1.7.2 (B.3 helper signature misses 5 closure-captured externals from lambda body 3044-3311 + 6 rdtsc sampling locals from lambda preamble). This is the meta-gap class operator named — the third instance of "planning missed body-content detail" — and the SHAPE is "lambda-closure-capture enumeration vs free-function helper signature derivation."

Per `feedback_iteration_spiral_signals_audit_meta_gap`: three instances of related plan-body-content-detail gaps = META meta-discipline candidate (track for Stage 2 DRAFT codification at ship close).

---

## DESIGN_SPECS catalog ingested (80+ files)

Catalog walked dynamically per skill spec:

| Subdir | Count | Specs particularly relevant to .B.4 |
|---|---|---|
| audit-methodologies/ | 3 | audit-driven-pre-coding-gate (APPLIED) + audit-scope-taxonomy + audit-report-format |
| concurrency-patterns/ | 4 | concurrency-model-summary + phase-separated-drainer + spsc-ring-embedded-in-hot-struct + cross-thread-snapshot-publish |
| data-disciplines/ | 9 | cache-line-discipline + cache-layout-discipline-for-hot-side-structs + decision-first-cluster-layout + function-struct-alignment + per-snapshot-cluster + raii-destructor-with-cluster |
| doc-disciplines/ | 2 | categorical-triggers + file-size-split |
| feature-patterns/ | 3 | per-horizon-barrier-blending + runtime-toggleable-bench-gate + shadow-load-state-transition |
| framework-patterns/ | 32 | bitmap-flag-api + slow-path-gate-registry-pattern (APPLIED at B.3) + autopopulate + type-trait-dispatch (H13) + multi-action-registry-walker-family + manual-fields-inventory + heterogeneous-registry + sidecar-override + universal-cfg-field-registry + framework-composition-overview + canonical-sister-extension-discipline (sister doc references) + meta-registry + slot-state-foreach + sink-fn-pointer-for-optional + persisted-struct-with-ephemeral-field |
| ledger-templates/ | 1 | ledger-entry-templates |
| meta-disciplines/ | 6 | canonical-sister-extension-discipline (APPLIED) + structural-fix-preferred-decision-framework (APPLIED) + pattern-codification-lifecycle + implementation-layer-blindspot-taxonomy + train-serve-execution-layer-parity (APPLIED — M5 first canonical) + doc-frontmatter-convention |
| plan-templates/ | 4 | future-oriented-plan-template (APPLIED) + sprint-master-plan-template + design-spec-template + postmortem-template |
| refactor-patterns/ | 21 | shared-helper-extract-for-train-serve-mirror-close (APPLIED — first canonical) + orchestration-helper-with-pod-args (sister APPLIED) + branchless-dispatch-discipline (H7+H20; APPLIED at B.3 gating) + decision-time-data-binding (Class 27 reference) + structural-fix-preferred + branchless-math-kernel + cfg-flag-eligibility + cross-walker-struct-field-uniqueness + bitmap-overflow-protection + multi-bit-state-encoding + cfg-scope-discipline + generic-ring-buffer-template + failure-attribution-buffer |
| wire-format-patterns/ | 6 | wire-format-byte-preservation-discipline (H9; APPLIED) + struct-padding-determinism (H12) + avx512-byte-determinism (H10) + pre-post-cfg-registry-split + prng-choice-for-replay-determinism + wire-format-canonical-body-invariants-helper |

Doc-debt finding: 0 (all relevant specs have ## Trade-offs + apply-when sections).

---

## Per-DESIGN_SPEC verdict (relevant 24 — others N-A)

| Pattern | Verdict | Notes |
|---|---|---|
| `shared-helper-extract-for-train-serve-mirror-close` | **APPLIED** | First canonical at .B.4; 5 helpers as Stage 2 → Stage 3 first canonical. Plan body Decision A + Acceptance criteria cite directly. EngineCommon.hpp file-header doc block (line 6-7) cites the spec by name. |
| `orchestration-helper-with-pod-args` | **APPLIED-NEUTRAL** | Sister pattern; Decision A 5-helper structure matches (POD args via FPN<F> + struct refs + nullable *). Acknowledged at v1.6 4-pillar self-audit. |
| `canonical-sister-extension-discipline` | **APPLIED** | Plan body Decision A "Canonical sister registries considered" table satisfies plan-time discipline. v1.3 N4 5-helper split rationale cites `Sharded_ValidatePartialExitCfg` sister precedent. v1.7 D1-B selection cites `slow-path-gate-registry-pattern.md` sister-canonical fit. |
| `slow-path-gate-registry-pattern` | **APPLIED-AT-B.3a** | Plan body Step B.3a adds 1 row `X(ENGINE_WIDE, BREAKEVEN_ON_PROFIT, ...)` to FOREACH_SLOW_PATH_GATE per Decision D1-B. Verified actual registry at `CoreFrameworks/SlowPathGateRegistry.hpp:69-117` is 4-tuple `(scope, name, predicate, doc)` — plan body schema MATCHES. CRITICAL fix at v1.7.1 (ENGINE_WIDE → state.global_gate_state per canonical sister :2344 + :3558) is structurally sound. |
| `structural-fix-preferred-decision-framework` | **APPLIED** | M5 → Class 18 mirror cohort (7 PARITY findings) → Option D ARCHITECT (shared helper extract) is textbook structural fix at recurrence count ≥4. |
| `train-serve-execution-layer-parity` (M5) | **APPLIED — first canonical** | Plan body Decision D codifies M5 + MG-1 + MG-2 amendments; D1-D5 land at Phase D Step D.1-D.5 + Step D.6-D.10. |
| `pattern-codification-lifecycle` | **APPLIED** | D3 Stage 6 promotion of `feedback_verify_symbol_existence_at_plan_drafting_time` per § Stage 5/6 codification procedure. |
| `framework-composition-overview` | **N-A** | This ship doesn't change cfg composition topology. |
| `audit-driven-pre-coding-gate` | **APPLIED** | Phase A Step A.3 fires gate (DONE 2026-05-25); 3 parallel agent re-audits at v1.7 → v1.7.1 → v1.7.2. |
| `audit-scope-taxonomy` | **APPLIED** | This invocation = `plan <plan-path>` (plan-mode); per spec. |
| `branchless-dispatch-discipline` (H7+H20) | **APPLIED** | B.3 PARITY-032 gating per D1-B uses BITMAP_IS_SET on cached gate bit (cached at SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE entry; branchless cmov via X_AUTOPOP_ENGINE_WIDE_INCLUDE per landed registry pattern). v1.7 4-pillar self-audit confirms no new Class 28 hand-wave. |
| `cache-line-discipline` (H6) | **APPLIED-NEUTRAL** | Helper extract is boundary-stable refactor — operates on existing `alignas(64)`-decorated structs by reference. Per existing /dod-audit RE-AUDIT verdict at v1.2: `CoreContext<F>` `alignas(64)` cluster + `EventLoopState<F>` `alignas(64)` cluster + `OrderManagerState<F>` `alignas(64)` clusters all preserved. NO new struct layout introduced. |
| `bitmap-flag-api` | **APPLIED-NEUTRAL** | Helper bodies use BITMAP_IS_SET / CORE_STATE_FLAG_SET per established API. No new bitmap fields. |
| `bitmap-overflow-protection-discipline` | **APPLIED-VERIFIED** | New PARITY-032 row adds 1 bit to FOREACH_SLOW_PATH_GATE → 11/16 used → static_assert at `SlowPathGateRegistry.hpp:170` enforces ≤16. Plan body v1.7.2 cites "Bit budget: 10/16 + 1 new = 11/16 (safe)" — verified safe. |
| `decision-time-data-binding-pattern` (Class 27) | **APPLIED-IMPROVED** | BNB cfg mutation isolated to one-shot helper (ApplyBnbDiscount) prevents 0.75^N compounding accounting bug per /dod-audit v1.2 worked verification. No new scalar cfg-mirror caches introduced. Per-core decision-time binding (Order.effective_fee_rate) preserved by-construction since helper operates AT BOOT, not on Order lifecycle. |
| `type-trait-dispatch-via-tt-namespace` (H13) | **APPLIED-NEUTRAL** | No new typed-field dispatch. tt::Strategy_InitPerCore (existing pattern) used per landed BootPerCore body line 413-415. NO reinterpret_cast punning. |
| `wire-format-byte-preservation-discipline` (H9) | **APPLIED-VERIFIED** | Plan body Verification gate "HMAC byte preservation: wire-format byte-preservation invariants pass" + Pre-coding concern 3. Helper extract MUST NOT change cfg state read by stamp emit — boundary-stable refactor preserves. |
| `struct-padding-determinism-pattern` (H12) | **N-A** | No new structs in byte-equivalence contexts. |
| `avx512-byte-determinism-pattern` (H10) | **N-A** | No new SIMD kernels. |
| `multi-action-registry-walker-family` | **N-A** | This ship doesn't introduce new registry walker family. |
| `manual-fields-inventory-pattern` | **N-A** | No new exempted manual fields. |
| `heterogeneous-registry-pattern` | **N-A** | This ship doesn't introduce heterogeneous registry. |
| `sidecar-override-pattern-for-registry-auto-flows` | **N-A** | No new sidecars. |
| `multi-bit-state-encoding-pattern` | **N-A** | No new K-state field encoding. |

---

## Per-DOD-invariant verdict (H1-H20)

| Invariant | Verdict | Notes |
|---|---|---|
| H1 (no malloc/new/vector/string/function) | **APPLIED-VERIFIED** | EngineCommon.hpp landed body uses NO malloc/new/vector/string/function. ApplyBnbDiscount uses FPN_Mul (stack); BootGlobal uses EventLoopState_Init reference-based; BootPerCore uses CORE_STATE_FLAG_SET (bitmap). Per spec |
| H2 (no virtual/shared_ptr/unique_ptr on hot path) | **N-A** | Helpers are boot + slow-path only. |
| H3 (no mutex/condvar/sleep_for/pthread_rwlock) | **APPLIED-VERIFIED** | No sync primitives in EngineCommon.hpp landed body. |
| H4 (FPN<F=64> for accounting; no float/double on accounting paths) | **APPLIED-VERIFIED** | ApplyBnbDiscount uses FPN_Mul (line 154-155). BootPerCore ML branch uses FPN_ToDouble for cfg knob extraction at ConfidenceScorer_BindCompositeCfg (acceptable; display-bound conversion, NOT accounting math). PARITY-032 cohort plan body Step B.3 V2 reference uses double current_price + FPN_FromDouble at fee-floor compute — preserves existing semantic. |
| H5 (no scalar JSON/strstr/atof in parser hot path) | **N-A** | No parser changes. |
| H6 (cross-thread fields alignas(64); cluster by access pattern) | **APPLIED-VERIFIED** | All struct args (`EventLoopState<F>` + `OrderManagerState<F>` + `ExecutionCore<F>`) are existing alignas(64) clusters per /dod-audit v1.2 RE-AUDIT. Helper extract doesn't introduce new struct fields. |
| H7 (hot path BRANCHLESS for data-dependent dispatch) | **N-A** | Helpers are boot + slow-path only; hot path UNTOUCHED per plan frontmatter. |
| H8 (hot p99 ≤500ns; slow p99 ≤100μs) | **APPLIED-WITH-MITIGATION** | Plan body Step B.6 + Verification gate include slow-path latency micro-benchmark gate. PARITY-032 SET cohort adds +50-200ns per cycle (within ≤100μs budget). UNSET cohort: ~0ns added (cached bit BITMAP_IS_SET is 1-cycle). |
| H9 (wire-format byte preservation for HMAC-signed bodies) | **APPLIED-VERIFIED** | Pre-coding concern 3 + Verification gate; boundary-stable refactor preserves cfg state read by stamp emit. |
| H10 (AVX-512 SIMD MUST have bytewise-identical scalar fallback) | **N-A** | No SIMD kernels in this ship. |
| H11 (math kernels constant-iter + branchless) | **APPLIED-NEUTRAL** | ApplyBnbDiscount uses `for (int c = 0; c < MAX_EXECUTION_CORES; ++c)` (compile-time constant loop bound — branchless). BootGlobal Regime_Init loop same. |
| H12 (structs in byte-equiv contexts have explicit zero-init padding) | **N-A** | No new structs in byte-equivalence contexts. |
| H13 (tt:: type-trait dispatch; NO reinterpret_cast punning) | **APPLIED-VERIFIED** | Plan body Decision D1-C deferral rationale explicitly cites "type-erased dispatch (H13 violation)" as reason to defer uniform fn_ptr — discipline correctly observed. BootPerCore body uses tt::Strategy_InitPerCore. No reinterpret_cast in landed code. |
| H14 (NO C++ bitfield syntax; use MBS_*/BITMAP_*) | **APPLIED-VERIFIED** | All bitmap manipulation in landed EngineCommon.hpp uses BITMAP_IS_SET / CORE_STATE_FLAG_SET. |
| H15 (every X-macro registry has FOREACH_REGISTRY row) | **APPLIED-VERIFIED** | FOREACH_SLOW_PATH_GATE already enrolled at `MetaRegistry.hpp:105` per landed grep. Plan body Verification gate "H15 metaregistry: 3 unregistered registries enrolled" closes any debt. |
| H16 (every MetadataFlag has FOREACH_DERIVED_FILTER row OR exemption) | **N-A** | No new MetadataFlag bits. |
| H17 (PerCoreCfg<F> X-macro only; no manual field declarations) | **N-A** | No PerCoreCfg<F> changes in this ship. |
| H18 (custom-semantics via SIDECAR OVERRIDE; never parallel wide-variant registries) | **N-A** | No new sidecars or registries with custom semantics. |
| H19 (FOREACH_REGISTRY rows LEVEL>0 declare PARENT) | **APPLIED-VERIFIED** | FOREACH_SLOW_PATH_GATE row at MetaRegistry.hpp:105 has `LEVEL=1, PARENT=FOREACH_REGISTRY` — verified. |
| **H20** (branchless preferred for SP/HP data-dependent dispatch EVEN WHEN NOMINALLY SLOWER) | **APPLIED-VERIFIED** | D1-B PARITY-032 gating via cached gate bit (cmov via SLOW_PATH_GATE_AUTOPOPULATE) instead of inline cfg read. 4-pillar self-audit verifies no new Class 28 hand-wave; sister-instance of canonical wrapper pattern at `ControllerEventLoop.hpp:3799` in production since v5.14.9.B.0. |

**Net H1-H20 verdict:** all relevant invariants APPLIED-VERIFIED or APPLIED-NEUTRAL. No invariant violation introduced by .B.4.

---

## CRITICAL finding (1)

### CRIT-1 — Plan body Step B.3 helper signature MISSES 5 closure-captured externals + 6 lambda-preamble rdtsc locals from body extract range 3044-3311

**Severity:** CRITICAL (compile-time fail OR silent behavior change at coding-time)

**Surface:** `subplans/2026-05-24-v5.15.5.F.4d.1.B.4-train-serve-execution-layer-parity.md` Phase B Step B.3 + Decision H signature row + Pre-coding concern 1 (helper signature stability)

**Pattern:** This is the THIRD instance of the operator-named meta-class "planning missed body-content detail":
1. v1.5 D1: fabricated `OrderManager_RegisterCore` (symbol-existence)
2. v1.7 D1-B CRITICAL: scope/state mismatch (`state.cores[c].gate_state` → `state.global_gate_state`)
3. **v1.7.3 (THIS finding)**: body-args under-enumeration (helper signature 6 args + body needs 11 inputs)

**The catch:**

Plan body Step B.3 + Decision H spec the helper signature as:
```cpp
EngineCommon_SlowPathCycleOneCore(cfg, c, state, oms, price, ts_us)  // 6 args
```

But comprehensive grep of `EngineSharded.hpp:3044-3311` (the body extract source range cited in plan body Step B.3) reveals the body uses **5 closure-captured externals** that are NOT in the helper signature:

| Symbol | Origin | Body use site | Helper signature handling |
|---|---|---|---|
| `g_depth_shared` | Module-scope global (`EngineSharded.hpp:~510`); same in backtest? | Lines 3053-3057 (`active_idx + snapshots[].imbalance/.spread/.mid_price` reads inside `MASK_GATE_CFG_DEPTH_ENABLED` branch) | **MISSING** — neither cfg nor state args reach the global |
| `last_price` (atomic<double>) | Per-core slow thread closure capture (`EngineSharded.hpp:~2810` lambda preamble) | Line 3068 (`price_d = last_price.load(relaxed)`) | **MISSING** — helper takes `FPN<F> price` (caller would need to do the atomic load) |
| `last_volume` (atomic<double>) | Same as above | Line 3069 (`volume_d = last_volume.load(relaxed)`) | **MISSING** — no volume arg |
| `ticks_produced` (atomic<uint64_t>) | Producer-thread shared atomic captured by all slow threads | Line 3130 (`pp_now_tick = ticks_produced.load(relaxed)` for seqlock publish) | **MISSING** — no ticks_produced arg |
| `now_tick` (uint64_t) | Lambda-preamble local (line ~2800; `ticks_produced.load(acquire)`) | Line 3243 (passed to `EventLoop_TimeExitOneCore`) | **MISSING** — `ts_us` arg is different (microseconds wall-clock vs producer-tick-count) |

Plus **6 lambda-preamble rdtsc-sampling locals** declared OUTSIDE 3044-3311 range:

| Local | Origin | Body use site | Handling |
|---|---|---|---|
| `_sp_t0` | Lambda preamble (`__rdtsc()` at slow-path entry) | Line 3276-3278 (CoreLatencyStats_Sample for entire cycle) | **MISSING** — helper would need re-declare OR drop sample |
| `_sec_t_other_start` | Lambda preamble (`= _sp_t0`) | Line 3096 (UpdateRolling section sample start delta) | **MISSING** |
| `_sec_t_rebuild_start` | Body line 3093 (in-range; OK) | Line 3120-3121 (rebuild section sample) | OK (in-range) |
| `_sec_t_push_start` | Body line 3118 (in-range; OK) | Line 3142-3143 (push section sample) | OK (in-range) |
| `_sec_t_te_start` | Body line 3140 (in-range; OK) | Line 3247-3249 (time-exit section sample) | OK (in-range) |
| `_sec_t_tsl_start` | Body line 3246 (in-range; OK) | Line 3264 (trail-SL section sample) | OK (in-range) |
| `_sec_t_tail` | Body line 3262 (in-range; OK) | Line 3265 (tail sample) | OK (in-range) |

Net: 5 closure-captured externals + 2 lambda-preamble locals (`_sp_t0` + `_sec_t_other_start`) = **7 inputs not in plan signature**.

**The actual ImplementationFn signature would need (~11 args) OR equivalent structural reshape:**

```cpp
// Option α (faithful 1:1 extract; 11 args):
template <unsigned F>
void EngineCommon_SlowPathCycleOneCore(
    const ControllerConfig<F>& cfg,
    int c,
    EventLoopState<F>& state,
    OrderManagerState<F>& oms,
    FPN<F> price,                                          // (was passed in plan v1.7.2)
    uint64_t ts_us,                                        // (was passed in plan v1.7.2)
    double last_price_d,                                   // NEW — caller does last_price.load(relaxed)
    double last_volume_d,                                  // NEW — caller does last_volume.load(relaxed)
    uint64_t pp_now_tick,                                  // NEW — caller does ticks_produced.load(relaxed)
    uint64_t now_tick,                                     // NEW — caller does ticks_produced.load(acquire) at slow-path entry
    DepthSharedState* depth_shared,                        // NEW — module-scope global; backtest provides own
    uint64_t _sp_t0,                                       // NEW — rdtsc sample anchor (or drop sampling from helper body)
    uint64_t _sec_t_other_start                            // NEW — rdtsc sample anchor (or drop)
);
```

OR **Option β** (drop rdtsc sampling from helper; backtest doesn't use it; keep ~9 args + caller-side sampling):

```cpp
// Option β (~9 args; rdtsc sampling stays in caller):
template <unsigned F>
void EngineCommon_SlowPathCycleOneCore(
    const ControllerConfig<F>& cfg,
    int c,
    EventLoopState<F>& state,
    OrderManagerState<F>& oms,
    FPN<F> price,
    uint64_t ts_us,
    double last_price_d,
    double last_volume_d,
    uint64_t pp_now_tick,
    uint64_t now_tick,
    DepthSharedState* depth_shared
);
// Caller wraps with rdtsc samples around helper call
```

OR **Option γ** (POD args struct per orchestration-helper-with-pod-args pattern; recommended for >6 args):

```cpp
template <unsigned F>
struct SlowPathCycleInputs {
    FPN<F> price;
    uint64_t ts_us;
    double last_price_d;
    double last_volume_d;
    uint64_t pp_now_tick;
    uint64_t now_tick;
    DepthSharedState* depth_shared;
};

template <unsigned F>
void EngineCommon_SlowPathCycleOneCore(
    const ControllerConfig<F>& cfg,
    int c,
    EventLoopState<F>& state,
    OrderManagerState<F>& oms,
    const SlowPathCycleInputs<F>& inputs
);
```

**Why this is CRITICAL not HIGH:**

- The plan body Step B.3 explicitly says "Extract per-core slow-path-cycle body from `EngineSharded.hpp:3036-3312`" — but the cited range has external dependencies the signature can't reach
- Coder following plan v1.7.2 verbatim WILL hit compile error at first `g_depth_shared` reference inside helper body (helper file `EngineCommon.hpp` doesn't include `EngineSharded.hpp`; no transitive access to module-scope global)
- OR coder will silently introduce a NEW global include from EngineCommon.hpp → EngineSharded.hpp → header cycle catastrophe (Phase A Step A.6.5 trial compile gate would catch but only AFTER body landed)
- OR coder will silently use a non-canonical alternative path (e.g., bypassing depth-share read entirely → silent feature_matrix drift in backtest cohort using `MASK_GATE_CFG_DEPTH_ENABLED`)

**Suggested fix:**

Plan body amendment v1.7.3:
1. **Update Step B.3 + Decision H + Pre-coding concern 1** to use Option γ (POD args struct) per `orchestration-helper-with-pod-args-pattern.md` sister pattern (already cited as PATTERN SISTER in plan body Canonical sister table). Sister precedent: `tt::Stamp_AssembleAndEmit<F>(path, secret, cfg, args)` — same shape for >6 args.
2. **Update Step C.3 + C.4** caller migration sites to populate `SlowPathCycleInputs` struct from caller-scope locals before invoking helper:
   ```cpp
   // LIVE per_core_slow thread:
   SlowPathCycleInputs<F> inputs;
   inputs.price            = mtm_price;
   inputs.ts_us            = rebuild_ts_us;
   inputs.last_price_d     = last_price.load(std::memory_order_relaxed);
   inputs.last_volume_d    = last_volume.load(std::memory_order_relaxed);
   inputs.pp_now_tick      = ticks_produced.load(std::memory_order_relaxed);
   inputs.now_tick         = now_tick;
   inputs.depth_shared     = &g_depth_shared;
   EngineCommon_SlowPathCycleOneCore(cfg, c, state, oms, inputs);
   
   // BACKTEST per-tick (in ShardedBacktest_RunTick callback):
   SlowPathCycleInputs<BACKTEST_FP> inputs;
   inputs.price            = price_fpn;
   inputs.ts_us            = ts_us;
   inputs.last_price_d     = price_d;             // backtest synthesizes from replay
   inputs.last_volume_d    = volume_d;
   inputs.pp_now_tick      = 0;                   // backtest doesn't have producer ticks
   inputs.now_tick         = now_tick;            // backtest synthesizes
   inputs.depth_shared     = &backtest_depth_shared;  // backtest's own DepthReplayState bridge
   EngineCommon_SlowPathCycleAllCores(cfg, state, oms, inputs);
   ```
3. **Update Step B.3 rdtsc sampling guidance** — keep `_sp_t0` + `_sec_t_other_start` declarations in CALLER scope (live + backtest); WRAP helper call with sampling. Backtest may skip sampling (LATENCY_PROFILING build flag) per existing conditional compile pattern.
4. **Update Phase A Step A.4 boot CSV** to ALSO produce a slow-path-lambda body-args CSV at coding time (already flagged at v1.7 M1+M6 + at v1.7.2 as "Step B.6 NEW per M1+M6 sister CSV: Slow-path lambda body enumeration at coding time" — but this CRIT shows the enumeration SHOULD happen NOW at plan-time, not coding-time, per the same logic that Phase A Step A.4 enumerates boot CSV at plan-time).

**Effort estimate:** ~30-60 min plan body amendment (4 sections updated) + ~5 min create slow-path-lambda body-args CSV.

**Cross-reference:**

- Operator-named meta-class "planning missed body-content detail" — track as Stage 2 DRAFT META-discipline candidate at ship close. Three instances cohort signal per `feedback_iteration_spiral_signals_audit_meta_gap`. Memory candidate: `feedback_verify_body_content_at_plan_drafting_time` (sister to `feedback_verify_symbol_existence_at_plan_drafting_time` Stage 6 + extends Phase A Step A.4 boot enumeration discipline to ALL extracted-body ranges — not just boot).
- Sister pattern: `orchestration-helper-with-pod-args-pattern.md` (Option γ application).
- Sister discipline: `feedback_enumerate_consumers_before_registry_row_deletion` is the CONSUMER side; this CRIT is BODY-CONTENT side (helper EXTRACT enumeration).

---

## HIGH findings (3)

### HIGH-1 — Plan body Pre-coding concern 11 (bytewise-identical math preservation per O2) does NOT cover lambda-body extract scope

**Severity:** HIGH (preserves correctness but missed coverage class)

**Surface:** Plan body v1.6 O2 amendment + Pre-coding concern 11

**Symptom:** v1.6 O2 codified "bytewise-identical math preservation at structural-fix extract" specifically for `core_balance` precompute (caller-side vs helper-internal). This is GOOD discipline. But the discipline was scoped to BootPerCore (Phase A Step A.4 boot CSV body); it was NOT applied to SlowPathCycleOneCore (Phase B Step B.3 lambda-body extract).

**Worked example:** The lambda body at 3068-3071 has:
```cpp
double price_d = last_price.load(std::memory_order_relaxed);
double volume_d = last_volume.load(std::memory_order_relaxed);
FPN<F> mtm_price = price_d > 0.0 ? FPN_FromDouble<F>(price_d) : FPN_Zero<F>();
```

If helper takes `FPN<F> price` arg, caller must do the `price_d > 0.0 ? FPN_FromDouble<F>(price_d) : FPN_Zero<F>()` precompute (bytewise-identical per O2 discipline). LIVE caller would do it; BACKTEST caller would do it. Plan body doesn't enumerate this discipline application at lambda-body scope.

**Suggested fix:** Extend Pre-coding concern 11 to ALL body-extract scopes (boot + slow-path-cycle): "preserve all arithmetic at caller-side OR prove helper-internal recompute is bytewise-identical." Apply specifically at Step C.3 + C.4 caller migration: enumerate all pre-helper computations preserved verbatim from lambda body.

**Effort estimate:** ~10 min Pre-coding concern 11 expansion.

**Cross-reference:** `feedback_v1.6_bytewise_identical_math_preservation` (memory) + sister to CRIT-1.

### HIGH-2 — Plan body Phase A Step A.6.5.b "header-cycle trial compile" verification does NOT include the EngineCommon.hpp → DepthSharedState (or equivalent for g_depth_shared) include path

**Severity:** HIGH (risk class is real; v1.7.1 M3.B intent was right but coverage incomplete)

**Surface:** Plan body Phase A Step A.6.5.b (v1.7.1 M3.B expansion)

**Symptom:** v1.7.1 M3.B correctly identified header-cycle risk + added trial compile in 6 build dirs. But the trial uses minimal `tests/test_engine_common_header.cpp` that ONLY includes EngineCommon.hpp + `main(){return 0;}`. This catches direct cycles. But the CRIT-1 finding (g_depth_shared closure-captured global) would force EngineCommon.hpp to include something like `#include "DataStream/BinanceDepth.hpp"` (where DepthSharedState lives) OR receive depth_shared as POD arg per Option γ. Plan body doesn't enumerate the include topology decision.

**Suggested fix:** Phase A Step A.6.5.a additional verification: enumerate ALL closure-captured externals + identify their home headers; verify EngineCommon.hpp transitive include topology BEFORE coding. Pair with CRIT-1 Option γ POD args struct (which makes include topology trivial — POD args struct in EngineCommon.hpp has no external dependency).

**Effort estimate:** ~10 min Step A.6.5.a expansion.

**Cross-reference:** Decouples from CRIT-1 only if Option γ adopted.

### HIGH-3 — Plan body Step C.4.5 (PARITY-031 backtest regime sample) does NOT verify the slow-path-cycle body now produces per-core regime BEFORE the feature collector reads it

**Severity:** HIGH (sequencing risk in backtest path)

**Surface:** Plan body Phase C Step C.4.5 + Decision F

**Symptom:** Decision F + Step C.4.5 specify: backtest feature collector reads `state.cores[BACKTEST_REGIME_SAMPLE_CORE].regime_state.current_regime` AFTER deleting `fc_ctx.regime_state` allocation. But the per-core regime IS POPULATED inside SlowPathCycleOneCore body (lambda 3105-3115 calls `EventLoop_RebuildOneCore` which writes `state.cores[c].regime_state`). 

Backtest sequencing per Step C.4 is: `EngineCommon_SlowPathCycleAllCores(cfg, state, oms, price, ts_us)` then feature collector reads regime. The wrapper fans N times — but does AllCores complete ALL cores before feature collector reads? Verify: backtest per-tick callback ordering at `ShardedBacktest_RunTick` (the call site) — does it invoke AllCores BEFORE the feature collector loop, or interleave?

If interleaved, feature collector at iteration c=k may read `state.cores[0].regime_state` which was last updated at tick t-1 (stale). If sequential (AllCores then feature collector), the read is fresh.

**Suggested fix:** Plan body Step C.4.5 amendment: verify the call ordering at `ShardedBacktest_RunTick` callback. Add explicit sequencing note: "AllCores wrapper MUST complete BEFORE feature collector reads `state.cores[BACKTEST_REGIME_SAMPLE_CORE].regime_state.current_regime`. Verify at coding time via callback structure inspection."

**Effort estimate:** ~10 min plan body amendment + ~10 min `ShardedBacktest_RunTick` callback inspection at coding-time.

**Cross-reference:** Decision F.

---

## MEDIUM findings (5)

### MED-1 — Plan body Decision A 5-helper table cites `EngineCommon_SlowPathCycleAllCores` per Option D "viewer decoupling boundary" but doesn't enumerate POD args impact for v6.0 viewer attach surface

**Severity:** MEDIUM (future-orientation hand-wave)

**Surface:** Plan body Decision A + Decoupling roadmap breadcrumb section

**Symptom:** Plan body Decoupling section says "EngineCommon_SlowPathCycleAllCores becomes the mmap-publish boundary (viewer reuses wrapper)" — but if signature includes 5 closure-captured externals (per CRIT-1), viewer would need to populate those from its own context (which may differ from engine context — viewer is observation only). Per Option γ POD args struct: viewer populates struct + calls; per Option α/β raw args: viewer's call site is more brittle.

**Suggested fix:** If Option γ adopted (POD args struct), document at Decoupling section that the struct IS the viewer-attach contract. If Option α/β, note that viewer-attach surface may need a thin wrapper. Either way, codify decision in plan body.

**Effort estimate:** ~5 min Decoupling section note.

### MED-2 — Plan body Step B.6 "slow-path latency micro-benchmark" verification gate doesn't account for closure-capture indirection cost

**Severity:** MEDIUM (perf assumption gap)

**Surface:** Plan body Phase B Step B.6 + Verification gate Slow-path latency-neutral micro-benchmark row

**Symptom:** Plan body says "SlowPathCycleOneCore helper-inlined generated code identical to current inline body at -O2 (objdump diff)." But inline lambda bodies and free-function template instantiations may differ slightly at -O2: closure captures become struct field reads vs free-function arg passes (different addressing modes). For a 12-arg POD-struct call (Option γ), the call site needs to construct the struct (~5-10ns vs lambda's zero) before invoking helper.

**Suggested fix:** Plan body Step B.6 amendment: clarify that "bytewise-identical generated code" means EITHER bytewise-identical OR within ≤5ns variance for arg-passing setup (acceptable per H8 budget). objdump diff on the helper body itself; treat caller-side arg setup as separate measurement.

**Effort estimate:** ~5 min wording amendment.

### MED-3 — Plan body Step C.4 DELETE enumeration doesn't include `EventLoop_BreakevenOnProfit` wrapper at `ControllerEventLoop.hpp:3796-3804` correctly (already addressed at v1.7.1 M1.B but not verified)

**Severity:** MEDIUM (verified via grep at v1.7.1 M1.B but wrapper deletion semantic deserves double-check)

**Surface:** Plan body Phase C Step C.4 last bullet (v1.7.1 M1.B addition)

**Symptom:** Plan body says "DELETE wrapper OR annotate as kept-for-tests if any test invokes directly. Verify via `grep "EventLoop_BreakevenOnProfit\b"` post-C.4 returns ZERO non-test sites." Verify NOW (not coding-time): grep the codebase to confirm wrapper currently has 1 non-test consumer (centralized backtest arch at `EngineSharded.hpp:1928-1954` per Step C.4) AND zero unit-test direct calls.

```
$ grep -rn "EventLoop_BreakevenOnProfit\b" /home/caramel/code/FoxML_Trader_v2/
ControllerEventLoop.hpp:3796:inline void EventLoop_BreakevenOnProfit(EventLoopState<F>* state,
ControllerEventLoop.hpp:3802:        EventLoop_BreakevenOnProfitOneCore(state, cfg, current_price, c);
```

Only 1 site (definition); no consumers visible to grep. Wrapper is dead code AT HEAD `09204b3` (no callers). After D1-B PARITY-032 closure with cached gate bit + Step C.3 SlowPathCycleOneCore migration, wrapper stays dead. Plan body M1.B is correctly identifying this.

**Status:** ALREADY-OK; advisory note only.

### MED-4 — Plan body Step D.6 CI tool pattern enumeration (v1.7.1 M4.B) doesn't include closure-captured external grep patterns

**Severity:** MEDIUM (CI tool gap class)

**Surface:** Plan body Phase D Step D.6 (v1.7.1 M4.B expansion)

**Symptom:** v1.7.1 M4.B enumerated patterns for fabricated symbol catch (cfg.* / MASK_* / FOREACH_* / Function_Name / file:line / state.cores[c].* etc.). But doesn't include closure-captured external symbols pattern (`g_<lowercase>` / `last_<lowercase>` / atomic_X.load() patterns). These would have caught the CRIT-1 catch class — body cites `last_price.load(relaxed)` but helper signature has no `last_price` arg.

**Suggested fix:** Step D.6 CI tool pattern enumeration amendment: add `[a-z_]+\.load\(` (atomic load() calls — verify the loaded value reaches helper signature OR is computed at caller from helper args). Add `g_[a-z_]+` (module-scope globals — verify the global is reachable via helper transitive includes OR passed as POD arg).

**Effort estimate:** ~5 min Step D.6 amendment.

### MED-5 — Plan body Verification gate "Bytewise-identical math preservation (v1.6 NEW per O2)" only covers default_per_core cohort

**Severity:** MEDIUM (verification gate coverage gap)

**Surface:** Plan body Verification gate row "Bytewise-identical math preservation"

**Symptom:** Verification gate row says "Phase C migrated callers (LIVE + BACKTEST) both have identical 12-line precompute block for total_balance + default_per_core + per-iter core_balance." But doesn't extend to slow-path-cycle precompute (per HIGH-1 finding): `price_d > 0.0 ? FPN_FromDouble<F>(price_d) : FPN_Zero<F>()` mtm_price precompute, depth section reads when `MASK_GATE_CFG_DEPTH_ENABLED`, etc.

**Suggested fix:** Verification gate row expansion: "Phase C migrated callers (LIVE + BACKTEST) preserve all pre-helper precompute math bytewise-identically across BOTH boot precompute (core_balance) AND slow-path-cycle precompute (mtm_price / depth-section / pp_now_tick / now_tick). grep verifies pre-helper math identical pre-vs-post `.B.4`."

**Effort estimate:** ~5 min Verification gate row expansion.

---

## LOW findings (4)

### LOW-1 — `BACKTEST_REGIME_SAMPLE_CORE` constant location: in EngineCommon.hpp:129 but doc says "Limits.hpp named constants pattern"

**Severity:** LOW (precedent reference clarity)

**Surface:** Plan body Decision F + Cross-references section (v1.3 amendment)

**Symptom:** Plan body says "N1 future-readable constant precedent: `Limits.hpp` named constants pattern." But the constant landed at EngineCommon.hpp:129 (verified at landed code), not Limits.hpp. The decision is fine (constant belongs at its consumer site); precedent reference should be updated for accuracy.

**Suggested fix:** Update Cross-references section: "N1 precedent: `Limits.hpp` named-constants discipline (constants near their consumer or in shared utility header)." OR re-think: should constant live in Limits.hpp for global visibility? Operator decision; LOW priority.

### LOW-2 — Plan body Step B.3a includes legacy comment about "v1.7.2 L-2 corrected" but the corrected text now seems internally inconsistent

**Severity:** LOW (doc-debt internal-consistency)

**Surface:** Plan body Phase B Step B.3a (after the macro snippet)

**Symptom:** The "(v1.7.2 L-2 corrected: was `MASK_WS_DEAD_TIME_FLATTEN_ENABLED` which is the cfg-side bit; the gate-side sister is `MASK_WS_FLATTEN_ACTIVE`)" parenthetical is inside the macro doc block. The macro itself uses `MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT` correctly. Parenthetical is INTERNALLY CONSISTENT but harder to parse for cold-pickup.

**Suggested fix:** Move L-2 correction parenthetical OUT of macro doc-block; place in separate "Sister consumer pattern context" sub-bullet.

### LOW-3 — EngineCommon.hpp landed body file-header doc block (line 30-39) "CONST-CORRECTNESS DISCIPLINE" cites `Sharded_ValidatePartialExitCfg` sister precedent

**Severity:** LOW (verify-actually-exists)

**Surface:** `CoreFrameworks/EngineCommon.hpp` line 38

**Symptom:** Comment cites "Sharded_ValidatePartialExitCfg (existing one-shot cfg validator; same shape)" — verify the function actually exists.

```
$ grep -rn "Sharded_ValidatePartialExitCfg" /home/caramel/code/FoxML_Trader_v2/
[NEEDS VERIFICATION]
```

**Suggested fix:** Verify at coding-time grep that `Sharded_ValidatePartialExitCfg` exists at cited location. Plan body cites `ControllerEventLoop.hpp:1140` per /dod-audit v1.2 verification — accept the prior verification but add this LOW to a coding-time double-check.

### LOW-4 — Plan body status line v1.7.2 amendment table M-1 says "fabricated macro name" was caught but the FIX-COMMIT for SlowPathGateRegistry shows the macro IS `SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE` (verified line 236)

**Severity:** LOW (consistent verified)

**Surface:** Plan body v1.7.2 amendment row M-1

**Status:** ALREADY-OK; landed registry confirms `SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE` is the correct macro name (line 236 of SlowPathGateRegistry.hpp). v1.7.2 M-1 fix was correct.

---

## META-recommendation: NEW DESIGN_SPECS candidates from this audit

### Candidate Stage 2 DRAFT — `body-content-enumeration-at-plan-time-discipline` (NEW)

**Class signal:** Three instances of "planning missed body-content detail" at .B.4:
1. v1.5 D1: fabricated symbol citation
2. v1.7 D1-B: scope/state struct mismatch
3. v1.7.3 CRIT-1 (THIS audit): body-args under-enumeration

Per `feedback_iteration_spiral_signals_audit_meta_gap` (3+ cycles = META-gap signal), this warrants Stage 2 DRAFT codification.

**Sister/parent disciplines:**
- `feedback_verify_symbol_existence_at_plan_drafting_time` (Stage 6 per v1.7 D3 promotion; SYMBOL EXISTENCE side)
- `feedback_enumerate_consumers_before_registry_row_deletion` (Stage 6; CONSUMER ENUMERATION side, registry-row + struct-member sub-rules)
- v1.6 O2 "bytewise-identical math preservation at structural-fix extract" (math side)
- THIS NEW: BODY-CONTENT side — when extracting helper from inline/lambda body, enumerate ALL body inputs (closure captures + module-scope globals + atomic shared state + lambda-preamble locals + rdtsc samples) BEFORE finalizing signature

**Trigger:** Any plan proposing function-body extract from inline/lambda body OR per-thread shared state context. Run comprehensive grep for: closure captures + atomic loads + module-scope globals + lambda-preamble locals + rdtsc samples. Compare against proposed helper signature. Audit signal: any external symbol in body range NOT in signature = scope-creep risk.

**Codification path:**
- Stage 2 DRAFT now (`.B.4` ship close codifies)
- Stage 3 first canonical = `.B.4` body extract uses Option γ POD args struct + slow-path-lambda body-args CSV at plan-time (not coding-time)
- Stage 4 cohort migration = next ship that extracts helper from lambda body applies same procedure
- Stage 5+ promotion = if pattern recurs across 2+ ships → CLAUDE.md item

**File location:** `DESIGN_SPECS/refactor-patterns/body-content-enumeration-at-plan-time-discipline.md` (would be NEW file at ship close)

**Memory codification:** `feedback_verify_body_content_at_plan_drafting_time` (NEW memory at ship close; sister to `feedback_verify_symbol_existence_at_plan_drafting_time`)

### Candidate /readiness Check 33 (NEW)

Sister to v1.7 D3 NEW Checks 31 + 32:

**Check 33: Body-content enumeration verified** — for any plan proposing function-body extract from inline/lambda body, verify ALL closure captures + module-scope globals + atomic shared state + lambda-preamble locals are enumerated in helper signature OR explicitly handled per Option α/β/γ pattern. Audit by grepping body extract range for external symbol patterns + cross-checking against helper signature.

---

## Verdict + recommendations

### Verdict: YELLOW-WITH-1-CRITICAL

CRIT-1 (body-args under-enumeration) is BLOCKING for Phase B Step B.3 body coding unlock. Plan body amendment v1.7.3 required before coding.

HIGH-1/2/3 are RECOMMENDED at v1.7.3 amendment cycle but not strictly blocking.

MED + LOW are advisory; can be folded into v1.7.3 amendment OR deferred to coding-time/ship-close.

### Recommendations (operator triage menu)

**RIGHT-NOW (v1.7.3 plan body amendment cycle):**
- CRIT-1 fix: adopt Option γ (POD args struct) per orchestration-helper-with-pod-args sister pattern; amend Step B.3 + Decision H + Pre-coding concern 1 + Step C.3/C.4 caller migration + Step B.6 verification + Phase A Step A.4 boot CSV → add slow-path-lambda body-args CSV
- HIGH-1 fix: extend O2 bytewise-identical math preservation discipline to lambda-body extract scope
- HIGH-2 fix: extend Phase A Step A.6.5.a include-topology verification
- HIGH-3 fix: verify ShardedBacktest_RunTick callback ordering at plan-time
- META: codify Stage 2 DRAFT body-content-enumeration discipline + memory at ship close (no plan body change needed; ship-close action)

**DEFERRED-OR-CODING-TIME:**
- MED-1/2/4/5 — fold into v1.7.3 amendment if time allows; otherwise defer
- MED-3 — verified at audit; no action needed
- LOW-1/2/3 — coding-time double-check
- LOW-4 — verified; no action

**DEFERRED-PER-FUTURE-HEADACHE-VS-OPTIMIZATION:**
- N/A — all findings here are anti-pattern survival (CRIT-1 = compile-time fail; HIGH-1/2/3 = correctness gap risk class); per `feedback_future_headache_vs_optimization_scope_framework`, these are future-headache class (close at ship)

### Operator next-step menu (per `feedback_consult_on_audit_findings` + `feedback_proportionate_response_to_audit_findings`):

**Option A** — Apply v1.7.3 amendments now (CRIT-1 + HIGH-1/2/3 + META codification queued for ship close). Re-fire /parity-check + /dod-audit at v1.7.3 before Phase B Step B.3 unlock. Estimated 60-90 min for amendments + 15-30 min audit re-fire.

**Option B** — Apply ONLY CRIT-1 fix at v1.7.3; defer HIGH-1/2/3 to coding-time discovery (risk: may surface again at coding-time as smaller catches → micro-iteration spiral). Estimated 30-45 min for amendment.

**Option C** — Apply CRIT-1 + HIGH-1/2/3 + ALL MED fixes at v1.7.3 (comprehensive single-pass amendment). Estimated 90-120 min.

**Option D** — Accept current state with documented exemption for CRIT-1 (NOT recommended — risks compile-time fail at Phase B Step B.3 coding-time). Skip recommended.

Per `feedback_plan_right_not_fast` + `feedback_motivated_collaborator_for_caramel`: Option C delivers highest planning-depth value AND directly addresses operator's framing of "third instance of meta-gap class" as warranting structural close not just patch. Option A is acceptable proportionate response. Option B is risk of micro-iteration spiral. Operator decides.

---

## Cross-references

- Plan body: `subplans/2026-05-24-v5.15.5.F.4d.1.B.4-train-serve-execution-layer-parity.md` v1.7.2
- Landed code: `CoreFrameworks/EngineCommon.hpp` (B.0/B.1/B.2 bodies at WIP-checkpoint commit `09204b3`)
- Lambda body extract source: `CoreFrameworks/EngineSharded.hpp:3044-3311` (per_core_slow lambda body) + `~2800-2820` (lambda preamble)
- Sister registry: `CoreFrameworks/SlowPathGateRegistry.hpp:69-243` (FOREACH_SLOW_PATH_GATE + AUTOPOPULATE macros — verified)
- DESIGN_SPECS catalog: `tick-trader-percore-workspace/DESIGN_SPECS/` (80+ files; 11 subdirs)
- Sister patterns applied: `shared-helper-extract-for-train-serve-mirror-close.md` v0.1 (Stage 2 → Stage 3 first canonical) + `orchestration-helper-with-pod-args-pattern.md` v1.0 (sister pattern; Option γ recommended adoption per CRIT-1 fix) + `slow-path-gate-registry-pattern.md` v1.0 (Stage 3 ACTIVE; D1-B applies) + `branchless-dispatch-discipline.md` v1.0 (H7+H20; D1-B applies branchless) + `canonical-sister-extension-discipline.md` (M1a sister parity verification applied at v1.7.1)
- Audit-methodology meta-disciplines: `train-serve-execution-layer-parity.md` (M5; first canonical) + `pattern-codification-lifecycle.md` (D3 Stage 6 promotion) + `structural-fix-preferred-decision-framework.md`
- Predecessor audits: /dod-audit v1.2 RE-AUDIT (GREEN) + /precoding-audit-gate v1.6 (YELLOW) + /blindspot-scan v1.6 (YELLOW) + /bug-check v1.6 (YELLOW) + /blindspot-scan v1.7 (YELLOW with CRIT) + /parity-check v1.7 (YELLOW) + /bug-check v1.7 (YELLOW) + /parity-check v1.7.1 (GREEN-WITH-3-MINOR-DRIFT)
- Operator memories: `feedback_iteration_spiral_signals_audit_meta_gap.md` + `feedback_verify_symbol_existence_at_plan_drafting_time.md` + `feedback_enumerate_consumers_before_registry_row_deletion.md` + `feedback_v1.6_bytewise_identical_math_preservation` (the v1.6 O2 codification) + `feedback_implementation_detail_blindspot_recovery_via_taxonomy.md` (M4)
- Audit-driven pre-coding gate spec: `audit-driven-pre-coding-gate.md` (parallel multi-skill pass at v1.6 + v1.7 cycles)

---

**End of /dod-audit v1.7.3 — synthesis returned for operator review per `consult-before-coding` discipline. Never auto-amend.**
