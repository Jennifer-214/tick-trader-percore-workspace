---
type: decision-log
plan: v5.15.5.F.4d.1.D.1 (doc-system end-goal-language sweep — META pre-.E gate)
version: v0.5 (as-shipped-mid-ship)
sprint: v5.15-live-readiness
established: 2026-05-28
status: mid-ship checkpoint (Phases A-B + sweep done; F-H remaining)
sister_docs:
  - subplans/2026-05-28-v5.15.5.F.4d.1.D.1-doc-system-end-goal-language-sweep.md (plan body)
  - handoffs/2026-05-28-v5.15.5.F.4d.1.D.1-doc-sweep-checkpoint-handoff.md
---

# v5.15.5.F.4d.1.D.1 — decision log

Per `feedback_session_decision_log_discipline` (heavily-amended session: plan v0.1→v0.5, 3 audit cycles, multiple operator-decided scope shifts). Decisions captured retroactively at session close; full context in the plan body execution notes + the handoff.

## Decisions (operator-decided)

<!-- D/C/F: DD-1 -->
**DD-1: Conceptual-only scope (option A).** `.D.1` sweeps only the CONCEPTUAL hyphenated `per-core`→`per-node` adjective in forward-looking timeless docs. Code symbols (`CoreContext`/`MAX_CORES`/`state.cores`/`FOREACH_PER_CORE_CFG_FIELD`/cfg-fields/underscore `per_core`) DEFER to `.E.1` (rename with the code, citations together). Caught by the loop-fusion-pattern.md proof: renaming `CoreContext`→`NodeContext` in prose cites a not-yet-existing symbol + creates half-renamed phrases ("cores[16] × NodeContext"). Hyphenated `per-core` is safe (can't be inside a C identifier → no substring mangling).
<!-- STATUS: landed -->

<!-- D/C/F: DD-2 -->
**DD-2: Terminology-evolution bridge, NOT history rewrite.** Forward-looking timeless docs (CLAUDE/DOCS/DESIGN_SPECS + active `.E` plan bodies) swept to per-node NOW; historical-record docs (postmortems/handoffs/changelogs/bug-catalog/shipped-plans) PRESERVED truthfully + bridged via DESIGN_PHILOSOPHY § 15 Glossary evolution-note. Rationale: rewriting history falsifies the evolution + breaks `.E.1`'s "rename Core→Node" narrative coherence. NEW memory `feedback_terminology_evolution_bridge_not_history_rewrite` + going-forward rule. Generalizes archived-changelog-preservation.
<!-- STATUS: landed -->

<!-- D/C/F: DD-3 -->
**DD-3: Bug-pattern CATALOG preserved (excluded from sweep).** `recurring-bug-patterns/` + RECURRING_BUG_PATTERNS.md = historical-record (class identities + worked examples tied to the per-core era; Class 26 IS "global consumer reading per-core field"). Reverted the inconsistent Class 25 index rename. Tool excludes the catalog. DOCS ledgers (PARITY/TECH_DEBT/HOT_PATH_CHANGELOG/FEATURE_LOOKUP/MANUAL_FIELDS_INVENTORY/KNOWN_ISSUES) similarly excluded (accumulating-historical / track current per-core CODE → update at `.E.1`).
<!-- STATUS: landed -->

<!-- D/C/F: DD-4 -->
**DD-4: Findings fold INTO `.E`, not a parallel fix-stream.** The separate audit's ~141 findings are `fix_ship`-routed; at `.E.0` (post-`.D.1`-ship) each `.E` plan ingests its sidecar (fix-DESIGN in the plan + `findings_sidecar:` backlink); fix-IMPLEMENTATION lands at each ship's coding time (`.E` rewrites those surfaces anyway). Runtime-confirm provisionals FIRST. PRE-`.E` characterization + CI safety net (the de-risk gate) = expand `.E.0`. This synthesis resolves the proceed-`.E`-vs-correctness-first tension (handoff §F): proceed gated behind the correctness net.
<!-- STATUS: pending (lands at .E.0; gated on .D.1 ship) -->

<!-- D/C/F: DD-5 -->
**DD-5: DECIDED (2026-05-28, fresh session) — Synthesis path (= DD-4): proceed `.E`, gated behind the pre-`.E` correctness net.** Operator confirmed the sequence: finish `.D.1` → codify audit findings + wire `findings_sidecar:` backlinks → audit `.E.0` + run the pre-`.E` correctness audits (runtime-confirm the 2 CRITICALs + fixedpoint cluster on a disposable clone → write PERSIST characterization tests + CI invariant checks → lay per-finding fix-designs in each `.E` plan body, as a `.E.0` expansion) → `.E` coding proceeds gated behind that net. Pure Path 1 (proceed-no-net) and pure Path 2 (correctness-first-then-decide) both rejected in favor of the non-exclusive synthesis: the correctness work IS the safety net that makes the `.E` rework safe (a reintroduced regression breaks the build instantly). "More usable" is the sober read; the net earns the `.E` commitment with evidence rather than fatigue.
<!-- STATUS: decided (synthesis = DD-4; .E gated behind .E.0 correctness-net expansion) -->

## Commitments (claude-said-will-do — carried in handoff/tasks)

- Class 36 logged (2 sub-shapes) + regression tests — DONE.
- 5 memories → 2 want DESIGN_SPECS + file-size-split amend (task #18).
- CI-tool quality bars (pattern-detector not denylist; extend-by-yield) at codification (task #16).
- PRE-PAPER-TEST mini-ship (task #19); 5 audit-skills eval (task #20); runtime-confirm CRITICALs (task #17); audit codification post-`.D.1` (task #16).

## Discoveries (this session)

- Hook drift: `.D`-era Check 11 in installed hook but not versioned source (closed `.D.1` A.3; canonical-source discipline).
- Class 36 sub-shape A (overlap) caught at apply-preview; sub-shape B (file-path rename broken links) caught at post-write verification → reverted + fixed.
- Scope gap: workspace `DOCS/` missed in first pass (not engine-symlinked) → closed.
- 2 accuracy corrections at close: glossary `ExecutionCore` "preserved" was not D-27-backed (→ PROPOSED/verify-at-`.E.1`); `conc-5` "structurally closes" was inference (→ LIKELY/verify).
- Phase F tool blind-spot — TRANSITION-DOC over-flagging (`.D.1` pickup 2026-05-28; PERVASIVE, not one-off): `check_doc_rename_classification.py --apply` flags `per-core`→`per-node` in docs that are *about* the rename, where `per-core` must stay. Confirmed in TWO surfaces this pickup: (1) `rename-candidates-running-list.md` (8 hits) — `Old name → Suggested new` catalog table; renaming corrupts pairs (`per-core → per-node` becomes `per-node → per-node`). (2) `.E.0`/`.E.1`/`E-MASTER-REFERENCE` plan bodies (ALL 16 flagged hits verified LEAVE) — Class 26 + TECH_DEBT-129 title citations (DD-3 preserved), `Core → Node` transition-descriptions, accurate current-state claims (`currently per-core sharded for hot path`), anti-pattern warnings (`don't reintroduce per-core iteration`), code-fence comments, file paths, surface-tag vocabulary. The `.E` plan bodies are bilingual-by-necessity (per-node = target narrative; per-core = citation/transition/current-state) → tool over-flags pervasively. DECISION: Phase F RENAME auto-apply SKIPPED for running-list + `.E` plans (would corrupt citations + falsify current-state); these docs already terminologically correct. Phase F additive deliverables (D-65/66, plan-template Tests-changed, Check 45, `.E.1` Tests-changed) PROCEED (not renames). ESCALATION (HIGH; Class 36 extension + tool): the prose-token doc-rename tool is NOT safe for `.E.1`'s 5,000-site CODE rename — needs code-aware (symbol/AST) detection + transition-catalog recognition (`type: running-list` / `Old name` columns / catalog-citation context). Flag for `.E.1` rename approach + `.E.0` findings ingestion.

## STATUS rollup

Mid-ship: A-B + timeless-doc sweep DONE + committed (engine `ce2173b` local; workspace pushed). Remaining: manual cases + F-H + ship close. Then audit codification (gated on `.D.1` ship). **DD-5 DECIDED 2026-05-28 next session = synthesis/DD-4** (proceed `.E` gated behind `.E.0` correctness-net expansion).
