# TOOLCHAIN CENSUS — in-code doc-system north star (§ 4 / § 5 / § 7.5 / § 8 / § 8.5 / § 8.6 / § 10) vs CODE at 2026-08-18

> Saved verbatim at receipt by the orchestrator (i-class agent report, 2026-08-18 north-star gap census, agent B of 3).

**Agent:** I-class (investigative), read-only census. **Method:** north-star doc read in full; every checklist item verified against workspace code (`/home/caramel/code/tick-trader-percore-workspace/tools/` + plugin + engine `.githooks`/`build.sh`), the D-414 finding register read in full with OPEN items spot-verified at HEAD, plus the E.1.2.B subplan, decision-log status header (D-97..D-419), MASTER banners, and the 2026-08-14 engine-resume handoff. **Headline:** the north star's § 8 status prose froze at ~2026-07-15/19; since then the entire E.1.2.B `0.2` contract+gate layer closed (D-418), the D-413/D-414/D-415 derived-facts-integrity arc closed all four leaves + AR-8, `0.3` plugin cutover shipped, `0.4`/`0.5` plugin surfaces landed through 2026-08-16 (RC-F card, shipped-asm card, doc-viewer, filterable trees), and the D-415 **park** deliberately re-inverted fact authority to script-side (frozen foxtag core, opt-in, re-arms at v1). The doc is stale in the *built* direction on ~10 items and accurate on the § 8.5/§ 8.6 substrate items (0.7/0.8), which genuinely remain.

**Governing state anchors:**
- Decision log status: "`0.2` COMPLETE at D-418 … `0.5` shipped-asm substrate at D-419" — `/home/caramel/code/tick-trader-percore-workspace/plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md:8`.
- D-414 register: all four leaves + AR-8 CLOSED — `/home/caramel/code/tick-trader-percore-workspace/plans/v5.15-live-readiness/plan_checks/2026-08-10-D414-toolchain-sweep-finding-register.md:115-133`.
- Seam state at engine resume: plugin arc "CLOSED at feature-complete 0.5"; sidecars auto-emit every build; TD-257 closed — `/home/caramel/code/tick-trader-percore-workspace/plans/v5.15-live-readiness/handoffs/2026-08-14-E.1.2-d305-tail-engine-resume-handoff.md:17-27,65-67`.
- The D-415 park (authority inversion, load-bearing for several verdicts): `/home/caramel/code/tick-trader-percore-workspace/tools/foxtag/README.md:138-149`.

---

## (1) VERDICT TABLE

| # | Checklist item | Verdict | Evidence |
|---|---|---|---|
| 1 | foxtag subcommand inventory | **BUILT** — live dispatch: `validate` `units` `unit` `tags` `grammar` `parity-dump` `layout` `codegen` `fields` `selftest` `codegen-selftest` | `tools/foxtag/foxtag_main.cpp:342-368`; `fields` → `cmd_fields` at `tools/foxtag/foxtag_layout.hpp:434`. **README's own § Commands omits `fields`** (`tools/foxtag/README.md:39-78` — minor doc staleness) |
| 2 | `foxtag update` (increment 2c, D-327 STRUCT writer) | **PARTIAL — function BUILT Python-side; core subcommand deliberately absent.** The STRUCT `[DERIVED]` writer is `check_cache_layout.py --fix` (`refresh_derived`, present-or-inserts the SIZE/ALIGN/CACHE_LINES/STRADDLE quartet, idempotent, tri-state since leaf 2) + `--isolate` per-header probes. Core-native `update` explicitly deferred until the Python gate retires (anti-Class-18) and further parked by D-415 | `tools/foxtag/README.md:153-157` (roadmap rationale); MASTER UPDATE 13 (commits `4b75a2f`/`9c88436`); register leaf-2 close `…finding-register.md:91-101` |
| 3 | `:FoxTagUpdate` whole-codebase autopopulate+fix | **BUILT under a different name + a deliberately SPLIT shape.** Whole-codebase batch = `tools/update_toolchain.py` (D-374, landed D-418): layout `--fix` → call-graph DECLARED-SKIP (M10) → cite-repair (D-417) → indexes → verify; explicit-invoke-ONLY, hooks forbidden. Interactive per-unit = plugin `:FoxSymdepsDerived` (preview) / `!` (writes the stable call-graph subset) | `tools/update_toolchain.py:1-25`; `DOCS/TOOLCHAIN_CONTRACTS.md:97-107` (family 5); `tools/plugins/fox-symdeps.nvim/lua/fox-symdeps/tagwriter.lua:1-8`; command registry `…/init.lua:296` |
| 4 | RC-B compile-DB story | **PARTIAL.** Landed: `tools/compile_command.py` = the ONE compile-command source (TD-257 substrate; tri-state; `--db`; envelope names the answering db; teeth wired `tools/check_session_docs.sh:192`); plugin probes read the SHIPPING db (plugin `9209258`, 2026-08-14); per-build 1:1 asm sidecars via objdump-from-binary (engine `/home/caramel/code/FoxML_Trader_v2/build.sh:23-25`, `emit_asm_for_dir` at `:108,150,165,177` — D-419, "the ONLY honest per-function asm under -flto"). Remaining: per-header db entries + a general header→enclosing-TU mapping — `compile_command.py:16-17` marks it "0.5 design work, NOT improvised here"; foxtag's header→TU pick still prefers `main.cpp` (`tools/foxtag/README.md:158-161`) | as cited |
| 5 | Generalized drift-gate (layout always + call-graph always + codegen pinned) | **PARTIAL.** LAYOUT: HARD strict-new on BOTH TUs (`tools/check_session_docs.sh:474-479`; shrink-ONLY baseline with growth-refusal + orphan report, AR-8 holes 8/9; `--isolate` in NO standing row, stated residual). CALL-GRAPH: A2 `--callgraph-check` ADVISORY, declared-PARTIAL M10 (`:485-488`; existence + co-occurrence; MISSING-consumer detection awaits the v1 generator) + HARD teeth (`:188`). CODEGEN: **not gated at tag level and nothing to gate** — zero written `[BUILD]_`/`[INSTANTIATION]_`/`[BRANCHES]_` in the engine corpus (grep = 0; `tagwriter.lua:5-7` volatile-never-written by design); codegen enforcement instead lives shipping-basis (Check N conformance budgets, `.githooks/pre-commit:303` + the D-419 sidecars). GRAMMAR/SCHEMA: full-tree validate + schema-version HARD (`:110-123`). **Cadence caveat:** the sweep fires at session-close + staged handoff/MASTER commits (pre-commit Check P, `.githooks/pre-commit:530,559-579`), not every code commit; Check T parity is PARKED (`:341`, `FOXTAG_REARM=1`) | as cited |
| 6 | RC-D′ per-function cache-line access-density | **BUILT** (plugin): `M.density(fields, touches)` computes DISTINCT 64B lines per FUNCTION from per-fn field-reference sets — no longer the struct-span fallback; HUD renders it (`t` overlay, "hot & slow") | `tools/plugins/fox-symdeps.nvim/lua/fox-symdeps/writers.lua:45-50,190,228`; `…/hud.lua:348,368,425` |
| 7 | RC-F (a) per-field slice + (b) register-fit axis — producer side | **BUILT, one-producer-clean (the linchpin HOLDS).** Chain: foxtag `fields` (C++ core producer; foxtag-only — the Lua emitter has no per-field mode) → `tools/check_register_fit.py` (D-366; imports `foxtag_client.fields`; ADVISORY-never-gate; `register_fit/1` envelope; teeth `check_session_docs.sh:207`) → plugin card `regfit.lua` `<leader>dR` (commit `c0ecbbe`) which **subprocesses the analyzer and only renders** — kind-asserted envelope decode, refusal-honest, zero plugin-local computation. H14 both-costs banner on every card | `tools/check_register_fit.py:18-20,34-38,55` ; `tools/plugins/fox-symdeps.nvim/lua/fox-symdeps/regfit.lua:1-9,72-90` |
| 8 | Scaffold generator | **MISSING** (confirmed under alternates). `tagadd.lua` = vocab-derived TAG-token picker/merger only (merge-only; an untagged unit is NAMED, never format-guessed — `tagadd.lua:1-6`); `tagwriter.lua` writes DERIVED into EXISTING blocks; `refresh_derived` inserts the quartet into an existing empty `[DERIVED]`. No tool emits a fresh `[STRUCT]…[END_STRUCT]` skeleton. Forward home: the "declarative code-gen" idea, `0.4` idea-stage (`subplans/2026-07-19-E.1.2.B-toolchain-v1-plugin-first.md:185`) | greps: `scaffold|skeleton|END_STRUCT-emit` = 0 producer hits |
| 9 | Complete per-type `[DERIVED]` set | **SPECIFIED; enforcement PARTIAL by design.** Schema carries the per-type axis-set as a DECLARATIVE TABLE ("never a parser branch") — `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md:77-84`; `foxtag grammar --json` emits the axes sets. Enforced: struct layout quartet (writer + strict-new gate + baseline pawl); registry COLUMN-count==tuple-arity (schema:235); call-graph via A2 (partial). Not enforced: function codegen axes (deliberately live-preview-only) and per-type COMPLETENESS (empty `[DERIVED]` shells are legal — e.g. the three Regime units named at register leaf-4) | as cited |
| 10a | § 4: annotated include blocks | **PARTIAL** — `[INCLUDES]` NAME-LIST axis exists with real usage (schema:84, real site `:698`); the per-include what-it-provides annotation is taxonomy gap 15/synthesis-13 and by the taxonomy's own words the name-list "loses the what-it-provides" (`DESIGN_SPECS/doc-disciplines/format-input-space-taxonomy.md:202,227`) | as cited |
| 10b | § 4: constexpr/const-rationale tag | **RESOLVED-BY-SURVEY as a non-shape** — zero taxonomy occurrences; absorbed by `[COMMENT]_[label]` / WHY-blocks; no dedicated tag was ratified | taxonomy full-grep = 0 |
| 10c | § 4: helper-declarations section | **RESOLVED** — `[REGION]` (cross-fn/beyond-function grouping) + generalized `[SECTION]` + the tier-2 member model | schema:150,209-221; taxonomy synthesis item 3 |
| 10d | § 4: file-header multi-section blocks | **RESOLVED** — labeled `[COMMENT]_[<LABEL>]` partitions, canonical LANDED (7 real partitions in `tests/schema_golden/golden_file_header.hpp`) | schema:244 |
| 10e | § 4: `[SWAR]` axis | **DECIDED, detector MISSING** — representation locked as the first canonical sub-tag `[BIT_PACKED]_[SWAR]` (D-fmt-5, direction "auto-detected from the idiom", schema:229; format slate locked 2026-07-06, taxonomy:229); no auto-detector exists in foxtag or any checker (grep = 0) | as cited |
| 10f | § 4: comment-shape survey / taxonomy PENDING | **COMPLETED** — `format-input-space-taxonomy.md` holds all 3 surveys + the unified 14-item synthesis + the 8 locked format decisions; the schema locked v1.0 against it (D-345/D-346, 2026-07-14). (The taxonomy's own frontmatter `status: accumulating (1 of 3)` is stale vs its body table — a micro-fix.) | taxonomy:20-24,208-230; schema:20 |
| 11 | § 8.5: FOREACH_PLANE / multi-syntax / self-hosting | **MISSING, all three** (deliberately sequenced `0.8`, last per D-372). FOREACH_PLANE: spec ~85% written (`DESIGN_SPECS/framework-patterns/doc-intelligence-toolchain-architecture.md:94`) but zero code artifact (grep `.py/.hpp/.lua` = 0). Multi-comment-syntax: zero `#`/`--` handling in `foxtag.hpp`. Self-hosting: zero `tools/*.py` or plugin `.lua` carries its own tag block (grep `^# [FILE]/[SCHEMA]` = none) | `subplans/…toolchain-v1-plugin-first.md:190` |
| 12a | § 8.6: pre-push heavier gate | **MISSING** — `.githooks/` has only `pre-commit`; `setup_hooks.sh` wires pre-commit only; zero "pre-push" hits tree-wide. Scoped `0.7` (with the PRODUCER/GATE `[TAG]` role). Sister: TECH_DEBT-252 (plugin repo runs no hooks) | `/home/caramel/code/FoxML_Trader_v2/.githooks/` listing; `tools/setup_hooks.sh:1-40`; subplan:189 |
| 12b | § 8.6: import-from-core lint | **BUILT** — `tools/check_import_from_core.py` (new roller = red; grandfathered shrink-list `tools/lib/import_from_core_baseline.txt`, currently 11 lines) + HARD teeth (`check_session_docs.sh:138`) | tool docstring `:1-20` |
| 12c | § 8.6: the check_meta_registry roll-your-own-root anti-pattern | **FIXED** — it now imports `ENGINE` from `check_doc_metadata` + `WORKSPACE` from `foxroots` | `tools/check_meta_registry.py:43-44` |
| 12d | § 8.6: producer-vs-gate surface split (was "operator UNSURE") | **DECIDED** — D-375: producers→plugin+CLI, gates→CI; `tools/core/` rename DROPPED for `foxroots.py` (MASTER UPDATE 16). The structural `PRODUCER`/`GATE` `[TAG]` role remains `0.7`-pending | MASTER:53; subplan:189 |
| 13a | § 10: D-SWAR | **DECIDED** (see 10e) | schema:229 |
| 13b | § 10: complete `[DERIVED]` set per type | **DECIDED/SPECIFIED** (see 9) | schema:77-84 |
| 13c | § 10: `:FoxTagUpdate` naming + "one command, both interactive + CI (likely yes)" | **DECIDED — and the "likely yes" was overturned:** write-vs-verify separation (D-374/D-418) split it into `update_toolchain.py` (batch, human-invoked, hook-wiring is a contract violation) + `:FoxSymdepsDerived` (interactive). No dual-mode command exists | `DOCS/TOOLCHAIN_CONTRACTS.md:14-17,103-107` |
| 13d | § 10: doc home (doc-disciplines/ vs tag-system/) | **UNDECIDED-BUT-MOOT** — no explicit decision found (zero `tag-system/` dir hits); resolved de facto: schema/north-star/taxonomy stay under `DESIGN_SPECS/doc-disciplines/`, the architecture spec under `framework-patterns/`, contracts at `DOCS/TOOLCHAIN_CONTRACTS.md` | greps |

**D-414 register open-vs-closed (checklist source 3):** the register is fully terminal — every finding carries CLOSED-with-commit / EXEMPTED / GRANDFATHERED-with-home / MOOT-BY-PARK / HOMED (`…finding-register.md:121`), and the AR-8 holes 7–10 all closed same-commit (`:127-130`). Spot-verified at HEAD: leaf-4's `[CONSUMERS]` corrections are live in source (`/home/caramel/code/FoxML_Trader_v2/Strategies/StrategyInterface.hpp:165`, `RegimeDetector.hpp:15` region — phantom gone, verified sets present). Residuals that survive the arc, correctly homed: **MOOT-BY-PARK** set (C++ detector partial-state, parity § 4/§ 5 verdict-vars, foxtag selftest standing wiring) → the v1 re-arm contract; **HOMED** set (TUISharedState ×3 + NodeContext.regime_state → E.1.2; TrainingPanelState ×9 → TD-269; torn-cfg publication → E.1.3-candidate; BuddyAllocatorState untraced); **genuinely still open small:** F9's shared M-of-N skip-warn helper never landed (`tools/cascade.py:171-172` still bare-`continue`s on read failure — A4 called it gold-plating-tier) and the `[ROW_COUNT]_[<tool: 4 …>]` hybrid remains (now angle-form, A2-skipped by design — `StrategyInterface.hpp:163`).

---

## (2) GENUINELY-MISSING, ranked (effort/value one-liners)

1. **The v1 foxtag call-graph GENERATOR** (missing-consumer detection; A2 stays declared-PARTIAL without it) — medium-large effort; the single biggest fact-integrity hole; already the named v1 re-arm contract item (contracts § 3).
2. **Pre-push heavier gate (`0.7`)** — small effort (hook + row selection over the existing sweep); closes the real cadence gap that ordinary code commits never run the layout/call-graph gates (Check P fires only on handoff/MASTER).
3. **`0.8` substrate: FOREACH_PLANE registry + multi-comment-syntax parser + toolchain self-hosting** — large effort but spec ~85% pre-written; unlocks plane-scoped vocab/gating and tags on the toolchain's own `.py`/`.lua`.
4. **RC-B residual: per-header compile-db entries + general header→enclosing-TU mapping** — medium; explicitly parked as design work (`compile_command.py:16-17`); the shipping-db + sidecar path covers the asm use-case meanwhile.
5. **Registry `[ROW]`/`[COLUMN]` card + row-browser** — medium; § 6 calls the registry card "the biggest format gap"; grammar landed (D-339), zero render exists (plugin grep = 0).
6. **Scaffold/skeleton generator** (fresh unit-block emission; the declarative code-gen idea) — medium; authoring friction only, conversion is done so value is forward-looking.
7. **`[SWAR]` auto-detector** (the RC-C source-level half) — small-medium; representation decided, nothing detects the idiom.
8. **AI-plane surface + AST fact-producer migration (`0.6`, TD-256)** — medium-large; regex parsers measured at ~34% FP on function bodies; depends on RC-B item 4.
9. **`[STRADDLE_EXEMPT]` field-existence validation** — small once the emitter carries full field lists; recorded v1 contract item (contracts § 1 tail).
10. **Standing `--isolate` gate row** — small code, real per-commit clang cost; deliberately not taken (AR-8 hole 9 residual, stated at every claim site).
11. **F9 shared M-of-N skip-warn helper** (`cascade.py` first) — trivial; gold-plating tier per the A-class verdict, but it is the register's only unclosed code item.
12. **`1.0.0` cohesion close**: parity re-arm + per-surface acceptance batteries + `TOOLCHAIN_VERSION` bump (currently `0.1.0`) — the D-415 park's exit condition; effort = verification, not construction.

Not-missing-by-decision (do not count as gaps): dual-panel compare (deferred `1.x`), custom LSP (`1.x`), clang-tidy/sanitizers-in-CI (engine plane, own ship), codegen written-axis gating (dormant until anyone writes a `[BUILD]`-pinned fact — the shipping-basis Check N + D-419 sidecars are the live codegen truth path).

---

## (3) STALE-IN-DOC — north star says missing/broken; code says built

| North-star claim | Reality at HEAD | Proof |
|---|---|---|
| § 7.5 `:FoxTagUpdate` ⬜ | Built as `update_toolchain.py` (D-418) + `:FoxSymdepsDerived!`; the one-command dual-mode shape was overturned by D-374's write-vs-verify law | `tools/update_toolchain.py:1-25` |
| § 7.5 derived-write 🟡 "no struct size, partial DERIVED" | Struct quartet writer complete: present-or-inserts, `--isolate`, tri-state, refuses `none`-on-partial; corpus re-ground (27 fabricated `none` flipped) | register leaf-2 `:91-101`; MASTER UPDATE 13 |
| § 7.5 "complete schema ⬜ (blocked on the 7 decisions)" | Schema LOCKED `[SCHEMA]_[v1.0]` 2026-07-14 (D-346); the format slate RESOLVED 2026-07-06; all 3 surveys + synthesis landed | schema:20; taxonomy:208-230 |
| § 4 "PENDING the comment-shape survey" | Survey completed + synthesized; § 4's gap list is answered item-by-item (see verdicts 10a-10f) | taxonomy:20-24 |
| § 5 RC-A/RC-C/RC-E | All three built INTO `foxtag codegen` (instantiation anchor / real width-class incl. scalar-xmm / vacuity floor rc-2) — D-351 | `tools/foxtag/README.md:19,103-107` |
| § 5 RC-D (hardcoded clang; no struct size) | `foxtag layout` clang record-layout producer (D-350) + codegen g++-only (D-321); sizes flow via the cache-gate | README:18,97-102 |
| § 5 RC-D′ struct-span fallback | Real per-function distinct-64B-line density computed | `writers.lua:45-50,190,228` |
| § 5 RC-F "operator-envisioned" | Fully built end-to-end: `foxtag fields` → `check_register_fit.py` (`register_fit/1`) → `<leader>dR` card; one producer, render-only plugin | verdict 7 citations |
| § 7.5 NAVIGATE "tag-enriched + filterable trees ⬜ · doc-viewer ⬜ · tag-query ⬜" | Trees + browse-by-tag landed (plugin `46f4dbb`, `5330636`); doc-viewer landed (`docview.lua:3-5` via `citable_ids.py --where/--resolve`, tri-state); tag-query partial (`foxtag units --type/--tag/--name`; no fact-threshold query) | as cited |
| § 7.5 VERIFY "generalized drift-gate ⬜ · code-tag index ⬜" | Layout HARD ×2 TUs + A2 call-graph + grammar/schema-version HARD + reciprocal-supersession HARD + contract-stale advisory all standing (`check_session_docs.sh:110-123,474-501`); code-tag index built + index-currency HARD (`:102`) | as cited |
| § 8 "Remaining increment 2c = `foxtag update` + RC-B + the generalized drift-gate" | Materially superseded: writer landed Python-side, RC-B substrate landed (compile_command + shipping db + sidecars), drift-gate landed for layout+call-graph+grammar; what actually remains is the v1 generator, per-header db, and the parked core-native writer | verdicts 2/4/5 |
| § 3 "ONE shared fact-producer" (as a statement of current authority) | Architecturally true, but D-415 **inverted runtime authority** for layout/inventory back to script-side (Lua emitter / Python collector authoritative; frozen foxtag = parity-gated opt-in via `--backend foxtag`/`FOXTAG_INVENTORY=1`); `fields`/`codegen` remain foxtag-native. One core in design, two-lane in authority until the v1 re-arm | `tools/foxtag/README.md:138-149`; register A8 (`:81`) |
| § 6/§ 7.5 "overlays half-broken / branchtag false-green" | Vacuity floor in the core (RC-E) + explorer honesty relabel + budget/SIMD chips + shipped-asm live-follow landed across the 2026-08-13/14 plugin arc | plugin log `be03cbc`..`77ab06e` |

Accurate-as-written (not stale): scaffold generator 🟡-pending; dual-panel ⬜; graph-browser re-root/history ⬜ (not evidenced under any module name I checked); § 8.5 all-three-missing; § 8.6 pre-push missing.

---

## (4) SPOTS MOST WORTH AN ADVERSARIAL REFUTE

1. **The sweep-cadence claim (verdict 5 caveat).** I concluded the layout/call-graph gates run only at handoff/MASTER commits + session close, from `.githooks/pre-commit:530,559` alone. Refute by checking whether `/close-session`, `/ship`, or `build.sh test` invoke `check_session_docs.sh` or `check_cache_layout.py` on other paths — if they do, the pre-push gap (missing item 2) is smaller than ranked.
2. **RC-D′ "BUILT".** I verified the computation (`writers.lua:50`) and the HUD help text but did not execute the overlay; the north-star bug was possibly a RENDER fallback — a second render path in `facts.lua`/`layout.lua` could still show the struct span. Run the `t` overlay on a multi-function struct to confirm.
3. **D-345 "all 14 shapes covered" is a schema self-claim.** I verified 5 of the § 4 shapes individually; the include-annotation answer (10a PARTIAL) is the weakest — an a-class should walk the 14-item synthesis list against grammar tokens one-by-one; any other uncovered shape flips a RESOLVED to PARTIAL.
4. **"Graph-browser missing"** is my weakest absence: `browse.lua` is just a struct picker, but I did not read `neotree.lua`, `trace.lua`, `cockpit.lua`, `dashboard.lua` — a re-root/history browser could live there under another name.
5. **0.4-vs-0.5 increment attribution.** I attributed plugin commits to increments by message prefixes; the superseded 2026-08-10 handoff (ten boundaries) is the authoritative sequence — if 0.4 is only partially closed, the "not-missing" set shifts.
6. **The register's terminal-disposition claim.** I spot-verified two OPEN residuals (F9 in `cascade.py`, the ROW_COUNT hybrid) and the leaf-4 source corrections, but not the F9 cohort's other ~8 sites nor the provenance smalls (`[ORIGIN]_[AUTO]` over hand lines, `[UPDATED]` restamps) — a sweep of those would either confirm "closed at leaf 4's curation commit" or add a small open item.
7. **`foxtag fields` semantics.** Verdict 7 rests on docstrings + the dispatch site; I did not read `cmd_fields`' body (`foxtag_layout.hpp:434`) — confirm it emits ALL fields (not just straddlers) and that the D-415 park does not silently disable it for `check_register_fit.py` in a foxtag-less checkout (the tool "gates on `core_available()`" — meaning RC-F is foxtag-REQUIRING; a fresh clone without the built binary loses the axis: worth stating to the operator).

**End of census.** Read-only throughout; no files written or edited.
