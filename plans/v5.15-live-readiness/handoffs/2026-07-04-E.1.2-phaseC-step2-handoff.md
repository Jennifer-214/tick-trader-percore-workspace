---
status: active   # THE one live handoff (no-arg /accept-handoff resolves here). Supersedes 2026-07-01-E.1.2-reformalize-handoff.md (its reformalize + precoding-gate work landed + was superseded by the Position close-out + Phase-C Step 1 this session).
sprint: v5.15-live-readiness
ship: v5.15.5.F.4d.1.E.1.2
engine_head: 84d73e0
workspace_head: e145c92
predecessor_tag: v5.15.5.F.4d.1.E.1.1 (0ee227a)
audit_tier: HIGH-RISK
decision_log: decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
pickup: /accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-04-E.1.2-phaseC-step2-handoff.md
---

# E.1.2 Phase C — Step 2 (the persist view) pickup handoff (2026-07-04)

**State:** E.1.2's Position close-out + the NodeCtx-registry unify (Step 1) SHIPPED + PUSHED. NEXT = Phase C **Step 2** (the byte-identical persist view), fully designed. Build 2a→2d, then Steps 3-5, gated by the mandatory close-gate.

## What landed at this session close (PREDECESSOR CONTEXT — verify at pickup)
- **Position close-out (engine `2c11922`, pushed):** `Sharded_SlotNode(slot, partial_on)` — branchless inverse of `Sharded_LegSlot` (`ControllerEventLoop.hpp` ~:1132) — routed at all 6 open-coded engine slot→node sites (fixes the `:856` ungated `slot>>1` warm-restart misattribution). `_pad_pos[7]={0}` DMI + `Position_Reset`→`*p=Position<F>{}` (Option B; the H12 hole). `tools/check_slot_node_derive.py` + pre-commit **Check O** + `test_check_slot_node_derive.py`. Position stays 128B (derive owner, don't store). D-294/D-295/D-296.
- **Phase-C Step 1 (engine `84d73e0`, pushed):** NodeCtx registry unify — `FOREACH_NODE_CTX_INIT_FIELD` + `RESET_FIELD` → ONE `FOREACH_NODE_CTX_FIELD` with a `RESET` column (RST=paper-reset subset; NORST=boot-only). Build **3693/0**; `static_assert(NODE_CTX_RESET_FIELD_COUNT==15)` pins reset byte-identical; MetaRegistry 2 rows→1; 5 comment refs. D-297/D2.
- **Tooling (workspace `487ae87`, pushed):** `cascade.py registry <FOREACH_NAME>` (D-298 — footprint UPFRONT: DEFINITION / H15-ENROLLMENT / EXPANDER / REFERENCE + compiler-blind flag) + its cohort (TOOLS.md, DESIGN_SPEC v1.3 § registry-mode, `feedback_run_cascade_upfront` memory).

## NEXT ACTION — Phase C Step 2 (persist view), byte-identical-first
The wire format is a HISTORICAL artifact — reproduce the v10 hand-loop EXACTLY (zero change) FIRST, prove it with a **frozen golden**, THEN apply the v11 delta. Full 37-row wire map + all 5 findings: `plan_checks/2026-07-04-E.1.2-phaseC-serializer-sweep-synthesis.md` (**READ FIRST**).

- **2a — `regime` delegate** (`Strategies/RegimeDetector.hpp`, near `RegimeState<F>` :460-475): MIRROR the `confidence` template (`ML_Headers/ConfidenceScore.hpp:993-1033`) — `FOREACH_REGIME_PERSIST_FIELD(X)` (7 fields IN WIRE ORDER: current_regime/proposed_regime/hysteresis_count/hysteresis_threshold/last_strategy_id [int×5], regime_start_tick [uint64_t], regime_start_time [time_t]) + `RegimeState_FieldwiseWrite/Read/CommitPersistedFields` (templated `<F>`) + `static_assert(..._COUNT==7)`.
- **2b — `feeder` delegate** (`ML_Headers/LinearRegression3X.hpp`, near `RegressionFeederX<F>` :44-48): `FOREACH_FEEDER_PERSIST_FIELD(X)` (`price_samples` [**FPN_Binary<F>** not Money — byte-identical 16B but type-honest, R1], MAX_WINDOW=8; head; count) + templated walkers + count-lock ==3.
- **2c — ordered `FOREACH_NODE_PERSIST_FIELD`** (new `MemHeaders/NodePersistFieldRegistry.hpp`): 37 rows in EXACT wire order (synthesis §B): 23 SCALAR + 1 BIT (node_kill_tripped / MASK_NODE_STATE_KILL_TRIPPED) + 2 PAD (1B after strategy_state_kind, 3B after kill) + 3 DELEGATE (regime/feeder/confidence). **THE INTERLEAVE** — the 3 prediction doubles (staged/active/last_confidence) sit BETWEEN the feeder and confidence delegates; the ordered registry captures it by construction (a flat-then-delegates walk REORDERS them = silent D-110). Tuple `X(NAME,TYPE,KIND,ARG)`, KIND∈{SCALAR,BIT,PAD,DELEGATE}; SAVE/LOAD/COMMIT via presence-dispatch (SCALAR fwrite sizeof; BIT extract+1B; PAD N zeros; DELEGATE sub-walker). H15: new registry needs a `MetaRegistry.hpp` row.
- **2d — wire in + golden:** replace the hand-loop (`ShardedSnapshotPersist.hpp:172-261` save / :436-499 load). Prove BYTE-IDENTICAL to v10 — a FROZEN committed golden byte-file + a Save→Load→compare with a full-poison **distinct-per-field** fixture (NOT byte-compare-alone; it's blind to a symmetric drop — A2).

## REMAINING ITEMS — every open item + its home (EXTRA-THOROUGH per operator directive)
| Item | Home | Status |
|---|---|---|
| Phase C Step 2 (persist view 2a-2d) | this handoff · task #15 · the synthesis | **NEXT** |
| Step 3 — persist-layout HASH (D1) + per-delegate count-locks + persist⊆init cross-check | D-297/D1 · synthesis §finding-1 | pending |
| Step 4 — v11 delta (drop node_dd_pct + strategy_id + strategy_state_kind; add partner_pending_pnl + drift-kill FLAG only; price_samples→FPN; regime_start_time→int64) + SHARDED 10→11 + version/retire cascade (PORTFOLIO retire + CONTROLLER delete + `identifier_ledger.txt` + `check_identifier_retirement.py` LOCKSTEP + 3 test sites `controller_test.cpp:11394/26749-57/26819-20`) | D-294/D-297 · synthesis §version-cascade | pending |
| Step 5 — D-289 deletions (Portfolio_Save/Load, PortfolioController_*Snapshot) + F-096 (`Async.hpp:842` double→Money) + AM-4 mid-pair fixture (bit↔partner_pending consistency; snapshot between 2nd-leg-exit and partner_pending clear `:1602`) | D-289 · synthesis | pending |
| MANDATORY close-gate | D-297 | pending — `calls_graph_diff.sh` CLEAN + `check_latency_path_conformance.py` clean + 3693-suite + the D2 init/reset-identical test. Phase C does NOT close until ALL pass |
| Ship-close codification | task #13 | pending — (a) NEW RECURRING_BUG_PATTERNS class (byte-serialized hand-struct + parallel serializer → silent drop); (b) strengthen `check_struct_alignment.py(c)` to flag un-zero-init pad (the `_pad_pos` guard-gap, doc'd in struct-padding-determinism § guard-gap); (c) the 4 DESIGN_SPEC updates (persisted-struct-coexistence · wire-byte-preservation · registry-coverage · multi-bit-state — several partly done) |
| TD-229 — DriftHistory 256-sample ring → streaming-EWMA at scale | `DOCS/tech-debt/open.md` | open (scale-forward; nothing at 16 nodes) |
| TD-227 — partner_pending_pnl not persisted | `DOCS/tech-debt/open.md` | open → **CLOSES at Step 4** (AM-4 persists it) |
| FEATURE_LOOKUP entries | `FEATURE_LOOKUP.md` | owed at ship-close (persist-registry + SHARDED bump = operator-visible: warm-restart fidelity + snapshot version) |

## Capture-completeness (verified this close)
Nothing lost — every session artifact reached a durable home:
- **Decisions D-291–D-298** → `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (all 8 sentineled; independent Stage-5.5 review confirmed each exists as a real entry).
- **The 5 sweep findings + the 37-row wire map** → `plan_checks/2026-07-04-E.1.2-phaseC-serializer-sweep-synthesis.md`.
- **Memories** → `feedback_resource_use_gated_on_existence_not_felt_need` + `feedback_run_cascade_upfront_for_rename_and_registry` (both indexed, MEMORY_EXTENDED.md) + `project_remote_push_needs_operator_password` **REFINED** (the `dangerouslyDisableSandbox` push mechanism the operator taught this session).
- **Tools** → `cascade.py registry` + `check_slot_node_derive.py` (both in `DOCS/TOOLS.md`; the latter is pre-commit Check O).
- **Ledgers** → TD-229 (DriftHistory) opened in `DOCS/tech-debt/open.md`; TD-227 (partner_pending) closes at Step 4.
- **Sprint SSoT** → MASTER **s14** banner landed. *(The "are you sure we captured everything" probe caught its initial omission + the push-memory refinement + a self-skipped Stage-4.5 harvest — an AR-8 6th-consecutive close-completeness recurrence, recorded in `meta-anti-pattern-index.md`; `check_close_completeness.py` is the overdue M7 structural fix.)*
- **Ship-close still-owed** (HOMED, not dropped) → task #13 codification + the FEATURE_LOOKUP entries (both in the REMAINING ITEMS table above).

## TaskList state at handoff write
Serialized so pending tasks survive `/accept-handoff` pickup (per `/handoff` Stage 1.5) — the E.1.2 work AND the parked non-E.1.2 items (all also homed in MASTER's s9–s11 banners):
| # | Task | Status |
|---|---|---|
| 15 | Code E.1.2 Phase C+ — Step 2 (persist view 2a–2d) → Steps 3-5 → the close-gate | **in_progress** (Step 1 shipped) |
| 13 | Codify serialized-struct-drift class + CI guard + NodeState registry-generation | pending (ship-close) |
| 9 | #11 boot-surface GUI config-error warn-banner (the doable half; global-refuse BLOCKED on TD-223) | pending |
| 10 | #14 OQ1 — wire the inert sharded kill/exposure controls (own capital ship, D-273) | pending |
| 11 | #8 fox-symdeps.nvim / .clang-format coexistence fix | pending |

## Critical pickup-time reads
- `plan_checks/2026-07-04-E.1.2-phaseC-serializer-sweep-synthesis.md` — the 37-row wire map (§B) + revised shape + all 5 findings + the version cascade. **THE Step-2 spec.**
- `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` D-291..D-298 (D-297 = the decision set D1-D5).
- `subplans/2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md` — the ⚠ SUPERSEDED-BODY banner (executable truth = the amendments + D-291..298, NOT the pre-reversal body).
- `ML_Headers/ConfidenceScore.hpp:993-1033` (delegate template) + `CoreFrameworks/ShardedSnapshotPersist.hpp:172-261`/:436-499 (the v10 hand-loop to reproduce) + `MemHeaders/NodeCtxInitRegistry.hpp` (the Step-1 unified registry; the persist⊆init cross-check reads it).

## Surface kit for Step 2
- **RUN:** `cascade.py registry FOREACH_NODE_PERSIST_FIELD` (once created — its blast-radius); the close-gate quartet.
- **Avoid:** Class-18 mirror (single-source the wire) · flat-then-delegates REORDER (D-110) · symmetric-drop-blind gate (A2) · naive-auto-commit v5.11.15 leak (moot — D4 drops both strategy fields).
- **Invariants:** H9 (wire byte preservation) · H12 (pad-zero) · H21 (SHARDED append-only + the bump) · H15 (new registry → MetaRegistry row).

## ⏩ Post-write addendum (2026-07-04 close)
Workspace advanced `487ae87 → e145c92` AFTER this handoff was written — all **close-out apparatus, NOT E.1.2 code**: the commit-time doc-gate (**D-299** wire the workspace pre-commit hook + Check P · **D-300** consolidate to ONE canonical union hook · **D-301** `tools/setup_hooks.sh` durability + close-session Stage 7.5) + **TECH_DEBT-204** closed. Engine `84d73e0` is unchanged; the Step-2 pickup above is current. Net effect for the next session: the doc floor is now enforced at COMMIT time (a broken handoff / stale MASTER can't commit). **On a fresh clone, run `tools/setup_hooks.sh` once** to wire the gate in both repos.
