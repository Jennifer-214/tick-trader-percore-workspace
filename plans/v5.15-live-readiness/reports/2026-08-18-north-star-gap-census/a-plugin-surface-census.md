# PLUGIN-SURFACE CENSUS — in-code doc-system north star (§ 6 + § 7.5) vs fox-symdeps.nvim at HEAD `77ab06e`

> Saved verbatim at receipt by the orchestrator (i-class agent report, 2026-08-18 north-star gap census, agent A of 3).

**Agent:** I-class (investigative, read-only census). **Date:** 2026-08-18.
**North-star doc:** `/home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md` (§ 6 lines 122-184, § 7.5 lines 196-224; its marks date from ~2026-07-19).
**Plugin:** `/home/caramel/code/tick-trader-percore-workspace/tools/plugins/fox-symdeps.nvim/` — hereafter `<P>`. Own git repo; HEAD `77ab06e` (2026-08-16). The heavy cycle the doc's marks predate: **2026-08-09..08-16** (0.4 arc `0b08c1e..46f4dbb`, parity fleet `30f48f7..f657dbb`, 0.5 arc `be03cbc..77ab06e`).
**Cross-source:** fleet register `/home/caramel/code/FoxML_Trader_v2/plans/v5.15-live-readiness/plan_checks/2026-08-10-plugin-parity-fleet-register.md` (LIVE, "REGISTER CLOSED — ZERO tail").
**Coverage boundary:** full reads of `<P>/lua/fox-symdeps/{init,actions,menu,tagcursor,tagcontext,panel,followcard,ui,facts,tagwriter,unitindex,browse,docview(1-110),status,ambient,diagnostics}.lua`, targeted reads of `hud.lua` (keys/render/filter/help), `asmshipped.lua` (header/chips), `regfit.lua`, `dashboard.lua` (tiles), `cockpit.lua`; greps across all 55 modules for MISSING confirmations (SECTION/CONTAINS, DEFERRED, SEAM, timeline, orphan, REGISTRY-facet, breadcrumb/history); `<P>/DOCS/{ROADMAP,DECISIONS}.md`; `git log -60`. Not line-audited: asmexplorer/asmview/asmdiff(partial)/writers/compose/clangd/layout/recordlayout/sizeprobe/nodemodel internals — none carried a checklist item that the greps + registry rows didn't answer.

---

## 1. VERDICT TABLE

| # | Checklist item (§ 6 / § 7.5) | Verdict | Evidence |
|---|---|---|---|
| 1 | **Cursor-tracking by enclosing unit** (outer↔`[END_X]`; HUD/panel/derived work anywhere inside a unit) | **BUILT** | `<P>/lua/fox-symdeps/tagcursor.lua` (whole module: debounced follow, per-buffer changedtick cache, `vim.b.fox_unit` + `User FoxUnitChanged`, `:FoxUnit`); `tagcontext.lua:32-56` (`enclosing_block`, scan-up with light-unit skip + angle-form names per `9bd417c`), `:82-97` (`resolve_unit`, unit-first), `:102-117` (`resolve`). Wired into HUD entry `init.lua:121-133`, board `panel.lua:35`, follow `followcard.lua:35`, `:FoxSymdepsDerived` `init.lua:297`, menu `init.lua:329`. Commits `0b08c1e` + `fb917c9` (2026-08-09). |
| 2 | **HUD vs PANEL two-roles**: transient auto-follow vs persistent multi-card EXPLICIT-add accumulate board; l/Enter jump; L open-beside | **BUILT (roles), refined to 3 surfaces; the two per-entry keys PARTIAL** | Board: `panel.lua:1-8` header states the § 6 role verbatim; `add()` `:141-186` (explicit `<leader>dD`, opens-if-closed, dedupe re-select, CARD_CAP 8, never closes-on-reuse — `toggle` aliased to `add` `:190`); H/L flip, x drop, s compare `:171-175`. Auto-follow: `followcard.lua` (whole; `FoxUnitChanged`-driven, `<leader>df`); cockpit docks the FOLLOW card, not the board (`cockpit.lua:18-24`). Commit `699d1c2` ("the § 6 ROLE SWAP"). In-card `l`/`<CR>` jump: `hud.lua:280-282`. **Remains:** § 6's "**L** = open the [tree-entry's] unit in the panel beside you" — no such per-entry key (`hud.lua:274-302` map list has no `L`; board `L` = next-tab), and no per-entry "compare with →" (see row 7). |
| 3 | **Collapsed-by-default trees/impact views** | **BUILT** | `hud.lua:545-552` (`_sec_collapsed` default TRUE — "clean overview by default: every section folded", persists per-section across re-inspects); file nodes fold at >5 entries (`init.lua:35`); dashboard tiles default `collapsed = true` (`dashboard.lua:51-58`) + per-group folds default closed (`20d204e`, `6c0125f`). |
| 4 | **Context-gated actions** (menu shows only valid per unit type + resolvability; D-328) | **BUILT** | `<P>/lua/fox-symdeps/actions.lua:29-125` registry (`all`/`types`/`when(ctx)` rows), `:176-196` `for_type` w/ explicit-ctx gates + unknown-type validation `:159-174`; tag-TYPE resolved from the SOURCE block for both invokers (`9e92f25`, fleet P2/P3 closed); template resolvability handled gracefully (`hud.lua:434-441` names the `template_args` knob; `1b2962a`, `0b3fa8d`). Menu shows write-tier icons `menu.lua:9-38` (`cd971ac`). |
| 5 | **Persistent output / popout pane + pop-out doc-viewer** | **BUILT** | Output log: `ui.lua:41-80` — every `notify_raw` lands in a 200-entry ring; `<leader>dn` / menu row opens newest-first pane (`62382e5`). Doc-viewer: `docview.lua` — `[REFERENCE]` subcat-routed via `citable_ids.py --where`/`--resolve`, tri-state honest (FOUND float / named ERROR / named MISSING refusal, never blank), `p` = PIN to persistent rightmost split (`docview.lua:100-110`), FILE-header fallback; commits `e380a5a`, `c15a479`, `e116ae2`, `f3bf273`. |
| 6 | **Facts in the HUD** (branchless verdict + derived facts, no false-green) | **BUILT** | Layout facts + honesty states `hud.lua:427-462` (incl. `probe_error` surfaced `:442-444` — L1 closed); per-function branch verdicts branchless/branches/data `branchtag.lua:55-74`; hot-path budget lens reads the blessed sidecar `lenses/hotpath.lua:1-9`; SHIPPED-truth card w/ basis-honest BUDGET+SIMD chips `asmshipped.lua:271-276,750-778` (`ce60547`) + branch-class marks + honest INLINED-AWAY/STALE/NO-SIDECAR states `asmshipped.lua:6-13` (`be03cbc`, `5c3a092`); derived-facts preview = menu row `actions.lua:45-46`; curated `[REFERENCE]` Docs section in the HUD (`hud.lua:371`, docs_refs lens). False-green roots fixed: exact qualified-name asm match `asmdiff.lua:37-50,211`; trust discipline codified `<P>/DOCS/DECISIONS.md:58-64`. Caveat: the unit's written `[DERIVED]`/`[TAG]` lines are not themselves rendered as a HUD section (facts arrive from live probes; preview via `:FoxSymdepsDerived`). |
| 7 | **Tag-enriched + tag-filterable trees** + "compare with →" hook per entry | **BUILT except the per-entry compare hook** | Enrichment (R3): every tree entry resolves to its enclosing tagged unit + `[TAG]` list via the disk-side `unitindex.lua:46-69`, rendered `hud.lua:585-595` (`1b95f7a`). Filter: `/` haystack includes unit name + tags `hud.lua:514-521`; `T` = tag PICKER listing only tags present in the tree `hud.lua:474-503` (`46f4dbb`, `5330636`); corpus-wide browse-by-`[TAG]` `browse.lua:77-129`. **Missing half:** no "compare with →" hook on entries — compare only pairs the two most-recent board cards (`panel.lua:92-95`). |
| 8 | **Responsive layout / fill-available-space + dual-panel COMPARE w/ connective tissue** | **PARTIAL** | Placement policy SSoT `ui.lua:1-39` (aspect-driven right-strip/bottom-strip, card clamp, reading-pane, pin width — fleet S1-S4/S9 closed); COMPARE shipped v1 `panel.lua:60-119` (`28fc247`): companion strip = previously-selected card, static, width-gated (`ui.lua:7-8`). **Missing:** the § 6 connective tissue (shared call-graph edges, cross-card blast-radius, false-sharing between the pair) — companion is an independent second card; and the N-card (>2) generalization. Note "fill available space" was REDEFINED by operator call 2026-08-10 (fleet answer #3): readability caps stay, ultrawide surplus = MORE PANES. |
| 9 | **Nav-cluster subsumption** (harpoon / aerial / dropbar / treesitter-context) | **PARTIAL** | Harpoon role (mark/jump board): BUILT — `panel.lua` accumulate board + global `<leader>d[`/`d]` flips (`init.lua:252-255`). Dropbar role (breadcrumb chip): PARTIAL — the provider exists (`tagcursor.statusline()` `tagcursor.lua:82-87` + `vim.b.fox_unit`) but no in-plugin chrome surface consumes it (the winbar chip `status.lua` shows SIZE, not the enclosing unit). Treesitter-context role (sticky `[TYPE]`): PARTIAL-by-different-shape — the follow card tracks the enclosing unit in a side card; no sticky header line. Aerial role (`[CONTAINS]`/`[SECTION]` outline TOC): MISSING — zero hits for a SECTION-based outline across all modules (grep 2026-08-18; only `layout.lua:32` unrelated). todo-comments role: MISSING (see row 16, `[DEFERRED]`). |
| 10 | **Per-`[TYPE]` card facets** (FUNCTION / STRUCT / REGISTRY-`[ROW]`/`[COLUMN]`) | **PARTIAL — FUNCTION + STRUCT rich; REGISTRY facet MISSING** | FUNCTION: calls/called-by/trace in the one Hud renderer (`init.lua:48-65`) + shipped-asm card w/ instr/SIMD/budget + call-follow (`asmshipped.lua`) + branch verdicts. STRUCT: layout/fields/byte-map/embedders/blast-radius (`init.lua:66-93`, byte_layout_cascade lens) + register-fit card `regfit.lua` (`c0ecbbe`). REGISTRY: **no surface** — grep across all 55 modules finds REGISTRY only in the retired hardcoded unit lists (`tagcontext.lua:14`, `tag_grammar_adapter.lua:31`); no actions row gates on `registry` (`actions.lua:96-114` types = function/struct only); fleet refute-spot had already flagged "REGISTRY facets unbuilt". Upstream dep: the `[ROW]`/`[COLUMN]` sub-schema is itself a Layer-1 format gap (north-star line 178 "the biggest format gap"). |
| 11 | **Better module grouping** (~40 modules by concern) | **MISSING** | `<P>/lua/fox-symdeps/` is flat: 54 .lua files + one `lenses/` subdir (ls 2026-08-18). No producers/overlays/UI/adapters grouping. |
| 12 | **The unit CARD — universal collapsed/expanded renderer** (§ 7.5 SEE, marked missing) | **PARTIAL** | One Hud renderer IS reused by float / board cards / follow card / compare companion (`hud.open` + shared `M.inspect` fetch, `init.lua:42-95`; `panel.lua:45-49`; `followcard.lua:25-29`) with kind-dispatched sections and shared collapse machinery — the de-facto card. But it is polymorphic over treesitter kind (function/type), not over the full `[TYPE]` set (REGISTRY/FILE render as generic), and asmshipped/regfit/dashboard are separate card implementations unified only at the `ui.lua` placement layer. |
| 13 | **Graph-browser** (re-root on edge-follow + back/forward history) (§ 7.5 NAVIGATE, marked missing) | **PARTIAL** | Exists on ONE surface: shipped-asm CALL-FOLLOW walks the call graph by keypress with a back stack (`asmshipped.lua:558,623` — "`b` pops back"; `5c3a092`). The general HUD-tree version (re-root on a consumer/embedder/uses edge + breadcrumbs + back/forward) is NOT built — no history/breadcrumb machinery in `hud.lua`/`panel.lua` (grep); `<P>/DOCS/ROADMAP.md:92-93` ("graph navigation") + `:201-202` (Tier-1 "[converged] Graph-walk drill-in") both still unchecked. |
| 14 | **Tag-query** ("all SLOW_PATH fns > 500 instr") (§ 7.5 NAVIGATE, marked missing) | **MISSING (pieces exist)** | No compound tag-x-metric query surface under any name. Pieces that would compose it: browse-by-tag corpus walk (`browse.lua:77-129`), repo-wide `[DERIVED]` corpus scan (`dashboard.lua:184-198`), HUD `T` filter, budgets JSON. Checked: keymap registry `init.lua:201-257`, actions registry, module list — nothing matches. |
| 15 | **Derived-write / refresh DERIVED in place** (§ 7.5 AUTHOR, marked half: "no struct size, partial DERIVED" — still true?) | **PARTIAL — materially advanced; the doc's wording now wrong in kind** | `:FoxSymdepsDerived!` writes `[UPSTREAM]`/`[CONSUMERS]` in place, MERGE-preserving the cache-gate's `[SIZE]`/`[ALIGN]`/`[STRADDLE]`, then saves (`init.lua:296-325`; `tagwriter.lua:44-67`); menu row `derived-write` w/ comments-only write-tier (`actions.lua:96-98`). "No struct size" is now a settled DESIGN CALL, not a gap: volatile facts (instr/simd) stay live-preview; layout facts are owned by `check_cache_layout.py --fix` (`tagwriter.lua:2-8`; the tool + `--fix` + idempotency verified at `/home/caramel/code/tick-trader-percore-workspace/tools/check_cache_layout.py:82,290,426`). Producer invocation from the editor: bless flows run in-editor terminals (`actions.lua:117-124`, `9209258`); **missing:** a menu row invoking `check_cache_layout --fix` (unblocked today) and `foxtag update` (blocked — toolchain increment 2c, north-star line 244-246). TAG ADD writes the `[TAG]` line (`tagadd.lua`, `7da124e`). |
| 16 | **FIFTH ROLE items** | **1 of 6 BUILT** | `[REFERENCE]` resolver: **BUILT** = docview (row 5; jumps to D-num/PARITY/spec/memory defining sites via `citable_ids.py`). Registry/row browser: **MISSING** (row 10). Orphan/dead-unit detector: **MISSING** plugin-side (greps for orphan/zero-consumer views: only unrelated hits `asmshipped.lua:750`, `docview.lua:140`; engine-side `calls_graph_diff.sh` is CLI-only). Version-timeline per unit: **MISSING** (no hits). `[SEAM]`/parity view: **MISSING** (no hits). `[DEFERRED]` work-queue: **MISSING** (no hits; todo-comments unreplaced). |
| 17 | **§ 6 "Current gaps" line** (branchtag false-green / diagnostics 2-step / ambient-status blank-on-templates + flicker) — still true? | **MOSTLY STALE; two residues live** | Branchtag false-green: **root fixed** — exact qualified-name block match (`asmdiff.lua:37-50`), name-position matcher on shipped asm (`asmshipped.lua:55-80`, `644aa45`, `77ab06e`), honesty relabel of the editor-flags explorer + the 1:1 sidecar truth surface (`be03cbc`); trust discipline codified `DECISIONS.md:58-64`. Diagnostics 2-step: **STILL TRUE** — toggle says "inspect a struct to populate" (`diagnostics.lua:102-103`); publish only fires from an inspect (`init.lua:78`). Blank-on-templates: status chip fixed (`◇ Sym <T>` `status.lua:15`); sizeprobe resolves canonical instantiations via `template_args` (`0b3fa8d`, `init.lua:19-22`); ambient lens **still silent** on unresolved templates (`ambient.lua:31`). Flicker: not explicitly addressed — ambient still clears-then-async-repaints (`ambient.lua:26-37`); chip clears on nil-context (`status.lua:39-41`). |

---

## 2. GENUINELY-MISSING — ranked by value-per-effort for a dogfooding operator

1. **HUD graph-walk drill-in** (re-root on the selected tree entry + breadcrumb/back). Highest converged demand (`ROADMAP.md:201-202` marks it Tier-1 [converged] and the roadmap marquee); every seed exists (tag-enriched entries already name the target unit `hud.lua:590-595`, board history, `Hud:reset`, and asmshipped's back-stack proves the shape `asmshipped.lua:623`). Unblocks the § 7.5 graph-browser role for free.
2. **Per-entry "compare with →" / open-beside key** (§ 6 rows 2+7 residue). One buffer-local key on tree entries routing to `panel.add`-beside + companion-pick. Small, closes two named § 6 bullets, and makes the board reachable from analysis output instead of only from source.
3. **In-editor "refresh layout facts" menu row** — `check_cache_layout.py --fix` via the existing `bless_term` pattern (`actions.lua:14-25`). Trivial effort, UNBLOCKED today, and it completes the D-372 app-level loop ("invoke the producers to REFRESH DERIVED in place") for the layout axis. (`foxtag update` stays blocked on toolchain increment 2c — sibling surface.)
4. **Compare connective tissue** (§ 6's stated value of the dual-panel: shared call-graph edges, cross-pair blast-radius, false-sharing). "NEW PRESENTATION, not new analysis" still holds — writers/false_sharing/byte_layout_cascade lenses already compute the inputs; the work is a join pane between the two cards. Moderate effort, high engine-relevance (H6/H22).
5. **`[DEFERRED]` work-queue picker.** A `browse.by_tag` clone over `[DEFERRED]` lines → quickfix/picker. Near-zero effort (the rg+unitindex pipeline is written), and its value compounds as conversion coverage grows.
6. **Orphan/dead-unit dashboard tile** (units whose written `[CONSUMERS]` is absent/empty). A configuration of the existing `[DERIVED]`-corpus scan (`dashboard.lua:184-198`). Low effort; caveat — only meaningful on the converted corpus, so it inherits conversion coverage.
7. **Tag-query composer** (tag set x metric predicate over the written corpus + budgets). Moderate effort; becomes cheap after 1 (shared corpus walk) and after the dashboard's ranked view generalizes.
8. **REGISTRY/X-macro card facet** (`[ROW]`/`[COLUMN]`). Named "the biggest gap" in § 6 — but **BLOCKED upstream** on the Layer-1 sub-schema (sibling agent's surface). Do not start plugin-side first; the fleet refute-spot already made this call ("function/struct+universal suffices until D-334 cards land").
9. **Diagnostics one-step + ambient template chip + flicker polish.** Small trio: auto-populate straddle diags on toggle (run the layout fetch for the enclosing struct), render an ambient `<T>` chip like status.lua does, repaint-before-clear.
10. **Breadcrumb chip wiring + `[CONTAINS]`/`[SECTION]` outline TOC** (dropbar/aerial roles). Providers exist (`tagcursor.statusline`); the TOC needs a section-line scan similar to `unitindex._parse`. Moderate-low.
11. **Version-timeline / `[SEAM]` parity view.** Real but lowest urgency: timeline needs the 899-instance version-tag harvest; the seam view matters at the next ML arc, not this one.
12. **Module grouping by concern.** Cheap but zero user-facing payoff and a require-churn risk across 55 modules + tests; do it opportunistically at the next structural edit, not as a deliverable.

---

## 3. STALE-IN-DOC — items the north star marks missing/half that are BUILT

| Doc mark (§ / line) | Reality | Proof |
|---|---|---|
| § 6 line 183-184 "all the UX targets above are unbuilt" | The entire 0.4 arc contradicts it | Rows 1-7 above; commits `0b08c1e..46f4dbb` (2026-08-09..11) |
| § 7.5 NAVIGATE "tag-enriched + filterable trees" — missing | BUILT | `1b95f7a` + `46f4dbb`; `hud.lua:585-595, 474-534`; `unitindex.lua` |
| § 7.5 NAVIGATE "doc-viewer ([REFERENCE] float)" — missing | BUILT (+ pin, + subcat routing, + dead-link refusal) | `e380a5a`, `c15a479`, `e116ae2`; `docview.lua` |
| § 7.5 NAVIGATE "dual-panel compare" — missing | BUILT v1 (side-by-side; connective tissue outstanding) | `28fc247`; `panel.lua:60-119` |
| § 7.5 NAVIGATE "action menu 🟡 (all-hotkeys-shown, m/M conflict)" | FIXED and exceeded: context+when-gated, write-tiers, key suffixes derived, collision guard, menu-as-root | `cd971ac`, `99e6824`, `9bd8d97`, `fb03f31`; `actions.lua`, `menu.lua:9-38` |
| § 7.5 SEE "overlays 🟡 (false-green, blank-on-template)" | Largely fixed (exact asm match, probe-error surfacing, chip `<T>`, width-lit de-noise, 1:1 sidecar truth); residue = ambient template/flicker (row 17) | `asmdiff.lua:37-50`; `hud.lua:442-444`; `status.lua:15`; `be03cbc`; `DECISIONS.md:58-64` |
| § 7.5 SEE "compiled-reality probes 🟡 (RC-A templates break them)" | Largely fixed: `template_args` canonical instantiation + injected-class-name rejection + the sidecar path needs no recompile at all | `0b3fa8d`, `1b2962a`; `init.lua:19-22`; `asmshipped.lua:1-13` |
| § 7.5 AUTHOR "derived-write 🟡 (no struct size, partial DERIVED)" | Advanced + re-scoped: stable-facts write+merge+save shipped; "no struct size" is now the deliberate stable/volatile division (layout facts = `check_cache_layout --fix`), not an omission | `init.lua:296-325`; `tagwriter.lua:1-8,44-67` |
| § 7.5 FIFTH ROLE "[REFERENCE] resolver" — listed as unbuilt | BUILT = docview (same machinery as the doc-viewer row) | `docview.lua:42-98` |
| § 8 line 247 "Plugin major-features work, overlays/asm half-broken (phase 5, operator's session)" | Stale: overlays trust-fixed; asm now has the sidecar-1:1 SHIPPED card family (follow, sync, chips, call-follow, inline attribution) | `be03cbc..77ab06e` (2026-08-13..16) |

---

## 4. SUPERSEDED / SPEC-MOVED — do not "finish" these to the letter of § 6

1. **"HUD auto-follows the cursor" (§ 6 line 137)** — superseded by the 3-surface refinement (`699d1c2`): float HUD = point-at transient; **follow card** (`<leader>df`) carries the auto-follow role; board = accumulate. Cockpit docks the follow card (`cockpit.lua:18-24`). Any § 6 rework should target the follow card, not re-add follow to the float.
2. **Keybind-safety "move nav/action keys off d-prefix" (§ 6 lines 174-175)** — **PARKED by the operator** (fleet register K2, answer #4: "i can just hit u if i delete"). Not missing work; revisit only on her call.
3. **"Fill available space" (§ 6 line 160)** — operator disposition (fleet answer #3): readability caps stay; ultrawide surplus becomes MORE PANES, not wider panels. The remaining runway is the N-card generalization, not width growth.
4. **Layer-stack rule + menu-as-root (2026-08-10, fleet answers #1 + tail batch)** — ADDITIVE law on top of § 6's context-gating: opened-item > menu > HUD, ancestors collapse (`actions.lua:204-215`, `hud.lua:303-319`); the menu carries EVERY valid option with launchers derived from the keymap registry (`actions.lua:189-195`, `init.lua:384-392`). Future § 6 work must honor both.
5. **The `n` doc-mention sweep + `opts.doc_dirs`** — DELETED by operator call (`8100e8c`); curated `docs_refs` + docview supersede (`README.md:88-89`). A § 6 reading that expects a mention-grep lens is dead.
6. **`[DERIVED]` authorship split (T6)** — the editor writes stable call-graph facts only; volatile codegen facts never write; layout facts are the cache-gate's (`tagwriter.lua:1-8`). Supersedes any reading of D-372 where the plugin itself computes-and-writes every DERIVED axis.

---

## 5. REFUTE-SPOTS for the paired a-class

1. **"Facts in the HUD = BUILT" is my most refutable call.** The § 6 sentence wants the branchless verdict IN the HUD/panel card; today the verdict lives in adjacent surfaces (branchtag overlay, hotpath lens section, asmshipped chips) and the HUD's own Layout section carries size facts only. If you read "in the HUD" strictly, this drops to PARTIAL.
2. **Ambient flicker (row 17)** — I inferred still-open from the clear-then-async-repaint shape (`ambient.lua:26-37`) without a runtime repro; the debounce-on-CursorHold may make it imperceptible in practice. Verify live before ranking the polish item.
3. **MISSING verdicts rest on greps + the two registries** (keymap `init.lua:201-257`, actions `actions.lua:29-125`) per the mission protocol; the plugin renames concepts aggressively, so a fifth-role feature hiding inside `aggregate.lua`/`neotree.lua`/`asmview.lua` (not line-read) would escape me. I consider it unlikely (the registries are the operator's own everything-reachable rule) but it is the structural hole in a 55-module census.
4. **"REGISTRY facet blocked upstream"** — I took the north-star's own claim (line 178) plus the fleet refute-spot at face value; if the Layer-1 sub-schema landed in the toolchain since (sibling agent's surface), my rank-8 "do not start" flips to "start now".
5. **Value ranking #1 vs #3** — I ranked graph-walk above the `--fix` menu row on dogfood frequency; an a-class could argue the reverse on the D-372 app-completion criterion (the north star's own V1 bar), which #3 closes and #1 does not.
6. **README/ROADMAP internal staleness** (README `21 tests` vs 46 `test_*.lua` files on disk; ROADMAP Tier-0 boxes unchecked though `DECISIONS.md:58-64` records them done) — minor, but it means the plugin's OWN docs also can't be trusted as marks, which is exactly how I treated them; a refuter should not cite ROADMAP checkboxes against my BUILT verdicts.

**Bottom line:** of the 17 checklist items, 7 are BUILT, 6 PARTIAL, 4 MISSING — against a doc whose § 6 closes with "all the UX targets above are unbuilt". The genuinely-missing core is now: HUD-tree graph-walk, compare connective tissue, the REGISTRY facet (upstream-blocked), the fifth-role tool family minus docview, and a short polish tail — with the single cheapest D-372-closing move being the `check_cache_layout --fix` menu row.
