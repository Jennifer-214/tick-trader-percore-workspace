# v5.15.5.F.4c.2 — Bandit 5-state ghost-training expansion handoff prompt

**Created:** 2026-05-15 (after v5.15.5.F.4c.3 B.1 final ships)
**Target ship:** `v5.15.5.F.4c.2` — Bandit 5-state ghost-training expansion + per-core override generalization
**Branch:** `feat/v5.15-live-readiness` (sprint branch; same as predecessor)
**Predecessor tag:** `v5.15.5.F.4c.3` (B.1 final; 12 class closures + comprehensively branchless OrderManager + Pattern 5 sink-fn-pointer first canonical)
**Plan file:** `plans/v5.15-live-readiness/subplans/2026-05-14-v5.15.5.F.4c.2-bandit-ghost-training-5-state-expansion.md`
**Sprint MASTER:** `plans/v5.15-live-readiness/MASTER.md`
**Predecessor postmortem:** `plans/v5.15-live-readiness/postmortems/2026-05-15-v5.15.5.F.4c.3-postmortem.md`

---

## Paste this prompt into a fresh Claude Code session to start `v5.15.5.F.4c.2`

```
I'm starting v5.15.5.F.4c.2 — Bandit 5-state ghost-training expansion + per-core override
generalization. This builds on the v5.15.5.F.4c.3 B.1 final ship which closed Class 27
(OMS scalar cfg-mirror) + Class 28 first canonicals (HandleFill Pattern 1 + multiple
Pattern 3 mask-selects + Pattern 5 sink-fn-pointer) + comprehensively branchless OrderManager.

This is a fresh context window; do NOT trust any prior-session memory — verify everything
against current code.

## Step 0 — orient + verify state (MANDATORY)

1. SHA-diff trigger check:
   - `cat /home/caramel/code/FoxML_Trader_v2/Version.hpp` — must contain `5.15.5.F.4c.3`
   - `cd /home/caramel/code/FoxML_Trader_v2 && git tag --sort=-creatordate | head -3` — confirm
     `v5.15.5.F.4c.3` is latest tag
   - `cd /home/caramel/code/FoxML_Trader_v2 && git status` — should be clean (or only
     `claude_session.md` untracked)
   - `./build/controller_test 2>&1 | tail -3` — must show 3148 / 0
   - `python3 tools/check_per_core_registry_integrity.py 2>&1 | tail -3` — must show
     `all 6 structural checks PASS` + `0 Section C exemption(s) on file`

   If ANY value diverges: stop + investigate via `git log v5.15.5.F.4c.3..HEAD` before proceeding.

2. Read in parallel:
   - `CLAUDE.md` (always-loaded; H20 + 14 hard invariants)
   - `CLAUDE.local.md` (going-forward rules INDEX)
   - `plans/v5.15-live-readiness/MASTER.md` (sprint MASTER)
   - `plans/v5.15-live-readiness/subplans/2026-05-14-v5.15.5.F.4c.2-bandit-ghost-training-5-state-expansion.md` (THE plan)
   - `plans/v5.15-live-readiness/postmortems/2026-05-15-v5.15.5.F.4c.3-postmortem.md` (predecessor closure detail)
   - `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md` (auto-memory index)
   - `DOCS/RECURRING_BUG_PATTERNS.md` Class 24 (Capability-cfg surface mismatch — directly relevant to ghost-training scope)
   - `DESIGN_SPECS/multi-state-dispatch-with-per-state-update-metadata.md` (if exists — `.F.4c.2` introduces this pattern)

3. Required-reading BEFORE WRITING CODE:
   - `DOCS/STRATEGY_AND_CODING_RULES.md` (private; 11+ strict invariants)
   - `plans/_cross-cutting/2026-05-06-latency-path-discipline.md` (7 latency-path rules)
   - `DOCS/DESIGN_PHILOSOPHY.md` § 2 (Hard Invariants — INCLUDES H20) + § 4 + § 6 + § 7 + § 11

## Step 1 — re-verify plan + check for drift

Run `/readiness plans/v5.15-live-readiness/subplans/2026-05-14-v5.15.5.F.4c.2-bandit-ghost-training-5-state-expansion.md`.
Address any GAP findings before resuming code.

## Step 2 — pre-coding audit gate (MANDATORY per Caramel's discipline)

This ship is MED risk per plan body. Fire the multi-skill audit gate BEFORE coding:

```
/precoding-audit-gate plans/v5.15-live-readiness/subplans/2026-05-14-v5.15.5.F.4c.2-bandit-ghost-training-5-state-expansion.md
```

The audit_set runs `/parity-check + /trace-deps + /readiness + /merge-scan + /dod-audit` in parallel.
Synthesizes convergent findings into one report. Returns GREEN/YELLOW/RED verdict + triage list.

**Per going-forward rule (feedback_consult_on_audit_findings):** after pre-coding checks complete,
PRESENT findings to Caramel + list potential fixes + iterate. Do NOT auto-proceed.

## Step 3 — DESIGN_SPECS pattern check

The .F.4c.2 plan calls for a NEW DESIGN_SPEC:
- `multi-state-dispatch-with-per-state-update-metadata.md` (NEW; per-state update metadata table
  format for bandit algorithm dispatching across 5 ghost-training states)

Plus reuses existing patterns:
- `categorical-tag-applicability-pattern.md` (per-core override generalization)
- `cfg-scope-discipline.md` (per-core cfg discipline — KEY canonical from .F.4c.3)
- `branchless-dispatch-discipline.md` Pattern 1 (state dispatch table; same shape as
  HandleFill Pattern 1 first canonical)
- `decision-time-data-binding-pattern.md` (per-core bandit cfg flows via Order pre_resolved
  semantic when applicable)

## Step 4 — design philosophy reminders (LOAD-BEARING)

From the latest .F.4c.3 ship close:
- **H20 INVARIANT** (CLAUDE.md): Branchless preferred for SP/HP data-dependent dispatch EVEN
  WHEN NOMINALLY SLOWER. Apply Pattern 1/3/4/5 per `branchless-dispatch-discipline.md`.
- **Decision-time data binding** (Pattern 4): per-instance cfg values bind at decision time +
  flow forward with in-flight object. Per-core bandit cfg follows this discipline.
- **Pattern 5 sink-fn-pointer** (NEW at .F.4c.3): optional side-effect calls eliminated at
  callsite via fn-pointer dispatch with noop default. Apply if .F.4c.2 introduces new optional
  emit paths (calibration extensions, bandit telemetry).
- **cfg-scope-discipline** (per-core single-param consumer fns; consumer-over-per-core-array
  for multi-slot dispatch; recovery-path nullable pointer). All canonical shapes documented
  in `cfg-scope-discipline.md` Stage 3 at .F.4c.3 close.
- **CI Check 7 + 0 Section C exemptions invariant** — Class 27 prevention live; any new cfg-mirror
  scalar field on subsystem state = build failure. .F.4c.2 must respect this.
- **Hot path UNTOUCHED** — `tools/calls_graph_diff.sh` verify at ship close. .F.4c.2 changes
  should not affect hot-path latency.

## Step 5 — bandit ghost-training scope (per .F.4c.2 plan body)

Plan body has full detail. Headlines:
- Wire missing `Thompson_Update` production loop (silent ML capability gap caught by
  `/ml-audit` 2026-05-14; Class 24 instance)
- Extend `FOREACH_BANDIT_ALGORITHM` 3-state → 5-state with per-state update metadata
  - Current states: EXP3, THOMPSON, EXP3_GHOST_THOMPSON (3)
  - Target states: EXP3, THOMPSON, EXP3_THOMPSON_GHOST, THOMPSON_EXP3_GHOST, BOTH_OP_BOTH_GHOST (5)
- Introduce `PER_CORE_OVERRIDE_SCALAR_DOMAINS` macro (per-core scalar cfg field override
  registration via FOREACH meta-registry; complements existing per-core bitmap framework)
- NEW DESIGN_SPEC `multi-state-dispatch-with-per-state-update-metadata.md`
- Cfg surface migrations:
  - `bandit_algorithm` enum table extended
  - Per-state operational/ghost update metadata table
  - Per-core override discipline applied (per-core bandit algo selection)

## Step 6 — TECH_DEBT items in surface area

- Reconcile ReconcileTrade exchange-fee-from-source (Class 27 corner case; B.1 deferred;
  doesn't block .F.4c.2 but is in the same ML-pipeline-correctness family)
- Per-core ConfidenceScorer cfg-mirror audit (potential Class 27 instance for
  `confidence_*` cfg fields; cohort sweep candidate)
- Bandit state subsystem cfg-mirror audit (Class 27 — does ThompsonBandit / Exp3State carry
  scalar cfg mirrors that flatten per-core distinction?)

Fire `/accounting-audit module:ml-pipeline` early in the ship to surface Class 27 instances
adjacent to ghost-training scope.

## Step 7 — operator collaboration norms (per memory rules)

- Address Caramel as Caramel / she / her
- Don't use AskUserQuestion modal boxes; present options as plain text inline
- Evaluate options on robustness + latency + design philosophy, NOT time
- After pre-coding audits, present findings + iterate BEFORE coding (consult discipline)
- Auto-write TECH_DEBT for deferred items
- "Branchless even when slightly slower" — Caramel's stated principle; applies to all
  SP/HP/drainer data-dependent dispatch even when H20 exception #4 would technically allow
  the branch. Apply Pattern 1/3/5 default; reach for `__builtin_expect`-only when alternatives
  fail.
- Don't measure structural work by LOC; lead with classes closed + patterns codified +
  future-work-becomes-mechanical
- Caramel has ADHD + practices delayed reward (deep design work has intrinsic value per
  memory); don't rush to ship when design depth is the value

## Step 8 — filesystem conventions

- Workspace: `/home/caramel/code/tick-trader-percore-workspace`
- Engine: `/home/caramel/code/FoxML_Trader_v2`
- Cite WORKSPACE paths in chat / cross-refs (not engine-side symlink paths)
- `Edit` tool refuses writes through symlinks — `readlink -f <path>` first if uncertain
- New design docs default to private (workspace) per CLAUDE.local.md going-forward rule
- Auto-private gitignore patterns: `*_AUDIT.md`, `FUTURE_*.md`, `*-design-notes.md`,
  `*-suggestions.md`, `*-hft-*.md`, `*-OPTIMIZATION-*.md`, `STRATEGY_AND_CODING_RULES.md`,
  `*.local.md`

## Step 9 — sprint-close verification gate (.F.4c.2 ship close)

Before declaring `v5.15.5.F.4c.2` shipped:
- `./build.sh test` GREEN
- controller_test all tests pass (3148 at predecessor baseline + new bandit tests)
- depth_recorder_test 17/17
- `tools/check_per_core_registry_integrity.py` PASS all checks 0 Section C exemptions
- `tools/calls_graph_diff.sh verify` confirms hot path bytewise-unchanged
- `/parity-check module:ML-pipeline` GREEN
- `/dod-audit module:bandit` GREEN
- `/ml-audit` GREEN (specifically: Thompson_Update wired into production loop)
- `/bug-check class_24 module:bandit` CLEAN
- `Version.hpp` bumped 5.15.5.F.4c.3 → 5.15.5.F.4c.2 (RENAMING per plan note —
  .F.4c.3 ships BEFORE .F.4c.2; tag order follows ship order, not phase number)
- Tag `v5.15.5.F.4c.2` annotated (note: tag number diverges from chronological ship order;
  this is per `feedback_rename_plans_to_match_ship_order.md` — rename plan if needed)
- Postmortem written
- `/sync-workspace` if workspace updated

## Step 10 — post-ship audit-sweep (per .F.4c.3 lessons)

After `v5.15.5.F.4c.2` tag lands, run the `/post-ship-audit` skill:

```
/post-ship-audit v5.15.5.F.4c.2 plans/v5.15-live-readiness/subplans/2026-05-14-v5.15.5.F.4c.2-bandit-ghost-training-5-state-expansion.md
```

Generates postmortem doc; catches hand-waves + optimization gaps + structural gaps + discipline
drift. Caramel's catches during .F.4c.3 (HandleFill 2D → 1D, DrainPostFill recompute gap, etc.)
demonstrate this audit's value.

## Closing reminder

`.F.4c.3` B.1 final delivered 12 class closures + 4 framework codifications + Pattern 5 first
canonical. Per Caramel's principle "comprehensive branchless even when slightly slower" — the
.F.4c.2 ship should continue that discipline. Bandit dispatch state machine is a Pattern 1
candidate; ensure 5-state → handler dispatch is via fn-pointer table, not switch/if.

Caramel has been deeply engaged throughout the .F.4 sprint. Surface decisions before coding;
iterate. Her catches are sharp + load-bearing — respect the "consult before coding" discipline.

Good luck.
```

---

## Notes for future-Claude reading this handoff doc

- Prompt above is self-contained — paste as FIRST message in fresh `claude code` session
- Includes 10 steps + design philosophy reminders + filesystem conventions + sprint-close gate
- `.F.4c.3` postmortem captures full closure context; required reading at Step 0.2
- Engine TAG `v5.15.5.F.4c.3` is the anchor (commit + tag landed at .F.4c.3 r-8 ship close)
- New skill `/post-ship-audit` (introduced at .F.4c.3 B.0d) is the canonical post-ship review tool
- Pattern 5 `sink-fn-pointer-for-optional-side-effect-pattern.md` is NEW at .F.4c.3 — apply
  if .F.4c.2 introduces new optional emit paths

---

## Quick links

- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- This ship's plan: `plans/v5.15-live-readiness/subplans/2026-05-14-v5.15.5.F.4c.2-bandit-ghost-training-5-state-expansion.md`
- Predecessor postmortem (.F.4c.3): `plans/v5.15-live-readiness/postmortems/2026-05-15-v5.15.5.F.4c.3-postmortem.md`
- DESIGN_SPECS catalog: `DESIGN_SPECS/README.md`
- NEW: `DESIGN_SPECS/sink-fn-pointer-for-optional-side-effect-pattern.md` (Pattern 5)
- branchless-dispatch-discipline: `DESIGN_SPECS/branchless-dispatch-discipline.md`
- decision-time-data-binding: `DESIGN_SPECS/decision-time-data-binding-pattern.md`
- cfg-scope-discipline: `DESIGN_SPECS/cfg-scope-discipline.md`
- Memory index: `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md`
