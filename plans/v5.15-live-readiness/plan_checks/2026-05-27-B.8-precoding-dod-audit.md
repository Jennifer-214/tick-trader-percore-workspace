---
type: audit-report
audit: dod-audit
scope: plan
plan: v5.15.5.F.4d.1.B.8
established: 2026-05-27
status: complete
verdict: GREEN
audit_tier: HIGH-RISK
sister_audits: [precoding-audit-gate, parity-check, trace-deps, readiness, blindspot-scan]
---

# /dod-audit report — v5.15.5.F.4d.1.B.8 (plan-mode) — 2026-05-27

## Catalog ingested

- **80+ patterns** from `tick-trader-percore-workspace/DESIGN_SPECS/` (data-disciplines/ + refactor-patterns/ + framework-patterns/ + wire-format-patterns/ + meta-disciplines/)
- **Patterns directly engaged by this plan body:**
  - `per-snapshot-cluster-layout-pattern.md` (Stage 3; `PerCoreCfg<F>` cluster integrity)
  - `cache-line-discipline.md` (Stage 2; alignas / false-sharing prevention)
  - `struct-padding-determinism-pattern.md` (Stage 5; H12 byte-equivalence)
  - `branchless-dispatch-discipline.md` (Stage 5; H7/H20 — mechanical substitution, no new branches)
  - `cfg-field-categorization-discipline.md` (Stage 3; PER_CORE_FLAT_SYNC_PARAMETER consumer-side analog)
  - `cfg-scope-discipline.md` (per-core fields read via core_id index)
  - `decision-time-data-binding-pattern.md` (Class 27 closure sister; LOW-1 closes vestigial instance)
  - `canonical-sister-extension-discipline.md` (Check 10 extends Check 9; M7 next canonical)
  - `structural-enforcement-when-memory-insufficient.md` (M7 parent; Check 10 = next canonical)
  - `single-source-of-truth-discipline.md` (PER_CORE_FIELD_LIST derived from FOREACH_PER_CORE_CFG_FIELD)
- **Doc-debt findings:** NONE

## Per-invariant verdict

| Invariant | Verdict | Notes |
|---|---|---|
| **H4** (FPN<F> for accounting; no float/double on accounting paths) | **PRESERVED** | All 4 HIGH fix sites preserve `FPN<F>` storage; `FPN_ToDouble` conversion is display-side / slow-path-helper compute pattern (precedent at sister sites line 860/1918/1962); LOW-1 deletion CLOSES H4-violating vestigial `double fee_rate_taker_d` (was H4-borderline; classified as Class 27 sub-instance). Net H4 improvement. |
| **H6** (alignas(64) cross-thread; cluster by access pattern) | **PRESERVED** | No struct changes proposed to `PerCoreCfg<F>` (which is `alignas(64)`; line 285) or `ControllerConfig<F>`. DrainerConstants is drainer-thread-local POD (per file header line 25-27) — no cross-thread concern. |
| **H7** (hot path branchless) | **PRESERVED** | All fix sites are SLOW-PATH / drainer-init / GUI / display. Plan body line 14: `hot_path: UNTOUCHED`. Substitution is mechanical field rebind; no dispatch introduced. |
| **H12** (explicit padding for byte-equivalence) | **PRESERVED** | DrainerConstants is NOT used in `memcmp` / `sha256` / `hmac_*` / wire-format / stamp body contexts (grep verified: 0 hits in CoreFrameworks/ + tests/). Sizeof change 24→16 (with `_pad[7]` adjustment) is internal-only POD layout change; static_assert update is mechanical bookkeeping, not byte-equivalence concern. |
| **H13** (tt:: type-trait dispatch; no reinterpret_cast) | **N/A** | No type-erased dispatch in fix scope. |
| **H14** (no C++ bitfield syntax) | **N/A** | No struct field declarations of bitfield form. |
| **H15** (meta-registry coverage) | **PRESERVED** | Check 10's `PER_CORE_FIELD_LIST` derives from `FOREACH_PER_CORE_CFG_FIELD` (single source of truth; same convention as Check 1). No new registry added; no FOREACH_REGISTRY row needed. |
| **H20** (branchless preferred SP/HP) | **PRESERVED** | Fix sites already use branchless `!FPN_IsZero(X) ? X : Y` ternary — substitution preserves shape exactly (just rebinds field path; no dispatch logic added). |

## Summary

| Pattern | CLEAN | APPLIED | MISSED | DEFERRED |
|---|---|---|---|---|
| per-snapshot-cluster-layout-pattern | OK | 1 (PerCoreCfg) | 0 | 0 |
| cache-line-discipline | OK | 1 (PerCoreCfg alignas(64)) | 0 | 0 |
| struct-padding-determinism-pattern | OK | — | 0 | 0 |
| branchless-dispatch-discipline | OK | 4 (existing ternary preserved) | 0 | 0 |
| cfg-field-categorization-discipline | OK | 1 (PER_CORE_FLAT_SYNC_PARAMETER consumer-side analog) | 0 | 0 |
| cfg-scope-discipline | OK | 4 NEW (HIGH-1/2/3/4 + MED-1 fixes are direct applications) | 0 | 0 |
| decision-time-data-binding-pattern | OK | — (LOW-1 closes vestigial sub-instance) | 0 | 0 |
| canonical-sister-extension-discipline | OK | 1 (Check 10 extends Check 9) | 0 | 0 |
| structural-enforcement-when-memory-insufficient (M7) | OK | 1 NEW (Check 10 = next canonical) | 0 | 0 |
| single-source-of-truth-discipline | OK | 1 (FOREACH_PER_CORE_CFG_FIELD as PER_CORE_FIELD_LIST source) | 0 | 0 |

## Findings (severity-ordered)

**NO BLOCKING / HIGH / MEDIUM / LOW findings.**

The plan body proactively engaged every DOD-relevant pattern correctly. Mechanical observations only:

### OBS-1 — DrainerConstants sizeof bound is precise

`DrainerConstants` (24B) post-LOW-1 deletion of `double fee_rate_taker_d` (8B):
- 4B `int registered_count` + 4B `int drain_count` + 1B `bool partial_on` + 7B `_pad[7]` = 16B
- New `static_assert(sizeof(DrainerConstants) == 16, ...)` is correct
- `_pad[7]` size correct for alignof(8) padding to 16B boundary
- File header comment block (lines 5-45) will need updating: "Total 24B" → "Total 16B"; "7B trailing pad" remains correct (still 7B trailing pad; the `double` field at offset 0 went away, but pad bytes track to next alignof(8) multiple)

**Not a finding; mechanical follow-up.** Plan body Phase B Step B.6 already cites these touch points.

### OBS-2 — `core_id_for_pos` scope verification at MED-1 line 249

Verified: `core_id_for_pos` declared at `ShardedSnapshot.hpp:216` (per-iteration local in while-loop over portfolio active_bitmap). Scope extends through line 274 (end of position loop body). Plan body Phase B Step B.5 substitution `cfg->cores[core_id_for_pos].fee_rate_taker` is scope-safe.

### OBS-3 — Decision-time-data-binding-pattern.md cross-ref clarity

Plan body cites this spec as "NO-APPLY direct" (Decision A canonical-sister table). Rationale: the 4 HIGH sites are slow-path strategy-adapt compute, not fill-event-bound. Confirmed correct framing — fee_rate IS pre-resolved onto Order objects at submit time (`o->pre_resolved.fee_rate` per ControllerConfig.hpp:1338 + line 1256 comment), but the slow-path ratchet helpers need cfg-side read for fee_floor cap math (not order-side). Direct per-core indexing IS the right shape.

## Recommendations

### Address now (ship-blocking or strong recurring class)

NONE — verdict GREEN.

### Address during current ship (folded into open plan)

Already-in-plan-body refinements (mechanical follow-ups, not new findings):

1. **DrainerConstants.hpp file header comment block** — update lines 5-45 ("Total 24B" → "Total 16B"; "Replaces the prior ad-hoc pattern of … repeated `FPN_ToDouble(cfg.fee_rate_taker)` conversions" — second clause becomes stale post-LOW-1 since the cached `fee_rate_taker_d` was the value that closed that 6-site reuse; deletion implies original reuse pattern returned). Reword to reflect post-deletion state honestly. **Suggested:** add to plan body Phase B Step B.6 mechanical checklist.

2. **Check 10 detection signature verification** — plan body specifies whitelist `{c, core, core_id, slot, i}`. Phase A enumeration deliverable (`plan_checks/2026-05-27-B.8-cohort-enumeration.csv`) should verify each whitelisted symbol's per-core scope convention is established by code-search before locking the whitelist. Specifically: `i` is broadly used (boot-time loop variants, NON-per-core loop counters in unrelated code) — Phase A should classify each `i` use case + tighten the regex if `i` would generate false positives outside per-core scope. **Already implicit in Phase A.4 classification step; flagging here for explicit triage at Phase A close.**

### Defer with TECH_DEBT entry

NONE.

### CLEAN — no action

All other patterns audited (per Summary table); no application gaps.

## Verdict: GREEN

**Plan body engages every DOD-relevant pattern correctly.** Hot path UNTOUCHED preserved. H4/H6/H7/H12/H13/H14/H15/H20 all PRESERVED or N/A. Cache layout undisturbed (no struct changes to alignas(64) PerCoreCfg<F>; DrainerConstants is drainer-thread-local POD, not byte-equivalence). Branchless ternary shape preserved at all 4 HIGH sites. M7 escalation correctly applies canonical-sister-extension (Check 10 sister to Check 9). Single-source-of-truth discipline preserved (PER_CORE_FIELD_LIST derives from FOREACH_PER_CORE_CFG_FIELD).

Two mechanical observations (OBS-1 DrainerConstants file header comment refresh post-LOW-1 + OBS-3 confirming decision-time-data-binding "NO-APPLY direct" framing is correct) are not findings; they're refinements the plan body already implicitly handles.

Proceed to coding upon completion of remaining 4 SHAPE audits (parity-check / trace-deps / readiness / blindspot-scan) + operator review per `feedback_consult_on_audit_findings`.

---

## Cross-references

- Plan body: `plans/v5.15-live-readiness/subplans/2026-05-27-v5.15.5.F.4d.1.B.8-accounting-cohort-fix.md`
- Predecessor audit: `plans/v5.15-live-readiness/plan_checks/2026-05-27-post-B.7-accounting-audit.md` (YELLOW; 4 HIGH + 1 MED + 1 LOW informed this plan)
- Sister precoding-audit-gate reports: `plan_checks/2026-05-27-B.8-precoding-{parity-check,trace-deps,readiness,blindspot-scan}.md` (pending fire)
- Audit synthesis (post-5-agent fire): `plan_checks/2026-05-27-B.8-precoding-synthesis.md` (pending)
- Code citations verified:
  - `MemHeaders/DrainerConstants.hpp:64-74` (POD struct + static_asserts; no byte-equivalence usage)
  - `MemHeaders/DrainerConstants.hpp:5-45` (file header comment block touchup at LOW-1)
  - `CoreFrameworks/ControllerConfig.hpp:285` (`PerCoreCfg<F>` alignas(64))
  - `CoreFrameworks/ControllerConfig.hpp:375` (`FPN<F> fee_rate_taker` field — H4 preserved)
  - `CoreFrameworks/ControllerEventLoop.hpp:3580-3635` (HIGH-1 EventLoop_TrailingSLRatchetOneCore; core_id param scope)
  - `CoreFrameworks/ControllerEventLoop.hpp:3654-3680+` (HIGH-2 EventLoop_BreakevenOnProfitOneCore; core_id param scope)
  - `CoreFrameworks/ShardedSnapshot.hpp:216,249` (MED-1; core_id_for_pos scope spans 216-274)
  - `CoreFrameworks/ControllerEventLoop.hpp:860,1918,1962` (sister-pattern existing per-core fee_rate reads via `effective_cores[slot/core_id].fee_rate_taker` — precedent for HIGH-1/2/3/4 substitution shape)
  - `tools/check_per_core_registry_integrity.py:405-449,614-637` (Check 9 sister-pattern for Check 10 structure)

---

**End of dod-audit report — verdict GREEN.**
