---
type: handoff
status: active   # the one live handoff; no-arg /accept-handoff resolves here. Supersedes 2026-06-15-E.1-decomposed (its "code E.1.0" next-action is now IN PROGRESS).
ship_tag: ".E.1.0 IN PROGRESS — 3 of ~6 §4d gate pieces landed + WIRED; next = H8 (fresh I/A agent pass)"
plan_type: live-readiness
sprint_end_goal: make the codebase maintainable + live-ready (v5.X professionalization); live gated behind the whole E-series
ship_end_goal: ".E.1.0 stands up the §4d guard NET (grep-CI H14/H7-H20 + determinism gate H10 + latency H8) that E.1.1's rename + E.1.2's SoA relayout run UNDER (guards-before-behavior, D-82). 3 of ~6 pieces done + wired; H8 + H7/H20 + baseline + V-class verify remain."
predecessor_handoff: handoffs/2026-06-15-E.1-decomposed-next-E1.0-handoff.md (superseded — its NEXT ACTION "code E.1.0" is now IN PROGRESS; 3 gates landed this session)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (latest D-227 — the .E.1.0 gate-disposition reshape + operator steers [D1/D2 + must-fixes + seam], logged at the session-close capture-audit)
plan_body: plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.0-guard-hole-closure.md (read § "IMPLEMENTATION PROGRESS" + § "3:3 GATE VERDICT + RESHAPE" — the CURRENT state)
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, the E.1.0 plan body § IMPLEMENTATION PROGRESS + § 3:3 GATE VERDICT + RESHAPE, the 6 plan_checks/2026-06-16-E.1.0-precoding-* reports (esp. I3-measurement for H8), regen CODE_MAP at pickup]
engine_head: 572c978 (.E.0.10 ship-close — UNCHANGED this session; NO engine code — all CI-tooling/apparatus)
workspace_head: at-or-after the /sync-workspace at this session's close (the 3 gates + wiring + 3:3 reports + body/guard-matrix/TOOLS/run_all_tests/.githooks edits)
coding_status: E.1.0 IN PROGRESS — 3 of ~6 pieces (must-fix #1 SSoT, H14 RED gate, H10 marquee gate) built + teeth-proven + WIRED + doc-CI green. NO engine code (all CI tooling). NEXT = H8 (fresh I/A agent pass) → H7/H20 advisory → baseline → V-class verify.
defers: "H8 (run a FRESH I/A agent pass FIRST — substrate not ratchet-ready), H7/H20 advisory + the grep-CI aggregator (returns at ≥2 gates), baseline→HOT_PATH_CHANGELOG, V-class verify. Ship-close: 'vacuously-green guard' RBP Class + seam-fold to E.1.2. FINDING: .githooks not mirrored by /sync-workspace (engine hook edits silently lost) — structural fix homed."
---

# Handoff — `.E.1.0` gates 1-3 landed + WIRED → next = CODE H8 (fresh I/A agent pass)

## TL;DR (the 60-second pickup)

- **`.E.1.0` is the §4d guard-NET ship** (first `.E.1` leaf): stand up CI gates that RED-build a future hot-path/determinism/latency regression, BEFORE E.1.1's rename + E.1.2's SoA relayout (guards-before-the-behavior, D-82).
- **3 of ~6 pieces landed this session — all teeth-proven + WIRED + doc-CI green** (NO engine code — all CI tooling):
  - **must-fix #1** — `tools/lib/sharded_files.txt` SSoT (calls_graph_diff loads it via `mapfile` + a missing-SSoT FATAL guard).
  - **H14** (`check_h14_no_bitfield.py` + `test_*`) — decidable RED gate, 0 hits, teeth-proven (7 detected / 17 look-alikes ignored). Wired: pre-commit **Check M**.
  - **H10** (`check_h10_simd_determinism.sh` + `h10_kernel_harness.cpp` + `_selftest`) — the marquee. Compiles the REAL `RidgeBlender_BuildCorr` ×{`-march=skylake-avx512`, `-mno-avx512f`}, memcmps, asserts the `__AVX512F__` split (non-vacuous). Wired: `check_determinism.sh` **gate 4** + pre-commit **Check F** + `run_all_tests.sh`.
  - All 5 tools enrolled in `DOCS/TOOLS.md`; guard-matrix §4a flipped **H14/H10 → ENFORCED**.
- **The 3:3 pre-coding audit (6 agents) is DONE** — it reshaped E.1.0: **H14+H10 = RED gates · H7/H20 → advisory (grep undecidable) · H8 → build-minimal-bench-now, ratchet→E.1.2**. Reports: `plan_checks/2026-06-16-E.1.0-precoding-*`.
- **NEXT = H8** (latency bench) — **run a FRESH I/A agent pass FIRST** (operator-directed; real design uncertainty — the substrate isn't ratchet-ready).

## The ideal way to start

1. **`/accept-handoff`** (resolves here). Regen CODE_MAP, verify engine `572c978`, load baseline + nav-infra.
2. **Read the current state:** the E.1.0 plan body § "IMPLEMENTATION PROGRESS" (done/pending) + § "3:3 GATE VERDICT + RESHAPE" (locked gate dispositions) + the **I3-measurement** 3:3 report (the H8 substrate reality).
3. **H8 is next → run the I/A agent pass** (fresh agents, per operator). **I-class:** deep-map the real latency infra (the `LATENCY_BENCH`/`LATENCY_PROFILING` paths, `EngineSharded_DumpLatency`, `CoreLatencyStats.hpp:240` lifetime_p99, the sharded-vs-single-core emit, cycles→ns) + propose the minimal-correct bench. **A-class:** refute it (reproducible? measures the sharded path? non-vacuous?). THEN build H8 + consult before coding (it touches engine observability).

## H8 — the surface kit (what the next session's agents need)

- The 3:3 already found H8's substrate **isn't ratchet-ready**: no `LATENCY_BENCH` build target; the BENCH p99 line is the **single-core-dead** path; p99 is a 256-sample **sliding window**; slow-p99 emitted **nowhere**; numbers are **cycles not ns**. (Detail: the I3-measurement + A1-vacuity reports.)
- **Build (operator-decided D2 — minimal bench NOW, it's foundational for the whole .E sequence):** a `LATENCY_BENCH` target + a bounded-tick bench (fixed sample population) + a lifetime-p99 emit (`CoreLatencyStats.hpp:240` already COMPUTES it — print it) + cycles→ns via `tsc_ghz`. Ship the **ABSOLUTE-budget gate** (hot p99 ≤500ns / slow ≤100μs — noise-immune); the 10%-margin **ratchet-down rides to E.1.2** (its first real consumer). Touches the latency-EMIT path (observability) + build — **NO trading-logic change**.
- **Pattern to follow = H10's gate**: the same compile→measure→gate shape, wired into `check_determinism.sh`-or-a-latency-umbrella + pre-commit + `TOOLS.md` + a teeth-proof selftest. (Non-vacuity = assert the bench actually ran / N samples seen — the "vacuously-green guard" lesson.)

## Remaining after H8

- **H7/H20** advisory (`check_h7_h20_branchless`, sources `sharded_files.txt`, `KNOWN_COHORT --strict` allowlist, **NEVER a RED gate** — undecidable per the 3:3). THEN the grep-CI aggregator (`check_hot_path_invariants.sh`, reverted this session as premature at 1 gate) returns at ≥2 gates WITH an rc-propagation selftest.
- **baseline** capture (`.E.0.10` hot/slow p99) → `HOT_PATH_CHANGELOG`.
- **V-class verify**: 5 binaries + 3635/0 + `calls_graph_diff` CLEAN + run `check_determinism.sh` (now also in `run_all_tests.sh`).
- **Ship-close codifications**: the **"vacuously-green guard"** RBP Class (3 instances this session: H10-on-non-AVX / H7-H20-grep-as-RED / H8-no-BENCH-target); the **seam-fold to E.1.2** (E.1.2 = the first REAL consumer of the gates, not E.1.1 — the 3:3 A3 catch).

## Findings homed (don't drop)

- **`.githooks` not synced (apparatus gap)** — `/sync-workspace` does NOT mirror `.githooks/`. The engine `.githooks/pre-commit` (live, gitignored) silently drifts from the workspace `.githooks/pre-commit` (tracked). I propagated this session's Check F/M edits to the tracked copy (so they survive + sync), but the **recurrence is open**: structural fix = **symlink engine `.githooks` → workspace** (like `tools/`/`plans/`), OR add `.githooks` to the `/sync-workspace` mirror. Fixable-next-session (apparatus, not capital).
- **run_all_tests determinism gap — FIXED this session**: `check_determinism.sh` (incl. H10) was pre-commit-only; added it to `run_all_tests.sh` so "verify everything" now includes the determinism net.

## Operator collaboration reminders (every-turn)

- Address **Caramel as Caramel / she / her**. No AskUserQuestion modals — inline options.
- Evaluate on **robustness + latency + design philosophy, NOT time**.
- **H8 = run agents FRESH** (operator-directed this session). **Consult before coding** the bench (it touches engine observability).
- The §4d gates are **CI-tools** (pre-commit), not skill-invoked — correct; skill-wiring (e.g. `/hft-audit` ↔ H7/H20) is optional-future, not a gap.
