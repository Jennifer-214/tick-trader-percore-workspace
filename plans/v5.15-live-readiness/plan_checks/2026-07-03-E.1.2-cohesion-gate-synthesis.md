---
type: audit-synthesis
gate: /precoding-audit-gate — COHESION pass (3-audit: readiness + dod-audit + trace-deps)
target: subplans/2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md
engine_head: b10e778
date: 2026-07-03
combined_verdict: RED (cohesion) / GREEN (design + code-readiness)
per_audit_reports: [readiness-2026-07-03-cohesion.md, dod-audit-2026-07-03-cohesion.md, trace-deps-2026-07-03-cohesion.md]
---

# E.1.2 cohesion gate — synthesis

**Combined: RED on cohesion · GREEN on design + code-readiness.** The *design* is fully validated — every amended deliverable applies the right pattern (dod-audit), every cited symbol exists at HEAD with no fabrication (readiness + trace-deps), the dependency chains hold (D-289 deletion is orphan-safe). But the plan *document* is internally contradictory: the append-only amendments left the reformalized body (frontmatter, ship-goal, acceptance criteria, the "RE-GROUNDED" design-shape, Phases A.0→G, the TITLE) all **pre-reversal** (192B / SoA / *store* `owner_node_id` / PORTFOLIO-bump), reversed only by the AMENDMENTS block + D-292/D-294 100+ lines later. **A coder reading top-to-bottom builds the superseded ship.** Fix = a consolidation rewrite (fold the amendments UP into the body, then collapse the block to an audit-trail pointer). Two findings are more than cosmetic.

## Per-audit verdict
| Audit | Verdict | Headline |
|---|---|---|
| readiness | **RED** cohesion / GREEN code-readiness | 7 stale layers (K1–K7); K6 = latent D-110 bug if coded as written; all symbols verified, no fabrication |
| dod-audit | **GREEN** pattern-fit / YELLOW cohesion | every deliverable applies the right pattern, none misapplied; strike the stale body |
| trace-deps | **YELLOW** | no chain-breaking fabrication; chains hold; Y1 accessor-site undercount → class-close at risk; `drawdown_*` absent |

## Consolidation edit-list (each = fix-in-rewrite)
- **K1** Position 192B → **128B** (body + ship-goal + acceptance + RE-GROUNDED shape + the TITLE "SoA/Money relayout").
- **K2** `peak` → **defer to D-206** (strike from design-shape + Phase B).
- **K3** `owner_node_id` → **derive via `Sharded_SlotNode`**, not store (strike the `uint16_t` PERSIST).
- **K4** PORTFOLIO version → **retire, don't bump**; **KEEP SHARDED 10→11** (partner_pending changes the per-node wire).
- **K5** SoA/H10 → **re-home to .E.6/.E.7**; E.1.2 is AoS; determinism gate = populated multi-node Save→Load byte-compare.
- **K6 / Y4 · [MUST-FIX — LATENT BUG]** Phase C serializer polarity: body says *"NOT a flat `FOREACH_NODE_PERSIST_FIELD`"* (delegates-only) — a coder following it rebuilds the ~24-flat-field D-110 silent-zero drop. Reword to the **HYBRID** (flat registry + compose-sub-registry delegates) per BLK-1/Amendment-1.
- **K7** `node_dd_pct` → **drop (recompute-on-load)**; strike the Phase-F "reserve wire-locked".
- **Y1 · [SUBSTANTIVE]** accessor site-set: "6 sites" is imprecise — add `ShardedSnapshot.hpp:272` (a 6th ENGINE-side derive, same shape); **decide GUI scope explicitly** (6 GUI sites: ChartPanel/DashboardPanels); and add a **CI guard for new open-coded `(slot|idx)>>` derives** to close the slot-derive CLASS, not just patch `:856` (`feedback_close_the_class_vs_migrate_every_site`; D-294's guard-gap covered only the H12-pad class).
- **Y2** `drawdown_max`/`drawdown_current` return ZERO hits at HEAD — the "drop" is a no-op → strike/annotate.
- **Y3 / Y5** stamp the acceptance-criteria 192B/bump/SoA lines "SUPERSEDED — item 8"; fix the `:853-855` cite (it's `ControllerEventLoop.hpp`, not `Portfolio.hpp`).
- **Wiring** the current Position deliverables (`Sharded_SlotNode` + route + `:856`; `_pad_pos` H12 fix; stale comments; the count-lock; partner_pending; the size→version guard) have NO phase home (Phases predate the reversal) → the Phase-B rewrite carries them.

## LOW — operator's call
Pad-zero fix could be `*p = Position<F>{}` in `Position_Reset` (reuses the struct's `= init` DMIs + zeroes the pad *for free*, simpler than walker+memset; the walker is more OMS-consistent). Both correct.

## Disposition
All findings = **fix-in-consolidation-rewrite**; **zero design change** (design GREEN). K6/Y4 (latent bug) + Y1 (class-close) are the substantive ones; the rest are strike-the-stale-layer.

## Recommendation
Consolidation rewrite (fold amendments 1–8 up + apply the edit-list) → light re-verify → code. This is doc-debt from append-only amendments (the cost of iterating leaner-in-place), not a design gap — the gate did its job: it caught that the plan body lies about itself before a coder followed it.

## Anti-pattern / M7 note
Y1 is a `feedback_close_the_class_vs_migrate_every_site` instance (patch vs class-close). The append-only-amendment-leaves-body-stale pattern is itself a candidate meta-note: heavily-amended plans need a consolidation pass before coding (a `/readiness`-cohesion check as a standing pre-code gate).
