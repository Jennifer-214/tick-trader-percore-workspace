---
type: handoff
status: active   # the one live handoff (no-arg /accept-handoff resolves here). Supersedes 2026-06-17-E.1.1-MID-DIVE-phase2-next-handoff.md (the Phase-2 pickup; Phase 2 now DONE).
ship_tag: "E.1.1 — Core→Node rename + cfg-key CLEAN-BREAK + single_core LIVE deletion. MID-DIVE: pre-coding done (7-agent gate RED→amended + 8-agent state-audit, both folded to plan SSoT). single_core deletion Phase 1+2 DONE (3eadb53 + 7eacb80 + 2baf0f6). NEXT = Phase 3 (engine_mode registry-row delete + TD-185 fold)."
plan_type: refactor
deletion_scope: major   # single_core removal + cfg-key HARD-REFUSE clean-break — deletion-class. The 7-agent + 8-agent gates already ran this dive (B14/B15 satisfied; see plan §Gate-synthesis + §State-audit-refinements).
sprint_end_goal: make the codebase maintainable + live-ready (v5.X professionalization); HARD live-enable gated behind the whole E-series (E.1.3 ∧ E.1.5)
ship_end_goal: "A fully-renamed Core→Node tree on FROZEN layout (no Core*/MAX_EXECUTION_CORES/CoreContext/state.cores/core_id/per_core_/FOREACH_PER_CORE_CFG_FIELD residue — modulo the PRESERVE-list), the cfg parser HARD-REFUSING legacy core_* AND deleted global keys (clean-break, NOT alias), single_core gone, controller_test green + determinism golden byte-IDENTICAL to E.1.0, and the rename-cascade tool built for E.1.2 to reuse."
predecessor_handoff: handoffs/2026-06-17-E.1.1-MID-DIVE-phase2-next-handoff.md (SUPERSEDED — carried MID-DIVE→Phase-2; this doc carries Phase-2-DONE→Phase-3)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (D-239 = the E.1.1 dive resolutions; governing predecessors D-27 / D-61 / D-219 / D-223 / D-131 / H21)
plan_body: plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md — **READ BOTH SSoT sections:** §"Gate synthesis — amendments + corrected dive scope" (the 7-agent dive SSoT) + §"State-audit refinements (2026-06-20)" (the 8-agent sweep delta; SUPERSEDES specific claims above it)
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
dependency_graph: plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md
guard_matrix: plans/v5.15-live-readiness/E-guard-coverage-matrix.md
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, the E.1.1 plan body §"Gate synthesis" + §"State-audit refinements (2026-06-20)", decision-log D-239, DOCS/DESIGN_PHILOSOPHY.md §15 Glossary, DESIGN_SPECS/refactor-patterns/rename-ship-methodology.md, the Class-36 section in DOCS/RECURRING_BUG_PATTERNS.md, DOCS/STRATEGY_AND_CODING_RULES.md, plans/_cross-cutting/2026-05-06-latency-path-discipline.md, the .E dependency-graph DAG, regen CODE_MAP at pickup]
engine_head: 2baf0f6 (Phase-2 include-prune; branch feat/v5.15-live-readiness; tree CLEAN; PUSHED — origin==local at 2baf0f6. Dive commits: 2b8bd6c helpers + 3eadb53 Phase1 + 7eacb80 Phase2-cut + 2baf0f6 include-prune, all on origin)
workspace_head: 6f825a1 pushed (this session: PARITY-009..018 closure + TD-209 + plan §State-audit-refinements + handoff/MASTER updates + CODE_MAP regen). A further /sync-workspace at THIS handoff's close backs up this doc + the supersede flip.
coding_status: mid-coding-checkpoint — single_core deletion Phase 1 (3eadb53) + Phase 2 (cut 7eacb80 + include-prune 2baf0f6) DONE, ALL GATES GREEN (3635/0 ANSI+GUI · fresh asan/ubsan 3635/0 · determinism golden byte-identical · conformance + calls_graph clean · doc-CI clean); NEXT = Phase 3 (engine_mode registry-row delete + readers + parity_harness retire + tools cohort + fold/close TD-185); then TD-175 cascade tool + Core→Node rename + cfg clean-break + C-2 guard + V-class
---

# Handoff — `E.1.1` resume at Phase 3 (the `engine_mode` registry-row delete)

## TL;DR (the 60-second pickup)

- **E.1.1 is MID-DIVE, deep in.** The `single_core` 3-phase deletion: **Phase 1** (orphan `LegacyReferenceDriver`, `3eadb53`) + **Phase 2** (legacy single-threaded engine cut `7eacb80` + dead-`#include` prune `2baf0f6`) are **DONE, PUSHED, ALL GREEN**.
- **NEXT = Phase 3** — the FINAL `single_core`-deletion leaf (atomic): delete the `engine_mode` field (via its **registry row**, H17) + readers + parser + enums, **retire** parity_harness, and **fold/close TD-185**.
- **THE DIVE SSoT** = the plan body §"Gate synthesis" + §"State-audit refinements (2026-06-20)". Read both FIRST — the State-audit section carries 2 Phase-3 corrections that save a wasted cycle.
- An **8-agent adversarial state-audit** (2026-06-20) re-verified the whole dive green + the plan accurate, and folded refinements into the plan.
- **Guard posture (operator: "keep going just use ci guards"):** rides under `.E.1.0`'s standing gates (conformance + determinism golden + pre-commit M/H/K/F/N + calls_graph); micro-commits, each guard-verified. A slip RED-builds on engine source.

## The ideal way to start

1. **`/accept-handoff`** (resolves here). Regens CODE_MAP + loads the baseline + the E.1.1 plan body (both SSoT sections) + D-239 + the DAG + the guard-matrix + SUBAGENT_ARMING.
2. **Confirm state:** `git -C /home/caramel/code/FoxML_Trader_v2 log --oneline -5` → `2baf0f6` → `7eacb80` → `3eadb53` → `2b8bd6c` → `90b2daf` (.E.1.0 tag). `git status` CLEAN. `./build.sh test` 3635/0. `python3 tools/check_latency_path_conformance.py` green.
3. **Read** the plan §"Gate synthesis" + §"State-audit refinements (2026-06-20)" (the SSoT) + D-239.
4. **Resume Phase 3** (below) — atomic, guard-driven, micro-commits.

## TaskList state at handoff write (recreate verbatim at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | **in_progress (THE SHIP)** | E.1.1 — Core→Node rename + cfg-key clean-break + single_core LIVE deletion |
| #2 | pending | E.1.2 — NodeState SoA / Money relayout (successor leaf) |
| #3 | pending | #12 — Subagent registry i/a/v/d/c: discoverability pointer + DESIGN_SPEC remainder |
| #4 | pending | TECH_DEBT-204 — .githooks not mirrored by /sync-workspace |
| #5 | pending | #11 — Umbrella sweep finish: stale size comments + preventive pins + wire size tool |
| #6 | pending | #15 [HIGH] — trade-branch → branchless (H20) optimization leaf (D-236) |
| #7 | pending | #3 — grep-CI aggregator at ≥2 gates (re-confirm vs the static ASM gate) |
| #8 | **completed** | Node→CPU legibility helpers (committed 2b8bd6c) |

**Recreate via TaskCreate.** #1 is the live ship; #8 is done; #2 is the successor.

## Per-phase status — the single_core 3-phase deletion + the rest of E.1.1

| Phase | Status | Notes |
|---|---|---|
| Pre-coding (7-agent gate RED→amended + 8-agent state-audit) | **DONE** | both folded to the plan SSoT |
| node→CPU legibility helpers | **DONE** | `2b8bd6c` (behavior-preserving; outside the bisect) |
| Phase 1 — orphan `LegacyReferenceDriver` delete | **DONE** | `3eadb53` |
| Phase 2 — legacy single-threaded engine cut + unconditional dispatch | **DONE** | `7eacb80` (1116 lines; clean excision, zero orphans) |
| Phase 2 tail — dead-`#include` prune (9) | **DONE** | `2baf0f6` |
| **Phase 3 — `engine_mode` registry-row delete + readers + parity_harness retire** | **NEXT** | atomic; H17 |
| TD-175 cascade tool (widen scan to `tools/`+`build.sh`+`.githooks/`) | pending | BEFORE the bulk rename |
| The Core→Node rename | pending | cascade-tool-driven; PRESERVE-list is the exclusion set |
| The cfg clean-break (HARD-REFUSE) | pending | refuse at the OUTER block fall-through `ControllerConfig.hpp:3099`, NOT `~:3097` |
| C-2 mirror parity-guard | pending | `check_gui_engine_cfg_key_parity.py` + delete stale GUI `confidence_freshness_tau` row |
| V-class verify | pending | build + char-tests + fresh asan/ubsan + calls_graph + determinism golden byte-IDENTICAL + the new parity guard + **the `node_id` 3-hit allowlist** |

## ★ RESUME POINT — Phase 3 (the `engine_mode` registry-row delete; ATOMIC)

The `single_core` path is GONE from `main.cpp` (Phase 2). Phase 3 removes the `engine_mode` field + its machinery + the parity_harness, atomically (one commit). 8-agent-verified scope + **2 corrections** (these save a wasted cycle — read them):

**THE ROOT EDIT (H17):** delete the `engine_mode` row at **`CfgFieldRegistry.hpp:393`** (it's in `FOREACH_GLOBAL_CFG_FIELD`). This **AUTO-DELETES** the `cfg.engine_mode` struct field (it's X-macro-generated) → the compiler then **cascades a build error at every `.engine_mode` site**. ✗ **DO NOT hunt for a "field-count `static_assert` to decrement"** — the size-assert at `CfgFieldRegistry.hpp:1053` auto-derives BOTH sides from the X-macro; it stays valid with NO manual edit (8-agent correction; the original ② re-scope's "decrement any field-count assert" is a NON-EDIT).

**THE CASCADE SITES** (the compiler flags each; here's the map):
- `ControllerConfig.hpp:49-50` — both `ENGINE_MODE_*` enum constants.
- `ControllerConfig.hpp:2863-2868` — the `engine_mode` parser branch.
- **`ControllerConfig.hpp:1860`** — `cfg.engine_mode = ENGINE_MODE_SHARDED;` in `ControllerConfig_Default()` (+ its `:1853-1859` comment) [**8-agent ADD** — un-named in the original ②; build-caught].
- `Async.hpp:325` — sharded reload reader (excise the `engine_mode` line).
- `PortfolioController._HotReload:1968-81` — reader (excise the `engine_mode` lines; **do NOT collateral-delete the sister `num_execution_cores`**).
- `controller_test.cpp:1624` — drop `engine_mode` from the `HAS_SIDE_EFFECT` aggregate.

**parity_harness.cpp: RETIRE, not repoint** (8-agent correction) — `engine_mode` is already written-but-never-read (`Backtest_Run` unconditionally calls `BacktestSharded_Run` TODAY), so "repoint to sharded-vs-sharded" is a no-op tautology. Retire the harness (or excise its single_core↔sharded diff).

**KEEP (survives):** `ControllerConfig_IsLiveCapital` predicate (7 sharded callers: `ShardedSnapshot.hpp:345`, `Async.hpp:565`, `LiveReadiness.hpp:199/250`, `Run.hpp:509/593`, `EngineTUI.hpp:1671`) · `PortfolioController` the STRUCT (tests/backtest/EngineTUI/MetricsLog/ShardedSnapshotPersist — its deeper central-drainer removal = **E.1.3**, the non-atomic TD-002 half) · `num_execution_cores` (59 live refs; separate H22 node-count field).

**FOLD/CLOSE TD-185** — the `single_core` `*_ExitAdjust` family + `original_tp` back-door dead-code cohort (in Strategies/ExecutionCore, NOT main.cpp) — **subsumed** by the single_core deletion. Delete it here + move TD-185 → `closed.md`. (Surfaced again by the pre-commit Check on `7eacb80`/`2baf0f6` overlapping main.cpp.)

**The GUI-block tail (folded from Phase 2):** `main.cpp`'s dead `#ifdef USE_IMGUI_GUI` CandleAccumulator/GuiThread include block + the stale `:64` SDL-locale comment (grep-confirmed dead) — prune here OR with the rename (whichever; re-verify `build_gui`).

**tools cohort:** add `scan_class_44_cfg_orphan.py` to the §HIGH tools-cohort (its `DEAD_FILES` rationale references single_core).

**Phase-3 done-check:** `build.sh test` 3635/0 + `build_gui` green + `calls_graph_diff` CLEAN + conformance green + determinism golden byte-IDENTICAL + no `engine_mode` residue (grep) + TD-185 closed. Commit: `wip(.E.1.1): Phase 3 — delete engine_mode field/registry-row/enums/readers + retire parity_harness + close TD-185`.

## THEN (still E.1.1, after Phase 3)

- **TD-175 cascade tool** — build the rename-consumer-enumeration on `check_struct_alignment.py`; **WIDEN its scan to `tools/`+`build.sh`+`.githooks/`** (the §HIGH coverage gap: a rename in the apparatus commits GREEN today — no cascade tool/done-grep is wired). Co-migrate `check_identifier_retirement.py` ledger (CORE_STATE_FLAG_* via `--update`) + `check_doc_rename_classification.py` KEEP_TOKENS.
- **The Core→Node rename** — cascade-tool-driven. **PRESERVE-list (exclusions):** `ExecutionCore`/`CoreFrameworks`/`MULTICORE`/**`FoxML_Core`** (8-agent: elevate to PRIMARY) + the **`experiments/per_core_sharding/`** path (8-agent: tool MUST exclude it — real on-disk dir + a live `ExecutionCore.hpp:312` path comment) + CPU-hardware "core" (pinning/SMT/`cpu_id`). Anchor each family longest-first (RBP Class 36 non-idempotency; `CoreContext` ⊂ `CoreContextDisplayMeta`). **`node_id` collision-check is STALE:** the 3 `node_id` hits (`Run.hpp:202/213`) are this plan's OWN helper commit `2b8bd6c` — the intended target namespace, NOT a foreign collision → V-class needs a 3-hit allowlist. Per-site human classification: `EngineTUI.hpp:1331/1340` ("controller core" / "resetting core 0 … core 3").
- **The cfg clean-break** — HARD-REFUSE at the OUTER `:2968`-block fall-through (**`ControllerConfig.hpp:3099`**, NOT `~:3097` [C-1: `~:3097` is inside the valid-index block, misses bad-index]) + the 8 recognized-suffix branches refuse on out-of-range idx + the GUI `SettingsPanel.hpp:922` SECOND parser in lockstep + the **global-key refuse** for `engine_mode=`/`num_execution_cores=` (NOT optional — else silent sharded-LIVE / silent node-count revert).
- **C-2 mirror parity-guard** — NEW `tools/check_gui_engine_cfg_key_parity.py` (GUI-writer-key ⊆ engine-parser-key) + DELETE the stale `confidence_freshness_tau` GUI row (`SettingsPanel.hpp:669`); the engine deleted that override (`ControllerConfig.hpp:172`) so post-HARD-REFUSE the GUI would brick its own `engine.cfg`. **Cross-link:** this is the same key PARITY-006's freshness_tau fix renames — coordinate.
- **V-class verify** before the final commit (+ the `node_id` 3-hit allowlist).

## Arming for the next action (Phase 3)

For **Phase 3 (the `engine_mode` registry-row delete)** — surface is cfg-registry + readers + a test:
- **Route to:** guard-driven micro-commits (no judgment-skill needed for the deletion itself). For confidence on the registry-row delete's blast-radius: `/dependency-chain-trace chain:engine_mode` (write/read sites) + `/trace-deps` (already run at the gate; re-fire only if scope shifts). For TD-185's `*_ExitAdjust` removal: confirm dead via `/dead-code-trace` or a grep before deleting.
- **RUN (not just read):** `./build.sh test` (expect 3635/0) + `./build.sh gui` (the GUI-block tail) + `python3 tools/check_latency_path_conformance.py` + `bash tools/calls_graph_diff.sh` + `tools/check_determinism.sh` (golden byte-IDENTICAL) + `tools/run_sanitizer_suite.sh` (fresh asan/ubsan) after the cut; regen `./tools/gen_code_map.sh`.
- **Read first:** the plan §"Gate synthesis" + §"State-audit refinements" (SSoT) · `DOCS/STRATEGY_AND_CODING_RULES.md` (H17 registry discipline) · `DESIGN_SPECS/refactor-patterns/rename-ship-methodology.md` (for the later rename).
- **Invariants in play:** H17 (`engine_mode` is a registry row, not a manual field — the row delete IS the edit) · H21 (no persisted-wire identifier touched; `engine_mode` verified not-persisted/wire per D-239 ②) · H22 (`num_execution_cores` is the surviving node-count field — don't collateral-delete) · H7/H8/H10/H14/H20 (standing gates the edit keeps green).
- **⚠️ ENV NOTE:** dispatched **subagents are hard-denied on `g++`/compiler invocation** in this environment (the build-verify subagent this session couldn't compile; the MAIN agent CAN). So **run build/sanitizer/conformance gates from the main agent**, not a dispatched subagent.

## Capture-completeness (this session — nothing lost)

- **Decisions → log:** NO new decision this session — Phase 2 execution + housekeeping created no operator-steered decision; **D-239** governs (already logged). The 2 Phase-3 corrections (non-edit `static_assert`; parity_harness retire-not-repoint) + the rename refinements are FINDINGS from the 8-agent state-audit, folded into the plan §"State-audit refinements (2026-06-20)" — that IS their home (E.1.1-scope, not separate D-entries).
- **Findings → ledger:** 8-agent state-audit findings → plan §State-audit-refinements ✓. **PARITY-009..018 CLOSED** (code-grounded batch-closure entry in PARITY_ISSUES.md audit-log: `PostLoadSetup` shipped + `bandit_algorithm` stamp-binding + audit-log resolutions) ✓. **TD-209 opened** (V-class `/verify-implementation` skill unhomed) ✓. **TD-185 classified** subsumed → closes in Phase 3 (noted in RESUME POINT). NO new TECH_DEBT beyond TD-209.
- **Memories/skills → indexed:** NONE new this session — the session's discipline applications (verify-before-ledger-flip; preview-before-`sed` on capital code; subagent-compiler-permission quirk) reinforced existing memories (`feedback_tag_disposition_at_fix_time`, `feedback_golden_master_over_reimplemented_oracle`, `feedback_adversarial_framing_default_for_checks`), not new ones. NO new skill.
- **State SSoT updated:** MASTER banner flipped (Phase 2 + prune landed, 2026-06-20); prior active handoff (2026-06-17 phase2) → `superseded`; this doc → `active` (singleton invariant held). Engine PUSHED (`2baf0f6`); workspace PUSHED (`6f825a1`).
- **Verified:** `check_session_docs.sh` SWEEP CLEAN (12 HARD + 4 ADV) at close; ALL engine gates green (3635/0 ANSI+GUI · fresh asan/ubsan 3635/0 · determinism golden byte-identical · conformance + calls_graph clean).

---

## Paste this prompt into a fresh Claude Code session to resume E.1.1 at Phase 3

```
I'm resuming E.1.1 (Core→Node rename + cfg-key CLEAN-BREAK + single_core LIVE deletion) for the v5.15-live-readiness sprint. It is MID-DIVE — pre-coding is DONE (7-agent gate + 8-agent state-audit, folded to the plan) and the single_core deletion Phase 1+2 are DONE + PUSHED (2b8bd6c helpers + 3eadb53 Phase1 + 7eacb80 Phase2-cut + 2baf0f6 include-prune; HEAD 2baf0f6, all green).

Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-20-E.1.1-phase3-next-handoff.md

This is a fresh context window; do NOT trust prior-session memory — verify against current code (HEAD should be 2baf0f6).

The DIVE SSoT is the E.1.1 plan body §"Gate synthesis" + §"State-audit refinements (2026-06-20)" — read both + D-239 first.

NEXT ACTION = Phase 3 (atomic, the FINAL single_core leaf): delete the engine_mode field via its REGISTRY ROW at CfgFieldRegistry.hpp:393 (H17 — the row delete auto-deletes the struct field + cascades; the "decrement field-count static_assert" is a NON-EDIT, auto-derived) + both ENGINE_MODE_* enums (ControllerConfig.hpp:49-50) + the parser branch (:2863-2868) + ControllerConfig.hpp:1860 (_Default factory) + the 2 readers (Async.hpp:325, PortfolioController._HotReload:1968-81 — do NOT collateral-delete num_execution_cores) + controller_test.cpp:1624 + RETIRE parity_harness (not repoint — engine_mode already written-but-never-read) + fold/close TD-185 (single_core *_ExitAdjust dead-code) + add scan_class_44 to the tools cohort. KEEP ControllerConfig_IsLiveCapital (7 sharded callers) + PortfolioController the struct. Then build.sh test + build_gui + calls_graph + conformance + determinism + fresh sanitizers → commit.

Guard posture (operator directive): "keep going just use ci guards" — micro-commits compile-gated; a slip RED-builds on engine source. ENV NOTE: run build/compiler gates from the MAIN agent (dispatched subagents are hard-denied on g++ here).

Honor D-239: ① file-renames in-scope · ② single_core clean-delete NO tombstone · ③ gate-scope _PER_CORE→_PER_NODE · ExecutionCore PRESERVE. After Phase 3: TD-175 cascade tool (widen to tools/) → the Core→Node rename (PRESERVE-list incl. FoxML_Core + experiments/per_core_sharding/; node_id 3-hit allowlist for V-class) → cfg HARD-REFUSE at ControllerConfig.hpp:3099 (NOT ~:3097) + C-2 GUI parity-guard → V-class.
```

---

## Quick links

- This ship's plan (the DIVE SSoT): `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md` (§"Gate synthesis" + §"State-audit refinements (2026-06-20)")
- Decision log: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-239 + D-27/D-61/D-219/D-223/D-131)
- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md` (top banner: the 2026-06-20 Phase-2 UPDATE)
- Predecessor handoff (superseded): `plans/v5.15-live-readiness/handoffs/2026-06-17-E.1.1-MID-DIVE-phase2-next-handoff.md`
- Successor: `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md` (INBOUND: node→CPU legibility-in-NodeState forward-promise)
- Terminology SSoT: `DOCS/DESIGN_PHILOSOPHY.md` §15 Glossary (Cluster > Node[2 CPUs] > CPU core; ExecutionCore PRESERVE)
- Rename methodology: `DESIGN_SPECS/refactor-patterns/rename-ship-methodology.md` · RBP Class 36 (rename non-idempotency) in `DOCS/RECURRING_BUG_PATTERNS.md`
- Standing gate the edit must keep green: `tools/check_latency_path_conformance.py`
- Dependency DAG: `plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md` · Guard matrix: `plans/v5.15-live-readiness/E-guard-coverage-matrix.md`
