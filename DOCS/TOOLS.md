# TOOLS.md — `tools/` inventory + disposition registry

**Every `tools/*.{sh,py}` MUST have a row here.** Enforced by `tools/check_tools_inventory.py` (CI guard; a new tool without a row = red build — the canonical-sister of `check_meta_registry.py` for code registries). This closes the rot class: a tool that exists but isn't invoked/dispositioned (the `gen_code_map`/`CODE_MAP.md` failure — built, never wired, silently stale) can no longer hide. Codified `.E` Session-7 (D-134/D-135/D-136).

**Re-audit:** `/tools-audit` (quarterly cadence; sister `/anti-spaghetti` + `/metadata-audit`) — drift + orphan + spec-coverage + reference-vs-invocation. **Key metric:** *invocation* (a real `bash/python tools/x` call-site), NOT a prose mention — per D-115.

## Disposition taxonomy
| Disposition | Meaning |
|---|---|
| **STANDING-CI** | Run by the pre-commit hook or a CI umbrella (`check_determinism.sh`, `check_session_docs.sh`) — directly or transitively |
| **SKILL-WIRED** | Invoked by ≥1 skill (real call-site in the SKILL.md body) |
| **ONE-OFF-MANUAL** | Operator-run analysis/viz; no wiring expected (no spec required) |
| **TEST-HARNESS** | A test for another tool; run by the test runner |
| **ORPHANED(wire\|retire)** | Exists but nothing invokes it — must be wired or retired |
| **PLANNED** | Tracked future tool — referenced (disclaimed) but not yet built; has a fallback now (off-disk OK) |
| **RETIRED** | Deleted tool — tracked only to account for lingering historical references (off-disk OK) |

## Inventory

| Tool | Disposition | Invoked by | Spec | Purpose |
|---|---|---|---|---|
| `check_determinism.sh` | STANDING-CI | pre-commit (Check F); `run_all_tests.sh`; umbrella | ✓ | `.E.0.1` determinism-net umbrella (runs the FP + locale + `.E.1.0` H10-SIMD gates) |
| `check_fp_determinism.sh` | STANDING-CI | `check_determinism.sh` | — | `.E.0.1` FP-determinism gate |
| `check_locale_determinism.sh` | STANDING-CI | `check_determinism.sh` | ✓ | `.E.0.1` locale-determinism guard |
| `check_h10_simd_determinism.sh` | STANDING-CI | `check_determinism.sh` (gate 4); pre-commit (Check F) | — | `.E.1.0` H10 gate — compiles the REAL AVX-512 kernel `RidgeBlender_BuildCorr` two ways (`-march=skylake-avx512` vs `-mno-avx512f` scalar `#else`) + memcmps; ASSERTS the `__AVX512F__` split so it can't go scalar-vs-scalar (non-vacuous). Harness: `h10_kernel_harness.cpp` |
| `check_h10_simd_determinism_selftest.sh` | TEST-HARNESS | test runner; manual | — | negative selftest for `check_h10` (teeth) — a 1e-12 kernel perturbation MUST be caught + AVX==scalar holds on the real unperturbed run |
| `check_h14_no_bitfield.py` | STANDING-CI | pre-commit (Check M) | — | `.E.1.0` H14 gate — no C++ bitfield syntax (`name : N`); decidable RED gate, 0 hits today; comment/string/ternary/label-safe (statement-boundary match + keyword blocklist). Check M calls it directly; a grep-CI aggregator script lands when H7/H20 joins (≥2 gates) |
| `test_check_h14_no_bitfield.py` | TEST-HARNESS | test runner; manual | — | negative selftest for `check_h14` (teeth) — 7 bitfield forms detected, 17 look-alikes (ternaries incl. multi-line, labels, enum-bases, ctor-inits, comments) correctly ignored |
| `run_sanitizer_suite.sh` | STANDING-CI | ship acceptance (operator / `/ship`) | — | pinned-run-conditions sanitizer gate: `ulimit -s unlimited` + `detect_leaks=0` with documented WHY (TECH_DEBT-161; A.5 found the gate depended on ambient shell state) |
| `run_sanitizer_suite_selftest.sh` | TEST-HARNESS | test runner; manual | — | negative self-test for `run_sanitizer_suite.sh`: RED-on-failing-lane + `[FAIL]`-line surfaced + GREEN-on-pass + RED-on-missing-lane (hermetic stub lanes via `FOXML_SUITE_ROOT`; stubs beside tools, NOT /tmp — noexec-hardened hosts) |
| `run_all_tests.sh` | ONE-OFF-MANUAL | operator + `.E.0.10` fix-ship verification (could be `/ship`-wired) | ✓ (`--selftest`) | UNIFIED test runner — COMPOSES `build.sh test` (C++ suite) + `check_session_docs.sh` (doc/plan CI umbrella) + `calls_graph_diff.sh` (orphan) + `run_sanitizer_suite.sh` (under `--full`) → ONE green/red; "running the tests verifies everything" (TECH_DEBT-176 / D-201). Composes the canonical runners — does NOT reimplement them (Class-21 avoidance) |
| `check_session_docs.sh` | STANDING-CI | hook; close/accept/precoding/readiness; umbrella | ✓ | one-shot doc/plan CI sweep (runs the doc checks below) |
| `setup_hooks.sh` | ONE-OFF-MANUAL | fresh clone / new machine (manual); idempotent | — (self-verifying: fails loud if a hook is missing/broken) | Wires the pre-commit GATE in BOTH repos (D-300/D-301; closes TECH_DEBT-204): sets `core.hooksPath=.githooks` in engine+workspace + creates the engine `.githooks` DIR-symlink → the workspace canonical (matching `tools/`/`plans/`). WHY: the wiring is machine-local (NOT tracked) → a fresh clone silently loses the whole gate (the "workspace gated nothing" bug, D-299). Run once per machine. |
| `check_capture_audit.py` | STANDING-CI | hook; `check_session_docs`; capture/accept-handoff | — | mechanical capture-audit checks 1/4/8 (index / decision-sentinel / skill-linkage) + 13 decision-log completeness (advisory; new-invariant→D-entry create→capture M7 guard) |
| `check_doc_metadata.py` | STANDING-CI | hook; `check_session_docs` (+ **HARD `--reciprocal-supersession` + `--reciprocal-selftest` teeth, (g)-4/D-416**); capture/doc-create/metadata-audit/ship/sync | ✓ (+ `--reciprocal-selftest`) | doc frontmatter bidirectional + index; **(g)-4**: every `supersedes:` answered by `superseded_by:` + superseded status (standalone mode — rc unpolluted by vocab advisories) check. Roots now imported from `foxroots` (E.1.2.B 0.1); re-exports ENGINE/WORKSPACE/MEMORY_DIR/`_resolve_memory_dir` for back-compat. |
| `check_import_from_core.py` | STANDING-CI (wiring pending) | `python3 tools/check_import_from_core.py` (+ `--selftest`); wires into `check_session_docs` | ✓ (`--selftest` — planted roll-own-root + hardcoded-abs REDs, foxroots-importer passes) | **the import-from-core lint (E.1.2.B 0.1 / D-375)** — a `tools/*.py` needing a repo root MUST `from foxroots import …` instead of rolling its own `Path(__file__).parent.parent` or a hardcoded `Path("/home/…")`. Closes the roll-your-own-root straggler CLASS structurally: a NEW roller REDs. **CLASS FULLY MIGRATED 2026-07-20 — the baseline is EMPTY (0 KNOWN-PENDING).** All 7 grandfathered pathlib rollers moved to `from foxroots import ENGINE`, and the lint was widened the same day to the `os.path` spelling it had been blind to (4 more tools, **3 of them measurably broken** through the `tools/` symlink — including `check_latency_path_conformance`, a HARD pre-commit gate, and `check_identifier_retirement`, the H21 guard). An empty baseline is the proof the class is closed rather than merely tracked. Dogfoods — imports foxroots for its own root. |
| `check_toolio_kind_parity.py` | STANDING-CI | `check_session_docs` (HARD live + `--selftest` teeth rows, 2026-08-12) | ✓ (`--selftest`, 6 teeth: planted missing-consumer / plugin-ghost / stale-exempt all FIRE; mutilated-Lua block + missing-json = rc-2 REFUSAL, never an empty-set pass) | **The TD-258 repo-floor tooth** — toolio payload kinds (`lib/toolio_schemas.json`, D-380/D-384) ⇄ the plugin's consumed-kind registry (`toolio_kinds.lua` `M.consumed`/`M.exempt`): a NEW producer kind with no plugin surface REDs the floor instead of waiting for someone to run `:checkhealth`; the exempt tier is the producer-first escape hatch (reason required) and is itself parity-checked (stale exemptions flag). Strict Lua block parse (one `["kind/N"]` key per line, closing `}` at column 0) — a reshaped table is a NAMED refusal (Class 57), rc 0/1/2 = ok/drift/refusal. Roots from `foxroots`. Sister layers: runtime `assert_consumed` at both plugin decode seams + the plugin suite tooth. |
| `check_toolio_kind_parity_selftest.sh` | STANDING-CI (D-137 wrapper) | `check_session_docs` teeth row; manual | ✓ (IS the negative self-test — wraps `check_toolio_kind_parity.py --selftest`, rc-checked) | The D-137 enrollment-visible artifact for the TD-258 floor tooth: proves the gate FIRES on planted drift and REFUSES (rc 2) on mutilated inputs rather than empty-passing. |
| `compile_command.py` | PRODUCER (terminal + the plugin 0.5 asm/layout cards) + STANDING-CI teeth | operator/terminal (`python3 tools/compile_command.py <file>…` → pasteable `(cd … && <cmd>)` lines); `--json` → ONE `compile_command/1` envelope; `check_session_docs` runs `--selftest` (teeth row, 2026-08-12) | ✓ (`--selftest`, 7 teeth: verbatim RESOLVED · honest MISSING · relative-key equivalence · **multi-target TU emits one row per entry** (engine/engine_gui/suite flag sets never silently collapse) · undecodable/missing db = rc-2 REFUSAL expect_red · envelope round-trip) | **The ONE compile-command source (TD-257 substrate; ideas §2; D-397 fact-source, D-337 one-core-N-consumers applied to compiler invocations).** Resolves the EXACT invocation(s) `compile_commands.json` records for a file — discovered at the engine-root symlink (today → `build_clangd/`; the shipping-build db rides the build.sh 1:1-asm leaf, cutover = `--db`, not a rewrite). Tri-state: MISSING row for a no-entry file (header→enclosing-TU mapping = 0.5 design work, never improvised); db unreadable = REFUSAL (Class 57). Plugin side: `compile_command/1` rides `toolio_kinds.lua` M.exempt (producer-first) until the 0.5 card lands. Roots from `foxroots`. |
| `citable_ids.py` | LIBRARY (imported) + runnable | imported by `check_capture_audit` Check 4 + Check 14; `python3 tools/citable_ids.py` prints the defining-form index; **`--resolve <relpath>…` emits the `cited_path/1` toolio envelope** (batch tri-state RESOLVED/RENAMED/MISSING; rc 0 includes MISSING rows — a dead link is a fact; the 0.4 doc-viewer dead-link seam, D-417); **`--where <id>…` emits `defining_site/1`** (citable id → ACTIVE defining site(s) FOUND file:line / honest MISSING; citation-form→key mapping data-driven from the namespace spec incl. the PARITY/CLASS spec-vs-citation mismatches; the [REFERENCE] doc-viewer lookup consumed by `fox-symdeps` docview.lua) | ✓ STANDING-CI **`--selftest`**, wired HARD in `check_session_docs.sh` (C5, 2026-07-20) — ~27 both-direction cases over synthetic fixtures: by-definition-not-by-mention, zero-pad equivalence, the grandfathered `-175a` suffix (and its uppercase/Unicode rejections), block boundaries incl. the F-4 suffixed-sibling over-run and the field-anchor backward walk, multi-site cardinality, and registry-fatal-not-fallback. Plus the older `__main__` non-vacuity (exits 2 under 50 ids — a broken SSoT path, not a clean corpus). **Before C5 the `--selftest` flag was ACCEPTED AND IGNORED** (no argparse; it and any bogus flag printed the index and exited 0) — a vacuity flag that was itself vacuous, in the module whose whole job is membership. | **The ONE resolver for every global citable-ID namespace (H · M · T · AR/WH/PL/CP · B · Class · TECH_DEBT · PARITY · D/C/F), BY DEFINING FORM.** Namespace spec lives in DATA (`lib/citable_id_namespaces.json`) and is REGEX-FREE by construction — anchor kind + literal prefix + flags, matched with `startswith` + a digit scan — so `foxtag`'s hand-parser can read it (D-393 pt 4: a shared file carrying regexes CREATES parity risk). **Defining-form, never by-mention:** the predecessor built TECH_DEBT/CLASS membership from raw text, so a set that admits anything it has SEEN could not go red — Class-51 inside the resolver, and why CI never saw D-389's 8 dangling citations. ONE RESOLVER, THREE CONSUMERS (CI gate · tag system · the `0.4` plugin doc-viewer — its `--resolve` envelope surface LANDED 2026-08-10, selftest leg T6 + live tri-state smoke). D-399. |
| `mine_reference_tags.py` | OPERATOR-INVOKED WRITER (D-374 flag-not-auto: dry-run DEFAULT, `--fix` writes, the git diff is the review surface; NEVER hook-wired) | operator, at `[REFERENCE]`-coverage passes (0.4) | ✓ `--selftest` (9 teeth: prose-mining · dead-id drop · tag-line exclusion · hand-written-first merge · banner insert · innermost attribution · idempotency · fenced-subcat · validator-grammar) | **MINE, NEVER INVENT** (the D-413/D-414 law on the reference axis): lifts EXISTING in-span id mentions (`per D-142` / `H4` / `Class 51` / `TECH_DEBT-N` / `PARITY-N`) into `[REFERENCE]_[SUBCAT]_[…]` tags — additive merge (a hand-written id is never removed/reordered), every id validated against `check_code_tag_blocks.load_reference_index` (write-side shares the VALIDATOR's core; TD-270 tracks converging the viewer's `--where` core to 1:1). Subcat scope = fenced+indexed only (INVARIANT/DECISION/TECH_DEBT/CLASS/PARITY — unfenced = vocab red, index-less = unverifiable, both excluded BY DESIGN). Enables the docview surface corpus-wide. Maiden dry-run 2026-08-10: 418 ids / 177 units / 77 files; 13 unresolved mentions excluded. |
| `check_close_out_completeness.py` | STANDING-CI (ADVISORY) | `check_session_docs` (window-scoped); `/close-session` | ✓ (`--selftest`, 5 teeth incl. an existence check on every surface path) | **M8 step-5 / TECH_DEBT-250 — the close-out's JUDGMENT half, finally gated.** For a commit window, asserts each auto-write surface (`tools/CLAUDE.md` · `FEATURE_LOOKUP.md` · `LANDMINES.md` · `PARITY_ISSUES.md` · `tech-debt/open.md`) received at least one commit, or an explicit `--explain '<path>=<reason>'`. **Built because the class recurred TWICE consecutively** — a 22-commit session shipped with ZERO commits to four owed ledgers while the mechanical sweep stayed green throughout, because none of them is mechanically gated. Both times the only trigger was OPERATOR PUSHBACK, which is M7's definition of a discipline that is remembered rather than enforced. **M10:** TOTAL for *was the surface touched*, PARTIAL for *was the entry right* — a green means the surfaces were touched, never that the capture was complete. ADVISORY by design: it fires on a window, so a HARD wiring would red every mid-session run. |
| `check_close_out_completeness_selftest.sh` | STANDING-CI | hook/`check_session_docs` (via the tool's `--selftest`) | ✓ (IS the selftest) | D-137 discoverable negative self-test — proves an UNTOUCHED auto-write surface goes RED and that `--explain` suppresses it. Anchored on a SYNTHETIC path, deliberately: anchoring it to a live ledger would make it pass or fail on whether that ledger happened to be touched recently, which is the live-value anchoring that left the H21 version-decrease tooth dead for an unknown period. |
| `check_import_from_core_selftest.sh` | STANDING-CI | hook/`check_session_docs` (via the tool's `--selftest`) | ✓ (IS the selftest) | D-137 discoverable non-vacuity wrapper for `check_import_from_core` — proves it FLAGS a planted roll-own-root + hardcoded-absolute and PASSES a foxroots-importer. |
| `foxroots.py` | LIBRARY (imported) + runnable | imported by `check_doc_metadata` (+ every root-consuming tool, migrating in E.1.2.B 0.1); `python3 tools/foxroots.py` introspects/smoke-tests | ✓ (`__main__` non-vacuity — exits 2 if the engine marker `Version.hpp` is absent, i.e. mis-resolved) | **the ONE machine-portable repo-root resolver (SSoT; E.1.2.B 0.1 / D-375).** ENGINE/WORKSPACE/MEMORY_DIR + `_resolve_memory_dir`, extracted verbatim from `check_doc_metadata` (env-override → sibling-default → `Version.hpp` shape-check + sibling recovery; the `.absolute()`-not-`.resolve()` Landmine-5 caveat carries over). Closes the roll-your-own-root straggler class + the portability-dead hardcoded-absolute gates (`check_conversion_completeness` migrated; `rebuild_doc_indexes`/`subdivide_design_specs` pending). The import-from-core lint (0.1) REDs any tool that rolls its own root instead of importing this. |
| `toolio.py` | LIBRARY (imported) + runnable | imported by producers/gates emitting the tool-I/O envelope (E.1.2.B 0.1.5+); `tools/toolio_selftest.sh` (`--selftest`); `python3 tools/toolio.py` prints the registry | ✓ (`--selftest` — 1 positive + 3 negative controls: good payload+finding accepted [schema sourced from registry]; bad payload-row arity / bad findings-row arity / unknown table each RED) | **the standardized tool-I/O envelope emit/read + schema-validate SSoT (E.1.2.B 0.1.5 / D-376/D-382/D-384).** `emit()`/`read()` over the language-neutral `lib/toolio_schemas.json` registry (a payload `{tables}` per kind + the cross-cutting envelope-level `findings/1`), read never-hardcoded; the foxtag C++ core carries a parity-honest twin (the `Version.hpp` model, D-382). Imports `foxroots` for its root. |
| `toolio_selftest.sh` | STANDING-CI | hook / `check_session_docs` (via the tool's `--selftest`) | ✓ (IS the selftest) | D-137 discoverable non-vacuity wrapper for `toolio.py` — plants a bad payload row + a bad findings row + an unknown table (each REDs) and a known-good envelope (round-trips, schema from the registry). |
| `check_code_tag_blocks.py` | STANDING-CI | `check_session_docs` (wired — full-tree scan + selftest + **A2 `--callgraph-check` ADVISORY + `--callgraph-selftest` HARD teeth, D-414 leaf-4**); `rebuild_doc_indexes` (imports the collector) | ✓ (`--selftest` + `--callgraph-selftest`) | in-code `[CATEGORY]` tag-block validator (the code-side sister of check_doc_metadata; category-set AND `[REFERENCE]`-subcat membership DERIVED from the schema SSoT fences — fold a row there = tracked here, zero edits; reuses its vocab loader; checks category-validity / `[END_*]` closer-matching for the FULL locked unit model incl. ENUM/TYPE/STRATEGY / `[TAG]` vocab / one-category-per-line / `[REFERENCE]`-resolution incl. PARITY, SOURCE+URL+AUDIT existence-unchecked / graceful-degrade). Exports `collect_file_tags`/`engine_source_files` (the code-tag-index collector — one grammar, one file-list). Deferred: ladder-order + DERIVED-drift + prose-codegen-lint (P4) |
| `check_code_tag_blocks_selftest.sh` | STANDING-CI | hook (D-137); `check_session_docs` | ✓ (IS the selftest) | D-137 discoverable negative self-test for check_code_tag_blocks — thin wrapper running its `--selftest` (proves RED on each violation class + GREEN on clean per-type blocks) |
| `check_cache_layout_selftest.sh` | STANDING-CI | `check_session_docs` (D-137) | ✓ (IS the selftest) | D-137 discoverable negative self-test for check_cache_layout (STANDING-CI HARD since D-414 leaf-3) — thin wrapper running its `--selftest`: RED on planted straddle/size-drift/partial violations (incl. the partial+size-drift BOTH-fire combo), GREEN on clean/exempt; writer tri-state + ORIENT-ONLY/OR-FOLD arming + exempt-tier parser teeth + baseline shrink/orphan/dormant-exempt pure-helper teeth (AR-8). Sister to check_code_tag_blocks_selftest.sh |
| `update_toolchain.py` | MANUAL (write-side; flag-not-auto — hook-wiring is a CONTRACT VIOLATION) | operator / close-ritual, explicitly invoked only | ✓ (`--selftest`, expect_red repair + exclusion teeth) | **D-374/D-418 ONE-ACTION update orchestrator** — regenerates WRITTEN derived state in dependency order: layout `--isolate --fix` → call-graph (DECLARED-SKIP until the v1 foxtag axis) → **cite auto-repair** (consumes the (g) RENAMED payloads via `resolve_cited_path` over REWRITABLE live docs only — frozen records + decision-logs + evidence cites untouchable) → `rebuild_doc_indexes` → the verify sweep. Idempotent (2nd run = 0-diff, Class 56); `--dry-run` previews. WRITE-side sister of `check_session_docs.sh`; contract = `DOCS/TOOLCHAIN_CONTRACTS.md` §5 |
| `emit_record_layout.lua` | CANDIDATE | (E.1.2.A; called by `check_cache_layout.py` via headless nvim) | — | struct-LAYOUT fact emitter for the cache-layout gate (D-320) — REUSES the fox-symdeps `recordlayout.parse` + `straddlers` + `sizeprobe` flag-extraction (SSoT; no Class-18 / two-fact-cores mirror per D-310) → `{record:{size,align,straddlers}}` JSON via clang `-fdump-record-layouts` (LAYOUT facts only — Itanium-ABI-identical to the shipped g++, D-321; CODEGEN facts stay g++). LAYOUT-flag subset (drops codegen `-O`/`-march`/`-flto`, ABI-irrelevant + trips clang; TECH_DEBT-231); absolute TU; namespace-strip match. **D-421 step 2 — two ADDITIVE changes for COMPLEMENT consumers:** (a) `members:[{name,type,off}]` emitted for explicitly-requested records (the parser always carried `r.fields`; the emitter merely projected it away — straddle asks *which fields cross a line*, a partition guard asks *which fields exist at all*); (b) `--require-all` makes a requested-but-absent record FATAL rc 2. Strictness is OPT-IN on purpose: `check_cache_layout.py` batches ~193 requests of which ~77 are legitimately absent and reports them TU-scope-honestly, and making absence fatal by default converted that gate into "could not run" across all 193 — worse than either pass or fail. Also matches the ns-stripped-TEMPLATE spelling (`NodeContext<64>`), which previously matched NOTHING and returned `[]` with rc 0 — a complement consumer trusting that would have computed over zero members and called a 49-field struct fully covered. |
| `foxtag/` (`foxtag.hpp` + `foxtag_main.cpp` → the `foxtag` binary, gitignored; built by `foxtag/build.sh`) | CANDIDATE | `foxtag/parity_check.sh` (the migration gate); plugin `tagadapter.parse` + CI cutover PENDING parity-gated adoption (D-349) | — (selftest: `foxtag selftest`) | **the D-337 central tag-toolchain CORE, increment 1** — C++ parser + scanner + query engine over `[SCHEMA]_[v1.0]`; grammar DERIVED at runtime from the schema SSoT fences + doc-tag-vocabulary (never hardcoded); mirrors the Python validator line-for-line (BYTE-IDENTICAL violations). CLI: `validate` / `units` / `unit <file> <line>` (the plugin keystone — enclosing block as JSON) / `tags` / `grammar` / `parity-dump` / `layout <tu> [Struct…]` (**2a** — clang record-layout → size/align/straddlers JSON; parity-proven straddler-exact on the 204-record census, D-350) / `codegen --header/--params/--call` (**2b** — g++ probe with the RC-A instantiation anchor → instr / 3-class branches / floats / RC-C SIMD width-class / RC-E never-green; EXACT vs the conformance analyzer's ratchet baseline — Regime_Classify 489/8, D-351) / `selftest` + `codegen-selftest`. ~19ms full-tree vs Python ~147ms. **Docs: `tools/foxtag/README.md`.** Remaining 2c = `foxtag update` + compile-DB (RC-B) + drift-gate |
| `foxtag/build.sh` | CANDIDATE | `foxtag/parity_check.sh`; manual | — | builds the foxtag core CLI (g++ -std=c++20 -Werror; dev-plane, never linked into the engine) |
| `foxtag_client.py` | STANDING-CI (transitively — the consumers below are) | `check_cache_layout.py` (layout backend) · `rebuild_doc_indexes.py` (inventory backend) | ✓ (`foxtag/README.md`) | **the ONE Python↔foxtag-core seam** (D-337 "CI via the Python binding", D-352): binary discovery + subprocess/JSON transport + decode in one place; `core_available()` gates every consumer with a Python fallback (a foxtag-less checkout keeps all gates alive). pybind11 slots in BEHIND this API if in-process speed is ever needed — no consumer changes |
| `foxtag/parity_check.sh` | **STANDING-CI** | **pre-commit Check T** (trigger-scoped to staged toolchain files, wired 2026-07-20 / D-387(3)); still run manually before ANY consumer cutover to the core | ✓ (§1 validity leg + §1b path-arg, both with proven teeth) | **the D-337 migration gate** — golden-master parity vs the Python oracle: byte-identical validate output (header/violations/exit code) + identical unit/tag inventory + identical SSoT grammar counts. PASS = the core may be consumed; FAIL = `check_code_tag_blocks.py` stays authoritative |
| `check_cache_layout.py` | STANDING-CI (HARD `--strict-new` on BOTH TUs — main + suite `foxml_suite.cpp` — in `check_session_docs`; promoted 2026-08-10 D-414 leaf-3, suite row ADV→HARD at the AR-8 hole-9 close [suite polices 13/13 baselined structs vs main's 4/13; `--isolate` rides no standing row — the stated residual]. SHRINK-ONLY baseline `tools/lib/cache_layout_baseline.txt` ENFORCED since AR-8: `--emit-baseline` refuses growth rc 2; `--strict-new` reports orphan keys TU-scope-honestly; dormant `[STRADDLE_EXEMPT]` tags reported advisory) | `check_session_docs` (HARD strict-new ×2) | ✓ (`--selftest`) | **layout backend PARKED (D-415, 2026-08-10; was CUT OVER at D-352):** `--backend {auto,nvim,foxtag}`, auto = the LUA emitter (script-side authority during churn; foxtag = explicit opt-in probe with Lua fallback-on-failure; the v1 re-arm flips auto back) — via `foxtag_client.py`. cache-layout GATE (D-320) — parses converted `[STRUCT]` blocks → joins `emit_record_layout.lua` → GATE (a cache-line straddler on a CROSS-THREAD `[THREAD]` struct = H6 false-sharing → FAIL; a `[SIZE]`-tag drift vs real `sizeof` → FAIL) → TRACE (field + offset + 64B-line + `[THREAD]` context). **Context-aware** — the `[THREAD]` tag decides whether a straddle MATTERS (cross-thread = bug, cold single-thread = benign). **NEVER auto-aligns** (reorder breaks wire/persist H21/H12) — detect + flag + suggest. Refresh-mode (write the tags) + `check_session_docs` HARD-wire pending (needs converted structs = b) |
| `check_conversion_completeness.py` | STANDING-CI | `check_session_docs` (wired HARD — `--selftest` + baseline) | ✓ (`--selftest`) | **conversion COVERAGE gate (E.1.2.A, the planned Next-increment item 3)** — every code unit that WARRANTS a block (schema § Coverage proportionality: a struct is TRIVIAL/exempt at ≤3 POD fields + no method + no `alignas`) actually HAS one, of the right TYPE, sited as its OWN block. **C1** lumped (struct/registry buried in another unit's `[CODE]` — the GateControlNetwork class, invisible to the validator) · **C2** missing block · **C3** struct-w/o-`[DERIVED]`. Template-tolerant (`FixedPoint<2,64>`) + tier-3-aware + count-helper + same-family-sub-registry exempt (policy #1). `--baseline tools/lib/completeness_baseline.txt` grandfathers the existing 49 (shrinks as Phase-C cleanup lands → then HARD-zero); a NEW lumped/missing unit fails immediately. Non-vacuity `--selftest` (ExecutionCore clean / GateControlNetwork flagged / trivial exempt). foxtag cutover pending a `code-units` producer (D-349 contract; Python authoritative meanwhile). |
| `check_conversion_completeness_selftest.sh` | STANDING-CI | hook; `check_session_docs` (via the tool's `--selftest`) | ✓ (IS the selftest) | D-137 discoverable negative self-test for check_conversion_completeness — thin wrapper running its `--selftest` (proves it FLAGS the GateControlNetwork half-conversion + passes ExecutionCore + exempts trivial return-structs + same-family sub-registry variants). |
| `check_schema_version.py` | STANDING-CI | `check_session_docs` (wired HARD) | ✓ (`--selftest`) | **`[SCHEMA]` version COHESION gate (D-371)** — every converted `[SCHEMA]_[ver]` carries the LOCKED version, the value DERIVED from the spec's `Status: LOCKED — [SCHEMA]_[v1.0]` line (SSoT; auto-follows a bump). Catches the `v1`-vs-`v1.0` drift the validator's `[SCHEMA]_[` prefix-gate can't see (found+fixed 2026-07-18). Scans `.hpp`/`.cpp` via the corpus contract's `derived_facts` profile. ⚠️ **It does NOT scan `.py`** — it long claimed to ("tool fixtures too — the drift's origin"), but `ENGINE.rglob("*.py")` yields exactly one vendored file which the vendor filter then drops, so ZERO `.py` were ever scanned; the 61 real tool `.py` live under `tools/`, a DIRECTORY symlink neither walker descends. Claim removed 2026-07-20; the gap is TOMBSTONED as `deferred.python_sources` in `corpus_contract.json` (TECH_DEBT-253). Read its green as covering `.hpp`/`.cpp` ONLY. `[SCHEMA]_[exempt]` opts out; `WIP_EXEMPT` is now empty (RegimeDetector converted 2026-07-18 → whole corpus version-checked, HARD-zero). DELIBERATELY separate from the parity-gated validator (`check_code_tag_blocks`/foxtag) so foxtag↔Python `validate` byte-parity stays intact. Non-vacuity `--selftest`. |
| `check_corpus_membership.py` | STANDING-CI | `check_session_docs` (wired HARD) | ✓ (`--selftest`) | **Corpus MEMBERSHIP pin (D-386/D-396)** — resolves each contract profile's PIN population and diffs it against `tools/goldens/corpus--<profile>.txt`, EXACT including order. The contract states the RULES; this proves they still resolve to the same FILES (different failure surfaces — a rule stays valid while a file silently leaves the corpus and stops being checked). **SCAN ≠ PIN:** the scan is gitignore-blind by design (D-393 pt 2), the pin is git-TRACKED ONLY, because a golden is a committed distributed artifact that must resolve identically on a fresh clone (the first bless pinned 31 untracked entries incl. two mkstemp random-named scratch files → an unconditional 31-line RED on any other machine). **A LIST, not a count:** commit `1da1c1c` moved SIX files' identities with the tracked count going 167→167, delta ZERO. Missing golden = HARD `rc=2`, never "nothing to compare". Re-bless via `--bless` → `bless.py` (TTY-gated, D-394). Deliberately SEPARATE from the parity-gated validator so foxtag↔Python `validate` byte-parity stays intact (the `:39` corollary; same call as D-371). |
| `bless.py` | LIBRARY + runnable (`--selftest`, **`--console`** — the operator BLESS MENU: enumerates every blessable record with per-row drift status, number-pick dispatches to that record's OWN D-394 gate; roster coverage of `tools/goldens/*.txt` selftest-enforced) | imported by `check_corpus_membership`; the intended path for every future golden | ✓ (`--selftest`, 8 teeth incl. all three refusals) | **The ONE re-bless path for every output golden (D-394).** `compare()` → {match, drift, missing}; `bless()` enforces the full contract: a TTY is REQUIRED, the per-file diff is SHOWN along with what the record CURRENTLY holds, a typed confirmation is demanded, non-interactive HARD-REFUSES `rc=2` (fails fast, never blocks on stdin), there is **no `--yes`/`--force`**, and a no-op does NOT write (D-369 stamp-on-change). Exists as ONE shared helper per TECH_DEBT-255's trigger — `check_identifier_retirement --update` currently rewrites the H21 tombstone ledger with no diff and no confirmation, the OPPOSITE posture, which is why -255 existed. **MIGRATED 2026-07-20** — `check_identifier_retirement --update` now routes through here, so the corpus golden and the H21 tombstone ledger share ONE implementation of the D-394 contract and cannot drift into opposite postures again (TECH_DEBT-255 CLOSED). **`--update-budgets` ABSORBED 2026-07-20** — all three committed baselines (corpus goldens · the H21 identifier ledger · the latency ratchet) now share ONE re-bless control. ⚠️ bless.py is the CONTROL, not a claim they are all goldens: a golden asks *is the output still what we blessed*, a ratchet asks *has this metric regressed past its ceiling*, and the taxonomy stays distinct — what they share is that a rewrite must be a deliberate human act. **The property it buys:** per D-385/M10 a delegated agent cannot re-bless a golden through any TOOL path (honest scope per the 2026-08-14 a-class: a raw shell redirect over the file stays physically possible — the triad that keeps a golden a TOTAL oracle is TTY-gated tool paths + the golden being a REVIEWED committed artifact + the symmetric guard red on a golden edited against an unchanged producer). Module tier, NOT `tools/lib/` (C-389: `tools/` = CODE, `tools/lib/` = DATA and a non-recursive-glob guard blind spot). |
| `check_corpus_membership_selftest.sh` | STANDING-CI | hook; `check_session_docs` (via the tool's `--selftest`) | ✓ (IS the selftest) | D-137 discoverable negative self-test for check_corpus_membership — thin wrapper running its `--selftest` (proves a planted ADD / DELETE / count-unchanged RENAME / REORDER are each FLAGGED, an absent golden is a HARD failure not a pass, and the git-tracked PIN filter is non-trivial at SCAN 200 vs PIN 171). |
| `check_schema_version_selftest.sh` | STANDING-CI | hook; `check_session_docs` (via the tool's `--selftest`) | ✓ (IS the selftest) | D-137 discoverable negative self-test for check_schema_version — thin wrapper running its `--selftest` (proves it FLAGS a drifted `[SCHEMA]_[v1]` fixture, passes the locked version, and opts out `[SCHEMA]_[exempt]`). |
| `add_vocab.py` | CANDIDATE | manual (the D-365 one-action; `check_session_docs` covers the index-currency it regens) | ✓ (`--selftest`) | **the D-365 add-vocab ONE-ACTION** — insert a vocab tag into its SSoT + regen the indexes + verify it derives, in one drift-proof command. `--category NAME` → the ` ```category-set ` fence (in-code-documentation-schema.md; UPPER_SNAKE); `--concern`/`--surface TAG --desc` → the doc-tag-vocabulary.md axis table (lower-hyphen). Then `rebuild_doc_indexes` + confirm the tag DERIVES into the validator/vocab + the index-currency `--check` passes — wrapping the multi-step flow that drifts by hand (a forgotten regen → index-currency red, the 2026-07-18 miss). PURE insert helpers (`--selftest`-proven: append + dup-refusal per axis). The surface-PARITY half (plugin `tag_grammar_adapter.lua` ↔ fence, the D-365 drift watch-point) is plugin-lane (#8). |
| `add_vocab_selftest.sh` | CANDIDATE | hook; `check_session_docs` (via the tool's `--selftest`) | ✓ (IS the selftest) | D-137 discoverable negative self-test for add_vocab — thin wrapper running its `--selftest` (proves the insert helpers append/insert + refuse duplicates per axis). |
| `check_register_fit.py` | PRODUCER (the plugin `<leader>dR` card, LIVE 2026-08-14) + manual | `--json --struct <name>` → ONE `register_fit/1` envelope (regfit.lua consumes, assert_consumed-gated); manual text mode | ✓ (`--selftest`) | **the RC-F per-field register-fit / access-cost analyzer (D-366)** — for each field, is it a SINGLE aligned `mov` (size ∈ {1,2,4,8,16,32,64} ∧ off%size==0) or does it cost more (unaligned/split load, or shift+mask for a bit-packed sub-word)? The "40→1 mov" surfacer (Caramel's de Bruijn / `(~A&B)==B`, `Learning_cpp/deep_dives`). Per-field facts via a NEW parity-safe **`foxtag fields`** command (the shared clang record-layout dump + `field_size` resolver — one producer, no mirror; `layout`'s parity JSON untouched, parity_check still PASS). **ADVISORY — never a gate** (the H14 tension: the engine bit-packs DELIBERATELY for cache footprint, so it SHOWS both costs + flags candidates, NEVER auto-unpacks/reorders — H21/H12; operator decides per field). Corpus-wide surfaced 46 unaligned + 319 multi-op fields (`XGBHyperparams.tree_method` 16B@28, `SubmitCommand._pad`@49). Feeds the plugin's per-field card (Caramel's render). Non-vacuity `--selftest`. |
| `check_register_fit_selftest.sh` | CANDIDATE | hook; `check_session_docs` (via the tool's `--selftest`) | ✓ (IS the selftest) | D-137 discoverable negative self-test for check_register_fit — thin wrapper running its `--selftest` (proves access_cost classifies single-mov / unaligned / multi-op / unknown correctly). |
| `node_persist_layout.py` | LIBRARY (imported) + runnable | imported by `check_identifier_retirement.py` (the paired-bump rule + the shared `_macro_body/_rows/_args` FOREACH-parse SSoT — one-direction dependency; fires with Check H / /readiness Check 46 / post-ship-audit); `python3 tools/node_persist_layout.py` diffs the live layout vs the golden (rc 0/1/2 = ok/drift/refusal); `--bless` → bless.py (D-394 TTY); `--emit-initial` first-establishment ONLY (refuses if golden exists); teeth via `node_persist_layout_selftest.sh` (the doc-sweep HARD row) | ✓ (`--selftest`, 21 teeth: planted DROP/ADD/**count-neutral NAME-SWAP** (the triple-vacuity case)/TYPE-change/REORDER/delegate-internal-DROP each fire · unmapped-delegate + missing-macro REFUSE rc2 · real-tree non-vacuity: 29 parent + 17 delegate-internal rows resolve) | **The E.1.2 D-305/D-302 persist-layout audit** — parses `FOREACH_NODE_PERSIST_FIELD` (MemHeaders/NodeCtxPersistRegistry.hpp) + walks INTO the regime/feeder/confidence delegate sub-registries → the flattened ordered wire LISTING (name-inclusive, order-sensitive, symbolic count tokens verbatim), frozen at `tools/goldens/node_persist_layout.txt` (a LIST, never a digest — diffs name rows). Closes the D-208 M7 triple-vacuity: a size-neutral count-neutral row swap that count-locks, size guards, and a same-commit-regenerated byte-golden are all simultaneously blind to. The PAIRED-BUMP rule (layout delta ⟹ SHARDED_SNAPSHOT_VERSION bump same-commit, else RED) lives in `check_identifier_retirement.py` (it owns the version row + the bless flow). **E.1.2/D-421 — the shared parser now (a) expands nested `FOREACH_<NAME>(X)` invocations, resolving `__has_include` guards against the including file's dir the way the preprocessor does, and (b) anchors `X(` on a token boundary. Both were silent UNDER-counts of enrolled registries: the nested form dropped `FOREACH_STRATEGY`'s 5th row (the H21 ledger carried 4 of 5 StrategyId codes for its whole life), and `body.find("X(")` matched the `X(` inside `MAX(` (phantom row; latent — no enrolled registry trips it today). `_macro_body(text, macro, base_dir=None)` — omitting `base_dir` preserves the old first-match behaviour exactly. 4 positive-control teeth, each verified to FAIL against the pre-fix parser.** |
| `node_persist_layout_selftest.sh` | STANDING-CI | hook/`check_session_docs` (HARD row; IS the D-137 negative self-test for `node_persist_layout.py`) | ✓ (IS the selftest) | D-137 discoverable negative self-test wrapper for `node_persist_layout.py` — proves every planted-mutation tooth fires + refusals refuse + the real registry resolves (non-vacuity). |
| `check_reset_before_producer_selftest.sh` | STANDING-CI | `check_session_docs` (HARD row; IS the D-137 negative self-test for `check_reset_before_producer.py`) | ✓ (IS the selftest) | D-137 discoverable negative self-test wrapper — proves the planted producer-before-reset FIRES, the correct order PASSES (the positive control, without which the check could red on everything and still look rigorous), the inverse direction inverts the verdict, a vanished reset OR producer site REFUSES rc 2 rather than empty-passing, and both live rules resolve at HEAD. That last leg is load-bearing: the guard's entire subject is a regex pair, and a pattern that stops matching is indistinguishable from a clean codebase unless absence is fatal. |
| `check_reset_before_producer.py` | STANDING-CI (2 HARD rows in `check_session_docs`: the teeth wrapper + the live gate) | `check_session_docs.sh` · `python3 tools/check_reset_before_producer.py` (rc 0 ok / 1 violation / 2 refusal) | ✓ (`check_reset_before_producer_selftest.sh` — D-137 wrapper) | **A per-pass RESET must sit on the correct side of its PRODUCER (Class 44 sub-shape B).** `strategy_halt_reason = SHALT_OK` sat **59 lines BELOW** the dispatcher that writes it — unconditional, same straight-line block — so the producer's output was discarded every rebuild and **17 of 20 SHALT codes were unobservable from `bc37c62` (2026-04-30) until D-421**. It hid behind partial success: the two codes written *after* the reset survived, so the panel always showed something. The fix commit paired the two lines with comments saying they are not interchangeable — the weakest possible guard for an ORDERING property, and exactly what a "these look redundant, merge them" refactor breaks silently with no test failing. Both close reviews independently named the same remedy: assert reset_line < every producer_line in the same function. Declarative `RULES` rows carry the file/function/reset-regex/producer-regexes/direction, because which order is correct is a design fact a scanner cannot derive. **Scope is deliberately narrow:** source order inside ONE function, no control-flow analysis — sound for the straight-line "reset at the top of each pass" contract and nothing else (M10: PARTIAL by construction, stated so the green is not over-read). A vanished reset OR producer pattern REFUSES rc 2 rather than passing, since a rule that can no longer fail is worse than a violation. **Sister:** `scan_class_44_cfg_orphan.py` explicitly excludes this direction and homes the general form at **TECH_DEBT-175** (the AST struct-field produce/consume tracker) — when that lands, these rules become its regression fixtures, not a second implementation. Its own first live run RED-ed on a mis-encoded rule direction (the author's), not on wrong code. |
| `check_node_ctx_partition.py` | **STANDING-CI** (pre-commit **Check Q**, trigger-scoped; + 2 HARD rows in `check_session_docs`: the teeth wrapper + the live gate). Was CANDIDATE from D-421 step 2 until its stated precondition — the exemption registry — landed at engine `587d44c`; wired in the same breath rather than "later", since a built-but-uninvoked tool is precisely the rot class this inventory exists to close. **Check Q is not redundant with the aggregator row:** Check P fires `check_session_docs` only on staged handoff/MASTER commits, and the failure this guard exists to catch — a field added to `NodeContext<F>` and never enrolled — arrives in an ORDINARY CODE commit that Check P never sees. Check Q triggers on the STRUCT's home (`ControllerEventLoop.hpp`) as well as the two registries, because the dangerous edit is the one that touches the struct and NOT the registry; keying on the registry alone would only catch the commits that already remembered. rc 2 is a FAIL there, never a pass | pre-commit Check Q · `check_session_docs.sh` · `python3 tools/check_node_ctx_partition.py` (rc 0 accounted / 1 finding / 2 refusal) | ✓ (`--selftest`, 17 teeth; D-137 wrapper `check_node_ctx_partition_selftest.sh`) | **The COMPLEMENT guard for the per-node persist partition.** Every other persist guard points rows-FORWARD — the count-lock pins 29 rows, `node_persist_layout.py` freezes the flattened listing, `check_identifier_retirement.py` pins the version — so all three answer *"are the rows we have right?"* and none answers *"are these all the rows there should be?"*. `NodeContext<F>` is HAND-declared (`ControllerEventLoop.hpp:315`), making `FOREACH_NODE_PERSIST_FIELD` a COVERAGE registry over a struct that exists independently of it, so a field added and never enrolled is invisible to every existing check. Subtracts the persist rows + a new `FOREACH_NODE_CTX_PERSIST_EXEMPT` (NAME, CATEGORY, rationale) from the real clang-derived member list (49 members at v11) and REDs in three directions: **UNACCOUNTED** (the `node_gross_wins` v4.7.25 silent-drop class, TECH_DEBT-196), **STALE-EXEMPT** (an exemption naming a non-member — protects nothing while making the partition read as accounted), **CONTRADICTION** (both persisted and exempt — one declaration is false and the tool refuses to guess which). Categories are a closed set phrased as FALSIFIABLE claims, because the `ic.actuals` bug had a stated reason that was documented and false; free-text reasons are exactly how a red gets silenced. Deliberately does NOT judge whether a reason is TRUE (a code-reading judgment; mechanizing it would manufacture the same false confidence) nor compute bytes (that is `node_persist_layout.py` + the byte-golden). |
| `check_node_ctx_partition_selftest.sh` | STANDING-CI (D-137 wrapper) | `check_session_docs` teeth row; manual | ✓ (IS the negative self-test — wraps `check_node_ctx_partition.py --selftest`, rc-checked) | D-137 discoverable negative self-test for the NodeContext partition guard: proves all three RED directions FIRE (UNACCOUNTED / STALE-EXEMPT / CONTRADICTION), that every refusal REFUSES rc 2 rather than empty-passing (absent persist registry · absent exemption registry · unknown category · stub rationale · unterminated rationale), and — the leg that catches a parser which quietly stopped parsing — that the struct's member set and the registry's covered-name set actually **OVERLAP** at HEAD. That last one is load-bearing: two disjoint vocabularies subtract to "everything unaccounted" or "everything fine" depending on direction, and BOTH read as a working guard. Carries positive controls too (clean partition → 0 findings; a well-formed exemption ACCEPTED; an always-RED category is a valid recordable row that still REDs), without which the gate could fail on everything and still look rigorous. |
| `check_amendment_cascade.py` | STANDING-CI (ADVISORY) | `check_session_docs` (ADV live + HARD teeth row); `/capture-audit` Check 12; `/precoding-audit-gate` Stage 2.5 | ✓ (`--selftest`, 22 teeth; D-137 wrapper `check_amendment_cascade_selftest.sh`) | **CP-1 (cascade-not-propagated) mechanization — TECH_DEBT-148 CLOSED 2026-08-16.** Greps the phrasing an amendment RETIRED, not the file it amended: take the diff's `-` side, subtract everything still on its `+` side, and any UNTOUCHED live doc still carrying the remainder is a candidate. Both sets are closed (a diff window, a corpus), so the question is decidable — unlike "is this reference semantically stale?", which is not. Built on its own trigger: TECH_DEBT-148 deferred it pending *"a CP-1 recurrence outside the memory corpus that the manual procedure misses"*, and that fired when Class 58's detection signature was corrected while the ACTIVE handoff, MASTER and the D-421 STATUS block all kept instructing the next session to build the refuted shape — `check_session_docs` green throughout. **M10: PARTIAL and ADVISORY.** A green means only "no untouched live doc repeats a phrasing this window retired"; an amendment that supersedes purely by ADDING retires nothing textually and is invisible to it. Output is TIERED, not filtered — HIGH (citable IDs / identifiers) vs LOW (prose) — because the rarity discriminator was **measured and failed**: `first-order` (noise) and `gate-reachability` (the real cascade) both live in ~5-6 live files. Nothing is silently dropped; a dropped finding is invisible, an unranked one is merely tedious. Historical-record filter (the CP-1 M3 surface) preserves `postmortems/`/`changelogs/`/`reports/`/`plan_checks/` + `status: superseded` frontmatter, and deliberately reads only the leading 1200 bytes so a `status:` buried in prose cannot silence a live doc. Corpus roots named EXPLICITLY (Landmine 19). Roots from `foxroots`. |
| `check_amendment_cascade_selftest.sh` | STANDING-CI (D-137 wrapper) | `check_session_docs` teeth row; manual | ✓ (IS the selftest) | D-137 discoverable negative self-test for the CP-1 detector — proves both-sides and added-only terms are never reported as retired, the preserve tiers fire, a `status:` deep in prose does NOT silence a live doc, the rarity gate spares citable IDs, and BOTH controls hold (positive: a real retired term is found; negative: an unrelated term yields nothing — a detector that reds on everything is as useless as one that reds on nothing). |
| `check_forward_promise_audit.py` | STANDING-CI | hook; `check_session_docs`; +6 skills | ✓ | forward-promise landed-verification (Check 11) |
| `check_plan_body_symbol_existence.py` | STANDING-CI | hook; `check_session_docs`; +4 skills | ✓ | Class-14 plan-body symbol-existence (B-Plus) |
| `check_plan_body_tests_section.py` | STANDING-CI | hook; plan-dive/precoding/readiness | — | Check-45 tests-section presence |
| `check_doc_rename_classification.py` | STANDING-CI | hook | — | B19 prose-token rename-classification guard |
| `check_cfg_key_prefix_drift.py` | STANDING-CI | `run_all_tests.sh` (+ `--selftest` teeth) | — | `.E.1.1` Class-53 rename-completeness guard — diffs cfg-file keys + operator-facing source string-literals vs the parser's accepted per-node prefix (`node_`) + a retired-prefix/key ledger + the §15 PRESERVE allowlist; selftest-proven non-vacuous (asserts its own parser ground-truth at the parse site). Sister to `check_doc_rename_classification.py` (.md side) + `scan_class_44_cfg_orphan.py` (cfg-flag side). pre-commit trigger-scoping owed (handoff) |
| `check_cfg_duplicate_keys.py` | STANDING-CI | `check_session_docs` (HARD 5b + `--selftest` teeth) | ✓ | cfg-key uniqueness guard (2026-08-15, from the i-class GUI-persistence sweep) — a key may appear AT MOST ONCE per cfg file. WHY it is capital-relevant: the engine parser (`ControllerConfig_Load`) assigns on EVERY occurrence and `continue`s to the next FILE line ⇒ **last-wins**, while the GUI's `cfg_write_field` line-anchored-searches and `break`s ⇒ **first-match**. On a duplicated key the operator's edit lands on a line the parser overrides ⇒ **SILENTLY INERT** (found live on `node_3_stop_loss_pct`, `partial_exit_enabled` ×3, `node_1_offset_stddev_mult`). Deliberately does NOT auto-pick a winner — which value was intended is an operator decision. Working-tree scan (the cfgs are gitignored-in-place, so pre-commit has no staged surface). 7-tooth `--selftest` (positive + negative + comment ×2 + whitespace + prose + prefix + file round-trip). Sister to `check_cfg_key_prefix_drift.py` (prefix drift, not intra-file duplication) |
| `check_cfg_duplicate_keys_selftest.sh` | TEST-HARNESS | `check_session_docs` (HARD teeth); test runner; manual | — | negative self-test for `check_cfg_duplicate_keys.py` (D-137 teeth) — TWO layers: (1) the tool's inline 7-tooth `--selftest` over the scan function, and (2) an END-TO-END PROCESS-LEVEL proof (plant a duplicate in a temp cfg, assert the process exits non-zero; then assert a clean cfg exits 0). Layer 2 exists because the inline teeth cannot catch the Class-51 sub-shape-B′ "decorative detector" mode — a correct scan whose `main()` fails to gate on the result would keep the inline teeth green and only an exit-code assertion catches it |
| `check_cfg_key_prefix_drift_selftest.sh` | TEST-HARNESS | `run_all_tests.sh`; test runner; manual | — | negative self-test for `check_cfg_key_prefix_drift.py` (D-137 teeth) — delegates to the tool's inline `--selftest` (asserts its own parser ground-truth + proves RED-on-a-retired-prefix-target) |
| `check_cfg_gate_caller_coverage.py` | STANDING-CI | `run_all_tests.sh` (+ `--selftest` teeth) | — | `.E.1.1` ③ item-6 config-compiler CALLER-COVERAGE recurrence guard (D-256 #6 / D-260 C-5) — every C++ `ControllerConfig_Load` caller MUST gate via `cfg_capital_gate_ok`/`cfg_compile_ok` or be EXEMPT-with-reason (GUI observability / legacy-dead TUI); a NEW ungated caller = RED (the founding-bug class). The call-graph half of item-6 (the static half = the no-capital-row-is-`MANUAL_PARSER` `static_assert` in `ControllerConfig.hpp`). `--selftest`-proven non-vacuous. Sister to `check_cfg_key_prefix_drift.py` |
| `check_cfg_gate_caller_coverage_selftest.sh` | TEST-HARNESS | `run_all_tests.sh`; manual | — | negative self-test for `check_cfg_gate_caller_coverage.py` (D-137 teeth; ③ item-6) — delegates to the tool's inline `--selftest` (proves gate-detection distinguishes gated/ungated → non-vacuous) |
| `check_meta_registry.py` | STANDING-CI | `check_session_docs`; capture/registry-fit | ✓ (`--selftest`) | `FOREACH_REGISTRY` meta-registry coverage (H15) — Check 1 unregistered-macro / Check 2 row-without-#define / Check 3 LEVEL-PARENT. Uses the canonical `check_doc_metadata.ENGINE` resolver (2026-07-19 — was the lone straggler rolling its own `Path(__file__).parent.parent` → false `exit 2` from the workspace-path invocation). `--selftest` teeth via the extracted `evaluate_registry` core. |
| `check_meta_registry_selftest.sh` | TEST-HARNESS | hook; `check_session_docs` (via the tool's `--selftest`) | ✓ (IS the selftest) | D-137 discoverable negative self-test for check_meta_registry — proves Check 1/2/3 teeth + clean-pass + the canonical-ENGINE path-resolution guard (the 2026-07-19 fix keeps locating the real registry). |
| `check_capital_adversarial_audit.py` | STANDING-CI | `check_session_docs` (advisory) | — | TECH_DEBT-164 part 3 / AR-8 failsafe — standing scan: every capital TEST block carries `// ADV-REFUTE` (independent refute ran) or `// ADV-SELF` (opt-out + reason); guards the binding adversarial-default against self-attestation |
| `check_capital_adversarial_audit_selftest.sh` | TEST-HARNESS | test runner; manual | — | negative self-test for `check_capital_adversarial_audit.py` (D-137): RED on an unmarked capital assert, GREEN on `// ADV-REFUTE` marked (hermetic temp-dir probes) |
| `check_tools_inventory.py` | STANDING-CI | hook (via `check_session_docs`) | ✓ (this doc) | THE tool-rot guard — every `tools/*.{sh,py}` has a row here (D-136) |
| `check_identifier_retirement.py` | STANDING-CI | pre-commit (Check H); readiness (Check 46); post-ship-audit | ✓ | H21 tombstone guard — persistence/wire identifiers (snapshot/format VERSIONs + persisted enum CODES) append-only + immutable vs the golden `identifier_ledger.txt` (Class 40; `.E` #11 guard-hardening). **`--update` is TTY-gated since 2026-07-20** — it routes through `bless.py` (D-394: diff shown, typed confirmation, non-interactive HARD-REFUSES rc=2, no-op ⇒ no write), so a delegated agent can no longer rubber-stamp the H21 golden by "re-baselining a red" (TECH_DEBT-255 CLOSED). Verify mode — what pre-commit/readiness/post-ship-audit actually call — is unchanged. `IDENTIFIER_LEDGER` overrides the ledger path for the self-test ONLY. **E.1.2/D-421 — `_parse_foreach` now comment-strips (it never did, while its sibling consumer of the same library always has), expands nested `FOREACH_<NAME>(X)` rows, and passes the registry file's own dir as `base_dir` so `__has_include` guards resolve. Effect: `STRATEGY_EMA_CROSS = 4` entered the ledger (47 → 48 ids). Enrollment ≠ coverage — `StrategyId` was enrolled from day one and still only 4 of its 5 codes were ever parsed, which is WORSE than un-enrolled because the category exists so the surface reads as covered. `STRATEGY_AUTO` stays out deliberately: it is `= NUM_STRATEGIES_REAL`, a derived sentinel with no independently-assignable value, so guarding the count guards it transitively.** |
| `check_tech_debt.py` | STANDING-CI | pre-commit (Check J, advisory); `check_session_docs` (+ **ADV `--contract-stale` + `--contract-stale-selftest` teeth, D-408/D-416**); post-ship-audit; readiness (Check 25) | ✓ (`--contract-stale-selftest`) | tech-debt surface advisory — surfaces OPEN TECH_DEBT overlapping the staged/ship files (classify subsume/adjacent/defer per `feedback_opportunistic_tech_debt_closure`); also `--stale` / `--close` modes (`.E` #11) |
| `check_struct_alignment.py` | STANDING-CI | pre-commit (Check K) | — | struct-alignment guard (Knight/H21-class) — every `alignas(>16)` struct (struct- or member-level) must NOT be allocated via bare malloc/calloc/realloc (those give 16B → misaligned = UB + broken aligned-SIMD/false-sharing); `new`/aligned_alloc/posix_memalign/arena-with-align OK. Surfaced by ubsan's first run during `.E` #11 Ship-A (RollingStats + EventLoopState); TECH_DEBT-157. Advisory (b): flags alignas types lacking a `static_assert(alignof==N)` lock. **(c) byte-serialization size-pin coverage (D-202, ENFORCING):** an in-tree type serialized via fwrite/fread/memcmp/SHA/HMAC MUST carry `static_assert(sizeof(T)==N)` (H9/H12) → fails the build on an unpinned serialized type (a silent layout change would be a wire/persist break; bump the snapshot VERSION per H21). The "catch what I forget when I change a core struct" guard. `FOXML_ENGINE` env override for the self-test |
| `test_check_struct_alignment.py` | TEST-HARNESS | test runner; manual | — | NEGATIVE self-test (teeth) — proves the guard goes RED + names the type on (a) a bare-malloc-of-`alignas(64)` violation (GREEN on `aligned_alloc`) AND (c) a byte-serialized struct with no `sizeof` pin (GREEN when pinned); a guard that's only ever GREEN could be silently broken (D-202) |
| `check_money_gross_single_source.py` | STANDING-CI | pre-commit (Check G) | — | D-190/M7 guard — the realized + unrealized price-diff P&L gross MUST route through `Money_FillGross` (Portfolio.hpp); catches a re-introduced open-coded 2-mul form (`round(exit*qty)-round(entry*qty)`) that diverges 1 ULP from the 1-mul books under decimal half-even (PARITY-038, Landmine 8). Uses `.absolute()` not `.resolve()` per Landmine 5 (symlinked `tools/`) |
| `check_money_gross_single_source_selftest.sh` | TEST-HARNESS | post-ship-audit; manual | — | NEGATIVE self-test (teeth) — proves the D-190 gross-SSoT guard catches an injected 2-mul gross + no false positive on the clean tree |
| `check_slot_node_derive.py` | STANDING-CI | pre-commit (Check O) | — | slot→node derive class-close guard (D-294/D-295/D-296, Y1) — the logical node owning a portfolio slot is derived by `Sharded_SlotNode(slot, partial_on)` (ControllerEventLoop.hpp, the branchless inverse of `Sharded_LegSlot`); fails the build on a NEW open-coded `(slot\|idx)>>1` / `>>partial` derive in `CoreFrameworks/` — the drift class behind the `:856` ungated-`slot>>1` bug (per-node stats misattributed to node/2 on event-log warm-restart, LOW today / latent-MED). Route via the accessor, or grandfather a legit exception with a trailing `// SLOT_DERIVE_OK: <reason>`. GUI/ is OUT of scope (its 4 display-only derives grandfathered pending the E-series decouple, D-295). Teeth: a NEGATIVE self-test (`test_check_slot_node_derive.py`, D-137) proves RED-on-injected-violation / GREEN-on-routed-or-marked / RED-on-vacuous; plus INLINE self-tests (regex detects the 3 canonical forms + rejects non-slot shifts like `order_id>>60`; FAILS on 0-files-scanned — the symlink trap that made the first draft vacuously pass). `.absolute()` not `.resolve()` (LANDMINES 5/7); `FOXML_ENGINE` override |
| `test_check_slot_node_derive.py` | TEST-HARNESS | test runner; manual | — | NEGATIVE self-test (teeth, D-137) — drives `check_slot_node_derive.py` against a throwaway CoreFrameworks tree (FOXML_ENGINE override): proves RED + names the file on an injected `slot >> 1`, GREEN when routed through `Sharded_SlotNode` or marked `// SLOT_DERIVE_OK`, and RED on a vacuous 0-file scan |
| `check_always_loaded_budget.py` | STANDING-CI | `check_session_docs` (HARD); `--selftest` teeth-proof | ✓ | always-loaded doc context-budget guard — CLAUDE.md/local + MEMORY.md vs the harness byte caps (40k / 24.4KB); M7 close of the silent-truncation class (convention-only compression recurred; `.E` #11 doc-budget) |
| `check_fpn_doc_size_currency.py` | STANDING-CI | `check_session_docs` (HARD 7) | — | FPN-doc-size currency guard (M7) — every doc stating a SINGLE `FPN<...>` byte size must match the code's `static_assert(sizeof(FPN<64>) == N)` in `FixedPoint/FixedPointN.hpp` (SSoT; auto-tracks the next flip). Closes the Ship-A drift class: the 24B→16B flip left ~12 docs stating stale "24B" and manual convention proved insufficient at that many sites. Parses canonical from code → WARN+exit-0 if absent (never false-fails). M3 false-positive surface: whole-file excludes (changelogs/postmortem/decision-log/HOT_PATH_CHANGELOG/recurring-bug-patterns/LATENCY_OPTIMIZATION/CHANGELOG) + line/12-line-window historical markers (HISTORICAL/superseded/pre-Ship-A/`was NNB`/`NN→MM`/old sign-mag layout members) + FPN-bound prose only (not co-occurring non-FPN byte quantities) + single-FPN-size only (pairs/multiples valid). Dual-root resolver (engine for the parse, workspace for the docs; `FOXML_ENGINE`/`FOXML_WORKSPACE` overrides; `.absolute()` not `.resolve()` — LANDMINES 5/7) |
| `test_check_fpn_doc_size_currency.py` | TEST-HARNESS | test runner; manual | — | NEGATIVE self-test (teeth) — proves the FPN-doc-size guard goes RED + names the file on a present-tense `FPN<64>=24B` drift (vs a 16B canonical), stays GREEN on a matching `16B` doc, and does NOT flag a HISTORICAL `was 24B, now 16B` line (M3). Hermetic temp tree via `FOXML_ENGINE` |
| `check_struct_size_budget.py` | STANDING-CI (candidate; manual now → wire to pre-commit at the umbrella-sweep close) | `--selftest` teeth-proof; manual | — | `.E.1.0` s2 — sizeof + cache-residency guard for NON-serialized runtime structs (the gap `check_struct_alignment.py(c)` leaves — it only pins byte-SERIALIZED types; RollingStats fell through → the "~1.5MB" comment for a 64KB struct, D-229). Per manifest row: COMPILES `sizeof(<instantiation>)` (never trusts a comment), flags a struct exceeding its cache tier (L1d/L2 DERIVED from the host via `getconf` — no magic constants, the guard doesn't reproduce the disease), and emits the `static_assert(sizeof==N)` pin to paste (compile-time > CI). Adding a struct = 1 manifest row. The first concrete guard of the **derived-facts-drift meta-pattern** (mechanically verify size/cache/complexity/latency facts; never hand-comment). `--selftest` = a 1KB fake budget MUST flag the 64KB ring (teeth). In-repo-tree tempdir (`/tmp` noexec LANDMINE). `FOXML_ENGINE` override. Sister: `check_struct_alignment.py` (serialized) + `check_fpn_doc_size_currency.py` (docs) |
| `check_struct_size_budget_selftest.sh` | TEST-HARNESS | test runner; manual; D-137 inventory-guard | — | NEGATIVE self-test (teeth) for `check_struct_size_budget.py` — drives the tool's `--selftest` (a 1KB budget vs the real 64KB `RollingStats<64,1024>` ring); the comparison MUST flag it. Proves the guard bites |
| `check_latency_path_conformance.py` | STANDING-CI — pre-commit Check N (trigger-scoped, HARD; `SKIP_LATENCY_CONFORMANCE_CHECK` bypass) + `run_all_tests.sh` (gate + `--selftest`, both HARD) | `--selftest` teeth; manual | ✓ | `.E.1.0` H8 — STATIC latency-path conformance analyzer (the 2nd derived-fact-budget gate; sister to `check_struct_size_budget.py`). **Exit 0 — full hot + slow + ML-kernel coverage.** Disassembles the PRODUCTION (`-O3 -march=native`, no `LATENCY_PROFILING`/`USE_XGBOOST`) probe-wrappers of a **7-row KERNEL manifest** (D-238 kernel-granularity — gate the bounded per-cycle LEAVES: hot `ExecutionCore_Tick` + the slow-path RollingStats / regime / confidence / ridge feature kernels; NOT the `EventLoop_RebuildOneCore` ORCHESTRATOR, which over-inlines the whole pipeline incl. cold persistence in a single-TU probe → signal flood) + gates FROM ASM: instruction-count budget · branch-classification {loop / rare-cold / **data-dependent-warm**} (the H7/H20 meter → 0 per D-235; SUBSUMES the H7/H20 grep) · no scalar-float incl. AVX/FMA (H4) · no div (§5) · no malloc/free/__libc/lock/stdio/throw (H1/Rule 2) · no indirect/vtable (H2) · **un-analyzed-warm-work** (transitive recursion into same-TU callees, MAX_DEPTH 5, fail-loud — STRUCTURAL not positional, the cold-quartile-hole fix). MECHANIZES `latency-path-discipline.md` + the hot/slow invariants. **Exemptions single-sourced to the engine's H4-sanctioned seams (D-237):** a per-row source-keyed `allow` (file-level for pure-feature files, `file:line` for the mixed `FixedPointN.hpp`; **forbidden-calls exempt by `file:line`-ONLY**, never file-level) + `FEATURE_MATH_EXTERN` (the libm transcendentals / double min-max-round / int128↔double conversions the feature domain sanctions). **ASSERTS its OWN non-vacuity** (symbol found + real body via length-prefixed mangled match incl. `.isra`/`.constprop` clones — else it IS the Class-51 vacuously-green guard it codifies against; refuses an optimized-away wrapper). Two-layer w/ the deferred PMU step (`perf stat -e branch-misses` confirms allowlisted branches ~0% mispredict). Manifest = 1 row/kernel; grandfathered baseline at `tools/lib/latency_path_budgets.json` (7 rows: instructions + data_dependent); `--update-budgets` re-baseline (pinned toolchain + tolerance); probe-wrapper disasm for always-inline templates (in-repo tmp — `/tmp` noexec LANDMINE); `FOXML_ENGINE` override. D-234..D-238. **15 `--selftest` teeth all fire**; the build's load-bearing self-catches (each found by an INDEPENDENT adversarial pass — NOT the builder's dogfood; AR-8, 5 passes): AVX/FMA `vfmadd` blindness · forbidden-call-blind-to-external-syms (→ `objdump -r`) · a `jmp*`-tail-call detector that false-flagged GCC switch-tables (REVERTED → `call *`-only + the transitive/tail-jmp scan) · the **decorative div-gate** (counted but un-gated) · the **cold-quartile false-green** · the **forbidden-file-level hole**. NOT manifested (in-tool NOTE comments): `EventLoop_RebuildOneCore` (orchestrator) · `Model_Predict*` (XGBoost external-API stub — coverage gap homed TD-206) · `OnlineCycleStep` (.text.unlikely tail-jmp). **SHIPPED `.E.1.0`** (tag `v5.15.5.F.4d.1.E.1.0`, 2026-06-17): the conformance DESIGN_SPEC (`audit-methodologies/static-latency-path-conformance-analysis.md`) + the Class-51 first-canonical catalog entry LANDED (s5). |
| `check_latency_path_conformance_selftest.sh` | TEST-HARNESS | test runner; manual; D-137 inventory-guard | — | NEGATIVE self-test (teeth) for `check_latency_path_conformance.py` — drives `--selftest`; a float-injection probe MUST trip the H4 detector. CAUGHT the AVX/FMA-detector hole during the `.E.1.0` dogfood (`mulsd\|addsd` missed the `-march=native` fused `vfmadd`) — load-bearing, not ceremony |
| `check_handoff_capture_completeness.py` | STANDING-CI | `check_session_docs` (HARD); `--selftest` teeth-proof | — | `.E.1.0` un-bypassable enforcement of the handoff `## Capture-completeness` convention — the single `status: active` handoff MUST carry a SUBSTANTIVE capture-completeness section (addresses decisions→log · findings→ledger · memories/skills→indexed, each enumerated OR explicit-"none", no placeholders). M7 escalation: `/handoff` Stage 1.8 + the template section are bypassable by hand-writing (the `.E.1.0` D-227/TECH_DEBT-204 miss); this red-builds a handoff that omits/stubs the section |
| `check_handoff_capture_completeness_selftest.sh` | TEST-HARNESS | test runner; manual | — | negative selftest for `check_handoff_capture_completeness` (teeth) — rejects a missing / thin / placeholder section, accepts a full one |
| `check_handoff_active_singleton.py` | STANDING-CI | `check_session_docs` (HARD); `--selftest` teeth-proof | ✓ | handoff-active singleton guard — ≤1 `status: active` handoff across `plans/**/handoffs/` (the explicit-state SSoT no-arg `/accept-handoff` resolves by; supersede-on-write replaces fragile mtime resolution); `.E` #11 handoff-state-machine |
| `check_index_currency.py` | STANDING-CI | `check_session_docs` (HARD 9); `--selftest` teeth-proof | ✓ | close-ritual index-currency guard (M7; TECH_DEBT-194) — the sprint MASTER CURRENT-STATE banner's "Pickup → handoffs/..." pointer MUST name the singleton `status: active` handoff; cross-checks the two SSoTs (composes with `check_handoff_active_singleton` — that enforces ≤1 active, this enforces MASTER-names-it). Closes the recurring WH-2 stale-banner shape caught only by operator prompt (the `.E.0.10` 2026-06-13 instance). v1 = pointer cross-check (deterministic; advisory on no-MASTER/no-pointer/0-or->1-active); date-staleness = v2. Landmine-5-safe resolver |
| `check_navinfra_cohort_reference.py` | STANDING-CI | `check_session_docs` (HARD 8) | ✓ (adversarial-multi-agent-audit-methodology.md § Nav-infra) | nav-infra cohort guard (M7) — every skill in the adversarial-audit spec's `applies_at_skills` MUST reach the nav-infra consult (DAG + CODE_MAP + per-edit data-flow/downstream-blast-radius trace) via the shared Stage-0 doc citation OR a direct ref; a cohort skill that drops it = red build. Closes the "operator hand-nudges the DAG/CODE_MAP in every session" surface (2026-06-11; the `.E.0.10` pickup). Content-marker root resolver (Landmine-5 safe). Teeth-proofed inline (synthetic gap → exit 1, names the offender) |
| `check_per_node_registry_integrity.py` | SKILL-WIRED | accounting/capture/dod/readiness | ✓ | `PerCoreCfg` X-macro integrity (H17 Check 2) |
| `calls_graph_diff.sh` | SKILL-WIRED | dust/post-ship-audit/readiness/ship | ✓ | strategy/regime orphan-diff (legacy vs sharded) |
| `check_field_name_uniqueness.py` | SKILL-WIRED | blindspot-scan | ✓ | cfg field-name uniqueness |
| `check_struct_field_uniqueness.py` | SKILL-WIRED | readiness | ✓ | struct field-name uniqueness |
| `check_storage_t_coverage.py` | SKILL-WIRED | blindspot-scan | ✓ | `storage_t` coverage check |
| `dedupe_findings.py` | SKILL-WIRED | plan-dive | — | findings dedupe |
| `migrate_memory_frontmatter.py` | SKILL-WIRED | capture-audit/sync-workspace | — | memory frontmatter re-derive + sister-symmetrize |
| `rebuild_doc_indexes.py` | SKILL-WIRED + STANDING-CI (`--check`) | ship; index-rebuild; `check_session_docs` (currency) | — | regenerate doc indexes (CLAUDE.md skill table / DESIGN_SPECS README + TAG_INDEX / **DOCS/CODE_TAG_INDEX.md** — the code-side twin via the validator's shared grammar, E.1.2.A P2); `--check` = the standing currency guard |
| `gen_code_map.sh` | SKILL-WIRED | dust/plan-check/readiness/ship | NEEDS (#24) | code-intelligence index (fn map + `--types`/`--structs`/`--macros`/`--full` type→sites blast-radius). `CODE_MAP.md` is gitignored (generated); now regen-on-use (ship Stage 4 unconditional + readiness regen-first) — no more stale-copy rot |
| `cascade.py` | SKILL-WIRED | trace-deps/dependency-chain-trace; run_all_tests (`--selftest`); manual (rename/registry ships) | ✓ | TD-175a change-cascade tool. `rename` = **ENUMERATE-ONLY** rename worklist across engine src **+ the compiler-blind apparatus** (`tools/`/`build.sh`/`.githooks/` — commit GREEN on a stale regex) + the `#include`-cascade + the expected-residual allowlist (gate R1: no code `--apply`; the compiler is the code-token oracle per rename-ship-methodology Phase 4). **`registry <FOREACH_NAME>`** = ENUMERATE one registry macro's footprint UPFRONT, role-classified (DEFINITION / **H15-ENROLLMENT** / EXPANDER / REFERENCE) + the MetaRegistry-enrollment status checked + compiler-BLIND sites flagged (D-298 — the M7 close of finding a registry rename's enrollment + apparatus refs reactively; run BEFORE a rename/merge/delete, not after). `struct <T>` reserved → `.E.1.2`. The standing apparatus-currency net rides `check_tools_inventory.py` (R2 `build.sh`-scan). Spec: `refactor-patterns/rename-cascade-enumeration-tooling.md` (v1.3) |
| `cascade_selftest.sh` | TEST-HARNESS | test runner; `run_all_tests.sh`; manual | — | NEGATIVE self-test (teeth) for `cascade.py` — a planted stale `FOREACH_PER_CORE_CFG_FIELD` + a `//` comment-resident token MUST be caught; PRESERVE (ExecutionCore/FoxML_Core/cpu_id) + `experiments/per_core_sharding/` MUST be spared (non-vacuity, Class-51) |
| `test_check_navinfra_cohort_reference.py` | TEST-HARNESS | test runner; manual | — | NEGATIVE self-test (teeth) — proves the nav-infra cohort guard goes RED + names the offender when a cohort skill drops the reference, and GREEN when all reach the consult (hermetic temp workspace tree) |
| `test_check_tools_inventory.py` | TEST-HARNESS | test runner | — | NEGATIVE self-test — proves the rot-guard goes RED on unenrolled-tool + broken-ref violations (teeth) |
| `test_check_doc_rename_classification.py` | TEST-HARNESS | test runner | — | tests for `check_doc_rename_classification.py` |
| `test_check_plan_body_tests_section.py` | TEST-HARNESS | test runner | — | tests for `check_plan_body_tests_section.py` |
| `test_memory_guard.py` | TEST-HARNESS | test runner | — | tests for the memory guard |
| `test_foxtag_client.py` | TEST-HARNESS | test runner; manual | — | NEGATIVE self-test (teeth, D-137) for `foxtag_client.py` — hermetic stub-binary probes: missing binary / garbage JSON / malformed rows / nonzero-exit all degrade to the documented empty/None shapes (never a crash, never a fabricated result); valid output decodes exactly (the seam DISCRIMINATES) |
| `chart.py` | ONE-OFF-MANUAL | — | n/a | metrics charting (operator viz) |
| `feature_overlay.py` | ONE-OFF-MANUAL | — | n/a | feature-overlay viz (operator) |
| `compare_scalers.sh` | ONE-OFF-MANUAL | — | n/a | scaler-comparison CLI wrapper (manual; `compare_scalers.cpp`) |
| `subdivide_design_specs.py` | ONE-OFF-MANUAL | — | n/a | one-time DESIGN_SPECS subdivision (used once) |
| `check_determinism_selftest.sh` | SKILL-WIRED | /post-ship-audit | ✓ | the net's NEGATIVE self-test — verifies each gate goes RED on its injected regression (the net has teeth) |
| `scan_class_27_full.py` | SKILL-WIRED | /bug-check (Step 3) | ✓ | full Class-27 (scalar cfg-mirror) mechanical scan |
| `scan_class_44_cfg_orphan.py` | STANDING-CI | `check_session_docs` (HARD, `--strict`); /bug-check (Step 3) | ✓ (docstring) | full Class-44 cfg-flag-ORPHAN scan — operator-settable `MASK_*_CFG_*` with NO live sharded reader (read only on dead `PortfolioController`/TUI/GUI); enumerates the universe from the `FOREACH_*_CFG_FLAG` registries (SSoT). Standalone full-scan sibling of `check_per_node_registry_integrity.py` Check 11's cfg-MUTATION (same two-tool shape as Class 27's Check 7 + `scan_class_27_full.py`). `.E.0.10` #9/#10; the full struct-field produce/consume tracker is TECH_DEBT-175 |
| `test_scan_class_44_cfg_orphan.py` | TEST-HARNESS | test runner; manual | — | NEGATIVE self-test (teeth) for `scan_class_44_cfg_orphan.py` (D-137) — proves it FLAGS a synthetic dead-only orphan + SPARES a live-wired flag + oracle-PASSes, in a hermetic temp tree |
| `validate_feature_mask.sh` | SKILL-WIRED | /ml-audit (Section B) | ✓ | feature-mask e2e validation |
| `check_plan_enumeration_completeness.py` | SKILL-WIRED | precoding-audit-gate (Stage 2.5) | NEEDS (#24) | plan's claimed enumeration SET ⊇ the code-intel tool output (catches AR-1 summarize-and-drop — the tool was run, the summary lost members; D-141 / `.E` Session-8) |
| `test_check_plan_enumeration_completeness.py` | TEST-HARNESS | test runner | — | NEGATIVE self-test — proves the under-enumeration guard goes RED on a dropped-file plan + names it (teeth) |
| `check_identifier_retirement_selftest.sh` | TEST-HARNESS | post-ship-audit; manual | — | NEGATIVE self-test — proves the H21 tombstone guard goes RED on renumber / version-decrease / silent-removal (teeth) |
| `check_tech_debt_selftest.sh` | TEST-HARNESS | post-ship-audit; manual | — | NEGATIVE self-test — proves the tech-debt advisory DETECTS overlaps (+ no false positives) + `--stale --strict` goes RED (teeth) |

*Non-executable helpers (not in the guard's scope): `replay_locale_gate.cpp` / `fp_determinism_golden.cpp` / `compare_scalers.cpp` (compiled by the `.sh` gates), `*_baseline.txt` / `*_golden.txt` / `identifier_ledger.txt` (frozen data).*

## Planned / retired (tracked off-disk — Check 2 accounts for references)

| Tool | Disposition | Status |
|---|---|---|
| `emit_decisions_to_honor.py` | PLANNED | C-class decision-currency mechanization (D-226) — given a surface/plan, emit the locked decisions it must HONOR / not re-litigate (greps the decision-log `<!-- D/C/F -->`/`<!-- STATUS -->` sentinels + the surface→spec map). Manual sweep runs now (the `.E.1` 223-decision sweep, D-223); build when the manual sweep recurs (M7). Sister to the `--emit-decision-status` candidate on `check_capture_audit.py`. Spec: `plan-decomposition-and-future-aware-agent-arming.md` §3. |
| `stamp_model.sh` | RETIRED | bash-CLI stamper DELETED at v5.15.5.F.4d.1.B.3 (Path C) — stamping is now in-process / foxml_suite GUI auto-stamp. Lingering runnable cites in /ship + /parity-check fixed `.E` Session-7. |

## Actionable (status)
1. ✅ **3 orphans WIRED** (`.E` Session-7): `check_determinism_selftest.sh` → /post-ship-audit · `scan_class_27_full.py` → /bug-check Step 3 · `validate_feature_mask.sh` → /ml-audit Section B.
2. ✅ **`gen_code_map` regen FIXED:** `CODE_MAP.md` is gitignored (generated) — now regen-on-use (ship Stage 4 unconditional + readiness regen-first); no more stale-copy rot.
3. ⏳ **Spec gaps among load-bearing tools** (paced, #24): `check_capture_audit.py`, `check_plan_body_tests_section.py`, `migrate_memory_frontmatter.py`, `dedupe_findings.py`, `rebuild_doc_indexes.py`, `gen_code_map.sh` are STANDING/WIRED with no DESIGN_SPEC — not blocking.
