# /dod-audit report — v5.15.5.F.4d.1.B.3-legacy-empty-out — 2026-05-17

**Scope:** plan-mode (`/dod-audit plan <plan_path>`)
**Target plan:** `plans/v5.15-live-readiness/subplans/2026-05-17-v5.15.5.F.4d.1.B.3-legacy-empty-out.md` v1.2 DRAFT
**Engine HEAD:** `9b62a72` (v5.15.5.F.4d.1.B.2 ship close)
**Ship:** `v5.15.5.F.4d.1.B.3`
**Reviewer:** Explore subagent for /dod-audit (Layer 2 per SKILLS_HIERARCHY)

---

## Catalog ingested

- 76 patterns in `DESIGN_SPECS/*.md` (catalog walked dynamically)
- Patterns deeply consulted for this scan (the 9 named in the focus-areas list + 11 inferred from plan content):
  - pattern-codification-lifecycle, autopopulate-pattern-for-production-caller-class, x-macro-registry-with-presence-dispatch, categorical-tag-applicability-pattern, cfg-flag-eligibility-criteria, registry-coverage-ci-check-pattern, bitmap-overflow-protection-discipline, wire-format-byte-preservation-discipline, cfg-derived-consumer-framework, canonical-sister-extension-discipline, sidecar-override-pattern-for-registry-auto-flows, metadata-bit-driven-derived-filter-framework, meta-registry-pattern-for-codebase-registry-discipline, framework-composition-overview, type-trait-dispatch-via-tt-namespace, decision-time-data-binding-pattern, structural-fix-preferred-decision-framework, branchless-dispatch-discipline, multi-state-dispatch-with-per-state-update-metadata, future-oriented-plan-template
- Doc-debt findings: NONE (catalog complete for surfaces this plan touches)

---

## Summary

| Pattern | CLEAN | APPLIED | MISSED | DEFERRED |
|---|---|---|---|---|
| pattern-codification-lifecycle | — | 3 | 1 | 0 |
| canonical-sister-extension-discipline | — | 5 | 0 | 0 |
| cfg-derived-consumer-framework | — | 4 | 0 | 0 |
| x-macro-registry-with-presence-dispatch | — | 3 | 1 | 0 |
| autopopulate-pattern-for-production-caller-class | — | 3 | 0 | 0 |
| categorical-tag-applicability-pattern | ✅ | 0 | 0 | 0 |
| cfg-flag-eligibility-criteria | — | 1 | 0 | 0 |
| registry-coverage-ci-check-pattern | — | 1 | 1 | 0 |
| bitmap-overflow-protection-discipline | ✅ | 0 | 0 | 0 |
| wire-format-byte-preservation-discipline | — | 2 | 1 | 0 |
| sidecar-override-pattern-for-registry-auto-flows | — | 1 | 0 | 0 |
| metadata-bit-driven-derived-filter-framework | — | 2 | 0 | 0 |
| meta-registry-pattern-for-codebase-registry-discipline | — | 1 | 0 | 0 |
| structural-fix-preferred-decision-framework | — | 3 | 0 | 0 |
| branchless-dispatch-discipline | — | 1 | 0 | 0 |
| future-oriented-plan-template | — | 1 | 0 | 0 |

**Tally:** 31 APPLIED + 4 MISSED + 0 DEFERRED. Net pattern-application discipline is strong; 4 MISSED findings cluster around 2 themes: (a) Decision D scope under-counts the bandit/thompson 4 POST_CFG mirror rows (Step 1.6.2 acknowledges as unknown), (b) Decision B `reason_buf` extension needs Stage 2 DRAFT DESIGN_SPEC before pre-coding tag per pattern-codification-lifecycle.

---

## Findings (severity-ordered)

### HIGH-1 — Decision D scope incomplete: 4 bandit/thompson POST_CFG mirror entries omitted from explicit deletion list

- **Pattern:** x-macro-registry-with-presence-dispatch.md (single source of truth at master-registry granularity) + canonical-sister-extension-discipline.md (DELETE legacy fold-out verdict)
- **Plan section:** "Design space + future-oriented choice" Decision D + Step 1.6.2 (line 212) + Verification gate (line 356)
- **Symptom:** Decision D explicitly names **5 prefixed-only fields** (`ml_tp_pct` / `ml_sl_pct` / `barrier_blend_mode` / `bandit_blend_ratio` / `per_horizon_barrier_blend`). But `StampBoundModelConstRegistry.hpp:454-483` has **9 prefixed `inference_cfg_*` rows** in POST_CFG block:
  - Lines 454-465: 5 fields named in plan body
  - Lines 469-483: 4 bandit/thompson rows (`inference_cfg_bandit_algorithm`, `inference_cfg_thompson_mu_prior`, `inference_cfg_thompson_precision_prior`, `inference_cfg_thompson_precision_obs`, `inference_cfg_thompson_exp3_blend_alpha`) — added at `.F.4d` per PARITY-026 close
  - Verification: 4 thompson + bandit_algorithm + thompson_exp3_blend_alpha rows ARE flagged `STAMP_BOUND_CFG_DERIVED` in master cfg registry (`CfgFieldRegistry.hpp:598-612`) at `.B.2` close (cohort-migrated). The POST_CFG prefixed entries at `:469-483` are therefore mirror duplicates that WILL double-emit once Step 1.6.4 production canonical body emit migration lands.
  - Step 1.6.2 line 212 acknowledges this as known unknown: "Decision D scope clarification: 4 thompson rows already cohort-migrated at `.B.2`; need to verify whether their POST_CFG mirror entries need deletion at `.B.3` to avoid double-emit". This is the right question but it isn't decided.
- **Why HIGH:** wire-format byte preservation is at risk. If those 4 POST_CFG rows survive while master-registry walker emits the unprefixed sister names, the canonical body produces BOTH:
  - `bandit_algorithm=2\nthompson_mu_prior=0.0\n...` (framework walker, unprefixed)
  - `inference_cfg_bandit_algorithm=2\ninference_cfg_thompson_mu_prior=0.0\n...` (POST_CFG walker, prefixed mirror)
  - HMAC chain breaks vs `.B.2` artifacts (because both new lines weren't present pre-`.B.3`) AND vs intended `.B.3` shape (because legacy prefixed names also fire). Stamp_format_version bump alone won't resolve — wire bytes for v2 must be SINGLE-COPY of each field.
- **Suggested fix:** Promote Step 1.6.2 line 212 from "need to verify" → DECIDED. Add to plan body:
  - Decision D mechanism 1 expands scope: **delete 9 prefixed-only POST_CFG `inference_cfg_*` rows** (`StampBoundModelConstRegistry.hpp:454-483` entire block), not just 5
  - Update "Canonical sister registries considered" line 134 to read "DELETE 9 (per Decision D mechanism 1)" not "DELETE 5"
  - Update Verification gate line 356 to grep `rg "inference_cfg_(ml_tp_pct|ml_sl_pct|barrier_blend_mode|per_horizon_barrier_blend|bandit_blend_ratio|bandit_algorithm|thompson_mu_prior|thompson_precision_prior|thompson_precision_obs|thompson_exp3_blend_alpha)"` returns ZERO production hits
  - Add explicit test fixture: parse `.B.2` v1 stamp with prefixed names AND `.B.3` v2 stamp with unprefixed names; both produce same StampInferenceCfgInputs<F> after AUTOPOPULATE
- **Effort estimate:** ~10-15 min plan body amendment + 0 implementation delta (Step 1.6.2 will need to delete the 4 extra rows alongside the 5 it already names; Step 2's `FOREACH_STAMP_BOUND_MODEL_CONST_POST_CFG` block deletion sweeps all 9)
- **Cross-ref:** `DESIGN_SPECS/wire-format-patterns/wire-format-byte-preservation-discipline.md` Layer 4 round-trip HMAC test + Layer 5b structural invariants I2/I4

### HIGH-2 — Decision B `reason_buf` extension lacks Stage 2 DRAFT DESIGN_SPEC at HEAD; required by pattern-codification-lifecycle before pre-coding tag

- **Pattern:** pattern-codification-lifecycle.md Stage 2 (write DESIGN_SPEC before first reference application)
- **Plan section:** "DESIGN_SPECs landed/amended at this ship" → "Conditionally NEW" section (lines 173-177) + "Pre-coding triggers" item 6 (line 406)
- **Symptom:** Plan body line 175 declares `framework-reason-buffer-extension.md (NEW) — codifies the reason_buf + reason_cap extension pattern for framework consumers that need operator-visible first-drift attribution. Sister to existing autopopulate-pattern. Stage 2 DRAFT at planning; Stage 3 first reference if Decision B picks option (a) which is the auto-pick. ~30-60 LOC spec.` Pre-coding trigger item 6 (line 406) restates: "NEW DESIGN_SPECs Stage 2 DRAFT exist on disk (especially `framework-reason-buffer-extension.md` if Decision B (a))".
  - **Verification at HEAD:** `ls /home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/framework-reason-buffer-extension.md` → "No such file or directory".
  - Decision B is AUTO-PICKED option (a). Per pattern-codification-lifecycle.md Stage 2 ("Author a `DESIGN_SPECS/<pattern>.md` file with: Problem statement / Design space explored / The pattern (concrete shape, code template) / Trade-offs + when to apply / Reference implementations / Lessons / `## Audit detection` section / Patterns NOT used here / Cross-references"), the DESIGN_SPEC must exist on disk BEFORE Stage 3 first reference (the actual coding at Step 0.5a).
  - Pre-coding triggers list it as item 6, but doesn't include a checkbox or step in the IN-scope section ensuring it's written before `pre-v5.15.5.F.4d.1.B.3` rollback anchor.
- **Why HIGH:** the plan body Stage 2 DRAFT requirement is load-bearing per the going-forward rule `feedback_audit_canonical_sister_before_new_infra` + the lifecycle stage discipline. Skipping Stage 2 means the pattern (reason_buf as a framework primitive for first-drift attribution) lands at Stage 3 without prior documentation, replicating the v5.14.11.B mega-bundle's lesson "skipping Stage 6 enforcement is load-bearing". Caramel has rejected this pattern of "ship the fix; codify later" multiple times — pattern-codification-lifecycle.md line 197: "Time pressure forces 'ship the fix; codify later' — but EXPLICITLY queue codification as TECH_DEBT with trigger; don't let it rot".
- **Suggested fix:** Make Stage 2 DRAFT writing an explicit Step 0 sub-action with concrete pre-coding-tag blocking, not just a "Pre-coding triggers" item:
  - Add **Step 0.a** to plan body Step 0: "Write Stage 2 DRAFT `framework-reason-buffer-extension.md` at `tick-trader-percore-workspace/DESIGN_SPECS/`. Required sections: Problem statement (sr.reason first-drift attribution semantic + operator UX value-add); Design space explored (Path A: extend framework signature with 2 args vs Path B: separate helper); The pattern (concrete shape — char* reason_buf + size_t reason_cap + first-write-wins via reason_buf[0]=='\\0' guard); Trade-offs + when to apply (apply when framework consumer needs operator-visible per-row attribution); Reference implementations (Stage 3 first reference = `.B.3` Step 1.6.6 CoreModelZoo drift walker migration); `## Audit detection` (Symptom: framework consumer writes scattered local reason strings without uniform first-write-wins guard); Cross-references."
  - Add to **Pre-coding triggers** as a HARD blocker (checkbox at item 6 → mandatory before `pre-tag`)
  - Move Stage 3 first reference (Step 1.6.6 drift walker migration) to depend on Step 0.a completion explicitly (sequencing constraint update on line 311 in "Steps" section)
- **Effort estimate:** ~30-60 min for Stage 2 DRAFT (matches plan body's own estimate at line 175) + ~5 min plan body amendment
- **Cross-ref:** `DESIGN_SPECS/meta-disciplines/pattern-codification-lifecycle.md` Stage 2 (write DESIGN_SPEC) + `feedback_audit_canonical_sister_before_new_infra` (the framework-extension-before-tag rule)

### MED-1 — Decision C Approach A struct-gen .bss cost lacks bound + needs verification not just estimate

- **Pattern:** x-macro-registry-with-presence-dispatch.md (registry expansion shape — unconditional struct-gen is canonical) + bitmap-overflow-protection-discipline.md (compile-time bound discipline) + structural-fix-preferred-decision-framework.md (close cleanly + verify)
- **Plan section:** "Design space" Decision C Approach (A) (line 70-76) + Step 1.6.3 (line 215-219)
- **Symptom:** Decision C Approach (A) cites "~1.9-2.5KB .bss cost" but doesn't show derivation. Master registry has 78 per-core rows + 13 global rows + 13 ML_CFG_FLAG rows ≈ 104 total. If struct-gen produces `uint8_t has_<name>; T <name>;` per row, with T mostly `FPN<F>` (24 bytes) + some `int` (4) + `uint16_t`/`uint8_t` cfg_flags bitmap-resident...
  - Worst case: 104 × (1 + 24) = ~2600 bytes per StampInferenceCfgInputs struct
  - Same shape for ModelStampResult struct: another ~2600 bytes
  - Total .bss inflation: ~5200 bytes if both structs auto-gen unconditional. Plan body cites ~1.9-2.5KB; this lowballs by ~2× because it only counts ONE struct, not both.
  - Decision C Approach (A) row "Latency: UNTOUCHED (struct size grows ~1.9-2.5KB; .bss only)" — verifiable claim but unverified
  - Also: per `bitmap-overflow-protection-discipline.md`, when struct-gen happens unconditionally over a master cfg registry, the consumer that walks via metadata-bit filter (`CFG_FIELD_FOR_EACH_SET_BIT`) ONLY reads fields where the bit is set. The struct has the storage but the walker silently skips non-flagged rows. This is correct, but it means ~80% of struct fields are dead-weight per call site. Acceptable in slow-path/boot-only paths; not acceptable on hot path.
  - Per `cfg-derived-consumer-framework.md` line 138 "Adding new cfg field with derived behavior" Step 1 + Step 3 — "all consumer macros automatically pick up the new row. No consumer code touches." This is the "all fields available" pattern referenced in plan body line 74. APPLIED at framework level; consistent with framework intent.
- **Why MED:** the chosen Approach A is structurally correct + matches framework intent. But the cited .bss budget is incomplete (counts 1 of 2 structs) and verification at Step 9 build close (line 273) doesn't explicitly check struct sizes via `sizeof(StampInferenceCfgInputs<64>)` + `sizeof(ModelStampResult<64>)`.
- **Suggested fix:** Add to Step 9 verification gate (line 273): `static_assert(sizeof(StampInferenceCfgInputs<64>) < 4096, "struct-gen .bss budget exceeded; reconsider Approach B");` + `static_assert(sizeof(ModelStampResult<64>) < 4096, "...");` co-located in `ModelInference.hpp` after struct definition. Update Decision C cost row from "~1.9-2.5KB" to "~5KB total across both structs (auto-verified via static_assert)". Document the dead-weight rationale: "struct stores all 104 fields; metadata-bit-filter walkers read only the ~24 flagged at HEAD. Cohort growth from 24 → 104 over future ships extends naturally without struct revision."
- **Effort estimate:** ~5 min plan body amendment + ~10 min static_assert addition in Step 9 verification
- **Cross-ref:** `DESIGN_SPECS/refactor-patterns/bitmap-overflow-protection-discipline.md` (co-located static_assert pattern; same shape applies to struct-size budgets)

### MED-2 — Stamp_format_version bump procedure (Step 1.6.7) is the FIRST canonical wire-format version bump — pattern-codification-lifecycle Stage 2 DRAFT for the procedure DESIGN_SPEC amendment should be written before Step 1.6.7.4

- **Pattern:** pattern-codification-lifecycle.md Stage 2 + wire-format-byte-preservation-discipline.md Layer 4 (round-trip HMAC test) + structural-fix-preferred-decision-framework.md (codify procedure before recurrence)
- **Plan section:** Step 1.6.7 5-substep procedure (line 226-231) + "DESIGN_SPECs landed/amended" `wire-format-byte-preservation-discipline.md` amendment (line 169)
- **Symptom:** Step 1.6.7.4 declares "DESIGN_SPEC amendment to `wire-format-byte-preservation-discipline.md` (procedure section) per spec amendment scope above". This is mandatory per the going-forward rules. But the procedure DESIGN_SPEC amendment is written DURING/AFTER Step 1.6.7.2 (the actual bump), which means contributors implementing Step 1.6.7.2 don't have the codified procedure to follow. Worse: per `pattern-codification-lifecycle.md` Stage 1 + Stage 2, the procedure should be written BEFORE the first canonical application, not after.
  - Plan body line 169 cites the amendment: "(1) extract literal → `STAMP_FORMAT_VERSION_CURRENT` constant; (2) add `MAX_SUPPORTED_STAMP_FORMAT_VERSION` parser bounds check; (3) bump CURRENT N → N+1; (4) v(N) stamp-load failure-mode test fixture (operator-visible mismatch error); (5) postmortem documents the bump rationale." The 5-step pattern IS the procedure; it's documented inline in the plan body but not codified in DESIGN_SPECS.
  - Per `wire-format-byte-preservation-discipline.md` Layer 4 (round-trip HMAC test) — Step 1.6.7.3 (v1 fixture failure-mode test) aligns, but Layer 5 (snapshot test with locked hash) is suspended at HEAD per Layer 5b Option F structural invariants. Plan body should explicitly cite which Layer applies + how Layer 5b's `STAMP_BOUND_CFG_run_generic_invariants()` interacts with the version bump.
- **Why MED:** the procedure exists in the plan body — contributors COULD follow it. But codifying in DESIGN_SPECS BEFORE Step 1.6.7.2 means future stamp_format_version bumps (eventual v2 → v3) inherit the procedure mechanically. Codifying after means the next bump rediscovers it.
- **Suggested fix:** Reorder Step 1.6.7.4 to be Step 1.6.7.0 (pre-coding for the bump). Write the DESIGN_SPEC amendment at plan-time (before pre-coding tag), so the bump procedure is documented FIRST, then the bump itself applies the procedure. Add explicit citation of Layer 4 (round-trip HMAC) + Layer 5b (structural invariants pass) in Step 1.6.7.2's verification block.
- **Effort estimate:** ~30 min for DESIGN_SPEC amendment + ~10 min plan body reordering
- **Cross-ref:** `DESIGN_SPECS/meta-disciplines/pattern-codification-lifecycle.md` Stage 2 (DESIGN_SPEC before first reference) + `wire-format-byte-preservation-discipline.md` Layer 4/5b

### MED-3 — Step 4 CI Check 9 mechanism choice: compile-time static_assert preferred per registry-coverage-ci-check-pattern.md decision matrix when source data is X-macro-driven

- **Pattern:** registry-coverage-ci-check-pattern.md "Mechanism choice: compile-time vs runtime" (lines 84-148) — explicit preference for compile-time static_assert when source data is X-macro-driven
- **Plan section:** Step 4 (line 246-249) — "Add CI Checks 9-12 (cohort eligibility verification)" + "Tools at `tools/check_metadata_bit_to_derived_filter_coverage.py` (per H16)"
- **Symptom:** Step 4 specifies CI Check 9 implementation as a Python tool (`tools/check_metadata_bit_to_derived_filter_coverage.py`). Per `registry-coverage-ci-check-pattern.md` line 119 "Strictly better than Python CI tool when applicable" — the canonical preference is compile-time static_assert when:
  - Source data is X-macro-driven (`FOREACH_*` registry rows enumerable at compile time) ✓ (STAMP_BOUND_CFG_DERIVED-flagged rows ARE in `FOREACH_PER_CORE_CFG_FIELD` + `FOREACH_GLOBAL_CFG_FIELD` + `FOREACH_ML_CFG_FLAG` registries)
  - Predicate can be expressed as X-macro reduction (bitmap OR, count, etc.) ✓ (count of STAMP_BOUND_CFG_DERIVED-flagged rows = count of framework consumer macro walker invocations)
  - Result fits a single `static_assert(expr, message)` evaluable at compile time ✓
  - **Canonical first application at v5.15.5.F.4d.1.A**: H16 enforcement via `FOREACH_DERIVED_FILTER` reduction co-located in `CoreFrameworks/CfgFieldRegistry.hpp` — was "originally planned as `.D` Check 9 Python tool; revised to compile-time at `.F.4d.1` planning per 'compile-time mechanism preferred when source data X-macro-driven' amendment"
  - **Plan body proposes regressing back to Python tool**. Same decision matrix applies to Check 9. Step 4 should mirror the `.A`-shipped pattern (compile-time static_assert) not introduce a Python tool.
- **Why MED:** the Python tool DOES enforce the discipline, but it's a runtime variant when compile-time is available. Per `registry-coverage-ci-check-pattern.md` line 144 "Preference order: compile-time first if data permits; runtime Python tool when compile-time not feasible." Plan body's Check 9 is feasibly compile-time per the criteria; using Python adds maintenance overhead + Python interpreter dependency + skippable via env toggle.
- **Suggested fix:** Reframe Step 4 Check 9 as compile-time static_assert co-located in `CoreFrameworks/CfgFieldRegistry.hpp` (sister to H16's `FOREACH_DERIVED_FILTER` reduction). Concretely: for each row flagged STAMP_BOUND_CFG_DERIVED in master, the framework's per-bit mask `g_*_cfg_stamp_bound_cfg_derived_mask` must have the same bit set. `static_assert(g_per_core_cfg_stamp_bound_cfg_derived_mask.<axis>_popcount == FOREACH_PER_CORE_CFG_FIELD_STAMP_BOUND_CFG_DERIVED_COUNT, "derived filter mask popcount mismatch — adding a row flagged STAMP_BOUND_CFG_DERIVED requires consumer macro to walk via the auto-generated mask; investigate which row is mis-flagged");` Plan body should drop the Python tool reference; cite `registry-coverage-ci-check-pattern.md` line 119 as rationale.
- **Effort estimate:** ~30 min static_assert authoring + ~10 min plan body Step 4 + Verification gate amendment
- **Cross-ref:** `DESIGN_SPECS/framework-patterns/registry-coverage-ci-check-pattern.md` "Mechanism choice" decision matrix + `.F.4d.1.A` H16 first canonical (`CfgFieldRegistry.hpp:1143` per code reference)

### LOW-1 — Decision E (CfgDriftCheck) — SURFACING is correct; recommend explicit cross-ref to per-shape canonical body invariants from Layer 5b

- **Pattern:** wire-format-byte-preservation-discipline.md Layer 5b structural invariants + categorical-tag-applicability-pattern.md "applicability vs runtime cfg gating composable axes" + sidecar-override-pattern-for-registry-auto-flows.md
- **Plan section:** Decision E (line 92-112) + "Canonical sister registries considered" row 7 (line 137)
- **Symptom:** Decision E correctly surfaces the genuinely-sharp trade-off + recommends (E.2) per proportionate-response discipline. Per the plan template + `categorical-tag-applicability-pattern.md` "Categorical applicability vs runtime cfg gating" (line 304), the bandit boundary's `BITMAP_IS_SET(MASK_ML_CFG_BANDIT_ENABLED)` vs `(cfg.bandit_algorithm != 0)` semantic difference is exactly the "applicability vs runtime gating" composability axis from the spec — both must be true for behavior to fire.
- **Why LOW:** Decision E's surfacing is correct + the (E.2) recommendation honors the proportionate-response menu. Documentation can be sharper to give operator the conceptual hook: "this is the canonical applicability-vs-gating composability axis from `categorical-tag-applicability-pattern.md`".
- **Suggested fix:** Add to Decision E rationale paragraph (after line 105): "Per `categorical-tag-applicability-pattern.md` § 'Categorical applicability vs runtime cfg gating': drift-check's `BITMAP_IS_SET(MASK_ML_CFG_BANDIT_ENABLED)` IS the applicability gate (feature on/off); framework's `(cfg.bandit_algorithm != 0)` IS the runtime cfg gating (which bandit selected). These are orthogonal axes; (E.3) collapsing them changes drift detection's gate semantic. (E.2) honoring the orthogonality is the structurally correct middle path."
- **Effort estimate:** ~3 min plan body amendment
- **Cross-ref:** `DESIGN_SPECS/framework-patterns/categorical-tag-applicability-pattern.md` line 304

---

## Verdict: YELLOW

- 2 HIGH findings (HIGH-1 wire-format double-emit risk; HIGH-2 Stage 2 DRAFT DESIGN_SPEC blocker)
- 3 MED findings (struct-gen budget verification; stamp_format_version procedure ordering; CI Check 9 mechanism choice)
- 1 LOW finding (Decision E rationale sharpening)

YELLOW because HIGH-1 is a wire-format byte preservation risk that would manifest as HMAC chain break post-`.B.3` if not addressed at Step 1.6.2 + 1.6.4 + 2 coupling. HIGH-2 is a process discipline blocker (Stage 2 DRAFT before Stage 3 first reference per `pattern-codification-lifecycle.md`).

GREEN-able with:
- HIGH-1 fix: amend Decision D scope to 9 prefixed POST_CFG entries (not 5); add explicit verification gate entry for both prefixed AND unprefixed wire keys via rg grep
- HIGH-2 fix: add Step 0.a (write Stage 2 DRAFT `framework-reason-buffer-extension.md` BEFORE pre-coding tag); make pre-coding trigger item 6 a hard blocker
- MED-1/MED-2/MED-3: address as plan body amendments before pre-coding tag

---

## Decision E pattern verdict

**Decision E (E.2) is the structurally correct middle path** per:
1. `feedback_proportionate_response_to_audit_findings` — full menu surfaced; honest evaluation per option
2. `categorical-tag-applicability-pattern.md` § "Categorical applicability vs runtime cfg gating composable axes" — distinct gate semantics are NOT a Class 18 mirror; they're orthogonal axes. (E.3) collapses orthogonality → operator-visible drift behavior shift
3. `canonical-sister-extension-discipline.md` table line 75-77 menu (A/B/C/D/NO-FOLD) — (E.2) ≈ (C) FOLD into canonical sister + (B) ACCEPT WITH RATIONALE composite ("close most; preserve bandit boundary documented")

Surface to Caramel inline at audit synthesis. (E.2) is the auto-pick if no operator override surfaces a stronger reason for (E.1) or (E.3).

---

## Recommendations

### Address before pre-coding tag (HIGH; ship-blocking if not done)

1. **HIGH-1:** Amend plan body Decision D scope: delete **9** prefixed POST_CFG `inference_cfg_*` entries (`StampBoundModelConstRegistry.hpp:454-483`), not 5. Update line 134 + line 212 + Verification gate line 356.
2. **HIGH-2:** Make Stage 2 DRAFT `framework-reason-buffer-extension.md` writing an explicit Step 0.a sub-action with hard pre-coding-tag blocking. Required by `pattern-codification-lifecycle.md` Stage 2.

### Address during current ship (folded into Steps 0-9)

3. **MED-1:** Add static_assert for `sizeof(StampInferenceCfgInputs<64>) < 4096` + `sizeof(ModelStampResult<64>) < 4096` at Step 9 verification gate.
4. **MED-2:** Reorder Step 1.6.7 — promote DESIGN_SPEC amendment from Step 1.6.7.4 to Step 1.6.7.0 (codify procedure BEFORE first canonical bump). Or write at Step 0 (plan-time).
5. **MED-3:** Reframe Step 4 CI Check 9 as compile-time static_assert (sister to `.A`-shipped H16 `FOREACH_DERIVED_FILTER` reduction), not Python tool. Per `registry-coverage-ci-check-pattern.md` decision matrix.
6. **LOW-1:** Add 2-3 sentences to Decision E rationale citing `categorical-tag-applicability-pattern.md` § "Categorical applicability vs runtime cfg gating composable axes" — sharpens the (E.2) auto-pick conceptually.

### Defer with TECH_DEBT entry

(NONE — all findings are addressable in-ship.)

### CLEAN — no action

- **categorical-tag-applicability-pattern.md** — plan body amends master cfg rows with metadata bits + applicable categories; aligns with discipline
- **bitmap-overflow-protection-discipline.md** — no new bitmap fields proposed; ML_CFG_FLAG count check protected per overflow guard at `MlCfgFlagRegistry.hpp:120+`
- **canonical-sister-extension-discipline.md** — plan body's "Canonical sister registries considered" section is complete (11 candidates with verdicts); per the discipline this is exemplary
- **future-oriented-plan-template.md** — plan body is canonical first NEW application of the template; all required sections present (Design space + Canonical sister + Bug classes closed + DESIGN_SPECs landed + Verification gate + TECH_DEBT auto-write + Pre-coding triggers)
- **autopopulate-pattern-for-production-caller-class.md** — Step 1.5 + Step 1.6.5 swap legacy AUTOPOPULATE → POPULATE_FROM_DERIVED; consistent with cfg-derived-consumer-framework discipline

---

## Notes for /precoding-audit-gate synthesis

These findings should compose with parallel /merge-scan + /parity-check + /readiness + /trace-deps + /anti-spaghetti output. Cross-skill correlation hypotheses:

- HIGH-1 likely surfaces in /parity-check (wire-format byte preservation under stamp_format_version=2)
- HIGH-2 likely surfaces in /readiness Check 27 (pattern application audit) + Check 29 (canonical-sister-extension-discipline citation)
- MED-3 likely surfaces in /merge-scan (computation reuse opportunity — sister to existing `.A`-shipped FOREACH_DERIVED_FILTER mask reduction)

If 2+ skills converge on same finding, escalate severity by 1 tier per `/precoding-audit-gate` synthesis convention.

---

**End of /dod-audit report.** Surface to Caramel for triage; await consult before plan body amendment.
