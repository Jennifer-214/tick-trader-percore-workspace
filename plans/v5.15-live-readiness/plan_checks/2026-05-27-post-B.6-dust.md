---
type: audit-report
audit: dust
scope: wide
sprint: v5.15-live-readiness
ship_context: post-v5.15.5.F.4d.1.B.6 ship close (EngineSharded subfolder split + test_common.hpp extract)
established: 2026-05-27
verdict: MODERATE
operator: Caramel
output_target: plans/v5.15-live-readiness/plan_checks/
sister_audits: [2026-05-25-v5.15.5.F.4d.1.B.4-anti-spaghetti-quarterly.md, 2026-05-25-v5.15.5.F.4d.1.B.4-dod-audit-RE-AUDIT.md]
---

# /dust report — 2026-05-27 — Post-`.B.6` WIDE codebase cleanup sweep

## Verdict — MODERATE

**Summary.** The codebase is in solid shape post-`.B.6`. No bug-class anti-pattern recurrence, no production dead-paths from the `.B.4` centralized-arch surface deletion that escaped cleanup, hot path verifiably untouched. However, the subfolder split exposed pre-existing comment-drift residue that the TECH_DEBT-131 entry undercounted (live operator-facing docs `KNOWN_ISSUES.md` / `PARITY_ISSUES.md` / `HOT_PATH_CHANGELOG.md` / `CLAUDE_INVARIANTS.md` carry 14+ additional stale `EngineSharded.hpp:LINENO` refs not in the 7-item count). Also surfaced 2 dead-code utility functions (~46 LOC) orphaned by `.B.4` centralized-arch deletion that haven't been swept yet.

**Verdict-driving findings:**
1. **TECH_DEBT-131 SCOPE UNDERCOUNTED** — entry enumerates 7 sibling-source-file refs but live (non-archived) DOCS carry 14+ additional `EngineSharded.hpp:LINENO` refs at the same surface (operator-facing doc cohort missed per `feedback_operator_facing_doc_cohort_at_cfg_deletion`).
2. **Dead-code orphans from `.B.4` centralized-arch deletion** — 2 helper functions (`EventLoop_UpdateRollingStateAllCores` + `EventLoop_RebuildAllParameters_PerCore`) have zero production callers (only tests + comments). Third candidate `EventLoop_RebuildAllParameters` (no-suffix variant) has zero production calls + 36 test calls (test-only surface).
3. **Stale "centralized arch" comments** in 5 production files referencing deleted architecture.

**Verdict-stabilizing observations:**
- Hot path discipline maintained: zero `if (engine_arch ...)` / `engine_arch ==` / `ENGINE_ARCH_CENTRALIZED` references in production code — `.B.4` 51-site SHARDED full surface deletion was thorough.
- TECH_DEBT-130 (4 nullptr guards in Async.hpp) — verified accurate (lines 238/286/368/465 — implicit-truthiness guards `if (shared_ptr)` / `if (candle_acc_ptr)`); entry's quoted form `if (gui_quit_ptr != nullptr)` is imprecise but the underlying surface is correct.
- INDEX shim (96 lines) clean — no stale references to reverted Phase B.4.1 sub-paths (`Run/Utilities.hpp` etc.).
- Version.hpp clean — no stale Run-subfolder paths.
- 4 inline globals (test_common.hpp + 3 in Boot.hpp) properly using C++17 `inline` per new DESIGN_SPEC (no `static` regressions).

---

## High-leverage cleanup candidates (do these first)

### 1. **Dead-code sweep — 2-3 orphaned `EventLoop_*AllCores*` helpers** — 30-60 min, ~46-150 LOC removal

**Findings (verified zero production callers via comprehensive grep):**

| Helper | File:Line | Body LOC | Production callers | Test callers | Verdict |
|---|---|---|---|---|---|
| `EventLoop_UpdateRollingStateAllCores` | `CoreFrameworks/ControllerEventLoop.hpp:2225-2236` | 12 | **0** (comment refs only; was for centralized arch) | 0 (no tests use it) | **DEAD — DELETE** |
| `EventLoop_RebuildAllParameters_PerCore` | `CoreFrameworks/ControllerEventLoop.hpp:2259-2294` | 36 | **0** (comment refs only; was for centralized + backtest pre-`.B.4`) | 0 | **DEAD — DELETE** |
| `EventLoop_RebuildAllParameters` (no-suffix) | `CoreFrameworks/ControllerEventLoop.hpp:2096-2161` | 66 | **0** (comments only in BacktestSharded.hpp:384 + ShardedBacktestDriver.hpp:26/90/101/109/119) | **36+ test sites** in `tests/controller_test.cpp` + experiments | **TEST-ONLY — keep but mark** |

**Verification commands run:**
```bash
# Production call sites (excludes tests/, experiments/, comments):
grep -rn "EventLoop_UpdateRollingStateAllCores\|EventLoop_RebuildAllParameters_PerCore" \
  CoreFrameworks/ Strategies/ ML_Headers/ Backtest/ GUI/ DataStream/ main.cpp \
  --include="*.hpp" --include="*.cpp" | grep -v ":[0-9]*://"
# Returns: only definitions + 1 comment ref in Run.hpp:872
```

**Decision matrix per per `feedback_proportionate_response_to_audit_findings`:**

| Option | What | Verdict |
|---|---|---|
| **A — Delete both dead helpers** | Remove 48 LOC + 5 stale comments | **RECOMMENDED** (no callers; rollback trivial; reduces ControllerEventLoop.hpp from 3,836 → 3,790 lines) |
| B — Keep + mark as test-only | Add `// CALLER: tests only post-.B.4` comment | Possible if planning to revive centralized arch — unlikely given .B.4 surface deletion + `feedback_backwards_compat_not_default_concern` |
| C — Keep silently | Status quo | NOT RECOMMENDED (silent dead-code = future-headache; sister to `feedback_future_headache_vs_optimization_scope_framework`) |

**Sister anti-pattern:** Class 26 (Sister-Pattern Co-location at modification time) — these dead helpers are sister to `EventLoop_UpdateEmaPriceAllCores` (which IS used in `Async.hpp:270` + `ShardedBacktestDriver.hpp:348`); the 3-helper cohort needs Update + UpdateRollingState + RebuildAllParameters audit together.

**Conflicts with `.B.7`-`.B.11` umbrella:** **NONE.** `.B.7` is BacktestPanels split (different surface).

**Sister TECH_DEBT to open:** TECH_DEBT-132 — Dead `EventLoop_*AllCores*` helpers post-`.B.4` deletion (sister to TECH_DEBT-029 file-size + `.B.5` umbrella).

---

### 2. **Operator-facing DOCS stale `EngineSharded.hpp:LINENO` ref sweep** — 45-90 min, ~14 ref updates

**TECH_DEBT-131 enumerated 7 sibling-source-file refs but missed an entire cohort:**

| Doc layer | File:Line | Stale ref | Severity |
|---|---|---|---|
| **CLAUDE_INVARIANTS.md** (Reference Doc per CLAUDE.md table) | `:173` | `EngineSharded.hpp` time-exit ~line 1110 | HIGH (canonical reference; CLAUDE.md cites this doc for invariant lookup) |
| **CLAUDE_INVARIANTS.md** | `:239` | `EngineSharded.hpp:drain_with_submit` | HIGH (consumer reads when changing OMS drainer; drain_with_submit is now `EngineSharded/Async.hpp:754 EngineSharded_Async_DrainWithSubmit`) |
| **KNOWN_ISSUES.md** | `:143` | Lambda capture warning at `EngineSharded.hpp:2085` | MED (operator-onboarding doc) |
| **KNOWN_ISSUES.md** | `:319` | `CoreFrameworks/EngineSharded.hpp:2663` | MED |
| **HOT_PATH_CHANGELOG.md** | `:258` | `CoreFrameworks/EngineSharded.hpp:~2906` — sell-side | LOW (changelog; closed entries — leave per archived-changelog discipline) |
| **HOT_PATH_CHANGELOG.md** | `:484-485` | `~88` / `~2860` ensemble REFUSE | LOW (closed; leave) |
| **HOT_PATH_CHANGELOG.md** | `:666, 772, 845` | Surface-added bodies cite EngineSharded.hpp | LOW (closed; leave) |
| **PARITY_ISSUES.md** | `:186` | `CoreFrameworks/EngineSharded.hpp:1244` | MED (open issue context) |
| **PARITY_ISSUES.md** | `:469` | `EngineSharded.hpp:749` (PARITY-026 closure context) | HIGH (operator reference for closed issue verification) |
| **PARITY_ISSUES.md** | `:544, 553` | `EngineSharded.hpp:1075-1240` + `:1157-1240` (boot block) | HIGH (boot ref) |
| **PARITY_ISSUES.md** | `:610` | `EngineSharded.hpp:1180` + `:1200` (exit bandit boot) | HIGH (boot ref) |
| **PARITY_ISSUES.md** | `:655-656, 697` | `EngineSharded.hpp:1108-1131` + `:~2796-2820` + `:1229` (single-zoo boot/hot-swap) | HIGH (boot ref) |
| **PARITY_ISSUES.md** | `:798, 803` | `EngineSharded.hpp:646-694` (Exp3 snapshot publish) | MED-HIGH |

**Per `feedback_archived_changelog_preservation_discipline`:** HOT_PATH_CHANGELOG.md entries are timeless history → **LEAVE** all 5 refs. Per `feedback_operator_facing_doc_cohort_at_cfg_deletion`: the CLAUDE_INVARIANTS / KNOWN_ISSUES / PARITY_ISSUES refs are **operator-facing live docs** → **UPDATE**.

**Recommended update strategy (`feedback_categorical_triggers_over_hardcoded_refs`):** Replace `EngineSharded.hpp:1244` with categorical refs like `EngineSharded/Run.hpp boot block` or `EngineSharded module slow-path` — these survive future grow-and-split cycles. Hard-anchored line refs will break again at next sub-split (e.g., when `.B.6 next` extracts more from Run.hpp).

**Conflicts with `.B.7`-`.B.11` umbrella:** NONE; these are doc-only updates.

**Action:** AMEND TECH_DEBT-131 to enumerate full cohort (currently 7-item; should be 14-item including 7 live-DOCS refs).

---

### 3. **Stale "centralized arch" comments referencing deleted architecture** — 30-45 min, ~5 site cleanup

**Findings (post-`.B.4` deleted ENGINE_ARCH_CENTRALIZED + ENGINE_ARCH_PER_CORE_SLOW from production):**

| File:Line | Stale text | Refers to |
|---|---|---|
| `CoreFrameworks/EngineCommon.hpp:12` | "backtest; centralized arch via AllCores wrapper" | DELETED arch |
| `CoreFrameworks/EngineCommon.hpp:814` | "Fan wrapper: BACKTEST (centralized arch) calls this ONCE per tick" | DELETED arch |
| `CoreFrameworks/ControllerEventLoop.hpp:88` | "centralized arch: Producer thread writes ALL N cores at fan_out" | DELETED arch |
| `CoreFrameworks/ControllerEventLoop.hpp:2254` | "Replaces RebuildAllParameters in centralized arch + backtest" | DELETED arch (in dead-code helper above) |
| `CoreFrameworks/EngineSharded/Run.hpp:1047` | "centralized engine main.cpp:362 does balance check only" | DELETED arch |
| `CoreFrameworks/EngineSharded/Run.hpp:872` | "EventLoop_UpdateRollingStateAllCores helper" | Dead helper above |

**Sister:** B14 unconditionalization latent assumption pillar (post-`.B.4`) — when removing cfg-gate, sister-comment cleanup is part of cohort enumeration.

**Recommended:** sweep with `sed` + manual review; replace with present-tense ("backtest path") OR delete if comment was *describing* the deleted variant.

**Conflicts with `.B.7`-`.B.11`:** NONE.

---

## Medium-leverage items

### 4. **`Run.hpp` `EngineSharded_Run` mega-function body (~2050 raw lines / 1,406 code-LOC)** — 1-2 days IF picked

The `EngineSharded_Run` template (`Run.hpp:382-2434`) is a **single function spanning 2,050 raw lines** / 1,406 code-LOC. Run.hpp passes the file-size discipline by **code-LOC counting** (1,406 < 1,500 threshold per file-size-split-discipline.md v1.3) but per-FUNCTION the body is far over typical "oversized function" thresholds.

**However, this is NOT actionable in `.B.7`-`.B.11` umbrella scope** — the umbrella per CHANGELOG row B.6 was scoped post-revert to: "6 files genuinely need splits (BacktestPanels + ControllerEventLoop + CoreModelZoo + BacktestEngine + DashboardPanels + PortfolioController)". Run.hpp was specifically **DROPPED** after Phase B.4.1 revert (commit 6323c17) per the code-LOC methodology.

**Verdict per `feedback_no_defer_for_effort`:** OK to defer — the function's structure has natural section breaks (lines 372 + 1047 + 1542 + 1641 are commented section boundaries) but cost of further splitting (~1-2 days focused) exceeds umbrella scope. Tag as: **DEFERRED-INDEFINITE** unless future work (.B.X+ further extraction OR runtime-decoupling work in v6.X) makes it productive.

**Sister TECH_DEBT to open:** TECH_DEBT-133 — `EngineSharded_Run` mega-function deferred per code-LOC methodology (sister to file-size umbrella).

### 5. **`EngineSharded_Async_FanOut` 25-arg signature** — already tracked

The fan_out hoist landed at `.B.6` Phase B.2 with **25-arg explicit signature** (per CHANGELOG: "fan_out 25-arg hoist + drain_with_submit + bench histogram"). Verified in `Async.hpp:135-225`:

```cpp
inline bool EngineSharded_Async_FanOut(
    // Explicit lambda params: 4
    // By-value captures: 8
    // By-ref captures: 11
    // File-local-static args: 2+
    ...
)
```

Per `feedback_enumerate_block_scope_statics_before_hoist` (Class 35), the 25-arg signature is **expected** — final result of comprehensive block-scope-static enumeration during Phase B.2. This is GOOD — proves the discipline catches what would otherwise be silent corruption. **NO ACTION** beyond keeping as worked example for B18 pillar at next promotion candidate.

### 6. **`Async.hpp` 460 code-LOC** — under threshold; no action

Async.hpp is 905 raw lines / 460 code-LOC = passes 1,500 code-LOC discipline. The 4 nullptr-guards per TECH_DEBT-130 are the only flagged surface; deferred per H20 sister + branchless-dispatch discipline (cost is ≤0.4μs worst-case in producer-thread).

### 7. **Version-stale comment scan in CoreFrameworks/** — 659 hits but mostly load-bearing

659 raw `v3.X / v4.X / v5.X-not-current` refs in CoreFrameworks/Strategies/ML_Headers — these are mostly **load-bearing history** (changelog-style comments documenting when a feature/refactor landed). Per `feedback_categorical_triggers_over_hardcoded_refs`, version-anchored comments are FINE when they're describing historical context, not when they're directives. Sample (HOLD per discipline):
- `GateParameters.hpp:127`: `// --- v5.4.0 Phase 3.3 trailing TP ratchet ---` → section marker, KEEP
- `CoreLatencyStats.hpp:59`: `// v4.7.36: lifetime log-bucket histogram` → context anchor, KEEP

**NO ACTION**.

---

## Low-leverage / cosmetic

### 8. **Comment-only refs in `Run.hpp` (`Run/Utilities.hpp` etc.) from reverted B.4.1 sub-split** — 0 work needed

Verified: INDEX shim (`EngineSharded.hpp`) + Version.hpp are CLEAN. Recent commits' `git log --name-only` shows `Run/Utilities.hpp` etc. but those files **do not exist on disk** (verified `ls EngineSharded/Run/` returns nothing). This is git history; no actual stale refs in tree.

### 9. **Empty plan-of-record for `.B.6` postmortem audit findings flagged as YELLOW** — no work

CHANGELOG row notes: "Phase C 4-agent comprehensive audit verdict: GREEN with 2 YELLOW/cosmetic findings logged for post-paper-test triage." Verified TECH_DEBT-130 + TECH_DEBT-131 are open with explicit triggers. NO action; everything captured.

### 10. **NK_* enum constants check** — clean

Spot-checked NK_KILL_TRIGGER / NK_ORPHAN_DETECTED etc. — all NK_* constants are referenced. No sprint-debt orphans.

---

## Acceptable noise (don't fix unless you're already in the file)

- **WIP-B5.1 commit** `9dcb0db` (Async.hpp include fix for `-Wc++20-extensions` warning) — already fixed; commit captured the polish.
- **Single TODO marker** at `Async.hpp:441`: `// TODO: move kill switch eval to drainer thread, or use an...` — load-bearing optimization note; matches `feedback_categorical_triggers` (categorical TODO describing future-direction context, not stuff-to-do). KEEP.
- **`PortfolioController.hpp` legacy single_core comments** (lines 20/357/628) — comments describe the legacy single-core path that `main.cpp` still uses (`PortfolioController_Init` etc. at `main.cpp:333+` is the legacy path, gated on `engine_mode=single_core`). These are accurate, not stale. KEEP.

---

## TECH_DEBT entries — change recommendations

| ID | Status | Action |
|---|---|---|
| **TECH_DEBT-130** | OPEN | **VERIFIED ACCURATE** — lines 238/286/368/465 in Async.hpp confirmed via grep; minor copy-edit needed (entry says `if (gui_quit_ptr != nullptr)` but actual code is `if (shared_ptr)` + `if (candle_acc_ptr)`); OPTIONAL polish |
| **TECH_DEBT-131** | OPEN — AMEND | **SCOPE UNDERCOUNTED** — entry enumerates 7 sibling-source-file refs but additional 14+ refs exist in live operator-facing DOCS (KNOWN_ISSUES.md / PARITY_ISSUES.md / CLAUDE_INVARIANTS.md). **Recommend AMEND** to enumerate full 21-ref cohort + cite `feedback_operator_facing_doc_cohort_at_cfg_deletion` as parent meta-rule + cite `feedback_categorical_triggers_over_hardcoded_refs` as update-strategy guide (categorical refs over line-anchored) |
| **TECH_DEBT-132 (NEW)** | OPEN | **OPEN NEW** — Dead `EventLoop_UpdateRollingStateAllCores` + `EventLoop_RebuildAllParameters_PerCore` helpers (~48 LOC; zero production callers post-`.B.4` centralized-arch deletion). Sister: TECH_DEBT-029 file-size discipline; Class 26 cohort co-location. Severity LOW (compiles fine; no runtime impact). Trigger: address at next file touch OR fold into `.B.7+` if natural |
| **TECH_DEBT-133 (NEW)** | OPEN-DEFERRED-INDEFINITE | **OPEN NEW** — `EngineSharded_Run` mega-function 2,050 raw lines / 1,406 code-LOC (under file-size threshold per v1.3 methodology but per-fn body is far over typical limits). Sister: TECH_DEBT-029. Trigger: future runtime-decoupling work (v6.X) OR per-core-drainer-architecture work (TECH_DEBT-129) creates natural extraction surfaces |
| **TECH_DEBT-134 (NEW)** | OPEN | **OPEN NEW** — Stale "centralized arch" comments in 5 production-file sites referencing `.B.4`-deleted architecture (EngineCommon.hpp:12/814 + ControllerEventLoop.hpp:88/2254 + Run.hpp:1047/872). Severity LOW; cosmetic; fold into next stale-comment audit OR `.B.7+` work at adjacent sites. Sister to TECH_DEBT-131 + B14 unconditionalization pillar |

---

## Punch list ranked by ROI (cleanup effort vs maintainability gain)

| Rank | Item | Effort | Maintenance gain | ROI |
|---|---|---|---|---|
| **1** | **Delete 2 dead `EventLoop_*AllCores*` helpers** (TECH_DEBT-132) | 30-60 min | HIGH (closes Class 26 cohort drift; reduces ControllerEventLoop.hpp by 48 LOC; eliminates future "is this still used?" question) | **HIGHEST** |
| **2** | **AMEND TECH_DEBT-131 scope** to include 14+ live-DOCS refs | 15-30 min | HIGH (enables proper sweep at next /metadata-audit; closes operator-facing-doc cohort discipline gap) | HIGH |
| **3** | **Sweep TECH_DEBT-131 + AMENDED cohort** (21 refs in 7 src + 4 live-DOCS) | 45-90 min | MED (cosmetic; comments only; but enables grep-by-categorical-trigger discipline for future-proofing) | MED-HIGH |
| **4** | **Sweep 5 "centralized arch" stale comments** (TECH_DEBT-134) | 30-45 min | MED (closes B14 cohort late-residue; comment-only) | MED |
| **5** | TECH_DEBT-130 polish (4 nullptr guards via `__builtin_expect` or `#ifdef USE_IMGUI_GUI`) | 30-60 min | LOW (runtime-dead under build flag; ≤0.4μs producer-thread cost) | LOW (defer per post-paper-test trigger) |
| **6** | DEFER `EngineSharded_Run` mega-function split (TECH_DEBT-133) | N/A | N/A | N/A (defer to v6.X or .B.X+ extraction) |

---

## Conflicts with queued `.B.7`-`.B.11` umbrella work

**NONE** — none of the dust findings conflict with file-size umbrella scope:

- **`.B.7` (BacktestPanels.hpp 4,639 code-LOC split)** — touches `Backtest/` directory; dust findings touch `CoreFrameworks/` + `DOCS/`. ZERO surface overlap.
- **`.B.8` (ControllerEventLoop.hpp 1,748 code-LOC split)** — touches `CoreFrameworks/ControllerEventLoop.hpp` where dead helpers live (TECH_DEBT-132) + stale comments (TECH_DEBT-134 lines 88 + 2254). **OPPORTUNITY:** if `.B.8` is rescoped to fold in TECH_DEBT-132 + the .hpp:88/2254 stale comments, that's "address at next file touch" per the natural-fold heuristic in `feedback_future_headache_vs_optimization_scope_framework`. NO conflict; pure positive synergy.
- **`.B.9`-`.B.11` (CoreModelZoo + BacktestEngine + DashboardPanels + PortfolioController)** — none touch the dust-finding surface.

**Recommendation:** Operator-triage whether to fold TECH_DEBT-132 + TECH_DEBT-134 ControllerEventLoop sites into `.B.8` Phase amendments (natural same-file work) OR open as standalone mini-ship between `.B.7` and `.B.8`.

---

## Discipline references

- `feedback_operator_facing_doc_cohort_at_cfg_deletion` (TECH_DEBT-131 amendment)
- `feedback_categorical_triggers_over_hardcoded_refs` (update strategy for refs)
- `feedback_archived_changelog_preservation_discipline` (HOT_PATH_CHANGELOG entries LEAVE)
- `feedback_count_code_loc_not_total_lines` (Run.hpp justification for non-split)
- `feedback_future_headache_vs_optimization_scope_framework` (dead-helper sweep priority)
- `feedback_consult_on_audit_findings` (INFORMATIONAL output; operator triages)
- `feedback_proportionate_response_to_audit_findings` (decision matrix per finding)
- `feedback_iteration_spiral_signals_audit_meta_gap` (B14/B15 pillars at `.B.6`)

---

**End of /dust report — 2026-05-27 — Post-`.B.6`.**

Output: INFORMATIONAL only. Per `feedback_consult_on_audit_findings`, operator triages with main session.

Next: `.B.7` BacktestPanels split start; folding TECH_DEBT-132/-134 into `.B.8` ControllerEventLoop scope is operator decision.
