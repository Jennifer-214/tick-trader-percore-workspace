---
type: audit-report
skill: /blindspot-scan
date: 2026-06-09
target_plan: plans/v5.15-live-readiness/subplans/2026-05-30-v5.15.5.F.4d.1.E-money-numeric-core-foundation.md (v0.3, post-A.5 re-audit)
scope: Ship-B REMAINING work only (decimal money; D-97..D-167 DECIDED items out of scope)
head: 0e48150 (v5.15.5.F.4d.1.E.0.8; suite 3246/0)
verdict: YELLOW — 4 SILENT-RISK pillar findings (all NEW vs the 2026-06-01 pass; that pass's 2 SILENT risks verified structurally LANDED at HEAD), no RED / architecture-killer
predecessor_scan: plan_checks/blindspot-scan-2026-06-01-11.md (B1 INT_MIN UB → UBSan lane + D-147 saturate LANDED at E.0.7; B6 trait split → disjoint is_fp_binary_v/is_fp_decimal_v LANDED, verified FixedPointN.hpp:97-107 + CfgFieldDispatch entry asserts :63-66 exclude decimal)
---

# /blindspot-scan — Ship-B (decimal money) — 2026-06-09

## Summary

- Pillars fired: 15 (B1-B15; B17/B18/B19 checked for trigger-applicability — none fire)
- GUARDED-BY-BUILD: 5 (B2, B5, B6-at-HEAD, B7, B11)
- SILENT-RISK: 4 (B1, B8, B10, B12-low)
- N-A / IRRELEVANT: 6 (B3, B4, B9, B13, B14, B15)
- June-1 differentiation: this pass walked the taxonomy FRESH against Ship-B remaining scope. The June-1 SILENT pair (INT_MIN UB; trait discrimination) is verified guarded at HEAD (UBSan lane + D-147 saturate probe ~`controller_test.cpp:24455+`; disjoint traits `FixedPointN.hpp:97-107` with `is_FPN_v` retired; all 8 dispatcher entry `static_assert`s gate `is_fp_binary_v` and EXCLUDE decimal → red-build, verified `CfgFieldDispatch.hpp:63/180/233/274/331/400/456/505`). Not replayed.

## Per-pillar verdicts

| Pillar | Verdict | Finding (evidence at HEAD) |
|---|---|---|
| **B1** type-change cascade | **SILENT-RISK** | The TYPE cascade itself is LOUD (O-1 disjoint types: decimal fails every dispatcher entry assert; `FPN_*` fns take `FPN_Binary<F>` only → cross-use red-builds). Two body-content surfaces are NOT build-caught: **(a) decimal parse-branch value semantics** — `tt::cfg_parse_field` binary branch (`CfgFieldDispatch.hpp:72-80`) does `parse_double_fast → ÷100 if KIND_DOUBLE_PCT → clamp(double bounds) → FromDouble`; a decimal branch that does the same via ToDouble/FromDouble detour COMPILES, passes the family assert, and silently re-imports the lossy path this ship exists to kill. Pct-scale must be exact digit-shift (÷100 of a 10⁸-scaled int — exact for ≤6dp venue inputs); clamp must be decimal-exact, not double-domain. **(b) MANUAL_PARSER bypass:** `fee_rate_maker`/`fee_rate_taker` carry `MANUAL_PARSER` (`CfgFieldRegistry.hpp:128/:140`; walker filter `:2150`) — they SKIP the registry parser entirely. The type flip red-builds the manual assignment site, but the red-build forces *a* change, not the RIGHT change (lazy fix = double-detour conversion, silently lossy on the most money-central cfg values). Plan's M1/B3 rows name neither surface. |
| **B2** field-name collision | GUARDED-BY-BUILD | `commission` is FREE on `Order`/`OrderPreResolved`/`OrderEvent` (verified; only `OrderResult.commission` exists — different struct, no collision). No new registry rows → `check_field_name_uniqueness.py` unaffected. `sizeof(OrderPreResolved<64>)==32` (`Order.hpp:148`) + `sizeof(Order<64>)==256` (`:403`) break LOUD on field add → forces deliberate re-derive. |
| **B3** transitional coexistence | N-A | Both instantiations are PERMANENT by design (not transitional). Mid-migration mixed-typed money fields red-build at cross-uses (O-1) until consistent. No struct growth concern (16B→16B; +16B commission on Order = RAM-pool trivial). |
| **B4** Surface-G applicability | IRRELEVANT | No `has_<name>` struct-gen in Ship-B scope. (The plan's *other* B4 — price-domain decision — is a SHAPE-layer open the plan already flags "Pick one"; its cascade is LOUD: `FPN_LessThan(tick.price, bg_price_threshold)` at `GateParameters.hpp:171/:198` red-builds whichever way, since compare fns are type-bound. Settle pre-code; enumerate the chosen option's container set Tick/GateParameters/ExecutionCore-hot-cluster/ema_price-ingress.) |
| **B5** compile-time scaling | GUARDED-BY-BUILD | One new instantiation domain (`<10,8>`) × ~8 dispatcher branches + op set — far under the 1000-instantiation threshold. |
| **B6** STORAGE_T variant coverage | GUARDED-BY-BUILD at HEAD, **enumeration residue → punch #6** | Entry asserts exclude decimal at all 8 trait-gated fns (verified). Residue: the plan's B3 row names "3 dispatchers + drift-compare" but the tt:: family is **8 template fns** — parse `:57`, save `:180`, render `:233`, +1 `:274`, emit `:331`, `cfg_struct_cast` `:400` (dual-type SrcT/DstT chain `:420-425`), stamp-compare ×2 `:456/:505` (dual-type chains `:463-467/:514-519`). `cfg_save_field`'s decimal branch is round-trip-LOAD-BEARING (10⁸-scaled ints exceed double's 2⁵³ exact range → `%.17g` does NOT guarantee save→load round-trip; save must emit exact decimal string, not just the wire emit). Dual-type chains need every decimal×{binary,float,decimal} combo dispositioned or `always_false`-rejected. Extend `check_storage_t_coverage.py` PER-FN (it exists; currently checks variant-branch presence from the 2 master registries). |
| **B7** include topology | GUARDED-BY-BUILD | Decimal lives in `FixedPointN.hpp` (no new edges). FP64 absorb REMOVES the one edge (`FixedPointN.hpp:1246`). #5/#6 need no new cross-dir includes (quantize takes step params; `SymbolFilters` stays in BinanceOrderAPI). |
| **B8** type-sensitive consumers | **SILENT-RISK** | **Replay cannot reproduce LIVE-booked commission.** Verified: `OrderEvent` carries NO fee/commission field (`OrderEventLog.hpp:79-92` — price/qty/tp/sl only); replay RECOMPUTES fees from a SINGLE passed-in `fee_rate` (`:636`, muls `:657/:676` — note: single rate for entry+exit also diverges from production's per-Order maker/taker `pre_resolved` rates TODAY). Post-B1 (LIVE books venue-reported `n`/`N` — today explicitly non-authoritative per `BinanceUserData.hpp:360-361` comment), replay-from-event-log CANNOT match LIVE P&L by construction: reported ≠ computed is the exact drift B1 closes. The C2 "replay==production rounding differential" acceptance row slams into a VALUE-SOURCE divergence rounding can't fix — and if the differential only exercises paper/computed fees, it PASSES while live replay silently drifts. Raw-byte persist sites (`fwrite`/`fread`/memcmp/SHA) are additionally type-blind → compile unchanged regardless of semantic flip → only the epoch gates guard them (see B10). |
| **B9** unverified audit claims | N-A + 1 correction | 65 anchors tool-re-derived 2026-06-09; my spot-checks all verify (Tick `:32-43`, GateParameters `:171/:198`, BinanceUserData `:361/:378`, Reconcile `:546`, dispatchers, registry `:674-675`, OrderEventLog header). ONE plan claim is false at HEAD: § Architecture lists **Floor/Ceil/Round as "radix-agnostic ops (REUSE the certified bodies)"** — (i) they are SCALE-dependent (binary = frac-word mask; decimal = mod-10⁸ via divmul remainder), and (ii) the HEAD generic bodies (`FixedPointN.hpp:1102/:1132/:1163`) are DEAD sign-magnitude relics referencing `.w[]`/`.sign`/`FRAC_WORDS` — members the 16B core doesn't have; NO `<64>` specializations exist → uninstantiable, zero call sites. "Reuse" them is impossible; a fresh GENERIC body with an embedded `>>FRAC`/mask would compile for `<10,8>` silently wrong. |
| **B10** struct layout drift | **SILENT-RISK** | The hazard is INVERTED from the usual B10: layout does NOT change (16B→16B, no padding, `has_unique_object_representations` still holds, F-076 `:475` fine, R1 offset ladder unaffected) — which means **NOTHING mechanical fires when the value ENCODING flips** (2⁶⁴-scale → 10⁸-scale ≈ 1.8×10¹¹ misread factor). Two unguarded epoch surfaces: **(a) snapshot versions** — the D-144 test floor is `>=13/9/6` *explicitly forward-compatible* (`controller_test.cpp:~24446-24453`: ">= keeps it forward-compatible with any later ship") and the discipline text keys the bump to "the same ship **sizeof(FPN)** changed" — sizeof does NOT change at Ship B → no static_assert breaks, no sizeof trigger, the `>=` test passes UN-bumped → a Ship-A-era binary-money snapshot loads cleanly into the decimal engine. **(b) OrderEventLog has NO version field at all** (`OrderEventLogFileHeader` `:126-131`: magic "OMSEL01\0" + `fpn_width`(=64, unchanged) + `entry_size`(16B fields, unchanged) + `reserved[2]`) → old binary-scaled event logs load + REPLAY into the decimal engine silently misscaled. The plan's R1 ladder covers OrderEventLog only via entry_size asserts — which don't fire at equal size. |
| **B11** if-constexpr context | GUARDED-BY-BUILD | Radix forks live inside template fns (`FixedPoint<R,F>` ops; tt:: dispatchers already template-context). Non-template X-macro expansion contexts unchanged by Ship B. The D-151 deduction lesson is already structurally settled (FPN_Binary = concrete specialization, not alias); decimal `FixedPoint<10,8>` deduces normally in `template<int R,int F>` fn params. One coding-time rule: `FPN_Binary<64>` and `FixedPoint<2,64>` are DISTINCT layout-identical types both matching `is_fp_binary_v` — bridge only via `.v` member copy, never `reinterpret_cast` (H13). |
| **B12** row ordering | SILENT-RISK (LOW) | Emit order = registry row order; the STORAGE_T flip is in-place. BUT the money-golden + stamp HMAC both REGENERATE at this epoch → an accidental row reorder during the ~30-row sweep would be silently ABSORBED into the new goldens (B12's normal net is DOWN for exactly this ship). Cheap guard: freeze row ORDER explicitly (no regrouping money rows) + one-shot emitted-key-sequence diff pre/post BEFORE regen. |
| **B13** cross-walker uniqueness | IRRELEVANT | No struct-generating walker changes; no new fields enter ModelStampResult via registries. |
| **B14** deletion ordering | N-A (below threshold) | FP64 absorb = **2-file cohort**, not ≥3: `FixedPoint64.hpp` (defs) + `FixedPointN.hpp` (`#include` at `:1246` + 3 COMMENT refs `:1263/:1288/:1298`). Zero build-file/test refs (rg-verified). Post-C1-hoist, `FP64_*` appears call-site-DEAD (H21: compiled-in dead code) → sequence: remove the include first, red-build probe confirms dead, then delete file + scrub comments. |
| **B15** unconditionalization | N-A | No cfg-gate removal in scope (Check-F un-bypass = CI-hook re-enable, not engine unconditionalization). |
| B17/B18/B19 | N-A | No header decomposition / lambda hoist / doc sweep in Ship-B scope. IF the open `FPN_*` op-family naming decision (D-163 non-goal) lands as a rename inside Ship B → fire `rename-ship-methodology` as its own pass (B19-adjacent), not ad-hoc. |

## Punch-list (pre-coding amendments, ordered)

1. **[HIGH·silent·persistence-epoch] Ship-B snapshot version bumps have no mechanical trigger** (B10a). Add an explicit R3-B row to the plan: CONTROLLER/SHARDED/PORTFOLIO → ≥14/10/7, **raise the controller_test `>=` floor constants in the SAME COMMIT as the first money-field type flip** (the floor must rise at each epoch to have teeth), and extend the D-144 discipline wording from "sizeof changed" to "value-ENCODING (scale/radix) changed". ~15 min plan edit.
2. **[HIGH·silent·persistence-epoch] OrderEventLog epoch gate absent** (B10b). Amend the persistence row: bump magic `"OMSEL01"`→`"OMSEL02"` (or claim a `reserved[]` word as a version) when OrderEvent money fields flip scale; add OrderEventLog to the "old artifacts version-rejected" acceptance row. ~10 min plan edit.
3. **[HIGH·silent·consumer-flow] Booked-commission replay parity** (B8). The plan must DECIDE + enumerate: persist the booked commission per fill event (new OrderEvent field → entry_size changes → header gate rejects old logs LOUD, good; flows into `Portfolio_FromEventLog` + `OrderEventLog_ReplayInto` so replay BOOKS rather than recomputes) OR explicitly scope the C2 replay==production differential to computed-fee paths and document the live-booked replay limitation. Note the differential must include a reported≠computed scenario or it proves nothing about the B1 fix. ~30 min plan amendment.
4. **[HIGH·silent·value-semantics] Decimal parse-branch spec** (B1). Plan must pin the decimal `cfg_parse_field` branch semantics: exact `FromString` → EXACT pct÷100 (digit-shift) → decimal-exact clamp → store; FORBID any ToDouble/FromDouble detour inside the branch (CI-greppable); enumerate the **MANUAL_PARSER money rows** (`fee_rate_maker`/`fee_rate_taker`) as a second parse surface needing the same treatment; add a cfg-file→stored round-trip differential over ALL money rows (incl. MANUAL_PARSER + `cfg_save_field` save→load) to the D-100 gate. ~30 min plan edit.
5. **[MED-HIGH·plan-claim + compiles-if-generic] Per-op radix-disposition table** (B9/B1). Correct § Architecture: Floor/Ceil/Round are scale-dependent, and the HEAD generic bodies are dead sign-magnitude relics (uninstantiable on the 16B core). Add a table classifying every op: agnostic-on-`.v` / radix-forked (`if constexpr` + `always_false` else) / binary-only (`static_assert(RADIX==2)` — Exp/Sqrt/Log/Sin/Cos so accidental decimal use red-builds, "never needs" ≠ "cannot call"). Decimal rounding routes ONLY through #4's `(q,r)`. Disposition the dead generics (delete or port — they're sign-mag bodies incoherent with the retained-for-D-129 width story). ~45 min.
6. **[MED·enumeration] Full tt:: dispatcher family** (B6). Replace "3 dispatchers + drift" with the grep-derived 8-fn family (incl. `cfg_save_field` round-trip-load-bearing exact-string emit, `cfg_render_field` H4-exempt display, `cfg_struct_cast` + 2 stamp-compare dual-type chains with every decimal×{binary,float,decimal} combo dispositioned); extend `check_storage_t_coverage.py` per-fn both-branch. ~20 min plan edit.
7. **[LOW] B12 net-down note**: freeze FOREACH money-row ORDER during the STORAGE_T sweep; one-shot emitted-key-sequence diff pre/post before golden/HMAC regen. ~5 min.
8. **[LOW] FP64 absorb sequencing** (B14-lite): remove `FixedPointN.hpp:1246` include first → red-build probe proves call-site-dead → delete + scrub 3 comment refs. ~5 min note.

## Recommended next move

**(X) Audit-first: amend the plan body (~2h total) before Ship-B's focused `/precoding-audit-gate`.** Items 1-4 are silent-corruption-class on capital/persistence surfaces (heavier-default posture, D-77); all are plan-text amendments, no code. Items 5-6 fold naturally into the already-planned #4/#5/B3 sections. None block the settled architecture (decision layer untouched — this is implementation-detail hardening, as designed).

## Inflection check

- Iteration count since last meta-gap codification: 2nd fire against this plan body (June-1 → June-9); both passes' findings map cleanly into EXISTING pillars (B1/B6/B8/B10/B12) — **no NEW B-row needed**.
- Pattern worth carrying forward (not a new pillar; a B10 sub-note): "layout-UNCHANGED semantic-encoding flip" is the inverse of classic B10 — when sizeof/offsets survive a migration, every size/offset-keyed guard goes quiet exactly when the version/epoch gate matters most. Candidate one-line amendment to the B10 Detection mechanism at next taxonomy touch.
- June-1's 2 SILENT risks: both verified structurally landed at HEAD (mechanical guards working as designed — the "mechanize the pillar" move held).
