---
type: handoff
status: active   # THE one live handoff. Supersedes 2026-07-19-E.1.2.B-0.1.5-0.3-shipped-next-0.2-contract-gate-handoff.md. ≤1-active enforced by check_handoff_active_singleton.py.
ship_tag: E.1.2.B
plan_type: mixed
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: make every toolchain surface (CLI · local-CI · plugin · doc-viewer · AI) a thin consumer of the ONE core, versioned 0.x→1.0.0 — "the code becomes the IDE", felt in the editor
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-07-19-E.1.2.B-0.1.5-0.3-shipped-next-0.2-contract-gate-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [DESIGN_SPECS/framework-patterns/doc-intelligence-toolchain-architecture.md, DESIGN_SPECS/meta-disciplines/calibration-corpus-non-vacuity-discipline.md, DESIGN_SPECS/framework-patterns/standardized-tool-io-envelope-and-payload.md]
coding_status: 0.2 BB-1 DESIGN LOCKED + data artifacts BLESSED · NO CONSUMER CODE WRITTEN · wiring is the next action
---

# E.1.2.B `0.2` — the corpus contract is designed, blessed, and swept; next = WIRE IT

**Created:** 2026-07-19 (late) · **Branch:** `feat/v5.15-live-readiness`

> **⚠️ READ THIS FIRST.** This session produced **zero consumer code** and **eleven decisions**. That is not a stall — the design went through an armed I→A cascade, an operator-called precedent sweep, and an adversarial anti-pattern sweep, and **each one materially changed the shape.** Three separate times, a design that looked correct was refuted by measurement. **Do not skip to implementation without reading § "The five things that will bite you."**

## State at handoff write — THREE repos, ALL pushed

| Repo | HEAD | Remote | Note |
|---|---|---|---|
| Engine `FoxML_Trader_v2` | `4c076ed` | ✅ synced | **ZERO engine code touched all session** (dev-plane only) |
| Workspace | `9848766`+ (this close's commits) | ✅ synced | plans/specs/decisions/tools (`tools/` is a SYMLINK into here) |
| Plugin `fox-symdeps.nvim` | `6dcbbd7` on `feat/hud-robustness` | ✅ synced | its own nested repo; gitignored by the workspace |

**All three remotes are ALIVE** (`Jennifer-214`). ⚠️ The memory `project_workspace_remote_gone_commit_only` was **STALE and has been corrected** — it said "no remote, never push," which had left 77 workspace commits unbacked-up. Sync/close now **DO push**. Verify remote state with `git ls-remote --exit-code origin HEAD`, never by assumption.

**Untracked scratch — DELETE the two probe artifacts before doing golden work:** `tmpfsvq5cyu.cpp` and `tmpo0ujx6rw/` are compile-probe leftovers, and they are exactly what poisoned the first bless (D-396). They are harmless to the CURRENT goldens (which pin git-tracked entries only), but any future ad-hoc probe leaves another `tmpXXXXXX.cpp`, so clearing them is hygiene not ceremony. Leave `build_clangd/`, `resume`, `note` (operator's).

## What SHIPPED this session

**Data artifacts (blessed, committed, NOT yet read by anything):**
- `tools/lib/corpus_contract.json` — the single-sourced corpus membership rules. Profiles (`validate` / `derived_facts`), explicit exclusions, symlink policy, sort rule, the anchored converted-selector, path-argument semantics, the `python_sources` tombstone.
- `tools/goldens/corpus--validate.txt` (**171**) + `corpus--derived_facts.txt` (**166**) — **the toolchain's first output goldens.**
- `tools/goldens/README.md` — the golden / exception-baseline / ratchet / rules three-way distinction, at the point of confusion.

**Doc corrections:** 13 stale STATUS sentinels swept · the handoff HEAD table · `tools/CLAUDE.md` gained **THREE harvest waves** (6 from `0.1.5`/`0.3`, 6 from the `0.2` I→A sweep, 5 from the precedent+anti-pattern sweeps — incl. the `JVal`-no-bool-kind rule that caused a real defect) · the tool-I/O spec gained a KNOWN-GAP block (the envelope frame is unvalidated) · `public-private-boundary-and-ecosystem-discipline.md` **SUPERSEDED** (it stated the code-only-public law as governing, ~2 weeks after everything went all-private).

**Ledgers — FOUR opened:** TECH_DEBT-**252** (plugin repo runs no git hooks) · **253** (`check_schema_version.py` scans ZERO `.py` — a wired HARD gate structurally unreachable through the `tools/` dir symlink) · **254** (`rebuild_doc_indexes` silently drops one-off spec types; `DESIGN_SPECS/README.md` claims 15 types, renders 11; `stage: 6-cadence-locked` has zero users while `CLAUDE.md` advertises it) · **255** (`check_identifier_retirement --update` overwrites the H21 golden with NO diff and NO confirmation — the OPPOSITE posture to D-394, so the two goldens in-tree currently disagree on re-bless safety).

## ▶ NEXT ACTION — wire the contract (BB-1), in this order

Nothing reads the contract or the goldens yet. **The design is locked; this is execution against a locked spec.**

1. **Python** — `engine_source_files(profile=…)` + **`resolve_paths()`** + `corpus_contract()` in `check_code_tag_blocks.py`; route the `--paths` branch at `:598-601` through `resolve_paths`. Both new fns are additive with a defaulted arg, so the two existing callers are source-compatible.
2. **C++ twins** in `foxtag.hpp` beside `scan_files:790`; route `foxtag_main.cpp:32`. Give `read_file` (`:47-53`) an error path so a directory becomes a real failure instead of `{}`.
3. **`parity_check.sh` §1b** — the path-argument parity section (it has NONE today; `:20-21` invokes both bare, which is exactly why every path-arg divergence is ungated). Follow the §3c idiom: guard deps · assert non-emptiness BEFORE comparing · print `[RAN]` · carry the skip into the verdict.
4. **The bless path** — TTY-gated per D-394.

## The five things that will bite you

1. **⭐ D-393 pt 3 named the WRONG FIX SITE, and C-395 corrects it.** It says "extend `engine_source_files()`." **The vacuous-green does not live there** — the `--paths` branch bypasses the enumerator entirely (`check_code_tag_blocks.py:598-601`; `foxtag_main.cpp:32`, with the silent skip one line below at `:37`). **Implementing D-393 as literally written lands a real fix that leaves build-blocker 1 FULLY OPEN, and it will look done.** The contract must export a path RESOLVER.
2. **⭐ SIX enumerators in TWO families, not three.** `check_cache_layout.py:442`, `check_register_fit.py:108`, `check_schema_version.py:72` each re-copy the body and add `schema_golden` + `DOCS`. **The profile split is LOAD-BEARING, not cosmetic** — flatten it and the cache-layout STANDING-CI gate REDs with `ExecutionCore [SIZE] 192B vs real 68352B`, and the natural-looking fix writes 68352B **into a copy-paste template**, propagating a wrong derived fact into every future conversion. Do not collapse it as over-engineering.
3. **⭐ SCAN population ≠ PIN population (D-396).** The enumerator is gitignore-blind (D-393 pt 2, unchanged). The GOLDEN pins **git-tracked only**. Getting this wrong is not theoretical — the first bless pinned two mkstemp scratch files and made the gate green on exactly one disk.
4. **`--bless` REQUIRES A TTY (D-394)**, shows the per-file diff, and **non-TTY HARD-REFUSES rc=2**. No `--yes` escape hatch. ⚠️ **The plan body's `0.2` prose previously said the opposite ("NO confirmation prompt") — corrected, but if you see that phrasing anywhere it is stale.** The property this buys: per D-385/M10 a delegated agent is structurally *incapable* of blessing a golden.
5. **`_encoding_law`: ALL SCALARS ARE STRINGS in the contract JSON.** The C++ `JVal` has `Kind {STR, ARR, OBJ, OTHER}` and **no bool/number kind**, so a bare `true` reads as an EMPTY STRING on exactly one of the two readers — silently. Measured with a compiled probe. **Do NOT "clean this up" to native JSON types.**

## Decisions this session (D-391 → D-396, C-391 → C-396)

- **D-391 — WITHDRAWN.** "Anchor, don't layer" was already codified 7 weeks earlier as `feedback_golden_master_over_reimplemented_oracle` (D-74). See C-391.
- **D-392 / C-392** — `0.2` scope corrections; then (e) proven **NOT** a lockstep C++/Python change (measured: hardening drops exactly one id, `TECH_DEBT-101`, cited from zero engine files → `validate` output byte-unchanged → parity stays green with Python alone).
- **D-393 + C-395 + C-396** — the corpus contract; its fix-site correction; the a-class narrowing (symlink clause, sort clause, the cache-layout exclusion dependency).
- **C-393** — the FROZEN-FIXTURE CORPUS **DROPPED** from `0.2` (b). The operator's approval was conditional (*"assuming it helps keep everything in check"*) and the condition was measured and FAILED: the fixtures are transitively LIVE (9 and 20 `#include`s; `foxtag layout` on one returns the real `tt::ExecutionCore<64>`), they score **0 of 3** on the defects being chased (the by-mention index reads workspace LEDGERS, not the scanned corpus — structurally unreachable), they had already drifted (29% tag overlap with the file they name, stale one day after freezing), and they contradict D-362, whose spec mandates the calibration corpus GROWS. `(b)` narrowed to the grammar golden.
- **D-394** — TTY-required `--bless`; **OVERRIDES C-392 item 2** on the refutation's own internal tension (if bless must never be batched, "a prompt breaks batchability" is moot).
- **D-395** — precedent sweep: **the plan SHRINKS, does not restructure.**
- **D-396** — SCAN vs PIN population split.

## PENDING / residual — every item homed

| # | Item | Home |
|---|---|---|
| 1 | **BB-1 wiring** (Python + C++ + parity §1b + bless) | ▶ NEXT ACTION |
| 2 | **BB-2** `parity_check.sh` contract widening — validity leg (`PY_RC == 0`, not just `==CXX_RC`), non-emptiness asserts on §1/§2, check `build.sh` rc at `:17` | F-393; blocked on BB-1 |
| 3 | **BB-3** `registry_id:` backfill — 2-of-192 adoption, so an (e) guard keyed off it is vacuously green on 190 specs | C-392 item 7 |
| 4 | **`calibration-corpus-non-vacuity-discipline.md` extension** — the 3 blessed-output lifecycle clauses (three-way distinction cross-ref · who may re-bless = D-394 · epoch binding for rules-vs-content change). **NOT YET WRITTEN.** | D-395 |
| 5 | **NEW spec earned:** "promoting a DIFFERENTIAL gate to an ABSOLUTE gate widens its contract" — the only thing the sweep found with no precedent | D-395 |
| 6 | **Epoch binding** — `contract_version` should gate the golden: a RULES change must trip a tripwire demanding a version bump, distinct from a CONTENT re-bless. Reuse the `[ASSERT]_[EPOCH_TRIPWIRE]` shape (`MONEY_ENCODING_EPOCH`); **cross-ref, do not re-derive.** | D-395 Q2 |
| 7 | `0.5` RC-F **re-scoped** to a plugin card render — the analysis is BUILT | D-395 |
| 8 | `0.3` soak-then-delete the two `_RETIRED_UNIT` Lua copies | needs a NON-SKIPPED plugin parity run |
| 9 | Doc-CI defects: `DESIGN_SPECS/README.md` claims 15 types renders 11; 3 specs lack `type:`/`stage:`; `stage: 6-cadence-locked` has zero users | **TECH_DEBT-254** |
| 10 | `check_identifier_retirement --update` writes its golden with **no diff and no confirmation** — an agent can rubber-stamp it today; posture is the OPPOSITE of D-394's | **TECH_DEBT-255** |

## HARD rules

1. **Dev-plane** — `tools/CLAUDE.md` governs; engine H1–H22 do NOT apply. Cite **T1–T12**.
2. **T4/D-349** — Python stays **CI-AUTHORITATIVE**. A `foxtag corpus` producer is the `0.7` convergence target behind parity, NOT this increment.
3. **T11/D-383** — an armed I→A sweep precedes IMPLEMENTATION of a toolchain change. **It has been run for `0.2` (twice, plus a precedent sweep and an anti-pattern sweep). Do not re-sweep; implement.**
4. **T12/M10** — classify the acceptance oracle before delegating. PARTIAL ⇒ hand-review before commit.
5. **Never touch engine code on this ship.** It has stayed at `4c076ed` all session.

## Arming for the next action

- **Read:** `tools/CLAUDE.md` (T1–T12 + the gotchas — 2 harvest waves now) · `tools/lib/corpus_contract.json` **in full, including the `_why` fields — they carry the measurements** · D-393/C-395/C-396/D-394/D-396 · the plan body's CURRENCY BANNER + `0.2` SWEEP OUTCOME block.
- **Run (not just read):** `bash tools/foxtag/parity_check.sh` · `./tools/check_session_docs.sh` · `python3 tools/check_register_fit.py --selftest` · and **probe BB-1 to confirm it is still live**: `tools/foxtag/foxtag validate /nonexistent/x.hpp` should return rc=0 today (that is the bug).
- **Anti-patterns:** **Class-51** (three live instances found this session) · Class-18 (the two-walker surface the contract creates) · **AR-8** (the builder is model-bounded — this session's only real defect was found by an adversarial pass on the orchestrator's own artifact, not by its author).
- **Invariants:** T1 · T4 · T5 · T10 · T11 · T12.

## TaskList state (recreate via /accept-handoff Stage 7)

| ID | Status | Subject |
|---|---|---|
| #1–#4, #6 | completed | D-372 resolution · `0.1` · Step 0 · `0.1.5` · `0.3` |
| #5 | in_progress | **`0.2` TOOLCHAIN CONTRACT + GATE layer** |
| #14 | pending | **BB-1 corpus contract — design LOCKED, wiring is next** |
| #15 | pending | BB-2 `parity_check.sh` contract widening (blocked by #14) |
| #16 | pending | BB-3 `registry_id` backfill |
| #17 | pending | BB-0 sweeps — **DONE this session**, mark completed at pickup |
| #7 | pending | `0.3` soak-then-delete residual |
| #8–#13 | pending | `0.4` → `1.0.0` |

## Paste into a fresh session

```
Picking up E.1.2.B 0.2. The corpus contract is DESIGNED, BLESSED, and SWEPT — but NOTHING READS IT YET.
Zero consumer code was written last session; eleven decisions were.

Run: /accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-19-E.1.2.B-0.2-contract-blessed-sweeps-done-next-wiring-handoff.md

NEXT: wire BB-1 — Python (engine_source_files(profile=) + resolve_paths() + corpus_contract(), route
the --paths branch at check_code_tag_blocks.py:598) -> C++ twins in foxtag.hpp -> parity_check.sh §1b
-> the TTY-gated bless.

FIVE THINGS THAT WILL BITE:
1. D-393 pt 3 names the WRONG fix site. The vacuous-green is on the --paths branch, which BYPASSES the
   enumerator. Implementing it literally leaves build-blocker 1 open AND LOOKS DONE. (C-395)
2. SIX enumerators in TWO families. The profile split is LOAD-BEARING — flatten it and cache-layout
   REDs, and the natural fix writes 68352B into a copy-paste TEMPLATE. (C-396)
3. SCAN population != PIN population. Enumerator is gitignore-blind; the GOLDEN pins git-tracked only.
   The first bless pinned two mkstemp scratch files. (D-396)
4. --bless REQUIRES a TTY; non-TTY hard-refuses rc=2; no --yes. Any prose saying "no confirmation
   prompt" is STALE. (D-394)
5. ALL SCALARS ARE STRINGS in corpus_contract.json — the C++ JVal has no bool kind, so a bare `true`
   reads as EMPTY on one of two readers, silently. Do not "clean it up".

T11 IS SATISFIED — 0.2 has had two I->A cascades plus precedent and anti-pattern sweeps. IMPLEMENT,
do not re-sweep.

HARD: dev-plane (cite T1-T12, not H1-H22); Python stays CI-authoritative (T4/D-349); engine stays at
4c076ed — never touch engine code on this ship.
```

## Capture-completeness (verified — nothing lost)

Checked mechanically where a tool exists, by reading where one does not.

- **Decisions → LOGGED.** D-391…D-396 + C-391…C-396, each with paired `<!-- D/C/F -->` + `<!-- STATUS -->` sentinels; `check_capture_audit.py` Check 4 **PASSES** (sentinels matched across 4 logs). **D-391 is recorded as WITHDRAWN rather than deleted** — the reversal is the value. Three entries carry verbatim corrections of their own earlier claims (C-392 item 1, C-393 item 6, D-396's `sort._enforcement`).
- **Ledgers → WRITTEN.** TECH_DEBT-**252** (plugin repo runs no git hooks) · TECH_DEBT-**253** (`check_schema_version` scans ZERO `.py`; wired HARD, unreachable through the `tools/` dir symlink). Both carry a fix shape, a trigger, and cross-refs.
- **Anti-pattern HARVESTED → AR-15** (`meta-anti-pattern-index.md`): *structure verified, consequence assumed* — THREE instances this session (C-392's lockstep claim · C-395's fix site · D-396's `json_parse` assumption). Distinct from AR-8: AR-8 is about WHO verifies, AR-15 is about WHAT the verification establishes. Filed as a CATALOG row rather than a new memory precisely to avoid the C-391 fourth-restatement mistake.
- **Memories → 1 CORRECTED, indexed.** `project_workspace_remote_gone_commit_only` rewritten (its stale "never push" rule was what left 77 workspace commits unbacked-up); `MEMORY.md` index line updated; `check_capture_audit.py` Check 1 **PASSES**. **Deliberately NOT minted:** a "verify the consequence, not just the structure" memory — that shape recurred twice, but `feedback_verify_by_context_not_count` and `feedback_passing_test_is_not_verification` already occupy the space, and minting a fourth restatement is the exact C-391 mistake. Flagged to the operator instead; **still an open call.**
- **Specs → 1 SUPERSEDED, 1 AMENDED, 1 OWED.** `public-private-boundary-and-ecosystem-discipline.md` superseded (stated the code-only-public law as governing ~2 weeks after all-private) · `doc-intelligence-toolchain-architecture.md` gained the corpus-contract section · `standardized-tool-io-envelope-and-payload.md` gained the KNOWN-GAP block. **OWED:** the `calibration-corpus-non-vacuity-discipline.md` extension (3 lifecycle clauses) + the ONE new spec earned (differential→absolute gate widening) — both in § PENDING rows 4-5.
- **Reference docs.** `tools/CLAUDE.md` gained a second gotcha harvest wave (6 facts). Plan body re-grounded with a CURRENCY BANNER + the precedent-sweep outcome. MASTER banner UPDATE 19.
- **Code.** `corpus_contract.json` + 2 goldens + `goldens/README.md`. **No consumer code** — stated plainly so it is not mistaken for a wired feature.
- **Known gaps, homed not hidden:** the frontmatter/index defects (`DESIGN_SPECS/README.md` claims 15 types, renders 11; 3 specs lack `type:`/`stage:`; `stage: 6-cadence-locked` has zero users) → PENDING row 9. The `check_identifier_retirement --update` rubber-stamp hole → PENDING row 10.

## Why this handoff is long

The session ran six agents and reversed its own design three times — each reversal measured, not argued. The reversals are the load-bearing content: a reader who skips them will re-implement a design that was already refuted. **If you only need to start: the paste-block plus § "The five things that will bite you" are sufficient.**
