# v5.15.5.F.4d.1 — Thread A FULL framework consolidation — SUB-MASTER

**Branch:** stay on `feat/v5.15-live-readiness` (sprint branch)
**Rollback anchor:** tag `pre-v5.15.5.F.4d.1` before any edits (created at Step 0 of `.F.4d.1.A`)
**Predecessor:** `v5.15.5.F.4d` MERGED (shipped 2026-05-16; engine `545b087` + GPG-signed tag; Thread B FULL + Thread A foundation + 3 TECH_DEBT fold-ins)
**Successor:** `.F.4e` (KIND_STRING + 5 GUI metadata derived filter applications — validates framework via real second-source apps)
**Status:** **SUB-MASTER v1.3 — `.A` SHIPPED + `.B` SPLIT LOCKED (2026-05-17)**. `.A` shipped 2026-05-17 (engine `39b9947` + GPG-signed tag + pushed to engine remote). `.B` split into `.B.1`/`.B.2`/`.B.3` per `/readiness` audit recommendation + CRIT-1 wider-scope acceptance (Path γ-class structural critique #2 caught at pre-coding audit gate Batch 1; β4 FOREACH_DRIFT_GATE duplicated canonical FOREACH_CFG_DERIVED_INFERENCE_CFG with 93% row overlap; `/anti-spaghetti` skill first canonical run confirmed 3-way triplet drift surface). Plan body drafts: `.B.1` v1.0 + sidecar v1.0 (framework consolidation ~6-8h) + `.B.2` v1.0 skeleton (cohort migration ~6-8h) + `.B.3` v1.0 skeleton (legacy empty-out ~4-6h). 2 NEW DESIGN_SPECs Stage 2 DRAFT at planning close (`canonical-sister-extension-discipline.md` + `cfg-derived-consumer-framework.md`; Stage 3 first reference at `.B.1` ship). 3 NEW going-forward rules codified (canonical-sister audit + plan citation + audit cadence). `/anti-spaghetti` skill spec live at `claude-skills/anti-spaghetti/SKILL.md`. Original `.B` v1.2 plan body SUPERSEDED. Per-sub-ship audit→update→implement→ship cycle locked. **Next: `.B.1` pre-coding tag + Step 0 of coding.**
**Created:** 2026-05-16 per `.F.4d` MERGED ship close (TECH_DEBT-085 dedicated ship — Thread A FULL scope deferred from `.F.4d` due to ~15-25h focused effort exceeding single-session scope)
**Path γ correction (v1.1 sub-master update 2026-05-16):** `.A` originally proposed building `DerivedFilterFramework.hpp` (3 macro variants) + `DerivedFilterRoster.hpp` (Level-1 meta-registry) + new `FOREACH_REGISTRY` row + parallel walker mechanism. Pre-coding audit gate's `/merge-scan` caught the structural critique: the proposed runtime walker DUPLICATES existing compile-time infrastructure at `CfgFieldRegistry.hpp:1020-1159` (since `.F.4c.3`). Path γ correction adopted — `.A` lands consumer over existing FOREACH_METADATA_BIT + reusable invariants helper + composition audit registry + 2 NEW DESIGN_SPECs (`composed-filter-mask-pattern.md` + `wire-format-canonical-body-invariants-helper.md`) + spec amendments to `metadata-bit-driven-derived-filter-framework.md` + `framework-composition-overview.md`. `.B`/`.C`/`.D` largely unaffected. 4 TECH_DEBT entries opened for follow-up gaps. Phase 2 Level 4 codebase pattern survey scheduled to find more retrofit candidates.

---

## Sub-master role + sub-ship breakdown

This file is the **UMBRELLA** for `.F.4d.1`. It enumerates the 10-item scope at high level + groups items into 4 dependency-aligned sub-ships. **Per-sub-ship full plan body files to be created at next session:**

| Sub-ship | Scope | Items | Plan body file | Est. | Risk | Status |
|---|---|---|---|---|---|---|
| **`.F.4d.1.A`** | Framework infrastructure + first canonical (Path γ+ v2: 1-row FOREACH_METADATA_BIT + StampBoundDerivedFilter.hpp + wire_format_invariants.hpp + CFG_COMPOSE_AUDIT_DECISIONS + H16 static_assert) | Items 1, 2, 10 | `subplans/2026-05-16-v5.15.5.F.4d.1.A-framework-infra.md` v1.4 | ~5-7h actual | LOW-MED | **SHIPPED 2026-05-17** (engine `39b9947` + tag pushed) |
| **`.F.4d.1.B.1`** (NEW) | Framework consolidation: NEW `MemHeaders/CfgGateRegistry.hpp` sparse sidecar + 3 derived-filter consumer macros (INFERENCE_CFG_POPULATE_FROM_DERIVED + STAMP_CFG_POPULATE_FROM_DERIVED + DRIFT_CHECK_AUTOPOPULATE) + 3 production site migrations + 2 NEW DESIGN_SPECs Stage 3 + 4 amended DESIGN_SPECs | Path γ-class structural pivot (replaces β4 sidecar) | `subplans/2026-05-17-v5.15.5.F.4d.1.B.1-framework-consolidation.md` v1.0 + sidecar v1.0 | ~6-8h | MED | DRAFT — ready for pre-coding tag |
| **`.F.4d.1.B.2`** (NEW) | Cohort migration: 24-row → STAMP_BOUND_CFG_DERIVED bit + FOREACH_ML_CFG_FLAG 5→6 sig + Winsor parse-time validation + gap_acceptable_threshold migration + retroactive `.A.7` cohort + 4 ModelInference walker migrations + StampHelper.hpp:156 migration + CoreModelZoo.hpp:243 migration + CRIT-6 byte order decision | Items 3, 4, 9 (subset) | `subplans/2026-05-17-v5.15.5.F.4d.1.B.2-cohort-migration.md` v1.0 skeleton | ~6-8h | MED-HIGH | SKELETON — full draft after `.B.1` ships |
| **`.F.4d.1.B.3`** (NEW) | Legacy empty-out: DELETE FOREACH_CFG_DERIVED_INFERENCE_CFG + FOREACH_STAMP_BOUND_CFG + 14 controller_test.cpp count-assertion migrations + CI Checks 9-12 + v5.14 stamp fixture regression + DESIGN_SPECS spec-body cleanup | Items 9, 7, 8 (residual) | `subplans/2026-05-17-v5.15.5.F.4d.1.B.3-legacy-empty-out.md` v1.0 skeleton | ~4-6h | MED | SKELETON — full draft after `.B.2` ships |
| **`.F.4d.1.C`** | Sidecar override + bit-packed inventory | Items 5, 6 (FOREACH_DRIFT_OVERRIDE sidecar + DriftOverride + RegistryRosterEntry + ManualFieldInventoryEntry; multi-bit-state-encoding canonicals 6/7/8) | `subplans/2026-05-16-v5.15.5.F.4d.1.C-sidecar-bitpacked.md` v1.2 | ~3-5h | LOW-MED | STUB |
| **`.F.4d.1.D`** | CI verification + fixture regression | Items 7, 8 (residual; CI Checks 9-12 may move to `.B.3` per split; v5.14 stamp fixture regression — could move per CRIT-6 outcome) | `subplans/2026-05-16-v5.15.5.F.4d.1.D-ci-fixture.md` v1.2 | ~2-4h | LOW | STUB |

**Dependency order:** **.A (shipped) → .B.1 → .B.2 → .B.3 → .C → .D** (.C optionally interleavable after `.B.3` lands; .C is largely independent of .B's cfg-derived consumer framework).

**Original `.B` v1.2 plan body:** SUPERSEDED 2026-05-17 per `/readiness` audit recommendation + CRIT-1 wider-scope acceptance (Path γ-class structural critique #2 catch). Split into `.B.1`/`.B.2`/`.B.3` for defensive ordering + each-ship-under-8h-focused scope. See `plan_checks/2026-05-17-v5.15.5.F.4d.1.B-audit-synthesis.md` for full audit context + decision rationale.

**Sequencing rationale:**
- **.A foundation must ship first** — Items 1 (DerivedFilterFramework macros) + 2 (StampBoundDerivedFilter consumer) define the API surface .B's migration consumes
- **.B is widest blast radius** — 24-row migration + 12+ consumer sites + ML_CFG_FLAG sig migration; most regression-prone; benefits from .A landing clean as foundation
- **.C is independent** — sidecar override + bit-packed inventory don't depend on .B's migration sequence; can interleave after .A if testing sidecar pattern in isolation is preferred
- **.D ships last** — CI Checks 9-12 + v5.14 fixture regression VALIDATE .A/.B/.C; must run after they land

Each sub-ship gets its own GPG-signed tag: `v5.15.5.F.4d.1.A`, `v5.15.5.F.4d.1.B`, `v5.15.5.F.4d.1.C`, `v5.15.5.F.4d.1.D`. Plus umbrella `v5.15.5.F.4d.1` tag at the end (after .D ships) with umbrella postmortem.

---

---

## Why this ship exists

`v5.15.5.F.4d` MERGED shipped Thread B FULL (bandit/thompson cohort) + Thread A FOUNDATION (H15-H20 codified + 4 Thread A DESIGN_SPECs Stage 3 ACTIVE + STAMP_BOUND_CFG_DERIVED metadata bit reserved + FOREACH_BANDIT_SIDE first canonical enrolled). Thread A's framework consolidation BIG charters (DERIVED_FILTER framework + 24-row migration + sidecar override + bit-packed inventory + CI Checks 9-12 + Layer 5b + ML_CFG_FLAG sig migration + fixture regression) deferred as residual scope because:

- ~15-25h focused work (multi-day; not single-session push per `feedback_no_defer_for_effort` + `feedback_evaluate_options_on_robustness_latency_design_not_time` — both apply)
- Foundation at `.F.4d` provides clear blueprint (STAMP_BOUND_CFG_DERIVED bit reserved; DESIGN_SPECS Stage 3 ACTIVE; H15-H20 codified)
- Honest scoping > heroic single-session push (compaction risk; quality risk)

**Operator framing 2026-05-16 (`.F.4d` ship close):** "ok so we handle this stuff [`.F.4d`], then address TD-085 [this ship], and then we fix the ML side, so we have both sides being structurally sound and maintainable, and this sould make it stupid easy to update going forward" — Caramel.

This ship is the BRIDGE between `.F.4d` (Thread B + foundation) and `.F.4e` (5 GUI metadata derived filters validating the framework via real second-source apps).

---

## Scope (10 items per merged plan body Thread A Charters 8-14)

### Item 1 — `CoreFrameworks/DerivedFilterFramework.hpp` (NEW)

3 macro variants per `metadata-bit-driven-derived-filter-framework.md` Stage 3 ACTIVE:

- `DERIVED_FILTER_DECLARE_GUI(NAME, SOURCE_FOREACH, METADATA_BIT)` — runtime walk over descriptor array; per-row callback fn-pointer invocation. For GUI-only filters (no HMAC chain).
- `DERIVED_FILTER_DECLARE_WIRE_FORMAT(NAME, SOURCE_FOREACH, METADATA_BIT, LOCKED_HASH_VAR, FIXTURE_PATH)` — extends GUI variant with Layer 5b hash-lock scaffold (synthetic populate fn + `fnv1a_64` canonical body hash + LOCKED const).
- `DERIVED_FILTER_DECLARE_WIRE_FORMAT_TWO_SOURCE(NAME, SOURCE_FOREACH, METADATA_BIT, BITMAP_SOURCE, BITMAP_FIELD, LOCKED_HASH_VAR, FIXTURE_PATH)` — extends WIRE_FORMAT with bitmap-source walking for fields living in sister `FOREACH_*_CFG_FLAG` bitmap registry.

**Mechanism (Option B from spec):** runtime walk over `g_*_cfg_field_descriptors[]` array filtering on `metadata_flags & METADATA_BIT`. ~80 fields × 1 cycle each = ~80ns at boot/load-time. No schema change; uniform across all 7+ future applications.

**Estimate:** ~2-3h.

### Item 2 — `CoreFrameworks/StampBoundDerivedFilter.hpp` (NEW)

First canonical application via `WIRE_FORMAT_TWO_SOURCE` variant. Consumes:
- Source registry: `FOREACH_CFG_FIELD` filtered by `STAMP_BOUND_CFG_DERIVED` metadata bit
- Bitmap source: `FOREACH_ML_CFG_FLAG` + `cfg.ml_cfg_flags` field (for 4-5 bitmap-resident STAMP_BOUND_CFG_DERIVED bools after Item 4)
- Locked hash: `LOCKED_STAMP_BOUND_DERIVED_HASH_V5_15_5_F4D_1`
- Fixture path: `tests/fixtures/v5_14_stamp_canonical.bin`

Auto-generates:
- POST_CFG mirror entries in `FOREACH_STAMP_BOUND_MODEL_CONST_POST_CFG` (auto-extension)
- CfgDerivedInferenceCfgRegistry entries (cfg → inf wiring)
- CfgDriftCheckRegistry entries (drift compare; auto-derived from source row)

**Estimate:** ~2-3h (including Layer 5b hash-lock test + LOCKED const derivation).

### Item 3 — 24-row FOREACH_STAMP_BOUND_CFG migration

Per merged plan body Charter 12 + Stage 1 audit. Migrate 24 source rows from legacy `FOREACH_STAMP_BOUND_CFG` (`StampBoundCfgRegistry.hpp:99-179`) to metadata-bit-flagged source rows in `FOREACH_PER_CORE_CFG_FIELD` / `FOREACH_GLOBAL_CFG_FIELD`:

- **22 rows migrate cleanly** — add `STAMP_BOUND_CFG_DERIVED` bit to existing source row metadata_flags
- **1 row** (`gap_acceptable_threshold`) requires NEW source row in `FOREACH_GLOBAL_CFG_FIELD` (per Q2.A; comment-tag for AFFECTS_STAMP_PARITY reclassification at v5.15.6.C)
- **2 pre-canonical legacy parity gaps** — `ml_buy_threshold` source row at `CfgFieldRegistry.hpp:517` + `bandit_blend_ratio` source row at `CfgFieldRegistry.hpp:521` are missing STAMP_BOUND bit; ADD bit first, then flip to STAMP_BOUND_CFG_DERIVED; for bandit_blend_ratio also DELETE manual POST_CFG entry at `StampBoundModelConstRegistry.hpp:295`
- **4 retroactive `.A.7` cohort migrations** — `ml_tp_pct` + `ml_sl_pct` + `barrier_blend_mode` + `per_horizon_barrier_blend` already in POST_CFG manually; flag source rows; framework regenerates; DELETE manual POST_CFG entries

**Always-emit canonical (Q3.G):** Every flagged source row emits to stamp unconditionally. Drift-check `gate_when` expressions auto-generated per cohort:

| Cohort | gate_when expression |
|---|---|
| Default | `STAMP_HAS(*h, inference_cfg)` |
| Bandit/Thompson (4 fields) | `STAMP_HAS(*h, inference_cfg) && BITMAP_IS_SET(cfg.ml_cfg_flags, MASK_ML_CFG_BANDIT_ENABLED)` |
| Soft-risk degradation (4 fields) | `STAMP_HAS(*h, inference_cfg) && cfg.risk_degradation_curve != 0` |
| Ridge (5 fields) | `STAMP_HAS(*h, inference_cfg) && BITMAP_ANY(cfg.ml_cfg_flags, MASK_ML_CFG_RIDGE_WITHIN_HORIZON \| MASK_ML_CFG_RIDGE_ACROSS_HORIZONS)` |
| Composite confidence (5 fields) | `STAMP_HAS(*h, inference_cfg) && BITMAP_IS_SET(cfg.ml_cfg_flags, MASK_ML_CFG_CONFIDENCE_COMPOSITE_ENABLED)` |
| Winsor (2 fields) | `STAMP_HAS(*h, inference_cfg)` (cfg parse-time validation enforces bounds) |

**Winsor cohort cfg parse-time validation (~10 LOC at ControllerConfig.hpp parser):** enforces `winsor_pct_low > 0 && winsor_pct_high < 1 && winsor_pct_low < winsor_pct_high` at boot; moves compound predicate from emit-time to cfg parse boundary.

**Legacy registry empty-out:** `#define FOREACH_STAMP_BOUND_CFG(X)` macro body fully deleted post-migration.

**Estimate:** ~3-4h.

### Item 4 — `FOREACH_ML_CFG_FLAG` 5→6 arg sig migration

Per merged plan body Charter 9. Extend `FOREACH_ML_CFG_FLAG` tuple by 1 column → 6-col `X(NAME, legacy_field, display_label, section, metadata_flags, doc)`. All 12 existing rows migrate mechanically. 5 STAMP_BOUND rows get `STAMP_BOUND_CFG_DERIVED` metadata bit; 7 other rows get `0` (no metadata).

X-macro consumer macros migrate 5→6 arg sig:
- `X_GEN_ML_CFG_BIT` at `MlCfgFlagRegistry.hpp:70` — auto-enum generator
- `X_GEN_ML_CFG_MASK` at `MlCfgFlagRegistry.hpp:82-83` — auto-mask generator

`ML_CFG_FLAG_AUTOPOPULATE_FROM_SEPTUPLE` at `MlCfgFlagRegistry.hpp:92-103` is NOT a consumer (hand-written; no sig update needed).

**Estimate:** ~1-2h.

### Item 5 — `FOREACH_DRIFT_OVERRIDE(X)` sparse sidecar registry

Per merged plan body Charter 10 + `sidecar-override-pattern-for-registry-auto-flows.md` Stage 3 ACTIVE first canonical.

5 sidecar rows for XGBoost training-only WARN_ALWAYS + CROSS_BINARY + EPS_TIGHT semantics (replaces 19-row wide-variant Class 21 anti-pattern). Indexed by parent `FIELD_IDX`. Branchless O(1) array lookup at drift-check emission selects default vs sidecar values per row.

8-byte bit-packed `DriftOverride` struct (multi-bit-state-encoding INVARIANT canonical application #6):
- `has_override` (1 bit)
- `severity` (1 bit) — REFUSE_STRICT vs WARN_ALWAYS
- `category` (1 bit) — INFERENCE_CFG vs CROSS_BINARY
- `compare_kind` (2 bits) — EPS_DEFAULT / EPS_TIGHT / EPS_EXACT / RESERVED
- `eps_idx` (1 byte) — eps-table index for tolerance lookup

Closes TECH_DEBT-059 (stamp-vs-runtime-drift narrow-vs-wide variant; sidecar supersedes).

**Estimate:** ~2-3h.

### Item 6 — Bit-packed `RegistryRosterEntry` + `ManualFieldInventoryEntry`

Per merged plan body Charter 13 + CLAUDE.md item 30 INVARIANT. Multi-bit-state-encoding canonical applications #7 + #8.

- `RegistryRosterEntry` — meta-registry topology data carrier (level + type tokens packed into single byte)
- `ManualFieldInventoryEntry` — CI cross-check infrastructure (exemption_kind + rationale_idx packed)

Sufficient evidence for the pattern's status as a structural discipline (5+ canonical applications: Order::pre_resolved sub-struct + Order::flags_packed bandit bits 17-25 + DriftOverride + RegistryRosterEntry + ManualFieldInventoryEntry).

**Estimate:** ~1-2h.

### Item 7 — CI Checks 9-12

Per merged plan body Charter 14 + `registry-coverage-ci-check-pattern.md` Shape A:

- **CI Check 9** — STAMP_BOUND_CFG_DERIVED coverage (every metadata-bit-flagged source row has a derived filter consumer OR documented exemption)
- **CI Checks 10-12** — placeholder slots for GUI-only derived filters at `.F.4e` (HIDDEN_BY_DEFAULT + RESTART_REQUIRED + SAFETY_CRITICAL + IS_SECRET + DEPRECATED)

Cohort-eligibility CI per `cfg-flag-eligibility-criteria.md` (every new cfg field passes 5-criteria check + cohorts of 2+ siblings audited per cohort-audit rule 2026-05-11).

**Estimate:** ~1-2h.

### Item 8 — v5.14 stamp fixture regression test

Pre-migration v5.14 stamps load cleanly on `.F.4d.1` engine. Legacy stamp byte format preserved. HMAC chain integrity verified. Round-trip parse + re-emit byte-equivalent.

Fixture file: `tests/fixtures/v5_14_stamp_canonical.bin` (committed at this ship).

**Estimate:** ~1-2h.

### Item 9 — 12+ consumer migration sites + `CFG_DRIFT_AUTOPOPULATE` companion macro

Per merged plan body Charter 14 + `autopopulate-pattern-for-production-caller-class.md`. Inline drift loop at `ML_Headers/CoreModelZoo.hpp:225-247` (and ~12 sister sites) migrates from walking legacy `FOREACH_STAMP_BOUND_CFG` to walking auto-generated drift rows via `CFG_DRIFT_AUTOPOPULATE` companion macro.

Sister pattern to `STAMP_CFG_AUTOPOPULATE` + `INFERENCE_CFG_AUTOPOPULATE` (canonical at `.A.7`).

**Bounded audit:** confirm no engine consumer reads "stamp value present vs absent" as the feature-enabled signal (uses cfg flag instead). `.A.7` cohort runs production-clean with cfg-flag reads so canonical shape established. Low risk; flag exceptions for migration via Class 25 sweep precedent.

**Estimate:** ~2-3h.

### Item 10 — Layer 5b hash lock implementation

Per `wire-format-byte-preservation-discipline.md` § 5b. First wire-format derived filter to ship validates the Layer 5b methodology:

- Synthetic populate fn covers all 22+ flagged fields with deterministic test values
- Canonical body emit uses locale-pinned formatting (Layer 5b § Layer 2)
- `LOCKED_STAMP_BOUND_DERIVED_HASH_V5_15_5_F4D_1` constant locked at ship time
- Round-trip HMAC test against `tests/fixtures/v5_14_stamp_canonical.bin`

Layer 5b fires on accidental reorder + forces deliberate decision on intentional change.

**Estimate:** ~1-2h.

---

## Sub-ship boundaries (FINALIZED at sub-master draft 2026-05-16)

**Boundaries:** 4 sub-ships (.A/.B/.C/.D) with dependency order .A → .B → .C → .D (C optionally interleavable after .A). Each sub-ship gets its own plan body file + pre-coding audit gate + GPG-signed tag + postmortem + auto-writes. See "Sub-master role + sub-ship breakdown" section at top of this file for the full table.

**Why these 4 boundaries chosen** (not just per-item):

- **.A** groups Items 1 + 2 + 10 because they're tightly coupled: DerivedFilterFramework.hpp (macros) + StampBoundDerivedFilter.hpp (first canonical consumer) + Layer 5b hash lock (verifies the canonical body byte preservation). Shipping any one without the others leaves an incomplete framework surface.
- **.B** groups Items 3 + 4 + 9 because they share consumer-migration shape: 24-row source-row migration (Item 3) consumes the framework + ML_CFG_FLAG sig migration (Item 4) consumes the framework + 12+ consumer sites (Item 9) all swap legacy walkers for framework AUTOPOPULATE invocations. Shipping incrementally would leave consumer sites partially migrated (Class 18 mirror risk during transition).
- **.C** groups Items 5 + 6 because they share the bit-packed-struct shape: FOREACH_DRIFT_OVERRIDE sidecar + DriftOverride struct + RegistryRosterEntry + ManualFieldInventoryEntry are 3 canonical applications of `multi-bit-state-encoding-pattern.md` INVARIANT (canonicals 6/7/8 of the INVARIANT, sister to canonicals 1-5 already landed). Shipping these together provides 3 canonical reference points + INVARIANT proof.
- **.D** ships last because CI Checks 9-12 + v5.14 fixture regression VALIDATE .A/.B/.C structurally. Must run AFTER they land. Fixture regression in particular needs production framework code in place to test against.

**Per-sub-ship full plan body files TO BE CREATED at next session** (per handoff `handoffs/2026-05-16-v5.15.5.F.4d.1-planning-handoff.md`). Each gets full plan body draft + pre-coding audit gate + cold-pickup handoff + operator consult before coding starts.

---

## Out-of-scope (explicit — DO NOT absorb)

- **Backtest-side framework parity** — sequenced as `v5.16+ ML-side parity` per CLAUDE.local.md ship-after table
- **GUI live-vs-backtest unification** — sequenced as `v5.16+ ML-side parity`
- **Training harness 1:1 with live execution** — sequenced as `v5.16+ ML-side parity`
- **5 GUI metadata derived filter applications** (HIDDEN_BY_DEFAULT + RESTART_REQUIRED + SAFETY_CRITICAL + IS_SECRET + DEPRECATED) — sequenced at `.F.4e` (validates framework via real second-source apps)
- **Any hot path changes** — `BG_Evaluate` / `SG_Evaluate` / `ExecutionCore_Tick` zero changes; mechanical migration is slow-path / drainer / boot / tests only

---

## Pre-coding triggers (REQUIRED before promoting STUB → ACTIVE)

This stub captures scope + sequence + dependencies + risk profile. Before any code lands:

1. **Full plan body draft** — expand each Item section with: surface (file:line refs verified against HEAD at that future moment), key changes, verification gate per-item, anti-patterns to avoid, "if something goes wrong" recovery notes
2. **Pre-coding audit gate** — fire `/precoding-audit-gate` Layer-1 orchestrator (parity + trace-deps + readiness + DOD + merge-scan) per `.F.4d` cadence
3. **Cold-pickup handoff** — write handoff doc per `/handoff` skill that loads the audit synthesis + this plan body
4. **Consult operator** post-audit-gate per `feedback_consult_on_audit_findings` discipline BEFORE coding
5. **Pre-tag rollback anchor** — create `pre-v5.15.5.F.4d.1` GPG-signed tag at start of coding

---

## Tests

Baseline at `.F.4d.1` start: 3174 controller_test + 17 depth_recorder_test (`.F.4d` close). Expected new:
- Item 1 (framework macros): ~5-10 walker invocation tests
- Item 2 (StampBoundDerivedFilter): ~5-10 HMAC round-trip + Layer 5b hash-lock regression
- Item 3 (24-row migration): ~5-10 cohort-specific drift-check tests + always-emit verification
- Item 4 (ML_CFG_FLAG sig): ~5 mechanical compile-time count tests
- Item 5 (DRIFT_OVERRIDE sidecar): ~10 per-cohort override semantic tests
- Item 6 (bit-packed inventory): ~5 round-trip encode/decode tests per canonical
- Item 7 (CI Checks 9-12): infrastructure (not test count)
- Item 8 (v5.14 fixture regression): 1 fixture file + 1 regression test (HIGH-VALUE single test)
- Item 9 (consumer migration): test fixtures may need adjustment for AUTOPOPULATE walker shape; counts neutral or +
- Item 10 (Layer 5b): infrastructure (uses Item 8 fixture)

Expected baseline post-ship: ~3220-3250 tests.

---

## Verification gate

**Universal:** `./build.sh test` GREEN; all 5 binaries (`gui suite tsan asan all`) clean; CI Check 1-12 PASS (4 new from this ship); `/parity-check` GREEN; `/merge-scan` GREEN; `/bug-check` CLEAN (no Class 18/19/21/23 introductions).

**Framework-specific:**
- `rg FOREACH_STAMP_BOUND_CFG /home/caramel/code/FoxML_Trader_v2/` returns ZERO hits OUTSIDE the header itself (legacy registry fully consumed by framework or migrated)
- 24 source rows carry `STAMP_BOUND_CFG_DERIVED` metadata bit (verify via X-macro reduction count)
- FOREACH_ML_CFG_FLAG_COUNT static_assert post-sig migration
- Layer 5b hash regression: `LOCKED_STAMP_BOUND_DERIVED_HASH_V5_15_5_F4D_1` matches synthesis output
- v5.14 stamp fixture loads + parses + re-emits byte-equivalent

**Hot path:** UNTOUCHED (mechanical migration; framework code is slow-path/boot/test only). `tools/calls_graph_diff.sh verify` confirms.

**Replay determinism:** cfg roundtrip byte-identical for migrated fields. Pre-`.F.4d.1` v5.14 stamps load on `.F.4d.1` engine bytewise-identical to `.F.4d` behavior.

**HOT_PATH_CHANGELOG:** NONE entry (slow-path + boot only).

---

## Ship close (when ready)

- Version.hpp bump `5.15.5.F.4d` → `5.15.5.F.4d.1`
- Git tag `v5.15.5.F.4d.1` annotated + GPG-signed
- Postmortem at `plans/v5.15-live-readiness/postmortems/2026-05-XX-v5.15.5.F.4d.1-postmortem.md`
- Auto-writes per CLAUDE.local.md contracts:
  - `DOCS/CHANGELOG.md` `.F.4d.1` row
  - `DOCS/TECH_DEBT.md` -085 CLOSED
  - `tick-trader-percore-workspace/FEATURE_LOOKUP.md` — framework consolidation entry
  - `tick-trader-percore-workspace/DESIGN_SPECS/README.md` catalog (4 specs already Stage 3 ACTIVE pre-ship; verify)
  - `CLAUDE.local.md` sprint state row updates (Most-recent ship → `.F.4d.1`; In-progress → `.F.4e` next)
- `/sync-workspace` after ship close

---

## Cross-references

- Predecessor ship: `2026-05-16-v5.15.5.F.4d-merged-postmortem.md` (Thread A foundation laid)
- Merged plan body Thread A Charters 8-14: `subplans/2026-05-16-v5.15.5.F.4d-merged-framework-bandit-thompson.md` lines 124-247
- `DESIGN_SPECS/framework-patterns/metadata-bit-driven-derived-filter-framework.md` Stage 3 ACTIVE (mechanism + macro API + 3 variants + Layer 5b methodology)
- `DESIGN_SPECS/framework-patterns/sidecar-override-pattern-for-registry-auto-flows.md` Stage 3 ACTIVE (FOREACH_DRIFT_OVERRIDE companion pattern)
- `DESIGN_SPECS/framework-patterns/meta-registry-pattern-for-codebase-registry-discipline.md` Stage 3 ACTIVE (H19 topology discipline)
- `DESIGN_SPECS/framework-patterns/framework-composition-overview.md` Stage 3 ACTIVE (composition narrative — how the 4 Thread A specs compose)
- `DESIGN_SPECS/refactor-patterns/multi-bit-state-encoding-pattern.md` INVARIANT canonical applications 6/7/8 (DriftOverride / RegistryRosterEntry / ManualFieldInventoryEntry)
- `DESIGN_SPECS/framework-patterns/registry-coverage-ci-check-pattern.md` Shape A (CI Checks 9-12)
- `DESIGN_SPECS/wire-format-patterns/wire-format-byte-preservation-discipline.md` § 5b (Layer 5b methodology)
- `DESIGN_SPECS/refactor-patterns/cfg-flag-eligibility-criteria.md` (cohort eligibility framework)
- `DESIGN_SPECS/framework-patterns/autopopulate-pattern-for-production-caller-class.md` (CFG_DRIFT_AUTOPOPULATE pattern)
- CLAUDE.md H15-H20 (codified at `.F.4d`)
- CLAUDE.md item 31 (framework-driven extensibility meta-principle; codified at `.F.4d`)
- `CoreFrameworks/CfgFieldRegistry.hpp:144-145` (`STAMP_BOUND_CFG_DERIVED = 1u << 13` metadata bit; reserved at `.F.4d` foundation)
- TECH_DEBT-085 entry: `DOCS/TECH_DEBT.md` (full residual scope; pointer to this STUB)
