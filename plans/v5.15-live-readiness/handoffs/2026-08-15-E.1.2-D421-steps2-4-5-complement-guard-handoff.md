---
type: handoff
status: active   # THE one live handoff (supersede-on-write; ≤1-active enforced by check_handoff_active_singleton.py)
ship_tag: v5.15.5.F.4d.1.E.1.2
plan_type: coding
supersedes: 2026-08-14-E.1.2-steps3-5-v11-delta-handoff.md
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-08-14-E.1.2-steps3-5-v11-delta-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: freeze the capital-state WIRE on the registry-driven NodeContext persist serializer; every E.1.3+ spine reads this layout
sister_specs: [framework-patterns/meta-registry-pattern-for-codebase-registry-discipline.md, framework-patterns/registry-coverage-ci-check-pattern.md, wire-format-patterns/wire-format-byte-preservation-discipline.md]
coding_status: "D-421 steps 0-5 DONE. Step 2 (22 exemption rows + guard STANDING at pre-commit Check Q), step 4 (Class 58 codified), step 5 (DOMAIN column + derived vocab + CHECK: token). Step 3 fell out as designed. REMAINING: step 6 (gate-reachability — CRITICAL ② partially proven, see below), step 7 (FORMAT: + TradeReader), the domain classification of the baselined registries, and the E.1.2 close-gate (NPF_PROJECT_POISON + ship ritual)."
---

# E.1.2 / D-421 — steps 2 + 4 + 5: the complement guard, its class, and its generalization

## State at write — ANCHORS, not counts

| Repo | HEAD | Window this session | Remote |
|---|---|---|---|
| Engine `feat/v5.15-live-readiness` | `49df9d2` | `0a15f07..HEAD` | pushed, 0 ahead |
| Workspace `main` | see below | `8e2473b..HEAD` | pushed at write; close-out commits follow |

⚠ The workspace HEAD is deliberately not written as a SHA — this document is authored mid-close and
commits land after it. **Re-derive: `git -C ~/code/tick-trader-percore-workspace log 8e2473b..HEAD --oneline`.**

Untracked engine scratch (`notes_update.md` `resume` `tatoo.md` `tmpb924ezcj.cpp`) — operator files, keep.

## Re-derive at pickup (every number below comes from these, none from prose)

```bash
./tools/check_session_docs.sh                              # expect SWEEP CLEAN
python3 tools/check_node_ctx_partition.py                  # expect GREEN, all members accounted
python3 tools/check_node_ctx_partition.py --selftest       # expect ALL TEETH FIRE
python3 tools/check_meta_registry.py                       # expect Checks 1-4 PASS; Check 4 reports declared-vs-baselined
python3 tools/node_persist_layout.py                       # expect GREEN vs golden
python3 tools/check_identifier_retirement.py               # expect GREEN (no wire change this arc)
./build.sh test                                            # expect BUILD 0 + suite green
git -C . log 0a15f07..HEAD --oneline                       # the engine window
```

**Suite was green and UNCHANGED at every commit of this arc** — this work touches no bytes and no
hot path. If the count moved, something else did it.

## What landed

**Step 2 — the complement guard, closed end-to-end.**
`FOREACH_NODE_CTX_PERSIST_EXEMPT` in `MemHeaders/NodeCtxPersistRegistry.hpp` (engine `587d44c`):
every `NodeContext<F>` member is now either persisted or declared-exempt with a **falsifiable
category** and a rationale a reviewer can refute. `check_node_ctx_partition.py` went rc 2 (REFUSAL —
registry absent) → rc 0. Then wired STANDING (ws `4a542ec`, `4a855cd`): two HARD rows in
`check_session_docs` **plus pre-commit Check Q**.

Check Q is the part worth understanding: the aggregator row alone was NOT enough, because Check P
fires `check_session_docs` only on staged handoff/MASTER commits — and the failure this guard exists
to catch arrives in an ordinary code commit. It triggers on the STRUCT's home
(`CoreFrameworks/ControllerEventLoop.hpp`) as well as the registries, because the dangerous edit is
the one touching the struct and NOT the registry. It has since **live-fired and passed** on a real
commit (`49df9d2`).

⚠ **Stated precondition (do not let this go implicit):** Check Q's three-file trigger is complete
only because `NodeContext`'s body is HAND-DECLARED (`ControllerEventLoop.hpp:315`). The moment any
member arrives via an X-macro defined elsewhere — the direction H17 pushes struct bodies codebase-wide
— a member could join without touching any trigger file, and **a silently-skipped Check Q reads
identically to a passing one** (Class 51). Widen the trigger at that point.

**Step 3 — fell out as designed, no separate work.** `gate_state`'s always-RED
`UNESTABLISHED_UNTIL_FIRST_PASS` stopped describing the code once step 2's NSDMI established the
field (re-decided `DERIVED_EACH_PASS`, with the category's mandatory question now answerable);
`drift_history` became a row the moment the operator adopted P-3 option (a).

**Step 4 — Class 58 codified** (ws `83b54eb`), WITH its guard rather than before one: 3 sub-shapes,
8-instance census, the M3 distinction vs Class 4 discharged. Sister cohort amended same ship.

**Step 5 — the DOMAIN column** (engine `4abfd43`/`ebe0590`, ws `bc4c4e2`/`9c02db9`): every
`FOREACH_REGISTRY` row must now **declare** a well-formed domain **or sit on the shrinking
baseline** — re-derive the split with `python3 tools/check_meta_registry.py` (Check 4 prints
declared-vs-baselined). ⚠ Only a handful are declared; the rest are baselined-pending, and
**Check 4 validates the DECLARATION, not domain COVERAGE**. The first draft of this line said
"every row declares what its rows are the complete set of", which states the END state as the
landed state — the same precision the tool itself was corrected for at ws `f0cf754`, fixed in the
tool and left standing in the prose.

Vocabulary **DERIVED** from a `registry-domain-vocab` fence in the meta-registry spec (not
hardcoded — folding a token is one token, zero tool edits). New `CHECK:` token with
**file-existence teeth**, because it was the token most at risk of becoming a rubber stamp.

## ⚠ OPEN — operator decisions, none blocking

- **`drift_history` option (d)** — the zero-wire honest abstain — is ADOPTED but NOT BUILT.
  **Zero-wire CONFIRMED** (independently re-verified at review): `FOREACH_PER_NODE_STATE_FLAG` drives
  the DISPLAY `PerNodeSnap.state_flags` (`EngineTUI.hpp:1259`, `uint16_t`), a wholly different
  registry from `NodeStateFlagRegistry.hpp` → the persisted `NodeContext.node_state_flags`
  (`ControllerEventLoop.hpp:375`, `uint8_t`); only the latter is H21-ledgered, and no
  `fwrite`/HMAC/`memcmp` path over `PerNodeSnap` exists. Headroom fine — the overflow assert is
  `static_assert(PER_NODE_STATE_FLAG_COUNT <= 16)` at `PerNodeStateFlagsRegistry.hpp:126`; re-derive
  current usage with `grep -c '^\s*X(' MemHeaders/PerNodeStateFlagsRegistry.hpp`.
  ⚠ **But the leaf is BIGGER than the first draft claimed** — corrected at review, members named:
  **(1)** there is **NO source bit to project from.** `drift_state_flags` carries exactly two masks
  (`ConfidenceScore.hpp:1234/:1235`) and `ShardedSnapshot.hpp:525` reads `MASK_DRIFT_BREACHED`. A
  `DRIFT_WARMING` display bit needs either a THIRD `drift_state_flags` bit with its own writer, or a
  derivation from the sample count (`ConfidenceScore.hpp:1397/:1410`, the `>= 5` thresholds the
  exemption rationale already cites). Calling `:525` "the existing projection" understated the leaf
  by a whole producer. **(2)** the registry row · **(3)** a NEW projection block at `:~525` ·
  **(4)** the panel string · **(5-7)** three HAND-MAINTAINED COUNTS the row falsifies:
  `PerNodeStateFlagsRegistry.hpp:10`, `:68`, and `EngineTUI.hpp:1259` all say "11". Leaving them
  stale would be exactly the derived-fact drift this whole arc exists to kill.
- **the cfg-derived drift-check gate** (per-field / emit-the-bit / retire) — still open, and D-421
  deliberately sequences it AFTER step 6 so the choice is made knowing what else is dead.
- **`CLAUDE.md` always-loaded budget** at the ⚠ NEAR threshold (re-derive:
  `python3 tools/check_always_loaded_budget.py`). A judgment pass over always-loaded content.

## NEXT ACTION — step 6, gate-reachability

**Partially proven already; do not redo the proof, extend it.** The `inference_cfg` group bit is a
**three-way closed loop** and I verified it mechanically at `49df9d2`:

| Leg | Site | Condition |
|---|---|---|
| EMIT | `ML_Headers/StampBoundModelConstRegistry.hpp:730` | `STAMP_EMIT_CHECK_HAS_inference_cfg = STAMP_HAS(*inf, inference_cfg)` — emits only if already set |
| PARSE | `:747` | `STAMP_PARSER_SET_HAS_inference_cfg = STAMP_SET(r, inference_cfg)` — sets only if a key was read |
| CONSUME | `ML_Headers/CfgDriftCheckRegistry.hpp` | rows gated `STAMP_HAS(*h, inference_cfg)` → `FAILURE_MASK_cfg_binding_drift` |

Closing argument — **corrected by the Stage-6.5.4 adversarial review; the first draft of this
paragraph was wrong in three ways and is preserved as a lesson, not deleted:**

- The only **setter** of `has_flags` is `STAMP_SET` (the sole `BITMAP_SET` on it,
  `StampBoundModelConstRegistry.hpp:624`). ⚠ NOT "the only writer" — whole-struct writes exist
  (`ModelInference.hpp:542` `*m = ModelHandle<F>{}`; `StampHelper.hpp:185`, `NodeModelZoo.hpp:265`,
  `:2408`). They can only CLEAR or PROPAGATE, so the conclusion survives — but the original
  quantifier was false and presented as verified.
- The `STAMP_SET(…, inference_cfg)` set is **FIVE sites, not two**:
  **(1)** `StampBoundModelConstRegistry.hpp:715` — the **only emit-side producer**, and the single
  site whose reachability decides the whole loop. It is inert ONLY because its parent
  `STAMP_MODEL_CONST_AUTOPOPULATE` is quarantined behind `static_assert(false, …)` at `:677-682`
  (PARITY-022). **The first draft omitted this entirely** — an M9 failure inside a step-6 proof, and
  a regression against the D-421 STATUS block which already enumerated three.
  **(2)** `:747` parser dispatcher · **(3)** `NodeModelZoo.hpp:459` (circular, gated on `:458`) ·
  **(4)/(5)** `tests/controller_test.cpp:15572` and `:15646` — see below.

**So the sound claim is NOT "the bit can never be 1"** (it can, and does, in tests). It is:
*unreachable in production given* **(a)** no production emit path sets the group bit — the only
producer is quarantined; and **(b)** every group-key-bearing stamp on disk is pre-epoch
(`stamp_format_version=1` < `STAMP_FORMAT_VERSION_EPOCH_FLOOR = 3`, `ModelInference.hpp:166`) and
hard-refuses at `:1805-1812` → `sr.valid = 0` → the drift block at `NodeModelZoo.hpp:294` is skipped.
⚠ Leg (b) is a DATA + epoch-floor fact, not a code invariant: a hand-authored or branch-emitted **v3**
stamp carrying `inference_cfg_ml_tp_pct=` would set the bit and pass the floor. The generic checker
must model that.

⚠⚠ **HIGH — two TEST FIXTURES hand-set the group bit, and this is WHY the vacuity survived a whole
release train.** `tests/controller_test.cpp:15572` and `:15646` do `STAMP_SET(inf, inference_cfg)`
directly, then drive `stamp_write_for_model` → `verify_model_stamp` and assert. The comment above
the first (`:15568`) claims it *"mirrors Backtest_RunFullValidation"* — **stale**: production emits
via `BacktestEngine.hpp:1440` / `BacktestPanels.hpp:4047` → `Stamp_AssembleAndEmit`, whose 16
explicit `STAMP_SET(inf, …)` calls (`StampHelper.hpp:236-352`) never include the bare group bit. The
emit→parse→gate chain IS exercised, so nothing looked dead — **Class 51 × Class 12**. **This WILL
bite the step-6 fix**: anyone who fixes the gate and asks "does drift fire now?" against these
fixtures gets a green for the wrong reason. Both lines are MUST-TOUCH.

Gated rows in this walker: **4** — `CfgDriftCheckRegistry.hpp:257`, `:261`, `:266`, `:332`.
⚠ Do NOT re-derive with a bare `grep -c "STAMP_HAS(\*h, inference_cfg)"` — it returns 5, because
`:309` is a PROSE line. That is the exact no-comment-stripping fabrication D-421 **step 0** was
written to kill, and the first draft of this handoff reintroduced it as an authoritative command.

This is textbook **Class 58 sub-shape B**.

## ⚠ CORRECTED — the "36 fields" question is ALREADY ANSWERED, and it is the SAME BIT

**The first draft of this handoff said the 36 fields ride a different walker and called it "a
separate question, verify it, do not carry the number forward". That was FALSE and dangerous** — a
fresh session following it would have found no such walker and could reasonably have RETIRED a live
CRITICAL. `FOREACH_STAMP_BOUND_CFG` was **DELETED** at `.B.3` Step 2, 2026-05-24 (`Version.hpp:697`;
tombstoned at `MetaRegistry.hpp:82`), and `ModelInference.hpp:1817` is `r.valid = 0;`, not a walker.
The stale cite was inherited verbatim from `ML_Headers/StampHelper.hpp:229` — **AR-17, committed
inside the document that codifies AR-17.** (`StampHelper.hpp:229` is itself an owed stale-comment fix.)

**The truth:** the 36 are walked by `FOREACH_STAMP_BOUND_DERIVED_COHORT`
(`MemHeaders/CfgGateRegistry.hpp:285`); their drift check is `cfg_derived::drift_check_from_derived`
(`:517`), gated on `stamp_has_inference_cfg` (`:194/:197/:202/:205/:568/:588`), which the macro at
**`:813` supplies as `STAMP_HAS((handle), inference_cfg)` — the identical group bit** — called from
`ML_Headers/NodeModelZoo.hpp:304`. The cohort records presence in per-field `has_<name>` BYTES
(`:365` populate, `:643` parse) that the gate never reads. **All 36 are gated on a bit their own
walker never sets. D-421 ② and Class 58 sub-shape B stand exactly as written.**

The number is right and IS re-derivable: `CfgFieldRegistry.hpp` (31 grep hits **minus** the one
`FOREACH_METADATA_BIT` row at `:1441` = 30) + `MlCfgFlagRegistry.hpp` (5) +
`GateCfgFlagRegistry.hpp:45` (1) = **36**.

**So step 6's remaining work is NOT "verify the 36"** — it is: build the GENERIC gate-reachability
check, then choose the drift-gate fix. The review proposes the right shape for that checker, and it
matches the Class 58 detection signature already written at `class-58-…md:93-95`: stop trying to
prove an unbounded negative over the tree, and instead enumerate **the producer set of a gate bit
reachable from the production emit call sites** vs **its consumer set** — bounded, checkable, and it
would have caught all three of this paragraph's original errors.

**Then build the generic gate-reachability check** (the step-6 deliverable), and only then choose
the drift-gate fix — that ordering is D-421's, and the reason is that the fix should be chosen
knowing what else is dead.

## Also queued

- **Domain classification of the baselined registries** (`tools/lib/meta_registry_domain_baseline.txt`,
  shrinking to empty). **The natural fan-out surface** — N independent registry investigations, one
  clear per-item question. ⚠ It has a KNOWN BLOCKER: `STRUCT:` needs a SUBSET/filter notion before a
  second consumer can use it truthfully — `FOREACH_OMS_PER_SLOT_FIELD`'s domain is the
  `[MAX_PORTFOLIO_POSITIONS]` array subset of `OmsState`, not its members, and declaring
  `STRUCT:OmsState` would be Class 58 inside the fix for Class 58. Full detail in TECH_DEBT-274.
- **Step 7** — `FORMAT:` token, then `GUI/TradeReader.hpp`.
- **E.1.2 close-gate** — `NPF_PROJECT_POISON` 4th projection + FEATURE_LOOKUP operator-migration
  SECTION + postmortem + `/ship` ritual.

## Arming for the next action (M8)

- **Audit posture:** `audit_tier: HIGH` for the step-6 FIX (capital-adjacent: a drift detector on
  stamp↔runtime cfg binding). The step-6 CHECK is mechanical and does not need the gate.
- **Skills:** `/decision-check` on the drift-gate fork · `/trace-deps` or `/dependency-chain-trace`
  for the second walker's reachability · `/bug-check` before commit (Class 58 sub-shape B is now in
  the catalog and `/bug-check` reads it dynamically).
- **Tools to RUN:** `check_meta_registry.py` (+`--selftest`) · `check_node_ctx_partition.py`
  (+`--selftest`) · `check_session_docs.sh` · `check_close_out_completeness.py --since <WORKSPACE sha>`.
- **Anti-patterns in play:** Class 58 (all three sub-shapes) · Class 51 (the effect) · Class 57
  (refusal-not-empty) · Class 44 sub-B.
- **Invariants:** H15 · H21 · H9 · H22.
- **WILL BITE** — all four cost time this session, all four now homed in `tools/CLAUDE.md` +
  `DOCS/LANDMINES.md`: `bless.py` needs a REAL terminal (the `!` prefix captures stdout so
  `_isatty()` is false — the operator's `--close` failed on exactly this) · `check_close_out_completeness
  --since` resolves in the WORKSPACE repo, so an ENGINE sha silently passes having checked nothing ·
  `check_tools_inventory` reds on a realistic-looking `tools/*.py` string inside a negative-test
  FIXTURE · never start a second `./build.sh` while one is in flight (Landmine 18 — it truncates the
  binary and reports "Permission denied").

## TaskList (recreate at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | pending | **Step 6 — build the GENERIC gate-reachability check.** The `inference_cfg` finding is PROVEN and the 36-field question is ANSWERED (same bit — see NEXT ACTION); do NOT re-investigate either. Shape: enumerate a gate bit's PRODUCER set reachable from the production emit call sites vs its CONSUMER set (bounded + checkable), per `class-58-…md:93-95`. **MUST-TOUCH:** `tests/controller_test.cpp:15572`/`:15646` (fixtures that hand-set the bit and will green a broken fix) + the quarantine at `StampBoundModelConstRegistry.hpp:677`. |
| #2 | pending | **Step 6 fix — the drift-gate fork** (per-field / emit-the-bit / retire). D-421 sequences this AFTER #1 deliberately: choose knowing what else is dead. `audit_tier: HIGH` (capital-adjacent). |
| #3 | pending | **`drift_history` option (d)** — the adopted-but-unbuilt zero-wire abstain: one `FOREACH_PER_NODE_STATE_FLAG` row (`DRIFT_WARMING`) + the existing `ShardedSnapshot.hpp:525` projection + a panel string. Small, self-contained. |
| #4 | pending | **Domain classification** of the baselined registries (`tools/lib/meta_registry_domain_baseline.txt` → empty). THE fan-out surface. ⚠ blocked on the `STRUCT:` subset/filter design — see TECH_DEBT-274. |
| #5 | pending | **Step 7** — `FORMAT:` token, then `GUI/TradeReader.hpp`. |
| #6 | pending | **E.1.2 close-gate** — `NPF_PROJECT_POISON` 4th projection + FEATURE_LOOKUP operator-migration SECTION + postmortem + `/ship`. |
| #7 | pending (operator) | The three OPEN items above: `drift_history` (d) build order · the cfg-derived drift-gate choice · the `CLAUDE.md` always-loaded budget pass. |
| #8 | pending backlog | TECH_DEBT-274 (generic `STRUCT:` checker) · -172 (meta-registry spec full refresh) · -282 (mint the Class-58 Mn) · -249 (§11.5 contract guard). |

## Capture-completeness — the JUDGMENT ledger (Stage 6.5.2)

The eight `/capture-audit` checks no tool backs. Answered explicitly, because silence here is
indistinguishable from a skip.

- **Check 2 — plan-body currency.** OWED and DONE: the D-421 STATUS block led with *"STEP 2 IN
  PROGRESS — the 22 exemption ROWS are the remaining piece"*, which would have sent a fresh session
  to redo landed work. Rewritten to lead with steps 2+5 landed; the old text kept below as
  historical detail rather than deleted.
- **Check 3 — decision-log artifact.** PRESENT. No NEW D-number minted — this session IMPLEMENTED
  the already-decided D-421 build order. The one operator decision (P-3 option (a)+(d) for
  `drift_history`) is recorded in the D-421 STATUS block and in the exemption row's rationale.
- **Check 5 — handoff currency.** THIS doc supersedes the 2026-08-14 one; reciprocal frontmatter
  set; the singleton guard is in the re-derive block above.
- **Check 6 — M7 escalation candidates.** ONE, recorded: the close ritual was OFFERED rather than
  invoked (see the meta-harvest below, AR-8 5th instance) → strengthens the existing TECH_DEBT-278.
- **Check 7 — DESIGN_SPECS promotion.** `meta-registry-pattern-for-codebase-registry-discipline.md`
  gained the AS-BUILT section + the `domain` vocabulary fence. No Stage bump claimed: the pattern's
  enforcement half (a generic `STRUCT:` coverage checker) does not exist yet, and claiming promotion
  on a declaration-only capability is the exact over-read this arc is about.
- **Check 9 — memory ↔ spec sister completeness.** No NEW memory. One CORRECTED
  (`project_remote_push_needs_operator_password` — see below), gaining a sister link to
  `feedback_comments_point_in_time_verify_against_code`; the bidirectional guard RED-ed the one-way
  link on first run and the back-reference was added rather than the link dropped.
- **Check 10 — going-forward-rules currency.** No new Tier-0 rule. The session's disciplines were
  applications of existing ones, not new ones.
- **Check 12 — amendment cascade.** Class 58's sister cohort (Classes 4, 12, 30 frontmatter; 51 a
  prose section) amended in the SAME ship. TD-249↔TD-282 cross-linked bidirectionally.

**Meta-harvest (Stage 4.5):** **AR-17 NEW** — inherited `file:line` cites consumed into new durable
content (all of the `ControllerEventLoop.hpp` cites from the P1/P2 reports were stale; the arc had
already produced one such instance days earlier). **AR-8 bumped** — 5th instance, the
offer-instead-of-invoke variant.

**Tech debt worked, not stacked:** TECH_DEBT-196 + -167 CLOSED (operator-blessed at a real TTY;
both verified in code first, -196 closable for a day with nothing flipping it). TECH_DEBT-274
trigger FIRED but stays OPEN with precision — step 5 shipped less than it predicted, and closing on
the trigger alone would have been the phantom-guard shape inside the entry about phantom guards.
TECH_DEBT-172 partial progress recorded, explicitly NOT closed. **TECH_DEBT-282 NEW** — Class 58's
methodology Mn, HOMED rather than minted, with no number reserved (AR-14 sub-shape 4).

## Independent review (Stage 6.5.4) — verdict: **REFUTED**

An `a-class` adversarial pass (default-REFUTED, not the author) returned **3 HIGH · 4 MED · 1 LOW**
against the first draft of this document. **Every HIGH is fixed above**, and the corrections are
marked in place rather than silently applied, because what the review caught is more instructive
than the fixes. Frozen verbatim at
`reports/2026-08-15-d421-close-review/a-class-handoff-adversarial.md`. All three HIGH claims were
independently re-verified by orchestrator code-read before acting (AR-11).

**The sharpest finding, and it is the author's own anti-pattern twice over:** the caveat paragraph
warning a fresh session "the 36-field question is NOT proven, verify it" was **itself the false
claim in the document** — it named `FOREACH_STAMP_BOUND_CFG`, a registry DELETED at `.B.3` on
2026-05-24, and cited `ModelInference.hpp:1817`, which is `r.valid = 0;`. The stale cite was
inherited verbatim from a code comment (`StampHelper.hpp:229`) — **AR-17, the exact anti-pattern this
session codified, committed inside the handoff that codifies it.** Worse, "verify it, do not carry
the number forward" would have sent a reader to retire a **live CRITICAL** that D-421 ② and the
Class 58's own catalog file — both written this same session — record as settled.

The other two HIGHs are the same family: an unenumerated "ONLY" that dropped the one site deciding
the whole proof (M9, and a REGRESSION against the decision log, which already had three of the five),
and two test fixtures that hand-set the dead bit — which is the actual reason the vacuity survived a
release train, and which will silently green a broken step-6 fix.

**The pattern across all three: the mechanical half was green and every error lived in the judgment
half.** Every re-derive command in this document runs clean; every tool cited returns what it claims;
and the reasoning built on top of them pointed 180° away from the answer. That is the inverted-signal
sharpening (AR-8) demonstrated on the artifact whose own harvest section describes it.

**What the review confirmed** (so the coverage is auditable, not just the failures): the zero-wire
claim for option (d), Check Q's live firing on `49df9d2`, both HARD aggregator rows, every re-derive
command's exit state, the Class 58 sister-cohort claim exactly, all five ledger claims, and the
`check_close_out_completeness --since` WILL-BITE. **Not verified by the reviewer:** `./build.sh test`
(read-only agent; Landmine 18 makes an unsanctioned concurrent build unsafe) — so "BUILD 0 + suite
green" rests on the author's runs, stated here rather than left implicit.
