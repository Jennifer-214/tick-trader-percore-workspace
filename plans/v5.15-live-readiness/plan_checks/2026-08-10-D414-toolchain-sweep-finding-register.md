---
type: finding-disposition-register
ship: E.1.2.B 0.2 (D-414 toolchain derived-vs-ACTUAL sweep)
status: LIVE — dispositions flip at each fix commit. Corpus = the 4 I-class slice reports (2026-08-09: I-1 layout-fact reach · I-2 call-graph facts · I-3 validator parity · I-4 emit-boundary honesty) + the orchestrator's empirical probes. ⚠️ The raw agent transcripts + I-1's sweep_facts.json/analysis.json lived in /tmp and were LOST to the 2026-08-10 reboot — THIS FILE is the durable extraction (written same-day from the in-context reports; member lists named, not tallied, so leaf runs re-derive mechanically).
source_corpus: D-413/D-414/D-415 (decision log v5.15.5.F.4d.1.E-architecture-v2.md tail) + leaf-1 commit 9f4c5c2 + captures commit 815eae8
disposition_legend: "CLOSED-leaf-1 (9f4c5c2) | OPEN-leaf-2 (fact-core arc) | OPEN-leaf-3 (gate-arming arc) | OPEN-leaf-4 (call-graph/docs arc) | PENDING-A-VERIFY (a-class verdict outstanding) | MOOT-BY-PARK (foxtag-only hazard dissolved by the D-415 demotion; re-check at v1 re-arm) | HOMED-<leaf/ship> (engine-plane or other-owner; tracked there) | CLOSED-815eae8 (c-class currency items)"
verification_rule: "disposition flips ONLY on a code grep/READ or a run — never a token-count, never an assumption. A-class verdicts amend severity/attribution in place, dated."
dogfoods: feedback_tag_disposition_at_fix_time + feedback_document_as_you_go_over_catch_at_end + feedback_name_members_never_tallies_in_docs
---

# D-414 toolchain sweep — finding-disposition register

Derived-facts integrity sweep of the toolchain plane (the one-producer-N-consumers correctness carve-out). Headline lesson, proven live twice: **producer-vs-producer agreement is not validity** — both layout backends agreed NotifyState has no straddlers; both were wrong. Sequencing = D-414 compounding order; C++ posture = D-415 (defer to v1; script-side authority; teeth accrue as v1 acceptance oracles).

## 📍 CLOSED at leaf 1 (commit `9f4c5c2`, 2026-08-10) — mechanical honesty, seams + aggregator

- **F2 (I-4, HIGH) index-laundering loop** — `foxtag_client.inventory()` swallowed rc≠0 (partial rows parsed as truth) + `rebuild_doc_indexes.collect_code_tags` fell back only on ImportError/absent-binary → a broken core regenerated the code-tag index shrunken, and the natural "re-run the rebuild" would COMMIT it. **CLOSED-leaf-1**: `inventory()`/`layout()`/`fields()` return **None on rc-failure/empty/undecodable** (refusal ≠ empty facts); rebuild falls back LOUD to the Python collector; teeth flipped to pin refusal (test_foxtag_client 11/11 — the old teeth had pinned the flatten).
- **F4 (I-4, MED-HIGH) aggregator eats rc0 could-not-run warnings** — `run_hard`/`run_advisory` rendered "gate could not run" as unqualified ✅. **CLOSED-leaf-1**: rc0 + marker `could not run|produced nothing for|run FAILED` → ⚠️ row + detail (markers deliberately NARROW; the ⚠️-HARD row does NOT set HARD_FAIL — a-class was asked to challenge that choice).
- **F5 (I-4, MED) capital-audit crash rendered as the positive coverage claim** — rc never captured, verdict was grep-WARN only. **CLOSED-leaf-1**: `CAP_RC` captured; crash → `❌ ADV TOOL ERROR`, truthful non-blocking.
- **F6 (I-4, MED) enumerator-failure ≡ "nothing modified" on a HARD row** — B-Plus session scope's git calls were `2>/dev/null … || true`. **CLOSED-leaf-1**: `git rev-parse --git-dir` asserted before an empty MODIFIED can green.
- **F1/F3 consumer half** — "Checked N … all clean" counted blocks whose record was absent from the dump. **CLOSED-leaf-1**: policed/unpoliced split in the gate summary. (Producer halves OPEN below.)
- **D-415 demotion, first half** — `check_cache_layout` `auto` backend = Lua emitter (script-side authority; foxtag = opt-in probe with honest fallback). **CLOSED-leaf-1.** Remaining half = **park pre-commit Check T §4/§5 IN THE SAME COMMIT as the leaf-2 Lua detector fix** — empirically confirmed required: Check T fired on the leaf-1 commit and ran layout §4 live (it passed only because leaf 1 didn't touch detector logic).

## 📍 OPEN — leaf 2, the fact-core arc (detector + partial propagation + teeth + corpus re-ground)

- **F1 producer half (I-4, HIGH)** — the C++ port has NO partial state at all: verified-none / unverifiable / absent all emit `"straddlers":[]` (`foxtag_layout.hpp:308-329`, emit `:415-423`). Lua is the better twin (`{report, partial=N}`) but `emit_record_layout.lua:57-66` flattens partial → `[]` at the emit seam. **Lua half = OPEN-leaf-2** (propagate `partial` through JSON + writer distinguishes `none` vs `unresolved`); **C++ half = MOOT-BY-PARK** (documented into the foxtag freeze tombstone; fix at v1 re-derive).
- **F3 producer half (I-4, HIGH)** — both emitters clang-rc-blind; **empirically CONFIRMED** (orchestrator probe 2026-08-09: mid-TU syntax error → clang rc=1 yet dumps all records incl. post-error ones). Lua rc-gate = **OPEN-leaf-2**; C++ = MOOT-BY-PARK.
- **Detector reach (I-1)** — census at HEAD 4c076ed: **198 blocks → 136 verified-clean · 42 PARTIAL (written `none` is fabricated-by-flattening) · 20 no-record · 4 probe-failed headers**. Resolver blind-spot taxonomy (empirical): cv-qualifiers break prim lookup · fn-ptr TYPEDEFS unresolvable (NOT "=8" — `NotifyBackendFn`/`OrderCallback`/`LabelFn`/`ThompsonUpdateFn`) · `pthread_*`/`time_t`/`sig_atomic_t` absent from prim_sizes · sugar ARRAYS unresolvable (`Money[16]`, `Position<64U>[16]`, `FPN_Binary<64U>[1024]`) while scalars desugar · `std::atomic` records dropped by `layout_noise` · depth-1-only field capture. **OPEN-leaf-2** (minimal resolver set now; the O2b member-`sizeof` probe = the v1 endgame, recorded not built).
- **Hidden REAL straddlers the fix will surface** (named members; re-derive via `--isolate` post-fix): NotifyState `cond@40` (48B) · WsHeartbeatTelemetry `bucket_count@56` (20B — see exemption question) · CandleAccumulator `lock@262248` (a pthread mutex straddling two lines) · BinanceUserDataState `ws_host@656`/`rest_host@720` · DepthSharedState `symbol@880` · OrderEvent `reason@112` (32B inside the 144B ring element) · ControllerConfig `horizon_list@4656`/`node_time_exit_ticks@39824` · TrainingPanelState ×9 (suite-plane, biggest single fabrication) · ModelHandle/ModelStampResult/StampInferenceCfgInputs stamp-string crossers (cold, LOW).
- **Teeth that PIN the blindness — must FLIP with the fix (I-3)**: `test_recordlayout.lua:54-55` (depth-1-only pinned) + `:74-84` ("Partial excluded" pinned) + `check_cache_layout.py --selftest` case 5 (not-in-dump→PASS) + NO partial-record case anywhere. Leaf 2 adds a **NotifyState-shaped KNOWN-ANSWER fixture** (nested system type + real straddler; `static_assert(offsetof)`-truthed expected values — I-3's T-1 kernel).
- **WsHeartbeatTelemetry exemption question — PENDING-A-VERIFY**: author comment (`ControllerEventLoop.hpp:~724`) says "padding intentional"; single producer-writer both lines. If confirmed benign-by-design, leaf 2 needs a `[STRADDLE_EXEMPT]`-style vocabulary (schema SSoT + parser + gate honor) BEFORE the corpus refresh, else a decided-benign layout becomes a standing warning.
- **Corpus `--fix` re-ground** after the detector fix (~42 `[STRADDLE]` rewrites, tool-owned, no hand edits) + diff eyeball + triage of any new armed-struct findings.
- **`check_cache_layout.py:5` "SAME source" over-claim** — false at the straddle axis (dump-core vs `layout.lua` clangd-hover core; D-310/D-320's two-cores mirror is back). Comment fix = leaf 2; core CONVERGENCE = **HOMED-0.3-sidecar + (g)-5 dive** (HUD onto the ONE core at v1 per D-415).

## 📍 OPEN — leaf 3, the gate-arming arc (make the H6 gate able to fire where H6 lives)

- **[THREAD] arming gaps (I-1)** — cross-thread-in-reality structs with no block-level [THREAD], so the gate structurally cannot fire on them: **NodeContext** (`ControllerEventLoop.hpp:300` — a written straddler `regime_state@56` ALREADY EXISTS; the tag alone makes the gate evaluate, zero tool change) · TUISnapshot/PerNodeSnap (`EngineTUI.hpp:867/:1091` — PerNodeSnap carries 3 written straddlers `:1386`) · NotifyState · BinanceAdapterState/PendingSubmission · BinanceUserDataState · CandleAccumulator · LatencyHistogram · OrderEvent (parent armed `:244`, element not) · TrainingPanelState/RunControlState/OptimizerPanelState. Uncertain (consumer-trace first): OrderResult · DrainerConstants · BuddyAllocatorState. **PENDING-A-VERIFY** (traces requested), then engine comment-tier edits.
- **Discriminator fragility (I-1)** — `cross_thread` is LAST-[THREAD]-line-wins (`check_cache_layout.py:66-67`); field-level [THREAD] tags inside [CODE] can silently disarm a block (`OrderManager.hpp:273` stays armed only by line order; `Order.hpp:211` disarmed — review). Fix: orient-tier-only or any-line-≥2. **OPEN-leaf-3.**
- **CI reach (I-1)** — standing sweep runs the gate single-TU (`main.cpp`) ADVISORY → **75/198 blocks unpoliced** (Backtest 30 · GUI 19 · CoreFrameworks 8 · DataStream 6 · ML_Headers 7 · MemHeaders 5); the promote-to-HARD comment's own condition ("once conversion lands") is MET but unexecuted. Cheap win **O4b**: gate over TU pair `{main.cpp, foxml_suite.cpp}` + ADV→HARD. Blind in BOTH modes: **SPSCRing** (`SPSCRing.hpp:29`, typename-template probe emits invalid `SPSCRing<64>`) + **RollingStats** (multi-width stage-2 refusal); probe-gen also malforms per-instantiation names (`FixedPoint<2,64><64>`); 4 probe-failed headers (ParameterSlot/SPSCRing rc=2; GuiThread/ModelValidation empty — GUI flags). **OPEN-leaf-3.**
- **F8 (I-4, MED)** — isolate-loop `merged.update(run_emitter(...) or {})` (`check_cache_layout.py:410-418`) + `check_register_fit.py:74`: one failed header of N silently joins the coverage gap; only total failure is loud. Fix: M-of-N failed-header report + warn/red. `or {}` interim-safe post-leaf-1 (None still flattens THERE — deliberately left). **OPEN-leaf-3.**
- **F11 (I-4, MED)** — plugin `writers.lua:209-212` discards LSP err → zero-writers/zero-touches feeds the HUD write-set the operator consults when DECLARING [THREAD] — the gate's arming input. Fix: err-count carried, HUD renders "N fields unresolved". **OPEN-leaf-3** (plugin repo).

## 📍 ENGINE-PLANE findings — HOMED, not this arc (surface + track; fix at the owning leaf)

- **TUISharedState (`DataStream/EngineTUI.hpp:1424`) — the sweep's sharpest engine find.** Gate-ARMED, written `[STRADDLE]_[none]` `:1523`, and THREE real hidden sub-64B straddlers on the GUI↔engine control cluster: `swap_strategy_requested@58168` (16B, L908→909) · `kill_reset_per_node@58184` (64B) · `manual_close_requested@58248` (64B) — GUI-written, engine-read-and-cleared; **`manual_close_requested` is read+cleared by the DRAINER (order path)**; the seqlock word `seq@58112` cohabits L908 with GUI-written request flags. **PENDING-A-VERIFY** (drainer site trace) → **HOMED-E.1.2** (NodeState-SoA bytes leaf owns realignment; struct is memcpy'd whole under the seqlock — check `TUISnapshot_ReadInto` consumers first). Interim: leaf-2's honest refresh will WRITE these straddlers as facts (visibility without relayout).
- **WsHeartbeatTelemetry** (`ControllerEventLoop.hpp:688`, H6-referenced) — realign-vs-exempt decision rides the leaf-2 exemption vocabulary (above).
- **ReconciliationLoopState `std::atomic<double> last_drift_usdt@17080`** (`ReconciliationLoop.hpp:36`) — out of D-414 scope; flag for the next **H4 sweep** (atomic<double> on a drift surface).
- **Definition-split census (I-1, calibrates D-413 C2)**: 81 records / 236 fields >64B cross lines by necessity — exclusion KEPT (dominated by cold config/results blobs); HUD renders spans as SPAN + a count, never a 236-item wall. No misaligned sub-64-element array straddle found in the checked corpus.

## 📍 OPEN — leaf 4, the call-graph axis + doc-truth (I-2)

- **NO gate exists for written `[UPSTREAM]`/`[CONSUMERS]`** — write-once-never-verified; the schema's §CI-enforcement item-3 present-tense claim is FALSE for this axis (the validator's own header admits DEFERRED, `check_code_tag_blocks.py:24-25`); `tools/foxtag/README.md:158-161`'s "today only layout is written" premise false since pilot `d4812de` (2026-07-06). **OPEN-leaf-4** (doc-truth rewording + the A2 gate below).
- **Census of the ENTIRE written call-graph corpus (8 lines / 2 files — 100% verified): 1 VERIFIED · 2 DRIFTED · 2 FABRICATED.** VERIFIED: `CumDelta_Init` (3/3 callers exact). DRIFTED: `RegimeSignals` (`RegimeDetector.hpp:150-151`) missing struct `PortfolioController` member `:287` + `PortfolioController_Tick` `:1767`; `CumDeltaState` (`:193-194`) missing `PortfolioController` member-ptr `:318` — attribution (incomplete-at-write vs clangd warm-up vs classify exclusion) **PENDING-A-VERIFY**. FABRICATED: **`StrategyParameters_Dispatch`** in `StrategyInterface.hpp:164` + `RegimeDetector.hpp:15` — symbol NEVER existed (git log -S: only its introduction); real dispatch consumers = `Strategy_InitPerCore/_AdaptPerCore/_ExitAdjustPerCore/_FreePerCore` (`StrategyLifecycle.hpp:153/238/404/466`) — replacement set **PENDING-A-VERIFY**, then fix the written lines.
- **Provenance misclaims**: `[ORIGIN]_[AUTO]` stamped block-wide over hand-authored call-graph lines (`9939ed6`); `[UPDATED]` restamped by the LAYOUT writer over older call-graph lines (freshness marker misrepresents the axis's age); empty `[DERIVED]` shells where pilot placeholders were DELETED-not-filled to satisfy C6 hard-zero (`Regime_ComputeSignals`/`Regime_Init`/`FOREACH_REGIME_PERSIST_FIELD`); sole surviving `[ROW_COUNT]_[<tool: 4 …>]` hybrid placeholder (`StrategyInterface.hpp:162`); file-level [CONSUMERS] in the ORIENT tier (`RegimeDetector.hpp:15`) — schema-legality **PENDING-A-VERIFY** (two semantics under one tag name = Class-18 seam if sanctioned).
- **A2 gate (recommended shape)** — standing check: every written `[UPSTREAM]`/`[CONSUMERS]` entry gets (1) symbol-existence vs CODE_MAP/source (kills fabrication) + (2) reference-presence in the consumer's span (kills staleness). **Declared-PARTIAL structurally** (prints its non-coverage: cannot catch MISSING consumers — that needs the real generator, v1-foxtag per D-415). Natural sibling of (g) step-4's checks. **OPEN-leaf-4.**
- Producer inventory (context): 3 live independent "who-uses-X" computations (plugin facts.lua/clangd · gen_code_map regex · calls_graph_diff regex) + the written lines as a 4th hand-touched copy; no pair diffed. `calls_graph_diff CLEAN` is NOT mitigation for this axis (different fact core).

## 📍 OPEN — validator/parity plane (I-3) + smalls

- **§1 validate validity is ONE-SIDED** — the landed F-393 legs prove "both call the clean corpus clean"; nothing standing proves "both call broken broken for the right reasons" (T-2 bilateral planted-violation leg). `foxtag selftest` (12 real cases) + `codegen-selftest` are MANUAL-ONLY. Disposition: **re-home to the v1 acceptance battery** per D-415 (standing wiring = MOOT-BY-PARK) — **PENDING-A-VERIFY**.
- **§2 inventory** both-parsers-skip-same-shape exposure; consumer preference (rebuild prefers foxtag inventory) — flip script-side? (parity §2 green ⇒ byte-safe). **PENDING-A-VERIFY**; leaf-1 rc-honesty already closed the broken-binary path.
- **F7 (I-4)** parity §4/§5 skip without verdict-NOTE vars → unqualified "PARITY: PASS" in a deps-less env. Likely **MOOT-BY-PARK** standing; survives as a **v1 re-arm requirement** (record in the (a) contract).
- **F9 cohort (~10 sites)** except-continue corpus-shrink: `check_struct_alignment.py:84,111,130,161` (Check K scope) · `check_meta_registry.py:93,100` (H15) · `check_fpn_doc_size_currency.py:280` · `cascade.py:171,211,286` · `check_cache_layout.py:47-50`. Shared read-helper (SKIPPED-N + nonzero) vs per-site — proportionality **PENDING-A-VERIFY**. **OPEN-leaf-3/4.**
- **F10** Python `main()` lacks the empty-corpus refusal the C++ has (`foxtag_main.cpp:36-44` vs `check_code_tag_blocks.py:761-794`) — netted twice elsewhere; residual **PENDING-A-VERIFY**; mirror if confirmed. 
- **add_vocab.py:100-103** post-regen `--check` vacuous-in-context (F-393 queued "drop or move BEFORE regen"; was UNHOMED — homed HERE). Fix inline at leaf 3/4.
- **§5 codegen** baseline blessed-from-own-past (LOW-MED; wired Python known-shape teeth mitigate) + **§3 grammar** common-mode (LOW, **HOMED-D-386** goldens). Footnotes: `check_struct_size_budget.py:52-59` getconf-fallback unlabeled in output; `gen_code_map.sh --structs` prints "(none)" on rg failure.
- **RBP codification owed at arc close**: NEW Class — **emit-boundary honesty flattening** (honest partial/unknown collapsed to a clean default at a boundary; sisters: pipe-swallow, counted-but-not-gated, silent-empty-input). M3 false-positive surface: cosmetic/display `or {}` fallbacks are LEGITIMATE; B-Plus's own two handlers flatten toward STRICTER (the honest direction) — cite as counter-example. 22 sites found this sweep; recurrence proven.

## 📍 CLEARED surfaces (I-4, verified no instance — scope record)

`.githooks/pre-commit` (15 checks rc-captured) · `check_determinism.sh` + 4 sub-gates (non-vacuity asserted) · `run_all_tests.sh` · `run_sanitizer_suite.sh` · `calls_graph_diff.sh` (fails red-direction) · `toolio.py` (schema-driven + negative controls) · B-Plus's own except-handlers (stricter-direction).

## 📍 c-class currency items — CLOSED at `815eae8`

F-393 sentinel CLOSED (legs verified landed) · task-#6 straggler set corrected (-140/-163/-175a/-204/-250 + capital-`**Status:**` parser hazard) · TD-259 anchors-relief folded · plan footer v1.3→v1.5 synced. D-387/D-398 stale execution-glosses: recorded in D-414's narrative (deliberately not retro-edited; the category-error is already ledgered at `DOCS/tech-debt/open.md:3888`).
