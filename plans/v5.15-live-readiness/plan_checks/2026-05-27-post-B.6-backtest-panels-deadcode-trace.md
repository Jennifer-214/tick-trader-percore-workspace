---
type: plan-check
audit: dead-code-trace
scope: module:Backtest/BacktestPanels
date: 2026-05-27
ship_target: v5.15.5.F.4d.1.B.7
status: COMPLETE
verdict: NO-DEAD-PANELS — plan body Decision A misidentified panel inventory
sister_specs:
  - DESIGN_SPECS/meta-disciplines/audit-scope-taxonomy.md
  - feedback_enumerate_consumers_before_registry_row_deletion.md
  - feedback_backwards_compat_not_default_concern.md
---

# /dead-code-trace — Backtest/BacktestPanels.hpp (post-`.B.6` pre-`.B.7`)

## TL;DR

**Plan body `.B.7` Decision A enumerates 7 sub-files (`TrainModel.hpp` / `WalkForward.hpp` / `MultiHorizonCollect.hpp` / `MultiHorizonTrain.hpp` / `Replay.hpp` / `Optimizer.hpp` / `HeldOut.hpp` + `Common.hpp`) on the assumption that BacktestPanels.hpp contains 7 standalone panels matching those names. THIS ASSUMPTION IS WRONG.**

Actual panel inventory:

```
GUI_Panel_DataBrowser   (line 375,  95 LOC)   — Data window
GUI_Panel_RunControl    (line 474,  40 LOC)   — Run Control window
GUI_Panel_Results       (line 523,  90 LOC)   — Results window
GUI_Panel_PastRuns      (line 1027, 868 LOC)  — Past Runs window (tabbed: class / regression)
GUI_Panel_Comparison    (line 1952, 129 LOC)  — Comparison window
GUI_Panel_Optimizer     (line 2140, 215 LOC)  — Optimizer window
GUI_Panel_Training      (line 4164, 2212 LOC) — Training window (multi-section, multi-button)
```

**All 7 panels are ALIVE.** All are unconditionally rendered every frame from `foxml_suite.cpp:404-412`. No DEAD panels exist to delete. The plan body's `TrainModel` / `WalkForward` / `MultiHorizonCollect` / `MultiHorizonTrain` / `HeldOut` items are NOT separate panels — they are buttons / collapsing sections WITHIN the single `GUI_Panel_Training` window. `Replay` does not exist at all (the word appears only in a tooltip string at line 493). Splitting BacktestPanels.hpp along the plan body's proposed lines therefore requires fragmenting the monolithic Training panel into sub-files keyed on its internal sections, not splitting at the existing panel-function boundary.

**Operator's "dormant ones serve no purpose" framing does not apply at the panel granularity.** It MAY apply at the section-within-Training granularity, but verifying that requires a separate scope (`module:Backtest/BacktestPanels/Training-panel`) — out of scope for this dead-code-trace pass.

**Recommended `.B.7` scope amendment:** before re-attempting the split, the plan body needs Decision A re-grounded against actual file structure. Three options surface (Section 6 below).

---

## Per-panel verdict table

| Panel | Classification | LOC | Render dispatch site | Worker fn status | Doc refs | Tests | Last meaningful edit | Recommendation |
|---|---|---|---|---|---|---|---|---|
| **GUI_Panel_DataBrowser** | ALIVE | 95 | `foxml_suite.cpp:404` | n/a (file selection only) | `ML_USAGE.md` ("Run Control tab") | none | active (data file selection feeds all workers) | KEEP |
| **GUI_Panel_RunControl** | ALIVE | 40 (panel) + helpers | `foxml_suite.cpp:405` | `backtest_worker_fn` (line 220, real Backtest_Run call) | `CLAUDE_FOXML_SUITE.md` ("Run Control — pick a date range + strategy, kick off a backtest") + `ML_USAGE.md` step 3 | none | `c519104` 2026-05-24 (cfg path bugfix) | KEEP |
| **GUI_Panel_Results** | ALIVE | 90 | `foxml_suite.cpp:406` | n/a (read-only stats render) | implicit (always shown post-backtest) | none | stable since v5.10.x | KEEP |
| **GUI_Panel_PastRuns** | ALIVE | 868 | `foxml_suite.cpp:410` | `PastRuns_Scan` worker | `ML_TRAINING.md` ("Open the Past Runs panel") | none | `d3b9b15` v5.15.5.F.6 (Delete button bugfix) | KEEP |
| **GUI_Panel_Comparison** | ALIVE | 129 | `foxml_suite.cpp:407` | `Comparison_SaveRun` (in-memory snapshot) | `CLAUDE_FOXML_SUITE.md` ("Comparison panels") | none | stable | KEEP |
| **GUI_Panel_Optimizer** | ALIVE | 215 | `foxml_suite.cpp:411` | `optimizer_worker_fn` (line 2123, calls `Backtest_RunSweep`) | NOT in README / QUICKSTART / ML_USAGE / ML_TRAINING / CLAUDE_FOXML_SUITE | none | `c519104` 2026-05-24 (cfg path bugfix), `9a14931` v5.10.0a.ABC (UX work) | **KEEP — but operator-visibility weak (DORMANT-leaning)** |
| **GUI_Panel_Training** | ALIVE | 2212 | `foxml_suite.cpp:412` | 7 workers: `train_model_worker_fn` / `walkforward_worker_fn` / `hp_sweep_worker_fn` / `fullvalidation_worker_fn` / `train_multi_horizon_worker_fn` / `collect_multi_horizon_worker_fn` / `mh_per_horizon_parallel_worker` — all wired to real Backtest_RunFullValidation / RunHyperparamTrainSweep / model training | `ML_TRAINING.md` + `ML_USAGE.md` + `CLAUDE_FOXML_SUITE.md` (all extensively reference Training/Train Model/Walk-Forward) | none | `1752fde` v5.14.post1 (stamp body migration fix) | KEEP (further internal-section audit may identify dormant subsections — separate scope) |

---

## Sub-section inventory (Training panel — `GUI_Panel_Training` body)

The Training panel contains 7 logical sections that the `.B.7` plan body MIS-CATEGORIZED as separate panels:

| Section | Line range | UI shape | Worker dispatched | Reachable? | Operator-doc reference |
|---|---|---|---|---|---|
| Label config + horizons + side | ~4169-4395 | inline controls (combo + InputText) | n/a | always | `ML_TRAINING.md` "Label Type" |
| Collect Features (single) | 4405-4461 | button (only when `panel_eff_horizon_count <= 1`) | `backtest_worker_fn` | conditional (1 horizon) | `ML_USAGE.md` step 4: "Check Collect Features, run backtest again" |
| Collect Multi-Horizon | 4488-4566 | button (only when `panel_eff_horizon_count > 1`) | `collect_multi_horizon_worker_fn` | conditional (>1 horizon) | none explicit but visible UX |
| Advanced (v5.9.5h) XGBoost hyperparams | 4827-4970 | CollapsingHeader | n/a | always (collapsed default) | `ML_TRAINING.md` (sample_weight context) |
| Train Model (single) | 4978-5147 | button | `train_model_worker_fn` | conditional (1 horizon) | `ML_USAGE.md` step 4: "Click Train Model" |
| Train Multi-Horizon | 5151-5320 | button | `train_multi_horizon_worker_fn` | conditional (>1 horizon) | `README.md` ("Train multi-horizon at v5.11.41+") |
| Save Run | 5423+ | button | inline filesystem ops | always | implicit |
| Walk-Forward Validation | 5664-6010 | inline section + button | `walkforward_worker_fn` | always (gated `can_train`) | `ML_USAGE.md`: "Walk-Forward — trains on historical folds, tests on future" + `ML_TRAINING.md` "Held-out + walk-forward" |
| Hyperparam Sweep | 6030-6210 | inline section + CollapsingHeader + button | `hp_sweep_worker_fn` | always (gated `can_hp`) | NOT in operator docs (only inline tooltip) — **DORMANT-leaning within Training** |
| Full Validation (held-out + auto-stamp) | 6013-6310 | inline section + button | `fullvalidation_worker_fn` | always | `ML_TRAINING.md`: "Backtest_RunFullValidation" extensively documented; THIS IS THE STAMP-EMITTING WORKFLOW |
| Phase Timing (last run) | 6348+ | CollapsingHeader | n/a | always | none |

---

## Detailed evidence — Optimizer panel (DORMANT-leaning ALIVE)

**Why ALIVE not DEAD:** Optimizer is unconditionally rendered (`foxml_suite.cpp:411`) and `optimizer_worker_fn` calls real `Backtest_RunSweep` (`BacktestPanels.hpp:2128` → `BacktestEngine.hpp:2341`). The worker is functional.

**Why DORMANT-leaning:** comprehensive grep across operator-facing docs returns ZERO results:

```bash
grep -rE "Optimizer|optimizer_panel|OptimizerPanel|Run Sweep|hyperparam.*sweep" \
  README.md DOCS/QUICKSTART.md DOCS/ML_USAGE.md DOCS/ML_TRAINING.md \
  DOCS/CLAUDE_FOXML_SUITE.md DOCS/CONFIGURATION.md 2>/dev/null
# RESULT: zero matches (operator docs never mention Optimizer panel)
```

Only `BacktestPanels.hpp:2155` references the workflow ("OptimizerPanel hyperparam Combo" from v5.10.0a.ABC commit). Last meaningful change: `9a14931` v5.10.0a.ABC (2025-09 timeframe per commit chain). Subsequent edits are bugfixes (cfg path correction at `c519104`).

**Recommendation:** DO NOT DELETE — verify with operator instead. The panel is functional and operator may use it without document; or it may genuinely be dormant. Operator decision required.

---

## Detailed evidence — Hyperparam Sweep section within Training (DORMANT-leaning)

**Why ALIVE not DEAD:** rendered + functional + reachable from `Run Hyperparam Sweep` button (line 6095). Worker (`hp_sweep_worker_fn`) calls real `Backtest_RunHyperparamTrainSweep`.

**Why DORMANT-leaning:** NOT documented in operator-facing docs (`ML_USAGE.md` / `ML_TRAINING.md` / `README.md` / `CLAUDE_FOXML_SUITE.md` / `QUICKSTART.md` all miss it). Only inline tooltip describes it. Code comment (`BacktestPanels.hpp:2467`): "v5.10.0a.E — Hyperparam Sweep state. Mirrors wf_* / tm_* worker pattern."

**Recommendation:** verify with operator whether this surface is in active use. If yes → KEEP. If no → consider deletion separately (saves ~180 lines, BUT requires operator confirmation per Class 33 anti-pattern + `feedback_enumerate_consumers_before_registry_row_deletion`).

---

## DEAD code candidates — NONE

Comprehensive grep verification per `feedback_enumerate_consumers_before_registry_row_deletion`:

1. **Every `GUI_Panel_*` function** is called from `foxml_suite.cpp:404-412` unconditionally.
2. **Every worker `*_worker_fn`** is dispatched via `pthread_create` from a real button click handler.
3. **Every struct** (`DataPanelState` / `RunControlState` / `ComparisonState` / `PastRunsState` / `OptimizerPanelState` / `TrainingPanelState`) is instantiated in `foxml_suite.cpp` and threaded through render calls.
4. **No orphaned helper functions** found via reverse-grep of every static inline definition.

Verification probes used (per `dead-code-trace` skill methodology):

```bash
grep -rln "GUI_Panel_DataBrowser\|GUI_Panel_RunControl\|GUI_Panel_Training\|\
GUI_Panel_Optimizer\|GUI_Panel_Results\|GUI_Panel_PastRuns\|GUI_Panel_Comparison" \
  /home/caramel/code/FoxML_Trader_v2 --include="*.cpp" --include="*.hpp"
# Hits: foxml_suite.cpp + BacktestPanels.hpp (definitions). 100% coverage.

grep -rEln "OptimizerPanel|TrainingPanel|backtest_worker_fn|train_model_worker_fn|\
optimizer_worker_fn|walkforward_worker_fn|fullvalidation_worker_fn|hp_sweep_worker_fn|\
train_multi_horizon_worker_fn|collect_multi_horizon_worker_fn" /home/caramel/code/FoxML_Trader_v2/tests/
# Hits: tests/controller_test.cpp ONLY references workers (no panel render tests)
```

Comprehensive coverage across `*.cpp`, `*.hpp`, `*.md`, `*.cfg.example`, `tests/`, `tools/*.sh`, `tools/*.py`. All confirmed ALIVE.

---

## Proposed `.B.7` scope amendments (3 options)

The plan body needs Decision A re-grounded. Three shapes surface:

### Option A — Re-define Decision A against actual structure (recommended)

Replace plan body Decision A enumeration with actual panel inventory + intra-Training section split:

```
BacktestPanels.hpp (INDEX shim — 50 lines)
├── BacktestPanels/
│   ├── Common.hpp              ← BacktestRunConfig + worker arg structs + shared helpers
│   ├── DataBrowser.hpp         ← 95 lines  (GUI_Panel_DataBrowser + DataPanelState + helpers)
│   ├── RunControl.hpp          ← 200 lines (GUI_Panel_RunControl + RunControlState + backtest_worker_fn + SamplesSnapshot_Compute + collect_multi_horizon_worker_fn)
│   ├── Results.hpp             ← 90 lines  (GUI_Panel_Results + ResultsPnlColor)
│   ├── PastRuns.hpp            ← 870 lines (GUI_Panel_PastRuns + PastRunsState + PastRun + scanner helpers)
│   ├── Comparison.hpp          ← 130 lines (GUI_Panel_Comparison + ComparisonState)
│   ├── Optimizer.hpp           ← 215 lines (GUI_Panel_Optimizer + OptimizerPanelState + optimizer_worker_fn)
│   └── Training/               ← split required (2212 lines)
│       ├── State.hpp           ← TrainingPanelState struct + Init (~200 lines)
│       ├── Workers.hpp         ← all 5 training workers (train_model + walkforward + hp_sweep + fullvalidation + train_multi_horizon + mh_per_horizon_parallel + mh_run_one_horizon_fv) (~1500 lines)
│       └── Render.hpp          ← GUI_Panel_Training body (~700 lines)
```

Pros: clean panel-per-file boundary; works with real file structure; Training subfolder isolates the worker complexity.

Cons: 11 sub-files instead of 8.

### Option B — Skip split entirely (no-op `.B.7`)

Per `feedback_count_code_loc_not_total_lines`: BacktestPanels.hpp is 4,639 code-LOC over 1,500 hard threshold for source headers. **Splitting IS warranted** — Option B is not viable unless threshold is renegotiated.

### Option C — Defer until operator-visibility verified (delete dormant first)

Before split: open dialog with operator to confirm or reject Optimizer panel + Hyperparam Sweep section as actively used. If both rejected as truly dormant:

- Delete `GUI_Panel_Optimizer` + `OptimizerPanelState` + `optimizer_worker_fn` + dispatch in `foxml_suite.cpp` → **~250 LOC reduction**
- Delete Hyperparam Sweep section + `hp_sweep_worker_fn` + `HyperparamSweepWorkerArgs` + `OptimizerRange[]` fields in `TrainingPanelState` → **~250 LOC reduction**
- Post-deletion: BacktestPanels.hpp = 4,639 - 500 = ~4,139 code-LOC. Still over threshold; split STILL required.

Net effect: cleanup reduces split surface area but doesn't eliminate it. Combine with Option A (delete then split).

---

## Risks + verification asks for operator

1. **Optimizer panel dormancy unverified.** Operator may use it without doc reference. RISK: silent deletion of operator workflow. **ASK:** does Caramel use the Optimizer panel (sweep parameters like TP/SL/risk_pct)? Last meaningful semantic edit was v5.10.0a.ABC (2025-09 timeframe).

2. **Hyperparam Sweep section dormancy unverified.** Same shape — possibly operator-used without doc. **ASK:** does Caramel use the Hyperparam Sweep section inside Training panel? Worker calls `Backtest_RunHyperparamTrainSweep`; this is XGBoost hyperparameter grid search.

3. **Plan body Decision A names misleading.** `TrainModel.hpp` / `WalkForward.hpp` / `HeldOut.hpp` plan body sub-files do NOT correspond to existing panels. RISK: re-using these names creates sub-files that pull from ONE function body, fragmenting Training panel by section rather than splitting at function boundaries. May or may not be desirable; surface for operator decision.

4. **`Replay.hpp` plan body sub-file references nonexistent code.** No Replay panel exists. RISK: WIP step `B.8` (extract Replay) would terminate with "nothing to extract". **ASK:** is this an oversight in plan body, OR did the plan body anticipate ADDING a Replay panel as part of the split? If the latter, that's feature work not split work.

5. **`HeldOut.hpp` plan body sub-file references nonexistent panel.** Held-Out validation is integrated into Full Validation inside Training panel; not a separate panel. Same plan-body-vs-reality drift.

6. **Conservative DORMANT classification preserved.** Per skill discipline, when uncertain between DORMANT and DEAD, defaulted to DORMANT. Operator may downgrade to DEAD after confirming non-use.

---

## Estimated LOC accounting

| Option | Delta | Post-delta total code-LOC | Over 1500 threshold? | Notes |
|---|---|---|---|---|
| No deletion | 0 | 4,639 | YES (3.1×) | Split required regardless |
| Delete Optimizer only | -215 | 4,424 | YES (2.95×) | Marginal |
| Delete Optimizer + HP Sweep | -500 | 4,139 | YES (2.76×) | Marginal |
| Delete + split (Option A+C) | -500 + restructure | ~600/file × 8 files | NO per file | Optimal |

---

## Plan body amendments needed (per `.B.7` plan body file)

`/home/caramel/code/tick-trader-percore-workspace/plans/v5.15-live-readiness/subplans/2026-05-25-v5.15.5.F.4d.1.B.7-backtestpanels-subfolder-split.md` — sections requiring rewrite:

- **Lines 41-47:** Sub-file enumeration — replace with actual panel inventory (Option A above).
- **Lines 71-100:** Decision A — rewrite around real structure.
- **Lines 114-120:** Extraction order — re-sequence per real dependencies.
- **Lines 220-232:** Step plan — re-derive once Decision A re-grounded.
- **Lines 265:** Verification criteria — verify against actual panel names.

Caller (main session) drives amendment cycle with operator. Audit-re-fire trigger per `feedback_substantive_amendment_audit_refire`: this IS a substantive amendment (architectural decision shift — sub-file boundary changes), so `/precoding-audit-gate` re-fire BEFORE `.B.7` coding starts.

---

## Cross-references

- Plan body: `/home/caramel/code/tick-trader-percore-workspace/plans/v5.15-live-readiness/subplans/2026-05-25-v5.15.5.F.4d.1.B.7-backtestpanels-subfolder-split.md`
- Umbrella sub-master: `/home/caramel/code/tick-trader-percore-workspace/plans/v5.15-live-readiness/subplans/2026-05-25-v5.15.5.F.4d.1.B-file-size-maintenance.md`
- Target file: `/home/caramel/code/FoxML_Trader_v2/Backtest/BacktestPanels.hpp` (6,376 total / 4,639 code-LOC)
- Consumer: `/home/caramel/code/FoxML_Trader_v2/foxml_suite.cpp` (only direct external consumer)
- Tests: `/home/caramel/code/FoxML_Trader_v2/tests/controller_test.cpp` (workers only; no render tests)

## Methodology citations

- `dead-code-trace` skill: spawned per skill methodology (Identify → Verify Trace → Classify → Emit Plan).
- `feedback_enumerate_consumers_before_registry_row_deletion`: comprehensive grep across `*.cpp`, `*.hpp`, `*.md`, `*.cfg.example`, `tests/`, `tools/*.sh`, `tools/*.py`. No DEAD classification without multi-pattern multi-file-type grep coverage.
- `feedback_backwards_compat_not_default_concern`: prepared to surface DELETE-class operator decisions without preserve-and-deprecate layer (operator explicitly flagged this convention 2026-05-26).
- Conservative DORMANT-over-DEAD classification per skill output discipline: irreversible deletion costlier than over-preservation.
