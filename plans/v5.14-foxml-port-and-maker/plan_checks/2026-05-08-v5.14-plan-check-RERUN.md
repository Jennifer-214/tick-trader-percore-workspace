# /plan-check report — v5.14 expanded sprint — 2026-05-08 (RERUN)

**Verdict:** **YELLOW** — 3 verify-at-code-time items + 2 cfg-clarity nits; no architectural blockers

## Plan summary

- 12 sub-plans (was 9; +3 NEW + 2 EXTENDED)
- Effort: ~45-65 days (was 22-30; +50% scope)
- Critical path: v5.14.0 ✅ → v5.14.1 → v5.14.9 (depends on .1)
- 8 GREEN, 4 YELLOW, 0 RED per-sub-plan

## Per-sub-plan verdicts

| Sub-plan | Verdict | Notes |
|---|---|---|
| v5.14.0 | GREEN | SHIPPED |
| v5.14.1 | YELLOW | .E Spearman IC drift wiring needs trace-deps |
| v5.14.2 | GREEN | All ensemble primitives reused |
| v5.14.3 | GREEN | Surface G overlay clean |
| v5.14.4 | GREEN | Back-compat shim explicit |
| v5.14.5 | YELLOW | Bundled hash bumps; .B/.C trace-deps recommended |
| v5.14.6 | GREEN | /bug-check skill scope tight |
| v5.14.7 | YELLOW | BinanceAdapter_CancelOrder + BookSnapshot wiring verify-at-code |
| v5.14.8 | YELLOW | .A max_staleness X-macro hash-isolation TBD |
| v5.14.9 | GREEN | Tight scope; trace-deps already GREEN |
| v5.14.11 | GREEN | Thompson trace-deps GREEN; mt19937 deterministic |
| v5.14.12 | GREEN | Welford incremental + drift reset |

## Cross-plan integration

- ✓ Dependency edges acyclic; v5.14.9 → v5.14.1 explicit
- ✓ v5.14.0.B Ridge override precedence over Thompson (v5.14.11) documented
- ✓ Stamp body extensions all use Surface G `has_*` flag pattern
- ✓ X-macro discipline: v5.14.5 appends only (LABEL + FEATURE bundled)
- ⚠ Cfg namespace: v5.14.1 + v5.14.9 both use `confidence_*` prefix; distinct names but cognitive load risk

## Critical findings (verify-at-code-time)

### #1 — v5.14.8.A FEATURE_REGISTRY_HASH X-macro isolation (HIGH)

Plan claims hash-compute X-macro reads ONLY `(id, name, version, enabled)`,
ignoring NEW 7th column `max_staleness_minutes` → hash STABLE. Must
verify at code time that FeatureRegistry.hpp has TWO X-macro callers:
- Feature definition X-macro (7 columns)
- Hash compute X-macro (4 columns)

**If isolation absent at code time:** either add it OR document
FEATURE_REGISTRY_HASH bump in master invariants table.

### #2 — v5.14.1.E Spearman IC drift detection wiring (MEDIUM)

v5.14.1.E adds Spearman IC with cfg toggle. v5.10.0e drift detection
currently consumes Pearson via `RollingIC_Compute`. Plan claims
`DriftHistory_Push` can be wired to select either via cfg.

**Verify at code time:** trace `DriftHistory_Push` call site; confirm
non-local refactor isn't needed (otherwise increase v5.14.1.E effort).

### #3 — v5.14.7 BinanceAdapter_CancelOrder + BookSnapshot (MEDIUM)

Plan lists `BinanceAdapter_CancelOrder` as TRULY NEW; assumes
`binance_signed_request` supports `method="DELETE"`. Verify at
code time.

BookSnapshot accessor: plan references `book_snap.best_bid` /
`best_ask`; struct has `bids[5]` / `asks[5]` arrays (index `[0]` =
top-of-book). Plan should use `book_snap.bids[0].price`.

**Verify at code time:** confirm `BinanceOrderAPI_CancelOrder` can
be added on existing infra; confirm slow-path access to BookSnapshot.

## Worth-fixing nits

1. **Cfg namespace clarity:** v5.14.9 fields share `confidence_*`
   prefix with v5.14.1's. Recommend rename to
   `confidence_risk_degradation_*` (or `risk_degradation_*`).
2. **Master plan test estimate:** Update gate to expect ~2190 tests
   (~+131 new across sprint vs original ~+50-80 estimate).
3. **v5.14.8.A hash decision:** Master invariants table should
   document whether max_staleness is INCLUDED or EXCLUDED from
   FEATURE_REGISTRY_HASH.

## Verdict: **YELLOW → proceed with verify-at-code-time gates**

No architectural blockers. 3 must-verify items at code time
(properly scoped at sub-ship readiness gates). 12 sub-plans
cohesive. Architectural invariants preserved with documented
deliberate exceptions.

**Sprint kickoff sequence:** v5.14.0 ✅ → v5.14.1 → Phase 2 → Phase 3 → Phase 4 (or parallel where deps allow).
