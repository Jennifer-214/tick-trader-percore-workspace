---
type: handoff
status: active   # the one live handoff; supersedes 2026-06-16-E.1.0-gates-1-3-wired-next-H8 (its "code H8" next-action is now FULLY DESIGNED + foundation-hardened; H8 BENCH BUILD is the next move)
ship_tag: ".E.1.0 — H8 design CONVERGED (A⁺, 2× 3:3 + cascade); foundation INSTRUMENTS hardened (derived-facts-drift tool + 2 meta-patterns codified); NEXT = build the H8 bench → then the trade-branch H20 fix (#15)"
plan_type: live-readiness
sprint_end_goal: make the codebase maintainable + live-ready (v5.X professionalization); live gated behind the whole E-series
ship_end_goal: ".E.1.0 stands up the §4d guard NET. H14/H10 ENFORCED (prior session). This session: H8 design fully converged (A⁺) + the FOUNDATION INSTRUMENTS that the re-audit needs (the size+cache guard tool + the derived-facts-drift + critical-moment-determinism meta-patterns). REMAINING: build the H8 bench → the trade-branch H20 fix → H7/H20 advisory → re-sweep → V-class → ship."
predecessor_handoff: handoffs/2026-06-16-E.1.0-gates-1-3-wired-next-H8.md (SUPERSEDED — its "fresh I/A pass on H8" is DONE: two full 3:3 sweeps + a cascade bridge converged the A⁺ design)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (latest D-231; this session added D-228/229/230/231)
plan_body: plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.0-guard-hole-closure.md (read § "H8 — 3:3 EXECUTED + ORCHESTRATOR-VERIFIED" + § "SCOPED A⁺ 3:3 — A-CLASS VERDICTS" — the CONVERGED H8 design)
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, the E.1.0 plan body § H8-3:3-EXECUTED + § A-CLASS-VERDICTS, decision log D-228→231, the 2 NEW DESIGN_SPECs (mechanical-verification-of-derived-code-facts.md + critical-moment-determinism-over-average-latency.md), the 3 NEW memories (feedback_mechanically_verify_derived_code_facts + feedback_critical_moment_determinism_not_average_latency + feedback_invoke_composable_skills_dont_hand_execute), tools/check_struct_size_budget.py, D-230 (the cluster/SPMC LOOKAHEAD), regen CODE_MAP at pickup]
engine_head: 572c978 (UNCHANGED — no committed engine code). **UNCOMMITTED working-tree change: ML_Headers/RollingStats.hpp carries 4 NEW static_assert size-pins (compile-verified, zero-behavior) — a clean pending change; commit with the H8 work or standalone.**
workspace_head: at-or-after the /sync-workspace at this session's close (the tool + selftest + 2 specs + 2 memories + D-228..231 + the plan-body sections + the homed idea + TOOLS enrollment)
coding_status: NO committed engine code (RollingStats.hpp static_asserts uncommitted-but-compile-verified). Session = DESIGN (H8 A⁺ converged) + APPARATUS (the size+cache tool + meta-pattern codifications) + ARCHITECTURE CAPTURE (cluster/SPMC/cache lookahead). NEXT = build the H8 bench (rides consult-before-coding — engine latency-emit path) → the trade-branch H20 fix (#15).
defers: "trade-branch H20 fix (#15, LOCKED behind H8 — it needs the bench to verify); umbrella-sweep finish (#11 — the other ~15-20 stale size comments); subagent registry (#12); re-sweep with the hardened tools (#14); the doc-debt (RollingStats.hpp:187/:198 'O(W)' + PortfolioController.hpp:253/256/257 stale-size comments → fix at the RollingStats leaf); the H8 absolute-vs-ratchet + the slow-gate REBUILD-vs-whole-cycle (A⁺ locked the shape; impl pending the build)."
---

# Handoff — `.E.1.0` H8 design CONVERGED + foundation hardened → next = BUILD the H8 bench

## TL;DR (the 60-second pickup)

- **H8 is fully designed (A⁺), not just sketched.** Two complete 3:3 sweeps + a cascade bridge converged it. The next move is to **BUILD the H8 bench** (rides consult-before-coding — it touches the engine latency-emit path). The design + every refutation is in the plan body § "H8 — 3:3 EXECUTED" + § "A-CLASS VERDICTS".
- **A ship-stopper was caught:** the worst-case-ML gate would have shipped GREEN measuring SimpleDip, because the ML-degrade path stamps `strategy_id=ML` — so the gate asserts FLAGS (`MODEL_LOAD_FAILED|MODEL_CORRUPT==0`) + an inference-ran counter, NOT a label. Class-51 first canonical.
- **A foundational guard gap was found + closed:** a struct size comment said "~1.5MB" for a 64KB struct (= ×16-cores-at-24B, a per-engine total on a per-core field). Root: nothing guarded non-serialized struct sizes. Built `tools/check_struct_size_budget.py` (sizeof + host-derived cache-budget; 10 structs pinned) + `static_assert` size-pins on RollingStats + codified the **derived-facts-drift meta-pattern** (measure, never hand-comment).
- **A latency-philosophy principle was codified:** *the cost is jitter-at-the-critical-moment, not low average.* The trade-event push is a data-dependent branch → the rare trade is a GUARANTEED mispredict+flush exactly when committing capital. **Locked as the #1 hot-path fix (Task #15, H20-on-principle), behind the H8 bench that verifies it.**

## The ideal way to start

1. **`/accept-handoff`** (resolves here). Regen CODE_MAP, verify engine `572c978`, load baseline + nav-infra + the 2 NEW DESIGN_SPECs + the 3 NEW memories.
2. **Read the converged H8 design:** plan body § "H8 — 3:3 EXECUTED + ORCHESTRATOR-VERIFIED" (the surviving mechanics) + § "SCOPED A⁺ 3:3 — A-CLASS VERDICTS" (what got refuted/reshaped) + **D-228** (the locked directions).
3. **Build the H8 bench** (the next move — consult-before-coding at the build gate). Scope below.

## H8 — the surface kit (what the bench build needs)

- **The emit doesn't exist yet** — build the sharded per-core REBUILD/hot p99 emit FIRST (today `[OMS_BENCH]` is drainer-only; the `[BENCH]` hot line is the dead single-core path).
- **Gate WINDOWED p99** (not `lifetime_p99` — the log-bucket quantizes ±2× → false-fails the 500ns gate at the host's 2.5GHz invariant TSC).
- **Vacuity-proof = FLAGS not labels:** assert `MODEL_LOAD_FAILED|MODEL_CORRUPT==0` + an `ml_inference_ran` counter (the degrade stamps `strategy_id=ML`).
- **Slow gate = A⁺:** gate `slow_path_breakdown[REBUILD]` ≤100µs (teeth) + capture the WHOLE-cycle p99 as a regression-FLOOR (NOT a hard ≤100µs — it's ROLLING-dominated 100-300µs *by design*). Floor capture rides to E.1.2; E.1.0 = absolute + run-relative.
- **Emit in the `[OMS_BENCH]` `if constexpr(BENCH)` block**, NOT the always-on `DumpLatency` (which would leak to production). Gate reads the struct field in-process, never parses its own stderr (Class-18 mirror).
- **(i)/(ii) split:** CI gates p50-p99 (intrinsic-cost, reproducible backtest); the deep tail p999/p9999 is operator `perf` profiling → `.E.2` observability (`fox_node_latency_hot_p9999_ns`). Do NOT expand `CoreLatencyStats` (256→10k ring = 640KB blows L1d).
- **ADD (this session's fold):** cache-miss perf-counters + per-tick classification (trade / param-update / quiet) so the bench ALSO verifies the #15 trade-branch fix + attributes the p99.
- **Pattern = the landed H10 gate** (compile→measure→gate + a non-vacuity assert).

## ⭐ FORWARD LOOKAHEAD — how THIS session reshapes the downstream `.E` plans

The operator asked specifically for this. Each downstream plan is now affected:

- **E.1.1 (Core→Node rename):** the H8 floor capture **moved OUT of E.1.1** (a names-only rename is p99-inert → the floor protects nothing there). E.1.1 instead gets **proof-of-neutrality** — run the gate to prove the rename was byte/perf-neutral. The new emit reads `cores[c]`/`display_meta[c]` → E.1.1's rename sweep MUST include the H8-added lines (they land before E.1.1).
- **E.1.2 (NodeState SoA relayout):** the slow-path optimization **does NOT fold here** — A-3 refuted the subsumption (the rolling-stats are a DISJOINT cohort from Portfolio/Position SoA + low-ROI: ROLLING is COMPUTE-dominated, 11 pushes + regression, not `rolling_baseline` cache; `rolling_baseline` is 64KB w/ a ~64-byte per-cycle hot set). E.1.2 still ARMS the H8 whole-cycle ratchet + the size tool covers its SoA structs.
- **E.1.3 (aggregator-coherence) — THE BIG ONE:** the shared-market-state insight homes here. The rolling stats are a pure function of the shared fan-out tick → compute ONCE per cluster, share across the N nodes via a **seqlock-published snapshot = SPMC** (1 cluster-writer / N node-readers; non-blocking, preserves per-node independence/H22 — NOT an SPMC ring which couples consumers). This UNIFIES with the engine→viewers snapshot publish (one primitive). The "shared across cores" comment (`PortfolioController.hpp:254`) shows it was the v4.3 intent, never realized. **D-230 captures it.**
- **E.1.5 (per-cluster purity):** the shared-market-state + H22 (a node is a pure function of its own inputs + its cluster's shared resources — sharing STRENGTHENS purity).
- **E.1.6 (multi-exchange):** the **DOS constraint** — N-streams-per-node = a mini-DOS on the venue (rate-limit/conn-cap/IP-ban) → the cluster owns ONE venue connection (cluster = one venue × N risk-sharded nodes). Live-readiness REQUIREMENT, not just an optimization. + the cache-pinning DEPLOYMENT requirement (isolcpus + HT-off — the SMT-pinning `EngineSharded_SmartSlowPathPins` is best-effort).
- **The optimization leaf (a NEW own `.E` leaf — NOT E.1.2):** the slow-path COMPUTE optimization (the 11 pushes + regression) + cache-residency + the **trade-branch H20 fix (#15, elevated)**. Constraint: same-W-better-layout (smaller-W = alpha loss).
- **ALL future hot-path work** is now governed by `critical-moment-determinism-over-average-latency.md` (jitter-where-it-matters, not average — settled doctrine).
- **ALL future audits** are governed by `mechanical-verification-of-derived-code-facts.md` — the hand-asserted latency budgets ("~100-300µs ROLLING / ~5-30µs REBUILD") are UNVERIFIED until the H8 bench measures them; do NOT lock decisions/ROI on them.

## Findings homed (don't drop)

- **The uncommitted RollingStats static_asserts** (engine working tree) — compile-verified, commit with H8 or standalone.
- **The doc-debt** (the `O(W)` comment + the 3 stale size comments) → fix at the RollingStats leaf (D-229 STATUS: "queued to the doc-fix deliverable").
- **VERIFIED stale comments this session (each its own row — NOT top-N; → #11 umbrella sweep):** (a) `LatencyHistogram` "~640B" → actual **576B**; (b) `ParameterSlot.hpp:37` "~1KB memcpy ~20-30ns" for `GateParameters<64>` → actual **192B/~4ns** (+ the stale "~63ns ExecutionCore_Tick" Phase-05 number); (c) the two log-histograms report OPPOSITE bucket bounds (`CoreLatencyStats` upper vs `LatencyHistogram` lower — I-2 doc-rot). **GREP-CANDIDATES not yet tool-verified** (→ #11): `OmsPhasedDrain` "~7KB" · `ThompsonBandit` "~112B" · `FeatureStandardizer` "~600B" · `CfgGateRegistry` "~1-1.5KB" · the flag-registry "N bytes per core" claims · the `RollingStats.hpp:198` "computation is O(W)". The 6 preventive pins (CoreLatencyStats=1600 / LatencyHistogram=576 / Position=128 / Portfolio=2112 / GateParameters=192 / ExecutionCore=68352 — all tool-measured) ride #11 too.
- **The remaining instrument-hardening:** #11 umbrella-sweep finish, #12 subagent registry, #13 codify the rest (Class-51 + cascade-bridge), #14 re-sweep — all behind the H8 bench + the trade-branch fix (operator: H8 to the front).
- **The close-session PROCESS (Task #16 — ✅ DONE).** Eval: MOSTLY a COMPLIANCE failure — the handoff-TaskList capture (`/handoff` Stage 1.5) + the Stage-5.5 git-diff checklist ALREADY exist; this session hand-executed the close (Write'd the handoff instead of invoking `/handoff`) → silently skipped both. The 2 residual REAL gaps are now **CLOSED**: `check_handoff_capture_completeness.py` enforces a TaskList snapshot + `rebuild_doc_indexes.py --check` (sweep HARD-1b) enforces index currency. **ROOT DISCIPLINE codified** (`feedback_invoke_composable_skills_dont_hand_execute`): invoke composable skills, don't hand-execute their steps.
- **The homed ML-inference idea** (`plans/_ideas/2026-06-16-ml-inference-feature-window-granularity.md` — moved from a loose `note` file).

## Capture-completeness (this session — nothing lost; close-session-verified)

- **Decisions → log:** D-228 (H8 A⁺ directions) · D-229 (foundation-hardening sequence + the size-miscalc mechanism) · D-230 (cache-residency + cluster/SPMC + DOS, CONTEXT-not-decision) · D-231 (p99-variance + trade-branch, CORRECTED+ELEVATED). All paired sentinels. Verified by the independent review (CLEAN).
- **Findings → ledger:** NO new TECH_DEBT/PARITY entries this session — the session's findings homed to the decision log (D-228..231) + the E.1.0 plan body (the A-class verdicts) + `plans/_ideas/` (the ML-inference idea); the doc-debt (stale `O(W)` + size comments) → D-229 ("fix at the RollingStats leaf"); the `.githooks`-sync gap remains TECH_DEBT-204 (task #7).
- **Codifications → DESIGN_SPECS + memories:** 2 specs (`mechanical-verification-of-derived-code-facts.md` + `critical-moment-determinism-over-average-latency.md`) + **3 memories** (`feedback_mechanically_verify_derived_code_facts` + `feedback_critical_moment_determinism_not_average_latency` + `feedback_invoke_composable_skills_dont_hand_execute` — all indexed in MEMORY_EXTENDED). Class-51 + cascade-bridge codifications still QUEUED (#13). Close-session GUARDS added (#16 done): the handoff task-section enforcement + `rebuild_doc_indexes.py --check` (sweep HARD-1b).
- **Tools → built + enrolled:** `check_struct_size_budget.py` + `_selftest.sh` (TOOLS.md, D-137-compliant). RollingStats size-pins applied + compile-verified.
- **Tasks:** #10 (tool, in_progress) · #11-14 (instrument-hardening) · #15 (trade-branch H20, HIGH). #1 (H8 bench) pushed to the FRONT.
- **Verified:** doc-CI sweep CLEAN · independent deliverable-review CLEAN · mechanical capture-audit (index/sentinels/skill-linkage) PASS.

## Pending work (TaskList snapshot — recreate ALL at pickup; the narrative above covers only the headline items)

| # | Task | Status | Note |
|---|---|---|---|
| 1 | **H8 latency bench** | NEXT (front) | the converged A⁺ build; rides consult-before-coding; also classifies trade/param/quiet ticks to verify #15 |
| 2 | H7/H20 branchless ADVISORY gate | pending | never RED (undecidable per the 3:3); sources `sharded_files.txt`; KNOWN_COHORT --strict allowlist |
| 3 | grep-CI aggregator returns at ≥2 gates | pending (blocked by #2) | `check_hot_path_invariants.sh` + an rc-propagation selftest |
| 4 | baseline capture → HOT_PATH_CHANGELOG | pending | folds INTO the H8 bench (the bench captures the `.E.0.10` reference) |
| 5 | V-class verify | pending | 5 binaries + 3635/0 + calls_graph_diff CLEAN + check_determinism.sh explicit |
| 6 | ship-close codifications | pending | the seam-fold to E.1.1/E.1.2 (the cascade's un-written-fold finding); Class-51 also in #13 |
| 7 | **TECH_DEBT-204** — `.githooks` not mirrored by /sync-workspace | pending (deferrable apparatus) | structural fix = symlink engine `.githooks` → workspace |
| 8 | cascade-check (H8 across E.1.1/E.1.2) | ✅ done | ran as the cascade BRIDGE in the A⁺ 3:3 |
| 9 | slow-path optimization homing | ✅ done (CORRECTED) | A-3 REFUTED the E.1.2-subsumption → it's an **OWN `.E` leaf** (compute+cache, disjoint cohort, low-ROI cache) — NOT E.1.2 |
| 10 | size+cache guard tool | ✅ done | tool + selftest + RollingStats pins + the derived-facts-drift spec |
| 11 | umbrella sweep finish | pending | the other ~15-20 stale size comments + the 6 preventive pins + wire to pre-commit |
| 12 | subagent registry + custom agent types | pending | stop over-dispatching general-purpose; I/A/cascade/V roles + routing index |
| 13 | codify Class-51 (first canonical = the vacuity hole + the false-RED inverse) + cascade-bridge | pending | derived-facts-drift DONE; these 2 remain |
| 14 | RE-RUN the I→A sweep with the hardened tools/agents | pending | the converged H8 design is PROVISIONAL until this (re-open is intended); before the E.1.0 close |
| 15 | **[HIGH] trade-branch → branchless (H20)** | pending | LOCKED on principle (critical-moment determinism); behind the H8 bench that verifies it |
| 16 | harden the close-session capture process | ✅ done | landed the 2 guards (handoff task-section enforcement + index-currency HARD-1b) + the invoke-don't-hand-execute memory; #2/#3 were compliance, not missing codification |

## Operator collaboration reminders (every-turn)

- Address **Caramel as Caramel / she / her**. No AskUserQuestion modals — inline options.
- Evaluate on **robustness + latency + design philosophy, NOT time**. Latency = **critical-moment determinism, not average** (now codified).
- **Thoroughness > pace** (operator-stated this session): harden the instruments → measure → then optimize. The trade-branch fix is justified ON PRINCIPLE (H20) — the bench confirms, doesn't gate.
- **H8 bench build = consult-before-coding** (engine latency-emit path). Hot-path BEHAVIOR changes (the trade-branch fix) ride the audit gate.
- **Architecture calls** (realizing the shared-market-state, cluster→venue topology) = propose→operator-ratify. The APPARATUS (tools/sweeps/codifications) = autonomous-with-boundary-reports.
