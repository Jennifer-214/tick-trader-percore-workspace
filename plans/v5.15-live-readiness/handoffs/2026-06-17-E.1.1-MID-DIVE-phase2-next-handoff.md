---
type: handoff
status: active   # the one live handoff (no-arg /accept-handoff resolves here). Supersedes 2026-06-17-E.1.1-core-node-rename-handoff.md (the pre-dive scaffold pickup).
ship_tag: "E.1.1 — Core→Node rename + cfg-key CLEAN-BREAK + single_core LIVE deletion. MID-DIVE: pre-coding done + 7-agent gate RED→amended (folded to the plan SSoT); coding STARTED (helpers 2b8bd6c + Phase 1 3eadb53). NEXT = Phase 2 (the main.cpp untangle)."
plan_type: refactor
deletion_scope: major   # single_core removal (16→12 files) + the cfg-key HARD-REFUSE clean-break — deletion-class. /accept-handoff Stage-4 deletion-class awareness already SATISFIED this dive (the 7-agent gate ran; B14/B15 are in the plan §Gate-synthesis).
sprint_end_goal: make the codebase maintainable + live-ready (v5.X professionalization); HARD live-enable gated behind the whole E-series (E.1.3 ∧ E.1.5)
ship_end_goal: "A fully-renamed Core→Node tree on FROZEN layout (no Core*/MAX_EXECUTION_CORES/CoreContext/state.cores/core_id/per_core_/FOREACH_PER_CORE_CFG_FIELD residue — modulo the PRESERVE-list), the cfg parser HARD-REFUSING legacy core_* AND deleted global keys (clean-break, NOT alias), single_core gone, controller_test green + determinism golden byte-IDENTICAL to E.1.0, and the rename-cascade tool built for E.1.2 to reuse."
predecessor_handoff: handoffs/2026-06-17-E.1.1-core-node-rename-handoff.md (SUPERSEDED — was the PRE-dive scaffold/RED-pre-gate pickup; this doc carries the gate-amended dive state + the 2 commits)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (D-239 = the E.1.1 dive resolutions; the governing predecessors = D-27 / D-61 / D-219 / D-223 / D-131 / H21)
plan_body: plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md (NO LONGER a stale scaffold — its §"Gate synthesis — amendments + corrected dive scope" is the DIVE SSoT; the "## Design-shape — RE-GROUNDED (C-1...)" is current vs HEAD; the "## Design-shape (SUPERSEDED...)" block is the old scaffold retained for audit trail)
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
dependency_graph: plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md
guard_matrix: plans/v5.15-live-readiness/E-guard-coverage-matrix.md
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, the E.1.1 plan body §Gate-synthesis (the SSoT), decision-log D-239, DOCS/DESIGN_PHILOSOPHY.md §15 Glossary, DESIGN_SPECS/refactor-patterns/rename-ship-methodology.md, DOCS/recurring-bug-patterns/class-36-*.md, DOCS/STRATEGY_AND_CODING_RULES.md, plans/_cross-cutting/2026-05-06-latency-path-discipline.md, the .E dependency-graph DAG, regen CODE_MAP at pickup]
engine_head: 3eadb53 (Phase-1 commit; branch feat/v5.15-live-readiness; tree CLEAN; PUSHED to origin 90b2daf..3eadb53 — origin==local; the 2 dive commits [helpers 2b8bd6c + Phase 1 3eadb53] are backed off-machine)
workspace_head: at-or-after the /sync-workspace at this session's close (this session: E.1.1 plan body + DESIGN_PHILOSOPHY §15 + per-node-purity spec + scan_class_44.py + CODE_MAP + decision-log D-239 + 2 memory files + MEMORY_EXTENDED index)
coding_status: mid-coding-checkpoint — pre-coding COMPLETE + Phase 1 of the single_core 3-phase deletion DONE; NEXT = Phase 2 (main.cpp untangle); the rename bulk + cfg clean-break + tools cohort + V-class remain
---

# Handoff — `E.1.1` MID-DIVE → resume at Phase 2 (the `main.cpp` untangle)

## TL;DR (the 60-second pickup)

- **E.1.1 is MID-DIVE, not pre-dive.** The predecessor handoff's "SCAFFOLD/RED-pre-gate, first action = re-ground" is DONE. This session ran the whole pre-coding cycle (C-class re-ground → 7-agent `/precoding-audit-gate` deletion-class → verdict **RED (major amendment, NOT re-design)** → folded into the plan body) AND started coding (2 commits).
- **2 commits landed** on `feat/v5.15-live-readiness` (pushed to origin; HEAD `3eadb53`):
  - **`2b8bd6c`** — node→CPU legibility helpers (`EngineSharded_{NodeHotCpu,DrainerCpu,NodeSlowCpuBase}` + a `[PER-NODE CPU TOPOLOGY]` doc block in `Run.hpp`). Behavior-preserving (each helper returns the identical expression); `build.sh test` **3635/0**; conformance + calls_graph CLEAN. **Standalone, OUTSIDE the names-only rename bisect.**
  - **`3eadb53`** — **Phase 1** of the single_core 3-phase deletion: orphan `LegacyReferenceDriver.hpp` deleted (226 lines, zero callers) + 2 refs cleaned. `build.sh test` 3635/0; `scan_class_44 --strict` clean.
- **THE DIVE SSoT** is the plan body §"Gate synthesis — amendments + corrected dive scope" — read it FIRST. It holds the 2 CRITICALs + the HIGH set + the terminology SSoT + the forward-promises + the 3-phase plan. The scaffold's old grep counts are SUPERSEDED (kept below for audit trail).
- **NEXT ACTION = Phase 2 — the `main.cpp` untangle** (the delicate capital-path part; see below). `3eadb53` is the restore point.
- **Guard posture (operator: "keep going just use ci guards"):** the rename rides UNDER `.E.1.0`'s standing gates (conformance + determinism golden + pre-commit M/H/K/F + calls_graph). A careless slip RED-builds, not silent. Micro-commits, each guard-verified.

## The ideal way to start

1. **`/accept-handoff`** (resolves here). It regens CODE_MAP + loads the baseline + the E.1.1 plan body + the DAG + the guard-matrix + SUBAGENT_ARMING.
2. **Confirm the predecessor state:** `git -C /home/caramel/code/FoxML_Trader_v2 log --oneline -3` should show `3eadb53` (Phase 1) → `2b8bd6c` (helpers) → `90b2daf` (.E.1.0 tag). `git status` CLEAN. `python3 tools/check_latency_path_conformance.py` green (the standing gate, intact). `./build.sh test` 3635/0.
3. **Read the plan body §"Gate synthesis — amendments + corrected dive scope"** (the SSoT) + D-239 (the decisions).
4. **Resume Phase 2** (below) — guard-driven, micro-commits.

## TaskList state at handoff write (recreate verbatim at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | **in_progress (THE SHIP)** | E.1.1 — Core→Node rename + cfg-key clean-break + single_core LIVE deletion |
| #2 | pending | E.1.2 — NodeState SoA / Money relayout (the successor leaf; INBOUND-receives the node→CPU legibility-in-NodeState forward-promise) |
| #3 | pending | #12 — Subagent registry i/a/v/d/c: discoverability pointer + DESIGN_SPEC remainder |
| #4 | pending | TECH_DEBT-204 — .githooks not mirrored by /sync-workspace |
| #5 | pending | #11 — Umbrella sweep finish: stale size comments + preventive pins + wire size tool |
| #6 | pending | #15 [HIGH] — trade-branch → branchless (H20) optimization leaf (D-236) |
| #7 | pending | #3 — grep-CI aggregator at ≥2 gates (re-confirm vs the static ASM gate) |
| #8 | **completed** | Node→CPU legibility helpers (committed `2b8bd6c`) |

**Recreate via TaskCreate.** #1 is the live ship; #8 is done (the helpers); #2 is the successor.

## Per-Phase status — what's NEXT

| Phase | Status | Notes |
|---|---|---|
| Pre-coding (re-ground + 7-agent gate + fold) | **DONE** | verdict RED→amended; SSoT = plan §Gate-synthesis |
| node→CPU legibility helpers | **DONE** | `2b8bd6c` (behavior-preserving, outside the bisect) |
| Phase 1 — orphan `LegacyReferenceDriver` delete | **DONE** | `3eadb53` (3635/0, scan_class_44 clean) |
| **Phase 2 — the main.cpp untangle** | **NEXT** | legacy block delete + dispatch rewrite + the intermixed-setup untangle (delicate; guard-driven) |
| Phase 3 — engine_mode registry-row + readers + parity_harness + tools cohort | pending | atomic; H17 registry-row delete |
| TD-175 cascade tool (widen scan to tools/) | pending | BEFORE the bulk rename |
| The Core→Node rename | pending | driven by the cascade tool; the PRESERVE-list is the executor's exclusion set |
| The cfg clean-break (HARD-REFUSE) | pending | the corrected `:3098-:3099` outer fall-through + GUI parser + global-key refuse |
| C-2 mirror parity-guard | pending | `check_gui_engine_cfg_key_parity.py` + delete stale GUI row |
| V-class verify | pending | build + char-tests + fresh asan/ubsan + calls_graph + determinism golden byte-IDENTICAL + the new parity guard |

## ★ RESUME POINT — Phase 2 (the delicate main.cpp untangle)

The 3-phase single_core deletion is leaves-first; Phase 1 (orphan) is done. **Phase 2 = the `main.cpp` legacy engine removal + dispatch rewrite + the intermixed-setup untangle.** It is NOT a clean line-cut — that's why this session paused here (delicate capital-path judgment, prime-directive "slow down", best fresh).

**The cut (3eadb53 = restore point):**
- DELETE the legacy single-threaded engine block **`main.cpp:286-1339`** + the helper **`engine_force_close_all` (`main.cpp:59-120`** incl. its `[AIRTIGHT RECONNECT]` doc at `:59-64`; legacy-only — sole caller `:782`, INSIDE the doomed block; zero use elsewhere — re-verified at HEAD). main() ends at **`:1340 }`** (no functions after).
- REWRITE the dispatch **`main.cpp:272-285`** (`if (ccfg.engine_mode == ENGINE_MODE_SHARDED){ <oms_bench> tt::EngineSharded_Run<64,BENCH>(ccfg,bcfg); return 0; }`) → **UNCONDITIONAL sharded run** (drop the `if` wrapper; main = setup → sharded run → `return 0;` → `}`).
- **KEEP** the `live_capital_cfg_conflict` refuse at **`main.cpp:266-270`** (gate guardrail G1 — it fires for sharded too). The **NEW-1 single_core refuse `:299-308`** DIES with the block (its purpose — block live on the dead path — is satisfied by the path no longer existing; the `ControllerConfig_IsLiveCapital` predicate it calls SURVIVES with its other ~7 sharded callers).
- **THE DELICATE PART:** main()'s setup (`125-271`) intermixes **sharded-needed** (config parse + the `:266-270` conflict refuse) with **legacy-only state** (`ctrl` `PortfolioController` / `pool` `OrderPool` / `log` `TradeLog` / `rolling_long` — e.g. `:1337 free(ctrl.rolling_long)`). Deleting the block **ORPHANS** that legacy setup → unused-variable errors. **Untangle GUARD-DRIVEN:** make the cut (line-range, e.g. `sed -i '59,120d;286,1339d' main.cpp` — both ranges are INPUT-line-numbered, non-overlapping; too large for a surgical Edit) → rewrite the dispatch → `build.sh test` → the compiler flags every now-unused legacy local → remove them → build to green. Iterate. `3eadb53` restores if a boundary's off.

**Phase-2 done-check:** `build.sh test` 3635/0 + `calls_graph_diff` CLEAN (hot path UNTOUCHED) + conformance green + the conflict refuse `:266-270` intact + no `engine_mode`-conditional dispatch left. Commit `wip(.E.1.1): Phase 2 — delete legacy single_core engine + unconditional sharded dispatch`.

## THEN (still E.1.1, after Phase 2)

- **Phase 3 (atomic):** `engine_mode` field = the **`CfgFieldRegistry.hpp:393` REGISTRY ROW** (H17 — "delete the field" = delete the X-macro row + decrement any field-count `static_assert`) + both `ENGINE_MODE_*` enums (`ControllerConfig.hpp:49-50`) + the parser branch (`:2863-2868`) + the 2 SURVIVING readers `Async.hpp:325` & `PortfolioController._HotReload:1968-81` (excise the `engine_mode` lines — do NOT collateral-delete the sister `num_execution_cores`) + `parity_harness.cpp` disposition (retire or repoint — its single_core↔sharded diff is deleted) + the tools cohort. **PortfolioController the STRUCT SURVIVES** (tests/backtest/EngineTUI/MetricsLog/ShardedSnapshotPersist); its deeper central-drainer removal = **E.1.3** (the non-atomic TD-002 half).
- **Build the TD-175 cascade tool** on `check_struct_alignment.py` — **WIDEN its scan to `tools/`+`build.sh`+`.githooks/`** (the gate found the cascade/done-grep was source-only, missing the vacuously-green tool cohort: `scan_class_27_full.py:272`, `check_identifier_retirement.py` [**COMMIT-BLOCKER**: pre-commit Check H freezes `CORE_STATE_FLAG_*`; co-migrate the ledger + SOURCES via `--update`], `build.sh:271` [`[ -f ]` silent-skip], `check_storage_t_coverage`/`check_field_name_uniqueness`, `check_meta_registry:28`, `check_doc_rename_classification` [its `KEEP_TOKENS` deferred code-symbols TO E.1.1 — co-migrate]).
- **The Core→Node rename** (389 `core_id` + 231 `per_core_` + 22-file `state.cores` + 16-file `MAX_EXECUTION_CORES` + the 6 `Core*` file-basename renames [~27 `#include` cascade] + 10 H15 meta-rows `MetaRegistry.hpp:40-43/80-81/85-87` + the 5 child `PARENT_NAME` refs `:46-50` + `check_per_core_registry_integrity.py`→`_node_`). **RE-DERIVE counts via the cascade tool, NEVER the pasted numbers.** **PRESERVE-list (executor exclusions):** `ExecutionCore`/`ExecutionCore_Tick`/`CoreFrameworks`/`MULTICORE`/`FoxML_Core` + the `experiments/per_core_sharding/` path + CPU-hardware "core" (pinning/SMT/`cpu_id`). Anchor each rename family independently (RBP **Class 36** non-idempotency; `CoreContext` ⊂ `CoreContextDisplayMeta`). Disambiguation rule: `core_*`/`Core*` shard ids → node; "core"=CPU hardware → stays.
- **The cfg clean-break:** HARD-REFUSE at the **OUTER `:2968`-block fall-through (between `:3098` and `:3099`)**, NOT `~:3097` [**C-1 CRITICAL** — `~:3097` is inside the valid-index block + misses bad-index] + the **8 recognized-suffix branches refuse on out-of-range `idx`** (today they silently `continue`) + the **GUI `SettingsPanel.hpp:922` SECOND parser** in lockstep + the **global-key refuse** for `engine_mode=`/`num_execution_cores=` (NOT optional — else silent sharded-LIVE / silent node-count-revert-to-4).
- **C-2 mirror parity-guard:** NEW `tools/check_gui_engine_cfg_key_parity.py` (GUI-writer-key ⊆ engine-parser-key) + DELETE the stale `confidence_freshness_tau` GUI row (`SettingsPanel.hpp:669`) — the engine parser deleted that override (`ControllerConfig.hpp:172`), so post-HARD-REFUSE the GUI would brick its own `engine.cfg` (the concrete operator-brick A-2 found).
- **V-class verify** before the final commit.

## Decisions this dive (D-239 in the decision log)

- **① file-basename renames IN-SCOPE** (the 6 `Core*` files + ~27 `#include` cascade, folded into the TD-175 tool).
- **② single_core CLEAN-DELETE, NO tombstone** — `engine_mode` verified NOT persisted/wire → full clean delete; the operator cfg KEYS HARD-REFUSE (loud, not silent-ignore, not alias); ZERO tombstones (operator anti-bricked-bits preference; generalized into `feedback_tombstone_the_name_reclaim_the_nonpersisted_bit`).
- **③ gate-scope `_PER_CORE`→`_PER_NODE`** rename (`FOREACH_CFG_GATE_PER_CORE` / `FIELD_IDX_PER_CORE_*` / `FOREACH_SLOW_PATH_GATE_PER_CORE_COUNT`) — consistency; the scope IS the per-node scope (operator: "each node uses 2 cores = a real semantic diff").
- **ExecutionCore PRESERVE** — glossary §15 RATIFIED (was PROPOSED/NOT-YET); the conformance manifest names `ExecutionCore_Tick`. **Premise:** a NODE owns **2 CPUs** (hot+slow thread pair, across 2 physical cores), `node_id ≠ cpu_id`.

## Arming for the next action (Phase 2 → the rename)

- **Route to:** guard-driven micro-commits (no judgment-skill needed for Phase 2's deletion — it's a guard-driven untangle). For the bulk RENAME later: `/trace-deps` + `/dod-audit` (already run at the gate; re-fire only if scope shifts). For the cfg clean-break: route through the corrected-refuse design in the plan §Gate-synthesis.
- **RUN (not just read):** `./build.sh test` + `python3 tools/check_latency_path_conformance.py` + `bash tools/calls_graph_diff.sh` + `python3 tools/check_determinism.sh` (golden byte-IDENTICAL — names don't change bytes) after each batch; regen `./tools/gen_code_map.sh`; the TD-175 cascade tool before the bulk rename.
- **Read first:** the plan §Gate-synthesis (SSoT) · `rename-ship-methodology.md` (verify the EXECUTOR vs the grep) · `DOCS/recurring-bug-patterns/class-36-*.md` · `DOCS/STRATEGY_AND_CODING_RULES.md` · `plans/_cross-cutting/2026-05-06-latency-path-discipline.md`.
- **Invariants in play:** H7/H8/H10/H14/H20 (the standing gates the names-only edit must keep green) · H15 (MetaRegistry enrollment + the CI-check sister-cohort rename together) · H17 (`engine_mode` is a registry row, not a manual field) · H21 (no persisted-wire identifier touched; the cfg-KEY clean-break is correct precisely because keys are operator-text) · H22 (the rename IS the per-node terminology bridge).

## Capture-completeness (this session — nothing lost)

- **Decisions → log:** **D-239** written (the E.1.1 dive resolutions — ExecutionCore PRESERVE / ② single_core clean-delete no-tombstone / ③ gate-scope rename / ① file-renames). Paired `<!-- D/C/F: D-239 -->` + `<!-- STATUS: DECIDED 2026-06-17 -->` sentinels. Governing predecessors (D-27/D-61/D-219/D-223/D-131) unchanged.
- **Findings → ledger:** the 7-agent gate's findings (2 CRITICAL + HIGH/MED/LOW) are folded into the plan body §"Gate synthesis — amendments" (the dive SSoT) — that IS their home (E.1.1-scope; not separate TECH_DEBT). NO new TECH_DEBT opened this session. One OBSERVATION homed in this handoff (not a gap): the pre-existing clangd `&cores` static-capture at `Run.hpp:~1645` (GCC + clang asan/tsan accept it; only clangd-LSP flags it; candidate tidy, sister to .B.6 Class-35 — fold into Phase 2/3 since that file's being edited).
- **Memories/skills → indexed:** 2 memories — `feedback_scaffold_outline_depth_at_dive` (NEW; indexed in MEMORY_EXTENDED.md) + `feedback_tombstone_the_name_reclaim_the_nonpersisted_bit` (GENERALIZED: clean-delete is the default, HARD-REFUSE for operator keys, single_core canonical example). NO new skill. Docs: DESIGN_PHILOSOPHY §15 glossary (ExecutionCore ratified + disambiguation rule) + per-node-purity-scale-invariance.md sentence sharpened (2 CPUs / `node_id ≠ cpu_id`).
- **State SSoT updated:** the sprint MASTER banner flipped to E.1.1 MID-DIVE; the old active handoff superseded; this handoff written `status: active` (singleton invariant held).
- **Verified:** `check_session_docs.sh` SWEEP CLEAN (12 HARD + 4 ADV) at close; an INDEPENDENT Stage-5.5 deliverable-completeness review returned **CLEAN** (all 9 deliverables VERIFIED + cross-coherence COHERENT across D-239 + plan body + glossary + per-node-purity + both memories; edits surgical; privacy boundary correct). `build.sh test` 3635/0 on the shipped state; calls_graph CLEAN; conformance green.

---

## Paste this prompt into a fresh Claude Code session to resume E.1.1

```
I'm resuming E.1.1 (Core→Node rename + cfg-key CLEAN-BREAK + single_core LIVE deletion) for the v5.15-live-readiness sprint. It is MID-DIVE — pre-coding is DONE (7-agent gate RED→amended, folded into the plan body) and coding has STARTED (2 commits: 2b8bd6c node→CPU helpers + 3eadb53 Phase 1 of the single_core deletion).

Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-17-E.1.1-MID-DIVE-phase2-next-handoff.md

This is a fresh context window; do NOT trust prior-session memory — verify against current code (HEAD should be 3eadb53).

The DIVE SSoT is the E.1.1 plan body §"Gate synthesis — amendments + corrected dive scope" — read it + D-239 first.

NEXT ACTION = Phase 2 (the delicate main.cpp untangle): delete the legacy engine block main.cpp:286-1339 + engine_force_close_all (59-120) + rewrite the dispatch 272-285 to UNCONDITIONAL sharded + untangle the orphaned legacy setup (ctrl/pool/log) from the sharded-needed setup in main 125-271 (GUARD-DRIVEN: cut → build flags the unused → clean to green). KEEP the conflict refuse 266-270. 3eadb53 is the restore point.

Guard posture (operator directive): "keep going just use ci guards" — the rename rides under .E.1.0's standing gates (conformance + determinism golden byte-IDENTICAL + pre-commit M/H/K/F + calls_graph); micro-commits compile-gated; a slip RED-builds not silent.

Honor D-239: ① file-renames in-scope · ② single_core clean-delete NO tombstone · ③ gate-scope _PER_CORE→_PER_NODE · ExecutionCore PRESERVE. PRESERVE-list (do NOT rename): ExecutionCore/CoreFrameworks/MULTICORE/FoxML_Core + CPU-hardware "core". The cfg HARD-REFUSE goes at the OUTER :2968-block fall-through (:3098-:3099), NOT ~:3097. Watch RBP Class 36 (rename non-idempotency).
```

---

## Quick links

- This ship's plan (the DIVE SSoT): `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md` (§"Gate synthesis — amendments + corrected dive scope")
- Decision log: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-239 + D-27/D-61/D-219/D-223/D-131)
- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- Successor: `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md` (INBOUND: node→CPU legibility-in-NodeState forward-promise)
- Terminology SSoT: `DOCS/DESIGN_PHILOSOPHY.md` §15 Glossary (Cluster > Node[2 CPUs] > CPU core; ExecutionCore PRESERVE)
- Rename methodology: `DESIGN_SPECS/refactor-patterns/rename-ship-methodology.md` · RBP `DOCS/recurring-bug-patterns/class-36-*.md`
- The standing gate the rename must keep green: `tools/check_latency_path_conformance.py`
- Dependency DAG: `plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md` · Guard matrix: `plans/v5.15-live-readiness/E-guard-coverage-matrix.md`
