---
type: handoff
status: active   # the one live handoff (no-arg /accept-handoff resolves here). Supersedes 2026-06-20-E.1.1-phase3-next-handoff.md (Phase 3 now DONE).
ship_tag: "E.1.1 — Core→Node rename + cfg-key CLEAN-BREAK + single_core LIVE deletion. MID-DIVE: pre-coding done (7-agent gate + 8-agent state-audit, folded to plan SSoT). single_core deletion COMPLETE (Phase 1 3eadb53 + Phase 2 7eacb80/2baf0f6 + Phase 3 b9ce419 — engine_mode delete + parity_harness retire). NEXT = TD-175 cascade tool → the Core→Node rename → cfg HARD-REFUSE → C-2 guard → V-class."
plan_type: refactor
deletion_scope: major   # the rename + cfg-key HARD-REFUSE clean-break remain (deletion/clean-break class); the 7+8-agent gates already ran this dive (B14/B15 satisfied; see plan §Gate-synthesis + §State-audit-refinements).
sprint_end_goal: make the codebase maintainable + live-ready (v5.X professionalization); HARD live-enable gated behind the whole E-series (E.1.3 ∧ E.1.5)
ship_end_goal: "A fully-renamed Core→Node tree on FROZEN layout (no Core*/MAX_EXECUTION_CORES/CoreContext/state.cores/core_id/per_core_/FOREACH_PER_CORE_CFG_FIELD residue — modulo the PRESERVE-list), the cfg parser HARD-REFUSING legacy core_* AND deleted global keys (clean-break, NOT alias), single_core gone (DONE), controller_test green + determinism golden byte-IDENTICAL to E.1.0, and the rename-cascade tool built for E.1.2 to reuse."
predecessor_handoff: handoffs/2026-06-20-E.1.1-phase3-next-handoff.md (SUPERSEDED — carried Phase-2-DONE→Phase-3; this doc carries Phase-3-DONE→rename)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (D-239 = the E.1.1 dive resolutions; governing predecessors D-27 / D-61 / D-219 / D-223 / D-131 / H21)
plan_body: plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md — **READ BOTH SSoT sections:** §"Gate synthesis — amendments + corrected dive scope" (the 7-agent dive SSoT) + §"State-audit refinements (2026-06-20)" (the 8-agent sweep delta; the rename-phase refinements live there)
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
dependency_graph: plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md
guard_matrix: plans/v5.15-live-readiness/E-guard-coverage-matrix.md
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, the E.1.1 plan body §"Gate synthesis" + §"State-audit refinements (2026-06-20)", decision-log D-239, DOCS/DESIGN_PHILOSOPHY.md §15 Glossary, DESIGN_SPECS/refactor-patterns/rename-ship-methodology.md, the Class-36 section in DOCS/RECURRING_BUG_PATTERNS.md, DOCS/STRATEGY_AND_CODING_RULES.md, plans/_cross-cutting/2026-05-06-latency-path-discipline.md, the .E dependency-graph DAG, regen CODE_MAP at pickup]
engine_head: b9ce419 (Phase-3 engine_mode delete + parity_harness retire; branch feat/v5.15-live-readiness; tree CLEAN; PUSHED — origin==local at b9ce419. Dive commits: 2b8bd6c helpers + 3eadb53 Phase1 + 7eacb80 Phase2-cut + 2baf0f6 include-prune + b9ce419 Phase3, all on origin)
workspace_head: e42ab8f (the Phase-3 close chain: e293da3 apparatus [controller_test label + parity_harness DELETE + INVARIANTS_MAP/locale/plan-body-tests cohort + TD-140 close + TD-185 defer + CODE_MAP] → 2f7296d [supersede + new handoff + MASTER banner] → e42ab8f [plan-body Phase-3-done markers + E-MASTER TD-140 cell + the addendum's 4 precision fixes]; PUSHED — origin==local at e42ab8f)
coding_status: mid-coding-checkpoint — single_core deletion COMPLETE (Phase 1 3eadb53 + Phase 2 7eacb80/2baf0f6 + Phase 3 b9ce419), ALL GATES GREEN (3635/0 ANSI+GUI · fresh asan/ubsan 3635/0 · determinism golden byte-identical · conformance + calls_graph clean · doc-CI clean · pre-commit Check M/H/K green); NEXT = TD-175 cascade tool (BEFORE the bulk rename) → the Core→Node rename → cfg clean-break (HARD-REFUSE) → C-2 mirror parity-guard → V-class
---

# Handoff — `E.1.1` resume at the Core→Node rename (the `single_core` deletion is DONE)

## ⏩ Addendum (2026-06-21, post-write — independent c-class review + precision fixes)

After this handoff was first written, an exhaustive "are-we-missing-anything" close-out sweep + an **INDEPENDENT c-class currency/capture review** ran — verdict **CLEAN** (zero missed captures, zero incoherence, zero blocking; both repos clean+pushed; `check_session_docs` SWEEP CLEAN 12 HARD + 4 ADV). The review caught 4 MINOR doc-precision items (all line-shift artifacts of the Phase-3 deletions; none touch the shipped code) — **all folded into the body below**: (1) `workspace_head` refreshed to `e42ab8f`; (2) the stale `engine_mode` COMMENT count corrected ~15 → **~18 across 12 files** (the rename-sweep target — don't stop at 15); (3) the cfg HARD-REFUSE (`:3099`) + C-2 (`:172`) absolute line cites flagged **PRE-Phase-3** (shifted ~19-25 lines by the deletions; shift again at the rename → RE-DERIVE at the cfg phase — the STRUCTURAL anchors hold); (4) the FEATURE_LOOKUP ship-close owed-item expanded to "revise 4 stale lines + add 2." Earlier-session currency fixes (plan-body Phase-3-done markers + E-MASTER TD-140 cell) landed at `e42ab8f`.

## ⏩ Update 2026-06-21 (s2) — ① the cascade tool is now BUILT + validated; resume at ② the rename (NOT ① the tool)

The body below frames the NEXT ACTION as "① the TD-175 cascade tool." **That is now DONE** — supersede it with this: **resume at ② the Core→Node rename.** What landed (workspace `9cffbb4` build + `42e462e` completeness fix; engine UNCHANGED + tagged `pre-v5.15.5.F.4d.1.E.1.1` @ `b9ce419` = the rename rollback anchor):
- **`tools/cascade.py rename`** (enumerate-only, **TD-175a CLOSED**) + `cascade_selftest.sh` teeth + the **`check_tools_inventory.py` `build.sh`-scan R2 fix** — wired (`run_all_tests` + TOOLS.md + `rename-ship-methodology` §Mech + `/trace-deps` + `/dependency-chain-trace`); the **Phase-2 enumeration freeze** is frozen at `plan_checks/2026-06-21-E.1.1-rename-cascade-enumeration-freeze.md` (**3645 sites**).
- **D-240** (a `/decision-check` I→A refined the build): **R1** rename=enumerate-only [compiler is the code-token oracle; no code `--apply`] · **R2** standing net rides `check_tools_inventory`, not a new subcommand.
- A post-build **a-class completeness audit** caught + fixed a Class-33 under-count (**2118→3645 sites** — hand-list+START-anchor missed CoreSlowState / the core_<stat> family / embedded compounds / a whole file / guards) via the **family-regex matcher** + per-family selftest controls + a mechanical **freeze⊇raw-rg cross-check** (0 real misses).
- **The SSoT for ②** is the plan body §"Execution update (2026-06-21)" — the consumer-completeness model (BOTH worlds: code=compiler oracle / apparatus=cascade+teeth) + the compounding rename sequence (ONE atomic code+apparatus commit → cfg cluster GUARD-FIRST → V-class → ship-close). Drive ② from the freeze.

## TL;DR (the 60-second pickup)

- **E.1.1 is MID-DIVE, deep in. The `single_core` deletion is COMPLETE** — Phase 1 (`3eadb53`), Phase 2 (`7eacb80` + `2baf0f6`), **Phase 3 (`b9ce419` — engine_mode field/registry-row/enums/parser/readers + parity_harness retire)** are all DONE, PUSHED, ALL GREEN. **TD-140 CLOSED**; **TD-185 deferred to E.1.3** (coupling verified — its `*_ExitAdjust` callers all live in `PortfolioController`'s central-drainer, which E.1.3 removes).
- **NEXT = the actual Core→Node RENAME** (the ship's namesake), gated by **① the TD-175 cascade tool** (the de-risk that MUST come first).
- **THE DIVE SSoT** = the plan body §"Gate synthesis" + §"State-audit refinements (2026-06-20)". The rename-phase refinements (PRESERVE-list, `node_id` 3-hit allowlist, the §HIGH tools-cohort, the cfg HARD-REFUSE location) live in §"State-audit refinements".
- **Guard posture (operator: "keep going just use ci guards"):** rides under `.E.1.0`'s standing gates (conformance + determinism golden + pre-commit M/H/K/F/N + calls_graph); micro-commits, each guard-verified. **CAVEAT (load-bearing):** the standing gates catch ENGINE-SOURCE slips, but `tools/`/`build.sh`/`.githooks/` renames commit **GREEN** today — that's exactly why ① the cascade tool widens the scan there FIRST.

## The ideal way to start

1. **`/accept-handoff`** (resolves here). Regens CODE_MAP + loads the baseline + the E.1.1 plan body (both SSoT sections) + D-239 + the DAG + the guard-matrix + SUBAGENT_ARMING + `rename-ship-methodology.md`.
2. **Confirm state:** `git -C /home/caramel/code/FoxML_Trader_v2 log --oneline -6` → `b9ce419` → `2baf0f6` → `7eacb80` → `3eadb53` → `2b8bd6c` → `90b2daf` (.E.1.0 tag). `git status` CLEAN. `./build.sh test` 3635/0. `python3 tools/check_latency_path_conformance.py` green.
3. **Read** the plan §"Gate synthesis" + §"State-audit refinements (2026-06-20)" (the SSoT) + D-239 + `rename-ship-methodology.md` (Phase 1-6, esp. Phase-1 substring matrix + Phase-4 ONE-mechanical-commit + Phase-5 executor-vs-grep lesson) + RBP Class 36.
4. **Resume at ① the TD-175 cascade tool** (below) — then the rename, guard-driven, micro-commits.

## TaskList state at handoff write (recreate verbatim at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | **in_progress (THE SHIP)** | E.1.1 — Core→Node rename + cfg-key clean-break + single_core LIVE deletion |
| #2 | pending (blocked-by #1) | E.1.2 — NodeState SoA / Money relayout (successor leaf) |
| #3 | pending | #12 — Subagent registry i/a/v/d/c: discoverability pointer + DESIGN_SPEC remainder |
| #4 | pending | TECH_DEBT-204 — .githooks not mirrored by /sync-workspace |
| #5 | pending | #11 — Umbrella sweep finish: stale size comments + preventive pins + wire size tool |
| #6 | pending | #15 [HIGH] — trade-branch → branchless (H20) optimization leaf (D-236) |
| #7 | pending | #3 — grep-CI aggregator at ≥2 gates (re-confirm vs the static ASM gate) |
| #8 | **completed** | Node→CPU legibility helpers (committed 2b8bd6c) |

**Recreate via TaskCreate.** #1 is the live ship; #8 is done; #2 is the successor (blocked by #1).

## Per-phase status — the single_core 3-phase deletion (DONE) + the rest of E.1.1

| Phase | Status | Notes |
|---|---|---|
| Pre-coding (7-agent gate RED→amended + 8-agent state-audit) | **DONE** | both folded to the plan SSoT |
| node→CPU legibility helpers | **DONE** | `2b8bd6c` (behavior-preserving; outside the bisect) |
| Phase 1 — orphan `LegacyReferenceDriver` delete | **DONE** | `3eadb53` |
| Phase 2 — legacy single-threaded engine cut + unconditional dispatch | **DONE** | `7eacb80` (1116 lines) + `2baf0f6` (9 dead #includes) |
| **Phase 3 — `engine_mode` delete (registry-row→cascade) + parity_harness retire** | **DONE** | `b9ce419` — root edit `CfgFieldRegistry.hpp:393` (H17) auto-cascaded the struct field + both `ENGINE_MODE_*` enums + parser + the 2 hot-reload readers; parity_harness RETIRED (CMake target + source + cohort). Closed TD-140; TD-185→E.1.3. All gates green incl. fresh asan/ubsan 3635/0. |
| **① TD-175 cascade tool (widen scan to `tools/`+`build.sh`+`.githooks/`)** | **NEXT** | BEFORE the bulk rename — load-bearing, not optional |
| ② The Core→Node rename | pending | cascade-tool-driven; PRESERVE-list is the exclusion set |
| ③ The cfg clean-break (HARD-REFUSE) | pending | refuse at the OUTER-block fall-through, past the recognized-suffix branches, NOT the valid-index block — ⚠️ absolute line cites are PRE-Phase-3; RE-DERIVE at the cfg phase |
| ④ C-2 mirror parity-guard | pending | `check_gui_engine_cfg_key_parity.py` + delete stale GUI `confidence_freshness_tau` row |
| ⑤ V-class verify | pending | build + char-tests + fresh asan/ubsan + calls_graph + determinism golden byte-IDENTICAL + the new parity guard + **the `node_id` 3-hit allowlist** |

## ★ RESUME POINT — ① the TD-175 cascade tool (BEFORE the bulk rename)

The `single_core` path + `engine_mode` are GONE. The rename is the next substantial surface (~600+ sites) — but a rename in `tools/`/`build.sh`/`.githooks/` commits **GREEN** today (no cascade tool / done-grep watches those files; `.E.1.0`'s standing gates only catch engine-*source* slips). So **build the cascade tool FIRST** (the §HIGH "load-bearing, not optional" item):

- **Build the rename-consumer-enumeration on `tools/check_struct_alignment.py`** (it + `tools/test_check_struct_alignment.py` already exist). Add the WHOLE-tree rename-consumer-enumeration (one comprehensive grep across all access patterns + file types per `feedback_enumerate_consumers_before_registry_row_deletion`) + the ① `#include`-cascade tooth (for the 6 file-basename renames).
- **WIDEN the scan to `tools/` + `build.sh` + `.githooks/`** — the coverage gap. Same-commit, teeth RED→GREEN, the TOOL-REGEX cohort that goes silently dead on a rename (per `rename-ship-methodology.md` Phase-3 TOOL-REGEX bucket): `scan_class_27_full.py:272` · `check_identifier_retirement.py` (**commit-blocker** — Check H freezes `CORE_STATE_FLAG_*`; co-migrate ledger+SOURCES via `--update`) · `check_storage_t_coverage.py` · `check_field_name_uniqueness.py` · `scan_class_44_cfg_orphan.py` (its DEAD_FILES single_core refs) · `build.sh:268/271` (`[ -f ]` silent-skip) · `check_meta_registry.py:28` · `tools_verification_baseline.txt` · `check_doc_rename_classification.py` (its KEEP_TOKENS deferred code-symbols TO E.1.1 — co-migrate). The `check_per_core_registry_integrity.py` integrity check's INTERNAL body hardcodes the old names (coupled rename, not a `git mv`).
- **EXCLUDE `experiments/per_core_sharding/`** from the cascade tool (8-agent: real on-disk dir + a live `ExecutionCore.hpp:312` path comment; the gate's "tool runs the WHOLE source tree" directly contradicts the PRESERVE-list there).

## THEN (the rest of E.1.1, after ① the cascade tool)

- **② The Core→Node rename** — cascade-tool-driven. Surface (RE-DERIVE at dive — counts drift): ~389 `core_id` · 231 `per_core_*` · 22 `.cores[` · 16 files `MAX_EXECUTION_CORES`→`MAX_EXECUTION_NODES` · 7 files `FOREACH_PER_CORE_CFG_FIELD`→`FOREACH_PER_NODE_CFG_FIELD` (H15: 10 meta-rows in `MetaRegistry.hpp` + the CI check `check_per_core_registry_integrity.py`→`check_per_node_registry_integrity.py`) · `CoreContext`→`NodeContext` (anchored single-pass — `CoreContext` ⊂ `CoreContextDisplayMeta`, RBP Class 36 non-idempotency) · 6 file-basename renames (`CoreModelZoo`/`CoreCtx*`/`CoreStateFlag*`/`PerCoreStateFlags`/`CoreLatencyStats`) + ~27 `#include` sites.
  - **★ PRESERVE-list (exclusions):** `ExecutionCore`/`ExecutionCore_Tick` (the hot-path ENGINE; renaming breaks the conformance gate's kernel manifest) · `CoreFrameworks` (dir/namespace) · `MULTICORE_TUI` · **`FoxML_Core`** (8-agent: PRIMARY — a naive sweep makes `FoxML_Node`) · the **`experiments/per_core_sharding/`** path · CPU-hardware "core" (pinning/SMT/`cpu_id` + "physical core"/"single-thread-per-core" comments). Anchor each family longest-first.
  - **`node_id` collision-check is STALE:** the 3 `node_id` hits (`Run.hpp:202/213`) are this plan's OWN helper commit `2b8bd6c` (`NodeHotCpu(int node_id)`) — the intended target namespace, NOT a foreign collision → **V-class needs a 3-hit allowlist** or it misfires post-rename.
  - Per-site human classification: `EngineTUI.hpp:1331/1340` ("controller core" / "resetting core 0 … core 3") + `MLStatusPanel.hpp` 4 runtime `"core %d:"` strings (not compile-caught).
  - **ONE mechanical commit for the whole code-token pass** (+ TOOL-REGEX cohort); NO temporary alias/bridge (blinds the totality oracle). Doc sweep via `tools/check_doc_rename_classification.py` (Phase 5; verify the EXECUTOR against the substring matrix BEFORE `--write` + idempotency proof).
- **③ The cfg clean-break (HARD-REFUSE)** — at the OUTER `:2968`-block fall-through (⚠️ the `:2968`/`:3097`/`:3099` line cites are **PRE-Phase-3** — the [ENGINE MODE]+parser deletions shifted them ~19-25 lines up, and the rename shifts them again → **RE-DERIVE at the cfg phase**; the STRUCTURAL anchor is authoritative: refuse PAST the 8 recognized-suffix branches, NOT inside the valid-index block [C-1: a bad index must reach the refuse]) + the 8 recognized-suffix branches refuse on out-of-range idx + the GUI `SettingsPanel.hpp:922` SECOND parser in lockstep + the **global-key refuse** for `engine_mode=`/`num_execution_cores=` (NOT optional — else silent sharded-LIVE / silent node-count revert; **NOTE post-Phase-3:** `engine_mode=` is currently a silently-ignored unrecognized key — the HARD-REFUSE makes it loud-fail). `core_N_*`→`node_N_*` keys (operator migrates her `engine.cfg`).
- **④ C-2 mirror parity-guard** — NEW `tools/check_gui_engine_cfg_key_parity.py` (GUI-writer-key ⊆ engine-parser-key) + DELETE the stale `confidence_freshness_tau` GUI row (`SettingsPanel.hpp:669`); the engine deleted that override (`ControllerConfig.hpp:~153` — was `:172` pre-Phase-3, shifted by the [ENGINE MODE] deletion; the load-bearing `SettingsPanel.hpp:669` cite is EXACT) so post-HARD-REFUSE the GUI would brick its own `engine.cfg`. **Cross-link PARITY-006** (same key — coordinate). Homed: TECH_DEBT-208 (the structural single-source mirror; the E.1.1 guard makes deferring safe).
- **⑤ V-class verify** before the final commit (build + char-tests + fresh asan/ubsan + calls_graph + determinism golden byte-IDENTICAL + the new parity guard + the `node_id` 3-hit allowlist).
- **At E.1.1 ship-close (owed, not now):** FEATURE_LOOKUP — **add 2 entries** (`single_core` LIVE-path deletion + `core_N_*`→`node_N_*` cfg clean-break) **AND REVISE 4 stale lines** that still describe single_core/engine_mode as live (`FEATURE_LOOKUP.md:20` "single_core LIVE HARD-REFUSED"→now deleted · `:1073` `engine_mode` as a live HAS_SIDE_EFFECT registry row→now deleted · `:1177-1178` single_core LIVE/backtest behavior) — per the independent review. + a decoupling-roadmap breadcrumb (deletion removes a runtime-coupling path → mild ⬆️ for the headless split). **Physical move TD-140 open.md→closed.md** (batched at ship-close per convention; SSoT status already `closed`). Sweep the **~18 stale `engine_mode` COMMENTS across 12 files** (NOT ~15 — the earlier estimate undercounted per the independent review; EngineSharded ×3 / BacktestEngine ×2 / Run ×2 / SettingsPanel ×2 + singles) WITH the rename (gate-on-CODE-residue: live CODE is ZERO).

## Arming for the next action (① the TD-175 cascade tool)

For **① the cascade tool** — surface is CI-tooling (Python rename-enumeration on `check_struct_alignment.py`) + the TOOL-REGEX co-migration cohort:
- **Route to:** guard-tooling build (build + teeth-proof RED→GREEN; no judgment-skill needed for the tool itself). For the rename surface it must enumerate: `/trace-deps` (consumer-chain) + `/dependency-chain-trace chain:<token>` for blast-radius confidence. For the rename execution that follows: apply `rename-ship-methodology.md`'s Phase 1-6 recipe directly.
- **RUN (not just read):** `python3 tools/<new-cascade-tool>.py --selftest` (teeth) + `./tools/check_session_docs.sh` (the doc-CI sweep, which includes the tools-inventory check — enroll the new tool in `DOCS/TOOLS.md`) + after the rename: `./build.sh test` + `./build.sh gui` + `python3 tools/check_latency_path_conformance.py` + `bash tools/calls_graph_diff.sh` + `tools/check_determinism.sh` (golden byte-IDENTICAL) + `tools/run_sanitizer_suite.sh` + regen `./tools/gen_code_map.sh`.
- **Read first:** `DESIGN_SPECS/refactor-patterns/rename-ship-methodology.md` (Phase 1 substring matrix + Phase 3 TOOL-REGEX cohort + Phase 5 executor-vs-grep lesson + the `DOCS/TOOLS.md`-inventory sister-scan lesson) · RBP **Class 36** (rename non-idempotency) · the plan §"State-audit refinements" (the §HIGH tools-cohort list + the standing-gate coverage caveat).
- **Invariants in play:** H15 (the `FOREACH_PER_CORE_CFG_FIELD` MetaRegistry enrollment + the integrity-check rename together — sister-cohort) · H21 (the rename NEVER touches persisted/wire identifiers — cfg KEYS are operator-text not wire; verify by grep pre AND post) · the standing `.E.1.0` gates (H7/H8/H10/H14/H20 — names-only edit keeps them green, or RED-builds a slip).
- **⚠️ ENV NOTE:** dispatched **subagents are hard-denied on `g++`/compiler invocation** in this environment (LANDMINE 11). Run build/sanitizer/conformance gates from the **MAIN agent**, not a dispatched subagent.

## Capture-completeness (this session — nothing lost)

- **Decisions → log:** **NO new operator-steered decision this session.** D-239 governs (already logged); Phase 3 EXECUTED the already-decided plan. The 2 Phase-3 findings — (a) TD-185 coupling VERIFIED → defer to E.1.3 (all `*_ExitAdjust` callers in PortfolioController's central-drainer); (b) parity_harness RETIRE-not-excise (its single_core↔sharded diff is a tautology) — are FINDINGS folded into the ledger/commit, not D-entries (E.1.1-scope execution, not new architectural decisions). No decision-log entry owed.
- **Findings → ledger:** **TD-140 CLOSED** (open.md `status: closed` + closure note; physical move to closed.md homed to E.1.1 ship-close per the batch convention) ✓. **TD-185 disposition UPDATED** (2026-06-21 Phase-3 coupling VERIFIED → DEFER to E.1.3; stays open) ✓. **NO new TECH_DEBT opened.** parity_harness retire cohort all homed/handled (CMake + INVARIANTS_MAP ×3 + locale EXCLUDE + check_plan_body_tests_section.py trigger + its selftest). The **~18** stale `engine_mode` COMMENTS across 12 files (the earlier "~15" undercounted, per the independent review) are HOMED to the rename sweep (gate-on-CODE-residue; live CODE is ZERO — verified).
- **Memories / skills → indexed:** **NONE new this session** (clean guard-driven execution; no new collaboration/judgment learning surfaced, no operator pushback). No new skill. (MEMORY.md / MEMORY_EXTENDED.md unchanged.)
- **State SSoT updated:** prior active handoff (2026-06-20 phase3) → `superseded`; this doc → `active` (singleton invariant held — verify `tools/check_handoff_active_singleton.py`). MASTER banner flipped (Phase 3 landed, 2026-06-21) + pickup repointed here. Engine PUSHED (`b9ce419`); workspace PUSHED (this close via /sync-workspace).
- **Verified:** `check_session_docs.sh` SWEEP CLEAN (12 HARD + 4 ADV) at close; ALL engine gates green at b9ce419 (build test 3635/0 · build_gui ok · **fresh asan/ubsan 3635/0** · determinism golden byte-identical · conformance + calls_graph clean); pre-commit Check M/H/K green (**H21: 46 wire-ids match the ledger — engine_mode confirmed non-persisted**).

---

## Paste this prompt into a fresh Claude Code session to resume E.1.1 at the rename

```
I'm resuming E.1.1 (Core→Node rename + cfg-key CLEAN-BREAK + single_core LIVE deletion) for the v5.15-live-readiness sprint. It is MID-DIVE — pre-coding is DONE (7-agent gate + 8-agent state-audit, folded to the plan) and the single_core deletion is COMPLETE (Phase 1 3eadb53 + Phase 2 7eacb80/2baf0f6 + Phase 3 b9ce419 — engine_mode delete + parity_harness retire; HEAD b9ce419, all green).

Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-21-E.1.1-rename-next-handoff.md

This is a fresh context window; do NOT trust prior-session memory — verify against current code (HEAD should be b9ce419).

The DIVE SSoT is the E.1.1 plan body §"Gate synthesis" + §"State-audit refinements (2026-06-20)" — read both + D-239 + rename-ship-methodology.md first.

NEXT ACTION = ① the TD-175 cascade tool (BEFORE the bulk rename): build the rename-consumer-enumeration on tools/check_struct_alignment.py + WIDEN its scan to tools/+build.sh+.githooks/ (load-bearing — apparatus renames commit GREEN today; the standing gates only catch engine-source slips) + co-migrate the TOOL-REGEX cohort (check_identifier_retirement ledger via --update [Check H is a commit-blocker], check_doc_rename_classification KEEP_TOKENS, scan_class_27/44, check_meta_registry, build.sh:268/271) + EXCLUDE experiments/per_core_sharding/. Teeth-proof RED→GREEN + enroll in DOCS/TOOLS.md.

THEN ② the Core→Node rename (cascade-tool-driven; ONE mechanical commit; PRESERVE-list = ExecutionCore/CoreFrameworks/MULTICORE/FoxML_Core/experiments/per_core_sharding/CPU-hardware-core; anchor longest-first per RBP Class 36; node_id 3-hit allowlist for V-class; H15 MetaRegistry + integrity-check rename together) → ③ cfg HARD-REFUSE at the outer-block fall-through past the recognized-suffix branches (RE-DERIVE the line — the :3099/:3097 cites are pre-Phase-3, shifted by the deletions) + GUI SettingsPanel:922 lockstep + global-key refuse (engine_mode=/num_execution_cores=) + core_N_*→node_N_* → ④ C-2 check_gui_engine_cfg_key_parity.py + delete stale confidence_freshness_tau GUI row → ⑤ V-class verify.

Guard posture (operator): "keep going just use ci guards" — micro-commits compile-gated; a slip RED-builds on engine source (but NOT on tools/build.sh/.githooks — that's why ① comes first). ENV NOTE: run build/compiler gates from the MAIN agent (dispatched subagents are hard-denied on g++ here, LANDMINE 11).

Honor D-239: ① file-renames in-scope · ② single_core clean-delete NO tombstone (DONE) · ③ gate-scope _PER_CORE→_PER_NODE · ExecutionCore PRESERVE. H21: the rename never touches persisted/wire identifiers (cfg keys are operator-text) — verify by grep pre AND post.
```

---

## Quick links

- This ship's plan (the DIVE SSoT): `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md` (§"Gate synthesis" + §"State-audit refinements (2026-06-20)")
- Decision log: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-239 + D-27/D-61/D-219/D-223/D-131)
- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md` (top banner: the 2026-06-21 Phase-3-landed UPDATE)
- Predecessor handoff (superseded): `plans/v5.15-live-readiness/handoffs/2026-06-20-E.1.1-phase3-next-handoff.md`
- Successor: `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md` (INBOUND: node→CPU legibility-in-NodeState forward-promise)
- Terminology SSoT: `DOCS/DESIGN_PHILOSOPHY.md` §15 Glossary (Cluster > Node[2 CPUs] > CPU core; ExecutionCore PRESERVE)
- Rename methodology: `DESIGN_SPECS/refactor-patterns/rename-ship-methodology.md` · RBP Class 36 (rename non-idempotency) in `DOCS/RECURRING_BUG_PATTERNS.md`
- Standing gate the rename must keep green: `tools/check_latency_path_conformance.py`
- Dependency DAG: `plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md` · Guard matrix: `plans/v5.15-live-readiness/E-guard-coverage-matrix.md`
- LANDMINE 11 (subagents g++-denied): `DOCS/LANDMINES.md`
