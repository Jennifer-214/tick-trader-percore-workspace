---
type: handoff
status: active   # THE one live handoff. Supersedes 2026-07-17-E.1.2.A-P6-162of163-fixedpointn-finding-next-handoff.md (FixedPointN converted + committed). ≤1-active enforced by check_handoff_active_singleton.py.
ship_tag: E.1.2.A
plan_type: refactor   # in-code documentation infrastructure + the completeness-gate toolchain — maintainability/framework deliverable, NOT a capital-path change
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: the locked [SCHEMA]_[v1.0] tag system rolled out to EVERY engine file + the CI COMPLETENESS gate standing (structural + DERIVED + doc axes) → then toolchain foxtag-cutover + the plugin V1 pass → V1 integrated system (D-358)
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-07-17-E.1.2.A-P6-162of163-fixedpointn-finding-next-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md, DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md]
deletion_scope: none   # comments-only + lossless-gated everywhere; MARK-never-delete ([DEPRECATED])
coding_status: phaseC-open   # Phase-T (the completeness gate) COMPLETE + standing; Phase-C cleanup 1/~48 (GCN done, the worked template); 47 remaining
---

# E.1.2.A — completeness-gate STANDING; Phase-C cleanup open (1/~48); DERIVED-facts backlog homed

**Created:** 2026-07-17
**Target:** Phase-C cleanup of the ~47 remaining lumped/missing units (comments-only, tool-gated) → then flip the gate HARD-zero + codify the going-forward rule → then the foxtag toolchain-cutover + the plugin V1 pass (D-358)
**Branch:** feat/v5.15-live-readiness
**Plan / decision-log:** `plans/v5.15-live-readiness/subplans/2026-07-05-E.1.2.A-comment-system-and-doc-consolidation.md` **§ COMPLETENESS-GATE** (the whole sequenced phase + guardrails) + decision log `v5.15.5.F.4d.1.E-architecture-v2.md`
**Sprint MASTER:** `plans/v5.15-live-readiness/MASTER.md`

**Engine state at handoff write:**
- HEAD: `e9a8ab8` (GateControlNetwork restructure, Phase-C 1/48)
- Prior: `3657b32` (FixedPointN P6.128, 163/163 tracked)
- Version.hpp: `5.15.5.F.4d.1.E.1.1`
- Suite baseline: `3702/0` (verify `./build.sh test`) — comments-only work did NOT move it
- Working tree: `M Strategies/RegimeDetector.hpp` (CARAMEL'S WIP — DO NOT TOUCH / DO NOT git-add; its `:543`/`:627` `[STATE]`/`[INIT]` reds are the accepted-known sweep reds) + untracked scratch. Otherwise clean.

**Workspace state at handoff write:**
- HEAD `4872f1e` (the completeness-gate toolchain batch) + uncommitted (pending this close's `/sync-workspace`): the plan (§ COMPLETENESS-GATE additions), `tools/check_conversion_completeness.py` (selftest → synthetic corpus-independent known-bad), `tools/lib/completeness_baseline.txt` (regen 49→47 after GCN), `DOCS/CODE_TAG_INDEX.md` + `DESIGN_SPECS/TAG_INDEX.md` (regen), + this handoff.

---

## TaskList state at handoff write (recreate via TaskCreate)

| ID | Status | Subject |
|---|---|---|
| #1 | in_progress | E.1.2.A umbrella — in-code documentation system |
| #2 | in_progress | Phase 4/5 residual — PLUGIN-side toolchain items (foxtag cutover + plugin) |
| #3 | in_progress | Phase 6 — [SCHEMA]_[v1.0] rollout (163/163 TRACKED converted; 15 gitignored tests/ = bonus, pending) |
| #8 | completed | Completeness gate — checker BUILT + calibrated |
| #9 | completed | Completeness gate — Phase T (tool+CI): policies + CI baseline + TOOLS.md + commit |
| #10 | in_progress | Completeness gate — Phase C cleanup: 1/~48 done (GCN); 47 remaining |
| #4/#8b/#9b | pending | plugin V1 polish · 15 gitignored tests/ · final sweep + V1 north-star |

---

## What landed this session

**Engine (committed):**
- **`3657b32` P6.128** — `FixedPoint/FixedPointN.hpp` converted (the money-numeric SSoT, last tracked file → **163/163**). 22 units (FILE + 3 core `[STRUCT]`s + `[TYPE]_[Money]` + traits + epoch + the certified kernels + `Money_*`/`fp2_*`). Comments-only (LOSSLESS), suite 3702/0. The **vestigial generic `FPN_*` `.w` op family LEFT UNCONVERTED** with a carve-out `[COMMENT]` + `[FUTURE_WORK]_[TECH_DEBT]_[TECH_DEBT-242]` (do-it-twice avoidance; the D-163 reshape). Accuracy-nudge landed (the "ported to `.v`" overclaim corrected). First-in-corpus template-specialization block naming (`[STRUCT]_[FixedPoint<2,64>]`).
- **`e9a8ab8`** — `ML_Headers/GateControlNetwork.hpp` restructured: the lumped `[FUNCTION]_[GCN_forward]` block (2 structs + 2 fns) → **4 proper units** (`GCN_input`/`GCN_network` structs + `GCN_forward`/`GCN_backward` fns). **THE worked template for Phase-C.** Comments-only (LOSSLESS); operator's personal code-local comments preserved VERBATIM (independent-review-confirmed).

**Workspace (`4872f1e` + this-close sync):**
- **THE completeness gate** — `tools/check_conversion_completeness.py` (+ `_selftest.sh`): the planned coverage-gate increment. **C1** lumped / **C2** missing / **C3** no-DERIVED, with the ≤3-field-POD proportionality exemption; template-tolerant + tier-3-aware + count-helper + same-family-sub-registry exempt (policy #1). Corpus-independent non-vacuity `--selftest` (4 checks). **Wired HARD into `check_session_docs` with a grandfathered baseline** (`tools/lib/completeness_baseline.txt`, now **47**; a NEW lumped/missing unit fails immediately). Enrolled in TOOLS.md.
- **`[CLASS]` resolver fix** — `check_code_tag_blocks.py` now scans the per-class subfiles (`class-NN-*.md`) not just the main catalog → all 55 classes resolve (was a corpus-wide dangling-`[CLASS]` gap); +2 selftest guards.
- **`[DEPRECATED]`/`[MARKED_FOR_DELETION]` vocab** (mark, never delete — sister to `[OUTDATED_INFO]` + H21).
- **`tools/CLAUDE.md`** — context-aware toolchain orientation (loads when editing `tools/`): the dev-plane's own invariants (one-parser-N-consumers / grammar-derived / migration-contract / non-vacuity / mark-not-delete).
- template-spec naming backfill in `CODE_TAG_TEMPLATES.hpp`; plan § COMPLETENESS-GATE (the full sequenced phase + guardrails + tag-browser payoff + DERIVED-facts backlog).

---

## THE remaining work (the finish line)

### 1. Phase-C cleanup — ~47 lumped/missing units (task #10). Comments-only, tool-gated.
Run `python3 tools/check_conversion_completeness.py` for the live list. Each fix: give the lumped/missing unit its own proper block (GCN is the worked template — split a lumped block, preserve code-local comments VERBATIM, D-326). Per fix: `lossless.py` (code byte-identical) → validator → `check_conversion_completeness` (the finding drops) → regen the baseline (`--write-baseline tools/lib/completeness_baseline.txt`, it shrinks) → commit. **HARD rules unchanged:** NEVER touch `RegimeDetector.hpp` (Caramel's WIP; its 3 findings stay grandfathered until she finishes); code bytes NEVER change (comments-only). **When the count → 0 (excl. RegimeDetector): flip the CI check HARD-zero.** Note the `*_LegacyV1` structs (ConfidenceScore) get a block **+ a `[DEPRECATED]` marker** (policy #2 — live wire-compat, documented not deleted).

### 2. DERIVED-facts maturation backlog (the operator's 2026-07-17 findings; § COMPLETENESS-GATE axes 2-3).
- **C4-advisory (empty DERIVED)** — many `[STRUCT]` blocks have an empty `[DERIVED]` because the layout emitter doesn't cover them (D-327). The real fix = extend `check_cache_layout`/`emit_record_layout`/the foxtag layout producer to cover more structs; a C4-advisory surfaces the backlog.
- **C5-preventive (written codegen quartet)** — a guard flagging a WRITTEN `[SIZE]_[N instr]`/`[SIMD]`/`[BRANCHES]` in a block. **Corpus is CLEAN of these now (verified: 0 written)** — D-327 IS respected; the BanditLearning screenshot was the LIVE HUD PREVIEW (the `0 instr` = un-instantiated-template display, D-327-anticipated). So C5 is a REGRESSION guard, not a cleanup driver. (Plugin UX polish: show `N/A (un-instantiated)` instead of `0 instr` in the live preview.)
- **Call-graph / data-flow coverage** — some blocks lack filled `[UPSTREAM]`/`[CONSUMERS]` (plugin `:FoxSymdepsDerived!` fill coverage).

### 3. Going-forward rule to CODIFY (after Phase-C hits HARD-zero).
An always-loaded rule: "every new code unit gets a `[SCHEMA]` block" (deliberately deferred until the rollout completes — a hard "all units blocked" rule is premature mid-conversion). The completeness gate is the enforcement; the rule is the human/AI-facing codification.

### 4. foxtag cutover + the plugin V1 pass (D-358; Caramel's plugin session, D-353).
The completeness check is Python (D-349 migration contract: Python authoritative until parity-gated). The cutover = add a `foxtag code-units` producer (C++) → rewrite the check as `foxtag units` ∆ `foxtag code-units` → `parity_check.sh`-gate → cut over. Plugin: the tag-browser (faceted `foxtag units --tag X --type Y` → expandable tree, D-334) + the DERIVED-write-role + the UX polish.

---

## Plugin PARITY tests (the operator asked "what do I test in the plugin") — this session touched ZERO plugin code
Everything this session was CLI tools + CI gates + schema/vocab. The `fox-symdeps.nvim` plugin was NOT modified. Two PARITY checks worth verifying at pickup:
1. **Vocab pickup** — does the plugin surface the new `[DEPRECATED]`/`[MARKED_FOR_DELETION]` tags (it reads `doc-tag-vocabulary.md`)? A `foxtag units --tag DEPRECATED` should work; the plugin's tag-completion should offer them.
2. **`[CLASS]` resolver parity** — the resolver fix (scan `class-NN-*.md` subfiles) was Python-only. Does the plugin's `[REFERENCE]_[CLASS]` resolution have the SAME subfile-scanning gap? If it only reads the main catalog, `[CLASS]_[23]` etc. dangle in the plugin. Port the subfile-glob fix to the plugin resolver.

---

## Accepted-known red (NOT yours to fix)
`check_session_docs.sh` full-tree scan is RED on `Strategies/RegimeDetector.hpp:543 [STATE]` + `:627 [INIT]` — Caramel's uncommitted WIP pilot. Present all session as the 2 accepted RegimeDetector reds. Every other sweep check GREEN.

---

## Capture-completeness (this session — nothing lost)
- **Decisions:** the 2 policy calls (sub-registry family = `[SECTION]` variants; `*_LegacyV1` = block + `[DEPRECATED]`) + the tool→CI→cleanup sequencing + the foxtag-cutover-per-contract are captured in the plan § COMPLETENESS-GATE. (Candidate decision-log D-entries for the next session: the completeness-gate increment + the 2 policies + the `[DEPRECATED]` vocab — currently homed in the plan; formalize as D-359..D-36x if the log wants them.)
- **Findings → ledger:** the DERIVED-facts backlog (C4/C5/call-graph) + the going-forward-rule + the foxtag-cutover are homed in the plan. TECH_DEBT-242 (the vestigial FPN_* reshape) unchanged.
- **Memories/skills → indexed:** NONE new this session — no memory/skill/DESIGN_SPEC minted. The disciplines this session surfaced (the CALIBRATION-CORPUS golden-broken×golden-complete fixture set · the DERIVED-facts maturation axes · the mark-never-delete `[DEPRECATED]` convention · the going-forward "new unit → a block" rule) are all homed in the plan § COMPLETENESS-GATE — candidates for memory / DESIGN_SPEC codification next session (flagged, not yet formalized; proportionate — confirm-recurring first).
- **Meta-error harvested:** I over-claimed "the plugin WROTE the codegen facts (D-327 violation)" from a screenshot, FALSIFIED by a corpus grep (0 written). Lesson captured in the plan's DERIVED-CORRECTNESS note — an instance of `feedback_ground_design_in_real_code` (verify a "the tool did X wrong" claim against the actual corpus BEFORE asserting). No new catalog row (existing-discipline instance).
- **Independent review:** ALL-CLEAN (16/16); operator personal comments preserved verbatim (confirmed).

---

## Paste this prompt into a fresh Claude Code session

```
type: handoff-pickup

I'm picking up E.1.2.A — the completeness gate is STANDING; Phase-C cleanup is open (1/~48 done, GCN is the worked template).

Run: /accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-17-E.1.2.A-completeness-gate-phaseC-open-handoff.md

State: the conversion-COMPLETENESS gate (tools/check_conversion_completeness.py) is built, calibrated, and wired HARD into check_session_docs with a grandfathered baseline (47 tracked; a NEW lumped/missing unit fails). Phase-C = fix the ~47 remaining lumped/missing units (comments-only, tool-gated; GateControlNetwork.hpp e9a8ab8 is the worked template). Then flip the gate HARD-zero + codify the going-forward rule. Then the DERIVED-facts maturation backlog (C4 empty-DERIVED / C5 codegen-write regression guard / call-graph coverage) + the foxtag cutover + the plugin V1 pass.

HARD rules: (1) NEVER touch or git-add Strategies/RegimeDetector.hpp — Caramel's WIP (its :543/:627 reds are accepted-known). (2) Code bytes NEVER change — comments-only, lossless-gated. (3) MARK, never delete — a rotted/legacy unit gets [DEPRECATED]/[MARKED_FOR_DELETION], never a code deletion.

Do NOT trust prior-session memory — verify against current code. Caramel will iterate with you (the plugin half is HER session, D-353).
```

## Quick links
- Plan (the whole phase): `plans/v5.15-live-readiness/subplans/2026-07-05-E.1.2.A-comment-system-and-doc-consolidation.md` § COMPLETENESS-GATE
- The gate: `tools/check_conversion_completeness.py` (`--selftest` · `--baseline tools/lib/completeness_baseline.txt` · `--write-baseline`)
- Worked template: `ML_Headers/GateControlNetwork.hpp` @ `e9a8ab8`
- Toolchain orientation: `tools/CLAUDE.md`
- Schema SSoT: `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md`
