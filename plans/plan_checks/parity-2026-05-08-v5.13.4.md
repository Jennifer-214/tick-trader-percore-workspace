# /parity-check report — v5.13.4 (sell-side bandit)

**Date:** 2026-05-08
**Commit SHA:** c03749e (v5.13.4.D — sell-side bandit tests + propagation)
**Audit scope:** v5.13.4 (4 sub-tags: A/B/C/D)
**Cross-check baseline:** post-v5.9.4a protections + v5.10.0a bandit pattern

## Plan summary

- **HEAD:** c03749e, **Tests:** 2048/2048 (all pass)
- v5.13.4.A: `EnsembleModelZoo.exit_bandits[NUM_REGIMES]` +
  `EnsembleModelZoo_InitExitBandits` helper; cfg fields
  `exit_bandit_enabled` + `exit_bandit_lr`; per-slot int8_t pair
  `last_exit_predicted_arm[16]` + `last_exit_predicted_regime[16]`
  on OrderManagerState
- v5.13.4.B: reward attribution in `EventLoop_DrainPostFillOneCore`
  exit-loop body (NOT HandleFill); 5-condition gate; counterfactual
  formula using `original_tp` (locked at entry)
- v5.13.4.C: `<core_model_dir>/exit_bandit_state.json` persistence
  (separate file); boot load + shutdown save in EngineSharded
- v5.13.4.D: 30 new tests; engine.cfg.example propagation;
  CHANGELOG + HOT_PATH_CHANGELOG entries

---

## Findings by severity

### CRITICAL — None
### HIGH — None

### MEDIUM

**M.1 — Counterfactual fee_rate parameter, not cfg-bound**
- File: `ControllerEventLoop.hpp:1402`
- Risk: Paper-test fee assumption may diverge from live taker rate.
- Already-protected: parameter explicit in signature (discoverable);
  default 0.001 conservative.
- Action: document in OPERATOR_DEPLOYMENT.md

**M.2 — Regime captured at submit, used at fill (intentional lock-in)**
- Files: `EngineSharded.hpp:2963` (capture) vs.
  `ControllerEventLoop.hpp:1386` (read)
- Behavior: Bandit attribution uses regime at SUBMIT time, even if
  regime changes before fill.
- Verdict: DOCUMENT-ONLY (intentional — stable, monotonic
  attribution; matches buy-side at v5.10.0a.G.8)

**M.3 — Per-slot int8_t bounds-checked at consume site**
- File: `ControllerEventLoop.hpp:1388-1390`
- Already-protected: explicit `chosen_arm >= 0 && chosen_arm <
  exit_predictor_count && regime >= 0 && regime < NUM_REGIMES`
- Action: optional assertion at capture site for early failure on
  trainer→engine model mismatch.

### LOW

**L.1 — InitExitBandits single-arm comment doesn't reference buy-side**
- File: `CoreModelZoo.hpp:1163-1167`
- Verdict: cosmetic; align comment with v5.10.0a.G.7

**L.2 — Missing end-to-end snapshot test for exit attribution**
- Components tested individually; full chain (load model → submit
  → fill → verify reward credited) deferred.
- Verdict: DEFERRED (low priority post-ship)

### DOCUMENT-ONLY

**D.1 — Handle indirection: exit_bandits uses exit_predictor_count
(not primary_count); buy_bandits uses primary_count via v5.11.62
indirection.** Intentional separation; document in CoreModelZoo.

**D.2 — Stamp body unchanged; sidecar file pattern chosen.**
Forward-compat by absence; smaller blast radius than Surface G
extension.

---

## Cross-cutting concerns

### G.1 — Production-caller field-population audit (Section L)

| Caller | Field | Populated? |
|---|---|---|
| `EngineSharded.hpp:2961` slow-path capture | `last_exit_predicted_arm[pidx]` | YES |
| `EngineSharded.hpp:2963` slow-path capture | `last_exit_predicted_regime[pidx]` | YES |
| `ControllerEventLoop.hpp:1418` post-attrib clear | both fields | YES (set to -1) |
| `OrderManager.hpp:526` Init | both fields | YES (-1) |
| `ControllerEventLoop.hpp:1385-1386` consumer | both fields | YES (read) |

**GREEN — no silent-gap pattern.**

### G.2 — Initialization audit (Section H)

| Field | Init? | Pattern |
|---|---|---|
| `last_exit_predicted_arm[16]` | YES | `-1` per slot |
| `last_exit_predicted_regime[16]` | YES | `-1` per slot |
| `exit_bandits[NUM_REGIMES]` | YES | `memset(0)` in EnsembleModelZoo_Init |
| `initialized_exit_bandits` | YES | `0` until _InitExitBandits |
| `last_predicted_exit_horizon_idx` | YES | `-1` sentinel |

**GREEN — v5.9.1a discipline preserved.**

### G.3 — Persistence audit (Section C/L)

| Surface | Wired? | Notes |
|---|---|---|
| Save | YES (shutdown) | EngineSharded.hpp:3323 |
| Load | YES (boot) | EngineSharded.hpp:1191 |
| Separate file | YES | `<base_dir>/exit_bandit_state.json` |
| Bundle-ID check | YES | CoreModelZoo.hpp:1807-1809 |
| Forward-compat | YES | Missing file → uniform priors |

**GREEN.**

---

## Behavior matrix

| Scenario | Trainer | Engine | Identical? |
|---|---|---|---|
| `exit_bandit_enabled=0` (default) | No reward | Skip | YES |
| Per-slot capture stability | N/A | Stable across cycles | YES |
| Counterfactual formula | uses `original_tp` | uses `original_tp` | YES |
| Regime at attribution | N/A | Locked at submit | YES (intentional) |
| Bundles without exit_bandit_state.json | uniform | uniform | YES |
| Fee rate assumption | cfg-bound | parameter (0.001 default) | CALIBRATE |

---

## Verdict: **GREEN — Ship-ready**

**Rationale:**
1. Struct parity: exit_bandits[] mirrors buy_bandits[] structure
2. Cfg parity: fields wired through ControllerConfig → boot → gate
3. Per-slot capture parity: stable across cycles (write at submit,
   read at fill, clear post-attribution)
4. Reward attribution parity: original_tp (entry-locked), not
   ratchet'd take_profit_price; matches buy-side pattern
5. Persistence parity: separate JSON file forward-compat;
   bundle-ID protection
6. Test coverage: 30 new tests; 2048/0 pass
7. Hot path UNTOUCHED; default cfg zero-overhead

**Punch list for v5.13.5+:**
1. Add capture-site assertion for arm/regime bounds (early failure)
2. Document exit-bandit fee assumption in OPERATOR_DEPLOYMENT.md
3. End-to-end snapshot test (deferred, low priority)
4. Cross-reference v5.13.4 ↔ v5.10.0a.G.7 in CLAUDE.md /
   PARITY_LIFECYCLE.md
