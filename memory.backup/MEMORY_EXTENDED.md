# Memory index — EXTENDED (Tier-2, on-demand)

> Deep-technical / implementation-specific disciplines moved out of always-loaded MEMORY.md 2026-06-11 (TECH_DEBT-163), + the home for NEW Tier-2 (process / pattern / deep-technical) memories. Loaded on-demand by the work-mode skills + when doing that specific work. The always-relevant collaboration / judgment / audit-posture / user / project memories stay in MEMORY.md. Memory-index guards accept MEMORY.md OR here as indexed.

## Deep-technical / implementation-specific (Tier-2)

- [Comments are point-in-time — verify the code not the comment](feedback_comments_point_in_time_verify_against_code.md) — stale codegen/perf/size comments aren't truth; verify the compiled code (disasm/tool), flag+suggest the fix on mismatch (the CMOV-vs-je miss); armed into SUBAGENT_ARMING §2.5
- [Pessimistic-by-default sim is the trustworthy signal](project_pessimistic_sim_is_trustworthy.md) — make the backtest FAIL (overestimate costs on every axis: cost/fill/timing/liquidity), never flatter it; conservative non-zero defaults (pessimistic-by-default, never optimistic-by-omission); survives-failure > highest-average-return (D-203; A9 first canonical)
- [Avoid substring replace_all on member-access patterns](feedback_avoid_substring_replace_all_on_member_access.md) — `config.X` mangles `ctrl->config.X`; inventory variations OR per-prefix targeted edits
- [Prefer action-parameterized walker over per-consumer bodies](feedback_prefer_action_parameterized_walker_over_per_consumer_walker_bodies.md) — `FOREACH_<COHORT>_COHORT(BASE_X)` meta-walker prevents drift across consumers
- [Wire-context vs cfg-file parser separation](feedback_wire_context_vs_cfg_file_parser_separation.md) — dual-context parser MUST take a `bool wire_context` param OR split the functions
- [Train-serve execution-layer parity META gap (M5)](feedback_train_serve_execution_layer_meta_gap.md) — pre-coding gate includes a train-serve EXECUTION-LAYER walk (boot + slow-path-cycle) for any HIGH-RISK EngineSharded ship
- [Enumerate helper signature args before extract (M6)](feedback_enumerate_helper_signature_args_before_extract.md) — body-content enumeration CSV before plan-body lock
- [Multi-surface deletion ordering (B14)](feedback_multi_surface_deletion_ordering_discipline.md) — deletion across ≥3 files w/ compile interdeps: enumerate + classify per kind + sequence leaves-first (cfg-row last)
- [Unconditionalization latent-assumption audit (B15)](feedback_unconditionalization_latent_assumption_audit.md) — removing a cfg-gate via "always-true" → enumerate latent per-arch/per-mode assumptions; verify none load-bearing BEFORE unconditionalizing
- [Operator-facing doc cohort at cfg deletion](feedback_operator_facing_doc_cohort_at_cfg_deletion.md) — cfg/feature deletion → sweep README + QUICKSTART + cfg.example + sister operator-docs in the deletion cohort
- [Archived changelog preservation](feedback_archived_changelog_preservation_discipline.md) — DO NOT modify archived changelog files/rows even when removing the feature; only the CURRENT CHANGELOG gets a new row at ship close
- [Categorize by consumer-pattern not field-name](feedback_categorize_by_consumer_pattern_not_field_name.md) — field-name taxonomy is NOT a substitute for consumer-pattern analysis (walker behavior + override membership + actual reads)
- [Cfg field categorization at registry-add time](feedback_cfg_field_categorization_at_registry_add_time.md) — 4-category decision tree; re-categorization is a 5-step atomic migration (partial = orphan state)
- [C++17 inline variable for header-only shared state](feedback_cpp17_inline_variable_for_shared_state_across_tus.md) — header-only globals → C++17 `inline` (never `static` = per-TU copies; never extern+cpp)
- [Machine-portable resolver for committed tool paths](feedback_machine_portable_resolver_for_committed_tool_paths.md) — committed tool with a per-machine path → env-override + derived-default + `.exists()`-guard, NOT a hardcode
- [Forward-decl at global scope not inside namespace tt](feedback_forward_decl_at_global_scope_not_namespace.md) — `namespace tt { class X; }` shadows ::X; forward-decls at GLOBAL scope OR `#include` (B17)
- [Enumerate block-scope statics before hoist](feedback_enumerate_block_scope_statics_before_hoist.md) — hoist a lambda into a named fn → enumerate ALL enclosing block-scope statics; pass each as explicit arg (Class 35/B18)
- [Capture + check are model-bounded](feedback_capture_and_check_are_model_bounded.md) — self-check + document-as-you-go run WITHIN your model of a change's footprint → systematically miss OUT-OF-MODEL sites (index/pointer/sibling/elsewhere-ledger); only EXTERNAL enumeration closes it (independent reviewer / structural full-set guard / a mutating subagent's change-manifest); recurrence of a miss-class = build the guard, not more manual rechecks; the deepest M7 why (the always-loaded adversarial-default + passing-test triggers point here)
- [Synthesis needs its own adversarial pass](feedback_synthesis_needs_its_own_adversarial_pass.md) — a synthesis's own RECOMMENDATIONS need their OWN independent A-pass; verifying the input FINDINGS ≠ verifying the conclusions (the 85-agent synth had 3 refuted recs; the converged `sg_*`-reuse fix was BROKEN, a-class-caught); fire I→A / 3:3:3 against the RECS, not just the findings; sisters [[feedback_adversarial_framing_default_for_checks]] + [[feedback_capture_and_check_are_model_bounded]]
- [Arm sub-agents PLAN-aware + FUTURE-aware + SPEC-CITED](feedback_arm_subagents_plan_and_future_aware.md) — beyond M8 nav-infra: load what predecessors LANDED + the decisions to HONOR (currency sweep) + where the surface is GOING + the governing DESIGN_SPEC for the piece; the tombstone re-litigation is what this prevents; body = plan-decomposition-and-future-aware-agent-arming.md
- [Define-done + arm scout-first subagents (M8)](feedback_define_done_and_arm_scout_subagents.md) — a close TERMINATES only when an enumerated Definition-of-Done (code+producer / test-per-fix / sanitizers / trackers-flipped / parity / promises-honored / obligations-homed / docs-indexed / meta-codified) is verified by a FULL-CONTEXT scout-first subagent (load refs+toolchain+nav-infra+domain-skill → scout → execute; adversarial-independence on the VERDICT only); trickle-discovery IS the never-terminating loop; the M7 escalation of AR-8; sisters [[feedback_adversarial_framing_default_for_checks]] + [[feedback_capture_and_check_are_model_bounded]]
- [Agent-class fan-out vocab — I/A/V/D/C](feedback_a_class_i_class_fanout_vocab.md) — I=INVESTIGATIVE (map surface/write-set/blast-radius), A=ADVERSARIAL (FIND/REFUTE, default-refuted), V=post-impl VERIFY (DoD), D=DECOMPOSITION (cut-lines), C=CURRENCY (re-ground + decision-sweep); lifecycle C→D→I→A→[code]→V; label agents I-1/A-2/V-1/D-1/C-1; /decision-check + /precoding-audit-gate are the skills that ARE this pattern; + the two meanings of "cascade" (staged I→A vs the /dependency-chain-trace blast-radius audit)
- [Re-cascade I+A on the corrected shape when supplied-shape was wrong](feedback_recascade_audit_on_corrected_shape.md) — a fan-out finds the supplied seams/shape (handoff/register/spec) materially wrong → re-run I→A on the CORRECTED shape before building; don't build on the discovery pass's invalidated frame (F-059 .E.0.10 canonical)
- [Run dedicated audit skills, not just armed prompts](feedback_run_dedicated_audit_skills_not_just_armed_prompts.md) — design swarms on DOD/hot-path/cache/concurrency surfaces RUN /dod-audit + /hft-audit (the skill's catalog walk), never approximate with a general agent armed only with the H-invariants (the skill is a checklist, the prompt a hint); use /precoding-audit-gate which composes them; operator-caught at A6 2026-06-15 — the dedicated passes surfaced the FPN_Binary cfg type / bit-5 / single-writer-ordering the armed prompts missed
- [V-class: post-implementation verification pass](feedback_v_class_post_implementation_verification.md) — after implementing, BEFORE commit, run a single V-class (the post-coding complement to I/A): build + char-tests + SANITIZERS (asan/ubsan, per-implementation not ship-close-only) + calls_graph_diff + doc-CI + the domain audits on the SHIPPED code → a M8 DoD verdict; codify as a skill + add V to the I/A vocab; caught a pre-existing UAF (TD-202) at A6 that ship-close-only asan would have deferred
- [Tombstone the name, reclaim the non-persisted bit](feedback_tombstone_the_name_reclaim_the_nonpersisted_bit.md) — an identifier's identity is its persisted/wire NAME not its runtime storage slot; a non-persisted bitmap bit reuses freely (retire the row IN PLACE → no renumber, reclaim the slot for the next flag); never freeze it or widen to dodge retirement (DOD minimum-footprint); stamp/snapshot-persisted bits still freeze (gate-flag cohort `.E.0.10`)
- [Deletable-by-construction; tombstone only the wire/persist surface](feedback_prefer_deletable_cascade_over_tombstone.md) — design internal identifiers (registry rows / masks / non-persisted fields) so deleting a "part" auto-cascades through every consumer (registry/compiler-driven, zero hand-cleanup); reserve H21 tombstoning for the IRREDUCIBLE wire/persist surface + MINIMIZE that surface (ask whether an id needs to cross the line at all); COMPOSES with H21, doesn't override it; first canonical = item-4's global-flat leg riding `FOREACH_PER_NODE_ARRAY_OVERRIDE` → auto-dissolves when E.1.2 deletes the arrays (D-278); operator-articulated 2026-06-28; sister [[feedback_tombstone_the_name_reclaim_the_nonpersisted_bit]]
- [Mechanically verify derived code-facts](feedback_mechanically_verify_derived_code_facts.md) — never hand-comment a size/cache/complexity/latency fact; `static_assert`/tool/benchmark it (hand-math drifts + carries invisible scope/version assumptions: `rolling_baseline` "~1.5MB" was 64KB = ×16-cores-at-24B per-engine-on-a-per-core-field); the latency budgets ("~100-300µs ROLLING") are the SAME unverified class — MEASURE before locking decisions/ROI; INVERSE = a guard that false-REDs cache-disciplined code (`ExecutionCore` over-flag); guard = `check_struct_size_budget.py` (M7); spec `mechanical-verification-of-derived-code-facts.md`; sisters [[feedback_capture_and_check_are_model_bounded]] + [[feedback_ground_design_in_real_code]]
- [Critical-moment determinism over low average latency](feedback_critical_moment_determinism_not_average_latency.md) — the latency cost is JITTER-correlated-with-the-action-that-matters (order placement / signal commitment), NOT low average; a spike on the trade tick is the worst shape (the trade-event push branch `ExecutionCore.hpp:483` GUARANTEES a mispredict+flush exactly when committing capital — "rare+well-predicted=low" is the average-case trap); the deep WHY of H20 (branchless even when nominally slower) + reframes H8 (predictable AT the critical moment); branchless trade ACCEPTED (dummy-write/quiet-tick); average-vs-jitter is CLOSED doctrine; spec `critical-moment-determinism-over-average-latency.md`
- [Invoke composable skills, don't hand-execute their steps](feedback_invoke_composable_skills_dont_hand_execute.md) — an orchestrator's "invoke /X via the Skill tool" means INVOKE it, not approximate its body "to be efficient"; hand-executing silently SKIPS the sub-skill's stages (this session: hand-wrote the handoff via Write → skipped /handoff Stage 1.5 TaskList-capture + close-session Stage 5.5 git-diff-checklist → dropped tasks + collapsed capture, operator-caught); mechanical backstops added (handoff task-section guard + `rebuild_doc_indexes --check`) but the DISCIPLINE is the fix; sister [[feedback_run_dedicated_audit_skills_not_just_armed_prompts]]
- [Scaffold outlines; depth + scope-growth at the dive](feedback_scaffold_outline_depth_at_dive.md) — scaffold subplans are deliberately thin guides; the re-ground/gate EXPANDS scope at the dive (proven-best workflow) — don't flag that growth as a planning miss
- [Rename enumerator is a World-2 aid; the compiler is World-1](feedback_rename_enumerator_is_world2_aid_compiler_is_world1.md) — a rename-enumeration tool de-risks the compiler-BLIND surface (apparatus + build-bound doc-data-files); the COMPILER is code-consumer-completeness (red→green loop catches the enumerator's narrow spots: decl/arrow/bare-field, `core_`+regex-metachar, `tests/` scope); doc-data-files co-migrate WITH the code; sister [[feedback_capture_and_check_are_model_bounded]]
- [Surface FOUNDATIONAL decisions, not just tactical ones](feedback_surface_foundational_decisions_not_just_tactical.md) — when presenting a decision set for sign-off, classify TACTICAL (clear default → rec + low ceremony) vs FOUNDATIONAL/compounding (shapes the trajectory, downstream ships extend it, hard to reverse → surface EXPLICITLY for judgment + how-it-compounds); NEVER fold a compounding decision into "obviously correct" + present only the local calls (operator caught it: "are these the most structural and compounding fixes for the lifecycle of E?"); sisters [[feedback_audit_own_proposals_with_same_rigor]] + [[feedback_operator_pushback_as_audit_signal]]
- [No margin / no leverage (project capital constraint)](project_no_margin_trading.md) — loss-side ≤100% (can't lose >amount-bought / the node's risk allocation; `>100%`=margin=ERROR); TP/gains generous ≤~1000% (a >100% TP target is rare-but-legit; gains aren't margin-bounded); Σ node allocations ≤100% (cross-node → E.5 / TECH_DEBT-211); ASYMMETRIC by construction (a uniform ≤100% is a BUG); reuse `tt::barrier_is_corrupt` (SSoT); first enforced by ③ (D-248); overflow-homed here per the always-loaded budget cap; sisters [[feedback_heavier_default_audit_posture_for_capital]] + [[feedback_single_source_the_computation_not_just_the_mode]]
- [Remote push needs operator confirmation](project_remote_push_needs_operator_password.md) — COMMITS are free (sign off the warm GPG cache, no interaction); a non-interactive PUSH from the tool HANGS (no fillable pinentry → a 2-min timeout + `ssh-add -l` "no agent") → RUN the push so the prompt reaches her terminal + she enters the password, or ask her to push; she stated it directly (*"you can commit … but for pushing to remote, you have to get my input"*); overflow-homed here per the always-loaded budget cap; sister [[feedback_micro_commits_compile_gated]]
