---
type: handoff
status: active   # THE one live handoff (supersede-on-write; ≤1-active enforced by check_handoff_active_singleton.py)
ship_tag: v5.15.5.F.4d.1.E.1.2
plan_type: coding
supersedes: 2026-08-17-E.1.2-D426-stamp-put-guard-blocked-on-cpp-standard-handoff.md
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-08-17-E.1.2-D426-stamp-put-guard-blocked-on-cpp-standard-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
audit_tier: HIGH
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: freeze the capital-state WIRE on the registry-driven NodeContext persist serializer; every E.1.3+ spine reads this layout
sister_specs: [meta-disciplines/dead-code-and-identifier-retirement-discipline.md, framework-patterns/type-trait-dispatch-via-tt-namespace.md, meta-disciplines/meta-anti-pattern-index.md, meta-disciplines/advertised-capability-never-exercised.md]
coding_status: "D-426 CLOSED end-to-end. The bit-without-value class is now structurally unspellable: STAMP_PUT landed, every emit site converted, the STAMP_SET guard ARMED (zero refusals = the completeness proof). The zero-emit row DELETED + blessed (the ledger shrank by exactly that one key; re-derive the total from the guard) along with BOTH consumers found while scoping. The ledger-semantics fork resolved as NO CHANGE — refuted by its own /decision-check, which instead found the H21 name-burn INERT for 3 of its 4 names (fixed + teethed). D-427 NEW: viewer is ImGui-decoupled, fox-tui dropped. NEXT = queue item 1, the .B.3 parse→handle leg (PARITY-042, capital-adjacent)."
---

# E.1.2 — the class is closed, the guard is armed, and the sweep that kept missing siblings has a name

## State at write — ANCHORS, not counts

| Repo | HEAD | Window this session | Remote |
|---|---|---|---|
| Engine `feat/v5.15-live-readiness` | `76e4b8e` | `cddd8f6..76e4b8e` | pushed |
| Workspace `main` | at-or-after `84fa4d2` | `64e43b4..HEAD` | pushed at sync |

Untracked engine scratch (`notes_update.md` `resume` `tatoo.md` `tmpb924ezcj.cpp`) — operator files, keep.

⚠️ **`scripts/download_data.sh` + `scripts/sync_archives.sh` carry UNCOMMITTED operator edits** (cwd-anchoring fixes, in the operator's voice, appeared mid-session). NOT part of this ship and deliberately left alone. They look like real fixes worth committing.

## Re-derive at pickup (every number below comes from these, none from prose)

```bash
./tools/check_session_docs.sh                      # expect SWEEP CLEAN
./build.sh test && ./build/controller_test         # expect BUILD 0 + suite green
python3 tools/check_identifier_retirement.py       # expect GREEN
bash tools/check_identifier_retirement_selftest.sh # expect PASS — incl. the 4 NEW burn legs
./tools/check_determinism.sh                       # expect GREEN (wire changed this session)
git -C . log cddd8f6..HEAD --oneline
git -C ../tick-trader-percore-workspace log 64e43b4..HEAD --oneline
```

⚠️ **The suite count MOVED this session, once, by design.** The only intended movement is the deletion removing its own assertions; every other step left it flat. Re-derive the current value — do not carry a literal forward.

---

## What landed

**① The tool-hardening leaf (ws `a0beae3`).** The H21 name-burn was **inert for 3 of its 4 names** — `retired_name_check()` matched `#define` only, while burned names come back as `#define`, `X(…)` registry rows, AND bare enum members. Measured with positive controls: the fee-rate keys resurrected as `ADD (ok)` (the precise outcome the module's own comment claimed was prevented) and `STAMP_BIT_fees` resurrected producing **no output at all**. Now whole-word over comment-stripped code — **shape-enumeration was the bug**, so the fix does not enumerate shapes. Teeth both directions: 3 positive controls (one per shape) + a negative control proving tombstone COMMENTS stay silent. Also: the pre-commit Check-H display was `tail -12` and threw away the CAUSE (head-anchored now), and the violation remedies were enum-shaped on every category — telling the operator *not to drop a row H21 requires them to drop* (now category-aware via `NAME_KEYED`).

**② The zero-emit DELETED + blessed (engine `4dcbbb8`).** Row, bit, MASK, STANDALONE entry, production bit-set, and **both consumers**. The bless review was mechanized before it ran — 1 REMOVED / 0 ADDED, all survivors in a single uniform −1 bucket, relative order preserved, other categories byte-unchanged — so the operator checked a PROPERTY, not 45 lines.

**③ Conversion + guard ARMED (engine `1a38687`, `76e4b8e`; ws `84fa4d2`).** Production emit path then tests converted to `STAMP_PUT`; the guard armed LAST, producing **zero refusals** — which is the point: it doubles as a TOTAL oracle for "did we convert them all", a question compiles-and-green cannot answer. Group bits correctly untouched throughout.

**④ D-427 — the viewer decision** (see below).

## 🔴 THE NEXT ACTION — queue item 1, the `.B.3` parse→handle leg

**The capital-adjacent one, and the last big open item from the D-426 queue.** ~30 cfg-derived `ModelHandle` fields are declared and read but written by NOTHING — absence established against a NAMED search space:
`rg "handle->thompson_precision_prior|handle->thompson_precision_obs|handle->bandit_blend_ratio|handle->ml_tp_pct"` over `ML_Headers/ CoreFrameworks/ Backtest/ GUI/ MemHeaders/` returns EMPTY (re-verified at this session's pickup). Two `REFUSE_STRICT` rows (`ML_Headers/CfgDriftCheckRegistry.hpp` — `thompson_precision_prior`, `thompson_precision_obs`) therefore compare a permanent `0` against a non-zero cfg default, behind a gate that reads **cfg only, not `STAMP_HAS`** — so unlike the vacuous rows these are reachable.

**Severity is NOT "the engine won't boot"** — a compiled probe corrected that. At the shipped default it returns 0. The real shape: **OFF**, every load emits guaranteed-false drift lines that saturate a capital safety channel so real drift is indistinguishable from noise; **ON** (`held_out_gate_strict=1`, the live-readiness posture), every model refuses, so the rational operator response is to disable the gate. **A safety control that pressures you into switching it off is the Knight shape.** PARITY-042. Probe re-runnable at `~/.cache/foxml_probe/`.

⚠️ **Apply AR-19 to this one** (the new meta-row this session produced): scope the sweep by TRACING the field to every reader, not by the blast radius of the last fix. That mistake is exactly what hid two consumers this session.

## Also queued (carried forward)

- **Gate granularity (step 6 proper)** — LAST, and every agent agreed. Four designs refuted; do not lead with it.
- **Step 7** — `FORMAT:` token, then `GUI/TradeReader.hpp` (its legacy-schema read is separately known-broken, D-421 ⑤).
- **Domain classification** of the baselined registries — blocked on the `STRUCT:` subset notion (TECH_DEBT-274).
- **`MBS_OrderSetBanditContext`** — getter read in production, setter never called.
- **Backtest-side Thompson save** + the **empirical** bandit question (loop CLOSING is proven; PROFITABLE is not).
- **E.1.2 close-gate** — `NPF_PROJECT_POISON` 4th projection + FEATURE_LOOKUP + postmortem + `/ship`.
- **From the /decision-check, homed not done:** the PRE_CFG↔POST_CFG **seam is order-blind under BOTH designs** (verified fix in hand: split `stamp-key` into two `SOURCES` rows; needs one bless) · **36 of ~82 signed-body keys unenrolled** (the `STAMP_BOUND_CFG_DERIVED` cohort) · wire-format invariant **I5 is a literal tautology** while its spec advertises it as catching walker-order regression.
- **Operator-raised:** a general **unused-function / dependency-chain checker** — the shape that would have caught all four sites of this session's pattern. Already half-designed and scattered: Class 44's `closure_mechanism` names exactly this detector and records that NO existing tool catches it; TECH_DEBT-087 is the same idea scoped to one registry; `calls_graph_diff.sh` is orphan-detection scoped to strategy/regime; the D-422 register is its manual form. **The gap is consolidation, not conception.**
- **TECH_DEBT-286** (`_GROUPS` ↔ enum drift) · **-284** (H16 unenforceable) · **-283** · **-274** · **-094** (now PARTIALLY subsumed — the `bandit_blend_ratio` slice closed by deletion; STILL OPEN there: `ml_tp_pct`, `ml_sl_pct`, `barrier_blend_mode`, and `per_horizon_barrier_blend` via sister TECH_DEBT-100).

## ⚠️ NEEDS THE OPERATOR TTY

`tools/goldens/citable-ids.txt` is stale for **`D-426`, `D-427`, `TECH_DEBT-285`, `TECH_DEBT-286`, `PARITY-042`** (verified by grep at close; `D-425` is present). Nothing reds — the capture-audit leg is an index FLOOR, so it catches shrinkage, not lag. Re-bless at pickup. `bless.py` needs a REAL terminal; the `!` prefix makes `_isatty()` false.

## D-427 — the viewer is Dear ImGui, decoupled. `fox-tui` is DROPPED.

Operator decision this session, superseding **D-7 + D-26** on the renderer question (both were `STATUS: landed` as DECISIONS but never EXECUTED — `engine_gui` still builds, `.E.2` never happened — so this retired a plan, not a migration). Their STATUS lines now point FORWARD at D-427.

**Only "who renders" changed.** Headless engine · read-only viewer + command-sender · `fox-cli` · multi-viewer · mmap state exposure · lifecycle independence all stand. No landed decoupling work is invalidated.

⚠️ **`EngineTUI.hpp` is TWO things — do not delete it wholesale.** The RENDER half dies; **`TUISnapshot`/`PerNodeSnap`, the seqlock engine→display DATA CONTRACT, SURVIVES and is promoted** — the roadmap already names it as the one site that reads money safely AND as the protocol that BECOMES the mmap'd region. The contract IS the monitoring plane.

Homed, not done: the `TUI`-prefix rename (needs its own leaf — `PerNodeSnap` is pinned in `tools/lib/cache_layout_baseline.txt` + `alignas(64)` discipline ⇒ cache-layout re-bless) · the `.E.2` plan body, EV-3, and the canonical binary-name list still carry the `fox-tui` premise. Memory: `project_viewer_is_imgui_decoupled_not_tui`.

## Arming for the next action (M8)

- **Audit posture:** `audit_tier: HIGH`. Queue item 1 is a capital safety-control surface.
- **Skills:** `/ml-audit` (the drift-gate layer is ML-inference material) · `/parity-check` (train↔serve identity — PARITY-042 is literally a parity finding) · `/decision-check` if the fix has a genuine fork · `/bug-check` before commit.
- **Tools to RUN:** `check_session_docs.sh` · `check_identifier_retirement.py` + its selftest · `check_determinism.sh` (any wire touch) · `check_cache_layout.py --strict-new` · `calls_graph_diff.sh`.
- **Anti-patterns in play:** **AR-19** (NEW — sweep scoped by the predecessor's blast radius; apply it deliberately here) · **AR-1** (bumped ×2 — categorical-over-unenumerated AND truncate-and-conclude) · Class 58 (A′ sibling-asymmetry · B gate-reachability · C consumer-side) · Class 51 · Class 44 · Class 2 (display asserting what execution doesn't do).
- **Invariants:** H21 (+ Rule 1a) · H9 · H4 · H22.
- **WILL BITE:**
  - **Never `| head -N` a set you are about to conclude over.** That is how a PRODUCTION consumer was missed this session. Cap only for a readability peek; re-run uncapped before any claim about membership.
  - `rg <pat> .` from the engine root is **BLIND** to `tests/`/`tools/`/`plans/`. Name roots explicitly.
  - This shell is **zsh**; **never read `$?` after a pipeline** — a pre-commit hook BLOCKS it. Capture rc directly.
  - `/tmp` is **noexec** — build probes under `~/.cache/`.
  - **B-Plus compiles every ` ```cpp ` fence.** A verbatim excerpt in an evidence doc is not a compile claim — tag it `text` (TECH_DEBT-285). This blocked a commit this session.
  - **This surface's comments are measurably unreliable** — an a-class measured seven false checkable claims here, and this session found three more (two in the guard's own file, one in the STANDALONE registry). Verify before relying.

---

## Capture-completeness — the JUDGMENT ledger (Stage 6.5.2)

- **Check 2 — plan-body currency.** DONE. D-426 carries four amendment blocks; D-427 minted with a paired STATUS sentinel; D-7 + D-26 STATUS lines amended to point FORWARD rather than being silently retired.
- **Check 3 — decision-log artifact.** PRESENT. One new entry (D-427), two amendments to D-426, two supersession amendments.
- **Check 5 — handoff currency.** THIS doc supersedes the 2026-08-17 blocked-on-C++17 one; reciprocal frontmatter set; singleton guard in the re-derive block.
- **Check 6 — M7 escalation candidates.** **ONE recorded, NOT acted on:** agent reports were not saved at receipt — the operator had to ask *"did you make sure to save the report?"* — despite `feedback_save_agent_reports_verbatim` existing and being explicit that the orchestrator writes them at receipt. One instance this session; if it recurs, that memory has proven insufficient and wants structural enforcement (a `/decision-check` post-step that writes the reports automatically).
- **Check 7 — DESIGN_SPECS promotion.** `type-trait-dispatch-via-tt-namespace.md` gained a landed application + a scope clarification on its own boost::hana rejection. `dead-code-and-identifier-retirement-discipline.md` gained the wire-key remedy inversion + the mechanization corollary. No Stage promotions.
- **Check 9 — memory ↔ spec sister completeness.** ONE new memory (`project_viewer_is_imgui_decoupled_not_tui`), indexed in `MEMORY.md` under Project state. It is a project-state fact, not a pattern, so it pairs with the roadmap + D-427 rather than a DESIGN_SPEC — deliberate.
- **Check 10 — going-forward-rules currency.** No new Tier-0 rule. The session's disciplines were applications of existing ones; the two NEW shapes went to the meta-anti-pattern index (AR-19 new, AR-1 bumped) which is the correct home for audit-reasoning shapes.
- **Check 12 — amendment cascade.** Sweep CLEAN at close. D-7/D-26 → D-427 propagation was done explicitly (log + roadmap + memory), which is the cascade this check exists to catch.

**Meta-harvest (Stage 4.5):** **AR-19 NEW** — *a remediation sweep scoped by what the PREVIOUS fix touched, instead of by tracing the SUBJECT to every reader.* Four sites of one pattern across three sweeps, each of which removed the `fees` instance and left the `bandit` sibling three lines away, adjacent to a comment describing its own twin's removal. Sister to Class 58 one layer up — the *nobody asks for the complement* question, at the SWEEP rather than the registry. **AR-1 bumped ×2**: the refuted "only mid-insert differs" quantifier, and a NEW sharpening — *truncate-and-conclude* (`| head -20` at the invocation, then concluding over the capped set; distinct from the existing summarize-and-drop row because the dropped members were never seen, so the plan-enumeration guard structurally cannot catch it).

**Tech debt worked, not stacked:** TECH_DEBT-094 marked PARTIALLY SUBSUMED with the surviving 4-of-5 named. No new TECH_DEBT opened — the session's finds were either fixed now or homed in the decision log / meta-index.

## Independent review (Stage 6.5.4)

<!-- REVIEW-VERDICT -->
**PENDING — fill from the a-class adversarial pass before this handoff is considered closed.**

## TaskList (recreate at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | pending | **The `.B.3` parse→handle leg** — the capital one (PARITY-042). Apply **AR-19**: trace the field to every reader, don't scope by the last fix. |
| #2 | pending (operator TTY) | **Re-bless `tools/goldens/citable-ids.txt`** — stale for D-426, D-427, TECH_DEBT-285, -286, PARITY-042. |
| #3 | pending | **`stamp-key` seam split** — PRE_CFG↔POST_CFG order-blindness; verified fix in hand (two `SOURCES` rows), needs one bless. |
| #4 | pending | **The unused-function / dependency-chain checker** (operator-raised) — consolidate Class 44's `closure_mechanism` + TECH_DEBT-087 + `calls_graph_diff.sh` rather than build new. |
| #5 | pending | Gate granularity (step 6 proper) — **LAST**. |
| #6 | pending | Step 7 (`FORMAT:` → `TradeReader`) · domain classification · `MBS_OrderSetBanditContext` · backtest Thompson save · the empirical bandit backtest. |
| #7 | pending | **E.1.2 close-gate** — `NPF_PROJECT_POISON` 4th projection + FEATURE_LOOKUP + postmortem + `/ship`. |
| #8 | pending (operator) | Commit the `scripts/*.sh` cwd-anchoring fixes if wanted — uncommitted, not mine, left alone. |
