---
type: handoff
status: superseded   # superseded 2026-07-15 by 2026-07-15-E.1.2.A-toolchain-done-conversion-sprint-open-handoff.md (phases 2-4 executed + P6 opened that session). Was: supersedes 2026-07-06.
ship_tag: E.1.2.A
plan_type: refactor   # in-code documentation infrastructure (tag system) — a maintainability/framework deliverable, not a capital-path change
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: a locked, plugin-ready in-code documentation tag-format ([SCHEMA]_[v1.0]) + the toolchain/plugin/corpus that makes the codebase navigable ("code becomes its own IDE")
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-07-06-tag-system-full-design-locked-schema-v2-core-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md, DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md, DESIGN_SPECS/doc-disciplines/format-input-space-taxonomy.md]
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md, DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md, plans/v5.15-live-readiness/MASTER.md, plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md]
deletion_scope: none
coding_status: phase-1-design-complete   # FORMAT is LOCKED; phases 2-6 (implementation) are the pickup
---

# E.1.2.A handoff — in-code documentation tag system: **[SCHEMA]_[v1.0] LOCKED, phase 1 DONE**

**Created:** 2026-07-14
**Target ship:** E.1.2.A — in-code documentation system (the "tag system": structured `[CATEGORY]_[value]` comment tags that make the codebase self-navigable; a nvim plugin `fox-symdeps` reads them so "code becomes its own IDE")
**Branch:** feat/v5.15-live-readiness

**State at handoff write time:**
- Engine repo HEAD: `d4812de` — `wip(.E.1.2.A): pilot conversion — RegimeDetector.hpp` (Version.hpp = `5.15.5.F.4d.1.E.1.1`)
- Workspace repo HEAD: `0c9d2a5` — `docs(E.1.2.A): LOCK [SCHEMA]_[v1.0]` (the 9 schema commits `b64968e`→`0c9d2a5` are **local only — NOT pushed**, per Caramel's standing constraint; she enters the push password herself)
- Engine working tree: `M Strategies/RegimeDetector.hpp` (Caramel's uncommitted pilot WIP — **do not touch**) + untracked scratch dirs
- Workspace working tree at write: 2 close-out edits (README index rebuild + schema footer DRAFT→LOCKED) — committed locally at close (see Capture-completeness)
- Independent deliverable review (Stage 5.5): **COMPLETE** — every deliverable verified on disk, nothing fabricated/stubbed/missing.

---

## TaskList state at handoff write (recreate verbatim at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | in_progress | E.1.2.A umbrella — in-code documentation system (phase 1 done; phases 2-6 remain) |
| #2 | completed | Complete schema against the taxonomy (phase 1 — format design → **LOCKED v1.0**) |
| #3 | pending | Toolchain + Check-P fix (RC-A..E, compile-DB, branchtag, keybind, cache-align) — phase 4 (C++ core + tooling, D-337) |

**Recreate at pickup** via TaskCreate: #1 in_progress, #2 completed, #3 pending.

---

## What phase 1 delivered (this session) — the FORMAT is frozen

The 6-phase build is: **(1) design the format ✅ DONE → (2) propagate to specs/templates → (3) dogfood corpus → (4) toolchain + C++ core (D-337) → (5) fox-symdeps plugin rework (operator-owned) → (6) incremental codebase conversion.** Phase 1 is closed:

- **`[SCHEMA]_[v1.0]` LOCKED (D-346).** The frozen contract = the ONE parse rule (innermost-bracket `\[([^\[\]]+)\]`, token[0]=category) + the ladder + the node model + the member model + the 76-token category set + the families. Reversible by version bump (nothing is converted yet). It's v1.0 (not v2) because there was never a v1 in the wild — the old "schema-v2" naming in the predecessor handoff predates this relock.
- **Decisions D-339 → D-346** (all in the decision log, each with a `<!-- STATUS -->` sentinel; independent-review-verified 1:1 against the 9 commits):
  - **D-339** — node model: ONE universal skeleton per `[TYPE]`; per-type variance is a declarative kind→`[DERIVED]`-axes table, never a parser branch (no per-type adapters — anti-Class-18).
  - **D-340** — member model: 3 tiers — (1) grouping `[SECTION]`; (2) fact-scoping per-type vocab `[REGION]`(struct)/`[ROW]`+`[COLUMN]`(registry)/`[VALUE]`(enum)/`[WIRE_FIELD]`(wire-parser); (3) nested sub-unit via `[CONTAINS]`. Same `[KIND]_[id]_[why]` grammar every tier.
  - **D-341..D-343** — adopt-pass of D-309's 7 grammar-gap dispositions into the schema doc.
  - **D-344** — plugin-alignment check (walked north-star §6/§7.5 targets) → caught + folded the missed D-309 #3 (registry topology `[PARENT]`/`[CHILDREN]`/`[SIDECARS]`/`[OVERRIDES]`). Validated the instinct to run the plugin check before locking.
  - **D-345** — wire/persist grounding: `[WIRE_FIELD]` grammar (JSON key-addressed / FIX-CSV ordinal) via a BinanceUserData dry-run; confirmed all 14 taxonomy shapes covered.
  - **D-346** — the v1.0 LOCK.
- **All 10 per-type worked examples** in the schema doc (function `Regime_Classify`, struct `ExecutionCore`, nested-struct+assert `CfgFieldDescriptor`, registry `FOREACH_GLOBAL_CFG_FIELD`, enum `OrderState`, type `Money`, file `CfgFieldRegistry.hpp`, macro `BITMAP_IS_SET`, test `test_config_parser`, wire-parser `ud_parse_execution_report`) — each dogfooded against REAL engine code, so V1.0 is the lock-in template to roll out to the whole codebase.

**SSoT deliverable (read this first at pickup):** `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md` — `stage: 3-first-canonical`, `version: 1.0`, `locked: 2026-07-14`. The plugin end-goal spec it must satisfy: `DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md` §6/§7.5.

---

## Accept-known open item (homed here, not lost)

**`Strategies/RegimeDetector.hpp` — Caramel's uncommitted pilot-conversion WIP.** The doc-CI tag-blocks full-tree sweep flags 2 violations:
- `:543` — `// [STATE]` (bare category not in the locked 76-token set)
- `:627` — `// [INIT]` (same)

These are HER in-progress pilot tags, not a defect in the locked schema. **Reconcile when resuming the pilot:** either (a) add `[STATE]`/`[INIT]` to the category-set via a schema version bump (v1.0→v1.1 — the set is APPEND-ONLY per H21-style discipline), or (b) rename them to existing valid categories (e.g. `[SECTION]` sub-groups). This is the ONLY non-green item in the doc-floor; everything else is clean. **Do not touch the file** until Caramel resumes it.

---

## Capture-completeness (this session — nothing lost)

- **Decisions → log:** D-339 through D-346 each have a `**D-N —**` entry + `<!-- STATUS -->` sentinel in `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`. Independent review confirmed all 8 present and 1:1 with the 9 commits. **No un-logged decisions.**
- **Findings → ledger:** one finding surfaced — the RegimeDetector `[STATE]`/`[INIT]` sweep flag — **homed in this handoff** as the accept-known pilot item (a WIP reconciliation, not a deferred deliverable, so no TECH_DEBT entry). The independent review's one cosmetic finding (footer "v1.0 DRAFT") was **fixed** this session, not deferred. **No new TECH_DEBT / PARITY this session.**
- **Memories/skills → indexed:** **no NEW memory or skill files** this session (the schema-lock work is captured in the decision log + the schema SSoT, which is where it belongs — not new memory). MEMORY.md was **compacted** (always-loaded budget): 34 specific-work disciplines retiered MEMORY.md → MEMORY_EXTENDED.md (lossless — 145 total links preserved, every file indexed exactly once, verified by the bidirectional guard). MEMORY.md now 16.7KB (was 24.7KB).
- **Verified:** `check_session_docs.sh` — all HARD + ADV checks GREEN **except** the accept-known RegimeDetector WIP (above); `doc-metadata bidirectional+index (memories)` ✅; `always-loaded doc budget` ✅; `DESIGN_SPECS index currency` ✅ (rebuilt); `handoff-active singleton` ✅ (this handoff supersedes the prior). Independent Stage 5.5 review: **COMPLETE**.

---

## Arming for the next action

The immediate next action is **phase 2 — propagate `[SCHEMA]_[v1.0]` into the file/code templates + specs** (then phase 3 dogfood corpus → phase 4 toolchain/C++ core). The surface is **doc/spec apparatus** (light gate-weight per `feedback_process_weight_by_surface_blast_radius` — validate-by-use, not heavy pre-gate). Arm it:

- **Route to:** no heavy domain audits (this is doc/framework work, not capital-path). Use `/doc-create` for new template scaffolds; when phase 4 opens the C++ core (D-337 — the parser/query engine + Python bindings + the CI/plugin/your-use tool suite), THEN route the toolchain surface through `/dod-audit` (registry-fit of the tool suite) + `/registry-fit-audit` (the category-set as a registry). A decision-shaped fork (e.g. C++-core API shape) → `/decision-check`.
- **Run (not just read):** `tools/check_code_tag_blocks.py` (the validator — category-set derived from the schema doc's ```category-set fence; RUN it on every converted block) + `tools/check_session_docs.sh` (the doc/plan sweep) + `tools/rebuild_doc_indexes.py --check` (index currency).
- **Read first:** the schema SSoT `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md` (the frozen contract — every template must conform) + `in-code-doc-system-north-star.md` §6/§7.5 (the plugin end-goal the format+tooling together must serve) + `format-input-space-taxonomy.md` (the 14 shapes the format covers). Then the 3 E.1.2.A subplans in `plans/v5.15-live-readiness/subplans/` (`*comment-system-and-doc-consolidation*`, `*grammar-gap-dispositions*`, `*readout-axes-and-classify-vocab*`).

---

## Paste this prompt into a fresh Claude Code session to start phase 2

```
I'm picking up E.1.2.A (the in-code documentation "tag system") for the v5.15-live-readiness sprint.
Phase 1 (FORMAT design) is DONE — [SCHEMA]_[v1.0] is LOCKED. My job is the IMPLEMENTATION (phases 2-6).

This is a fresh context window; do NOT trust prior-session memory — verify everything against current code.

## Step 0 — orient
- Run `/accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-14-E.1.2.A-schema-v1.0-locked-phase1-done-handoff.md`
  (loads the baseline + this handoff + the schema SSoT + decision log; verifies git state; recreates the TaskList).
- If /accept-handoff is unavailable, manually: read CLAUDE.md + CLAUDE.local.md + MEMORY.md + MEMORY_EXTENDED.md,
  then the SSoT `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md` (the frozen v1.0 contract),
  then `in-code-doc-system-north-star.md` §6/§7.5 (plugin end-goal), then this whole handoff.

## Step 1 — verify the lock is intact
- `grep -n 'locked: 2026-07-14' DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md` (frontmatter says LOCKED v1.0)
- `bash tools/check_session_docs.sh` — expect ALL green EXCEPT the 2 accept-known RegimeDetector.hpp WIP tags
  (:543 [STATE], :627 [INIT] — Caramel's uncommitted pilot; do NOT touch her file).
- Confirm the schema + close-out commits are local + unpushed (`cd tick-trader-percore-workspace && git log --oneline @{u}..HEAD` — 10 at handoff write: the 9 schema `b64968e`→`0c9d2a5` + the close-out).

## Step 2 — the work (phase 2 → onward)
Phase 2: propagate [SCHEMA]_[v1.0] into the file + per-type code templates (so new files/code pull from a conforming
  example) — use the 10 worked examples in the schema doc as the templates. RUN `tools/check_code_tag_blocks.py`
  on each template block to prove it validates.
Phase 3: build the dogfood corpus (a handful of real converted files) — validator-green.
Phase 4: the C++ core (D-337) — parser/query engine + Python bindings + the CI-gate / plugin / your-use tool suite
  (pending TaskList #3: Toolchain + Check-P — RC-A..E, compile-DB, branchtag, keybind, cache-align).
Phase 5: fox-symdeps plugin rework (operator-owned; has a rough scaffold, far from the north-star end-state).
Phase 6: incremental codebase conversion (1 file at a time, validator-gated).

Do NOT auto-proceed on any non-trivial decision — present findings + consult Caramel first (she iterates before coding).

## Operator norms
- Address Caramel as Caramel / she / her. No AskUserQuestion modal boxes — inline options.
- Evaluate options on robustness + latency + design philosophy, NOT time.
- Commit locally at clean boundaries; do NOT push (she enters the push password herself).
- Do NOT touch Strategies/RegimeDetector.hpp — it's her uncommitted pilot WIP.
```

---

## Quick links
- SSoT (the locked format): `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md`
- Plugin end-goal: `DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md` §6/§7.5
- Input-space taxonomy: `DESIGN_SPECS/doc-disciplines/format-input-space-taxonomy.md`
- Decision log (D-339..346): `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`
- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- E.1.2.A subplans: `plans/v5.15-live-readiness/subplans/2026-07-05-E.1.2.A-*.md` (3 files)
- Validator: `tools/check_code_tag_blocks.py` · Doc sweep: `tools/check_session_docs.sh`
