# v5.15 sprint kickoff handoff prompt

**Generated:** 2026-05-11 (end of v5.14 close session)
**Purpose:** Self-contained handoff prompt to drop into a fresh Claude Code session for v5.15 sprint planning. Plan-only this session — DO NOT touch engine code until Caramel greenlights.
**Predecessor session:** v5.14 sprint close + v5.14.post1 patch + boot-log analysis + sprint-shape consult.

---

## Copy-paste prompt for fresh session

(Everything below this line is the prompt body. Paste it into the new session.)

---

# v5.15 sprint kickoff — Live-readiness hardening (C-full shape)

You're picking up cold. v5.14 sprint is sealed at engine tag `v5.14` (commit `c4e45d1`)
+ post-release patch `v5.14.post1` (commit `1752fde`) for a missed-migration bugfix in
`Backtest/BacktestPanels.hpp:train_model_worker_fn`. All 2904 tests pass on the
`feat/v5.14-foxml-port-and-maker` branch.

Today's task: **draft `plans/v5.15-live-readiness/MASTER.md` + per-ship sub-plans
for v5.15 sprint**. DO NOT touch engine code yet. Plan-only this session; consult
Caramel before writing any code per `feedback_consult_on_audit_findings.md` memory
+ CLAUDE.local.md "after pre-coding checks, ALWAYS consult before coding" rule.

---

## Sprint charter

**Name:** v5.15 Live-readiness hardening + ModelHandle structural unification

**Scope (C-full shape; previously consulted):**

| Sub-ship | Charter | Bundled TECH_DEBT closure |
|---|---|---|
| **v5.15.0** | ModelHandle migration to FOREACH_STAMP_BOUND_MODEL_CONST X-macro generation (canonical wire-key field names + bit-packed `has_flags`); the asymmetric struct surviving since v5.14.8.A | **TECH_DEBT-014** |
| **v5.15.1** | Model Health CollapsingHeader in ML Status panel (Option B from prior consult); surfaces drift state during live op (feature_hash, label_hash, scaler, cfg drift, model_age, build_flags, stamp_hmac_not_verified) | **TECH_DEBT-028** (PerCoreSnap bool-as-uint8 → bitmap, opportunistic) |
| **v5.15.2** | Live-readiness boot gate + cfg `mode=live` REFUSE if pre-flight items incomplete + /readiness skill wider-build check addition | **TECH_DEBT-033** (wider-build verification in /readiness skill) |
| **v5.15.3** | Multi-horizon worker stamping (closes v5.10.X TODO surfaced by boot log warning "4/4 handles missing grid_member_count"); wire `stamp_write_for_model` into multi-horizon training path | (closes a TODO; no formal TECH_DEBT entry exists for this) |
| **v5.15.4** | Strict-mode + non-empty-secret defaults when `cfg.mode=live` + single-zoo hot-swap strict-mode unification | **TECH_DEBT-005** |

**Sprint sizing:** ~1000 LOC total (~400 LOC for v5.15.0 ModelHandle migration alone; the rest spread across .1-.4). Estimated 4-5 days.

**Charter rationale:** see `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-11-v5.14-post-release-fixes.md` for boot-log analysis that motivated this sprint + chat-time consult records (this prompt's predecessor session).

---

## Cold-pickup completeness (per CLAUDE.local.md going-forward rule)

**1. Branch state.** Create `feat/v5.15-live-readiness` from engine tag `v5.14` (commit `c4e45d1`) — NOT from `v5.14.post1`, because we want a clean baseline. Verify via:
```bash
cd /home/caramel/code/FoxML_Trader_v2
git checkout -b feat/v5.15-live-readiness v5.14
```

Workspace branch follows: `main` continues (workspace doesn't sprint-branch).

**Rollback anchors:**
- `pre-v5.15` = `v5.14` (sprint baseline)
- `pre-v5.15.0` = engine HEAD pre-ModelHandle-migration

**2. Phase execution order MATCHES dependency order:**
- v5.15.0 FIRST — ModelHandle migration is foundational; v5.15.1 consumes the unified ModelHandle for drift surfacing
- v5.15.1 — Model Health panel depends on .0's verify_model_stamp path producing drift flags
- v5.15.2 — boot gate is independent; can ship parallel after .0
- v5.15.3 — multi-horizon worker stamping is independent
- v5.15.4 — strict-mode defaults consume .2 boot gate's checklist

**3. First concrete move per sub-ship:**
- **v5.15.0** Step 0: `grep -n "uint8_t  has_" ML_Headers/ModelInference.hpp | head -30` — enumerate the current direct-field has_* declarations on ModelHandle (~17 fields per the registry source-of-truth at `ML_Headers/StampBoundModelConstRegistry.hpp:60-280` ish). Save the list to working file. Migrate one field at a time to STAMP_HAS pattern + bit-pack into uint64_t has_flags.
- **v5.15.1** Step 0: read `MemHeaders/FailureModeRegistry.hpp` for existing FOREACH_FAILURE_MODE entries; identify the 4-7 new BIT_FLAG entries to add (FEATURE_HASH_DRIFT / LABEL_HASH_DRIFT / BUILD_FLAGS_DRIFT / SCALER_DRIFT / CFG_BINDING_DRIFT / STAMP_HMAC_NOT_VERIFIED / MODEL_AGE_WARN). Confirm uint16_t headroom or expand to uint32_t.
- **v5.15.2** Step 0: read `CoreFrameworks/EngineSharded.hpp` boot path for current cfg validation steps; identify insert point for live-readiness checklist gate.
- **v5.15.3** Step 0: locate `train_multi_horizon_worker_fn` (or `mh_per_horizon_parallel_worker`) — actual name needs grep. Diff against `train_model_worker_fn` (now-canonical at `Backtest/BacktestPanels.hpp:3206+` post-v5.14.post1) for the stamping pattern to apply.
- **v5.15.4** Step 0: `grep -n "stamp_verification_mode\|held_out_stamp_secret\|cfg.mode" CoreFrameworks/ControllerConfig.hpp` — find current cfg field definitions; design the `mode=live` default-flip logic.

**4. Function / constructor / macro names** (be specific):
- `ModelHandle` struct in `ML_Headers/ModelInference.hpp`
- `StampInferenceCfgInputs` struct (canonical reference; already migrated)
- `ModelStampResult` struct (also already migrated)
- `STAMP_HAS(handle, name)`, `STAMP_SET(handle, name)`, `STAMP_CLR(handle, name)` — macros from `MemHeaders/BitmapMacros.hpp`
- `STAMP_MODEL_CONST_AUTOPOPULATE(target, source)` — autopopulate companion at `ML_Headers/StampBoundModelConstRegistry.hpp:680+`
- `FOREACH_STAMP_BOUND_MODEL_CONST(X)` — registry walk at `ML_Headers/StampBoundModelConstRegistry.hpp:60-280`
- `verify_model_stamp` — load-time verification at `ML_Headers/ModelInference.hpp:2000+ ish`
- `stamp_write_for_model` — emit at `ML_Headers/ModelInference.hpp:1786+`
- `FAILURE_IS_SET(flags, MASK_FAILURE_*)`, `FAILURE_SET`, `FAILURE_CLR` — failure mode registry accessors
- `FOREACH_FAILURE_MODE(X)` — registry at `MemHeaders/FailureModeRegistry.hpp`
- `PerCoreSnap` struct in `DataStream/EngineTUI.hpp`
- `ML_Status` panel in `GUI/DashboardPanels.hpp` (or similar GUI dir)

**5. File:line refs for cited tests / baselines:**
- 2904/2904 tests pass at `v5.14.post1`; baseline anchor
- `tests/controller_test.cpp` test sites referencing ModelHandle: `grep -n "ModelHandle\|h\.has_" tests/controller_test.cpp` — enumerate tests that need updating during v5.15.0 migration
- Canonical migration reference for v5.15.0: `Backtest/BacktestEngine.hpp:1147-1220` (`Backtest_RunFullValidation`) — already-migrated sibling showing the canonical STAMP_SET + canonical-wire-name pattern
- v5.14.post1 sibling fix at `Backtest/BacktestPanels.hpp:3206-3266` — second canonical reference for the StampInferenceCfgInputs migration shape (applies similarly to ModelHandle)

**6. Stale-claim audit (before committing the plan):**
- Verify `train_multi_horizon_worker_fn` name + location via grep (may be named differently; recent v5.13.5 worked in this area)
- Verify FailureModeRegistry uint16_t storage + headroom — read the actual file
- Verify GUI panel file name + ML Status panel location — `find GUI/ -name "*.hpp" | head` + grep for "ML Status"
- Verify current cfg fields: `stamp_verification_mode`, `mode`, `held_out_stamp_secret`, `require_mlockall`, `ws_dead_time_flatten_enabled`, `reconcile_mode`, `model_max_age_hours`, `calibration_log_path` — confirm all exist

**7. Effort claims reconcile with file size deltas:**
- v5.15.0 ModelHandle migration: ~400 LOC estimate. Verify by `wc -l ML_Headers/ModelInference.hpp` (currently ~2500+ lines) and `grep -c "uint8_t  has_" ML_Headers/ModelInference.hpp` (~17 fields).
- v5.15.1 Model Health panel: ~200 LOC estimate. Verify by reading current ML_Status panel size + adding CollapsingHeader scope.
- v5.15.2 boot gate: ~150 LOC. Boot path in `CoreFrameworks/EngineSharded.hpp` is well-bounded.

**8. Source-audit references with paths:**
- Predecessor sprint: `plans/v5.14-foxml-port-and-maker/MASTER.md` (sealed; v5.14.post1 noted in postmortems)
- Predecessor postmortem: `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-11-v5.14.11-session-postmortem.md` (last sub-sprint)
- Post-release-fixes log: `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-11-v5.14-post-release-fixes.md` (v5.14.post1 entry)
- Boot log analysis context: chat-time review of operator's `./engine` boot output identified 5 issue classes (feature_hash mismatch, empty stamp secret, mlockall failed, hardcoded strategies, missing grid_member_count); this prompt's predecessor session walked through these

**9. Predecessor / dependent plans named with paths:**
- Predecessor: `plans/v5.14-foxml-port-and-maker/MASTER.md` (sealed `v5.14` umbrella + `v5.14.post1` patch)
- Sister: `TESTING_NOTES/v5.12-v5.14-mini.md` (operator paper-test guide; v5.15 ships consume this)
- Dependent (post-v5.15): operator paper-test session — see step 10

**10. Tag names locked + rollback anchors:**
- Sprint umbrella: `v5.15` (annotated; created after all sub-ships ship)
- Sub-ship tags: `v5.15.0`, `v5.15.1`, `v5.15.2`, `v5.15.3`, `v5.15.4`
- Pre-tag rollback: `pre-v5.15` = `v5.14`; `pre-v5.15.0` = engine HEAD pre-migration
- Post-sprint patches if needed: `v5.15.post1`, `v5.15.post2`, etc. (per the `.postN` scheme established v5.14.post1)

---

## DESIGN_SPECS / CLAUDE.md cross-references

Each sub-ship MUST reference the relevant DESIGN_SPECS docs in its sub-plan. The
catalog is at `tick-trader-percore-workspace/DESIGN_SPECS/README.md` (26 patterns).

**v5.15.0 (ModelHandle migration) MUST read before coding:**
- `DESIGN_SPECS/x-macro-registry-with-presence-dispatch.md` — Y3 dispatch + per-struct PRESENCE column
- `DESIGN_SPECS/autopopulate-pattern-for-production-caller-class.md` — STAMP_MODEL_CONST_AUTOPOPULATE companion
- `DESIGN_SPECS/pre-post-cfg-registry-split-for-emit-order-preservation.md` — PRE/POST split if needed
- `DESIGN_SPECS/bitmap-flag-api.md` — BITMAP_* / STAMP_HAS_SET ergonomics
- `DESIGN_SPECS/wire-format-byte-preservation-discipline.md` — HMAC chain byte-equivalence during migration
- `DESIGN_SPECS/struct-padding-determinism-pattern.md` — verify ModelHandle padding is deterministic post-migration
- CLAUDE.md items: **13** (X-macro registry), **15** (parity-tested-by-construction), **19** (structural fix preferred), **20** (bit-packed flag storage), **21** (AUTOPOPULATE companion), **22** (PRE/POST split), **27** (struct padding determinism)

**v5.15.1 (Model Health panel + PerCoreSnap bitmap migration) MUST read:**
- `DESIGN_SPECS/bitmap-flag-api.md` — BITMAP_* API (FAILURE_IS_SET / SET / CLR)
- `DESIGN_SPECS/per-snapshot-cluster-layout-pattern.md` — alignas(64) cluster layout when adding PerCoreSnap fields
- `DESIGN_SPECS/transient-aggregation-bitmap-pattern.md` — for any function-local aggregation
- CLAUDE.md items: **1** (Portfolio bitmap precedent), **12** (display ↔ execution invariant — every new PerCoreSnap field needs a GUI surface), **20** (bit-packed flag storage)

**v5.15.2 (Live-readiness boot gate) MUST read:**
- `DESIGN_SPECS/slow-path-gate-registry-pattern.md` — if applying gate-registry pattern to boot checklist
- `DESIGN_SPECS/curve-registry-pattern.md` — pattern for enum-driven dispatch (if cfg `mode` becomes enum)
- CLAUDE.md item **18** (slow-path latency; boot gate is one-time cost so doesn't apply but reference for discipline)

**v5.15.3 (Multi-horizon worker stamping) MUST read:**
- Canonical reference: `Backtest/BacktestEngine.hpp:1147-1220` (Backtest_RunFullValidation) + `Backtest/BacktestPanels.hpp:3206-3266` (train_model_worker_fn post-v5.14.post1)
- `DESIGN_SPECS/wire-format-byte-preservation-discipline.md` — stamp body emit must preserve canonical body bytes

**v5.15.4 (Strict-mode + secret defaults in live) MUST read:**
- `DESIGN_SPECS/cfg-flag-eligibility-criteria.md` — apply criteria to new cfg derivations (mode=live IMPLIES strict, etc.)
- CLAUDE.md item **13** (reuse-audit), **19** (structural fix preferred for the strict-mode unification with TECH_DEBT-005)

**All sub-ships MUST consider:**
- `DESIGN_SPECS/audit-driven-pre-coding-gate.md` — pre-coding audit gate for HIGH-RISK ships
- `DESIGN_SPECS/structural-fix-preferred-decision-framework.md` — when to absorb adjacent debt vs defer
- `DESIGN_SPECS/pattern-codification-lifecycle.md` — capture any new patterns that emerge

---

## TECH_DEBT closures (audit-write contract per CLAUDE.local.md)

This sprint closes 4 entries. Each closure MUST update `DOCS/TECH_DEBT.md`:

| Entry | Sub-ship | Closure form |
|---|---|---|
| **TECH_DEBT-014** (ModelHandle migration) | v5.15.0 | ✅ CLOSED v5.15.0 — full migration to FOREACH_STAMP_BOUND_MODEL_CONST X-macro generation |
| **TECH_DEBT-028** (PerCoreSnap bool-as-uint8 → bitmap) | v5.15.1 | ✅ CLOSED v5.15.1 — 4 bool fields migrated to uint8 bitmap with MASK_* constants |
| **TECH_DEBT-033** (wider-build verification in /readiness) | v5.15.2 | ✅ CLOSED v5.15.2 — /readiness Check N+1 added |
| **TECH_DEBT-005** (single-zoo hot-swap strict-mode unification) | v5.15.4 | ✅ CLOSED v5.15.4 — strict-mode handling unified across boot + hot-swap surfaces |

If a closure can't fully land in its sub-ship, downgrade to substantial-progress and defer the rest to TECH_DEBT-N+1.

**Note:** TECH_DEBT-033 was discussed during v5.14.post1 closeout but NOT yet written to the ledger. Step 0 for v5.15.2 includes opening TECH_DEBT-033 in `DOCS/TECH_DEBT.md` before sub-ship work begins.

---

## Audit cadence (per Caramel's 2026-05-10 rule)

**Don't auto-trigger audits.** Suggest when impact-criteria fire; wait for operator greenlight.

**HIGH-RISK sub-ships (warrant pre-coding multi-audit gate):**
- **v5.15.0** ModelHandle migration — HIGH-RISK. Suggest `/parity-check` + `/trace-deps` + `/readiness` + `/dod-audit` in parallel BEFORE coding starts. Class is "mechanical-migration with HMAC chain implications + ~14-20 caller sites at risk" — same class as v5.14.8.A.merged, which had a missed site (post1 bug). Pre-coding audit catches missed sites.

**MEDIUM-RISK sub-ships (suggest selective audits):**
- **v5.15.2** boot gate — `/parity-check` if cfg parser touched + `/dod-audit` for slow-path-gate-registry-pattern compliance
- **v5.15.4** strict-mode defaults — `/parity-check` for HMAC chain preservation (cfg=live default-flip mustn't change stamp body emit bytes)

**LOW-RISK sub-ships (sub-ship-close spot-audit):**
- v5.15.1 (Model Health panel) — well-trodden pattern (FOREACH_FAILURE_MODE extension); /merge-scan after
- v5.15.3 (multi-horizon stamping) — mechanical addition; /readiness checks Step 0 file:line refs

**Mid-sprint audit suggestion fires when:**
- HIGH-RISK ship just shipped → before subsequent dependent ships
- New pattern field-tested for first time → audit before propagation
- Cross-cutting changes (registry extensions, etc.) → /merge-scan + /dod-audit

---

## Verification checklist (sprint close)

Per `TESTING_NOTES/v5.12-v5.14-mini.md` discipline:
- [ ] `./build.sh test` — all tests pass (target: 2904+ from baseline; +N new)
- [ ] `./build.sh gui suite tsan asan all` — wider build clean (NEW post-v5.14.post1 discipline)
- [ ] `./build.sh test` re-run after wider builds to ensure no test-target regression
- [ ] Stamp body byte-equivalence test for ModelHandle migration (v5.15.0) — synthesize representative ModelHandle pre/post-migration; emit; verify SHA-256-locked byte-identical to v5.14.post1 baseline OR document expected differences with explicit registry rationale
- [ ] HMAC chain test — load a v5.14-era stamp on v5.15-engine; verify loads cleanly (Surface G forward-compat preserved)
- [ ] Hot path UNTOUCHED (per CLAUDE.md item 12 display↔execution invariant — new PerCoreSnap fields in v5.15.1 are slow-path only, but verify with `DOCS/HOT_PATH_CHANGELOG.md` audit)
- [ ] All 4 TECH_DEBT closures land in `DOCS/TECH_DEBT.md` with proper status updates
- [ ] CHANGELOG.md row written for sprint umbrella
- [ ] HOT_PATH_CHANGELOG entries for any slow-path additions
- [ ] Version.hpp bumped per sub-ship (5.14.post1 → 5.15.0 → ... → 5.15)
- [ ] All sub-ship postmortems written before sprint umbrella tag
- [ ] Workspace synced after sprint umbrella

---

## Plan deliverables (this session)

**Write these files; DO NOT touch engine code:**

1. `plans/v5.15-live-readiness/MASTER.md`
   - Charter, sprint scope, sub-ship list with sizing, branch state, rollback anchors, cold-pickup 10 fields, verification gate

2. `plans/v5.15-live-readiness/subplans/2026-05-12-v5.15.0-modelhandle-migration.md`
   - HIGH-RISK sub-ship; full design space + audit recommendations + canonical-reference cite + 20-field migration table + test plan + HMAC-byte-equivalence verification + rollback discipline

3. `plans/v5.15-live-readiness/subplans/2026-05-12-v5.15.1-model-health-panel.md`
   - FOREACH_FAILURE_MODE extension table (4-7 new entries with severity + storage class) + PerCoreSnap field additions + GUI CollapsingHeader spec (per CLAUDE.md item 12 display↔execution invariant) + PerCoreSnap bitmap migration table for TECH_DEBT-028 absorption

4. `plans/v5.15-live-readiness/subplans/2026-05-12-v5.15.2-live-readiness-boot-gate.md`
   - Pre-flight checklist enumeration (the 9 operator-side items) + cfg `mode=live` REFUSE logic + /readiness skill wider-build check addition for TECH_DEBT-033

5. `plans/v5.15-live-readiness/subplans/2026-05-12-v5.15.3-multi-horizon-worker-stamping.md`
   - Locate actual worker function name + canonical-reference cite + stamping pattern application

6. `plans/v5.15-live-readiness/subplans/2026-05-12-v5.15.4-live-mode-strict-defaults.md`
   - `mode=live` default-flip design + single-zoo hot-swap strict-mode unification for TECH_DEBT-005

7. (Optional in this session, can defer) Pre-coding audit reports for v5.15.0:
   - `plans/plan_checks/parity-check-2026-05-12-v5.15.0.md`
   - `plans/plan_checks/trace-deps-2026-05-12-v5.15.0.md`
   - `plans/plan_checks/readiness-2026-05-12-v5.15.0.md`
   - `plans/plan_checks/dod-audit-2026-05-12-v5.15.0.md`
   - `plans/plan_checks/2026-05-12-v5.15.0-fresh-audits-synthesis.md`

   But Caramel hasn't greenlit running these yet; CONSULT before launching audit
   agents. Per `feedback_consult_on_audit_findings.md` memory.

---

## Consultation workflow

After drafting MASTER + subplans + any amendments from initial /plan-check spot
review, **PRESENT the full plan to Caramel** with:

1. Final sub-ship sequencing + dependencies
2. TECH_DEBT closure summary (which entries close in which ship)
3. Audit recommendations + their rationale (HIGH/MEDIUM/LOW risk classification)
4. Estimated effort per sub-ship
5. Open questions (anything you couldn't fully resolve from this prompt)
6. Caramel's "harder upfront for easier future" rule check — confirm no shortcuts
   on structural fix vs direct patch where applicable
7. Recommendation: "proceed to pre-coding audits for v5.15.0?" OR "any plan
   amendments needed first?"

**Do NOT proceed to coding until Caramel greenlights.** Per `feedback_no_defer_for_effort.md` + `feedback_consult_on_audit_findings.md` memories.

---

## What's intentionally NOT in scope for v5.15

- Operational safety audit (drawdown circuit breaker, daily PnL cap, position
  size limits, kill switch) — Caramel hasn't confirmed whether these exist
  today; deferred to ops-side investigation before/during paper-test
- Order book data source (TECH_DEBT-008 maker MVP) — deferred indefinitely
  per existing TECH_DEBT
- Paper-test execution — sequenced AFTER v5.15 ships
- v5.15.0 ModelHandle migration's WIDER companion (TECH_DEBT-029 source-file-length
  reduction) — separate cleanup sprint
- CLAUDE.md context-management cleanup (TECH_DEBT-032) — separate sprint

---

## References (load on demand as needed)

**Engine codebase entry points:**
- `CLAUDE.md` (always loaded; items 1-27)
- `CLAUDE.local.md` (private project memory; going-forward rules)
- `DOCS/CLAUDE_ML_INVARIANTS.md` (when touching FeatureRegistry / MLBuildContext / train-serve path)
- `DOCS/CLAUDE_INVARIANTS.md` (when touching OMS / kill switch / snapshot / fee math / hot path)
- `DOCS/CLAUDE_INTEGRATION.md` (when adding cfg field / GUI panel / strategy / per-core override)
- `DOCS/CLAUDE_REVIEW.md` (multi-day change planning)
- `DOCS/STRATEGY_AND_CODING_RULES.md` (private; 11 strict invariants for hot/slow-path code)
- `DOCS/LATENCY_OPTIMIZATION_AUDIT.md` (private; 13 parts of optimization intel)

**Workspace deliverables:**
- `tick-trader-percore-workspace/DESIGN_SPECS/README.md` (26 patterns catalog)
- `tick-trader-percore-workspace/DOCS/TECH_DEBT.md` (34 entries; 005, 014, 028, 033 to close)
- `tick-trader-percore-workspace/DOCS/HOT_PATH_CHANGELOG.md` (sprint-end if any hot-path additions)
- `tick-trader-percore-workspace/DOCS/CHANGELOG.md` (sprint-end row)
- `tick-trader-percore-workspace/TESTING_NOTES/v5.12-v5.14-mini.md` (operator paper-test guide; reference for paper-test cadence)

**Active TECH_DEBT lookups:**
- TECH_DEBT-005 — workspace `DOCS/TECH_DEBT.md` around line 145
- TECH_DEBT-014 — workspace `DOCS/TECH_DEBT.md` around line 367
- TECH_DEBT-028 — workspace `DOCS/TECH_DEBT.md` around line 605
- TECH_DEBT-033 — to be added (this session's prerequisite; or in v5.15.2 plan)

---

## Operator profile

Caramel (Jennifer Lewis) — senior C++ trading-systems engineer. High latency
discipline, prefers structural-fix-when-recurring, dislikes deferred items,
wants compile-time enforcement over runtime checks. Asks for honest
disagreement when applicable. Consults heavily before coding decisions
(per `feedback_consult_on_audit_findings.md`).

Code style: `Pattern_FunctionName`, C-style with templates, no classes (with
RAII destructor exceptions per CLAUDE.md), branchless hot paths, `FPN<F>` in
hot-path math, X-macro registries for multi-site additions.

Don't auto-mark things as deferred; defer is last-ditch.

---

## Sentinel for sprint start

When MASTER + subplans are drafted, present this summary to Caramel:

> v5.15 plan drafted. MASTER + 5 subplans created at
> `plans/v5.15-live-readiness/`. Sprint scope: 4 TECH_DEBT closures
> (-014, -028, -033, -005) + 5 sub-ships across live-readiness +
> ModelHandle structural unification + Model Health panel + boot gate +
> multi-horizon stamping + live-mode strict defaults. Estimated 4-5 days.
> Recommend pre-coding audit gate (/parity-check + /trace-deps + /readiness
> + /dod-audit in parallel) before v5.15.0 ModelHandle migration ships.
> Ready to proceed? Any plan amendments needed first?

Then WAIT for greenlight. Don't preemptively launch audits or write code.
