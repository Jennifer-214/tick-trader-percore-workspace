# /merge-scan audit — v5.13.0 (sell-side ML) — 2026-05-08

## VERDICT: **GREEN** ✓

All ~510 new lines across 11 files **reuse existing infrastructure
strategically**. No parallel registries, no duplicated ensemble
logic, no duplicate CSV writers.

---

## Key Findings Summary

### 1. `last_exit_was_predicted[]` + `last_exit_predicted_p[]` — REUSES_HELPER

- Follows existing per-slot array pattern
  (`MAX_PORTFOLIO_POSITIONS` indexing)
- Reuses existing SPSC ring release-acquire fence for thread
  visibility
- Distinct purpose (ML attribution) vs. `last_fill[]` (financial
  tracking)

### 2. `OrderManager_OpenCalibrationLog` — EXTENDS_EXISTING

- Reuses ShardedTradeLog's header-detection pattern
  (`fseek` + `ftell`)
- Different columns/cadence (complementary to `trade_log`, not
  parallel)
- Single-writer guarantee enforced (drainer thread)

### 3. `last_exit_prediction` / `last_exit_dominant_horizon` — NEW (Intentional)

- Intentional sell-side parallel to buy-side
  `staged_prediction` / `active_prediction`
- Shape consistency verified (double + int horizon index)
- Different lifecycle (ephemeral per-cycle vs. position-bound)

### 4. `SHALT_EXIT_PREDICTED` — REUSES_HELPER

- Single X-macro append to `FOREACH_SHALT` (no parallel registry
  created)
- Enum/array auto-generation via existing registry pattern
- Test assertions correctly updated (NUM_SHALT_CODES 17→18)

### 5. ML_BuildParameters sell-side ensemble block — REUSES_HELPER

- Uses existing `Model_Predict_Normalized` + ensemble-averaging
  pattern
- Different model array (`exit_predictor` vs. `primary_handles`)
- Reuses already-computed features (sibling-scaler consistency
  enforced)

### 6. cfg fields (use_exit_model / exit_threshold / exit_signal_model_dir / calibration_log_path) — EXTENDS_EXISTING

- Intentional buy-side/sell-side parallel naming
  (`use_exit_model` mirrors `use_aot_inference`)
- `exit_threshold=0.6` matches `ml_buy_threshold` pattern
- Parser uses same `CFG_PARSE_*` macros (no duplication)

---

## Additional Quality Notes

- **CSV headers**: Minor duplication acceptable (headers are
  semantically distinct; parameterization would obscure intent)
- **SPSC fence**: Reuses existing ring release-acquire semantics
  (`uint8_t` naturally atomic on x86)
- **Init loops**: Follows existing pattern; intentional separation
  for clarity (one-time boot operation)
- **Cross-plan conflicts**: None (single coordinated 3-part ship
  v5.13.0.A → .B → .C)

---

## Design Excellence

The implementation demonstrates **disciplined infrastructure reuse**:

- Per-slot arrays mirror `last_realized_return` pattern
- CSV logging extends ShardedTradeLog pattern
- ML ensemble reuses `Model_Predict_Normalized`
- Registry follows X-macro discipline
- Sell-side path cleanly separated (different sinks, different
  inputs, different lifecycle)

---

## CRITICAL GAP CLOSED

The `exit_predictor` `buy_class_idx` aliasing fix
(`CoreModelZoo.hpp:1313–1315`) prevents silent semantic inversion
that would have caused exits to fire during dips (VALLEY class)
instead of peaks (PEAK class). This was caught by /plan-check +
/readiness Check 19 *before* coding started.

---

**Ship is approved for production. No refactoring needed.**
