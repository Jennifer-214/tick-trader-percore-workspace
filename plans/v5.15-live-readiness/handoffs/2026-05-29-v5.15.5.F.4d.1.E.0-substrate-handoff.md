---
type: handoff
ship_tag: v5.15.5.F.4d.1.E.0
template: _TEMPLATE-e-ship-planning-handoff.md (2nd instance; + NEW SEAM section per D-72 / task #11 — first seam-structured handoff)
predecessor: v5.15.5.F.4d.1.D.1 (doc-system end-goal-language sweep; SHIPPED 2026-05-28 dc37b24)
successor: v5.15.5.F.4d.1.E.1 (Foundation; first coding ship — gated on .E.0 GREEN)
audit_tier: HIGH-RISK (.E.0 = audit gate for the whole .E sub-sprint)
engine_head: dc37b24
workspace_head: 1ef30e2 (or later — this handoff + session work committed at /sync-workspace)
findings_sidecar: plans/v5.15-live-readiness/plan_checks/E.0-audit-reports/pre-implementation-findings/CANONICAL-FINDINGS.md
---

# `.E.0` substrate handoff — verification apparatus built; pre-`.E.1` net + per-plan dives next

**Created:** 2026-05-29 (`/close-session`). **Branch:** `feat/v5.15-live-readiness`.
**Pickup:** `/accept-handoff plans/v5.15-live-readiness/handoffs/2026-05-29-v5.15.5.F.4d.1.E.0-substrate-handoff.md`

This session **built the `.E.0` verification apparatus** — the machinery that makes the `.E` rework safe AND evidence-grounded. It did **not** touch `.E` engine code. Everything is grounded in evidence from the *current* codebase (the 141-finding read-only audit + live symbol/grep verification), per operator directive: *"make this update solid based on evidence we find in the previous codebase."*

## 1. Ship scope + end goal
`.E.0` — the audit gate for the whole `.E` sub-sprint, expanded (v0.2) to ALSO ingest the 141-finding engine audit + stand up the pre-`.E` correctness net (**Job A** plan-audit + **Job B** findings-ingestion + safety-net). End goal: every `.E.x` plan GREEN-ready with per-finding fix-designs + the safety net (characterization tests + CI gates) green BEFORE `.E.1` codes. Predecessor `.D.1` (shipped). Successor `.E.1` Foundation. Audit tier **HIGH-RISK**.

## 2. Git state (pickup verify)
Engine HEAD `dc37b24` (+ tag `v5.15.5.F.4d.1.D.1`; clean of code — `tools/dedupe_findings.py` untracked, see SEAM). Workspace HEAD `1ef30e2` + this session's work (pushed at Stage 7). Branch `feat/v5.15-live-readiness`. Rollback anchor `pre-v5.15.5.F.4d.1.D.1`.

## 3. Findings sidecar + per-finding fix-design status  ⟵ the core (the EVIDENCE base)
`findings_sidecar: CANONICAL-FINDINGS.md` — **NEW this session: the deduped canonical index (141 raw → 124 distinct, lossless).** Every entry is grounded in an actual code read; reproducible via `python3 tools/dedupe_findings.py --merge-map <dir>/findings-merge-map.txt --emit-index <dir>/CANONICAL-FINDINGS.md`. (The raw sidecars had unreliable ids — 16 colliding; see merge-map for the eyes-on reconciliation.)
- **fix_ship slices:** standalone(pre-`.E.1`)=74 · `.E.1`=25 · `.E.3`=9 · `.E.2`=6 · `.E.5`=6 · `.E.6`=2. (2 CRITICAL / 30 HIGH / 60 MED / 32 LOW.)
- **fix-design status:** only `.E.1` audited so far → **RED** (see `E.1-dive-synthesis.md`). E.2-E.6 slices NOT yet laid into their plans (0 fix-designs).
- **NOT yet done:** runtime-confirm the 2 CRITICALs (`conc-5`, `live-bc-1`) + the 49 provisionals on a disposable clone.
- **Discipline:** runtime-confirm provisionals BEFORE any test/CI; PERSIST (behavior `.E` keeps) → characterization tests before the rename; CHANGES-BY-DESIGN → new-invariant test AS PART of the ship.

## 4. Cross-ship invariants this ship must preserve
H1-H20; Cluster/Node hierarchy (D-15); aggregator-as-single-writer (D-54); per-node economic isolation (`.E.5`); the safeguard→ship distribution (D-68; matrix §3). `.E.0`'s job: verify each `.E.x` plan respects these + the DAG + ingests its findings slice.

## 5. Required reading (ship surface)
**Apparatus built this session (the NEW substrate — read first):**
- `E-guard-coverage-matrix.md` — enforcement ladder + verifiability triad + safeguard→ship distribution + rolling-window cadence + the matrix (the **completeness contract**: no HOLE cell at `.E` close w/o accepted-rationale). [D-68..72]
- `plan_checks/E.0-audit-reports/pre-implementation-findings/CANONICAL-FINDINGS.md` + `findings-merge-map.txt` — the evidence base.
- `plan_checks/E.0-audit-reports/E.1-dive-synthesis.md` — `.E.1` RED verdict + punch-list (first `/plan-dive` output; template for dives 2-9).
- `claude-skills/plan-dive/SKILL.md` — the reproducible per-plan multi-layer dive.
- decision-log `D-67..D-72`.

Plus: `.E.0` plan body (v0.2 — Job A + Job B), `_SESSION-CONTEXT.md` (audit pickup), `E-MASTER-REFERENCE.md`, the `E-dependency-graph`, + CLAUDE.md / CLAUDE.local.md / MEMORY.md baseline.

## 6. Pre-coding gate + the evidence-grounded work sequence
Per the safest-order analysis (confirm reality → lock current behavior → only then change it) + DD-5:
1. **Pre-`.E.1` foundational batch** — runtime-confirm the 2 CRITICALs + the `FPN_Sqrt`/`USE_NATIVE_128` byte-diff on a **disposable clone**; fix the FP-determinism + strict-aliasing-UB + parity-oracle-stub. These **gate the net** (the determinism gate + characterization tests can't be trusted until the FP path is confirmed + the parity oracle isn't a stub — see `fpmem-1/2`, `hpg-bc-1`).
2. **Build the net** — PERSIST characterization tests (freeze current correct behavior) + the 3 CI gates (determinism byte-compare; latency ratchet; 3-path backtest≡paper≡live identity), GREEN on the current engine BEFORE `.E.1` codes.
3. **Amend `.E.1`** per `E.1-dive-synthesis.md` punch-list: resolve the `conc-5` submit-path topology (per-cluster MPSC declared but `SPSCRing.hpp:12` = "no MPSC"; Tests claim per-node single-producer — contradiction); design the atomic-FPN aggregator + reconcile Phase F with the D-54 push model; wire `findings_sidecar:` + lay the 25 fix-designs; add safeguard phases; fix stale anchors.
4. **Dive E.2-E.X** — `/plan-dive <plan>` each, **sequentially** (rolling-window), ingesting its canonical-findings slice.

Fire `/plan-dive` (or `/precoding-audit-gate` HIGH-RISK) before any plan codes. Consult before coding (`feedback_consult_on_audit_findings`).
⚠️ `.E.1`'s ~5,000-site Core→Node rename MUST use **symbol/AST-aware** tooling, NOT prose-token (TECH_DEBT-142 / B19 / Class 36).

## 7. Verification gate (`.E.0` GREEN)
Each `.E.x` plan has `findings_sidecar:` + per-finding fix-designs (`/plan-dive` GREEN). PERSIST characterization tests + CI invariant checks green on the current engine. 49 provisionals confirmed-or-dismissed; both CRITICALs resolved or `.E.1`-design-verified. Guard-matrix: no open HOLE cell w/o accepted-rationale. NEW anti-pattern Classes 37+ codified; `/capture-audit` clean. **Tie-back:** `.E.0` GREEN ⇒ the `.E` rework is gated behind a regression-proof, evidence-grounded net (DD-5).

---

## SEAM (rolling-window; NEW per D-72 — first seam-structured handoff; advances task #11)

**Outbound seam — what THIS session delivered (next session builds on):**
- Verification apparatus: `dedupe_findings.py` + `CANONICAL-FINDINGS.md` (evidence base) + `E-guard-coverage-matrix.md` + `/plan-dive` + `E.1-dive-synthesis.md`.
- Decisions `D-67..D-72`; `.E.0` plan body v0.2 (Job A + Job B); `/plan-dive` registered in CLAUDE.md skill-suite.
- ⚠️ **`tools/dedupe_findings.py` is in the ENGINE working tree, UNTRACKED** — the workspace push does NOT cover it. Its OUTPUT (`CANONICAL-FINDINGS.md`) IS workspace-backed and the tool is reproducible/documented, so nothing is lost — but **`git add` it at the next engine touch** (`.E.0`/`.E.1` ship close) so it's off-machine.

**Inbound seam — what next session MUST verify on pickup (`/accept-handoff` does most):**
- Engine HEAD still `dc37b24`; workspace at-or-after this session's push.
- `CANONICAL-FINDINGS.md` regenerates clean (141 raw / 124 distinct / lossless) via the `dedupe_findings.py` command above.
- `/plan-dive` invocable (in skill list); `/capture-audit --deep` clean.
- `tools/dedupe_findings.py` present (untracked) — if missing, regenerate from this handoff + the spec.

## TaskList (recreate on pickup) — 13 tasks
| # | Task | Status |
|---|---|---|
| #1 [#16] | Expand `.E.0` + audit codification | in_progress (expansion + substrate DONE; codify Classes 37+ + lay E.2-E.6 fix-designs remain) |
| #2 [#17] | Runtime-confirm 49 provisionals + 2 CRITICALs (disposable clone) | pending — **next** |
| #3 [#18] | Codify 2 session memories → DESIGN_SPECS + file-size amend | pending |
| #4 [#19] | PRE-PAPER-TEST correctness mini-ship (55 findings) | pending (after runtime-confirm) |
| #5 [#20] | Evaluate 5 audit-recommended lenses → /plan-dive layers | pending |
| #6 [#21] | Per-node CPU poll-mode toggle (extends D-33) | pending |
| #7 [#22] | Reusable `.E`-ship handoff template | DONE |
| #8 [#23] | Reformat CLAUDE.md + CLAUDE.local.md | in_progress (CLAUDE.local.md de-chunk done) |
| #9 [#2-13] | `.E` pipeline (E.1 → E.X) — gated on `.E.0` GREEN | pending |
| #10 | Add missing `.D.1` row to engine `DOCS/CHANGELOG.md` (Stage 4.5 gap) | pending |
| #11 | Rework handoff template for seam cadence (this handoff = first instance) | pending (advanced) |
| #12 | Dedup + aggregate findings → canonical index | DONE |
| #13 | Per-plan correctness-audit skill (`/plan-dive`) | DONE |

## Pickup — first action
Run `/accept-handoff` on this doc. Then **the pre-`.E.1` foundational batch** (§6 step 1): runtime-confirm the 2 CRITICALs + the `FPN_Sqrt`/`USE_NATIVE_128` diff on a disposable clone — the evidence that gates the net. *(Alternative first move: validate `/plan-dive` by firing it on `.E.1` — calibrates the skill against `E.1-dive-synthesis.md` before dives 2-9 rely on it.)*

**End of `.E.0` substrate handoff.**
