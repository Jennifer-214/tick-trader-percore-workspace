# /trace-deps + /readiness audit — v5.14.2.E (PostLoadSetup + stamp body migration + TECH_DEBT system)

**Plan:** `plans/2026-05-08-v5.14.2-hot-swap-ensemble.md` (.E sections)
**Date:** 2026-05-09
**Auditor:** Manual /trace-deps + /readiness combined audit per Layer 2 execution model

---

## Verdict: YELLOW (design GREEN; 2 process clarifications needed pre-coding)

**Design:** Architecturally sound. X-macro registries are the right structural fix for Class 18; stamp body migration via Surface G is backward-compatible; skill spec additions are proportionate.

**Process gaps:**
1. PARITY-011 + PARITY-012 referenced in plan but not in ledger
2. Stamp body field count needs clarification (9 total vs 6 new)

Both fixable in ~10 min before coding.

---

## Per-sub-tag findings

### v5.14.2.E.1 — PostLoadSetup X-macro registries: PASS

- X-macro shape correct (heterogeneous tuple expressions)
- Helper signatures right
- All 4 refactor targets verified (boot ensemble, boot single-zoo, hot-swap ensemble, hot-swap single-zoo, backtest both)
- PARITY-009 sub-gaps verified real
- PARITY-010 verified in existing ledger
- Strict-mode handling preserved at caller level (helper returns int)
- Tests realistic (~12)

### v5.14.2.E.2 — expected.cfg → stamp body migration: YELLOW (count clarification)

- StampBoundCfgRegistry growth 13 → 15 verified (existing has 13)
- New StampBoundModelConstRegistry (4 entries) follows Surface G pattern
- Backward compat (legacy fallback to expected.cfg) well-designed
- Tests realistic (~8)

**Clarification needed:** "9 expected.cfg fields" vs "6 new stamp body fields" — resolution: of 9 original fields, 2 cfg fields are NEW to stamp body (ml_buy_threshold, gap_acceptable_threshold), 3 cfg fields ALREADY stamp-bound (v5.9.2b/v5.9.4a: barrier_gate_enabled, held_out_fraction, training_poll_interval), 4 architectural NEW to stamp body. So 2+4=6 NEW, 3 already-bound, 9 total.

### v5.14.2.E.3 — TECH_DEBT.md + skill specs: PASS

- 5 initial entries are real (verified each)
- Auto-write contract mirrors PARITY_ISSUES contract
- Check 24 + Check 25 distinct from existing checks
- CLAUDE.md item 19 distinct from item 13 (philosophy vs technical pattern)
- Class 18 strengthen sub-clause correct shape

---

## Cross-cutting verification

| Aspect | Verdict | Notes |
|---|---|---|
| Effort estimate (~10h for 750 LOC) | PASS | Sub-linear vs A-D's ~5-6h for 165 LOC; refactors amortize setup |
| Test count (~25 new) | PASS | ~1 test / 24 LOC; reasonable for registry + parity coverage |
| Rollback story | PASS | Each .E.1/.E.2/.E.3 atomic + independently revertible |
| Check 22 (downstream re-audit) | YELLOW | v5.14.3 plan should be re-audited AFTER .E.3 lands (Check 24/25 are added to readiness; could surface new findings on v5.14.3) |
| Master plan downstream impact | PASS | v5.14.3-12 not blocked by v5.14.2.E |

---

## Critical blockers (must fix before coding)

### Blocker #1: PARITY-011 + PARITY-012 not in ledger

Plan references PARITY-011 (single-zoo hot-swap missing VerifyExpected) and PARITY-012 (backtest single-zoo missing ValidateAgainstCfg) but `DOCS/PARITY_ISSUES.md` only contains PARITY-009 + PARITY-010.

**Fix:** Add both entries to ledger before coding starts. Per auto-write contract.

### Blocker #2: Stamp body field count ambiguous

Plan says "5 cfg fields" without clarifying NEW vs already-stamp-bound. Resolution above: 2 NEW cfg + 3 already-bound + 4 NEW architectural = 9 total, 6 NEW.

**Fix:** Amend plan with explicit enumeration.

---

## Recommendation

Proceed to coding after both blockers fixed (~10 min). Design is correct; process gaps are documentation/ledger discipline only.
