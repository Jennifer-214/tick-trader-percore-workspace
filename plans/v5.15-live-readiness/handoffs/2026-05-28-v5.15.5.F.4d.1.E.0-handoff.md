---
type: handoff
ship_tag: v5.15.5.F.4d.1.E.0
template: _TEMPLATE-e-ship-planning-handoff.md (first instance)
predecessor: v5.15.5.F.4d.1.D.1 (doc-system end-goal-language sweep; SHIPPED 2026-05-28)
successor: v5.15.5.F.4d.1.E.1 (Foundation — Core→Node rename + per-node drainer absorption)
audit_tier: HIGH-RISK (.E.0 is the audit gate for the whole .E sub-sprint)
engine_head: dc37b24
engine_tag: v5.15.5.F.4d.1.D.1
workspace_head: a88debf
findings_sidecar: plans/v5.15-live-readiness/plan_checks/E.0-audit-reports/pre-implementation-findings/_SESSION-CONTEXT.md
---

# `.E.0` handoff — audit + findings-ingestion + pre-`.E` correctness net

**Created:** 2026-05-28 (end of the `.D.1` ship session). **Branch:** `feat/v5.15-live-readiness`.
**Pickup:** `/accept-handoff plans/v5.15-live-readiness/handoffs/2026-05-28-v5.15.5.F.4d.1.E.0-handoff.md`

---

## 1. Ship scope + end goal

`.E.0` — **the audit gate for the entire `.E` sub-sprint, expanded to also ingest findings + stand up the pre-`.E` correctness net.** End goal: every `.E.X` plan body has a detailed per-finding fix-design + a `findings_sidecar:` backlink; the PERSIST characterization tests + CI invariant checks are written + green on the current engine; the 2 CRITICALs + fixedpoint cluster are runtime-confirmed. `.E.0` GREEN ⇒ the rework is safe to begin (a reintroduced regression breaks the build instantly). Predecessor `.D.1` shipped. Successor `.E.1` Foundation. **Audit tier: HIGH-RISK.**

This resolves **DD-5** (decided 2026-05-28): proceed with `.E` (the usability win) **gated behind this correctness net** (the de-risk). Not pure-proceed, not pure-pause — the synthesis.

## 2. Git state (pickup verify)

- Engine HEAD `dc37b24` + tag `v5.15.5.F.4d.1.D.1` (GPG-signed, pushed). Branch `feat/v5.15-live-readiness`. Rollback anchor `pre-v5.15.5.F.4d.1.D.1`.
- Workspace HEAD `a88debf` (or later self-referential handoff refreshes).
- Engine working tree: clean of code (untracked `.E.2`-staged `DOCS/*.md` skeletons only). Build 3239/0.

## 3. Findings sidecar + per-finding fix-design status

**Audit pickup (read first, self-contained):** `plan_checks/E.0-audit-reports/pre-implementation-findings/_SESSION-CONTEXT.md` (+ `MASTER-BACKLOG.md`). ~141 findings, all about the CURRENT engine (exist regardless of `.E`).

**Sidecar → ship routing** (already `fix_ship`-mapped; wire `findings_sidecar:` into each plan frontmatter as step 2 below):

| Sidecar | Count | Lands at |
|---|---|---|
| `E.1-findings.md` | 30 | `.E.1` |
| `E.2-findings.md` | 14 | `.E.2` |
| `E.3-findings.md` | 10 | `.E.3` |
| `E.5-findings.md` | 6 | `.E.5` |
| `E.6-findings.md` | 2 | `.E.6` |
| `PRE-PAPER-TEST-findings.md` | 55 | correctness gate (paper-test milestone) |
| `BACKLOG-STANDALONE-findings.md` | 24 | TECH_DEBT / `.F` |
| `codebase-wide-ml-bandit-audit.md` | F1-F22 | per routing |

**4 RED sections (do not lose any — not just the CRITICALs):** concurrency (`conc-5` CRITICAL), live-binance (`live-bc-1` CRITICAL), regime-features (RED, HIGH, no CRITICAL), fixedpoint-mem (RED, 23 findings incl 9 HIGH — `USE_NATIVE_128` / `FPN_Sqrt` cluster).

**2 provisional CRITICALs:**
- `conc-5` — multi-producer race on per-core `submit_queue` SPSC (drainer + per-core slow thread both push). **LIKELY closed by `.E.1` drainer absorption** (slow-path becomes sole producer) — but that's an INFERENCE; **VERIFY at `.E.1` design + tsan; do NOT skip the fix on assumption.**
- `live-bc-1` — user-data WS at Binance GLOBAL while orders/listen-key use Binance US (endpoint split breaks live fills). Fix in CURRENT code (`.E` won't auto-fix). Confirm via config trace.

**Discipline:** runtime-confirm the 49 provisionals on a **disposable throwaway clone** BEFORE writing any test/CI (never a test for an unconfirmed bug). Split the 122 test-gap + 97 ci-tool findings into PERSIST (behavior `.E` keeps — `FPN_Sqrt` determinism, fee/accounting, regime-given-input, parser byte-equivalence → characterization tests BEFORE `.E`) vs CHANGES-BY-DESIGN (e.g. `conc-5` drainer-SPSC → new-invariant test AS PART of `.E.1`).

## 4. Cross-ship invariants this ship must preserve

From `subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md` + decision-log-v2 (D-1..D-66). Key: hot-path purity (H7/H8); train-serve parity (M5); stamp/wire byte-preservation (H9); the Cluster/Node/Deployment hierarchy (D-15); aggregator-as-single-writer (D-54); per-node economic isolation (D-2 sub-accounts). `.E.0`'s job is to verify each `.E` plan respects these + the dependency DAG.

## 5. Required reading (ship surface)

- **`.E.0` plan body** (the one being expanded): `subplans/2026-05-28-v5.15.5.F.4d.1.E.0-precoding-plan-audit-verification.md`
- **Audit pickup:** `_SESSION-CONTEXT.md` (+ MASTER-BACKLOG + the 7 sidecars)
- **`.E` context:** `E-MASTER-REFERENCE.md`, `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-1..D-66), `subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md`, the `.E.1`-`.E.X` plan bodies
- **`.D.1` outcome:** `postmortems/2026-05-28-v5.15.5.F.4d.1.D.1-postmortem.md` + `decision-logs/v5.15.5.F.4d.1.D.1-doc-sweep.md`
- **The escalation:** TECH_DEBT-142 (`DOCS/tech-debt/open.md`) + B19 pillar (`implementation-layer-blindspot-taxonomy.md`)
- Baseline: CLAUDE.md / CLAUDE.local.md (de-chunked 2026-05-28; 28.5k) / MEMORY.md (auto)

## 6. Pre-coding gate + the `.E.0` expansion (DO THIS FIRST)

**Step 1 — EXPAND the `.E.0` plan body** (it is currently a pure-audit gate; the spec to add lives in `handoffs/2026-05-28-v5.15.5.F.4d.1.D.1-doc-sweep-checkpoint-handoff.md` §E "CONCRETE `.E.0` UPDATE SPEC"). Add: (a) a "Findings ingestion + pre-`.E` safety net" phase; (b) per-`.E`-ship findings audit (audit each plan vs its sidecar + lay per-finding fix-designs IN that plan); (c) per-plan `findings_sidecar:` frontmatter backlink + D-52 cite; (d) acceptance-criteria additions ("all PERSIST characterization tests written + green" / "CI invariant checks live" / "49 provisionals runtime-confirmed or accepted/dismissed").

**Step 2 — per-`.E`-ship verification** (the thing we're now ready to do): audit each `.E.X` plan body against its findings sidecar; lay a concrete per-finding fix-design in that plan; record which ship lands it.

**Step 3 — codify** (per `_SESSION-CONTEXT` §7): NEW Classes start at **37** (36 = `.D.1`'s) + CI-tool/test specs + NEW DESIGN_SPECS (`/doc-create`) + TECH_DEBT/PARITY rows + E-MASTER index rows. Run `/capture-audit` after.

**Step 4 — runtime-confirm** the 2 CRITICALs + fixedpoint cluster on a disposable clone.

⚠️ **Feeds `.E.1`'s verification — TECH_DEBT-142 (HIGH):** the `.D.1` prose-token doc-rename tool is **structurally unsafe for `.E.1`'s ~5,000-site CODE rename** (over-renames citations / current-state / transition-context — proven at `.D.1` Phase F). `.E.1`'s plan must design the Core→Node rename around **symbol/AST-aware** tooling, NOT prose-token substitution. Per B19 pillar + Class 36.

Fire `/precoding-audit-gate` HIGH-RISK against the expanded `.E.0` + each `.E` plan; consult before coding.

## 7. Verification gate (`.E.0` GREEN criteria)

- Each `.E.X` plan body has `findings_sidecar:` + per-finding fix-designs (in its D-52 section).
- All PERSIST characterization tests written + green on the current engine; CI invariant checks (H1-H20 etc.) live.
- 49 provisionals runtime-confirmed (or explicitly accepted/dismissed); both CRITICALs resolved or `.E.1`-design-verified.
- NEW Classes 37+ / DESIGN_SPECS / TECH_DEBT codified; `/capture-audit` clean.
- Tie-back: `.E.0` GREEN ⇒ the `.E` rework is gated behind a regression-proof safety net (DD-5 synthesis satisfied).

---

## TaskList (recreate on pickup)

| # | Task | Status |
|---|---|---|
| #16 | Audit codification post-`.D.1` (Classes 37+, findings_sidecar wiring, CI/test specs, `.E.0` expansion) | in_progress (this window) |
| #17 | Runtime-confirm 49 provisionals incl. 2 CRITICALs on disposable clone | pending |
| #18 | Codify 2 session memories → DESIGN_SPECS + file-size-split amend | pending |
| #19 | PRE-PAPER-TEST correctness mini-ship (55 findings gate paper-test) | pending (after runtime-confirm) |
| #20 | Evaluate 5 audit-recommended skills (net-new vs extend by yield) | pending |
| #21 | Per-node CPU poll-mode toggle (busy-poll/pinned vs yield/uncapped) — extends D-33 | pending (→ `.E.1`/`.E.2`) |
| #22 | Reusable `.E`-ship handoff template | DONE (`_TEMPLATE-e-ship-planning-handoff.md`; this handoff is its first instance) |
| #23 | Reformat CLAUDE.md + CLAUDE.local.md | CLAUDE.local.md de-chunk DONE (53.7k→28.5k); CLAUDE.md + architecture-restructure → `.E.1`/`.E.2` |
| #2-13 | `.E` pipeline (E.1 Foundation → … → E.X) | pending (gated on `.E.0` GREEN) |

## Pickup — first action

Run `/accept-handoff` on this doc, then **Step 1 (expand the `.E.0` plan body per `.D.1` handoff §E SPEC)** before per-plan verification. Decide scope/timing of the CLAUDE.md reformat (task #23) when convenient — it folds into `.E.1`/`.E.2`.

**End of `.E.0` handoff.**
