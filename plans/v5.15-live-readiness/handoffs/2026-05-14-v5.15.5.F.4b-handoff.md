# v5.15.5.F.4b handoff prompt — CfgFieldRegistry foundation + tt:: dispatch + KIND_DOUBLE/_PCT migration

**Created:** 2026-05-14
**Target ship:** `v5.15.5.F.4b` — first coding ship of the universal cfg field registry sprint
**Branch:** `feat/v5.15-live-readiness`
**Pre-tag rollback anchor:** create `pre-v5.15.5.F.4b` at Step 0 (no predecessor `.F.4*` engine tag exists yet — `.F.4a` was design specs only, committed in workspace `c1dd8f9` + this session's `73cb782`)
**Plan file:** `plans/v5.15-live-readiness/subplans/2026-05-13-v5.15.5.F.4b-foreach-cfg-field-registry-implementation.md` (374 lines, fully cold-pickup-ready as of 2026-05-13/14)
**Sprint MASTER:** `plans/v5.15-live-readiness/MASTER.md`
**Predecessor postmortem:** `plans/v5.15-live-readiness/postmortems/2026-05-13-v5.15.5-E-postmortem.md` (most recent close)
**Current engine version:** `5.15.5.E` (per Version.hpp); ship target bumps to `5.15.5.F.4b`

**Pattern matches detected (12):** this ship touches an unusually wide set of architectural patterns. Required reading list at Step 3 reflects this.

---

## Paste this prompt into a fresh Claude Code session to start `.F.4b`

```
I'm picking up v5.15.5.F.4b (CfgFieldRegistry foundation + tt:: dispatch + KIND_DOUBLE/_PCT migration + full descriptor design lock-in) for the v5.15-live-readiness sprint.

This is a fresh context window; do NOT trust any prior-session memory — verify everything against current code per CLAUDE.local.md feedback_compaction_degrades_treat_handoffs_as_hints.md memory rule.

## Step 0 — orient + verify state (BEFORE planning anything)

1. Run in parallel:
   - `cat /home/caramel/code/FoxML_Trader_v2/Version.hpp` — confirm "5.15.5.E" (or higher if a sub-ship has shipped since this handoff was written)
   - `cd /home/caramel/code/FoxML_Trader_v2 && git log --oneline -10` — confirm latest engine commit is `f72caef v5.15.5.F.3` or newer (.F.4* commits if a sub-ship has shipped since this handoff)
   - `cd /home/caramel/code/FoxML_Trader_v2 && git tag --sort=-creatordate | head -10` — confirm `v5.15.5.F.3` exists; verify no `v5.15.5.F.4b` tag yet
   - `cd /home/caramel/code/FoxML_Trader_v2 && git status` — confirm clean working tree
   - `cd /home/caramel/code/tick-trader-percore-workspace && git log --oneline -5` — confirm workspace latest is `73cb782 v5.15.5.F.4 planning complete: sketch expansions + new plans + cross-cutting docs` or newer

2. Read these in parallel (load context):
   - `/home/caramel/code/CLAUDE.md` (workspace-root project instructions)
   - `/home/caramel/code/FoxML_Trader_v2/CLAUDE.md` (engine repo project instructions; codified design philosophy items 1-30)
   - `/home/caramel/code/FoxML_Trader_v2/CLAUDE.local.md` (PRIVATE overlay — CONDENSED 2026-05-14 to a 191-line INDEX: rule one-liners + DESIGN_SPECS pointers + auto-write contracts + skill-load contracts. Deep-dive bodies live in DESIGN_SPECS / DOCS/RECURRING_BUG_PATTERNS.md / memory/. Pre-condense rollback anchor at workspace `CLAUDE.local.md.pre-condense-2026-05-14.backup` if reference needed.)
   - `/home/caramel/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md` (auto-memory index)
   - `/home/caramel/code/tick-trader-percore-workspace/DOCS/LANDMINES.md` (NEW 2026-05-14; operational pitfalls. Currently: XGBoost+libgomp pthread parallelism segfault landmine. Read before debugging segfaults / races / library quirks.)
   - `/home/caramel/code/FoxML_Trader_v2/plans/v5.15-live-readiness/MASTER.md` (sprint master plan)
   - `/home/caramel/code/FoxML_Trader_v2/plans/v5.15-live-readiness/subplans/2026-05-13-v5.15.5.F.4-universal-cfg-registry-sprint.md` (UMBRELLA — 9-sub-ship plan with verification gate template + scope-split decisions)
   - `/home/caramel/code/FoxML_Trader_v2/plans/v5.15-live-readiness/subplans/2026-05-13-v5.15.5.F.4b-foreach-cfg-field-registry-implementation.md` (THE plan for this ship)
   - `/home/caramel/code/FoxML_Trader_v2/plans/v5.15-live-readiness/postmortems/2026-05-13-v5.15.5-E-postmortem.md` (most-recent postmortem; latency-vs-cache + AoS time-series lessons)

3. Read BEFORE WRITING CODE (per CLAUDE.local.md required reading map for cfg / GUI / parser surfaces):
   - `/home/caramel/code/FoxML_Trader_v2/DOCS/STRATEGY_AND_CODING_RULES.md` (11 strict invariants — no malloc, no virtual, no mutex, branchless hot path, FPN-only accounting, etc.)
   - `/home/caramel/code/FoxML_Trader_v2/plans/_cross-cutting/2026-05-06-latency-path-discipline.md` (7 latency-path rules; cfg surface IS slow-path-adjacent via .F.4e ResolvedCoreCfg)
   - `/home/caramel/code/FoxML_Trader_v2/DOCS/LATENCY_OPTIMIZATION_AUDIT.md` (Gemini sweep; touch cfg + parser sections)

## Step 1 — re-verify plan against current code (HEAD)

The plan was drafted 2026-05-13 + amended 2026-05-14 with the FULL descriptor design (categorical columns + lives_in_struct + 128-byte budget + uint16_t metadata_flags). It's cold-pickup-ready but the codebase may have moved since.

Run `/readiness plans/v5.15-live-readiness/subplans/2026-05-13-v5.15.5.F.4b-foreach-cfg-field-registry-implementation.md` and address every GAP / stale-reference finding. Common stale claims to verify:
- ControllerConfig.hpp field declarations match what the plan assumes (~213 fields; templated `template <unsigned F> struct ControllerConfig`)
- No conflicting `FOREACH_CFG_FIELD` macro already exists in the codebase
- `Strategies/StrategyRegistry.hpp` (or wherever FOREACH_STRATEGY lives) struct + signature — `.F.4b` extends it with category-mask column
- BacktestCfg / ControllerCfg / SecretsCfg / TrainingCfg structs exist (referenced via `lives_in_struct` enum even though `.F.4b` only populates STRUCT_CFG rows)
- 5 binaries build cleanly at HEAD: `./build.sh test gui suite tsan asan`
- `controller_test` baseline test count (plan expects ≥1822; verify)

## Step 2 — pre-coding audit gate (multi-skill parallel — SUGGESTED, NOT auto-triggered)

Per `DESIGN_SPECS/audit-methodologies/audit-driven-pre-coding-gate.md`, this ship MEETS the trigger criteria (closes 6+ recurring bug class structurally; touches wire format via STAMP_BOUND derived filter; adds ~40+ cfg fields to a new registry; refactors functions used at multiple call sites; picks up work from previous compaction-degraded session).

**Suggested pre-coding audits** (operator decides whether to fire per CLAUDE.local.md "going-forward rule: suggest mid-sprint audits when work impacts downstream plans" — present the suggestion + 1-sentence rationale per audit + wait for operator response):

Spawn these IN PARALLEL via Agent tool with Explore subagents (Layer 2; each subagent reads its own SKILL.md spec + does NOT spawn nested subagents).

**NOTE 2026-05-14:** the audit skills now have a Stage 0 DESIGN_SPECS preload step (added alongside CLAUDE.local.md condense). Each subagent reads the matching DESIGN_SPECS bodies into context BEFORE walking checks — so findings cite specific pattern rules by filename, not just by name. The 12 patterns for `.F.4b` (Step 3 below) should all be warm in the audit subagents' contexts when they run.

1. `/parity-check` — Stage 0 preloads wire-format-byte-preservation + autopopulate-pattern + x-macro-registry + pre-post-cfg-split + struct-padding + avx512-byte-determinism + cfg-flag-eligibility + Classes 18-21. Focus: train↔serve identity preserved across registry migration; stamp body unchanged by STAMP_BOUND derived filter; HMAC chain byte-identical for legacy stamps via Layer 5b CI hash test
2. `/trace-deps` — Stage 0 preloads structural-fix-preferred-decision-framework + categorical-tag-applicability + x-macro-registry + RECURRING_BUG_PATTERNS Classes 13-21. Focus: ControllerConfig.hpp field offsets + cfg accessor call sites (~6 site classes including parser / panel / save / per-core override / drift check / cfg.example); dispatch routing via lives_in_struct
3. `/readiness` (FULL 28-check pass) — Stage 0 preloads DESIGN_SPECS by 12 plan-surface keyword categories (cfg-field / X-macro / bit-pack / K-state / stamp body / hot path / SIMD / sliding-window / mirror sites / per-core / snapshot / audit-driven-gate). Focus: cold-pickup completeness for the WHOLE sub-sprint (.F.4b through .F.4i + v5.15.6 stub); new cfg field eligibility for ~40 KIND_DOUBLE/_PCT entries via cfg-flag-eligibility-criteria.md cohort audit; X-macro variant selection (5-col → 12-col tuple per descriptor)
4. `/merge-scan` — Focus: REUSE opportunities (~30 FOREACH_ registries already exist in codebase; verify CfgFieldRegistry doesn't duplicate; verify metadata_flags bitmap overlap with existing FailureModeRegistry / EzooInitFlagRegistry / etc.); mirror-incomplete patterns
5. `/dod-audit` — already loads DESIGN_SPECS/*.md catalog dynamically (registry-driven). Focus: DESIGN_SPECS pattern application; cache-layout (128-byte CfgFieldDescriptor; FPN<F>=24B implications); branchless mask dispatch for categorical filtering; bit-packing inside metadata_flags

After all reports return, synthesize convergent findings to `plans/plan_checks/2026-05-14-v5.15.5.F.4b-fresh-audits-synthesis.md`. THEN consult Caramel before coding. Do NOT auto-proceed even if findings look clearly addressable (per CLAUDE.local.md feedback_consult_on_audit_findings memory).

## Step 3 — design check against pattern library (12 patterns matched)

Required reading (DESIGN_SPECS catalog in workspace `/home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/`):
- `README.md` (full 26-pattern catalog + "I need to..." quick discovery)

For this ship specifically (in order of relevance):

1. **`universal-cfg-field-registry-pattern.md`** — THE PATTERN being instantiated. Read first. Describes CfgFieldDescriptor + 12-column tuple + tt:: dispatch + AUTOPOPULATE family + STAMP_BOUND derived filter. Categorical applicability columns + lives_in_struct discriminator + 128-byte cache budget decision.
2. **`categorical-tag-applicability-pattern.md`** — NEW pattern (2026-05-14); describes the categorical-tag system used by `.F.4b`'s descriptor design. Vocabulary discipline rules (5) + 10 anti-patterns + cross-file cfg unification via lives_in_struct + CI consistency tests.
3. **`bitmap-overflow-protection-discipline.md`** — NEW pattern (2026-05-14); applies to category enum overflow guards (StrategyCategory uint32_t + OpModeCategory uint16_t + metadata_flags uint16_t) AND to the existing bitmap inventory in codebase (covered in audit at `.F.4h`).
4. **`x-macro-registry-with-presence-dispatch.md`** — base X-macro registry pattern + Y3 dispatch via token-paste. CfgFieldRegistry uses this. Recently amended (2026-05-14) with "Universal cfg field registry as reference impl" + "Derived filter sister registry pattern" sections.
5. **`registry-tuple-as-single-source-of-truth.md`** — 5-col → 12-col Option D tuple expansion. CfgFieldRegistry's 12-column tuple is the largest Option D application to date.
6. **`autopopulate-pattern-for-production-caller-class.md`** — AUTOPOPULATE companion macros for parser dispatch / save dispatch / cfg.example emission / GUI render walk.
7. **`heterogeneous-registry-pattern.md`** — SCOPE COLUMN dispatch pattern (Kind enum as scope axis); parent of CfgFieldRegistry.
8. **`wire-format-byte-preservation-discipline.md`** — Layer 5b derived-filter byte-order locking via CI hash test (NEW section 2026-05-14); applies to STAMP_BOUND_DERIVED canonical byte order preservation.
9. **`bitmap-flag-api.md`** — BITMAP_* macros for metadata_flags bitmap operations; CLAUDE.md item 20.
10. **`cfg-flag-eligibility-criteria.md`** — 5-criteria framework; cohort-audit rule applies when adding new boolean cfg fields (verify cohort applicability for ~40 initial KIND_DOUBLE/_PCT entries).
11. **`per-bit-per-core-override-pattern.md`** — PER_CORE_OK metadata bit auto-emits per-core override storage; pattern is THE primary mechanism closing per_core_gap drift class.
12. **`structural-fix-preferred-decision-framework.md`** — `.F.4b` IS the canonical structural-fix-preferred application (closes 6+ recurring drift classes via single registry + dispatcher).

Don't write code until the matched-pattern docs are read + integration plan articulated.

## Step 4 — design philosophy reminders (load-bearing rules from CLAUDE.local.md + memory + DESIGN_SPECS)

Key going-forward rules + memory entries to load:

- **Categorical applicability for new cfg fields (set 2026-05-14)** — declare `applies_to_*` columns; don't hardcode strategy/regime/op_mode names. Use specific capability tags (STRAT_CAT_USES_BANDIT) not instance names (STRATEGY_ML).
- **Bitmap overflow static_assert is mandatory (set 2026-05-14)** — every bitmap field paired with FOREACH_X registry needs `static_assert(COUNT <= sizeof(TYPE) * 8)` co-located.
- **Cross-file cfg surfaces use lives_in_struct (set 2026-05-14)** — ONE CfgFieldDescriptor + lives_in_struct discriminator; never parallel descriptors per cfg file.
- **Prefer structural fix > direct patch for recurring bug classes** — `.F.4b` IS the canonical application; closes 6+ drift classes structurally.
- **Defer is last-ditch, never effort-avoidance.** Implement the FULL descriptor design at `.F.4b` (categorical columns + lives_in_struct + uint widths + 128-byte budget). Don't strip to "minimum viable" — that'd retread the v5.9.5b production-caller class shape.
- **No MVP for plumbing/refactor work.** `.F.4b` is structural; ship the full documented design (per `feedback_no_mvp_for_plumbing_only_for_unknown_unknowns.md` memory).
- **Boundary-stable refactors preferred over wide cascades.** New descriptor type is the boundary; cfg field DECLARATIONS in ControllerConfig.hpp stay (option) OR are auto-generated from registry (stretch goal per `.F.4b` Deliverable B).
- **Hot path UNTOUCHED.** Hot path target ≤500ns p99. `.F.4b` is parser + GUI only; no hot/slow-path impact. ResolvedCoreCfg comes at `.F.4e` (different ship).
- **Branchless mask compute > switch on enum on slow path** (CLAUDE.md item 18; cfg metadata read at 60 Hz GUI is NOT slow-path; switch is fine here).
- **Reuse-audit before adding new code** (CLAUDE.md item 16). Scan ~30 existing FOREACH_ registries for reuse opportunities.
- **Latency-additions get tracked** (CLAUDE.md item 17). `.F.4b` HOT_PATH_CHANGELOG entry = NONE (boot + GUI only).
- **Bump Version.hpp on every ship.** Ship tag `v5.15.5.F.4b` → Version.hpp bumped to `5.15.5.F.4b` in same commit.
- **Don't use AskUserQuestion modal boxes.** Present options inline as text; Caramel wants full conversation scrollable.
- **Evaluate options on robustness + latency + design philosophy, NOT time.** Time is essentially never the deciding factor.
- **After pre-coding audits, ALWAYS consult before coding.** Present findings + list potential fixes + iterate with Caramel BEFORE coding. Do NOT auto-proceed even if findings look clearly addressable.
- **Address Caramel as Caramel / she / her** in conversation. Persisted docs can keep "operator" for general-collaborator audiences.
- **Don't measure structural work by LOC** (`feedback_dont_measure_structural_work_by_loc.md` memory — NEW 2026-05-14). `.F.4b` is registry + dispatcher infrastructure; LOC delta is incidental; classes-closed + future-work-becomes-mechanical is the metric.

## Step 5 — TECH_DEBT items in surface area (6 overlapping)

Filter `DOCS/TECH_DEBT.md` for entries overlapping the cfg surface; expect these to overlap:

- **TECH_DEBT-006** — Stamp-bound CFG dual registry (OPEN → CLOSED by `.F.4b` STAMP_BOUND derived filter). Address in this ship.
- **TECH_DEBT-050** (OPEN) — controller.cfg integration deferred to v5.15.6. Adjacent surface; do NOT absorb into `.F.4b` (scope cap). Verify `.F.4b`'s descriptor design accommodates by checking LivesInStruct enum has STRUCT_CONTROLLER_CFG value.
- **TECH_DEBT-051** (OPEN) — secrets.cfg integration deferred to v5.15.6.B. Same; verify STRUCT_SECRETS_CFG + IS_SECRET / LOG_VALUE_FORBIDDEN metadata flags exist in descriptor.
- **TECH_DEBT-052** (OPEN) — Training cfg integration deferred to v5.15.6.C. Same; verify STRUCT_TRAINING_CFG + KIND_RANGE_INT/_DOUBLE reserved (KIND enum has placeholder values 8/9).
- **TECH_DEBT-053** (OPEN) — Phase 2 cfg struct unification (merge cfg structs) deferred to v5.16+. Same surface; `.F.4b` keeps structs separate via lives_in_struct routing (Phase 1).
- **TECH_DEBT-054** (OPEN) — Regime + risk-mode + feature categorical rollout deferred to v5.16+. `.F.4b` populates strategy + op_mode dimensions only; regime/risk columns default to `_CAT_ALL`. Verify columns exist in descriptor.

Run `/readiness` Check 25 (TECH_DEBT scan) explicitly during Step 1 readiness.

## Step 6 — operator collaboration norms

- Address Caramel as Caramel / she / her in conversation. Not "operator" in chat.
- Don't use AskUserQuestion modal boxes. Present options inline as text.
- Evaluate options on robustness + latency + design philosophy, NOT time.
- When pre-coding checks complete, present findings + fixes + iterate BEFORE coding. Do NOT auto-proceed.
- Suggest mid-sprint audits when downstream sub-ships impact a new surface (e.g., before `.F.4e` slow-path-touch ship). Wait for greenlight; don't auto-trigger.
- Auto-write TECH_DEBT entries for any deferred items (auto-write contract per CLAUDE.local.md 2026-05-09).
- Auto-write PARITY_ISSUES entries for any new parity findings (auto-write contract per CLAUDE.local.md 2026-05-09).
- Auto-write FEATURE_LOOKUP.md entry if `.F.4b` lands an operator-visible feature (registry-driven Settings tab IS operator-visible).
- Auto-write decoupling-roadmap entry — `.F.4b` parser dispatch is GUI ↔ runtime interface touch; POSITIVE positioning for v6.0 viewer split.

## Step 7 — filesystem conventions

- Engine repo: `/home/caramel/code/FoxML_Trader_v2`
- Workspace: `/home/caramel/code/tick-trader-percore-workspace` (private; gitignored from engine)
- Plans: `workspace/plans/<sprint-dir>/{MASTER.md, subplans/, plan_checks/, postmortems/, handoffs/}` — symlinked from engine `plans/`
- DESIGN_SPECS catalog: `workspace/DESIGN_SPECS/` (26 patterns + README; NEW: categorical-tag-applicability-pattern.md + bitmap-overflow-protection-discipline.md from 2026-05-14)
- Skill outputs: `plans/plan_checks/<skill>-<YYYY-MM-DD>-<scope>.md`
- TECH_DEBT auto-write: `DOCS/TECH_DEBT.md` (PER-FILE SYMLINK to workspace — Edit tool REFUSES to write through symlinks; check `readlink -f path` first; edit via workspace direct path)
- PARITY_ISSUES auto-write: `DOCS/PARITY_ISSUES.md` (same — symlinked)
- HOT_PATH_CHANGELOG: `DOCS/HOT_PATH_CHANGELOG.md` (same — symlinked)
- RECURRING_BUG_PATTERNS: `DOCS/RECURRING_BUG_PATTERNS.md` (same — symlinked)
- FEATURE_LOOKUP.md: `workspace/FEATURE_LOOKUP.md` (workspace-native; no engine source)
- LANDMINES.md: `workspace/DOCS/LANDMINES.md` (workspace-native, NEW 2026-05-14; operational pitfalls; auto-write contract per condensed CLAUDE.local.md when a non-obvious pitfall costs >1h to debug)
- CLAUDE.local.md: engine path is the source of truth (NOT symlinked); CONDENSED 2026-05-14 to a 191-line INDEX pointing to DESIGN_SPECS / memory / SKILL.md deep-dives; `/sync-workspace` copies to `workspace/CLAUDE.local.md.backup`. Pre-condense rollback: `workspace/CLAUDE.local.md.pre-condense-2026-05-14.backup` (812-line original).
- **DOCS/ symlink editing convention** (per v5.11.43 migration; if Edit fails with "Refusing to write through symlink", the file IS a symlink — resolve via `ls -l` or `readlink -f` and edit the workspace direct path).

## Step 8 — sprint-close verification gate (this ship + carries to subsequent .F.4 ships)

Before declaring `v5.15.5.F.4b` shipped:

### Universal gates (per umbrella verification template)
- All 5 binaries build cleanly (`./build.sh test gui suite tsan asan`)
- `controller_test` passes (≥1822 + new tests for registry parity + categorical applicability + overflow guards)
- Cfg roundtrip byte-identical for migrated fields (parse `engine.cfg.example` → save → diff)
- `static_assert(sizeof(CfgFieldDescriptor) <= 128)` holds (2 cache lines budget)
- Bitmap overflow static_asserts present for any bitmap touched (StrategyCategory uint32_t / OpModeCategory uint16_t / metadata_flags uint16_t)
- HOT_PATH_CHANGELOG entry = NONE (parser + GUI only; no hot/slow-path impact)
- FEATURE_LOOKUP.md entry written (registry-driven Settings tab IS operator-visible)
- PARITY_ISSUES.md scan: no new parity issues
- TECH_DEBT.md: TECH_DEBT-006 (stamp-bound dual registry) flipped to CLOSED with commit hash
- Decoupling-roadmap entry written (parser dispatch = GUI ↔ runtime interface; POSITIVE breadcrumb)

### Categorical-tag-specific gates
- CI Test 1 (no orphan categories): every defined STRAT_CAT_* / OP_MODE_CAT_* has ≥1 instance declaring it
- CI Test 2 (no orphan cfg fields): every cfg row's applies_to_strategy_cat + applies_to_op_mode_cat is non-zero (`_CAT_ALL` if universal)
- CI Test 3 (instance self-consistency): STRAT_CAT_USES_THOMPSON requires STRAT_CAT_USES_BANDIT (capability dependency)
- Categorical filtering is GUI-only (parser + persist semantics unchanged) — verified via roundtrip test

### Wire-format-preserving-ship-specific gates (for STAMP_BOUND derived filter)
- Layer 5b canonical body snapshot hash test (synthetic-populated body via derived walk = `LOCKED_STAMP_BOUND_DERIVED_HASH_V5_15_5_F4` constant)
- Round-trip HMAC test against a real v(N-1) production stamp fixture

### Version + sync
- Version.hpp bumped to `5.15.5.F.4b` in the same commit as the ship tag
- Git tag `v5.15.5.F.4b`
- `/sync-workspace` after ship lands

## Closing reminder

`.F.4b` is the STRUCTURAL FOUNDATION of the v5.15.5.F.4 sprint — the descriptor design LOCKS HERE for the remaining 8 sub-ships (`.F.4c` through `.F.4i` + v5.15.6). Subsequent sub-ships add rows + populate masks; **no descriptor schema changes after `.F.4b`**.

Closes 6+ recurring drift classes structurally (parser_gap / panel_gap / persist_gap / per_core_gap / stamp_drift_gap / cfg.example_doc_gap + hardcoded-instance-gating class 19). This is the canonical structural-fix-preferred application (CLAUDE.md item 19).

If you find yourself writing complex plumbing, stop — check `DESIGN_SPECS/README.md` "I need to..." section. There's almost certainly a pattern that applies — and for this ship specifically, 12 patterns apply directly.

Good luck. Caramel will iterate with you on pre-coding audit findings before coding starts.
```

---

## Notes for future-Claude reading this handoff doc

- Prompt above is self-contained — paste as FIRST message in a fresh `claude code` session.
- Includes 9 steps + design philosophy reminders + filesystem conventions.
- Follow them in order; don't skip Step 1 (re-verification against current code).
- This ship's plan is already cold-pickup-ready (374 lines, fully expanded). Step 1 should be FAST — most readiness checks pass; focus on stale-claim verification (struct shapes, file:line refs) since plan was drafted 2026-05-13 + amended 2026-05-14.

---

## Quick links

- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- This ship's plan: `plans/v5.15-live-readiness/subplans/2026-05-13-v5.15.5.F.4b-foreach-cfg-field-registry-implementation.md`
- Umbrella: `plans/v5.15-live-readiness/subplans/2026-05-13-v5.15.5.F.4-universal-cfg-registry-sprint.md`
- Latest postmortem: `plans/v5.15-live-readiness/postmortems/2026-05-13-v5.15.5-E-postmortem.md`
- DESIGN_SPECS catalog: `tick-trader-percore-workspace/DESIGN_SPECS/README.md`
- NEW design specs (2026-05-14): `categorical-tag-applicability-pattern.md` + `bitmap-overflow-protection-discipline.md`
- LANDMINES.md (NEW 2026-05-14): `tick-trader-percore-workspace/DOCS/LANDMINES.md`
- CLAUDE.local.md condense (2026-05-14): now a 191-line index; rollback anchor at `workspace/CLAUDE.local.md.pre-condense-2026-05-14.backup`
- Latency rules: `plans/_cross-cutting/2026-05-06-latency-path-discipline.md`
- Coding invariants: `DOCS/STRATEGY_AND_CODING_RULES.md`
- Workspace commit reference: `7f16054 docs + skills: condense CLAUDE.local.md to index; preload DESIGN_SPECS in audit skills` (this handoff was updated against this state)
