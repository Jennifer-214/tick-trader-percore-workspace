---
type: handoff
status: superseded   # superseded 2026-07-16 by 2026-07-16-E.1.2.A-P6-95of174-giants-next-handoff.md (normalize+uplift COMPLETE; 95/174)
ship_tag: E.1.2.A
plan_type: refactor   # in-code documentation infrastructure — maintainability/framework deliverable, not a capital-path change
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: the locked [SCHEMA]_[v1.0] tag system IMPLEMENTED — toolchain done, corpus proven, the codebase converted, the plugin consuming it ("code becomes its own IDE")
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-07-15-E.1.2.A-toolchain-done-conversion-sprint-open-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md, DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md, DESIGN_SPECS/doc-disciplines/format-input-space-taxonomy.md]
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md, DOCS/CODE_TAG_TEMPLATES.hpp, plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md]
deletion_scope: none
coding_status: phase-6-batch-20-checkpoint   # 87/174 converted; NEXT SESSION = normalize+verify pass over the converted set FIRST, then batch 21
---

# E.1.2.A handoff — **P6 checkpoint @ batch 20 (87/174 converted; D-354/D-355) · NEXT = cosmetic-normalize + error-identification pass, THEN batch 21**

**Created:** 2026-07-16 (close of the batch-1..20 conversion mega-session)
**Branch:** feat/v5.15-live-readiness
**Engine HEAD:** `07c388e` (59 sprint commits since the pre-sprint anchor `d5f8abd`; tree clean except `M Strategies/RegimeDetector.hpp` — Caramel's untouchable pilot WIP — + untracked `build_clangd/`, `resume`, `tmpo0ujx6rw/`)
**Workspace HEAD:** at or after `a9423ae` (this close's sync commit lands after this doc)

**WHY this close (operator-directed):** Caramel called the checkpoint mid-sprint — *"should we close out, handoff, and then attempting identifying errors like this and fixing in another session, since compaction may be degrading otput?"* — after spotting stacked `//======` bars at file tails. Those bars are HER original end-of-file decoration (proven pre-existing via git history), but the worry is legitimate: a long compacted session converting files mechanically is exactly where insertion artifacts slip in. So the NEXT session's FIRST job is a fresh-eyes verification/normalize pass over the converted set — BEFORE converting anything new.

---

## TaskList state at handoff write (recreate verbatim at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | in_progress | E.1.2.A umbrella — in-code documentation system (P1-P4 done; P5 operator; P6 in progress) |
| #2 | completed | Complete schema against the taxonomy (phase 1 — LOCKED v1.0) |
| #3 | in_progress | Phase 4/5 residual — PLUGIN-side toolchain items (branchtag swap, keybind, facts.lua wiring to the core) — operator's session; seam ready. NOTE: `<leader>dd` Layout fix LANDED this session (plugin `0b3fa8d`) — Caramel must RESTART nvim to pick up `template_args` |
| #4 | completed | Phase 2 — propagate [SCHEMA]_[v1.0] into templates + 3-surface alignment |
| #5 | in_progress | Phase 6 — roll out the tag system (87/174 converted; normalize+verify pass next, then batch 21) |

---

## Sprint state: what's converted (mechanical census at close, engine `07c388e`)

| Dir | Converted / total | Remaining |
|---|---|---|
| CoreFrameworks (incl. EngineSharded/) | **53/53 COMPLETE** | — |
| Strategies | **10/10 COMPLETE** (incl. her pilot RegimeDetector) | (untracked `Strategies/private/EmaCross.hpp` excluded from census) |
| ML_Headers | 24/35 | the 11 heavies (batch-21 list below) |
| MemHeaders | 0/24 | all |
| DataStream | 0/15 | all |
| Backtest | 0/11 | all |
| GUI | 0/12 | all |
| tests | 0/12 | all (dir-symlink: census needs `find -L`) |
| FixedPoint | 0/1 | FixedPoint.hpp |
| **TOTAL** | **87/174** | 86 tracked + 1 untracked |

**Batch-21 queue (ML_Headers heavies, ascending size):** RollingStats 458 · BanditAlgorithmRegistry 465 · FlowFeatures 479 · FeatureStandardizer 543 · BanditLearning 766 · FeatureRegistry 786 · StampBoundModelConstRegistry 803 · RidgeBlender 816 · ConfidenceScore 1161 · ModelInference 2007 · NodeModelZoo 3131. Then MemHeaders → DataStream → Backtest → GUI → tests → FixedPoint.

**Whole-sprint integrity (triple-proven on operator challenge, D-355):** comment-stripped diff of ALL 85 sprint-touched files vs anchor `d5f8abd` = **0 code deltas**; per-file multiset check over every non-comment ± diff line = **0 real code differences**; suite **3697/0 on every batch** (incl. replay-determinism memcmp tests). The CODE is untouched — every open question below is COMMENT-layer only.

---

## ⚠ NEXT SESSION — Step 1 is the NORMALIZE + ERROR-IDENTIFICATION pass (not conversion)

**GOLD STANDARD (operator directive at close): `CoreFrameworks/ExecutionCore.hpp` and the other early CoreFrameworks conversions are what every converted file should LOOK like** — "execution core and other early ones should be what we try to get them to look like." ExecutionCore is the D-353 first conversion, operator-reviewed, and carries ZERO detector flags — when normalizing any file, diff its visual shape (bar tiers, block spacing, heading demotes) against ExecutionCore's, not against your own judgment.

Fresh-eyes sweep over all 87 converted files. Three artifact classes to find/fix (all comments-only; lossless gate still applies to every edit):

1. **Mismatched demote bar pairs (MY insertion artifacts — fix these).** A plain-text heading sandwiched between bars of DIFFERENT widths (thin `//----` vs wide `//====`). **VERIFIED list (mechanical scan at close, all 7 confirmed sprint-introduced vs anchor `d5f8abd` — NOT her originals):**
   - `CoreFrameworks/OrderGates.hpp:107` — "// no more packing/unpacking - compare FPN_Binary fields…"
   - `CoreFrameworks/Portfolio.hpp:42` — "// Im not sure how many positions i really want to track…"
   - `CoreFrameworks/Portfolio.hpp:292` — "// similar to the pool allocator, will need more work…"
   - `ML_Headers/BuildFlags.hpp:11` — "// Build flags fingerprint (v5.9.5h Phase 10 / v5.10…"
   - `ML_Headers/GateControlNetwork.hpp:90` — under "// BACKWARD PASS" (:88) — ("GCN" in earlier notes = THIS file)
   - `ML_Headers/ROR_regressor.hpp:47` — "// zeroes everything out, call once at startup"
   - `Strategies/MeanReversion.hpp:92` — "// INIT"

   (Line numbers valid at engine `07c388e`; anchor by the quoted heading text if lines shift.) The compaction-era claim that MLStrategy INIT/ADAPT + LinearRegressionSimple carry mismatches was CHECKED and is FALSE — those pairs are matched thin/thin; do not "fix" them. Detector (re-run to confirm zero after fixing; it only catches the bar/heading/bar sandwich shape — the eyeball sweep below still applies for other shapes):
   ```python
   import subprocess, re
   conv = subprocess.run(["grep","-rl","^// \\[FILE\\]_\\[","CoreFrameworks","Strategies","ML_Headers","MemHeaders","DataStream","Backtest","GUI","FixedPoint"],capture_output=True,text=True).stdout.split()
   bar = re.compile(r'^\s*//\s*([=\-]{8,})\s*$')
   for f in sorted(conv):
       lines = open(f).read().split("\n")
       for i in range(1, len(lines)-1):
           m1, m2 = bar.match(lines[i-1]), bar.match(lines[i+1])
           mid = lines[i].strip()
           if m1 and m2 and mid.startswith("//") and not bar.match(lines[i]) \
              and not re.match(r'^//\s*\[', mid) and mid != "//" \
              and m1.group(1)[0] != m2.group(1)[0]:
               print(f"{f}:{i+1}  {mid[:60]}")
   ```
2. **Trailing decorative bar piles at file tails (HER original decoration — HER call).** Stacked `//======` bars now sitting directly against `[END_*]` closers (e.g. GCN tail). Pre-existing, preserved per preserve-voice. Present the collapse option to Caramel; do NOT bulk-delete without her go.
3. **Any OTHER insertion artifact** — stray `[END-of-...]` fragments (the MeanReversion class, all four known ones already removed), orphan bars, double-blank runs created by block insertion, closer-name mismatches the validator can't see (it checks grammar, not aesthetics).
4. **Unrelocated unit-WHY banners (operator-caught at close — THE BIG ONE; this is most of the normalize pass).** The schema puts a unit's WHY-banner INSIDE the block as a below-code `[COMMENT]` (verbatim, preserve-voice). Much of the sprint instead wrapped code+banner wholesale, leaving banners in TWO wrong placements — **mechanical scan at engine `07c388e`:**
   - **(a) Stranded ABOVE the `//======` opener, outside the block: 12 banners in 8 files** — `CoreFrameworks/EngineCommon.hpp` ×5 (114 lines — the worst; where the operator spotted it) · `ML_Headers/VolScaler.hpp` · `ML_Headers/EzooInitFlagRegistry.hpp` · `CoreFrameworks/CfgFieldRegistry.hpp` · `CoreFrameworks/GateParameters.hpp` · `ML_Headers/BarrierBlendModeRegistry.hpp` · `CoreFrameworks/ShardedSnapshot.hpp` · `ML_Headers/PerArmFlagRegistry.hpp` ×1 each.
   - **(b) Wrapped INSIDE the `[CODE]` fence at unit top (never extracted): ~224 candidate runs across 55/87 files** (column-0 comment runs ≥2 lines preceding column-0 code inside a fence). Worst: ControllerConfig ×26 · ControllerEventLoop ×21 · OrderManager ×12 · Reconcile ×10 · ThompsonBandit ×10 · CfgFieldRegistry ×9 · OrderEventLog ×9 · PortfolioController ×9 (re-run the detector below to regenerate the full ranked table with line numbers).
   - **⚠ (b) is an UPPER BOUND needing a per-unit judgment split:** a flagged run is EITHER a unit-WHY banner (→ RELOCATE verbatim into that unit's below-code `[COMMENT]`; numbering + kept provenance lines stay as written) OR a legitimate code-local comment — X-macro row-group headers, field/sub-group comments, step comments — which **STAY inline per D-326**. Registry files (CfgDriftCheckRegistry, PerArmFlagRegistry, bandit_dispatch_table…) are mostly the latter. When unsure, compare against how `ExecutionCore.hpp` treats the equivalent shape (the gold standard).
   - **60/87 files currently have ZERO `[COMMENT]` blocks.** That stat alone is NOT a defect list — a unit whose original code had no WHY-banner correctly has no `[COMMENT]` (never fabricate commentary); it's the banner-present-but-unrelocated cases above that get fixed.
   - Detector (both placements; re-run to zero on (a), and to judged-clean on (b)):
   ```python
   import subprocess, re
   conv = subprocess.run(["grep","-rl","^// \\[FILE\\]_\\[","CoreFrameworks","Strategies","ML_Headers","MemHeaders","DataStream","Backtest","GUI","FixedPoint"],capture_output=True,text=True).stdout.split()
   bar = re.compile(r'^\s*//[=\-]{8,}\s*$'); tag = re.compile(r'^\s*//\s*\[')
   opener = re.compile(r'^\s*//\s*\[(FUNCTION|STRUCT|REGISTRY|ENUM)\]_\[')
   codeish = re.compile(r'^(template|inline|struct|static|constexpr|typedef|using|#define|#include|extern|void|int|uint|bool|Money|FPN|enum|union|alignas|class )')
   for f in sorted(conv):
       lines = open(f).read().split("\n")
       for i in range(1, len(lines)-1):   # (a) prose stranded above an opener bar
           if bar.match(lines[i]) and i+1 < len(lines) and opener.match(lines[i+1]):
               j = i-1
               while j >= 0 and not lines[j].strip(): j -= 1
               run = 0
               while j >= 0 and lines[j].strip().startswith("//") and not bar.match(lines[j]) and not tag.match(lines[j]):
                   run += 1; j -= 1
               if run >= 2: print(f"(a) {f}:{i+1-run}  run={run}")
       in_code = False; i = 0                       # (b) banner runs inside [CODE]
       cl = []
       for l in lines:
           if re.match(r'^\s*//\s*\[CODE\]', l): in_code = True; continue
           if re.match(r'^\s*//\s*\[END_CODE\]', l): in_code = False; continue
           if in_code: cl.append(l)
       while i < len(cl):
           if cl[i].startswith("//") and not bar.match(cl[i]) and not tag.match(cl[i]):
               j = i
               while j < len(cl) and cl[j].startswith("//") and not bar.match(cl[j]) and not tag.match(cl[j]): j += 1
               k = j
               while k < len(cl) and not cl[k].strip(): k += 1
               if j-i >= 2 and k < len(cl) and codeish.match(cl[k]): print(f"(b) {f}  run={j-i}  {cl[i][:60]}")
               i = j
           else: i += 1
   ```
5. **Comment-content completeness audit vs anchor (belt-and-braces).** The lossless gate proved CODE identical; it never checked that every original COMMENT survived (moved or in place). Diff the normalized comment-text multiset of each converted file vs `d5f8abd`, allowing only (a) new tag/bar lines and (b) the Refinement-A drops enumerated in that file's conversion commit message. Anything else missing = silently dropped voice → restore it.

**Sweep mechanics suggestion:** grep-driven candidates (`rg -n "^//={10,}" -A1` piles; thin/wide bar adjacency patterns) + spot-read every flagged site; every fix re-runs the per-file lossless gate + validator; one `docs(E.1.2.A-P6.norm):` commit series. Suite once at end (comments can't change codegen, but the gate is the gate).

---

## THE CONVERSION RECIPE (unchanged — proven over 87 files; carry forward verbatim)

Per file: **read WHOLE file → rot-check comments → convert → verify chain → commit that file ONLY.**

1. `[FILE]` block at top; per-unit blocks; **code byte-verbatim inside `[CODE]`**; LIGHT units ([MACRO]/[TEST]/[ASSERT]) = thin bars, NO [CODE], NO closer.
2. Unit-WHY banners → below-code `[COMMENT]` verbatim (preserve-voice); step/field comments STAY inline (D-326); `=== X ===` in-body headers → `[SECTION]` bars (D-329).
3. **Refinement A:** present-tense line-digit refs (file.hpp:NNN, "at line N", stale "CLAUDE.md item N") → de-digited to symbol/positional form; dated/SHA-anchored provenance KEEPS as history. DERIVED-class stale facts DROPPED, enumerated in the commit.
4. `[TAG]` from vocab (reuse-renames; never `[WIRE_FORMAT]`/`[REGISTRY]` as TAG values); `[REFERENCE]` ids must resolve; struct `[DERIVED]` via `check_cache_layout.py --fix` (emitter can't-probe → bare `[DERIVED]` gap-note per D-327 — existing instances: PortfolioController, RewardTracker).
5. **Verify chain (every file):** (a) lossless comment-stripped diff vs HEAD = 0 → (b) `check_code_tag_blocks.py --paths` → (c) cache-layout `--fix` then gate → (d) `./build.sh test` 3697/0 → (e) full-tree validator = ONLY the 2 accept-known RegimeDetector reds. The lossless gate, inline (also usable batch-wide by diffing vs anchor `d5f8abd` instead of HEAD):
   ```python
   import subprocess, difflib
   F = "<FILE>"
   orig = subprocess.run(["git","show",f"HEAD:{F}"],capture_output=True,text=True).stdout
   new = open(F).read()
   cl = lambda t:[l.split("//")[0].rstrip() for l in t.split("\n") if l.split("//")[0].strip()]
   d = [x for x in difflib.unified_diff(cl(orig),cl(new),lineterm="",n=0) if x[0] in "+-" and x[:3] not in ("+++","---")]
   print("LOSSLESS:", "PASS" if not d else f"FAIL {len(d)}"); [print(" ",x[:140]) for x in d[:10]]
   ```
6. **Hard-learned session norms:** read-exact-bytes-before-replace (assert-guarded `rep()`; heading forms VARY — GCN shares one bar across two headings, MlCfg uses wide dash bars); NEVER pipe commit output through `head` (SIGPIPE kills hooks); `rg -n` not `rg -rn`; tooltip/string-literal content is CODE BYTES — never edit (accepted rot documented in commit instead; existing instance: CfgFieldRegistry tooltips).

---

## What ELSE landed this session (beyond the 87 conversions)

- **D-354 — Check-10 exemptions structurally re-keyed:** line-anchored `CHECK_10_SECTION_D_EXEMPTIONS` → in-code `KEEP-AS-GLOBAL` comment markers (window 8, comment-carried) in `tools/check_per_node_registry_integrity.py`. Conversions no longer break the integrity gate on line shift. `rg KEEP-AS-GLOBAL` = live exemption inventory.
- **fox-symdeps `<leader>dd` Layout fix (plugin `0b3fa8d`):** `template_args` canonical-instantiation substitution (`ExecutionCore<F>` → `<64>`) + namespace requalify via clang did-you-mean retry; HUD `@ F=64`; `<leader>da` asserts compile. Her `~/.config/nvim/init.lua` got `template_args = { F = "64" }`. **She must restart nvim.** Plugin suite 38/38.
- **~60 rot catches** across batches (dead D-294 consumer enumeration; 4 EngineCommon anchors shifted by the sprint's own P6.46; last "CLAUDE.md item N" old-numbering cites retired; "line ~3274" in a 2475-line file; StampHelper's six-pack).

## Accept-known open items (homed, not lost)

- **RegimeDetector `[STATE]` :543 / `[INIT]` :627** — the ONLY full-tree validator reds; Caramel's uncommitted pilot WIP; reconcile WITH HER on pilot-resume. **NEVER touch or git-add this file.**
- **`Strategies/private/EmaCross.hpp` (untracked, gitignored)** carries a 3-line `KEEP-AS-GLOBAL` comment added this session above its fee-rate fallback ternary (needed by the D-354 marker-keyed Check 10; comment-only; per D-354). It lives ONLY in the working tree — do not delete the file assuming it's a stray.
- **TECH_DEBT-119 looks already-done-but-open** — EngineCommon.hpp IS the extraction it asks for. Needs HER confirmation to close; do not close by fiat.
- **Ledger line-ref rot class** — e.g. TECH_DEBT-097 cites `StampHelper.hpp:156` (pre-conversion line). Workspace-side sweep, future session (same Refinement-A class, ledger surface).
- **Plugin `TAG-INTEGRATION.md`** stale pre-fork grammar (P5, operator-owned) · **v1.1 vocab-alias candidates** `[WIRE_FORMAT]`/`[REGISTRY]` (D-348) · **validator cutover to foxtag core LAST** (D-349 contract) — all carried from predecessor.

## Capture-completeness (this session)

- **Decisions → log:** D-354 (Check-10 re-key) + D-355 (checkpoint + integrity proof + plugin fix + next-session charter), both with sentinels. No un-logged decisions.
- **Findings → ledger/homed:** the mismatched-bar-pair artifact class + tail bar-pile question → HOMED in this handoff's normalize-pass charter (next session's Step 1, operator-directed — not a TECH_DEBT because it closes next session); TECH_DEBT-119 stale-open question → surfaced in Accept-known (needs HER confirm, not closed by fiat); ledger line-ref rot class (TD-097 et al) → Accept-known carry-forward (workspace-side sweep, Refinement-A class); emitter can't-probe gaps (PortfolioController, RewardTracker) → bare `[DERIVED]` gap-notes IN-FILE per D-327 (self-homing); CfgFieldRegistry tooltip string-literal rot → accepted + documented in its conversion commit (string literals are code bytes). **No NEW ledger entries needed** — every finding is fixed-now, in-file-homed, or charter-homed above.
- **No NEW memory/skill files** — session was mechanical execution of the proven recipe; the two structural changes are D-354/D-355 + this handoff.
- **Verified at close:** `check_session_docs.sh` all HARD checks green except the 2 accept-known RegimeDetector reds (operator-accepted); suite 3697/0 at engine HEAD.

---

## Paste this prompt into a fresh Claude Code session to continue

```
I'm continuing E.1.2.A phase 6 — the [SCHEMA]_[v1.0] conversion sprint, picking up at the
batch-20 checkpoint (87/174 converted).

## Step 0 — orient
- Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-16-E.1.2.A-P6-batch20-checkpoint-normalize-pass-next-handoff.md
- Read the handoff's NORMALIZE-PASS section + CONVERSION RECIPE + DOCS/CODE_TAG_TEMPLATES.hpp.

## Step 1 — the normalize + error-identification pass (BEFORE any new conversion)
GOLD STANDARD = CoreFrameworks/ExecutionCore.hpp + the early CoreFrameworks conversions
(Caramel's directive) — normalize every file's visual shape toward THAT, not your own taste.
Sweep all 87 converted files for the three artifact classes in the handoff: (1) mismatched
demote bar pairs — FIX the VERIFIED 7-site list in the handoff (OrderGates:107, Portfolio:42
+ :292, BuildFlags:11, GateControlNetwork:90, ROR_regressor:47, MeanReversion:92 — all
mechanically confirmed sprint-introduced; re-run the handoff's inline detector after);
(2) trailing decorative bar piles at file tails — Caramel's original decoration, present
the option, HER call; (3) any other insertion artifact (eyeball sweep — the detector only
catches the sandwich shape); (4) THE BIG ONE — unrelocated unit-WHY banners: 12 stranded
above openers (8 files) + ~224 candidate runs wrapped inside [CODE] fences (55 files;
upper bound — split per-unit: WHY-banner -> relocate verbatim to below-code [COMMENT];
X-macro row-group/field/step comments STAY inline per D-326; ExecutionCore = the reference
for the split); (5) comment-content completeness audit vs anchor d5f8abd (only enumerated
Refinement-A drops may be missing). Every fix: lossless gate + validator; commit series
docs(E.1.2.A-P6.norm). Expect (4) to be MOST of the pass — plan the session around it.

## Step 2 — resume batch 21 (ML_Headers heavies, ascending)
RollingStats → BanditAlgorithmRegistry → FlowFeatures → FeatureStandardizer → BanditLearning
→ FeatureRegistry → StampBoundModelConstRegistry → RidgeBlender → ConfidenceScore →
ModelInference → NodeModelZoo. Follow the recipe EXACTLY (5-step verify chain per file).

## Norms
- Address Caramel as Caramel / she / her. No AskUserQuestion boxes.
- NEVER touch or git-add Strategies/RegimeDetector.hpp (her uncommitted pilot WIP).
- Commit locally; no remote exists — nothing to push.
- A conversion failing ANY verification step gets fixed or reverted — never committed red.
- Tooltip/string-literal content is CODE BYTES — never edit.
```

## Quick links
- Worked examples: `CoreFrameworks/ExecutionCore.hpp` (first, D-353) · any batch-20 ML_Headers file (latest shape)
- Copy-source: `DOCS/CODE_TAG_TEMPLATES.hpp` · Grammar SSoT: `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md`
- Toolchain: `tools/foxtag/README.md` · `tools/check_code_tag_blocks.py` · `tools/check_cache_layout.py`
- Decision log (D-306..D-355): `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`
- Integrity-proof method (if the worry recurs): comment-stripped diff vs `d5f8abd` per D-355
