---
type: handoff
status: active   # THE one live handoff. Supersedes 2026-07-18-E.1.2.A-c4-steps-1-2-shipped-next-corpus-fill-handoff.md (corpus fill DONE + RegimeDetector converted + HARD-zero flipped). ≤1-active enforced by check_handoff_active_singleton.py.
ship_tag: E.1.2.A
plan_type: refactor   # in-code documentation toolchain — dev-plane; engine H1-H22 do NOT govern (tools/CLAUDE.md)
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: the [SCHEMA]_[v1.0] tag system + the north-star toolchain (fact-producers → plugin → custom-IDE); the codebase CONVERSION is now COMPLETE
coding_status: E.1.2.A-CONVERSION-COMPLETE + HARD-ZERO   # corpus 100% green (all 17 HARD checks); next = plugin lane (#7/#8/#10, Caramel) / per-instantiation (#15)
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-07-18-E.1.2.A-c4-steps-1-2-shipped-next-corpus-fill-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md, DESIGN_SPECS/framework-patterns/isolated-per-struct-layout-probe.md, DESIGN_SPECS/framework-patterns/doc-intelligence-toolchain-architecture.md, DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md]
deletion_scope: none   # comments-only + lossless everywhere
---

# E.1.2.A — [SCHEMA] CONVERSION COMPLETE + corpus 100% green; next = plugin lane / per-instantiation

**Created:** 2026-07-18
**Target:** the ship's mechanical conversion + toolchain cohesion is DONE. Next actionable = Task #7 (foxtag cutover + plugin V1 — Caramel's session; the foxtag↔Python parity prerequisite is now GREEN) or Task #15 (per-instantiation DERIVED emit — needs the `[INSTANTIATION]` full-tuple schema decision).
**Branch:** feat/v5.15-live-readiness
**Plan:** `plans/v5.15-live-readiness/subplans/2026-07-05-E.1.2.A-comment-system-and-doc-consolidation.md`
**Decision log:** `...v5.15.5.F.4d.1.E-architecture-v2.md` (D-369..D-371)
**MASTER:** `...MASTER.md` (UPDATE 14)

## State at handoff write
- **Engine HEAD `4c076ed`** (RegimeDetector conversion — comment-only, 0 code lines, build 3702/0). **Workspace HEAD `5862e8d`.**
- **Off-limits WIP still uncommitted (Caramel's — DO NOT touch/git-add/revert):** `CoreFrameworks/ExecutionCore.hpp` (a `_pad_line0` cache-padding tweak). *(RegimeDetector.hpp is now CONVERTED + committed — it was WIP; Caramel released it this session.)*
- Untracked scratch (not ours; leave): `build_clangd/`, `resume`, `tmpo0ujx6rw/`, `note`.

## What landed this session — all committed, corpus 100% GREEN
- **Corpus-wide DERIVED fill (Task #4)** — ~900 layout DERIVED tags (`[SIZE]/[ALIGN]/[CACHE_LINES]/[STRADDLE]`) + `[ORIGIN]`/`[UPDATED]` provenance across all 8 source dirs (engine `9939ed6`); comment-only/lossless, blast-radius gate clean (no cross-thread H6 straddle / size-drift).
- **D-369 — provenance/freshness grammar:** `[ORIGIN]_[AUTO|MANUAL]` + `[UPDATED]` (stamp-on-change → idempotent) → schema fence/table + § WRITTEN-vs-LIVE + arch spec + `check_cache_layout` writer + selftest; retires the ad-hoc `(tool-refreshed…)` prose.
- **D-370 — `match_layout`:** multi-instantiation REFUSAL (no arbitrary wrong fact — `RollingStats` = tracked remainder, Task #15) + explicit-instantiation space-normalize; homed in `isolated-per-struct-layout-probe.md`.
- **D-371 — toolchain cohesion:** NEW `[SCHEMA]` version guard `check_schema_version.py` (SSoT-derived from the spec's LOCKED line; wired HARD; D-137 wrapper; TOOLS.md) + foxtag CLASS-subfile resolver fix → **foxtag↔Python PARITY PASS** + `v1`→`v1.0` fixture standardization.
- **RegimeDetector converted + #6 HARD-zero flip:** 4 units blocked (`TickRateState`/`RegimeState`/`FOREACH_REGIME_PERSIST_FIELD` wire-persist/`Regime_Init`) → completeness baseline **zeroed** (a new un-blocked/lumped unit now fails CI) + version-guard WIP-exemption removed + DESIGN_SPECS index regen'd. **Corpus 100% green — all 17 HARD checks pass** (C1=0/C2=0/C3=0).

## NEXT ACTION (armed) — the conversion is done; remaining work is Caramel's lane or gated
- **#7/#8/#10 — plugin lane (Caramel's session):** the foxtag cutover (parity PASS = the prerequisite met) · the plugin's D-369 `ORIGIN`/`UPDATED` writer/render update (its `:FoxSymdepsDerived!` still emits the old prose) · `tag_grammar_adapter.lua` drift (D-365, the native-Lua mirror doesn't inherit fence edits) · the cache-line-density placeholder bug.
- **#15 — per-instantiation DERIVED emit:** needs a SCHEMA decision — `[INSTANTIATION]` must carry the FULL sizeof-able tuple for a multi-PARAM template (`RollingStats<F,W>` lists only W); then the tool probes each declared tuple → keyed facts (collapsed where uniform).
- **#9 — add-vocab one-action + surface-parity CI (D-365):** the fact-producer/CI half is receiver-lane (unblocked).
- **#11–#14 — north-star increments (D-366/D-367):** RC-F register-fit / `FOREACH_PLANE` / multi-comment-syntax parser / toolchain self-hosting.
- **P4 — workspace consolidation** (plan § P4): the bounded dated-doc audit + de-sprawl.

## HARD rules
1. **NEVER touch/git-add/revert `CoreFrameworks/ExecutionCore.hpp`** — Caramel's uncommitted WIP.
2. **Comments-only** for any conversion/fill (lossless-gated); the completeness gate is now **HARD-zero** — a new un-blocked unit fails CI immediately.
3. The version guard / foxtag parity / completeness are all **strict** now — keep them green.

## TaskList (recreate via /accept-handoff Stage 7)
| # | Task | Status |
|---|------|--------|
| 1–6 | corpus fill + D-369/370/371 + spec-homing + RegimeDetector + HARD-zero flip | **completed** |
| 7 | foxtag update cutover + plugin V1 UX (Caramel's) | pending |
| 8 | plugin grammar 1:1 + surface-parity gate (D-365) | pending |
| 9 | add-vocab one-action (`add_vocab.py`) + surface-parity CI | **completed** (one-action done; surface-parity CI = plugin-lane #8) |
| 10 | plugin cache-line-density placeholder bug (Caramel's) | pending |
| 11 | register-fit RC-F axis (`check_register_fit.py` + `foxtag fields`) | **completed** (fact-producer done; plugin render = #8/#10) |
| 12–14 | FOREACH_PLANE / multi-syntax parser / self-hosting (D-367) | pending |
| 15 | per-instantiation DERIVED emit (needs `[INSTANTIATION]` full-tuple) | pending |

## Capture-completeness (this session — nothing lost)
- **Decisions → LOGGED:** D-369 (provenance/freshness grammar) · D-370 (multi-instantiation refusal + explicit-instantiation space-normalize) · D-371 (`[SCHEMA]` version guard + foxtag CLASS-resolver fix + `v1`→`v1.0` standardization) — all in the decision log with paired `<!-- D/C/F -->`+`<!-- STATUS -->` sentinels, triple-homed (log + spec + plan), each STATUS = DECIDED/LANDED.
- **Findings → homed/ledgered:** the 4 dogfood-found tool bugs all FIXED + captured — foxtag CLASS resolver (→ D-371 + `foxtag.hpp`, parity PASS) · schema-version drift (→ D-371 + `check_schema_version.py` guard) · multi-instantiation wrong-fact + explicit-instantiation miss (→ D-370 + `match_layout`). `RollingStats` per-instantiation → **Task #15** (tracked). The foxtag fix = dev-plane parity, captured in D-371 by merit (not an engine/capital `PARITY_ISSUES` ledger item).
- **Memories/skills → indexed:** no NEW memories or skills this session (the work is decisions, logged). NEW tools `check_schema_version.py` + `check_schema_version_selftest.sh` enrolled in `DOCS/TOOLS.md` (D-137 discoverable-selftest satisfied); DESIGN_SPECS index regen'd (README/TAG_INDEX/CODE_TAG_INDEX + CLAUDE.md skill table). MASTER **UPDATE 14** + this handoff supersede the prior.
- **TaskList snapshot** (recreate at pickup via /accept-handoff Stage 7): **#1–6 completed**; #7–15 pending (per the table above).

## Paste into a fresh Claude Code session
```
I'm picking up E.1.2.A — the [SCHEMA] conversion is COMPLETE (corpus 100% green, HARD-zero enforced, foxtag parity green).

Run: /accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-18-E.1.2.A-conversion-complete-hard-zero-next-plugin-handoff.md

NEXT: the plugin lane (#7/#8/#10 — Caramel's; foxtag parity green so the cutover prerequisite is met) / per-instantiation (#15, needs the [INSTANTIATION] full-tuple schema call) / north-star increments (#11-14) / P4 workspace consolidation.

HARD: never touch/commit CoreFrameworks/ExecutionCore.hpp — Caramel's WIP. Comments-only, lossless. Completeness is HARD-zero. Do NOT trust prior-session memory — verify against current code.
```

## Quick links
- Commits: engine `9939ed6` (fill) + `4c076ed` (RegimeDetector); workspace `bfd7328` + `eb3fc2f` + `fbbf421` + `5862e8d`.
- Plan: `...2026-07-05-E.1.2.A-comment-system-and-doc-consolidation.md` · Decision log: D-369..D-371 · MASTER: UPDATE 14.
- Specs: `isolated-per-struct-layout-probe.md` · `doc-intelligence-toolchain-architecture.md` · `in-code-documentation-schema.md` · `in-code-doc-system-north-star.md`.
- Tools (this session): `check_schema_version.py` (+ `_selftest.sh`) · `check_cache_layout.py` (D-369/370) · `foxtag/foxtag.hpp` (CLASS fix).
