---
type: handoff
status: superseded   # → 2026-08-10-E.1.2.B-derived-facts-integrity-arc-handoff.md (the D-413/414/415 arc replaced this world; the §(g)-4 design section here REMAINS the binding spec for that task). Was: supersedes 2026-07-20 (the addenda pile).
ship_tag: E.1.2.B
plan_type: mixed
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: make every toolchain surface (CLI · local-CI · plugin · doc-viewer · AI) a thin consumer of the ONE core, versioned 0.x→1.0.0 — "the code becomes the IDE", felt in the editor
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-07-20-E.1.2.B-0.2-blind-guards-closed-next-f-triage-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [meta-disciplines/calibration-corpus-non-vacuity-discipline.md, meta-disciplines/meta-anti-pattern-index.md, framework-patterns/doc-intelligence-toolchain-architecture.md, doc-disciplines/in-code-doc-system-north-star.md]
coding_status: 0.2 — (b)(c)(e)(f) DONE · (g) steps 1–3 DONE, step 4 MID-SCOUT, step 5 pending its dive · (a) partial (completes with (d)) · PLUS the 0.4 plugin arc RUNNING IN PARALLEL (5 slices landed) · D-412 C++-FIRST policy BINDING on all new producers
---

# E.1.2.B `0.2` — (g) 1–3 done · plugin humanization arc live · **D-412: C++-first is POLICY**

**Written 2026-08-09 at compaction-close.** Branch `feat/v5.15-live-readiness`. The predecessor
handoff is an addenda-pile; THIS doc is the current truth. Re-derive anything you would act on
(§ Re-derive).

## State at write

| Repo | HEAD | Note |
|---|---|---|
| Engine `FoxML_Trader_v2` | `4c076ed` | **UNTOUCHED across the entire arc** (dev-plane only, HARD rule) |
| Workspace | see `git log` | all pushed; window since `b82df57` (2026-07-20) spans the whole arc |
| Plugin `fox-symdeps.nvim` | `cd971ac` | branch **`feat/hud-robustness`** (NOT main — a `push origin main` false-"up-to-date" bit once; always push/verify THE BRANCH). Merge-to-main = operator housekeeping, someday, no urgency |

**⚠️ PUSH DISCIPLINE:** first SSH attempt may drop ("Bye Bye"); retry once. Verify pushes against
the REMOTE (`git ls-remote`), never the push message. This shell is **zsh** (`pipestatus`
lowercase); the hooks are **bash** (`PIPESTATUS`).

## ▶ THE BINDING NEW DECISION — D-412 (operator confirmed 2026-08-09)

**C++-FIRST FOR NEW FACT-PRODUCERS.** A new toolchain capability lands as a foxtag
producer/subcommand (typed structs in the ONE core; Python shim only where CI wiring needs it) —
T1's "a new capability = a foxtag producer consumed by all" made binding. **Existing Python
migrates PER-PRODUCER**, opportunistically when touched, behind the T4/D-349 parity gate with
soak, each surface gated on its own D-411 unit-test net. **Big-bang REJECTED.** Duct-tape
carve-out: time-of-need scripts stay legal, they just can't become load-bearing. **First test
case: the (g) step-5 resolver-lift dive must evaluate landing citation-resolution FOXTAG-side**
(the plan body's older "lift into `citable_ids.py`" wording predates D-412). Read D-412 in the
log before designing ANY new tool.

## Where `0.2` stands (order D-398: …→(g)→(d); (a) rides (d))

**(g) DONE — steps 1–3** (all commits pushed, all with teeth proven both directions):
1. **Annotation markers** (`[NOW: path:line]` verified-fresh-pointer · `[CITE-AS-EVIDENCE]` ·
   legacy `[PATH SUPERSEDED]` honored) — append-only docs can FIX a stale cite; decision-log hard
   cites went 14→0 (8 repointed at CURRENT code by opening targets, 6 evidence-marked).
2. **Rename-map resolver** — before MISSING, B-Plus tries git's own rename records
   (chain-resolved, existence-filtered) then a unique-basename move probe (never guesses).
   New status **RENAMED** carries the current path (the auto-repair payload). Live-proven:
   `CoreLatencyStats.hpp→NodeLatencyStats` (git map) · `EngineSharded/Run.hpp→CoreFrameworks/…`
   (basename; git holds NO R-record for the dir move — searched `git log --diff-filter=R --find-renames --name-status` over the engine: 6 records total, none touching `EngineSharded/`). `parity_harness.cpp` correctly STAYS missing
   (retired at `.E.1.1`, deleted not renamed).
3. **Severity flip** — live-doc OOB/MISSING **BLOCK** (`n_hard_live`); FROZEN records exempt
   (derived from `frozen_record_paths` — one source); today's rot grandfathered in
   `tools/lib/citation_anchor_baseline.txt` (171 keys, SHRINK-ONLY; regenerate via
   `--all --anchors-only --emit-anchor-baseline`). **AND the gate that would enforce it was
   itself pipe-swallowed**: hook Check A judged `python | tee`'s exit (= tee's, always 0) —
   proven live, fixed via `PIPESTATUS[0]`; `.githooks` is ONE symlinked file, both repos covered.
   B-Plus teeth now 15.

**(g) step 4 — MID-SCOUT, design DECIDED, nothing written yet.** Content-staleness checks:
- **(3a) supersede-unmarked** → reciprocal-supersession check (every `supersedes:` answered by
  `superseded_by:` + status on the target) in **`check_doc_metadata.py`** (owns the frontmatter
  corpus; its flat `parse_frontmatter` is the join substrate). The v1-log canonical instance is
  ALREADY marked-clean → the check may land HARD-able with zero live findings; verify corpus-wide
  before choosing tier.
- **(3b) D-408's ABANDONED shapes** → **`check_tech_debt.py`** (owns ledger-entry parsing), new
  `--contract-stale` mode: (i) **fired-trigger** — `trigger:` names a git-tag-shipped tag while
  status hasn't moved (tag membership via engine `git tag --list`; a trigger naming a ship ABSENT from
  that tag list AND from `Version.hpp`'s changelog → INFO tier, e.g. `TECH_DEBT-063`'s
  `trigger: sub-ship-.F.4e` — zero `.F.4e` hits in either search space); (ii) **churning-status-but-static** — transitional status, no commit across ~3 ship
  closes; (iii) **empty-tier** — a flow-through ledger file with zero entry churn since creation.
  **`DOCS/tech-debt/in-flight.md` is the confirmed live exemplar for ALL THREE** (last commit
  `6be0bac`, 2026-05-19; holds TECH_DEBT-063 + TECH_DEBT-092). Land ADVISORY-first in
  `check_session_docs`, fix the in-flight.md instances, then consider HARD. NEVER key on
  wall-clock age (D-408; the existing `--stale` months-mode is the superseded-in-spirit sister).
- Per D-412: these extend EXISTING Python owners (touched-tool strangler applies later, not now —
  extension ≠ new producer).

**(g) step 5 (pending)** — the resolver lift; dive under D-412 (foxtag-side candidate). This is
also the **CONVERGENCE point with the plugin**: the `0.4` doc-viewer dead-link check consumes it.

**Then (d)** the D-374 update-orchestrator (the write side; (g)'s RENAMED payloads are an input),
**with (a)** completing alongside.

## The plugin arc (0.4) — five slices landed, all headless-proven, branch `feat/hud-robustness`

1. **`tagcursor.lua`** — cursor-tracking by enclosing unit (§6's foundational primitive):
   debounced, changedtick+span cached, `vim.b.fox_unit` + `User FoxUnitChanged` + `:FoxUnit`.
   Landed with an unplanned **live-buffer↔C++ parity proof** (both resolvers: `FUNCTION
   ExecutionCore_Init 263–318 [ENGINE][BOOT_TIME]`).
2. **HUD cursor-anywhere** — `inspect_cursor` routes `tagcontext.resolve()` (word-first) with the
   heal branch; blank-line-in-unit resolves the declared symbol.
3. **The §6 ROLE-SWAP** — panel = ACCUMULATE BOARD (`<leader>dD` ADDS; auto-rotate +
   close-on-reuse DELETED; `H/L`/`d[`/`d]` flip, `x` drops, `q` closes); NEW `followcard.lua` =
   the auto-follow HUD role (consumes `FoxUnitChanged`; `<leader>df`; **cockpit docks THIS**);
   shared `hud.attach_*` helpers (live-refresh · external-reload with REFCOUNTED autoread ·
   last-window guard); **`tagcontext.resolve_unit()`** = unit-first resolution for board+follow
   (the first smoke PASSED while following comment WORDS — caught by reading printed members,
   not the green assertion).
4. **COMPARE** — `s` in-board opens a companion strip on the previously-selected card (zero hud
   changes; width-gated 140 cols/36 lines; dies with `s`/its card's drop/the board).
5. **Humanization ×4** (the §9 arc, operator-named: "the tools work; making it human-usable IS
   the work"): per-field straddle diagnostics (root cause: layout.lua COMPUTED each field's
   position and dropped it — items now carry lnum/col; each straddler gets a WARN on its own
   line with boundary math + the decl keeps a count summary) · ambient size chip →
   `right_align` out of git-blame's eol lane (`ambient_pos` knob) · **menu refresh** (stale
   pre-role-swap label fixed; follow/board/compare entries; `when()` runtime gates — §6
   context-gating real; **⚠/✎ write-tier icons derived from T6's vocabulary** (⚠ edits source ·
   ✎ writes tag-comments · blank read-only, with legend); menu DOCKS to the open
   board/follow-card/HUD via `anchor_win`).

**§9's METHOD (operator): surfaces DERIVE from the existing vocab/doc system — an affordance
cites which vocab axis it renders, or it doesn't belong.**

**Dogfood keys:** HUD key mid-unit · `:FoxUnit` · `<leader>dD`×2 then `s` · `<leader>df` ·
`<leader>dg` (now points at the straddling FIELD) · `m` in-HUD (docked menu with icons).

## The idea corpus — `plans/_ideas/2026-08-07-operator-ideas-luts-asm-compile-piping.md`

**§1–§10 are the operator's recovered original list** (provenance recorded; transmitted in
fragments 2026-08-07→09): LUTs · asm compile-cmd piping · template autogen · **declarative
tag-driven codegen ("the big thing" — AMENDED: deterministic, grammar-driven, explicitly NOT
LLM-codegen; local-AI at most proposes declarations)** · plane index · **C++ core (→ now D-412)**
· tools-structure/portability (restructure WAITS on the rename-absorber, rides the migration) ·
chain HEAD/TAIL/seq labels + data-flow graph · the IDE-humanization arc + method · tag-scoped
mass ops + foxtag as the sub-agent CONTEXT PACKER. The Pending section is the landing pad for
further fragments — capture on arrival, always.

## Recent bites (will save you an hour each)

1. **Pipes swallow gate verdicts.** `cmd | tee` reports tee's exit. Audit any gate pipeline;
   `PIPESTATUS[0]` (bash) / `pipestatus[1]` (zsh). Three instances this arc; one was IN a gate.
2. **`resolve()` vs `resolve_unit()`**: word-first is for point-at-a-thing (HUD); unit-first for
   board/follow. Picking wrong = following comment words.
3. **`--anchors-only`** makes corpus-wide B-Plus sweeps fast (no compile probes) — TD-259's
   anchor-half relief; also the baseline generator.
4. **Anchor statuses now tier**: bare OOB/MISSING = blocking · `…/frozen` · `…/baselined` ·
   RENAMED (carries the fix) — read the qualifier before reacting.
5. **An EMPTY baseline proves nothing until the detector's reach is measured** (the arc's
   founding lesson; AR-16 for values-from-renderings generally).

## Capture-completeness (judgment ledger, Check-N form)

| Check | Verdict |
|---|---|
| Check 2 plan-body frontmatter | ✅ unchanged this stretch |
| Check 3 decision artifacts | ✅ D-405–407, D-412 all carry paired sentinels + nav rows + range current (D-97..D-412) |
| Check 5 handoff currency | ✅ THIS doc (replaces the addenda-pile; predecessor → superseded) |
| Check 6 M7 escalation | ✅ none owed this stretch (the pipe-swallow fix WAS the structural close) |
| Check 7 spec promotion | ✅ none eligible |
| Check 9 memory→spec | ✅ no new memories minted (session-size memory landed earlier, indexed) |
| Check 10 going-forward rules | ✅ none new (D-412 is a decision, not a collaboration rule) |
| Check 12 cascade | ✅ D-412: body+STATUS+nav+range+§6-flip; §9 method; ideas amendments |
| **6.5.4 adversarial review** | ⚠️ **SKIPPED — DECLARED** (compaction pressure; silence ≠ verdict). `/accept-handoff`'s Stage 4–6 verification at pickup is the compensating control; treat this doc's prose as HINTS and re-derive |

**Findings → ledger:** none this session needing a NEW ledger row — the stretch's findings were
each fixed in place with teeth (the pipe-swallow · dropped-lnum · eol-collision · stale menu) and
are recorded in their commit messages + Class-51/AR-16 rows from the earlier closes; TECH_DEBT-263
(resolver adoption) and -259 (anchors-half relieved) were updated in prior sessions, nothing new
opened or owed.

**Auto-write surfaces this stretch:** ledgers/specs/landmines all current from the earlier closes;
this stretch's captures went to the ideas file + decision log + handoff (the §9/§10/D-412 layer).

## OPERATOR notes

- **Golden**: blessed at 878; **D-412 has since been minted** → +1 addition pending. Additions-only
  = no urgency; bless at next cadence point (`python3 tools/bless.py --console`).
- **Plugin branch merge** (feat/hud-robustness → main): your call, someday.

## TaskList state (recreate via /accept-handoff Stage 7)

| ID | Status | Subject |
|---|---|---|
| #4 | in_progress | **(g)** — steps 1–3 DONE; ▶ step 4 next (design DECIDED, see § above); step 5 dive under D-412 |
| #3 | pending | OPERATOR TTY: golden bless — +1 addition pending (D-412); additions-only, next cadence point |
| #5 | pending | (d) D-374 update-orchestrator (LAST; (g)'s RENAMED payloads are an input) |
| #6 | pending | (a) contract docs — completes with (d); contracts stay language-neutral per D-412 |
| #7 | pending | Backlog: TECH_DEBT-254(c) · -246 (policy call) · -259 (anchors-half relieved by --anchors-only) |
| #8 | pending | Ledger id-line format split (3 spellings) + `status: closed`-in-open.md stragglers (-140/-250) |
| — | (thread) | Plugin: N>2 board layouts + tag-enriched trees (converges with (g)-5); humanization items as dogfood surfaces them |

## Re-derive anything you would ACT on

```
git -C /home/caramel/code/tick-trader-percore-workspace log --oneline -15
git -C /home/caramel/code/FoxML_Trader_v2/tools/plugins/fox-symdeps.nvim log --oneline -8
python3 tools/check_capture_audit.py --check 14          # id corpus (0 baselined expected)
python3 tools/check_plan_body_symbol_existence.py --selftest   # 15 teeth
./tools/check_session_docs.sh                            # the full floor
python3 tools/bless.py --console                         # blessables menu
rg -n "D/C/F: D-412" plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
```

## Paste into a fresh session

```
Picking up E.1.2.B 0.2. Run:
/accept-handoff plans/v5.15-live-readiness/handoffs/2026-08-09-E.1.2.B-0.2-g3-humanization-cpp-policy-handoff.md

BINDING: D-412 — C++-first for NEW fact-producers; strangler for existing; no big-bang.
NEXT (either thread, operator's pick):
  A) (g) step 4 — content-staleness checks. Design DECIDED (see handoff): reciprocal-supersede
     in check_doc_metadata + --contract-stale (fired-trigger/churning/empty-tier) in
     check_tech_debt; ADVISORY first; in-flight.md is the live exemplar for all three.
  B) plugin — N>2 board layouts + tag-enriched trees (converges with (g)-5's resolver, whose
     dive must evaluate FOXTAG-side landing per D-412).
WILL BITE: pipes swallow gate verdicts (PIPESTATUS[0]) · resolve_unit for board/follow ·
read anchor-status qualifiers · re-derive, never trust prose counts.
Engine stays 4c076ed. The 6.5.4 adversarial pass was DECLARED-SKIPPED at close — verify this
doc against HEAD as you load it.
```
