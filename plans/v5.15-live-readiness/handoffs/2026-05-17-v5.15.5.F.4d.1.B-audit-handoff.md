# v5.15.5.F.4d.1.B — in-depth audit + cycle handoff

**Created:** 2026-05-17 (end of `.F.4d.1.A` Path γ+ v2 ship close session)
**Target ship:** `v5.15.5.F.4d.1.B` (migration + consumer — second sub-ship of `v5.15.5.F.4d.1` umbrella; TECH_DEBT-085 Thread A FULL)
**Branch:** `feat/v5.15-live-readiness` (continue on sprint branch)
**Pre-tag rollback anchor (engine):** `v5.15.5.F.4d.1.A` (GPG-signed; from previous ship close 2026-05-17)
**Predecessor ship postmortem:** `plans/v5.15-live-readiness/postmortems/2026-05-17-v5.15.5.F.4d.1.A-postmortem.md`
**Sub-master:** `plans/v5.15-live-readiness/subplans/2026-05-16-v5.15.5.F.4d.1-thread-a-framework-full.md` v1.2 Path γ+ v2 LOCKED
**`.B` plan body:** `plans/v5.15-live-readiness/subplans/2026-05-16-v5.15.5.F.4d.1.B-migration-consumer.md` v1.2 + body residual cleanup annotations
**`.B` sidecar:** `plans/v5.15-live-readiness/subplans/2026-05-16-v5.15.5.F.4d.1.B-migration-consumer-examples.md` v1.1 + body cleanup notes

**Session purpose:** Pick up at `.A`-shipped state. Run **IN-DEPTH AUDIT** (6 layers, deeper than `.A`'s 5-audit gate) on `.B` plan body. Triage findings with Caramel per `feedback_consult_on_audit_findings`. Update `.B` plan body if findings warrant. Then implement `.B` (~6-8h focused; wide blast radius; per-Phase mid-flight tags discipline).

---

## Engine state at handoff write (verify on cold-pickup)

- **HEAD:** `39b9947` (`v5.15.5.F.4d.1.A ship close: Path γ+ v2 framework infra`)
- **Tag:** `v5.15.5.F.4d.1.A` (GPG-signed annotated; from 2026-05-17 ship close)
- **Pre-tag anchor:** `pre-v5.15.5.F.4d.1.A` (rollback target if `.A` needs revert; preserved)
- **Branch:** `feat/v5.15-live-readiness` (clean — no uncommitted engine changes; not yet pushed to remote per CLAUDE.md push-discipline)
- **Tests:** 3196 controller_test + 17 depth_recorder_test GREEN at HEAD (.A baseline)
- **5 binaries:** test / gui / suite / tsan / asan all build clean at HEAD
- **CI checks:** check_meta_registry.py 3 PASS (63/63); check_per_core_registry_integrity.py 6 PASS (0 Class 27 exemptions); calls_graph_diff empty
- **Working tree:** **CLEAN** (engine repo)
- **Workspace state:** mirrored via `/sync-workspace` at 2026-05-17 ship close

Verify via:
```bash
cd /home/caramel/code/FoxML_Trader_v2
git rev-parse HEAD                    # expect: 39b9947...
git status --short                    # expect: empty
head -10 Version.hpp                  # expect: ENGINE_VERSION_STRING "5.15.5.F.4d.1.A"
git tag --sort=-creatordate | head -5 # expect: v5.15.5.F.4d.1.A at top + pre-v5.15.5.F.4d.1.A
./build.sh test 2>&1 | tail -3        # expect: 3196 passed, 0 failed (.A baseline)
```

If divergent: another session may have advanced state. Reconcile by reading commits since `39b9947` before proceeding.

---

## What `.A` Path γ+ v2 shipped (predecessor context)

Per `postmortems/2026-05-17-v5.15.5.F.4d.1.A-postmortem.md`:

1. 1-row addition to FOREACH_METADATA_BIT (STAMP_BOUND_CFG_DERIVED at bit 13; auto-generates global + per-core masks)
2. `CoreFrameworks/StampBoundDerivedFilter.hpp` NEW — first canonical consumer using CFG_FIELD_FOR_EACH_SET_BIT
3. `tests/wire_format_invariants.hpp` NEW — reusable I1-I5 helper
4. CFG_COMPOSE_AUDIT_DECISIONS X-macro registry (Gap 1 mitigation)
5. H16 compile-time static_assert (against FOREACH_METADATA_BIT enrollment)
6. cli_explain_mask bug FIX (was ~0ULL; now ~(has_side_effect | hidden_by_default))
7. stamp_emit_mask alias DELETE (zero consumers; redundant)
8. 22 new test sections (T1-T13)
9. 6 DESIGN_SPECS updated/created
10. 6 TECH_DEBT entries opened (-087/-088/-089/-090/-091/-092)

**Path γ correction recognized that existing `FOREACH_METADATA_BIT` + `cfg_compute_mask` + `CFG_FIELD_FOR_EACH_SET_BIT` infrastructure at `CfgFieldRegistry.hpp:1020-1159` IS the canonical mechanism — Path γ uses it instead of building parallel walker macros.**

---

## Required reading (parallel batch on cold pickup)

- `/home/caramel/code/FoxML_Trader_v2/CLAUDE.md` (always-loaded; H1-H20 invariants codified)
- `/home/caramel/code/FoxML_Trader_v2/CLAUDE.local.md` (operator-private; sprint state row v1.2 Path γ+ v2 LOCKED; 2 new going-forward rules at "Process & sprint": auto-pick-future-oriented + per-sub-ship cycle)
- `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md` (auto-memory index; load-bearing entries cross-ref'd)
- `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/feedback_auto_pick_future_oriented.md` (NEW principle from `.A` ship)
- **THIS HANDOFF:** `plans/v5.15-live-readiness/handoffs/2026-05-17-v5.15.5.F.4d.1.B-audit-handoff.md`
- **`.A` postmortem:** `plans/v5.15-live-readiness/postmortems/2026-05-17-v5.15.5.F.4d.1.A-postmortem.md`
- **`.A` findings doc:** `plans/v5.15-live-readiness/plan_checks/2026-05-16-v5.15.5.F.4d.1-tech-debt-audit-findings.md` § "Path γ+ v2 LOCKED scope"
- **`.A` audit reports (10 docs):** `plans/plan_checks/{parity-check,trace-deps,readiness,dod-audit,merge-scan}-2026-05-16-v5.15.5.F.4d.1.A.md` + `plans/v5.15-live-readiness/plan_checks/d1-d5 audit reports + plan-context-sweep`
- **Sub-master:** `plans/v5.15-live-readiness/subplans/2026-05-16-v5.15.5.F.4d.1-thread-a-framework-full.md` v1.2
- **`.B` plan body v1.2 + sidecar v1.1:** `subplans/2026-05-16-v5.15.5.F.4d.1.B-migration-consumer.md` + `-examples.md` (note body residual cleanup annotations at top of each)
- **2 NEW DESIGN_SPECs Stage 3 first reference:** `DESIGN_SPECS/framework-patterns/composed-filter-mask-pattern.md` + `DESIGN_SPECS/wire-format-patterns/wire-format-canonical-body-invariants-helper.md`
- **3 Path γ-corrected DESIGN_SPECs:** `DESIGN_SPECS/framework-patterns/metadata-bit-driven-derived-filter-framework.md` v1.2 + `framework-composition-overview.md` v1.1 + 3 status-corrected specs

---

## In-depth audit specification (6 LAYERS — deeper than `.A`'s standard gate)

Per Caramel's "in depth audit" directive at handoff request. `.B` is THE wide-blast-radius ship of the umbrella (24-row migration + 12+ consumer sites + ML_CFG_FLAG 5→6 sig + Winsor parse-time validation + β4 cohort gate sidecar + legacy registry empty-out). Standard 5-audit gate isn't enough — need codebase-wide pattern sweep + dependency chain trace.

### Layer 1 — Standard `/precoding-audit-gate` (5 audits parallel)

```
audit_set = parity + trace-deps + readiness + dod-audit + merge-scan
plan_path = plans/v5.15-live-readiness/subplans/2026-05-16-v5.15.5.F.4d.1.B-migration-consumer.md
focus_keywords = "24-row FOREACH_STAMP_BOUND_CFG migration, FOREACH_ML_CFG_FLAG 5→6 sig, tt::cfg_emit_synthetic_field<T>, β4 FOREACH_DRIFT_GATE cohort gate sidecar, Winsor cfg parse-time validation, CFG_DRIFT_AUTOPOPULATE companion macro, 12+ consumer migration sites, HMAC chain byte preservation, locale-pin Layer 2"
```

### Layer 1 +ext — `/accounting-audit` (NEW for `.B`)

`.B`'s 24-row cohort migration affects HMAC chain wire format + stamp body byte preservation. Per `feedback_consult_on_audit_findings` + the `accounting-audit` skill spec, fire it scoped to wire-format paths:

```
audit_scope = module:wire-format,module:HMAC-chain
focus_keywords = "stamp body byte preservation, HMAC chain integrity, locale pin Layer 2, canonical body order, has_<field> bit accuracy, ternary normalization for bitmap-bool emit"
```

### Layer 2 — `/dependency-chain-trace` (deep flow trace)

Trace `STAMP_BOUND_CFG_DERIVED` bit propagation end-to-end:

```
chain:STAMP_BOUND_CFG_DERIVED
  → 24 source rows (cohort flagging at .B)
  → ML_CFG_FLAG 5→6 sig migration (5 STAMP_BOUND-eligible bitmap rows)
  → cfg_compute_mask auto-gen (already lands at .A; .B flags rows)
  → g_global/per_core_cfg_stamp_bound_cfg_derived_mask population
  → CFG_FIELD_FOR_EACH_SET_BIT consumer iteration
  → CFG_DRIFT_AUTOPOPULATE companion macro walks
  → β4 cohort gate dispatch (FOREACH_DRIFT_GATE + DriftGateKind table)
  → tt::cfg_emit_synthetic_field<T> per-type emit (Path γ replaces stub)
  → StampBoundDerivedFilter.hpp canonical body emit
  → HMAC chain participation (stamp body locked at .D fixture)
```

Output: graph of all sites that read/write the bit. Lifecycle classification per site (boot vs slow path vs test). Cohort sibling discovery. Blast-radius assessment.

### Layer 3 — Level 4-style codebase pattern survey (5 dimensions)

Same shape as `.A`'s Phase 2 Level 4 audit. Five dimensions tailored to `.B`:

| D | Audit chain | Question |
|---|---|---|
| **D1** | `chain:tt::cfg_emit_synthetic_field<T>` — NEW helper at `.B` | Type-trait dispatch coverage for all 24 cohort row types (FPN<F=64>, int, uint8_t, double, bool). Sister types not yet covered? |
| **D2** | `chain:FOREACH_DRIFT_GATE + chain:DriftGateKind + chain:g_drift_gate_lookup_*` (β4 sidecar) | Cohort gate dispatch table — 5 cohort fns + 15-row sparse registry + 2 sparse arrays + dispatch table. Per-cohort gate_when expressions verified against actual cfg flags (BANDIT_THOMPSON / RISK_DEGRADATION / RIDGE_ANY / COMPOSITE_CONFIDENCE / Winsor implicit). |
| **D3** | `chain:winsor_pct_low + chain:winsor_pct_high + chain:ControllerConfig_Load<F>` | Winsor cfg parse-time validation placement — `~10 LOC at ControllerConfig.hpp parser post-parse / pre-acceptance`. Sister cohort fields needing similar parse-time validation? (e.g., per_horizon_barrier_blend?) |
| **D4** | `chain:CFG_DRIFT_AUTOPOPULATE + chain:STAMP_CFG_AUTOPOPULATE + chain:INFERENCE_CFG_AUTOPOPULATE` (AUTOPOPULATE family) | Companion macro sister patterns; ensure CFG_DRIFT_AUTOPOPULATE follows existing pattern shape; consumer migration sites (CoreModelZoo.hpp:225-247 + StampHelper.hpp:150 + ConfidenceScore.hpp:729) — verify all 12+ residual references actually exist + are correctly mappable. |
| **D5** | Stage 2/3 ACTIVE DESIGN_SPECS scan (sister to `.A` D4) | Other specs with similar drift; verify NEW DESIGN_SPECs at `.A` (composed-filter-mask-pattern + wire-format-canonical-body-invariants-helper) accurately describe what `.B` does. |

### Layer 4 — `/plan-context-sweep` (re-verify after `.A` corrections)

`.A` plan body cleanup annotations added to `.B` body for SUPERSEDED v1.0 references. Sweep again to verify:
1. `.B` body residual references (Lines 114, 234, 247, 260-265, 642, 827 — SUPERSEDED walker fn names) are still flagged correctly
2. New `.B` content (β4 sidecar + Winsor + CFG_DRIFT_AUTOPOPULATE) aligns with Path γ canonical mechanism (CFG_FIELD_FOR_EACH_SET_BIT iteration)
3. Sister plans (`.F.4e`/`.F.4f`/`.F.4g`+) — verify STALE banner on `.F.4e` is still adequate; check whether `.F.4f` cleanup ship plans should fold in TECH_DEBT-090 or stay separate

### Layer 5 — `/test-strength-audit` (regression prevention)

Per `.A` plan body Step 6 expectations (~20-30 new tests at `.B`):

```
audit_target = git diff between pre-v5.15.5.F.4d.1.B and HEAD (when pre-tag lands)
audit_target_2 = tests/controller_test.cpp existing 24-row cohort tests (fixtures may need adjustment after migration)
focus = test assertion strength changes (==  → >=) without justification + deleted check() lines without redundancy explanation + new tests that mask drift
```

Run AFTER pre-coding tag exists; this is a "during implementation" audit too — fire periodically as `.B` Phase tags land.

### Total audit fire shape

- Layer 1: 5 parallel agents (~30 min)
- Layer 1 +ext: 1 agent (~10 min)
- Layer 2: 1 agent (`/dependency-chain-trace`) (~15 min)
- Layer 3: 5 parallel agents (~30 min)
- Layer 4: 1 agent (`/plan-context-sweep`) (~10 min)
- Layer 5: 1 agent (`/test-strength-audit`) (~10 min; can run during implementation)

**Total ~13-14 parallel agents** (vs `.A`'s 11 = 5 Phase 0 + 6 Phase 2). Heavier; reflects wide blast radius. Total wall clock ~1-1.5h dispatched in parallel batches.

---

## Step-by-step pickup workflow

### Step 0 — Orient + verify state (mandatory)

Run state-verification block at top of this handoff. Confirm `39b9947` HEAD + `v5.15.5.F.4d.1.A` tag + 3196/0 tests + clean working tree.

Read in parallel the required-reading list. Per `feedback_compaction_degrades_treat_handoffs_as_hints`: VERIFY all handoff claims against actual files at HEAD before acting.

### Step 1 — Recreate TaskList

Suggested tasks:

| Subject | Status | Depends on |
|---|---|---|
| Verify state + read required-reading + handoff | pending → in_progress | — |
| Fire Layer 1 + 1+ext `/precoding-audit-gate` for `.B` (6 parallel agents) | pending | Step 1 |
| Fire Layer 2 `/dependency-chain-trace` for STAMP_BOUND_CFG_DERIVED | pending | Step 1 |
| Fire Layer 3 Level 4 codebase pattern survey (5 parallel agents) | pending | Step 1 |
| Fire Layer 4 `/plan-context-sweep` | pending | Step 1 |
| Synthesize all audit findings into one master doc | pending | All Layer 1-4 complete |
| Triage findings with Caramel | pending | synthesis complete |
| Update `.B` plan body v1.2 → v1.3 if findings warrant | pending | triage complete |
| Tag `pre-v5.15.5.F.4d.1.B` + start coding `.B` | pending | plan body updated |
| Implement `.B` Steps 0-14 (per plan body) | pending | pre-tag created |
| Fire Layer 5 `/test-strength-audit` during/after implementation | pending | implementation in flight |
| Build verify + tag `v5.15.5.F.4d.1.B` + postmortem | pending | implementation done |
| `.C` audit cycle | pending — blocked by `.B` ship | — |

### Step 2 — Fire Layer 1-4 audits IN PARALLEL BATCHES

Batch 1 (5 agents, Layer 1):
- parity-check
- trace-deps
- readiness
- dod-audit
- merge-scan

All scoped to `.B` plan body + `.B` sidecar.

Batch 2 (in parallel with Batch 1; 1 agent, Layer 1+ext):
- accounting-audit (scope: wire-format + HMAC-chain modules)

Batch 3 (in parallel with above; 1 agent, Layer 2):
- dependency-chain-trace on `STAMP_BOUND_CFG_DERIVED` propagation

Batch 4 (after Batches 1-3 return OR in parallel if context budget allows; 5 agents, Layer 3):
- D1: tt::cfg_emit_synthetic_field<T> coverage
- D2: β4 FOREACH_DRIFT_GATE dispatch
- D3: Winsor parse-time validation placement + sister cohorts
- D4: CFG_DRIFT_AUTOPOPULATE family + consumer site enumeration
- D5: Stage 2/3 ACTIVE DESIGN_SPECs alignment

Batch 5 (after Batch 4 OR in parallel; 1 agent, Layer 4):
- plan-context-sweep across `.B`/`.C`/`.D`/`.F.4e`/`.F.4f+`

**Total: 13 agents (6 + 1 + 1 + 5 = 13).** Fire in 2-3 message batches to avoid overwhelming the orchestrator.

### Step 3 — Synthesize audit findings

Master synthesis doc at `plans/v5.15-live-readiness/plan_checks/2026-05-17-v5.15.5.F.4d.1.B-audit-synthesis.md`. Structure per `.A`'s synthesis:
1. Per-audit verdict table
2. Combined verdict (GREEN/YELLOW/RED)
3. CRITICAL findings (convergent across audits)
4. HIGH findings (should resolve before coding)
5. MED findings (Step 0 polish)
6. LOW findings (notes/future-work)
7. Plan amendment list (ordered)
8. Recommendations

### Step 4 — Triage findings with Caramel

Per `feedback_consult_on_audit_findings` + `feedback_no_question_boxes`:
- Present inline (NO AskUserQuestion modal)
- Per finding: file:line + severity + proposed fix
- Iterate until plan locked
- Apply `feedback_auto_pick_future_oriented` — auto-pick when future-oriented choice is clear; surface only genuine ambiguities

### Step 5 — Update `.B` plan body if needed

Apply triage decisions. If findings reshape scope (Path γ-style structural pivot), update plan body v1.2 → v1.3. If findings are minor (mechanical), apply during implementation.

Always preserve `.B`'s already-recognized body residual cleanup annotations (top of plan body + sidecar; Path γ+ v2 LOCKED).

### Step 6 — Tag pre-coding rollback anchor

```bash
cd /home/caramel/code/FoxML_Trader_v2
git tag -s -a pre-v5.15.5.F.4d.1.B -m "pre-coding rollback anchor for v5.15.5.F.4d.1.B migration + consumer"
```

### Step 7 — Implement `.B` per plan body Steps 0-14

Per `.B` plan body v1.2 + body residual cleanup annotations. Wide blast radius (~6-8h focused). Discipline reminders:
- Mid-flight tags per Phase (Step 2 ML_CFG_FLAG / Step 3 bitmap walker / Step 4 22-row migration / Step 6 parity gaps / Step 7 retro / Step 9 consumer / Step 11 β4)
- Build verify after each Phase
- Per-Phase test count growth tracking
- HMAC chain byte preservation via existing Layer 2 locale pin (StampBoundDerivedFilter.hpp at `.A`)
- Test fixture sweep — some existing fixtures reference `FOREACH_STAMP_BOUND_CFG_COUNT` or per-field absence; sweep for stale assumptions
- Invariants helper (wire_format_invariants.hpp at `.A`) verifies populated body shape as migration progresses

### Step 8 — Fire Layer 5 `/test-strength-audit` periodically during implementation

After each major Phase, fire `/test-strength-audit` against the diff. Catches weakening (== → >= without justification; deleted checks without redundancy explanation). Pre-merge verification per `feedback_consult_on_audit_findings` discipline applies.

### Step 9 — Build verify + ship close

Per `.A` cycle:
- 5 binaries clean (test/gui/suite/tsan/asan)
- calls_graph_diff verify (hot path UNTOUCHED OR documented changes per HOT_PATH_CHANGELOG)
- CI checks PASS (check_meta_registry.py + check_per_core_registry_integrity.py)
- Version.hpp bump 5.15.5.F.4d.1.A → 5.15.5.F.4d.1.B
- Commit + tag (annotated + GPG-signed) + push (await Caramel authorization per CLAUDE.md push-discipline)
- Postmortem at `plans/v5.15-live-readiness/postmortems/2026-05-XX-v5.15.5.F.4d.1.B-postmortem.md`
- Auto-writes per CLAUDE.local.md contracts (CHANGELOG, FEATURE_LOOKUP, DESIGN_SPECS README, sprint state)
- `/sync-workspace`

### Step 10 — `.C` audit cycle next

Per per-sub-ship cycle discipline locked 2026-05-17.

---

## Sub-ship cycle discipline reminders

Per CLAUDE.local.md "Process & sprint" rule (set 2026-05-17):

- Each sub-ship: audit → triage → update → implement → ship → postmortem
- Natural shipping boundary forces forward motion
- Without it: Level 5/6/7 audits would find more sister drifts indefinitely
- Discipline: "audit ENOUGH to make THIS sub-ship structurally sound; bank rest as TECH_DEBT for future cycles"

Per `feedback_auto_pick_future_oriented` (set 2026-05-17):

- At each design choice: ask "which option is more future-oriented per trajectory + sister patterns + framework-discipline alignment?"
- If clear from context: auto-pick + state alternative considered + state rationale chosen
- Only escalate genuine sharp trade-offs OR true uncertainty about which is more future-oriented
- Never punt with "your call" framing on auto-decidable decisions

---

## Path γ correction context (for fresh session orientation)

If a fresh session reads ONLY this handoff (without the full audit reports), the Path γ correction history is:

1. `.A` v1.2 plan body originally proposed building parallel walker macros (`DerivedFilterFramework.hpp` 3 macro variants + `DerivedFilterRoster.hpp` Level-1 meta-registry + new FOREACH_REGISTRY row)
2. Pre-coding `/precoding-audit-gate`'s `/merge-scan` caught structural critique: the proposed walker DUPLICATES existing compile-time infrastructure at `CfgFieldRegistry.hpp:1020-1159` (`FOREACH_METADATA_BIT` + `cfg_compute_mask` + `CFG_FIELD_FOR_EACH_SET_BIT` since `.F.4c.3`)
3. Path γ correction: use existing infrastructure (1-row FOREACH_METADATA_BIT addition; consumer via CFG_FIELD_FOR_EACH_SET_BIT; reusable invariants helper)
4. Path γ+ Level 2 extension: composition audit registry (Gap 1 mitigation)
5. Path γ+ v2 (final ship scope): + 3 DRIFT-MAJOR DESIGN_SPECs status corrections + cli_explain_mask bug fix + stamp_emit_mask alias delete + 2 new TECH_DEBT entries + STALE banner on `.F.4e` + body residual cleanup annotations on `.B`/`.B-examples`/`.D`

**Bottom line: `.B`'s scope per plan body v1.2 (24-row migration + ML_CFG_FLAG sig + tt::cfg_emit_synthetic_field<T> + β4 cohort gate sidecar + Winsor parse-time validation + 12+ consumer site migration + legacy registry empty-out + CFG_DRIFT_AUTOPOPULATE) is UNCHANGED from before Path γ correction. The "simplification" is that `.B`'s CFG_DRIFT_AUTOPOPULATE walker uses CFG_FIELD_FOR_EACH_SET_BIT (existing infra) instead of a custom framework walker.**

The body residual cleanup annotations on `.B` plan body v1.2 + sidecar v1.1 enumerate the SUPERSEDED v1.0 framework macro references that need mechanical correction at `.B` update step (Step 5 of this handoff workflow).

---

## Decision points carried into next session

1. **Push `.A` commit + tag to engine remote?** Per Caramel's "lets commit and do a handoff" — interpretation ambiguous. Local commit + tag done; not yet pushed. CLAUDE.md default says don't push without explicit authorization. **Action: confirm with Caramel at session start.**

2. **In-depth audit scope** — 6 layers proposed in this handoff. If Caramel wants to narrow/widen, surface options at session start.

3. **`.B` test count expectation** — ~3196 + ~25-30 new = ~3221-3226 controller_test post-`.B`.

4. **TECH_DEBT-091 (plan-context drift detection cadence)** — opened at `.A`; possibly fold into `.F.4f` cleanup OR codify earlier as `pattern-codification-lifecycle.md` Stage 3 substep.

5. **TECH_DEBT-092 (X_GEN_* namespace collision CI)** — effectively closed by C++ duplicate-symbol detection at `.A`; status update may close fully (no separate CI check needed).

---

## Notes for future-Claude

- Per `feedback_compaction_degrades_treat_handoffs_as_hints`: verify ALL handoff claims (state values, file:line refs, audit synthesis claims, decision summaries) against actual files at HEAD before acting.
- The audit-update-implement-ship cycle discipline is locked; don't auto-proceed past the audit→triage step without operator consult.
- `.B` is THE wide-blast-radius ship of the umbrella; per plan body "MED — wide blast radius" + "per-Phase mid-flight tags recommended".
- Caramel's collaboration norms (from MEMORY.md):
  - Address as Caramel / she / her
  - No AskUserQuestion modal — inline scrollable
  - Auto-pick future-oriented when clear; surface only genuine ambiguities (NEW at `.A`)
  - Don't measure structural work by LOC
  - Deep design work has intrinsic value
  - MVP → professional transition phase

---

## Pickup prompt for fresh session (copy-paste)

```
I'm picking up v5.15.5.F.4d.1.B in-depth audit per handoff at
plans/v5.15-live-readiness/handoffs/2026-05-17-v5.15.5.F.4d.1.B-audit-handoff.md.

Predecessor v5.15.5.F.4d.1.A shipped 2026-05-17 LOCALLY (engine commit
39b9947 + GPG-signed tag v5.15.5.F.4d.1.A; NOT pushed; awaiting operator
authorization per CLAUDE.md push-discipline). Path γ+ v2 framework infra:
1-row FOREACH_METADATA_BIT + StampBoundDerivedFilter.hpp consumer + reusable
wire_format invariants helper + CFG_COMPOSE_AUDIT_DECISIONS audit registry +
H16 static_assert + cli_explain_mask bug fix + stamp_emit_mask delete +
6 TECH_DEBT entries opened + 4 DESIGN_SPECs corrected + 2 NEW Stage 3 first
reference + auto-pick-future-oriented operating principle codified.
3196 tests + 5 binaries clean. My job: 6-layer in-depth audit of .B per
handoff specification + triage + update + implement + ship cycle.

## Step 0 — orient + verify state (MANDATORY)

Verify:
- git rev-parse HEAD should be 39b9947
- git status --short should be empty
- head -10 Version.hpp should show 5.15.5.F.4d.1.A
- git tag --sort=-creatordate | head -3 should show v5.15.5.F.4d.1.A at top
- ./build.sh test 2>&1 | tail -3 should show 3196 passed, 0 failed

Required reading (parallel batch):
- CLAUDE.md + CLAUDE.local.md + MEMORY.md
- feedback_auto_pick_future_oriented.md
- THIS handoff + .A postmortem + .A findings doc
- .B plan body v1.2 + sidecar v1.1 (note body residual cleanup annotations)
- 6 DESIGN_SPECs from .A (composed-filter-mask + wire-format-invariants-helper NEW;
  metadata-bit-driven v1.2 + framework-composition v1.1 + 3 status-corrected)
- Sub-master v1.2

## Step 1 — Recreate TaskList per handoff "Step 1" section

## Step 2 — Confirm Caramel decision on push (.A commit + tag to engine remote)

Per handoff Decision point #1. Inline question; don't auto-proceed.

## Step 3 — Fire 6-LAYER IN-DEPTH AUDIT (13 parallel agents in 2-3 batches)

Per handoff Step 2 "In-depth audit specification (6 LAYERS)" section.
Total ~13 agents:
- Layer 1: 5 standard /precoding-audit-gate audits
- Layer 1 +ext: 1 /accounting-audit
- Layer 2: 1 /dependency-chain-trace on STAMP_BOUND_CFG_DERIVED
- Layer 3: 5 Level 4-style codebase pattern survey (D1-D5)
- Layer 4: 1 /plan-context-sweep
- Layer 5: /test-strength-audit later during implementation

## Step 4 — Synthesize audit findings + triage with Caramel per cycle

Per handoff Step 3-4. Don't auto-proceed past triage.

## Step 5+ — Update .B plan body if needed → tag pre-v5.15.5.F.4d.1.B → implement → ship → postmortem → /sync-workspace

## Operator collaboration norms

- Address Caramel as Caramel / she / her
- No AskUserQuestion modal — inline scrollable
- Auto-pick more future-oriented when trade-off clear; surface only genuine ambiguities
  (per feedback_auto_pick_future_oriented at .A)
- Per-sub-ship audit→update→implement→ship cycle prevents endless planning loops
- After per-step audit checkpoints, ALWAYS consult before proceeding
- Don't measure structural work by LOC
- Deep design work has intrinsic value

Good luck.
```

---

**End of handoff.** `.A` shipped locally; `.B` in-depth audit + cycle next session.
