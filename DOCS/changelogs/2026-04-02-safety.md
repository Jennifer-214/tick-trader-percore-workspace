# v3.6.1 — Exit Record Slot Reuse Fix + Fee Floor Enforcement (2026-04-02)

Four safety bugs found and fixed. All related to positions exiting through the hot-path exit gate.

## Bug 1: Kill Switch False Trigger (Exit Buffer Equity Gap)

**Root cause:** PositionExitGate (hot path) clears the bitmap bit instantly. DrainExits (slow path) credits balance later. Between these events, `equity = balance + Portfolio_ComputeValue` is missing the exited position's value — it's in neither. The kill switch sees a phantom equity crash and fires.

**Previous fix (approximate):** Added `current_price × qty` for pending exits. Wrong because it uses market price instead of actual exit price, and ignores fees and slippage.

**Airtight fix:** New `ExitBuffer_PendingProceeds()` helper in Portfolio.hpp computes exact net proceeds matching what `RecordExit` will credit: `(exit_price - slippage) × qty - fees`. Used in the hot-path kill check. Zero cost on the common case (no pending exits).

**Files:** Portfolio.hpp (helper), PortfolioController.hpp (kill check ~line 654)

## Bug 2: Win/Loss Misclassification

**Root cause:** `RecordExit` counted wins as `reason == 0` (TP exit) regardless of P&L. A TP exit where round-trip fees exceeded gross profit was counted as a "win" — inflating win rate and masking fee floor violations.

**Fix:** Win requires TP exit AND positive P&L. A TP exit that loses money after fees counts as a loss.

**Files:** PortfolioController.hpp (RecordExit ~line 370)

## Bug 3: Fee Floor Violation After Regime Transition

**Root cause:** Fee floor (TP >= entry + entry × fee_rate × 3) was only enforced at fill time. Three regime transitions tighten TP with `FPN_Min` without re-checking:
- TRENDING/MILD_TREND/DOWNTREND → RANGING (line 408)
- TRENDING → MILD_TREND (line 432)
- any → TRENDING_DOWN (line 446)

When volatility drops after fill (common: enter during spike, regime calms), the tightened TP falls below fee breakeven. Position hits TP, exits for gross +$5, pays $6 in fees, net -$1.

**Live example:** Entry $66,749, exit $66,872 (TP), fee $4.27+$1.50 = $5.77, gross +$5.54, net -$1.93. The TP was tightened by a regime transition below the $200 fee floor.

**Fix:** Added fee floor enforcement (`FPN_Max(tp, fee_floor)`) after every `FPN_Min` on TP in `Regime_AdjustPositions`. The fee floor is computed identically to fill time.

**Files:** Strategies/RegimeDetector.hpp (3 locations in Regime_AdjustPositions)

## Audit Results

Also audited these locations — confirmed safe:
- **Slow-path kill check** (line 1252): runs after DrainExits, no gap
- **Peak equity tracking** (line 1244): runs after DrainExits, no gap
- **Circuit breaker** (line 915): uses cached portfolio_delta, inherently approximate, acceptable
- **TUI snapshot** (EngineTUI.hpp:866): copies on slow path after DrainExits, no gap
- **Momentum_ExitAdjust**: only widens TP (FPN_Max), can't violate fee floor
- **EmaCross_ExitAdjust**: only widens TP (FPN_Max), can't violate fee floor

## Bug 4: Exit Record Slot Reuse (the big one)

**Root cause:** ExitRecord stored only `position_index` and `exit_price`. All other position data (entry_price, quantity, entry_fee) was read from the position slot at drain time. But the slot can be reused by a new fill between exit gate and drain.

With `max_positions=1`, every exit frees slot 0 and every fill takes slot 0. The race window is the entire slow-path interval (~100 ticks / ~30 seconds):

```
PositionExitGate     → clears bit 0, writes exit_buf {idx=0, exit_price}
Fill consumption     → writes NEW position into slot 0 (OVERWRITES)
DrainExits           → reads slot 0 → gets WRONG entry_price, quantity, entry_fee
```

**Live impact:** Position A ($66,652 entry, 0.045 qty) exits at SL. Position B ($66,480 entry, 0.031 qty) fills same slot. DrainExits computes P&L against B's entry, credits wrong amount. Balance goes haywire ($936 phantom drawdown on ~$20 real losses). Kill switch fires at 9% on a 0.2% real loss. Position B gets sold again later (double sell).

**Fix:** ExitRecord now snapshots `entry_price`, `quantity`, `entry_fee`, `pair_index` at exit time in PositionExitGate. RecordExit reads exclusively from the record, never the slot. ExitBuffer_PendingProceeds reads from the record. Zero slot dependency after bitmap clear — the slot can be reused immediately with no race.

This was a latent bug since the exit buffer was designed, not introduced by any specific change.

**Files:** Portfolio.hpp (ExitRecord struct, PositionExitGate, PendingProceeds), PortfolioController.hpp (RecordExit, DrainExits, time-based exit), main.cpp (session close)

## Tests

20 new assertions (259 → 279):
- Exit buffer equity gap: pending proceeds bridge the gap exactly
- Win/loss: fee-dominated TP exits classified as losses
- Fee floor: all three regime tightening paths enforce TP >= 3× round-trip fees
- Slot reuse regression: exit A, fill B into same slot, verify P&L uses A's data
