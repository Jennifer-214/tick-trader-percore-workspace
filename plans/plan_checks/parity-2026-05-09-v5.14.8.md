# /parity-check report — v5.14.8 — 2026-05-09

## Plan summary
- HEAD: `1b4563d` (v5.14.6 close)
- Test suite: 2430 base tests
- Audit scope: stamp + features + scaler (3 of 5 sub-extensions in plan)
- Cross-check baseline: post-v5.9.4a protections inventory + v5.11.18a feature-mask binding

## Findings by severity

### CRITICAL (5 new findings)

**CRITICAL-1: Stamp body schema NOT YET EXTENDED with v5.14.8 new fields**
- Plan claims 5 new stamp body fields (scaler_fit_data_hash, removal_reasons_csv, environment_meta×5) but NONE are defined in ModelStampResult or StampInferenceCfgInputs structs
- File locations: ModelStampResult at `ML_Headers/ModelInference.hpp:1187-1336`; StampInferenceCfgInputs at lines 1922-2073
- Status: Fields do not exist (grep returns zero hits)
- Cross-ref: ALREADY-PROTECTED-by-v5.9.3a (Surface G pattern exists; plan must reuse it)
- Fix: Add struct field definitions with `has_*` forward-compat flags per v5.9.3a precedent

**CRITICAL-2: FEATURE_REGISTRY_HASH STABILITY NOT VERIFIED for max_staleness_minutes column**
- Plan adds 7th column to FOREACH_FEATURE (max_staleness_minutes) and claims hash stays STABLE by using caller-defined `#define X(...)` macros
- Problem: The hash-compute X-macro at line 562 is NOT shown in plan's 5 "verified call sites" (lines 69-77)
- Risk: Hash compute runs with wrong signature or column count, reads garbage
- Gap class: Missing 6th X-macro call site; plan audited only 5 of 6
- Fix: Plan MUST show hash-compute X-macro update (line 562) with 7-param signature

**CRITICAL-3: Stale-feature observability surface MISSING — no PerCoreSnap field**
- Plan adds `stale_feature_events_total` counter (line 86) + Health_LogCriticalRateLimited, but PerCoreSnap struct does NOT have the field
- Per Section J discipline: Each silent-failure mode needs distinct PerCoreSnap field
- Current state: `ml_nan_feature_events` exists (line 1095 of EngineTUI.hpp); `ml_stale_feature_events` DOES NOT
- Fix: Add PerCoreSnap field + populator + ML Status panel branch (30 min)

**CRITICAL-4: Stamp body CANONICAL-ORDER POSITION NOT ANCHORED**
- Plan claims new fields land at "positions 24+" but doesn't verify placement in FOREACH_STAMP_BOUND_CFG or show struct layout
- Problem: 5 new fields are NOT cfg-bound (not in FOREACH_STAMP_BOUND_CFG) and NOT architectural (not like expected_role)
- Risk: Struct ordering mismatch → HMAC signature invalid → verifier fails
- Fix: Plan v5.14.8 Step 0 MUST anchor: position (24+N+6) for scaler_fit_data_hash, (24+N+7) for removal_reasons_csv, etc.

**CRITICAL-5: Production-caller field-population gap — scaler_fit_data_hash WIRING**
- Plan Step 3 shows Python trainer hashing + C++ verifier checking, but NO CITATION of which caller populates `inf.has_scaler_fit_data_hash`
- Class: v5.9.5b shape (field in struct, verifier reads it, production caller never populates)
- Expected: BacktestPanels@3206 or Backtest_RunFullValidation must populate
- Gap: Plan jumps from trainer → stamp structure → verifier without showing the middle production caller
- Fix: Plan MUST cite exact BacktestPanels line (probably after scaler_sha256@3256) where scaler_fit_data_hash is wired

### HIGH

**HIGH-1: Scaler_fit_data_hash production-caller audit incomplete (Section L)**
- Plan Step 3 missing: "which caller(s) will populate inf.has_scaler_fit_data_hash + inf.scaler_fit_data_hash in StampInferenceCfgInputs?"
- Pattern: BacktestPanels@3253-3256 shows existing scaler_sha256 wiring; new hash must follow
- Fix: Plan Step 3 MUST add code block for BacktestPanels caller (15 min)

### MEDIUM

**MEDIUM-1: Stamp body canonical-order anchor missing (design doc gap)**
- 5 new fields lack explicit position assignment in HMAC-covered body structure
- Precedent: v5.14.2.E.2.B (lines 1305-1328) + v5.14.3.B (lines 1330-1336) show manual position discipline
- Fix: Plan MUST show full struct declarations with has_* flags + field order
- Severity: MEDIUM because pattern is established; just needs pre-identification

**MEDIUM-2: Health_LogCriticalRateLimited storage location not pre-verified**
- Plan Step 1 uses static `uint64_t last_*_log_us = 0;` without citing where it lives
- Pattern: Already established in v5.9.0b (ml_model_load_failed uses same); just needs explicit cite
- Fix: Plan Step 1 MUST cite existing site (e.g., CoreModelZoo.hpp line ~200) or add declaration

## Cross-cutting audit results

| Section | Finding | Verdict |
|---|---|---|
| B (Feature pipeline) | FOREACH_FEATURE X-macro hash-compute signature not shown | GAP — CRITICAL-2 |
| D (Scaler sidecar) | scaler_fit_data_hash production-caller wiring omitted | GAP — CRITICAL-5 + HIGH-1 |
| E (Stamp body) | New field struct definitions + canonical-order positions missing | GAP — CRITICAL-1 + CRITICAL-4 |
| J (Observability) | stale_feature_events_total counter incomplete (missing PerCoreSnap field + populator + panel) | GAP — CRITICAL-3 |
| L (Production-caller audit) | scaler_fit_data_hash not wired through BacktestPanels | GAP — CRITICAL-5 + HIGH-1 |

## Overall verdict

**DRIFT-RISK → CONDITIONAL PASS**

Plan is INTENT-SOUND (all 5 extensions reuse v5.9+ patterns; no novel parity surfaces) but SPEC-INCOMPLETE.

5 blockers must be addressed before v5.14.8.A code lands:
1. CRITICAL-2: Show FOREACH_FEATURE hash-compute X-macro update (line 562) with 7-param signature
2. CRITICAL-1: Show struct field definitions (ModelStampResult + StampInferenceCfgInputs) with Surface G has_* flags
3. CRITICAL-4: Add stamp body canonical-order position anchor subsection
4. CRITICAL-5: Add BacktestPanels production-caller code block (scaler_fit_data_hash wiring)
5. CRITICAL-3: Add PerCoreSnap field + populator + ML Status panel branch

Estimated effort to close plan gaps: 2.5 hours (documentation + plan refinement, no code yet)

Plan approval: Conditional — approve with revision per blockers. No code should land until plan revisions complete.
