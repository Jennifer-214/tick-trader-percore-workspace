# v5.15.5.F.4c.3 — Global vs per-core cfg registry split (architectural)

**Drafted:** 2026-05-15 (post `.F.4c.1` paper-test surfaced cfg-surface architectural debt)
**Branch:** stay on `feat/v5.15-live-readiness` (sprint branch)
**Predecessor:** `v5.15.5.F.4c.1` shipped 2026-05-15 (tag `v5.15.5.F.4c.1`; engine `88043ea`)
**Successor:** `v5.15.5.F.4d` (wire-format framework + STAMP_BOUND derived filter — built natively on per-core registry from day one)
**Rollback anchor:** tag `pre-v5.15.5.F.4c.3` at Step 0
**Risk:** MED-HIGH — touches cfg surface, parser, ControllerConfig struct, per-core resolver, stamp body emit, drift check, Settings panel, backtest path, ~50-100 test fixtures, engine.cfg file format. Hot path UNTOUCHED. Pre-coding audit gate MANDATORY before code lands.
**Effort:** structural ship — measured by classes closed + framework primitives composed, not LOC. Largest single architectural change in the `.F` umbrella.
**Skipped predecessor:** `.F.4c.1.1` polish — rationale in `postmortems/2026-05-15-skipped-v5.15.5.F.4c.1.1-rationale.md`. Three planned polish items absorbed naturally into this ship.

---

## Why this ship exists

`v5.15.5.F.4c.1` paper-test surfaced two compounding cfg-surface issues:

1. **Visual:** Global tab has many duplicate empty section headers (pre-existing `.F.4c` bug worsened by `.F.4c.1` adding 19 cohort rows). Operator can't usefully scan the Settings panel.
2. **Architectural:** Trading parameters live at global scope with optional per-core override. Each core nominally has its own ridge_lambda / confidence_* / thompson_* / bandit_algorithm, but the cfg surface presents them as "set a global, optionally override per-core." Operator can't reason cleanly about "what config is this core actually running."

Caramel's stated architectural intent (2026-05-15):
> *"global settings honestly shouldnt have any settings that control actual trading strats, it should mostly be the gui settings right? like all actual trading settings should be only per core right? for finer control, no global overrides, splits into a single global registry, and then a reuseable per engine core registry"*

The structural answer: **split `FOREACH_CFG_FIELD` into TWO registries** with clear scope discipline. System/GUI/operational/recording knobs stay global (operator sets once); trading/ML/risk/regime knobs become per-core authoritative (each core has its own row instance — NO global default, NO override mechanism). The "set globally + override per-core" pattern that's been the source of Class 24 recurrence dies structurally.

This is the foundation `.F.4d`'s wire-format framework + `.F.4e`'s GUI metadata derived filters + `.F.4d.1`'s bandit ghost-training expansion all consume. Built on the wrong registry shape = retrofit pain at every subsequent ship. Locking the architecture NOW is correct sequencing.

---

## Scope (locked 2026-05-15)

### A. Two-registry framework infrastructure

| Component | LOC | Notes |
|---|---|---|
| `FOREACH_GLOBAL_CFG_FIELD(X)` X-macro registry | (new declaration; ~25-30 row migrations) | **Truly global only — system/training/recording/engine-wide mode/acknowledgments.** Trading + risk knobs ARE NOT here. Examples: system (`num_execution_cores`, `engine_mode`, `engine_arch`, `require_mlockall`, `init_arena_use_hugepages`, `slow_path_max_secs`, `slow_path_pin_offset`, `sharded_force_synthetic`); training (`xgb_train_nthread`, `xgb_eval_nthread`, `csv_load_workers`, `multi_horizon_max_threads`, `feature_collect_max_gb`, `wf_split_max_gb`, `held_out_max_gb`, `xgb_subsample`, `xgb_colsample_bytree`, `xgb_min_child_weight`, `xgb_seed`, `xgb_tree_method`, `auto_stamp_on_held_out`, `held_out_stamp_secret`, `held_out_gate_strict`); recording (`record_ticks`, `record_depth`, `record_max_days`); engine-wide mode (`trading_mode`, `model_verify_strict`, `reconcile_mode`, `oms_event_log_mode`, `oms_bench_enabled`, `pay_fees_in_bnb`, `use_aot_inference`); acknowledgments (`acknowledge_hardcoded_strategy_in_live`, `acknowledge_hot_swap_with_open_positions`, `allow_cross_major_engine`). |
| `FOREACH_PER_CORE_CFG_FIELD(X)` X-macro registry | (new declaration; ~70-80 row migrations including symbol axis + ml_cfg_flags bool migration via A2 hybrid) | **Trading / ML / risk / regime / strategy / entry / exit / KILL SWITCHES + DRAWDOWN + SYMBOL per-core.** Operator-refined 2026-05-15: kill switches + max-drawdown moved here (different cores warrant different risk envelopes). Examples: strategy (`strategy`, `model_path`, `model_dir`, `horizon_list`, `ensemble_blend_mode`, `disabled_horizons`); trading (`take_profit_pct`, `stop_loss_pct`, `fee_rate*`, `slippage_pct`, `risk_pct`); entry (`entry_offset_pct`, `offset_min/max`, `volume_multiplier`, `spacing_multiplier`, `min_long_slope`, `min_buy_delta`, `vwap_offset`, `min_stddev_pct`); regime thresholds; ML (`ml_*`, `bandit_algorithm`, `thompson_*`, `ridge_*`, `winsor_*`, `confidence_*`, `barrier_blend_mode`, `exit_*`); exits (`tp_*`, `sl_*`, `hold_score`, `time_exit`, `partial_exit_*`, `breakeven_*`, gate recovery); strategy-specific (`momentum_*`, `simpledip_*`, `mr_*`, `emacross_*`); **per-core risk gates (NEW): `kill_switch_daily_loss_pct`, `kill_switch_drawdown_pct`, `max_drawdown_pct`, `max_exposure_pct`, `enable_mtm_kill_switch`**; **symbol axis (NEW): `symbol` migrated per-core with boot-time uniformity check until multi-symbol DataStream support**; **A2 bitmap-bool migration (NEW): all 12 ml_cfg_flags bits (ridge_within_horizon, ridge_across_horizons, confidence_composite_enabled, exit_blender_mode, bandit_enabled, etc.) migrate to flat KIND_BOOL rows in the per-core registry; runtime bitmap rebuilt from rows at slow-path rebuild**. |
| `g_global_cfg_field_descriptors[]` + `g_per_core_cfg_field_descriptors[]` | (auto-gen from each registry) | Same `CfgFieldDescriptor` struct from `.F.4b/.F.4c`; two arrays |
| Per-registry bitmap masks + composed views | (auto-gen via `FOREACH_METADATA_BIT` applied twice) | Each registry gets its own mask set: `g_global_cfg_render_mask` / `g_global_cfg_save_mask` / etc. + `g_per_core_cfg_render_mask` / ... |
| `cfg_field_names_unique` static_assert | (extended) | Per-registry uniqueness check (each registry's rows internally unique); cross-registry-name-collision is ALLOWED only if intentional (a global `xgb_seed` and per-core `xgb_seed` — but the discipline doc discourages this; one or the other) |
| `GlobalRenderTable<F>` + `PerCoreRenderTable<F>` | (function-pointer tables auto-gen) | Mirrors `CfgRenderTable<F>` from `.F.4c`; two instances |

`tt::cfg_parse_field<T>` / `cfg_save_field<T>` / `cfg_assign_field<T>` / `cfg_diff_field<T>` / `cfg_render_field<T>` are **UNCHANGED** — they're type-trait dispatch operating on `T&`, not registry-aware. Same primitives consume either registry.

### B. ControllerConfig<F> restructure

```cpp
// NEW shape — one PerCoreCfg<F> instance per execution core
template <unsigned F>
struct PerCoreCfg {
    // ~50 trading fields, X-macro-generated from FOREACH_PER_CORE_CFG_FIELD
    FOREACH_PER_CORE_CFG_FIELD(EMIT_CFG_STRUCT_FIELD)
};

template <unsigned F>
struct ControllerConfig {
    // ~15 global fields — X-macro-generated from FOREACH_GLOBAL_CFG_FIELD
    FOREACH_GLOBAL_CFG_FIELD(EMIT_CFG_STRUCT_FIELD)
    
    // Per-core trading config — N authoritative instances
    PerCoreCfg<F> cores[MAX_EXECUTION_CORES];  // 16
    
    // Non-cfg manual fields (per .F.4d MANUAL_FIELDS_INVENTORY discipline):
    //   - Runtime state, derived/computed, internal helpers
    //   - String-typed per-core fields not yet in X-macro framework
    //     (core_model_path[], core_model_dir[], core_horizon_list[],
    //      core_ensemble_blend_mode[], core_disabled_horizons[]) — migrate
    //     to FOREACH_PER_CORE_STRING_CFG_FIELD at .F.4e
};
```

Deletions from current `ControllerConfig<F>`:
- All scalar trading fields move to `PerCoreCfg<F>`
- `PerCoreOverrides<F>` struct DELETED (the override mechanism is no longer needed; per-core IS authoritative)
- `core_overrides[16]` field DELETED (replaced by `cores[16]`)
- `ControllerConfig_ResolveForCore` becomes trivial direct-read (or DELETED entirely if no callers remain that need a "resolved view")
- `PER_CORE_OVERRIDE_BITMAP_DOMAINS` macro DELETED (override-bitmap mechanism eliminated; bitmap-resident bools in `ml_cfg_flags` migrate to per-core registry directly as KIND_BOOL rows OR stay in ml_cfg_flags as per-core-instance bitmap field; decide at audit)

### C. Parser updates — `[core N]` section syntax + hard-break legacy

```
# engine.cfg new format
# Global section (no header — defaults at top of file)
num_execution_cores=4
engine_mode=sharded
require_mlockall=1
record_ticks=0
held_out_stamp_secret=path/to/secret
trading_mode=paper
kill_switch_daily_loss_pct=3.0

# Per-core sections
[core 0]
strategy=ml
model_dir=models/classification/multi_2year_01
risk_pct=15.0
take_profit_pct=3.0
stop_loss_pct=1.5
ridge_lambda=0.15
bandit_algorithm=1

[core 1]
strategy=ml
model_dir=models/classification/short_horizon
risk_pct=10.0
take_profit_pct=2.5
stop_loss_pct=1.0
ridge_lambda=0.20
bandit_algorithm=2

# ... [core 2], [core 3] sections similarly
```

Parser logic:
1. Lines before the first `[...]` header → parsed against `FOREACH_GLOBAL_CFG_FIELD` registry (writes to `ControllerConfig<F>` flat fields)
2. After `[core N]` header → parsed against `FOREACH_PER_CORE_CFG_FIELD` registry (writes to `cfg.cores[N].<field>`)
3. Unknown global key → ERROR with migration hint: `"key '<name>' moved to per-core scope at v5.15.5.F.4c.3. Place under a [core N] section. See migration guide."`
4. Unknown per-core key → ERROR with migration hint
5. `[core N]` where N >= num_execution_cores → WARN + ignore (operator might be staging future-core cfg)

**Hard-break per Caramel's directive 2026-05-15:** legacy `take_profit_pct=3.0` at global scope = ERROR. No backward-compat shim. Operator runs migration tool once.

### D. Engine.cfg migration tool — DROPPED FROM SCOPE

Per operator directive 2026-05-15: *"we dont really need this, its not like I have alpha"* — Caramel rewrites `engine.cfg` by hand for the new sectioned format. One-time operator cost; cleaner than carrying a tool that becomes stale + maintained-but-rarely-used.

What ships instead:
- Operator-facing migration guide at `workspace/DOCS/CFG_SCOPE_MIGRATION_GUIDE.md` (manual rewrite checklist + example new-format engine.cfg)
- The example engine.cfg in the repo is updated to the new sectioned format at this ship; serves as the migration reference

### E. Per-core stamp body emit + drift check

Each core's HMAC stamp covers ITS per-core cfg fields (filtered by STAMP_BOUND metadata):

```cpp
// At stamp emit time (per-core boot or model load):
void EmitCoreStamp(const PerCoreCfg<F>& core_cfg, int core_id, ...) {
    char body[4096];
    int n = build_canonical_body(body, sizeof(body), &core_cfg, core_id);  // walks STAMP_BOUND per-core rows
    // HMAC-SHA256 over body + secret
}

// At boot drift check:
for (int c = 0; c < num_execution_cores; ++c) {
    auto& core_cfg = cfg.cores[c];
    // Compare core_cfg fields against core's stamp; fire drift bits per Class 23/24 discipline
}
```

The wire format CHANGES at this ship: per-core stamps are emitted per core (4 stamps for 4 cores vs 1 unified stamp). Layer 5b hash recomputation required. Per-core stamp fixtures replace the v5.14 unified fixture.

**Open question for audit gate:** what's the byte layout of per-core stamps? Options:
- (a) Each core gets its own stamp file: `models/core_0.stamp` + `models/core_1.stamp` + ... — distinct files
- (b) One unified stamp file with per-core sections inside: `models/cores.stamp` containing 4 inline bodies
- (c) Hybrid — stamps in per-core model directories: `models/<core_N_model_dir>/cfg.stamp`

Likely (c) — fits the existing per-core model_dir convention; each core's stamp lives with its model.

### F. Settings panel updates

- **Global tab** renders ~15 system/GUI rows via `GlobalRenderTable<F>` + `g_global_cfg_render_mask` bitmap walker
- **Per-core tabs (Engine 0 / Engine 1 / etc.)** render ~50 trading rows via `PerCoreRenderTable<F>` + `g_per_core_cfg_render_mask` bitmap walker, with `cores[c]` as the field source
- Section headers auto-deduplicate naturally (each registry's rows are author-section-grouped at declaration; ~15 rows in 3-4 sections globally, ~50 rows in ~10 sections per-core)
- "Empty (0.00) means inherit from Global" hint at top of per-core tabs is DELETED — there's no inheritance anymore; each core's values are authoritative
- SliderFloat / SliderInt → InputFloat / InputInt (text-entry preference per Caramel 2026-05-15)
- Reset+Modified UI ships in this ship (the FPN<F>-vs-ImTextureRef operator== ambiguity from `.F.4c.1` gets resolved alongside the input-widget swap)

### G. Hot path verification (UNTOUCHED)

`tools/calls_graph_diff.sh` confirms hot path bytewise-identical:
- `BG_Evaluate` / `SG_Evaluate` already read from per-core cached params via seqlock
- `EventLoop_UpdateRollingStateOneCore` / `RebuildOneCore` slow path reads from `cores[c].<field>` directly (replaces resolve-from-global-or-override)
- No new state, no new branches in hot path

### H. ML side + backtest path update — done in same ship (inseparable)

Per operator directive 2026-05-15: *"document when we update ML side, or maybe we do both at once?"* — the ML-side read-site migration IS part of this ship. The fields moving global→per-core (ridge_*, confidence_*, thompson_*, bandit_algorithm, winsor_*, etc.) have read sites scattered across:

| File | Read sites |
|---|---|
| `ML_Headers/RidgeBlender.hpp` | ridge_lambda, ridge_cost_penalty, ridge_min_ic_floor reads |
| `ML_Headers/ConfidenceScore.hpp` | confidence_freshness_tau_secs, confidence_capacity_target_dollars, confidence_capacity_kappa, confidence_rmse_baseline reads |
| `ML_Headers/ThompsonBandit.hpp` | thompson_mu_prior, thompson_precision_prior, thompson_precision_obs reads |
| `ML_Headers/CoreModelZoo.hpp` | bandit_algorithm dispatch + reward update + winsor application |
| `Strategies/StrategyParameters.hpp` | composes per-core parameters from cfg reads |
| `Strategies/MLStrategy.hpp` | ml_buy_threshold, ml_tp_pct, ml_sl_pct reads |
| `CoreFrameworks/ControllerEventLoop.hpp` | slow-path per-core rebuild reads |

Each `cfg.<field>` read becomes `cfg.cores[c].<field>` (or via the per-core slice already in scope at the call site — many sites already have `core_idx` available). Mechanical via grep + targeted edits. ML-side audit is part of the verification gate.

**Why same-ship instead of split:** the global cfg fields literally disappear at this ship (moved to per-core). Any ML-side read that wasn't updated is a compile-error at this ship; nothing ships partially-broken. The split-ship alternative would require carrying both global + per-core in parallel for a window — exactly the architecture anti-pattern this ship is closing.

### H.b Backtest path update

`Backtest_Run` / `BacktestSharded_Run` + harness paths:
- Replace `cfg.take_profit_pct` reads with `cfg.cores[backtest_core_idx].take_profit_pct`
- Backtest cfg shape (`backtest.cfg`) may need its own section syntax; defer to `.F.4i` (backtest cfg integration plan)
- For now: backtest reads from the loaded engine.cfg's per-core sections matching the core index it's simulating

### I. Test fixture migration

~50-100 sites in `tests/controller_test.cpp` set cfg fields directly (`cfg.take_profit_pct = 3.0`). Mechanical migration via grep + replace:
- Trading fields: `cfg.<name>` → `cfg.cores[0].<name>` (most tests run with 1 core)
- Multi-core tests: explicit `cfg.cores[c].<name>` per core
- Global fields: unchanged

Audit script verifies all test compilation post-migration (build-clean is the gate; runtime semantics preserved by construction).

### J. DESIGN_SPECs codification

Two new DESIGN_SPECs ship Stage 2 DRAFT → Stage 3 ACTIVE at `.F.4c.3` close:

1. **`per-instance-registry-pattern.md`** (NEW) — the framework spec: X-macro registry that instantiates per-instance of some axis; per-core as first canonical application; future axes catalog (per-symbol / per-strategy / per-horizon / per-regime). Composes with bitmap dispatcher + tt:: dispatch + derived filter framework.
2. **`cfg-scope-discipline.md`** (NEW) — decision-discipline for choosing WHERE a cfg field lives (global vs per-core, future axes). Anti-pattern: "global default + per-core override" FORBIDDEN. Sister to `cfg-flag-eligibility-criteria.md` + `categorical-tag-applicability-pattern.md`. Closes Class 24 structurally.

Updates to existing specs:
- `universal-cfg-field-registry-pattern.md` § "Composes with per-core split (v5.15.5.F.4c.3)" — note dual-registry application
- `universal-registry-bitmap-dispatcher-pattern.md` § "Dual-registry application" — note dispatcher applies cleanly across multiple X-macro registries
- `DESIGN_SPECS/README.md` catalog — add 2 new entries, bump count

### K. Documentation updates

- **CLAUDE.md item 31** (framework discipline meta-principle) — note the per-instance dimension addition
- **CLAUDE.local.md** going-forward rules:
  - "Per-core scope by default for trading config" (set 2026-05-15, codified at .F.4c.3)
  - Sprint state tracker updates (current/next ship rows)
- **DOCS/RECURRING_BUG_PATTERNS.md Class 24** — note the per-core split is the structural close at `.F.4c.3`
- **DOCS/CHANGELOG.md** — ship row
- **DOCS/PARITY_ISSUES.md** scan — verify per-core stamp fixtures close any parity concerns
- **DOCS/TECH_DEBT.md** entries closed:
  - TECH_DEBT-063 (field_defs[] elimination) — partial close via the migration
  - Possibly new entries for `.F.4e` string-cfg per-core support

---

## Pre-coding audit gate (MANDATORY before code lands)

Per CLAUDE.local.md "Audit-driven pre-coding gate" + this ship's MED-HIGH risk + wire-format-affecting status:

Fire `/precoding-audit-gate` with:

```
plans/v5.15-live-readiness/subplans/2026-05-15-v5.15.5.F.4c.3-global-vs-per-core-registry-split.md
parity,trace,readiness,merge,dod
"Layer 5b hash recomputation per-core (stamp body format change)"
"ControllerConfig<F> struct layout change — sizeof + offset locks + cache alignment"
"Cfg section parser correctness — global-then-[core N]-then-[core N+1] state machine"
"Engine.cfg migration tool — round-trip legacy → new → loadable parity"
"Hot path BYTEWISE UNTOUCHED via calls_graph_diff"
"Backtest path migration — Backtest_Run reads from cores[backtest_core_idx]"
"Test fixture migration scope — ~50-100 sites, mechanical via grep"
"Bitmap-resident bool migration LOCKED to A2: ml_cfg_flags bits flat KIND_BOOL rows in per-core registry; runtime bitmap rebuilt from rows at slow-path rebuild; hot path branchless mask dispatch UNCHANGED"
"Symbol axis migration to per-core with boot-time uniformity check (single-symbol DataStream constraint until multi-symbol ship)"
"Cfg-scope-discipline.md anti-pattern enforcement — no field appears in BOTH registries"
"DESIGN_SPECs DRAFT v1.0 → Stage 3 transition criteria"
```

Targeted audit foci:

1. **`/parity-check`** — Layer 5b methodology for per-core stamps (separate from `.F.4d`'s unified-stamp Layer 5b). Per-core stamp fixture procurement (4 stamps for 4 cores). HMAC byte preservation across the registry split (each core's emit path produces bytewise-identical output to the would-be-legacy unified emit when collapsed). v5.14 unified stamp fixture migrates to v5.15.5.F.4c.3 per-core fixture; cross-version compat NOT required (hard-break ship; v5.14 stamps don't load).

2. **`/trace-deps`** — every API in this plan exists at current HEAD `88043ea` (post `.F.4c.1`). `PerCoreOverrides<F>` → `PerCoreCfg<F>` rename verified. `ControllerConfig_ResolveForCore` callers enumerated; either updated to direct-read or deleted.

3. **`/readiness`** — 28+3 check pass. Cold-pickup completeness. Test count baseline ≥3144 post `.F.4c.1`. Rollback anchor `pre-v5.15.5.F.4c.3` specification. Effort vs LOC reconciliation (structural ship; LOC incidental).

4. **`/merge-scan`** — verify the migration tool reuses existing parser logic (DRY); per-core stamp emit reuses existing canonical-body builder; Settings panel global + per-core tabs share the same render walker template (parameterized by registry).

5. **`/dod-audit`** — bitmap-resident bool decision (cfg.ml_cfg_flags as per-core bitmap field vs migrating each bit to KIND_BOOL per-core row). Cache alignment of `cores[16]` array (`PerCoreCfg<F>` size ÷ 64 → cache-aligned per-core fits AVX-512 boundary). Class 23 prevention still intact at every NEW tt:: dispatch consumer.

Synthesis target: GREEN expected; flag any wire-format-byte-preservation or hot-path-touch concerns as RED for re-design.

Caramel-consult on synthesis findings BEFORE coding starts (per `feedback_consult_on_audit_findings`).

---

## Step 0 — Foundation

### 0.A — Rollback anchor + baseline (5 min)

```bash
cd /home/caramel/code/FoxML_Trader_v2
git tag pre-v5.15.5.F.4c.3
./build.sh test && ./build/controller_test 2>&1 | tail -5    # expect 3144 from .F.4c.1
cat Version.hpp                                                # expect 5.15.5.F.4c.1
```

### 0.B — Confirm DESIGN_SPECs in workspace (5 min)

```bash
ls /home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/per-instance-registry-pattern.md
ls /home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/cfg-scope-discipline.md
```

Both must be present (Stage 2 DRAFT v1.0) before code lands.

### 0.C — Cfg field scope audit + classification table (~2 hr)

Walk current `FOREACH_CFG_FIELD` row-by-row. For each, classify:
- **GLOBAL_ONLY** — would the operator EVER want different cores to have different values? If NO, global.
- **PER_CORE** — if YES, per-core.
- **REMOVED** — if the field is deprecated and shouldn't migrate, mark for deletion.

Output: classification table → `plans/plan_checks/cfg-field-scope-classification-2026-05-15.md`.

Expected breakdown (preliminary):
- ~15 GLOBAL rows: num_execution_cores, engine_mode, engine_arch, require_mlockall, init_arena_use_hugepages, record_ticks, record_depth, xgb_*, held_out_*, kill_switch_*, max_drawdown_pct, max_exposure_pct, trading_mode, acknowledge_*, reconcile_mode, sharded_force_synthetic, slow_path_max_secs, slow_path_pin_offset
- ~50 PER_CORE rows: take_profit_pct, stop_loss_pct, fee_rate*, slippage_pct, risk_pct, entry_offset_pct, offset_min/max, volume_multiplier, spacing_multiplier, min_long_slope, min_buy_delta, vwap_offset, min_stddev_pct, regime thresholds, ridge_*, winsor_*, confidence_*, thompson_*, bandit_algorithm, ml_*, exit_*, partial_exit_*, tp_*/sl_* trail, hold_score, time_exit, momentum_*, emacross_*, simpledip_*, mr_*, sl_cooldown_*, regime_hysteresis, gate_*, ...
- ~3 REMOVED: reconcile_dry_run (deprecated), pay_fees_in_bnb (operator-side cfg; doesn't affect engine behavior), legacy strings the parser stopped reading at v5.14.x

Caramel reviews the classification table before Step 1 lands.

---

## Step 1 — Two-registry framework infrastructure (~200 LOC)

Declare the two X-macro registries with appropriate row migrations. Per-row metadata preserved from `.F.4c.1` (STAMP_BOUND, HAS_SIDE_EFFECT, WARN_ON_CLAMP, etc.) where applicable; PER_CORE_OK metadata bit REMOVED (every per-core registry row IS per-core by construction).

Generate descriptor arrays + bitmap masks for each registry. The framework primitives compose: each registry gets its own `g_*_cfg_field_descriptors[]` + `g_*_cfg_render_mask` + `*RenderTable<F>::fns[]`.

`cfg_field_names_unique` static_assert extended to operate per-registry (each registry internally unique; cross-registry check optional).

---

## Step 2 — Cohort migration + ControllerConfig restructure (~300 LOC + ~300 LOC deletions)

Move rows from `FOREACH_CFG_FIELD` to appropriate new registry per the scope classification table.

Delete `PerCoreOverrides<F>` struct + `PER_CORE_OVERRIDE_BITMAP_DOMAINS` X-macro + `core_overrides[16]` field.

Add `PerCoreCfg<F> cores[MAX_EXECUTION_CORES]` (where `MAX_EXECUTION_CORES = 16` per current limit) to ControllerConfig<F>.

Static_asserts for `sizeof(PerCoreCfg<F>)` and `sizeof(ControllerConfig<F>)` + per-field offset locks for critical cross-thread snapshot fields.

`ControllerConfig_ResolveForCore` simplified or deleted (depending on remaining callers).

---

## Step 3 — Parser updates (~150 LOC)

Cfg parser state machine:
- `parse_state = GLOBAL_SECTION` initially
- On `[core N]` header → `parse_state = CORE_SECTION_N` (sets active core index)
- Lines parse against the registry matching parse_state
- Unknown key → ERROR with migration hint (key + expected scope + cfg.example reference)
- Section close (next `[...]` or EOF) → flush; advance state

Save path (`save_cfg` / similar): emits global keys at top, then `[core N]` sections in order.

---

## Step 4 — Engine.cfg migration tool (~100 LOC)

Python or shell script at `tools/migrate_cfg_v5_15_5_F4c3.py`. Reads legacy engine.cfg, emits new sectioned cfg. Round-trip test: legacy → migrate → load → save → diff (semantic equivalence).

---

## Step 5 — Per-core stamp body + drift check (~200 LOC)

Per-core stamp emit at boot for each loaded core's model. Drift check fires per-core: each core's stamp vs each core's current cfg. v5.15.5.F.4c.3 stamp fixture committed at `tests/fixtures/v5_15_5_F4c3_per_core_stamp.bin` (4 cores worth).

Layer 5b hash lock per core: `LOCKED_PER_CORE_STAMP_HASH_V5_15_5_F4C3[NUM_CORES]` array constant.

---

## Step 6 — Settings panel updates (~200 LOC)

Global tab walker uses `g_global_cfg_render_mask` + `GlobalRenderTable<F>` over `cfg.<field>`. Per-core tabs walker uses `g_per_core_cfg_render_mask` + `PerCoreRenderTable<F>` over `cfg.cores[c].<field>`.

Reset+Modified UI ships in this ship (FPN<F>-vs-ImTextureRef operator== ambiguity resolved alongside Slider→Input swap).

---

## Step 7 — Backtest path + test fixture migration (~100 LOC backtest + ~200 LOC test migrations)

Backtest reads from `cfg.cores[backtest_core_idx]`. Test fixture sites grep'd + migrated mechanically. Build-clean is the gate; runtime semantics preserved by registry shape.

---

## Step 8 — DESIGN_SPECs ship + documentation updates (~400 LOC docs)

- `per-instance-registry-pattern.md` Stage 2 DRAFT → Stage 3 ACTIVE
- `cfg-scope-discipline.md` Stage 2 DRAFT → Stage 3 ACTIVE
- Updates to existing specs + CLAUDE.md item 31 + CLAUDE.local.md + Class 24
- FEATURE_LOOKUP entry for the cfg surface change
- CHANGELOG entry

---

## Step 9 — Verification gate

### Universal gates
- [ ] All 5 binaries build cleanly (`./build.sh test gui suite tsan asan`)
- [ ] `controller_test` passes (≥3144 baseline + new ~15 test functions covering split semantics)
- [ ] Paper-trade 60 sec — no parsing errors, sectioned cfg loads cleanly, per-core tabs render the appropriate registry, Settings text-entry works
- [ ] Migration tool round-trip: legacy → migrate → load → save → diff (semantic equivalence; bytewise per Layer 5b discipline)

### Architectural gates
- [ ] `FOREACH_GLOBAL_CFG_FIELD` + `FOREACH_PER_CORE_CFG_FIELD` both declared + populated
- [ ] `PerCoreCfg<F>` struct generated from per-core registry; `sizeof(PerCoreCfg<F>) % 64 == 0` (cache-alignment)
- [ ] `cores[16]` array in ControllerConfig<F>; each per-core access lands in distinct cache lines (no false sharing)
- [ ] `PerCoreOverrides<F>` deleted; `PER_CORE_OVERRIDE_BITMAP_DOMAINS` deleted; `core_overrides[]` deleted
- [ ] `ControllerConfig_ResolveForCore` direct-read or deleted; no resolve-from-global-or-override logic remains

### Parser gates
- [ ] `[core N]` section syntax parses correctly for 1, 2, 4, 8, 16 cores
- [ ] Unknown global key produces clear migration-hint error
- [ ] Unknown per-core key produces clear migration-hint error
- [ ] `[core N]` where N >= num_execution_cores produces WARN + ignore
- [ ] Multiple `[core N]` sections for same N produce ERROR

### Wire-format gates
- [ ] Per-core stamp fixture committed (4-core test fixture)
- [ ] Layer 5b hash lock per core
- [ ] HMAC byte preservation per core (round-trip parse→emit→hash)
- [ ] Cross-version compat: v5.14 unified stamps DO NOT load (hard-break per Caramel's directive); operator gets clear "model must be retrained for v5.15.5.F.4c.3 per-core stamp format" error

### Hot path discipline
- [ ] Hot path UNTOUCHED — `tools/calls_graph_diff.sh` confirms bytewise-identical
- [ ] HOT_PATH_CHANGELOG entry: NONE (slow path + boot + GUI only)

### DESIGN_SPECs + invariant codification
- [ ] `per-instance-registry-pattern.md` Stage 3 ACTIVE with file:line refs from shipped code
- [ ] `cfg-scope-discipline.md` Stage 3 ACTIVE
- [ ] Updates to `universal-cfg-field-registry-pattern.md` + `universal-registry-bitmap-dispatcher-pattern.md` post-ship
- [ ] CLAUDE.md item 31 updated
- [ ] CLAUDE.local.md going-forward rule "Per-core scope by default for trading config" added
- [ ] DESIGN_SPECS/README.md catalog updated (+2 entries; bump count)
- [ ] DOCS/RECURRING_BUG_PATTERNS.md Class 24 updated (structural close noted at .F.4c.3)

### Version + sync
- [ ] `Version.hpp` bumped `5.15.5.F.4c.1` → `5.15.5.F.4c.3`
- [ ] Git tag `v5.15.5.F.4c.3` annotated + signed
- [ ] Postmortem at `plans/v5.15-live-readiness/postmortems/2026-05-15-v5.15.5.F.4c.3-postmortem.md`
- [ ] FEATURE_LOOKUP entry for cfg surface architectural change
- [ ] `/sync-workspace` after ship lands

### Auto-write contracts
- [ ] CHANGELOG.md row
- [ ] TECH_DEBT entries closed (TECH_DEBT-063 partial close)
- [ ] PARITY_ISSUES.md scan (per-core stamps close any pre-split parity concerns)

### Final consult before commit
- [ ] Caramel reviews scope classification table (any disputed global-vs-per-core decisions)
- [ ] Caramel reviews 2 new DESIGN_SPECS post-shipped-implementation refinement
- [ ] Per CLAUDE.local.md "after pre-coding audits, ALWAYS consult before coding" — same post-coding

---

## Anti-patterns to avoid

- **Class 14 (Plan API drift):** every API reference verified at audit gate.
- **Class 18 (Mirror-incomplete):** dual-registry walker shares the SAME template (parameterized by which registry); no per-walker duplication.
- **Class 19 (Hardcoded enum names):** INT_ENUM labels via X_GEN_LABEL extern from per-instance registry.
- **Class 21 (Parallel descriptors):** the TWO registries are NOT parallel — they're scope-disjoint by design. Cross-registry name collision allowed only with explicit per-axis rationale.
- **Class 23 (Type-erased dispatch):** 3-barrier prevention intact at every NEW tt:: consumer.
- **Class 24 (Capability-cfg surface mismatch):** STRUCTURAL CLOSURE at this ship. The going-forward rule + DESIGN_SPECS prevent recurrence.
- **Half-migration:** every field classified at Step 0.C either GLOBAL or PER_CORE (or REMOVED). No "ambiguous" fields lingering. Each row has explicit scope rationale in the classification table.
- **Backward-compat creep:** Caramel's directive is hard-break. No shim. Migration tool is the answer for operator pain.

---

## If something goes wrong

- **Layer 5b per-core hash mismatch:** synthetic populate missed a field OR per-core row order changed accidentally. Investigate before re-locking the constants. Don't blindly update.
- **Cfg parser state machine error:** `[core N]` header parsed mid-line OR multiple sections detected. Add unit test for the failing input + fix state machine.
- **Test fixture compile error:** likely a `cfg.<name>` access that should now be `cfg.cores[0].<name>`. Mechanical grep+fix.
- **Migration tool emits malformed output:** round-trip test failure. Diff legacy parse against migrated parse; fix the tool.
- **Hot path regression:** calls_graph_diff catches it. Investigate which slow→hot call boundary changed; usually a missed seqlock read.
- **Backtest non-determinism:** backtest path reads from `cores[backtest_core_idx]`; verify the resolved field matches what backtest_core_idx core would see in production.
- **Rollback:** `git reset --hard pre-v5.15.5.F.4c.3`. Postmortem + iterate.

---

## Pointer to specs

### NEW DESIGN_SPECs (ship at .F.4c.3)
- `DESIGN_SPECS/per-instance-registry-pattern.md` — Stage 2 DRAFT → 3 ACTIVE
- `DESIGN_SPECS/cfg-scope-discipline.md` — Stage 2 DRAFT → 3 ACTIVE

### Existing specs this ship applies/extends
- `DESIGN_SPECS/universal-cfg-field-registry-pattern.md` — dual-registry composition (append section)
- `DESIGN_SPECS/universal-registry-bitmap-dispatcher-pattern.md` — dual-registry application (append section)
- `DESIGN_SPECS/type-trait-dispatch-via-tt-namespace.md` — N/A (tt:: dispatch unchanged)
- `DESIGN_SPECS/wire-format-byte-preservation-discipline.md` — per-core Layer 5b applies
- `DESIGN_SPECS/categorical-tag-applicability-pattern.md` — discipline sister; per-instance scope is sibling axis to categorical applicability
- `DESIGN_SPECS/multi-state-dispatch-with-per-state-update-metadata.md` (from `.F.4c.2`/`.F.4d.1` drafting) — composes naturally

### Documentation
- CLAUDE.md item 31 — per-instance dimension addition
- CLAUDE.local.md going-forward rule "Per-core scope by default for trading config" (set 2026-05-15)
- DOCS/RECURRING_BUG_PATTERNS.md Class 24 — structural close
- workspace/DOCS/CFG_SCOPE_MIGRATION_GUIDE.md (NEW; operator-facing migration guide)

---

## Cold-pickup completeness checklist

| Field | Value |
|---|---|
| Branch | `feat/v5.15-live-readiness` (stay on) |
| Rollback anchor | tag `pre-v5.15.5.F.4c.3` at Step 0.A |
| Tag name | `v5.15.5.F.4c.3` |
| Predecessor plan | `.F.4c.1` ship (paper-test polish + 18-row STAMP_BOUND cohort + Class 24 codification) |
| Successor plan | `.F.4d` wire-format framework (built natively on per-core registry from day one) |
| Tests baseline | post-`.F.4c.1` = 3144 + ~15 new test functions covering split semantics |
| Effort | structural — measured by classes closed (Class 24 structural close + override pattern eliminated) + framework primitives composed (per-instance registry as first canonical application of generalizable pattern) |
| Risk | MED-HIGH — cfg surface + ControllerConfig restructure + parser state machine + stamp body schema + ~50-100 test migrations. Hot path UNTOUCHED. |
| Audit references | `.F.4c.1` paper-test (operator-reported 2026-05-15); `/ml-audit` 2026-05-14 cfg-↔-ML mismatch findings; operator's stated architectural intent (2-month-painlessness criterion) |
| Source-audit | `CoreFrameworks/CfgFieldRegistry.hpp` (entire FOREACH_CFG_FIELD), `CoreFrameworks/ControllerConfig.hpp` (struct + ResolveForCore + parser), `ML_Headers/StampBoundCfgRegistry.hpp`, `GUI/SettingsPanel.hpp` (Global tab + per-core tabs), `Backtest/`, `tests/controller_test.cpp` (~50-100 test sites) |
| FEATURE_LOOKUP entry | YES — operator-visible: cfg file format change to sectioned `[core N]` syntax + per-core authoritative cfg + migration tool |
| Predecessor postmortem | `postmortems/2026-05-15-skipped-v5.15.5.F.4c.1.1-rationale.md` — documents the polish-ship skip in favor of this architectural ship |

---

**End of v5.15.5.F.4c.3 plan body.** Pre-coding audit gate FIRED 2026-05-15; amendments below resolve findings.

---

## Post-audit-gate amendments — 2026-05-15

Pre-coding audit gate (5 audits in parallel: parity / trace / readiness / merge / dod) returned **YELLOW** with 1 RED gate + 4 HIGH + 7 MED findings + 5 reuse-harvest opportunities + 4 DOD-discipline refinements. Operator (Caramel) reviewed synthesis at `plans/plan_checks/2026-05-15-v5.15.5.F.4c.3-fresh-audits-synthesis.md` + locked the following resolution decisions.

### Scope corrections

**CRITICAL-1 — Symbol axis DEFERRED to follow-up subplan `.F.4c.3.A`.** Audit found `symbol` lives on `BinanceConfig` (`DataStream/BinanceCrypto.hpp:64`) + `BacktestCfg`, NOT `ControllerConfig<F>`. Plus `KIND_STRING` infrastructure doesn't ship until `.F.4e`. This ship cannot cleanly migrate symbol axis without infra that doesn't exist yet. New subplan stub at `plans/v5.15-live-readiness/subplans/2026-05-15-v5.15.5.F.4c.3.A-symbol-axis-per-core-migration.md` (fires after `.F.4e`). Open UX question for that subplan: CAPS text-entry vs validated-list dropdown of Binance trading pairs.

**CRITICAL-2 — A2 bitmap-bool migration specifics LOCKED**:
- **12 bits in `FOREACH_ML_CFG_FLAG`** (re-audit verified via `rg -c "    X\("` against `MlCfgFlagRegistry.hpp:52-64` = 12; first-audit pass + initial amendment both miscounted as 13; re-audit corrected)
- **Bit position anchored to FOREACH_ML_CFG_FLAG enum ordinal at compile time** — each KIND_BOOL row carries its `MASK_ML_CFG_*` enum value as compile-time row metadata. Rebuild walker writes `bitmap |= ((uint16_t)row_value) << enum_ordinal`. Pattern precedent: FOREACH_PER_ARM_FLAG mask discipline at v5.15.5.A.2.b.
- **4 stamp-emit-via-BITMAP_BIT rows DEFER to `.F.4d`** — these emit through `HANDLE_STAMP_EMIT_BITMAP_BIT` at `ML_Headers/StampBoundCfgRegistry.hpp:106-146`. `.F.4d`'s wire-format framework two-source variant naturally handles bitmap-bit stamp emit. `.F.4c.3` migrates the **8 non-stamp-emit bits** (12 total - 4 deferred = 8 flat KIND_BOOL rows added to per-core registry); the 4 remaining bits stay in `ml_cfg_flags` source through `.F.4d`.
- Co-located `static_assert(FOREACH_ML_CFG_FLAG_COUNT <= sizeof(ml_cfg_flags_runtime_bitmap) * 8)` enforces bitmap-overflow discipline.

**F6 extension ADOPTED** — apply A2 pattern to ALL 5 cfg-domain bitmaps in same ship (not just `ml_cfg_flags`):
- `lifecycle_cfg_flags` (uint8_t; FOREACH_LIFECYCLE_CFG_FLAG)
- `gate_cfg_flags` (uint8_t; FOREACH_GATE_CFG_FLAG)
- `ml_cfg_flags` (uint16_t; FOREACH_ML_CFG_FLAG; 8 bits at .F.4c.3 + 4 at .F.4d)
- `risk_cfg_flags` (uint8_t; FOREACH_RISK_CFG_FLAG)
- `ops_cfg_flags` (uint8_t; FOREACH_OPS_CFG_FLAG)

Same structural fix; same ship closes 4 sibling-asymmetry recurrence-risks in one cohort. Total: ~32 new KIND_BOOL rows in per-core registry across 5 domains at `.F.4c.3` (lifecycle ~7 + gate ~6 + ml 8 + risk ~5 + ops ~6); the 4 remaining ml_cfg_flags stamp-emit-BITMAP_BIT rows migrate at `.F.4d`.

### HIGH findings resolution

**HIGH-1 — `_BuildParameters` sig treatment LOCKED to Option A.** 4 strategy fns (`SimpleDip_BuildParameters`, `MR_BuildParameters`, `EmaCross_BuildParameters`, ML branches) change sig: `(const ControllerConfig<F>* config, ...)` → `(const PerCoreCfg<F>* core_cfg, ...)`. Call sites pass `&cfg.cores[c]`. Body changes `config->field` → `core_cfg->field`. Boundary-stable refactor per `feedback_reduce_touch_sites.md`. 4 fn sigs + 11 call sites; compile-error catches missed sites.

**Execution-time scope amendment (2026-05-15 during WIP2c scoping):** `_BuildParameters` bodies actually read ~15 un-classified per-core fields (strategy-TP-overrides `mr_tp_pct` / `simpledip_tp_pct` / `emacross_tp_pct` / `mr_sl_pct` / `simpledip_sl_pct` / `emacross_sl_pct`; non-registry fields `offset_stddev_mult` / `offset_stddev_min` / `offset_stddev_max` / `confidence_hard_block_threshold` / `barrier_blend_mode` / `ensemble_min_agreement_pct` / `breakout_min` / `ror_tp_bonus` / `momentum_tp_r2_min` / `momentum_r2_min`) plus 1 genuinely-global field (`poll_interval` used for tick→time conversion in `ML_BuildParameters`). To preserve strict single-param Option A discipline + close Class 25 (Scope-erosion in consumer fn — new class codified at DOCS/RECURRING_BUG_PATTERNS.md), WIP2c is split into two sub-commits:

- **WIP2c.1: Classify-first.** Add the ~15 un-classified per-core fields to `FOREACH_PER_CORE_CFG_FIELD` + `PerCoreCfg<F>` (additive only; PopulateCoresFromFlat X-macro walker auto-extends). Build green; tests unchanged.
- **WIP2c.2: Strict single-param migration.** 5 fn sigs (the 4 strategy fns + Strategy_BuildParameters dispatcher) take `const PerCoreCfg<F>* core_cfg` exclusively. Body adapts `config->X` → `core_cfg->X` for ALL per-core reads. `poll_interval` extracted as scalar arg in `ML_BuildParameters` (caller-resolved at the call site). Call sites pass `&cfg.cores[c]` + the scalar poll_interval. ~5 fn sigs + 11+ call sites + ~12 row additions (vs original "4 fn sigs + 11 call sites" estimate).

Rationale: full cfg pointer in per-core consumer = Class 25 anti-pattern (operator sets `cfg.cores[2].X` but consumer reads `config->X` = flat field = wrong scope). Closes structurally by removing the wrong-scope access path from the type system. See `DESIGN_SPECS/cfg-scope-discipline.md` § "Consumer function signatures over per-core slices" (NEW at this ship) + `DOCS/RECURRING_BUG_PATTERNS.md` Class 25 (NEW) for full discipline.

**HIGH-2 — Existing per-core arrays LOCKED**:
- `core_strategies[16]` → `cores[c].strategy` (1 KIND_INT_ENUM row in per-core registry)
- `core_risk_pct[16]` → `cores[c].risk_pct` (already in plan scope; verify deduplication at Step 1)
- `core_strategies_explicit_set` (partner-bitmap) → **DELETABLE.** Under per-core authoritative scope, "explicit" derives from `core_idx < num_execution_cores`. Step 2 audit verifies no production read distinguishes from this.
- 5 string arrays (`core_model_path` / `core_model_dir` / `core_horizon_list` / `core_ensemble_blend_mode` / `core_disabled_horizons`) → STAY MANUAL at `.F.4c.3`; migrate to per-core string rows at `.F.4e` with KIND_STRING cohort.

**HIGH-3 — Test fixture count honest**: **414 `cfg.<field>=` writes in `tests/controller_test.cpp`** + ~32 production read sites. Plan's "~50-100" was 4-5× undercount. Realistic effort: 8-12 hr migration + 4-6 hr audit. NEW centralized helper: `controller_test_init_cfg_for_core_zero(cfg)` concentrates future test cfg-init.

**HIGH-4 — Subsumed by F7 reuse harvest** (see below): `STAMP_CFG_AUTOPOPULATE` + `INFERENCE_CFG_AUTOPOPULATE` macros extended to accept cfg-source parameter instead of hardcoding `cfg` caller variable name. Per-core dispatch passes `cfg.cores[c]` directly. Eliminates per-call-site alias-creation.

### Reuse harvest ADOPTED (F1 + F2 + F4 + F7)

**F1 — Dual-registry walker template** — ONE `RenderRegistryWalker<Target, Table, WORDS>` template consumed at Global + per-core tab call sites + Settings panel. Class 18 prevention by construction. ~80 LOC saved at `.F.4c.3`.

**F2 — Per-core slice reference convention** — fn sigs take `const PerCoreCfg<F>& core_cfg` (already locked by HIGH-1 Option A). Forward-compat for `.F.4f+` AoS-vs-SoA re-layout.

**F4 — Multi-action registry walker family** — `FOREACH_REGISTRY_ACTION(X)` roster of 5 actions (parse/save/render/stamp/drift) × M registries = 10+ walker instances become 5 walker types × 1 template + 2 instantiations. ~150 LOC at `.F.4c.3` saves ~500 LOC across `.F.4d/.F.4e/.F.5+`. NEW DESIGN_SPEC `multi-action-registry-walker-family.md` ships Stage 2 DRAFT at `.F.4c.3` close.

**F7 — Stamp-emit autopopulate parameterization** — `STAMP_CFG_AUTOPOPULATE(inf, cfg_source)` + `INFERENCE_CFG_AUTOPOPULATE(inf, cfg_source)` macros take cfg-source parameter. **Concrete mechanism**: macro body adds `const auto& cfg = (cfg_source);` local-alias as first line; existing X() row bodies' `cfg.<field>` syntax preserved unchanged (NO row-by-row rewrite). Per-core call sites become `STAMP_CFG_AUTOPOPULATE(inf, global_cfg.cores[c])` — operator-readable + mechanically minimal.

### DOD-discipline locked

**DOD-F1 — `alignas(64)` + `alignof` static_assert**:
```cpp
template <unsigned F>
struct alignas(64) PerCoreCfg {
    FOREACH_PER_CORE_CFG_FIELD(EMIT_CFG_STRUCT_FIELD)
};
static_assert(sizeof(PerCoreCfg<64>) % 64 == 0, "PerCoreCfg<F> size cache-aligned");
static_assert(alignof(PerCoreCfg<64>) == 64, "PerCoreCfg<F> alignment cache-aligned");
```
Both `sizeof%64==0` AND `alignof==64` required per `per-snapshot-cluster-layout-pattern.md` Rule 3 + cross-thread-snapshot-publish-cluster-isolation.

**DOD-F2 — A2 discipline statement**:
- Bit position anchored via compile-time row metadata (NOT iteration order)
- Co-located `bitmap-overflow-protection` static_assert per registry domain
- SET-discipline at rebuild walker per `registry-bitmap-set-discipline.md` Shapes A/B — every cfg-row data-write triggers the bitmap-rebuild walker (no manual-bit-set bypass paths)

**MED-1 — v5.14 cross-version refusal mechanism**: new stamp body **HEADER field** `cfg_scope_split_version="5.15.5.F.4c.3"` (lands alongside `engine_version` + `format_version` in the stamp body header section at `ModelInference.hpp:1710`, NOT as a STAMP_BOUND data row — data-row position shifts when peer rows are added → wire-byte drift risk across `.F.4d/.F.4e` future ships). At boot, stamp without this field OR with older version = explicit ERROR (not silent has_*=0 default load). Forces operator retrain post-`.F.4c.3` hard-break.

**MED-4 — GUI cfg-mirror Option α**: GUI owns separate `gui_engine_cfg` populated from file at boot; engine owns its own `ControllerConfig`. Communicate via file + reload-signal channel; NEVER pointer-share state across threads. Aligns with H3 (no mutex/condvar/sleep_for/rwlock) + thread-isolation going-forward rule (set 2026-05-14).

### Per-core stamp + drift additions

**HMAC stamp file naming disambiguation** (NEW; not in original plan body):
- Per-core stamps live at `<core_N_model_dir>/cfg-core-<N>.stamp` (NOT `cfg.stamp`)
- Filename includes core idx so multiple cores deploying SAME model_dir get distinct stamps
- Use case: same trained model deployed to core 0 + core 2 with different per-core cfg envelopes; each core's stamp is its own file

**Per-core drift UX in model-health panel** (NEW):
- Boot-time drift check fires per core; per-core drift status published to `PerCoreSnap`
- GUI model-health panel renders per-core drift: which cores have stamp-vs-cfg drift, severity per finding, fields affected
- Sister to FOREACH_MODEL_HEALTH_FLAG registry (shipped `.F.3`); extends per-handle flags to per-core composite flags
- Operator can SEE drift per-core, not just "drift exists somewhere"

### Updated DESIGN_SPECs ship list

`.F.4c.3` ships **4 Stage 2 DRAFT DESIGN_SPECs** (promoted to Stage 3 ACTIVE at ship close):
1. `per-instance-registry-pattern.md` (DRAFT v1.0; already written)
2. `cfg-scope-discipline.md` (DRAFT v1.0; already written)
3. **`multi-action-registry-walker-family.md`** (NEW; per F4 adoption)
4. **`cfg-section-parser-state-machine.md`** (NEW; per /merge-scan F5 + .F.4c.3 section parser implementation)

### Documentation amendments

**CLAUDE.md item 31 update** — concrete one-line addition:
> Per-instance dimension (per-core; future: per-symbol / per-strategy / per-horizon / per-regime) is a primary framework axis. See `DESIGN_SPECS/per-instance-registry-pattern.md` for the canonical pattern + future application catalog.

**`.F.4i` backtest cfg integration as known-followup note**: `backtest.cfg` migrates to per-core section syntax (same shape as `engine.cfg`) when per-core backtest-specific cfg surface needs grow (likely post-`.F.5` if per-core ML hyperparameter backtests become a thing). Note added to plan body's "Pointer to specs" section.

**Migration guide** — `workspace/DOCS/CFG_SCOPE_MIGRATION_GUIDE.md` ships at `.F.4c.3`:
- Manual rewrite checklist (operator-facing)
- Example new-format `engine.cfg` (4-core sample)
- Field-by-field global vs per-core classification list (output of Step 0.C audit table)
- Migration validation steps (cfg loads, settings panel populates, paper-trade 60 sec)

**Step 0.C classification table format spec** — concrete columns:
| field_name | current_struct_field | classification (GLOBAL/PER_CORE/REMOVED) | rationale | dependent_read_sites |

**Step 9 gate addition — calls_graph_diff baseline refresh**:
```bash
tools/calls_graph_diff.sh refresh-baseline  # capture post-Step-2 baseline
tools/calls_graph_diff.sh verify             # confirm bytewise-identical hot path
```

### ~15 new test functions enumerated (MED-7)

1. `test_v5_15_5_F4c3_per_core_resolver_returns_cores_value` — direct read matches `cfg.cores[c].field`
2-4. `test_v5_15_5_F4c3_section_parser_{1,4,16}_cores` — section parsing across core counts
5. `test_v5_15_5_F4c3_section_parser_unknown_global_key` — error with migration hint
6. `test_v5_15_5_F4c3_section_parser_unknown_per_core_key` — error with migration hint
7. `test_v5_15_5_F4c3_section_parser_extra_section_warn` — `[core N]` where N >= num_execution_cores produces WARN
8. `test_v5_15_5_F4c3_section_parser_duplicate_section_error` — multiple `[core 0]` produces ERROR
9. `test_v5_15_5_F4c3_scope_discipline_no_trading_in_global` — every trading-adjacent field in per-core registry (static_assert)
10. `test_v5_15_5_F4c3_scope_discipline_no_field_in_both_registries` — cross-registry name-collision check
11. `test_v5_15_5_F4c3_per_core_stamp_emit_byte_identity` — per-core stamps bytewise-identical for identical input
12. `test_v5_15_5_F4c3_per_core_drift_check_fires_per_core` — drift detection per-core not global
13. `test_v5_15_5_F4c3_bitmap_rebuild_correctness` — rebuild walker writes correct bit positions from flat KIND_BOOL rows
14. `test_v5_15_5_F4c3_bitmap_rebuild_round_trip` — pre-rebuild + post-rebuild bitmaps identical when no field changes
15. `test_v5_15_5_F4c3_v5_14_stamp_cross_version_refusal` — v5.14 stamp without `cfg_scope_split_version` hard-errors

### Revised effort

- Plan + DESIGN_SPECs work: ~3 hr (revised from ~2.5)
- Code work post-greenlight: ~2 weeks intensive structural work (unchanged)
- Risk classification: MED-HIGH (unchanged)

### Open audit-gate decisions — ALL RESOLVED 2026-05-15

| Decision | Operator answer |
|---|---|
| Symbol axis defer to `.F.4e` via `.F.4c.3.A` | YES |
| A2 13 bits + enum-ordinal anchor + defer stamp-emit-BITMAP_BIT | YES |
| HIGH-1 Option A sig | YES |
| F1 + F2 + F4 + F7 reuse harvest | YES |
| F6 cohort extension (A2 to all 5 cfg-domain bitmaps) | YES |
| HMAC stamp file naming disambiguation | YES |
| Per-core drift UX | YES |
| GUI cfg-mirror Option α | YES |

All locked. Next step: re-fire audit subset (parity + trace + dod) to verify GREEN before coding starts from Step 0.A.

---

**End of v5.15.5.F.4c.3 plan (post-audit-gate amended 2026-05-15).** Pre-coding audit gate fires before any code lands.
