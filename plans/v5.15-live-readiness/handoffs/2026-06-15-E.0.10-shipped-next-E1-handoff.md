---
type: handoff
status: active   # the one live handoff; no-arg /accept-handoff resolves here. Prior (2026-06-15 A6-ingress-uaf) flipped to superseded.
ship_tag: .E.0.10 (SHIPPED) → next .E.1
plan_type: live-readiness
sprint_end_goal: make the codebase more maintainable for future development (v5.X professionalization); live-readiness gated behind the whole E-series
ship_end_goal: ".E.0.10 is CLOSED + tagged (v5.15.5.F.4d.1.E.0.10). NEXT ship = .E.1 Foundation (Core→Node rename + per-node drainer absorption + multi-exchange registry). This handoff sets up the .E.1 dive."
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-15-E.0.10-A6-ingress-landed-uaf-next-handoff.md (superseded — its NEXT ACTION, TD-202, is done + .E.0.10 shipped)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (latest D-222 = the .E.0.10 close session)
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md (status CLOSED; the queryable .E.1 disposition record)
plan_body: plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E.1-foundation.md (the .E.1 plan — v0.1, RED/pre-audit-gate; carries this session's TD-202 fold)
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, the .E.1 plan_body, the guard-coverage matrix §4d, the register (CLOSED), decision-log D-222, the .E.0.10 postmortem, DOCS/tech-debt/open.md TD-202/203/162, regen CODE_MAP at pickup]
engine_head: 572c978 (.E.0.10 SHIP CLOSE — Phase-D live-gate + Version .E.0.10; signed tag v5.15.5.F.4d.1.E.0.10; predecessor 3ee95dc A6-ingress + e781828 TD-202-fix)
workspace_head: at-or-after 3c43458 (.E.0.10 close — test + guard-matrix §4d + TD-203 + FEATURE_LOOKUP + D-222 + postmortem; the status-flip + this-handoff + memory-sync refresh commits sit on top)
coding_status: SHIP CLOSED — .E.0.10 tagged + pushed (both repos 0/0). No mid-flight code. The next session OPENS .E.1 (a fresh ship; pre-coding gate RED).
defers: ".E.1-folded: the single-owner-disk_file event-log rework (TD-202 Reset 3-way race + LoadFromDisk recovery-integrity gate + the FIX-2 create→publish TOCTOU) · A20 warm-tail replay+watermark · A21 venue-net qty leg · the NEW-1 predicate per-cluster relocation. Homed-deferred: #16 V-class SKILL · TD-203 (Phase-D gate removal @ v5.16) · TD-162 (warm-restart fee)."
---

# Handoff — `.E.0.10` SHIPPED ✅; NEXT = the `.E.1` Foundation dive

## TL;DR (the 60-second pickup)

- **`.E.0.10` is DONE + tagged** (`v5.15.5.F.4d.1.E.0.10`, engine `572c978` + workspace `3c43458`, both pushed). It was the pre-`.E.1` correctness net + the capital-correctness backlog burn-down. The MASTER SHIP-CLOSE banner is the one-paragraph summary; the postmortem (`2026-06-15-...E.0.10-postmortem.md`) is the full arc.
- **NEXT = `.E.1` Foundation** — Core→Node rename (~5,000 sites) + per-node drainer absorption + multi-exchange registry + cluster topology. It is the **riskiest ship of the sprint** (hot path TOUCHED). `.E.0.10` was its safety net; that net is now complete + green.
- **`.E.1`'s FIRST-order work (before the rename):** close the recorded **H7 / H10 / H14 / H20 + H8** hot-path/determinism HOLE cluster (grep-CI + the determinism gate + the latency ratchet) — per the guard-matrix §4a:110 + §4d STOP-verdict. You can't freeze hot-path behavior the rename is about to reshape, so these guards go in FIRST, then the rename rides them.
- **No fire to put out.** Everything this session is committed + green (3635/0 + fresh asan/ubsan). One pending mechanical: `/sync-workspace` to back up the new memories (run as part of this close-out).

## The ideal way to start (see state, THEN move)

1. **`/accept-handoff`** (no arg → resolves here). It regens CODE_MAP, verifies git state (engine `572c978` + the tag; workspace at-or-after `3c43458`), loads the baseline + nav-infra, and routes the next action.
2. **Confirm the close is real** (the `/accept-handoff` Stage 4.5 predecessor-verify): `git tag --list v5.15.5.F.4d.1.E.0.10` + `git tag --verify` (signed) · `rg "E.0.10" Version.hpp` (== `5.15.5.F.4d.1.E.0.10`) · the postmortem exists · the register/MASTER/plan all read SHIPPED.
3. **Then read the `.E.1` surface** — the `.E.1` plan body (it's `RED`/pre-audit-gate; carries the D-214 addendum + the TD-202 single-owner-disk_file fold) + the guard-matrix §4d (the STOP-condition + what `.E.1` inherits) + the register (CLOSED — the queryable disposition of every deferred item).
4. **`.E.1` is a HIGH-RISK ship → fire `/precoding-audit-gate` (+ `/blindspot-scan`) BEFORE coding** — it touches the hot path, the drainer, OMS state, thread topology. The guard-matrix §4d names its first-order hardening worklist.

## What `.E.0.10` delivered (so you have the full close context)

- **Net-1 regression net** — 6 characterization tests (golden-master) + the H1-H20 CI invariant net + the live-disposition finding register (141 `.E` + 93 TECH_DEBT + 5 PARITY re-triaged).
- **A-series adversarial exit-chain audit** → RBP Class 43/44/45/46 + the **4 durable-now capital fixes**: NEW-1 single capital-authority predicate (`ControllerConfig_IsLiveCapital`; RBP Class 47), A20 seed-don't-replay, A21 venue-net cash-leg detect-only, A17 paper-synth.
- **A6 capital guard** — EGRESS (`GateParameters_FinalizeEmit` range-validate + `SHALT_BAD_PCT`) + INGRESS (corrupt-model detect→drop→majority-SHALT; `barrier_is_corrupt` SSoT + `MODEL_CORRUPT`; RBP Class 49).
- **TD-202 OMS event-log UAF** (this session) — quiesce-first `OrderEventLog_Init` (RBP Class 50). A 3-I→3-A `/precoding-audit-gate` reshaped it: ring-UAF is test-only, but a PRODUCTION `OrderEventLog_Reset` 3-way disk race + a `LoadFromDisk` recovery hole were found → `.E.1`-folded.
- **Phase-D** — blanket live-capital boot-gate (`LiveReadiness_Verify` refuses live until `.E`; H21 tombstone → TECH_DEBT-203).
- **NEW disciplines:** `fix-toward-future-trajectory` (design fixes as forward-compatible foundation increments — the subagent classes carry it) + the V-class post-implementation-verification methodology v1.1 + the anti-stale sanitizer gate (`run_sanitizer_suite.sh` rebuilds each lane fresh — it had been grading months-old binaries).
- **Verify:** controller_test 3635/0 + FRESH asan+ubsan 3635/0 (0 sanitizer errors) + calls_graph CLEAN + doc-CI CLEAN.

## Impact on future plans — what `.E.1` inherits (folded this ship)

The `.E.1` plan body (`2026-05-28-...E.1-foundation.md`) carries the folds — read its **D-214 addendum + the `:1926` TD-202 GATE EXPANSION**:
- **Event-log single-owner-`disk_file` rework** (THE `.E.1` event-log lifecycle work): the writer owns open/close/rotate via a polled atomic; Reset/Init SIGNAL it, never touch the `FILE*` cross-thread — closes the production `OrderEventLog_Reset` 3-way race + the FIX-2 create→publish TOCTOU. **Plus the `LoadFromDisk` body-integrity gate** (wire the unused OMSEL02 `reserved` checksum) so a torn file is rejected, not booked into the warm-restart balance.
- **A20** warm-tail replay + watermark persist + venue-exact reconcile fee. **A21** venue-net qty leg + the authoritative ledger-booked correction + the reconciler torn-read. **NEW-1** predicate per-cluster relocation (the H22 scale-out seam — zero authorizer edits).
- **The cross-thread torn-read class** (9 sites, 3 live-capital) — the aggregator-single-writer + seqlock-published money snapshot. This is what Phase-D's live-gate GUARDS until it lands.
- **Guard-matrix first-order work:** close H7/H10/H14/H20+H8 (the hot-path/determinism cluster) BEFORE the rename.

## Capture-completeness (this session — nothing lost)

Committed + verified: TD-202 fix (engine `e781828` + the close `572c978`) · the codifications (RBP Class 50, `fix-toward-future-trajectory` DESIGN_SPEC+memory, V-class spec v1.1, the `run_sanitizer_suite` anti-stale fix) · the gate synthesis + 7 agent reports (`plan_checks/2026-06-15-td202-uaf-gate/`) · decision-log **D-222** · the postmortem · FEATURE_LOOKUP (A6 + Phase-D) · TD-202 expansion + TD-203 · the guard-matrix §4d · the `.E.1` fold · the MASTER/register/plan status flips.
- **DONE (this close-out): `/sync-workspace` ran** — the NEW memory `feedback_fix_toward_future_trajectory_not_static_state` + the edited `feedback_v_class_post_implementation_verification` (v1.1) + the `MEMORY.md` index line are validated ("All frontmatter valid"; sisters symmetrized) + mirrored to `memory.backup/` + committed+pushed (workspace `4fd9651`). CHANGELOG `.E.0.10` row + this handoff-currency fix landed at the close-out tail. Nothing pending.

## Homed-deferred (do NOT silently drop; each has a home)

- **#16 — the V-class SKILL** (`/verify-implementation`, or `/post-ship-audit` PRE-commit mode) + add **V** to the I/A fan-out vocab (`feedback_a_class_i_class_fanout_vocab`). The METHODOLOGY is fully codified (spec v1.1 + memory + fresh-build discipline); only the executable skill is unbuilt. It earned its keep 3× this session — a strong do-now-while-fresh candidate. Home: TaskList #16.
- **TECH_DEBT-203** — remove the Phase-D blanket live-gate at `.E`/v5.16 when live-readiness lands (H21 tombstone; greppable `H21 TOMBSTONE` in `LiveReadiness.hpp`).
- **TECH_DEBT-162** — warm-restart event-log fee-recompute (folded into the `.E.1` venue-fee work).
- The TD-202 LOW side-findings (`usleep(100)` drainer stall, dead `writer_realloc_failed_count`, live-shutdown late-push) — recorded in TD-202's GATE UPDATE; subsumed by the `.E.1` SPSC rework.

## Operator collaboration reminders (every-turn)
- Address **Caramel as Caramel / she / her**. No AskUserQuestion modal boxes — inline options.
- Evaluate options on **robustness + latency + design philosophy, NOT time**.
- **After pre-coding checks, ALWAYS consult before coding** — present findings + iterate; don't auto-proceed. `.E.1` is HIGH-RISK capital — heavier-default audit posture.
- **Run the DEDICATED audit skills**, not armed prompts (the A6 + TD-202 lesson). **Fix toward the documented `.E` trajectory**, not the static present (`feedback_fix_toward_future_trajectory_not_static_state`).
- Compaction degrades — **verify every claim in this handoff against current code** before trusting it (the register's own shapes were 3/5 wrong this sprint — code-read, don't inherit).
