# v5.14 — consolidated findings + code-base slotting

Consolidates Pass 1 (architecture) + Pass 2 (math) + Pass 3
(training pipeline) FoxML_Core audits into a single actionable table
mapped to the v5.14 sprint structure.

**Source documents:**
- `plans/2026-05-08-foxml-core-port-ideas.md` — Pass 1 (architecture)
- `plans/2026-05-08-foxml-core-math-deep-audit.md` — Pass 2 (math)
- `plans/2026-05-08-foxml-core-training-deep-audit.md` — Pass 3 (training)
- `plans/2026-05-08-MASTER-v5.14-foxml-port-and-maker.md` — master

---

## All findings ranked by sprint placement

### v5.14 (CURRENT SPRINT — 22 candidate items, deduplicated)

| # | Finding | Source pass | Slots into | Sub-ship | Effort | Dep on |
|---|---|---|---|---|---|---|
| 1 | Ridge risk-parity blending | Pass 2 #1 | `ML_Headers/RidgeBlender.hpp` (NEW) + `Strategies/StrategyParameters.hpp` ML dispatch | v5.14.0 | 3-5d | — |
| 2 | Composite confidence (IC × Fresh × Cap × Stab) | Pass 1 #1 + Pass 2 #2 | Extends `ML_Headers/ConfidenceScore.hpp` ConfidenceScorer struct + feeds v5.12.1.D sizing path | v5.14.1.A/B/C | 2-3d | — |
| 3 | Feature winsorization (MAD / percentile bounds) | Pass 2 + Pass 3 #2 | `ML_Headers/FeatureStandardizer.hpp` sidecar extension + Surface G `has_winsor_bounds` flag | v5.14.1.D | 1-2d | — |
| 4 | Hot-swap exit_predictor coverage | v5.13.6.B finding | Extends `EngineSharded.hpp:~2748` REFUSE → call `EnsembleModelZoo_LoadFromCfg` | v5.14.2 | 2d | — |
| 5 | 3-layer registry fingerprinting | Pass 1 #3 | Extends `ML_Headers/FeatureRegistry.hpp:400` single-layer hash + Surface G stamp body extension | v5.14.3 | 3-4d | — |
| 6 | Multi-mode reconciliation (STRICT/WARN/AUTO_SYNC) | Pass 1 #4 | Extends `CoreFrameworks/Reconcile.hpp:22` (Phase 2 deferred comment exists) + new `Reconcile_ApplyMissedFills` helper | v5.14.4 | 2-3d | — |
| 7 | CS percentile-rank target | Pass 2 #3 + Pass 3 #4 | `Backtest/LabelFunctions.hpp` FOREACH_TARGET append (`LABEL_CS_PERCENTILE_RANK`) | v5.14.5 | 1d | — |
| 8 | CS robust z-score (MAD) target | Pass 3 #3 | FOREACH_TARGET append (`LABEL_CS_ZSCORE_ROBUST`) | v5.14.5 | 1d | — |
| 9 | CS vol-scaled demeaned target | Pass 2 #3 | FOREACH_TARGET append (`LABEL_CS_VOLSCALED_DEMEANED`) | v5.14.5 | 1d | — |
| 10 | `/bug-check` skill | v5.13.5 close | New skill at `.claude/skills/bug-check/SKILL.md`; reads RECURRING_BUG_PATTERNS.md + runs detection greps | v5.14.6 | 4-6h | — |
| 11 | Maker order MVP (POST_ONLY + cancel-and-replace) | Pass 1 #6 + operator question | `Order.hpp:53-54` (LIMIT enums RESERVED) + new `BinanceAdapter` POST_ONLY path + cancel-and-replace state machine | v5.14.7 | 3-4d | — |
| 12 | Stale-model age check | Pass 3 #1 | `ML_Headers/CoreModelZoo.hpp` extend + cfg `model_max_age_hours` | v5.14.8.A | 1-2d | — |
| 13 | Stale-feature gating per-feature | Pass 3 #2 | `ML_Headers/FeatureRegistry.hpp` per-row `max_staleness_minutes` + Features_PackAll skip-if-stale | v5.14.8.B | 1-2d | — |
| 14 | Scaler fit-on-data fingerprint | Pass 3 #9 | Stamp body Surface G `has_scaler_fit_data_hash` flag | v5.14.8.C | 1d | — |
| 15 | Feature `removal_reasons` lineage | Pass 3 #7 | Stamp body extension; CSV column for per-feature removal reason | v5.14.8.C | 1d | — |
| 16 | Environment metadata in stamp | Pass 3 #12 | Stamp body Surface G `has_environment_meta` (TF/PyTorch versions, CUDA info) | v5.14.8.C | 1d | — |

**Sub-ship totals:**
- v5.14.0 Ridge: 3-5d
- v5.14.1 Confidence + winsor: 3-4d
- v5.14.2 Hot-swap ensemble: 2d
- v5.14.3 3-layer fingerprint: 3-4d
- v5.14.4 Multi-mode reconcile: 2-3d
- v5.14.5 CS targets x3: 2d
- v5.14.6 /bug-check skill: ~4h
- v5.14.7 Maker MVP: 3-4d
- **v5.14.8 lineage + stale (NEW from Pass 3): 4-5d**

**Total v5.14 sprint:** ~23-30d (single-thread); fits if phases run
in parallel where dependencies allow.

---

### v5.15 (DEFERRED — 7 items)

| # | Finding | Source pass | Effort | Notes |
|---|---|---|---|---|
| 17 | Spearman IC (rank correlation; replaces Pearson where appropriate) | Pass 2 #4 + Pass 3 #5 | 1-2d | Adds new `RollingICSpearman` alongside existing `RollingIC` (Pearson) |
| 18 | Cost-aware IC penalty | Pass 2 #6 | <1d | Inline with Ridge; trivial extension |
| 19 | Soft risk degradation ladder | Pass 1 #5 | 3-4d | Builds on v5.14.1 composite confidence |
| 20 | Bayesian Thompson sampling alternative | Pass 2 #5 | 4-5d | Alternative to Exp3-IX; not replacement |
| 21 | Leakage Sentinel automaton | Pass 3 #6 | 2-3d | Python tools/; offline QA |
| 22 | Regime-conditional features | Pass 3 #8 | 2-3d | New computed features in registry |
| 23 | Portfolio turnover tracking | Pass 3 #10 | 2d | Extends ConfidenceScorer rolling buffers |

---

### v5.16+ (DEFERRED — 6 items)

| # | Finding | Source pass | Effort | Notes |
|---|---|---|---|---|
| 24 | Fractional differentiation features | Pass 2 #7 | 2d | NOT in FoxML_Core; design from scratch (Lopez de Prado) |
| 25 | PSI + KS distribution shift | Pass 2 #8 | 2d | Python tools/ for daily reporting |
| 26 | Online correlation matrix updates | Pass 2 #9 | 2-3d | Optimization over Ridge's full-recompute |
| 27 | Calibration metrics on v5.13.0.B log | Pass 2 #10 + Pass 3 | 1d | Python tools/ post-process Brier + ECE |
| 28 | Online quantile sketches (t-digest / KLL) | Pass 2 (future note) | 3-4d | NOT in FoxML_Core; design from scratch |
| 29 | Cost-adjusted backtest metrics | Pass 3 #6 | 1d | Python tools/ for daily reporting |

---

### NOT PORTING (engine has BETTER infra OR not applicable)

- **Bandit learning** — already shipped v5.10.0a + v5.13.4 (Pass 1 auditor incorrectly flagged as missing)
- **Order lifecycle** — already shipped (PENDING → SUBMITTED → FILLED state machine)
- **Reconciliation skeleton** — already shipped (just needs multi-mode extension; that's #6 above)
- **Persistent snapshots** — already shipped (ShardedSnapshotPersist.hpp)
- **Ranking losses** — training-time only; offline Python pipeline; not C++ port
- **Sharpe/Sortino/Calmar** — daily reporting layer; not engine
- **CheckpointManager** — training-side Python; not engine
- **Feature engineering recipes** (50+ technical indicators) — XGBoost already learns these
- **TensorFlow/PyTorch infra** — framework-specific
- **Pickle/joblib serialization** — Python-specific
- **Determinism seeding** — engine already has it (replay-determinism v5.9.2 test)
- **CV discipline / embargo / purged k-fold** — training-time; offline
- **Hyperparameter optimization** — offline training

---

## Sidecar files needed per sub-ship

| Sub-ship | Sidecar artifacts |
|---|---|
| v5.14.0 Ridge | `tests/controller_test.cpp` (~14 tests); `engine.cfg.example` (5 cfg fields); `DOCS/CHANGELOG.md` row; `DOCS/HOT_PATH_CHANGELOG.md` SLOW-PATH ADD |
| v5.14.1 Confidence+winsor | tests; cfg.example (4 fields); CHANGELOG row; HOT_PATH_CHANGELOG (slow-path winsor clip ~5-15ns); Python trainer winsor-bounds writer |
| v5.14.2 Hot-swap ensemble | tests (mocked ezoo swap); CHANGELOG row; HOT_PATH_CHANGELOG (boot-only) |
| v5.14.3 3-layer fingerprint | tests (round-trip layer-2 hash); cfg.example (overlay path field); CHANGELOG row; Python trainer overlay-patch sidecar writer; `tools/verify_overlay_hash.sh` script |
| v5.14.4 Multi-mode reconcile | tests (each mode); cfg.example (reconcile_mode enum); CHANGELOG row; mock Binance trade-replay fixture |
| v5.14.5 CS targets ×3 | tests (compute fns + LABEL_REGISTRY_HASH bump); CHANGELOG row (DELIBERATE retrain warning); Python trainer side support |
| v5.14.6 /bug-check skill | `.claude/skills/bug-check/SKILL.md`; auto-symlinked; ~1 test plan_check report on baseline |
| v5.14.7 Maker MVP | tests (POST_ONLY submit; cancel-and-replace state machine); cfg.example (5 maker cfg fields); CHANGELOG row; HOT_PATH_CHANGELOG (slow-path price-ladder read ~5ns); calibration log extension for fill-rate diag |
| v5.14.8 Lineage+stale | tests (stale-model age; stale-feature skip; round-trip stamp body new fields); cfg.example (stale-thresholds + overlay); CHANGELOG row; Python trainer environment-capture writer |

---

## Ordering / phase-dependency graph

```
Phase 1 (Math + safety; load-bearing for paper-test alpha):
  v5.14.0 Ridge ─────┐
  v5.14.1 Conf+winsor ─┤
  v5.14.2 Hot-swap ────┴──> Phase 1 close → first paper-test session

Phase 2 (Operational + lineage):
  v5.14.3 3-layer hash ─┐
  v5.14.4 Multi-recon ──┤
  v5.14.5 CS targets ───┤
  v5.14.6 /bug-check ───┤
  v5.14.8 Lineage+stale ┴──> Phase 2 close → second paper-test session

Phase 3 (Maker MVP; off by default):
  v5.14.7 Maker ─────> ship; enable in dedicated maker-mode paper-test
```

No hard cross-phase dependencies (each sub-ship is independent).
v5.14.6 `/bug-check` is independent of all but ideally ships
mid-sprint so it can scan post-Phase-1 code.

---

## Cohesion verification plan

After all sub-plans drafted, run:
1. `/plan-check` on master + all sub-plans (cross-plan integration
   matrix verification)
2. `/parity-check` at v5.13.6 baseline (snapshot pre-v5.14
   protections inventory)
3. For each sub-plan: strengthened `/readiness` Check 19 (NEW +
   REUSE claim verification via grep)
4. For each sub-plan touching ≥3 new fns: `/trace-deps`
   (dependency-chain audit)
5. /merge-scan on baseline (catch latent reuse opportunities
   before sprint adds new code that might duplicate them)

Each sub-plan needs PASS verdict on Check 19 + /trace-deps before
its sub-ship code is written.

---

## Open decisions for operator

1. **v5.14.8 phase placement** — bundle with Phase 2 (lineage +
   safety enrichment) seems natural; confirms?
2. **v5.14.5 LABEL_REGISTRY_HASH bump** — operator must retrain
   any active models post-flip. Acceptable since v5.14 is pre-
   paper-test anyway?
3. **v5.14.0 Ridge across-horizons mode default** — ship default
   off (preserve bandit selection); flip to on after Phase 1
   paper-test if Ridge weights look sane?
4. **v5.14.7 Maker MVP enable order** — ship code-complete in
   v5.14.7 with `cfg.maker_orders_enabled=0` default; operator
   opts in for dedicated paper-test session? (recommended path)
5. **Pass 3 audit's "Environment metadata in stamp"** — capture
   compiler version + CUDA + libraries at trainer time. Is this
   useful for operator audit, or noise? (default: ship as
   informational; operator can ignore)
