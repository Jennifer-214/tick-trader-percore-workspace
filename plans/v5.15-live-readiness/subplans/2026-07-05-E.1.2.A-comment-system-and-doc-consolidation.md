# E.1.2.A — In-code documentation system + workspace consolidation

**Type:** infrastructure / QOL foundation (dev-apparatus — LIGHT process; dogfood-iterate, validate-by-use, NOT heavy pre-gate per `feedback_process_weight_by_surface_blast_radius`).
**Status:** DRAFT / LIVING 2026-07-05. Co-evolves with the operator's parallel `fox-symdeps.nvim` session.
**Gates:** the resumption of E.1.2 2d+ AND all more-complex-architecture work.
**Refs:** D-306, D-307 (+ its amendment); `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md`; `tools/plugins/fox-symdeps.nvim`.

## Rationale

Before pushing to more complex architectures, the infrastructure + knowledge base must be organized to *support* the code, not drift beside it (operator, 2026-07-05). A one-time foundation investment that compounds: navigation, organization, drift-resistance. Fits the MVP→professional transition (`user_mvp_to_professional_transition`).

## End goal

A polished, machine-parseable **and** human-readable in-code documentation layer (the comment/tag schema), its CI tooling + scaffold generator, the codebase converted to it, and a consolidated / de-sprawled / dated-info-free workspace — so the knowledge base actively supports the code.

## Scope (bounded)

- **IN:** the comment schema (D-306/307) + its CI validator + scaffold generator + (if DERIVED fork-b) the derived-refresh tool; codebase conversion to the schema; workspace doc consolidation (de-sprawl) + dated-info identification & resolution.
- **OUT (explicit):** new engine features; the E.1.2 persist code (2d+); "reorganize everything forever" — P4 is scoped to CONSOLIDATE + DATE-CHECK existing docs, not rewrite them.

## Phases

- **P0 — Schema lock (CURRENT GATE).** Resolve the open forks — **DERIVED (a) plugin-live-only vs (b) tool-refreshed-in-comment** (orchestrator leans b); the **bars** (`====` between groups + `----` under group-name); the schema↔plugin **contract** as the plugin co-evolves. Sweep the worked examples to the locked style. → spec v1.0 LOCKED.
- **P1 — Tooling.** CI parser/validator (vocab · ladder · one-category-per-line · `[END_*]` closers · `[REFERENCE]` resolution) + the scaffold generator (emits the block skeleton) + (fork-b) the derived-refresh tool. Coordinate the contract with the plugin.
- **P2 — Pilot conversion.** Convert a few representative units (a hot struct, a slow kernel, a `FOREACH_*` registry, a file header) → validate the format + tooling in real use → adjust the schema if the pilot surfaces friction.
- **P3 — Rollout conversion.** Incremental, tool-assisted, **preserve-voice** (relocate existing WHY-prose verbatim; add structure around it), file-by-file. New code uses the format immediately.
- **P4 — Workspace consolidation (bounded).** De-sprawl overlapping docs; identify dated info; resolve each (update / archive-with-tombstone / delete). Scoped to consolidate + date-check, NOT rewrite.

## Deferred behind this ship

E.1.2 2d+ (ordered `FOREACH_NODE_PERSIST_FIELD` emitter · count-locks + paired-bump + layout tool · REC-A fold + D-304 doc-fixes) + more-complex-architecture work. Resume after E.1.2.A closes. The 2b/2c code is already committed (`8648e27`), so the persist work is at a clean, resumable boundary.

## Acceptance criteria

- Schema spec v1.0 LOCKED (forks resolved; examples swept to the final style).
- CI validator + scaffold generator exist and pass their self-tests (golden fixture per the schema self-test).
- Codebase converted, or a clear tracked remainder with a resolution plan.
- Workspace consolidated; no known dated info without a resolution.
- The plugin consumes the schema (operator's parallel session) — the contract holds.

## Open / co-evolving

- **P0 forks** are the current blockers (operator's call: DERIVED a/b, bars).
- Living plan: the plugin session may reshape the contract; P0 tracks that.
- Anxiety-antidote note (operator, 2026-07-05): the ideas are captured (D-306/307); this plan is the container so they stop competing for attention. No external clock — the foundation compounds.
