# v5.15.5.F.4f — Cleanup ship: close `.F.4c.3` deferred scope (TECH_DEBT-076 to -080 + -082) [STUB — PRE-CODING]

**Branch:** stay on `feat/v5.15-live-readiness` (sprint branch)
**Rollback anchor:** tag `pre-v5.15.5.F.4f` before any edits (created at Step 0)
**Predecessor:** `.F.4e` (KIND_STRING + KIND_FILE_PATH + 6 GUI metadata derived filter applications including FAILURE_MODE GUI Class-18 closure per Step 3a)
**Successor:** `.F.4g` (ResolvedCoreCfg slow-path resolution cache; renamed from `.F.4f` pre-cascade)
**Status:** **STUB — PRE-CODING placeholder.** Do NOT execute against this file as canonical. Full plan body draft + pre-coding audit gate REQUIRED before promoting to ACTIVE.
**Created:** 2026-05-16 per `.F.4d` pre-coding subplan verification audit (operator decision to insert cleanup ship between `.F.4e` second-source validation and `.F.4g` ResolvedCoreCfg; sequences `.F.4c.3` deferred-scope closure structurally BEFORE `.F.4` umbrella close)

---

## Why this ship exists

`v5.15.5.F.4c.3` (shipped 2026-05-15) shipped ~60% of its original subplan scope. The structural OMS work (Class 27/28/29 closure + comprehensively branchless OrderManager + Pattern 5 sink-fn-pointer) absorbed mid-flight from the DrainPostFill recompute-from-cfg gap; the planned WIP2d-1.B.2 / WIP2e / WIP2f / WIP2g/h phases were pushed to keep the ship boundary focused.

The deferred phases are FUNCTIONAL at HEAD via transitional infrastructure (`ControllerConfig_PopulateCoresFromFlat` shadow walker + legacy `PerCoreOverrides<F>` struct + 89 flat per-core field declarations + 9 band-aid `PopulateCoresFromFlat` test fixture call-sites). But the transitional infrastructure should not persist long-term — confuses contributors, hides the canonical shape, leaves Class 25 mirror-tracking risk at the residual consumer sites.

**This ship:** atomic flag-day cleanup. Bundles 5+ TECH_DEBT items into one focused effort with proper rollback anchor + sub-ship boundaries for high-risk phases.

**Operator framing 2026-05-16:** *"finish current then do incomplete items"* — Caramel. Sequenced AFTER `.F.4d` (framework consolidation) + `.F.4e` (second-source validation) so the new framework infrastructure lands cleanly BEFORE mechanical cleanup. Pure cleanup ship; zero framework or feature additions; minimum scope-creep risk.

---

## Scope (5 primary TECH_DEBT items + 2 `.F.4c` smaller deferrals + 1 conditional `.F.5` residual)

### Phase 1 — `TECH_DEBT-076` ControllerEventLoop + EngineSharded + Backtest wrapper migrations

Apply single-param `const PerCoreCfg<F>*` discipline to remaining ~10-15 ControllerEventLoop wrapper sites + EngineSharded boot defaults + Backtest path. Per `DESIGN_SPECS/cfg-scope-discipline.md` § "consumer over per-core array" + "single-param sig discipline". Sister-canonical to `.F.4c.3` WIP2c.0/2c.2 Class 25 sweep.

**Effort:** ~4-6h focused. **Risk:** LOW (mechanical migration).
**Sites:** enumerated in original `.F.4c.3` subplan body (Steps 4-7 region).

### Phase 2 — `TECH_DEBT-077` A2 bitmap-bool migration (28 KIND_BOOL flat rows → domain bitmaps)

Identify candidate KIND_BOOL rows that pass cfg-flag-eligibility criteria per `DESIGN_SPECS/cfg-flag-eligibility-criteria.md`. Migrate from KIND_BOOL direct-int storage to bitmap-bit storage in appropriate domain (`ml_cfg_flags`, `lifecycle_cfg_flags`, or new domain per audit outcome). Sister to `.F.4c.1`'s 18-row STAMP_BOUND cohort migration via established bitmap-dispatcher pattern.

**Effort:** ~3-4h focused. **Risk:** LOW-MED (cohort-uniform mechanical).

### Phase 3 — `TECH_DEBT-080` `[core N]` section parser syntax (operator-facing cfg syntax)

Add `[core 0]`, `[core 1]`, ... section header syntax to engine.cfg parser. Operator writes section-scoped key=value lines instead of verbose `core_N_X=Y` flat-prefix form. Parser detects `[core N]` header + scopes subsequent key=value lines until next section header or EOF. Legacy flat-prefix syntax remains acceptable post-shipping (section syntax is sugar).

**Effort:** ~2-3h focused. **Risk:** LOW (operator UX; not load-bearing).
**Site:** `CoreFrameworks/ControllerConfigParser.hpp` parser + `engine.cfg` example documentation. In-code TODO at `ControllerConfig.hpp:2097` explicitly says "Future ship .F.4c.3 Step 3 introduces [core N] section parser".

### Phase 4 — `TECH_DEBT-079` Atomic flag-day (89 flat field declarations + ~414 test fixture migrations + 9 band-aid call removals)

Atomic flag-day deletion of all 89 flat per-core field declarations from ControllerConfig (`int core_0_strategy`, `FPN<F> core_0_risk_pct`, etc.) + sweep migration of ~414 test fixture writes from `cfg.core_0_X = Y` shape to `cfg.cores[0].X = Y` shape + remove the 7-9 band-aid `ControllerConfig_PopulateCoresFromFlat(&cfg)` call-sites in tests. After flag-day, the shadow walker becomes orphaned + can be deleted (Phase 5); legacy flat fields gone; `PerCoreCfg<F>` sole source-of-truth.

**Effort:** ~6-10h focused. **Risk:** HIGH (largest deferred scope; ~414 test fixture writes; mechanical migration but high-touch — risk of breaking tests if migration is imprecise; needs careful per-fixture verification).

**Sub-step recommendation:** boundary-stable refactor approach — do test fixture migration FIRST (with band-aid `PopulateCoresFromFlat` calls still in place), verify tests GREEN, THEN delete the 89 flat field declarations + band-aid calls + orphaned shadow walker. Two-pass minimizes test-failure surface.

### Phase 5 — `TECH_DEBT-078` Legacy `PerCoreOverrides<F>` + `ControllerConfig_ResolveForCore` + `core_overrides[16]` deletion

Delete `PerCoreOverrides<F>` struct definition at `CoreFrameworks/ControllerConfig.hpp:~255` + `ControllerConfig_ResolveForCore` function at `:~1404` + `core_overrides[16]` field on ControllerConfig + all caller sites still using `ResolveForCore` resolution path. Comment at `CfgFieldRegistry.hpp:700` says "TRANSITIONAL — delete at WIP2f".

**Effort:** ~2-3h focused. **Risk:** LOW once Phase 1 + Phase 4 complete (close-out work).
**Dependency:** REQUIRES Phase 1 + Phase 4 completion (forward dependency — consumer migration must finish before legacy types can be deleted).

### Phase 6 — `.F.4c` smaller deferrals (surfaced in backfilled `.F.4c` postmortem 2026-05-16)

- **Per-core override path refactor**: Subplan item 5 deferred post-`.F.4c` ship; mechanically absorbed at WIP2f scope per TECH_DEBT-078 Phase 5. Verify deletion in Phase 5 covers it; if residual sites remain, sweep here.
- **Reset/Modified UI**: Subplan item 6 deferred post-`.F.4c`; smaller scope (FPN<F> `operator==` vs ImGui `ImTextureRef` ambiguity investigation needed). Can fold here OR defer further to a focused UI-polish mini-ship. Decision at coding time per operator preference.

### Phase 7 (CONDITIONAL) — `TECH_DEBT-082` `.F.5` 3 unmigrated fields per-core eligibility audit

Per decision at `.F.4d` pre-coding subplan verification audit, `confidence_ic_floor` / `lazy_rebuild_price_threshold_pct` / `exit_threshold` were not absorbed by `.F.4d` Thread B at planning time. If `.F.4d` Thread B does NOT pick them up at coding time (decision at Section 5.G-J of `.F.4d` merged plan body), bundle here for cohort-harmonization completeness. ~1-2h focused.

---

## Sub-ship boundaries (tentative — finalize at pre-coding stage)

Recommended split into 3-5 sub-ships with their own sub-tags + commits:

- **`.F.4f.A`** — Phase 1 + Phase 3 (ControllerEventLoop wrapper migrations + `[core N]` parser syntax — boundary-stable cleanup; LOW risk)
- **`.F.4f.B`** — Phase 2 (bitmap-bool migration — uniform mechanical; LOW-MED risk)
- **`.F.4f.C`** — Phase 4 (atomic flag-day; HIGH risk; largest scope; deserves its own boundary)
  - Sub-step **.C.1** — test fixture sweep with band-aids still in place
  - Sub-step **.C.2** — flat field declaration deletion + band-aid removal
- **`.F.4f.D`** — Phase 5 (legacy `PerCoreOverrides<F>` deletion; close-out; LOW risk post-Phase 1+4)
- **`.F.4f.E`** — Phase 6 + Phase 7 (`.F.4c` smaller deferrals + conditional `.F.5` residuals; LOW-MED risk)

(Sub-ship boundaries are NOT finalized — adjust at pre-coding draft based on Phase-4 risk profile.)

---

## Out-of-scope (explicit — DO NOT absorb)

- **AoS-by-core re-layout (`.F.4i`)** — depends on slow-path profile data; separate ship per CLAUDE.local.md "Open architectural decisions"
- **Symbol axis full migration (TECH_DEBT-081)** — depends on `.F.4e` KIND_STRING infrastructure; separate ship per operator decision
- **Any new framework additions** — pure cleanup ship; zero framework or feature additions allowed (scope-creep guard)
- **Hot path touches** — `BG_Evaluate` / `SG_Evaluate` / `ExecutionCore_Tick` zero changes; mechanical cleanup is slow-path / drainer / boot / tests only

---

## Pre-coding triggers (REQUIRED before promoting STUB → ACTIVE)

This stub captures scope + sequence + dependencies + risk profile. Before any code lands:

1. **Full plan body draft** — expand each Phase section with: surface (file:line refs verified against HEAD at that future moment), key changes, verification gate per-phase, anti-patterns to avoid, "if something goes wrong" recovery notes.
2. **Pre-coding audit gate** — fire `/precoding-audit-gate` Layer-1 orchestrator (parity + trace-deps + readiness + DOD + merge-scan) per `.F.4d` cadence. Address findings before coding.
3. **Cold-pickup handoff** — write handoff doc per `/handoff` skill that loads the audit synthesis + this plan body.
4. **Consult operator** post-audit-gate per `feedback_consult_on_audit_findings` discipline BEFORE coding.

---

## Tests

Baseline at `.F.4f` start: post-`.F.4e` test count (likely ~4100-4150). Expected new:
- Phase 1: ~5-10 regression tests for ControllerEventLoop wrapper sig changes
- Phase 2: ~5-10 regression tests for bitmap-bool migration (per-bit set/clear/get parity)
- Phase 3: ~10-15 parser tests for `[core N]` syntax (legacy flat-prefix continues to work; section syntax sugar parity)
- Phase 4: TEST FIXTURE MIGRATION RISK — ~414 fixture writes change shape; need careful per-fixture verification; no NEW tests expected (existing tests just re-pointed at new field shape)
- Phase 5: removal verification (compile-test that `PerCoreOverrides<F>` + `ResolveForCore` symbols GONE)
- Phase 6/7: minimal regressions

---

## Verification gate

**Universal:** `./build.sh test` GREEN; all 5 binaries (`gui suite tsan asan all`) clean; CI Check 1-12 PASS; `/parity-check` GREEN; `/merge-scan` GREEN; `/bug-check` CLEAN (no new Class 18/19/21/23/25/27/28 instances introduced).

**Cleanup-specific:**
- `rg PerCoreOverrides /home/caramel/code/FoxML_Trader_v2/` returns ZERO hits (symbol GONE codebase-wide)
- `rg ControllerConfig_ResolveForCore` returns ZERO hits (function GONE)
- `rg core_overrides\[16\]` returns ZERO hits (field GONE)
- `rg ControllerConfig_PopulateCoresFromFlat` returns ZERO hits (shadow walker GONE after Phase 4)
- 89 flat per-core field declarations DELETED from ControllerConfig (verify struct shrunk by expected byte count via `static_assert(sizeof(ControllerConfig) == ...)`)
- `[core N]` section parser passes round-trip parity tests with legacy flat-prefix form

**Hot path:** UNTOUCHED (mechanical cleanup; no HP/SP/drainer surface). `tools/calls_graph_diff.sh verify` confirms.

**Replay determinism:** cfg roundtrip byte-identical for migrated fields; backtest determinism preserved.

**HOT_PATH_CHANGELOG:** NONE entry (cleanup ship; no hot-path additions).

---

## Ship close

- Version.hpp bump `.F.4e` → `.F.4f` (same commit as tag)
- Git tag `v5.15.5.F.4f` annotated + GPG-signed
- Postmortem at `plans/v5.15-live-readiness/postmortems/2026-05-XX-v5.15.5.F.4f-postmortem.md`
- Auto-writes:
  - `DOCS/CHANGELOG.md` `.F.4f` row
  - `DOCS/TECH_DEBT.md` 5 closures (-076, -077, -078, -079, -080) + possibly -082 if Phase 7 fires
  - `FEATURE_LOOKUP.md` `[core N]` parser syntax entry (operator-visible)
  - `CLAUDE.local.md` sprint state Most-recent-ship row → `.F.4f` + update legacy transitional infra references
  - `DESIGN_SPECS/cfg-scope-discipline.md` § "Consumer over per-core array" — close-out note (legacy ResolveForCore eliminated)
- `/sync-workspace` after ship close

---

## Pointers

- TECH_DEBT-076 to -080 + -082 entries in `DOCS/TECH_DEBT.md`
- `.F.4c.3` postmortem § "Deferred from original subplan scope (substantial)" (added 2026-05-16) — original subplan phases enumerated
- Backfilled `.F.4c` postmortem (added 2026-05-16) — `.F.4c` smaller deferred items (per-core override path refactor + Reset/Modified UI)
- `DESIGN_SPECS/cfg-scope-discipline.md` § "Consumer over per-core array" + "single-param sig discipline" + Anti-pattern 1 (forbidden `PerCoreOverrides<F>` extension mechanism)
- `DESIGN_SPECS/cfg-flag-eligibility-criteria.md` (per-core eligibility framework for Phase 2 + Phase 7 audits)
- `DESIGN_SPECS/bitmap-flag-api.md` + `universal-registry-bitmap-dispatcher-pattern.md` (Phase 2 mechanism)
- CLAUDE.md item 13 (single source of truth via X-macro)
- CLAUDE.md item 19 (structural fix preferred when bug class can recur)
- CLAUDE.md item 23 (Class 23 anti-pattern — manual parser; relevant if Phase 7 picks up the 3 `.F.5` fields with manual parsers)
- CLAUDE.local.md "Ship after `.F.4e`" row (sprint-state authoritative sequence)
