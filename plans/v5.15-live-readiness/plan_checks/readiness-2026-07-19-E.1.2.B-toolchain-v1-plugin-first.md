# /readiness report — `2026-07-19-E.1.2.B-toolchain-v1-plugin-first.md` — 2026-07-19

**Context:** produced during `/accept-handoff` Stage 6 (pickup of the `status: active` handoff `2026-07-19-E.1.2.B-handoff.md`). Dev-plane ship; JUDGMENT-only pass (mechanical floor already GREEN via `check_session_docs.sh`).

## VERDICT: GREEN — proceed to build 0.1.5

The 0.1.5 design is well-specified (explicit enumerate-first build order + on-disk concrete spec + 4 DECIDED decisions D-376/377/378/379), dev-plane-correct (content-hash-not-HMAC, `git_head` staleness, SSoT emit/read, enumerate-before-generalize), and canonical-sister-clean (0 real duplications; extends `foxtag_client`/`foxroots`). All findings are same-day stale-prose / table-completeness hygiene — worth-fixing-during, none blocking the dive.

## Focus-question findings
- **(a) Is build 0.1.5 well-specified for a fresh session? — YES.** Build order explicit + enumerate-first (plan:144); concrete contract on disk (spec §1 envelope, §2 record-sets, D-378 kinds). Keystone `foxtag_main.cpp:127` `cmd_grammar` prints COUNTS, `--json` already parsed at :216 → precise bounded change. `tools/foxtag_client.py` exists (layout/fields/codegen/unit_at/inventory) with NO `grammar()`/envelope yet = the exact 0.1.5 gap on a real extension target.
- **(b) Cold-pickup (C.1–C.10) — one real cluster (C.6), YELLOW-lite:** stale prose about 0.1 (partially landed at HEAD). See fix list.
- **(c) Dev-plane correctness risk in 0.1.5 — NONE material.** Record-set typing DRAFT/locked-at-dive (enumerate-first); emit/read SSoT (D-378(3), spec:65 one-write-one-read foxroots-sibling); `.toolbus/` staleness via `target.git_head` (spec:37/52); content-hash-NOT-HMAC with explicit threat-model reasoning (spec:66 — correct; HMAC here would cargo-cult engine H9). Adoption per-consumer behind `parity_check` (D-349) → raw helpers don't silently break.
- **(d) Canonical-sister "5 candidates, 0 duplications" — SOUND.** foxroots EXTRACT real (`check_doc_metadata.py:6` re-exports); FOREACH_PLANE = the one new registry, correctly gated behind `/merge-scan`+`/anti-spaghetti` before 0.8. Table is stale w.r.t. 0.1.5 shared infra (see fix 5).

## Stale-prose fixes (worth-fixing-during, ~10 min — the plan's OWN line-20 Stale-prose guard)
1. **Pre-coding triggers (plan lines 221–222)** route the code-sample sidecar + `/precoding-audit-gate` to the **"0.1 dive"** — never updated to name the **0.1.5** dive now being greenlit. Fix so a future fresh session doesn't skip the 0.1.5 gate.
2. **Re-banner 0.1** to reflect partial-landed state: `foxroots.py` exists; `check_conversion_completeness.py` already `from foxroots import ENGINE` (:8) — the "`:32` FIRST portability-dead gate" is **already fixed**; `check_import_from_core.py` exists (selftest :110); 6 tools migrated.
3. **Sister-table path (plan line 98):** `tools/lib/foxroots.py` → **`tools/foxroots.py`** (imports are `from foxroots import …`, no `lib`).
4. **"2 NEW DESIGN_SPECs"** (Step 0 + trigger 5) → **3** (versioning + update-orchestrator + standardized-tool-io, all on disk).
5. **Canonical-sister table:** add the 0.1.5 shared-infra rows (envelope-emit helper [SISTER→foxroots], record-set reader, `foxtag_client` [EXTEND, exists], `.toolbus/` [SISTER→decoupling published-state]) so the table matches the discipline already met in the spec/decisions.

## Acceptable risk (don't block)
- Envelope wrapping vs `foxtag_client` raw-JSON helpers — handled by D-349 per-consumer-behind-parity.
- "dup DERIVED formatter in two more" (plan:28) is looser than cited; real hits `tag_grammar_adapter.lua:63` + `tagadapter.lua:16` + scatter. Class-18 mirror class is real; exact count imprecise. Cosmetic.

## Dependency verification (grep-confirmed)
| Claimed | Verified | Notes |
|---|---|---|
| `foxtag_main.cpp:127` cmd_grammar prints counts | ✅ :128 | keystone exact; `--json` at :216 |
| `tag_grammar_adapter.lua:27` hardcoded UNIT | ✅ | + parse rule :17-32, format_derived :63 |
| `tagcontext.lua:9-10` UNIT copy | ✅ | 2nd mirror |
| `tagadapter.install` never called from setup() | ✅ | def `tagadapter.lua:22`; only in a comment |
| `check_conversion_completeness.py:32` hardcoded ENGINE | ⚠️ already migrated → `from foxroots import ENGINE` (:8) | stale claim (fix 2) |
| `tools/lib/foxroots.py` | ⚠️ path wrong → `tools/foxroots.py` | fix 3 |
| 3 NEW DESIGN_SPECs on disk | ✅ | undercount as "2" (fix 4) |
| `.toolbus/` / `TOOLCHAIN_VERSION` | ✅ absent | correct — 0.1.5 / 0.1 deliverables |
| `foxtag_client.py` grammar()/envelope | ⚠️ absent | correct extension gap |
| decision log D-376..D-379 DECIDED | ✅ | + DECIDED sentinels |

*(Report generated by the Explore Layer-2 executor of /readiness; verbatim-preserved at pickup. All facts grep-confirmed against HEAD `4c076ed`.)*
