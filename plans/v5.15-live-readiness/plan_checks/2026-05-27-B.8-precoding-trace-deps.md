---
type: audit-report
audit: /trace-deps
plan_audited: subplans/2026-05-27-v5.15.5.F.4d.1.B.8-accounting-cohort-fix.md
ship_tag: v5.15.5.F.4d.1.B.8
audit_tier: HIGH-RISK (1 of 5 in /precoding-audit-gate)
date: 2026-05-27
established: 2026-05-27
verdict: YELLOW
finding_counts:
  BLOCKING: 0
  HIGH: 2
  MED: 3
  LOW: 2
tags: [accounting, class-26-closure, pnl-drift, per-core-indexing, ci-tooling, fee-floor, trace-deps]
surface: [slow-path, oms-drainer, fee-floor, per-core-indexing, ci-tooling, gui-diag, strategy-ratchet, display]
sister_specs:
  - meta-disciplines/structural-fix-preferred-decision-framework.md
  - framework-patterns/cfg-field-categorization-discipline.md
  - meta-disciplines/canonical-sister-extension-discipline.md
  - refactor-patterns/decision-time-data-binding-pattern.md
---

# /trace-deps report — v5.15.5.F.4d.1.B.8 accounting cohort fix — 2026-05-27

## Summary

- Plan body audited: `subplans/2026-05-27-v5.15.5.F.4d.1.B.8-accounting-cohort-fix.md`
- NEW functions analyzed: 1 (`scan_check_10_violations` in `tools/check_per_core_registry_integrity.py`)
- Per-core consumer caller chains verified: 4 HIGH fix sites
- Comprehensive cohort enumeration cross-sweep: per-core-migrated fields (fee_rate / fee_rate_taker / fee_rate_maker / slippage_pct / fee_floor_mult) × per-core consumer sites
- Result counts: 2 HIGH / 3 MED / 2 LOW / 0 BLOCKING

**Verdict: YELLOW** — Audit's 4 HIGH fix sites are STRUCTURALLY CORRECT per per-core caller chains; sister-cohort scope is ALMOST complete but trace-deps surfaces 2 HIGH additional UNINDEXED-GLOBAL sites the audit missed (RegimeDetector.hpp:760+789+808 + StrategyParameters.hpp:1762) + 3 MED informational items + 2 LOW design issues. Plan should EXPAND Phase A scope before scope-lock to include these, OR document explicit rationale for excluding them.

---

## Caller chain verdict per HIGH fix site (all CONFIRMED per-core)

### HIGH-1: `ControllerEventLoop.hpp:3605-3606` — `EventLoop_TrailingSLRatchetOneCore`

**Signature** (verified at file:line 3581):
```
inline void EventLoop_TrailingSLRatchetOneCore(EventLoopState<F>* state,
                                                const ControllerConfig<F>& cfg,
                                                const RollingStats<F, W>& rolling,
                                                double current_price,
                                                int core_id) {
    ...
    double fee_taker_d = FPN_ToDouble(cfg.fee_rate_taker);  // ← line 3605 GLOBAL READ
```

**Caller chain:**
```
EngineCommon.hpp:718 (EngineCommon_SlowPathCycleOneCore body)
  ├─ Function signature: `inline void EngineCommon_SlowPathCycleOneCore(const ControllerConfig<F>& cfg, int c, ...)` at :476
  ├─ Body iterates `c` as per-core loop variable
  └─ Calls: `EventLoop_TrailingSLRatchetOneCore(&state, cfg, sst->rolling_short, price_d, c)`
                                                                                          ^^^
                                                                                          per-core `c` → `core_id`
```

Verdict: **CONFIRMED per-core**. `core_id` is the per-core slot.
Action: Apply Phase B Step B.1 fix (`cfg.fee_rate_taker` → `cfg.cores[core_id].fee_rate_taker`).
**Cross-ref:** Class 26 closure pattern; sister to `.B.7` Async.hpp:814+853 WRONG-INDEX → UNINDEXED-GLOBAL sub-shape.

---

### HIGH-2: `ControllerEventLoop.hpp:3670-3671` — `EventLoop_BreakevenOnProfitOneCore`

**Signature** (verified at file:line 3655):
```
inline void EventLoop_BreakevenOnProfitOneCore(EventLoopState<F>* state,
                                                const ControllerConfig<F>& cfg,
                                                double current_price,
                                                int core_id) {
    ...
    double fee_taker_d = FPN_ToDouble(cfg.fee_rate_taker);  // ← line 3670 GLOBAL READ
```

**Caller chain:**
```
EngineCommon.hpp:734 (EngineCommon_SlowPathCycleOneCore body)
  ├─ Same enclosing fn as HIGH-1 (per-core `c` variable in scope)
  └─ Calls: `EventLoop_BreakevenOnProfitOneCore(&state, cfg, price_d, c)`
                                                                       ^
                                                                       per-core `c` → `core_id`
```

Verdict: **CONFIRMED per-core**. Same caller pattern as HIGH-1 (PARITY-032 closure pair at `.B.4`).
Action: Apply Phase B Step B.2 fix.

---

### HIGH-3: `StrategyLifecycle.hpp:272-273` — `Strategy_WriteRatchetSL`

**Signature** (verified at file:line 264):
```
inline bool Strategy_WriteRatchetSL(EventLoopState<F>* state, int slot,
                                     FPN<F> proposed_sl, FPN<F> entry_price,
                                     const ControllerConfig<F>* cfg) {
    ...
    FPN<F> fee_taker = !FPN_IsZero(cfg->fee_rate_taker)  // ← line 272 GLOBAL READ
        ? cfg->fee_rate_taker : cfg->fee_rate;
```

**Caller chain (4 callers via comprehensive grep):**
```
1. Strategies/private/EmaCross.hpp:216 (EmaCross_ExitAdjustSharded)
   ├─ Signature: `inline void EmaCross_ExitAdjustSharded(EventLoopState<F>* state, int slot, ...)` at :182
   └─ Calls: `Strategy_WriteRatchetSL(state, slot, trailing_sl, entry, cfg)`
                                              ^^^^
                                              per-core slot

2. Strategies/Momentum.hpp:393 (Momentum_ExitAdjust)
   ├─ Signature: per-core; `slot` in scope (verified at :393 context)
   └─ Calls: `Strategy_WriteRatchetSL(state, slot, trailing_sl, entry, cfg)`

3. Strategies/MeanReversion.hpp:613 (MeanReversion_ExitAdjust; forward-decl at :561)
   ├─ Per-core; `slot` in scope
   └─ Calls: `Strategy_WriteRatchetSL(state, slot, trailing_sl, entry, cfg)`

4. Strategies/MLStrategy.hpp:314 (MLStrategy_ExitAdjust)
   ├─ Per-core; `slot` in scope
   └─ Calls: `Strategy_WriteRatchetSL(state, slot, trailing_sl, entry, cfg)`

5. CoreFrameworks/ControllerEventLoop.hpp:2530 (per-core rebuild post-regime-adjust)
   ├─ Inside per-core outer scope (slot in scope via outer per-core dispatch)
   └─ Calls: `Strategy_WriteRatchetSL(state, slot, tight_sl, entry, cfg)`
```

Verdict: **CONFIRMED per-core via ALL 5 caller sites** (audit cites 3 callers; trace-deps finds 4-5 with MLStrategy + the ControllerEventLoop.hpp:2530 site).
Action: Apply Phase B Step B.3 fix.
**Note for Plan amendment:** Audit cites "3 callers (EmaCross + Momentum + MeanReversion forward-decl)" — plan body should also acknowledge `MLStrategy.hpp:314` (4th caller) AND the in-file caller at `ControllerEventLoop.hpp:2530`. Caller-count update from 3 → 5 doesn't change the fix (all per-core), but completeness matters for postmortem accuracy.

---

### HIGH-4: `ControllerEventLoop.hpp:3042-3043` — `EventLoop_RebuildOneCore` GUI diag

**Signature** (verified at file:line 2240):
```
template <unsigned F, unsigned W = 128, unsigned WL = 512>
inline void EventLoop_RebuildOneCore(EventLoopState<F>* state, int slot, ...)
```

**Outer-loop scope verification at ~line 2315:**
```
ControllerEventLoop.hpp:2315-2316:
  ControllerConfig<F> resolved_cfg = ControllerConfig_ResolveForCore(*config, slot);
                                                                              ^^^^
                                                                              per-core slot
```

At line 3042-3043 (deep inside `EventLoop_RebuildOneCore` body), `slot` is the fn parameter at :2241. `resolved_cfg` is the per-core resolved cfg from `ControllerConfig_ResolveForCore(*config, slot)`. Both `slot` AND `resolved_cfg.cores[slot]` are in-scope at the fix site.

**Verification of audit's recommended fix:** `resolved_cfg.cores[slot].fee_rate_taker`. The strategy_TpFloor invocation at :3061 (same body) ALREADY correctly uses `&resolved_cfg.cores[slot]` — verifies sister pattern within same function body.

Verdict: **CONFIRMED per-core**. `slot` is fn parameter, `resolved_cfg.cores[slot]` is in-scope, sister `Strategy_TpFloor` call at :3061 confirms the correct pattern.
Action: Apply Phase B Step B.4 fix.

---

## Audit's 4 HIGH verdict summary

All 4 HIGH fix sites have **CONFIRMED per-core caller chains**. Plan body fix shapes are correct as proposed in Scope IN section code blocks. No DRIFT findings on the 4 HIGH sites.

---

## NEW findings (audit scope-gap)

### HIGH-5 NEW (SCOPE-EXPAND CANDIDATE): `Strategies/RegimeDetector.hpp:760,789,808` — `Regime_AdjustPositions` (3 fee_floor sites)

**Signature** (verified at :686):
```
inline void Regime_AdjustPositions(Portfolio<F> *portfolio,
                                     const RollingStats<F> *rolling,
                                     int old_regime, int new_regime,
                                     const uint8_t *entry_strategy,
                                     const ControllerConfig<F> *cfg)
```

**Body reads (3 sites):**
```
:760  FPN_Mul(FPN_Mul(pos->entry_price, cfg->fee_rate), cfg->fee_floor_mult)
:789  FPN_Mul(FPN_Mul(pos->entry_price, cfg->fee_rate), cfg->fee_floor_mult)
:808  FPN_Mul(FPN_Mul(pos->entry_price, cfg->fee_rate), cfg->fee_floor_mult)
```

**Caller chain (3 callers, ALL in `PortfolioController.hpp` legacy single_core):**
```
PortfolioController.hpp:1750
PortfolioController.hpp:1777
PortfolioController.hpp:1975
```

Verdict: **LEGACY single_core path**. `PortfolioController.hpp` is the deprecated single-core architecture (per CLAUDE.md "Legacy single_core LIVE is deprecated"). Per audit Cat 8 LEGACY-KEEP verdict, these are NOT Class 26 (legacy has no per-core dimension).

Action: **NO-FIX**. Cross-ref plan body Phase A.4 LEGACY classification. However: Check 10 detection logic at Phase C step 4 (legacy fn exclusion) should explicitly verify these sites don't trigger false-positives. Recommend Plan amendment to Phase C step 4 detection logic: add `PortfolioController.hpp` to whitelist (legacy single_core path; not Class 26).

**Severity: MED (informational)** — would have been HIGH if `Regime_AdjustPositions` had ANY per-core caller, but all 3 callers are legacy single_core. False-positive surface concern for Check 10.

---

### HIGH-6 NEW (POTENTIAL SCOPE-EXPAND): `Strategies/private/EmaCross.hpp:143-144` — `EmaCross_ExitAdjust` (LEGACY single_core)

**Signature** (verified at :108):
```
inline void EmaCross_ExitAdjust(Portfolio<F> *portfolio, FPN<F> current_price,
                                 const RollingStats<F> *rolling,
                                 EmaCrossState<F> *state,
                                 const ControllerConfig<F> *cfg) {
    ...
    FPN<F> fee_rate = !FPN_IsZero(cfg->fee_rate_taker)  // ← :143
        ? cfg->fee_rate_taker : cfg->fee_rate;
```

**Caller chain:** `PortfolioController.hpp:906` (legacy single_core)

Verdict: **LEGACY single_core path** — NOT Class 26. Sister Cat 8 LEGACY-KEEP.

Action: **NO-FIX**. Same Check 10 false-positive concern as HIGH-5; `PortfolioController.hpp` whitelist confirmation needed.

**Severity: LOW (informational)** — concerns Check 10 detection accuracy.

---

### MED-2 NEW (informational, audit-completeness): `StrategyParameters.hpp:1762-1763` — already-per-core variant uses `core_cfg`

**Signature context** (verified at :1762):
```
FPN<F> fee_taker = !FPN_IsZero(core_cfg->fee_rate_taker)
    ? core_cfg->fee_rate_taker : core_cfg->fee_rate;
```

This site uses `core_cfg` (NOT `cfg`); `core_cfg` is `PerCoreCfg<F>*` (already per-core). The `core_cfg` variable threads through from the enclosing per-core dispatcher.

Verdict: **CORRECT pattern** (sister-canonical reference for the audit's 4 HIGH fixes). `core_cfg` is the per-core scope; no Class 26.

Action: **NO-FIX**. Sister-canonical example. Plan body should cite this site as the canonical "what good looks like" example in Phase D Class 26 catalog amendment Worked Examples; this is the reference shape that all 4 HIGH fixes will normalize to.

**Severity: MED (information)** — improves catalog amendment quality.

---

### MED-3 NEW (Check 10 detection logic concern): scan_check_10_violations function signature derivation

Plan Phase C Step C.1 design specifies `CHECK_10_PER_CORE_CONSUMER_SYMBOLS = {'c', 'core', 'core_id', 'slot', 'i'}` whitelist.

**Trace-deps finding:** the 4 HIGH fix sites use these context symbols:
- HIGH-1: `core_id` (fn parameter)
- HIGH-2: `core_id` (fn parameter)
- HIGH-3: `slot` (fn parameter)
- HIGH-4: `slot` (fn parameter from outer `EventLoop_RebuildOneCore`)

Whitelist symbols `c` + `i` are NOT used at the 4 HIGH fix sites — they're added speculatively per audit findings (`i` per Phase A scope verify boot-time loop-iterator convention). RECOMMENDATION: Plan body should grep ALL per-core consumer functions in the codebase + verify which symbols are actually used as per-core context (rather than speculative whitelist expansion). May find sites that use `idx` (e.g., ShardedSnapshot.hpp:201 uses `int idx = __builtin_ctz(bm)` for per-position context, NOT per-core; verify `idx` isn't mistakenly added).

Verification grep at trace-deps time:
```
grep -rn 'int slot\|int core_id\|int c\|int i' --include='*.hpp' CoreFrameworks/EngineCommon.hpp Strategies/StrategyLifecycle.hpp CoreFrameworks/ControllerEventLoop.hpp
```

Per-core fn parameter symbol convention observations (codebase-wide):
- `int slot` — per-core, used in StrategyLifecycle.hpp + most ExitAdjust functions
- `int core_id` — per-core, used in HIGH-1/2 fn signatures
- `int c` — per-core, used in EngineCommon.hpp body iteration
- `int i` — used in MULTIPLE contexts (boot-time + inner loop counter); ambiguous
- `int idx` — used for per-POSITION (bit-index in active_bitmap), NOT per-core

Action: Plan body Phase C step 1 should be amended to (a) verify each whitelist symbol against codebase usage; (b) document that `idx` is per-position NOT per-core (anti-confusion); (c) consider `slot` AND `core_id` AND `c` as PRIMARY whitelist; `i` as SECONDARY (heuristic for boot-time only).

**Severity: MED** — informs Check 10 implementation accuracy; may reduce false-positive rate.

---

### MED-4 NEW (Plan Phase E test coverage check): partial vs full coverage of all 4 HIGH sites

Plan Phase E Step E.1 specifies "4 cores × 2 fields × 4 consumer paths = ~32 assertions". Trace-deps verifies each consumer path is independently testable:

- HIGH-1 (`EventLoop_TrailingSLRatchetOneCore`): testable via `Strategy_WriteRatchetSL` proxy (capturing actual ratchet_sl writes); existing `.B.7` test pattern fits
- HIGH-2 (`EventLoop_BreakevenOnProfitOneCore`): same testing harness; gain_pct triggered above net_profit_threshold
- HIGH-3 (`Strategy_WriteRatchetSL`): direct invocation (testable in isolation per existing test at controller_test.cpp:9885)
- HIGH-4 (`EventLoop_RebuildOneCore` GUI diag): testable via `state->display_meta[slot].diag_tp_pct_floor` read after RebuildOneCore

All 4 sites are independently testable. Plan's regression test depth is sufficient.

**Severity: MED (positive confirmation)** — Phase E approach valid.

---

### LOW-2 NEW (TECH_DEBT-138 entry shape): consistent with `.B.7` cohort

Cross-ref `.B.7` `TECH_DEBT-132` (dead helpers delete) + `TECH_DEBT-134` (stale comments cleanup). Both were NEW+CLOSED same ship. `TECH_DEBT-138` shape (LOW-1 dead `DrainerConstants.fee_rate_taker_d` field delete) matches:
- NEW+CLOSED same ship
- Audit-surfaced
- Mechanical delete
- Size assert update

Verdict: **CONFIRMED entry shape consistency**. No findings.

**Severity: LOW (positive confirmation)** — bookkeeping check passes.

---

### LOW-3 NEW (Check 10 sanity-verify test fixture choice)

Plan Phase C step 7 specifies revert-detect-reapply test using HIGH-1 fix site. Trace-deps observation: HIGH-1 is the SIMPLEST site (single-line read; clear `core_id` context). Good choice for sanity verify. Alternative: HIGH-4 has a more complex pattern (`resolved_cfg.fee_rate_taker` — `resolved_cfg` is a STACK-LOCAL copy from ResolveForCore; Check 10 detection logic must handle this aliasing correctly — `resolved_cfg.X` should also flag, not just `cfg.X` / `cfg->X`).

Recommendation: Plan body Phase C step 7 should sanity-verify against BOTH HIGH-1 (simple `cfg.X`) AND HIGH-4 (`resolved_cfg.X` aliased per-core var). HIGH-4 reveals whether Check 10 handles local-variable aliasing.

**Severity: LOW (Check 10 robustness)** — recommend Phase C step 7 amendment.

---

## Phase A cohort completeness verdict

**Verdict: EXPAND-SCOPE** — fold 2 NEW informational items into Phase A enumeration to verify scope-completeness.

**Phase A scope amendments recommended:**

1. **Explicit LEGACY classification** of `Regime_AdjustPositions:760+789+808` + `EmaCross_ExitAdjust:143-144` (3 sites in RegimeDetector + 1 site in EmaCross, all called from `PortfolioController.hpp` legacy single_core). Per Cat 8 LEGACY-KEEP verdict.

2. **Sister-canonical reference** at `StrategyParameters.hpp:1762-1763` (uses `core_cfg` — what good looks like). Cite as reference shape in Phase D Class 26 catalog amendment.

3. **Whitelist symbol verification**: Audit codebase fn signatures for per-core context symbols — confirm primary set `{slot, core_id, c}` + heuristic `{i}`; explicitly DOCUMENT `idx` is per-position (NOT per-core).

4. **MED-1 lines 328-331 KEEP-AS-GLOBAL exemption**: Per audit Cat KEEP-AS-GLOBAL verdict (Settings panel intentional global display); add Section D exemption with rationale to `MANUAL_FIELDS_INVENTORY.md` OR Check 10 whitelist filename if scope warrants.

**Verdict justification:** trace-deps finds no NEW Class 26 instances beyond the 4 HIGH; legitimate concern is making Check 10 detection logic ROBUST against legitimate per-core consumer patterns. Phase A enumeration as designed is sound; recommend 4 minor refinements (1-3) for completeness + (4) for false-positive prevention.

---

## Check 10 design verification

Plan Phase C step 1-7 design:
- ✓ Detection signature matches all 4 HIGH patterns
- ⚠ Whitelist `{c, core, core_id, slot, i}` — recommend trim/document per MED-3
- ⚠ Detection logic should handle `resolved_cfg.X` aliasing (per LOW-3)
- ✓ Sanity verify via revert-detect-reapply matches `.B.7` Check 9 precedent
- ✓ Sister to Check 9 via canonical-sister-extension-discipline
- ✓ Per-core for-loop enclosing scope detection planned (step 1)

Verdict: **YELLOW** — design is sound; minor refinements recommended.

---

## TECH_DEBT-138 dependency verification

Verified shape consistency with `.B.7` predecessor entries `TECH_DEBT-132` + `TECH_DEBT-134`:
- All 3 entries: NEW + CLOSED same ship
- All 3 entries: audit-surfaced (LOW severity / mechanical delete / cleanup-class)
- TECH_DEBT-138 ID is NEXT available (135-137 are pre-allocated per .B.6 / .B.7 ship close cohort)

Verdict: **GREEN** — entry shape consistent with cohort.

---

## Per-finding triage list

| # | Severity | Surface | Location | Action |
|---|---|---|---|---|
| HIGH-1 verify | n/a (CONFIRMED) | slow-path | `ControllerEventLoop.hpp:3605-3606` | Apply Phase B Step B.1 fix |
| HIGH-2 verify | n/a (CONFIRMED) | slow-path | `ControllerEventLoop.hpp:3670-3671` | Apply Phase B Step B.2 fix |
| HIGH-3 verify | n/a (CONFIRMED, +2 callers) | strategy ratchet helper | `StrategyLifecycle.hpp:272-273` | Apply Phase B Step B.3 fix; update plan body caller-count 3→5 |
| HIGH-4 verify | n/a (CONFIRMED) | slow-path GUI diag | `ControllerEventLoop.hpp:3042-3043` | Apply Phase B Step B.4 fix |
| HIGH-5 NEW | MED (LEGACY-KEEP) | regime detection | `RegimeDetector.hpp:760+789+808` | NO-FIX; Phase A.4 LEGACY classification; Check 10 false-positive prevention via `PortfolioController.hpp` whitelist |
| HIGH-6 NEW | LOW (LEGACY-KEEP) | strategy adjust | `EmaCross.hpp:143-144` | NO-FIX; same as HIGH-5 |
| MED-2 NEW | MED (positive ref) | strategy parameters | `StrategyParameters.hpp:1762-1763` | Phase D Class 26 catalog: cite as sister-canonical reference |
| MED-3 NEW | MED | Check 10 design | Phase C step 1 | Amend whitelist + document `idx` is per-position |
| MED-4 NEW | MED (positive) | regression test depth | Phase E | No action; Phase E design is sufficient |
| LOW-1 KEEP-AS-GLOBAL | n/a | display | `ShardedSnapshot.hpp:328-331` | Apply Phase B Step B.5 (comment + exempt at Check 10) |
| LOW-2 NEW | LOW (positive) | TECH_DEBT-138 | `DOCS/TECH_DEBT.md` | No action; shape verified |
| LOW-3 NEW | LOW | Check 10 sanity verify | Phase C step 7 | Verify against HIGH-1 AND HIGH-4 (resolved_cfg aliasing) |

---

## Recommendations for plan amendment

1. **Phase A.4 LEGACY classification (explicit)**: Add `PortfolioController.hpp:1750+1777+1975` + `EmaCross.hpp:143-144` + `RegimeDetector.hpp:760+789+808` to LEGACY-KEEP classification with explicit rationale "called from `PortfolioController.hpp` legacy single_core path; no per-core dimension; not Class 26 per Cat 8".

2. **Phase C step 1 whitelist amendment**: trim primary whitelist to `{slot, core_id, c}` + heuristic `{i}` for boot-time only; explicitly document `idx` is per-position context (NOT per-core).

3. **Phase C step 4 false-positive prevention**: add `PortfolioController.hpp` to fn-name / file path exclusion (legacy single_core path).

4. **Phase C step 7 sanity-verify expansion**: revert HIGH-4 fix (not just HIGH-1) to also verify Check 10 handles `resolved_cfg.X` aliasing correctly. Both reverts → tool fails; both re-applies → tool clean.

5. **Phase D Class 26 catalog amendment**: cite `StrategyParameters.hpp:1762-1763` as the sister-canonical reference shape ("what good looks like" — per-core consumer reading per-core field via `core_cfg`).

6. **Plan body HIGH-3 caller-count correction**: update "3 callers (EmaCross + Momentum + MeanReversion forward-decl)" to "5 callers (EmaCross_ExitAdjustSharded + Momentum + MeanReversion + MLStrategy + ControllerEventLoop.hpp:2530)" for postmortem accuracy.

---

## Cross-references

- `DESIGN_SPECS/meta-disciplines/structural-fix-preferred-decision-framework.md` — Class 26 recurrence 17 = MANDATORY structural fix
- `DESIGN_SPECS/framework-patterns/cfg-field-categorization-discipline.md` — PER_CORE_FLAT_SYNC_PARAMETER category; consumer-side discipline
- `DESIGN_SPECS/meta-disciplines/canonical-sister-extension-discipline.md` — Check 10 extends Check 9 (same tool, sister surface)
- `DESIGN_SPECS/refactor-patterns/decision-time-data-binding-pattern.md` — Class 27 sister closure (Order_BindPreResolved already captures fee_rate per-core at submit)
- `tools/check_per_core_registry_integrity.py` Check 9 (precedent at .B.7; M7 4th canonical paired-access mismatch)
- `DOCS/recurring-bug-patterns/class-26-global-consumer-reading-per-core-field.md` (to be amended at Phase D)
- `plans/v5.15-live-readiness/subplans/2026-05-27-v5.15.5.F.4d.1.B.7-bugfix-cleanup-umbrella-close.md` predecessor ship (Check 9 first canonical for paired-access mismatch)
- `plans/v5.15-live-readiness/plan_checks/2026-05-27-post-B.7-accounting-audit.md` audit informing this plan

---

**End of /trace-deps report v1.0 (2026-05-27).** Verdict: **YELLOW — proceed with 6 minor plan amendments before scope-lock**. All 4 HIGH fix sites have CONFIRMED per-core caller chains; sister-cohort enumeration is ALMOST complete; recommendations are scope-completeness + Check 10 robustness, NOT new bug instance discovery.
