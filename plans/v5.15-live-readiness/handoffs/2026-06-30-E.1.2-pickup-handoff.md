---
type: handoff
status: active   # active = THE one live handoff (no-arg /accept-handoff resolves here). Supersedes 2026-06-28-E.1.1-item4-core-next-handoff.md (E.1.1 shipped).
ship_tag: E.1.2-nodestate-soa-layout
plan_type: refactor
sprint_end_goal: v5.15 live-readiness — make the codebase live-ready + more maintainable
ship_end_goal: "E.1.2 — open Position/NodeState to the successor SoA layout ONCE (the determinism-gated wire/layout freeze): persist the D-206 peak + owner_node_id + riding-flag, registry-drive the NodeContext persist-serializer, delete the dead serializers, Money-ize the residual feature-typed money fields. The successor-foundation leaf the rest of .E builds on."
predecessor_handoff: 2026-06-28-E.1.1-item4-core-next-handoff.md
engine_head: 0ee227a
workspace_head: "at or after 00b8c18 (this close advances it via /sync-workspace)"
branch: feat/v5.15-live-readiness
deletion_scope: major   # E.1.2 deletes the dead Portfolio_Save/Load + PortfolioController_*Snapshot serializers (capital-persistence surface). RECEIVER: enumerate-consumers-FIRST (feedback_enumerate_consumers_before_registry_row_deletion); /accept-handoff Stage 4 auto-fires /blindspot-scan B14 at the pre-coding gate. Downgrade to minor only if the enumeration proves zero live callers AND zero compile-interdependency.
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, "plans/v5.15-live-readiness/plan_checks/2026-06-30-E-umbrella-hotpath-forward-audit-synthesis.md", "plans/v5.15-live-readiness/plan_checks/2026-06-30-E.1.2-per-field-declaration-table.md", "plans/_future/2026-06-30-hot-slow-path-optimization-architecture-and-futures.md", "plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md", "plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md", "plans/v5.15-live-readiness/MASTER.md", "DESIGN_SPECS/data-disciplines/per-node-purity-scale-invariance.md", "DESIGN_SPECS/refactor-patterns/decision-time-data-binding-pattern.md"]
coding_status: planning-incomplete   # the E.1.2 subplan body is a DRAFT/SCAFFOLD; the FIRST pickup action is to REFORMALIZE it around the amended spec + per-field declaration table in the plan_checks synthesis. Do NOT code against the current draft body.
---

# E.1.2 pickup handoff — NodeState SoA / layout freeze (successor foundation)

**Created:** 2026-06-30
**Target ship:** E.1.2 — `2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md` (the layout/wire freeze leaf; successor-foundation #6)
**Branch:** feat/v5.15-live-readiness
**Pre-tag rollback anchor:** `v5.15.5.F.4d.1.E.1.1` (E.1.1 shipped; this is the rollback point)
**Engine HEAD at write:** `0ee227a` · **Version.hpp:** `5.15.5.F.4d.1.E.1.1` · **Workspace HEAD:** `00b8c18`
**Working tree:** clean (only `?? --help`, `?? build_clangd/` untracked — harmless)
**Doc-CI at write:** `check_session_docs.sh` SWEEP CLEAN (all HARD pass).

> **This handoff is a CLEAN close after a heavy DESIGN + VALIDATION session, not a mid-coding checkpoint.** No E.1.2 code has been written. The session's product is: E.1.1 shipped (tag local-only), the E.1.2 plan AMENDED (in the synthesis doc), the candidate "constraint-driven layout" invariant VALIDATED (3:3 → not an H-row; a narrow capital-flavored core survives), and the hot/slow futures captured. The pickup's job is to CONSOLIDATE → CODIFY (small) → FORMALIZE E.1.2 → gate → code.

---

## ⚠️ FIRST loose end — E.1.1 origin push is PARKED on your SSH

E.1.1 is committed (`0ee227a`) + signed-tagged (`v5.15.5.F.4d.1.E.1.1`) **locally only** — the tool shell has no ssh-agent. Caramel runs this herself (the `! ` prefix works in-session):

```
git -C /home/caramel/code/FoxML_Trader_v2 push origin feat/v5.15-live-readiness && git -C /home/caramel/code/FoxML_Trader_v2 push origin v5.15.5.F.4d.1.E.1.1
```

Verify `git -C /home/caramel/code/FoxML_Trader_v2 log origin/feat/v5.15-live-readiness -1` shows `0ee227a` before treating E.1.1 as fully shipped.

---

## RESUME PATH (ordered — the pickup's actual work)

1. **Decisions ALREADY logged as D-282..D-286** (2026-06-30) in `v5.15.5.F.4d.1.E-architecture-v2.md`: D-282 the 3:3 no-H-row verdict + ownership/replica-coherence core · D-283 the amended E.1.2 freeze spec (7 items) · D-284 mode-cluster-homogeneous-unless-SHADOW · D-285 the hot/slow optimization futures · D-286 the synthesis-needs-its-own-adversarial-pass meta-lesson. **No re-writing — they're captured.** At reformalize, CROSS-REFERENCE them; add new D-entries only for sub-decisions the reformalization itself makes.

2. **Codify the SMALL slate (NO CLAUDE.md edit, NO new H-row — the 3:3 refuted both):**
   - Fold the **ownership / replica-coherence** model into `DESIGN_SPECS/data-disciplines/per-node-purity-scale-invariance.md` (the H22 spec): owner = **finest tier of independent determination** (coarser tiers *derive by aggregation, never co-write*); the `deployment ⊐ cluster ⊐ node ⊐ position` hierarchy (mark cluster tier forward/E.1.6 per TECH_DEBT-190); + the derived-field + pipeline-staged field categories.
   - Add the **replica-freshness** clause to `DESIGN_SPECS/refactor-patterns/decision-time-data-binding-pattern.md`: a replica **tracks** its owner — re-derived FROM the owner's value + kept fresh — never **(a)** independently re-derived from different inputs [drift; the `live_tp` Fight #4] nor **(b)** a stale unrefreshed copy [Class-27]. (A bare "owner stays SSoT" green-lights both — this is the capital tooth.)
   - Optional small Stage-2 DRAFT `state-replica-coherence.md` if the two folds don't hold it cleanly.
   - One memory: **synthesis needs its own adversarial pass** (the original 85-agent audit's GLOBAL synthesis recs went un-refuted → 3 were wrong, caught only by the dedicated A-class pass on the *recommendations*, not the findings).

3. **REFORMALIZE the E.1.2 subplan body** around the AMENDED spec + the per-field declaration table. The current subplan body is a DRAFT (`status: scaffold`) — supersede it; use the future-oriented plan template. **⚠️ RECONCILE THE E.1.3/E.1.4 SEAM-MAP FIRST:** D-223 (`decision-log:1430`) assigns the O(1) aggregator + cross-thread torn-read closure + kill-hierarchy — **the HARD LIVE-ENABLE GATE** — to **E.1.4**, but the synthesis per-leaf verdicts + the E.1.2 subplan `successor:` pointer use **E.1.3 = aggregator/coherence** (sliding fill-completeness to E.1.4). This is the single most capital-load-bearing gate in .E; the leaf-owner is ambiguous. Fix the numbering across the 3 docs before either leaf claims the torn-read close (synthesis `:64` flags it but doesn't resolve it).

4. **`/precoding-audit-gate`** against the reformalized plan (HIGH-RISK: determinism-gated wire/layout freeze on capital state → 5-agent tier + `/blindspot-scan` B14 for the deletion). Then **consult Caramel** (the standing rule: between agent rounds + after pre-coding checks, present findings, do NOT auto-proceed).

5. **Code E.1.2.** Open Position ONCE. Golden-master determinism guard. Then `/ship`.

---

## ⚠️ The pre-divergence implementation QUEUE — still pending, NOT superseded by the E.1.2 thinking

This session went DEEP on E.1.2 design + the hot-path/invariant thinking — but three items were queued **before** that divergence and still need implementing. The resume path above leads with E.1.2 because that's what this session de-risked; **it is NOT a statement that E.1.2 jumps the queue.** The ordering is Caramel's call.

| # | Item | State | Where |
|---|---|---|---|
| **#11** | boot-surface **GUI config-error warn-banner** (the DOABLE half) | the GUI banner is doable NOW; the *global-unknown-key refuse* half is **BLOCKED** on TD-223 (sibling-parser false-refuse) — do NOT attempt the refuse half until TD-223 | task #4; MASTER s10 |
| **#14** | **OQ1 — WIRE the inert sharded kill/exposure controls** | **decided-WIRE** per **D-273** — a real kill-switch/exposure-cap **capital ship**, deserves its OWN session (heavier audit posture per `feedback_heavier_default_audit_posture_for_capital`) | task #5; D-273 |
| **#15** | **GAP-2 — strategy-state type-confusion OOB on GUI swap** | **folds INTO the E.1.2 layout seam** (so this one IS coupled to E.1.2; the others are independent) | task #7 |

**Ordering options (operator's call):** (a) clear the smaller queued items first — **#11 GUI banner** (quick) + **#14 OQ1** (its own capital ship) — then E.1.2; or (b) E.1.2 now that it's design-ready (heavy determinism-gated freeze), with #11/#14 still queued. #15 rides with E.1.2 either way. **Confirm the order with Caramel at pickup before starting** (don't auto-pick E.1.2 just because this handoff details it).

---

## The AMENDED E.1.2 spec (the real ship — supersedes the draft subplan body)

The per-field declaration table (owner/tier/thread/wire for every Position + NodeContext + OMS + cluster field, cited) lives in **`plans/v5.15-live-readiness/plan_checks/2026-06-30-E.1.2-per-field-declaration-table.md`** (the verdict/why is in the sister `…-E-umbrella-hotpath-forward-audit-synthesis.md`) — that table IS the E.1.2 constraint-declaration the reformalized plan builds from, and its §3 "fights" are the design decisions the pre-coding gate resolves. The 7 items:

1. **Open `Position<F>` ONCE to its successor shape (≈192B):** add the D-206 persisted **peak** + **owner_node_id** + **riding-flag**, reserve generously. Capital-bearing wire/layout freeze → do it in ONE cycle (`feedback_design_once_maintain_forever` + `feedback_complete_structural_fix_not_incremental_prelive`). Single `static_assert(sizeof)` + golden-master, not a sizeof-only guard.
2. **Registry-drive the `NodeContext` persist-serializer** — the hand `fwrite`-per-field loop at `ShardedSnapshotPersist.hpp:176-233` is a missed `FOREACH_NODE_PERSIST_FIELD` (the `FOREACH_OMS_FIELD` pattern already does exactly this for OMS). This is the genuinely-new structural fix (extends H17 to the serializer surface; future enforcement = the meta-registry `SERIALIZER_GENERATED` flag, Stage-2 until ≥2 non-cfg conformers).
3. **Delete the dead serializers** — `Portfolio_Save`/`Portfolio_Load` + `PortfolioController_*Snapshot` (enumerate-consumers-FIRST; `deletion_scope: major` → B14 audit).
4. **FPN→Money sketch fix + Money-ize the F-096 leg-split** — residual feature-typed (`FPN_Binary`) money fields → `Money` (H4 money-path discipline).
5. **The 5 missing wire `Money` fields** — explicit wire-surface ledger (don't silently drop; `feedback_no_silent_caps`).
6. **Golden-master determinism guard** — freeze REAL serializer output + byte-diff (`feedback_golden_master_over_reimplemented_oracle`), NOT a sizeof check.
7. **Mode ownership: cluster-owns `trading_mode` (homogeneous clusters)** — no per-node override key exists today; the only per-node wedge is SHADOW (RESERVED/unimplemented). Resolve per-node only if SHADOW is deliberately wanted (a capability choice, not a discovered fact). **⚠️ RESERVE-NOT-DELETE:** "cluster-owns" is about the VALUE's SSoT — it does NOT mean delete the per-node `NodeState.hot.mode` slot. A-1 REFUTED the 85-agent audit's delete-`hot.mode` rec (E.1.5 needs `cluster.trading_mode` per D-217/D-218; E.1.6 needs `NodeState.hot.mode` per D-22/D-32) → the E.1.2 layout **RESERVES the per-node mode slot**; only the value-authority is cluster.

**The "fights" the per-field table surfaced are mostly E.1.3-homed, NOT E.1.2:** `realized_pnl` finest-tier ownership (= the E.1.3 O(1) aggregator: node owns, deployment derives) · `live_tp` track-the-owner coherence (= the E.1.3 mirror↔book surface). E.1.2 only needs to RESERVE for them, not solve them.

---

## What this session VALIDATED (the 3:3 on "constraint-driven layout" — read before re-opening it)

Caramel + I converged on a layout principle and validated it via a 3:3 (3 i-class + 3 a-class) **before** codifying. **Verdict: NOT a new H23, NOT a new CLAUDE.md principle.** Don't re-propose it as an H-row — here's why (all in the synthesis doc § "3:3 INVARIANT-VALIDATION"):
- The "layout-as-a-solve" half is **~already codified**: CLAUDE.md **item 28** (`latency-vs-cache-decision-framework`) + `decision-first-cluster-layout-pattern.md` + **H6** + **H22** + **H17**. Re-codifying = a doc-layer split-brain.
- The "5-constraint set" is **incomplete** (misses field-order, stride-`%64`, SIMD-lane-width, ABI-padding — all in existing specs) and "registry-generate everything" is **false-today** (the `Money`/`FPN_Binary` primitives, the wire-framing headers, the venue HMAC body, the bespoke cache-tuned hot structs `ExecutionCore`/`GateParameters` are CORRECTLY hand-laid — `ExecutionCore.permission` sits on its own line *because another CPU atomic-stores it*).
- What **survives** is the narrow ownership/replica-coherence core in resume-step-2 above (folds into the H22 + decision-time-binding specs).

---

## TaskList state at handoff write (recreate via TaskCreate at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | completed | E.1.1 ship — Core→Node rename + cfg clean-break + single_core deletion |
| #2 | completed | ③ config-compiler — capital/feature cfg validation |
| #3 | completed | Vocab sweep #13 — Core→Node operator-facing strings |
| #8 | completed | E.1.1 ship-close — doc sweep + tag (push PARKED on SSH) |
| #10 | completed | E-umbrella hot-path forward audit (5:5/7:7) → CONSULT |
| #11 | completed | Hot/slow-path optimization — capture + 3 I-class verify → CONSULT |
| #12 | completed | Constraint-driven-layout invariant — 3:3 validation (→ NOT an H-row; codify narrow core) |
| #9 | in_progress | E.1.2 — reformalize the scaffold into a coding plan (THE next ship) |
| #6 | pending | E.1.2 — NodeState SoA / Money relayout (the code) |
| #4 | pending | #11 boot-surface — GUI config-error warn-banner (DOABLE half) |
| #5 | pending | #14 OQ1 — WIRE the inert sharded kill/exposure controls (own ship) |
| #7 | pending | #15 GAP-2 — strategy-state type-confusion OOB on GUI swap (→ E.1.2 seam) |

Mark the completed/in_progress/pending exactly as above; #9 is the live next action.

---

## Arming for the next action (the reformalize-then-gate)

The next action is **reformalize E.1.2 (a determinism-gated capital-persistence wire/layout freeze) → `/precoding-audit-gate`**. Surface = persistence + layout + per-node state. Arm it:
- **Route to:** `/dod-audit` (DESIGN_SPECS layout-pattern application) + `/parity-check` (wire-format byte-preservation, serializer round-trip) + `/trace-deps` (the dead-serializer consumer enumeration) + `/blindspot-scan` B14 (deletion ordering) + `/accounting-audit` (the FPN→Money money-path fields). HIGH-RISK → 5-agent tier. ARM each agent per M8 (docs + toolchain + scout-first; adversarial agent independent on the verdict).
- **Run (not just read):** `./build.sh test` + golden-master byte-compare; `tools/gen_code_map.sh` (regen at pickup); `check_struct_alignment.py` (byte-serialized size-pins); `check_session_docs.sh` (doc floor).
- **Read first:** `STRATEGY_AND_CODING_RULES.md` (H4/H9/H12/H21 — money-math, wire-byte-preservation, padding-init, Knight-Capital identifier-retirement) + `wire-format-byte-preservation-discipline.md` + the per-node-purity + decision-time-binding specs (resume-step-2). Invariants in play: **H4 H6 H9 H12 H17 H21 H22**.

---

## Critical pickup-time reads (nav-infra + durable artifacts)

- **`plans/v5.15-live-readiness/plan_checks/2026-06-30-E.1.2-per-field-declaration-table.md`** — THE E.1.2 constraint-declaration: the per-field owner/tier/thread/wire table + the 9 "fights" (= the design decisions the pre-coding gate resolves), cited. Build the reformalized plan from this.
- **`plans/v5.15-live-readiness/plan_checks/2026-06-30-E-umbrella-hotpath-forward-audit-synthesis.md`** — THE synthesis (the WHY): the amended E.1.2 spec, the 85-agent audit verdicts, the A-class refutations, the 3:3 invariant-validation verdict.
- **`plans/_future/2026-06-30-hot-slow-path-optimization-architecture-and-futures.md`** — the optimization futures (select-drop→Option-C, the 8B-per-market build-time migration shape + its E.1.2 `sizeof(Money)` obligation, per-task-core coarse-cut-when-saturated, feature-clipping/SWAR, SIMD-off-hot, ns→instruction-counts). DOCUMENTED-not-do-now; the E.1.2 obligation (size the relayout so an 8B `Money` swaps cleanly) is the only do-now thread.
- **`decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`** — the .E decision SSoT; D-282+ go here.
- **The E.1.2 subplan** (`subplans/2026-06-15-…-E.1.2-nodestate-soa-layout.md`) — DRAFT; supersede at reformalize.
- **`MASTER.md`** — sprint pipeline + the .E successor-foundation ordering.
- Regen `tools/gen_code_map.sh` at pickup (CODE_MAP currency); consult `DOCS/TOOLS.md` + `DOCS/RECURRING_BUG_PATTERNS.md` (Class 25/26/27 cfg-scope, Class 47 split-brain) per `/accept-handoff` Stage 3.6.

---

## Capture-completeness (this session — nothing lost)

- **Decisions → log:** WRITTEN as **D-282..D-286** in `v5.15.5.F.4d.1.E-architecture-v2.md` (3:3-verdict / amended-E.1.2-spec / mode / futures / synthesis-needs-A-pass) — captured at session close, NOT deferred. The full detail also lives in the plan_checks synthesis + the per-field-declaration-table doc + the `_future` note.
- **Findings → ledger:** the A-class refutations + the per-field "fights" are homed in the plan_checks synthesis; the E.1.3-homed fights (aggregator-ownership, live_tp-coherence) are flagged there for the E.1.3 plan. TECH_DEBT-190 (cluster-aggregate gap) cross-referenced. No new un-homed debt.
- **Memories/skills → indexed:** ONE new memory this session — `comments-are-point-in-time-verify-code-behavior-claims-against-the-code` (indexed in MEMORY_EXTENDED.md; the CMOV-vs-`je` stale-comment catch); `SUBAGENT_ARMING.md §2.5` + a CODEGEN row in `mechanical-verification-of-derived-code-facts.md` carry it structurally. No new skills. The synthesis-needs-A-pass memory is OWED (resume-step-2).
- **Verified:** `check_session_docs.sh` SWEEP CLEAN at write time; `/handoff` Stage-6 supersede done (prior active → superseded).

---

## Operator collaboration norms (every-turn Tier-0)

Address Caramel as Caramel / she / her · no AskUserQuestion modals (inline text) · evaluate on robustness + latency + design, NOT time · **between agent rounds AND after pre-coding checks: present findings + CONSULT, never auto-proceed** (the standing rule this whole session ran under) · commit/push only when asked · auto-write TECH_DEBT/PARITY on deferral.

---

## Closing value statement

E.1.2 is the **successor-foundation leaf** — the one determinism-gated cycle where Position/NodeState open to their final shape. Everything downstream in .E (the aggregator, the exit-redesign D-206, the multi-exchange .E.1) builds on this freeze, so the bar is "open it ONCE, correctly" (avionics-grade, `user_correctness_first_not_ship_fast`). The heavy design + validation is DONE and on disk — the pickup has a clear, de-risked path with fresh headroom. Good luck.
