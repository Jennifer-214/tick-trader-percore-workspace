---
status: active
created: 2026-07-05
ship: E.1.2.A — in-code documentation system (schema v1 PROVEN + validator increment-1 in standing CI; [REFERENCE]-resolution → conversion → detector next)
supersedes: 2026-07-05-E.1.2.A-information-infrastructure-kickoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
---

# Handoff — E.1.2.A: schema locked, validator built, pilot next

## One-paragraph pickup

A long design+build session (2026-07-05) took the in-code documentation schema from "v1.0 locked but unverified" to **v1 PROVEN + a working, self-maintaining validator in standing CI**. The schema was stress-tested by TWO i-class gap-scans (24 agents, 16 real-unit dry-runs): grammar STRUCTURE sound, every gap ADDITIVE (not a redesign); the core surface (function/struct/registry/type) CONVERGED; the variants (enum/wire-version/strategy/file/test) grew their own category-sets (a **bounded** remainder). `check_code_tag_blocks.py` (increment 1) is built, self-tested (11/11), mixed-state robust (in-file `[SCHEMA]` whitelist), category-set DERIVED from the spec SSoT (self-maintaining), freeform-region-safe, and wired HARD into `check_session_docs`. Everything captured (D-308–D-316) + committed (workspace `7db262f`; **engine source UNCHANGED** at `8648e27`). **Next = `[REFERENCE]`-resolution → first real conversion → completeness detector** (operator's compounding order); `[REFERENCE]`-resolution is already designed.

## State (committed)
- **Engine `8648e27`** — UNCHANGED (no engine source touched; all work is in workspace-backed `tools/` + `tests/` + `DESIGN_SPECS/` + `plans/`).
- **Workspace `7db262f`** — schema completion + the validator. Session commits: `c07a6c0` (reconcile) · `ef8e276` · `35c297f` · `9bd951d` · `005b211` · `9e2ddce` · `7db262f`.

## The decisions (SSoT = decision-log D-306..D-316)
- **D-306/307** — schema design + fork-b (pre-session).
- **D-308** — schema v1 completion (fork-b reconcile · `[DOC]`-drop · `[ITERATIONS]`/`[FUTURE_WORK]` · `[CODE]`/`[END_CODE]` · cache `[ALIGN]`/`[CACHE_LINES]`/`[STRADDLE]` · `[BRANCHES]`/`[FLOAT]`/`[BUILD]` · `[SCOPE]` · `[FROZEN]`/`[GOLDEN]` · structural annotations · `[THREAD]`→curated · preserve-voice carve-out · `[LAT_EXEMPT]`-unit-level · **reproducibility = instruction-count, NOT ns**).
- **D-309** — 7 grammar gaps + dispositions (gap-scan 1: `[REGION]`/`[INSTANTIATION]`/registry-topology/`[COMPLEXITY]`/`[APPLY_AFTER]`/`[MUTATES]`/`[WIRE_VERSION]`).
- **D-310** — plugin: g++-unified fact-core + ONE grammar / dual ROLES (no per-source adapters).
- **D-311** — v1 = 8 workstreams.
- **D-312** — follow-up gap-scan (variants + numeric-contract family `[DOMAIN]`/`[FAULT_SIGNAL]`/`[SEAM]`/`[GATED_BY]` + 2 meta-findings: prose-lies-about-codegen; `[BUILD]` needs x86-64-v3).
- **D-313** — plugin verification (PARTIAL; **tagadapter-parser is the keystone**) + PILOT-the-proven-surface.
- **D-314** — validator increment-1 + mixed-state / in-file-whitelist / single-source design.
- **D-315** — `[FILE]` block = file-level 7-axis readout + top-of-file identity/whitelist gate.
- **D-316** — validator-in-CI · golden fixture · dogfood-caught freeform gap (CLOSED `9e2ddce`) · plugin HUD `FPN_Binary<F>` probe failure = the `[INSTANTIATION]` gap plugin-side · nav-graph payoff.

## Critical pickup-time reads
- `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md` — the schema SSoT (grammar · the ` ```category-set ` block the validator reads · templates · variants · CI-enforcement 1–5).
- `plans/v5.15-live-readiness/subplans/2026-07-05-E.1.2.A-comment-system-and-doc-consolidation.md` — umbrella plan (current-state overlay · 8 workstreams · validator status · **next-increment order + `[REFERENCE]`-resolution design**).
- `plans/.../subplans/2026-07-05-E.1.2.A-readout-axes-and-classify-vocab.md` — readout axes + classify vocab + **both gap-scan results**.
- `plans/.../subplans/2026-07-05-E.1.2.A-grammar-gap-dispositions.md` — disposition proposal (adopt/fold/defer + plugin-support).
- decision-log `v5.15.5.F.4d.1.E-architecture-v2.md` → **D-308..316**.
- `tools/check_code_tag_blocks.py` + `check_code_tag_blocks_selftest.sh` — the validator (increment 1).
- `tools/plugins/fox-symdeps.nvim/DOCS/{ROADMAP,TAG-INTEGRATION}.md` — the plugin side (operator-owned).

## NEXT ACTION — operator's compounding order (reference → conversion → detector)
> ⚠ **Prerequisite audit (2026-07-05, independent a-class REFUTE — verdict INSUFFICIENT-to-start-conversion; now fixed in the spec):** Task 1 is startable NOW (minus the AUDIT arm); Task 2 has a BLOCKING STEP 0 that was mislabeled non-blocking. Folded below + into the spec (new ` ```reference-subcats ` block + the vocab-live ⚠ note + the disposition-categories-not-in-fence note).

1. **`[REFERENCE]`-resolution** *(DESIGNED — build fresh; UNBLOCKED)*: a DECOUPLED file-wide resolver — grep `[SUBCAT]_[id]` for the ref subcats + flag dangling, **per the spec's new ` ```reference-subcats ` block** (source + id-form per subcat: INVARIANT→CLAUDE.md `H<n>` · DECISION→UNION of decision-logs `D-<n>` · DESIGN_SPEC/MEMORY→basenames · TECH_DEBT/CLASS/PLAN→ledgers · **AUDIT = existence-unchecked, do NOT red on it**). **Decoupled from the block-parse loop** → sidesteps the `[SUPPORTING_DOCS]`-inside-`[COMMENT]`-inside-prose interaction. Load targets once; check membership; add a self-test (valid + dangling); wire into `validate_file`. Code↔doc link integrity → powers the plugin's open-the-doc jump-nav.
2. **First real conversion** —
   - **STEP 0 (BLOCKING — do FIRST; was MIS-labeled "non-blocking #14"):** vocab-reconcile. The validator is LIVE + checks `[TAG]` vs `doc-tag-vocabulary.md`. The component family (`[ENGINE]`/`[ML]`/`[GUI]`/`[DATA_PLANE]`/`[DEV_PLANE]`) + the classify family (`[CAPITAL_BEARING]`/`[NON_CAPITAL]`/`[DECIMAL]`/`[FIXED_POINT]`/`[DETERMINISTIC]`/`[BIT_PACKED]`/`[DRAINER]`/`[PERSISTED]`/`[FROZEN]`/`[GOLDEN]`/`[CRITICAL]`/`[SUPPORTIVE]`) are NOT in the vocab, and several DON'T cleanly fold (`[DECIMAL]`↔`fixed-point-math`, `[CAPITAL_BEARING]`↔`capital-safety`, `[BIT_PACKED]`↔`bitmap-packed`) → a **design decision per value (new-row vs alias)**, not a mechanical add. Land the rows FIRST — until then every natural target (a `[FILE]` block, the Money type) REDS in CI.
   - Then convert an engine unit end-to-end — **stay on the PROVEN surface (function / struct / registry)** to avoid the un-folded disposition categories (`[REGION]`/`[DOMAIN]`/`[WIRE_VERSION]`/… still throw UNKNOWN). Validate, plant-a-drift → red. The per-file gate is all-or-nothing: opting a file in (first `[SCHEMA]_[`) means reconciling ALL its pre-existing **single-word ALL-CAPS bracketed** section comments (`[INIT]`/`[STATE]`/`[APPLY]`/`[SHUTDOWN]`/… — e.g. `[INIT]`×13 in `RegimeDetector.hpp`, the canonical example file), not just literal `[SECTION]`.
3. **Completeness detector** — CODE_MAP unit inventory vs blocks-present → "these units have no block yet" (the coverage gate).
Also deferred: ladder-order · DERIVED-vs-ground-truth drift · the prose-codegen-lint (spec CI item 5).

## Pre-pilot decisions (need operator)
1. `[WIRE_VERSION]` variant-vs-template (D-309 said DERIVED-under-`[REFERENCE]`; D-312 argues full VARIANT). 2. `[BUILD]` pins a concrete microarch (x86-64-v3), not `-march=native` (else asm-DERIVED false-REDs across build hosts). 3. prose-codegen-lint: build it (CI item 5) or accept-risk-documented. 4. tagadapter PARSER first (plugin — gates all read-back features).

## Remaining #14 propagation (lands WITH the tooling; does NOT block the pilot)
`CLAUDE.md` categorical trigger · `SUBAGENT_ARMING.md` (agents know the grammar before touching code) · ~7 skills (`/doc-create`, `/metadata-audit`, `/handoff`, `/readiness`, `/dod-audit`, `/strategy-template`) · MASTER pointer.

> ⚠ **EXCEPTION — the vocab-reconcile is NOT in this "non-blocking" set** (audit correction): the code-`[TAG]` component + classify families are **STEP 0 of conversion** (NEXT ACTION #2) — they must be reconciled into `doc-tag-vocabulary.md` before the first conversion or CI reds. Only the 395-drift *frontmatter* portion is deferrable; the code-`[TAG]` values block.

## Plugin follow-ups (operator-owned; tie to the schema)
- **tagadapter PARSER** — null today (`M.available=false`); the keystone that gates all read-back. `check_code_tag_blocks.py` is a working reference implementation of the grammar parse.
- **sizeprobe/recordlayout: instantiate templates at the `[INSTANTIATION]` pin** — fixes the `FPN_Binary<F>` HUD size-probe failure + the `s`/`t`/`w`/`c` lenses (they all need the resolved layout of a concrete instantiation, e.g. `FPN_Binary<64>`).
- **nav-graph**: `[FILE]`/`[CONTAINS]` = clickable index → quick-jump; `[CONSUMERS]`→caller · `[REFERENCE]`→doc · `[END_*]`→fold.

## Definition of Done (this ship)
Schema v1 stable (✓ + gaps dispositioned) · validator increment-1 in CI (✓) · `[REFERENCE]`-resolution + completeness detector built · a pilot conversion proven (real unit validated + red-on-drift) · variant category-sets dispositioned · #14 propagation done · plugin adapter (operator). **Working-v1 = {validator + adapter + pilot}** — a subset, live before full conversion.

## Capture-completeness (this session — nothing lost)
- **Decisions → log:** D-308..D-316 all written to decision-log `v5.15.5.F.4d.1.E-architecture-v2.md` with `<!-- D/C/F -->` + `<!-- STATUS -->` sentinels (schema completion · 7+8 grammar-gap dispositions · plugin g++/dual-role · 8-workstream v1 · variant follow-up · plugin PARTIAL · validator increment-1 · `[FILE]` block · validator-in-CI/golden/dogfood/`[INSTANTIATION]`). MASTER banner updated (UPDATE 2).
- **Findings → ledger:** the dogfood-caught freeform-region gap was CLOSED in-session (`9e2ddce`, not deferred). The remaining deferred validator checks + the #14 propagation + the plugin follow-ups are HOMED in the plan's "Next increments" + this handoff (no unhomed debt). TECH_DEBT-166/194 (doc-index drift) noted adjacent, left tracked; no new PARITY. **B-Plus basename false-positive (HOMED):** editing MASTER re-triggered B-Plus on its historical `.E.1.1` banners, which cite bare basenames (`ControllerConfig.hpp:1860`); the files exist (verified 3413/1632/1787 lines, cited lines valid) but B-Plus can't resolve basename→subdir → false MISSING. This close-out landed with `SKIP_PLAN_BODY_CHECK=1` (verified-real, not fabrication); FIX = teach B-Plus basename resolution (or exempt MASTER's historical banners) — a recurring false-positive worth a tool pass.
- **Memories/skills → indexed:** none created this session — the schema spec + the decision-log ARE the durable capture; no new `memory/` file or skill, so MEMORY.md is unchanged (nothing to index).
- **Verified:** `check_session_docs.sh` SWEEP CLEAN · `check_code_tag_blocks.py --selftest` 11/11 · active-handoff singleton = 1.

## TaskList state at handoff write
| # | task | status |
|---|---|---|
| 1 | E.1.2.A — in-code documentation system (umbrella) | in_progress |
| 3 | P1 — grammar CI validator (increment 1 BUILT + in standing CI) | in_progress (increment 1 done) |
| 4 | P1 — scaffold generator + golden fixture (golden ✓; generator pending) | pending |
| 7 | P2 — pilot conversion (the "first real conversion" next-step) | pending |
| 14 | Unified fact-grammar layer (#14 propagation: CLAUDE.md/SUBAGENT_ARMING/skills/vocab) | pending |
| NEXT | `[REFERENCE]`-resolution → first real conversion → completeness detector | pending |

## Verify at pickup
- `bash tools/check_session_docs.sh` → SWEEP CLEAN (validator's 2 checks + all doc-CI green).
- `python3 tools/check_code_tag_blocks.py --selftest` → 11/11.
- decision-log D-308..316 present; workspace committed `7db262f`; engine at `8648e27` (unchanged).
