---
type: handoff
status: active   # THE one live handoff (no-arg /accept-handoff resolves here). Supersedes 2026-07-16 batch20-checkpoint. ≤1-active enforced by check_handoff_active_singleton.py.
ship_tag: E.1.2.A
plan_type: refactor   # in-code documentation infrastructure — maintainability/framework deliverable, not a capital-path change
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: the locked [SCHEMA]_[v1.0] tag system IMPLEMENTED — toolchain done, corpus proven, the codebase converted, the plugin consuming it ("code becomes its own IDE")
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-07-16-E.1.2.A-P6-batch20-checkpoint-normalize-pass-next-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md, DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md, DESIGN_SPECS/doc-disciplines/format-input-space-taxonomy.md]
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md, DOCS/CODE_TAG_TEMPLATES.hpp, plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md]
deletion_scope: none
coding_status: phase-6-134of174   # DataStream 12/15 (P6.107-112) — NEXT = BinanceOrderAPI (A4/A5 sister surface) → TUIAnsi → EngineTUI, then Backtest/GUI/tests/FixedPoint
---

# E.1.2.A handoff — **P6 @ 95/174 (normalize + uplift COMPLETE, one standard; D-356) · NEXT = the 3 GIANTS: ConfidenceScore → ModelInference → NodeModelZoo**

**Created:** 2026-07-16 (close of the normalize/uplift/batch-21 session — 17 commits)
**Branch:** feat/v5.15-live-readiness
**Engine HEAD:** `7d6249e` (17 session commits `c80e67a`..`7d6249e`; tree clean except `M Strategies/RegimeDetector.hpp` — Caramel's untouchable pilot WIP — + untracked `build_clangd/`, `resume`, `tmpo0ujx6rw/`)
**Workspace HEAD:** at or after `8b1fddd` (this close's sync commit lands after this doc)

**WHY this close (operator-directed):** compaction limit — Caramel: *"we hit compaction limit and i think it degrades implementation of this"*. Same discipline as the batch-20 checkpoint. Everything is committed + gate-verified; nothing in flight.

## ⏩ Addendum — 2026-07-16 PM continuation session (SUPERSEDES the counts/next-action below)

- **P6.norm2 `c352225`** — the 10-site batch-21 fence-top/stranded judgment pass: 9 relocations (incl. 1 detector-missed RidgeBlender_BuildCorr sibling) + 1 orphan banner DROPPED (rg-proven: `Bandit_SetArmName` absent at HEAD AND anchor) + 1 FeatureRegistry stale-count one-worder + StampBound:230 judged STAY (det4b FALSE-POSITIVE — prefix-matches the GROUPS sub-macro; expected values re-baselined in § checker battery below). **The close-time "battery at expected values" claim was inaccurate** (compaction-degradation evidence — validates the checkpoint policy).
- **P6.95 `43d3bd4` — ConfidenceScore.hpp CONVERTED (giant 1/3).** 21 blocks; 26 tagged asserts; 2 registry ecosystems; 24 tool-filled DERIVED; 6 rot-fixes enumerated in the commit. Full verify chain green; suite 3697/0 twice.
- **Corpus verification re-run at pickup:** anchor code-identity vs `d5f8abd` = **94/94 ZERO-DELTA** (pre-P6.95; RegimeDetector WIP excluded; the 4 schema_golden fixtures are sprint-born, correctly absent at anchor).
- **P6.96 `b34ba21` — ModelInference.hpp CONVERTED (giant 2/3, the H9 stamp heart, 2026-07-17).** 19 blocks; wire prose verbatim (stamp spec relocated whole into verify_model_stamp's [COMMENT]). **TWO REAL FINDINGS homed:** ⚠ **TECH_DEBT-237 (HIGH)** — the Ship-B "pre-epoch stamps [1,2] HARD-INVALID (unconditional floor below)" comment is a **Class-38 PHANTOM**: the flip (`838bf09`) changed only constants+comments, the floor was NEVER coded (verify_model_stamp RELAXED-accepts [0,3]; zero callers check; rg + flip-diff verified). Mitigated today only by no-live-models + the Phase-D live-gate — **pre-live must-close**. · **TECH_DEBT-238 (LOW)** — the FOREACH_LEGACY_PREFIXED_KEY deletion target cited TECH_DEBT-101, dangling in every ledger file; re-homed, folds into the TD-237 fix ship (the floor deadens the layer, H21). MetaRegistry.hpp:65's TD-101 cite is a string literal (code bytes) — fix rides the TD-237 ship.
- **Census now: 97/174 · ML_Headers 34/35.** Engine HEAD `b34ba21`.
- **TD-237/TD-238 FIX SHIP LANDED `ea67eda` (2026-07-17, operator-greenlit "fix stuff"):** the pre-epoch FLOOR is real (verify_model_stamp check 0c; `STAMP_FORMAT_VERSION_EPOCH_FLOOR=3` pinned by a 2nd EPOCH_TRIPWIRE assert; valid=0 incl. absent=0; dev-mode-bypass-proof) + the FOREACH_LEGACY_PREFIXED_KEY layer DELETED (H21 tombstone; MetaRegistry row retired; 5 cite sites updated) + **BONUS: MetaRegistry.hpp:60 missing line-continuation backslash FIXED** (the 43-row bulk block sat OUTSIDE the FOREACH_REGISTRY macro; latent — header included by NOBODY, H15 guard is text-level; standalone compile-probe now clean). Suite **3697 → 3702/0** (5 new adversarially-shaped floor tests; 5 v5.2.0 fixtures made epoch-current, intent-preserving). Also rg-proven: the cited "Phase F SOFT bump fixture test" NEVER existed. TD-237 + TD-238 moved open→closed.
- **P6.97 — NodeModelZoo.hpp CONVERTED (giant 3/3, the biggest file in the repo, 2026-07-17; commit follows this addendum).** 23 blocks: [FILE] w/ model-bundle [SEAM] · 5 [STRUCT] (NodeModelZoo, PerArmBarriers, PredictionRecord, PerArmDrift, EnsembleModelZoo w/ tiered cluster [SECTION]s) · 2 [REGISTRY] (FOREACH_ENSEMBLE_POST_LOAD 2-col + FOREACH_SINGLE_ZOO_POST_LOAD 2-col — the PARITY-009..012 close story) · 15 [FUNCTION] family blocks · 5 [ASSERT] tags. Rot-fixes: 3 more defunct "CLAUDE.md item 9/25/28" refs · stale line refs (EngineSharded:1173-1201/:2803-2806, self-file :349-350/:1280-1281/L206-226/~1471) · "All 7 setup steps" (registry is 11) · pre-A.2.c `initialized_*` field names → the MASK_EZOO_* bits. det4b caught 9 own fence-top placements pre-commit (fixed; the detector bites every giant — working as designed). **Battery expected values re-baselined: det4 STRANDED = 1 accepted (the H21 FOREACH_LEGACY_PREFIXED_KEY tombstone at ModelInference — a deliberate at-site retirement record, not a banner) + det4b relocate-class = 1 (the StampBound FP, unchanged).**
- **ALL THREE GIANTS DONE. Census 98/174 · ML_Headers 35/35 COMPLETE.**
- **P6.98 `2db364b` — MemHeaders batch OPENED (2026-07-17): 4 small files converted** (DebugLog [+the ~150µs→H8 ≤100µs budget rot-fix], OmsPushExitHelper, PositionFieldRegistry [5-col wire registry], ArchFieldDriftRegistry [4-col; the FOREACH_STAMP_BOUND_CFG→DRIFT_CHECK_FROM_DERIVED mechanism rot-fix]). **Census 104/174 · MemHeaders 6/24** (P6.99 `6c63b1a` added DrainerConstants + PoolAllocator — the original-voice file, [EDIT [date]] markers re-formed as [COMMENT] partitions). Suite 3702/0.
- **P6.100 `83773a7` (2026-07-17): RunHistory + DisplayMetaRegistry + HmacSha256** (+2 hygiene restores: ArchFieldDrift [COMMENT] had COPIED 4 file-header paras — deduped to a pointer, dupes back to baseline 2; PoolAllocator one-line final partition tripped det1's mismatched-bar sandwich — `//` spacer, det1 back to 0). Rot: DisplayMeta dead "CLAUDE.md item 12/13/19/21" ×6 + ".B.3 will ship"→SHIPPED; HmacSha256 `tools/stamp_model.sh` RETIRED at .B.3 Path C → in-process claim.
- **P6.101 `e6a6ad7`: PerNodeStateFlagsRegistry + NodeStateFlagRegistry + OmsExitPredictorMetaRegistry** (the flag/MBS trio; light `[MACRO]_[<FAMILY>_*]` blocks introduced for accessor families per ShardedSnapshotPersist precedent). Rot: PerNodeStateFlags claimed SCALE_/GATE_ prefixes — code emits STATE_FLAG_ (3 wrong-prefix sites); OmsExitPredictorMeta WRITER cite EngineSharded.hpp → EngineCommon.hpp (slow-path-body extraction moved it; OMS_META_PACK verified at EngineCommon:731).
- **P6.102 `53f6dd0`: LatencyHistogram + InitArena. Census 112/174 · MemHeaders 14/24.** Suite 3702/0. **Two learnings:** (1) `check_cache_layout --fix` REWRITES quartet lines but never CREATES them — author the 4 lines at conversion, then canary-probe (plant 999B → --fix restores truth) proves emitter coverage; (2) LatencyHistogram's "PHASE 7.B DEFERRED" was rot — boot dispatch (main.cpp:219) + coarse drainer-cycle bracket + stderr summary LANDED; TD-045 ledger annotated with the same currency note (its "boot dispatch NOT YET WIRED" surface list was stale too). Also fixed: "~640 bytes" → 576 B (emitter-confirmed).
- **P6.103 `7fdb934`: OmsStateFlagRegistry + BitmapMacros + OmsPhasedDrain.** Rot incl. the "promotes toward CLAUDE.md item" prediction that HAPPENED (MBS = H14) + a REAL size finding: OmsDrainBuckets "~7 KB" is actually **147,480 B (~144 KB; Command=256 B × 576)** — prose re-grounded (8 MB stack fine; touch bounded by event count).
- **P6.104 `01956b1`: NodeCtxSummaryFieldRegistry + NodeCtxInitRegistry.** Rot incl. BOTH format-choice lists missing the Money "%.8f" branch (Ship-B); "per_regime added at Phase 6" -> landed as EMPTY placeholder; the 3-tuple doc predating the D-297 4-tuple unification.
- **P6.105 `1c943ee`: BuddyAllocator + FailureModeRegistry + HealthLog. Census 120/174 · MemHeaders 22/24.** BuddyAllocator NOT-WIRED verified (zero includers → D-327 gap-note quartet); GROUP_DRIFT "(7 entries)" -> 8. Suite 3702/0 every batch.
- **P6.106 `179824d`: CfgGateRegistry + OmsFieldRegistry — MemHeaders 24/24 COMPLETE. Census 122/174.** Rot incl. CfgGate's "BOTH SIDECARS EMPTY" (superseded — .B.2 populated 16 entries), the header listing the REJECTED BITMAP gate forms (real gates = COHORT_GATE_* at MlCfgFlagRegistry:119+), "4 enrollments pending" (LANDED, verified in MetaRegistry), OmsField's "promotion threshold" prediction (HAPPENED — H14). **Discipline note: one in-macro `/* */` comment edit was self-caught by the lossless gate and reverted byte-exact — in-macro comments (backslash-continued rows) stay CODE BYTES.** Checker fix: gap_census2.py now skips static_asserts on continuation lines (macro-body guards fire at expansion, not file scope). Suite 3702/0.
- **DataStream OPENED (operator: "keep going with DataStream"). P6.107 `f5f9c39`: MockGenerator + WebSocketUtil + MetricsLog + CalibLogColRegistry.** REAL FINDING: **MetricsLog.hpp is ORPHANED at HEAD** (zero includers/callers incl. tests — rg skips gitignored tests/, swept explicitly; MockGenerator by contrast IS live via tests/test_common). [FILE] block states it; TD-031 annotated (refactor MOOT vs dead file — DELETE-vs-revive is the operator call). TD-030 annotated too (its deferred cfg=2 telemetry LANDED at Step 8 § M via Order::flags_packed bits 17-25; remaining delta = the split exp3-vs-thompson pick-time arms — closeable at operator triage). CalibLog "EXACT 9-column shape" → the legacy 9-col PREFIX (47 cols now).
- **P6.108 `379a29d`: TradeLog + TickRecorder + DepthRecorder** (recorder trio; determinism net GREEN on the emit surfaces). **P6.109 `f1379ca`: DepthReplayState (record→replay [SEAM] line) + BinanceDepth. Census 131/174 · DataStream 9/15.** Suite 3702/0 every batch. New grammar learnings: [SEAM] is its own top-level line (NOT [REFERENCE]_[SEAM]); single-token bracket member headings ([ADVANCE]) read as unknown categories → use [SECTION]_[name].
- **P6.110 `12630bd`: FauxFIX** (single-file batch — UNDER-GATED: battery skipped; 2 genuine det4b sites caught + fixed at P6.111). **P6.111 `80a4e2e`: BinanceUserData (133/174 · DataStream 11/15).** The .E.0.10 **A2/A4/A5 capital findings now live AT THE PARSE SITE** — ud_parse_execution_report's [COMMENT] carries a labeled findings partition with TECH_DEBT-169/171 refs (both verified OPEN) + the D-123 pointer; the [FILE] OVERVIEW flags CAPITAL_BEARING + the findings. **Battery re-baseline: det4b relocate = 2 accepted** (StampBound:230 + FauxFIX:102 — both the documented prefix-match FP mechanism: block-head name prefixes the next member's identifier).
- **P6.112 `865c8af`: BinanceCrypto (134/174 · DataStream 12/15).** Scripted transform (assert-guarded, diff eyeballed); the v5.11.16 audit note's self-file line cite de-specified; CLOSE/RECONNECT/HAS-PENDING single-token banners → [SECTION]s (+ leftover closer bars cleaned after a det1 sandwich). Suite 3702/0.
- **NEXT = the last 3 DataStream (BinanceOrderAPI 959 [the A4 fee-fabrication + A5 sister surface — same at-site findings treatment as BinanceUserData: findings partition in the [COMMENT] + TD-169/171 refs] → TUIAnsi 1259 → EngineTUI 2204) → Backtest (11) → GUI (12) → tests (12) → FixedPoint (1).** Run the FULL battery on every batch incl. single-file ones (the P6.110 lesson); [OVERVIEW] text must not mention bracketed category tokens (validator collision — say "the comment block"). Recipe: per-file read-whole→rot-check→convert; lossless+validator per file; batch suite; batch commits. Registry files get [COLUMN] legends; det4b catches fence-top notes (relocate to [COMMENT]); accessor-macro families get light [MACRO] blocks; [DERIVED] quartets are AUTHORED then --fix-trued (canary-probe 999B proves emitter coverage; zero-includer headers get the D-327 gap-note instead); [COLUMN]_[TYPE] collides with the category vocabulary — spell it [type]; [REFERENCE]_[CLASS] takes a SINGLE id (list form doesn't validate); a light-block's trailing prose above a following opener needs a thin closing bar (det4 walks up past blanks).

---

## TaskList state at handoff write (recreate at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | in_progress | E.1.2.A umbrella — in-code documentation system (P1-P4 done; P5 operator; P6 in progress) |
| #2 | completed | Complete schema against the taxonomy (phase 1 — LOCKED v1.0) |
| #3 | in_progress | Phase 4/5 residual — PLUGIN-side toolchain items (operator's session; seam ready). Caramel may still need the nvim RESTART for `template_args` |
| #4 | completed | Phase 2 — propagate [SCHEMA]_[v1.0] into templates + 3-surface alignment |
| #5 | in_progress | Phase 6 — roll out the tag system (95/174; the 3 giants next) |
| #6 | pending | fox-symdeps polish backlog — operator-noted: `<leader>dd` Layout HUD should show HOVERED-MEMBER breakdown (offsetof, member size, cache line(s), padding); data already plugin-side (recordlayout.parse / foxtag layout) — render addition. Collect further minor items here. |

---

## Sprint census at close (engine `7d6249e`)

| Dir | Converted / total | Remaining |
|---|---|---|
| CoreFrameworks (incl. EngineSharded/) | **53/53 COMPLETE** | — |
| Strategies | **10/10 COMPLETE** | (untracked `Strategies/private/EmaCross.hpp` excluded) |
| ML_Headers | **32/35** | ConfidenceScore (1161) · ModelInference (2007) · NodeModelZoo (3131) |
| MemHeaders | 0/24 | all |
| DataStream | 0/15 | all |
| Backtest | 0/11 | all |
| GUI | 0/12 | all |
| tests | 0/12 | all (dir-symlink: census needs `find -L`) |
| FixedPoint | 0/1 | FixedPoint.hpp |
| **TOTAL** | **95/174** | 79 tracked |

**Corpus state: ONE standard.** The normalize pass (5 classes) + the vintage-gap uplift ran to ZERO on every mechanical check. Anchor `d5f8abd` code-identity re-proven 0 deltas across all converted files. Suite **3697/0** at every one of the 17 commits.

## What landed this session (all commits, newest first)

- `7d6249e` P6.94 RidgeBlender (95/174) — H10/H11 canonicals annotated; stale "~5KB" killed (DERIVED says 2048B)
- `3f02228` P6.93 StampBoundModelConstRegistry — the H9 stamp-wire registry; 9-col [COLUMN] legend; stale "empty registry"/"bit-packing later" plan-tense killed
- `5c45afa` P6.92 FeatureRegistry — [SEAM] + 7-col legend; stale "all 34"/"removal in v5.9" killed (rg-verified)
- `76b88f2` P6.91 BanditLearning — [DIAGRAM]_[formula] first canonicals; stale "reader uses strtod" killed
- `696afdf` P6.90 FeatureStandardizer — [WIRE_FIELD] sidecar map + [WIRE_VERSION]; 7 stale claims killed (v5.9.3a-era NO-CALLERS etc.)
- `5a6070e` **P6.uplift — 34 files to the full standard** (18 [DERIVED] · 29 [ASSERT] incl. NEW family REGISTRY_COVERAGE · 64 [SECTION] · 2 [COLUMN] · 4 FOREACH wraps, 2 D-340 nested)
- `7af459b` P6.89 FlowFeatures — 8 blocks; pre-Ship-A layout numbers killed (asserts = SSoT)
- `70e2b99` P6.88 BanditAlgorithmRegistry — ecosystem-block precedent (typedef+fns+macro+masks in one [REGISTRY])
- `10a786c` P6.norm dedupe — EventLoopAggregates batch-era COPIED banner (the recheck catch; the ONLY duplication corpus-wide)
- `3fe14f0` P6.87 RollingStats — batch 21 opened; [INSTANTIATION]/[COMPLEXITY] first real uses
- `d999952` P6.norm GCN tail pile collapsed (operator-approved)
- `fc5f235` P6.norm class-4b — **181 banner relocations, 49 files** (position-aware classifier + splice; 14 STAY sites individually judged; 135 mid-body STAY per D-326)
- `f1e0c20` P6.norm class-4a — 12 stranded banners + EngineCommon's 2 headers → [SECTION]
- `c80e67a` P6.norm class-1 — 7 verified bar pairs + 5 same-shape siblings

**Decision log:** D-356 (this session, with sentinels). Schema doc updated workspace-side (REGISTRY_COVERAGE family 1-line-add).

---

## ⚠ NEXT SESSION — the 3 GIANTS, one at a time, full recipe each

Order: **ConfidenceScore (1161) → ModelInference (2007) → NodeModelZoo (3131)**. One file = read WHOLE → convert → full verify chain → commit → next. ModelInference is the stamp emit/parse heart (H9 HMAC wire); NodeModelZoo is the biggest file in the repo. Expect real judgment work; do NOT rush multiple giants into one degraded context — one giant per sitting is a fine pace. After the giants: MemHeaders → DataStream → Backtest → GUI → tests → FixedPoint (small files; batch several per session).

### THE RECIPE — thorough-first-time standard (v2; supersedes the batch-20 recipe — apply the FULL treatment AT conversion, no cleanup pass later)

Per file: **read WHOLE file → rot-check comments (rg-VERIFY every stale-looking claim before dropping) → convert → verify chain → commit that file ONLY.**

1. `[FILE]` block (TAG from vocab / [SCHEMA]_[v1.0] / [OVERVIEW] / [CONTAINS] listing every unit; [SEAM] line where train↔serve; [DIAGRAM]_[formula] when the file has a headline formula). File banner stays VERBATIM below the block (BuildFlags shape) minus enumerated Refinement-A drops.
2. Per-unit blocks: [STRUCT] / [FUNCTION] / [REGISTRY]. **Family/ecosystem grouping precedents:** small helper fns ride the flagship fn's section (ROR/FlowFeatures shape, noted in [OVERVIEW] + [CONTAINS]); a registry's typedef+compute-fns+generated-consumers share ONE [REGISTRY] block with [SECTION]s (BanditAlgorithmRegistry/FeatureRegistry/StampBoundModelConstRegistry shape); nested blocks inside a parent fence are legal (D-340; column-0 frames fine).
3. **Unit-WHY banners → below-code [COMMENT] VERBATIM at conversion time** (labeled partitions for multiple topics; internal blank lines → `//`). Rule: fence-top run whose next code is the unit's OWN signature = unit-WHY (relocate); a run describing a DIFFERENT sub-part or mid-body = code-local STAY (D-326). Fwd-decl notes stay with their decls.
4. Bracket headings `[X]` → `[SECTION]_[X verbatim]` (thin bars); `── N. x ──` phase markers in big fns → bar-wrapped [SECTION]s (RollingStats_Push shape). Never leave a raw bracket heading (validator reds it).
5. [COLUMN] legend on every registry (one line per tuple column; count MUST match arity — validator asserts). Sparse [ROW] only where rationale exceeds the doc column (wire-slot reassignment stories).
6. [ASSERT]_[FAMILY]_[expr] on every FILE-SCOPE static_assert (in-fence asserts = code, no tag). Families: LAYOUT_LOCK / BITMAP_OVERFLOW / EPOCH_TRIPWIRE / OVERLAP_EXCLUSION / PADDING_FREE / **REGISTRY_COVERAGE** (minted this session — registry arity/coverage/enrollment locks).
7. [DERIVED] on every [STRUCT]: seed the 4 lines (`[SIZE]_[0B]` etc.) then `check_cache_layout.py --fix --paths <file>` fills from truth; can't-probe template shapes → the D-327 one-line gap-note (exact text: grep RewardTracker.hpp). Registries: NO [DERIVED] (no tool fills ROW_COUNT yet — corpus convention).
8. [INSTANTIATION] on multi-instantiation templates; [COMPLEXITY] where amortized/worst-case differs; [REFERENCE]s must RESOLVE (the validator checks — a dangling [CLASS]/[DESIGN_SPEC] reds; plan-era Class IDs like "Class 24" are often NOT in RECURRING_BUG_PATTERNS — check first). [FUTURE_WORK]_[TECH_DEBT]_[TECH_DEBT-NNN] for tracked redesigns.
9. **Refinement A with VERIFICATION:** every stale-looking claim gets rg-checked before dropping (callers exist? counts right? schedule happened?). Drops/rewrites ENUMERATED in the commit message. Stale layout/count/mechanism claims are the big classes; "presented-present-tense old format" sketches die, pointers to the SSoT replace them.
10. **Verify chain (every file):** (a) lossless comment-stripped diff vs HEAD = 0 → (b) `check_code_tag_blocks.py --paths` → (c) cache-layout `--fix` then gate → (d) the checker battery (see below) → (e) `./build.sh test` 3697/0 → (f) full-tree validator = ONLY the 2 accept-known RegimeDetector reds. Commit `docs(E.1.2.A-P6.NN)` with the rot enumeration.

### The standing checker battery (PRESERVED — run from the workspace copy)

`plans/v5.15-live-readiness/tools-staging/e12a-conversion-checkers/` (copied from session scratchpad at this close; **cd to the ENGINE root to run** — they use relative dirs):
- `gap_census2.py` — the uplift census (fence-aware, nested-fence depth-counted): file-scope untagged asserts / bare headings / structs w/o DERIVED / registries w/o COLUMN / unfenced orphan FOREACHs. Expected: `0 · 4 · 0 · 0 · 0` (the 4 = accepted prose skip-list: Portfolio:42/:314-ish, GCN, ROR one-liners).
- `recheck_ladder.py` — block structure + section ladder order (the P4 validator gap, working prototype). Expected 0.
- `recheck_dupes.py` — duplicated ≥3-line comment runs. Expected 2 (both anchor-pre-existing legit: ShardedBacktestDriver drain-note ×2, ShardedTradeLog chokepoint ×2).
- `det1_mismatch.py` / `det4_banners.py` / `det4b_v2.py` — the normalize detectors (census + bar sandwiches + stranded/fence-top banners). Expected 0 / 0 / **relocate-class 1** — the ONE accepted det4b FALSE-POSITIVE at StampBoundModelConstRegistry `[SECTION]_[GROUP DECLARATIONS]` (detector prefix-matches `FOREACH_STAMP_BOUND_MODEL_CONST_GROUPS`; the run is section-local STAY prose per D-326). **Re-baselined at P6.norm2 `c352225` 2026-07-16:** the original "relocate-class 0" was written while batch-21 files still carried 9 unapplied fence-top relocations + 1 stranded orphan banner (end-of-session compaction degradation — the close-time claim was inaccurate); the 10-site judgment pass dispositioned all (9 relocated incl. 1 detector-missed BuildCorr sibling · 1 orphan DROPPED rg-proven `Bandit_SetArmName` absent at HEAD+anchor · 1 stale-count one-worder in FeatureRegistry · StampBound STAY). Anchor code-identity re-proven 94/94 ZERO-DELTA post-pass; suite 3697/0.
- `lossless.py <files...>` — the per-file lossless gate.
- `n3_transform.py` — the banner-relocation transform (reference implementation; giants likely need per-site judgment instead).
These are P4 validator-increment candidates (fold into `check_code_tag_blocks.py` when P4 lands; enroll in DOCS/TOOLS.md then).

### Hard-learned norms (carry forward verbatim + this session's additions)

- read-exact-bytes-before-replace (fail-loud rep(); dumps that truncate at N chars have burned us TWICE — copy from Read output, not from clipped python prints); NEVER pipe commit output through `head` (SIGPIPE kills hooks; `tail` is safe); `rg -n` not `rg -rn`; tooltip/string-literal content is CODE BYTES — never edit.
- **Scripted transforms are fine but are where artifacts breed (operator directive): every scripted pass gets diff-EYEBALL on real output + the gate battery before commit.** Checker false-positives happen (boolean-vs-depth fence tracking) — when two checkers disagree, the real validator (`check_code_tag_blocks.py`) is the SSoT arbiter.
- clangd diagnostics on this codebase are stale-preamble NOISE (redefinition/no-template storms after header edits) — the lossless gate + suite are the truth; never chase clangd.
- NEVER touch or git-add `Strategies/RegimeDetector.hpp` (her uncommitted pilot WIP; its 2 validator reds are operator-accepted). `Strategies/private/EmaCross.hpp` untracked + carries a needed KEEP-AS-GLOBAL comment — don't delete.
- Commit locally; no engine remote — nothing to push. Stage files EXPLICITLY (never `-A`; RegimeDetector is modified in-tree).

## Accept-known open items (homed, not lost)

- **RegimeDetector `[STATE]`/`[INIT]` reds** — Caramel's pilot WIP; reconcile WITH HER on pilot-resume.
- **The 4 census "bare headings"** — skip-listed PROSE (not headings); permanent accepted residual.
- **TECH_DEBT-119 looks already-done-but-open** — needs HER confirmation; do not close by fiat.
- **Ledger line-ref rot class** (TD-097 et al) — workspace-side sweep, future session.
- **Plugin TAG-INTEGRATION.md** stale grammar (P5, operator-owned) · **v1.1 vocab-alias candidates** [WIRE_FORMAT]/[REGISTRY] (D-348) · **validator cutover to foxtag core LAST** (D-349).
- **P4 validator increments** — ladder-order + DERIVED-drift + the checker-battery promotion (this session's prototypes ready).

## Capture-completeness (this session)

- **Decisions → log:** D-356 (sentinels ✓). Schema REGISTRY_COVERAGE line-add landed (workspace, in-file provenance note).
- **Findings → homed:** EventLoopAggregates dup FIXED; vintage gap CLOSED (census to zero); the 14-STAY + 135-mid-body judgment sets recorded in fc5f235; checker battery preserved to tools-staging; census/ladder/dupes expected-values documented above.
- **Memory:** feedback_script_transforms_need_output_eyeball.md (NEW — her explicit directive) + MEMORY.md line.
- **Verified at close:** suite 3697/0 at HEAD; full-tree validator = the 2 accept-knowns; checker battery at expected values; `check_session_docs.sh` state = green except the same accepted reds.

---

## Paste this prompt into a fresh Claude Code session to continue

```
I'm continuing E.1.2.A phase 6 — the [SCHEMA]_[v1.0] conversion sprint, picking up at
95/174 (normalize + uplift complete; corpus at ONE standard; D-356).

## Step 0 — orient
- Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-16-E.1.2.A-P6-95of174-giants-next-handoff.md
- Read the handoff's RECIPE v2 + checker battery section + DOCS/CODE_TAG_TEMPLATES.hpp.
- Gold references: CoreFrameworks/ExecutionCore.hpp (original) + ML_Headers/RidgeBlender.hpp
  + ML_Headers/BanditAlgorithmRegistry.hpp + ML_Headers/FeatureStandardizer.hpp (this
  session's shapes: fn blocks / registry ecosystem / WIRE_FIELD map).

## Step 1 — the giants, ONE per sitting, full recipe v2 each
ConfidenceScore (1161) → ModelInference (2007; the H9 stamp emit/parse heart — extra care,
wire-format prose is load-bearing) → NodeModelZoo (3131; biggest file in the repo).
Every stale-looking claim rg-VERIFIED before dropping; drops enumerated in the commit.
Verify chain per file: lossless → validator --paths → cache-layout --fix + gate → the
checker battery (tools-staging/e12a-conversion-checkers/, run from engine root; expected
values in the handoff) → ./build.sh test 3697/0 → full-tree validator (2 accept-knowns).

## Step 2 — after the giants
MemHeaders (24) → DataStream (15) → Backtest (11) → GUI (12) → tests (12) → FixedPoint (1).
Small files; several per session; same recipe.

## Norms
- Address Caramel as Caramel / she / her. No AskUserQuestion boxes.
- NEVER touch or git-add Strategies/RegimeDetector.hpp (her uncommitted pilot WIP).
- Scripted transforms OK but eyeball their real diffs + run the gates before commit.
- Tooltip/string-literal content is CODE BYTES — never edit.
- Commit locally, stage explicitly (never -A); no remote exists.
- A conversion failing ANY verification step gets fixed or reverted — never committed red.
```

## Quick links
- Worked examples: `ML_Headers/RidgeBlender.hpp` (latest, richest) · `CoreFrameworks/ExecutionCore.hpp` (the original gold standard)
- Copy-source: `DOCS/CODE_TAG_TEMPLATES.hpp` · Grammar SSoT: `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md`
- Toolchain: `tools/check_code_tag_blocks.py` · `tools/check_cache_layout.py` · `plans/v5.15-live-readiness/tools-staging/e12a-conversion-checkers/`
- Decision log (D-306..D-356): `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`
- Integrity-proof method: comment-stripped diff vs `d5f8abd` per D-355/D-356 (re-proven 0/88 this session)
