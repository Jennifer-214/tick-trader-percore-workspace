---
type: handoff
status: active   # THE one live handoff (supersede-on-write; ≤1-active enforced by check_handoff_active_singleton.py)
ship_tag: v5.15.5.F.4d.1.E.1.2.D
plan_type: refactor
supersedes: 2026-08-20-E.1.2.C-tail-3G-shipped-leg4-ready-handoff.md
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-08-20-E.1.2.C-tail-3G-shipped-leg4-ready-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
decision_ref: D-430
audit_tier: HIGH
deletion_scope: none
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: close the training + model-artifact surface — the one surface in this codebase with no class, no spec and no guard
coding_status: mid-ship — E.1.2.C's remaining agent-side work SHIPPED; E.1.2.D opened with six operator decisions owed
---

# E.1.2.D — the training + artifact surface, and the fourteen-month inversion a refuted proposal found

## State at write — ANCHORS, not counts

| Repo | HEAD anchor | Window this session | Remote |
|---|---|---|---|
| Engine `feat/v5.15-live-readiness` | `273cd4c` | `2092b95..273cd4c` | pushed |
| Workspace `main` | at-or-after `8cd4209` + this close's commits | `8555ad7..HEAD` | pushed at this close |

Engine untracked operator files unchanged (`scripts/*.sh` edits + 4 scratch files — keep, per
`feedback_keep_operator_scratch_files_as_history`). Ledger re-blessed at the operator TTY this
session (D-394 ritual, once). **No `/ship` tag cut** — Version.hpp deliberately still `.E.1.1`.

## Re-derive at pickup (every number from these, none from prose)

```bash
./tools/check_session_docs.sh                      # expect SWEEP CLEAN
./build.sh test && ./build/controller_test         # expect BUILD 0 + suite green (REDERIVE the count)
./build.sh gui                                     # the suite TU — BacktestPanels' only real includer
./tools/check_determinism.sh                       # expect GREEN
python3 tools/check_identifier_retirement.py       # expect GREEN (92 at write — the tombstone dropped one)
python3 tools/check_cache_layout.py --tu foxml_suite.cpp --strict-new   # expect rc=0 / 0 NEW
./tools/calls_graph_diff.sh                        # expect CLEAN
git log 2092b95..HEAD --oneline
git -C ../tick-trader-percore-workspace log 8555ad7..HEAD --oneline
```

## The session in one paragraph

Picked up the E.1.2.C tail handoff via `/accept-handoff`; every predecessor claim re-derived green.
The one open agent-side item was the register's `#16`/A-12 flag, which verified as *mechanism sound,
assertion missing*. The operator then asked for a `/decision-check` on the adjacent F3 tier-flip
proposal — **that gate REFUTED its own subject and is what found everything else.** Its refutation
(the premise "REFUSE gates training" is false; it gates COLLECT only) led into the buy-side class
aliasing, where a fourteen-month capital inversion was sitting: the buy path read `P(peak)` — the
class that means *do not buy* — and the bandit had been paying −50 bps for correct calls. Three
i-class scans at the operator's direction then mapped the surrounding surface (every finding carries an
S1/S2/S3-Fn id in the digest — cite the id, never a tally), and
the day closed with a validation harness that had been measuring a model nobody shipped. Decisions
at **D-430**; the findings are homed in the NEW **E.1.2.D** plan.

## What landed (commit per leaf; messages are the detail SSoT)

Engine (`2092b95..273cd4c`): `97f12c9` the barrier-primary ENTRY inversion — two named class rules
whose asymmetry is the point · `c103a92` the trained-on label was never the declared label (and that
silently voided the Label-Kind CSV) · `c78206b` `expected_num_classes` TOMBSTONED (H21; dead
duplicate of `model_num_outputs`) · `057e891` the F3 gate now gates TRAINING and sees the per-horizon
CSV · `cd9c2c7` Save Run no longer clobbers the buy model + the `expected.cfg` label-direction check
· `1cfa658` a side flip clears the Label-Kind CSV · `f99e102` the validation harness now measures the
model you SHIP · `6b1a9dd` three live-read closures on the paths the operator is about to use · `273cd4c` the
Stage-6.5.4 review's three — `Model_PrimaryBuyClassIdx(2)` returned an out-of-range class (C.3i was
vacuously green at exactly that width), the standalone Walk-Forward and Run-Full-Validation buttons
trained at `Defaults()` and overwrote the model's signed stamp with 6/0.1/200, and the comments on the
live extraction line still taught the inverted rule. Closes leaf 10 (`WalkForwardWorkerArgs` finally
has a snap block) and collapses the mapping to `Training_SnapshotHyperparams`.

Workspace (`8555ad7..HEAD`): `4bc1509` **RETIRED_NAMES learns NAMESPACES** (the guard extension the
tombstone forced) · `5ab2f44`/`1ca071e` the C.3i + C.3j pins · `0036081` the three-scan report +
digest · `a28fbd1`/`8cd4209` the E.1.2.D plan · this close's D-430 + meta-index rows + handoff.

**Operator can NOW:** train and validate against the model she actually ships — the buy signal reads
`P(valley)`, the bandit learns forwards, the per-horizon label kind reaches the labels, the stamp
records her real hyperparameters, and bandit state can physically be written.

## 🔴 NEXT ACTIONS (ordered)

1. **OPERATOR, and nothing from this session is live until step 1:** `./build.sh gui` then
   **relaunch the suite** — the running process predates every fix above.
2. **Collect once, then re-run the multi-horizon TRAIN** for honest metrics.
   ⚠️ **CORRECTED 2026-08-22 by the Stage-6.5.4 review, and then FIXED in code.** As first written
   this step said "Run Full Validation", which was WRONG and DESTRUCTIVE: `fullvalidation_worker_fn`
   passed no `hp_override`, so that button still measured `6/0.1/200` *and* overwrote the target
   model's signed stamp with those values — undoing on disk the thing this session fixed. The
   `= nullptr` default that makes the parameter safe for un-updated callers is exactly what made the
   omission invisible. **Both RFV entry points are now wired** (plus the standalone Walk-Forward
   button, a third entry point found by the same enumeration), so either path is safe — but the
   multi-horizon Train is the one already proven end-to-end. RFV still never saves a MODEL; it does
   write a STAMP.
3. **The dogfood oracle this session could not automate** —
   `grep -E "^xgb_(max_depth|learning_rate|n_estimators)" models/classification/twins_horizon_7500/barrier.json.stamp`
   must read the operator's panel values. **ALREADY SATISFIED, empirically, by the operator's own
   post-HEAD run:** `models/classification/run_1_horizon_{7500,15000,30000}/barrier.json.stamp` each
   read `xgb_max_depth=3 xgb_learning_rate=0.05 xgb_n_estimators=350`. The hyperparameter threading
   is therefore verified end-to-end on the multi-horizon path — this is no longer an open oracle.
   Note `twins_horizon_7500` is NOT the newest artifact; re-derive the target from disk.
4. **D-a FIRST if you intend to change the layout** — its own trigger is BEFORE leg 4, and training
   more horizons first produces more flat-layout artifacts to migrate. The `mkdir` stopgap means
   leg 4 *can* run either way; this is an ordering preference, not a block.
5. **Leg 4 still needs K≥2** — exit is trained on `twins_horizon_7500` only, and that artifact is
   516 bytes and unstamped (a failed run). Train exit on a 2nd horizon, set
   `use_exit_model=1 exit_bandit_enabled=1`.
6. **The six E.1.2.D decisions** (§ OPERATOR DECISIONS OWED in the plan). **D-a (model layout
   nested-vs-flat) is the one with a trigger: BEFORE leg 4**, because the flat layout has no NODE
   for the bundle — which is why four bandit-state files had nowhere to live. Tonight's
   `mkdir models/classification/twins` is the STOPGAP — it makes the four state
   files writable and is verified inert to all three walkers, but it does not close the layout question.
7. **Then** the E.1.2.D sequenced leaves — leaf 5 (batched multi-target label pass) first: it is the
   only one with a **TOTAL** oracle and is worth 2.65× measured.

## Arming for the next action (M8)

- **Read first:** D-430 (this session's decision record) · the E.1.2.D plan § OPERATOR DECISIONS
  OWED + § SELF-CORRECTIONS · `reports/2026-08-21-training-artifact-surface-scan/README.md` (the
  digest — each finding carries an id, a severity and a disposition).
- **Skills:** `/decision-check` for D-a when it opens (route its agents to `/ml-audit` +
  `/dod-audit`; the material is artifact-layout + discovery) · `/ml-audit` if leg-4 findings reshape
  anything · `/post-ship-audit` + `/ship` at close.
- **Tools to RUN:** the re-derive block above · `check_cache_layout.py --tu foxml_suite.cpp --fix`
  after ANY GUI-state struct change (bit twice more this session) · `gen_code_map.sh` at pickup.
- **Anti-patterns in play:** Class 13 (snap-capture drift — the NEW sub-shape *"snap block complete,
  consumer bypasses it"* is this session's) · Class 51 (vacuously-green guard — `#22` is a live
  instance) · Class 12 · Class 18 · Class 44 · **AR-20** + **WH-8** (new, harvested this close).
- **Invariants:** H9/H12 (`ControllerConfig` is RAW-fingerprinted — do NOT grow it for convenience;
  a `static_assert` catches you) · H21 · H1 (**scoped to hot/slow/drainer/parser** — the suite-side
  label pass is already heap-based by design; do not over-constrain leaf 5 on this) · H4 · H22.
- **WILL BITE:** engine `DOCS/` are per-FILE symlinks so `DOCS/tech-debt/*` has no engine-side path ·
  `tests/`+`tools/`+`plans/` ARE dir symlinks · **a compound command starting with `cd` re-homes
  every later step** (WH-8: 3 occurrences this session — one report written into
  `models/classification/plans/`, two pushes aimed at the wrong repo) · `grep -r` does not follow
  file symlinks · pre-commit **Check F is FILE-SET TRIGGERED**, so a parse-surface violation can
  land and sit until an unrelated commit arms it (SC-1).

## Capture-completeness (the EIGHT judgment checks, explicit)

- **Check 2 (plan frontmatter):** VERIFIED — the NEW E.1.2.D plan carries `type` / `ship_tag` /
  `audit_tier: HIGH` / `decision_log` / `owning_findings` / `reports_dir` / `sister_specs`.
- **Check 3 (decision-log artifact):** VERIFIED — **D-430 written this close**, covering all eight
  decision threads. It was owed and unwritten until the close ritual; recorded here because
  document-as-you-go would have caught it earlier.
- **Check 5 (handoff currency):** THIS document supersedes
  `2026-08-20-E.1.2.C-tail-3G-shipped-leg4-ready-handoff.md` (flipped `superseded` +
  `superseded_by:` at this close). Its PENDING list is discharged except leg 4 + D6/D7 + `/ship`,
  all re-listed above as deliberate carries.
- **Check 6 (M7 escalation candidates):** **ONE, and it is real** — pre-commit **Check F is
  file-set triggered**, so a known-pending-manifest violation can commit clean and sit. Candidate:
  fire Check F on any staged file that appears in `locale_determinism_known_pending.txt`. Recorded
  as SC-1 in the E.1.2.D plan.
- **Check 7 (DESIGN_SPECS promotion):** NONE — no spec's stage moved. The artifact-surface
  codification (E.1.2.D **D-f**) is a NEW-spec candidate awaiting the operator's call; codifying it
  now would be from a census the operator has not yet triaged.
- **Check 9 (memory→spec sisters):** NONE — zero new memory files, zero new skills this session.
  Captures went to D-430, the E.1.2.D plan, the scan digest, and the meta-anti-pattern index.
- **Check 10 (going-forward rules):** NONE new. The two harvested shapes landed as catalog rows
  (**AR-20**, **WH-8**), not as always-loaded rules — correct per the doc-layer separation.
- **Check 12 (amendment cascade):** VERIFIED — the E.1.2.C register cascaded to `#16`/`#19`/`#20`/
  `#21(a)`/`#21(b)`/`#22` dispositions; D-428/D-429 remain accurate (this session opened D-430
  rather than amending them); the meta-index gained AR-20 + WH-8; CODE_MAP + DESIGN_SPECS README +
  TAG_INDEX + CODE_TAG_INDEX all regenerated and the workspace CODE_MAP mirror re-synced.

**Volatile-count discipline:** every quantity above is a SHA, an ID, or a state. Suite and guard
counts appear only inside the re-derive fence.

## Independent-review record (Stage 5.5 + 6.5.4)

- **Deliverable-completeness review (Stage 5.5): NOT COMPLETED — the operator stopped the agent
  mid-run.** It is recorded as not-run rather than assumed-clean. Its checklist is preserved in this
  session's transcript; the highest-value items it was armed to check and that therefore remain
  **UNVERIFIED BY INDEPENDENT EYES** are: (a) whether `hp_override == nullptr` truly reproduces the
  prior behaviour at all three trainer sites (one had a hand-fixed unbalanced brace), (b) whether the
  `#22` guard's honest-error framing survived into the register, and (c) generated-index currency.
  **A fresh session should re-fire it before `/ship`.**
- **Adversarial handoff review (6.5.4): RUN, and its verdict was REFUTED-WITH-FINDINGS.** The
  close-out enforcer refused to accept "recorded as not run" as a disposition — *"stating the gap is
  right and does not close it"* — so it was fired. It earned that insistence completely:
  - **CRITICAL** — NEXT ACTION 2 was **wrong and destructive**. `fullvalidation_worker_fn` passed no
    `hp_override`, so the Run-Full-Validation button still measured `6/0.1/200` **and overwrote the
    target model's signed stamp with them** — and this document then told the reader that reading
    `6/0.1/200` meant the fix had failed. Following it literally would have damaged a correct
    artifact and sent the next session hunting a regression in working code. **FIXED in code**
    (both RFV entry points + the standalone Walk-Forward button, a third one the same enumeration
    found); the instruction is corrected above.
  - **HIGH** — `Model_PrimaryBuyClassIdx(2)` returned an out-of-range index 2, silently clamped to
    class 0 by both consumers: the same failure SHAPE as the bug it fixes, introduced by the fix,
    and green because C.3i's `k >= 2` invariant passed vacuously at the one untested value. Fixed
    (`>= 3` → 2, `== 2` → 1) with four range cells added.
  - **HIGH** — three comments in `ModelInference.hpp` still taught the inverted rule on the live
    extraction line. Fixed. This is precisely the survival mechanism D-430 §(1) names.
  - **HIGH** — the three i-class scan reports were never persisted (only the digest was), so five
    `owning_findings` ids do not resolve and one of them (`S2-F9`) is cited as evidence inside
    decision D-a. **NOT fixed — the source reports are gone.** Cite the digest, not the ids.
  - **MED** — `tree_method` remains a split-brain (booster/WF/held-out take the panel value, the
    stamp still takes `config_used`), so "four configurations collapse to one" is really
    four-collapse-to-two. Homed, not fixed.
  - Also corrected from its findings: the `WH-5`→`WH-8` id in three places here plus D-430 and the
    catalog row's own body; the Tests-section claim of a stamp pin that does not exist; leaf 4's
    `<pending>` SHA; the NEXT-ACTIONS ordering that put leg-4 training ahead of its own D-a trigger.
  - **What it conceded after trying to break them:** exit-path byte-identity, `nullptr` bytewise
    parity at all three trainer sites (including the hand-fixed brace), the `Training_AnyWorkerRunning`
    wiring, the no-format-bump argument, and every gate in the re-derive fence.
- **Deliverable-completeness review (Stage 5.5): NOT COMPLETED — the operator stopped the agent
  mid-run.** Recorded as not-run rather than assumed-clean. Its unverified-items list was itself
  incomplete: the 6.5.4 review found nine items that were unverified AND unlisted, of which the
  entry-point miss was the one that mattered. **Re-fire it before `/ship`.**

## TaskList (recreate at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | pending (operator) | Rebuild + relaunch BOTH binaries — nothing from this session is live until then |
| #2 | pending (operator) | Collect once → Run Full Validation on `twins` → grep the stamp for 2/0.05/350 |
| #3 | pending (operator) | Train exit on a 2nd horizon (K≥2) + set `use_exit_model=1 exit_bandit_enabled=1` |
| #4 | pending (operator fork) | **D-a** model layout nested-vs-flat — trigger: BEFORE leg 4 |
| #5 | pending (operator fork) | D-b VOL_BARRIER/BARRIER tier · D-c #18 p_peak · D-d #22 re-disposition · D-e D4 summary.txt · D-f codify the class |
| #6 | pending | Re-fire Stage 5.5 independent review (stopped this close) before `/ship` |
| #7 | pending | E.1.2.D leaf 5 — batched multi-target label pass (TOTAL oracle; 2.65× measured) |
| #8 | pending | E.1.2.D leaves 8/10/11/12/13 — aliasing round-trip, WF snap block, Verify-Stamp, `csv_load_workers`, the small truths |
