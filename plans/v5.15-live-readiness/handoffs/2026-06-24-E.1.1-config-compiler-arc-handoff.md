---
type: handoff
status: superseded   # superseded 2026-06-28 by 2026-06-28-E.1.1-item4-core-next-handoff.md (item-4/B merge landed; item-4 CORE is the next pickup)
ship_tag: E.1.1-config-compiler-arc
plan_type: live-readiness
sprint_end_goal: v5.15 live-readiness — make the codebase live-ready + more maintainable
ship_end_goal: "③ config-compiler — boot-time capital-cfg validation (ALWAYS-ABORT all modes / NO-MARGIN); the D-255/D-256 6-item scope. The MALFORMED + UNKNOWN-KEY + caller-coverage surface is CLOSED (item-2 v-class CLEAN + the clean-break keystone + N1 2nd-parser + GAP-1-safety). item-4 (value-range caps) is NEXT."
predecessor_handoff: 2026-06-23-E.1.1-item2-pernode-handoff.md
engine_head: be5d803
workspace_head: "at or after d87619c (this close advances it via /sync-workspace)"
branch: feat/v5.15-live-readiness
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, "plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md", "plans/v5.15-live-readiness/MASTER.md", "plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (D-255/D-256 + D-257..D-264)", "DESIGN_SPECS/framework-patterns/config-compiler-validation-pattern.md", "DOCS/recurring-bug-patterns/class-52-swallow-and-coerce-capital-config-parse.md", "DOCS/recurring-bug-patterns/class-53-rename-completeness-gap-uncompiled-surface.md"]
coding_status: mid-ship checkpoint (9 green engine commits landed + UNPUSHED; the item-4-first chain is NEXT, teed up for fresh context)
---

# E.1.1 ③ config-compiler ARC — handoff (the keystone + N1 + GAP-1 landed; item-4 next)

## ⏩ NEXT ACTION (start here)

Pick up at **item-4 — per-field capital value-range caps** (a post-resolve sweep over the resolved `nodes[c]` capital fields, validating each against the `barrier_is_corrupt` / `BarrierValidation` constants → set `CFG_FAULT_CAPITAL_OUT_OF_RANGE`). It is the first SUBSTANTIAL new build of the chain (not a thin gate/capture), which is why it was deliberately teed up for fresh context. **It REUSES the keystone** built this arc: the `OUT_OF_RANGE` bit is already allocated (`CfgFieldRegistry.hpp:225`), the gate (`cfg_compile_ok`) already refuses on it, and A2 units are settled (fraction-space). **item-3** (dollar fields >0) folds into the same sweep.

Before coding item-4: read `DESIGN_SPECS/framework-patterns/config-compiler-validation-pattern.md` (the model item-4 extends) + consult `/accounting-audit` on the cap thresholds (consult-before-coding).

## State (verify at pickup)

| Field | Value |
|---|---|
| Engine HEAD | `be5d803` · branch `feat/v5.15-live-readiness` · **9 ahead of origin (UNPUSHED — all green)** |
| Workspace HEAD | at or after `d87619c` (this close advances it) |
| Tests | **3676 / 0** (controller_test); `build.sh gui` CLEAN (foxml_suite + engine_gui + engine + controller_test) |
| Pre-commit | GREEN on every commit (Check F determinism / M H14 / H identifier-retirement / K struct-layout / L P&L-gross) |

**PUSH on operator's word:** `git push origin feat/v5.15-live-readiness` (9 commits). Not yet pushed.

## What landed this arc (9 engine commits `45d5be8..be5d803`)

The **MALFORMED + UNKNOWN-KEY + caller-coverage** surface of the config-compiler — i.e. the founding bug (malformed/unrecognized capital cfg silently disables a control) is closed at value-level + key-level + 2nd-parser + caller-coverage:

1. `45d5be8..890b368` — ③ item-2: the 4 capital GLOBALS migrated into the registry + the per-node override malformed-capture (b1/b2) + the legacy-array hand-roll + the strstr→exact-suffix fold (the per-node founding bug). **v-class DoD CLEAN** (build + asan/ubsan 3665/0 + calls_graph CLEAN + determinism + accounting-SSoT no-bypass + non-vacuity proven via fix-revert).
2. `98267be` — B1: unit-agnostic malformed-refuse (decoupled from the cap bit).
3. (string migration `f56e863` + dead-macro hygiene `916d642`) — the ② rename residue: 4 active `core_*_strategy` cfg keys + the operator-string cohort migrated; the walker-shadowed dead `CFG_PARSE_MONEY/_POS` macros + 3 callers deleted (TD-215 partial).
4. `1032c9f` — **the clean-break** (the keystone): unknown SHARDED-key HARD-REFUSE, scoped `core_*/node_*`, own `UNKNOWN_KEY` bit, + the `FEATURE_MALFORMED` bit allocated for C1/C2. Fixed a doc-lie at `:2658`.
5. `04035ae` — **N1**: BinanceConfig venue-selector malformed-capture (`tt::parse_int_checked` + `binance_config_ok` + the boot gate) — the silent `use_testnet`→PRODUCTION flip closed.
6. `be5d803` — **GAP-1 safety**: the BacktestEngine optimizer base-cfg gate (`cfg_capital_gate_ok`, fail-the-run).

**Workspace this arc:** the NEW `tools/check_cfg_key_prefix_drift.py` guard (selftest-non-vacuous; it found 1 site the eyeball missed) wired into `run_all_tests.sh` + enrolled in `TOOLS.md`; the cfg + operator-string rename-residue migration; ~6 new controller_test blocks (the clean-break + N1-detector tests).

## The config-compiler model (the KEYSTONE — reuse this, don't reinvent)

Canonical spec: **`DESIGN_SPECS/framework-patterns/config-compiler-validation-pattern.md`** (5 composable parts, engine-cited at `be5d803`):
1. **Fault-bit taxonomy** — `uint32 cfg_load_fault_flags`, DISTINCT bit per fault class (`CfgFieldRegistry.hpp:224-227`); never merge severities (Class-49). 28 free bits.
2. **Universal gate** — `cfg_compile_ok` (`:1333`) / `cfg_capital_gate_ok` (`:1344`); every cfg-load path routes through it (boot refuses on any bit; ALWAYS-ABORT all modes, no-margin D2).
3. **Scoped loop-tail unknown-key refuse** — `core_*/node_*`-scoped (false-refuse-safe in the multi-parser shared-cfg setup, `main.cpp:75/76`).
4. **Single-source-at-parse-primitives** — `Money_FromString` SSoT for capital money; `parse_double_fast_checked` / `parse_int_checked` (locale-immune) for the RAW/int/feature channels. refuse-don't-coerce.
5. **Second-parser reuse** — a sibling boot parser gets its OWN fault flag + the boot gates on the union (N1).

## What's PENDING — the item-4-first chain (each REUSES the keystone)

| Item | What | Reuses |
|---|---|---|
| **item-4** (NEXT) | per-field ≤100% loss / ≤~1000% gain caps via the post-resolve sweep → `OUT_OF_RANGE` | the bit (allocated) + the gate + `BarrierValidation`; A2 units settled (fraction) |
| **item-3** | dollar fields > 0 | folds into item-4's sweep |
| **C1/C2** (#11) | the walker's ~47 FPN/float feature branches (`CfgFieldDispatch.hpp:122-135`) → `parse_double_fast_checked` → `FEATURE_MALFORMED` | the bit (allocated) + the primitive; **DECISION owed: refuse-vs-warn final call** (D-261 DECIDED-IN-PRINCIPLE: refuse-don't-coerce; the final call rides the tier build) |
| **item-5** | the Async.hpp:321 reload gate | the gate |
| **item-6** | the recurrence / caller-coverage guard (locks "every cfg-load path gated") | structural — the close of the GAP-1 class |
| **vocab** (#12) | the ~60-85 operator-facing core/Core/PER-CORE strings (point-in-time; partial cleanup done) + extend `check_doc_rename_classification.py` to source string-literals + the §15 allowlist. **The HealthLog JSONL `"core"` key is H21-PRESERVED (a wire identifier; var is `node_id`) — NOT a vocab target** (Class-53 false-positive surface) | Class 53 + the cfg-key guard |
| **codify** (#7) | the slate is now LANDED this close (Class 52/53 + the config-compiler spec); remaining = the rename-ship-methodology amendment (the non-code-surface-oracle requirement) | — |

## Homed items (tracked — NOT lost)

- **#10 GAP-1 GUI warn-banner** — `GUI/SettingsPanel.hpp:887` is OBSERVABILITY not safety (loads the GUI display mirror, not a running engine). Needs a warn-banner state field + its display↔execution render row (a focused `build_gui` feature). D-262. The engine boot gate IS the capital-safety.
- **global-unknown-key refuse** — broaden the clean-break beyond `core_*/node_*` once the multi-parser UNIFICATION lands (the global key-set becomes explicit). D-258. Blocked-on N1's successor (fold the BinanceConfig key-set knowledge).
- **N1 full integration test** — the `BinanceConfig_Load("use_testnet=banana")` → `!binance_config_ok` integration test is **V-class-owed** (openssl blocks including BinanceCrypto.hpp in zero-dep controller_test; the detector primitive IS tested). D-260.
- **TD-140 engine_arch** — DELETE the dead `engine_arch=per_core_slow|centralized` dispatch (vestigial post-single_core); delete-not-tombstone (D-263; it's not a wire/persisted slot).
- **TD-215** — the remaining dead `CFG_PARSE_PCT/MONEY` capital branches (partial-closed this arc).
- **pre-commit trigger-scoping** for `check_cfg_key_prefix_drift.py` — it's wired into `run_all_tests.sh` (STANDING); the pre-commit (commit-time, cfg/source-scoped) hook wiring is the bonus follow-up.

## Decisions this arc (decision-log D-257..D-264 — read them)

D-257 execution-order (keystone-first) · D-258 clean-break namespace-scoping + global-refuse deferral · D-259 fault-bit taxonomy (distinct bits) · D-260 N1 reuse-on-2nd-parser + refuse-not-safe-default · D-261 C1/C2 distinct-bit + refuse (homed) · D-262 GUI observability-not-safety · D-263 engine_arch delete-not-tombstone · D-264 rename-completeness-gap codified + vocab-migrate-now.

## Codifications this arc

- **Class 52** — swallow-and-coerce parsing of capital/config input (the cohort + the refuse-don't-coerce/single-source-at-primitives fix). `DOCS/recurring-bug-patterns/class-52-*.md`.
- **Class 53** — rename-completeness gap on the compiler-invisible surface (cfg data + operator strings, no oracle). `DOCS/recurring-bug-patterns/class-53-*.md`.
- **The config-compiler-validation-pattern spec** (Stage 3-first-canonical).

## Capture-completeness (this session — nothing lost)

- **Decisions → log:** D-257..D-264 (8 entries) appended to `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`, each with `D/C/F` + `STATUS` sentinels.
- **Findings → ledger / homed:** no NEW TECH_DEBT opened — the fixable-now items were FIXED this close (the cfg-key guard wiring + TOOLS.md enrollment). HOMED (tracked): #10 GAP-1 GUI warn-banner + the global-unknown refuse · #11 C1/C2 feature tier · #12 vocab migration · TD-140 engine_arch DELETE · TD-215 (partial-closed) · the N1 `BinanceConfig_Load` integration test (V-class-owed) · the cfg-key guard's pre-commit trigger-scoping. DONE (not homed): the clean-break + N1 + the BacktestEngine GAP-1 gate.
- **Memories/skills → index:** NO new operator-collaboration memory this arc (the decisions are design/project-specific — captured in the decision-log + the spec, not generalizable preferences). Codifications INDEXED: Class 52 + Class 53 (`DOCS/RECURRING_BUG_PATTERNS.md` rows + `splits_into`) + the config-compiler-validation-pattern spec (`DESIGN_SPECS/README.md` + `TAG_INDEX.md`; `check_index_currency.py` exit 0). The cfg-key guard enrolled in `TOOLS.md` + wired into `run_all_tests.sh`.

### TaskList snapshot (recreate at pickup)

| # | Status | Task |
|---|---|---|
| 1 | in_progress | E.1.1 ship — Core→Node rename + cfg clean-break + single_core deletion |
| 2 | in_progress | ③ config-compiler — item-2 + clean-break + N1 + GAP-1-safety DONE; **item-4 (caps) NEXT** |
| 3 | pending | ⑤ V-class — asan/ubsan + determinism re-cert + the N1 `BinanceConfig_Load` integration test |
| 4 | pending | E.1.1 ship-close — doc sweep + FEATURE_LOOKUP + cores-prose + TD-140 (engine_arch DELETE) |
| 5 | pending | E.1.2 — NodeState SoA / Money relayout (successor leaf) |
| 6 | in_progress | All-strategies-private restructure + capture (D-251) |
| 7 | pending | ③ codification — Class 52/53 + the config-compiler spec LANDED; the rename-ship-methodology amendment remains |
| 8 | pending | core_→node_ migration + cfg-key guard — guard BUILT + wired; the residue sweep + pre-commit scoping remain |
| 9 | pending | (folded into #7 — Class 52 + the spec LANDED this close) |
| 10 | pending | Boot-surface — N1 + GAP-1-safety DONE; GUI warn-banner + global-unknown refuse remain |
| 11 | pending | C1/C2 feature-determinism tier — bit allocated; the tier build + the refuse-vs-warn final call |
| 12 | pending | Vocab drift — guard built; the ~60-85 string sweep + the classifier source-string extension |

## Critical pickup-time reads

- `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` — D-255/D-256 (the 6-item scope) + D-257..D-264 (this arc).
- `DESIGN_SPECS/framework-patterns/config-compiler-validation-pattern.md` — the model item-4/C1-C2 extend.
- `DOCS/recurring-bug-patterns/class-52-*.md` + `class-53-*.md` — the anti-patterns the next edits must not reintroduce.
- `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md` — the E.1.1 ship body.
- `CoreFrameworks/ControllerConfig.hpp` (`cfg_capture_node_money_override` :1364 / `cfg_compile_ok` :1333 / the loop-tail refuse ~:3134) + `CfgFieldRegistry.hpp:224-227` (the taxonomy).

## Next-action surface kit (item-4)

| Kit element | For item-4 |
|---|---|
| Domain skill | `/accounting-audit` (the cap thresholds; consult before coding) |
| Mechanical | `check_per_node_registry_integrity.py` (per-node cfg integrity) · `./build.sh test` (gate) · the v-class (DoD) at close |
| DESIGN_SPECS | `config-compiler-validation-pattern.md` · `universal-cfg-field-registry-pattern.md` (the walker the sweep complements) |
| Anti-pattern classes | Class 52 (swallow-coerce) · Class 48 (sentinel-value-as-control) · Class 49 (no signal-merge — keep `OUT_OF_RANGE` distinct) |
| Invariants | H22 (per-node purity — validate the RESOLVED per-node value) · H4 (Money math) · D2 (no-margin, ALWAYS-ABORT) |

## Predecessor context (what landed at the item2-pernode handoff close)

The predecessor (`2026-06-23-E.1.1-item2-pernode-handoff.md`) carried ③ item-2 mid-coding. This arc CLOSED item-2 (the per-node founding bug) + v-class-verified it (DoD CLEAN) + extended into the clean-break + N1 + GAP-1. The 4 capital globals + the per-node overrides + the legacy arrays all now malformed-capture → refuse. No rework; the design held.
