---
type: handoff
status: superseded   # SUPERSEDED 2026-07-17 by 2026-07-17-E.1.2.A-completeness-gate-phaseC-open-handoff.md (FixedPointN converted + committed P6.128 → 163/163; the completeness gate built + standing). ≤1-active enforced by check_handoff_active_singleton.py.
ship_tag: E.1.2.A
plan_type: refactor   # in-code documentation infrastructure — maintainability/framework deliverable, not a capital-path change
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: the locked [SCHEMA]_[v1.0] tag system rolled out to EVERY engine file (P6), then toolchain + plugin functional pass → V1 integrated system (D-358)
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-07-17-E.1.2.A-P6-137of174-backtest-next-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md, DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md]
deletion_scope: none
coding_status: phase-6-162of163-tracked   # TRACKED corpus 162/163 (only FixedPointN left, convert-settled-parts). Gitignored remaining: 15 tests/. Then V1 toolchain+plugin pass (D-358).
---

# E.1.2.A P6 handoff — tracked corpus 162/163; FixedPointN finding logged; V1 sequenced

**Created:** 2026-07-17
**Target:** finish P6 comment-standardization (FixedPointN settled-parts + 15 gitignored tests/) → then the V1 toolchain+plugin pass (D-358)
**Branch:** feat/v5.15-live-readiness
**Pre-tag rollback anchor:** `pre-v5.15.5.F.4d.1.E.1.2`
**Plan / decision-log:** `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-353..D-358)
**Sprint MASTER:** `plans/v5.15-live-readiness/MASTER.md`

**Engine state at handoff write:**
- HEAD: `0a9857d` (P6.127 — Limits+Version)
- Latest tag: `pre-v5.15.5.F.4d.1.E.1.2`
- Version.hpp: `5.15.5.F.4d.1.E.1.1`
- Suite baseline: `3702/0` (verify `./build.sh test`)
- Working tree: `M Strategies/RegimeDetector.hpp` (CARAMEL'S WIP — DO NOT TOUCH / DO NOT COMMIT) + untracked scratch dirs. Otherwise clean.

**Workspace state at handoff write:**
- HEAD `3427219`; uncommitted (this session, pending sync): `DOCS/tech-debt/open.md` (TECH_DEBT-242), the decision log (D-357/D-358), `DOCS/CODE_TAG_INDEX.md` (session-end regen). The `/sync-workspace` at close commits these.

---

## TaskList state at handoff write (recreate verbatim via TaskCreate)

| ID | Status | Subject |
|---|---|---|
| #1 | in_progress | E.1.2.A umbrella — in-code documentation system |
| #2 | in_progress | Phase 4/5 residual — PLUGIN-side toolchain items |
| #3 | in_progress | Phase 6 — roll out tag system (162/163 tracked; GUI done, EmaCross done) |
| #4 | pending | fox-symdeps plugin polish backlog (V1 plugin pass — persistent-panel add-symbols independent of `<leader>dD`; no-flicker UX; richer info display) |
| #7 | in_progress | Convert FixedPointN settled parts (leave vestigial FPN_* block) |
| #8 | pending | Convert the 15 gitignored tests/ files |
| #9 | pending | Final no-loose-ends sweep + record V1 north-star |

(#5 Log TECH_DEBT-242, #6 Update E decision-log D-357 — both COMPLETED this session.)

---

## What landed this session (P6.126 → P6.127; tracked 160→162/163)

**Committed (engine):**
- **P6.126 `3788f3c`** — `GUI/DashboardPanels.hpp` (16 blocks). **GUI DIRECTORY 100% (12/12).**
- **P6.127 `0a9857d`** — `Limits.hpp` + `Version.hpp` (FILE-block-only SSoT files).

**Converted but NOT committed (gitignored):**
- `Strategies/private/EmaCross.hpp` — private strategy (untracked; `252973f` untracked it). Verified via **2-file lossless** (reconstructed original vs current, comment-stripped = 0 delta) + validator + full build 3702/0. No commit (gitignored). `[DERIVED]` filled (48B/16/1/none). Its `EmaCross_Init/Adapt` used `[SECTION]`; the 3 substantial fns + struct got full blocks.

**Denominator correction:** the historical "/174" is stale — the real TRACKED corpus is **163** (intervening ships — E.1.1 single_core + parity_harness deletion etc. — removed files). **162/163 tracked converted; only `FixedPoint/FixedPointN.hpp` remains tracked-unconverted.** Plus 15 gitignored `tests/` (bonus; verify via build + 2-file lossless, no commits).

---

## THE FixedPointN finding (the reason FixedPointN is convert-settled-parts, not full) — READ THIS

The cold max-care read of `FixedPoint/FixedPointN.hpp` (money SSoT, converted last) surfaced: the binary **`FPN_*` op family** (`FPN_Mul`/`FPN_Zero`/`FPN_Mag*`/transcendentals `FPN_Sqrt`/`InvSqrt`/`Sin`/`Exp`/conversions `FPN_FromDouble`/`ToDouble`/`FromInt`/`ToString`/`FromString`) still carries **pre-Ship-A 24B sign-magnitude bodies** (`.w[]`/`.sign`/`FPN_Binary<F>::N`/`::FRAC_WORDS`) on a `.v`-only 16B `FPN_Binary<64>` (concrete `FPN_Binary<64>::N` errors "not a member"; the templates compile via a GCC dependent-qualified-id leniency — genuine ODR-use `g(){return FPN_Zero<64>();}` DOES emit a symbol, so INSTANTIABLE, not uninstantiable).

- **NOT campaign-caused** (`git diff HEAD` on the file = empty; last touched by Ship A/B commits). **NOT a new bug.** It is the **D-163** deferred `FPN_*` op-family reshape, never executed (Ship B named `Money_*` per D-176, left binary `FPN_*` untouched).
- **The live `.v` surfaces are clean + correct:** `Money_*` on `FixedPoint<10,8>` + certified `umul_128x128_256`/`udiv256_qr`/`divmul_pow10` kernels (D-176/D-142); native `fp2_*` on `FixedPoint<2,64>` (D-142, slice 423/0). Engine green 3702/0.
- **UNRESOLVED (do NOT overclaim):** whether the engine's feature math genuinely RUNS on the `FPN_*` family (live) or routes through `fp2_*`/`Money_*` (dead-in-engine). Engine-green is NOT a deadness proof (instantiation doesn't hard-error). **A build-routing trace (`nm`/disasm a strategy TU) settles it** — that's the reshape ship's step 1. (I initially over-claimed "provably dead" in the ledger and CORRECTED both entries — the correction is IN TECH_DEBT-242 + D-357; do not re-introduce the overclaim.)

**Logged:** **TECH_DEBT-242** (`open.md`) + **D-357** (decision log). It's an E-series cleanup (the op-surface finalization deferred twice inside E), not urgent, not blocking.

---

## Remaining P6 work (the finish line before V1)

**1. `FixedPoint/FixedPointN.hpp` — convert SETTLED PARTS only (task #7).** Per D-357 disposition:
   - CONVERT: the `[FILE]` block (note the vestigial-`FPN_*` carve-out + TECH_DEBT-242 ref), the 3 core type `[STRUCT]`s (`FPN_Binary<64>`, `FixedPoint<2,64>`, `FixedPoint<10,8>`/`Money`), the traits (`is_fp_binary`/`is_fp_decimal` + `EngineMoneyT`/`MONEY_ENCODING_EPOCH`), the `Money_*` decimal family (~1579+), the certified kernels (`umul_128x128_256`/`udiv256_qr`/`divmul_pow10`), the `fp2_*` binary ops (~1257-1324).
   - LEAVE UNCONVERTED (with a pending-reshape marker in the FILE header): the entire `FPN_*` `.w` generic family (~140-1256). Documenting to-be-reshaped code = do-it-twice.
   - FIX: the header's "ported to `.v`" line (line ~38) — accuracy-nudge (the fn-family bodies were NOT ported; only the ~2,144 consumer sites rode the flip).
   - Tracked file → **standard `lossless.py` (git-HEAD) works**; validator; `check_cache_layout.py --fix` for the 3 structs; `./build.sh test`; battery baselines (det4 stranded 1, det4b 2, det1 0, ladder 0, gap 4). Commit as the next P6.NNN → tracked **163/163**.

**2. The 15 gitignored `tests/` (task #8)** — no commits; verify via build + 2-file-lossless (reconstruct original from a pre-edit read, comment-strip diff — the pattern used for EmaCross). Rot-check each. Proportionality: `controller_test.cpp` (29K) = FILE block + its section banners, NOT per-test blocks; `money_*_vectors.hpp` = data tables (FILE block + light); `schema_golden/{registry,struct_hot,wire_parser}.hpp` + `wire_format_invariants.hpp` + `sharded_snapshot_v10_golden.hpp` = golden fixtures. **money_* vectors need care (money semantics).** Sweep with `rg --no-ignore`.

**3. Then the V1 pass (D-358)** — toolchain (foxtag core cutovers, validator LAST per D-349) + plugin functional pass → V1. **Plugin = Caramel's session (D-353).** Plugin polish scope in D-358(b) + task #4.

---

## Accepted-known red (NOT yours to fix)

`check_session_docs.sh` full-tree scan RED on `Strategies/RegimeDetector.hpp:543 [STATE]` + `:627 [INIT]` (UNKNOWN category). These are **Caramel's uncommitted WIP** (`+` additions in `git diff HEAD`; her separate pilot conversion). Present all session as the "2 accepted RegimeDetector reds." **DO NOT touch RegimeDetector.hpp** (D-353: never `git add` it). Every other sweep check is GREEN.

---

## Capture-completeness (this session — nothing lost)

- **Decisions → log (decision-log SSoT):** **D-357** (FixedPointN vestigial-FPN finding + disposition; CORRECTED from an initial "dead/latent" overclaim to "unresolved/instantiable") + **D-358** (V1 sequencing: standardization → toolchain+plugin → V1, incl. the operator's 2026-07-17 plugin-polish scope). Both carry `<!-- D/C/F -->` + `<!-- STATUS -->` sentinels. No other operator-steered decision this session went un-logged.
- **Findings → ledger:** **TECH_DEBT-242** (vestigial `FPN_*` `.w` op-family bodies; the op-body reshape/dedup — DISTINCT from TECH_DEBT-142 which is the `.E.1` doc-rename tooling). Homed with trigger = the reshape ship. No other surfaced finding left unhomed.
- **Memories/skills → indexed:** none new this session (no new memory/skill/DESIGN_SPEC minted). The meta-shapes this session surfaced (cold-read-anomaly-→-escalate-before-consulting-the-log; categorical-claim-in-a-ledger-before-the-disproving-test) are noted here for a follow-up meta-anti-pattern-index harvest if they recur — captured, not formalized (proportionate; not yet confirmed-recurring).
- **Operator-facing feature (FEATURE_LOOKUP):** none — pure docs pass (comments-only) + finding-capture; no new cfg/observability/persistence.
- **Verified:** `check_session_docs.sh` = all GREEN except the pre-existing RegimeDetector WIP red (above); `check_cache_layout` gate GREEN; suite 3702/0 at both commits; CODE_TAG_INDEX regenerated (D-353 session-end regen).

---

## Arming for the next action

For `convert FixedPointN settled parts` — surface is the **money-numeric SSoT (H4/H9/H12)**, so arm it:
- **Read first:** the corrected **TECH_DEBT-242** + **D-357** (the settled-vs-vestigial boundary + the do-it-twice rationale) · `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md` (grammar SSoT) · the converted sister `Strategies/SimpleDip.hpp`/`MeanReversion.hpp` (STRUCT `[DERIVED]` + `[SECTION]` pattern) · the D-142/D-163/D-176 decision-log entries (why the FPN_* family is deferred).
- **Run (not just read):** `plans/v5.15-live-readiness/tools-staging/e12a-conversion-checkers/lossless.py FixedPoint/FixedPointN.hpp` (tracked → git-HEAD) · `tools/check_code_tag_blocks.py --paths` · `tools/check_cache_layout.py --fix --paths` (the 3 type structs) · `./build.sh test` · the det4/det4b/det1/ladder/gap battery.
- **Route to (judgment — await greenlight, don't auto-fire):** `/accounting-audit` is available if any Money_* semantics claim in an OVERVIEW needs a second set of eyes — but this is comments-only, so the bar is "does the prose describe the code accurately," gated by lossless + eyeball, not a full audit.
- **M8:** if delegating the tests/ batch, arm each agent with the 2-file-lossless recipe + the schema + `rg --no-ignore` (tests are gitignored); keep code byte-verbatim.

---

## Paste this prompt into a fresh Claude Code session

```
type: handoff-pickup

I'm picking up E.1.2.A P6 — the FINISH of the in-code [SCHEMA]_[v1.0] comment-standardization, then the V1 toolchain+plugin pass.

Run: /accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-17-E.1.2.A-P6-162of163-fixedpointn-finding-next-handoff.md

State: TRACKED corpus is 162/163 — ONLY FixedPoint/FixedPointN.hpp remains tracked-unconverted, and it is CONVERT-SETTLED-PARTS-ONLY (leave the vestigial FPN_* .w op family unconverted per D-357/TECH_DEBT-242 — do-it-twice vs the deferred reshape). Then 15 gitignored tests/ (no commits; 2-file-lossless verify). Then V1: toolchain cutovers + the plugin functional pass (Caramel's session; polish scope in D-358(b) + task #4).

HARD rules: (1) NEVER touch or git-add Strategies/RegimeDetector.hpp — Caramel's WIP (its :543/:627 [STATE]/[INIT] reds are accepted-known). (2) Code bytes NEVER change — comments-only, lossless-gated per file. (3) Do NOT re-introduce the "FPN_* is provably dead" overclaim — it's UNRESOLVED (instantiable; live-vs-dead needs a build-routing trace = the reshape ship's job).

Do NOT trust prior-session memory — verify against current code. Caramel will iterate with you.
```

---

## Quick links
- Decision log: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-353..D-358)
- TECH_DEBT-242: `DOCS/tech-debt/open.md`
- Schema SSoT: `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md`
- Conversion checkers: `plans/v5.15-live-readiness/tools-staging/e12a-conversion-checkers/` (lossless.py + battery)
- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
