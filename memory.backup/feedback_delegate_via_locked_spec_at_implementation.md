---
name: feedback_delegate_via_locked_spec_at_implementation
description: design = hands-on solo (taste/decisions un-delegatable + operator-understanding); implementation = parallelize agents against the LOCKED spec; the tag-system docs ARE the delegation substrate — no framework to build
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 9f2efedc-135a-42ff-a6c6-e3c32c03b1af
---

Caramel repeatedly wishes for "a system to coordinate multiple agents" but doesn't want to build that framework + doesn't want to lose visibility ("I don't know what all is being done"). The resolution, by phase:

- **DESIGN phase → hands on everything.** The taste, the vision, the format/architecture decisions ARE the product — un-delegatable — and she's building her own understanding, which delegation would rob. During design: consult + co-decide, do NOT fan out.
- **IMPLEMENTATION phase (once the spec is LOCKED) → it parallelizes, and she builds no framework.** The coordination "system" she wishes for IS the locked spec (north-star / taxonomy / decision-log). The orchestrator runs agents AGAINST the docs; she reviews at boundaries; everything lands in the docs so she keeps visibility. Offer to fan out agents once the design is locked.

**Why:** it dissolves the tension (control-and-understanding vs speed) by splitting it across phases + naming the spec as the coordinator.

**How to apply:** during a design/decision cycle, don't propose fanning out; co-decide. The moment the spec/decision is LOCKED, surface "I can fan this out against the spec, you review at boundaries." And carry the meta she landed: **a tagged/documented codebase IS the delegation substrate** — building the tag system builds the very thing that makes handing work to an agent (or a future hire) safe, because they read the tags instead of needing her. Sisters: [[feedback_structure_the_judgment_loop_not_output]] · [[feedback_independence_for_judgment_not_mechanical]] · [[feedback_consult_on_audit_findings]].
