# v5.9 — ML Hardening: silent-failure surfacing + train-serve parity

**Date:** 2026-05-02 (complete — 6 phases, 18 tags, 1 calendar day)
**Branch:** `feat/v5.9-ml-hardening`
**Tags:** `v5.9.0` → `v5.9.0a` → `v5.9.0b` → `v5.9.0c` → `v5.9.0d` (Phase 1)
       → `v5.9.1` → `v5.9.1a` (Phase 2 + init polish)
       → `v5.9.2` → `v5.9.2a` → `v5.9.2b` → `v5.9.2c` (Phase 3 + drift snapshot tests + stamp-bound cfg + runtime polish)
       → `v5.9.3a` → `v5.9.3b` → `v5.9.3c` (Phase 4: scaler infra → apply sites → parity validation)
       → `v5.9.4` → `v5.9.4a` (Phase 5: cross-binary handshake + log rotation; full closure of paper-test bugs + 4 carryovers)
       → `v5.9.5` → `v5.9.5a` → `v5.9.5b` → `v5.9.5c` → `v5.9.5d` (Phase 6: postmortem + `/parity-check` skill + sprint-exit build-warning hotfix + half-wired StampInferenceCfgInputs hotfix + bash CLI parity + GUI stamp UX hardening)
**Tests:** 1030 → 1289 (+259 across 20+ EXTENSIBILITY blocks)
**Hot path:** UNTOUCHED in every v5.9 ship — every change is slow-path,
init-time, cfg-parse, or display-time

## Trigger

The v5.8 paper-test soak surfaced a class of silent-failure modes
that the existing test suite + readiness checks didn't catch:

1. **Silent ML model load failure** — operator paper-tested for
   hours without realizing the ML core fell through to SimpleDip
   because the model file was missing/refused. No GUI surface, no
   stderr CRITICAL.
2. **Silent NaN propagation** — feature-pack and label-compute paths
   could produce NaN/Inf without any guard, polluting training
   matrices and producing junk predictions at inference.
3. **Silent cfg defaults** — backtest.cfg with missing per-core
   strategy fields fell through to SIMPLE_DIP across the board with
   no boot warning.
4. **Silent ConfidenceScorer fallback** — invalid tau values
   silently overrode to defaults at init time, no log.
5. **Silent walk-forward purge violation** — folds could violate the
   purge gap invariant without any post-gen check.

The audit (`DOCS/V5_9_ML_HARDENING_AUDIT.md`) catalogued 27 findings
across 10 categories. The plan is to close them in 6 phases:

- **Phase 0** — audit doc + ML invariants spec (~3h)
- **Phase 1** — visibility + immediate guards (~11h, 9 findings)
- **Phase 2** — edge cases + observability polish (~5h, 5 findings)
- **Phase 3** — train-serve parity regression test (~5h)
- **Phase 4** — feature standardization (~7h, MODEL_FORMAT_VERSION 5→6)
- **Phase 5** — recipes + readiness extensions (~4h)
- **Phase 6** — postmortem (~2h)

This file documents Phases 1 + 2 (shipped). Phases 3–6 will append
as they ship.

## Phase 1 — Visibility + immediate guards (5 sub-letter ships)

Strategy was to ship each gap-closure as its own letter so each
review window stayed small.

### v5.9.0 — NaN/Inf guards + WF purge post-check + stamp_format_version

Closes V5_9_AUDIT-#1, #6, #8, #15.

- **Feature pack output validation.** `Features_PackAll` now
  returns `-1` sentinel when any feature's FPN value is outside the
  finite envelope (via new `FPN_IsValidFinite` branchless helper at
  `FixedPoint/FixedPointN.hpp:721`) OR when the float-cast produces
  NaN/Inf. Caller branches on the sentinel and skips the prediction
  cycle. Two-layer check catches both FPN saturation (1e15
  threshold) AND IEEE-754 NaN.
- **Prediction NaN/Inf guard.** `Model_Predict` and
  `Model_PredictMulti` callers in `StrategyParameters.hpp` check
  `isnan(prediction) || isinf(prediction)` post-call; skip the
  cycle on hit. Previously, NaN flowed straight into the threshold
  comparison and the entry gate fired on essentially-random
  outcomes.
- **Walk-forward purge post-check.** `ValidationSplit_Generate`
  now loops folds after generation and FATALs on any fold where
  `train_end + purge_gap > test_start`. Catches the
  PurgeGap_Compute → fold_size_n math drift class.
- **Stamp format version.** New `stamp_format_version=1` field
  emitted into stamp body when `format_version >= 5`. Both bash
  (`tools/stamp_model.sh`) and in-process emitters parity-pinned.
  Defensive: gives v5.9.3 (when MODEL_FORMAT_VERSION moves to 6) a
  forward-compat schema field to bump.

### v5.9.0a — audit doc + CLAUDE_ML_INVARIANTS.md

Doc-only ship. `DOCS/V5_9_ML_HARDENING_AUDIT.md` (27 findings,
severity-classified, IDs `V5_9_AUDIT-#N` for traceability) and
`DOCS/CLAUDE_ML_INVARIANTS.md` (13 load-bearing rules covering
train-serve parity, FEATURE_REGISTRY_HASH, MLBuildContext
population, models-load-once, ML→SimpleDip CRITICAL log,
`FPN_Zero` on missing state, locale pinning, atomic stamp write,
deploy ritual, scaler FeatureId-indexed, strict mode default).

Bonus: removed 11 unused FeatureComputeCtx aux fields. Two struct
fields remain: `signals` + `short_rolling`. Anything else gets
cargo-culted by future feature additions and adds confusion.

### v5.9.0b — visibility (model load + entry log enrichment)

Closes V5_9_AUDIT-#2, #3.

- **HEALTH_CRITICAL = -2 + HEALTH_WARN = -1 levels.** Filter logic
  fixed (`level > 0 && level > s->min_level` — was `level <
  min_level` which inverted negative-level handling). New
  `Health_LogCriticalRateLimited` helper for per-core
  rate-limited CRITICAL logs.
- **Model load failure surface.** `CoreContext.model_load_failed`
  bit set when `CoreModelZoo_LoadFromDir/Legacy` returns 0 OR
  strict verify fails. Distinct from `model_loaded == 0` (which
  could mean "operator didn't configure ML"). Surfaced via
  `PerCoreSnap.ml_model_load_failed` → ML Status panel renders
  red **LOAD FAILED** state.
- **Entry log enrichment.** Entry health-log row now carries
  `prediction`, `threshold`, `effective_threshold`, `confidence`,
  `registry_hash` — operator can now post-hoc reconstruct the
  decision context for any fired entry.
- **ML Status panel (NEW).** `GUI/MLStatusPanel.hpp` — stateless
  per-core ML observability. Renders model state tri-state,
  prediction/threshold/effective_threshold/confidence, NaN
  counters with red indented sub-line. Direct render call from
  main loop (NOT in `FOREACH_PANEL(X)` registry — it's a
  cross-cutting observability surface, not a "stateful panel").

### v5.9.0c — cfg awareness (cfg-source visibility + default-vs-deliberate)

Closes V5_9_AUDIT-#4, #5.

- **Cfg-source visibility.** `TUISnapshot.source_cfg_path[256]` —
  operator can now see "which cfg did this engine actually read?"
  in the engine header panel. Engine header renders the path when
  the snapshot has it. foxml_suite shows the cfg path as a tooltip
  next to the run target.
- **Default-vs-deliberate cfg tracking.** New
  `ControllerConfig.core_strategies_explicit_set` (uint16_t bitmap)
  — bit `i` is set when `core_<i>_strategy=` line is encountered in
  the cfg. Boot WARN at parse end fires when
  `num_execution_cores > 0` AND bitmap is 0 (every core fell
  through to SIMPLE_DIP default).
- **Tri-state Per-Core P&L marker.** GUI Per-Core P&L panel
  renders core index suffix `!` (deliberately set), `?` (defaulted),
  or no suffix (AUTO). Tooltip on `?` explains
  `V5_9_AUDIT-#5`.

### v5.9.0d — Train Model worker thread (UI freeze fix)

Closes V5_9_AUDIT-#7.

`Train Model` button on the foxml_suite Training panel previously
ran XGBoost synchronously on the UI thread, freezing for 5–30s
during training. Lifted into a worker thread mirroring the
v5.8.7 fullvalidation_worker_fn pattern:

- `train_model_worker_fn` — snapshots user-modifiable state
  (max_depth, learning_rate, n_estimators, label_type,
  model_path) at entry; polls `state->tm_cancel` between
  `XGBoosterUpdateOneIter` calls (bounded latency = one iter
  time, ~100ms–1s); sets `tm_complete=1`, `tm_running=0` on
  every exit path (success, error, cancel).
- UI shows pulsing `ProgressBar` + `Cancel Training` button
  while the worker is alive.
- `pthread_detach` (no join, mirrors FV worker pattern).

## Phase 2 — Edge cases + observability polish

Single tag (per plan: "Commit + tag: v5.9.1"). Closes 5 audit
findings (V5_9_AUDIT-#3, #9, #12, #13, #21).

### v5.9.1 — edge-case + observability polish

- **Label NaN guards per-type.** `BacktestStats.nan_labels_total +
  nan_labels_dropped` counters. Wrapper at
  `BacktestEngine.hpp:510` (the per-sample `label_fn` loop):
  binary→`0.5f`, regression→`0.0f`, multiclass→`NAN` sentinel.
  Both training paths (walk-forward + held-out TrainSingleFold)
  filter NAN-marked samples at compaction. Past Runs panel
  surfaces `nan_labels_total` when > 0 with red tooltip.
  In practice, only triggers on degenerate input (sample_price=0)
  — not reachable on real Binance ticks. Defensive upgrade only.
- **Warmup progress observable.**
  `PerCoreSnap.warmup_progress_pct` (uint8_t, 0..100) populated
  from `cores[i].slow_state->rolling_short.count` vs
  `min_warmup_samples` (engine default 64 fallback).
  `EventLoop_RebuildOneCore` emits `[core N] warmup complete
  (...)` to stderr exactly once per core per session via the new
  `CoreContext.warmup_log_emitted` edge flag. ML Status panel
  shows `warmup: NN%` line when pre-warmup with tooltip
  explaining why predictions aren't firing yet.
- **ConfidenceScorer tau<=0 hardening.**
  `ConfidenceScorer_Init` logs WARN when tau<=0; previously
  silent fallback to default. ControllerConfig parser explicitly
  rejects tau<=0 — the error is caught at the cfg layer instead
  of silently overriding at init time.
- **Confidence hard-floor (DEFAULT DISABLED).** New cfg
  `confidence_hard_block_threshold` (default `0.0` = disabled,
  preserves pre-v5.9.1 behavior bytewise; audit-recommended
  `0.05` once retrained). When `> 0.0` AND
  `confidence_enabled=1`, hard-blocks ML entries with raw
  confidence below the floor; emits `SHALT_LOW_CONFIDENCE` on
  the strategy_halt_reason channel. New
  `MLBuildContext.out_strategy_halt_reason` pointer wires
  through to the dispatcher's existing
  `strategy_halt_reason` parameter — single-source-of-truth
  for SHALT propagation.
- **`DOCS/ML_TRAINING.md` (NEW).** Class-weight tuning guidance
  for the trainer side. Calls out the v5.8 paper-test
  4.2/48.3/47.5 PEAK_VALLEY_STABLE distribution and recommends
  inverse-frequency `sample_weight`. Anti-patterns: SMOTE
  up-sampling (look-ahead bias), threshold-lowering
  (compounds noise), half-strength weights (heuristic).

### v5.9.1a — init polish (uninit-field fix)

Caught by v5.9.1 parity audit (`/ml-audit parity`):
`EventLoopState_Init` doesn't explicitly initialize the 7 new
CoreContext fields added in v5.9.0b + v5.9.1:

- `model_load_failed` (v5.9.0b)
- `last_ml_critical_log_us` (v5.9.0b)
- `last_ml_threshold` (v5.9.0b)
- `last_ml_effective_threshold` (v5.9.0b)
- `nan_feature_events_total` (v5.9.0b)
- `nan_prediction_events_total` (v5.9.0b)
- `warmup_log_emitted` (v5.9.1)

Stack-allocated `EventLoopState<F>` doesn't zero memory — these
fields read garbage on the first slow-path rebuild. In practice
the layout placed them next to other zero-init'd fields so most
boots happened to read 0, but it's not a guarantee — the
warmup-complete log could have fired spuriously, the NaN
counter could have read non-zero on first display, etc.

Single-commit fix: explicit init for all 7 fields at the end
of the existing per-core init loop, with comment pointing to
the v5.9 audit.

## Train-serve parity verification

Phase 1 + Phase 2 are net-additive for default cfg:

| Operator profile | Pre-v5.9 | Post-v5.9 | Identical? |
|---|---|---|---|
| Default cfg, no ML | unchanged | unchanged | ✅ |
| `confidence_enabled=1`, defaults otherwise | damping only | damping only (hard_floor=0.0 disabled) | ✅ |
| Backtest binary label, normal price data | NaN labels (rare) into XGBoost | wrapped to 0.5 (rare) | ✅ (NaN unreachable) |
| Backtest multiclass label, normal price data | NaN labels (rare) into XGBoost | NAN-marked, skipped | ✅ (NaN unreachable) |
| Operator opts in to `confidence_hard_block_threshold=0.05` | (feature didn't exist) | hard-block active | ✅ (intended opt-in) |

`FEATURE_REGISTRY_HASH` unchanged (no `FOREACH_FEATURE` edits).
Models trained pre-v5.9 still load + predict identically.
Stamps from v5.8 still verify.

## Skills shipped alongside

- **`/ml-audit`** — systematic ML pipeline audit (10-category full /
  5-category parity / narrow function/file mode). The audit pattern
  used to surface the original 27 findings, formalized so future
  sprints can re-run it without rebuilding the methodology.
- **`/ship`** — post-coding ritual (build verify + calls_graph_diff
  + version bump + commit + tag + push). Captures the gates Jenny
  has been running manually since v5.7. Hot-path-touch confirmation
  prompt; refuses force push, refuses --no-verify.

## Verification gate (every Phase 1 + 2 ship)

- `./build.sh test gui suite` — clean
- `./tools/calls_graph_diff.sh` — CLEAN throughout
- Hot path UNTOUCHED in every ship
- 1030 → 1091 tests, +61 across 4 v5.9 EXTENSIBILITY blocks
- Branch + tag pushed to `origin` after each ship

## Phase 3 — Train-serve parity regression test (4 sub-letter ships)

Closes V5_9_AUDIT-#10, #11, #14, #16, #17, #18, #19, #20, #22, #23.

### v5.9.2 — Phase 3 train-serve parity regression test

- **`tests/parity_harness.cpp` activated.** Existing scaffolding from
  v5.7 fully reusable. New invariant: bytewise-identical predictions
  across two backtest runs given the same tick file + cfg + model.
- **Replay determinism test.** Spawns two `BacktestSharded_Run`
  invocations on a fixed CSV. Compares per-tick prediction sequence
  byte-for-byte. Fails on any divergence.
- **MLBuildContext population audit.** Verifier walks every field
  consumed by ML strategy dispatcher; both live + backtest must
  populate all of them. Caught one stale field (held-over from v5.0
  era).

### v5.9.2a — Phase 3.5 drift protection snapshot tests

- **Snapshot tests for FOREACH_FEATURE compute fns.** Each feature's
  Compute_* body has a fixed-input → fixed-output snapshot (Q32
  bit-exact). Catches body changes that bypass FEATURE_REGISTRY_HASH
  (which only tracks structural/X-macro changes).
- **Snapshot tests for label fns.** Same shape for `Label_Binary`,
  `Label_Regression`, `Label_Multiclass_TripleBarrier`,
  `Label_Multiclass_PeakValleyStable`.
- **Snapshot tests for ConfidenceScorer + SimpleDip strategy.**
  Coverage extended to load-bearing inference-time math.
- **`DOCS/PARITY_LIFECYCLE.md`** + **`DOCS/PARITY_VERIFICATION_CHECKLIST.md`**
  (NEW) — formalize the invariant categories per surface.

### v5.9.2b — Phase 3.6 stamp-bound inference cfg

Closes V5_9_AUDIT-#22 ("cfg silently affects model behavior").

- **9 inference-affecting cfg fields stamp-bound:**
  `confidence_threshold_scale`, `barrier_gate_enabled`,
  `confidence_hard_block_threshold`, `held_out_fraction`,
  `freshness_tau`, `bandit_blend_ratio`, `fee_rate_maker`,
  `fee_rate_taker`, `training_poll_interval` (last two added later
  in this phase + v5.9.4a respectively).
- **Cross-major engine_version detection.**
  `cross_major_engine` flag set when stamp's engine_version major
  differs from current build. Refuse load in strict mode; WARN
  otherwise.
- **Tau range hardening.** Cfg parser explicitly rejects invalid
  tau values at parse time, not at init.

### v5.9.2c — Phase 3.7 runtime parity polish

- **CSV row sort invariant.** `BacktestSharded_Run` asserts
  monotonically-increasing timestamps before the first tick. Catches
  out-of-order CSV inputs that would produce different RollingStats
  evolution than live (which is monotonic by construction).
- **HeldOutSplit token invariant.** Held-out fraction binding via
  stamp body's `held_out_fraction` field — reload with mismatched
  cfg refused in strict mode.
- **PerCoreSnap audit.** Every CoreContext field operator might want
  visibility into has a matching `PerCoreSnap` field + populator
  call in `TUI_CopySnapshotSharded`. Caught 3 silent-display gaps.
- **IC buffer drift fix.** `ConfidenceScorer` IC ring buffer was
  initialized post-warmup but read by display code pre-warmup;
  fix moves init to `ConfidenceScorer_Init`.

## Phase 4 — Feature standardization (3 sub-letter ships)

Closes V5_9_AUDIT-#24, #25, #26.

Strategy: `MODEL_FORMAT_VERSION` stayed at 5 (Phase 4 deliverable
ended up being **forward-compat** rather than format bump — the
scaler is a sidecar file, not part of the model format itself).
This was the single biggest design insight of the sprint: stamp body
extension via Surface G `has_*` flag pattern is strictly more
maintainable than format-version bumps because it doesn't break
old stamps.

### v5.9.3a — Phase 4 Subship 1: feature standardizer infrastructure (disabled)

- **`ML_Headers/FeatureStandardizer.hpp`** (NEW). Sidecar file format:
  per-feature mean + stddev, FOREACH_FEATURE-indexed, Q32 fixed-point
  with stddev_floor for numerical safety. Embedded
  `feature_registry_hash` binds the scaler to the X-macro registry
  it was computed against — independent of model fingerprint.
- **`ModelHandle.scaler` inline struct.** No heap allocation;
  scaler travels with the handle.
- **`Model_Init`/`Model_Free` discipline.** All scaler fields zero-
  init'd; load failure flips `scaler_load_failed=1` → distinct
  PerCoreSnap field + ML Status panel branch.
- **Stamp body fields.** `scaler_sha256`, `scaler_feature_registry_hash`,
  `has_scaler_*` flags. Forward-compat: legacy stamps without these
  fields still load (default `has_*=0`).
- **CLI + GUI tooling** for scaler emission. `tools/stamp_model.sh`
  takes `--scaler-path`. Disabled by default (apply path inactive
  until v5.9.3b).

### v5.9.3b — Phase 4 Subship 2: scaler apply at 5+1 sites

- **5 inference apply sites** (live + backtest — exact mirrors).
- **1 training integration site** (compute scaler post-fit, write
  sidecar atomically via `.tmp + rename`).
- **Two-layer NaN guard at apply.** Pre-apply via
  `Features_PackAll` + post-apply finite check. Drops the cycle on
  hit. Distinct counter from feature NaN counter.
- **Three-tier strict-mode behavior at scaler load:**
  refuse (`strict=1` + missing/mismatch) / warn-with-surface
  (`strict=0` + mismatch) / silent-forbidden (no fallback to
  hardcoded scaler — that would defeat the parity invariant).

### v5.9.3c — Phase 4 Subship 3: parity validation closure + retrain ritual docs

- **Parity validation extended.** Replay determinism test now
  loads a scaled model + sidecar; verifies bytewise identical
  predictions across two runs.
- **`DOCS/ML_TRAINING.md`** updated with "Scaler-aware training"
  section: step-by-step retrain ritual for operators — train →
  emit scaler → atomic-rename both companion files together →
  verify stamp body. Explicit ordering matters (emit scaler before
  stamp emission) because the stamp's `scaler_sha256` is computed
  against the on-disk file.
- **`DOCS/CLAUDE_ML_INVARIANTS.md`** updated: 4 new rules covering
  scaler binding + sidecar atomic-write + apply NaN guard +
  3-tier strict mode.

## Phase 5 — Cross-binary handshake + recipes + log rotation (2 sub-letter ships)

Closes V5_9_AUDIT-#9b, #21b, #27 + readiness Checks 15-17.

### v5.9.4 — Phase 5 cross-binary handshake + log rotation + Class 12

- **Cross-binary handshake.** Engine boot WARNs when stamp's
  `engine_version` minor differs from current build. Suppressible
  via `acknowledge_cross_binary_version_drift=1` cfg flag (default
  0 — vocal-by-default, op-out for known-safe upgrades).
- **Health log rotation.** `health.jsonl` rotates at 100MB → up to
  10 historical files (`health.jsonl.1` … `health.jsonl.10`). Atomic
  `rename()` swap — no log loss across rotation.
- **Class 12 ml-audit category.** "Cross-binary build flag fingerprint"
  added to `/ml-audit` (currently DOCUMENT-ONLY; v5.10 candidate).
- **Readiness Checks 15-17.** Atomic file writes / locale pinning /
  GUI render-thread blocking I/O — added to `/readiness` skill.

### v5.9.4a — Full closure: paper-test bugs + 4 carryovers + v5.10 design notes

Caught in 2026-05-02 paper testing soon after v5.9.4 ship:

**Paper-test bug B (auto-stamp gate).** Auto-stamp on held-out was
silently no-op'ing because the gate was a single boolean that
conflated three distinct error states. Fix: 3-state diagnostic
(skipped / refused-for-cause / completed) at suite UI.

**Paper-test bug C (multiclass "no edge" diagnosis).** Display
hardcoded `val_acc < 0.52f` as the "no edge" threshold — fine for
binary, wrong for multiclass. A 3-class with 5.9/47.4/46.7
distribution showed val=46.7% which "looked like 50% random" but
the actual baseline was max-class-freq=47.4%. Fix:
`multiclass_baseline_accuracy()` helper in `BacktestEngine.hpp`
returns `max(1/K, max_class_freq)`; diagnosis bands relative to
baseline (≤baseline+1pp = no edge / [+1pp,+3pp] = marginal /
≥+3pp = real edge).

**4 deferred items closed:**

1. **Warmup log strategy name** (V5_9_AUDIT carryover).
   `[core N] warmup complete` line now includes the active strategy
   name (e.g. `[core 0] warmup complete (64/64 samples) — ml_strategy
   active`). Operator can verify cfg vs runtime at boot.

2. **`model_num_outputs` stamp body field.** Catches output-dimension
   mismatch (e.g. binary model loaded into 3-class slot).
   `StampInferenceCfgInputs.has_num_outputs + model_num_outputs`;
   `ModelStampResult.has_model_num_outputs + model_num_outputs`;
   `ModelHandle.stamp_model_num_outputs + has_stamp_num_outputs`.
   `CoreModelZoo._TryLoadRole` verifies stamp's value vs handle's;
   refuse strict / warn non-strict. CLI: `tools/stamp_model.sh
   --model-num-outputs=N`.

3. **`training_poll_interval` cadence WARN.** Engine boot WARN when
   `cfg.poll_interval` differs from any loaded handle's stamp value.
   Same suppression cfg as cross-binary handshake.

4. **Gap I worker auto-unlink orphan scaler.** Train Model worker
   unlinks the previous scaler sidecar on retrain start, preventing
   orphan-scaler-stamp-mismatch. Observable via separate stderr line.

**Multiclass UX upgrades** (display-side; do not affect training):

- Walk-forward + Train output now print baseline + per-class
  accuracy + 3-tier diagnosis when num_classes > 2.
- Past Runs surfaces baseline-aware "no edge" reason.
- Foxml Suite ML Status panel renders multiclass-specific rows.

**v5.10 design notes** (`DOCS/v5.10-design-notes.md` NEW, ~445 lines):
10 forward-looking ideas including operator's parallel hyperparameter
search idea (idea #1), FOREACH_TARGET retrofit (idea #2), drift
detection (idea #3), build flag fingerprinting (idea #10).

## Phase 6 — Postmortem (this ship)

### v5.9.5 — Postmortem + `/parity-check` skill

This file (Phase 1 + 2 originally, fully expanded to 6 phases at
sprint close).

- **`/parity-check` skill** (NEW, `.claude/skills/parity-check/SKILL.md`).
  Distinct from `/ml-audit`. Walks every train-serve handoff surface
  systematically — features, labels, scaler, stamp body, cfg,
  threading, build flags. Output is severity-classified findings
  report; user decides what to ship next. Captures the methodology
  used in the 2026-05-02 audit that found 11 new gaps post-v5.9.2,
  driving v5.9.2c + v5.9.4 + v5.9.4a.
- **2 KDDs added to `CLAUDE.md`** (NaN-free feature pack invariant +
  parity-tested-by-construction discipline).
- **`DOCS/V5_9_ML_HARDENING_AUDIT.md` flipped to status="CLOSED"**
  — all 27 findings annotated with closing tag.

### v5.9.5a — Sprint-exit build-warning hotfix

Caught when running `./build.sh test gui suite` as part of the
post-v5.9.5 testing pass. The `test`-only build at v5.9.5 ship
time didn't surface this; full-target build did.

**Bug:** `FeatureStandardizer.hpp` Persist (line 320) and
Load-verify (line 221) both staged the on-disk body in a
manually-sized stack buffer:
```cpp
uint8_t body[8 + 4 + sizeof(double) * NUM_REGISTERED_FEATURES * 2];
```

The original v5.9.3a layout was `magic(4) + nfeat(4) +
floor_q(4) + mean(8N) + stddev(8N)` = 12 + 16N. Then v5.9.3a's
`registry_hash` (uint64_t, 8 bytes) was added between nfeat and
floor_q in the writes — **but the buffer-size formula wasn't
updated**. Actual writes: `magic(4) + nfeat(4) + hash(8) +
floor_q(4) + mean(8N) + stddev(8N)` = 20 + 16N = 564 bytes for
N=34. Buffer was sized at 12 + 544 = 556 bytes. **Off-by-8 stack
overflow on every scaler write + load.**

Tests passed (1255/0) because:
- `fwrite(body, 1, off=564, f)` wrote the correct 564 bytes to
  disk (data integrity preserved at the file format layer).
- The 8 overflowed stack bytes landed in dead stack space on the
  current GCC + `-O2` layout (no observable symptom).
- The existing file-size test (`controller_test.cpp:11042`,
  expected_size = 596) verified the disk format, not the stack
  buffer.

Why the compiler caught it: `gcc -Wstringop-overflow` on the
`gui` + `suite` builds (which use different optimization flags
than `test`) propagated the offset analysis through `memcpy`.

**Fix (3-part):**

1. **Single source of truth** for body layout. New file-scope
   constant `SCALER_BODY_BYTES` computed via `sizeof()` per field.
   Both buffer declarations now reference the constant; if a
   field's type changes (e.g. `registry_hash` upgraded to 128-bit),
   the buffer auto-updates. Adding a new field forces editing the
   layout list — drift becomes mechanical, not silent.

2. **Self-documenting comment** describing the on-disk format
   `[body || sha256(body)]`, every field's byte size + type +
   purpose + provenance, and the worked total `20 + 16N bytes`.
   Future readers (and audits) understand the layout without
   tracing the writes.

3. **`/parity-check` skill** updated with new Section K
   (build-warning audit). Source-read audits miss bugs the compiler
   can statically detect; every full audit pass must run
   `./build.sh test gui suite` and grep for `-Wstringop-overflow` /
   `-Waggressive-loop-optimizations` / `-Wuninitialized` warnings
   in serialization + load + init paths. Severity classification
   provided. **The `/parity-check` agent run that declared
   sprint-exit "PASS" missed this exact bug because it was
   source-read only — methodology gap closed.**

**Lesson (added to postmortem):** sprint-exit verification must
include `./build.sh test gui suite` (all 3 targets), not just
`test`. The `test` target uses tighter optimization flags that
happen to suppress the constprop analysis pass that catches this
class. Different build configs produce different warning sets;
audit must walk all of them.

**Files touched:**
- `ML_Headers/FeatureStandardizer.hpp` — `SCALER_BODY_BYTES`
  constant + 2-site buffer fix + layout comment
- `.claude/skills/parity-check/SKILL.md` — Section K
  (build-warning audit category)
- `DOCS/changelogs/2026-05-02-v5.9-ml-hardening.md` — this
  addendum

**Verification:**
- `./build.sh test gui suite` — clean (no scaler-related warnings)
- 1255/0 tests pass (existing scaler round-trip test exercises
  both write + load paths)
- Disk file format unchanged: 596 bytes total = 564 body + 32
  SHA. Existing scaler files load cleanly post-fix.

Hot path UNTOUCHED.

### v5.9.5b — Half-wired StampInferenceCfgInputs hotfix

Caught while running the v5.9.5 testing pass — Jenny asked "is the
model stamping the GUI wired in properly?" and the answer was
"the surfaces look right, but the in-process stamp emit at
`Backtest_RunFullValidation` passes nullptr for `inf`."

**Bug:** v5.9.2b + v5.9.3a + v5.9.4a added 10 inference-affecting
fields to the stamp body schema:

- 9 cfg fields from v5.9.2b: `confidence_threshold_scale`,
  `barrier_gate_enabled`, `confidence_hard_block_threshold`,
  `held_out_fraction`, `freshness_tau`, `bandit_blend_ratio`,
  `fee_rate_maker`, `fee_rate_taker`, `training_poll_interval`
- 1 model dimension from v5.9.4a: `model_num_outputs`

Each ship added the field to the struct, the verifier code, AND a
unit test that constructed a synthetic `StampInferenceCfgInputs`
and verified the round-trip. The unit tests passed every time.

**But the production caller was never wired.** `BacktestEngine.hpp:894`
(`Backtest_RunFullValidation`'s auto-stamp emit) called
`stamp_write_for_model(...)` without the optional `inf`
argument — passing nullptr by default.

Effect: every stamp emitted by the GUI suite's "Run Full
Validation" button (the common operator workflow) was missing all
10 fields. Operator could change cfg between training and
inference; the engine had no mechanism to catch the drift because
the stamp never recorded the training-time cfg. v5.9.4a's
`model_num_outputs` protection (refuse on output-dimension
mismatch) was inert in the suite-emit path.

The bug is the v5.9.4a `model_num_outputs` issue at greater scope —
the entire v5.9.2b inference cfg binding work was half-wired.
Tests passed for the API but not for the production invocation.

**Why /parity-check missed it (sprint-exit, source-read agent run):**
the audit walked "is the field defined / verified / round-trip-
tested?" — all yes. It didn't walk "does every production caller
populate the field?" That's the methodology gap closed by Section L
(see below).

**Fix:** populate `StampInferenceCfgInputs` from `data->config_used`
+ `label_type` immediately before the emit:

```cpp
StampInferenceCfgInputs inf = {};
inf.has_inference_cfg = 1;
inf.confidence_threshold_scale =
    FPN_ToDouble(data->config_used.confidence_threshold_scale);
inf.barrier_gate_enabled = data->config_used.barrier_gate_enabled;
// ... 8 more cfg fields ...
if (data->config_used.bandit_enabled) {
    inf.has_bandit = 1;
    inf.bandit_blend_ratio =
        FPN_ToDouble(data->config_used.bandit_blend_ratio);
}
if (data->config_used.cost_gate_enabled) {
    inf.has_fees = 1;
    inf.fee_rate_maker = FPN_ToDouble(data->config_used.fee_rate_maker);
    inf.fee_rate_taker = FPN_ToDouble(data->config_used.fee_rate_taker);
}
inf.has_training_poll_interval = 1;
inf.training_poll_interval = data->config_used.poll_interval;
{
    int K = LabelType_NumClasses(label_type);
    inf.has_num_outputs   = 1;
    inf.model_num_outputs = (K >= 2) ? K : 1;
}

// Pass &inf as last arg to stamp_write_for_model
```

Bandit + fees fields gated by their respective cfg flags
(don't emit when disabled — preserves legacy stamp shape for
operators who run with defaults). model_num_outputs derived
from `LabelType_NumClasses` (single source of truth).

**`/parity-check` Section L added** — production-caller field-
population audit. For every newly-added stamp body / serialization
field, audit that:

1. Field defined in struct
2. Function under test populates round-trip
3. **Every production caller populates it** (the gap class)
4. CLI tool exposes it
5. GUI suite exposes it via cfg/UI

Items 3+ are the silent-failure-class. Includes a one-line grep
heuristic: `grep -rn "StructName\s\+[a-z_]\+\s*=" --include="*.hpp"
$REPO | grep -v tests/`. If the answer is "no production callers"
but the verifier reads the fields, the protection is silently
disabled.

**Out of scope (deferred to v5.9.5c+ if needed):**

- Bash CLI `tools/stamp_model.sh` doesn't yet expose all 9 cfg
  flags either. Production operator workflow is suite-side, so
  bash CLI parity is a smaller priority. Mechanical surface to
  add CLI flags + canonical body emit lines.
- `Backtest_RunFullValidation` doesn't currently persist a
  scaler sidecar — `feature_scaler_present + scaler_sha256` left
  unset for now. The Train Model worker DOES persist a scaler +
  log the SHA but doesn't auto-stamp. Wiring the suite-train-then-
  stamp-with-scaler path requires more design work (where does
  the scaler sidecar live across training runs?).

**Tests added (20):**
- Full round-trip with all 10 fields populated + matching values
- Gated-off bandit/fees → has_* fields stay 0 (forward-compat)
- 4× label_type → model_num_outputs mapping
  (LABEL_WIN_LOSS / LABEL_FORWARD_PNL / LABEL_REGIME / LABEL_PEAK_VALLEY_STABLE)

**Files touched:**
- `Backtest/BacktestEngine.hpp` — wire 10 inf fields from
  `data->config_used` + `label_type`, pass `&inf` to
  `stamp_write_for_model`
- `tests/controller_test.cpp` — 20 new EXTENSIBILITY tests
- `.claude/skills/parity-check/SKILL.md` — Section L
  (production-caller field-population audit)
- `DOCS/changelogs/2026-05-02-v5.9-ml-hardening.md` — this addendum

**Verification:**
- `./build.sh test gui suite` — clean
- 1275/0 tests pass (1255 + 20 v5.9.5b)
- Hot path UNTOUCHED
- Existing stamps continue to load cleanly (legacy `has_*=0` path
  unchanged)

### v5.9.5c — Bash CLI parity for full inference cfg binding

Caught when running `/readiness` on the v5.9.5c plan: Jenny asked
"why are we updating the CLI tool, like as a backup?" — fair
pushback. GUI is the primary path (Run Full Validation auto-stamps
with full inf via v5.9.5b). CLI is for manual workflows + the
test harness (v5.8.8 bash↔in-process round-trip pin).

But the CLI was missing 9 flags that the in-process emit had.
Asymmetric production paths = footgun for the test harness AND for
operators using the CLI for manual re-stamping (rare but real).

**Bug:** `tools/stamp_model.sh` was extended in v5.8.8 (engine_version,
feature_registry_hash) and v5.9.4a (model_num_outputs, scaler_*),
but the 9 v5.9.2b/v5.9.4 inference cfg flags were never added.
Result: bash-stamped models had no protection from cfg drift;
v5.8.8 round-trip test only verified 2 fields; signature parity
between bash + in-process was technically broken when in-process
emitted with non-empty inf.

**Fix:**
- Added 9 CLI flags: `--confidence-threshold-scale`,
  `--barrier-gate-enabled`, `--confidence-hard-block-threshold`,
  `--held-out-fraction`, `--freshness-tau`, `--bandit-blend-ratio`,
  `--fee-rate-maker`, `--fee-rate-taker`,
  `--training-poll-interval`.
- Canonical body emit lines inserted in the EXACT order matching
  in-process at `ML_Headers/ModelInference.hpp:1158-1210`. Order is
  load-bearing — HMAC verifies the entire body byte-for-byte;
  signatures diverge on any byte difference.
- Conditional emit semantics mirror in-process:
  - 5 core cfg fields (confidence_threshold_scale through
    freshness_tau) emit together when ANY is supplied (matches
    has_inference_cfg gate)
  - bandit_blend_ratio gated on its own (matches has_bandit gate;
    in-process gates on cfg.bandit_enabled=1)
  - fee_rate_maker + fee_rate_taker emit together (matches has_fees
    block; partial fees would diverge from in-process)
  - training_poll_interval emits independently
- Locale: existing `LC_NUMERIC=C` pin handles the new doubles via
  `awk 'printf "%g"'` — no new locale concerns.
- Test: extended v5.8.8 bash-parity block with 13 round-trip
  assertions (one per field + signature verify). All pass.
- Doc: `tools/stamp_model.sh` usage block expanded with new flags;
  `DOCS/changelogs/2026-05-02-v5.9-ml-hardening.md` references
  `DOCS/ML_TRAINING.md` for the operator workflow doc (was
  incorrectly referencing a non-existent `RETRAIN_RITUAL.md` —
  fixed in this ship).

**Why this matters beyond CLI parity:** the v5.8.8 round-trip test
is the tripwire that catches "bash + in-process diverge on the
canonical body byte sequence." Pre-v5.9.5c, that test only
exercised 2 fields. v5.9.5c expands coverage to all 14 stamp body
fields. Future stamp body extensions should add a v5.x.y bash
flag + extend the round-trip test in the same ship.

**Files touched:**
- `tools/stamp_model.sh` — 9 CLI args + 9 emit blocks + usage
- `tests/controller_test.cpp` — +14 EXTENSIBILITY tests (1275 → 1289)
- `DOCS/changelogs/2026-05-02-v5.9-ml-hardening.md` — this addendum
  + fixed v5.9.3c phantom RETRAIN_RITUAL.md reference (the doc
  content actually lives in ML_TRAINING.md)
- `Version.hpp` — 5.9.5b → 5.9.5c

**Verification:**
- `./build.sh test gui suite` — clean
- 1289/0 tests pass (1275 + 14 v5.9.5c)
- Hot path UNTOUCHED (CLI tool + tests only)
- Bash-signed stamp with all 14 fields verifies via in-process
  HMAC check (the load-bearing parity invariant)

### v5.9.5d — GUI stamp UX hardening (3 HIGH-severity fixes)

After a sprint-exit GUI gap audit, three operator-visible
gaps surfaced that defeated Jenny's "not relying on CLI as
backup" bar:

1. **Train Model: scaler SHA-256 hidden in stderr** — operator
   couldn't see the value needed to bind a scaler to a stamp via
   GUI; had to drop to a terminal.
2. **Train Model: no "next step" guidance** — status said "Model
   saved" but didn't tell operator to run Full Validation next to
   actually get a usable stamp.
3. **Verify Stamp: minimal output** — showed only "OK — engine=X
   registry=Y", omitting all the v5.9.2b/v5.9.4a/v5.9.5b cfg fields
   that were JUST stamped. Operator couldn't audit recorded vs
   runtime cfg without manually `cat`-ing the stamp file.

**Fix #1 — scaler SHA visible in GUI** (`BacktestPanels.hpp`):
- New `TrainingPanelState.scaler_sha256_hex[80]` field
- Worker copies the hex into the field after Persist + SHA compute,
  BEFORE setting `tm_complete=1` (worker→GUI ordering follows the
  v5.9.0c pattern: single-writer publishes via volatile flag; UI
  reads after observing flag flip; x86 aligned-byte writes are
  atomic at the primitive level)
- Training panel renders `scaler_sha256:` row with copyable
  `InputText(ReadOnly)` widget when populated; tooltip explains
  how to use the value (auto-bound by Run Full Validation;
  manually pass `--scaler-sha256=...` to stamp_model.sh).

**Fix #2 — Train Model "next step" hint** (one-line change):
- Status message now ends with `" — next: Run Full Validation to
  auto-stamp"`. Operator workflow continuation explicit.

**Fix #3 — Verify Stamp expandable details**:
- New `PastRun.stamp_verify_full` (`ModelStampResult`) +
  `stamp_verify_has_full` capture the full result on
  successful verify.
- New "Stamp details" `ImGui::TreeNode` renders below the OK/FAIL
  line. Shows everything the stamp recorded:
  - generalization gap + threshold
  - model_format_version + stamp_format_version
  - engine_version + feature_registry_hash
  - cross-major engine WARN if applicable
  - model_num_outputs (v5.9.4a, when stamped)
  - training_poll_interval (v5.9.4a, when stamped)
  - scaler binding + sha256 (v5.9.3a, when stamped — copyable hex)
  - inference cfg block (v5.9.2b: confidence_threshold_scale,
    barrier_gate_enabled, confidence_hard_block_threshold,
    held_out_fraction, freshness_tau)
  - bandit_blend_ratio (when stamped)
  - fee_rate_maker/taker (when stamped)

Operator can now visually audit "stamp says X, runtime cfg is Y"
without leaving the suite. Closes the v5.9.5b/c observability loop:
the in-process stamp emit was complete (v5.9.5b), the bash CLI
emit was complete (v5.9.5c), now the GUI display is complete too.

**NOT in scope (deferred to v5.10):**
- **Run Full Validation persisting + binding scaler** (the v5.9.5b
  scope-out): RFV trains a held-out model but doesn't currently
  call `FeatureStandardizer_Compute/Persist`. Architectural decision
  — should RFV duplicate Train Model's scaler-persist or pick up an
  existing sidecar from a prior Train Model run? v5.10 design.
- **Stamps Inspection panel** (table-view of all stamped models +
  fields). v5.10 Idea #14.
- **Past Runs stamp_ok column** + cfg-mismatch indicator. v5.10
  Idea #14b.
- **Engine boot WARN capture in foxml_suite** (cross-binary minor,
  cadence WARNs only in stderr today). v5.10 Idea #15.
- **ML Status panel stamp-recorded cfg row**. v5.10 Idea #16.

**Files touched:**
- `Backtest/BacktestPanels.hpp` — 3 fixes, ~110 line delta
- `DOCS/changelogs/2026-05-02-v5.9-ml-hardening.md` — this addendum
- `Version.hpp` — 5.9.5c → 5.9.5d

**Verification:**
- `./build.sh test gui suite` — clean (all 3 targets)
- 1289/0 tests (no regressions)
- foxml_suite smoke launch — no crash
- Hot path UNTOUCHED (suite GUI display work only)

**Operator validation steps** (for paper test):
1. Click Train Model in foxml_suite Training panel. Verify status
   shows "next: Run Full Validation" hint + scaler_sha256 row
   appears with copyable hex.
2. Click Run Full Validation with auto_stamp_on_held_out=1 +
   secret. Verify Past Runs shows the run with [stamped] prefix.
3. Click Verify Stamp on the stamped run. Verify "OK" line appears
   AND a "Stamp details" expandable shows below it with all
   recorded fields.
4. (For live test) Drop a stamped model into engine.cfg's per-core
   path. Boot engine_gui. Verify model loads cleanly + no
   cross-binary WARNs at boot.

### v5.9.5h — XGBoost hyperparameter ownership + build flag fingerprint + Past Runs stamp_ok column

Operator-facing operator control over XGBoost training hyperparams +
cross-build deploy detection + finer-grained Past Runs stamp status.
First sub-ship of master plan v5.9 → v5.10 sprint architecture.

**Items shipped (3 bundled):**

**#21 — XGBoost hyperparameter ownership** (~8h, the core work):
- NEW: `Backtest/XGBHyperparams.hpp` — single source of truth struct
  (8 fields: max_depth, learning_rate, n_estimators, subsample,
  colsample_bytree, min_child_weight, seed, tree_method) + factory
  + apply helper. Used by all 3 training sites (Train Model worker,
  WalkForward folds, HeldOut training). Pre-v5.9.5h hardcoded across
  the 3 sites with subtle divergences; now centralized.
- 5 new cfg fields in `ControllerConfig.hpp`: `xgb_subsample`,
  `xgb_colsample_bytree`, `xgb_min_child_weight`, `xgb_seed`,
  `xgb_tree_method`. max_depth/lr/n_estimators stay GUI-tunable
  only (per-experiment, not per-deploy). Defaults match pre-v5.9.5h
  hardcoded values bytewise — non-tuning operators get bytewise-
  identical training output.
- foxml_suite Train Model panel: new "Advanced (v5.9.5h)" collapsing
  header with sliders for subsample/colsample, InputInt for
  min_child_weight/seed (clamped), Combo for tree_method.
- engine_gui Settings panel: 5 new field_defs entries under "ML
  Hyperparams" section. Operator declares expected hyperparams;
  engine WARNs at boot if stamp differs.
- Stamp body extension (canonical position 17, Surface G `has_*`
  forward-compat): 8 hyperparam fields emit when has_xgb_hyperparams=1.
  Verifier parser uses macro-expanded else-if chain (PARSE_XGB_INT /
  PARSE_XGB_DOUBLE) for source clarity.
- ModelHandle extended with stamp_xgb_* fields + has_xgb_hyperparams
  flag; CoreModelZoo._TryLoadRole copies sr.xgb_* → handle
  post-Model_Load.
- EngineSharded boot-WARN per loaded role: compare stamp_xgb_* vs
  cfg.xgb_* per-field. WARN-only (hyperparams don't affect inference;
  forensics + reproducibility only). Same suppression flag as
  v5.9.4a poll_interval (`acknowledge_cross_binary_version_drift=1`).
- foxml_suite Verify Stamp tree extended with "XGBoost hyperparams at
  training time" group (renders 8 fields when has_xgb_hyperparams=1).
- Bash CLI `tools/stamp_model.sh` extended with 8 new flags
  (--xgb-max-depth, --xgb-learning-rate, etc.). Canonical body emit
  byte-matches in-process (HMAC parity preserved).
- 25+ new tests including bash↔in-process round-trip for all 8 fields.

**#10 — Build flags fingerprint** (~2h):
- NEW: `ML_Headers/BuildFlags.hpp` — constexpr FNV-1a hash over
  canonical flag string (`USE_NATIVE_128`, `USE_XGBOOST`, `__OPTIMIZE__`).
  `BUILD_FLAGS_HASH()` computed at compile time.
- Stamp body extension (canonical position 18): `build_flags_hash=<hex>`.
  Surface G `has_*` forward-compat. Stamps record training-build's hash.
- ModelHandle.stamp_build_flags_hash + has_build_flags_hash; populated
  in CoreModelZoo._TryLoadRole.
- EngineSharded boot-WARN: `[build_flags] WARN: core N role=X stamp
  claims build_flags_hash=A but current build is B (cross-build drift)`.
  Same suppression flag.
- Operator scenario: train on dev box (-O2, no NATIVE_128), deploy to
  prod (-O3, +NATIVE_128) → stamp's hash differs from build's →
  visible WARN. Forensics, not refusal.

**#19 — Past Runs stamp_ok column** (~30 min):
- New "Stamp" column on the Past Runs table (binary + multiclass
  variants both updated). 4-state rendering:
  - missing (no .stamp file): dim "—"
  - present, unverified: yellow "?"
  - verified OK: green "✓"
  - verified FAIL: red "✗"
- Tooltips guide operator to Verify Stamp button for full details.
- Reuses existing PastRun.stamp_verify_state field (v5.8.9); pure
  display-layer change.

**Operator validation steps:**

1. Train a model with default cfg → stamp records xgb_subsample=0.8,
   xgb_seed=42, etc.
2. Verify Stamp on the stamped run → "Stamp details" tree shows
   "XGBoost hyperparams at training time" group with all 8 values.
3. Edit `backtest.cfg`'s `xgb_subsample=0.5` → run another Train Model
   → new stamp records 0.5 (not 0.8).
4. Boot engine_gui with `cfg.xgb_subsample=0.8` and load the
   stamp-says-0.5 model → boot WARN fires:
   `[xgb_hyperparams] WARN: ... claims xgb_subsample=0.5 but cfg=0.8`
5. Past Runs panel shows green ✓ for stamped + verified runs;
   click any run's Verify Stamp button to populate the verification.

**Files touched (12):**
- NEW: `Backtest/XGBHyperparams.hpp` (factory + apply helper)
- NEW: `ML_Headers/BuildFlags.hpp` (constexpr hash)
- `Backtest/BacktestEngine.hpp` (WF + HeldOut sites use struct;
  RFV stamp populate)
- `Backtest/BacktestPanels.hpp` (Train Model worker + UI sliders +
  Verify Stamp tree extension + Past Runs stamp_ok column)
- `CoreFrameworks/ControllerConfig.hpp` (5 cfg fields + parser
  + defaults)
- `CoreFrameworks/EngineSharded.hpp` (boot-WARN for hyperparams +
  build_flags)
- `ML_Headers/ModelInference.hpp` (StampInferenceCfgInputs +
  ModelStampResult + ModelHandle extensions; stamp emit + parser)
- `ML_Headers/CoreModelZoo.hpp` (handle population from stamp)
- `GUI/SettingsPanel.hpp` (5 field_defs entries)
- `tools/stamp_model.sh` (9 new CLI flags incl. build-flags-hash)
- `engine.cfg.example` + `backtest.cfg` (5 new field documentation)

**Verification:**
- `./build.sh test gui suite` — clean (all 3 targets)
- 1289 → 1318 tests (+29 v5.9.5h EXTENSIBILITY block)
- Hot path UNTOUCHED (training is slow-path; stamp emit is init-time;
  cfg parse is boot-time)
- Bash↔in-process canonical body byte-identical (HMAC verify proof
  in tests)
- Forward-compat: legacy stamps (no xgb_* / no build_flags_hash)
  load with has_*=0 + no WARNs

**Out of scope (in v5.9.5i):**
- Inference cfg load-time enforcement (Idea #12) — comparison logic
  for the v5.9.2b 9 inference cfg fields
- ML Status panel recorded cfg row (Idea #16) — pairs with #12 since
  they share the comparison kernel
- Stamps Inspection panel (Idea #14) — full table audit view

These ship together as v5.9.5i.

## Sprint statistics

- **21 tags shipped** (v5.9.0 → v5.9.5d + 15 sub-letter ships)
- **+259 tests** (1030 → 1289, +25.1%)
- **All 27 V5_9_AUDIT findings closed** (no carryovers to v5.10)
- **Hot path UNTOUCHED** every ship — verified per-ship with
  `tools/calls_graph_diff.sh`
- **1 calendar day** (2026-05-02 dawn → 2026-05-02 evening)
- **2 NEW skills shipped alongside** (`/ml-audit` v5.9.0a;
  `/parity-check` v5.9.5)
- **`MODEL_FORMAT_VERSION` unchanged** at 5 — Surface G stamp body
  extension (`has_*` forward-compat) used everywhere instead

## Lessons learned

### 1. Surface G stamp body extension > format-version bumps

Phase 4 was originally planned as `MODEL_FORMAT_VERSION` 5→6 to add
the scaler. Mid-sprint we recognized: scaler is a sidecar file, not
a model file change. Stamp body's `has_scaler_*` forward-compat
flags handle it without breaking old stamps. Generalized: any new
parity-enforced field should be added via `has_<field>` flag, not
format bump. **Format bumps lose data; flag extensions don't.**

KDD #15 candidate (parity-tested-by-construction).

### 2. Display ↔ execution invariant rigor at multiclass boundaries

Bug C (hardcoded 0.52) was caught only because a paper test
happened to have a 3-class label that looked numerically similar
to a noisy binary. The display branch had been added before the
multiclass label landed — display path didn't know about K. Same
class of bug as v5.6.0's "every BG/SG term must have a GUI surface"
invariant; multiclass is the next dimension that needs the same
discipline.

Fix is single-source-of-truth helper (`multiclass_baseline_accuracy()`)
that consumes the actual class counts. Anywhere display code
references the baseline, route through the helper.

### 3. Silent-failure-class items aren't deferrable

The 4 carryover items were initially scoped for v5.10 ("non-blocking,
operator-workflow polish"). Operator pushed back: every one was
silent-failure-class (warmup log without strategy name = "did the
right strategy load?" requires log-correlation; model_num_outputs =
"does this model match my slot?" requires manual inspection of
stamp + cfg). All 4 ended up in v5.9.4a closure.

Lesson: silent-failure-class items deserve same-sprint closure even
when they're "small."

### 4. Per-surface readiness vs sprint-exit parity audit

`/readiness` runs against a plan; verifies "is this plan implementable
and correct?" 4 separate readiness checks during the sprint each
passed but missed the cross-cutting findings the parity audit found.

Lesson: pre-coding readiness ≠ sprint-exit parity audit. The
2026-05-02 parity walk caught 11 issues that 4 readiness checks
collectively missed because readiness verifies "this plan against
this surface" while parity audits walk "every surface against every
protection." `/parity-check` formalizes this for v5.10+.

### 5. Postmortem section is itself worth ~30 minutes

Writing this changelog forced a structural review of what shipped.
Caught two doc-only gaps (Phase 4 was actually MODEL_FORMAT_VERSION-
unchanged, not v6; sprint took 1 day not 1 week). The act of
synthesizing across 17 ship messages clarifies what the sprint
actually accomplished vs what the original plan said it would.
**Postmortem isn't ceremony — it's the audit pass that catches
plan-vs-actual drift.**

## v5.10 forward references

`DOCS/v5.10-design-notes.md` (created in v5.9.4a) lists 10
candidates. Sprint-close priority ordering:

1. **Operator's parallel hyperparameter search idea** (idea #1).
   4 cores train models with different barrier widths in parallel;
   ConfidenceScorer ensemble OR pick-best-validated post-walk-forward.
   Phased: single-best first, ensemble later.
2. **FOREACH_TARGET X-macro retrofit** (idea #2). Currently
   `Label_*` lacks the registry-hash protection that `Features_*`
   has. Same X-macro pattern would close that gap structurally.
3. **Drift detection / model retirement** (idea #3). Live
   distribution comparison (raw + scaled) with retirement triggers.

Items 4-10 are smaller but worth tracking. Whatever ships v5.10
should run `/parity-check` first.

## Skills shipped during the sprint

- **`/ml-audit`** (v5.9.0a) — systematic ML pipeline audit. 10-category
  full / 5-category parity / narrow function/file mode.
- **`/ship`** (v5.9.0a) — post-coding ritual: build verify, calls_graph_diff,
  version bump, commit, tag, push branch + tag.
- **`/parity-check`** (v5.9.5) — train↔serve identity audit (this ship).

## Verification gate (every v5.9 ship)

- `./build.sh test gui suite` — clean
- `./tools/calls_graph_diff.sh` — CLEAN throughout
- Hot path UNTOUCHED in every ship
- 1030 → 1289 tests, +259 across 20+ v5.9 EXTENSIBILITY blocks
- Branch + tag pushed to `origin` after each ship
- Pre-tag (`pre-vX.Y.Z`) created before each ship for rollback safety
