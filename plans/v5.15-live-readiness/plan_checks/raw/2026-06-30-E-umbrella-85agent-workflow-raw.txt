{
  "summary": "E-umbrella forward audit: enumerate what every remaining E leaf needs from the E.1.2 frozen layout, with the hot path as the priority lens — branch inventory, layout input-space, tech-debt cascade, realignment risks.",
  "agentCount": 85,
  "logs": [
    "Phase A: running the conformance analyzer + enumerating the hot-path data-dependent-warm branches",
    "Phase A done: 13 data-dependent-warm branches inventoried",
    "Phase B: per-leaf I->A across 6 leaves (5:5, 7:7 on E.1.3 + E.1.5)",
    "Phase C: synthesizing 6 per-leaf verdicts into the frozen-layout set + branch roadmap"
  ],
  "result": {
    "hotPathBaseline": {
      "summary": "Consolidated H7/H20 jitter inventory across 3 investigations (all HEAD 0ee227a, check_latency_path_conformance.py @ production -O3 -march=native, no LATENCY_PROFILING). HOT PATH — ExecutionCore_Tick (2097 instr, 25 data-dependent-warm, allowlisted=0): this is the fused BG+SG+entry body (BG/SG inlined from GateParameters.hpp into ExecutionCore_Tick_Impl; PositionExitGate Portfolio.hpp:478 is legacy/centralized, NOT on the gated sharded path). The \"25 warm → drive to 0\" (D-235) decomposes as 1 genuine steady-state removable + 7 guarded-rare needing allowlist/tool reconciliation + 12 tool-flattening artifacts (cold-nested under the line-504 __builtin_expect push gate, which flat-asm analysis can't model). Only ExecutionCore.hpp:337-338 (active?live:cached TP/SL) executes every steady tick unguarded — and its \"CMOV-style, both loads parallel, verified in bench\" comment is STALE: Money is 16B/__int128, x86 has no 128-bit cmov, GCC emitted a real `je`; the bench cited predates the Ship-B 16B-Money epoch (likely measured the old 8/24B FPN type where cmov WAS emitted). Drop-in fix Money_BlendOnMask exists (FixedPointN.hpp:1876). Position AoS→SoA relayout is provably hot-path-inert (hot gate reads ExecutionCore<F>, never Portfolio.positions[]); steady working set is ~5 cache lines (L0 + cached_seq + 3×cached_params@offset704), not the \"single line 0\" the stale 24B-Money comments (ExecutionCore.hpp:64-82) claim. SLOW PATH — RollingStats_Push (3598 instr, 18 warm, 14 spills — only path with spills): the real H11 constant-iter violation is the 3 monotonic-deque back-pop while-loops (RollingStats.hpp:325/334/343, classified loop not warm — back-edges), worst-case O(W)/Push; primary D-229 leaf candidate. Regime_Classify (489 instr, 8 warm): classify if/else-if cascade (RegimeDetector.hpp:593-605) is a discrete 4-way LUT candidate (session-mult LUT precedent exists); RegimeDetector.hpp:571 `(int)FPN_ToDouble(regime_model_weight)` is an analyzer-exempt FPN→double→int crossing worth an H4 refute. EventLoop_RebuildOneCore (~5-30µs orchestrator) is UNMETERED — absent from the 7-probe budget set; its gate cascade (:2760-3081) has zero static branch classification (coverage hole). Budgets JSON is NOT stale vs E.1.1. Cross-investigation A-class refute priorities: (1) verify 337/338 je maps to the ternary not loop/guard bleed + Money_BlendOnMask is bytewise-identical+truly branchless + quantify the both-loads load-port cost vs ~100%-predicted branch (MOOT-UNREACHABLE risk); (2) tool-classification gap — why 4 explicit __builtin_expect(,0) at 315/454/504 land WARM not rare-cold (determines whether D-235 means code edits or allowlist rows); (3) confirm Tier-C sites sit on the taken-side of 0x2d1/0x5b1 so they're excluded from steady jitter.",
      "branches": [
        {
          "location": "CoreFrameworks/ExecutionCore.hpp:337-338 (asm 0x7a, 0x3e0)",
          "classification": "HOT · TIER-A steady-state · every-tick unguarded · the ONE real H7/H20 violation",
          "why": "`Money tp = active ? core->live_tp : core->cached_params.sg_take_profit_price;` (and sl :338). Ternary on runtime `active` flag, runs every tick with no __builtin_expect. Code comment (:334-336) claims CMOV-style parallel loads 'verified in bench' but asm emits `je` in BOTH instantiations — 16B __int128 has no x86 128-bit cmov, compiler branched. Bench claim predates Ship-B 16B-Money epoch (likely measured 8/24B FPN where cmov was real).",
          "removable": "YES — drop-in: replace ternary with Money_BlendOnMask(live, cached, -(uint64_t)active) (FixedPointN.hpp:1876, AND/OR mask-blend, same shape already used for the live-<64> mask-select). Refute first: confirm BlendOnMask lowers branchless+saturates identically, and quantify both-16B-loads load-port cost vs the ~100%-predicted branch (active flips only on entry/exit). Latency win small but it IS the live violation + stale comment."
        },
        {
          "location": "CoreFrameworks/ExecutionCore.hpp:315 (asm 0x59,0x61,0x3bf,0x3c7)",
          "classification": "HOT · TIER-B guarded-rare · seqlock cached-param miss",
          "why": "`if (__builtin_expect(s_now != core->cached_seq || (s_now & 1ULL), 0))`. The `||` → 2 branches × 2 instantiations = 4 hits. Genuinely ~1/256 ticks. Tool put these in WARM despite the explicit cold hint (the `||` second compare confuses the rare-cold heuristic).",
          "removable": "NO code change — disposition is allowlist row ('warmup-100%-predicted' per the tool's own prescription) OR fix the tool's rare-cold heuristic gap. This pair is the canonical tool-classification refute (#2)."
        },
        {
          "location": "CoreFrameworks/ExecutionCore.hpp:454 (asm 0x55f)",
          "classification": "HOT · TIER-B guarded-rare · leg-B SG (non-PAIR_BRANCHLESS instantiation only)",
          "why": "`if (__builtin_expect(active_b, 0))`. Predicted-not-taken by design; only 1 hit because the PAIR_BRANCHLESS=true instantiation already replaces this with mask-blend (:443-452).",
          "removable": "NO — the branchless variant already exists for the other instantiation; this is cold-by-intent. Allowlist."
        },
        {
          "location": "CoreFrameworks/ExecutionCore.hpp:504 (asm 0x2d1,0x5b1)",
          "classification": "HOT · TIER-B guarded-rare · event-push gate (the enclosing guard for all of Tier-C)",
          "why": "`if (__builtin_expect(can_enter | can_exit_a | can_exit_b, 0))`. The canonical rare-branch; predicted not-taken; only retires when a trade actually fires.",
          "removable": "NO — cold-by-intent. Load-bearing for the inventory: everything in Tier-C is nested under this branch's taken-side, so the flat analyzer over-counts them as steady warm."
        },
        {
          "location": "CoreFrameworks/ExecutionCore.hpp:657 (asm 0x4c)",
          "classification": "HOT · TIER-B · cfg-flag dispatch (pair_branchless wrapper)",
          "why": "`if (pair_branchless)` — runtime branch on `cached_params.flags & GATE_FLAG_PAIR_ACTIVE`; cfg changes ~1/256 ticks → near-perfectly predicted (documented :629-642).",
          "removable": "NO — legitimate H20 exception (genuine cfg-flag dispatch per branchless-dispatch-discipline.md decision matrix)."
        },
        {
          "location": "CoreFrameworks/ExecutionCore.hpp:506,516 (asm 0x743,0x903,0x74c,0x90c,0xaf8,0xbc0)",
          "classification": "HOT · TIER-C cold-nested (6 hits) · executes only on a tick that fires an exit",
          "why": "`if (can_exit_a)` (:506) / `if (can_exit_b)` (:516). Sit on the taken-side of the line-504 __builtin_expect(,0) push gate — unreachable unless a trade exits.",
          "removable": "NO — tool-flattening artifact. Not p99 steady-state jitter; exclude from the count (refute #3: confirm via asm they're on the line-504 taken side)."
        },
        {
          "location": "CoreFrameworks/ExecutionCore.hpp:542,548,557 (asm 0x81a,0x9e4,0x84c,0xa16,0x862,0xa2c)",
          "classification": "HOT · TIER-C cold-nested (6 hits) · entry-fire only",
          "why": "`!Money_IsZero(tp_pct)` (:542) / `!Money_IsZero(sl_pct)` (:548) / `pair_active` leg-B entry (:557). TP/SL recompute paths, reached only on a fired-entry tick (nested under line-504).",
          "removable": "NO — tool-flattening artifact; cold-nested, not steady jitter."
        },
        {
          "location": "FixedPoint/FixedPointN.hpp:1633 (asm 0x1152,0x1494,0x18fe)",
          "classification": "HOT · TIER-C cold-nested (3 hits) · entry-fire only",
          "why": "`jae` in Money_Add overflow-saturate carry-compare (s > MONEY_MAX). Reached only from entry-path live_tp/live_sl = Money_Add(tick.price, Money_Mul(...)) (:543/549/570). 128-bit __int128 saturate is structurally non-vectorizable (AVX-512 has no 128-bit-lane compare → 2×64 cmp+sbb).",
          "removable": "NO — cold/rare (entry only), and the 128-bit saturate compare is inherent to the Money encoding. Not steady jitter."
        },
        {
          "location": "ML_Headers/RollingStats.hpp:325,334,343 (loop back-edges; classified loop=13, NOT in warm list)",
          "classification": "SLOW · H11 constant-iter VIOLATION · the structural D-229 leaf · runs every slow cadence unconditionally",
          "why": "Three amortized-O(1) monotonic-deque back-pop while-loops (min :325-332 FPN_GreaterThan, max :334-341 FPN_LessThan, vmax :343-350 FPN_LessThan). Data-dependent iteration count, worst-case O(W)/Push. The body is otherwise branchless-by-mask; these while-loops are the residual H11 non-conformance the comment (:322-324) acknowledges.",
          "removable": "PARTIAL — PRIMARY D-229 leaf candidate (only true H11 violation in probe set, has the 14-spill register-pressure signal, clean golden-master-diffable kernel). Novel-alt: fixed-K unrolled mask-blend pop (changes amortized-O(1)→fixed-O(K), more work in common single-pop case). REFUTE-worthy: needs PMU evidence the while-jitter costs more than unconditional K-work, and whether the 14 spills (not the branches) are the real p99 driver."
        },
        {
          "location": "ML_Headers/RollingStats.hpp:232,343,367 + FixedPointN.hpp:1321,1339,1364,1683 (asm 9f,b0,c1,1b98,1ec1,...)",
          "classification": "SLOW · 18 data-dependent-warm (RollingStats_Push, 3598 instr, 14 spills)",
          "why": ":232 deque-update guards (×3), :343 vmax back-pop guard, :367 volume_delta mask path; 11 of the 18 are FPN compare/mul saturation branches inside FPN_Mul/FPN_SubSat/FPN_AddSat (FixedPointN.hpp:1321/1339/1364/1683 — shared with the hot path).",
          "removable": "MOSTLY NO — the FPN-internal saturation branches are shared kernel deps; the deque-update guards collapse if the :325/334/343 while-loops are restructured. Register-pressure (14 spills) may dominate p99 over the branches — refute the branch-centric framing."
        },
        {
          "location": "Strategies/RegimeDetector.hpp:593-605 (asm 0x3cc,0x3d5 + :594,:602)",
          "classification": "SLOW · Regime_Classify discrete dispatch cascade (489 instr, 8 warm, 0 spills)",
          "why": "The classify if/else-if/else cascade: :593 (trending_score>=2 && has_crossover && consistent && trending>volatile) → nested :594 down>up; :602 else-if volatile_score>=2 arm; :620-621 hysteresis commit. :600 strong-vs-mild already a ternary (cmov). H20 generalizes branched dispatch.",
          "removable": "YES (secondary) — genuine 4-way discrete dispatch → LUT/score-table viable (mirrors the SESSION_BY_HOUR[24] precedent at ControllerEventLoop.hpp:2327-2335 / TECH_DEBT-040). Smaller win than R1 (8 branches, no spills). Fold the :571 H4 question first."
        },
        {
          "location": "Strategies/RegimeDetector.hpp:569,571 (asm — :569 ML-enrichment branch)",
          "classification": "SLOW · ML-mode dispatch (H20 candidate) + H4-exempt float crossing (adversarial-worth)",
          "why": ":569 `if (!FPN_IsZero(sig->model_score))` diverges ML vs non-ML cores (model-mode-dependent). :571 `int weight = (int)FPN_ToDouble(cfg->regime_model_weight)` — FPN_Binary→double→int crossing on the slow path, on the model_score!=0 arm; this is the analyzer's forbidden=1/float=18 EXEMPT (allow-listed) source.",
          "removable": "REFUTE-GATED — challenge the H4 exemption: is :571 a legitimate display/heuristic-only weight, or a real feature-path float crossing that should go through a named Money_*/integer-LUT seam? (getenv+fprintf debug block :630-659 is the correctly-gated second exempt source.)"
        },
        {
          "location": "CoreFrameworks/ControllerEventLoop.hpp:2760-3081 (EventLoop_RebuildOneCore, ~5-30µs orchestrator)",
          "classification": "SLOW · UNMETERED coverage hole · gate-compute cascade (heaviest branch cluster)",
          "why": "NOT in the 7-probe budget set — zero static branch classification today. Source-grep maps the cascade: staleness :2760, recovery :2770/2774, book-imbalance :2819, alloc :2841, MTM-kill :2884-2923, SL-cooldown :2956, volume-sig :2966/2973, spacing :2997, vwap-offset :3003/3010, slope :3016/3023, buy-delta :3028/3034, stddev :3039/3045, TP-floor :3073-3081. Plus 2 data-dependent bitmap while-loops (:2524-2532 SL-tighten ≤2 slots; :2887 MTM sweep) and strategy-dispatch branches (:2476 STRATEGY_AUTO, :2571 STRATEGY_ML, :2440 regime gate).",
          "removable": "DEFER as a D-229 leaf — it's the orchestrator not a kernel; per-branch cfg-presence guards are mostly __builtin_expect-rare (flag off); writes pending_params SSoT (large blast radius); not a clean golden-master unit. But A-class should push on the conformance-gate coverage hole (largest slow consumer, no metering). Session-mult LUT (:2327-2335) is the in-function branchless precedent for any transition-frequent branch."
        }
      ]
    },
    "perLeaf": [
      {
        "leaf": "E.1.3 — aggregator + cross-thread coherence (SPINE-2, capital cluster). NOTE: the task's leaf identity matches E-dependency-graph.md:125-140 (E.1.3 ★ = aggregator + torn-read + absorption + conc-5, SPINE-2), but decision-log:1435 (DECIDED 2026-06-15) maps the aggregator+torn-read to E.1.4 — unreconciled artifact drift (see openQuestions).",
        "verdict": "YELLOW — layout-readiness conditional. The dominant directive (Money-not-FPN for the slow_account/drainer_state/AggregatorState money cluster) is sound, load-bearing, and negative-cost, and the only genuine wire freeze (Position + owner_node_id) has a clear path. But the freeze cannot go GREEN: (1) the cluster-dimension cap SSoT constants (NUM_EXCHANGES / MAX_NODES_PER_CLUSTER / MAX_CLUSTERS) do NOT exist in code, so owner_node_id width and every cluster-sized array literally cannot be frozen yet (Refute 6 N1, HARD); (2) the E.1.3-vs-E.1.4 seam-map drift + the drift==0 OUTBOUND-under-INERT-fill contradiction question what this leaf even commits; (3) several layout-shaping operator forks remain open (D-206 Position pre-reserve, balance home, kill-eval scope). NOT RED: the runtime clusters (NodeState/ClusterState/AggregatorState — confirmed 0 code hits, not wire-serialized) grow at their consuming leaf for a recompile + size-budget-tool cost with NO version bump (Refute 5), so getting their interior wrong is a design-discipline re-touch, not a wire break — which de-risks everything except the Position wire decision and the undefined cap symbols.",
        "frozenLayoutRequirements": [
          "MONEY TYPING (survives all 7 lenses; THE load-bearing requirement): slow_account / drainer_state / AggregatorState money fields freeze as Money (FixedPoint<10,8>, 16B), NOT FPN<F> (24B). Foundation sketch still encodes the DEAD D-214-F1 24B-split-FPN premise (E.1-foundation.md:432-436, :677, :695-697). Justification is H4 + persist-scale-match, NOT cmpxchg16b: FPN binary-scale (2^F) vs Money decimal-scale (10^8) mismatch against the Money-typed persisted bytes (OmsFieldRegistry.hpp:246) + Money-typed kill reader = a D-110 warm-restart catastrophe. Negative-cost (16B<24B → cluster shrinks). Live code already Money (OrderManager.hpp:309-310).",
          "PER-NODE account in slow_account = ONE realized_pnl + ONE open_notional + ONE running peak + allocated_balance (all Money) — relocations of the EXISTING per-node node_realized (ControllerEventLoop.hpp:411), node_open_notional (:452), node_peak_balance (:467). NOT triple-stored. The running peak is the only path-dependent irreducible. drawdown_max / drawdown_current are DROPPED (see realignRisks: zero consumers / derivable).",
          "SEQLOCK pack: account_state_seqlock as FIRST field of slow_account, counter-first ordering, whole cluster alignas(64) (H6), reader double-read-with-parity per cross-thread-multiword-read-consistency-discipline.md. NEW determinism requirement (Refute 4 N1): the seqlock counter + struct _padding MUST be SKIP_PERSIST in the field-projected FOREACH_OMS_FIELD persist walk — else the runtime-varying counter leaks into the wire and breaks warm-restart byte-stability (H9/H12).",
          "owner_node_id on Position (D-209) — the GENUINE wire freeze (Position IS serialized, ShardedSnapshotPersist.hpp:169/187; opens ONCE per D-55 SoA + D-206 coordination). BLOCKED on cap-symbol SSoT (see realignRisks): width must encode global node space but MAX_NODES_PER_CLUSTER/NUM_EXCHANGES/MAX_CLUSTERS don't exist in code. MUST stamp at ALL create-sites incl. bypass paths (time-exit Submit ControllerEventLoop.hpp:3314-3315, Portfolio_OpenSlot/Add Portfolio.hpp:262/273, partial-leg-B Async.hpp:869) — default-0 persists and drifts warm-restart attribution (Refute 4 N3).",
          "PER-NODE kill latch + trips counter (Refute 2 N1, NEW): the LIVE per-node MtM kill is a SLOW-thread-owned latch — NODE_STATE_FLAG_SET(KILL_TRIPPED) (ControllerEventLoop.hpp:2925) + node_ks_trips_total (uint32_t, :473). The kill_flags sketch (foundation:465-469) has neither and its 3 atomics are documented 'aggregator/operator sets' = the WRONG WRITER. Freeze must add a slow-owned latch + trips counter OR explicitly relocate the kill DECISION to the aggregator (a semantic scope change to flag).",
          "D-274 pre-sized per-node strategy-state UNION (re-homed to E.1.2 per D-274 STATUS): the NodeState relayout owns the strategy_state buffer LAYOUT — a union sized to max(StrategyState) + a strategy_state_kind guard on the build dispatcher (closes the SimpleDip->Momentum 384B OOB overread).",
          "A22 intended_* torn-read surface (D-201) — the cross-thread read is REAL (drainer reads nodes[slot].intended_qty Async.hpp:842, intended_tp :868, intended_sl :900). SHAPE is OPEN (dedicated seqlock pack vs TradeEvent-payload carry vs reuse param seqlock — see openQuestions). Because NodeState is runtime-only, the freeze-obligation is design-discipline (leaf:67 design-once), not a wire break.",
          "ClusterState.nodes[MAX_NODES_PER_CLUSTER] + node_count + cluster_kill_flag (foundation:655,658-659) for the aggregator's integrity-walk — present in sketch; confirm at freeze (sizes gated on the cap-symbol SSoT above)."
        ],
        "branchReductions": [
          {
            "branch": "TP/SL ternary active ? live_tp : cached_params.sg_take_profit_price (ExecutionCore.hpp:337-338, emits je) -> Money_BlendOnMask (FixedPointN.hpp:1876)",
            "freeNowInE12": true,
            "approach": "It is an E.1.2 relayout-epoch / Money-blend item, SCOPE-FENCED OUT of E.1.3 (F4 survives — leaking it muddies the coherence-bug rollback isolation). But Refute 3 R1 corrects the framing: this is a CONFORMANCE edit (H7/H20), NOT a latency win — 'active' is written only inside the __builtin_expect(...,0) push block so the je is ~100% predicted/~0 variance; BlendOnMask forces unconditional 2x 16B loads + per-field mask/AND/OR every tick. Do it for H7/H20-meter conformance + stale-comment deletion (the :334-336 'CMOV verified in bench' is stale — Money is 16B __int128, no x86 128-bit cmov) and PMU-check for a steady-state p99 REGRESSION; do NOT advertise as a speedup."
          },
          {
            "branch": "kill -> hot path via the existing branchless permission atomic (ExecutionCore.hpp:474 acquire-load, :476 can_enter mask-fold)",
            "freeNowInE12": false,
            "approach": "No branch added or removed — E.1.3 changes ONLY the writer side (relocates EventLoop_KillSwitchEvaluate to the drainer thread + swaps its INPUT oms->balance -> seqlock snapshot). LOAD-BEARING OUTBOUND INVARIANT E.1.3 must commit (currently unstated, Refute F3): kill/halt reaches the hot path ONLY via the existing branchless permission atomic; NO new hot-path read of ClusterState/AggregatorState/kill_flags; the kill-flag structure is slow/drain-thread-only. permission is cache-line-isolated (ExecutionCore.hpp:100, static_assert :178)."
          },
          {
            "branch": "collapse cluster-halt into the single can_enter reduction mask (F6)",
            "freeNowInE12": false,
            "approach": "REJECTED for E.1.3. Distinct semantics -> SSoT coupling; re-touches the hot path inside the highest-risk coherence ship. can_enter already AND-folds permission (:476) + GATE_FLAG_BUY_BLOCKED veto (:366-368) as branchless ANDs — there is no branch to remove (~1-cycle mask, below H8 noise floor)."
          },
          {
            "branch": "trade-decision-tick event-ring push branchless (D-231 Task #15) + param-update seqlock-miss p99 spike (ExecutionCore.hpp:309-321)",
            "freeNowInE12": false,
            "approach": "NOT E.1.3 scope — this is the OPTIMIZATION thread, homed to the H8 bench (measure) + the optimization leaf (fix) per D-231/D-230. Do NOT smuggle into the coherence ship; E.1.3 is hot-path-branch INERT (grep of ExecutionCore/GateParameters/RegimeDetector for the new structs = EMPTY)."
          }
        ],
        "techDebt": [
          "TD-129 (per-core drainer architecture, Option C full) — STRONG ~0-marginal; this leaf IS TD-129 (open.md:2640): hoist EngineSharded_Async_DrainWithSubmit (Async.hpp:785) + OrderManager_Tick + phased-drain buckets into the per-node slow path; central-drain path goes.",
          "TD-002 (centralized ControllerEventLoop/drainer removal) — DRAINER HALF only, partial-by-design; single_core sub-deletion landed E.1.1. Verify the frontmatter does NOT claim full centralized-path removal (that is its own LOW ship).",
          "TD-170 (A3/A7/A8 FlattenAll robustness) — STRONG near-0 at the reshard; A3 site LIVE + drifted to ControllerEventLoop.hpp:3454-3455 (ledger cites :3443). A3/A7/A8 all sit in the resharded EventLoop_FlattenAll surface.",
          "TD-197 / torn-read class — THE headline structural close: EventLoop_KillSwitchEvaluate reads racing oms->balance off the producer thread while the drainer writes it (16B Money tears); KNOWN-RACE TODO live at Async.hpp:449-464. Closed by reading the seqlock-published Money snapshot. PLUS A22/D-201 = the 2nd torn-read surface. F-2-AMEND makes .E.1 a HARD LIVE-ENABLE GATE on this class. CAVEAT: confirm the torn-read closure is E.1.3 (dependency-graph) not E.1.4 (stale decision-log:1435) before claiming the close.",
          "TD-220 (two dead SAFETY_CRITICAL kill_switch_* cfg fields; Class-47 split-brain) — STRONG dead-consumer removal. PRECISION CHECK: the consumers are in a SEPARATE file PortfolioController.hpp (:959/968/1592/1612) — confirm E.1.3 actually deletes them, else the dead read survives. Coordinate with D-273 (sharded kill is DRAWDOWN-ONLY; the session-relative daily-loss kill is its OWN ship, Task #13, STATUS=WIRE).",
          "Class-26 (cross-node iteration) / Class-27 (scalar cfg-mirror) / A1 (warm-restart global take_profit_pct) — H22 violation instances this leaf de-flattens. global_kill_mirror / per_cluster_kill_mirror reads MUST be annotated SANCTIONED H22 EXCEPTION (safety override, single-writer per D-193) so the candidate H22 CI check does not mis-fix them.",
          "REJECTED (drop from subsumption): TD-196 (DISK-persist wire-desync class — runtime seqlock never touches fwrite/fread; its own trigger names E.1.2 relayout, mis-homed); TD-171/A5 (rides the .E.1 adapter D-123 / ExchangeAdapter.hpp:43, NOT the coherence spine — A6 landed 3ee95dc, A5 still OPEN; frontmatter listing TD-171 closed is an over-claim unless A5 is carved out)."
        ],
        "realignRisks": [
          "WORST mis-freeze (the surviving CRITICAL): FPN<F> (24B) instead of Money (16B) for the slow_account/drainer_state/AggregatorState money cluster — D-110 warm-restart catastrophe (binary-scale persisted bytes read by a decimal-scale Money kill reader). Fix is NEGATIVE-cost. The cmpxchg16b/16B-align rationale is DROPPED (Refutes 2/3): a non-sequitur under the single-writer-seqlock model (seqlock uses plain stores + a counter, no 128-bit CAS, no 16-byte-align requirement); the real reasons are H4 + snapshot-byte-match.",
          "balance/ks_peak_balance home (L1 REJECTED as stated, Refutes 2/6): these are DEPLOYMENT-GLOBAL singletons (OrderManager.hpp:309/502) — folding them into a PER-NODE slow_account fails the H22 (N+1) test (node N's published state would change on node M's fill) and replicates a Class-27 mirror. The per-node pack carries the per-node account; the global the global-kill reads is a DERIVED sum homed in AggregatorState (or the per-node MtM kill uses per-node fields, which it already does at ControllerEventLoop.hpp:2884-2933). Do NOT fold global money into the per-node pack.",
          "Cap-symbol SSoT UNDEFINED (Refute 6 N1, HIGH — a hard pre-freeze blocker for the wire-bound part): NUM_EXCHANGES / MAX_NODES_PER_CLUSTER / MAX_CLUSTERS exist ONLY in the foundation sketch — Limits.hpp defines only MAX_EXECUTION_NODES=16 and MAX_PORTFOLIO_POSITIONS=16. owner_node_id width AND every cluster-sized array (ClusterState.nodes[], per_cluster[]) size on vapor. Define the cap constants (with MAX_NODES_PER_CLUSTER<=256 ∧ NUM_EXCHANGES<=256 to make uint16_t (cluster<<8|node) correct) at the freeze or these stay unfrozen.",
          "Per-node MtM kill reads cross-thread positions[] (Refute 2 N4 + Refute 6 F-P2): the live per-node kill recomputes unrealized from oms->portfolio.positions[] + px (ControllerEventLoop.hpp:2884-2892) — a seqlock-published slow_account does NOT close that read, and open_notional (entry-value) is NOT MtM unrealized. positions[] is NOT relocated in the foundation (orders go to drainer_state.orders :448; positions stay shared). active_bitmap is uint16_t capping 8 nodes (Portfolio.hpp:152,156). Decide per-NodeState-owned positions[] (pure/H22) vs shared MAX-array at freeze.",
          "capital_allocated value-coupling (Refute 6 N2): default_per_node = (total_balance*default_risk)/num_nodes (Async.hpp:363) — adding the (N+1)th node re-divides and changes EVERY existing node's allocated_balance (and thus its peak/drawdown kill base, ControllerEventLoop.hpp:2898-2906) UNLESS an explicit per-node risk_pct is set. The 'per-node-pure' label for capital_allocated is FALSE on the default allocator. Layout placement is fine; the coupling is an allocator-semantics flag, not a layout-freeze blocker.",
          "Drainer budget axis (Refute 3 N1): the relocated kill-eval (TODO Async.hpp:463 'move to drainer thread') lands on the DRAINER, whose budget is p99 <=10us, NOT the <=100us slow-rebuild budget the leaf cites. Meter the relocated eval + its seqlock double-read at 10us and add it to the conformance probe set (EventLoop_RebuildOneCore is already an unmetered hole).",
          "Leg-split conservation FORM (Refute 4 N2): the residual-subtract form legA=Money_Mul(intended,pct); legB=Money_Sub(intended,legA) is MANDATORY — two independent Money_Mul half-even roundings need not sum to intended, so a naive translation fires the conservation assert / leaks a sub-satoshi. This rides E.1.2's in-place type-fix at Async.hpp:842-851 (TD-167), not just E.1.3's assert.",
          "drawdown_max DROPPED (Refutes 1/2 N2: zero consumers anywhere) + drawdown_current DROPPED (derivable on-the-fly: (peak-balance)/peak, the live form at ControllerEventLoop.hpp:3261-3266). Freezing two reader-less Money fields violates NECESSITY; keep only the running peak.",
          "DROPPED from realignRisks (refuted as freeze blockers): the writer-model FORK (AggregatorState is E.1.3/E.1.4-created, 0 layout coupling; F-2-AMEND/D-230 settle the cross-thread surface as single-writer-seqlock; the D-54 per-fill multi-writer push is unimplementable-as-written on 16B Money -> reconcile at E.1.4, not the E.1.2 freeze); session_start_equity/daily-reset named reserve (D-273 is WIRE but its OWN ship Task #13 with an undrawn field set — keep the generic slow_account._padding1[24]); the drainer H8 histogram NodeState reserve (already the inline global g_engine_drainer_cycle_hist, Async.hpp:12/92/106); Order_BindPreResolved fee-rate scratch + slot>>partial_on storage (already on Order::pre_resolved / a pure function)."
        ],
        "openQuestions": [
          "D1 (the highlighted call) — Is Position FINAL at E.1.2, i.e. pre-reserve the D-206 192B peak field now? Reframe (Refute 7): the +64B is hot-path-INERT (peak lands in a cold 3rd cache line; the hot gate reads tp@0/sl@16 in cache-line 0), so footprint is NOT the objection. The genuine cost is the persist/warm-restart A1 trap — reserving a PERSISTED peak that no E.1.2/E.1.3 code initializes means warm-restart reads garbage (D-206's own #1 risk, ShardedSnapshotPersist.hpp:591-687). D-209 'Position opens ONCE' + D-55 SoA argue reserve-now; D-206 is 'pending operator sequencing' + HIGH-RISK + not leaf-scheduled. DECISION: reserve AND initialize the peak persist round-trip in the same freeze, OR keep Position 128B and accept a 2nd forced Position-open when D-206's exit-redesign ships. owner_node_id (D-209) lands at E.1.2 regardless.",
          "Seam-map drift (HIGH): the dependency-graph (E-dependency-graph.md:125-140) places aggregator + torn-read + absorption at E.1.3 ★ (SPINE-2, the task framing), but decision-log:1435 (DECIDED 2026-06-15) maps E.1.3=drainer-absorption, E.1.4=aggregator+torn-read+kill-switch-hierarchy. Which is canonical? Resolve + re-ground the stale artifact, and pin the {aggregator INFRA + seqlock publish = E.1.3} vs {per-fill push MATH = E.1.4} split that both seams imply.",
          "drift==0 OUTBOUND is unvalidatable under E.1.3's own residual-HOLE contract (S7): E.1.3 builds the seqlock-publish infra but the per-fill push that exercises drift==0 lives in the fill handler that the contract keeps INERT until E.1.4. Either (a) E.1.3 wires the push as PLUMBING (the contract must explicitly bless this plumbing-not-math edit) or (b) re-state drift==0 as E.1.4's OUTBOUND.",
          "Balance home + reconstruct-vs-store: where is the global balance/ks_peak the GLOBAL kill reads — AggregatorState derived-sum vs an explicit stored field? And is per-node balance reconstructed (start + realized) or stored explicitly? The refutations genuinely split: Refute 1 (derive from start+realized), Refute 4 (keep explicit — the balance==start+realized identity is incidental not contractual, a future fee-only/top-up adjustment drifts the reconstruction), Refute 6 (derived sum in AggregatorState). Settle the SSoT.",
          "Kill-eval SCOPE (H22): the current EventLoop_ClearAllPermissions clears EVERY core (a deployment-aggregate kill — adding the (N+1)th node changes WHEN existing nodes halt). Does the relocated eval read a node-local slow_account snapshot or the global aggregate? per_node_kill is produced by NO code today (vestigial/H21 dead-slot risk if only the global path is wired). The operator may WANT a deployment-wide circuit-breaker as the live-gate semantics — name the intent so the frozen data-dependency is correct.",
          "Define the cluster-dimension cap SSoT constants (NUM_EXCHANGES / MAX_NODES_PER_CLUSTER / MAX_CLUSTERS) at the freeze — owner_node_id width + every cluster-sized array depend on them and they do not yet exist in code.",
          "kill_flags shape FORK (F3): keep 3 separate std::atomic<uint8_t> (writer-independent, safe, no lost-update) vs a single MBS_*/manual shift-mask bit-pack (H14) that forces disjoint-bit fetch_or/fetch_and RMW. Recommend 3 separate atomics OR commit F3 with an enforced disjoint-bit-RMW-only guard. Compounded: the sketch's atomics have the WRONG WRITER and omit the slow-owned per-node KILL_TRIPPED latch + node_ks_trips_total (must be added or the kill decision explicitly moved to the aggregator). Keep the kill_flags line alignas(64)-isolated, rare-write/frequent-read.",
          "positions[] ownership: per-NodeState-owned positions[] (pure, horizontally scalable, closes the per-node MtM kill's cross-thread read) vs the current shared MAX_PORTFOLIO_POSITIONS array sliced by Sharded_LegSlot. Bears directly on whether the seqlock slow_account actually makes the per-node kill input coherent.",
          "TD-220/D-273 fork: re-wire the session-relative daily-loss kill (its own ship, Task #13, D-273=WIRE) vs tombstone the dead OMS-aggregate kill pair (H21) — and confirm E.1.3 actually deletes the PortfolioController.hpp consumers. item-4 must NOT cap-validate cfg rows 543/544 as 'armed' (D-273, Class-12 false-credibility)."
        ]
      },
      {
        "leaf": "E.1.4 — fill-completeness (A16 partial-accumulate / A20-A21 venue-net reconcile / Class-46 close-by-fill_qty / §4c conservation / §5 event-log)",
        "verdict": "YELLOW — Position half is GREEN (safe to freeze at 128B for E.1.4: fill adds ZERO new Position fields; A16 weighted-avg + Class-46 partial-close mutate existing Money fields in place). NodeState half is the contingent surface. The leaf-map's three biggest freeze-urgency claims are REFUTED on grounded code: (1) owner_node_id is NOT required by E.1.4 — A21 boot attribution derives from order_id>>60 (the decode ProcessFillCommand:1393 already trusts), and D-209's boot-collapse is Reconcile.hpp:245's search-then-fallback-to-0, fixable with zero layout change; (2) the FillEvent<->NodeState back-edge is NOT a wire/determinism design-once-buster — the SPSCRing<FillEvent> is never serialized (grep-clean in ShardedSnapshotPersist), so the static_asserts are runtime alignment guards, re-opening NodeState costs a recompile not a snapshot bump; (3) slow_account does NOT need exact 96B pre-reservation — per-node money is serialized field-by-field (ShardedSnapshotPersist:187-246), so adding a money field later is append-only/version-neutral (H21-clean). What SURVIVES and gates the freeze: the STALE-SKETCH H4 regression (sketch types money FPN; live wire is already Money) MUST be fixed before E.1.2 freezes the money cluster; E.1.2's freeze enumeration is INCOMPLETE if it omits the fill read-set AND the two snapshot serializers; and the Position-relayout AoS->SoA is the largest determinism hazard (breaks both Portfolio_Save:570 PERSIST-walk and ShardedSnapshotPersist:165/169 raw blob). Not RED — no surviving claim forces an unavoidable design-once break once the over-engineered urgency is stripped; not GREEN — the H4 sketch fix + the SOFT(NodeState)/HARD(Position+fwrite-sequence) freeze-scope decision are real pre-freeze must-resolves.",
        "frozenLayoutRequirements": [
          "Position consumed read-set (DON'T-DELETE; survives AoS->SoA): entry_price, entry_fee, quantity, take_profit_price, stop_loss_price — read by handle_sell_fill OrderManager.hpp:1225-1229 on the DRAINER (<=10us, correctly not a hot violation). The fill leaf is the SECOND consumer of these (after the hot exit gate + OMS_OpenPositionCost:1493); take_profit_price/stop_loss_price are shared with the gate. Plus Portfolio.active_bitmap (uint16, race guard :1219). E.1.2's layout-freeze enumeration is INCOMPLETE if it omits this fill read-set. (NOT needed: original_sl, entry_timestamp_us; pair_index already exists Portfolio.hpp:59.)",
          "The two snapshot serializer fwrite SEQUENCES are the genuine H21 wire-locked surface — both MUST be re-derived field-by-field under AoS->SoA without silently dropping any field (a missed SoA field = silent-zero-on-restore, the D-110 warm-restart money round-trip hazard): Portfolio_Save PERSIST-prefix walk (Portfolio.hpp:570) + ShardedSnapshotPersist raw whole-struct Position blob (ShardedSnapshotPersist.hpp:165/169, dumps all 128B incl H12 _pad_pos[7]). This is the determinism-critical consumer pair the map's hot-path-inert enumeration omitted.",
          "Per-node money persisted as Money NOT FPN (H4): the LIVE sharded snapshot already writes node_realized/node_fees/node_open_notional/node_peak_balance/node_dd_pct as sizeof(Money) field-by-field (ShardedSnapshotPersist.hpp:187-213). E.1.2 must place the v11-persisted fill-money fields into this fwrite sequence as Money — freezing the FPN-typed foundation.md sketch would REGRESS the live wire encoding and make §4c conservation operate on feature-encoded money.",
          "CONDITIONAL — owner_node_id (D-209): land in E.1.2 on D-209's OWN settled warrant (sub-pool forward-reservation), NOT on a fabricated E.1.4/A21 requirement. If landed it fits the existing _pad_pos[7] (Portfolio.hpp offset 121) -> sizeof(Position)==128 unchanged, zero offset cascade; the E.1.2 PORTFOLIO version bump (7->8) already absorbs the pad->field wire-semantic change (H21). IF landed it MUST be PERSIST-kind and round-trip-verified in BOTH serializers (ShardedSnapshotPersist raw-dump auto-captures by offset; Portfolio_Save requires an explicit PERSIST row) — a SKIP_PERSIST or one-serializer-only add re-introduces D-209 on every warm restart (reload owner_node_id=0).",
          "NOT frozen for this leaf (explicitly leave forward-OPEN): FillEvent<F> POD field set (design in E.1.4 where it is pinned, or reserve only a cache-line-rounded opaque _reserved stub — ring is never serialized so no snapshot cost either way); slow_account exact-money reservation (pin-EXACT only if E.1.2 takes money ownership, else leave-open/keep money in OMS — NEVER half-reserve the sketch-guessed _padding1[24] against a ~96-208B real need); per-slot SoA scratch (last_exit_fill_price/last_exit_fee/last_realized_return/last_*_mask/bitmap — ephemeral OMS sibling arrays, non-persisted, relocatable for free); Position 128->192B expansion (zero fill warrant — see realignRisks)."
        ],
        "branchReductions": [
          {
            "branch": "E.1.4 entire fill surface (handle_sell_fill OrderManager.hpp:1214 / handle_buy_fill :1196 / A16 partial-accumulate / A20-A21 reconcile / F-096 leg-split Async.hpp:842-851 / event-log §5)",
            "freeNowInE12": false,
            "approach": "HOT-PATH-INERT — SURVIVES (well-grounded). All fill code writes ONLY oms->portfolio.* and oms->* scratch on DRAINER/async threads; grep is clean for live_tp=/live_sl=/->active= writers in OrderManager.hpp + Async.hpp. Fill handlers operate on oms->portfolio, a DIFFERENT object than the hot ExecutionCore<F> -> E.1.4 cannot by construction add or remove a hot branch (0 added / 0 removed to the 2097-instr ExecutionCore_Tick inventory). REMOVABLE column is EMPTY for this leaf; the H20/D-235 drive-warm->0 lever is NOT available (no fill-coupled warm site). De-risk PROOF must be RUN (tools/calls_graph_diff.sh against sharded_files.txt), not asserted. NOT an E.1.2 action."
          },
          {
            "branch": "ExecutionCore.hpp:337-338 — active?live_tp:cached ternary (map claimed 'the ONE real steady-state hot violation, removable via Money_BlendOnMask')",
            "freeNowInE12": false,
            "approach": "REFUTED + DEMOTED + ORTHOGONAL to E.1.4. `active` (core->active read once :327) is a STABLE state flag, ~100% predicted (flips only on the rare __builtin_expect(,0) line-504 push-gate tick) -> ~0 steady p99 contribution, ZERO misprediction variance. Money_BlendOnMask (FixedPointN.hpp:1876) is branchless+deterministic but issues BOTH 16B __int128 operands unconditionally every tick where the branch loaded only the taken side -> net p99 neutral-to-slightly-NEGATIVE, not a win. H20 governs TICK-DATA-DEPENDENT dispatch; `active` is not tick-data-dependent -> plausibly H20-EXEMPT. Supporting bench predates the Ship-B 16B-Money epoch (unmeasured for current type). Reclassify from 'removable steady-state violation' to 'stale-comment/cmov-stale cleanup, perf-neutral, bench-before-claiming'; it is an ExecutionCore concern, NOT this leaf's, and must NOT be credited to the H20 lever."
          }
        ],
        "techDebt": [
          "TECH_DEBT-167 / F-096 (partial-leg double leg-split) — SUBSUMED, LAYOUT-INERT (Position.quantity already Money, Portfolio.hpp:58; no Position byte reservation). REFINED: it is a cross-binary DETERMINISM violation not just conservation/H4 — order_qty_d = full_qty*Money_ToDouble(partial_pct) at Async.hpp:847/849 can FMA-contract differently across -march=native binaries -> divergent leg qty -> divergent snapshot. The map's 'two Money_Mul forms + assert' STILL drifts under half-even; correct fix = conservation BY CONSTRUCTION: legA=Money_Mul(intended,p); legB=Money_Sub(intended,legA) -> legA+legB==intended structurally, §4c assert is defensive not load-bearing. OWNERSHIP-AMBIGUOUS SEAM: assert+Money-ization land in E.1.4 (conservation home) with TD-167 trigger re-pointed, OR E.1.2 closes Money-ization explicitly and §4c references it — risk of falling between the two ships.",
          "TECH_DEBT-169 / A2+A4 (venue parser drops partial-qty + commission) — SUBSUMED by A20/A21, LAYOUT-INERT (zero Position coupling). double->Money conversion is at the OMS BOUNDARY (FillEvent, a new POD), not inside Position (Reconcile.hpp:63/407/572/608; OrderManager.hpp:1013/1428/1535). Survives uncontested.",
          "TECH_DEBT-202 / RBP Class 50 (event-log single-owner disk_file) — SUBSUMED by §5, ZERO Position-layout coupling (NOT an E.1.2 opportunistic-close candidate). CoreFrameworks/OrderEventLog.hpp struct lifecycle + the unused OrderEventLogFileHeader.reserved checksum word. Survives uncontested.",
          "RBP Class-46 (terminal actions gate on ORDER_FILLED not handler-reached) — SUBSUMED, the core fill-completeness fix; math/control not layout. Today handle_sell_fill whole-closes via Portfolio_CloseSlot(...,fill_price) (OrderManager.hpp:1243) over WHOLE positions[slot].quantity, clearing the bit unconditionally. SEAM (see realignRisks): the edit sites live in OrderManager_ProcessFillCommand (:1474 slot-free, :1442/:1450 book-gate), NOT HandleFill — use PLAN-BODY cites not the ~9-line-STALE RBP catalog cites.",
          "RBP Class-38 (INERT -filled reconcile term) — FLIPS LIVE this same ship when A16 lands: OMS_ExpectedFreeCash Money_Sub(requested_qty,filled_qty) at OrderManager.hpp:1520 (documented INERT, CO-GATED with A16). A16 makes filled_qty cumulative (today OVERWRITTEN at :1428) -> term goes live -> §4c conservation must account for it.",
          "RBP Class-45 (original_tp live-vs-replay divergence) — CORRECTLY NOT CLOSED (disposition right). Leaf freeze-FLAGs it (A25 operator design-decision disclaim); original_tp already in Position (offset 80) -> no layout change, no E.1.2 action.",
          "TECH_DEBT-195 (warm-restart exit-disarm window) — FLAG FOR EXPLICIT RE-HOMING (the cohort's fall-through risk): fill-ORTHOGONAL boot-ordering/flags-publish defect (cached_params.flags never published at restore, ShardedSnapshotPersist:669-680), touches neither fill handlers nor FillEvent nor conservation. 'verify subsumed by §6' is a vacuous hedge — re-home to E.1.2 (NodeState/restore skeleton) or E.1.5 (per-cluster boot). The wfa-1 witness (controller_test.cpp, commit ada6b7c) is EXPECTED-TO-INVERT @ .E.1; whoever owns the flags-publish fix must re-derive that assertion."
        ],
        "realignRisks": [
          "[HIGH · LOUD · the load-bearing survivor] STALE-SKETCH H4 regression: foundation.md sketches NodeState/Aggregator money as FPN<F> (realized_pnl :434, drainer core_fees, open_notional :435; Aggregator cached_total_realized_pnl/cached_realized_pnl all-FPN). The LIVE sharded snapshot ALREADY serializes these as sizeof(Money) (ShardedSnapshotPersist:187-213) — so freezing the sketch verbatim is a WIRE-ENCODING REGRESSION (FPN bits != Money bits) + an H4 violation + makes §4c conservation operate on feature-encoded money. The Aggregator sketch additionally carries a dead-size premise ('FPN is 24 bytes / __int128 atomic for 96-bit FPN') that predates Ship-B's 16B Money — the atomic-aggregation layout reasoning is built on a retired width. FIX BEFORE E.1.2 freezes the money cluster.",
          "[MED · reframed DOWN from the map's CRITICAL] slow_account sizing: the accumulator set is real and partly NON-DERIVABLE — there is NO stored starting_balance anchor (only Init param OrderManager.hpp:865) so §4c balance==start+Sigma-realized cannot re-anchor from a snapshot; ks_peak_balance is a path-dependent running Money_Max (:502/:1252) not recomputable from a flat snapshot. BUT this does NOT force exact pre-reservation: per-node money is serialized FIELD-BY-FIELD (:187-246), so a later money-field add is APPEND-ONLY / version-neutral (precedent: node_gross_wins/idle_cycles appended at v4.7.25/v5.4.3). The TRAP is the sketch-guessed _padding1[24] against a ~96-208B real need — a HALF-reserved cluster bakes a wrong size into the alignas chain and is worse than reserving nothing. RESOLUTION: pin the EXACT money set now (if E.1.2 takes money ownership) OR leave slow_account's money region explicitly forward-OPEN / keep money in OMS until the ownership-move leaf (E.1.4/E.1.5) lands. (ks_min_balance/ks_max_drawdown_pct are kill-switch CONFIG thresholds :500-501, may route to seqlock params not slow_account.)",
          "[MED · DOWNGRADED from CRITICAL — over-engineered per grounding] FillEvent<->NodeState back-edge: the SPSCRing<FillEvent> is NEVER serialized (grep-clean in ShardedSnapshotPersist), so sizeof(FillEvent) feeding sizeof(NodeState) busts only RUNTIME alignment static_asserts (offsetof(drainer_state)%64==0), NOT the wire format — re-opening NodeState later = a recompile + a one-line static_assert update, NOT a SHARDED_SNAPSHOT_VERSION bump and NOT a determinism re-cert; and it carries ZERO hot-path perf cost (drainer_state is cache-line-aligned, produced by cluster-WS / consumed by node-slow, neither end the hot thread). The map's 'move ring to ClusterState to decouple' escape FAILS independently (per-node SPSC ring; shared=SPMC violates H22; ClusterState is itself E.1.2-frozen) — but that is moot since the ring needn't be pinned at all. RESOLUTION: SOFT-freeze the NodeState runtime clusters; design FillEvent in E.1.4 (or reserve an opaque cache-line _reserved stub). Note Aggregator already consumes a partial shape (Aggregator_OnFill ... fill.net_pnl) so the field set is partially constrained.",
          "[NEGATIVE · bears on D1/192B — SURVIVES, all refutes converge] Fill path consumes NO per-position PEAK -> provides ZERO warrant for the D-206 128->192B Position pre-reserve. ks_peak is OMS-level (oms->ks_peak_balance OrderManager.hpp:502, Money_Max :1252), never per-Position; A16 keep-slot-open uses only existing fields. The 128->192B warrant must come from the riding-stop/trailing-stop leaf, NOT smuggled under 'fill' cover. BOUNDARY TO DEFEND.",
          "[DE-RISK · SURVIVES] Position is LOW realign risk for this leaf: A16 (entry_price weighted-avg, quantity+=Qf, entry_fee+=Ff) and Class-46 (reduce quantity by fill_qty, apportion entry_fee) are MUTATE-IN-PLACE on existing Money fields (PositionFieldRegistry.hpp:53-55). The self-refute 'repeated apportionment drifts 1-ULP -> forces original_qty/original_fee on Position' is REFUTED: apportion against the CURRENT decremented quantity + book the RESIDUAL on the final leg -> conserves bit-exactly with no original-basis field (Money_FillGross is the 1-mul SSoT Portfolio.hpp:408). Add an original_qty/original_fee basis field ONLY if §4c actually trips — not pre-emptively.",
          "[SHAPE-DRIFT · coder re-ground] The plan's ':1508 INERT stub, make it live' does NOT resolve at HEAD 0ee227a — :1508 is the OMS_ExpectedFreeCash Class-38 comment; handle_sell_fill is ALREADY live and buggy (whole-closes via Portfolio_CloseSlot(...,fill_price):1243 ignoring fill_qty), not an inert stub. The Class-46 fix MODIFIES a live handler (close-by-fill_qty), it does not 'activate a stub'. The :1508 stub cite is forward-state (post-E.1.3 scaffold) absent at current HEAD."
        ],
        "openQuestions": [
          "[D1 — is Position FINAL at 128B?] This leaf's answer is YES for the fill surface: fill adds ZERO new Position fields (A16/Class-46 mutate existing). The 128->192B D-206 per-position-PEAK pre-reserve gets ZERO warrant from fill — if Position grows to 192B it is on the riding-stop leaf's warrant, decided THERE not here. owner_node_id (if landed) fits existing _pad_pos[7], 128B unchanged. OPERATOR CALL: freeze Position at 128B now and require the 192B warrant to be justified by the riding-stop leaf in isolation?",
          "[owner_node_id: store vs decode] D-209's boot-collapse is fixable with ZERO layout change by decoding (t.order_id>>60)&0xF (the encode at OrderManager.hpp:961, the decode ProcessFillCommand already trusts at :1393) instead of Reconcile.hpp:245's search-then-fallback-to-0. owner_node_id-on-Position cannot even self-populate at boot (positions are being reconstructed FROM venue trades). OPERATOR CALL: (a) fix Reconcile's node recovery by decode (closes D-209 boot collapse, no field) AND/OR (b) still land owner_node_id in E.1.2 as a cheap sub-pool forward-reservation into _pad_pos[7] — but if (b), it MUST be PERSIST-kind + round-trip both serializers (else warm-restart re-collapses to D-209). Drop the map's 'REQUIRED by E.1.4' framing.",
          "[NodeState freeze scope: SOFT vs HARD] Grounded fact: only the Position struct offsets (wire blob ShardedSnapshotPersist:169) and the persisted-field fwrite SEQUENCE (:173-246) are H21/determinism-locked; the five alignas(64) NodeState runtime clusters (hot/slow_account/drainer_state/binding/kill_flags) are NOT serialized and NOT fingerprinted -> recompile-cheap to relayout. OPERATOR CALL: declare the freeze policy explicitly as HARD-freeze(Position offsets + fwrite sequence) + SOFT-freeze(NodeState runtime clusters), so downstream-leaf re-touches of the runtime clusters are not mis-treated as design-once snapshot-bump events.",
          "[FillEvent timing] Pin FillEvent's full field set in E.1.2 (one leaf early) vs design it in E.1.4 where it belongs? Grounded fact (ring never serialized) favors E.1.4-design with at most an opaque cache-line _reserved stub in E.1.2; but Aggregator's partial net_pnl consumption is a soft constraint. OPERATOR CALL.",
          "[A16 stale-TP write-set gap — NOT a branch, a correctness seam] A16 re-accumulates into an open slot at a NEW weighted-avg entry_price, but live_tp/live_sl were armed at the FIRST fill price (ExecutionCore.hpp:543) and are NOT re-armed by any fill handler (can_enter blocked by any_active=1 -> :543 never re-runs -> STALE TP/SL vs new basis). OPERATOR CALL: re-arm via slow-path SetParameters on weighted-avg re-entry vs accept original-basis TP (Class-45 disclaim). REQUIRES verification: if A16 re-entry is actually routed as a fresh enter_a on a closed-then-reopened slot, :543 re-runs and this is MOOT — confirm nothing re-publishes live_tp via ExecutionCore_SetParameters/RebuildOneCore. Adversarial guard: any A16/A20/reconcile design that quietly adds a fill->ExecutionCore write (e.g. reconcile re-arming live_tp to venue-truth basis) BREAKS the clean hot-path decoupling.",
          "[crossLeafSeams INBOUND contradiction <- E.1.3] E.1.3's 'frozen drain-shell' boundary is drawn WRONG vs the Class-46 fix sites: the unconditional slot-free (order_bitmap&=~(1<<slot) at :1474) and the book-gate (if ORDER_FILLED {...HandleFill} at :1442/:1450) both live in OrderManager_ProcessFillCommand (:1379, invoked from the drain shell), NOT inside HandleFill — so A16 keep-slot-open + intra-session partial-accumulate CANNOT be done 'inside HandleFill only'. OPERATOR CALL: either E.1.3's freeze explicitly EXCLUDES terminal-action gating in ProcessFillCommand (hands it to E.1.4), or the contract's 'leaf math only' scoping understates E.1.4's true write-set. The leaf's load-bearing control-flow seam (orthogonal to layout)."
        ]
      },
      {
        "leaf": "E.1.5 — per-cluster purity (H22 scale-invariance): NEW-1 trading_mode per-cluster authority relocation + A34 cluster same-symbol concentration governor. Grounded at HEAD 0ee227a (leaf scaffold cited 572c978 — stale, c-class re-ground mandated).",
        "verdict": "YELLOW — layout-readiness is CONDITIONAL. After weighting 7 refutations, E.1.5 is hot-path-INERT and the SURVIVING frozen set E.1.2 must reserve is small, byte-cheap, and well-grounded (owner_node_id in existing pad, Money-typing, per-node-granular published snapshot, BUY_BLOCKED reuse). NOT GREEN because the leaf-map OVER-RESERVED (per-symbol slab, MAX_SYMBOLS_PER_CLUSTER mint, bespoke cluster.trading_mode mirror, frozen hot.mode all refuted as denormalizations/premature generalizations) and OVER-CLAIMED the H22 closure (the (N+1)th-cluster ZERO-edit property is deferred to E.1.6, not delivered here), and TWO unrecorded forks (A34 push-vs-pull; per-cluster-mode form) MUST be settled at the E.1.2 freeze. NOT RED because nothing is structurally blocked and no contradiction is unresolvable. Ready to freeze once the operator settles the forks below.",
        "frozenLayoutRequirements": [
          "owner_node_id on Position (D-209, net-new; 0 files at HEAD = unbuilt) — BYTE-FREE: fits existing _pad_pos[7] at offset 121, preserves the sizeof(Position<64>)==128 / 2-cache-line size-pin (Portfolio.hpp:70,117). PORTFOLIO_SNAPSHOT_VERSION bumps 7->8 regardless (HEAD is 7 Ship-B DECIMAL, Portfolio.hpp:534 — NOT 6). Must ship with a PINNED warm-restart derivation rule + non-vacuous round-trip test: a pre-D-209 snapshot has no owner_node_id, so load MUST deterministically recover the ORIGINAL owning node, never the loading context (Class-45 guard).",
          "Per-node slow_account money fields frozen as Money (16B FixedPoint<10,8>), NOT FPN_Binary — open_notional / realized_pnl / capital_allocated / drawdown_max / drawdown_current (H4/D-214). Byte-NEUTRAL (both 16B: FixedPointN.hpp:44 FPN, :102/:107 Money) — this is a TYPE-correctness freeze (half-even/saturate accounting semantics), NOT an atomic-accumulator enabler (see branchReductions/realignRisks: 16B has no native atomic add).",
          "E.1.3's published per-cluster snapshot MUST RETAIN per-node granularity: each node's published open_notional + boot-immutable node.hot.symbol. This is the LOAD-BEARING freeze constraint — it is exactly what the A34 pull-over-own-<=16-nodes design reads. If the snapshot collapses to a cluster-SCALAR, the pull breaks and A34 is forced back to a slab (re-open). NO per-(cluster x symbol) slab, NO MAX_SYMBOLS_PER_CLUSTER mint.",
          "alignas(64) on the published cross-thread per-node slow_account slices (H6) — the slices A34 pulls. NOTE the A34-SLAB-specific alignas(64) demand from the leaf-map dissolves with the pull design; alignment survives only on the per-node published slots E.1.3 needs anyway.",
          "ClusterRunning per_cluster[] aggregate Money fields frozen as Money (H4) — but with NO symbol dimension. The flat per_cluster[] container shape is E.1.2-frozen (E.1.2 plan:34/64); refute 2/6 confirm it is NOT E.1.3-re-touchable, so the symbol-vs-scalar question is an E.1.2 decision (answer: scalar per-cluster + per-node-granular pull, no symbol axis).",
          "GateParameters.flags stays uint8_t (GateParameters.hpp:140) — A34 reuses GATE_FLAG_BUY_BLOCKED (0x20). CONFIRMED saturated: all 8 bits 0x01..0x80 allocated (GateParameters.hpp:38-79). E.1.2 must NOT reserve a private A34 flag bit (would widen uint8->uint16 and relayout the seqlock-published cached_params). A34 instead adds a COLD slow-path halt-reason enum sibling for forensic distinguishability (halt_reason nibble has free slots: max HALT_IMBALANCE=10, packed &0x0F display-only at ControllerEventLoop.hpp:3108).",
          "trading_mode STAYS physically in ControllerConfig as the STAMP_BOUND SSoT — do NOT tombstone/relocate the stamp field. CONFIRMED STAMP_BOUND | STAMP_BOUND_CFG_DERIVED | SAFETY_CRITICAL (CfgFieldRegistry.hpp:418, row literally annotated 'THE capital-authority field (NEW-1)') + walked into the HMAC-signed stamp body (StampHelper.hpp:159) + in the raw void*+sizeof fingerprint (ControllerConfig.hpp:1320-1323). Per-cluster reachability is a boot-derived read-only mirror / ResolveForCluster seam (FORM is an open question), never a field that replaces the cfg SSoT.",
          "NodeState.hot.mode must NOT be frozen as a writable per-node authority field. Hot path reads mode ZERO times (grep ExecutionCore.hpp = 0) so placement in .hot is a DOD error AND a Class-47 split-brain hazard. Freeze it as DELETED (no per-node mode field — simplest) OR relocated to the boot-immutable binding block as a read-only mirror; resolve split-brain at LAYOUT, not convention."
        ],
        "branchReductions": [
          {
            "branch": "A34 same-symbol concentration veto",
            "freeNowInE12": true,
            "approach": "ZERO new hot-path branch — rides the EXISTING branchless veto mask. Slow path EventLoop_RebuildOneCore sets GATE_FLAG_BUY_BLOCKED (0x20) on pending_params.flags exactly as book-imbalance does; hot consumer is the already-paid-for mask at ExecutionCore.hpp:366-368/GateParameters.hpp:182 (blocked = flags & 0x20; blocked_mask = -blocked; bg_fires &= ~blocked_mask). Free now because the flags byte is reused (no bit reservation, no cached_params widening). The cluster-aggregate read stays OFF the hot path (pull runs at slow entry-gate); aggregate->hot is brokered entirely through the slow flag write — this is the H22 hot-path witness the OUTBOUND seam should assert."
          },
          {
            "branch": "Capital-mode (live/paper) gate",
            "freeNowInE12": true,
            "approach": "ZERO new hot-path branch and zero new per-tick read. trading_mode is slow/boot-only (0 hot reads). Capital authority gates the SLOW-path permission grant (ControllerEventLoop), consumed branchlessly at ExecutionCore.hpp:474-476 via __atomic_load_n(&core->permission, ACQUIRE) masked into can_enter. NEW-1's relocation changes WHERE the slow/boot predicate reads from, never the hot mask. Contributes 0 to the D-235 warm->0 count — do NOT credit E.1.5 with hot-path branch reduction; its hot deliverable is NON-REGRESSION of the H22 pure-local-input guarantee."
          }
        ],
        "techDebt": [
          "TECH_DEBT-190 (A34 cluster-aggregate same-symbol concentration governor) — the leaf's DELIVERABLE, downstream of E.1.3+E.1.4's published per-cluster slice. CANNOT close 'during the E.1.2 relayout'; confirm not mis-scheduled as an E.1.2-relayout-time close.",
          "TECH_DEBT-168 (live-OFF per-cluster authority) — PARTIALLY subsumed. NEW-1's predicate-body relocation lands, but the full (N+1)th-cluster ZERO-edit closure is DEFERRED to E.1.6 (refute 6: global single-valued representations survive — g_sharded_binance_adapter init Run.hpp:592-593, scalar TUISnapshot::live_trading EngineTUI.hpp:855, global-drainer paper-reset Async.hpp:580). Verify don't assume. Do NOT tombstone cfg.trading_mode (STAMP_BOUND) to achieve closure.",
          "RBP Class 47 (split-brain authority) — completed ONLY if NodeState.hot.mode is deleted or relocated read-only (refute 1/5/7). The predicate landed .E.0.10; NodeState.hot.mode left writable re-opens the split-brain.",
          "RBP Class 45 + TECH_DEBT-186 (A1 warm-restart take_profit_pct vs per-strategy override) — leaf owns A1 verify (mostly landed .E.0.10; relocation seam Async.hpp:904,913). NEW: owner_node_id warm-restart adds a SECOND Class-45 instance (refute 4 N1). Both verifies must be NON-VACUOUS (run, not assume).",
          "H22 §4a CI-candidate row — opportunistic structural close. Class-51 VACUITY guard: non-vacuity comes from a READ-SITE detector (Class-26 unindexed-global-read shape; substrate exists tools/check_per_node_registry_integrity.py Check 10) dispatching off the post-ControllerConfig.hpp:2975 EXACT-suffix match, NOT from field-tombstone (cfg.trading_mode stays SSoT) and NOT from the OLD substring matcher.",
          "EXPLICITLY NOT SUBSUMED (precision guard): TECH_DEBT-167 / F-096 (partial-exit leg-split double — Async.hpp:842-851 Money_ToDouble(partial_pct)) — H4/conservation concern homed in E.1.2 relayout + E.1.4 fill spine, only ADJACENT to E.1.5. Do NOT absorb here (subsumption-not-adjacency).",
          "ID-namespace caveat: TD-190/TD-168 are GLOBAL TECH_DEBT IDs; bodies NOT readable at HEAD (DOCS/TECH_DEBT.md index stops at -124). Closed-disposition write-back at ship-close targets the workspace ledger; index needs a -125..-224 backfill."
        ],
        "realignRisks": [
          "[CRITICAL — UNRECORDED FORK] A34 read-substrate shape. If E.1.2 freezes the published per-cluster slice as a SCALAR-per-cluster (or per_cluster[NUM_EXCHANGES] keyed by exchange_id, which also conflates cluster==exchange — refute 6), per-symbol concentration is underivable and E.1.5 is forced to re-open the alignas(64) capital struct (H12 byte-equiv) + re-bump a snapshot VERSION E.1.2 just bumped = design-once violation. The push-slab-vs-pull-over-own-nodes fork is genuinely UNRECORDED in D-1..D-256. Settle AT E.1.2 (recommend pull — reserves nothing, per-cluster-pure, no cluster==exchange conflation).",
          "[CRITICAL] 'ZERO authorizer-site edits / structurally unrepresentable' is materially OPTIMISTIC. Predicate ControllerConfig_IsLiveCapital(const ControllerConfig<F>& cfg) (ControllerConfig.hpp:2212) takes cfg, NOT a cluster; only a per-NODE resolver exists (ControllerConfig_ResolveForCore), no per-cluster resolved ControllerConfig. Reading cluster.trading_mode either changes the signature (wide cascade) or needs a per-cluster resolved cfg embedded. NEW-1 is necessary-but-insufficient; (N+1)th-cluster closure deferred to E.1.6. Do NOT credit E.1.5 with closing the H22 scale-break.",
          "[HIGH — DETERMINISM] trading_mode is STAMP_BOUND | STAMP_BOUND_CFG_DERIVED | SAFETY_CRITICAL (CfgFieldRegistry.hpp:418, CONFIRMED) in the HMAC-signed stamp body (StampHelper.hpp:159) + raw fingerprint (ControllerConfig.hpp:1320-1323). The leaf's 'structurally unrepresentable (field removed)' and 'determinism LOW / fingerprint stable' framings are INCOMPATIBLE. Keep cfg.trading_mode as SSoT; non-vacuity via read-site detector, not deletion. The leaf's OUTBOUND->DETERMINISM 'LOW' rating is wrong for this field.",
          "[HIGH] PortfolioController_HotReload save/restore-protects config.trading_mode (PortfolioController.hpp:1964,1976 — CONFIRMED) so a per-node-triggered reload bulk-copy can't flip live/paper. The leaf's 'ZERO authorizer-site edits' enumeration OMITS this site. Any per-cluster-mode scheme that embeds a resolved cfg in ClusterState re-arms a per-node-flips-cluster-authority hazard (Knight-class H22 break). Freeze must GUARANTEE no per-node reload path can write cluster capital authority.",
          "[HIGH — PERF/PORTABILITY] If the push-slab path is chosen, the 'atomic-add into 16B Money' is NOT a native op: x86-64 has no lock xadd for 128b -> lowers to a lock cmpxchg16b CAS retry loop, and the build has -O3 -march=native with NO -mcx16, so a portable target routes 16B atomics through libatomic's lock pool = H1/H3 violation on a money path. The codebase NEVER puts 16B under atomic (uint64_t accumulators + seqlock for multi-word Money). This is the decisive argument for pull (reserves no atomic).",
          "[MED — PURITY] NodeState.hot.mode writable/independent = Class-27 scalar-mirror / Class-47 split-brain; the (N+1)th node could carry mode=live while its cluster is paper. Hot path reads mode 0 times so .hot placement is also a DOD error. Delete or relocate to binding read-only; settle at E.1.2 not E.1.5.",
          "[LOW — verify at dive] cfg_load_fault_flags guaranteed-0 discipline: ControllerConfig fingerprinted RAW (no field exclusion); CLUSTER_PURITY / live_capital_cfg_conflict bits folding into cfg_load_fault_flags must be 0 at every Fingerprint_Compute call site (assert; the sizeof pin is vacuous, the assert is the guarantee; D-247/D-256, cfg_capital_gate_ok ControllerConfig.hpp:1335). No live models => fingerprint regen free. Verify, do not re-escalate.",
          "[C-CLASS] Leaf scaffold cites HEAD 572c978; you are at 0ee227a. Predicate is ControllerConfig.hpp:2212 NOT :2062/:2012. PORTFOLIO_SNAPSHOT_VERSION is 7 at HEAD (Ship-B DECIMAL) NOT 6 (refute 4's version claim is itself stale). ClusterState/NodeState/owner_node_id/cluster.trading_mode/MAX_SYMBOLS_PER_CLUSTER all grep-EMPTY = genuinely UNBUILT => E.1.2 is the real freeze point. Re-ground all file:line at the dive."
        ],
        "openQuestions": [
          "[FORK 1 — settle AT E.1.2] A34 governor: PUSH per-(cluster x symbol) Money slab vs PULL over the cluster's own <=16 nodes' published slow_account.open_notional at slow entry-gate time. Recommend PULL — refuted on 6 lenses as over-reserve: it reserves nothing, is per-cluster-pure, needs no MAX_SYMBOLS_PER_CLUSTER mint, no 16B-atomic (= H1/H3 portability hazard), no cluster==exchange conflation, and D-211/TD-190 already records the model ('the aggregator already publishing the account snapshot is the natural home'). One-node-one-symbol (D-104) makes cluster same-symbol exposure = sum over nodes where node.symbol==X. D-54's push-race argument is a hot-path/global-kill-switch concern, far weaker for a slow-path entry gate.",
          "[FORM — settle AT E.1.2] Per-cluster capital-mode reachability: bespoke ClusterState.trading_mode uint8 mirror (Class-27 risk; re-creates the split-brain NEW-1 closed) vs ResolveForCluster seam (mirrors the existing per-node ResolveForCore) vs keep-cfg-SSoT + read-site detector. Recommend: keep cfg.trading_mode as the STAMP_BOUND SSoT; reach per-cluster mode via ResolveForCluster or a boot-derived read-only mirror; get H22 non-vacuity from a read-site detector. NOTE trading_mode has NO core_N override (resolved==global for every node today), so the strict H22 driver is FORWARD E.1.6 heterogeneity (cluster A live / B paper), not current per-shard purity.",
          "[D1 — Position FINAL / D-206 PEAK pre-reserve] E.1.5 does NOT force the D-206 128B-vs-192B PEAK/riding-flag decision. E.1.5's ONLY Position dependency is owner_node_id, which is BYTE-FREE (fits the existing _pad_pos[7] at offset 121, preserves the 128B / 2-cache-line size-pin, Portfolio.hpp:70,117) and is consumed INDIRECTLY (aggregator reconcile, not A34 — A34 reads the published aggregate slice, never Position[]). So the D-206 PEAK/riding-flag call can be made on its OWN merits, NOT gated by E.1.5. If D-206 independently decides to grow Position to 192B, owner_node_id rides along free; if Position stays 128B FINAL, owner_node_id still fits. Either way E.1.5 is A34-INERT on Position size.",
          "[FORM] NodeState.hot.mode disposition: DELETE entirely (no per-node mode field — simplest; a field that can't exist can't split-brain) vs RELOCATE to the boot-immutable binding block as a layout-enforced read-only mirror. Settle at E.1.2.",
          "[RECORD AT E.1.2] Is the per-cluster aggregate (ClusterState/AggregatorState) SERIALIZED into the SHARDED snapshot wire body? ShardedSnapshot.hpp has zero ClusterState/AggregatorState refs at HEAD; AggregatorState reads as a D-54 recompute-on-cycle runtime structure. This decision determines whether a later re-open is a snapshot-VERSION/H9/H12 event vs a mere recompile + size-pin bump — the leaf's 're-bumps a snapshot VERSION' escalation is UNVERIFIED and likely overstated unless E.1.2 elects to persist the aggregate. Record it explicitly.",
          "[FREEZE-TIMING — operator call] A34 concentration-BOUND cfg field: addable at E.1.5 as a normal H17 FOREACH_CFG_FIELD 1-row registry addition (auto-regenerates the 53056 size-pin, fingerprint regen free — no live models) per refutes 1/4/7, vs pre-reserved at E.1.2 per refute 2 (FIT lens: adding the row later bumps the static_assert ControllerConfig.hpp:1323 + fingerprint, touching the struct E.1.2 declared FINAL). Recommend: addable at E.1.5; but if E.1.2 is finalizing ControllerConfig's size-pin anyway, reserving the row(s) then is near-free design-once insurance. NOTE the kill-threshold cohort lives in AggregatorState (per_cluster_kill_drawdown_threshold), NOT ClusterState — do not size A34's bound with it; DISTINCT from .E.5 max_per_node_pct (per-NODE single-position bound)."
        ]
      },
      {
        "leaf": "E.1.6 — multi-exchange (FOREACH_EXCHANGE adapter machinery, 4-state mode, venue-sourced fee, ENUM_STRICT metadata widen)",
        "verdict": "YELLOW — E.1.6 is layout-ready in the load-bearing sense: code-grounded, it adds ZERO per-tick branches (hot-path NULL survives all five lenses) and its entire NodeState/ClusterState substrate is forward-created by E.1.2's freeze, never re-touched by E.1.6. The leaf-map's headline urgency — \"reserve E.1.6's NodeState/ClusterState fields NOW or the struct re-opens → snapshot epoch / golden regen\" — is REFUTED for every NodeState/ClusterState field by the actual persistence code: the sharded snapshot serializes per-node state by FIELD PROJECTION (ShardedSnapshotPersist.hpp:172-246), and the ONLY struct-image dump is Position (line 169, fwrite sizeof(Position<F>)). ClusterState never enters the persist file at all (pure runtime: pthread_t/atomics/fn-ptrs). So growing NodeState/ClusterState later costs a recompile, not a determinism epoch. YELLOW (not GREEN) because the genuine wire-frozen surface — Position — carries unresolved operator decisions (D1: pre-reserve D-206 PEAK + owner_node_id in the wire struct vs route to OMS sibling SoA; owner_node_id derivability), and the sketch bakes an H22 hard-invariant violation (per_cluster[NUM_EXCHANGES] / cluster_id≡ExchangeEnum) that must be decoupled before the freeze even though it is itself epoch-free to fix. RED is not warranted: the refuted threat REDUCES risk, hot path is clean, and no blocking layout flaw survives.",
        "frozenLayoutRequirements": [
          "HARD (wire epoch) — Position struct layout: Position is the SOLE struct-image-dumped type (ShardedSnapshotPersist.hpp:169 / load :394; PORTFOLIO_SNAPSHOT_VERSION #define = 7 at HEAD, bump 7->8). Any field added to the wire-Position later = a forced PORTFOLIO epoch + golden regen. Therefore D-209 owner_node_id (IF persisted) + D-206 PEAK (IF in wire-Position) + D-55 SoA must land in ONE PORTFOLIO 7->8 bump, or not at all. This is the ONLY field set whose deferral carries the epoch cost the leaf-map attributes broadly.",
          "HARD (silent encoding epoch) — NodeState/drainer/Aggregator money fields = Money (16B), never FPN_Binary<F>. ALREADY landed in NodeContext (node_realized/node_fees/node_open_notional/allocated_balance/node_peak_balance/node_dd_pct all fwritten as sizeof(Money), :187-213); only the SKETCH is stale-FPN. The surviving obligation is 'don't regress + extend the type to the WHOLE accounting set' — FPN->Money is byte-SIZE-identical (both 16B) so a late flip is a silent 2^64-vs-10^8 reinterpretation that sizeof static_asserts CANNOT catch (a MONEY_ENCODING_EPOCH bump of capital wire data with no tripwire). Set MUST include AggregatorState money (per_cluster[].cached_realized_pnl/open_notional/drawdown_current, global cached_total_*, *_kill_drawdown_threshold) + drainer core_realized/core_open_notional, not just NodeState.core_fees.",
          "HARD (H12 pad determinism) — any new Position field inserted among the Money fields MUST carry explicit int<N>_t _padding = 0; Position is raw-fwritten WHOLE (incl padding); implicit inter-field padding → two saves of identical state diverge in pad bytes → non-deterministic snapshot. Add explicit-pad static_asserts alongside the new offsetof asserts; do NOT inherit the existing implicit-init _pad_pos[7] assumption.",
          "STRUCTURAL (epoch-free but H6/homogeneous-array correctness, lay-once) — ClusterState embeds the adapter as a FIXED-SIZE fn-pointer table BY VALUE (tt::ExchangeAdapter<F> = fn-ptrs + void* ctx, confirmed size-invariant at ExchangeAdapter.hpp:84-103), NOT template<typename AdapterT> struct ClusterState. Templated form => ClusterState<Binance> != sizeof ClusterState<Alpaca> => no homogeneous per_cluster[] => structurally impossible aggregator. Sketch :620 is the worst-mis-freeze; the leaf itself flags 'fn-pointer-table, NOT template specialization'.",
          "STRUCTURAL (design-once, epoch-free) — ExchangeEnum pinned fixed-width (e.g. uint8_t-backed) so adding FOREACH_EXCHANGE rows never resizes ClusterState.exchange_id; carry exchange_id as a FIELD of ClusterState, never as the cluster-array index.",
          "STRUCTURAL (H22, epoch-free to fix but a hard-invariant violation if frozen wrong) — decouple cluster_id from ExchangeEnum and dimension every cluster-indexed array (per_cluster[], per_cluster_kill[], per_cluster_kill_drawdown_threshold[]) by MAX_CLUSTERS, NOT NUM_EXCHANGES. binding.cluster_id is the routing index, not 'the FOREACH_EXCHANGE index'.",
          "RUNTIME (epoch-free, grow-lazily) — NodeState.binding{cluster_id, subaccount_id, client_order_id_prefix[16]}: cold/boot-immutable routing keys, re-derived from cfg on warm restart, never serialized. Reserve for software tidiness only, explicitly NOT under epoch threat. client_order_id_prefix is DERIVABLE ('C<cluster><sub><node>') from the other three — compute on submit/boot, storing it is denormalization, not a layout need.",
          "NOT a layout field (confirmed by all lenses) — per-exchange fee/precision (tick/lot/price_precision: 0 hits in CfgFieldRegistry): venue-SOURCED at boot via FOREACH_EXCHANGE .rodata registry + D-272/D-276 defer-to-source-authority. OPEN: record explicitly whether fee/precision rides cfg/.rodata (no ClusterState reservation) OR adds ClusterState.fee_maker/fee_taker (Money) — decides whether ClusterState grows.",
          "NOT a layout field — metadata_flags uint16->uint32 widen: CfgFieldDescriptor is .rodata constexpr, consumed only by the parse/save walker, never hashed/memcmp'd/fwritten; not in the cfg-pin (D-258 hashes the cfg INSTANCE not the descriptor). Zero Position/NodeState/ClusterState constraint, no snapshot bump, no golden regen. Keep its CfgFieldRegistry blast-radius OUT of the layout-freeze scope."
        ],
        "branchReductions": [
          {
            "branch": "E.1.6 per-tick branch additions (4-state mode / venue-fee / metadata dispatch)",
            "freeNowInE12": true,
            "approach": "Already free — NONE added. Code-confirmed hot-path NULL: fee_rate/->mode/trading_mode = 0 hits in ExecutionCore.hpp + GateParameters.hpp; fee pre-resolved at submit (Order_BindPreResolved), mode read only by slow/boot/display mirrors, metadata validated at parse/boot in the walker. Zero hot bytes, zero hot branches. Frontmatter hot_path: untouched CONFIRMED."
          },
          {
            "branch": "ExecutionCore.hpp:337-338 active?live:cached TP/SL ternary (the baseline's claimed branch-removal 'win')",
            "freeNowInE12": false,
            "approach": "Orthogonal to E.1.6 (no exchange/mode/fee lever touches it) AND refuted as a p99 win: predicate is the slow-changing 'active' position-state flag, ~100% predicted in any steady window => near-zero variance, the only mispredicts coincide with the rare event-push at :504. Money_BlendOnMask has 0 call sites repo-wide (UNVALIDATED — the 'verified in bench' comment is stale per the 16B-Money epoch); converting adds 2 unconditional 128-bit operand loads + ~6 mask uops to delete a branch that does not mispredict. Defensible ONLY as H7/H20 conformance + stale-comment cleanup, NOT a latency dividend, NOT E.1.6's to carry."
          },
          {
            "branch": "RollingStats.hpp:325/334/343 while-loop back-pop -> fixed-K mask-blend",
            "freeNowInE12": false,
            "approach": "Slow path (<=100us, 200x the hot budget). Converting amortized-O(1) to fixed-O(K) ADDS work in the common single-pop case to suppress data-dependent iteration; the cited p99 signal is 14 register spills, not the back-edges. Refuted absent PMU evidence the while-jitter exceeds added K-work + unchanged spills. Conformance/cleanup, not a p99 reduction; not E.1.6 scope."
          },
          {
            "branch": "Regime_Classify cascade (RegimeDetector.hpp:593-605) -> LUT",
            "freeNowInE12": false,
            "approach": "Slow path; regime is hysteretic/highly autocorrelated by design (sticky-predicted within a run) => cascade predicts near-perfectly => near-zero variance. H20-conformance change, not a latency dividend; not E.1.6 scope."
          }
        ],
        "techDebt": [
          "TECH_DEBT-224 (MetadataFlag zero-headroom widen, HIGH) — SUBSUMED, survives. ENUM_STRICT (D-253) is the first bit forcing uint16->uint32; once paid, MULT+FEE+DOLLAR cap-bits ride at ~0 marginal. Refutations confirm the widen is .rodata, layout/snapshot-orthogonal (no cascade). Lockstep-widen the coverage-assert masks (EXEMPT/ALL/ENROLLED bitsets, cfg_compute_mask param, overflow guard) or Class-51 vacuity follows.",
          "CAPITAL_BOUND_FEE (HIGH) — SUBSUMED, strongest. E.1.6 BUILDS the FOREACH_EXCHANGE venue registry = the exact venue fee-schedule SSoT the FEE cap must defer to (D-272/D-276); its home is load-bearing on the deliverable, not a free bit. fee_rate_maker/_taker at CfgFieldRegistry.hpp:710,711.",
          "Class 12 (wired-but-unexercised fail-OPEN enum, HIGH) — SUBSUMED, survives. KIND_INT_ENUM WARNs-then-DEFAULTS on unknown token (CfgFieldDispatch.hpp:177-187); E.1.6 is its FIRST real consumer (exchange_id / 4-state mode / subaccount_id) => ENUM_STRICT both exercises the dormant branch AND adds teeth (typo'd exchange silently defaulting to Binance). Cross-ref Class 51.",
          "Class 51 (vacuous-green) — WATCH/LANDMINE the widen must NOT introduce (HIGH), not a debt to close. Widen the enum underlying to uint32 but leave the coverage-assert bitsets uint16 => bits >=16 masked off => H16 assert passes vacuously over a truncated mask. Lockstep widen mandatory.",
          "TECH_DEBT-162 (warm-restart fee-recompute) — NOT subsumed; natural owner E.1.4. CROSS-LINK only: E.1.6 EXPOSES the per-venue fee SSoT; E.1.4/TD-162 should CONSUME it, not re-internalize a fee number. Homed-visible cross-link, not a close.",
          "TECH_DEBT-135 (Class 11 regime_names[]) — ADJACENCY not subsumption (MED), do NOT close on adjacency. Fit target is FOREACH_REGIME (4 regimes), not the new FOREACH_EXCHANGE. Close IFF a /registry-fit-audit is ~0 marginal; live drift found (GUI panels hold inconsistent 5-entry arrays vs the 4-regime registry) makes the rewire NOT ~0 => cross-link + leave tracked.",
          "DO NOT CLAIM as subsumed: TD-203 (v5.16 Phase-D gate removal), TD-167/F-096 (partial-exit leg-split = E.1.2/E.1.4 fill-path; partial_exit_pct is KIND_DOUBLE range-bounded [0,1], needs no new metadata bit)."
        ],
        "realignRisks": [
          "WORST MIS-FREEZE (survives, all lenses confirm) — freezing ClusterState as template<typename AdapterT>{ AdapterT adapter; } (sketch :620) instead of embedding tt::ExchangeAdapter<F> (fn-ptr table + void* ctx, size-invariant, ExchangeAdapter.hpp:84-103) by value. Templated => heterogeneous ClusterState sizes => no homogeneous per_cluster[] => aggregator structurally impossible. Landed OrderManagerState model is already correct (adapter by value, OrderManager.hpp:472). Reserve-now cost ~0.",
          "H22 HARD-INVARIANT VIOLATION baked into the sketch (ELEVATED from leaf-map's 'secondary push' by the FIT lens) — per_cluster[NUM_EXCHANGES] + cluster_id ≡ ExchangeEnum fails D-193/H22 ('scales across N nodes AND N clusters; adding the (N+1)th cluster requires ZERO change') the instant cluster_count > NUM_EXCHANGES — exactly the sketched 4-subaccount Binance case split into two capital-isolated clusters. H22 (operator-ratified 2026-06-11) overrides any earlier one-cluster=one-exchange convenience. Fix: dimension by MAX_CLUSTERS, cluster_id distinct from ExchangeEnum. Epoch-free to fix but a correctness must-get-right-once.",
          "Money-type freeze (survives, REFRAMED) — NodeState money is ALREADY Money in landed NodeContext; the surviving risk is (a) regressing to the stale-FPN sketch — a SILENT 16B encoding-epoch no sizeof-assert catches — and (b) under-enumerating the set: AggregatorState money (per_cluster cached_*, global cached_total_*, kill_drawdown_thresholds) + drainer core_realized/core_open_notional must be Money too, or E.1.6 inherits a Money->FPN mismatch at the FPN_AtomicAdd aggregation seam. Bonus: 16B Money is cmpxchg16b-native => Money-izing UNBLOCKS the lock-free O(1) aggregator the FPN<24B> sketch worried about.",
          "Per-subaccount rate-limit granularity (NEW, survives) — ClusterState reserves ONE rate triple (cluster/per-IP weight, correct) but Binance per-account ORDER-COUNT limits are per-API-key = per-subaccount. The SubAccountPool entry shape is unspecified; freeze it without its own rate triple and E.1.6 re-opens the pool to add per-subaccount throttle state.",
          "ExchangeEnum width-pin (MED, design-once, epoch-free) — ClusterState.exchange_id is the first boot-immutable field; pin ExchangeEnum fixed-width so late FOREACH_EXCHANGE adds never resize ClusterState.",
          "Adapter per-cluster HOME (HIGH) — own-in-ClusterState vs ClusterState-references-OrderManagerState (today adapter lives by value in OrderManagerState). E.1.6 wires a per-cluster producer/adapter/WS thread set; E.1.2 must decide at freeze or pay a layout re-lay.",
          "owner_node_id global-addressing (survives, but CONTESTED) — IF persisted, must encode {cluster_id, local_node} or a flat id wide enough for MAX_CLUSTERS x MAX_NODES so multi-cluster fee/reconcile can disambiguate node-0-of-A vs -B. BUT two lenses argue it is DERIVABLE today (one-position-per-node: owner = which portfolio array + slot) => may not need to be a persisted Position field at all; D-209 is a FORWARD reservation for the deferred flat sub-pool, not present necessity.",
          "RULED OUT of layout-freeze scope (confirmed): metadata_flags widen (.rodata, no struct constraint), per-exchange fee/precision (venue .rodata), 4-state mode (fits existing byte, see N-1 H21), topology.mode (FOREACH_GLOBAL_CFG_FIELD row), subaccount topology (pool + uint32 binding id, no persisted byte). And the leaf-map's 'snapshot epoch re-open' framing for NodeState/ClusterState — refuted; only Position carries a real epoch."
        ],
        "openQuestions": [
          "D1 (THE call) — Is Position FINAL at E.1.2, and do we pre-reserve D-206 PEAK + owner_node_id IN the wire-Position now (one PORTFOLIO 7->8 bump), OR keep Position at 128B and route PEAK/riding to OMS sibling SoA arrays? The C.5 revert precedent (PositionFieldRegistry transient per-slot state -> OMS sibling arrays, NOT wire-Position) + node_peak_balance ALREADY persisting per-node argue keep-Position-lean; the 'Position is the one real epoch surface, get it right once' argument argues reserve-now. Position is the ONLY struct genuinely under epoch threat, so this is the highest-stakes freeze decision.",
          "Is owner_node_id even needed as a persisted Position field, or derivable from (portfolio array, slot index) in the current 1-pos-per-node model? If derivable, the PORTFOLIO epoch + Position widen is AVOIDABLE entirely. Confirm derivability before paying. (Honor D-209 as a forward reservation; do not re-litigate it — just decide whether to persist now.)",
          "H22: is one-cluster=one-exchange locked FOREVER (initial-deployment convenience), or do N capital-isolated Binance subaccount-clusters need cluster_id decoupled from ExchangeEnum + per_cluster[] dimensioned by MAX_CLUSTERS? H22 (operator-ratified) says the layout must NOT foreclose N-clusters. Highest-value disagreement to force before the freeze.",
          "trading_mode: persist a per-node NodeState mode byte, OR keep it cfg-derived / boot-resolved runtime mirror (NOT serialized)? Persisting creates a second source of truth = warm-restart capital-authority divergence (operator edits cfg PAPER<->LIVE between save/restore; the snapshot byte silently overrides). It is also STAMP_BOUND — relocating storage off cfg requires re-pointing the FOREACH_STAMP_BOUND_CFG reader in the same commit or the stamp hashes a stale/zero mode. Recommended: keep cfg-derived, mode is a runtime mirror; then the claimed snapshot-epoch freeze decision evaporates.",
          "NodeState.hot.mode placement: the .hot alignas(64) cluster vs slow_account/cold? Enumeration finds ZERO hot-path mode readers across ALL remaining E leaves. .hot placement wastes hot-line working-set and (if mode is written post-boot, e.g. SHADOW) risks false-sharing invalidation => p99 spike. Two lenses recommend freezing AGAINST .hot and renaming so the name cannot later invite a per-tick read. Design-once decision E.1.6's consumer pattern is uniquely positioned to settle.",
          "Money-ize scope: extend to the WHOLE accounting set (AggregatorState per_cluster cached_*, global cached_total_*, *_kill_drawdown_threshold + drainer core_realized/core_open_notional), not just NodeState? Omitting AggregatorState leaves a Money->FPN mismatch at the aggregation seam with no re-open budget. Also unblocks the cmpxchg16b lock-free aggregator.",
          "Per-subaccount rate-limit: does the SubAccountPool entry carry its OWN rate triple (per-API-key order-count limit), or only the cluster-level triple (per-IP weight)? Under-reserving forces an E.1.6 pool re-open.",
          "Adapter HOME: own-in-ClusterState vs ClusterState-references-OrderManagerState? Must be settled at the freeze, not deferred.",
          "Fee/precision: add ClusterState.fee_maker/fee_taker (Money) + precision ints, OR record explicitly that fee/precision rides cfg/.rodata venue registry and stays OUT of the runtime struct? Decides whether ClusterState grows; all lenses agree it is venue-sourced, so the default is 'stays out + recorded'.",
          "RE-GROUND-BEFORE-FREEZE (mechanical, but blocking): the sub-master sketch predates HEAD — (a) sketch :424 enum-comment orders backtest|paper|live|shadow => backtest FIRST = RENUMBERS persisted PAPER=0/LIVE=1/SHADOW=2 (H21/Knight-Capital violation); the freeze MUST append backtest=3, never copy the sketch enum. (b) All CfgFieldRegistry.hpp offsets drifted ~+7 vs HEAD; mode enum ControllerConfig.hpp:41-43 (not :59-61, -18). (c) FPN '24-byte' money sketches + template-ClusterState/tt::submit_order<ExchangeT> dispatch all predate the landed 16B-Money + fn-pointer-adapter epochs. (d) PORTFOLIO_SNAPSHOT_VERSION #define = 7 at HEAD (header comments stale-say 6) => the bump is 7->8 (orchestrator correct; the '6->7' reading is wrong)."
        ]
      },
      {
        "leaf": "E-leaf OPT (D-229 / #9 — slow+hot branchless/compute reduction). No dedicated plan; exists as D-229/D-235/D-236 + a sub-master tracking line. Runs LATE (E.1.M-class, after E.1.2's freeze); verified layout-order-INDEPENDENT.",
        "verdict": "YELLOW — Conditionally ready. OPT imposes essentially NO structural layout coupling on E.1.2's freeze: every OPT target is verified layout-inert (#15 reads ExecutionCore<F> only, never Portfolio.positions[]) and wire-free (RollingStats serializes only derived scalars; not in the persist path), so all branch/compute reduction homes to THIS late leaf and does NOT need the freeze to land first. The leaf-map's single headline coupling — layoutNeed #1, RollingStats must be pointer-held or OPT's fixed-K restructure RED-builds the size-pin and re-opens NodeState — is a REFUTED PHANTOM on three confirmed grounds (it cites the legacy controller PortfolioController.hpp:252, not the frozen struct; the sharded path is already structurally forced pointer-held at ControllerEventLoop.hpp:274; and fixed-K adds no field so sizeof(RollingStats) is invariant). NOT GREEN, because the E.1-foundation v0.1 sketch AS-WRITTEN would silently bake re-opens (FPN money fields, omitted node_peak_balance, params-only partial-flatten of ExecutionCore) — these are MANDATORY pre-freeze sketch-corrections, not optional. Apply the frozenLayoutRequirements below and OPT is provably re-touch-free.",
        "frozenLayoutRequirements": [
          "MONEY-NOT-FPN: NodeState slow_account + AggregatorState + drainer_state money fields MUST be Money, NOT FPN<F>. v0.1 sketch (E.1-foundation.md:434-440 realized_pnl/open_notional/capital_allocated/drawdown_max; :452-454 core_realized/core_open_notional/core_fees; :684-688 aggregator) declares them FPN<F> — H4 violation. Live code already uses Money (node_open_notional ControllerEventLoop.hpp:452, node_peak_balance :467); the kill gate computes Money_Sub(node_peak_balance,current_value) in Money at :2906 — freezing FPN breaks the live Money-domain compute, not just hygiene. CONFIRMED at HEAD.",
          "PRESERVE node_peak_balance: MUST be an explicit per-node Money slow_account field. Live per-node state (ControllerEventLoop.hpp:467 'Money node_peak_balance; peak of current_value over core lifetime'), ratcheted :2901-2902 (Money_Max), consumed by MTM-kill gate :2906. It is irreducible path-dependent running-max state; drawdown_max (a lossy normalized (peak-current)/peak ratio) provably does NOT subsume it. v0.1 sketch carries drawdown_max/current but OMITS node_peak_balance — a silent omission that forces a slow_account re-open independent of D-206. CONFIRMED at HEAD.",
          "KEEP SLOW-STATE KERNELS POINTER-HELD: NodeSlowState<F>/RollingStats MUST stay out-of-line (pointer) in the frozen NodeState — already structurally forced (NodeSlowState ~272KB; 16 inline copies overflow the 8MB stack; live pointer ControllerEventLoop.hpp:274). The freeze must NOT regress to by-value embed. This pointer FIREWALL — not the refuted size-pin reasoning — is what makes OPT re-touch-free: any future RollingStats sizeof change is a local static_assert bump in RollingStats.hpp:141-144, firewalled from NodeState. (The original layoutNeed #1 'RED-builds size-pin -> re-opens NodeState' premise is REFUTED/phantom; the keep-pointer-held requirement survives as the correct-reason restatement.)",
          "EXECUTIONCORE HOMING-MODE: E.1.2 DoD MUST pick explicitly — keep ExecutionCore<F> POINTER-HELD (as today, ControllerEventLoop.hpp:267 'ExecutionCore<F>* core') OR EMBED-WHOLE (carrying the :176-185 line-0 + permission-own-line static_asserts). NEVER the v0.1 params-only partial-flatten (E.1-foundation.md:417-425 homes only params/adapter/symbol/mode/variant_id), which leaves the ~15 ExecutionCore hot fields (active/live_tp/live_sl :77-82, permission-own-line :100, seqlock trio :126-132) HOMELESS; homing them later shifts slow_account/drainer/kill offsets + sizeof(NodeState) -> NodeState re-opens. '#15 adds zero fields' is true of the BODY edit but does NOT make absorbing ExecutionCore a pure rename — absorption is a LAYOUT operation.",
          "drawdown_current / node_dd_pct: EXCLUDE from the frozen SSoT layout, or keep ONLY as an explicitly-labeled cross-thread cache (never SSoT). It is a DERIVED display field 'recomputed each rebuild' (ControllerEventLoop.hpp:468); freezing it as a seqlock-published slow_account field adjacent to its inputs (peak, current_value) is an SSoT cross-seqlock drift hazard. If kept, type it Money/decide ratio-vs-money explicitly (it is a dimensionless percent, not a money amount). The leaf-map's 'retype FPN->Money' is necessary-but-insufficient here."
        ],
        "branchReductions": [
          {
            "branch": "ExecutionCore.hpp:337-338 — `Money tp = active ? core->live_tp : core->cached_params.sg_take_profit_price;` (sl :338). The #15 hot trade-branch H20 instance.",
            "freeNowInE12": true,
            "approach": "Layout-INERT + wire-free; does NOT need the freeze (can land independently in OPT). Replace ternary with Money_BlendOnMask(live, cached, -(uint64_t)(active!=0)) — HARDEN the mask with (active!=0) rather than raw -(uint64_t)active: active is canonical-boolean today (masked &1 at :475, set by mask op :609) so both are equal now, but the harden makes byte-identity structural not contingent on a future write. Bytewise-identical to the ternary's pick: FixedPointN.hpp:1876-1879 is pure sign-extended mask-blend (a&m)|(b&~m), no saturation. PERF-NEUTRAL, NOT a p99 win: `active` flips only on entry/exit (write site ExecutionCore.hpp:609), so the je is ~100% predicted across the no-trade tick mass -> ~0 variance; the blend ADDS a guaranteed 2nd 16B load + ~6 ALU ops. Justified by H7/H20 LETTER-conformance + retiring the STALE :334-336 bench comment ('branch was 4 ns slower', pre-Ship-B 24B-Money epoch, invalidated by Ship-B 16B). New comment MUST say 'branchless for H7, perf-neutral predicted branch' + a fresh PMU number — NEVER 'faster' (re-spawns the stale-derived-fact class)."
          },
          {
            "branch": "RollingStats.hpp:325/334/343 — three monotonic-deque back-pop while-loops (min FPN_GreaterThan, max/vmax FPN_LessThan) over min_dq[W]/max_dq[W]/vmax_dq[W]. The only true H11 (constant-iter) violation in the slow probe set.",
            "freeNowInE12": true,
            "approach": "DO NOT fixed-K restructure — REFUTED as net-negative. The monotonic-deque invariant requires popping ALL 'worse' entries: K<W is CORRECTNESS-BREAKING (retains stale entries -> wrong price_min/max/volume_max); the only correct fixed-iter form is K=W, which converts amortized-O(1) (comment :322-324) into UNCONDITIONAL O(W) — at default W=128 that is ~384 FPN ops/push vs amortized ~3 (~100x mean regression on the binding slow path) AND amplifies the 14-spill register pressure (longer live ranges) that may itself be the real p99 driver. Worst-case O(W) tail is sub-budget vs the 100us slow ceiling. Correct disposition: ALLOWLIST with the documented amortized-O(1) rationale (already at :322-324). Layout-inert/wire-free confirmed."
          },
          {
            "branch": "RegimeDetector.hpp:593-605 — 4-way discrete regime cascade (trending/volatile/ranging precedence).",
            "freeNowInE12": true,
            "approach": "OPTIONAL / cosmetic — low priority. Hysteresis-stabilized (committed regime persists across slow cycles) -> predicted branches, ~0 variance, 489 instr / 0 spills, vastly sub-budget. IF done: branchless MASK-SELECT of precomputed regime CODES preserving EXACT 4-way precedence — NOT a 1-D score-table LUT (precedence/tie-break-fragile vs the SESSION_BY_HOUR[24] template; use TD-040 as TEMPLATE only). DETERMINISM-LOAD-BEARING: regime codes are wire-persisted (ShardedSnapshotPersist version 10) + warm-restart state -> bytewise-identical classification gate + NO regime-code renumbering (H21). GATED on resolving the RegimeDetector.hpp:571 H4 question first."
          },
          {
            "branch": "The 24 other grandfathered data-dependent-warm branches (D-235/D-236), incl. ExecutionCore.hpp:504/516/542/548/557 (bandit/ensemble arm-dispatch), :315 seqlock-miss, :454/:657 H20-exception variants.",
            "freeNowInE12": true,
            "approach": "CURATION not reduction (analyzer/allowlist job, no code edit, no layout). Convert each KNOWN_COHORT entry -> curated-allowlist-with-warmup-rationale OR reduced-to-branchless. The :504/516/542/548/557 set ARE bandit/ensemble arm-dispatch -> KEEP allowlisted, do NOT reduce in isolation (overlaps deferred TECH_DEBT-174 bandit work). This curation IS the leaf's reason to exist (D-236 explicitly homes curation+reduction on OPT, not E.1.0 which only built the report-only baseline)."
          }
        ],
        "techDebt": [
          "D-235 + D-236 — the 25 grandfathered data-dependent-warm branches; curation + reduction IS OPT's charter (the leaf's reason to exist, not adjacent debt). REFRAMED by adversarial lens: the deliverable is letter-conformance (H7/H20/H11) + curation, NOT p99 wins — all three primary branches are predicted/slow-path/sub-budget.",
          "#15 hot trade-branch H20 (ExecutionCore.hpp:337-338); subsumes at ~0 marginal cost the stale doc-debt at :334-336 ('4ns slower' bench, pre-Ship-B-invalidated) — rewrite with mask-blend + perf-neutral rationale + fresh PMU when #15 lands.",
          "D-229 RollingStats H11 doc-debt — surviving but disposition flips to ALLOWLIST (not fixed-K). RE-POINT the STALE cite: decision-log 'RollingStats.hpp:187 O(W)' is stale (:187 is now deque-head init); the live loops are :325/334/343. Verify the doc-debt re-points at the live loops.",
          "Regime_Classify -> branchless-mask-select (RegimeDetector.hpp:593-605) using CLOSED TECH_DEBT-040 (SESSION_BY_HOUR[24]) as TEMPLATE only — TD-040 NOT re-subsumed/re-closed. Optional/cosmetic.",
          "NOT SUBSUMED — F-096 / TECH_DEBT-167 (partial-exit leg-split): CONFIRMED live `double full_qty = Money_ToDouble(...)` at EngineSharded/Async.hpp:842, double mul :847/849, requantized via money_from_double_payload — a live H4 + money-round-trip-through-double violation. This is an E.1.2 Decision-K money-ize (layout-coupled; reads state.nodes[slot].intended_qty + cfg partial_exit_pct), NOT an OPT branch-reduction. .E.1 cannot close with a surviving double money field — and that gate is E.1.2's, not OPT's. MIS-HOME risk if filed under OPT.",
          "NOT SUBSUMED — TECH_DEBT-174 bandit (+ A11/A12, adaptive-entry D-211): the :504/516/542/548/557 gate-compares are bandit/ensemble arm-dispatch; OPT's reduction OVERLAPS but does not subsume the bandit redesign. Cross-link + fold-the-overlap-when-scaffolded, or keep allowlisted and defer to the bandit leaf.",
          "NOT SUBSUMED — EventLoop_RebuildOneCore conformance-gate COVERAGE HOLE: largest slow consumer yet ABSENT from the 7-probe budget set; D-229 defers the rewrite (orchestrator not a kernel; large blast radius; not a clean golden-master unit). Surface the coverage hole here; do NOT fold the rewrite into OPT.",
          "NEW / refute-gated (no formal TD today) — RegimeDetector.hpp:571 `int weight = (int)FPN_ToDouble(cfg->regime_model_weight)` (CONFIRMED at HEAD; model_score!=0 arm :569-573): OPT closes it as an H4 fix ONLY IF the refute confirms a real feature-path crossing needing a named Money_*/integer-LUT seam; leave if legit display/heuristic integer weight."
        ],
        "realignRisks": [
          "MONEY-NOT-FPN re-cascade (SURVIVES): freezing slow_account/aggregator/drainer money fields as FPN<F> per the stale v0.1 sketch bakes an H4 violation OPT's later aggregator/kill-compute reads inherit AND breaks the live Money-domain compute at ControllerEventLoop.hpp:2906. Corrected-shape: fix the sketch to Money BEFORE the freeze.",
          "node_peak_balance SILENT OMISSION (SURVIVES): v0.1 slow_account carries drawdown_max/current but omits node_peak_balance (live :467, kill gate :2906). drawdown_max does not subsume it -> omission forces a slow_account re-open independent of D-206.",
          "ExecutionCore PARTIAL-FLATTEN homelessness (SURVIVES — the real FIT re-open trap, restating layoutNeed #3 backwards): the v0.1 params-only NodeState hot cluster homelesses the ExecutionCore hot fields; homing them later shifts downstream offsets + sizeof(NodeState) pin -> NodeState re-opens. Safe shapes = pointer-held OR embed-whole; the partial-flatten is the one shape that forces the re-touch. Resolve homing-mode at freeze.",
          "STALE LAYOUT COMMENT ported into the freeze (SURVIVES, CONFIRMED): ExecutionCore.hpp:62-85 narrates 'Money=24B', 'live_sl spanning 56-80', 'CMOV at hot path lines ~287-288', '24+24+8pad=64B' — all pre-Ship-B WRONG (Money is 16B; ternary is at 337-338). The static_asserts :176-185 use sizeof(Money) so they hold, but if E.1.2 absorbs/relocates this cluster by reading the PROSE offsets it bakes a wrong derived offset (the exact D-229 derived-facts-drift class). Absorb from sizeof/offsetof NEVER the comment; rewrite prose to 16B. (The :334-336 BRANCH comment fix rides OPT; this LAYOUT comment fix rides E.1.2.)",
          "Position 128B-vs-192B (SURVIVES but OUT-OF-OPT-LEAF — D-206/D-209's freeze ask, not D-229's): freezing Position at 128B (TP@0/SL@16/qty@32/entry@48, PORTFOLIO_SNAPSHOT_VERSION=6, CONFIRMED) without reserving D-206 {peak_price, riding_flag}(+owner_node_id) risks a 2nd PORTFOLIO wire bump + a full A1 warm-restart re-arm re-verification when D-206 lands. OPT's ONLY input — peak/riding cache-line placement — is REFUTED as a HOT constraint: D-206's decided design keeps peak-tracking SLOW-path + routes give-back through the EXISTING ratchet_sl (hot SG reads effective_sl=Money_Max(sl,ratchet_sl), never reads peak), so peak/riding go COLD (Position line 1/2), NOT a hot single-line {TP,SL,peak,riding} carve. The hot-regression realign risk is therefore a phantom; the reserve-now-vs-defer call survives as the D1 open question.",
          "#15 <-> D-206 SEQUENCING (DOWNGRADED, mostly refuted): the leaf-map feared D-206's exit redesign reshapes the same :337-338 SG TP/SL select. But D-206's cheapest-struct design is ZERO hot-gate change / ZERO ExecutionCore growth (routes give-back via ratchet_sl). If that holds, the :337-338 select is NOT reshaped and #15 can land independently with no re-derivation. Residual: confirm against D-206's FINAL design before landing #15 (see openQuestions)."
        ],
        "openQuestions": [
          "D1 (Position FINAL vs pre-reserve D-206 peak — the operator decision this leaf most surfaces): freeze Position at 128B now, or pre-reserve {peak_price, riding_flag}(+owner_node_id) to 192B per D-209 'Position storage opens ONCE' (decision-log:1288)? TENSION TO RESOLVE: refute-1 argues a trailing-SL give-back is fully expressible as a ratcheted SL level (ratchet_sl IS the materialized trailing state) -> defer the reserve + freeze 128B. refute-5 argues (CONFIRMED at HEAD) node_peak_balance is a peak of EQUITY current_value (:467) for the MTM kill, NOT a per-position price/MFE peak, so it CANNOT subsume D-206's give-back-from-peak input -> the 128B-MOOT hedge is code-false and the 192B reserve is the design-once path. OUT-OF-OPT-LEAF (D-206/D-209 own the call); OPT only asks: IF reserved, place peak/riding COLD (line 1/2), never a hot single-line carve.",
          "ExecutionCore homing-mode at the freeze: pointer-held (as today) vs embed-whole — E.1.2 DoD must decide explicitly; the v0.1 partial-flatten is forbidden either way.",
          "drawdown_current / node_dd_pct disposition: EXCLUDE from the frozen layout (it is derived, recomputed each rebuild :468) or keep ONLY as a labeled cross-thread cache (never SSoT)? And decide its type explicitly (dimensionless percent vs Money).",
          "#15 <-> D-206 sequencing: confirm D-206's FINAL exit redesign does NOT reshape the ExecutionCore.hpp:337-338 SG TP/SL select (refute-5 says it routes via ratchet_sl with zero hot-gate change). If confirmed, #15 lands independently; if D-206 DOES reshape the select, sequence #15 AFTER D-206 to avoid re-deriving the mask-blend.",
          "RegimeDetector.hpp:571 H4: is regime_model_weight a legit display/heuristic integer weight (analyzer-exempt) or a real feature-path crossing needing a named Money_*/integer-LUT seam? Gates both whether OPT closes it as an H4 fix and whether the Regime mask-select proceeds.",
          "Snapshot-version arithmetic re-grounding: the orchestrator's 'PORTFOLIO 7->8' premise is STALE — HEAD is PORTFOLIO_SNAPSHOT_VERSION=6 and SHARDED_SNAPSHOT_VERSION=10 (CONFIRMED). OPT is wire-free so this does not gate OPT, but E.1.2's bump arithmetic must use the actual HEAD constants (H21 append-only) before any freeze bump."
        ]
      },
      {
        "leaf": "D-206 — exit trailing-profit redesign (ride-past-TP → ratchet SL UP → give-back-from-peak; the Position peak/MFE field). Layout-readiness for the E.1.2 Position/NodeState freeze. Grounded at HEAD 0ee227a.",
        "verdict": "YELLOW — Position layout is NOT yet safe to freeze as the leaf-map specifies. The structural survivors (riding-flag as a named zero-init field, version threading 7→8/10→11, warm-restart A1 re-arm extension, exit_bandits MAX-sizing, ExitRecord-MFE separation) are sound and code-confirmed, BUT the single most consequential freeze decision — the D1 'is Position FINAL / reserve a 16B peak' call — is genuinely contested by a mechanically-grounded refutation (peak may not be a necessary PERSISTED Position field; route give-back through the existing persisted stop_loss_price Money_Max floor channel, precedent PortfolioController.hpp:684-685). Freezing the wrong choice = the exact second-wire-break E.1.2 exists to prevent. Not RED: the leaf is workable and the open set is small + enumerable. Not GREEN: peak-vs-floor + the leg model + the persist-serialization shape (AoS-record vs SoA-column) must be settled BEFORE the freeze, and several map mechanics (free GATE_FLAG bit; give-back as a clean ratchet-writer sibling; 192B AoS geometry) are refuted/stale.",
        "frozenLayoutRequirements": [
          "riding-mode flag — per-position AND per-leg, PERSIST. Fits the existing uint8_t _pad_pos[7] @ Portfolio.hpp:70 at ZERO incremental size, BUT must be promoted to a NAMED =0-init field (H12), not bare pad: _pad_pos[7] today has no initializer, is never written in Position_Reset (Portfolio.hpp:221-231) or any Portfolio_OpenSlot (:359/:393), yet sits INSIDE the POSITION_PERSIST_BYTES prefix — already a latent uninitialized-wire-byte hazard. E.1.2 makes it load-bearing: the flag MUST be explicitly zeroed in Position_Reset + every OpenSlot/AddPosition site or a reused slot inherits prior riding state (A28 slot-reuse class).",
          "owner_node_id (D-209, I3) — 1 byte, same _pad_pos region, SAME named-zero-init + reset-at-all-sites requirement (node id 0 is a REAL node — no pad-carryover reasoning). Co-opens with this freeze; the 3-way co-open (D-55 SoA + D-206 + D-209) IS the E.1.2 freeze contract per decision-log:1288.",
          "PRESERVE original_tp (Money @ offset 80, Portfolio.hpp:127) — the SL ratchets TO this; already present. PRESERVE entry_price (offset 48) — needed for the D-253-owed runtime loss-floor clamp.",
          "NodeState.hot.params (seqlock GateParameters): ZERO new hot fields ONLY UNDER a leg-SHARED riding decision (reuse existing GATE_FLAG_TP_ENABLED clear; ratchet_sl/ratchet_tp already exist, written Strategy_WriteRatchetSL/TP @ StrategyLifecycle.hpp:284/311, consumed effective_sl/tp=Money_Max @ ExecutionCore.hpp:420/427). Per-LEG riding instead requires a NEW per-leg seqlock field (ratchet_tp_b / per-leg riding bits) → re-opens the frozen cross-thread struct. The freeze MUST pin the leg model first; it cannot lock hot.params until that call is made.",
          "exit_bandits slow arm-array MUST be sized from the registry as MAX(strategy arms, planned learned-ratchet arms), NOT a current-consumer count — fixed-N freeze locks out learned-ratchet arms and re-opens NodeState (the exact failure E.1.2 exists to prevent). Enumerate planned ratchet-arm cardinality AT the freeze. [MED]",
          "Warm-restart re-arm loop @ ShardedSnapshotPersist.hpp:651-679 MUST be extended to thread the riding/give-back-floor state AND perform an IMMEDIATE re-derive: the loop today recomputes live_tp/live_sl from entry×(1±ResolvePerFillTpPct) and overwrites the persisted TP/SL, and ratchet_sl/ratchet_tp live in EPHEMERAL cached_params — so a restored riding position loses its ratcheted protective stop until the next slow cadence unless re-derived in-loop (A1 silent-give-back class).",
          "Version threading: PORTFOLIO_SNAPSHOT_VERSION 7→8 (Portfolio.hpp:534 — actual #define is 7; the '=6' header prose @ :45/:55/:68/:95/:134/:568 is STALE, fix opportunistically) and SHARDED_SNAPSHOT_VERSION 10→11 (ShardedSnapshotPersist.hpp:94). D-206 persisted state rides E.1.2's EXISTING bump = 1 epoch; deferring = 2 epochs (H21).",
          "PositionEntryArgs Class-18 replay mirror @ Portfolio.hpp:343 — any new persisted Position field (peak if retained, riding-flag, owner_node_id) MUST be mirrored + populated at both Portfolio_OpenSlot sites (:359/:393) and zeroed in Position_Reset (:227), else replay/backtest positions start with zero state (train/serve parity gap).",
          "IF a 16B peak field is retained (pending the D1 open question): it MUST be a FOREACH_POSITION_FIELD PERSIST registry row appended BEFORE the manual _pad_pos (so it lands inside the POSITION_PERSIST_BYTES prefix and both persist paths carry it via offsetof/sizeof auto-tracking) — NEVER 'after _pad_pos' (would fall outside Portfolio_Save's prefix → silently dropped on the legacy/backtest path → A1 re-arm at original_tp). AND the trailing alignas pad created by growth (bytes ~151..191 at 192B) MUST be a named uint8_t _pad_tail[N]={0} field OR both writers must switch to POSITION_PERSIST_BYTES() — else ShardedSnapshot's sizeof-fwrite serializes indeterminate trailing bytes (H12/H9 cross-run determinism). MFE-for-REWARD belongs in ExitRecord (snapshotted at exit), separable from any per-tick persisted Position field."
        ],
        "branchReductions": [
          {
            "branch": "Position struct/column growth (peak/riding/owner) adding a hot-path branch or field",
            "freeNowInE12": true,
            "approach": "Hot-path-INERT and confirmed: rg 'positions[' ExecutionCore.hpp returns nothing; the per-tick SG gate reads core->live_tp/live_sl/live_tp_b/live_sl_b + cached_params.* only (327-478). Growth cannot grow hot working-set or add a hot branch. CAVEAT (Refute-2 N1): 'hot-inert' covers Position GROWTH only — it does NOT cover D-206's per-leg ride SIGNAL, which lives in the seqlock GateParameters, not positions[]."
          },
          {
            "branch": "Give-back stop via the existing monotonic-up Money_Max floor channel (no new hot field)",
            "freeNowInE12": true,
            "approach": "The hot gate already consumes the floor branchlessly: effective_sl=Money_Max(sl,ratchet_sl) @ ExecutionCore.hpp:420 → sl_hit_a. Give-back = one more SLOW-path floor writer; zero hot-gate growth. BUT NOT a clean drop-in sibling of EventLoop_TrailingSLRatchetOneCore: (a) confirmed @ ControllerEventLoop.hpp:3663-3667 that writer CAPS the floor DOWN to entry×(1-3·fee_taker) → a give-back inheriting it can never lock gains above ~breakeven (opposite of riding's intent) → needs its own clamp design; (b) that whole path is double (FPN_ToDouble/Money_ToDouble/new_ratchet_d/sl_floor_d → money_from_double_payload) = a slow-path H4 violation D-206 must Money-ize, not inherit."
          },
          {
            "branch": "Ride-past-TP suppression with zero new hot field (clear existing GATE_FLAG_TP_ENABLED)",
            "freeNowInE12": true,
            "approach": "Slow path clears GATE_FLAG_TP_ENABLED in pending_params.flags → tp_enabled=0 zeros the TP term (sg_fires_a=(tp_enabled&tp_hit_a)|(sl_enabled&sl_hit_a) @ ExecutionCore.hpp:431); no new field/branch; H7/H20-clean (alt hot 'if(riding)' on a Position read = Class-28). REFUTED ALT: the leaf-map's 'new GATE_FLAG_RIDING bit (free 0x20/0x40/0x80)' does NOT exist — confirmed all 8 bits assigned @ GateParameters.hpp:38-79 (0x20/0x40/0x80 = BUY_BLOCKED/PAIR_ACTIVE/STALENESS_ENABLED). A new bit widens flags → re-opens the frozen seqlock struct."
          },
          {
            "branch": "Per-leg independent riding (HAZARD F4) — the one place D-206 could GROW the hot/seqlock struct",
            "freeNowInE12": false,
            "approach": "tp_enabled is PER-CORE, leg-shared (confirmed by the :3678 comment 'pending_params is per-CORE … Both legs share it under partials'); leg B reuses the same mask @ ExecutionCore.hpp:451. Clearing GATE_FLAG_TP_ENABLED to ride leg A ALSO disables leg B's TP2 hard-exit; a single GATE_FLAG_RIDING bit has the identical leg-shared limit. Per-leg riding ⇒ NEW per-leg seqlock GateParameters state (×3 embedded) → re-opens the exact struct E.1.2 freezes. DECIDE THE LEG MODEL BEFORE FREEZING."
          },
          {
            "branch": "ExecutionCore.hpp:337/338 live-vs-cached ternary — fold into D-206 as a branch-reduction win",
            "freeNowInE12": false,
            "approach": "Do NOT fold and do NOT claim a branch-reduction win. The leaf-map itself correctly calls this ADJACENCY-not-subsumption (keep on its own leaf). Refute-3 strengthens: 'active' flips ~2×/position-lifecycle ⇒ ~100%-predicted, ~0-variance branch; the proposed Money_BlendOnMask (6-8 ALU) ADDS steady work and is heavier than the paired-cmov GCC already emits for the structurally-identical Money_Max __int128 ternary (FixedPointN.hpp:1854) used hot @420/427. At best a stale-comment fix; asm-verify (je vs paired cmov) before any conversion — NOT a p99 win."
          }
        ],
        "techDebt": [
          "S1 [load-bearing, SURVIVES]: 'Position-extension ⇒ snapshot-version-bump' class (PositionFieldRegistry.hpp:30-33) subsumed by E.1.2's already-paid bump (PORTFOLIO 7→8, SHARDED 10→11 + H21 tombstone + check_identifier_retirement --update + size-pin) — IF D-206's persisted state rides THIS bump. Marginal cost = the registry rows; deferral re-opens the entire cascade (2nd bump, 2nd tombstone, 2nd warm-restart re-arm, 2nd reconciliation). Drives the D1 decision.",
          "S3 [M7 structural-enforcement candidate, SURVIVES + UNGUARDED-confirmed]: check_struct_size_budget 'sizeof-changed ⇒ version-bump' guard GAP (decision-log:1274). Confirmed there is NO sizeof(Position)↔version static_assert — the only version tripwires are trait-keyed (is_fp_decimal_v→VERSION≥7 @ Portfolio.hpp:540-544; MONEY_ENCODING_EPOCH @ ShardedSnapshotPersist.hpp:99-101). Size-pin RED-builds but nothing FORCES the version bump = a silent-cascade hole. E.1.2's bump exercise is the natural wiring home.",
          "S2 [DEMOTED — NOT a clean subsumption]: is_trailing derived-facts-drift (EngineTUI.hpp:273/:1556 derive; :1935 + DashboardPanels.hpp:1331 read = 4 GUI consumers). Contested: Refute-1 says NOT subsumed (display-only; give-back writes the SL FLOOR, never take_profit_price, so tp!=original_tp is never triggered → derivation unaffected). Refute-4 NEW-3 says it is instead a CORRECTNESS REQUIREMENT for warm-restart classification consistency (persisted ratcheted TP is recomputed-away at restart → is_trailing reads FALSE for a riding position). Net: NOT a by-necessity subsumption — treat as adjacent/conditional, its disposition depends on the peak-vs-floor channel choice.",
          "ADJACENT (correctly NOT D-206-subsumed): F-096 leg-split double (TD-167/TECH_DEBT-168) @ CoreFrameworks/EngineSharded/Async.hpp:842-851 (still double/Money_ToDouble/order_qty_d at HEAD); D-253 owed slow-path runtime loss-floor clamp; TECH_DEBT-072/188 (D-209 owner-model). These INTERACT (open-Q #3 / Position-opens-once coordination) but are separately homed.",
          "OPPORTUNISTIC doc-fix (SURVIVES, all refutes agree): Portfolio.hpp:45-142 header prose says VERSION=6 while :534 #defines 7, and the :97-116 layout comment is stale (old geometry). E.1.2 edits this file for the bump → fix in the same pass."
        ],
        "realignRisks": [
          "R1 [CRITICAL but CONDITIONAL on the peak-vs-floor OQ]: deferring a needed persisted peak forces a SECOND full cascade (PORTFOLIO 8→9, SHARDED 11→12, re-thread warm-restart, re-mirror PositionEntryArgs, invalidate all snapshots written between E.1.2 and D-206). SURVIVES under refutes 2/4/5 IF peak is the persisted give-back anchor; Refute-1 attacks the PREMISE (persist the FLOOR via stop_loss_price, not the peak → no field, Position stays 128B). Resolve the OQ; the design-once cost is real ONLY if peak is retained.",
          "Warm-restart A1 [HIGH, SURVIVES + STRENGTHENED — all refutes converge]: ShardedSnapshotPersist.hpp:651-679 recomputes live_tp/live_sl from entry×(1±pct) and OVERWRITES the persisted TP/SL; ratchet_sl/ratchet_tp are EPHEMERAL (cached_params, not persisted). A restored riding position loses its ride AND its tighter protective stop until the next slow cadence. D-206 MUST extend this loop with an IMMEDIATE re-derive, not next-cadence.",
          "H12 trailing-pad / uninitialized-wire-byte [HIGH, SURVIVES — supersedes the map's R2 framing]: the map's 'two divergent geometries → torn snapshot' is mis-framed (Portfolio_Save uses POSITION_PERSIST_BYTES prefix @ :570-573, ShardedSnapshot uses sizeof @ :169/:394 — two INDEPENDENT self-consistent files, both auto-track via offsetof/sizeof). The REAL hazard: at 192B the trailing alignas pad has no field/initializer and sizeof-fwrite serializes it → cross-run/HMAC determinism break, masked today only because 128B = 2 lines exact. Remedy: name the pad =0 OR make both writers use POSITION_PERSIST_BYTES. Independent of AoS/SoA.",
          "_pad_pos already-uninitialized [HIGH, SURVIVES — Refute-4 NEW-1]: _pad_pos[7] (Portfolio.hpp:70) has no initializer, is written by no code, yet is INSIDE the persist prefix. Tucking riding-flag + owner_node_id there makes it load-bearing → MUST become named =0 fields zeroed at Position_Reset + every OpenSlot, else slot-reuse inherits prior riding/owner (A28 class). The map treats _pad_pos reuse as 'free'; it is not — it is a hazard to convert at the freeze.",
          "Leg-split conservation [HIGH, SURVIVES — Refute-4 NEW-2]: the map's 'Money-ize THEN add legA+legB==intended assert' will red-trip on legit inputs — the two legs are independently rounded half-even (Async.hpp:846/848) → sum differs from intended by up to 1 ulp. Per single-source-the-computation: legB MUST be DERIVED as Money_Sub(intended_qty, legA_qty), not independently rounded. The two-step prescription needs this refinement to make the assert non-vacuous AND deterministic.",
          "exit_bandits fixed-N freeze [MED, SURVIVES]: re-opens NodeState if learned-ratchet arm cardinality is not enumerated + reserved now.",
          "ExitRecord second frozen layout [MED, SURVIVES + reinforced]: ExitRecord (Portfolio.hpp:166-183) is a SECOND frozen layout NodeState-SoA does not name; the MFE/give-back-reward peak belongs HERE (snapshotted at exit), not necessarily in the per-tick Position. If it needs an MFE field, 'Position opens once' is satisfied but the exit-record layout still re-opens (partial design-once). Settle ExitRecord-MFE at the freeze.",
          "R3 [MED, SURVIVES]: D-55 SoA makes a future column-insert structurally cheaper (no offset shuffle) but does NOT remove the wire-format/version break or the warm-restart re-thread — the expensive parts of R1. 'just add the column later' does NOT rescue a deferred persisted field."
        ],
        "openQuestions": [
          "[D1 — THE freeze-blocking call, contested] Peak-vs-floor channel: reserve a 16B persisted Money peak in Position (leaf-map + decision-log:1277 → Position grows / new SoA column) OR route give-back through the EXISTING persisted stop_loss_price Money_Max floor channel (Refute-1; precedent CONFIRMED @ PortfolioController.hpp:684-685 ratchets persisted stop_loss_price=Money_Max(...)) and keep Position at 128B, with peak-for-REWARD living in ExitRecord? Mechanically the floor accumulator Money_Max(floor, price×(1-give_back_pct)) EQUALS peak×(1-give_back_pct) without storing peak, and persisting the floor is restart-strictly-better (zero re-derive window, kills the A1 subtlety). This single decision sets whether Position is FINAL at 128B or grows — resolve BEFORE freeze.",
          "[Give-back clamp — depends on D1] Can give-back share ratchet_sl/stop_loss_price, or does it need its own channel? CONFIRMED the existing writer caps the floor DOWN to entry×(1-3·fee_taker) @ ControllerEventLoop.hpp:3663-3667 → a sibling give-back can never lock gains above ~breakeven, the opposite of riding's intent. The map's 'one more ratchet_sl writer (sibling of EventLoop_TrailingSLRatchetOneCore)' glosses a fundamentally different clamp design.",
          "[Leg model — F4/F5] Leg-SHARED riding (zero new hot/seqlock field, reuse GATE_FLAG_TP_ENABLED clear, but legs A/B cannot ride independently — contradicts the leaf's stated per-leg requirement) vs PER-LEG riding (NEW per-leg seqlock GateParameters field → re-opens the frozen hot struct). Plus F5: riding MUST be armed PROACTIVELY (slow-path, threshold BELOW TP) before the hot gate fires tp_hit; make 'arm-before-TP' an explicit acceptance criterion and challenge it under a single gap-up tick jumping past both arming and TP. Decide BEFORE freezing NodeState.hot.params.",
          "[Persist serialization shape — settles whether R2/H12-pad apply] Does E.1.2's AoS→SoA pivot also convert snapshot serialization to a column-walk? If yes, the prefix-vs-full divergence + the AoS _pad_pos/trailing-pad mechanics dissolve (Refute-5) and peak/riding/owner become column arrays (cost = 1 registry row each, NOT a free _pad tuck). If persist stays AoS-record fwrite, the H12 trailing-pad naming + both-writer byte-count reconciliation survive. The map reasons in AoS everywhere despite I2 conceding SoA — pin the serialization shape at the freeze.",
          "[H4 Money-ization scope] The ratchet path (ControllerEventLoop.hpp:3626-3673, all double — CONFIRMED) and the F-096 leg-split (Async.hpp:842-851, still double — CONFIRMED at HEAD) are slow-path H4 violations. E.1.2's subplan (...E.1.2-nodestate-soa-layout.md:75) and decomp-D (:47) STALE-ASSERT F-096 is already Money-ized — WRONG vs HEAD. E.1.2 scope is 'Money-ize THEN assert', not 'assert only'. Flag to a-class/orchestrator to re-scope before freezing.",
          "[exit_bandits cardinality] Enumerate the planned learned-ratchet arm count NOW to MAX-size the arm-array, else NodeState re-opens (defeats 'open once').",
          "[ExitRecord-MFE] Settle whether ExitRecord needs a peak/MFE reward field at the freeze — it is a second frozen layout the NodeState-SoA row does not name; under Refute-1 this is where peak actually lives."
        ]
      }
    ],
    "global": {
      "frozenLayoutSet": [
        "MONEY-TYPING (CONFIRMED-NEEDED; needed by E.1.3/E.1.4/E.1.5/E.1.6/OPT — universal): ALL slow_account + drainer_state + AggregatorState money fields freeze as Money (FixedPoint<10,8>, 16B), NEVER FPN_Binary<F>. The foundation sketch (E.1-foundation.md:434-440/452-454/684-688) STALE-types these FPN<F> — that is an H4 violation + a SILENT encoding epoch (FPN binary-scale 2^F vs Money decimal-scale 10^8, both 16B so sizeof static_asserts CANNOT catch the reinterpretation) + a D-110 warm-restart catastrophe against the already-Money-typed persisted bytes (ShardedSnapshotPersist.hpp:187-213). Live code is already Money. Negative-cost. Set MUST enumerate the WHOLE accounting surface: per_cluster[].cached_realized_pnl/open_notional/drawdown_current, global cached_total_*, *_kill_drawdown_threshold, drainer core_realized/core_open_notional — not just NodeState.core_fees.",
        "PER-NODE account fields in slow_account (CONFIRMED-NEEDED; E.1.3/OPT): ONE realized_pnl + ONE open_notional + ONE node_peak_balance (running Money_Max, irreducible path-dependent) + allocated_balance — all Money, all RELOCATIONS of existing per-node fields (ControllerEventLoop.hpp:411/452/467), NOT triple-stored. node_peak_balance is silently OMITTED from the sketch and is consumed by the live MTM-kill gate (:2906) — its omission alone forces a slow_account re-open.",
        "node_peak_balance is EQUITY-peak NOT per-position price-peak (CONFIRMED; bears on D1): it is Money_Max of equity current_value for the MTM kill (:467), so it does NOT and CANNOT subsume D-206's per-position give-back-from-MFE anchor. The D1 peak question is a SEPARATE field from this one.",
        "DROP drawdown_max + drawdown_current (CONFIRMED-REFUTED as fields; E.1.3/E.1.5/OPT): drawdown_max has zero consumers anywhere; drawdown_current/node_dd_pct is derivable on-the-fly ((peak-balance)/peak, live form ControllerEventLoop.hpp:3261-3266, recomputed each rebuild). Freezing two reader-less Money fields violates necessity. If kept at all, ONLY as an explicitly-labeled cross-thread cache (never SSoT, decide ratio-vs-money type).",
        "SEQLOCK pack (CONFIRMED-NEEDED; E.1.3): account_state_seqlock as FIRST field of slow_account, counter-first ordering, whole cluster alignas(64) (H6), reader double-read-with-parity. DETERMINISM REQUIREMENT: the seqlock counter + struct _padding MUST be SKIP_PERSIST in the FOREACH_OMS_FIELD persist walk, else the runtime-varying counter leaks into the wire and breaks warm-restart byte-stability (H9/H12).",
        "owner_node_id on Position (CONFIRMED-NEEDED at E.1.2; D-209; needed by E.1.3 reconcile, E.1.5 aggregator, E.1.6 multi-cluster — but A21/E.1.4 framing of 'REQUIRED' is REFUTED, it is decodable from order_id>>60): byte-FREE in existing _pad_pos[7] @ offset 121 (sizeof(Position)==128 unchanged, zero offset cascade). MUST be a NAMED =0-init PERSIST-kind field (node id 0 is a real node — no pad-carryover), zeroed in Position_Reset + EVERY OpenSlot/Add/bypass site (time-exit Submit, Portfolio_OpenSlot/Add, partial-leg-B Async.hpp:869), mirrored in PositionEntryArgs, round-trip-verified in BOTH serializers. WIDTH BLOCKED on the cap-symbol SSoT below.",
        "CAP-SYMBOL SSoT (CONFIRMED-NEEDED, HARD pre-freeze blocker; E.1.5/E.1.6): NUM_EXCHANGES / MAX_NODES_PER_CLUSTER / MAX_CLUSTERS do NOT exist in code (Limits.hpp has only MAX_EXECUTION_NODES=16, MAX_PORTFOLIO_POSITIONS=16). owner_node_id width AND every cluster-sized array (ClusterState.nodes[], per_cluster[], per_cluster_kill[]) literally cannot be frozen without them. Define with MAX_NODES_PER_CLUSTER<=256 AND NUM_EXCHANGES<=256 so a uint16_t (cluster<<8|node) owner_node_id is correct.",
        "cluster_id DECOUPLED from ExchangeEnum (CONFIRMED-NEEDED; E.1.6; H22): dimension all cluster-indexed arrays by MAX_CLUSTERS NOT NUM_EXCHANGES; binding.cluster_id is the routing index, never the FOREACH_EXCHANGE index. The sketch's per_cluster[NUM_EXCHANGES] / cluster_id==ExchangeEnum is a baked H22 hard-invariant violation (breaks the instant cluster_count>NUM_EXCHANGES, e.g. 2 Binance subaccount-clusters).",
        "ClusterState adapter = tt::ExchangeAdapter<F> fn-ptr table BY VALUE (CONFIRMED-NEEDED; E.1.6): size-invariant (ExchangeAdapter.hpp:84-103), NEVER template<typename AdapterT> struct ClusterState (sketch :620 — heterogeneous sizes make a homogeneous per_cluster[] structurally impossible). Landed OrderManagerState model is already correct. Plus ExchangeEnum pinned fixed-width (uint8_t-backed), exchange_id carried as a FIELD.",
        "ExecutionCore HOMING-MODE pinned (CONFIRMED-NEEDED; OPT/E.1.3): E.1.2 DoD MUST pick pointer-held (as today, ControllerEventLoop.hpp:267) OR embed-whole (carry the :176-185 static_asserts). The v0.1 params-only PARTIAL-flatten (homes only params/adapter/symbol/mode) leaves ~15 ExecutionCore hot fields (active/live_tp/live_sl :77-82, permission-own-line :100, seqlock trio :126-132) HOMELESS; homing them later shifts slow_account/drainer/kill offsets + sizeof(NodeState) → NodeState re-opens. '#15 adds zero fields' is true of the BODY but absorption is a LAYOUT op.",
        "NodeSlowState/RollingStats POINTER-HELD (CONFIRMED-NEEDED; OPT): structurally forced (~272KB; 16 inline copies overflow 8MB stack). This pointer firewall — not the refuted size-pin reasoning — is what makes OPT re-touch-free: any future RollingStats sizeof change is a local static_assert bump firewalled from NodeState.",
        "NodeState.hot.mode NOT a writable per-node authority field (CONFIRMED-NEEDED; E.1.5/E.1.6): hot path reads mode ZERO times (grep ExecutionCore.hpp = 0) → .hot placement is a DOD error AND a Class-47 split-brain hazard. Freeze as DELETED (simplest) OR relocate to boot-immutable binding block as read-only mirror. Resolve split-brain at LAYOUT not convention.",
        "trading_mode STAYS in ControllerConfig as STAMP_BOUND SSoT (CONFIRMED; E.1.5/E.1.6): CONFIRMED STAMP_BOUND | STAMP_BOUND_CFG_DERIVED | SAFETY_CRITICAL (CfgFieldRegistry.hpp:418), in the HMAC-signed stamp body (StampHelper.hpp:159) + raw fingerprint (ControllerConfig.hpp:1320-1323). Do NOT tombstone/relocate. Per-cluster reachability is a boot-derived read-only mirror / ResolveForCluster seam, never a field replacing the cfg SSoT.",
        "GateParameters.flags STAYS uint8_t (CONFIRMED; E.1.5): all 8 bits 0x01..0x80 saturated (GateParameters.hpp:38-79). A34 REUSES GATE_FLAG_BUY_BLOCKED (0x20); E.1.2 must NOT reserve a private A34 bit (would widen uint8→uint16 and relayout the seqlock-published cached_params). A34 distinguishability rides a COLD slow-path halt_reason enum sibling (free nibble slots).",
        "PER-NODE kill latch + trips counter (CONFIRMED-NEEDED; E.1.3): the LIVE per-node MtM kill is a slow-thread-owned latch — NODE_STATE_FLAG_SET(KILL_TRIPPED) (:2925) + node_ks_trips_total (uint32_t, :473). The kill_flags sketch (foundation:465-469) has neither and its 3 atomics are 'aggregator/operator sets' = WRONG WRITER. Freeze must add a slow-owned latch + trips counter OR explicitly relocate the kill DECISION to the aggregator (a semantic scope change).",
        "kill_flags line alignas(64)-isolated, rare-write/frequent-read (CONFIRMED; E.1.3): keep 3 separate std::atomic<uint8_t> (writer-independent, no lost-update) — recommended over an MBS bit-pack that forces disjoint-bit fetch_or/fetch_and RMW. NO new hot-path read of ClusterState/AggregatorState/kill_flags: kill/halt reaches the hot path ONLY via the existing branchless permission atomic (ExecutionCore.hpp:474, cache-line-isolated static_assert :178).",
        "D-274 pre-sized strategy_state UNION (CONFIRMED-NEEDED; re-homed to E.1.2): sized to max(StrategyState) + a strategy_state_kind guard on the build dispatcher (closes the SimpleDip→Momentum 384B OOB overread).",
        "exit_bandits slow arm-array MAX-sized from the registry (CONFIRMED-NEEDED; D-206/OPT): MAX(strategy arms, planned learned-ratchet arms), NOT a current-consumer count — fixed-N freeze locks out learned-ratchet arms and re-opens NodeState. Enumerate planned ratchet-arm cardinality AT the freeze.",
        "A22 intended_* torn-read surface (CONFIRMED real, SHAPE-OPEN; E.1.3; D-201): drainer reads nodes[slot].intended_qty/tp/sl (Async.hpp:842/868/900) cross-thread. SHAPE (dedicated seqlock pack vs TradeEvent-payload carry vs reuse param seqlock) is open; because NodeState is runtime-only the freeze-obligation is design-discipline (design-once), not a wire break.",
        "ClusterState.nodes[MAX_NODES_PER_CLUSTER] + node_count + cluster_kill_flag (CONFIRMED; E.1.3/E.1.5/E.1.6): present in sketch for the aggregator integrity-walk; sizes gated on the cap-symbol SSoT. NodeState/ClusterState/AggregatorState are confirmed NOT wire-serialized (0 ShardedSnapshotPersist hits) → their interior is SOFT-freeze (recompile cost, NO version bump), de-risking everything except Position + the cap symbols.",
        "D-206 riding-mode flag — per-position AND per-leg (CONDITIONAL on D1/leg-model; D-206): fits _pad_pos at zero size as a NAMED =0-init PERSIST field, zeroed at all sites (A28 slot-reuse class). Per-LEG riding instead requires NEW per-leg seqlock GateParameters state → re-opens the frozen cross-thread struct; leg model MUST be pinned before hot.params locks.",
        "D-206 16B persisted peak (SPECULATIVE-CONTESTED — the D1 decision; D-206): if retained it is a FOREACH_POSITION_FIELD PERSIST row appended BEFORE _pad_pos (inside POSITION_PERSIST_BYTES prefix), carried by BOTH writers, with a named trailing _pad_tail[N]={0} (H12), AND initialized in the warm-restart round-trip the same freeze (else A1 garbage). REFUTED-as-necessary by the floor-channel alternative: route give-back through the EXISTING persisted stop_loss_price Money_Max floor (precedent PortfolioController.hpp:684-685; floor==peak*(1-give_back) without storing peak; restart-strictly-better) and home MFE-for-reward in ExitRecord. Position stays 128B under the floor path.",
        "NOT-FROZEN / explicitly forward-OPEN (CONFIRMED; E.1.4/E.1.6): FillEvent<F> POD field set (ring never serialized — design in E.1.4 or reserve an opaque cache-line _reserved stub); per-exchange fee/precision (venue-SOURCED .rodata, D-272/D-276 — record explicitly that it stays OUT of ClusterState); metadata_flags uint16→uint32 widen (.rodata constexpr, not hashed/fwritten — zero struct constraint); per-slot SoA scratch (ephemeral OMS sibling arrays); binding{cluster_id,subaccount_id,client_order_id_prefix} (boot-immutable, re-derived, never serialized — prefix is derivable, storing it is denormalization).",
        "HARD wire-locked surfaces (CONFIRMED; E.1.4/E.1.6/D-206): Position struct layout is the SOLE struct-image-dumped type (ShardedSnapshotPersist.hpp:169 raw sizeof(Position) blob) — any added Position field forces a PORTFOLIO epoch + golden regen. BOTH serializer fwrite sequences (Portfolio_Save PERSIST-prefix walk :570 + ShardedSnapshotPersist raw blob :165/169) MUST be re-derived field-by-field under AoS→SoA without silently dropping any field (a missed SoA field = silent-zero-on-restore = D-110). E.1.2's freeze enumeration is INCOMPLETE if it omits the fill read-set (entry_price/entry_fee/quantity/take_profit_price/stop_loss_price, OrderManager.hpp:1225-1229) or these two serializers."
      ],
      "branchRoadmap": [
        {
          "branch": "ExecutionCore.hpp:337-338 — active ? live_tp : cached_params.sg_take_profit_price (and sl :338); emits a real `je`, the ONE every-tick unguarded H7/H20 instance",
          "owner": "optimization-leaf (OPT) — NOT an E.1.2 relayout win (layout-inert, wire-free); scope-fenced OUT of E.1.3 coherence ship",
          "freeNowInE12": false,
          "approach": "Replace with Money_BlendOnMask(live, cached, -(uint64_t)(active!=0)) (FixedPointN.hpp:1876). REFRAME: this is a CONFORMANCE edit + stale-comment deletion, NOT a latency win — `active` flips only inside the __builtin_expect(,0) push block so the je is ~100% predicted/~0 variance; the blend ADDS an unconditional 2nd 16B __int128 load + ~6 mask uops, net p99 neutral-to-slightly-negative. The :334-336 'CMOV verified in bench' comment is STALE (predates Ship-B 16B-Money; 16B __int128 has no x86 128-bit cmov). Refute first: confirm BlendOnMask is bytewise-identical + truly branchless, harden the mask with (active!=0), asm-verify je-vs-cmov, attach a FRESH PMU number, and NEVER advertise as a speedup. May opportunistically ride E.1.2's ExecutionCore comment-rewrite but is not E.1.2's branch-win."
        },
        {
          "branch": "ExecutionCore.hpp:315 — seqlock cached-param miss `if(__builtin_expect(s_now!=cached_seq || (s_now&1ULL),0))` (4 hits: || × 2 instantiations)",
          "owner": "optimization-leaf (OPT) — tool-classification reconcile",
          "freeNowInE12": false,
          "approach": "NO code change. Genuinely ~1/256 ticks; the || second-compare confuses the tool's rare-cold heuristic → lands WARM despite the explicit cold hint. Disposition = allowlist row ('warmup-100%-predicted') OR fix the tool heuristic. Canonical tool-refute #2 (determines whether D-235 means code edits or allowlist rows)."
        },
        {
          "branch": "ExecutionCore.hpp:454 (leg-B SG non-PAIR_BRANCHLESS) + :504 (event-push gate) + :657 (pair_branchless cfg dispatch)",
          "owner": "optimization-leaf (OPT) — allowlist, cold/cfg-flag by intent",
          "freeNowInE12": false,
          "approach": "NO code change. :454 branchless variant already exists for the other instantiation (cold-by-intent). :504 is the canonical rare-branch (only retires on a fired trade) and is the enclosing guard for all Tier-C. :657 is a genuine H20 cfg-flag-dispatch exception (~1/256, documented). All three are allowlist rows."
        },
        {
          "branch": "ExecutionCore.hpp:506/516/542/548/557 + FixedPointN.hpp:1633 — Tier-C cold-nested (exit-fire/entry-fire only; 6+6+3 hits)",
          "owner": "optimization-leaf (OPT) — tool-flattening artifacts, EXCLUDE from steady count",
          "freeNowInE12": false,
          "approach": "NO code change. These sit on the taken-side of the line-504 __builtin_expect(,0) push gate — unreachable unless a trade fires; the flat asm analyzer can't model the cold-nesting. :1633 is the 128-bit Money_Add saturate carry-compare (structurally non-vectorizable, entry-only). Refute #3: confirm via asm they sit on the 0x2d1/0x5b1 taken side, then exclude. The :504/516/542/548/557 set also OVERLAPS bandit/ensemble arm-dispatch (TD-174) — keep allowlisted, do NOT reduce in isolation."
        },
        {
          "branch": "RollingStats.hpp:325/334/343 — three monotonic-deque back-pop while-loops (the only true H11 constant-iter violation in the probe set)",
          "owner": "optimization-leaf (OPT) — slow path (<=100us)",
          "freeNowInE12": false,
          "approach": "DO NOT fixed-K restructure — REFUTED net-negative: the monotonic-deque invariant requires popping ALL worse entries, so K<W is CORRECTNESS-BREAKING and the only correct fixed-iter form K=W converts amortized-O(1) → unconditional O(W) (~384 FPN ops/push at W=128 vs ~3), amplifying the 14-spill register pressure that may itself be the real p99 driver. Worst-case O(W) is sub-budget. Disposition = ALLOWLIST with the documented amortized-O(1) rationale (:322-324). Re-point the stale D-229 decision-log cite (':187' → :325/334/343)."
        },
        {
          "branch": "RegimeDetector.hpp:593-605 — 4-way discrete regime classify cascade (489 instr, 8 warm, 0 spills)",
          "owner": "optimization-leaf (OPT) — slow path, OPTIONAL/cosmetic",
          "freeNowInE12": false,
          "approach": "Low priority — hysteresis-stabilized → near-perfectly predicted, ~0 variance, vastly sub-budget. IF done: branchless MASK-SELECT of precomputed regime CODES preserving EXACT 4-way precedence (NOT a 1-D score-table LUT — precedence/tie-break fragile; use the SESSION_BY_HOUR[24]/TD-040 LUT as TEMPLATE only). DETERMINISM-LOAD-BEARING: regime codes are wire-persisted (SHARDED v10) — bytewise-identical gate + NO code renumbering (H21). GATED on resolving the :571 H4 question first."
        },
        {
          "branch": "RegimeDetector.hpp:569/571 — ML-mode dispatch + `(int)FPN_ToDouble(regime_model_weight)` FPN→double→int crossing (analyzer-exempt source)",
          "owner": "optimization-leaf (OPT) — refute-gated H4",
          "freeNowInE12": false,
          "approach": "Challenge the H4 exemption: is :571 a legit display/heuristic-only integer weight, or a real feature-path float crossing that should route through a named Money_*/integer-LUT seam? Gates both whether OPT closes it as an H4 fix AND whether the Regime mask-select proceeds. (The getenv+fprintf debug block :630-659 is the correctly-gated second exempt source.)"
        },
        {
          "branch": "RollingStats_Push residual 18 warm / 14 spills — FPN compare/mul saturation branches inside FPN_Mul/FPN_SubSat/FPN_AddSat (FixedPointN.hpp:1321/1339/1364/1683)",
          "owner": "optimization-leaf (OPT) — shared kernel deps",
          "freeNowInE12": false,
          "approach": "MOSTLY NO change — the FPN-internal saturation branches are shared hot+slow kernel deps; the deque-update guards collapse if the :325/334/343 while-loops are restructured (they won't be). Refute the branch-centric framing: register-pressure (14 spills) may dominate p99 over the branches — needs PMU evidence."
        },
        {
          "branch": "EventLoop_RebuildOneCore (ControllerEventLoop.hpp:2760-3081, ~5-30us) — heaviest slow branch cluster, UNMETERED coverage hole",
          "owner": "optimization-leaf (OPT) — DEFER rewrite; A-class push on the conformance-gate coverage hole",
          "freeNowInE12": false,
          "approach": "Largest slow consumer yet ABSENT from the 7-probe budget set → zero static branch classification today. DEFER the rewrite as a D-229 leaf (orchestrator not a kernel; writes pending_params SSoT = large blast radius; per-branch cfg-presence guards are mostly __builtin_expect-rare; not a clean golden-master unit). Surface the metering/coverage HOLE now; do NOT fold the rewrite into OPT. Session-mult LUT (:2327-2335) is the in-function branchless precedent."
        },
        {
          "branch": "A34 same-symbol concentration veto (E.1.5 deliverable)",
          "owner": "E.1.5 (per-cluster purity leaf)",
          "freeNowInE12": true,
          "approach": "ZERO new hot branch — rides the EXISTING branchless veto mask. Slow path sets GATE_FLAG_BUY_BLOCKED (0x20) on pending_params.flags exactly as book-imbalance does; hot consumer is the already-paid mask at ExecutionCore.hpp:366-368/GateParameters.hpp:182. Free because the flags byte is reused (no bit reservation, no cached_params widening). The cluster-aggregate read stays OFF the hot path (PULL runs at slow entry-gate). Aggregate→hot is brokered entirely through the slow flag write — the H22 hot-path witness."
        },
        {
          "branch": "Capital-mode (live/paper) gate (E.1.5/NEW-1)",
          "owner": "E.1.5 (per-cluster purity leaf)",
          "freeNowInE12": true,
          "approach": "ZERO new hot branch and zero new per-tick read. trading_mode is slow/boot-only (0 hot reads). Capital authority gates the SLOW-path permission grant, consumed branchlessly at ExecutionCore.hpp:474-476 via __atomic_load_n(permission, ACQUIRE) masked into can_enter. NEW-1 changes WHERE the slow/boot predicate reads from, never the hot mask. Contributes 0 to D-235; do NOT credit E.1.5 with hot-path branch reduction."
        },
        {
          "branch": "E.1.4 entire fill surface (handle_sell_fill/handle_buy_fill, A16 partial-accumulate, A20-A21 reconcile, Class-46 close-by-fill_qty, §5 event-log)",
          "owner": "E.1.4 (fill-completeness leaf)",
          "freeNowInE12": false,
          "approach": "HOT-PATH-INERT by construction — all fill code writes only oms->portfolio.* + oms->* scratch on DRAINER/async threads (a DIFFERENT object than the hot ExecutionCore<F>); grep-clean for live_tp=/live_sl=/->active= writers in OrderManager.hpp + Async.hpp. 0 added / 0 removed to the 2097-instr hot inventory; the D-235 drive-warm→0 lever is NOT available here. De-risk PROOF must be RUN (tools/calls_graph_diff.sh), not asserted. NOT an E.1.2 action."
        },
        {
          "branch": "E.1.3 kill→hot path (writer-side relocation only) + F6 collapse-cluster-halt-into-can_enter (REJECTED)",
          "owner": "E.1.3 (aggregator/coherence leaf)",
          "freeNowInE12": false,
          "approach": "No hot branch added or removed — E.1.3 changes ONLY the writer side (relocates EventLoop_KillSwitchEvaluate to the drainer + swaps its INPUT oms->balance → seqlock snapshot). LOAD-BEARING OUTBOUND INVARIANT E.1.3 must commit: kill/halt reaches the hot path ONLY via the existing branchless permission atomic; NO new hot read of ClusterState/AggregatorState/kill_flags. F6 (fold cluster-halt into can_enter) is REJECTED for E.1.3 — distinct semantics → SSoT coupling, re-touches the hot path inside the highest-risk coherence ship; can_enter already AND-folds permission + BUY_BLOCKED branchlessly (no branch to remove)."
        },
        {
          "branch": "trade-decision-tick event-ring push branchless (D-231 #15) + param-update seqlock-miss p99 spike",
          "owner": "optimization-leaf (D-231/D-230 measure-then-fix)",
          "freeNowInE12": false,
          "approach": "NOT E.1.3/E.1.2 scope — the optimization thread, homed to the H8 bench (measure) + the optimization leaf (fix). Do NOT smuggle into the coherence ship (E.1.3 is hot-path-branch INERT — grep of ExecutionCore/GateParameters/RegimeDetector for the new structs is EMPTY)."
        }
      ],
      "techDebtCascade": [
        "TD-129 (per-core drainer architecture, Option C full) — E.1.3 IS this leaf; STRONG ~0-marginal opportunistic close: hoist EngineSharded_Async_DrainWithSubmit (Async.hpp:785) + OrderManager_Tick + phased-drain buckets into the per-node slow path; central-drain path goes.",
        "TD-197 / torn-read class — THE headline structural close (CAVEAT: confirm it's E.1.3 per dependency-graph, NOT E.1.4 per stale decision-log:1435): EventLoop_KillSwitchEvaluate reads racing oms->balance off the producer thread while the drainer writes it (16B Money tears; KNOWN-RACE TODO live Async.hpp:449-464). Closed by reading the seqlock-published Money snapshot. PLUS A22/D-201 = 2nd torn-read surface. F-2-AMEND makes .E.1 a HARD LIVE-ENABLE GATE on this class.",
        "TD-002 (centralized ControllerEventLoop/drainer removal) — DRAINER HALF only, partial-by-design; single_core sub-deletion landed E.1.1. Verify frontmatter does NOT over-claim full centralized-path removal.",
        "TD-170 (A3/A7/A8 FlattenAll robustness) — STRONG near-0 at the reshard; A3 site LIVE, drifted to ControllerEventLoop.hpp:3454-3455 (ledger cites :3443 — re-point).",
        "TD-220 (two dead SAFETY_CRITICAL kill_switch_* cfg fields; Class-47 split-brain) — STRONG dead-consumer removal, but PRECISION-CHECK: consumers are in the SEPARATE file PortfolioController.hpp (:959/968/1592/1612) — confirm E.1.3 actually deletes them. Coordinate with D-273 (sharded kill is DRAWDOWN-ONLY; session-relative daily-loss kill is its own ship, Task #13, STATUS=WIRE). item-4 must NOT cap-validate cfg rows 543/544 as 'armed' (Class-12 false-credibility).",
        "Class-26 (cross-node iteration) / Class-27 (scalar cfg-mirror) / A1 (warm-restart global take_profit_pct) — H22 instances E.1.3 de-flattens. global_kill_mirror / per_cluster_kill_mirror reads MUST be annotated SANCTIONED H22 EXCEPTION (safety override, single-writer per D-193) so the candidate H22 CI check does not mis-fix them.",
        "TD-167 / F-096 (partial-leg-split double, Async.hpp:842-851) — CONFIRMED live double money round-trip (Money_ToDouble + double-mul + requantize) at HEAD; SUBSUMED but OWNERSHIP-AMBIGUOUS SEAM: Money-ization is an E.1.2 relayout Decision-K (layout-coupled), §4c conservation assert is E.1.4. Fix = conservation BY CONSTRUCTION (legA=Money_Mul(intended,p); legB=Money_Sub(intended,legA)) — NOT two independent half-even roundings. .E.1 cannot close with a surviving double money field. Risk of falling between E.1.2 and E.1.4 — re-point the trigger explicitly. NOT OPT, NOT E.1.5.",
        "TD-169 / A2+A4 (venue parser drops partial-qty + commission) — SUBSUMED by A20/A21 (E.1.4), LAYOUT-INERT; double→Money conversion at the OMS boundary (FillEvent), not inside Position.",
        "TD-202 / Class-50 (event-log single-owner disk_file) — SUBSUMED by §5 (E.1.4), ZERO Position-layout coupling (NOT an E.1.2 opportunistic-close candidate).",
        "Class-46 (terminal actions gate on ORDER_FILLED not handler-reached) — SUBSUMED, the core E.1.4 fill-completeness fix; edit sites are in OrderManager_ProcessFillCommand (:1474/1442/1450), NOT HandleFill — use plan-body cites not the stale RBP catalog cites.",
        "Class-38 (INERT -filled reconcile term, OrderManager.hpp:1520) — FLIPS LIVE this same E.1.4 ship when A16 makes filled_qty cumulative; §4c conservation must account for it.",
        "Class-45 / TD-186 (original_tp + A1 warm-restart take_profit_pct vs per-strategy override) — E.1.5/E.1.4 own the verify (mostly landed .E.0.10); owner_node_id warm-restart adds a SECOND Class-45 instance. Both verifies NON-VACUOUS (run, not assume).",
        "TD-195 (warm-restart exit-disarm window; cached_params.flags never published at restore, ShardedSnapshotPersist:669-680) — FLAG FOR EXPLICIT RE-HOMING (cohort fall-through risk): fill-orthogonal; 'verify subsumed by §6' is a vacuous hedge → re-home to E.1.2 (NodeState/restore skeleton) or E.1.5 (per-cluster boot).",
        "TD-224 (MetadataFlag uint16→uint32 widen; ENUM_STRICT first bit) — SUBSUMED by E.1.6, .rodata/layout-orthogonal; LOCKSTEP-widen the coverage-assert masks (EXEMPT/ALL/ENROLLED bitsets, cfg_compute_mask, overflow guard) or Class-51 vacuity follows.",
        "CAPITAL_BOUND_FEE — SUBSUMED by E.1.6 (BUILDS the FOREACH_EXCHANGE venue fee-schedule SSoT the FEE cap defers to, D-272/D-276); strongest subsumption.",
        "Class-12 (wired-but-unexercised fail-OPEN enum, CfgFieldDispatch.hpp:177-187) — SUBSUMED by E.1.6 (first real consumer: exchange_id/4-state mode/subaccount_id) + ENUM_STRICT adds teeth.",
        "Class-51 (vacuous-green) — WATCH/LANDMINE on the E.1.6 widen (do NOT leave coverage-assert bitsets uint16 while the enum widens to uint32); NOT a debt to close.",
        "TD-162 (warm-restart fee-recompute) — NOT subsumed; natural owner E.1.4. CROSS-LINK: E.1.6 EXPOSES the per-venue fee SSoT, E.1.4/TD-162 CONSUMES it.",
        "TD-135 / Class-11 (regime_names[]) — ADJACENCY not subsumption (live drift found: GUI panels hold 5-entry arrays vs the 4-regime registry → rewire NOT ~0); cross-link + leave tracked.",
        "TD-190 (A34 cluster-aggregate same-symbol governor) — E.1.5 DELIVERABLE (downstream of E.1.3/E.1.4 published slice); CANNOT close 'during the E.1.2 relayout'.",
        "TD-168 (live-OFF per-cluster authority) — PARTIALLY subsumed by E.1.5 NEW-1; full (N+1)th-cluster ZERO-edit closure DEFERRED to E.1.6 (global singletons survive: g_sharded_binance_adapter, scalar TUISnapshot::live_trading, global-drainer paper-reset). Do NOT tombstone cfg.trading_mode to achieve closure.",
        "TD-174 bandit (+ A11/A12, adaptive-entry D-211) — NOT subsumed by OPT; the :504/516/542/548/557 gate-compares are bandit/ensemble arm-dispatch, OPT's reduction OVERLAPS but does not subsume the redesign. Cross-link + keep allowlisted.",
        "D-235 + D-236 (the 25 grandfathered data-dependent-warm branches) — OPT's CHARTER (curation + reduction); reframed: deliverable is letter-conformance (H7/H20/H11) + curation, NOT p99 wins.",
        "D-229 RollingStats H11 — OPT; disposition flips to ALLOWLIST (not fixed-K); re-point the stale decision-log cite to the live :325/334/343 loops.",
        "TD-040 (SESSION_BY_HOUR[24] LUT) — TEMPLATE only for any Regime mask-select; NOT re-subsumed/re-closed.",
        "S1/S3 (Position-extension version-bump class + the missing sizeof(Position)↔version static_assert guard, decision-log:1274) — D-206/E.1.2; the size-pin RED-builds but nothing FORCES the version bump = a silent-cascade hole; E.1.2's bump exercise is the natural wiring home (M7 candidate).",
        "REJECTED from subsumption (do not over-claim): TD-196 (DISK-persist wire-desync — runtime seqlock never touches fwrite; mis-homed, its trigger names E.1.2 relayout); TD-171/A5 (rides the .E.1 adapter D-123, NOT the coherence spine; A6 landed 3ee95dc, A5 still OPEN — frontmatter listing TD-171 closed is an over-claim); TD-203 (v5.16 Phase-D gate removal)."
      ],
      "realignRisks": [
        "[CRITICAL #1 — the surviving universal] FPN<F> (stale-sketch 24B) instead of Money (16B) for the slow_account/drainer_state/AggregatorState money cluster: a D-110 warm-restart catastrophe (binary-scale 2^F persisted bytes read by a decimal-scale 10^8 Money kill reader) + H4 + a SILENT encoding epoch that sizeof static_asserts CANNOT catch (both 16B). All 6 leaves converge. Fix is NEGATIVE-cost. The cmpxchg16b/16B-align rationale is a NON-SEQUITUR (seqlock uses plain stores + a counter, no 128-bit CAS) — the real reasons are H4 + snapshot-byte-match.",
        "[CRITICAL #2 — the D1 mis-freeze, now fully decidable] Freezing Position WRONG on the peak-vs-floor channel: (a) reserving a PERSISTED 16B peak that no E.1.2/E.1.3 code initializes → warm-restart reads garbage (D-206's own #1 risk); (b) freezing 128B then forcing a SECOND full PORTFOLIO/SHARDED epoch + warm-restart re-arm re-verification when D-206 ships. Genuinely contested: route give-back via the EXISTING persisted stop_loss_price Money_Max floor (precedent PortfolioController.hpp:684-685; floor==peak*(1-give_back), restart-strictly-better, no re-derive window) keeps Position 128B; reserve-now honors D-209 'opens once'. node_peak_balance is EQUITY-peak (MTM kill), so it does NOT subsume D-206's give-back anchor — the 128B-MOOT hedge is code-false.",
        "[CRITICAL/HIGH #3 — hard pre-freeze blocker] Cap-symbol SSoT UNDEFINED: NUM_EXCHANGES / MAX_NODES_PER_CLUSTER / MAX_CLUSTERS exist ONLY in the sketch (Limits.hpp has only MAX_EXECUTION_NODES=16). owner_node_id width AND every cluster-sized array size on vapor. The wire-bound part of the freeze literally cannot complete without defining them (with the <=256 ∧ <=256 constraint for a correct uint16_t packed id).",
        "[CRITICAL/HIGH #4 — baked H22 violation, epoch-free to fix but a hard invariant] per_cluster[NUM_EXCHANGES] + cluster_id≡ExchangeEnum fails D-193/H22 the instant cluster_count>NUM_EXCHANGES (e.g. 2 capital-isolated Binance subaccount-clusters). Dimension by MAX_CLUSTERS; cluster_id distinct from ExchangeEnum. H22 (operator-ratified 2026-06-11) overrides any one-cluster=one-exchange convenience.",
        "[HIGH #5 — the real FIT re-open trap] ExecutionCore PARTIAL-flatten homelessness: the v0.1 params-only NodeState hot cluster homelesses the ~15 ExecutionCore hot fields; homing them later shifts slow_account/drainer/kill offsets + sizeof(NodeState) pin → NodeState re-opens. Safe shapes = pointer-held OR embed-whole; the partial-flatten is the ONE shape that forces the re-touch.",
        "[HIGH #6] node_peak_balance SILENT OMISSION from the sketch (live :467, kill gate :2906; drawdown_max does not subsume it) → omission alone forces a slow_account re-open independent of D-206.",
        "[HIGH #7 — all D-206 refutes converge] Warm-restart A1: ShardedSnapshotPersist.hpp:651-679 recomputes live_tp/live_sl from entry×(1±pct) and OVERWRITES persisted TP/SL; ratchet_sl/ratchet_tp + riding/owner state live in EPHEMERAL cached_params → a restored riding position loses its ratcheted protective stop AND attribution until the next slow cadence. Must IMMEDIATE-re-derive in-loop.",
        "[HIGH #8] ClusterState templated-on-adapter (sketch :620) instead of tt::ExchangeAdapter<F> fn-ptr table by value → heterogeneous ClusterState sizes → homogeneous per_cluster[] impossible → aggregator structurally impossible. Landed OrderManagerState model is already correct.",
        "[HIGH #9 — supersedes the map's torn-snapshot framing] H12 uninitialized-wire-byte: _pad_pos[7] already has NO initializer, is written by NO code, yet sits INSIDE the persist prefix (latent hazard); tucking riding-flag/owner_node_id there makes it load-bearing (A28 slot-reuse). At 192B a trailing alignas pad with no field is sizeof-fwritten as indeterminate bytes → cross-run/HMAC determinism break (masked today only because 128B = 2 exact lines). Name the pads =0 or switch both writers to POSITION_PERSIST_BYTES().",
        "[HIGH #10] 16B-atomic-add hazard IF the A34 push-slab path is chosen: x86-64 has no lock xadd for 128b; -O3 -march=native with NO -mcx16 routes 16B atomics through libatomic's lock pool = H1/H3 violation on a money path. Decisive argument for PULL (reserves no atomic).",
        "[HIGH #11] Per-node MtM kill reads cross-thread positions[] (recomputes unrealized from oms->portfolio.positions[] + px, :2884-2892): a seqlock-published slow_account does NOT close that read (open_notional is entry-value, not MtM unrealized), and positions[] is NOT relocated in the foundation (active_bitmap is uint16_t capping 8 nodes). Decide per-NodeState-owned positions[] (pure/H22) vs shared MAX-array at the freeze.",
        "[HIGH #12] STAMP_BOUND trading_mode determinism: in the HMAC-signed stamp body + raw fingerprint. The leaf's 'structurally unrepresentable (field removed)' framing is INCOMPATIBLE — keep cfg.trading_mode as SSoT; non-vacuity via a read-site detector, not deletion. PortfolioController_HotReload save/restore-protects config.trading_mode (:1964/1976) — any per-cluster scheme embedding a resolved cfg re-arms a per-node-flips-cluster-authority (Knight-class H22) break.",
        "[MED #13] slow_account HALF-reservation trap: the sketch-guessed _padding1[24] against a ~96-208B real need bakes a WRONG size into the alignas chain (worse than reserving nothing). Per-node money serializes field-by-field (append-only/version-neutral), so resolution = pin the EXACT money set now OR leave the money region explicitly forward-OPEN; never half-reserve.",
        "[MED #14] kill_flags WRONG-WRITER + missing slow-owned KILL_TRIPPED latch + node_ks_trips_total; the sketch's 3 atomics are 'aggregator/operator sets'. Add the slow-owned latch or explicitly relocate the kill DECISION to the aggregator (a semantic scope change).",
        "[MED #15] capital_allocated value-coupling: default_per_node=(total*risk)/num_nodes → adding the (N+1)th node re-divides every existing node's allocated_balance (and its kill base) UNLESS an explicit per-node risk_pct is set. The 'per-node-pure' label is FALSE on the default allocator (an allocator-semantics flag, not a layout-freeze blocker).",
        "[MED #16] balance/ks_peak_balance are DEPLOYMENT-GLOBAL singletons (OrderManager.hpp:309/502) — folding them into a per-node slow_account fails the H22 (N+1) test + replicates a Class-27 mirror. The global the global-kill reads is a DERIVED sum homed in AggregatorState; do NOT fold global money into the per-node pack.",
        "[MED #17] Leg-split conservation FORM: legB MUST be Money_Sub(intended,legA), NOT independently half-even rounded (two roundings need not sum to intended → fires the §4c assert / leaks a sub-satoshi). This rides E.1.2's in-place Money-ization (TD-167), not just E.1.4's assert.",
        "[MED #18] NodeState.hot.mode writable = Class-27 scalar-mirror / Class-47 split-brain (the (N+1)th node could carry mode=live while its cluster is paper) + a DOD error (0 hot reads). Delete or relocate read-only — at E.1.2, not E.1.5.",
        "[MED #19] Drainer budget axis: the relocated kill-eval lands on the DRAINER (p99<=10us), NOT the <=100us slow-rebuild budget the leaf cites. Meter the relocated eval + its seqlock double-read at 10us; EventLoop_RebuildOneCore is already an unmetered hole.",
        "[MED #20] STALE LAYOUT COMMENTS ported into the freeze: ExecutionCore.hpp:62-85 ('Money=24B', 'live_sl 56-80', 'CMOV at ~287-288', '24+24+8pad=64B') + Portfolio.hpp version='6' prose (#define is 7). The static_asserts use sizeof so they hold, but absorbing/relocating by reading the PROSE bakes wrong derived offsets (the D-229 derived-facts-drift class). Absorb from sizeof/offsetof NEVER prose; rewrite to 16B.",
        "[MED #21] exit_bandits fixed-N freeze re-opens NodeState if learned-ratchet arm cardinality is not enumerated + MAX-reserved now.",
        "[MED #22] ExitRecord is a SECOND frozen layout the NodeState-SoA row does not name; the MFE/give-back-reward peak belongs HERE (snapshotted at exit). Settle ExitRecord-MFE at the freeze.",
        "[MED #23] Per-subaccount rate-limit granularity: ClusterState reserves ONE rate triple (per-IP weight) but Binance per-account ORDER-COUNT limits are per-API-key = per-subaccount; the SubAccountPool entry must carry its OWN rate triple or E.1.6 re-opens the pool.",
        "[LOW #24] FillEvent↔NodeState back-edge busts only RUNTIME alignment static_asserts (ring never serialized), NOT the wire — SOFT-freeze NodeState; design FillEvent in E.1.4 with at most an opaque _reserved stub. A16 stale-TP re-arm seam + cfg_load_fault_flags guaranteed-0 discipline are verify-at-dive, not re-escalate."
      ],
      "e12Recommendations": [
        "FIX THE SKETCH BEFORE FREEZING THE MONEY CLUSTER: retype ALL slow_account + drainer_state + AggregatorState money fields FPN<F>→Money (16B) in E.1-foundation.md (:434-440/452-454/684-688). Enumerate the WHOLE accounting set (per_cluster cached_realized_pnl/open_notional/drawdown_current, global cached_total_*, *_kill_drawdown_threshold, drainer core_realized/core_open_notional), not just NodeState.core_fees — omitting AggregatorState leaves a Money→FPN mismatch at the aggregation seam with no re-open budget. Justify by H4 + persist-byte-match, NOT cmpxchg16b.",
        "DEFINE THE CAP-SYMBOL SSoT IN Limits.hpp at the freeze: NUM_EXCHANGES, MAX_NODES_PER_CLUSTER (<=256), MAX_CLUSTERS (<=256). Without them owner_node_id width + every cluster-sized array are unfrozen. Make owner_node_id uint16_t = (cluster<<8)|node.",
        "DECOUPLE cluster_id from ExchangeEnum; dimension all cluster-indexed arrays (per_cluster[], per_cluster_kill[], per_cluster_kill_drawdown_threshold[]) by MAX_CLUSTERS not NUM_EXCHANGES; carry exchange_id as a FIELD of ClusterState; pin ExchangeEnum fixed-width (uint8_t-backed). (H22.)",
        "EMBED the adapter as tt::ExchangeAdapter<F> fn-ptr table BY VALUE in ClusterState — NEVER template<typename AdapterT> struct ClusterState (sketch :620). Decide the per-cluster adapter HOME (own-in-ClusterState vs reference OrderManagerState) at the freeze.",
        "RESOLVE ExecutionCore HOMING-MODE explicitly in the E.1.2 DoD: pointer-held (as today) OR embed-whole (carry the :176-185 static_asserts). FORBID the v0.1 params-only PARTIAL-flatten in the plan body.",
        "RESTORE node_peak_balance as an explicit per-node Money running-max in slow_account; DROP drawdown_max (0 consumers) and drawdown_current (derivable) from the SSoT layout (or keep only as a labeled cross-thread cache, typed as a dimensionless percent not Money).",
        "KEEP NodeSlowState/RollingStats POINTER-HELD in the frozen NodeState (the sizeof-firewall, not the refuted size-pin reasoning); add the account_state_seqlock counter + struct _padding as SKIP_PERSIST in the FOREACH_OMS_FIELD persist walk (determinism).",
        "LAND owner_node_id at E.1.2 in the existing _pad_pos[7] as a NAMED =0-init PERSIST-kind field, zeroed in Position_Reset + EVERY OpenSlot/Add/bypass site, mirrored in PositionEntryArgs (Class-18 replay), round-trip-verified in BOTH serializers (explicit Portfolio_Save PERSIST row + ShardedSnapshotPersist raw-blob). Drop the 'REQUIRED by E.1.4' framing — A21 boot attribution decodes from order_id>>60.",
        "CONVERT _pad_pos to named =0 fields (riding-flag + owner_node_id) — it is already an uninitialized-wire-byte hazard inside the persist prefix; tucking load-bearing state there without zeroing inherits prior slot state (A28).",
        "EXTEND the warm-restart re-arm loop (ShardedSnapshotPersist.hpp:651-679) to thread riding/floor/owner state with an IMMEDIATE in-loop re-derive (not next-cadence), since ratchet_sl/ratchet_tp are ephemeral cached_params.",
        "THREAD versions using ACTUAL HEAD constants: PORTFOLIO_SNAPSHOT_VERSION 7→8 (Portfolio.hpp:534; fix stale '=6' prose) + SHARDED_SNAPSHOT_VERSION 10→11 (ShardedSnapshotPersist.hpp:94). H21 append-only + tombstone + check_identifier_retirement --update + size-pin. Add a sizeof(Position)↔version static_assert (closes the S3 silent-cascade guard gap).",
        "MONEY-IZE the leg-split (Async.hpp:842-851, TD-167) IN the E.1.2 relayout as conservation-by-construction: legA=Money_Mul(intended,p); legB=Money_Sub(intended,legA). §4c's assert becomes defensive not load-bearing. The E.1.2 subplan's 'F-096 already Money-ized' assertion is STALE vs HEAD — re-scope to 'Money-ize THEN assert'.",
        "DESIGN A34 as PULL over the cluster's own <=16 nodes' published per-node open_notional at slow entry-gate time — NO per-(cluster×symbol) slab, NO MAX_SYMBOLS_PER_CLUSTER mint, NO 16B-atomic, NO cluster==exchange conflation. REQUIRES the E.1.3 published per-cluster snapshot to RETAIN per-node granularity + boot-immutable node.hot.symbol (the load-bearing E.1.3↔E.1.5 constraint).",
        "REUSE GATE_FLAG_BUY_BLOCKED for A34 (flags byte saturated — do NOT widen uint8→uint16); add a COLD slow-path halt_reason enum sibling for forensic distinguishability.",
        "KEEP cfg.trading_mode as the STAMP_BOUND SSoT — do NOT tombstone/relocate; reach per-cluster mode via a ResolveForCluster seam or boot-derived read-only mirror; get H22 non-vacuity from a read-site detector (Class-26 unindexed-global-read shape; substrate exists at tools/check_per_node_registry_integrity.py Check 10).",
        "DELETE or relocate-read-only NodeState.hot.mode (0 hot readers across all E leaves; .hot is a DOD error + split-brain). Rename so the name cannot later invite a per-tick read.",
        "MAX-SIZE exit_bandits from the registry (strategy arms + planned learned-ratchet arms); pre-size the D-274 strategy_state union to max(StrategyState) + a strategy_state_kind guard.",
        "ENUMERATE in the E.1.2 freeze the fill read-set (entry_price/entry_fee/quantity/take_profit_price/stop_loss_price) AND both snapshot serializer fwrite sequences; re-derive them field-by-field under AoS→SoA without dropping any field (a missed SoA field = silent-zero-on-restore D-110).",
        "KEEP the kill_flags as 3 separate alignas(64)-isolated std::atomic<uint8_t> (writer-independent); add the slow-owned KILL_TRIPPED latch + node_ks_trips_total OR explicitly move the kill decision to the aggregator. NO new hot-path read of ClusterState/AggregatorState/kill_flags — kill reaches hot ONLY via the existing branchless permission atomic (the E.1.3 OUTBOUND invariant to state explicitly).",
        "RE-GROUND all file:line at HEAD 0ee227a before coding (CfgFieldRegistry offsets drifted ~+7; predicate at ControllerConfig.hpp:2212; mode enum :41-43). APPEND a 4th mode state (e.g. backtest=3) — NEVER renumber the persisted PAPER=0/LIVE=1/SHADOW=2 (H21/Knight-Capital).",
        "FOLD the slow-path H4 doc-debt: rewrite the stale ExecutionCore.hpp:62-85 layout prose to 16B in the same E.1.2 file-touch (absorb offsets from sizeof/offsetof, never the comment); fix the Portfolio.hpp version='6' prose opportunistically.",
        "DECLARE the freeze policy explicitly: HARD-freeze(Position offsets + both fwrite sequences) + SOFT-freeze(NodeState/ClusterState/AggregatorState runtime clusters — confirmed not serialized, recompile-cheap), so downstream-leaf re-touches of the runtime clusters are not mis-treated as design-once snapshot-bump events."
      ],
      "openDecisions": [
        "D1 (THE call — now decidable against the full input-space): Is Position FINAL at 128B, or pre-reserve a 16B D-206 peak (→192B)? The input-space confirms: fill (E.1.4) provides ZERO peak warrant; E.1.5 A34 is Position-size-INERT; node_peak_balance is EQUITY-peak (MTM kill) so it CANNOT subsume D-206's per-position give-back anchor; owner_node_id lands byte-free in _pad_pos regardless. The genuine fork is peak-vs-floor CHANNEL: route give-back through the EXISTING persisted stop_loss_price Money_Max floor (precedent PortfolioController.hpp:684-685; floor==peak*(1-give_back) without storing peak; persist-the-floor is restart-strictly-better, kills the A1 re-derive window; Position stays 128B; peak-for-reward lives in ExitRecord) VS reserve a 16B persisted peak per D-209 'Position opens once'. IF reserve: MUST initialize the peak in the persist round-trip the SAME freeze (else A1 garbage) AND place peak/riding COLD (cache-line 1/2), never a hot single-line carve. NOTE the existing trailing-ratchet writer CAPS the floor DOWN to entry×(1-3·fee_taker) (:3663-3667) — a give-back sibling needs its OWN clamp, it is not a clean drop-in.",
        "D2 (delete legacy controller / centralized-drainer path): The legacy PortfolioController.hpp holds the dead kill_switch_* consumers (TD-220), the HotReload trading_mode save/restore-protect, and the legacy centralized PositionExitGate (Portfolio.hpp:478, NOT on the gated sharded path). Decide whether E.1.3 deletes the centralized-drainer half now (TD-129 Option C full) vs partial-by-design, and CONFIRM the frontmatter does not over-claim full centralized-path removal (TD-002 is drainer-half only; the centralized-path removal is its own LOW ship). Confirm E.1.3 actually deletes the PortfolioController.hpp kill consumers (separate file).",
        "Seam-map drift (HIGH — resolve before claiming the TD-197 torn-read close): the dependency-graph places aggregator + torn-read + absorption at E.1.3 ★ (SPINE-2, the task framing), but decision-log:1435 (DECIDED 2026-06-15) maps E.1.3=drainer-absorption, E.1.4=aggregator+torn-read+kill-hierarchy. Which is canonical? Re-ground the stale artifact and pin the {aggregator INFRA + seqlock publish = E.1.3} vs {per-fill push MATH = E.1.4} split that both seams imply.",
        "drift==0 OUTBOUND validatability (S7): E.1.3 builds the seqlock-publish infra but the per-fill push that exercises drift==0 lives in the E.1.4 fill handler the contract keeps INERT. Either (a) E.1.3 wires the push as blessed PLUMBING (state this explicitly) or (b) re-state drift==0 as E.1.4's OUTBOUND.",
        "Balance home + reconstruct-vs-store: where does the GLOBAL kill read balance/ks_peak — an AggregatorState derived-sum vs an explicit stored field — and is per-node balance reconstructed (start+realized) or stored explicitly? The refutes genuinely split (Refute-1 derive, Refute-4 keep-explicit since balance==start+realized is incidental not contractual, Refute-6 derived-sum). Settle the SSoT (and note there is NO stored starting_balance anchor today — only an Init param).",
        "Kill-eval SCOPE (H22): EventLoop_ClearAllPermissions clears EVERY core (a deployment-aggregate kill — adding the (N+1)th node changes WHEN existing nodes halt). Does the relocated eval read a node-local slow_account snapshot or the global aggregate? per_node_kill is produced by NO code today (vestigial / H21 dead-slot risk). The operator may WANT a deployment-wide circuit-breaker as the live-gate semantics — NAME the intent so the frozen data-dependency is correct.",
        "positions[] ownership: per-NodeState-owned (pure, horizontally scalable, closes the per-node MtM kill's cross-thread read) vs the current shared MAX_PORTFOLIO_POSITIONS array sliced by Sharded_LegSlot. Bears directly on whether the seqlock slow_account actually makes the per-node kill input coherent.",
        "NodeState.hot.mode disposition: DELETE entirely (simplest — a field that can't exist can't split-brain) vs RELOCATE to the boot-immutable binding block as a layout-enforced read-only mirror. Settle at E.1.2.",
        "FillEvent timing: pin the full field set in E.1.2 (one leaf early) vs design in E.1.4 with at most an opaque cache-line _reserved stub (ring never serialized favors E.1.4; Aggregator's partial net_pnl consumption is a soft constraint).",
        "Is the per-cluster aggregate (ClusterState/AggregatorState) SERIALIZED into the SHARDED snapshot wire body? (0 refs at HEAD; reads as a D-54 recompute-on-cycle runtime structure.) This decides whether a later re-open is a snapshot-VERSION/H9/H12 event vs a mere recompile + size-pin bump — record it explicitly; the 're-bumps a VERSION' escalation is likely overstated.",
        "A34 read-substrate FORK (push per-(cluster×symbol) Money slab vs PULL over own nodes) — recommend PULL (over-reserve refuted on 6 lenses; reserves nothing, per-cluster-pure, no MAX_SYMBOLS_PER_CLUSTER, no 16B-atomic, no cluster==exchange conflation). Settle AT E.1.2 (it freezes the per_cluster[] container shape).",
        "Per-cluster capital-mode FORM: bespoke ClusterState.trading_mode uint8 mirror (Class-27 risk) vs ResolveForCluster seam vs keep-cfg-SSoT + read-site detector — recommend the latter. NOTE trading_mode has NO core_N override today (resolved==global for every node), so the strict H22 driver is FORWARD E.1.6 heterogeneity (cluster A live / B paper), not current per-shard purity.",
        "owner_node_id store-vs-decode: D-209's boot-collapse is fixable with ZERO layout change by decoding (order_id>>60)&0xF (encode OrderManager.hpp:961, ProcessFillCommand already trusts the decode :1393) instead of Reconcile.hpp:245's search-then-fallback-to-0. Decide whether to ALSO land the persisted field (cheap _pad_pos forward-reservation) — and if so it MUST be PERSIST-kind + round-trip both serializers.",
        "A16 stale-TP re-arm seam: A16 re-accumulates at a new weighted-avg entry_price but live_tp/live_sl were armed at the FIRST fill price and are NOT re-armed by any fill handler. Re-arm via slow-path SetParameters on weighted-avg re-entry vs accept original-basis TP (Class-45 disclaim). VERIFY whether A16 re-entry routes as a fresh enter_a on a reopened slot (then :543 re-runs and this is MOOT).",
        "TD-220/D-273 fork: re-wire the session-relative daily-loss kill (its own ship, Task #13, D-273=WIRE) vs tombstone the dead OMS-aggregate kill pair (H21) — and confirm E.1.3 actually deletes the PortfolioController.hpp consumers. item-4 must NOT cap-validate cfg rows 543/544 as 'armed' (Class-12 false-credibility).",
        "Leg model (D-206 F4/F5): leg-SHARED riding (zero new hot/seqlock field, reuse GATE_FLAG_TP_ENABLED clear, but legs cannot ride independently) vs PER-LEG riding (NEW per-leg seqlock GateParameters field → re-opens the frozen hot struct). Plus: riding must be armed PROACTIVELY (slow-path, threshold below TP) before the hot gate fires — make 'arm-before-TP' an explicit acceptance criterion and stress it against a gap-up tick jumping past both arming and TP. Decide BEFORE locking NodeState.hot.params.",
        "Persist serialization SHAPE: does the AoS→SoA pivot also convert snapshot serialization to a column-walk? If yes, the prefix-vs-full divergence + the _pad_pos/trailing-pad mechanics dissolve and peak/riding/owner become column arrays (cost = 1 registry row each, not a free pad tuck). If persist stays AoS-record fwrite, the H12 trailing-pad naming + both-writer byte-count reconciliation survive. Pin it at the freeze."
      ]
    }
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Hot-path baseline"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Investigate"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Refute"
    },
    {
      "type": "workflow_phase",
      "index": 4,
      "title": "Synthesize"
    },
    {
      "type": "workflow_phase",
      "index": 5,
      "title": "Global synthesis"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "hp:0",
      "phaseIndex": 1,
      "phaseTitle": "Hot-path baseline",
      "agentId": "ace8e4e31b040070c",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796484811,
      "queuedAt": 1782796484810,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/caramel/code/FoxML_Trader_v2/CoreFrameworks/Portfolio…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nYou map the CURRENT hot/slow path branch inventory — the ground truth for an E-umbrella f…",
      "lastProgressAt": 1782796653500,
      "tokens": 70510,
      "toolCalls": 11,
      "durationMs": 168688,
      "resultPreview": "## I-1 SURFACE MAP — Hot/slow branch inventory (lens: ExecutionCore_Tick / inlined BG/SG / PositionExitGate)\n\n**Tool run:** `check_latency_path_conformance.py` against the PRODUCTION `-O3 -march=native` build (no LATENCY_PROFILING). `ExecutionCore_Tick`: instructions=2097, **DATA-DEPENDENT-WARM=25, allowlisted=0 → 25 unallowed** (loop=22, rare-cold=15). H4 float=0, div=0, spills=0, indirect=0. Thi…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "hp:1",
      "phaseIndex": 1,
      "phaseTitle": "Hot-path baseline",
      "agentId": "af9eb8b1ad20997ed",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796484814,
      "queuedAt": 1782796484810,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== Position/P…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nYou map the CURRENT hot/slow path branch inventory — the ground truth for an E-umbrella f…",
      "lastProgressAt": 1782796687857,
      "tokens": 78856,
      "toolCalls": 14,
      "durationMs": 203043,
      "resultPreview": "## SURFACE MAP — Hot/Slow Branch Inventory · CACHE-SoA + 128-bit-Money lens\n\n**Tool run:** `check_latency_path_conformance.py` @ HEAD 0ee227a — `ExecutionCore_Tick` (hot): 2097 instr, **25 data-dependent-warm** (matches ratcheted baseline in `tools/lib/latency_path_budgets.json`). Ground-truth offsets compiled from source (`sizeof(Money)=16`, `sizeof(GateParameters<64>)=192`, `sizeof(ExecutionCore…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "hp:2",
      "phaseIndex": 1,
      "phaseTitle": "Hot-path baseline",
      "agentId": "a020885c68acd13ff",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796484815,
      "queuedAt": 1782796484810,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/caramel/code/FoxML_Trader_v2/CoreFrameworks/Controlle…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nYou map the CURRENT hot/slow path branch inventory — the ground truth for an E-umbrella f…",
      "lastProgressAt": 1782796632584,
      "tokens": 75650,
      "toolCalls": 15,
      "durationMs": 147769,
      "resultPreview": "SLOW-PATH BRANCH INVENTORY — i-class map (lens: EventLoop_RebuildOneCore / RollingStats_Push / Regime_Classify). All cites are HEAD 0ee227a; analyzer ran clean against this HEAD (production flags `-O3 -march=native`, no LATENCY_PROFILING). Budgets JSON (`tools/lib/latency_path_budgets.json`) baselines RollingStats_Push=18 / Regime_Classify=8 data-dependent — both match current ASM, so the budget f…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "hp-synth",
      "phaseIndex": 1,
      "phaseTitle": "Hot-path baseline",
      "agentId": "aac92be5e3ffc4625",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796687924,
      "queuedAt": 1782796687860,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Consolidated H7/H20 jitter inventory across 3 investigation…",
      "promptPreview": "Consolidate these 3 hot/slow-path branch investigations into a CONCISE inventory — the data-dependent-warm branches (the H7/H20 jitter meter), each with location + classification + why + removability. Keep it tight enough to use as shared context in a forward audit.\n\n--- 0 ---\n## I-1 SURFACE MAP — Hot/slow branch inventory (lens: ExecutionCore_Tick / inlined BG/SG / PositionExitGate)\n\n**Tool run:*…",
      "lastProgressAt": 1782796768457,
      "tokens": 63435,
      "toolCalls": 1,
      "durationMs": 80532,
      "resultPreview": "{\"summary\":\"Consolidated H7/H20 jitter inventory across 3 investigations (all HEAD 0ee227a, check_latency_path_conformance.py @ production -O3 -march=native, no LATENCY_PROFILING). HOT PATH — ExecutionCore_Tick (2097 instr, 25 data-dependent-warm, allowlisted=0): this is the fused BG+SG+entry body (BG/SG inlined from GateParameters.hpp into ExecutionCore_Tick_Impl; PositionExitGate Portfolio.hpp:4…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "I:E.1.3:LAYOUT",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a02cd7853c86b7e07",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768463,
      "queuedAt": 1782796768461,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== OMS struct …",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796926419,
      "tokens": 76137,
      "toolCalls": 10,
      "durationMs": 157956,
      "resultPreview": "I have enough grounding. Returning my lens-scoped findings.\n\n---\n\n## E.1.3 [LAYOUT] — input-space the freeze MUST cover (SPINE-2 aggregator/coherence)\n\nScope of this leaf (grounded): GLOBAL O(1) push aggregator writing `NodeState.slow_account`/`kill_flags`; torn-read closure moving the kill-switch off the racing `oms->balance` onto a seqlock-published money snapshot; drainer absorption into `NodeS…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "I:E.1.3:HOTPATH-BRANCH",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a3048756c42bfd914",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768464,
      "queuedAt": 1782796768461,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/caramel/code/FoxML_Trader_v2/CoreFrameworks/Controlle…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796905917,
      "tokens": 70746,
      "toolCalls": 9,
      "durationMs": 137453,
      "resultPreview": "I have the complete picture for this lens. E.1.3 is a scaffold (no code landed); the baseline branch inventory is supplied, so my job is the ADD/REMOVE delta against the hot path. All claims grounded below.\n\n---\n\n## E.1.3 — LENS: [HOTPATH-BRANCH] findings (SPINE-2 coherence)\n\n**Bottom line: E.1.3 is hot-path-branch INERT. It ADDS zero hot-path branches and REMOVES/converts zero of the baseline's 2…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "I:E.1.3:TECHDEBT",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a2abc7a4e5975d023",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768465,
      "queuedAt": 1782796768461,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== FlattenAll…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796990531,
      "tokens": 103927,
      "toolCalls": 18,
      "durationMs": 222066,
      "resultPreview": "## E.1.3 — TECHDEBT lens findings (subsumption vs adjacency)\n\nAll cites HEAD `0ee227a`. The leaf plan frontmatter declares `td_closed: [TD-002, TD-129, TD-170, TD-171, TD-196, TD-197]` + the D-255 fold (TD-220). I ground each against code and split STRONG-subsumption from OVER-CLAIM. Leaf plan `file:line` are HEAD `572c978` and have drifted (flagged).\n\n### STRONG subsumption — E.1.3's scope IS the…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "I:E.1.3:REALIGN",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a0b0a0b5be51f7573",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768466,
      "queuedAt": 1782796768461,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== Async.hpp 4…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796940317,
      "tokens": 94915,
      "toolCalls": 11,
      "durationMs": 171851,
      "resultPreview": "## E.1.3 REALIGN AUDIT — what forces a re-touch of the E.1.2-frozen layout\n\nLens: the single worst mis-freeze E.1.3 (aggregator/coherence) would punish + reserve-now cost. All claims code/plan-cited; the foundation sketch line-refs are the **STALE** layout E.1.2 will freeze from.\n\n---\n\n### THE SINGLE WORST MIS-FREEZE — `slow_account` money cluster frozen as FPN (24B, non-atomic) instead of Money (…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "I:E.1.3:SEAM",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a2461641b55c4b8c7",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768467,
      "queuedAt": 1782796768461,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "for p in \"D-209\" \"D-214\" \"D-206\" \"D-235\" \"owner_node_id\"; d…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796938537,
      "tokens": 95608,
      "toolCalls": 10,
      "durationMs": 170070,
      "resultPreview": "I have what I need. Here are the SEAM-lens findings for E.1.3.\n\n---\n\n## E.1.3 (aggregator + coherence, SPINE-2) — SEAM findings [cross-leaf dependency lens only]\n\n**Grounding:** leaf plan `…E.1.3-aggregator-coherence.md`; sub-master sketch `…E.1-foundation.md:417-470` (NodeState) / `:620-649` (ClusterState); decision-log `…E-architecture-v2.md`; code at HEAD `0ee227a`. Leaf plan's own file:line ar…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "I:E.1.3:CONCURRENCY",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a300f15ad839c7f11",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768467,
      "queuedAt": 1782796768461,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== intended_ f…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796933664,
      "tokens": 82401,
      "toolCalls": 9,
      "durationMs": 165196,
      "resultPreview": "Sufficient to render the findings. Here is my CONCURRENCY-only map for E.1.3 against the E.1.2 freeze.\n\n---\n\n## E.1.3 (aggregator + coherence, SPINE-2) — [CONCURRENCY] freeze-implications for E.1.2\n\n**Scope of lens:** the cross-thread reads/writes this leaf ADDS, and what cache-line placement + explicit padding the E.1.2 freeze must bake NOW so E.1.3 cannot re-touch the frozen `NodeState`/`Cluster…"
    },
    {
      "type": "workflow_agent",
      "index": 11,
      "label": "I:E.1.3:PURITY",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "aa8b8dcb83f28803e",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768468,
      "queuedAt": 1782796768462,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== positions[…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796955442,
      "tokens": 80702,
      "toolCalls": 10,
      "durationMs": 186974,
      "resultPreview": "I have enough to ground the PURITY-lens findings precisely. Returning tight, code-cited results.\n\n---\n\n## E.1.3 (aggregator + coherence) — PURITY [H22] findings ONLY\n\n**Ground truth at HEAD 0ee227a (verified, not recalled):**\n- ONE global OMS: `state.oms` is a single `OrderManagerState<F>` (`CoreFrameworks/EngineSharded/Async.hpp:244, 464`); `oms->balance` is a single global field; `oms->portfolio…"
    },
    {
      "type": "workflow_agent",
      "index": 12,
      "label": "I:E.1.4:LAYOUT",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a6ab83caeffd5ff15",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768490,
      "queuedAt": 1782796768489,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "sed -n '600,620p;240,250p' DataStream/Reconcile.hpp 2>/dev/…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796939745,
      "tokens": 91082,
      "toolCalls": 11,
      "durationMs": 171255,
      "resultPreview": "## E.1.4 (fill-completeness) — [LAYOUT] input-space the E.1.2 freeze must cover\n\nLens-scoped: exactly what fields/state E.1.4 reads/writes from the frozen Position/Portfolio/NodeState/ClusterState. All `file:line` @ HEAD 0ee227a; leaf-plan offsets (`OrderManager.hpp:1214/1243/1450/1508`) re-verified CURRENT (not stale). The **foundation.md NodeState sketch IS stale** — see Finding 4.\n\n### A. Posit…"
    },
    {
      "type": "workflow_agent",
      "index": 13,
      "label": "I:E.1.4:HOTPATH-BRANCH",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "abcb94f88debda31c",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768491,
      "queuedAt": 1782796768489,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/caramel/code/FoxML_Trader_v2/CoreFrameworks/Execution…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796910161,
      "tokens": 75845,
      "toolCalls": 10,
      "durationMs": 141670,
      "resultPreview": "## E.1.4 fill-completeness — HOTPATH-BRANCH lens findings\n\nGround: fill leaf runs on the DRAINER/slow/async threads, not `ExecutionCore_Tick`. All claims HEAD 0ee227a.\n\n**HEADLINE: E.1.4 adds ZERO steady-state hot-path branches and removes NONE.** The fill path is hot-path-INERT — same provable-inert property the baseline establishes for the Position AoS→SoA relayout (hot gate reads `ExecutionCore…"
    },
    {
      "type": "workflow_agent",
      "index": 14,
      "label": "I:E.1.4:TECHDEBT",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a03ea7f217c7f52c3",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768492,
      "queuedAt": 1782796768489,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== Class-46 C…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796964992,
      "tokens": 82809,
      "toolCalls": 13,
      "durationMs": 196500,
      "resultPreview": "## E.1.4 (fill-completeness) — TECHDEBT lens findings\n\nScope: what E.1.4 SUBSUMES, and which items are **layout-coupled to E.1.2** (closeable opportunistically at ~0 marginal cost during the Position relayout) vs **fill-math-internal** (E.1.4 owns regardless of layout). All cites HEAD `0ee227a`. Ledger bodies live in `../tick-trader-percore-workspace/DOCS/tech-debt/open.md` (engine `DOCS/TECH_DEBT…"
    },
    {
      "type": "workflow_agent",
      "index": 15,
      "label": "I:E.1.4:REALIGN",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a909be3bdda4069f8",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768493,
      "queuedAt": 1782796768490,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== OMS start/i…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796960850,
      "tokens": 95929,
      "toolCalls": 12,
      "durationMs": 192357,
      "resultPreview": "## E.1.4 (fill-completeness) — REALIGN lens findings\n\nSingle lens: what would FORCE a re-touch of the E.1.2-frozen layout if THIS leaf is mis-frozen. Code-cited, severity-classified, precision over breadth.\n\n---\n\n### THE SINGLE WORST MIS-FREEZE — `FillEvent<F>` is an E.1.4 deliverable but is embedded BY VALUE in the E.1.2-frozen NodeState [CRITICAL]\n\n`FillEvent<F>` is defined by THIS leaf (leaf sc…"
    },
    {
      "type": "workflow_agent",
      "index": 16,
      "label": "I:E.1.4:SEAM",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "acaeb4a59558bd2e8",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768494,
      "queuedAt": 1782796768490,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 f=$(rg -l \"filled_qty…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796980380,
      "tokens": 92742,
      "toolCalls": 10,
      "durationMs": 211886,
      "resultPreview": "I have the full seam picture. Here is my tight, code-cited findings list for the SEAM lens only.\n\n---\n\n# I-CLASS [E.1.4 fill-completeness] — SEAM map (INBOUND/OUTBOUND + hot-path/frozen-layout touchpoints)\n\n**Grounding:** HEAD `0ee227a`. Plan cites HEAD `572c978`; its OMS line numbers (`handle_sell_fill:1214`, `Portfolio_CloseSlot:1243`, `HandleFill:1450`) **still match HEAD** — plan body is curre…"
    },
    {
      "type": "workflow_agent",
      "index": 17,
      "label": "I:E.1.5:LAYOUT",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a28dd6911e512b310",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768509,
      "queuedAt": 1782796768507,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; grep -rn \"node_symbo…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796952983,
      "tokens": 88630,
      "toolCalls": 13,
      "durationMs": 184474,
      "resultPreview": "I have what I need for the LAYOUT lens. Synthesizing the tight, code-cited findings.\n\n---\n\n## E.1.5 (per-cluster purity) — LAYOUT LENS: what the freeze must cover\n\n**Leaf** `plans/.../2026-06-15-v5.15.5.F.4d.1.E.1.5-per-cluster-purity.md` · two concerns: **NEW-1** (relocate `ControllerConfig_IsLiveCapital` to read `cluster.trading_mode`) + **A34** (per-cluster same-symbol concentration governor). …"
    },
    {
      "type": "workflow_agent",
      "index": 18,
      "label": "I:E.1.5:HOTPATH-BRANCH",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "ac37401f13e171db5",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796768510,
      "queuedAt": 1782796768508,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -n \"GATE_FLAG_\" CoreFrameworks/GateParameters.hpp | he…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782796879127,
      "tokens": 77862,
      "toolCalls": 9,
      "durationMs": 110617,
      "resultPreview": "Surface mapped. Returning findings for the HOTPATH-BRANCH lens only.\n\n---\n\n## E.1.5 (per-cluster purity) — HOTPATH-BRANCH findings\n\n**Headline: E.1.5 is hot-path-INERT by design — it ADDS zero hot-path state and zero per-tick branches, and REMOVES none. Both its concerns (NEW-1 relocation, A34 governor) live on slow/boot/aggregator paths. The single live risk is A34 being mis-sited as a per-tick c…"
    },
    {
      "type": "workflow_agent",
      "index": 19,
      "label": "I:E.1.5:TECHDEBT",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a44912ff78b17ca73",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796879128,
      "queuedAt": 1782796768508,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== IsLiveCapi…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797096671,
      "tokens": 87845,
      "toolCalls": 23,
      "durationMs": 217542,
      "resultPreview": "## E.1.5 (per-cluster purity) — TECHDEBT-lens findings\n\nSingle lens: what tech-debt this leaf SUBSUMES vs. closes opportunistically at ~0 marginal cost. All cites HEAD `0ee227a`, grep-verified.\n\n### ID-namespace + grounding caveat (read first)\n- The leaf's `td_closed: [TD-190, TD-168]` = global **TECH_DEBT-190 / TECH_DEBT-168** (confirmed via decision-log `...E-architecture-v2.md:1207,1305` + adju…"
    },
    {
      "type": "workflow_agent",
      "index": 20,
      "label": "I:E.1.5:REALIGN",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "af1d9e71456e87022",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796905918,
      "queuedAt": 1782796768508,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2/plans/v5.15-live-read…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797080125,
      "tokens": 77721,
      "toolCalls": 12,
      "durationMs": 174207,
      "resultPreview": "## FINDINGS — E.1.5 (per-cluster purity) · LENS: [REALIGN] what FORCES a re-touch of the E.1.2-frozen layout\n\nGrounding: leaf plan `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.5-per-cluster-purity.md`; freeze leaf `...E.1.2-nodestate-soa-layout.md`; coherence leaf `...E.1.3-aggregator-coherence.md`; decision-log `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`; ClusterStat…"
    },
    {
      "type": "workflow_agent",
      "index": 21,
      "label": "I:E.1.5:SEAM",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "ae85641df6e6a5cb0",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796910163,
      "queuedAt": 1782796768508,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2/plans/v5.15-live-read…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797092080,
      "tokens": 88024,
      "toolCalls": 10,
      "durationMs": 181916,
      "resultPreview": "I have the seam surface mapped. Returning my tight, code-cited findings for the SEAM lens only.\n\n---\n\n## I-CLASS SEAM MAP — E.1.5 (per-cluster purity) · lens = INBOUND/OUTBOUND cross-leaf deps + hot-path/frozen-layout touch points\n\n**Verdict up front:** E.1.5 is almost entirely a body-edit + slow-path leaf, but it has **3 hard demands on the E.1.2 LAYOUT-FREEZE that the E.1.2 skeleton does NOT vis…"
    },
    {
      "type": "workflow_agent",
      "index": 22,
      "label": "I:E.1.5:CONCURRENCY",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a70c6a119db9b83e2",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796926421,
      "queuedAt": 1782796768508,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== Money / FPN…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797128947,
      "tokens": 88211,
      "toolCalls": 9,
      "durationMs": 202526,
      "resultPreview": "Confirmed grounding. FPN_Binary<64>=16B and Money=16B (FixedPoint asserts :44/:107); the sketch's \"24-byte FPN\" reasoning is stale. `trading_mode` has zero runtime write sites (boot/parse only). `FPN_AtomicAdd`/`Money_AtomicAdd` does not exist. Here is the tight CONCURRENCY-lens findings list.\n\n---\n\n## E.1.5 (per-cluster purity) — [CONCURRENCY] lens — what the E.1.2 layout-freeze must bake NOW\n\n**…"
    },
    {
      "type": "workflow_agent",
      "index": 23,
      "label": "I:E.1.5:PURITY",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a391711875a79e67b",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796933665,
      "queuedAt": 1782796768508,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== per-cluster…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797075946,
      "tokens": 79190,
      "toolCalls": 10,
      "durationMs": 142281,
      "resultPreview": "## [PURITY] (H22 scale-invariance) findings — E.1.5 leaf, what the E.1.2 layout-freeze must enable/not-break\n\nLens: is E.1.5's state a pure function of its own local inputs, and does the frozen layout pass the (N+1)th-node/cluster zero-change test? Critical-only. All cites HEAD `0ee227a`; foundation sketch line-refs are v0.1 (WILL drift).\n\n---\n\n**P1 — CRITICAL · the v0.1 `ClusterState` sketch has …"
    },
    {
      "type": "workflow_agent",
      "index": 24,
      "label": "I:E.1.6:LAYOUT",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a1072c89f001e92b9",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796938538,
      "queuedAt": 1782796768543,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== adapter em…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797089217,
      "tokens": 87132,
      "toolCalls": 8,
      "durationMs": 150679,
      "resultPreview": "## E.1.6 (multi-exchange) — LAYOUT lens findings\n\n**Scope of lens:** what E.1.6's adapter/WS/registry machinery + metadata-widening + per-exchange fee/precision *reads from* the FROZEN Position/Portfolio/NodeState/ClusterState layout. Today none of NodeState/ClusterState/ExchangeAdapterHandle/owner_node_id exist (grep 0-hit, `CoreFrameworks/`+`MemHeaders/`) — they are E.1.2's to create, so this is…"
    },
    {
      "type": "workflow_agent",
      "index": 25,
      "label": "I:E.1.6:HOTPATH-BRANCH",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a078b141e2093c714",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796939746,
      "queuedAt": 1782796768544,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -rn \"no_trade_band\\|MBS_\\|is_maker\" CoreFrameworks/Exe…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797063576,
      "tokens": 71487,
      "toolCalls": 12,
      "durationMs": 123830,
      "resultPreview": "## E.1.6 (multi-exchange) — HOTPATH-BRANCH findings\n\n**Lens verdict: E.1.6 ADDS ZERO hot-path branches and REMOVES/converts ZERO existing data-dependent-warm branches. The frontmatter `hot_path: untouched` is CONFIRMED-correct under code-grounded tracing.** Every deliverable lands on boot/slow/submit paths. One layout-justification refute-spot ties back to the E.1.2 freeze.\n\n### Per-deliverable ho…"
    },
    {
      "type": "workflow_agent",
      "index": 26,
      "label": "I:E.1.6:TECHDEBT",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "ad02afdcc2ed875c7",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796940318,
      "queuedAt": 1782796768544,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; sed -n '600,660p' pl…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797187621,
      "tokens": 100668,
      "toolCalls": 20,
      "durationMs": 247303,
      "resultPreview": "## E.1.6 (multi-exchange) — TECHDEBT lens findings\n\nAll cites HEAD `0ee227a`. The leaf plan body's D-255 fold line #s are anchored at HEAD `9c93d1d` and **have drifted ~7 lines** (verified below) — flag STALE for the dive.\n\n---\n\n### Distinction up front (subsumption vs adjacency)\nMy lens asks what E.1.6 **subsumes** (rides its own structural need at ~0 marginal cost) vs merely sits **adjacent** to…"
    },
    {
      "type": "workflow_agent",
      "index": 27,
      "label": "I:E.1.6:REALIGN",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "aea379e1e80888ed7",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796952985,
      "queuedAt": 1782796768544,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo ---DLOG---; gre…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797156286,
      "tokens": 94594,
      "toolCalls": 7,
      "durationMs": 203301,
      "resultPreview": "I have what I need. Verified the landed adapter is a fixed-size fn-pointer table, and read the sub-master ClusterState/NodeState sketches + the metadata_flags enum + D-209/D-206.\n\n---\n\n# E.1.6 (multi-exchange) — [REALIGN] findings: what forces a re-touch of the E.1.2-frozen layout\n\n**Lens scope reminder:** E.1.2 freezes Position / NodeState / ClusterState ONCE; every later E leaf (incl. this one) …"
    },
    {
      "type": "workflow_agent",
      "index": 28,
      "label": "I:E.1.6:SEAM",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a8447dc386c4df538",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796955443,
      "queuedAt": 1782796768544,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== IsLiveCapi…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797174050,
      "tokens": 84398,
      "toolCalls": 12,
      "durationMs": 218607,
      "resultPreview": "Surface mapped and code-grounded. Here is the SEAM findings list for E.1.6.\n\n---\n\n## E.1.6 (multi-exchange) — SEAM lens findings [I-class, HEAD 0ee227a]\n\n**Headline:** E.1.6 is purely additive (frontmatter `hot_path: untouched`, no snapshot bump) — BUT it is the heaviest CONSUMER of E.1.2's frozen layout in the whole `.E.1` chain. Every one of its four deliverables reads a field that **does not ex…"
    },
    {
      "type": "workflow_agent",
      "index": 29,
      "label": "I:OPT:LAYOUT",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "ab0c20aa101d274a9",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796960851,
      "queuedAt": 1782796768553,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/caramel/code/FoxML_Trader_v2/CoreFrameworks/Execution…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797086654,
      "tokens": 91689,
      "toolCalls": 8,
      "durationMs": 125803,
      "resultPreview": "I have the grounding I need. Here is my tight LAYOUT-lens finding set.\n\n---\n\n## OPT leaf (D-229/#9 slow + #15 hot) — LAYOUT input-space the E.1.2 freeze must cover\n\n**Lens verdict up front:** the OPT leaf reads/writes almost NOTHING from `Position`/`Portfolio`. Its hot work lives in `ExecutionCore<F>`; its slow work lives in `RollingStats`. The freeze's real OPT-coverage burden is on the two struc…"
    },
    {
      "type": "workflow_agent",
      "index": 30,
      "label": "I:OPT:HOTPATH-BRANCH",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "af7df1ccb4547bcf4",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796964993,
      "queuedAt": 1782796768553,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/caramel/code/FoxML_Trader_v2/CoreFrameworks/Execution…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797114614,
      "tokens": 97136,
      "toolCalls": 13,
      "durationMs": 149620,
      "resultPreview": "I have the surface grounded. All key sites verified against HEAD. Returning the structured findings for the [HOTPATH-BRANCH] lens.\n\n---\n\n## OPT LEAF (D-229/#9 + #15) — [HOTPATH-BRANCH] surface map\n\n**Frame check (honor D-235/D-236, flag staleness):** The OPT leaf is the *consumer* of the branch roadmap the E.1.0 conformance analyzer (the meter) produced. D-235 (`decision-log :1517`) + D-236 (`:152…"
    },
    {
      "type": "workflow_agent",
      "index": 31,
      "label": "I:OPT:TECHDEBT",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a304d45947dcb3e6b",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796980382,
      "queuedAt": 1782796768553,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; sed -n '183,192p' ML…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797183368,
      "tokens": 74293,
      "toolCalls": 18,
      "durationMs": 202986,
      "resultPreview": "## TECHDEBT lens — OPT leaf (D-229/#9 · slow+hot branch/compute reduction) · HEAD 0ee227a\n\n**Grounding note up front (the load-bearing finding):** OPT is a **DISJOINT cohort from E.1.2's Portfolio/Position SoA relayout** — decision-log D-229 carved it out explicitly: *\"the slow-path optimization = a COMPUTE+CACHE job … DISJOINT cohort from E.1.2's Portfolio/Position SoA → its OWN `.E` leaf, NOT fo…"
    },
    {
      "type": "workflow_agent",
      "index": 32,
      "label": "I:OPT:REALIGN",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a9cf1485596bb8685",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782796990533,
      "queuedAt": 1782796768554,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== D-229 / #9…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797182542,
      "tokens": 81953,
      "toolCalls": 10,
      "durationMs": 192009,
      "resultPreview": "## REALIGN findings — OPT leaf (D-229/#9) · lens: what forces a re-touch of the E.1.2-frozen layout\n\n**Scope note up front:** OPT itself reads only *existing* fields and forces **no** layout change. The realign risk is entirely a *consumer-enumeration* obligation OPT owes the LAYOUT-FREEZE leaf: the slow→hot give-back/peak machinery that #15 (hot-trade-branch) and the deferred MTM/trailing D-229 w…"
    },
    {
      "type": "workflow_agent",
      "index": 33,
      "label": "I:OPT:SEAM",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "ac9693928193a816e",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797063578,
      "queuedAt": 1782796768554,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"===Money_BlendO…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797278334,
      "tokens": 101058,
      "toolCalls": 9,
      "durationMs": 214756,
      "resultPreview": "All claims grounded against HEAD. Here is my findings list for the SEAM lens only.\n\n---\n\n## I-CLASS [SEAM] FINDINGS — OPT leaf (D-229/#9 + D-235/#15 + D-236), HEAD 0ee227a\n\n**Leaf status (stale-vs-HEAD):** The OPT leaf has **NO dedicated plan** — it exists only as D-229/D-235/D-236 + a tracking line in the `.E.1` sub-master. It is **\"E.1.M-class\"**: explicitly NOT placed in the locked chain `E.1.0…"
    },
    {
      "type": "workflow_agent",
      "index": 34,
      "label": "I:D-206:LAYOUT",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a45528f23d2dd1b99",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797075947,
      "queuedAt": 1782796768554,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; grep -n \"D-206\\|D-20…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797246590,
      "tokens": 88614,
      "toolCalls": 11,
      "durationMs": 170642,
      "resultPreview": "## D-206 LAYOUT LENS — what the trailing-exit redesign needs from the FROZEN Position/Portfolio/NodeState/ClusterState\n\nGrounded at HEAD 0ee227a. Decisions honored: D-206 (fold), D-55 (Position SoA), D-209 (\"Position opens once\"), D-253 (the owed loss-floor guard). Lens = the exact input-space the E.1.2 freeze must cover for this leaf.\n\n### A. Position struct — the ONLY struct D-206 forces to grow…"
    },
    {
      "type": "workflow_agent",
      "index": 35,
      "label": "I:D-206:HOTPATH-BRANCH",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a8bdd2f5d1b027b2e",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797080126,
      "queuedAt": 1782796768554,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== hot gate re…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797229094,
      "tokens": 80172,
      "toolCalls": 7,
      "durationMs": 148967,
      "resultPreview": "[HOTPATH-BRANCH] findings — D-206 exit trailing-profit redesign. All cites @ HEAD 0ee227a.\n\n## What D-206 ADDS to the hot path: ZERO fields, ZERO branches (if built as the plan specifies)\n\n**F1 — Position 128→192B is provably hot-path-INERT (confirms baseline; strengthens the \"open-once\" case).**\nThe hot SG gate reads `core->live_tp/live_sl/cached_params.*` only — `rg \"positions\\[\" CoreFrameworks/…"
    },
    {
      "type": "workflow_agent",
      "index": 36,
      "label": "I:D-206:TECHDEBT",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a9fd1049f57f28ffb",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797086655,
      "queuedAt": 1782796768554,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== warm-resta…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797309766,
      "tokens": 85284,
      "toolCalls": 18,
      "durationMs": 223111,
      "resultPreview": "## [TECHDEBT] D-206 leaf — subsumption findings (Position PEAK + riding-flag relayout)\n\nLens = what does this leaf SUBSUME / let E.1.2 close at ~0 marginal during the relayout. Code-cited, HEAD `0ee227a`. Precision over breadth; sorted SUBSUMED → ADJACENT → STALE-FLAG.\n\n### SUBSUMED — close at ~0 marginal if PEAK + riding-flag ride E.1.2's bump\n\n**S1 · \"Position struct extension ⇒ snapshot-version…"
    },
    {
      "type": "workflow_agent",
      "index": 37,
      "label": "I:D-206:REALIGN",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "adef3a3a2acc0c565",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797089218,
      "queuedAt": 1782796768554,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== Position pe…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797253571,
      "tokens": 85986,
      "toolCalls": 10,
      "durationMs": 164353,
      "resultPreview": "## REALIGN findings — D-206 (exit trailing-profit PEAK field) vs the E.1.2 layout freeze\n\n**Lens:** what would FORCE a re-touch of the E.1.2-frozen `Position`/persist layout if D-206 is mis-frozen. Code-cited, HEAD 0ee227a.\n\n### Ground truth (scouted)\n- `Position<F>` = 128B / 2 cache lines exact, no trailing pad; 9 PERSIST fields via `FOREACH_POSITION_FIELD` (`MemHeaders/PositionFieldRegistry.hpp:…"
    },
    {
      "type": "workflow_agent",
      "index": 38,
      "label": "I:D-206:SEAM",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "aa609fcef06188ebb",
      "agentType": "i-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797092081,
      "queuedAt": 1782796768554,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== is_trailing…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797282470,
      "tokens": 99162,
      "toolCalls": 10,
      "durationMs": 190388,
      "resultPreview": "SEAM-lens findings grounded against HEAD. My single lens: D-206's INBOUND/OUTBOUND cross-leaf dependencies + exactly where it touches the hot path and the E.1.2 frozen layout.\n\n---\n\n## D-206 SEAM MAP (leaf: exit trailing-profit redesign / the Position PEAK field)\n\n**One-line seam:** D-206 is the *sole* driver of the D1 \"is Position FINAL at E.1.2\" decision — it is the only remaining leaf that dema…"
    },
    {
      "type": "workflow_agent",
      "index": 39,
      "label": "map:E.1.4",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a5f58436e721e725a",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797096707,
      "queuedAt": 1782796980391,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Consolidate these 5 single-lens investigations of E-leaf E.1.4 (fill-completeness) into ONE structured leaf-map. De-dupe; KEEP the code-cited specifics; preserve hot-path findings prominently.\n\n--- investigation 1 ---\n## E.1.4 (fill-completeness) — [LAYOUT] input-space the E.1.2 freeze must cover\n\nLens-scoped: exactly what fields/state E.1.4 reads/writes from the frozen Position/Portfolio/NodeStat…",
      "lastProgressAt": 1782797202796,
      "tokens": 67752,
      "toolCalls": 1,
      "durationMs": 106089,
      "resultPreview": "{\"layoutNeeds\":[\"NEW FIELD owner_node_id (D-209) on Position<F> — REQUIRED by E.1.4, ZERO occurrences in code at HEAD 0ee227a (grep clean across CoreFrameworks/ MemHeaders/). Type uint8_t (node id, MAX_PORTFOLIO_POSITIONS ≤16). Needed by A21 venue-net reconstruction leg: boot reconcile has empty in-flight table → all fills default to core 0 (Reconcile.hpp:245) → Portfolio_OpenSlot overwrites → N f…"
    },
    {
      "type": "workflow_agent",
      "index": 40,
      "label": "map:E.1.3",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "ad4f9b0c673c80d8e",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797114616,
      "queuedAt": 1782796990564,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Consolidate these 7 single-lens investigations of E-leaf E.1.3 (aggregator + cross-thread coherence (SPINE-2, capital cluster)) into ONE structured leaf-map. De-dupe; KEEP the code-cited specifics; preserve hot-path findings prominently.\n\n--- investigation 1 ---\nI have enough grounding. Returning my lens-scoped findings.\n\n---\n\n## E.1.3 [LAYOUT] — input-space the freeze MUST cover (SPINE-2 aggregat…",
      "lastProgressAt": 1782797237436,
      "tokens": 78732,
      "toolCalls": 1,
      "durationMs": 122819,
      "resultPreview": "{\"layoutNeeds\":[\"slow_account money pack: balance (Money) AND ks_peak_balance (Money) MUST sit INSIDE the single seqlock-published slow_account pack — today they scatter across 3 OMS clusters (balance@OrderManager.hpp:309 / realized_pnl@310 in one; ks_peak_balance@502 in a separate COLD post-portfolio cluster), so the current layout structurally CANNOT yield one coherent kill-input read (L1 HIGH).…"
    },
    {
      "type": "workflow_agent",
      "index": 41,
      "label": "map:E.1.5",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "a7f6b8b806d4dfdb5",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797128956,
      "queuedAt": 1782797128955,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Consolidate these 7 single-lens investigations of E-leaf E.1.5 (per-cluster purity (H22 scale-invariance)) into ONE structured leaf-map. De-dupe; KEEP the code-cited specifics; preserve hot-path findings prominently.\n\n--- investigation 1 ---\nI have what I need for the LAYOUT lens. Synthesizing the tight, code-cited findings.\n\n---\n\n## E.1.5 (per-cluster purity) — LAYOUT LENS: what the freeze must c…",
      "lastProgressAt": 1782797233347,
      "tokens": 76875,
      "toolCalls": 1,
      "durationMs": 104391,
      "resultPreview": "{\"layoutNeeds\":[\"[HIGH/CONVERGENT 7 lenses] ClusterState.trading_mode — uint8_t, ABSENT from the E.1.2/E.1-foundation ClusterState sketch (foundation.md:621-660 lists exchange_id/endpoints/threads/adapter/rate-limit/health/cluster_kill_flag/nodes[]/node_count, NO trading_mode). Relocation TARGET for NEW-1; source = cfg.trading_mode uint8 enum {PAPER=0,LIVE=1,SHADOW=2} (ControllerConfig.hpp:41-42; …"
    },
    {
      "type": "workflow_agent",
      "index": 42,
      "label": "map:E.1.6",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "af84efd5911483242",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797187624,
      "queuedAt": 1782797187623,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Consolidate these 5 single-lens investigations of E-leaf E.1.6 (multi-exchange) into ONE structured leaf-map. De-dupe; KEEP the code-cited specifics; preserve hot-path findings prominently.\n\n--- investigation 1 ---\n## E.1.6 (multi-exchange) — LAYOUT lens findings\n\n**Scope of lens:** what E.1.6's adapter/WS/registry machinery + metadata-widening + per-exchange fee/precision *reads from* the FROZEN …",
      "lastProgressAt": 1782797291396,
      "tokens": 71408,
      "toolCalls": 1,
      "durationMs": 103771,
      "resultPreview": "{\"layoutNeeds\":[\"NodeState.hot.mode — uint8_t byte (MBS_* 2-bit slot, 6 bits headroom), holds the relocated global trading_mode PAPER=0/LIVE=1/SHADOW=2 (ControllerConfig.hpp:41-43, NOT :59-61 — cite drifted -18) + appended backtest=3 (H21, never renumber). The 2-bit SEMANTIC is E.1.6's; the BYTE must be E.1.2's or E.1.6 lands a new persisted byte → snapshot epoch re-open (sketch :424).\",\"NodeState…"
    },
    {
      "type": "workflow_agent",
      "index": 43,
      "label": "A:E.1.4:NECESSITY",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a2acb2ddb969751b1",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797202803,
      "queuedAt": 1782797202800,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "rg -n \"starting_balance|start_balance|ks_peak_balance|ks_mi…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797408378,
      "tokens": 90431,
      "toolCalls": 11,
      "durationMs": 205575,
      "resultPreview": "## REFUTE VERDICT — NECESSITY lens (can each layout-need be DERIVED / recomputed / deferred instead of stored in the frozen struct?)\n\nDefault-skeptical posture applied. Every claim grounded at HEAD 0ee227a.\n\n---\n\n### REFUTED — `owner_node_id` on Position is NOT a hard E.1.2 freeze requirement (the map's #1 layoutNeed + INBOUND-LAYOUT seam)\n\nThe map asserts: \"if E.1.2 freezes Position without `owne…"
    },
    {
      "type": "workflow_agent",
      "index": 44,
      "label": "A:E.1.4:FIT",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a988cc4045c80aa34",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797202805,
      "queuedAt": 1782797202800,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; grep -rn \"FillEvent\\…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797520769,
      "tokens": 103090,
      "toolCalls": 15,
      "durationMs": 317964,
      "resultPreview": "## A-CLASS REFUTE VERDICT — Lens [FIT]: does the E.1.2 freeze accommodate E.1.4 (fill-completeness) without a later re-touch?\n\nGrounded at HEAD `0ee227a`. Decision-log D-209 (`plans/.../v5.15.5.F.4d.1.E-architecture-v2.md:1288`) is honored as settled, not re-litigated.\n\n### REFUTED (leaf-map claims that do not survive code)\n\n**R1 — `owner_node_id` is \"REQUIRED by E.1.4 / A21 venue-net READS a per-…"
    },
    {
      "type": "workflow_agent",
      "index": 45,
      "label": "A:E.1.4:PERF-TRUTH",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a1d6ba4f77e923491",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797202806,
      "queuedAt": 1782797202800,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== PositionExi…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797428020,
      "tokens": 81171,
      "toolCalls": 10,
      "durationMs": 225214,
      "resultPreview": "## REFUTE VERDICT — E.1.4 leaf-map, PERF-TRUTH lens\n\nGrounded all claims at HEAD 0ee227a. Default-skeptical; conceding only what the code proves.\n\n### REFUTED\n\n**R1 — \"ExecutionCore.hpp:337-338 is the ONE real steady-state hot violation, removable=YES via Money_BlendOnMask\" is a perf-overclaim (the textbook \"micro-opt the predictor already handles\").**\nThe map propagates this from the baseline int…"
    },
    {
      "type": "workflow_agent",
      "index": 46,
      "label": "A:E.1.4:CORRECTNESS",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "ad811dca974ba11c2",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797202807,
      "queuedAt": 1782797202801,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/caramel/code/FoxML_Trader_v2/CoreFrameworks/EngineSha…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797465303,
      "tokens": 91681,
      "toolCalls": 11,
      "durationMs": 262495,
      "resultPreview": "Grounded against HEAD 0ee227a. My lens: determinism / warm-restart money round-trip (D-110) / snapshot-version / H4/H9/H12.\n\n## REFUTED\n\n**R1 [HIGH · NEW] — \"Position AoS→SoA is provably hot-path-inert\" is true but the consumer enumeration OMITS the two determinism-critical consumers: the snapshot serializers.** The map (and the orchestrator's \"hot-path-inert claim must enumerate consumers\") lists…"
    },
    {
      "type": "workflow_agent",
      "index": 47,
      "label": "A:E.1.4:SIMPLICITY",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a5ac9fdff5b6096c6",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797202807,
      "queuedAt": 1782797202801,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== Does ANY sn…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797425535,
      "tokens": 83588,
      "toolCalls": 7,
      "durationMs": 222728,
      "resultPreview": "## A-CLASS REFUTE VERDICT — E.1.4 leaf-map, SIMPLICITY lens\n\n**Headline: the map over-engineers the freeze by treating the entire `NodeState` runtime layout as wire-frozen / design-once-or-pay-a-snapshot-bump. Grounding the cost in code: only TWO things are genuinely H21-wire-locked, and neither is FillEvent or the slow_account money cluster.**\n\nI confirmed what the SHARDED snapshot actually seria…"
    },
    {
      "type": "workflow_agent",
      "index": 48,
      "label": "A:E.1.5:NECESSITY",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "aca72f1dcbe0cc257",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797233354,
      "queuedAt": 1782797233351,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; sed -n '1296,1310p' …",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797540487,
      "tokens": 106487,
      "toolCalls": 13,
      "durationMs": 307133,
      "resultPreview": "## A-CLASS REFUTE VERDICT — E.1.5 leaf-map, NECESSITY lens\n\nDefault-refuted. I ground every claim against HEAD 0ee227a. Bottom line: the leaf-map over-reserves the frozen layout. The single highest-leverage \"freeze decision\" it raises (the A34 per-(cluster×symbol) slab) is **already settled toward the cheap path by D-211/TD-190 itself**, and several \"layout needs\" are denormalizations of facts the…"
    },
    {
      "type": "workflow_agent",
      "index": 49,
      "label": "A:E.1.5:FIT",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a7514126b78ed40d8",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797233356,
      "queuedAt": 1782797233351,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== FOREACH_HAL…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797511509,
      "tokens": 90663,
      "toolCalls": 13,
      "durationMs": 278153,
      "resultPreview": "VERDICT — lens [FIT]: REFUTE that the E.1.2 frozen layout accommodates E.1.5 without a later re-touch. Grounded @ HEAD 0ee227a.\n\n## REFUTE 1 (NEW MISS — strongest): the A34 concentration-bound cfg field re-opens the SIZE-PINNED + FINGERPRINTED `ControllerConfig` at E.1.5\n\nThe leaf-map rated the A34 concentration BOUND as `[MED/SEAM] … does NOT exist … sibling of per_cluster_kill_drawdown_threshold…"
    },
    {
      "type": "workflow_agent",
      "index": 50,
      "label": "A:E.1.5:PERF-TRUTH",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a697a947c1a7bec3f",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797237437,
      "queuedAt": 1782797233351,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/caramel/code/FoxML_Trader_v2/FixedPoint/FixedPointN.h…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797502082,
      "tokens": 89274,
      "toolCalls": 9,
      "durationMs": 264645,
      "resultPreview": "REFUTE VERDICT — lens [PERF-TRUTH], target E.1.5 leaf-map, grounded @ HEAD 0ee227a.\n\n## REFUTED\n\n**R1 [HIGH] — \"16B __int128-backed types are __int128-atomic on x86_64\" is itself a perf-truth overclaim — same species as the stale cmov comment it corrects.**\nThe map (layoutNeeds [HIGH/H4]) dismisses the foundation sketch's split-slot rationale as STALE and asserts 16B Money can be fed by `atomic-ad…"
    },
    {
      "type": "workflow_agent",
      "index": 51,
      "label": "A:E.1.5:CORRECTNESS",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a5c508fbf9be9597a",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797246591,
      "queuedAt": 1782797233352,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== SHARDED sna…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797474319,
      "tokens": 85923,
      "toolCalls": 9,
      "durationMs": 227728,
      "resultPreview": "VERDICT — REFUTE lens: determinism (D-110 money round-trip / snapshot-version / H4-H9-H12). Grounded at HEAD 0ee227a.\n\n## REFUTED\n\n**R1 (PRIMARY, HIGH) — the NEW-1 `trading_mode` relocation is mis-scoped LOW; it is a STAMP_BOUND/H9/H21 determinism hazard, and the map's two headline claims about it are mutually exclusive.**\nThe leaf-map's determinism seam (`crossLeafSeams` OUTBOUND→DETERMINISM, \"LO…"
    },
    {
      "type": "workflow_agent",
      "index": 52,
      "label": "A:E.1.5:SIMPLICITY",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a4c6f3329a58167f2",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797253573,
      "queuedAt": 1782797233352,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "echo \"=== ResolveForCore: does it produce a full Controller…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797429581,
      "tokens": 78274,
      "toolCalls": 6,
      "durationMs": 176008,
      "resultPreview": "## REFUTE VERDICT — lens: SIMPLICITY (less frozen-layout surface)\n\nThe leaf-map **over-engineers the E.1.2 freeze on its two biggest-ticket items** (the A34 aggregate slab and the dedicated `cluster.trading_mode` field), and proposes to *constrain* a third field (`NodeState.hot.mode`) that should not be frozen at all. Each demand can be replaced by a path that reserves strictly LESS frozen layout.…"
    },
    {
      "type": "workflow_agent",
      "index": 53,
      "label": "A:E.1.5:SCALE",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "ab073fcd1b27d2d85",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797278336,
      "queuedAt": 1782797233352,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== HotReload p…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797537099,
      "tokens": 91119,
      "toolCalls": 9,
      "durationMs": 258763,
      "resultPreview": "## A-CLASS REFUTE VERDICT — Lens [SCALE] (H22 (N+1)th-cluster) — leaf E.1.5\n\nDefault-refuted. Ground HEAD 0ee227a. Two settled decisions honored (D-217 do-now relocation, D-34/D-54 global O(1) aggregator) — I refute the leaf-map's H22 *property claims*, not the relocation itself.\n\n### REFUTE-1 [CRITICAL] — \"ZERO authorizer-site edits / 8 sites byte-identical / per-shard read of global mode structu…"
    },
    {
      "type": "workflow_agent",
      "index": 54,
      "label": "A:E.1.5:OVER-RESERVE",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a9115a1403a24175e",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797282471,
      "queuedAt": 1782797233353,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== GATE_FLAG …",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797579244,
      "tokens": 105044,
      "toolCalls": 11,
      "durationMs": 296772,
      "resultPreview": "## REFUTE VERDICT — OVER-RESERVE lens on the E.1.5 leaf-map\n\nGrounded at HEAD `0ee227a`. The leaf-map's central freeze pressure is itself the over-reserve.\n\n### REFUTED (speculative reservation — make the leaf justify the byte, it can't)\n\n**R1 — PRIMARY: the per-(cluster × symbol) Money slab + `MAX_SYMBOLS_PER_CLUSTER` mint is unjustified.** The leaf-map frames scalar-per-cluster freeze as the \"WO…"
    },
    {
      "type": "workflow_agent",
      "index": 55,
      "label": "A:E.1.3:NECESSITY",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a901650aaa208d914",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797291397,
      "queuedAt": 1782797237446,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== TradeEvent …",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797524101,
      "tokens": 98674,
      "toolCalls": 11,
      "durationMs": 232704,
      "resultPreview": "I have enough grounded evidence. The drainer histogram is already an `inline global` (`g_engine_drainer_cycle_hist`, Async.hpp:12,92), not a NodeState field. TradeEvent (TradeEvent.hpp:54-66) carries price/leg/type and has spare space. node_realized already exists per-node (ControllerEventLoop.hpp:411,882). Verdict follows.\n\n---\n\n# A-CLASS REFUTE VERDICT — E.1.3 leaf-map, NECESSITY lens\n\n**Posture…"
    },
    {
      "type": "workflow_agent",
      "index": 56,
      "label": "A:E.1.3:FIT",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "ab811c0b292da1cfe",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797309767,
      "queuedAt": 1782797237446,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "echo \"=== CoreContext: intended_qty + node_ks_trips_total +…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797649393,
      "tokens": 117313,
      "toolCalls": 15,
      "durationMs": 339626,
      "resultPreview": "Confirmed. I have enough grounded evidence. Final verdict.\n\n---\n\n## A-CLASS REFUTE VERDICT — [FIT] lens on the E.1.3 leaf-map vs the E.1.2 layout-freeze\n\n**Posture: REFUTED.** The leaf-map's slow_account field-set does NOT accommodate E.1.3's actual consumers. I found live per-node kill state the map omits, an invented field with zero consumers, and two mis-homed claims. The frozen `NodeState.slow…"
    },
    {
      "type": "workflow_agent",
      "index": 57,
      "label": "A:E.1.3:PERF-TRUTH",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "acc92f50a8b2c107b",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797408380,
      "queuedAt": 1782797237446,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "echo \"=== KNOWN-RACE 449-464 ===\"; sed -n '445,470p' CoreFr…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797621952,
      "tokens": 83296,
      "toolCalls": 13,
      "durationMs": 213572,
      "resultPreview": "VERDICT — lens [PERF-TRUTH]. All claims grounded at HEAD 0ee227a.\n\n## REFUTED\n\n**R1 — The TP/SL ternary → `Money_BlendOnMask` is sold as a latency/p99 win; it is NOT. It is a near-pure latency LOSS that buys only conformance.** (ExecutionCore.hpp:337-338; blend at FixedPointN.hpp:1876-1879)\nThe baseline (carried into the leaf-map's branchImpact[3]/F4) calls this \"the ONE real H7/H20 violation … re…"
    },
    {
      "type": "workflow_agent",
      "index": 58,
      "label": "A:E.1.3:CORRECTNESS",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a2b609ce47e1920d7",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797425536,
      "queuedAt": 1782797237446,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "f=\"plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797690864,
      "tokens": 92830,
      "toolCalls": 15,
      "durationMs": 265327,
      "resultPreview": "Determinism-lens grounding complete. Verdict below — every claim cited to HEAD `0ee227a`.\n\n---\n\n## REFUTE VERDICT — [CORRECTNESS / determinism] lens on the E.1.3 leaf-map\n\n### CLAIMS I REFUTE\n\n**R1 — \"re-opening NodeState.slow_account forces a SECOND sharded version bump on top of E.1.2's 10→11\" (realignRisks, F-realign-1). PARTIALLY WRONG — conflates in-memory layout with wire layout.**\nThe shard…"
    },
    {
      "type": "workflow_agent",
      "index": 59,
      "label": "A:E.1.3:SIMPLICITY",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a6569b4a01c3f57fc",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797428021,
      "queuedAt": 1782797237447,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -rEn \"(SHARDED|PORTFOLIO|CONTROLLER)_SNAPSHOT_VERSION\"…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797666092,
      "tokens": 92575,
      "toolCalls": 11,
      "durationMs": 238070,
      "resultPreview": "## REFUTE VERDICT — [SIMPLICITY lens] on the E.1.3 leaf-map's E.1.2-freeze demands\n\n**Meta-refute (the spine of every sub-refute below):** The leaf-map conflates two struct classes that carry *opposite* freeze economics, and applies wire-grade \"freeze-once-or-re-touch-a-FINAL-struct\" rigor to the wrong one.\n\n- **WIRE-bound (genuinely must-freeze):** `Position` + the sharded/portfolio snapshot payl…"
    },
    {
      "type": "workflow_agent",
      "index": 60,
      "label": "A:E.1.3:SCALE",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "aaf06478bc01c5331",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797429582,
      "queuedAt": 1782797237447,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "echo \"=== def grep ===\"; rg -n \"define\\s+(NUM_EXCHANGES|MAX…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797688471,
      "tokens": 97399,
      "toolCalls": 14,
      "durationMs": 258889,
      "resultPreview": "## REFUTE VERDICT — lens [SCALE] / H22 (N+1)-node·cluster\n\nGrounded at HEAD 0ee227a. I read the live code, the foundation sketch (E.1-foundation.md:420-712), and the kill/account paths. Default-refuted; one central claim collapses.\n\n### REFUTED\n\n**R1 — `layoutNeeds[L1]` \"balance + ks_peak_balance MUST sit INSIDE the per-node slow_account pack\" is H22-REGRESSIVE — it re-flattens the exact global it…"
    },
    {
      "type": "workflow_agent",
      "index": 61,
      "label": "A:E.1.3:OVER-RESERVE",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a92e8e9351c791e9b",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797465308,
      "queuedAt": 1782797237447,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 DL=plans/v5.15-live-r…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797733948,
      "tokens": 103745,
      "toolCalls": 8,
      "durationMs": 268640,
      "resultPreview": "## A-CLASS REFUTE VERDICT — Lens: [OVER-RESERVE] (critical-only)\n\nDefault-skeptical. Every claim grounded at `file:line`/decision-log. I make the leaf justify every byte it asks E.1.2 to freeze.\n\n---\n\n### REFUTED — over-reserve (do NOT freeze these in E.1.2)\n\n**R1 — `AggregatorState` global-total line + `per_cluster[]` slice array (realign C-1 \"CRITICAL\"; leaf:67 INBOUND \"freeze per_cluster[] pres…"
    },
    {
      "type": "workflow_agent",
      "index": 62,
      "label": "map:OPT",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "aa2a9cb794151a798",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797474321,
      "queuedAt": 1782797278346,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Consolidate these 5 single-lens investigations of E-leaf OPT (the optimization leaf (D-229 / #9 — slow+hot branchless/compute reduction)) into ONE structured leaf-map. De-dupe; KEEP the code-cited specifics; preserve hot-path findings prominently.\n\n--- investigation 1 ---\nI have the grounding I need. Here is my tight LAYOUT-lens finding set.\n\n---\n\n## OPT leaf (D-229/#9 slow + #15 hot) — LAYOUT inp…",
      "lastProgressAt": 1782797575516,
      "tokens": 68713,
      "toolCalls": 1,
      "durationMs": 101195,
      "resultPreview": "{\"layoutNeeds\":[\"RollingStats / slow_state must be POINTER-HELD (out-of-line) in the frozen NodeState, NOT embedded by-value. It currently embeds by-value at PortfolioController.hpp:252 (RollingStats<F> rolling;) AND is pointer-alloc'd at :475, carrying its own size-pins (RollingStats.hpp:141-144). If NodeState embeds it by value, OPT's later fixed-K deque restructure changes sizeof(RollingStats) …"
    },
    {
      "type": "workflow_agent",
      "index": 63,
      "label": "A:E.1.6:NECESSITY",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a61914b7cef8d1766",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797502084,
      "queuedAt": 1782797291406,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== load side:…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797790878,
      "tokens": 105193,
      "toolCalls": 14,
      "durationMs": 288794,
      "resultPreview": "VERDICT — NECESSITY lens (REFUTE that the claimed layout-needs must be stored in the frozen struct). Grounded at HEAD 0ee227a.\n\n## HEADLINE REFUTE (mis-costed across the whole map): the \"snapshot epoch re-open\" threat is a PHANTOM for every NodeState/ClusterState field\n\nThe map repeatedly prices a deferred field-add as a **snapshot-epoch re-open** (golden regen / can't-restore-old-snapshots): mode…"
    },
    {
      "type": "workflow_agent",
      "index": 64,
      "label": "A:E.1.6:FIT",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "aefb3a6522d5d6c36",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797511510,
      "queuedAt": 1782797291406,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== D-15 / D-28…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797811034,
      "tokens": 100667,
      "toolCalls": 10,
      "durationMs": 299524,
      "resultPreview": "I have enough grounding. The decision log is decisive: **D-193/H22** (operator-ratified, 2026-06-11) mandates scaling across \"N nodes AND N clusters\" with the operational test \"adding the (N+1)th cluster requires ZERO change to existing per-shard logic.\" That collides head-on with the sketch's dimensioning. Verdict below.\n\n---\n\n## FIT-LENS REFUTE VERDICT — E.1.6 leaf-map vs the proposed E.1.2 free…"
    },
    {
      "type": "workflow_agent",
      "index": 65,
      "label": "A:E.1.6:PERF-TRUTH",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a56693298fb8dd0cb",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797520770,
      "queuedAt": 1782797291407,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"=== Money_Blend…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797674273,
      "tokens": 75652,
      "toolCalls": 7,
      "durationMs": 153502,
      "resultPreview": "I have enough grounding. Verdict below.\n\n## REFUTE VERDICT — [PERF-TRUTH lens] on the E.1.6 leaf-map\n\n### Claims that SURVIVE (E.1.6's own branchImpact)\n\n- **\"hot-path NULL on both axes — E.1.6 adds ZERO per-tick branches\"** — SURVIVES, code-confirmed. `fee_rate`/`fee_rate_taker` = **0 hits** in `CoreFrameworks/ExecutionCore.hpp` + `CoreFrameworks/GateParameters.hpp`; the only `fee_rate` consumers…"
    },
    {
      "type": "workflow_agent",
      "index": 66,
      "label": "A:E.1.6:CORRECTNESS",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a67c39c3e498280f2",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797524102,
      "queuedAt": 1782797291407,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== trading_mo…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797801959,
      "tokens": 88938,
      "toolCalls": 12,
      "durationMs": 277857,
      "resultPreview": "## REFUTE VERDICT — lens: [CORRECTNESS] determinism (D-110 round-trip / snapshot-version / H4·H9·H12)\n\nGrounded at HEAD 0ee227a. Snapshot versions confirmed: PORTFOLIO=**7** (Portfolio.hpp:534), SHARDED=**10** (ShardedSnapshotPersist.hpp:94), CONTROLLER=**14** (PortfolioController.hpp:2025) — orchestrator's \"7→8 / 10→11\" is correct vs HEAD. (NOTE: Portfolio.hpp:45-142 header comments still say \"=6…"
    },
    {
      "type": "workflow_agent",
      "index": 67,
      "label": "A:E.1.6:SIMPLICITY",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "abdca44709cb8714a",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797537101,
      "queuedAt": 1782797291407,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== ExchangeAd…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797766453,
      "tokens": 91917,
      "toolCalls": 9,
      "durationMs": 229352,
      "resultPreview": "## A-CLASS REFUTE VERDICT — lens: [SIMPLICITY / minimize frozen-layout surface]\n\n**Headline: the leaf-map materially OVER-FREEZES.** Its load-bearing premise — *\"reserve E.1.6's fields in NodeState/ClusterState NOW or the struct re-opens → snapshot epoch (plan:97 forbids)\"* — is REFUTED by the actual persistence code for **every NodeState/ClusterState field**. The genuine wire-freeze surface is ~1…"
    },
    {
      "type": "workflow_agent",
      "index": 68,
      "label": "map:D-206",
      "phaseIndex": 2,
      "phaseTitle": "Investigate",
      "agentId": "aca9b3d4fad4e8d87",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797540489,
      "queuedAt": 1782797309772,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Consolidate these 5 single-lens investigations of E-leaf D-206 (exit trailing-profit redesign (the Position peak field)) into ONE structured leaf-map. De-dupe; KEEP the code-cited specifics; preserve hot-path findings prominently.\n\n--- investigation 1 ---\n## D-206 LAYOUT LENS — what the trailing-exit redesign needs from the FROZEN Position/Portfolio/NodeState/ClusterState\n\nGrounded at HEAD 0ee227a…",
      "lastProgressAt": 1782797647646,
      "tokens": 69904,
      "toolCalls": 1,
      "durationMs": 107157,
      "resultPreview": "{\"layoutNeeds\":[\"peak_price (NEW Money 16B, PERSIST) — append after the 9 existing rows in FOREACH_POSITION_FIELD @ MemHeaders/PositionFieldRegistry.hpp:51-59; max-favorable-excursion / give-back anchor; no peak/MFE state exists today (only core_peak_balance equity kill-switch). COLD / 3rd cache line, slow-path written+read, NOT hot-gate read. SOLE field forcing Position 128->144->alignas(64)->192…"
    },
    {
      "type": "workflow_agent",
      "index": 69,
      "label": "synth:E.1.4",
      "phaseIndex": 4,
      "phaseTitle": "Synthesize",
      "agentId": "aa12e3021025f3857",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797575556,
      "queuedAt": 1782797520777,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "E.1.4 — fill-completeness (A16 partial-accumulate / A20-A21…",
      "promptPreview": "Synthesize the FINAL layout-readiness verdict for E-leaf E.1.4 (fill-completeness), weighting the adversarial refutations against the leaf-map. Drop refuted/speculative claims; keep only what survives, code-cited.\n\nLEAF-MAP:\n{\"layoutNeeds\":[\"NEW FIELD owner_node_id (D-209) on Position<F> — REQUIRED by E.1.4, ZERO occurrences in code at HEAD 0ee227a (grep clean across CoreFrameworks/ MemHeaders/). …",
      "lastProgressAt": 1782797829971,
      "tokens": 92044,
      "toolCalls": 3,
      "durationMs": 254415,
      "resultPreview": "{\"leaf\":\"E.1.4 — fill-completeness (A16 partial-accumulate / A20-A21 venue-net reconcile / Class-46 close-by-fill_qty / §4c conservation / §5 event-log)\",\"verdict\":\"YELLOW — Position half is GREEN (safe to freeze at 128B for E.1.4: fill adds ZERO new Position fields; A16 weighted-avg + Class-46 partial-close mutate existing Money fields in place). NodeState half is the contingent surface. The leaf…"
    },
    {
      "type": "workflow_agent",
      "index": 70,
      "label": "A:OPT:NECESSITY",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a490ec07ea1b17a13",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797579245,
      "queuedAt": 1782797575584,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "sed -n '410,470p;620,700p' plans/v5.15-live-readiness/subpl…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797789341,
      "tokens": 84575,
      "toolCalls": 8,
      "durationMs": 210095,
      "resultPreview": "I have enough grounding. Here is my NECESSITY-lens verdict.\n\n---\n\n## REFUTE VERDICT — [NECESSITY] lens on E.1.2 layout-needs\n\nDefault: not-needed-in-the-freeze unless code proves otherwise. Grounded at HEAD 0ee227a.\n\n### REFUTED claims\n\n**layoutNeed #1 — \"RollingStats must be POINTER-HELD or OPT's restructure RED-builds the size-pin and re-opens the frozen NodeState\" → REFUTED (premise fails on it…"
    },
    {
      "type": "workflow_agent",
      "index": 71,
      "label": "A:OPT:FIT",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a103b0f2ca02693c1",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797621954,
      "queuedAt": 1782797575585,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== node_peak_…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797872933,
      "tokens": 101079,
      "toolCalls": 14,
      "durationMs": 250978,
      "resultPreview": "FIT-lens verdict (REFUTE that the proposed frozen layout accommodates the OPT/D-229 leaf without a later re-touch). All claims grounded at HEAD 0ee227a.\n\n## REFUTED\n\n**R1 — realignRisks #1 (\"the single genuine layout coupling\") cites the WRONG struct and is structurally a phantom on the frozen path.**\nThe map asserts the by-value RollingStats embed lives at `PortfolioController.hpp:252` (`RollingS…"
    },
    {
      "type": "workflow_agent",
      "index": 72,
      "label": "A:OPT:PERF-TRUTH",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "af81fbce35eda512d",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797647647,
      "queuedAt": 1782797575585,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -rn \"core->active\\b\\|->active =\\|\\.active =\\|active_b …",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797805501,
      "tokens": 77397,
      "toolCalls": 6,
      "durationMs": 157854,
      "resultPreview": "PERF-TRUTH refute complete. Grounded at HEAD 0ee227a against ExecutionCore.hpp:315-610, RollingStats.hpp:300-367/141-144, FixedPointN.hpp:1850-1879.\n\n## REFUTED claims\n\n**R1 — #15 (ExecutionCore.hpp:337-338 ternary→Money_BlendOnMask) is NOT a p99-variance win. The PERF framing is false.**\n`core->active` is written ONLY at ExecutionCore.hpp:609-610 (`core->active = (active | enter_a_eff) & ~exit_a_…"
    },
    {
      "type": "workflow_agent",
      "index": 73,
      "label": "A:OPT:CORRECTNESS",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a82bc129cbd4d95f7",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797649394,
      "queuedAt": 1782797575585,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; echo \"=== how active…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797832302,
      "tokens": 83894,
      "toolCalls": 9,
      "durationMs": 182908,
      "resultPreview": "Grounded all claims in code at HEAD. Verdict below from the [CORRECTNESS / determinism] lens.\n\n## REFUTED claims\n\n**R1 — The RollingStats \"WIRE-FREE\" evidence cites the WRONG file (conflates GUI snapshot with warm-restart snapshot).**\nThe map asserts: *\"RollingStats restructure is WIRE-FREE ... (ShardedSnapshot.hpp:58-64 serializes only derived scalars price_avg/stddev/min/max, never the deque int…"
    },
    {
      "type": "workflow_agent",
      "index": 74,
      "label": "A:OPT:SIMPLICITY",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a117a0860fc95c00a",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797666094,
      "queuedAt": 1782797575586,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "echo \"=== positions[] reads on hot/gated path (ExecutionCor…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797872330,
      "tokens": 94972,
      "toolCalls": 12,
      "durationMs": 206235,
      "resultPreview": "I have enough grounding across code and the decision log. Final verdict.\n\n---\n\nVERDICT (lens = SIMPLICITY / less-frozen-surface). The leaf-map over-freezes on two axes; one of its \"simpler\" hedges is a code-false trap.\n\n## REFUTED\n\n**R1 — layoutNeed #1 (\"RollingStats must be POINTER-HELD in the frozen NodeState, else OPT's fixed-K restructure RED-builds the size-pin AND re-opens NodeState\"). This …"
    },
    {
      "type": "workflow_agent",
      "index": 75,
      "label": "synth:E.1.5",
      "phaseIndex": 4,
      "phaseTitle": "Synthesize",
      "agentId": "ab49a671193294f8c",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797674274,
      "queuedAt": 1782797579253,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "E.1.5 — per-cluster purity (H22 scale-invariance): NEW-1 tr…",
      "promptPreview": "Synthesize the FINAL layout-readiness verdict for E-leaf E.1.5 (per-cluster purity (H22 scale-invariance)), weighting the adversarial refutations against the leaf-map. Drop refuted/speculative claims; keep only what survives, code-cited.\n\nLEAF-MAP:\n{\"layoutNeeds\":[\"[HIGH/CONVERGENT 7 lenses] ClusterState.trading_mode — uint8_t, ABSENT from the E.1.2/E.1-foundation ClusterState sketch (foundation.m…",
      "lastProgressAt": 1782797951478,
      "tokens": 99289,
      "toolCalls": 5,
      "durationMs": 277204,
      "resultPreview": "{\"leaf\":\"E.1.5 — per-cluster purity (H22 scale-invariance): NEW-1 trading_mode per-cluster authority relocation + A34 cluster same-symbol concentration governor. Grounded at HEAD 0ee227a (leaf scaffold cited 572c978 — stale, c-class re-ground mandated).\",\"verdict\":\"YELLOW — layout-readiness is CONDITIONAL. After weighting 7 refutations, E.1.5 is hot-path-INERT and the SURVIVING frozen set E.1.2 mu…"
    },
    {
      "type": "workflow_agent",
      "index": 76,
      "label": "A:D-206:NECESSITY",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a0415420eeef0b991",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797688473,
      "queuedAt": 1782797647657,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/caramel/code/FoxML_Trader_v2/CoreFrameworks/Portfolio…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797969658,
      "tokens": 98562,
      "toolCalls": 10,
      "durationMs": 281185,
      "resultPreview": "## REFUTE VERDICT — lens [NECESSITY]: are the claimed frozen-Position layout-needs real?\n\nGrounded at HEAD 0ee227a. Bottom line: the map's **central thesis is REFUTED** — the one field that forces Position 128→192B (`peak_price`) is **not a necessary persisted field**. The map names the wrong field; the load-bearing persisted state is the give-back **FLOOR**, which already has a working, restart-s…"
    },
    {
      "type": "workflow_agent",
      "index": 77,
      "label": "A:D-206:FIT",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a30945fef426ab444",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797690865,
      "queuedAt": 1782797647657,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2 echo \"===== F-096 @ A…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782798088561,
      "tokens": 117716,
      "toolCalls": 12,
      "durationMs": 397696,
      "resultPreview": "## FIT VERDICT — D-206 (exit trailing-profit / Position PEAK) vs the E.1.2 freeze\n\nDefault-refuted; I concede only what the code proves sound. Every cite is HEAD `0ee227a`.\n\n### REFUTED claims\n\n**[R-1 · CRITICAL] `layoutNeeds#4` + headline \"NET: ZERO new hot fields … Freeze can lock hot.params against D-206\" — REFUTED by the seqlock channel.**\nThe leaf's OWN requirement is per-leg independent ridi…"
    },
    {
      "type": "workflow_agent",
      "index": 78,
      "label": "A:D-206:PERF-TRUTH",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "aa1251149a174a50e",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797733950,
      "queuedAt": 1782797647658,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/caramel/code/FoxML_Trader_v2/CoreFrameworks/GateParam…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782797976003,
      "tokens": 84547,
      "toolCalls": 7,
      "durationMs": 242053,
      "resultPreview": "## REFUTE VERDICT — lens [PERF-TRUTH] on the D-206 leaf-map + hot-path baseline\n\nScope note: the D-206 **leaf-map itself is mostly clean under this lens** — it explicitly disclaims any branch-reduction win (\"ADJACENCY not subsumption… keep the 337/338 fix on its own leaf; do not claim a branch-reduction win D-206 doesn't earn\"). The refutable perf claims live in the **hot-path BRANCH BASELINE** th…"
    },
    {
      "type": "workflow_agent",
      "index": 79,
      "label": "A:D-206:CORRECTNESS",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "aa993b340570e62cd",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797766458,
      "queuedAt": 1782797647658,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/caramel/code/FoxML_Trader_v2/CoreFrameworks/EngineSha…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782798011230,
      "tokens": 94601,
      "toolCalls": 11,
      "durationMs": 244772,
      "resultPreview": "VERDICT — A-class adversarial, lens = [CORRECTNESS: determinism / warm-restart money-exact round-trip / snapshot-version / H4/H9/H12]. All grounded at HEAD 0ee227a.\n\n## REFUTED (map claim is wrong or unsoundly reasoned)\n\n**REFUTE-1 — R2 \"torn snapshot from two divergent geometries\" is mis-framed; the REAL hazard is H12 uninitialized trailing pad.** Verified: `Portfolio_Save` writes `POSITION_PERSI…"
    },
    {
      "type": "workflow_agent",
      "index": 80,
      "label": "A:D-206:SIMPLICITY",
      "phaseIndex": 3,
      "phaseTitle": "Refute",
      "agentId": "a10d07ef51e2e58ba",
      "agentType": "a-class",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797789343,
      "queuedAt": 1782797647658,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/caramel/code/FoxML_Trader_v2; f=$(find . -path ./b…",
      "promptPreview": "ENGINE /home/caramel/code/FoxML_Trader_v2 @ HEAD 0ee227a (tag v5.15.5.F.4d.1.E.1.1). You are pre-armed: read DOCS/SUBAGENT_ARMING.md FIRST. Ground EVERY claim in code (file:line) or the decision log; flag where a plan is STALE vs HEAD (its offsets predate E.1.1). The HOT PATH is the priority lens throughout.\n\nWHY THIS AUDIT: E.1.2 is the LAYOUT-FREEZE leaf — every downstream E leaf reads Position/…",
      "lastProgressAt": 1782798026850,
      "tokens": 103244,
      "toolCalls": 12,
      "durationMs": 237506,
      "resultPreview": "VERDICT — A-CLASS [SIMPLICITY lens] on the D-206 leaf-map. Grounded at HEAD 0ee227a.\n\n## REFUTED (over-engineered freeze surface)\n\n**REFUTE-1 — The leaf-map prices the peak field in AoS-192B terms that E.1.2 is *removing*. [the core refutation]**\nThe map's heaviest cost claims — R1 (\"128→144→alignas→192B, 3 cache lines\"), R2 (\"prefix-vs-full persist geometries DIVERGE at 192B → torn snapshot, reco…"
    },
    {
      "type": "workflow_agent",
      "index": 81,
      "label": "synth:E.1.3",
      "phaseIndex": 4,
      "phaseTitle": "Synthesize",
      "agentId": "a263588fb358b333b",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797790879,
      "queuedAt": 1782797733959,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "E.1.3 — aggregator + cross-thread coherence (SPINE-2, capit…",
      "promptPreview": "Synthesize the FINAL layout-readiness verdict for E-leaf E.1.3 (aggregator + cross-thread coherence (SPINE-2, capital cluster)), weighting the adversarial refutations against the leaf-map. Drop refuted/speculative claims; keep only what survives, code-cited.\n\nLEAF-MAP:\n{\"layoutNeeds\":[\"slow_account money pack: balance (Money) AND ks_peak_balance (Money) MUST sit INSIDE the single seqlock-published…",
      "lastProgressAt": 1782798157947,
      "tokens": 118891,
      "toolCalls": 5,
      "durationMs": 367067,
      "resultPreview": "{\"leaf\":\"E.1.3 — aggregator + cross-thread coherence (SPINE-2, capital cluster). NOTE: the task's leaf identity matches E-dependency-graph.md:125-140 (E.1.3 ★ = aggregator + torn-read + absorption + conc-5, SPINE-2), but decision-log:1435 (DECIDED 2026-06-15) maps the aggregator+torn-read to E.1.4 — unreconciled artifact drift (see openQuestions).\",\"verdict\":\"YELLOW — layout-readiness conditional.…"
    },
    {
      "type": "workflow_agent",
      "index": 82,
      "label": "synth:E.1.6",
      "phaseIndex": 4,
      "phaseTitle": "Synthesize",
      "agentId": "ad4a5776c288a26c5",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797811038,
      "queuedAt": 1782797811036,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "E.1.6 — multi-exchange (FOREACH_EXCHANGE adapter machinery,…",
      "promptPreview": "Synthesize the FINAL layout-readiness verdict for E-leaf E.1.6 (multi-exchange), weighting the adversarial refutations against the leaf-map. Drop refuted/speculative claims; keep only what survives, code-cited.\n\nLEAF-MAP:\n{\"layoutNeeds\":[\"NodeState.hot.mode — uint8_t byte (MBS_* 2-bit slot, 6 bits headroom), holds the relocated global trading_mode PAPER=0/LIVE=1/SHADOW=2 (ControllerConfig.hpp:41-4…",
      "lastProgressAt": 1782798018144,
      "tokens": 84432,
      "toolCalls": 4,
      "durationMs": 207106,
      "resultPreview": "{\"leaf\":\"E.1.6 — multi-exchange (FOREACH_EXCHANGE adapter machinery, 4-state mode, venue-sourced fee, ENUM_STRICT metadata widen)\",\"verdict\":\"YELLOW — E.1.6 is layout-ready in the load-bearing sense: code-grounded, it adds ZERO per-tick branches (hot-path NULL survives all five lenses) and its entire NodeState/ClusterState substrate is forward-created by E.1.2's freeze, never re-touched by E.1.6. …"
    },
    {
      "type": "workflow_agent",
      "index": 83,
      "label": "synth:OPT",
      "phaseIndex": 4,
      "phaseTitle": "Synthesize",
      "agentId": "a24165e88d4b72cd0",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782797872936,
      "queuedAt": 1782797872935,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "E-leaf OPT (D-229 / #9 — slow+hot branchless/compute reduct…",
      "promptPreview": "Synthesize the FINAL layout-readiness verdict for E-leaf OPT (the optimization leaf (D-229 / #9 — slow+hot branchless/compute reduction)), weighting the adversarial refutations against the leaf-map. Drop refuted/speculative claims; keep only what survives, code-cited.\n\nLEAF-MAP:\n{\"layoutNeeds\":[\"RollingStats / slow_state must be POINTER-HELD (out-of-line) in the frozen NodeState, NOT embedded by-v…",
      "lastProgressAt": 1782798171001,
      "tokens": 90100,
      "toolCalls": 8,
      "durationMs": 298064,
      "resultPreview": "{\"leaf\":\"E-leaf OPT (D-229 / #9 — slow+hot branchless/compute reduction). No dedicated plan; exists as D-229/D-235/D-236 + a sub-master tracking line. Runs LATE (E.1.M-class, after E.1.2's freeze); verified layout-order-INDEPENDENT.\",\"verdict\":\"YELLOW — Conditionally ready. OPT imposes essentially NO structural layout coupling on E.1.2's freeze: every OPT target is verified layout-inert (#15 reads…"
    },
    {
      "type": "workflow_agent",
      "index": 84,
      "label": "synth:D-206",
      "phaseIndex": 4,
      "phaseTitle": "Synthesize",
      "agentId": "aad8592c91957c6d0",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782798088567,
      "queuedAt": 1782798088563,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "D-206 — exit trailing-profit redesign (ride-past-TP → ratch…",
      "promptPreview": "Synthesize the FINAL layout-readiness verdict for E-leaf D-206 (exit trailing-profit redesign (the Position peak field)), weighting the adversarial refutations against the leaf-map. Drop refuted/speculative claims; keep only what survives, code-cited.\n\nLEAF-MAP:\n{\"layoutNeeds\":[\"peak_price (NEW Money 16B, PERSIST) — append after the 9 existing rows in FOREACH_POSITION_FIELD @ MemHeaders/PositionFi…",
      "lastProgressAt": 1782798325103,
      "tokens": 87584,
      "toolCalls": 4,
      "durationMs": 236535,
      "resultPreview": "{\"leaf\":\"D-206 — exit trailing-profit redesign (ride-past-TP → ratchet SL UP → give-back-from-peak; the Position peak/MFE field). Layout-readiness for the E.1.2 Position/NodeState freeze. Grounded at HEAD 0ee227a.\",\"verdict\":\"YELLOW — Position layout is NOT yet safe to freeze as the leaf-map specifies. The structural survivors (riding-flag as a named zero-init field, version threading 7→8/10→11, w…"
    },
    {
      "type": "workflow_agent",
      "index": 85,
      "label": "global-synth",
      "phaseIndex": 5,
      "phaseTitle": "Global synthesis",
      "agentId": "a393088d1630fa61a",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782798325144,
      "queuedAt": 1782798325107,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are the FINAL synthesizer of an E-umbrella forward audit whose purpose is to let E.1.2 freeze the Position/Portfolio/NodeState/ClusterState layout ONCE, with the HOT PATH as the priority.\n\nHOT-PATH BASELINE: {\"summary\":\"Consolidated H7/H20 jitter inventory across 3 investigations (all HEAD 0ee227a, check_latency_path_conformance.py @ production -O3 -march=native, no LATENCY_PROFILING). HOT PAT…",
      "lastProgressAt": 1782798690113,
      "tokens": 105025,
      "toolCalls": 1,
      "durationMs": 364968,
      "resultPreview": "{\"frozenLayoutSet\":[\"MONEY-TYPING (CONFIRMED-NEEDED; needed by E.1.3/E.1.4/E.1.5/E.1.6/OPT — universal): ALL slow_account + drainer_state + AggregatorState money fields freeze as Money (FixedPoint<10,8>, 16B), NEVER FPN_Binary<F>. The foundation sketch (E.1-foundation.md:434-440/452-454/684-688) STALE-types these FPN<F> — that is an H4 violation + a SILENT encoding epoch (FPN binary-scale 2^F vs M…"
    }
  ],
  "totalTokens": 7538683,
  "totalToolCalls": 833
}