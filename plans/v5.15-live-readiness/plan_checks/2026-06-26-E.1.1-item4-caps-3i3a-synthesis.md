---
type: precoding-cascade-synthesis
ship_tag: v5.15.5.F.4d.1.E.1.1
item: "③ item-4 — per-field capital value-range caps (+ item-3 dollar floors)"
date: 2026-06-26
cascade: 3-I -> 3-A (i-class x3, a-class x3) + orchestrator AR-11 code-read
verdict: YELLOW (sound + codeable; 5 operator decisions + the legacy-array CRITICAL before coding)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
engine_head: be5d803
status: DECISIONS-MADE 2026-06-27 (decision-log D-265..D-271); deeper investigation IN FLIGHT (D-I1 full write-path / D-I2 B blast-radius / D-I3 hot-swap) before coding
---

# E.1.1 ③ item-4 — 3-I→3-A pre-coding cascade synthesis

**Verdict: YELLOW.** The decided design (post-resolve sweep → `CFG_FAULT_CAPITAL_OUT_OF_RANGE` → `cfg_compile_ok` ALWAYS-ABORT; D-248/D-253/D-254) is SOUND and codeable. The cascade surfaced 1 CRITICAL coverage hole (4-agent consensus), 1 scope-expander, 1 design-shape correction, and several nuances — all needing an operator call BEFORE coding.

## Cascade
- **I-1** sweep mechanics + coverage · **I-2** cap thresholds + units · **I-3** fault-bit/gate/hybrid-seam/determinism
- **A-1** refute coverage · **A-2** refute cap semantics/units · **A-3** refute integration/determinism/risk-triage
- **Orchestrator AR-11 code-reads** (verify, don't propagate): barrier_is_corrupt body · `*_mult` rows + override clamp · node_risk_pct denominator · starting_balance kill-disarm · cap-shape decision text.

## AR-11 code-read verdicts (orchestrator-confirmed against HEAD be5d803)

1. **CRITICAL — legacy-array channel must be swept explicitly (4-agent consensus: I-1/I-2/I-3/A-3).** `risk_pct`/`max_drawdown_pct` flow through `node_risk_pct[16]`/`node_max_drawdown_pct[16]` (`FOREACH_MANUAL_PER_NODE_FIELD`, CfgFieldRegistry.hpp:835/838). `ResolveForCore` (ControllerConfig.hpp:1452-1486) does NOT merge them → `nodes[c].risk_pct` is a stale shadow = global. Live consumers read the ARRAYS: allocation `total_balance * node_risk_pct[c]` (Async.hpp:355, Run.hpp:923-924, BacktestSharded.hpp:273-274), kill threshold (ControllerEventLoop.hpp:2918). **A `nodes[c]`-only sweep passes `node_0_risk_pct=999` → 999% allocation = the founding bug.** Sweep MUST read both arrays directly, 16 slots. CONFIRMED in code (Async.hpp:347-358 read).

2. **SCOPE-EXPANDER (A-1, orchestrator-CONFIRMED) — the `*_mult` stop-distance surface.** Momentum's operative SL = `stddev * momentum_sl_mult` (StrategyParameters.hpp:568-573); capped `stop_loss_pct` is used ONLY when `momentum_sl_mult==0`. Trailing SL/TP for MR/ML/EmaCross = `stddev * sl_trail_mult` / `*_trail_mult`. These rows are **meta=0 / UNTAGGED** (CfgFieldRegistry.hpp:534/535/555/556/562/564/687 — verified, none carry CAPITAL_BOUND) → a metadata-bit sweep SKIPS them. Flat path clamps to `[0,10]` (CfgFieldDispatch.hpp:129), but the **per-node override path is UNCLAMPED** (`cfg_capture_node_raw_override` ControllerConfig.hpp:1391-1400, `return FPN_FromDouble(d)` no clamp). So `node_2_momentum_sl_mult=999` (override) → stop placed ~999σ away = effectively disabled = same silent-disable class as `max_drawdown_pct=999`. Partial backstop: capped `max_drawdown_pct` kill (if itself sane). **OPERATOR SCOPE DECISION.**

3. **DESIGN-SHAPE (A-2 vs I-2; orchestrator-adjudicated → A-2 direction, reconciled).** `barrier_is_corrupt` body (BarrierValidation.hpp:37-41) reduces, for a LOSS field via dummy-slot `barrier_is_corrupt(0.0,v)`, to `v<0 || v>1.0` — functionally identical to a Money-native `capital_out_of_range(v, cap)`. But the dummy-slot reuse is a **cascade landmine**: barrier_is_corrupt is a shared MODEL-barrier SSoT (also StampHelper.hpp:261 + NodeModelZoo.hpp:1138); a future pair-clause (e.g. `tp<sl`) silently rewrites the cfg-cap meaning → mass false-refuse at boot (Knight/H21 shape). Plus a needless `Money->double` crossing + 2 vacuous `isfinite` clauses. **Resolution: Money-native single-field predicate IMPORTING the SSoT constants** (`BARRIER_SANE_MAX_SL=1.0`/`BARRIER_SANE_MAX_TP=10.0`), dispatched by the LOSS/GAIN bit. This single-sources the BOUNDS (no Class-43 — addresses I-2) while decoupling the predicate (addresses A-2), and is consistent with **D-253's own words: "single-field caps via the constants is correct"** (decision-log:1632). Refines D-248's "reuse the function" framing.

4. **node_risk_pct denominator (A-1, CONFIRMED).** Async.hpp:347-358: global `risk_pct` path = `(total * risk_pct)/num_nodes` (DIVIDED); array path = `total * node_risk_pct[c]` (DIRECT). Different quantities → validate `node_risk_pct[c] ≤ 1.0` AND `risk_pct ≤ 1.0` SEPARATELY, not as one "effective" value. (`max_drawdown_pct` IS clean array-else-global.)

5. **item-3 dollar floors (A-2, CONFIRMED).** `starting_balance > 0` is LOAD-BEARING: PortfolioController.hpp:957/966 guard the kill checks on `!Money_IsZero(session_start_equity/peak_equity)` → `starting_balance=0` seeds 0 → both kills SILENTLY SKIPPED. `min_kill_loss` must be `≥0` (reject negative only) NOT `>0` — it's a dollar floor (ControllerEventLoop.hpp:2922); `0` = fire on pct alone = fail-safe; `>0`-refuse would false-refuse a legit "off". Stay Money-space (Money_IsZero/Money_Lt), not barrier_is_corrupt.

6. **Units HOLD (I-1/I-2/A-2 + orchestrator).** Stored = FRACTION (÷100 at parse, CfgFieldDispatch.hpp:127); barrier bounds + caps fraction → NO `/100` in the sweep; do NOT use descriptor `clamp_max=100.0` (percent-space, vacuous 10000% ceiling). RegimeDetector.hpp:724 `×100` is benign (stddev-mult convention). Boundary `>` inclusive (exactly 100%/1000% PASS) correct for no-margin; no Money→double precision flip (1.0/10.0 exact).

7. **Determinism / Class-49 / H22 (I-3/A-3, HOLD).** GUARANTEE-0 holds IFF sweep is INSIDE `ControllerConfig_Load` (after PopulateCoresFromFlat:3287, before return:3289) — live path has no fingerprint-zeroing backstop, only the main.cpp:203 gate. OUT_OF_RANGE distinct bit (Class-49 PASS). Boot-time only, no hot-path, per-node-pure (H22 intact). **LOW hardening (A-3):** the BacktestSharded.hpp:131 flag-zeroing is success-path-only; the :127 early-return leaves flags non-zero → the train-fingerprint GUARANTEE rests on a GUI `sample_count>=10` gate (fragile for future headless training) — zero flags on the early-return too / assert flags==0 at the fingerprint caller.

## Settled implementation shape (no decision needed)
- Standalone `cfg_capital_value_sweep(cfg)` called from `ControllerConfig_Load:3288` (NOT folded into PopulateCoresFromFlat — runs in Default too; I-1/I-3 over I-2).
- Set A (15 CAPITAL_BOUND rows, 10 LOSS + 5 GAIN): `if constexpr` X-macro walk over FOREACH_PER_NODE_CFG_FIELD reading `nodes[c].name` (mirror EMIT_PER_NODE_COPY:1527) — NOT a FIELD_IDX mask-offset read (no such helper exists for nodes[c]).
- Set B (2 legacy arrays): explicit effective-value check per slot.
- Set C (item-3 dollar globals): explicit Money-space floor.
- Enumerate from the CAPITAL_BOUND bits, never a prose list (Class 33).
- Cites drifted: real sweep site :3288, legacy arrays :2934/:2935 (not the stale D-253/254 :3255/:2847).

## OPERATOR DECISIONS — RESOLVED 2026-06-27 (→ decision-log D-265..D-271)

**Resolved:** legacy-array swept via **B = UNIFY** into the resolved per-node read (D-267, blast-radius owed) · **`*_mult` IN-SCOPE** via the single path (D-268) · cap shape = **Money-native helper importing the SANE constants**, decoupled from barrier_is_corrupt (D-266) · floors `starting_balance>0` / `min_kill_loss≥0` (D-269) · cfg-reload gate → item-5, **strategy + ML-model hot-swap accounted for by the single path** (D-270) · reframe to **ONE capital-validation PATH + spec** (D-265); codification slate (D-271). **Invasive-is-fine for foundation/trust (operator).** Sequencing: deeper sweeps (D-I1/D-I2/D-I3) → reassess B → adversarial pass → code.

### Original open decisions (now resolved; retained for record)
- **D-A `*_mult` scope:** in item-4 / fast-follow / homed-with-binding-closer? (same silent-disable class; cleanest = the per-node RAW override respects clamp_max OR refuses out-of-range).
- **D-B cap shape:** Money-native predicate importing the SSoT constants (orchestrator rec) vs the decided dummy-slot barrier_is_corrupt reuse. Refines D-248.
- **D-C item-3 floors:** confirm `starting_balance>0` + `min_kill_loss>=0` (not >0); confirm dollar set = {starting_balance, min_kill_loss}.
- **D-D hot-reload (Risk-3):** pull the `Async.hpp:335` cfg_compile_ok gate into item-4's commit (~3 lines, warn-keep-old) vs defer to item-5. Standing live hole today (malformed/unknown already detected-and-discarded there).
- **D-E recurrence guard:** ship item-4 with an array-aware non-vacuous test (`node_N_risk_pct=999` MUST fault) + comment-pinned array list NOW (a cohort-count static_assert is Class-51-vacuous against the array channel); the compile-time guard can be item-6.

## Deeper round — D-I1 (complete capital write→store→read map) findings (2026-06-27)
- **N1 (CRITICAL, reinforces single-path):** the optimizer `ConfigField_Set` + `use_config_override` channel SKIPS `ControllerConfig_Load` (BacktestSharded.hpp:117) → a sweep *inside* Load never runs → the gate (:125) reads stale `flags==0`. The validator MUST be a STANDALONE pure fn invoked at EVERY cfg-materialization (Load + the `config_override` branch before :125 + consumed-by-reload), not buried in Load. Reconcile vs item-4-A-3's "bounded/no-promotion" disposition in the adversarial pass: does the optimizer config_override path EVER persist/promote an artifact (D-I1 says "trains against garbage"; A-3 said display-only/no-fingerprint)? Two call sites: Backtest_RunSweep (BacktestEngine.hpp:2381/2383) + WF cell (:2496/2498).
- **N2 (HIGH):** `fee_rate_maker`/`fee_rate_taker` (CfgFieldRegistry.hpp:703/704, `HAS_SIDE_EFFECT` → walker-SKIPPED) parse via the manual inline (CC.hpp:2341-2348) → malformed-fault but NO clamp → `fee_rate_taker=50` (5000%) accepted (every fill unprofitable). UNTAGGED. Legacy `fee_rate` (:500) IS walker-clamped. → OQ2.
- **N3 (MED, refines D-269):** `min_kill_loss` is an AND-clause `Money_Gt(drop, min_kill_loss)` (ControllerEventLoop.hpp:2922); `min_kill_loss > starting_balance` → kill NEVER trips (silent-disable, founding-bug class). D-269 floors only the LOW side (≥0); needs a HIGH-side bound (`< starting_balance`).
- **N4 (LOW/hygiene):** ~24 dead walker-shadowed `CFG_PARSE_PCT/FPN` capital branches (CC.hpp:2261-2492) use `.value` (discards `.flags`) + no clamp = a re-armed founding-bug landmine. Fold the delete into ③ ship-close (or the D-253 bulk-delete). → OQ4.
- **N5 (LOW):** Async.hpp:316 comment claims fee rates are boot-only-preserved on hot-reload, but the code (:325-334) doesn't preserve them → fees DO change on reload (comment drift).
- **OQ1 (SAFETY GAP > item-4; NEEDS VERIFICATION):** `max_exposure_pct` + `kill_switch_daily_loss_pct` have NO sharded reader per D-I1 (legacy PortfolioController only; EngineCommon configures drawdown-only) → INERT capital controls on the PRODUCTION path (Class-12 wired-but-unexercised). Adversarial pass MUST re-grep (OrderGates/ExecutionCore/the sharded tick path) before concluding. If confirmed, bigger than item-4 — its own disposition.
- **OQ5 (scope-definition):** the validation-asymmetry (override skips clamp; only W1 flat-walker clamps) is UNIVERSAL across all RAW overrides, not just capital. 3 tiers: **(a)** capital loss-amplifiers (the stops) ; **(b)** + all stddev-mults incl. entry triggers (`momentum_breakout_mult`:557 [flat-only], `emacross_dip_mult`) ; **(c)** + the ~30 entry-threshold RAW overrides (`momentum_r2_min`/`no_trade_band_mult`/...) whose out-of-range silently DISABLES trading (fail-safe-ish, different from loss-amplify). Cleanest single-path framing: **Layer 1** = enforce descriptor `clamp_MAX` universally post-resolve (closes the asymmetry class; safe — upper-bound only, leaves `0=inherit` alone) ; **Layer 2** = the tighter capital caps on top. Operator scope call.

## Deeper round — D-I2 (option-B blast-radius) findings (2026-06-27)
- **B SPLITS cleanly → RESOLVES the leaf question (D-267):** merge+migrate = **E.1.1** (NO layout change, allocation byte-identical, golden-regen-free-ish); array DELETION + size-pin bump = **WIP2g/E.1.2**. The E.1.1 merge **SUBSUMES the AR-11 legacy-array CRITICAL** — after merge a `nodes[c]`-only sweep is correct (merge at `PopulateCoresFromFlat` runs before the sweep site + in `Default`).
- **SEAM CORRECTION:** the merge seam is `ControllerConfig_PopulateCoresFromFlat` (:1514-1533, where `nodes[c]` is written), NOT `ResolveForCore` (D-267 named it wrong — ResolveForCore only sees `PER_NODE_OVERRIDE_FIELDS`, which the arrays are NOT in → building B there silently does nothing).
- **Merge rule (byte-safe) = RAW COPY, 0=inherit PRESERVED + consumers keep the two-branch math.** CANNOT collapse to one formula: FP non-associativity (`total*(risk/num) ≠ (total*risk)/num`) + `Money` 8dp requant → byte break. The divided-vs-direct semantics is intrinsic to the allocation, stays in the consumer.
- **Consumer set = 4 array readers** (Run.hpp:923, Async.hpp:354, BacktestSharded.hpp:273, ControllerEventLoop.hpp:2918). `nodes[c].risk_pct/.max_drawdown_pct` read by NOBODY today (dead mirror). PortfolioController reads = TEST-ONLY (zero prod callers).
- **Determinism:** golden SHIFTS (free — no live models, no unit test pins a fingerprint hex) but allocation byte-identical. Size-pin (53056) does NOT move for merge-only (only at deletion). Persist/wire/HMAC NOT touched.
- **Infra:** extend the H18 sidecar (`FOREACH_PER_NODE_NO_FLAT_FIELD_SYNC` shape) → a `FOREACH_PER_NODE_ARRAY_OVERRIDE` table, run AFTER `EMIT_PER_NODE_COPY` (last-wins over the walker double-write). Option C (fold arrays into `PER_NODE_OVERRIDE_FIELDS`) REJECTED — ResolveForCore would destroy the 0=inherit sentinel → breaks the divided-default allocation.
- **Sibling cohort (NOT capital → E.1.2):** `node_strategies` (dual-source-split-reader ALREADY live — 5 direct readers despite syncing to `nodes[c].strategy`) + `node_time_exit_ticks`. Generalize the merge mechanism so they follow trivially; do NOT bundle (keeps E.1.1 capital-scoped + bisectable).

## Deeper round — D-I3 (hot-swap capital surfaces) findings (2026-06-27)
- **Strategy swap is covered BY LOAD-COMPLETENESS — no swap-time hook needed.** Two runtime swap paths exist (regime-AUTO per-cycle, ControllerEventLoop.hpp:2437/2478; GUI explicit, Run.hpp:1742), but both only RE-SELECT among already-resolved fields. Validating the WHOLE per-node surface at load (incl. `*_mult`, D-268) makes every swap capital-safe. Can't scope to "active strategy" — a regime transition reads other strategies' fields (the ratchet reads `momentum_tp_mult` for a non-Momentum node).
- **Model reload is WELL-COVERED** — barriers guarded at the load seam by `barrier_is_corrupt` (D-221, boot + hot-swap). Forward action = NAME it in the D-271 spec as the 2nd ingress sharing the SANE constants. No new code.
- **GAP-2 (HIGH, GUI builds, NEW, not homed):** GUI runtime strategy-swap (Run.hpp:1742) reassigns `strategy_id` WITHOUT re-allocating the typed `strategy_state` buffer → type-confusion + OOB read (SimpleDip→Momentum: `Momentum_BuildParameters` reads a hundreds-of-bytes `MomentumState` out of a ~24B `SimpleDipState` → garbage entry price + position size). `StrategyLifecycle.hpp:415-417` explicitly anticipates it. NOT a cfg-validation bug (cfg is fine; the typed state buffer is wrong) → NOT item-4; home to a NAMED E.1.3+ seam. Fix = pre-sized per-node strategy-state union OR arena-aware re-init (not a one-liner). Reachability VERIFY owed (adversarial).
- **GAP-3 (LOW):** regime-ratchet reads FLAT `momentum_tp_mult` not `nodes[slot]` (H22-adjacent, bounded) → H22 ledger note.
- **H22 PASS** — all swap surfaces per-node-pure.

## Adversarial pass (A-B / A-SAFETY / A-COMPLETE) — 2026-06-27
**A-B (refute B byte-identity): B CONFIRMED sound** — all 5 D-I2 claims hold vs code, conditional on 3 invariants (raw-copy/0-sentinel; sidecar STRICTLY after `EMIT_PER_NODE_COPY`; atomic 4-consumers+sidecar + non-vacuous override test). Caught the D-267 wrong-seam (→ reconciled in the log). Fingerprint shift free but SILENT → document in ship notes. Optimizer `config_override` byte-identical anyway (mutates only global flat). Full detail: D-267 STATUS.

**A-SAFETY (refute OQ1 / GAP-2 / N1):**
- **OQ1 CONFIRMED — the headline; a live-readiness REQUIREMENT bigger than item-4.** `max_exposure_pct` + `kill_switch_daily_loss_pct` (the latter `SAFETY_CRITICAL`) are read ONLY inside `PortfolioController_Tick` (PortfolioController.hpp:915), which has ZERO call sites (dead; `main.cpp:213-222` — `EngineSharded_Run` is the sole path). NO sister coverage on the sharded path: the kill is DRAWDOWN-ONLY (`EngineCommon.hpp:191-193` configures `ks_min_balance=0` + drawdown; `ks_peak_balance` is LIFETIME peak-to-trough, no daily/session reset → daily-loss genuinely absent) and there is NO global exposure cap (only per-node budget via `node_risk_pct`). Operator sets both in GUI + sees them on TUI → believes armed; neither fires. **item-4 capping them (its sweep auto-includes rows 543/544) would lend false credibility to dead controls (Class-12 amplification).** DISPOSITION OWED (operator): WIRE the sharded readers (session-relative daily-loss kill + global exposure cap — table-stakes for live; the exposure cap relates to the cross-node Σ already homed E.5/TD-211) OR TOMBSTONE the 2 fields (H21). NOT an active danger (live is gated off by the Phase-D blanket gate) but a hard must-fix BEFORE the live-gate lifts. NEW sister: balance-FLOOR kill also permanently disabled on sharded (`EngineCommon.hpp:192` hardcodes `Money_Zero` floor); no cfg maps to it (no false-assurance) → kill-switch-completeness ledger.
- **GAP-2 CONFIRMED — reachable + genuine OOB read (E.1.3+, NOT item-4).** GUI concrete→concrete swap reachable (SettingsPanel.hpp:1290-1304; flat-only Run.hpp:1733); mutates `strategy_id` only; `Strategy_BuildParameters` blind-casts with NO kind-guard (StrategyParameters.hpp:1666/1709-1712) UNLIKE the kind-guarded Adapt/ExitAdjust. Sizes: `SimpleDipState=32B` / `MomentumState=704B`, first deref `live_breakout_mult` at offset 416 → **384B overread**. Read-only (Adapt is kind-guarded → no arena corruption); `USE_IMGUI_GUI`-build only. Fix = pre-sized per-node state union OR arena-aware re-init → named E.1.3+ seam. TECH_DEBT owed.
- **N1 BOUNDED — A-3's triage holds; D-I1's "trains against garbage" REFUTED.** No promotion-to-live: `config_override` only in the optimizer sweep w/ `collect_features=0` (no artifact); Save Run writes a verbatim copy of the ORIGINAL `backtest.cfg` + a comparison-only `expected.cfg` (never loaded live); live boot reads operator `engine.cfg` @ main.cpp:203. Actionable (matches synthesis N1): the validator MUST be a standalone pure predicate invoked BY `cfg_capital_gate_ok` (so all 3 callers — live/optimizer-base/materialized — get it); sweep-inside-Load alone misses `config_override`. Placement item, not a live hole.

**A-COMPLETE (refute single-path completeness):**
- **CONFIRMED SOUND:** `nodes[c]` DOES reflect per-node overrides (`PopulateCoresFromFlat` ← `ResolveForCore` applies `node_overrides[c]`) → a `nodes[c]` sweep sees overrides; the override channel does NOT escape. The biggest structural worry is CLOSED.
- **Claim-1 (completeness) PARTIAL-REFUTED — 4th capital source = FEES.** `fee_rate_maker/taker` are `nodes[c]` capital fields tagged `STAMP_BOUND_CFG_DERIVED|HAS_SIDE_EFFECT`, NOT `CAPITAL_BOUND` → the Set-A enumeration SKIPS them. Charged every fill (Order.hpp:361, OrderManager.hpp:1522) + feed the trailing-ratchet SL cap (StrategyLifecycle.hpp:275, `sl_floor = entry×(1−3×fee_rate_taker)`); parsed NO-clamp. MED-HIGH: gross poison is buy-block-backstopped (`tp_pct < 3×fee → BUY_BLOCKED`) but a MODERATE poison (0.5% vs 0.1% intended) slips → silent ~5× fee bleed; descriptor `clamp_max=5.0` (=500%) too loose → needs a TIGHT explicit cap. (slippage_pct + partial_exit_pct same class, lower blast.)
- **Claim-2 (universal Layer-1 clamp-max) PARTIAL-REFUTED — THE design correction.** "Universal" is un-implementable + vacuous: (1) 94 `WARN_ON_CLAMP` fields are designed to CLAMP+WARN, not fault → a fault-based universal pass = false-refuse regression (Class-21/Knight boot-cascade); a clamp-based universal pass = silent-clamp (the founding-bug class). Can't be both → needs PER-FIELD disposition. (2) the percent capital fields have `clamp_max=100.0` = 10000% in fraction space → Layer-1 does NOTHING for them (`risk_pct=9000%` passes); the explicit capital caps are load-bearing, not "on top." (3) non-registry manual-parse fields (`vol_mult_min/max`) have no descriptor → not "ALL."
- **Claim-3 (*_mult ≤10) PARTIAL-REFUTED (a Class-33 in MY synthesis).** ≤10 holds for the stddev STOP mults, but `fee_floor_mult`(≤20)/`spacing_multiplier`(≤20)/`volume_multiplier`(≤100) are non-conformers → a uniform-10 cap FALSE-REFUSES legit configs. Cap MUST be per-descriptor `clamp_max`, never uniform-10. (The synthesis's "the *_mult set ≤10" asserted a property over a set without enumerating = Class-33; caught by the adversary — AR-8 model-bounded-self-check.) The 0-sentinel rationale is field-imprecise (`sl_mult=0` → SL-at-entry not pct-mode; `fee_floor_mult=0` disables the fee-floor guard) but the pass-0 conclusion holds (fail-safe). Document `0` per-field.
- **Claim-4 (no swap-time hook) HOLDS-UP** — both swap paths re-select already-resolved fields; GAP-2 correctly out (state-buffer bug, not cfg).
- **Framing note:** D6/D10 runtime-MUTATE `resolved nodes[slot].entry_offset_pct/volume_multiplier` (bounds themselves unvalidated) + `live_*` heap strategy state = a 3rd storage neither `nodes[c]` nor barrier; ALL entry filters → fail-safe (no capital blast) but the spec's "validates every value a consumer reads" is literally false for them — document the load→adapt bound-chain.
- **OQ1 INDEPENDENTLY CONFIRMED** (2nd agent).

**The corrected design (A-COMPLETE's simpler/safer shape — reuses the framework, no new infra):** ONE validator dispatching on a per-field DISPOSITION:
- **FAULT-on-out-of-range** = the capital / loss-amplifier set (CAPITAL_BOUND rows + the stop-distance mults + **fees + slippage + partial_exit + dollar floors**), each with an EXPLICIT SANE cap (barrier ≤100%/≤1000% for tp/sl; fee ≤~1%; NOT the vacuous descriptor `clamp_max`) — a "tag the row" move (add `CAPITAL_BOUND`/a fee bit to 703/704/675).
- **CLAMP+WARN** (existing `WARN_ON_CLAMP`) = the tolerant set, left as-is, never converted to fault.
- standalone predicate invoked BY `cfg_capital_gate_ok` (N1: covers live + optimizer-base + `config_override`).

## CONSOLIDATED VERDICT (2026-06-27): GREEN to build — after the A-COMPLETE reshape
Direction CONFIRMED (single-path + Money-native validator + B-merge). Shape CORRECTED (per-field disposition, explicit caps, fees folded, per-descriptor mult cap) → D-272. Two orthogonal findings bigger than item-4 homed: OQ1 (D-273, operator disposition owed) + GAP-2 (D-274, E.1.3+ seam). Pre-code gate remaining: `/accounting-audit` on the explicit cap thresholds (fee ≤?, the sane caps) + the OQ1 disposition. Then code. NO round 4 — this is the convergence point.

## Accounting-gate (pre-code /accounting-audit consult) — 2026-06-27: CLEAR to code, 2 corrections folded
- **No accounting BLOCKER. CLEAR to code item-4 + B.** No mis-tag among the 15 CAPITAL_BOUND rows; the FAULT additions (fees/slippage/partial_exit) all accounting-correct.
- **FEE (the open slot) — RESOLVED shape:** NO venue fee source exists today (fees operator-set; the venue commission datum is demoted to audit-only, BinanceUserData.hpp:359-360; /api/v3/account returns the tier but isn't read for fees, BinanceOrderAPI.hpp:776/835; precision DOES defer-to-source via exchangeInfo, BinanceOrderAPI.hpp:59/77 — fees are the one venue quantity that doesn't). Disposition = FAULT, **GLOBAL scope** (check `cfg.fee_rate`/`fee_rate_maker`/`fee_rate_taker` directly — fees are global, mirrored to nodes[c] only; ordering-robust). Cap = a NAMED venue-published-max constant `BINANCE_SPOT_FEE_SANE_MAX` (site with BARRIER_SANE_MAX_*), recommended **0.002 (0.2% = 2× Binance spot published taker max 0.1%)** — **OVERRIDES the decision-log "≤~1%" (too loose: the 0.5% moderate-poison the fold targets is < 1% → a 1% cap passes it).** 0.2% catches garbage + the 0.5% poison + allows a conservative operator over-model + false-refuses ZERO real configs. Stored fraction (no /100); applied pre-BNB-discount. **Venue-wiring seam:** the true defer-to-source (extract /api/v3/account makerCommission/takerCommission, mirroring the exchangeInfo precision precedent) homes to E.1.6 multi-exchange — item-4's bound is the interim bridge.
- **min_kill_loss high-side (N3) — RESOLVED:** require `min_kill_loss < starting_balance` (FAULT if ≥; `==` also disables, Money_Gt strict) as a CROSS-FIELD post-resolve assertion (NOT per-field — needs starting_balance resolved), same scope as the dollar floors. NEW (MED): `< starting_balance` is necessary-NOT-sufficient — `drop` is per-node, allocation-bounded (a fraction of starting_balance), so the fully-correct bound is `< per-node-allocation`; home the allocation-aware tightening to E.5/TD-211 (same resolved-allocation computation as the cross-node Σ).
- **partial_exit_pct cap = ≤1.0 (fraction-of-position), NOT the GAIN ≤1000%** (semantically distinct; per-node override unclamped, ControllerConfig.hpp:2958; 0=no-partial passes).
- **starting_balance>0 is a SHARDED-path requirement (strengthens item-3):** starting_balance=0 → node_peak_balance inits to alloc=0 → Money_Gt(peak,0) false → dd never computed → the SHARDED per-core kill never trips (ControllerEventLoop.hpp:2898) — not just the legacy path.
- **D-273 cross-ref:** item-4 caps 543/544 (max_exposure_pct / kill_switch_daily_loss_pct) with correct bounds, but they're DEAD on the sharded path → ship notes must NOT read "validated" as "armed." (kill_switch_drawdown_pct:547 IS wired — not in the dead set.)
- **B-merge accounting: byte-identical CONFIRMED + backtest↔live parity preserved** (node_risk_pct feeds allocated_balance→kill only, not fee/P&L/balance; max_drawdown_pct kill-threshold pure-Money raw-copy). Pre-existing ledger note (not B/item-4): allocation computes in double then requants to Money (the FP non-assoc is WHY the two-branch can't collapse).

## Homed / ledger (not item-4 blockers)
- `slippage_pct` (:505) untagged capital-cost field — /accounting-audit "intentionally out?" (out-of-range is fail-safe).
- Degenerate-0 on BASE `take_profit_pct`/`stop_loss_pct` (TP=SL=entry; not caught by a range cap) — separate >0/degeneracy check or homed.
- EngineTUI.hpp:414 reads Money `max_drawdown_pct` via FPN_ToDouble (binary scale) → garbage display (Class-41, display-only, pre-existing) → ledger row.
- Cross-node Σ over-allocation (16×100%=1600%) — already homed to cluster/E.5 (D-253-D3, TECH_DEBT-211).
