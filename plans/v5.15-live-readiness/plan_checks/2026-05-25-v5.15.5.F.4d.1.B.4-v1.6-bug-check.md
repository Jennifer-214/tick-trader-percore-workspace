---
type: audit-report
audit: /bug-check
target: plans/v5.15-live-readiness/subplans/2026-05-24-v5.15.5.F.4d.1.B.4-train-serve-execution-layer-parity.md
target_version: v1.6
established: 2026-05-25
scope: plan body only (not codebase-wide)
verdict: YELLOW (3 NEW Class 14 instances; 0 NEW instances of other classes; all 5 targeted closures structurally intact)
codebase_head: 09204b3 (v5.15.5.F.4d.1.B.4 WIP-checkpoint 1: EngineCommon.hpp skeleton + B.0 ApplyBnbDiscount + B.1 BootGlobal bodies)
---

# /bug-check report — v5.15.5.F.4d.1.B.4 v1.6 plan body

## Summary

| Verdict | Count |
|---|---|
| CLEAN (no Class N instances surfaced) | 27 |
| KNOWN-only (instances are the closure targets) | 4 (Classes 18, 21, 24, 28) |
| **NEW instances** (require triage) | **1 class with 3 site instances (Class 14)** |
| N/A (audited-clean / out-of-surface) | 0 |
| Doc-debt findings | 0 |

**Targeted closures verified:** Class 18, Class 21, Class 24 (cohorts), Class 27 (verified not introduced), Class 28 (mitigation correct per H20 framing). Classes 30+ (bytewise-identical math discipline at extract) recommendation = SISTER (not distinct class).

**NEW finding requiring operator triage: 3 Class 14 instances (fabricated cfg field references — `breakeven_on_profit_threshold` per-core field cited 8x + `execution_core_count` cited 2x).** Same shape as v1.5 D1 (OrderManager_RegisterCore → EventLoopState_RegisterCore drift). The v1.5-codified `feedback_verify_symbol_existence_at_plan_drafting_time` discipline should have caught these at v1.6 drafting — discipline not yet applied to v1.6's NEW cited symbols.

---

## Per-class verdicts

### Class 1 — Strategy lifecycle orphans
- **Surface:** live (slow-path strategy dispatch)
- **Verdict:** CLEAN
- **Notes:** Plan body's `tt::Strategy_InitPerCore(&state, c, state.cores[c].strategy_id, ...)` in BootPerCore body content (line 538) closes Class 1-related orphan (PARITY-029 was: backtest didn't call Strategy_InitPerCore; pre-v5.4 F7 bug). The structural extract ENSURES both sides call lifecycle init by-construction.

### Class 2 — Display ↔ execution divergence
- **Surface:** gui + live
- **Verdict:** CLEAN
- **Notes:** Plan touches boot + slow-path-cycle body only; no GUI display reads/writes proposed.

### Class 3 — Drain count under partials
- **Surface:** drainer
- **Verdict:** CLEAN
- **Notes:** Plan does not touch OMS drainer / OMS_DrainSubmit / OrderManager_Tick.

### Class 4 — Snapshot save/load asymmetry
- **Surface:** boot
- **Verdict:** CLEAN
- **Notes:** Plan body does NOT propose changes to snapshot save/load shape. Helper extraction preserves caller-side init ordering per Decision G (statics stay in caller).

### Class 5 — Reset Paper completeness
- **Surface:** boot
- **Verdict:** CLEAN
- **Notes:** Plan doesn't touch Reset Paper path; helper extraction is orthogonal.

### Class 6 — OMS counter persistence
- **Surface:** boot
- **Verdict:** CLEAN
- **Notes:** OMS counter persistence orthogonal; oms.ezoo_refs/core_cfg_refs wire (post-helper) preserves current write pattern at EngineSharded.hpp:1078 verbatim.

### Class 7 — Threading topology violations
- **Surface:** audited-clean — N/A
- **Verdict:** N/A (delegated skip)

### Class 8 — User-configurable features silently inactive
- **Surface:** live (cfg → runtime consumption)
- **Verdict:** CLEAN (plan IS the structural closure path)
- **Notes:** Plan body closes PARITY-027/028/029/030/031/032 (cfg flag → execution consumption gaps) via structural extract. This ship is the Class 8 sister closure mechanism at the train↔serve mirror surface.

### Class 9 — Shutdown blocking on unwanted operations
- **Surface:** boot (shutdown ordering)
- **Verdict:** CLEAN
- **Notes:** Plan doesn't propose shutdown ordering changes. Helper extraction is boot-init only.

### Class 10 — Strategy-regime mismatch
- **Surface:** live (regime → strategy coupling)
- **Verdict:** CLEAN
- **Notes:** Regime classification stays in SlowPathCycleOneCore per Decision F PARITY-031 closure (named constant + comment block); no coupling break introduced.

### Class 11 — Extensibility friction silent drift
- **Surface:** live (multi-site addition pattern)
- **Verdict:** CLEAN
- **Notes:** Plan body REDUCES multi-site addition pressure by extracting shared helpers (5 in EngineCommon.hpp). Future ML capability adds flow through ONE helper, not two parallel sites.

### Class 12 — Wired-but-unexercised ML paths
- **Surface:** ml
- **Verdict:** CLEAN (plan body fold-in actively exercises ML paths)
- **Notes:** PARITY-027 closure (use_exit_model fold-in to BootPerCore body) + PARITY-028 closure (ConfidenceScorer_BindCompositeCfg + RollingTurnover_Init fold-in) actively wire ML capability into BOTH train + serve. ezoo zoo allocation explicit per arch (LIVE: aligned_alloc; BACKTEST: static + Free+Init prior-run state). No "wired but never called" Class 12 shape introduced.

### Class 13 — Worker-thread snap-capture drift
- **Surface:** training / GUI worker threads
- **Verdict:** CLEAN
- **Notes:** Plan body doesn't touch worker thread snap-capture surface (foxml_suite trainer / GUI workers untouched).

### Class 14 — Plan calls a function or struct field that doesn't exist
- **Surface:** plan-time
- **Verdict:** **NEW-3 (3 fabricated-symbol cites in v1.6 amendments)**
- **Severity:** HIGH (same shape as v1.5 D1 fabricated symbol catch)
- **Hits:**

| # | Symbol cited | Sites in plan body | Actual API | Severity |
|---|---|---|---|---|
| 14a | `cfg.cores[c].breakeven_on_profit_threshold` (PER-CORE FPN field) | Lines 17, 41, 50, 154, 370, 555-558, 775 (8 sites in v1.6 O3 mitigation + cross-refs) | Field does NOT exist. Actual gating: `BITMAP_IS_SET(cfg.lifecycle_cfg_flags, MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT)` (GLOBAL bitmap flag at ControllerEventLoop.hpp:3799). Verified: `rg "breakeven_on_profit_threshold"` returns empty across codebase. | HIGH — invalidates v1.6 O3 PARITY-032 branchless-gating sketch as written |
| 14b | `cfg.execution_core_count` | Line 492 (ApplyBnbDiscount body sample) + Line 572 (SlowPathCycleAllCores wrapper sample) | Field does NOT exist. Actual field: `cfg.num_execution_cores` (verified at ControllerConfig.hpp:1847-1848 + LiveReadiness.hpp:104-139). Sister B.0 already shipped at EngineCommon.hpp:143 correctly uses `MAX_EXECUTION_CORES` (not execution_core_count). | HIGH — code samples in plan body would not compile if copied verbatim |
| 14c | `OrderManager_RegisterCore` mentioned at EngineCommon.hpp line 191 (SKELETON FILE in HEAD) | EngineCommon.hpp:191 (description comment) | Actual: `EventLoopState_RegisterCore` (verified at ControllerEventLoop.hpp:1053; per v1.5 D1 enumeration drift catch). Skeleton-side carryover of v1.5 D1 fix not applied. | MED — comment-only at HEAD; visible drift |

- **Triage notes:**
  - 14a is the most critical — v1.6 O3 mitigation depends on the field EXISTING. Options to triage:
    - (i) Use the GLOBAL gate `MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT` for the branchless gate (already in `EventLoop_BreakevenOnProfit` wrapper at ControllerEventLoop.hpp:3799; pre-existing `BITMAP_IS_SET` is already efficient branchless). The whole O3 mitigation premise ("threshold=0 cohort pays ~5ns; threshold>0 cohort pays full ~50-200ns") may be MOOT because the existing wrapper already gates via lifecycle bitmap.
    - (ii) Add the per-core FPN field `cfg.cores[c].breakeven_on_profit_threshold` as NEW cfg field in this ship (1 row in FOREACH_CFG_FIELD; framework-auto-flow) — but introduces NEW scope creep + Class 24 surface check.
    - (iii) Reframe O3 mitigation to operate at the existing wrapper boundary (early-out on global bitmap is ALREADY branchless-equivalent).
  - 14b is a 2-site mechanical rename (`cfg.execution_core_count` → `cfg.num_execution_cores`) in plan body code samples. Plan amendment.
  - 14c is HEAD-side comment-only; commit a Cleanup to EngineCommon.hpp:191 ("OrderManager_RegisterCore" → "EventLoopState_RegisterCore").
- **Discipline reflection:** v1.5 codified `feedback_verify_symbol_existence_at_plan_drafting_time` AFTER catching D1 (OrderManager_RegisterCore fabrication). v1.6 amendments introduced 3 NEW fabricated symbols WITHOUT applying that discipline. The discipline applies to ANY new symbol cited in an amendment, not just amendments to existing citations. Recommendation: discipline application MUST trigger at every amendment cycle for ANY newly-cited symbol, not just at v1.0 plan body draft time. Worth amending `feedback_verify_symbol_existence_at_plan_drafting_time` memory file to clarify "every amendment cycle" coverage.

### Class 15 — Function signature drift
- **Surface:** plan-time
- **Verdict:** CLEAN
- **Notes:** All cited helper signatures (`EngineCommon_ApplyBnbDiscount(cfg&)`, `EngineCommon_BootGlobal(cfg&, state&, oms&)`, etc.) match HEAD skeleton at EngineCommon.hpp:140-247. v1.6 O1 amendment to BootPerCore 8 args is INTENDED — explicit signature redesign, not drift. Plan body Decision H (line 328-343) explicitly documents the refinement.

### Class 16 — Naming convention drift breaks X-macro dispatcher
- **Surface:** plan-time
- **Verdict:** CLEAN
- **Notes:** Plan body doesn't propose new X-macro registry. EngineCommon helpers are direct fn calls; no FOREACH_* added.

### Class 17 — Architectural deferral without adjacent struct grep
- **Surface:** plan-time
- **Verdict:** CLEAN
- **Notes:** Plan body's adjacent struct context already comprehensive at Decision F (regime_state consumer enumeration completed at Step C.4.5 per v1.4 N5 + v1.5 D-codified discipline).

### Class 18 — Mirror plans missing data-flow dependencies (STRENGTHENED call-sequence)
- **Surface:** plan-time (closure target)
- **Verdict:** **KNOWN-CLOSURE-TARGET** (this ship structurally closes the 7-PARITY cohort by extracting 5 shared helpers)
- **Closure verification:**
  - The 5-helper extract REPLACES the parallel call sequences in EngineSharded.hpp + BacktestSharded.hpp with shared bodies. Future Class 18 instances at the train-serve mirror surface die structurally (both callers MUST call same helper; compile-time enforced).
  - Strengthened call-sequence enumeration (per Class 18 STRENGTHENED amendment): Phase A Step A.4 produces authoritative 71-row CSV at `plan_checks/2026-05-25-B4-boot-call-sequence-enumeration.csv`. Per-row classification verified.
  - Does v1.6 preserve mirror state elsewhere? NO. v1.6 O1 dropping `oms` arg from BootPerCore + O2 caller-precompute core_balance reduce helper-side state. Helper body owns boot work; caller owns statics + post-helper wires. Mirror state DOES NOT migrate to helper API.
- **Recommendation:** Closure verified. No NEW Class 18 instance introduced.

### Class 19 — Hardcoded instance names in applicability gating
- **Surface:** live + slow-path + GUI
- **Verdict:** CLEAN
- **Notes:** Plan body's `if (cfg.core_strategies[c] == STRATEGY_ML)` branches at caller sites (plan body Step C.1 line 608 + C.2 line 669) are arch-specific ML zoo allocation, not capability gating. Categorical pattern already covered at CfgFieldRegistry.hpp via STRAT_CAT_* metadata; arch-specific zoo allocation has no categorical alternative (literal type discrimination required for aligned_alloc vs static array choice). N/A surface for categorical conversion.

### Class 20 — Bitmap field without overflow guard
- **Surface:** registry + bitmap pairs
- **Verdict:** CLEAN
- **Notes:** Plan body doesn't propose new bitmap fields. Existing `lifecycle_cfg_flags` bitmap (8-bit) is enrolled in `FOREACH_LIFECYCLE_CFG_FLAG` per MetaRegistry.hpp:46 + has BITMAP_IS_SET accessors verified.

### Class 21 — Multiple parallel descriptors (cross-file drift)
- **Surface:** cfg + descriptor surfaces (closure target)
- **Verdict:** **KNOWN-CLOSURE-TARGET** (5-helper shared API replaces parallel boot/slow-path-cycle sequences)
- **Closure verification:**
  - v1.6 O1 explicitly DROPS unused `oms` arg from BootPerCore to AVOID weak-uniformity surface (sister-canonical minimal-args pattern). This is the right call.
  - 5-helper API uniformity verified at plan body Decision H (line 336-341):
    - ApplyBnbDiscount(cfg&) — 1 non-null ref
    - BootGlobal(cfg& + state& + oms&) — 3 non-null refs
    - BootPerCore(cfg& + c + state& + tick_ring& + core& + zoo* + ezoo* + FPN) — 5 non-null refs + 2 nullable ptrs + 1 POD
    - SlowPathCycleOneCore(cfg& + c + state& + oms& + price + ts_us) — 3 non-null refs + 3 PODs
    - SlowPathCycleAllCores(cfg& + state& + oms& + price + ts_us) — 3 non-null refs + 2 PODs
  - Decision H rule: `&` for non-nullable; `*` for explicit-nullable; ApplyBnbDiscount only non-const-cfg helper (documented). NO weak-uniformity (e.g., empty struct sentinel for non-applicable subsystem) introduced.
  - All 5 helpers maintain CONSISTENT first-3-args shape (cfg first; then either state-only or per-core context) — uniformity preserved at the per-arg-position level.
- **Other weak-uniformity surfaces?** Audit confirms NONE survive in v1.6 5-helper API.
- **Recommendation:** Closure verified. No NEW Class 21 instance introduced.

### Class 22 — Runtime cfg gating scattered
- **Surface:** cfg gating consumers
- **Verdict:** CLEAN
- **Notes:** Plan doesn't propose new scattered cfg gating predicates. Existing `requires_cfg` discipline orthogonal.

### Class 23 — Type-erased typed-field write via reinterpret_cast through char* offset
- **Surface:** registry-driven typed-field access
- **Verdict:** CLEAN
- **Specific check (per focus area):** Is the LIVE `oms.ezoo_refs[c] = (void*)ezoo_ptr` cast (caller-side post-helper wire at plan body Step C.1 line 633) a Class 23 instance? **NO.** Reasons:
  1. Class 23 specifically targets `*reinterpret_cast<T*>((char*)dst + offset) = v` — punning a typed write through char* offset arithmetic with implicit-type assumption from Kind enum.
  2. The pattern `oms.ezoo_refs[c] = (void*)ezoo_ptr` is a direct C-style cast from typed pointer (`EnsembleModelZoo<F>*`) to `void*` for storage in an EXPLICITLY-DECLARED `void*[]` array.
  3. The retrieval is via `static_cast<EnsembleModelZoo<F>*>(oms->ezoo_refs[pslot])` at OrderManager.hpp:707 (typed-back-cast; no offset arithmetic).
  4. The array `ezoo_refs[MAX_EXECUTION_CORES]` is EXPLICITLY-TYPED `void*[]` at OrderManager.hpp:624 — no Kind enum decoupling type from field; no implicit-type assumption.
  5. The pattern is the EXISTING SHIPPED code at EngineSharded.hpp:1078 (since v5.15.5.F.4d Step 7 § F) — verified safe + tested. Plan body preserves it verbatim.
- **Recommendation:** No NEW Class 23 instance. Existing pattern preserved correctly.

### Class 24 — Capability-cfg surface mismatch (3-cohort closure target)
- **Surface:** ml ↔ cfg surface (closure target)
- **Verdict:** **KNOWN-CLOSURE-TARGET** (plan closes 4 capability cohorts per v1.6 plan body Decision A)
- **Closure verification per cohort:**
  - **use_exit_model:** Plan body Step B.2 line 526 + Step C.1/C.2 both call `EngineCommon_BootPerCore` which includes ML branch with model load + `state.cores[c].ensemble_handle` wire. Both train + serve call same helper → capability ENABLED on both sides by-construction. CLOSURE STRUCTURALLY COMPLETE.
  - **confidence_composite_enabled (ConfidenceScorer_BindCompositeCfg):** Plan body Step B.2 line 536 explicitly NEW for BACKTEST: "ConfidenceScorer_BindCompositeCfg(...)" in BootPerCore body. Both sides call same helper. CLOSURE STRUCTURALLY COMPLETE.
  - **pay_fees_in_bnb (ApplyBnbDiscount):** Plan body Step B.0 line 490-494 — both train + serve call `EngineCommon_ApplyBnbDiscount(cfg)` once. PARITY-030 closure. CLOSURE STRUCTURALLY COMPLETE.
  - **breakeven_on_profit (PARITY-032):** **PARTIAL VERIFICATION CONCERN.** Plan body cites `cfg.cores[c].breakeven_on_profit_threshold` (per-core FPN) as the operator-tunable that ENABLES per-core breakeven — but **this field does not exist** (see Class 14 finding 14a above). Actual gate is `MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT` (global bitmap). The cohort is CORRECTLY identified (per_core_slow arch missing breakeven fold-in IS a real PARITY drift); but the v1.6 O3 mitigation premise — "operator can configure per-core threshold" — is fictitious. The actual closure mechanism (fold EventLoop_BreakevenOnProfit into SlowPathCycleOneCore) is structurally sound; only the per-core-threshold gating sketch is impacted.
  - **Recommendation:** Plan amendment to either (a) clarify breakeven is GLOBAL bitmap gated (drop per-core threshold framing), OR (b) add the per-core threshold field as NEW cfg field this ship (scope creep + new Class 24 surface check).
- **Other closures (no remaining cfg flag enables capability without corresponding fn call):** Yes, plan body Decision F + Step C.4.5 explicitly handle PARITY-031 regime-state per-core via named constant `BACKTEST_REGIME_SAMPLE_CORE`. Comprehensive consumer enumeration completed at v1.4 N5 + v1.5 D-codification.

### Class 25 — Scope-erosion in per-core consumer function
- **Surface:** per-core consumer execution
- **Verdict:** CLEAN
- **Notes:** Plan body's BootPerCore takes `const ControllerConfig<F>& cfg` (full cfg) but the body content explicitly indexes `cfg.cores[c].*` per per-core access pattern (e.g., line 536 `cfg.cores[c].confidence_window`). Helper is INDEX-EXPLICIT throughout (c arg present in every per-core access). Not a per-core consumer that takes full-cfg and reads global fields lazily — it's an explicit per-core orchestrator that requires both cfg context (for global cohort fields like `cfg.pay_fees_in_bnb`) and per-core slice (`cfg.cores[c].*`). Decision E (helper signature stability) preserves this discipline. SlowPathCycleOneCore similarly explicit-indexed.
- **Class 25 follow-up:** Per `class-25` § "two-param convenience sig FORBIDDEN" — does BootPerCore violate this? **NO**, because the function is NOT a per-core consumer (those take ONLY `const PerCoreCfg<F>*`). BootPerCore is an ORCHESTRATOR that needs both. Per CLAUDE.local.md Class 25 surface: "genuine globals caller-resolved as scalar args" — orchestrators legitimately need full cfg.

### Class 26 — Global consumer reading per-core field
- **Surface:** cfg-flow + slow-path + hot-path + oms-drainer
- **Verdict:** CLEAN
- **Notes:** Plan body indexes `cfg.cores[c].*` explicitly throughout helper body content; no global-consumer-reading-per-core anti-pattern introduced.

### Class 27 — Single-value cache flattens per-instance (FOCUS-AREA verification)
- **Surface:** subsystem state caches
- **Verdict:** CLEAN (verified per focus area)
- **Focus verification:**
  - Does BootPerCore helper stash cfg snapshot in any subsystem state? **NO.** Verified by reading EngineCommon.hpp BootPerCore declaration (line 202-206) and v1.6 plan body Phase B Step B.2 body content sketch (lines 522-540):
    - All cfg reads inside helper body are `cfg.cores[c].*` direct accesses (no cache field on EventLoopState / OrderManagerState / ExecutionCore introduced).
    - Per-instance data flows from caller per `decision-time-data-binding-pattern.md`: ML zoo allocation done by CALLER (not helper); `core_balance` precomputed by CALLER per v1.6 O2; tick_ring + core passed by ref per Decision H.
  - Caller-side caches? Plan body Step C.1 line 588-593 (LIVE precompute) + C.2 line 649-654 (BACKTEST precompute) use FUNCTION-SCOPE `double` locals (`total_balance`, `default_per_core`, `core_balance`) — these are NOT scalar cfg-mirror cache fields on subsystem state. Function-scope locals at boot are legitimate (one-shot compute; not cached state across cycles).
  - Note: Plan body O2 explicitly addresses the bytewise-identical math discipline. The "caller-precompute over helper-recompute" choice MITIGATES Class 27 risk by keeping cfg-derived computation at one site (caller scope; not persisting in subsystem state).
- **Recommendation:** Class 27 anti-pattern not introduced. v1.6 O2 discipline (caller-precompute bytewise-identical math) is structurally aligned with `decision-time-data-binding-pattern.md`.

### Class 28 — Branchy SP/HP dispatch when branchless feasible (CRITICAL focus)
- **Surface:** SP/HP/drainer/producer dispatch
- **Verdict:** KNOWN-CLOSURE-TARGET-WITH-SKETCH-CAVEAT
- **CRITICAL Focus per v1.6 O3 — Is the proposed gating ACTUALLY branchless?**
  - **Plan body sketch (line 557):**
    ```cpp
    uint64_t bo_mask = -(uint64_t)(!FPN_IsZero(cfg.cores[c].breakeven_on_profit_threshold) ? 1 : 0);
    if (bo_mask) EventLoop_BreakevenOnProfit(...);
    ```
  - **Analysis:** The MASK computation uses `-(uint64_t)(...)` style (per BITMAP_* pattern, branchless ✓). BUT the `if (bo_mask)` is STILL A BRANCH on the resulting mask value. This is NOT cmov-friendly because the call to `EventLoop_BreakevenOnProfit(...)` has side effects + can't be predicated by cmov; a real branch jump is required at the call site.
  - **Plan acknowledgment:** Plan body line 557 explicitly says "**or use cmov-friendly pattern per `branchless-dispatch-discipline.md`**" — acknowledging the sketch is intermediate. Plus line 558 explicitly says "**NOT acceptable:** hand-wave `if (cfg.cores[c].breakeven_on_profit_threshold > 0)` plain branch on slow path — Class 28 anti-pattern per H20".
  - **Hand-wave check:** The sketch as written is NOT a hand-wave (mask computation is branchless; ack of cmov-friendly alternative; reference to Pattern 1-5 from branchless-dispatch-discipline.md). It's an intermediate sketch with explicit "TODO refine to cmov-friendly" framing. This is acceptable PER-PHASE-B-CODING-DISCIPLINE.
  - **Coding-time discipline:** When B.3 lands, the SlowPathCycleOneCore body MUST resolve the sketch to one of:
    - **Pattern 5 (sink-fn-pointer)**: Pre-resolve breakeven callable into `state.cores[c].slow_cycle_actions.breakeven_fn` at boot — points at `noop_breakeven` when disabled OR `real_breakeven_on_profit` when enabled. Per-cycle cost: 1 indirect call (no branch). Sister to existing `OmsState::on_entry_fill_emit` Pattern 5 canonical.
    - **Pattern 4 (pre-resolution)**: Pre-resolve at boot whether to fire breakeven into `state.cores[c].slow_cycle_flags` bitmap; per-cycle = 1 BITMAP_IS_SET (already branchless-mask in HW).
    - **Plain BITMAP_IS_SET** (the existing wrapper pattern at ControllerEventLoop.hpp:3799) — already-canonical; reuse via early-return inside SlowPathCycleOneCore body.
  - **Sketch as-shipped would be Class 28 instance.** Sketch as-amended-during-B.3 with Pattern 4/5 closure would NOT be.
- **Additional caveat:** The v1.6 O3 mitigation premise depends on `cfg.cores[c].breakeven_on_profit_threshold` field existing (Class 14 finding 14a above). Resolution of 14a may also resolve O3 framing (e.g., gate on existing global bitmap → no per-core threshold needed → Pattern 4 BITMAP_IS_SET trivial).
- **Recommendation:**
  - Plan body amendment to specify the FINAL gating pattern explicitly (not just sketch + "or cmov-friendly"). Decide: Pattern 4 (BITMAP_IS_SET on existing global lifecycle_cfg_flags) OR Pattern 5 (sink-fn-pointer) OR introduce new per-core field.
  - Coding-time discipline: B.3 reviewer MUST verify the final pattern matches one of the canonical 5 patterns per `branchless-dispatch-discipline.md` BEFORE ship close.

### Class 29 — Silent zero-fee-rate from Order missing pre-resolution binding
- **Surface:** Order construction sites
- **Verdict:** CLEAN
- **Notes:** Plan body doesn't add new Order construction sites. ApplyBnbDiscount pre-loop mutator preserves cfg.cores[c].fee_rate_* on BOTH sides → Order pre-resolution at submit time will use BNB-discounted rates consistently. Closure side-effect: PARITY-030 closure REDUCES Class 29 risk on backtest side (where pre-helper bug allowed Orders to bind un-BNB-discounted rates).

### Class 30 — Sibling array on subsystem state without registry enrollment
- **Surface:** subsystem-state struct ↔ canonical registry
- **Verdict:** CLEAN
- **Notes:** Plan body doesn't propose new subsystem state arrays. No new FOREACH_*_PER_SLOT_FIELD enrollment needed.

### Class 31 — Hardcoded refs in always-loaded docs
- **Surface:** ci-tooling, registry (doc-discipline)
- **Verdict:** N/A — plan body is EPHEMERAL (not always-loaded)
- **Notes:** Plan bodies are explicitly the EPHEMERAL doc layer per `feedback_claude_md_guidelines_not_stuff_to_do`. Specific TECH_DEBT-NNN, version refs, ship-tag refs etc. are APPROPRIATE in plan body. N/A surface.

### Class 32 — Mega-file accumulation past size threshold
- **Surface:** ci-tooling, registry (file-size discipline)
- **Verdict:** CLEAN
- **Notes:** Plan body is 836 lines (under 1200-line plan body threshold per file-size-split-discipline.md). No sidecar required yet. v1.6 amendments added ~30 lines (within drift tolerance).

---

## Classes 30+ recommendation per focus area

### Should the "bytewise-identical math preservation at structural-fix extract" discipline (v1.6 O2) codify as NEW Class?

**Recommendation: NO — codify as SISTER to existing meta-disciplines + memory file (Stage 2), not new Class.**

**Reasoning:**
- The discipline articulated at v1.6 O2 is: "When extracting helper from existing inline body, preserve all arithmetic at caller-side OR prove helper-internal recompute is bytewise-identical to caller. Hidden divergence surface: caller-side clamp vs helper-internal unclamped recompute → divergent results when cfg value out-of-range."
- This is NOT a bug class that recurs invisibly across surfaces — it's a SHAPE-LEVEL discipline at the structural-fix-extract layer. Sister to:
  - `feedback_verify_symbol_existence_at_plan_drafting_time` (verify-before-cite at planning time)
  - `feedback_enumerate_consumers_before_registry_row_deletion` (consumer enumeration at deletion time)
  - `feedback_audit_canonical_sister_before_new_infra` (sister-discipline at extension time)
- The discipline is naturally located in MEMORY file space (operator-collaboration rule + audit-methodology) rather than RECURRING_BUG_PATTERNS class space (recurring code-smell with detection grep).
- Class space is for patterns where:
  - There's a mechanical Detection grep that catches instances
  - The pattern recurs in code (multiple sites in production) without operator intent
  - It causes silent runtime bugs (corruption / divergence / missed capability)
- The O2 discipline doesn't fit those criteria — it's a planning-time discipline applied to ANY structural-fix extract. The closer analog is M5 (train-serve-asymmetry-sweep) meta-discipline at DESIGN_PHILOSOPHY § 11.5, not a Class entry.

**Recommendation: codify O2 as NEW memory file `feedback_preserve_bytewise_identical_math_at_extract.md` Stage 2 DRAFT at this ship close (per plan body line 774 "Memory codification at ship close"). Sister to existing 3 disciplines above. Mention in DESIGN_PHILOSOPHY § 11.5 as meta-discipline sister entry only if 2nd application surfaces (per pattern-codification-lifecycle.md Stage 4 cohort threshold).**

### Anti-patterns at helper-extract boundary

Examination of plan body for shape-anti-patterns at the helper-extract boundary:

| Anti-pattern shape | Risk in v1.6 plan | Verdict |
|---|---|---|
| Extracting math without preserving exact arithmetic order | HIGH RISK — explicitly addressed by O2 (caller precomputes preserves verbatim from LIVE :898-906 + :915-920 + BACKTEST :234-238 + :258-263) | MITIGATED |
| Extracting branchless without preserving mask-compute | LOW RISK — no math kernel extraction; only orchestration extraction | N/A |
| Extracting per-core state init without preserving init ordering | MED RISK — addressed via Phase A Step A.4 71-row CSV enumeration as authoritative source; explicit per-row ORDERING preserved | MITIGATED |
| Extracting helper that changes lifetime semantics (e.g., heap-alloc → stack-alloc) | LOW RISK — Decision H pointers vs refs preserves caller-controlled storage; helper doesn't own storage | MITIGATED |
| Extracting helper that adds side effects (logging / metrics) not in original | LOW RISK — plan body explicit EXCLUDES tracing additions (M5 false-positive surface lists logging stays in caller per Decision G) | MITIGATED |

**No NEW anti-pattern shapes surfaced at helper-extract boundary.**

---

## Synthesis

### v1.6 amendment impact

| Metric | Value | Target |
|---|---|---|
| NEW Class instances introduced by v1.6 | **3** (all Class 14: cited cfg fields don't exist) | 0 |
| Class closures verified intact | 5 of 5 (Classes 18, 21, 24×3 cohorts via cohort accounting; Class 27 verified not regressed; Class 28 verified mitigation framing correct with sketch caveat) | 5 |
| Doc-debt findings | 0 | 0 |
| Discipline gaps surfaced | 1 (v1.5 `feedback_verify_symbol_existence_at_plan_drafting_time` not applied to v1.6 amendments) | 0 |

### Verdict: YELLOW

- 3 NEW Class 14 instances require triage before Phase B coding starts. The fabricated `cfg.cores[c].breakeven_on_profit_threshold` field is most critical because the v1.6 O3 PARITY-032 mitigation premise depends on it.
- 5-of-5 targeted closures verified structurally complete. The v1.6 5-helper extract correctly CLOSES Class 18 / Class 21 / Class 24 (4 cohorts) per the plan body intent.
- O2 bytewise-identical math discipline correctly applied; recommend codifying as memory file (not new Class).
- O3 PARITY-032 mitigation FRAMING is correct per H20 + branchless-dispatch-discipline; sketch as-written needs B.3-coding-time refinement to Pattern 4 or Pattern 5 BEFORE final code lands. Acceptable as intermediate sketch; not acceptable as final.

### Recommended next actions (operator triage)

1. **HIGH** — Plan amendment v1.7: address Class 14 finding 14a + 14b by:
   - **14a:** Decide breakeven gating: (i) preserve existing GLOBAL bitmap gate `MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT` (drop per-core threshold framing); (ii) add NEW per-core cfg field as scope expansion this ship; (iii) reframe O3 mitigation to operate at existing wrapper boundary.
   - **14b:** Mechanical rename `cfg.execution_core_count` → `cfg.num_execution_cores` at plan body lines 492 + 572.
2. **MED** — Comment cleanup in HEAD: EngineCommon.hpp:191 `OrderManager_RegisterCore` → `EventLoopState_RegisterCore` (sister to v1.5 D1 fix; carryover gap in skeleton-side description).
3. **MED** — Memory amendment: extend `feedback_verify_symbol_existence_at_plan_drafting_time` discipline to clarify "every amendment cycle covers ALL newly-cited symbols, not just initial draft."
4. **LOW** — At Phase B Step B.3 coding time: verify SlowPathCycleOneCore breakeven gate uses Pattern 4 (BITMAP_IS_SET) or Pattern 5 (sink-fn-pointer) — NOT the intermediate sketch from line 557.
5. **LOW** — At ship close: codify O2 bytewise-identical math discipline as new memory file `feedback_preserve_bytewise_identical_math_at_extract.md` Stage 2 DRAFT (sister to 3 existing disciplines).

### Consult-before-coding gate

Per CLAUDE.local.md "After pre-coding checks, ALWAYS consult before coding": **operator review required for finding 14a triage path** before Phase B Step B.2 body unlock. Findings 14b / 14c are mechanical; can be applied in v1.7 plan amendment without consult. O3 final pattern selection (Pattern 4 vs 5 vs reframe) ties to 14a resolution.

---

## Cross-references

- Predecessor audits: `plan_checks/2026-05-25-v5.15.5.F.4d.1.B.4-blindspot-scan.md`, `2026-05-25-v5.15.5.F.4d.1.B.4-parity-check-FINAL-FINAL-RE-AUDIT.md`, `2026-05-25-v5.15.5.F.4d.1.B.4-dod-audit-RE-AUDIT.md`, `2026-05-25-v5.15.5.F.4d.1.B.4-accounting-audit.md`, `2026-05-25-v5.15.5.F.4d.1.B.4-anti-spaghetti-quarterly.md`
- v1.5 enumeration drift findings: `plan_checks/2026-05-25-B4-enumeration-drift-findings.md`
- Boot call sequence CSV: `plan_checks/2026-05-25-B4-boot-call-sequence-enumeration.csv`
- Class 14 canonical: `DOCS/recurring-bug-patterns/class-14-plan-calls-non-existent-function.md`
- Class 18 canonical: `DOCS/recurring-bug-patterns/class-18-mirror-plans-missing-data-flow.md`
- Class 21 canonical: `DOCS/recurring-bug-patterns/class-21-multiple-parallel-descriptors.md`
- Class 23 canonical: `DOCS/recurring-bug-patterns/class-23-type-erased-typed-field-write.md`
- Class 24 canonical: `DOCS/recurring-bug-patterns/class-24-capability-cfg-surface-mismatch.md`
- Class 27 canonical: `DOCS/recurring-bug-patterns/class-27-single-value-cache-flattens-per-instance.md`
- Class 28 canonical: `DOCS/recurring-bug-patterns/class-28-branchy-sp-hp-dispatch.md`
- v1.5 discipline memory: `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/feedback_verify_symbol_existence_at_plan_drafting_time.md`
- EngineCommon.hpp HEAD: `CoreFrameworks/EngineCommon.hpp` (248 lines; ApplyBnbDiscount + BootGlobal bodies + 3 declarations)
