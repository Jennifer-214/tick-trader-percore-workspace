---
type: handoff
status: active   # THE one live handoff. Supersedes 2026-07-19-E.1.2.B-0.2-contract-blessed-sweeps-done-next-wiring-handoff.md. ≤1-active enforced by check_handoff_active_singleton.py.
ship_tag: E.1.2.B
plan_type: mixed
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: make every toolchain surface (CLI · local-CI · plugin · doc-viewer · AI) a thin consumer of the ONE core, versioned 0.x→1.0.0 — "the code becomes the IDE", felt in the editor
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-07-19-E.1.2.B-0.2-contract-blessed-sweeps-done-next-wiring-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [DESIGN_SPECS/meta-disciplines/calibration-corpus-non-vacuity-discipline.md, DESIGN_SPECS/meta-disciplines/differential-to-absolute-gate-contract-widening.md, DESIGN_SPECS/meta-disciplines/advertised-capability-never-exercised.md, DESIGN_SPECS/framework-patterns/doc-intelligence-toolchain-architecture.md]
coding_status: 0.2 — BB-1/2/3 CLOSED · (b)(c)(e-guard) DONE · (a) partial · NEXT = (f) per-ID triage, then (g), then (d)
---

# E.1.2.B `0.2` — the gate layer is WIRED; next = (f) the ID-triage corpus

**Created:** 2026-07-20 · **Branch:** `feat/v5.15-live-readiness` · session window **`2167d9d..HEAD`** (`git log --oneline 2167d9d..HEAD` to count), engine **UNTOUCHED**

> **⚠️ READ § "The five things that will bite you" BEFORE writing code.** This session's defects
> were *all* semantic — a guard that couldn't fail, a lint blind to one spelling, a fixture anchored
> to a moved value. None were type errors and none would have been caught by a green run.

## State at handoff write

| Repo | HEAD | Note |
|---|---|---|
| Engine `FoxML_Trader_v2` | `4c076ed` | **ZERO engine code touched all session** (dev-plane only, HARD rule) |
| Workspace | *(see `git log`)* | window `2167d9d..HEAD`; **⚠️ VERIFY PUSH STATE** — `git rev-list --count @{u}..HEAD` |
| Plugin `fox-symdeps.nvim` | `6dcbbd7` | untouched this session |

**⚠️ PUSHES REQUIRE CARAMEL'S SIGN-OFF.** SSH is per-operation-confirm; the first attempt often
times out (~2 min), a retry succeeds. `ssh-add -l` reporting "Error connecting to agent" is NORMAL.
Do NOT diagnose a broken key. Never put a push in an unattended step.

## ▶ NEXT ACTION — (f) the ID-triage corpus

**Corpus state, re-derived 2026-07-20 after an adversarial review REFUTED the first version of this
section.** Do not trust a remembered number here; re-run `python3 tools/check_capture_audit.py --check 14`
and the in-process finding dump before planning.

**23 unique findings remain** (was 51). Composition: `defined-twice` 7 · `sequence-gap` 11 ·
`cited-but-undefined` 5. Note the corpus counts UNIQUE keys, while the raw finding list is longer
because one id is cited from many files — `PARITY-39` alone had 6 citing sites. Counting sites and
calling them ids is how "8" and "24" both looked correct for the same set.

<!-- VOLATILE-OK-SECTION: postmortem — restating what this document previously claimed IS the finding here; the live state is re-derived above -->

**What the previous version of this section got WRONG** (a-class review, 2026-07-20 — recorded because
the failure is more instructive than the fix):

> It claimed the 8 dangling ids had **"no content anywhere"** and that any tombstone would be
> **"inventing history"**, and on that basis parked the whole deliverable on Caramel. **Three of the
> eight had recoverable content sitting in the repo.** What was actually verified was "no defining row
> in any *ledger file*" — true, tool-checked. What was *written* was "no content anywhere" — false, and
> never searched for. That is **AR-15 (structure verified, consequence assumed)**, which this very
> document arms the reader against. Worse, the recommended disposition — tombstone-as-unknown, "content
> unrecoverable" — would have appended a permanent falsehood to an append-only ledger, making **H21 the
> invariant the action violated** rather than the one it invoked.

**Already resolved, mechanically, with zero operator input:**

| id | what was actually wrong | fix |
|---|---|---|
| `PARITY-39` | full entry existed at `DOCS/PARITY_ISSUES.md` as a chronological audit-log bullet, never given a structured `id:` row | entry transcribed from the bullet + its disposition register (status closed, FIXED `.E.0.10`) |
| `TECH_DEBT-16` | complete body drafted at `v5.14.9-soft-risk-degradation-ladder.md:912` under "entry shape (auto-write per CLAUDE.local.md)", never transcribed | entry transcribed verbatim into `open.md` |
| `TECH_DEBT-101` | debt already re-homed to `TECH_DEBT-238` on 2026-07-16; only the forwarding pointer was missing | forwarding tombstone in `closed.md`; slot RESERVED per H21 (it is cited from engine source) |

Plus **23 DECISION collisions dissolved** by implementing the `superseded` exemption that TD-249's
CONCRETE PARSE SPEC already prescribed — and which TD-249 recorded as *FIXED* while the code contained
no such logic (`grep supersed tools/citable_ids.py` → 0). `D-1..D-13`, `C-1..C-4`, `F-1..F-6` were
baselined as defects against a log the spec explicitly exempts. **That was 45% of the triage corpus
unreachable by definition** — a guard that cannot converge is the guard that gets ignored (M3), which
is Class-51 re-instantiated one layer up, in the baseline rather than the resolver.

**What genuinely remains, and what it needs:**

- **5 `cited-but-undefined`** — `TECH_DEBT-102 / -103 / -104 / -125 / -128`. Before escalating ANY of
  these, **search outside the four ledger `sources`** — `plans/**`, decision logs, prior-sprint MASTERs.
  That search is what the previous version skipped, and it resolved 3 of 3 where it was run. Only the
  residue after that search is Caramel's to disposition.
- **7 `defined-twice`** — `TECH_DEBT-29 / -107 / -114 / -116 / -118 / -164 / -175`. TD-249's own sweep
  classifies `-164` and `-175` as NOT defects ("a 1-line forwarding tombstone" / "distinct suffixed
  IDs"). Confirm that reading before treating either as work.
- **11 `sequence-gap`** — empty slots with no tombstone. Mechanical: each needs a tombstone or a
  documented reservation. No operator knowledge required.

**Also in (f) per the plan body, and absent from the previous version of this section:** the 5
split-brain TECH_DEBT ids (`-029/-107/-114/-116/-118`, each `open=1 closed=1` at HEAD), the `B16` and
`D-245`/`D-246` tombstones, zero-pad normalization so `Class 07` ≡ `Class 7`, and the `F-M1` finding-ID
prefix convention. **The zero-pad item is the direct fix for the PARITY-39 root cause** — the PARITY
namespace anchors on `id: PARITY-` and the ledger spells it `PARITY-039`; the equivalent `_why_int`
normalization already exists for TECH_DEBT. The previous version dropped the item that dissolves its
own blocker.

**⚠️ OPERATOR-ONLY, and required before the corpus reads honestly:** the ID-set golden and the
findings baseline both need re-blessing — the defining index has GROWN (the triage fixes above, plus
this session's new decisions) past the pinned golden, and the baseline still holds the pre-exemption finding set. Re-derive
both before acting; do not trust any figure in this paragraph:

```
python3 tools/check_capture_audit.py --check 14      # index size vs golden, live-vs-baselined
wc -l tools/goldens/citable-ids.txt                  # what the golden currently pins
grep -vc '^#' tools/lib/citable_ids_baseline.txt     # what the baseline currently holds
```

over-broad baseline does not hide a regression (the check compares live ⊆ baseline, and NEW is 0), but
it misrepresents the corpus size to whoever reads it next. **`tools/bless.py` is TTY-gated by D-394 and
will HARD-REFUSE non-interactively — this cannot be automated, by design.**

<!-- VOLATILE-OK: order is ANCHORED to D-398, which owns it; read the decision, not this line -->
**DEFINITION OF DONE for (f)** — stated here because the handoff never did, which on a document about close-out completeness is the ironic gap. The SSoT is the baseline file's own header (`tools/lib/citable_ids_baseline.txt:2`): *"A finding NOT in here FAILS. Shrinking this to EMPTY is deliverable (f)."* So (f) is finished when the baseline is EMPTY and `--check 14` stays green with no baselined entries — every id either defined, tombstoned, or exempt by a logged decision. Partial credit is measured by what the baseline still holds, not by how many ids were touched.

Then **(g)** citation currency (severity flip + rename-map resolver), then **(d)** the update
orchestrator. Order locked at **D-398**, dependency-derived.

## The five things that will bite you

<!-- VOLATILE-OK-SECTION: postmortem — restating what this document previously claimed IS the finding here; the live state is re-derived above -->

1. **⭐ A guard's first run is mostly YOUR noise.** Check 14's first run: 18 raw dangling citations resolved to 8 real (`citable_ids_baseline.txt:10`). `T13`-`T19` were TEST ids, `H23` a proposed invariant, `Class 0` a format template. AR-14's false-positive surface already forbade mechanizing scope collisions — I ignored a discipline this repo had codified *and my own docstring claimed to honour*. **De-noise BEFORE baselining**, or the triage corpus starts as noise and gets abandoned.
2. **⭐ Disbelieve counts that look wrong.** My META regex reported **8 of 10 meta-disciplines double-defined** — all false (`DESIGN_PHILOSOPHY` has a *defining* table in bold and a cross-ref index in plain). Tightening it then silently dropped **6 of 22** H-numbers. Both caught only by checking against a known range. **Never trust a match count you didn't sanity-check against an expected set.**
3. **⭐ A perf fix that changes results is a behaviour change in a costume.** Two optimisation attempts on Check 14 each altered the finding count (145→141) while barely moving the clock. Reverted both. Only the provably-safe filter kept (every citable id ends in a digit).
4. **`--all` on B-Plus is UNRUNNABLE** — 1237 serial compile probes × 451ms = **9.3 min**, times out. TECH_DEBT-259. This is why a fabricated symbol survived in a live deliverable.
5. **Engine stays at `4c076ed`.** Dev-plane work cites **T1–T12** for its OWN invariants, never H1–H22 — those govern engine code. The one deliberate exception is **H21** (append-only identifiers), which is invoked on the DOC plane because ledger ids are externally-cited slots with the same reuse hazard; (f) rests on it entirely.

## What landed (window `2167d9d..HEAD`)

**All three build-blockers CLOSED.** BB-1 corpus contract wired on both readers (6 `--paths` sites — the handoff said 1, C-395 said 3, measurement found 6). BB-2 the validity leg. BB-3 `registry_id` — whose framing was wrong: the denominator isn't 192, it's the `Mn` specs, because `registry_id` is the *reverse* map and every namespace already has a defining-form anchor.

**`0.2` deliverables:** (b) goldens ✅ · (c) tools-gate WIRED as pre-commit **Check T** ✅ · (e) the citable-ID guard ✅ (folded as **Check 14**, not a new tool) · (a) partial · (d)(f)(g) open.

**TECH_DEBT closed:** 245 (gitignore-blind gates) · 255 (bless rubber-stamp) · 254(a)(b) · 249 guard-half.
**TECH_DEBT opened:** 256 (AST matchers) · 257 (**HIGH** — compile_commands 1:1) · 258 (plugin registry surface) · 259 (B-Plus `--all`) · 260 (clang-tidy).

**Two broken guards found and repaired.** The H21 self-test had a **dead tooth** — a hardcoded `SHARDED_SNAPSHOT_VERSION|8` against a ledger at `|10`, so the sed was a no-op and it reported "guard broken" when the truth was "fixture failed to plant". And `check_identifier_retirement` itself was **broken through the workspace path** (rolled its own root; `tools/` is a dir symlink). The lint that should have caught it was **half-blind** — it matched only the pathlib spelling, so 4 `os.path` rollers went unseen, 3 measurably broken. **The roll-your-own-root class is now fully CLOSED**: 11 rollers migrated, baseline EMPTY.

**Three new specs:** `differential-to-absolute-gate-contract-widening.md` · `advertised-capability-never-exercised.md` · the blessed-output lifecycle folded into `calibration-corpus-non-vacuity-discipline.md` (promoted `2-draft` → `3-first-canonical`).

<!-- VOLATILE-OK: order is ANCHORED to D-398, which owns it; read the decision, not this line -->
**Decisions:** **D-397** static-analysis arc placed at named increments (formal verification REJECTED on merit) · **D-398** `0.2` order (e)→(f)→(g)→(d) · **D-399** the citable-ID guard's shape.

## Arming for the next action

- **Read:** `tools/lib/citable_id_namespaces.json` **including `_why_*` fields — they carry the measurements** · TECH_DEBT-249 — now carries a **⚠️ CORRECTED (D-401)** banner because its PARTIAL-CLOSED block still states the REFUTED "no content anywhere" premise (left unedited beneath the banner per the don't-erase-history convention); it ALSO contains **two conflicting 8-id lists** (the 2026-07-19 sweep and the 2026-07-20 PARTIAL-CLOSED block differ in four members, and the matching COUNT is what hides the divergence). Trust neither; re-derive with `--check 14` · TECH_DEBT-219 (the ledger split, likely root cause) · D-399.
- **Run (not just read):** `python3 tools/citable_ids.py` (its summary NOW applies the D-400 supersede exemption — it previously printed 23 RAW DECISION double-definitions, contradicting `--check 14`; one exemption, one call site) (prints the defining index) · `python3 tools/check_capture_audit.py --check 14` · `./tools/check_session_docs.sh` · `bash tools/foxtag/parity_check.sh`.
- **Anti-patterns:** **AR-14** (citable-ID drift — and its FP surface, which I violated) · **Class-51** · **AR-15** (structure verified, consequence assumed) · the NEW `advertised-capability-never-exercised`.
- **Invariants:** T1 · T4 · T5 · T11 · T12 · **H21** (this whole deliverable is H21 on the doc plane).

## Gate state — all green at handoff

`check_session_docs.sh` clean · `parity_check.sh` PASS incl. §1b + validity leg · pre-commit gained **Check T** · tools-inventory clean · import-from-core baseline **EMPTY** (the roll-your-own-root class is CLOSED — that one is a STATE, not a tally, so it is safe to trust from prose) · membership pin EXACT on both profiles · both goldens' teeth proven. **Counts deliberately omitted — re-derive them; see the note at the end of this document.**

## Paste into a fresh session

```
Picking up E.1.2.B 0.2. The GATE LAYER is wired; next is (f), the ID-triage corpus.

Run: /accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-20-E.1.2.B-0.2-gates-wired-next-f-triage-handoff.md

NEXT: (f) — the ID-triage corpus. RE-DERIVE IT FIRST; do not trust any count you read:
  python3 tools/check_capture_audit.py --check 14

THE BLOCKER IS MOSTLY MECHANICAL. An earlier version of this handoff claimed the dangling
ids had "no content anywhere" and escalated them to Caramel. That was REFUTED: 3 of 3
searched had recoverable content in the repo (PARITY-39 was a full entry missing only its
`id:` row; TECH_DEBT-16 a complete body drafted in a plan and never transcribed;
TECH_DEBT-101 already re-homed to -238). All three are now defined. Separately, 23 DECISION
collisions were spec-exempt noise and dissolved via the `superseded` exemption (D-400).

So: BEFORE escalating ANY id, search outside the four ledger `sources` — plans/**, decision
logs, prior-sprint MASTERs. That search is what the earlier version skipped. Only the residue
after it is Caramel's to disposition. Writing "content unrecoverable" over recoverable history
would put a permanent falsehood in an append-only ledger — H21 violated by the action that
invokes it (D-401). Most of the remainder is tombstones + dedup needing no operator input.
Then (g) citation currency, then (d) the update orchestrator. Order locked at D-398.

WILL BITE:
1. A guard's first run is mostly YOUR noise — Check 14's 18 raw dangling went to 8 real (T13-T19 =
   test ids, H23 = a proposed invariant, Class 0 = a format template). De-noise BEFORE
   baselining or the triage corpus starts as noise.
2. Disbelieve counts. My META regex called 8 of 10 meta-disciplines double-defined; all
   false. Tightening it then dropped 6 of 22 H-numbers. Check against a KNOWN range.
3. A perf fix that changes results is a behaviour change. Two attempts altered the finding
   count while barely moving the clock; both reverted.
4. B-Plus --all is unrunnable (9.3 min). TECH_DEBT-259.
5. Engine stays at 4c076ed. Dev-plane cites T1-T12; H21 is the deliberate exception, applied on the doc plane.

all gates green. Verify push state: git rev-list --count @{u}..HEAD
```



## ⚠️ THE CLOSE-OUT ITSELF FAILED FIRST — read this before running yours

This session's close-out was **hand-rolled and incomplete**, and the operator caught it, not a gate.
A session (window `2167d9d..HEAD`) shipped with **ZERO commits** to four auto-write surfaces that each had an owed
entry: `tools/CLAUDE.md` (the gotcha harvest — its own text calls that "the entire point"),
`FEATURE_LOOKUP.md`, `DOCS/LANDMINES.md`, `DOCS/PARITY_ISSUES.md`.

**The mechanical sweep was green the whole time** — because none of those files is mechanically
gated. The green was read as "close-out done". That is the exact shape this session spent all day
finding elsewhere: *a green that means less than its reader thinks.*

**It is the SECOND consecutive recurrence.** The predecessor handoff records the same failure about
itself ("the first close was HAND-ROLLED… only the MECHANICAL subset of `/capture-audit` ran").
Both times the only trigger was operator pushback — M7's definition of a discipline that is
remembered rather than enforced.

**So it was escalated STRUCTURALLY rather than remembered again** (C-391's lesson: a fourth
restatement is the mistake, not the coverage). **`tools/check_close_out_completeness.py` now
exists** — TECH_DEBT-250's actual deliverable, overdue across 6+ sessions. Wired ADVISORY into
`check_session_docs` (window-scoped, so a HARD wiring would red every mid-session run). Teeth proven
against the real failure: all four surfaces were zero across `2167d9d..f6c91ab`, so it **would have
RED'd at the close I got wrong**.

**M10 on it:** TOTAL for *was the surface touched*, PARTIAL for *was the entry right*. A green means
the surfaces were touched, never that the capture was complete. It exists to make the judgment
checks' ABSENCE visible — not to replace them.

### The judgment half of /capture-audit — RUN this session, results recorded

Checks 1/4/8/11/13/14 are tool-backed. **2, 3, 5, 6, 7, 9, 10, 12 are judgment and had never run.**

| Check | Result |
|---|---|
| **Check 2** plan-body frontmatter | ❌→✅ the body had **no frontmatter at all**; added `audit_tier: MED` + `sister_specs` |
| **Check 3** decision-log artifact existence | ✅ all 7 artifacts D-397/398/399 cite exist |
| **Check 5** handoff currency | ✅ this document |
| **Check 6** Stage-6 / M7 escalation | ❌→✅ **the close-out recurrence** → built the guard above |
| **Check 7** DESIGN_SPECS promotion | ✅ no eligible drafts (calibration-corpus already promoted) |
| **Check 9** memory → spec cross-ref | ❌→✅ both new specs had **zero** memory refs; linked from `feedback_guards_compound_enforcement_is_leverage` + `feedback_passing_test_is_not_verification` rather than minting restatements |
| **Check 10** going-forward-rule currency | ✅ deliberately NO new rule — this is a *second instance*, so M7 says structure, not a fourth memory |
| **Check 12** CP-1 amendment cascade | ✅ sister links made reciprocal on every amended spec |

Also fixed: the decision log's own frontmatter said `D-97..D-396` (stale by three) and its
NAVIGATION INDEX stopped at D-396 — both current now, with a gate-layer index appended.

## ⚠️ TWO ITEMS ONLY CARAMEL CAN CLEAR — the ID-set golden AND the findings baseline

`tools/goldens/citable-ids.txt` is pinned BELOW what the index now resolves — more ids were
defined after the last bless (D-399 and PARITY|41 early in the session, then PARITY-039,
TECH_DEBT-016 and TECH_DEBT-101 when the dangling-id triage was corrected). All are legitimate
creations: H21 is append-only, so additions are legal and Check 14 correctly stays green — the
golden flags REMOVALS, not additions.

<!-- VOLATILE-OK: the exact figures are deliberately omitted; the command below is the source -->
Re-derive the gap before blessing — `python3 tools/check_capture_audit.py --check 14` prints both
the index size and the baselined count. The findings baseline ALSO needs re-blessing: it still holds
the pre-exemption finding set, which over-reports the corpus size to whoever reads it next.

**But they are unprotected until blessed.** If any of them is silently dropped later, the golden cannot
catch it — it was never in the golden. That is a gap-in-waiting rather than a live defect.

Clearing it requires a TTY by **my own D-394 gate**, which is the control working exactly as
designed and is also why I could not do it:

```
python3 tools/check_capture_audit.py --check 14     # confirm only additions, zero removals
python3 - <<'EOF'
import sys; sys.path.insert(0,'tools')
from citable_ids import defining_index
import bless as b
idx = defining_index()
b.bless('tools/goldens/citable-ids.txt',
        sorted(f"{ns}|{r}" for ns,e in idx.items() for r in e), 'citable-ids')
EOF
```

It will show the per-line diff and demand `bless citable-ids` typed exactly.

## TaskList state (recreate via /accept-handoff Stage 7)

| ID | Status | Subject |
|---|---|---|
| #1 | completed | BB-1 corpus-contract wiring (both readers, all 6 `--paths` sites) |
| #2 | completed | the two golden-discrimination gaps (sort closed; DOCS-exclusion recorded as not-a-defect) |
| #3 | completed | BB-2 parity validity leg |
| #4 | completed | BB-3 `registry_id` (framing corrected: denominator is the `Mn` specs, not 192) |
| #5 | completed | the two owed codifications (blessed-output lifecycle + gate-widening spec) |
| #6 | completed | stale prose ×3 (golden counts · refuted collision · header/footer pair) |
| #7 | completed | delete probe scratch + re-measure the contract's declared scan counts |
| #8 | completed | residual queue worked through |
<!-- VOLATILE-OK: historical — records how the scope estimate moved during discovery -->
| #9 | completed | path-arg routing scope (1 → 3 → measured 6) |
| #10 | completed | TD-245 subsumption call (closed) / TD-246 (adjacent, sized) |
| #11 | completed | Class-14 fabrication `_build_ref_index()` corrected + annotated |
| #12 | completed | decision-log STATUS staleness ×3 |
| **#13** | **pending** | **▶ (f) ID-TRIAGE CORPUS** — re-derive the live set with `check_capture_audit.py --check 14` before planning; the earlier framing ("blocker is NOT mechanical / no content exists") was **REFUTED** — see NEXT ACTION. Most of the residue is mechanical; search `plans/**` + decision logs + prior MASTERs before escalating any id. Golden + baseline both need an operator TTY re-bless (D-394). |
| #14 | pending | (g) citation + content currency — severity flip (MISSING/OOB → HARD) + rename-map resolver + supersede check |
| #15 | pending | (d) D-374 update-orchestrator (LAST — it orchestrates writers, and (g) adds one) |
| #16 | pending | (a) contract docs — per-tool consumes/emits/guarantees (PARTIAL). **Homed:** completes alongside (d), which is the last writer-touching deliverable and will change what the contracts must state. Not floating. |
| #17 | pending | TECH_DEBT-254(c) `stage: 6-cadence-locked` — promote qualifying specs or retire the value AND its CLAUDE.md recipe together |
| #18 | pending | TECH_DEBT-246 functions-unguarded — SIZED (~401 defs), blocked on a proportionality POLICY call |
| #19 | pending | TECH_DEBT-259 B-Plus `--all` unrunnable — speed fix is NOT the closure; the cadence is |

## Capture-completeness

- **Decisions → LOGGED.** D-397/398/399, paired `<!-- D/C/F -->` + `<!-- STATUS -->`; Check 4 PASSES (the short `<!-- D:` sentinel form, 19 uses, was being silently dropped, taking 5 real ids with it — it had been silently skipping 19, including D-372..D-381).
- **Ledgers → 5 opened (256–260), 3 closed (245, 255, 254 partial), 249 partial.**
- **Specs → 2 NEW + 1 EXTENDED + 1 PROMOTED.** Reciprocal sister links verified bidirectional.
- **Memory → 1 CORRECTED:** `feedback_delegate_via_locked_spec_at_implementation` said *"implementation = parallelize agents against the LOCKED spec"*; Caramel's instruction is the opposite for code authoring. Rewritten + MEMORY.md index updated.
- **A factual error I committed and corrected:** the new anti-pattern spec listed the nvim plugin's features as "advertised but never exercised". **Wrong** — Caramel corrected it: they work, they want taste. Replaced, and added a § False-positive surface (M3) with the binary test that separates the two.
- **Known gaps, homed not hidden:** (f) dispositions → TECH_DEBT-249 · `6-cadence-locked` → 254(c) · TD-246 sized-not-started · B-Plus `--all` → 259.

---

## ⚠️ A NOTE ON THE NUMBERS IN THIS DOCUMENT

Counts here are SNAPSHOTS and several will be stale by the time you read them — the commit count
was wrong twice in a row before being replaced with a SHA range, because a raw count goes stale on
the very commit that records it. That is unfixable by writing a better number.

**Re-derive anything you would ACT on, do not trust it from this page:**

```
git log --oneline 2167d9d..HEAD | wc -l          # session size
python3 tools/citable_ids.py                     # id counts per namespace
python3 tools/check_capture_audit.py --check 14  # dangling / baselined
./tools/check_session_docs.sh                    # HARD/ADV check counts
python3 tools/check_tools_inventory.py           # tools enrolled
```

The STABLE facts — the ones worth trusting from prose — are the ones with an ANCHOR rather than a
tally: engine HEAD is `4c076ed`; the build-blockers BB-1/2/3 are closed; deliverables (b), (c) and
(e) are complete and (a) is partial, so the remaining order is **(f)→(g)→(d)** per D-398; and (f)'s
open ids are listed BY ID rather than by count.

<!-- VOLATILE-OK: postmortem — quoting the stale values IS the finding -->
**This paragraph previously failed its own rule** — it read "3 of 7 deliverables" and "(e)→(f)→(g)→(d)"
after (e) had landed, and the number had gone stale in a way no proofread catches: the plan body's
"3" meant `{b, c, a-partial}` while this document's "3" meant `{b, c, e}`. **The count stayed 3 while
its membership rotated underneath it.** That is the tell that nobody re-derived it — and it sat in the
one paragraph instructing the reader to stop re-deriving. Name the members, never the tally; a
membership list goes visibly wrong, a count goes silently wrong.

**The lesson, recorded because it recurred three times in one close:** a volatile value written into
a document is stale by construction. This is the prose-plane twin of the `gen_code_map`
stamp-on-change defect fixed in code this same session — a stamp that moves when nothing moved is
churn, and a count written mid-session is wrong before it is committed.

---

## INDEPENDENT REVIEW (AR-8 — mandatory per `/close-session` Stage 6.5.4)

<!-- VOLATILE-OK-SECTION: postmortem — restating what this document previously claimed IS the finding here; the live state is re-derived above -->

**Reviewer:** a-class adversarial agent, default-REFUTED, independent of the author. **Verdict: REFUTED.**
Ran seven mechanical gates (all green, all confirmed) and found the PROSE did not survive contact with HEAD.

**What it caught that four consecutive self-checks did not:**

| # | finding | disposition |
|---|---|---|
| CRITICAL-1 | the NEXT ACTION's blocking premise was **false** — 3 of 8 "content-less" ids had recoverable content; the recommended tombstone-as-unknown would have written a permanent falsehood into an append-only ledger | **FIXED** — all 3 defined (`PARITY-039` entry · `TECH_DEBT-016` transcribed · `TECH_DEBT-101` forwarding tombstone); D-401 logged |
| CRITICAL-2 | (f) was truncated from the real corpus to 8 ids, and "THE BLOCKER IS NOT MECHANICAL" was false of the rest | **FIXED** — section re-derived; the 5 split-brain ids + zero-pad + tombstone residue restored from the plan body |
| HIGH-3 | 23 of 51 baseline entries violated the guard's own written parse spec — the `superseded` exemption TD-249 recorded as *FIXED* was never implemented | **FIXED** — `active_sites()` + `supersede_exempt`; corpus 51 → 23; D-400 logged |
| HIGH-4 | the "STABLE facts — trust these from prose" paragraph carried the two most stale values in the document | **FIXED** in all three copies (handoff, plan body, MASTER); D-402 logged |
| HIGH-5 | `TECH_DEBT-250` read `status: open` / "still unbuilt" against a tool that exists and is wired | **CLOSED**, with the stale evidence marked historical rather than erased |
| MED-6..12 | two conflicting 8-id lists · non-reproducible citation rows · stale commit count · "counts deliberately omitted" (there were ~15) · `T1–T12 never H1–H22` while resting the next action on H21 · `400 + 19 ≠ 405` · the 70% misattributed to the wrong measurement | **FIXED** (the reproducibility/zero-pad item is homed in (f), where its own root-cause fix already lives) |

**Conceded as verified-TRUE by the review, not padding:** engine untouched at `4c076ed` · golden/index
diff exact · the bless snippet executable as written · all 11 named paths exist · `check_session_docs.sh`
SWEEP CLEAN · `parity_check.sh` PASS incl. §1b · handoff-active singleton == 1 · `import_from_core`
baseline EMPTY (the roll-your-own-root class genuinely closed) · Check-backing correct · the 9.30 min
arithmetic holds.

**The meta-lesson, and why Stage 6.5.4 now exists.** Four self-checks returned clean on a document whose
central claim was false. The mechanical gates were green the whole time and stayed green — they were
never the thing that was wrong. **A green from a partially-mechanised ritual reads exactly like done**,
which is why the judgment half is now enforced by `check_close_out_completeness.py` and the independent
review is mandatory rather than advisory. This finding is itself the third consecutive recurrence of
the AR-8 class, so it escalated to structure (M7) rather than to another restatement. D-403 logged.

---

## ADDENDUM — the apparatus audit (2026-07-20, after the independent review)

<!-- VOLATILE-OK-SECTION: postmortem — the gap descriptions quote measured state at the time; re-derive live state with the commands in this section -->

Caramel asked two questions that each found a real gap. Recording both, because the *questions*
generalize better than the fixes.

**"Are we making use of the new toolchain and CI gates?"** — Partly no. `/sync-workspace` had been
hand-rolled: `cp` the memory files, commit, push. That skips `migrate_memory_frontmatter.py`, which
canonicalizes the frontmatter the harness mangles on agent-written memories and re-derives sister
links from body `[[links]]`. When finally run it healed 7 files, 4 of which the manual pass never
opened. **The skip was invisible because hand-fixing WORKED every time.** That is the signature of
the whole class: the tool is not faster than the hand version, it is *correct* where the hand version
is merely plausible.

**"Can you audit the other skills for the same gap?"** — An independent audit did, and refuted my
framing in three ways worth carrying forward:

1. **WIRED is not binary.** Three tiers: the hook (fires on commit, but every check is
   staged-path-scoped) · `check_session_docs.sh` (fires ONLY when a handoff or MASTER is staged) ·
   `run_all_tests.sh` (**nothing** triggers it). ~25 HARD checks sit behind the middle tier. Calling
   the third tier "wired" would be the same error the audit exists to catch.
2. **The naive fix would have shipped a vacuously-green gate.** `migrate_memory_frontmatter.py`
   returned 0 unconditionally — no `--check`. Wiring `--dry-run` as-is = Class 51. Adding `--check`
   was a *precondition*, not a follow-up.
3. **The guard I built to make this visible was blind to it.** `check_sync_owed` byte-compared backup
   vs live, so a hand-rolled `cp` produces zero drift and it goes green. Its own docstring stated the
   refutation. It detected *sync never ran*, not *sync ran by hand* — and the latter is the failure.

**Landed:** `--check` gate mode (proven red on planted drift, green on restore) · `check_sync_owed`
rewired onto it · **CHECK 4 invocation truth** in `check_tools_inventory.py` · 2 dead selftests wired
HARD · `/close-session` Stage 6.5 moved into the numbered flow and its **Stage-5 number collision
fixed** (I reused a live section number on the day I spent enforcing H21) · the
advertised-capability spec corrected: **a skill is not a cadence, because nothing fires a skill.**

**Decision:** **D-404** — a skill is not a cadence; verify the "Invoked by" claim rather than
trusting it. Carries the three-tier WIRED model and why CHECK 4 must resolve transitivity +
wrapper equivalence (without both it flags working wiring, and a noisy guard is an ignored
guard — this same failure one level up).

**Homed, not done → `TECH_DEBT-261`.** Ranked, with item 1 the one that matters:
`check_per_node_registry_integrity.py` is the only **CAPITAL-surface** tool reachable solely by
typing a slash-command (H17 / H22 / Class 25-27); cited by 4 skills, triggered by none. Then ~10
further never-run selftests — sharpest being the determinism teeth, where the net *is* hook-wired and
runs constantly but nothing proves it can still FAIL.

**Re-derive the apparatus state before acting on any of this:**

```
python3 tools/check_tools_inventory.py          # CHECK 4 — rows whose 'Invoked by' is false
python3 tools/migrate_memory_frontmatter.py --check
python3 tools/check_close_out_completeness.py   # both halves + sync-owed
./tools/check_session_docs.sh                   # the full floor
```

---

## ⚠️ READ FIRST IF YOU ARE PICKING UP COLD — the one structural finding

Three review passes over this close produced nine defects. Classified by SHAPE, **all nine are the
same defect**: a fact stated in two places, one DERIVED from the other, with no mechanical link.
Not nine problems — one problem, nine times. **`TECH_DEBT-262` holds the full table and the
candidate fix.**

**Do not read the nine as a quality problem with this session.** Five predate it. One
(`TOOLS.md` invocation-truth) had four instances fixed reactively the day before and reopened
immediately — a class that regenerates after a manual sweep is structural by definition. Long
context explains why they were MISSED here; it does not explain why they EXIST.

**The trap for you specifically:** each instance is cheap and satisfying to point-fix, and every
point-fix leaves the generator running. This session closed four of them and the fifth appeared
within the hour. **If you find a tenth, fix it — then go read TECH_DEBT-262 before fixing an
eleventh.**

**Design the general guard before building it.** A half-general derived-fact mechanism would itself
become a derived fact that drifts, which is the joke this entry is already the punchline of.
