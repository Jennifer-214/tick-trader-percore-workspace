---
type: handoff
status: active
ship_tag: .E.0.10
plan_type: live-readiness
sprint_end_goal: make the codebase more maintainable for future development (v5.X professionalization); live-readiness gated behind the whole E-series
ship_end_goal: land the four durable-now capital-safety fixes (NEW-1 / A21 / A20 / A17) that the A16 fill-path cascade surfaced as INDEPENDENT of the .E.1 rework
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-13-E.0.10-A24-closed-A16-decisions-next-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
required_reading: [decision-log D-212 + D-213 + D-214, register §cascade-trace 2026-06-13 + §keystone-validation-outcome, fill-path-completeness-and-normalization-discipline.md (spine 1), RECURRING_BUG_PATTERNS Class 46, CoreFrameworks/LiveReadiness.hpp, CoreFrameworks/EngineSharded/Run.hpp:465-600, CoreFrameworks/ControllerConfig.hpp (NormalizeForMode :2008), CoreFrameworks/ReconciliationLoop.hpp, CoreFrameworks/Reconcile.hpp, CoreFrameworks/OmsFieldRegistry.hpp, CoreFrameworks/OrderManager.hpp (paper synth :1013 + ACK :1413), characterization-test-discipline.md, DOCS/STRATEGY_AND_CODING_RULES.md, DOCS/LANDMINES.md (Landmine 10), CODE_MAP (regen at pickup)]
engine_head: f2ef5d6 (clean per git; NO engine code landed this session — DESIGN_SPECS edits are engine-side gitignored-in-place)
workspace_head: a021c8d + UNCOMMITTED this-session captures (D-213/D-214 + register cascade blocks + RBP Class 46 + DESIGN_PHILOSOPHY § 4 + the .E.1 addendum + the spine-1 spec) — review + commit at pickup, or treat as in-flight capture
coding_status: planning-complete (design arc CLOSED; Phase 4 = code)
defers: none
deletion_scope: none
---

# Handoff — `.E.0.10` Phase 4 — the four durable-now capital fixes (fresh-session pickup)

## TL;DR (the 30-second pickup)

- **The A16 fill-path design arc is COMPLETE + captured.** A `/decision-check` → 3-agent cascade → 4-agent keystone-validation distilled the elusive "core issue" into a **three-spines** framing (decision-log **D-214**). A proposed single "keystone" was **REJECTED as over-lumped** (AR-6) — the adversarial caught it; the system worked.
- **Phases 1-3 of the D-214 codification plan are DONE + verified** (doc SWEEP CLEAN ×2): the guards (AR-6 bump + RBP Class 46), the design guardrails (spine-1 spec + the § 4 latency principle), and the `.E.1`-plan amendment.
- **Phase 4 (THIS handoff) = land the four durable-now CAPITAL code fixes** — NEW-1 → A21 → A20 → A17 — each with the fix + char-test + 5-way verify drill. These are INDEPENDENT of the `.E.1` rework (the aggregator would only RENAME them, not fix them) → durable-now.
- **No code landed this session** (engine HEAD still `f2ef5d6`); all captures are docs/plans/decision-log. **The workspace working tree is DIRTY with this session's captures — review/commit at pickup.**

## The three-spines framing (the design arc's conclusion — read D-214 for the full record)

The accounting/fill spine's findings are NOT one problem. They are **three orthogonal spines**, triaged per-finding by subsumption-not-adjacency:

1. **Fill-completeness / protocol-temporal-normalization** — the fill path's actual root (A16, the SELL-partial whole-close, A2/A4, A17, A18, NEW-3). "Code assumes a fill is single/complete/sync." Fix = normalized fill event + terminal-actions-gate-on-`ORDER_FILLED` + reconstruct-net-not-replay. Codified: `DESIGN_SPECS/data-disciplines/fill-path-completeness-and-normalization-discipline.md` + RBP **Class 46**. The real handler → `.E.1` (foundation items 53/54 + the 2026-06-13 addendum).
2. **Cross-thread coherence** — the 9-site torn-read class + A22 (`cross-thread-multiword-read-consistency-discipline.md`; publish-once/consume-many). → `.E.1` aggregator.
3. **Per-node / per-cluster purity (H22)** — A1/A24 + **A34** (`per-node-purity-scale-invariance.md`). A34 (cluster-aggregate concentration "owned by nothing") is the one genuine cluster-composition hole → a per-cluster-pure governor at `.E.1`.

**The independents (NO spine — durable-now, THIS handoff):** A20 (persistence), A21 (formula), NEW-1 (mode-flag decoupling). The unification lives at the META level: a reasoning shape (AR-6) + "the accounting subsystem never got the hot path's SSoT+H22+publish discipline" — NOT a single grand mechanism.

## Phase 4 — the four fixes, IN ORDER (compounding), EACH with the full drill

**The drill per fix** (the repeatable `.E.0.10` discipline): fix + a **characterization test that VARIES the divergence-revealing fields** (anti-vacuous, per `characterization-test-discipline.md` — the A1 bug hid behind a fixture where global==override) + `./build.sh test` + an independent adversarial `// ADV-REFUTE` verify + `tools/calls_graph_diff` CLEAN + `tools/run_all_tests.sh --full` (asan/ubsan) + a **micro-commit** (compile-gated; `wip(.E.0.10):` prefix). **Mind Landmine 10** — `tests/` is a workspace symlink; `git add tests/` from the engine fails "beyond a symbolic link" (stage the workspace path).

1. **NEW-1 (HIGH; first — highest compounding) — the live-safety gate field-decoupling (Knight-shaped).** `LiveReadiness_Verify` enforces its pre-flight REFUSE suite ONLY under `live = (trading_mode == TRADING_MODE_LIVE)` (`LiveReadiness.hpp:230,241,256`), but real orders are authorized by `use_real_money` (`Run.hpp:577,493`; OMS `MASK_OMS_STATE_LIVE_TRADING` ← `live_trading = use_real_money`). `NormalizeForMode` only acts `if (trading_mode==LIVE)` (`ControllerConfig.hpp:2009`) — **NO field couples them** (orchestrator-verified). ⇒ `use_real_money=1` + `trading_mode≠LIVE` places real orders while the 8 pre-flight REFUSEs (HMAC stamps / feature+label hash drift / model age / secret / mlockall) PASS un-enforced. **Fix = a boot consistency guard `use_real_money=1 ⇒ trading_mode==LIVE`** (cleanest — retroactively arms the WHOLE pre-flight suite + the `model_verify_strict`/`reconcile_mode` strict-flips at once). NOTE: `use_real_money=1` already refuses on missing secrets.cfg (`Run.hpp:581`) + hardcoded-strategy-in-live (`:493-507`) — those are NOT the gap; the centralized pre-flight SUITE is.
2. **A21 (HIGH/CRIT) — reconciler open-notional formula.** `ReconciliationLoop.hpp:129` — `expected_usdt = oms_balance − inflight_buy_notional` excludes a FILLED-open position's notional from BOTH terms → `ProcessReconcile` (`OrderManager.hpp:~1481`) clobbers `oms->balance` mid-position → false kill-switch trip + permanent P&L double-count. **Wrong even with an atomic read** (a FORMULA bug; the `.E.1` aggregator does NOT touch it). **Fix = subtract the FILLED-open-position notional from `expected_usdt` + do NOT clobber `oms->balance` while `active_bitmap != 0`.**
3. **A20 (HIGH) — watermark persistence.** `last_seen_trade_id` is `SKIP_PERSIST` (`OmsFieldRegistry.hpp:300`) + seeded 0 every boot → on a live `AUTO_SYNC` boot, `GetMyTrades(fromId=0)` replays ALL recent trades → double-book. **Fix = seed/persist the watermark + bound the `GetMyTrades` window.**
4. **A17 (LOW/observability, but a hard co-requisite of the future A16) — paper/REST `order_complete`.** The paper synthetic fill (`OrderManager.hpp:~1013`) `memset`s `cmd.result` then never sets `order_complete` → paper orders land `ORDER_PARTIAL`. **Fix = set `order_complete=1` on the paper synthetic fill + the REST adapter — genuine FILL only, NOT the ACK-only path (`fill_qty==0 → ORDER_ACKNOWLEDGED`, `:1413`).** Verified SAFE by both cascade agents: only flips paper/backtest `total_filled` under-count→correct; NO perturbation to OrderEventLog replay (`OEVT_FULL_FILL` is unconditional), calibration, bandit, reconciler, or backtest parity.

## NOT in Phase 4 (homed — do NOT pull forward)

- The A16 **real partial-fill handler** (weighted-avg re-entry + keep-slot-open + the **SELL-partial-reduce** = the `handle_sell_fill:1234` `Portfolio_CloseSlot`-ignores-`fill_qty` whole-close fix, RBP Class 46) + **A22** + **A29** + **A34** → `.E.1` (foundation items 53/54 + the 2026-06-13 addendum).
- A2/A4/A5 (parser decimal/commission/side) → **D-123**. A11/A12/A10 (bandit/dual-arm) → **.E.M**. F-059 (exit golden-master) + A6 (chokepoint tp/sl guard) → the `.E.0.10` net (TaskList #3/#4). A36 (breakeven-on-partial flag) → tombstone-flag + rehome-to-D-206, decided WITH the cfg-flag-orphan cohort (TaskList #6).

## Arming for the next action (M8 — the immediate next action is **NEW-1**)

The surface is **boot-path live-safety + money accounting**, so arm it:
- **Route to:** `/hft-audit` (the NEW-1 boot/gate path + the `use_real_money` vs `trading_mode` coupling) + `/accounting-audit` (A21/A17 — the fill→book `Money`/P&L SSoT + the reconciler `expected_usdt` formula) + `/trace-deps` (the `LiveReadiness_Verify` consumers + the `use_real_money`/`trading_mode` readers) + `/bug-check` (RBP Class 46 for A17; the Knight-Capital/H21 family for NEW-1). ARM each agent per M8 — reference docs + the toolchain to RUN + scout-first; keep the adversarial agent independent on the verdict.
- **Run (not just read):** `./build.sh test` · `tools/run_all_tests.sh --full` (asan/ubsan) · `tools/calls_graph_diff` · `tools/check_per_core_registry_integrity.py` (if a cfg surface is touched) · regen `./tools/gen_code_map.sh`.
- **Read first:** `DOCS/STRATEGY_AND_CODING_RULES.md` (OMS/boot invariants) + the spine-1 spec + `characterization-test-discipline.md` + the H-invariants in play: **H4** (Money math), **H22** (per-node purity), **H8** (latency budgets), **H21** (Knight-Capital identifier/safety-control discipline — the NEW-1 shape).

## TaskList state at handoff write (recreate at pickup)

| # | Status | Subject |
|---|---|---|
| (done) | ✅ completed | A16 decision-check + cascade + keystone-validation → D-212/213/214; three-spines |
| (done) | ✅ completed | Phase 1 — AR-6 bump + RBP Class 46 (SWEEP CLEAN) |
| (done) | ✅ completed | Phase 2 — spine-1 fill-completeness spec + § 4 latency principle (SWEEP CLEAN) |
| (done) | ✅ completed | Phase 3 — .E.1 foundation-plan D-214 amendment (verified-clean; B-Plus RED is the design-plan false-positive) |
| 12 | ⬜ pending | **Phase 4 — land NEW-1 → A21 → A20 → A17** (each fix+test+5-way-verify) ← THIS handoff |
| 3 | ⬜ pending | F-059 — exit-chain golden-master (after A24/A25; now unblocked) |
| 4 | ⬜ pending | A6 — chokepoint gate-emit tp/sl-validity guard (closes the .E.0.10 net with F-059) |
| 5 | ⬜ pending | .E.M adaptive-entry redesigns (homed stub) |
| 6 | ⬜ pending | cfg-flag-orphan cohort — A36 + A13/A14/A35 (+A37) wire-or-tombstone |
| 7 | ⬜ pending | Class-44 + Class-46 detector CI guard (the structural close) |
| 8 | ⬜ pending | D10 re-activation paper-test (A24 behavior-validation) |

## Critical pickup-time reads

- decision-log `v5.15.5.F.4d.1.E-architecture-v2.md` — **D-212** (A16 synthesis) + **D-213** (cascade findings) + **D-214** (keystone-validation → three-spines + the corrected codification plan; the Phase-4 fixes' full grounding).
- register `plan_checks/E.0.10-finding-disposition-register.md` — **§ cascade-trace (2026-06-13)** (NEW-1/sell-partial/no-reaper/A20-A21-reclass/the .E.1 gaps) + **§ keystone-validation outcome** (the three-spines + F2/A34/latency-gap).
- `DESIGN_SPECS/data-disciplines/fill-path-completeness-and-normalization-discipline.md` (spine 1) + RBP **Class 46** (`tick-trader-percore-workspace/DOCS/recurring-bug-patterns/class-46-...md`).
- NEW-1 surface: `CoreFrameworks/LiveReadiness.hpp` (`:193-271`) + `CoreFrameworks/EngineSharded/Run.hpp:465-600` + `CoreFrameworks/ControllerConfig.hpp` (`NormalizeForMode` :2008).
- A21/A20 surface: `CoreFrameworks/ReconciliationLoop.hpp` (`:104-130`) + `CoreFrameworks/Reconcile.hpp` (`:205-245`, `:343` AutoCancelStale) + `CoreFrameworks/OmsFieldRegistry.hpp:300`.
- A17 surface: `CoreFrameworks/OrderManager.hpp` (`:996-1021` paper synth, `:1413` ACK, `:1432` state-set).
- `DOCS/LANDMINES.md` (Landmine 10 — the engine/workspace `tests/` symlink commit-split).

## Known-state (verify/be-aware at pickup)

1. **The doc SWEEP shows B-Plus RED — it is a DESIGN-PLAN FALSE-POSITIVE, not a fabrication.** The Phase-3 amendment pulled the forward-design `.E.1` plan into the B-Plus symbol-existence scan for the first time. All 10 "fabrications" + 6 line-anchor issues are the **pre-existing 2026-05-28 `(NEW)`-sketches + drifted line-refs at lines 367-1425** — **ZERO are the D-214 amendment** (verified by reading every flagged block; the amendment is at ~:1913+). The stale anchors re-derive at the `.E.1` pre-coding gate per the plan's own `:1910` addendum. **NEW finding (TECH_DEBT candidate, operator to decide):** `check_plan_body_symbol_existence` needs a forward-design-plan exemption (skip `(NEW)`/`stage:draft` blocks, or don't scan design-stage plans).
2. The **capital-test adversarial-markers** ADV advisory (5 unmarked blocks) is the KNOWN backlog — NEW capital tests (the Phase-4 char-tests) need `// ADV-REFUTE` or `// ADV-SELF` markers.
3. **Uncommitted state:** the workspace working tree holds this session's captures (D-213/214 + register + RBP Class 46 + DESIGN_PHILOSOPHY § 4 + the .E.1 addendum); the engine-side DESIGN_SPECS edits (meta-AP-index + the spine-1 spec) are gitignored-in-place (need `/sync-workspace`). Review + commit before Phase-4 code, or carry as in-flight capture.

## Predecessor context (what landed across this design session — verify at pickup, Stage 4.5)

- **Decisions:** D-212 (A16 synthesis), D-213 (cascade findings: NEW-1 gate-decoupling + sell-partial + no-reaper + A20/A21-reclass + the .E.1 plan-completeness gaps + A17-verified-safe), D-214 (keystone-validation → three-spines + the F2/A34/latency-gap + the corrected codification plan). All in `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`.
- **Codifications:** AR-6 recurrence note (`meta-anti-pattern-index.md`); RBP **Class 46** (index row + numbering note + per-class file); the spine-1 DESIGN_SPEC; DESIGN_PHILOSOPHY § 4 effective-latency principle (3 contracts); the `.E.1` foundation-plan 2026-06-13 addendum.
- **Engine:** HEAD `f2ef5d6` (UNCHANGED — no engine code this session). Tests last known 3388/0 (A24 close); asan/ubsan 3246/0.
- **Rollback anchor:** `ckpt-a24-closed` (engine `f2ef5d6` + workspace `5c28fcb`) — the last clean code-state. (This session added only docs on top.)

## Pickup command

`/accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-13-E.0.10-phase4-durable-now-capital-fixes-handoff.md`

**Immediate next action:** review/commit this session's doc captures (or carry as in-flight) → then **implement NEW-1** (the `use_real_money ⇒ trading_mode==LIVE` boot consistency guard) with the full fix+char-test+5-way-verify drill → then A21 → A20 → A17. Consult Caramel on each fix's shape before coding (consult-before-coding); do NOT auto-proceed.
