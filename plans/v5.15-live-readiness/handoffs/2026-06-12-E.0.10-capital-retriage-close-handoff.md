---
type: handoff
status: active
ship_tag: .E.0.10
plan_type: live-readiness
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness)
ship_end_goal: a pre-`.E.1` correctness/characterization net + the capital bugs the net surfaced, closed where durable
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-11-E.0.10-A1-closed-capital-closures-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, register (bug-hunt § + A-rows), plan-body, sprint-MASTER, nav-infra (DAG + CODE_MAP-regen + path-discipline docs per /handoff Stage 2.9)]
engine_head: 5b02cce (A3/A8 FlattenAll honest-count + leg-gate; clean tree; on feat/v5.15-live-readiness)
workspace_head: c220fe5 (PUSHED — register + ledgers + 3 memories + specs + 2 CI tools + decision-log D-196/D-197 + meta-index AR-10 + TECH_DEBT-173 + .E.1 fold + this handoff)
deletion_scope: none
coding_status: mid-coding-checkpoint — A3/A8 landed; the Net-1 NET itself (F-059 + MED-tier char) is the next deliverable
---

# .E.0.10 handoff — capital re-triage + nav-infra apparatus close (2026-06-12)

**Target ship:** `.E.0.10` — Net-1 correctness net + the capital bugs it surfaced (pre-`.E.1` regression safety).
**Branch:** `feat/v5.15-live-readiness` · **Engine HEAD:** `5b02cce` (clean tree, 3368/0) · **Register (live disposition SSoT):** `plan_checks/E.0.10-finding-disposition-register.md`.

> **Compaction note:** this session ran VERY long (capital re-triage + a full apparatus-wiring arc). Treat every claim as a HINT; verify against current code (`feedback_compaction_degrades_treat_handoffs_as_hints`). This close ran the full `/close-session` ritual incl. a 3-agent adversarial re-grade of A5/A6 + an independent deliverable-completeness review (verdict CLEAN).

## What landed this session (the arc)

1. **The whole A-series got an adversarial re-triage — most were OVER-RATED (the headline "capital bugs" were mostly phantoms/dormant).** Outcome (D-197): **A1** = the ONE real capital bug (already fixed `b89b60d`); **A3** = durable honest-count contract landed (`5b02cce`) + failure-mechanism→`.E.1`; **A8** = future-proof landed; **A7** = MOOT-UNREACHABLE; **A5** = dormant-tripwire (folds into D-123); **A6** = LOW (chokepoint guard owed); **A2/A4** = data-loss stop → D-123.
2. **A3/A8 FIXED + committed (`5b02cce`, micro-commit, 3368/0).** `EventLoop_FlattenAll`: honest count (`submitted += pushed`) + loud `SHORTFALL` log + leg `(slot&1)&partial_on` (gated on partials — corrects the bare `slot&1`). The queue-full half-flatten MECHANISM + retry are **`.E.1`-subsumed** (it reworks the SPSC submit topology) → **folded into the `.E.1` plan §TL;DR #2** (two-foundations: NOT pinned here).
3. **3 methodology codifications** (the recurring over-rating gap — A7/A5/A6 ×3): **MOOT-UNREACHABLE** disposition (`feedback_moot_unreachable_disposition` + register legend) · **"Reachability before severity — a confirmed PATH ≠ a reachable BUG; trace the trigger's INFLOW"** (adversarial-multi-agent-audit-methodology.md § + meta-anti-pattern **AR-10**) · **micro-commits, compile-gated** (`feedback_micro_commits_compile_gated`).
4. **The nav-infra apparatus arc (operator-driven, D-196):** the nav-infra (DAG + CODE_MAP + `/dependency-chain-trace`/`/trace-deps`) + the impact/data-flow line of reasoning is now a **first-class, CI-ENFORCED input** to pickup + the 18-skill audit/plan-check cohort. ONE inheritance point (skill-knowledge-consultation Stage-0 item 6) + `/handoff` Stage 2.9 + **`check_navinfra_cohort_reference.py`** (HARD 8 in `check_session_docs.sh`; teeth-proofed) — red-builds if any cohort skill drops it. Takes the operator OUT of the manual-enforcement loop.
5. **Branchless sweep HOMED (TECH_DEBT-173):** H7/H20 are convention-only (HOLE per guard-coverage-matrix). Owed = sweep SP/HP for data-dependent-mispredicting branches (KEEP warmup-stable/~100%-predicted) + convert + a **deterministic branch-scanner TOOL (not LLM)** as the CI gate. Rides `.E.1` (optimization defers; `.E.1` rewrites those surfaces).

## The OPEN QUEUE — immediate next = the Net-1 NET ITSELF (the ship's deliverable, still mostly unwritten)

- **#7 — F-059 / hpg-bc-1 golden-master oracle (THE NEXT ACTION).** Freeze the REAL per-fill `tp_pct`/`sl_pct` → `live_tp`/`live_sl` (`ExecutionCore.hpp:~543`) → SG-trigger → exit-P&L chain the `LegacyReferenceDriver` stub never covered. Bug-hunt sharpened the gaps: trigger-vs-book SLIPPAGE (books at `tick−slip`, not `live_tp`, `ExecutionCore.hpp:~508`+`:~1887`) + MOM stddev-TP discarded by flat `tp_pct` (`:543`). Golden-master = freeze REAL output + diff (`feedback_golden_master_over_reimplemented_oracle`). `// ADV-REFUTE` binding.
- **#8 — MED-tier characterization:** F-090/093 (parser full price/qty/commission), F-046 (FlattenAll balance — happy-path only; the failure path is `.E.1`'s per two-foundations), F-018 (partial-exit money vs real `handle_sell_fill`→`DrainPostFill`; slot-MAPPING is `.E.1` changes-by-design).
- **A6 chokepoint guard (the one durable-now capital guard, LOW):** a gate-emit tp/sl-validity invariant (sl_pct≥0, `sg_stop_loss_price < entry`) at the `Strategy_BuildParameters` funnel covering ALL 6 strategy emits + a stamp-load floor on `label_*_pct` (`CoreModelZoo.hpp:~347`). **clamp + gate-zero/SHALT, NOT an assert** (don't abort a node on bad fixture data). Each = fix + char-test (per the new char-test-discipline) + 3-agent refute + `// ADV-REFUTE`.
- **A2/A4 data-loss STOP (→ D-123 decimal):** accumulate partial-fill qty + parse `"z"` (`BinanceUserData.hpp:~339`); pass reconcile commission. The decimal-exactness rides D-123; the data-loss stop is durable-now.
- **A5 → D-123:** add the venue side field to `OrderResult` in the SAME struct-touch as D-123's decimal rework (the chokepoint is `OrderManager_ProcessFillCommand`; `"S"` is currently unparsed).
- **Codification guards:** #21 H22 DESIGN_SPEC + CI-check · #10 torn-read class + the A1 reconstruct-vs-forward class → RECURRING_BUG_PATTERNS + CI (TECH_DEBT-165; note #10-reconstruct is N=1 → candidate-class, fold its CI into H22's #21) · #12 char-test-discipline DESIGN_SPEC + AP4 extension (governs #7/#8 — do it BEFORE the char-tests for right-first-time).
- **#4 conc-5 verify** (tsan on a disposable clone — the `.E.1` HARD live-enable gate confirmation).
- **Backlog re-triage #1/#2/#5** (fpmem granular ×8 + the E.1-bucket MED/LOW + PRE-PAPER-TEST PERSIST + the 93 TECH_DEBT + 5 PARITY → register). **Status: nothing is UNHOMED** (every CRIT/HIGH is register-dispositioned OR bucket-homed to its `.E` ship); this is re-VERIFICATION, not debt-discovery.
- **#13 `.E.1` B-Plus reconciliation** (NOW actively trips — folding A3 into the `.E.1` plan made it session-modified; the 10 illustrative `(NEW)` fragments + 1 stale `Run.hpp` citation are the documented FP; a clean sweep needs `SKIP_PLAN_BODY_CHECK=1` until fenced). · **#20** module CLAUDE.md (4 left) · **#23** carry-forward check.

## Nav-infra pickup reads (per the new /handoff Stage 2.9 — for per-edit impact + data-flow)
- **Dependency-graph DAG:** `subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md` (`.E.1` is the high-risk root; F-059 touches the exit chain).
- **CODE_MAP:** regen `./tools/gen_code_map.sh` at pickup (anti-fabrication; grep it for `ExecutionCore` / `SG_Evaluate` / the exit-chain symbols before editing).
- **Per-function impact:** `/dependency-chain-trace chain:<symbol>` before touching the exit chain (write/read sites + data-flow + blast radius).
- **Path-discipline:** `DOCS/STRATEGY_AND_CODING_RULES.md` + `plans/_cross-cutting/2026-05-06-latency-path-discipline.md` (F-059 touches the hot-path exit; H7 branchless).

## TaskList (recreate via TaskCreate at pickup — 15 open)

| # | Status | Subject |
|---|---|---|
| #7 | pending | **F-059 golden-master oracle (THE NEXT ACTION)** — real per-fill exit chain |
| #8 | pending | MED-tier characterization (F-090/093 · F-046 happy-path · F-018 money) |
| A6 | pending | A6 chokepoint guard — gate-emit tp/sl-validity invariant (6 strategies) + stamp-load floor |
| #17 | pending | A2/A4 data-loss STOP (accumulate qty / parse "z" / commission) → D-123 |
| #21 | pending | H22 DESIGN_SPEC + CI-check (per-node-purity) |
| #10 | pending | torn-read class CI (9 sites) + A1 reconstruct-class (candidate; fold CI into #21) → RECURRING_BUG_PATTERNS |
| #12 | pending | char-test-discipline DESIGN_SPEC + AP4 extension (governs #7/#8) |
| #4 | pending | conc-5 verify (tsan on clone) — `.E.1` live-enable gate |
| #5 | pending | backlog→`.E.1`-home mapping (93 TECH_DEBT + 5 PARITY) |
| #1 | in_progress | fpmem granular ×8 re-triage → register |
| #2 | in_progress | re-triage `.E.1` bucket MED/LOW + PRE-PAPER-TEST PERSIST |
| #13 | pending | `.E.1` B-Plus reconciliation (fence the 10 illustrative fragments) |
| #20 | in_progress | module CLAUDE.md (4 left: Strategies/ML_Headers/MemHeaders/FixedPoint) |
| #23 | pending | codify the /handoff prior-queue carry-forward check |
| A5 | pending | A5 fold into D-123 OrderResult (venue side field) |

## ⚠️ Drift items + landmines
1. **`check_session_docs.sh` RED on the `.E.1` B-Plus** (#13) — VERIFIED known-FP (10 illustrative `RING_SIZE`/`F`-template fragments at lines 367–1425 + 1 stale `Run.hpp` citation at 1380; NONE from this session's A3 fold). Clean sweep = `SKIP_PLAN_BODY_CHECK=1` until fenced.
2. **5-block capital-test adversarial-marker backlog** (standing advisory; D-110/oms-ts-2/core_fees carry no `// ADV-REFUTE`). Add markers as each gets an independent refute.
3. **Engine `DOCS/tech-debt/` is NOT symlinked** — ledger via the WORKSPACE `DOCS/tech-debt/open.md` path (Landmine 9).
4. **Adversarial-default + reachability-before-severity BINDING** — capital verification is independent-refute + trace the trigger's inflow; `// ADV-SELF` is opt-out-with-reason.
5. **Module CLAUDE.md engine symlinks are LOCAL-only** (gitignored; the workspace `*/CLAUDE.md` are the SSoT).

## Git state
- **Engine:** `5b02cce` on `feat/v5.15-live-readiness` — **PUSHED** (origin == `5b02cce`); A3/A8; clean tree, 3368/0; pre-commit guards passed.
- **Workspace:** `c220fe5` — **PUSHED** (register + 3 memories [moot-unreachable / micro-commits / no-unhomed-debt] + 2 specs + 2 CI tools + TOOLS.md + decision-log D-196/D-197 + meta-index AR-10 + TECH_DEBT-173 + the `.E.1` fold + this handoff + memory.backup refresh). Prior active handoff superseded.

## Pickup
```
/accept-handoff /home/caramel/code/tick-trader-percore-workspace/plans/v5.15-live-readiness/handoffs/2026-06-12-E.0.10-capital-retriage-close-handoff.md
```
**Immediate next action:** the **Net-1 net** — F-059 golden-master (#7), ideally after the char-test-discipline spec (#12) so the characterization fixtures are right-first-time (vary the fields whose divergence the assertion could mask). Coding is READINESS-gated; consult-before-coding is binding.

## Operator collaboration (every-turn)
Address Caramel as Caramel / she / her. No AskUserQuestion modals — inline options. Evaluate on robustness + latency + design, not time. After pre-coding checks, consult before coding. Adversarial/independent-refute + reachability-before-severity is the BINDING default. Defer needs SUBSUMPTION (the plan's design redoes the fix), never ADJACENCY. **Don't leave tech debt that has no future plan — unhomed debt is code smell** (everything must trace to a home/plan or close now). **Micro-commits, compile-gated; never push broken code.** Coding is READINESS-gated, not earned.
