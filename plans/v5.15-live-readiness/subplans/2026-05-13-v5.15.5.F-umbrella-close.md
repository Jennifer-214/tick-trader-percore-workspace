# v5.15.5.F — Umbrella close ritual (SKETCH)

**Branch:** `feat/v5.15-live-readiness`
**Predecessor:** all `.F.*` sub-ships SHIPPED
**Status:** SKETCH — standard ritual; expand if anything unusual

---

## Pre-flight checklist

- [ ] ALL `.F.*` sub-ships SHIPPED with their own tags
- [ ] `controller_test` passes
- [ ] Paper trade for ≥5 min — no regressions vs pre-`.F` baseline
- [ ] All 5 binaries build (`./build.sh test gui suite tsan asan`)
- [ ] Backtest determinism verified at every `.F.4e` / `.F.4f` / `.F.4g` sub-ship boundary

## Steps

### 1. Version.hpp bump

Edit `Version.hpp`:
```cpp
#define VERSION_STRING "5.15.5.F"  // bump from "5.15.5.E" or last sub-ship
```

### 2. Postmortem

Write `plans/v5.15-live-readiness/postmortems/2026-05-13-v5.15.5-F-postmortem.md`:
- Sprint context: paper-test surfaced 6 distinct bugs + 1 cfg surface class
- Sub-ships shipped (list with tags)
- Structural wins: 3 new design specs (model-health-flag-registry-pattern, registry-bitmap-set-discipline, universal-cfg-field-registry-pattern, slow-path-cfg-resolution-cache-pattern) — note 3 of these promoted via the codify-as-patterns-mature rule
- Class closures: live-vs-replay divergence (.F.1.B); panel/parser/persist drift class (.F.4 sprint); registry-bitmap SET discipline (.F.3 codification)
- Latency wins (if .F.4e / .F.4g shipped): cache-miss reduction measurements + slow-path p99 deltas
- TECH_DEBT entries added (note carry-overs to v5.16)
- Pattern codification lifecycle stage updates per `pattern-codification-lifecycle.md`

### 3. HOT_PATH_CHANGELOG entries

Per CLAUDE.md item 17, verify entries exist for:
- `.F.4e` ResolvedCoreCfg slow-path entry (resolution cost + cache-miss savings)
- `.F.4f` K-state cohort pack (branchless predicate cycle savings)
- `.F.4g` AoS-by-core re-layout (cache-miss delta from `perf stat`)

### 4. FEATURE_LOOKUP.md auto-writes

Per CLAUDE.local.md going-forward rule (2026-05-12), add entries for:
- `.F.3` Model health flags (new operator-visible: ML Status panel registry-driven display)
- `.F.4` cfg surface expansion (new operator-visible: 100+ cfg fields newly rendered in SettingsPanel)
- `.F.5` per-core ML overrides (new operator-visible: 13 new `core_N_*` cfg keys)

### 5. TECH_DEBT.md scan

Add entries for deferred items:
- `__builtin_prefetch` at slow-path entry (from `.F.4e` spec future-work)
- AVX-512 batch-load of ResolvedCoreCfg (from `.F.4e` spec)
- Delta-cache resolved struct (from `.F.4e` spec)
- File picker dialog for KIND_FILE_PATH (from `.F.4d` spec)
- CI script for registry/parser parity (status from `.F.4d`)

### 6. PARITY_ISSUES.md scan

Verify no new parity issues surfaced. Particular care:
- `.F.4e` ResolvedCoreCfg — verify train/serve parity unchanged (FP conversion paths preserve determinism)
- `.F.4g` AoS-by-core — verify cfg parse semantics preserved (operator cfg.cfg backwards compatible)

### 7. Decoupling roadmap update

Per CLAUDE.local.md going-forward rule, add breadcrumbs to `plans/_future/2026-05-12-decoupling-endgoal-roadmap.md`:
- `.F.4*` series exposes universal cfg registry — POSITIVE positioning for decoupled Settings UI (registry can be marshaled over IPC; cfg parse becomes a serializable contract)
- `.F.4e` ResolvedCoreCfg — POSITIVE positioning for headless engine (cfg → resolved struct is independent of GUI; resolved struct could be published per-core via shared memory)

### 8. Workspace sync

```bash
cd /home/caramel/code/FoxML_Trader_v2
# Verify all symlinks resolve; verify plans/, .claude/skills/ mirror workspace
/sync-workspace  # or manual:
cd /home/caramel/code/tick-trader-percore-workspace
git status  # verify clean
```

### 9. Tag + push

```bash
cd /home/caramel/code/FoxML_Trader_v2
git tag v5.15.5.F
git push --tags

cd /home/caramel/code/tick-trader-percore-workspace
git push
```

### 10. Optionally: ultrareview

If you want a cloud review of the whole sprint, you (Caramel) invoke `/ultrareview` manually. Not Claude-launchable.

## What's next (post-`.F`)

- **v5.15.5.G or v5.15.6:** depends on next paper-test findings + Gemini cleanup priority
- **v5.16:** post-v5.15 paper-test era; sequenced with decoupling sprint (per `2026-05-08-v6.0-CANDIDATE-headless-service-colo.md`)
- **TECH_DEBT carryovers:** prefetch + AVX-512 batch-load + delta-cache from `.F.4e` spec (revisit when slow-path budget tightens)
