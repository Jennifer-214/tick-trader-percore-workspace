# /parity-check report — v5.14.8 AMENDED — 2026-05-09

## Executive summary

- **Original verdict**: DRIFT-RISK (5 CRITICAL + 1 HIGH + 2 MEDIUM)
- **Amended verdict**: CONDITIONAL APPROVE (7 original blockers structurally closed; 6 new contingencies introduced)

The amendment transforms from a manual incremental approach to a structural architectural fix via two new X-macro registries (FOREACH_STAMP_BOUND_MODEL_CONST + FOREACH_FAILURE_MODE).

This closes:
- The v5.9.5b production-caller N-site bug class (CRITICAL-5 + HIGH-1)
- The Section J observability N-site class (CRITICAL-3)

But introduces **3 CRITICAL code-review gates** + **3 MEDIUM gates** that must pass before shipping.

## Original findings closure status

| Finding | Status | How |
|---|---|---|
| CRITICAL-1: Stamp body schema fields missing | ALREADY-CLOSED | Registry-driven struct generation (.0 + .A) |
| CRITICAL-2: FEATURE_REGISTRY_HASH stability not verified | ALREADY-CLOSED | Caller-defined X-macro pattern; gated by NEW-5 |
| CRITICAL-3: Stale-feature observability missing PerCoreSnap | CLOSURE-CONTINGENT | FOREACH_FAILURE_MODE; gated by NEW-1 |
| CRITICAL-4: Canonical-order position not anchored | ALREADY-CLOSED | Registry row order; gated by NEW-2 |
| CRITICAL-5: Production-caller field population gap (v5.9.5b shape) | CLOSURE-CONTINGENT | STAMP_MODEL_CONST_AUTOPOPULATE; gated by GATE-CRITICAL-5 |
| HIGH-1: scaler_fit_data_hash caller audit incomplete | ALREADY-CLOSED | Same as CRITICAL-5 closure |
| MEDIUM-1: Canonical-order documentation gap | ALREADY-CLOSED | Registry makes ordering declarative |
| MEDIUM-2: Health_LogCriticalRateLimited storage location | ALREADY-CLOSED | FOREACH_FAILURE_MODE auto-generates rate-limit static |

## New findings from amended approach (6 items)

### NEW-1 (HIGH): Panel-branch auto-generation pattern NOT PROVEN

- Amendment claims FOREACH_FAILURE_MODE auto-generates ML Status panel branches
- No precedent in codebase: StampBoundCfgRegistry has no panel branches; PerCoreSnap manual enum shows no X-macro generation
- Gate required: v5.14.8.B code must prove either (a) TUI X-macro template OR (b) new code-gen infrastructure
- Impact if failed: PerCoreSnap field auto-generates, but panel branches remain manual → N-site class incomplete

### NEW-2 (CRITICAL): 24-field migration wire-format preservation contingent

- Risk: HMAC signature changes if FOREACH_STAMP_BOUND_MODEL_CONST registry rows defined in different order than current emit
- Evidence: Current ModelStampResult (lines 1188-1336) + stamp_write_for_model emit (line ~1896) must have IDENTICAL field order
- Gate required: Pre-v5.14.8.A code review must verify registry row order matches current emit order byte-for-byte
- Impact if failed: Existing v5.14.6 stamps fail verification (HMAC mismatch or parse error)

### NEW-3 (HIGH): 38+ caller field-access preservation contingent

- Risk: If X-macro generates different field names than current manual struct, callers break
- Gate required: Spot-check 3-5 callers (CoreModelZoo_VerifyExpected, EngineSharded boot, BacktestEngine) for identical field-access syntax
- Impact if failed: Silent data corruption (some callers read wrong field; type errors may not catch due to similar types)

### NEW-4 (MEDIUM): stale_feature_events counter semantics unclear

- Risk: Is this a uint32_t counter (incremented per skip) or uint8_t flag (0/1)?
- Gate required: Code clarification (populate at Features_PackAll when stale skip occurs)
- Impact if failed: Counter saturates at 255 (uint8_t) or display renders garbage

### NEW-5 (MEDIUM): 6 X-macro call sites may miss FOREACH_FEATURE 7-column update

- Risk: Missing even one X-macro signature update causes compilation error
- Sites: Enum gen (~507), FEATURE_NAMES[] (515), FEATURE_VERSIONS[] (521), FEATURE_ENABLED_FLAGS[] (527), hash-compute (562), Features_PackAll (626, 693)
- Gate: v5.14.8.E code review grep all 6, verify each updated to 7-param
- Impact if failed: Compilation failure (loud; doesn't ship)

### NEW-6 (MEDIUM): model_max_age_hours cfg field must exist or be added

- Risk: v5.14.8.E assumes ControllerConfig.hpp contains model_max_age_hours; if not, CoreModelZoo_CheckStaleModel fails
- Gate: Pre-code grep ControllerConfig.hpp; if missing, add field with default=0
- Impact if failed: Compilation error or feature silently disabled

## Code-review gates (before shipping)

**CRITICAL gates (block landing):**
1. GATE-NEW-2: Verify FOREACH_STAMP_BOUND_MODEL_CONST row order = current stamp_write_for_model emit order
2. GATE-NEW-1: Verify panel-branch auto-generation in v5.14.8.B (either X-macro or code-gen)

**HIGH gates (should pass before v5.14.8.A review):**
3. GATE-NEW-3: Spot-check 3 ModelStampResult callers (field-access syntax identical)
4. GATE-CRITICAL-5: Verify STAMP_MODEL_CONST_AUTOPOPULATE called at EVERY StampInferenceCfgInputs site

**MEDIUM gates (document before shipping):**
5. GATE-NEW-5: Grep all 6 X-macro sites in FeatureRegistry.hpp, verify 7-param signature
6. GATE-NEW-6: Grep ControllerConfig for model_max_age_hours; add if missing

## Amendment assessment

| Aspect | Rating | Notes |
|---|---|---|
| Architectural soundness | EXCELLENT | Structural fix via X-macro closure of 2 recurring N-site bug classes |
| Pattern precedent | GOOD | Reuses proven FOREACH_STAMP_BOUND_CFG; extends to model-const + failure-modes |
| Implementation readiness | CONDITIONAL | 3 critical gates + 3 medium gates must pass; gates are verification, not architecture |
| Regression risk | LOW | Wire format preservation + caller preservation design sound; risks are execution verification |
| Effort estimate | 22-26h | Upfront investment higher than original plan, but closes entire bug class structurally |

## Final verdict

CONDITIONAL APPROVE — Approve with gates.

The amended plan is ARCHITECTURALLY SUPERIOR to the original (structural fix vs. manual N-site pattern). The 7 original blockers are addressed by defensible design. The 6 new findings are primarily code-review gates, not architectural gaps. With proper gate verification before landing, the amendment closes the parity audit with a GREEN verdict.

Operator framing: "more work upfront, but it solves so many issues going forward" — appropriate for this scope.
