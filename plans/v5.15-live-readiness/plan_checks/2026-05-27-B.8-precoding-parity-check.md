---
type: audit-report
audit: parity-check
audit_tier: HIGH-RISK (pre-coding gate)
established: 2026-05-27
ship: v5.15.5.F.4d.1.B.8 (DRAFT v1.0; pre-coding audit)
plan: subplans/2026-05-27-v5.15.5.F.4d.1.B.8-accounting-cohort-fix.md
HEAD: 45e00de (post-`.B.7` ship; clean tree)
scope: train-serve + backtest-live identity audit scoped to .B.8 accounting cohort fix
status: complete
verdict: GREEN-with-discoveries (no parity-blockers; Phase A enumeration must absorb 1 NEW HIGH UNINDEXED-GLOBAL site discovered during this audit + 1 KEEP-AS-GLOBAL clarification)
findings_count: 6 (0 BLOCKING / 1 HIGH NEW / 1 MED / 2 LOW / 2 INFO)
tags: [parity-check, accounting, fee-floor, class-26, pre-coding-gate, B.8]
sister_specs:
  - wire-format-patterns/wire-format-byte-preservation-discipline.md
  - refactor-patterns/decision-time-data-binding-pattern.md
  - refactor-patterns/cfg-scope-discipline.md
  - meta-disciplines/canonical-sister-extension-discipline.md
---

# /parity-check report — 2026-05-27 — v5.15.5.F.4d.1.B.8 pre-coding gate

## Headline

**Verdict: GREEN-with-discoveries.** No parity-blocking issues for the `.B.8` accounting cohort fix plan. Backtest↔live accounting parity, train↔serve fee-floor parity, HMAC wire-format byte-equivalence, and Class 27 decision-time-binding cohort all PRESERVED by the proposed fixes. NEW regression tests at Phase E will NOT break parity-harness baselines (test isolation verified). MED-1 display-side fix correctly aligns TUI net_pnl with per-core execution path. LOW-1 dead-state deletion mechanically verified UNREAD outside DrainerConstants itself.

**Phase A scope expansion REQUIRED:** One NEW HIGH UNINDEXED-GLOBAL Class 26 site discovered during this audit (`Strategies/private/EmaCross.hpp:143-144` — legacy single_core path; verdict LEGACY-EXEMPT but warrants Phase A documentation). One MED-1 line-139 site discovered as KEEP-AS-GLOBAL headline display (correctly OUTSIDE per-position loop).

**Plan body strength:** Decisions A/B/C/D logically sound; sister-cohort framing accurate; Check 10 IS the right structural-fix shape; effort estimates realistic; H4/H6/H7/H20 invariants UNTOUCHED at all fix sites.

## Summary

| Severity | Count | Notes |
|---|---|---|
| BLOCKING | 0 | No parity-blocking issues |
| HIGH (NEW) | 1 | 1 additional UNINDEXED-GLOBAL site discovered during audit (legacy single_core path; Phase A scope clarification) |
| MED | 1 | ShardedSnapshot.hpp:139 KEEP-AS-GLOBAL clarification needed (headline display, OUTSIDE per-position loop) |
| LOW | 2 | (a) Plan body could cite SlowPath.hpp:63-68 comment explicitly as sister-canonical precedent; (b) Phase A Cohort enum CSV format spec missing classification taxonomy |
| INFO | 2 | (a) HMAC byte-equivalence VERIFIED N/A by code-grep; (b) Stamp body parity surface — `inference_cfg_fee_rate_taker` reads `cfg.fee_rate_taker` GLOBAL at training time (intentional; trainer captures global tier 0; SAFE) |

---

## Findings (per-severity)

### [HIGH-NEW] [PARITY-CONSUMER-SCAN] `Strategies/private/EmaCross.hpp:143-144` — legacy `EmaCross_ExitAdjust` reads `cfg->fee_rate_taker` UNINDEXED

**Cited code (lines 143-144):**
```cpp
FPN<F> fee_rate = !FPN_IsZero(cfg->fee_rate_taker)
    ? cfg->fee_rate_taker : cfg->fee_rate;
```

**Function signature (line 108):**
```cpp
inline void EmaCross_ExitAdjust(Portfolio<F> *portfolio, FPN<F> current_price,
                                 const RollingStats<F> *rolling,
                                 EmaCrossState<F> *state,
                                 const ControllerConfig<F> *cfg)
```

**Scope analysis:** No per-core context (no `core_id` / `slot` / `c` / `i` parameter; takes `Portfolio<F>*` directly — single-core legacy path). Verified caller: `PortfolioController.hpp:906` (legacy single_core engine; sister to PortfolioController.hpp:592 / :1244 which use `ctrl->config.fee_rate_taker` GLOBAL UNINDEXED).

**Verdict:** **LEGACY-EXEMPT per Cat 8 (audit report Section: "## NOT FLAGGED — Legacy single_core")**. Legacy single_core path has NO per-core dimension; global cfg.fee_rate_taker is correct. NOT Class 26.

**Suggested action:** Plan body Phase A scope-lock section (line 195-198 of plan body) explicitly lists this finding as "FALSE-POSITIVE — legacy single_core path" in Phase A enumeration CSV. Sister to Cat 8 LEGACY-KEEP verdict in audit report.

**Cross-ref:**
- Sister site: `CoreFrameworks/PortfolioController.hpp:592,1244,1601,1848` (legacy single_core)
- Audit report `2026-05-27-post-B.7-accounting-audit.md` Cat 8 LEGACY-KEEP verdict
- Plan body "NOT IN scope" table row "Legacy single_core PortfolioController.hpp:592,1244,1601,1848 global reads (legacy architecture; not Class 26)"

---

### [MED] [DISPLAY-PARITY] `CoreFrameworks/ShardedSnapshot.hpp:139` — headline `snap->fee_rate_pct = FPN_ToDouble(cfg->fee_rate)` OUTSIDE per-position loop

**Cited code (line 139):**
```cpp
snap->fee_rate_pct = FPN_ToDouble(cfg->fee_rate) * 100.0;
```

**Scope analysis:** This site sits at line 139, BEFORE the per-position display loop (which starts after line 207). At line 139, `core_id_for_pos` is NOT in scope (yet). `snap->fee_rate_pct` is an ENGINE-WIDE headline field (paired with `snap->max_positions`, `snap->max_drawdown`, etc. at lines 135-138). Same scope category as MED-1 lines 328-331 (Settings panel global-display).

**Verdict:** **KEEP-AS-GLOBAL — headline display field, by design.** Same KEEP-AS-GLOBAL semantic as plan body's existing MED-1 lines 328-331 clarification.

**Suggested action:** Plan body Step B.5 expansion — extend the "KEEP-AS-GLOBAL" inline comment discipline to BOTH line 139 (headline `fee_rate_pct`) AND lines 328-331 (Settings panel). Both are display-only / engine-wide headline fields, NOT per-position drift sites. Either add to the Phase A CSV with classification "DISPLAY-HEADLINE-KEEP-AS-GLOBAL" rationale, OR document inline comment at line 139 same as plan body's existing comment-add at lines 328-331.

**Cross-ref:**
- Plan body Step B.5 (current MED-1 fix scope) — extend to line 139
- Audit report `2026-05-27-post-B.7-accounting-audit.md` MED-1 section (lines 328-331 sister)

---

### [LOW] [PLAN-BODY-CITATION] Plan body Decision A "(a) Consumer-side substitution + Check 10 extension" — could cite SlowPath.hpp:63-68 comment as sister-canonical precedent

**Context:** Plan body Decision A picks option (a) "consumer-side substitution + Check 10 extension" but doesn't cite the sister-canonical precedent at `CoreFrameworks/EngineSharded/SlowPath.hpp:63-68`:

```cpp
// v5.15.5.F.4c.3 WIP2d-1.B.1 — static const fee_rate_taker_d cache DELETED (Class 27
// fn-local variant; froze first-cfg-value globally). fee_rate_taker_for_cf scalar
// param chain DELETED from EventLoop_DrainPostFill / DrainPostFillOneCore signatures —
// OneCore reads per-core fee from o->pre_resolved.fee_rate (Order carries pre-resolved
// value via Order_BindPreResolved at submit time). See decision-time-data-binding-
// pattern.md + RECURRING_BUG_PATTERNS Class 27.
```

The `.F.4c.3 WIP2d-1.B.1` ship CLOSED a different Class 27 sub-shape at this same surface family. The `.B.8` plan body's Decision A could reference this comment block as proof that consumer-side per-core indexing IS the established pattern at this surface family.

**Verdict:** LOW (plan body strength concern only; no execution impact)

**Suggested action:** Plan body Decision A "Auto-pick rationale" section (lines 119-123) — add bullet "Sister-canonical precedent: `.F.4c.3 WIP2d-1.B.1` migrated `oms->fee_rate_*` scalar fields to `o->pre_resolved.fee_rate` per-Order pre-resolution (same surface family; consumer-side per-core indexing IS the established pattern; .B.8 closes the slow-path consumer sites the .F.4c.3 migration missed)."

**Cross-ref:**
- `CoreFrameworks/EngineSharded/SlowPath.hpp:63-68` (sister-canonical comment block)
- `CoreFrameworks/Order.hpp:356-364` (Order_BindPreResolved canonical site)

---

### [LOW] [PHASE-A-CSV-FORMAT] Plan body Phase A CSV classification taxonomy not enumerated

**Context:** Plan body Step A.4 specifies CSV columns "file, line, fn_name, field_name, classification, rationale" with classifications "REAL Class 26 / DISPLAY / BOOT-TIME / LEGACY / FALSE-POSITIVE". Audit-during-this-check surfaced 2 additional cases the taxonomy may not cover cleanly:

1. **DISPLAY-HEADLINE-KEEP-AS-GLOBAL** (engine-wide headline fields like `snap->fee_rate_pct`, `snap->cfg_fee`, Settings panel global view) vs. **DISPLAY-PER-POSITION** (per-position display that should track per-core like `ps->net_pnl` at line 249-250)
2. **PRE-RESOLVED-DECISION-TIME** (Order/event-bound consumer; reads `o->pre_resolved.fee_rate`, NOT cfg.fee_rate_taker; SAFE per Class 27 closure)

**Verdict:** LOW

**Suggested action:** Plan body Step A.4 — split "DISPLAY" classification into 2 sub-categories (HEADLINE vs PER-POSITION) so Phase A CSV doesn't accidentally lump line 139 + line 249 together (they have OPPOSITE correct verdicts). Add "PRE-RESOLVED-DECISION-TIME" classification for completeness.

**Cross-ref:** Plan body Step A.4

---

### [INFO] [HMAC-BYTE-EQUIVALENCE-VERIFIED-N/A] No stamp body / cfg parser / wire-format changes — HMAC chain SAFE

**Verification approach (per Section M claim → evidence chain):**

1. **Stamp body parity surface:** `ML_Headers/StampBoundModelConstRegistry.hpp:294-297` defines `inference_cfg_fee_rate_taker` as a STAMP_BOUND field. Trainer reads `inf->fee_rate_taker` (the `StampInferenceCfgInputs` struct's GLOBAL `fee_rate_taker`). Engine drift-check reads `FPN_ToDouble(cfg.fee_rate_taker)` (GLOBAL). Both sides read the SAME global field; the .B.8 fixes touch CONSUMER sites in slow-path / strategy ratchet / GUI diag — NOT the trainer-stamp emit OR the engine drift-check field. Result: HMAC chain BYTE-EQUIVALENT (no stamp body changes).

2. **Cfg parser:** `CoreFrameworks/ControllerConfig.hpp:2229-2230` parses `fee_rate_taker` into `cfg.fee_rate_taker` GLOBAL (unchanged at .B.8). `CoreFrameworks/CfgFieldRegistry.hpp:675` registry row UNCHANGED at .B.8. Per-core override emission via `core_N_fee_rate_taker=X` flows through existing FOREACH_PER_CORE_CFG_FIELD machinery (auto-flow; no changes proposed). Result: wire-format cfg parser BYTE-EQUIVALENT.

3. **CfgDriftCheckRegistry.hpp:278-281:** drift check reads `FPN_ToDouble(cfg.fee_rate_taker)` GLOBAL for the `INFERENCE_CFG` cohort. This is correctly GLOBAL because the stamp body captures `inf->fee_rate_taker` GLOBAL at training time. Drift check parity preserved (no .B.8 changes to this surface).

**Verdict:** INFO (verified-safe; documenting evidence chain per Section M discipline)

**Cross-ref:**
- `DESIGN_SPECS/wire-format-patterns/wire-format-byte-preservation-discipline.md` Layer 5b
- `ML_Headers/StampBoundModelConstRegistry.hpp:294-297` (stamp body field; UNCHANGED)
- `ML_Headers/CfgDriftCheckRegistry.hpp:278-281` (drift check; UNCHANGED)

---

### [INFO] [TRAIN-SERVE-PARITY-AT-STAMP-BODY] Trainer captures GLOBAL `cfg.fee_rate_taker` (intentional; trainer is single-config per training run)

**Context:** Stamp body's `inference_cfg_fee_rate_taker` reads `inf->fee_rate_taker` (the trainer's GLOBAL cfg value). The drift check at engine load reads `FPN_ToDouble(cfg.fee_rate_taker)` GLOBAL. The per-core `cfg.cores[c].fee_rate_taker` divergence at PRODUCTION TIME does NOT affect train-serve parity at the STAMP-BODY layer because:

1. Trainer doesn't have per-core dimension (single config, single training run, single fee_rate captured)
2. Engine stamp drift-check reads GLOBAL to compare to trainer's GLOBAL — correct apples-to-apples
3. The .B.8 fixes touch DOWNSTREAM consumer sites (ratchet SL, breakeven, fee floor, GUI diag) — NOT the stamp body layer

**Verdict:** INFO (verified-safe; train-serve parity at stamp body layer UNAFFECTED by .B.8 fixes)

**Important nuance for documented-risk PARITY entry at ship close (G.6):** the `.B.8` plan body proposes a NEW DOCUMENTED-RISK PARITY entry for `fee_rate_taker` historical calibration tainted-results advisory. The advisory frame should make clear: train-serve parity at STAMP layer is UNAFFECTED (stamp captures global); the tainted-calibration risk is downstream at strategy ratchet / breakeven / fee floor sites where per-core divergence silently miscalibrated when operator used differing per-core fee_rate_taker values.

**Cross-ref:**
- `ML_Headers/StampBoundModelConstRegistry.hpp:294-297`
- Plan body Step G.6 (DOCUMENTED-RISK PARITY entry; advisory framing)

---

## Section-by-section walk (10-category checklist)

### Section A — Tick consumption parity
**Verdict:** PASS (UNTOUCHED). `.B.8` fixes touch slow-path / strategy ratchet / GUI diag — NOT tick parsing. `Tick<F>` (FPN) ↔ `HistoricalTick` (double) struct surface UNAFFECTED.

### Section B — Feature pipeline parity
**Verdict:** PASS (UNTOUCHED). `.B.8` fixes touch fee-floor compute paths in slow-path / strategy ratchet — NOT `Features_PackAll` / `Regime_ComputeSignals` / `FeatureRegistry`. Feature compute pipeline UNAFFECTED.

### Section C — Label pipeline parity
**Verdict:** PASS (UNTOUCHED). `.B.8` fixes do not touch any `Label_*` fn body. Snapshot tests preserved.

### Section D — Scaler sidecar binding
**Verdict:** PASS (UNTOUCHED). `.B.8` fixes do not touch scaler binding, `scaler_sha256`, `feature_registry_hash`. Sidecar binding UNAFFECTED.

### Section E — Stamp body schema parity
**Verdict:** PASS (verified per HMAC-byte-equivalence INFO finding above). No stamp body field additions / reorderings / format-string changes proposed at `.B.8`. `inference_cfg_fee_rate_taker` GLOBAL semantic UNCHANGED.

### Section F — Cfg parity (inference-affecting fields)
**Verdict:** PASS. `CfgDriftCheckRegistry.hpp:278-281` reads `cfg.fee_rate_taker` GLOBAL — paired correctly with trainer's GLOBAL capture at stamp body. `.B.8` consumer-side fixes at slow-path / strategy ratchet are DOWNSTREAM of stamp-body emit/drift-check and do not affect this surface.

### Section G — Cross-binary handshake
**Verdict:** PASS (UNTOUCHED). `.B.8` fixes do not bump engine_version, model_format_version, or any cross-binary handshake surface.

### Section H — Threading + initialization
**Verdict:** PASS. `.B.8` fix sites are all SLOW-PATH (`EventLoop_TrailingSLRatchetOneCore` / `EventLoop_BreakevenOnProfitOneCore` / `Strategy_WriteRatchetSL` / `EventLoop_RebuildOneCore`) + drainer cycle (LOW-1 DrainerConstants init) + GUI display (MED-1). Hot path UNTOUCHED. `ConfidenceScorer_Init` + boot-time init paths UNAFFECTED.

### Section I — Determinism
**Verdict:** PASS. Replay determinism: per-core fee_rate_taker reads are deterministic (cfg is boot-set immutable per `DrainerConstants.hpp:84` comment); same input bytes → same output bytes. NEW regression tests are NEW test sections in `tests/controller_test.cpp` — they do NOT modify existing test fixtures or parity-harness baselines.

### Section J — Observability surface coverage
**Verdict:** PASS. MED-1 fix corrects `ps->net_pnl` display to track per-core fee_rate — IMPROVES observability surface coverage (currently TUI shows wrong net_pnl on cores with differing fees). No silent-failure modes introduced.

### Section L — Production-caller field-population audit (v5.9.5b)
**Verdict:** PASS. The 4 HIGH fix sites are CONSUMER reads of `cfg.fee_rate_taker`. They don't ADD a new field; they correct UNINDEXED consumer reads to per-core indexed. No new stamp body / serialization fields to populate. The trainer-side stamp body emit at `Backtest_RunFullValidation` / GUI Run Model worker is UNAFFECTED (those populate `inf->fee_rate_taker` GLOBAL, unchanged).

### Section K — Build-warning audit
**Verdict:** PASS (deferred to Phase F.1 `./build.sh all` — mechanical). No warnings expected from consumer-side substitution (`cfg.X` → `cfg.cores[core_id].X` is type-preserving FPN<F> field access).

### Section M — Claim → evidence chain
**Verdict:** PASS for this report. All findings cite file:line + actual code excerpt + verification result. HMAC byte-equivalence claim VERIFIED by reading `StampBoundModelConstRegistry.hpp:294-297` + `CfgDriftCheckRegistry.hpp:278-281` (both UNCHANGED at .B.8).

### Section N — Row-order parity
**Verdict:** N/A (no wire-format row-order changes; all stamp body / cfg registry rows UNCHANGED).

---

## Backtest ↔ Live accounting parity (focused walk per audit prompt #1)

**Audit prompt:** "Backtest ↔ Live accounting parity preserved by the proposed fixes (BacktestSharded shares same EngineCommon_ApplyBnbDiscount + per-core cfg.cores[c] flow as LIVE)"

**Verification:**
1. `Backtest/BacktestSharded.hpp:198+203` calls `EngineCommon_ApplyBnbDiscount(cfg)` — SAME helper as LIVE path (`CoreFrameworks/EngineSharded/Run.hpp:693+698`).
2. `EngineCommon.hpp:154-165` applies BNB factor to `cfg.cores[c].fee_rate_maker` + `cfg.cores[c].fee_rate_taker` for all `c in [0, MAX_EXECUTION_CORES)`.
3. The 4 HIGH consumer sites that .B.8 fixes read `cfg.cores[core_id].fee_rate_taker` after BNB discount applied — IDENTICAL value across LIVE + BACKTEST per cfg.cores[c] symmetry.
4. `Backtest/BacktestEngine.hpp` (legacy `Backtest_Run`) uses SINGLE-CFG PortfolioController flow — separate architecture, no cross-path parity surface at .B.8 fix sites.

**Verdict:** PARITY PRESERVED. .B.8 fixes do not introduce backtest↔live drift.

---

## Train ↔ Serve parity at fee-floor surface (focused walk per audit prompt #2)

**Audit prompt:** "Train ↔ Serve parity at fee-floor surface (per-core fee_rate_taker reads MUST be consistent across LIVE engine + BACKTEST engine + parity_harness)"

**Verification:**
1. **LIVE engine** (post-fix): reads `cfg.cores[core_id].fee_rate_taker` at HIGH-1/2/3/4 consumer sites.
2. **BACKTEST engine** (BacktestSharded; shares slow-path body with LIVE per `EngineCommon_*` consolidation): same `cfg.cores[c]` post-fix — consistent.
3. **parity_harness.cpp** (295 lines; checked): does not reference `fee_rate_taker` directly. parity_harness is a wire-format / determinism harness, not an accounting harness. The .B.8 fixes do NOT touch any surface parity_harness exercises.

**Verdict:** TRAIN-SERVE PARITY PRESERVED. Per-core fee_rate_taker reads will be CONSISTENT post-fix across LIVE + BACKTEST. parity_harness baseline UNTOUCHED.

---

## HMAC wire-format byte-equivalence (focused walk per audit prompt #3)

**Audit prompt:** "HMAC wire-format byte-equivalence (N/A here — no stamp body changes; no cfg parser changes; verify this assumption)"

**Verification (per INFO finding above):** VERIFIED N/A.

1. Stamp body field `inference_cfg_fee_rate_taker` reads `inf->fee_rate_taker` GLOBAL at training time — UNCHANGED.
2. Cfg parser at `ControllerConfig.hpp:2229-2230` reads `fee_rate_taker=X` → `cfg.fee_rate_taker` GLOBAL — UNCHANGED.
3. Engine drift-check at `CfgDriftCheckRegistry.hpp:278-281` reads `cfg.fee_rate_taker` GLOBAL for INFERENCE_CFG comparison — UNCHANGED (correctly global to pair with trainer's global capture).
4. Per-core override emission flows through existing FOREACH_PER_CORE_CFG_FIELD — UNCHANGED.

**Verdict:** HMAC byte-equivalence ASSUMPTION VERIFIED. Stamp body HMAC chain SAFE.

---

## NEW regression tests don't break parity-harness baselines (focused walk per audit prompt #4)

**Audit prompt:** "NEW regression tests at Phase E don't break existing parity-harness baselines"

**Verification:**
1. `tests/parity_harness.cpp` is 295 lines; checked via `grep -c '^'` — UNCHANGED at .B.8.
2. Plan body Phase E adds NEW test section to `tests/controller_test.cpp` (~16-32 NEW assertions) — fully isolated from parity_harness binary.
3. The NEW assertions set up 4-core cfg with differing per-core `fee_rate_taker` values (0.0008 / 0.0010 / 0.0012 / 0.0015) — this is a NEW test fixture, not a modification of existing fixtures.

**Verdict:** Parity-harness baselines PRESERVED. NEW regression tests cannot break parity-harness.

---

## Decision-time data binding (focused walk per audit prompt #5)

**Audit prompt:** "Decision-time data binding pattern (Class 27 closure sister) — verify the proposed fixes don't introduce ANY NEW Class 27 scalar cfg-mirror caches at the consumer sites"

**Verification:**
1. HIGH-1/2/3/4 fixes change `cfg.fee_rate_taker` (UNINDEXED) → `cfg.cores[core_id].fee_rate_taker` (per-core indexed direct read). This is the CORRECT shape per `cfg-scope-discipline.md` — per-core fields read via per-core slot at decision time.
2. The fixes do NOT introduce a scalar cache on any subsystem state struct — they read directly from per-core cfg slot each time the consumer fires.
3. LOW-1 DELETES a vestigial Class 27 sub-instance (DrainerConstants.fee_rate_taker_d) — IMPROVES Class 27 surface, doesn't introduce new instances.

**Verdict:** NO NEW Class 27 instances introduced. Decision-time-data-binding pattern is honored (the per-core slot acts as the binding context; per-core context flows forward via the per-core fn parameter / for-loop iterator, NOT via cached subsystem state).

---

## MED-1 display-side fix doesn't introduce per-core-vs-global drift (focused walk per audit prompt #6)

**Audit prompt:** "MED-1 fix at ShardedSnapshot.hpp:249-250 — verify display-side fix doesn't introduce per-core-vs-global drift between TUI snapshot and actual execution path"

**Verification:**
1. **Pre-fix:** `cfg->fee_rate` GLOBAL at line 249-250 (incorrect; should track per-core).
2. **Post-fix (per plan body):** `cfg->cores[core_id_for_pos].fee_rate_taker` + fallback `cfg->cores[core_id_for_pos].fee_rate` — tracks per-core; aligns with execution path which uses per-core fee_rate via Order pre_resolved (Class 27 closure).
3. **Execution path it aligns with:** `Order.hpp:356-364 Order_BindPreResolved` binds `o->pre_resolved.fee_rate` from `core_cfg.fee_rate_taker` (per-core); fill accounting at `PostFill` reads `o->pre_resolved.fee_rate` per Order. Display now matches execution.

**Verdict:** MED-1 fix CORRECTLY aligns display to per-core execution. NO new display↔execution divergence. Existing divergence (display=global, execution=per-core) is being CLOSED.

**Important MED-1 sister site (lines 328-331):** Plan body correctly KEEPS-AS-GLOBAL. These are the Settings panel display (engine-wide headline view). Sister to ShardedSnapshot.hpp:139 — both are headline display, both KEEP-AS-GLOBAL by design (operator-facing engine summary view).

---

## LOW-1 dead-state deletion doesn't break silent dependency (focused walk per audit prompt #7)

**Audit prompt:** "LOW-1 fix at DrainerConstants.hpp:65,100 — verify dead-field deletion doesn't break any silent dependency (was UNREAD per audit; verify mechanically)"

**Verification (mechanical grep):**

```bash
rg -n 'fee_rate_taker_d' --type-add 'cpp:*.{cpp,hpp,h,cc}' --type=cpp
```

Results (only 5 matches; all within DrainerConstants itself or sister-doc comments):
- `MemHeaders/DrainerConstants.hpp:65` — field DECLARATION (to be deleted)
- `MemHeaders/DrainerConstants.hpp:84` — comment about the field (to be removed/updated)
- `MemHeaders/DrainerConstants.hpp:100` — `DrainerConstants_Init` WRITE (to be deleted)
- `CoreFrameworks/EngineSharded/Run.hpp:80` — `#include` directive comment (mentions field name; cosmetic, can update)
- `CoreFrameworks/EngineSharded/SlowPath.hpp:63` — historical comment about deletion at `.F.4c.3 WIP2d-1.B.1` of a DIFFERENT (fn-local) static cache (NOT a read of this field)

**Verdict:** UNREAD VERIFIED. LOW-1 deletion safe; no silent dependency.

**Sister concern (sizeof static_assert):** Plan body Step B.6 correctly notes `static_assert(sizeof(DrainerConstants) == 24, ...)` needs update. Current layout: 8B (fee_rate_taker_d) + 4B (registered_count) + 4B (drain_count) + 1B (partial_on) + 7B (_pad) = 24B. Post-delete: 4B (registered_count) + 4B (drain_count) + 1B (partial_on) + 7B (_pad) = 16B. Plan body correctly anticipates this.

---

## Cross-cutting verifications

### Hot path UNTOUCHED claim
All 4 HIGH fix sites + MED-1 + LOW-1 are SLOW-PATH / drainer / GUI:
- ControllerEventLoop.hpp:3580+3655 — slow-path SP rebuild (called per-core from EngineCommon slow-path body)
- StrategyLifecycle.hpp:264 — slow-path strategy helper (called from MR/Momentum/EmaCross slow-path ExitAdjust)
- ControllerEventLoop.hpp:3042 — slow-path GUI diag capture (called per-core in RebuildOneCore)
- ShardedSnapshot.hpp:249 — TUI snapshot publication (called from GUI thread; NOT hot path)
- DrainerConstants.hpp:65,100 — drainer cycle init (called once per drainer cycle)

NONE of these are BG_Evaluate / SG_Evaluate / ExecutionCore_Tick / hot path SPSC ring drain. Hot path verification via `tools/calls_graph_diff.sh verify` will be GREEN.

### H4 / H6 / H7 / H20 invariants
- **H4:** All fix sites read `cfg.cores[core_id].fee_rate_taker` (FPN<F>) and convert to double for display/cap math. Display-only float OK; storage stays FPN<F>. PASS.
- **H6:** No cache-line layout changes; per-core cfg cluster alignas(64) UNCHANGED. PASS.
- **H7:** Hot path UNTOUCHED; H7 doesn't apply. PASS by-no-change.
- **H20:** Slow-path / drainer dispatch UNCHANGED at the fix sites; substitution is a single field-access change, not a dispatch change. PASS.

### Effort estimate sanity check (per skill spec)
Plan body estimates ~3-4h focused effort. Sanity-check:
- Phase A enumeration ~30 min — reasonable (grep + classify; 5-10 candidate fields × 4-6 file paths)
- Phase B fixes ~30 min — reasonable (4 HIGH + 1 MED + 1 LOW; single-line substitutions)
- Phase C Check 10 impl ~45 min — reasonable (mirror Check 9 structure; ~50 LOC of Python)
- Phase D catalog amend ~15 min — reasonable
- Phase E regression tests ~30 min — reasonable (mirror .B.7 test pattern)
- Phase F build verify ~30 min — reasonable
- Phase G ship close ~60 min — reasonable
TOTAL: ~3-4h. PASS.

---

## Suggested ship sequence (no changes from plan body)

`.B.8` as standalone post-ship hotfix per plan body Decision B. Plan body sequence is well-formed.

---

## NOT-A-BUG verifications

- **`Strategies/StrategyParameters.hpp:1762-1763`** — reads `core_cfg->fee_rate_taker`. SCOPE: `core_cfg` is `const PerCoreCfg<F>*` (the per-core struct itself, NOT a global cfg). `core_cfg->fee_rate_taker` IS per-core indexed-by-construction. NOT Class 26. VERIFIED-SAFE.
- **`CoreFrameworks/Order.hpp:356-364 Order_BindPreResolved`** — reads `core_cfg.fee_rate_taker` (`PerCoreCfg<F>` param). Per-core by construction at decision time. NOT Class 26. CANONICAL decision-time-binding (Class 27 closure precedent).
- **`CoreFrameworks/ControllerConfig.hpp:1338 Fee_Compute`** — reads `cfg->fee_rate_taker` UNINDEXED. Caller-side discipline per fn comment (line 1325): "Caller-side discipline (CLAUDE.md "Maker/Taker Accuracy" invariant in c7)". Only legacy caller is `PortfolioController.hpp` single_core path (per audit Cat 8 LEGACY-EXEMPT). Sharded path uses `o->pre_resolved.fee_rate` per Class 27 closure. VERIFIED-SAFE (legacy-only consumer).
- **`CoreFrameworks/EngineSharded/Run.hpp:1253`** — `fee_taker_i = FPN_ToDouble(cfg.cores[i].fee_rate_taker)` — CORRECTLY per-core indexed in boot-time TP validation loop. NOT Class 26. VERIFIED-SAFE.
- **`CoreFrameworks/EngineCommon.hpp:159`** — `cfg.cores[c].fee_rate_taker = FPN_Mul(..., bnb_factor)` — CORRECTLY per-core indexed in BnbDiscount apply. VERIFIED-SAFE.

---

## Audit-driven plan body verification

Plan body cited surface verifications (each verified at file:line):
- HIGH-1 cited at ControllerEventLoop.hpp:3605-3606 — CONFIRMED (read lines 3580-3635 directly)
- HIGH-2 cited at ControllerEventLoop.hpp:3670-3671 — CONFIRMED (read lines 3654-3693 directly)
- HIGH-3 cited at StrategyLifecycle.hpp:272-273 — CONFIRMED (read lines 264-287 directly)
- HIGH-4 cited at ControllerEventLoop.hpp:3042-3043 — CONFIRMED (read lines 3020-3070 directly)
- MED-1 cited at ShardedSnapshot.hpp:249-250 — CONFIRMED (read lines 240-275 directly)
- LOW-1 cited at DrainerConstants.hpp:65,100 — CONFIRMED (read full file)

All plan body code-block "BEFORE/AFTER" diffs match actual code at HEAD. Plan body symbol existence (Check 32 B-Plus precursor) PASS for this audit.

---

## Phase A scope expansion items (informational)

Items the Phase A enumeration should explicitly classify:

| Site | File:line | Classification |
|---|---|---|
| `EmaCross_ExitAdjust` legacy single_core | `Strategies/private/EmaCross.hpp:143-144` | LEGACY (single_core; not Class 26) |
| `Strategy_BuildParameters` BUY_BLOCKED fee-floor check | `Strategies/StrategyParameters.hpp:1762-1763` | PRE-RESOLVED (core_cfg = PerCoreCfg<F>; already per-core) |
| `Fee_Compute` helper | `ControllerConfig.hpp:1337-1340` | LEGACY (single_core consumer only; per-core path uses o->pre_resolved.fee_rate) |
| Headline snap field | `ShardedSnapshot.hpp:139` | DISPLAY-HEADLINE-KEEP-AS-GLOBAL (engine-wide summary) |
| Settings panel display | `ShardedSnapshot.hpp:330` | DISPLAY-HEADLINE-KEEP-AS-GLOBAL (per existing plan body MED-1 lines 328-331) |
| `Order_BindPreResolved` | `Order.hpp:356-364` | PRE-RESOLVED-DECISION-TIME (canonical Class 27 closure; sister-precedent) |
| Stamp body emit | `StampBoundModelConstRegistry.hpp:294-297` | TRAIN-SERVE-GLOBAL-INTENTIONAL (trainer captures global; engine drift-check pairs global; correct) |
| CfgDriftCheckRegistry | `CfgDriftCheckRegistry.hpp:278-281` | TRAIN-SERVE-GLOBAL-INTENTIONAL (paired with stamp body) |

---

## Verdict

**GREEN-with-discoveries.** Plan body is well-formed; the 4 HIGH + 1 MED + 1 LOW fixes are correctly scoped; Check 10 IS the right structural-fix shape; Class 26 catalog amendment is correctly framed; M7 escalation is justified; HMAC byte-equivalence is preserved by-construction; train-serve parity at stamp layer is UNAFFECTED.

**No coding-blockers.** Coding may proceed after operator review of:
1. Phase A CSV classification taxonomy expansion (LOW finding: split DISPLAY into HEADLINE vs PER-POSITION; add PRE-RESOLVED-DECISION-TIME class)
2. Plan body Step B.5 extension to ShardedSnapshot.hpp:139 (MED finding: KEEP-AS-GLOBAL inline comment)
3. Decision A "auto-pick rationale" cite of SlowPath.hpp:63-68 sister-canonical precedent (LOW finding)
4. DOCUMENTED-RISK PARITY entry framing (per INFO finding: train-serve at stamp-body layer UNAFFECTED; tainted-calibration risk is downstream consumer surface only)

These are all plan body strength improvements, not parity-blocking issues.

---

**End of /parity-check report.** No PARITY_ISSUES.md auto-write required (no NEW open parity findings; DOCUMENTED-RISK PARITY entry for `fee_rate_taker` historical calibration is plan body's own ship-close G.6 deliverable, not a discovery from this audit).
