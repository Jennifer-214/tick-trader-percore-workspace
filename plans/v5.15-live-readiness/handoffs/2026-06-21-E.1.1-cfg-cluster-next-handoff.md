---
type: handoff
status: superseded
superseded_by: 2026-06-22-E.1.1-cfg-config-compiler-gates-next-handoff.md
ship_tag: v5.15.5.F.4d.1.E.1.1
plan_type: refactor
sprint_end_goal: make the codebase more maintainable for future development (v5.X professionalization) — and converge the per-node-sharded vision (.E series)
ship_end_goal: Core→Node mechanical rename (DONE) + cfg-key CLEAN-BREAK (HARD-REFUSE legacy core_ keys) + single_core LIVE deletion (DONE) — leaf 2 of the FOUNDATION cluster
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-21-E.1.1-rename-next-handoff.md
deletion_scope: minor
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md, plans/v5.15-live-readiness/MASTER.md, plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md, plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md]
coding_status: mid-coding-checkpoint (② LANDED; resume at ③ the cfg cluster)
---

# E.1.1 handoff — resume at ③ the cfg CLEAN-BREAK cluster

**Created:** 2026-06-21 (s4 — the close-session handoff)
**Target ship:** v5.15.5.F.4d.1.E.1.1 — Core→Node rename + cfg-key clean-break + single_core deletion
**Branch:** feat/v5.15-live-readiness
**Rollback anchor:** tag `pre-v5.15.5.F.4d.1.E.1.1` @ engine `b9ce419`
**Plan:** `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md` (SSoT = §"Execution update (2026-06-21)" → the "② LANDED" subsection)
**Sprint MASTER:** `plans/v5.15-live-readiness/MASTER.md` (▶ s3 banner = the ② LANDED state)

**Engine state at handoff write:**
- HEAD: `1da1c1c` (the ② rename commit) — clean working tree
- Rollback tag: `pre-v5.15.5.F.4d.1.E.1.1` @ `b9ce419`
- Tests: **3635 / 0** (verify: `./build.sh test`)
- Determinism net GREEN · H8 conformance clean · calls_graph clean · Check-H 46/46 · per-node integrity clean · both pre-commit hooks green

**Workspace state at handoff write:**
- HEAD: `b89461e`+ (close-out polish tail: handoff write → memory sync → CODE_MAP regen → the ALL-N executor-gap fix → close-session skill-hardening; **none change the ③ pickup state** — the engine anchor `1da1c1c` is what ③ resumes from) — clean working tree
- doc-CI: `check_session_docs.sh` SWEEP CLEAN

---

## ✅ What LANDED this session (the ② mechanical rename — DONE)

The ship's namesake is **committed + green** (engine `1da1c1c` + workspace `c356e03` / `b9e5ab9` / `965aa75`):
- **Content-sub 3633 sites** (cascade matcher: `core_id`→`node_id`, `per_core_*`→`per_node_*`, `CoreContext`→`NodeContext`, `FOREACH_PER_CORE`→`FOREACH_PER_NODE`, `CORE_STATE_FLAG`→`NODE_STATE_FLAG`, `MAX_EXECUTION_CORES`→`MAX_EXECUTION_NODES`, the `cores[]` array, …). **PRESERVE held** (0 corruption): `ExecutionCore` / `CoreFrameworks` / `FoxML_Core` / `cpu_id` / `MULTICORE` untouched.
- **6 engine file git-mv** (history-preserving): CoreModelZoo / CoreCtxInitRegistry / CoreCtxSummaryFieldRegistry / CoreStateFlagRegistry / PerCoreStateFlagsRegistry / CoreLatencyStats → Node*. **1 tool git-mv:** `check_per_core_registry_integrity.py` → `check_per_node_registry_integrity.py`.
- **identifier_ledger.txt `--update`** (CORE_STATE_FLAG→NODE_STATE_FLAG names, **values preserved** — H21-sanctioned; Check-H GREEN).
- **The cfg-key parser now RECOGNIZES `node_`** (`strncmp` prefix `core_`→`node_`, offset 5 unchanged). ③ adds the loud-fail on TOP of this.

**The World-1 compiler caught every cascade-matcher gap** (the red→green loop, 7 rounds — see the NEW memory `feedback_rename_enumerator_is_world2_aid_compiler_is_world1`): the `.cores[`-too-narrow (decl/arrow/bare), apparatus regex-metachar `core_` (`core_\w+`/`"core_"`/`core_<digit>`), `tests/` scope, the parser prefix. The DOC-DATA-FILE consumers (MANUAL_FIELDS_INVENTORY Section-A, TOOLS.md, 6 skill tool-refs, a plan citation) were caught by the build + doc-CI gates.

---

## ▶ RESUME HERE — ③ the cfg CLEAN-BREAK cluster (GUARD-FIRST)

The parser already recognizes `node_`. ③ adds the behavior change (D-219/D-223 HARD-REFUSE — loud-fail, NOT alias/tombstone). **Sequence (guard FIRST):**

1. **C-2 GUI↔engine cfg-key parity guard FIRST** — build `tools/check_gui_engine_cfg_key_parity.py` (the two-hand-mirror root, TECH_DEBT-208) + delete the stale `GUI/SettingsPanel.hpp:669` `confidence_freshness_tau` row (RE-DERIVE the line). Guard before the change so the lockstep is enforced, not hoped.
2. **HARD-REFUSE the unrecognized key** — at the `ControllerConfig.hpp` per-node outer-block **fall-through** (the `strncmp(key,"node_",5)==0` block, was `:2929` region post-content-sub — **RE-DERIVE the exact line**), past the 8 recognized-suffix branches, loud-fail an unrecognized `node_N_*` key + each branch refuses out-of-range idx (closes the Knight silent-revert-to-default class).
3. **Global-key refuse** — `engine_mode=` / `num_execution_cores=` (legacy global keys) → loud-fail.
4. **GUI parser lockstep** — `GUI/SettingsPanel.hpp:922` (the `strncmp(p,"node_",5)` block) mirrors the engine refuse.
5. **Operator (Caramel) migrates** her `backtest.cfg` / `engine_sharded.cfg`: `core_N_*` → `node_N_*` (she does this — operator-config text, clean-break).

Then **④/⑤ V-class** (only asan/ubsan remain — build/determinism/conformance/calls_graph/Check-H already green) → **ship-close** doc sweep = **TWO cohorts, a tool each** (⚠️ ALL-N scout executor-gap catch 2026-06-21): **(a)** per-core CONCEPTUAL reframe via `check_doc_rename_classification.py` (per-core/single_core tokens); **(b)** the **CODE-SYMBOL doc-citation cohort** — `CoreModelZoo`/`CoreContext`/`CoreSlowState`/`PerCoreSnap`/`per_core_*`/`FOREACH_PER_CORE_*`/`.cores[`/`MAX_EXECUTION_CORES` cited as CURRENT across **~38 live DESIGN_SPECS + reference DOCS (CLAUDE_INVARIANTS / CLAUDE_ML_INVARIANTS HOW-TO recipes / ML_TEST_RECIPES / OPERATOR_DEPLOYMENT) + ~40 operational skill refs** — via **`cascade.py rename --docs`** (enumerate → classify → mutate; `check_doc_rename_classification.py` DELIBERATELY EXCLUDES code symbols so it CAN'T do this cohort). **PRESERVE** historical worked-examples (`trace-deps:187-188`, `check-29:19/72`, `parity-check:470/667/674`, `merge-scan:157`). **Already fixed at close:** the FUNCTIONAL `check-17` trigger (`CoreModelZoo`→`NodeModelZoo` + `PerCoreSnap`→`PerNodeSnap` — was inert). **(c)** FEATURE_LOOKUP revise-4-add-2 + the ~535 CPU-vs-shard "cores" comment-prose human-classification [shard→node / CPU-hardware→stay] + ~18 `engine_mode` comments + TD-140 move + version bump + /ship + postmortem.

---

## TaskList state at handoff write (recreate verbatim)

| ID | Status | Subject |
|---|---|---|
| #1 | in_progress | E.1.1 — Core→Node rename + cfg-key clean-break + single_core deletion (② DONE; ③/⑤/close remain) |
| #12 | pending | ③ cfg cluster — HARD-REFUSE + global-key refuse + C-2 GUI parity guard + operator cfg migration |
| #13 | pending | ⑤ V-class — asan/ubsan on the rename (only remaining V-class leg) |
| #14 | pending | E.1.1 ship-close — Phase-5 doc sweep + FEATURE_LOOKUP + cores-prose classification + TD-140 move |
| #2 | pending | E.1.2 — NodeState SoA / Money relayout (successor leaf) |

Recreate via TaskCreate; mark #1 in_progress, the rest pending.

## Per-Step status at handoff write

| Phase | Status | Landed at | Blocked by |
|---|---|---|---|
| single_core LIVE deletion (Phase 1-3) | LANDED | `3eadb53`/`7eacb80`/`2baf0f6`/`b9ce419` | — |
| ① cascade tool (TD-175a) | LANDED | `9cffbb4`/`42e462e` | — |
| ② Core→Node mechanical rename | LANDED | `1da1c1c` (+ workspace `c356e03`) | — |
| ③ cfg CLEAN-BREAK cluster | **NEXT** (ready) | — | — |
| ⑤ V-class asan/ubsan | PENDING | — | ③ |
| ship-close (doc sweep + close) | PENDING | — | ③, ⑤ |

## Arming for the next action (③ the cfg cluster)

The surface is **cfg-flow + per-node + capital-config parsing** (operator-facing config; a wrong refuse = a silent-default capital risk — the exact Knight class D-219 closes). Arm it:
- **Route to:** `/trace-deps` (the `node_N_*` parse→store→consume call-sequence + every cfg-key emit/parse site) + `/dod-audit` (registry/cfg-flow pattern fit) + `cfg-scope-discipline.md` (RECURRING_BUG_PATTERNS Class 25/26/27) + `/decision-check` if the HARD-REFUSE scope needs adjudication. ARM each agent per M8 (docs + toolchain + scout-first; adversarial agent independent on the verdict).
- **Run (not just read):** the new `check_gui_engine_cfg_key_parity.py` (build it FIRST) + `./build.sh test` (the parser tests — v5.9.0c/v5.10.0a.G.6/v5.11.18a/v5.14.9.F.6 are the node_N_* parse tests, all green now) + `run_all_tests.sh --full` (asan/ubsan for ⑤) + regen `./tools/gen_code_map.sh`.
- **Read first:** `DOCS/STRATEGY_AND_CODING_RULES.md` (parse-path rules) + `cfg-scope-discipline.md` + H21 (the clean-break is correct because cfg-KEYS are operator-text, NOT wire/HMAC — D-223) + H22 (per-node purity). The plan §"Locked decisions honored" D-219/D-223 + TD-197 closure (the HARD-REFUSE SUBSUMES the orphan-key class).

## Capture-completeness (this session — nothing lost)

- **Decisions → log:** D-240 (cascade tool design / R1 enumerate-only / R2 check_tools_inventory build.sh-scan / mechanism / rename sequence) is in `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` with paired sentinels (independent review VERIFIED it reached all homes: log + spec + plan + MASTER + handoff + ledger). **No un-logged decision** this session (the rename executed pre-decided D-27/D-239; R1/R2/mechanism/sequence all inside D-240). **D-241** (post-handoff, 2026-06-21) — the OPEN manual-TP/SL-in-decoupled-world decision (A drop / B fox-cli verb) — is logged `STATUS: OPEN` + homed in `.E.2` + the decoupling roadmap + TD-184; it's a `.E.2` decision, not a ③ blocker.
- **Findings → ledger:** TECH_DEBT-175a CLOSED (`tech-debt/open.md`, status closed 2026-06-21; physical move to closed.md batched at ship-close per TD-140 convention). The cascade-matcher narrow-spots are captured-as-methodology (not debt) — the compiler+gates are the proven backstop, documented in the memory + rename-ship-methodology.
- **Memories/skills → indexed:** NEW memory `feedback_rename_enumerator_is_world2_aid_compiler_is_world1` indexed at `MEMORY_EXTENDED.md` (bidir sister-ref to `feedback_capture_and_check_are_model_bounded` added both ways). No new skills.
- **Specs:** `rename-ship-methodology.md` PROMOTED Stage 3→4-cohort (v1.0→1.1; .E.1.1 = its 2nd application) + the DOC-DATA-FILE bucket + the World-1/World-2 lesson. `rename-cascade-enumeration-tooling.md` (v1.2, this session). DESIGN_SPECS README rebuilt.
- **Verified:** doc-CI `check_session_docs.sh` SWEEP CLEAN; independent close-review (Stage 5.5) returned the code sound + 2 blockers FIXED (memory tags + bidir → green; spec committed) + 2 nits fixed.

---

## Paste this prompt into a fresh session to resume

```
I'm resuming v5.15.5.F.4d.1.E.1.1 (Core→Node rename + cfg clean-break) for the v5.15-live-readiness sprint.

Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-21-E.1.1-cfg-cluster-next-handoff.md

This is a fresh context — verify everything against current code; do NOT trust prior-session memory.

② the Core→Node mechanical rename is DONE + committed + green (engine 1da1c1c; 3635/0 + all gates). RESUME at ③ the cfg CLEAN-BREAK cluster (GUARD-FIRST): build the C-2 GUI↔engine parity guard + delete the stale SettingsPanel.hpp:669 row FIRST → then the HARD-REFUSE loud-fail at the ControllerConfig.hpp per-node fall-through (RE-DERIVE the line) + global-key refuse (engine_mode=/num_execution_cores=) + GUI parser lockstep → Caramel migrates her cfg → ⑤ V-class asan/ubsan → ship-close.

The cfg clean-break is the ONLY behavior change in this ship + it's a capital-config surface — fire the pre-coding audit gate (/trace-deps + /dod-audit + cfg-scope-discipline) and CONSULT Caramel on findings before coding (do NOT auto-proceed). Address Caramel as Caramel/she/her; no AskUserQuestion modals; evaluate on robustness+latency+design, not time.
```

## Quick links
- This ship's plan: `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md`
- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- Decision log: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-240)
- Dependency DAG: `plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md`
- NEW memory: `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/feedback_rename_enumerator_is_world2_aid_compiler_is_world1.md`
- Rename recipe: `DESIGN_SPECS/refactor-patterns/rename-ship-methodology.md` (Stage 4) + `rename-cascade-enumeration-tooling.md`
