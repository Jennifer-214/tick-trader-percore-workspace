# A-CLASS REFUTE REPORT — E.1.2.C ML-verification program (plan-level pre-coding gate)

> Saved verbatim at receipt 2026-08-20 (orchestrator write per `feedback_save_agent_reports_verbatim`).

**Target:** `plans/v5.15-live-readiness/subplans/2026-08-20-v5.15.5.F.4d.1.E.1.2.C-ml-verification-program.md` @ engine HEAD `417e534` context (`417e524` + docs commits) + the seven i-class reports in `plans/v5.15-live-readiness/reports/2026-08-20-ml-verification-program/`.
**Method:** default-refuted; every membership claim from uncapped greps over explicitly named roots `CoreFrameworks/ Strategies/ ML_Headers/ Backtest/ GUI/ DataStream/ MemHeaders/ FixedPoint/ tests/ tools/` (Landmine 19 honored); two compiled probes with positive controls (scratchpad `argprobe2.cpp`, control fired; rc captured directly); one python class-sweep (`tail_sweep.py`).

## VERDICT LINE

**PLAN: SOUND-WITH-AMENDMENTS-12.** The program's factual foundation survives adversarial verification in full — I set out to refute H-1 and could not (I re-proved it independently). But leg 0's settled shape is **internally contradictory as an instruction set** (A-1/A-2/A-3), two acceptance oracles are PARTIAL without being marked (M10 table below), and the seven reports collectively missed one live-hazard cascade (A-2), one recorded-decision contradiction (A-3), and one class-sweep (A-4).

## What I conceded (refutes attempted, failed — the plan's spine is real)

- **H-1 (fan-shift) is REAL, triple-verified.** Direct read: fan at `CoreFrameworks/ControllerEventLoop.hpp:2073-2078` passes 10 positional args into OneCore's 12-param signature (`:1627-1654`); arg 9 `exit_bandit_enabled` binds param 9 `confidence_ic_variant` (`:1644`), arg 10 `fee` (double 0.001) binds param 10 `int exit_bandit_enabled` (`:1649`). My own compiled probe (independent of the i-class one) reproduces `ControllerEventLoop.hpp:2078:42: warning: conversion from 'double' to 'int'` with a positive control. OneCore's only production caller is the fan (uncapped grep; `Backtest/BacktestSharded.hpp:777` is a comment).
- **Archaeology framing CONFIRMED (leg-0 gate item):** `f973b5c` (v5.14.1.F) is a REGRESSION, not fan-authored-wrong — its own commit message says *"added confidence_ic_variant param to EventLoop_DrainPostFillOneCore; default 0 preserves back-compat"*: the author reasoned about callers that omit the tail and missed the one caller (the fan) that passes everything after it.
- **Include-cycle REFUTED:** `CoreFrameworks/EngineCommon.hpp:102` already includes `ControllerEventLoop.hpp`; nothing in ControllerEventLoop.hpp includes EngineCommon.hpp; `EngineSharded/SlowPath.hpp` and `ShardedBacktestDriver.hpp` / `Run.hpp` / `BacktestSharded.hpp` all sit above EngineCommon. Relocating the binder into the `EngineCommon_*` family creates **no cycle and no template-instantiation problem**. Backtest sites already include EngineCommon; `Run.hpp` calls the binder directly.
- **Leg ordering has NO hidden circular dependency:** leg 2's drift walk runs at load time (`EngineCommon.hpp:416`, `Run.hpp:1915/:1980`), untouched by leg 0's drainer forwarding; leg 3 is load/train-time. Leg 0's seam test needs NO leg-3 exit models — every gate at `:1985-2001` (READY bit, `exit_predictor_count`, meta, predicted bitmap, positions) is test-fabricatable on a synthetic zoo, as C.3c already does for the leaf. The one circularity is confined to one acceptance clause (A-5).
- **Determinism:** at default cfg the leg-0 fix is byte-inert — OneCore's only `node_cfg` consumers are `:1958-1960` and `:2031`, both resolving to EXP3 when `bandit_algorithm=0`; the fee is consumed only inside the dead-at-default block (`:2014`); ic_variant forwards 0. **No golden pins backtest bandit output** (`tools/check_determinism.sh` = FP-golden/locale/replay-locale/SIMD gates only; `tests/schema_golden/` has no bandit byte-golden). Behavior changes only for non-default cfgs — the intended fix.
- **Plan-body checkable claims verified TRUE at HEAD:** 36-field cohort (30 `CfgFieldRegistry.hpp` rows + 5 `MlCfgFlagRegistry.hpp` + 1 `GateCfgFlagRegistry.hpp:45` — my own naive count of 31 was the error, not the report's); stamp-key ledger = 45 rows (`45→46` correct, "46→47" correctly rejected); ledger 93 ids; `:15582` fixture + MUST-TOUCH warning present verbatim; `backtest.cfg:456` = `bandit_enabled=0` exactly; `engine.cfg` = 304 lines, zero ML keys; `FOREACH_IC_VARIANT` = variant 0 only (`ML_Headers/ICVariantRegistry.hpp:56-61`); D-428 exists (`decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md:2922`); R1's early-return is real code (`Strategies/StrategyParameters.hpp:913`).

## LEG-0 TEST-SITE COUNT (measured, not carried)

**33 test call sites**, not "~40": **30** wrapper calls, all 3-arg `EventLoop_DrainPostFill(&r->state, &r->oms, 0)` (`tests/controller_test.cpp:9101…10777`), + **3** OneCore calls, all 4-arg (`:10738`, `:10779`, `:10834`). Re-derive: `grep -cE "^\s*(tt::)?EventLoop_DrainPostFill(OneCore)?\(" tests/controller_test.cpp`. No multi-line or longer-arg test calls exist (comma-tail grep = 0). Production sites: the fan (internal), `SlowPath.hpp:72`, `ShardedBacktestDriver.hpp:285/:485` = 4. The "~40" figure lives only in the backtest report's option-B row; the plan should import 33 when it pins the shape.

## AMENDMENTS (numbered, severity-tagged)

**A-1 [HIGH — leg-0 shape incoherence; a cold session cannot execute it as written].** The settled shape's three clauses are mutually unsatisfiable in their strong forms: *(i)* "de-default the tail so the next mid-signature insertion is a compile error", *(ii)* the adopted option-B's "keep the 3-4-arg head stable to preserve ~40 test call sites", *(iii)* option C's own pin that `node_cfg` keeps a `nullptr` default for the 3-4-arg test callers. If tail params lose defaults, all 33 test sites fail to compile; if `node_cfg` stays defaulted, the compile-error guarantee holds only for type-crossing insertions (a second pointer param would silently re-map — and note H-1's FIRST half was int→int, invisible to any warning flag). The plan must pin: the exact post-fix signatures of OneCore, the wrapper (does `EventLoop_DrainPostFill` survive at all, or does the binder absorb the fan — if it dies, 30 sites break outright), and the disposition of the 33 sites (mechanical +arg edit vs. retained defaults with the guard burden moved to the CI probe). Recommendation as the safer option: params-struct or fully-de-defaulted signature + one mechanical 33-site sweep — the sites are uniform one-liners; the churn is trivial and buys the real compile-time closure the plan claims.

**A-2 [HIGH — new capital-adjacent hazard leg 0 creates; missed by all seven reports].** Leg 0 forwards `cfg.confidence_ic_variant` into OneCore for the first time (today it reaches only the snapshot display, `ShardedSnapshot.hpp:617`). The registry row is `INT(0, 0, 4)` with tooltip "1+=future" (`CoreFrameworks/CfgFieldRegistry.hpp:689`) — values 1-4 parse clean and don't clamp — while `FOREACH_IC_VARIANT` has only variant 0 and the dispatcher returns **0.0 for out-of-range** (`ML_Headers/ConfidenceScore.hpp:887-896`). Post-fix, an operator setting `confidence_ic_variant=1` (a documented, GUI-rendered key) reproduces exactly the drift-poisoning the plan's headline warns about for the flag: constant-0 IC → floor breach → `auto_kill_on_drift` spurious per-node kill (`ControllerEventLoop.hpp:1884-1897`, `:1916-1926`). Couple leg 0 with a registry-derived clamp (max = `FOREACH_IC_VARIANT_COUNT - 1`, or dispatcher clamp-to-0) — one line, closes the class instead of relocating it.

**A-3 [HIGH — the fee forward contradicts a recorded design decision; the simpler/safer option is in the codebase already].** The `SlowPath.hpp:92-97` comment the plan files as mere stale-comment debt records the WIP2d-1.B.1 **decision**: the `fee_rate_taker_for_cf` param chain was to be DELETED in favor of decision-time per-order fee (Class 27 / `decision-time-data-binding-pattern`). The deletion was never completed — but the substrate exists: `PerNodeCfg<F>` carries `fee_rate_maker`/`fee_rate_taker` (proven compiling at `CoreFrameworks/Order.hpp:469-473`). Since shape C already passes `&cfg.nodes[c]` as `node_cfg`, the counterfactual fee is readable at point of use (`Money_ToDouble(node_cfg->fee_rate_taker)`) — per-node-correct (H22), zero new params, and it COMPLETES the recorded intent. Leg 0's "forward flag/node_cfg/**fee**" instructs the opposite (a global scalar threaded down two hops — the exact mirrored-arg category being removed, and an H22 shape). Pin: the fee param DIES; fee reads from `node_cfg` at `:2014`; the `:92-97` comment then becomes TRUE rather than erased.

**A-4 [MED-HIGH — the class sweep the reports never ran; leg 3 is about to re-seed the class].** Sweep result (heuristic, lower bound): **10 production functions** share the ≥8-param/≥2-defaulted-tail shape — `EventLoop_DrainPostFill` (9/6) · `EventLoop_RebuildAllParameters` (**23/17**, tests call 3-arg) · `NodeModelZoo_TryLoadRole` (**14/7**) · `NodeModelZoo_LoadFromDir` (12/6) · `NodeModelZoo_VerifyExpected` (8/2) · `EnsembleModelZoo_LoadFromCfg` (9/4) · `Regime_ComputeSignals` (19/11) · `ML_BuildParameters` (8/2) · `Strategy_BuildParameters` (12/6) · `ReconciliationLoop_Init` (8/2). The acute one: **leg 3 modifies `TryLoadRole`** — 8 production call sites with heterogeneous tails (single-zoo `:687-708` vs ensemble `:2127-2158`, which already diverge on `cfg_ptr`/secret — folded #6/#7 are literally tail-divergence artifacts of this signature). Pin the rule for leg 3 (params-struct, or append-only + all-8-sites-explicit), and home the remaining 8 functions (TECH_DEBT sweep entry) or explicitly scope the guard claim. Also note: `-Werror=float-conversion` catches ONLY type-crossing shifts — state in the plan that it is a PARTIAL class guard; the signature fix is the closure.

**A-5 [MED — leg-0 acceptance clause is synthetic-only or circular].** "Backtest completion writes all four bandit-state files" cannot hold in a real backtest until leg 3: `EnsembleModelZoo_SaveExitBanditState` self-guards on `MASK_EZOO_EXIT_BANDITS_READY` + `exit_predictor_count >= 2` (`ML_Headers/NodeModelZoo.hpp:2666-2668`; Thompson sibling `:2779`), and READY requires loaded exit models (`:1861-1868`). Re-scope: leg 0 asserts buy-Exp3 + buy-Thompson files (buy-Thompson READY needs only `primary_count>=2`, `:3462-3466`) + saver-calls-exist; the four-file assertion moves to leg 4 or is explicitly a synthetic-zoo test.

**A-6 [MED — leg-2 acceptance is an absence oracle with two structural escape hatches].** "ZERO INFERENCE_CFG drift lines whose stamp carried the key" goes vacuously green on any load shape where the walk or the parse never runs: (a) ensemble loads skip the drift walk (folded #6 — correctly out of scope, but the acceptance doesn't pin the shape); (b) **NEW find**: the legacy path `NodeModelZoo_LoadLegacy` (`ML_Headers/NodeModelZoo.hpp:774-781`) is production-called at `EngineCommon.hpp:327` for `node_N_model_path` cfgs and calls `Model_Load` directly — **no stamp parse at all** → no `sr` → the TryLoadRole-sited walker never touches those handles → the false REFUSE_STRICT noise SURVIVES leg 2 on model_path deployments. The plan must name the legacy-load residual and pin a disposition (walker/verify in LoadLegacy · document-as-intended · skip-unverified-handles in the walk). Also pin: the acceptance run is a SINGLE-ZOO dir load, with the trained-without-feature positive control on the SAME load shape.

**A-7 [MED — plan-body ordering contradiction].** § Legs declares dependency order 0→1→2→3→4; § Bookkeeping adopts "Commit order A(retire)→B(key)→C(role)→D(walker)" — which places three leg-3 commits before leg-2's walker. Both compile in either order, but a plan cannot carry two orderings; a cold session picks one arbitrarily. Pin one.

**A-8 [MED — hot-swap combined story is coherent but UNHOMED; three cross-report findings missing from the 16-item register].** (a) bundle-id keyed on PRIMARY fingerprints (`NodeModelZoo.hpp:2597`, `:2671-2674`): swapping exit models while keeping primaries reloads stale exit bandit/Thompson state against new arms; (b) hot-swap horizons cached from pre-swap (`CoreFrameworks/HotSwap.hpp:97-109`) — exit.json adoption via swap requires the boot horizon set; (c) `Settings_RescanModels` can't select an exit-only dir (`GUI/SettingsPanel.hpp:978-983`). All three become LIVE the moment leg 3 mints real exit models — add to the register. Also reconcile: `EnsembleHotSwap.hpp:99` is tests-only legacy at HEAD (serve report correction) — carry the correction; the legacy duplicate is an H21-family dead-code candidate. Guard placement note: both swap paths converge on `LoadFromCfg→TryLoadRole`, so the leg-3 side-guard covers swaps.

**A-9 [LOW-MED — M10 markings].** Extend the leg-0 seam test to assert ALL re-bound params (as drafted it asserts pulls + ic_variant = 2 of the 4 mis-bound positions; fee and node_cfg forwarding stay un-oracled). Leg-4 arms should echo/assert state-file digest at run start.

**A-10 [LOW — guard feasibility, measured].** Engine production TU (`main.cpp`, `-fsyntax-only -Wfloat-conversion`): **exactly 1** float-conversion warning — the defect itself → `-Werror=float-conversion` is landable on the engine target at zero sweep cost post-fix. Unmeasured: controller_test / GUI / XGBoost TUs — measure in-build before flipping -Werror there. Side-find: the tests TU is uncompilable outside the build dir because g++ realpath-resolves the `tests/` directory symlink and `../FixedPoint` then misses — a directory-symlink sister of folded finding #14; worth the same LANDMINES line.

**A-11 [LOW — default-visible new files].** Leg 0's completion savers make every ensemble backtest write `buy_thompson_state.json` (READY needs only `primary_count>=2`) where today only `bandit_state.json` lands — the accidental-transfer surface (register #9) widens to Thompson state. One line in #9's disposition.

**A-12 [LOW — register #16 has no oracle].** Mixed-output exit arms (binary WILL_PEAK beside 3-class PVS in `exit_predictor[]`) appears in no leg's acceptance; if leg 3's WILL_PEAK default ships, the K=1-output consumer-math check is unowned. Promote to a leg-3 gate item or name the sixth leg.

## M10 ORACLE CLASSIFICATION (per acceptance line)

| Leg | Acceptance clause | Oracle | Hand-review? |
|---|---|---|---|
| 0 | seam test: pulls moved through production binder | **PARTIAL** (2 of 4 re-bound positions asserted) | YES until extended per A-9 |
| 0 | ic_variant un-perturbed | TOTAL (direct assert) | no |
| 0 | backtest writes all four state files | **PARTIAL/circular** (A-5) | YES — re-scope |
| 0 | -Werror/CI probe as class guard | **PARTIAL by construction** (type-crossing only; A-4) | mark in plan |
| 1 | protocol + "signature verified" log | TOTAL for HMAC-load only; noise list default-cfg-conditioned | YES (operator leg — inherently) |
| 2 | ZERO drift lines w/ stamp-carried key | **PARTIAL** (absence oracle; two bypass shapes — A-6) | YES until load-shape pinned |
| 2 | trained-without-feature still fires | TOTAL positive control IF same load shape | no (once pinned) |
| 3 | exit.json+stamp produced; loads; wrong-side REFUSE/WARN; key+tree burned | per-claim TOTAL (burn mechanical-TOTAL); D2-dependent clause now resolved by the D2 verdict | only the D2-dependent clause |
| 4 | SHALT fires → pulls>0/non-uniform → reload | TOTAL for loop-closure existence | no |
| 4 | A/B matrix interpretation | inherently **PARTIAL** (out-of-sample consistency) | YES (make explicit) |

## New finds not in any report (summary)

(1) A-2 ic_variant cfg-range hazard; (2) A-3 PerNodeCfg-fee point-of-use option + the decision-contradiction; (3) A-4 the 10-function defaulted-tail cohort incl. leg-3's own target; (4) A-6 `LoadLegacy` stamp-less bypass (`EngineCommon.hpp:327`); (5) A-10 measured -Werror feasibility + tests-symlink compile blindness; (6) A-11 buy-Thompson file at default; (7) the 33-vs-~40 measured count; (8) A-7 plan-internal ordering contradiction.

**Key files:** `CoreFrameworks/ControllerEventLoop.hpp` · `CoreFrameworks/EngineSharded/SlowPath.hpp` · `CoreFrameworks/EngineCommon.hpp` · `CoreFrameworks/ShardedBacktestDriver.hpp` · `CoreFrameworks/Order.hpp` · `CoreFrameworks/CfgFieldRegistry.hpp` · `CoreFrameworks/HotSwap.hpp` · `ML_Headers/NodeModelZoo.hpp` · `ML_Headers/ConfidenceScore.hpp` · `ML_Headers/ICVariantRegistry.hpp` · `Backtest/BacktestSharded.hpp` · `tests/controller_test.cpp` · `tools/identifier_ledger.txt` · the plan + seven reports under `plans/v5.15-live-readiness/`.

I do not edit and do not auto-proceed; this report is the deliverable for orchestrator triage.
