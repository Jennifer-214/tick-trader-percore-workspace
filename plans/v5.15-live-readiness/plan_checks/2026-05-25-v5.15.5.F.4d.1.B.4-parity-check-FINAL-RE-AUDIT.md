---
type: plan-check
audit_kind: parity-check-FINAL-RE-AUDIT-v1.3
target_ship: v5.15.5.F.4d.1.B.4 (v1.3)
audit_date: 2026-05-25
engine_head: 64e7101
verdict: YELLOW
---

# /parity-check FINAL RE-AUDIT — .B.4 v1.3 — 2026-05-25

## Verdict: YELLOW (Phase B unlock conditional on 1 amendment + 2 acknowledgments)

GREEN-mostly. v1.3 amendments N1-N4 verified at HEAD `64e7101`. All 7 PARITY entries close by-construction. v1.2 amendments still hold. **One NEW MED finding** surfaced (N1 incomplete deletion enumeration — missing the `:607` Regime_Classify call site). Two LOW acknowledgments (Step A.4 ambiguities pre-existing and acknowledged as Phase A2 work).

Phase B unlock = **PROCEED CONDITIONALLY** after applying one v1.4 amendment (N5) at plan body. Conditional items are mechanical — no further re-audit needed if applied.

---

## v1.3 amendment verification

### N1 — PARITY-031 closure (`BACKTEST_REGIME_SAMPLE_CORE = 0` named constant) — **GREEN-WITH-CAVEAT**

- ✅ Named constant approach is internally consistent and future-readable per operator rationale
- ✅ Value `0` correctly preserves pre-`.B.4` `sample_regimes=0` semantic
- ✅ Step C.4.5 deletion enumeration MATCHES actual code: `fc_ctx.regime_state` declared at `:541`, initialized at `:548`, read at `:612` — line numbers CORRECT
- ✅ Documentation block at Decision F text explains the choice; grep-discoverable
- ✅ No competing consumer of `fc_ctx.regime_state` outside the 3 enumerated lines (comprehensive grep confirms)

**NEW MED finding — N5 (consumer enumeration gap):** Plan body N1 deletion enumeration lists `:541-548` (allocation) + `:612` (read) but **MISSES** the `:607` `Regime_Classify(&fc->regime_state, &sig, fc->cfg)` WRITE site.

Post-N1 implementation, `fc_ctx.regime_state` will be deleted but `:607` will reference a deleted member → compile error OR (if the line is also deleted without enumeration) silent loss of the in-hook Regime_Classify call (which is correct semantically since per-core slow-path cycle now classifies, but plan body MUST enumerate the deletion explicitly).

**Action:** Amend Step C.4.5 to also DELETE the `:607` Regime_Classify call. The rationale: post-extract, `state.cores[BACKTEST_REGIME_SAMPLE_CORE].regime_state.current_regime` is already classified by `EngineCommon_SlowPathCycleOneCore` (per Decision A row 4 "+ per-core regime"). Driver hook fires AFTER slow-path body finishes (`ShardedBacktestDriver.hpp:393` "fire the slow-path hook AFTER all driver work finishes"), so the in-hook classify is now redundant + would double-classify if left in. Verification gate: grep `Regime_Classify` in `BacktestSharded.hpp` returns zero sites post-deletion.

Severity MED because: silent loss of feature is a feature-collector regression risk if not enumerated. Mechanical fix.

### N2 — Step C.4 explicit deletion enumeration — **GREEN-WITH-1-ACK**

- ✅ `ShardedBacktestDriver.hpp:378-383` line range MATCHES actual code:
  - `:378` EventLoop_TimeExit
  - `:380` EventLoop_TrailingSLRatchet
  - `:383` EventLoop_BreakevenOnProfit
- ✅ `EngineSharded.hpp:1925-1955` line range MATCHES actual centralized-arch block (gated by `if (cfg.engine_arch != ENGINE_ARCH_PER_CORE_SLOW)` at :1928):
  - `:1932` EventLoop_TimeExit
  - `:1934-1936` EventLoop_TrailingSLRatchet
  - `:1940` EventLoop_BreakevenOnProfit
  - `:1949-1953` EventLoop_CheckWsStaleness (NOT in N2 scope — separate concern; centralized-arch only)
- ✅ Comprehensive grep `TimeExit\|TrailingSLRatchet\|BreakevenOnProfit` codebase-wide returns:
  - 2 production fire sites in centralized-arch (correctly enumerated for deletion)
  - 1 per-core fire site at `EngineSharded.hpp:3242-3256` (per_core_slow arch — KEEPS; folds into SlowPathCycleOneCore)
  - **ZERO** other fire sites that would cause double-fire risk
- ✅ Verification gate has explicit post-delete grep check (good safety net)

**Low-severity ACK A1:** `EngineSharded.hpp:1928` is the gating `if` that opens the `:1928-1954` block. The block at :1928-1954 contains:
- `:1932-1940` — TimeExit/TrailingSL/Breakeven (correctly enumerated)
- `:1949-1953` — `EventLoop_CheckWsStaleness` (centralized-arch only; per_core_slow has its own clock at `:3242`-region per the comment at `:1946-1948`)

If the entire `if (cfg.engine_arch != ENGINE_ARCH_PER_CORE_SLOW) { ... }` block at :1928-1954 is deleted as a whole, CheckWsStaleness goes with it — but is it equivalently handled in per_core_slow arch? Per the comment at :1946-1948: "Centralized arch reads clock once for the gate; per_core_slow path (below) shares clock with the existing sp_last_tick_us update". So yes, per_core_slow has CheckWsStaleness coverage at the per-core slow body.

**No amendment needed:** Plan body Step C.4 says "DELETE — CRITICAL to avoid double-fire" without specifying exactly which sub-lines. Implementer should DELETE the WHOLE `if (cfg.engine_arch != ENGINE_ARCH_PER_CORE_SLOW)` block at :1928-1954, since centralized-arch is being retired entirely by this ship (the per_core_slow arch is the only path remaining post-extract per existing comments + design). Acknowledged as Phase B implementer-level decision. Could be documented as `:1928-1954 (entire centralized-arch gated block; includes CheckWsStaleness — verify per_core_slow already covers via :3242-region)` for explicitness, but plan body's current Step C.4 language is sufficient.

### N3 — Phase A Step A.4 boot order example corrected — **GREEN**

- ✅ Verified at `CoreFrameworks/EngineSharded.hpp` HEAD `64e7101`:
  - `:690` `if (cfg.pay_fees_in_bnb) { ... }` BNB cfg mutation — CORRECT
  - `:742` `EventLoopState_Init(&state, &oms);` — CORRECT
  - `:750` `EventLoopState_ConfigureKillSwitch(&state, ...);` — CORRECT
- ✅ Order: BNB :690 → EventLoopState_Init :742 → ConfigureKillSwitch :750 — VERIFIED

**Low-severity ACK A2 (pre-existing; not introduced by v1.3):** Step A.4 example marks `EventLoopState_Init(&state, &oms)` at :742 as `[→ BootGlobal? OR stays caller — verify]` — this is acknowledged as Phase A Step A.4 work (must resolve before Phase B unlock). Plan body's ambiguity here is acceptable because Step A.4 is itself a verification step intended to fully enumerate. No additional v1.3 work needed — this is the work of Step A.4 itself.

**Low-severity ACK A3 (pre-existing):** Step A.4 example doesn't enumerate the per-core `Regime_Init` loop at `:760-762` (`Regime_Init(&state.cores[i].regime_state, ...)`). This loop semantically belongs in BootPerCore (per-core operation reading per-core cfg `cfg.cores[i].regime_hysteresis`). Same acknowledgment: Step A.4 enumeration will catch this at execution time. Phase B unlock requires Step A.4 actually catches both ambiguities, but plan body's current language permits the catch.

### N4 — 5th helper `EngineCommon_ApplyBnbDiscount` extracted — **GREEN-WITH-NUANCE**

- ✅ 5-helper structure preserves train-serve identity across all 7 PARITY entries (per-helper PARITY closure mapping at Decision A table)
- ✅ Caller sequence correctly ordered: ApplyBnbDiscount (non-const) → BootGlobal (const) → per-core BootPerCore loop (const)
- ✅ BNB mutation happens ONCE before BootGlobal — preserves C1 catastrophic-bug fix
- ✅ Const-cfg BootGlobal preserves HMAC byte preservation (no further cfg mutation post-ApplyBnbDiscount; stamp emit reads stable cfg)
- ✅ Body extracted from `EngineSharded.hpp:690-699` (BNB block) — verified against HEAD
- ⚠️ **Sister-precedent citation `Sharded_ValidatePartialExitCfg` is SHAPE-similar but CONST-NESS-DIFFERENT.** Verified at `CoreFrameworks/ControllerEventLoop.hpp:1140` — sister takes `const ControllerConfig<F>*` (validation only, no mutation). ApplyBnbDiscount takes non-const ref (mutates).
  - The actual unifying pattern is "pre-loop one-shot helpers with explicit names" (which IS the canonical sister rationale per plan body). Const-ness divergence does NOT invalidate sister claim — it just means ApplyBnbDiscount is the **first canonical** for non-const-cfg pre-loop helpers.
  - Plan body Decision A note "ApplyBnbDiscount the only non-const-cfg helper (documented)" already handles this. No action.

**5-helper API concern:** None. 5 helpers vs 4 is operator-decided per future-maintainability. Type-system enforcement (const ref signature) makes future contributors create ApplyXxx siblings for cfg mutations rather than silently extending BootGlobal. Sound choice.

**`BACKTEST_REGIME_SAMPLE_CORE` placement (audit-of-N1 sub-question):** ✅ Correctly placed in `CoreFrameworks/EngineCommon.hpp` (the new header that owns the 5 helpers). Future contributor reading EngineCommon.hpp sees both the helpers + the named constant alongside. Alternative (Limits.hpp / BacktestSharded.hpp) would split related concerns. Placement is correct.

**ApplyBnbDiscount sister-helper-for-future-cfg-mutation framing (premature pattern check):** Plan body Decision A claims "future contributor cannot silently extend BootGlobal with cfg mutations (type system enforces const)". This is correct WHEN a future contributor wants to add cfg mutation. But it doesn't FORCE ApplyXxx pattern — a future contributor could still mutate cfg inline before BootGlobal call (in caller scope). The discipline is convention, not enforcement. NOT premature pattern (well-reasoned via type-system signature) but framing is slightly stronger than reality. Minor doc-language note; no action.

---

## 7 PARITY entries re-verification

| Entry | Closure path | Verified at HEAD | Verdict |
|---|---|---|---|
| **PARITY-026** | Hotfix `6fd0ba3` already closed; 5-helper BootGlobal preserves via single ConfigureKillSwitch call site | `EngineSharded.hpp:749-753` + `BacktestSharded.hpp:217-220` — both present | ✅ CLOSED (hotfix) + preserved by extract |
| **PARITY-027** | `SlowPathCycleOneCore` consolidates UpdateRollingStateOneCore + RebuildOneCore + TimeExitOneCore + TrailingSLRatchetOneCore — closes by-construction | Live has at `:3242-3256` (per_core_slow); backtest gets identical via SlowPathCycleAllCores wrapper | ✅ CLOSES by 5-helper structure |
| **PARITY-028** | `BootPerCore` includes BindCompositeCfg + RollingTurnover_Init | Live has at `:1141` + `:1149`; backtest currently MISSING (only `:408` ConfidenceScorer_Init) — closes by adding to BootPerCore | ✅ CLOSES by 5-helper structure |
| **PARITY-029** | `BootPerCore` includes Strategy_InitPerCore | Live has at `:1165`; backtest currently MISSING — closes by adding to BootPerCore | ✅ CLOSES by 5-helper structure |
| **PARITY-030** | `ApplyBnbDiscount` (NEW v1.3) — single helper for BNB cfg mutation called by BOTH boot paths | Live has at `:690-699`; backtest currently MISSING (no equivalent) — closes by adding ApplyBnbDiscount to both boot paths | ✅ CLOSES by 5-helper structure (N4 strengthens) |
| **PARITY-031** | Named constant `BACKTEST_REGIME_SAMPLE_CORE = 0` + delete `fc_ctx.regime_state` (closes regime-state asymmetry by reading per-core regime directly) | `BacktestSharded.hpp:541-548 + 612` enumerated; **+:607 must also be deleted per N5** | ⚠️ CLOSES IF N5 amendment applied (otherwise stale Regime_Classify call site) |
| **PARITY-032** | NEW; folded into SlowPathCycleOneCore — BreakevenOnProfit per-core via fold-in | Currently only fires at centralized-arch `:1940` (and at backtest driver `:383`); zero per-core fire — closes by adding to SlowPathCycleOneCore | ✅ CLOSES by 5-helper structure (Decision E) |

**All 7 close** post-v1.4 N5 amendment. Without N5, PARITY-031 has residual code (`:607` Regime_Classify call) that compiles against deleted `fc_ctx.regime_state` member — compile error AT IMPLEMENTATION TIME (caught by build) but plan body should pre-enumerate the deletion.

---

## v1.2 amendments still hold

- **C1** (BNB cfg one-shot mutation; catastrophic per-core fix) — ✅ N4 strengthens by extracting to dedicated ApplyBnbDiscount helper; one-shot semantics preserved
- **C2** (PARITY-031 closure via fc_ctx.regime_state delete) — ✅ N1 refines with named constant approach; **N5 amendment finishes the consumer enumeration**
- **C3** (Option D 4-helper structure) — ✅ N4 expands to 5; same shape, additional separation
- **H1** (Per-core boot call sequence enumeration before Phase B) — ✅ Step A.4 unchanged; ACK A2 + A3 noted as Step A.4 work
- **H2** (Static-scope discipline; ~30 statics stay in caller) — ✅ Decision G unchanged; v1.3 doesn't introduce new statics
- **H3** (NEW PARITY-032: BreakevenOnProfit fold-in) — ✅ Decision E + Step B.3 + verification gate cover; per-core fold-in confirmed needed (no current per-core fire at `:3242`-region)
- **H4** (H15 metaregistry CI bookkeeping bundle at Phase D Step D.5) — ✅ Decision unchanged
- **M1-M5** — ✅ All unchanged at v1.3
- **L1-L2** — ✅ L2 N4 EMPHASIZED (`Sharded_ValidatePartialExitCfg` sister precedent now cited prominently per N4)

No v1.2 regressions.

---

## NEW concerns (if any)

### N5 (NEW MED — single conditional amendment for v1.4)

**Finding:** N1 Step C.4.5 enumeration MISSES `Backtest/BacktestSharded.hpp:607` `Regime_Classify(&fc->regime_state, &sig, fc->cfg)` call site, which writes to `fc->regime_state` and becomes a write to deleted state post-N1.

**Severity:** MED (compile-time caught at implementation if missed, but plan body should pre-enumerate per consumer-enumeration discipline)

**Action:** Amend plan body Step C.4.5 to add deletion of `:607` Regime_Classify call with explicit rationale: "post-extract, `state.cores[BACKTEST_REGIME_SAMPLE_CORE].regime_state.current_regime` is already classified by SlowPathCycleOneCore upstream; hook fires AFTER slow-path body per `ShardedBacktestDriver.hpp:393` comment; in-hook classify is now redundant + would double-classify if retained."

**Verification gate addition:** Add to v1.4: "grep `Regime_Classify` in `Backtest/BacktestSharded.hpp` returns zero sites post-deletion."

**Source pattern:** `feedback_enumerate_consumers_before_registry_row_deletion` — applies here. fc_ctx.regime_state has 3 access patterns (init / write / read); plan body N1 only enumerated 2 (init + read). One comprehensive grep would have surfaced all 3.

### Two LOW acknowledgments (NOT amendments; pre-existing Step A.4 work)

- **ACK A2**: Step A.4 boot enumeration must resolve `EventLoopState_Init` → BootGlobal vs caller (currently TBD in plan body — acceptable because Step A.4 IS the resolution step)
- **ACK A3**: Step A.4 boot enumeration must catch the per-core `Regime_Init` loop at `:760-762` (belongs in BootPerCore reading per-core `cfg.cores[i].regime_hysteresis`)

These are Phase A2 work, not plan body amendments. Mention here for verification continuity.

---

## Phase B unlock recommendation: PROCEED CONDITIONALLY

**Conditional on:** Apply N5 amendment to plan body (mechanical addition to Step C.4.5 + verification gate). No further re-audit needed if applied — the amendment is a finite consumer-enumeration completion, not a structural change.

**After N5 applied:** Plan body becomes v1.4. Verdict moves to **GREEN**. Phase B unlock unblocked.

**Without N5:** Phase B can still proceed because compiler catches `:607` reference-to-deleted-member at build time, BUT the consumer-enumeration discipline (per memory `feedback_enumerate_consumers_before_registry_row_deletion`) requires plan-time enumeration. Operator decision: do v1.4 amendment now (1-2 lines plan body edit) vs catch at coding time + amend then.

Recommendation: **APPLY N5 NOW** (1-2 line plan body amendment) → v1.4 → GREEN → Phase B unlock. Cost is trivial; discipline is preserved.

---

**End of FINAL re-audit.** v1.3 amendments substantively close N1-N4 as designed; 1 enumeration gap (N5) surfaces; 2 LOW pre-existing Step A.4 work items acknowledged. After v1.4 N5 amendment, plan body is GREEN for Phase B unlock.
