# Archived: engine-root `claude-skills/` (May-6-2026 era)

Salvaged 2026-06-09 at the A.5 gate consult (synthesis S-15; operator directive:
"sweep, add anything we don't have in the workspace, then delete — all stuff like
that belongs in the private workspace"). Engine-side dir was gitignored, never public.

**HISTORICAL RECORD ONLY — factually stale.** These describe the single-core
PortfolioController ancestor (BuyGate/Strategy_Dispatch switch, strcmp-chain cfg
parser, 24B sign-magnitude FPN, "TICK" snapshot v5, 101-assertion test suite).
Superseded by: CLAUDE.md + DESIGN_PHILOSOPHY (standards/architecture),
/strategy-template + DOCS/CLAUDE_INTEGRATION.md (adding strategies),
DOCS/CLAUDE_ML_INVARIANTS.md + in-process XGBoost MLStrategy (model integration —
note the actual design went in-process, not the IPC sketched here).
Do NOT cite as current; per terminology-evolution discipline these bodies are
preserved un-rewritten.
