# v5.15.5.F.4 — Universal cfg field registry sprint (umbrella)

**Branch:** stay on `feat/v5.15-live-readiness` (current sprint branch; rollback tag pattern: `pre-v5.15.5.F.4<phase>`)
**Predecessor:** v5.15.5.F.3 (registry-bitmap SET discipline; engine commit `f72caef`; workspace `654f464`)
**Successor:** v5.15.5.F.5 (per-core Thompson + bandit override audit; uses .F.4's per-core override emission)
**Status:** design-specs DONE 2026-05-13 (this sprint's .F.4a); implementation starts at .F.4b

---

## Foundation (already done)

Both design specs written and committed to workspace 2026-05-13:

1. `DESIGN_SPECS/universal-cfg-field-registry-pattern.md` — FieldDescriptor + AUTOPOPULATE family + tt:: dispatch + bitmap metadata
2. `DESIGN_SPECS/slow-path-cfg-resolution-cache-pattern.md` — ResolvedCoreCfg + cache-layout discipline + K-state enum cohort + branchy-override rationale

Audit findings (Explore agent 2026-05-13) recorded in handoff doc; 123 missing cfg fields broken into ~15 domain categories.

---

## Sprint sub-ships

| Sub-ship | Description | LOC est | Risk |
|---|---|---|---|
| .F.4a | Design specs (DONE) + cohort audit (DONE) | 0 (specs already written) | low |
| .F.4b | `CfgFieldRegistry.hpp` + tt:: dispatch + KIND_DOUBLE + KIND_DOUBLE_PCT migration (~40 fields) | ~700 LOC | medium |
| .F.4c | KIND_INT + KIND_INT_ENUM + KIND_BOOL migration (~80 fields) | ~400 LOC | medium |
| .F.4d | KIND_STRING + KIND_FILE_PATH migration + GUI metadata features (HIDDEN_BY_DEFAULT / RESTART_REQUIRED / SAFETY_CRITICAL) | ~300 LOC | low |
| .F.4e | ResolvedCoreCfg struct + ResolveCoreCfg + RebuildOneCore migration to read from resolved struct | ~500 LOC | high (slow-path touch) |
| .F.4f | K-state enum cohort pack via item 30 (bandit_algorithm, engine_arch, risk_degradation_curve, barrier_blend_mode, regime_state_current, regime_state_proposed) | ~250 LOC | medium |
| .F.4g | Per-core override AoS-by-core re-layout (cfg struct + parser + save/load) | ~400 LOC | high (cfg touch) |

**Total estimate:** ~2550 LOC net (likely NEGATIVE since registry-driven consolidation deletes ~600 LOC parser + ~1500 LOC panel + ~200 LOC save).

**Sprint sequencing rule:** each sub-ship is independently testable + rollback-anchored. Build all 5 binaries (`./build.sh test gui suite tsan asan`) at each tag. Backtest determinism check at .F.4e and .F.4g (hot+slow-path touch).

---

## Cohort-audit findings (per cfg-flag-eligibility-criteria.md + .F.4a audit)

### Per-core override eligibility (8 new fields graduate to PER_CORE_OK)

Per Explore agent audit + spec compose:

| Field | Domain | Current | Recommended | Justification |
|---|---|---|---|---|
| `bandit_algorithm` | ML | GLOBAL-ONLY | PER_CORE_OK | A/B Thompson vs Exp3-IX across cores |
| `thompson_rng_seed` | ML | GLOBAL-ONLY | PER_CORE_OK | Independent reproducibility per core |
| `momentum_min_tp_margin_pct` | Strategies | GLOBAL-ONLY | PER_CORE_OK | Cohort with momentum_* family (4 fields) |
| `momentum_min_buy_delta_recent` | Strategies | GLOBAL-ONLY | PER_CORE_OK | Cohort |
| `momentum_min_r2` | Strategies | GLOBAL-ONLY | PER_CORE_OK | Cohort |
| `momentum_require_last_win` | Strategies | GLOBAL-ONLY | PER_CORE_OK | Cohort |
| `confidence_hard_block_threshold` | ML | GLOBAL-ONLY | PER_CORE_OK | Per-core aggression vs safety |
| `confidence_ic_floor` | ML | GLOBAL-ONLY | PER_CORE_OK | Pair with confidence_hard_block_threshold |
| `confidence_ic_floor_window` | ML | GLOBAL-ONLY | PER_CORE_OK | Pair with confidence_ic_floor |
| `lazy_rebuild_force_period_us` | Engine | GLOBAL-ONLY | PER_CORE_OK | Per-strategy cadence |
| `lazy_rebuild_price_threshold_pct` | Engine | GLOBAL-ONLY | PER_CORE_OK | Pair with lazy_rebuild_force_period_us |
| `exit_threshold` | Strategies | GLOBAL-ONLY | PER_CORE_OK | Per-core exit model participation |
| `param_max_age_ticks` | Engine | GLOBAL-ONLY | PER_CORE_OK | Per-core staleness tolerance |
| `recovery_delay_secs` | Engine | GLOBAL-ONLY | PER_CORE_OK | Per-strategy recovery window |

**13 fields total** (not 8 as initial estimate; cohort-audit surfaced 5 more).

### Fields staying GLOBAL-ONLY (per cohort-audit)

- Thompson Bayesian priors (`thompson_mu_prior`, `thompson_precision_prior`, `thompson_precision_obs`) — training-time assumptions; must match training.
- Ridge tuning (`ridge_lambda`, `ridge_cost_penalty`, `ridge_min_ic_floor`) — training-time cohort.
- Composite confidence training constants (`confidence_freshness_tau_secs`, `confidence_capacity_*`, `confidence_rmse_baseline`) — training-derived; uniform across cores.
- Architecture / system (`require_mlockall`, `engine_arch`, `trading_mode`, `init_arena_use_hugepages`) — boot-only system gates.
- Operational monitoring (`health_log_*`, `oms_*`, `reconcile_*`) — uniform observability surface.

---

## Latency opportunities surfaced (from cross-spec audit)

Per `slow-path-cfg-resolution-cache-pattern.md` Implementation checklist:

1. **Hot-path cfg drift check audit** (cross-spec verify): confirm no slow-path entry calls drift-check (should be boot+hot-swap only). Investigate during .F.4e.
2. **`__builtin_prefetch` at slow-path entry**: prefetch `per_core_overrides[core_idx]` to L1 before resolution body executes. Hides L2→L1 latency (~10ns/cycle/core). Future-work flag.
3. **AVX-512 batch-load of ResolvedCoreCfg** (item 25): 3 × 64-byte cache lines → 3 uops. Future-work; only worth it if cycle measurement shows resolution is bottleneck.
4. **Delta-cache resolved struct**: if cfg.epoch unchanged + override bits unchanged since last cycle, reuse last cycle's resolved struct entirely. Cost: 1 cycle epoch compare. Future-work.
5. **K-state cohort multi-bit state** (item 30 cohort): 6 fields → uint16_t (2 bytes) vs 24 bytes. ~22 bytes/core × 16 = 352 bytes saved.

Each is logged in TECH_DEBT.md after .F.4e baseline measurements; addressed in subsequent sprints if budget tightens.

---

## Going-forward decisions (deferred to mid-sprint review)

- **Stamp-bound subset:** keep `FOREACH_STAMP_BOUND_CFG` as separate registry (PRE/POST split per item 22 for HMAC ordering) OR consolidate into FOREACH_CFG_FIELD with STAMP_BOUND metadata? Decision at .F.4e: if HMAC byte-ordering must remain stable, keep separate; if not, consolidate.
- **Per-core override storage layout:** SoA (current — per-field arrays) vs AoS-by-core (re-layout in .F.4g)? Re-layout has high blast radius; decision after .F.4e measures cache-miss baseline.
- **CI script for registry/parser parity:** worth writing? Decision after .F.4b: if registry-driven parser eliminates manual sites, the script becomes optional (no alternate parser site to drift). If we keep both for transition, script is critical.

---

## Verification gates (per sub-ship)

Every sub-ship MUST pass before merging:

- [ ] Build all 5 binaries cleanly (`./build.sh test gui suite tsan asan`)
- [ ] `controller_test` passes (≥1822 assertions; new tests for registry parity)
- [ ] Cfg roundtrip: parse cfg.example → save cfg → diff against original (byte-identical)
- [ ] For .F.4e + .F.4g: backtest determinism (same cfg + same ticks → same outputs byte-identical)
- [ ] For .F.4e: slow-path p99 latency measured before + after; verify <100 μs maintained
- [ ] HOT_PATH_CHANGELOG entry written (especially .F.4e — slow-path touch)
- [ ] FEATURE_LOOKUP.md update if operator-visible feature added (cfg fields newly surfacing → yes)
- [ ] PARITY_ISSUES.md scan (no new parity gap surfaces from migration)
- [ ] TECH_DEBT.md update with deferred items (future-work flags from spec)

---

## Cold-pickup orientation

If picking this up fresh:
1. Read `DESIGN_SPECS/universal-cfg-field-registry-pattern.md` (~300 lines)
2. Read `DESIGN_SPECS/slow-path-cfg-resolution-cache-pattern.md` (~300 lines)
3. Read `2026-05-13-v5.15.5.F.4b-foreach-cfg-field-registry-implementation.md` (first sub-ship to code)
4. Read this umbrella for context
5. Start at .F.4b Step 0
