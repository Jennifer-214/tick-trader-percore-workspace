---
type: handoff
status: superseded   # the tail-session close
superseded_by: 2026-08-20-E.1.2.C-tail-3G-shipped-leg4-ready-handoff.md
ship_tag: v5.15.5.F.4d.1.E.1.2.C
plan_type: coding
supersedes: 2026-08-17-E.1.2-D426-closed-guard-armed-next-B3-parse-handle-handoff.md
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-08-17-E.1.2-D426-closed-guard-armed-next-B3-parse-handle-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
decision_ref: D-428
audit_tier: HIGH
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: the train→serve→learn ML loop verified end-to-end (E.1.2.C program, D-428); then the rest of E
coding_status: "E.1.2.C legs 0/2/3 SHIPPED in one session (7 engine commits, all green, pushed). The exit-bandit reward loop is REACHABLE (first time since v5.14.1.F), PARITY-043 is dead through the real consumer, ensemble-only deployments SERVE (R1), the exit tree+cfg key are RETIRED+BURNED, and side=1 now emits CO-LOCATED exit.json with the expected_role guard ENFORCED (D2: O1-only). NEXT = the deferred 3-role table tests + GUI polish batch + 3G-i settings grouping + leg 4 empirical."
---

# E.1.2.C — legs 0/2/3 shipped; the ML loop is structurally alive

## State at write — ANCHORS, not counts

| Repo | HEAD | Window this session | Remote |
|---|---|---|---|
| Engine `feat/v5.15-live-readiness` | `22433b0` | `0d71d4a..22433b0` (7 commits) | push at this close |
| Workspace `main` | at-or-after `c0514da` + this close | `9a9be03..HEAD` | push at this close |

Untracked engine operator files unchanged (`scripts/*.sh` edits + 4 scratch files — keep). `engine.cfg.example` + `backtest.cfg` carry in-place comment fixes (untracked private cfgs, deliberate).

## Re-derive at pickup (every number from these, none from prose)

```bash
./tools/check_session_docs.sh                      # expect SWEEP CLEAN
./build.sh test && ./build/controller_test         # expect BUILD 0 + suite green (was 3760/0 at write — REDERIVE)
./tools/check_determinism.sh                       # expect GREEN
python3 tools/check_identifier_retirement.py       # expect GREEN (93 at write; the burn set gained exit_signal_model_dir)
./tools/calls_graph_diff.sh                        # expect CLEAN
git log 0d71d4a..HEAD --oneline                    # the 7 session commits, each self-describing
```

## The session in one paragraph

The operator directed the E.1.2.C ML-verification program (D-428): 7 i-class investigators mapped the surfaces (reports verbatim at `reports/2026-08-20-ml-verification-program/` — READ THE HEADLINES), 3 a-class refuters closed the gate (D2 → **O1-only, no new wire key**; R1 → **CONFIRMED**, dispatch fix adopted; plan → SOUND-WITH-AMENDMENTS-12, all folded). Then legs 0/2/3 were coded and shipped same-day. Also that morning: the suite cwd-anchor fix (`417e524` — Collect Features was dead from any non-root launch), PARITY-044 minted, FUTURE_ML gained the private MoE/entry-aware-exit entry, and the E.1.2.C plan body (`subplans/2026-08-20-v5.15.5.F.4d.1.E.1.2.C-ml-verification-program.md`) carries the full amended state.

## What landed (commit per leg; messages are the detail SSoT)

1. **`417e524`** — suite cwd-anchor at boot (+ Data-panel resolved-cwd empty-state). TD-287 sisters homed.
2. **`56deb39` leg 0** — the FAN-SHIFT fix: de-defaulted signatures (33-site sweep, compiler-enumerated), exit-flag+fee derive from node_cfg at point of use, ONE shared binder (`EngineCommon_DrainPostFill`) for live+backtest, ic-variant clamp (A-2), 3 backtest savers, seam test C.3d (4 exact-value oracles), `-Werror=float-conversion` armed (caught 4 latent same-class bugs immediately).
3. **`7168953` leg 2** — PARITY-043: the FIFTH walker (`COPY_RESULT_TO_HANDLE_FROM_DERIVED`) + TryLoadRole call + comment-truth sweep + C.3e (production emit→parse→copy→ValidateAgainstCfg; before/after tiers >0 → 0/0).
4. **`6fc5655` leg 3-pre** — R1: ensemble-aware dispatch (`:913` gate + ensemble-precedence role arms + R2 scaler seam) + the 3 blind sisters (swap refusal, live-readiness model check via NEW `node_has_serving_model`, 5 drift gates via NEW `aggregate_ezoo_drift`) + C.3f sentinel pin.
5. **`a81859d` 3-retire** — `exit_signal_model_dir` + `models/exit` tree DELETED; name BURNED in `RETIRED_NAMES` (first proactively-burned cfg name key — the burn REDDED on every surviving advertisement until they died with it, exactly as designed); D-254 size pin 53056→52800 (fingerprint epoch free, s7 precedent); replica test deleted with justification.
6. **`22433b0` 3-role** — `Training_ResolveRole` pure helper (side=1 ⇒ role `exit`, CO-LOCATED); WILL_PEAK side-default; F3 producer-side label gate; F1 FV `req_role` basename derivation; F2 `Model_RoleCheckDecide` pure fn ENFORCING `expected_role` at the chokepoint + NEW `ml_role_mismatch` failure row (count pin 15→16); exit-blindness `role_files[]` cohort ×3.

**Operator can NOW:** train exit models from the suite (side=Exit → co-located `exit.json`, engine auto-discovers under `node_N_model_dir`), and buy-side e2e per `reports/…/e2e-verification-protocol.md` (note its Shape-A `pred:` expectations pre-date the R1 fix — they're achievable NOW; its §2a false-drift ignore-list is MOSTLY MOOT post-leg-2).

## 🔴 NEXT ACTIONS (ordered)

1. **The deferred 3-role table tests** — two PURE fns, trivially table-driven: `Training_ResolveRole` (Backtest/BacktestPanels.hpp; 8 cells) + `Model_RoleCheckDecide` (ML_Headers/NodeModelZoo.hpp; the D2 verdict's 12-cell table, `a-class-D2-guard-fork-verdict.md` § Required output 3 IS the expected-value oracle). Place beside C.3f.
2. **GUI polish batch (operator-reported, all small):** (a) the per-horizon RESULTS TABLE's Horizon column reads the LIVE `ui_horizon_csv` box instead of the run-time snapshot — mislabels rows after edits (screenshot 2026-08-20 evening; find the results-table render in `GUI_Panel_Training` and snapshot horizons into the mh state at click); (b) TP/SL tooltip leads with the unit ("percent: 0.5 = 0.5%" — the operator hit this live); (c) the `[collect-mh]` pos/neg log labels are misleading for 3-class (peak+valley lumped as "pos").
3. **3G-i — the Settings category-GROUPING fix** (duplicate headers; plan § AMENDED has the full shape: one-time category→field index at init + curated order, Global AND per-node tabs, NO registry reorder — row order is wire-load-bearing).
4. **Leg 4 empirical** — NOW UNBLOCKED: train ≥2 exit models (side=Exit, horizons ≥2) → Stage 2 loop-closure (paper: `use_exit_model=1 exit_bandit_enabled=1`, watch `SHALT_EXIT_PREDICTED` → `exit_bandit_state.json` pulls>0 → reload) → Stage 3 A/B per `bandit-loop-state.md` § "What the verification program must run" (both arms `exit_blender_mode=0`; delete state JSONs between fresh arms — they carry across runs BY DESIGN).
5. **Open forks:** D6 (scaler: restore emit in the mh path vs retire the apparatus — couples with deleting the DEAD `train_model_worker_fn` + Save Run block) · D7 (exit-bandit observability: PerNodeSnap mirror vs JSON forensics) · the folded-findings register (plan § register, 16+3 items) triage.
6. **Operator TTY (whenever):** `bless.py` re-bless `tools/goldens/citable-ids.txt` — stale for **7 ids** (D-426, D-427, TECH_DEBT-285/-286, PARITY-042/-043/-044). Needs a real terminal.

## Arming for the next action (M8)

- **Read first:** the plan body (`subplans/2026-08-20-…E.1.2.C-ml-verification-program.md` — the AMENDED section IS current state) + the D2 verdict (its decision table = test oracle for next-action 1) + the 7-commit log.
- **Skills:** `/bug-check` before commit · `/ml-audit` if leg-4 design shifts · `/post-ship-audit` at E.1.2.C close.
- **Tools to RUN:** the re-derive block above; `check_cache_layout.py --strict-new` if any struct changes (none expected in next-actions 1-2).
- **Anti-patterns in play:** Class 51 (the deleted replica is the cautionary tale — new pins must drive REAL fns) · AR-19 (trace to every reader) · the defaulted-mid-tail class (A-4: 9 remaining functions; TECH_DEBT entry owed at close) · Class 57 (zsh `$?` after pipeline — hook-enforced).
- **Invariants:** H21 (the burn is live — any `exit_signal_model_dir` reintroduction in code REDs) · H9/H12 (the D-254 pin moved once already today) · H4 · H22.
- **WILL BITE:** `tests/`+`tools/`+`DOCS/` are workspace SYMLINKS (test/tool edits commit in the WORKSPACE repo; `grep -r` does not follow them — use `-R` or `rg`) · `/tmp` noexec (probes → `~/.cache`) · nullptr into a deduced `<F>*` param fails deduction (use the common_type wrapper pattern or cast) · B-Plus compiles ```cpp fences in plan bodies (use ```text).

## TaskList (recreate at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | pending | 3-role pure-fn table tests (Training_ResolveRole 8 cells + Model_RoleCheckDecide 12 cells; oracle = the D2 verdict's table) |
| #2 | pending | GUI polish batch: results-table horizon snapshot bug + TP/SL unit-first tooltip + [collect-mh] 3-class labels |
| #3 | pending | 3G-i Settings category-grouping fix (plan § AMENDED shape) |
| #4 | pending | Leg 4 empirical (train ≥2 exit models → Stage 2 loop-closure paper → Stage 3 A/B) |
| #5 | pending | D6 scaler fork + dead train_model_worker_fn/Save-Run deletion (coupled) · D7 exit observability |
| #6 | pending | E.1.2.C close-out: FEATURE_LOOKUP leg entries · PARITY-042/-043/-044 status flips · A-4 defaulted-tail TECH_DEBT entry · CHANGELOG · /post-ship-audit |
| #7 | pending (operator TTY) | citable-ids re-bless (7 ids) |

## memories/skills → index

memories/skills → index: NONE this session — no new memory files, no new skills; the session's captures went to the decision log (D-428), the ledgers (PARITY-044, TD-287), FUTURE_ML (private entry), and the plan body. (`project_viewer_is_imgui_decoupled_not_tui` et al. pre-date this session.)

## Capture-completeness (Stage 6.5.2, abbreviated per compaction pressure)

- **Memories/skills → indexed:** none this session — no new memory files, no new skills; captures went to the decision log, ledgers, FUTURE_ML, and the plan body.
- Decision log: D-428 present with STATUS; the D2/R1 resolutions live in the plan AMENDED section + verdict reports (all committed). D-423's false premise corrected inside D-428's entry.
- Ledgers: PARITY-044 minted (its four fix legs now LANDED — flip status at next triage with the mechanism line); TD-287 opened; TD-094/240/198 cross-linked in the plan register. FEATURE_LOOKUP: cwd-anchor entry written; the E.1.2.C legs' entries OWED at ship close (next session).
- Reports: 7 i-class + 3 a-class verdicts verbatim in `reports/2026-08-20-ml-verification-program/`.
- NOT done this session (owed): the pure-fn tests (next-action 1) · FEATURE_LOOKUP leg entries · PARITY-042/-043/-044 status flips · the A-4 defaulted-tail TECH_DEBT entry · CHANGELOG rows · `/post-ship-audit` — all E.1.2.C-close items, none blocking pickup.
