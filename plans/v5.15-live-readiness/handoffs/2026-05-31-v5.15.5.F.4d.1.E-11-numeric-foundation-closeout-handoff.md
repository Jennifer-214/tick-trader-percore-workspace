---
type: handoff
ship_tag: "#11 numeric-foundation-unification (DESIGN PASS COMPLETE; close-out track next; no git tag — monotonic-at-ship per D-88/D-108)"
plan_type: refactor (structural core unification + bug-class closure) + feature edge (decimal money + quantization)
sprint: v5.15-live-readiness
sprint_end_goal: make the codebase more maintainable for future development (MVP → professional infrastructure; the .E sub-sprint is the foundational rework)
ship_end_goal: "Close binary-money-imprecision + parallel-numeric-type + silent-domain-cast + absent-quantization via ONE unified FixedPoint<RADIX,FRAC> (decimal <10,8> money / binary <2,64> features) on a 16B two's-complement backend, reusing the .E.0.1-certified bodies, so .E.1 lands on the FINAL numeric shape."
coding_status: planning — STEP-5 DESIGN PASS COMPLETE (#1-#6 framed + verified); resume at the CLOSE-OUT track (prove-magic → step6 fold → step7 audits → step8 code)
predecessor: v5.15.5.F.4d.1.E.0.6 (.E.0.1 determinism net; SHIPPED 2026-05-31, commit 3f415a0) — THE NET this ship runs under
predecessor_handoff: handoffs/2026-05-31-v5.15.5.F.4d.1.E-11-numeric-foundation-precoding-handoff.md
successor: v5.15.5.F.4d.1.E.1 (Foundation Core→Node rename + multi-exchange) — lands on #11's final numeric shape
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (D-97..D-129; D-125..D-129 = THIS session)
engine_head: 3f415a0 (feat/v5.15-live-readiness; UNCHANGED this session — all design/docs, no engine code)
workspace_head: e454e24 + this session's uncommitted design docs (PENDING /sync-workspace)
pickup: /accept-handoff <this doc>
deletion_scope: none
---

# #11 numeric-foundation — DESIGN-PASS-COMPLETE handoff. Resume at the close-out track (NOT design).

**Created 2026-05-31** (a ~6-hour design session, post the precoding handoff). This session ran the **step-5 new-function design pass** for #11 + a deep numeric-representation exploration, and made a major architectural addition (16B two's-complement). **No engine code** — HEAD unchanged at `3f415a0`. Everything is captured in the decision log (D-125..D-129) + two design sidecars; this handoff is the map so you resume cold at the **close-out track**, not the design.

## 0. One-paragraph orientation
#11 unifies the numeric core into ONE `FixedPoint<RADIX,FRAC>`: **decimal `<10,8>` money** (exact/source-exact) + **binary `<2,64>` features** (today's `FPN<64>` behavior), on a **16B two's-complement backend** (NEW this session — D-125; was sign-magnitude 24B). The step-5 design pass is **COMPLETE** — all six new-function clusters (#1-#6) are framed + Step-0-verified. The decision is SETTLED, the forks are resolved (D-122..124), the representation is decided + verified (D-125..129). **Next = the close-out track: prove the `divmul_pow10` magic → fold into the plan body (step 6) → `/blindspot-scan` + re-fire `/precoding-audit-gate` (step 7) → code, phased P1-P5 (step 8).** This is verification + process + code, NOT more design.

## 1. Engine + workspace state (verify at pickup)
- **Engine HEAD:** `3f415a0` (`feat/v5.15-live-readiness`). UNCHANGED this session. Rollback anchor / predecessor tag: `v5.15.5.F.4d.1.E.0.6` (GPG-signed). #11 has NO pre-tag yet (coding not started).
- **Engine working tree:** no tracked code changes; 18 untracked `DOCS/*.md` are pre-existing (.E.2 doc pre-drafts + workspace-symlinked private docs — NOT #11, not a blocker).
- **Workspace HEAD:** `e454e24` + UNCOMMITTED this session: decision log (D-125-129), the readiness report, the plan body (operator-rewritten top), + 2 NEW sidecars (below). **PENDING `/sync-workspace`** to push off-machine.
- **⚠ Workspace-hygiene to reconcile:** an *undated* `subplans/v5.15.5.F.4d.1.E-new-function-designs.md` is present untracked — NOT the dated `2026-05-31-...E-11-new-function-designs.md` this session wrote. Verify which is canonical / dedupe at `/sync-workspace` (sister to task #9 working-tree-mutation guard).

## 2. What landed THIS session (the design pass + the captures)
**Decisions (decision log D-125..D-129, Session-6 addendum — the SSoT):**
- **D-125** — adopt **16B two's-complement** storage for the unified core (was sign-magnitude 24B). ~2× cache density (4/line, no straddle). **Value-equivalence-netted** (decode old↔new, assert values identical — NOT byte-equivalence; bytes change by design) + fresh byte-determinism re-cert. Value-preserving → re-stamp not retrain. Sequenced as a **compaction step BEFORE the money migration** (binary-first → money-second; never both unanchored, D-73). REVERSIBLE.
- **D-126** — 16B **Step-0 VERIFIED**: transcendentals clean-port (abs-first), no symmetric-range/neg-zero dependency, range robust for BTC (~52-bit max intermediate, 11 bits headroom under 2⁶³). High-price tail (~60 bits @ $1M, 3-bit margin) **resolved by feature-saturation = full-confidence** (correct for a decision input; direct-magnitude features fail-safe, ratio features get graceful saturation). 4 action items: graceful ratio-feature saturation · −2⁶³ Negate/Abs guard · port FP64 backend in lockstep (TWO files) · LargeTradeState base-qty-not-notional audit.
- **D-127** — fees = **TWO numbers**: BOOKED (exact; live=reported-commission D-109, paper=computed) + DECISION (conservative taker-rate buffer for SL/TP/breakeven). F-B fee-asset (base/quote/BNB) **folded in, not deferred** (PL-4 catch).
- **D-128** — rounding = venue-matched where the venue defines it (fees round-up D-109; tick/lot quantize) + **half-even (banker's) internal**, uniform incl. replay. **Closes D-105's open MODE subpart.**
- **D-129** — further width-compression PARKED with rationale (keep the 64.64 buffer NOW): 8B (gated on stats-restructure) / per-instrument (multi-asset future) / sig-fig (further future).

**Design framings (HOW) — sidecar `subplans/2026-05-31-v5.15.5.F.4d.1.E-11-new-function-designs.md`:**
- **#1 type** 16B two's-complement (radix-independent storage; sign in top bit; 16B no-padding) · **#2 shared multiply** abs-in/unsigned-core(HOIST FP64_Mul)/sign-out, fork-only-at-reduce, value-equivalent by construction · **#3 `divmul_pow10`** Granlund-Montgomery reciprocal ÷10⁸ (constexpr M/S, reuses #2's wide product, returns quotient+remainder, fixed-cost ~20cyc vs __udivti3) · **#4 banker's rounding** branchless `q + ((2r>SCALE)|((2r==SCALE)&(q&1)))` · **#5 exact decimal FromString** digit-accumulate, no double/strtod, locale-immune · **#6 FPN_Quantize** venue tick/lot + MIN_NOTIONAL + exact qty wire-string.

**Plan body** (`subplans/2026-05-30-...E-money-numeric-core-foundation.md`): this session added only a STATUS banner (top) flagging the body's stale "open decision" prose + pointing to the decision log as SSoT. The body itself is STILL the original — stale prose ("pending O-1/O-2/O-3", "open decisions"), frontmatter cites D-97..D-106. **The #1-#6 designs + P1-P5 phasing live in the new-fn-designs sidecar + this handoff, NOT in the plan body yet.** Folding them in (+ the stale-prose/frontmatter fixes) is **step-6 work — NOT done.** (A transient editor "Execution Plan" rewrite appeared mid-session but did NOT persist to disk; verified at close.)

## 3. NEXT — the close-out track (Per-Step status)
| Step | Status | Note |
|---|---|---|
| step 5 design pass (#1-#6) | ✅ DONE | this session; sidecar + D-125-129 |
| **Prove the `divmul_pow10` magic** | **NEXT** | (M,S) exact over the dividend range — Granlund-Montgomery bound, ENUMERATE-not-eyeball (`enumerate_set_before_categorical_claim`); + the D-100 external-decimal-oracle gate (confirm-exists-or-build; the #1 ship risk = a deterministic-but-WRONG money core) |
| step 6 — fold into plan body | PENDING | designs + D-125-129 + the precoding-gate findings (C1-C5/H1-H5; synthesis `plan_checks/2026-05-31-money-numeric-core-foundation-fresh-audits-synthesis.md`) + fix the stale frontmatter |
| step 7 — `/blindspot-scan` + re-fire `/precoding-audit-gate` | PENDING | on the AMENDED plan (several CRITICALs reshape the surfaces those scan) |
| step 8 — code, phased P1-P5 | PENDING | **P1** unify binary-only + PROVE byte-identical to the `.E.0.1` golden (the 16B compaction; if not byte-identical, STOP before money) → **P2** decimal type + D-100 gate → **P3** money migration + rounding → **P4** stamp + model retrain (the epoch) → **P5** persistence/recovery round-trip (D-110) |

## 4. TaskList state at handoff write (recreate verbatim)
| ID | Status | Subject |
|---|---|---|
| #1 | completed | `.E.0.1` determinism net ship (tag v5.15.5.F.4d.1.E.0.6) |
| #2 | pending | Codify `.E.0.6` tail (AR-4 wiring + locale-sister reciprocation) |
| #3 | in_progress | **#11 numeric-foundation** (design pass DONE; resume close-out track) |
| #4 | pending | D-98 backlog → `.E`-home mapping |
| #5 | pending | `.E.1` Core→Node rename (blocked by #3 + #8) |
| #6 | in_progress | SWAR parse (POST-#11; surveyed+verified+fold-framed; seed sidecar; gets its own D-94 dedicated pass) |
| #7 | pending | concern-tags |
| #8 | pending | Standing CI orphan-guard (`.E.1`-prep) |
| #9 | pending | Un-attributed working-tree mutation guard |
| #10 | pending | Detached-stdin hang-class remainder |
| #11 | pending | Harvest at `/close-session`: PL-4 severity-adjective tell + F-B instance |
| #12 | pending | Numeric-width design space (16B DECIDED+VERIFIED / 8B-restructure / per-instrument future) |
| #13 | pending | Future numeric-optimization stack coherence (the "use it later" guarantee) |
| #14 | pending | DESIGN_SPEC candidate: divide-by-invariant-constant reciprocal-multiply (wide) + codebase scan |

## 5. Other captures this session (don't lose)
- **Future-optimization stack** (task #13): 16B now → 8B (gated on stats-restructure: mean-centering/Welford) + SoA (D-55) + AVX (gated on 8B — 128-bit is SIMD-hostile; AVX has no integer-divide so the reciprocal-multiply is the SIMD-divide enabler) + per-instrument width + sig-fig. The **value-equivalence net is the reusable re-representation tool** that makes any of these safe later.
- **`divmul_pow10` is a general primitive** (task #14): divide-by-invariant-constant for wide types → DESIGN_SPEC candidate (`swar`... no: `divide-by-constant-reciprocal-multiply`) composing `branchless-math-kernel-pattern`; scan the codebase for beneficiary sites (variance /n², etc.).
- **SWAR parse** (task #6): surveyed (canonical-sister) + verified (WORKS+GENERALIZES) + fold-framed. Seed: `subplans/2026-05-31-v5.15.5.F.4d.1.E-swar-parse-design-notes.md`. POST-#11; targets the post-#11 decimal FromString; one new spec (`swar-digit-extraction-pattern.md`), rest reused. 3 traps pinned (endianness=bswap / fixed-width-8=bail / range-validate-not-nibble). Gets its own D-94 dedicated pass (/hft + latency bench).
- **Meta-harvest** (task #11): PL-4 severity-adjective tell ("minor"/"lower-priority" as the laundering word) + the F-B instance → fold into `meta-anti-pattern-index.md` at `/close-session` (catalog rule: harvest writes, no ad-hoc).

## 6. Paste this into a fresh session to pick up

```
I'm picking up #11 numeric-foundation-unification (decimal money + unified FixedPoint<RADIX,FRAC> on a 16B two's-complement backend) for the v5.15-live-readiness sprint. The step-5 DESIGN PASS is COMPLETE; I'm resuming the CLOSE-OUT track (verification + process + code, NOT design).

First: /accept-handoff plans/v5.15-live-readiness/handoffs/2026-05-31-v5.15.5.F.4d.1.E-11-numeric-foundation-closeout-handoff.md

That loads + verifies everything (git state, decision-log D-97..D-129 status, the two design sidecars, the readiness report, recreates the 14-task TaskList). This is a fresh context — verify everything against current code; do NOT trust prior-session memory.

My immediate next action (per the handoff's Per-Step table): PROVE the divmul_pow10 magic (M,S) exact over the dividend range — Granlund-Montgomery bound, enumerate-not-eyeball — and confirm-or-build the D-100 external decimal oracle (the #1 ship risk). THEN step 6 (fold the #1-#6 designs + D-125-129 + the precoding-gate findings into the plan body, fix the stale frontmatter) → step 7 (/blindspot-scan + re-fire /precoding-audit-gate on the amended plan) → step 8 (code, phased P1-P5; P1 = the 16B binary compaction PROVEN byte-identical to the .E.0.1 golden BEFORE touching money).

Required reading: CLAUDE.md + CLAUDE.local.md + MEMORY.md (auto), the decision log D-97..D-129, the #11 plan body (subplans/2026-05-30-...E-money-numeric-core-foundation.md — read its STATUS banner at top; the body prose is still stale/pre-fold, so the decision log + the new-fn-designs sidecar are the current truth, not the body), the two design sidecars (2026-05-31-...E-11-new-function-designs.md + ...E-swar-parse-design-notes.md), the precoding-gate synthesis (plan_checks/2026-05-31-money-numeric-core-foundation-fresh-audits-synthesis.md), and DOCS/STRATEGY_AND_CODING_RULES.md + the latency-path discipline before any code.

Operator norms: address Caramel as Caramel/she/her; no AskUserQuestion modals (inline options); evaluate on robustness+latency+design, not time; after pre-coding checks, consult before coding (don't auto-proceed); MED/LOW findings get a disposition too.
```

## 7. First action
`/accept-handoff <this doc>` → then **prove the `divmul_pow10` magic + the D-100 oracle** (the close-out track's first step). Consult Caramel before coding (the gate never auto-proceeds).

**End of #11 design-pass-complete handoff. The design is done + captured; resume by verifying + closing out, not designing.**
