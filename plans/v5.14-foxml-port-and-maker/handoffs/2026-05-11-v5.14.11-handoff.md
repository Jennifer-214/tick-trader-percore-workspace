# v5.14.11 handoff prompt — Online correlation matrix updates (Ridge optimization)

**Created:** 2026-05-11
**Target ship:** v5.14.11 — Online correlation matrix updates (Ridge `BuildCorr` O(N²K) → incremental Welford-style outer-product accumulator; ~25-30% Ridge cycle savings; AVX-512 outer-product vectorization opportunity)
**Branch:** `feat/v5.14-foxml-port-and-maker`
**Pre-tag rollback anchor:** `v5.14.10` (umbrella; commit `e0cc877`)
**Plan file:** `plans/v5.14-foxml-port-and-maker/subplans/2026-05-08-v5.14.11-online-corr-update.md`
**Sprint MASTER:** `plans/v5.14-foxml-port-and-maker/MASTER.md`
**Predecessor postmortem:** `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-10-v5.14.10-session-postmortem.md` (v5.14.10 lessons captured 2026-05-11)

---

## Stage 0 note for incoming session — v5.14.10 closure status

v5.14.10 mega-bundle SHIPPED (umbrella commit `e0cc877` 2026-05-10; pushed 2026-05-11; workspace sync `2655102`):
- 7 sub-tags (.0 PerCoreSnap layout / .A ThompsonBandit + Registry / .B engine wiring / .C persistence / .D FULL Bayesian dashboard + FOREACH_CALIB_LOG_COL / .E propagation / .F FOREACH_TRADE_LOG_COL)
- Tests: 2796 → 2882 (+86 new; 0 failed)
- 2 NEW DESIGN_SPECS (`per-snapshot-cluster-layout-pattern.md`, `calibration-log-column-registry.md`)
- TECH_DEBT-010 + -011 substantial + -027 RESOLVED; 5 new deferred entries (-026/-028/-029/-030/-031)
- PARITY-013/014/015 RESOLVED

**v5.14.10 postmortem WRITTEN** at `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-10-v5.14.10-session-postmortem.md` (authored 2026-05-11). Captures 6 design pivots (cfg=2 dual-mode validity via per-arm reward observability; cache-layout convergence to D+E unified cluster; PRNG choice splitmix64-not-mt19937_64; Pattern Variant A/B emergence; source-file-length musing → TECH_DEBT-029; MetricsLog scope-cap design-deferral) + 7 architectural surprises + 8 going-forward rule candidates + lessons synthesis. **Read this BEFORE opening v5.14.11** — captures lessons that may apply to v5.14.11's Ridge optimization work (especially the AVX-512 byte-determinism precedent + Welford pattern reuse + replay-determinism contract discipline).

---

## Paste this prompt into a fresh Claude Code session to start v5.14.11

```
I'm picking up v5.14.11 (Online correlation matrix updates — Ridge optimization)
for the v5.14-foxml-port-and-maker sprint. This is a fresh context window;
do NOT trust any prior-session memory — verify everything against current code.

## Step 0 — orient + verify state (BEFORE planning anything)

1. Run in parallel:
   - `cat /home/caramel/code/FoxML_Trader_v2/Version.hpp` — confirm "5.14.10"
   - `cd /home/caramel/code/FoxML_Trader_v2 && git log --oneline -5` — latest should be `e0cc877 v5.14.10 umbrella` on top of `807a513 v5.14.10.F`, `50d2f24 v5.14.10.E`, `609e2b8 v5.14.10.D`
   - `cd /home/caramel/code/FoxML_Trader_v2 && git tag --sort=-creatordate | head -8` — confirm v5.14.10 + v5.14.10.A-F + pre-tags exist
   - `cd /home/caramel/code/FoxML_Trader_v2 && git status` — clean tree (only context_cont.md untracked expected)

2. Read these in parallel (load context):
   - `CLAUDE.md` (engine repo project instructions)
   - `CLAUDE.local.md` (private overlay; design philosophy + going-forward rules)
   - `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md`
     (auto-memory index; ~17 entries Caramel has built up; entries
     marked feedback_* are load-bearing operator preferences)
   - `plans/v5.14-foxml-port-and-maker/MASTER.md` (sprint master plan; v5.14.10
     entry should reference 7-sub-tag mega-bundle just shipped)
   - `plans/v5.14-foxml-port-and-maker/subplans/2026-05-08-v5.14.11-online-corr-update.md` (THE plan; STALE HEADING — see Step 1)
   - `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-10-v5.14.10-session-postmortem.md` (most recent postmortem; v5.14.10 lessons including 6 design pivots + 7 architectural surprises + 8 going-forward rule candidates)
   - `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-10-v5.14.9-session-postmortem.md` (predecessor postmortem; pattern library codification + audit-driven discipline establishment)

3. Read BEFORE WRITING CODE (per CLAUDE.local.md required reading):
   - `DOCS/STRATEGY_AND_CODING_RULES.md` (11 strict invariants: no malloc, no virtual, no mutex, no sleep_for, no atof/strstr in parsers, cache alignment, branchless, AVX-512-friendly data layouts, SCHED_FIFO/isolcpus deployment)
   - `plans/_cross-cutting/2026-05-06-latency-path-discipline.md` (7 latency-path rules; Ridge cycle is on slow-path budget)
   - `DOCS/LATENCY_OPTIMIZATION_AUDIT.md` (Gemini sweep; Ridge-relevant items in Parts 1, 4, 5; AVX-512 vectorization patterns in Part 11 — fold into .B design before kickoff)

## Step 1 — re-verify plan against current code (HEAD)

Plan was drafted 2026-05-08 with stale numbering. Codebase moved through v5.14.10 in the intervening 3 days (7 sub-tags shipped). Plan's "Pre-existing work audit" REUSE claims + file:line refs may be stale.

ALSO: plan heading reads **"v5.14.12"** (stale; renumbered to v5.14.11 on 2026-05-10 per MASTER section "Numbering note (set 2026-05-10)"). **Predecessor refs say "v5.14.11 close (Phase 4 final)"** — also stale; should be "v5.14.10 close" (Phase 4 final). Fix the heading + predecessor refs.

Run `/readiness plans/v5.14-foxml-port-and-maker/subplans/2026-05-08-v5.14.11-online-corr-update.md` and address every GAP / stale-reference finding. Common stale claims to verify:
- `RidgeBlender_BuildCorr` at `ML_Headers/RidgeBlender.hpp:~270` — verify exact line; cited fn signature still matches
- `RidgeWeights<F>` struct at same file — verify shape (post-v5.14.10 may have added fields for Thompson/cfg=2 telemetry that affect padding)
- `EnsembleModelZoo.reward_ring` PredictionRecord ring — STILL exists (v5.14.10.D didn't touch reward attribution path); verify field shape unchanged
- `ML_BuildParameters` dispatch site at `Strategies/StrategyParameters.hpp` — line refs likely shifted by ~80 lines after v5.14.10.B's bandit dispatch wiring + v5.14.10.B's hysteresis-skip + Ridge override mutual-exclusion check
- `MAX_RIDGE_MODELS` constant — verify it's still defined + value (probably 8 = ENSEMBLE_HORIZON_MAX); if changed, struct sizing needs update
- `cfg.ridge_within_horizon` slow-path-gate (cached as `MASK_RIDGE_WITHIN_ACTIVE`) — interaction with new `cfg.ridge_online_corr` — does the new flag also need a slow-path-gate predicate? See Step 2 below.
- `cfg.bandit_algorithm != 0` Thompson check now mutually-excludes Ridge override (added in v5.14.10.B at `StrategyParameters.hpp:~915-920`). The Ridge call site has changed shape — re-read before swapping the BuildCorr call.

## Step 2 — pre-coding audit gate (multi-skill parallel — QUALIFIES)

Per `DESIGN_SPECS/audit-methodologies/audit-driven-pre-coding-gate.md`, this ship FIRES the gate:
- Adds 5+ new entries (~3 fields in RidgeOnlineState struct + 2 fns + 1 cfg = 6+ entries) ✓
- Picks up work from previous (compacted) session (plan from 2026-05-08; codebase moved 3 days; v5.14.10 just shipped 7 sub-tags) ✓

Spawn these audits IN PARALLEL via Agent tool with general-purpose subagents:

1. `/parity-check` — focus: replay-determinism (Welford incremental updates must produce same correlation matrix as full BuildCorr within 1e-9 tolerance; identical prediction sequence + identical cfg → identical output bytewise within tolerance); AVX-512 vectorization byte-equivalence (mirror v5.11.7 Bandit_GetProbabilities pattern: sum reductions stay scalar; `_mm512_div_pd` not mul-by-reciprocal)
2. `/trace-deps` — focus: plan file:line claims (RidgeBlender.hpp ~270; ML_BuildParameters dispatch shifted post-v5.14.10.B); fn signature changes; cfg.ridge_online_corr eligibility check
3. `/readiness` (full 28-check pass) — cold-pickup completeness; new cfg field eligibility (per cfg-flag-eligibility-criteria.md — `ridge_online_corr` is BOOLEAN → ELIGIBLE for FOREACH_ML_CFG_FLAG bitmap migration; verify); Version.hpp bump discipline; mirror-fn audit (RidgeBlender_BuildCorr ↔ RidgeBlender_FinalizeCorr shape symmetry)
4. `/merge-scan` — focus: reuse of Welford pattern from `RollingStats.hpp:83-87` (true Welford O(1) update; sister technique); reuse of v5.11.7 Bandit_GetProbabilities AVX-512 vectorization pattern + bytewise-determinism comments
5. `/dod-audit` — focus: cfg-flag-eligibility-criteria (cfg.ridge_online_corr is bool; should it land in FOREACH_ML_CFG_FLAG?); slow-path-gate-registry-pattern (should ridge_online_corr have a cached `MASK_RIDGE_ONLINE_CORR_ACTIVE` predicate alongside MASK_RIDGE_WITHIN_ACTIVE?); structural-fix-preferred (1k-cycle periodic recompute reset is a code-smell — better to track Welford numerical stability or use Kahan summation?)

After all reports return, synthesize convergent findings to:
`plans/plan_checks/2026-05-XX-v5.14.11-fresh-audits-synthesis.md`

THEN consult Caramel before coding. Per CLAUDE.local.md `feedback_consult_on_audit_findings`: present findings + list potential fixes + iterate with Caramel. Do NOT auto-proceed even if findings look clearly addressable. Smaller-scope recommendations have been wrong 3/3 times in v5.14 sprint vs Caramel's "do it right now" instinct.

## Step 3 — design check against pattern library

Required reading (DESIGN_SPECS catalog):
- `DESIGN_SPECS/README.md` (full 19-pattern catalog + "I need to..." quick discovery; 2 NEW from v5.14.10: per-snapshot-cluster-layout-pattern + calibration-log-column-registry)

Pattern-specific reading + integration check:

- **`DESIGN_SPECS/refactor-patterns/cfg-flag-eligibility-criteria.md`** (STRONG MATCH — `cfg.ridge_online_corr` is boolean)
  Plan adds 1 new cfg field (`cfg.ridge_online_corr`, INT 0/1). Per the 5-criteria framework:
  - Boot-frozen ✓ (operator opt-in via engine.cfg)
  - Engine-wide ✓ (no per-core variant proposed)
  - Hot-path-tolerant ✓ (slow-path read; ~1-2ns BITMAP_IS_SET acceptable)
  - No compile-time elision benefit ✓ (operator-toggleable runtime flag)
  - Domain-coherent ✓ (ML domain — Ridge optimization)
  → MIGRATE to `FOREACH_ML_CFG_FLAG`. Pattern matches `cfg.ridge_within_horizon` precedent (also boolean ridge gate; though that one stayed direct cfg field for unclear historical reasons — verify if migration is appropriate or if there's a reason to stay direct).

- **`DESIGN_SPECS/framework-patterns/slow-path-gate-registry-pattern.md`** (STRONG MATCH — sister to existing MASK_RIDGE_WITHIN_ACTIVE)
  Plan's wiring at `Strategies/StrategyParameters.hpp` reads `cfg.ridge_online_corr` per-cycle. Should be cached as `MASK_RIDGE_ONLINE_CORR_ACTIVE` in `gate_state->flags` mirroring `MASK_RIDGE_WITHIN_ACTIVE` (v5.14.0) + `MASK_THOMPSON_ACTIVE` (v5.14.10.B). 1 new entry in `FOREACH_SLOW_PATH_GATE` at `CoreFrameworks/SlowPathGateRegistry.hpp:69-100` (currently 9 entries; 7 bits headroom in uint16; safely fits).

- **`DESIGN_SPECS/framework-patterns/registry-tuple-as-single-source-of-truth.md`** (MEDIUM MATCH if cfg-flag-bitmap migration chosen)
  When new cfg-flag lands in `FOREACH_ML_CFG_FLAG` (Option D 5-col tuple from v5.14.9.F.5), it auto-flows to: cfg parser + GUI field + per-core override + stamp-binding (via STAMP_CFG_AUTOPOPULATE) + docs.

- **`DESIGN_SPECS/meta-disciplines/structural-fix-preferred-decision-framework.md`** (MEDIUM MATCH — periodic full-recompute reset)
  Plan's "1k-cycle periodic recompute reset" feels like an admission that Welford accumulation drifts. Is there a better numerical-stability technique (Kahan summation; compensated summation) that eliminates the periodic reset? Consider during .A design pass.

Soft/partial matches:

- **`DESIGN_SPECS/wire-format-patterns/wire-format-byte-preservation-discipline.md`** — N/A (no stamp body; no persistence file; v5.14.11 is runtime-only state)
- **`DESIGN_SPECS/framework-patterns/curve-registry-pattern.md`** — N/A (only 1 mode swap: cfg=0 full vs cfg=1 incremental; not a 3+ named-mode dispatch)
- **`DESIGN_SPECS/data-disciplines/per-snapshot-cluster-layout-pattern.md`** (NEW v5.14.10.0) — RidgeOnlineState lives ON ezoo (slow-path-only state; no cross-thread snapshot); pattern doesn't apply directly. BUT: if any of the new state fields ever surface to PerCoreSnap for diagnostic display, follow the pattern.

Don't write code until pattern docs are read + integration plan articulated in the synthesis doc.

## Step 4 — design philosophy reminders (load-bearing rules from CLAUDE.local.md + memory)

- **Defer is last-ditch, never effort-avoidance.** Implement properly the first time. Smaller-scope recommendations have been wrong 3/3 times in v5.14 sprint vs Caramel's "do it right now" instinct.
- **Structural fix > direct patch for recurring bug classes.** If the periodic-reset hack indicates Welford drift is a CLASS issue, prefer Kahan summation or numerically-stable equivalent over the reset hack.
- **No MVP for plumbing/refactor work.** This is plumbing-heavy (math kernel + dispatch + cfg field). Ship the full design — slow-path-gate predicate + cfg-flag-eligible bitmap migration if appropriate.
- **Boundary-stable refactors preferred over wide cascades.** RidgeOnlineState is a NEW struct extending RidgeWeights or sitting alongside on ezoo; doesn't modify existing public API. Cascade-free.
- **Hot path UNTOUCHED.** This ship's perf optimization is on slow path (Ridge cycle ~3µs of 100µs budget); savings ~700-900ns. Hot path target ≤500ns p99 unaffected.
- **Branchless mask compute > switch on enum on slow path** (CLAUDE.md item 18). Only 2 modes (full vs incremental); 1 cfg-gated branch is fine.
- **Reuse-audit before adding new code** (CLAUDE.md item 16). Welford pattern at `RollingStats.hpp:83-87` already exists; reuse the math approach (don't reinvent). AVX-512 vectorization pattern at v5.11.7 Bandit_GetProbabilities — reuse the `_mm512_*_pd` discipline (no `_mm512_reduce_add_pd`; left-to-right scalar reductions for byte-determinism).
- **Latency-additions get tracked** (CLAUDE.md item 17). Add HOT_PATH_CHANGELOG entry with cost estimate (savings: ~700-900ns/cycle Ridge when cfg=1; default cfg=0 unchanged). Branchless analysis. AVX-512 byte-determinism notes.
- **Replay determinism is sacred.** Welford incremental + scalar full BuildCorr must produce IDENTICAL correlation matrix within tight tolerance (1e-9) over K=64 records. AVX-512 path must be BYTEWISE-IDENTICAL to scalar path on test harness (not just within tolerance — cross-binary determinism requires bit-for-bit). Mirror v5.11.7 Bandit_GetProbabilities AVX-512 byte-determinism contract.
- **Bump Version.hpp on every ship.** Each `vX.Y.Z` tag MUST include a Version.hpp bump in the same commit. Sub-tags (.A/.B/.C) each bump (5.14.10 → 5.14.11.A → .B → .C → 5.14.11 umbrella).
- **Don't use AskUserQuestion modal boxes.** Present options inline as text. Caramel wants full conversation scrollable.
- **Evaluate options on robustness + latency + design philosophy, NOT time.** Time is essentially never the deciding factor.
- **Address Caramel as Caramel / she / her** in conversation. Persisted docs may keep "operator" for general-audience framing.
- **Avoid substring `replace_all` on member-access patterns.** Two distinct mangling shapes (Shape A chained-prefix, Shape B variable-name-suffix) caught in v5.14.9.F. Use targeted Edit for member-access refactors.
- **Suggest mid-sprint audit when downstream sub-ships impact new surface.** Wait for greenlight; don't auto-trigger. Confirmed v5.14.9.F.2 + v5.14.10 amendment-re-audit HIGH-RISK pattern.

From auto-memory MEMORY.md (operator-validated):
- Prefer ripgrep over grep (`feedback_prefer_ripgrep`).
- Compaction degrades — treat handoff prompts as HINTS; re-verify against code (`feedback_compaction_degrades_treat_handoffs_as_hints`).
- Overengineering boundary: pick harder choice when future work much easier (`feedback_overengineering_boundary_when_future_easier`).
- Rename plans when ship order diverges from numbering (this plan's "v5.14.12" → "v5.14.11" rename qualifies + predecessor reference fix).
- After pre-coding checks, ALWAYS consult before coding. Do NOT auto-proceed.
- Don't ship MVP for plumbing/refactor work (`feedback_no_mvp_for_plumbing_only_for_unknown_unknowns`).
- Don't use AskUserQuestion modal boxes (`feedback_no_question_boxes`).

## Step 5 — TECH_DEBT items in surface area (per Stage 4 of /handoff)

Run `/readiness` Check 25 (TECH_DEBT scan) at synthesis time. Likely-overlapping entries:

- **TECH_DEBT-021** (OPEN) — Post-paper-test profiling: domain bitmap collapse OR further split decisions. v5.14.11 is an OPTIMIZATION ship — its measured impact (~700-900ns Ridge savings) is part of the post-paper-test profile data that informs the v5.14.9.F-.F.6 collapse-or-further-split decision. **Recommend: not blocking for v5.14.11; mention in CHANGELOG that v5.14.11 perf data should feed TECH_DEBT-021 decision.**

- **TECH_DEBT-029** (OPEN) — Source file length reduction. `ML_Headers/RidgeBlender.hpp` is currently small (~340 LOC); v5.14.11 adds ~300-400 LOC (RidgeOnlineState + UpdateOnline + FinalizeCorr + AVX-512 path + tests). Resulting file size ~700 LOC; well within the 1500-line threshold from the entry. **Recommend: not triggered by this ship.**

- **TECH_DEBT-026** (OPEN) — Per-core override of `bandit_algorithm`. Not directly related to v5.14.11; mention only if cfg.ridge_online_corr decision opens up a similar per-core override question.

- **TECH_DEBT-022** (OPEN) — Engine.cfg parser perfect-hash. v5.14.11 adds 1 cfg field; marginal contribution to the parser. **Recommend: not triggered.**

If `cfg.ridge_online_corr` migrates to FOREACH_ML_CFG_FLAG (per Step 3 cfg-flag-eligibility check), no NEW TECH_DEBT entry needed (ML_CFG_FLAG already proven pattern from v5.14.9.F.2).

## Step 6 — operator collaboration norms

- Address Caramel as Caramel / she / her. Not "operator" in conversation.
- Don't use AskUserQuestion modal boxes. Present options inline as text.
- Evaluate options on robustness + latency + design philosophy, NOT time.
- When pre-coding checks complete, present findings + fixes + iterate BEFORE coding. Do NOT auto-proceed.
- Suggest mid-sprint audit when downstream sub-ships impact new surface (going-forward rule from v5.14.9). Wait for greenlight.
- Auto-write TECH_DEBT entries for any deferred items (CLAUDE.local.md auto-write contract).
- Auto-write PARITY_ISSUES entries for any new parity findings (CLAUDE.local.md auto-write contract).
- Avoid `replace_all` on member-access patterns (Shape A + B mangling lesson from v5.14.9.F).

## Step 7 — filesystem conventions

- Workspace path: `/home/caramel/code/tick-trader-percore-workspace`
- Engine repo: `/home/caramel/code/FoxML_Trader_v2`
- Plans symlinked: engine `plans/` → workspace `plans/` (DIR symlink; transparent to internal moves)
- Sprint dirs: `workspace/plans/<sprint-dir>/{MASTER.md, subplans/, plan_checks/, postmortems/, handoffs/}`
- This sprint: `workspace/plans/v5.14-foxml-port-and-maker/`
- DESIGN_SPECS catalog: `workspace/DESIGN_SPECS/` (19 patterns + README; 2 NEW from v5.14.10: per-snapshot-cluster-layout-pattern + calibration-log-column-registry)
- Skill outputs go to `plans/plan_checks/<skill>-<YYYY-MM-DD>-<scope>.md` (neutral); batches into sprint dir at close
- TECH_DEBT auto-write: `DOCS/TECH_DEBT.md` (symlinked from workspace; engine-side reads through symlink)
- PARITY_ISSUES auto-write: `DOCS/PARITY_ISSUES.md` (symlinked from workspace)
- HOT_PATH_CHANGELOG: `DOCS/HOT_PATH_CHANGELOG.md` (symlinked from workspace; v5.14.10 entry added 2026-05-10)
- All engine→workspace symlinks verified resolve correctly (v5.14.10 sync confirmed 22 files flowed through cleanly).

## Step 8 — sprint-close verification gate

Before declaring v5.14.11 shipped:
- Tests pass (currently 2882; expect ~+10 new for v5.14.11 per plan)
- `/parity-check` GREEN at v5.14.11 (focus: Welford ↔ full BuildCorr equivalence within 1e-9 tolerance over K=64 records; AVX-512 path bytewise-identical to scalar path)
- `/merge-scan` GREEN (REUSES Welford pattern from RollingStats.hpp; REUSES v5.11.7 AVX-512 byte-determinism pattern; doesn't duplicate full BuildCorr)
- `/trace-deps` PASS (all citations match HEAD)
- `/latency-track` entry in `DOCS/HOT_PATH_CHANGELOG.md` for Ridge cycle savings (~700-900ns when cfg=1; default cfg=0 unchanged); branchless analysis; AVX-512 byte-determinism notes
- `/bug-check` CLEAN (no new recurring-bug-class instances)
- Replay determinism test passes: identical prediction sequence + identical cfg → identical correlation matrix (online + full produce same result within 1e-9 tolerance; AVX-512 + scalar produce BYTEWISE IDENTICAL output via the v5.11.7 discipline)
- Cfg.example doc updated for `ridge_online_corr` cfg field (or for the FOREACH_ML_CFG_FLAG bitmap entry if migrated)
- Periodic full-recompute reset (cycles_since_recompute > 1000) test verifies online-finalized corr matches full recompute exactly after the reset triggers
- Version.hpp bumped to 5.14.11 in the same commit as the umbrella tag

## Closing reminder

This ship's value is the BANDIT/RIDGE PERFORMANCE OPTIMIZATION. It's a small ship (~430 LOC across 3 sub-tags per the plan; could grow to ~500 LOC with cfg-flag-bitmap migration + slow-path-gate predicate + AVX-512 path). The math is well-understood (Welford's algorithm; standard online statistics). The plumbing is straightforward (cfg-gated swap of one fn).

If you find yourself doing major architectural work in this ship, stop — check the plan. v5.14.11 is meant to be CLEANUP after v5.14.10's mega-bundle. Big architectural decisions belong in v5.15+ (per master plan).

**Replay-determinism is sacred.** Welford's algorithm is numerically stable but NOT bytewise-identical to full BuildCorr in finite-precision. Test must verify "within 1e-9 tolerance" — NOT "bytewise identical." This is a TOLERANCE contract, not a bytewise contract. (Distinct from AVX-512 vs scalar which IS bytewise per the v5.11.7 discipline.)

Good luck. Caramel will iterate with you on the audit findings before coding.
```

---

## Notes for future-Claude reading this handoff doc

- Prompt above is self-contained — paste it as FIRST message in a fresh `claude code` session to start v5.14.11.
- Follow the 9-step workflow in order. Don't skip Step 1 (re-verification against current code) — that's where stale-plan-claim issues surface (the "v5.14.12" heading + "v5.14.11 close" predecessor refs).
- Plan file at `subplans/2026-05-08-v5.14.11-online-corr-update.md` (note the stale "v5.14.12" heading inside — fix during Step 1).
- This sprint dir has 5 postmortems from prior sub-ships (v5.14.1 / .8 / .9 / .10 + POSTMORTEM_v5.14.1.md). v5.14.10 postmortem authored 2026-05-11 captures 6 design pivots + 7 architectural surprises + 8 going-forward rule candidates. Read the v5.14.10 postmortem BEFORE opening v5.14.11 — Surprise 7 (DOCS/ symlinks must be edited via workspace path) + going-forward rule #4 (mechanical citation drift discipline) + lesson #6 (Caramel's "harder upfront for easier future" rule validated 4× in this sprint) all directly apply to v5.14.11's pre-coding workflow.

---

## Quick links

- Sprint MASTER: `plans/v5.14-foxml-port-and-maker/MASTER.md`
- v5.14.11 plan: `plans/v5.14-foxml-port-and-maker/subplans/2026-05-08-v5.14.11-online-corr-update.md` (heading stale)
- Latest postmortem: `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-10-v5.14.10-session-postmortem.md` (authored 2026-05-11)
- Predecessor postmortem: `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-10-v5.14.9-session-postmortem.md`
- DESIGN_SPECS catalog: `DESIGN_SPECS/README.md` (19 patterns; 2 NEW from v5.14.10)
- cfg-flag eligibility (strong match): `DESIGN_SPECS/refactor-patterns/cfg-flag-eligibility-criteria.md`
- slow-path-gate registry (strong match): `DESIGN_SPECS/framework-patterns/slow-path-gate-registry-pattern.md`
- registry-tuple-SSOT (medium match if migration): `DESIGN_SPECS/framework-patterns/registry-tuple-as-single-source-of-truth.md`
- Audit-driven pre-coding gate: `DESIGN_SPECS/audit-methodologies/audit-driven-pre-coding-gate.md` (FIRES for this ship)
- Latency rules: `plans/_cross-cutting/2026-05-06-latency-path-discipline.md`
- Coding invariants: `DOCS/STRATEGY_AND_CODING_RULES.md`
- v5.11.7 AVX-512 byte-determinism precedent: `ML_Headers/BanditLearning.hpp:139-162` (`Bandit_GetProbabilities` SIMD vectorization with byte-determinism comments)
- Welford pattern reference: `ML_Headers/RollingStats.hpp:83-87` (true Welford O(1) update)

---

## /handoff skill closing summary

- **Patterns matched (strong):** cfg-flag-eligibility-criteria, slow-path-gate-registry-pattern
- **Patterns matched (medium):** registry-tuple-as-single-source-of-truth, structural-fix-preferred-decision-framework
- **Patterns N/A:** wire-format-byte-preservation (no persistence/stamp), curve-registry-pattern (only 2 modes), per-snapshot-cluster-layout (no cross-thread snapshot)
- **TECH_DEBT overlapping:** -021 (post-paper-test profiling — v5.14.11 perf data feeds this); minor others (-022/-026/-029) not triggered
- **Audit gate fires:** YES — ship qualifies (≥6 new entries + cold-pickup risk; 2/5 criteria)
- **PARITY surface:** Welford ↔ full BuildCorr equivalence within 1e-9 tolerance + AVX-512 ↔ scalar bytewise identity (different tolerance contracts; document carefully in tests)
- **Output:** this file, at `plans/v5.14-foxml-port-and-maker/handoffs/2026-05-11-v5.14.11-handoff.md`
- **Generator skill version:** /handoff v0.1 (second invocation; v5.14.10 was first)
- **Operator action:** review prompt; edit if any of the 9 steps need customization; paste fenced block into fresh `claude code` session. v5.14.10 postmortem already written 2026-05-11 (was previously flagged as gap; now closed).
