# v5.15 — Live-readiness hardening + ModelHandle structural unification — MASTER

> [!IMPORTANT]
> **▶ CURRENT STATE (2026-06-13) — the MASTER body below is HISTORICAL (drafted 2026-05-12, pre-`.E`); preserved as the original sprint-origin record, not the live state.**
> Most recent ship: **`v5.15.5.F.4d.1.E.0.9` — Ship B (decimal money)**, SHIPPED 2026-06-10 (engine `c2d0987`, suite 3285/0).
> The **`.E` sub-sprint** (per-node sharding + decimal-money numeric core + live-readiness) is the live trajectory — full index at `E-MASTER-REFERENCE.md`. **`.E.0` phase COMPLETE**: `.E.0.1` determinism net = tag `.E.0.6`; `.E.0.2` meta-error-tracking = tag `.E.0.5`; numeric core A/A.5/B = `.E.0.7/.8/.9` — all shipped.
> **`.E.0.10` Net-1 (pre-`.E.1` characterization net) IN-FLIGHT (updated 2026-06-11):** D-190 P&L-gross capital bug FIXED + the **cross-thread torn-read CLASS** found (9 sites, 3 live capital-control) → **`.E.1` is now a HARD LIVE-ENABLE GATE**. **Progress 2026-06-11:** oms-ts-1 fee-exact characterization (suite 3347/0 at that milestone); adversarial-default made BINDING (TECH_DEBT-164 part 3 + AR-8/AR-9); context-aware loading shipped (TECH_DEBT-163); **5-agent adversarial money-hunt → 14 findings (3 NEW HIGH capital bugs A1-A8)**, all documented (register + TECH_DEBT-168→171 + PARITY-039 + folded into `.E.1`/SWAR); **A1 CLOSED** (per-node TP/SL single-source helper + restore fix; 3-agent independent refute SOUND; suite **3368/0**); **H22 scale-invariance invariant** added; over-defer rule made BIDIRECTIONAL; module-scoped CLAUDE.md rollout (3/7). **Progress 2026-06-12:** A19/A3/A8/A9 LANDED (engine `2b38bd6`, suite ~3373/0; A9 = slippage SSoT at the Submit chokepoint + pessimistic-by-default default per D-203); **A25 + A28 LANDED** (`0e1f6d2`, suite 3386/0 — per-fill fill-priced `original_tp` carried Async-submit→Order→handle_buy_fill + `Position_Reset` SSoT; A18 reverted→D-209/TD-188; **F1 follow-up fixed** — deep-check 2026-06-12: producer now resolves from `resolved_strategy_id` not configured AUTO→global); deep-audit cohort extended A16–A27 + **A28–A31 newly surfaced** by the 2-critic deep-harvest (Init/Clear reset-subset = A19-class; GUI-drag inert on sharded exit; `is_trailing` perma-false; single_core `*_ExitAdjust` dead-code); **D-202** (struct-cascade tooling tools-first re-cut + check (c) gap-fill) / **D-203** (pessimistic-by-default) / **D-206** (**exit trailing-profit redesign PARKED** → its own E sub-ship; subplan stub written) / **D-207 + AR-11** (the `/decision-check` dogfood meta-failure → Stage-3 hardened: mandatory code-read on factual agent-disagreement); **TD-178 CLOSED** (`/decision-check` built) + **TD-179..186** opened (cascade-tooling gaps + the A26/A27/A29/A31/reconstruct-path homes). **Progress 2026-06-13 (A16 fill-path DESIGN ARC — no engine code; all docs/decision-log/specs):** A16 `/decision-check` → 3-agent cascade → 4-agent keystone-validation distilled the accounting-spine findings into a **three-spines** framing (**D-212/D-213/D-214**; a single "keystone" REJECTED as over-lumped per AR-6 — the adversarial caught it). Codification Phases 1-3 LANDED (doc SWEEP CLEAN ×2): AR-6 recurrence note + **RBP Class 46** (completeness-assuming terminal action on a possibly-partial event) + the spine-1 **fill-completeness DESIGN_SPEC** + the DESIGN_PHILOSOPHY § 4 effective-latency principle (3 contracts: critical-path/reaction/variance) + the `.E.1` foundation-plan **D-214 addendum** (F2 FPN→Money/H4 + F1 16B-atomic + F3 + define FillEvent + A34 per-cluster-pure owner + OrderEvent H21 + the spine-1/A22/A29 folds + the completeness-pass). NEW durable-now capital-safety finds (Phase 4; NOT `.E.1`-subsumed): **NEW-1** (live-safety gate field-decoupling — LiveReadiness keys off `trading_mode` but capital off `use_real_money`, no coupling; Knight-shaped) · **A21** (reconciler open-notional formula, wrong-even-atomic) · **A20** (watermark persistence) · **A17** (paper/REST `order_complete`). **Phase 4 (the 4 durable-now capital fixes A17/A21/A20/NEW-1) LANDED + pushed (engine `f4049c3`); `.E.0.10` Net-1 is NOT closed — the characterization net (F-059 golden-master keystone) + the MISSING live-enable HARD gate + the structural closes remain. Pickup → `handoffs/2026-06-14-E.0.10-net1-batchA-done-batchB-remaining-handoff.md` (the ACTIVE one).** [updated 2026-06-14 — Net-1 keystone **F-059** (oms-ts-1c/1d/1e) + **F-090/093** (parser full-output) + **PERSIST** (non-money passthrough) LANDED + committed + green (build **3497/0**; workspace `e5b78ca`, all test-only, engine HEAD unchanged). A **5-I + 5-A shape-verification cascade** verified every remaining Net-1 target against code (3/5 register shapes were WRONG); **wfa-1 OVERTURNED** to a real paper-mode warm-restart exit-disarm window (**TECH_DEBT-195**, AR-11 code-confirmed) + per-core persist count-lock gap (**TECH_DEBT-196**). REMAINING = the 3 chars **wfa-1 + F-046 + F-018** (write with FULL fan-outs) + the Phase-C structural closes + the **live-enable HARD gate DECISION** (Phase D).]
> **▶ UPDATE 2026-06-14 PM — Net-1 characterization net COMPLETE.** All 6 chars landed + committed + green (**suite 3600/0**): F-059/F-090-093/PERSIST (prior) + **wfa-1 `ada6b7c`** + **F-046 `e76c029`** + **F-018 `81372ab`** (this session, each via a FULL 3-I→3-A adversarial cascade, AR-11 clean, all test-only). REMAINING for `.E.0.10` close = Phase-C structural closes (#9/#10/A6 + guard-matrix-no-HOLE) + **Phase-D = IMPLEMENT the already-DECIDED blanket live-gate** (D-77/F-2 + D-168: live BLOCKED until `.E` complete + v5.16 + explicit greenlight — Phase-D is implementation in `LiveReadiness_Verify`, NOT a new decision) + ship close. Detail: `plan_checks/E.0.10-finding-disposition-register.md` § "NET-1 COMPLETE". **Pickup → `handoffs/2026-06-14-E.0.10-net1-done-10-built-stepB-next-handoff.md` (SUPERSEDED — see the next banner).**
> **▶ UPDATE 2026-06-14 [session close] — #9 RESOLVED + A6 EGRESS landed + gate-substrate direction RATIFIED.** This session: the **gate-substrate 3-I→3-A cascade** (**D-219** — consolidate within the EXISTING `GateParameters` substrate, NOT a new registry; hot-swap is seqlock-provided; taker-output-shape only; A13's naive port = a live Momentum-fires-every-tick bug → **RBP Class 48**); **#9 cohort RESOLVED** (cfg-flag-orphan CLASS structurally closed by the standing `scan_class_44_cfg_orphan.py --strict` guard / `check_session_docs` HARD 10; per-flag retirement = tombstone-the-NAME + reclaim-the-non-persisted-BIT-IN-PLACE — NOT freeze/widen, codified retirement-spec Rule 2 + memory; mechanical retirement homed TD-197/TD-002); **A6 reshaped by a 2-I→2-A pre-coding gate** + **EGRESS LANDED** (engine `7d6a941` — `GateParameters_FinalizeEmit` range-validate `[0,10000.0]` [catches negative + the NaN/+Inf→`+sat` a sign-clamp misses] + override-first-wins `SHALT_BAD_PCT`, ONE dispatcher call = M7-structural; suite **3612/0** + 12 char-tests + hot-path CLEAN); **2 `_future` docs** (book-aware fill model + warm-start history-replay state-priming). REMAINING for `.E.0.10` close = the A6 **INGRESS** half (corrupt-model refuse at `CoreModelZoo_TryLoadRole` + blend isnan-guard + **SHALT-the-node + retrain-warning** [operator-decided, NOT the existing SimpleDip degrade]) + guard-matrix-no-HOLE + **Phase-D** (the DECIDED blanket live-gate boot-refuse) + ship close. **Next session re-runs a FRESH 3-I→3-A swarm to (a) verify the egress landed clean + (b) build the ingress.** **Pickup → `handoffs/2026-06-14-E.0.10-A6-egress-landed-ingress-next-handoff.md` (SUPERSEDED — see the next banner).**
> **▶ UPDATE 2026-06-15 [session close] — A6 INGRESS landed; A6 capital-guard COMPLETE (egress + ingress both in).** This session: the **A6 corrupt-model INGRESS** (engine `3ee95dc`, 14 files; workspace `dd03954`, 11 files) — per-arm corrupt-barrier detect at load (SSoT `barrier_is_corrupt`, NEW `ML_Headers/BarrierValidation.hpp`; fractions, `SANE_MAX_SL=1.0`/`SANE_MAX_TP=10.0`) → per-arm DROP (`corrupt_arms_mask`) → on a **MAJORITY** of corrupt arms **SHALT the node** (`EnsembleZoo_FinalizeCorrupt` + NEW CoreState `MODEL_CORRUPT` bit 5 + `SHALT_MODEL_CORRUPT`=19 + `ml_model_corrupt` failure bit + GUI red render + cfg `model_corrupt_shalt_ratio` default 0.5), **distinct from MISSING→SimpleDip degrade** (D-220/**D-221**); trainer-emit floor folded in (`StampHelper`). Caramel's refinements all in (MAJORITY not fixed-66%, SANE_MAX-as-%, per-arm visibility, fully-disable corrupt arms). Suite green; pre-commit Checks L/H/K GREEN. Captures: **D-221** + **RBP Class 49** + **TECH_DEBT-198..202** + the **V-class** post-implementation-verification concept (memory). **🔴 BLOCKER before `.E.0.10` close: `TECH_DEBT-202` — a PRE-EXISTING OMS async-writer heap-use-after-free** (`OrderEventLog_AsyncWriterRoutine` pops the SPSC ring after `delete oms`; Ship-B vintage, NOT A6; asan surfaced it this session; homed to the `.E` multi-SPSC rework). REMAINING for `.E.0.10` close = fix **TD-202** → guard-matrix-no-HOLE → **Phase-D** blanket live-gate boot-refuse → ship close. **Pickup → `handoffs/2026-06-15-E.0.10-A6-ingress-landed-uaf-next-handoff.md` (SUPERSEDED — see the SHIP CLOSE banner below).**
> **▶ UPDATE 2026-06-15 [SHIP CLOSE] — `.E.0.10` SHIPPED ✅** (tag `v5.15.5.F.4d.1.E.0.10`; engine `572c978` + workspace `3c43458`, both pushed, 0/0). The close-path completed this session: **TD-202** OMS event-log UAF FIXED (quiesce-first `OrderEventLog_Init`, **RBP Class 50**; a 3-I→3-A `/precoding-audit-gate` reshaped the inherited "few-line test-only" framing — the ring-UAF IS test-only, but a PRODUCTION `OrderEventLog_Reset` 3-way disk race + a `LoadFromDisk` recovery hole were found → folded to `.E.1` single-owner-`disk_file`) → **guard-matrix §4d** (records the complete `.E.0.10` guard set; the H7/H10/H14/H20 hot-path/determinism HOLE cluster is explicitly `.E.1`-owned, deferred by design) → **Phase-D** blanket live-capital boot-gate (`LiveReadiness_Verify` refuses live until `.E` lands; via the NEW-1 predicate; H21 tombstone → TECH_DEBT-203) → version bump + signed tag + push. NEW codifications: the **fix-toward-future-trajectory** discipline (design fixes as forward-compatible foundation increments) + the **V-class** post-implementation-verification methodology v1.1 + the **anti-stale sanitizer-gate fix** (`run_sanitizer_suite.sh` now rebuilds each lane fresh — it had been grading months-old binaries; ubsan 3246→3632 once fresh). VERIFY: controller_test **3635/0** + FRESH asan+ubsan **3635/0** (0 sanitizer errors) + calls_graph CLEAN + doc-CI CLEAN. Decision log **D-222**; postmortem `postmortems/2026-06-15-v5.15.5.F.4d.1.E.0.10-postmortem.md`. **`.E.0.10` (the pre-`.E.1` correctness net) is COMPLETE.** Homed-deferred: **#16** V-class SKILL (`/verify-implementation` — methodology codified, skill not yet built) · the `.E.1` event-log single-owner-`disk_file` rework (the Reset race + recovery-integrity gate) · TD-203 (Phase-D gate removal @ v5.16) · TD-162 (warm-restart fee). **Pickup → `handoffs/2026-06-15-E.0.10-shipped-next-E1-handoff.md` (the new ACTIVE one).**
> **NEXT = `.E.1` Foundation: Core→Node rename + per-node drainer absorption + multi-exchange registry** (v0.1 plan, **RED**/pre-audit-gate). Pre-`.E.1` gates (per the `.E.0.5` DoD) = Net-1 PERSIST characterization ✅ + guard-matrix-no-HOLE ✅ (both now MET — `.E.1` is unblocked to dive). `.E.1`'s FIRST-order matrix work = close the recorded H7/H10/H14/H20 hot-path/determinism HOLE cluster (grep-CI + determinism gate + latency ratchet) BEFORE the rename.
> **▶ UPDATE 2026-06-15 [`.E.1` planning phase — no engine code] — `.E.1` DECOMPOSED into 7 sub-ship leaves (N=7, D-225).** The v0.1 plan went through cycle-1 `/precoding-audit-gate` + a **D/A decomposition stress-test** → it is now a **SUB-MASTER (umbrella)** over E.1.0–E.1.6 (`subplans/2026-06-15-…E.1.{0-6}-*.md`): E.1.0 guards (§4d HOLE) → E.1.1 rename+cfg-clean-break → E.1.2 NodeState SoA/Money layout → **E.1.3 ★** aggregator+torn-read+absorption → E.1.4 fill-completeness → **E.1.5 ★** per-cluster purity → E.1.6 multi-exchange; **HARD live-enable = E.1.3 ∧ E.1.5**. Apparatus codified this phase (**D-223..226**: the `plan-decomposition-and-future-aware-agent-arming` + `plan-hierarchy-and-sub-master-decomposition` specs · the **I/A/V/D/C** agent taxonomy + the tabula-rasa fan-out template · the C-class decision-currency sweep). **NEXT = re-ground + deep-gate + CODE `E.1.0`** (the §4d guard HOLE-closure — guards before the rename). Decision detail: `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` D-223..226 + the `.E.1` sub-master § DECOMPOSITION. **Pickup → `handoffs/2026-06-15-E.1-decomposed-next-E1.0-handoff.md` (SUPERSEDED — see the next banner).**
> **▶ UPDATE 2026-06-16 [`.E.1.0` coding — CI tooling, no engine code] — gates 1-3 of ~6 landed + WIRED.** The §4d guard-NET ship is in progress: **must-fix #1** (`tools/lib/sharded_files.txt` SSoT, sourced by `calls_graph_diff`) + **H14** (`check_h14_no_bitfield.py` — decidable RED gate, 0 hits, teeth-proven; pre-commit Check M) + **H10** (`check_h10_simd_determinism.sh` — the marquee; compiles the REAL `RidgeBlender_BuildCorr` AVX-512 vs `-mno-avx512f` scalar + memcmp, `__AVX512F__` split asserted = non-vacuous; `check_determinism.sh` gate 4 + pre-commit Check F + `run_all_tests.sh`). All 5 tools enrolled in `DOCS/TOOLS.md`; guard-matrix §4a flipped **H14/H10→ENFORCED**; doc-CI green. The **3:3 pre-coding audit (6 agents)** reshaped E.1.0 (H14+H10 RED · H7/H20→advisory [grep undecidable] · H8→build-minimal-bench-now, ratchet→E.1.2). **NEXT = H8** (latency bench — run a FRESH I/A agent pass first; the substrate isn't ratchet-ready) → H7/H20 advisory → baseline → V-class verify. Findings: `.githooks` not mirrored by `/sync-workspace` (engine hook edits silently drift — propagated this session; structural fix homed) · `run_all_tests` determinism gap FIXED. **Pickup → `handoffs/2026-06-16-E.1.0-gates-1-3-wired-next-H8-handoff.md` (SUPERSEDED — see the next banner).**
> **▶ UPDATE 2026-06-16 (s2) [`.E.1.0` — H8 design CONVERGED + foundation hardened; no committed engine code] — the H8 latency gate is FULLY DESIGNED (A⁺) via two complete 3:3 sweeps + a cascade-bridge.** The worst-case-ML **vacuity ship-stopper** was caught (the ML-degrade stamps `strategy_id=ML` → the gate asserts FLAGS `MODEL_LOAD_FAILED|MODEL_CORRUPT==0` + an inference-ran counter, NOT a label — Class-51 first canonical). A foundational guard gap was found + closed: a struct size comment said "~1.5MB" for a 64KB struct (= ×16-cores-at-24B, a per-engine total on a per-core field) → built `check_struct_size_budget.py` (sizeof + host-derived cache-budget; 10 structs) + RollingStats `static_assert` size-pins + codified **2 meta-patterns** (`mechanical-verification-of-derived-code-facts` + `critical-moment-determinism-over-average-latency` — *jitter-where-it-matters, NOT average*; settled doctrine). The **trade-branch H20 fix LOCKED** (#15, on principle — the rare trade is a guaranteed mispredict+flush exactly when committing capital). **D-228..231** (incl. the SPMC/shared-market-state + DOS lookahead → E.1.3/5/6). **NEXT = build the H8 bench** (it also verifies #15) → the trade-branch fix → re-sweep → V-class → ship. **Pickup → `handoffs/2026-06-16-E.1.0-foundation-hardened-next-H8-bench-handoff.md` (SUPERSEDED — see the next banner).**
> **▶ UPDATE 2026-06-16 (s3) [`.E.1.0` — H8 RESHAPED to a static conformance analyzer; first cut landed/dogfooded/wired; no committed engine code] — a hardened 3-I→3-A re-pass (D-232) on the converged H8-bench design FOUND 3 things the prior 3:3 missed** (substrate vacuity / a missing `ml_inference_ran` counter / a degrade-mask gap missing `CFG_DRIFT_STRICT_REFUSED`) → operator reshaped H8 from a **runtime latency bench → a STATIC ASM instruction-budget CONFORMANCE ANALYZER** (instruction-count budgets, NOT wall-clock-ns; deterministic, diffable, zero engine behavior → E.1.0 stays a true enforcement-scaffold). `tools/check_latency_path_conformance.py` (+ `_selftest`) **BUILT + dogfooded** (the dogfood CAUGHT an AVX/FMA float-detector hole — the selftest teeth fired; fixed) **+ WIRED** (TOOLS.md + `/hft-audit` + `/dod-audit` + CoreFrameworks/CLAUDE.md + `latency-path-discipline.md`); it gates instruction-count + branch-classification {loop / rare-cold / **data-dependent-warm = the H7/H20 meter**; SUBSUMES the H7/H20 grep} + no-float(H4)/div/malloc/indirect/spill + **asserts its OWN non-vacuity** (Class-51 — proved itself by refusing RollingStats's optimized-to-2-instr wrapper). Independent deliverable review **CLEAN**. **D-232..236** (the re-pass · static-tool fork-resolution · conformance-analyzer extension · the zero-data-dependent-branch E-goal · curation+reduction → the optimization leaf, NOT E.1.0). **NEXT = finish the tool** (slow-path callee-symbol follow + set the grandfathered-25 baseline + wire pre-commit/CI + teeth-proof the non-float checks) → the 2 DESIGN_SPECs + Class-51 → baseline (#4) → V-class (#5) → ship `.E.1.0`. **Pickup → `handoffs/2026-06-16-E.1.0-conformance-analyzer-landed-next-finish-tool-handoff.md` (SUPERSEDED — see the next banner).**
> **▶ UPDATE 2026-06-16 (s4) [`.E.1.0` — H8 conformance analyzer FINISHED (full foundation); no committed engine code] — the tool went from first-cut → the full structural foundation** (operator: *"id rather do more work now creating a good foundation than anything else"* / *"foundation and structural fixes are always going to be preferred"*). `tools/check_latency_path_conformance.py` now **exits 0 with full hot + slow + ML-kernel coverage**: a **7-row KERNEL manifest** (D-238 kernel-granularity — gate the bounded per-cycle LEAVES, NOT the `EventLoop_RebuildOneCore` orchestrator which over-inlines the whole pipeline incl. cold persistence → signal flood) + **source-keyed `allow` + `FEATURE_MATH_EXTERN` exemptions single-sourced to the engine's H4-sanctioned feature seams** (D-237; forbidden-calls exempt by `file:line`-ONLY, never file-level) + a **transitive HARD-invariant scan into same-TU callees** (MAX_DEPTH 5, fail-loud — the cold-quartile-hole fix, STRUCTURAL not positional). **CI-WIRED** (pre-commit Check N trigger-scoped + `run_all_tests.sh` gate + `--selftest`, all HARD) + **grandfathered baseline** (`tools/lib/latency_path_budgets.json`, 7 rows); **15 `--selftest` teeth fire**. **5 independent adversarial passes found the load-bearing holes the builder's dogfood + teeth could NOT** (AR-8 model-bounded-self-check floor): the **decorative div-gate** (counted but un-gated) · the **cold-quartile false-green** · the **forbidden-file-level hole** — each fixed STRUCTURALLY, not logged. **D-237** (B scope-expansion) **+ D-238** (s4 implementation decisions). Captures: **TD-205** (FINDING A H3 pthread cold/sanctioned) · **TD-206** (XGBoost coverage gap — `Model_Predict*` external-API stub) · **TD-207** (M7 — independent-review-by-default for mid-session tool builds; the AR-8 3rd-consecutive harvest-timing miss) · the **i/a/v/d/c agent registry** (`claude-agents/` + `DOCS/SUBAGENT_ARMING.md`, live next session) · TOOLS.md:54 currency-fix. **[s5 UPDATE 2026-06-16 — the 3 codifications LANDED in-session (operator "codify everything while we have context"): the conformance DESIGN_SPEC `audit-methodologies/static-latency-path-conformance-analysis.md` + Class-51 first-canonical `class-51-vacuously-green-guard.md` (the dangling `sister_docs` forward-ref that 13 files pointed at is CLOSED) + the mechanical-verification D-233 amendment (LATENCY/COMPLEXITY runtime→static); README/TAG_INDEX rebuilt (178 specs), all bidirectional-sister + index-currency + doc-CI GREEN, D-238 STATUS→LANDED.] **NEXT = TAG `.E.1.0`** (verify build + conformance tool green → bump Version.hpp `.E.0.10`→`.E.1.0` + /ship) → E.1.1 (rename) → E.1.2 (relayout).** **Pickup → `handoffs/2026-06-16-E.1.0-conformance-analyzer-FINISHED-next-shipclose-codifications-handoff.md` (SUPERSEDED — see the SHIP CLOSE banner below).**
> **▶ UPDATE 2026-06-17 [SHIP CLOSE] — `.E.1.0` SHIPPED ✅** (tag `v5.15.5.F.4d.1.E.1.0`; engine `90b2daf`, GPG-signed, PUSHED; branch `572c978..90b2daf`). The **H8 STATIC latency-path conformance analyzer** (`tools/check_latency_path_conformance.py`) + the **§4d guard-NET** — first leaf of the decomposed `.E.1` foundation (D-225); **zero engine code** (private CI apparatus + the s5 codifications, all gitignored-in-place/workspace-backed; the tag commit = Version.hpp bump only, which also carried up the prior-session RollingStats size-pins `226a652`). Pre-tag verify ALL GREEN: `build.sh test` **3635/0** · `run_all_tests --fast` ALL HARD PASS (suite + doc-CI + orphan + determinism + conformance + 15 teeth) · `calls_graph_diff` CLEAN · doc-CI SWEEP CLEAN. The tool is now a **STANDING gate** (pre-commit Check N + `run_all_tests.sh`, both HARD) — E.1.1's rename must keep it green. The 3 ship-close codifications had landed s5 (conformance DESIGN_SPEC + Class-51 first-canonical + mechanical-verification D-233 amendment); this close also fixed the `TOOLS.md:54` "ship-close-pending" currency drift. NO new decision this session (the tag executed the already-DECIDED D-238). **NEXT = `E.1.1` Core→Node rename + cfg-key CLEAN-BREAK + `single_core` LIVE deletion** (RED/pre-gate — the SCAFFOLD's grep counts are stale vs HEAD; first action = **C-class re-ground → /precoding-audit-gate (deletion-class) → consult → code**; the cfg clean-break is the one behavior change [HARD-REFUSE legacy `core_*` keys, operator migrates]) → `E.1.2` (NodeState SoA relayout, first real consumer of the tool). **Pickup → `handoffs/2026-06-17-E.1.1-core-node-rename-handoff.md` (SUPERSEDED — see the MID-DIVE banner below).**
> **▶ UPDATE 2026-06-17 [`.E.1.1` MID-DIVE — pre-coding done + coding STARTED] — E.1.1 is IN PROGRESS** (Core→Node rename + cfg-key CLEAN-BREAK + single_core LIVE deletion). Pre-coding COMPLETE: C-class re-ground (corrected the SCAFFOLD's stale grep counts) → 7-agent `/precoding-audit-gate` (deletion-class: 4 i-class + 2 a-class + completeness-critic) → verdict **RED (major amendment, NOT re-design)** → the FULL gate synthesis folded into the plan body as the dive SSoT (§"Gate synthesis — amendments + corrected dive scope"). **D-239** decisions: ExecutionCore PRESERVE (glossary §15 RATIFIED; premise: a NODE owns 2 CPUs, `node_id ≠ cpu_id`) · ② single_core CLEAN-DELETE no-tombstone (`engine_mode` verified not-persisted/wire) · ③ gate-scope `_PER_CORE`→`_PER_NODE` · ① file-renames in-scope. CODING STARTED — 2 commits (pushed to origin; HEAD `3eadb53`): `2b8bd6c` node→CPU legibility helpers (behavior-preserving; 3635/0; outside the names-only bisect) + `3eadb53` Phase 1 of the single_core 3-phase deletion (orphan `LegacyReferenceDriver` delete). **NEXT = Phase 2** — the delicate `main.cpp` untangle (delete the legacy engine block `:286-1339` + `engine_force_close_all` + dispatch-rewrite-to-unconditional-sharded + untangle the orphaned legacy `ctrl`/`pool`/`log` setup from the sharded-needed setup, GUARD-DRIVEN; `3eadb53` is the restore point) → Phase 3 (`engine_mode` registry-row [H17] + readers + parity_harness + tools cohort) → TD-175 cascade tool (widen scan to `tools/`) → the rename → cfg clean-break (HARD-REFUSE at the OUTER `:2968`-block fall-through, NOT `~:3097`) → C-2 mirror parity-guard → V-class. Riding under `.E.1.0`'s standing gates (operator: "keep going just use ci guards"). **Pickup → `handoffs/2026-06-17-E.1.1-MID-DIVE-phase2-next-handoff.md` (the new ACTIVE handoff).**
> **▶ UPDATE 2026-06-20 [`.E.1.1` Phase 2 LANDED + include-prune] — the legacy `single_core` engine is GONE.** An 8-agent adversarial state-audit preceded coding (tree fully green; plan accurate; refinements folded to plan §"State-audit refinements"). **Phase 2 LANDED:** `7eacb80` (delete main.cpp legacy single-threaded engine — 1116 lines + `engine_force_close_all` + unconditional sharded dispatch) + `2baf0f6` (prune 9 orphaned #includes); both green (controller_test 3635/0 ANSI+GUI · determinism+conformance+calls_graph clean · hook-gated). Clean excision (legacy locals were inside the deleted block → zero orphans). Housekeeping: TD-209 opened (V-class skill); PARITY-009..018 closed (code-grounded). **NEXT = Phase 3** (`engine_mode` registry-row delete [H17] + 2 readers + `ControllerConfig.hpp:1860` + parity_harness retire + tools cohort + delete the engine_mode field [closes TD-140, the Phase-3 root edit] + fold/close TD-185) → TD-175 cascade tool → rename → cfg clean-break + C-2 guard → V-class. Engine pushed (`2baf0f6` → origin) + workspace pushed (`6f825a1`). **Pickup → `handoffs/2026-06-20-E.1.1-phase3-next-handoff.md` (SUPERSEDED — Phase 3 done).**
> **▶ UPDATE 2026-06-21 [`.E.1.1` Phase 3 LANDED — `engine_mode` deleted + `parity_harness` retired] — the `single_core` deletion is COMPLETE (all 3 phases).** Phase 3 `b9ce419`: delete the `engine_mode` cfg field via its `FOREACH_GLOBAL_CFG_FIELD` registry row (`CfgFieldRegistry.hpp:393`, H17 — the row delete auto-cascaded the X-macro struct field + both `ENGINE_MODE_*` enums + the parser branch + the 2 hot-reload readers, `num_execution_cores`/`trading_mode` preserved) + retire `parity_harness` (CMake target + source + INVARIANTS_MAP/locale/plan-body-tests cohort — its single_core↔sharded diff is a tautology now that `Backtest_Run` calls `BacktestSharded_Run` unconditionally). **TD-140 CLOSED**; **TD-185 → E.1.3** (Phase-3 coupling VERIFIED — all `*_ExitAdjust`/`Regime_AdjustPositions` callers live in `PortfolioController`'s central-drainer, which E.1.3 removes; does NOT excise standalone-clean). Gates ALL GREEN: build test 3635/0 · build_gui · **fresh asan/ubsan 3635/0** · determinism golden byte-IDENTICAL · conformance + calls_graph clean · doc-CI clean · pre-commit M/H/K green (H21: 46 wire-ids match the ledger — `engine_mode` confirmed non-persisted). engine_mode CODE residue ZERO (the ~18 remaining [across 12 files; the earlier "~15" undercounted, per the 2026-06-21 independent review] are stale COMMENTS, deferred to the rename sweep). Engine PUSHED (`b9ce419`); workspace PUSHED. **NEXT = ① the TD-175 cascade tool** (build rename-consumer-enumeration on `check_struct_alignment.py` + WIDEN scan to `tools/`+`build.sh`+`.githooks/` — load-bearing, apparatus renames commit GREEN today) **→ ② the Core→Node rename → ③ cfg HARD-REFUSE clean-break → ④ C-2 GUI parity-guard → ⑤ V-class.** **Pickup → `handoffs/2026-06-21-E.1.1-rename-next-handoff.md` (the new ACTIVE handoff; resume at ① the cascade tool, then the rename).**
> **▶ UPDATE 2026-06-21 (s2) [`.E.1.1` — ① the TD-175 cascade tool BUILT + completeness-validated; no engine code] — the rename de-risk MAP is done + trustworthy; the rename itself is next.** `tools/cascade.py rename` (enumerate-only, **TD-175a**) + the `check_tools_inventory.py` `build.sh`-scan (gate **R2**) BUILT + teeth-green + wired (`run_all_tests` + TOOLS.md + `rename-ship-methodology` §Mech + `/trace-deps` + `/dependency-chain-trace`). **D-240** gate refinement (a `/decision-check` I→A, operator-ratified): **R1** `rename`=enumerate-only [the compiler is the code-token oracle — no code `--apply`; a code `--apply` only adds a Class-36 corruption surface] · **R2** the standing net rides the existing `check_` guards, not a new subcommand. A post-build **a-class completeness audit** ("ensure the tool is finding all places") caught a Class-33 under-count [hand-list + START-anchor missed `CoreSlowState` / the ~50 `core_<stat>` field family / embedded compounds / `*_cores` / a whole file / guards — **2118→3645 sites**] → fixed via the **family-regex matcher** + per-family selftest controls + a mechanical **freeze ⊇ raw-`rg` cross-check** [0 real misses; closes AR-8 self-attestation]. **Phase-2 enumeration freeze** frozen (`plan_checks/2026-06-21-…-freeze.md`, 3645 sites). TD-175 split → **TD-175a CLOSED** + remainder (AST `struct` cascade) → E.1.2. **Consumer-completeness — BOTH worlds:** World-1 code = compiler oracle (red-build); World-2 apparatus = cascade-enumerated + validated + teeth. Rollback anchors: engine tag `pre-v5.15.5.F.4d.1.E.1.1` @ `b9ce419` + workspace `9cffbb4`+`42e462e`. **NEXT = ② the Core→Node rename** (ONE atomic mechanical commit — code [compiler oracle] + apparatus [cascade/teeth] together) **→ ③ cfg cluster (C-2 guard FIRST, then HARD-REFUSE + global-key refuse + GUI lockstep) → ④ V-class (determinism golden byte-IDENTICAL + sanitizers + calls_graph + parity guard + node_id allowlist) → ⑤ ship-close.** **Pickup → `handoffs/2026-06-21-E.1.1-rename-next-handoff.md` (ACTIVE; the cascade tool is now BUILT — resume at ② the rename, not ① the tool).**
> **▶ UPDATE 2026-06-21 (s3) [`.E.1.1` ② the Core→Node mechanical rename LANDED] — engine `1da1c1c` + workspace `c356e03`.** The ship's namesake is committed + GREEN: content-sub 3633 sites + 6 engine git-mv (CoreModelZoo/CoreCtx*/CoreStateFlagRegistry/PerCoreStateFlagsRegistry/CoreLatencyStats→Node*) + the tool git-mv (check_per_core→per_node) + `identifier_ledger` `--update` (CORE_STATE_FLAG→NODE_STATE_FLAG, values preserved, **Check-H 46/46**) + tests/ + the cfg-parser prefix (recognizes `node_`). VERIFY: **3635/0** · determinism net byte-identical · H8 conformance clean · calls_graph clean · per-node integrity clean · BOTH pre-commit hooks GREEN. **The World-1 compiler caught every cascade-matcher gap** (the `.cores[`-too-narrow decl/arrow/bare, apparatus regex-metachar `core_`, `tests/` scope, `core_<digit>`, the parser prefix) — the red→green loop proving the compiler-for-code-consumers model; PRESERVE held (0 ExecutionCore/FoxML_Core/CoreFrameworks corruption). Rollback anchor: tag `pre-v5.15.5.F.4d.1.E.1.1` @ `b9ce419`. **NEXT = ③ the cfg cluster** (HARD-REFUSE loud-fail @ `ControllerConfig.hpp:~2929` + global-key refuse [engine_mode=/num_execution_cores=] + the C-2 GUI parity guard + delete `SettingsPanel.hpp:669` + operator migrates `backtest.cfg`/`engine_sharded.cfg`) **→ ⑤ V-class** (asan/ubsan — the only remaining leg; build/determinism/conformance/calls_graph already green) **→ ship-close** (doc sweep Phase 5 + FEATURE_LOOKUP + the ~535 CPU-vs-shard "cores" comment-prose human-classification + TD-140 move). **Pickup → `handoffs/2026-06-21-E.1.1-cfg-cluster-next-handoff.md` (SUPERSEDED — see the s4 banner below).**
> **▶ UPDATE 2026-06-22 (s4) [`.E.1.1` ③ the cfg cluster FULLY DESIGNED as a "config compiler"; NO engine code] — design + decision-capture only.** An 8-agent I→A pre-coding cascade + an operator design pass reshaped ③ from "HARD-REFUSE at the per-node fall-through" into a **config-validation pass (a "config compiler"):** one pass collects ALL problems as a compiler-style readout (**ERRORS** block / **WARNINGS** proceed); a clean compile GATES every fresh start (live AND backtest, operator-confirmed); hot-reload/GUI WARN-keep-old; **flag-based, NEVER abort-in-`ControllerConfig_Load`** (3 cascade confirms — kills a running engine + crashes the GUI's `Settings_Load`); KEY-MATCH-based fault; output = terminal + `config_error_log` (NOT a GUI banner — the GUI deprecates at E.2). Captured: **D-242** (config compiler) + **D-243** (③ = buffer-fix only; global deployment root → E.2) + **D-244** (backtest = CLI runner + read-only viewer, open/.E.2) + the cascade SSoT `plan_checks/2026-06-21-E.1.1-cfg-clean-break-precoding-cascade.md` + NEW memory `feedback_surface_foundational_decisions_not_just_tactical`. Engine HEAD unchanged (`1da1c1c`, **3635/0**); workspace `466c718`. **NEXT (do NOT code first): a FRESH pre-coding gate + a TARGETED adversarial re-gate on the config-compiler DELTAS** (errors/warnings · key-match-not-field-written · compile-gating-every-fresh-start · the `cfg_load_fault_flags` bitmap + fixed-buffer readout · terminal+`config_error_log` output — NOT a full re-sweep) **→ THEN code ③ → ⑤ V-class → ship-close.** **Pickup → `handoffs/2026-06-22-E.1.1-cfg-config-compiler-gates-next-handoff.md` (SUPERSEDED — see the s5 banner below).**
> **▶ UPDATE 2026-06-22 (s5) [`.E.1.1` ③ GATES PASSED → CODED — steps 1-3 committed, build-gated 3635/0].** The fresh pre-coding gate + the targeted re-gate on the config-compiler deltas PASSED; ③ coding landed across 3 build-gated commits: `7b35517` `CAPITAL_BOUND_{LOSS,GAIN}` bits + 15-field cohort tagging (5 GAIN / 10 LOSS) + the both-active compile guard; `a75fddf` the `cfg_load_fault_flags` field + the fingerprint size-pin (`sizeof(ControllerConfig<64>)==53056`) + determinism wiring; `03830e8` the parse-point MALFORMED-capture (FLAT path) + `cfg_compile_ok()` + the main.cpp boot-refuse. **Founding bug (`stop_loss_pct=banana` → SL silently disabled) CLOSED for the flat/global path.** Mid-coding the value-only sweep was found blind to MALFORMED (`banana`→0 = a valid sentinel post-parse) → reshaped to the **HYBRID** (**D-254** + the malformed/hybrid **ADDENDUM**): parse-flag-capture for MALFORMED/OVERFLOW (abort + a reasoned line) + a post-resolve out-of-range value-sweep; `EXCESS_DP`→WARN; ALWAYS-ABORT all modes (D2); NO MARGIN (loss ≤100% / gain ≤1000%). Captured: **D-247→D-254** + the ADDENDUM + **D-251** (all-strategies-private; the leaked `EmaCross.hpp` gitignored go-forward, `252973f`). Independent Stage-5.5 review CLEAN; doc-CI clean; AR-7 meta-harvest bumped. Engine HEAD `03830e8` (untagged; Version.hpp `.E.1.0`). **REMAINING:** 4b per-node malformed-capture · 4c the out-of-range post-resolve sweep (`ControllerConfig.hpp:3277`) · backtest+GUI `cfg_compile_ok` boot-checks · `core_→node_` key clean-break + dead-`CFG_PARSE_PCT/MONEY` deletion (TD-215) · tests → ⑤ V-class → ship-close. **Pickup → `handoffs/2026-06-22-E.1.1-cfg-config-compiler-coding-handoff.md` (SUPERSEDED — see s6 below).**
> **▶ UPDATE 2026-06-22 (s6) [`.E.1.1` ③ — 17-agent reframe → D-255 CONVERGED scope; item 1 of 6 LANDED `c6967be`].** A 17-agent pre-coding reframe audit (operator-pushed "are we foundational enough / won't redesign in 2 plans") **course-corrected an orchestrator over-rotation** — a found Stage-2 `[section]`-parser spec was amplified into "the foundational answer / the patch is redesign-risk," and 3 independent agents proved it ORTHOGONAL to capital validation (the post-resolve sweep is parser-agnostic; **AR-6 recurrence**). **CONVERGED on D-255:** the D-254 metadata-bit + post-resolve-sweep design is sound + foundational (survives E.1.6/E.2/E.5/.E.3); close the founding bug **correctness-first** via 6 build-gated items; **DEFER** the uint16→uint32 widening + `CAPITAL_BOUND_DOLLAR` (→ E.1.6 — `ENUM_STRICT` is the real consumer; no free uint16 slot) + the `[section]` parser (→ E.2). NEW criticals the prior 28-agent cascade missed (in the CONSUMPTION + binary-boot paths): **C-1** `cfg_compile_ok` had ONE caller (`main.cpp:203`) → the GUI/suite/backtest binary ran malformed cfg UNGATED; the dollar floors need `>0`; per-field ≤100% (incl. `node_N_*` overrides) is ③'s job (only cross-node Σ → E.5); the recurrence guard must be compile-time. **Item 1 LANDED** (`c6967be`, 3635/0, all 5 pre-commit gates green): `cfg_capital_gate_ok()` single-source gate wired into boot + backtest (closed C-1). **Hooks-open refinement** (operator-directed): MIGRATE the 4 non-registry globals into the registry (not hand-roll-then-delete). Findings homed as fold-blocks into the E.2/E.5/E.1.6/E.1.3 bodies (NOT pointers — the AR-12 lesson) + AR-12/AR-6 (meta-index) + TD-219/220/221. **REMAINING:** item 2 (MIGRATE the 4 globals — trips the `53056` size-pin → golden regen owed) · items 3-6 (dollar `>0` · per-field ≤100% via `barrier_is_corrupt` · the `Async.hpp:321` reload gate + its H4 double-math smell `:347-356` · the compile-time recurrence guard) · ParseFast `*_checked` · tests · ⑤ V-class · ship-close (FEATURE_LOOKUP owed here). Live record = `plan_checks/2026-06-22-E.1.1-③-precoding-adversarial-findings.md` § CONVERGED PLAN + **D-255** (`decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md:1649`). **Pickup → `handoffs/2026-06-22-E.1.1-③-item2-migrate-handoff.md` (ACTIVE).**
> **▶ UPDATE 2026-06-23 [`.E.1.1` ③ item-2(a) MIGRATE + B1 LANDED — the 4 capital GLOBALS' founding bug CLOSED].** Per **D-256** (the forward-seam inflection corrected item-2's scope back to D-255 = migrate + per-node; AR-13 codified). **Engine PUSHED:** `890b368` migrate the 4 globals (`starting_balance`/`min_kill_loss`/`min_sl_tp_ratio`/`min_hold_gain_pct`) into `FOREACH_GLOBAL_CFG_FIELD` (size-neutral, `sizeof==53056` held) + `98267be` **B1** unit-agnostic malformed-refuse (decoupled the FATAL from the `CAPITAL_BOUND` bit-gate → ANY decimal-`Money` malformed value boot-refuses; the 4 `meta=0` globals now covered + contract-test-pinned). VERIFY: **3642/0** + gui ok + determinism GREEN + pre-commit M/H/K. **D2 RESOLVED (coded):** golden-regen MOOT — no committed cfg-fingerprint golden exists; the epoch shifted but the determinism gate is GREEN; dev fixtures re-stamp free (no live models). Captures: **D-256** + **AR-13** (forward-input-space under-enumeration) + 4 fold-blocks (E.2/E.5/E.1.2/E.1.5) + TD-215/216/221 + the item-2 sweep SSoT. **REMAINING:** item-2 per-node (b/c/d — in-macro flag-capture + legacy-array hand-roll + 7-`strstr` fold; the 7 re-vet pins) → items 3-6 → ⑤ V-class → ③ ship-close. **Pickup → `handoffs/2026-06-23-E.1.1-item2-pernode-handoff.md` (SUPERSEDED — see the s7 banner below).**
> **▶ UPDATE 2026-06-24 (s7) [`.E.1.1` ③ — the MALFORMED + UNKNOWN-KEY + caller-coverage surface CLOSED; 9 commits `45d5be8..be5d803`, UNPUSHED].** This arc (operator "keep rolling"): ③ item-2 per-node founding bug CLOSED + **v-class DoD CLEAN** (asan/ubsan 3665/0, accounting-SSoT no-bypass, non-vacuity proven via fix-revert); then the **config-compiler KEYSTONE** built design-once + reused — **the clean-break** (`1032c9f`: unknown SHARDED-key HARD-REFUSE scoped `core_*/node_*`, false-refuse-safe in the 2-parser shared-cfg setup, own `UNKNOWN_KEY` bit + the `FEATURE_MALFORMED` bit pre-allocated for C1/C2) → **N1** (`04035ae`: BinanceConfig venue-selector malformed-capture via `tt::parse_int_checked` + `binance_config_ok` boot gate — the silent `use_testnet`→PRODUCTION flip CLOSED) → **GAP-1 safety** (`be5d803`: the BacktestEngine optimizer base-cfg gate). VERIFY: controller_test **3676/0** + `build.sh gui` CLEAN + pre-commit GREEN (F/M/H/K/L) on every commit. Captures: **D-257..D-264** (8 decisions) + **RBP Class 52** (swallow-and-coerce) + **Class 53** (rename-completeness-gap) + the **config-compiler-validation-pattern** DESIGN_SPEC (Stage 3) + the NEW `check_cfg_key_prefix_drift.py` guard (wired into `run_all_tests.sh` + `--selftest` teeth) + the cfg/string rename-residue migration. Independent Stage-5.5 review CLEAN (one mechanical gap — this banner — fixed at close). **REMAINING: item-4 (per-field ≤100%/≤1000% caps via the post-resolve sweep — the first SUBSTANTIAL build, teed for fresh context) → item-3 (folds in) → C1/C2 → item-5/6 → vocab → ⑤ V-class → ③ ship-close.** **Pickup → `handoffs/2026-06-24-E.1.1-config-compiler-arc-handoff.md` (SUPERSEDED — see the s8 banner below).**
> **▶ UPDATE 2026-06-28 (s8) [`.E.1.1` ③ item-4 step-1 = B's merge LANDED + PUSHED; engine `9c93d1d`].** item-4 (per-field capital value-range validator) opened with a full 3-I→3-A cascade (+3 deeper i-class +3 adversarial +accounting-gate; SSoT `plan_checks/2026-06-26-E.1.1-item4-caps-3i3a-synthesis.md`). **Step 1 LANDED** (`9c93d1d`; **3680/0** + 1066/0; independent close-review CLEAN): the legacy capital arrays `node_risk_pct`/`node_max_drawdown_pct` MERGE into `nodes[c]` via the new `FOREACH_PER_NODE_ARRAY_OVERRIDE` sidecar (H15-enrolled; last-wins; byte-identical; 4 consumers migrated) → ONE authoritative per-node read → the legacy-array CRITICAL SUBSUMED. **SCOPE-CORRECTED (D-275/D-276):** the `metadata_flags` `uint16→uint32` widening is a metadata-bit FRAMEWORK expansion → the `*_mult` + **FEE**(venue-sourced) + partial_exit cap-bits DEFER to E.1.6 (TD-224); item-4 CORE = the 15 LOSS/GAIN fields (existing bits) + explicit dollar floors + `min_kill_loss<starting_balance`. Captures: **D-265..D-276** + **TECH_DEBT-224** + the E.1.6 widening fold + memory `feedback_complete_structural_fix_not_incremental_prelive`. **REMAINING: item-4 CORE (the `capital_value_out_of_range` helper + the LOSS/GAIN post-resolve sweep + dollar floors + `cfg_capital_gate_ok` wiring + tests) → item-3 folds in → C1/C2 → item-5/6 → vocab → ⑤ V-class → ③ ship-close (D-271 codification slate owed here).** **Pickup → `handoffs/2026-06-28-E.1.1-item4-core-next-handoff.md` (SUPERSEDED — see the s9 banner below).**
> **▶ UPDATE 2026-06-29 (s9) [`.E.1.1` ③ — the config-compiler arc CODE-COMPLETE + the D-271 codification slate DONE; engine `6981c85`+`d8fe6c6` + workspace `1725742`+this-close, PUSHED].** This arc (operator "finish this stuff off" → "capture as much as possible"): **item-4 CORE LANDED** (`6981c85`, **3690/0** + 10 char-tests) — the per-node out-of-range sweep `ControllerConfig_CapitalRangeSweep<F>` (if-constexpr walk over `FOREACH_PER_NODE_CFG_FIELD` + the F3 exhaustiveness `static_assert`) + the explicit dollar floors; a **4-agent sweep caught the F1 global-inherit hole** (an inheriting node's `nodes[c]==0` passed the `nodes[c]`-only sweep while the allocator used the un-swept global `risk_pct=999`) → the registry-driven global-flat leg + the **F2 optimizer range-endpoint gate**. Then **item-5** (live-reload re-validate-the-merged-cfg + warn-keep-old) + **C1/C2** (the ~47 FPN/float feature fields → `parse_double_fast_checked` → `FEATURE_MALFORMED`; byte-identical for valid inputs → NO determinism shift) + **item-6** (the recurrence guard: a no-`MANUAL_PARSER`-on-a-`CAPITAL_BOUND`-row `static_assert` + the CI `check_cfg_gate_caller_coverage.py`, `--selftest`-proven, wired into `run_all_tests.sh`) → engine `d8fe6c6`. VERIFY: controller_test **3693/0** + FRESH asan/ubsan **3693/0** (0 sanitizer errors) + determinism GREEN + caller-coverage PASS + gui ok + doc-CI CLEAN. **D-271 codification slate DONE:** **D-279/D-280** + RBP **Class 54** (validation-asymmetry-across-write-paths) + **Class 55** (dual-source-storage-split-readers) + the config-compiler **FEATURE_LOOKUP** entry + **TD-212 PARTIAL-CLOSED** (capital subset done; non-capital ~36 fields → E.2) + the **Landmine 5 recurrence** note + (at item-4 close) **D-277/D-278** the deletable-cascade-over-tombstone discipline (memory + 2 DESIGN_SPECS). **REMAINING (E.1.1 ship-close): the vocab sweep (~60-85 `core_`→`node_` operator strings + classifier, #13) · boot-surface GUI warn-banner + global-unknown-key refuse (#11) · OQ1 inert-control wire/tombstone (#14). Then E.1.2 (NodeState SoA / `Money` relayout — owns the legacy-array DELETION the B-merge teed up + the GAP-2 fix, #6/#15).** **Pickup → `handoffs/2026-06-28-E.1.1-item4-core-next-handoff.md` (SUPERSEDED — see the s11 banner below).**
> **▶ UPDATE 2026-06-29 (s10) [`.E.1.1` vocab #13 DONE — the Core→Node rename completes on the operator-facing surface; engine `baf5e5a` + workspace `890ac09`, PUSHED].** ~190 operator strings / 24 files (runtime logs + GUI labels/headers/tooltips + cfg-field display names + rendered registry descriptions) + a CORRECTNESS fix (`main.cpp:175-187` help pointed at the boot-refused `core_<N>_symbol=`; the real key is `node_<N>_symbol=`, verified `CfgFieldRegistry:838`/parser `:3051`). Method = a conservative letter-lookbehind script + a FULL git-diff review (caught + fixed a `score`→`snode` substring over-match + one CPU-core comment mis-rename `Run.hpp:245`). VERIFY: controller_test **3693/0** + gui ok + cfg-key-prefix drift guard CLEAN. Captures **D-281** + **TD-225** (the KEEP residuals — 2 persisted H21 surfaces + ~17 fn-symbol/doc_string MEDs). **REMAINING E.1.1: boot-surface #11 (global-refuse BLOCKED — sibling-parser false-refuse, TD-223; only the GUI banner is doable) · OQ1 #14 (decided-WIRE per D-273 — a real kill-switch/exposure-cap capital ship → its own session). Then E.1.2 #6 (the layout leaf + GAP-2).** **Pickup → `handoffs/2026-06-28-E.1.1-item4-core-next-handoff.md` (SUPERSEDED — see the s11 banner below).**
> **▶ UPDATE 2026-06-30 (s11) [`.E.1.1` SHIPPED (tag `v5.15.5.F.4d.1.E.1.1` @ engine `0ee227a`, push parked on SSH); + a DESIGN+VALIDATION session for E.1.2 (no code).** This session: the **E-umbrella hot-path forward audit** (85-agent workflow) + **3 A-class refutations** + a **3:3 on the candidate "constraint-driven layout" invariant** → **D-282..D-286**. Verdict: NOT a new H-row / NOT a CLAUDE.md principle (it's ~already item-28 + H6/H17/H22); a narrow **ownership/replica-coherence** core survives (folds into the H22 + decision-time-binding specs). The **amended E.1.2 freeze spec** (7 items) + the **per-field declaration table** are captured (`plan_checks/2026-06-30-E.1.2-per-field-declaration-table.md` + `…-E-umbrella-hotpath-forward-audit-synthesis.md`); hot/slow **futures** documented (`plans/_future/2026-06-30-…`). **⚠️ STILL-PENDING PRE-DIVERGENCE IMPLEMENTATION QUEUE (NOT superseded by the E.1.2 thinking — these were queued BEFORE we went deep on E.1.2):** **#11 boot-surface GUI warn-banner** (the doable half; the global-unknown-key refuse is BLOCKED on TD-223 sibling-parser false-refuse) · **#14 OQ1** — WIRE the inert sharded kill/exposure controls (decided-WIRE per D-273; a real capital ship → own session) · **#15 GAP-2** strategy-state type-confusion OOB on GUI swap (folds INTO the E.1.2 layout seam). **E.1.2 #6** (the NodeState SoA / `Money` relayout leaf) is now DESIGN-READY but is the heavy determinism-gated freeze. **ORDERING IS OPERATOR'S CALL** — the queued #11/#14 (small-ish implementation) vs E.1.2 (design-ready, heavy). **Pickup → `handoffs/2026-06-30-E.1.2-pickup-handoff.md` (SUPERSEDED — see the s12 banner below).**
> **▶ UPDATE 2026-07-01 (s12) [E.1.2 pickup session — DESIGN + CODIFICATION, no engine code] — cycle-1 codification LANDED + Fight #4 dispositioned + the E.1.2 reformalize inputs assembled.** This session (**D-287..D-290**): the ownership/replica-coherence **MIDDLE-PATH** codification landed (H22 §"Cross-tier ownership + field categories" + `decision-time-binding` v1.4 replica-coherence + memory `feedback_synthesis_needs_its_own_adversarial_pass` + bidirectional sisters; the 3:3:3 sweep refuted the draft's copy-only/torn-read-prescribing/fabricated-flag wording); **Fight #4** (the hot `live_tp`/`live_sl` decision-tick anchor) fully investigated via a **3-wide I→A** → REAL bug (config-scoped; inert@slippage=0, bites configured-slip+live), fix = **branchless seqlock-mask-select re-arm + A25-mirrored `per_fill_sl`** (NOT `sg_*`-reuse — a-class proved it BREAKS), HOME = folded into the **E.1.3** §Known-folds (AR-12 gap closed); **delete both dead Position serializers** (D-289, ledger/SOURCES lockstep + preserve TICK-magic); **tools-over-grep** arming discipline (D-290, SUBAGENT_ARMING §3 search-first). **TECH_DEBT-227** opened (`partner_pending_pnl`); **TD-168** moved open→closed (A1 sibling of Fight #4). Engine unchanged `0ee227a` (docs/apparatus; doc-CI + independent Stage-5.5 review CLEAN; a mid-session nvim clang-format artifact on `FixedPointN.hpp` reverted → task #8). **NEXT = REFORMALIZE the E.1.2 scaffold** into a coding plan (cascade-tools-first `gen_code_map --composition/--byte-context` → `/precoding-audit-gate` HIGH 5-agent + `/blindspot-scan` B14 → consult → code). **Pickup → `handoffs/2026-07-01-E.1.2-reformalize-handoff.md` (the new ACTIVE handoff).**
> **▶ UPDATE 2026-07-03 (s13) [E.1.2 reformalize COMMITTED + close-out — no engine code] — the uncommitted 2026-07-02 Fable-window triage is closed out.** An `/accept-handoff` pickup found the 2026-07-01 reformalize handoff was one step STALE: a **2026-07-02 Fable-window triage** had (a) REFORMALIZED the E.1.2 scaffold → a full `status:reformalized-coding-plan v1.0` (ship-goal + 8 acceptance criteria + 7-phase plan A.0→G + test-enum + currency corrections, re-grounded HEAD `b10e778`) and (b) annotated every E-subplan with a `fable_fit:` triage line (E.M=HIGH top-pick, E.1.3=HIGH hardest-gate, E.4/E.5/E.8/E.X/E.1.6=MED) — but never committed/closed (no banner, no handoff flip). **Engine forensically PRISTINE** (diff-since-tag `v5.15.5.F.4d.1.E.1.1` = `.clang-format` ONLY · empty working tree · no stash/reset/hidden-branch · HEAD pushed `0 0`) → the drift was design-layer BOOKKEEPING only, ZERO half-done code. This close committed the complete 2026-07-02 work + this banner + the handoff flip; deleted a stray `--help` CODE_MAP-dump artifact. B-Plus line-anchors are 0-fabrication (pre-commit Check A `[ok]`); the residual drift/notfound is dominated by tool false-positives on compound-expression cites (spot-verified ACCURATE: sole-serializer `ShardedSnapshotPersist.hpp:169` + F-096 `Async.hpp:842`) + the E.1.3 fold-block's fix-at-coding comment cites — the gate re-grounds them (only `Fingerprint.hpp:180`→`BacktestPanels.hpp:3157` genuinely off). **NEXT = `/precoding-audit-gate` (HIGH, 5-agent + `/blindspot-scan` B14) on the reformalized E.1.2 — on Fable per its `fable_fit` (the relayout CODE is settled-design + Opus-fine) → consult → code.** **Pickup → `handoffs/2026-07-01-E.1.2-reformalize-handoff.md` (ACTIVE; see its ⏩ Addendum — reformalize DONE, resume at the GATE).**
> **▶ UPDATE 2026-07-04 (s14) [`.E.1.2` Phase C — Position close-out + the NodeCtx-registry unify (Step 1) SHIPPED; the serializer unify is UNDERWAY] — engine `2c11922`+`84d73e0`, PUSHED.** The reformalized-E.1.2 gate (s13) was consulted via a **4-agent Phase-C serializer sweep** (→ `plan_checks/2026-07-04-E.1.2-phaseC-serializer-sweep-synthesis.md`; 5 findings + the **37-row wire map** + the decision set D1-D5). Then coding: **Position close-out** (`2c11922`) — `Sharded_SlotNode` branchless slot→node inverse routed at 6 engine sites (fixes the `:856` warm-restart misattribution) · `_pad_pos[7]={0}` H12 + `Position_Reset`=value-init (Option B) · `check_slot_node_derive.py`+pre-commit Check O+teeth; Position stays **128B** (**D-294** derive-not-store / **D-295** Option-B / **D-296** the accessor). **Phase-C Step 1** (`84d73e0`) — the NodeCtx registry unify: `FOREACH_NODE_CTX_INIT_FIELD`+`RESET_FIELD` → ONE `FOREACH_NODE_CTX_FIELD` (RESET column; `static_assert(...==15)` byte-identical-reset pin; MetaRegistry 2 rows→1; **3693/0**) (**D-297**/D2). Also built the **`cascade.py registry <FOREACH_NAME>`** subcommand (**D-298** — a registry's footprint UPFRONT incl. the H15 enrollment + the compiler-blind `tools/`/`.githooks/` refs; the M7 close of finding the Step-1 blast-radius REACTIVELY ×2) + its doc cohort + the `feedback_run_cascade_upfront` memory. **TD-229** opened (DriftHistory 256-ring → streaming-EWMA scale-forward). Independent Stage-5.5 review CLEAN (3/3; the one flag = CODE_MAP staleness → regen'd `068e040`). **NEXT = Phase C Step 2** (the byte-identical persist view: `regime`+`feeder` field-wise delegates → the 37-row ordered `FOREACH_NODE_PERSIST_FIELD` + SAVE/LOAD/COMMIT walkers → the FROZEN v10 golden → then Steps 3-5 [layout-hash · v11 delta + SHARDED 10→11 + version/retire cascade · D-289/F-096/AM-4] → the MANDATORY close-gate [calls_graph + latency-conformance + suite + init/reset-identical]). **Pickup → `handoffs/2026-07-04-E.1.2-phaseC-step2-handoff.md` (SUPERSEDED — see the next banner).**
> **▶ UPDATE 2026-07-05 (s15) [E.1.2.A "Information Infrastructure" — the tag-unified in-code doc SYSTEM designed + kicked off; E.1.2 2d+ DEFERRED behind it; no engine code beyond 2b/2c] — engine `8648e27`, workspace `8a9214e` + this commit.** A full design session generalized the Check-N `[LAT_EXEMPT]` marker into a **tag-unified knowledge layer** (docs-as-code — ONE comment/tag grammar across code + docs + editor, consumed by the operator's `fox-symdeps.nvim` plugin AND by AI). **D-306** (the SYSTEM) + **D-307** (+ amendments: plugin-owns-the-machine-layer · DERIVED=(b) tool-refreshed via the plugin's `facts.lua` · `[COMMENT]` dated-partitions (newest-first, stack) · hybrid layout (orient-above / code / detail-below) · full-bracketing + `- ` YAML lists + 3-tier render bars · **FULL-focused-conversion NOT as-we-go** [completeness = correctness for tool-consumed tags; as-we-go is itself a rot vector]). LANDED: **2b/2c persist delegates + 8 `[LAT_EXEMPT]` markers** (`8648e27`) · **latency-analyzer shift-robust fix** (Check N marker-based, not FILE:LINE) · **schema v1.0** (`DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md`) · the **E.1.2.A plan** (6 subplans: schema-finalize · tooling · plugin-integration · code-conversion · workspace-audit+consolidation · templates). **E.1.2 2d+ (ordered emitter · guards · REC-A fold) DEFERRED behind E.1.2.A.** The plugin is mostly built (`tools/plugins/fox-symdeps.nvim/DOCS/{ROADMAP,TAG-INTEGRATION}.md`; the `tagadapter.install{}` seam is one call from lighting up). Timeline: tooling in days → full tool-assisted conversion (~1-2 wks) → COMPLETE, then trivial + CI-gated maintenance. Guardrail (operator's): serves *building the engine* — working-v1, then back to the engine. **UPDATE 2 (close-out, same day): schema v1 PROVEN** (two i-class gap-scans, 24 agents, 16 dry-runs → grammar sound, all gaps ADDITIVE, core CONVERGED, variants a bounded remainder) **+ validator increment-1 BUILT + wired into standing CI** (`check_code_tag_blocks.py`: category-set DERIVED from the spec SSoT, in-file `[SCHEMA]` whitelist for mixed-state, freeform-region-safe, self-test 11/11) **+ a committed golden fixture** guarding the schema. **D-308..D-316** captured; **9 commits**; engine `8648e27` UNCHANGED. **UPDATE 3 (2026-07-06, s+): the ENTIRE tag-layer BUILT + the PILOT converted + format APPROVED + the custom-IDE endgame mapped.** `[REFERENCE]`-resolution + the **cache-layout CI gate (D-320)** + `emit_record_layout.lua` + **vocab-reconcile (D-322, 18 rows)** all landed. The **`fox-symdeps` plugin tag-layer is BUILT** (the `tagadapter.install` seam LIT): `tag_grammar_adapter` (read) · `tagwriter` (write-in-place, MERGES — keeps cache-gate layout, appends call-graph) · `tagcontext` (cursor-anywhere-in-block resolve) · `actions`+`menu` (the `<leader>dm` / HUD-`m` context action palette — keyboard-first, hjkl, tag-`[TYPE]`-filtered) · `:FoxSymdepsDerived[!]` (preview / write+autosave) — committed (plugin `e06e5d6`). **Schema refined + PROVEN on a real file: D-326** (code-local comments — struct fields · fn step-comments · sub-group headers — STAY in `[CODE]`; body WRAPPED not rewritten; fixed 2 leaked "moved OUT" framings) · **D-327** (`[DERIVED]` WRITTEN-stable vs LIVE-PREVIEW-volatile) · **D-328** (tag-`[TYPE]` action palette) · **D-329** (`[SECTION]_[label]` bar-wrapped within-fn markers → plugin TOC) · **D-330** (go-forward: full P3 → tools-alongside → plugin+docs alignment → **pop-out doc-viewer = a CUSTOM IDE**; the operator's design-once end goal, now the plan's stated End goal). **PILOT: `RegimeDetector.hpp` — 6 units converted** (RegimeSignals · CumDeltaState · CumDelta_Init · Regime_ComputeSignals w/ 8 bar-`[SECTION]` phases · RegimeState · Regime_Init), code **BYTE-IDENTICAL** (comments-only), validates clean, `--fix`'d layouts — **format operator-APPROVED** (engine `d4812de`). Next = finish `RegimeDetector.hpp` (persist-registry + `CumDelta_Push` + `TickRate*` + `Regime_Classify` + `Regime_ToStrategy` + `Regime_AdjustPositions`, mechanical) → full P3 → tools (the MISSING code-tag index) → the doc-viewer capstone. **Pickup → `handoffs/2026-07-06-E.1.2.A-tag-layer-built-pilot-converted-handoff.md` (SUPERSEDED — see the s16 banner below).**
>
> **▶ UPDATE 4 2026-07-06 (s16) [E.1.2.A — the in-code doc-system DESIGN fully LOCKED + schema-v2 core BUILT; no engine code].** An enormous design session caught that the tag-format was materially INCOMPLETE (proven on ONE comment-light file) + designed the whole in-house IDE toolchain. **D-331..D-338 + the D-fmt SLATE (8 format decisions) RESOLVED:** the integrated architecture (tags=substrate · ONE shared **C++ fact-producer core** D-337 · CI drift-gate · plugin surface) · **stop-conversion/complete-substrate-first** (D-332) · the **3-survey lossless-format taxonomy** (D-333) · the **plugin rework** (unit-card · HUD-vs-panel split · graph-browser · dual-panel compare · cascade-trees, D-334) · the **queryable-graph system frame** + tool inventory (D-335) · **toolchain root-causes RC-A..E** (D-336) · the **version-grammar** (D-338, H21-safe, no history wipe). **NEW design docs:** `DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md` + `format-input-space-taxonomy.md`. **Schema v2 CORE built** (`in-code-documentation-schema.md`: the fence + a grammar-additions section). **Plan re-scoped → the 6-phase build** (schema-complete → propagation+templates → dogfood → C++-core toolchain → plugin rework → convert). **NEXT = the remaining schema-v2 template DESIGN** (`[ROW]`/`[COLUMN]` · labeled `[COMMENT]` · concurrency block · `[ASSERT]` · wire-completeness · ASCII-bar fix · `[SCHEMA]` v2 bump — genuine grammar design, NOT mechanical) → then the 6-phase build. **Pickup → `handoffs/2026-07-06-tag-system-full-design-locked-schema-v2-core-handoff.md` (SUPERSEDED — see the s17 banner below).**
> **▶ UPDATE 5 2026-07-14 (s17) [E.1.2.A PHASE 1 DONE — the tag-format is FROZEN at `[SCHEMA]_[v1.0]`; no engine code].** The design work s16 teed up completed + LOCKED the format. **D-339..D-346:** the **node model** (ONE universal skeleton per `[TYPE]`; per-type variance = a declarative kind→`[DERIVED]`-axes table, never a parser branch — anti-Class-18) · the **3-tier member model** (`[SECTION]` grouping / per-type fact-scoping `[REGION]`·`[ROW]`+`[COLUMN]`·`[VALUE]`·`[WIRE_FIELD]` / nested `[CONTAINS]`) · the **adopt-pass** of D-309's 7 grammar-gap dispositions · a **plugin-alignment check** (walked north-star §6/§7.5) that CAUGHT + folded the missed registry-topology disposition (validated running the plugin check BEFORE the lock) · **wire/persist grounding** (`[WIRE_FIELD]`; all 14 taxonomy shapes covered) · the **v1.0 LOCK** (D-346 — v1.0 not v2 because there was never a v1 in the wild; the s16 "schema-v2" naming is retired). **All 10 per-type worked examples** landed in the schema SSoT (`in-code-documentation-schema.md`, `stage:3-first-canonical` · `locked:2026-07-14`), each dogfooded on REAL engine code → the roll-out template. **9 workspace commits** `b64968e`→`0c9d2a5` (LOCAL, unpushed). Independent Stage-5.5 review **COMPLETE**. MEMORY.md compacted (34 specific-work disciplines retiered to `MEMORY_EXTENDED.md`, lossless). **Accept-known:** `RegimeDetector.hpp` is Caramel's uncommitted pilot WIP — `:543 [STATE]`/`:627 [INIT]` aren't in the locked 76-token set (reconcile on pilot-resume; do NOT touch). **NEXT = IMPLEMENTATION (phases 2-6):** propagate v1.0 → templates → dogfood corpus → C++ core + toolchain (D-337, TaskList #3) → `fox-symdeps` plugin rework → incremental conversion. **Pickup → `handoffs/2026-07-14-E.1.2.A-schema-v1.0-locked-phase1-done-handoff.md` (SUPERSEDED — see s18).**
> **▶ UPDATE 6 2026-07-15 (s18) [E.1.2.A PHASES 2-4 DONE + P6 CONVERSION SPRINT OPEN — the toolchain is BUILT + parity-proven; engine code: ONE comments-only conversion].** **D-347..D-353**, every deliverable machine-verified: **P2 propagation** (validator caught up to the locked contract — ENUM/TYPE/STRATEGY closers + fence-derived ref-subcats + PARITY/SOURCE/URL; `DOCS/CODE_TAG_TEMPLATES.hpp` validator-green per-type copy-source, standing-CI-policed; the code-tag index `DOCS/CODE_TAG_INDEX.md` + `--check` currency; skills/CLAUDE.md/strategy-templates aligned; **the Landmine-5 ENGINE mis-derivation fixed at the path SSoT** — TAG_INDEX's 145 memory rows had been silently missing, restored) · **P3 dogfood corpus** (4 REAL units converted in `tests/schema_golden/`, PROVEN LOSSLESS by mechanical comment-stripped diff; labeled-`[COMMENT]` canonical landed; the fixtures had silently escaped the full-tree scan — rglob doesn't descend the tests/ dir-symlink — fixed) · **P4 the D-337 C++ CORE** (`tools/foxtag/` — parser/scanner/query CLI incl. the plugin-keystone `unit <file> <line>` JSON + the LAYOUT producer (clang; straddler-exact on the 204-record census) + the CODEGEN producer (g++; **RC-A instantiation anchor / RC-C real SIMD width-class / RC-E never-green built in**; EXACT vs the conformance analyzer's ratchet baselines — Regime_Classify 489/8) + **`foxtag_client.py`** the ONE Python binding (pybind11 slots behind it at need) + the first two consumer cutovers (cache-gate backend, index collector) — **the parity gate `tools/foxtag/parity_check.sh` is 8/8**; docs at `tools/foxtag/README.md`) · **P6.1 the FIRST real conversion** — `CoreFrameworks/ExecutionCore.hpp` (engine `d5f8abd`): code byte-identical (268=268 comment-stripped, diff 0), suite **3697/0**, real tool-owned DERIVED (`[SIZE]_[68352B]`), the cross-thread straddle gate ARMED on it. **P5 (plugin rework) = operator's session, seams ready. REMAINING P6: ~191 of 193 engine source files — the RECIPE is proven + documented in the handoff. Pickup → `handoffs/2026-07-15-E.1.2.A-toolchain-done-conversion-sprint-open-handoff.md` (SUPERSEDED — see s19).**
> **▶ UPDATE 7 2026-07-16 (s19) [E.1.2.A P6 BATCHES 1-20 — 87/174 files CONVERTED (CoreFrameworks 53/53 + Strategies 10/10 COMPLETE, ML_Headers 24/35); engine code byte-UNTOUCHED, comments-only] — engine `07c388e` (59 sprint commits), workspace `a9423ae`+this close.** One mega-session ran the proven recipe over 87 files, every one through the full 5-step chain (lossless comment-stripped diff = 0 · validator · cache-gate `--fix` · suite **3697/0** · full-tree = only the 2 accept-known RegimeDetector reds). **Whole-sprint integrity TRIPLE-PROVEN on operator challenge (D-355):** all 85 sprint-touched files comment-stripped-identical to the pre-sprint anchor `d5f8abd`; per-file multiset check over every non-comment ± line = 0 real code differences; the alarming `=`-bar piles at file tails are Caramel's ORIGINAL decoration (git-history-proven), preserved per preserve-voice. **Also landed:** **D-354** — Check-10 Section-D exemptions structurally RE-KEYED (line-anchored tuples → in-code `KEEP-AS-GLOBAL` comment markers in `check_per_node_registry_integrity.py`; conversions no longer break the gate on line shift) · the **fox-symdeps `<leader>dd` Layout fix** (plugin `0b3fa8d`: `template_args` canonical-instantiation substitution + namespace requalify; she must RESTART nvim) · **~60 rot catches** (dead D-294 consumer enumeration, stale "CLAUDE.md item N" cites, shifted anchors). **Close was OPERATOR-DIRECTED mid-sprint** (compaction-degradation concern after spotting the bar piles). **NEXT SESSION = (1) cosmetic-normalize + error-identification pass over the 87 converted files FIRST** (mismatched demote bar pairs: a VERIFIED 7-site list in the handoff — OrderGates:107 · Portfolio:42/:292 · BuildFlags:11 · GateControlNetwork:90 · ROR_regressor:47 · MeanReversion:92, each mechanically confirmed sprint-introduced vs anchor; the compaction-era MLStrategy/LinearRegressionSimple claims were checked FALSE; PLUS operator-caught at close — the [COMMENT]-relocation discipline was largely NOT applied through the sprint bulk: 12 unit-WHY banners stranded ABOVE openers (8 files; EngineCommon.hpp ×5) **+ ~224 candidate banner runs wrapped INSIDE [CODE] fences (55/87 files; upper bound — per-unit split needed: WHY-banner → relocate verbatim to below-code `[COMMENT]`; X-macro row-group/field/step comments STAY per D-326)**; 60/87 files carry zero [COMMENT] blocks (not itself a defect — no-banner units correctly have none); a comment-completeness-vs-anchor audit rides along; normalize GOLD STANDARD = ExecutionCore.hpp + early conversions per operator (ExecutionCore scans clean on every detector); the tail bar-pile collapse = HER call), **then (2) batch 21 = the 11 ML_Headers heavies** (RollingStats → … → NodeModelZoo 3131) → MemHeaders 24 → DataStream 15 → Backtest 11 → GUI 12 → tests 12 → FixedPoint 1. Carry-forwards: TECH_DEBT-119 looks already-done-but-open (needs HER confirm) · ledger line-ref rot class (TD-097 et al) · emitter can't-probe gaps (PortfolioController, RewardTracker — bare `[DERIVED]` notes per D-327). **Pickup → `handoffs/2026-07-16-E.1.2.A-P6-batch20-checkpoint-normalize-pass-next-handoff.md` (SUPERSEDED — see UPDATE 8).**
> **▶ UPDATE 8 2026-07-16 (s20) [E.1.2.A P6 NORMALIZE PASS COMPLETE + UPLIFT TO ONE STANDARD + BATCH 21: 9/11 heavies — 95/174 converted] — engine `7d6249e` (17 session commits c80e67a..7d6249e; suite 3697/0 at every one), workspace this close.** The batch-20 charter EXECUTED to zero: all 5 normalize classes (12 bar fixes `c80e67a` · 12 stranded banners `f1e0c20` · **181 wholesale-wrapped banner relocations across 49 files `fc5f235`** · class-5 completeness CLEAN · GCN tail pile collapsed on operator approval `d999952`). Operator-directed **corpus recheck** re-proved anchor code-identity 0/88 + caught the ONLY batch-era duplication (EventLoopAggregates copied-not-moved banner, fixed `10a786c`). Operator-caught **VINTAGE GAP** → census-scoped uplift `5a6070e` (18 [DERIVED]s · 29 [ASSERT] tags incl. NEW family **REGISTRY_COVERAGE** via the schema 1-line-add · 64 [SECTION]s · 2 [COLUMN] legends · 4 FOREACH wraps) — **the corpus is ONE standard**. Batch 21 then ran **9 heavies thorough-first-time** (RollingStats `3fe14f0` → BanditAlgorithmRegistry `70e2b99` → FlowFeatures `7af459b` → FeatureStandardizer `696afdf` → BanditLearning `76b88f2` → FeatureRegistry `5c45afa` → StampBoundModelConstRegistry `3f02228` → RidgeBlender `7d6249e`), each with rg-VERIFIED enumerated Refinement-A rot kills (stale NO-CALLERS/scope/layout/count claims). **ML_Headers 32/35.** Standing to-zero gates preserved at `tools-staging/e12a-conversion-checkers/` (census + ladder + dupes + detectors + lossless — P4 CI candidates). Close = operator compaction call (the batch-20 discipline again). **NEXT SESSION = the 3 GIANTS: ConfidenceScore (1161) → ModelInference (2007) → NodeModelZoo (3131)**, then MemHeaders 24 → DataStream 15 → Backtest 11 → GUI 12 → tests 12 → FixedPoint 1. Decision: **D-356**. **Pickup → `handoffs/2026-07-16-E.1.2.A-P6-95of174-giants-next-handoff.md` (the ACTIVE handoff — SUPERSEDED, see UPDATE 9).**
> **▶ UPDATE 9 2026-07-17 (s21) [E.1.2.A P6 GIANTS + MemHeaders 24/24 + DataStream 15/15 — 137/174 converted] — engine `2b4a79b` (P6.95..P6.115; suite 3702/0 at every close), workspace this close.** The 3 ML giants (ConfidenceScore / ModelInference / NodeModelZoo) + the FULL MemHeaders + DataStream directories landed via recipe v2 (whole-file read → rot-check → comments-only convert → lossless/validator/battery/suite gate → `&&`-chained commit). Real rot closed en route: EngineTUI failure/state-flag counts 2→11 + 7→11 (registry-verified); TD-237 pre-epoch stamp floor FIXED + TD-238 folded; TECH_DEBT-239 opened (MetricsLog ORPHANED — operator decides delete-vs-revive); A2/A4/A5 at-site pointers landed in BinanceUserData with the E.0.10 register as SSoT. Process: the P6.114 commit-red → AR-1 meta-index bump + the battery-AFTER-last-edit/`&&`-gate rule (P6.115 proved the loop — pre-commit battery caught 3 drifts). **Pickup → `handoffs/2026-07-17-E.1.2.A-P6-137of174-backtest-next-handoff.md`** (the ACTIVE one) — NEXT = Backtest 11 (BacktestPanels 6376 = the largest remaining) → GUI 12 → tests 12 → FixedPointN (max care).
> **▶ UPDATE 10 2026-07-17 (s22) [E.1.2.A P6 — Backtest + GUI + private/EmaCross + Limits/Version → TRACKED corpus 162/163; the FixedPointN vestigial-`FPN_*` finding LOGGED + V1 SEQUENCED] — engine `0a9857d` (P6.116..P6.127; suite 3702/0 at every close), workspace this close.** Backtest 11 + GUI 12 (GUI 12/12 COMPLETE, P6.116..P6.126) landed; the private `Strategies/private/EmaCross.hpp` converted (gitignored — 2-file-lossless verified, no commit); the two top-level SSoT files `Limits.hpp`+`Version.hpp` (P6.127). **Denominator corrected 174→163** (intervening single_core/parity_harness deletions) → **TRACKED corpus 162/163; only `FixedPoint/FixedPointN.hpp` remains** (+ 15 gitignored tests/). **The max-care read of FixedPointN surfaced the vestigial binary `FPN_*` op family** (`.w[]`/`.sign`/`::N` sign-magnitude bodies on the `.v`-only 16B core) — NOT campaign-caused (`git diff HEAD` empty) + NOT a new bug: the **D-163 deferred op-family reshape** never executed (Ship B did `Money_*` D-176, left binary `FPN_*` untouched); the live `.v` surfaces (`Money_*`+certified kernels D-176/D-142; `fp2_*` D-142) are clean+correct; live-vs-dead UNRESOLVED (a build-routing trace = the reshape ship's step 1). **Logged D-357 + TECH_DEBT-242** (CORRECTED from an initial "provably dead" overclaim → "instantiable/unresolved"; independent Stage-5.5 review confirmed the purge). **D-358 = V1 sequencing** (comment standardization → toolchain + plugin functional pass → V1; + the operator's 2026-07-17 plugin-polish scope: persistent-panel add-symbols independent of `<leader>dD`, no-flicker UX, richer info). Disposition: FixedPointN = convert-SETTLED-PARTS-only (leave the `FPN_*` block, do-it-twice vs the reshape). **Accept-known unchanged:** `RegimeDetector.hpp:543/627` reds = Caramel's WIP (forbidden to touch). **Pickup → `handoffs/2026-07-17-E.1.2.A-P6-162of163-fixedpointn-finding-next-handoff.md`** (SUPERSEDED — see UPDATE 11 below).
>
> **▶ UPDATE 11 2026-07-17 (s23) [E.1.2.A — FixedPointN CONVERTED (163/163) + the CI COMPLETENESS GATE built + standing; Phase-C cleanup open] — engine `e9a8ab8` (FixedPointN P6.128 `3657b32` + GateControlNetwork restructure `e9a8ab8`; suite 3702/0), workspace `4872f1e` + this close.** **`FixedPoint/FixedPointN.hpp` CONVERTED → TRACKED corpus 163/163** (22 units; the vestigial generic `FPN_*` `.w` op family LEFT UNCONVERTED per D-357/TD-242; comments-only, LOSSLESS). **THE conversion-COMPLETENESS gate BUILT + calibrated + wired HARD into `check_session_docs`** (`tools/check_conversion_completeness.py` + `_selftest`; **C1** lumped / **C2** missing / **C3** no-DERIVED, with the ≤3-field-POD proportionality exemption + template-tolerant + tier-3-aware + count-helper + same-family-sub-registry exempt; grandfathered baseline `tools/lib/completeness_baseline.txt` = **47**; a NEW lumped/missing unit fails) — the planned coverage-gate increment, catching the **GateControlNetwork class** (a real struct/registry buried in another unit's `[CODE]`, invisible to the validator = a Class-51 vacuously-green shape). **Phase-C cleanup OPEN 1/~48** (GateControlNetwork restructured lumped-block → 4 proper units = the worked template; 47 remain). Also: **`[CLASS]` resolver fix** (scan the `class-NN-*.md` subfiles → all 55 classes resolve; was a corpus-wide dangling-`[CLASS]` gap) · **`tools/CLAUDE.md`** (context-aware toolchain orientation) · **`[DEPRECATED]`/`[MARKED_FOR_DELETION]` vocab** (MARK-never-delete). **Policy calls:** sub-registry family = `[SECTION]` variants riding the parent block (tool exemption); `*_LegacyV1` = block + `[DEPRECATED]` (live wire-compat, not tombstone). **Homed (plan § COMPLETENESS-GATE):** the DERIVED-facts maturation backlog (C4 empty-DERIVED / C5 codegen-write REGRESSION guard [corpus verified CLEAN — the BanditLearning `0 instr` was the live HUD PREVIEW, D-327 respected, NOT a written bug] / call-graph coverage) + the **CALIBRATION-CORPUS** discipline (golden-broken × golden-complete fixtures, one per error class, every tool's `--selftest` asserts against it) + the going-forward "new unit → a `[SCHEMA]` block" rule (codify after HARD-zero) + the foxtag cutover + the plugin V1 pass (D-358; plugin = Caramel's session). Independent Stage-5.5 review **ALL-CLEAN 16/16** (operator personal comments preserved verbatim). **Accept-known unchanged:** `RegimeDetector.hpp:543/627` reds = Caramel's WIP (forbidden to touch). **Pickup → `handoffs/2026-07-17-E.1.2.A-completeness-gate-phaseC-open-handoff.md`** (SUPERSEDED — see UPDATE 12).
> **▶ UPDATE 12 2026-07-18 (s24) [E.1.2.A — Phase-C ACTIONABLE SWEEP COMPLETE (baseline 47→3); north-star pivot] — engine `c36e96b` (5 conversion commits `27036dc`..`c36e96b`; suite 3702/0 + gui ok at every close), workspace `5dc1830`.** Every actionable directory converted to `[SCHEMA]_[v1.0]`: **ML_Headers 13** (`27036dc`) · **MemHeaders** (`eccb032`, +5 function-local exemptions) · **DataStream+GUI** (`33dd595`; PerNodeSnap 277-line nested block, D-340) · **CoreFrameworks 9 capital-core** (`d718a4d`, +`calls_graph_diff` CLEAN hot-path-untouched, 2 nested blocks) · **Backtest 9** (`c36e96b`). **Baseline 47→3 — only the 3 RegimeDetector WIP reds remain** (Caramel's, off-limits). Every chunk: per-file lossless + validator + completeness + the **comment-stripped HEAD-vs-working 0-code-change proof** + build 3702/0 + doc-CI green; **zero engine code changed** (independent Stage-5.5 review: 30/30 files comments-only, voice preserved verbatim, 9/9 deliverables VERIFIED). **First `[DEPRECATED]`** (the 5 ConfidenceScore `*_LegacyV1` wire-readers, verified live-but-legacy via `ShadowLoadLegacyV1`; D-360). **TOOL (D-364):** function-local structs now EXEMPT in `check_conversion_completeness.py` (a struct inside a function body isn't a navigable unit — plugin-aligned; selftest-backed; `bce0b6e`; schema § Coverage propagated) — cleaned 5 pre-existing false-positives. **SPECS (toolchain-core capture):** NEW `framework-patterns/doc-intelligence-toolchain-architecture.md` (one-parser-N-consumers D-337 + grammar-derived + migration-contract D-349) + `meta-disciplines/calibration-corpus-non-vacuity-discipline.md` (D-362) + the **D-363** emitter-coverage amendment to `mechanical-verification-of-derived-code-facts.md`. **D-363** = C4 DERIVED-emitter-coverage pulled ACTIVE (mechanism pinned: `check_cache_layout --fix`'s `refresh_derived` only REWRITES existing tag-lines, never ADDs them → the write-logic + probe-TU coverage is the scoped fix). **NEXT = the north-star pivot** (`in-code-doc-system-north-star.md`): **Phase-4 core-utils** (C4 DERIVED-write / RC-A instantiation anchor / `foxtag update` — Python-authoritative first per D-349) then the **Phase-5 plugin UX** (cursor-track / HUD-PANEL / unit CARD / doc-viewer — Caramel's session, §6). HARD-zero flip + the going-forward rule wait on RegimeDetector. **Pickup → `handoffs/2026-07-18-E.1.2.A-phaseC-sweep-complete-next-northstar-handoff.md`** (SUPERSEDED — see UPDATE 13).
> **▶ UPDATE 13 2026-07-18 (s25) [E.1.2.A — the north-star pivot's C4 DERIVED-write steps 1+2 SHIPPED; next = the corpus-wide fill] — engine `c36e96b` UNCHANGED (zero engine code this session), workspace this close.** **Step-1 (`4b75a2f`):** `check_cache_layout refresh_derived` now PRESENT-OR-INSERTS the `[SIZE]/[ALIGN]/[CACHE_LINES]/[STRADDLE]` quartet into an EMPTY `[DERIVED]` (the reason `--fix` filled 0), idempotent (guards the FPN non-idempotent-apply class), 5-assertion calibration selftest. **Step-2 (`9c88436`):** the `--isolate` mode — per-header `sizeof`-forcing probes materialize every `[STRUCT]` main.cpp under-instantiates (1:1 with the binary by ABI determinism; `using namespace tt;` resolves global+tt:: uniformly; proven across global/tt::/template/multi-instantiation; Caramel's per-struct-isolation insight beat the monolithic probe). **3 design decisions captured (each triple-homed log+spec+plan):** **D-365** grammar/vocab 1:1-propagation + the plugin drift watch-point (`foxtag grammar` SSoT-emitter; the nvim plugin is the native-Lua mirror) · **D-366** register-fit/access-cost per-field axis (RC-F; the "40→1 mov" from `Learning_cpp/deep_dives`) · **D-367** plane first-class discipline-gating axis + the toolchain SELF-HOSTS (`FOREACH_PLANE` registry `{constraints·vocab·[DERIVED] axis-set}` + multi-comment-syntax parser + universal-skeleton/plane-scoped-vocab; rationale: the toolchain is one-producer-N-consumers → engine-grade rigor). Codified **Class 56** (non-idempotent in-place writer) + **Landmine 12** (harness clobbers memory frontmatter on malformed YAML). Independent Stage-5.5 review **ALL-CLEAR 19/19**. **Accept-known unchanged + EXTENDED:** `RegimeDetector.hpp:543/627` AND NOW ALSO `CoreFrameworks/ExecutionCore.hpp` (a cache-padding tweak, mtime this session) = **Caramel's WIP, forbidden to touch/commit/revert** (verified: zero engine Edits/commits this session). **NEXT = Task #4** — `check_cache_layout --isolate --fix` corpus-wide (CHUNKED per directory, gated — triage a new cross-thread straddle as a genuine H6 finding; handle multi-instantiation-template fill + per-header preludes). **Pickup → `handoffs/2026-07-18-E.1.2.A-c4-steps-1-2-shipped-next-corpus-fill-handoff.md`** (SUPERSEDED — see UPDATE 14).

> **▶ UPDATE 14 2026-07-18 (s26) [E.1.2.A — the corpus-wide DERIVED fill + toolchain cohesion + RegimeDetector converted → CONVERSION COMPLETE, corpus 100% green, HARD-zero flipped] — engine `9939ed6` (fill) + `4c076ed` (RegimeDetector), workspace `bfd7328`/`eb3fc2f`/`fbbf421`/`5862e8d`.** **Corpus-wide `check_cache_layout --isolate --fix`** filled ~900 layout DERIVED tags (`[SIZE]/[ALIGN]/[CACHE_LINES]/[STRADDLE]`) + `[ORIGIN]`/`[UPDATED]` provenance across all 8 source dirs (comment-only/lossless, blast-radius gate clean — no cross-thread H6 straddle / size-drift). **D-369** provenance/freshness grammar (`[ORIGIN]_[AUTO|MANUAL]` + `[UPDATED]` stamp-on-change → idempotent; schema fence/table/§WRITTEN-vs-LIVE + arch spec + `check_cache_layout` writer + selftest; retires the ad-hoc `(tool-refreshed…)` prose). **D-370** `match_layout` multi-instantiation REFUSAL (no arbitrary wrong fact — `RollingStats` = tracked remainder Task #15) + explicit-instantiation space-normalize (homed in `isolated-per-struct-layout-probe.md`). **D-371** toolchain cohesion sweep — NEW `[SCHEMA]` version guard `check_schema_version.py` (SSoT-derived from the spec's LOCKED line; wired HARD into `check_session_docs`; D-137 wrapper; TOOLS.md) + foxtag CLASS-subfile resolver fix → **foxtag↔Python PARITY PASS** (Task-#7 cutover prerequisite) + `v1`→`v1.0` fixture standardization. **#6 done — RegimeDetector CONVERTED** (Caramel released her WIP: 4 units blocked `TickRateState`/`RegimeState`/`FOREACH_REGIME_PERSIST_FIELD` wire-persist/`Regime_Init`; comment-only 0-code-change; build 3702/0) → **HARD-zero flip** (completeness baseline **zeroed** = a new un-blocked/lumped unit now fails CI; version-guard WIP-exemption removed; DESIGN_SPECS index regen'd). **Corpus 100% green — all 17 HARD doc/plan checks pass** (C1=0/C2=0/C3=0; foxtag parity green; schema-version + completeness strict). Dogfooding (Caramel's call) surfaced + fixed **4 previously-missed tool bugs** (foxtag CLASS resolver / schema-version drift / multi-instantiation wrong-fact / explicit-instantiation miss) — the payoff. **Accept-known unchanged:** `CoreFrameworks/ExecutionCore.hpp` = Caramel's WIP (forbidden to touch/commit). **NEXT = the plugin lane** (#7/#8/#10 — Caramel's session; foxtag parity green so the cutover prerequisite is met) / per-instantiation (#15, needs `[INSTANTIATION]` full-tuple) / north-star (#11-14) / P4 consolidation. **+ SAME-SESSION SOLO REMAINDER LANDED:** #9 (add-vocab one-action `add_vocab.py` — insert vocab → regen indexes → verify-derives, one drift-proof command; workspace `fa9cc63`) + #11 (RC-F per-field register-fit `check_register_fit.py` + a NEW parity-safe `foxtag fields` command — single-mov-vs-unaligned/multi-op access-cost, ADVISORY H14-tension; corpus 46 unaligned + 319 multi-op surfaced; `layout` parity still PASS; workspace `31c181d`). Remaining = the plugin lane (#7/#8/#10, Caramel) / per-instantiation (#15, gated on the `[INSTANTIATION]` full-tuple) / the speculative north-star #12-14. **Pickup → `handoffs/2026-07-18-E.1.2.A-conversion-complete-hard-zero-next-plugin-handoff.md`** (the new ACTIVE one).

> **▶ UPDATE 15 2026-07-19 (s26 extended close) [E.1.2.A close-out probes + D-372 V1 REDEFINITION] — engine `4c076ed` UNCHANGED, workspace this close.** The extended `/close-session` (operator probes "are you sure"/"positive?"/"are we using the tools?") landed capture + fixes: the meta-harvest (**AR-1** corpus-`--fix` + **AR-8** review-checklist-omission bumps), **TECH_DEBT-243/236** disposition-flips (the C4-fill closed part of TD-243; the conversion-complete MET TD-236's trigger), an independent **a-class adversarial pass** (verdict SUBSTANTIALLY-COMPLETE; its LOW residuals fixed — handoff completeness / the parity-safe-check spec-promotion / plan-status / ExecutionCore pre-D-369 note), and the **`check_meta_registry.py` straggler fix** (rolled its own `Path(__file__)` root → false `exit 2` from the workspace-path invocation → switched to the canonical `check_doc_metadata.ENGINE` + a teeth `--selftest`; the exact anti-pattern D-372 closes). **D-372 (operator brainstorm): V1 REDEFINED** — the toolchain PERVASIVELY-CONSUMED from ONE core (foxtag + `check_doc_metadata`; no tool rolls its own) across **CLI** + **local CI, no GitHub** (+ a NEW pre-push gate; keeps in-code `[SCHEMA]` docs synced-with-code automatically) + **the plugin as a NON-CLI APPLICATION of the whole toolchain** (scope increase — the custom-IDE endgame made primary); **plugin-first sequencing** (#7/#8/#10 AHEAD of #12–14). Next session = DESIGN D-372's open questions (producers-vs-gates split · `tools/core/` rename · pre-push scope · plugin feature-set · import-from-core lint) → BUILD plugin-first. Full: decision log **D-372** + north-star **§8.6**. **Pickup → the SAME active handoff** `handoffs/2026-07-18-E.1.2.A-conversion-complete-hard-zero-next-plugin-handoff.md` (updated with the D-372 direction).
> **▶ UPDATE 16 2026-07-19 (s27) [D-372 DESIGNED + homed → `E.1.2.B` sub-ship opened; toolchain V1 = plugin-first, all-in, versioned `0.x→1.0`] — engine `4c076ed` UNCHANGED, workspace this close.** A design session (3-agent survey — plugin / toolchain / vision) reconciled planned-vs-actual-vs-missing + resolved D-372's 5 open questions. **Convergent finding:** the one-core-N-consumers thesis is realized in the foxtag CORE but NOT the consumers — the nvim plugin makes ZERO foxtag calls (a native-Lua grammar MIRROR; `tagadapter.install` never called → dormant), and 14 tools roll their own repo-root (one — `check_conversion_completeness.py:32` — a portability-DEAD HARD CI gate, green only by path-literal-match, verified). **D-373** toolchain semver `X.Y.Z`, MAJOR-tied-to-`[SCHEMA]`, `0.x→1.0.0`, one `TOOLCHAIN_VERSION` SSoT · **D-374** a codified one-action UPDATE orchestrator (idempotent, verify-after; gates stay verify-only) · **D-375** `E.1.2.B` opened (AI-plane ADOPTED as a V1 surface; all-surfaces-in-V1 per the operator's cohesion call; producers→plugin+CLI / gates→CI resolved; `tools/core/` rename DROPPED for `foxroots.py`). **E.1.2.B `0.x→1.0` roadmap:** `0.1` foundation (`foxtag grammar --json` sets + `foxroots.py` + import-lint + plugin foxtag-client + kill the 2 UNIT mirrors + tag tests) → `0.2` update-skill → `0.3` plugin cutover (parity+soak) → `0.4` tag-native surfaces → `0.5` UX+RC-F → `0.6` AI-plane → `0.7` pre-push+producer/gate-role → `0.8` north-star substrate (#12-14) → `1.0.0` cohesion. **E.1.2.B inherits E.1.2.A's gate on the SoA relayout** — the full toolchain V1 gates resuming engine structural work (the operator's deliberate defer-the-engine call). 2 NEW Stage-2 DESIGN_SPECs (`toolchain-semantic-versioning` · `one-action-toolchain-update-orchestrator`) + `tools/CLAUDE.md` amended. Plan: `subplans/2026-07-19-E.1.2.B-toolchain-v1-plugin-first.md`. **Pickup → `handoffs/2026-07-19-E.1.2.B-handoff.md`** (the new ACTIVE — supersedes the E.1.2.A pickup; 0.1 foundation BUILT, 0.1.5 tool-I/O DESIGNED, next = build 0.1.5).
> **▶ UPDATE 17 2026-07-19 (s28) [E.1.2.B — `0.1.5` tool-I/O substrate + `0.3` plugin cutover BOTH SHIPPED; next = `0.2` REFRAMED to the CONTRACT+GATE layer] — engine `4c076ed` UNCHANGED (zero engine code), workspace + plugin this close.** **`0.1.5` (`498f2ad`/`0058be3`):** the tool-I/O envelope + uniform `{schema, rows}` record-set payload, with the payload COLUMN schemas lifted OUT of both implementations into a language-neutral **schema-as-DATA registry** (`tools/lib/toolio_schemas.json`, Caramel's call — emitters READ their schema, so a vocab change is a data edit, not a recompile + 90-file sweep). Python `toolio.py` (emit/read/validate, 4/4 selftest incl. 3 negative controls) + the C++ twin in `foxtag grammar --json` — proven **byte-for-byte identical incl. key order**, wired as parity §3b (191 members). `TOOLCHAIN_VERSION` = `0.1.0` (D-373 SSoT; missing → `exit 2`). **D-384** `findings/1` restructure: `status.findings` is a CROSS-CUTTING envelope field so its schema lives ONCE at envelope level and `verdict` is NOT a payload kind (the structural fix, not a per-kind mirror). **`0.3` (`6dcbbd7`, plugin repo):** the plugin's native-Lua grammar mirror is CUT OVER to foxtag — `nodemodel.lua` derives the unit set live (verified: **10 types incl. `ASSERT`, 6 closable** vs the retired table's 9-and-no-ASSERT — decisive proof it is live, not dormant), `enclosing_block` now filters to CLOSABLE openers (LIGHT units have no `[END_]`), `tagadapter.install` finally called. Parity §3c is **non-vacuous** (ran/skipped verdict + `count > 0`). Soak-pending-delete: both `_RETIRED_UNIT` copies stay in-tree until a NON-SKIPPED parity pass + dogfood soak. **The session's real lesson → M10/D-385/T12:** delegation is safe where the acceptance oracle is **TOTAL** (the C++ emit had byte-identity — the check IS the review) and needs a mandatory context-carrying hand-review where it is **PARTIAL** — everything went green while a hand read still found 2 defects no test could express (a per-token subprocess STORM on a present-but-broken binary; a hardcoded repo name in a publishable plugin). Sharpest instance is self-inflicted + kept verbatim in D-385: I asserted a "conformance gap" from an **unanchored** grep that matched selftest string literals — a FALSE finding in a decision log, from counting matches instead of reading them. **Structural response, not a resolution:** **D-386** output GOLDENS (make oracles TOTAL) + **D-387** `0.2` REFRAMED from "update skill" to the **toolchain CONTRACT + GATE layer** + `tools/CLAUDE.md` gains a **T1–T12 invariant index** (citable IDs mirroring H1–H22) + a gotchas section harvested from this session's actual misfires. 2 gate blind spots found + homed: **TECH_DEBT-245** (blind to gitignored source) + **246** (never checks FUNCTIONS — **501** `[FUNCTION]` blocks vs 195 `[STRUCT]`, the dominant unit type unguarded) — two holes in ONE enumerator, fixed together at `0.2`. **D-388** the 5-surface repo-split: **PARKED** (one system; 3 repos already cost real coordination tax). 2 new memories indexed. **NEXT = `0.2`** contract docs + output goldens + `parity_check.sh` extension + TD-245/246. **Pickup → `handoffs/2026-07-19-E.1.2.B-0.1.5-0.3-shipped-next-0.2-contract-gate-handoff.md`** (the new ACTIVE — supersedes the E.1.2.B pickup).
> **▶ UPDATE 18 2026-07-19 (s28 REOPENED close) [E.1.2.B — M10 codification COMPLETE + a citable-ID integrity class found and partly closed] — engine `4c076ed` UNCHANGED, workspace this close.** The `E.1.2.B` close was committed (`3ba0fe8`/`4aecc95`) then **reopened on operator instruction** (*"finish the M9 codification residual … and anything else similar"* + *"be sure we actually captured and harvested everything"*). The reopen found **six real defects the first close missed — including one the first close itself created.** **M10 COMPLETE** (all 8 §11.5 steps: spec `acceptance-oracle-totality-before-delegation.md` · `/decision-check` Stage 0 · `/readiness` Check 47 · table row · memory · step-5 CI deliberately OMITTED + labelled, since oracle totality is a judgment not a parseable property). **M9→M10 RENUMBER:** the slot was earmarked for enumerate-set by the `.E.0.10` close (2026-06-12) + `DESIGN_PHILOSOPHY:819` (2026-05-29) — older claim wins; D-385 carries a numbering tombstone so the two prior commits still resolve; **M9 promoted** with an explicit ⚠️ PARTIAL marker (TECH_DEBT-248). **§11.5 was 4-of-8 DEFECTIVE** (M4 wrong check range · M7 dead hook path · M6's canonical spec unreachable · `check-36` citing a memory that never existed · an "aggregating index" that aggregates neither M2/M4/M5/M6 nor the right range) — all repaired. **M5's gate gap CLOSED NOW** (capital-facing, not deferred): `/precoding-audit-gate` had ZERO train-serve trigger and the M5 spec was cited by ZERO skills, so the highest-capital meta-discipline was unreachable from any gate despite §11.5 claiming the trigger since codification. **D-389 — the real find:** citable IDs are a knowledge-base primitive and NOTHING enforced one-ID-one-meaning. Verified corpus-wide: `M9` double-defined · **`D-1`..`D-13` double-defined** across two decision logs with genuinely diverging content (`D-7` = opposite ImGui dispositions) while the older log still read `status: pre-plan-body` · `WH-7` cited canonically by 5 live docs with no SSoT row · **8 dangling TECH_DEBT citations** · **5 split-brain entries** in both open.md and closed.md (`-107`: 49-globals-OPEN vs 47-globals-CLOSED) · `D-245/246` minted-then-dropped. **This is H21 transposed to the doc plane.** Fixed in-flight: the renumber, the v1 supersede banner (ONE frontmatter line retiring 13 ambiguous IDs), WH-7 enrollment. **Why CI never saw it:** `check_code_tag_blocks.py:170` builds membership by MENTION over raw ledger text — a set that admits anything it has seen **cannot go red** (Class-51). **Prevention:** ONE EXTENDED tool (it already indexes 5 of 8 namespaces; it just never reads prose) merged with the §11.5 coverage checker → **TECH_DEBT-249**, sequenced into `0.2`; namespace-SCOPE overlap deliberately NOT mechanized (convention: prefix finding IDs `F-M1`). **TECH_DEBT-250** homes `check_close_out_completeness.py` — named by M8 as its candidate CI, documented overdue across 6+ sessions, still absent; this session is recurrence N+1 (six defects, zero mechanical detection, one operator prompt). **D-390 — doc rot is THREE mechanisms, folded as `0.2` (g).** The operator proposed a plane-qualified identifier (`D-tool-385`) to solve the namespace overlap; costing it killed the shape and exposed that "doc rot" had been conflated into one problem. **(1) ID ambiguity** → (e). **(2) Citation staleness** — `file:line` pointers to moved/renamed/deleted code — is **already detected and ignored**: B-Plus prints `146 drift + 4 OOB + 6 missing` on EVERY commit as advisory (the decision log cites `CoreLatencyStats.hpp`/`CoreModelZoo.hpp`, both gone — the documented `per-core`→`per-node` rename). Fix = **severity flip** (MISSING+OOB HARD, drift stays advisory so the gate keeps credibility) + a **rename-map resolver** that tries known renames before declaring MISSING. **(3) Content staleness** — the v1 decision log read `status: pre-plan-body` for 7 weeks after being superseded — gets a mechanical supersede check. **Scoped baseline-and-red-on-new per the operator's *"historical isn't extremely critical"* call:** grandfather today's staleness, fix the ~10 hard ones, red anything NEW; explicitly NOT mass-repairing the 146 drift. **Identifier scheme REJECTED on merit:** the letter already encodes plane (`H`/`T`/`Class`/`AR`/`M`/`B`), plane is METADATA so embedding it makes identity MUTABLE (a reclassification would rename a citable ID — the exact H21 violation this work prevents), and ~21,500 live citations include immutable commit messages. **ONE RESOLVER, THREE CONSUMERS:** `check_code_tag_blocks.py` already resolves `[REFERENCE]` citations inside CODE tag-blocks — lifting it to `tools/lib/citable_ids.py` serves the CI gate, the comment TAG system, and the plugin's `0.4` doc-viewer (a dead `[REFERENCE]` is the dead link it must not open). D-337 applied to citations. **FOLDED INTO `0.2` (operator call) — not left to triage:** the 8 dangling + 5 split-brain TECH_DEBT IDs + the tombstone/index-count/zero-pad/`F-M1`-prefix residue are now `0.2` deliverable **(f)**, sequenced after the guard so the set is mechanically enumerated. **Operator's reframe, now codified:** *"tech debt building up in the workspace is almost worse than the actual engine code, because it leads to dated and rotted information, and we cant make decisions with that"* — engine debt degrades the PRODUCT, knowledge-base debt degrades the DECISION INPUTS. Folded into `feedback_process_weight_by_surface_blast_radius` as its second refinement (the knowledge base IS a fact-producer) + **AR-14**. **Meta-lesson:** `-101` was found once, patched once, never swept — 8 siblings survived; patching an instance without sweeping its class IS the M7 signal. **NEXT unchanged = `0.2`** (contract + goldens + parity extension + TD-245/246 + now TD-249/250). **THIRD PASS (operator: *"did we fix and update everything?"*) — the honest answer was NO, and the re-verify found it.** Everything previously CLAIMED had landed (verified in the committed tree), but three first-agent findings had been neither fixed NOR homed — **unhomed debt**: **M1**'s `/trace-deps` cohort-parity amendment, **M2**'s `/parity-check` cross-tool emit-site section AND its plan-template wire-format section, **M3**'s "False-positive surface" template requirement. All four had been claimed by their § 11.5 rows *since codification* and none had ever existed. **All now written**; the rows were corrected to say so (incl. M2's amendment being Section **F**, not E — E is stamp-body schema parity). TECH_DEBT-**251** opened for the classes 1-24 FP-surface backfill — created immediately because citing it first would have been a dangling reference, i.e. the exact class this session closed. **M3 still has no DESIGN_SPEC and no memory, so its codification stays PARTIAL.** **Pickup → the SAME active handoff** `handoffs/2026-07-19-E.1.2.B-0.1.5-0.3-shipped-next-0.2-contract-gate-handoff.md` (updated with the second-phase section).

> **▶ UPDATE 19 2026-07-19 (s29) [E.1.2.B `0.2` — the corpus contract DESIGNED + first goldens BLESSED; the plan SHRINKS] — engine `4c076ed` UNCHANGED (dev-plane only), workspace this commit, plugin `6dcbbd7`.** ZERO consumer code written; **eleven decisions** (D-391→D-396, C-391→C-396). `0.2` went through an armed I→A cascade, a scoped re-sweep, an operator-called **precedent** sweep, and an adversarial **anti-pattern** sweep — and each one materially changed the shape. **Landed (data only, nothing reads it yet):** `tools/lib/corpus_contract.json` (single-sourced corpus membership; profiles · symlink policy · path-arg semantics) + `tools/goldens/{corpus--validate.txt (171), corpus--derived_facts.txt (166), README.md}` — the toolchain's **first output goldens**. **Three reversals, each measured not argued:** D-391 "anchor don't layer" WITHDRAWN (already codified 7 weeks earlier as D-74) · C-392 proved `0.2`(e) is **NOT** a lockstep C++/Python change (hardening drops exactly one uncited id → `validate` byte-unchanged → parity green with Python alone) · C-393 DROPPED the frozen-fixture corpus (the fixtures are transitively LIVE — 9 and 20 `#include`s; scores 0-of-3 on the defects being chased). **D-395 (precedent sweep): the plan SHRINKS, does not restructure** — `0.2`(a)-(g) survives intact, but 5 deliverables re-scope because the work exists (`0.5` RC-F is BUILT + selftested; `0.2`(b)'s spec would be a second SSoT; `0.2`(e)'s taxonomy is already AR-14; `0.8` FOREACH_PLANE is ~85% in the toolchain-architecture spec; `0.4` is ~95% verbatim in north-star §6). Exactly ONE new codification earned: *differential-gate → absolute-gate contract widening*. **D-396 (anti-pattern sweep, the SHAPE change): the first golden pinned MACHINE-LOCAL state** — 31 untracked entries incl. two mkstemp scratch files; a fresh clone diverged 31 lines unconditionally, **re-instantiating the class `0.1` closed** (machine-local PATH → machine-local CONTENT) and falsifying the plan's own acceptance criterion #4. Fixed by splitting SCAN vs PIN population, which **preserves D-393 pt 2 verbatim**. Found by an adversarial pass on the orchestrator's own artifact — the AR-8 shape working as intended. Also: `public-private-boundary-and-ecosystem-discipline.md` **SUPERSEDED** (stated the code-only-public law as governing ~2 weeks after all-private); memory `project_workspace_remote_gone_commit_only` **CORRECTED** (all three remotes are alive — the stale "never push" rule had left 77 workspace commits unbacked-up; now pushed). New: TECH_DEBT-**252** (plugin repo runs no hooks) + **253** (`check_schema_version` scans ZERO `.py` — wired HARD, unreachable via the `tools/` dir symlink). **NEXT = WIRE BB-1** (Python `resolve_paths` → C++ twins → `parity_check.sh` §1b → TTY-gated bless). **Pickup → `handoffs/2026-07-19-E.1.2.B-0.2-contract-blessed-sweeps-done-next-wiring-handoff.md` (the ACTIVE one).**
> **▶ UPDATE 20 2026-07-20 (s30) [E.1.2.B `0.2` — ALL THREE BUILD-BLOCKERS CLOSED; the GATE LAYER is wired] — engine `4c076ed` UNCHANGED (dev-plane only), workspace this close (window `2167d9d..HEAD`).** **BB-1** the corpus contract wired on BOTH readers — the handoff named 1 `--paths` site, C-395 named 3, measurement found **6**; the sort divergence (contract=merged / Python=unsorted readdir / C++=hpp-grouped) is closed and verified BY CONSEQUENCE, since parity sorts before diffing and the golden coincides only by accident of 2 lowercase-rooted `.cpp`. **BB-2** the validity leg — parity was purely DIFFERENTIAL, so two implementations agreeing the corpus was broken printed `OK : exit codes identical (1)` and reached PASS; teeth proven by replanting C-391(iii)'s own experiment. **BB-3** `registry_id` — whose framing was WRONG: the denominator is not 192 but the `Mn` specs, because `registry_id` is the REVERSE map and every namespace already has a defining-form anchor (TD-249's own parse spec resolves `Class N` **by filename**). **(c) WIRED** as pre-commit **Check T**, trigger-scoped, dogfooded on its own commit. **(e)** the citable-ID guard landed as **Check 14 folded into `check_capture_audit`** — not a new tool, because capture_audit carried a SECOND parser over the same `<!-- D/C/F -->` form and was **silently skipping 19 sentinels** (D-372..D-381, this ship's own origin decisions, never verified to have a paired STATUS). **TD closed:** 245 · 255 (+ its `--update-budgets` sibling; all three committed baselines now share ONE TTY-gated bless control) · 254(a)(b) · 249 guard-half. **TD opened:** 256 AST matchers · **257 HIGH** compile_commands 1:1 · 258 plugin registry surface · 259 B-Plus `--all` unrunnable · 260 clang-tidy. **TWO PRE-EXISTING BROKEN GUARDS repaired:** the H21 self-test had a DEAD TOOTH (hardcoded `SHARDED_SNAPSHOT_VERSION|8` vs a ledger at `|10` — the sed was a no-op, so it reported *guard broken* when the truth was *fixture failed to plant*), and `check_identifier_retirement` itself was BROKEN through the workspace path; the lint that should have caught it was HALF-BLIND (pathlib spelling only), hiding 4 `os.path` rollers of which 3 were measurably broken including a HARD pre-commit gate. **The roll-your-own-root class is now FULLY CLOSED — 11 rollers migrated, baseline EMPTY.** **3 NEW/EXTENDED specs:** `differential-to-absolute-gate-contract-widening` · `advertised-capability-never-exercised` (5 instances in one session, incl. one the author created that same day) · the blessed-output lifecycle folded into `calibration-corpus-non-vacuity-discipline` (promoted 2-draft→3-first-canonical). **D-397** the static-analysis arc placed at named increments (TD-257→`0.5` PRECONDITION · TD-258→`0.4` CONSTRAINT · TD-256→`0.6` · LSP→`1.x`; **formal verification REJECTED on merit**) · **D-398** `0.2` order (e)→(f)→(g)→(d) · **D-399** the guard's shape. **`0.2` STATUS: (b) (c) (e) done · (a) partial · BB-1/2/3 closed.** **NEXT = (f)** the ID-triage corpus — re-derived 2026-07-20 to 23 unique findings (was 51) after an a-class review refuted the earlier framing: 23 DECISION collisions were spec-exempt noise, and `PARITY-39`/`TECH_DEBT-16`/`TECH_DEBT-101` all had recoverable content and are now defined. Re-run `--check 14` rather than trusting any count here. **Pickup → `handoffs/2026-08-09-E.1.2.B-0.2-g3-humanization-cpp-policy-handoff.md` (superseded 2026-08-10 — see UPDATE 24).** *(UPDATE 23, 2026-08-09: (g) 1–3 DONE incl. the pipe-swallowed Check-A gate fix · plugin humanization arc = 5 slices · **D-412 C++-first CONFIRMED** · ideas §1–§10 = the operator's recovered list, provenance recorded. Next: (g)-4 [design decided] or plugin trees — either thread.)* *(UPDATE 21, same day: four blind guards closed — a vacuously-green HARD gate, a 12-site anchor re-encoding, the un-enumerated `--close` ledger writer, and both Stage-4.5 prose recipes. D-405/406/407 · TECH_DEBT-263 · Class-51 modes E+F · AR-16. (f) is STILL the next action, unchanged.)* **▶ UPDATE 22 (2026-08-07): (f) COMPLETE** — live corpus **0**, baseline **EMPTIED** (mechanical DoD per its own header). Last 3 dispositioned at operator triage: **-102** retroactive OPEN (the `.B.3` closure claim proven false; homed on the real surface, a `/parity-check` cross-walker wire-key amendment) · **-125** TOMBSTONED, split per D-409 → **-266** (pattern anchor, closed) + **-267** (DriftHistory deferral, open) · plan-local **"034b"** promoted as **-268** (closed-superseded by Ship-A) with the citing spec repointed · **D-245/D-246** decision-slot tombstones written. **NEXT = (g)** (order D-398; D-408 extended its scope). Remaining operator-TTY: the GOLDEN bless (additions-only; ship-close cadence per D-410). **▶ UPDATE 24 (2026-08-10): the DERIVED-facts INTEGRITY arc (D-413/D-414/D-415)** — operator-caught `[STRADDLE]` fabrication → 4-I+A sweep → tri-state fact-core (leaves 1–3 SHIPPED, 3 repos) → H6 gate now **HARD `--strict-new`** over a SHRINK-ONLY baseline → **D-415 defers ALL C++ to v1** (foxtag frozen-kept; Check T PARKED, `FOXTAG_REARM=1`). Class 57 + the pipe-swallow M7 hook + TD-269 + E.1.2-plan fold landed. Disposition SSoT = `plan_checks/2026-08-10-D414-toolchain-sweep-finding-register.md`. **Pickup → `handoffs/2026-08-10-E.1.2.B-derived-facts-integrity-arc-handoff.md` (the ACTIVE one).** Leaf-4 remainder + (g)-4 are the next threads. **▶ UPDATE 25 (2026-08-10, session close): `0.2` IS COMPLETE — (b)(c)(e)(f)(g)(d)(a) ALL CLOSED (D-416 (g)-4 staleness pair · D-417 (g)-5 resolver lift · D-418 (d) update_toolchain orchestrator [maiden run: 40 cite auto-repairs; completed-sprints = historical tier, corpus re-scoped] + (a) DOCS/TOOLCHAIN_CONTRACTS.md). Post-close triage: 8 straggler/fired-trigger ledger closes on code-read (TD-140/163/175a/204/250 re-filed; TD-119/120/126 closed — all were shipped-work-never-filed); contract-stale queue 0 WARN / 55 INFO. Pickup pointer UNCHANGED (the same ACTIVE handoff, kept continuously current). Next: 0.4 plugin arc · 55 INFO re-homes · task-#6 id-row half · E.1.2 (engine).** **▶ UPDATE 26 (2026-08-10→11 overnight, session close): the PLUGIN CYCLE CLOSED + the tag-native feature wave — engine `a71b893` · workspace this close · plugin `80dba5d`.** Pickup re-derivation verified the short close (one stray: Notify.hpp `[CONSUMERS]`, committed `d5a6881`) → **AR-8 PAID** (a-class over leaves 2/3: leaves STAND, ZERO false exemption reasons; holes 7-10 CLOSED ws `6df91fa` — OR-fold arming [armed-set delta ZERO over 198 blocks, enumerated] · shrink-only ENFORCED + TU-scope-honest orphan pawl · suite-row HARD · exempt tier) → **the 0.4 doc-viewer end-to-end** (`citable_ids --resolve`/`--where` envelopes + registry rows; docview float/`p`-pin/chooser, all 11 fenced subcats routed-or-named; corpus MINED **550+132 `[REFERENCE]` ids** on operator GREEN — `mine_reference_tags.py`, mine-never-invent, 14 teeth) → **operator-directed 3-agent parity fleet** (I-1/I-2/I-3, ~30 divergences) → register CLOSED **zero-tail** same night (one registry/runner/renderer · the operator's LAYER-STACK rule · dm-EQUAL · menu-as-root · keymap registry with derived docs · notify voice ×71 · menu-seam teeth; suite 42/42) → **the tag-native wave**: R3 `unitindex` tree enrichment + `/`+`T`-picker tag filtering + `<leader>dt` browse-by-tag + TAG ADD (✎, vocab-derived) + the dashboard REWIRED onto the tag corpus (origin-TU false-facts bug dead; dir→file→list with per-group folds, `g` grouped⇄ranked). **Codified:** `reports/` verbatim-at-receipt (SUBAGENT_ARMING §6.5 + 2 memories + frozen-path enrollment) · TD-254 CLOSED incl. the FIRST `stage: 6-cadence-locked` promotion · TD-270 opened (reference two-cores) · register-pressure idea (ideas §11.5, 0.5/TD-257) · `.toolbus/` first-consumer shift (§11 v.5). **E.1.2 ARMED:** c-class currency verdict (`reports/2026-08-11-e12-currency/`) — the plan is ONE SUPERSESSION LAYER BEHIND (Steps 1/2a/2b/2c shipped 2026-07-04, `2c11922`+`84d73e0`+`8648e27`); pickup = the 11-item punch-list (amendment-9 banner first) then the D-305 tail (`FOREACH_NODE_PERSIST_FIELD` grep-zero); amendment 5 EXECUTED; ship close retires exactly the 4 engine baseline keys. Golden blessed **888** (operator TTY, +D-417/D-418/TD-270, committed at close). Disposition SSoTs: the D-414 register + the plugin-parity fleet register + `reports/`. **Pickup → the SAME active handoff (fourth-boundary frontmatter). Next: E.1.2 punch-list → the D-305 tail (ENGINE — the fun part) · 0.4-infra rides 0.5 · 55 INFO · #5/#6.** **▶ UPDATE 27 (2026-08-12, 60-min session): OPERATOR ROUTING — the PLUGIN arc finishes BEFORE the engine E.1.2 resume** (UPDATE 26's "Next: E.1.2 punch-list first" is RE-ORDERED, not voided — the c-class report + punch-list stand ready; the 3 frontmatter edits started at pickup were REVERTED clean, and every key report anchor was re-verified live at `a71b893` [the report's `Portfolio.hpp:488/:901` partner-field cites do NOT verify — re-derive at the dive]). **TD-258 half 1 LANDED** (plugin `9d6c98b`: `toolio_kinds.lua` consumed-kind registry + `:checkhealth` parity leg vs `toolio_schemas.json` + suite tooth; 43/43; tri-state refusal per Class 57) — a new producer kind with no plugin surface is now VISIBLE; entry stays OPEN (remaining: repo-floor tooth + kind-keyed view derive; fix-shape's `parity_check.sh` home re-pointed to the live floor, that harness parked at D-415). **LATER SAME SESSION — TD-258 CLOSED (entry moved open→closed):** half 2 landed as `assert_consumed` LOAD-BEARING at both decode seams (docview defining_site/1+cited_path/1 · nodemodel grammar/1 — plugin `3566412`; wrong/unregistered kind = named runtime refusal) + `tools/check_toolio_kind_parity.py` wired **HARD** on the floor (6-teeth selftest; strict-parse refusal per Class 57; teeth + live rows in `check_session_docs.sh`) + TOOLS.md/FEATURE_LOOKUP enrollment. Drift loud at FOUR layers (runtime / :checkhealth / suite 43/43 / floor). **0.5 OPENED same session — the TD-257 SUBSTRATE landed:** `tools/compile_command.py` (the ONE command source; ideas §2's terminal half usable TODAY — pasteable `(cd … && <exact cmd>)` per db entry, multi-target TUs never collapse; `--json` = NEW `compile_command/1` envelope; 7 teeth on the floor) + **the TD-258 gate FIRED LIVE on its first real producer** (missing-consumer caught at the floor; exempt-tier'd "view rides 0.5" — the producer-first path working as designed, same night it shipped). TD-257 progress-noted (closure = consumers cut over + provenance-stamped facts + wrong-flags non-vacuity + the shipping-build db). **BUILD HALF LANDED next sitting (2026-08-13, engine `2654ad4`):** `CMAKE_EXPORT_COMPILE_COMMANDS=ON` (real per-build-dir dbs; live wrong-flags evidence on first look — Release `-DNDEBUG` absent from the editor db) + `./build.sh asm` (1:1 objdump sidecars from the SHIPPED binaries — the only honest per-function asm under `-flto`; provenance-stamped; `Notify_Send` proven inlined-everywhere, the truth behind the empty 1:1 render). **CARDS-LEAF VIEWER HALF LANDED same sitting (plugin):** `asmshipped.lua` `<leader>ds` — the SHIPPED-asm card (sidecar-read, never recompile; FOUND w/ provenance + cross-binary "also in" · INLINED-AWAY by name · ⚠ STALE sha16 banner · refusal sans sidecars; newest-first sweep) + `asmexplorer` relabeled the honest "editor-flags approximation" + health sidecar leg; suite 44/44; live-smoked (Portfolio_Init 4 blocks/24 refs exact; Notify_Send 8 standalone fleet-wide yet inlined-away in engine_gui — the bare-`0` dogfood case now legible BOTH ways). `compile_command/1` stays exempt-tier'd (asmflags is the EXPERIMENTAL-set store by design; the envelope's consumer = the probe cutover). **SYNC LANDED same sitting (engine `dc6affc` + plugin `730cee1`):** `-g` on the 4 LTO targets (zero-codegen; verified through the LTO link) · sidecars AUTO-EMITTED by every build (`emit_asm_for_dir` chained — always 1:1 with live, the operator's ask) · `objdump -l` → the `<leader>ds` card is BIDIRECTIONALLY cursor-synced (both panes highlight; inlined-from attribution to TRUE origin lines; per-function SHIPPED instruction count in the header — live smoke: Portfolio_Init 102 insns, all mapped, inlined Position_Reset attributed). Roadmap captured ideas §12 (operator-confirmed rungs: --inlines attribution · per-line cost chips · insn-vs-H7/H8-budget join · §11.5 register-pressure UNBLOCKED · cross-binary diff · perf-annotate ceiling). **TD-257 CLOSED 2026-08-14 (entry open→closed):** flags sealed at every consumer — latency checker HARD-derives from `build/`'s db (pinned list had drifted 4 count-affecting axes; wrong-flags tooth 82-vs-30; budgets operator-blessed under real flags, hot-path HOLDS, `_provenance` recorded) · plugin `flags_for` + `compile_command.py` resolve SHIPPING-FIRST with the editor symlink as the liveness fallback (the operator's best-of-both split) · the card reads the sidecars (the binary itself). Riders: in-editor BLESS rows (`:terminal` = real pty, D-394 intact) · `./build.sh test` chains the conformance report (non-fatal; gate stays pre-commit) · the follow surface + the 'for'-bug fix chain (tagged→treesitter→keyword-guarded cword) + wheat-mix sync band w/ `sync_alpha`/`sync` dials (plugin `..3db363f`). **▶ THE PLUGIN ARC CLOSED (2026-08-14, operator call — at FEATURE-COMPLETE 0.5, deliberately NOT v1.0.0: v1 remains the D-415 C++-conversion milestone, whose parked gates [Check T, FOXTAG_REARM] re-arm only there):** the 0.5 feature wave complete — RC-F card (`<leader>dR`) · the shipped-asm BROWSER (follow · bidirectional sync · ⏎ marker/callee/caller · b · I inline-attribution · r · ?-help · budget/SIMD/branch chips · ▲ data-dep marks) · output log (`<leader>dn`) · in-editor bless. Verification battery GREEN (load-all · 46/46 suite · live smokes · floor). Remainder HOMED at the 0.5 plan line (registry [ROW]/[COLUMN] cards · .toolbus — post-engine tail; R5 was already the board's design; 0.6–0.8 later increments). **NEXT: E.1.2 ENGINE (the punch-list per task #2/the c-class report, then the D-305 tail).** Pickup → `handoffs/2026-08-14-E.1.2-d305-tail-engine-resume-handoff.md` (superseded same day — see UPDATE 28). **▶ UPDATE 28 (2026-08-14, session close): D-305 TAIL ITEMS 1+2 LANDED — the persist registry IS the wire spec, byte-identical-PROVEN, and the triple-vacuity guard is live.** Engine `63364a7` (the ordered 29-row `FOREACH_NODE_PERSIST_FIELD` + SAVE/READ/COMMIT projections + count-lock trio + H15; all three serializer loops converted; suite 3702/0 with the frozen v10 golden byte-identical + positive control) + `965c7a1` (REC-A fold, A3-guardrailed, shadow-leg verified) + `47c06b9` (GUI LTp99 lifetime columns — operator-verified sub-500ns lifetime p99 across all 4 nodes — + 4 spot-check quick-kills incl. the D-1 boot-brick row + the cfg_write_field truncate/stack-smash). Workspace: `tools/node_persist_layout.py` (15 teeth) + the **PAIRED-BUMP rule** in `check_identifier_retirement.py` + 2 `wire-const` ledger rows + bless roster/selftest wired HARD — **AR-8 PAID pre-commit** (a-class refute: NOT REFUTED on the 5 contract properties; 2 latent vacuity modes + a Class-51-C tooth + the unwired bless tooth found and CLOSED before first commit; 1 agent claim refuted by code-read; frozen report `reports/2026-08-14-persist-layout-guard/`). Specs: wire-format v1.2 **Layer 5c** minted; retirement spec's predicted mechanization marked LANDED; Class 51 +2 prevented instances; NEW memory (spot-check→plan-homes routing). Display-truth cohort DOCUMENTED as roadmap **EV-1/EV-2** plan items (operator directive: NOT TECH_DEBT) w/ 2 frozen I-class reports. Operator TTY: corpus pins + D-419 blessed; wire-const `--update` pends the v11 batch. **REMAINING = Steps 3–5 (the v11 DELTA: AM-4 re-locate-first → partner_pending_pnl add + node_dd_pct drop → SHARDED 10→11 + PORTFOLIO retire + CONTROLLER delete → D-289 → F-096) + the close-gate — the paired-bump's maiden live firing.** Pickup → `handoffs/2026-08-14-E.1.2-steps3-5-v11-delta-handoff.md` (the ACTIVE one; the plan banner's items 3–4 are the executable spec); SUPERSEDED — see UPDATE 29). **▶ UPDATE 29 (2026-08-15, session close): D-421 STEPS 2+4+5 LANDED — the COMPLEMENT question is now asked, codified, and generalized.** Engine `49df9d2` · workspace this close (window `8e2473b..HEAD`). **Step 2** — `FOREACH_NODE_CTX_PERSIST_EXEMPT` written, so every `NodeContext<F>` member is persisted OR declared-exempt with a FALSIFIABLE category; `check_node_ctx_partition.py` went rc 2 REFUSAL → rc 0, then wired STANDING as pre-commit **Check Q** + 2 HARD aggregator rows + a D-137 teeth wrapper. Check Q is not redundant with the aggregator: Check P fires only on staged handoff/MASTER, and the failure this guard catches arrives in an ORDINARY CODE commit — it triggers on the STRUCT's home as well as the registries, because the dangerous edit is the one that touches the struct and NOT the registry. It has since live-fired and passed. **Step 3** fell out as designed (both open items became rows). **Step 4** — **Class 58** codified WITH its guard: 3 sub-shapes (producer-side · gate-reachability · consumer-side), 8-instance census, M3-vs-Class-4 discharged, sister cohort amended same ship. **Step 5** — the **`domain` column** on every `FOREACH_REGISTRY` row + dispatching Check 4 + a vocabulary DERIVED from a spec fence (not hardcoded) + the `CHECK:` token with file-existence teeth. The rule that carries the value: **declaring NOTHING fails**. Classification paces on a shrinking baseline, because a WRONG domain is worse than an absent one. **Findings the gates produced, not the code:** all 14 inherited `ControllerEventLoop` cites were STALE (→ **AR-17** NEW) · a `[CLASS]_[58]` phantom reference caught in the registry built to prevent phantom references · Check 4 nearly shipped saying "classified" where it meant "declared", which would have made the DOMAIN column its own Class-51 vacuity. **Tech debt WORKED not stacked:** TD-196 + TD-167 **CLOSED** (operator-blessed at a real TTY; -196 had been closable for a day) · TD-274's trigger FIRED but it stays OPEN with precision (step 5 shipped the declaration half, not enforcement — closing on the trigger alone would have been the phantom-guard shape inside the phantom-guard entry) · TD-172 partial-progress, explicitly not closed · **TD-282** NEW (Class 58's Mn, HOMED not minted, no number reserved per AR-14). **Step 6 PARTIALLY PROVEN:** the `inference_cfg` group bit is a three-way closed loop (emit/parse/consume each gated on it; `STAMP_SET` is the only `has_flags` writer tree-wide and neither of its two sites is unconditional) so its drift rows can never fire — Class 58 sub-shape B, live. ⚠ The "36 fields" claim rides a DIFFERENT walker and is NOT yet verified — do not inherit it. **NEXT = step 6** (extend the proof to `FOREACH_STAMP_BOUND_CFG`, then build the generic gate-reachability check, THEN choose the drift-gate fix). **Pickup → `handoffs/2026-08-16-E.1.2-D422-D423-unwired-capability-and-bandit-loop-handoff.md` (the ACTIVE one).** **▶ UPDATE 30 (2026-08-16, session close): D-421 STEP 6 TIER 0 LANDED + D-422/D-423/C-424/D-425 — and ⚠️ UPDATE 29's `NEXT` IS VOID.** Engine `f2a6321` (UPDATE 29's *"engine `4c076ed` UNCHANGED"* is a month stale) · workspace this close. **UPDATE 29 said *build the generic gate-reachability check*. That design is REFUTED — do not build it.** A prototype graded `inference_cfg` GREEN (the actual CRITICAL) and `feature_mask` RED (benign): inverted on both headline cases. Two sibling designs died with it — the `STAMP_EMIT_PRODUCER_<G>` marker is a NO-OP (the emit walk token-pastes `STAMP_EMIT_CHECK_HAS_##group`, so an undefined dispatcher already fails to compile) and the bit-keyed producer SIDECAR is refuted on H18 (column-not-sidecar for a dense mandatory property). **All three failed the same way: the `fees` bit HAD a producer, so any bit-scoped check graded it green while two of its rows emitted zeros into an HMAC-signed body — a coverage check over an emitted format must key on the WIRE KEY, not the group bit.** H21 spec Rule 1a; full evidence in **D-425**. **LANDED:** Tier 0 (the `fees` group DELETED, not tombstoned) · **D-422** unwired-capability register (functions whose only callers are tests) · **D-423** the exit-side learning loop CLOSED — SELECT wired via `BanditAlgorithm_Apply`, attribution re-pointed at the arm the bandit chose, Thompson persistence restored, `bandit_enabled` wired and re-defaulted to 1 · **C-424** register risk closed (swept for comments CLAIMING a live consumer: one hit, truthful) · two capital-path bugs, both caused by a comment asserting a guard that was not there. **AR-8 PAID — and the a-class REFUTED three of my own claims,** which is why it ran: the H21 guard has no `SOURCES` row for the stamp registry, so the GREEN I cited as proof-of-safety was **vacuous** (Class 51, in the session that spent the day cataloguing Class 51); the exit loop closes only with the Ridge exit-blender OFF; and a sibling uninit-stack site at `ShardedSnapshot.hpp:780` was MISSED by the fix that claimed the class closed (now patched). **NEXT = task #10 FIRST** — enroll `FOREACH_STAMP_BOUND_MODEL_CONST` in `check_identifier_retirement.py` (~58 ids ⇒ blessed `--update` at the operator TTY). It is a PREREQUISITE, not a follow-up: task #1 (Tier 2, dissolve the emit-side bitmap) is a far larger deletion on that same signed wire, and today's H21 procedure would hand it the identical meaningless green. **THEN the operator's fork: Tier 2 (preferred — makes the class unrepresentable) vs Tier 1 (per-WIRE-KEY checker shaped so Tier 2 deletes it).** Also newly runnable: `tools/run_sanitizer_tests.sh` — asan/ubsan/tsan could not execute at all before today (instrumented `main` frame ~72MB vs an 8MB ulimit; by-design boot arenas tripping LSan); all three now 3755/0/rc=0. **Pickup → `handoffs/2026-08-16-E.1.2-D422-D423-unwired-capability-and-bandit-loop-handoff.md` (the ACTIVE one; its READ FIRST table and the post-review corrections are the executable spec).**
>
> **▶ UPDATE 2026-08-16 [E.1.2 D-421 step 6 — SHAPE REFUTED before a line was written; no engine code beyond comment fixes].** The step-6 "generic gate-reachability checker" named above is **DEAD**. An `a-class` built it to spec and ran it at HEAD: **green on `inference_cfg`** (the founding CRITICAL) and **RED on `feature_mask`** — inverted on both headline cases. It is undecidable as specified because a gate is a *(struct-instance, bit)* pair crossing a process/time boundary, the "circular producer" heuristic is syntactically identical to 7 CORRECT propagations (`NodeModelZoo.hpp:383/:409/:417/:444/:448/:474/:481`), producers have ≥5 syntactic forms, and 12 mask families are token-paste fragments. **Replacement = the EMIT-SIDE check** (≥1 setter reachable from `tt::Stamp_AssembleAndEmit`), covering **group values AND standalone bits**, with the universe derived from `enum StampHasFlagBit` — *never* from `FOREACH_..._GROUPS` (**5 rows / 6 group bits at HEAD** — corrected 2026-08-17; it read "6 / 7" before the `fees` deletion at `f90b696`, and the line is load-bearing for UPDATE 31's C++17 narrative so the stale count mattered) or `FOREACH_..._STANDALONE` (6 of 17 + a phantom `bandit` row), both of which are themselves Class 58 sub-shape A. ⚠️ **Re-derive both from the source, not from this line** — that is the whole lesson of the row it sits in. **Operator direction: the STRUCTURAL compile-time close is preferred over a scanner** (design pass in flight). **New findings:** an i-class sweep reports **10 of 24 STAMP gate bits unreachable at production emit** — orchestrator-VERIFIED so far: **`feature_mask`**, a parity-critical REFUSE gate (`ModelInference.hpp:1880-1893`) that has **never fired** (0 of 16 on-disk stamps carry the key; `StampHelper.hpp` never sets it; the engine always takes the WARN arm, whose text misattributes it to "pre-v5.11.18a" stamps). The other 9 were PENDING adversarial verification — **DISCHARGED 2026-08-17 (UPDATE 31 / D-426): CONFIRMED, and worse than recorded.** The 9 `inference_cfg_*` rows are inert as VALUES (nothing reads them), which is why a value-oriented trace stops; their absence means `MASK_inference_cfg` is never set, and that bit is the sole gate for the ENTIRE cfg-derived drift layer plus 4 rows of a second registry, three of them `REFUSE_STRICT`. Full chain + the do-NOT (adding the group-bit producer emits nine zeros into the signed body — `fees` again): **PARITY-042**. Class 58 amended (recurrence 8→11; its own detection signature corrected — the prior "producer set vs consumer set" guidance was the refuted shape). **Landmine 19** homed: `rg <pat> .` from the engine root is blind to `tests/`/`tools/`/`plans/` and **no flag rescues it**, which is how the test-fixture producer set stayed invisible.

> **▶ UPDATE 31 (2026-08-17, session close): task #10 CLOSED — the H21 guard finally COVERS the signed body — and a FOURTH step-6 design refuted, behind two live defects. Engine `e76cf9b` · workspace this close (window `b828559..HEAD`).** UPDATE 30's `NEXT = task #10 FIRST` is **DONE**: `FOREACH_STAMP_BOUND_MODEL_CONST` enrolled in `check_identifier_retirement.py` as category `stamp-key` (positional — emit order IS canonical wire order, so a REORDER lands as RENUMBERED and a dropped key as REMOVED), blessed at the operator TTY. Its GREEN now means something on this surface; before today it would have printed identically if the whole registry had been deleted. Two sub-parts worth carrying: the parser's row-name filter was uppercase-only, silently excluding every WIRE-KEY registry while admitting the ENUM-CODE ones (widening was **measured non-perturbing before it was made**; and it was never a false green — such a row parsed to ZERO and tripped the PARSE ERROR refusal, so the class was *un-enrollable*, not *uncovered*); and three NEW selftest teeth anchored on the **CODE** side so they prove teeth before AND after a bless, avoiding the live-value anchoring trap this same selftest fell into once. **⚠️ UPDATE 30's fork ("Tier 2 vs Tier 1") is SUPERSEDED — it is no longer the live question.** A 3-agent adversarial pass (fired because the operator asked *"are you sure it's the best option, do we need to test stuff for this?"* on an HMAC-signed wire change — two triggers of the opt-OUT default) **refuted a FOURTH design** and reordered the queue behind two live defects. **④ per-key bits — REFUTED**, by the same mechanism that killed ①-③: bit-set and value-write are **separate C++ statements**, so no *granularity* change couples them; the fault is **co-location**. The proof is a live instance inside the very set the proposal cited as already-protected. **LIVE DEFECT 1 —** `inference_cfg_bandit_blend_ratio` sets its bit and never assigns its value, so an HMAC-signed model-identity document carries that key as `0` beside the truthful cfg-derived line (measured by compiled probe: two byte counts, two signatures). Same cause as `fees`, **three lines above where `fees` was deleted**, and **armed by this sprint's `bandit_enabled` 0→1 flip**. It survived the sweep that killed `fees` because a comment written *during* that sweep asserts the row emits `=X`; there is no `=X`. **LIVE DEFECT 2 — the `.B.3` migration has no parse→handle leg:** ~30 cfg-derived `ModelHandle` fields are declared and read but never written, so two `REFUSE_STRICT` drift rows compare `0` against a cfg default of `1.0` behind a gate that reads **cfg only, not `STAMP_HAS`**. Discharged by compiled probe with a passing positive control. **Severity is NOT "the engine won't boot"** — the prediction overstated that and the probe corrected it: at the shipped default it returns 0. The real shape is that the drift gate is **unusable in both positions** — OFF it saturates a capital safety channel with guaranteed-false lines so real drift is indistinguishable from noise; ON (`held_out_gate_strict=1`, the live-readiness posture) every model refuses, so the rational operator response is to disable the gate. A safety control that pressures you into switching it off is the Knight shape. **`STAMP_PUT` LANDED** (additive, byte-neutral): the value-carrying setter that makes the pair one expression. **The `STAMP_SET` refusal is DESIGNED and PROVEN but REVERTED** — armed, it rejected exactly the emit sites with zero other error classes, including the live defect, but a GROUP bit has no field of its own so the assert was too broad. The correct rule (refuse only when a same-named MEMBER exists) is **blocked on a C++17-vs-C++20 call**: C++20 spells it inline (`requires`), C++17 needs a `void_t` trait per name, and the obvious name-universe (`FOREACH_..._GROUPS`) is **drifted from `enum StampHasFlagBit`** — the same hole UPDATE 30's line above already warns about. **NEXT = pick the route (A move to C++20 / B fix the drift then generate / C generate from the enum); then the rest is mechanical.** All three agent reports + the probe evidence saved verbatim at `plans/v5.15-live-readiness/reports/2026-08-17-stamp-emit-gate-audit/`. **Pickup → SUPERSEDED by UPDATE 32 below; that handoff is now `status: superseded`. Its READ FIRST table listing all four refuted designs is still valid — do not rebuild any.**
>
> **▶ UPDATE 32 (2026-08-17, later the same day): D-426 CLOSED END-TO-END — the class is structurally unspellable, and the ledger-semantics fork resolved as NO CHANGE. Engine `76e4b8e` · workspace this close (window `64e43b4..HEAD`).** `STAMP_PUT` + every emit site converted + **the `STAMP_SET` guard ARMED**, which produced ZERO refusals — the completeness proof that arming it LAST was designed to give. The `inference_cfg_bandit_blend_ratio` zero-emit **DELETED and blessed** along with BOTH consumers found while scoping (a panel display and a production sr→handle copy) — **the FOURTH site of one pattern across three sweeps**, each of which removed the `fees` twin and left the `bandit` sibling three lines away. That shape is now catalogued as **AR-19**. **The proposed ledger-semantics change was REFUTED by its own `/decision-check`** (mid-insert and duplicate-key both went RED→GREEN under it; and it did not even solve the stated problem) — the real defects were a `tail -12` in the pre-commit display and an **H21 name-burn that was INERT for 3 of its 4 names**, both fixed and teethed. **NEW D-427: the viewer is Dear ImGui, decoupled as a monitoring plane; `fox-tui` DROPPED; D-7/D-26 reversed before execution** — ⚠️ but `EngineTUI.hpp`'s `TUISnapshot` DATA CONTRACT survives and is promoted (it is the mmap foundation; only the renderer dies). **NEXT = queue item 1, the `.B.3` parse→handle leg (PARITY-042, capital-adjacent).** **Pickup → `handoffs/2026-08-17-E.1.2-D426-closed-guard-armed-next-B3-parse-handle-handoff.md` (the ACTIVE one).**

> **▶ UPDATE 33 (2026-08-20): `E.1.2.C` OPENED — the ML-verification program (D-428); the 7-agent i-class fan is COMPLETE; plan drafted; a-class refute pass next.** Operator directive: verify exit+buy model training, the bandit + ML integration within live, THEN the rest of E. Plan: `subplans/2026-08-20-v5.15.5.F.4d.1.E.1.2.C-ml-verification-program.md` · reports verbatim at `reports/2026-08-20-ml-verification-program/`. **Headline finds:** the **FAN-SHIFT defect** — `EventLoop_DrainPostFill` forwards 10 positional args into OneCore's 12-param signature since v5.14.1.F ⇒ the exit-bandit reward UPDATE is DEAD on every production path (live AND backtest), and flipping `exit_bandit_enabled=1` in LIVE would poison the drift-IC/auto-kill safety channel instead (compiled-probe-proven + independently re-derived by a second agent; D-423's "reward half works" premise corrected at D-428) · **PARITY-043 cohort measured = 36 fields, 33 written-by-nothing** (fix = the missing 5th `cfg_derived` walker; two REFUSE_STRICT rows fire falsely on EVERY load at a default cfg now that `bandit_enabled` defaults ON) · **PARITY-044 fix shape settled** (co-located `exit.json` role emit + WILL_PEAK side default + T2 retirement of `exit_signal_model_dir`/the exit tree; open forks D2/D4/D6/D7/R1 in the plan body) · **the live train path emits NO scaler** (sole producer = `train_model_worker_fn`, DEAD code, zero callers since v5.11.44; Save Run is dead UI) · **at HEAD there is NO functioning train↔serve cfg-parity gate at all** (042 vacuous + 043 false-firing + ensemble loads skip the walk + single-zoo boot never HMAC-verifies). Also this session: the suite **cwd-anchor fix SHIPPED** (engine `417e524` — Collect Features was dead from any non-root launch cwd; sisters homed TD-287) + PARITY-044 minted + FUTURE_ML gained the MoE-framing/entry-aware-exit private entry. **NEXT = the a-class pass (R1 reachability probe · D2 guard fork · plan-level refute) → consult → leg 0 (the fan-shift fix).** Leg 1 (the operator's buy-side e2e protocol run) is unblocked NOW — protocol at `reports/2026-08-20-ml-verification-program/e2e-verification-protocol.md`. **Pickup → the ACTIVE 2026-08-17 handoff (still current for the E.1.2 queue) + D-428 + the plan body.**

> **▶ UPDATE 34 (2026-08-20, late): `E.1.2.C` CODED — legs 0/2/3 SHIPPED in one day; the ML loop is structurally ALIVE.** Seven engine commits (`417e524` cwd-anchor → `56deb39` leg 0 fan-shift fix → `7168953` leg 2 fifth-walker/PARITY-043 → `6fc5655` leg 3-pre R1 dispatch fix → `a81859d` 3-retire T2 burn → `22433b0` 3-role exit.json+guard), workspace test commits paired, ALL pushed except the final pair (push at this close). Suite 3760/0 (members moved by design: +14 new E.1.2.C pins, −6 retired, −4 replica); determinism GREEN throughout; identifier guard GREEN 93; `-Werror=float-conversion` armed on engine+tests (caught 4 latent same-class bugs on first build). **What is now TRUE:** exit-bandit reward learning reachable (first time since v5.14.1.F) + seam-tested with exact-value oracles · PARITY-043 dead through the real consumer (before/after 0/0 tiers) · ensemble-only deployments SERVE (R1 dispatch fix + 3 blind sisters + 5 vacuous live-readiness drift gates fixed) · `exit_signal_model_dir`+exit tree RETIRED and BURNED (first proactively-burned cfg name key; D-254 pin 53056→52800) · side=1 emits CO-LOCATED `exit.json` with WILL_PEAK default + F1/F2/F3 `expected_role` enforcement (D2 verdict O1-only; zero wire keys, zero blesses). **NEXT (handoff):** the deferred 3-role table tests (2 pure fns) · tooltips/unit-hint polish + the per-horizon results-table label bug (reads the LIVE horizons box, operator report) · 3G-i Settings category-grouping · leg 4 empirical (train exit models first — the operator can NOW) · D6 scaler decision · D7 observability · citable-ids TTY re-bless (7 ids). **Pickup → `handoffs/2026-08-20-E.1.2.C-legs-0-2-3-shipped-handoff.md` (the NEW ACTIVE).**

> **▶ UPDATE 35 (2026-08-20, evening): `E.1.2.C` TAIL SHIPPED — the entire agent-side queue is DONE; leg 4 (operator empirical) is the only thing left before `/ship` close.** Engine `29a9a3a..f4bbafe` + workspace `d16bce9..ee00034`+close, ALL pushed. **Landed:** the deferred 3-role tables (C.3g exhaustive over FOREACH_TARGET×side + C.3h = the D2 verdict's decision table, driving the REAL fns — `Training_ResolveRole` relocated to `LabelFunctions.hpp` for ANSI-TU reach) · the operator-reported GUI polish batch (results-table horizon snapshot bug + unit-first TP/SL tooltips + 3-class collect-mh labels + the `..., K exit predictors` boot count = leg-4's Stage-1 oracle) · **3G-i** Settings section-GROUPING (one header per canonical section; the 72-headers-with-colliding-ImGui-IDs class dead; registries untouched — wire-load-bearing) · **3G-ii** the Model Dir BUNDLE PICKER (families as ONE entry via the LOADER'S OWN `Model_ParseHorizonSibling`, extracted so picker+boot can never disagree; resolution preview + click-gated stamp verify; the old scan couldn't even see `models/<class>/<run>_horizon_*`) · **EnsembleHotSwap.hpp BURIED** (H21; proven zero production callers; 5 tautological cells died with it, Free-completeness cell survives) · **PARITY-043 + PARITY-044 CLOSED at the ledger** with mechanism lines (042 surface-updated, still open) · TD-288 minted (the A-4 defaulted-tail cohort, members named) · register currency dispositions (5 CLOSED / 2 surfaced / 2 PARTIAL / **1 FLAG: #16/A-12 mixed-output gate item NOT found landed — verify before program close**) · Landmine 21 (symlink-RESOLUTION family) · corpus + citable-ids operator-blessed ×2. Decisions → **D-429** (D-428 STATUS amended to current truth). Suite green at window end (re-derive; 3850/0 at write). **NEXT: leg 4 (operator trains; runbook in the plan/protocol) → D6/D7 forks → the A-12 flag → `/ship` close (version bump deliberately deferred — Version.hpp still `.E.1.1` until the tag cuts).** **Pickup → `handoffs/2026-08-20-E.1.2.C-tail-3G-shipped-leg4-ready-handoff.md` (the NEW ACTIVE).**

> **Live sprint-state SSoT for the `.E` era:** `CLAUDE.local.md` § Current sprint state + `E-MASTER-REFERENCE.md` + `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-168..D-189).

**Date drafted:** 2026-05-12 (post v5.14 sprint close + v5.14.post1 patch)
**Branch:** `feat/v5.15-live-readiness` (CREATE from engine tag `v5.14.post1` =
commit `1752fde`, per 2026-05-12 amendment — v5.14 has known
BacktestPanels.hpp gui/suite compile errors that post1 fixed; branching
from v5.14 would break v5.15.0's wider-build verification gate. post1 fix
lines later get replaced by v5.15.3.B helper switch — second structural
touch of the same code).
**Predecessor:** v5.14 sealed at `v5.14`; post-release patch `v5.14.post1`
(commit `1752fde`) for `train_model_worker_fn` stamp body migration gap fix
**Rollback anchor:** `pre-v5.15` = `v5.14` (sprint baseline)
**Sister plans:**
- `subplans/2026-05-12-v5.15.0-modelhandle-migration.md` — HIGH-RISK
- `subplans/2026-05-12-v5.15.1-model-health-panel.md` — LOW-RISK
- `subplans/2026-05-12-v5.15.2-live-readiness-boot-gate.md` — MEDIUM-RISK
- `subplans/2026-05-12-v5.15.3-multi-horizon-worker-stamping.md` — LOW-RISK
- `subplans/2026-05-12-v5.15.4-live-mode-strict-defaults.md` — MEDIUM-RISK

---

## Why this sprint exists

v5.14 shipped the MATH layer (Ridge blending, composite confidence,
Thompson bandit, hot-swap ensemble, stamp body unification) + the
STRUCTURAL discipline (X-macro registries, AUTOPOPULATE companions,
parity-tested-by-construction). The engine boots, models load,
inference fires, drift is detected.

What's missing is the **operator-side surface**: visibility +
gates + strict defaults that turn "engine runs" into "engine
runs safely under live conditions." A `./engine` boot log review
2026-05-11 surfaced 5 specific gaps:

1. **feature_hash mismatch** silent in UI — drift detected, not surfaced
2. **Empty stamp secret** accepted in live mode — model verification toothless
3. **mlockall failed** soft-warning only — page faults under live load possible
4. **"4/4 handles missing grid_member_count"** — multi-horizon worker
   never wired to stamp emit path (TODO since v5.10.X)
5. **Hardcoded strategy fallback** when per-core not configured — operator
   may not realize cores defaulted

Plus the architectural gap from v5.14.8.A: ModelHandle survived the
Option 1 stamp body unification as an asymmetric struct (manual
declarations + inconsistent prefix policy + 16 uint8_t `has_*` direct
fields where ModelStampResult/StampInferenceCfgInputs got registry-driven
+ bit-packed `has_flags` via STAMP_SET()).

v5.15 closes **both classes** in one focused sprint: the operational
visibility surfaces AND the ModelHandle structural unification. Plus
absorbs **6 TECH_DEBT entries** with naturally-overlapping surface
area, per the "structural fix preferred when bug class can recur"
rule (CLAUDE.md item 19; CLAUDE.local.md going-forward rule
2026-05-09).

**Operator framing 2026-05-12:** *"lets fully deal with these,
remember even if its more work today, creating solutions that make
it easier going forward are preferred."* — Caramel.

---

## Architectural invariants (PRESERVE through sprint)

| Invariant | Verification |
|---|---|
| **Hot path UNTOUCHED** | All new work is slow-path-only or boot-only. `BG_Evaluate` / `SG_Evaluate` / `ExecutionCore_Tick` zero changes. Verified via `tools/calls_graph_diff.sh` + bench gate. |
| **HMAC chain byte-equivalent** | v5.15.0 ModelHandle migration MUST NOT change canonical stamp body byte layout (registry order preserved; PRE_CFG/POST_CFG halves unchanged). v5.15.4 cfg-default-flip MUST NOT change stamp body emit at the canonical byte level. Verified via round-trip HMAC test + SHA-256 lock on representative stamps. |
| **Forward-compat Surface G `has_*` flags** | v5.14-era stamps MUST load cleanly on v5.15 engine (no `MODEL_FORMAT_VERSION` bump). Verified via legacy-stamp load test. |
| **Parity-tested-by-construction (item 15)** | Every train→serve handoff surface touched (trading_mode cfg field stamp-binding, ModelHandle field re-route, multi-horizon worker stamp emit) gets a registry/binding/snapshot rather than ad-hoc test. New `trading_mode` stamps via FOREACH_STAMP_BOUND_CFG. |
| **Branchless on hot path** | Zero new hot-path branches. Slow-path predicate caches (`is_live` etc.) use cached state at slow-path entry (item 18(c)). |
| **No mode field except via stamp-bound cfg** | New `trading_mode` cfg field MUST be stamp-bound (model carries its training-time mode) so future paper-vs-live divergence is auditable. |
| **Default cfg = pre-v5.15 behavior** | `trading_mode = 0` (PAPER) is default; legacy cfgs unset = PAPER; behavior unchanged unless operator explicitly sets `trading_mode=live`. |
| **No new TECH_DEBT** | All 6 absorbed entries close; no "deferred substantial-progress" downgrades unless honestly required. |

---

## Sprint sizing (post-amendments 2026-05-12)

| Sub-ship | LOC | Time | Risk | Key amendments |
|---|---|---|---|---|
| v5.15.0 | ~580 | 5-6h | HIGH | comprehensive Step 0 grep (caught missed ModelValidation.hpp + FeatureRegistryOverlay.hpp sites); Option C shared MASK_STAMP_HAS_* bit positions; file:line corrections |
| v5.15.1 | ~220 | 3-4h | LOW | FOREACH_ARCH_FIELD_DRIFT new registry; drift consolidation into existing CFG X-macro loop (closes /merge-scan HIGH-1) |
| v5.15.2 | ~330 | 4-5h | MEDIUM | FOREACH_LIVE_READINESS_CHECK X-macro (closes /merge-scan MEDIUM-2); ControllerConfig.hpp:2371 parser location fix |
| v5.15.3 | ~200 | 3.5-4h | MEDIUM | Stamp_AssembleAndEmit helper from RFV (closes PARITY-020 + PARITY-021 structurally); FullValidationResults plumb-through (3 req_grid_* fields appended); libgomp setenv at foxml_suite.cpp:main(); v5.11.45 forced-serial WORKAROUND REMOVED; FOREACH_CLI_MODE DEFERRED to TECH_DEBT-034 |
| v5.15.4 | ~330 | 5-6h | MEDIUM+ | shadow-load (`aligned_alloc(64)` + `__atomic_exchange_n` + Free-old) replaces broken HotSwapSnapshot (closes PARITY-023); `alignas(64)` retrofit on EnsembleModelZoo + CoreModelZoo + size static_asserts; ControllerConfigKeyExplicit uint16_t bitmap; DoubleBufferedAtomic<T> DEFERRED to TECH_DEBT-035 |
| **Total** | **~1660** | **21-25h** | — | **~6-8 days** (-190 LOC vs pre-amendment after closing speculative scope-creep + adding structural primitives) |

## Sub-ship phasing

### v5.15.0 — ModelHandle X-macro migration + verify_model_stamp parser refactor [HIGH-RISK; 5-6h]

**Surface:** `ML_Headers/ModelInference.hpp` ModelHandle struct (~16 has_*
direct fields → `uint64_t has_flags` bit-packed) + `verify_model_stamp`
parser (~700 LOC if-else chain → data-driven dispatch table).

**Why bundled:** ModelHandle migration touches every caller that reads
`h.has_<field>` and every site that populates ModelHandle.has_* from the
parser. The parser refactor closes the same Class 18 mirror (FOREACH_*
emit was registry-driven at v5.14.8; parse was left manual). Both sides
of the boundary become registry-driven together. Closing them in
separate ships would have us touching ModelHandle twice.

**TECH_DEBT closures:** -014 (ModelHandle migration) + **-003**
(verify_model_stamp parser refactor)

**Sub-tags:**
- .A — ModelHandle struct rewrite via X-macro generation; STAMP_HAS/SET
  bitmap macros; ~14-20 caller migrations (CoreModelZoo, EngineSharded
  boot, StrategyParameters reads, tests)
- .B — verify_model_stamp parser → data-driven dispatch table
  (table-of-`{key, parser_fn, target_offset, mask_bit}` walked at parse
  time; adding a new key becomes a 1-row registry addition)
- .C — HMAC round-trip + byte-equivalence tests; legacy-stamp load test
  for forward-compat verification

**Pre-coding audit gate:** `/parity-check` + `/trace-deps` + `/readiness`
+ `/dod-audit` in parallel BEFORE coding starts. Same class as
v5.14.8.A.merged (which had the train_model_worker_fn site miss → post1
patch). The mechanical-migration class requires the per-site verification
list, not "rebuild and see if test passes."

### v5.15.1 — Model Health CollapsingHeader + PerCoreSnap bitmap [LOW-RISK; 3-4h]

**Surface:** `GUI/MLStatusPanel.hpp` (new CollapsingHeader "Model Health"
between existing "Ensemble (multi-horizon)" + "Thompson Bayesian
dashboard") + `MemHeaders/FailureModeRegistry.hpp` (FOREACH_FAILURE_MODE
extension: ~7 new BIT_FLAG entries) + `DataStream/EngineTUI.hpp`
PerCoreSnap (4 bool-as-uint8 fields → uint8_t `per_core_state_flags`
bitmap; existing `state_flags` uint16_t may absorb if headroom permits).

**New failure-mode entries (BIT_FLAG storage class):**
- FEATURE_HASH_DRIFT (feature_registry_hash at load vs current)
- LABEL_HASH_DRIFT (label_registry_hash at load vs current)
- BUILD_FLAGS_DRIFT (build_flags_hash at load vs current)
- SCALER_DRIFT (scaler.feature_registry_hash vs handle.feature_registry_hash)
- CFG_BINDING_DRIFT (stamp-bound cfg field count or value mismatch)
- STAMP_HMAC_NOT_VERIFIED (held_out_stamp_secret was empty at load)
- MODEL_AGE_WARN (training_timestamp_us > model_max_age_hours)

Confirm uint16_t failure_flags headroom (currently 2 entries pre-.1; +7 =
9 of 16 slots used) OR expand to uint32_t if needed.

**TECH_DEBT closures:** -028 (PerCoreSnap bool-as-uint8 → bitmap)

**Display↔execution invariant (CLAUDE.md item 12):** every new
PerCoreSnap field gets its GUI render in the same sub-ship. The 4
existing fields being bitmap-migrated keep their existing GUI renders.

### v5.15.2 — Live-readiness boot gate + cfg.mode introduction + breakeven_on_profit wire-up + /readiness wider-build [MEDIUM-RISK; 4-5h]

**Surface:** `CoreFrameworks/EngineSharded.hpp` boot path (new
LiveReadiness_Verify gate; cfg_mode enum dispatch) +
`CoreFrameworks/ControllerConfig.hpp` (NEW `trading_mode` cfg field as
uint8 enum) + `CoreFrameworks/ControllerConfigParser.hpp` (string-keyed
enum parse, mirroring `reconcile_mode` precedent at line 2371-2381) +
`CoreFrameworks/PortfolioController.hpp` (breakeven_on_profit wire-up
near line 670 where `breakeven_on_partial` is already read).

**cfg.mode introduction (structural design):**

```cpp
// New enum near reconcile_mode (~line 855 in ControllerConfig.hpp):
enum TradingMode : uint8_t {
    TRADING_MODE_PAPER  = 0,  // default; gates default-off
    TRADING_MODE_LIVE   = 1,  // pre-flight REFUSE on missing items
    TRADING_MODE_SHADOW = 2,  // future: live data + simulated fills
};
uint8_t trading_mode;  // TradingMode; default 0 (PAPER)
```

- Parser: `trading_mode=paper|live|shadow` string-keyed (mirrors
  reconcile_mode at 2371-2381)
- Stamp-bound via `FOREACH_STAMP_BOUND_CFG` — model carries training-time
  mode for audit trail (parity-tested by construction)
- Slow-path predicate cache: `gate_state.is_live` derived once at
  slow-path entry (mode rarely changes; item 18(c) cached predicate)
- Legacy cfgs default unset → PAPER → behavior unchanged

**Pre-flight checklist (REFUSE when `trading_mode == LIVE` and any item
fails; WARN otherwise):**

1. `held_out_stamp_secret` nonempty
2. `mlockall` succeeded (cfg.require_mlockall + mlockall() syscall OK)
3. All cores have explicit `core_N_strategy` cfg (no hardcoded fallback)
4. All cores have model loaded if strategy is ML
5. `model_max_age_hours` > 0 AND no stale-model handles
6. `feature_registry_hash` matches across all loaded handles
7. `label_registry_hash` matches across all loaded handles
8. `build_flags_hash` matches across all loaded handles
9. No `STAMP_HMAC_NOT_VERIFIED` failure flags set after model load

**breakeven_on_profit wire-up (TECH_DEBT-024 close):**

Located dormant entry at `CoreFrameworks/LifecycleCfgFlagRegistry.hpp:58`
(`MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT` defined; zero read sites).
Sister `breakeven_on_partial` IS wired at
`CoreFrameworks/PortfolioController.hpp:670`. Wire-up adds a parallel
read site for breakeven_on_profit triggered when position crosses net
profit (not partial fill). ~30-50 LOC + test. Updates registry comment
to remove "DORMANT" marker.

**TECH_DEBT-033 /readiness skill wider-build check:** Add Check N+1 to
`/readiness` skill: verify last sprint's close included `./build.sh gui
suite tsan asan all` (not just `test`). v5.14.post1 was the warning
shot; structural fix is the skill-side gate.

**TECH_DEBT closures:** -024 (breakeven_on_profit wire-up) + -033
(wider-build check)

### v5.15.3 — Multi-horizon worker stamping + libgomp pthread-race fix (CLAUDE.local.md landmine close) [MEDIUM-RISK; 2.5-3h]

**Surface:** `Backtest/BacktestPanels.hpp:3792` (`train_multi_horizon_worker_fn`)
+ per-horizon FV helper at `:3419`. Boot log "4/4 handles missing
grid_member_count" surfaces this: multi-horizon worker writes models but
never calls `stamp_write_for_model`. Single-horizon path (post-v5.14.post1)
properly stamps via `train_model_worker_fn` at line 3206.

**Canonical references for the pattern application:**
- `Backtest/BacktestEngine.hpp:1147-1220` (`Backtest_RunFullValidation`)
- `Backtest/BacktestPanels.hpp:3206-3266` (`train_model_worker_fn`,
  post-v5.14.post1)

**Apply:** assemble `StampInferenceCfgInputs` via
`STAMP_MODEL_CONST_AUTOPOPULATE` companion; call `stamp_write_for_model`
per horizon after each model file write. HMAC chain implications: each
stamp carries its own grid_member_count + horizon_idx + horizon_count;
all 3 fields already in FOREACH_STAMP_BOUND_MODEL_CONST (verified during
v5.14.8.E).

**Parallel-mode bundled** (Caramel 2026-05-12 — *"i dont really wanna
defer this, we should come up with a fix"*): the v5.11.45 segfault
landmine gets the proper fix already documented in CLAUDE.local.md
("Known landmine" 2026-05-07): `setenv("OMP_NUM_THREADS", "1", 1)` at
`foxml_suite.cpp:main()` BEFORE any pthread or library init. libgomp's
team-pool allocation race eliminated because all teams have size 1 → no
allocation needed → no race surface. Forced-serial workaround at
`BacktestPanels.hpp:3886-3912` removed; default `multi_horizon_max_threads`
behavior restored.

**Trade-off:** XGBoost loses internal OpenMP parallelism (single-horizon
training ~3-4x slower on multi-core CPUs); parallel multi-horizon trains
N horizons in ~per-horizon-time (N-x speedup for grid workflows). Net
throughput favorable for grid-training-heavy workflows. ./engine (live
binary) unaffected — engine doesn't run XGBoost training; inference is
single-threaded per CLAUDE.md item 18.

### v5.15.4 — mode=LIVE strict defaults + hot-swap unification (single-zoo + ensemble) [MEDIUM-RISK; 3.5-4.5h]

**Surface:** `CoreFrameworks/EngineSharded.hpp` boot (~line 2820,
single-zoo hot-swap site for TECH_DEBT-005) + post-parse normalize pass
(new function `ControllerConfig_NormalizeForMode` at parse-end) that
applies `trading_mode == LIVE` default-flips.

**Strict default-flips when `trading_mode == LIVE` and operator didn't
override:**

- `model_verify_strict` 0 → 1 (was WARN; becomes STRICT)
- `reconcile_mode` 1 (WARN) → 0 (STRICT) — if dry_run not explicitly set
- `cfg.held_out_stamp_secret` empty → boot REFUSE (already in v5.15.2's
  checklist; this is the corollary on parse side)

**TECH_DEBT-005 unification (hot-swap strict-mode handling via shadow-load):**

Boot does Free + null + flag on validate failure. Hot-swap dispatcher
in `EngineSharded.hpp` (single-zoo + ensemble) both do
flag-only (v5.10.0c "log-and-leave" semantics; the TODO comment
notes it). [Line anchors stripped — pre-`.B.6` subfolder split.]

**Plan-draft 1 (capture-pointer-and-revert) had a FATAL FLAW** caught by
/parity-check PARITY-023: `_Free` destroys data IN-PLACE; captured
pointer points at freed memory after the swap; revert impossible.

**Correct fix: shadow-load pattern (NEW DESIGN_SPEC).** Allocate NEW
ezoo/zoo into SEPARATE memory via `aligned_alloc(64, sizeof(T))`; load
+ validate into the new allocation; atomically swap pointer via
`__atomic_exchange_n` ONLY after validate succeeds; Free OLD state
AFTER swap (single-owner: per-core slow-path thread is sole reader of
container pointer; safe to Free immediately).

Plus: **`alignas(64)` retrofit on EnsembleModelZoo + CoreModelZoo**
(neither currently has explicit alignment despite containing
cache-aware members — ModelHandle alignas(64) post-v5.15.0;
RidgeWeights AVX-512 vectorized per item 25). Required for
`aligned_alloc(64, sizeof(T))` to actually satisfy member alignment.

~330 LOC + tests. NEW DESIGN_SPEC: `shadow-load-state-transition-pattern.md`.

**TECH_DEBT closures:** -005 (hot-swap strict-mode unification)

---

## v5.15.5 series (MASTER amendment 2026-05-18 — pipeline catch-up)

**Status: substantial sub-ship series not previously catalogued in MASTER.** MASTER last touched 2026-05-11; the v5.15.5.B / .C / .F series (universal cfg field registry consolidation; cfg-stamp-binding framework; cross-tool decoupling) shipped/in-flight without MASTER updates.

This section is a catch-up index — per-ship detail lives in the respective subplans/postmortems; this section provides navigation.

### v5.15.5.B — EventLoopState cache-layout sweep + 9 Class-18 mirror closures [SHIPPED]
- Tag: `v5.15.5.B.*`
- 9 mirror-state instances closed structurally; foundational for mmap-mediated decoupling
- Decoupling-positioning: ⬆️⬆️⬆️ STRONGLY POSITIVE per `plans/_future/2026-05-12-decoupling-endgoal-roadmap.md`

### v5.15.5.C — OrderManagerState cache-layout sweep + bit-packing + wire-format registry [SHIPPED]
- Tag: `v5.15.5.C.*`
- OMS cluster reorg + multi-bit state encoding substrate
- Decoupling-positioning: ⬆️⬆️⬆️ STRONGLY POSITIVE

### v5.15.5.F.4b/c/c.3/d — Universal cfg field registry framework consolidation [SHIPPED]
- Series: `.F.4b` (boolean subset; KIND_BOOL) → `.F.4c` (KIND_DOUBLE/_PCT subset; Path γ correction Option E) → `.F.4c.3` (per-core sharding + Class 27 closure + WIP2d-1.B.0c CI tool) → `.F.4d` (STAMP_BOUND derived filter framework; sister specs landed)
- Closes TECH_DEBT-009 PARTIAL (boolean + KIND_DOUBLE subsets); H15/H16/H17/H18/H19/H20 codified
- NEW DESIGN_SPECS landed across series: universal-cfg-field-registry-pattern, type-trait-dispatch-via-tt-namespace, categorical-tag-applicability-pattern, branchless-dispatch-discipline, audit-scope-taxonomy, decision-time-data-binding-pattern, multi-state-dispatch-with-per-state-update-metadata, sidecar-override-pattern-for-registry-auto-flows, meta-registry-pattern-for-codebase-registry-discipline, metadata-bit-driven-derived-filter-framework, framework-composition-overview

### v5.15.5.F.4d.1 — Cfg-derived consumer framework + cross-tool elimination [IN-FLIGHT]
- `.A` (LANDED 2026-05-17): framework primitive infrastructure; metadata-bit-driven-derived-filter-framework Option E first canonical
- `.B.1` (LANDED 2026-05-17): NEW `MemHeaders/CfgGateRegistry.hpp` framework consolidation; sidecar pattern + 3 derived-filter consumer template fns + 12 sister-extension verdicts. NEW DESIGN_SPECS: cfg-derived-consumer-framework + canonical-sister-extension-discipline + failure-attribution-buffer-pattern
- `.B.2` (LANDED 2026-05-17): cohort migration; 15 cfg fields flag STAMP_BOUND_CFG_DERIVED; FOREACH_ML_CFG_FLAG 5→6 col migration; COHORT_GATE_* macros codified
- **`.B.3` (IN-FLIGHT 2026-05-19): Legacy empty-out + cross-tool decoupling via Phase L + Phase K 47-globals registry-default sweep (v1.16 scope expansion 2026-05-19 per Caramel "no defers" directive).** Plan body: `subplans/2026-05-17-v5.15.5.F.4d.1.B.3-legacy-empty-out.md` v1.16. Engine state at WIP-checkpoint 7 (Step 1.6.2 cohort bit-add + Step 0.5d.b/c verified covered + Step 1.5 INFERENCE_CFG_AUTOPOPULATE elimination LANDED 2026-05-19). Scope: Steps 1.6.2 ✓ → 0.5d.b/c ✓ → 1.5 ✓ → Phase F same-commit trio (Step 1.6.4 + 1.6.7 + 1.6.8' **Phase L**) → 1.6.6 → Step 2 (legacy registry deletion FORCED LAST) → Steps 8.5 + 8.6 (Phase K NEW v1.16: spec extension + TECH_DEBT-107 47-globals sweep) → Steps 3-9 ship close. **Phase L is framework-driven CLI binary `tools/stamp_model_cli.cpp` replacing `tools/stamp_model.sh` (closes TECH_DEBT-001 — oldest open item; 3 major versions overdue).** Estimated ~17-23h focused coding from current state (~3.5-4h v1.16 Phase K addition). **CLOSES 14 TECH_DEBT entries total** (-001/-018/-093/-094/-095/-096/-097/-098/-099/-100/-102/-104/-107 NEW v1.16/-109); NEW 6 entries (-101/-103/-105 [folds to .D]/-106 [folds to .D]/-110/-111 [folds to .D]); net -8 (was -6 pre-v1.16). **v1.16 sprint arc adjustment:** `.C` adds per-core override emission CLI (2nd canonical of framework-driven-cli-binary-pattern → proves pattern + enables CI tool at `.D`); `.D` consolidates TECH_DEBT-105+106+111 into ONE `tools/check_framework_consumer_invariants.py` (sister to `registry-coverage-ci-check-pattern.md` Shape B). After `.D`, framework consolidation truly locked; only categorical/trigger-based deferrals remain.
- **`.B.4` (SHIPPED 2026-05-27): Train-serve execution-layer parity structural extract + B-full SHARDED centralized-arch full surface deletion + Phase Cx-cfg-cohort comprehensive cfg field re-categorization.** Plan body: `subplans/2026-05-24-v5.15.5.F.4d.1.B.4-train-serve-execution-layer-parity.md` v1.7.6 (5 path iterations Path 1→2v5). Postmortem: `postmortems/2026-05-27-v5.15.5.F.4d.1.B.4-postmortem.md`. WIP-12 framework layer extract → WIP-13 BACKTEST slow-path migration → WIP-14a operator-facing-doc cohort + stale comment cleanup → WIP-14b 51-site `engine_arch=centralized` SHARDED full surface deletion (B14 1st canonical leaves-first ordering at OPERATOR-USE layer) → WIP-15 Phase C.4.5 PARITY-031 closure (BACKTEST_REGIME_SAMPLE_CORE named constant) + Phase C.6 parity_harness `--pay-fees-in-bnb` extension → WIP-16 9-field GLOBAL re-categorization + 2 H14 violation closures (enable_mtm_kill_switch + sl_cooldown_adaptive → CFG-FLAG BITMAP) + NEW EMIT_PER_CORE_CFG_DEFAULT_GLOBAL_MIRROR walker. **Closes 7 PARITY entries** (026/027/028/029/030/031/032). **NEW DESIGN_SPECS**: `cfg-field-categorization-discipline.md` (Stage 2 DRAFT v1.0; 4-category decision tree + 5-step re-categorization migration + sister-pattern co-location + DOD audit). **Stage promotions**: M5 (train-serve execution-layer parity) Stage 2→3 first canonical via EngineCommon extract; M6 (body-content arg enumeration) Stage 2→3 via WIP-12 helper extracts; M7 (structural enforcement when memory insufficient) Stage 2→3 via B-Plus v0.4 generator mode + /capture-audit Check 8 mechanical sidecar; B14 (multi-surface deletion ordering) Stage 2→3 first canonical via WIP-14b 51-site cohort; B15 (unconditionalization latent assumption audit) Stage 2 DRAFT 1st instance (Stage 3 deferred to 2nd canonical). **Class catalog updates**: Class 25 recurrence_count 2→3 (Cx-B exit_threshold cosmetic fix); Class 26 recurrence_count 1→11 (10 NEW worked instances from 9-field GLOBAL re-categorization); Class 33 NEW (consumer-enumeration-undercount on deletion). **5 NEW sister memories**: feedback_operator_pushback_as_audit_signal, feedback_categorize_by_consumer_pattern_not_field_name, feedback_cfg_field_categorization_at_registry_add_time, feedback_no_question_boxes (canonical file; M7 escalation evidence), feedback_motivated_collaborator_for_caramel (amended). **NEW /readiness Check 44** (cfg field categorization plan-time verification; sister to CI Check 8 commit-time). Tests: 3215/0 (verified at ship close; earlier 3217 claim was pre-WIP-16 baseline). GPG-signed tag `v5.15.5.F.4d.1.B.4`. 18 commits ahead of origin pushed at ship close.
- **`.B.5`-`.B.11` (NEW; QUEUED 2026-05-25): File-size discipline maintenance umbrella.** Sibling-umbrella plan body: `subplans/2026-05-25-v5.15.5.F.4d.1.B-file-size-maintenance.md`. Closes TECH_DEBT-029 + TECH_DEBT-114 structurally across 7 ships. Codifies subfolder split pattern as Stage 3 first canonical at `.B.6` ship (NEW addition to `file-size-split-discipline.md` v1.0 → v1.1). ~7-12 days focused total. Per-ship plan bodies drafted at planning-time per `feedback_plan_right_not_fast`.
- `.C / .D / .F.4e` future ships: per `feedback_new_plans_use_future_oriented_template`; details when scoped

**Decoupling-positioning across .F.4d.1.B series:** ⬆️⬆️⬆️ STRONGLY POSITIVE — first framework-driven CLI binary precedent (Phase L) enables FOREACH_CLI_MODE registry application at decoupling sprint; cfg-stamp-binding never-refactor-again at runtime + cross-tool surfaces.

**See:** `plans/v5.15-live-readiness/subplans/2026-05-17-v5.15.5.F.4d.1.B.3-legacy-empty-out.md` for in-flight ship details; `plans/_future/2026-05-12-decoupling-endgoal-roadmap.md` for accumulated breadcrumbs.

---

## TECH_DEBT closure table

| Entry | Sub-ship | Closure form |
|---|---|---|
| **TECH_DEBT-003** | v5.15.0 | ✅ CLOSED — `verify_model_stamp` parser refactored to data-driven dispatch table; new keys = 1-row registry addition |
| **TECH_DEBT-005** | v5.15.4 | ✅ CLOSED — hot-swap (single-zoo + ensemble) unified via shadow-load pattern (`aligned_alloc(64)` + `__atomic_exchange_n` + Free-old); replaces broken HotSwapSnapshot design (PARITY-023 fatal flaw) |
| **TECH_DEBT-028** | v5.15.1 | ✅ CLOSED — PerCoreSnap 4 bool-as-uint8 fields migrated to existing `state_flags` uint16_t bitmap with 4 new MASK_* constants |
| **NEW TECH_DEBT-034** | (deferred to v5.16+) | FOREACH_CLI_MODE registry + batch mode CLI + per-run logging structure — speculative scope cut from v5.15.3 after root-cause reframe; foundation prepared by v5.15.3.A helper extraction |
| **NEW TECH_DEBT-035** | (deferred to v5.16+) | Engine-side state-exposure protocol + DoubleBufferedAtomic<T> template extraction — premature for v5.15.4 (HotSwap is single-owner; doesn't need it); BinanceDepth.hpp:80-89 stays as canonical precedent |
| **CLAUDE.local.md landmine** | v5.15.3.C | ✅ FIXED — XGBoost+libgomp+pthread segfault via process-startup `setenv("OMP_NUM_THREADS", "1", 1)`; v5.11.45 forced-serial workaround REMOVED |
| **TECH_DEBT-014** | v5.15.0 | ✅ CLOSED — ModelHandle migrated to FOREACH_STAMP_BOUND_MODEL_CONST X-macro generation; bit-packed `has_flags` uint64_t |
| **TECH_DEBT-024** | v5.15.2 | ✅ CLOSED — `breakeven_on_profit` wired in `PortfolioController.hpp` ratchet path; "DORMANT" marker removed |
| **TECH_DEBT-033** | v5.15.2 | ✅ CLOSED — `/readiness` skill Check N+1 added (verify last sprint's close ran wider build, not just `test`) |

**Total: 6 closures.** Each MUST update `tick-trader-percore-workspace/DOCS/TECH_DEBT.md`
in the corresponding sub-ship per the auto-write contract (CLAUDE.local.md going-forward rule 2026-05-09).

---

## Audit cadence (per Caramel's 2026-05-10 rule)

| Sub-ship | Risk | Pre-coding audit | Sub-ship-close audit |
|---|---|---|---|
| v5.15.0 | HIGH | `/parity-check` + `/trace-deps` + `/readiness` + `/dod-audit` in parallel BEFORE coding | `/parity-check` GREEN; HMAC byte-equivalence proven |
| v5.15.1 | LOW | none | `/merge-scan` for FOREACH_FAILURE_MODE reuse opportunities |
| v5.15.2 | MEDIUM | `/parity-check` (cfg parser + stamp-binding for new trading_mode); `/dod-audit` (slow-path-gate-registry-pattern for boot gate) | `/dod-audit` GREEN; new trading_mode stamp-bound + parity-tested |
| v5.15.3 | MEDIUM | `/parity-check` (cross-mode byte-equivalence of serial vs parallel per-horizon stamps; libgomp setenv side-effects) + `/dod-audit` (helper extraction; landmine-close documentation discipline) | ASan + TSan clean under 3/8-thread parallel training; engine load test verifies no `grid_member_count` warnings |
| v5.15.4 | MEDIUM | `/parity-check` (cfg-default-flip MUST NOT change stamp body emit bytes); `/dod-audit` (snapshot infrastructure for hot-swap unification) | `/parity-check` GREEN; legacy-cfg load test verifies no behavior change |

**Mid-sprint audit suggestions fire when:** v5.15.0 HIGH-RISK ship closes
→ before v5.15.1/.2/.3/.4 inherit any new patterns; new ModelHandle
canonical layout field-tested for first time → audit before v5.15.3
multi-horizon worker uses the same pattern.

---

## Verification gate (sprint close: v5.15 umbrella)

- [ ] All tests pass (~2904 → ~2950+ target; +N new tests across sub-ships)
- [ ] `./build.sh test` GREEN
- [ ] `./build.sh gui suite tsan asan all` GREEN (NEW post-v5.14.post1 discipline)
- [ ] `./build.sh test` re-run after wider builds — no test-target regression
- [ ] **HMAC chain byte-equivalence test** for ModelHandle migration
      (v5.15.0): synthesize representative ModelHandle pre/post-migration;
      emit stamp body; verify SHA-256-locked byte-identical to v5.14.post1
      baseline OR document expected differences with explicit registry
      rationale
- [ ] **Legacy-stamp load test** (v5.15.0): load a v5.14-era stamp on
      v5.15-engine; verify loads cleanly (Surface G forward-compat
      preserved; no MODEL_FORMAT_VERSION bump)
- [ ] **trading_mode round-trip test** (v5.15.2): set
      `trading_mode=paper|live|shadow` in cfg; parse; verify
      stamp body carries the parsed value; verify boot gate REFUSE/WARN
      dispatches correctly
- [ ] **Pre-flight checklist trip test** (v5.15.2): with
      `trading_mode=live` and ONE pre-flight item failing, verify boot
      REFUSES + logs the specific failure with actionable message
- [ ] **breakeven_on_profit ratchet test** (v5.15.2): with the bit set,
      verify SL ratchets to breakeven when position crosses net profit;
      with bit unset, verify no ratchet
- [ ] **Single-zoo hot-swap unification test** (v5.15.4): hot-swap
      simulating validate failure; verify pre-swap snapshot reverts
      cleanly; in-flight predictions unaffected
- [ ] Hot path UNTOUCHED — verified via `tools/calls_graph_diff.sh` +
      `DOCS/HOT_PATH_CHANGELOG.md` audit (new PerCoreSnap fields in
      v5.15.1 are slow-path only)
- [ ] All 6 TECH_DEBT closures land in `DOCS/TECH_DEBT.md` with proper
      status updates
- [ ] CHANGELOG.md row written for sprint umbrella (v5.15)
- [ ] HOT_PATH_CHANGELOG entries for any slow-path additions
- [ ] Version.hpp bumped per sub-ship (5.14.post1 → 5.15.0 → 5.15.1 →
      ... → 5.15)
- [ ] Sub-ship postmortems written before sprint umbrella tag
- [ ] Workspace synced after sprint umbrella

---

## Cold-pickup completeness (10 fields per CLAUDE.local.md rule)

1. **Branch state.** Create `feat/v5.15-live-readiness` from engine tag
   `v5.14` (commit `c4e45d1`), NOT from `v5.14.post1`:
   ```bash
   cd /home/caramel/code/FoxML_Trader_v2
   git checkout -b feat/v5.15-live-readiness v5.14
   ```
   Workspace branch follows: `main` continues. Rollback anchors:
   `pre-v5.15` = `v5.14`; `pre-v5.15.0` = engine HEAD pre-ModelHandle-migration.

2. **Phase execution order MATCHES dependency order:**
   - v5.15.0 FIRST — ModelHandle migration is foundational; v5.15.1
     consumes the unified `has_flags` for drift surfacing; v5.15.3
     consumes the canonical pattern for multi-horizon stamping
   - v5.15.1 SECOND — Model Health panel reads v5.15.0's drift bits
   - v5.15.2 — independent of .1; can ship parallel after .0 closes
   - v5.15.3 — independent; can ship parallel after .0 closes. Closes
     CLAUDE.local.md "Known landmine" 2026-05-07 (XGBoost+libgomp+pthread)
     via process-startup setenv fix; also lands per-horizon stamp emit
     for both serial + parallel multi-horizon workers.
   - v5.15.4 — consumes v5.15.2's trading_mode field

3. **First concrete move per sub-ship:** see each subplan's "Step 0"
   section.

4. **Function / constructor / macro names** (verified against current
   code 2026-05-12):
   - `ModelHandle` struct at `ML_Headers/ModelInference.hpp:239`
     (16 uint8_t has_* fields currently)
   - `FOREACH_STAMP_BOUND_MODEL_CONST_PRE_CFG` at
     `ML_Headers/StampBoundModelConstRegistry.hpp:267`
   - `FOREACH_STAMP_BOUND_MODEL_CONST_POST_CFG` at `:377`
   - `STAMP_MODEL_CONST_AUTOPOPULATE` at `:601`
   - `STAMP_MODEL_CONST_AUTOPOPULATE_ONE` at `:680`
   - `STAMP_HAS / SET / CLR` macros at `MemHeaders/BitmapMacros.hpp:78-90`
   - `FOREACH_FAILURE_MODE` at `MemHeaders/FailureModeRegistry.hpp:122`
     (uint16_t storage; static_assert at :212)
   - `FOREACH_LIFECYCLE_CFG_FLAG` at
     `CoreFrameworks/LifecycleCfgFlagRegistry.hpp:55`
     (`MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT` at :58, dormant per
     TECH_DEBT-024)
   - `breakeven_on_partial` wire-up site at
     `CoreFrameworks/PortfolioController.hpp:670` (sister; canonical
     reference for breakeven_on_profit wire-up)
   - `reconcile_mode` parser at
     `CoreFrameworks/ControllerConfig.hpp:2371-2381` (canonical
     reference for trading_mode string-keyed enum parse)
   - `EngineSharded_Run` in `CoreFrameworks/EngineSharded.hpp`
     (boot path; insert point for LiveReadiness_Verify gate)
   - Single-zoo hot-swap site in `CoreFrameworks/EngineSharded.hpp`
     (`v5.10.0c "log-and-leave"` semantics; TECH_DEBT-005 target)
   - `train_model_worker_fn` (single-horizon canonical post-v5.14.post1)
     at `Backtest/BacktestPanels.hpp:3206-3266`
   - `train_multi_horizon_worker_fn` (target for v5.15.3 stamping) at
     `Backtest/BacktestPanels.hpp:3792`
   - `mh_per_horizon_parallel_worker` (parallel variant; default-off per
     CLAUDE.local.md XGBoost+libgomp landmine 2026-05-07) at
     `Backtest/BacktestPanels.hpp:3763`
   - `MLStatusPanel.hpp` at `GUI/MLStatusPanel.hpp` (~504 LOC; existing
     CollapsingHeaders at :320 "Ensemble (multi-horizon)" + :420
     "Thompson Bayesian dashboard"; Model Health inserts between)
   - `PerCoreSnap` struct at `DataStream/EngineTUI.hpp:981`

5. **File:line refs for cited tests / baselines:**
   - 2904/2904 tests pass at `v5.14.post1` (postmortem confirmation;
     handoff baseline). v5.15.0 target ~+20 new tests; sprint umbrella
     target ~+50.
   - tests/controller_test.cpp ModelHandle test sites: enumerate via
     `grep -n "h\.has_\|ModelHandle " tests/controller_test.cpp` at
     v5.15.0 Step 0 (migration list)
   - Canonical migration reference for v5.15.0:
     `Backtest/BacktestEngine.hpp:1147-1220`
     (`Backtest_RunFullValidation` already-migrated sibling)
   - v5.14.post1 sibling fix at `Backtest/BacktestPanels.hpp:3206-3266`
     (`train_model_worker_fn` post-migration; canonical reference for
     multi-horizon stamping pattern)
   - breakeven_on_profit test currently at
     `tests/controller_test.cpp:21803` ("default breakeven_on_profit bit
     OFF (DORMANT — TECH_DEBT-024)"). v5.15.2 amends + adds wired-up
     tests.

6. **Stale-claim audit (completed 2026-05-12 pre-draft):**
   - ✅ ModelHandle has 16 uint8_t has_* fields (handoff said ~17;
     actual 16)
   - ✅ FOREACH_FAILURE_MODE uint16_t storage with static_assert
     (`MemHeaders/FailureModeRegistry.hpp:212`); 2 entries pre-v5.15;
     +7 = 9 of 16; safe
   - ✅ MLStatusPanel is its own file `GUI/MLStatusPanel.hpp` (handoff
     said "DashboardPanels.hpp or similar")
   - ✅ `train_multi_horizon_worker_fn` at `BacktestPanels.hpp:3792`
   - ✅ All cited cfg fields exist: `require_mlockall` (493),
     `model_max_age_hours` (554), `calibration_log_path` (655),
     `held_out_stamp_secret` (768), `reconcile_mode` (865),
     `model_verify_strict` (1538)
   - ⚠️ **`cfg.mode` does NOT exist** — handoff assumed it does;
     v5.15.2 INTRODUCES `trading_mode` cfg field as part of structural
     design (not derived predicate hack)
   - ✅ TECH_DEBT-033 NOT yet in ledger (handoff correct); v5.15.2 Step
     0 writes the entry before sub-ship work
   - ✅ TECH_DEBT-003 surface (`verify_model_stamp` parser ~700 LOC)
     confirmed at `ML_Headers/ModelInference.hpp` (entry at
     TECH_DEBT.md:120-132)
   - ✅ TECH_DEBT-024 surface (`breakeven_on_profit` dormant at
     LifecycleCfgFlagRegistry.hpp:58; sister at PortfolioController.hpp:670)
   - ✅ Tests count: 2904 per handoff (postmortem confirmation; not
     re-run this session). `tests_passed` counter in
     `tests/controller_test.cpp:63`.

7. **Effort claims reconcile:**
   - v5.15.0 ModelHandle migration: ~400 LOC + ~150 LOC parser refactor
     = ~550 LOC. `ML_Headers/ModelInference.hpp` is ~2500+ lines;
     migration reaches ~14-20 caller files.
   - v5.15.1 Model Health panel: ~200 LOC GUI + ~50 LOC PerCoreSnap
     bitmap = ~250 LOC. MLStatusPanel.hpp 504 lines → ~700 post.
   - v5.15.2: ~150 LOC boot gate + ~80 LOC trading_mode cfg + ~50 LOC
     breakeven wire-up + ~20 LOC /readiness check = ~280 LOC.
   - v5.15.3 multi-horizon stamping: ~50 LOC. Mechanical pattern apply.
   - v5.15.4: ~50 LOC strict defaults + ~130 LOC hot-swap snapshot
     infrastructure (covers both single-zoo + ensemble surfaces) = ~180 LOC.

8. **Source-audit references with paths:**
   - Predecessor sprint:
     `plans/v5.14-foxml-port-and-maker/MASTER.md` (sealed)
   - Last sub-sprint postmortem:
     `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-11-v5.14.11-session-postmortem.md`
   - Post-release-fixes log:
     `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-11-v5.14-post-release-fixes.md`
   - Boot log analysis context: chat-time review 2026-05-11; 5 issue
     classes documented
   - Kickoff handoff (this prompt's source):
     `plans/v5.15-live-readiness/handoffs/2026-05-12-v5.15-kickoff-handoff.md`

9. **Predecessor / dependent plans named with paths:**
   - Predecessor: `plans/v5.14-foxml-port-and-maker/MASTER.md` (sealed
     `v5.14` umbrella + `v5.14.post1` patch)
   - Sister: `TESTING_NOTES/v5.12-v5.14-mini.md` (operator paper-test
     guide; v5.15 ships consume this)
   - Dependent (post-v5.15): operator paper-test session

10. **Tag names locked + rollback anchors:**
    - Sprint umbrella: `v5.15` (annotated; created after all sub-ships ship)
    - Sub-ship tags: `v5.15.0`, `v5.15.1`, `v5.15.2`, `v5.15.3`, `v5.15.4`
    - Pre-tag rollback: `pre-v5.15` = `v5.14`;
      `pre-v5.15.0` = engine HEAD pre-migration;
      `pre-v5.15.1` etc. created after each preceding sub-ship lands
    - Post-sprint patches if needed: `v5.15.post1`, `v5.15.post2`, etc.
      (per `.postN` scheme established v5.14.post1)

---

## Data-Oriented Design analysis (per-sub-ship pattern application)

Caramel's framing 2026-05-12: *"make sure were analuzing for proper
struct alignment as well, and other stuff similar to that like the
bitmaps please, those are always welcome."* — DOD discipline is
always-on. Every sub-ship that touches a struct, snapshot field, or
state cluster gets an explicit DOD pass against the catalog. Items
flagged below are MUST-APPLY at the indicated sub-ship; the relevant
DESIGN_SPECS doc + CLAUDE.md item are cited inline.

### v5.15.0 — ModelHandle migration

| DOD concern | Decision | Reference |
|---|---|---|
| **Struct alignment** | `alignas(64)` on ModelHandle (read in slow-path inference dispatch; sized for cache-line); explicit static_assert(sizeof(ModelHandle) % 64 == 0) | `per-snapshot-cluster-layout-pattern.md`; CLAUDE.md item 12 |
| **Bit-packing has_*** | 16 uint8_t has_* fields → 1 uint64_t `has_flags`; saves 15 bytes per handle; branchless `STAMP_HAS(h, name)` check via mask AND | `bitmap-flag-api.md`; CLAUDE.md item 20 |
| **Bit-position stability** | bit positions assigned by FOREACH expansion order; MUST be stable across builds OR use named MASK_STAMP_HAS_* constants (preferred — already established for ModelStampResult) | `x-macro-registry-with-presence-dispatch.md` |
| **Cluster layout (hot-vs-cold)** | hot cluster (booster ptr, has_flags, expected_num_features, expected_num_classes) FIRST 64B; cold cluster (training_run_name char[64], training_timestamp_us, scaler bind data char[65]) SECOND 64B+ | `per-snapshot-cluster-layout-pattern.md` |
| **Padding determinism** | explicit `int<N>_t _padding<N> = 0;` fields between sub-clusters if the migrated layout exposes a gap; struct used in byte-equivalence test contexts (HMAC round-trip) | CLAUDE.md item 27; `struct-padding-determinism-pattern.md` |
| **False sharing risk** | ModelHandle is read by inference (slow-path); never cross-thread-mutated post-load. No false sharing risk. | — |
| **AVX-512 byte determinism** | N/A (no SIMD math on ModelHandle directly) | — |

### v5.15.1 — Model Health panel + PerCoreSnap bitmap

| DOD concern | Decision | Reference |
|---|---|---|
| **PerCoreSnap alignas(64)** | already established via v5.14.10.0 cluster layout. New drift bits MUST land in existing failure_flags uint16_t cluster (no new top-level fields if avoidable). | `per-snapshot-cluster-layout-pattern.md` |
| **Failure-mode bitmap headroom** | uint16_t failure_flags currently 2 entries; +7 = 9 of 16 used. Safe margin remains. **Verify static_assert at FailureModeRegistry.hpp:212 still passes.** | `bitmap-flag-api.md`; CLAUDE.md item 20 |
| **PerCoreSnap 4-bool bitmap (TECH_DEBT-028)** | 4 uint8_t booleans (`ml_scaler_present`, `drift_breached`, `drift_kill_tripped`, `core_kill_tripped`) → uint8_t `per_core_state_flags` bitmap with 4 MASK_PER_CORE_* constants. Saves 3 bytes per PerCoreSnap. | `bitmap-flag-api.md`; CLAUDE.md item 20 |
| **New value-fields (hashes, timestamps)** | `label_hash_at_load`, `feature_hash_at_load`, `build_flags_hash_at_load` (uint64 each), `model_age_seconds_at_load` (uint64) — cluster together; insert in cold-cluster region per CLAUDE.md item 12 display↔execution invariant (GUI reads cold, no hot-path consumer). | `per-snapshot-cluster-layout-pattern.md` |
| **Cross-thread sharing** | slow-path writes hashes at model-load; GUI thread reads. Standard PerCoreSnap publication via double-buffered TUISnapshot (already established). No new sync infrastructure needed. | CLAUDE.md item 8 (TUI decoupling) |
| **Padding determinism** | PerCoreSnap not in byte-equivalence test path today; verify if v5.15.1 introduces any (e.g., snapshot SHA-256 lock test). If yes: explicit padding fields. | CLAUDE.md item 27 |

### v5.15.2 — Live-readiness boot gate + trading_mode + breakeven wire-up

| DOD concern | Decision | Reference |
|---|---|---|
| **trading_mode field placement** | new uint8_t `trading_mode` in ControllerConfig — pack with sister uint8_t cfg fields (`reconcile_mode` at line 865 is the natural sibling cluster); avoid creating a new isolated field that wastes 7 bytes of padding | `per-snapshot-cluster-layout-pattern.md` (cfg analog: HOT-CLUSTER alignas(8) at start of 5 domain bitmaps per v5.14.9.F); item 13 reuse-audit |
| **Cohort audit for trading_mode** | per CLAUDE.local.md going-forward rule 2026-05-11 (cohort-audit when new cfg field has siblings): siblings = `reconcile_mode` (uint8_t enum), `model_verify_strict` (int tri-state). `trading_mode` is uint8_t enum; same family. **Cohort verdict:** all 3 are enum-valued, not boolean, not BIT_FLAG-eligible. Direct uint8_t storage is correct; cohort already homogeneous. No migration needed. | `cfg-flag-eligibility-criteria.md` (5-criteria + cohort section); CLAUDE.local.md 2026-05-11 |
| **trading_mode dispatch (slow-path predicate cache)** | derived `gate_state.is_live = (trading_mode == TRADING_MODE_LIVE)` once at slow-path entry; cached predicate per CLAUDE.md item 18(c) | `slow-path-gate-registry-pattern.md` |
| **Boot gate REFUSE list** | one-time cost path (boot only); no alignment concern. Static array of `LiveReadinessCheck { const char* name; bool (*fn)(const ControllerConfig<F>&, const EngineShardedState<F>&); }` — table-driven dispatch; new check = 1-row addition | `curve-registry-pattern.md` (enum-driven dispatch precedent) |
| **breakeven_on_profit read site** | mirrors `BITMAP_IS_SET(ctrl->config.lifecycle_cfg_flags, MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PARTIAL)` at PortfolioController.hpp:670; same bitmap; no new field | `bitmap-flag-api.md`; CLAUDE.md item 20 |
| **Padding determinism** | trading_mode stamp-bound via FOREACH_STAMP_BOUND_CFG; stamp body emit MUST stay byte-equivalent for legacy stamps (Surface G `has_trading_mode` flag preserves forward-compat). | CLAUDE.md item 27 + item 15 |

### v5.15.3 — Multi-horizon worker stamping

| DOD concern | Decision | Reference |
|---|---|---|
| **No new struct fields** | mechanical pattern apply only; stamp body assembly uses existing STAMP_MODEL_CONST_AUTOPOPULATE companion | — |
| **HMAC chain byte preservation** | per-horizon stamp emit walks the same FOREACH_STAMP_BOUND_MODEL_CONST registry; each stamp's body is byte-identical to single-horizon emit modulo per-horizon fields (grid_member_count, horizon_idx, horizon_count) | `wire-format-byte-preservation-discipline.md` |
| **No alignment regression** | no struct changes | — |

### v5.15.4 — Live mode strict defaults + hot-swap unification

**AMENDED 2026-05-12 post-PARITY-023:** original capture-pointer + Revert
design was structurally broken (Free destroys data in-place; captured
pointers point at freed memory). Plan amended to use SHADOW-LOAD pattern
(`shadow-load-state-transition-pattern.md`): allocate new state into
SEPARATE memory + load + validate + atomic_exchange + Free-old. No
HotSwapSnapshot struct needed; no revert path needed; failure just Free's
the new (failed) allocation.

| DOD concern | Decision | Reference |
|---|---|---|
| **Hot-swap shadow-load** | `aligned_alloc(64, sizeof(EnsembleModelZoo<F>))` for new state allocation; `__atomic_exchange_n(slot, new, __ATOMIC_ACQ_REL)` for swap; Free OLD state after swap succeeds; on failure Free NEW state with pre-swap untouched. NO HotSwapSnapshot struct; no revert path. Same pattern for single-zoo (`CoreModelZoo<F>*`) + ensemble (`EnsembleModelZoo<F>*`) cases. | `shadow-load-state-transition-pattern.md` (canonical pattern); v5.15.4 subplan |
| **alignas(64) retrofit on EnsembleModelZoo + CoreModelZoo** | required for `aligned_alloc(64)` allocation to round-trip cleanly + cache-line discipline on cross-thread access. `static_assert(sizeof(T) % 64 == 0)` verifies size alignment. | `per-snapshot-cluster-layout-pattern.md` |
| **trading_mode default-flip without struct change** | post-parse normalize pass mutates existing cfg fields (model_verify_strict, reconcile_mode); no new fields. No alignment concern. | — |
| **Stamp body byte-equivalence** | default-flip MUST NOT change stamp body emit at canonical byte level — verified via `/parity-check` (cfg-default-flip is a parse-time normalize; stamp emit reads normalized value; legacy cfgs with explicit overrides still produce same stamp body bytes) | `wire-format-byte-preservation-discipline.md`; CLAUDE.md item 15 |
| **Cross-thread atomic swap** | `__atomic_exchange_n` on aligned pointer = single x86_64 instruction; lock-free; readers see old OR new, never torn. Single-owner write (boot gate / operator-triggered slow-path) means no RCU grace period needed before Free-old. | CLAUDE.md item 5 (lock-free reader-side discipline) |

### Cross-sprint DOD invariants (every sub-ship)

- No new false-sharing risk introduced (cross-thread writes always on
  separate cache lines from hot-path reads)
- All new struct fields with byte-equivalence consumers (HMAC,
  memcmp, SHA-256-lock tests) get explicit `_padding<N> = 0` fields
  per CLAUDE.md item 27 if a gap exists
- All new boolean storage uses BITMAP_* API (CLAUDE.md item 20); no new
  byte-per-flag patterns
- All new enum cfg fields go through the cohort-audit per
  CLAUDE.local.md going-forward rule 2026-05-11
- `/dod-audit` skill runs at each sub-ship close to verify the catalog
  was applied; HIGH-RISK ships also at pre-coding gate

---

## DESIGN_SPECS / CLAUDE.md cross-references

Catalog: `tick-trader-percore-workspace/DESIGN_SPECS/README.md`
(27 patterns post-v5.14.11.B).

| Sub-ship | DESIGN_SPECS that MUST be read | CLAUDE.md items |
|---|---|---|
| v5.15.0 | x-macro-registry-with-presence-dispatch, autopopulate-pattern-for-production-caller-class, pre-post-cfg-registry-split-for-emit-order-preservation, bitmap-flag-api, wire-format-byte-preservation-discipline, struct-padding-determinism-pattern, audit-driven-pre-coding-gate, structural-fix-preferred-decision-framework | 13, 15, 19, 20, 21, 22, 27 |
| v5.15.1 | bitmap-flag-api, per-snapshot-cluster-layout-pattern, transient-aggregation-bitmap-pattern | 1, 12, 20 |
| v5.15.2 | slow-path-gate-registry-pattern, curve-registry-pattern (for trading_mode enum dispatch), cfg-flag-eligibility-criteria (cohort audit per going-forward rule 2026-05-11) | 13, 18 |
| v5.15.3 | wire-format-byte-preservation-discipline, autopopulate-pattern-for-production-caller-class, avx512-byte-determinism-pattern (cross-mode byte-identity test extends item 25 discipline) | 15, 16 (reuse-audit; helper extraction), 21, 22, 25 |
| v5.15.4 | shadow-load-state-transition-pattern (PRIMARY — canonical pattern for HotSwap unification), cfg-flag-eligibility-criteria, structural-fix-preferred-decision-framework, per-snapshot-cluster-layout-pattern (alignas(64) retrofit on zoo containers), wire-format-byte-preservation-discipline (default-flip MUST NOT change stamp body bytes) | 5, 13, 15, 19, 27 |

---

## What's intentionally NOT in scope for v5.15

Per "no rotting tech debt" rule, this list captures explicit deferrals:

- **Operational safety audit** (drawdown circuit breaker, daily PnL cap,
  position size limits, kill switch) — sequenced AFTER v5.15 closes;
  paper-test informs design
- **Order book data source / Maker MVP** (TECH_DEBT-008) — deferred
  indefinitely per existing entry; no consistent order book data source
- **Paper-test execution** — sequenced AFTER v5.15 ships
- **TECH_DEBT-011 FOREACH_PER_CORE_SNAP_FIELD full registry** —
  10-15h architectural ship; would scope-creep v5.15.1 by ~5x. The
  failure-mode subset (7 new entries in v5.15.1) doesn't trigger the
  broader visible-state registry; defer until 5+ general PerCoreSnap
  fields land in one umbrella.
- **TECH_DEBT-009 FOREACH_CFG_FIELD non-bool subset** — trigger
  requires 3+ new non-bool cfg fields in one umbrella; v5.15 adds 1
  (trading_mode). Defer.
- **TECH_DEBT-018 /precoding-audit Layer 1 orchestrator skill** —
  workflow improvement; manual 4-subagent dispatch field-tested in
  v5.15.0. Codification = separate ship.
- **TECH_DEBT-022 cfg parser perfect-hash dispatch** — boot-only
  optimization; not live-readiness scope.
- **TECH_DEBT-026 per-core bandit_algorithm override** — different
  feature; not gated by live-readiness.
- **TECH_DEBT-029 Source file length reduction** — separate cleanup
  sprint per its entry.
- **TECH_DEBT-031 MetricsLog FOREACH registry** — different surface; no
  v5.15 touch.
- **TECH_DEBT-032 CLAUDE.md context-management cleanup** — separate
  sprint per its entry.

---

## References

**Engine codebase entry points:**
- `CLAUDE.md` (always loaded; items 1-27)
- `CLAUDE.local.md` (private project memory; going-forward rules)
- `DOCS/CLAUDE_ML_INVARIANTS.md` — read for v5.15.0 (FeatureRegistry /
  MLBuildContext / train-serve path) + v5.15.3 (multi-horizon worker
  stamping)
- `DOCS/CLAUDE_INVARIANTS.md` — read for v5.15.2 (boot path / cfg parser)
  + v5.15.4 (cfg defaults)
- `DOCS/CLAUDE_INTEGRATION.md` — read for v5.15.1 (new GUI panel / new
  PerCoreSnap fields / FOREACH_FAILURE_MODE extension) + v5.15.2 (new
  cfg field trading_mode)
- `DOCS/CLAUDE_REVIEW.md` — referenced for the 10-item cold-pickup
  checklist (this MASTER follows it)
- `DOCS/STRATEGY_AND_CODING_RULES.md` (private; 11 strict invariants)
- `DOCS/LATENCY_OPTIMIZATION_AUDIT.md` (private; 13 parts; relevant for
  v5.15.2 if boot gate touches slow-path)

**Workspace deliverables:**
- `tick-trader-percore-workspace/DESIGN_SPECS/README.md` (27 patterns)
- `tick-trader-percore-workspace/DOCS/TECH_DEBT.md` (30 entries; 6 to
  close)
- `tick-trader-percore-workspace/DOCS/HOT_PATH_CHANGELOG.md` (sprint-end
  if hot-path additions)
- `tick-trader-percore-workspace/DOCS/CHANGELOG.md` (sprint-end row)
- `tick-trader-percore-workspace/TESTING_NOTES/v5.12-v5.14-mini.md`
  (operator paper-test guide; reference for paper-test cadence post-v5.15)
