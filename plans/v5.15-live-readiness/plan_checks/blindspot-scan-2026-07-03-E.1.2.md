# /blindspot-scan report — E.1.2 NodeState SoA/Money relayout — 2026-07-03

**Plan:** `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md` (v1.0-reformalized, HEAD `b10e778`)
**Scope emphasis:** B14 (multi-surface deletion ordering) + B15 (unconditionalization latent assumption); + type-change cascade / zero-init / row-order / TD-180 version-bump guard per orchestrator kit.
**Fire posture:** post-audit-gate implementation-detail pass (M4). READ-ONLY. No auto-proceed.
**Tools RUN:** `gen_code_map.sh --composition "Position<64>"` + `--composition "Position<F>"` + `--byte-context "Position<64>"`; `check_struct_alignment.py`; `check_identifier_retirement.py` SOURCES/ledger read.

## Summary
- Pillars fired: 8 (B1, B10/H12, B12, B14, B15 + type-cascade / zero-init / TD-180 sub-walks)
- GUARDED-BY-BUILD: 2 (B12 row-order; the retirement-guard *hard* failures + test static_asserts)
- SILENT-RISK: 4 (B10/H12 zero-init; B1 tool-blind container claim; B14 orphan-cohort residue; TD-180 no size→version guard)
- N-A / SAFE: 2 (B15 classic unconditionalize form N-A; deletion-live-consumer assumption SAFE)
- Overall: **YELLOW → leaning RED on B10/H12 (capital-bearing) + TD-180**

## Per-pillar verdicts

| Pillar | Verdict | Finding (file:line HEAD b10e778) |
|---|---|---|
| **B10/H12 zero-init** | **SILENT-RISK (capital)** | `Position_Reset` (`Portfolio.hpp:221-231`) is a HAND-LIST of 9 fields — the exact "subset-zeroing class" its own comment (`:216-219`) warns against; appending `peak`/`owner_node_id` registry rows silently re-opens it. `Portfolio_OpenSlot(args)` (`:358-383`) + `Portfolio_AddPositionWithExits` (`:268-284`) write fields individually with NO `Position_Reset`. `Portfolio_RemovePosition` (`:299-301`) clears only the bitmap bit — slot Position bytes persist. On slot reuse the new reserved fields carry STALE prior-occupant bytes. Live serializer `ShardedSnapshotPersist.hpp:169` dumps `sizeof(Position<F>)` (FULL struct) → stale bytes persisted → D-206 `peak` feeds the hot ratchet = capital bug + cross-run non-determinism. **NOT caught by the golden-master round-trip** (save-stale→load-stale round-trips identically). |
| **B1 type-cascade** | **SILENT-RISK (unsound justification; conclusion holds)** | Plan's "`--composition Position<64>` = 0 transitive containers → CONTAINED" is a TOOL ARTIFACT. Re-ran BOTH concrete + template form → both report "0 DIRECT + 0 TRANSITIVE", yet `Portfolio<F>` literally holds `Position<F> positions[16]` (`Portfolio.hpp:155`); `PortfolioController.hpp:159`, `OrderEventLog.hpp:710`, `OrderManager.hpp:308` (`alignas(64) Portfolio<F> portfolio`) all contain it; `ShardedSnapshotPersist.hpp:391` load-staging `Position<F> positions[16]`. The `--composition` tool is BLIND to `Type<F> name[N]` array-member edges (D-202 cascade-tool defect). Real reason it's contained: NONE of the containers is whole-struct byte-serialized or carries `static_assert(sizeof==N)`; OMS is held by POINTER (`state->oms->`) so the one nearby pin `sizeof(NodeContext<64>) % 64 == 0` (`ControllerEventLoop.hpp:555`) is both unaffected AND modulo-tolerant. Verified-safe, but by audit not by the cited tool. |
| **B14 deletion ordering** | **SILENT-RISK (orphan residue) + GUARDED (hard fails)** | Retirement lockstep is real: deleting `#define CONTROLLER_SNAPSHOT_VERSION` (`PortfolioController.hpp:2025`) while keeping SOURCES row → `check_identifier_retirement.py:189` `sys.exit` PARSE-ERROR (Check H hard-crash). Deleting `#define` + SOURCES row (line **58** specifically, NOT the ":57-59" block — 57=SHARDED/59=PORTFOLIO are BUMPS, they STAY) without `--update` → `compare():239` REMOVED violation (soft RED). So {dead fns + `#define` + SOURCES row 58 + `--update`} = ONE atomic commit. **UNDER-ENUMERATED cohort:** deleting `PortfolioController_LoadSnapshot` orphans `ConfidenceScorer_ShadowLoadLegacyV1` (`ConfidenceScore.hpp:1122`, sole caller `PortfolioController.hpp:2219`) + `LEGACY_CONFIDENCE_VERSION` (`:2026`, sole use `:2217`) → silent dead `static inline` (H21: inline deadness NOT compiler-caught). Stale "=11" comments: `ConfidenceScore.hpp:1050/1065/1066`. **H21 drop-vs-tombstone tension:** `--update` (`write_ledger:209`) writes only live SOURCES → silently DROPS CONTROLLER=14 row, violating the ledger's own "NEVER drop a row; tombstone" header — defensible (dead path, 14 never reused) but the tool offers no tombstone path once the `#define` is gone; the golden ledger loses that 14 existed. **Test cohort:** deleting CONTROLLER breaks 3 test sites not 1 — `controller_test.cpp:11395` (`SHARDED==10u`), `:26749-26757` (triple `>=` static_assert + runtime, references CONTROLLER), `:26819` (`CONTROLLER==14`). Plan cites only "≈:26819". |
| **B15 unconditionalize** | **N-A (classic) / SAFE (deletion-assumption)** | No `if(cfg.X==VALUE)`→unconditional gate-removal in this plan. Deletion-latent-assumption "no live consumer breaks" HOLDS: dead serializers have 0 live callers (rg confirmed comment-only refs; 0 test callers); the SHARED ConfidenceScore field-walk survives via `ShardedSnapshot_Load` (`ConfidenceScore.hpp:1050` names both paths; live path uses field-walk not ShadowLoadLegacyV1). Only latent GAP = the orphan residue (folded into B14). |
| **TD-180 version-bump guard** | **SILENT-RISK (open; bites HERE)** | TD-180 (workspace `open.md:3296`, sev med-high) CONFIRMED unclosed; reformalize handoff `:78` names E.1.2 as where it bites. NO size→version guard exists. Both epoch tripwires are TYPE-keyed, not SIZE-keyed: `Portfolio.hpp:540` (`!is_fp_decimal_v<...> \|\| VERSION>=7`), `ShardedSnapshotPersist.hpp:99` (`MONEY_ENCODING_EPOCH==0 \|\| ...`). A Position SIZE change (128→192) trips NEITHER. The size-pin (`Portfolio.hpp:117`) trips LOUD but is satisfied by editing the literal to 192 — no version bump forced. **The golden-master byte-compare does NOT cover it** (round-trip is self-consistent at any single version; fixtures regen AT the new layout → passes regardless of the `#define`). Retirement tool sees VERSION still=7 → GREEN. Version-equality tests only catch a bump you DID make. So NOTHING forces the bump — handoff's "or let the golden-master cover it" is UNSOUND for this failure mode. |
| **B12 row-order drift** | **GUARDED-BY-BUILD** | `FOREACH_POSITION_FIELD` append-only (`PositionFieldRegistry.hpp:42-59`); offset `static_assert`s (`Portfolio.hpp:122-130`) trip LOUD on any insert/reorder of existing fields. Phase C single-registry-drive co-derives struct+wire order (eliminates the B12 divergence class). Caveat: new-field offset asserts MUST be added (plan commits, Phase B) — omit → future reorder of the reserved fields is silent. |

## SILENT-RISK punch-list (ordered by severity)

1. **[B10/H12 · CAPITAL · BLOCKING] Convert `Position_Reset` (`Portfolio.hpp:221`) to a `FOREACH_POSITION_FIELD` walk** (`X(name,type,init,...) p->name = init;`) so new registry rows auto-zero. THEN ensure every open path resets: `Portfolio_OpenSlot(args)` (`:358`) + `Portfolio_AddPositionWithExits` (`:268`) call `Position_Reset` first (or explicitly zero the reserved fields). `AddPosition`(:288)/`Init`(:232)/`ClearPositions`(:425) already route through Reset → auto-covered once Reset is registry-driven. Closes the subset-zeroing class STRUCTURALLY (guards-compound) vs re-patching per-site.
2. **[TD-180 · BLOCKING] Wire a size→version tripwire**, co-located with the Position size-pin (~zero marginal cost since the literal is already being edited 128→192): `static_assert(sizeof(Position<64>) != 192 || PORTFOLIO_SNAPSHOT_VERSION >= 8, "Position grew to 192B — bump PORTFOLIO_SNAPSHOT_VERSION to 8 in THIS commit");` mirroring the existing type-keyed tripwire at `Portfolio.hpp:540`. Do NOT rely on the golden-master (proven unsound above). TD-180 is SUBSUMED by this ship → close it here, don't defer.
3. **[B1 · MED] Re-justify the "contained" claim** on the REAL container set (`Portfolio.hpp:155`, `OrderManager.hpp:308`, `PortfolioController.hpp:159`, `OrderEventLog.hpp:710`, `ShardedSnapshotPersist.hpp:391`) + the "none whole-serialized / none exact-size-pinned / OMS-by-pointer" verification — STOP citing `--composition = 0 containers` (tool is blind to `Type<F> arr[N]`). File a TECH_DEBT on the D-202 cascade tool's array-member blindness (it gave a FALSE all-clear on a core-struct layout change — the exact class the tool exists to prevent).
4. **[B14 · MED] Extend the Phase-D deletion cohort** (leaves-first): delete `ConfidenceScorer_ShadowLoadLegacyV1` (`ConfidenceScore.hpp:1122`) + `LEGACY_CONFIDENCE_VERSION` (`PortfolioController.hpp:2026`) + sweep stale "=11" comments (`ConfidenceScore.hpp:1050/1065/1066`, `PortfolioController.hpp:2019/2023/2213`) in the same commit as `PortfolioController_LoadSnapshot`. Enumerate the SOURCES row precisely = line **58** only. Enumerate all 3 test sites (`:11395`, `:26749-26757`, `:26819`).
5. **[B14/H21 · LOW-MED] Document the CONTROLLER=14 drop as an intentional H21 exception** in the decision log (dead path, 14 never reused) — `--update` silently drops the ledger row with no tombstone trail; note the tool has no tombstone path once the `#define` is deleted.
6. **[B10/H12 · MED] Confirm Phase C (field-walk) actually lands** for BOTH save (`:169`) and the load-mate TRIPLE (`:391`/`:394`/`:497`) — the field-walk is what makes the 192B layout H12-hole-immune (walks named fields, skips padding). If any leg keeps a `sizeof`-blob memcpy, every inter-field padding hole in the reserved ~48B becomes an uninitialized wire byte. Enumerate the reserved sub-layout with explicit `_pad`+`=0` fields as the belt-and-suspenders.

## Novel alternatives considered (feedback_proactive_novel_alternative_consideration)
- **Zero-init:** rather than fix `Position_Reset` AND each open-site, make `Portfolio_RemovePosition` (`:300`) call `Position_Reset` on close so a freed slot is ALWAYS clean → any future open-path is safe-by-default. Verdict: ATTRACTIVE (defense-in-depth) but adds a slow-path write on every close; RECOMMEND the registry-walk `Position_Reset` as primary (closes the class) + this as optional hardening. NOT a substitute for open-site correctness.
- **TD-180:** generalize the size literal to a `POSITION_WIRE_LAYOUT_HASH` (offset+type digest) → version binding, catching REORDERS at equal size too (the D-181 epoch-net shape). Verdict: superior coverage but heavier; the size-keyed `static_assert` is the proportionate close for THIS ship (a reorder is already caught by the offset asserts). Home the hash-binding to the TD-180 close if it recurs.

## Blocking gaps (consult before coding)
- **B10/H12 zero-init** and **TD-180** are the two BLOCKING items — both are capital/determinism SILENT-RISKs invisible to the stated golden-master gate. The other four are amend-the-plan (enumeration/justification) not code-blockers.

## Inflection check
- New pillars surfaced: 0 (findings map to existing B1/B10/B12/B14/B15 + the D-202 tool-defect class).
- Meta-signal: the D-202 cascade tool's array-member blindness caused the plan's central "contained" claim to rest on a false all-clear — candidate for a tool-hardening TECH_DEBT (the tool is the mandatory CoreFrameworks gate; a false-negative there is high-leverage).

**Spots most worth an adversarial (a-class) refute:** (1) my claim the golden-master round-trip canNOT catch a forgotten version bump — push on whether fixtures are cross-version-frozen anywhere; (2) my "OMS-by-pointer so NodeContext %64 is safe" — push on whether any container IS whole-serialized/memcmp'd off the paths I checked; (3) whether `Position_Reset` registry-drive is truly the SSoT or whether a replay/FromEventLog path constructs Position bytes bypassing all of Init/Open/Add.
