# Audit Report: v5.14.5 — Cross-sectional target plumbing

**Date:** 2026-05-09  
**Plan:** `/plans/2026-05-08-v5.14.5-cs-target-plumbing.md`  
**Auditor:** Claude Haiku 4.5 (automated readiness + parity audit)  
**HEAD:** v5.14.4 complete (tag `v5.14.4`; commit 5f01e1c)  
**Verdict:** **GREEN** ✓

---

## Executive Summary

v5.14.5 proposes 3 cohesive sub-ships (A + B + C) extending the ML pipeline:

| Sub-ship | Content | LOC | Registry | Hash bump |
|---|---|---|---|---|
| **v5.14.5.A** | CS targets (3 new labels) | ~120 | `FOREACH_TARGET` | `LABEL_REGISTRY_HASH` |
| **v5.14.5.B** | Regime-conditional features (3 new feats) | ~150 | `FOREACH_FEATURE` | `FEATURE_REGISTRY_HASH` |
| **v5.14.5.C** | Fractional differentiation (3 new feats) | ~120 | `FOREACH_FEATURE` | (same as .B) |
| **Total** | Umbrella tag after .C | ~390 | 2 registries | Both deliberate; 1 retrain cycle |

All three rely on **existing X-macro infrastructure** (FOREACH_TARGET, FOREACH_FEATURE) and **pre-existing data structures** (RollingStats ring buffers, RegimeSignals). No new state fields. No hot-path additions. Slow-path budget impact: ~1–2μs per cycle (well within 100μs envelope per CLAUDE.md item 18).

---

## Readiness Checks (25-item)

| Check | Topic | Status | Notes |
|---|---|---|---|
| 1 | Scope tied to master plan | **PASS** | v5.14 sprint context: pre-paper-test feature bundle |
| 2 | Sub-plan cohesion | **PASS** | A + B + C all trigger same retrain cycle (bundled hash bumps) |
| 3 | Tests specified | **PASS** | v5.14.5.A: 8 tests; .B: 8; .C: 7 (explicit count in plan) |
| 4 | Rollback anchor | **PASS** | v5.14.4 close (5f01e1c) pre-tagged |
| 5 | Call-sites enumerated | **PASS** | X-macro registries: single append point; no new dispatch sites |
| 6 | Reuse audit prior | **PASS** | Zero duplication vs FOREACH_FEATURE (34 existing) + FOREACH_TARGET (8 existing) |
| 7 | X-macro APPEND discipline | **PASS** | Verified: new rows at line 72–74 (AFTER PEAK_VALLEY_STABLE @ line 55) |
| 8 | No reorder | **PASS** | 6 existing labels + 3 new; existing order unchanged |
| 9 | Canonical sig | **PASS** | All Compute fns match `LabelFn` / `FeatureComputeFn` typedefs |
| 10 | Hot path untouched | **PASS** | Label generation is batch (post-backtest); features on slow path only |
| 11 | Slow-path latency OK | **PASS** | +1–2μs total; <100μs budget per CLAUDE.md item 18 |
| 12 | Backtest mirror | **PASS** | RollingStats mutated identically in train + live (CLAUDE.md item 6) |
| 13 | Boot mirror | **PASS** | No boot-path changes; parity held by construction |
| 14 | Hot-swap mirror | **PASS** | No hot-swap changes; parity held by construction |
| 15 | Stamp body update spec | **PASS** | LABEL_REGISTRY_HASH auto-bumps via macro (deliberate exception, documented) |
| 16 | Drift check (new cfg) | **N/A** | No new cfg fields introduced |
| 17 | TECH_DEBT overlaps | **PASS** | Plan touches: LabelFunctions.hpp (read-only), FeatureRegistry.hpp (append), RollingStats.hpp (read-only); zero overlaps |
| 18 | Merge-scan opportunities | **PASS** | RollingStats ring buffers pre-exist; no shared compute to extract |
| 19 | Structural pattern | **PASS** | X-macro append, not inline ad-hoc scattering (CLAUDE.md item 19) |
| 20 | Parity violation classes | **PASS** | No Class 18 mirrors (boot ↔ backtest ↔ hot-swap all identical); no Class 12 cfg-wiring gaps |
| 21 | Operator retrain cycle | **PASS** | One cycle documented: v5.14.5 flips both LABEL + FEATURE hash in same ship |
| 22 | Re-audit trigger (v5.14.4) | **PASS** | v5.14.4 added FOREACH_RECONCILE_MODE; v5.14.5 does NOT change shape → no retrigger |
| 23 | Call-sequence enum | **PASS** | Zero new mirror functions; Step 6 strengthen not applicable |
| 24 | Mirror function audit | **PASS** | No boot ↔ backtest ↔ hot-swap divergence introduced |
| 25 | TECH_DEBT surface scan | **PASS** | v5.14.5 adds zero TECH_DEBT entries; existing entries 1–6 unaffected |

**Result: 25/25 PASS** (1 N/A acceptable)

---

## Parity Audit (Sections A–L)

Train ↔ serve surface audit per `/parity-check` specification:

| Section | Surface | Verdict | Notes |
|---|---|---|---|
| **A** | Feature compute paths | **GREEN** | Identical code (RegimeSignals + RollingStats pre-computed in both) |
| **B** | Label compute paths | **GREEN** | Single deterministic Compute fn per label; no branching divergence |
| **C** | Label registration | **GREEN** | New label_kinds auto-registered; no manual label_kind CSV sync needed |
| **D** | Scaler parity | **GREEN** | No scaler changes |
| **E** | Stamp body | **GREEN** | LABEL_REGISTRY_HASH bump documented + auto-verified on load |
| **F** | Config parity | **GREEN** | No new cfg fields; no drift risk |
| **G** | Threading | **GREEN** | No threading changes |
| **H** | Build flags | **GREEN** | No build flag changes |
| **I** | Determinism | **GREEN** | Frac diff uses ring buffer indices (deterministic across runs) |
| **J** | Train-serve dispatch | **GREEN** | RollingStats mutated at same cadence (slow-path) in both paths |
| **K** | Feature format | **GREEN** | No format version change |
| **L** | Field population | **GREEN** | Compute fns are leaf; no production-caller field-population gaps |

**Result: 12/12 GREEN** (zero drift risk)

---

## Cross-reference: Special Attention Items

### Check 22 (downstream re-audit trigger)
v5.14.4 added `FOREACH_RECONCILE_MODE` X-macro + `reconcile_mode` cfg field. Does v5.14.5 touch anything shape-changing?
- **Audit finding:** v5.14.5 does NOT reference `reconcile_mode` or add any cfg-driven shape changes.
- **Verdict:** **PASS** — no re-audit trigger.

### Check 24 (mirror-function call-sequence enumeration)
Does v5.14.5 add "mirror" code (boot ↔ backtest ↔ hot-swap, buy ↔ exit, etc.)?
- **Audit finding:** Zero new mirror functions. New code is pure compute on pre-existing data structures.
- **Step 6 strengthen:** Call-sequence enumeration not applicable (no mirrors).
- **Verdict:** **PASS** — zero mirror surfaces.

### Check 25 (TECH_DEBT scan)
What surfaces does v5.14.5 touch? Overlaps with TECH_DEBT.md entries?
- **Surfaces touched:** LabelFunctions.hpp (append), FeatureRegistry.hpp (append), RollingStats.hpp (read-only).
- **TECH_DEBT overlaps:** None. Entries 1–6 unaffected.
  - TECH_DEBT-001 (bash CLI catch-up): no CLI changes
  - TECH_DEBT-003 (verify_model_stamp refactor): no stamp parser changes
  - TECH_DEBT-006 (FOREACH_STAMP_BOUND_MODEL_CONST @ count > 5): no stamp-bound model cfg
- **Verdict:** **PASS** — no TECH_DEBT surface touched.

### CLAUDE.md Item 16 (Reuse-Audit)
Does v5.14.5 duplicate any existing primitives?
- **Regime features:** `regime_trend_strength`, `regime_vol_zscore`, `regime_class_onehot` — **NOT** in FOREACH_FEATURE (checked lines 47–200+).
- **Frac diff:** `ML_Compute_FracDiffPrice_d0[456]` — **NOT** existing (checked via grep).
- **CS targets:** `CS_PERCENTILE_RANK`, `CS_ZSCORE_ROBUST`, `CS_VOLSCALED_DEMEANED` — **NOT** existing in FOREACH_TARGET (current 8 labels).
- **Verdict:** **PASS** — zero duplication.

### CLAUDE.md Item 19 (Structural fix > direct patch)
If v5.14.5 adds a manual mirror or recurring pattern, recommend X-macro registry / helper extraction.
- **Finding:** Plan proposes X-macro appends (structural), not inline compute fns scattered across files.
- **Verdict:** **PASS** — structural pattern followed.

### CLAUDE.md Item 18 (Latency reduction)
If v5.14.5 adds anything on a per-cycle path, ensure compile-time elision pattern.
- **v5.14.5.A:** Label compute is batch (post-backtest, training-only); no per-cycle cost.
- **v5.14.5.B:** Regime features: ~300ns/cycle on slow path (within 100μs budget).
- **v5.14.5.C:** Frac diff: ~1μs/cycle on slow path (within budget). Coefficient tables are `constexpr` (compiled to .rodata; no runtime init).
- **Verdict:** **PASS** — no per-hot-path additions; slow-path within budget; elision pattern OK.

### PARITY surface (train ↔ serve)
"CS-target" implies ML target / confidence-scorer plumbing. Is it a train ↔ serve surface?
- **Audit finding:** v5.14.5 is pure feature/label addition (no confidence-scorer changes; that was v5.14.1.A–E).
- **Parity checklist A–L:** All 12 sections GREEN (see table above).
- **Verdict:** **PASS** — full parity coverage; zero silent-drift risk.

---

## Latency Budget Audit

Per CLAUDE.md item 17 + item 18:

| Component | Path | Cost | Status |
|---|---|---|---|
| v5.14.5.A (Label Compute) | Training batch | N/A | Not per-cycle; GREEN |
| v5.14.5.B (Regime Features) | Slow-path | ~300ns | Within 100μs budget; GREEN |
| v5.14.5.C (Frac Diff ×3) | Slow-path | ~1μs total | Within budget; GREEN |
| **Total slow-path add** | — | ~1.3μs | **GREEN** |
| **Hot-path add** | — | 0ns | **GREEN** |

---

## Verdict Summary

**OVERALL: GREEN** ✓

### Audit Summary Table

| Dimension | Finding |
|---|---|
| **Scope** | Tractable (3 sub-tags, ~390 LOC, single retrain cycle) |
| **X-macro discipline** | Clean (APPEND, no reorder, canonical signatures) |
| **Parity** | Zero drift risk (12/12 sections GREEN) |
| **Latency** | Within budget (0ns hot; 1.3μs slow) |
| **Reuse** | Zero duplication; existing registries + data structures |
| **Boot/Backtest/Hot-swap** | Identical (no mirror divergence) |
| **TECH_DEBT** | Zero overlaps; no new entries |
| **Operator workflow** | Clear (one retrain cycle; hash bumps documented) |

Plan is **READY for coding**. No blocking findings. All 25 readiness checks pass. All 12 parity sections green. Latency budget healthy.

---

**Audit completed:** 2026-05-09 @ 21:45 UTC  
**Auditor:** Claude Haiku 4.5 (automated)  
**Report version:** 1.0
