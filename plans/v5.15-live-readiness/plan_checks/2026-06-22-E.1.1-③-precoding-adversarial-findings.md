---
type: plan-check
subtype: precoding-adversarial-findings
ship_tag: v5.15.5.F.4d.1.E.1.1
workstream: "③ config-compiler — pre-write adversarial scan on the 4b implementation shape"
date: 2026-06-22
head_at_scan: 03830e8
status: LIVING (open completeness items pending — see § Open enumeration)
design_ssot: plans/v5.15-live-readiness/plan_checks/2026-06-22-E.1.1-config-compiler-IA-cascade-synthesis.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
agents: [a-class correctness refuter, a-class coverage refuter]
disposition: PENDING operator decision (R-explicit vs R-structural + the completeness sweep)
---

# ③ pre-write adversarial scan — findings (the 4b shape, before the diff existed)

Two `a-class` adversarial refuters (default-refuted, cross-checked; no AR-11 conflict) on the proposed 4b malformed-capture shape. Fired BEFORE writing — the point was to catch a shape/coverage flaw while it's free to fix.

## Verdicts
- **Correctness refuter — CONFIRMED-SOUND (all 7 axes).** The per-node 4b M1 shape (descriptor-gated `_PARSE_OV_PCT` + unconditional capture on the 2 legacy arrays) is correct. The `999`→OVERFLOW worry is a **non-bug** (`999`→flags=0; OVERFLOW floor ~92e9). Class 48 clean (`0`=inherit + clean values never fault), EXCESS_DP stays WARN, all override names compile-resolve, wire-context genuinely file-only (B2 holds), determinism untouched, M1 reuses the registry tag (no Class 27). One architectural caveat: 3 hand-rolled copies of the flat-walker gate = SSoT divergence.
- **Coverage refuter — REFUTED (real gap).** Per-node set is complete, but the GLOBAL claim is false.

## FINDING 1 (🔴 verified) — 4 non-registry capital fields silently uncovered
Four capital-bearing Money fields are parsed by LIVE, flag-discarding, hand-rolled branches the step-3 walker provably never touches (no registry row → no `CAPITAL_BOUND` → walker can't gate; no registry key → not walker-shadowed; their manual branch is the ONLY parse path).

| Field | Site (`ControllerConfig.hpp`) | Macro | `=banana` → | Severity |
|---|---|---|---|---|
| `starting_balance` | `:2253` | `CFG_PARSE_MONEY` (`:2211`, takes `.value`) | balance **0** → every exposure/drawdown limit ×0 | 🔴 founding bug — the **denominator** |
| `min_kill_loss` | `:2318` | `CFG_PARSE_MONEY_POS` (`:2204`; `<0`→0 does NOT catch malformed-0) | kill floor **$0** | 🔴 founding bug — **SAFETY-CRITICAL** |
| `min_sl_tp_ratio` | `:2264` | `CFG_PARSE_MONEY` | ratio floor 0 (disabled) | 🟠 relational |
| `min_hold_gain_pct` | `:2446` | `CFG_PARSE_PCT` (`:2219`) | hold-gain gate 0 (disabled) | 🟠 gate |

Verified: all 4 absent from every `FOREACH_*_CFG_FIELD` (grep empty); the 3 macros all take `Money_FromString(val).value` and discard `.flags`; `Money_FromString("banana")={value:0, flags:MALFORMED}` (`FixedPointN.hpp:1791`). Shipping ③ without these = a **false-completeness ship** — the founding bug stays open on the balance denominator + a safety floor while the ship declares the capital-malformed surface closed.

## FINDING 2 (meta / methodology) — a known homing was DROPPED across a decision supersession
The design's own SSoT NAMED all 4 (synthesis item 4: *"4 non-registry capital fields → migrate INTO the registry OR explicit DOLLAR/ratio checks (NOT percent)"*) — but that homing lived inside **D-250**, which was **CLOSED/superseded** by D-252→D-253. The homing closed *with the entry*, and was not carried into the D-254 coding scope (the REMAINING-③ list never mentions the 4). **Root cause: superseding a decision dropped its still-live obligation.** Class-33 (categorical "nothing-else" over an under-enumerated set) + a codifiable methodology gap: *when a decision is superseded, its NAMED-but-UNRESOLVED homings must be carried forward or explicitly re-homed, not closed by association.* → candidate AR/Mn bump.

## FINDING 3 (🔴 second-order) — the planned TD-215 deletion would BRICK these 4
③'s REMAINING scope includes *"delete the dead `CFG_PARSE_PCT/MONEY` capital branches (TD-215, walker-shadowed)."* These 4 branches are in that `CFG_PARSE_MONEY/PCT` family but are **LIVE, not shadowed**. Deleting them removes the only parse path → with the planned HARD-REFUSE, `starting_balance=10000` becomes an unknown-key fault → **operator can't configure their balance/kill-floor.** TD-215's "25 dead branches" set MUST be verified field-by-field (registry-shadowed = safe to delete; non-registry = live, keep-or-migrate-first).

## Code-time blindness (why the scan was load-bearing)
This would have shipped GREEN: build passes (branches compile); the planned List-C tests assert on the registry fields (`stop_loss_pct=banana`, `risk_pct=999`), NOT the 4 dropped fields → tests pass; V-class (asan/ubsan/determinism/calls_graph) is not a coverage check → passes; `cfg_compile_ok()` only gates fields that SET a fault bit, and these never do → passes. A coverage-enumeration gap is invisible to the entire code→build→test→V-class→boot-gate pipeline. Only an adversarial "is the SET complete?" agent (or a structural guard) catches it → motivates a recurrence guard (M7).

## Open enumeration (NOT yet proven complete — do before locking scope)
We found 4 via ONE agent on ONE file. Not yet swept:
1. The **GUI parser** (`GUI/SettingsPanel.hpp:922`, a second cfg parser) — does it validate any capital? Likely a parallel gap.
2. The **full TD-215 "25 dead branches" set** — verify each is genuinely walker-shadowed (vs live like these 4).
3. **Other dropped homings** across the D-250→D-254 transition (the 7-round cascade shed findings at each supersession).
4. **Other cfg entry points** beyond `ControllerConfig_Load` + the GUI parser (Binance/backtest/other loaders).

## Disposition (PENDING operator)
- Re-home the 4-field malformed-capture into ③ (rec: yes — founding bug + SAFETY-CRITICAL; homing pre-exists).
- HOW: **R-explicit** (malformed-capture at the 4 sites, mirrors 4b's legacy hand-roll; dollar fields fault, no percent cap) vs **R-structural** (migrate into the registry + single-source the gate + a recurrence guard; needs a dollar-appropriate metadata concept → uint16→uint32 bit-width, which E.1.6 needs anyway).
- The per-node 4b shape itself is CONFIRMED — write it as specified once the global-coverage route is chosen.
→ once decided: log as D-255 (the dropped-homing re-homed) + update the IA-cascade SSoT REMAINING list + annotate TD-215 + (if R-structural) the guard spec.

---

# COMPLETENESS SWEEP (6 agents, 2026-06-22) — interim, agents 1-4 of 6 (5-6 pending)

Fired at operator direction ("1.5-2x, completely sure, solidest foundation"). Each finding verified-grounded. The sweep materially widened the surface beyond the original 4-field gap.

## FINDING 4 (🔴 GUI agent) — the LIVE hot-reload path bypasses the boot gate (Knight-shape)
- The GUI has its OWN `atof`-based parser (`SettingsPanel.hpp:871/:914/:933/:949`) — no `Money_FromString`, no `.flags`, no `cfg_compile_ok` (also violates the GUI's own "never atof" rule). Parses every per-node capital field + fees. `atof("banana")=0.0` → founding bug GUI-side.
- **HEADLINE:** `CoreFrameworks/EngineSharded/Async.hpp:319-335` — live reload does `new_cfg = ControllerConfig_Load(...)` then **`cfg = new_cfg`** with NO `cfg_compile_ok`, NO fault inspection, NO keep-old. Even when the walker SETS the fault flag in `new_cfg`, Async ignores it → recomputes `allocated_balance` from poisoned `risk_pct` → seqlocks to hot path. ③ closes boot (`main.cpp:203`); a running engine swallows a poisoned capital value on every reload. **Gate exists, live re-entry routes around it (H21 Knight shape).** Was PLANNED (synthesis item 5) but uncoded + NOT in the REMAINING-③ caller-wiring list → at-risk-of-dropped.
- Fix (single-source, not a 3rd validator/Class-21): gate `Async.hpp:321` with `cfg_compile_ok(new_cfg)` + keep-old-on-fault. **MUST be in ③ scope** — foundation-critical for E.2 (decoupled runtime re-reads cfg on a control signal). + clamp the GUI per-node `InputFloat` to the descriptor bound (defense-in-depth) + fix stale `clamp_max=100→1000` for TP (B5).

## FINDING 5 (🔴 TD-215 agent) — the "25 dead branches" is 31, and 2 deletion traps
- Actual count: **28** capital `CFG_PARSE_MONEY/_POS/PCT` macro branches + **3** hand-inlined (`fee_floor_mult:2466`, `fee_rate_maker:2302`, `fee_rate_taker:2307`) = **31**. The "25" is coincidentally the delete-safe total but derived from the wrong set.
- Partition: **24 macros DELETE-SAFE** (walker-shadowed) + **4 MIGRATE-FIRST** (the LIVE non-registry: `starting_balance`/`min_sl_tp_ratio`/`min_kill_loss`/`min_hold_gain_pct`) + **`fee_floor_mult` DELETE-SAFE but inline** (TD-215's macro-only wording MISSES it → dead code left) + **`fee_rate_maker/taker` LIVE-KEEP** (`HAS_SIDE_EFFECT` → walker-exempt → set the `*_explicitly_set` flags; a loose "delete inline capital branches" sweep BRICKS them).
- The 4 LIVE fields run the INFERIOR manual macro (no clamp, no D-254 fault-refuse) → active capital-safety hole TODAY. Split TD-215: MIGRATE-FIRST the 4 (before HARD-REFUSE) → then DELETE the 25 shadowed (widen wording to "all walker-shadowed capital parse branches incl. hand-inlined") → EXPLICITLY EXCLUDE `fee_rate_maker/taker`.

## FINDING 6 (census agent) — uncovered-at-HEAD is ~20 (3 idioms), but the genuinely-DROPPED-from-plan set = the 4 flat scalars
- The ~20 uncovered-at-HEAD = the 2 legacy arrays + 11 `_PARSE_OV_PCT` capital overrides (**both already in 4b's scope** — covered-by-plan, just not landed) + the 4 flat scalars (genuinely dropped) + 3 non-capital outliers (correctly excluded). So 4b's per-node coverage is right; the dropped set is the 4.
- **Unit-class:** 2 of the 4 are DOLLAR (`starting_balance`, `min_kill_loss`) — don't fit `CAPITAL_BOUND_LOSS/GAIN` (percent caps) cleanly. **Cascade-free insight:** the malformed-fault capture (`CfgFieldDispatch.hpp:100-112`) keys ONLY on `(CAPITAL_BOUND_*) && (MALFORMED|OVERFLOW)` — it does NOT use unit-class for the FAULT decision → the dollar fields could migrate with the existing `CAPITAL_BOUND_LOSS` bit for malformed-capture WITHOUT a uint16→uint32 widening. (Caveat: the out-of-range 4c sweep would then wrongly percent-cap a dollar field → unit-awareness still needed there; → Agent 6 to resolve.)
- **4c does NOT exist yet** (expected — pending), but `cfg_load_fault_flags` is written at EXACTLY 2 sites (walker `:2169/:2187`); the comment at `ControllerConfig.hpp:1337-1338` describes the post-resolve sweep as if it exists = a doc-lie (premature) → fix when 4c lands.

## FINDING 7 (🟠 dropped-homing agent) — the supersession-drop pattern recurred 3×, not once + a ledger landmine
- The drop is NOT isolated. (1) D-250 4-field homing [known]. (2) **E.2 is an ORPHAN**: `config_error_log` file + the non-capital percent sweep + the D-174c broad-refuse were all prose-homed "→ E.2" in decision-log/addendum but **never written into the E.2 subplan body** (grep-confirmed zero trace). (3) **E.5 half-homed** (submit-time only; no boot-Σ line-item). The E.1.2/E.1.5/E.1.6 folds ARE real (dated blocks in their bodies) — the asymmetry (E.1.x got homings, E.2/E.5 got pointers) is the smoking gun: E.2/E.5 predate the arc (2026-05-28) + were never amended.
- **Methodology refinement (sharper than FINDING 2):** a NAMED obligation is carried ONLY if it lands as executable content in a destination plan body OR a tracked TD with disposition — a pointer in a decision-log STATUS / addendum / handoff is NOT a homing. (Covers both supersession AND forward-defer.)
- **Ledger-split landmine:** `DOCS/TECH_DEBT.md` (symlinked, CLAUDE-documented) is frozen at TD-124; live tracking (125-218) is in the un-symlinked `tick-trader-percore-workspace/DOCS/tech-debt/open.md`. An agent grepping the documented path sees a phantom "these TDs don't exist." Doc-layer SSoT break → own homing.

## FINDING 8 (cross-file agent) — boot-vs-runtime split bounds ③'s scope
- **Boot-time (in ③ scope, `cfg_compile_ok`-gateable):** the 4 dollar/ratio globals + `fee_rate_maker`/`fee_rate_taker` (manual, `HAS_SIDE_EFFECT`-exempt, flag-discarding) + the **entire per-node override path** (`node_N_risk_pct`/`node_N_max_drawdown_pct` + `_PARSE_OV_PCT` cohort). The per-node override path is an **H22 asymmetry**: `stop_loss_pct=banana` refuses boot, but `core_0_stop_loss_pct=banana` silently zeroes node 0's SL — the global is guarded, the per-node override of the SAME field is not.
- **Runtime (NOT ③ — separately homed):** the live REST capital ingest in `DataStream/BinanceOrderAPI.hpp` — `free` balance (live sizing!), `executedQty`, `cummulativeQuoteQty`, lot/notional filters — all silent-0.0 on parse failure. **KNOWN-TEMPORARY → D-123 / `.E.3`** (the string-direct REST rework); needs a RUNTIME refuse/halt guard, not the boot gate. `BinanceCrypto.hpp:749` WS price/qty already captures+warns (fine). Backtest inherits ③ (no separate parser); secrets/CLI/env ingest NO capital. → ③'s boot surface = `ControllerConfig_Load` + the GUI parser + `Async.hpp`; the only other capital ingest is live-REST (`.E.3`).

## FINDING 9 (i-class foundation architecture) — the design-once recommendation
- **Parse-idiom topology:** walker (covered, step-3) + 3 LIVE hand-rolled idioms (legacy arrays idiom-2 [WIP2g-transitional] / override macro idiom-3 / hand-rolled global macros idiom-4 [dead for registry rows = Class-51 trap if you fold there; LIVE for the 4 non-registry globals]).
- **Recommended end-state (R-structural via O4, NOVEL):** the D-254 **post-resolve sweep (4c) IS already the one-validated-path** for out-of-range — extend it to cover idioms 2+4 (auto-flows, H22-faithful, ONE site) instead of O1 migrate-everything-now (blocked on WIP2g) or O2 shared-helper (convention, not structure) or O3 hand-roll-all (R-explicit fallback). Malformed-capture stays parse-point (extended to idioms 2+4; D1 already locked idiom-2 hand-roll-now). Ratio/gate fields (`min_sl_tp_ratio`/`min_hold_gain_pct`) get malformed-capture but NO out-of-range cap (no natural sane-cap → don't invent one = the uniform-cohort anti-pattern).
- **Unified metadata model (bits, not a schema column — honors `.F.4b` lock):** loss%→`CAPITAL_BOUND_LOSS`(14, exists) · gain%→`CAPITAL_BOUND_GAIN`(15, exists) · **dollar→`CAPITAL_BOUND_DOLLAR`(NEW 16, capture-no-cap)** · ratio/mult→NO bit (trap rows) · enum→`ENUM_STRICT`(NEW 17, E.1.6).
- **uint16→uint32 widening = DESIGN-ONCE NOW.** Needs 2 bits now (DOLLAR + ENUM_STRICT); `_reserved` (uint16) was placed FOR this (`:156-157` comment) → absorbs the widening → `sizeof(CfgFieldDescriptor)` likely unchanged (128B) → the **53056 cfg-pin UNAFFECTED**. ~7 type-site edits, all compile-enforced. Serves ③ + E.1.6 + E.2 together; alternative = do-it-twice.
- **Recurrence guard = EXTEND `check_per_node_registry_integrity.py`** (it already parses both sets the diff needs; co-house per the C-2 shared-extraction decision). NOT a `static_assert` (parse-site flag-discard is invisible to the type system); NOT `check_capital_adversarial_audit.py` (that's the TEST-marker guard). Grep-based, per-surface teeth, close-the-class (NEW capital parse-site = RED; existing = KNOWN-PENDING shrinking).
- **Foundation fitness:** ③ owns per-field value-validation; E.5 owns cross-node Σ (cluster-aggregator, D3); E.1.6/E.2 extend via the same bit/sweep pattern; the widening makes them low-touch.
- **4 self-flagged refute targets** (where the design-once thesis is load-bearing): (1) is `_reserved` absorption ACTUALLY size-neutral (verify `-fdump-record-layouts`)? is deferring the widening to E.1.6 lower-risk? (2) does `CAPITAL_BOUND_DOLLAR` earn its slot, or just hand-roll the dollar malformed-capture without a bit? (3) does the guard host generalize to the GLOBAL registry + can grep robustly tell dead-vs-live without re-implementing the walker (vacuous-green risk on the guard itself)? (4) does O4 contradict the D1-locked hand-roll or layer cleanly?

---

# CONSOLIDATED SYNTHESIS (all 6 agents; cross-checked)

**Cross-check (AR-11):** one inter-agent tension — Agent 2 (census) leans DON'T-widen (malformed-fault is unit-agnostic → dollar fields can take the existing bit / stay hand-rolled, the widening is a YAGNI risk) vs Agent 6 (foundation) leans WIDEN-NOW (the out-of-range sweep DOES need to distinguish dollar from percent, + E.1.6 needs a bit anyway). **Factual core (orchestrator code-read):** the widening is size-neutral (✓ `_reserved` adjacency + design-intent comment); 4c is unimplemented (✓ constant never set). **Judgment core (operator's call):** widen-now-design-once vs hand-roll-dollar-now-widen-at-E.1.6 — hinges on migrate-vs-hand-roll the dollar fields. No factual conflict; the tension is a sequencing judgment, surfaced not resolved-by-fiat.

**The converged picture:** the gap is FOUNDATION-shaped, not 4 fields. The real surface = the registry-walker-vs-hand-rolled split, with capital leaking through (a) the 4 non-registry globals, (b) the per-node override path (H22 asymmetry), (c) the `Async.hpp` live-reload bypass, (d) the GUI parallel parser. Plus process gaps: 3× dropped-homing (E.2 orphan, E.5 half-homed), the ledger-split landmine, TD-215 needs splitting (31 not 25).

**Scope boundary (what's ③ vs homed-elsewhere):**
- **③ (boot capital validation):** malformed-capture for idioms 2+3+4 + the 4 globals · the `Async.hpp` live-reload gate · (decision) the structural foundation (widening + DOLLAR bit + 4c sweep + guard) · the GUI per-node clamp + stale `clamp_max` · TD-215 split.
- **Homed elsewhere:** live-REST capital → `.E.3` · cross-node Σ → E.5 cluster-aggregator (D3) · E.1.6 `ENUM_STRICT` (③ widens to leave the slot) · the E.2 orphan homings (config_error_log / non-capital sweep / D-174c) → WRITE into the E.2 body · E.5 boot-Σ line-item.

**Disposition (PENDING operator — the decision):**
- The malformed-capture across the uncovered boot surface + the `Async.hpp` gate = the **urgent correctness** (closes the founding bug end-to-end; non-negotiable for ③).
- The **structural foundation** (registry migration + uint32 widening + DOLLAR bit + 4c out-of-range sweep + recurrence guard) = design-once; worth doing per the foundation goal, but the widening-now decision is foundational/compounding with a live sequencing tension.
- Sequencing options: (1) **foundation-first** — resolve the widening (a targeted A-refute of Agent 6's thesis or operator call) → full R-structural → code once; or (2) **correctness-first** — land malformed-capture + the Async gate now (closes the bug) → structural foundation as a tracked design-once follow-up.

**Capture TODO (once disposed):** D-255 (the dropped homing re-homed + the decision) · IA-cascade SSoT REMAINING update · split + annotate TD-215 · write the E.2 orphan-homing fold-block + the E.5 boot-Σ row · the recurrence-guard spec · the ledger-split landmine (own TD/LANDMINE) · the methodology codification (a NAMED obligation is carried only as executable plan-body/TD content, never a decision-log/handoff pointer — AR/Mn bump) · the Async-gate + GUI-clamp homing.

---

# CONVERGED PLAN (all 7 reframe agents + AR-11 verification) — SUPERSEDES the PATCH-vs-[section] framing above

## Framing correction (3 agents: second-opinion, future-input-space, [section]-architecture)
The PATCH-vs-`[section]` dichotomy was a **false fork** — and the orchestrator's "the spec changes everything" was an over-rotation. The real ③ design = **D-254 (metadata-bit + post-resolve sweep), parser-agnostic, already partly coded, and IS the foundational/HYBRID answer.** The 7-round cascade already refuted "route everything through the walker" (vacuous clamp / sentinel-destruction / infeasible-for-legacy-arrays). `[section]` is a **complementary** operator-UX ship (it closes the never-matched-typo-silently-defaults half the metadata-bit can't) — BUT ③'s already-planned **HARD-REFUSE** (unknown-key → loud fail) covers that safety hole, so `[section]` defers to its own ship / E.2. The **uint32 widening + `CAPITAL_BOUND_DOLLAR` are NOT needed** for the founding-bug close — malformed-capture is **unit-agnostic** (registry-fit + second-opinion + hft converge: the flags are "did it parse as a number," not unit-specific) → defer the widening to E.1.6 (where `ENUM_STRICT` is the real second consumer).

## AR-11 verified (orchestrator code-read)
`cfg_compile_ok` has EXACTLY ONE caller: `main.cpp:203` (engine binary). `foxml_suite.cpp` does NOT call it; `BacktestSharded.hpp:123` ZEROES `cfg_load_fault_flags` + doesn't gate. → **the capital guarantee holds ONLY for the standalone engine binary; the GUI/suite/backtest binary boots + runs with ZERO capital validation.** (Accounting CRITICAL-3 confirmed; registry-fit "zero callers" was wrong.)

## The converged ③ scope (correctness-first — closes founding bug + live exposure + parity)
1. **SINGLE-SOURCE THE ABORT** in `ControllerConfig_Load` (call `cfg_compile_ok`/abort at end of Load) → suite + backtest inherit it for free. [CRITICAL-3 — the structural fix]
2. **MALFORMED-CAPTURE for ALL uncovered capital** — thread `fault_out` through the legacy `CFG_PARSE_MONEY/_POS/PCT` macros + `_PARSE_OV_PCT` + the 2 legacy arrays (covers the 4 globals + per-node overrides). [No widening — boundary-stable]
3. **`>0`/SANE checks for the dollar floors** — `starting_balance>0` (the denominator), `min_kill_loss>0`+sane (live safety floor — a corrupt/huge value makes the drawdown kill NEVER fire). Malformed-capture alone is insufficient. [Accounting CRITICAL-2]
4. **PER-FIELD ≤100% caps** (risk_pct/*_sl_pct/max_drawdown_pct incl. the `node_N_*` overrides) — ③'s job NOW; only the cross-node Σ → E.5. Via an out-of-range check that **EXTENDS `tt::barrier_is_corrupt`** (the shipped SSoT — never re-state the constants; Class-43 risk). [Accounting HIGH-2 + registry-fit MISSED-1]
5. **The Async live-reload gate** (`Async.hpp:321` → `cfg_compile_ok(new_cfg)` + keep-old). [multiple — highest live exposure]
6. **Recurrence guard = compile-time `.rodata` static_asserts** (cohort-count + assert-no-capital-row-is-`MANUAL_PARSER`, where grep is vacuous) + the Python host checks ONLY `cfg_compile_ok` caller-coverage (where grep is non-vacuous). [registry-fit — avoids the Class-51 guard trap]

## Defer (homed, not dropped)
- uint32 widening + `CAPITAL_BOUND_DOLLAR` → E.1.6 (lands with `ENUM_STRICT`, its real consumer).
- `[section]` parser → own ship / E.2 (the HARD-REFUSE covers the unknown-key hole in ③; `[section]`'s extra value is scope-discipline + UX).
- Dead SAFETY_CRITICAL kill fields (`kill_switch_daily_loss_pct`/`kill_switch_drawdown_pct` consumed only by deprecated single_core `PortfolioController` — Class-47 split-brain) → single_core-deprecation cleanup (E.1.3).

## Foundation verdict (the operator's core "won't redesign in 2 plans" question)
**The real design IS foundational.** The metadata-bit rail survives the future input space (future-input-space audit: E.1.6 `ENUM_STRICT` = named 1-bit extension; E.2 `LivesInStruct` already seeded; E.5 Σ → cluster-aggregator; .E.3 → runtime guard) — PROVIDED the founding-bug close is done STRUCTURALLY (single-source abort + thread `fault_out`/migrate), NOT hand-rolled-then-deleted. The `[section]` parser + the widening are real future work, correctly deferred to where their consumers live.

## ParseFast (operator's fold-in)
No "better thing"/future spec exists — `ParseFast.hpp` is the custom solution (locale-immune `from_chars` wrapper, good as-is). The gap = the wrappers discard the `.ec` `from_chars` already computes. Fold-in = add a `*_checked` variant + switch ONLY capital/price/balance-sensitive sites (NOT the 58 blanket); FoxLIB promotion = separate (`/foxlib-promotion`).
