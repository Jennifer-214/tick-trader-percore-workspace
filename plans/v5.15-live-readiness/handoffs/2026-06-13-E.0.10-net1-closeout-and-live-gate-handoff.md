---
type: handoff
status: superseded   # 2026-06-14 → handoffs/2026-06-14-E.0.10-net1-batchA-done-batchB-remaining-handoff.md (F-059 + F-090/093 + PERSIST LANDED+committed+green; wfa-1/F-046/F-018 + the live-gate carried forward)
ship_tag: .E.0.10
plan_type: live-readiness
sprint_end_goal: make the codebase more maintainable for future development (v5.X professionalization); live-readiness gated behind the whole E-series
ship_end_goal: CLOSE the .E.0.10 Net-1 characterization net — F-059 golden-master exit (KEYSTONE) + the remaining characterization tests + the structural closes + the live-enable HARD gate. The 4 durable-now capital fixes (A17/A21/A20/NEW-1) are DONE.
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-13-E.0.10-phase4-durable-now-capital-fixes-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
required_reading: [the register (finding dispositions — SSoT), decision-log D-215/D-216/D-217, CoreFrameworks/LiveReadiness.hpp (the 8 REFUSE checks — NO torn-read gate = the live-gate finding), CoreFrameworks/ExecutionCore.hpp:543 (the F-059 target), characterization-test-discipline.md, the .E.1 foundation plan (the A20/A21/NEW-1 folds), DOCS/STRATEGY_AND_CODING_RULES.md, DOCS/LANDMINES.md (Landmine 10), CODE_MAP (regen at pickup)]
engine_head: 8878155 (NEW-1 single-authority predicate + the D-218 conflict HARD-REFUSE reconciliation; the 4 fixes committed + PUSHED)
workspace_head: d21bc1c (NEW-1 char-tests [non-vacuous] + Class 47 + design-spec + D-217/D-218 + .E.1 fold + the comprehensive capture pass)
coding_status: Phase-4 DONE (4 capital fixes); the Net-1 characterization net + structural closes + the live-gate REMAIN
defers: conc-5/torn-read → .E.1 aggregator; F-044/045 maker → .E.5/.6; F-096 fix → .E.1; .E.M exit-redesign; D10 paper-test (runtime)
deletion_scope: none
---

# Handoff — `.E.0.10` Net-1 close-out (the 4 capital fixes are DONE; the characterization net + the live-gate remain)

> **▶ POST-WRITE UPDATE (2026-06-13, after this handoff was written):** the operator's "are you sure we captured everything?" caught a **code-vs-decision DRIFT** — NEW-1's conflict-handling shipped as explicit-wins, but D-217/ADV-2 decided **hard-refuse**. RECONCILED (**D-218**): `ControllerConfig_Load` sets `live_capital_cfg_conflict` → `main.cpp` REFUSES boot on a `use_real_money`/`trading_mode` conflict; the char-test rewritten **NON-VACUOUS** (pins the flag, not the safe-outcome proxy that hid it); build **3419/0**. Root-cause = an **effort-driven silent deviation (NOT compaction)** + a vacuous test + the mechanical audit being structural-not-semantic → codified `feedback_implement_reconcile_decision_code_binding` (the decision↔code contract; M7). The comprehensive capture pass also landed: **FEATURE_LOOKUP** (the trading_mode migration) + the register **Phase-4 close-out** (the 4 fixes DONE + the 2-engine-findings re-disposition + the live-gate-VERIFIED-MISSING). The 5/6 other NEW-1 items were verified vs code — **no other drift**. **Engine HEAD advanced past `f4049c3`** by the D-218 reconciliation commit (git log). Pickup next-action unchanged: **F-059**.

## TL;DR (the 30-second pickup)

- **Phase 4 (the 4 durable-now capital fixes) is DONE + committed + captured.** A17 (`6c2910b`) · A21 (`6a07e06`) · A20 (`47bd889`) · NEW-1 (`f4049c3` engine / `f7f962f` workspace). Each got a full 4-agent fan-out → fix → anti-vacuous char-test → 5-way verify → `.E.1` fold → captures. Suite **3416/0** + asan/ubsan **3246/0** + GUI ok.
- **But `.E.0.10` is NOT done.** It is the **Net-1 characterization net** — freeze current behavior in tests so `.E.1` can refactor the OMS/exit/reconcile paths safely. The 4 fixes were durable-now finds *during* the net; the net's keystone — **F-059 (the exit-chain golden-master)** — + the remaining characterization tests + the structural closes are STILL OPEN. **F-059 is the next-action.**
- **🔴 CRITICAL finding this session — the live-enable HARD gate is MISSING.** `LiveReadiness_Verify` has 8 REFUSE checks (secret / mlockall / cores-strategy / ml-models / model-age / feature-hash / label-hash / stamps-hmac) but **NONE blocks on the torn-read class / conc-5**. So a sharded live boot (`trading_mode=live` + valid secrets/models) RUNS with the Knight-shaped reconciler-corrects-on-a-torn-read race live. The register's #1 close-out item is unimplemented → Phase D, needs a decision-log + a boot-refuse.

## What landed (Phase 4 — committed)

| Fix | What it closed | Commit | Captures |
|---|---|---|---|
| **A17** | REST `order_complete` DERIVED from venue `status` (not hardcoded =1; D-106 let-the-venue-decide) — RBP Class-46 false-positive surface | `6c2910b` | D-215 |
| **A21** | Reconciler cash-leg `Money`/H4 (`balance − Σ_open − Σ_inflight`) + `ProcessReconcile` made **detect-only** (deleted the balance clobber + the `ks_peak` ratchet) | `6a07e06` | D-216, `.E.1` fold |
| **A20** | Boot **seed-don't-replay** (`Reconcile_SeedWatermark`) — kills the AUTO_SYNC double-book; reachability resolved live-now-HIGH (the Phase-2 comment was stale); **persist REVERSED to `.E.1`** (the fan-out found paper never consumes the watermark → vacuous) | `47bd889` | D-216, `.E.1` fold |
| **NEW-1** | **Split-brain collapse** — `ControllerConfig_IsLiveCapital := trading_mode==LIVE` is the sole capital authority; `use_real_money` → parse-alias + tombstone (H21); **legacy single_core LIVE hard-refused** (operator's Knight ruling); 6 co-hardening (HotReload-protect, GUI checkbox removed, tooltip-inversion fix, SHADOW tombstone, conflict-refuse, explicit-bit) | `f4049c3` | D-217, **RBP Class 47**, DESIGN_SPEC, `.E.1` fold |

## The close-out sequence (what's left, IN ORDER)

### Phase A0 — verify it went to plan + the owed captures (start here at pickup)
- **Flip the register dispositions:** the 4 fixes → DONE; the **2 engine findings** — `oms-ts-2` (per-core vs OMS P&L rounding-divergence, 25% diverge by 1 ULP) → *likely* CLOSED-by-D-190 (the gross SSoT) — CONFIRM by code-read + flip; `tsa-live-2` (boot-reconcile same-core fill collapse) → cold-boot-DEFUSED-by-A20 (no replay), the warm-tail collapse homed `.E.1` — flip with that note.
- **🔴 The live-gate finding → register** (OPEN-CRITICAL, Phase D) + a **decision-log entry** (the gate is missing; what to block on).
- **Owed ledgers:** **F-096** (partial-exit per-leg ENTRY qty split in `double` = H4 violation on the sizing path + leg-A+leg-B doesn't conserve `intended_qty`) → the register OWES a **PARITY/TECH_DEBT entry** (the *fix* rides `.E.1`; the *ledger* is owed NOW). **NEW-1 FEATURE_LOOKUP** entry (the `use_real_money`→`trading_mode` operator migration + the legacy-refuse boot gate — the auto-write contract owes it). The **5 unmarked capital-test ADV-markers** (the doc-CI advisory backlog — add `// ADV-REFUTE`/`// ADV-SELF`).

### Phase B — the characterization net (the ACTUAL `.E.0.10` deliverable)
- **F-059** — golden-master exit (KEYSTONE). **Target: the `ExecutionCore.hpp:543` citation is STALE — D-198 RE-GROUNDED it** to `ControllerEventLoop.hpp:1873-1890` + `Portfolio.hpp:201-203` + `Momentum.hpp:280-282` (the live `live_tp`/exit computation). Use those + re-verify at pickup. Template `oms-ts-1`; freezes the per-fill TP/SL exit path before `.E.1` rewrites it (byte-identity false-by-construction without it — `hpg-bc-1`).
- `oms-ts-1`, `wfa-1` (remaining HIGH targets).
- `F-090`/`F-093` (parser FULL output — price/qty/commission, not just the flags), `F-046` (`oms-ts-4`: FlattenAll balance + partials-on path), `F-018` (`oms-ts-5`: partial-exit money path — money ONLY; the slot-MAPPING is `.E.1` changes-by-design, do NOT freeze it). Per the register's 2026-06-11 false-floor re-triage.
- PERSIST characterization subset (PENDING per the register).
- **DISCIPLINE (load-bearing):** each characterization test earns the **3-lens adversarial pass** (complete / non-vacuous / not-frozen-bug) BEFORE "done" — `oms-ts-2`/`tsa-live-2` proved a green test can freeze a FALSE invariant (held only on rounding-clean inputs). `characterization-test-discipline.md` + `feedback_passing_test_is_not_verification`.

### Phase C — structural closes
- **cfg-flag-orphan cohort (#9)** — wire-or-tombstone A36 + A13/A14/A35/A37 (the Class-44 cfg-flag sub-variant; the `check_per_core_registry_integrity.py` Check-10 extension is its CI slice).
- **Class-44/46 detector CI guard (#10)** — the structural close (guards compound); the write-with-no-live-read / zero-caller-setter / completeness-gated detector.
- **A6 (#7)** — chokepoint gate-emit tp/sl-validity guard.

### Phase D — pre-`.E.1` gates + close
- **🔴 The live-enable HARD gate** — decision-log + a `LiveReadiness` REFUSE check (or a boot-refuse) that blocks live until the torn-read class/conc-5 `.E.1` fix. **Decision needed:** block on the torn-read class specifically, or a blanket "live blocked pending `.E.1`."
- **guard-matrix-no-HOLE** check (pre-`.E.1` gate per the `.E.0.5` DoD — every invariant `.E.1` touches is an enforced row).
- `.E.0.10` ship close: version bump + tag + postmortem + push.

## STAYS HOMED — do NOT pull forward (merit, NOT effort — closing now = build-twice, the over-accretion failure mode per D-216)

- **conc-5 / the 9-site torn-read class → `.E.1` aggregator** (single-writer + seqlock-published money snapshot). This is the live-gate's dependency.
- F-044/F-045 (maker-path fee) → `.E.5`/`.E.6`. **F-096** (partial-exit H4-double FIX) → `.E.1` partial-exit rework (the *ledger* is owed now — Phase A0). A16 real partial-fill handler + A22 + A29 + A34 → `.E.1`. `.E.M` exit trailing-profit redesign (#8, D-206). D10 paper-test (#11 — needs a runtime session; operator call).

## Arming for the next action (M8 — the immediate next is F-059)
- **Route to:** `/accounting-audit` (the exit-chain `Money`/P&L path F-059 freezes) + `/hft-audit` (the `SG_Evaluate`/`ExecutionCore` exit trigger) + `/bug-check` (Class 43/45/46 — the SSoT/completeness family the characterization must NOT freeze-as-bug). ARM each per M8.
- **Run (not just read):** `./build.sh test` · `tools/run_all_tests.sh --full` (asan/ubsan) · `tools/calls_graph_diff` · regen `./tools/gen_code_map.sh`.
- **Read first:** the register (dispositions — SSoT) + `characterization-test-discipline.md` (the 3-lens pass) + `ExecutionCore.hpp:543` (F-059 target) + `DOCS/STRATEGY_AND_CODING_RULES.md`. H-invariants: **H4** (Money), **H8** (latency), **H22** (purity), **H21** (Knight — the live-gate).
- **LANDMINE 10:** `tests/` is a workspace symlink — `git add tests/` from the engine fails ("beyond a symbolic link"); stage the WORKSPACE path. Edit `plans/`, `DESIGN_SPECS/`, `DOCS/recurring-bug-patterns/`, `RECURRING_BUG_PATTERNS.md`, `CLAUDE.md` at the WORKSPACE path (they're symlinks; the harness is unreliable through them).

## Git state at handoff
- Engine: **`8878155`** (`feat/v5.15-live-readiness`) — the 4 fixes + the **D-218 conflict HARD-REFUSE** reconciliation, committed + **PUSHED** to origin.
- Workspace: **`d21bc1c`** (`main`) — char-tests (non-vacuous) + Class 47 + design-spec + D-217/D-218 + `.E.1` fold + the comprehensive capture pass, committed + **PUSHED**.
- The cfgs (`engine.cfg`/`engine_sharded.cfg`/`backtest.cfg`) — gitignored; locally migrated `use_real_money`→`trading_mode`.
- **All pushed to origin** (engine `8878155`, workspace `d21bc1c`). `/accept-handoff`'s forward-drift logic walks any newer commits.
