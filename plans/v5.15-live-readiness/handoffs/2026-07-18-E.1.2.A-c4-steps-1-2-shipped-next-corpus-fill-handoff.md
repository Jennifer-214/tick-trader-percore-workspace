---
type: handoff
status: superseded   # 2026-07-18 → handoffs/2026-07-18-E.1.2.A-conversion-complete-hard-zero-next-plugin-handoff.md (the corpus fill DONE + RegimeDetector converted + HARD-zero flipped; corpus 100% green). Was: THE one live handoff.
ship_tag: E.1.2.A
plan_type: refactor   # in-code documentation toolchain — maintainability deliverable, dev-plane; engine H1-H22 do NOT govern (tools/CLAUDE.md)
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: the [SCHEMA]_[v1.0] tag system + the north-star toolchain (fact-producers → plugin → custom-IDE); C4 DERIVED-write is the current increment
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-07-18-E.1.2.A-phaseC-sweep-complete-next-northstar-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md, DESIGN_SPECS/framework-patterns/doc-intelligence-toolchain-architecture.md, DESIGN_SPECS/framework-patterns/isolated-per-struct-layout-probe.md, DESIGN_SPECS/meta-disciplines/calibration-corpus-non-vacuity-discipline.md, DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md]
deletion_scope: none   # comments-only + lossless everywhere
coding_status: C4-step1+step2-SHIPPED   # refresh_derived present-or-insert (4b75a2f) + --isolate mode (9c88436). NEXT = step-3/4 corpus-wide fill (Task #4).
---

# E.1.2.A — C4 DERIVED-write steps 1+2 SHIPPED; next = the corpus-wide fill (step-3/4)

**Created:** 2026-07-18
**Target:** Task #4 — run `check_cache_layout.py --isolate --fix` corpus-wide to fill the ~243 empty `[DERIVED]` blocks, then gate the blast radius (triage new cross-thread straddles as H6 findings).
**Branch:** feat/v5.15-live-readiness
**Plan:** `plans/v5.15-live-readiness/subplans/2026-07-05-E.1.2.A-comment-system-and-doc-consolidation.md`
**Decision log:** `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-365..D-367)
**MASTER:** `plans/v5.15-live-readiness/MASTER.md`

## ⚠️ State at handoff write (VERIFIED via two are-you-sure loops)

- **Engine HEAD `c36e96b` — UNCHANGED all session (zero engine-code commits by this session).**
- **Engine working tree = TWO off-limits WIP files, BOTH Caramel's — DO NOT touch / git-add / revert / commit:**
  - `M Strategies/RegimeDetector.hpp` — big WIP (127-line churn); its `:543 [STATE]` / `:627 [INIT]` are the accepted doc-CI reds (`reds ⊆ allowlist`; clears when Caramel converts it → Task #6).
  - `M CoreFrameworks/ExecutionCore.hpp` — **NEW this session** (mtime 15:18): a cache-line padding tweak (`-_pad_line0[8]`, "Finding A"), Caramel's parallel register-fit/cache-line work. NOT this session's doing (verified: zero engine Edits, no `--fix` on real files, no engine commits).
  - Untracked scratch (NOT ours; leave): `build_clangd/`, `resume`, `tmpo0ujx6rw/`.
- **Workspace HEAD `9c88436`** (7 commits this session; all pushed via the close-session sync). Untracked `note` (not ours; leave).

## What landed this session — INDEPENDENT REVIEW ALL-CLEAR (19/19)

**Two build steps of the C4 DERIVED-write increment SHIPPED** (D-363):
- **Step-1 (`4b75a2f`)** — `check_cache_layout.py refresh_derived` now **present-or-inserts**: it INSERTS `[SIZE]/[ALIGN]/[CACHE_LINES]/[STRADDLE]` into an EMPTY `[DERIVED]` (the reason `--fix` filled 0), not just rewrites. IDEMPOTENT (2nd `--fix` = 0 changes — guards the FPN non-idempotent-apply class). Ships a 5-assertion calibration selftest (REWRITE/INSERT/IDEMPOTENT/COVERAGE-BOUND/ROUND-TRIP).
- **Step-2 (`9c88436`)** — the `--isolate` mode: `isolate_layouts()` groups converted `[STRUCT]`s by header, generates a per-header isolated TU (prelude `<cmath>`/`<cstdint>`/`<cstddef>` for RC-B std-dep hygiene + the header + `using namespace tt;` + a `sizeof`-forcer per struct; templates at `<64>` via `_struct_is_template`), runs `run_emitter` (one producer, D-337), merges the layouts. **The insight (Caramel's):** per-struct isolation beats the monolithic all-headers probe — seconds not 2min, no RC-B include-order fragility, `using namespace tt;` resolves global AND `tt::` uniformly (no per-struct ns parsing), 1:1 with the binary by ABI determinism (D-321). PROVEN across global (`HistoricalTick` 32B) / `tt::` (`LabelDef` 48B, `PhaseTimer` 80B) / template (`RollingStats<64>` 8640B, `RORRegressor<64>` 272B) / multi-instantiation. All 12 `--selftest` assertions green.

**3 design decisions captured + committed** (each triple-homed: decision log + spec + plan):
- **D-365 (`63b316e`)** — grammar/vocab 1:1-propagation: adding vocab is a ONE-site fence fold; `foxtag grammar` is the SSoT-emitter seam; the **nvim plugin is the drift watch-point** (`tag_grammar_adapter.lua` native-Lua mirror, zero foxtag calls). Home: `doc-intelligence-toolchain-architecture.md § grammar-propagation`. Tasks #8/#9.
- **D-366 (`94776c6`)** — the **register-fit / access-cost per-field axis (RC-F)**, grounded in `Learning_cpp/deep_dives` (de Bruijn single-`imul`; `(~A&B)==B`; the "40→1 mov"). Per-field size/offset/cache-line + single-mov-iff `size∈{1,2,4,8,16,32,64} ∧ offset mod size==0` else shift+mask; the tool shows BOTH costs (H14 bit-pack tension), operator decides. Home: north-star §5 RC-F. Task #11.
- **D-367 (`b7b1c7c`)** — **plane first-class discipline-gating axis + the toolchain self-hosts.** `[ENGINE]`/`[DEV_PLANE]`/… become a first-class GATING axis (path-derived value; a `FOREACH_PLANE` registry of `{constraints · valid [TAG] vocab · [DERIVED] axis-set}`); universal skeleton + plane-scoped vocab (toolchain DERIVED = pipeline-reality, a different KIND than engine compiled-reality); a multi-comment-syntax parser (`//`/`#`/`--`) so the toolchain self-hosts. RATIONALE: the toolchain is one-producer-N-consumers → almost MORE critical than the engine → engine-grade rigor. Home: `doc-intelligence-toolchain-architecture.md § plane-first-class` + north-star §8.5. Tasks #12/#13/#14.

**Codifications:** Class 56 (non-idempotent in-place writer) + Landmine 12 (harness clobbers memory frontmatter on malformed YAML — the `[]` defect swept 2/2) + `calibration-corpus § writer tools` (every writer selftest asserts idempotency + round-trip). **Memories:** the process-weight carve-out (a fact-producer/verifier toolchain is correctness-critical despite the apparatus plane), NEW `user_owns_core_design_meet_confidence_genuinely`, `feedback_address_user_as_caramel` (+ no honorifics/"boss").

## NEXT ACTION (armed) — Task #4: the corpus-wide fill

Run `check_cache_layout.py --isolate --fix` corpus-wide → `isolate_layouts` materializes every `[STRUCT]` → `refresh_derived` fills the ~243 empty `[DERIVED]` blocks (comments-only, lossless, idempotent — all proven in step-1/2). **Do it CHUNKED (per directory), gated, verified — NOT all-at-once** (Caramel: the toolchain is correctness-critical; don't rush).

- **Files:** `tools/check_cache_layout.py` (the `--isolate --fix` path is built; read `isolate_layouts` + `refresh_derived`). The recipe + edge-cases are in **Task #4's description** (the TaskList) — read it.
- **Then gate the blast radius:** re-run the cache-layout HARD gate after the fill — a NEW cross-thread straddle on a `[THREAD]`-declared struct = a GENUINE **H6 false-sharing finding to TRIAGE** (never auto-suppress); a benign single-thread straddle = fine.
- **Edge-cases surfaced in step-2 (handle here):** (a) multi-instantiation templates — `RollingStats` materialized at `<64>`/`<64,256>`/`<64,512>`/`<64,1024>`; `match_layout` picks the FIRST by bare name → decide which fills the `[STRUCT]_[RollingStats]` block (primary `F=64,W=default`, or `[INSTANTIATION]`-key each); (b) per-header prelude edge-cases (a header needing more std includes than the common prelude — RC-B; extend as they surface); (c) templates whose args can't default → `[INSTANTIATION]` tag or a documented C4-remainder.
- **Discipline:** lossless (comment-stripped diff == HEAD) + validator + `./build.sh test` baseline + doc-CI green (the accepted RegimeDetector red only) + commit per chunk. Then the `foxtag update` cutover behind `parity_check.sh` (D-349, later).

## HARD rules (do NOT trip)
1. **NEVER touch / git-add / revert `Strategies/RegimeDetector.hpp` OR `CoreFrameworks/ExecutionCore.hpp`** — BOTH are Caramel's uncommitted WIP.
2. **Code bytes NEVER change** — comments-only, lossless-gated. The `--fix` writer touches ONLY `[DERIVED]` comment tags, never fields (H21/H12).
3. **MARK, never delete** — `[DEPRECATED]`/`[MARKED_FOR_DELETION]`, never a code deletion.
4. **Function-local structs are tool-EXEMPT** (D-364); the completeness gate handles them.

## The task pipeline (TaskList — recreate via /accept-handoff Stage 7)
- **#4 (NEXT)** — C4 step-3/4: the corpus-wide `--isolate --fix` + blast-radius gating.
- **#6** — HARD-zero completeness flip + always-loaded rule (BLOCKED on Caramel converting RegimeDetector).
- **#5 / #9** — toolchain-spec remainder + bidirectional sister sweep / add-vocab one-action.
- **#10 / #11** — plugin cache-line-density placeholder bug (Caramel's session) / register-fit RC-F axis.
- **#12 / #13 / #14** — `FOREACH_PLANE` registry / multi-comment-syntax parser / toolchain self-hosting (D-367; north-star V1).
- **#7 / #8** — foxtag update cutover + plugin V1 (Caramel's) / plugin grammar 1:1 parity gate.

## Arming for the next action (route the receiver — surface, don't auto-fire)
- **Skills:** none heavy — dev-plane mechanical tool work; the **DoD is the tool's `--selftest` + the lossless comment-stripped diff + doc-CI green**. A design DECISION (e.g. the multi-instantiation-template pick) → `/decision-check`.
- **Tools to RUN:** `python3 tools/check_cache_layout.py --selftest` (non-vacuity) → `check_cache_layout.py --isolate --fix <chunk>` → `tools/check_session_docs.sh` (expect the accepted RegimeDetector red) → the lossless proof.
- **Specs:** `in-code-documentation-schema.md § [DERIVED] axes` + `§ WRITTEN-vs-LIVE-PREVIEW (D-327)` (write ONLY the layout quartet, never the codegen axes) · `mechanical-verification-of-derived-code-facts.md` · `calibration-corpus-non-vacuity-discipline.md § writer tools`.
- **Anti-patterns:** Class 56 (non-idempotent writer — proven idempotent), Class 51 (vacuously-green — the selftest guards it). **Invariants:** engine H1-H22 do NOT govern (dev-plane); the gate ENFORCES H6 (straddle) / H12 (padding) / H21 (never reorder a serialized struct).

## Capture-completeness (this session — nothing lost; independent review ALL-CLEAR 19/19)
- **Decisions → LOGGED:** D-365 (grammar/vocab 1:1-propagation) · D-366 (register-fit RC-F) · D-367 (plane first-class + toolchain self-hosting) — all in the decision log with paired `<!-- D/C/F -->`+`<!-- STATUS -->` sentinels, each TRIPLE-homed (log + spec + plan), independently verified.
- **Findings → homed/ledgered:** register-fit axis (RC-F → Task #11) · plugin cache-line-density bug (RC-D′ → Task #10) · the accepted-red-allowlist structural terminus (TD-166) · the DESIGN_SPECS-index-drift-on-edit shape (recurred 3× this session → reinforces the TD-166 auto-regen fix). Class 56 (non-idempotent in-place writer) + Landmine 12 (harness memory-frontmatter clobber; `[]` defect swept 2/2) codified.
- **Memories/skills → indexed:** the process-weight carve-out (a fact-producer/verifier toolchain is correctness-critical despite the apparatus plane) · NEW `user_owns_core_design_meet_confidence_genuinely` (indexed in MEMORY.md) · `feedback_address_user_as_caramel` (+ no honorifics/"boss"). No new skills. `calibration-corpus § writer tools` + `doc-intelligence-toolchain-architecture § grammar-propagation + § plane-first-class` + north-star §5/§8.5 landed.
- **TaskList snapshot** (recreate at pickup via /accept-handoff Stage 7):

| # | Task | Status |
|---|------|--------|
| 1 | Phase-C actionable [SCHEMA] conversion sweep | completed |
| 2 | C4 step-1: refresh_derived present-or-insert + selftest | completed |
| 3 | C4 step-2: --isolate mode (per-header sizeof-forcing) | completed |
| 4 | C4 step-3/4: corpus-wide --isolate --fix + blast-radius gate | **pending — NEXT** |
| 5 | Toolchain-spec remainder + bidirectional sister sweep | pending |
| 6 | HARD-zero completeness flip + going-forward rule | pending (blocked on RegimeDetector) |
| 7 | foxtag update cutover + plugin V1 UX (Caramel's) | pending |
| 8 | Plugin grammar 1:1 + surface-parity gate (D-365) | pending |
| 9 | Add-vocab one-action + surface-parity CI (D-365) | pending |
| 10 | Plugin cache-line-density placeholder bug (Caramel's) | pending |
| 11 | Register-fit/access-cost per-field axis (RC-F, D-366) | pending |
| 12 | FOREACH_PLANE registry (D-367) | pending |
| 13 | Multi-comment-syntax parser (D-367) | pending |
| 14 | Toolchain self-hosting conversion (D-367) | pending |

## Paste into a fresh Claude Code session
```
I'm picking up E.1.2.A — C4 DERIVED-write steps 1+2 are SHIPPED (refresh_derived present-or-insert 4b75a2f + the --isolate mode 9c88436, all proven). NEXT = Task #4: the corpus-wide fill.

Run: /accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-18-E.1.2.A-c4-steps-1-2-shipped-next-corpus-fill-handoff.md

NEXT ACTION: run check_cache_layout.py --isolate --fix corpus-wide (CHUNKED per directory, gated, verified — NOT all at once) to fill the ~243 empty [DERIVED] blocks, then re-run the cache-gate and TRIAGE any new cross-thread straddle as a genuine H6 finding. Handle the edge-cases in Task #4's description (multi-instantiation-template fill pick, per-header prelude additions).

HARD rules: NEVER touch/git-add Strategies/RegimeDetector.hpp OR CoreFrameworks/ExecutionCore.hpp — BOTH are Caramel's WIP. Comments-only, lossless. MARK never delete. Do NOT trust prior-session memory — verify against current code.
```

## Quick links
- The tool (`--isolate --fix` built): `tools/check_cache_layout.py`
- North-star (the vision): `DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md` (§5 RC-A..F · §8.5 planes/self-hosting)
- Toolchain architecture: `DESIGN_SPECS/framework-patterns/doc-intelligence-toolchain-architecture.md` (§ grammar-propagation · § plane-first-class)
- Schema SSoT: `DESIGN_SPECS/doc-disciplines/in-code-documentation-schema.md` · Plan: `plans/v5.15-live-readiness/subplans/2026-07-05-E.1.2.A-comment-system-and-doc-consolidation.md`
