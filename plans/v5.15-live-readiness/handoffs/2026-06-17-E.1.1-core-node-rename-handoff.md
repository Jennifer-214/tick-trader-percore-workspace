---
type: handoff
status: active   # the one live handoff (no-arg /accept-handoff resolves here). Supersedes 2026-06-16-E.1.0-conformance-analyzer-FINISHED (its only remaining step — the tag — is DONE: .E.1.0 SHIPPED).
ship_tag: "E.1.1 — Core→Node mechanical rename + cfg-key CLEAN-BREAK + single_core LIVE deletion. SCAFFOLD/RED-pre-gate. NEXT = C-class re-ground (RE-DERIVE the stale grep counts vs HEAD) → /precoding-audit-gate (deletion-class) → consult → code."
plan_type: refactor
deletion_scope: major   # single_core LIVE removal (16 files) + cfg-key HARD-REFUSE clean-break — deletion-class spanning ≥3 files w/ compile-time interdeps → /accept-handoff Stage 4 auto-fires /blindspot-scan B14/B15
sprint_end_goal: make the codebase maintainable + live-ready (v5.X professionalization); HARD live-enable gated behind the whole E-series (specifically E.1.3 ∧ E.1.5)
ship_end_goal: "A fully-renamed Core→Node tree on FROZEN layout (no Core*/MAX_EXECUTION_CORES/CoreContext/state.cores/core_id/per_core_/FOREACH_PER_CORE_CFG_FIELD residue), the cfg parser HARD-REFUSING legacy core_* keys (clean-break, NOT alias — closes the Knight silent-revert-to-default class), single_core LIVE gone, controller_test green + byte-behavior-neutral (determinism golden byte-IDENTICAL to E.1.0), and the rename-cascade tool built for E.1.2 to reuse."
predecessor_handoff: handoffs/2026-06-16-E.1.0-conformance-analyzer-FINISHED-next-shipclose-codifications-handoff.md (SUPERSEDED — .E.1.0 SHIPPED)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (latest D-238; the E.1.1-governing decisions are D-27 / D-61 / D-219 / D-223 / D-131 / H21)
plan_body: plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md (a SCAFFOLD — its "Design-shape" grep COUNTS are grounded against the OLD HEAD 572c978 + are EXPLICITLY stale; the C-class re-ground RE-DERIVES them at dive)
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
dependency_graph: plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md
guard_matrix: plans/v5.15-live-readiness/E-guard-coverage-matrix.md
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, the E.1.1 plan body (the SCAFFOLD), decision log D-27/D-61/D-219/D-223, DOCS/SUBAGENT_ARMING.md (M8 arming SSoT), the .E dependency-graph DAG, the E-guard-coverage-matrix, regen CODE_MAP at pickup]
engine_head: 90b2daf (the .E.1.0 tag commit — Version.hpp bump only; GPG-signed; PUSHED to origin/feat/v5.15-live-readiness). Tag v5.15.5.F.4d.1.E.1.0 pushed. Working tree CLEAN. Predecessor baseline = .E.1.0 tag.
workspace_head: at-or-after the /sync-workspace at this session's close (this session: MASTER banner + TOOLS.md:54 currency-fix + the old-handoff supersede + this new handoff)
coding_status: planning-complete-for-predecessor / E.1.1 is RED-pre-gate — NO E.1.1 code yet; the dive's first action is the re-ground + gate, NOT coding
---

# Handoff — `.E.1.0` SHIPPED → next leaf = `E.1.1` Core→Node rename + cfg clean-break

## TL;DR (the 60-second pickup)

- **`.E.1.0` SHIPPED** ✅ — tag `v5.15.5.F.4d.1.E.1.0` (commit `90b2daf`, GPG-signed, pushed). The H8 STATIC latency-path conformance analyzer (`tools/check_latency_path_conformance.py`) + the §4d guard-NET; zero engine code. It is now a **STANDING gate** (pre-commit Check N + `run_all_tests.sh`, both HARD) — **E.1.1's rename must keep it green** (a names-only edit cannot regress instruction-count/branch/H4/div/malloc without RED-building).
- **NEXT = E.1.1** — the mechanical `Core → Node` identifier substitution on the FROZEN hot path (names only — bytes + logic unchanged), PLUS the cfg-key **CLEAN-BREAK** (`core_N_*` → `node_N_*` keys + parser HARD-REFUSES unrecognized legacy keys — loud fail, NOT a tombstone/alias; D-219/D-223), PLUS the `single_core` LIVE **deletion** (D-61). Risk MED: massive mechanical surface, but pure identifier substitution on FROZEN logic UNDER E.1.0's net; the cfg clean-break is the only behavior change.
- **E.1.1 is a SCAFFOLD, RED/pre-gate.** Its plan-body "Design-shape" grep counts (351 `core_id`, 217 `per_core_*`, 22 `state.cores` files, 16 `single_core` files, the 8 cfg-parser branches) are grounded against the **OLD HEAD `572c978`** and are **EXPLICITLY stale** — the plan itself says "RE-DERIVE at dive."
- **Your first action is NOT coding.** It is: **C-class re-ground** (re-derive every grep count + verify the cited file:line anchors vs current HEAD `90b2daf`) → **`/precoding-audit-gate`** (deletion-class: `/blindspot-scan` B14 multi-surface-deletion-ordering + B15) → **consult Caramel** → then code. (This is why `/readiness` was NOT separately fired at handoff-write: the mandated re-ground IS the currency/readiness pass, and the plan already declares its counts stale — firing it now would only re-confirm "stale, re-derive at dive.")

## The ideal way to start

1. **`/accept-handoff`** (resolves here). It regens CODE_MAP + loads the baseline + nav-infra + the E.1.1 scaffold + the DAG + the guard-matrix + `SUBAGENT_ARMING.md`, and — because `deletion_scope: major` — it auto-fires `/blindspot-scan` B14/B15 at the pre-coding gate.
2. **Confirm the predecessor landed clean:** `git tag --verify v5.15.5.F.4d.1.E.1.0` (signed) · `python3 tools/check_latency_path_conformance.py && --selftest` (the standing gate is green BEFORE you start renaming, so any RED after is YOUR edit) · `cat Version.hpp | grep ENGINE_VERSION_STRING` (== `.E.1.0`).
3. **C-class re-ground** (the FIRST substantive step — the scaffold demands it): RE-DERIVE every grep count + file list in the plan-body "Design-shape" against current HEAD; resolve the open `MAX_EXECUTION_NODES` vs `MAX_NODES_PER_CLUSTER` naming question (lean: keep `MAX_EXECUTION_NODES` for the deployment-wide cap so it doesn't collide with the per-cluster count E.1.2 adds); confirm the cfg-parser line anchors (`ControllerConfig.hpp:2878-2968`, the 8 `strncmp(key,"core_",5)` branches) still hold.
4. **`/precoding-audit-gate`** (deletion-class) → **consult Caramel** (consult-before-coding) → THEN the rename.

## TaskList state at handoff write (preserve verbatim for fresh-session pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | completed | TAG .E.1.0 (conformance analyzer ship-close) — DONE: v5.15.5.F.4d.1.E.1.0 |
| #2 | **in_progress (NEXT)** | E.1.1 — Core→Node rename + cfg clean-break (the dive: re-ground → gate → code) |
| #3 | pending | E.1.2 — NodeState SoA/Money relayout (first real consumer of the conformance tool; INBOUND-receives the split-off ml_inference_ran counter / ML-worst-case gate / ratchet / floor per D-237 FORK-B) |
| #4 | pending | #12 — Subagent registry: i/a/v/d/c LIVE (confirmed in agent-types list) + discoverability pointer + DESIGN_SPEC remainder |
| #5 | pending | #7 — TECH_DEBT-204: .githooks not mirrored by /sync-workspace (structural symlink-mirror) |
| #6 | pending | #11 — Umbrella sweep finish: stale size comments (PortfolioController.hpp:253/256/257) + preventive pins + wire size tool |
| #7 | pending | #15 [HIGH] — trade-branch → branchless (H20) optimization leaf (D-236) |
| #8 | pending | #3 — grep-CI aggregator at ≥2 gates (moot/note — the static ASM gate may subsume it; re-confirm) |

**Recreate this TaskList** via TaskCreate at pickup. #2 (E.1.1) is the live focus; #4-#8 are separate post-rename deliverables; #3 (E.1.2) is the successor leaf.

## Per-Step status — what's NEXT

| Item | Status | Notes |
|---|---|---|
| `.E.1.0` tag + push | **DONE** | `90b2daf`, signed, pushed; the conformance tool is a STANDING gate now |
| E.1.1 C-class re-ground | **NEXT** | RE-DERIVE the stale grep counts + file:line anchors vs HEAD; resolve the MAX_EXECUTION_NODES naming Q |
| E.1.1 /precoding-audit-gate | after re-ground | deletion-class (B14/B15); consult before coding |
| E.1.1 the rename + cfg clean-break + single_core delete | after gate + consult | names-only on FROZEN logic; determinism golden must stay byte-IDENTICAL |
| E.1.2 (NodeState SoA relayout) | post-E.1.1 | the orthogonal BYTES-on-FROZEN-NAMES anchor; first real consumer of the conformance tool |

## Coding-time discoveries (from the E.1.0 arc — assume they shape what you'll hit)

- **The conformance tool is now a STANDING gate** the rename rides under. A names-only edit changes no bytes → the determinism golden (`±USE_NATIVE_128` byte-compare) must stay byte-IDENTICAL across the rename, and the H7/H8/H14/H20 instruction/branch gates must stay green or RED-flag a careless-rename slip. E.1.1's value to the guard-matrix is PROVING the net catches a careless-rename hot-path regression (no NEW guard rows).
- **The cfg clean-break is the ONE behavior change** in an otherwise-mechanical leaf: an operator's existing `engine.cfg` with `core_N_*` keys will HARD-REFUSE (loud-fail-by-design, closes the Knight silent-revert class) → **Operator migration impact**: the operator renames `core_N_*` → `node_N_*` herself. No SOFT-compat alias (D-219/D-223 clean-break is the decided posture). Surface in FEATURE_LOOKUP + the ship's migration note.
- **MetaRegistry rename ripple (H15):** `FOREACH_PER_CORE_CFG_FIELD`'s meta-registry enrollment + the integrity CI check (`check_per_core_registry_integrity.py` → `check_per_node_registry_integrity.py`) rename together — a sister-cohort amendment (don't rename the registry + leave the check pointing at the old name).
- **`MAX_CORES` is a PHANTOM** — the real symbol is `MAX_EXECUTION_CORES` (the decision-log shorthand misleads). Target `MAX_EXECUTION_NODES`. Collision check at scaffold time: `MAX_EXECUTION_NODES`/`NodeContext`/`FOREACH_PER_NODE_CFG_FIELD` = 0 hits (clean substitution) — RE-VERIFY at dive.

## TECH_DEBT scoreboard (in-flight status at handoff write)

**E.1.1 closure targets (cited in the scaffold):**
| TECH_DEBT | Title | Status |
|---|---|---|
| TD-197 | cfg-flag-orphan retirement | the HARD-REFUSE parser SUBSUMES the class; reclaim-not-freeze retirements batch here |
| TD-002 | (PARTIAL) `single_core` sub-deletion | D-61 `single_core` LIVE removal lands E.1.1; the central-drainer removal is E.1.3 (cross-linked, not double-counted) |
| TD-175 | rename-cascade tool | `check_struct_alignment.py` EXISTS; build the rename-consumer-enumeration piece on top, use on the rename, E.1.2 reuses for SoA |
| TD-129 / TD-135 | rename-adjacent stale naming | VERIFY-subsumed at ship close |

**Carried open (NEW from the E.1.0 arc — NOT E.1.1 targets unless opportunistically subsumed):**
| TECH_DEBT | Title | Status |
|---|---|---|
| TD-204 | .githooks not mirrored by /sync-workspace | open (manual-mirror until the structural symlink-mirror lands; task #5) |
| TD-205 | FINDING A — H3 pthread via Notify_Send cold/sanctioned | open (dispositioned; not a fix) |
| TD-206 | XGBoost coverage gap (Model_Predict* external-API stub) | open |
| TD-207 | M7 — independent-review-by-default for mid-session tool builds | open |
| TD-203 | Phase-D blanket live-gate removal @ v5.16 (H21 tombstone) | open (deferred-by-design) |

## Pre-pickup self-audit (4-pillar discipline)

| Pillar | Verdict | Notes |
|---|---|---|
| 1. DESIGN_SPECS cross-check | `rename-ship-methodology.md` (the executor-vs-grep-matcher lesson — verify the EXECUTOR, not just the grep) · `dead-code-and-identifier-retirement-discipline.md` (H21 NAME-vs-bit; the cfg-key clean-break Rule-2 reclaim-not-freeze) · `per-node-purity-scale-invariance.md` (the Core→Node rename IS the H22 terminology bridge) | the scaffold's `sister_specs` |
| 2. Anti-pattern catalog | watch **Class 36** (overlapping-span substitution corruption + file-path-rename broken-links — the rename-tool hazard: non-idempotent --apply double-renaming `Node`-inside-`NodeContext`; anchor the executor) + **Class 40/H21** (don't recycle a wire-visible slot — N/A here: cfg KEYS are operator-text not wire, so clean-break is correct) | the rename is the Class-36 surface; the cfg-key break is H21-SAFE by the operator-text-vs-wire distinction |
| 3. Operator-impact | **YES — the cfg clean-break breaks existing `engine.cfg`** (HARD-REFUSE on `core_N_*`). Mandatory migration note: operator renames keys to `node_N_*`. Loud-fail-by-design. | the one behavior change; FEATURE_LOOKUP + migration note required at ship close |
| 4. Novel alternative | clean-break+HARD-REFUSE REFINES the v0.1 "extend FOREACH_LEGACY_PREFIXED_KEY"(alias) — the alias tombstone is explicitly NOT reintroduced (D-219/D-223; A-adjudication §3 verified clean) | decided posture |

## Arming for the next action

For **the E.1.1 C-class re-ground → `/precoding-audit-gate` → rename** — the surface is a codebase-wide mechanical rename + a cfg-parser deletion-class clean-break on the FROZEN hot path. Arm it:

- **Route to:** **`c-class`** (the re-ground — re-derive every stale grep count + verify file:line anchors vs HEAD `90b2daf`; the scaffold DEMANDS this first) → **`/precoding-audit-gate`** (deletion-class) composing **`/trace-deps`** (the 351+217+22-file consumer enumeration — `feedback_enumerate_consumers_before_registry_row_deletion`) + **`/dod-audit`** + **`/blindspot-scan` B14** (multi-surface deletion ordering for `single_core`) **+ B15** + **`/registry-fit-audit`** (the `FOREACH_PER_CORE_CFG_FIELD` + H15 MetaRegistry rename ripple + the TD-135 `regime_names[]` sibling-array). ARM each agent per M8 (docs + toolchain + scout-first; the adversarial/a-class agent stays independent on the verdict).
- **Run (not just read):** `tools/check_latency_path_conformance.py` (+ `--selftest`) **BEFORE** you rename (green baseline, so any RED is your edit) · `tools/check_determinism.sh` (the golden must stay byte-IDENTICAL — names don't change bytes) · `tools/calls_graph_diff.sh` · `./build.sh test` + `./tools/run_all_tests.sh` after each mechanical batch · regen `./tools/gen_code_map.sh` · the TD-175 rename-cascade enumeration on top of `check_struct_alignment.py` · the H15 integrity-check rename (`check_per_core_registry_integrity.py` → `..._node_...`).
- **Read first:** the E.1.1 scaffold (entire) · `DOCS/STRATEGY_AND_CODING_RULES.md` (hot-path rename touches H4/H7/H8/H14/H20 surfaces — names only, but the gates are live) · `plans/_cross-cutting/2026-05-06-latency-path-discipline.md` · `rename-ship-methodology.md` · the cfg-parser block `CoreFrameworks/ControllerConfig.hpp:2878-2968`. **Invariants in play:** H7/H8/H10/H14/H20 (the standing gates the names-only edit must keep green) · H15 (MetaRegistry enrollment renames with the registry) · H21 (cfg-KEY clean-break is correct precisely because keys are operator-text, NOT persisted-wire) · H22 (the rename IS the per-node-purity terminology bridge).

## Capture-completeness (this session — nothing lost)

This session was a SHORT ship-close: the predecessor sessions (s4/s5) built + codified everything; this session ran the pre-tag verification + tagged `.E.1.0` + closed out. Every dimension addressed:

- **Decisions → log:** NO new operator decision this session — the tag executed the ALREADY-DECIDED plan (D-238 "the ONLY remaining E.1.0 step is the tag"). Nothing to add to the decision log. (The s4/s5 decisions D-237/D-238 were logged in their sessions.)
- **Findings → ledger:** ONE drift found + FIXED-not-logged (Stage 4.6): `TOOLS.md:54` said the conformance DESIGN_SPEC + Class-51 entry were "ship-close-pending" — they LANDED s5, so the line was stale → corrected to "SHIPPED .E.1.0". No NEW TECH_DEBT this session. TD-204..207 carried open (the E.1.0-arc finds; homed last session).
- **Memories/skills → indexed:** NO new memory or skill this session (a tag + close ritual codifies nothing new — the apparatus already existed). The i/a/v/d/c agents + SUBAGENT_ARMING.md were verified LIVE (in the agent-types list), not newly created.
- **State SSoT updated:** the sprint MASTER banner flipped "NEXT = TAG .E.1.0" → a SHIP-CLOSE banner (`.E.1.0` SHIPPED); the old active handoff superseded; this E.1.1 handoff written `status: active` (singleton invariant held — verify via `check_handoff_active_singleton.py`).
- **Verified:** `check_session_docs.sh` SWEEP CLEAN (12 HARD + 4 ADV) at pickup AND re-run at close; `build.sh test` 3635/0 + `run_all_tests --fast` ALL HARD PASS + `calls_graph_diff` CLEAN on the shipped state; commit + tag GPG-signed (verified Good signature); the two false-alarm "missing" artifacts at pickup (Class-51 file + the 5 agents) RESOLVED as engine-relative-path artifacts of workspace-backed files (verified present at the workspace path).

---

## Paste this prompt into a fresh Claude Code session to start E.1.1

```
I'm picking up E.1.1 (Core→Node mechanical rename + cfg-key CLEAN-BREAK + single_core LIVE deletion) for the v5.15-live-readiness sprint. Predecessor .E.1.0 SHIPPED (the H8 conformance analyzer + §4d guard-NET; tag v5.15.5.F.4d.1.E.1.0).

Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-17-E.1.1-core-node-rename-handoff.md

This is a fresh context window; do NOT trust prior-session memory — verify against current code.

E.1.1 is a SCAFFOLD, RED/pre-gate. Its grep COUNTS are grounded against an OLD HEAD (572c978) and are EXPLICITLY stale. Your FIRST action is NOT coding — it is:
  1. C-class re-ground: RE-DERIVE every grep count + verify file:line anchors vs current HEAD (90b2daf). Resolve the MAX_EXECUTION_NODES vs MAX_NODES_PER_CLUSTER naming question.
  2. /precoding-audit-gate (deletion-class — single_core removal + cfg-key HARD-REFUSE; /blindspot-scan B14/B15 auto-fire).
  3. Consult Caramel BEFORE coding (consult-before-coding).
  4. Then the rename: names-only on FROZEN logic; the conformance tool + determinism golden are STANDING gates the edit must keep green (byte-IDENTICAL golden — names don't change bytes). The cfg clean-break is the ONE behavior change (HARD-REFUSE legacy core_* keys; operator migrates engine.cfg).

Honor the locked decisions: D-27 (Core→Node hard rename) / D-61 (single_core delete) / D-219+D-223 (cfg-key clean-break + HARD-REFUSE, NOT alias) / D-131 (epoch-free) / H21 (snapshot VERSION untouched — E.1.2 owns it; cfg-KEY break is correct because keys are operator-text not wire). Watch RBP Class 36 (rename-executor non-idempotency — anchor the substitution). E.1.2 (NodeState SoA relayout) is the successor.
```

---

## Quick links

- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- This ship's plan (SCAFFOLD): `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md`
- Sub-master (umbrella): `plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E.1-foundation.md`
- Successor: `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md`
- Decision log: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-27/D-61/D-219/D-223 govern E.1.1)
- Guard matrix: `plans/v5.15-live-readiness/E-guard-coverage-matrix.md`
- Dependency DAG: `plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md`
- The standing gate E.1.1 must keep green: `tools/check_latency_path_conformance.py`
- M8 arming SSoT: `DOCS/SUBAGENT_ARMING.md` + `claude-agents/{i,a,v,d,c}-class.md`
- DESIGN_SPECS catalog: `tick-trader-percore-workspace/DESIGN_SPECS/README.md`
