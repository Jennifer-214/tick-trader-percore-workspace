---
type: subplan-scaffold
ship_tag: v5.15.5.F.4d.1.E.1.3
fable_fit: HIGH - hardest gate in the .E.1 arc (torn-read closure + aggregator coherence + kill-hierarchy + Fight-#4 owner<->hot re-sync); codes AFTER E.1.2 but DESIGNABLE-AHEAD now - best model on the highest-stakes capital-concurrency design. (2026-07-02 Fable-window triage)
anchor: pre-v5.15.5.F.4d.1.E.1.3
parent: 2026-05-28-v5.15.5.F.4d.1.E.1-foundation.md
predecessor: 2026-06-15-v5.15.5.F.4d.1.E.1.2-relayout.md
successor: 2026-06-15-v5.15.5.F.4d.1.E.1.4-fill-completeness.md
status: scaffold
risk: HIGH
hot_path: true
spine: SPINE-2 (coherence)
live_gate: PRECONDITION-1-of-2 (HARD live-enable = E.1.3 ∧ E.1.5)
decisions_honored: [D-34, D-54, D-193/H22, D-209, D-168, cut-line-principle-4]
td_closed: [TD-002, TD-129, TD-170, TD-171, TD-196, TD-197]
guard_rows: [§4b-conc-5, §4b-torn-read-class, §3-kill-switch-hierarchy, §3-tested-flatten-all, §3-watchdog-liveness, §4a-H6, §4a-H3, §4a-H7, §4a-H8, FPN_AtomicAdd-determinism, Class-50-single-owner-disk_file]
source: plan_checks/2026-06-15-E.1-decomp-A-adjudication.md §2 (E.1.3) + the RESIDUAL-HOLE contract §3
---

# E.1.3 — GLOBAL O(1) aggregator + torn-read class + drainer absorption + conc-5

> SCAFFOLD (not a coding body). Meaningful enough to reason + sequence + seed the dive body; the full body is re-grounded against HEAD by a C-class pass when this leaf becomes the dive (per `plan-hierarchy-and-sub-master-decomposition.md` §3). All file:line are HEAD `572c978` and WILL drift — re-verify at dive.

## Scope

**SPINE-2 (coherence). The single highest-risk ship in `.E.1` — its seams are capital-safety-critical.** Four braided concerns under one rollback anchor (they share one inbound = the frozen `NodeState`/`ClusterState` layout, and one outbound = a coherent published snapshot; a coherence break here is unambiguously a coherence bug because the rename [E.1.1] and the fill-MATH [E.1.4] live in OTHER ships):

1. **GLOBAL O(1) push aggregator** — D-34 (GLOBAL, NOT per-cluster) + D-54 (push-model O(1)). Atomic on the 16B `Money` (`FixedPoint<10,8>`) via native `__int128` `cmpxchg16b` — **the 24B/split-slot premise is DEAD** (D-214-F1: 16B is native-atomic, no split). The aggregator is the **SOLE writer** of the global + per-cluster kill flags; every node mirrors via an atomic read of the published snapshot (never writes the global).
2. **Cross-thread torn-read CLASS closure** — 9 sites, 3 LIVE-capital (incl. the Knight-shaped reconciler-corrects-on-a-torn-read). Closed STRUCTURALLY via single-writer + seqlock-published money snapshot (reader-side per `cross-thread-multiword-read-consistency-discipline.md`), NOT a per-site patch.
3. **Per-node slow-path drainer absorption** — the central drainer goes (the `Run.hpp` lambda + the `ControllerEventLoop` central drain); relocate `OrderManager_Tick` + the phased-drain buckets into the per-node slow path. **The M6 body-content enumeration MUST capture the 4 omitted drainer responsibilities** (see "Known folds it owns" — this is the de-risk for the residual HOLE).
4. **conc-5** — single-producer-per-node `submit_queue` (assert + tsan-clean). 3-kill-flag bit-pack (F3). H1 boot-reconcile `std::vector<std::future>` → `std::array<pthread_t, MAX_NODES>` + manual join. A3 half-flatten retry / back-pressure design (TD-170). A22 `intended_*` gets its OWN seqlock pack (the money-aggregator does NOT reach it).

## Spine

SPINE-2 of the three D-214 spines (purity / **coherence** / fill). Coherence is upstream of BOTH SPINE-1 (fill, E.1.4) and SPINE-3 (purity, E.1.5): the published snapshot is the source the fill reconciler (E.1.4) and the A34 governor (E.1.5) read. **SPINE-2-before-SPINE-1 is a HARD data dependency, not a preference**: `handle_sell_fill` WRITES `oms->balance`/`oms->realized_pnl` (`OrderManager.hpp:1249-1250`) → `EventLoop_KillSwitchEvaluate` READS `oms->balance` cross-thread (`Async.hpp:449`) → the ledger must be coherent (single-writer/seqlock) BEFORE the fill math is reworked.

## TD-closed (one line each)

- **TD-002** — central drainer removal (the substantive close; `single_core` sub-deletion landed in E.1.1, cross-linked). The one TD spanning two ships.
- **TD-129** — per-node drainer architecture (the absorption).
- **TD-170** — FlattenAll retry / back-pressure (A3 half-flatten design).
- **TD-171** — conc-5 / capital guards remainder (single-producer enforcement).
- **TD-196** — snapshot count-lock superseded by the seqlock published snapshot.
- **TD-197** — the cross-thread torn-read class — the STRUCTURAL close (single-writer + seqlock). Closes RBP Class 26/27 structurally + the torn-read class.

## Locked decisions

- **D-34** — aggregator GLOBAL (NOT per-cluster). The per-cluster *view* is a slice of the global running aggregate (E.1.5 A34 governs on that slice).
- **D-54** — push-model O(1) (each fill/close pushes its delta; no O(N) scan).
- **D-193 / H22** — single-writer IS the purity mechanism (a node never writes shared state → scale-invariance preserved).
- **D-209** — `owner_node_id` (landed E.1.2) is the attribution foundation the aggregator reads.
- **D-168** — this ship is the GATE, NOT the enable. Live stays OFF until ALL of `.E` / v5.16.
- **D-214-F1** — 16B `__int128` native atomic; the 24B/split-slot premise is retired.

## §4 guard-rows this leaf fills

- **§4b conc-5** — single-producer-per-node, tsan-clean ENFORCED (HOLE → ENFORCED).
- **§4b the torn-read class** — `/bug-check` CLEAN after the structural close.
- **§3 kill-switch-hierarchy** + **§3 tested-flatten-all** + **§3 watchdog/liveness** — the aggregator-single-writer kill path + A3 tested FlattenAll.
- **§4a H6** — `alignas(64)` on the seqlock-bound packs (money snapshot + A22 `intended_*`).
- **§4a H3** — boot-reconcile drops `std::future`/`std::vector` for `std::array<pthread_t>` + manual join (no `std::mutex`/`condition_variable` reintroduced).
- **§4a H7 / §4a H8** — the absorbed drain stays branchless (grep-CI under E.1.0's net); drainer p99 ≤100μs (slow-path budget) + the drainer H8 histogram relocates intact.
- **FPN_AtomicAdd / `USE_NATIVE_128` determinism row** — the aggregator's 128-bit atomic is the GATE's reference consumer (2× run + ±`USE_NATIVE_128` byte-compare).
- **Class-50 single-owner-`disk_file` detector** — PARTIAL → ENFORCED (the detector; the event-log class-rework itself is E.1.4).

## INBOUND seam (needs from E.1.2)

The **FINAL `NodeState` / `ClusterState` layout** — frozen, SoA, Money-typed, `alignas(64)`. Specifically: the `slow_account` / `drainer_state` / `kill_flags` / `binding` clusters present + aligned; `owner_node_id` on `Position`; the `per_cluster[]` flat aggregate slot array; the snapshot VERSION already bumped + tombstoned. **The struct is FINAL — this ship reads/writes that frozen layout, never re-touches it.** E.1.0's grep-CI guards that the absorption stays branchless.

## OUTBOUND seam (the RESIDUAL-HOLE contract — EXPLICIT, both sides)

> **THE E.1.3 → E.1.4 RESIDUAL-HOLE CONTRACT (per the A-class adjudication §3 — a HARD outbound-seam contract, NOT a re-decomposition).**

E.1.3 lands: **"complete, tsan-clean fill-consumption PLUMBING (ring-drain + snapshot-publish) + the INERT leaf reached inside `OrderManager_HandleFill` (`OrderManager.hpp:1450`), with the fill MATH untouched."**

The three contract clauses this ship MUST satisfy so the seam at `:1450` is clean:

1. **The M6 body-content enumeration is COMPLETE** — the absorption CSV enumerates ALL drainer responsibilities (the 4 named below + any the enumeration surfaces), and E.1.3 does NOT close until each has a relocated home. (If the absorption drops one, a fill-accounting break in E.1.4 traces back to a *plumbing* gap here → the bisection muddies across the seam. This clause is the de-risk.)
2. **The plumbing is tsan-clean + the INERT leaf is REACHED by a test** that asserts "a fill COMMAND drains, dispatches, and HITS the leaf at `:1450`" WITHOUT asserting leaf math. The INERT leaf is the `OrderManager.hpp:1508` form, **unchanged**.
3. **E.1.4's inbound check** (stated on E.1.4) is "the `:1450` seam is reached + the plumbing is FROZEN" — E.1.4 touches ONLY the `handle_*_fill` leaf bodies + `FillEvent`, never the drain shell.

Other OUTBOUND guarantees: torn-read class `/bug-check` CLEAN; aggregator always-exact (drift == 0); conc-5 single-producer tsan-clean; the published snapshot is the SOURCE that E.1.4 (reconciler torn-read leg) AND E.1.5 (A34) read.

> ★ **HARD live-enable = E.1.3 (coherence) ∧ E.1.5 (purity), BOTH required (D-168).** This ship closes precondition 1 of 2 (the torn-read class — the D-168 BLOCKER). Live stays BLOCKED until this lands AND E.1.5's per-cluster purity lands (+ all of `.E` / v5.16). This is stated identically on E.1.5.

## Design-shape (code-grounded; HEAD 572c978, WILL drift — re-verify at dive)

**The aggregator (NEW).** A GLOBAL `AggregatorState` (0-hit NEW symbol per the A-class code-read) holding a 16B `Money` running total + the per-cluster slice slots, written by ONE thread (the drainer), published via seqlock. Native `__int128` `cmpxchg16b` — no split-slot. Reader-side double-read-with-sequence-check per `cross-thread-multiword-read-consistency-discipline.md`.

**The torn-read closure (the Knight-shaped site).** `Async.hpp:434-449` — the live KNOWN-RACE: `EventLoop_KillSwitchEvaluate(&state)` (`:449`) reads `oms->balance` off the producer thread while the drainer writes it (`OrderManager.hpp:1249`). The in-code TODO names the fix: "move kill switch eval to drainer thread, OR use an atomic balance snapshot." **Resolution: the kill-switch reads the seqlock-PUBLISHED money snapshot, never the racing `oms->balance`.** Why coherence precedes fill-math: the fill handler WRITES the ledger the kill-switch READS.

**The drainer absorption.** The central drainer hoist is `EngineSharded_Async_DrainWithSubmit` (`Async.hpp:770`); relocate `OrderManager_Tick` + the phased-drain buckets into the per-node slow path. The `Run.hpp` central-drain lambda goes (TD-002).

**conc-5 + boot-reconcile.** Single-producer-per-node `submit_queue` (assert at push). Boot reconcile: `std::vector<std::future>` → `std::array<pthread_t, MAX_NODES>` + manual join (H1/H3).

## Rollback anchor

`pre-v5.15.5.F.4d.1.E.1.3`. **A break here is a CONCURRENCY break (race / torn / drift)** — the rename (E.1.1) + fill-math (E.1.4) are in OTHER ships, so a tsan / aggregator-drift / kill-switch failure is unambiguously coherence. The single highest-risk ship gets the cleanest isolation — that is the entire point of the by-spine cut. `git bisect` lands on this anchor.

## Known folds it owns

- **The 4 omitted drainer responsibilities (the M6 body-content MUST capture ALL of these — the residual-HOLE de-risk):**
  1. **Phased-drain ordering** — the bucket sequence (the order fills are applied across phases) must relocate intact.
  2. **`Order_BindPreResolved` fee-rate binding** — the fee-rate is bound pre-resolution; the absorbed drain must preserve the bind site.
  3. **The `slot >> partial_on` logical-core mapping** — A8's `.E.0.10`-HARDENED slot↔core index (F-046 confirmed routing is by SLOT, `cmd.core_id == slot`); RE-HOME it, do NOT re-do it (already landed `.E.0.10`).
  4. **The drainer H8 latency histogram** — the per-drain latency instrumentation relocates with the drain.
- **A22 `intended_*` own seqlock pack** — the money-aggregator does NOT reach `intended_*`; it gets its own pack (`alignas(64)`, H6).
- **3-kill-flag bit-pack (F3)** — MBS_*/manual shift-mask per H14 (no C++ bitfields).
- **A3 half-flatten retry / back-pressure** (TD-170) — the FlattenAll robustness design (queue-full path; tested-flatten-all guard-row).
- **The INERT fill leaf (`OrderManager.hpp:1508`)** — composes UNCHANGED as the E.1.3 stub; the leaf math is E.1.4's only edit.
- **Fight #4 — the ExecutionCore mirror↔book coherence (D-288; AR-12-corrected — was only a POINTER in the synthesis, now folded IN so it doesn't fall through the crack).** The hot `core->live_tp`/`live_sl` arm from the DECISION tick (`ExecutionCore.hpp:543/549`) while the owner `Position.take_profit_price` books from the FILL (A25); no post-fill re-sync → live≠owner under slippage (TP fires early / SL fires late; INERT at `slippage_pct=0`, bites at configured-slippage + live — a live-readiness bug, config-scoped to any resolved `tp_pct`/`sl_pct != 0`). This leaf OWNS mirror↔book coherence, so close it here: **re-sync the hot fields from the seqlock-PUBLISHED owner** — a **branchless mask-select** on the existing hot seqlock read + a fill-confirm flag in `GateParameters._pad[6]` (H7/H20, NO exit-path branch), NEVER a bare drainer store into a hot field (multi-writer) NOR a cross-thread read of the owner's multi-word `Money` (torn-read). NOT `sg_*`-reuse (D-288: it's rewritten every rebuild → drifts the band + re-opens the v5.5.5 postmortem). The **SL leg also needs the A25-omitted `per_fill_sl`** owner plumbing (mirror `per_fill_tp` via the existing `ResolvePerFillSlPct`; grows `OrderPreResolved` 48→64B — SPSC-only, NO wire coupling, tool-verified). Discipline: `decision-time-data-binding-pattern.md` v1.4 (Fight #4 = the canonical hot-replica instance). ZERO E.1.2 layout coupling; interim backtest caveat until this lands = run `slippage_pct=0`. `entry_price` hot mirrors are DEAD writes (not part of the fix). **This re-sync mechanism is SHARED with register-A29** (the GUI-drag writes `pos->take_profit_price` but the hot gate ignores it — the *same* "hot `live_tp`/`live_sl` not synced from the owner" root; the mirror↔book re-sync serves both — reconcile at the A29/D-241 dive). **At the fix, correct the 4 stale fill-anchor comments** that assert fill-anchoring the code doesn't yet do (they become TRUE when the fix lands): `ExecutionCore.hpp:331`, `ShardedSnapshotPersist.hpp:637`, `StrategyParameters.hpp:426-427`, `GateParameters.hpp:94`; and `GateParameters.hpp:21` ("params persisted in v11+") is SEPARATELY false (GateParameters is not byte-persisted) — fix it too. **Sibling not-the-same:** A1/TD-168 (warm-restart used GLOBAL `take_profit_pct` vs the per-strategy override) is a DIFFERENT root on these same fields, CLOSED at `.E.0.10`.

## Shape-match note (do NOT re-schedule landed work)

A8's hardened `slot >> partial_on` mapping is ABSORBED (re-home) not re-built — it landed `.E.0.10` (F-046). The aggregator/torn-read/drainer surfaces are NEW (`AggregatorState`/`ClusterState` are 0-hit per the A-class code-read). The NEW-1 predicate is NOT touched here (it's E.1.5's relocate). NEW-1/A20/A21/A17 already landed `.E.0.10` — this ship builds the coherence substrate they will (in E.1.4/E.1.5) read coherently.

## [D-255 fold 2026-06-22] — INBOUND from E.1.1 ③: retire/re-wire the dead SAFETY_CRITICAL kill_switch_* fields (TD-220, Class-47)

The ③ accounting-audit (D-255) found `kill_switch_daily_loss_pct` / `kill_switch_drawdown_pct` are tagged `SAFETY_CRITICAL | CAPITAL_BOUND_LOSS` but consumed ONLY by the deprecated single_core `PortfolioController` (`:959/:968/:1592/:1612`) + a second `EngineCommon_BootGlobal:190-193` mechanism — the LIVE sharded MtM kill uses `min_kill_loss` + `max_drawdown_pct` instead (a Class-47 split-brain + a GUI display-lie: the GUI shows them SAFETY_CRITICAL while a sharded operator setting them gets nothing). E.1.3 is where `PortfolioController` dies (the TD-002 central-drainer half), so it's the home: either **re-wire** the two fields into the sharded kill chain OR **retire** them (H21 tombstone — they're GUI-visible). ③ validates their VALUES (the cohort); this leaf resolves the dead-path consumption. Cross-ref: TD-220 · D-255.
