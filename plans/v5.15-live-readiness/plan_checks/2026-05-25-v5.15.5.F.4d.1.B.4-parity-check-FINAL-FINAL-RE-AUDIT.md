---
type: plan-check
audit_kind: parity-check-FINAL-FINAL-RE-AUDIT-v1.4
target_ship: v5.15.5.F.4d.1.B.4 (v1.4)
audit_date: 2026-05-25
engine_head: 64e7101
verdict: GREEN
---

# /parity-check FINAL-FINAL RE-AUDIT — .B.4 v1.4 — 2026-05-25

## Verdict: GREEN

Phase B unlock: **PROCEED**.

v1.4's single amendment (N5) closes the v1.3 audit's MED finding (missed Regime_Classify write site at `BacktestSharded.hpp:607` in Step C.4.5 consumer enumeration). Comprehensive grep at HEAD `64e7101` confirms exactly 3 access sites of `fc_ctx.regime_state` / `fc->regime_state`, all enumerated in v1.4 Step C.4.5. No new concerns surfaced. All v1.2 + v1.3 amendments hold.

---

## N5 verification

### Consumer enumeration completeness — VERIFIED

Ran `rg "fc_ctx\.regime_state|fc->regime_state" Backtest/` against HEAD `64e7101`:

```
Backtest/BacktestSharded.hpp:548:    Regime_Init(&fc_ctx.regime_state, (int)cfg.regime_hysteresis);
Backtest/BacktestSharded.hpp:607:                    Regime_Classify(&fc->regime_state, &sig, fc->cfg);
Backtest/BacktestSharded.hpp:612:                ctx.current_regime = fc->regime_state.current_regime;
```

**Exactly 3 hits.** All 3 sites are enumerated in v1.4 Step C.4.5:
- `:541` (field declaration) + `:548` (Regime_Init call) — grouped as "`:541-548` field allocation" (atomic deletion unit; cannot have one without the other in a working compile)
- `:607` — Regime_Classify WRITE site (the v1.4 N5 catch)
- `:612` — read site (collapse-N-to-1)

Sister codebase-wide enumeration (`rg "fc_ctx\.regime_state|fc->regime_state" -n` — no path filter) returns identical 3 sites. No external consumers exist. No callers outside `Backtest/BacktestSharded.hpp`.

Also verified `FeatureCollectCtx` struct usage codebase-wide returns only 3 sites in `BacktestSharded.hpp` (`:526` struct definition, `:543` ctx instantiation, `:556` lambda cast). No external propagation of fc_ctx that would carry regime_state to a non-Backtest consumer.

### `:607` site verification — VERIFIED

Direct read of `Backtest/BacktestSharded.hpp:607`:

```cpp
if (d->rolling && d->rolling_long && d->regime_ror && d->ema_price) {
    Regime_Classify(&fc->regime_state, &sig, fc->cfg);
}
```

Confirmed: this IS the Regime_Classify call (not some other call), and the first argument `&fc->regime_state` is the WRITE target. v1.4 Step C.4.5 cites this site correctly as WRITE.

Surrounding context (`:602-608` comments) explicitly confirms the v5.14.5.B semantic: "classify regime BEFORE feature pack so ctx.current_regime reflects the prior cycle's hysteresed state." Per Decision F rationale, per-core regime classification already happens inside `EngineCommon_SlowPathCycleOneCore` body post-extract, making this central-classify redundant. Deletion is structurally sound.

### Deletion order discipline — VERIFIED

v1.4 Step C.4.5 prescribes order:
1. DELETE `:607` (write site)
2. DELETE `:612` (read site)
3. DELETE `:541-548` (field allocation; safe to delete LAST after read + write sites gone)
4. ADD `ctx.current_regime = state.cores[BACKTEST_REGIME_SAMPLE_CORE].regime_state.current_regime;`

**Within a single commit, order doesn't matter for compile correctness** (file as a whole compiles after all 4 actions land). The plan body's "safe to delete LAST" annotation is good review discipline — it ensures step-by-step intermediate states wouldn't compile-fail if reviewer steps through actions. Discipline is correct.

**Note on `:541-548` grouping:** The range `:541-548` covers BOTH `:541` (field declaration in struct) AND `:548` (Regime_Init call OUTSIDE struct). These are paired — field cannot exist without Init, Init cannot reference a deleted field. Treating as atomic deletion unit is correct. The plan body's "field allocation" framing covers both correctly.

### Verification gate well-specified — VERIFIED

v1.4 Step C.4.5 includes:
- **PRE-coding:** `rg "fc_ctx\.regime_state\|fc->regime_state" Backtest/` BEFORE coding starts (verify enumeration matches at coding time; catches drift if HEAD shifts)
- **POST-delete:** grep returns ZERO hits across `Backtest/`

Both gates concrete + grep-checkable. Mechanical verification possible.

### v1.4 Step C.4.5 introduces NO new concerns

- Effort estimate update (~18-20 LOC delta vs v1.3's ~15) is honest accounting for +3-5 LOC of `:607` deletion + verify step
- ADD step still uses v1.3 named constant `BACKTEST_REGIME_SAMPLE_CORE`
- No new TECH_DEBT entries needed
- No new PARITY findings surfaced
- No regression to other amendments

---

## All prior amendments hold

### v1.2 amendments (C1-C3 + H1-H4 + M1-M5 + L1-L2)

Spot-checked CRIT + HIGH amendments via HEAD code reads:

- **C1 (BNB cfg mutation one-shot):** `EngineSharded.hpp:690` confirmed as `if (cfg.pay_fees_in_bnb)` — one-shot scope structurally preserved at code (will be extracted to `EngineCommon_ApplyBnbDiscount` per N4 5-helper structure)
- **C2 (PARITY-031 closure shape):** N5 amendment IS the v1.4 refinement to C2; verified above
- **C3 (Option D AllCores wrapper):** Decision A v1.3 5-helper table preserves AllCores wrapper as helper #5 (thin ~10 LOC for-loop)
- **H1 (Per-core boot call sequence enumeration):** Step A.4 retains MANDATORY enumeration gate before Phase B unlock with N3-corrected order
- **H3 (PARITY-032 fold-in):** Decision E retains "NEW PARITY-032 — CLOSE via SlowPathCycleOneCore fold-in"; Step B.3 retains PARITY-032 inclusion in helper body
- **M3 (parity_harness pay_fees_in_bnb cohort):** Step C.6 retains expansion

All v1.2 amendments unchanged by v1.4. No regression.

### v1.3 amendments (N1-N4)

Verified at HEAD `64e7101`:

- **N1 (PARITY-031 named constant `BACKTEST_REGIME_SAMPLE_CORE = 0`):** Decision F preserved in v1.4 with named constant approach. v1.4 Step C.4.5 ADD step uses `state.cores[BACKTEST_REGIME_SAMPLE_CORE]` — correctly references v1.3 constant
- **N2 (Step C.4 double-fire deletion enumeration):**
  - `ShardedBacktestDriver.hpp:378-383` — VERIFIED. Direct read shows TimeExit (`:378`) + TrailingSLRatchet (`:380`) + BreakevenOnProfit (`:383`). Cite is accurate.
  - `EngineSharded.hpp:1925-1955` — VERIFIED. Centralized arch `if (cfg.engine_arch != ENGINE_ARCH_PER_CORE_SLOW)` block at `:1928-1954` contains TimeExit (`:1932`) + TrailingSLRatchet (`:1934`) + BreakevenOnProfit (`:1940`). Plan body's `:1925-1955` cites the broader enclosing context including comments — accurate.
- **N3 (Boot order: BNB `:690` → EventLoopState_Init `:742` → ConfigureKillSwitch `:750`):** Direct read of EngineSharded.hpp confirms exact lines:
  - `:690` — `if (cfg.pay_fees_in_bnb) {` ✓
  - `:742` — `EventLoopState_Init(&state, &oms);` ✓
  - `:750` — `EventLoopState_ConfigureKillSwitch(&state,` ✓
  Order: BNB → Init → ConfigureKillSwitch. Step A.4 example is now correct.
- **N4 (5th helper `EngineCommon_ApplyBnbDiscount`):** Decision A v1.3 5-helper table at plan body `:132-138` lists all 5. Step B.0 preserves dedicated body extraction. Step C.1 (live) + Step C.2 (backtest) both call ApplyBnbDiscount FIRST before BootGlobal. Sister precedent `Sharded_ValidatePartialExitCfg` cited at Decision A rationale.

All v1.3 amendments unchanged by v1.4. No regression.

---

## 7 PARITY entries closure

For PARITY-026 through PARITY-032: confirm by-construction closure with v1.4 5-helper structure + N5-expanded Step C.4.5 deletions.

| Entry | Closure mechanism | v1.4 verdict |
|---|---|---|
| PARITY-026 | Already closed by hotfix `v5.15.5.F.4d.1.B.2.h1-killswitch-fix` (commit 6fd0ba3) | CLOSED (predecessor) |
| PARITY-027 | `EngineCommon_SlowPathCycleOneCore` body extract per Step B.3; both EngineSharded + ShardedBacktest call same helper | CLOSE by-construction |
| PARITY-028 | `EngineCommon_BootPerCore` includes BindCompositeCfg + RollingTurnover_Init (Step A.4 enumeration); backtest now gains these calls | CLOSE by-construction |
| PARITY-029 | `EngineCommon_BootPerCore` includes Strategy_InitPerCore (Step A.4 enumeration); backtest now gains this call | CLOSE by-construction |
| PARITY-030 | `EngineCommon_ApplyBnbDiscount` called from BOTH EngineSharded + ShardedBacktest paths (Step C.1 + C.2); backtest now gains BNB cfg mutation | CLOSE by-construction |
| PARITY-031 | v1.4 Step C.4.5 deletes 3 sites (`:541-548` field + `:607` write + `:612` read) + adds named-constant read from `state.cores[BACKTEST_REGIME_SAMPLE_CORE]` | CLOSE by-construction (v1.4 N5 expansion ensures :607 write site not missed) |
| PARITY-032 | `EngineCommon_SlowPathCycleOneCore` includes EventLoop_BreakevenOnProfit fold-in per H3; both paths now exercise breakeven_on_profit_threshold | CLOSE by-construction |

All 7 close at ship close. The N5 amendment specifically strengthens PARITY-031 closure — without :607 deletion, the central Regime_Classify would still execute (writing to a now-deleted field — compile fail), OR if field kept (compile OK), would result in dead write to never-read field. N5 ensures the field deletion is paired with the write site deletion.

---

## NEW concerns surfaced

**None.** The audit found no new structural, sequencing, or parity concerns at v1.4. Spot-checks against all v1.2 + v1.3 amendments pass. Consumer enumeration is comprehensive. Deletion order is sound. PARITY closures all by-construction.

---

## Meta-observation (codification candidate)

**N5 catch validates the discipline extension.** v1.2 + v1.3 Decision F drafting did NOT comprehensively grep all consumers of `fc_ctx.regime_state` before proposing deletion — `:607` write site was missed across 2 amendment cycles. v1.4 audit caught it. This is `feedback_enumerate_consumers_before_registry_row_deletion` extended from registry-row deletion to **struct-member deletion** — same principle, different scope.

**Worth codifying** at ship close (per v1.4 frontmatter `v1_4_amended` line + Step C.4.5 amendment note + plan body `v1.3 → v1.4 amendment summary` meta-observation). Options:

- (a) Memory file amendment: extend `feedback_enumerate_consumers_before_registry_row_deletion.md` body with "Applies to ANY non-local symbol deletion (registry rows, struct fields, function-scope statics)"
- (b) NEW memory file: `feedback_enumerate_consumers_before_struct_member_deletion.md` sister to existing
- (c) DESIGN_SPECS amendment: add to `audit-scope-taxonomy.md` consumer-enumeration section

Recommendation: option (a) — sister discipline lives under same principle name (consumer enumeration before deletion); same workflow + grep pattern + verify gate. Codify at `.B.4` ship close per discipline-codification-at-close pattern.

**Iteration spiral concern check:** This is amendment cycle #4 for v1.2→v1.3→v1.4 (each cycle finding 1-3 items). Per `feedback_iteration_spiral_signals_audit_meta_gap`, 4+ cycles with shrinking findings IS the signal — BUT the meta-gap (struct-member-deletion consumer enumeration) was identified at v1.4 cycle and discipline extension is the codification response. Iteration ends at GREEN here; v1.5 not anticipated. Healthy depth, not noise spiral — operator decision (option B maximum verification) validated.

---

## Phase B unlock recommendation: PROCEED

v1.4 plan body is READY for Phase B implementation. No further audit cycles needed. Codify N5 meta-observation discipline extension at ship close.

**Coding sequence recommended:**

1. Step 0: Create rollback anchor tag `pre-v5.15.5.F.4d.1.B.4`
2. Phase A: Step A.1-A.6 scaffolding + boot-call-sequence enumeration + Caramel consult on Step A.4 + A.5
3. Phase B: Step B.0-B.6 helper bodies (5 helpers, build-incremental)
4. Phase C: Step C.1-C.6 caller migration with explicit deletion enumeration discipline
5. Phase D: Step D.1-D.5 meta-discipline codification + ship close
