---
type: plan-check
subtype: precoding-cascade
ship_tag: v5.15.5.F.4d.1.E.1.1
workstream: "③ cfg CLEAN-BREAK cluster (HARD-REFUSE unrecognized keys)"
date: 2026-06-21
head_at_cascade: 1da1c1c
status: SCOPE-LOCKED-PENDING-OPERATOR-SIGNOFF + adversarial re-gate
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
parent_plan: plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md
governs_decisions: [D-219, D-223]   # the clean-break+hard-refuse fork (pre-decided; not re-litigated here)
new_decisions_proposed: [D-242-F1-fault-model, D-243-F2-idx-postloop, D-244-F3-cadence-policy, D-245-F4-guard-now-walker-at-F4e, D-246-F5-operator-terminology]
---

# ③ cfg CLEAN-BREAK — pre-coding I→A cascade (SSoT)

Durable capture of the 8-agent pre-coding cascade + the foundation-reframe (F1–F5) + the LOCKED scope. Survives compaction. The decision-log entries (D-242..246) + the plan-body fold land on operator sign-off; the adversarial re-gate runs against THIS locked scope before coding.

## Verdict
🟢 **GREEN-to-code with required amendments.** No RED, no re-design. The D-219/D-223 clean-break+hard-refuse decision holds. The cascade materially corrected the plan's gate-synthesis: 2 GUI→engine bricks (not 1), the refuse *mechanism* the plan under-specified (must be flag-based, never abort-in-parser), a HIGH `fgets` spurious-refuse, and the foundation-reframe (this ③ is the cfg-validation FOUNDATION E.1.2/E.1.5/E.1.6/E.2 all extend).

## Decisions landed (live — supersede the open-call framing below where noted)
- **D-242** (logged) — ③ cfg validation = a "config compiler": full compiler-style readout (ERRORS block / WARNINGS proceed) + a clean compile GATES every fresh start (live + backtest, operator-confirmed "clean compile"); hot-reload/GUI warn-keep-old; flag-based (never abort-in-Load); key-match mechanism. ③ ships the framework + KEY/INDEX errors; the semantic value-validation sweep → E.2.
- **D-243** (logged) — config path handling: ③ does the **buffer-robustness fix ONLY** (size line buffer ~320 + clean overflow error; fixes the spurious-compile-error + the silent path-truncation). The operator's **single global deployment ROOT** (relative paths feeding models/logs/data/everything, machine-portable derived default) → **E.2** (a root setting in ③ w/ no consumer = Class-44 orphan flag; deployment-root is E.2's; ③'s compiler is the seed). Homed: D-243 (E.2-body fold at E.2's dive).
- Supersession of the open-call list below: **D1→D-242** (backtest compile-gated); **D2→D-243** (buffer-fix-only; root→E.2); **GUI warn routing→D-242** (terminal + `config_error_log`, NOT a banner — the GUI deprecates at E.2); **backtest CLI-vs-GUI→D-244** (open, .E.2).
- **Operator-delegated micro-calls (2026-06-22, "these don't need my input"):** co-house both ⊆ guards in `check_per_node_registry_integrity.py` · guard-now + parser data-drive→`.F.4e` (I-3) · tab label "Engine N"→"Node N" · GUI terminology Cores→Nodes. **③ is now FULLY SCOPED; only the adversarial re-gate on the locked scope remains before coding.**

## Cascade roster
| Agent | Class | Headline |
|---|---|---|
| I-1 | trace-deps | full cfg-key lifecycle; **the cadence-split** (Load has no error channel; boot + 2 hot-reload + GUI callers); legacy-global set CLOSED (engine_mode/num_execution_cores/core_N_*); engine_mode danger overstated (single_core path gone) |
| I-2 | dod-audit | C-2 guard = EXTEND to Check 12 (named sister tool doesn't exist); Class 25/27 clean; 7-suffix-vs-registry Class-18 → home-as-debt |
| A-1 | adversarial | **brick #2** `num_execution_nodes_PLACEHOLDER` (:456); out-of-range silent-swallow; core_N_* is the dominant migration surface |
| A-2 | adversarial | refuse-mechanism core HELD; **HIGH R-C** `fgets` line[256] overflow → spurious boot-refuse on deep model paths; R-E placement (check right after load) + backtest-WARN; only 7 suffix branches need idx handling (not the :2929 block) |
| A-3 | adversarial | no third brick; no index-shift corruption (load matches by key-string); guard teeth-gap (no --selftest); **the bricks don't refuse TODAY — ③ CREATES them → atomic-ship ordering** |
| I-3 | trace-deps/dod | F4 = **GUARD-NOW, data-drive at .F.4e** (pull-forward fails subsumed-vs-adjacent + injects frozen-bisect risk; pure walker impossible — strategy aliases not registry-derivable); the ⊆ guard IS the structural class-close |
| I-4 | trace-deps | test red-build risk **EMPTY** (② cleaned all keys); 1 silent-ignore flip (:24098); 6-7 characterization tests; **sibling precedent `ConfigField_Set` already rejects unknown keys (:18376)**; **HIGH: fault must be key-match-based not field-written-based** |
| I-5 | trace-deps | **GUI calls the full engine parser (Settings_Load:887) → abort-in-parser = GUI crash (3rd confirm of flag-not-abort)**; guard scope correction = **GUI-recognized ⊆ engine-recognized** (read-side, not just writer-side); GUI warn = banner (R2); rename key-safe |

## LOCKED ③ scope (F1–F5 + D1/D2)

### F1 — cfg-fault ERROR MODEL: extensible bitmap (seeds E.2 hierarchical-config-validation)
- Add `cfg_load_fault_flags` (uint32) to **`ControllerConfig`** (cold boot-flags region, next to `live_capital_cfg_conflict` @ ControllerConfig.hpp:576). **NOT `PerNodeCfg`** → no size-pin touched (pins are on PerNodeCfg @ :321/332/336); not alignas(64); not hot-path; not wire. **Zero hot-path cache impact, breaks zero static_assert** (verified).
- Bits seeded by ③: `UNKNOWN_KEY`, `BAD_NODE_IDX`. Room for E.1.5 `CLUSTER_PURITY`, E.1.6 `EXCHANGE_INVALID`.
- **Fold `live_capital_cfg_conflict` into the bitmap at E.1.5** (the natural moment — NEW-1 relocates that predicate), NOT now (avoid premature capital-field churn). ③ seeds; E.1.5 consolidates.
- **MECHANISM (I-4 HIGH): key-match-based** — a fault = "no parser branch matched this key," NOT "no field got written." A field-written test would over-fire on good-key/bad-value (ensemble_blend_mode=garbage keeps defaults without writing).
- Align fault semantics with the sibling precedent `ConfigField_Set` (already returns 0 on unknown key).

### F2 — idx-validation: ONE post-loop pass, not 7 patches (H22; compounds to E.1.5 per-cluster bounds)
- Keep the physical array-bounds guard (`idx<16` = MAX_EXECUTION_NODES array size, anti-overflow).
- Replace the 7 scattered silent-`continue`s (ControllerConfig.hpp:2839/2847/2858/2866/2879/2892/2902) with ONE post-loop validation: track touched node-slots; any touched slot `>= num_execution_nodes` → fault. H22-shaped; E.1.5 re-scopes the bound to per-cluster.
- Unknown per-node *suffix* (`node_3_bogus`) already reaches the loop bottom via the :2929-block fall-through → caught by the single-point refuse (decided: faults, yes).

### F3 — boot-vs-runtime cadence: FLAG-based, explicit policy (seeds runtime-mutable-vs-boot-time pattern)
- **The refuse NEVER `exit()`/abort inside `ControllerConfig_Load`** (3 independent confirmations: A-2 hot-reload bulk-copy; I-5 GUI Pass-A crash; I-1 no-error-channel). It sets the F1 flag; callers act per cadence:
  - **boot/live** (main.cpp:~76) → check flag **right after load, BEFORE mlockall + symbol-uniformity** (A-2 R-E) → abort/return 1.
  - **backtest** (BacktestSharded.hpp:119 file-load branch only — NOT the use_config_override branch; BacktestEngine.hpp:2367) → **WARN + continue** (D1: research ergonomics; backtest writes no orders).
  - **hot-reload** (Async.hpp:335 bulk-copy + EngineTUI.hpp:600→PortfolioController_HotReload) → **WARN + keep-old** (don't bulk-copy / don't call HotReload if flagged). CONFIRM the EngineTUI path isn't dead post-single_core-deletion.
  - **GUI** (Settings_Load:887 Pass-A) → **WARN banner** (R2 — fixed-buffer SettingsState warn rendered in the Settings panel; H1/H3-safe), never abort.

### F4 — guard-now, walker at .F.4e (structural class-close via CI guard)
- Add **parser-suffix ⊆ registry** check to `tools/check_per_node_registry_integrity.py` (~30-40 LOC; tool already parses both sides). Closes the desync→brick CLASS at CI — a future manual-per-node field added without a parser branch fails the build, can't become a silent-default-then-brick.
- Data-driving the manual-per-node parser → **.F.4e** (already scoped there per every FOREACH_MANUAL_PER_NODE_FIELD rationale; pulling forward = churn + MED-HIGH frozen-bisect risk; pure walker impossible — strategy aliases not derivable from FOREACH_STRATEGY).
- DISTINCT from the C-2 guard below (different ⊆ relation).

### F5 — operator terminology + migration message + tests
- (a) **GUI terminology rename** Cores→Nodes / Per-Core→Per-Node / "Core N"→"Node N" (display-only; **key-safe** — every key built from literal `"node_"` + key_suffix, never a label; do NOT let a find-replace touch a `key_suffix` value — Class 36). Tabs already "Engine %d" (reconcile to "Node %d" — minor).
- (b) **Refuse message = migration hint**: on a `core_`-prefixed key → "keys renamed core_→node_ at E.1.1; did you mean `node_0_strategy`?" (reusable at future renames).
- (c) **Characterization tests** (I-4 List C) + the List B flip.

### C-2 guard — CORRECTED SCOPE (I-5): GUI-recognized ⊆ engine-recognized
- The new guard (the C-2 brick-class close) asserts **GUI-recognized-set ⊆ engine-recognized-set** — read-side superset: `field_defs[]` keys ∪ `per_node_fields[]` suffixes ∪ GUI heterogeneous `node_` branches. Strictly subsumes writer-side (catches display-only rows). The GUI parser is an INDEPENDENT hand-list (the brick root cause) — writer-only ⊆ leaves it unverified.
- TWO distinct guards total: (1) F4 parser-suffix ⊆ registry (engine-internal), (2) GUI-recognized ⊆ engine-recognized (C-2 brick class). Co-house or separate — operator call (minor).
- Both **teeth-proofed RED on the 2 bricks BEFORE the row deletes** (add `--selftest`; A-3 teeth-gap). Class-51 non-vacuity.

### The 2 bricks (delete; A-3 proved positionally safe — load matches by key-string, no index-shift)
- `GUI/SettingsPanel.hpp:456` `num_execution_nodes_PLACEHOLDER` — dead phantom widget (incomplete .F.4c deletion); the real `num_execution_nodes` is registry-rendered.
- `GUI/SettingsPanel.hpp:669` `confidence_freshness_tau` — the C-2 brick; **no knob loss** (live `confidence_freshness_tau_secs` is registry-rendered independently). Coordinate with PARITY-006.

### D2 — fgets overflow (A-2 R-C, HIGH): harden BEFORE the refuse
- `char line[256]` overflows on `node_N_model_dir=<deep path>` >~237 chars; if the overflow tail has `=` (ML dirs: `fold=3/`), it becomes a bogus key → the new refuse aborts boot on a VALID cfg. **Option (a):** drain-to-EOL + flag the real long line (also fixes the latent silent-truncation bug — close-out-now/subsumed). Lands before the refuse.

### Atomic-ship ordering (A-3): the bricks don't refuse TODAY — ③ CREATES them
- The 2 row-deletes + both guards (teeth-RED-proven) + the fgets harden land **in the same commit as, or strictly before,** the reject-on-unknown-key code. Guard-green gates the refuse's merge.

## Open calls needing operator sign-off (recommendations marked)
1. **D1 — backtest semantics:** WARN+continue (rec) vs abort. → **WARN.**
2. **D2 — fgets fix:** Option (a) drain+flag+fix-latent-bug (rec) vs (b) enlarge buffer. → **(a).**
3. **GUI warn routing:** R2 SettingsPanel banner (rec) vs R1 stderr-only. → **R2.**
4. **F1 fold-timing:** seed bitmap now, fold live_capital_cfg_conflict at E.1.5 (rec) vs fold now. → **seed-now-fold-at-E.1.5.**
5. **Two guards co-housed or separate** (minor). → operator preference.
6. **Tab label "Engine N" → "Node N"?** (minor terminology).

## Codification candidates (capture on sign-off)
- **Decision log D-242..246** (F1 fault-model / F2 post-loop-idx / F3 cadence-policy / F4 guard-now+walker-at-.F.4e / F5 operator-terminology).
- **NEW anti-pattern (RECURRING_BUG_PATTERNS):** "incomplete deletion left as a live `_PLACEHOLDER`/tombstone-by-rename row that still renders+writes" (brick #2). Sibling of dead-code/identifier-retirement (Class 40) + display-lie (Class 2).
- **DESIGN_SPECS seeds:** F1 → `hierarchical-config-validation-pattern` (E.2, planned); F3 → `runtime-mutable-vs-boot-time-config-pattern` (E.1/E.2, planned). ③ = first-canonical seed for both.
- **Debt:** the `fgets`-256 latent truncation bug → **fixed in-ship** (D2a subsumes; close-out-now, not deferred). The manual-per-node-parser data-driving → .F.4e (homed; the F4 guard de-risks the wait). The GUI Pass-B hand-list elimination → .F.4e (same home; I-5 novel alt).
- **DAG:** add the F1 (fault-accumulator) + F3 (boot-vs-runtime) forward-seams that E.1.5/E.1.6/E.2 extend.
- **FEATURE_LOOKUP:** the HARD-REFUSE + operator migration (core_→node_) at ship close.

## Test plan (I-4)
- **List A (red-build fixtures): EMPTY** — ② cleaned all test cfg keys (verified present-vs-absent).
- **List B (silent-ignore→assert-refuse): 1** — controller_test.cpp:24092-24110 (`confidence_freshness_tau=400.0` @ :24098) flip to assert the fault bit.
- **List C (NEW characterization): 6-7** — clean-load-no-fault (Class-51 non-vacuity guard) · unknown-top-key→fault+boot-abort · unknown-per-node-suffix→fault · out-of-range-idx semantics · the 2 brick keys→caught · backtest/hot-reload WARN-not-abort · the deleted-key tombstone-meets-refuse regression.

## Path forward
1. Operator signs off the 6 open calls.
2. **Adversarial re-gate** on THIS locked scope (substantive-amendment re-fire) — refute targets: F1 key-match mechanism (over/under-fire), F3 cadence completeness + EngineTUI-path-dead, F4 guard-now (.F.4e nearness / determinism-net-catches-walker), the read-side guard scope, atomic ordering.
3. Fold D-242..246 into the decision log + the corrected edit-set into the plan body; write the anti-pattern + spec seeds + DAG seams + debt homes.
4. Code the atomic ship → ⑤ V-class → ship-close.
