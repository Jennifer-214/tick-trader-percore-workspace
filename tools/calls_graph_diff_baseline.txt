# calls_graph_diff baseline — known legacy orphans that are EXPECTED
#
# These functions are called from PortfolioController.hpp (legacy single-core
# controller) but not from any sharded entrypoint. They look orphan-shaped
# to calls_graph_diff.sh, but they are NOT actually unused — controller_test.cpp
# (167+ references) and main.cpp's legacy single-core path still exercise
# them. Removal would require migrating the test suite + main.cpp's legacy
# path, which is its own multi-day sprint, separate from any current ship.
#
# Lines starting with # or blank are ignored.
#
# Policy (per Jennifer 2026-05-01): never remove code without a replacement.
# This file is the policy in action — orphans documented as expected, not
# removed, until a deliberate test-migration ship lands.
#
# When the test suite migrates to the sharded substrate (post-v5.10?), this
# file shrinks toward empty. New entries should NEVER be added by routine
# work — they signal a real Class 1 (strategy lifecycle orphan) bug.

# Legacy strategy _BuySignal — old single-core path's gate-conditions setter.
# Sharded path uses _BuildParameters (returns GateParameters) instead.
EmaCross_BuySignal
MeanReversion_BuySignal
MLStrategy_BuySignal
Momentum_BuySignal
SimpleDip_BuySignal

# Legacy strategy _ExitAdjust — old single-core trailing logic.
# Sharded path uses _ExitAdjustSharded which routes through Strategy_WriteRatchetSL.
EmaCross_ExitAdjust
MLStrategy_ExitAdjust
Momentum_ExitAdjust

# Legacy MLStrategy_Adapt — uses const void* cfg (include-cycle workaround).
# Sharded path skips Adapt for ML (model weights don't change at runtime).
# When test suite migrates, this can be deleted along with the void* signature.
MLStrategy_Adapt

# Legacy regime adjustment — old per-position retune on regime transition.
# Sharded path uses Regime_AdjustPositionsSharded which writes ratchet_tp/sl.
Regime_AdjustPositions

# Dead-defined: legacy variant kept for backward compat in tests.
# Sharded uses MeanReversion_ExitAdjustSharded.
MeanReversion_ExitAdjust
