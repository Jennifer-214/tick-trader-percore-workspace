# v5.15.5.F.4d.1.B.3 — Legacy empty-out — plan body

**Branch:** `feat/v5.15-live-readiness`
**Predecessor:** `v5.15.5.F.4d.1.B.2` (cohort migration; shipped 2026-05-17 LOCAL; engine `9b62a72`; GPG-signed tag; push pending operator authorization)
**Pre-tag rollback anchor:** `pre-v5.15.5.F.4d.1.B.3` (create at Step 0 of coding)
**Successor:** `v5.15.5.F.4d.1.C` (sidecar override + bit-packed inventory)
**Sub-master:** `subplans/2026-05-16-v5.15.5.F.4d.1-thread-a-framework-full.md` v1.3
**Status:** **DRAFT v1.15 (2026-05-18) — FULL plan body re-synthesized per `/precoding-audit-gate` 6-audit fire against v1.14** (v1.15 expands Phase L (Decision G + Step 1.6.8') scope from "framework-call thin wrapper" → "full Tier B closure for cfg-stamp-binding surface" per `feedback_motivated_collaborator_for_caramel` + Caramel directive "time isn't an issue + most optimal solution for never-refactoring-again goal" 2026-05-18. Audit findings reframed: CRIT-1 (9-flag gap) is symptom of Class 21 at CLI INTERFACE LAYER — structural fix is **X-macro auto-gen of CLI flag table + value-receiver struct + parse dispatch from FOREACH_*_CFG_FIELD walkers** (sister discipline to engine's emit walker; eliminates manual flag-sync recurrence vector). v1.15 ALSO adds extensibility-test pattern (X-macro walker synthesizes value per flagged row + validates round-trip byte-identity) — eliminates the v5.14.1.B.3.E manual 17-field test block as recurrence vector. NEW DESIGN_SPEC `framework-driven-cli-binary-pattern.md` v1.0 → v1.1 (Audit detection section per pattern-codification-lifecycle.md + X-macro flag auto-gen + test pattern + stale FPN.hpp include fix). NEW DESIGN_SPEC `cfg-derived-consumer-framework.md` v1.2 → v1.3 (codifies extensibility test pattern as sister to "Adding a new consumer concern"). Other audit findings as one-time patches (CRIT-2 CMake 4-line; HIGH-2 forward-refs in 3 sister specs; HIGH-3 FPN.hpp → FixedPoint64.hpp; MED-1 4 stale doc sites; MED-2 README catalog). TECH_DEBT-111 NEW (CI defense-in-depth: tools/check_cli_flag_drift.py; LOW priority defer). Phase L net estimate: ~7-10h coding + ~2.5-3h amendment work pre-coding. **v1.14 → v1.15**)

**Status v1.14 (2026-05-18) superseded:** (v1.14 amendment landed Phase L NEW Decision G + Step 1.6.8' replacing v1.10 Step 1.6.8 bash-patches with framework-driven C++ CLI binary `tools/stamp_model_cli.cpp`. NEW DESIGN_SPEC `framework-driven-cli-binary-pattern.md` Stage 2 DRAFT v1.0 at workspace. Cross-refs landed: wire-format-byte-preservation-discipline.md § Layer 7 (structural-elimination cross-ref); decoupling-endgoal-roadmap.md breadcrumb; CLAUDE.local.md going-forward rule. TECH_DEBT-110 NEW (deprecation shim deletion target). TECH_DEBT-106 status amended (partial obviation by Phase L). Bug classes: Class 18/19/21/22 cross-tool surface closures added. Canonical sister inspection extended (5 new candidates). **v1.13 → v1.14**)

**Status v1.12 (2026-05-18) superseded:** (v1.12 incorporates Meta-gap M4 codification mid-coding per `DOCS/DESIGN_PHILOSOPHY.md` § 11.5: NEW DESIGN_SPEC `implementation-layer-blindspot-taxonomy.md` (Stage 2 DRAFT v1.0; 12 categories B1-B12) + NEW skill `/blindspot-scan` (Layer-2 audit) + NEW feedback memory `feedback_implementation_detail_blindspot_recovery_via_taxonomy` + 4 skill amendments (`/precoding-audit-gate` extended audit_set / `/readiness` Checks 36-39 / `/trace-deps` TYPE-SENSITIVE classification / `/parity-check` Section M + N) + 2 NEW CI tools (`check_field_name_uniqueness.py` + `check_storage_t_coverage.py` both PASS at HEAD) + META program restructure (CLAUDE.md Purpose-driven; CLAUDE.local.md tightened index + meta-disciplines pointer; DESIGN_PHILOSOPHY § 11.5 added). First canonical `/blindspot-scan` fire returned YELLOW — 3 amendments (B3 transitional / B8 TYPE-SENSITIVE / B9 cfg_drift_compare claim correction) needed in Step 1.6.3; 1 annotation needed in Step 1.6.4 (row-ordering per Check 39). NEW Step 9 ship-close auto-write expansion captures META + Phase A deliverables. TECH_DEBT-108 CLOSED (H4-compliant exemption) + TECH_DEBT-109 NEW (skill drift audit; refined scope). **v1.11 → v1.12**)

**Status v1.11 (2026-05-18) superseded:** (v1.11 incorporates 3-audit synthesis after Session D framework primitives landed: BUILD-FORCED sequence RE-ORDERED per /readiness CRIT — Step 1.5 moved from BEFORE 1.6.3 to AFTER (depends on unprefixed inf fields from Step 1.6.3 Approach A struct-gen + Step 1.6.2 bit-add); 9→15 key scope alignment per /parity-check M-4 (Decision F + Step 1.6.7.4 + fixture); Step 1.6.4.a regression-lock semantic clarification per /parity-check M-2; Step 1.6.7.3 + Step 1.6.4 + Step 1.6.8 "MUST land in same commit" clauses per /parity-check M-3; Step 0.5d.b/c/d deferral notes per /readiness HIGH; Step 9 TECH_DEBT-107 entry; NEW "Coding-time discoveries Session D" section. **v1.10 → v1.11**)
**v1.10 supersedes:** (v1.10 closes 3 meta-gaps surfaced at v1.9 RE-SWEEP — Batch 2 audit fire against v1.9 caught 2 NEW CRIT + 2 NEW HIGH + bookkeeping. **M1 NEW META-GAP — sister-registry parity/migration discipline** (caught FOREACH_GATE_CFG_FLAG 5→6 col gap; codified at `canonical-sister-extension-discipline.md` § Temporal evolution; ADD Step 0.5d.a.0 BEFORE Step 0.5d.a walker extension). **M2 NEW META-GAP — cross-tool emit-site enumeration discipline** (caught stamp_model.sh:221 hardcoded `stamp_format_version=1` literal + line 244 orphan `inference_cfg_freshness_tau`; codified at `wire-format-byte-preservation-discipline.md` § Layer 7; EXPANDED Step 1.6.8 enumeration to include all wire-format literals + per-site disposition + cross-ref comments). **M3 NEW META-GAP — anti-pattern codification false-positive surface** (caught Class 32 text could conflate "legitimate namespacing prefix" with "duplicate-emit asymmetry"; codification refined at ship close with explicit "False-positive surface" subsection). Decision D scope STILL 15 entries (lines 101/105 stale-9 references CORRECTED to match v1.6 header at line 80). v1.9 closed operator-tooling enumeration gap (Step 1.6.8 stamp_model.sh ~30 min mechanical). v1.8 closed consumer-enumeration gap (149 sites). v1.7 closed 2 material findings. v1.6 closed TECH_DEBT-104. v1.5 closed Class 32 codification + Decision D 9→10. v1.4 closed 5 readiness gaps. v1.3 incorporated 4 Caramel pushbacks + Decision F. v1.2 first canonical NEW plan body.) (v1.10 closes 3 meta-gaps surfaced at v1.9 RE-SWEEP — Batch 2 audit fire against v1.9 caught 2 NEW CRIT + 2 NEW HIGH + bookkeeping. **M1 NEW META-GAP — sister-registry parity/migration discipline** (caught FOREACH_GATE_CFG_FLAG 5→6 col gap; codified at `canonical-sister-extension-discipline.md` § Temporal evolution; ADD Step 0.5d.a.0 BEFORE Step 0.5d.a walker extension). **M2 NEW META-GAP — cross-tool emit-site enumeration discipline** (caught stamp_model.sh:221 hardcoded `stamp_format_version=1` literal + line 244 orphan `inference_cfg_freshness_tau`; codified at `wire-format-byte-preservation-discipline.md` § Layer 7; EXPANDED Step 1.6.8 enumeration to include all wire-format literals + per-site disposition + cross-ref comments). **M3 NEW META-GAP — anti-pattern codification false-positive surface** (caught Class 32 text could conflate "legitimate namespacing prefix" with "duplicate-emit asymmetry"; codification refined at ship close with explicit "False-positive surface" subsection). Decision D scope STILL 15 entries (lines 101/105 stale-9 references CORRECTED to match v1.6 header at line 80). v1.9 closed operator-tooling enumeration gap (Step 1.6.8 stamp_model.sh ~30 min mechanical). v1.8 closed consumer-enumeration gap (149 sites). v1.7 closed 2 material findings. v1.6 closed TECH_DEBT-104. v1.5 closed Class 32 codification + Decision D 9→10. v1.4 closed 5 readiness gaps. v1.3 incorporated 4 Caramel pushbacks + Decision F. v1.2 first canonical NEW plan body.)
**Audit reports informing this plan body (Batch 1 — against v1.2):**
- `plan_checks/parity-check-2026-05-17-v5.15.5.F.4d.1.B.3.md` (YELLOW; 1 CRIT + 3 HIGH)
- `plan_checks/trace-deps-2026-05-17-v5.15.5.F.4d.1.B.3.md` (YELLOW; 3 CRIT + 6 HIGH)
- `plan_checks/readiness-2026-05-17-v5.15.5.F.4d.1.B.3.md` (YELLOW; 4 HIGH; Check 29/30 PASS)
- `plan_checks/dod-audit-2026-05-17-v5.15.5.F.4d.1.B.3.md` (YELLOW; 2 HIGH)
- `plan_checks/merge-scan-2026-05-17-v5.15.5.F.4d.1.B.3.md` (GREEN; Decision E recommendation)
- `plan_checks/accounting-audit-2026-05-17-v5.15.5.F.4d.1.B.3.md` (YELLOW; 1 CRIT + 3 HIGH)

**Audit reports informing this plan body (Batch 2 RE-SWEEP — against v1.9):**
- `plan_checks/parity-check-2026-05-17-v5.15.5.F.4d.1.B.3-v1.9-resweep.md` (YELLOW; 1 NEW CRIT + 2 NEW HIGH + 1 MED + 1 LOW)
- `plan_checks/trace-deps-2026-05-17-v5.15.5.F.4d.1.B.3-v1.9-resweep.md` (GREEN; 1 NEW MED + 1 LOW — inflection signal)
- `plan_checks/readiness-2026-05-17-v5.15.5.F.4d.1.B.3-v1.9-resweep.md` (YELLOW; 1 NEW HIGH + 3 NEW MED + 3 NEW LOW — bookkeeping-class)
- `plan_checks/dod-audit-2026-05-17-v5.15.5.F.4d.1.B.3-v1.9-resweep.md` (GREEN; 3 LOW — inflection reached)
- `plan_checks/merge-scan-2026-05-17-v5.15.5.F.4d.1.B.3-v1.9-resweep.md` (GREEN; 2 minor candidates DEFERRED)

**Pre-coding audit synthesis (Batch 1):** `plan_checks/2026-05-17-v5.15.5.F.4d.1.B.3-audit-synthesis.md`
**Pre-coding audit synthesis (Batch 2 RE-SWEEP):** `plan_checks/2026-05-17-v5.15.5.F.4d.1.B.3-v1.9-resweep-synthesis.md`

---

## Why this ship exists

`.B.1` shipped framework infrastructure (`CfgGateRegistry.hpp` + 3 consumer template fns + `tt::` dispatch quartet → septet; walker iterated 0 rows). `.B.2` shipped cohort migration (24 fields flag `STAMP_BOUND_CFG_DERIVED` + framework activated across 3 cohort registries + Path γ #3 PARTIAL closure via 6 `COHORT_GATE_*` macros + FPN<F> 6 comparison operators codebase-wide + Winsor parse-time validation + 22 new tests). At `.B.2` close, legacy registries are STAGED for deletion: framework walker wired but production wire-format still flows through `FOREACH_STAMP_BOUND_CFG`.

`.B.3` is the FINALIZATION ship of `.F.4d.1.B` split. This ship:

- **Forces the switch** at production walker sites: 4 ModelInference.hpp sites (`:1199`, `:1401`, `:1643`, `:1788`) + 1 drift walker (`CoreModelZoo.hpp:243`) + 2 production inf-populate calls + 8 test sites
- **Deletes legacy registries** entirely: `FOREACH_STAMP_BOUND_CFG` body + `FOREACH_CFG_DERIVED_INFERENCE_CFG` body + file + **15 prefixed POST_CFG entries per v1.6 scope** (9 cohort + 1 standalone bandit_blend_ratio + 5 v1.6 model-state for Class 32 FULL closure)
- **Closes 8 LOAD-BEARING TECH_DEBT-09X items** + 4 NEW opened (TECH_DEBT-101 parser back-compat deprecation; TECH_DEBT-102 Class 31 detection mechanism; TECH_DEBT-103 locale-pin optimization; TECH_DEBT-104 surviving Class 32 model-state instances)
- **Bumps `stamp_format_version` 1 → 2 SOFT** (parser dual-recognizes v1 + v2 wire keys; emit produces v2 only; NO operator action required for existing v1 stamps)
- **Closes 6 bug classes STRUCTURALLY**: Class 11 (Extensibility friction) + Class 12 (Wired-but-unexercised ML paths) + Class 14 (Plan calls struct field that doesn't exist) + Class 18 (Mirror state/code at inf struct + wire-format-emit + double-emit surfaces) + Class 21 (Multiple parallel descriptors) + Class 24 (Capability-cfg surface mismatch for per_horizon_barrier_blend)
- **Codifies 2 NEW anti-pattern classes** in RECURRING_BUG_PATTERNS.md: Class 31 (Wire-format duplicate-key emit from sister registries; 10 instances closed at this ship) + Class 32 (Prefixed/unprefixed struct field mirror in X-macro auto-gen registry; 10 instances closed; ~5 sibling instances at model-state cohort tracked TECH_DEBT-104)
- **Validates v5.14 stamp fixture** load semantic under SOFT bump (existing v1 stamps continue loading on `.B.3+` engine)
- **Lands CI Checks 9-12** (STAMP_BOUND_CFG_DERIVED coverage via compile-time static_assert per `/dod-audit` MED-3)

After `.B.3` ships, framework consolidation phase is STRUCTURALLY COMPLETE. `.C` (sidecar override + bit-packed inventory) and `.D` (CI verification + fixture regression) round out `.F.4d.1` umbrella.

**Caramel's professionalization-phase context:** per `user_mvp_to_professional_transition` + `user_deep_design_work_intrinsic_value`. After `.B.3`, adding a new cfg field = 1 row in master registry. Framework discipline at cfg/stamp/drift surface locked.

---

## Design space + future-oriented choice

Per `future-oriented-plan-template.md` Stage 3 ACTIVE. Each option evaluated on Robustness / Latency / Design alignment / Future-easier / **Operator migration impact** (NEW column per Caramel pushback #4).

### Decision A — `gap_acceptable_threshold` cleanup at `.B.3` with Path α 12-col cascade

**Context:** at `.B.2`, master registry row + GUI render added; manual decl at `ControllerConfig.hpp:889`, default at `:1729`, parser at `:2555` LEFT IN PLACE (coding-time Discovery 8: `FOREACH_GLOBAL_CFG_FIELD` doesn't auto-gen struct fields — different from `FOREACH_PER_CORE_CFG_FIELD` which auto-gens `PerCoreCfg<F>` per H17). H17 invariant covers per-core only.

| Option | Description | Robustness | Latency | Design | Future-easier | Operator migration | Verdict |
|---|---|---|---|---|---|---|---|
| **(a) Path α** | 12-col cascade: add STORAGE_T column to all 47 FOREACH_GLOBAL_CFG_FIELD rows; ControllerConfig<F> auto-gens cfg fields | Closes H17 violation; eliminates 3-site manual storage; column symmetry with per-core | UNTOUCHED (boot-time only) | ✓ H17 uniform across registries | YES — future global cfg fields auto-gen mechanically | NONE | **CHOSEN** |
| (b) Path β if-constexpr | Keep 11-col; use if-constexpr in struct-gen template fn | Patches per-row; doesn't generalize | UNTOUCHED | ✗ if-constexpr in struct decl context doesn't work | NO | NONE | rejected — mechanism gap |
| (c) Defer to `.F.4f` | Patch symptom; H17 violation persists | UNTOUCHED | ✗ H17 asymmetry | NO | NONE | rejected — `feedback_no_defer_for_effort` |

**Auto-pick rationale:** (a) Path α per `feedback_motivated_collaborator_for_caramel`. 47-row cascade is real work but eliminates H17 asymmetry permanently. Sister to existing PerCoreCfg<F> auto-gen mechanism per `universal-cfg-field-registry-pattern.md`. Effort: ~3-4h (per `/trace-deps` HIGH-1; v1.2's ~50-100 LOC estimate was Path β-biased).

### Decision B — Extend framework `drift_check_from_derived` with `reason_buf + reason_cap` args

**Context:** legacy drift walker at `CoreModelZoo.hpp:225-247` sets `sr.reason` on first drift (operator-visible error message). New framework `drift_check_from_derived` at `CfgGateRegistry.hpp:315+` uses mask-select `failure_flags` per H20 branchless. No reason buffer in current signature.

| Option | Description | Robustness | Latency | Design | Future-easier | Operator migration | Verdict |
|---|---|---|---|---|---|---|---|
| **(a)** | Extend framework with `char* reason_buf, size_t reason_cap` + first-drift attribution per `failure-attribution-buffer-pattern.md` Stage 2 DRAFT | Preserves operator UX | UNTOUCHED (slow-path) | ✓ framework primitive; codifies existing 3-recurrence pattern | YES — future drift consumers + parser/scaler attribution surfaces reuse | NONE | **CHOSEN** |
| (b) | Accept behavior change — drift detected via failure_flags only | UX degrades to "drift detected" without WHICH field | UNTOUCHED | ✗ erodes operator UX | NO | NONE | rejected |

**Auto-pick rationale:** (a). Reason text is LOAD-BEARING for live operator UX. Drift detection at model load is critical failure mode; operator needs WHICH field. ~30 LOC framework extension. Stage 2 DRAFT spec written at audit synthesis time per `failure-attribution-buffer-pattern.md`.

### Decision C — Approach A unconditional struct-gen (REVERTED from reactive Approach B per Caramel pushback #1)

**Context:** at HEAD, struct fields auto-generated via X-macro walk over `FOREACH_STAMP_BOUND_CFG` (`ModelInference.hpp:1196-1199` for `ModelStampResult` parser side; `:1640-1643` for `StampInferenceCfgInputs` emit side). After Step 2 deletes legacy registry, struct-gen needs to walk master cfg registries.

| Approach | Description | Robustness | Latency | Design | Future-easier | Operator migration | Verdict |
|---|---|---|---|---|---|---|---|
| **(A)** Unconditional struct-gen | Generate `uint8_t has_<name>; T <name>;` for ALL master per-core + global rows (no preprocessor metadata filter) | Closes Class 14/18/21 trio; struct holds all fields | UNTOUCHED (struct size grows ~5KB across both structs; .bss only) | ✓ matches H17 PerCoreCfg<F> precedent; aligned with `metadata-bit-driven-derived-filter-framework.md` § Option E | YES — adding new flagged row = 1 master row; struct auto-includes | NONE | **CHOSEN (REVERTED from reactive B)** |
| (B) Macro-level filter | `FOREACH_<X>_FILTER_BY_STAMP_BOUND_CFG_DERIVED(X)` parallel-mirror macro | Class 21 anti-pattern (parallel-mirror registry per `metadata-bit-driven-derived-filter-framework.md` § Option A explicit rejection lines 34-44) | UNTOUCHED | ✗ Class 21 introduction | NO — every new bit needs new FILTER X-macro | NONE | **REJECTED — Class 21 anti-pattern** |
| (C) Defer | Keep legacy struct-gen alive via workaround | Patches symptom | UNTOUCHED | ✗ erodes consolidation | NO | NONE | rejected — `feedback_no_defer_for_effort` |

**Auto-pick rationale:** (A) per `metadata-bit-driven-derived-filter-framework.md` § Option E + H17 PerCoreCfg<F> precedent. My v1.2 reactive flip to Approach B was caught by Caramel pushback #1 as Class 21 anti-pattern; REVERTED. Approach A is structurally clean; bounded .bss cost (~5KB across both structs); char[N] handling via Step 0.5c forward-compat primitive extension to `tt::cfg_parse_field<T>` (sister to `.F.4e` KIND_STRING work).

### Decision D — Delete 15 prefixed POST_CFG entries (scope EXPANDED 5 → 9 → 10 → 15 per Caramel "reduce future headache" directive)

**v1.6 scope correction:** scope is 15 entries (was 10 at v1.5). 5 additional entries added per Caramel directive closing TECH_DEBT-104 at `.B.3`:

**Original 10 (v1.5):**
1-9. POST_CFG section at `StampBoundModelConstRegistry.hpp:454-483` (9 cohort entries: ml_tp_pct, ml_sl_pct, barrier_blend_mode, per_horizon_barrier_blend, bandit_algorithm, thompson_mu_prior, thompson_precision_prior, thompson_precision_obs, thompson_exp3_blend_alpha)
10. Standalone `inference_cfg_bandit_blend_ratio` at `:296-297`

**5 ADDITIONAL at v1.6 (Class 32 full closure):**
11. `inference_cfg_confidence_threshold_scale` at `:281-282` — master per-core source at `CfgFieldRegistry.hpp:538` (bit-add via Step 1.6.2)
12. `inference_cfg_barrier_gate_enabled` at `:283-284` — bitmap-flag source at `cfg.gate_cfg_flags` (needs Step 0.5d framework walker extension for `FOREACH_GATE_CFG_FLAG` + bit-add via Step 1.6.2)
13. `inference_cfg_confidence_hard_block_threshold` at `:285-286` — master per-core source at `CfgFieldRegistry.hpp:644` (bit-add via Step 1.6.2)
14. `inference_cfg_fee_rate_maker` at `:299-300` — master per-core source at `CfgFieldRegistry.hpp:660` (bit-add via Step 1.6.2)
15. `inference_cfg_fee_rate_taker` at `:301-302` — master per-core source at `CfgFieldRegistry.hpp:661` (bit-add via Step 1.6.2)

After v1.6: Class 32 FULL closure (no surviving instances); TECH_DEBT-104 NOT opened.

**Context:** at HEAD `.B.2`, double-emit Class 18 mirror is LIVE: thompson cohort fields emit BOTH `bandit_algorithm=N` (FOREACH_STAMP_BOUND_CFG walker) AND `inference_cfg_bandit_algorithm=N` (FOREACH_STAMP_BOUND_MODEL_CONST_POST_CFG walker introduced at `.F.4d` PARITY-026 close). `/trace-deps` CRIT-3 verified live double-emit.

| Mechanism | Description | Wire format impact | Robustness | Verdict |
|---|---|---|---|---|
| **(1)** | DELETE 15 prefixed POST_CFG entries per v1.6 scope expansion (5 thompson cohort + 4 retroactive `.A.7` cohort + 1 standalone bandit_blend_ratio + 5 model-state cohort for Class 32 FULL closure: confidence_threshold_scale + barrier_gate_enabled + confidence_hard_block_threshold + fee_rate_maker + fee_rate_taker); framework walker emits unprefixed names | Wire keys change `inference_cfg_<name>=` → `<name>=`; ordering changes (master-registry order) | Couples with Decision F SOFT version bump; closes Class 18 mirror + Class 32 FULL | **CHOSEN** |
| (2) Add unprefixed sister fields | Both keys emitted | Adds wire bytes; HMAC chain regression | Class 18 mirror PRESERVED | rejected |
| (3) Rename via search-replace | Same as (1) | Risk: substring replace_all mangles `inf->has_inference_cfg` per `feedback_avoid_substring_replace_all_on_member_access` | Error-prone | rejected |

**Auto-pick rationale:** mechanism (1). Scope EXPANDED to **15 entries** per v1.6 Caramel "reduce future headache" directive (was 9 at audit synthesis CRIT-CONV-2; expanded 9→10 at v1.5, 10→15 at v1.6 for Class 32 FULL closure). Couples with Decision F (F.2) SOFT compat — parser back-compat layer at parse side preserves load semantic for v1 stamps with legacy prefixed keys.

### Decision E — CfgDriftCheck partial consolidation (E.2)

**Context:** per `.B.2` postmortem Discovery 7 — `CfgDriftCheckRegistry` at `ML_Headers/CfgDriftCheckRegistry.hpp:194-322` has DIFFERENT bandit gate semantic than `FOREACH_STAMP_BOUND_CFG`. `/merge-scan` walked all 18 rows: **4 SAME semantic** (substitutable with COHORT_GATE_*), **14 DIFFERENT semantic** (preserve as-is).

| Option | Description | Sites eliminated | Behavior change | Verdict |
|---|---|---|---|---|
| (E.1) | Leave separate; Path γ #3 stays PARTIAL | 0 | NONE | rejected — underpicks |
| **(E.2)** | Migrate 4 substitutable rows to shared COHORT_GATE_* macros; preserve 14 distinct-semantic rows | 4 | NONE | **CHOSEN** |
| (E.3) | Migrate CfgDriftCheck entirely to framework consumer | ~18 | Operator-visible bandit-boundary semantic shift; potential `cfg_binding_drift` taint | rejected — overpicks |

**Auto-pick rationale:** (E.2) per convergent 3-audit recommendation (`/merge-scan` + `/dod-audit` LOW-1 + `/accounting-audit` HIGH-2). Per `categorical-tag-applicability-pattern.md` — the bandit boundary's `BITMAP_IS_SET(BANDIT_ENABLED)` vs `(bandit_algorithm != 0)` is canonical applicability-vs-runtime-gating orthogonality, NOT Class 18 mirror. (E.2) closes Path γ #3 PARTIAL → MOSTLY (3-of-3 cohort cells unified for substitutable subset; 14-row exemption documented).

### Decision G — Framework-driven C++ CLI binary replaces `tools/stamp_model.sh` (NEW v1.14 amendment 2026-05-18; Phase L)

**Catalyst:** Honest re-evaluation prompted by Caramel pushback "are you sure the upcoming coding cycle is optimal for the goal of not having to worry about refactoring for maintenance again?" + memory `feedback_no_defer_for_effort` (3-instance pattern of my "smaller scope" recommendations being wrong) + memory `feedback_motivated_collaborator_for_caramel` (best-software discipline; pick path that produces lasting structural fix).

**Original (v1.10) Decision context:** Step 1.6.8 was scoped to apply Layer 7 § per-site disposition discipline to `tools/stamp_model.sh` (6 wire-key renames + 1 version literal bump + 1 orphan delete + 1 preserve-with-comment). This closes the IMMEDIATE wire-format drift at `.B.3` but preserves the recurrence vector — the next cfg field add with stamp-binding would require manual bash sync (the 7th+ event in a 6+ recurrence history).

**v1.14 evidence + decision:** Per `structural-fix-preferred-decision-framework.md` § Step 2:
- `tools/stamp_model.sh` recurrence count: 6+ cross-tool sync events documented in script header (v5.2.3 / v5.8.8 / v5.9.3b / v5.9.4a / v5.9.5c / v5.11.18a + the .B.3 batch)
- 4+ occurrences = STRUCTURAL FIX MANDATORY
- Script header line 12 itself documents (since v5.2.3): "Phase 2 (v5.3.x?) replaces this with `tools/stamp_model.cpp` that runs validation directly from CLI" — Phase 2 ~3 major versions overdue

**Decision (per Caramel "let's get these as structural fixes that will last a while" + "time isn't an issue + most optimal solution"):**
- **Replace `tools/stamp_model.sh` with `tools/stamp_model_cli.cpp`** — framework-driven C++ CLI binary that calls existing `stamp_write_for_model` API directly **AND auto-generates its CLI flag table + value-receiver struct + parse dispatch from FOREACH_*_CFG_FIELD walkers via X-macro discipline**. Eliminates Class 21 at BOTH wire emit AND CLI INTERFACE layers (vs v1.14's wire-emit-only scope).
- **Pattern codified at NEW `framework-driven-cli-binary-pattern.md` v1.1** (lands pre-Phase-L coding at workspace `DESIGN_SPECS/`; Stage 2 DRAFT extended at v1.15 amendment per audit findings):
  - Wire emit via framework call (v1.0 scope)
  - **NEW v1.1: X-macro auto-gen flag table** (longopts[] auto-generated from FOREACH_PER_CORE_CFG_FIELD + FOREACH_GLOBAL_CFG_FIELD + FOREACH_ML_CFG_FLAG + FOREACH_GATE_CFG_FLAG filtered by STAMP_BOUND_CFG_DERIVED + FOREACH_STAMP_BOUND_MODEL_CONST_PRE_CFG + POST_CFG unfiltered)
  - **NEW v1.1: X-macro auto-gen value-receiver struct + per-flag parse dispatch via tt::cfg_parse_field**
  - **NEW v1.1: Extensibility test pattern** (X-macro walker synthesizes value per flagged row + runs stamp emit/parse + validates round-trip byte-identity per row — adding a new flagged row = test auto-validates it)
  - **NEW v1.1: `## Audit detection` section** per pattern-codification-lifecycle.md discipline
- **Sister DESIGN_SPEC amendment**: `cfg-derived-consumer-framework.md` v1.2 → v1.3 — codifies extensibility test pattern as sister to "Adding a new consumer concern" section (X-macro test walker is registry-agnostic; applies to ANY cfg-derived consumer cohort)
- **Stage 3 first canonical** = this ship's Phase L `tools/stamp_model_cli.cpp`

**v1.14 scope was incomplete (caught by /precoding-audit-gate 6-audit fire 2026-05-18):**

v1.14 Phase L scope closed the WIRE EMIT seam (CLI calls framework → no bash mirror). But /trace-deps + /blindspot-scan independently flagged that v1.14 L2's "23 explicit CLI flags" was a parallel descriptor of FOREACH_PER_CORE_CFG_FIELD — manual sync of CLI longopts[] is itself Class 21 at the INTERFACE LAYER. Operator's existing XGBoost training workflow (using --xgb-* flags from bash) would have broken silently. The v1.15 re-synthesis catches this: structural fix is X-macro auto-gen, NOT manual 9-flag-addition patch.

**Closes (cross-tool + CLI interface surfaces):**
- Class 18 (mirror state/code) at WIRE EMIT layer (v1.14 scope) + at CLI INTERFACE LAYER (v1.15 scope; longopts[] auto-gen)
- Class 19 (hardcoded instance names) at bash wire keys (v1.14) + at CLI flag names (v1.15; flag names = registry names by construction)
- Class 21 (multiple parallel descriptors) at wire emit (v1.14) + at CLI flag table (v1.15)
- Class 22 (runtime cfg gating scattered) at bash inline gating (v1.14) — CLI inherits framework's centralized gating
- **NEW: cfg-derived-consumer test recurrence vector** (manual 17-field round-trip test block in controller_test.cpp v5.14.1.B.3.E) — closed by extensibility test pattern (X-macro walker validates all flagged rows)

**Lasting impact (the never-refactor-again acceptance criteria):**

Adding a NEW cfg field with stamp-binding post-Phase-L requires:
1. Add 1 row to FOREACH_PER_CORE_CFG_FIELD (or sibling) with STAMP_BOUND_CFG_DERIVED bit
2. (Optional) Add 1 row to FOREACH_CFG_GATE_*_GLOBAL sidecar if gate semantic differs from default
3. **DONE**

What auto-flows (no manual sync):
- ✓ C++ struct fields (StampInferenceCfgInputs + ModelStampResult) — via STAMP_RESULT_DERIVED_FIELDS_AUTO_GEN
- ✓ Wire emit (canonical body for HMAC) — via populate_stamp_cfg_from_derived
- ✓ Wire parse (verify_model_stamp) — via parse_stamp_cfg_to_derived
- ✓ Drift check (CoreModelZoo + EnsembleHotSwap) — via drift_check_from_derived
- ✓ Inf populate (Stamp_AssembleAndEmit) — via populate_inference_cfg_from_derived
- ✓ Cfg parser + GUI render + CLI-cfg-render — via FOREACH_PER_CORE_CFG_FIELD walkers
- ✓ **CLI flag `--<name>` (appears in tools/stamp_model_cli)** — via X-macro auto-gen longopts[]
- ✓ **CLI value-receiver field + parse dispatch** — via X-macro auto-gen + tt::cfg_parse_field
- ✓ **Round-trip test validation** — via X-macro extensibility test walker

**ZERO manual sync work for cfg-stamp-binding surface post-Phase-L.** Truly never-refactor-again at this surface.

**Effort delta from v1.14 scope:** v1.14 estimated Phase L ~4-6h. v1.15 expanded Phase L ~7-10h (+3-4h for X-macro flag auto-gen + extensibility test pattern + DESIGN_SPEC extensions). Per `feedback_motivated_collaborator_for_caramel` + Caramel directive: time isn't an issue; optimal solution is the right call.

**Sub-steps (L1-L8 expanded):** L1 (DESIGN_SPEC v1.0 → v1.1 amendment) / L2 (CLI binary with X-macro auto-gen) / L3 (CMakeLists 4-line build target) / L4 (verification tests + extensibility test + Layer 5b CLI emit) / L5 (deprecation shim) / L6 (cross-ref updates: 3 sister specs + README) / L7 (cfg-derived-consumer-framework.md v1.2 → v1.3 extensibility test pattern section) / L8 (TECH_DEBT-111 NEW CI defense-in-depth defer) — detailed at Step 1.6.8' below.

### Decision F — SOFT compat parser dual-recognition (NEW per Caramel pushback #4)

**Context:** `/parity-check` CRIT-1 + `/accounting-audit` HIGH-1 framed wire-format change as binary STRICT vs LENIENT. Caramel pushback #4 ("ideally we would never have to use [restamp_model.sh]") exposed the design gap.

| Option | Description | Operator action | Wire-format change | Verdict |
|---|---|---|---|---|
| (F.1) | HARD break — STRICT mode refuses v1 stamps; restamp_model.sh tool | Operator re-stamps/re-trains all production models | Hard | rejected per Caramel goal |
| **(F.2)** | SOFT compat — parser accepts v1 + v2 wire keys; emit v2 only; SOFT version bump 1→2 signals new EMIT format | NONE — v1 stamps continue loading | Soft (parser dual-recognition; emit transitions) | **CHOSEN** |
| (F.3) | Transition double-emit | NONE | Class 18 mirror at emit | rejected — Class 18 anti-pattern |

**Auto-pick rationale:** (F.2). Per `wire-format-byte-preservation-discipline.md` Layer 6 Surface G — parser tolerance for forward-compat is documented pattern; sister application (parser tolerance for back-compat closed-set legacy keys) is appropriate same-pattern. Eliminates operator migration burden entirely. ~40-50 LOC parser back-compat layer (closed set of **15 legacy prefixed keys per Decision D v1.6 scope expansion**; not recurring registry). Anti-pattern check: NOT Class 18 mirror at parser level (closed set + time-bounded; future TECH_DEBT entry tracks deprecation when all production v1 stamps gone). v1.11 amendment per /parity-check M-4: 9→15 key scope alignment to match Decision D v1.6 scope (was 9 at v1.3 Decision F draft; Decision D expanded 9→10→15 at v1.5/v1.6 but Decision F wording stayed stale until v1.11).

---

## Canonical sister registries considered

Per `canonical-sister-extension-discipline.md` § expanded verdict menu.

| Candidate sister | At HEAD | New artifact / migration | Verdict | Rationale |
|---|---|---|---|---|
| `FOREACH_GLOBAL_CFG_FIELD` struct-gen | doesn't auto-gen struct fields | Extend via sister mechanism to per-core PerCoreCfg<F> auto-gen | **EXTEND** | Sister has 95% structural overlap; same X-macro shape; same KIND_TOKEN dispatch (with STORAGE_T column add via Path α); same `tt::cfg_field<T>` discipline |
| `cfg_derived::drift_check_from_derived` signature | `MemHeaders/CfgGateRegistry.hpp:315+` | Extend with `char* reason_buf, size_t reason_cap` | **EXTEND** | Adds 2 args to existing fn; caller passes nullptr to skip; framework writes first-drift attribution if non-null |
| `FOREACH_STAMP_BOUND_CFG` | `ML_Headers/StampBoundCfgRegistry.hpp:99-179` | **DELETE entirely at `.B.3`** | **DELETE** | Framework walker fully replaces; ZERO production consumers post-Step 1.6.X migrations |
| `FOREACH_CFG_DERIVED_INFERENCE_CFG` | `MemHeaders/CfgDerivedInferenceCfgRegistry.hpp:101-123` | **DELETE entirely at `.B.3`** | **DELETE** | Framework walker fully replaces; ZERO production consumers post-Step 1.5 |
| `FOREACH_STAMP_BOUND_MODEL_CONST_POST_CFG` 9 prefixed `inference_cfg_*` entries + 1 standalone `inference_cfg_bandit_blend_ratio` at :296 | `ML_Headers/StampBoundModelConstRegistry.hpp:454-483` (POST_CFG section) + `:296-297` (standalone) | **DELETE 10** per Decision D mechanism 1 | **DELETE** | Framework walker emits unprefixed; deletion closes Class 18 (LIVE since `.F.4d` PARITY-026) + Class 31 (NEW; duplicate-key emit) + Class 32 (NEW; prefix asymmetry) instances |
| `FOREACH_STAMP_BOUND_MODEL_CONST` (parent registry) | Same file; sister registry | **NO-CHANGE** | **NO-FOLD (different concern)** | Model constants (scaler params, build flags hash, etc.) are NOT cfg-derived; only the 9 cfg-derived POST_CFG entries get pulled |
| `FOREACH_CFG_DRIFT_CHECK` | `ML_Headers/CfgDriftCheckRegistry.hpp:194-322` | Per Decision E (E.2) | **EXTEND PARTIAL** | 4 rows migrate to COHORT_GATE_*; 14 preserve distinct semantic per `categorical-tag-applicability-pattern.md` |
| `FOREACH_CFG_GATE_PER_CORE` + `_GLOBAL` | `MemHeaders/CfgGateRegistry.hpp` | Add sidecar entries if 5 prefixed-only fields have cohort gates differing from defaults | **EXTEND (sparse rows)** | Per Decision D mechanism 1, 5 prefixed-only fields flag STAMP_BOUND_CFG_DERIVED; cohort gates default `true` unless explicit entry |
| `FOREACH_ML_CFG_FLAG` `metadata_flags` column | `ML_Headers/MlCfgFlagRegistry.hpp` 6-arg sig | **EXTEND 1 row** — `PER_HORIZON_BARRIER_BLEND` metadata_flags `0` → `STAMP_BOUND_CFG_DERIVED` | **EXTEND** | TECH_DEBT-100 closes here; inf struct unification at Decision D enables |
| `STAMP_CFG_AUTOPOPULATE` macro | `StampBoundCfgRegistry.hpp:230` | **DELETE (with parent header)** | **DELETE** | Sister at framework: `INFERENCE_CFG_POPULATE_FROM_DERIVED` covers |
| `FOREACH_STAMP_BOUND_CFG_COUNT` constant | `StampBoundCfgRegistry.hpp:264` | **MIGRATE** to `cfg_field_count(g_per_core_cfg_stamp_bound_cfg_derived_mask) + cfg_field_count(g_global_cfg_stamp_bound_cfg_derived_mask)` popcount sum | **MIGRATE** | Per `.A`-shipped dual-mask helper; same value at HEAD `.B.2` |
| Parser back-compat for **15 legacy prefixed wire keys** (Decision F; v1.11 9→15 key scope alignment) | NEW closed-set X-macro at `ModelInference.hpp` parser | **NEW INLINE MERGE** (closed-set; not recurring registry) | **INLINE MERGE** | Per `canonical-sister-extension-discipline.md` § INLINE MERGE — small bounded back-compat layer; time-bounded deprecation tracked via TECH_DEBT entry |

**NEW v1.14 amendment — Phase L (Decision G) canonical sister inspection (5 candidates):**

| Candidate sister | At HEAD | New artifact / migration | Verdict | Rationale |
|---|---|---|---|---|
| `wire-format-byte-preservation-discipline.md` Layer 7 (cross-tool emit-site enumeration) | DESIGN_SPEC discipline-only | **NO-FOLD as structural fix** | **NO-FOLD** | Layer 7 codifies cross-tool sync as DISCIPLINE; doesn't eliminate the seam STRUCTURALLY. Phase L is the structural-fix sibling that obviates Layer 7 at framework-driven cross-tool surfaces. Layer 7 still applies for non-framework-driven cross-tool surfaces (e.g., bash diagnostic tools without C++ equivalent) |
| `registry-coverage-ci-check-pattern.md` (CI tooling structural-fix mechanism) | Python CI check pattern | **NO-FOLD** | **NO-FOLD** | A CI check (e.g., `tools/check_bash_stamp_parity.py`) could DETECT bash↔C++ drift but doesn't ELIMINATE the seam. Inferior structural close. **Acceptable as defense-in-depth POST-Phase-L** (e.g., post-migration CI verifies no NEW bash scripts emit wire format — defer to TECH_DEBT-106 narrowed scope) |
| `cfg-derived-consumer-framework.md` v1.2 | DESIGN_SPEC + framework infrastructure at `MemHeaders/CfgGateRegistry.hpp` | **EXTEND (consumer)** | **EXTEND** | New CLI binary IS a new consumer of the framework — same shape as engine + Stamp_AssembleAndEmit are consumers. CLI calls `stamp_write_for_model` (which uses `populate_stamp_cfg_from_derived` internally) directly. No new framework infrastructure required |
| `tools/compare_scalers.cpp` (existing standalone C++ tool in tools/) | C++ binary precedent | **EXTEND (sister tool)** | **EXTEND** | Existing C++ tool in `tools/` dir — sister precedent for C++ tool builds via CMake. `compare_scalers.cpp` doesn't use framework (different concern; scaler comparison). New `tools/stamp_model_cli.cpp` is 2nd C++ tool + 1st framework-driven C++ tool — establishes the canonical pattern |
| `framework-driven-cli-binary-pattern.md` (NEW spec) | does NOT exist at HEAD | **NEW Stage 2 DRAFT** (Stage 3 first canonical = `tools/stamp_model_cli.cpp`) | **NEW DRAFT** | Pattern not codified pre-`.B.3` v1.14; this ship lands Stage 2 DRAFT at workspace `DESIGN_SPECS/`; Stage 3 first canonical reference = Phase L `tools/stamp_model_cli.cpp`. Pattern body covers problem statement / design space α-ζ / pattern shape / build integration / verification / lifecycle |

**NEW v1.15 amendment — X-macro auto-gen at CLI INTERFACE LAYER (additional 6 canonical sisters per CRIT-1 restructure):**

| Candidate sister | At HEAD | New artifact / migration | Verdict | Rationale |
|---|---|---|---|---|
| `x-macro-registry-with-presence-dispatch.md` § Y3 dispatch | DESIGN_SPEC — token-paste presence dispatch pattern | **COMPOSE** | **COMPOSE** | X-macro auto-gen of longopts[] uses Y3 dispatch shape (token-paste registry name → emit longopts entry). Sister to existing FOREACH_STAMP_BOUND_CFG emit walker at `ML_Headers/StampBoundCfgRegistry.hpp`. No new dispatch mechanism — reuses existing |
| `autopopulate-pattern-for-production-caller-class.md` | DESIGN_SPEC — production-caller class extinction via X-macro AUTOPOPULATE companion | **COMPOSE** | **COMPOSE** | CLI flag table auto-gen IS the CLI's AUTOPOPULATE companion at the flag interface layer (vs existing AUTOPOPULATE at the value-population layer). Same N-site → 1-site closure shape. Adding new cfg field with stamp-binding = 1 row in master → CLI flag auto-appears (sister to has_* auto-set) |
| `cross-walker-struct-field-uniqueness-discipline.md` | DESIGN_SPEC + CI check at `tools/check_struct_field_uniqueness.py` | **COMPOSE** | **COMPOSE** | X-macro auto-gen of value-receiver struct (`struct CliArgs`) parallels existing struct-field-uniqueness discipline; sister CI check `tools/check_cli_flag_drift.py` (deferred to TECH_DEBT-111) shape borrows from `check_struct_field_uniqueness.py` |
| `tools/compare_scalers.cpp` flag parsing | Manual getopt loop (~6 flags); no X-macro auto-gen | **NO-FOLD (different scale)** | **NO-FOLD** | `compare_scalers` has small bounded flag set (6 flags; no registry mirror); X-macro auto-gen would be over-engineering at that scale. compare_scalers stays manual; Phase L's stamp_model_cli is FIRST canonical of X-macro auto-gen at CLI interface |
| Existing tt:: dispatchers (`tt::cfg_parse_field<T>` + `tt::cfg_populate_inf_field<T>` + `tt::cfg_emit_field<T>`) | At `CoreFrameworks/CfgFieldDispatch.hpp` | **COMPOSE** | **COMPOSE** | CLI parse dispatch uses existing `tt::cfg_parse_field<T>` for per-flag value parsing — same template that engine + Stamp_AssembleAndEmit use. Zero new framework infrastructure |
| Existing `cfg_derived::parse_stamp_cfg_to_derived<F>` (parser-side meta-walker) | At `MemHeaders/CfgGateRegistry.hpp:421` | **EXTEND (consumer)** | **EXTEND** | CLI's parse dispatch can REUSE this existing parser-side meta-walker directly OR have its own sister walker — depends on whether wire-key name == CLI flag name. At HEAD wire keys ARE the cfg field names (snake_case), so REUSE is possible. Decision deferred to L2 coding-time |

**Verdict (UPDATED v1.15; recount per /readiness LOW finding):** 23 candidates considered total (12 pre-v1.14 + 5 from v1.14 Phase L + 6 NEW v1.15 X-macro flag auto-gen sisters); 7 EXTEND; 4 COMPOSE; 4 DELETE; 1 EXTEND PARTIAL (Decision E); 1 MIGRATE (test fixture mechanical); 4 NO-FOLD (different concern OR Layer 7 discipline kept for non-framework surfaces OR scale-mismatch on compare_scalers + 1 v1.15 NO-FOLD on compare_scalers flag-parsing scale-mismatch); 1 INLINE MERGE (parser back-compat closed-set); 1 NEW DRAFT (`framework-driven-cli-binary-pattern.md` v1.0 → v1.1 amendment). Sum: 7+4+4+1+1+4+1+1 = 23 ✓

---

## Bug classes this ship closes (structural impact)

| Class | Title | Closure mechanism at `.B.3` |
|---|---|---|
| **Class 11** | Extensibility friction | STRUCTURAL — adding new cfg field = 1 row in master + framework auto-flows |
| **Class 12** | Wired-but-unexercised ML paths | STRUCTURAL — eliminates parallel walker prefixed/unprefixed duality |
| **Class 14** | Plan calls function/struct field that doesn't exist | STRUCTURAL FULL closure at cfg-derived consumer surface |
| **Class 18** | Mirror state/code | STRUCTURAL FULL closure at inf struct + wire-format-emit + active double-emit surfaces **+ NEW v1.14: STRUCTURAL FULL closure at cross-tool WIRE EMIT layer via Phase L (CLI calls framework)** **+ NEW v1.15: STRUCTURAL FULL closure at CLI INTERFACE LAYER via X-macro auto-gen longopts[] + value-receiver + parse dispatch (CLI flag table auto-generated from FOREACH_*_CFG_FIELD; no manual mirror)** **+ NEW v1.15: STRUCTURAL closure at TEST LAYER via extensibility test pattern (X-macro walker validates all flagged rows; v5.14.1.B.3.E manual 17-field block eliminated)** |
| **Class 19** | Hardcoded instance names in applicability gating | **NEW v1.14: STRUCTURAL closure at cross-tool WIRE EMIT layer via Phase L (bash wire keys eliminated)** **+ NEW v1.15: STRUCTURAL closure at CLI FLAG NAME layer via X-macro auto-gen (CLI flag names = registry field names by construction; no hardcoded flag strings)** |
| **Class 21** | Multiple parallel descriptors | STRUCTURAL FULL closure at cfg-derived stamp surface **+ NEW v1.14: STRUCTURAL FULL closure at cross-tool WIRE EMIT layer via Phase L (bash mirror eliminated)** **+ NEW v1.15: STRUCTURAL FULL closure at CLI INTERFACE LAYER via X-macro auto-gen (longopts[] + value-receiver + parse dispatch all auto-generated from FOREACH_*_CFG_FIELD walkers; no parallel descriptor at CLI binary)** |
| **Class 22** | Runtime cfg gating scattered in code paths | **NEW v1.14: STRUCTURAL closure at cross-tool surface via Phase L** (bash's inline cfg gating eliminated; CLI binary inherits framework's centralized gating via `cfg_gate::lookup_populate`) |
| **Class 24** | Capability-cfg surface mismatch | STRUCTURAL closure for per_horizon_barrier_blend (bit-add activates framework walker) |
| **Class 31 NEW** | Wire-format duplicate-key emit from sister registries | INSTANCE closure (10 POST_CFG entries deleted including standalone bandit_blend_ratio at :296); pattern CODIFIED at this ship; detection mechanism added to `/parity-check` Section |
| **Class 32 NEW** | Prefixed/unprefixed struct field mirror in X-macro auto-gen registry (column-1 wire-key name ≠ get_value source name) | **15 INSTANCES CLOSE FULLY at this ship (v1.6 scope expansion per Caramel "reduce future headache" directive)** via Decision D mechanism 1 (align column-1 to source name); ZERO surviving instances at cfg-derived stamp surface; pattern CODIFIED at ship close. Includes 4 master per-core cohort fields + 1 cfg-bitmap-flag field (via Step 0.5d framework walker extension for FOREACH_GATE_CFG_FLAG sister to FOREACH_ML_CFG_FLAG handling). |
| Path γ #3 | 3-way cohort gate predicate drift | PARTIAL → MOSTLY closure per Decision E (E.2); 4-row substitution to COHORT_GATE_* |

**Audit verification at ship close:** `/bug-check` CLEAN for Class 11/12/14/18/21/24 + Class 31 + Class 32 instances gone. `/parity-check` GREEN under SOFT v1/v2 compat. `/merge-scan` GREEN with documented (E.2) partial-with-exemption.

---

## DESIGN_SPECs landed/amended at this ship

**NEW (Stage 2 DRAFT exists on disk; Stage 3 first reference at `.B.3` ship close):**

- **`failure-attribution-buffer-pattern.md`** Stage 2 DRAFT — written 2026-05-17 at audit synthesis time. Stage 3 first reference: Decision B framework `reason_buf + reason_cap` extension at `CfgGateRegistry.hpp:315+`. Codifies existing 3-recurrence pattern (`ModelStampResult.reason` + `OverfitDetection.reason` + `Reconcile.refusal_reason`) + Decision B is 4th canonical.

- **`framework-driven-cli-binary-pattern.md`** Stage 2 DRAFT v1.0 (NEW v1.14 amendment 2026-05-18) **→ v1.1 amendment (v1.15 expansion)** — written pre-Phase-L coding at workspace `DESIGN_SPECS/`. Stage 3 first canonical reference at this ship's Phase L (Step 1.6.8') = NEW `tools/stamp_model_cli.cpp` (replaces `tools/stamp_model.sh` 6+ cross-tool sync recurrence count). **v1.0 covered:** problem statement + design space α-ζ explored / pattern concrete shape (thin C++ wrapper over framework API) / build system integration / deprecation shim discipline / verification approach / pattern lifecycle. **v1.1 NEW v1.15 sections per audit findings:** (a) X-macro auto-gen of CLI flag table (longopts[] + value-receiver struct + parse dispatch via tt::cfg_parse_field from FOREACH_*_CFG_FIELD walkers — eliminates Class 21 at CLI INTERFACE LAYER); (b) Extensibility test pattern (X-macro walker synthesizes value per flagged row + validates round-trip byte-identity per row); (c) `## Audit detection` section per pattern-codification-lifecycle.md discipline; (d) Fix stale include path `FixedPoint/FPN.hpp` → `FixedPoint/FixedPoint64.hpp` (per /trace-deps HIGH-3 finding). Sister discipline: `wire-format-byte-preservation-discipline.md` Layer 7 (codifies cross-tool sync discipline; this pattern STRUCTURALLY ELIMINATES at framework-driven surfaces). Closes Class 18 + 19 + 21 + 22 at WIRE EMIT layer + CLI INTERFACE layer + TEST layer.

- **`cfg-derived-consumer-framework.md`** v1.2 → v1.3 (NEW v1.15 amendment 2026-05-18) — codifies extensibility test pattern as sister to existing "Adding a new consumer concern" section. Pattern is registry-agnostic; applies to ANY cfg-derived consumer cohort. v1.3 amendment body: NEW section "Extensibility test pattern for cohort consumers" — X-macro walker shape + synthetic value generator helper + per-row validation + pattern shape with code sample. Stage 3 first canonical = Phase L's L4 extensibility test at `tests/controller_test.cpp` (replaces v5.14.1.B.3.E manual 17-field test block). Cross-ref to `framework-driven-cli-binary-pattern.md` v1.1. Future canonical: model-const cohort extensibility test (FOREACH_STAMP_BOUND_MODEL_CONST round-trip; defer to next ship that warrants it).

- **`canonical-sister-extension-discipline.md`** § "Temporal evolution + cohort migration of sister registries" — NEW v1.15: add forward-ref to `framework-driven-cli-binary-pattern.md` as the structural-fix sibling that obviates Layer 7 discipline at framework-driven surfaces. Cross-tool surface added as additional axis where the discipline applies.

- **`structural-fix-preferred-decision-framework.md`** § "Reference implementations" — NEW v1.15: add Phase L `tools/stamp_model_cli.cpp` (6+ recurrence; STRUCTURAL FIX MANDATORY) as canonical example of "Structural fix with new pattern (strategic moment)". Sister to v5.14.8.A.merged FOREACH_STAMP_BOUND_MODEL_CONST extraction precedent.

- **`DESIGN_SPECS/README.md`** catalog — NEW v1.15: add `framework-driven-cli-binary-pattern.md` entry under "Framework discipline patterns" subsection (per MED-2 audit finding).

**AMENDED (Stage update at this ship — mandatory per `.B.2` postmortem auto-write contract):**

- **`cfg-scope-discipline.md`** v1.X → v1.X+1 — NEW section "Override-inherit pattern" documenting global flat = base; per-core = override (0 = inherit, non-zero = override) per `.B.2` Discovery 5. Prevents future audit misread.

- **`wire-format-byte-preservation-discipline.md`** v1.X → v1.X+1 — NEW section "Procedure for SOFT wire-format changes via parser back-compat" per Decision F (F.2). Documents the canonical SOFT bump pattern: (1) extract literal to `STAMP_FORMAT_VERSION_CURRENT` constant; (2) add `MAX_SUPPORTED` parser bounds check (RELAXED — accept both versions); (3) bump CURRENT; (4) add parser back-compat strcmp branches for legacy wire keys (closed set); (5) emit new format only; (6) TECH_DEBT entry tracks eventual deprecation. Sister to Layer 6 Surface G discipline.

- **`cfg-derived-consumer-framework.md`** v1.1 → v1.2 (corrected — actual spec at HEAD is v1.1 ACTIVE, not v1.2; v1.10 plan body claim of v1.2 → v1.3 was a stale-version reference). v1.2 amendment scope:
  - NEW section "Production walker migration discipline" — `.B.3` first non-empty cohort walk lessons
  - NEW section "Inf struct unification" — Decision C Approach A struct-gen rationale
  - NEW section "Parser back-compat for legacy wire keys" — Decision F SOFT compat shape
  - **NEW section "Action-parameterized meta-walker for cohort consumer template fns" (v1.13)** — codifies `FOREACH_STAMP_BOUND_DERIVED_COHORT(BASE)` meta-walker shape + per-consumer X-macro naming convention (`X_<base>_<scope>` for scopes PER_CORE / GLOBAL / ML_CFG_FLAG / GATE_CFG_FLAG) + structural-enforcement explanation (drift impossible by construction); references STAMP_RESULT_DERIVED_FIELDS_AUTO_GEN as sister meta-walker (struct-gen unconditional)
  - **CLARIFY section "Adding a new consumer concern" line 155** — distinguish FOREACH-X-macro walker (compile-time field-name access for production value-extraction consumers via `tt::` dispatch; Class 23 avoidance) vs `CFG_FIELD_FOR_EACH_SET_BIT` (runtime idx access for GUI render / observability / diagnostic walks)
  - Cross-ref `feedback_prefer_action_parameterized_walker_over_per_consumer_walker_bodies.md`

- **`metadata-bit-driven-derived-filter-framework.md`** v1.2 → v1.3 — Body cleanup per `/parity-check` HIGH-1 + audit synthesis HIGH-CONV-G. REMOVE SUPERSEDED Option B macro descriptions (`DERIVED_FILTER_DECLARE_GUI/WIRE_FORMAT/WIRE_FORMAT_TWO_SOURCE`); replace with Option E canonical body text (`FOREACH_METADATA_BIT` + `cfg_compute_mask` + `CFG_FIELD_FOR_EACH_SET_BIT`). Banner-only revision was insufficient.

**Pre-coding requirement:** all NEW Stage 2 DRAFT specs on disk BEFORE pre-coding tag. `failure-attribution-buffer-pattern.md` DONE at synthesis time.

---

## Anti-pattern catalog cross-check (Classes 1-30 + new Class 31 + new Class 32)

Per Caramel pushback #3. Each v1.3 amendment verified against `DOCS/RECURRING_BUG_PATTERNS.md`.

| Class | v1.3 amendment status |
|---|---|
| 1 (Strategy lifecycle orphans) | N/A — no strategy changes |
| 2 (Display ↔ execution divergence) | N/A — no display changes |
| 3-7 | N/A |
| 8 (User-configurable features silently inactive in sharded) | CLEAN — per_horizon_barrier_blend bit-add activates framework walker in sharded mode (verify at coding) |
| 9-10 | N/A |
| 11 (Extensibility friction) | **CLOSES** |
| 12 (Wired-but-unexercised ML paths) | **CLOSES** |
| 13 (Worker-thread struct extended without snap-capture update) | N/A |
| 14 (Plan calls function/struct field that doesn't exist) | **CLOSES** at cfg-derived consumer surface |
| 15 (Function signature drift) | CLEAN — framework signature extension + per-call migration in same commit |
| 16 (Naming convention drift breaks X-macro dispatcher) | N/A — no new X-macro sig changes (FOREACH_ML_CFG_FLAG already at 6-arg from `.B.2`) |
| 17 (Architectural deferral made without grepping adjacent struct fields) | CLEAN — `.B.2` Discovery 6 documented; Decision D closes |
| 18 (Mirror state/code) | **CLOSES** at inf struct + wire-format-emit + active double-emit surfaces |
| 19 (Hardcoded instance names) | N/A |
| 20 (Bitmap field without overflow guard) | CLEAN — no new bitmap fields; ml_cfg_flags uint16_t has static_assert at HEAD |
| 21 (Multiple parallel descriptors) | **CLOSES** at cfg-derived stamp surface |
| 22 (Runtime cfg gating scattered) | CLEAN — COHORT_GATE_* macros from `.B.2`; (E.2) at `.B.3` extends to CfgDriftCheck |
| 23 (Type-erased reinterpret_cast) | CLEAN — uses `tt::` dispatch per H13 |
| 24 (Capability-cfg surface mismatch) | **CLOSES** for per_horizon_barrier_blend |
| 25 (Scope-erosion in per-core consumer) | CLEAN — `.B.2` Discovery 5 documented override-inherit (intentional) |
| 26 | (not allocated) |
| 27 (Scalar cfg-mirror cache) | CLEAN — no new mirror caches; reason_buf is caller-allocated; framework state-free |
| 28 (Branchy SP/HP dispatch) | CLEAN — boot/load-time only; H7/H20 unaffected |
| 29 (Silent zero-fee-rate from missing pre-resolution) | CLEAN — no fee-rate path touched |
| 30 (Sibling array on subsystem state without registry enrollment) | CLEAN — no new subsystem state arrays |
| **31 NEW (Wire-format duplicate-key emit from sister registries)** | **INSTANCE CLOSES** at this ship (9 POST_CFG entries deleted); pattern CODIFIED at ship close |

**NO new anti-pattern instances introduced.** 6 classes structurally close. 1 new class codified.

---

## Operator migration impact

**Under Decision F (F.2) SOFT compat:**

- Operator action required: **NONE**
- Existing v1 stamps continue loading on `.B.3+` engine (parser dual-recognition for **15 legacy prefixed wire keys** per Decision D v1.6 scope; v1.11 9→15 alignment per /parity-check M-4)
- Drift check fires correctly on both v1 and v2 stamps
- New stamps emit v2 format only (unprefixed keys, master-registry declaration order)
- restamp_model.sh tool: NOT SHIPPED (per Caramel goal — ideally never use it)

**Trained models trajectory:**
- v1 stamps trained pre-`.B.3` continue working on `.B.3+` engine indefinitely (until back-compat deprecation in future ship)
- v2 stamps emitted by `.B.3+` engine work on `.B.3+` engine
- Mixed scenario (some v1 models, some v2 models): all load correctly

**Future migration cleanup (TECH_DEBT-101 NEW):**
- Track eventual deprecation of v1 back-compat parser layer
- Target ship: when operator confirms all production v1 stamps eliminated (e.g., after natural retraining cycles)

**No operator-visible downstream tooling impact:** verified via codebase scan — no bash scripts or external dashboards parse `inference_cfg_*` wire keys directly. Prefixed keys are internal to `ModelInference.hpp` parser.

**Engine-downgrade gotcha (Gap 3 closure — `.B.3+` engine emits v2 wire keys):**

After `.B.3` ships, new stamps emit unprefixed wire keys (`bandit_algorithm=N` etc.). If you downgrade engine version for testing/debugging (e.g., test against `.B.2` engine which only knows prefixed `inference_cfg_*` keys), v2 stamps DON'T parse on older engines → silent zero population of cohort fields → drift check fires incorrectly OR fields default-initialize.

- **Forward-compat works:** v1 stamps trained pre-`.B.3` continue loading on `.B.3+` engine (parser back-compat at Step 1.6.7.4)
- **Backward-compat does NOT work:** v2 stamps trained on `.B.3+` engine DON'T load correctly on `.B.2` or earlier engines
- **Mitigation:** don't downgrade engine version while running v2-stamp-trained models. If downgrade is needed for debugging, use scratch models trained on the downgraded engine version.
- **FEATURE_LOOKUP entry "Gotchas" line:** documents this explicitly at ship close.

---

## Novel alternative consideration

Each decision re-evaluated for novel design fit given specific `.B.3` purpose. CONFIRMED: existing patterns ARE right for each (per Caramel pushback meta-question + audit synthesis cross-DESIGN_SPECS check).

| Decision | Novel alternative considered | Verdict |
|---|---|---|
| A | KIND_TOKEN-derived STORAGE_T shim | REJECTED — KIND_TOKEN doesn't uniquely determine STORAGE_T |
| B | Failure-attribution callback fn OR failure-record array | REJECTED — purpose is "first-drift attribution at model load"; existing 3-recurrence pattern fits exactly |
| C | Type-erased flat buffer + offset table | REJECTED — consumer code expects typed `inf.<name>` direct access; bounded .bss cost acceptable |
| D | Keep prefixed + emit unprefixed dual | REJECTED — preserves Class 18 at wire surface |
| E | Full CfgDriftCheck redesign with 2-column applicability_when + runtime_when gate | REJECTED — outside `.B.3` scope (future cfg-derived-consumer-framework evolution) |
| F | Parser dual-recognition + emit v2 only + SOFT version bump | **ACCEPTED** — sister to wire-format-byte-preservation Layer 6 Surface G; eliminates restamp tool |

---

## Meta-gaps surfaced at v1.9 RE-SWEEP (codified at v1.10)

Per `feedback_iteration_spiral_signals_audit_meta_gap` discipline. Batch 2 audit re-sweep against v1.9 (5 audits parallel; synthesis at `plan_checks/2026-05-17-v5.15.5.F.4d.1.B.3-v1.9-resweep-synthesis.md`) surfaced **3 NEW meta-gaps** that v1.3 audits (against v1.2) couldn't catch because the v1.9-expanded scope didn't exist yet. Per Caramel directive "we should also update docs to account for where needed" — each meta-gap drives DESIGN_SPEC pre-amendment + skill amendment + feedback memory at ship close. This SECTION exists per `feedback_audit_own_proposals_with_same_rigor` 4-pillar pillar 1 (DESIGN_SPECS cross-check) and Caramel's session directive to land discipline infrastructure-level so future ships inherit mechanically.

### Meta-gap M1 — Sister-registry parity/migration discipline (composite a + b)

**Catalyst:** `/parity-check` HIGH-2 + `/trace-deps` MED-1 CONVERGENT. Plan body Step 1.6.2 entry 12 said `barrier_gate_enabled` "metadata_flags gains STAMP_BOUND_CFG_DERIVED" — but FOREACH_GATE_CFG_FLAG at HEAD is 5-col (no metadata_flags column). Sister FOREACH_ML_CFG_FLAG was migrated to 6-col at `.B.2` (engine `de41ff2`); sister-registry parity not verified at plan-time.

**Pattern shape (two halves):**

- **M1a (DEFENSIVE — sister-registry parity verification):** when plan body references sister-registry columns/bits/fields, verify column exists at HEAD before plan body lock. Class 14 instance prevention.
- **M1b (OFFENSIVE — sister-registry sig migration as cohort discipline):** when ANY X-macro registry undergoes sig migration (col-count change, type column addition, metadata-flags addition), audit cohort sisters for parity-or-deferred-with-rationale; migrate cohort atomically OR defer with explicit registry-header NOTE.

**Doc updates landed at v1.10 + ship close:**

- ✓ `canonical-sister-extension-discipline.md` § Temporal evolution + cohort migration sub-sections (M1a + M1b) — landed v1.10 Stage 2 DRAFT extension; Stage 3 first canonical reference at `.B.3` ship (Step 0.5d.a.0 is the canonical M1b first application; `/readiness` Check 36 first invocation is M1a's canonical first application)
- ⏳ `/readiness` Check 36 NEW at ship close — "Sister registry parity verified for any plan body claim referencing sister-registry columns/bits/fields"
- ⏳ `/trace-deps` skill amendment at ship close — verify sister-registry col-count parity across registries with shared consumer X-macros
- ⏳ NEW feedback memory `feedback_verify_sister_registry_parity_pre_coding.md` at ship close

**TECH_DEBT entry NEW at ship close:** TECH_DEBT-105 — future CI tool `tools/check_sister_registry_sig_parity.py` (LOW priority defer — discipline alone is sufficient at current cohort scale; CI tool warranted when 3+ family members exist with shared consumer pattern)

### Meta-gap M2 — Cross-tool emit-site enumeration discipline

**Catalyst:** `/parity-check` CRIT-1 + HIGH-1. v1.9 Step 1.6.8 enumerated wire-key migrations in `tools/stamp_model.sh` lines 240-262 (6 cohort fields) but missed: (a) line 221 `stamp_format_version=1` hardcoded literal — engine bumps to v2 at `.B.3`; CLI would emit v1-versioned + v2-keyed stamps; (b) line 244 `inference_cfg_freshness_tau=` orphan emit — engine deleted at v5.14.9.D. The v1.9 enumeration was PARTIAL because it scoped to "wire keys in the deletion cohort" without scanning for ALL wire-format literals in cross-process emit sites.

**Pattern shape:** when wire-format changes (version bump, key rename, format-string change, field deletion), engine-side changes are insufficient — ALL cross-process emit sites (CLI tools, training scripts, recording tools) must be enumerated + migrated atomically.

**Doc updates landed at v1.10 + ship close:**

- ✓ `wire-format-byte-preservation-discipline.md` § Layer 7 — Cross-tool emit-site enumeration discipline — landed v1.10 Stage 2 DRAFT extension; Stage 3 first canonical reference at `.B.3` ship (Step 1.6.8 expansion with comprehensive enumeration + per-site disposition + cross-ref comments)
- ⏳ `/parity-check` Section E amendment at ship close — scan `tools/*.sh` + cross-process emit sites when wire-format change proposed
- ⏳ `future-oriented-plan-template.md` amendment at ship close — wire-format-changing plans MUST include "Cross-tool emit-site enumeration" section
- ⏳ NEW feedback memory `feedback_enumerate_cross_tool_emit_sites_on_wire_format_change.md` at ship close

**TECH_DEBT entry NEW at ship close:** TECH_DEBT-106 — future CI tool `tools/check_cross_tool_emit_parity.py` (LOW priority defer — discipline alone is sufficient at current 2-tool scale; CI tool warranted when 3+ cross-tool emit sites exist)

### Meta-gap M3 — Anti-pattern codification false-positive surface

**Catalyst:** `/merge-scan` LOW finding on proposed Class 32 codification. Proposed text could conflate "duplicate-emit asymmetry" (anti-pattern; e.g., `inference_cfg_bandit_algorithm=` + `bandit_algorithm=` BOTH emitted) with "legitimate namespacing prefix" (NOT anti-pattern; e.g., `engine_version=` / `schema_hash=` with descriptive prefix and no unprefixed counterpart). Future `/bug-check` runs could false-positive on PRE_CFG entries that legitimately use namespacing.

**Pattern shape:** when codifying a new anti-pattern class, the description should explicitly distinguish anti-pattern instances from legitimate sibling patterns that match the textual shape but are semantically different.

**Doc updates at ship close (M3 deferred at v1.10 to coincide with Class 32 codification itself):**

- ⏳ Class 32 codification text at `DOCS/RECURRING_BUG_PATTERNS.md` — include explicit "**False-positive surface**" subsection: legitimate namespacing prefixes (engine_version, schema_hash) are NOT this anti-pattern; this Class 32 covers ONLY prefixed/unprefixed pair where both keys emit to wire with same semantic
- ⏳ `RECURRING_BUG_PATTERNS.md` intro polish — note codification template "When adding a new Class, include 'False-positive surface' subsection distinguishing instances from legitimate sibling patterns"
- ⏳ NEW feedback memory `feedback_distinguish_anti_pattern_from_legitimate_sibling_at_codification.md` at ship close (LOW priority; could fold into existing `feedback_audit_own_proposals_with_same_rigor` as pillar refinement — judgment call at memory-write time)

### Meta-gap M4 — Implementation-detail audit layer above SHAPE (codified v1.12 mid-coding 2026-05-18)

**Catalyst:** operator question "are there issues we're not aware of?" mid-Step-1.6.3-coding after 3 batches of SHAPE-level audits (Batch 1 v1.2 / Batch 2 RE-SWEEP v1.9 / Batch 3 v1.10 pre-coding) had all returned GREEN/YELLOW-with-amendments. The 3 prior audit batches had exhausted SHAPE-layer findings (parity / trace-deps / readiness / merge / dod / accounting); operator's meta-question forced surfacing of 12 implementation-detail blind spots that NONE of the 8 SHAPE-layer audit skills targeted by design.

**Pattern shape:** SHAPE audits catch design-layer concerns ("is the design right?"). IMPLEMENTATION-DETAIL audits catch code-layer concerns ("will the code compile and run without surprise rework?"). The 12 blind spots map to 12 RECOGNIZABLE CATEGORIES:

| Pillar | Category | Detection mechanism |
|---|---|---|
| B1 | Type-change cascade when struct field types shift mid-migration | `/trace-deps` TYPE-SENSITIVE classification; `/blindspot-scan` Pillar B1 |
| B2 | Field-name collision across heterogeneous registries unifying into shared struct | `tools/check_field_name_uniqueness.py` CI tool |
| B3 | Transitional state coexistence with bounded growth | `/readiness` Check 37 |
| B4 | Semantic-vs-mechanical pattern mirror (Surface G applicability per registry type) | `/blindspot-scan` Pillar B4 + DESIGN_SPEC annotation |
| B5 | Compile-time scaling threshold | `/blindspot-scan` Pillar B5 + future build-time CI gate |
| B6 | STORAGE_T variant coverage gap | `tools/check_storage_t_coverage.py` CI tool |
| B7 | Include topology cycle risk | `/readiness` Check 38 |
| B8 | Type-sensitive consumer classification (extends Class 14 prevention) | `/trace-deps` TYPE-SENSITIVE classification |
| B9 | Unverified audit claims (claim → no evidence chain) | `/parity-check` Section M |
| B10 | Struct layout drift across mixed-width fields | H12 invariant clarification per `/blindspot-scan` Pillar B10 |
| B11 | Context-dependent C++ construct (if-constexpr template-context requirement) | `/blindspot-scan` Pillar B11 |
| B12 | Cross-registry row ordering for wire-format emit | `/parity-check` Section N + `/readiness` Check 39 |

**Doc updates landed at v1.12 + ship close:**

- ✓ NEW DESIGN_SPEC `implementation-layer-blindspot-taxonomy.md` (Stage 2 DRAFT v1.0) — landed 2026-05-18; first canonical reference at `.B.3` ship close (this plan; first `/blindspot-scan` fire)
- ✓ NEW skill `/blindspot-scan` SKILL.md — Layer-2 audit for the 12-pillar taxonomy
- ✓ NEW feedback memory `feedback_implementation_detail_blindspot_recovery_via_taxonomy.md`
- ✓ NEW CI tool `tools/check_field_name_uniqueness.py` (Pillar B2; PASS at HEAD — 163 fields unique across 4 registries)
- ✓ NEW CI tool `tools/check_storage_t_coverage.py` (Pillar B6; PASS at HEAD — 7 STORAGE_T variants covered)
- ✓ `audit-driven-pre-coding-gate.md` amendment — `/blindspot-scan` added to audit catalog as IMPLEMENTATION-DETAIL layer (sister to SHAPE-layer audits)
- ✓ `/precoding-audit-gate` SKILL.md amendment — `blindspot` added to extended audit_set + ship-type recommendations
- ✓ `/readiness` SKILL.md amendment — Checks 36-39 (sister-registry parity verification M1 / transitional state budget B3 / include topology B7 / row-order parity B12)
- ✓ `/trace-deps` SKILL.md amendment — TYPE-SENSITIVE consumer classification section (B8)
- ✓ `/parity-check` SKILL.md amendment — Section M (claim → evidence chain B9) + Section N (row-order parity B12)
- ✓ `/handoff` SKILL.md amendment — Step 2 pre-coding audit gate extended with SHAPE + IMPLEMENTATION-DETAIL layer split; `/blindspot-scan` conditional fire criteria documented
- ✓ DESIGN_PHILOSOPHY § 11.5 NEW section — meta-discipline registry (M1+M2+M3+M4 catalog) + portal hierarchy
- ✓ DESIGN_PHILOSOPHY § 13 cross-reference index — 4 M-rows added
- ✓ CLAUDE.md restructure — Purpose section + generalized How-To + Skill suite by concern + portal hierarchy diagram
- ✓ CLAUDE.local.md restructure — Meta-disciplines pointer + tightened going-forward index + thin sprint state pointer + `/blindspot-scan` row in Skill load contracts
- ✓ DESIGN_SPECS/README.md catalog entry for `implementation-layer-blindspot-taxonomy.md`
- ✓ canonical-sister-extension-discipline.md + wire-format-byte-preservation-discipline.md cross-refs to M4 spec (M1 + M2 sister specs)

**TECH_DEBT entries opened/closed:**

- ✓ TECH_DEBT-108 CLOSED — `double` STORAGE_T in FOREACH_PER_CORE_CFG_FIELD (H4 violation candidate; verified compliant — 2 rows are documented voting-threshold/statistical-metric exemptions per ControllerConfig.hpp:316 comment + H4 "display-only OK" extension to threshold-only comparison; no migration needed)
- ✓ TECH_DEBT-109 NEW (refined scope) — skill SKILL.md drift audit for 24 remaining skills; 250+ refs sampled, majority LEGITIMATE WORKED EXAMPLES (postmortem context + historical "added after vX.Y" markers — KEEP). Genuine drift ~10-30 across 17 skills. Cost ~1.5-2h pragmatic triage. Deferred from `.B.3` per scope-bounded landing.

**Inflection check:** `/blindspot-scan` first canonical fire (2026-05-18) walked all 12 pillars + verified completeness; NO NEW pillars (B13+) surfaced. 12-category taxonomy completeness CONFIRMED. M4 codification COMPLETE — future ships inherit + apply mechanically.

### Inflection assessment at v1.12

Per `feedback_iteration_spiral_signals_audit_meta_gap`:
- v1.2 → v1.9: 7 iterations; consumer-enumeration meta-gap (v1.8) closed; tooling-enumeration (v1.9) closed
- v1.9 → v1.10: 1 iteration; sister-registry parity/migration (M1) + cross-tool enumeration completeness (M2) + anti-pattern codification precision (M3) — all NEW META-GAPS (not same-surface getting smaller)
- v1.10 → v1.11: 1 iteration; coding-time discoveries Session D + audit synthesis amendments (D-1 through D-5)
- v1.11 → v1.12: 1 iteration; **Meta-gap M4 codification mid-coding** — operator meta-question + 3-batch SHAPE-saturation = canonical inflection signal triggering NEW IMPLEMENTATION-DETAIL audit layer (sister to SHAPE-layer audit gate); 12-pillar taxonomy + skill + CI tools + sister skill amendments landed

The v1.12 findings are FRESH-SURFACE meta-gap at NEW audit-layer dimension. NOT iteration-spiral pollution. v1.12 closes M4 at infrastructure level (1 NEW DESIGN_SPEC + 1 NEW skill + 1 NEW feedback memory + 4 sister skill amendments + 2 NEW CI tools + META program restructure across CLAUDE.md / CLAUDE.local.md / DESIGN_PHILOSOPHY). **First canonical `/blindspot-scan` fire 2026-05-18 verified completeness: NO NEW pillars surfaced; 12-category taxonomy confirmed at first application. Per `feedback_iteration_spiral_signals_audit_meta_gap` discipline: M4 inflection reached at first canonical fire; future ships inherit mechanically.**

---

## Coding-time discoveries (Session D — v1.11)

Per `.B.2` postmortem Lesson 1 pattern + `feedback_iteration_spiral_signals_audit_meta_gap` discipline. Session D coding caught 5 discoveries that informed v1.11 amendments:

### D-1 — engine_version[16] truncation contract was undocumented (Caramel-caught)

**Catalyst:** 2 pre-existing test failures (v5.8.6 + v5.8.8) Caramel suspected were ship-relevant. Investigation revealed `ModelStampResult::engine_version[16]` silently truncates current `ENGINE_VERSION_STRING "5.15.5.F.4d.1.B.2"` (18 chars) to "5.15.5.F.4d.1.B" — strcmp fails. Failures started at `.B.0` when sub-tag version era pushed past 15-char usable capacity.

**Resolution:** documented [16] truncation as INTENTIONAL design (field used for cross-major version check + display; not bit-identity verification). Wire body retains full string (HMAC chain unaffected). Tests v5.8.6 + v5.8.8 updated to `strncmp(..., 15)` matching documented contract. NET test delta: 3235 PASS + 2 FAIL → 3237 PASS + 0 FAIL.

**Lesson:** "pre-existing environmental" hand-wave can mask real ship-relevant bugs. Caramel's domain knowledge surfaced this; pure compaction-based handoffs would have missed.

### D-2 — 5th FOREACH_GATE_CFG_FLAG consumer X-macro (`_PARSE_OV_BITMAP_ROW_gate`) caught at build time

**Catalyst:** Step 0.5d.a.0 5→6 col sig migration enumerated 4 consumer X-macros via initial grep. After landing the 4, build broke with error referencing 5th consumer (`_PARSE_OV_BITMAP_ROW_gate` at `ControllerConfig.hpp:2943`) which the initial grep missed (different naming convention).

**Resolution:** added `_PARSE_OV_BITMAP_ROW_gate` to 6-arg sig (sister to `.B.2` `_PARSE_OV_BITMAP_ROW_ml` migration).

**Lesson:** Meta-gap M1a (sister-registry parity verification) discipline I just codified would catch this PRE-CODING via comprehensive consumer enumeration. Validates the discipline.

### D-3 — Step 1.5 INFERENCE_CFG_AUTOPOPULATE swap blocked by Step 1.6.3 struct-gen

**Catalyst:** Step 1.5 attempted swap. Build still GREEN but `feedback_audit_canonical_sister_before_new_infra` instincts surfaced the issue: framework consumer writes UNPREFIXED `inf.<name>` fields; legacy walker writes PREFIXED `inf.inference_cfg_<name>` fields; the prefixed fields exist on inf struct at HEAD (via FOREACH_STAMP_BOUND_MODEL_CONST POST_CFG) but unprefixed don't (until Approach A unconditional struct-gen lands at Step 1.6.3). Swapping without Step 1.6.3 first would silently drop 16 prefixed fields from wire format (HMAC chain regression).

**Resolution:** reverted Step 1.5 attempted swap. v1.11 BUILD-FORCED list re-ordered: Step 1.5 moved AFTER Step 1.6.3 + Step 1.6.2. /readiness audit verified the dependency.

**Lesson:** BUILD-FORCED sequencing in plan body has assumed independence in some places; coding-time validation catches these. v1.11 BUILD-FORCED is comprehensive per re-ordered phases A-K.

### D-4 — Registry vs manual default value MISMATCHES at TECH_DEBT-093 closure

**Catalyst:** Step 1.6.1 (TECH_DEBT-093 full closure) added `EMIT_GLOBAL_CFG_DEFAULT` auto-defaults mechanism. Audit against existing manual defaults revealed VALUE MISMATCHES (e.g., warmup_ticks registry default=0 but manual=128). Mass-deletion would silently change behavior.

**Resolution:** deleted ONLY the gap_acceptable_threshold manual default (registry default 0.05 matches manual; safe). Documented other 47 manual defaults retained pending registry-vs-manual audit (TECH_DEBT-107 NEW at ship close).

**Lesson:** "registry is source of truth" invariant requires defaults audit when registry was retrofitted into existing manual code. v1.11 captures TECH_DEBT-107 explicitly.

### D-5 — Decision F scope drift from 9→15 keys (v1.5/v1.6 expansion not propagated)

**Catalyst:** /parity-check M-4. Decision F (parser back-compat) was specified for 9 keys at v1.3 when Decision D scope was 9. Decision D expanded 9→10 at v1.5 and 10→15 at v1.6 but Decision F + Step 1.6.7.4 + Step 1.6.7.5 fixture wording stayed stale at 9.

**Resolution:** v1.11 amendment aligns Decision F + Step 1.6.7.4 + Step 1.6.7.5 fixture to 15-key scope per /parity-check M-4. No solution change — scope reconciliation only.

**Lesson:** When iterative scope expansion (9→10→15) happens, dependent solution specs need parallel updates. Plan-body audits at iteration boundaries catch this; v1.9 RE-SWEEP didn't catch it; v1.11 3-audit synthesis did. Future planning amendments should audit all decisions cross-referenced when ANY decision's scope changes.

### D-6 — META program codification mid-coding (operator-triggered Meta-gap M4)

**Catalyst:** mid-Step-1.6.3-pre-coding, operator asked the META question — "are there issues we're not aware of?" Caramel's question forced me to surface 12 implementation-detail blind spots that 3 batches of SHAPE-level audits (parity / trace-deps / readiness / merge / dod) had not caught — by design, those audits target SHAPE concerns, not IMPLEMENTATION-DETAIL. The 12 blind spots mapped 1:1 to 12 RECOGNIZABLE CATEGORIES → Meta-gap M4 codification fired.

**Resolution:** META program executed mid-coding before Step 1.6.3 substantive code:
- NEW DESIGN_SPEC `implementation-layer-blindspot-taxonomy.md` (Stage 2 DRAFT v1.0)
- NEW skill `/blindspot-scan` (Layer-2 audit; 12-pillar walk)
- NEW feedback memory `feedback_implementation_detail_blindspot_recovery_via_taxonomy.md`
- 2 NEW CI tools (`check_field_name_uniqueness.py` + `check_storage_t_coverage.py`; both PASS at HEAD)
- 4 sister skill amendments (/precoding-audit-gate / /readiness / /trace-deps / /parity-check + /handoff /blindspot-scan integration)
- META program restructure: CLAUDE.md Purpose-driven + CLAUDE.local.md tightened index + DESIGN_PHILOSOPHY § 11.5 meta-discipline registry
- DESIGN_SPECS/README.md catalog entry + cross-refs from M1+M2 sister specs to M4 spec
- TECH_DEBT-108 CLOSED (verified H4-compliant) + TECH_DEBT-109 NEW (skill drift audit; refined scope)

**Lesson:** § 11.5 PROCESS works as designed — operator meta-question + 3-audit-batch saturation IS the canonical inflection signal per `feedback_iteration_spiral_signals_audit_meta_gap`. Triggering the 8-step codification procedure (recognize → DESIGN_SPEC → skill amendment → /readiness Check → CI tool → memory → DESIGN_PHILOSOPHY row → re-audit at first canonical fire) closed M4 structurally in ~3-4h mid-coding. **The bet:** discipline infrastructure prevents recurrence at future ships (pays back at first re-application; verified immediately via first canonical `/blindspot-scan` fire catching B3/B8/B9 pre-coding rather than mid-coding rebuild). Per `feedback_motivated_collaborator_for_caramel` + `user_mvp_to_professional_transition`: this IS the professionalization-phase work; slower ship now beats refactor later (per operator framing).

---

## Scope

### IN scope (Steps 0-9 — substantial expansion vs v1.2)

**Step 0 — Pre-coding rollback anchor + DESIGN_SPECs Stage 2 DRAFT on disk:**
- `git tag -s -a pre-v5.15.5.F.4d.1.B.3 -m "..."`
- Verify `failure-attribution-buffer-pattern.md` Stage 2 DRAFT on disk (DONE at synthesis time)
- Verify Stage 2 DRAFTs for amendment specs (cfg-scope-discipline + wire-format-byte-preservation procedure section + cfg-derived-consumer-framework + metadata-bit-driven-derived-filter-framework body cleanup)

**Step 0.5 — Framework primitive extensions BEFORE walker migrations:**
- **0.5a** — `cfg_derived::drift_check_from_derived` template fn at `CfgGateRegistry.hpp:315+` extended with `char* reason_buf, size_t reason_cap` args + first-drift attribution per `failure-attribution-buffer-pattern.md` (Decision B (a))
- **0.5b** — `FOREACH_GLOBAL_CFG_FIELD` Path α 12-col cascade: add STORAGE_T column to all 47 global rows; extend `ControllerConfig<F>` struct-gen mechanism to walk global registry with STORAGE_T per row (sister to per-core PerCoreCfg<F> auto-gen per H17) (Decision A (a))
- **0.5c** — `tt::cfg_parse_field<T>` char[N] branch addition (forward-compat primitive sister to `.F.4e` KIND_STRING work; ~5-10 LOC; mechanical sister to existing `tt::stamp_parse_field<T>` char[N] handling) (per `/trace-deps` HIGH-2)
- **0.5d (NEW v1.6 — TECH_DEBT-104 closure; v1.7 honest effort revision; v1.10 added 0.5d.a.0 sister-registry sig migration sub-step per Meta-gap M1)** — Framework walker extension for `FOREACH_GATE_CFG_FLAG` bitmap source. Currently `cfg_derived::populate_stamp_cfg_from_derived` template fn at `MemHeaders/CfgGateRegistry.hpp:296-305` walks `FOREACH_ML_CFG_FLAG` only. Sister registry `FOREACH_GATE_CFG_FLAG` exists at `CoreFrameworks/GateCfgFlagRegistry.hpp:46` (verified at v1.7) but is **5-col** at HEAD (`X(NAME, legacy_field, display_label, section, doc)`); sister `FOREACH_ML_CFG_FLAG` was migrated to **6-col** at `.B.2` (engine commit `de41ff2`; added `metadata_flags` column). v1.9 RE-SWEEP `/parity-check` HIGH-2 + `/trace-deps` MED-1 convergently caught the gap — Step 0.5d.a as v1.9-written cannot compile because the walker's `if constexpr (((metadata_flags) & STAMP_BOUND_CFG_DERIVED) != 0)` filter references a column that doesn't exist on sister.

  **Sub-steps:**
  - **0.5d.a.0 (NEW v1.10 — Meta-gap M1b first canonical; ~30 min):** FOREACH_GATE_CFG_FLAG 5→6 col sig migration BEFORE walker extension. Sister to `.B.2` FOREACH_ML_CFG_FLAG migration (engine commit `de41ff2`); per `canonical-sister-extension-discipline.md` § Sister-registry sig migration as cohort discipline.
    - **At `CoreFrameworks/GateCfgFlagRegistry.hpp:46`:** add `metadata_flags` column → `X(NAME, legacy_field, display_label, section, metadata_flags, doc)` 6-col tuple
    - **All 6 rows:** add `metadata_flags = 0` initially (DEPTH_ENABLED / GATE_EMA_ENABLED / NO_TRADE_BAND_ENABLED / COST_GATE_ENABLED / PARAM_STALENESS_GATE_ENABLED stay at `0`)
    - **BARRIER_GATE_ENABLED row (line 51):** set `metadata_flags = CfgFieldDescriptor::STAMP_BOUND_CFG_DERIVED` (this is the trigger for Class 32 closure; enables Step 0.5d.a walker filter)
    - **Update 4 consumer X-macros to 6-arg sig:** X_GEN_GATE_CFG_BIT (line 58) + X_GEN_GATE_CFG_MASK (line 70) + GUI/SettingsPanel.hpp X-macro (find via `rg "FOREACH_GATE_CFG_FLAG"` at coding time) + ControllerConfig.hpp parser X-macro (likely `_PARSE_OV_BITMAP_ROW_gate` per `.B.2` `_PARSE_OV_BITMAP_ROW_ml` precedent; verify via grep)
    - **Update file header comment lines 23-27:** the NOTE about barrier_gate_enabled stamp-binding via FOREACH_STAMP_BOUND_MODEL_CONST gets a v1.10 amendment — "STAMP_BOUND_CFG_DERIVED via metadata_flags column (added 2026-05-18 at `.B.3` per Meta-gap M1b cohort migration); stamp emission migrates to framework walker per Step 0.5d.a; POST_CFG entry at StampBoundModelConstRegistry.hpp:283-284 deleted at Decision D mechanism 1"
    - **Verify GATE_CFG_FLAG_AUTOPOPULATE_FROM_HEX macro at lines 80-90** stays UNCHANGED — autopopulate companion doesn't care about metadata_flags column (sister to ML_CFG_FLAG_AUTOPOPULATE_FROM_SEPTUPLE which stayed unchanged at `.B.2`)
    - Build verify atomically; mismatched consumer X-macro arity → compile error catches drift
  - **0.5d.a (~30 min; UNCHANGED from v1.9):** Add parallel emit pass `X_STAMP_CFG_POPULATE_GATE_CFG_FLAG` walking `FOREACH_GATE_CFG_FLAG`. Sister to `X_STAMP_CFG_POPULATE_ML_CFG_FLAG` at lines 296-305 — copy-pattern + adapt `MASK_GATE_CFG_*` prefix + read `cfg.gate_cfg_flags`. Mechanical. (Step 0.5d.a.0 must land FIRST per BUILD-FORCED sequencing.)
  - **0.5d.b (~1-2h):** Parser-side handling — bitmap-flag wire keys like `barrier_gate_enabled=1` need parser dispatch that sets the corresponding bit in `sr.gate_cfg_flags` AND populates the auto-gen discrete field `sr.barrier_gate_enabled` (per Approach A unconditional struct-gen). Verify existing pattern at HEAD for ml_cfg_flag fields (`confidence_composite_enabled=1` etc.) + extend to gate_cfg_flag. May need additional dispatch macro in parser X-macro.
  - **0.5d.c (~30 min):** Consumer migration sub-decision at Step 1.6.3 — Approach A unconditional struct-gen produces discrete `int barrier_gate_enabled; uint8_t has_barrier_gate_enabled;` on StampInferenceCfgInputs + ModelStampResult. Decision: consumers can read EITHER discrete field OR bitmap. Discrete field preferred for consistency with current `CoreModelZoo.hpp:402-403` access pattern + Decision D mechanism 1 sister fields.
  - **0.5d.d (~30 min):** Same dispatch extensions for `populate_inference_cfg_from_derived` + `drift_check_from_derived` template fns. Sister-pattern.

  **Total Step 0.5d effort: ~3.5-4.5h focused** (v1.10 added Step 0.5d.a.0 sister-registry 5→6 col migration ~30 min; v1.7 revised v1.6 estimate ~1-2h after verification of FOREACH_GATE_CFG_FLAG infrastructure + parse-side complexity).

  Enables Step 1.6.2 v1.6 entry 12 (`barrier_gate_enabled` bit-add **landed at Step 0.5d.a.0 as integral to the 5→6 col migration**) + Class 32 FULL closure.

**Step 1 — Migrate test count-assertion sites (per CRIT-CONV-3 + audit verification):**
- 4 `FOREACH_STAMP_BOUND_CFG_COUNT >= N` sites at lines 4057, 4893, 22189, 25381 → `cfg_field_count(g_per_core_cfg_stamp_bound_cfg_derived_mask) + cfg_field_count(g_global_cfg_stamp_bound_cfg_derived_mask) >= N`
- 2 `FOREACH_STAMP_BOUND_CFG(X_COUNT_*)` walker count sites at 23600, 23618 → cohort-presence check via descriptor lookup
- Verify all assertions STRENGTHEN per `/test-strength-audit`

**Step 1.5 — `INFERENCE_CFG_AUTOPOPULATE` swap (DEFERRED from `.B.1`):**
- 1 production site at `StampHelper.hpp:183` → `INFERENCE_CFG_POPULATE_FROM_DERIVED` (framework macro wrapper at `CfgGateRegistry.hpp:382`)
- 1 test site range at `controller_test.cpp:24962-25047` (A.7.4 + A.7.5)
- Verify wire-format byte preservation under SOFT bump (Decision F covers)

**Step 1.6 — 8 LOAD-BEARING TECH_DEBT-09X closures + scope expansions:**

- **Step 1.6.1 — TECH_DEBT-093 (Decision A (a)):** After Step 0.5b struct-gen extension lands — DELETE manual decl at `ControllerConfig.hpp:889`; DELETE manual default at `:1729`; DELETE manual parser at `:2555`. `gap_acceptable_threshold` auto-gen via FOREACH_GLOBAL_CFG_FIELD.

- **Step 1.6.2 — TECH_DEBT-094 + TECH_DEBT-100 + TECH_DEBT-104 (v1.6 expanded — all paired):** 10 fields bit-add per Decision D mechanism 1:

  **Original 5 (v1.5):**
  - `CfgFieldRegistry.hpp:534` (ml_tp_pct): metadata_flags gains `STAMP_BOUND | STAMP_BOUND_CFG_DERIVED`
  - `:535` (ml_sl_pct): same
  - `:537` (bandit_blend_ratio): same
  - `:646` (barrier_blend_mode): same
  - `MlCfgFlagRegistry.hpp:70` (`PER_HORIZON_BARRIER_BLEND`): metadata_flags `0` → `STAMP_BOUND_CFG_DERIVED` (TECH_DEBT-100)

  **5 ADDITIONAL at v1.6 (Class 32 full closure; TECH_DEBT-104):**
  - `CfgFieldRegistry.hpp:538` (confidence_threshold_scale): metadata_flags gains `STAMP_BOUND_CFG_DERIVED`
  - `:644` (confidence_hard_block_threshold): metadata_flags gains `STAMP_BOUND_CFG_DERIVED`
  - `:660` (fee_rate_maker): metadata_flags gains `STAMP_BOUND_CFG_DERIVED`
  - `:661` (fee_rate_taker): metadata_flags gains `STAMP_BOUND_CFG_DERIVED`
  - `CoreFrameworks/GateCfgFlagRegistry.hpp:51` (`barrier_gate_enabled` BARRIER_GATE_ENABLED row): metadata_flags gains `STAMP_BOUND_CFG_DERIVED` (**LANDED at Step 0.5d.a.0 as part of 5→6 col sig migration; this line preserved as cohort documentation cross-reference**)

  Wire-key change: 15 keys total (10 in v1.5 + 5 new) — `inference_cfg_<name>=X` → `<name>=X` — couples with Step 1.6.7 SOFT version bump (parser back-compat enumerates ALL 15 legacy keys).
  Verify `trading_mode` row at FOREACH_GLOBAL_CFG_FIELD has STAMP_BOUND_CFG_DERIVED bit (per HIGH-CONV-H verification)

  **BLOCKING DEPENDENCY (Gap 5 closure — `/trace-deps chain:per_horizon_barrier_blend` verified):** Step 1.6.2 bit-add REQUIRES bare `inf.<name>` unprefixed fields to exist on StampInferenceCfgInputs + ModelStampResult structs. At HEAD `.B.2`, only PREFIXED `inf.inference_cfg_<name>` exist for the 5 fields (POST_CFG-only). Framework walker can't generate emit/drift-check without the bare field.

  **Consumer migration scope (v1.5 enumeration — was implicit in v1.4):**

  After Decision D deletes 10 prefixed POST_CFG entries + Approach A unconditional struct-gen creates bare fields, consumers reading `.inference_cfg_<name>` must migrate to `.<name>`:

  **Production sites (2 total):**
  - `Backtest/BacktestPanels.hpp:1867` — `v.inference_cfg_bandit_blend_ratio` → `v.bandit_blend_ratio` (display panel read; mechanical replace_all-safe per `feedback_avoid_substring_replace_all_on_member_access` — full-token match)
  - `ML_Headers/CoreModelZoo.hpp:411-412` — `handle->inference_cfg_bandit_blend_ratio = sr.inference_cfg_bandit_blend_ratio` → `handle->bandit_blend_ratio = sr.bandit_blend_ratio` (drift check + handle population paired)

  **Test fixture sites (~12 total in `tests/controller_test.cpp`):**
  - Lines 12534, 12562 — bandit_blend_ratio assert + verify
  - Lines 13324, 13363, 13511 — same + verify_round_trip variant
  - Line 21471 — bandit_blend_ratio sentinel test
  - Lines 25003-25006 — handle h assign tests for 4 retroactive `.A.7` fields (ml_tp_pct, ml_sl_pct, barrier_blend_mode, per_horizon_barrier_blend)
  - Lines 25007-25014 — same fields verify pattern
  - Lines 25028-25035 — INFERENCE_CFG_AUTOPOPULATE verify (gets swapped at Step 1.5 anyway)
  - Lines 25046-25048 — inf_off gate-off verify
  - Lines 26174-26177 — bandit_algorithm sentinel test

  Per `/test-strength-audit` discipline: each test fixture update STRENGTHENS assertions (not weakens); rename is mechanical but verify intent preserved.

  **5 ADDITIONAL consumer migration sites at v1.6 (TECH_DEBT-104 full closure):**
  - `CoreModelZoo.hpp:400-405` (3 sites) — `handle->inference_cfg_confidence_threshold_scale = sr.inference_cfg_confidence_threshold_scale` → `handle->confidence_threshold_scale = sr.confidence_threshold_scale`; same pattern for `barrier_gate_enabled` + `confidence_hard_block_threshold`
  - `CoreModelZoo.hpp:416-419` (2 sites) — `handle->inference_cfg_fee_rate_maker = sr.inference_cfg_fee_rate_maker` → `handle->fee_rate_maker = sr.fee_rate_maker`; same for `fee_rate_taker`
  - Plus corresponding test fixture updates in `tests/controller_test.cpp` for assignments + asserts to these 5 fields (additional ~5-10 test sites)

  **Total v1.8 consumer migration scope (per comprehensive cross-grep):** **149 total references across 8 files** for the 15 deletion-scope field names. Distribution:
  - `tests/controller_test.cpp` — 80 sites (mostly test fixture renames `.inference_cfg_<name>` → `.<name>` + `STAMP_HAS(_, inference_cfg_<name>)` → `_.has_<name>`)
  - `ML_Headers/StampBoundModelConstRegistry.hpp` — 27 sites (the POST_CFG entries themselves being deleted; sites disappear with parent macro entry)
  - `ML_Headers/CfgDriftCheckRegistry.hpp` — 17 sites (15 STAMP-side field renames per Step 1.6.6.b + 2 STAMP_HAS gate-expression sites at line 250 + 290; migrate to `STAMP_HAS(_, inference_cfg)` group bit OR per-row has_)
  - `ML_Headers/CoreModelZoo.hpp` — 14 sites (5 v1.6 model-state + 1 bandit_blend_ratio paired write/read at :411-412 + STAMP_HAS at :409 + other patterns)
  - `Backtest/BacktestPanels.hpp` — 7 sites (5 v1.6 model-state at :1855/1857/1859/1871/1872 + bandit_blend_ratio at :1867 + STAMP_HAS at :1865)
  - `ML_Headers/ModelInference.hpp` + `MlCfgFlagRegistry.hpp` + `StampHelper.hpp` — 4 sites total (mostly comments/docs; cosmetic)

  **Coding-time procedure (per `feedback_enumerate_consumers_before_registry_row_deletion`):**
  ```bash
  # Per field, one mechanical replace_all sweep (full-token safe per feedback_avoid_substring_replace_all_on_member_access):
  for FIELD in ml_tp_pct ml_sl_pct barrier_blend_mode per_horizon_barrier_blend \
              bandit_algorithm thompson_mu_prior thompson_precision_prior thompson_precision_obs \
              thompson_exp3_blend_alpha bandit_blend_ratio \
              confidence_threshold_scale barrier_gate_enabled confidence_hard_block_threshold \
              fee_rate_maker fee_rate_taker; do
      # Step 1: Rename direct field access
      rg -l "inference_cfg_${FIELD}\b" | xargs sed -i "s/\binference_cfg_${FIELD}\b/${FIELD}/g"
      # Step 2: Migrate STAMP_HAS bit references (case-by-case; bit enum entry disappears)
      # Per call site: STAMP_HAS(_, inference_cfg_<FIELD>) → _.has_<FIELD>
      # OR: STAMP_HAS(_, inference_cfg) group bit if group-level semantic suffices
      # Build verify after each field
  done
  ```

  **Build verifies between field renames** catch any consumer pattern missed by replace_all (e.g., a `STAMP_HAS` site needing per-call migration vs replace_all). Per-field iteration takes ~1-2 min × 15 fields = ~20-30 min mechanical.

  **Per-field STAMP_HAS migration** (cannot replace_all; semantic decision per site):
  - `BacktestPanels.hpp:1865`: `STAMP_HAS(v, inference_cfg_bandit_blend_ratio)` → `v.has_bandit_blend_ratio` (per-row has_)
  - `CfgDriftCheckRegistry.hpp:250`: `STAMP_HAS(*h, inference_cfg_bandit_blend_ratio)` → `h->has_bandit_blend_ratio`
  - `CoreModelZoo.hpp:409`: `STAMP_HAS(sr, inference_cfg_bandit_blend_ratio)` → `sr.has_bandit_blend_ratio`
  - `tests/controller_test.cpp` — multiple sites; per-call migration

  **Per-entry has_##name preservation:** all 15 fields retain per-entry `has_<name>` Surface G semantic via Approach A unconditional struct-gen.

  **Per-entry has_##name preservation:** all 15 fields retain per-entry `has_<name>` Surface G semantic via Approach A unconditional struct-gen.

  **Anti-pattern context:** the prefix asymmetry that requires this consumer migration IS the Class 32 NEW pattern codified at ship close (see "Bug classes this ship closes" section).

  **Mandatory sequencing:**
  1. Step 0.5b Path α global struct-gen extension lands FIRST
  2. Step 1.6.3 Decision C Approach A unconditional struct-gen lands SECOND (produces bare `inf.<name>` fields for ALL flagged master per-core + global + ML_CFG_FLAG rows)
  3. THEN Step 1.6.2 bit-add works (bare fields exist; framework walker generates emit + drift-check)
  4. Step 2 legacy registry deletion happens LAST

  **Anti-pattern verification per `/trace-deps chain:per_horizon_barrier_blend`:**
  - Class 8 (silently inactive in sharded): CLEAN — `StrategyParameters.hpp:1348-1349` reads `BITMAP_IS_SET(core_cfg->ml_cfg_flags, MASK_ML_CFG_PER_HORIZON_BARRIER_BLEND)` unconditionally in sharded per-core decision path; bit-add doesn't change evaluation
  - Class 31 (wire-format duplicate): CLEAN — only POST_CFG entry exists at `StampBoundModelConstRegistry.hpp:463-465`; no PRE_CFG sister; no duplicate emission risk
  - Class 24 (capability-cfg surface mismatch): CLEAN — no site reads legacy scalar `cfg.per_horizon_barrier_blend`; all 5 reads use bitmap semantic
  - Drift-check semantic preserved: `CfgDriftCheckRegistry.hpp:310-313` has `REFUSE_STRICT` severity + `INFERENCE_CFG` surface + `EXACT` comparison; bandit-boundary distinction (line 313 master gate fires always when feature set; dependent ml_tp_pct/ml_sl_pct/barrier_blend_mode at lines 300/304/308 fire only when feature ON) preserved per Decision E (E.2)

  **Documentation references confirmed safe:** `GUI/MLStatusPanel.hpp:225` has help-text mention of `cfg.per_horizon_barrier_blend=1` (DOC ONLY; no code read). No production code path reads legacy scalar.

  **Per-core override emission:** NO new per-core override surface at `.B.3`. Per-core override infrastructure for this field deferred to v5.15.6 (per `StrategyParameters.hpp:1342-1343` comment). Not in `.B.3` scope.

- **Step 1.6.3 — TECH_DEBT-095 (Decision C Approach A):** ModelInference struct-gen migrations (per HIGH-CONV-C corrected struct identification):
  - `ModelInference.hpp:1196-1199` (**ModelStampResult** parser-side struct-gen) — replace `FOREACH_STAMP_BOUND_CFG(X)` walker with unconditional master-cfg struct-gen
  - `ModelInference.hpp:1396-1401` (parser dispatch) — replace `FOREACH_STAMP_BOUND_CFG(X)` walker with master registry walker filtered by STAMP_BOUND_CFG_DERIVED bit
  - `ModelInference.hpp:1640-1643` (**StampInferenceCfgInputs** emit-side struct-gen) — same shape as :1199
  - Preserve per-entry `has_<name>` Surface G semantic per HIGH-CONV-I

  **v1.12 amendment — `/blindspot-scan` pre-coding audit findings per Meta-gap M4 (Pillar B3 / B8 / B9):**

  **Transitional state coexistence annotation (Pillar B3):** Between Step 1.6.3 landing and Step 2 (legacy registry deletion), ModelStampResult + StampInferenceCfgInputs structs hold BOTH the 144 master-registry-driven fields (NEW via Approach A unconditional struct-gen) AND the 27 legacy FOREACH_STAMP_BOUND_CFG fields (legacy walker still present) AND 10 POST_CFG fields (still present until Step 2). Estimated transitional struct-size peak ~1.1-1.5KB per struct (per `/blindspot-scan` Pillar B3 verdict). Within acceptable bounds (boot-time/load-time struct; not hot-path; not byte-equivalence input per H12). Coexistence resolves at Step 2 legacy registry deletion (FORCED LAST among migration Steps). Bounded growth + bounded coexistence window = SILENT-RISK → ACCEPT WITH RATIONALE.

  **Consumer migration scope — TYPE-SENSITIVE classification (Pillar B8 per `/trace-deps` amendment):** After Approach A unconditional struct-gen, currently-flagged STAMP_BOUND_CFG_DERIVED rows (27 in master per-core) shift struct field types from FOREACH_STAMP_BOUND_CFG declared types (often `double`) to master registry STORAGE_T (often `FPN<F>`). Per /trace-deps TYPE-SENSITIVE classification (B8): ~80-100 sites in `tests/controller_test.cpp` are TYPE-SENSITIVE-READ (compare `sr.<field> == 0.005` against literal `double`); ~10-20 sites are TYPE-SENSITIVE-WRITE (`handle->X = sr.X` assignment); rest are TYPE-AGNOSTIC. Per-site mitigation: wrap with `FPN_ToDouble(...)` OR rely on `operator==(FPN<F>, double)` (FPN<F> has 6 comparison operators codebase-wide since `.B.2`). Effort: ~5 min per TYPE-SENSITIVE site; mechanical replace_all for AGNOSTIC sites. Coding-time procedure: build verify after each test fixture batch to catch missed TYPE-SENSITIVE sites incrementally.

  **`tt::cfg_drift_compare<T>` evidence chain (Pillar B9 per `/parity-check` Section M):** Per Meta-gap M4 claim → evidence chain requirement: prior `/parity-check` MEDIUM-1 framed `tt::cfg_drift_compare<T>` as "auto-handles FPN/double cross-type comparison via implicit conversion." VERIFIED at coding time via reading `CfgFieldDispatch.hpp:454-457` template definition (cite range corrected per `/parity-check` v1.12 re-fire — initially cited 382-388 was stale): actual mechanism is EXPLICIT `FPN_ToDouble(both sides)` conversion within the template's `if constexpr (std::is_floating_point_v<StampT> && is_FPN_v<CfgT>) { return stamp_val != FPN_ToDouble(cfg_val); }` branch (not implicit). Wire bytes + drift detection semantics preserved either way; correction is wording-level.

- **Step 1.6.4 — TECH_DEBT-096:** Production canonical body emit migration (`ModelInference.hpp:1782-1788`). Replace `FOREACH_STAMP_BOUND_CFG(X)` walker with `cfg_derived::populate_stamp_cfg_from_derived<F>(canonical + n, sizeof(canonical) - n, *cfg_ptr)` call. **This IS the wire-format-changing step — couples with Step 1.6.7 SOFT version bump.**

  **v1.12 amendment — Wire-format row-ordering annotation (per `/readiness` Check 39 + `/blindspot-scan` Pillar B12):** Master registry FOREACH_PER_CORE_CFG_FIELD declaration order DIFFERS from legacy FOREACH_STAMP_BOUND_CFG body order for the currently-flagged STAMP_BOUND_CFG_DERIVED rows. Row-order change is **INTENTIONAL under SOFT-bump** per Decision F (F.2) — wire keys + row order both change v1 → v2; parser back-compat layer (Step 1.6.7.4) handles v1 legacy stamps; emit walker produces v2 canonical body in master-registry order. Layer 5b structural invariants (`tests/wire_format_invariants.hpp` I1-I5) TOLERATE the diff (master-registry-order invariant is the v2 reference; v1 ordering preserved in parser-side acceptance only). Step 1.7 Layer 5b invariants invocation verifies v2 emit shape; Step 1.6.7.5 v1 LOAD test verifies v1 parser acceptance. SILENT-RISK explicitly resolved by SOFT-bump procedure + parser back-compat layer + intentional reorder annotation.

  **Step 1.6.4.a (Gap 1 closure — locale-pin re-entrancy verification per `/accounting-audit` HIGH-3; v1.11 semantic clarification per /parity-check M-2):** `tt::cfg_emit_field<T>` at `CfgFieldDispatch.hpp:321-362` does INNER `uselocale(LC_NUMERIC=C)` pin (lines 332-334) + restore (358-359). Caller `stamp_write_for_model` at `ModelInference.hpp:~1700` does OUTER pin per Layer 2. Verified at v1.4: outer + inner are FUNCTIONALLY CORRECT (newlocale creates separate locale_t; uselocale(prev) restores outer's pin) but redundant. NOT a correctness hazard. **v1.11 CLARIFICATION (per /parity-check M-2):** The byte-identity assertion at Step 1.6.4 test fixture is a **REGRESSION-LOCK for v2 canonical body shape** — NOT a v1↔v2 byte equivalence check. v1 and v2 canonical bodies INTENTIONALLY differ (Decision D mechanism 1 changes wire keys prefixed→unprefixed). Layer 5b structural invariants I1-I5 at Step 1.7 + Step 1.6.7.5 v1 LOAD test handle v1↔v2 back-compat verification.

  **Sub-step deliverable:** Add explicit byte-identity assertion at Step 1.6.4 test fixture that verifies canonical body output bytes EXACTLY match expected (compile-time string literal of canonical body emitted by framework walker for known cfg state). Catches any layer-changing drift (locale, format, ordering). Sister to `wire_format_invariants.hpp` I3 invariant which catches actual locale leak codebase-wide.

  **TECH_DEBT-103 NEW (open at .B.3 ship close):** Optimize per-row inner locale-pin in `tt::cfg_emit_field<T>` to skip when outer thread pin already active. ~25 locale create/free cycles per stamp emit eliminated. Target ship: TBD; LOW priority (slow-path optimization). Mechanism: add `cfg_emit_field_inner` variant that skips locale dance; framework walker uses inner-variant when invoked under outer-pin context.

- **Step 1.6.5 — TECH_DEBT-097 (LANDED at WIP-checkpoint 4 `9e99e91`):** `StampHelper.hpp:156` `STAMP_CFG_AUTOPOPULATE(inf, cfg)` migration. Per CRIT-CONV-3 verification: 1 production + 7 test sites total. Production swap to framework consumer; 7 test sites at lines 4825, 4845, 4863, 22305, 22326, 22737, 22748 (current HEAD line numbers post-WIP-checkpoint-3) mechanical migration to framework consumer (`INFERENCE_CFG_POPULATE_FROM_DERIVED`). Comment block at `StampHelper.hpp:149-158` rewritten to reflect framework-driven mechanism (master FOREACH_PER_CORE_CFG_FIELD + FOREACH_GLOBAL_CFG_FIELD filtered by STAMP_BOUND_CFG_DERIVED bit). Test banner + printf updated to reflect migrated cohort framework name.

- **Step 1.6.5b — NEW v1.13 (action-parameterized walker structural fix; absorbs Step 0.5d.d for populate_inf + drift_check):** Refactor 4 cfg-derived consumer template fns + STAMP_RESULT_DERIVED_FIELDS_AUTO_GEN struct-gen meta-walker to use shared `FOREACH_STAMP_BOUND_DERIVED_COHORT(BASE)` meta-walker. **Closes Class 21 instance at cfg-derived consumer template fn surface** (sister-consumer asymmetric registry coverage drift caught at Session E — `populate_inference_cfg_from_derived` walked only per-core + global; `drift_check_from_derived` walked only per-core + global + ml_cfg_flag; missing walkers cause silent stamp-binding gaps for cohort fields). Structural fix: meta-walker = single source of truth for cohort coverage; consumer can't omit a registry because meta-macro expands to all 4 FOREACH invocations unconditionally + per-consumer X-macros must exist by naming convention for compile cleanly.

  **Scope (5 sites; ~4h bounded):**
  - **5a — Build `FOREACH_STAMP_BOUND_DERIVED_COHORT(BASE)` meta-walker** in `MemHeaders/CfgGateRegistry.hpp` (~5 LOC; expands to 4 `FOREACH_<REGISTRY>(BASE##_<SCOPE>)` invocations).
  - **5b — Add explicit `#include` for `MlCfgFlagRegistry.hpp` + `GateCfgFlagRegistry.hpp`** at top of `MemHeaders/CfgGateRegistry.hpp` (self-contained header per WIP2d-1.B.0 Shortsighted #5 close).
  - **5c — Refactor `populate_inference_cfg_from_derived`** (line 228) — add `X_INFERENCE_CFG_POPULATE_ML_CFG_FLAG` + `X_INFERENCE_CFG_POPULATE_GATE_CFG_FLAG` X-macros (NEW walkers; closes 2 of 2 missing walkers; absorbs Step 0.5d.d for populate_inf side).
  - **5d — Refactor `populate_stamp_cfg_from_derived`** (line 258) — consolidate existing 4 walker bodies under meta-walker; X-macro bodies VERBATIM (wire-format byte preservation; Layer 5b invariants verify).
  - **5e — Refactor `drift_check_from_derived`** (line 331) — add `X_DRIFT_CHECK_GATE_CFG_FLAG` X-macro (NEW walker; closes Step 0.5d.d for drift_check side; requires `handle.barrier_gate_enabled` field which Step 1.6.3 Decision C Approach A struct-gen now provides).
  - **5f — Refactor `parse_stamp_cfg_to_derived`** (line 420) — consolidate existing 4 walker bodies under meta-walker (parser already walks all 4; refactored for consistency + future-drift prevention).
  - **5g — Refactor `STAMP_RESULT_DERIVED_FIELDS_AUTO_GEN`** (line 527) — replace 4 hardcoded FOREACH invocations with `FOREACH_STAMP_BOUND_DERIVED_COHORT(_STAMP_RESULT)`; rename file-scope X-macros (`_STAMP_RESULT_PER_CORE_FIELD` → `_STAMP_RESULT_PER_CORE`, etc.) to match `BASE##_<SCOPE>` convention.
  - **5h — Enroll `FOREACH_STAMP_BOUND_DERIVED_COHORT` in `FOREACH_REGISTRY`** at `CoreFrameworks/MetaRegistry.hpp` as Level 1 meta-walker with parent FOREACH_REGISTRY (sister to FOREACH_PER_CORE_DOMAIN_BITMAP shape; meta-walker over derived cohort).
  - **5i — Build verify + Layer 5b invariant tests** — `./build.sh test`; verify error count drops (closes some Class 14 instances from missing field declarations); verify `wire_format_invariants.hpp` I1-I5 PASS post-refactor (master-registry-order invariant preserved; X-macro bodies VERBATIM).

  **Structural enforcement:** consumer MUST define all 4 X-macros (PER_CORE / GLOBAL / ML_CFG_FLAG / GATE_CFG_FLAG) for meta-walker expansion to compile cleanly. Missing any X-macro = compile error at FOREACH expansion site (preprocessor fails on undefined identifier in macro body). Drift impossible by construction.

  **Closes:** Class 21 instance at cfg-derived consumer template fn surface; Class 18 instance at struct-gen + consumer-walker mirror; populate_inf framework-coverage gap (2 missing walkers); Step 0.5d.d deferral for populate_inf + drift_check (gate_cfg_flag walker addition INCLUDED in scope).

  **Sister meta-walker precedent:** STAMP_RESULT_DERIVED_FIELDS_AUTO_GEN at HEAD `9e99e91` (CfgGateRegistry.hpp:527-531) ALREADY expands to 4 hardcoded FOREACH invocations for struct-field declarations — proves the meta-walker shape works at this surface. Step 5g unifies struct-gen under the same parameterized meta-walker; new registry addition to cohort auto-extends BOTH struct-gen AND all 4 consumer template fns.

  **NOT in scope at this step (deferred):**
  - Step 0.5d.b (gate_cfg_flag walker parser dispatch — parse_stamp_cfg_to_derived already walks gate_cfg_flag; parser-side fully covered)
  - Step 0.5d.c (consumer migration decision — separate concern; consumer-side rename + migration is its own substep)
  - `/trace-deps` Section M framework-coverage-parity check amendment (B9 audit-methodology gap — deferred to next ship per `feedback_iteration_spiral_signals_audit_meta_gap` inflection check; structural fix is primary close).

  **Risk + mitigation:** Wire-format byte preservation is the load-bearing risk (populate_stamp_cfg_from_derived is the canonical body emitter). X-macro bodies are VERBATIM copies of current walker bodies; emit order preserved by FOREACH walker semantics (same registry → same order); Layer 5b invariants tolerate the structural reshape. Step 1.6.7.5 v1 LOAD test catches HMAC drift post-refactor.

- **Step 1.6.6 — TECH_DEBT-098 (Decision B (a)) + Decision E (E.2):**
  - **Step 1.6.6** main: `CoreModelZoo.hpp:225-247` drift walker migration. Replace inline `FOREACH_STAMP_BOUND_CFG(X)` drift walk with `DRIFT_CHECK_FROM_DERIVED(...)` macro wrapper at `CfgGateRegistry.hpp:382` (NOT raw template fn invocation per `/trace-deps` CRIT-1). FAILURE_MASK symbol: `FAILURE_MASK_cfg_binding_drift` (NOT `cfg_inference_drift` — see CRIT-CONV-4). Extended framework signature per Step 0.5a preserves `sr.reason` first-drift attribution.
  - **Step 1.6.6.a (NEW per Decision E):** 4 CfgDriftCheck row substitutions to COHORT_GATE_* macros at `CfgDriftCheckRegistry.hpp` — line 272 `thompson_exp3_blend_alpha` gate → `COHORT_GATE_BANDIT_BLEND_STATE_4`; lines 300 (`ml_tp_pct`), 304 (`ml_sl_pct`), 308 (`barrier_blend_mode`) gates → `COHORT_GATE_PER_HORIZON_BARRIER`

  - **Step 1.6.6.b (NEW at v1.7 — material finding from final pre-tag verification):** CfgDriftCheckRegistry.hpp STAMP-side field-access rename — **15 row-edits** for all rows whose STAMP-side value reads a field that's renamed by Decision D mechanism 1:
    - Line 236: `h->inference_cfg_confidence_threshold_scale` → `h->confidence_threshold_scale`
    - Line 240: `h->inference_cfg_barrier_gate_enabled` → `h->barrier_gate_enabled`
    - Line 245: `h->inference_cfg_confidence_hard_block_threshold` → `h->confidence_hard_block_threshold`
    - Line 249: `h->inference_cfg_bandit_blend_ratio` → `h->bandit_blend_ratio`
    - Line 255: `h->inference_cfg_bandit_algorithm` → `h->bandit_algorithm`
    - Line 259: `h->inference_cfg_thompson_mu_prior` → `h->thompson_mu_prior`
    - Line 263: `h->inference_cfg_thompson_precision_prior` → `h->thompson_precision_prior`
    - Line 267: `h->inference_cfg_thompson_precision_obs` → `h->thompson_precision_obs`
    - Line 271: `h->inference_cfg_thompson_exp3_blend_alpha` → `h->thompson_exp3_blend_alpha`
    - Line 275: `h->inference_cfg_fee_rate_maker` → `h->fee_rate_maker`
    - Line 279: `h->inference_cfg_fee_rate_taker` → `h->fee_rate_taker`
    - Line 299: `h->inference_cfg_ml_tp_pct` → `h->ml_tp_pct`
    - Line 303: `h->inference_cfg_ml_sl_pct` → `h->ml_sl_pct`
    - Line 307: `h->inference_cfg_barrier_blend_mode` → `h->barrier_blend_mode`
    - Line 311: `h->inference_cfg_per_horizon_barrier_blend` → `h->per_horizon_barrier_blend`

    All 15 rows: gate expressions PRESERVED per Decision E (E.2) — 4 substitutable substituted at Step 1.6.6.a, 11 distinct-semantic preserved. STAMP-side field-access uniformly renamed.

    Effort: ~30-60 min mechanical replace_all-safe (full-token matches; no substring conflict per `feedback_avoid_substring_replace_all_on_member_access`).

- **Step 1.6.7 — TECH_DEBT-099 (REORDERED sub-steps per CRIT-CONV-5):**
  - **1.6.7.0 (NEW):** WRITE `wire-format-byte-preservation-discipline.md` SOFT bump procedure section as Stage 2 DRAFT (lands at Step 0 — BEFORE coding sub-steps 1.6.7.1-6 apply the procedure)
  - **1.6.7.1:** Extract literal `"stamp_format_version=1\n"` at `ModelInference.hpp:1745-1748` to `static constexpr uint32_t STAMP_FORMAT_VERSION_CURRENT = 1;` constant
  - **1.6.7.2:** Add `static constexpr uint32_t MAX_SUPPORTED_STAMP_FORMAT_VERSION = 2;` constant + parser bounds check at `:1346-1351` (RELAXED — accept v1 AND v2)
  - **1.6.7.3:** Bump `STAMP_FORMAT_VERSION_CURRENT` 1 → 2
  - **1.6.7.4 (NEW per Decision F (F.2); v1.11 9→15 key scope alignment per /parity-check M-4):** Parser back-compat layer for **15 legacy prefixed wire keys** at `ModelInference.hpp` parser. Closed-set X-macro `FOREACH_LEGACY_PREFIXED_KEY(X)` listing the 15 keys with their unprefixed sister field names per Decision D v1.6 scope (9 thompson/.A.7 cohort + 1 standalone bandit_blend_ratio + 5 model-state cohort: confidence_threshold_scale + barrier_gate_enabled + confidence_hard_block_threshold + fee_rate_maker + fee_rate_taker). Per `canonical-sister-extension-discipline.md` § INLINE MERGE. Clear comment block + TECH_DEBT-101 reference at dispatch site. **MUST land in SAME COMMIT as Step 1.6.4 + Step 1.6.7.3** per /parity-check M-3 (avoid version label / wire keys ordering hazard).
  - **1.6.7.5 (Gap 2 closure — concrete fixture synthesis procedure):** v(1) stamp LOAD test.

    **Fixture creation approach (option B — hand-written self-contained):** Test fixture is a compile-time string literal of canonical body bytes with v1 prefixed wire keys; HMAC computed at test time via known test secret.

    ```cpp
    // tests/controller_test.cpp — Step 1.6.7.5 test section:
    SECTION(".B.3 v1 stamp back-compat: legacy prefixed wire keys load cleanly");
    {
        // Hand-written v1 canonical body — exact bytes a .B.2 engine would emit
        // for the known cfg state below. Contains prefixed `inference_cfg_*` keys
        // (the 9 we delete at .B.3 emit; parser back-compat at Step 1.6.7.4 handles).
        const char* v1_canonical_body =
            "model_format_version=5\n"
            "stamp_format_version=1\n"
            "trained_on_iso=2025-01-15\n"
            "engine_version=5.15.5.F.4d.1.B.2\n"
            // ... PRE_CFG entries ...
            // V1 prefixed cohort wire keys (parser back-compat at 1.6.7.4 dual-recognizes):
            "inference_cfg_bandit_algorithm=4\n"
            "inference_cfg_thompson_mu_prior=0.123\n"
            "inference_cfg_thompson_precision_prior=1.0\n"
            "inference_cfg_thompson_precision_obs=1.0\n"
            "inference_cfg_thompson_exp3_blend_alpha=0.5\n"
            "inference_cfg_ml_tp_pct=2.0\n"
            "inference_cfg_ml_sl_pct=1.0\n"
            "inference_cfg_barrier_blend_mode=0\n"
            "inference_cfg_per_horizon_barrier_blend=0\n"
            // ... POST_CFG entries (non-cfg-derived model state) ...
            ;
        const char* test_secret = "test-secret-for-v1-fixture-only";

        // Write fixture to tmp + HMAC-sign in-process:
        char tmp_path[] = "/tmp/v1_fixture_XXXXXX.stamp";
        // mkstemp + write canonical body + "\n" + sig — see existing fixture
        // patterns at tests/controller_test.cpp for write helpers

        // Verify .B.3 engine loads v1 stamp cleanly:
        ModelStampResult sr = verify_model_stamp(tmp_path, test_secret,
                                                 0.1, MODEL_FORMAT_VERSION);
        check("v1 stamp loads on .B.3 engine (back-compat parser)", sr.valid == 1);
        check("v1 cohort field populated: bandit_algorithm",  sr.bandit_algorithm == 4);
        check("v1 cohort field populated: thompson_mu_prior", sr.thompson_mu_prior == 0.123);
        // ... per-field verification ...

        // Cfg drift fires correctly when cfg differs from stamp:
        ControllerConfig<64> cfg{};
        cfg.bandit_algorithm = 2;  // differs from stamp's 4
        ModelStampResult sr2 = verify_model_stamp_with_cfg(tmp_path, test_secret,
                                                            0.1, MODEL_FORMAT_VERSION, &cfg);
        check("cfg drift detected on v1 stamp (back-compat parser feeds drift check)",
              sr2.valid == 0 && sr2.inference_cfg_drift_count > 0);
        check("drift reason references bandit_algorithm",
              strstr(sr2.reason, "bandit_algorithm") != nullptr);
    }
    ```

    **NOT NEEDED:** binary fixture file in `tests/fixtures/` (self-contained string literal approach eliminates filesystem dependency). **NOT NEEDED:** build engine at `.B.2` state to emit fixture (canonical body bytes are hand-written + deterministic). Self-contained test fixture per `wire-format-byte-preservation-discipline.md` Layer 4 round-trip discipline.
  - **1.6.7.6 (NEW):** TECH_DEBT-101 entry tracking eventual deprecation of v1 back-compat (target ship: when production v1 stamps eliminated)

**Step 1.6.8' — Phase L (SUPERSEDES Step 1.6.8 bash-patches per v1.14 amendment 2026-05-18): structural elimination of cross-tool wire-format mirror via framework-driven C++ CLI binary.** Replaces `tools/stamp_model.sh` (6+ cross-tool sync recurrence events) with `tools/stamp_model_cli.cpp` that calls existing `stamp_write_for_model` framework API directly. **Closes Class 18 + 19 + 21 + 22 at cross-tool surface; drift impossible by construction** (CLI uses framework, doesn't mirror it). Per `feedback_no_defer_for_effort` + `feedback_motivated_collaborator_for_caramel` + `structural-fix-preferred-decision-framework.md` § Step 2 (4+ recurrence = MANDATORY structural fix; current count 6+).

**Why upgrade from v1.10 bash-patch scope:** v1.10's Step 1.6.8 was Layer 7 § per-site disposition discipline applied to the bash script (6 wire-key renames + 1 version literal bump + 1 orphan delete + 1 preserve-with-comment). That closes the IMMEDIATE drift at `.B.3` but preserves the recurrence vector — next cfg field add with stamp-binding still requires manual bash sync (the 7th+ sync event). v1.14 recognizes the bash script has tracked 6+ cross-tool sync events across versions v5.2.3 / v5.8.8 / v5.9.3b / v5.9.4a / v5.9.5c / v5.11.18a + the .B.3 batch — well past the 4× recurrence-mandatory threshold. Layer 7 discipline does NOT eliminate the seam structurally; framework-driven CLI binary DOES. v1.14 elevates Step 1.6.8 from discipline-application to structural-elimination per `feedback_structural_fix_for_recurring_class` (parent rule) + the `tools/stamp_model.sh` header line 12 itself which has documented "Phase 2 (v5.3.x?) replaces this with `tools/stamp_model.cpp` that runs validation directly from CLI" since v5.2.3 (~3 major versions overdue).

**Sub-step decomposition (L1-L8 v1.15 expanded scope):**

**L1 — DESIGN_SPEC `framework-driven-cli-binary-pattern.md` v1.0 → v1.1 amendment (lands pre-Phase-L coding at workspace `DESIGN_SPECS/`).** v1.0 covered wire-emit-via-framework + deprecation shim + operator workflow. v1.1 amendment per audit findings adds:
- **NEW section "X-macro auto-gen of CLI flag table"** — Pattern shape: walk FOREACH_PER_CORE_CFG_FIELD + FOREACH_GLOBAL_CFG_FIELD + FOREACH_ML_CFG_FLAG + FOREACH_GATE_CFG_FLAG (filtered by STAMP_BOUND_CFG_DERIVED bit) + FOREACH_STAMP_BOUND_MODEL_CONST_PRE_CFG + POST_CFG (unfiltered; architectural constants) → emit `static struct option longopts[]` array; X-macro auto-gen value-receiver struct fields; per-flag parse dispatch via existing `tt::cfg_parse_field<T>` (no new framework infrastructure). Sister discipline to engine's `populate_stamp_cfg_from_derived` walker. **Adding a new cfg field with stamp-binding = 1 registry row → CLI flag `--<name>` auto-appears at next compile.** Drift impossible by construction at CLI interface layer.
- **NEW section "Extensibility test pattern"** — X-macro walker synthesizes deterministic value per flagged row (via `synthetic_value_for_field<T>(field_name_hash)` helper) + sets cfg field + runs stamp emit/parse via framework + validates round-trip byte-identity per row. Adding a new flagged row = test auto-validates byte-identity round-trip works. Eliminates the v5.14.1.B.3.E manual 17-field test block as recurrence vector. Pattern is registry-agnostic; codified in sister spec `cfg-derived-consumer-framework.md` v1.2 → v1.3 (per L7 sub-step below).
- **NEW section "Audit detection"** per `pattern-codification-lifecycle.md` § Stage 2 discipline. Grep signatures: `tools/*.sh` files mirroring FOREACH_*_CFG_FIELD wire keys; bash scripts with hardcoded `stamp_format_version=` literals; CLI binaries with manual `{"--flag", ...}` longopts entries instead of X-macro auto-gen.
- **FIX stale include path** — line 142 `#include "../FixedPoint/FPN.hpp"` → `#include "../FixedPoint/FixedPoint64.hpp"` (per /trace-deps HIGH-3 finding; actual FPN header filename verified at HEAD).

**L2 — NEW `tools/stamp_model_cli.cpp` (~250-300 LOC; framework-driven C++ CLI binary).** Calls existing `stamp_write_for_model` at `ML_Headers/ModelInference.hpp:1688` directly. **X-macro auto-gen discipline (NEW v1.15 scope per CRIT-1 structural fix; resolved per `/blindspot-scan` v1.15 B11 finding: file-scope `if constexpr` filter is IMPOSSIBLE in C++ — `if constexpr` requires template or function scope; static-array initializer is file scope. RESOLUTION: emit ALL rows at file scope; filter applied at parse-dispatch time via descriptor.metadata_flags runtime check. See spec `framework-driven-cli-binary-pattern.md` v1.1 § Step 2.5 for canonical pattern body):**

**B13 CRITICAL cross-walker longopts[] collision resolution** (per `/blindspot-scan` v1.15 finding): 3 names (`xgb_min_child_weight` / `xgb_seed` / `xgb_train_nthread`) appear in BOTH FOREACH_GLOBAL_CFG_FIELD AND FOREACH_STAMP_BOUND_MODEL_CONST_POST_CFG at HEAD. Emitting both walkers into longopts[] would produce DUPLICATE flag entries → getopt_long ambiguity → silent operator-visible bug. RESOLUTION: reuse existing `FOREACH_STAMP_RESULT_FIELD_EXCLUSION` sidecar at `MemHeaders/CfgGateRegistry.hpp:512-515` (already excludes these 3 names at struct-gen layer per H18 SIDECAR OVERRIDE pattern + Pillar B13). Cfg walker (`X_GEN_LONGOPT_ALL_CFG`) wraps each row with `#define/#undef` redirect bracket: excluded names redirect to dead `_longopts_excluded_<name>` macro (zero bytes emitted). MC walker emits authoritative entry per architectural-constant semantic. Sister to existing struct-gen pattern at `CfgGateRegistry.hpp:506-509`:

```cpp
// Per CfgGateRegistry.hpp:506-509 struct-gen exclusion precedent.
// Apply same pattern to longopts[] auto-gen.

#define X_GEN_LONGOPT_ALL_CFG(STORAGE_T, KIND_TOKEN, name, label, section, meta, payload, tooltip, applies_strat, applies_op, applies_regime, applies_risk, lives_in_struct) \
    {#name, required_argument, 0, 0},

// Exclude collision names at cfg walker site (MC walker is authoritative):
#define _LONGOPT_OVERRIDE_xgb_min_child_weight(STORAGE_T, KIND_TOKEN, name, label, section, meta, payload, tooltip, applies_strat, applies_op, applies_regime, applies_risk, lives_in_struct) /* skip */
#define _LONGOPT_OVERRIDE_xgb_seed(STORAGE_T, KIND_TOKEN, name, label, section, meta, payload, tooltip, applies_strat, applies_op, applies_regime, applies_risk, lives_in_struct) /* skip */
#define _LONGOPT_OVERRIDE_xgb_train_nthread(STORAGE_T, KIND_TOKEN, name, label, section, meta, payload, tooltip, applies_strat, applies_op, applies_regime, applies_risk, lives_in_struct) /* skip */
// (At #define site for X_GEN_LONGOPT_ALL_CFG; use _LONGOPT_OVERRIDE_##name dispatch via Y3 token-paste; default body emits longopts entry)
```

Alternative (simpler): use CI gate via `tools/check_cli_flag_drift.py` (TECH_DEBT-111) to verify no flag-name collisions exist post-emit; if collision detected, build fail with actionable error. Defer collision-resolution mechanism choice to L2 coding-time.

```cpp
// Sketch — X-macro auto-gen longopts[] from 6 registries (emit ALL rows; filter at parse-dispatch)

#define X_GEN_LONGOPT_CFG(STORAGE_T, KIND_TOKEN, name, label, section, meta, payload, tooltip, applies_strat, applies_op, applies_regime, applies_risk, lives_in_struct) \
    {#name, required_argument, 0, 0},

#define X_GEN_LONGOPT_CFG_FILTERED(STORAGE_T, KIND_TOKEN, name, label, section, meta, payload, tooltip, applies_strat, applies_op, applies_regime, applies_risk, lives_in_struct) \
    /* DEPRECATED v1.15 per /blindspot-scan B11: file-scope if-constexpr filter is impossible in C++. \
     * Use X_GEN_LONGOPT_ALL_CFG unfiltered + apply STAMP_BOUND_CFG_DERIVED filter at parse-dispatch time. \
     * Parse dispatch reads descriptor.metadata_flags at runtime: \
     *   if ((desc.metadata_flags & STAMP_BOUND_CFG_DERIVED) == 0) return;  // not a stamping flag; ignored \
     * Approach: file-scope longopts[] sees ALL rows; runtime filter eliminates non-stamping noise. */

#define X_GEN_LONGOPT_BITMAP(NAME, legacy_field, display_label, section, metadata_flags, doc) \
    {#legacy_field, required_argument, 0, 0},  /* filter by metadata_flags & STAMP_BOUND_CFG_DERIVED */

#define X_GEN_LONGOPT_MC(name, group, presence, type, fmt, default_val, get_value, emit_when, doc) \
    {#name, required_argument, 0, 0},

static struct option longopts[] = {
    // Workflow flags (CLI-only; hardcoded — not registry-driven)
    {"model",           required_argument, 0, 'm'},
    {"secret",          required_argument, 0, 's'},
    {"wf-mean-val",     required_argument, 0, 0},
    {"held-out-metric", required_argument, 0, 0},
    {"gap-threshold",   required_argument, 0, 0},
    {"trained-on",      required_argument, 0, 0},
    {"format-version",  required_argument, 0, 0},
    {"force",           no_argument,       0, 'f'},

    // Auto-gen from cfg-derived cohort (filtered by STAMP_BOUND_CFG_DERIVED bit)
    FOREACH_PER_CORE_CFG_FIELD(X_GEN_LONGOPT_CFG_FILTERED)
    FOREACH_GLOBAL_CFG_FIELD(X_GEN_LONGOPT_CFG_FILTERED)
    FOREACH_ML_CFG_FLAG(X_GEN_LONGOPT_BITMAP)
    FOREACH_GATE_CFG_FLAG(X_GEN_LONGOPT_BITMAP)

    // Auto-gen from model-const cohort (unfiltered; architectural constants per FOREACH_STAMP_BOUND_MODEL_CONST)
    FOREACH_STAMP_BOUND_MODEL_CONST_PRE_CFG(X_GEN_LONGOPT_MC)
    FOREACH_STAMP_BOUND_MODEL_CONST_POST_CFG(X_GEN_LONGOPT_MC)

    {0, 0, 0, 0}  // sentinel
};

#undef X_GEN_LONGOPT_CFG
#undef X_GEN_LONGOPT_CFG_FILTERED
#undef X_GEN_LONGOPT_BITMAP
#undef X_GEN_LONGOPT_MC

// Sister X-macro auto-gen value-receiver struct (parallel pattern; per-flag has_*+value)
struct CliArgs {
    /* workflow flags hardcoded */
    const char* model_path; const char* secret; double wf_mean_val; /* etc. */
    /* registry-driven via X-macro */
    FOREACH_PER_CORE_CFG_FIELD(X_GEN_CLI_FIELD_CFG_FILTERED)
    /* etc. for all 6 registries */
};

// getopt_long parse loop dispatches per-flag via tt::cfg_parse_field<T>:
while ((c = getopt_long(argc, argv, "m:s:f", longopts, &longopt_idx)) != -1) {
    if (c == 0 && longopt_idx > 0) {  // long option
        const char* flag_name = longopts[longopt_idx].name;
        // Dispatch to per-registry parser via name lookup (use existing
        // cfg_derived::parse_stamp_cfg_to_derived sister or direct walk).
        cli_args_dispatch(flag_name, optarg, &cli_args);
    } else if (c == 'm') { cli_args.model_path = optarg; }
    /* etc. for workflow flags */
}

// Post-parse: construct ControllerConfig<64> from received values (overrides defaults)
ControllerConfig<64> cfg = ControllerConfig_Default<64>();
apply_cli_args_to_cfg(cli_args, cfg);  // X-macro walker: if cli_args.has_<name>, cfg.<name> = cli_args.<name>

// Construct StampInferenceCfgInputs from model-const cohort
StampInferenceCfgInputs inf{};
apply_cli_args_to_inf(cli_args, inf);  // X-macro walker for model-const fields

// Call framework API directly
StampWriteResult r = stamp_write_for_model(
    cli_args.model_path, cli_args.secret, cli_args.format_version,
    cli_args.trained_on_iso, cli_args.wf_mean_val, cli_args.held_out_metric,
    cli_args.gap_threshold, cli_args.force,
    cli_args.feature_registry_hash, cli_args.engine_version, &inf);
```

**CLI flag naming convention:** Registry uses `snake_case` (e.g., `ridge_lambda`). Bash script also uses `snake_case` flags (`--ridge-lambda` actually uses kebab; need to verify). Per /trace-deps audit, bash uses `--confidence-threshold-scale` (kebab) but registry uses `confidence_threshold_scale` (snake). **Naming reconciliation needed at L2 coding:** either (a) accept BOTH forms (auto-gen both kebab + snake variants), (b) auto-convert snake→kebab via macro magic, (c) standardize on snake everywhere. Decision deferred to L2 coding-time per /readiness Check 30 operator-impact discipline. Lean: (c) snake_case CLI flags matching registry (cleaner; breaks bash backward-compat for some flags — covered by deprecation shim period).

Defense-in-depth: pin LC_NUMERIC=C in main() before any framework call (framework already pins internally; redundant pin is cheap + protects against any non-framework code paths).

**Additional operator-UX scope (v1.15 — minor scope additions per never-refactor-again audit):**

- **`--help` text auto-gen from tooltip column** — X-macro walker emits per-flag help text from FOREACH_*_CFG_FIELD's `tooltip` column (existing column; already populated for engine.cfg.example auto-gen). Adding a new cfg field with stamp-binding = `--help` output auto-includes it; no manual help-text maintenance. ~15 LOC; sister to longopts[] X-macro walker
- **Exit code parity with bash script** — bash uses `exit 0` for success, `exit 1` for refuse-on-gap, `exit 2` for invalid args. C++ CLI mirrors via explicit `return` statements: `return 0` on `r.ok == 1`; `return 1` on refuse (gap > threshold); `return 2` on getopt parse failure or invalid model path. Per `feedback_surface_operator_migration_path_proactively` — operator scripts that check `$?` continue to work post-migration
- **Deprecation aliases for kebab-case bash flags** — IF bash flags use kebab-case (e.g., `--confidence-threshold-scale`) while registry uses snake_case (`confidence_threshold_scale`), provide deprecation alias layer in CLI binary main() that translates kebab→snake with one-line stderr warning. Operator scripts using legacy kebab flags continue working during retention period (TECH_DEBT-110 deletion target). Defer decision to L2 coding-time after verifying which flags are kebab vs snake in current bash script. **Interaction note** (per /readiness LOW v1.15): kebab-alias decision interacts with CLI flag naming convention reconciliation above — if (c) "standardize on snake_case CLI flags" is chosen at L2 coding-time, deprecation aliases COVER backward-compat for any kebab-case bash flags during retention; if (a) "accept both forms" is chosen, deprecation alias layer is the mechanism. Decisions should be evaluated together at L2 coding-time, not independently.

**L3 — Build system integration: CMake 4-line target pattern (CRIT-2 fix; matches `tools/compare_scalers` sister precedent at `CMakeLists.txt:248-251`):**

```cmake
add_executable(stamp_model_cli tools/stamp_model_cli.cpp)
target_compile_options(stamp_model_cli PRIVATE -O2 -march=native)
target_include_directories(stamp_model_cli PRIVATE ${CMAKE_SOURCE_DIR}/..)
target_link_libraries(stamp_model_cli PRIVATE ssl crypto Threads::Threads)
```

`ssl crypto` for HMAC via OpenSSL; `Threads::Threads` for framework's pthread inclusion. Without these, build links fail. Output at `build/stamp_model_cli`.

**L4 — Verification — round-trip + byte-identity + legacy + extensibility + Layer 5b CLI emit (EXPANDED v1.15):**
- **Round-trip** — stamp a model via CLI binary (subprocess call); verify .stamp via engine `verify_model_stamp`; HMAC valid; round-trip values match expected
- **Byte-identity** — stamp same model via CLI binary AND via engine in-process for known cfg state; canonical body bytes byte-for-byte identical (`memcmp == 0`)
- **Legacy verification** — bash-stamped model from older repo (committed test fixture `.stamp` file) verifies on `.B.3+` engine (forward-compat per Surface G + Decision F SOFT parser back-compat)
- **Workflow replication** — every bash-script scenario reproducible via CLI flags (5+ representative scenarios from bash script's documented usage at `tools/stamp_model.sh:30-50`)
- **NEW v1.15: Extensibility test (X-macro walker)** — synthesize deterministic value per STAMP_BOUND_CFG_DERIVED-flagged row → set cfg → emit stamp → parse back → validate per-row byte-identity round-trip. Adding a new flagged row = test auto-validates that row's round-trip works. **Eliminates the v5.14.1.B.3.E manual 17-field test block as recurrence vector.** Sister pattern codified at `cfg-derived-consumer-framework.md` v1.3 (per L7 below).
- **NEW v1.15: Layer 5b CLI emit invariants test** — extend `tests/wire_format_invariants.hpp` I1-I5 to ALSO verify CLI binary's canonical body output satisfies the structural invariants (line count == popcount; format consistent; locale-pinned; row presence; canonical order). Sister to existing engine-side Layer 5b test; same invariants apply to both emit paths.

**L5 — Deprecation shim at `tools/stamp_model.sh`:** Replace bash script body (716 lines) with 1-line `exec` redirect to `build/stamp_model_cli` with `"$@"` flag passthrough. Header preserved as deprecation notice cross-ref'd to DESIGN_SPEC + new CLI binary. **TECH_DEBT-110 NEW tracks bash-shim deletion target ship** (typical retention: 1-2 ship cycles for operator script migration).

**L6 — Cross-ref updates EXPANDED (per audit findings HIGH-2 + MED-2):**
- `wire-format-byte-preservation-discipline.md` § Layer 7 — append note "Layer 7 discipline OBVIATED at framework-driven cross-tool surfaces; see `framework-driven-cli-binary-pattern.md`. Layer 7 still applies for non-framework-driven cross-tool surfaces (e.g., bash diagnostic tools without C++ equivalent)." **[ALREADY LANDED at v1.14]**
- `canonical-sister-extension-discipline.md` § "Temporal evolution + cohort migration of sister registries" — add cross-tool surface as additional axis where the discipline applies; **NEW v1.15: add forward-ref to `framework-driven-cli-binary-pattern.md`** as the structural-fix sibling that obviates Layer 7 discipline at framework-driven surfaces
- `cfg-derived-consumer-framework.md` — add forward-ref to `framework-driven-cli-binary-pattern.md` as 5th canonical consumer (sister to populate_stamp_cfg + populate_inference_cfg + drift_check + parse_stamp_cfg)
- `structural-fix-preferred-decision-framework.md` § Reference implementations — add Phase L `tools/stamp_model_cli.cpp` (6+ recurrence; STRUCTURAL FIX MANDATORY) as canonical example of "Structural fix with new pattern (strategic moment)"
- `DESIGN_SPECS/README.md` catalog — add `framework-driven-cli-binary-pattern.md` entry under "Framework discipline patterns" subsection
- `CLAUDE.local.md` going-forward rules — already added at v1.14

**L7 — NEW v1.15: `cfg-derived-consumer-framework.md` v1.2 → v1.3 amendment.** Codifies extensibility test pattern (X-macro walker validates all flagged rows via synthetic round-trip) as sister to existing "Adding a new consumer concern" section. Pattern is registry-agnostic; applies to ANY cfg-derived consumer cohort. v1.3 amendment body:
- NEW section "Extensibility test pattern for cohort consumers" — X-macro walker shape; synthetic value generator helper; per-row validation; pattern shape with code sample
- Cross-ref to `framework-driven-cli-binary-pattern.md` v1.1 as 1st canonical Stage 3 application (Phase L test code at controller_test.cpp)
- Future canonical: model-const cohort extensibility test (FOREACH_STAMP_BOUND_MODEL_CONST round-trip; defer to next ship that warrants it)

**L8 — TECH_DEBT-111 NEW: CI defense-in-depth check (LOW priority defer).** `tools/check_cli_flag_drift.py` — Python script that verifies `tools/stamp_model_cli.cpp` uses X-macro auto-gen (flags any manual `{"--flag", ...}` longopts entries that bypass the registry walk). Warranted at 2nd framework-driven CLI binary canonical (per `feedback_framework_layer_payoff_diminishing_returns` — pattern earns its place at 2+ applications). Cross-ref `registry-coverage-ci-check-pattern.md` Shape B (anti-pattern enforcement CI mechanism category).

**Total scope (v1.15 EXPANDED):** ~250-300 LOC C++ CLI binary (vs v1.14 ~150-200) + ~30 LOC build system + ~150-250 LOC verification tests (extensibility + Layer 5b + workflow replication; vs v1.14 ~50-100) + ~5 LOC deprecation shim + ~30 LOC cross-ref updates + ~100 LOC DESIGN_SPEC v1.0 → v1.1 amendments + ~80 LOC NEW cfg-derived-consumer-framework.md v1.2 → v1.3 amendment. **~7-10h focused** (vs v1.14 ~4-6h estimate; +3-4h for X-macro discipline + extensibility test + DESIGN_SPEC extensions). Per Caramel directive: "time isn't an issue + most optimal solution for never-refactoring-again goal" — expanded scope is the right call.

**Lasting-impact assessment per `feedback_motivated_collaborator_for_caramel` + Caramel's "never refactor again" goal:**
- **Adding a new cfg field with stamp-binding post-Phase-L:** 1 row in master FOREACH_PER_CORE_CFG_FIELD with STAMP_BOUND_CFG_DERIVED bit → framework auto-flows → CLI inherits for free → zero cross-tool sync work
- **Adding a new CLI flag (operator-controllable parameter) post-Phase-L:** add flag parsing + ControllerConfig assignment in `stamp_model_cli.cpp` (~5 lines) — this IS new operator capability, not maintenance drift; acceptable
- **Wire format SOFT bump (like v1→v2 at this ship) post-Phase-L:** version literal lives ONCE at engine `STAMP_FORMAT_VERSION_CURRENT`; CLI inherits via framework call; no manual cross-tool sync
- **Class 18 + 19 + 21 + 22 instances at cross-tool surface:** STRUCTURALLY IMPOSSIBLE to recur (no mirror; CLI calls framework)

**Lower-hit files (corrected v1.10 per `/readiness` v1.9 RE-SWEEP MED-2):** comment-only historical refs in 7 files (ConfidenceScore.hpp + CfgFieldDispatch.hpp:384 + StampHelper.hpp:150 + MlCfgFlagRegistry.hpp 3 sites + CfgGateRegistry.hpp 2 sites + engine.cfg.example:784) — ACCEPTED as-is per Layer 7 § per-site disposition "PRESERVE WITH CROSS-REF COMMENT" (refer to legacy macros for context; not consumer/emit code; comment drift acceptable at this scope). v1.14 amendment: these are NOT in Phase L scope (Phase L is specifically the bash→C++ migration; comment-only refs are independent).

**Phase L step couplings:**
- L1 (DESIGN_SPEC) → L2 (CLI binary): spec body lands FIRST so CLI implementation has reference
- L2 (CLI binary) → L3 (build system): binary code exists before build target added
- L2 + L3 → L4 (tests): tests verify binary works
- L4 PASS → L5 (deprecation shim): bash shim only lands after tests verify CLI fully replaces bash
- L5 → L6 (cross-ref updates): cross-ref updates at ship close once Phase L lands
- **Phase L MUST land in same commit as Step 1.6.7.3 (version bump 1→2) per Layer 7 § Cross-tool version literal sync discipline** — same coupling that applied to the v1.10 bash-patches now applies to the C++ CLI version literal at engine STAMP_FORMAT_VERSION_CURRENT constant + CLI inheritance via framework call

**Operator migration impact (per `feedback_surface_operator_migration_path_proactively`):**
- Operator action required during retention period: NONE (deprecation shim preserves `tools/stamp_model.sh --model X` invocations)
- Operator action after shim deletion (target ship per TECH_DEBT-110): update scripts to invoke `build/stamp_model_cli` directly (mechanical replace_all; flag interface unchanged)
- Backward compatibility: bash-stamped models (pre-`.B.3`) continue to verify on `.B.3+` engine via Decision F SOFT parser dual-recognition
- Forward compatibility: CLI-stamped models (`.B.3+`) emit v2 format (unprefixed keys); operator workflow ↔ CLI surface ↔ wire format chain preserved end-to-end

**Step 1.7 (NEW per HIGH-CONV-F narrow scope) — Invoke EXISTING Layer 5b helper at first non-zero cohort:**
- `tests/wire_format_invariants.hpp` from `.A` ship already supports dual-mask context (verified at synthesis)
- Add test invocation of `run_wire_format_canonical_body_invariants(ctx)` at first non-zero cohort context (post Step 1.6.4 production walker migration)
- Verifies framework walker output satisfies I1-I5 invariants (line count == popcount; format consistent; no comma decimals (locale pin); per-row name appears when bit set; per-core before global)
- Effort: ~30-60 min

**Step 2 — DELETE legacy registry headers:**
- `MemHeaders/CfgDerivedInferenceCfgRegistry.hpp` — full file delete (orphaned post Step 1.5)
- `ML_Headers/StampBoundCfgRegistry.hpp:99-179` — `FOREACH_STAMP_BOUND_CFG(X)` macro body fully empty (replace with `// SUPERSEDED — see DESIGN_SPECS/framework-patterns/cfg-derived-consumer-framework.md`)
- `ML_Headers/StampBoundCfgRegistry.hpp:230` — `STAMP_CFG_AUTOPOPULATE` macro body deleted
- `ML_Headers/StampBoundCfgRegistry.hpp:264` — `FOREACH_STAMP_BOUND_CFG_COUNT` deleted (test sites migrated at Step 1)
- **BUILD BREAKS HERE if Steps 1.5 + 1.6.3 + 1.6.4 + 1.6.5 + 1.6.6 not addressed** — verifies migrations land BEFORE this step

**Step 3 — Remove `FOREACH_REGISTRY` enrollment rows:**
- `CoreFrameworks/MetaRegistry.hpp:52` — `FOREACH_STAMP_BOUND_CFG` row deleted
- `CoreFrameworks/MetaRegistry.hpp:99` — `FOREACH_CFG_DERIVED_INFERENCE_CFG` row deleted
- Per H15 + H19; `check_meta_registry.py` verifies

**Step 4 — Add CI Check 9 (compile-time static_assert per `/dod-audit` MED-3):**
- Concrete shape: `static_assert(cfg_field_count(g_per_core_cfg_stamp_bound_cfg_derived_mask.words) + cfg_field_count(g_global_cfg_stamp_bound_cfg_derived_mask.words) >= 24, "STAMP_BOUND_CFG_DERIVED coverage regression");` co-located with mask declarations at `CfgFieldRegistry.hpp:1079-1088`
- Sister to H16 first canonical at `CfgFieldRegistry.hpp:1143`
- NO Python tool (per `/dod-audit` MED-3)
- Checks 10-12 placeholder slots reserved for `.F.4e` GUI metadata derived filters

**Step 5 — v5.14 stamp fixture regression test (under Decision F (F.2)):**
- Verify v1 stamps LOAD CLEANLY on `.B.3` engine (back-compat parser handles legacy keys)
- Verify drift check fires correctly on v1 stamps
- Verify cfg fields populated correctly from legacy wire keys
- NOT STRICT refuse test (per Decision F (F.2))

**Step 6 — DESIGN_SPECS spec-body cleanup + NEW sections (EXPANDED v1.10 per Meta-gaps M1 + M2):**
- `cfg-scope-discipline.md` — NEW "Override-inherit pattern" section
- `wire-format-byte-preservation-discipline.md` — NEW "Procedure for SOFT wire-format changes via parser back-compat" section (Stage 2 DRAFT at Step 1.6.7.0) **+ NEW Layer 7 "Cross-tool emit-site enumeration discipline"** (Stage 2 DRAFT landed v1.10 per Meta-gap M2; Stage 3 first canonical reference at Step 1.6.8 expansion)
- `cfg-derived-consumer-framework.md` v1.2 → v1.3 — production walker migration + inf struct unification + parser back-compat sections
- `metadata-bit-driven-derived-filter-framework.md` v1.2 → v1.3 — REMOVE SUPERSEDED Option B body; replace with Option E canonical
- `failure-attribution-buffer-pattern.md` Stage 2 → Stage 3 promotion at ship close
- **`canonical-sister-extension-discipline.md` — NEW § "Temporal evolution + cohort migration of sister registries"** sub-sections (M1a parity verification + M1b sig migration as cohort discipline) — Stage 2 DRAFT landed v1.10 per Meta-gap M1; Stage 3 first canonical reference at Step 0.5d.a.0 (M1b) + `/readiness` Check 36 first invocation (M1a)

**Step 7 — Plan body residual SUPERSEDED ref cleanup:**
- Original `.B` plan body (SUPERSEDED at `.B.1` split) lines 114/234/247/260-265/642/827 + sidecar 247/289/292/558 mechanical cleanup if not already done at `.B.1`/`.B.2`

**Step 8 — `/test-strength-audit` final pass:**
- Anti-regression scan over `.B.1`+`.B.2`+`.B.3` cumulative diff
- Verify no `==` → `>=` weakenings without justification; no deleted `check(...)` without redundancy/obsolescence justification

**Step 9 — Build verify + ship close (extensive; v1.10 EXPANDED with Meta-gap M1 + M2 + M3 auto-writes; v1.12 EXPANDED with Meta-gap M4 + META program landing):**

**v1.12 NEW ship-close auto-writes (META + Phase A landed mid-coding):**

- `tick-trader-percore-workspace/DESIGN_SPECS/meta-disciplines/implementation-layer-blindspot-taxonomy.md` (Stage 2 DRAFT v1.0; 12-category taxonomy B1-B12 + first canonical reference at this ship)
- `tick-trader-percore-workspace/claude-skills/blindspot-scan/SKILL.md` (NEW skill; Layer-2 audit firing after SHAPE audits; walks 12-pillar taxonomy)
- `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/feedback_implementation_detail_blindspot_recovery_via_taxonomy.md` (NEW operator-collaboration rule)
- `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md` index updated for M4 memory link
- `tools/check_field_name_uniqueness.py` (NEW CI tool; Pillar B2 — PASS at HEAD: 163 fields unique across 4 registries)
- `tools/check_storage_t_coverage.py` (NEW CI tool; Pillar B6 — PASS at HEAD: 7 STORAGE_T variants covered)
- `audit-driven-pre-coding-gate.md` AMENDED — `/blindspot-scan` added to audit catalog as IMPLEMENTATION-DETAIL layer
- `claude-skills/precoding-audit-gate/SKILL.md` AMENDED — `blindspot` in extended audit_set + ship-type recommendations
- `claude-skills/readiness/SKILL.md` AMENDED — Checks 36-39 (sister-registry parity / transitional state / include topology / row-order parity)
- `claude-skills/trace-deps/SKILL.md` AMENDED — TYPE-SENSITIVE consumer classification section (Pillar B8)
- `claude-skills/parity-check/SKILL.md` AMENDED — Section M (claim → evidence chain B9) + Section N (row-order parity B12)
- `claude-skills/handoff/SKILL.md` AMENDED — Step 2 pre-coding audit gate extended with SHAPE + IMPLEMENTATION-DETAIL layer split
- `DOCS/DESIGN_PHILOSOPHY.md` § 11.5 NEW — meta-discipline registry (M1+M2+M3+M4) + portal hierarchy + 8-step Mn codification procedure
- `DOCS/DESIGN_PHILOSOPHY.md` § 13 cross-ref index — 4 M-rows added
- `CLAUDE.md` (workspace) RESTRUCTURED — Purpose section + generalized How-To (no sprint-version-specific phasing) + Skill suite by concern (9 categories) + portal hierarchy diagram
- `CLAUDE.local.md` RESTRUCTURED — Meta-disciplines pointer at top + tightened going-forward index (bloated 4-pillar / iteration-spiral / consumer-enum / future-headache rules compressed) + thin sprint state pointer (body moved to MASTER plan) + `/blindspot-scan` row in Skill load contracts
- `DESIGN_SPECS/README.md` — catalog entry for `implementation-layer-blindspot-taxonomy.md` (Discipline / process patterns category)
- `canonical-sister-extension-discipline.md` cross-ref to M4 spec (M1 sister)
- `wire-format-byte-preservation-discipline.md` cross-ref to M4 spec (M2 sister)
- `claude-skills/parity-check/SKILL.md` Section E + E.5 → renamed Section M + N (alphabetic continuation after L; cosmetic ordering fix)
- TECH_DEBT-108 CLOSED (verified H4-compliant exemption — `double` STORAGE_T in voting/statistical rows is documented intentional design per ControllerConfig.hpp:316 + H4 "display-only OK" extension to threshold-only comparison)
- TECH_DEBT-109 NEW (refined scope) — skill SKILL.md drift audit deferred; sampling revealed ~10-30 genuine drift across 17 skills (majority of 250+ refs are legitimate worked examples)

**v1.10 ship-close auto-writes (preserved from v1.11):**

- `./build.sh all` — 5 binaries clean
- Tests GREEN (expect ~3245-3275 with `.B.3` additions)
- Hot path UNTOUCHED — `tools/calls_graph_diff.sh verify`
- CI gates: `check_meta_registry.py` PASS (3 checks; expected ~63 enrolled after deletions) + `check_per_core_registry_integrity.py` PASS + NEW Check 9 PASS
- Version.hpp bump `5.15.5.F.4d.1.B.2` → `5.15.5.F.4d.1.B.3` with full deliverables + **12 TECH_DEBT CLOSED at ship close (UPDATED v1.15 per pre-coding TECH_DEBT audit)**:
  - **TECH_DEBT-001 (NEW v1.15 ADDITION): `tools/stamp_model.sh` bash → C++ wrapper migration** — closes at Phase L (Step 1.6.8'). **This is the OLDEST TECH_DEBT item closed at this ship**; originally tracked from v5.2.3 (script header line 12 documents the planned "Phase 2 (v5.3.x?) replaces this with tools/stamp_model.cpp"). ~3 major versions overdue prior to Phase L's structural-fix execution.
  - **TECH_DEBT-018 (NEW v1.15 ADDITION): Codify `/precoding-audit` Layer 1 orchestrator skill** — closes as `/precoding-audit-gate` skill (canonical at workspace `claude-skills/precoding-audit-gate/SKILL.md`; name evolved during codification). Fired on `.B.3` at v1.14 → v1.15 amendment for 6-audit parallel verification.
  - TECH_DEBT-093 + 094 + 095 + 096 + 097 + 098 + 099 + 100 (8 entries) — cfg-stamp-binding migrations (per phase F sub-step decomposition)
  - TECH_DEBT-102 + 104 — Class 31 detection mechanism + model-state cohort (closed at v1.6 scope expansion + ship close auto-write)
- + **7 NEW TECH_DEBT entries** (101 v1 back-compat deprecation; 103 locale-pin optimization; 105 NEW per M1 — sister-registry sig parity CI tool; 106 NEW per M2 — cross-tool emit parity CI tool; 107 NEW Phase A META landing; 110 NEW Phase L bash-shim deletion target; 111 NEW Phase L CLI flag drift CI defense-in-depth)
- + Decision F NEW + Class 31 + Class 32 codification + **Decision D 15-entry scope (v1.6 expansion; v1.10 doc-stale fixes)** + 2 production + ~12 test consumer migrations + **NEW Step 0.5d.a.0 sister-registry 5→6 col migration (Meta-gap M1b first canonical)** + **NEW Step 1.6.8 → Step 1.6.8' Phase L expansion per v1.15 re-synthesis** (cross-tool elimination via framework-driven CLI binary + X-macro auto-gen + extensibility test)
- Commit with structured message
- `git tag -s -a v5.15.5.F.4d.1.B.3`
- Postmortem at `postmortems/2026-05-18-v5.15.5.F.4d.1.B.3-postmortem.md`
- CHANGELOG.md `.B.3` row
- `CLAUDE.local.md` sprint state row update (Most recent → `.B.3`; Previous → `.B.2`)
- `DOCS/TECH_DEBT.md` — 8 entries CLOSED + 4 NEW (-101 + -103 + -105 + -106; -102 CLOSED at ship close auto-write per skill amendment; -104 CLOSED at v1.6 scope expansion)
- **`DOCS/RECURRING_BUG_PATTERNS.md` — NEW Class 31 + Class 32 codification with "False-positive surface" subsection per Meta-gap M3** (Class 31: Wire-format duplicate-key emit from sister registries; Class 32: Prefixed/unprefixed struct field mirror in X-macro auto-gen registry — explicit text distinguishing duplicate-emit asymmetry from legitimate namespacing prefix per /merge-scan v1.9 RE-SWEEP candidate)
- **`DOCS/RECURRING_BUG_PATTERNS.md` intro polish (NEW v1.10 per Meta-gap M3):** add codification-template note "When adding a new Class, include 'False-positive surface' subsection distinguishing instances from legitimate sibling patterns"
- **NEW feedback memories written at ship close** (6-7 memories total):
  - v1.3 batch (4 memories): `feedback_audit_own_proposals_with_same_rigor` + `feedback_surface_operator_migration_path_proactively` + `feedback_proactive_novel_alternative_consideration` + `feedback_recheck_designspecs_on_pushback`
  - **v1.10 batch NEW (2-3 memories per Meta-gaps M1 + M2 + M3):**
    - `feedback_verify_sister_registry_parity_pre_coding.md` (M1a + M1b composite — sister-registry parity verification + sig migration as cohort discipline)
    - `feedback_enumerate_cross_tool_emit_sites_on_wire_format_change.md` (M2 — comprehensive cross-process emit enumeration when wire-format changes)
    - `feedback_distinguish_anti_pattern_from_legitimate_sibling_at_codification.md` (M3 — LOW priority; could fold into existing `feedback_audit_own_proposals_with_same_rigor` as pillar refinement; judgment call at memory-write time)
  - **v1.13 batch NEW (1 memory per Step 1.6.5b action-parameterized walker landing — already WRITTEN pre-coding per `feedback_plan_right_not_fast` discipline):**
    - `feedback_prefer_action_parameterized_walker_over_per_consumer_walker_bodies.md` (codifies the meta-walker discipline for ≥2 sister consumer template fns sharing registry cohort coverage; drift impossible by construction; cross-ref Class 21 closure precedent at v5.14.2.E.1 PostLoadSetup + Step 1.6.5b first canonical at cfg-derived consumer surface)
- **CLAUDE.local.md going-forward rules amendment** — 4 mandatory sections for plan body amendments (DESIGN_SPECS cross-check / Anti-pattern catalog / Operator migration / Novel alternative) + **2 NEW rules from v1.10 per Meta-gaps M1 + M2:** "Verify sister-registry parity pre-coding when plan body references sister-registry columns/bits/fields" + "Enumerate cross-tool emit sites comprehensively when wire-format changes (engine + CLI + scripts + recording tools)"
- **`/readiness` skill amendment** — Check 31 (anti-pattern catalog rigor) + Check 32 (operator migration surfaced) + Check 33 (novel alternative considered) + Check 34 (comprehensive consumer enumeration for registry-row-deletion scope) + Check 35 (iteration-spiral meta-gap recognition) + **Check 36 NEW (sister-registry parity verified for any plan body claim referencing sister-registry columns/bits/fields per Meta-gap M1a)** + **Check 37 NEW (cross-tool emit-site enumeration done for plans with wire-format changes per Meta-gap M2)**
- **`/trace-deps` skill amendment** — ALWAYS produce comprehensive consumer enumeration as standard trace output when scope includes registry-row deletion / rename / column-1-vs-source-name change. Adds explicit step: "comprehensive consumer grep covering ALL access patterns (dot / arrow / STAMP_HAS / token-paste / macro) codebase-wide for each symbol in deletion scope; report per-file site counts + per-pattern site counts; flag if consumer scope > 30 sites for procedure-based migration approach". **Plus NEW v1.10 per Meta-gap M1a:** verify sister-registry col-count parity across registries with shared consumer X-macros when symbol existence verification is in scope. Sister to existing skill structure (currently focuses on call-sequence + lifecycle).
- **`/parity-check` skill amendment** — Class 31 detection (cross-walker wire-key enumeration; flag wire-key collisions across emitting walkers) per TECH_DEBT-102 closure at ship close. ~1h skill spec amendment. **Plus NEW Section E.5 v1.10 per Meta-gap M2:** scan `tools/*.sh` + cross-process emit sites when wire-format change proposed; report per-file site counts + per-pattern site counts + disposition per site (Layer 7 § per-site disposition framework).
- **`/precoding-audit-gate` skill amendment** — orchestrator synthesis step verifies per-deletion-scope-field consumer enumeration done (relies on `/trace-deps` standard output enrichment above). Rejects synthesis if comprehensive consumer enumeration missing for scope involving registry-row deletion / rename. **Plus NEW iteration-spiral tracking: track plan body amendment iteration count; after 4 iterations with shrinking-but-material findings, flag for META-gap audit asking "what audit METHODOLOGY gap caused this spiral?". Per `feedback_iteration_spiral_signals_audit_meta_gap`.**
- **`audit-driven-pre-coding-gate.md` DESIGN_SPEC amendment** — new section "Iteration spiral as meta-gap signal" codifying the recognition pattern + 5 common meta-gaps (consumer enumeration / cross-pattern access / cross-file-type / sister-registry / naming-asymmetry). Cross-reference `feedback_iteration_spiral_signals_audit_meta_gap.md`. Sister to existing skill orchestration discipline.
- **`future-oriented-plan-template.md` Decisions table** — ADD "Operator migration impact" column + "Comprehensive consumer enumeration" section requirement (procedure-based capture if >30 sites; per-file counts mandatory) + **NEW v1.10 per Meta-gap M2:** "Cross-tool emit-site enumeration" section requirement for plans with wire-format changes (per-file + per-pattern + per-site disposition).
- `tick-trader-percore-workspace/FEATURE_LOOKUP.md` — entry for stamp_format_version SOFT bump + parser back-compat + cross-tool sync discipline
- `/sync-workspace` for off-machine backup
- Push only after operator authorization

### NOT IN scope (explicit deferral)

| Item | Defers to | Rationale |
|---|---|---|
| `FOREACH_STAMP_BOUND_MODEL_CONST` consolidation | Future ship | Different concern (model state vs cfg-derived) |
| CoreCtx INIT/RESET/SUMMARY trio consolidation | `.F.4f` cleanup ship | Different domain |
| Backend-side framework parity | `v5.16+` | Per sub-master out-of-scope |
| GUI live-vs-backtest unification | `v5.16+` | Per sub-master out-of-scope |
| 5 GUI metadata derived filter applications | `.F.4e` | Validates framework via second-source apps |
| AFFECTS_STAMP_PARITY framework consumer | `v5.14.6.C` | Distinct metadata bit |
| Parser back-compat for v1 wire keys deprecation | TECH_DEBT-101; target ship TBD | When production v1 stamps eliminated |
| restamp_model.sh tool | NEVER — per Caramel goal | Decision F (F.2) eliminates need |
| Hot path optimizations | NEVER at `.B.3` | Pure consolidation; H7/H8 preserved |

---

## Steps sequencing (BUILD-FORCED)

**Critical sequencing constraint** (build BREAKS if violated; v1.11 RE-ORDERED per 3-audit synthesis 2026-05-18 — Step 1.5↔1.6.3↔1.6.2 coupling discovered at coding-time + verified by /readiness audit):

**Phase A (LANDED — framework primitives + setup):**
- Step 0 + 0.5a + **0.5d.a.0 (FOREACH_GATE_CFG_FLAG 5→6 col migration MUST land before 0.5d.a)** + 0.5b.A + 0.5b.B + 0.5c (framework primitive extensions) FIRST
- Step 0.5d.a (gate_cfg_flag walker emit) — independent at this phase

**Phase B (LANDED — test count migrations; validates 0.5b primitives):**
- Step 1 (test count-assertion migrations) — independent

**Phase C (LANDED — gap_acceptable_threshold full closure):**
- Step 1.6.1 (TECH_DEBT-093 FULL closure: decl + parser + HAS_SIDE_EFFECT bit + default; auto-defaults mechanism added)

**Phase D (NEW v1.11 — substantive struct-gen unblocker; restored sequencing):**
- **Step 1.6.3 (struct-gen migrations) — LINCHPIN; lands FIRST in this phase. Provides unprefixed `inf.<name>` discrete fields on StampInferenceCfgInputs + ModelStampResult via Decision C Approach A unconditional struct-gen. UNBLOCKS Step 1.6.2 + Step 1.5 + Step 0.5d.b/c/d cascade.**
- **Step 1.6.2 (15-entry cohort bit-add per v1.6 scope; trading_mode verify) — depends on Step 1.6.3 struct fields existing + Step 0.5d.a.0 sister-registry sig migration**
- **Step 0.5d.b (parser-side handling for bitmap-flag wire keys) + 0.5d.c (consumer migration decision) + 0.5d.d (other template fn extensions: drift_check + populate_inference_cfg) — DEFERRED at Session D per WIP-checkpoint comment at CfgGateRegistry.hpp:397-401; now sequenced after Step 1.6.3 provides discrete fields**

**Phase E (NEW v1.11 — INFERENCE_CFG_AUTOPOPULATE swap NOW SAFE):**
- **Step 1.5 (INFERENCE_CFG_AUTOPOPULATE swap; 2 sites) — REORDERED from v1.10 placement before 1.6.3 (which was wrong; would silently drop 16 prefixed fields from wire format). Framework consumer NOW picks up unprefixed equivalents via Step 1.6.3 struct-gen + Step 1.6.2 bit-add.**

**Phase F (NEW v1.11 — wire-format-affecting migrations; tightly coupled):**
- Step 1.6.4 (canonical body emit migration) — MUST land before Step 2; couples tightly with Step 1.6.7
- **Step 1.6.4 + Step 1.6.7.3 (version bump 1→2) MUST land in SAME COMMIT** per /parity-check M-3 — landing 1.6.7.3 before 1.6.4 would mark stamps `stamp_format_version=2` while containing v1 wire keys
- Step 1.6.5 (STAMP_CFG_AUTOPOPULATE swap; 8 sites total) — LANDED at WIP-checkpoint 4 `9e99e91`; MUST land before Step 2 (DONE)
- **Step 1.6.5b NEW v1.13 (action-parameterized meta-walker structural refactor; 5 sites: 4 consumer template fns + 1 struct-gen meta-walker) — independent of wire-format change; X-macro bodies VERBATIM preserve emit order; can land before OR after 1.6.4 + 1.6.7 same-commit trio. Closes Class 21 at consumer template fn surface + absorbs Step 0.5d.d for populate_inf + drift_check.**
- Step 1.6.6 (drift walker migration) + 1.6.6.a (4 CfgDriftCheck row substitutions) + **1.6.6.b (15-row STAMP-side rename per v1.7; NOTE post-rename `h-><name>` reads UNPREFIXED `FPN<F>` field per /parity-check M-1 — consumer-side type binding at drift compare goes through `cfg_drift_compare` which handles FPN<F>↔FPN<F> via memcmp)** — depends on Step 0.5a framework reason_buf extension (LANDED)

**Phase G (NEW v1.11 — SOFT bump SPEC + procedure; couples with Phase F):**
- Step 1.6.7.0 (DESIGN_SPEC amendment Stage 2 DRAFT — SOFT bump procedure section + Layer 7 Cross-tool sync subsection per Meta-gap M2) — at coding start
- Step 1.6.7.1-3 (constant + bounds + bump) — couple with Step 1.6.4
- Step 1.6.7.4 (parser back-compat layer per Decision F for **15 legacy prefixed keys** per Decision D v1.6 scope) — couples with Step 1.6.4
- Step 1.6.7.5 (v1 fixture LOAD test; **15-key fixture per Decision D v1.6 scope**) — after Step 1.6.7.4
- Step 1.6.7.6 (TECH_DEBT-101 entry) — at ship close

**Phase H (NEW v1.11; UPGRADED v1.14 amendment 2026-05-18 — Phase L structural elimination supersedes v1.10 bash-patches):**
- **Step 1.6.8' Phase L (UPGRADED v1.14; SUPERSEDES v1.10 Step 1.6.8 bash patches — structural elimination via framework-driven C++ CLI binary `tools/stamp_model_cli.cpp` replacing `tools/stamp_model.sh`; 6 sub-steps L1-L6 covering DESIGN_SPEC draft + CLI binary + build system + verification + deprecation shim + cross-ref updates) MUST land in SAME COMMIT as Step 1.6.7.3** per Layer 7 § Cross-tool version literal sync discipline (now obviated for this surface; version literal lives ONCE at engine STAMP_FORMAT_VERSION_CURRENT; CLI inherits via framework call). Closes Class 18 + 19 + 21 + 22 at cross-tool surface; drift impossible by construction per `framework-driven-cli-binary-pattern.md` Stage 3 first canonical.

**Phase I (NEW v1.11 — invariant invocation):**
- Step 1.7 (Layer 5b invariant invocation) — after Step 1.6.4

**Phase J (FORCED LAST — legacy registry deletion):**
- **Step 2 (DELETE legacy registries) — FORCED-LAST among migration steps**

**Phase K (ship close):**
- Step 3-9 — after Step 2

**v1.10 BUILD-FORCED list ERROR caught at coding-time (Session D):** Step 1.5 was placed BEFORE Step 1.6.3 but actually DEPENDS ON it. v1.11 RE-ORDERED to correct sequence per /readiness audit synthesis 2026-05-18.

---

## Verification gate

**Universal:**
- `./build.sh test` GREEN; 5 binaries clean
- `/parity-check` GREEN — wire-format byte preservation under SOFT bump (v1 stamps load cleanly; v2 emit produces deterministic bytes)
- `/merge-scan` GREEN — Path γ #3 PARTIAL → MOSTLY per Decision E (E.2)
- `/bug-check` CLEAN — Classes 11/12/14/18/21/24 STRUCTURAL FULL closure; Class 31 + Class 32 instances gone
- `/test-strength-audit` GREEN

**Ship-specific (v1.10 numbers corrected per Decision D 15-entry scope + Meta-gap M1 + M2 auto-writes):**
- `rg "FOREACH_STAMP_BOUND_CFG\b"` returns ZERO production hits (only header SUPERSEDED comment + post-deletion documentation refs)
- `rg "FOREACH_CFG_DERIVED_INFERENCE_CFG\b"` returns ZERO hits codebase-wide
- `rg "STAMP_CFG_AUTOPOPULATE\b"` returns ZERO production-call hits
- `rg "INFERENCE_CFG_AUTOPOPULATE\b"` returns ZERO production-call hits
- **15 prefixed POST_CFG entries deleted** (9 cohort at `StampBoundModelConstRegistry.hpp:454-483` + 1 standalone bandit_blend_ratio at `:296-297` + 5 v1.6 expansion model-state entries at `:281-286/299-302`)
- **FOREACH_GATE_CFG_FLAG migrated 5→6 col per Step 0.5d.a.0 (Meta-gap M1b first canonical); 4 consumer X-macros updated atomically**
- **Phase L (Step 1.6.8') STRUCTURAL ELIMINATION complete (UPGRADED v1.14 → v1.15 per /precoding-audit-gate findings; supersedes v1.10 Layer-7-discipline-applied bash-patches):** NEW `tools/stamp_model_cli.cpp` framework-driven C++ CLI binary (~250-300 LOC; X-macro auto-gen longopts[] + value-receiver struct + parse dispatch via tt::cfg_parse_field; calls `stamp_write_for_model` framework API directly) + NEW `framework-driven-cli-binary-pattern.md` v1.1 Stage 2 DRAFT + NEW `cfg-derived-consumer-framework.md` v1.3 (extensibility test pattern) + Phase L verification tests (round-trip + byte-identity + legacy verification + extensibility test + Layer 5b CLI emit invariants) + `tools/stamp_model.sh` deprecation shim (1-line `exec` redirect; TECH_DEBT-110 deletion target). Closes Class 18/19/21 at cross-tool WIRE EMIT + CLI INTERFACE + TEST layers. Drift impossible by construction: adding new cfg field with stamp-binding = 1 row in master FOREACH_PER_CORE_CFG_FIELD → framework auto-flows wire emit + CLI flag + parse + extensibility test validation.
- v5.14 stamp fixture: loads + drift fires correctly per Step 1.6.7.5
- NEW CI Check 9 static_assert PASS
- **8 TECH_DEBT-09X CLOSED + 4 NEW** (101 v1 back-compat + 103 locale-pin + 105 sister-registry-sig-parity CI + 106 cross-tool-emit-parity CI; 102 + 104 already CLOSED at ship close auto-write)
- NEW Class 31 + Class 32 in `DOCS/RECURRING_BUG_PATTERNS.md` (Class 32 with NEW "False-positive surface" subsection per Meta-gap M3)
- **6-7 NEW feedback memories written** (4 from v1.3 batch + 2-3 from v1.10 batch for Meta-gaps M1/M2/M3 per Step 9 enumeration)
- CLAUDE.local.md going-forward rules amended (4 mandatory sections + **2 NEW rules per Meta-gaps M1 + M2**)
- **`/readiness` skill amended (Checks 31-37 — 7 new checks total: 31/32/33 from v1.3 batch + 34/35 from v1.8/1.9 batch + 36 NEW per M1a + 37 NEW per M2)**
- `future-oriented-plan-template.md` amended (Operator migration column + Comprehensive consumer enumeration section + NEW Cross-tool emit-site enumeration section per Meta-gap M2)
- **2 DESIGN_SPEC amendments landed at v1.10 (Stage 2 DRAFT additions):** `canonical-sister-extension-discipline.md` § Temporal evolution + `wire-format-byte-preservation-discipline.md` § Layer 7 (both Stage 3 first canonical references at `.B.3` ship)

**Hot path:** UNTOUCHED. `tools/calls_graph_diff.sh verify` confirms.

**Replay determinism:**
- cfg roundtrip byte-identical for migrated fields under SOFT v2 bump
- v1 stamps load on `.B.3+` engine bytewise-deterministic via parser back-compat
- v2 stamps load on `.B.3+` engine bytewise-deterministic via framework walker

**HOT_PATH_CHANGELOG:** NONE entry (`.B.3` is slow-path/boot/test only).

---

## TECH_DEBT auto-write expectations

**At `.B.3` ship close, mark CLOSED:**
- TECH_DEBT-093 — gap_acceptable_threshold manual cfg storage cleanup (Step 1.6.1)
- TECH_DEBT-094 — 5 prefixed-only fields bit-add + inf struct unification (Step 1.6.2)
- TECH_DEBT-095 — ModelInference struct-gen migrations (Step 1.6.3)
- TECH_DEBT-096 — Production canonical body emit migration (Step 1.6.4)
- TECH_DEBT-097 — StampHelper.hpp:156 STAMP_CFG_AUTOPOPULATE migration (Step 1.6.5)
- TECH_DEBT-098 — CoreModelZoo drift walker migration + framework reason-buffer extension (Step 1.6.6)
- TECH_DEBT-099 — stamp_format_version SOFT bump procedure (Step 1.6.7)
- TECH_DEBT-100 — per_horizon_barrier_blend ML_CFG_FLAG bit-add (Step 1.6.2 paired)

**At `.B.3` ship close, NEW entries opened:**
- **TECH_DEBT-101 NEW** — Parser back-compat layer for v1 prefixed wire keys; target ship TBD (when production v1 stamps eliminated)
- ~~TECH_DEBT-102 NEW~~ — **CLOSED AT `.B.3` SHIP CLOSE** per Caramel "close at this ship if reduces future headache" directive — `/parity-check` skill spec amendment (cross-walker wire-key enumeration) batches with other skill amendments (Checks 31-33); ~1h auto-write at ship close
- **TECH_DEBT-103 NEW** — Locale-pin optimization in `tt::cfg_emit_field` (no-pin variant when outer Layer 2 pin already active); ~25 cycles per stamp emit eliminated; LOW priority (slow-path performance optimization; NOT future-headache reducer per Caramel optimization-vs-future-headache framework); target ship TBD
- ~~TECH_DEBT-104 — CLOSED AT `.B.3` v1.6 (per Caramel "reduce future headache" directive)~~ — 5 model-state Class 32 instances closed via Step 1.6.2 v1.6 expansion + Step 0.5d framework walker extension
- **TECH_DEBT-105 NEW (v1.10 per Meta-gap M1b)** — Future CI tool `tools/check_sister_registry_sig_parity.py` enforcing sister-registry col-count parity across registries with shared consumer X-macros (e.g., FOREACH_*_CFG_FLAG family). LOW priority defer — discipline alone is sufficient at current cohort scale (3 family members: FOREACH_ML_CFG_FLAG 6-col + FOREACH_GATE_CFG_FLAG 6-col post-`.B.3` + FOREACH_LIFECYCLE_CFG_FLAG 5-col deferred); CI tool warranted when 3+ family members exist with shared consumer pattern AND deferred-with-rationale cohort members exist
- **TECH_DEBT-106 NEW (v1.10 per Meta-gap M2; STATUS-AMENDED v1.14 — partial obviation by Phase L)** — Future CI tool `tools/check_cross_tool_emit_parity.py` comparing wire keys emitted by engine vs CLI helpers. **v1.14 amendment:** Phase L at this ship structurally ELIMINATES the engine↔`tools/stamp_model.sh` cross-tool seam via `tools/stamp_model_cli.cpp` framework-driven CLI replacement. TECH_DEBT-106 scope NARROWS to "verify NO NEW bash scripts emit wire format" (defense-in-depth post-Phase-L); LOW priority defer; CI tool warranted when 2+ framework-driven CLI binaries exist OR a NEW non-framework cross-tool surface emerges.

- **TECH_DEBT-110 NEW (v1.14 per Phase L)** — `tools/stamp_model.sh` deprecation shim deletion target. **What's deferred:** delete the 1-line `exec` shim at `tools/stamp_model.sh` (introduced in Phase L Step L5 to preserve operator workflow continuity during retention period). **Why deferred (not effort-avoidance):** operator scripts/aliases hardcoded to `tools/stamp_model.sh --model X` invocation need migration time to `build/stamp_model_cli --model X`. Typical retention: 1-2 ship cycles (matches Decision F SOFT compat philosophy). **Trigger:** explicit operator confirmation that all CLI invocation sites updated. **Cross-ref:** `framework-driven-cli-binary-pattern.md` § Deprecation shim discipline.

- **TECH_DEBT-111 NEW (v1.15 per Phase L L8)** — CI defense-in-depth check `tools/check_cli_flag_drift.py`. **What's deferred:** Python script that verifies `tools/stamp_model_cli.cpp` uses X-macro auto-gen for `longopts[]` (flags any manual `{"--flag", ...}` entries that bypass the registry walk + would reintroduce Class 21 drift). **Why deferred (not effort-avoidance):** at 1st canonical framework-driven CLI binary, X-macro discipline is enforced by code review + the audit gate; CI tool warranted at 2nd canonical when 2+ framework-driven CLI binaries exist (per `feedback_framework_layer_payoff_diminishing_returns` — pattern earns its place at 2+ applications). **Trigger:** 2nd framework-driven CLI binary lands (e.g., schema migration CLI; per-core override emission CLI). **Cross-ref:** `framework-driven-cli-binary-pattern.md` v1.1 § Audit detection; `registry-coverage-ci-check-pattern.md` Shape B (anti-pattern enforcement CI mechanism category).
- **TECH_DEBT-107 NEW (v1.11 per Step 1.6.1 TECH_DEBT-093 FULL closure investigation)** — Registry default vs manual default audit for 47 globals (mismatches identified at Step 1.6.1 closure; e.g., warmup_ticks registry default=0 but manual=128). Audit + reconcile (either update registry default OR keep manual override + document rationale per field). LOW priority; future-headache reducer via full auto-defaults activation when registry is single source of truth for all 48 globals. Currently auto-defaults via FOREACH_GLOBAL_CFG_FIELD(EMIT_GLOBAL_CFG_DEFAULT) runs first; 47 manual defaults override (no behavior change). Closing TECH_DEBT-107 = audit each of 47 + delete redundant manuals OR update registry defaults to match.

**At `.B.3` ship close, IN-FLIGHT → CLOSED:**
- TECH_DEBT-085 — Thread A FULL framework consolidation umbrella (`.F.4d.1.A` + `.B.1` + `.B.2` + `.B.3` substantially close; `.C` + `.D` round out)

**FEATURE_LOOKUP.md entry at ship close:**
- **Stamp format version 2 SOFT compat (v5.15.5.F.4d.1.B.3+)** — What: stamp_format_version=2 with wire keys reshaped (**15 prefixed `inference_cfg_<name>` keys deprecated at emit per v1.6 scope expansion** — 9 cohort POST_CFG + 1 standalone bandit_blend_ratio + 5 model-state cohort: confidence_threshold_scale + barrier_gate_enabled + confidence_hard_block_threshold + fee_rate_maker + fee_rate_taker; framework emits unprefixed; parser dual-recognizes both). Cfg flags: stamp_format_version is internal. Fallback: PARSER ACCEPTS BOTH versions; v1 stamps continue to load on `.B.3+` engine with NO operator action. **Cross-tool elimination (v1.15 per Phase L; STRUCTURAL UPGRADE from v1.10's Layer 7 discipline-application):** `tools/stamp_model.sh` REPLACED with framework-driven `tools/stamp_model_cli.cpp` that calls `stamp_write_for_model` framework API directly + X-macro auto-gen of CLI flag table from FOREACH_*_CFG_FIELD walkers. Version literal lives ONCE at engine `STAMP_FORMAT_VERSION_CURRENT` (added at Step 1.6.7.1); CLI inherits via framework call. Bash deprecation shim (1-line `exec` redirect) preserves operator workflow continuity per `feedback_surface_operator_migration_path_proactively`. Where to verify: Step 1.6.7.5 hand-written v1 fixture round-trip test + Phase L L4 byte-identity test (CLI emit ≡ engine in-process emit) + Phase L L4 extensibility test (X-macro walker validates all flagged rows). Paper-test sanity: existing trained models work unchanged on `.B.3+`; new training via CLI binary produces v2 stamps; mixed cohort works. Gotchas: parser back-compat is a CLOSED SET (15 keys); engine-downgrade hazard documented (`.B.3+` v2 stamps DON'T load on `.B.2` and earlier — forward-compat works, backward does NOT); future deprecation tracked via TECH_DEBT-101 (parser back-compat) + TECH_DEBT-110 (bash shim deletion target). Related: TECH_DEBT-099, framework consolidation, Class 31 + Class 32 codification, Meta-gaps M1 + M2, Phase L structural elimination (closes Class 18/19/21 at WIRE EMIT + CLI INTERFACE + TEST layers; framework-driven-cli-binary-pattern.md v1.1).

---

## Pre-coding triggers (REQUIRED before promoting DRAFT v1.15 → ACTIVE coding)

1. ✓ Plan body draft complete (this document v1.15)
2. ✓ Batch 1 audit synthesis written at `plan_checks/2026-05-17-v5.15.5.F.4d.1.B.3-audit-synthesis.md` (against v1.2)
2.5. ✓ **Batch 2 RE-SWEEP audit synthesis** written at `plan_checks/2026-05-17-v5.15.5.F.4d.1.B.3-v1.9-resweep-synthesis.md` (against v1.9; 5 audits parallel)
3. ✓ Stage 2 DRAFT `failure-attribution-buffer-pattern.md` on disk
3.5. ✓ **Stage 2 DRAFT extensions landed at v1.10 per Meta-gaps M1 + M2:** `canonical-sister-extension-discipline.md` § Temporal evolution sub-sections + `wire-format-byte-preservation-discipline.md` § Layer 7 sub-section
4. ✓ 6 audits fired Batch 1 + 5 audits fired Batch 2 + both syntheses convergent + meta-gaps identified
5. ✓ Operator triage with full proportionate-response menu + 4 Caramel pushbacks integrated (Batch 1) + 3 meta-gaps codified at v1.10 (Batch 2 RE-SWEEP)
6. ✓ Plan body amended v1.2 → v1.3 → ... → v1.9 → v1.10 → v1.11 (Session D 3-audit synthesis) → v1.12 (Meta-gap M4 mid-coding) → v1.14 (Phase L initial; Decision G + Step 1.6.8') → v1.15 (Phase L re-synthesis; X-macro auto-gen + extensibility test + audit-punch-list amendments per /precoding-audit-gate 6-audit fire) (15 iterations across full lifecycle; multiple meta-gap recognition cycles per `feedback_iteration_spiral_signals_audit_meta_gap`)
7. ✓ DESIGN_SPECS cross-check section (this doc)
8. ✓ Anti-pattern catalog cross-check (Classes 1-30 + NEW 31 + NEW 32) section (this doc)
9. ✓ Operator migration impact section (this doc)
10. ✓ Novel alternative consideration section (this doc)
10.5. ✓ **Meta-gaps surfaced at v1.9 RE-SWEEP section** (this doc; M1 + M2 + M3 with doc-update destinations)
11. Operator final greenlight before pre-coding tag
12. Pre-tag rollback anchor `pre-v5.15.5.F.4d.1.B.3` created
13. CI checks PASS at HEAD (`check_meta_registry.py` + `check_per_core_registry_integrity.py`)
14. `/sync-workspace` for off-machine backup

---

## Cross-references

- Sub-master: `subplans/2026-05-16-v5.15.5.F.4d.1-thread-a-framework-full.md` v1.3
- Predecessor postmortem: `postmortems/2026-05-17-v5.15.5.F.4d.1.B.2-postmortem.md`
- Predecessor audit synthesis: `plan_checks/2026-05-17-v5.15.5.F.4d.1.B.2-audit-synthesis.md`
- THIS ship Batch 1 audit synthesis: `plan_checks/2026-05-17-v5.15.5.F.4d.1.B.3-audit-synthesis.md`
- **THIS ship Batch 2 RE-SWEEP audit synthesis (v1.9 against v1.10 informer):** `plan_checks/2026-05-17-v5.15.5.F.4d.1.B.3-v1.9-resweep-synthesis.md`
- 6 individual Batch 1 audit reports: `plan_checks/{parity-check,trace-deps,readiness,dod-audit,merge-scan,accounting-audit}-2026-05-17-v5.15.5.F.4d.1.B.3.md`
- **5 individual Batch 2 RE-SWEEP audit reports:** `plan_checks/{parity-check,trace-deps,readiness,merge-scan,dod-audit}-2026-05-17-v5.15.5.F.4d.1.B.3-v1.9-resweep.md`
- Sprint roadmap: `ROADMAP-2026-05-17-to-paper-test.md`
- Sister plan stub: `subplans/2026-05-16-v5.15.5.F.4d.1.C-sidecar-bitpacked.md`
- Framework spec (Stage 3 ACTIVE): `tick-trader-percore-workspace/DESIGN_SPECS/framework-patterns/cfg-derived-consumer-framework.md` v1.2 → v1.3 amend
- Sister discipline spec (Stage 3 ACTIVE): `canonical-sister-extension-discipline.md` v1.1
- Plan template (Stage 3 ACTIVE): `future-oriented-plan-template.md` v1.1 → v1.2 amend (operator migration column)
- Wire-format spec: `wire-format-byte-preservation-discipline.md` (SOFT bump procedure section amendment landed)
- Cfg-scope spec: `cfg-scope-discipline.md` (override-inherit section amendment landed)
- Metadata-bit framework spec: `metadata-bit-driven-derived-filter-framework.md` v1.2 → v1.3 body cleanup
- **NEW Stage 2 DRAFT (v1.3 batch):** `failure-attribution-buffer-pattern.md` (on disk; Stage 3 at ship close)
- **NEW Stage 2 DRAFT extensions landed at v1.10 per Meta-gaps M1 + M2:** `canonical-sister-extension-discipline.md` § Temporal evolution + cohort migration sub-sections (M1a + M1b) + `wire-format-byte-preservation-discipline.md` § Layer 7 Cross-tool emit-site enumeration discipline (M2). Stage 3 first canonical references at `.B.3` ship close.
- TECH_DEBT entries: `DOCS/TECH_DEBT.md` (TECH_DEBT-093 through -100 CLOSED + 101 + 103 + 105 + 106 NEW; 102 + 104 already CLOSED)
- Recurring bug patterns: `DOCS/RECURRING_BUG_PATTERNS.md` (NEW Class 31 + Class 32 codification with "False-positive surface" subsection per Meta-gap M3)
- Hard invariants: CLAUDE.md H15 + H17 + H18 + H19 + H20

---

**End of `.B.3` plan body v1.15 DRAFT.** Lifecycle: v1.2 first canonical → v1.3 (4 Caramel pushbacks + Decision F) → v1.4-v1.9 (audit-driven amendments) → v1.10 (3 meta-gaps M1+M2+M3 codified at Batch 2 RE-SWEEP) → v1.11 (Session D 3-audit synthesis; BUILD-FORCED sequence re-ordered) → v1.12 (Meta-gap M4 codified mid-coding; META program restructure) → v1.14 (Phase L initial; Decision G + Step 1.6.8' framework-driven CLI replaces v1.10 bash-patches; closes Class 18/19/21/22 at cross-tool WIRE EMIT layer) → v1.15 (Phase L re-synthesis per /precoding-audit-gate 6-audit fire findings; X-macro auto-gen CLI flag table + extensibility test pattern; closes Class 18/19/21/22 at WIRE EMIT + CLI INTERFACE + TEST layers; full Tier B closure for cfg-stamp-binding surface — "never refactor again" goal achieved at this surface). **READY for operator final greenlight + pre-coding tag.**

Effort estimate: **~26-39h focused** (v1.10's ~22-32h baseline + Phase L v1.14's ~4-6h + Phase L v1.15 re-synthesis +3-4h for X-macro discipline + extensibility test + DESIGN_SPEC v1.0→v1.1 expansion). Bug classes closed STRUCTURALLY: 6+ (Classes 11/12/14/18/19/21/22/24 across multiple layers; Class 31 + Class 32 codified). Plus full Tier B closure at cfg-stamp-binding surface (CLI INTERFACE LAYER + TEST LAYER via X-macro auto-gen + extensibility test). NEW DESIGN_SPECS Stage 2/3: 2 NEW (`failure-attribution-buffer-pattern.md` from v1.3 batch + `framework-driven-cli-binary-pattern.md` v1.1 from v1.14/v1.15) + 4 EXTENDED at v1.10/v1.15 (canonical-sister-extension § Temporal evolution + cross-tool axis; wire-format-byte-preservation § Layer 7 + structural-elimination cross-ref; cfg-derived-consumer-framework v1.2→v1.3 extensibility test pattern; structural-fix-preferred Phase L Reference implementation). NEW meta-gaps codified: 4 (M1 sister-registry parity/migration + M2 cross-tool emit-site enumeration + M3 anti-pattern false-positive surface + M4 implementation-detail audit layer). Operator workflow: unchanged (Decision F eliminates restamp need; Phase L CLI flag interface preserves bash continuity via deprecation shim). TECH_DEBT NET: **-2** (9 CLOSED: -093 through -100 + -102 + -104; 7 NEW: -101 parser back-compat / -103 locale-pin optimization / -105 sister-registry-sig-parity CI / -106 cross-tool-emit-parity CI / -107 Phase A META landing / -110 bash-shim deletion / -111 CLI flag drift CI). Plus 7+ NEW feedback memories codified, 3+ new going-forward rules, 2 new anti-pattern classes, 9 new `/readiness` Checks (31-39 cumulative).
