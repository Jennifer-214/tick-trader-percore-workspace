---
type: handoff
status: active
ship_tag: .E.0.10
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-12-E.0.10-A25-decided-A25-impl-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
required_reading: [register (§Routing map + A1–A31 + the /decision-check verdict §), decision-log D-204/205/206/207/208/209, per-node-position-ownership-model.md, DOCS/STRATEGY_AND_CODING_RULES.md, cfg-scope-discipline.md, CLAUDE.md H22, CODE_MAP (regen at pickup)]
engine_head: 0e1f6d2 (A25+A28 landed; on feat/v5.15-live-readiness; clean)
workspace_head: at or after 7ef81cc (this handoff + the /decision-check Stage 2.5 amendment refresh it — verify "at or after")
coding_status: fix-ship in flight — A9/A19/A1/A3/A8/D-190/A25/A28 LANDED; A24 is the next CODE step (fix-approach decision pending /decision-check)
deletion_scope: none
---

# .E.0.10 handoff — A25+A28 landed, A24 next (2026-06-12 close)

> **Why this handoff:** A25 (per-fill `original_tp` SSoT) + A28 (Position reset) landed + committed (engine `0e1f6d2`, suite 3378/0, independently adversarially-verified). The design dialogue they surfaced (the boot-reconcile collapse → per-node-position-ownership model) is fully captured. The next CODE step is **A24**. Nothing is lost — workspace synced `7ef81cc`.

## State at handoff write
- Engine HEAD `0e1f6d2` (A25+A28), branch `feat/v5.15-live-readiness`, working tree clean. Suite **3378/0**.
- Workspace synced `7ef81cc` (char-test + all design docs pushed).
- Pre-write gate (`check_session_docs.sh`) CLEAN (1 advisory: 5 KNOWN unmarked capital-test `// ADV-REFUTE` blocks — backlog, NOT A25's char-test which IS marked).

## TaskList state at handoff write (recreate verbatim)
| ID | Status | Subject |
|---|---|---|
| #1 | completed | A9 — slippage at fill chokepoint |
| #2 | completed | A25 #5 decision (= option b) |
| #3 | completed | A25 — per-fill original_tp SSoT (+A28; A18 reverted) |
| #4 | pending (NEXT) | A24 — cfg-mutation single-source (H22) |
| #5 | pending | A16 — partial-fill state-gate |
| #6 | pending | F-059 — exit golden-master (AFTER A24/A25) |
| #7 | pending | A6 — chokepoint gate-emit validity guard |
| #8 | completed | TD-186 harvest (spec v1.3 + RBP Class 45) |
| #9 | pending | H22 spec (per-node-purity-scale-invariance.md, at A24 close) |

## THE NEXT ACTION — A24 (cfg-mutation single-source, H22; HIGH)

**What A24 fixes (register A24):** `RebuildOneCore` mutates `volume_multiplier`/`entry_offset_pct`/`spacing_multiplier` on the FLAT `resolved_cfg` (`ControllerEventLoop.hpp:2360/2426/2978`), but every production consumer reads the boot-frozen per-core shadow `cores[slot]` (`:2700/:2998`) or strategy-owned `live_*` → the mutations NEVER reach a live consumer. Session vol-mult, the **D10 P&L-regression filter-tighten safety loop (default-ON)**, spike relaxation — all silently inert. Regression from the per-core migration (`49649b8`). The literal H22 CI-check candidate.

**FIX-APPROACH DECISION — run `/decision-check` FIRST** (the binding adversarial-default + the operator's "run investigative/adversarial checks"):
- (a) mutate `resolved_cfg.cores[slot]` directly (single-source the per-core view), OR
- (b) sync the mutated override-eligible fields to `cores[slot]` before the consumer.
The register leans (a). **Run `/decision-check` ROUTING the agents to the surface-matched skills** (the NEW Stage 2.5 landed this session): A24 = cfg-flow/H22 → `/trace-deps` + `/dod-audit` + `cfg-scope-discipline.md` (+ Class 25/26/27). Consult Caramel before coding.

**Co-deliverable — the H22 spec (task #9):** author `DESIGN_SPECS/data-disciplines/per-node-purity-scale-invariance.md` — CLAUDE.md's **H22 row DANGLING-REFERENCES it** (the file does not exist; D-193 task #21 owed). A24 is its canonical worked example. Author at A24 close.

**Verify:** 3-lens char-test (`characterization-test-discipline.md`) + `// ADV-REFUTE` + `run_all_tests.sh --fast` + independent adversarial verify (resolve agent code-fact disagreements by READING the code — AR-11).

## Then the rest of the fix-ship (in order)
A16 (partial-fill state-gate — gate HandleFill + slot-free on `ORDER_FILLED` not `event_log_mode`; fold A17) → F-059 (exit-chain golden-master, AFTER A24/A25; freeze-flags the #5 replay divergence + A9/A10) → A6 (chokepoint gate-emit tp/sl-validity guard). Each = fix + 3-lens char-test + `// ADV-REFUTE` + run_all_tests + surface-matched adversarial verify.

## GOING-FORWARD REFINEMENT (landed this session — APPLY it)
`/decision-check` now has **Stage 2.5 — surface-matched skill routing** (MANDATORY): route the investigative + adversarial agents to the DOMAIN skill matching the decision's MATERIAL (money→`/accounting-audit`; hot-path→`/hft-audit`; cfg/registry→`/trace-deps`+`/dod-audit`+`cfg-scope-discipline`; ML→`/ml-audit`; train-serve→`/parity-check`), not just the generic lens. Apply it firing A24's checks (+ every subsequent fix's).

## What landed this session (PREDECESSOR CONTEXT — A25+A28)
- **A25** (per-fill `original_tp` SSoT): carry leg-effective `tp_pct` Async-submit → `SubmitCommand` → `Order::pre_resolved` → `handle_buy_fill` (`original_tp = fill×(1+tp_pct)`); `Order` 256→272 (in-flight SPSC-only, cascade-checked byte-safe; pre-commit Check H/K green). #5 = option (b) (F-059 freeze-flag). engine `0e1f6d2`.
- **A28/TECH_DEBT-182** (CLOSED): `Position_Reset` SSoT — full-struct reset closes the subset-zeroing class (sister A19).
- **A18 REVERTED**: the unconditional buy-on-active-slot guard flips the boot-reconcile collapse last-write→first-write (broke `tsa-live-2`) → re-homed to **TECH_DEBT-188** + `per-node-position-ownership-model.md` (venue-net reconcile @`.E.1`).
- **Design captured:** LIVING DESIGN_SPEC `data-disciplines/per-node-position-ownership-model.md` (boot-reconcile root cause + venue-net foundation @`.E.1` + the gated sub-pool **future-expansion**) + TECH_DEBT-188 + **D-209** + **D-208** (`/decision-check` front-load-refuted verdict) + **TECH_DEBT-186 harvest** (decision-time-data-binding v1.3 + RBP **Class 45**) + vocab (`future-expansion`/`reconcile-recovery`/`scale-invariance`).
- char-test 5-check 3-lens + independent adversarial verify **TRUSTWORTHY**. Suite 3378/0.

## Critical pickup-time reads (nav-infra, first-class)
- The disposition **register** — A1–A31 routing map + the **A24 row** + the `/decision-check` verdict §.
- **decision-log D-204..D-209** (A25 carry, #5, the front-load verdict, the position-ownership design + A18 revert).
- **`per-node-position-ownership-model.md`** — the venue-net/`.E.1` design (so you know what A18 deferred TO + the boot-reconcile collapse root cause).
- **CODE_MAP** — regen `./tools/gen_code_map.sh` at pickup.
- Path-discipline: `DOCS/STRATEGY_AND_CODING_RULES.md` + `cfg-scope-discipline.md` (A24 touches per-core cfg) + CLAUDE.md H22.

## Paste this into a fresh session

```
I'm picking up .E.0.10 (the net-1 correctness fix-ship) for v5.15-live-readiness.

Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-12-E.0.10-A25-A28-landed-A24-next-handoff.md

This is a fresh context — do NOT trust prior-session memory; verify against current code.

After PICKUP-READY: the next fix is A24 (cfg-mutation single-source, H22 — RebuildOneCore mutates
the flat resolved_cfg but consumers read the boot-frozen cores[slot]; the default-ON D10 safety loop
is dead). Run /decision-check on the fix-approach (mutate cores[slot] directly vs sync-to-cores[slot]),
ROUTING the agents to the surface-matched skills (/trace-deps + /dod-audit + cfg-scope-discipline per
the NEW Stage 2.5). Consult Caramel on findings before coding. Co-deliver the H22 spec
per-node-purity-scale-invariance.md (CLAUDE.md H22 dangling-references it). Then A16 -> F-059 -> A6.
```

## Quick links
- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- Decision log: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-204..D-209)
- Register: `plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md`
- Position-model design: `DESIGN_SPECS/data-disciplines/per-node-position-ownership-model.md`
- Predecessor handoff: `plans/v5.15-live-readiness/handoffs/2026-06-12-E.0.10-A25-decided-A25-impl-handoff.md` (superseded)
