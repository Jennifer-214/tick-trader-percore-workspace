# v5.15.5.F.4d.1.B.3 — Step 1.6.5 walker-refactor handoff (cross-session pickup)

**Created:** 2026-05-18 (mid-Step-1.6.5 mid-coding; structural-fix analysis surfaced action-parameterized walker pattern as Class 21 closure shape for sister-consumer drift)
**Target ship:** `v5.15.5.F.4d.1.B.3` — Legacy empty-out (Step 1.6.5 macro swap + NEW Step 1.6.5b walker refactor + downstream cascade)
**Branch:** `feat/v5.15-live-readiness`
**Pre-tag rollback anchor:** `pre-v5.15.5.F.4d.1.B.3` at engine commit `9b62a72`
**Plan file:** `plans/v5.15-live-readiness/subplans/2026-05-17-v5.15.5.F.4d.1.B.3-legacy-empty-out.md` (v1.12 — pending v1.13 amendment per this handoff's structural-fix finding)
**Sprint MASTER:** `plans/v5.15-live-readiness/MASTER.md`
**Predecessor handoff:** `plans/v5.15-live-readiness/handoffs/2026-05-18-v5.15.5.F.4d.1.B.3-step-1.6.5-coding-handoff.md` (Session E pickup; THIS handoff supersedes for the structural-fix finding)

---

## 1. Engine state at handoff write time (VERIFY ON COLD-PICKUP)

| Field | Value |
|---|---|
| HEAD commit | `3298ab2` (WIP-checkpoint 3) |
| Branch | `feat/v5.15-live-readiness` |
| Latest tag | `pre-v5.15.5.F.4d.1.B.3` (rollback anchor) |
| Version.hpp | `5.15.5.F.4d.1.B.2` (unbumped; auto-write at Step 9) |
| Build errors baseline | **224** (matches plan-body expectation; pre-Step-1.6.5 build state) |
| CI tools | All 5 PASS (`check_meta_registry` / `check_per_core_registry_integrity` / `check_field_name_uniqueness` / `check_storage_t_coverage` / `check_struct_field_uniqueness`) |
| WIP-checkpoint commits | 3 (`c13c127` / `a406120` / `3298ab2`) |

---

## 2. Working tree state (UNCOMMITTED at handoff write)

**Two files modified — Step 1.6.5 partial work (macro swap + comment updates per Option A inline-merge):**

```
M ML_Headers/StampHelper.hpp
M tests/controller_test.cpp
```

**Per-file change summary:**

### `ML_Headers/StampHelper.hpp:149-156` — production-site macro swap + comment block update

- Comment block at `149-155` rewritten to reflect framework-driven mechanism (was: "Walks FOREACH_STAMP_BOUND_CFG and populates inf.<field> + has_<field> per the registry's emit_when guard"; now: "Walks master FOREACH_PER_CORE_CFG_FIELD + FOREACH_GLOBAL_CFG_FIELD filtered by STAMP_BOUND_CFG_DERIVED metadata bit; populates inf.<field> + has_<field> per the per-field cohort gate from cfg_gate::lookup_populate")
- Line 156: `STAMP_CFG_AUTOPOPULATE(inf, cfg);` → `INFERENCE_CFG_POPULATE_FROM_DERIVED(inf, cfg);`
- Migration-note line appended: "Migrated from legacy STAMP_CFG_AUTOPOPULATE at v5.15.5.F.4d.1.B.3 Step 1.6.5 (closes Class 18 mirror at inf-struct surface; cohort framework consolidation)."

### `tests/controller_test.cpp` — 7 macro swaps + test banner/printf update

- Lines 4810-4815: test section banner + printf banner updated to reflect migrated macro name + retain v5.14.1.E.E.B historical marker
- Lines 4825, 4845, 4863, 22305, 22326, 22737, 22748: `STAMP_CFG_AUTOPOPULATE(inf, cfg);` → `INFERENCE_CFG_POPULATE_FROM_DERIVED(inf, cfg);` (full-token replace_all; verified-safe per `feedback_avoid_substring_replace_all_on_member_access`)
- Note: 5 comment-only references at `4810`/`4815` updated; `22263`/`22295`/`22729`/`25281`/`25318` test-body comments NOT YET UPDATED (deferred to Step 1.6.5b walker-refactor commit OR to Step 2 legacy-deletion commit)

### Operator decision pending (at session start)

Three choices for handling the working tree at cold-pickup:

- **(a) Commit as WIP-checkpoint 4 before structural-fix refactor.** Captures Step 1.6.5 macro-swap work as a clean rollback anchor; cold session starts with clean working tree. Commit message: `v5.15.5.F.4d.1.B.3 WIP-checkpoint 4: Step 1.6.5 macro swap (8 sites; partial — walker refactor to follow)`.
- **(b) Keep dirty; absorb into Step 1.6.5b commit.** Walker-refactor commit includes both the macro swap + the refactor. Single commit captures full Step 1.6.5 closure.
- **(c) Revert + restart per structural-fix design.** Discard current macro swap; re-do as part of walker-refactor scope. Only warranted if the walker-refactor design changes the macro swap's correctness (it does NOT per current analysis — INFERENCE_CFG_POPULATE_FROM_DERIVED is the right target name; the refactor changes what's INSIDE that macro, not the callers).

**Recommendation: (a) — WIP-checkpoint 4 for clean rollback anchor.** Matches the multi-session rollback-anchor pattern established at .B.3 (checkpoints 1/2/3). The macro swap is structurally independent of the walker refactor; preserving as separate checkpoint allows surgical revert if walker refactor needs rework.

---

## 3. CRITICAL STRUCTURAL FINDING (read first — supersedes prior handoff's Step 1.6.5 scope)

**Session E (mid-Step-1.6.5) discovery: sister-consumer asymmetric registry coverage = Class 21 instance at consumer template fn level.**

### 3.1 — The bug

The 3 cfg-derived consumer template fns at `MemHeaders/CfgGateRegistry.hpp` have ASYMMETRIC registry coverage:

| Template fn | Walks | Missing |
|---|---|---|
| `populate_stamp_cfg_from_derived` (line 258) | per-core + global + ml_cfg_flag + gate_cfg_flag | — (complete) |
| `drift_check_from_derived` (line 331) | per-core + global + ml_cfg_flag | gate_cfg_flag (line 397: Step 0.5d.d DEFERRED) |
| `populate_inference_cfg_from_derived` (line 228) | per-core + global ONLY | **ml_cfg_flag + gate_cfg_flag MISSING** |

Step 0.5d.d (Task #3 pending) was scoped to extend populate_inf + drift_check to walk the missing registries. But the **shape of the bug** is broader — it's a recurring Class 21 instance at the consumer-template-fn level. Plan body Phase D Step 0.5d.d patches THIS instance; the structural fix prevents the recurrence class.

### 3.2 — Why this matters (recurrence vectors)

Drift will recur every time:

1. **New consumer template fn added** (e.g., future `reconcile_from_derived`) — author must remember to walk all 4 registries; sister-cohort discipline operator-memory-dependent
2. **New registry added to the cohort** (e.g., future `FOREACH_LIFECYCLE_CFG_FLAG` with STAMP_BOUND_CFG_DERIVED rows) — all existing consumers need new walker; sister-extension operator-memory-dependent
3. **New metadata bit activates a new cohort** (e.g., AFFECTS_STAMP_PARITY for training-side stamps) — new family of consumers needs same coverage discipline

The bug existed at HEAD because incremental ship-by-ship extension (`.B.1`: 2 walkers per consumer; `.B.2`: extended populate_stamp + drift_check with ml_cfg_flag; `.B.3`: extended populate_stamp with gate_cfg_flag) didn't apply the discipline uniformly. populate_inf fell behind in 2 ships' worth of extensions.

### 3.3 — Audit-methodology gap (M4 / Pillar B9 instance)

The /trace-deps audit at `plan_checks/trace-deps-2026-05-18-step-1.6-pre-coding.md` line 67-77 verified Step 1.6.5 sites + concluded "Mechanical migration safe" — but only checked SYMBOL EXISTENCE + LINE NUMBERS, NOT framework-coverage parity vs the legacy macro. This is a B9 (unverified audit claim) instance — claim → evidence chain didn't reach the actual walker body comparisons.

The M4 implementation-detail audit layer would catch via /blindspot-scan Pillar B9 IF the audit explicitly compared what each macro touches at field-level. Adding to /trace-deps Section M for future amendment (deferred to next ship per `feedback_iteration_spiral_signals_audit_meta_gap` inflection check).

### 3.4 — Class 21 closure precedent in this codebase

Per `DOCS/RECURRING_BUG_PATTERNS.md` Class 21 (line 1371-1404): "Multiple parallel descriptors for similar surfaces." The canonical closure shape is **ONE single source of truth + many consumer views that walk it uniformly** with compile-time enforcement.

**Reference precedent — Class 18/21 closure at boot/backtest/hot-swap surfaces (v5.14.2.E.1):**

> The class extinguished structurally for the model-load surface area by v5.14.2.E.1's `EnsembleModelZoo_PostLoadSetup` + `CoreModelZoo_PostLoadSetup` helpers + `FOREACH_ENSEMBLE_POST_LOAD` / `FOREACH_SINGLE_ZOO_POST_LOAD` X-macro registries. Adding a new post-load step is ONE line in the registry; boot, backtest, hot-swap inherit automatically. Compile-time enforced inclusion at all sites; bypass impossible.

Same shape applies to the cfg-derived consumer drift. The structural fix is an action-parameterized walker macro family where the meta-macro is the single source of truth for cohort coverage; consumers can't omit a registry because the meta-macro expands to all 4 walker invocations regardless of consumer.

---

## 4. REFERENCE: action-parameterized walker structural fix design

**FULL DESIGN REFERENCE lives in companion doc:**
**`plans/v5.15-live-readiness/handoffs/2026-05-18-v5.15.5.F.4d.1.B.3-step-1.6.5-walker-refactor-design-reference.md`**

That doc contains:
- Problem statement + recurrence vectors (Section 1)
- Class 21 closure precedent (Section 2; references RECURRING_BUG_PATTERNS line 1371-1404 + v5.14.2.E.1 PostLoadSetup precedent)
- Pattern shape with full C++ code samples (Section 3 — `FOREACH_STAMP_BOUND_DERIVED_COHORT` meta-macro + per-scope X-macro definitions + structural-enforcement explanation)
- Alternatives β1-β4 considered + rejected with rationale (Section 4)
- Mapping to existing canonical patterns (Section 5)
- Risk + mitigation (Section 6)
- Scope + effort per sub-step (Section 7)
- Ship-close auto-write items REVISED (Section 7 — 2 additions to plan body v1.12 Step 9 list)
- Residual uncertainties (Section 8)
- Cross-references (Section 9)

The 4 subsections below (4.1 through 4.8) SUMMARIZE the reference doc; READ THE REFERENCE for full design + code samples + alternatives analysis.

### 4.1 — Pattern shape (summary)

**Build a parameterized X-macro walker family in `MemHeaders/CfgGateRegistry.hpp`** that abstracts the 4-registry walk + per-row gate logic into a shared mechanism; consumers supply per-row action macros via token-paste-by-naming-convention.

```cpp
// In MemHeaders/CfgGateRegistry.hpp — NEW walker macro family

// Per-scope X-macro names follow convention: X_<CONSUMER>_<SCOPE>
// where SCOPE ∈ {PER_CORE, GLOBAL, ML_CFG_FLAG, GATE_CFG_FLAG}

// Meta-macro — single source of truth for STAMP_BOUND_CFG_DERIVED cohort coverage:
// Consumer passes BASE token (e.g., X_INFERENCE_CFG_POPULATE); macro pastes the 4 scope suffixes.
#define WALK_ALL_STAMP_BOUND_DERIVED(BASE_X_MACRO)                                  \
    FOREACH_PER_CORE_CFG_FIELD(BASE_X_MACRO##_PER_CORE)                             \
    FOREACH_GLOBAL_CFG_FIELD(BASE_X_MACRO##_GLOBAL)                                 \
    FOREACH_ML_CFG_FLAG(BASE_X_MACRO##_ML_CFG_FLAG)                                 \
    FOREACH_GATE_CFG_FLAG(BASE_X_MACRO##_GATE_CFG_FLAG)

// Structural enforcement: consumer MUST define all 4 X-macros (PER_CORE / GLOBAL / ML_CFG_FLAG /
// GATE_CFG_FLAG) for WALK_ALL_STAMP_BOUND_DERIVED to compile. Missing any X-macro = compile error
// at that FOREACH expansion site. Drift impossible.
```

### 4.2 — Per-consumer X-macro definitions (the action functors)

Each consumer defines 4 X-macros following the naming convention. Internal body identical to current per-consumer-per-registry walker (verbatim):

```cpp
// Consumer 1: populate_inference_cfg_from_derived
template <unsigned F, typename InfT>
inline void populate_inference_cfg_from_derived(InfT& inf, const ControllerConfig<F>& cfg) {
    (void)inf; (void)cfg;

    #define X_INFERENCE_CFG_POPULATE_PER_CORE(STORAGE_T, KIND_TOKEN, name, label, section, meta, payload, tooltip, applies_strat, applies_op, applies_regime, applies_risk, lives_in_struct) \
        if constexpr (((meta) & CfgFieldDescriptor::STAMP_BOUND_CFG_DERIVED) != 0) {           \
            constexpr size_t _idx = FIELD_IDX_PER_CORE_##name;                                  \
            const bool _gate = cfg_gate::lookup_populate(_idx, /*is_per_core*/true, cfg);       \
            tt::cfg_populate_inf_field(cfg.name, inf.name, inf.has_##name, _gate);              \
        }

    #define X_INFERENCE_CFG_POPULATE_GLOBAL(STORAGE_T, KIND_TOKEN, name, label, section, meta, payload, tooltip, applies_strat, applies_op, applies_regime, applies_risk, lives_in_struct) \
        if constexpr (((meta) & CfgFieldDescriptor::STAMP_BOUND_CFG_DERIVED) != 0) {           \
            constexpr size_t _idx = FIELD_IDX_GLOBAL_##name;                                    \
            const bool _gate = cfg_gate::lookup_populate(_idx, /*is_per_core*/false, cfg);      \
            tt::cfg_populate_inf_field(cfg.name, inf.name, inf.has_##name, _gate);              \
        }

    // NEW per refactor: ml_cfg_flag bitmap-bit walker (currently MISSING at HEAD)
    #define X_INFERENCE_CFG_POPULATE_ML_CFG_FLAG(NAME, legacy_field, display_label, section, metadata_flags, doc) \
        if constexpr (((metadata_flags) & CfgFieldDescriptor::STAMP_BOUND_CFG_DERIVED) != 0) { \
            const int _bit_val = BITMAP_IS_SET(cfg.ml_cfg_flags, MASK_ML_CFG_##NAME) ? 1 : 0;  \
            inf.has_##legacy_field = 1;                                                         \
            inf.legacy_field = _bit_val;                                                        \
        }

    // NEW per refactor: gate_cfg_flag bitmap-bit walker (currently MISSING at HEAD)
    #define X_INFERENCE_CFG_POPULATE_GATE_CFG_FLAG(NAME, legacy_field, display_label, section, metadata_flags, doc) \
        if constexpr (((metadata_flags) & CfgFieldDescriptor::STAMP_BOUND_CFG_DERIVED) != 0) { \
            const int _bit_val = BITMAP_IS_SET(cfg.gate_cfg_flags, MASK_GATE_CFG_##NAME) ? 1 : 0; \
            inf.has_##legacy_field = 1;                                                         \
            inf.legacy_field = _bit_val;                                                        \
        }

    WALK_ALL_STAMP_BOUND_DERIVED(X_INFERENCE_CFG_POPULATE)

    #undef X_INFERENCE_CFG_POPULATE_PER_CORE
    #undef X_INFERENCE_CFG_POPULATE_GLOBAL
    #undef X_INFERENCE_CFG_POPULATE_ML_CFG_FLAG
    #undef X_INFERENCE_CFG_POPULATE_GATE_CFG_FLAG
}
```

Consumer 2 (`populate_stamp_cfg_from_derived`) + Consumer 3 (`drift_check_from_derived`) follow same shape — each defines 4 X-macros + invokes `WALK_ALL_STAMP_BOUND_DERIVED(X_<base>)` once. Current populate_stamp_cfg per-registry walker bodies (lines 263-321) become the X-macro bodies verbatim; current ml_cfg_flag walker for drift_check (line 383) becomes that X-macro's body. The missing walkers (ml_cfg_flag + gate_cfg_flag for populate_inf; gate_cfg_flag for drift_check) get NEW X-macros following the same shape.

### 4.3 — Why structural enforcement actually works

The compile-time enforcement is **token-paste-by-convention**: `WALK_ALL_STAMP_BOUND_DERIVED(X_INFERENCE_CFG_POPULATE)` expands to four `FOREACH_<REGISTRY>(X_INFERENCE_CFG_POPULATE_<SCOPE>)` invocations, where each FOREACH then invokes the named X-macro. If the consumer doesn't define `X_INFERENCE_CFG_POPULATE_ML_CFG_FLAG`, the preprocessor expansion produces undefined identifier in the macro body — **compile error at the FOREACH expansion site**.

Drift impossibility:
- Consumer **cannot** silently skip a registry. The meta-macro expands to 4 walker invocations unconditionally.
- The 4 X-macros must exist by naming convention. Missing one = compile fail.
- The X-macros themselves can be no-op for legitimate skip-cases (consumer documents rationale), but the skip is EXPLICITLY VISIBLE.

### 4.4 — Why this shape is the right one (alternatives considered)

**Alternative β1: Single C++ template fn parameterized by an Action functor (no X-macro).** Requires runtime field access via descriptor offset → re-introduces Class 23 anti-pattern (typed access via offset). Rejected.

**Alternative β2: Three independent walker bodies + CI tool checking coverage parity.** What my prior proposal was. Adds discipline layer + CI tool; drift catchable but possible. The action-parameterized walker is structurally simpler AND prevents the bug class. Rejected per Class 21 closure precedent.

**Alternative β3: Refactor to use CFG_FIELD_FOR_EACH_SET_BIT (per `metadata-bit-driven-derived-filter-framework.md` Option E).** Works for runtime-typed access (GUI render reads descriptor metadata). Doesn't apply to cfg-derived consumers needing compile-time field access (cfg.name / inf.name / handle.name). Rejected per type-safety constraint.

**Alternative β4: Manual sister-extension only (the option α from prior consult).** Extend populate_inf + drift_check to walk missing registries; no walker family. Patches THIS instance; future drift catchable only by audit discipline. Rejected per `feedback_structural_fix_for_recurring_class`.

### 4.5 — Mapping to existing canonical patterns

| Pattern | How this fix uses it |
|---|---|
| `autopopulate-pattern-for-production-caller-class.md` | Parent pattern — production-caller class with companion X-macro walker. This refactor extends with action-parameterization at the walker-family level |
| `x-macro-registry-with-presence-dispatch.md` § Y3 dispatch | X-macro action functors invoked via token-paste-by-name; the per-scope X-macros ARE Y3 dispatch handlers per scope |
| `cfg-derived-consumer-framework.md` v1.1 | This fix codifies the implicit "ONE master + many consumer views" invariant via structural enforcement; amendment to v1.2 documents the action-parameterized variant |
| `canonical-sister-extension-discipline.md` § INLINE MERGE | Folds 3 per-consumer walker bodies into 1 shared walker macro family; INLINE MERGE per expanded menu |
| `meta-registry-pattern-for-codebase-registry-discipline.md` | `WALK_ALL_STAMP_BOUND_DERIVED` is a Level-1 meta-macro (single source of truth for cohort coverage); enrolled in `FOREACH_REGISTRY` if it survives codification as a registry-shaped artifact |

**No new framework layer.** No new DESIGN_SPEC body. The pattern is documented as an extension to existing `cfg-derived-consumer-framework.md` at ship close.

### 4.6 — Risk + mitigation

| Risk | Mitigation |
|---|---|
| Wire-format byte preservation under refactor (populate_stamp_cfg is the load-bearing emit walker) | X-macro bodies are VERBATIM copies of current walker bodies; emit order preserved by FOREACH walker semantics (same registry → same order); Layer 5b invariants `tests/wire_format_invariants.hpp` I1-I5 catch any regression; Step 1.6.7.5 v1 LOAD test catches HMAC drift |
| Per-scope X-macro signature mismatch (per-core/global have 13-tuple; ml_cfg_flag/gate_cfg_flag have 6-tuple) | Per-scope X-macros take per-scope signature verbatim; the action functor IS scope-specific; matches current walker-body-per-scope pattern at HEAD (populate_stamp_cfg lines 263/278/297/313) |
| Preprocessor naming convention fragility (typo in BASE token = silent dispatch to undefined macros) | Compile-time error at FOREACH expansion site catches typos immediately; very low risk |
| Macro expansion debuggability | `#undef` at end of each consumer fn limits macro scope; compiler error messages identify the FOREACH site cleanly; X-macros are domain-standard in this codebase |
| Step 0.5d.d obviation (Task #3 sub-step) | The refactor INCLUDES the gate_cfg_flag walker addition for populate_inf + drift_check; Step 0.5d.d sub-steps .b/.c (parser dispatch + consumer migration decision) remain separate concerns |

### 4.7 — Scope at this ship

| Action | Effort | Risk |
|---|---|---|
| Build `WALK_ALL_STAMP_BOUND_DERIVED` meta-macro + per-scope walker design verification | ~30 min | LOW (mechanical sister-pattern verification) |
| Refactor `populate_stamp_cfg_from_derived` to use new walker family (keeps current 4-registry coverage; structural unification) | ~45 min | MED (wire-format byte preservation; verified via Layer 5b invariants + I1-I5 tests post-refactor) |
| Refactor `populate_inference_cfg_from_derived` to use walker family + add ml_cfg_flag + gate_cfg_flag X-macros (THIS closes the current instance) | ~45 min | LOW (boot-time only; not wire-format) |
| Refactor `drift_check_from_derived` to use walker family + add gate_cfg_flag X-macro (closes Step 0.5d.d sub-step) | ~30 min | LOW (slow-path drift comparison; logic verbatim) |
| Build verify + run wire-format invariant tests (`./build.sh test`; verify error count drops from ~224 + invariants pass) | ~30 min | LOW |
| **Total** | **~3-3.5h** | **bounded** |

### 4.8 — Ship-close auto-write items (REVISED from plan body v1.12 Step 9)

Per this finding, ship-close auto-write list at Step 9 gets **2 additions** (not new infrastructure — refinement of existing items):

| Item | Status | Effort | Rationale |
|---|---|---|---|
| **AMEND `cfg-derived-consumer-framework.md` v1.1 → v1.2** | NEW | ~30 min | Document action-parameterized walker pattern as canonical for ≥2 sister consumer template fns sharing registry cohort coverage. Sister-section to existing "Adding a new consumer concern" with the meta-macro structural-enforcement variant. |
| **NEW memory rule `feedback_prefer_action_parameterized_walker_over_per_consumer_walker_bodies.md`** | NEW | ~15 min | "When ≥2 consumer template fns walk the same registry cohort with different per-row actions, prefer action-parameterized walker macro family over per-consumer walker bodies. Drift becomes structurally impossible (meta-macro = single source of truth for coverage). Codified at .B.3 v1.13 mid-coding after sister-consumer drift caught in populate_inf vs populate_stamp coverage." Sister to `feedback_structural_fix_for_recurring_class` + `feedback_audit_canonical_sister_before_new_infra`. |

**NOT added (obviated by structural fix):**

- ❌ NEW H invariant — pattern enforces by construction
- ❌ NEW DESIGN_SPEC — existing `cfg-derived-consumer-framework.md` covers via v1.2 amendment
- ❌ NEW meta-discipline row M5 in § 11.5 — M4 catches audit-methodology gap; structural fix prevents bug class
- ❌ NEW CI tool — drift impossible by construction
- ❌ NEW /readiness Check — no plan-time discipline to verify (structural enforcement is automatic)

**Note on Caramel's "why dropping the to-do list" question:**

The items above with ❌ are items from my SESSION E PROPOSAL (mid-Step-1.6.5 over-engineered response), NOT from the existing plan body v1.12 Step 9 ship-close auto-write list. The plan body v1.12 Step 9 list stands; this handoff only adds 2 items (spec amendment + memory rule) and explicitly drops items I had over-proposed earlier in the same conversation. No existing ship-scope items are being dropped.

---

## 5. Updated TaskList state (RECREATE AT FRESH-SESSION PICKUP)

| ID | Status | Subject |
|---|---|---|
| #1 | in_progress | Step 1.6.3 — ModelInference struct-gen migrations (build-GREEN gate) |
| #2 | pending | Step 1.6.2 — 15-entry cohort bit-add + 149-site consumer migration (Phase G) |
| #3 | pending | Step 0.5d.b/c/d — gate_cfg_flag walker parser dispatch + consumer decision + drift **(.d portion ABSORBED by NEW Task #13 walker-refactor; .b + .c remain pending)** |
| #4 | pending | Step 1.5 — INFERENCE_CFG_AUTOPOPULATE swap (Phase H) |
| #5 | pending | Step 1.6.4 — Production canonical body emit migration (Phase F; SAME COMMIT as Step 1.6.7+8) |
| #6 | in_progress | Step 1.6.5 — STAMP_CFG_AUTOPOPULATE callers migration (8 sites; macro swap LANDED in working tree; AWAITING walker-refactor at Task #13 for full closure) |
| #7 | pending | Step 1.6.6 — CoreModelZoo drift walker migration .a + .b (Phase I) |
| #8 | pending | Step 1.6.7 — stamp_format_version SOFT bump 1→2 (Phase F; SAME COMMIT as Step 1.6.4+8) |
| #9 | pending | Step 1.6.8 — tools/stamp_model.sh migration (Phase F; SAME COMMIT) |
| #10 | pending | Step 1.7 — Layer 5b invariants invocation (Phase I) |
| #11 | pending | Step 2 — DELETE legacy registries (Phase J; FORCED LAST) |
| #12 | pending | Steps 3-9 — ship close (Phase K; CI Check 9 + DESIGN_SPECS amendments + auto-writes) |
| #13 NEW | pending | **Step 1.6.5b — action-parameterized walker refactor (Class 21 structural closure at consumer surface; absorbs Step 0.5d.d for populate_inf + drift_check)** |

Recreate via TaskCreate per row at fresh-session pickup. Mark #1 and #6 as in_progress per status above.

---

## 6. Pending decisions at session start (GREENLIGHT GATES)

Two decisions Caramel must make at the start of the next session:

### Decision 1 — Working-tree commit strategy

Per Section 2 above: (a) WIP-checkpoint 4 commit / (b) absorb into walker-refactor commit / (c) revert + restart.

**Recommendation: (a) WIP-checkpoint 4 commit** to capture Step 1.6.5 macro-swap work as a clean rollback anchor before walker-refactor begins.

### Decision 2 — Greenlight on Step 1.6.5b walker-refactor

The structural-fix analysis (Sections 3 + 4 above) recommends the action-parameterized walker refactor as the most future-thinking fix for sister-consumer asymmetric coverage drift. ~3-3.5h scope; bounded risk via existing Layer 5b invariant tests; aligns with Class 21 closure precedent + existing canonical patterns.

**Greenlight gate:** does Caramel accept the walker-refactor design + scope, or does she want deeper analysis / alternative approach?

**Residual uncertainties to surface (was 10% in my prior message; now refined):**

1. **Per-scope X-macro signature handling** — per-core/global X-macros take 13-tuple signature; ml_cfg_flag/gate_cfg_flag take 6-tuple signature. Per-scope X-macro definitions vary by signature shape. This matches current per-registry walker structure at HEAD (populate_stamp_cfg has 4 separate X-macro walkers with per-registry signatures) — not a new concern, just scoped-out implementation detail.
2. **Naming convention** — `WALK_ALL_STAMP_BOUND_DERIVED` vs `FOREACH_STAMP_BOUND_DERIVED_COHORT` vs other. CLAUDE.md convention is `FOREACH_*` for X-macro registries; `WALK_*` would be a NEW name shape. Recommend `FOREACH_STAMP_BOUND_DERIVED_COHORT(BASE_X_MACRO)` to match codebase convention; the macro IS a meta-walker that takes a base name + expands 4 walkers.
3. **Step 1.6.5b vs Step 1.6.5 numbering** — clean if Step 1.6.5 is the original macro-swap (8 sites at callers) and Step 1.6.5b is the framework consolidation behind the macro. Could be renamed to Step 1.6.5.refactor or similar at plan body v1.13 amendment.

---

## 7. Required reading at fresh-session pickup

**Auto-loaded (verify):**
- `CLAUDE.md` (orientation; always loaded)
- `CLAUDE.local.md` (operator overlay + sprint state index)
- `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md` (memory index + key feedback memories)

**THIS handoff doc** — read FULLY especially Section 3 + Section 4 (the structural-fix analysis)

**Plan + audit (load before coding):**
- Plan body v1.12: `plans/v5.15-live-readiness/subplans/2026-05-17-v5.15.5.F.4d.1.B.3-legacy-empty-out.md` — focus on Phase F sequencing + Meta-gap M4 (lines 339-455) + Step 1.6.5 detail (line 633)
- Predecessor handoff: `plans/v5.15-live-readiness/handoffs/2026-05-18-v5.15.5.F.4d.1.B.3-step-1.6.5-coding-handoff.md` — Step 1.6.5 verification details + Phase C audit baseline

**DESIGN_SPECS (load for refactor design):**
- `tick-trader-percore-workspace/DESIGN_SPECS/framework-patterns/cfg-derived-consumer-framework.md` (Stage 3 ACTIVE v1.1; the master spec for this surface — pending v1.2 amendment per this finding)
- `tick-trader-percore-workspace/DESIGN_SPECS/framework-patterns/autopopulate-pattern-for-production-caller-class.md` (parent pattern)
- `tick-trader-percore-workspace/DESIGN_SPECS/meta-disciplines/canonical-sister-extension-discipline.md` (Stage 3 ACTIVE v1.1; INLINE MERGE shape applies)
- `tick-trader-percore-workspace/DESIGN_SPECS/framework-patterns/metadata-bit-driven-derived-filter-framework.md` (Option E context — for understanding which approach DOESN'T apply + why)

**Anti-pattern catalog:**
- `DOCS/RECURRING_BUG_PATTERNS.md` Class 21 (line 1371-1404; the closure precedent) + Class 18 STRENGTHENED (line 1225-1289; PARITY-009/010/011/012 closure shape)

**Memory rules (key):**
- `feedback_structural_fix_for_recurring_class.md` (the meta-rule applied here)
- `feedback_audit_canonical_sister_before_new_infra.md` (sister-extension discipline)
- `feedback_framework_layer_payoff_diminishing_returns.md` (inflection-point check; this is consolidation-phase work, not past-inflection layer addition)
- `feedback_proportionate_response_to_audit_findings.md` (option C FOLD vs D ARCHITECT)
- `feedback_plan_right_not_fast.md` (planning depth over speed)
- `feedback_motivated_collaborator_for_caramel.md` (best-software discipline)
- `feedback_consult_on_audit_findings.md` (consult before coding — STILL applies for Decision 2 greenlight)

**Source files (load before refactor):**
- `MemHeaders/CfgGateRegistry.hpp` — the file being refactored (lines 220-475 contain the 3 consumer template fns)
- `ML_Headers/MlCfgFlagRegistry.hpp` — FOREACH_ML_CFG_FLAG signature (for per-scope X-macro shape)
- `CoreFrameworks/GateCfgFlagRegistry.hpp` — FOREACH_GATE_CFG_FLAG signature
- `CoreFrameworks/CfgFieldRegistry.hpp` — FOREACH_PER_CORE_CFG_FIELD + FOREACH_GLOBAL_CFG_FIELD signatures + FIELD_IDX_* constants

---

## 8. Pre-pickup self-audit (4-pillar discipline per `feedback_audit_own_proposals_with_same_rigor`)

| Pillar | Verdict | Notes |
|---|---|---|
| 1. DESIGN_SPECS cross-check | PASS — aligns with `cfg-derived-consumer-framework.md` (implicit invariant codified explicitly) + `autopopulate-pattern-for-production-caller-class.md` (extends parent pattern with action-parameterization) + `canonical-sister-extension-discipline.md` (INLINE MERGE per expanded menu) + Class 21 closure precedent at `RECURRING_BUG_PATTERNS.md` line 1371 |
| 2. Anti-pattern catalog | CLEAN — Class 21 (closes this instance + structurally prevents recurrence at consumer surface); Class 23 (no offset access — compile-time field access preserved); Class 18 (closes mirror state between consumer walker bodies); does NOT introduce NEW Class instances |
| 3. Operator-impact | NONE — no wire-format change (bytes preserved verbatim via X-macro body copies); no operator action required; transparent to live operation; ship-close auto-write delta is +2 items (1 spec amendment + 1 memory rule) |
| 4. Novel alternative considered | Per Section 4.4 above: β1 (template fn with offset access — REJECTED Class 23); β2 (manual sister-extend + CI tool — REJECTED structural-fix-preferred); β3 (CFG_FIELD_FOR_EACH_SET_BIT — REJECTED type-safety constraint); β4 (manual sister-extend without framework — REJECTED recurrence-class persistence). Action-parameterized walker is structurally cleanest + matches Class 21 closure precedent. |

---

## 9. Cold-pickup prompt (paste into fresh Claude Code session)

```
I'm picking up v5.15.5.F.4d.1.B.3 (Legacy empty-out — Step 1.6.5 walker refactor + downstream) for the
v5.15-live-readiness sprint. This is cross-session pickup after structural-fix analysis surfaced
the action-parameterized walker pattern for sister-consumer asymmetric coverage drift.

## Step 0 — orient + verify state (MANDATORY)

1. SHA-diff trigger check (must match handoff anchor):
   cd /home/caramel/code/FoxML_Trader_v2
   git rev-parse HEAD                    # expect 3298ab2 OR a NEW commit if WIP-checkpoint 4 landed
   head -10 Version.hpp                  # expect 5.15.5.F.4d.1.B.2
   git status --short                    # expect either empty (WIP-checkpoint 4 committed) OR 2 modified files (StampHelper.hpp + controller_test.cpp)
   git tag --list 'pre-v5.15.5.F.4d.1.B.3'   # expect: tag exists

2. CI checks (must PASS):
   python3 tools/check_meta_registry.py 2>&1 | tail -3
   python3 tools/check_per_core_registry_integrity.py 2>&1 | tail -3
   python3 tools/check_field_name_uniqueness.py 2>&1 | tail -3
   python3 tools/check_storage_t_coverage.py 2>&1 | tail -3
   python3 tools/check_struct_field_uniqueness.py 2>&1 | tail -3

3. Build state check — EXPECT BROKEN:
   ./build.sh test 2>&1 | grep -c 'error:'   # expect ~190-224 errors (~30 fewer if walker refactor lands; ~224 if at handoff state)

Any divergence → investigate via `git log 3298ab2..HEAD` before proceeding.

## Required reading (parallel; per the handoff doc):
- CLAUDE.md + CLAUDE.local.md + MEMORY.md (auto-loaded)
- THIS HANDOFF: plans/v5.15-live-readiness/handoffs/2026-05-18-v5.15.5.F.4d.1.B.3-step-1.6.5-walker-refactor-handoff.md
- Plan body v1.12: plans/v5.15-live-readiness/subplans/2026-05-17-v5.15.5.F.4d.1.B.3-legacy-empty-out.md
- DESIGN_SPECS: cfg-derived-consumer-framework.md + autopopulate-pattern-for-production-caller-class.md + canonical-sister-extension-discipline.md
- RECURRING_BUG_PATTERNS.md Class 21 + Class 18 STRENGTHENED

## Recreate TaskList per handoff Section 5 (13 active entries; #1 + #6 in_progress)

## Greenlight gates BEFORE coding (Section 6 of handoff):

DECISION 1 — Working-tree commit strategy: WIP-checkpoint 4 commit (recommended) OR absorb into walker-refactor commit OR revert + restart. Operator decides at session start.

DECISION 2 — Greenlight on Step 1.6.5b walker-refactor design. Operator reads Sections 3 + 4 of handoff; either greenlights the action-parameterized walker fix OR requests deeper analysis / alternative design.

## Coding sequence per Section 4.7 of handoff (AWAITING Decision 2 greenlight):

A. Build WALK_ALL_STAMP_BOUND_DERIVED meta-macro family in CfgGateRegistry.hpp (~30 min)
B. Refactor populate_stamp_cfg_from_derived (~45 min; MED risk — Layer 5b invariants verify byte preservation)
C. Refactor populate_inference_cfg_from_derived + add ml_cfg_flag + gate_cfg_flag X-macros (~45 min)
D. Refactor drift_check_from_derived + add gate_cfg_flag X-macro (~30 min)
E. Build verify + wire-format invariant tests (~30 min)

Total: ~3-3.5h walker refactor.

Then return to plan body Phase F sequence: Step 1.6.4 + 1.6.7 + 1.6.8 SAME-COMMIT trio per /parity-check M-3.

## Per consult-before-coding discipline:
Caramel will iterate on findings. Per `feedback_consult_on_audit_findings` — present
findings inline; don't auto-proceed past substantive discoveries. Per `feedback_plan_right_not_fast` —
sit with options long enough to honestly evaluate; speed heuristics undercut planning depth.

Session E discovered Class 21 instance at consumer template fn level (3 cfg-derived consumers had
asymmetric registry coverage); structural fix via action-parameterized walker pattern. The fix is
THE most future-thinking answer per Class 21 closure precedent + sister-canonical extension discipline.
Walker refactor obviates the H21/M5/CI-tool over-engineering I initially proposed.
```

---

## 10. Notes for future-Claude

- The structural-fix analysis took 4 iterations to land on the action-parameterized walker pattern. Initial reflex was "add H21 + new DESIGN_SPEC + new M5 + new CI tool" — exactly the framework-layer-past-inflection anti-pattern. Caramel's pushback caught it. The lesson: per `feedback_recheck_designspecs_on_pushback` + `feedback_audit_canonical_sister_before_new_infra`, ALWAYS check existing canonical patterns + anti-pattern catalog BEFORE proposing new infrastructure.
- The Class 21 closure shape (single source of truth + many consumer views with compile-time enforcement) is well-precedented in this codebase. Future Class-18/21 instances should default to this shape — meta-macro that expands to N walker invocations; consumer must define N X-macros by naming convention to compile.
- Wire-format byte preservation is the load-bearing risk at this refactor. Verify Layer 5b invariants (`tests/wire_format_invariants.hpp` I1-I5) PASS post-refactor + v1 stamp LOAD test (Step 1.6.7.5) PASS. If either fails, X-macro body deltas vs current populate_stamp_cfg need debugging.
- Per /trace-deps re-fire: line numbers in plan body / handoff may drift between authoring time and HEAD. Always verify file:line via `rg` at coding time per `feedback_compaction_degrades_treat_handoffs_as_hints`.
- Per Caramel's "never refactor again" + "best software" discipline + this session's exhausting analysis: the action-parameterized walker IS the structurally-correct answer for this surface AND for sister cohort surfaces in the future. AFFECTS_STAMP_PARITY cohort (training-side; future ship) gets its own walker family with same shape. Pattern is generalizable.

---

**End of v5.15.5.F.4d.1.B.3 Step 1.6.5 walker-refactor handoff.**

Working tree has Step 1.6.5 macro-swap partial work LANDED but uncommitted; Section 2 documents the diff. Section 4 contains the full structural-fix design as reference. Section 6 lists 2 greenlight gates at session start. Section 9 has the cold-pickup prompt.

Pickup expected to take ~3-3.5h focused on walker refactor + ~1h on downstream Step 1.6.4 + 1.6.7 + 1.6.8 same-commit trio.
