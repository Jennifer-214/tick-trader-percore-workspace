---
type: handoff
status: superseded   # superseded 2026-06-23 by 2026-06-23-E.1.1-item2-pernode-handoff.md (item-2(a) migrate + B1 landed; per-node b/c/d next)
ship_tag: E.1.1-③-config-compiler
plan_type: live-readiness
sprint_end_goal: v5.15 live-readiness — make the per-node sharded engine live-ready + the codebase more maintainable.
ship_end_goal: ③ "config compiler" — boot-time capital-cfg validation that ALWAYS-ABORTs on a malformed/out-of-range capital value (no silent-wrong-capital), converged to the D-255 correctness-first HYBRID (6 items).
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-22-E.1.1-cfg-config-compiler-coding-handoff.md
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, plans/v5.15-live-readiness/plan_checks/2026-06-22-E.1.1-③-precoding-adversarial-findings.md, plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md, plans/v5.15-live-readiness/plan_checks/2026-06-22-E.1.1-config-compiler-IA-cascade-synthesis.md, plans/v5.15-live-readiness/MASTER.md]
coding_status: mid-coding-checkpoint (D-255 6-item scope; item 1 LANDED, next = item 2)
deletion_scope: minor
---

# E.1.1 ③ config-compiler handoff — item 2 (MIGRATE the 4 globals) pickup

**Created:** 2026-06-22
**Target:** ③ "config compiler" — the D-255 converged 6-item correctness-first HYBRID, item 1 of 6 LANDED.
**Branch:** `feat/v5.15-live-readiness`
**Pre-tag rollback anchor:** `pre-v5.15.5.F.4d.1.E.1.1`
**LIVE record (read this first):** `plans/v5.15-live-readiness/plan_checks/2026-06-22-E.1.1-③-precoding-adversarial-findings.md` § CONVERGED PLAN
**Decision:** `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` **D-255** (`:1649`)

## Engine state at handoff write
- HEAD `c6967be` · branch `feat/v5.15-live-readiness` · tree CLEAN · build-gated **3635/0**
- **NOT pushed** (1 ahead of `origin/feat/v5.15-live-readiness`); a fresh pickup runs `git fetch` + verifies `HEAD == c6967be` OR pushes first.
- Version.hpp: `5.15.5.F.4d.1.E.1.0` (E.1.1 in-flight, NOT yet tagged)

## The one-paragraph orientation
③ stops the founding bug: a **capital** cfg value loading **silently wrong** (`stop_loss_pct=banana`→0→SL off; `risk_pct=999`→999% alloc). A 17-agent reframe audit this session **corrected an over-rotation** (the orchestrator had amplified a found Stage-2 `[section]`-parser spec into "the foundational answer / the patch is redesign-risk" — 3 independent agents proved it ORTHOGONAL: the validation is parser-agnostic, AR-6 recurrence) and **converged on D-255**: the real design (D-254 metadata-bit + post-resolve sweep) is sound + foundational; close the bug **correctness-first**, defer the uint32 widening + `[section]` to where their consumers live (E.1.6 / E.2). **6 items, build-gated micro-commits.** Item 1 (the single-source gate) LANDED. Policy is firm: **NO MARGIN** (loss ≤100% / gain ≤1000%), **D2 ALWAYS-ABORT all modes**.

---

## TaskList state at handoff write (recreate verbatim via TaskCreate)

| ID | Status | Subject |
|---|---|---|
| #1 | in_progress | E.1.1 ship — Core→Node rename + cfg clean-break + single_core deletion |
| #2 | in_progress | ③ config-compiler — D-255 6-item scope (item 1 LANDED; items 2-6 + ParseFast + tests remain) |
| #3 | pending | ⑤ V-class — asan/ubsan + determinism re-cert (the rename + ③) |
| #4 | pending | E.1.1 ship-close — doc sweep + FEATURE_LOOKUP + cores-prose + TD-140 move |
| #5 | pending | E.1.2 — NodeState SoA / Money relayout (successor leaf) |
| #6 | in_progress | All-strategies-private restructure + capture (D-251) |
| #7 | pending | Consolidate ③ capture post-deep-pass (D-249 + codification slate + homes) |

**Recreate this TaskList** via TaskCreate; mark #1/#2/#6 in_progress, the rest pending.

---

## Per-item status (the D-255 converged ③ scope)

| Item | Status | Landed at | Notes |
|---|---|---|---|
| 1 — single-source the gate + wire every boot path | **LANDED** | `c6967be` | `cfg_capital_gate_ok()` helper (`ControllerConfig.hpp:1353`) + main.cpp:203 refactor + `BacktestSharded.hpp:125` gate. Closed C-1 (suite/backtest ran malformed cfg ungated). |
| 2 — MIGRATE the 4 globals into the registry | **NEXT** | — | the founding-bug close for the globals + the hooks-open structural move (below). |
| 3 — dollar floors `>0`/sane | PENDING | — | `starting_balance` (denominator) + `min_kill_loss` (safety floor) — FAULT-not-clamp. |
| 4 — per-field ≤100% caps | PENDING | — | risk/sl/dd incl. `node_N_*` overrides, via `tt::barrier_is_corrupt` (NOT restated constants — Class-43). Only cross-node Σ → E.5. |
| 5 — Async live-reload gate | PENDING | — | `Async.hpp:321` → `cfg_capital_gate_ok(new_cfg, ...)` keep-old-on-fault (the Knight-shape live bypass). + fold the H4 double-math smell at `:347-356`. |
| 6 — recurrence guard | PENDING | — | compile-time `.rodata` static_asserts (cohort-count + no-capital-row-is-`MANUAL_PARSER`) + Python `cfg_compile_ok` caller-coverage. |
| ParseFast `*_checked` + capital-site swaps | PENDING | — | a `parse_double_fast_checked` (surface the `.ec`) + switch ONLY capital/price/balance sites (GUI `atof` parser + `BinanceOrderAPI` `free`-balance). NET-NEW. |
| tests + ⑤ V-class | PENDING | — | pos/neg controls (each capital test carries an adversarial marker per the doc-CI) + asan/ubsan + determinism re-cert. |

---

## NEXT ACTION — item 2: MIGRATE the 4 non-registry capital globals into the registry

The 4 globals are parsed by hand-rolled flag-discarding `CFG_PARSE_MONEY/PCT` branches the walker never reaches → the founding bug is OPEN for them today. The **hooks-open** move (D-255, operator-directed "leaving hooks open for E.X") is to MIGRATE them into `FOREACH_GLOBAL_CFG_FIELD` (NOT hand-roll fault_out — that gets deleted when the idioms die) → the registry walker validates them by construction + the validation SURVIVES the future `[section]` parser:

| Field | manual decl | hand-rolled parse | migrate to |
|---|---|---|---|
| `starting_balance` | (struct field) | `CFG_PARSE_MONEY` `:2253` | `X(Money, KIND_DOUBLE, starting_balance, ...)` (precedent `fee_floor_mult:492`) |
| `min_kill_loss` | `:1030` | `CFG_PARSE_MONEY_POS` `:2318` | `Money, KIND_DOUBLE` |
| `min_sl_tp_ratio` | (struct field) | `CFG_PARSE_MONEY` `:2264` | `Money, KIND_DOUBLE` (ratio; consumed legacy-only) |
| `min_hold_gain_pct` | (struct field) | `CFG_PARSE_PCT` `:2446` | `KIND_DOUBLE_PCT` (a name-liar — a hold/exit gate, NOT a capital cap; gets malformed-capture, NO ≤100% cap) |

Per field: delete the manual struct-field decl + the manual parse branch, add the registry row (auto-generates both). **🔴 HEADS-UP (the careful part — why this is its own focused chunk):** moving the fields changes the `ControllerConfig` layout → **trips `static_assert(sizeof(ControllerConfig<64>) == 53056)`** (`ControllerConfig.hpp:1331`) → a **pin bump + golden regen** is owed (the H17 size-pin guard FORCING the determinism re-verification, working exactly as designed — see `Backtest/CLAUDE.md` § GOLDEN-REGEN; this is a money-encoding-epoch-style regen, the goldens absorb the new layout). Do the migrate, let it RED-build the pin, measure the new sizeof (`clang -fdump-record-layouts`), bump the pin, regen + refreeze the golden, re-verify determinism.

---

## Coding-time discoveries this session (assume these shape what you hit)

- **The reframe (AR-6 recurrence):** `[section]` parser is ORTHOGONAL to capital-validation (the post-resolve sweep is parser-agnostic; D-252 already refuted route-through-the-walker as vacuous/destructive/infeasible). Do NOT re-open PATCH-vs-`[section]`; it's a false fork. `[section]` → E.2.
- **The widening is NOT needed for the founding bug** (3 agents): malformed-capture is unit-agnostic (the parse flags are "did it parse as a number," not unit-specific). The uint16→uint32 widening + `CAPITAL_BOUND_DOLLAR` are deferred to E.1.6 (where `ENUM_STRICT` is the real second consumer, and there's NO free uint16 slot — bit 15 is the last; adding any bit IS the widening). Don't add a dollar bit in ③.
- **C-1 (the verified critical that became item 1):** `cfg_compile_ok` had ONE caller (`main.cpp:203`); the GUI/suite/backtest binary (`foxml_suite.cpp` + `BacktestSharded.hpp:123` zeroes the flag) ran malformed cfg ungated. Fixed.
- **The 4 globals' migration touches the size-pin** (above) — the one careful step.
- **`min_kill_loss`/`starting_balance` need `>0`** (item 3), not just malformed-capture: a corrupt/huge `min_kill_loss` makes the drawdown kill NEVER fire; `starting_balance=0` zeroes every limit.

---

## Capture-completeness (this session — nothing lost)

- **Decisions → log:** **D-255** (the converged 6-item ③ scope + the deferrals + the hooks-open/migrate-over-hand-roll refinement) at `decision-logs/...E-architecture-v2.md:1649`, with a paired `<!-- D/C/F: D-255 -->` + `<!-- STATUS: DECIDED -->` sentinel. No un-logged decisions (the Stage-5.5 review verified D-255 reached the log + the SSoT + the findings doc).
- **Findings → ledger:** all 17-agent findings homed — the findings doc (FINDINGS 1-9 + CONVERGED PLAN); the deferred homings WRITTEN as fold-blocks into the E.2/E.5/E.1.6/E.1.3 destination BODIES (not pointers — the AR-12 lesson); the decoupling-roadmap positioning; **TD-219** (ledger-split landmine) + **TD-220** (Class-47 dead `kill_switch_*`) + **TD-221** (B-Plus whole-file-vs-diff tool false-positive) in `tech-debt/open.md`. No unhomed debt. (A post-close exhaustive 4-dimensional sweep refreshed the owning-docs — MASTER s6 banner + this subplan's frontmatter + E-MASTER-REFERENCE now reflect the reframe/D-255/item-1; the prior coding-handoff is SUPERSEDED.)
- **Memories/skills → indexed:** no new memory files (D-255 is decision-log). **AR-12** (pointer-mistaken-for-homing) + an **AR-6 recurrence** added to `meta-anti-pattern-index.md` — both enrolled as a TABLE ROW + the frontmatter `status:` bumped to 23 rows (the Stage-5.5 review caught the table-not-enrolled gap; fixed). No new skills. The ParseFast `*_checked` work + the all-private restructure (#6) remain queued.
- **Verified:** `check_session_docs.sh` — all HARD pass EXCEPT a pre-existing B-Plus failure (E.2/E.5 v0.1-draft design-sketch code blocks + decision-log bridge-preserved historical cites; my new content is citation-clean, confirmed; the failure is PRE-EXISTING, surfaced by the whole-file re-scan on my fold-block touch — **ACCEPT**, a B-Plus diff-vs-whole-file refinement candidate). The Stage-5.5 independent review: **9/10 VERIFIED + 1 fixed** (the AR-12 table enrollment). Item 1 build-gated 3635/0 + all 5 pre-commit gates green. **Tools verified rename-compatible + functional** (`check_per_node_registry_integrity.py` exit 0; no stale-broken tool; the old `check_per_core_*` fully migrated).

---

## Arming for the next action (item 2 — the registry migrate)

The surface is **cfg-flow / global registry / determinism size-pin** (a cfg-field migration with a golden-regen):
- **Route to:** `/trace-deps` + `/dod-audit` (the cfg-registry migration shape) + `cfg-scope-discipline.md` + `universal-cfg-field-registry-pattern.md` + RECURRING_BUG_PATTERNS Class 25/26/27 (cfg-scope) + **Class 48** (sentinel destruction — the `0`=inherit / dollar-`0` must not be clamp-rewritten). For the determinism step: `/hft-audit` + `Backtest/CLAUDE.md § GOLDEN-REGEN` + `wire-format-byte-preservation-discipline.md`.
- **Run (not just read):** `./build.sh test` (the size-pin RED-build is EXPECTED — it's the H17 guard firing) → `clang -fdump-record-layouts` for the new sizeof → bump the `:1331` pin → regen + refreeze the golden (`fp_determinism_golden.txt` + the `Fingerprint_Compute` golden per `Backtest/CLAUDE.md`) → `./build.sh test` 3635/0 + `./build.sh gui`/`suite` (Check 31 wider-build, since this touches a cfg field GUI renders) → `python3 tools/check_per_node_registry_integrity.py` → `./tools/gen_code_map.sh`.
- **Read first:** `DOCS/STRATEGY_AND_CODING_RULES.md` (H17 cfg-registry discipline) + the D-255 § CONVERGED PLAN + the no-margin memory `project_no_margin_trading`. **Invariants in play:** **H17** (cfg fields auto-generated from `FOREACH_CFG_FIELD`, no manual decls — this migration IS the H17-faithful move), **H9/H12** (the size-pin + golden regen — the determinism re-cert), **H4** (Money math), **H22** (per-node purity — the per-field validation the migrated globals enable).
- **M8:** if you spawn agents (e.g. a pre-migrate `/trace-deps` of the 4 fields' consumer sites, or a post-migrate determinism verify), ARM each with the docs + the toolchain to RUN + scout-first; keep any adversarial verify independent on the verdict.

**Tests to write (with the later items):** `starting_balance=banana`→abort · `min_kill_loss=banana`→abort · `starting_balance=0`→abort (denominator, item 3) · `risk_pct=999`→abort (item 4) · `take_profit_pct=2.0`→clean · `0`=inherit preserved (NOT a fault) · backtest gate-on-fault (item 1, verify) · Async reload keep-old (item 5). Adversarial marker on each capital test (doc-CI).

---

## Paste this into a fresh session to resume

```
I'm resuming E.1.1 ③ (the config-compiler / capital-cfg validation) for the v5.15-live-readiness sprint, at item 2 of the D-255 converged 6-item scope.

Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-22-E.1.1-③-item2-migrate-handoff.md

That loads the baseline + the LIVE record (the findings-doc CONVERGED PLAN) + D-255 + the SSoT, regens CODE_MAP, verifies engine HEAD c6967be + branch feat/v5.15-live-readiness + clean tree + 3635/0, and recreates the TaskList.

Then: item 1 (single-source capital-gate) is LANDED + committed (c6967be). My NEXT action is item 2 — MIGRATE the 4 non-registry capital globals (starting_balance / min_kill_loss / min_sl_tp_ratio @ Money+KIND_DOUBLE; min_hold_gain_pct @ KIND_DOUBLE_PCT) from manual struct fields + their hand-rolled CFG_PARSE_MONEY branches INTO FOREACH_GLOBAL_CFG_FIELD → walker-validated by construction (hooks-open). HEADS-UP: this trips the sizeof(ControllerConfig<64>)==53056 pin → pin bump + golden regen owed (H17 guard working). Then items 3-6 (dollar >0, per-field ≤100% via barrier_is_corrupt, the Async.hpp:321 reload gate, the recurrence guard) + ParseFast *_checked + tests + ⑤ V-class.

Policy is firm: NO MARGIN (loss≤100% / gain≤1000%), ALWAYS-ABORT all modes (D2). Widening + [section] are DEFERRED (E.1.6 / E.2) — do NOT re-open them; [section] is ORTHOGONAL to capital (AR-6 recurrence). Build-gated micro-commits (wip(.E.1.1): prefix). Consult Caramel before each substantive step; don't auto-proceed on findings.
```

## Quick links
- LIVE record / converged plan: `plans/v5.15-live-readiness/plan_checks/2026-06-22-E.1.1-③-precoding-adversarial-findings.md`
- Decision: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-255 @ :1649)
- SSoT: `plans/v5.15-live-readiness/plan_checks/2026-06-22-E.1.1-config-compiler-IA-cascade-synthesis.md` § REMAINING ③
- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- Meta-harvest: `DESIGN_SPECS/meta-disciplines/meta-anti-pattern-index.md` (AR-12 + AR-6 recurrence, Session-18)
- No-margin policy: `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/project_no_margin_trading.md`
- Golden-regen discipline: `Backtest/CLAUDE.md` § GOLDEN-REGEN
