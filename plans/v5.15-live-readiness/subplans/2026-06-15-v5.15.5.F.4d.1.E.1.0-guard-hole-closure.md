---
type: subplan-scaffold
ship_tag: v5.15.5.F.4d.1.E.1.0
ship_kind: enforcement-scaffold (CI-only; zero behavior change)
parent: 2026-05-28-v5.15.5.F.4d.1.E.1-foundation.md  # sub-master (umbrella)
predecessor: none (.E.0.10 baseline — HEAD 572c978; 3635/0)
successor: 2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md
status: IN-FLIGHT DIVE — full body expanded 2026-06-16 (C-class re-ground applied); awaiting I/A gate + consult before code
established: 2026-06-15
spine: FOUNDATION (no spine)
risk: LOW  # pure enforcement; no runtime change → any break is a CI-config break or a surfaced pre-existing violation
hot_path: NOT TOUCHED (CI gates only; gates RED-build a future hot-path regression, change no hot code here)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
dependency_graph: plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md
guard_matrix: plans/v5.15-live-readiness/E-guard-coverage-matrix.md  # §4a H7/H10/H14/H20/H8 + §3 ratchet/gate
rollback_anchor: pre-v5.15.5.F.4d.1.E.1.0
sister_specs:
  - meta-disciplines/plan-hierarchy-and-sub-master-decomposition.md  # the scaffold lifecycle this leaf rides
  - refactor-patterns/branchless-dispatch-discipline.md  # H7/H20 — the grep-CI codifies its decision matrix
  - concurrency-patterns/cross-thread-multiword-read-consistency-discipline.md  # (sister; E.1.3 consumes the determinism gate)
---

# E.1.0 — §4d guard HOLE-cluster closure (guards-first)

**Status:** SCAFFOLD (2026-06-15). Full coding body expanded by a C-class re-ground pass when this becomes the in-flight dive. First leaf of the `.E.1` FOUNDATION cluster (E.1.0 → E.1.1 → E.1.2).

## Scope
Close the `.E.1`-surface HOLE cluster recorded in the guard-matrix §4a — the convention-only hot-path/determinism/branchless invariants that are the highest-blast-radius surface (§4a:110) — by standing them up as CI gates BEFORE the rename reshapes the tree. Three gate families: (1) **grep-CI** for H7 (hot-path branchless), H14 (no C++ bitfield syntax), H20 (SP/HP branchless dispatch); (2) the **determinism gate** for H10 (SIMD scalar fallback byte-identical) — 2× run + `±USE_NATIVE_128` byte-compare; (3) the **latency ratchet** for H8 (`LATENCY_BENCH` p99 budgets as a CI gate). PURE enforcement scaffold: ZERO behavior change — the only outputs are new CI gates + any pre-existing violations they surface for triage. This is THE NET the rest of `.E.1` rides (D-72 rolling-window); guards before the behavior they freeze (D-82 two-foundations).

## Spine / layer
FOUNDATION (no D-214 spine). The guards-first ship — opens the net E.1.1's careless-rename and E.1.2's first SoA AVX kernel run UNDER.

## TD-closed
None directly — this leaf OPENS the enforcement net the rest ride (per the A-adjudication §2 E.1.0 + D-225). It is the precondition, not a debt-closer.

## Locked decisions honored
- **D-82** (two-foundations): freeze the CURRENT deterministic hot-path behavior with the determinism gate BEFORE E.1.1 reshapes names / E.1.2 reshapes bytes — the rework must not silently change determinism.
- **D-83** (guard-matrix bounds foundation-hardening): "make it solid first" terminates when every invariant the next phase touches is an ENFORCED row. This leaf flips the §4a H7/H10/H14/H20 + H8 rows HOLE/PARTIAL → ENFORCED — the stop-condition for foundation-hardening on the `.E.1` surface.
- **D-72** (rolling-window net): this ship's STANDING CI gate IS the net, not the per-ship audits.
- **D-131** (no live models): the determinism gate's golden is a regen-at-will fixture — an epoch break is free, so re-baselining the byte-compare reference at any later layout change costs ~0.

## §4 guard-rows it fills
Per guard-matrix §4a (Hard invariants) + §3 (safeguard→ship distribution):
- **H7** hot-path branchless: `1/3 → likely HOLE` ⟶ **ENFORCED** (grep-CI).
- **H10** SIMD scalar fallback byte-identical: `HOLE → determinism gate` ⟶ **ENFORCED** (2× run + `±USE_NATIVE_128` byte-compare).
- **H14** no C++ bitfield syntax: `HOLE → grep CI` ⟶ **ENFORCED** (grep-CI).
- **H20** branchless SP/HP dispatch: `1/5 → likely HOLE` ⟶ **ENFORCED** (grep-CI).
- **H8** hot p99 ≤500ns / slow ≤100μs: `PARTIAL → latency ratchet (.E.1)` ⟶ **ENFORCED** (`LATENCY_BENCH` budgets as CI ratchet; §3:63 "set early so every later ship is gated").
- §3 rows: latency-ratchet · determinism-gate · CI-merge-gate ⟶ stood up here.

## INBOUND seam (needs from predecessor)
The clean `.E.0.10` baseline: HEAD 572c978, controller_test **3635/0**, FRESH asan+ubsan 3635/0, `calls_graph_diff` CLEAN, guard-matrix §4d recording the COMPLETE current guard set (the net E.1.0 extends). §4d explicitly records the H7/H10/H14/H20 hot-path/determinism HOLE cluster as E.1.0-owned + deferred-by-design (two-foundations) — so the inbound contract is "the net is complete EXCEPT this recorded cluster, which is exactly this leaf's work."

## OUTBOUND seam (invariant the successor relies on)
A **STANDING green CI** that RED-builds, in any later ship: a new hot-path data-dependent branch (H7), a C++ bitfield (H14), a non-byte-identical SIMD fallback (H10), an SP/HP branch where branchless was available (H20), a hot p99 regression past budget (H8), or a determinism break (`±USE_NATIVE_128` divergence). **E.1.1 rides this**: it proves the gate catches a careless-rename hot-path regression — the rename runs UNDER the net, on FROZEN logic, so a test/gate break is a rename typo not a silent perf/determinism slip. *This is the net, not the audits* (A-adjudication §2 E.1.0 OUTBOUND).

## Design-shape (code-grounded against HEAD 572c978)
- **Latency substrate already present** (no new profiling infra — wire the EXISTING flags into a CI gate): `LATENCY_PROFILING` / `LATENCY_LITE` / `LATENCY_BENCH` declared `CMakeLists.txt:18-20` (`LATENCY_BENCH` = "Headless profiling to stderr", `:62-63` defines `LATENCY_PROFILING LATENCY_BENCH`); `build.sh latency` → `build_lat/` (`build.sh:211` `cmake -B build_lat -DLATENCY_PROFILING=ON`). The ratchet parses `LATENCY_BENCH` stderr against the H8 budgets (hot p99 ≤500ns / slow ≤100μs).
- **Determinism gate**: `USE_NATIVE_128` is the established determinism axis (the byte-compare reference toggle — `±USE_NATIVE_128` must produce bytewise-identical golden; the existing FP determinism-net + `run_sanitizer_suite.sh`-style rebuild-from-source discipline is the model — V-class v1.1 sanitizer-freshness). Gate = build both, run the replay/golden, `memcmp` the outputs.
- **grep-CI gates** (new `tools/check_*.py`, wired into the pre-commit `.githooks/pre-commit` Check chain + the `check_session_docs.sh`-style aggregator pattern): H14 = grep for C++ bitfield syntax (`identifier : N` in struct bodies) → the codebase mandates `SHIFT_*`/`MASK_*` + `MBS_*`/`BITMAP_*` instead; H7/H20 = flag data-dependent `if`/`switch` on the hot/SP dispatch paths, honoring the `branchless-dispatch-discipline.md` exception matrix (boot-time-only / `__builtin_expect`-rare / `if constexpr` / genuine binary predicate). NOTE: H7/H20 grep is heuristic — scope to the hot/SP path files + accept a KNOWN-PENDING allowlist if pre-existing violations surface (close-the-class-vs-migrate-every-site, D-84), shrinking-only.
- **Hot-path files in scope** (the grep targets): the per-core hot path `CoreFrameworks/ExecutionCore.hpp` (the 500ns steady path — money compares, branchless), the producer fan-out `CoreFrameworks/EngineSharded/Async.hpp` / `Run.hpp`, the strategy SP dispatch `Strategies/StrategyParameters.hpp`. (RE-DERIVE the exact file set at dive per `feedback_paste_tool_output_dont_summarize` — `calls_graph_diff.sh` enumerates the live hot path.)

## Rollback anchor
`pre-v5.15.5.F.4d.1.E.1.0` (create at the dive's Phase A.0). Bisectable: zero runtime change → any break is a CI-config break or a surfaced pre-existing violation. The cleanest first cut.

## Known folds / findings it owns
- **Surfaced-violation triage**: a grep-CI or determinism gate that goes RED on first run reveals a PRE-EXISTING violation (not new). Disposition per close-the-class-vs-migrate-every-site (D-84): the gate ships as a NET; pre-existing hits go to a KNOWN-PENDING allowlist (shrinking-only, never-growing) so the gate is armed immediately while paced cleanup follows. Auto-write any such finding to TECH_DEBT with a categorical trigger.
- **H10 first real validation deferred to E.1.2**: H10's grep/gate is ARMED here, but its first real SoA AVX kernel exercise is E.1.2's relayout (§4a H10 "re-validated in E.1.2's first SoA AVX kernel" — A-adjudication §4b). E.1.0 stands the gate up; E.1.2 is its first non-trivial consumer.
- **Latency BASELINE capture**: the ratchet needs a baseline to ratchet against — capture the `.E.0.10` hot/slow p99 here as the reference the later leaves cannot regress past. Document in HOT_PATH_CHANGELOG.

## C-class re-ground findings (2026-06-16 — pre-draft currency sweep vs HEAD 572c978)

These CORRECT the scaffold above; fold into the full coding body. Captured per document-as-you-go.

1. **H10 determinism axis — SHAPE CORRECTION (the one real change).** `USE_NATIVE_128` production pin was ABSORBED at Ship-B P5 (`CMakeLists.txt:221/233/244` — "one FP path only"); the scaffold's "`±USE_NATIVE_128` byte-compare" is no longer a production toggle. BUT it survives as a *diagnostic* in `tools/check_fp_determinism.sh` (`-DUSE_NATIVE_128` ×{O3,O0}, proves native==generic) = gate (1) of the standing `tools/check_determinism.sh` (FP-golden + locale + replay-locale), already wired as pre-commit **Check F**. ⟹ the FP-native determinism axis is **ALREADY ENFORCED**. The scaffold CONFLATED two axes: FP-native (`USE_NATIVE_128` — gated) vs the **H10 SIMD scalar-fallback** axis (AVX-512 kernels byte-identical to scalar — `ML_Headers/RidgeBlender.hpp` + `BanditLearning.hpp` — **NOT gated**). **CORRECTION: E.1.0's H10 deliverable = EXTEND `check_determinism.sh` with an AVX-512-vs-scalar-fallback byte-compare (canonical-sister, NOT parallel infra); the FP-native sub-axis is done.** Guard-matrix §4a H10 splits at the dive: FP-native = ENFORCED · SIMD-fallback = the remaining HOLE E.1.0 closes. *(D-entry candidate — confirm direction before the §4a edit.)*

2. **Hot-path grep-target set (H7/H20) — refined.** Authoritative = `tools/calls_graph_diff.sh` `SHARDED_FILES[]` (EngineCommon · ControllerEventLoop · EngineSharded + Boot/SlowPath/Async/Run · ShardedBacktestDriver · StrategyParameters · StrategyLifecycle) ∪ the 500ns kernel {`ExecutionCore.hpp`, `GateParameters.hpp`}. Broader + more precise than the scaffold's 4-file guess (had `Async`, missed `ControllerEventLoop`/`GateParameters`). Reuse `SHARDED_FILES` as the SSoT so the grep targets never drift from the calls-graph guard.

3. **H14 starts GREEN.** 0 real C++ bitfields today → no allowlist seed. The grep MUST exclude `?:` ternaries + comment lines (the false-positive surface — a naive `\w+ : N` hits `count : 0` ternaries + `// file:982` comment refs). Teeth-proof: RED on a real `name : N;` struct field, GREEN otherwise.

4. **LATENCY anchors confirmed.** `LATENCY_PROFILING/LITE/BENCH` @ `CMakeLists.txt:18-20`; `LATENCY_BENCH` defs @ `:62-63` (exact). `build.sh build_latency()` ~`:209` (minor drift from scaffold's `:211`).

5. **Wire-in point.** Pre-commit Check chain A-G (each with a `SKIP_*` bypass + teeth-proof): B-Plus(A) · forward-promise(B) · doc-rename(C) · Tests-section(D) · doc-floor(E) · determinism-net(F) · doc-metadata(G). New grep-CI = new Check letters; the H10 SIMD axis extends Check F. Mirror the `check_session_docs.sh` aggregator shape.

**Shape impact:** only the **H10 row's** shape changed — toward LESS work (extend an existing sister; FP-axis already done). H7/H14/H20/H8 keep their shape. Net E.1.0 = slightly smaller + cleaner; ZERO new infra paralleled.

---

## FULL CODING BODY — expanded 2026-06-16 (C-class re-ground applied; awaiting I/A gate)

**End goal (1-sentence):** stand up a STANDING green CI net — grep-CI (H7/H14/H20) + a SIMD-fallback determinism gate (H10) + a latency ratchet (H8) — that RED-builds any later-ship hot-path data-dependent branch / C++ bitfield / non-byte-identical SIMD fallback / latency regression / determinism break, with **ZERO engine behavior change**, so E.1.1's rename + E.1.2's relayout run UNDER a net.

**Ship type:** enforcement-scaffold (CI-only). **Hot path:** NOT touched (gates only). **Operator migration impact:** NONE (no cfg/wire/behavior change). **Risk:** LOW — any break = a CI-config break or a *surfaced pre-existing* violation.

### Phase A.0 — anchor + baseline
- Create rollback anchor `pre-v5.15.5.F.4d.1.E.1.0`.
- Capture the `.E.0.10` latency baseline: `./build.sh latency` (`build_lat/`, `-DLATENCY_PROFILING=ON`), run the fixed replay, record the `EngineSharded_DumpLatency` per-core table (`min p50 p95 p99 max`, `Run.hpp:315`) + the Phase-7.B drainer-cycle histogram (slow p99). **Floor-subtract** the rdtscp overhead (`Run.hpp:329`). Write to `DOCS/HOT_PATH_CHANGELOG.md` as the E.1.0 reference the later leaves cannot regress past.

### Phase B — grep-CI (H14 first = the GREEN-start confidence build, then H7/H20)
**B.1 — H14 `tools/check_h14_no_bitfield.py` (starts GREEN):**
- Scope: struct/union bodies across `CoreFrameworks/ Strategies/ MemHeaders/ ML_Headers/ FixedPoint/ DataStream/ GUI/ Backtest/` headers.
- Detect: `<type> <name> : <int>;` C++ bitfield. **EXCLUDE** `?:` ternaries, `//`+`/* */` comments, access-specifiers (`public:`/`private:`/`protected:`), ctor init-lists, `case` labels (the verified false-positive surface — a naive `\w+ : N` hits `count : 0` ternaries + `// file:982` comment refs).
- Today: **0 hits → GREEN, no allowlist seed.** Mandated alternative present (`MBS_*`/`BITMAP_*`, `MemHeaders/BitmapMacros.hpp`).
- Teeth-proof: inject `uint8_t x : 3;` → RED; remove → GREEN.

**B.2 — H7/H20 `tools/check_h7_h20_branchless.py` (heuristic + KNOWN-PENDING allowlist):**
- Scope (SSoT-shared): reuse `tools/calls_graph_diff.sh` `SHARDED_FILES[]` ∪ the 500ns kernel {`ExecutionCore.hpp`, `GateParameters.hpp`}. Source the file list from the SAME array so the grep targets never drift from the calls-graph guard.
- Detect: data-dependent `if`/`switch`/`?:` on the marked hot/SP dispatch (BG_Evaluate / SG_Evaluate / ExecutionCore_Tick / per-core dispatch).
- **HONOR** the `branchless-dispatch-discipline.md` exception matrix: boot-time-only · `__builtin_expect`-rare · `if constexpr` (compile-time) · genuine binary predicate w/ no alternative computation. NOT violations.
- **HEURISTIC** (a perfect hot-path-branch detector is undecidable) → KNOWN-PENDING allowlist for current hits: **shrinking-only, never-growing** (D-84 close-the-class-vs-migrate). Each allowlisted hit → a TECH_DEBT entry + a categorical trigger (homed, not unhomed). **Sizing comes from the I-class pass** (enumerate current hits → fix-now if few / allowlist if many). The false-pos/neg rate is the #1 thing the A-class refutes.
- Teeth-proof: inject a data-dependent hot-path branch → RED; revert → GREEN.

### Phase C — H10 SIMD-fallback determinism gate (EXTEND `check_determinism.sh`, do NOT parallel)
- FP-native axis (`USE_NATIVE_128` diagnostic) is ALREADY gate (1) of `check_determinism.sh` (pre-commit Check F) — leave it.
- ADD gate (4): a harness TU exercising the AVX-512 kernels (`ML_Headers/RidgeBlender.hpp` + `BanditLearning.hpp`; the `#if defined(__AVX512F__)` paths at `RidgeBlender.hpp:59/493`) compiled ×{`-march=native` (AVX-512 ON), `-march=native -mno-avx512f` (scalar fallback)}; run identical fixtures; `memcmp` outputs → **byte-identical or RED** (the H10 invariant).
- **Reuse the `check_fp_determinism.sh` compile-2-ways harness shape** (canonical-sister, not new infra).
- Teeth-proof (`check_determinism_selftest.sh` "net guards the net"): perturb the scalar fallback → RED; revert → GREEN.
- NOTE: H10's first non-trivial CONSUMER is E.1.2's SoA AVX kernel — E.1.0 ARMS the gate; E.1.2 exercises it.

### Phase D — H8 latency ratchet (`tools/check_latency_ratchet.sh`)
- Build `build_lat`, run the fixed replay, parse `EngineSharded_DumpLatency` stderr (per-core hot p99) + the Phase-7.B histogram (slow p99). Floor-subtract.
- Gate: hot p99 ≤ **500ns** AND slow p99 ≤ **100μs** (H8 absolute) AND ≤ baseline × (1 + margin) (ratchet vs the E.1.0 reference). **Ratchet-DOWN only** — a new lower p99 lowers the ceiling; never silently raised (`/test-strength-audit` discipline).
- Noise: perf is noisy → an explicit margin (~10%) + median-of-N-runs absorbs jitter. **MEDIUM complexity** (the one non-trivial piece).
- Placement: a **MERGE-gate / on-demand CI lane**, NOT a per-commit pre-commit hook (perf builds are too slow for every commit; guard-matrix §3 "CI-as-merge-gate").

### Phase E — wire-in + aggregator + teeth-proof
- `.githooks/pre-commit`: add **Check H** (H14) + **Check I** (H7/H20), each with a `SKIP_*` bypass (chain A-G already established); the **H10 SIMD gate extends Check F**; the latency ratchet is the merge-lane (referenced from CI, not pre-commit).
- Add the grep-CI + the determinism extension to the `check_session_docs.sh`-style aggregator so one invocation runs them.
- **Teeth-proof EVERY gate** (inject→RED→revert→GREEN) — a gate that can't be shown to catch its target is not shipped.

### Tests / verification (gate self-tests — per `feedback_test_change_enumeration_per_plan_body`)
- **NEW** self-tests: each grep-CI + the H10 gate + the ratchet ships a teeth-proof (inject→RED→revert→GREEN), mirrored on `check_determinism_selftest.sh`. *(a) modified: none. (b) broken-replaced: none. (c) NEW: the 4 gate self-tests.*
- **NO** `controller_test` changes (zero engine behavior).
- Build: 5 binaries unchanged; `calls_graph_diff.sh` CLEAN (hot path untouched).

### Acceptance criteria
- ✅ 4 gates live + teeth-proofed (H14 grep · H7/H20 grep+allowlist · H10 SIMD-fallback · H8 ratchet); FP-native axis confirmed already-gated.
- ✅ Guard-matrix §4a flips: H14→ENFORCED · H7→ENFORCED(+allowlist) · H20→ENFORCED(+allowlist) · H10 SIMD-fallback→ENFORCED (FP-native already ENFORCED) · H8→ENFORCED(ratchet).
- ✅ Zero engine behavior change; 5 binaries + `3635/0` unchanged; hot path untouched (`calls_graph_diff` CLEAN).
- ✅ Any surfaced pre-existing violation HOMED (TECH_DEBT + categorical trigger, shrinking-only allowlist) — **ZERO new debt from E.1.0's own code**.
- ✅ OUTBOUND seam armed: the net RED-builds a careless-rename perf/branch/determinism regression for E.1.1.

### Open for the I/A gate (the spots most worth refuting)
1. **H7/H20 grep false-pos/neg rate** + how big the allowlist starts (fix-now vs homed-allowlist).
2. **H10 gate determinism-of-itself** — does `-mno-avx512f` actually exercise the scalar `#else` for the chosen kernels, and is the harness output stable run-to-run?
3. **Latency ratchet baseline stability** — is `build_lat` p99 reproducible enough on this machine to ratchet without false-fails?
4. **Sister-extension vs parallel-infra** — confirm Phase C/D genuinely EXTEND (`check_determinism.sh` / `DumpLatency`) rather than mirror.
5. **Any hot-path file outside `SHARDED_FILES ∪ {ExecutionCore,GateParameters}`** the H7 grep would miss.

---

## 3:3 GATE VERDICT + RESHAPE (2026-06-16 — operator-steered; SUPERSEDES the gate dispositions in the FULL CODING BODY above)

Full 3-I→3-A pre-coding sweep (reports: `plan_checks/2026-06-16-E.1.0-precoding-{I-surface,I2-ecosystem,I3-measurement,A1-vacuity,A2-debt,A3-necessity}.md`). Verdict: **YELLOW — ship E.1.0, reshaped.** These dispositions are LOCKED (operator-steered 2026-06-16) and supersede the "4 RED gates" framing above.

### Locked gate dispositions
- **H14 no-bitfield grep → RED gate (KEEP).** Decidable, 0 hits today, highest reachable tier. Condition: multi-line-ternary-safe regex (exclude `?:` across line breaks + comments).
- **H10 SIMD-fallback determinism → RED gate (KEEP — the marquee; the real protection for E.1.2's SoA AVX relayout).** Extend `check_determinism.sh` (gate 4); reuse the `check_fp_determinism.sh` compile-2-ways harness. **2 conditions:** (1) HARD-ASSERT `__AVX512F__` was defined in the native lane (force `-march=skylake-avx512`) — else vacuously-green on a non-AVX runner; (2) compile + memcmp the REAL kernel `#else` fallback, NOT a re-implemented scalar oracle (golden-master-over-reimplemented-oracle).
- **H7/H20 branchless → ADVISORY smoke (DEMOTED from RED; operator: do NOT drop).** A grep is undecidable here — can't separate a compliant fn-pointer dispatch from a violation, over-fires on ~130 legit SP-dispatch sites, can't catch a branchless→branchy TRANSFORMATION of an existing site. Ship as a Tier-2 advisory complementing `/hft-audit`'s existing Class-28 cadence sweep; teeth-proof injects a NEW branch OUTSIDE the allowlist. **HOMED forward-item:** a stronger non-grep detector (AST/clang-query or calls-graph-based) is the only path to a real RED H7/H20 — track for a later .E leaf if it earns out (NOT achievable via grep).
- **H8 latency → BUILD MINIMAL BENCH INFRA NOW + absolute-budget gate; ratchet → E.1.2 (operator: foundational — set the substrate up properly for the whole .E sequence).** Current substrate isn't gate-ready (no `LATENCY_BENCH` build target; the BENCH p99 line is the single-core-dead path; p99 is a 256-sample sliding window; slow-p99 emitted nowhere; cycles-not-ns). E.1.0 lands the minimal correct substrate: a `LATENCY_BENCH` build target + a bounded-tick bench (fixed sample population) + a lifetime-p99 emit (`CoreLatencyStats.hpp:240` already COMPUTES it — just print it) + cycles→ns via `tsc_ghz`. Ship the ABSOLUTE-budget assert now (hot p99 ≤500ns / slow ≤100µs — noise-immune); the 10%-margin RATCHET-DOWN rides to E.1.2 (its first real consumer). **Scope note:** touches the latency-EMIT path (observability) + the build — NO trading-logic change; stays within freeze-current-behavior.

### 3 must-fixes (LOCKED)
1. Extract `tools/lib/sharded_files.sh` — sourceable SSoT for the hot/sharded file list; BOTH `calls_graph_diff.sh` and the H7/H20 advisory source it (the array is currently un-sourceable inside `calls_graph_diff.sh` → a 2nd consumer would COPY = Class-18 mirror that the .E sequence rots as it ADDS sharded files — NodeState/ClusterState @ E.1.2).
2. Pre-commit Check letters **M/N**, not H/I (H = identifier-retirement, I = reserved — both taken).
3. Allowlist = the existing `KNOWN_COHORT --strict` shrink-forcing mechanism (machine-enforced never-grows), NOT a convention-only baseline file.

### Seam re-point
OUTBOUND seam re-points: **E.1.2 (SoA relayout) is the first REAL consumer** (changes AVX bytes + latency → H10/H8 genuinely bite). E.1.1 (names-only rename) gets proof-of-neutrality (the net proves the rename was byte/perf-neutral) — lighter, not vacuous. Fold into the E.1.2 scaffold's INBOUND seam at E.1.0 ship-close.

### Guard-tool anti-pattern (operator-raised — "do we need anti-patterns for tools?")
YES — capture now, codify at ship-close (pattern-codification cadence):
- **NEW "Vacuously-green guard"** — a gate that passes WITHOUT exercising its target (3 instances this sweep: H10 on a non-AVX runner · H7/H20-grep-as-RED · H8 with no BENCH target). Fix-signature: every gate ASSERTS its target was actually exercised (AVX arm compiled / bench ran / N samples seen). → new RECURRING_BUG_PATTERNS Class at E.1.0 ship-close.
- The non-sourceable-shared-list (must-fix #1) = a **Class-18 (mirror-that-drifts)** instance at the tooling surface.
- Precedent: `check_tools_inventory.py` Check 3 (D-137) already red-builds a load-bearing tool lacking a teeth-proof selftest — the tools layer already gets anti-pattern treatment; this extends it.

---

## IMPLEMENTATION PROGRESS (2026-06-16 — session close; 3 of ~6 pieces landed + WIRED)

**DONE + verified + wired** (all local in the workspace; doc-CI GREEN):
- **must-fix #1** — `tools/lib/sharded_files.txt` SSoT; `calls_graph_diff.sh` loads it via `mapfile` (byte-identical behavior + a missing-SSoT FATAL exit-2 guard). Realized as `.txt` not `.sh` (cross-language: the H7/H20 consumer is Python).
- **H14** (`check_h14_no_bitfield.py` + `test_check_h14_no_bitfield.py`) — decidable RED gate, 0 hits; teeth-proven (7 detected / 17 look-alikes ignored incl. A1's multi-line ternary). Wired: pre-commit **Check M** (direct call; the grep-CI aggregator is deferred to H7/H20 — avoid premature abstraction at 1 gate).
- **H10** (`check_h10_simd_determinism.sh` + `h10_kernel_harness.cpp` + `_selftest.sh`) — the marquee. Compiles the REAL `RidgeBlender_BuildCorr` ×{`-march=skylake-avx512`, `-mno-avx512f`}, memcmps, ASSERTS the `__AVX512F__` split (non-vacuous, proven directly). Teeth-proven (1e-12 perturbation caught). Wired: `check_determinism.sh` **gate 4** + pre-commit **Check F** (trigger widened to the AVX kernels).
- **Enrollment**: all 5 tools in `DOCS/TOOLS.md` (the inventory red-built until enrolled — guards-compound working). Guard-matrix §4a flipped: **H14 → ENFORCED · H10 → ENFORCED**.

**PENDING** (next session — run FRESH per operator):
- **H8** (latency bench) — **run an I/A agent pass FIRST** (genuine design uncertainty; unlike H10's mechanical non-vacuity). The substrate isn't ratchet-ready (no `LATENCY_BENCH` build target; single-core-dead BENCH path; sliding-window p99; slow-p99 emitted nowhere; cycles-not-ns). Build the minimal bench: a `LATENCY_BENCH` target + a bounded-tick bench + a lifetime-p99 emit (`CoreLatencyStats.hpp:240` already computes it) + cycles→ns; ship the ABSOLUTE-budget gate; ratchet-down → E.1.2.
- **H7/H20** advisory (sources `sharded_files.txt`; `KNOWN_COHORT --strict` allowlist) → then the grep-CI aggregator returns (≥2 gates) WITH its rc-propagation selftest.
- **baseline** capture → `HOT_PATH_CHANGELOG`; **V-class verify** (5 binaries + 3635/0 + `calls_graph_diff` CLEAN + run `check_determinism.sh` EXPLICITLY — see the finding below).
- **Ship-close codifications**: the "vacuously-green guard" RBP Class; the seam-fold to E.1.2.

**FINDING homed** (the "are HARD CI checks wired?" honest gap): the determinism net (now incl. H10) is HARD at **pre-commit** (Check F blocks) but is **NOT in `run_all_tests.sh`** (the "verify everything" umbrella never included the determinism net — pre-existing). → the E.1.0 V-class verify must run `check_determinism.sh` explicitly, OR add it to `run_all_tests.sh` (a general CI-completeness call for the next session; candidate TECH_DEBT).

---

## H8 — 3:3 EXECUTED + ORCHESTRATOR-VERIFIED (2026-06-16 session-2; awaiting operator A⁺/B call + cascade-check before build)

Full 3-I→3-A fresh pass fired (operator-directed) + every load-bearing factual claim verified by orchestrator code-read (AR-11). Agents armed M8-style; reports in-conversation (fold to `plan_checks/` at build). **Status: design CONVERGED, build BLOCKED on (1) cascade-check across E.1.1/E.1.2 + codebase, (2) optimization-homing decision, (3) operator slow-gate call.**

### VERIFIED bench mechanics (the 3:3 diffs — A-class broke the I-class proposals; all confirmed by code-read)
- **Gate on the WINDOWED `p99_ns` (exact sorted cycles), NOT `lifetime_p99_ns`.** Host = `i7-11850H @ 2.50GHz`; `CalibrateTscGhz` (`Run.hpp:157-175`) measures the **invariant TSC** (~0.4ns/tick). The lifetime histogram's power-of-2 bucket ceilings (`CoreLatencyStats.hpp:248`) quantize: true 205ns→reports 409.6ns, true 410ns→reports **819.2ns → false-fails the 500ns gate**. Lifetime stationarity only matters for a *ratchet* (E.1.2); the *absolute* gate needs exactness. Non-vacuity teeth = `window_size >= 256` (ring filled).
- **Both BENCH axes required.** `-DLATENCY_BENCH=ON` (compile) gives the hot line; the slow/drainer `[OMS_BENCH]` line is gated by the runtime template param `BENCH` ← `cfg.oms_bench_enabled` (`main.cpp:279-283`). Bench.cfg MUST set BOTH or the slow half is silently unmeasured (half-vacuous). Gate asserts BOTH lines present (mirror H10's `AVX512=1` AND `=0`).
- **Emit in the `[OMS_BENCH]` `if constexpr(BENCH)` idiom, NOT in `DumpLatency`.** `DumpLatency` is **always-on** (`Run.hpp:2345`, unconditional) → a line added there leaks into every production run. **Gate reads the struct field in-process** (`CoreLatencyStats_Snapshot(...).p99_ns` / `LatencyHistogram_Percentile(...)`) and asserts in C++ — NEVER parses its own stderr (the struct→string→regex round-trip is the real Class-18 mirror risk).
- **Decoupling-fit: ALIGNED (survives).** An in-process runtime-side gate reading raw engine `CoreLatencyStats` never crosses the process boundary (the decoupling endgame's actual line); *more* decoupled than reading `TUISnapshot`. stderr-now is the right forward step; the durable seam is the readout *call*, the line format is expected to reshape at `.E.2` (machine-parseable per roadmap `:669`). No new UI-coupling; the dead single-core `tui.hot_*` path (`main.cpp:1221`, `#ifndef MULTICORE_TUI`) is not resurrected.

### CORRECTION (factual error in the prior audit + this plan body)
- **The slow path IS instrumented** — the I3-measurement report's gap #5 ("slow p99 measured nowhere") + this plan body's earlier "slow-p99 emitted nowhere" are **WRONG**. Verified: always-on per-core `CoreLatencyStats slow_path_latency` (`ControllerEventLoop.hpp:678`, enabled `Run.hpp:1654`) brackets the FULL slow-rebuild cycle (`EngineCommon.hpp:503`→`:772-774`) + a 5-section breakdown; surfaced live to TUI/GUI (`EngineTUI.hpp:1796`). What's missing = a headless stderr emit + any budget ASSERTION, NOT the measurement. (I3-report carries the propagated error — correct at build.)

### THE REFRAME (operator insight — slow latency = reactivity = alpha)
The hot path picks up new params once per slow cycle (seqlock). So slow-cycle latency is the FLOOR on regime-shift reactivity → alpha-relevant in volatile regimes, not hygiene. This reframes the slow gate.

### Slow-gate — A⁺ LOCKED as DIRECTION (operator 2026-06-16 s2; implementation pending the scoped A⁺ 3:3)
The H8 budget reads "slow path **REBUILD** cycle ≤100µs" (`CoreFrameworks/CLAUDE.md:86`), but the always-on instrument brackets the WHOLE cycle = ROLLING-dominated **100-300µs steady-state by design** (`SpSectionRegistry.hpp:42`). REBUILD alone = ~5-30µs (fits). **A⁺ (LOCKED):** gate `slow_path_breakdown[REBUILD]` ≤100µs now (teeth) + capture+freeze the WHOLE-cycle as a regression-floor (NOT a hard budget — whole-cycle ≤100µs would RED-by-design) + home the ROLLING optimization (→ E.1.2) with a whole-cycle ratchet-down toward the ≤100µs reactivity target. Rejected: pure-B (gates nothing now); advisory-whole-cycle (toothless — the vacuously-green guard the ship codifies against, Class 51; slow-p99 is decidable so it doesn't earn H7/H20's advisory escape hatch). **Implementation specifics (REBUILD emit / floor mechanics / ratchet / per-core-worst-case / the whole-cycle-floor proportionality question) → the scoped A⁺ 3:3.**

### Gate identity — the (i)/(ii) split + soft-real-time/SLO framing (operator 2026-06-16 s2)
Latency = a PHYSICAL, non-deterministic measurement (soft-real-time; bounded-with-high-probability, no WCET) — gate the percentile ENVELOPE + tolerance, not a point (the GPU/SLO model). The ladder maps onto the split: **p50–p99 = intrinsic code-cost = the reproducible CI regression gate** (single-threaded backtest or pinned-synthetic); **p999–p9999 = the tail (contention/preemption/NUMA) = live-only = operator-run `perf` profiling, NOT a CI gate.** INSTRUMENT-CAP FINDING: `CoreLatencyStats` windowed ring = 256 samples (p99 ceiling; p999 needs ~1k, p9999 ~10k); the lifetime log-histogram spans all samples but quantizes (the 2× bucket error). So the deep tail needs instrument work (bigger ring / finer histogram) OR belongs in live-profiling — decide in the A⁺ 3:3.

### Bench substrate — OPEN (the live-vs-backtest split)
Latency is a PHYSICAL measurement (non-deterministic, hardware/load-dependent) — categorically unlike H10/H14. The sharded backtest runs the SAME code but SINGLE-THREADED (`Backtest/CLAUDE.md`; `BacktestSharded.hpp:167-170` "no drainer thread; synchronously") → a reproducible LOWER BOUND, not live. Split: (i) intrinsic-cost **regression gate** in CI (backtest or pinned-synthetic) vs (ii) **live-latency validation** as an operator-run profiling step (not a deterministic CI gate). Decide which E.1.0 ships.

### Optimization-homing — RESOLVED → E.1.2 (operator 2026-06-16 s2; SUBSUMPTION, accept scope growth)
The slow-path-reactivity optimization (branchless SP per H20 + cut the `rolling_baseline` cache footprint so whole-cycle → ≤100µs) **homes in E.1.2** — E.1.2 is already the cache-relayout ship, so the rolling-stats SoA relayout is SUBSUMED at near-zero marginal cost (`feedback_opportunistic_tech_debt_closure`). Operator accepts E.1.2 grows beyond initial scope + closes more debt than first scoped (candidate fold: the older `.F.4g-resolved-core-cfg-slow-path-cache` plan). Fold into the E.1.2 scaffold's scope + INBOUND at E.1.0 ship-close; the whole-cycle ratchet-down (A⁺) tightens as E.1.2's relayout lands. **Constraint: "same W, better layout" — NOT smaller W** (shrinking W degrades regime detection = alpha loss). poll_interval (regime-adaptive) is a cheaper parallel reactivity lever to weigh IN E.1.2.

### Doc-debt to home (fix at build; #1 waits on the A⁺/B call)
1. `CoreFrameworks/CLAUDE.md:86` ≤100µs row stale/ambiguous vs the whole-cycle instrument (100-300µs) — reconcile (re-label REBUILD vs add whole-cycle row) **per the slow-gate decision**.
2. `RollingStats.hpp:187` "O(W)" comment stale — amortized-O(1) since v5.11.2.C (doc-rot).
3. `Run.hpp:963` "LIVE-only" comment wrong — the slow-stats enable is unconditional.
4. I3-measurement report gap #5 + this body's "emitted nowhere" — correct (see CORRECTION above).

### Oversights logged (4-pillar)
per-core/strategy variance (gate the worst-case ML config) · poll_interval as a cheaper reactivity lever · W↔regime-quality tradeoff (same-W-better-layout only) · branchless-SP payoff is variance-reduction not headline speed (H20 rationale).

---

## SCOPED A⁺ 3:3 — A-CLASS VERDICTS (2026-06-16 s2; 3-I → cascade-bridge → 3-A + orchestrator AR-11 verify; PENDING operator ratification)

The scoped A⁺ implementation 3:3 (with the cascade-bridge meta-discipline dogfooded) REFUTED several I-class recommendations on hard evidence + verified two corrections. **Net: E.1.0 SHRINKS (floor/deep-tail/optimization move OUT to proper homes) + a ship-stopper caught.** Two findings CHALLENGE D-228 (§2 deep-tail, §3 E.1.2-homing) → operator to ratify before D-228 update.

### SURVIVED (the converged gate — build these)
- Windowed p99 (not lifetime) · emit in `[OMS_BENCH]` `if constexpr(BENCH)` (not always-on `DumpLatency`) · gate reads struct in-process (not stderr-parse) · REBUILD ≤100µs ABSOLUTE + whole-cycle FLOOR-only (A-1: the one correct slow-gate call) · code-isolated (cascade: race-free, the emit runs post-thread-join `Run.hpp:2311`<`~2448`; hot path untouched).

### RESHAPED (A-class refuted I-class)
1. **🔴 VACUITY HOLE (ship-stopper):** I-1's `strat=` worst-case assert is VACUOUS — the ML-degrade path stamps `out->strategy_id = STRATEGY_ML` (`StrategyParameters.hpp:824`), so `strat=ML` prints even after a silent SimpleDip fallback (broad entry points: alloc-fail `Run.hpp:928/936`, boot-validate `EngineCommon.hpp:319/374`, file-present-but-stale `Run.hpp:1841/1854/1906/1917`, + `MODEL_CORRUPT`). FIX: gate on degrade FLAGS (`MASK_CORE_STATE_MODEL_LOAD_FAILED | MODEL_CORRUPT == 0`, published `ShardedSnapshot.hpp:616`) + a positive `ml_inference_ran_total > 0` counter. Assert the path was EXERCISED, never a LABEL. → **Class-51 FIRST CANONICAL worked example** (ship-close task #6).
2. **MISSING EMIT INFRA:** no sharded per-core REBUILD/hot p99 emit exists (`[OMS_BENCH]` = drainer-only; `[BENCH]` hot = dead single-core path, no p99). E.1.0 must BUILD the sharded per-core p99 emit FIRST → then the gate reads it.
3. **FLOOR → E.1.2 (not E.1.0):** E.1.1 is names-only → cannot move whole-cycle p99 → the floor protects nothing at E.1.0/E.1.1; E.1.2 re-baselines it anyway. Capture the floor at E.1.2 (where the ratchet consumes it). E.1.1 stays names-only, no floor dependency.
4. **DROP committed `latency_baseline.txt`:** the bench emits raw CYCLES (`Run.hpp:2444` `p99=%llu cy`) → a committed absolute is machine-frequency + turbo-specific (meaningless cross-machine/CI) AND duplicates `HOT_PATH_CHANGELOG.md` (SSoT violation, Task #4 routes there). Use RUN-RELATIVE floor.

### CHALLENGES D-228 (operator to ratify)
- **§2 deep-tail (you leaned expand):** A-2 fair-adjudicated → DEFER to `.E.2`. Operator right the sister spec (`runtime-toggleable-bench-gate-pattern.md` + shipped `LatencyHistogram`) makes it cheap, BUT it's bench-only VISIBILITY = `.E.2`'s deliverable (`built-in-observability-pattern.md:28` `fox_node_latency_hot_p9999_ns`), NOT a CI GATE — deep tail is ungateable in CI (single-thread backtest `ShardedBacktestDriver.hpp:8-19` has NO tail). Trap: "expand the instrument"→`CoreLatencyStats` is the EXPENSIVE always-on one (256→10k ring = 640KB blows L1d); the spec means a DIFFERENT bench-gated instrument. REC: p99-gate now + deep-tail observability at `.E.2`; optional bench-only hybrid (compile-gated, hot-only) as a `.E.2` down-payment if eyes-on-tail wanted now.
- **§3 E.1.2-homing (you locked subsumption):** A-3 REFUTED + orchestrator-VERIFIED → it's a BALLOON + LOW-ROI, not a ~0-cost subsume. ROLLING is COMPUTE-dominated (11 Push + regression, `ControllerEventLoop.hpp:2183-2204`); `rolling_baseline` = 1-of-4 RollingStats, ~64-BYTE per-cycle hot working set (NOT 64KB resident — cold between once-per-cycle pushes); DISJOINT cohort from E.1.2's Portfolio/Position SoA (`Portfolio.hpp` has 0 RollingStats refs; different structs/access). A same-W cache relayout can't move a compute-bound section. The `.F.4g` candidate-fold is a SEPARATE ~500-LOC HIGH-RISK cfg-cache ship (10-15 files) — folding all into E.1.2 violates its locked layout-only/struct-FINAL charter. REC: OWN `.E` leaf (E.1.M-class), re-scoped to the COMPUTE lever (fewer/cheaper pushes + the regression math), NOT just rolling_baseline's cache.

### VERIFIED corrections (AR-11, orchestrator code-read)
- `sizeof RollingStats<64,1024>` = **65,984 B (64.4KB)**, not ~1.5MB (compiled). The `PortfolioController.hpp:253/256/257` size comments (24KB/393KB/1.5MB) are ALL stale post-Ship-A — doc-rot to fix.
- The slow path IS instrumented (prior I3-report gap#5 + this body's "emitted nowhere" wrong — already corrected above).

### NEW homings
poll_interval adaptive = cheap for non-ML, ML-feature-distribution string for ML (`CoreModelZoo.hpp:1391` reward-lookback + label-autocorrelation `CfgFieldRegistry.hpp:281`) → carve "static for ML nodes" · the un-written E.1.2 fold → register as `<!-- D -->` sentinel (`check_forward_promise_audit` covers ledger auto-writes, NOT plan-scope folds; `/capture-audit` Check 4 catches sentinel-tagged) · 3 stale size comments → fix at the leaf that touches RollingStats.

---

## HARDENED I→A RE-PASS (2026-06-16 s3 — Task #14; SSoT = decision log D-232)

A FRESH hardened 3-I→3-A pass (M8-armed, with the instruments that didn't exist at the prior A⁺ 3:3) RE-CONFIRMED the core mechanics but RE-OPENED substrate + scope + the vacuity mask. **The re-run paid off — it caught 3 things D-228's A⁺ missed.** Every load-bearing claim orchestrator-verified by code-read (AR-11). Full ledger + the AR-11 disagreement-resolutions = **D-232**. Disposition of every finding below (none dropped).

> **⮑ RESOLVED 2026-06-16 s3 (D-233) — FORK A/B settled.** H8 ships as a **STATIC ASM instruction-budget TOOL** (hot + slow path; instruction-count budgets, NOT wall-clock-ns), sister to `check_struct_size_budget.py`. FORK A = the static axis (option 1, automates the manual `-S` inspection); option 2 (PMU/running) DEFERRED to the planned running-validation step; option 3 (wall-clock-ns) DROPPED. **FORK B DISSOLVES** — the static tool is zero-engine-behavior → E.1.0 stays in-charter; the counter / ML-worst-case / `CFG_DRIFT_STRICT_REFUSED`-mask were runtime-bench solutions → they ride the option-2 running step (no run-vacuity for a static tool). **Process right-sized**: apparatus → BUILD + dogfood-iterate + teeth-proof (no 3:3 re-confirm). The MUST-FIXES below re-home accordingly: (a)/(b)/(c) [vacuity mask / counter / per-core worst-case] → the running step; (d) floor-measure → moot (no wall-clock floor); (e) calls_graph orphan-wiring → still applies (the tool is a new file, not an engine fn — N-A for engine orphans); (f) doc-debt → unchanged (#11/D-229). The instruction-count ratchet-down is feasible at E.1.0 (deterministic; pinned toolchain + tolerance).

**✅ SURVIVED (build these as-is):** windowed-`p99_ns` not `lifetime_p99_ns` (±2× pow2-bucket false-fail reproduced @ `CoreLatencyStats.hpp:248`) · in-process struct read not stderr-parse (Class-18) · `window_size>=256` a TRUE liveness teeth (the "garbage-ring" path REFUTED) · slow-gate A⁺ (REBUILD ≤100µs absolute + whole-cycle floor-only) · race-free post-join emit (joins by `Run.hpp:2311` < emit `:2438`).

**🔴 FORK A — SUBSTRATE (PENDING operator ratification; reshapes D-228):** no clean CI substrate. Single-threaded backtest = VACUOUS (no `CoreLatencyStats_Enable` — it lives in `EngineSharded_Run`; the backtest is a separate fn) AND blind to H8's motivating failure class (false-sharing needs a 2nd core). Threaded synthetic `EngineSharded_Run`(BENCH=true) = faithful + enabled but not CI-reproducible (jitter/floor/TSC→ns each corrupt an absolute-ns gate on a shared runner). Options: **A1 operator-run harness** (matches `latency-path-discipline.md:346`) **+ A2 CI gross-catastrophe cycles-tripwire** on the backtest *(recommended)* · A3 push for a real pinned-synthetic CI gate. Guard-row flips to ENFORCED-via-harness, NOT ENFORCED-via-CI-gate.

**🟡 FORK B — SCOPE/CHARTER (ship-minimal-and-split):** the counter (new runtime field+increment) + backtest-enable BREACH E.1.0's enforcement-scaffold/zero-behavior bar. **E.1.0 keeps:** `LATENCY_BENCH` build target + a per-core p99 emit/readout **REUSING `DumpLatency`'s iteration shape** (canonical-sister; NOT a new parallel emitter — corrects the "build the emit FIRST" overstatement: `DumpLatency` @ `Run.hpp:309-333` + the TUI @ `:2080-2089` already emit per-core p99) + the absolute/REBUILD checks. **SPLIT → E.1.2 INBOUND:** the `ml_inference_ran` counter + ML-worst-case gate + the ratchet + the floor capture. **→ .E.2:** the deep tail (256-ring caps at p99 = a hard instrument limit). **2 wording amendments** (else paper-ENFORCED = a Class-51 instance): acceptance "zero behavior change" → "zero TRADING-behavior change + a BENCH-gated observability emit"; "H8→ENFORCED(ratchet)" → "ENFORCED(absolute/harness); ratchet→E.1.2".

**🟡 MUST-FIXES (regardless of fork):**
1. Vacuity mask **+= `CFG_DRIFT_STRICT_REFUSED`**, REGISTRY-sourced (`FOREACH_CORE_STATE_FLAG` degrade subset; H15/H18) — NOT a hardcoded 2-bit literal. *(A-1 ship-blocker — a strict-refused ML core passes the 2-bit mask today.)*
2. Inference-ran proof covers **single-zoo + ensemble** (`predict_call_count` is ensemble-only).
3. Per-core gate = **MAX p99 over ALL `num_cores` + bench the WORST-CASE ML config** — LOCK as acceptance criterion (H22), not an open item.
4. Floor = **MEASURED (empty-bracket calibration)**, not a hand-asserted ~30ns constant (≈ a real regression's magnitude).
5. `calls_graph_diff` **orphan-wiring** for the new BENCH-gated emit fn (or self-RED vs the acceptance criterion).
6. Doc-debt corrections: **L192 STALE-WRONG** (`run_all_tests.sh:49` DOES wire `check_determinism.sh` HARD) · **L227 imprecise** (the `Run.hpp:963` "LIVE-only" comment — the enable is unconditional within `EngineSharded_Run`, not live-gated; the backtest just never reaches it) · the size/`O(W)` comments → #11/D-229.

**6 UNVERIFIED hand-asserted budgets** (REBUILD ~5-30µs / ROLLING ~100-300µs / PUSH+TIME_EXIT+TRAIL / hot ~100-500ns / rdtscp ~25-30ns / instrument ~50ns) — derived-facts-drift: NO decision/ROI locks on them until the bench MEASURES them.

**Class-51 "vacuously-green guard"** gains a 4th first-canonical instance (a gate fail-CLOSED because its instrument's runtime key is never set on its own substrate) → Task #13.

**NEXT:** operator ratifies FORK A/B → a SCOPED re-confirm on the corrected (harness) shape (`feedback_recascade_audit_on_corrected_shape` — the substrate finding is a material reshape) → consult-before-coding → build.

### Tool-design lessons → fold into the ship-close conformance DESIGN_SPEC (#6)
Surfaced + PROVEN this session (the per-detector teeth caught 2 real vacuity bugs); the spec must capture all FOUR:
1. **A static-ASM detector can be a STRONGER gate than a source-grep** — the compiled ASM is ground truth where the source check is undecidable. This is why the **branch-count SUBSUMES the H7/H20 grep** (the grep over-fires + can't separate a compliant fn-pointer dispatch from a violation; the ASM branch is unambiguous). General rule: when a source-grep invariant is undecidable, the compiled-ASM may be the decidable detector.
2. **A static-ASM detector's mnemonic patterns MUST enumerate the FULL variant space** (SSE + AVX/`v`-prefixed + FMA + scalar `s[sd]` AND packed `p[sd]`; the full allocator/new-delete/pthread name space). PROVEN by the dogfood: the float detector was SSE-only (`mulsd|addsd`) and **silently missed the `-march=native` fused `vfmadd`** → fixed. **Corollary the independent re-verify added — broadening can OVER-fire:** I then broadened the indirect detector to add `jmp *` tail-calls, but that **false-flagged GCC `switch` jump-tables** (same unindexed `jmp *%rax`, index in a prior `movslq`) → reverted to `call *` only (see IMPLEMENT-status). So a broadened pattern needs BOTH its own teeth AND a no-false-positive check; under-enumeration (lesson) and over-enumeration are twin hazards.
3. **The selftest teeth ARE the guard against under-enumeration** — EVERY detector needs an inject→RED teeth probe, not just float. The per-detector teeth table (6 cases: float/div/forbidden-call/indirect/branch/non-vacuity) is what surfaced lessons 2 + 4.
4. **A detector over an UNLINKED `.o` is BLIND to external symbols** — an external `call malloc` shows as `call <placeholder>` with the symbol only in a *relocation*; the forbidden-call detector was **VACUOUS** (a Class-51 vacuously-green guard *inside the Class-51-codifying tool* — it would pass green regardless of malloc) until `objdump -r` + reloc-parsing. Lesson: a detector must read what it actually claims to check (linked binary OR the relocations), and the teeth prove it does. *This is the headline worked-example for both the spec AND Class-51's first canonical (#13).*

**IMPLEMENT status (finish-item #4 done; an independent re-verify then caught + fixed 2 more):** (1) branch-count-subsumes-grep DECIDED (D-234); branch DETECTION teeth-proofed — the rare-vs-data-dependent *classification* stays the heuristic per D-236 (not teeth'd; CAVEAT: `--update-budgets` makes it a live ratchet gate, so the 0.75 heuristic becomes load-bearing then — verify at that step). (2) variant-completeness: float (AVX/FMA), div, forbidden-call (reloc-aware via `objdump -r` + full allocator/new-delete[`_Znwm/_Znwj`/`_Znam/_Znaj`]/pthread space), indirect = **`call *` only** (EXCLUDING indexed `*(,%idx,scale)` fn-ptr-table calls = the good branchless pattern); spill = labeled heuristic-ADVISORY. (3) ALL 6 detector teeth FIRE (verified). **An independent re-verify (the operator's repeated "are you sure" — model-bounded-self-check escalation) caught + FIXED 2 real CORRECTNESS bugs** (empirically verified): (a) the indirect detector **false-flagged GCC `switch` jump-tables** (they dispatch via an *unindexed* `jmp *%rax` — the index is in a prior `movslq` → indistinguishable from a real tail-call without data-flow) → **reverted the `jmp *` flagging** (proven: switch→indirect=0, `call *`→indirect=1); (b) `--update-budgets` silently dropped a skipped manifest row from the baseline → now **WARNS** (incomplete-baseline guard). HOMED finish-items (reviewer-surfaced): the 4 remaining D-233 manifest rows (`BG_Evaluate`/`SG_Evaluate` are inlined into `ExecutionCore_Tick` → covered transitively; `EventLoop_RebuildOneCore`/`Regime_Classify` need probes) · indirect-**tail-call** (`jmp *`) detection via a look-back (the only way to separate it from a jump-table) · the `--selftest` exit-code hardening (currently keys on a string — works, but a soft mirror). REMAINING tool finish (the original #1/#2/#3): slow-path callee-symbol follow + grandfathered-25 baseline + CI-wire.
