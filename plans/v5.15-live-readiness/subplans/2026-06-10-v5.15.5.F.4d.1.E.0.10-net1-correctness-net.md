---
type: subplan
ship_tag: v5.15.5.F.4d.1.E.0.10
ship_kind: pre-`.E.1` correctness net — characterization tests + CI invariant checks + backlog re-triage. TEST/TOOLING ONLY (no engine code; cannot break the hot path).
status: DRAFT v0.1 (pre-audit-gate)
audit_tier: HIGH-RISK (code-risk LOW — test/tooling; COMPLETENESS-risk HIGH — gates the riskiest ship `.E.1` + re-triages the capital-correctness backlog. Heavier-default posture applies to the enumeration, not the build.)
sprint: v5.15-live-readiness
predecessor: v5.15.5.F.4d.1.E.0.9 (Ship B — decimal money)
successor: v5.15.5.F.4d.1.E.1 (Foundation — Core→Node rename; this ship is its safety net)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
implements: the un-executed `.E.0` Job-B (Net-1) — re-scoped post-Ship-B; D-73/D-74 (Net-1/Net-2 split) + D-98 (backlog→`.E` structural absorption) + D-110 (decimal recovery round-trip)
findings_sidecar: PRODUCED by this ship (the re-triaged live-disposition finding register)
sister_specs:
  - feedback_phased_pre_rework_correctness_foundation.md (the bedrock→lock-current→gate sequence)
  - feedback_golden_master_over_reimplemented_oracle.md (characterization = freeze real output + diff)
  - feedback_two_foundations_determinism_vs_correctness.md (Net-1 freezes current behavior; deliberate fixes are separate)
  - feedback_guard_matrix_bounds_foundation_hardening.md (harden until no HOLE for `.E.1` surfaces)
  - feedback_tag_disposition_at_fix_time.md (the register dogfoods this; WH-7 structural close)
naming_note: proposes re-aligning semantic = tag from here (no separate semantic number; this is just `.E.0.10`). Kills the D-88 divergence that caused this session's "is `.E.0.2` done?" confusion. Operator-confirm.
---

# `.E.0.10` — Net-1 pre-`.E.1` correctness net + backlog re-triage

## TL;DR

The un-executed `.E.0` Job-B, finally — re-scoped for the post-Ship-B engine. Build the **Net-1 regression safety net** (characterization tests + H1–H20 CI invariant checks on the *current decimal engine*) so the `.E.1` ~5,000-site Core→Node rename **cannot silently regress behavior** — a reintroduced bug becomes an instant red build, not a silent capital loss. Net-1 was correctly *deferred* behind the numeric core (freezing a golden against the old `FPN_Binary` money format would have been worthless — Ship B re-encoded it); now the format is stable, so it's unblocked.

But first it must confront the stale backlog: the **141 `.E` findings + 93 TECH_DEBT + 5 PARITY** (D-98) have no live per-item disposition and predate the numeric core. Phase 1 re-triages them into a **live-disposition register** (structured data — dogfoods the WH-7 discipline), runs the **D-110 recovery probe** early, and produces the **backlog→`.E.1`-home mapping** (task #10). The build is scoped from what discovery finds.

## Findings cross-check (verified against the corpus, 2026-06-10)

Corpus LOCATED + read: `plan_checks/E.0-audit-reports/pre-implementation-findings/` (`CANONICAL-FINDINGS.md` + per-ship sidecars + `PRE-PAPER-TEST-findings.md` [55] + `MASTER-BACKLOG.md`). Buckets: E.1=30 (1 CRIT/9 HIGH) · E.2=14 · E.3=10 · E.5=6 · E.6=2 (1 CRIT) · PRE-PAPER-TEST=55 (21 HIGH) · BACKLOG-STANDALONE=24. Every finding already carries `fix_ship` · `ci_tool_candidate` · `test_gap` · `needs_runtime_confirmation` metadata.

**Result: the plan is SOUND; the findings sharpen it 6 ways.**

1. **The biggest cluster needs REAL per-finding verification — NOT a freebie (VERIFIED 2026-06-10, correcting an earlier guess).** fpmem-1..6 (23 findings, 9 HIGH) was the largest section. A grep against current source: `FixedPoint64.hpp` is DELETED ✓ — BUT `USE_NATIVE_128` is **STILL PRESENT** (8 source refs `FixedPointN.hpp`+`BuildFlags.hpp` + 3 CMake), `_to_fp64`/`FixedPoint64` refs **still 5** in `FixedPointN.hpp`, and `FPN_InvSqrt` **still routes through `sqrt(double)`** (FixedPointN.hpp:936 — the IEEE-round-trip shape the finding flagged). So the native-determinism / tested≠shipped / conversion-pun findings are **NOT confirmed closed**; Net-2 (F-056/58) + the certified-generic delegation may close *some*, but that's a per-finding grep, never an assumption. **Lesson (paid twice this session): nothing is closed until grepped — the re-triage is load-bearing, not a rubber-stamp.**

2. **The Net-1 characterization surfaces now have concrete finding IDs** (turns "PERSIST surfaces" into a finding-backed checklist):
   - fee/accounting exact-value → `oms-ts-1` (the ±$15 fee-blind check whose comment lies it verifies fees) + `oms-ts-2` (`sum(core_realized)==oms->realized_pnl` / `sum(core_fees)==oms->total_fees` never reconciled)
   - snapshot/recovery money-exact (the D-110 cluster) → `persist-dod-1` (torn un-synchronized snapshot save) + `wfa-1` (warm-restart EXIT-DISARMED) + `tsa-live-2` (`Reconcile_ApplyMissedFills` asserts count-not-balance) + `persist-8` (paper-reset zombie active flag)
   - parser byte-equiv / the F-059 stub-oracle → `hpg-bc-1` (`LegacyReferenceDriver` validates the `SG_Evaluate` stub → byte-identity claim false for the real exit path)
   - regime-given-input → `rsf-ts-1` (`Regime_Classify` has no direct classification test)

3. **Task #10 routing PARTIALLY EXISTS** — every finding already carries a `fix_ship`, so findings→`.E`-home mapping is done at the finding level. The GAP is (a) live disposition post-Ship-B + (b) the 93 TECH_DEBT + 5 PARITY items (likely not findings-routed). → Phase 1e narrows to **disposition + ledger-item routing**, not a from-scratch mapping.

4. **conc-5 + 49 provisionals need runtime-confirm on a DISPOSABLE CLONE** (corpus hard-constraint #3; both CRITs are provisional). Phase 1c/1d adopt the throwaway-clone discipline; fpmem provisionals are likely mooted (#1) → confirm cheaply.

5. **The findings' own §7.5 ("ensuring findings get USED") IS this plan's thesis** — 97 `ci_tool_candidate`→CI + 122 `test_gap`→regression = "the real guarantee, machine-enforced." Net-1's deliverable. The WH-7 disposition guard extends it (filed ≠ handled ≠ dispositioned).

6. **SCOPE (operator-confirmed 2026-06-10 — expand where it strengthens the foundation):** Net-1 characterizes **every surface the `.E.1` rename TOUCHES and should keep INVARIANT** — broader than a minimal E.1-bucket cut, because more characterization of the kept surfaces = a stronger net, which is the whole point. **The LIMIT (where expansion stops making sense):** NOT the CHANGES-BY-DESIGN surfaces (`conc-5` concurrency model + drainer→per-node absorption — freezing behavior you're about to deliberately change = a worthless golden, two-foundations), and NOT the future-ship surfaces (E.2–E.6 — characterize at their own ships, where they actually exist). So: **rename-touched-invariant = IN; changes-by-design + not-yet-built = OUT.** The FIXES for buggy kept-surfaces (e.g. `oms-ts-1`'s fee-blind check) still stay SEPARATE (characterize-now vs fix-later).

## End goal + acceptance criteria

**End goal:** `.E.1` is *safe to start* — current behavior on the surfaces the rename KEEPS is frozen as a regression net; the backlog is a live-queryable register with every item dispositioned + `.E.1`-homed; the decimal recovery path is proven money-exact.

**Acceptance:**
- [ ] PERSIST characterization tests GREEN on the current engine (fee/accounting · regime-given-input · parser byte-equiv · snapshot money-exact round-trip).
- [ ] H1–H20 CI invariant checks live for the surfaces `.E.1` touches (close the guard-matrix HOLEs the rename surface needs; the rest stay TBD-during-`.E.1`-dive).
- [ ] **D-110 probe GREEN** — snapshot save→load round-trips every money field EXACTLY under the decimal format (or its failure is fixed + regression-tested).
- [ ] Live finding-register: every one of the 141+93+5 items carries `open | in-flight | closed-by-<ship> | superseded` + an `.E.1`-home where applicable.
- [ ] `conc-5` "`.E.1` closes it" claim VERIFIED (not inherited as inference).
- [ ] **Disposition-tracker CI guard live** (M7 structural close of WH-7 — an un-dispositioned finding = red build).
- [ ] Both standing CI gates (determinism + replay) still GREEN; suite still 3285/0.

## Phase 0 — designation + anchor
Re-align semantic = tag at `.E.0.10` (operator-confirm); pre-tag anchor; `/sync-workspace`.

## Phase 1 — DISCOVERY / re-triage (the scoping audit — this is where the rigor goes)
1a. **Re-locate the backlog.** Find where the 141 `.E` findings actually live now (the guard-matrix references `E.1-findings.md` etc. that aren't on disk — locating them is step one) + the 93 open TECH_DEBT + 5 open PARITY.
1b. **Re-triage each against the post-Ship-B engine** → `closed-by-numeric-core | still-open | newly-created-by-Ship-B`, written into a **live-disposition register (structured)**. Many will already be closed by Ship A/A.5/B — the register makes that *queryable* instead of assumed.
1c. **D-110 probe** (early, cheap, high-signal): save a populated snapshot → load → assert every money field round-trips EXACTLY under decimal. CRITICAL-or-clear in one run.
1d. **Verify `conc-5`** (submit_queue single-producer race) — is it truly "`.E.1` CHANGES-BY-DESIGN closes it," or a live CRITICAL needing handling? (currently tagged "LIKELY/verify".)
1e. **backlog→`.E.1`-home mapping** (task #10): each still-open item → the `.E` ship that structurally closes it.
**Output:** a scoped Net-1 surface list + the known finding-register + the `.E.1`-readiness picture, branches made visible.

## Phase 2 — Net-1 BUILD (scoped from Phase 1)
- **PERSIST characterization tests** (golden-master: run real path → freeze real output → diff) for the surfaces `.E.1` keeps. The rename later renames the symbols; the assertions stay.
- **H1–H20 CI invariant checks** the rename touches (grep-CI / static_assert) — closes the guard-matrix HOLEs on the `.E.1` surface.
- **The findings-disposition tracker + its CI guard** — the M7 structural prevention of WH-7 (un-dispositioned item = red build). This is the "never again" guard.

## Phase 3 — CLOSE
Standing CI gates green; register live + complete; the `.E.1` plan's `findings_sidecar:` populated from the register; postmortem + CHANGELOG.

## Verification approach (token-calibrated — NOT blanket fan-out)
- **Mechanical** (finding-still-exists · D-110 pass · suite green · invariant-check fires) → run the tool, **zero subagents**. Most of Phase 1.
- **Routine judgment** (is this finding still valid post-Ship-B?) → **one agent** per batch.
- **3–5 adversarial quorum on EXACTLY ONE dimension** — completeness of the money-surface enumeration ("is there a capital surface the rename touches with no characterization?"). A miss = false floor = worthless net. This is the only place the fan-out earns its tokens (the #11 money-core pattern: 6/6 real edges single audits missed). Plus one standing completeness-critic.

## Canonical sister registries considered (the disposition register is a NEW registry)
- **TECH_DEBT `{open,in-flight,closed}.md` 3-file split** — the EXISTING disposition mechanism. The finding-register EXTENDS/mirrors its pattern (status-by-location), does NOT reinvent it. Likely: the register IS the findings slice of the same discipline.
- **`E-guard-coverage-matrix.md`** — the existing distribution surface (§4b finding-classes-by-owning-ship); the register feeds it.
- **`findings_sidecar:` convention** — each `.E.x` plan's per-finding fix-design carrier; the mapping populates these.

## Tests changed (per test-enumeration discipline)
- **NEW:** PERSIST characterization test sections (fee/accounting · regime · parser · snapshot round-trip); H1–H20 CI invariant checks; the disposition-tracker CI guard + its negative self-test (AR-4 — prove it goes RED on an un-dispositioned item).
- **Modified/broken-replaced:** none expected (additive; if the D-110 probe fails, the fix gets its own regression test).

## Operator migration impact
None — test/tooling only; no cfg/wire/persistence/engine change. (If the D-110 probe surfaces a real recovery bug, that fix carries its own migration note.)

## Required reading
`.E.0` plan Phase F2 (the original Net-1 spec) · D-73/D-74/D-98/D-110 · `E-guard-coverage-matrix.md` · `feedback_golden_master_over_reimplemented_oracle` + `feedback_phased_pre_rework_correctness_foundation`.

## Decision log (this ship)
- (pending operator confirm) re-align semantic = tag at `.E.0.10`; build the WH-7 disposition-tracker CI guard as the M7 structural close; verification = mechanical-zero / routine-one / capital-completeness-quorum-of-3–5.
