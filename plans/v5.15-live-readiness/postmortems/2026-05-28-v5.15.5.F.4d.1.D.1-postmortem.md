---
type: postmortem
ship_tag: v5.15.5.F.4d.1.D.1
date: 2026-05-28
ship_kind: doc-system end-goal-language sweep (META; no engine code)
predecessor: v5.15.5.F.4d.1.D
successor: v5.15.5.F.4d.1.E.0 (audit + pre-.E correctness net)
hot_path: UNTOUCHED
sister_docs:
  - subplans/2026-05-28-v5.15.5.F.4d.1.D.1-doc-system-end-goal-language-sweep.md (plan body v0.5)
  - decision-logs/v5.15.5.F.4d.1.D.1-doc-sweep.md (DD-1..DD-5)
  - handoffs/2026-05-28-v5.15.5.F.4d.1.D.1-doc-sweep-checkpoint-handoff.md
---

# v5.15.5.F.4d.1.D.1 — postmortem (doc-system end-goal-language sweep)

## Ship summary

META doc-system sweep — **no engine C++ touched** (only `Version.hpp` string bump). Reshaped the doc system to the post-`.E` architecture (per-node / Cluster-Node-Deployment / multi-exchange / headless) BEFORE any `.E` coding, so every subsequent ship plans against current canonical context. Conceptual `per-core`→`per-node` swept in **forward-looking timeless docs**; **code symbols deferred to `.E.1`** (rename with the code); **historical-record docs preserved** + bridged via `DESIGN_PHILOSOPHY § 15 Glossary` (per `feedback_terminology_evolution_bridge_not_history_rewrite`).

## Headline finding (most consequential outcome) — rename-tool escalation

**TECH_DEBT-142 (HIGH):** the prose-token doc-rename tool (`check_doc_rename_classification.py`) is **structurally unsafe for `.E.1`'s ~5,000-site CODE rename.** At Phase F it over-flagged renames in docs that are *about* the rename — where the old token must STAY:
- transition-catalog tables (running-list `Old name → Suggested new` rows),
- catalog/ledger title citations (Class 26 "Global consumer reading **per-core** field", TECH_DEBT-129 "**Per-core** drainer architecture" — preserved per DD-3),
- accurate current-state claims ("Engine is *currently* per-core sharded for hot path").

For DOCS this was caught by manual triage. At `.E.1`'s code scale (~5,000 sites) it **cannot** be hand-triaged — `.E.1` needs **symbol/AST-aware** tooling (clang-based or equivalent), NOT prose-token substitution. **Triple-captured so `.E.1`'s window can't miss it:** B19 pillar worked-example + TECH_DEBT-142 + the ⚠️ `E-MASTER-REFERENCE § 7` callout.

## What landed

**Prior session (Phases A–B + bulk sweep):** 2 NEW CI tools (`check_doc_rename_classification.py` + `check_plan_body_tests_section.py`/Check 45) + canonical pre-commit hook (closed a `.D`-era versioned-source drift) + ~525 conceptual renames across timeless docs + `DESIGN_PHILOSOPHY § 15 Glossary` (canonical SSoT + evolution-note) + Class 36 logged.

**This session (Phases F–H):**
- **Manual cases:** `CLAUDE_INVARIANTS.md` "Partial Exits — Two-Position-per-**Core**"→"**Node**" heading + conceptual prose (`each node owns 2 slots`, `node c`, `max nodes`); code symbols (`MAX_PORTFOLIO_POSITIONS`, `Sharded_LegSlot`, `ExecutionCore_Tick`) LEFT for `.E.1`. 13 other tool-flagged hits verified **LEAVE** (per-CoreContext code symbol / `single_core` delete-candidate / code-fence / transition-doc).
- **Rename sweep of `.E` plan bodies + running-list: SKIPPED** — all 16 + 8 tool-flagged renames verified LEAVE (transition-docs; auto-apply would corrupt citations + falsify current-state).
- **Additive deliverables:** D-65/D-66 (`.E` decision log); plan-template "Tests changed" section (`future-oriented-plan-template.md`); `/readiness` Check 45 sidecar + table row + **closed the Check 44 sister-cohort gap** (sidecar existed, table row missing); `.E.1` **first-canonical** Tests-changed section (grounded in Core→Node + drainer-absorption + multi-exchange + topology scope); B19 pillar (`implementation-layer-blindspot-taxonomy.md`, augmented with this session's concrete finding); CLAUDE.md § How-to-find glossary cross-ref; `E-MASTER § 7` escalation callout.
- **Phase G:** decoupling-roadmap reframed (`converging at .E.2` Status section + bridge note + canonical-binary-name bridge; **original 2026-05-12 vision body preserved**); dir-banner READMEs in `postmortems/` + `handoffs/` (point to § 15 Glossary).
- **TECH_DEBT opened:** -140 (`engine_mode` vestigial), -141 (`BacktestSharded_Run` unification), -142 (rename-tool escalation), -143 (public CHANGELOG backfill — see below).
- `Version.hpp` bump `5.15.5.F.4d.1.D` → `.D.1`.

## The discipline working in real time

`verify-before-write` caught **two over-rename hazards** (running-list transition-catalog; `.E` plan bodies) that a blind `--apply --write` would have corrupted, **and** caught my own accuracy slip (Phase G roadmap reframe wrote "pre-`.E` per-node sharded" → fixed to "per-core", since it's a historical-state claim). This is the operator's "learning + documenting actually serves a purpose" thesis paying off: the accumulated discipline (transition-doc recognition; DD-3 catalog preservation; current-state-claim accuracy) turned a potential corruption into a clean ship + a load-bearing `.E.1` escalation.

## Scope adaptations from the pre-drafted plan

| Pre-draft assumed | Reality | Adaptation |
|---|---|---|
| ~14 conceptual renames in `.E` plan bodies | transition-docs; ~0 legit renames | rename sweep SKIPPED |
| Check 45 inline `### Check N` section | SKILL.md uses table-row + sidecar files | sidecar `check-45-*.md` + table row |
| Decoupling roadmap YAML `status:` frontmatter | prose `**Status:**` line; already "endgoal"-framed | prose reframe + Status section |
| `.D.1` CHANGELOG row "follows `.D` pattern" | `.D` has NO row; whole v5.12→v5.15 cycle uncovered | row SKIPPED → TECH_DEBT-143 |

## CHANGELOG finding → TECH_DEBT-143

The public `CHANGELOG.md` + `DOCS/changelogs/` both lapsed mid-May 2026 — the entire `v5.12 → v5.15.5.F.4d.1` cycle is uncovered except an anomalous floating `.B.7` row. Root cause: high-velocity internal sub-ship tags don't map to coarse public CHANGELOG rows (which is what TECH_DEBT-126 two-axis versioning addresses). The sprint's real record lived in workspace postmortems + decision logs + GPG tags. `.D.1`'s per-ship record = GPG tag + Version.hpp bump + this postmortem (no lone CHANGELOG row). Backfill routed to `.F` / umbrella close, aligned with the external-version-scheme decision.

## DD-5 resolved

Synthesis (= DD-4): proceed `.E` **gated behind the pre-`.E` correctness net** (`.E.0` expansion: runtime-confirm CRITICALs → PERSIST characterization tests + CI invariant checks → per-finding fix-designs in each `.E` plan). Recorded in the `.D.1` decision log.

## Follow-ons

- **TECH_DEBT-142** → `.E.1` rename-approach design (symbol/AST tooling — load-bearing; do NOT reuse the prose-token tool for code).
- **TECH_DEBT-140/141** → `.E.0.1`/`.E.1` (vestigial cfg + backtest unification).
- **TECH_DEBT-143** → `.F` / umbrella close (CHANGELOG backfill; aligns TECH_DEBT-126).
- **Task #21** — per-node CPU poll-mode toggle (busy-poll/pinned vs yield/uncapped; extends D-33; operator-requested) → `.E.1`/`.E.2`.
- **Task #22** — reusable `.E`-ship planning-handoff template + one-context-window-per-plan workflow (operator-requested) → `.E.0` prep.
- **Audit codification** (Classes 37+, `findings_sidecar:` wiring into each `.E.X` plan, CI/test specs, `.E.0` expansion) — was gated on this ship's rename-collision clearing; now unblocked → next.

## Lessons (for `.E.1` especially)

1. **Prose-token rename tools are unsafe for code renames AND for transition-PLANNING docs** (B19). `.E.1` must use AST/symbol-aware tooling.
2. **Rename-planning docs are bilingual-by-necessity** — per-node for the target narrative, per-core for citations / transition-descriptions / current-state. Don't auto-rename them.
3. **Historical-state claims keep their era's terminology** — "pre-`.E` per-core sharded" is correct; renaming to per-node falsifies history (the slip caught + fixed this ship).

## Verification

- doc-rename tool re-run: stable, LEAVE-dominant (148 LEAVE); the RENAME-flagged residual is transition-doc (correctly left).
- Build: `./build.sh test` → **3239/0 expected** (only `Version.hpp` string bump touched engine; result confirmed at ship close).
- No engine C++ touched; hot path **UNTOUCHED**.
- B19 anchored after B18; TECH_DEBT-140..143 appended; SKILL.md Check 44+45 rows present; `E-MASTER § 7` callout landed; CLAUDE_INVARIANTS heading rename created no broken anchor links (only historical changelog prose mentions, correctly preserved per DD-3).
