---
type: handoff
status: active   # the one live handoff (what no-arg /accept-handoff resolves to). Supersedes 2026-06-16-E.1.0-conformance-analyzer-landed-next-finish-tool (its "finish the tool" is DONE).
ship_tag: ".E.1.0 — H8 static latency-path CONFORMANCE ANALYZER: tool FINISHED (full foundation). NEXT = E.1.0 ship-close codifications (conformance DESIGN_SPEC + Class-51 first-canonical + mechanical-verification amendment) → tag E.1.0 → E.1.1 → E.1.2."
plan_type: live-readiness
sprint_end_goal: make the codebase maintainable + live-ready (v5.X professionalization); live gated behind the whole E-series
ship_end_goal: ".E.1.0 stands up the §4d guard NET. H14/H10 ENFORCED (prior sessions). This + the predecessor session built H8 = a STATIC ASM instruction-budget conformance analyzer (deterministic, zero engine behavior). The TOOL is now FINISHED (full foundation: 7-kernel manifest + source-allow/libm exemptions + transitive scan + CI-wired + grandfathered baseline + 5-pass adversarially verified). REMAINING before the E.1.0 tag: the ship-close codifications (the conformance DESIGN_SPEC + the Class-51 first-canonical catalog entry + the mechanical-verification amendment), then tag."
predecessor_handoff: handoffs/2026-06-16-E.1.0-conformance-analyzer-landed-next-finish-tool-handoff.md (SUPERSEDED — its "finish the tool (4 finish-items)" is DONE this session)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (latest D-238; this session added D-237 + D-238)
plan_body: plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.0-guard-hole-closure.md (read § "HARDENED I→A RE-PASS (2026-06-16 s3)" + the RESOLVED banner — the CURRENT H8 shape; the earlier FULL CODING BODY's H8/Phase-D framing is SUPERSEDED-for-H8)
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
dependency_graph: plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, the E.1.0 plan body § "HARDENED I→A RE-PASS (s3)", decision log D-232→238, tools/check_latency_path_conformance.py (the deliverable), DOCS/SUBAGENT_ARMING.md (M8 arming SSoT — NEW), the .E dependency-graph DAG, regen CODE_MAP at pickup]
engine_head: 226a652 (the RollingStats size-pins — `fix(.E.1.0)`; from a PRIOR session; NOT pushed; working tree CLEAN). This + the predecessor session added NO engine commits — the conformance analyzer + agents + arming-doc all live in tools/ + .claude/ + DOCS/-symlinks (gitignored-in-place, workspace-backed). Predecessor baseline = .E.0.10 tag 572c978.
workspace_head: at-or-after the /sync-workspace at this session's close (this session: 7 modified + 3 untracked workspace files — see "What landed this session")
coding_status: mid-coding-checkpoint — the H8 conformance-analyzer is FINISHED (apparatus, in tools/); REMAINING before E.1.0 ships = the 3 ship-close CODIFICATIONS (docs, not code) → then the E.1.0 tag
---

# Handoff — `.E.1.0` conformance-analyzer FINISHED → next = ship-close codifications → tag

## TL;DR (the 60-second pickup)

- **The tool is DONE.** `tools/check_latency_path_conformance.py` went from "first cut" (predecessor session) to the **full structural foundation** this session (operator: *"id rather do more work now creating a good foundation than anything else"* / *"foundation and structural fixes are always going to be preferred"*). It now **exits 0 with full hot + slow + ML-kernel coverage**, gates a **7-row KERNEL manifest** from PRODUCTION ASM, and carries the complete exemption model. Decisions: **D-237** (B scope-expansion: source-keyed exemptions + full coverage; supersedes D-236's meter-only framing for the float/div-exemption axis) + **D-238** (the s4 implementation decisions: kernel-granularity / source-allow+FEATURE_MATH_EXTERN / XGBoost homed-gap / FINDING-A cold-sanctioned / CI-wired+baselined / 5 verification passes).
- **5 independent adversarial passes found the load-bearing holes the builder's dogfood + teeth could NOT** (AR-8, the model-bounded-self-check floor — exactly why independent verification mattered): the **decorative div-gate** (div counted/teethed but `main` never GATED it), the **cold-quartile false-green** (the positional heuristic skipped a delegating call), the **forbidden-file-level hole** (file-level `allow` was exempting malloc/stdio, not just float/div). Each was fixed STRUCTURALLY, not logged.
- **It's WIRED + baselined.** Pre-commit Check N (trigger-scoped, `SKIP_LATENCY_CONFORMANCE_CHECK` bypass) + `run_all_tests.sh` (gate + `--selftest`, both HARD); grandfathered baseline at `tools/lib/latency_path_budgets.json` (7 rows). **15 `--selftest` teeth all fire.**
- **NEXT = the 3 E.1.0 ship-close CODIFICATIONS (docs, not code), then tag:** (#6) the conformance **DESIGN_SPEC** (the tool-design lessons: kernel-granularity / orchestrator-over-inline / source-allow / libm-exemption / forbidden-file:line / the 5-pass AR-8 story) + the **mechanical-verification amendment** (D-233: reclassify LATENCY/COMPLEXITY rows runtime→compile-time tier in `mechanical-verification-of-derived-code-facts.md`); (#13) the **Class-51 "vacuously-green guard" first-canonical** catalog entry (closes the *dangling forward-reference* — `mechanical-verification-of-derived-code-facts.md`'s `sister_docs` already points at "Class 51" which does NOT exist yet). Then **tag E.1.0 → rename E.1.1 → E.1.2** (E.1.2 = the relayout, first real consumer of the tool).

## The ideal way to start

1. **`/accept-handoff`** (resolves here). Regen CODE_MAP; load baseline + nav-infra + the decision log D-232→238 + `DOCS/SUBAGENT_ARMING.md` (NEW this session) + the plan body § "HARDENED I→A RE-PASS (s3)".
2. **RUN the tool to see it green:** `cd /home/caramel/code/FoxML_Trader_v2 && python3 tools/check_latency_path_conformance.py 2>&1 | tail -30` — exit 0, 7 kernels analyzed. Then `python3 tools/check_latency_path_conformance.py --selftest` — 15 teeth all fire.
3. **Read the CURRENT shape:** D-237 + D-238 in the decision log (the s4 full-foundation decisions) + the tool's MANIFEST + NOTE comments (the 7 rows + why RebuildOneCore/Model_Predict*/OnlineCycleStep are NOT manifested).
4. **Do the 3 codifications** (the next-action arming below), then tag E.1.0.

## What landed this session (s4 — the full foundation)

**Workspace changes (7 modified + 3 untracked; all uncommitted until this close's /sync-workspace):**

| File | Change |
|---|---|
| `tools/check_latency_path_conformance.py` (M) | THE deliverable, finished: `_target_body_symbol` (length-prefixed mangled-name body finder incl. `.isra`/`.constprop` clones, largest non-vacuous) replaced the reloc-only `_callee_symbol`; `analyze_path` transitive HARD-invariant recursion (MAX_DEPTH 5, STRUCTURAL not positional — the cold-quartile fix) + fail-loud `unanalyzed_warm`; `_hard_findings` single-source gate (teeth assert the GATE not the count) incl. the **div-gate fix** + **forbidden-file:line-only** filter; `FEATURE_MATH_EXTERN` (libm) + `BENIGN_EXTERN` (+getenv/secure_getenv); 7-row MANIFEST (+3 ML kernels) + NOT-manifested NOTE comments; 15 `--selftest` teeth. |
| `tools/lib/latency_path_budgets.json` (??) | grandfathered baseline (7 rows: instructions + data_dependent). Set via `--update-budgets`. |
| `tools/run_all_tests.sh` (M) | +2 lines (the gate + `--selftest`, both HARD) after the determinism line. |
| `.githooks/pre-commit` (M) | Check N (trigger-scoped grep; `SKIP_LATENCY_CONFORMANCE_CHECK` bypass; re-baseline hint). **NOT auto-mirrored by /sync-workspace — manually copied to workspace this session; TD-204 tracks the structural fix.** |
| `DOCS/SUBAGENT_ARMING.md` (??) | M8 arming SSoT (nav-infra + tools + dedicated-skill methodology + the i/a/v/d/c registry). Was engine-only (would have been lost) → now a workspace-real file + engine symlink. |
| `claude-agents/{i,a,v,d,c}-class.md` (??) | the 5 role agent definitions (frontmatter + body → SUBAGENT_ARMING.md) + `.claude/agents` symlink. **Live NEXT session** (harness loads agents at startup). |
| `decision-logs/…E-architecture-v2.md` (M) | +D-237 (B scope-expansion) +D-238 (s4 implementation decisions); D-237 STATUS REFINED ("7 KERNELS not 4-incl-orchestrator"). |
| `DOCS/tech-debt/open.md` (M) | +TD-205 (FINDING A: H3 pthread cold/sanctioned) +TD-206 (XGBoost coverage gap) +TD-207 (M7: independent-review-by-default for mid-session tool builds + close exhaustive-capture). |
| `DESIGN_SPECS/meta-disciplines/meta-anti-pattern-index.md` (M) | AR-8 6th-instance bump (the 5 adversarial passes + the 3rd-consecutive harvest-timing miss → M7 escalated to TD-207). |
| `DOCS/TOOLS.md` (M) | line-54 currency fix (the row said "RED-BY-DESIGN (exit 1)" with the now-DONE finish-items as open — the one un-tracked drift the Stage-5.5 review caught). |

## TaskList state at handoff write (preserve verbatim for fresh-session pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | completed | Finish + ship the H8 static latency-path conformance analyzer (hot+slow) |
| #2 | completed | H7/H20 branchless gate (subsumed by the branch-classifier) |
| #3 | pending | grep-CI aggregator at ≥2 gates (moot/note — see below) |
| #4 | completed | Baseline capture → HOT_PATH_CHANGELOG (grandfathered baseline JSON) |
| #5 | completed | V-class verify (binaries + suite green + calls_graph + determinism) |
| #6 | **pending** | **Ship-close codifications: conformance DESIGN_SPEC + mechanical-verification amendment (D-233)** |
| #7 | pending | TECH_DEBT-204 — .githooks not mirrored by /sync-workspace (structural symlink-mirror) |
| #8 | completed | Cascade-check H8 across E.1.1/E.1.2 |
| #9 | completed | Slow-path optimization homing |
| #10 | completed | Size+cache guard tool (check_struct_size_budget.py) |
| #11 | pending | Umbrella sweep finish (stale size comments + preventive pins + wire size tool) |
| #12 | in_progress | Subagent registry + custom agent types (agents authored; LIVE-verify next session + discoverability pointer + DESIGN_SPEC) |
| #13 | **pending** | **Codify Class-51 "vacuously-green guard" (first canonical) + cascade-bridge (closes the dangling forward-ref)** |
| #14 | completed | RE-RUN the I→A sweep with hardened tools/agents |
| #15 | pending | [HIGH] trade-branch → branchless (H20) — optimization leaf (D-236) |
| #16 | completed | Harden close-session capture process |
| #17 | pending → DISPOSITIONED | [ENGINE — FINDING A] H3 pthread reachability — verified cold/sanctioned, homed TD-205 |
| #18 | completed | [ENGINE — FINDING B2] Slow-rebuild file-I/O + system() (triaged cold/sanctioned) |

**Recreate this TaskList** via TaskCreate at pickup. The live next-action set is **#6 + #13** (the E.1.0 ship-close codifications), then the tag. #3/#7/#11/#12/#15 are post-tag / separate-deliverable.

## Per-Step status — what's NEXT

| Item | Status | Notes |
|---|---|---|
| The conformance tool (full foundation) | **DONE** | exit 0, 7 kernels, exemptions, CI-wired, baselined, 5-pass verified |
| #6 conformance DESIGN_SPEC | **NEXT** | author `DESIGN_SPECS/<...>/static-latency-path-conformance-analyzer.md` (Stage 2-draft → 3-first-canonical); the tool-design lessons |
| #6 mechanical-verification amendment | **NEXT** | reclassify LATENCY/COMPLEXITY runtime→compile-time in `mechanical-verification-of-derived-code-facts.md` (D-233) |
| #13 Class-51 first-canonical | **NEXT** | `DOCS/recurring-bug-patterns/class-51-vacuously-green-guard.md` (highest existing is class-50) — closes the dangling `sister_docs` forward-ref |
| tag E.1.0 | after #6 + #13 | bump Version.hpp `.E.0.10` → `.E.1.0` in the tag commit; `/ship` |
| E.1.1 (rename) → E.1.2 (relayout) | post-tag | E.1.2 = first real consumer of the tool |

## Coding-time discoveries from this session (assume they shape what you'll hit next)

- **D-238 / kernel-granularity:** orchestrators over-inline in single-TU probes (`EventLoop_RebuildOneCore` pulls the whole pipeline incl. cold persistence → ~190 signals / flood). GATE THE LEAF KERNELS, not the orchestrator. The 7-row manifest is the leaves; RebuildOneCore/Model_Predict*/OnlineCycleStep carry in-tool NOTE comments explaining why they're NOT rows.
- **D-237 / source-keyed exemptions:** the H4-sanctioned feature seams (libm transcendentals, money↔feature conversions) are exempted by a per-row source-keyed `allow` (file-level for pure-feature files; `file:line` for the mixed `FixedPointN.hpp`) + `FEATURE_MATH_EXTERN` (name-whitelist). **Forbidden-calls (malloc/lock/stdio/throw) exempt by `file:line`-ONLY** — a file-level allow must NOT wave through a malloc. (This was a latent hole the final verify caught + I fixed structurally.)
- **The 5 adversarial passes (AR-8):** my dogfood + the growing teeth set declared "clean" at every round; the INDEPENDENT passes found the div-gate / cold-quartile / forbidden-file-level holes. The lesson is codified — **TD-207** (M7 escalation: independent-review-by-default for mid-session tool builds; the harvest/capture self-skip recurred a 3rd consecutive session). Arm the v/a-class agents (NEW this session) on any future tool-build.
- **FINDING A (H3 pthread):** cold/sanctioned (kill-trip edge, KILL_TRIPPED set before the Notify_Send, sanctioned per CLAUDE_INVARIANTS.md:131). Homed TD-205. Not a bug.

## TECH_DEBT scoreboard (in-flight status at handoff write)

**NEW this session:**
| TECH_DEBT | Title | Status |
|---|---|---|
| TD-205 | FINDING A — H3 pthread via Notify_Send cold/sanctioned (kill-trip edge) | open (dispositioned; not a fix) |
| TD-206 | XGBoost coverage gap (Model_Predict* = 39-instr external-API stub; not analyzable) | open |
| TD-207 | M7 — independent-review-by-default for mid-session tool/code builds + close exhaustive-capture | open |

**Carried (surface overlap):**
| TECH_DEBT | Title | Decision |
|---|---|---|
| TD-204 | .githooks not mirrored by /sync-workspace | open; manual-mirror this session; structural fix = task #7 |

## Pre-pickup self-audit (4-pillar discipline)

| Pillar | Verdict | Notes |
|---|---|---|
| 1. DESIGN_SPECS cross-check | `mechanical-verification-of-derived-code-facts.md` (the amendment target) · `meta-registry`/`x-macro` (the manifest pattern) · the NEW conformance spec (#6) | the codifications ARE the DESIGN_SPECS work |
| 2. Anti-pattern catalog | CLEAN — Class-51 (the tool codifies AGAINST it + asserts its own non-vacuity); the #13 catalog entry IS the codification | no anti-pattern introduced |
| 3. Operator-impact | NONE — the tool is private apparatus (gitignored-in-place, workspace-backed); the bypass env-var is documented; no engine behavior change | the E.1.0 tag will bump Version.hpp (operator-visible) |
| 4. Novel alternative | the static-ASM-conformance axis WAS the novel alternative (D-233/234 — superseded the runtime-bench); accepted | meter = instruction-budget, not wall-clock-ns |

## Arming for the next action

For **the 3 E.1.0 ship-close codifications (#6 + #13)** — the surface is doc-codification (DESIGN_SPECS + the bug-class catalog), so arm it:
- **Route to:** `/doc-create` (the conformance DESIGN_SPEC + the Class-51 entry scaffolding) + the `pattern-codification-lifecycle.md` discipline (Stage 2-draft → 3-first-canonical); `/capture-audit` (verify the new docs land indexed + the dangling forward-ref closes). The codifications are NOT a capital/code surface — no `/accounting-audit` / `/hft-audit` needed. If you want an independent read of the authored specs before tag, route a **v-class** agent (NEW this session) at them (per TD-207 — the mid-session-build review default).
- **Run (not just read):** `./tools/check_session_docs.sh` (the doc-CI sweep — confirms the new specs are indexed + bidirectional + the forward-ref resolves) · `python3 tools/check_latency_path_conformance.py && … --selftest` (confirm still green pre-tag) · `./build.sh test` + `run_all_tests.sh` (pre-tag green) · regen `./tools/gen_code_map.sh`.
- **Read first:** the tool itself (`tools/check_latency_path_conformance.py` — the spec documents IT) · `mechanical-verification-of-derived-code-facts.md` (the amendment target + its dangling `sister_docs: Class 51`) · `DOCS/recurring-bug-patterns/class-50-*.md` (the format sibling for the Class-51 entry) · D-237/D-238 (the source decisions).
- **M8 scout-first:** if you spawn the v-class reviewer, arm it with SUBAGENT_ARMING.md (the standing half) + the spec paths + the tool (the subject) + scout-before-execute; keep it independent on the verdict.

## Capture-completeness (this session — nothing lost)

This was a `/close-session` in **exhaustive-capture mode** (operator: *"make sure we being thorough about what is being captured, not TOP N, i wanna cap every thing"*). Every dimension addressed:

- **Decisions → log:** **D-237** (B scope-expansion) + **D-238** (s4 implementation decisions) both written to the decision log with paired `<!-- D/C/F -->` + `<!-- STATUS -->` sentinels; D-237 STATUS refined for the 7-kernel correction. (Verified by the Stage-5.5 review — item #7 VERIFIED.) No un-logged operator decision this session.
- **Findings → ledger:** **TD-205** (FINDING A H3), **TD-206** (XGBoost gap), **TD-207** (M7 independent-review-default) all homed in `DOCS/tech-debt/open.md`. FINDING B2 + OnlineCycleStep homed in the tool's in-manifest NOTE comments. The TOOLS.md:54 drift FIXED (not logged) per Stage 4.6.
- **Memories/skills → indexed:** no NEW memory file this session (the AR-8 + capture shapes were BUMPS to the existing `meta-anti-pattern-index` AR-8 row + TD-207, not new memories — the catalog is the right home). The 5 NEW agent files (`claude-agents/{i,a,v,d,c}-class.md`) + `DOCS/SUBAGENT_ARMING.md` are the task-#12 registry (live next session; discoverability pointer + DESIGN_SPEC pending = #12 remainder).
- **Verified:** `check_session_docs.sh` SWEEP CLEAN (all HARD); the Stage-5.5 independent review VERIFIED items 1-9 + flagged the TOOLS.md drift (FIXED) + confirmed items 1-3 (the codifications) are homed-as-pending (this handoff #6/#13) + the privacy boundary CLEAN.
- **Still-pending (homed, NOT lost):** the 3 ship-close codifications (#6 conformance DESIGN_SPEC + mechanical-verification amendment; #13 Class-51 first-canonical) — gated "before the E.1.0 tag" per D-237/D-238 STATUS; this handoff is their home. The dangling `Class 51` forward-ref in `mechanical-verification-of-derived-code-facts.md` `sister_docs` is EXPECTED (the spec was authored ahead of the catalog entry; #13 closes it).

---

## Paste this prompt into a fresh Claude Code session to start the E.1.0 ship-close

```
I'm picking up .E.1.0 ship-close (the H8 conformance analyzer is FINISHED; remaining = 3 codifications → tag) for the v5.15-live-readiness sprint.

Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-16-E.1.0-conformance-analyzer-FINISHED-next-shipclose-codifications-handoff.md

This is a fresh context window; do NOT trust prior-session memory — verify against current code. The tool is DONE + wired + baselined + 5-pass-verified. Your next action is the 3 ship-close CODIFICATIONS (docs, not code), then tag E.1.0:

  #6  the conformance DESIGN_SPEC (the tool-design lessons: kernel-granularity / orchestrator-over-inline / source-allow / libm-exemption / forbidden-file:line / the 5-pass AR-8 story)
  #6  the mechanical-verification amendment (D-233: reclassify LATENCY/COMPLEXITY rows runtime→compile-time in mechanical-verification-of-derived-code-facts.md)
  #13 the Class-51 "vacuously-green guard" first-canonical catalog entry (closes the dangling sister_docs forward-ref)

THEN bump Version.hpp .E.0.10 → .E.1.0 in the tag commit + /ship. Then E.1.1 (rename) → E.1.2 (relayout, first real consumer).

Arm per the "Arming for the next action" section: /doc-create + pattern-codification-lifecycle.md; run check_session_docs.sh + the conformance tool (confirm green) + build.sh test pre-tag. Consult Caramel before tagging.
```

---

## Quick links

- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- This ship's plan: `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.0-guard-hole-closure.md`
- Decision log: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-237 + D-238)
- The deliverable: `tools/check_latency_path_conformance.py` + `tools/lib/latency_path_budgets.json`
- M8 arming SSoT: `DOCS/SUBAGENT_ARMING.md` (NEW) + `claude-agents/{i,a,v,d,c}-class.md`
- DESIGN_SPECS catalog: `tick-trader-percore-workspace/DESIGN_SPECS/README.md`
