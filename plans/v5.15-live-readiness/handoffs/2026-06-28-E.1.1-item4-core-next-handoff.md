---
type: handoff
status: active   # active = THE one live handoff (no-arg /accept-handoff resolves here). Supersedes 2026-06-24-E.1.1-config-compiler-arc-handoff.md.
ship_tag: E.1.1-item4-core-next
plan_type: live-readiness
sprint_end_goal: v5.15 live-readiness — make the codebase live-ready + more maintainable
ship_end_goal: "③ config-compiler item-4 — the per-field CAPITAL value-range validator. The founding bug (risk_pct=999 / max_drawdown=999 silently disabling capital controls) + the legacy-array dual-source hazard. B's merge (step 1) LANDED; the CORE validator (steps 2b/3) is NEXT."
predecessor_handoff: 2026-06-24-E.1.1-config-compiler-arc-handoff.md
engine_head: d8fe6c6
workspace_head: "at or after 1725742 (this close advances it via /sync-workspace)"
branch: feat/v5.15-live-readiness
deletion_scope: none
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, "plans/v5.15-live-readiness/plan_checks/2026-06-26-E.1.1-item4-caps-3i3a-synthesis.md", "plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (D-265..D-276)", "plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md", "plans/v5.15-live-readiness/MASTER.md", "DESIGN_SPECS/framework-patterns/config-compiler-validation-pattern.md", "CoreFrameworks/CfgFieldRegistry.hpp", "CoreFrameworks/ControllerConfig.hpp"]
coding_status: "③ config-compiler CODE-COMPLETE (items 1-6 + C1/C2 LANDED + pushed; engine d8fe6c6) + D-271 codification slate DONE (see the ⏩ Addendum); NEXT = vocab sweep / boot-surface / E.1.2 — the item-4 body below is HISTORICAL"
---

> [!IMPORTANT]
> ## ⏩ Addendum — 2026-06-29 (post-write; SUPERSEDES the item-4 body below)
>
> **The ③ config-compiler arc is CODE-COMPLETE + fully codified.** Everything below (item-4 step-1 landed, the CORE validator "next") is HISTORICAL. Since this handoff was written:
>
> - **item-4 CORE validator LANDED** (engine `6981c85`) — the per-node out-of-range sweep `ControllerConfig_CapitalRangeSweep<F>` (if-constexpr walk over `FOREACH_PER_NODE_CFG_FIELD`) + the dollar floors + the **global-flat F1 leg** (a 4-agent sweep caught the global-inherit hole: an inheriting node's `nodes[c]==0` made the `nodes[c]`-only sweep pass while the allocator used the un-swept global) + the **F2 optimizer range-endpoint gate**. 10 char-tests.
> - **item-5 (live-reload gate)** — `Async.hpp` re-validates the merged reload (warn-keep-old); **C1/C2 (feature-malformed tier)** — ~47 FPN/float fields → `parse_double_fast_checked` → `CFG_FAULT_FEATURE_MALFORMED` (byte-identical for valid → no determinism shift); **item-6 (recurrence guard)** — a `static_assert` (no `CAPITAL_BOUND` row is `MANUAL_PARSER`) + the CI tool `check_cfg_gate_caller_coverage.py` (`--selftest`-proven, wired into `run_all_tests.sh`). Engine `d8fe6c6`, workspace `1725742`. **controller_test 3693/0 + FRESH asan/ubsan 3693/0 + determinism GREEN + caller-coverage PASS + doc-CI CLEAN.**
> - **D-271 codification slate DONE** — D-279/D-280 (decision log); RBP **Class 54** (validation-asymmetry-across-write-paths) + **Class 55** (dual-source-storage-split-readers); the config-compiler **FEATURE_LOOKUP** entry; **TD-212 PARTIAL-CLOSED** (capital subset done; non-capital ~36 fields → E.2); **Landmine 5 recurrence** note; the `config-compiler-validation-pattern.md` out-of-range half + the **D-278 deletable-cascade** memory + the retirement-spec complement (landed at item-4 close).
>
> **NEXT ACTION (operator picks at pickup):** ③ is CLOSED + the **vocab sweep #13 ✅ DONE** (`baf5e5a` — ~190 operator core→node strings + the `main.cpp` boot-refused-key correctness fix; D-281). Remaining E.1.1 ship-close = the boot-surface **GUI warn-banner** (#11 — the global-unknown-key refuse half is BLOCKED on the multi-parser unification per TD-223; only the banner is doable) · **OQ1** WIRE the inert sharded kill/exposure controls (#14 — decided WIRE per D-273; a real capital-control ship → its own session + pre-coding gate). Then **E.1.2** (the NodeState SoA / `Money` relayout leaf, #6) — owns the legacy-array DELETION the B-merge teed up + the GAP-2 strategy-state OOB fix (#15). **Engine HEAD `baf5e5a`; workspace HEAD `890ac09`.** Re-run `/accept-handoff` to re-baseline.

# E.1.1 ③ item-4 — handoff (B's merge landed; the CORE capital validator is next)

**Created:** 2026-06-28
**Target:** item-4 — per-field capital value-range validator (the founding-bug closure)
**Branch:** feat/v5.15-live-readiness
**Pre-tag rollback anchor:** `pre-v5.15.5.F.4d.1.E.1.1`
**Plan file:** `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md`
**Item-4 cascade SSoT:** `plans/v5.15-live-readiness/plan_checks/2026-06-26-E.1.1-item4-caps-3i3a-synthesis.md`

**Engine state at handoff write:**
- HEAD: `9c93d1d` (B's merge; 1 ahead of origin → pushed at this close)
- Latest tag: `pre-v5.15.5.F.4d.1.E.1.1`
- Version.hpp: `5.15.5.F.4d.1.E.1.0`
- Test baseline: `3680/0` (`./build/controller_test`) + `1066/0` (parity); H15 + per-node-registry integrity PASS
- Working tree: clean (B's merge committed)

**Workspace state:** HEAD `dc02c30` + this-close sync (D-272/D-275 cross-links, E.1.6 widening fold, TECH_DEBT-224, the SSoT status update, MEMORY.md trim, this handoff).

**Verify-on-write:** independent close-out review (close-session Stage 5.5) returned **CLEAN-TO-HANDOFF** — all 6 deliverables VERIFIED, cross-coherence ALL-AGREE, no fabricated anchors, B-merge byte-identical with last-wins ordering confirmed. Re-run `/accept-handoff` at pickup to re-baseline.

---

## What landed this session — item-4 step 1 (B's merge)

The legacy per-node capital arrays `node_risk_pct[16]` / `node_max_drawdown_pct[16]` now **merge into the resolved `nodes[c]` struct** so there is ONE authoritative per-node read (kills the dual-source hazard where `ResolveForCore` never merged the arrays → `nodes[c].risk_pct` was a dead mirror while the live allocator/kill-switch read the arrays directly).

- NEW `FOREACH_PER_NODE_ARRAY_OVERRIDE(X)` sidecar (`CfgFieldRegistry.hpp` ~:941) — 2 rows: `X(risk_pct, node_risk_pct)`, `X(max_drawdown_pct, node_max_drawdown_pct)` + `EMIT_PER_NODE_ARRAY_OVERRIDE` (raw-copy `nodes[c].T = source[c]`, preserves 0=inherit).
- Applied in `ControllerConfig_PopulateCoresFromFlat` (`ControllerConfig.hpp:1543`) **strictly AFTER** the `EMIT_PER_NODE_COPY` walker (last-wins).
- H15 enrollment row in `MetaRegistry.hpp:44` (Level 1, parent `FOREACH_REGISTRY` → H19 OK).
- 4 consumers flipped to `nodes[c]` — **byte-identical** (same two-branch divided-vs-direct math, only the read source changed): `EngineSharded/Run.hpp:923`, `EngineSharded/Async.hpp:354`, `Backtest/BacktestSharded.hpp:273`, `ControllerEventLoop.hpp:2918`.
- **SUBSUMES the legacy-array CRITICAL** — a `nodes[c]`-only sweep is now correct. (Array DELETION + size-pin bump = E.1.2, the layout-change leaf.)

Committed: engine `9c93d1d` (7 files, +61/-8) + workspace `dc02c30`. Full pre-coding cascade (3-I→3-A + 3 deeper i-class + 3 adversarial + accounting-gate, GREEN-to-build) in the SSoT; decisions D-265..D-275.

---

## TaskList state at handoff write (recreate verbatim at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | in_progress | E.1.1 ship — Core→Node rename + cfg clean-break + single_core deletion |
| #2 | in_progress | ③ config-compiler — item-4 per-field capital caps (**THE NEXT ACTION**) |
| #3 | pending | ⑤ V-class — asan/ubsan + determinism re-cert + N1 integration test |
| #4 | pending | E.1.1 ship-close — doc sweep + FEATURE_LOOKUP + cores-prose + TD-140 |
| #5 | pending | E.1.2 — NodeState SoA / Money relayout (successor leaf) |
| #6 | in_progress | All-strategies-private restructure + capture (D-251) |
| #7 | pending | ③ codification — rename-ship-methodology amendment (residual) |
| #8 | pending | core_→node_ migration residue + cfg-key guard pre-commit scoping |
| #9 | completed | (folded into #7) Class 52 + config-compiler spec — LANDED |
| #10 | pending | Boot-surface — GUI warn-banner + global-unknown-key refuse |
| #11 | pending | C1/C2 feature-determinism tier — refuse-vs-warn final call |
| #12 | pending | Vocab drift — ~60-85 operator-string sweep + classifier extension |
| #13 | pending | OQ1 — wire-or-tombstone the inert sharded kill/exposure controls |
| #14 | pending | GAP-2 — strategy-state type-confusion OOB on GUI swap → E.1.3+ seam |

---

## NEXT ACTION — item-4 CORE (the founding-bug closure; steps 2b/3, NO widening)

Uses the EXISTING `CAPITAL_BOUND_LOSS/GAIN` metadata bits — **no metadata widening** (that defers to E.1.6; see CRITICAL CONTEXT below). Build, compile-gated:

1. **The Money-native predicate** `capital_value_out_of_range(Money v, Money cap)` importing `tt::BARRIER_SANE_MAX_SL=1.0` / `BARRIER_SANE_MAX_TP=10.0` (`ML_Headers/BarrierValidation.hpp`). DECOUPLE from `tt::barrier_is_corrupt` (D-266 — the dummy-slot reuse is a cascade landmine; share the CONSTANTS, not the function).
2. **The post-resolve sweep** over the 15 `CAPITAL_BOUND_LOSS/GAIN` per-node fields via the EXISTING `g_per_node_cfg_capital_bound_loss/gain` masks, reading the B-merged `nodes[c]` → set `CFG_FAULT_CAPITAL_OUT_OF_RANGE`. MUST be POST-resolve (0=inherit at capture-time; the real value isn't known until the inherit-chain collapses — D-265).
3. **The 2 dollar floors EXPLICIT** (no bit): `starting_balance > 0` (strict — `=0` silently disarms both kills + is the return/exposure denominator, D-269), `min_kill_loss ≥ 0` (reject NEGATIVE only; `0` = fail-safe "fire on pct alone"), + the `min_kill_loss < starting_balance` cross-field. Stay Money-space (`Money_IsZero`/`Money_Lt`).
4. **Wire as a STANDALONE predicate invoked BY `cfg_capital_gate_ok`** — so it ALSO covers the backtest optimizer's `config_override` path that skips `ControllerConfig_Load` (the GAP-1 caller-coverage class).
5. **Tests + determinism re-cert** (V-class at close: `./build.sh test` + asan/ubsan + `check_per_node_registry_integrity.py`).

Pre-code reading: `config-compiler-validation-pattern.md`. The `/accounting-audit` gate ALREADY RAN (CLEAR — fee venue-sourcing resolved; see the SSoT accounting-gate section), so it need not re-fire unless scope shifts.

---

## ⚠️ CRITICAL CONTEXT — do NOT widen metadata_flags in item-4

The `metadata_flags` bitmap is at **0 headroom** (`uint16`, bits 14/15 = `CAPITAL_BOUND_LOSS/GAIN`). Adding any new `CAPITAL_BOUND_*` bit needs the `uint16→uint32` widening — which is the metadata-bit **FRAMEWORK expansion** (enum underlying type + `cfg_compute_mask` template param + `ALL_METADATA_BITS_IN_USE`/`EXEMPT` reductions [Class-51 vacuity trap if not widened with the enum] + the composed-mask audit forcing an include/exclude decision per new bit per composed mask). That DEFERS to E.1.6, designed ONCE for its full consumer set (ENUM_STRICT + DOLLAR + MULT) — D-275, TECH_DEBT-224, the E.1.6 plan D-255/item-4-B fold. **item-4's dollar floors are EXPLICIT (no bit); the `*_mult` cap is NOT in item-4** (it needs `CAPITAL_BOUND_MULT` → E.1.6).

---

## Arming for the next action (M8 session-arm)

Surface = **per-node capital cfg validation** (slow-path/boot; H22 per-node purity + H4 Money math). Arm it:
- **Route to:** `/accounting-audit` (cap thresholds — already ran CLEAR, re-fire only if scope shifts) + `/trace-deps` (the validator's call-sequence into `cfg_capital_gate_ok` + the `config_override` caller path) + `/dod-audit` (registry/mask-driven pattern application). A design DECISION mid-build → `/decision-check` routing its agents to those.
- **Run (not just read):** `tools/check_per_node_registry_integrity.py` (H17 + Class 25/26 paired-access) · `./build.sh test` · V-class (`run_all_tests.sh --full` asan/ubsan) at close · `tools/gen_code_map.sh` (regen) · `calls_graph_diff.sh` (confirm the hot path stayed UNTOUCHED).
- **Read first:** `config-compiler-validation-pattern.md` + `universal-cfg-field-registry-pattern.md` + `cfg-scope-discipline.md`; H22 (per-node purity) / H4 (Money) / D2; anti-pattern Classes **52** (swallow-and-coerce capital parse) / **48-49** / **33** (enumerate-the-set) / **25/26** (per-core scope + slot index).

---

## Open items homed (nothing dangling)

- **OQ1 / D-273 → task #13, TECH_DEBT-220:** `max_exposure_pct` + `kill_switch_daily_loss_pct` are DEAD on the production sharded path (read only in the call-site-less `PortfolioController_Tick`). Decision = WIRE them (a sharded session-relative daily-loss kill + a global exposure cap + arm/decide the balance-floor kill) as their OWN live-readiness ship. item-4 validates 543/544 as cfg but must NOT present them as "armed."
- **GAP-2 / D-274 → task #14, E.1.2:** GUI strategy-swap type-confusion OOB read (704B `MomentumState` ← 32B `SimpleDipState` slot, deref +416). COMPLETE structural fix (pre-sized per-node strategy-state union + a `strategy_state_kind` guard on the build dispatcher), scoped as ONE fix at E.1.2 (the NodeState relayout leaf). Read-only + GUI-only + gated meanwhile.
- **Deferred capital FAULT-set → E.1.6 widening (D-276, TECH_DEBT-224):** the `*_mult` cap + **FEE** (`fee_rate_maker/taker`; cap = VENUE-SOURCED, defer-to-source-authority) + **partial_exit** (verify-at-dive: LOSS/GAIN-taggable → item-4 core, else new-bit → E.1.6) all need new `CAPITAL_BOUND_*` bits → the `uint16→uint32` widening at E.1.6, NOT item-4. slippage separately homed (D-269 `/accounting-audit` question). [D-275 originally carried only MULT; D-276 completed the set — operator capture-probe catch.]
- **③ ship-close codification slate (D-271):** complete `config-compiler-validation-pattern.md` into its single-path form (item-4 = 2nd canonical → Stage-4 cohort) + author 2 anti-pattern classes (VALIDATION-ASYMMETRY-across-write-paths · DUAL-SOURCE-STORAGE-split-readers). Owed at ③ ship-close, NOT at item-4 build.
- Remaining ③: item-3 dollar floors FOLD into item-4's core (this next action); item-5/6, C1/C2 feature tier, vocab sweep still pending. ⑤ V-class + ③ ship-close pending.

---

## Capture-completeness (this session — nothing lost)

- **Decisions → log:** D-265..D-275 ALL carry paired `<!-- D/C/F -->` + `<!-- STATUS -->` sentinels in `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (independent-review VERIFIED; no stray-char residue). D-275 = the widening scope-correction + the core-now/widening→E.1.6 resolution (SUPERSEDES D-272 for MULT/DOLLAR; back-pointer added to D-272).
- **Findings → ledger:** TECH_DEBT-224 (the `*_mult` cap deferred to E.1.6) opened in `DOCS/tech-debt/open.md`. OQ1→TD-220 (existing). GAP-2 homed to E.1.2 (D-274). The E.1.6 plan widening fold extended with `CAPITAL_BOUND_MULT` as the 3rd consumer.
- **Memories/skills → indexed:** NEW memory `feedback_complete_structural_fix_not_incremental_prelive.md` indexed in `MEMORY.md` (trimmed under the 24,400 B cap this close). No new skills.
- **Verified:** `check_session_docs.sh` SWEEP CLEAN (after the MEMORY.md cap fix) + independent close-out review CLEAN-TO-HANDOFF. Meta-harvest: one caught-not-shipped recurrence (Class-33/enumerate-first), already catalogued, no new enforcement.
- **Capture-probe catch (2026-06-28):** operator probe ("are we missing capturing anything?") found D-275's deferral under-enumerated D-272's FAULT set — it carried only MULT; **FEE + partial_exit were un-homed**. FIXED before close: **D-276** (completes the deferred set + preserves the fee venue-sourced-cap constraint) + TD-224 sibling note + the E.1.6 fold consumer-set correction (ENUM_STRICT + DOLLAR + MULT + FEE + partial_exit). Itself an AR-1/enumerate-the-set instance (the deferral named a representative member, not the set) — the third this session, all caught.

---

## Paste this prompt into a fresh session to pick up item-4

```
I'm picking up E.1.1 ③ item-4 (the per-field capital value-range validator) for the v5.15-live-readiness sprint.

Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-28-E.1.1-item4-core-next-handoff.md

That loads the baseline + the item-4 SSoT + the decision log (D-265..D-275) + CfgFieldRegistry/ControllerConfig + the nav-infra, verifies git state, recreates the TaskList, and reports the concrete next action.

My next action is item-4's CORE (the founding-bug closure; steps 2b/3, NO metadata widening): the Money-native capital_value_out_of_range helper + a post-resolve sweep over the 15 CAPITAL_BOUND_LOSS/GAIN fields (existing masks, reading B-merged nodes[c]) + the 2 explicit dollar floors + min_kill_loss<starting_balance + wire it through cfg_capital_gate_ok + tests. Do NOT add CAPITAL_BOUND_MULT/DOLLAR bits (the widening defers to E.1.6 per D-275). Consult Caramel on findings before coding.
```

---

## Operator collaboration norms (carry forward)
- Address Caramel as Caramel / she / her. No AskUserQuestion modal boxes — inline text.
- Evaluate options on robustness + latency + design philosophy, NOT time.
- Consult before coding — present findings + iterate; never auto-proceed.
- Commit/push only when she asks. Micro-commits, compile-gated.

## Quick links
- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- Item-4 SSoT: `plans/v5.15-live-readiness/plan_checks/2026-06-26-E.1.1-item4-caps-3i3a-synthesis.md`
- Decision log: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`
- E.1.6 widening home: `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.6-multi-exchange.md` (D-255/item-4-B fold)
- Pattern: `DESIGN_SPECS/framework-patterns/config-compiler-validation-pattern.md`
