# Pre-coding audit gate synthesis — v5.15.5.F.4c.2 (bandit 5-state ghost-training)

**Date:** 2026-05-15
**Engine HEAD:** `7538ace` = tag `v5.15.5.F.4c.3`
**Plan audited:** `plans/v5.15-live-readiness/subplans/2026-05-14-v5.15.5.F.4c.2-bandit-ghost-training-5-state-expansion.md`
**Plan drafted:** 2026-05-14 at `.F.4c.1` baseline — BEFORE `.F.4c.3` shipped 2026-05-15 (which materially restructured the per-core architecture this plan reaches into)
**Audit gate fire:** 5 subagents parallel (parity / trace / readiness / merge / dod)

---

## Per-audit verdict table

| Audit | Verdict | Critical count | High count | Med count |
|---|---|---|---|---|
| /parity-check | RED | 2 | 1 | 2 |
| /trace-deps | RED | 2 | 2 | 1 |
| /readiness | RED | 3 | 1 | 4 |
| /merge-scan | RED | 1 | 2 | 2 |
| /dod-audit | RED | 1 | 3 | 1 |

## Combined verdict: **RED**

Plan body is structurally stale. Amendment 1 is architecturally rejected by all 5 audits (4 convergent + 1 confirmatory). Plan rebase against `.F.4c.3` baseline is the cleaner path.

---

## Critical findings (CONVERGENT — 4-5 agents flag the same item)

### CRIT-1 — DELETE Amendment 1 (`PER_CORE_OVERRIDE_SCALAR_DOMAINS`)

**Agents converging:** parity / trace / readiness / merge / dod (all 5)

**Why:**
- `bandit_algorithm` is **ALREADY per-core** at `CoreFrameworks/CfgFieldRegistry.hpp:592` (row inside `FOREACH_PER_CORE_CFG_FIELD` line 412+).
- Per-core parser **already accepts** `core_N_bandit_algorithm=X` at `CoreFrameworks/ControllerConfig.hpp:2129-2140` (`EMIT_PER_CORE_CFG_PARSER_CASE`).
- Production strategy code **already reads** `core_cfg->bandit_algorithm` at `Strategies/StrategyParameters.hpp:936/963/1009` (single-param consumer sig per `cfg-scope-discipline.md`).
- `PerCoreOverrides<F>` is TRANSITIONAL — retires at WIP2f per `DOCS/MANUAL_FIELDS_INVENTORY.md:39` + `CoreFrameworks/CfgFieldRegistry.hpp:699-700`. Adding `PER_CORE_OVERRIDE_SCALAR_DOMAINS` invests in a soon-deleted mechanism.
- `cfg-scope-discipline.md` § Anti-pattern 1 (lines 78-95) explicitly FORBIDS the "global default + per-core override" shape. `bandit_algorithm` is listed as canonical PER-CORE example at line 136.

**Replacement (structural simplification — ~30% plan scope reduction):**
- Drop Amendment 1's X-macro proposal + parser auto-flow + resolver auto-flow.
- New Step 3.0: consumer migration sweep — see CRIT-2 below.
- Per-core asymmetry test (`core_0_bandit_algorithm=1` + `core_1_bandit_algorithm=2`) lands via existing parser; no new infrastructure.

### CRIT-2 — Class 25 consumer migration sweep (Step 3.0 NEW)

**Agents:** dod-audit (HIGH-2; new material finding); merge-scan (F-B confirms ShardedSnapshot.hpp:764)

**Why:**
- 19+ consumer sites still read flat `cfg.bandit_algorithm` instead of per-core `core_cfg->bandit_algorithm`:
  - `ML_Headers/CoreModelZoo.hpp:1009/1132/1521`
  - `ML_Headers/ThompsonBandit.hpp:11/16`
  - `ML_Headers/BanditAlgorithmRegistry.hpp:14/186`
  - `CoreFrameworks/SlowPathGateRegistry.hpp:95`
  - `CoreFrameworks/ShardedSnapshot.hpp:759/764`
  - `DataStream/EngineTUI.hpp:1230`
  - `GUI/MLStatusPanel.hpp:442`
  - `ML_Headers/StampBoundCfgRegistry.hpp:162-170`
  - `ML_Headers/MlCfgFlagRegistry.hpp:55`
- Works today only via shadow-window legacy flat field at `CoreFrameworks/ControllerConfig.hpp:1319`.
- After WIP2g flat-field deletion, every flat read fails. This is the ACTUAL structural bug this ship should close.

**Effort:** ~3-4h mechanical migration to `core_cfg->bandit_algorithm` (single-param sig per `cfg-scope-discipline.md`). Some sites may need slow-path-rebuild snapshot if cross-core context isn't available at the read.

### CRIT-3 — cfg=2 semantic stance: assign `EXP3_OP_THOMPSON_GHOST` to value 2

**Agents:** parity (Option C) + dod-audit (HIGH-3) converge

**Why:**
- Legacy cfg=2 = BOTH = "Exp3-drives + Thompson-logs-telemetry" — operational meaning was "Thompson present + Exp3 makes decisions".
- New EXP3_OP_THOMPSON_GHOST has SAME operational shape: "Thompson present + Exp3 makes decisions" — only diff is Thompson now actually learns from rewards (the FIX this ship ships; legacy stamps benefit naturally).
- Bytewise back-compat preserved: literal `2` on wire parses identically; HMAC chain bytes unchanged.
- No legacy stamp drift refusal (cfg=2 still semantically valid).
- Pure THOMPSON (state where Thompson drives) gets value 3 or 5; BLENDED gets value 4 or 5.

**Plan amendment:** Step B (the 5-state enum table) renumber:
```
X(EXP3,                        0, BanditAlgo_Exp3_Apply,         1, 0, EXP3,     "Exp3-IX op only")
X(EXP3_OP_THOMPSON_GHOST,      2, BanditAlgo_Exp3_Apply,         1, 1, EXP3,     "Exp3 op; Thompson shadow-update")
X(THOMPSON,                    3, BanditAlgo_Thompson_Apply,     0, 1, THOMPSON, "Thompson op only; Exp3 frozen")
X(THOMPSON_OP_EXP3_GHOST,      4, BanditAlgo_Thompson_Apply,     1, 1, THOMPSON, "Thompson op; Exp3 shadow-update")
X(BLENDED,                     5, BanditAlgo_Blended_Apply,      1, 1, BLENDED,  "EXPERIMENTAL Exp3 + Thompson blend")
```
Note: value 1 (currently THOMPSON in `.F.4c.3` enum) gets reused for EXP3_OP_THOMPSON_GHOST — there's NO existing cfg=1=THOMPSON on the wire in legacy stamps that match this scheme since current `.F.4c.3` enum has `THOMPSON = 1` (legacy). Need to verify: any stamps that exist with current `THOMPSON=1` semantic? If yes, also alias value 1 to a near-equivalent (THOMPSON_OP_EXP3_GHOST is closest match — Thompson-drives + Exp3-also-active). Decide at amendment.

### CRIT-4 — Plan registry name `FOREACH_CFG_FIELD` doesn't exist

**Agent:** readiness (HIGH)

**Why:**
- `.F.4c.3` split `FOREACH_CFG_FIELD` into `FOREACH_GLOBAL_CFG_FIELD` (`CoreFrameworks/CfgFieldRegistry.hpp:248`) + `FOREACH_PER_CORE_CFG_FIELD` (`:412`).
- Plan Step 4.A/4.B references `FOREACH_CFG_FIELD` 4 times.
- New `thompson_exp3_blend_alpha` (was `bandit_blend_alpha` — see CRIT-5) belongs in `FOREACH_PER_CORE_CFG_FIELD` per `cfg-scope-discipline.md` default-per-core rule.

### CRIT-5 — `bandit_blend_alpha` is Class 21 parallel-descriptor collision

**Agents:** trace-deps (Finding 4) + merge-scan (F6) converge

**Why:**
- Existing `bandit_blend_ratio` already exists at `CoreFrameworks/CfgFieldRegistry.hpp:521` with identical metadata profile (FPN<F>, default 0.5, [0,1], STRAT_CAT_ML | STRAT_CAT_USES_BANDIT).
- Different semantics: existing = Exp3 ↔ baseline model blend with ramp-up (per `ML_Headers/BanditLearning.hpp:115`); proposed = Exp3 ↔ Thompson blend for state 4 BLENDED.
- Two cfg fields with `bandit_blend_*` prefix invites operator confusion + Class 21 parallel-descriptor risk.

**Recommended fix:** rename new field `bandit_blend_alpha` → **`thompson_exp3_blend_alpha`** (disambiguates explicitly). Place in `FOREACH_PER_CORE_CFG_FIELD` (per cfg-scope-discipline.md default).

---

## High findings (1-2 agents flag)

### HIGH-1 — Reward-attribution dispatch violates H20 (branchless invariant)

**Agents:** dod-audit (HIGH-1) + merge-scan (F2)

**Why:**
- Plan §D uses `if (bandit_exp3_updates_in_state(algo)) Bandit_Update(...)` mask-AND-shift branchy dispatch on slow-path data-dependent path.
- H20 (added at `.F.4c.3`): branchless preferred for SP/HP data-dependent dispatch.
- `branchless-dispatch-discipline.md` decision matrix — exceptions: boot-time-only / `__builtin_expect`-rare / `if constexpr` compile-time / single binary predicate without alternative. None apply here.

**Recommended fix:** Pattern 1 fn-pointer table indexed by state value, composed with Amendment 2's metadata columns via X-macro reduction:
```cpp
#define _REWARD_DISPATCH_ENTRY(name, val, fn, exp3_up, thompson_up, drives, doc) \
    [val] = ((exp3_up) && (thompson_up)) ? both_reward<F> \
          : (exp3_up)                    ? exp3_only_reward<F> \
          : thompson_only_reward<F>,
template <unsigned F>
static constexpr RewardUpdateFn<F> g_reward_dispatch[FOREACH_BANDIT_ALGORITHM_COUNT] = {
    FOREACH_BANDIT_ALGORITHM(_REWARD_DISPATCH_ENTRY)
};
```
Adding a 6th state = 1 row in `FOREACH_BANDIT_ALGORITHM`. Sister to HandleFill Pattern 1 first canonical (`.F.4c.3`). Composes cleanly with Amendment 2.

**Bonus:** Pattern 5 sink-fn-pointer composition opportunity — this would be 2nd canonical Pattern 5 application after the OmsState trade_log/calibration_log_file pattern at `.F.4c.3`.

### HIGH-2 — Plan number `.F.4c.2 < .F.4c.3` = Version.hpp monotonicity violation

**Agent:** readiness (CRITICAL Check 10)

**Why:**
- Tag `v5.15.5.F.4c.2` < current `Version.hpp` `5.15.5.F.4c.3` = monotonicity downgrade.
- Per `feedback_rename_plans_to_match_ship_order` (MEMORY.md): rename plan file to match ship chronology so tag + version stay monotonic.

**Options:**
- **(a)** Rename to `.F.4c.4` — clean monotonic; ~5min file rename + plan-body refs
- **(b)** Rename to `.F.4c.3.5` — implies "hot-fix to .F.4c.3" which doesn't match scope
- **(c)** Merge with `.F.5` per current sprint MASTER (which says `.F.5` is "per-core Thompson + bandit override audit") — semantically aligned but conflates this ship's bandit-specific scope with the .F.5 broader audit charter

**Recommendation:** **(a) rename to `.F.4c.4`** unless operator wants to repurpose `.F.5`.

### HIGH-3 — File:line refs drifted

**Agents:** parity + trace-deps + readiness + merge-scan + dod-audit (all surface this)

**Confirmed drifts:**
- `ControllerEventLoop.hpp:1731` → actual **`:1755`** (exit-side reward attribution)
- `ControllerConfig_ResolveForCore` cited `:1287-1298` → actual **`:1404-1434`** (+117 lines)
- `PER_CORE_OVERRIDE_BITMAP_DOMAINS` cited `:247` → actual `:248` (trivial)

**Verified stable:**
- `Thompson_Update` at `ML_Headers/ThompsonBandit.hpp:183` ✓
- `thompson_bandits[]` memset at `CoreModelZoo.hpp:1134` ✓
- Buy-side reward sites `CoreModelZoo.hpp:1341/1402` ✓
- `FOREACH_BANDIT_ALGORITHM` at `BanditAlgorithmRegistry.hpp:87-90` ✓ (still 3-state)

### HIGH-4 — Predecessor mismatch

**Agent:** readiness (CRITICAL Check 6) + trace-deps (Finding 2)

**Why:**
- Plan body line 5: "Predecessor: `v5.15.5.F.4c.1`"
- Reality: `.F.4c.3` shipped 2026-05-15 between draft + audit
- Test baseline drifted: plan says "post-`.F.4c.1` + ~10 new" → reality post-`.F.4c.3` is 3148 controller_test + 17 depth_recorder_test + 856 parity_harness

---

## Medium findings

### MED-1 — `bandit_blend_alpha` (renamed `thompson_exp3_blend_alpha`) STAMP_BOUND wire-up gap

**Agent:** parity-check

**Why:**
- Plan Step 4.B/G adds field with STAMP_BOUND metadata.
- Plan Step 6 says "Layer 5b hash recomputed if needed (`.F.4d` handles framework consumption)".
- At `.F.4c.2`-ship time the canonical stamp body emission is still `FOREACH_STAMP_BOUND_CFG` at `ML_Headers/StampBoundCfgRegistry.hpp:99`. If row lands in per-core registry only, the field is silently NOT stamp-bound until `.F.4d`.

**Recommended fix:** Add row to `FOREACH_STAMP_BOUND_CFG` at lines 161-170 (5th in bandit cohort) with `emit_when=(cfg.bandit_algorithm == 5)` — only when BLENDED state active. OR defer the field's stamp-binding to `.F.4d` framework consumption with explicit TECH_DEBT note (plan body doesn't currently flag this gap).

### MED-2 — EnsembleModelZoo size + cache-alignment for exit-side Thompson

**Agent:** parity-check

**Why:**
- Adding `alignas(64) ThompsonBanditState thompson_exit_bandits[NUM_REGIMES]` per plan Option B.
- `static_assert(sizeof(EnsembleModelZoo<64>) % 64 == 0)` at `ML_Headers/CoreModelZoo.hpp:1075` must continue to hold.
- Plan estimates ~560B add; actual likely ~1.4KB (`sizeof(ThompsonBanditState) × NUM_REGIMES`).

**Recommended fix:** explicit Step 0.A.b pre-build sizeof verification + padding spec; build will fail-fast at static_assert if violated.

### MED-3 — Internal plan contradiction (Scope A vs Step 0.B on exit-side decision)

**Agent:** readiness

**Why:** Scope A line 67 says "Decide at pre-coding audit gate — option A defer vs option B add". Step 0.B line 235 says "LOCKED to Option B". Body is internally inconsistent.

**Recommended fix:** delete the Scope A waffle text.

### MED-4 — Class 24 status drifted

**Agent:** readiness

**Why:** Plan body says Class 24 "codification will land at `.F.4c.1`". Verified: already codified at `DOCS/RECURRING_BUG_PATTERNS.md:1563`. Plan's "Anti-patterns to avoid" needs reframing — this ship is the *second* canonical example, not "NEW at `.F.4c.1`".

### MED-5 — X_GEN_LABEL convention

**Agent:** merge-scan

**Why:** Plan's `_BANDIT_EXP3_MASK_BIT` form matches existing precedents but uses leading underscore. Rename to `X_GEN_*` (e.g., `X_GEN_BANDIT_EXP3_UPDATE_MASK_BIT`) for consistency with `X_GEN_BANDIT_ALGO_*` family at `BanditAlgorithmRegistry.hpp:95-150`.

### MED-6 — Merge Thompson_Update with `MASK_EZOO_THOMPSON_READY` guard

**Agent:** merge-scan

**Why:** 3-4 sites would each repeat the bitmap check. Bake into dispatch helper (`Thompson_Update_Guarded`) so callsite can't forget.

---

## What stays valid (don't touch in amendment)

- **Core charter:** Wire `Thompson_Update` to production reward attribution (Class 24 2nd canonical instance).
- **5-state ghost-training expansion** (with cfg-value renumber per CRIT-3).
- **Amendment 2 metadata-driven dispatch matrix** — per-state metadata columns in X-macro row + auto-computed masks via reduction. The pattern is architecturally sound; compose with Pattern 1 fn-pointer table per HIGH-1.
- **NEW DESIGN_SPEC `multi-state-dispatch-with-per-state-update-metadata.md`** — already drafted (Stage 2 DRAFT v1.0); ship its first canonical at this ship close.
- **Exit-side Thompson via `thompson_exit_bandits[NUM_REGIMES]` mirror** (Option B) — closes side-asymmetry per Caramel 2026-05-14 directive.
- **`MASK_EZOO_EXIT_THOMPSON_READY` add** — 1-row registry change at `MemHeaders/EzooInitFlagRegistry.hpp:79` (4/8 bits used, 4 free in uint8_t).
- **~10 new test functions** in `tests/controller_test.cpp` extending v5.14.10 Thompson section.

---

## Cold-pickup viability

Per `feedback_compaction_degrades_treat_handoffs_as_hints`: a fresh session picking up this plan as-drafted would lose **>60 min** re-deriving context (per-`.F.4c.3` postmortem read, Amendment 1 architectural rebase, `cfg-scope-discipline.md` Stage 3 amendments, file:line ref re-verification, Class 24 status reconciliation, tag chronology decision). The plan body is structurally invalidated.

---

## Recommended path forward

**Two viable options:**

### Option A — Patch the existing plan (~45-60 min)

Apply ~10-12 amendments to plan body:
1. Update predecessor `.F.4c.1` → `.F.4c.3`; rebase test baseline (3148 + 17 + 856)
2. DELETE Amendment 1 entirely (CRIT-1)
3. Replace dispatch shape with Pattern 1 fn-pointer table (HIGH-1)
4. Renumber enum values per CRIT-3 (value 2 = EXP3_OP_THOMPSON_GHOST)
5. Rename `bandit_blend_alpha` → `thompson_exp3_blend_alpha` (CRIT-5)
6. Place new field in `FOREACH_PER_CORE_CFG_FIELD` (CRIT-4)
7. Add Step 3.0 consumer migration sweep (CRIT-2; ~3-4h additional work)
8. Fix all file:line refs (HIGH-3)
9. Fix `FOREACH_CFG_FIELD` references (split into GLOBAL + PER_CORE)
10. Reframe Class 24 history (MED-4)
11. Resolve Scope A vs Step 0.B contradiction (MED-3)
12. Rename plan + tag to `.F.4c.4` (HIGH-2)

Pros: preserves existing plan structure + amendment history.
Cons: patches stack — plan body becomes harder to read, references stale baselines.

### Option B — Full rebase (~30-45 min)

Write new `.F.4c.4` plan against `.F.4c.3` baseline. Captures structural simplifications upfront:
- Scope A: wire Thompson_Update (3 buy-side sites + 1 exit-side)
- Scope B: extend `FOREACH_BANDIT_ALGORITHM` 3→5 with metadata columns (Amendment 2 shape)
- Scope C: NEW `thompson_exp3_blend_alpha` cfg field in per-core registry
- Scope D: Pattern 1 fn-pointer dispatch table composed with metadata reduction
- Scope E: exit-side `thompson_exit_bandits[]` mirror + `MASK_EZOO_EXIT_THOMPSON_READY`
- Step 3.0: consumer migration sweep — 19+ sites flat → per-core
- NEW DESIGN_SPEC consumption (Stage 2 DRAFT → Stage 3 ACTIVE)
- ~10 new test functions

Pros: clean against current architecture; honestly reflects what's actually changing post-`.F.4c.3`; ~30% scope reduction visible.
Cons: requires new file write.

### Operator decision required

Three decision points blocking coding:

1. **Plan rename:** `.F.4c.4`? `.F.5` merge? Other?
2. **cfg=2 stance:** assign `EXP3_OP_THOMPSON_GHOST` to value 2 (preserve legacy operational meaning + add Thompson-learning)? Or renumber wholesale + ship one-time legacy stamp remap?
3. **Plan strategy:** Option A patch (~45-60 min) OR Option B rebase (~30-45 min)?

Post-decisions: I'll amend/rebase the plan, then re-run `/precoding-audit-gate` to confirm GREEN before coding starts (per consult-before-coding discipline).

---

## Auto-write items pending

- **PARITY-008** (`DOCS/PARITY_ISSUES.md`): cfg=2 semantic break decision needs durable record once stance locked
- **TECH_DEBT** entry: Class 25 consumer-migration sweep (19+ flat reads of `cfg.bandit_algorithm`) — should be tracked even if not closed in this ship
- **DESIGN_SPEC update:** `multi-state-dispatch-with-per-state-update-metadata.md` composition note (line 120-128 references `PER_CORE_OVERRIDE_SCALAR_DOMAINS`) — needs amendment to align with `cfg-scope-discipline.md` direct PerCoreCfg<F> mechanism

---

**End of synthesis.** GREEN expected post-amendment + audit-gate re-run.
