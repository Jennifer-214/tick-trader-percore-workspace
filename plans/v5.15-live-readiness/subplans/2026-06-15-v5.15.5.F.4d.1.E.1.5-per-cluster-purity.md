---
type: subplan-scaffold
ship_tag: v5.15.5.F.4d.1.E.1.5
anchor: pre-v5.15.5.F.4d.1.E.1.5
parent: 2026-05-28-v5.15.5.F.4d.1.E.1-foundation.md
predecessor: 2026-06-15-v5.15.5.F.4d.1.E.1.4-fill-completeness.md
successor: 2026-06-15-v5.15.5.F.4d.1.E.1.6-multi-exchange.md
status: scaffold
risk: HIGH
hot_path: true
spine: SPINE-3 (purity)
live_gate: PRECONDITION-2-of-2 (HARD live-enable = E.1.3 ∧ E.1.5)
decisions_honored: [D-193/H22, D-209, D-217, D-218, D-168]
td_closed: [TD-190, TD-168]
guard_rows: [§4a-H22-candidate, A34-per-cluster-purity]
source: plan_checks/2026-06-15-E.1-decomp-A-adjudication.md §2 (E.1.5)
---

# E.1.5 — per-cluster authority relocation + A34 concentration governor

> SCAFFOLD (not a coding body). Meaningful enough to reason + sequence + seed the dive body; the full body is re-grounded against HEAD by a C-class pass when this leaf becomes the dive (per `plan-hierarchy-and-sub-master-decomposition.md` §3). All file:line are HEAD `572c978` and WILL drift — re-verify at dive.

## Scope

**SPINE-3 (purity). The per-cluster-purity completion ship + HARD-gate precondition 2.** Two concerns under one rollback anchor (a break here is a PURITY break — a node reading global, or a concentration mis-aggregation — isolated from fill-math + coherence; the smallest of the three capital spines: NEW-1 is trivial, A34 is the substance):

1. **NEW-1 per-cluster authority relocation** — relocate the predicate body `cfg.trading_mode` → `cluster.trading_mode` (one-cluster = one-exchange). **The H22 scale-out seam: a SINGLE body edit, ZERO authorizer-site edits.** The predicate `ControllerConfig_IsLiveCapital` ALREADY EXISTS (it landed `.E.0.10`); this ship moves WHERE it reads the mode from (global cfg → the node's cluster), so a Node never independently authorizes live-capital — it INHERITS its cluster's decision.
2. **A34 per-cluster CONCENTRATION governor** — N nodes long the same symbol = N× concentration owned by NOTHING (the one genuine cluster-composition hole). A per-cluster-PURE governor on the aggregator: a cluster's risk-governance is a pure function of its OWN nodes (its slice of the global running aggregate). **DISTINCT from `.E.5` `max_per_node_pct`** (that bounds a single node; A34 bounds the cluster's same-symbol aggregate).

## Spine

SPINE-3 of the three D-214 spines (**purity** / coherence / fill). SPINE-3 is downstream of BOTH SPINE-2 (coherence, E.1.3 — A34 governs on E.1.3's per-cluster aggregate slice) and SPINE-1 (fill, E.1.4 — A34 governs on the FILL-FED aggregate, so the fill path must be conservation-correct first). D-217/D-218: NEW-1 is the per-cluster relocation of the `.E.0.10` HARD-REFUSE predicate (RBP Class 47).

## TD-closed (one line each)

- **TD-190** — cluster-aggregate exposure / A34 concentration governor (the substance).
- **TD-168** — live-OFF-till-`.E` per-cluster authority — verify subsumed (the relocation makes a per-shard read of a global authority field UNREPRESENTABLE; A1 mostly landed, verify).

## Locked decisions

- **D-193 / H22** — per-node / per-cluster purity. The relocation IS the purity mechanism: a Node never reads a global authority field; it reads its cluster's.
- **D-209** — `owner_node_id` (E.1.2) underpins the per-cluster aggregate attribution.
- **D-217** — NEW-1 per-cluster relocation (the single-authority predicate, fan-out-confirmed).
- **D-218** — NEW-1 HARD-REFUSE on the `use_real_money`/`trading_mode` conflict (reconciled `.E.0.10`; the relocation preserves the hard-refuse semantics, now per-cluster). RBP Class 47.
- **D-168** — live OFF until ALL of `.E` / v5.16; this ship closes precondition 2 of the HARD gate.

## §4 guard-rows this leaf fills

- **§4a H22-candidate** — "a per-shard read of a global cfg field that HAS a `core_N_*` override" → CI-candidate. **The relocation makes this UNREPRESENTABLE** for `trading_mode` (a Node can no longer read the global mode — it reads `cluster.trading_mode`). This is the canonical H22-violation-made-impossible row.
- **A34 per-cluster-purity row** — the concentration governor is a pure function of the cluster's own nodes (no cross-cluster coupling, no flattened-global read).

## INBOUND seam (needs from E.1.3 + E.1.2)

- From **E.1.3**: the GLOBAL aggregator's **per-cluster running aggregate slice** (A34 governs on the per-cluster running aggregate — the published, coherent snapshot, never a racing read).
- From **E.1.2**: the `cluster.trading_mode` field (the relocation TARGET in `ClusterState`) + the UNCHANGED authorizer call-sites (the body edit moves WHERE `ControllerConfig_IsLiveCapital` reads from; the call-sites do not change — that is the ZERO-authorizer-site-edits property).
- From **E.1.4**: the conservation-correct fill path feeds the aggregate A34 governs on (A34 on a torn / mis-booked aggregate would be meaningless — hence purity is the LAST capital spine).

## OUTBOUND seam (to E.1.6)

**Capital authority is per-cluster-PURE.** Specifically: the (N+1)th-cluster test passes (adding a cluster requires ZERO change to existing per-cluster authority logic — the operational H22 test); concentration is owned by the governor (no N× same-symbol hole). E.1.6's `FOREACH_EXCHANGE` machinery then wires into this PURE per-cluster container (the framework rides the purity, never re-opening it).

> ★ **HARD live-enable = E.1.3 (coherence) ∧ E.1.5 (purity), BOTH required (D-168).** This ship closes precondition 2 of 2 (per-cluster authority + A34 — the second gate). **With E.1.3's coherence (precondition 1) AND this ship's purity (precondition 2), BOTH preconditions of the HARD live-enable gate are satisfied** — but live still stays OFF until all of `.E` / v5.16 (D-168). This is stated identically on E.1.3.

## Design-shape (code-grounded; HEAD 572c978, WILL drift — re-verify at dive)

**The NEW-1 relocation (a SINGLE body edit).** `ControllerConfig_IsLiveCapital` (`ControllerConfig.hpp:2045`) — the predicate body currently reads `cfg.trading_mode`. Relocate it to read `cluster.trading_mode` (the field lands in `ClusterState` at E.1.2). **ZERO authorizer-site edits** — every call site keeps calling `ControllerConfig_IsLiveCapital` (or its per-cluster successor); only the predicate's internal source moves. The cfg-file KEY stays a parse-alias (note at `ControllerConfig.hpp:594`). The HARD-REFUSE conflict semantics (D-218, RBP Class 47) are preserved, now resolved per-cluster.

**The A34 governor (the substance).** A per-cluster CONCENTRATION check on E.1.3's per-cluster aggregate slice: sum the cluster's same-symbol exposure across its OWN nodes; refuse / gate an entry that would push the cluster past its concentration bound. Per-cluster-PURE — reads ONLY the cluster's slice of the global running aggregate (D-34: the aggregator is global, the per-cluster *view* is a slice). The per-cluster aggregate slots come from E.1.2/E.1.3.

## Rollback anchor

`pre-v5.15.5.F.4d.1.E.1.5`. **A break here is a PURITY break** — a node reading global, or a concentration mis-aggregation — isolated from fill-math (E.1.4) + coherence (E.1.3), both upstream-CLOSED. The smallest capital ship (NEW-1 trivial; A34 the substance). `git bisect` lands on this anchor.

## Known folds it owns

- **The H22 operational test** — the (N+1)th-cluster test (adding a cluster = ZERO change to existing per-cluster authority logic). Write it as the OUTBOUND acceptance witness.
- **A1 verify** — TD-168's A1 leg (a warm-restart reading the GLOBAL `take_profit_pct` not the node's per-strategy override) is MOSTLY landed `.E.0.10`; VERIFY it is fully subsumed by the relocation pattern, do not assume.
- **The `core_N_*` → `node_N_*` override read** — after E.1.1's rename, confirm A34 + NEW-1 read the per-NODE override, never the flattened global (the H22-candidate CI row is the structural guard).
- **DISTINCT-from-`.E.5` note** — A34 (per-cluster same-symbol aggregate) is NOT `max_per_node_pct` (per-node single-position bound, `.E.5`). Keep them separate concerns; do not conflate at design time.

## Shape-match note (do NOT re-schedule landed work)

The NEW-1 predicate `ControllerConfig_IsLiveCapital` EXISTS (`ControllerConfig.hpp:2045`) — it landed `.E.0.10` (D-217/D-218, RBP Class 47). This ship RELOCATES it (the `cfg.trading_mode` → `cluster.trading_mode` body edit), it does NOT build it. The HARD-REFUSE conflict-handling (D-218) is already reconciled `.E.0.10` — preserve it, do not re-implement. A34 is NEW (the per-cluster aggregate slots come from E.1.2/E.1.3). A20/A21/A17 (the other `.E.0.10` durable-now fixes) are NOT this ship's surface.
