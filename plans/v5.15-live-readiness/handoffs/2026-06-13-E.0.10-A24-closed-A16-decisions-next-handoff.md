---
type: handoff
status: superseded
ship_tag: .E.0.10
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-12-E.0.10-A25-A28-landed-A24-next-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
required_reading: [decision-log D-211 + D-212, register (§Routing map + A1–A36 + the /decision-check verdict §), per-node-position-ownership-model.md, per-node-purity-scale-invariance.md (H22 spec), the .E.1 foundation plan (items 53/54/58/62), OrderManager.hpp (the fill path), DataStream/BinanceUserData.hpp:339, Order.hpp:179, LANDMINES.md (Landmine 10), DOCS/STRATEGY_AND_CODING_RULES.md (OMS-drainer invariants — A16 surface), definition-of-done-and-armed-scout-verification.md (M8 close discipline), latency-path-discipline.md (the .E.1 fill-path latency constraint), CLAUDE.md H22, CODE_MAP (regen at pickup)]
engine_head: f2ef5d6 (A24 slice-redirect + A32 display; on feat/v5.15-live-readiness; clean; tag ckpt-a24-closed)
workspace_head: 5c28fcb + this handoff + the D-212/latency sync (verify "at or after")
coding_status: fix-ship in flight — A9/A19/A1/A3/A8/A25/A28 + the F1/F3/F7 deep-check fixes + A24 LANDED; A16 is the next CODE step but its TWO decisions are DEFERRED to this fresh session (see § DECIDE FIRST)
deletion_scope: none (A36 MAY tombstone an identifier — pending the A36 decision)
defers: none
---

# Handoff — `.E.0.10` — A24 CLOSED; A16 decisions next (fresh-session pickup)

## TL;DR (the 30-second pickup)

- **A24 is CLOSED + 5-way-verified + guarded + anchored** (`ckpt-a24-closed`). Engine clean at `f2ef5d6`.
- **A16 (partial-fill state-gate) is fully SYNTHESIZED (D-212) but its TWO decisions were DEFERRED to this fresh session** — the prior session hit its context limits, so the decisions were parked for un-degraded judgment. **Make them FIRST** (§ DECIDE FIRST), THEN implement.
- After A16: **F-059 → A6** close the `.E.0.10` net.
- Nothing is open or lost: A24 done; A16 homed in D-212; both repos committed.

## § DECIDE FIRST — the two operator decisions deferred to this session

Run the A16 `/decision-check` **FRESH here** (the synthesis is in D-212; re-ground it un-degraded — `feedback_compaction_degrades_treat_handoffs_as_hints`), then decide. Both are consult-before-coding — do NOT auto-proceed.

**Arm the A16 `/decision-check` (Stage 2.5 routing + M8) — the surface is OMS fill-path / money-booking / drainer-latency, so the agents must NOT boot blind:** route the investigative + adversarial agents to `/accounting-audit` (the fill→book `Money`/P&L SSoT), `/hft-audit` (the drainer ≤10μs budget + the D-212 latency constraint), `/bug-check` (Class 38 aspirational-comment + the partial-fill class), `/trace-deps` (the OMS state-machine consumers of `order_complete` / `ORDER_FILLED`) — ARMED with the reference docs (`STRATEGY_AND_CODING_RULES.md`, M8) + the mechanical toolchain to RUN (`./build.sh test`, `run_all_tests.sh --full` for asan/ubsan, `tools/calls_graph_diff`, regen `./tools/gen_code_map.sh`) + scout-first. (This matches how the A24 handoff armed *its* `/decision-check`.)

1. **A16 split** — confirm the recommended shape:
   - **`.E.0.10` loud-STOP (now):** gate `HandleFill` (`OrderManager.hpp:1438`) **and** the slot-free (`:1465`) on `ORDER_FILLED`; a PARTIAL fill → **keep the slot open, book nothing, emit a loud degrade-arm warning**. (Today the slot is freed UNCONDITIONALLY at `:1465` — that's the bug.)
   - **A17 co-land:** the paper synthetic fill (`~:1013`) + the REST adapter never set `order_complete` → set it, so the gate has a signal. Extend to the REST adapter.
   - **Full multi-fill reassembly FOLDS to `.E.1`** (the submit-path rework, item 54, alongside A3). NOT done in `.E.0.10`.
   - Recommended by the `/decision-check` + the AR-11 code-read (D-212). **Confirm or adjust.**
2. **A36** — **tombstone (lean — recommended)** vs **wire-emit**. The A36 row in the register is NOT yet updated with a verdict; this decision sets it.

## The A16 synthesis (D-212 — what the fresh `/decision-check` should re-ground)

- **The bug is SLOT-LIFECYCLE, not qty-accumulation.** AR-11 code-read: `OrderManager.hpp:1465` frees the slot (`order_bitmap &= ~(1<<slot)`) UNCONDITIONALLY; `filled_qty` (`:1419`) is per-invocation (only 4 refs — NOT an accumulator, despite `Order.hpp:179`'s aspirational "running total" comment = Class-38). So the fix is to gate the free/book on `ORDER_FILLED`, not to build qty-accumulation.
- **The fix shape:** gate `HandleFill` + the free on `ORDER_FILLED`; PARTIAL → keep slot, book nothing, warn loud (the `.E.0.10` loud-STOP). `handle_buy_fill` (`:1177`) lacks the active-slot guard `handle_sell_fill` (`:1210`) has — relevant to the reassembly.
- **`.E.1` already designs the scale-invariant fill path** — no new/scrapped design needed. The full multi-fill reassembly folds into `.E.1`'s submit-path rework: items **53** (per-node owns its OWN fill consumption; the central drainer is absorbed — Class 26 dies), **54** (submit-path rework; folds A3 + now A16; "characterize failure-path HERE not `.E.0.10`"), **58** (per-cluster shared resources), **62** (O(1) event-sourced aggregator) + the `FOREACH_EXCHANGE` adapter framework (per-venue fill parser + `fill_callback` normalization boundary + venue-net reconcile).
- **The protocol-agnostic fill event** (the structural answer to "two ways to fill = bad"): venue adapters normalize WS-per-increment (`"l"`) / REST-cumulative (`"z"`) / FIX → ONE common fill event; the OMS runs ONE state machine. The anti-pattern is the protocol leaking into the OMS core (= today's A16 bug — the OMS implicitly assumes a fill shape).
- **Latency (D-212 point 5 — the operator's "does normalization add latency? this feels bad" concern, VERIFIED + folded as a `.E.1` design CONSTRAINT, NOT a blocker):** the fill path is **post-trade / async** (WS/REST callback, ms-bounded by venue RTT), NOT the ≤500ns hot path (`BG/SG_Evaluate`) nor the ≤10μs drainer-submit; the hot path reads the **seqlock-published** position, unaffected. Normalization is **at-parse** (the adapter must parse the venue message regardless → into a common POD is the same work). Dispatch is **compile-time `tt::<verb><ExchangeT>`** (H13 monomorphization, **no vtable** — "protocol-agnostic" ≠ runtime-polymorphic). So POD fill event (no alloc, H1) + compile-time dispatch (no vtable, H2) + slow-path state machine (off hot path) = **zero hot-path cost**. The `.E.1` fill-path design pass MUST *verify* these three hold (POD · compile-time dispatch · slow-path), not assume them.

## Next steps (AFTER the two decisions land)

1. **Amend the `.E.1` plan:** fold A16 into the submit-path rework (item 54, alongside A3).
2. **Update the register:** A16 / A17 / A36 rows with the verdicts (the disposition-at-fix-time discipline).
3. **Implement the `.E.0.10` loud-STOP + A17** → 2-event partial-fill char-test → 5-way verify (suite + adversarial + DOD/HFT + calls_graph_diff + asan/ubsan) → guard (Check) → flip trackers → 2 commits → rollback anchor. (The repeatable drill.)
4. **Then F-059** (exit-chain golden-master; AFTER A24/A25 — now unblocked) **→ A6** (chokepoint gate-emit tp/sl-validity guard). These two close the `.E.0.10` net.

## What this session LANDED (A24 + the foundation pass)

- **A24 (engine `f2ef5d6`):** redirected the D6 session-mult / D10 brake / spike-spacing from the flat `resolved_cfg.{volume_multiplier,entry_offset_pct,spacing_multiplier}` to `resolved_cfg.cores[slot].X` (4 slice-writes ~:2343/:2414/:2421/:2976) + repointed the spacing GUI diag (~:2990) to the slice (A32 display fix). The dead D6/D10/spike adaptation now reaches the per-node consumer (SimpleDip/EmaCross/ML). Read-only clamp bounds stay flat (resolve-equal).
  - **Verified 5 ways:** suite 3388/0 · adversarial 7/7 · DOD/HFT 5/5 · `calls_graph_diff` CLEAN · asan/ubsan 3246/0.
- **Check-11 guard** (`tools/check_per_core_registry_integrity.py`) — detects a per-shard FLAT write of a per-node cfg field (the H22 cfg-surface mechanical close; 88 fields tracked; PASS + regex self-test).
- **H22 spec authored** (`DESIGN_SPECS/data-disciplines/per-node-purity-scale-invariance.md`) — closes CLAUDE.md's dangling H22 reference; A1 + A24 canonical instances; sister-linked with `per-node-position-ownership-model.md`.
- **D-211** (A24 option-c + the D6/D10/spike redesign directions) + **D-212** (the A16 synthesis — this handoff's § above).
- **`.E.M` stub** (`subplans/2026-06-12-...E.M-adaptive-entry-redesigns.md`) — D6 baseline-free volume normalization / D10 keep+measure / spike bandit-learned (A11/A12-gated) / cluster-aggregate.
- **Register Round-2** (Class-44 cohort: A32 display-inversion folded; A33 third-storage; A34 cluster-aggregate) + **TECH_DEBT 189-192** (third-storage convergence / cluster-aggregate risk / A24 flat-field deletion / dead-code cluster).
- **Class 44 completed** (recurrence 4→8; cfg-flag-orphan sub-variant) + **Class 2 extended** (A29/A30/A32/A35) + **Landmine 10** (the engine/workspace commit-split: `tests/` is a workspace symlink; the Edit tool tracks the literal path string; `git add tests/` in the engine fails "beyond a symbolic link").
- **`/accept-handoff` skill: always-on M8 session-arming** (the "arm the SESSION the way M8 arms a subagent" addition — no more re-asking each session) + Stage 3.6/3.7 + Stage 8 kit.
- **Memory** `feedback_proactive_novel_alternative_consideration` — extended to the foundational/structural-post-audit level (operator priority: post-audit, evaluate whether a better STRUCTURE exists vs patching the current one, as a first-class output).
- **`sister_specs` annotation fix** (`tools/check_doc_metadata.py` — `_strip_sister_annotation`; ~40 broken refs across `.E.1`–`.E.8` specs resolved).

## Critical pickup-time reads

- `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` — **D-211 + D-212** (the A24 + A16 records; D-212 point 5 = the latency answer).
- `plan_checks/E.0.10-finding-disposition-register.md` — §Routing map + A1–A36 + the `/decision-check` verdict §. (A16 row not yet carrying the loud-STOP verdict; A36 row not yet carrying a verdict — both pending this session's decisions.)
- `DESIGN_SPECS/data-disciplines/per-node-purity-scale-invariance.md` (H22) + `per-node-position-ownership-model.md` (the one-position-per-node model + the cluster-aggregate gap).
- `subplans/2026-05-28-v5.15.5.F.4d.1.E.1-foundation.md` — items **53 / 54 / 58 / 62** (where A16-full folds).
- `CoreFrameworks/OrderManager.hpp` — `:1013` (paper synthetic fill, A17), `:1177` `handle_buy_fill` (no active-slot guard), `:1210` `handle_sell_fill` (has the guard), `:1370` `OrderManager_ProcessFillCommand`, `:1404` (dedup-if-FILLED), `:1419` (`filled_qty` overwrite), `:1432` (sets FILLED/PARTIAL from `order_complete`), `:1438` (`HandleFill` gated on `event_log_mode==1`), `:1465` (the unconditional slot-free — **the bug**).
- `DataStream/BinanceUserData.hpp:339` — `fill_qty = "l"` (last-fill increment), not cumulative `"z"` (A2/D-123, unparsed).
- `CoreFrameworks/.../Order.hpp:179` — the aspirational `filled_qty`/`avg_fill_price` "running total"/"weighted across partials" comments — NEVER implemented (Class-38).
- `DOCS/LANDMINES.md` — **Landmine 10** (the engine/workspace commit-split; read before committing test changes).
- `CLAUDE.md` **H22** + the regen'd `DOCS/CODE_MAP.md`.

## TaskList snapshot (recreate at pickup)

| # | Status | Task |
|---|---|---|
| 1 | ✅ done | A24 — cfg-mutation single-source (H22) |
| 2 | ✅ done | H22 spec — per-node-purity-scale-invariance.md |
| 3 | ⬜ pending | **A16 — partial-fill state-gate (+ fold A17)** ← next code step (decisions first) |
| 4 | ⬜ pending | F-059 — exit-chain golden-master (after A24/A25 — now unblocked) |
| 5 | ⬜ pending | A6 — chokepoint gate-emit tp/sl-validity guard |
| 6 | ✅ done | Capture .E.0.10 A24 design session |
| 7 | ✅ done | Round-2: Class-44 detector + orphan sweep |
| 8 | ⬜ pending | .E.M adaptive-entry redesigns (homed stub) |
| 9 | ⬜ pending | cfg-flag-orphan cohort — A13/A14/A35 (+A37) wire-or-tombstone |
| 10 | ⬜ pending | Class-44 detector CI guard — the structural close |
| 11 | ✅ done | Codify Round-2 findings + H22 spec (foundation pass) |
| 12 | ⬜ pending | D10 re-activation paper-test (A24 behavior-validation) |

## Predecessor context (what landed at the A24 close — verify at pickup, Stage 4.5)

- **A24** (engine `f2ef5d6`) + the **H22 spec** + **Check-11** + **D-211/D-212** + register Round-2 + **TECH_DEBT 189-192** + Class 44 completed / Class 2 extended + **Landmine 10** + the `/accept-handoff` M8-arming + the memory extension + the `sister_specs` annotation fix.
- Rollback anchor: **`ckpt-a24-closed`** (engine `f2ef5d6` + workspace `5c28fcb`) — the clean A24-closed state.
- Tests 3388/0; asan/ubsan 3246/0.

## Rollback anchor

`ckpt-a24-closed` (engine `f2ef5d6` + workspace `5c28fcb`) — the clean, fully-verified A24-closed state. If A16 implementation goes sideways, reset here.

---

**Pickup command:** `/accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-13-E.0.10-A24-closed-A16-decisions-next-handoff.md`

**Immediate next action:** run the A16 `/decision-check` fresh (re-ground D-212 un-degraded) → make the two decisions (A16 split + A36) → then implement per § Next steps.
