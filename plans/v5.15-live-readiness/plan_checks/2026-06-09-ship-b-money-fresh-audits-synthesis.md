# Ship-B (decimal money) pre-coding gate — synthesis

**Date:** 2026-06-09 (Session 13) · **Plan:** `subplans/2026-05-30-v5.15.5.F.4d.1.E-money-numeric-core-foundation.md` v0.3 (same-day post-A.5 re-audit) · **HEAD:** `0e48150` (E.0.8) · **Tier:** HIGH-RISK (D-77 heavier-default)
**Audit set:** parity, trace-deps, readiness, merge-scan, dod, accounting, hft, blindspot + quorum ×3 (money-correctness, k=2-of-3) + adversarial verification pass + completeness critic = **13 agents**.
**Stage-2.5 mechanical floor (pre-spawn):** check_session_docs ALL HARD GREEN · B-Plus 0 fabrications · tests-section PASS (3 subcategories) · fee-site enumeration COMPLETE · decision-log sentinel scan: D-97..D-167 all decided.
**Execution note:** the operator accidentally denied some subagent permission prompts mid-run; all 11 reports landed regardless, and the verification pass re-derived every CRIT/HIGH from code (not from report text), absorbing any degraded step.

## 1. Per-audit verdicts

| Audit | Verdict | Report |
|---|---|---|
| /parity-check | YELLOW | `parity-check-2026-06-09-ship-b-money.md` (+ PARITY-037 written) |
| /trace-deps | YELLOW | `trace-deps-2026-06-09-ship-b-money.md` |
| /readiness | YELLOW | `readiness-2026-06-09-ship-b-money.md` |
| /merge-scan | YELLOW (reuse slate) | `merge-scan-2026-06-09-ship-b-money.md` |
| /dod-audit | YELLOW | `dod-audit-2026-06-09-ship-b-money.md` |
| /accounting-audit | YELLOW (2 CRIT found) | `accounting-audit-2026-06-09-ship-b-money.md` |
| /hft-audit | YELLOW (hot-path claim VERIFIES) | `hft-audit-2026-06-09-ship-b-money.md` |
| /blindspot-scan | YELLOW (4 SILENT-RISK) | `blindspot-scan-2026-06-09-ship-b-money.md` |
| Quorum 1/2/3 | see § 3 | `quorum-money-correctness-{1,2,3}-2026-06-09.md` |
| Verification pass | 11/12 REAL, 1 refuted | `verification-pass-2026-06-09-ship-b-money.md` |
| Completeness critic | 3 gaps + 2 pins | `completeness-critic-2026-06-09-ship-b-money.md` |

## 2. Combined verdict: **YELLOW — heavy amendment slate, zero architecture re-opens**

The DECISION layer (decimal + unified radix core + two-ship split + all D-97..D-167) was independently re-verified sound by every agent; nothing re-opens it. The hot-path-UNTOUCHED claim VERIFIES against HEAD (compares are radix-agnostic bare `.v` i128 compares; the 3 money-muls are confirmed `__builtin_expect`-rare). All findings are pre-coding plan amendments + design additions. The slate is heavier than a 30-minute edit — it is a dedicated amendment pass (~2-4 h plan work, no code) — but consistent with the June-1 step-7 precedent this synthesis stays YELLOW: amend, re-check, then code.

## 3. Quorum confirmation (k=2-of-3) + verification

| Finding | Q1 | Q2 | Q3 | Verdict |
|---|---|---|---|---|
| Commission-asset unit error (B1) | CRIT | HIGH | CRIT | **CONFIRMED 3/3 + V1 REAL** |
| OrderEvent carries no fee → replay≠booked | HIGH | HIGH | HIGH | **CONFIRMED 3/3 + V3 REAL** |
| Same-sizeof flip silences epoch guards | HIGH | HIGH×2 | HIGH | **CONFIRMED 3/3 + V4 REAL (a,b,c)** |
| Flag-loud overflow: no mechanism | MED | — | MED | CONFIRMED 2/3 (+dod F1) |
| #5 `ok=false` on LIVE fill path unspecified | LOW | — | LOW | CONFIRMED 2/3 |
| tickSize not loaded (PRICE_FILTER absent) | LOW | LOW | MED | CONFIRMED 3/3 (plan admits; wording fix) |
| Quantize-direction oversell risk | MED | CLEAN | — | **REFUTED (V11)** — oracle pins floor-to-step; quorum-2 right |

## 4. Findings register (severity · kind · disposition)

### CRITICAL — both verified REAL; plan amendments before any code

- **S-1 [CRIT·structural] B1 books commission in the wrong UNIT — and the plan body contradicts the already-decided D-127.** `BinanceUserData.hpp:361-363` parses `n`/`N`; `commission_asset` reaches no consumer (`ExchangeAdapter.hpp:56-57`); booking sinks are quote-denominated (`OrderManager.hpp:1207-1215`). BUY commission is base-denominated (≈price-factor understatement); BNB mode wrong too. **D-127 already folded F-B asset-aware booking — the operative body text (`:165`, B1 `:245`, acceptance `:333`) was never updated.** Corollary (V1): base-asset fee depletes received qty; engine books full `fill_qty` (`:1166`) → oversell/dust. **Disposition:** amend B1 + acceptance to D-127 shape — carry `(amount, asset)`; quote=direct, base=×fill_price exact + qty-depletion handling, BNB=explicit decision (incl. possible boot-refuse `pay_fees_in_bnb` LIVE) — + D-100 oracle rows over recorded fills incl. asset cases. *Specs: defer-to-source-authority (D-106), decision-time-data-binding.*
- **S-2 [CRIT·structural] Multi-fill vehicle gap — PRE-EXISTING LIVE bug B1 inherits.** `OrderManager.hpp:1383` sets ORDER_PARTIAL, then `:1414-1415` frees the slot unconditionally (comment `:1380-1382` contradicts code); fill events 2..N silently skipped (`:1335-1350`). B1's commission-carry has no vehicle after fill 1; `n` is per-fill → carry must ACCUMULATE. **Paper-fill trap (V2):** paper fills are set PARTIAL and *depend* on the unconditional free — a naive terminal-only-free fix leaks every paper slot. **Disposition:** designed sub-fix inside B1 — keep-slot-until-terminal + accumulate (qty/commission/weighted price) + multi-fill test (none exists) + paper-path regression guard.

### HIGH — all verified REAL or 2+-agent convergent

- **S-3 [HIGH·structural] Replay cannot reproduce booked money.** `OrderEvent` (`OrderEventLog.hpp:79-92`) has no fee field; both folds recompute from a rate (OEL `:656-677`, CEL `:861-880`); boot-Init replay passes nullptr cfg → **fees reconstruct as ZERO** (comment `:951-953`). Also three different rate-resolutions across families (per-Order pre_resolved vs per-core vs one global param — accounting HIGH-2). **Disposition:** persist the booked decimal fee per fill event (schema change rides the epoch); C2 differential re-scoped (computed paths + a reported≠computed case); rate-asymmetry collapses once the event carries the fee.
- **S-4 [HIGH·structural·wide] Layout-unchanged semantic flip silences every size-keyed epoch guard.** (a) version-test floor is `>=13/9/6` forward-compatible (`controller_test.cpp:~24448`); (b) R3/discipline text keys on "sizeof(FPN) changed" — 16B→16B never fires; (c) `OrderEventLogFileHeader` has **no version field** (`OrderEventLog.hpp:126-131`; magic/fpn_width/entry_size all unchanged) → old binary-scaled snapshots/logs load cleanly, misread ×~1.8e11. **Disposition:** explicit R3-B row — snapshot versions →14/10/7 + raise the test floor in the SAME commit as the first money flip + re-key discipline text "sizeof" → "value-encoding (scale/radix/semantics)" + event-log header version (magic `OMSEL01`→`OMSEL02` or claim a reserved word) + epoch-reject acceptance row covering BOTH. *Harvest candidate: "semantic-flip-at-identical-layout silences size-keyed guards" (B10 detection note + meta-anti-pattern-index row at close).*
- **S-5 [HIGH·structural] `cfg_drift_compare`/`cfg_drift_format_reason` silently no-drift for decimal.** StampT-only static_assert (`CfgFieldDispatch.hpp:456-461`), no `always_false` final else, falls through `return false` (`:486`; same shape `:505-532`) — `cfg_drift_compare<double, FixedPoint<10,8>>` compiles. The B3 "red-build" claim is true for the single-T seven, FALSE for the two dual-type dispatchers. **Disposition:** B6 exhaustive-else + StampT×CfgT family asserts land as a BLOCKING PRECONDITION step of the type flip; coverage tool covers dual-type chains.
- **S-6 [HIGH·structural] No mechanical pre-epoch stamp rejection.** `STAMP_FORMAT_VERSION` CURRENT=2/MAX=2 rejects only future (`ModelInference.hpp:141-142/:1544-1551`); fingerprint parse+display only. **Disposition:** bump →3 + hard-refuse pre-epoch stamps at load; lands with the epoch (tempered by no-live-models, but the guard ships with the flip).
- **S-7 [HIGH·design — RATIFY AT CONSULT] B4 price-domain fork must lock the EGRESS arm.** The cast-at-compare arm = per-tick cross-radix cast inside BG/SG (≈20-25cyc ×2-4 operands) — contradicts hot-path-UNTOUCHED + H8; egress (thresholds→money at gate-build, slow cadence) = zero per-tick delta. Convergent dod F4 + hft F2 + V7. **Disposition:** amend B4 "Pick one" → DECIDED egress; price-stats stay binary. *Spec: decision-time-data-binding.*
- **S-8 [HIGH·structural·wide] Internal lossy money round-trips unenumerated.** (a) paper fills `FPN_ToDouble`→double `OrderResult`→`FPN_FromDouble` (`OrderManager.hpp:995-996`→`:1369-1370`); (b) REST sync-fill double parse + double-division avg_price (`BinanceOrderAPI.hpp:533-537`); (c) `OrderResult` all-double (`ExchangeAdapter.hpp:43-58` — direction already decided D-123). **Disposition:** add to D-103/B6 + #5 cohort; retype the cmd-ring vehicle (scaled int64 satisfies the POD constraint); file named in plan (`ExchangeAdapter.hpp` was previously unnamed).
- **S-9 [HIGH·design] Decimal DIVISION has no design artifact.** Money÷money runtime-divisor sites confirmed (`PortfolioController.hpp:1186`; CEL `:2853/:2906/:3256`); `udiv_q64` hardcodes the `<<64` binary widening (`FixedPointN.hpp:1336`); naive `a.v*10^8/b.v` overflows AND emits `__udivti3`; D-100 oracle has no Div rows. **Disposition:** add fn **#7 decimal Div** (256-bit `a·10⁸` dividend variant) to the design sidecar + oracle Div rows + no-`__udivti3` acceptance check.
- **S-10 [HIGH·structural] SymbolFilters money doubles + quantization supersede-set.** `lot_step_size`/`min_notional` are double (`BinanceOrderAPI.hpp:76-79`); #6 must retype at filters load + DELETE `binance_round_qty`/`binance_step_decimals`/both `%.*f` emits (`:178/:184/:514/:559`) + cohort: `ShardedLiveSafety.hpp:204` (FORCE-CLOSE flatten — the B-ζ epoch-flatten path!) and `:77` (orphan close) + main.cpp legacy sites — all route through #6. (trace #2 + merge #3 + critic #3.)
- **S-11 [HIGH·structural] GUI cfg money WRITE path bypasses the dispatcher net** (completeness critic — uncovered by all 11). Legacy `field_defs[]` money rows (`SettingsPanel.hpp:292/:297`, `(float)atof` `:899`, `snprintf(float)` `:1043-1049`) + `per_core_fields[]` money rows (`:559-599/:1646-1654/:1317-1324`) own float copies — nothing red-builds; post-flip an operator edit silently float-quantizes exact-decimal money cfg, then drift-compare mismatches. **Disposition:** migrate residual money rows to the typed path / route saves through `cfg_save_field<T>`; extend the B3 coverage tool; decimal render branch must not replicate the float32 write-back shape.
- **S-12 [HIGH·structural] Boot capital-allocation money math in double — LIVE+BACKTEST twins** (completeness critic). `Run.hpp:888-910→:950` `FPN_ToDouble(starting_balance)` → double `(total*risk)/num_cores` → `FromDouble` → persisted `allocated_balance`; twin `BacktestSharded.hpp:263-266→:287`. In NO plan inventory. **Disposition:** designed decimal split (D-105 rounding) + oracle row + twin parity (code carries "bytewise-identical" discipline).
- **S-13 [HIGH·process] Execution-sequence layer missing** (readiness). **Disposition:** add "Ship-B execution sequence" section — **Step 0 = the `FPN_*` op-family naming decision** (gates every new fn name) → phases binding sidecar #3→#7 order + B1-B6 + casts + dispatcher branches + FP64 absorb + persistence/versioning + close ritual, with first-concrete-move + per-step files + `pre-<tag>` rollback anchor; + M4 retrain checklist artifact (≈6 steps); + Check-F un-bypass 4-step procedure + acceptance row; + re-stamp the stale design sidecar (still says "DRAFT step 5"; #1/#2 never marked SHIPPED).

### MED — every row carries a disposition (none dropped)

- **S-14 [MED→HIGH·mechanical] PARITY-037: KIND_DOUBLE_PCT defaults percent-form vs fraction-form reads** (`CfgFieldRegistry.hpp:674-675` `DBL(0.075/0.100)` vs `ControllerConfig.hpp:1527-1528` fractions; `cfg_assign_field` does no ÷100; masked only by manual-init ordering; ARMED by the planned manual-init deletion; tp/sl/risk_pct rows share the 100× shape). **Disposition:** normalize the PCT cohort units while writing the decimal branches; PARITY-037 ledgered by the parity agent.
- **S-15 [MED·structural·wide] Dispatcher scope = 8-9 entry points, not "3 + drift"** — incl. `cfg_save_field` already value-mutating money at GUI save (PCT `v×100`→`%.2f`: 0.00075→"0.07"→reload 0.0007) and `%.17g` save→load not round-tripping 10⁸-scaled ints past 2⁵³. **Disposition:** paste the full inventory into B3 (per `feedback_paste_tool_output_dont_summarize`); exact-string decimal save branch; per-fn coverage-tool assertions.
- **S-16 [MED·structural] "Floor/Ceil/Round radix-agnostic reuse" claim false at HEAD** — generics are dead sign-magnitude `.w[]`/`.sign` relics, uninstantiable on the 16B core. **Disposition:** per-op radix-disposition table in #1 (agnostic-on-`.v` / radix-forked+`always_false` / binary-only `static_assert(RADIX==2)`); decimal rounding only via #4's `(q,r)`.
- **S-17 [MED·design] Flag-loud overflow mechanism unspecified** (D-147 Ship-B posture). **Disposition:** pin at the D-93 design audit — keep branchless `of_mask` saturate + OR into a **sticky flag word** (sister: `FailureModeRegistry` `__atomic_fetch_or`), boundary-checked once per cycle; acceptance row. *Spec: branchless-dispatch-discipline (H20 matrix).*
- **S-18 [MED·structural] Decimal cfg parse rules + MANUAL_PARSER bypass.** Pin exact digit-shift pct (no ÷100-in-double), decimal clamp; `fee_rate_maker/taker` carry MANUAL_PARSER and bypass the dispatchers entirely — enumerate those rows; cfg-file→stored round-trip over ALL money rows joins the D-100 gate.
- **S-19 [MED·structural] Implementation reuse slate (merge-scan + dod F2):** ONE rate-agnostic `Fee_Apply(notional, rate)` chokepoint (mode baked: half-even default; round-UP ONLY inside `Money_FeeCompute`; per-site rate-select stays; `Fee_Compute` refit as wrapper; BNB excluded as rate-transform) + extract `umul_128x128_256` FIRST (two inline copies exist; #3 must not author a third; golden-guarded `fp2_mul` refactor; FP64 copy dies at absorb) + #5 shares the LIVE `FPN_FromString<64>` specialization front-end (`:1579-1607`), forks only the scale tail + delete dead `FPN_FromFP64/ToFP64` (`:466-489`, zero call sites) at absorb + one `(q,r)` round core, five consumers.
- **S-20 [MED·structural] Remaining accounting dispositions:** ExitBuffer LIVE bounded-mismatch documented (M3 note); runtime reconcile balance overwrite (`OrderManager.hpp:1424-1431`) added to D-103; `Reconcile.hpp:63` double commission field retype explicit in B6; numeric tolerance stated for the paper-vs-live fee differential (MED-4); trade-id dedup `(uint64_t)extract_double` 2⁵³ note (LOW, #5 cohort opportunistic).
- **S-21 [MED·hft acceptance set]:** decimal compare/min/max/IsZero = bare-`.v` bodies pinned + A.5-style A/B codegen oracle on `ExecutionCore_Tick` as acceptance (every decimal op body is NEW — `FixedPoint<RADIX,FRAC>` is declaration-only today); casts branchless (mask-select saturate; `__builtin_expect`-rare flag) + dec→bin rounding rule single-source; `Tick`/`GateParameters`/`ExecutionCore` `<F>` template-param disposition named before the re-type.

### LOW — ledgered here; fix-in-ship where adjacent

- **S-22 [LOW cluster]:** hoist per-tick `FPN_FromDouble<F>(1.0)` (`Async.hpp:261`) · GUI-drag TP/SL site (`Async.hpp:246-247`) added to D-103 list · `ExecutionCore.hpp:64-91` comments still 24B-era + `:176` sizeof operand re-point (7f1704e cohort miss) · plan blast-radius row Fingerprint cite: raw cfg SHA is `:180` (parity), my v0.3 edit set `:150` — fix back · freeze money-row ORDER + one-shot emitted-key-sequence diff before golden/HMAC regen · FP64 absorb: remove `FixedPointN.hpp:1246` include FIRST, red-build proves call-site-dead, then delete · depth book levels pinned BINARY explicitly (DepthRecorder emit+replay ambiguity) · bandit/pnl_feeder money→signal casts named in D-103 (CEL `:1681/:1692-1695/:1748-1760`) · #4 helper named at Step 0 + coarse per-phase effort tags · `BacktestSharded.hpp:84-85` is FromDouble-from-HistoricalTick, not string parse (plan wording).

## 5. Refuted / closed

- Quantize-direction oversell (quorum-1 #4): **REFUTED** — `decimal_oracle.py` pins floor-to-step; half-even is the mul-reduce only.
- Layer 5b: GREEN (value-format-tolerant invariants; reshape rides the epoch). Fingerprint zero-init (PARITY-035) holds. Registry-fit: no-new-registry-at-#11 CONFIRMED. Class 27: CLEAN (B1 carry IS decision-time binding). Atomicity: PASS. Seqlock/Tick/cache layout: PASS (16B→16B layout-neutral; compile-probed).

## 6. Recommended path

**(X) Amendment session before any code** (blindspot's recommendation, concurred): fold S-1..S-22 into the plan body + design sidecar (new fn #7; multi-fill design; flag-loud design; execution-sequence section; R3-B/event-log-version rows; oracle extensions). Then: D-93 design audits on #4/#5/#6/#7 + the B1/multi-fill design, light re-fire on the amended deltas, THEN code. One DECISION for Caramel to ratify: **S-7 lock B4 = egress arm** (auto-pick rationale: only latency-compatible option; per-tick alternative violates H8).

**M7 escalation check:** none — no codified-rule recurrence; S-4's guard-class note → harvest at close.
