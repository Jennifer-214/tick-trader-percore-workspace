# v5.15.5.A.7 handoff prompt — cfg-drift Tier 1 promotion + drift-check generalization + .A umbrella tag

**Created:** 2026-05-12
**Target ship:** v5.15.5.A.7 — Tier 1 cfg-drift promotion for barrier-blend cfg fields, generalized via a NEW `FOREACH_CFG_DRIFT_CHECK` X-macro registry that ALSO absorbs the 5 existing manual drift checks at `ML_Headers/ModelInference.hpp:1450-1599`, plus the v5.15.5.A umbrella commit + tag.
**Branch:** `feat/v5.15-live-readiness`
**Pre-tag rollback anchor:** commit `37adcc3` (v5.15.5.A.6 — per-horizon barrier dispatch observability surfaces)
**Latest sprint tag:** `v5.15.4` (sprint umbrella close); `pre-v5.15.5` rollback anchor exists; `pre-v5.15.5.A.2.d` mid-A rollback anchor exists
**Plan file:** `plans/v5.15-live-readiness/subplans/2026-05-12-v5.15.5-per-horizon-tp-sl-serving.md`
**Sprint MASTER:** `plans/v5.15-live-readiness/MASTER.md`
**Predecessor postmortem:** `plans/v5.15-live-readiness/postmortems/2026-05-12-v5.15-umbrella-postmortem.md` (sprint-close umbrella for v5.15.0 through v5.15.4)

---

## Paste this prompt into a fresh Claude Code session to start v5.15.5.A.7

```
I'm picking up v5.15.5.A.7 (cfg-drift Tier 1 promotion via NEW FOREACH_CFG_DRIFT_CHECK
X-macro registry + .A umbrella commit/tag) for the v5.15-live-readiness sprint.
This is a fresh context window after compaction; do NOT trust any prior-session
memory — verify everything against current code (current Version.hpp says 5.15.4
but engine HEAD is mid-sprint at .A.6).

---

## Step 0 — orient + verify state (BEFORE planning anything)

1. Run in parallel:
   - `cat /home/caramel/code/FoxML_Trader_v2/Version.hpp` — current "5.15.4" (will bump to 5.15.5 at .A umbrella; .A.7 itself does NOT bump)
   - `cd /home/caramel/code/FoxML_Trader_v2 && git log --oneline -15` — confirm 8 .A.* commits (0ff90aa, 381ef10, 0635a08, 969539f, d2a6384, d2a20ff, 7ccbd14, 37adcc3) + d61478f UX commit + 56435af v5.15 umbrella
   - `cd /home/caramel/code/FoxML_Trader_v2 && git tag --sort=-creatordate | head -10` — confirm `pre-v5.15.5.A.2.d`, `v5.15.4`, `pre-v5.15.5` exist (mid-A rollback anchors)
   - `cd /home/caramel/code/FoxML_Trader_v2 && git status` — confirm clean tree (1 untracked: `context_cont.md`)
   - `cd /home/caramel/code/FoxML_Trader_v2 && git rev-parse --abbrev-ref HEAD` — confirm `feat/v5.15-live-readiness`

2. Read these in parallel (load context):
   - `/home/caramel/code/FoxML_Trader_v2/CLAUDE.md` (engine repo project instructions; 27 items + load-bearing item 28 added mid-A for latency-vs-cache decision framework)
   - `/home/caramel/code/FoxML_Trader_v2/CLAUDE.local.md` (private overlay; design philosophy + 9 going-forward rules)
   - `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md` (auto-memory index; 19 entries)
   - `plans/v5.15-live-readiness/MASTER.md` (sprint master plan)
   - `plans/v5.15-live-readiness/subplans/2026-05-12-v5.15.5-per-horizon-tp-sl-serving.md` (THE plan for this ship; 944 lines)
   - `plans/v5.15-live-readiness/postmortems/2026-05-12-v5.15-umbrella-postmortem.md` (sprint umbrella; baseline for what discipline shipped already)

3. Read BEFORE WRITING CODE (per CLAUDE.local.md required reading):
   - `DOCS/STRATEGY_AND_CODING_RULES.md` (11 strict invariants)
   - `plans/_cross-cutting/2026-05-06-latency-path-discipline.md` (7 latency-path rules)
   - `DOCS/LATENCY_OPTIMIZATION_AUDIT.md` (Gemini sweep; touch ML-inference + parsing parts since .A.7 extends parser)

---

## Step 1 — re-verify .A's landed state against current code (HEAD = 37adcc3)

The plan was drafted 2026-05-12 morning. The codebase moved through 8 .A.* sub-commits
since. Plan claims may have drifted at the integration points. Specifically verify:

1. **Plan section "5-mode FOREACH_BARRIER_BLEND_MODE registry exists":**
   - `cat /home/caramel/code/FoxML_Trader_v2/ML_Headers/BarrierBlendModeRegistry.hpp`
   - Confirm MODE_F_LEGACY / BLEND_DRIVES / DOMINANT_DRIVES / SHADOW_ACTIVE bit constants
   - Confirm MODE_FLAGS[MODE_BARRIER_BLEND_COUNT] lookup table at file end
   - 5 modes total: LEGACY, BLEND, DOMINANT, BOTH_BLEND_DRIVES, BOTH_DOMINANT_DRIVES

2. **Plan section "Phase A per_arm_barriers AoS struct{tp,sl}[8] = 64B = 1 cache line":**
   - `grep -n "per_arm_barriers\|PerArmBarriers" /home/caramel/code/FoxML_Trader_v2/ML_Headers/CoreModelZoo.hpp | head -20`
   - Confirm `PerArmBarriers per_arm_barriers[ENSEMBLE_HORIZON_MAX]` in EnsembleModelZoo<F>
   - Confirm AoS struct shape (tp + sl floats, alignas(64))
   - Confirm Init zeros it + LoadFromCfg populates from buy_signal[i].label_tp_pct/sl_pct via STAMP_HAS predicate

3. **Plan section "Phase B mode-dispatch via MODE_FLAGS[]":**
   - `grep -n "MODE_FLAGS\|blend_drives\|dominant_drives" /home/caramel/code/FoxML_Trader_v2/Strategies/StrategyParameters.hpp | head -20`
   - Confirm dispatch at the cfg-fallback site post-weights_buf-finalization
   - Confirm `ml_cfg_flags & MASK_ML_CFG_PER_HORIZON_BARRIER_BLEND` master gate
   - Confirm fallback to `config->ml_tp_pct` / `ml_sl_pct` on cfg-disabled or no-blend-ready path

4. **Plan section "Observability surfaces (.A.6)":**
   - `grep -n "ml_last_buy_dominant_horizon\|ml_last_barrier_mode_used\|ml_barrier_shadow_event_count" /home/caramel/code/FoxML_Trader_v2/DataStream/EngineTUI.hpp /home/caramel/code/FoxML_Trader_v2/CoreFrameworks/ShardedSnapshot.hpp`
   - Confirm 3 new PerCoreSnap fields published
   - Confirm MLStatusPanel `barrier:` line rendering

5. **Plan section "ml_cfg_flags cohort migration":**
   - `grep -n "FOREACH_ML_CFG_FLAG\|PER_HORIZON_BARRIER_BLEND" /home/caramel/code/FoxML_Trader_v2/ML_Headers/MlCfgFlagRegistry.hpp`
   - Confirm `X(PER_HORIZON_BARRIER_BLEND, per_horizon_barrier_blend, ...)` entry exists

6. **Plan section "Per-core override":**
   - `grep -n "PER_CORE_OVERRIDE_INT_FIELDS\|barrier_blend_mode" /home/caramel/code/FoxML_Trader_v2/CoreFrameworks/ControllerConfig.hpp | head -10`
   - Confirm `INT(barrier_blend_mode)` entry in PER_CORE_OVERRIDE_INT_FIELDS

---

## Step 2 — pre-coding audit gate (per audit-driven-pre-coding-gate.md)

**Does .A.7 qualify?** YES — it qualifies on 3 of the 5 triggers:
- ✅ Touches wire format (FOREACH_STAMP_BOUND_CFG; HMAC-chain preservation critical)
- ✅ Adds 5+ new fields (4 new drift-check entries + 4 stamp body rows + 4 ModelHandle inference_cfg_* fields)
- ✅ Picks up work from a compacted session (this prompt is a recovery handoff)

Run THESE audits in parallel via Agent tool with Explore subagents BEFORE writing
any .A.7 code:

1. `/parity-check` — focus: F7 HIGH "HMAC append-at-END" was raised in the original audit (`plans/plan_checks/parity-check-2026-05-12-v5.15.5-per-horizon-tp-sl-serving.md`). Verify it's STILL open + which file path. Verify the 4 new FOREACH_STAMP_BOUND_CFG entries will land at the END of the registry, after `trading_mode` (currently at `ML_Headers/StampBoundCfgRegistry.hpp:175-176` per plan). Verify cohort behavior for the 4 new ModelHandle fields auto-flowing via STAMP_CFG_AUTOPOPULATE.

2. `/trace-deps` — focus: find ALL 5 manual drift-check sites in `ML_Headers/ModelInference.hpp` (around lines 1450-1599 per grep done in this handoff prep). The plan summary said "ModelValidation.hpp:185-214" which was wrong — verify HEAD; the file does NOT exist; the drift checks live in `ModelInference.hpp` `verify_model_stamp`. Walk each manual check; produce migration shape into FOREACH_CFG_DRIFT_CHECK.

3. `/readiness` — full 28-check pass on the .A.7 sub-section of the plan + cold-pickup completeness for the new registry; verify cohort-audit rule (Q: is the cohort of cfg-drift checks fully migration-ready?).

4. `/merge-scan` — focus: existing manual drift checks vs the new registry. Are there OTHER manual drift checks scattered in `verify_model_stamp` body that should ALSO migrate? Are the 5 currently-identified checks complete?

5. `/dod-audit` — focus: registry tuple shape (what fields does FOREACH_CFG_DRIFT_CHECK need? type, fmt, severity, refuse-in-strict bool, message); validate against `registry-tuple-as-single-source-of-truth.md` 5-col-tuple discipline.

After all 5 reports return, synthesize convergent findings to:
`plans/plan_checks/2026-05-12-v5.15.5-A-7-fresh-audits-synthesis.md`

THEN consult Caramel BEFORE coding. Do NOT auto-proceed even if findings look
addressable (per CLAUDE.local.md `feedback_consult_on_audit_findings.md` memory).

---

## Step 3 — design check against pattern library

Required reading (DESIGN_SPECS catalog at `tick-trader-percore-workspace/DESIGN_SPECS/`):

- `README.md` (full 26-pattern catalog + "I need to..." quick-discovery section)
- `x-macro-registry-with-presence-dispatch.md` — FOREACH_CFG_DRIFT_CHECK shape;
  presence-dispatch is critical (some checks are "always-run", some are
  "skip if has_<field>=0" guarded; needs `has_*_drift` flow analysis)
- `heterogeneous-registry-pattern.md` — pick Form 1 / 2 / 3 (SCOPE COLUMN vs
  DOMAIN SPLIT vs HYBRID); cfg-drift checks may need severity scope column
- `registry-tuple-as-single-source-of-truth.md` — 5-col tuple feeds parser +
  WARN/REFUSE dispatch + Tier 1/2 doc + telemetry counter from ONE source
- `wire-format-byte-preservation-discipline.md` — HMAC chain preservation
  CRITICAL; FOREACH_STAMP_BOUND_CFG extension MUST append at END (F7 HIGH
  from parity-check audit 2026-05-12)
- `autopopulate-pattern-for-production-caller-class.md` — STAMP_CFG_AUTOPOPULATE
  auto-flows the 4 new fields into ModelHandle / ModelStampResult /
  StampInferenceCfgInputs; verify the AUTOPOPULATE companion expansion picks
  up the new rows without manual TryLoadRole edits
- `cfg-flag-eligibility-criteria.md` — Cohort audit subsection: the 4 cfg
  fields being stamp-bound are a cohort (barrier-related); apply the 5-criteria
  framework before extending the registry
- `structural-fix-preferred-decision-framework.md` — CRITICAL for .A.7: the
  5 manual drift checks at lines 1450-1599 are EXACTLY the Class 18 mirror
  shape (parallel paths drift). Per CLAUDE.md item 19 + CLAUDE.local.md
  going-forward rule 2026-05-09 — structural-fix-preferred. Generalize to
  registry NOW, not after the next drift bug.

Don't write code until matched-pattern docs are read + integration plan articulated.

---

## Step 4 — design philosophy reminders (load-bearing rules from CLAUDE.local.md + memory)

- **Full closure, no defers.** Caramel's framing 2026-05-12 mid-.A: "i dont
  wanna defer this, this is alot of TECH_DEBT being removed, we should deal
  wiht all of it" — the .A.7 generalization is the closure for the manual-
  drift-check class. Implement the registry properly; do not patch the 4 new
  fields manually as a "smaller scope" alternative.
- **Defer is last-ditch, never effort-avoidance.** Implement properly the
  first time. "Smaller-scope" recommendations have been wrong 3/3 times in
  v5.14 sprint vs Caramel's "do it right now" instinct
  (`feedback_no_defer_for_effort.md`).
- **Structural fix > direct patch for recurring bug classes.** The 5 existing
  manual drift checks ARE a Class 18 mirror — adding a 6th, 7th, 8th manually
  would extend the class. Use FOREACH_CFG_DRIFT_CHECK + helper that walks
  the registry (CLAUDE.md item 19; CLAUDE.local.md 2026-05-09 rule).
- **No MVP for plumbing/refactor work.** MVP framing is reserved for
  genuinely-new features with unknown unknowns
  (`feedback_no_mvp_for_plumbing_only_for_unknown_unknowns.md`). Pattern-
  application work ships the full documented design.
- **Boundary-stable refactors preferred over wide cascades.** The drift-check
  generalization keeps `verify_model_stamp`'s public signature unchanged;
  swap the manual-branch body for a single helper that walks the registry.
- **Hot path UNTOUCHED.** verify_model_stamp runs at boot + hot-swap — slow
  paths only. Drift checks are NOT on the per-tick hot path. Latency budget
  for the migration is relaxed.
- **Branchless mask compute > switch on enum on slow path** (CLAUDE.md item
  18). The registry walks should use mask compute / template-bool dispatch,
  NOT switch on field name.
- **Reuse-audit before adding new code** (CLAUDE.md item 16). The existing
  `tt::stamp_parse_field<T>` templated helper (`ML_Headers/StampBoundModelConstRegistry.hpp:101+`)
  is the canonical type-dispatch helper. FOREACH_CFG_DRIFT_CHECK comparison
  body should reuse it for the value-compare logic.
- **Latency-additions get tracked** (CLAUDE.md item 17). The registry walk
  ADDS slow-path cycles vs the previous manual chain. Log in
  `DOCS/HOT_PATH_CHANGELOG.md` with cost estimate.
- **Bump Version.hpp on every ship.** The .A umbrella commit (NOT .A.7
  itself) bumps to v5.15.5. .A.7 standalone is a sub-commit — version
  stays 5.15.4 until the .A umbrella tag lands (`feedback_bump_version_per_ship.md`).
- **Don't use AskUserQuestion modal boxes.** Present options inline as text;
  Caramel wants full conversation scrollable (`feedback_no_question_boxes.md`).
- **Evaluate options on robustness + latency + design philosophy, NOT time.**
  Time is essentially never the deciding factor
  (`feedback_evaluate_options_on_robustness_latency_design_not_time.md`).
- **After pre-coding audits, ALWAYS consult before coding.** Present findings
  + list potential fixes + iterate with Caramel. Do NOT auto-proceed
  (`feedback_consult_on_audit_findings.md`).
- **Address Caramel as Caramel / she / her** in conversation. Persisted docs
  can keep "operator" for general-collaborator audiences
  (`feedback_address_user_as_caramel.md`).
- **Compaction degrades — verify handoff claims against current code**
  (`feedback_compaction_degrades_treat_handoffs_as_hints.md`). The summary
  in this handoff says "ModelValidation.hpp:185-214" but that file does
  NOT exist; drift checks live in `ML_Headers/ModelInference.hpp:1450-1599`.
  This handoff is a HINT — verify the file:line refs before coding.

---

## Step 5 — TECH_DEBT items in surface area

`/readiness` Check 25 scans `DOCS/TECH_DEBT.md` for surface overlap. Pre-scanned
candidates that touch .A.7's surface (`verify_model_stamp`, FOREACH_STAMP_BOUND_CFG,
ModelHandle, cfg-drift checks):

- **TECH_DEBT-001** (OPEN): Replace `tools/stamp_model.sh` bash CLI with thin C++
  wrapper binary
  - Surface: `tools/stamp_model.sh` ; the cfg fields being stamp-bound in .A.7
    auto-flow through STAMP_CFG_AUTOPOPULATE so bash CLI also drifts — verify
    the bash CLI is still in the "drift class" or whether v5.15.0 ModelHandle
    migration closed it indirectly
  - Decide: do NOT absorb (separate concern; defer); confirm the bash CLI does
    NOT need to know about the new cfg fields (it's used for manual stamp
    creation only)

- **TECH_DEBT-027** (OPEN, MEDIUM): Locale pinning gap in `Bandit_SaveJSON`
  - Surface: `ML_Headers/BanditLearning.hpp:369-435` (Bandit_SaveJSON);
    plan section "Phase D Step 0" calls for shadow JSON LC_NUMERIC=C pin
  - Decide: .A.7 plan mentions "locale pin (deferred — no shadow JSON yet)" —
    shadow JSON writer is DEFERRED to v5.15.6 since the ring writes haven't
    landed yet. Confirm with Caramel: defer to v5.15.6 OR opportunistically
    close TECH_DEBT-027 by pinning Bandit_SaveJSON now (mirror precedent).

- **TECH_DEBT-037** (OPEN, LOW): Cfg-derived inference_cfg_* fields live in
  FOREACH_STAMP_BOUND_MODEL_CONST, not FOREACH_STAMP_BOUND_CFG (taxonomy drift)
  - Surface: `ML_Headers/StampHelper.hpp:158-187`; registry-tuple taxonomy
    split between FOREACH_STAMP_BOUND_CFG and FOREACH_STAMP_BOUND_MODEL_CONST
  - Decide: .A.7 adds 4 new entries to FOREACH_STAMP_BOUND_CFG (NOT
    FOREACH_STAMP_BOUND_MODEL_CONST). Verify the cohort placement aligns with
    the existing field's "binding type" — are the new fields direct-cfg-mirror
    (FOREACH_STAMP_BOUND_CFG) or derived-from-cfg (FOREACH_STAMP_BOUND_MODEL_CONST)?
    Caramel's call.

- **TECH_DEBT-038** (OPEN; just added .A.1): FOREACH_BITMAP_WIDTH X-macro registry
  - Surface: `MemHeaders/BitmapMacros.hpp` (BITMAP_BIT/POPCOUNT/FIRST families)
  - Decide: NOT in .A.7 scope (per Caramel mid-.A "i was doing that and got
    sidetracked, this is more important though, and we should document where
    that left off"). Defer to v5.15.6 or a future sprint.

Run `/readiness` Check 25 (TECH_DEBT scan) explicitly. Any new entries created
during .A.7 audit MUST be auto-written per CLAUDE.local.md auto-write contract.

---

## Step 6 — operator collaboration norms

- Address Caramel as Caramel / she / her. Not "operator" in conversation.
- Don't use AskUserQuestion modal boxes. Present options inline as text.
- Evaluate options on robustness + latency + design philosophy, NOT time.
- When pre-coding checks complete, present findings + fixes + iterate BEFORE coding.
- Suggest mid-sprint audit when downstream sub-ships impact a new surface
  (per CLAUDE.local.md 2026-05-10 going-forward rule). Wait for greenlight.
- Auto-write TECH_DEBT entries for any deferred items (auto-write contract).
- Auto-write PARITY_ISSUES entries for any new parity findings.
- Sync workspace at end of session via `/sync-workspace`.

---

## Step 7 — filesystem conventions

- Workspace path: `/home/caramel/code/tick-trader-percore-workspace`
- Engine repo: `/home/caramel/code/FoxML_Trader_v2`
- Plans live in workspace; symlinked from engine `plans/` → workspace `plans/`.
- Sprint plans: `workspace/plans/v5.15-live-readiness/{MASTER.md, subplans/, plan_checks/, postmortems/, handoffs/}`
- DESIGN_SPECS catalog: `workspace/DESIGN_SPECS/` (26 patterns + README; mid-A
  added: `cache-layout-discipline-for-hot-side-structs.md`,
  `per-horizon-barrier-blending-with-shadow-mode.md`,
  `latency-vs-cache-decision-framework.md`,
  `enum-mode-flags-bitmap-lookup-pattern.md`)
- Skill outputs go to `plans/plan_checks/<skill>-<YYYY-MM-DD>-<scope>.md`
  (top-level plans/plan_checks/, NOT sprint-dir/plan_checks/ — verify; the
  sprint-dir/plan_checks/ does NOT currently exist)
- TECH_DEBT auto-write: `DOCS/TECH_DEBT.md` (symlinked from workspace)
- PARITY_ISSUES auto-write: `DOCS/PARITY_ISSUES.md` (symlinked from workspace)
- HOT_PATH_CHANGELOG: `DOCS/HOT_PATH_CHANGELOG.md` (symlinked from workspace)

**DOCS/ symlinks editing convention** (post-v5.11.43 migration; surfaced as
Surprise 7 in v5.14.10 postmortem; recurred mid-.A): many `DOCS/*.md` files
in the engine repo are PER-FILE SYMLINKS to workspace. The `Edit` tool REFUSES
to write through symlinks. ALWAYS check `readlink -f <path>` before editing
a `DOCS/*.md` file; if it resolves to a workspace path, edit via the
workspace path directly. Same applies to `CLAUDE.md`, `plans/*` (also
symlinks). Memory `project_engine_clauder_md_is_symlink.md` documents this.

Engine-tracked exceptions (NOT symlinked): QUICKSTART, OPERATOR_DEPLOYMENT,
CONFIGURATION, ML_USAGE, ML_TRAINING, CONTRIBUTING, LATENCY_PROFILING.

---

## Step 8 — .A.7 scope (the actual coding work)

### Pre-state
- 5 existing manual cfg-drift checks at `ML_Headers/ModelInference.hpp:1450-1599`
  (around `verify_model_stamp` body; verify exact line range against HEAD).
  These are: feature_hash, label_set_hash, feature_mask, build_flags_hash,
  scaler_binding (per `v5.15.1` postmortem section).
- 0 existing automated migration to a registry-driven helper.
- 4 NEW cfg fields need stamp-binding + Tier 1 drift check:
  - `barrier_blend_mode` (uint8_t / enum; default 0=LEGACY)
  - `per_horizon_barrier_blend` (bool; in `ml_cfg_flags` bitmap)
  - `ml_tp_pct` (FPN<F>; pre-existing cfg field, currently NOT stamp-bound)
  - `ml_sl_pct` (FPN<F>; pre-existing cfg field, currently NOT stamp-bound)

### Target state
- New `MemHeaders/CfgDriftCheckRegistry.hpp` (or co-located in
  StampBoundCfgRegistry depending on the design discussion) with
  `FOREACH_CFG_DRIFT_CHECK(X)` X-macro registry.
- Each entry tuple shape (proposed; refine per /dod-audit):
  `X(name, type, fmt, tier, severity, message)` where:
  - `name` = field identifier (e.g., `feature_hash`, `barrier_blend_mode`)
  - `type` = C++ type (e.g., `uint64_t`, `int`, `FPN<F>`)
  - `fmt` = printf format string (e.g., `"%016lx"`, `"%d"`, `"%g"`)
  - `tier` = 1 (refuse in strict) or 2 (WARN always)
  - `severity` = HIGH / MEDIUM / LOW for telemetry
  - `message` = operator-facing string template
- Helper `tt::Cfg_Drift_Check_Walk<F>(handle, cfg, strict_mode, &counters)`
  that walks the registry; replaces the 5 manual branches inline.
- 4 new entries appended at END of FOREACH_STAMP_BOUND_CFG per HMAC
  preservation (F7 HIGH parity finding). Use existing `STAMP_CFG_AUTOPOPULATE`
  to auto-flow `inference_cfg_<name>` fields into ModelHandle.
- Tests in `tests/controller_test_stamps.cpp` (or whichever file the v5.15.0
  stamp tests landed in; verify): 4 new per-mode dispatch tests + 4 new drift
  cases (Tier 1 strict-refuse, Tier 1 strict-ack-loose-load, Tier 2 WARN-load,
  ack-skip path).

### Sub-steps (proposed; finalize after /dod-audit feedback)

1. **Step 1 — locate exact drift-check sites** in `verify_model_stamp`:
   `grep -nE "drift|tier" /home/caramel/code/FoxML_Trader_v2/ML_Headers/ModelInference.hpp | head -40`
   The handoff prep found refs at lines 1450 / 1482 / 1488 / 1501 / 1507 / 1599
   — verify each is a Tier 1 / Tier 2 drift check; document the 5 sites
   with exact line ranges.

2. **Step 2 — design FOREACH_CFG_DRIFT_CHECK tuple shape** via /dod-audit
   feedback + Caramel consult. The 5 existing checks differ in shape (some
   compare uint64_t hash, some compare bitmask, some compare FPN value).
   Decide: single tuple shape with type-dispatch (mirror `tt::stamp_parse_field<T>`)
   OR multiple FOREACH_*_DRIFT_CHECK registries by check-type.

3. **Step 3 — write CfgDriftCheckRegistry.hpp** with the 9 entries (5 existing
   migrated + 4 new). Include presence-dispatch column for the conditional
   guards on `has_*_drift` (some checks only fire when the corresponding
   has_flag is set).

4. **Step 4 — extract `tt::Cfg_Drift_Check_Walk<F>` helper** that walks the
   registry; replace the 5 manual branches in `verify_model_stamp` with a
   single call. Returns counter values + sets failure_flags bits.

5. **Step 5 — append 4 new FOREACH_STAMP_BOUND_CFG entries** at END (after
   `trading_mode` row, per F7 HIGH HMAC preservation). Use `emit_when=1`
   + `DIRECT_FIELD` source for all 4 (mirror trading_mode precedent).

6. **Step 6 — verify ModelHandle picks up `inference_cfg_barrier_blend_mode`
   + `inference_cfg_per_horizon_barrier_blend` + `inference_cfg_ml_tp_pct`
   + `inference_cfg_ml_sl_pct`** via STAMP_CFG_AUTOPOPULATE auto-flow. No
   manual TryLoadRole edits should be required (compile-time enforcement
   per CLAUDE.md item 21).

7. **Step 7 — population at TryLoadRole** for the new fields auto-flows
   via STAMP_CFG_AUTOPOPULATE — verify it lands at the canonical site
   (look up via `grep -n "STAMP_CFG_AUTOPOPULATE" /home/caramel/code/FoxML_Trader_v2/ML_Headers/CoreModelZoo.hpp`).

8. **Step 8 — tests** — 4 new tests in `controller_test_stamps.cpp`:
   - test_barrier_blend_mode_tier1_strict_refuse
   - test_barrier_blend_mode_tier1_loose_warn
   - test_per_horizon_barrier_blend_drift_strict
   - test_ml_tp_pct_drift_strict_with_ack

9. **Step 9 — locale pin for shadow JSON** — DEFERRED (no shadow JSON ring
   writes yet; ring writes themselves deferred to v5.15.6 since the
   shadow telemetry is .A scope but the JSON flush is a separate concern
   per plan). Confirm with Caramel: skip OR opportunistic TECH_DEBT-027
   close via Bandit_SaveJSON precedent.

### .A umbrella commit (sub-step .A.8 effectively)

After .A.7 lands (commit individually as `v5.15.5.A.7 — cfg-drift Tier 1
promotion via FOREACH_CFG_DRIFT_CHECK + 4 new entries`):

1. Bump `Version.hpp` to `5.15.5` (PATCH 5, not 4):
   - `ENGINE_VERSION_PATCH 5`
   - `ENGINE_VERSION_STRING "5.15.5"`
2. Write sprint-close postmortem at
   `plans/v5.15-live-readiness/postmortems/2026-05-12-v5.15.5-A-postmortem.md`
   covering all 7 sub-commits + Caramel's iteration framing + design pivots
3. Write CHANGELOG entry at `DOCS/CHANGELOG.md` for v5.15.5
4. Auto-write FEATURE_LOOKUP.md entry (per CLAUDE.local.md 2026-05-12
   going-forward rule) at `tick-trader-percore-workspace/FEATURE_LOOKUP.md`
5. Auto-write decoupling-endgoal roadmap breadcrumb at
   `plans/_future/2026-05-12-decoupling-endgoal-roadmap.md` (per
   CLAUDE.local.md 2026-05-12 rule; barriers-in-stamp-body = POSITIVE
   breadcrumb since each model carries its own barriers vs cfg-driven)
6. Commit v5.15.5.A umbrella + tag `v5.15.5.A`
7. Push branch to origin

---

## Step 9 — sprint-close verification gate (before umbrella tag)

Before declaring v5.15.5.A shipped:
- [ ] All tests pass (currently ~3006 + ~6 new = ~3012; expect 4 new in .A.7
      + ~2 more from .A.3 BanditDisplayMeta etc. already landed)
- [ ] `/parity-check` GREEN at .A umbrella (F7 HIGH closed; no new HIGH/MEDIUM
      findings)
- [ ] `/merge-scan` GREEN (no missed reuse; FOREACH_CFG_DRIFT_CHECK absorbed
      all 5 existing checks)
- [ ] `/latency-track` entry in `DOCS/HOT_PATH_CHANGELOG.md` for the registry
      walk addition to `verify_model_stamp` (boot + hot-swap path, not hot
      tick path)
- [ ] `/bug-check` CLEAN (no new recurring-bug-class instances; verify Class
      18 mirror is closed by the registry generalization)
- [ ] Replay determinism test passes (.A.4 had per-arm RNG via splitmix64
      already pinned)
- [ ] Cfg.example doc updated for `barrier_blend_mode` + `per_horizon_barrier_blend`
      + per-core override `core_N_barrier_blend_mode`
- [ ] Version.hpp bumped to 5.15.5 in the same commit as the `v5.15.5.A` tag
- [ ] HOT_PATH_CHANGELOG.md confirms +0 ns hot-path delta (only slow-path +
      boot-path additions)
- [ ] FEATURE_LOOKUP.md entry added for "Per-horizon TP/SL serving"
- [ ] PARITY_ISSUES.md PARITY-024 entry marked CLOSED (was opened by
      /parity-check 2026-05-12 for the cfg-drift Tier 1 promotion)
- [ ] CHANGELOG.md v5.15.5.A entry added
- [ ] Decoupling roadmap breadcrumb written (POSITIVE: stamp-body barriers
      reduce engine-cfg coupling)

---

## Closing reminder

v5.15.5.A is the per-horizon TP/SL serving sub-ship — the load-bearing
parity surface closing the train-serve barrier-parity gap. Each multi-horizon
model is now served against its OWN trained barriers, not against
operator-set cfg defaults. This is the prerequisite for credible multi-
horizon ensemble support in production (Caramel 2026-05-12: "required for
multi-horizon/multi-model support").

.A.7 closes the structural-fix loop: instead of patching 4 cfg-drift checks
inline (Class 18 mirror extension), generalize the existing 5 manual checks
via FOREACH_CFG_DRIFT_CHECK. Adding the 6th, 7th, 8th, 9th drift check
becomes 1 row each — auto-flowing through STAMP_CFG_AUTOPOPULATE for the
ModelHandle side, auto-flowing through the registry walk for the
verify_model_stamp side. The next drift bug becomes structurally impossible
to introduce by extending the registry; only logic-bugs inside a single
helper remain.

If you find yourself writing complex plumbing, stop — check
`DESIGN_SPECS/README.md` "I need to..." section. There's almost certainly
a pattern that applies, and structural-fix-preferred discipline is the
load-bearing rule for this entire sprint.

Good luck. Caramel will iterate with you on findings before coding.
```

---

## Notes for future-Claude reading this handoff doc (not part of the copy-paste prompt)

- The 5 existing manual drift checks at `ML_Headers/ModelInference.hpp:1450-1599` were identified by grep during this handoff prep. They are around the following anchor lines (verify exact ranges; lines shift): 1450 (general drift comment), 1459 (pre-v5.8.1a feature drift WARN), 1482 (label drift WARN with `"(pre-v5.10.0d) — label drift NOT verified"`), 1488 (label set drift refuse), 1501 (feature-mask drift comment), 1507 (per-core feature subset drift refuse), 1599 (catch-all drift comment). The "5 checks" mentioned in this prompt's summary may map to 5 distinct surfaces; .A.7's first audit step verifies via /trace-deps.
- `ML_Headers/ModelValidation.hpp` does NOT exist as a file. The compaction summary erroneously said the drift checks live there; they actually live in `ModelInference.hpp` `verify_model_stamp` body. This is a confirmed example of "compaction degrades — verify against current code" (memory `feedback_compaction_degrades_treat_handoffs_as_hints.md`).
- DESIGN_SPECS catalog grew from 16 (mid-v5.14.10) to 26 (mid-v5.15.5.A) — verify the README count; new docs added mid-A include cache-layout-discipline + per-horizon-barrier-blending-with-shadow-mode + latency-vs-cache-decision-framework + enum-mode-flags-bitmap-lookup-pattern.
- CLAUDE.md grew from 27 items to 28 mid-A (item 28 = latency-vs-cache decision framework: cache miss = ~100ns = 75-100× cycle cost; branchless when mispredict > N/16%). Verify the count in HEAD before quoting numbers.
- The plan file itself (subplans/2026-05-12-v5.15.5-per-horizon-tp-sl-serving.md) is 944 lines; long. /readiness on the plan should consume the audit cost as the budget.
- The sprint umbrella is `v5.15.4` (closed); v5.15.5 is the extension sub-ship to the closed sprint. The `v5.15.5.A` is the umbrella tag for ALL of A.1 through A.7; .A.7 is the last sub-commit before the umbrella tag.
- The `pre-v5.15.5.A.2.d` git tag was a mid-A rollback anchor created before the Hot/Warm/Cold cluster reorg (which was the highest-risk single commit in .A). If .A.7 finds an issue requiring rollback past .A.6, the available rollback points are: `37adcc3` (just before .A.7), `pre-v5.15.5.A.2.d` (before the cluster reorg), `pre-v5.15.5` (before .A started; = v5.15.4), `v5.15.4` (sprint umbrella). Prefer the latest viable anchor; cluster reorg should remain intact since it touched cache-layout discipline patterns now field-tested.

---

## Quick links

- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- This ship's plan: `plans/v5.15-live-readiness/subplans/2026-05-12-v5.15.5-per-horizon-tp-sl-serving.md`
- Latest postmortem: `plans/v5.15-live-readiness/postmortems/2026-05-12-v5.15-umbrella-postmortem.md`
- DESIGN_SPECS catalog: `../tick-trader-percore-workspace/DESIGN_SPECS/README.md`
- Latency rules: `plans/_cross-cutting/2026-05-06-latency-path-discipline.md`
- Coding invariants: `DOCS/STRATEGY_AND_CODING_RULES.md`
- HMAC byte preservation: `../tick-trader-percore-workspace/DESIGN_SPECS/wire-format-byte-preservation-discipline.md`
- Structural fix decision: `../tick-trader-percore-workspace/DESIGN_SPECS/structural-fix-preferred-decision-framework.md`
- Audit-driven pre-coding gate: `../tick-trader-percore-workspace/DESIGN_SPECS/audit-driven-pre-coding-gate.md`
