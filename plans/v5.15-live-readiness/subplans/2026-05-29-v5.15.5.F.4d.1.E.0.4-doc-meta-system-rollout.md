---
type: subplan
ship_tag: v5.15.5.F.4d.1.E.0.4
ship_kind: doc/meta-system rollout — TECH_DEBT-115 structural close (bring memories into the structured tag/frontmatter system + close the CP-1/WH-1 meta-error family mechanically). Doc + CI-tooling + frontmatter migration; NO engine code.
plan_version: v0.1
status: DRAFT (2026-05-29; scoped from the .E.0.x doc-system scan + D-88; ready for focused execution)
execution_order: FIRST among the pre-.E.1 foundation pieces per D-88 (numbering .E.0.4 is allocation-order, not execution-order; reconcile at ship per feedback_bump_version_per_ship)
predecessor: v5.15.5.F.4d.1.E.0.2 (apparatus); sequenced BEFORE .E.0.1 coding (D-88)
successor: v5.15.5.F.4d.1.E.0.1 (FP/replay net) → .E.0.3 (det-IO) → .E.1
parent_umbrella: v5.15.5.F.4d.1.E sub-sprint
sprint: v5.15-live-readiness
risk: LOW to engine (NO engine code); MED process (the apparatus everything leans on; ~58-file migration; design errors compound — apply the apparatus's own discipline to itself)
audit_tier: MED-RISK
hot_path: UNTOUCHED (no engine code)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (D-88 sequencing; D-84/D-87 the fix; CP-1/WH-1 evidence)
implements_ledger: TECH_DEBT-115 (institutional-memory rollout — this executes the omitted memory slice + the active-curation + status reconciliation)
findings_sidecar: plans/v5.15-live-readiness/plan_checks/E.0.1-audit-reports/2026-05-29-E.0.1-fresh-audits-synthesis.md
sister_specs:
  - meta-disciplines/doc-frontmatter-convention.md (the ### memory/*.md spec — promote Stage 2→3)
  - meta-disciplines/doc-tag-vocabulary.md (the controlled vocab — curate for active specs)
  - meta-disciplines/meta-anti-pattern-index.md (CP-1/WH-1 — the family this closes mechanically)
  - meta-disciplines/canonical-sister-extension-discipline.md (REUSE existing infra, don't reinvent)
---

# v5.15.5.F.4d.1.E.0.4 — doc/meta-system rollout (TECH_DEBT-115 structural close)

**Status:** v0.1 DRAFT (2026-05-29). **Branch:** `feat/v5.15-live-readiness`. **Sequenced FIRST** among the pre-`.E.1` foundation pieces (D-88 — the meta-foundation compounds across every ship × session × the whole horizon). **Execution deserves focus** (a ~58-file migration); this plan is the ready-scope so the focused session starts running.

## TL;DR

Bring **memories into the structured tag/frontmatter system** the rest of the doc-corpus already uses, so the **existing** `check_doc_metadata` + `/metadata-audit` machinery covers them — closing the **CP-1** (sister-link asymmetry) + **WH-1** (link-resolution drift) meta-error family **mechanically (red build), forever.** This is TECH_DEBT-115's omitted **memory slice** (Phase-2-3 cohort listed DESIGN_SPECS/SKILLs/ledgers/bug-patterns but **not `memory/`**) + the **active-spec tag curation** + a **status reconciliation**. Per D-88: do the **structural close** (high-compounding); **defer the cosmetic** (niche/speculative-spec tags) to the `/metadata-audit` cadence.

**Canonical sisters (no new infra — REUSE):** `check_doc_metadata.py` (`check_bidirectional_sisters` + broken-ref + tag-vocab already exist), `/doc-create` (type-aware scaffolding — add a `memory` type), `doc-frontmatter-convention.md` (the `### memory/*.md` spec already exists, Stage-2-DRAFT). This ship brings memories INTO that system; it builds **no bespoke memory-link parser** (the patch we rejected).

## End goal + acceptance criteria

**End goal:** memories are first-class citizens of the structured doc-system — born with structured frontmatter (template), carrying `tags:` + `sister_specs:`, covered by the same mechanical checks + `TAG_INDEX` + `/find` as DESIGN_SPECS. The CP-1/WH-1 family becomes a red build for memories; the agent's institutional memory stops drifting. Realizes the TECH_DEBT-115 vision ("a system I never have to think about again that just works on types and tags").

**Acceptance:**
- ✅ Memory template exists + `/doc-create memory` scaffolds it (NEW memories born in the system).
- ✅ The ~58 `memory/*.md` carry `tags:` + `sister_specs:` frontmatter (sister_specs populated from the existing inline `[[links]]`/`Sister:` prose); frontmatter format normalized to the convention (currently inconsistent: old `name:/type:` vs `metadata:` blocks).
- ✅ `check_doc_metadata --bidirectional` covers memories → a one-way sister-link / broken `[[link]]` / undefined tag on a memory is a **red build** (CP-1/WH-1 closed mechanically for memories). Wired into the cadence (`/close-session` + pre-commit, per #14).
- ✅ Active-spec undefined-tags resolved — the tag-vocabulary curated to register (or normalize) the tags used by ACTIVE specs (the ~245-instance backlog, ACTIVE subset only).
- ✅ `TAG_INDEX` + `/find` include memories (the query payoff).
- ✅ Load-bearing Stage-2 DRAFTs (those with a landed first-canonical) promoted; not-yet-applied left.
- ✅ TECH_DEBT-115 statuses reconciled (Phase 2-3 tooling landed; index-row + per-phase statuses updated; this ship flips the memory-slice closed).
- ✅ `doc-frontmatter-convention.md` `### memory/*.md` promoted Stage 2→3 (first canonical = this ship).
- ✅ Cosmetic backlog explicitly deferred to `/metadata-audit` cadence (niche/speculative-spec tags; not-yet-applied Stage-2 DRAFTs).
- ✅ `/capture-audit` clean; no engine code touched.

## Scope — the bounded structural close (D-88; guard-matrix-bound)

**DO (load-bearing — compounds across every ship × session):**
1. **Memory template + `/doc-create memory` type** — the creation side (so new memories are born structured, not ad-hoc — the gap that let drift recur).
2. **Migrate the ~58 memories** — add `tags:` + `sister_specs:` (from inline links); normalize the inconsistent frontmatter to the convention. Additive — preserve existing bodies/links.
3. **Check coverage** — verify/wire `check_doc_metadata --bidirectional` (+ broken-ref + tag-vocab) over memories; cadence-wire (folds in #14).
4. **Active-spec tag curation** — register/normalize the tags used by ACTIVE specs in `doc-tag-vocabulary.md`.
5. **Load-bearing stage promotions** — promote Stage-2 DRAFTs with a landed canonical.
6. **TAG_INDEX + `/find` inclusion** of memories.
7. **TECH_DEBT-115 status reconciliation** (WH-2: Phase 2-3 tooling landed; statuses stale).

**DEFER (cosmetic — `/metadata-audit` quarterly cadence; compounds nothing yet):**
- Tags on niche/speculative specs (`kernel-tls`/`dpdk`/`userspace-tls` — `.E.3+` futures, inactive).
- Promoting Stage-2 DRAFTs with no canonical application yet.

## Phases (sequence)

- **Phase A — creation-side first:** memory template + `/doc-create memory` type (+ promote the `### memory/*.md` convention Stage 2→3). So any memory created during this ship is already correct.
- **Phase B — vocab ready:** curate `doc-tag-vocabulary.md` for the ACTIVE-spec tags (so tags resolve before migration; else the migration adds tags that immediately flag undefined).
- **Phase C — migrate the ~58 memories:** add `tags:` + `sister_specs:` (from inline links) + normalize frontmatter. Batch + verify. **Triage the sister-asymmetry backlog the migration surfaces:** reciprocate load-bearing sisters; INFO (not error) for intentional forward-links to not-yet-written memories.
- **Phase D — wire the guard:** confirm `check_doc_metadata --bidirectional` covers memories (red build on asymmetry/broken-ref/undefined-tag); cadence-wire (#14). Dogfood: it should now catch the 3-memory asymmetry from `.E.0.x` (the recurrence that motivated this).
- **Phase E — index + payoff:** `TAG_INDEX` + `/find` include memories; promote load-bearing stages.
- **Phase F — reconcile + close:** TECH_DEBT-115 statuses reconciled + memory-slice flipped closed; Version bump; CHANGELOG; `/capture-audit` clean; postmortem; `/sync-workspace`.

## Tests changed (Check 45)

No engine `tests/` touched (no engine code). CI-tooling tests: extend/verify `check_doc_metadata`'s memory-coverage (a fixture memory with a one-way sister-link must RED; with reciprocal must pass; a broken `[[link]]` must RED; a forward-link to unwritten = INFO). This is the mechanical proof the family is closed.

## Honest risks

- **R1 — migration surfaces a sister-asymmetry backlog** (existing one-way sisters across 58 files). *Mitigation:* triage at Phase C — reciprocate load-bearing, INFO the forward-refs; don't block on the long tail.
- **R2 — scope-creep into all ~245 tags.** *Mitigation:* the BOUND — active-spec tags only; speculative deferred to cadence. The compounding is in the structural close, not cosmetic curation.
- **R3 — frontmatter normalization breaks the minimal `metadata: type:` format** some memories use. *Mitigation:* additive + convention-conformant; preserve existing fields; the template defines the canonical shape.
- **R4 — `[[link]]` forward-refs to not-yet-written memories** flagged as broken. *Mitigation:* the memory system explicitly allows these → INFO, not error (per WH-1 false-positive surface).
- **R5 — over-claiming the family is "closed" before the check actually covers memories.** *Mitigation:* Phase D dogfood (the check must catch the `.E.0.x` 3-memory asymmetry) is the proof, not the assertion.

## Forward promises / routing

- **To the `.E` pipeline + all future ships:** a sound, queryable, self-checking institutional memory — the meta-foundation every ship leans on (the compounding rationale, D-88).
- **To `/metadata-audit` cadence:** the cosmetic tag backlog + not-yet-applied stage promotions.
- **Closes:** TECH_DEBT-115 memory slice; CP-1/WH-1 mechanically (for memories); subsumes #14's check-wiring + #17's fix-triage.

## Cross-references

- Decisions: D-88 (sequencing) · D-84/D-85/D-87 (the fix = use the structured system, not a bespoke parser) — decision-log v2 addendum.
- Ledger: TECH_DEBT-115 (the memory slice + scan quantification: ~245 undefined-tags / ~140 sister-asymmetries / 29 stale Stage-2 / 0-of-58 structured).
- Catalog: `meta-anti-pattern-index.md` CP-1 (sister-asymmetry) + WH-1 (link-resolution) — the family this closes.
- Tasks: #14 (cadence-wiring, subsumed) · #17 (the scan, done — its fix-triage is this ship).
- Canonical-sister inspection: REUSES `check_doc_metadata.py` + `/doc-create` + `doc-frontmatter-convention.md`; introduces NO new infra (per canonical-sister-extension-discipline).

**End of `.E.0.4` doc/meta-system rollout plan v0.1 (bounded structural close; ready for focused execution; NO engine code).**
