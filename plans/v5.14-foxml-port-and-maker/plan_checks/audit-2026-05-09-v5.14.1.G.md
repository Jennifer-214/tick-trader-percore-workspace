# Audit Report — v5.14.1.G Portfolio Turnover Tracking — 2026-05-09

**Plan:** `/home/caramel/code/FoxML_Trader_v2/plans/2026-05-09-v5.14.1.G-portfolio-turnover.md`
**Verdict:** **GREEN** ✅ — ready to code
**Auditor:** /trace-deps + /readiness combined audit (Layer 2 single-subagent
execution per skill execution model)

## Quick verdict matrix

| Section | Verdict |
|---|---|
| Section A — /trace-deps symbol verification (6 checks) | ALL PASS |
| Section B — Mirror data-flow (Class 18) | PASS |
| Section C — Snapshot constraint (Class 4 avoided) | PASS |
| Section D — /readiness Checks 1-23 | 20 PASS, 1 YELLOW (docs note), 2 N/A |
| Section E — Cold-pickup (C.1-C.10) | 10/10 PASS |

## Section A — /trace-deps symbol verification

1. **CoreContext struct** at `ControllerEventLoop.hpp:171-477` — VERIFIED.
   Cleanly ends `};` at 477; appending `RollingTurnover turnover;` works
   without naming collision. NOT in fwrite path (sharded-only state).
2. **weights_buf writes** at `StrategyParameters.hpp:858-1070` — VERIFIED.
   Buy-side line 871 (bandit) + 941 (Ridge override) + exit-side v5.14.1.E
   additions all populate before weighted prediction. Populator hook reads
   weights_buf POST-blend (correct timing).
3. **PerCoreSnap struct** at `ShardedSnapshotPersist.hpp:382-415` —
   VERIFIED. `float ml_portfolio_turnover` slots cleanly next to existing
   `ml_*` fields.
4. **__builtin_popcount** — VERIFIED, 11 existing usages across codebase
   (PoolAllocator, Portfolio, main.cpp). GCC 3.4+/Clang standard builtin.
5. **EnsembleModelZoo.predict_call_count** — VERIFIED at
   `CoreModelZoo.hpp:861`. Sister-pattern reference confirmed.
6. **MLBuildContext** struct extensible — VERIFIED at
   `StrategyParameters.hpp:97-178`. Stack-created per rebuild; not
   snapshot-bound. Plan's `turnover_state` + `turnover_topk` field
   additions follow established pattern.

## Section B — Mirror data-flow check (Class 18 prevention)

Populator reads → upstream sources verified:
- `weights_buf[]` ← buy/exit blend (verified Step 2)
- `ezoo->primary_count` ← in scope (line 949)
- `mctx->turnover_state` ← NEW MLBuildContext field, populated from
  `EventLoopState.cores[i].turnover` at ML_BuildParameters call site
- `mctx->turnover_topk` ← NEW field, populated from cfg.confidence_turnover_topk

Data-flow chain: ControllerConfig → CoreContext.turnover → MLBuildContext
fields → populator. Clean pull-through.

## Section C — Snapshot constraint (Class 4 avoidance)

Plan correctly puts RollingTurnover on **CoreContext** (per-core
EventLoopState), NOT on ConfidenceScorer.

PortfolioController.hpp fwrite sites verified:
- :2045 — `ctrl->portfolio.positions`
- :2094 — `&ctrl->confidence` (sizeof ConfidenceScorer; the trap)
- **NO fwrite of EventLoopState or CoreContext.**

✅ Class 4 (snapshot save/load asymmetry) avoided. RollingTurnover is
ephemeral observability only.

## Section D — /readiness Checks 1-23

| # | Check | Verdict |
|---|---|---|
| 1 | Hot path purity | PASS — slow-path only |
| 2 | Train-serve parity | PASS — pure observability; no feature/label changes |
| 3 | Surface area | PASS — 5 files touched (≤8 threshold) |
| 4 | Pointer init / heap | PASS — memset + stack; no malloc |
| 5 | Backward compat | PASS — no SHARDED_SNAPSHOT_VERSION / MODEL_FORMAT_VERSION touched |
| 6 | Multi-threading | PASS — single-writer per-core slow-path; TUI snapshot reads only |
| 7 | Test coverage | PASS — ~11 tests specified |
| 8 | Docs + invariants | YELLOW — recommend explicit DOCS/CHANGELOG.md entry citation |
| 9 | Forward maintenance | PASS — single struct standalone (not N-of-anything) |
| 10 | Rollback story | PASS — pre-v5.14.1.G + sub-tags clear |
| 11 | Architectural sprint | PASS — additive only |
| 12 | Display ↔ execution | PASS — diagnostic only; no execution decision depends on it |
| 13 | Strategy lifecycle | N/A |
| 14 | X-macro dispatch | N/A — single struct, not registry |
| 15 | ML feature parity | PASS — no FOREACH_FEATURE changes |
| 16 | Cfg field + stamp | PASS — non-stamped operator diagnostic |
| 17 | Model-load path | N/A |
| 18 | Reuse-audit | PASS — plan acknowledges existing rolling structs; bit-mask semantics genuinely new |
| 19 | Pre-existing-work (CRITICAL) | PASS — all NEW + REUSE claims verified |
| 20 | Future-proofness | PASS — single struct, cfg-tunable; no N-of-anything |
| 21 | Test count assertion | PASS — recommend `>=` not `==` during coding |
| 22 | Auto-trigger downstream | N/A — not umbrella ship |
| 23 | Latency accountability | PASS — ~600ns/cycle slow-path (0.6% of 100µs budget); HOT_PATH_CHANGELOG entry planned |

## Section E — Cold-pickup (10/10 PASS)

All 10 fields per CLAUDE.local.md rule present + accurate.

## Recommendations (non-blocking)

1. Use `>= 11` not `== 11` in test count assertion (Check 21)
2. Add explicit DOCS/CHANGELOG.md entry citation in Step 5.C
3. Commit HOT_PATH_CHANGELOG.md entry in same ship as Step 5.C code

## Verdict: GREEN — proceed to v5.14.1.G.A coding

No architectural blockers. Ready to ship per the established sub-tag plan.
