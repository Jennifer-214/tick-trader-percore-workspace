# Pre-coding audit gate synthesis — v5.15.5.F.4c.4 (REBASED)

**Date:** 2026-05-16
**Engine HEAD:** `7538ace` = tag `v5.15.5.F.4c.3`
**Plan audited:** `plans/v5.15-live-readiness/subplans/2026-05-16-v5.15.5.F.4c.4-bandit-5state-thompson-wire-class-25-sweep.md`
**Audit gate fire:** 8 subagents parallel (parity / trace / readiness / merge-scan / dod-audit / accounting:module:ML-pipeline / hft:module:bandit-dispatch / registry-fit:module:bandit-thompson)

---

## Per-audit verdict table

| Audit | Verdict | Critical | High | Med | Low |
|---|---|---|---|---|---|
| /parity-check | YELLOW | 0 | 2 | 2 | 1 |
| /trace-deps | YELLOW | 0 | 0 | 4 | 0 |
| /readiness | GREEN | 0 | 0 | 1 | 2 |
| /merge-scan | YELLOW | 0 | 2 | 2 | 1 |
| /dod-audit | YELLOW | 0 | 0 | 3 | 2 |
| /accounting-audit | YELLOW | 0 | 2 | 3 | 1 |
| /hft-audit | GREEN | 0 | 0 | 2 | 3 |
| /registry-fit-audit | GREEN | 0 | 0 | 1 | 0 |

## Combined verdict: **YELLOW** (no CRITICAL; substantial amendments)

The plan's core architecture is GREEN — Pattern 1 + Pattern 5 + metadata-driven dispatch + Class 25 consumer migration + Option C cfg=0/1/2 wire-byte preservation + first-canonical multi-state-dispatch spec promotion are all structurally sound per discipline. The amendment list folds in discoveries from across all 8 audits that align with "completely close out tech debt" + "framework discipline over ad-hoc per-instance code" directives.

Effort estimate update: 3-5h coding → **~7-9h coding** with all amendments folded in. Net wins: 2 additional pre-existing tech-debt closures + 4-row drift-check parity gap fix + 5 additional Class 28 cmov sites + cfg=2 telemetry preservation + Pattern 4 sub-struct expansion (Stage 2 → Stage 3) + meta-registry `FOREACH_BANDIT_SIDE` framework primitive.

---

## CRITICAL findings (none)

---

## HIGH findings (5; bundled into the ship)

### HIGH-1 — PARITY-026 (NEW) — Pre-existing drift-check gap on 4 STAMP_BOUND fields

**Audit:** parity-check
**Site:** `ML_Headers/CfgDriftCheckRegistry.hpp` — missing rows for `bandit_algorithm` + `thompson_mu_prior` + `thompson_precision_prior` + `thompson_precision_obs`. All 4 are STAMP_BOUND at `StampBoundCfgRegistry.hpp:163-170` (stamps emit) but drift-check doesn't verify them. Silent train-serve parity gap since v5.14.10.B (~2 months).

**Fix:** Add 4 rows to `CfgDriftCheckRegistry.hpp` bundled with new `thompson_exp3_blend_alpha` drift row work (same registry; same ~30 min). Tier classification:
- `bandit_algorithm` → WARN_ALWAYS Tier 2 (avoid false-positive on legacy cfg=2 stamps post-Option-C semantic flip)
- `thompson_mu_prior` / `thompson_precision_prior` / `thompson_precision_obs` → REFUSE_STRICT Tier 1

**Auto-write:** PARITY-026 entry block to `DOCS/PARITY_ISSUES.md` (parity-check subagent's Write blocked by harness; orchestrator handles in auto-write step).

### HIGH-2 — cfg=2 telemetry preservation gap

**Audit:** parity-check + accounting-audit converge
**Site:** Plan § B row 3 says `apply_fn = BanditAlgo_Exp3_Apply` for cfg=2 = EXP3_OP_THOMPSON_GHOST. Legacy `BanditAlgo_Both_Apply` (at `BanditAlgorithmRegistry.hpp:239-254`) did `Thompson_Sample(thompson)` side effect to populate `thompson_chosen_arm` for telemetry. Plan's `BanditAlgo_Exp3_Apply` doesn't call `Thompson_Sample` → operator parsers reading `thompson_chosen_arm` see stale values mid-run for cfg=2.

**Fix:** Add 2 new ghost-aware apply_fns (~20 LOC):
- `BanditAlgo_Exp3_Drives_Thompson_Ghost_Apply` (for cfg=2) — Exp3 drives + Thompson_Sample for telemetry side effect; chosen_arm = Exp3's argmax (correct reward attribution)
- `BanditAlgo_Thompson_Drives_Exp3_Ghost_Apply` (for cfg=3) — Thompson drives + Bandit_GetProbabilities for telemetry; chosen_arm = Thompson's sample

Update plan § B rows 3 + 4 to use these apply_fns instead of bare BanditAlgo_Exp3_Apply / BanditAlgo_Thompson_Apply.

### HIGH-3 — Class 25 sweep table incomplete; transitive fn-sig migrations

**Audit:** trace-deps
**Site:** Plan § H lists 2 inline Class 25 sites (`ShardedSnapshot.hpp:764` + `CoreModelZoo.hpp:2584-2587`) but the wrapping fns at the reward attribution sites don't have `core_cfg` in scope:
- `EnsembleModelZoo_TickRewardsFromLookback` at `CoreModelZoo.hpp:1300`
- `EnsembleModelZoo_TradeCloseReward` at `CoreModelZoo.hpp:1368`

Need sig migration + ~9 caller updates:
- Production: `StrategyParameters.hpp:1148` + `ControllerEventLoop.hpp:1695`
- Tests: `controller_test.cpp:13892/13905/13933/13940/14016/14344`

**Fix:** Add to plan § H table as transitive sites; sig becomes `_TickRewardsFromLookback(EnsembleModelZoo<F>* ezoo, const PerCoreCfg<F>* core_cfg, ...)`.

### HIGH-4 — Pre-existing Class 25 instance at exit-side reward attribution

**Audit:** accounting-audit
**Site:** `EventLoop_DrainPostFillOneCore` at `ControllerEventLoop.hpp:1393` has `double fee_rate_taker_for_cf = 0.001` default param. Caller at `EngineSharded.hpp:2481-2487` doesn't pass it → reward computation at line 1749 uses 10 bps regardless of actual per-core `cfg.cores[c].fee_rate_taker`. Bandit learns from rewards computed against WRONG fee assumption. Plus `core_cfg` slice already added at line 1397 but UNUSED in body.

**Fix:** Fold into § H Class 25 sweep — thread `core_cfg->fee_rate_taker` into reward computation at line 1749; drop default fallback. Pre-existing instance closed.

### HIGH-5 — cfg=2 semantic flip needs explicit documentation

**Audit:** accounting-audit HIGH-2
**Site:** Plan body claims cfg=2 wire-byte preservation. True for stamp bytes + Exp3-weights-drive-ensemble. But chosen_arm semantically FLIPS:
- Pre-ship cfg=2: chosen_arm = Thompson's sample (used for reward attribution AND calib log telemetry)
- Post-ship cfg=2: chosen_arm = Exp3's argmax (used for reward attribution); Thompson's sample becomes separate telemetry slot per HIGH-2 fix

**Fix:** Plan body explicitly documents this as a Class 24 sister bug fix (pre-ship cfg=2 was crediting wrong arm; post-ship correctly attributes to driver). Tooltip + `engine.cfg.example` mention bandit posterior trajectories will diverge from pre-`.F.4c.4` cfg=2 runs (strictly-better correct attribution).

---

## MEDIUM findings (10)

### MED-1 — X_GEN_BANDIT_ALGO_* macro arity migration (trace-deps)

5 consumer macros (`ENUM` / `COUNT_ONE` / `FN_PTR` / `TOSTRING` / `FROMSTRING`) currently accept 4-arg shape `(name, val, fn, doc)`. When FOREACH_BANDIT_ALGORITHM expands to 7-arg, all 5 need explicit sig update to 7-arg (most ignore new args). Build-blocker if missed.

### MED-2 — `bandit_algorithm` row clamp bump (registry-fit)

`CfgFieldRegistry.hpp:592` clamp `INT(0, 0, 2) → INT(0, 0, 4)`. 1-token fix. Otherwise parser rejects cfg=3/4. Ship-blocker if missed.

### MED-3 — Batch argmax sweep across ML pipeline (merge-scan + accounting + hft + dod converge)

Plan § K covers Thompson_Sample argmax. Plan § L misidentified Bandit_GetProbabilities (which has NO argmax; it's the AVX-512 softmax kernel). True Class 28 sister sites:
- `BanditAlgorithmRegistry.hpp:201-211` `BanditAlgo_Exp3_Apply` argmax
- `BanditLearning.hpp:311-313` `Bandit_Update` internal max-loop  
- `ML_Headers/ModelInference.hpp:815-819` `ModelEnsemble_PredictBest` confidence argmax
- `ML_Headers/ModelInference.hpp:941-944` `ModelEnsemble_PredictWeightedBlend` dominant_idx argmax

All 4 production sites get same 5-line cmov pattern. ~20 LOC total. Closes Class 28 across ML pipeline. Plan § L target corrected.

### MED-4 — `BanditAlgo_Both_Apply` orphan post-ship (accounting)

After cfg=2 reassigned to `BanditAlgo_Exp3_Drives_Thompson_Ghost_Apply`, the old `BanditAlgo_Both_Apply` becomes unreferenced. DELETE at Step 2 (with TECH_DEBT entry if any external test fixture references it).

### MED-5 — `FOREACH_BANDIT_SIDE(X) X(buy) X(exit)` X-macro (merge-scan)

Plan creates 6 parallel buy/exit mirror sites (3 Pattern 5 fn-pointers + 2 dispatch tables + init flags + init/load fns). Wrap in `FOREACH_BANDIT_SIDE` meta-X-macro reducer → auto-generate parallel symbols. Eliminates Class 18 mirror at registry level for all future per-side additions. ~1h infrastructure work; significant future-leverage.

### MED-6 — Pattern 4 sub-struct expansion (dod)

Plan's calib log emission needs `chosen_arm / regime / active_state / reward_bps` etc in scope at emit time. Pattern 4 sub-struct refinement: add `Order::pre_resolved.bandit_chosen_arm / .bandit_regime / .bandit_active_state / .bandit_reward_bps_attribution` (Order::pre_resolved sub-struct Stage 2 → Stage 3 per `decision-time-data-binding-pattern.md` § sub-struct refinement). 2nd canonical of sub-struct Stage 3 trigger. ~45min.

### MED-7 — Calib log per-arm parameterization (merge-scan + readiness + registry-fit converge)

Plan § M hardcodes 4 arms but `ENSEMBLE_HORIZON_MAX = 8` (verified at `CoreModelZoo.hpp:904`). Add `FOREACH_PERARM_CALIB_COL(X) X(exp3_w) X(thompson_mu) X(thompson_prec) X(thompson_pulls)` × `FOREACH_BANDIT_ARM(X) X(0) X(1) ... X(7)` matrix-reduce. Auto-extends if HORIZON_MAX bumps. Truncate-by-n_arms via existing ternary guard.

### MED-8 — Pattern 5 path consolidation (dod MED-1 + parity MED-2)

Plan introduces `ezoo->on_trade_close_calib_emit` parallel to existing `oms->on_exit_calibration` Pattern 5 sink. Two parallel emit paths at the same callsite = Anti-pattern 4. **Fix: EXTEND existing `oms->on_exit_calibration` body** to walk extended FOREACH_CALIB_LOG_COL; drop the ezoo parallel sink. Caller-scope contract for new columns satisfied via Pattern 4 sub-struct expansion (MED-6). Saves ~50 LOC.

### MED-9 — EnsembleModelZoo cluster placement + sizing pre-computation (hft F1 + parity MED-1)

3 new fn-pointer fields (24B) + `last_predicted_exit_thompson_arm` int (4B) + `thompson_exit_bandits[NUM_REGIMES]` alignas(64) (~960B) lack explicit HOT/WARM/COLD cluster placement. False-sharing risk. Pre-compute exact byte-add + explicit `_padding<N>` member if size%64 violated. Plan Step 0.A.b tighten to hard pre-flight blocker.

### MED-10 — Latency assertion text update (hft F5 + accounting)

Plan claim "~5-10ns per reward attribution event" understates by ~10x. Actual: ~80-140ns per event (4 arms × ~20-35ns per-arm × dispatch + body work). Still well within H8 100µs by 3 orders of magnitude. Plan Step 8 verification gate text update only.

---

## LOW findings (10)

- **LOW-1** Test fixture mask-derivation rebind (`controller_test:23541-23558`) — dod LOW-1
- **LOW-2** Thompson_Update `__builtin_expect(arm < 0 || ..., 0)` tag — hft F4
- **LOW-3** § L target retarget (Bandit_GetProbabilities → batch-argmax sites per MED-3) — trace-deps + hft + dod converge
- **LOW-4** File:line drift: `MemHeaders/EzooInitFlagRegistry.hpp` → `ML_Headers/`; `CfgDriftCheckRegistry.hpp:249 → :248`; Thompson_Sample 227-237 → 228-235 — trace-deps
- **LOW-5** `FOREACH_ENSEMBLE_POST_LOAD_COUNT 9 → 11` explicit constant bump — registry-fit
- **LOW-6** DESIGN_SPECS/README.md catalog row `.F.4c.2 → .F.4c.4` — dod MED-3
- **LOW-7** `MASK_ML_CFG_BANDIT_ENABLED` ↔ `cfg.bandit_algorithm` orthogonality postmortem note — parity LOW
- **LOW-8** `PortfolioController.hpp:638` Bandit_Update site (controller-level; different state shape) — verify not retrofitted — merge-scan #5
- **LOW-9** Pattern 1 template static-array linkage across TUs — hft F3 — verify during implementation
- **LOW-10** `BanditAlgo_Blended_Apply` alpha resolution as fn arg (NOT scalar mirror on BanditState) — readiness LOW-2 + registry-fit

---

## What stays valid (NO change needed)

- Core charter: wire Thompson_Update + 5-state expansion + ghost training
- Option C wire-format preservation for cfg=0/1/2 (still holds; cfg=2 telemetry preservation via new apply_fn fixes the subtle gap)
- `multi-state-dispatch-with-per-state-update-metadata.md` Stage 2 DRAFT → Stage 3 ACTIVE promotion (spec body matches plan implementation)
- Pattern 1 fn-pointer dispatch composed with Amendment 2 metadata via X-macro reduction
- Per-core direct registration via `FOREACH_PER_CORE_CFG_FIELD` (NOT `PerCoreOverrides<F>`)
- `thompson_exp3_blend_alpha` cfg field + STAMP_BOUND row
- Exit-side `thompson_exit_bandits[NUM_REGIMES]` mirror
- Class 25 sweep approach (just needs expansion to transitive sites per HIGH-3)
- Most existing § A-§ M structure (refinements only)

---

## Cold-pickup viability

Post-amendment: **10/10** (per /readiness). Pre-amendment was already 10/10; amendments enrich without breaking cold-pickup.

---

## Effort + risk reassessment

- Original `.F.4c.2` plan: ~3-4h
- Rebased `.F.4c.4` plan (pre-audit): ~3-5h
- Post-audit amendment-folded: **~7-9h coding** (substantially more work but each amendment closes additional tech debt)
- Risk stays MED — breadth of work; no new architectural complexity

---

## Auto-write items pending

- `DOCS/PARITY_ISSUES.md` — PARITY-026 entry (4 drift-check rows missing for STAMP_BOUND bandit/thompson cfg)
- `DOCS/TECH_DEBT.md` — close -026, -030, .F.5 charter absorbed; log NEW audit candidates (Ridge_* per-core cohort sweep)
- `DOCS/RECURRING_BUG_PATTERNS.md` — Class 18 / 19 / 24 / 25 / 28 second-canonical amendments
- `FEATURE_LOOKUP.md` — bandit 5-state + thompson_exp3_blend_alpha + per-arm calib columns entry
- `CHANGELOG.md` — `.F.4c.4` row
- `DESIGN_SPECS/README.md` — multi-state-dispatch Stage 2 → Stage 3 catalog update

---

## Recommended path forward

**Amend plan body to absorb all 5 HIGH + 10 MEDIUM + 10 LOW findings (~45-60 min plan amendment).** Once amended:

1. Re-fire `/precoding-audit-gate` against amended plan — OPTIONAL given convergent verdict (all findings are amendments, not structural redesigns); could skip to coding
2. OR proceed direct to code-execution after operator review of amended plan + spot-check verification

Operator decision required:
- **(A)** Amend plan + skip second audit gate re-fire + proceed to code-execution
- **(B)** Amend plan + re-fire one more audit gate to confirm GREEN + then proceed
- **(C)** Other

Given convergent verdicts (all amendments are addressable; zero CRITICAL findings; zero architectural redesigns; cumulative 26 amendments are mechanical-or-additive), I recommend (A) — amend plan + proceed. Re-fire is OPTIONAL but adds wall clock; the amendments aren't deeply intertwined enough to warrant double-audit.

---

**End of synthesis.** Comprehensive amendment list folds in 25 discoveries from 8 audits. Net result: tighter structural close + 4 additional pre-existing tech debt items absorbed + framework primitives (`FOREACH_BANDIT_SIDE` + Pattern 4 sub-struct Stage 3 expansion + telemetry-preserving ghost apply_fns) added for future-leverage.
