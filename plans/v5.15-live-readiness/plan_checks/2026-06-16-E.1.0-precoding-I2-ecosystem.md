---
type: precoding-audit-report
ship: v5.15.5.F.4d.1.E.1.0
lens: I2 — existing-guard-ecosystem integration + reinvention check
agent_class: Investigative (1 of 3 I → 3 A)
date: 2026-06-16
verdict: GREEN-with-2-EXTEND-corrections
head: 572c978
---

# E.1.0 precoding I2 — ecosystem-integration / reinvention lens

BLIND re-derivation against landed code (HEAD 572c978). Convergence with another lens = signal.

## (a) Existing-guard inventory (the surface E.1.0 must not duplicate)

**`tools/` CI guards (39 files; the load-bearing ones):**
`check_determinism.sh` (umbrella: (1) `check_fp_determinism.sh` (2) `check_locale_determinism.sh` (3) `replay_locale_gate.cpp`) · `check_fp_determinism.sh` · `check_locale_determinism.sh` · `check_determinism_selftest.sh` (the net's teeth-proof) · `calls_graph_diff.sh` (hot/sharded call-graph SSoT — `SHARDED_FILES[]`) · `check_struct_alignment.py` (regex CI: alignas/byte-serialize) · `check_meta_registry.py` · `check_per_core_registry_integrity.py` · `check_identifier_retirement.py` (H21) · `check_money_gross_single_source.py` · `check_tools_inventory.py` (enrollment 2-way) · `check_session_docs.sh` (HARD/ADVISORY aggregator, 10 lanes) + selftests.

**`.githooks/pre-commit` Checks (lettered A–L, gaps reserved):**
A B-Plus · B forward-promise · C doc-rename · D Tests-section · E doc-floor · F **determinism net** · G doc-metadata · H identifier-retirement · J tech-debt(advisory) · K struct-alignment · L gross-SSoT. (I reserved for calls_graph orphan-guard.)

**Guard-matrix §1 ladder:** T1 structural/compile · T2 CI check · T3 regression test · T4 runtime · T5 periodic sweep. Rule: one guard per class at the HIGHEST reachable tier; guard-sprawl is rot.

## Per-gate: EXTEND-existing vs net-new

| Gate | Verdict | Sister/extend point |
|---|---|---|
| **H14 no-bitfield** grep | **NET-NEW** (T2) | No existing forbidden-construct grep (H1/H2/H3 all TBD/HOLE in §4a — none built). Models exactly on `check_struct_alignment.py`'s regex-CI shape (the accepted T2 mechanism). 0 hits today → GREEN-start confirmed (grep verified empty). |
| **H7/H20 branchless** grep | **NET-NEW (T2) but PARTIALLY DUPLICATES `/hft-audit` (T5)** | `/hft-audit` SKILL.md §88+§97 ALREADY scans SP/HP/drainer if/switch/`?:` for Class-28 branchless violations AND already names `BG_Evaluate`/`SG_Evaluate`/`ExecutionCore_Tick`+AVX kernels with a codegen A/B oracle. The grep is the T2 standing complement to that T5 sweep — legitimate (continuous vs cadence), NOT a reinvention, PROVIDED the plan cross-refs `/hft-audit` so they share the Class-28 definition + don't drift. **Reuse `calls_graph_diff.sh::SHARDED_FILES[]` as the file-list SSoT** (plan C-finding #2 already locks this — confirmed correct: the array exists, is the live hot-path SSoT). |
| **H10 SIMD-fallback** determinism | **EXTEND `check_determinism.sh`** (add gate (4)) — NOT net-new | Plan C-finding #1 is CORRECT and load-bearing: the `±USE_NATIVE_128` FP-native axis is ALREADY gate (1) of `check_determinism.sh` (pre-commit Check F) via `check_fp_determinism.sh` (native_o3/native_o0/generic_o3 vs golden). E.1.0's REAL H10 deliverable = a NEW AVX-512-vs-scalar byte-compare. Targets VERIFIED REAL: `RidgeBlender.hpp:59/493/584` (`#if defined(__AVX512F__)` + `#else` scalar) + `BanditLearning.hpp` (23 AVX512 sites). Reuse `check_fp_determinism.sh`'s compile-2-ways `check()` harness shape (canonical-sister). |
| **H8 latency ratchet** | **NET-NEW** (T2 merge-gate) — no existing parser | Parse target VERIFIED: `EngineSharded_DumpLatency` (`Run.hpp:309`, the `min p50 p95 p99 max avg` table) + drainer `p99` (`Run.hpp:2440`). No tool parses it today. Genuinely net-new; correctly placed as merge-lane not pre-commit (perf build too slow). |

## (b) Tier check — is each at its highest reachable tier?

- **H14:** Could a `static_assert`/AST beat the grep (T1)? **No reachable T1** — C++ bitfield syntax is a SOURCE-TEXT construct; nothing instantiates to assert against, and the toolchain has ZERO clang/libclang/AST dep (every existing structural guard incl. `check_struct_alignment.py` is regex). Grep at T2 IS the highest reachable tier. Correct.
- **H7/H20:** Could it reuse the call-graph instead of fresh grep? Plan ALREADY does (`SHARDED_FILES[]` SSoT) — good. But a perfect hot-path-branch detector is undecidable → T2-grep is heuristic, correctly backed by a shrinking KNOWN-PENDING allowlist (D-84) + the T5 `/hft-audit` sweep above it. Right tier; the allowlist sizing is the #1 A-class refute target.
- **H10:** T2/T3 — extend at T2 (Check F). Correct; first real T3 consumer is E.1.2's SoA kernel (plan acknowledges).
- **H8:** T2 merge-gate — correct (hot path takes T1-only guards per §1; a latency RATCHET is necessarily a T2 measure-and-compare, not compile-time).

**No tier-misplacement found.**

## (c) Integration / wire-in

- New grep-CI → pre-commit **Check H is TAKEN** (identifier-retirement). Plan §Phase-E says "Check H (H14) + Check I (H7/H20)" — **CONFLICT: H is occupied; I is reserved for the calls_graph orphan-guard** (per the hook's CHECK J comment). **Use the next free letters (M, N)** — flag for the A-class. Mechanical but a real collision.
- H10 SIMD gate → extends `check_determinism.sh` gate (4); auto-flows into pre-commit Check F + `check_session_docs.sh` (no new wiring). Clean.
- `check_session_docs.sh` aggregator: new grep-CI should enroll as HARD lanes (mirrors the 10 existing). The H10 extension rides Check F transitively (already aggregated). 
- **`check_tools_inventory.py` enrollment is MANDATORY** (HARD 4 of the aggregator + pre-commit): every new `tools/check_*.{py,sh}` needs a `DOCS/TOOLS.md` row OR the commit RED-builds, AND load-bearing (STANDING-CI/SKILL-WIRED) tools need a negative selftest (D-137) — so each new gate must ship its teeth-proof selftest (plan Phase E already mandates inject→RED→revert→GREEN; confirm each is a `_selftest.sh`/`test_*.py` so enrollment Check 3 passes).

## Convergence-worthy flags for the A-class
1. Check-letter collision (H/I both taken) — use M/N.
2. H7/H20 grep MUST cross-ref `/hft-audit` (shared Class-28 def) to avoid two-definitions drift; allowlist start-size is the refute target.
3. Each new gate ships a selftest or `check_tools_inventory` Check-3 RED-builds.
4. All four EXTEND/net-new boundaries verified against landed code — no reinvention beyond the two corrections the plan already self-caught.
