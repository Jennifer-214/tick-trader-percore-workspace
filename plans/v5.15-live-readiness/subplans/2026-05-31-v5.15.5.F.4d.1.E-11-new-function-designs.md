---
type: new-function-designs
doc_kind: design-pass sidecar (D-93 new-fn design-audit home for #11)
ship: "#11 numeric-foundation unification (decimal money + unified FixedPoint<RADIX,FRAC>)"
sprint: v5.15-live-readiness
created: 2026-05-31
status: DRAFT — design pass (step 5): #1-#6 ALL framed (heavy-design inflection passed); remaining = prove the divmul_pow10 magic + step-6 plan fold + step-7 audits; locks into the #11 plan body at step 6
decision_log: decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (D-122..D-129 authoritative)
predecessor_sidecar: the .E.0.1 new-function-designs sidecar (D-93 pattern home)
sister_specs:
  - data-disciplines/fp-determinism-canonical-path-discipline.md (to author)
  - refactor-patterns/branchless-math-kernel-pattern.md
covers: "compounding sequence #1 type → #2 multiply → #3 divmul_pow10 → #4 rounding → #5 parse/casts → #6 quantize"
---

# #11 new-function designs (step-5 design-pass sidecar)

The D-93 new-function design-audit home for #11. Captures the design framings AS THEY LOCK so the design pass survives compaction — decisions live in the log (D-122..D-129); this is the HOW. Locks into the #11 plan body at step 6, then gets its focused design-audit before coding.

**Compounding sequence:** #1 type → #2 shared multiply → #3 `divmul_pow10` → #4 rounding → #5 parse/casts → #6 quantize. **Status: #1–#6 ALL framed — design pass essentially complete (#6 mechanical). Remaining is NOT design: prove the `divmul_pow10` magic (D-100 gate + design-audit) + step-6 plan fold + step-7 audits. (Note: the boundary casts to_binary/to_decimal — listed under "parse/casts" — are a trivial scaling pair, captured with #5's domain; the 1-cycle digit-extraction perf lever is the separate SWAR parse, task #6, riding on #5's exact parse.)**

## #1 — unified `FixedPoint<RADIX,FRAC>` type [LOCKED — D-125/126]
- **16B two's-complement `__int128`**, 64.64 (binary `<2,64>`) / scale-10⁸ (decimal `<10,8>`); 4 per cache line. Storage is **radix-independent** (a signed scaled integer); radix appears ONLY in the mul-reduce + div.
- Sign in the **top bit** (no separate sign field). The value type is a bare `__int128` → **16B, 16-aligned, NO padding field** (cleaner than sign-magnitude's 24B + explicit `_padding`; H12 padding concern moves entirely to the *containing* structs — stamp/snapshot — verify their layout at step-6).
- **Step-0 VERIFIED (D-126):** transcendentals clean-port (abs-first), no symmetric-range dependency, range robust for BTC (~52-bit max intermediate, 11 bits headroom under 2⁶³). High-price tail (~60 bits @ $1M, 3-bit margin) resolved by feature-saturation = full-confidence (correct for a decision input). Action items: graceful ratio-feature saturation · −2⁶³ Negate/Abs guard · port FP64 backend in lockstep · LargeTradeState base-qty-not-notional audit.

## #2 — the shared multiply [FRAMED]
`a × b` = **[extract sign + abs] → [unsigned 128×128→256 product — HOISTED certified body] → [reduce: RADIX-FORK] → [reapply sign]**.
- The certified **unsigned** product (FP64_Mul's guts) is reused **unchanged** (C1=A HOIST). Two's-complement operands are abs'd at entry (−2⁶³ guard), sign = `(A<0)^(B<0)` reapplied after the reduce → **value-equivalent to sign-magnitude BY CONSTRUCTION** (same unsigned product, same sign rule) — exactly what the D-125 net needs.
- Two's-complement nets a win: add/sub/compare SIMPLER (native), mul/div NEUTRAL (abs-in/out, same magnitude work), storage SMALLER (16B).
- The widen (256-bit product) is a **hard math constraint at 128-bit** (the exact `A·B` needs 2× the bits before ÷SCALE; can't skip for exact/deterministic). NOT a latency bottleneck (4 native partials, slow-path cadence, ~6ns vs the ≤100µs budget). Dissolving it = the 8B future ship (operands fit → native `__int128` product). Decimal's operands are small (≤63-bit) so its product is effectively native already; the genuine manual 256-bit widen is binary's alone.
- **Fork lives ONLY in the reduce** (everything before is shared + sign-agnostic).
- OPEN: pin against `FP64_Mul`'s exact body at framing→code.

## #3 — `divmul_pow10` (decimal reduce) [FRAMED]
Decimal reduce `C = P / 10⁸` via **Granlund–Montgomery reciprocal-multiply**: `q = (P × M) >> S`, **constexpr M/S** (10⁸ is compile-time). Replaces `__udivti3` (~40–100 *variable* cyc) with a fixed-cost multiply+shift (~20 cyc) that **REUSES #2's wide-multiply primitive** (same 256-bit product, shifted to grab the high end). Returns **(quotient q, remainder r = P − q·10⁸)** — `r` feeds #4.
- Mechanism: turns expensive arbitrary-÷ into cheap `×M` + **free power-of-2 shift** (`÷2ˢ`). **EXACT** (not approximate) — bit-identical `floor(P/10⁸)`, *iff the magic is proven*.
- **CORRECTNESS CRUX:** prove `(M,S)` exact over the full dividend range (G–M bound; enumerate-not-eyeball per `enumerate_set_before_categorical_claim`); validate vs the D-100 external decimal oracle. A wrong magic = silently-wrong money.
- Binary reduce, for contrast: `P >> 64` (free shift, no magic) → `divmul_pow10` is **decimal-only**.
- Rejected: factoring `10⁸=2⁸·5⁸` (truncating `P>>8` first corrupts the round).
- **GENERAL PRIMITIVE:** divide-by-invariant-constant for *wide* types → DESIGN_SPEC candidate (task #14) composing `branchless-math-kernel-pattern`; also the only way to SIMD-divide (AVX has no integer divide).

## #4 — the rounding helper [FRAMED — mode DECIDED D-128]
**Mode decided (D-128):** round-**HALF-EVEN (banker's)** for internal reduces; **venue-matched** where the venue defines it; **uniform EVERYWHERE incl. replay** (D-105). Branchless, from divmul_pow10's `(q, r)`, `0 ≤ r < SCALE`:
- **Half-even (banker's — internal default):**
  `round_up = (2r > SCALE) | ((2r == SCALE) & (q & 1));  result_mag = q + round_up`
  — `2r < 2·SCALE` (~28 bits, no overflow). The `(q & 1)` is the round-to-**EVEN** tie-break: an exact half rounds up only when `q` is odd (→ even). Unbiased over many fills.
- **Round-up (venue fee, D-109):** `round_up = (r != 0)` (= ceil at precision).
- **Truncate:** `round_up = 0` (binary doesn't use this helper — it shift-reduces; decimal-only).
- Operates on the **unsigned** `(q,r)` magnitude from #3; sign reapplied after (from #2). **Single source** → uniform incl. replay (D-105) by construction.

## #5 — exact decimal `FromString` [FRAMED]
Parse venue decimal strings (`"50000.50"`, `"0.00006150"`) DIRECTLY to `<10,8>`, never via double — kills the D-102 round-trip + the C4 lossy fill parse.
- **Single-pass digit-accumulate** into the 10⁸-scaled integer: `mantissa = mantissa*10 + (c-'0')` per char; track `frac_digits` after `.`; sign from leading `-`; non-digit/non-`.` → `ok=false`. Final scale-adjust: pad `×POW10[8−frac]` if <8dp / exact if ==8dp / round-via-#4 if >8dp (defensive). Pure integer — no reciprocal, no `strtod`, no double.
- **Exact** for ≤8dp (venue range); **simpler** than the binary `FromString` (drops `POW10_RECIP` — that existed only to reach a *binary* fraction; D-123). Deletes machinery, doesn't add.
- **Locale-immune** (pure digit math) → rides the `.E.0.1` net, like today's `FPN_FromString`.
- **Error-detecting** (`.E.0.3`-absorbed): returns `(value, ok)` — malformed/overflow SURFACED not swallowed (money path: a garbled fill is a real error, not a silent zero).
- Edges: **>8dp** can't occur for a supported venue (D-106 boot-guard: storage scale ≥ venue precision); if it arrives anyway → round-not-truncate + flag. **Overflow** → D-106 range guard / `ok=false`, never silent-wrap.
- Sites: `BinanceCrypto:744` (price/qty), `BinanceDepth`, `BacktestSharded` + C4 fill adapter (`BinanceUserData` `"L"`/`"l"`/`"n"` → decimal `OrderResult`). WS-thread/drainer cadence, not hot path.
- SWAR (task #6): digit-extraction vectorizable later; decimal scaling is the simple tail — decimal's shape makes a future SWAR pass *easier*.
## #6 — `FPN_Quantize` + MIN_NOTIONAL + exact qty wire-string [FRAMED — mechanical]
Detect-and-match the venue at order submit (D-104/D-106), off the already-loaded `SymbolFilters`. No architectural novelty:
- **Quantize** value to venue `tickSize`/`stepSize` = round-to-step (general divide by the *runtime* per-symbol step — submit cadence, not hot path, so no reciprocal-magic needed).
- **MIN_NOTIONAL enforcement** (bite B-α): `notional = price×qty`; if `< min_notional` → reject/flag. Today min_notional is parsed but NOT enforced → `-1013` venue rejections.
- **Exact qty→wire-string**: the `FromString` inverse (decimal → string, exact digit-emit) — a money-emit-parity site like the stamp.
- WS/submit cadence. Mechanical; works out from the established primitives.

---
**End — #11 new-function designs sidecar (DRAFT, design pass in progress). Locks into the #11 plan body at step 6, then the D-93 focused design-audit before coding.**
