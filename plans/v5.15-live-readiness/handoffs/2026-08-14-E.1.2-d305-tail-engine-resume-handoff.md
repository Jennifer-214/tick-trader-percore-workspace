---
type: handoff
status: active   # THE one live handoff (supersede-on-write: the plugin-arc handoff is superseded — that arc CLOSED 2026-08-14). ≤1-active enforced by check_handoff_active_singleton.py.
ship_tag: v5.15.5.F.4d.1.E.1.2
plan_type: coding
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: the LAST foundation leaf — freeze the capital-state WIRE (SHARDED 10→11; PORTFOLIO retires) on a registry-driven NodeContext persist-serializer; every E.1.3+ spine reads this layout
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-08-10-E.1.2.B-derived-facts-integrity-arc-handoff.md
supersedes: 2026-08-10-E.1.2.B-derived-facts-integrity-arc-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [data-disciplines/per-node-position-ownership-model.md, data-disciplines/cache-line-discipline.md, wire-format-patterns/wire-format-byte-preservation-discipline.md, meta-disciplines/dead-code-and-identifier-retirement-discipline.md]
coding_status: "FRESH — the D-305 tail has NOT started (FOREACH_NODE_PERSIST_FIELD is grep-zero). The plan re-ground (amendment 9, ws `2d4889c`) executed the full c-class punch-list 2026-08-14; the plan body's ⏩ EXECUTION-STATE banner IS the remaining-scope spec. Rollback anchor `pre-E.1.2-resume` @ engine `3c57534` (annotated, pushed)."
---

# E.1.2 engine resume — the D-305 tail (the plan is CURRENT; this doc points, the plan specifies)

**Written 2026-08-14 at the plugin-arc close.** The predecessor handoff carried the ENTIRE
`E.1.2.B` toolchain arc (ten boundaries, 2026-08-10→14) and is SUPERSEDED — read it only for
that arc's history. THIS pickup is ENGINE CODE on a capital-persistence surface.

## State at write (all pushed, remote-verified this session)

| Repo | HEAD | Note |
|---|---|---|
| Engine `feat/v5.15-live-readiness` | `3c57534` | = tag `pre-E.1.2-resume`; source-identical to `a71b893` (anchor-verification HEAD — interim commits touch only build.sh/CMakeLists) |
| Workspace `main` | `2d4889c` | amendment 9 (the punch-list execution) is the tip |
| Plugin `feat/hud-robustness` | `cca68d6` | arc CLOSED at feature-complete 0.5; verification battery green |

Untracked engine scratch (`notes_update.md`, `resume`, `tatoo.md`, `tmpb924ezcj.cpp`) = operator files, keep.

## Critical pickup-time reads (in order)

1. `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md` —
   **the ⏩ EXECUTION-STATE banner is the executable spec** (remaining scope, close-gate, carried
   open questions, cite-era rules). The AMENDMENTS block (1–8) still governs; AM-8 is stamped LANDED.
2. `plan_checks/2026-07-04-E.1.2-phaseC-serializer-sweep-synthesis.md` + `plan_checks/2026-07-04-E.1.2-phaseC-step2-iclass-ground-truth.md` — the D-305 remainder SSoTs.
3. `reports/2026-08-11-e12-currency/c-class-e12-plan-currency.md` — the corrected-anchor table (§1) + refute-spots.
4. **Required reading for THIS surface (CLAUDE.local table):** `DOCS/STRATEGY_AND_CODING_RULES.md` (persistence/slow-path) · `plans/_cross-cutting/2026-05-06-latency-path-discipline.md` · `DOCS/CLAUDE_INVARIANTS.md` (snapshot/OMS section) · `DOCS/DESIGN_PHILOSOPHY.md` §5 (determinism) + §2 H9/H12/H21.

## The work (D-305's order — the banner restates it with anchors)

1. Ordered `FOREACH_NODE_PERSIST_FIELD` (~24 flat scalars, 9 Money) + compose-sub-registry
   delegates (regime/feeder ALREADY LANDED at Step 2c) + the OMS-precedent count-lock
   (`MemHeaders/OmsFieldRegistry.hpp:423-433`) + paired-bump + the Python layout-audit tool.
2. REC-A fold + D-304 doc-fixes.
3. Steps 3–5: layout-hash · v11 delta + SHARDED 10→11 + BLK-2 (PORTFOLIO **RETIRES**) ·
   D-289 dead-serializer deletion · F-096 Money legs · AM-4 (partner_pending: persist pnl,
   RE-DERIVE bitmap; the `:488/:901` cites do NOT verify — re-derive fields at dive).
4. Close-gate: re-bless `cache_layout_baseline.txt` MINUS exactly the 4 engine keys (never the
   9 suite ones) · regen/RENAME `tests/sharded_snapshot_v10_golden.hpp` · ledger+SOURCES
   lockstep · ctest `:27013-27014` + `:11588` · AM-6 codification (class + guard together).

## Arming for the next action (M8 — route, don't rediscover)

- **Audit posture:** plan `audit_tier: HIGH` (capital wire freeze). The 2026-07-03 gate RAN
  pre-Step-1; amendment 9 is a currency RECORDING, not a design change — minimum at pickup =
  `/readiness` against the plan; judgment call on a light gate re-fire given the month gap.
- **Skills:** `/trace-deps` (serializer call-sequence) · `/parity-check` if the snapshot wire
  shifts vs any train-serve surface · `/accounting-audit` on the AM-4 partner-pnl work.
- **Tools to RUN:** `python3 tools/check_per_node_registry_integrity.py` (build.sh runs it) ·
  `python3 tools/check_cache_layout.py --isolate NodeContext` (settle regime_state@56) + `--isolate TUISharedState` (settle seq@58112) ·
  `tools/gen_code_map.sh --composition/--byte-context` at the dive (D-202 cascade-first rule) ·
  `./build.sh test` (now chains the latency conformance report; budgets are shipping-basis, operator-blessed).
- **The `[DERIVED]` toolchain inherited from the closed arc:** sidecars auto-emit every build;
  `<leader>ds` browses shipped code (use it ON the serializer work); probe flags derive from
  the shipping db (TD-257 closed).
- **WILL BITE:** the pipe-swallow hook (`$?` after a pipeline) + the `rg -r` typo hook ·
  Check T parked (`FOXTAG_REARM=1`) · Check N budgets re-bless via `--update-budgets` at a TTY
  (or the in-editor bless menu row) if a kernel intentionally grows.
- **Operator-TTY pending:** the D-419 golden bless (`bless.py --console`, or `<leader>dm` → Bless).

## Close-out at ship

Version bump + tag per `/ship` · the H21 tombstones (SHARDED v10, PORTFOLIO v7 retirement) ·
FEATURE_LOOKUP (operator migration impact SECTION: old warm-restart snapshots version-reject —
acceptable, epoch boundary, no live models) · postmortem · AM-6 codification lands WITH the guard.

## Capture-completeness (judgment ledger, Check-N form)

| Check | Verdict |
|---|---|
| Check 2 plan-body frontmatter | ✅ E.1.2 plan refreshed (in-flight/partially-executed, v1.2-execution-reground, dual rollback anchors) |
| Check 3 decision artifacts | ✅ D-419 minted + sentinel-paired + nav row + range D-97..D-419; **golden BLESSED by the operator FROM THE EDITOR** (the menu bless row's maiden run — `citable-ids.txt` in this commit) |
| Check 4 findings → ledger | ✅ TECH_DEBT-257 + TECH_DEBT-258 CLOSED this arc (entries moved open→closed with full dispositions); TD-269 (suite baseline keys) / TD-246 / TD-259 remain tracked-open; registry-cards + .toolbus HOMED at the plan's 0.5 line — no unhomed finding at this close |
| Check 5 handoff currency | ✅ THIS doc (supersedes the plugin-arc handoff — arc CLOSED; reciprocal `superseded_by` set; MASTER repointed) |
| Check 6 M7 escalation | ✅ none owed (no bug class recurred despite codification this arc) |
| Check 7 spec promotion | ✅ none eligible (the §12 rungs live in the ideas record until a spec earns first-canonical) |
| Check 9 memory→spec | ✅ one memory REFINED (sessions-reload cadence, `feedback_short_sessions_30_to_60_min` body; already indexed) |
| Check 10 going-forward rules | ✅ none new (capture-at-leaf-boundaries rides the refined memory) |
| **Independent review** | ⚠️ **DECLARED, not silent:** the plugin arc closed on the MECHANICAL battery (load-all · 46/46 suite, 127+ teeth · live smokes · floor) + the operator's live dogfood (the strongest judgment layer this codebase has — 5 bugs found-and-fixed same-evening). No a-class pass ran over the arc's diffs; queue one if desired. The ENGINE work ahead re-fires its own gates per the plan's HIGH tier. |

## TaskList (recreate at pickup)

| ID | Status | Subject |
|---|---|---|
| #12 | pending → in_progress | The D-305 tail: FOREACH_NODE_PERSIST_FIELD first (this doc § The work) |
| #6 | pending (operator TTY) | Golden bless: D-419 additions |
| #3/#4/#5 | pending backlog | TD-246/-259 · ledger stragglers half 2 · contract-stale 58 INFO |

## Re-derive at pickup

```
./tools/check_session_docs.sh
git -C . log --oneline -3 && git tag -l 'pre-E.1.2*'
rg -n "FOREACH_NODE_PERSIST_FIELD" CoreFrameworks/ MemHeaders/   # expect ZERO (not started)
python3 tools/check_capture_audit.py --check 14
rg -n "D/C/F: D-419" plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
```
