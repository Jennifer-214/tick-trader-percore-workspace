---
type: handoff
status: active   # the one live handoff; supersede-on-write (check_handoff_active_singleton.py enforces ≤1)
ship_tag: E.1.1-③-config-compiler
plan_type: live-readiness
sprint_end_goal: v5.15 live-readiness — make the per-node sharded engine live-ready + the codebase more maintainable.
ship_end_goal: ③ "config compiler" — boot-time capital-cfg validation that ALWAYS-ABORTs on a malformed or out-of-range capital value (no silent-wrong-capital), within E.1.1 (Core→Node rename + cfg clean-break + single_core deletion).
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-22-E.1.1-cfg-config-compiler-gates-next-handoff.md
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md, plans/v5.15-live-readiness/MASTER.md, plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md, plans/v5.15-live-readiness/plan_checks/2026-06-22-E.1.1-config-compiler-IA-cascade-synthesis.md]
coding_status: mid-coding-checkpoint-3 (③ steps 1-3 of ~7 committed; next = step 4b)
deletion_scope: minor
---

# E.1.1 ③ config-compiler handoff — capital-cfg validation (mid-ship, 3 commits in)

**Created:** 2026-06-22
**Target:** ③ "config compiler" — the boot-time capital-validation cluster inside E.1.1.
**Branch:** `feat/v5.15-live-readiness`
**Pre-tag rollback anchor:** `pre-v5.15.5.F.4d.1.E.1.1`
**Plan body:** `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md`
**Design SSoT:** `plans/v5.15-live-readiness/plan_checks/2026-06-22-E.1.1-config-compiler-IA-cascade-synthesis.md` (§ "③ CODING PROGRESS")
**Decision log:** `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-247→D-254 + the D-254 ADDENDUM)

## Engine state at handoff write
- HEAD: `03830e8`  ·  branch `feat/v5.15-live-readiness`  ·  working tree CLEAN (verified)
- **Engine commits are LOCAL** as of this handoff (`252973f`..`03830e8`; the operator's push-decision was pending at close) — a SAME-machine pickup has them; a CROSS-machine pickup must confirm they're on the remote before trusting "HEAD == 03830e8" (push the feature branch first if absent).
- Version.hpp: `5.15.5.F.4d.1.E.1.0` (E.1.1 is in-flight, NOT yet tagged)
- Test baseline: **3635 / 0** (`./build.sh test`)
- Workspace HEAD: `0d22be4` (pre-sync; this close pushes the docs)

## The one-paragraph orientation
③ stops the founding bug: a **capital** cfg value loading **silently wrong** — `stop_loss_pct=banana` → 0 → SL disabled, or `risk_pct=999` → 999% allocation. Policy (firm, operator): **NO MARGIN** — loss-side ≤ 100%, gain-side ≤ 1000%, Σ node allocations ≤ 100%; **ALWAYS-ABORT all modes** on a capital fault (D2). The design is the **HYBRID** (D-254 + its addendum): parse-point flag-capture for MALFORMED/OVERFLOW (a value-only sweep can't see them — `banana`→0 is a valid sentinel post-parse) **+** a post-resolve out-of-range value-sweep. Two metadata bits side-tag every capital field (`CAPITAL_BOUND_LOSS` / `CAPITAL_BOUND_GAIN`); `cfg_compile_ok()` is the boot gate; the callers ALWAYS-ABORT.

---

## TaskList state at handoff write (recreate verbatim via TaskCreate)

| ID | Status | Subject |
|---|---|---|
| #1 | in_progress | E.1.1 ship — Core→Node rename + cfg clean-break + single_core deletion |
| #2 | in_progress | ③ cfg CLEAN-BREAK cluster — config compiler (gates done; steps 1-3 committed; 4b/4c/wiring/key-clean-break/tests remain) |
| #3 | pending | ⑤ V-class — asan/ubsan + determinism re-cert on the rename + ③ |
| #4 | pending | E.1.1 ship-close — doc sweep + FEATURE_LOOKUP + cores-prose + TD-140 move |
| #5 | pending | E.1.2 — NodeState SoA / Money relayout (successor leaf) |
| #6 | in_progress | All-strategies-private restructure + capture (D-251, policy DECIDED 2026-06-22) |
| #7 | pending | Consolidate ③ capture post-deep-pass (D-249 + codification slate + homes) |

---

## Ship state across sessions

**Pre-tag anchor:** `pre-v5.15.5.F.4d.1.E.1.1` (E.1.1 coding start). E.1.1's earlier phases (Core→Node mechanical rename, single_core deletion, include-prune) already landed in prior sessions (see superseded handoffs 2026-06-17/20). THIS handoff covers the **③ config-compiler** sub-cluster.

**③ commits this session** (all build-gated **3635/0**, pre-commit clean incl. determinism net):
- `252973f` — chore(privacy): gitignore `Strategies/private/` + untrack the leaked `EmaCross.hpp` (bleeding-stop; go-forward only, NO history scrub per D-251)
- `7b35517` — ③ step 1: `CAPITAL_BOUND_{LOSS,GAIN}` bits (1u<<14 / 1u<<15) + 15-field cohort tagging (5 GAIN / 10 LOSS) + both-active XOR compile guard + overflow-guard repoint + 6 COMPOSE_NA cells + `cfg_mask_overlap_count` helper
- `a75fddf` — ③ step 2: `uint32_t cfg_load_fault_flags` field + fingerprint size-pin `static_assert(sizeof(ControllerConfig<64>)==53056)` + zero-at-capture (BacktestSharded) + the "re-checked at serve" doc-lie fix
- `03830e8` — ③ step 3: parse-point MALFORMED-capture (FLAT path) — walker `fault_out` sink + reasoned `[cfg] FATAL` + `cfg_compile_ok()` + main.cpp boot-refuse

**Independent Stage-5.5 review: CLEAN** (all 9 deliverables VERIFIED + substantive; no fabrication; edits surgical; determinism sound; deferred chunks properly homed).

---

## Per-Step status (③)

| Step | Status | Landed at | Notes |
|---|---|---|---|
| 1 — registry layer (bits + tagging + guards) | LANDED | `7b35517` | inert until the sweep reads the bits |
| 2 — fault field + fingerprint size-pin + determinism | LANDED | `a75fddf` | field zeroed by F-076 ctor; size-pin guards the hash |
| 3 — malformed-capture FLAT path + cfg_compile_ok + boot-refuse | LANDED | `03830e8` | **founding bug closed for the flat/global path** |
| 4b — per-node malformed-capture | **NEXT** | — | the `_PARSE_OV_PCT` override macro (`core_N_*`) + the idiom-2 legacy strstr branches (`node_N_risk_pct`/`node_N_max_drawdown_pct`) — capture `Money_FromString` MALFORMED\|OVERFLOW → `CFG_FAULT_CAPITAL_MALFORMED` (same as the walker did for flat) |
| 4c — out-of-range post-resolve sweep | PENDING | — | the HEART: X-macro walker over `cfg.nodes[c]` (read `CAPITAL_BOUND` bit → ÷100 FRACTION compare vs `BARRIER_SANE_MAX_SL`=1.0 / `BARRIER_SANE_MAX_TP`=10.0) + the 2 legacy-array loops + the 3 flat-global kill-switch checks (validate the flat global DIRECTLY per the coverage-verify pin) → `CFG_FAULT_CAPITAL_OUT_OF_RANGE`. **Insert after `ControllerConfig_PopulateCoresFromFlat(&cfg)` in `_Load` (`ControllerConfig.hpp:3277`), before `return cfg`.** |
| caller-wiring | PENDING | — | `cfg_compile_ok` boot-check in the **backtest** (`BacktestSharded_Run`) + **GUI** loaders (main.cpp boot path DONE) |
| key clean-break (orig ③ scope) | PENDING | — | `core_→node_` cfg-key HARD-REFUSE + GUI parity guard + **delete the dead `CFG_PARSE_PCT`/`CFG_PARSE_MONEY` capital branches** (TD-215, H21 hygiene — they're walker-shadowed dead code) |
| tests | PENDING | — | pos/neg controls (see below) |
| ⑤ V-class | PENDING (#3) | — | asan/ubsan + determinism re-cert (the new field + the size-pin) |

---

## Coding-time discoveries (prior session — assume these shape what you hit)

### D-254-ADDENDUM: the MALFORMED gap (why ③ is a HYBRID, not just a sweep)
- **Catalyst:** mid-coding, before writing the sweep, I checked the founding mechanic against the sweep's detection-space. `Money_FromString("banana")` → `{value=0, flags=MALFORMED}` (`FixedPointN.hpp:1787-1793`); the walker HAS the flag but only `fprintf`-WARNed it (`CfgFieldDispatch.hpp`). Stored `0` is a **valid sentinel** (inherit / "no SL") → the post-resolve VALUE-sweep passes it (`0 ≤ cap`) → **the founding bug survives a value-only sweep.**
- **Resolution:** HYBRID — parse-point flag-capture for MALFORMED/OVERFLOW (abort + reasoned line) + post-resolve sweep for out-of-range. `EXCESS_DP` (>8dp, rounded-but-valid) stays a WARN. `wire_context` stays corrupt-only (a stamped historical value never faults on reload — B2). Operator-confirmed ("abort and give reasoning for the malformed lines").
- **Lesson (meta, AR-7 bump):** when a design SIMPLIFIES the validation SITE, verify the new site can detect ALL the original FAULT-CLASSES, not just the one in focus. The 4-agent gate + the coverage-verify both checked FIELD-coverage, never FAULT-CLASS-coverage. (Harvested → `meta-anti-pattern-index.md` AR-7.)
- **D-174c reconciliation:** ③'s malformed-capture is the FIRST CANONICAL of the prior-decided parse-flag-refuse — capital subset, ALL modes (stricter than D-174c's deferred LIVE-only-all-fields, which remains → E.2).

---

## TECH_DEBT scoreboard (③ surface)

| ID | Title | Status |
|---|---|---|
| TECH_DEBT-212 | Vacuous percent-clamp — out-of-range caught nowhere (the ③ founding finding) | IN-FLIGHT — closes when 4c (the out-of-range sweep) lands |
| TECH_DEBT-215 | Dead `CFG_PARSE_PCT`/`CFG_PARSE_MONEY` capital branches (walker-shadowed; H21 hygiene) | IN-FLIGHT — deletes at the key-clean-break step |
| TECH_DEBT-211 | Σ no-margin (Σ node allocations ≤ 100%) — re-homed to the cluster-aggregator | OPEN (→ E.5 cluster work; NOT ③) |
| TECH_DEBT-213 | `node_model_path` (+ STRING/FILE_PATH per-node fields) get ZERO validation | OPEN (adjacent; not ③ scope) |
| TECH_DEBT-214 | drift-gate `ml_tp_pct` inversion | OPEN (adjacent) |

---

## Capture-completeness (this session — nothing lost)

- **Decisions → log:** every operator-steered decision has a `D-N` entry. D-247→D-253 (the ③ design arc) + **D-254** (post-resolve-sweep shape) + the **D-254 ADDENDUM** (the malformed/hybrid + abort-MALFORMED/OVERFLOW + WARN-EXCESS_DP + D-174c reconciliation, written this session). D-251 (all-strategies-private) covers the privacy commit. No un-logged decisions.
- **Findings → ledger:** every finding homed. TECH_DEBT-211..**217** in `DOCS/tech-debt/open.md` (each with trigger/home; **216** = Check-K blind to ControllerConfig's void* fingerprint serialization, **217** = serve doesn't recompute/gate the cfg-fingerprint — both surfaced + homed in the close-out exhaustive re-enumeration after operator "TOP-N vs ALL" pushback); the malformed gap → the decision-log addendum + the AR-7/AR-8 meta-bumps. No unhomed debt.
- **Memories/skills → indexed:** `project_no_margin_trading.md` exists + indexed in `MEMORY_EXTENDED.md`. No new skills this session. The all-private restructure (#6) is queued, NOT yet a memory beyond D-251 (do at ship-close per task #7).
- **Meta-harvest:** AR-7 bumped in `meta-anti-pattern-index.md` (Session-17) — the value-sweep detection-space gap.
- **Verified:** `check_session_docs.sh` SWEEP CLEAN; `check_forward_promise_audit.py` only 3 pre-existing INFO markers; independent Stage-5.5 review CLEAN.

---

## Arming for the next action (step 4b — per-node malformed-capture)

The surface is **cfg-parse / per-core override + legacy-array idioms** (mirror of the flat-path step 3 you can copy from `03830e8`):
- **Route to:** `/trace-deps` (the `_PARSE_OV_PCT` + `node_N_*` strstr call-sequences) + `cfg-scope-discipline.md` + RECURRING_BUG_PATTERNS Class 25/26/27 (per-core cfg). The pattern is already proven in step 3 — 4b extends it to the two non-walker idioms.
- **Run (not just read):** `./build.sh test` (expect 3635/0) after each chunk; `python3 tools/check_per_node_registry_integrity.py` for the per-node cfg surface; `./tools/gen_code_map.sh` if you add symbols.
- **Read first:** `DOCS/STRATEGY_AND_CODING_RULES.md` (parse-path invariants) + the SSoT "③ CODING PROGRESS" section (the exact remaining-chunk list) + the D-254 addendum. Invariants in play: **H22** (per-node purity — validate the node's RESOLVED value), **H4** (Money math), **H9/H12** (the fingerprint size-pin you must NOT break — if you add a field, bump the 53056 pin + regen the golden).
- **Key code seams** (verified this session): walker `tt::cfg_parse_field` now takes `uint32_t* fault_out` (`CfgFieldDispatch.hpp`); the fault vocab is `CFG_FAULT_CAPITAL_{MALFORMED,OUT_OF_RANGE}` (`CfgFieldRegistry.hpp`); `cfg_compile_ok()` is at `ControllerConfig.hpp` (after the struct); the sweep insertion point is `ControllerConfig.hpp:3277` (after `PopulateCoresFromFlat`). The 3 flat-global kill-switches (Finding A) are global-only — validate the flat global directly in the sweep.

**Tests to write (step 7, pos/neg controls):** `risk_pct=999`→abort · `take_profit_pct=11.0`→abort · `take_profit_pct=2.0`→clean · `stop_loss_pct=banana`→abort · `core_N_` + `node_N_` variants · `0`=inherit preserved (NOT a fault) · wire-context NOT aborted · `EXCESS_DP`→WARN-not-abort. (Adversarial markers required on each capital test per the doc-CI.)

---

## Paste this into a fresh session to resume

```
I'm resuming E.1.1 ③ (the config-compiler / capital-cfg validation) for the v5.15-live-readiness sprint.

Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-22-E.1.1-cfg-config-compiler-coding-handoff.md

That loads the baseline (CLAUDE.md/local/MEMORY), the E.1.1 plan body, the decision log (D-247→D-254 + addendum), the SSoT "③ CODING PROGRESS" section, regens CODE_MAP, verifies engine HEAD 03830e8 + branch feat/v5.15-live-readiness + clean tree + 3635/0, and recreates the TaskList.

Then: ③ steps 1-3 are committed (build-gated). My NEXT action is step 4b — per-node malformed-capture (the _PARSE_OV_PCT override macro + the node_N_risk_pct/node_N_max_drawdown_pct legacy strstr branches), mirroring the flat-path capture in commit 03830e8. Then 4c (the out-of-range post-resolve sweep at ControllerConfig.hpp:3277), the backtest+GUI cfg_compile_ok boot-checks, the core_→node_ key clean-break + dead-macro deletion (TD-215), the tests, then ⑤ V-class.

Policy is firm: NO MARGIN (loss ≤100% / gain ≤1000%), ALWAYS-ABORT all modes (D2). Build-gated micro-commits (wip(.E.1.1): prefix). Consult Caramel before each substantive step; don't auto-proceed on findings.
```

## Quick links
- Plan body: `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md`
- Design SSoT: `plans/v5.15-live-readiness/plan_checks/2026-06-22-E.1.1-config-compiler-IA-cascade-synthesis.md`
- Decision log: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`
- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- Meta-harvest: `DESIGN_SPECS/meta-disciplines/meta-anti-pattern-index.md` (AR-7, Session-17)
- No-margin policy: `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/project_no_margin_trading.md`
