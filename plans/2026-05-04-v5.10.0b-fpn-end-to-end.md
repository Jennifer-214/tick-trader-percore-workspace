# v5.10.0b — FPN-end-to-end slow path (cross-build determinism)

**Date:** drafted 2026-05-02; ships after v5.10.0a
**Branch:** `feat/v5.10.0b-fpn-e2e` (new)
**Predecessor:** v5.10.0a
**Effort estimate:** 2-3 days
**Hot path:** UNTOUCHED (already FPN; this affects slow-path)

## Theme

Cross-build determinism. v5.10 design Idea #11. Within-build,
v5.9.2's replay-determinism test verifies bytewise identity. But
across builds (different compiler / -O level / FMA support), IEEE-754
double additions can reorder, producing different RegimeSignals
values → silently different predictions.

`RollingStats`, `RegimeSignals`, `RORRegressor`, `FlowFeatures` use
`double` today. ~500 LOC of double math to convert to FPN<F>. After
conversion: bytewise identical across any compiler/flags.

## Phases (each its own sub-tag)

### B.1 — FPN versions of RollingStats primitives (1 day)

- `RollingStats_Push` already FPN, but mean/variance/slope ops use
  double internally
- Convert: mean, variance, correlation, slope, EMA, EWMA all to FPN
- Snapshot tests: verify FPN values within epsilon of double values
  for known inputs (regression bound for the conversion)

**Files:**
- `ML_Headers/RollingStats.hpp` (~200 LOC convert)
- `tests/controller_test.cpp` — 8+ tests (per-primitive epsilon
  bounds)

**Tag:** `v5.10.0b.1`

### B.2 — RegimeSignals refactor + replay-determinism (1 day)

- `Strategies/RegimeDetector.hpp` — convert all double-typed
  RegimeSignals fields to FPN
- `RORRegressor` + `FlowFeatures` — same
- Re-run v5.9.2 replay-determinism test → bytewise identical (now
  WITHIN build, will be across builds too)

**Files:**
- `Strategies/RegimeDetector.hpp` (~150 LOC)
- `ML_Headers/ROR_regressor.hpp` (~50 LOC)
- `ML_Headers/FlowFeatures.hpp` (~100 LOC)

**Tag:** `v5.10.0b.2`

### B.3 — Retrain models + MODEL_FORMAT_VERSION bump (4-6h)

- Retrain reference models (feature distributions shift slightly with
  FPN math)
- Bump `MODEL_FORMAT_VERSION` 5 → 6 (refusal-on-load for pre-v5.10
  stamps; legacy stamps say "model format too old, retrain")
- Update `DOCS/RETRAIN_RITUAL.md` with v5.10 retrain procedure

**Files:**
- `ML_Headers/Limits.hpp` — `MODEL_FORMAT_VERSION` 5 → 6
- `ML_Headers/ModelInference.hpp` — verifier rejects format < 6 in
  strict mode

**Tag:** `v5.10.0b` (final)

## Risk register

- High-risk refactor: ~500 LOC double → FPN; potential precision
  bugs that pass tests but fail in real backtests
- All existing models invalidated (must retrain) — operator coordination
- FPN sin/cos for hour-of-day signals: not first-class.
  **DESIGN GATE before B.1 coding (per /plan-check 2026-05-02):**
  - **Option A — Lookup table** (RECOMMENDED): 1440-entry precomputed
    FPN array (24 hours × 60 minutes); O(1) lookup; ~23 KB memory;
    bytewise deterministic across builds. ~30 min implementation.
  - **Option B — Polynomial approximation:** Taylor series in FPN.
    ~6 multiplications per call; ~50 LOC. More compute; deterministic.
  - **Option C — Cast to double for sin/cos only:** loses cross-build
    determinism for hour-of-day signal; other features stay FPN.
    ACCEPTABLE since hour-of-day is slowly-varying coarse signal.
  - **Lock decision before coding B.1.**

## Verification

- Build with `-O2`, `-O3`, with/without `-DUSE_NATIVE_128`, with/without
  FMA — all produce bytewise identical replay-determinism results
- Reference model retrained matches per-core P&L within epsilon
  of v5.9 reference (sanity check that conversion didn't break
  semantics)

## Lower-cost alternative

Per design notes line 484-490: pin compiler flags into stamp
fingerprint (already covered by v5.9.5h #10 build_flags). Strictly
weaker (refuses load on mismatch but doesn't enable cross-build
inference) but ~3h instead of 2-3 days.

If operator deems cross-build deployment a hypothetical concern not
a current need, defer FPN-end-to-end to v5.11+ and just rely on
build_flags refusal.
