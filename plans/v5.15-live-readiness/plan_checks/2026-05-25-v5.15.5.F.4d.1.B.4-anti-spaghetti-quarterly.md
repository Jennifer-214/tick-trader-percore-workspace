# /anti-spaghetti report — 2026-05-25 — v5.15.5.F.4d.1.B.4 (quarterly cadence + Phase A pre-coding audit gate)

**Subject:** Codebase-wide quarterly /anti-spaghetti audit; doubles as `.B.4` Phase A pre-coding audit gate sweep
**Trigger:** quarterly cadence (last canonical 2026-05-17 at `.B.2` Batch 2 → 8 days ago; well within cadence) + `.B.4` plan body trigger
**Plan body:** `subplans/2026-05-24-v5.15.5.F.4d.1.B.4-train-serve-execution-layer-parity.md` v1.0
**Engine HEAD:** `64e7101` (v5.15.5.F.4d.1.B.3 ship close)
**Skill methodology:** `claude-skills/anti-spaghetti/SKILL.md` Stage 3 ACTIVE v1.1
**Scope:** codebase-wide (`/home/caramel/code/FoxML_Trader_v2/`)

---

## Phase 1 — Registry enumeration (fresh codebase scan)

**Total `#define FOREACH_*(X)` registries discovered: 65** (raw `rg` count; +2 since 2026-05-17 first-run because `.B.3` deleted `FOREACH_STAMP_BOUND_CFG` + `FOREACH_CFG_DERIVED_INFERENCE_CFG` and added `FOREACH_STAMP_BOUND_DERIVED_COHORT` meta-walker + `FOREACH_CFG_GATE_PER_CORE/GLOBAL` sidecar + `FOREACH_STAMP_RESULT_FIELD_EXCLUSION`).

**MetaRegistry enrollment count:** 64 rows (per `tools/check_meta_registry.py`).

**Tool output:**
```
[meta-registry-CI] WARN: Check 1: codebase macro `FOREACH_LEGACY_PREFIXED_KEY` not in FOREACH_REGISTRY — register or document as EXEMPTION
[meta-registry-CI] WARN: Check 1: codebase macro `FOREACH_STAMP_RESULT_FIELD_EXCLUSION` not in FOREACH_REGISTRY — register or document as EXEMPTION
[meta-registry-CI] ERROR: Check 2 FAIL: FOREACH_REGISTRY rows have no matching #define in codebase: ['FOREACH_STAMP_BOUND_DERIVED_COHORT']
[meta-registry-CI] ERROR: meta-registry check FAILED with 1 violations
```

This surfaces 3 H15 discipline gaps that warrant inclusion in `.B.4` Phase D ship close (none new; all introduced at `.B.3`):
1. **`FOREACH_LEGACY_PREFIXED_KEY`** at `ML_Headers/ModelInference.hpp:1307` — back-compat dispatch; never enrolled
2. **`FOREACH_STAMP_RESULT_FIELD_EXCLUSION`** at `MemHeaders/CfgGateRegistry.hpp:619` — sidecar; never enrolled
3. **`FOREACH_STAMP_BOUND_DERIVED_COHORT`** — META-walker takes `BASE_X` parameter (not `X`), so regex doesn't match `\(X\)` filter; row enrolled at `MetaRegistry.hpp:103` but CI doesn't see the `#define` — needs CI regex amendment OR registry shape exemption

(These are not Class 18/21/24/27/28 structural anti-patterns — they're discipline-bookkeeping items. Flag them under "Phase 6 — H15 enrollment drift" below.)

---

## Phase 2 — Per-registry cross-comparison (predecessor recap + delta since 2026-05-17)

### Closed since predecessor audit (CRITICAL-1 from 2026-05-17 first run)

The 2026-05-17 first canonical run identified ONE CRITICAL: the 3-way triplet `FOREACH_CFG_DERIVED_INFERENCE_CFG × FOREACH_CFG_DRIFT_CHECK × FOREACH_STAMP_BOUND_CFG` (93% row overlap; Class 18+21+14 mirror). **STATUS: CLOSED structurally at `.B.3`** via:
- `FOREACH_CFG_DERIVED_INFERENCE_CFG` registry body DELETED (file deleted; consumers moved to `cfg_derived::populate_inference_cfg_from_derived<F>` template fn walking master cfg field registry via `FOREACH_STAMP_BOUND_DERIVED_COHORT` meta-walker)
- `FOREACH_STAMP_BOUND_CFG` registry body DELETED (same shape — consumers moved to `cfg_derived::populate_stamp_cfg_from_derived<F>` template fn)
- `FOREACH_CFG_DRIFT_CHECK` still present but consumer moved to `cfg_derived::drift_check_from_derived<F>` template fn; row count reduced from 22 → small residual
- New `FOREACH_STAMP_BOUND_DERIVED_COHORT(BASE_X)` action-parameterized meta-walker at `CfgGateRegistry.hpp:227` = single source of truth for cohort coverage; drift impossible by construction

**CRITICAL-1 verdict: ✅ STRUCTURALLY CLOSED at `.B.3` (verified 2026-05-25). The cohort-meta-walker pattern (Class 18+21+14 closed at framework boundary) is now the canonical sister extension precedent.**

### Open from predecessor audit (HIGH-1 CoreCtx trio)

`FOREACH_CORE_CTX_INIT_FIELD × FOREACH_CORE_CTX_SUMMARY_FIELD × FOREACH_CORE_CTX_RESET_FIELD` trio: 20-of-20 SUMMARY ⊂ INIT; RESET ⊂ INIT subset. **STATUS: UNCHANGED.** No work landed since predecessor; queued at successor `.C` ship per first-run recommendation.

**HIGH-1 verdict: ❓ STILL OPEN; queue at `.C` confirms (matches first-run recommendation; no scope-creep).**

---

## Phase 3 — NEW findings (since 2026-05-17 + .B.4 surface)

### CRITICAL findings (1)

#### CRIT-1 — Train-serve execution-layer mirror at boot + slow-path-cycle (Class 18 at function-body layer)

**Surface:** `CoreFrameworks/EngineSharded.hpp:670-1160` (boot block) + `:3036-3320` (per-core slow-path-cycle body) vs `Backtest/BacktestSharded.hpp:140-260` (boot) + `CoreFrameworks/ShardedBacktestDriver.hpp:200-380` (`ShardedBacktest_RunTick` body).

**Evidence:**
- **74 total "Mirrors" comment citations codebase-wide** (`rg "Mirrors" --type=cpp`) — 12 in `BacktestSharded.hpp` cite EngineSharded_Run-specific lines; many cite line numbers (e.g., "`lines 999-1019`", "`lines 794-818`", "`line ~1117`") that go stale per-patch
- **Class 18 instance count:** 12 `Mirrors EngineSharded_Run` per `rg -n Mirrors Backtest/BacktestSharded.hpp` + 1 `Mirrors EngineSharded` in `ShardedBacktestDriver.hpp:308` ("`EngineSharded_Run line ~1117`")
- **Drift evidence (verified):** 6 PARITY entries (PARITY-026 closed 2026-05-24 hotfix; PARITY-027/028/029/030/031 still open) document train-serve execution-layer breaks accumulated over ~14 months

**Path γ shape:** Two parallel function bodies (live `EngineSharded_Run` + backtest `BacktestSharded_Run` boot block + `ShardedBacktest_RunTick` cycle body) encode same boot/slow-path-cycle sequence; per-patch drift between them produces silent train-serve identity breaks. Class 18 mirror at execution layer.

**Cross-ref:** `RECURRING_BUG_PATTERNS.md` Class 18 (mirror state/code); Class 21 (parallel descriptors); Class 24 (capability-cfg surface mismatch for `use_exit_model=1` capability — live path runs ML exit-prediction submit; backtest doesn't).

**Sister-precedent table (response-menu evaluation per `feedback_proportionate_response_to_audit_findings`):**

| Option | Description | Mechanical filter | Evaluation | Verdict |
|---|---|---|---|---|
| (A) INLINE MERGE | Continue per-patch comment-pin discipline; fix individual PARITY findings as they surface | 6 individual patches × per-patch drift recurrence = high cost over time | Discipline has FAILED — 4 CRITs accumulated over 14 months | REJECTED |
| (B) ACCEPT WITH RATIONALE | Document that train-serve mirror is appropriate (e.g., distinct semantics justify) | Implausible — execution layer has SAME semantics on both sides by design | NOT APPLICABLE | REJECTED |
| (C) FOLD INTO CANONICAL SISTER | Make backtest call live's `EngineSharded_Run` directly | Live spawns threads; backtest doesn't — fold breaks single-thread determinism | NOT APPLICABLE | REJECTED |
| (D) ARCHITECT NEW FRAMEWORK | Extract `EngineCommon_BootPerCore` + `EngineCommon_SlowPathCycleOneCore` shared helpers in NEW `CoreFrameworks/EngineCommon.hpp`; both call sites delegate | 6 findings + 12 mirror citations + future train-serve adds = 1 site (not 2) | Closes Class 18 mirror by construction; boundary-stable; sister to `STAMP_RESULT_DERIVED_FIELDS_AUTO_GEN` (Phase F first canonical of unconditional struct-field auto-gen extracted from twin mirror); same pattern lineage at function-body layer | **CHOSEN** |

**Structural fix:** `.B.4` plan body Decision A (a) ALREADY PROPOSES (D). This audit retroactively VALIDATES the choice per honest evaluation of all 4 options.

**Effort:** ~2-3 days focused per plan body estimate.

**Risk:** **MED-HIGH** per plan body classification (touches both boot paths + slow-path-cycle body; mirror across `EngineSharded` + `BacktestSharded` + `ShardedBacktestDriver`).

**Bug classes closed:**
- Class 18 (mirror) — STRUCTURALLY CLOSED at execution layer (boot + slow-path-cycle)
- Class 21 (parallel descriptors) — boot sequence + slow-path body collapse to 1 helper
- Class 24 (capability-cfg surface mismatch) — `use_exit_model=1` exercisable from both sides post-helper-extract
- NEW Class 33 candidate — "Comment-pinned `Mirrors X lines Y-Z` discipline that drifts per-patch" — codify at ship close as sub-class of Class 18 with detection signature `rg "Mirrors.*lines"` (74 codebase instances; structural close at execution layer means 14+ get deleted)

**Recommendation: CLOSE AT `.B.4` per plan body. The .B.4 plan body Decision A (a) is the correct response.**

---

### HIGH findings (2)

#### HIGH-1 — CoreCtx INIT/RESET/SUMMARY trio (carried over from predecessor; unchanged)

`FOREACH_CORE_CTX_INIT_FIELD (40 rows) × FOREACH_CORE_CTX_SUMMARY_FIELD (20 rows; ⊂ INIT) × FOREACH_CORE_CTX_RESET_FIELD (subset of INIT)`. Same shape, different consumer behaviors. **Recommendation: continue queue at `.C` per first-run rec; do not absorb into `.B.4` (different surface; would distract from train-serve focus).**

#### HIGH-2 — Future sister-helper-extract scope verification (POST-.B.4 forward-looking)

If `.B.4` lands `EngineCommon_BootPerCore` + `EngineCommon_SlowPathCycleOneCore` per plan body, the FUTURE-CADENCE 6 layers documented at `plan_checks/2026-05-24-future-asymmetry-sweep-cadence.md` will likely surface 5 ADDITIONAL helper-extract opportunities at:

| Layer | Predicted helper-extract surface | Predicted finding density |
|---|---|---|
| 1 OMS / order submission | OrderManager_BootCommon (init+log+pool sizing) | 2-4 mirror sites |
| 2 GUI panel parallelism | foxml_suite mirrors engine_gui — Panel_BootCommon | 5-10 mirror sites |
| 3 Boot / init triple entry | main.cpp + engine_gui + foxml_suite all init cfg loader + SHALT + PostLoadSetup | 3-5 mirror sites |
| 4 Logging / observability | CSV emit shape across trade/calibration/metrics paths | 2-3 mirror sites |
| 5 Stamp body PRE_CFG/POST_CFG model-const cohort | Class 32 prefix asymmetry residuals | 1-2 mirror sites |
| 6 DataStream parsing | Backtest replay parser vs live Binance WS parser | 1-2 mirror sites |

**This is METHODOLOGY confirmation, NOT a finding** — the predicted 6-layer cadence already exists at workspace. The audit's job is to confirm: **YES, the EngineCommon shared-helper-extract pattern from `.B.4` is the right shape to apply at each of the 6 layers post-`.B.4`. Per `feedback_proportionate_response_to_audit_findings` Option D, the cost-justification math holds at scale (each layer ~1 helper extract closes 2-10 mirror sites + future drift recurrence vector).**

**Recommendation: queue 6 layers at MASTER.md per existing cadence; no new structural-fix shape needed; M5 meta-discipline + `/train-serve-asymmetry-sweep <layer>` skill landing at `.B.4` ship close is the correct enabler.**

---

### MED findings (5)

| # | Finding | Surface | Action |
|---|---|---|---|
| MED-1 | `FOREACH_CFG_GATE_PER_CORE × FOREACH_CFG_GATE_GLOBAL` sister sidecars | `MemHeaders/CfgGateRegistry.hpp:78,103` | KEEP separate — different cfg surfaces (per-core vs global); same H18 sparse-sidecar pattern; sister to `FOREACH_DRIFT_OVERRIDE` cohort lineage. NOT parallel — sparse sidecars indexed by `FIELD_IDX_PER_CORE_*` vs `FIELD_IDX_GLOBAL_*` namespaces |
| MED-2 | `FOREACH_ENSEMBLE_POST_LOAD × FOREACH_SINGLE_ZOO_POST_LOAD` same shape | `ML_Headers/CoreModelZoo.hpp` | UNCHANGED since 2026-05-17 first run — SINGLE_ZOO still 1 entry; not yet worth meta-template. Re-evaluate if SINGLE_ZOO grows past 1 |
| MED-3 | `FOREACH_OMS_FIELD × FOREACH_OMS_PER_SLOT_FIELD × FOREACH_OMS_META_SLOT × FOREACH_OMS_STATE_FLAG × FOREACH_OMS_STATE_MULTI_BIT` 5-registry cohort | `MemHeaders/Oms*Registry.hpp` | KEEP separate — OMS surface has 5 distinct cohort dimensions (field-level / per-slot / meta-slot / single-bit / multi-bit) per OMS layer 1 predicted findings; if Layer 1 sweep post-`.B.4` finds shared-helper extract candidate AT the OMS layer, that's the appropriate ship for re-evaluation |
| MED-4 | `FOREACH_GLOBAL_CFG_FIELD × FOREACH_PER_CORE_CFG_FIELD` parallel | `CoreFrameworks/CfgFieldRegistry.hpp` | KEEP separate — `lives_in_struct` axis is intentional per `categorical-tag-applicability-pattern.md`; sister registries with `cfg_derived::*` template fns dispatching on the namespace difference |
| MED-5 | Bandit-side meta-X-macro `FOREACH_BANDIT_SIDE` | `ML_Headers/bandit_dispatch_table.hpp` | KEEP — first canonical of buy/exit symmetric meta-walker (TECH_DEBT-084 close at `.F.4d`); intentional shape per RIGHT direction |

---

### LOW findings (count summary)

- ~24 small enum-dispatch registries (STRATEGY / REGIME / SHALT / HALT_REASON / SP_SECTION / SESSION_PHASE / etc.) deliberately distinct domains. NO fold candidate.
- 5 X-macro sister registries (`POSITION_FIELD × POSITION_FIELD_SKIP_PERSIST`; etc.) intentional split-by-discipline. KEEP.
- 8 `Mirrors` comment citations in legitimate sister patterns (Backtest WF state replication, BNB-pay discount mirror, sister hyperparameter sweep) — NOT structurally drift-prone; intentional pattern documentation. KEEP per false-positive surface (M3 discipline).

---

## Phase 4 — Class 27/28 regression check

### Class 27 (silent OMS scalar cfg-mirror cache; closed at `.F.4c.3`)

Per `tools/scan_class_27_full.py` (run 2026-05-25):
- Mode A: 17 LOW matches (all are non-OMS subsystem-state surfaces — `EventLoopAggregates::max_drawdown_pct`, `GateParameters::param_max_age_ticks`, etc.)
- Mode B: 0 matches (no static-const fn-local cfg cache hazards)
- **HIGH: 0 / MEDIUM: 0** — no regressions in OMS surface since `.F.4c.3` closure. ✅ CLEAN.

### Class 28 (branchy SP/HP data-dependent dispatch; closed at `.F.4c.3` H20 codification)

Strategy/regime/model dispatch in SP/HP/drainer/producer paths: `rg "if \(strategy ==|if \(regime ==|if \(model ==" Strategies/ CoreFrameworks/ ML_Headers/`. Filtered against `__builtin_expect`-rare / tests / comments → 0 actionable new instances. ✅ CLEAN.

---

## Phase 5 — Class 18/19/21/24 instance check

| Class | Instance count | Status |
|---|---|---|
| Class 14 (scattered manual cfg) | 0 (handled via `FOREACH_*_CFG_FIELD` master registries) | ✅ CLEAN |
| Class 18 (mirror state/code) | 74 `Mirrors` citations; 12-15 in `BacktestSharded.hpp` train-serve mirrors → **CLOSE AT `.B.4`** via EngineCommon helper extract (CRIT-1) | YELLOW → GREEN at `.B.4` |
| Class 19 (hardcoded enum names) | 0 NEW (1 hit at `main.cpp:847` is event-log STRATEGY_SWITCH detail string emit; not gating) | ✅ CLEAN |
| Class 21 (parallel wide-variant) | 0 NEW (CFG_GATE_PER_CORE/GLOBAL = sparse sidecar per H18, NOT parallel-wide; closed by-design) | ✅ CLEAN |
| Class 24 (capability-cfg surface mismatch) | 1 — `use_exit_model=1` live-only (PARITY-027); closes at `.B.4` by-construction | OPEN → GREEN at `.B.4` |
| Class 27 | 0 NEW | ✅ CLEAN |
| Class 28 | 0 NEW | ✅ CLEAN |
| Class 30 (sibling array without registry) | 0 NEW (all `[MAX_EXECUTION_CORES]` arrays are within registered cfg structs OR per-core slow_state pointers) | ✅ CLEAN |

---

## Phase 6 — H15 enrollment drift (3 items; from CI tool output)

| # | Macro | Location | Issue | Recommendation |
|---|---|---|---|---|
| H15-1 | `FOREACH_LEGACY_PREFIXED_KEY` | `ML_Headers/ModelInference.hpp:1307` | Not enrolled in `FOREACH_REGISTRY`; CI WARN | Add 1 row at `MetaRegistry.hpp` → "Legacy prefixed-key back-compat dispatch (v1 stamps load on v2 engine; TECH_DEBT-101 deletion gate)" |
| H15-2 | `FOREACH_STAMP_RESULT_FIELD_EXCLUSION` | `MemHeaders/CfgGateRegistry.hpp:619` | Not enrolled; CI WARN | Add 1 row → "Stamp-result field exclusion sidecar (suppresses dead model-state cohort fields from ModelStampResult)" |
| H15-3 | `FOREACH_STAMP_BOUND_DERIVED_COHORT` | `MemHeaders/CfgGateRegistry.hpp:227` enrolled at `MetaRegistry.hpp:103` but takes `BASE_X` parameter (not `X`); CI regex `\(X\)` doesn't match | **Two options:** (a) amend CI regex at `tools/check_meta_registry.py` to recognize action-parameterized meta-walker shape; (b) wrap meta-walker behind `#define FOREACH_STAMP_BOUND_DERIVED_COHORT(X)` shim macro. Option (a) preferred per `.B.4` meta-discipline focus |

**Recommendation: bundle 3 H15 items into `.B.4` ship close Phase D (5-LOC fix; closes H15 CI failure that's blocking next ship's build verify).**

---

## Top-line verdict

**YELLOW (substantial structural close warranted at `.B.4`).**

The codebase has **ONE CRITICAL structural-close opportunity at execution layer** (CRIT-1 train-serve mirror) + **ONE HIGH carried over** (HIGH-1 CoreCtx trio queue at `.C`) + **ONE HIGH forward-looking validation** (HIGH-2 6-layer cadence helper-extract shape confirmation) + 5 MED keep-as-is + 3 H15 enrollment drift bookkeeping items + Class 27/28/30 all CLEAN.

CRIT-1 is **already targeted at `.B.4`** per plan body Decision A (a). This audit RETROACTIVELY VALIDATES the choice via honest A/B/C/D evaluation (per `feedback_proportionate_response_to_audit_findings`). The 2026-05-17 first-run methodology evolution + 2026-05-24 ML↔LIVE asymmetry sweep + this audit converge on the same structural fix shape — **the train-serve execution-layer Class 18 mirror has accumulated 14 months of drift evidence and warrants `.B.4` shared-helper-extract**.

The methodology that previously found these (`/parity-check`, `/trace-deps`, `/readiness`, `/dod-audit`, `/merge-scan`, even earlier `/anti-spaghetti` runs) **MISSED this layer entirely**. The M5 meta-discipline codification at `.B.4` ship close fills the audit gap (sister to M1/M2/M3/M4 — registry pair / cross-tool emit / anti-pattern false-positive / implementation-detail blind-spot). This is the structural close of a meta-gap, not just an instance.

---

## Recommendations (per finding)

| Finding | Action | Where |
|---|---|---|
| **CRIT-1** | CLOSE at `.B.4` — `EngineCommon_BootPerCore` + `EngineCommon_SlowPathCycleOneCore` shared helpers in NEW `CoreFrameworks/EngineCommon.hpp`; both EngineSharded + BacktestSharded + ShardedBacktestDriver delegate. Plan body Decision A (a) already encodes the fix | `.B.4` Phase B (per plan body) |
| **HIGH-1** | QUEUE at `.C` per first-run recommendation (do NOT absorb into `.B.4` — different surface) | `.C` umbrella |
| **HIGH-2** | CONFIRM 6-layer cadence at `plan_checks/2026-05-24-future-asymmetry-sweep-cadence.md`; M5 meta-discipline + `/train-serve-asymmetry-sweep <layer>` skill land at `.B.4` Phase D | post-`.B.4` cadence |
| MED-1..5 | KEEP separate per per-finding evaluation; no folds warranted | N/A |
| **H15-1/2/3** | BUNDLE at `.B.4` Phase D Step D.5 ship close (5-LOC fix; closes CI failure blocking next ship's build verify) | `.B.4` Phase D bookkeeping |
| Class 27/28/30 | CLEAN; no regressions; ✅ | N/A |

**Pre-coding audit gate verdict for `.B.4`:** GREEN-FOR-PROCEED. Plan body Decision A (a) is the correct shape. CRIT-1 closes at `.B.4` by-construction; HIGH-2 validates the post-ship cadence; MED + LOW are appropriately deferred/kept; H15 enrollment drift bundles into Phase D as bookkeeping.

---

## Cross-references

- Predecessor audits:
  - `plan_checks/anti-spaghetti-first-run-2026-05-17-v5.15.5.F.4d.1.B.md` (first canonical; CRITICAL-1 triplet found; closed at `.B.3`)
  - `plan_checks/anti-spaghetti-on-amendment-2026-05-17-v5.15.5.F.4d.1.B.1.md` (amendment-time audit on `.B.1` proposed infrastructure; GREEN verdict)
- `.B.4` plan body: `subplans/2026-05-24-v5.15.5.F.4d.1.B.4-train-serve-execution-layer-parity.md` v1.0 DRAFT
- Future-cadence companion: `plan_checks/2026-05-24-future-asymmetry-sweep-cadence.md` (6-layer post-`.B.4` sweep schedule)
- ML↔LIVE sweep that surfaced CRIT-1: `plan_checks/2026-05-24-train-serve-asymmetry-sweep.md`
- Skill spec: `claude-skills/anti-spaghetti/SKILL.md` Stage 3 ACTIVE v1.1
- Anti-pattern catalog: `DOCS/RECURRING_BUG_PATTERNS.md` (Classes 1-30 codified; 31-33 cited from CLAUDE.local.md, awaiting per-class sub-file codification)
- CI tool: `tools/check_meta_registry.py` (H15 enforcement)
- M5 meta-discipline codification target: `DESIGN_SPECS/meta-disciplines/train-serve-execution-layer-parity.md` v0.1 → v1.0 at `.B.4` ship close
- NEW skill at `.B.4`: `/train-serve-asymmetry-sweep <layer>` parameterized per 6-layer cadence
- Sister memories: `feedback_audit_canonical_sister_before_new_infra`, `feedback_proportionate_response_to_audit_findings`, `feedback_iteration_spiral_signals_audit_meta_gap`, `feedback_train_serve_execution_layer_meta_gap`, `feedback_motivated_collaborator_for_caramel`

---

**End of report v1.0 — 2026-05-25.** YELLOW verdict; CRIT-1 close at `.B.4` per plan body Decision A (a); H15 bundle into Phase D; HIGH-1 carried at `.C`; HIGH-2 cadence validated. Plan body Phase A pre-coding audit gate GREEN-FOR-PROCEED on this dimension.
