---
type: handoff
status: superseded
superseded_by: 2026-06-24-E.1.1-config-compiler-arc-handoff.md
ship_tag: E.1.1-item2-pernode
plan_type: live-readiness
sprint_end_goal: v5.15 live-readiness — make the codebase live-ready + more maintainable
ship_end_goal: "③ config-compiler — boot-time capital-cfg validation (ALWAYS-ABORT all modes / NO-MARGIN); the D-255/D-256 6-item scope. The 4 capital GLOBALS' founding bug is CLOSED; the PER-NODE founding bug is NEXT."
predecessor_handoff: 2026-06-22-E.1.1-③-item2-migrate-handoff.md
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, "plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md", "plans/v5.15-live-readiness/MASTER.md", "plans/v5.15-live-readiness/plan_checks/2026-06-23-E.1.1-③-item2-precoding-sweep.md", "plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (D-256)"]
coding_status: mid-coding-checkpoint (item-2 (a)-migrate + B1 LANDED; per-node b/c/d NEXT)
---

# E.1.1 ③ config-compiler — item-2 PER-NODE handoff

**Created:** 2026-06-23
**Target ship:** E.1.1 ③ config-compiler (the D-255/D-256 6-item capital-cfg validation scope)
**Branch:** feat/v5.15-live-readiness
**Plan file:** `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md`
**Sprint MASTER:** `plans/v5.15-live-readiness/MASTER.md`
**Item-2 SSoT:** `plans/v5.15-live-readiness/plan_checks/2026-06-23-E.1.1-③-item2-precoding-sweep.md` + **D-256** (decision-log `v5.15.5.F.4d.1.E-architecture-v2.md`)

**Engine state at handoff write (PUSHED — origin == HEAD):**
- HEAD: `98267be` (B1) · prior: `890b368` (item-2(a) migrate) · pre-session: `c6967be` (item-1 C-1)
- Branch: feat/v5.15-live-readiness · working tree: clean
- Test baseline: **3642** passed / 0 failed (verify: `./build.sh test`)
- gui build: ok · determinism gate: GREEN · pre-commit M/H/K: GREEN

**Workspace state:** 10 artifacts UNCOMMITTED at write → `/sync-workspace` pushes them at close (incl. this handoff). After sync, a fresh pickup runs `/accept-handoff` which resolves by the `status: active` tag (not the SHA).

---

## TaskList state at handoff write (recreate verbatim via TaskCreate)

| ID | Status | Subject |
|---|---|---|
| #1 | in_progress | E.1.1 ship — Core→Node rename + cfg clean-break + single_core deletion |
| #2 | in_progress | ③ config-compiler — D-255/D-256 6-item scope (item-1 LANDED; item-2 (a)+B1 LANDED; per-node b/c/d NEXT) |
| #3 | pending | ⑤ V-class — asan/ubsan + determinism re-cert (the rename + ③) [blocked by #2] |
| #4 | pending | E.1.1 ship-close — doc sweep + FEATURE_LOOKUP + cores-prose + TD-140 move |
| #5 | pending | E.1.2 — NodeState SoA / Money relayout (successor leaf) |
| #6 | in_progress | All-strategies-private restructure + capture (D-251) |
| #7 | pending | Consolidate ③ capture post-deep-pass (codification slate + homes) |

---

## What LANDED this session (EXHAUSTIVE — all items, not top-N)

### Engine (2 commits, PUSHED to origin)
1. **`890b368` — item-2(a): migrate the 4 capital globals into the registry.** `starting_balance`/`min_kill_loss`/`min_sl_tp_ratio` (`Money, KIND_DOUBLE`) + `min_hold_gain_pct` (`KIND_DOUBLE_PCT`) → `FOREACH_GLOBAL_CFG_FIELD` (`CfgFieldRegistry.hpp` +13). Deleted the 4 manual struct decls + 4 manual defaults + 4 manual parse-branches (`ControllerConfig.hpp` −19). **Size-NEUTRAL** (`static_assert(sizeof(ControllerConfig<64>)==53056)` HELD; offsets shifted). Clamps intentionally NON-binding (real gates = item-3/4).
2. **`98267be` — B1: unit-agnostic malformed-refuse.** Removed the `CAPITAL_BOUND_LOSS|GAIN` bit-gate from the malformed/overflow FATAL in `tt::cfg_parse_field`'s decimal branch (`CfgFieldDispatch.hpp`) → ANY decimal-`Money` field's malformed/overflow now boot-refuses. Purely additive (the 15 bitted fields unchanged; the NEW coverage = non-bitted Money incl. the 4 meta=0 globals). **This is what CLOSES the founding bug for the 4 GLOBALS.**

VERIFY: build.sh test **3642/0** (+4 default-contract +3 B1 cases) · gui ok · determinism net GREEN · pre-commit M(H14)/H(46 ids)/K GREEN.

### Workspace (10 artifacts — `/sync-workspace` pushes)
3. **D-256** (decision-log `v5.15.5.F.4d.1.E-architecture-v2.md`) — item-2 scope restoration (migrate + per-node) + B1-blessed + D2-resolved + the per-node-parser re-vet; paired `STATUS:` sentinels.
4. **AR-13** (`DESIGN_SPECS/meta-disciplines/meta-anti-pattern-index.md`) — forward-input-space under-enumeration; table row (:65) + detail (:161) + frontmatter count → 24.
5. **Fold-block E.2** (`...E.2-headless-configs-docs.md`) — the headless SIGUSR1/UDS re-read MUST gate on `cfg_compile_ok(new_cfg)`+keep-old (a 2nd cfg-poisoning re-entry; FS-3).
6. **Fold-block E.5** (`...E.5-real-subaccounts-capital-framework.md`) — post-floor dollar-Σ: PASTE-not-assert the 5 `<$1→$1` floor sites (Class-33 risk; FS-3).
7. **Fold-block E.1.2** (`...E.1.2-nodestate-soa-layout.md`) — fault field is **guaranteed-0-on-clean-load NOT "excluded"** (`Fingerprint_Compute` is void*+sizeof; the `assert(==0)` at every call site IS the guarantee) + ADD the missing `static_assert(sizeof==N)` (size-pin alone is VACUOUS for a size-preserving relocation — the assert catches the poisoned-cfg fingerprint). FS-1 correction.
8. **Fold-block E.1.5** (`...E.1.5-per-cluster-purity.md`) — the "③ lands B → escalate" branch is PRE-EMPTED (③ lands A+); do NOT re-escalate — VERIFY the per-node validation landed (the H22-candidate CI row is Class-51-vacuous if built on the OLD substring matcher). + the NEW-1 `ControllerConfig_IsLiveCapital` relocation cites corrected (`:2062`/`:577`). FS-1.
9. **TD ledger** (`DOCS/tech-debt/open.md`) — TD-215 re-partition (the 4-field slice now CLOSED; bulk remains, incl. the dead `CFG_PARSE_MONEY_POS` macro) + TD-216 sharpen (size-pin vacuity for relocation) + TD-221 sharpen (B-Plus one-id-per-line limitation).
10. **The sweep doc** (NEW: `plan_checks/2026-06-23-E.1.1-③-item2-precoding-sweep.md`) — forward-seam coverage matrix + per-node-parser re-vet + the 7 pins + GREEN-to-code verdict. **The item-2 SSoT.**
11. **`DOCS/CODE_MAP.md`** — regen'd POST-coding (819 fns; line-cites current after the −19).
12. **`tests/controller_test.cpp`** (private, workspace-tracked) — `test_item2_capital_migrate_defaults` (ADV-REFUTE-marked): 4 default-contract assertions (incl. `min_hold_gain_pct` ÷100 → `MQ(0.001)`) + 3 B1 founding-bug-close cases (malformed→fault + cfg_compile_ok false; clean→ok positive control).

---

## Per-Step status (item-2 + the ③ roadmap)

| Step | Status | Landed at / Blocked by |
|---|---|---|
| item-1 (single-source gate + backtest/suite wire) | LANDED | `c6967be` (prior session) |
| item-2 (a) migrate 4 globals | LANDED | `890b368` |
| **B1** unit-agnostic malformed-refuse | LANDED | `98267be` |
| **item-2 (b)/(c)/(d) per-node coverage** | **NEXT (ready)** | — |
| item-3 dollar >0/sane | PENDING | item-2 |
| item-4 per-field ≤100% (barrier_is_corrupt) | PENDING | item-2/3 |
| item-5 Async.hpp:321 reload gate | PENDING | item-2 |
| item-6 recurrence guard | PENDING | items 3-4 |
| ParseFast `*_checked` (RAW channel) | PENDING | item-2(b) |
| ⑤ V-class (asan/ubsan + determinism + calls_graph) | PENDING | ③ complete |
| ③ ship-close codification slate | PENDING | ③ complete |

---

## NEXT ACTION (paste into a fresh session)

```
I'm picking up E.1.1 ③ config-compiler — item-2 PER-NODE coverage (b/c/d), v5.15-live-readiness.

## Step 0 — run /accept-handoff
/accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-23-E.1.1-item2-pernode-handoff.md

This loads the baseline + nav-infra, verifies git (engine HEAD 98267be PUSHED, 3642/0),
recreates the TaskList, regens CODE_MAP, and routes the next action's surface kit.

## The next action = item-2 per-node coverage (b/c/d) — close the PER-NODE founding bug
The 4 GLOBALS are done (migrate 890b368 + B1 98267be: a malformed global refuses boot).
B1 lives at the WALKER (tt::cfg_parse_field), which does NOT reach the per-node parser —
so the per-node overrides + legacy arrays still silently mis-store malformed/out-of-range:
  (b) route _PARSE_OV_PCT / _PARSE_OV_RAW overrides through IN-MACRO flag-capture
      (NOT cfg_parse_field — that would import the winsor_pct_high Class-48 sentinel bug);
  (c) hand-roll the 2 legacy arrays node_N_risk_pct / node_N_max_drawdown_pct
      malformed-capture NOW (D-255 D1, not deferred to WIP2g);
  (d) fold the 7 strstr -> exact-suffix-match at ControllerConfig.hpp:2975,
      preserving node_strategies_explicit_set |= 1<<idx (:2962) in the SAME commit.

## The 7 RE-VET PINS (CRITICAL — carry all 7)
1. IN-MACRO flag-capture, NEVER cfg_parse_field for the per-node overrides (Class-48 trap).
2. item-2 = MALFORMED-only; the <=100% cap is item-4 (do NOT re-state barrier literals here = Class-43).
3. ONE shared CFG_FAULT_CAPITAL_MALFORMED bit (no parallel mirror); _PARSE_OV_PCT is the LARGER hole.
4. List-C MUST add a NON-VACUOUS test: core_N_winsor_pct_high=0 -> inherit, NOT clamp-to-0.5
   (the planned stop_loss_pct=0 test is VACUOUS — clamp_min=0 already; Class-51).
5. parse_double_fast_checked is NET-NEW (RAW channel; empty-string = clean-inherit, not a fault).
6. preserve the strategy-bitmap set (:2962) + delete the 7 strstr in the SAME commit.
7. determinism is PARSE-PATH-ONLY (no hot/slow-path touch).

Build-gated micro-commits (wip(.E.1.1): prefix). Consult Caramel on anything CRITICAL; capture
specs/anti-patterns as you go. SSoT: the item-2 sweep doc + D-256.
```

---

## Arming for the next action (surface kit — item-2 b/c/d is the per-node cfg PARSER)

- **Route to (suggest, await go):** `/trace-deps` (the per-node parser call-sequence + the _PARSE_OV_* macro consumers) + `/dod-audit` (registry/macro fit) + `cfg-scope-discipline.md` (per-node vs global). A decision (e.g. the shared-bit shape) → `/decision-check`.
- **Run (not just read):** `./build.sh test` (3642 baseline +N) · `tools/check_per_node_registry_integrity.py` · `python3 tools/check_capital_adversarial_audit.py` (List-C markers) · regen `./tools/gen_code_map.sh` · the doc floor `tools/check_session_docs.sh` at close.
- **Read first:** `DOCS/STRATEGY_AND_CODING_RULES.md` (parse-path) + `RECURRING_BUG_PATTERNS.md` **Class-48** (sentinel destruction) + **Class-43** (literal re-statement) + **Class-51** (vacuous guard) + **Class-18** (mirror drift) + H22 (per-node purity) + H4 (money math) + H9/H12 (the fingerprint surface — the guaranteed-0 fold).
- **Files:** `CoreFrameworks/ControllerConfig.hpp` (the per-node parser; the `_PARSE_OV_*` macros + the 2 legacy arrays + the 7 strstr @:2975 + the strategy-bitmap @:2962) · `CoreFrameworks/CfgFieldDispatch.hpp` (the walker / `CFG_FAULT_CAPITAL_MALFORMED`).

---

## THEN (the rest of ③ + close)

- **item-3** dollar `>0`/sane (covers the dollar-fields' malformed→0 case the >0 check subsumes).
- **item-4** per-field ≤100% caps via `tt::barrier_is_corrupt` (closes **TD-212**; REUSE the primitive, never re-state the literals = Class-43; **A2's barrier domain/units concern is HOMED here** — settle by code-read whether `barrier_is_corrupt` conflates stored-fraction with the registry percent clamp_max).
- **item-5** `Async.hpp:321` GUI-reload gate (cfg_compile_ok + keep-old). **item-6** recurrence guard. **ParseFast `*_checked`** (the RAW channel; item-2(b) introduces it).
- **⑤ V-class** — asan/ubsan + determinism re-cert + `calls_graph_diff` on the shipped ③ + the rename (use the `v-class` agent / DoD). **RE-RUN the Stage-5.5 independent review** here (it was API-blocked this close — see dispositions).
- **③ ship-close codification slate** — `hierarchical-config-validation-pattern` DESIGN_SPEC + the `uniform-bound-over-heterogeneous-cohort` anti-pattern + FEATURE_LOOKUP entry. (Task #7.)

---

## Capture-completeness (this session — nothing lost)

- **Decisions → log:** **D-256** (the session's one operator-steered decision: item-2 scope restoration + B1-bless + D2-resolution) — logged with paired sentinels. No other operator-steered decisions this session.
- **Findings → ledger:** TD-215 (re-partition + dead `CFG_PARSE_MONEY_POS` macro homed to its bulk-delete) · TD-216 (sharpen) · TD-221 (sharpen). The B-Plus FP → TD-221. The fingerprint-epoch shift → documented in the E.1.2 fold-block + D-256 (free per no-live-models). All homed.
- **Memories/skills → indexed:** none new this session (AR-13 is a `meta-anti-pattern-index` row+detail — self-indexed in that catalog, not a `memory/` file; no new skill).
- **Verified:** `check_session_docs.sh` = 15/16 (the 1 RED = accepted TD-221 B-Plus FP — pre-existing E.2/E.5 design-sketch + line-drift, 0 real fabrications in session content); capture-audit-mechanical + decision-log-completeness + capital-test-markers checks all GREEN. CODE_MAP regen'd. Engine pushed.

---

## Accepted dispositions / known issues (carry forward)

- **B-Plus sweep RED = TD-221** (accepted tooling FP): the whole-file re-scan flags the PRE-EXISTING E.2 (2) + E.5 (7) v0.1-draft design-sketch code-blocks as "fabrications" + line-anchor drift from this session's coding moving code. **0 real fabrications in session content** (the fold-blocks are prose; the sweep doc is 0-fab). Sweep otherwise 15/16 GREEN. Do NOT chase — it's the documented TD-221 limitation.
- **Dead `CFG_PARSE_MONEY_POS` macro** (0-user after the migrate) — homed to the **TD-215 bulk-delete** (whitespace-fragile X-macro; batch it with the other dead CFG_PARSE_* branches, carefully).
- **findings-doc CONVERGED-PLAN item-2 prose** ("thread fault_out / hand-roll the macros") is the **PRE-D-256 reframe snapshot — SUPERSEDED** by D-256 (= MIGRATE + in-macro flag-capture). D-256 + the sweep doc are the LIVE SSoT; the findings-doc is historical.
- **cfg-fingerprint EPOCH shifted** (offsets moved) — but **no committed golden pins it** + determinism gate GREEN → dev model fixtures re-stamp free (per `project_no_live_models_dev_test_only`). Nothing to regen; documented.
- **Stage-5.5 independent review BLOCKED by 3 API transients** (529/500/529) → **self-verified PASS** instead (all deliverables landed substantive + cross-coherent + privacy-correct + no fabrication; the only missing layer is the INDEPENDENCE of the content check). Re-runnable against HEAD — fold it into the ⑤ V-class.
- **sweep-doc line-cites drifted** post-coding (e.g. `CfgFieldDispatch.hpp:100-112` changed by B1; `ControllerConfig.hpp:366` deleted) — the sweep doc is a pre-coding SNAPSHOT; THIS handoff carries the current cites. Don't trust the snapshot's line numbers; re-grep.

---

## Quick links
- Item-2 SSoT: `plans/v5.15-live-readiness/plan_checks/2026-06-23-E.1.1-③-item2-precoding-sweep.md`
- Decision log: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-256)
- Plan: `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.1-core-node-rename.md`
- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- Predecessor handoff (superseded): `2026-06-22-E.1.1-③-item2-migrate-handoff.md`
- Meta-anti-pattern catalog (AR-13): `DESIGN_SPECS/meta-disciplines/meta-anti-pattern-index.md`
