---
type: handoff
status: superseded   # → 2026-08-17-E.1.2-D426-stamp-put-guard-blocked-on-cpp-standard-handoff.md. Its NEXT ACTION (task #10, the stamp-key enrolment) is DONE and BLESSED, and its framing of the step-6 fork is superseded: a 3-agent adversarial pass refuted a FOURTH design and reordered the queue behind two live defects (D-426).
superseded_by: 2026-08-17-E.1.2-D426-stamp-put-guard-blocked-on-cpp-standard-handoff.md
ship_tag: v5.15.5.F.4d.1.E.1.2
plan_type: coding
supersedes: 2026-08-15-E.1.2-D421-steps2-4-5-complement-guard-handoff.md
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-08-15-E.1.2-D421-steps2-4-5-complement-guard-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
audit_tier: HIGH
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: freeze the capital-state WIRE on the registry-driven NodeContext persist serializer; every E.1.3+ spine reads this layout
sister_specs: [meta-disciplines/advertised-capability-never-exercised.md, meta-disciplines/dead-code-and-identifier-retirement-discipline.md, framework-patterns/registry-coverage-ci-check-pattern.md, meta-disciplines/calibration-corpus-non-vacuity-discipline.md]
coding_status: "D-421 step 6 Tier 0 LANDED (fees group deleted). D-422 (unwired-capability register) + D-423 (exit-side learning loop) + C-424 (register risk CLOSED) all landed. REMAINING: step 6 Tier 2-vs-Tier 1 fork (operator choice), step 7 (FORMAT: + TradeReader), the 65-registry domain classification, the E.1.2 close-gate. Register residual is CLEANUP, not a blocker."
---

# E.1.2 — the unwired-capability register, two capital bugs, and the exit-side learning loop

## State at write — ANCHORS, not counts

| Repo | HEAD | Window this session | Remote |
|---|---|---|---|
| Engine `feat/v5.15-live-readiness` | `f2a6321` | `b7ba7c4..HEAD` | pushed |
| Workspace `main` | `d451d5f` + this close | `cd9ab3c..HEAD` | pushed at write; close commits follow |
| Plugin `fox-symdeps.nvim` (own repo) | `77ab06e` | merged to `main`, both pushed | pushed |

Untracked engine scratch (`notes_update.md` `resume` `tatoo.md` `tmpb924ezcj.cpp`) — operator files, keep.

## Re-derive at pickup (every number below comes from these, none from prose)

```bash
./tools/check_session_docs.sh                     # expect SWEEP CLEAN
./build.sh test && ./build/controller_test        # expect BUILD 0 + suite green
./tools/check_determinism.sh                      # expect GREEN (wire content changed this session)
python3 tools/check_cache_layout.py --strict-new   # expect GREEN (structs shrank; DERIVED tags refreshed)
python3 tools/check_identifier_retirement.py      # expect GREEN
git -C . log b7ba7c4..HEAD --oneline              # the engine window
```

⚠ **Suite count moved BOTH directions this session and that is correct.** It rose to 3762 as fixes
landed, then fell to **3755** when the `fees` removal deleted 7 assertions that were testing the
removed group — including round-trips of fields that emitted zeros. **Those tests were passing on
false data.** Do not "restore" them.

---

## ⚠️ READ FIRST — step 6's shape changed THREE times. Do not build the refuted ones.

| Design | Verdict |
|---|---|
| Generic per-bit gate-reachability **scanner** | **REFUTED by prototype** — green on `inference_cfg` (the real CRITICAL), RED on `feature_mask`. Inverted on both headline cases |
| Tier-2 "require a `STAMP_EMIT_PRODUCER_<G>` marker" | **NO-OP** — verified by construction: an undefined group dispatcher ALREADY fails to compile (the emit walk token-pastes `STAMP_EMIT_CHECK_HAS_##group`) |
| Bit-keyed **producer sidecar** | **REFUTED** — H18 says COLUMN-not-sidecar for a dense mandatory property (and D-421's own meta-registry spec already ruled this identical fork); per-BIT is the wrong granularity |

**Why per-bit is wrong, and it is the load-bearing lesson:** the `fees` bit HAD a producer, so any
bit-scoped check graded it GREEN while two of its rows emitted zeros into a signed body. **A coverage
check over an emitted format must key on the WIRE KEY, not the group that gates it.** Codified as
H21 spec **Rule 1a**.

**What survives — the a-class ordering:** Tier 0 (DONE) → **Tier 2 (dissolve the emit-side bitmap)**
or, only if Tier 2 is out of scope, **Tier 1 (per-wire-key partition checker, shaped so Tier 2
DELETES it)**. Tier 2 = gate each row directly against `args`/`cfg` at emit time, no `inf`, no group
bit → the defect class becomes **unrepresentable**. The sibling half already works this way in
production (`cfg_derived::populate_stamp_cfg_from_derived`, `MemHeaders/CfgGateRegistry.hpp:425-440`).
Cascade is **~44 declarations in one file**, all `tests/controller_test.cpp` — and they are the
Class-58 fixtures the tree already flags, so rewriting them is the fix, not collateral.

**This is the operator's open fork.** Given the standing "do it right over quick fix" call, Tier 2
is the consistent answer, but it reopens the H9 verification surface and deserves a fresh head.

---

## What landed

**Two capital-path bugs, both caused by a comment asserting a guard that was not there.**

1. **Uninitialized stack → `tp_pct`/`sl_pct`.** `MASK_EZOO_BANDITS_READY` means "the init pass ran",
   not "bandits are usable": `InitBandits` sets it and returns early WITHOUT `Bandit_Init` when
   `n_arms < 2`. `Bandit_GetProbabilities` then writes NOTHING (both loops are `i < n_arms == 0`), so
   `weights_buf` reached the barrier blend uninitialized. Fixed by guarding select on
   `primary_count >= 2` — the same test `InitBandits` itself uses. Not sanitizer-catchable: that
   function carries `no_sanitize("address")`, and ASan does not detect uninitialized STACK anyway.
2. **A live-readiness boot gate green on a model of ANY age.** `training_timestamp_us` had no
   producer → no stamp carried it → the age-warn block never ran → `check_model_max_age_set` returned
   TRUE unconditionally. Opting IN to age gating produced a meaningless green.

**D-422 — the unwired-capability register.** 44 (corrected to 42) of 816 engine functions have only
test callers. Register at `plan_checks/2026-08-16-unwired-capability-register.md`.

**D-423 — the exit-side learning loop, closed and VERIFIED.** Three bandit systems, three verdicts:
legacy = full loop on a dead path (the operator's remembered instance, correct); sharded buy =
**works**; sharded exit = **was write-only**. Select wired via `BanditAlgorithm_Apply` (covers all
five algorithms; inherits the AVX-512 kernel + H10 scalar fallback by routing through the shared
function). Attribution re-pointed at the arm the bandit acted on. **⚠️ Closure is CONDITIONAL and the
first draft did not say so (independent review, 2026-08-16):** when the exit RIDGE blender is also
enabled, `StrategyParameters.hpp:1583-1585` OVERWRITES every bandit weight with Ridge's, so the blend
at `:1591-1593` is Ridge's decision while `:1595` still reports the BANDIT's arm as dominant — i.e.
in that configuration the bandit is back to write-only AND mis-attributed, which is verbatim the
pathology the fix's own comment at `:1517-1520` describes. Not a regression (the pre-change
attribution was argmax-of-raw-prediction, also not Ridge's), but the loop closes only with the exit
blender OFF. Thompson persistence restored on buy + exit + periodic; **the BACKTEST-side Thompson
save is still open** (`EnsembleModelZoo_SaveThompsonState` has exactly two callers,
`NodeModelZoo.hpp:3312` and `CoreFrameworks/EngineSharded/Run.hpp:2357`; nothing under `Backtest/`) — see TaskList #9. `bandit_enabled` wired + re-defaulted to 1.

**C-424 — register risk CLOSED.** Swept every remaining flagged function for a comment CLAIMING a
live consumer: **ONE hit, and it is truthful.** Nothing left is pretending to work → residual is
cleanup, E plan resumes.

**Tier 0 — the `fees` group deleted** (re-derive the touched set: `git show 5d45ecc..f2a6321 --stat -- ML_Headers/ CoreFrameworks/ tests/`). Deleted rather than tombstoned because every H21
condition was checked against a NAMED search space, not assumed. Re-derive both legs:

```bash
rg -n 'STAMP_BIT_fees|MASK_fees' ML_Headers/ CoreFrameworks/ Backtest/ tests/ tools/
#   -> exactly ONE hit, and it is the tombstone comment itself
#      (ML_Headers/StampBoundModelConstRegistry.hpp — the retired-names note). No live referent.
python3 tools/check_identifier_retirement.py     # -> GREEN, 48 identifiers match the ledger
```

The NAMES stay retired via that registry comment.

> ⚠️ **CORRECTED 2026-08-16 by independent review — read this before reusing the pattern above.**
> An earlier draft said *"the guard being GREEN is the load-bearing half: it proves no persisted/wire
> identifier was renumbered, reused, or silently dropped."* **That was false.**
> `tools/check_identifier_retirement.py` carries a CLOSED `SOURCES` list of 12 identifier families
> (snapshot/model/bandit/stamp FORMAT versions, `FOREACH_{BANDIT_ALGORITHM,STRATEGY,REGIME,SHALT,
> NODE_STATE_FLAG}`, two wire-width constants). **There is no row for
> `FOREACH_STAMP_BOUND_MODEL_CONST`, none for `STAMP_BIT_*`, and none for stamp wire keys.** The
> guard never inspected the registry this deletion touched — it would have printed the identical
> GREEN if the entire registry had been deleted. Class 51, and exactly the manufactured confidence
> this session spent the day cataloguing.
>
> **What actually clears the deletion** is the `rg` leg plus a tree-wide enumeration: no live
> referent survives, all three token-paste dispatcher families
> (`STAMP_EMIT_CHECK_HAS_*` / `STAMP_PARSER_SET_HAS_*` / `STAMP_AUTOPOPULATE_SET_HAS_*`) are
> consistently missing their `fees` variant, and a `--no-ignore --hidden` sweep plus a scan of the
> real `models/**/*.stamp` artifacts finds the retired keys in comments only. That is a sound
> conclusion reached by enumeration, not by the guard.
>
> **Now partially enforced:** the three burned names (`STAMP_BIT_fees`,
> `inference_cfg_fee_rate_maker`, `inference_cfg_fee_rate_taker`) are enrolled in `RETIRED_NAMES`,
> so a re-introduced spelling is a Knight-Capital red instead of a fresh "ADD (ok)".
> **Still owed, and it is a PREREQUISITE to TaskList #1, not a follow-up:** a `SOURCES` row for the
> stamp registry (~58 identifiers ⇒ needs a blessed `--update` at the operator TTY). Until that
> lands, do NOT read a GREEN from this guard as H21 coverage of a stamp-body change.

---

## ⚠️ OPEN — operator decisions, none blocking

- **Step 6 Tier 2 vs Tier 1** (above). The fork.
- **The cfg-derived drift-gate fork** (per-field / emit-the-bit / retire) — D-421 sequenced this
  AFTER step 6 deliberately, and we now know far more about what else is dead than we did.
- **`MBS_OrderSetBanditContext`** — the register's one surviving real find. The GETTER is read in
  production (`CoreFrameworks/OrderManager.hpp:839`) to pick which regime's per-arm Exp3 probabilities
  to log; the SETTER is never called, so `bandit_regime` is always 0 and **every calibration row
  attributes probabilities to regime 0**. Telemetry-grade, but it corrupts exactly the per-regime data
  needed to judge the now-live loops.
- **The bandit loop's PROFITABILITY is unproven.** The loop CLOSING is verified (Test C.3c, production
  calls both halves). Whether exit-bandit selection *improves* exit PnL needs an
  `exit_bandit_enabled=1` backtest vs the uniform baseline — a different kind of question.
- **`CLAUDE.md` always-loaded budget** is near its cap — run `python3 tools/check_always_loaded_budget.py` for the live reading; it is the only honest source, since the figure moves with every doc edit.

## Also queued

- **Step 7** — `FORMAT:` token, then `GUI/TradeReader.hpp` (which has its own known-broken legacy-schema read).
- **Domain classification** of the 65 baselined registries — the natural fan-out surface. Still blocked on the `STRUCT:` subset notion (TECH_DEBT-274).
- **E.1.2 close-gate** — `NPF_PROJECT_POISON` 4th projection + FEATURE_LOOKUP + postmortem + `/ship`.
- **TECH_DEBT-284** (H16 unenforceable) · **TECH_DEBT-283** (`ring_push_failures` write-only counter).

## Arming for the next action (M8)

- **Audit posture:** `audit_tier: HIGH` for Tier 2 (capital-adjacent wire surface). Tier 1 is mechanical.
- **Skills:** `/decision-check` on the Tier-2-vs-Tier-1 fork · `/bug-check` before commit · `/parity-check` if the stamp body changes shape.
- **Tools to RUN:** `check_session_docs.sh` · `check_determinism.sh` (wire) · `check_cache_layout.py --strict-new` (struct shrink) · `check_identifier_retirement.py` · `calls_graph_diff.sh`.
- **Anti-patterns in play:** Class 58 (all sub-shapes, incl. the NEW function-level one) · Class 51 · Class 12 · Class 44.
- **Invariants:** H21 (+ NEW Rule 1a) · H9 · H12 · H15 · H22.
- **WILL BITE:**
  - `rg <pat> .` from the engine root is **BLIND** to `tests/`/`tools/`/`plans/` and **NO flag rescues it** (Landmine 19). Name roots explicitly.
  - This shell is **zsh** — it does NOT word-split an unquoted `$VAR`; `rg pat $ROOTS` passes one bogus path and prints nothing.
  - `Fn\s*\(` misses **explicit template args** (`Fn<F>(...)`) — this read a live capital function as dead.
  - Macro-pasted names (`NODE_STATE_FLAG_SET(node, MODEL_CORRUPT)` → `MASK_NODE_STATE_MODEL_CORRUPT`) exist nowhere as literals.
  - `/tmp` is **noexec** — build probes in-tree.
  - `bless.py` needs a REAL terminal; the `!` prefix captures stdout so `_isatty()` is false.

---

## Capture-completeness — the JUDGMENT ledger (Stage 6.5.2)

- **Check 2 — plan-body currency.** DONE. D-422/D-423/C-424 minted with STATUS sentinels; the register carries its own RISK-CLOSED section.
- **Check 3 — decision-log artifact.** PRESENT. Three new entries (D-422, D-423, C-424), each with a paired STATUS.
- **Check 5 — handoff currency.** THIS doc supersedes the 2026-08-15 one; reciprocal frontmatter set; singleton guard in the re-derive block.
- **Check 6 — M7 escalation candidates.** ONE recorded: `check_forward_promise_audit.py`'s teeth were invoked by NOTHING for six days — now wired HARD, and the general rule ("every `--selftest` needs a standing invoker") is codified in `calibration-corpus-non-vacuity-discipline.md`.
- **Check 7 — DESIGN_SPECS promotion.** `registry-coverage-ci-check-pattern.md` 3-first-canonical → **4-cohort**, grounded explicitly on the LANDED applications only (App 5 is DESIGNED-NOT-LANDED and is named as NOT part of the basis).
- **Check 9 — memory ↔ spec sister completeness.** No NEW memory files this session. All codification went into existing specs (H21 Rule 1a, the ENGINE-CODE surface, the non-vacuity recurrence) — deliberate, since none of it is operator-collaboration guidance.
- **Check 10 — going-forward-rules currency.** No new Tier-0 rule. The session's disciplines were applications of existing ones.
- **Check 12 — amendment cascade.** Class 58's detection signature CORRECTED in place (it recommended the refuted shape); the cascade to handoff/MASTER/decision-log was done by hand and is now mechanically detectable via the NEW `check_amendment_cascade.py`.

**Meta-harvest (Stage 4.5):** no NEW meta-anti-pattern row. Every shape hit was an existing one
(AR-17 stale cites ×3, AR-8 self-attestation, M9 under-enumeration) — a quiet harvest, which per the
anti-ceremony guard is the signal to write nothing.

**Tech debt worked, not stacked:** TECH_DEBT-148 CLOSED (blessed at a real TTY). TECH_DEBT-283 + -284
NEW, both with triggers + acceptance. TECH_DEBT-282's trigger FIRED (a non-registry recurrence) and is
recorded. TECH_DEBT-240 independently confirmed by the register.

## Independent review (Stage 6.5.4) — RAN, and it refuted three load-bearing claims

**Status: RUN 2026-08-16** (a-class, read-only, independent of the author). It was skipped once for
context budget, the operator asked "is the handoff and close-out good?", and running it was the answer.
Verdicts: gaps-honestly-stated **CONFIRMED** · everything else **PARTIAL**, with completeness of the
uninit-stack fix **REFUTED**. Every gate it re-ran came back green and matching (suite 3755/0 exact,
determinism GREEN, cache-layout GREEN, identifier-retirement GREEN/48, sweep CLEAN); it found no
fabricated symbol and no false gate result. The defects were in the REASONING, not the numbers.

**What it refuted, all three now fixed or homed:**

1. **The H21 "proof" was vacuous** — this handoff claimed *"the guard being GREEN is the load-bearing
   half."* `check_identifier_retirement.py` has a CLOSED 12-family `SOURCES` list with **no row for the
   stamp registry**, so it never inspected what was deleted; it would print the same GREEN if the whole
   registry vanished. Corrected inline above. The three burned names are now in `RETIRED_NAMES`;
   enrolling the registry is **TaskList #10 and a PREREQUISITE to #1**.
2. **The exit loop's closure is CONDITIONAL** — with the Ridge exit-blender on, bandit weights are
   overwritten while the bandit's arm is still reported as dominant. Stated inline above.
3. **A sibling of the "closed" uninit-stack bug was unpatched** — `ShardedSnapshot.hpp:780-785`, the
   same READY-without-`primary_count>=2` shape, ×5 regimes. **Now fixed** (display-grade: it feeds the
   TUI snapshot, not a signed wire). The quantifier that hid it — *"the ONLY consumer not paired with a
   second condition"* — was true and useless, because it measured pairing with `MASK_EZOO_ACTIVE`, a
   property the same paragraph proves non-protective. Textbook M9: the set was enumerated against the
   wrong predicate.

**Two claims it actively tried to break and could not:** `weights[]` really is consumed at
`StrategyParameters.hpp:1591-1593` (the loop is closed, not write-only), and the `has_flags` bit
renumbering really is safe — verified via `gen_code_map.sh --byte-context`/`--composition`, not by
trusting the comment that said so.

**Its verdict, kept verbatim because it is the thing to carry forward:** *"a materially truthful handoff
with a dangerous epistemic defect — it treats a GREEN from a guard that does not cover the change as
proof the change was safe, and hands that pattern forward to a larger deletion on the same signed wire."*

**The structural close (M7):** the mechanical tooth for this check was itself vacuous — it regex-searched
for the words "independent review", so writing *"NOT RUN"* SATISFIED it. A declared skip now fires HIGH,
louder than a missing section, with three plantings and a negative control.

## TaskList (recreate at pickup)

| ID | Status | Subject |
|---|---|---|
| #1 | pending (operator) | **Step 6: Tier 2 vs Tier 1 fork.** Tier 2 = dissolve the emit-side bitmap (gate rows against `args`/`cfg` at emit time; the defect class becomes UNREPRESENTABLE; sibling half already does this at `MemHeaders/CfgGateRegistry.hpp:425-440`; cascade ≈44 decls in `tests/controller_test.cpp`, all Class-58 fixtures). Tier 1 = per-WIRE-KEY partition checker, shaped so Tier 2 DELETES it. ⛔ Do NOT build the three refuted designs — see the READ FIRST table. |
| #2 | pending | **Step 7** — `FORMAT:` token, then `GUI/TradeReader.hpp` (its legacy-schema read is separately known-broken, D-421 ⑤). |
| #3 | pending | **`MBS_OrderSetBanditContext`** — getter read at `OrderManager.hpp:839`, setter never called ⇒ every calibration row attributes per-arm Exp3 probabilities to regime 0. Telemetry-grade but it corrupts the data needed to judge the now-live loops. |
| #4 | pending | **Domain classification** of the 65 baselined registries — THE fan-out surface. Still blocked on the `STRUCT:` subset/filter notion (TECH_DEBT-274). |
| #5 | pending | **E.1.2 close-gate** — `NPF_PROJECT_POISON` 4th projection + FEATURE_LOOKUP operator-migration SECTION + postmortem + `/ship`. |
| #6 | pending (operator) | The OPEN decisions: the cfg-derived drift-gate fork (per-field / emit-the-bit / retire, deliberately sequenced after step 6) · the `CLAUDE.md` always-loaded budget pass (`python3 tools/check_always_loaded_budget.py` for the live reading). |
| #7 | pending | **Empirical:** the bandit loop CLOSING is proven; PROFITABILITY is not. Needs an `exit_bandit_enabled=1` backtest vs the uniform baseline — not a unit test. |
| #9 | pending | **Backtest-side Thompson save** — `EnsembleModelZoo_SaveThompsonState` has exactly two callers (`ML_Headers/NodeModelZoo.hpp:3312`, `CoreFrameworks/EngineSharded/Run.hpp:2357`); nothing under `Backtest/`. D-423's STATUS names it open; it had no TaskList home until the independent review flagged the gap. |
| #10 | **PREREQUISITE to #1** | **Enroll `FOREACH_STAMP_BOUND_MODEL_CONST` in `tools/check_identifier_retirement.py` `SOURCES`.** The guard has NO row for the stamp registry, so its GREEN says nothing about a stamp-body change — and #1 is a much larger deletion on that same HMAC-signed body. ~58 identifiers ⇒ needs a blessed `--update` at the operator TTY, which is why it is a decision and not a chore. Converts a vacuous green into a real one BEFORE the deletion that needs it. (The three burned `fees` names are already in `RETIRED_NAMES`.) |
| #11 | pending | **Four stale referents the `fees` deletion left** — `ML_Headers/ModelInference.hpp:1448` (lists `fees` among live group bits) · `ML_Headers/StampHelper.hpp:228-229` (contradicted by its own `:252`) · `StampBoundModelConstRegistry.hpp:532-533` ("6 + 7 = 13", actual 6 + 17 = 23) · `ML_Headers/CfgDriftCheckRegistry.hpp:119` (doc-example `STAMP_HAS(*h, fees)` would no longer compile). Same class this session opened with. |
| #12 | pending | **Dead regime-hysteresis inputs** — `prev_regime_id` and `regime_transition_cycles_remaining` (`ML_Headers/NodeModelZoo.hpp:1242-1243`) are declared, zeroed, and never assigned; the latter is only ever DECREMENTED. So the buy-side hysteresis branch at `Strategies/StrategyParameters.hpp:1169-1183` is structurally unreachable, and its "previous regime" would be a permanent 0 if it ran. Class-58 sitting inside the block this session edited most carefully. |
| #8 | pending backlog | TECH_DEBT-283 (`ring_push_failures` write-only) · -284 (H16 unenforceable) · -274 (generic `STRUCT:` checker) · -282 (mint the Class-58 Mn) · the register's cleanup residual (unadopted helpers; comment-fix or delete). |
