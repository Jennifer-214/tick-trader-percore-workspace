---
type: handoff
status: active
ship_tag: .E.0.10
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-12-E.0.10-deep-audit-and-A19-fix-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
required_reading: [register (§Routing map + A1–A27), decision-log D-204/D-205/D-206/D-207, the A25 plan in decision-log, characterization-test-discipline.md, subplans/2026-06-12-...E-exit-system-trailing-redesign.md (the PARKED redesign — context only), CODE_MAP (regen at pickup), DOCS/STRATEGY_AND_CODING_RULES.md, plans/_cross-cutting/2026-05-06-latency-path-discipline.md]
engine_head: 2b38bd6 (A9 landed; on feat/v5.15-live-readiness)
workspace_head: (this handoff's own commit — D-205/206/207 + AR-11 + decision-check Stage-3 + the exit-redesign subplan + TD-179/180/181)
coding_status: fix-ship in flight — A9 LANDED+committed; A25 DECIDED (carry mechanism locked, D-204/D-205) but NOT coded; A25 is the next CODE step
---

# .E.0.10 handoff — A25 decided (carry locked), ready to implement (2026-06-12 close)

> **Why this handoff:** a long decision-capture session (compaction hit). A25's fix DIRECTION + CARRY mechanism are now fully decided + adversarially-grounded (D-204/D-205); the next session EXECUTES A25 (writes code), then continues the fix-ship. The exit-system trailing-profit REDESIGN was surfaced + fully captured + PARKED (D-206 + a subplan stub) — it is NOT this ship. Everything below is on disk; nothing is lost (verified by an independent Stage-5.5 review at close: ALL-LANDED).

## State at handoff write
- Engine HEAD `2b38bd6` (A9 — slippage consume at the fill chokepoint), branch `feat/v5.15-live-readiness`, working tree clean.
- Suite was 3373/0 at A9 close. A25 is NOT coded yet (decision-only this session).
- Workspace: D-205/206/207 + AR-11 + the `/decision-check` Stage-3 hardening + the exit-redesign subplan + TD-179/180/181 committed via this close's `/sync-workspace`.

## TaskList state at handoff write (recreate verbatim)
| ID | Status | Subject |
|---|---|---|
| #1 | completed | A9 — consume pre_resolved.slippage_pct at fill chokepoint |
| #2 | pending (NEXT) | A25 — trail-trigger per-fill SSoT (set-site fix; carry locked per D-205) |
| #3 | pending | A24 — cfg-mutation single-source (H22) |
| #4 | pending | A16 — gate HandleFill + slot-free on ORDER_FILLED, not event_log_mode |
| #5 | pending | F-059 — exit-chain golden-master oracle (AFTER A24/A25) |
| #6 | pending | A6 — chokepoint gate-emit tp/sl-validity guard |

## THE NEXT ACTION — implement A25 (CARRY locked; resolve the pre-impl decisions FIRST, then execute)

**What A25 fixes (premise CONFIRMED by code-read this session — see D-205):** the 4 sharded per-strategy trails (`Momentum_ExitAdjustSharded:373` / `MeanReversion:565` / `MLStrategy:312` / `EmaCross:216`), dispatched via `Strategy_ExitAdjustPerCore` at `ControllerEventLoop.hpp:2716`, gate trail-arming on `original_tp`; the exit-bandit counterfactual (`ControllerEventLoop.hpp:1749`) reads it too. Today `original_tp` is set from `o->intended_tp` = `expected_entry × (1+tp_pct)` (expected-entry-priced); post-A9 the fill differs, so the trail arms at the wrong price + the bandit counterfactual is distorted. A25 makes `original_tp` fill-priced.

**The carry mechanism (LOCKED — D-204 + D-205; do NOT re-litigate):**
1. Add `Money tp_pct;` to `OrderPreResolved<F>` (`Order.hpp:142`, the documented extension point). Update `static_assert(sizeof(OrderPreResolved<64>)==48)` (was 32, Order.hpp:148) + `sizeof(Order<64>)==272` (was 256, Order.hpp:403) — CONSCIOUS bump; `Order` is in-flight SPSC-only (NOT persisted — the persisted struct is the separate `OrderEvent`), so no wire/snapshot/H21 concern, only a modest drainer (≤10μs) cache line, not the ≤500ns hot path.
2. In `Order_BindPreResolved` (`ControllerConfig.hpp:358`, beside `fee_rate`/`slippage_pct`): `o->pre_resolved.tp_pct = ResolvePerFillTpPct(o->strategy_id, core_cfg);` — REUSE A1's SSoT resolver (`StrategyParameters.hpp:311`); `strategy_id` is set @`OrderManager.hpp:969` BEFORE the bind @`:983`. (If a StrategyParameters↔ControllerConfig include cycle bites, resolve in `OrderManager_Submit` right after the bind, or carry `cmd.tp_pct` on `SubmitCommand` — fallback only.)
3. At `handle_buy_fill` (`OrderManager.hpp:1179-1181`): `Money per_fill_tp = !Money_IsZero(o->pre_resolved.tp_pct) ? Money_Add(fill_price, Money_Mul(fill_price, o->pre_resolved.tp_pct)) : o->intended_tp;` then pass `per_fill_tp` to `Portfolio_OpenSlot` instead of `o->intended_tp`. `Portfolio_OpenSlot` sets BOTH `take_profit_price` AND `original_tp` from that arg (`Portfolio.hpp:352/354`) → fixes the trail readers + the bandit in one shot. The `?:` keeps tp_pct==0 (legacy/test) on the existing path → bytewise-identical for the no-pct case. **Single-source the per-fill-TP compute in a helper** (used at the OpenSlot site; the agent found `handle_buy_fill` + the event-log-append are one fill, but verify whether the replay path `OrderEventLog.hpp:732` + the legacy `PortfolioController.hpp:1372-1396` back-door also need it — see the wiring-agent findings in D-205's spawned items).
4. **MUST route through `ResolvePerFillTpPct`** — NOT global `cfg.take_profit_pct` — or it re-opens the A1 bug + diverges from the warm-restart recompute (`ShardedSnapshotPersist.hpp:660` already does `entry × (1+ResolvePerFillTpPct(...))`; A25 must produce the same value for the same fill).

**Verification (per the fix-ship cycle):** 3-lens characterization test asserting `original_tp == fill × (1+tp_pct)` (NOT `expected_entry × (1+tp_pct)`) under a slipped fill + the tp_pct==0 bytewise-identical fallback; run `check_struct_alignment.py` (c) on the grown `Order` (confirms it's not byte-serialized → the assert bump is the only gate); `// ADV-REFUTE` marker; `tools/run_all_tests.sh --fast`; then an independent adversarial verify (per the BINDING adversarial-default + AR-11 — resolve any agent disagreement on a code fact by READING the code, never by fiat).

## A25 pre-impl notes — RESOLVE before/while coding (2026-06-12 deep-harvest; FULL list in the register's "A25-impl notes" subsection)

The carry above is locked — but the exhaustive deep-harvest surfaced downstream items the implementer MUST handle (these are NOT carry re-litigation):
- **#5 — a real DECISION, do FIRST:** the event-log REPLAY reconstructs `original_tp` from the expected-entry `e.tp` (`OrderEventLog.hpp:732` ← `:1337`) → post-A25 **live ≠ replay**. DECIDE before coding: store the fill-priced TP on `OrderEvent` (H21 event-log VERSION bump + tombstone) OR document `original_tp` as non-byte-reproducible across replay + have F-059 freeze-flag it. (The "reconstruct-path" Class — TECH_DEBT-186.)
- **#19 — land A18 WITH/BEFORE A25:** A25 widens A18's unguarded-`Portfolio_OpenSlot`-overwrite blast radius (now clobbers `original_tp`).
- **A28 (TECH_DEBT-182) — fold into the same `Portfolio` touch:** `Portfolio_Init`/`ClearPositions`/`AddPositionWithExits` zero a SUBSET of `Position` (the A19 bug class one field over) → full-reset them.
- **#8:** A25 flips Momentum's `original_tp` stddev→flat → moves its trail-ARMING threshold (cross-ref A10; F-059 freeze-flag).
- **#15:** A25 cleans the exit-bandit counterfactual → shifts the learned policy (intended; DORMANT until A11/A12 fix the orphaned producers).
- **#18:** post-A25 `Position::{original_tp,original_sl}` is a NEW slow↔drainer torn-read site → enroll in A22.
- **#6/#7 — non-targets (document):** mode-0 `EventLoop_OnEvent` OpenSlot + the single_core `PortfolioController` back-door (`:1372-1396`) are A25 NON-targets.

## Then the rest of the fix-ship (in order)
A24 (cfg-mutation single-source, H22) → A16 (gate HandleFill + slot-free on ORDER_FILLED not event_log_mode; fold A17) → F-059 (exit-chain golden-master, AFTER A24/A25) → A6 (chokepoint gate-emit tp/sl-validity guard). Each = fix + 3-lens char-test + `// ADV-REFUTE` + run_all_tests.sh --fast + adversarial verify. At A25 fix-close: bump Class 44 recurrence + add a repr-migration Derivation worked-example.

## PARKED (NOT this ship) — the exit-system trailing-profit redesign
D-206 + `subplans/2026-06-12-v5.15.5.F.4d.1.E-exit-system-trailing-redesign.md` (a charter stub). Vision: ride-past-TP → ratchet SL to old TP → give-back-from-peak reversal exit + a learned-ratchet/dual-bandit generalization. SUBSTANTIAL (3 absent pieces; HIGH-RISK hot-gate H7/H8; ~30% reusable = the `ratchet_sl` machinery). Cheapest struct plan: slow-path peak + reuse the ratchet channel + ONE `Position` 128→192B, COORDINATE with D-55 (Position SoA at `.E.1`). #1 risk = the warm-restart re-arm. A25 (fill-priced `original_tp`) is a PREREQUISITE. **Pending operator sequencing into the .E.N pipeline** — do not start without that.

## Session decision captures (all on disk)
- **D-204** — A25 = the SET-SITE fix (rejected the relocate-cascade + the pass-through).
- **D-205** — A25 CARRY mechanism (above) + the CORRECTION (a narrow-bandit reframe was RETRACTED; the sharded `*_ExitAdjustSharded` DOES read `original_tp`) + the resolve-by-fiat meta-failure.
- **D-206** — the exit redesign folded to E (parked).
- **D-207** — the `/decision-check` dogfood meta-failure → AR-11 + the Stage-3 hardening.
- **AR-11** (meta-anti-pattern-index) — cross-check-disagreement-resolved-by-fiat + the shared-blind-spot corollary; `/decision-check` Stage 3 now MANDATES an orchestrator code-read on a factual disagreement.
- **TD-179/180/181** (open.md) — the cascade-tooling dogfood gaps (`--composition` transitive walk / no `sizeof⇒version-bump` guard, M7 / `--help` + stale `Portfolio.hpp:97-116` comment).
- **A28–A31 + TD-182–187** (deep-harvest 2026-06-12) — new register findings (Init/Clear reset-subset = A19-class; GUI-drag-inert-on-sharded = live-safety; `is_trailing` perma-false; single_core `*_ExitAdjust` dead-code) + their ledger homes (incl. TD-186 reconstruct-path Class+spec, TD-167 sharpened with the F-096 hunt site). The register's **A25-impl notes** = the implementer's pre-impl checklist (above). MASTER banner refreshed to the 2026-06-12 tip.

## Critical pickup-time reads (nav-infra, first-class)
- The disposition **register** (`plan_checks/E.0.10-finding-disposition-register.md`) — the A1–A27 routing map + the A25 row.
- The **decision-log** D-204/D-205 (the A25 fix + carry, LOCKED) — read these BEFORE touching A25.
- **CODE_MAP** — regen `./tools/gen_code_map.sh` at pickup (it was stale/misrouted this session — see TD-181); use `--byte-context Order` / `--composition Order` for the struct-grow blast radius.
- Path-discipline: `DOCS/STRATEGY_AND_CODING_RULES.md` + `plans/_cross-cutting/2026-05-06-latency-path-discipline.md` (A25 touches the drainer + the Order struct).
- The **exit-redesign subplan** — context only (so you know what A25 is a prerequisite FOR; do NOT implement it).

## Paste this into a fresh session

```
I'm picking up .E.0.10 (the net-1 correctness fix-ship) for v5.15-live-readiness.

Run /accept-handoff plans/v5.15-live-readiness/handoffs/2026-06-12-E.0.10-A25-decided-A25-impl-handoff.md

This is a fresh context — do NOT trust prior-session memory; verify against current code.

After /accept-handoff returns PICKUP-READY: implement A25 per the LOCKED carry mechanism in
decision-log D-205 (add Money tp_pct to OrderPreResolved → bind via ResolvePerFillTpPct in
Order_BindPreResolved → consume at handle_buy_fill to set original_tp = fill×(1+tp_pct), with the
tp_pct==0 bytewise-identical fallback). Update the two sizeof asserts (OrderPreResolved 32→48,
Order 256→272 — conscious; Order is in-flight SPSC-only, not persisted). 3-lens char-test asserting
original_tp == fill×(1+tp_pct) NOT expected×(1+tp_pct); check_struct_alignment.py (c); run_all_tests.sh
--fast; independent adversarial verify (resolve any agent disagreement on a code fact by READING the
code — AR-11). Consult Caramel on findings before/after per the consult-before-coding norm.
Do NOT touch the parked exit-system redesign (D-206) — it awaits sequencing.
```

## Quick links
- Sprint MASTER: `plans/v5.15-live-readiness/MASTER.md`
- Decision log: `plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-204/205/206/207)
- Register: `plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md`
- Parked redesign: `plans/v5.15-live-readiness/subplans/2026-06-12-v5.15.5.F.4d.1.E-exit-system-trailing-redesign.md`
- Predecessor handoff: `plans/v5.15-live-readiness/handoffs/2026-06-12-E.0.10-deep-audit-and-A19-fix-handoff.md` (superseded)
