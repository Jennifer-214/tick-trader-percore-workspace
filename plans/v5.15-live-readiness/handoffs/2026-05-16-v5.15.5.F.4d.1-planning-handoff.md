# v5.15.5.F.4d.1 planning handoff — create 4 sub-ship plan bodies + pre-coding audit gate

**Created:** 2026-05-16 (end of `v5.15.5.F.4d` MERGED ship-close session)
**Target ship:** `v5.15.5.F.4d.1` umbrella (Thread A FULL framework consolidation; TECH_DEBT-085) — 4 sub-ships .A/.B/.C/.D
**Branch:** `feat/v5.15-live-readiness` (continue on sprint branch)
**Pre-tag rollback anchor:** `v5.15.5.F.4d` (GPG-signed; this session's ship close)
**Sub-master plan:** `plans/v5.15-live-readiness/subplans/2026-05-16-v5.15.5.F.4d.1-thread-a-framework-full.md`
**Predecessor session postmortem:** `plans/v5.15-live-readiness/postmortems/2026-05-16-v5.15.5.F.4d-merged-postmortem.md`

**Session purpose:** Convert sub-master STUB into 4 full sub-ship plan body drafts, fire pre-coding audit gate, consult Caramel on findings, then start coding `.F.4d.1.A`. This is a PLANNING session, not a coding session — full plan body drafts are the deliverable.

---

## Engine state at handoff write (verify on cold-pickup)

- **HEAD:** `545b087` (`v5.15.5.F.4d MERGED (ship close): Version bump 5.15.5.F.4c.3 → 5.15.5.F.4d`)
- **Tag:** `v5.15.5.F.4d` (GPG-signed; from 2026-05-16 ship close)
- **Branch:** `feat/v5.15-live-readiness` (clean — no uncommitted changes expected; sub-master + handoff committed in workspace; engine code at tagged state)
- **Tests:** 3174 controller_test + 17 depth_recorder_test GREEN
- **5 binaries:** test / gui / suite / tsan / asan all build clean
- **Working tree:** **CLEAN** (engine repo); fresh state from tagged ship close

Verify via:
```bash
cd /home/caramel/code/FoxML_Trader_v2
git rev-parse HEAD                    # expect: 545b087...
git status --short                    # expect: empty (no uncommitted)
head -10 Version.hpp                  # expect: ENGINE_VERSION_STRING "5.15.5.F.4d"
git tag --sort=-creatordate | head -3 # expect: v5.15.5.F.4d at top; pre-v5.15.5.F.4d; v5.15.5.F.4c.3
./build.sh test 2>&1 | tail -3        # expect: RESULTS: 3174 passed, 0 failed
```

If any value diverges from expected: ANOTHER session may have advanced state since this handoff. Reconcile by reading commits since `545b087` before proceeding.

---

## What `.F.4d` MERGED shipped (predecessor; substantial)

Per Version.hpp comment block + postmortem `2026-05-16-v5.15.5.F.4d-merged-postmortem.md`:

### Thread B FULL
- Pattern 5 Thompson_Update branchless dispatch + FOREACH_BANDIT_SIDE meta-X-macro (first canonical)
- g_buy_reward_dispatch + g_exit_reward_dispatch tables auto-derived from FOREACH_BANDIT_ALGORITHM metadata
- 5-state bandit_algorithm enum (EXP3/THOMPSON/EXP3_OP_THOMPSON_GHOST/THOMPSON_OP_EXP3_GHOST/BLENDED)
- Order::flags_packed bandit context bits 17-25 (Pattern 4; 5th canonical of multi-bit-state-encoding INVARIANT)
- FOREACH_OMS_PER_SLOT_FIELD 3→5 rows (Class 30 close)
- § F Pattern 5 path consolidation (per-core ezoo_refs/core_cfg_refs arrays on OmsState)
- § M Calib log 9→47 columns
- Class 24 sister + Class 25 sweep + 6 Class 28 cmov sites + Class 30 close
- PARITY-026 closed (5 new drift-check rows)
- thompson_exp3_blend_alpha NEW per-core cfg
- 15 Step 9 test sections / 26 assertions

### Thread A foundation (TECH_DEBT-085 residual deferred to THIS ship `.F.4d.1`)
- H15-H20 codified in CLAUDE.md hard invariants table
- 4 Thread A DESIGN_SPECs Stage 3 ACTIVE: metadata-bit-driven-derived-filter-framework + meta-registry-pattern-for-codebase-registry-discipline + sidecar-override-pattern-for-registry-auto-flows + framework-composition-overview
- FOREACH_BANDIT_SIDE enrolled in FOREACH_REGISTRY (H15 first canonical)
- STAMP_BOUND_CFG_DERIVED metadata bit (bit 13) reserved on CfgFieldDescriptor::MetadataFlag
- CLAUDE.md item 31 (framework-driven extensibility meta-principle) codified

### 3 substantial TECH_DEBT fold-ins closed
- TECH_DEBT-082 (3 `.F.5` residual fields → FOREACH_PER_CORE_CFG_FIELD; Class 23 manual-parser closure at 3 sites)
- TECH_DEBT-083 (IWYU sweep; 7 headers add explicit `<cstdint>`)
- TECH_DEBT-084 (FOREACH_BANDIT_SIDE cascade rename; 200+ refs across 14 files; persistence path back-compat alias)

---

## `.F.4d.1` sub-master structure (READ FIRST in next session)

**File:** `plans/v5.15-live-readiness/subplans/2026-05-16-v5.15.5.F.4d.1-thread-a-framework-full.md`

Sub-master enumerates 10-item scope grouped into 4 sub-ships:

| Sub-ship | Items | Plan body file (NEXT SESSION CREATES) | Est. |
|---|---|---|---|
| **.A** framework infra | 1 + 2 + 10 (DerivedFilterFramework.hpp + StampBoundDerivedFilter.hpp + Layer 5b hash lock) | `subplans/<YYYY-MM-DD>-v5.15.5.F.4d.1.A-framework-infra.md` | ~5-7h |
| **.B** migration + consumer | 3 + 4 + 9 (24-row migration + FOREACH_ML_CFG_FLAG 5→6 sig + 12+ consumer migration + CFG_DRIFT_AUTOPOPULATE) | `subplans/<YYYY-MM-DD>-v5.15.5.F.4d.1.B-migration-consumer.md` | ~6-9h |
| **.C** sidecar + bit-packed | 5 + 6 (FOREACH_DRIFT_OVERRIDE + DriftOverride + RegistryRosterEntry + ManualFieldInventoryEntry) | `subplans/<YYYY-MM-DD>-v5.15.5.F.4d.1.C-sidecar-bitpacked.md` | ~3-5h |
| **.D** CI + fixture | 7 + 8 (CI Checks 9-12 + v5.14 stamp fixture regression) | `subplans/<YYYY-MM-DD>-v5.15.5.F.4d.1.D-ci-fixture.md` | ~2-4h |

Dependency order: **.A → .B → .C (interleavable) → .D**. Each sub-ship gets own GPG-signed tag + postmortem.

---

## Step-by-step pickup workflow

### Step 0 — Orient + verify state (mandatory)

Run state-verification block at top of this handoff. Confirm `545b087` HEAD + `v5.15.5.F.4d` tag + 3174/0 tests + clean working tree. If diverged, reconcile before proceeding.

Read in parallel:
- `/home/caramel/code/FoxML_Trader_v2/CLAUDE.md` (always-loaded orientation; H1-H20 invariants codified)
- `/home/caramel/code/FoxML_Trader_v2/CLAUDE.local.md` (INDEX of going-forward rules; sprint state at top)
- `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md` (auto-memory index)
- `/home/caramel/code/tick-trader-percore-workspace/plans/v5.15-live-readiness/subplans/2026-05-16-v5.15.5.F.4d.1-thread-a-framework-full.md` (**SUB-MASTER — read first**)
- `/home/caramel/code/tick-trader-percore-workspace/plans/v5.15-live-readiness/postmortems/2026-05-16-v5.15.5.F.4d-merged-postmortem.md` (predecessor postmortem; what foundation `.F.4d` laid)
- `/home/caramel/code/tick-trader-percore-workspace/plans/v5.15-live-readiness/subplans/2026-05-16-v5.15.5.F.4d-merged-framework-bandit-thompson.md` Thread A Charters 8-14 (lines 124-247) — full Thread A scope spec
- `/home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/metadata-bit-driven-derived-filter-framework.md` (Stage 3 ACTIVE; mechanism specification — Option B runtime walk + 3 macro variants + Layer 5b methodology)
- `/home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/sidecar-override-pattern-for-registry-auto-flows.md` (Stage 3 ACTIVE; FOREACH_DRIFT_OVERRIDE companion pattern)
- `/home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/meta-registry-pattern-for-codebase-registry-discipline.md` (Stage 3 ACTIVE; H19 topology discipline)
- `/home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/framework-composition-overview.md` (Stage 3 ACTIVE; composition narrative — how the 4 Thread A frameworks compose)
- `/home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/multi-bit-state-encoding-pattern.md` (INVARIANT; canonicals 1-5 landed; canonicals 6/7/8 to land at .C)
- `/home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/wire-format-byte-preservation-discipline.md` § 5b (Layer 5b methodology — mandatory for wire-format derived filter variants)

### Step 1 — Create 4 sub-ship plan body drafts (~4-6h total; 1-2h each)

Per sub-master "Sub-ship boundaries" + sub-master "Scope" section. Each plan body should follow the `/readiness` 10-item cold-pickup completeness checklist:

1. **Branch state** — `feat/v5.15-live-readiness` continues; pre-tag rollback anchor named (e.g., `pre-v5.15.5.F.4d.1.A`)
2. **Phase execution order** — verify dependencies match sub-master sequencing (.A foundation before .B migration)
3. **First concrete move per sub-ship** — Step 0 commands (read state + verify baseline tests pass)
4. **Function / constructor / macro names** — verified against current code at HEAD:
   - `.A`: `g_global_cfg_field_descriptors[FIELD_IDX_GLOBAL_END]` + `g_per_core_cfg_field_descriptors[FIELD_IDX_PER_CORE_END]` arrays (target of runtime walk; verified existing at `CfgFieldRegistry.hpp:940/944`)
   - `.B`: 24-row FOREACH_STAMP_BOUND_CFG enumeration at `ML_Headers/StampBoundCfgRegistry.hpp:99-179`; 12-row FOREACH_ML_CFG_FLAG at `ML_Headers/MlCfgFlagRegistry.hpp`; consumer site at `ML_Headers/CoreModelZoo.hpp:225-247` (drift loop) + ~11 sister sites
   - `.C`: 19-row FOREACH_CFG_DRIFT_CHECK current state at `ML_Headers/CfgDriftCheckRegistry.hpp` (5 rows need sidecar override → narrow registry once FOREACH_DRIFT_OVERRIDE lands)
   - `.D`: `tools/check_per_core_registry_integrity.py` precedent for new CI tool (Check 8 added at `.F.4c.3`; Checks 9-12 follow same pattern)
5. **File:line refs for cited tests / baselines** — 3174/0 baseline post-`.F.4d`; counts per sub-ship below
6. **Stale-claim audit** — verify each plan body's assumptions against current code (FOREACH_STAMP_BOUND_CFG row count + ML_CFG_FLAG row count + consumer site count + descriptor array sizes + STAMP_BOUND_CFG_DERIVED bit defined at line 144-145)
7. **Effort claims reconcile** — .A ~5-7h LOC estimate breakdown + .B ~6-9h + .C ~3-5h + .D ~2-4h
8. **Source-audit references** — predecessor ship postmortem + sub-master + Thread A Charters 8-14 + DESIGN_SPECS Stage 3 ACTIVE
9. **Predecessor / dependent plans named** — predecessor `v5.15.5.F.4d`; successor `.F.4e`; sub-ship dependency chain .A→.B→.C→.D
10. **Tag names locked + rollback anchors** — `v5.15.5.F.4d.1.A` annotated + GPG-signed; `pre-v5.15.5.F.4d.1.A` rollback anchor (created at Step 0 of .A); same for .B/.C/.D

**Per-sub-ship plan body deliverable shape** (per `DOCS/CLAUDE_REVIEW.md` review checklist):
- **Why this sub-ship exists** (specific scope; why grouped this way)
- **Architectural invariants preserved** (H1-H20 + which sub-ship-specific concerns apply)
- **Implementation steps** (file:line anchored; concrete edits per item; expected build/test deltas)
- **Anti-patterns to avoid** (per item)
- **Per-cohort gate_when expressions** (.B specifically — Bandit/Thompson + Soft-risk + Ridge + Composite + Winsor cohorts need their drift-check gate semantics defined)
- **DOD analysis** per the sub-ship's touched structs (cluster alignment + bit-packing + branchless dispatch + cache layout per `cache-layout-discipline-for-hot-side-structs.md`)
- **Verification gate** (what GREEN looks like per item)
- **"If something goes wrong"** recovery notes per item
- **Test expectations** (counts + key new test names)
- **Ship close** (Version.hpp bump + tag + postmortem + auto-writes per CLAUDE.local.md contracts + sync-workspace)

**Sidecar examples file consideration** (per CLAUDE.local.md "Sub-plan sidecar files for substantial sections" rule set 2026-05-16):
- **.A sidecar** RECOMMENDED — `DerivedFilterFramework.hpp` 3 macro variants are substantive code (~150 LOC framework macros + Layer 5b synthetic populate fn + LOCKED hash methodology). Sidecar captures pre-coded examples so coder doesn't re-derive design at coding time.
- **.B sidecar** OPTIONAL — 24-row migration is mechanical (per source row: add `STAMP_BOUND_CFG_DERIVED` to metadata_flags column). FOREACH_ML_CFG_FLAG 5→6 sig migration is mechanical. Consumer migration sites need per-site Edit anchor verification (~12 sites at `CoreModelZoo.hpp:225-247` + sisters); sidecar could enumerate the 12 sites with current-state code samples.
- **.C sidecar** RECOMMENDED — bit-packed struct layout (DriftOverride 8-byte; RegistryRosterEntry; ManualFieldInventoryEntry) benefits from layout diagram + bit-position table. Sidecar M.2 macro-nesting gotcha precedent: pre-coded examples save coder from preprocessor edge cases.
- **.D sidecar** OPTIONAL — CI Check tool structure (sister to `tools/check_per_core_registry_integrity.py` Check 8). Could be a sidecar OR just file:line refs.

### Step 2 — Pre-coding audit gate per sub-ship (~1h per sub-ship; total ~4h or run in parallel via Agent)

Per sub-master "Pre-coding triggers (REQUIRED)" + CLAUDE.local.md going-forward rule "Suggest mid-sprint audits when work impacts downstream":

For `.F.4d.1.A` (framework infra; LOW-MED risk):
- Fire `/precoding-audit-gate` Layer-1 orchestrator with `audit_set` = parity + trace-deps + readiness + dod-audit + merge-scan
- `plan_path` = `subplans/<YYYY-MM-DD>-v5.15.5.F.4d.1.A-framework-infra.md`
- `focus_keywords` = "DerivedFilterFramework, runtime walk, Layer 5b hash, descriptor array, fnv1a_64, locale pin, byte preservation"

For `.F.4d.1.B` (migration + consumer; MED risk — wide blast radius):
- HIGH-risk audit: parity-check + trace-deps + readiness + dod-audit + merge-scan + bug-check (Class 18/19/21/23/25 specifically; this ship structurally closes Class 21 at derived-filter surface but must NOT introduce Class 18 mirror)
- focus_keywords = "FOREACH_STAMP_BOUND_CFG migration, FOREACH_ML_CFG_FLAG 5→6 sig, consumer migration, CFG_DRIFT_AUTOPOPULATE, always-emit gate_when per cohort, HMAC chain byte preservation"

For `.F.4d.1.C` (sidecar + bit-packed; LOW-MED):
- Standard audit: parity + trace-deps + readiness + dod-audit
- focus_keywords = "FOREACH_DRIFT_OVERRIDE, DriftOverride bit-pack, RegistryRosterEntry, ManualFieldInventoryEntry, multi-bit-state-encoding canonicals 6/7/8, sidecar override pattern"

For `.F.4d.1.D` (CI + fixture; LOW):
- Light audit: readiness + trace-deps
- focus_keywords = "CI Check 9-12 cohort eligibility, coverage gates, v5.14 stamp fixture regression, fixture file commit"

### Step 3 — Consult Caramel on audit findings (per `feedback_consult_on_audit_findings`)

After audit gate fires, present findings inline (no AskUserQuestion modal per `feedback_no_question_boxes`). For each finding:
- Specific file:line + what was caught
- Severity classification
- Proposed fix per item
- Iterate with Caramel before coding starts

Don't auto-proceed even if findings look clearly addressable. Caramel ALWAYS consults before coding.

### Step 4 — Tag pre-coding rollback anchor + start coding `.F.4d.1.A`

```bash
cd /home/caramel/code/FoxML_Trader_v2
git tag -s -a pre-v5.15.5.F.4d.1.A -m "pre-coding rollback anchor for v5.15.5.F.4d.1.A framework infra"
```

Then code .A per its plan body Step 1+. Build verify after each item lands (DerivedFilterFramework.hpp → StampBoundDerivedFilter.hpp → Layer 5b hash lock test).

Tag `v5.15.5.F.4d.1.A` at sub-ship close + write postmortem + auto-writes + sync-workspace.

Repeat for .B → .C → .D.

After .D ships: umbrella `v5.15.5.F.4d.1` annotated tag + umbrella postmortem (synthesizes 4 sub-ship postmortems).

---

## Decision points carried into next session

### Decision 1 — New DESIGN_SPEC candidate: "Type-erased per-core resource handle pattern"

3 applications identified at `.F.4d` ship close:
1. `void* ensemble_handle` on `CoreContext<F>` (pre-existing; CoreFrameworks → ML_Headers layer cross via void* + lazy cast at consumer)
2. `void* ezoo_refs[MAX_EXECUTION_CORES]` on `OrderManagerState<F>` (NEW at `.F.4d` § F — engine-wide state with per-core resource indexed by Order::core_id)
3. `const void* core_cfg_refs[MAX_EXECUTION_CORES]` (NEW at `.F.4d` § F — sister to #2 for cfg slice)

Pattern: "keep layer-X state agnostic via void* fields; cast at consumer site where layer-Y types are in scope; index by core_id for engine-wide state with per-core resources."

3 applications meets `pattern-codification-lifecycle.md` Stage 2 DRAFT threshold. Could:
- **Option A:** Draft Stage 2 DRAFT spec now as part of `.F.4d.1` planning — small (~1 page); evidence is clean
- **Option B:** Defer until `.F.4d.1` adds more applications (DERIVED_FILTER framework consumer might have its own type-erasure cast surfaces)

Recommend asking Caramel at start of next session.

### Decision 2 — `.F.4l` ML side audit ship sequencing

Per Caramel's framing 2026-05-16: "id rather address the remaining items for the live side, then shift to ML, and then we paper test". Sequencing locked:
- `.F.4d.1` (this ship — Thread A FULL) → `.F.4e` (5 GUI metadata derived filter validation) → `.F.4f` (cleanup) → `.F.4g/h/i/j/k` (remaining live-side maintainability) → **`.F.4l`** (NEW; ML side live-readiness audit + fixes — was originally proposed but operator chose to defer until live side is fully consolidated) → **paper test** → v5.16+ ML-side parity (structural)

`.F.4l` STUB plan body to be created when `.F.4f` ships (so audit happens against post-cleanup baseline). Not a `.F.4d.1` concern.

### Decision 3 — Auto-writes residual from `.F.4d`

Per CLAUDE.local.md auto-write contracts + Version.hpp ship-close comment, these auto-writes were DEFERRED from `.F.4d` ship close (could fold into `.F.4d.1` ship close OR separate "doc residual" mini-ship):
- `DOCS/RECURRING_BUG_PATTERNS.md` amendments (Class 30 codification + Class 24/25/28 amendments)
- `tick-trader-percore-workspace/DOCS/DESIGN_PHILOSOPHY.md` § 2 H15-H20 narrative addition
- `tick-trader-percore-workspace/DESIGN_SPECS/README.md` catalog count + Stage 3 ACTIVE marker verification (4 specs)

Recommend logging as **TECH_DEBT-086 doc residual** + folding into `.F.4d.1.D` ship close (CI + fixture; LOW-risk; doc work fits the same boundary).

---

## TaskList state recommendation for next session

Recreate TaskList for the planning session. Suggested tasks:

| Subject | Status |
|---|---|
| Verify state + read required-reading + sub-master | pending |
| Create plan body draft .F.4d.1.A (framework infra; with sidecar examples) | pending |
| Create plan body draft .F.4d.1.B (migration + consumer; sidecar optional) | pending |
| Create plan body draft .F.4d.1.C (sidecar override + bit-packed; with sidecar examples) | pending |
| Create plan body draft .F.4d.1.D (CI + fixture; sidecar optional) | pending |
| Pre-coding audit gate .F.4d.1.A (Layer-1 orchestrator + 5-skill) | pending — blocked by .A plan body draft |
| Consult Caramel on .A audit findings | pending — blocked by .A audit |
| Tag pre-v5.15.5.F.4d.1.A + start coding .A | pending — blocked by consult |
| Decision: type-erased per-core resource handle DESIGN_SPEC Stage 2 DRAFT now or defer | pending — consult Caramel at session start |

---

## Notes for future-Claude reading this handoff

- Per `feedback_compaction_degrades_treat_handoffs_as_hints`: verify all handoff claims (state values, file:line refs, current counts, DESIGN_SPECS Stage status) against actual code at HEAD before acting. Don't assume the handoff's claimed counts match reality without checking.
- The sub-master at `subplans/2026-05-16-v5.15.5.F.4d.1-thread-a-framework-full.md` is the CANONICAL scope for `.F.4d.1`. If sub-master + this handoff disagree, sub-master wins.
- The 4 sub-ship plan body files don't exist yet — your job is to CREATE them. Use date prefix matching session date (`<YYYY-MM-DD>-v5.15.5.F.4d.1.<letter>-<name>.md`).
- Don't auto-proceed past audit findings. Per `feedback_consult_on_audit_findings` — present findings inline, iterate with Caramel before coding.
- Each sub-ship's pre-coding triggers + plan body + audit gate + consult cycle is the same shape. Frequent commits encouraged; ship-close cadence per sub-ship (Version.hpp bump + tag + postmortem + auto-writes + sync per sub-ship close).

---

## Pickup prompt for fresh session (copy-paste)

```
I'm picking up v5.15.5.F.4d.1 planning per handoff at plans/v5.15-live-readiness/handoffs/2026-05-16-v5.15.5.F.4d.1-planning-handoff.md. Predecessor v5.15.5.F.4d MERGED shipped clean 2026-05-16; engine commit 545b087 + GPG-signed tag; 3174/0 tests; foundation laid for Thread A FULL framework consolidation. Sub-master STUB exists; my job is to create 4 sub-ship plan body drafts (.A/.B/.C/.D), fire pre-coding audit gate (starting with .A), consult with Caramel on findings, then start coding .A.

## Step 0 — orient + verify state (MANDATORY)

Verify state:
- `git rev-parse HEAD` should be 545b087
- `git status --short` should be empty
- `head -10 Version.hpp` should show ENGINE_VERSION_STRING "5.15.5.F.4d"
- `git tag --sort=-creatordate | head -3` should show v5.15.5.F.4d at top
- `./build.sh test 2>&1 | tail -3` should show 3174 passed, 0 failed

Required reading (in parallel):
- /home/caramel/code/FoxML_Trader_v2/CLAUDE.md
- /home/caramel/code/FoxML_Trader_v2/CLAUDE.local.md
- ~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md
- plans/v5.15-live-readiness/subplans/2026-05-16-v5.15.5.F.4d.1-thread-a-framework-full.md (SUB-MASTER — read first; canonical scope)
- plans/v5.15-live-readiness/postmortems/2026-05-16-v5.15.5.F.4d-merged-postmortem.md (predecessor; foundation context)
- plans/v5.15-live-readiness/subplans/2026-05-16-v5.15.5.F.4d-merged-framework-bandit-thompson.md Thread A Charters 8-14 (lines 124-247)
- DESIGN_SPECS/metadata-bit-driven-derived-filter-framework.md (Stage 3 ACTIVE; mechanism spec — Option B + 3 macro variants + Layer 5b)
- DESIGN_SPECS/sidecar-override-pattern-for-registry-auto-flows.md (Stage 3 ACTIVE; FOREACH_DRIFT_OVERRIDE pattern)
- DESIGN_SPECS/meta-registry-pattern-for-codebase-registry-discipline.md (Stage 3 ACTIVE; H19 topology)
- DESIGN_SPECS/framework-composition-overview.md (Stage 3 ACTIVE; 4-framework composition narrative)
- DESIGN_SPECS/multi-bit-state-encoding-pattern.md (INVARIANT; canonicals 6/7/8 land at .C)
- DESIGN_SPECS/wire-format-byte-preservation-discipline.md § 5b (Layer 5b methodology)

Recreate TaskList per handoff "TaskList state recommendation" section.

## Step 1 — Open decisions (consult Caramel at session start)

1. New DESIGN_SPEC candidate: "Type-erased per-core resource handle pattern" — 3 applications already evident (ensemble_handle + ezoo_refs + core_cfg_refs); draft Stage 2 DRAFT now or defer to after .F.4d.1 lands more apps?
2. Auto-writes deferred from .F.4d (RECURRING_BUG_PATTERNS amendments + DESIGN_PHILOSOPHY § 2 H15-H20 narrative + DESIGN_SPECS README catalog) — log as TECH_DEBT-086 + fold into .F.4d.1.D ship close, or handle as separate doc-only mini-ship?

## Step 2 — Create 4 sub-ship plan body drafts (~4-6h total)

Per sub-master "Sub-ship boundaries (FINALIZED)" section. Each plan body follows /readiness 10-item cold-pickup completeness checklist. Use date prefix `<YYYY-MM-DD>-v5.15.5.F.4d.1.<letter>-<name>.md`:
- .A framework-infra (Items 1+2+10; consider sidecar examples for DerivedFilterFramework.hpp 3 variants)
- .B migration-consumer (Items 3+4+9; sidecar optional — could enumerate 12 consumer sites)
- .C sidecar-bitpacked (Items 5+6; consider sidecar examples for DriftOverride bit-packing layout)
- .D ci-fixture (Items 7+8; sidecar optional)

Per-cohort gate_when expressions needed for .B (Bandit/Thompson + Soft-risk + Ridge + Composite + Winsor; per sub-master + merged plan body Thread A Charter 12).

## Step 3 — Pre-coding audit gate (per sub-ship; starting with .A)

Fire /precoding-audit-gate Layer-1 orchestrator for .A first:
- audit_set = parity + trace-deps + readiness + dod-audit + merge-scan
- plan_path = subplans/<YYYY-MM-DD>-v5.15.5.F.4d.1.A-framework-infra.md
- focus_keywords = "DerivedFilterFramework, runtime walk, Layer 5b hash, descriptor array, byte preservation"

## Step 4 — Consult Caramel on .A audit findings

Per feedback_consult_on_audit_findings: present findings inline (no AskUserQuestion modal); iterate with Caramel before coding. Don't auto-proceed.

## Step 5 — Tag pre-coding rollback anchor + start coding .A

After Caramel approves .A scope post-audit-consult:
- `git tag -s -a pre-v5.15.5.F.4d.1.A -m "pre-coding rollback anchor"`
- Start coding .A per plan body Step 1+
- Frequent commits + build-verify per item

## Operator collaboration norms (from CLAUDE.local.md memory)

- Address Caramel as Caramel / she / her (not "the operator")
- No AskUserQuestion modal — inline scrollable text
- Evaluate options on robustness + latency + design philosophy, NOT time
- After per-step audit checkpoints, ALWAYS consult before proceeding
- Auto-write TECH_DEBT for any deferred items
- Auto-write PARITY_ISSUES for any new parity findings
- Don't measure structural work by LOC — lead with classes closed + patterns codified

Caramel will iterate on per-step findings before proceeding. Don't auto-proceed.

Good luck.
```
