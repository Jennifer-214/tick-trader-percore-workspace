# /trace-deps report — E.1.2 Phase D (D-289 dead-serializer DELETION) — 2026-07-03

- **Auditor:** I-class (INVESTIGATIVE), armed per `DOCS/SUBAGENT_ARMING.md`; skill methodology = `claude-skills/trace-deps/SKILL.md`.
- **Engine HEAD:** `b10e778` (byte-identical to E.1.1 `0ee227a`). All `file:line` grep-re-grounded at HEAD.
- **Target:** `plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md` Phase D.
- **Decision honored:** D-289 (decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md:1835) — DELETE (not tombstone-in-place); whole-format retirement. NOT re-litigated (arming §4).
- **Disciplines:** H21 (append-only wire IDs / delete dead CODE never recycle a SLOT) · dead-code-and-identifier-retirement-discipline.md · Class 50 (re-init-defeats-join — N/A here, no join reset).

## VERDICT: YELLOW

The deletion is **SAFE** — all four serializers are **provably dead (zero live callers)** and **no live consumer is orphaned** (the ShardedSnapshot confidence sub-walker + the "TICK" refuse-magic both survive, verified). But **4 scope/precision gaps** must close before ship. **None RED-block the safety claim**; they are completeness + cite-precision gaps the exhaustive-capture + dead-code disciplines require closing.

---

## 1. Consumer enumeration — LIVE vs DEAD (the core "prove zero live callers")

Comprehensive grep across `CoreFrameworks/ Strategies/ DataStream/ FixedPoint/ MemHeaders/ ML_Headers/ GUI/ Backtest/ tests/ tools/` (source + tests + GUI + tools + backtest):

| Symbol | Site (HEAD) | Live callers | Verdict |
|---|---|---|---|
| `Portfolio_Save` | Portfolio.hpp:547 (def) | 0 — 4 other refs = comments (Portfolio.hpp:49; PositionFieldRegistry.hpp:13/77/88) | **DEAD** |
| `Portfolio_Load` | Portfolio.hpp:586 (def) | 0 — refs = comments only | **DEAD** |
| `PortfolioController_SaveSnapshot` | PortfolioController.hpp:2029 (def) | 0 | **DEAD** |
| `PortfolioController_LoadSnapshot` | PortfolioController.hpp:2104 (def) | 0 — 1 ref = comment (ConfidenceScore.hpp:1050) | **DEAD** |

Zero refs in GUI/DataStream/Backtest; zero in parity_harness/binance_test/test_common. **The plan's "dead" claim on all four is CORRECT.**

## 2. Ledger / tool-SOURCES lockstep — VERIFIED (sandbox-proven) + CITE DRIFT

**Tool crash-coupling CONFIRMED** by a read-only sandbox simulation (`check_identifier_retirement.py`, `parse_current()` :188-190):
- Delete `#define CONTROLLER_SNAPSHOT_VERSION` **but leave** the SOURCES row → `PARSE ERROR: found nothing for version (define CONTROLLER_SNAPSHOT_VERSION)` → **EXIT 1** (sys.exit). The plan's "or the retirement guard `sys.exit`-crashes" is REAL.
- Delete `#define` + SOURCES row + `--update` → ledger regenerated 46→45 identifiers, CONTROLLER row dropped, re-verify **GREEN**. Baseline tool run at HEAD = GREEN (46 identifiers).

**CITE CORRECTIONS (re-grounded at HEAD — the plan's line cites are imprecise):**
- Plan says delete "ledger row (`identifier_ledger.txt:55/57`)". **Correct = line 57 ONLY.** Line 55 = `PORTFOLIO_SNAPSHOT_VERSION|7` (BUMPED 7→8, NOT deleted), line 56 = `SHARDED_SNAPSHOT_VERSION|10` (BUMPED 10→11). A literal reading of "delete 55" would corrupt the PORTFOLIO row.
- Plan says delete "tool SOURCES row (`check_identifier_retirement.py:57-59`)". **Correct = line 58 ONLY.** Line 57 = SHARDED (bump), line 59 = PORTFOLIO (bump). Deleting 57-59 would silently drop SHARDED+PORTFOLIO version tracking (guard-coverage regression on two LIVE versions).
- **Mechanically:** the ledger is regenerated by `--update`, NOT hand-edited — the operator deletes SOURCES-row-58 + `#define`, then runs `--update`; the ledger row drops automatically. Ordering within the commit: remove `#define` + SOURCES row **together**, `--update` last (else `parse_current()` at main() :269 crashes before the `--update` branch at :277).

**H21 residual (LOW/advisory):** `--update` PERMITS a SILENT row-drop (no tombstone kept; ledger simply forgets `CONTROLLER=14`). D-289's whole-format-no-reuse reasoning makes this H21-clean (the identifier is gone; no slot to collide). But the guard does NOT mechanically prevent a FUTURE mistaken drop-then-reuse. Belt-and-suspenders would keep a tombstone row. Honoring D-289; noting the residual.

## 3. "TICK" refuse-magic (the one live consumer to KEEP) — PRESERVED + genuinely live

- `ShardedSnapshot_Load:329-333` refuses `magic == 0x4B434954u` via a **hardcoded literal** (comment `// PORTFOLIO_SNAPSHOT_MAGIC`), NOT a symbol reference → **self-contained; unaffected by any deleted symbol.**
- **Genuinely live + TESTED:** controller_test.cpp:6516-6531 ("Test 3: refuse legacy v11 magic cleanly") writes a `0x4B434954` + v11 file, calls `ShardedSnapshot_Load`, asserts refused (returns 0) + state untouched. The plan correctly preserves it (Phase D). **PASS.**

## 4. Shared-wire orphan risk — NO live leg orphaned (VERIFIED) + a REVERSE orphan cascade the plan MISSES

**Live confidence sub-walker SURVIVES (task focus #4 resolved):** the shared ConfidenceScore legs have LIVE sharded callers, and live in ConfidenceScore.hpp (untouched by the deletion):
- `ConfidenceScorer_FieldwiseWrite` — ShardedSnapshotPersist.hpp:260 (LIVE) [also dead PortfolioController:2094]
- `ConfidenceScorer_FieldwiseRead` — ShardedSnapshotPersist.hpp:487 (LIVE) [also dead :2228]
- `ConfidenceScorer_CommitPersistedFields` — ShardedSnapshotPersist.hpp:585 (LIVE, sole caller)
- `ConfidenceScorer_RecomputeRunningSums` — ShardedSnapshotPersist.hpp:588 (LIVE) [also dead :2234]

→ Deleting `PortfolioController_*Snapshot` does NOT orphan the live ConfidenceScore leg. **PASS.**

**REVERSE cascade the plan is SILENT on (MED — scope-completeness):** the deletion CREATES a new dead cluster. `PortfolioController_LoadSnapshot:2219` is the **SOLE** caller of:
- `ConfidenceScorer_ShadowLoadLegacyV1` (ConfidenceScore.hpp:1122)
- `LEGACY_CONFIDENCE_VERSION` (#define, PortfolioController.hpp:2026, used only at :2217)

and `ShadowLoadLegacyV1` is the sole user of `ConfidenceScorerLegacyV1` + `RollingIC/RMSE/Freshness/Capacity_LegacyV1` (ConfidenceScore.hpp:1082-1116). All go to **ZERO callers** on deletion. The code's OWN comment (ConfidenceScore.hpp:1070-1072): *"After TECH_DEBT-002 closes (legacy PortfolioController removal), they can be deleted entirely + the shadow-load helper goes with them."* → This is a **subsumed** TECH_DEBT-002 closure (orphaned by this exact deletion; ≈0 marginal cost). Leaving them = **dead-code-compiled-in** (H21 anti-pattern; the plan's own stated discipline) + a half-done retirement. Plan must **sweep them in Phase D OR home explicitly to TECH_DEBT-002 with a trigger** (currently neither).

## 5. Deletion ordering (leaves-first) — SAFE for the stated scope; the gap is the unenumerated cascade

- For {4 dead fns + CONTROLLER `#define` + SOURCES row + ledger}, the "ONE commit each" atomicity is CORRECT — nothing references a just-deleted symbol mid-sequence within the stated deletes; the atomic commit prevents the `parse_current()` sys.exit.
- The **orphan cascade** (finding #4) needs its own leaves-first order the plan omits: delete `PortfolioController_LoadSnapshot` FIRST (drops the inbound ref) → THEN `ShadowLoadLegacyV1` + `LegacyV1` structs + `LEGACY_CONFIDENCE_VERSION` are leaves → delete. Unaddressed because the sweep itself is omitted.

## BONUS findings

**B1 — Test-change enumeration under-specifies the CONTROLLER_SNAPSHOT_VERSION sites (MED).** `CONTROLLER_SNAPSHOT_VERSION` has 3 test refs (all in controller_test.cpp):
- **:26749 — a COMPILE-TIME `static_assert(CONTROLLER_SNAPSHOT_VERSION >= 13 + MONEY_ENCODING_EPOCH ...)`** → the test TU will **FAIL TO COMPILE** on delete. The plan's (a) MODIFIED cites only "≈:26819 4-version assert"; the (b) BROKEN-REPLACED catch-all covers it in spirit but does NOT name the compile-time static_assert. Pin all 3 (:26749 static_assert, :26755 check, :26819 check) + note the CONTROLLER term must be **EXCISED** (not value-bumped).
- Sibling edits at the same sites: PORTFOLIO `== 7`→`8` (:26820); SHARDED `== 10u`→`11u` (:26819). The `>= N + EPOCH` floors (:26749-26757) still pass at the bumped values (info only).

**B2 — PORTFOLIO_SNAPSHOT_MAGIC symbol orphan + PORTFOLIO version coherence (MED).** After Portfolio_Save/Load deletion:
- `PORTFOLIO_SNAPSHOT_MAGIC` (symbol, Portfolio.hpp:533) has **ZERO code consumers** — the refuse-guard (ShardedSnapshotPersist.hpp:329) + the test (controller_test.cpp:6519) both use the LITERAL `0x4B434954`. Its only symbol-uses (Portfolio.hpp:557/597, PortfolioController.hpp:2034/2110) are inside the deleted fns → orphaned.
- `PORTFOLIO_SNAPSHOT_VERSION` stays alive only via the **standalone** file-scope epoch `static_assert` (Portfolio.hpp:540-544) + the 3 tests. Post-deletion it governs **NO live serializer** (the sole live Position serializer = the sharded blob ShardedSnapshotPersist.hpp:169, keyed by SHARDED_SNAPSHOT_VERSION). The plan bumps it 7→8 without stating WHY it is retained-not-retired.
- **Needs explicit plan disposition:** either (a) RETAIN `PORTFOLIO_SNAPSHOT_VERSION`+`_MAGIC` as the Position-layout epoch marker / "legacy TICK format we refuse" SSoT — then keep+bump the static_assert, sweep its stale "bump to 7" message, and repoint the refuse-guard to use the `PORTFOLIO_SNAPSHOT_MAGIC` symbol (SSoT over a bare literal); or (b) retire the whole PORTFOLIO/TICK format symmetric with CONTROLLER (the guard needs only the magic VALUE, not the VERSION).

## Novel alternative considered

**Registry-drive the retirement sweep off `FOREACH_REGISTRY` / a `RETIRED_FORMATS` ledger** instead of hand-deleting per-symbol. **Verdict: REJECT for this ship.** D-289 is a one-shot whole-format retirement (2 formats), not a recurring class; the H21 guard (`check_identifier_retirement.py`) + its golden ledger already ARE the mechanical enforcement, and `--update` is the sanctioned re-baseline. A new retirement registry would be premature framework-layer infra (diminishing returns per `feedback_framework_layer_payoff_diminishing_returns`) with grep-zero cohort. The right structural move is the SMALLER one already surfaced: extend the SOURCES/ledger coverage to the bitmap/cfg-key identifiers (already homed to TECH_DEBT-152), not a parallel retirement registry.

## Spots most worth an adversarial (a-class) refute

1. **Is the ShadowLoadLegacyV1 / LegacyV1 sweep truly SUBSUMED by D-289 (close now, ≈0 cost) or genuinely SEPARATE (home to TECH_DEBT-002, defer)?** Push on whether it belongs in E.1.2 Phase D or a later leaf — the subsumption test (marginal-cost, not adjacency) vs the plan's frozen scope.
2. **Retain-and-bump vs retire PORTFOLIO_SNAPSHOT_VERSION/_MAGIC (B2).** Refute the "retain as epoch marker" reading: if no live serializer reads it, is the bump vestigial ledger-noise, and should the whole TICK format be retired symmetric with CONTROLLER? Counter: the refuse-guard + Position-layout-epoch invariant justify retention.
3. **H21 silent-drop-via-`--update` (finding #2 residual).** Is dropping the `CONTROLLER=14` ledger row (no tombstone) acceptable, or does H21's Knight-Capital lesson want a kept tombstone row as defense against a future mistaken reuse of the `CONTROLLER_SNAPSHOT_VERSION` identifier?

## Mechanical tools RUN (arming §3)
- `check_identifier_retirement.py` — baseline GREEN (46 ids). Read-only SANDBOX simulation of the deletion → confirmed sys.exit crash (leave-SOURCES-row) + clean `--update` drop (delete-both). The authoritative tool for this surface (ledger/SOURCES lockstep a grep can't see).
- Comprehensive multi-pattern grep across all code dirs + all file types (source/tests/GUI/tools/backtest) for the 4 fns + `CONTROLLER/PORTFOLIO_SNAPSHOT_VERSION` + `PORTFOLIO_SNAPSHOT_MAGIC` + the 5 shared/legacy ConfidenceScore symbols.
