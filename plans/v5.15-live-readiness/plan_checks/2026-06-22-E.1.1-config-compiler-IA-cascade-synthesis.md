---
type: plan-check
subtype: IA-cascade-synthesis
ship_tag: v5.15.5.F.4d.1.E.1.1
workstream: "③ config-compiler EXPANDED (A-narrow) — full I→A pre-coding cascade synthesis"
date: 2026-06-22
head_at_cascade: 1da1c1c
predecessor_ssot: plans/v5.15-live-readiness/plan_checks/2026-06-22-E.1.1-config-compiler-adversarial-regate.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md  # D-247
agents: [I-1..I-5 investigative, A-1..A-5 adversarial]
verdict: GREEN-to-code (hand-rolled inline shape) — pending operator sign-off + the inclusive-upper-bound call
---

# ③ config-compiler — I→A cascade synthesis (the adversarially-validated shape)

Full cascade on the EXPANDED A-narrow shape (D-247): 5 I-class mappers → 5 A-class refuters. The adversarial half **corrected the investigative half toward a simpler hand-rolled shape** — the elegant "reuse the registry dispatch" mechanism was found vacuous (clamp), destructive (sentinel), and infeasible (no descriptor / no offset-split). All claims orchestrator-cross-checked at HEAD `1da1c1c`.

## The I→A corrections (what the adversarial half changed)
| Investigative proposal (I-cascade + my framing) | Adversarial verdict | Corrected to |
|---|---|---|
| Route capital validation through `tt::cfg_parse_field` (descriptor clamp+flag) | **REFUTED** (A-1/A-3): the `KIND_DOUBLE_PCT` descriptor clamp is **vacuous in stored space** (value ÷100'd, bound not — `CfgFieldDispatch.hpp:95-96` own comment; `risk_pct=999`→9.99 < 100.0 → never clamps; AND `Money_FromString("999").flags=0` → no warn). The capital arrays have **no usable descriptor** anyway. | **Hand-rolled inline** flag-check + hand-written inclusive bound |
| Upgrade the `_PARSE_OV_PCT` macro to clamp via the descriptor (auto-flow to all overrides) | **REFUTED** (A-2): override fields use a `0=inherit` sentinel (resolver `:1372`); the binary clamp is **unconditional** (`CfgFieldDispatch.hpp:111`, no warn gate) → would silently rewrite a deliberate `0` to the floor = a new Class-48 across **every** override field | Macro change limited to **`atof`→`parse_double_fast`** (locale-immunity only, sentinel-preserving) |
| R1: add 10 registry rows for the descriptor-gap fields | **REFUTED** (A-2): the 10 are **deliberately bound-free** (`ControllerConfig.hpp:393` "adaptation clamps"; several ARE clamp bounds) → R1 reintroduces Class-48 | **R2-narrow**: leave unclamped, defer the bound question to E.2 |
| Out-of-range capital → clamp-WARN parity with the global path | **REFUTED** (A-3): the "global clamp-WARN baseline" is a **myth** — global `risk_pct=999` also silently stores 999% (vacuous clamp + clean parse). `max_drawdown_pct` clamped to its ceiling is a **dead kill switch** (`node_dd_pct∈[0,1.0]`, trip `Gt(dd,1.0)` never fires) | **ERROR-block, uniform** for all 4 capital fields |
| Fingerprint exclusion via a type-aware `Fingerprint_ComputeCfg` wrapper | **REFUTED** (A-5): offset-split is **infeasible** on the `void*` primitive (one contiguous `SHA256_Update`); the wrapper collapses to a scratch-copy; ONE caller; future bits live in the same field | **3-line scratch-copy** (zero the field) + ERROR-mask assert at the single caller `BacktestPanels.hpp:3157` |
| C-2 guard parses `field_defs[]` string literals | **REFUTED** (A-4): the GUI set has **3 surfaces** — A (literals) + B (X-macro flag keys, arg#2 across 5 registries/2 arities) + C (`FOREACH_CFG_FIELD` walker, outside `field_defs[]`). A literal-only guard is **vacuously-green** over B+C (the highest-drift cohort) | Guard must parse **all 3 surfaces**; teeth = a positive control **per surface** |
| C-2 guard stand-alone | **REFUTED** (A-4): the F4 + C-2 guards share the engine-recognized-set extraction (RHS of both ⊆) → 2 tools = 2 drifting reimplementations | **Co-house** in `check_per_node_registry_integrity.py` + factor a shared extraction helper (operator's prior call + the code agree) |
| Convert the 2 capital strstr branches to exact-match | **EXTENDED** (A-5): the 5 non-capital branches have the IDENTICAL substring under-fire (`node_0_model_path_typo`→writes slot 0 = wrong model/market/strategy); partial conversion = mixed semantics + fragile ordering | **Convert ALL 7** strstr branches to exact-match |

## THE LOCKED EDIT-SET (9 workstreams, all boot/slow-path — zero latency budget)

**1. Capital value-validation (the F-A founding fix) — hand-rolled inline, ERROR-block, sentinel-aware.** For the 4 capital fields — `risk_pct`/`max_drawdown_pct` (dedicated branches `ControllerConfig.hpp:2847`/`:2858`, legacy arrays) + `take_profit_pct`/`stop_loss_pct` (override macro `:2939`, `node_overrides[]`): capture `Money_FromString().flags` → ERROR on MALFORMED/OVERFLOW; **`0`=inherit is VALID (preserve the sentinel)**; non-zero out-of-range → ERROR with a hand-written **inclusive** upper bound (refuse `>100.0`, allow `100.0`). NOT via `tt::cfg_parse_field`. Two idioms touched (dedicated branches + override macro).
**2. Matcher fix — convert ALL 7 strstr branches** (`:2839-2918`) to exact-suffix-match (fold into the `:2929` `strcmp` idiom; two-digit/typo/no-underscore verified correct; ordering-shadow comments dissolve). Data-driving the parser stays `.F.4e`.
**3. Override macro** — `_PARSE_OV_PCT`/`_PARSE_OV_RAW` (`:2939-2940`): `atof`→`parse_double_fast` only (locale-immunity; sentinel-preserving). NO descriptor-clamp routing.
**4. Fault bitmap `cfg_load_fault_flags`** (uint32, `ControllerConfig` cold region next to `live_capital_cfg_conflict@:576`; bits UNKNOWN_KEY/BAD_NODE_IDX/BAD_VALUE/OUT_OF_RANGE). **Readout stack-local** in `_Load` (2 arrays err/warn, ERROR-prioritized). **Fingerprint: scratch-copy zero** + ERROR-mask assert at `BacktestPanels.hpp:3157` (check layout first — likely a NO-OP regen if the field lands in `:576`'s padding hole). Add the missing `static_assert(sizeof(ControllerConfig)==N)` (closes F-I/Check-K gap). **Fix the FALSE "re-checked at serve" comment** at `ControllerConfig.hpp:347-351` (no serve path recomputes/gates the fingerprint — `ModelInference.hpp:516` stores+displays only).
**5. Cadence consumers + shared `cfg_compile_ok()` helper** — boot `main.cpp:~95` (after freopen`:88`, before mlockall`:117`; abort), backtest `BacktestSharded.hpp:119` + `BacktestEngine.hpp:2367` (abort-on-ERROR), hot-reload `Async.hpp:321`→read `new_cfg.flags` BEFORE the `:335` copy → keep-old, GUI `SettingsPanel.hpp:887` (banner). One helper = readout SSoT.
**6. Global-unknown-key refuse** — loop-bottom fall-through (`~:3119`); catches `engine_mode=`/`num_execution_cores=` (0 parse branches).
**7. C-2 GUI⊆engine parity guard** — **co-housed** in `check_per_node_registry_integrity.py` + shared extraction helper; parses **all 3 GUI surfaces**; grep scope = **cfg-key string-literals** (not `node_id` C-identifiers — 7 in Run.hpp); teeth = positive control **per surface**, RED on the 2 bricks before they delete.
**8. Deletes + harden** — 2 GUI bricks (`SettingsPanel.hpp:456` `num_execution_nodes_PLACEHOLDER` [LIVE brick, not phantom] + `:669` `confidence_freshness_tau`); PARITY-006 → RESOLVED-by-deletion (half-obsolete, GUI row is the last vestige); fgets `line[256]@:2050`→~320 + overflow-drain + fault; delete dead `TUI_HandleInput`+`PortfolioController_HotReload` orphan pair (0 callers); `config_error_log` = terminal/stderr-only (defer FILE to E.2).
**9. Migration note (R-1, expanded scope — all gitignored, nil public/CI blast, but Caramel's local workflows fault):** `engine_sharded.cfg` (sharded boot config_path `Run.hpp:1200`) + `engine.cfg.example` (rewrite its `core_N_*` block + the `:599` syntax comment) + the 5 `models/classification/test_case_*/engine.cfg` fixtures.

## Resolved decisions (adversarial-validated; "decided unless operator objects")
- Hand-roll inline, NOT `tt::cfg_parse_field` (A-1/A-3 — vacuous clamp + no descriptor + sentinel destruction).
- ERROR-block, NOT clamp-WARN, uniform for all 4 capital (A-3 — dead-kill-switch math + myth baseline + closes founding class).
- Convert all 7 strstr, NOT 2 (A-5 — capital-routing fields; mixed-semantics hazard).
- R2-narrow, NOT R1 (A-2 — 10 fields deliberately bound-free).
- Scratch-copy, NOT wrapper (A-5 — offset-split infeasible; 1 caller).
- Co-house + shared helper, NOT stand-alone (A-4 — shared RHS extraction).
- 3-surface guard + per-surface teeth (A-4 — vacuous-green over B+C otherwise).

## OPERATOR DECISIONS (genuinely open)
1. **The inclusive upper bound for capital ERROR:** refuse `>100.0`, allow `100.0` (the registry schema's own ceiling = a legit single-position-all-in config). A-3's recommendation. Tighten (also reject 100%) only if you consider 100% risk_pct a footgun worth blocking.
2. **Overall sign-off** on the corrected (bigger-than-the-original-pitch) shape.

## New findings beyond the re-gate
- The `KIND_DOUBLE_PCT` descriptor clamp is **vacuous codebase-wide** (global + per-node) for percent fields — out-of-range percent is caught NOWHERE today. ③ leads the per-node edge; global reconciliation → E.1.5/E.2 (already flagged Tension-1).
- `ControllerConfig.hpp:347-351` comment "re-checked at serve" is **FALSE** (doc-lie; no serve recompute/gate) → fix in ③.
- The 10 descriptor-gap override fields are **deliberately bound-free** (`:393`) — never enroll them in `FOREACH_PER_NODE_CFG_FIELD`.
- PARITY-006 is **half-obsolete** (the legacy field+parser already deleted v5.14.9.D; the GUI brick `:669` is the last vestige) → RESOLVED-by-deletion.
- The matcher fix is **NEW BEHAVIOR** (typo'd keys now fault) → ③'s List-C tests gate it (③ is the behavior-change phase, distinct from the FROZEN-logic ② rename which already landed).

## Tests / V-class / golden
- **List-B flip:** `controller_test.cpp:24098` (legacy `confidence_freshness_tau=400.0` "parses without crash" → assert FAULTS). The current `confidence_window==32` surrogate is a weak Pattern-B assertion.
- **List-C (NEW):** substring-colliding typo PER converted suffix (`node_0_risk_pct_typo`, `node_0_model_path_typo`, … — not `node_3_bogus`, per F-H non-vacuity) · value-typo (`stop_loss_pct=banana`→ERROR) · out-of-range (`risk_pct=999`→ERROR; `risk_pct=100.0`→clean positive control) · `0`=inherit→clean (sentinel preserved) · clean-load→no-fault (Class-51 floor) · 2 brick keys→caught · backtest gate-on-ERROR · hot-reload WARN-keep-old · global-key refuse (`engine_mode=`/`num_execution_cores=`) + positive control (`num_execution_nodes=8`→clean) · fgets-overflow→clean error.
- **Golden:** likely NO-OP regen if `cfg_load_fault_flags` lands in `:576`'s padding (CHECK layout); no absolute fingerprint golden needed (no serve gate).
- **V-class:** build 3635+/0 · char-tests · fresh asan/ubsan (fgets + readout buffer) · `calls_graph_diff` (orphan pair DELETED not flagged; hot path untouched) · determinism golden (`fp_determinism_golden.txt` UNCHANGED proves no FP-math touch; F-076 green proves zero-init) · C-2 guard per-surface teeth · the size-pin static_assert.

## Pre-coding residual
If the operator signs off, this shape is GREEN-to-code. The corrected shape touched the parser + the fingerprint seam + the GUI guard — the I→A cascade IS the recascade-on-corrected-shape pass; no further gate owed before coding (the shape is now adversarially-validated). Code → ⑤ V-class → ship-close.

---

## Forward-compat correction (2026-06-22 sweep) — D-248 is the LOCKED design

An operator-directed forward-compat sweep against the future E-series surfaced a CORRECTNESS bug in the cascade's locked shape + corrected it.

**The bug:** the cascade's "refuse `>100` uniform for all 4 capital fields" is WRONG for `take_profit_pct` — it would reject a legit >100% TP target AND contradict the operator's OWN shipped `tt::barrier_is_corrupt` SSoT (`ML_Headers/BarrierValidation.hpp`: `SL≤100%`, `TP≤1000%`; "a >100% long take-profit is rare-but-legit"). No-margin caps the LOSS side ([[project_no_margin_trading]]); gains are not margin-bounded.

**The corrected design (D-248, LOCKED):** asymmetric caps reusing `barrier_is_corrupt` (loss-side ≤100%, TP ≤1000%) · **per-field bounds** (not a uniform constant — extensible to the cohort + the D-206 ratchet knobs + E.1.6 enums) · scope expanded **4→~12** (the per-strategy tp/sl overrides) · hand-rolled inline ERROR-block sentinel-aware · convert-all-7 · atof→parse_double_fast.

**Forward-compat verified:** the **D-206** SL/TP ratchet/give-back redesign (`subplans/2026-06-12-…E-exit-system-trailing-redesign.md`, captured-parked) does NOT conflict — it operates downstream of the cfg seed (cfg tp/sl stay target/floor; trail distance is a separate `sl_trail_mult`) and adds NEW per-node knobs as auto-flowing overrides → ③'s per-field framework validates them by construction. The **Σ≤100% no-margin allocation-sum** is cross-node → **TECH_DEBT-211 → E.5**. E.1.2/E.1.5/E.1.6/E.2 fold-blocks refined for the asymmetric/per-field shape.

**Anti-patterns this shape must avoid (for the re-audit):** uniform-bound-over-heterogeneous-cohort (the TP bug) · vacuous-percent-clamp (codebase-wide) · Class-48 sentinel-destruction (`0`=inherit) · Class-51 vacuous-green guard (per-surface teeth) · Class 25/26/27 cfg-scope.

→ Operator-directed NEXT: codify the slate + RE-RUN audit subagents on THIS corrected shape to keep hunting holes/gaps before coding.

---

## Re-audit round (2026-06-22) — 4 a-class on the corrected shape; the SHAPE RESHAPES again → D-249

Operator-directed loop-until-dry re-audit on the D-248 shape. The spine holds, but the SCOPE + per-field details were wrong — material holes, the load-bearing ones orchestrator-code-verified:
- **🔴 GLOBAL 3RD IDIOM (verified):** `CFG_PARSE_PCT` (`ControllerConfig.hpp:2187`) parses the GLOBAL capital fields (`:2264-2284`) with the identical flags-dropped/no-bound body; `ControllerConfig_ResolveForCore:1366` copies global-then-override → a no-override node INHERITS the unvalidated global → **E.1.5 purity break by inheritance.** ③ must harden the global idiom (3 idioms, not 2).
- **🔴 UNITS CONTRADICTION (ship-blocker, verified):** `barrier_is_corrupt` is FRACTION-space (`MAX_TP=10.0`/`MAX_SL=1.0`); the locked design also says percent-space ">100.0". Validate in FRACTION space on the ÷100'd value; strike the percent literal.
- **🔴 STALE DESCRIPTOR:** every TP descriptor `clamp_max=100.0` → reading it re-caps TP at 100% (the fixed bug). barrier wins for tp/sl; the descriptor is wrong for TP.
- **🟠 momentum dropped:** `momentum_tp_mult/sl_mult` (RAW stddev multipliers) absent from the cohort + the named outliers → no-margin loss-cap unenforced on 1/5 strategies.
- **🟠 over-claims:** "ratchet validated by construction" is false (validation hand-rolled OUTSIDE the parse macro → new knobs parse but aren't validated); "one helper" is false (3 primitives × 3 bound-sources × 3 units → ≥3 validators); the RAW channel is dead (`parse_double_fast` swallows `.ec`→0.0, colliding with the inherit sentinel).
- **🟡 homing:** TD-211's "even-split Σ-safe" depends on the unvalidated global `risk_pct` (corrected); the Σ-check is homed to E.5 which is submit-time-only (half-homed → add an E.5/E.1.5 boot-Σ line-item); the SAFETY_CRITICAL global kill-switch caps share the vacuous-clamp hole (closed for free by the primitive-macro fix). **Biggest unexamined surface:** the `ml_tp_pct/sl_pct` cfg-bound × STAMP_BOUND × barrier-bound three-SSoT reconciliation.

### THE RESHAPED DESIGN (D-249)
1. **Primitive/macro-layer fix (covers ALL 3 idioms):** one shared flag-capture + bound-check folded INTO the 3 flag-dropping macros (`CFG_PARSE_PCT` + `_PARSE_OV_PCT` + the dedicated branches) → closes the global hole + the E.1.5 break + the SAFETY_CRITICAL globals for free + makes validation auto-flow (the "by construction" claim becomes TRUE by folding validation into the parse macro, NOT a parallel hand-rolled block).
2. **Per-field routing TABLE (data, not prose):** each field → `{bound-source: BARRIER_TP | BARRIER_SL | LOSS≤100% | DESC[lo,hi] | MULT[lo,hi]; units: pct-÷100 | fraction | multiplier; zero: inherit | disable | default-split | reject | literal-WARN}`. Makes units explicit (fraction-space), the stale-descriptor trap unrepresentable (tp/sl = BARRIER, never DESC), and routes momentum + entry_offset_pct + partial_exit_pct + the per-field sentinels correctly. **This table IS the complete-surface enumeration that ends the piecemeal discovery.**
3. **`parse_double_fast_checked`** for the RAW channel; **WARN** on `ml_*_pct=0` (the silent-0%-TP-on-LEGACY footgun).
4. **Homes:** Σ(node_risk_pct) boot-check → E.5/E.1.5 line-item; verify the ML STAMP three-SSoT before coding; correct TD-211.

**Meta-lesson:** 3 rounds each found a SCOPE correction (per-node parser → asymmetric caps → global idiom) = the surface was under-enumerated initially (`feedback_dont_generalize_substrate_before_input_space_known`). The per-field routing table is the enumeration that closes it.

→ Operator-directed (2026-06-22): a 3rd DEEPER pass (assume-findings-not-OK) before converging + a public-private strategy-retirement decision (separate from ③; ③ = infrastructure). D-249 formal capture + the codification slate consolidate AFTER the deeper pass.

---

## 3rd DEEPER pass (2026-06-22, 5 deep a-class, assume-not-OK) — D-249's fold target was DEAD CODE; the live chokepoint is the WALKER → D-250

### CRUX RESOLVED (AR-11 orchestrator code-read) — Deep-1 vs Deep-5 factually disagreed; code read settles it
`ControllerConfig.hpp:2105-2160` + `:2264`: the registry walker `EMIT_GLOBAL/PER_NODE_CFG_PARSER_CASE` (`:2133-2160`) runs FIRST, matches each FOREACH-registry row via `strcmp`, calls `tt::cfg_parse_field`, `continue;`s. The code's OWN comment (`:2122-2124`): *"For fields IN the registries, the walk's `continue;` makes the manual lines unreachable."* ∴ **`CFG_PARSE_PCT(take_profit_pct)` etc. (`:2264+`) are DEAD** for every registry-row field → **Deep-1 RIGHT (verified); Deep-5's "over-fires on 22 callers" WRONG.** Folding the bound-check into `CFG_PARSE_PCT` (my SSoT + D-249 + the 4 prior rounds) = **Class-51 vacuous-green guard** (ships, never runs). Manual branch LIVE only for HAS_SIDE_EFFECT/MANUAL_PARSER rows (`fee_rate_maker/taker` `:2270`) + NON-registry fields.

### CORRECTED DESIGN (D-250 — supersedes D-249's macro fold) — live chokepoint = `tt::cfg_parse_field` (`CfgFieldDispatch.hpp:82-117`)
1. **Bound-source metadata IN the descriptor** (per-field enum `BARRIER_TP / BARRIER_SL / LOSS_CAP≤100% / DESC_RANGE[lo,hi] / MULT[lo,hi] / DOLLAR / NONE`) — the routing table AS auto-flow descriptor metadata. tp/sl → BARRIER (never the stale `clamp_max=100.0`); validate in FRACTION space.
2. **Walker consumes it** → validates the GLOBAL registry-row values (inheritance root → closes H22-by-inheritance) + AUTO-FLOWS (a new registry capital row validates free — "by construction" becomes TRUE here, where it was false at the dead macro).
3. **`_PARSE_OV_*` override macros consume the SAME metadata** (sentinel-aware: skip `0`=inherit, else bound-check).
4. **4 non-registry capital fields** (`min_kill_loss`[SAFETY]/`starting_balance`[denominator]/`min_sl_tp_ratio`/`min_hold_gain_pct`) → migrate INTO the registry (auto-flow) OR explicit DOLLAR/ratio checks (NOT percent).
5. **DELETE the 25 dead `CFG_PARSE_PCT/MONEY` capital branches** (H21 hygiene).
6. **Post-resolve Σ-aggregate** (Deep-5/Deep-4) — no-margin cross-node sum + the dollar-floor overshoot (`<$1→$1` at 5 sites lets Σ exceed balance even with Σ%≤100 → sum POST-FLOOR DOLLARS) → E.5.

### Remaining holes (homed)
- **Drift-gate INVERTED for ml_tp/sl_pct (Deep-2):** `COHORT_GATE_PER_HORIZON_BARRIER` fires when feature ON (moot) / silent when OFF (load-bearing) → false Tier-1 model-reject. ③ inverts it + scopes WARN-on-0 to LEGACY.
- **Fingerprint NO-OP UNPROVEN (Deep-2/4):** measure layout (`clang -fdump-record-layouts`) + `static_assert(sizeof==N)` FIRST; zero-at-capture (`BacktestSharded:122`) > zero-at-1-caller. Doc-lie "re-checked at serve" (`ControllerConfig:348`) confirmed false → fix.
- **3-SSoT (ml_tp/sl_pct):** cfg-descriptor `clamp_max=100.0` stale vs barrier 1000% → barrier wins; reconcile before coding.
- **Name-lies (units:fraction NOT pct):** `partial_exit_pct`, `momentum_min_tp_margin_pct`. **No-sane-bound:** `momentum_min_tp_margin_pct` + `*_mult` multipliers → MULT/none, never barrier.
- **LIVE alpha leak (Deep-3, verified):** `Strategies/private/EmaCross.hpp` tracked+pushed public; gitignore + `git rm --cached` (local) + history decision (operator) + reconcile boundary-law doc-drift (the law omits the `private/` tier → why the gitignore was never added). ③ = infrastructure (unaffected); retirement = census-only; D-206 = public-mechanism / private-policy split.

**Meta:** Deep-1 finally MAPPED the parser substrate (walker-shadows-macros + the complete field enumeration incl. 4 missed capital fields) that the prior 4 rounds proposed fixes WITHOUT. Substrate now mapped → design determinable → CONVERGENCE. 5 rounds / ~28 agents / no code = the treadmill is the risk now → decide the layer (walker-auto-flow, D-250) + the leak, then CODE; V-class verifies.

---

## D-252 supersedes D-250 (2026-06-22, post-independent-verify) — the design COLLAPSES into pure reuse

Independent a-class verify of D-250 (operator's "are you sure?") → **D-250 REFUTED; D-252 is the corrected design.** D-250's errors (all real, orchestrator-verified against `CfgFieldRegistry.hpp`): (a) proposed a `bound_source` enum COLUMN in `CfgFieldDescriptor` → VIOLATES the `.F.4b` schema LOCK (`:39-41` "ADD ROWS only; no schema changes"); (b) misapplied H18 — cited it as the canonical sister but the capital cohort is DENSE (~18/18) and H18's OWN skip-rule (`sidecar-…md:248`) says "skip when custom rows >50%"; (c) reinvented the EXISTING compile-time mask framework (`cfg_compute_mask` + `FOREACH_METADATA_BIT`, `:1129-1182` — verified; `SAFETY_CRITICAL` + `WARN_ON_CLAMP` already bits); (d) left 3 correctness preconditions undecided; (e) closed NO TD cleanly + introduced the TD-211 doc-lie (now fixed). **D-250 STATUS: CLOSED — superseded by D-252 due to findings** (the chokepoint-is-the-walker insight SURVIVES into D-252; only the validation-mechanism changed).

### D-252 (corrected design — pure reuse of verified extant infra; NO new schema/sidecar/enum)
1. **One new bit `CAPITAL_BOUND`** in `FOREACH_METADATA_BIT` (`:1155`) — `cfg_compute_mask` auto-generates its mask (sanctioned extension; honors the `.F.4b` lock).
2. **The walker `tt::cfg_parse_field`, on the bit, applies the asymmetric no-margin cap** sourced from `BarrierValidation.hpp` (`BARRIER_SANE_MAX_SL=1.0` loss / `BARRIER_SANE_MAX_TP=10.0` gain) — single-source the cap CONSTANTS, never a re-stated literal. [OPEN wrinkle for the verify cycle: `barrier_is_corrupt(tp,sl)` is a PAIR predicate but the walker is PER-FIELD → resolve as single-field caps via the constants at the walker, OR a post-parse pair check.]
3. **Policy = the existing `WARN_ON_CLAMP` bit:** no `CAPITAL_BOUND` → status-quo clamp (B1 safety, ~40 clamp fields untouched); `CAPITAL_BOUND` → ERROR.
4. **Stamp wire-context stays corrupt-only** (never policy-ERROR a signed historical value — B2).
5. **Boot: fail-closed in live, fail-open-loud in paper** (preserves the paper-test dev loop — B3).
6. **Override path:** thread `FIELD_IDX` into `_PARSE_OV_PCT` so the same mask+predicate fire for `core_N_` overrides.
7. **Fix the descriptor `clamp_max` NUMBER for TP → 1000%** (GUI slider + parser agree — B5).
8. **Boot-Σ + dollar-floor → E.5** (cross-node, not walker-closeable per H22); per-field value-bound = ③.
9. **③ full scope** = this validation-engine + the `core_→node_` key clean-break + the error-log/readout.

### D-251 — all-strategies-private (operator DECIDED 2026-06-22)
ALL real/tuned strategies → private. Public = the strategy FRAMEWORK + a "strategy SECTION" (make-your-own template/reference), NOT a tuned strategy. Backup = the **workspace** (reuse the existing `__has_include` + `/sync-workspace` private mechanism). Timing = **FOLD IN NOW**. History = **NO scrub; go-forward-private only.** EmaCross bleeding-stop committed (`252973f`). D-206 = public-mechanism / private-policy split. Reconcile the boundary-law doc-drift (omits the `private/` tier). FOLD: TD-185 (deprecated `*_ExitAdjust` in EmaCross) cleanup into the move.

→ Operator-directed: rerun an agent cycle to VERIFY D-252 — with **solid-foundation-for-the-rest-of-E + future-maintenance as the CENTRAL criterion** (does D-252 support E.1.2 / E.1.5 / E.1.6 / E.2 / E.5 + D-206; is a new validated field genuinely low-touch?). THEN code. Formal decision-log (D-249/D-250 closed, D-251, D-252) + codification slate consolidate post-cycle.

---

## D-252 verify cycle COMPLETE (3 agents, 2026-06-22) → D-253 (cycle-converged). Walker frame SURVIVES; 4 corrections + 1 coverage hole.

3-agent cycle (foundation-for-E + correctness + foundation/TD). ALL converge: **WORKS-WITH-CORRECTIONS — the walker-chokepoint frame is materially CORRECT (NO re-cascade); the DETAILS need 4 corrections + the 3rd idiom.** Determinism-neutral VERIFIED (masks in `.rodata`; fingerprint hashes the cfg INSTANCE, not the descriptor — Agent 1). Foundation verdict: D-253 SUPPORTS the rest of E (E.1.6/E.5/D-206 as named 1-bit/runtime extensions), does NOT lock them out.

### D-253 = D-252 + the cycle corrections
1. **TWO side-tagged bits `CAPITAL_BOUND_LOSS` / `CAPITAL_BOUND_GAIN`** (NOT one) — triple-confirmed (all 3). One bit can't tell the walker SL-cap(1.0) from TP-cap(10.0); no field→side discriminator exists. Consumes the last 2 uint16 headroom bits (`:150`); next → uint32 (cheap). Closes the uniform-bound-over-heterogeneous-cohort anti-pattern in miniature (one bit re-introduces it).
2. **SPLIT THE SEAM (Agent 2 CORRECTION 2):** the walker `tt::cfg_parse_field` is VOID + lacks `trading_mode` + trading_mode isn't resolved until POST-loop (`use_real_money` alias precedence `:2044/:2531`; `live_capital_cfg_conflict:576` defers post-loop for exactly this). → walker = DETECT + FLAG (fault to a `cfg` member) + corrupt-only in wire-context; **POLICY (ERROR-vs-keep, fail-closed-live/fail-open-paper) lives POST-LOOP in a `cfg_compile_ok()` helper.** D-252's "walker ERRORs at parse" was WRONG.
3. **THREE idioms (Agent 2 CORRECTION 3 — coverage hole):** idiom-1 walker (global keys ✓), idiom-3 override-macro (FIELD_IDX threading ✓), **idiom-2 the dedicated legacy-array strstr branches (`node_N_risk_pct`→`node_risk_pct[]:2847`, `node_N_max_drawdown_pct`→`[]:2858`) BYPASS the walker** (no descriptor/FIELD_IDX/metadata — TRANSITIONAL aux-registry only `:807/:810` "delete at WIP2g"). These are LOSS-SIDE no-margin fields → unvalidated = H22 break (`node_N_risk_pct=999`→999%). **③ hand-rolls idiom-2 validation (reuse the loss-cap constant) OR gates on WIP2g deleting the legacy arrays first** — verify ordering; loss-side capital can't sit unvalidated for an interim.
4. **Restore `parse_double_fast_checked`** (Agent 3 — DROPPED in D-250→D-252; RAW `_PARSE_OV_RAW` bare `atof("banana")==0` collides with `0`=inherit → silent capital corruption; D-206's RAW mult knobs ride it).
5. **Per-explicitly-tagged-row, NEVER name-heuristic** (Agents 2,3 — units-trap: `momentum_min_tp_margin_pct`/`partial_exit_pct` are fraction-space KIND_DOUBLE despite names; the `*_mult` RAW knobs get MULT bounds NOT barrier — 3.0 ≠ 300%). Trap rows get NEITHER bit.
6. **Pair-predicate RESOLVED (Agent 2):** `barrier_is_corrupt` is a pair SIGNATURE but SEPARABLE semantic (no tp×sl coupling) → single-field caps via the constants is CORRECT; strike the pair-check option. The tp-vs-sl RELATIONSHIP (`min_sl_tp_ratio`) is separate downstream-execution (`PortfolioController.hpp:1330`).
7. **Honest touch-count:** per-node bounded field = **2-touch** (registry row + override row) + one-time cost (2 bits + 6 `CFG_COMPOSE_AUDIT_DECISIONS` cells + the seam-split + the fault channel). NOT "pure low-touch." GLOBAL-only = genuine 1-touch. Genuine per-node 1-touch awaits WIP2f override-registry unification.

### Foundation extensions — NAMED + HOMED (not assumed-covered)
- **Enum set-membership (E.1.6):** numeric cap doesn't model it; `KIND_INT_ENUM` is a separate rail that WARNs-then-DEFAULTS (fail-OPEN) + 0 live rows. → `ENUM_STRICT`/`REFUSE_ON_UNKNOWN` bit (same pattern) = E.1.6's owed extension (SUPPORT). ③ scope-OUT enums explicitly.
- **Boot-Σ + dollar-Σ (the no-margin Σ guarantee) has NO LANDED OWNER:** D-252→E.5; E.5=submit-time-only; E.1.5 fold=unowned. → explicit LANDED E.5/E.1.5 acceptance row (boot-Σ AND submit AND post-floor-dollar); D-253 decision log states which Σ it defers. (TD-211.)
- **D-206 MULT loss-floor = RUNTIME guard** (not "validated by construction"): bounded `sl_trail_mult` doesn't cap realized loss (`ratchet_sl=price−stddev×mult`) → slow-path refuse `ratchet_sl<entry×(1−SANE_MAX_SL)` = D-206 acceptance. D-253 cap = percent-form ONLY.
- **Relational (min_sl_tp_ratio):** already downstream; out of the per-field walker.

### Homed residual TDs (Agent 3)
unhomed vacuous-clamp founding finding → NEW TD (close capital-subset at ③; E.2 global) · dead `CFG_PARSE_PCT/MONEY` deletion (H21) → tracked + verified-at-close · `node_model_path` zero-validation → TD, disposition `.F.4e`/E.1.6 · override-registry-duality consolidation → WIP2f.

### Pre-code verifies (2)
`ml_tp_pct`/`ml_sl_pct` THREE-SSoT ordering (STAMP_BOUND × STAMP_BOUND_CFG_DERIVED × CAPITAL_BOUND_GAIN; wire-context corrupt-only handles reload) · idiom-2 / WIP2g sequencing (hand-roll-in-③ vs delete-legacy-first).

**Meta:** the cycle (round 7, operator-directed) CONVERGED — the walker frame is stable across the last 3 rounds; corrections are bit-granularity + seam + idiom-coverage + homing, NOT layer. It caught 3 real defects coding-D-252-as-written would have shipped (the uniform-bit, the walker-ERROR-at-wrong-seam, the idiom-2 H22 hole). **D-253 is the codeable foundation** → capture D-253 + the homings + codification slate; CODE ③; V-class gate. NO round 8 (treadmill; residual = code-time + the 2 verifies).

### DECISIONS RESOLVED (2026-06-22, operator)
- **D1 — idiom-2:** HAND-ROLL the loss-cap check on the 2 dedicated legacy-array branches (`node_N_risk_pct:2847` / `node_N_max_drawdown_pct:2858`) in ③ NOW — close the H22 over-allocation hole immediately; do not wait on the WIP2g legacy-array deletion.
- **D2 — fail-mode:** ALWAYS ABORT on an out-of-range CAPITAL value, ALL modes ("bad to leave live"). This DROPS the mode-dependence → the post-loop policy is simply "any capital fault → ERROR-block boot" (no `trading_mode` needed at the policy point — simpler + sidesteps the trading-mode-not-resolved-at-parse timing). The seam-split still holds (walker DETECTS+FLAGS; post-loop `cfg_compile_ok()` ABORTS if any capital fault). WARN_ON_CLAMP non-capital fields keep status-quo clamp.
- **D3 — Σ owner = the CLUSTER (cluster-aggregator), not "a check in E.5".** The cross-node Σ(allocations) ≤ 100% is a property of the entity that owns the shared balance + sees all its nodes; a NODE structurally cannot (H22 — can't see siblings). So it is the **cluster-aggregator's boot-time invariant** (Σ ≤ 100%, → ≤ 1−reserve_pct once E.5 reserve lands), a SIBLING of the existing per-cluster concentration governor (TD-190 / A34). E.5 is where the cluster capital framework is BUILT; the cluster-aggregator is the OWNER. ③ records which Σ it defers (boot + submit + post-floor-dollar). TD-211 re-homed to cluster-aggregator ownership.

→ All decisions locked. NEXT: consolidate the formal decision-log (D-249/D-250 CLOSED, D-251, D-252 superseded, **D-253**) + home the residual TDs (vacuous-clamp NEW TD; dead-macro; node_model_path) + the codification slate; then CODE ③ (build-gated micro-commits); V-class gate.

---

## Pre-coding audit gate (4 agents, 2026-06-22) — DESIGN CONFIRMED; one coding-shape refinement (→ post-resolve sweep, D-254) + pins

Gate = DESIGN_SPECS-conformance + deps/call-sequence + parity/determinism + blindspot, on D-253. **DESIGN FRAME CONFIRMED — NO re-cascade** (all 4: the walker insight, the 2 bits, the asymmetry, ALWAYS-ABORT, the cluster-Σ are sound). Gate-1 CONFORMS; Gate-2 deps RESOLVE; Gate-3 parity/determinism SOUND (F-076 ctor structurally zeroes the fault field); Gate-4 CONCEDES the frame, REFUTES coding-readiness with the pins below.

### HEADLINE — a simpler CODING SHAPE (Gate-4 + Deep-5 converge): validate at ONE POST-RESOLVE SWEEP, not 4 parse-seams → D-254
D-253-as-written threads validation through 4 seams (walker `is_fp_decimal_v` branch + idiom-2 inline hand-roll + idiom-3 `_PARSE_OV_PCT` FIELD_IDX-threading + post-loop policy). A **single post-resolve sweep** over the RESOLVED `cfg.nodes[c]` (after `PopulateCoresFromFlat:3255`) + the 2 legacy arrays (`node_risk_pct[]`/`node_max_drawdown_pct[]`, idiom-2) — read the per-field `CAPITAL_BOUND_*` bit (via FIELD_IDX), compare the resolved FRACTION value against `BARRIER_SANE_MAX_*`, OR into `cfg_load_fault_flags`, then ALWAYS-ABORT — is the LOWER-RISK shape for the SAME bit-tagged design. It DISSOLVES: the highest trap (fraction-space-in-the-shared-`is_fp_decimal_v`-branch vacuous-green), the wire-context entanglement (no stamp-reload path post-resolve), the idiom-2 inline hand-roll (D1 — resolved values + the 2 arrays are swept at the one site), the idiom-3 macro threading, AND the `cfg_parse_field` signature change. STILL auto-flows (iterates registry descriptors) + MORE H22-faithful (validates the actual per-node resolved values the node trades on). Cost: one boot-time struct walk (negligible). The 2 bits + asymmetry + ALWAYS-ABORT + cluster-Σ all survive UNCHANGED — only the validation SITE moves. **RECOMMEND adopt (D-254).**

### Pins that survive EITHER shape (code-time)
- **Finding A (Gate-4) — cohort INCOMPLETE:** `kill_switch_daily_loss_pct:516` / `kill_switch_drawdown_pct:519` (both SAFETY_CRITICAL) + `max_exposure_pct:515` are loss-side `KIND_DOUBLE_PCT` with the identical vacuous hole + were OMITTED from the ~12. The per-row-bit move means NOT "closed for free" → tag them `CAPITAL_BOUND_LOSS`. Cohort ~12 → **~15** (enumerate-the-set).
- **Both-active assert (Gate-1):** a dedicated `constexpr` mask-AND-popcount==0 assert (≈6-line word-AND helper, sister to `cfg_compose_*`) beside the cohort-coverage assert `:1202` — NOT an H16 enrollment assert. Closes the uniform-cohort anti-pattern structurally.
- **Fraction-space:** compare the ÷100'd fraction vs `BARRIER_SANE_MAX_*`, NEVER the stale descriptor `clamp_max=100.0` (the sweep makes this cleaner — resolved values).
- **Determinism (Gate-3):** F-076 ctor `memset` structurally zeroes `cfg_load_fault_flags` (SOUND regardless of placement). BUT MEASURE the layout (`clang -fdump-record-layouts`) before adding it + ADD the absent `static_assert(sizeof(ControllerConfig<F>)==N)` + make `Fingerprint_Compute`'s cfg arg TYPED (`const ControllerConfig<F>&`) so Check-K is non-vacuous (it's blind today — the `void*` launders sizeof).
- **Gate-1 deviations (subsume):** repoint the stale overflow guard `:211` (watches bit 11 not the high bit); composition cells = `COMPOSE_NA`×6 (pin explicitly — count-asserted only, not mask-wired → a wrong cell builds green + lies); update the "2 bits headroom"→"0 bits" comment `:150`.
- **`parse_double_fast_checked` is NET-NEW** (grep empty — NOT "restored"); write in `ParseFast.hpp` (surface `.ec`); switch `_PARSE_OV_RAW`. Doc-lie "re-checked at serve" (`ControllerConfig:347-349`) confirmed FALSE → fix.
- **Precision:** tp/sl/risk/dd are PER_NODE registry rows (the per-node walker handles the flat root); "global idiom / 3 idioms" is imprecise — the genuine 3rd surface is idiom-2 (the legacy arrays). Functionally unaffected.

→ Operator decision: adopt the post-resolve-sweep coding shape (**D-254**, RECOMMENDED) + confirm Finding A (cohort → ~15). Then CODE; V-class gate. This is the LAST pre-code decision — the design is confirmed. [Operator: BOTH confirmed 2026-06-22.]

### Coverage/scope focused verify (1 a-class, 2026-06-22) — GREEN. Coverage COMPLETE, scope CLEAN; 2 code-time corrections. → CODE-READY.
- **Coverage CONFIRMED complete:** the required sweep set = **{X-macro walker over resolved `nodes[c]`} ∪ {2 hand-rolled legacy-array loops: `node_risk_pct[]` + `node_max_drawdown_pct[]`}**. No global-only capital field missed. `risk_pct`/`max_drawdown_pct` live ONLY in the legacy arrays (NOT folded into `nodes[c]`; consumed at `Run.hpp:923` / `ControllerEventLoop:2918`) → the 2 hand-rolled loops are mandatory (matches D1).
- **Finding A's 3 are PER-NODE, NOT global-only** (no override path → `nodes[c]` copy == flat global by construction) → the `nodes[c]` sweep catches them; the gate's "add a global pass" worry does NOT fire.
- **MECHANISM CORRECTION (gate text was imprecise):** there is NO generic `FIELD_IDX→&nodes[c].<field>` runtime accessor (the `tt::cfg_*_field` fns all take a `T& dst`). The sweep is an **X-macro EXPANSION walker** (a `FOREACH_PER_NODE_CFG_FIELD(MACRO)` body referencing `cfg->nodes[c].name` + reading `g_per_node_cfg_field_descriptors[FIELD_IDX_PER_NODE_##name].metadata_flags` — the live idiom at `ControllerConfig.hpp:2155` / `CfgFieldRegistry.hpp:775`) + the 2 hand-rolled legacy loops (aux arrays aren't registry-walkable). STILL auto-flows per-row via expansion. **Code to THIS idiom, not a phantom accessor.**
- **PIN (latent H22 seam):** the 3 Finding A fields are validated in `nodes[c]` but CONSUMED from the flat global (`PortfolioController` reads `ctrl->config.kill_switch_*`). Equal today (no override) but fragile if a future override lands → **lean: validate the FLAT GLOBAL directly for the 3 (what the consumer reads + SAFETY_CRITICAL), 3 explicit checks.**
- **Scope CLEAN:** boot-time read-only pass; no new surface / resolver / signature; only the new `cfg_load_fault_flags` field (in-scope) + the BarrierValidation include.

→ **CODE-READY.** Design confirmed (gate) + coverage complete & scope clean (this verify) + mechanism precise (X-macro walker + 2 hand-rolled loops). NEXT: CODE ③ build-gated; V-class gate.

---

## ③ CODING PROGRESS (2026-06-22) — steps 1-3 committed, build-gated 3635/0 each
- **7b35517 step 1** — `CAPITAL_BOUND_{LOSS,GAIN}` bits + 15-field cohort tagging (5 GAIN / 10 LOSS) + both-active compile guard + overflow-guard repoint + composition cells.
- **a75fddf step 2** — `cfg_load_fault_flags` field + fingerprint size-pin (`sizeof(ControllerConfig<64>)==53056`) + zero-at-capture + "re-checked at serve" doc-lie fix.
- **03830e8 step 3** — parse-point MALFORMED-capture (FLAT path): walker `fault_out` sink + reasoned `[cfg] FATAL` (field/value/cause) + `cfg_compile_ok()` + main.cpp boot-refuse. EXCESS_DP→WARN; wire_context corrupt-only. **Founding bug (`stop_loss_pct=banana`) closed for the flat/global path.**

### REMAINING ③ (build-gateable chunks)
- **4b — per-node malformed-capture:** the override macro `_PARSE_OV_PCT` (`core_N_*`) + the idiom-2 legacy strstr branches (`node_N_risk_pct`/`node_N_max_drawdown_pct`): same `Money_FromString` MALFORMED|OVERFLOW → `CFG_FAULT_CAPITAL_MALFORMED`.
- **4c — out-of-range post-resolve sweep:** an X-macro walker over `cfg.nodes[c]` (`CAPITAL_BOUND` bit → fraction-space compare vs `BARRIER_SANE_MAX_SL/TP`) + the 2 legacy-array loops + the 3 flat-global kill-switch checks (validate the flat global directly per the coverage-verify pin) → `CFG_FAULT_CAPITAL_OUT_OF_RANGE`. Insert after `PopulateCoresFromFlat` in `_Load`.
- **caller-wiring:** `cfg_compile_ok` boot-check in the backtest + GUI loaders (main.cpp done).
- **key clean-break (original ③ scope):** `core_→node_` HARD-REFUSE + GUI parity guard + DELETE the dead `CFG_PARSE_PCT/MONEY` capital branches (TD-215, H21 hygiene).
- **tests:** positive/negative controls (`risk_pct=999`→abort · `take_profit_pct=11.0`→abort · `=2.0`→clean · `banana`→abort · `core_N_`/`node_N_` variants · `0`=inherit preserved · wire-context NOT aborted · `EXCESS_DP`→WARN-not-abort).
- **⑤ V-class:** asan/ubsan + determinism re-cert (the size-pin + the new field).
- **post:** capture consolidation (task #7) + all-strategies-private restructure (task #6).
