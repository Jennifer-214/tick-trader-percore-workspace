# v5.14.10 handoff prompt — Bayesian Thompson sampling bandit

**Created:** 2026-05-10 (regenerated via `/handoff v5.14.10`)
**Target ship:** v5.14.10 — Bayesian Thompson sampling bandit (operator-toggleable vs existing Exp3-IX)
**Branch:** `feat/v5.14-foxml-port-and-maker`
**Pre-tag rollback anchor:** `v5.14.9` (umbrella, commit b09b2d5)
**Plan file:** `plans/v5.14-foxml-port-and-maker/subplans/2026-05-08-v5.14.10-bayesian-thompson-bandit.md` (note: plan heading reads "v5.14.11" — stale; renumbered to v5.14.10 in MASTER 2026-05-10; fix during Step 1)
**Sprint MASTER:** `plans/v5.14-foxml-port-and-maker/MASTER.md`
**Predecessor postmortem:** `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-10-v5.14.9-session-postmortem.md`

---

## /handoff skill metadata (this section is for the operator + skill audit)

**Generated:** 2026-05-10 via `/handoff v5.14.10 <plan-path>`
**Generator skill:** `tick-trader-percore-workspace/claude-skills/handoff/SKILL.md` v0.1 (first invocation)
**State at generation:** Version.hpp=5.14.9, branch=feat/v5.14-foxml-port-and-maker, latest=490618b, tag=v5.14.9, clean tree (1 untracked context_cont.md)

**Pattern matches from plan scan (Stage 3):**
- `curve-registry-pattern.md` — STRONG MATCH. Plan Step 3 dispatch is `switch (config->bandit_algorithm)` with 3 named modes (Exp3 / Thompson / Both). Canonical case for FOREACH_BANDIT_ALGORITHM registry + fn-pointer dispatch.
- `cfg-flag-eligibility-criteria.md` — RELEVANT. `bandit_algorithm` is an ENUM (3 values), not a boolean → NOT cfg-flag-eligible (fails criterion 5; bitmap is for booleans). Stays as direct int cfg field. Document the rejection if anyone proposes migration.
- `wire-format-byte-preservation-discipline.md` — RELEVANT. `thompson_state.json` is a new wire format. Plan documents forward-compat-by-absence shape (same as v5.13.4.C exit_bandit). Discipline doc captures byte-preservation invariants if HMAC binding is later added.
- `audit-driven-pre-coding-gate.md` — QUALIFIES. Ship has ≥5 new fields (5 cfg fields + ThompsonBandit struct + 3 fns + ~12 tests), new file (`ML_Headers/ThompsonBandit.hpp`), picks up plan from 2 days ago (post v5.14.9 closure). Fire all 5 audits in parallel.

**Soft / partial matches (worth checking but lower confidence):**
- `autopopulate-pattern-for-production-caller-class.md` — Save/Load pair (`SaveThompsonState` + `LoadThompsonState`) parallels existing `SaveBanditState` + `LoadBanditState`. Currently 2 sites each; AUTOPOPULATE threshold is 2+; consider if a future 3rd bandit algorithm (UCB?) would trigger.
- `structural-fix-preferred-decision-framework.md` — `ThompsonBanditState` is a PARALLEL ARRAY to `BanditState` (different storage, same dispatch contract). Class 18 mirror risk if 3+ algorithms ever land. Today: 2 algorithms = acceptable parallel. Note in postmortem if a 3rd is planned.

**Overlapping TECH_DEBT (Stage 4):**
- **TECH_DEBT-018** (OPEN) — Codify `/precoding-audit` Layer 1 orchestrator skill. Ship's pre-coding gate would benefit; not blocking. `/handoff` partially fills this gap (composes the gate by-reference in this prompt).
- **TECH_DEBT-022** (OPEN) — Engine.cfg parser perfect-hash dispatch. Ship adds 5 new cfg fields, marginally extends the linear strcmp parser; doesn't push past trigger threshold but should be noted.
- **TECH_DEBT-010** (OPEN) — FOREACH_CALIB_LOG_COL registry. Plan's dual-mode (cfg=2) extends calibration log with Thompson choice + Exp3 weights columns. If extension uses ad-hoc CSV writer code, this TECH_DEBT activates. Recommend ship absorbs the registry refactor OR documents explicit defer.

---

## Paste this prompt into a fresh Claude Code session to start v5.14.10

```
I'm picking up v5.14.10 (Bayesian Thompson sampling bandit) for the
v5.14-foxml-port-and-maker sprint. This is a fresh context window;
do NOT trust any prior-session memory — verify everything against
current code.

## Step 0 — orient + verify state (BEFORE planning anything)

1. Run in parallel:
   - `cat /home/caramel/code/FoxML_Trader_v2/Version.hpp` — confirm "5.14.9"
   - `cd /home/caramel/code/FoxML_Trader_v2 && git log --oneline -5` — latest should be 490618b "docs: update plans/ cross-refs for workspace reorg" (on top of b09b2d5 v5.14.9 umbrella)
   - `cd /home/caramel/code/FoxML_Trader_v2 && git tag --sort=-creatordate | head -5` — confirm v5.14.9 + sub-tags exist
   - `cd /home/caramel/code/FoxML_Trader_v2 && git status` — clean tree (only context_cont.md untracked expected)

2. Read these in parallel (load context):
   - `CLAUDE.md` (engine repo project instructions)
   - `CLAUDE.local.md` (private overlay; design philosophy + going-forward rules)
   - `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md`
     (auto-memory index; ~17 entries Caramel has built up; entries
     marked feedback_* are load-bearing operator preferences)
   - `plans/v5.14-foxml-port-and-maker/MASTER.md` (sprint master plan)
   - `plans/v5.14-foxml-port-and-maker/subplans/2026-05-08-v5.14.10-bayesian-thompson-bandit.md` (THE plan)
   - `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-10-v5.14.9-session-postmortem.md` (predecessor postmortem; full sprint log + 7 NEW DESIGN_SPECS + lessons)

3. Read BEFORE WRITING CODE (per CLAUDE.local.md required reading):
   - `DOCS/STRATEGY_AND_CODING_RULES.md` (11 strict invariants: no malloc, no virtual, no mutex, no sleep_for, no atof/strstr in parsers, cache alignment, branchless, AVX-512-friendly data layouts, SCHED_FIFO/isolcpus deployment)
   - `plans/_cross-cutting/2026-05-06-latency-path-discipline.md` (7 latency-path rules; ML inference IS on slow-path budget)
   - `DOCS/LATENCY_OPTIMIZATION_AUDIT.md` (Gemini sweep; bandit-relevant items in Parts 1, 4, 10)

## Step 1 — re-verify plan against current code (HEAD)

Plan was drafted 2026-05-08. Codebase moved through v5.14.0-.9 in the
intervening 2 days. Plan's "Pre-existing work audit" REUSE claims +
file:line refs may be stale.

ALSO: plan heading reads "v5.14.11" (stale). MASTER renumbered to
v5.14.10 on 2026-05-10. Fix the heading.

Run `/readiness plans/v5.14-foxml-port-and-maker/subplans/2026-05-08-v5.14.10-bayesian-thompson-bandit.md` and address every GAP / stale-reference finding. Common stale claims to verify:
- `ML_Headers/BanditLearning.hpp` exists + cited fns (`Bandit_Init`, `Bandit_Update`, `Bandit_GetProbabilities`) still exist + match signatures
- `EnsembleModelZoo.bandits[NUM_REGIMES]` field exists + still uses BanditState type
- `EnsembleModelZoo.exit_bandits[NUM_REGIMES]` (v5.13.4) exists; pattern to follow for `thompson_bandits[]`
- ML_BuildParameters dispatch site at `Strategies/StrategyParameters.hpp:~835` — line ref likely shifted post-v5.14.9.F.2 ML_CFG_FLAG migration (composite_enabled is now in `cfg.ml_cfg_flags` bitmap, not direct boolean)
- `cfg.bandit_algorithm` field — verify NOT already added by an earlier ship (was a v5.14.10/.11 spec; should be safe)
- `cfg.confidence_composite_enabled` migrated to `BITMAP_IS_SET(cfg.ml_cfg_flags, MASK_ML_CFG_CONFIDENCE_COMPOSITE_ENABLED)` post-v5.14.9.F.2 — Thompson dispatch shouldn't conflict but verify
- `Bandit_SaveJSON` + `Bandit_LoadJSON` still canonical persistence API
- `EnsembleModelZoo_SaveBanditState` + `LoadBanditState` (v5.10.0a.G.9) — function names + signatures match plan claims

## Step 2 — pre-coding audit gate (multi-skill parallel — QUALIFIES)

Per `DESIGN_SPECS/audit-methodologies/audit-driven-pre-coding-gate.md`, this ship FIRES the
gate: ≥5 new fields + new file + plan is 2 days old (cold-pickup risk).

Spawn these audits IN PARALLEL via Agent tool with Explore subagents:

1. `/parity-check` — focus: replay-determinism (Thompson seeded mt19937_64
   contract); thompson_state.json round-trip; cross-version load compatibility
2. `/trace-deps` — focus: plan file:line claims; ThompsonBandit fn signatures
   match BanditLearning patterns; dispatch site in StrategyParameters.hpp
3. `/readiness` (full 28-check pass) — cold-pickup completeness; new cfg
   field eligibility check (per cfg-flag-eligibility-criteria.md);
   Version.hpp bump discipline; mirror-fn audit (Bandit ↔ Thompson)
4. `/merge-scan` — focus: reuse opportunities between Exp3 + Thompson
   during cfg=2 "Both parallel" mode; shared RNG surface; calibration
   log column extension reuse
5. `/dod-audit` — focus: curve-registry-pattern application (3-mode
   switch); branchless dispatch vs switch; ThompsonBanditState cache layout

After all reports return, synthesize convergent findings to:
`plans/plan_checks/2026-05-XX-v5.14.10-fresh-audits-synthesis.md`

THEN consult Caramel before coding. Per CLAUDE.local.md `feedback_consult_on_audit_findings`: present findings + list potential fixes + iterate. Do NOT auto-proceed even if findings look clearly addressable. Smaller-scope recommendations have been wrong 3/3 times in v5.14 sprint vs Caramel's "do it right now" instinct.

## Step 3 — design check against pattern library

The plan was written BEFORE the v5.14.8 + v5.14.9 design specs landed.
Patterns matched by `/handoff` scan that the ship should integrate:

Required reading (DESIGN_SPECS catalog):
- `DESIGN_SPECS/README.md` (full 16-pattern catalog + "I need to..." quick discovery)

Pattern-specific reading + integration check:

- **`DESIGN_SPECS/framework-patterns/curve-registry-pattern.md`** (STRONG MATCH)
  Plan Step 3 dispatch is `switch (config->bandit_algorithm)` with 3 modes (Exp3 / Thompson / Both). Canonical case for FOREACH_BANDIT_ALGORITHM registry + fn-pointer dispatch. Same shape as FOREACH_DEGRADATION_CURVE (v5.14.9.A). Consider:
  - Define FOREACH_BANDIT_ALGORITHM in `ML_Headers/BanditAlgorithmRegistry.hpp` (new file)
  - Compute fns: `BanditAlgo_Exp3_Apply`, `BanditAlgo_Thompson_Apply`, `BanditAlgo_Both_Apply` (uniform dispatch contract)
  - fn-ptr dispatch table; bounds-checked wrapper
  - ToString / FromString auto-generated
  - Future 3rd algorithm (UCB?) = 1 row in registry

- **`DESIGN_SPECS/refactor-patterns/cfg-flag-eligibility-criteria.md`** (RELEVANT)
  `bandit_algorithm` is an INT enum (3 values), NOT a boolean. Fails criterion 5 (bitmap is for booleans only). Stays as direct cfg field. Document the rejection if anyone proposes migration to ML_CFG_FLAG bitmap. Same applies to `thompson_*_prior/precision/seed` (FPN/uint64 scalars; not bitmap candidates).

- **`DESIGN_SPECS/wire-format-patterns/wire-format-byte-preservation-discipline.md`** (RELEVANT)
  `thompson_state.json` is a new wire format. Plan says forward-compat-by-absence (same as v5.13.4.C exit_bandit). Discipline doc captures: deterministic serialization order, RNG state byte-equivalence across runs, version-stamp in the JSON header for future migration. Plan should add a `format_version` field at top of JSON.

- **`DESIGN_SPECS/audit-methodologies/audit-driven-pre-coding-gate.md`** (already invoked in Step 2)

Soft/partial matches (worth a glance):

- **`DESIGN_SPECS/framework-patterns/autopopulate-pattern-for-production-caller-class.md`** — Save/Load pair parallels existing pattern; today 2 algorithms = OK, document if 3rd is planned.
- **`DESIGN_SPECS/meta-disciplines/structural-fix-preferred-decision-framework.md`** — `ThompsonBanditState` is a PARALLEL ARRAY to `BanditState`. Acceptable today (2 algorithms, different storage shapes), but if 3+ ever land → Class 18 mirror. Note for postmortem.

Don't write code until pattern docs are read + integration plan articulated in the synthesis doc.

## Step 4 — design philosophy reminders (load-bearing rules)

From CLAUDE.local.md going-forward rules + auto-memory MEMORY.md:

- **Defer is last-ditch, never effort-avoidance.** Implement properly the first time. v5.14 sprint precedent: 3/3 smaller-scope recommendations were wrong vs Caramel's "do it right now" instinct.
- **Structural fix > direct patch for recurring bug classes.** If `BanditState` ↔ `ThompsonBanditState` mirror pattern accumulates a 3rd algorithm, retrofit FOREACH_BANDIT_ALGORITHM registry instead of triple-parallel-arrays. For today, 2 algorithms is the edge of acceptable parallel.
- **No MVP for plumbing/refactor work.** Reserved for genuinely-new features with unknown unknowns. Thompson math is well-understood; this ship is mostly plumbing. Ship the full design — registry-driven dispatch is the right shape, not an ad-hoc switch.
- **Boundary-stable refactors preferred over wide cascades.** ThompsonBanditState lives in NEW header; doesn't modify BanditState. Cascade-free.
- **Hot path UNTOUCHED.** Thompson runs on slow-path (ML_BuildParameters @ ~100µs budget). Don't add anything to BG_Evaluate / SG_Evaluate / ExecutionCore_Tick. p99 hot-path target ≤500ns.
- **Branchless mask compute > switch on enum on slow path** (CLAUDE.md item 18). If FOREACH_BANDIT_ALGORITHM lands, fn-ptr dispatch is correct (single indirect call ~1-2ns); plain switch on enum is also acceptable for 3 modes. Don't sprinkle algorithm-specific branches deep in ML_BuildParameters — hoist dispatch to the top.
- **Reuse-audit before adding new code** (CLAUDE.md item 16). Bandit_SaveJSON / LoadJSON utilities exist; Thompson should reuse them not duplicate. RNG state surface: check if any existing per-core RNG state could be reused.
- **Latency-additions get tracked** (CLAUDE.md item 17). New slow-path cost: ~80ns (Thompson 8 Gaussian draws via mt19937 + Box-Muller). Log in `DOCS/HOT_PATH_CHANGELOG.md` with cost estimate + branchless analysis. Note Box-Muller is NOT branchless (rejection sampling internally) — consider Ziggurat alternative or accept cost.
- **Replay determinism is sacred.** Thompson_Sample MUST use seeded mt19937_64 with `cfg.thompson_rng_seed` (default 42). NEVER `std::random_device` / `std::default_random_engine` in production paths. RNG state persisted in `thompson_state.json` for cross-run determinism.
- **Bump Version.hpp on every ship.** Each `vX.Y.Z` tag MUST include a Version.hpp bump in the same commit. From operator memory: caught a gap after 17 ships missed this. Sub-tags (.A/.B/.C/.D) each bump (5.14.9 → 5.14.10.A → .10.B etc.).
- **Don't use AskUserQuestion modal boxes.** Present options inline as text. Caramel wants full conversation scrollable.
- **Evaluate options on robustness + latency + design philosophy, NOT time.** Time is essentially never the deciding factor.
- **Address Caramel as Caramel / she / her** in conversation. Persisted docs may keep "operator" for general-audience framing.
- **Avoid substring `replace_all` on member-access patterns.** Two distinct mangling shapes (Shape A chained-prefix, Shape B variable-name-suffix) caught in v5.14.9.F. Use targeted Edit for member-access refactors.
- **Suggest mid-sprint audit when downstream sub-ships impact new surface.** Wait for greenlight; don't auto-trigger. Confirmed v5.14.9.F.2 HIGH-RISK pattern.

From auto-memory MEMORY.md (operator-validated):
- Prefer ripgrep over grep (`feedback_prefer_ripgrep`).
- Compaction degrades — treat handoff prompts as HINTS; re-verify against code (`feedback_compaction_degrades_treat_handoffs_as_hints`).
- Overengineering boundary: pick harder choice when future work much easier (`feedback_overengineering_boundary_when_future_easier`).
- Rename plans when ship order diverges from numbering (this plan's "v5.14.11" → "v5.14.10" rename qualifies).

## Step 5 — TECH_DEBT items in surface area (per Stage 4 of /handoff)

- **TECH_DEBT-018** (OPEN) — Codify `/precoding-audit` Layer 1 orchestrator skill. This ship's pre-coding gate would benefit; not blocking. `/handoff` (this skill) partially fills this gap by composing the gate as text. Decide: explicit defer (this ship doesn't need to build the skill) OR refresh the TECH_DEBT entry to reflect partial closure via `/handoff`.

- **TECH_DEBT-022** (OPEN) — Engine.cfg parser perfect-hash dispatch. Ship adds 5 new cfg fields (`bandit_algorithm`, `thompson_mu_prior`, `thompson_precision_prior`, `thompson_precision_obs`, `thompson_rng_seed`). Linear strcmp parser scales; 5 more entries marginal. Decide: explicit defer (no impact) OR absorb a small refactor.

- **TECH_DEBT-010** (OPEN) — FOREACH_CALIB_LOG_COL registry for calibration log CSV columns. Plan's cfg=2 dual-mode extends calibration log with Thompson choice + Exp3 weights columns. If extension is ad-hoc, this debt activates with the ship. **Recommend: absorb** the registry refactor into v5.14.10 IF the dual-mode telemetry is shipped in v5.14.10.B or .C. The pattern is well-defined; doing it ad-hoc reinforces the parallel-mirror class.

Run `/readiness` Check 25 (TECH_DEBT scan) at the synthesis stage to confirm decisions per item.

## Step 6 — operator collaboration norms

- Address Caramel as Caramel / she / her. Not "operator" in conversation.
- Don't use AskUserQuestion modal boxes. Present options inline as text.
- Evaluate options on robustness + latency + design philosophy, NOT time.
- When pre-coding checks complete, present findings + fixes + iterate BEFORE coding. Do NOT auto-proceed.
- Suggest mid-sprint audit when downstream sub-ships impact new surface (going-forward rule from v5.14.9). Wait for greenlight.
- Auto-write TECH_DEBT entries for any deferred items (CLAUDE.local.md auto-write contract).
- Auto-write PARITY_ISSUES entries for any new parity findings (CLAUDE.local.md auto-write contract).
- Avoid `replace_all` on member-access patterns (Shape A + B mangling lesson from v5.14.9.F).

## Step 7 — filesystem conventions

- Workspace path: `/home/caramel/code/tick-trader-percore-workspace`
- Engine repo: `/home/caramel/code/FoxML_Trader_v2`
- Plans symlinked: engine `plans/` → workspace `plans/` (DIR symlink; transparent to internal moves)
- Sprint dirs: `workspace/plans/<sprint-dir>/{MASTER.md, subplans/, plan_checks/, postmortems/, handoffs/}`
- This sprint: `workspace/plans/v5.14-foxml-port-and-maker/`
- DESIGN_SPECS catalog: `workspace/DESIGN_SPECS/` (16 patterns + README with "I need to..." quick discovery)
- Skill outputs go to `plans/plan_checks/<skill>-<YYYY-MM-DD>-<scope>.md` (neutral); batches into sprint dir at close
- TECH_DEBT auto-write: `DOCS/TECH_DEBT.md` (symlinked from workspace; engine-side reads through symlink)
- PARITY_ISSUES auto-write: `DOCS/PARITY_ISSUES.md` (symlinked from workspace)
- HOT_PATH_CHANGELOG: `DOCS/HOT_PATH_CHANGELOG.md` (symlinked from workspace)
- All 24 engine→workspace symlinks verified resolve post-reorg (verified 2026-05-10)

## Step 8 — sprint-close verification gate

Before declaring v5.14.10 shipped:
- Tests pass (currently 2796; expect ~12 new for Thompson per plan Step 6)
- `/parity-check` GREEN at v5.14.10 (focus: replay-determinism + thompson_state.json round-trip)
- `/merge-scan` GREEN (no missed reuse with Bandit_*; Thompson reuses Bandit_SaveJSON / LoadJSON utilities)
- `/latency-track` entry in `DOCS/HOT_PATH_CHANGELOG.md` for Thompson_Sample slow-path cost (~80ns expected)
- `/bug-check` CLEAN (no new recurring-bug-class instances)
- Replay determinism test passes: identical `cfg.thompson_rng_seed` + identical reward sequence → bytewise identical Thompson sample sequence across runs
- Cfg.example doc updated for `bandit_algorithm` enum (0=Exp3/default, 1=Thompson, 2=Both) + 4 Thompson hyperparams
- Persistence round-trip: save thompson_state.json → restart → load → bytewise identical posterior + RNG state
- If cfg=2 "Both parallel" mode shipped, calibration log columns documented + ship absorbs (or explicitly defers) TECH_DEBT-010 (FOREACH_CALIB_LOG_COL registry)
- Version.hpp bumped to 5.14.10 in the same commit as the umbrella tag

## Closing reminder

This ship's value is the BANDIT ALGORITHM choice: Thompson's posterior sampling (natural exploration; better for non-stationary rewards) vs Exp3's adversarial regret bounds (faster convergence; stationary). Crypto markets are non-stationary → Thompson may outperform. The plumbing (cfg, dispatch, persistence) should be the smallest part of the diff.

If you find yourself writing complex plumbing, stop — check `DESIGN_SPECS/README.md` "I need to..." section. The curve-registry-pattern is the strong fit for the 3-mode dispatch.

Replay-determinism is sacred. Box-Muller's rejection sampling is internal to mt19937_64; if you find yourself doing your OWN rejection loop on top, you've drifted off-pattern. Use `std::normal_distribution` with mt19937_64.

Good luck. Caramel will iterate with you on the audit findings before coding.
```

---

## Notes for future-Claude reading this handoff doc

- Prompt above is self-contained — paste it as FIRST message in a fresh `claude code` session to start v5.14.10.
- Follow the 9-step workflow in order. Don't skip Step 1 (re-verification against current code) — that's where stale-plan-claim issues surface (e.g., the "v5.14.11" heading + the post-v5.14.9.F.2 cfg flag bitmap migrations).
- Plan file is at `subplans/2026-05-08-v5.14.10-bayesian-thompson-bandit.md` (note the stale "v5.14.11" heading inside — fix during Step 1).
- This sprint dir has 4 postmortems from prior sub-ships; the v5.14.9 postmortem captured ALL late-sprint pivots + DESIGN_SPECS deliverables + lessons. Worth a full read for context.

---

## Quick links

- Sprint MASTER: `plans/v5.14-foxml-port-and-maker/MASTER.md`
- v5.14.10 plan: `plans/v5.14-foxml-port-and-maker/subplans/2026-05-08-v5.14.10-bayesian-thompson-bandit.md`
- Latest postmortem: `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-10-v5.14.9-session-postmortem.md`
- DESIGN_SPECS catalog: `DESIGN_SPECS/README.md` (16 patterns)
- Curve registry pattern (strong match): `DESIGN_SPECS/framework-patterns/curve-registry-pattern.md`
- Cfg-flag eligibility (relevant): `DESIGN_SPECS/refactor-patterns/cfg-flag-eligibility-criteria.md`
- Wire-format byte preservation (relevant): `DESIGN_SPECS/wire-format-patterns/wire-format-byte-preservation-discipline.md`
- Audit-driven pre-coding gate: `DESIGN_SPECS/audit-methodologies/audit-driven-pre-coding-gate.md`
- Latency rules: `plans/_cross-cutting/2026-05-06-latency-path-discipline.md`
- Coding invariants: `DOCS/STRATEGY_AND_CODING_RULES.md`

---

## /handoff skill closing summary

- **Patterns matched (strong):** curve-registry, cfg-flag-eligibility, wire-format-byte-preservation, audit-driven-pre-coding-gate
- **Patterns matched (soft):** autopopulate-arity-family, structural-fix-preferred
- **TECH_DEBT overlapping:** -018 (precoding-audit skill — partial closure via /handoff itself), -022 (parser perfect-hash; marginal), -010 (calib-log registry; absorb-or-defer decision)
- **Audit gate fires:** YES — ship qualifies (≥5 new fields + new file + 2-day-old plan)
- **Output:** this file, at `plans/v5.14-foxml-port-and-maker/handoffs/2026-05-10-v5.14.10-thompson-bandit-handoff.md`
- **Generator skill version:** /handoff v0.1 (first invocation)
- **Operator action:** review prompt; edit if any of the 9 steps need customization; paste fenced block into fresh `claude code` session.
