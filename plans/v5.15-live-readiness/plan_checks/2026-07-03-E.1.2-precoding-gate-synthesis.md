---
type: audit-synthesis
gate: /precoding-audit-gate (HIGH-RISK, 5-agent + completeness-critic + a-class verification)
audit_set: parity-check, dod-audit, trace-deps, accounting-audit, blindspot-scan(B14), completeness-critic, a-class-verify
target: subplans/2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md (status:reformalized-coding-plan v1.0)
engine_head: b10e778
date: 2026-07-03
combined_verdict: RED  # bounded-but-substantial amendment set before Phase B; DIRECTION sound, not a re-design
orchestrator: Opus 4.8 (fully-armed pickup session; gate fired here per operator, Fable-difference judged marginal)
---

# E.1.2 pre-coding gate — synthesis

**Combined verdict: RED** — the plan needs a bounded amendment set before Phase B. Every agent affirms the **direction** (freeze the struct, registry-drive the serializers, delete the dead serializers, Money-ize F-096); the RED is about the *mechanics + guards*, not the shape. Est. amendment effort ≈ half a focused session; no re-design.

## Per-audit verdict table
| Audit | Verdict | Headline |
|---|---|---|
| parity-check | **RED** | serializer drops ~24 flat node scalars (9 Money) → own D-110 criterion FAILS; PORTFOLIO version retire-not-bump |
| blindspot-scan | **RED** | `Position_Reset` stale-reserved-bytes (fwd) + no size→version guard — both golden-master-blind |
| dod-audit | **YELLOW→RED** | E.1.2 is AoS struct-grow, not SoA → H10 acceptance/guard vacuous (Class-51) |
| accounting-audit | **YELLOW** | F-096 fix ⅓-scoped (exit+non-partial still `double`); conservation assert vacuous (Class-51); partner_pending_pnl bitmap co-persist |
| trace-deps | **YELLOW (safe)** | deletion provably landable; wrong deletion cites + reverse orphan cascade + test cohort |
| completeness-critic | **YELLOW** | GAP-2 union homeless; D-110 test byte-blind; stale sizeof claim |
| a-class-verify | T1 CONFIRMED / T2 CONFIRMED / T3 REFINED | parity right on the serializer; struct-grow confirmed; Position_Reset not-a-live-bug |

## Disagreement resolved (AR-11 code-read)
parity-B2 ("serializer drops flat scalars") vs dod-F4 ("compose-sub-registries is correct"). a-class T1 read the actual save loop (`ShardedSnapshotPersist.hpp:172-261`) + D-283/D-287: **parity is right; dod is a false dichotomy.** The nested block needs delegates (true) AND the ~24 flat scalars need the flat registry D-283 mandated. Answer = **HYBRID compose(flat `FOREACH_NODE_PERSIST_FIELD` + regime_state + pnl_feeder + confidence)**.

---

## BLOCKING — must resolve before Phase B

**BLK-1 [CRIT·design] — Phase-C serializer must be the HYBRID, not delegates-only.** (parity-B2, a-class-T1 CONFIRMED.) As worded it drops ~24 flat scalars incl. 9 `Money` (`allocated_balance` :187, `node_realized`/`node_fees`/`node_open_notional` :192-194, `node_gross_wins`/`losses` :202-203, `last_entry_price` :207, `node_peak_balance`/`node_dd_pct` :212-213) + 3 doubles (:244-246) → fails its own acceptance criterion (`plan:52`) + violates **D-283 item(2)**. D-287 refuted only the `SERIALIZER_GENERATED` flag. **Fix:** build `FOREACH_NODE_PERSIST_FIELD` (flat DIRECT/BIT, mirroring `FOREACH_OMS_FIELD`) composed WITH the 3 delegates. → **rewrite Phase C.**

**BLK-2 [HIGH·design] — retire `PORTFOLIO_SNAPSHOT_VERSION`, don't bump 7→8 (H21).** (parity-B1 + trace-deps-4 + blindspot converge.) Phase D deletes its sole consumer (`Portfolio_Save/Load`); a v8 bump = a phantom version no emitter writes — the exact H21 hazard the ship is *about*; also internally asymmetric vs CONTROLLER=DELETE. The live snapshot gates on `SHARDED_SNAPSHOT_VERSION`. → **amend Phase D/G: retire symmetric with CONTROLLER; ledger `:55` + SOURCES `:59` (not the plan's cite).**

**BLK-3 [HIGH·structural] — add a size→version guard; tie it to SHARDED, not PORTFOLIO (TD-180 subsumed).** (blindspot-2, reconciled w/ BLK-2.) Both epoch tripwires are TYPE-keyed (`is_fp_decimal_v`, `MONEY_ENCODING_EPOCH`); a SIZE change 128→192 trips neither, and the golden-master is insensitive to a forgotten bump. **Fix:** `static_assert(sizeof(Position<64>)!=192 || SHARDED_SNAPSHOT_VERSION>=11)`. Closes TD-180 here.

**BLK-4 [HIGH·design] — strike the SoA/H10 acceptance criterion + §4a guard-row (vacuous, Class-51); fix the determinism gate.** (dod-F1 + a-class-T2 CONFIRMED.) E.1.2 is an AoS struct-grow (no AVX kernel over Position; `±USE_NATIVE_128` is **inert** — absorbed, flips a manifest string not the codepath). **Fix:** re-home D-55 SoA + H10-first-consumer to **.E.6/.E.7**; replace the gate with a plain Save→Load byte-compare over a POPULATED multi-node snapshot (parity-MED-3) + a reopen-same-slot fixture (T3).

**BLK-5 [HIGH·design] — rescope the F-096 fix to the whole `order_qty_d` variable.** (accounting-HIGH-1 + parity-MED-2 converge.) The plan fixes only the entry split (`Async.hpp:842`); the exit branch (`:829-830`) + non-partial (`:851`) still round-trip money through `double`, and the acceptance anchored to ":842" goes green while they leak (8dp perturbable). **Fix:** whole var → `Money order_qty` (exit = direct `positions[slot].quantity`; legs = `Money_Mul`/`Money_Sub`; update `:858` `>0.0` guard + `:896` ctor). Extract a shared `Money_LegSplitQty` (canonical sister `PortfolioController.hpp:1344`).

**BLK-6 [HIGH·design] — replace the vacuous conservation assert (Class-51).** (accounting-MED-1.) `legB:=Money_Sub(intended,legA)` makes `legA+legB==intended` an exact identity, and the legs drain in separate iterations (never one scope). **Fix:** by-construction documented guarantee OR a real per-node cross-iteration check — not the naive same-scope assert.

## REQUIRED (HIGH) — resolve before coding, not strictly blocking-the-direction

**AM-1 [HIGH·design] — disposition the GAP-2 strategy-state union (task #7).** (completeness-F-1.) Frozen-item #7 re-homed the union to E.1.2, but Phase F is silent, D-274 says "E.1.3+", and none of E.1.3–E.1.6 hold it. **Decide at the freeze:** inline union (re-touch the FINAL struct) vs keep `void*` + kind-guard (zero reserve). Record it either way.

**AM-2 [HIGH·structural] — upgrade the D-110 recovery test to assert RE-DERIVED state.** (completeness-E-1 + parity-MED-3 + T3.) The byte-compare is blind to a wrong re-derive (`live_tp/live_sl` recomputed on load, never serialized) AND to stale reserved bytes. `tests/` has **zero** Save/Load coverage today. Test must assert re-activated in-memory ExecutionCore state + reopen-same-slot + populated multi-node.

**AM-3 [HIGH·structural] — registry-drive `Position_Reset`; route OpenSlot/Add through it.** (blindspot-1 REFINED by T3.) Not a live bug (peak/owner_node_id don't exist yet) but Phase-B's "zero-init every new field at every site" IS the A28/TD-182 subset-zeroing hand-list the Reset SSoT (`Portfolio.hpp:216`) claims to have closed. Generate `Position_Reset` from `FOREACH_POSITION_FIELD` (the `init` column) + route `Portfolio_OpenSlot`/`AddPositionWithExits` through it.

**AM-4 [HIGH·design] — partner_pending_pnl: persist BOTH the Money field AND its bitmap, or neither.** (accounting-HIGH-2/MED-2, TD-227.) Persisting the pnl alone corrupts W/L on restart (`partner_pending_bitmap` not persisted → mis-pairs next exit). Genuinely lost otherwise; ~0 marginal cost with the serializer rebuild → persist **here**, atomically with its bit (folds into BLK-1's Phase C).

## MED — address-as-found

- **MED-1 [structural] reverse orphan cascade** — deleting `PortfolioController_LoadSnapshot` orphans the `ConfidenceScorerLegacyV1` shadow-load cluster (`ConfidenceScore.hpp:1082-1122` + `LEGACY_CONFIDENCE_VERSION`) → 0 callers; the code's own comment says delete-then. **Free TECH_DEBT-002 closure** → sweep Phase D or home. (trace-1 + parity-rider + blindspot-4)
- **MED-2 [mechanical] test cohort** — pin all 3 compile-break sites (`controller_test.cpp:11394`, `:26749-26757` compile-time `static_assert`, `:26819`), not just `:26819`; CONTROLLER must be EXCISED not bumped. (trace-3 + parity-HIGH-3 + blindspot-4)
- **MED-3 [structural] the `--composition`=0 claim is a TOOL ARTIFACT (D-202 defect)** — `gen_code_map --composition` is blind to `Type<F> arr[N]` array-member edges; `Portfolio<F>` holds `Position<F> positions[16]`, contained by OMS/PortfolioController/OrderEventLog. Conclusion (contained) still holds by AUDIT (no container whole-serialized/exact-size-pinned; OMS by pointer) but not by the cited tool. Re-justify in-plan by audit + **file a tool-hardening TECH_DEBT**. (blindspot-3)
- **MED-4 [design] `PORTFOLIO_SNAPSHOT_MAGIC` coherence** after Save/Load delete (0 code consumers) — retain-as-epoch-marker vs retire; decide with BLK-2. (trace-4)
- **MED-5 [mechanical] Phase-B layout hygiene** — tally the exact ~48B reserved sub-layout before finalizing offsetof asserts; update `check_struct_size_budget.py` manifest (128→192 / 2112→3136, D-229); add `owner_node_id=(cluster<<8)|node` cap-reconciliation `static_assert` vs `MAX_EXECUTION_NODES` (H22). (completeness-F-2 + dod-F6/F7)

## Currency-cite sweep (bundle — a literal coding-follow trips on these)
- Deletion cites "55/57" & "57-59" are **wrong** (55=PORTFOLIO/bump; 57/59=SHARDED/PORTFOLIO/bump; only CONTROLLER 57-ledger/58-SOURCES retire). (trace-2 + blindspot-4)
- Phase-F "ADD the missing `sizeof` static_assert" is **stale** — it EXISTS (`ControllerConfig.hpp:1323`, shipped E.1.1); `cfg_load_fault_flags` already in-struct (`:582`). Only the per-`Fingerprint_Compute` `fault_flags==0` assert is open. Do NOT re-add the pin. (completeness-E-2 + parity-MED-1)
- `Fingerprint.hpp:180` **wrong** → def `Fingerprint.hpp:174`, call site `BacktestPanels.hpp:3157`. (parity-MED-1)
- The old `.E.1-foundation.md:432` sketch still says `FPN<F>` + phantom `MAX_CORES` — copy-verbatim trap; the plan's own text is right (Money). (dod-F5)
- 30/60Hz GUI-publish comment drift (`ControllerEventLoop.hpp:195,630` vs 7×60Hz). (completeness-E-3)

## LOW / document
- `--update` silently drops CONTROLLER=14 (no tombstone) — DOCUMENT as an intentional H21 exception in the decision log. (blindspot-5)
- `peak`'s warm-restart consumer is E.1.3-deferred — state it (the peak persist test is byte-only at E.1.2). (completeness-F-3)

## VERIFIED-SOUND (the plan got these right — do not re-litigate)
- `node_dd_pct` recompute-on-load (drop the raw persist) ✓ · `node_peak_balance` stays persisted ✓ · GUI per_node doubles display-only/H4-exempt ✓ · `Money_Mul`/`Sub` semantics (partial_exit_pct is a fraction) ✓ · the compose-sub-registries mechanic for the NESTED parts ✓ (add the flat registry for scalars) · dead-serializer deletion provably safe (all 4 dead, TICK-magic preserved + test-covered, confidence leg survives) ✓ · slow_account Money-not-FPN discipline caught ✓ · the reserve set (intended_* seqlock, fill-record owner, hot.mode, 5 A-3 Money fields, per_cluster[], peak+owner_node_id) present ✓ · AggregatorState correctly NOT an E.1.2 reserve ✓.

## Systemic themes (Stage-4 anti-pattern verdict)
1. **Two Class-51 "vacuous guard" instances** — the conservation assert (BLK-6) + the H10 guard-row (BLK-4). The plan proposed guards that don't guard. Recurrence note for `class-51-vacuously-green-guard.md`.
2. **Two "the golden-master is blind to this" capital risks** — stale reserved bytes (AM-3) + a forgotten version bump (BLK-3). The acceptance criterion (byte-compare) is *necessary but not sufficient*; the gate spec itself was under-designed (compounded by BLK-4's inert `±USE_NATIVE_128`).
3. **Reformalize left ~5 semantically-consequential stale cites** (currency sweep) — the re-ground fixed headlines, not all cites.

## Recommended amendment sequence (before Phase B)
1. **BLK-1 + AM-4** — rewrite Phase C as the hybrid serializer (+ partner_pending_pnl+bitmap). *The biggest; everything else is smaller.*
2. **BLK-2 + BLK-3 + MED-4** — PORTFOLIO retire + SHARDED-keyed size→version guard.
3. **BLK-4** — strike SoA/H10, re-home to .E.6/.E.7, fix the determinism gate to Save→Load-byte-compare over a populated multi-node snapshot.
4. **BLK-5 + BLK-6** — whole-var Money F-096 + real conservation guarantee.
5. **AM-1** — GAP-2 union disposition (inline vs pointer+kind-guard).
6. **AM-2 + AM-3** — the D-110 test upgrade + registry-driven Position_Reset.
7. MED-1/2/3/5 + the currency-cite sweep + the LOW documents.

**M7 escalation check:** none (plan-quality findings, not a codified-rule-violated-3× surface). Candidate soft signal: a "reformalize cite-currency checklist" — noted, not codified.

**Consult-before-coding:** operator (Caramel) triages. No auto-proceed.
