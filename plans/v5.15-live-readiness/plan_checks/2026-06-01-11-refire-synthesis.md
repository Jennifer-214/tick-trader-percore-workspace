---
type: audit-synthesis
audited_plan: subplans/2026-05-30-v5.15.5.F.4d.1.E-money-numeric-core-foundation.md (v0.2, step-6-folded)
ship: "#11 numeric-foundation unification — RE-FIRE (step-7, post-step-6-fold)"
date: 2026-06-01
gate: /precoding-audit-gate RE-FIRE (HIGH-RISK, heavier-default D-77) — 7 audits + standing completeness-critic + 16B-layout quorum
predecessor_synthesis: plan_checks/2026-05-31-money-numeric-core-foundation-fresh-audits-synthesis.md (original, pre-D-125)
engine_head: e9244de
combined_verdict: YELLOW (decision + proof + hoist + 16B-no-padding all independently RE-VERIFIED sound; substantial pre-coding ENUMERATION-COMPLETENESS amendments required — no RED, no architecture-killer)
---

# #11 RE-FIRE synthesis (step-7) — the amended plan, with the 16B surface audited (D-135)

**Combined verdict: YELLOW.** The 16B-two's-complement decision, the proven `divmul_pow10`, the C1 FP64-body HOIST (value-equivalence by construction), and the 16B-no-padding layout were each **independently re-verified sound** (merge read `FP64_Mul`/`FPN_Mul` directly; hft confirmed the 3 hot muls are `__builtin_expect`-cold; the quorum agent re-checked the proof structure + the UOR/trivially-copyable properties by compile-probe). **No agent found an architecture-killer.** But the re-fire surfaced a **convergent enumeration-completeness gap** — the step-6 fold was correct at the DESIGN level and under-counted at the SITE-ENUMERATION level on three surfaces (16B layout-asserts, fee/emit sites, registry-boundary propagation), plus one important nuance the fold left implicit (the 16B compaction is deliberately LOSSY in the top magnitude bit). This is the re-fire doing its job on D-135 (the 16B surface the original gate never saw).

## Per-audit verdict
| Audit | Verdict | Headline |
|---|---|---|
| parity | YELLOW | the `is_FPN_v` wire dispatchers don't discriminate radix → decimal money would emit via `FPN_ToDouble→%.17g` (the lossy path this ship kills); prod drift path is double-StampT vs FPN-CfgT |
| trace | YELLOW | price-domain stats → gate-comparison crossing (`RollingStats.price_avg`→`bg_price_threshold` vs money `tick.price`) enumerated at symptom, not semantic — largest silent-cast cluster |
| merge | GREEN¹ | C1 hoist value-equivalence SOUND on independent read of `FP64_Mul`; divmul reuse is REAL; one MED (−2⁶³ abs edge under-pinned, routed to step-7) |
| dod | YELLOW | 16B-no-padding layout SOUND (UOR holds), but the H12/layout-assert relocation set is ~3× UNDER-ENUMERATED (Position wire-layout + Order + ExecutionCore:176 + FlowFeatures + the `==24` test) |
| accounting | YELLOW | **C3 CONFIRMED: LIVE COMPUTES fees, never books exchange-reported** (`result.commission` reaches no P&L site); fee-twin set is ≥7 not 3 |
| hft | GREEN | "hot path UNTOUCHED" is now PROOF-BACKED-TRUE; 16B is a strict cache WIN (`:176` assert holds at 40≤64); LOW-only (pad re-tune at code) |
| registry-fit | YELLOW | H4 disposition right in the TABLE but §Venue-SSoT prose + acceptance line 260 still MANDATE the premature `FOREACH_EXCHANGE` (contradiction); Check-29 section MISSING (2 ship-blockers per plan's own discipline) |
| completeness-critic | YELLOW | the hot-path cache-line/offset `static_assert` ladder (Order.hpp, ExecutionCore:176, Position offsets) is unenumerated — the "byte-context" framing structurally excludes it |
| 16B quorum | YELLOW | **16B is LOSSY: 128-bit magnitude → 127-bit** (top integer bit dropped). D-126 KNOWS + resolves via feature-saturation; the net certifies values-that-occur (correct bar) but acceptance should STATE the 2⁶³ ceiling explicitly |

¹ merge GREEN-with-one-MED.

## Findings — severity·kind + disposition (every row dispositioned per `feedback_address_med_low_findings_not_just_high_crit`)

### Ship-A blockers (the 16B binary compaction — must resolve before Ship A code)

- **R1 [HIGH·structural·wide] — the H12/layout-assert relocation set is ~3× under-enumerated.** [dod F1 + completeness B-1 + parity F5; convergent] The plan names ~6 sites; the breaking `sizeof(FPN)==24`/`offsetof`-multiple-of-24/disk-entry-size asserts actually number ~18, including: `Portfolio.hpp` Position offset ladder (9 `offsetof` + `POSITION_PERSIST_BYTES==184` + `sizeof(Position<64>)==192`; **Position is WIRE** — snapshot writes 16×) · `Order.hpp:148/150` (`OrderPreResolved==48`, `Order<64>==320`; **Order.hpp named nowhere**) · `ExecutionCore.hpp:176` (hot cache-line assert) · `FlowFeatures.hpp` cluster offsets · `OrderEventLog.hpp:425/465/508` (disk entry-size) · `tests/controller_test.cpp:24429-24432` (`static_assert(sizeof(FPN<64>)==24)` — a DIRECT compile break) + `:26073` memcmp · `tools/fp_determinism_golden.cpp` + `check_fp_determinism.sh` (the re-cert vehicle). **Disposition: fix-in-plan (Ship A)** — replace the 6-item set with the verbatim `gen_code_map --byte-context FPN` output + a `static_assert(sizeof|offsetof…FPN)` sweep (widen Phase-0a scan beyond `--byte-context` to `--composition` + the assert grep); add acceptance criterion "every FPN-spaced layout/size assert re-derived to 16B; the gen_code_map byte-context set re-certified." Sister: `enumerate_set_before_categorical_claim`.
- **R2 [HIGH·design] — the 16B compaction is deliberately LOSSY (128-bit magnitude → 127-bit); value-equivalence is BOUNDED, not universal.** [16B quorum; verified] Current `FPN<64>` = 128-bit magnitude (`uint64_t w[2]`) + sign + padding (24B struct); 16B `__int128` two's-comp = 127-bit magnitude (63 integer bits vs 64). A value with integer part in [2⁶³, 2⁶⁴) has no 16B image. **D-126 KNOWS this** and resolves it (post-#11 binary holds ONLY features; no feature reaches 2⁶³; worst real ~60 bits @ $1M with 3-bit margin; ceiling-saturation = correct decision input). The value-equivalence net certifies the values that OCCUR in the golden (the right bar). **Disposition: fix-in-plan (Ship A)** — amend the value-equivalence-net description + Ship-A acceptance to state explicitly: "value-equivalence holds for |integer part| < 2⁶³; [2⁶³,2⁶⁴) is intentionally dropped, guarded by feature-only-domain + saturation per D-126." Record the bound at the gate, not just in D-126 reasoning. (Decision stands — this is precision, not redesign.) **VERIFIED + SHARPENED 2026-06-01 (operator-prompted deep-check):** window `W=128` is fixed (only `RollingStats<64,128>` instantiated) → real headroom ~17 bits @ BTC / ~9 @ $1M (ceiling ≈ $30M price), better than the quorum agent's estimate (it assumed ~1000). AND the failure mode is graceful SATURATION-to-max, NOT wrap — `FPN_Mul` (`FixedPointN.hpp:612-622`) + `FP64_Mul` (`FixedPoint64.hpp:153-156`) both branchlessly saturate with IDENTICAL semantics (so the C1 hoist preserves it; no native-vs-generic overflow discrepancy). Deliverable sharpened: **the 16B mul MUST preserve the `of_mask` saturate-on-overflow** (the load-bearing property) + a max-magnitude probe test + record the 63-bit bound. FOLDED into the plan body's value-equiv-net + Ship-A acceptance.
- **R3 [MED·mechanical] — snapshot VERSION constants stale/unnamed.** [dod F2 + completeness] Plan says `CONTROLLER_SNAPSHOT_VERSION=11`; actual = **12** (`PortfolioController.hpp:2065`). `SHARDED_SNAPSHOT_VERSION=8` + `PORTFOLIO_SNAPSHOT_VERSION=5` unmentioned. All bump at the epoch (D-100 reject-old). **Disposition: fix-in-plan (Ship A)** — name all three + the bump.
- **R4 [LOW·mechanical] — ExecutionCore `_pad_hot`/`_pad_line0` need 16B re-derivation; stale "24B" comments.** [hft F-L1 + completeness B-4] The `:176/178` asserts are the guard (hold at 40≤64); the hand-sized pads must be re-tuned at code so line-0 stays single-load. Stale "FPN<64>=24B" comments (CfgFieldRegistry, Portfolio:82, Order:137) become false. **Disposition: fix-at-code (Ship A) + comment sweep.**

### Ship-B blockers (the decimal money core — must resolve before Ship B code, NOT before Ship A)

- **B1 [HIGH·structural] — C3 CONFIRMED: LIVE computes fees, never books the exchange-reported commission.** [accounting; traced end-to-end] `result.commission` (the venue `n`/`N`) is written at `BinanceUserData.hpp:378`, consumed ONLY at `Reconcile.hpp:546` — it reaches NO P&L site. `handle_buy/sell_fill` (`OrderManager.hpp:1142-1144/1187-1190`) compute `FPN_Mul(notional, fee_rate)`. So LIVE == paper/backtest (computed), a D-106 violation + paper↔live drift, present today. **Disposition: fix-in-ship (Ship B)** — upgrade the plan's conditional "verify… if computing, fix" to an UNCONDITIONAL fix: carry `result.commission` onto the in-flight Order (decision-time-data-binding) + book it at fill for LIVE; paper/backtest round-UP.
- **B2 [HIGH·structural·wide] — fee-twin / shared-rounding-helper enumeration undercount (≥7, not 3).** [accounting; convergent w/ trace] The replay==production differential + the "route all fee sites through #4's helper" guarantee must cover: `OrderManager:1144/1189` · `ControllerEventLoop:863/877` (replay) + `:1919/1963` (production) · `Portfolio:201` (kill-switch) · `OrderEventLog:656/675` (2nd replay path). The plan names "3 + ExitBuffer." **Disposition: fix-in-plan (Ship B)** — expand the fee-site set + the differential gate to the full ≥7; per `enumerate_set_before_categorical_claim`.
- **B3 [HIGH·structural] — the `is_FPN_v` wire dispatchers don't discriminate radix.** [parity F1+F2] `CfgFieldDispatch.hpp:348/423/466` all branch on `is_FPN_v<T>` → `FPN_ToDouble→%.17g`. If money becomes `<10,8>` and `is_FPN_v` matches it, decimal money re-enters the lossy double-emit path **on the HMAC wire body** — the exact thing this ship kills. The production drift path is double-StampT vs FPN-CfgT (`:466`), so decimal that doesn't round-trip through double false-drifts. The fold named the emit INTENT (H3) but not the dispatcher radix-fork that delivers it. **Disposition: fix-in-plan (Ship B)** — add an explicit "dispatcher radix-fork" item (`if constexpr (radix==10)` → exact-decimal-string) across all 3 dispatchers + the drift-compare; sister to #5 FromString-inverse.
- **B4 [HIGH·structural·wide] — the price-domain-statistics → gate-comparison crossing is the largest unenumerated silent-cast cluster.** [trace] `RollingStats.price_avg/min/max/vwap` (binary feature struct) → `entry_price` → `bg_price_threshold`/TP/SL (StrategyParameters egress, listed) → **compared against money `tick.price` in `BG_Evaluate`/`SG_Evaluate`** (`GateParameters.hpp:171/198`, NOT listed). O-1 makes this a compile-error (good backstop), but the plan's `enumerate_set` claim isn't satisfied. **Disposition: fix-in-plan (Ship B)** — add a "price-domain decision" row: price-stats stay binary + thresholds cast to money at gate-build, OR `tick.price` casts to binary at compare. Pick one; it governs `RollingStats<F>`/`GateParameters<F>`.
- **B5 [HIGH·mechanical] — registry-fit: 2 body contradictions + missing Check-29 section (ship-blockers per plan discipline).** [registry-fit F1/F2/F3] The H4 disposition (defer `FOREACH_EXCHANGE` to `.E.1`; #11 = compile-time `10⁸` const + `static_assert` + `FPN_Quantize` off `SymbolFilters`) landed only in the findings TABLE. §Venue-SSoT prose (line ~150) still names `FOREACH_EXCHANGE` as the venue-semantics home with no `.E.1` scoping; acceptance line ~260 still gates #11 close on "`FOREACH_EXCHANGE` Binance row carries {…}" — #11 can't pass its own acceptance without building the premature registry. The "Canonical sister registries considered" section (Check 29) is absent. **Disposition: fix-in-plan (now)** — rewrite §Venue-SSoT + acceptance to the const+guard+SymbolFilters framing; add the Check-29 section; declare the precision SSoT (M5: `SymbolFilters.qty_decimals` is the #11 source; `.E.1`'s column supersedes).
- **B6 [MED·structural] — C4 fill-result boundary disposition "named, not made"** + **`Reconcile.hpp:544-546` double-parses LIVE fill price/qty/commission** (a live-path `FromDouble` re-introduction not in the blast radius). [trace + accounting] **Disposition: fix-in-plan (Ship B)** — resolve C4 to guarded exact-`FromString` on the raw venue JSON strings (the double is itself a precision-discard); add the Reconcile parse to the #5 cohort.

### MED / LOW (dispositioned)
- **M-a [MED]** `BinanceOrderAPI.hpp` existing all-`double` qty rounding + `%.*f` wire-string (`:514/559`) is the site #6's "exact qty wire-string" SUPERSEDES — scope it in, not just the MIN_NOTIONAL gap. [completeness B-5] **fix-in-plan (Ship B).**
- **M-b [MED]** `GUI/TradeReader.hpp` RE-COMPUTES PnL in double from the trade log (not just display) → if the log emit goes decimal, its parse must follow; not pure display-exempt. [completeness B-6] **document + Ship-B note.**
- **M-c [MED]** ema_price has TWO implementations (`Async.hpp` producer + `PortfolioController.hpp` slow path) — both must take the identical money→binary cast (train-serve M5 byte-identity). [trace] **fix-in-plan (Ship B).**
- **L-a [LOW]** −2⁶³ abs guard is load-bearing (two's-comp `abs(INT_MIN)` overflows) + named (D-126); must land + be pinned against `FP64_Mul`'s exact body at code (the C1 mirror-drift guard). [merge + quorum] **fix-at-code; step-7/blindspot covers.**
- **L-b [LOW]** `Async.hpp:854` is DRAINER cadence (≤10μs), not producer — inventory mislabel; feature mul, disposition unchanged. [hft F-H2a] **document.**
- **L-c [LOW]** tag the overflow guard `__builtin_expect(…,0)` so in-range is the fall-through (H20-sanctioned cold-branch). [hft F-H7a] **fix-at-code.**

## Anti-pattern verdict
When amended, #11 still closes Class-21 (parallel-type, via unify — merge confirmed ONE multiply with a reduce-fork, no second body), the silent-domain-cast class (D-103 via O-1 — but the enumeration must be COMPLETED per B4), the absent-rounding class (D-105/D-128), and the internalized-external-authority class (D-106 — but B1 shows LIVE currently violates it). The re-fire itself is an instance of `enumerate_set_before_categorical_claim` applied to the audit layer: the fold's design was right; its SITE SETS (layout asserts, fee twins, dispatchers, price-domain) were each a categorical claim over an under-enumerated set.

## M7 escalation check
None codified-rule-recurrent. BUT a pattern worth noting: the step-6 fold correctly captured every DESIGN disposition yet under-enumerated SITE SETS three times (R1, B2, B4) — the same `enumerate_set` shape thrice in one fold. If this recurs at the next fold, it's an M7 candidate (a "fold must run the enumeration tool, not summarize it" structural rule). For now: a process note — **the relocation/fee/boundary sets should be the gen_code_map output verbatim, not a hand-summary.**

## Recommended path forward (operator decides — gate never auto-proceeds)
1. **Ship-A amendments first** (R1 + R2 + R3): these BLOCK Ship A (the immediate next code). R1 = paste the full tool enumeration into the relocation set + acceptance; R2 = state the 2⁶³ lossiness bound; R3 = name the 3 version bumps. ~all plan-precision edits.
2. **Ship-B amendments** (B1–B6 + M/L): these block Ship B, NOT Ship A — can land while Ship A codes. B5 (registry contradictions + Check-29) is a body-fix worth doing now since it's a live contradiction.
3. **THEN** `/blindspot-scan` on the amended body (the implementation-detail layer — the −2⁶³ abs, the dispatcher fork, the pad re-derivation are exactly its surfaces), then code Ship A.

**The decision, the proof, and the layout are sound — verified three ways. The work is completing the three under-enumerated site sets + stating the lossiness bound, so the implementer inherits the full break-radius, not a summary of it.**
