---
type: audit-report
audit: /bug-check
scope: pre-amendment-gate (v1.7.4 LANDED + v1.7.5 PLANNED scope)
plan_target: plans/v5.15-live-readiness/subplans/2026-05-24-v5.15.5.F.4d.1.B.4-train-serve-execution-layer-parity.md
ship_tag: v5.15.5.F.4d.1.B.4
date: 2026-05-26
engine_head: e0acb65
focus_classes: [14, 18, 21, 23, 24, 27, 28]
audit_tier: HIGH-RISK
---

# /bug-check report — `.B.4` v1.7.5 PRE-AMENDMENT-GATE — 2026-05-26

Pre-amendment audit verifying v1.7.5 PLANNED scope (per decision-log D15-D18 + C25-C30) against Classes 14/18/21/23/24/27/28 detection signatures. v1.7.4 plan body is CURRENT spec at workspace; v1.7.5 transition adds B-full SHARDED `engine_arch=centralized` full-surface deletion (D18) + cohort wrapper deletion (F17) + 3-WIP cadence (D17).

## Summary

| Verdict | Count | Notes |
|---|---|---|
| CLEAN | 4 | Classes 21 / 23 / 24 / 27 — N/A this ship per scope confinement |
| PLAN-NEW (v1.7.5) | 0 | NO Class N fabrications detected in v1.7.5 PLANNED scope |
| KNOWN-AMENDED | 2 | Class 14 (12 known prior + 0 NEW; recurrence_count amendment proposal below) + Class 18 (7 known + 1 NEW cohort instance pending) |
| Class 28 reduction | 1 | B-full DELETES 8 conditional branches — NET branchless improvement |
| Doc-debt findings | 0 | All Detection sections well-formed per Step 1 invariants |

## Per-class verdicts

### Class 14 — Plan calls a function or struct field that doesn't exist

**Verdict: KNOWN-AMENDED (12 known / 0 NEW in v1.7.5 PLANNED scope)**

**v1.7.5 PLANNED scope file:line citations VERIFIED at HEAD `e0acb65`:**

| Citation | Verified |
|---|---|
| `EngineSharded.hpp:1438` `if (cfg.engine_arch != ENGINE_ARCH_PER_CORE_SLOW)` | PASS |
| `EngineSharded.hpp:1453` `if (cfg.engine_arch != ENGINE_ARCH_PER_CORE_SLOW)` | PASS |
| `EngineSharded.hpp:1625` `cfg.engine_arch != ENGINE_ARCH_PER_CORE_SLOW)` | PASS |
| `EngineSharded.hpp:1637` `if (cfg.engine_arch != ENGINE_ARCH_PER_CORE_SLOW)` | PASS |
| `EngineSharded.hpp:1660` `if (cfg.engine_arch != ENGINE_ARCH_PER_CORE_SLOW)` | PASS |
| `EngineSharded.hpp:1695` `if (cfg.engine_arch != ENGINE_ARCH_PER_CORE_SLOW &&` | PASS |
| `EngineSharded.hpp:1718-1744` centralized-arch trio block (TimeExit + TrailingSLRatchet + BreakevenOnProfit + WS-staleness gate) | PASS |
| `EngineSharded.hpp:2484` boot-spawn gate `if (cfg.engine_arch == ENGINE_ARCH_PER_CORE_SLOW)` | PASS |
| `ControllerEventLoop.hpp:3435` `inline void EventLoop_TimeExit(...)` | PASS |
| `ControllerEventLoop.hpp:3722` `inline void EventLoop_TrailingSLRatchet(...)` | PASS |
| `ControllerEventLoop.hpp:3796-3804` `inline void EventLoop_BreakevenOnProfit(...) body` | PASS |
| `ControllerConfig.hpp:88-89` `ENGINE_ARCH_CENTRALIZED` + `ENGINE_ARCH_PER_CORE_SLOW` constants | PASS |
| `ControllerConfig.hpp:2800-2806` engine_arch string parser | PASS |
| `CfgFieldRegistry.hpp:396` `engine_arch` X-macro row | PASS |
| `EngineTUI.hpp:951` `uint8_t engine_arch` TUISnapshot field | PASS |
| `DashboardPanels.hpp:2165/2211/2216/2261/2274/2311/2324/2338/2357/2373` `s->engine_arch` gating | PASS |
| `BacktestSharded.hpp:423` `RegimeState<BACKTEST_FP> regime_state` | PASS |
| `BacktestSharded.hpp:489` `Regime_Classify(&fc->regime_state, &sig, fc->cfg)` | PASS |
| `BacktestSharded.hpp:494` `ctx.current_regime = fc->regime_state.current_regime` | PASS |
| `ShardedBacktestDriver.hpp:346` `EventLoop_UpdateRollingStateAllCores(...)` | PASS |
| `ShardedBacktestDriver.hpp:353-354` `EventLoop_UpdateEmaPriceAllCores(drv->state, *drv->ema_price)` — STAYS per N-4 REVERT | PASS |
| `ShardedBacktestDriver.hpp:356-364` `EventLoop_RebuildAllParameters_PerCore(...)` | PASS |
| `ShardedBacktestDriver.hpp:366` `EventLoop_PushParameters(drv->state)` | PASS |
| `ShardedBacktestDriver.hpp:367` `EventLoop_KillSwitchEvaluate(drv->state)` — STAYS per N-4 REVERT | PASS |
| `ShardedBacktestDriver.hpp:378-383` centralized trio `TimeExit + TrailingSLRatchet + BreakevenOnProfit` | PASS |
| `EngineCommon.hpp:133` `constexpr int BACKTEST_REGIME_SAMPLE_CORE = 0` | PASS |

**ALL v1.7.5 PLANNED scope citations resolve at HEAD `e0acb65`. Zero PLAN-NEW Class 14 instances in v1.7.5 amendment scope.**

**Residual fabrication scan against current v1.7.4 plan body:** Run grep for `current_book_imbalance` / `drv->depth_enabled` / `tick.timestamp_us[^0-9]` / `FPN_IsZero(double)` / `breakeven_on_profit_threshold` / `cfg.execution_core_count` / `OrderManager_RegisterCore` / `MASK_GATE_BREAKEVEN_ON_PROFIT` against plan body — all hits are within AMENDMENT-HISTORY sections (frontmatter v1.7.4 history block + lines 60-90 corrections table + line 290 D1 entry + line 833 reconsideration notes + line 1075 v1.7.4 correction note) explicitly labeled "fabricated" / "REJECTED" / "v1.7.4 path correction" — these are LEGITIMATE-HISTORICAL-REFERENCES per Class 14 KNOWN list convention, NOT actionable fabrications.

**Recurrence_count amendment proposal for Phase D ship close:**
- Current: 12 (per v1.7.4 frontmatter; +4 NEW-1/2/3/4 corrections at v1.7.4 + 2 additional B-Plus catches per F9)
- Proposed at Phase D: **STAY at 12** (no NEW v1.7.5 instances; B-Plus CI tool + line-anchor mode at v0.3 LANDED at WIP-10 per D15 closure validates M7 structural enforcement is now ENFORCING — line-anchor mode catches symbol drift at line-range level; symbols resolve at different positions catch via DRIFT verdicts; M7 thesis empirically validated)

**Sister discipline reminder (M6 body-content arg enumeration):** v1.7.5 PLANNED scope introduces N-6 9-arg signature already verified at v1.7.3; v1.7.5 amendment SHOULD re-fire B-Plus CI tool against amended body once C.4 BACKTEST caller code samples + B-full deletion code samples land. M6 sister memory `feedback_enumerate_helper_signature_args_before_extract` applies at every amendment cycle per Stage 6 extended scope.

### Class 18 — "Mirror" plans missing data-flow dependencies (STRENGTHENED call-sequence enumeration)

**Verdict: KNOWN-AMENDED (7 known prior; 1 NEW instance pending v1.7.5 capture per F17)**

**v1.7.5 PLANNED scope structurally CLOSES** a Class 18 instance per D18 + F17:

| Aspect | Status |
|---|---|
| Sister wrappers identified for cohort deletion | 3 of 3 — `EventLoop_TimeExit` (:3435) + `EventLoop_TrailingSLRatchet` (:3722) + `EventLoop_BreakevenOnProfit` (:3796-3804) per F17 |
| 4th sister wrapper hidden in centralized-arch block? | **NEGATIVE** — `EventLoop_CheckWsStaleness` at :3542 IS called from `EngineSharded.hpp:1742` centralized trio block, BUT `CheckWsStaleness` is also called from BOTH per_core_slow path (via `EngineCommon.hpp:776` inside `SlowPathCycleOneCore` body) AND `ShardedBacktestDriver.hpp:331`. It is engine-wide concern, NOT a centralized-only sister to the trio. Body uses `state->global_gate_state.flags` (ENGINE_WIDE scope) NOT per-core state. CheckWsStaleness STAYS post-B-full (continues to fire from both per_core_slow + BACKTEST paths). Verified via comprehensive grep at HEAD `e0acb65`. |

**Recurrence_count amendment proposal for Phase D ship close:**
- Current: 7 (per v1.7.3 amendment; +1 N-4 over-broad deletion would have reverted PARITY-026)
- Proposed at Phase D: **8** (+1 for F17 cohort-deletion mirror class instance — D18 STRUCTURALLY CLOSES this class instance via full-surface deletion; cohort-deletion logic IS the M5 train-serve execution-layer parity structural fix at the orphaned-wrapper layer; recurrence_count increments per closure-evidence-of-existence)

**Sister discipline reminder (Class 18 STRENGTHENED call-sequence enumeration):** D17 3-WIP split has B-full deletion + cohort wrapper deletion as one anchor (WIP-13) and BACKTEST migration as a separate anchor (WIP-12) — separation correct per Class 18 STRENGTHENED at v5.14.2.E.1 (the COHORT deletion logic for orphaned wrappers IS the closure pattern; aligns with `EnsembleModelZoo_PostLoadSetup` X-macro registry extraction precedent at PARITY-009/010/011/012 closure).

### Class 21 — Multiple parallel descriptors for similar surfaces (cross-file drift)

**Verdict: CLEAN — N/A this ship**

**Rationale:** Per task context analysis: `engine_arch` enum (`ENGINE_ARCH_CENTRALIZED` + `ENGINE_ARCH_PER_CORE_SLOW`) is itself a 2-value runtime dispatch surface (NOT a wide-variant parallel descriptor cohort). H18 SIDECAR OVERRIDE pattern N/A — v1.7.5 PLANNED scope does NOT introduce any new wide-variant registries. Full-surface deletion of `engine_arch` ELIMINATES the dispatch surface entirely (cleaner than sidecar override at this cohort size of 2 with deprecation intent).

**Recurrence_count: UNCHANGED at 2.**

### Class 23 — Type-erased typed-field write via reinterpret_cast through char* offset

**Verdict: CLEAN — N/A this ship**

**Rationale:** v1.7.5 PLANNED scope contains zero `reinterpret_cast<X*>((char*)Y + Z)` patterns. Deletion-scope ships do not introduce type-erasure dispatch surfaces. Detection grep `rg "reinterpret_cast<.*>.*\+\s*\w*offset"` against in-scope file deltas returns 0.

**Recurrence_count: UNCHANGED at 1.**

### Class 24 — Capability-cfg surface mismatch

**Verdict: CLEAN — N/A this ship**

**Rationale:** v1.7.5 PLANNED scope does NOT add new ML capabilities. Full-surface deletion of `engine_arch` cfg row REMOVES one cfg field (with full surface cleanup: parser + struct + GUI + TUISnapshot) — this is REVERSE direction from Class 24 (which is "added capability, missed cfg surface"); here we remove a cfg surface that has 0 ML/capability semantics (boot-time architectural dispatch only). Per `IS_BOOT_ONLY` metadata bit on the cfg row → no Settings panel runtime exposure to begin with.

**Recurrence_count: UNCHANGED at 3.**

### Class 27 — Single-value cache flattens per-instance distinction

**Verdict: CLEAN — N/A this ship**

**Rationale (per task context analysis):** Phase C.4 BACKTEST caller-precompute (`mtm_price` per v1.6 O2 bytewise-identical math) is at DECISION TIME (per-tick at caller scope; passed forward to helper) NOT at CACHE TIME (no subsystem state mirror introduced). Caller-precompute pattern matches Pattern 4 (pre-resolution at decision time) per `decision-time-data-binding-pattern.md` — this is the CANONICAL Class 27 closure shape, not the anti-pattern.

Additional verification: caller-precompute `mtm_price` is a per-cycle local variable (not stored in any subsystem state). Helper accepts as 9th arg per N-6 signature. Bytewise-identical math discipline preserves the existing LIVE per_core_slow lambda math at caller-side without introducing helper-internal recompute drift.

**Recurrence_count: UNCHANGED at 1.**

### Class 28 — Branchy SP/HP dispatch when branchless feasible

**Verdict: BRANCHLESS REDUCTION (NET decrease of 8 conditional branches)**

**Per task context:** B-full deletion REMOVES 8 conditional branches at EngineSharded.hpp `:1438` + `:1453` + `:1625` + `:1637` + `:1660` + `:1695` + `:1718` + `:2484`. These are `if (cfg.engine_arch != ENGINE_ARCH_PER_CORE_SLOW)` runtime checks — per H20, these are LEGITIMATE EXEMPTIONS (boot-time-only at `:2484` + cfg-derived dispatch at top-of-slow-path-cycle for `:1438/:1453/:1625/:1637/:1660/:1695/:1718`). They were branchy because they predated the deprecation intent.

**Post-B-full deletion: branchless improvement (0 of these 8 branches remain).** Boot-spawn gate at `:2484` UNCONDITIONALIZES (per_core_slow becomes the only path). Slow-path-cycle gates UNCONDITIONALIZE (centralized branches deleted entirely).

**Recurrence_count amendment proposal for Phase D ship close:**
- Current: 7 (per v5.15.5.F.4d MERGED close)
- Proposed at Phase D: **STAY at 7** (recurrence_count tracks NEW INSTANCES, not closures; closure-count is tracked separately in Class 28 ledger's "canonical closures" section — propose ADDITIONAL ENTRY to canonical closures list documenting `.B.4` v1.7.5 8-branch elimination)

**Sister discipline reminder:** Per H20 invariant + `branchless-dispatch-discipline.md`, even legitimate boot-time exemptions are preferred-eliminated when the surface deprecates. v1.7.5 PLANNED scope demonstrates the discipline by deleting rather than retaining-with-`__builtin_expect`-rare tag.

## Stale code samples in plans/ (per /bug-check plans scope)

**Scan result: ZERO stale anti-pattern code samples in v1.7.4 plan body that would re-introduce closed bug classes via paste.**

All historical fabrication references in v1.7.4 plan body (lines 24, 60-61, 85-86, 90, 179, 290, 693, 722, 833, 1075) are:
1. Within frontmatter `v1_X_amended:` history blocks — historical context preserved per amendment-history audit-trail discipline
2. Within v1.7.4 amendment summary corrections table (lines 60-90) — explicit "Fabrication" / "Actual at HEAD" pairings teaching the recurrence pattern
3. Within bug-class-closed entries — labeled as RESOLVED / RECONSIDERED / per v1.5 D1
4. Within v1.7.4 correction parenthetical — explicitly labels NEW-1 through NEW-4 as "v1.7.4 path correction" with HEAD-verified replacement

Per Class 14 "preserve-with-notice anti-pattern" lesson learned at v5.15.5.F.4c (5th detection event): **the v1.7.4 corrections are EXPLICIT explanatory text, NOT stale code samples in proposed-code blocks.** The actual proposed Phase C.4 BACKTEST caller code block at lines 1041-1073 uses CORRECTED field names (`drv->book_imbalance` per :102 + `*drv->current_spread` + `*drv->current_mid_price` deref + null-check pattern). Verified via direct read of plan body lines 1041-1073.

**No PLAN-NEW Class 14 instances. No stale-sample reintroduction risk.**

## NEW instance candidates in v1.7.5 PLANNED scope

**Per-class final verdict:**

| Class | NEW count in v1.7.5 | Notes |
|---|---|---|
| Class 14 | **0** | All v1.7.5 PLANNED file:line citations verified at HEAD `e0acb65` |
| Class 18 | **+1 cohort-deletion mirror instance (F17)** | Structurally CLOSED by D18 full-surface deletion; +1 recurrence_count proposed |
| Class 21 | 0 | N/A this ship |
| Class 23 | 0 | N/A this ship |
| Class 24 | 0 | N/A this ship |
| Class 27 | 0 | Caller-precompute mtm_price is Pattern 4 (decision-time), NOT cache-time anti-pattern |
| Class 28 | -8 branches | NET branchless improvement (8 conditional branches deleted) |

## Recurrence_count update proposals for Phase D ship close

| Class | Current | Proposed | Delta | Rationale |
|---|---|---|---|---|
| Class 14 | 12 | **12** | 0 | Zero v1.7.5 PLAN-NEW instances; B-Plus v0.3 line-anchor mode at WIP-10 validates M7 structural enforcement WORKING |
| Class 18 | 7 | **8** | +1 | F17 cohort-deletion mirror instance (3 sister wrappers orphaned post-B-full) STRUCTURALLY CLOSED via D18 |
| Class 21 | 2 | **2** | 0 | N/A this ship |
| Class 23 | 1 | **1** | 0 | N/A this ship |
| Class 24 | 3 | **3** | 0 | N/A this ship |
| Class 27 | 1 | **1** | 0 | Caller-precompute is Pattern 4 (canonical Class 27 closure shape) |
| Class 28 | 7 | **7** | 0 | Class 28 ledger "canonical closures" gets +1 entry documenting `.B.4` 8-branch elimination; recurrence_count UNCHANGED (tracks new instances, not closures) |

## Sister discipline reminders

**M6 (body-content arg enumeration before helper extract)** — applies to v1.7.5 amendment cycle:
- C.4 BACKTEST caller code block: body content already enumerated at v1.7.4 corrections (NEW-1 through NEW-4 path/field/deref fixes); v1.7.5 amendment should re-fire B-Plus CI tool against amended C.4 + B-full deletion code samples
- Sister memory: `feedback_enumerate_helper_signature_args_before_extract.md` Stage 6 promotion candidate ACTIVE
- /readiness Check 33 ENFORCES at amendment time

**M7 (structural enforcement when memory codification proves insufficient)** — applies to v1.7.5 amendment cycle:
- B-Plus v0.3 line-anchor mode LANDED at WIP-10 (engine `6b38ad5`); validates M7 thesis
- Pre-commit hook at `.git/hooks/pre-commit` rejects fabrications at commit-time
- /capture-audit skill mechanical drift check (10 checks; runs pre-commit + pre-handoff + standalone)
- Sister memory: `feedback_structural_enforcement_when_memory_insufficient.md` Stage 3 first canonical via B-Plus CI tool + /capture-audit skill
- v1.7.5 amendment cycle MUST re-fire B-Plus CI tool + `/capture-audit` + targeted `/bug-check` re-fire post-amendment GREEN before WIP-12 coding unlock

## Delegation summary

- Class 14-18 detection: DELEGATED to /trace-deps (this audit verified inline per task-prompt scope contract; full /trace-deps + /readiness + /precoding-audit-gate pending at v1.7.5 amendment cycle per C26)
- Class 12 (Wired-but-unexercised ML paths): N/A this ship
- Class 23 detection: EXECUTABLE inline (zero hits in v1.7.5 PLANNED scope)
- Class 27 detection: EXECUTABLE inline + cross-ref `/accounting-audit` for full closure verification at Phase D ship close
- Class 28 detection: EXECUTABLE inline + cross-ref `/hft-audit` for canonical closures ledger update at Phase D

## Suggested next actions

1. **Land v1.7.5 plan body amendment** (per C25) — capture B-full FULL-SURFACE deletion scope + cohort wrapper deletion (F17) + 3-WIP cadence (D17) + REFUSE decision (D18: NO REFUSE handling)
2. **Re-fire `/precoding-audit-gate` + `/blindspot-scan` + `/bug-check`** at v1.7.5 plan body per C26 BEFORE WIP-12 coding
3. **B-Plus CI tool re-run** against v1.7.5 amended plan body (pre-commit hook auto-fires; exit 0 required)
4. **Phase D ship close bookkeeping:**
   - Class 14 recurrence_count: STAY at 12 + amend "Known instances" with B-Plus v0.3 line-anchor mode validation note
   - Class 18 recurrence_count: 7 → 8 + ADD F17 cohort-deletion structural closure entry
   - Class 28 canonical closures: ADD `.B.4` v1.7.5 8-branch elimination entry

---

**End of /bug-check report.** Per `feedback_proportionate_response_to_audit_findings`: zero CRITICAL/HIGH findings in v1.7.5 PLANNED scope; one MED-bookkeeping action (Class 18 recurrence_count amendment + Class 28 canonical closures ledger update at Phase D). v1.7.5 amendment cycle UNBLOCKED from Class 14/18/21/23/24/27/28 perspective.
