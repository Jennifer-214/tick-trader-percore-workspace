---
type: handoff
status: active
ship_tag: .E.0.10
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-12-E.0.10-capital-retriage-close-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md
required_reading: [register (the §Routing map + A1–A27), decision-log D-198..D-201, characterization-test-discipline.md, representation-migration-completeness.md, subsystem-designs/exit-chain-tp-sl-design.md, tools/run_all_tests.sh]
engine_head: f90aa66 (A19 fix; on feat/v5.15-live-readiness)
workspace_head: (this handoff's own commit; deep-audit + routing + tools + A19 char-test)
coding_status: fix-ship STARTED — A19 LANDED + char-tested (suite 3372/0); A9 is the next fix
---

# .E.0.10 handoff — deep adversarial pass + A19 fix landed (2026-06-12, close-out before compaction)

> **Why this handoff:** a very long session (the context got audit-heavy — "poisoned by planning", operator's words). **The discovery phase is DONE; this fresh session is for EXECUTION** (the fix-ship), not more audit. Everything below is committed.
>
> **Mid-ship CHECKPOINT** — `.E.0.10` is IN-FLIGHT, NOT yet tagged (A19 is the first of several fixes). `/accept-handoff` Stage 4.5 has no predecessor-ship tag/CHANGELOG/postmortem to verify → SKIP it (advisory, not a block). The live TaskList at write-time was STALE (it still flagged `#7 F-059` as "THE NEXT ACTION" and never got A20–A27) — **the §TaskList capture below is the corrected SSoT; recreate from it, ignore any pre-audit ordering.**

## What this session did (the arc — all committed)
1. **#12 char-test-discipline spec + Class 43** (the never-authored "AP4" + the D-190 generalization) — adversarially verified (D-198); + meta-lessons AR-8 (mechanical-green ≠ content-verified) / AR-3 (transcription-trap) (D-199).
2. **6-agent deep adversarial pass over core trading code** ("really get a deep pass… prepare to run money") → **A16–A27, ~5 HIGH live-path capital bugs** (D-200/D-201): A16 partial-fill book-and-free · A20/A21 reconcile (FALSE kill-switch trip + restart double-apply) · A22/A23 2nd torn-read surface (`CoreContext`) · A24 dead session/D10-adaptive/spike cfg mutations. Each surface ALSO returned large **verified-correct** locks (Money/SSoT/drainer/SPSC/seqlock/kill-switch-logic/sizing/snapshot).
3. **The meta-pattern — `representation-migration-completeness.md`:** 3 incremental migrations (single-core→per-core, 24B→16B, expected-entry→per-fill) each left a SECOND representation; a consumer reads the STALE one (4 sub-shapes: scope/derivation/binding/threading). Explains A1/A9/A10/A22–A25. + **Class 44** + the **exit-chain subsystem-design catalogue**.
4. **Routed every finding → its E home** (register §Routing map): `.E.0.10` fix-ship (A9/A19/A25/A24/A16) · `.E.1` live-enable gate (A20/A21/A22/A23) · `.E.M` ML ship (A11/A12/A10; bandit FOLDED into E, TECH_DEBT-174) · D-123 (A2/A4/A5). TECH_DEBT routed to E (159/173→.E.1; 174→.E.M; 175/176→.E discipline).
5. **Tools:** TECH_DEBT-176 **unified test runner BUILT** (`tools/run_all_tests.sh` — composes `build.sh test` + `check_session_docs.sh` + `calls_graph_diff.sh` + `run_sanitizer_suite.sh`; `--selftest` PASS). TECH_DEBT-175 struct-tracker SCOPED (AST tool; future ship). TECH_DEBT-159 sharpened (16B regression angle).
6. **A19 FIXED** (engine `f90aa66`) — clear `ratchet_tp` symmetrically on the inactive slot (H22 cross-trade leak); char-test 4/4; **suite 3372/0**; `run_all_tests.sh --fast` all-green (its first real use).

## THE NEXT ACTION — the `.E.0.10` durable-now fix-ship (register §Routing map order)
**A9 → A25 → A24 → A16.** Each = fix + characterization test (`characterization-test-discipline.md`: complete write-set · non-vacuous · freeze-CORRECTED) + `./tools/run_all_tests.sh --fast` (must be all-green) + independent `// ADV-REFUTE`.
- **A9 (NEXT — HIGH):** slippage silently DEAD on the production paper/backtest path — `pre_resolved.slippage_pct` bound (`Order.hpp:363`) but READ at ZERO live sites. **Sequenced FIRST — it gates trusting paper-test results as live-readiness evidence.** FIX = consume `slippage_pct` at the fill chokepoint (`OrderManager_HandleFill` / paper `OrderManager_Submit` branch; entry `+slip` / exit `−slip`; paper-gated) + delete the dead `EventLoop_OnEvent` slip block (closes A15). Register A9/A15.
- **A25** — trail-trigger per-fill SSoT (mirror the A1 `ResolvePerFillTpPct`). **A24** — cfg-mutation single-source (mutate `cores[slot]` or sync before the consumer; H22). **A16** — gate `HandleFill`+slot-free on `ORDER_FILLED` (not `event_log_mode`).
- Then **F-059** (#7 — the exit-chain golden-master, now SHARPENED by A24/A25) + **A6** (#3 chokepoint tp/sl-validity guard). Then **`.E.1`**.

## TaskList capture (Stage 1.5 — `/accept-handoff` Stage 7 recreates from THIS; supersedes the stale live list)

**Immediate — the `.E.0.10` fix-ship (recreate as active; do IN THIS ORDER):**

| # | Task | Status | Severity | One-line |
|---|---|---|---|---|
| 1 | **A9** — paper/backtest slippage silently DEAD on production path | pending — **NEXT** | HIGH | consume `slippage_pct` at fill chokepoint + delete dead `OnEvent` block (closes A15); gates paper-test trust |
| 2 | **A25** — trail-trigger per-fill SSoT | pending | MED-HIGH | mirror A1 `ResolvePerFillTpPct`; single-source the trail trigger |
| 3 | **A24** — cfg-mutation single-source (session/D10-adaptive/spike) | pending | MED-HIGH | mutate `cores[slot]` or sync before consumer (H22); kills the dead-write |
| 4 | **A16** — mode-1 books PARTIAL fill as complete + frees slot | pending | HIGH | gate `HandleFill`+slot-free on `ORDER_FILLED`, not `event_log_mode` |
| 5 | **F-059** (#7) — exit-chain golden-master oracle | pending | — | real per-fill exit chain; SHARPENED by A24/A25 (do AFTER them) |
| 6 | **A6** (#3) — chokepoint gate-emit tp/sl-validity guard | pending | — | 6 strategies + stamp-load floor |

**Completed this session (do NOT recreate):** A19 ✅ · #12 char-test spec ✅ · 6-agent deep pass ✅ · `run_all_tests.sh` (TECH_DEBT-176) ✅ · finding-routing + register ✅ · meta-pattern + Class 43/44 + exit-chain catalogue ✅.

**Routed DOWNSTREAM (tracked in register §Routing map — NOT `.E.0.10` tasks; recreate when that ship opens):**
- **`.E.1`** live-enable gate: A20/A21 (reconcile) · A22/A23 (`CoreContext` torn-read) · #4 conc-5 tsan · #5 backlog→`.E.1` mapping · TECH_DEBT-159/173.
- **`.E.M`** ML ship: A11/A12 (bandit reward DEAD) · A10 (dual-arm TP) · bandit (TECH_DEBT-174).
- **D-123**: A2/A4/A5 (data-loss: accumulate qty / parse `z` / commission / venue-side).

## Verify with ONE command
`./tools/run_all_tests.sh --fast` → C++ suite + doc-CI + orphan check → one green/red. (`--full` adds the sanitizer lanes.)

## Drift / landmines
- The mechanical sweep (`check_session_docs.sh`) is CLEAN — but **clean-mechanical ≠ everything-tracked** (AR-8 lesson; the manual completeness check caught A26/A27 + the un-logged deep pass). Trust the register + the decision log as the SSoT.
- The clang/LSP "FixedPoint not found" diagnostics on `controller_test.cpp` are LSP false-positives (relative include path); the real `g++` build is 3372/0.
- A19 spanned BOTH repos: the fix → engine (`CoreFrameworks/`, public); the char-test → workspace (`tests/`, symlinked). Future fixes will too.

## Operator collaboration (every-turn)
Address Caramel as Caramel / she / her. No AskUserQuestion modals — inline. Evaluate on robustness+latency+design, NOT time. Consult before coding (capital). Adversarial-default + reachability-before-severity BINDING. Micro-commits, compile-gated. No unhomed debt. **This session is EXECUTION — the discovery is done; resist the planning-treadmill, just code the fix-ship.**
