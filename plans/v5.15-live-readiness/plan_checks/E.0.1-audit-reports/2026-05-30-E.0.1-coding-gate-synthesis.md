---
type: audit-synthesis
gate: /precoding-audit-gate (HARDENED — D-77 HEAVIER-default; Stage 2.5 + 3.5)
ship: v5.15.5.F.4d.1.E.0.1 (Net-2 FP+replay determinism net)
plan: subplans/2026-05-29-v5.15.5.F.4d.1.E.0.1-pre-E1-foundational-fix-net.md (v0.2 DRAFT)
date: 2026-05-30
engine_head: 0b841b3
audit_tier: HIGH-RISK (money-bearing; H10 guard-matrix HOLE → LLM pass is the floor on the determinism surface)
audit_set_fired: 3× determinism quorum (parity/achieves/regression) + dod + blindspot + hft + accounting + Stage-3.5 completeness-critic
readiness: GREEN (separate /accept-handoff Stage 6 this session; this gate is the deeper pass and supersedes it on the specifics below)
verdict: YELLOW
combined_severity: 0 CRITICAL · 0 HIGH (core fixes) · 1 operator-decision (F-076/Net-1) · ~8 pre-coding amendments · 4 ledger items
---

# `.E.0.1` coding-gate synthesis — 2026-05-30

**Combined verdict: 🟡 YELLOW.** The 5-fix core (F-054/55/56/57/58 + recorder-emit) is **sound — logic verified GREEN** by the determinism quorum + accounting + hft. No fix is wrong; no redesign needed; hot-path-untouched (for sqrt) independently re-confirmed; sqrt-nondeterminism **never touched a money/risk decision**. But the gate surfaced a cluster of **pre-coding plan-body amendments** — one a guaranteed compile-fail, one a wrong acceptance gate, and the two "standing CI gates" (the ship's whole point) are under-specified — plus **one operator decision** (F-076/Net-1 routing). This re-confirms the 2026-05-29 fresh-audits YELLOW with a refined, complete amendment list.

Per `feedback_proportionate_response_to_audit_findings` + operator directive (2026-05-30: "MED/LOW ≠ ignore — un-addressed findings are tech debt"): **every finding below has a concrete disposition.** Nothing dropped.

## Per-lens verdict
| Lens | Verdict | Headline |
|---|---|---|
| Quorum-A parity | GREEN | train-serve parity preserved (trainer foxml_suite also builds native); no committed goldens |
| Quorum-B achieves-goal | GREEN (3 YELLOW build-correctness) | every fix achieves determinism; AR-2 enumeration passed (sqrt is sole divergent op) |
| Quorum-C regression-hunt | GREEN | no fix breaks a passing test or real consumer; AR-1 set-enumeration done |
| dod-audit | GREEN (1 YELLOW doc-precision) | new fp-determinism spec author-new JUSTIFIED; F-058 mis-cites H13 |
| blindspot-scan | YELLOW (4 SILENT-RISK) | const char** retype, includes, build-dir asymmetry, recorder byte-format |
| hft-audit | GREEN (1 YELLOW acceptance) | sqrt off hot path; F-058 IS on it (benign); calls_graph_diff is the WRONG gate |
| accounting-audit | GREEN | sqrt never touched money/risk; F-058 UB real under OMS fee math; memcpy byte+H4-preserving |
| Stage-3.5 completeness-critic | YELLOW | F-076 routing conditional-on-undrafted-Net-1; CI gates un-wired; harness promotion under-specified |

## A. FIX IN `.E.0.1` BEFORE CODING (amendments + code)
1. **[compile-fail — triple-confirmed A/B/blindspot] `const char**` retype** at the 4 replay sites (`BacktestEngine.hpp:88,89,95,96` + `DepthReplayState.hpp:224-227`). `parse_double_fast_advance(const char*, const char**)` vs the sites' `char *p` → `char**`→`const char**` is ill-formed. The plan's "near-1:1 substitution" is wrong-as-written. Fix: `(const char**)&p` per site OR add a `char**` overload in `ParseFast.hpp`. Amend the plan claim.
2. **[2-of-3 A/B] Enumerate ALL FP-bearing test targets** for F-057: `controller_test` + `parity_harness` + **`depth_recorder_test`** + **`compare_scalers`** (`CMakeLists.txt:213/238/228/248`). Plan hedges "+ any FP-bearing target" — make explicit.
3. **[blindspot SR-1 + B] Includes:** `#include <cstring>` (F-058 — plan flags) **+ `#include <charconv>`** (recorder-emit `to_chars` — plan does NOT flag) in both recorder headers.
4. **[blindspot SR-3] Build-dir asymmetry + tested==shipped pin:** tsan/asan ALREADY build native via raw `CMAKE_CXX_FLAGS` (`build.sh:226/238`), so "tested≠shipped" is build-dir-specific. Gate the F-057 define on `if(USE_NATIVE_128)`; pin the determinism-harness TU with `#ifndef USE_NATIVE_128 #error` so it can't silently regress.
5. **[hft + completeness P2] Swap the hot-path-purity acceptance gate.** `tools/calls_graph_diff.sh verify` is an orphan-**strategy**-function detector (`MODULE_PATTERNS = Momentum_/MeanReversion_/…`) — it tracks zero FP/sqrt/`_to_fp64`/latency and would pass even if F-058 pessimized the hot path. Replace with: the determinism harness (byte-compare) + a concrete latency check. The plan's "(reuse) latency ratchet" cites no real mechanism (`LATENCY_BENCH` = eyeball avg/min/max, no p99/threshold/exit) — Class-14 acceptance fabrication; relabel.
6. **[completeness P1/P3] Specify the standing-CI wiring** — the load-bearing gap. The two promised "standing CI gates" (determinism + replay-locale) name no wiring target; `.github/` doesn't exist; the only CI surface is `tools/hooks/pre-commit` (fast text-scans). A compile-and-run gate there is a NEW cost class — decide the mechanism (pre-commit hook vs `build.sh` step vs a `tools/check_*.py`). The harness (`determinism-gate-seed-fp_sqrt_diff.cpp`, self-labeled "throwaway") needs a concrete `tools/` promotion path + a 2-build-flag driver + resolve the sqrt-only (`:176`) vs full-op-table (`:44`) ambiguity. Without this, D-70 Tier-2 "actual mechanization" is unmet and the H10 HOLE isn't really closed (P6).
7. **[blindspot SR-4] Recorder-emit write∧read round-trip assertion** + grep for committed recorder CSV fixtures before coding (confirm "no existing recordings" holds — preserve integer columns/delimiters/`\n`; `to_chars` returns no NUL).
8. **[hft + accounting MED-1] Plan-accuracy correction:** frontmatter calls `_to_fp64`/`_from_fp64` "accounting-path but not the 500ns loop" — INACCURATE; with `USE_NATIVE_128` default they're on **both** the hot path AND the accounting/OMS-fee path (`OrderManager.hpp:1124`). Benign (memcpy = same MOV at -O2+, byte-identical x86-LE: `FPN<64>.w`=`uint64[2]`=16B≡`__uint128_t`; alignof 8 vs 16 — the old cast's alignment UB is real), but name it + verify no `-O3 -flto` pessimization via the harness.

## B. OPERATOR DECISION (planning clarity)
9. **[completeness P5 — VERIFIED + CORRECTED] F-076 routing.** NOT a flat contradiction (the critic anchored on the synthesis's early "fold 5×" at lines 51/98 and missed its deeper fix-audit revision at 122/125: "route out UNLESS Net-1 characterizes the fingerprint" + open-question at 125). The v0.2 body (line 48) **adopted** that revision → `.E.0.3`, conditional on Net-1; the `.E.0.3` plan exists and claims F-076 (`:38`). **Real residual issue:** the conditional hinges on **Net-1, which has no subplan file yet** (P4), and F-076 is provisionally tracked in two plans. F-076 is empirically real + partly fatal-consumer (`Fingerprint.hpp:180` raw-SHA un-zero-init `ControllerConfig` → lineage; identical-value configs → 16,961/68,224 differing bytes; `_Default` memset alone insufficient). **Decision needed:** (i) is `.E.0.3` the right provisional home, and (ii) does Net-1 characterize the fingerprint (→ fold to `.E.0.1` with `Fingerprint_CanonicalizeConfig` field-wise-into-zeroed-buffer) or not (→ stays `.E.0.3`)? Until Net-1 is drafted the conditional can't resolve.

## C. FOLD TO TASK #1 (Classes 37+ codification)
10. **[dod] F-058 class naming:** it's aliasing/alignment UB with type known-correct — NOT H13/Class-23 type-erasure (wrong type written). Name the new class precisely; cite H13 as adjacent-distinct, not parent.
11. **[dod] New `fp-determinism-canonical-path-discipline.md`** needs bidirectional cross-refs at authoring.

## D. LEDGER (per operator directive — don't lose)
12. **TECH_DEBT (NEW):** `verify_ticks.sh:75` reads ts from column 6 (8-col aggTrades layout) but `TickRecorder.hpp:186` emits 4-col with ts in column 1 — PRE-EXISTING mismatch (not `.E.0.1`-introduced). Open it so the emit edit isn't blamed + it gets fixed.
13. **PARITY auto-write confirm:** PARITY-036 (recorder-emit), -035 (F-076 canonicalize), -034 (cfg-atof) referenced in plan → verify written to `PARITY_ISSUES.md` per auto-write contract.
14. **TECH_DEBT (watch):** `FP64` struct lacks `_padding=0`; safe today (not byte-compared anywhere) but if it ever enters a memcmp/SHA/wire path it needs it. Advisory row.

## E. DOCUMENT (operator-migration)
15. **[quorum-A MED] Old gitignored locally-trained models** shift serve-features post-F-056 (no fingerprint hash rejection) → one-time retrain at the boundary. Add to the plan's operator-migration table (R4 sister) as a real action, not just a note.

## F. CONFIRMED GREEN (no action)
sqrt never touched money/risk (sizing uses `RollingStats.hpp:374` range/4 stddev approximation, not sqrt; sharded LIVE OMS has no sqrt in fee/PnL/balance) · hot-path-untouched for sqrt (4 callers, all slow-path ML) · memcpy byte+H4+UB-clean · NR const-iter (12, unrolled) negligible latency · new spec author-new justified (no parallel-registry anti-pattern) · no committed goldens (fresh regen holds) · train-serve parity preserved.

## Anti-pattern verdict
The fixes REMOVE parallelism (SSoT-positive: one parser, one sqrt path); the one NEW spec is a legitimate higher layer, not a Class-21 parallel registry. **No anti-pattern reintroduced.** Candidate Classes 37+ (tested≠shipped / aliasing-alignment-pun-not-memcpy / locale-parse-on-determinism-path / stub-oracle / inconsistent-native-spec-set) → task #1.

## M7 escalation check
None — no codified rule violated-despite-memory at the same surface.

## Recommendation
Amend the plan body for items A.1–A.8 (~1–2h; the gate-fabrication and const-fail ones are non-optional), take the B.9 (F-076/Net-1) decision, write the D ledger entries, then re-confirm GREEN and code. **Consult-before-coding — no auto-proceed.**
