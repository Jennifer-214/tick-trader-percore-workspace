# v5.14.10 handoff prompt — Bayesian Thompson sampling bandit

**Created:** 2026-05-10 (immediately post v5.14.9 close)
**Target ship:** v5.14.10 — Bayesian Thompson sampling bandit (operator-toggleable vs existing Exp3-IX)
**Branch:** `feat/v5.14-foxml-port-and-maker` (continue from `v5.14.9` tag)
**Pre-tag rollback anchor:** `v5.14.9`
**Plan file:** `plans/v5.14-foxml-port-and-maker/subplans/2026-05-08-v5.14.10-bayesian-thompson-bandit.md`
**Predecessor postmortem:** `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-10-v5.14.9-session-postmortem.md`

---

## Paste this prompt into a fresh Claude Code session to start v5.14.10

```
I'm picking up v5.14.10 (Bayesian Thompson sampling bandit) for the
FoxML_Trader_v2 sprint. This is a fresh context window; do NOT trust
any prior-session memory — verify everything against current code.

## Step 0 — orient + verify state (BEFORE planning anything)

1. Run these in parallel:
   - `cat /home/caramel/code/FoxML_Trader_v2/Version.hpp` — confirm "5.14.9"
   - `cd /home/caramel/code/FoxML_Trader_v2 && git log --oneline -5` — confirm
     latest commit is the v5.14.9 umbrella (b09b2d5 or similar)
   - `cd /home/caramel/code/FoxML_Trader_v2 && git tag --sort=-creatordate | head -5`
     — confirm v5.14.9 tag exists
   - `cd /home/caramel/code/FoxML_Trader_v2 && git status` — confirm clean tree
     (only context_cont.md untracked is expected)

2. Read these in parallel (load context):
   - `CLAUDE.md` (engine repo project instructions)
   - `CLAUDE.local.md` (private overlay; design philosophy + going-forward rules)
   - `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md`
     (auto-memory index; read entries Caramel has built up)
   - `plans/v5.14-foxml-port-and-maker/MASTER.md` (sprint master plan)
   - `plans/v5.14-foxml-port-and-maker/subplans/2026-05-08-v5.14.10-bayesian-thompson-bandit.md`
     (THE plan for this ship)

3. Also read these RIGHT BEFORE WRITING CODE (per CLAUDE.local.md required reading):
   - `DOCS/STRATEGY_AND_CODING_RULES.md` (11 strict invariants; no malloc,
     no virtual, no mutex, branchless, AVX-512-friendly)
   - `plans/_cross-cutting/2026-05-06-latency-path-discipline.md`
     (7 architectural rules; ML inference IS on the slow-path latency budget)
   - `DOCS/LATENCY_OPTIMIZATION_AUDIT.md` (Gemini sweep findings;
     bandit-relevant items in Parts 1, 4, 10)

## Step 1 — re-verify plan against current code (HEAD)

The plan was drafted 2026-05-08. It's been 2 days; the codebase has moved
through v5.14.0–.9. The plan's "Pre-existing work audit" REUSE claims and
file:line refs may be stale.

Run `/readiness plans/v5.14-foxml-port-and-maker/subplans/2026-05-08-v5.14.10-bayesian-thompson-bandit.md`
and address every GAP or stale-reference finding. Common stale claims to verify:

- File:line refs (`ML_Headers/BanditLearning.hpp` exists? functions still
  exist? Have they been renamed/refactored?)
- `EnsembleModelZoo` struct shape (any field renames since 2026-05-08?)
- `ML_BuildParameters` dispatch site at `Strategies/StrategyParameters.hpp`
  (still ~line 835? cfg-flag-gated already via v5.14.9.F.2 ML_CFG_FLAG migration?)
- `cfg.bandit_algorithm` cfg field — the plan adds this. Verify it's NOT
  already added by v5.14.9.F.2 ML_CFG_FLAG bitmap (would be a collision)
- Persistence pattern — `EnsembleModelZoo_SaveBanditState` /
  `LoadBanditState` — still the canonical persistence API?

Also note: the plan title says "v5.14.11" (stale; was renumbered to
v5.14.10 in the master plan update 2026-05-10). Fix the heading.

## Step 2 — pre-coding audit pass (multi-skill parallel)

Per `DESIGN_SPECS/audit-driven-pre-coding-gate.md`, this ship qualifies for
the pre-coding audit gate (new pattern surface — Thompson is a NEW compute
mode in the bandit registry; ≥5 new fields/functions; touches ML build path).

Spawn these 4 audits IN PARALLEL via Agent tool with Explore subagents:
1. `/parity-check` — focus: train↔serve identity for the new bandit algorithm;
   stamp body if cfg.bandit_algorithm becomes stamp-bound; persistence parity
2. `/trace-deps` — focus: plan file:line claims; new functions match planned
   signatures; persistence path round-trip works
3. `/readiness` (full 28-check pass) — cold-pickup completeness; new cfg field
   eligibility (per cfg-flag-eligibility-criteria.md); X-macro variant selection
4. `/merge-scan` — focus: shared compute between Exp3 + Thompson during
   "Both parallel" mode (cfg=2); RNG state surface reuse

After all 4 reports return, write a synthesis doc to
`plans/plan_checks/2026-05-XX-v5.14.10-fresh-audits-synthesis.md` listing
convergent findings (3+ audits agree) + audit-specific findings.
THEN consult Caramel before coding. Do NOT auto-proceed even if findings
look addressable.

## Step 3 — design check against pattern library

The plan was written before the v5.14.8 + v5.14.9 design specs landed.
Check whether NEW patterns now apply:

Required reading (DESIGN_SPECS catalog):
- `DESIGN_SPECS/README.md` (16-pattern catalog + "I need to..." quick discovery)
- `DESIGN_SPECS/curve-registry-pattern.md` — Thompson is a NEW compute mode
  in the "bandit algorithm" axis. Should there be a FOREACH_BANDIT_ALGORITHM
  registry mirroring FOREACH_DEGRADATION_CURVE? (3+ modes if Exp3 + Thompson +
  future UCB; fn-pointer dispatch pattern)
- `DESIGN_SPECS/cfg-flag-eligibility-criteria.md` — cfg.bandit_algorithm
  is an ENUM, not a boolean. Stays as int cfg field; NOT a candidate
  for FOREACH_ML_CFG_FLAG bitmap (criterion 5 — bitmap is for booleans).
  But the enum-value should follow curve-registry-pattern shape.
- `DESIGN_SPECS/bitmap-flag-api.md` — verify whether ThompsonBanditState
  has 3+ boolean state flags (e.g., `enabled`, `warmed_up`, `replay_mode`);
  if so, candidate for bitmap migration
- `DESIGN_SPECS/wire-format-byte-preservation-discipline.md` — if Thompson
  state is persisted (it should be; replay determinism), the binary/JSON
  format becomes wire-format-protected. Plan for forward/backward compat.
- `DESIGN_SPECS/heterogeneous-registry-pattern.md` — if Thompson opens
  N new operator-facing cfg fields (mu_prior, precision_prior, etc.),
  consider whether a domain registry (ML_CFG_FLAG was extended in .F.2;
  these would be ML-domain SCALAR fields not booleans, so direct cfg fields
  + stamp-binding via FOREACH_STAMP_BOUND_CFG is the path)

## Step 4 — design philosophy reminders

These are LOAD-BEARING rules (from CLAUDE.local.md + memory):

- **Defer is last-ditch, never effort-avoidance.** Implement properly the first
  time. Smaller-scope recommendations have been wrong 3/3 times this sprint
  vs Caramel's "do it right now" instinct.
- **Structural fix > direct patch for recurring bug classes.** If Thompson
  has any "same pattern at multiple sites" shape (e.g., bandit save/load
  parallel to Exp3's), use X-macro registry / helper extraction with
  compile-time enforcement.
- **No MVP for plumbing/refactor work.** MVP framing is reserved for
  genuinely-new features with unknown unknowns. Thompson sampling math is
  well-understood; this ship is plumbing + algorithm. Ship the full design.
- **Boundary-stable refactors preferred over wide cascades.** If Thompson
  needs new fields on EnsembleModelZoo, prefer adding fields to the
  existing struct over splitting into a parallel `EnsembleModelZoo_v2`.
- **Hot path UNTOUCHED.** Thompson runs on slow-path (ML_BuildParameters).
  Don't add anything to BG_Evaluate / SG_Evaluate / ExecutionCore_Tick.
- **Branchless mask compute > switch on enum on slow path.** For algorithm
  dispatch in ML_BuildParameters, prefer template-bool dispatch or
  fn-pointer table (curve-registry-pattern) over switch.
- **Reuse-audit before adding new code.** Per CLAUDE.md item 16, scan for
  existing functions / shared state / conversion paths before writing new.
- **Latency-additions get tracked.** Per CLAUDE.md item 17, log new slow-path
  cost in `DOCS/HOT_PATH_CHANGELOG.md` with cost estimate + branchless
  analysis. Thompson_Sample takes N Gaussian draws — ~N×10ns per arm; N×4
  arms × N cores at slow-path cadence.
- **Replay determinism is sacred.** Thompson Gaussian draws MUST use a
  seeded RNG (mt19937_64 + saved state) — never `std::random_device` /
  `std::default_random_engine` in production paths.

## Step 5 — operator collaboration

- Address Caramel as Caramel / she / her. Not "operator" in conversation
  (persisted docs can keep "operator" for general-audience framing).
- Don't use AskUserQuestion modal boxes. Present options inline as text;
  Caramel wants the full conversation scrollable.
- Evaluate options on robustness + latency + design philosophy, NOT time.
  Time is essentially never the deciding factor in option selection.
- When pre-coding checks complete, present audit findings + list potential
  fixes + iterate with Caramel BEFORE coding. Do NOT auto-proceed.
- Don't ship MVP for plumbing/refactor work. MVP for genuinely-new
  features with external dependencies + unknown unknowns only.
- Suggest mid-sprint audit when downstream sub-ships impact a new surface
  (per CLAUDE.local.md going-forward rule). Wait for greenlight.

## Step 6 — file system conventions

- Workspace path: `/home/caramel/code/tick-trader-percore-workspace`
- Engine repo: `/home/caramel/code/FoxML_Trader_v2`
- Plans live in workspace; symlinked from engine `plans/` →
  `workspace/plans/`. Edit at `workspace/plans/v5.14-foxml-port-and-maker/`
  paths; auto-flows to engine.
- DESIGN_SPECS at `workspace/DESIGN_SPECS/` (16 patterns catalog)
- Skill outputs go to `plans/plan_checks/` (neutral) initially; mechanically
  batched into sprint-dir plan_checks/ at sprint close.
- New deferral items get auto-written to `DOCS/TECH_DEBT.md` per the
  auto-write contract (CLAUDE.local.md).
- New parity findings get auto-written to `DOCS/PARITY_ISSUES.md` per
  the auto-write contract.

## Step 7 — sprint-close verification gate (for v5.14.10 specifically)

Before declaring v5.14.10 shipped:
- Tests pass (currently 2796; expect ~30-50 new for Thompson)
- /parity-check GREEN at v5.14.10
- /merge-scan GREEN (no missed reuse with Exp3)
- /latency-track entry for Thompson_Sample slow-path cost
- /bug-check CLEAN
- Replay determinism test passes (seeded Gaussian draws → bytewise identical
  across runs)
- Cfg.example doc updated for `bandit_algorithm` enum (0=Exp3/default,
  1=Thompson, 2=Both)
- Persistence round-trip: save Thompson state → restart engine → load →
  bytewise identical state
- If "Both parallel" mode (cfg=2) is shipped, telemetry must log which
  algorithm chose what per fill (for offline A/B)

## Closing reminder

This ship's value is the bandit ALGORITHM choice — Thompson's posterior
sampling vs Exp3's adversarial regret bounds. The plumbing (cfg, persistence,
dispatch) is mechanical. The math should be the smallest part of the diff.

If you find yourself writing complex plumbing, stop — there's almost
certainly an X-macro registry or AUTOPOPULATE pattern that should be
applied first. Check DESIGN_SPECS/README.md "I need to..." section.

Good luck. Caramel will iterate with you on findings before coding.
```

---

## Notes for future-Claude reading this handoff doc

- The prompt above is self-contained — paste it as the FIRST message in a
  fresh `claude code` session to start v5.14.10.
- The handoff includes 7 steps + design philosophy reminders + filesystem
  conventions. Follow them in order; don't skip Step 1 (re-verification
  against current code) — that's where stale-plan-claim issues surface.
- Plan file is at `subplans/2026-05-08-v5.14.10-bayesian-thompson-bandit.md`
  (note the stale "v5.14.11" heading inside — fix during Step 1).
- This sprint dir already has 4 postmortems from prior sub-ships; the
  v5.14.9 postmortem captured ALL late-sprint pivots + DESIGN_SPECS deliverables
  + lessons. Worth a full read for context.

---

## Quick links

- Sprint MASTER: `plans/v5.14-foxml-port-and-maker/MASTER.md`
- v5.14.10 plan: `plans/v5.14-foxml-port-and-maker/subplans/2026-05-08-v5.14.10-bayesian-thompson-bandit.md`
- Latest postmortem: `plans/v5.14-foxml-port-and-maker/postmortems/2026-05-10-v5.14.9-session-postmortem.md`
- DESIGN_SPECS catalog: `DESIGN_SPECS/README.md` (16 patterns)
- Latency rules: `plans/_cross-cutting/2026-05-06-latency-path-discipline.md`
- Coding invariants: `DOCS/STRATEGY_AND_CODING_RULES.md`
