---
type: handoff
status: active   # THE one live handoff. Supersedes 2026-07-18-E.1.2.A-conversion-complete-hard-zero-next-plugin-handoff.md (E.1.2.A closed as schema+conversion record; E.1.2.B opened). ≤1-active enforced by check_handoff_active_singleton.py.
ship_tag: E.1.2.B
plan_type: mixed   # refactor (close Class-18 plugin mirror + root-resolver + doc-drift classes) + feature (new editor-native surfaces + the tool-I/O substrate). Dev-plane — engine H1-H22 do NOT govern (tools/CLAUDE.md).
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: make every toolchain surface (CLI · local-CI · plugin · doc-viewer · AI) a thin consumer of the ONE core, versioned 0.x→1.0.0 — the "code becomes the IDE" endgame, felt in the editor
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-07-18-E.1.2.A-conversion-complete-hard-zero-next-plugin-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [DESIGN_SPECS/framework-patterns/standardized-tool-io-envelope-and-payload.md, DESIGN_SPECS/doc-disciplines/toolchain-semantic-versioning.md, DESIGN_SPECS/framework-patterns/one-action-toolchain-update-orchestrator.md, DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md]
deletion_scope: none   # comments/docs + additive tooling; no code deletion
coding_status: 0.1-BUILT + 0.1.5-DESIGNED (not built)   # foundation shipped; the tool-I/O substrate is designed + captured, next = build 0.1.5
---

# E.1.2.B — Toolchain V1 (plugin-first) — 0.1 foundation BUILT, 0.1.5 tool-I/O DESIGNED; next = build 0.1.5

**Created:** 2026-07-19
**Target:** build `0.1.5` (the standardized tool-I/O substrate), then the rest of the `0.x→1.0.0` roadmap.
**Branch:** `feat/v5.15-live-readiness`
**Plan:** `plans/v5.15-live-readiness/subplans/2026-07-19-E.1.2.B-toolchain-v1-plugin-first.md`
**Decision log:** `...v5.15.5.F.4d.1.E-architecture-v2.md` (D-372 → D-379)
**MASTER:** `...MASTER.md` (UPDATE 16)

## State at handoff write
- **Engine HEAD `4c076ed`** UNCHANGED this session (zero engine code — E.1.2.B is dev-plane, workspace-side). Rollback anchor `pre-E.1.2.B` at `4c076ed`. **Workspace HEAD `343a9f9`** (this handoff's commit bumps it). Branch `feat/v5.15-live-readiness`. **Doc-CI sweep CLEAN** throughout.
- **OFF-LIMITS (Caramel's uncommitted WIP — DO NOT touch/git-add/revert):** `CoreFrameworks/ExecutionCore.hpp` (the `_pad_line0` cache-padding tweak). Unchanged this session.
- Untracked scratch (not ours; leave): `build_clangd/`, `resume`, `tmpo0ujx6rw/`, `note`.

## What landed this session — all committed, sweep green
- **E.1.2.B opened** as a new sub-ship under E.1.2 (sibling to E.1.2.A, which closed as the schema+conversion record; its Phase-5 → E.1.2.B). D-372's 5 open questions all resolved.
- **Decisions D-372 → D-379** (decision log): D-373 toolchain semver (MAJOR↔`[SCHEMA]`, `0.x→1.0`, `TOOLCHAIN_VERSION` SSoT) · D-374 one-action update orchestrator (idempotent, verify-after; gates stay verify-only) · D-375 E.1.2.B structure + AI-plane surface + all-in scope · **D-376 the tool-I/O ENVELOPE + uniform record-set PAYLOAD** · **D-377 `.toolbus/`** · **D-378 payload kinds (graph kinds) + `kind`-as-vocab + content-hash-NOT-HMAC** · **D-379 per-item schema tracking + versioning** (3 envelope version axes + per-kind payload-schema version + spec-per-artifact).
- **Plan + 4 DESIGN_SPECs** (stage-2 drafts): `toolchain-semantic-versioning` · `one-action-toolchain-update-orchestrator` · `standardized-tool-io-envelope-and-payload` (+ north-star amended). `tools/CLAUDE.md` carries the versioning + update-skill disciplines.
- **`0.1` FOUNDATION — BUILT + committed** (3 wip commits `cc4a3f7`/`4779b54`/`3bec579`): `tools/foxroots.py` = the ONE machine-portable repo-root resolver (SSoT); migrated `check_doc_metadata` (re-export) + `check_conversion_completeness` (the portability-DEAD HARD gate) + `rebuild_doc_indexes` + `subdivide_design_specs` → **ZERO hardcoded-absolute roots remain**. `check_import_from_core.py` lint wired HARD into `check_session_docs.sh` (+ `--selftest` + baseline; 7 rollers grandfathered as KNOWN-PENDING) → **the roll-your-own-root class is structurally closed**. Two bug classes closed (portability-dead + roll-your-own-root); the plugin-mirror Class-18 closes at 0.3.
- **`0.1.5` tool-I/O substrate — DESIGNED + captured, NOT built** (this session's design arc; verified grounded against real code — foxtag already emits structured `units --json`, the graph producers exist, no `[SCHEMA]` bump needed).

## Per-increment status (the 0.x→1.0.0 roadmap)
| Increment | Status |
|---|---|
| `0.1` foundation (foxroots + migrations + import-lint) | **BUILT + committed** |
| `0.1.5` tool-I/O substrate (envelope + record-set payload + `.toolbus/`) | **DESIGNED (D-376/377/378/379), NOT built — ▶ NEXT** |
| `0.2` update-skill · `0.3` plugin cutover · `0.4` tag-native surfaces · `0.5` UX+RC-F · `0.6` AI-plane · `0.7` pre-push+producer/gate-role · `0.8` north-star substrate · `1.0.0` cohesion | planned |

## ▶ NEXT ACTION — build `0.1.5` (the tool-I/O substrate)
1. **ENUMERATE all planned payloads FIRST** (the `feedback_dont_generalize_substrate_before_input_space_known` discipline — don't guess the universal shape): foxtag `units`/`layout`/`fields`/`codegen` outputs + the graph kinds (`CODE_MAP`, foxtag `RefIndex`, `gen_code_map`, the plugin's clangd call-hierarchy → `callgraph`/`depgraph`/`refgraph`/`blast_radius`) + `verdict` (gate findings) + `instantiation`.
2. **Lock the uniform record-set typing** so it fits ALL of them (named record-sets = `{schema, rows}`, tag-vocab-aligned; per-`kind` payload-schema version per D-379).
3. **Build:** `foxtag grammar --json` = the 1st canonical envelope (emit SETS not counts — `foxtag_main.cpp:127`; + `schema_version` — a small add, foxtag knows `v1.0` but doesn't emit it) → stand up `.toolbus/` (gitignored latest-wins) → `foxtag_client` function-call helpers.
4. **Adopt** (behind `parity_check`, alongside the tag system): the plugin reads `grammar.json` from the bus → derives its `UNIT` set + axes → kills the hardcoded Lua mirror (`tag_grammar_adapter.lua:27` + `tagcontext.lua:9-10`) — this feeds `0.3`.

## Arming for the next action
The next action (`0.1.5`) is **dev-plane** — `tools/CLAUDE.md` governs; engine H1-H22 do NOT. The payload-enumeration is a DESIGN/grounding step, not audit-heavy. Arm it:
- **Read first:** `tools/CLAUDE.md` (dev-plane governance) · the 4 sister_specs (esp. `standardized-tool-io-envelope-and-payload.md`) · north-star §6/§8.6 · the schema SSoT `in-code-documentation-schema.md` (the tag-serialization sibling).
- **Run (not just read):** `tools/foxtag/foxtag units --json | head` + `layout`/`fields`/`codegen` (ground the real payload shapes) · `./tools/foxtag/build.sh` (rebuild after the `grammar --json` add) · `tools/foxtag/parity_check.sh` (the cutover gate) · `check_session_docs.sh` (the doc floor) · `check_import_from_core.py` (the now-HARD lint — don't add a roll-your-own root).
- **Correctness-critical care** for the foundation-shaped pieces (the record-set typing = a contract; the emit/read helpers = the write/read SSoT); **light-iterate** the plugin UX. Route agents (if any) via M8 (arm docs + toolchain + scout-first).

## HARD rules
1. **NEVER touch/git-add/revert `CoreFrameworks/ExecutionCore.hpp`** — Caramel's uncommitted WIP.
2. Dev-plane: comments/docs + additive tooling; the `[SCHEMA]_[v1.0]` in-code grammar is LOCKED — the tool-I/O JSON is a SEPARATE serialization that rides the vocab (no `[SCHEMA]` bump).
3. `check_import_from_core` is now HARD in the sweep — a new tool needing a repo-root MUST `from foxroots import …`.
4. Design the universal payload shape ONLY after enumerating all payloads (don't guess).

## Capture-completeness (this session — nothing lost)
- **Decisions → LOGGED:** D-372..D-379, each with paired `<!-- D/C/F -->`+`<!-- STATUS -->` sentinels in the decision log; the design ones (D-376/377/378/379) triple-homed (log + spec + plan/0.1.5).
- **Findings → homed:** the portability-dead HARD gate (`check_conversion_completeness.py:32`) FIXED at 0.1; the 14→7 root-resolver stragglers grandfathered in `tools/lib/import_from_core_baseline.txt` (guarded, shrinking); `TECH_DEBT-126` (two-axis versioning) + `TECH_DEBT-176` (unified test runner / tool-composition) cross-linked as adjacent (not closed); **`TECH_DEBT-244` OPENED** (tools/ dir de-sprawl — the operator-flagged "keep updated" management concern, homed post-close on the second "are you sure?" probe — an unhomed in-chat commitment the external git-diff enumeration caught).
- **Specs/tools → indexed:** 4 DESIGN_SPECs (3 new + north-star amend), index regen'd; NEW tools (`foxroots.py`, `check_import_from_core.py` + selftest) enrolled in `DOCS/TOOLS.md`; `tools/CLAUDE.md` amended. MASTER UPDATE 16.
- **Memories → 1 EXTENDED (post-close capture-audit):** `user_structure_is_correctness_risk_control_for_capital` extended — the apparatus/rigor now explicitly extends to the dev-plane TOOLCHAIN held as FIRST-CLASS (justified by accessibility + decision-fatigue, NOT only capital; the D-379 rationale). No NEW memory file. *(Initially recorded "none owed"; the operator's "are you sure?" probe surfaced the under-capture — the capital-anchored memory didn't cover dev-plane tooling. The AR-8 recurrence shape: self-review is model-bounded; the probe is the external enumeration.)*
- **Verified:** `check_session_docs.sh` sweep CLEAN at write; all work committed.

## TaskList state at handoff write (recreate via /accept-handoff Stage 7)
| ID | Status | Subject |
|---|---|---|
| #1 | completed | DESIGN D-372 open questions — RESOLVED |
| #2 | pending | E.1.2.B 0.1 — Foundation (BUILT — mark completed on pickup; foxroots + migrations + import-lint all landed) |
| #12 | pending | **E.1.2.B 0.1.5 — tool-I/O envelope + record-set payload + .toolbus/ (▶ NEXT)** |
| #3 | pending | E.1.2.B 0.2 — update-skill orchestrator |
| #4 | pending | E.1.2.B 0.3 — plugin cutover (parity+soak; retire Lua mirror) |
| #5 | pending | E.1.2.B 0.4 — tag-native surfaces (trees/browse/doc-viewer/gate-verdicts) |
| #6 | pending | E.1.2.B 0.5 — UX + RC-F register-fit + registry cards |
| #7 | pending | E.1.2.B 0.6 — AI-plane fact-consumption surface |
| #8 | pending | E.1.2.B 0.7 — pre-push gate + PRODUCER/GATE [TAG] role |
| #9 | pending | E.1.2.B 0.8 — north-star substrate (FOREACH_PLANE + multi-syntax + self-hosting) |
| #10 | pending | E.1.2.B Step 0 — rollback tag (DONE) + sync-workspace |
| #11 | pending | E.1.2.B 1.0.0 — cohesion close |

*(Note: #2 `0.1` is BUILT — recreate it as completed. #12 `0.1.5` is the live NEXT.)*

## Paste into a fresh Claude Code session
```
I'm picking up E.1.2.B — toolchain V1 / plugin-first. The 0.1 foundation is BUILT (foxroots SSoT + zero hardcoded roots + the import-from-core lint HARD; two bug classes closed). The 0.1.5 tool-I/O substrate (envelope + uniform record-set payload + .toolbus/) is DESIGNED + captured (D-376/377/378/379), NOT built.

Run: /accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-19-E.1.2.B-handoff.md

NEXT: build 0.1.5 — enumerate ALL payloads first (foxtag units/layout/fields/codegen + the graph kinds CODE_MAP/RefIndex/gen_code_map + verdict + instantiation) so the uniform record-set typing fits all, THEN foxtag grammar --json (emit SETS not counts + schema_version) + .toolbus/ + foxtag_client helpers → the plugin reads grammar.json from the bus (kills the hardcoded Lua mirror, feeds 0.3).

HARD: never touch/commit CoreFrameworks/ExecutionCore.hpp — Caramel's WIP. Dev-plane (tools/CLAUDE.md governs; [SCHEMA]_[v1.0] in-code grammar LOCKED — the tool-I/O JSON is a separate serialization). check_import_from_core is now HARD. Do NOT trust prior-session memory — verify against current code.
```

## Quick links
- Plan: `plans/v5.15-live-readiness/subplans/2026-07-19-E.1.2.B-toolchain-v1-plugin-first.md`
- Decision log: `...decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-372..D-379) · MASTER UPDATE 16
- Specs: `standardized-tool-io-envelope-and-payload.md` · `toolchain-semantic-versioning.md` · `one-action-toolchain-update-orchestrator.md` · `in-code-doc-system-north-star.md` §6/§8.6
- Governance: `tools/CLAUDE.md` (dev-plane) · schema SSoT `in-code-documentation-schema.md`
