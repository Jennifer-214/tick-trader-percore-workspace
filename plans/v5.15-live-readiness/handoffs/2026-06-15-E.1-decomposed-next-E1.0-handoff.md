---
type: handoff
status: active   # the one live handoff; no-arg /accept-handoff resolves here. Prior (2026-06-15 E.0.10-shipped-next-E1) flipped to superseded.
ship_tag: ".E.1 DECOMPOSED (planning phase complete) → next = CODE E.1.0 (the first leaf)"
plan_type: live-readiness
sprint_end_goal: make the codebase more maintainable for future development (v5.X professionalization); live-readiness gated behind the whole E-series
ship_end_goal: ".E.1 is now a SUB-MASTER over 7 leaves (E.1.0–E.1.6, D-225). The PLANNING PHASE is done — decomposition LOCKED + apparatus codified + the tree built. NEXT = re-ground + deep-gate + CODE E.1.0 (the §4d guard HOLE-closure; guards BEFORE the rename)."
predecessor_handoff: handoffs/2026-06-15-E.0.10-shipped-next-E1-handoff.md (superseded — its NEXT ACTION "open .E.1" is DONE: .E.1 decomposed + the tree built this session)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (latest D-226; the .E.1 planning arc = D-223..226)
register: plans/v5.15-live-readiness/plan_checks/E.0.10-finding-disposition-register.md (the .E.1-homed items now have sub-ship owners — see its § .E.1 DECOMPOSITION)
plan_body: plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E.1-foundation.md (the .E.1 SUB-MASTER — read its § DECOMPOSITION; the 7 leaf scaffolds are its children)
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, the .E.1 sub-master § DECOMPOSITION, the E.1.0 scaffold, decision-log D-223..226, the plan-decomposition + plan-hierarchy specs, the guard-matrix §4d, regen CODE_MAP at pickup]
engine_head: 572c978 (.E.0.10 ship-close — UNCHANGED this session; NO engine code — all planning/apparatus)
workspace_head: UNCOMMITTED .E.1-planning artifacts pending /sync-workspace (2 new specs + the arm-subagents memory + the vocab/sister edits + the 7 scaffolds + the sub-master/DAG/MASTER/register/guard-matrix updates + D-223..226 + the 5 D/A decomposition reports + this handoff)
coding_status: PLANNING PHASE COMPLETE — .E.1 decomposed (N=7, D-225), apparatus codified (D-223..226), the tree built (sub-master + 7 scaffolds + DAG/matrix/register/MASTER); doc-CI green. NO engine code. NEXT = re-ground + deep-gate + CODE E.1.0.
defers: "E.1.1–E.1.6 are SCAFFOLDS (full bodies expanded per-dive, rolling-window). Homed-open (in the sub-master § Homed-open): TD-162 homing (E.1.4 vs E.1.6), MAX_EXECUTION_NODES vs MAX_NODES_PER_CLUSTER naming, TECH_DEBT-189 third-storage. Build-tier (D-226, FLAGGED not built): the /verify-implementation V-class skill (task #16) · emit_decisions_to_honor.py (PLANNED) · the tabula-rasa-template extraction + /precoding-audit-gate enforcement. TD-203 → v5.16."
---

# Handoff — `.E.1` DECOMPOSED (planning phase complete) → next = CODE `E.1.0`

## TL;DR (the 60-second pickup)

- **`.E.1` is decomposed.** The v0.1 plan was too big for one ship → it is now a **SUB-MASTER (umbrella)** over **7 leaves** (E.1.0–E.1.6; locked N=7, **D-225**): mechanical foundation **by-layer** (E.1.0 guards / E.1.1 rename / E.1.2 layout) → capital superstructure **by-spine** (E.1.3 coherence / E.1.4 fill / E.1.5 purity) → framework last (E.1.6). **HARD live-enable = E.1.3 ∧ E.1.5.**
- **The apparatus got codified** (**D-223..226**): the `plan-decomposition-and-future-aware-agent-arming` + `plan-hierarchy-and-sub-master-decomposition` DESIGN_SPECS · the **I/A/V/D/C** agent taxonomy + the **tabula-rasa fan-out template** (methodology spec §6) · the **C-class decision-currency sweep**. Reusable beyond `.E.1`.
- **The tree is built + mechanically clean:** the sub-master (§ DECOMPOSITION) + the 7 scaffolds + the DAG (§ `.E.1` decomposition) + MASTER + register + guard-matrix all updated; `check_session_docs.sh` GREEN.
- **NEXT = re-ground + deep-gate + CODE `E.1.0`** — the §4d guard HOLE-closure (H7/H10/H14/H20+H8 grep-CI + determinism gate + latency ratchet). **Guards BEFORE the rename** (two-foundations). This is the **first ENGINE code of `.E.1`.**
- **No engine code this session** (HEAD `572c978` unchanged — all planning, by design); the workspace holds the uncommitted artifacts → **run `/sync-workspace` to save.**

## The ideal way to start

1. **`/accept-handoff`** (no arg → resolves here). Regens CODE_MAP, verifies git (engine `572c978`), loads the baseline + nav-infra, routes the next action.
2. **Read the `.E.1` surface:** the sub-master § DECOMPOSITION (the N=7 map + the cross-ship seams + the **re-grounding corrections** caught at scaffold-time) + the **E.1.0 scaffold** + the guard-matrix §4d (the HOLE cluster E.1.0 closes).
3. **E.1.0 is the first leaf → C-class re-ground it** (the scaffold's file:line will have drifted — re-verify the hot-path files + the `LATENCY_BENCH`/`LATENCY_PROFILING` flags against HEAD) → expand to a full coding body → **deep-gate (LIGHT tier earned — it's CI/tooling, not engine logic)** → **CODE.**

## What this session did (the `.E.1` planning phase — NO engine code, by design)

1. **Cycle-1 `/precoding-audit-gate`** (readiness + trace-deps) on the v0.1 plan → architecture SOUND but **v0.1-stale + monolithic → DECOMPOSE** (caught the Knight-class H21 cfg-key rename + the `MAX_CORES`-phantom enumeration undercount).
2. **C-class decision-currency sweep** (all 223 `.E` decisions) → honored the locked set; caught 2 mis-builds (cluster-hierarchy-is-foundational per D-15/D-28; epoch-breaks-FREE per D-131) + stopped re-litigating the D-219 tombstone.
3. **D/A decomposition stress-test** (2 D-lenses by-layer/by-spine → 1 A-class adjudicator) → **locked N=7** (D-225), code-confirmed, refute-survived. The residual E.1.3→E.1.4 stub-boundary HOLE pinned (the scaffold carries it).
4. **Apparatus codified** (D-223/224/226): the 2 new specs · the agent taxonomy + tabula-rasa template · the C-class sweep · the TD-closure-per-leaf discipline.
5. **The tree built** (D-225): 3 cluster-agents (armed straight from the tabula-rasa template — the first dogfood) wrote the 7 scaffolds + dogfooded the C-class shape-match (caught the `:2045` predicate line, `PORTFOLIO_SNAPSHOT_VERSION 7→8`, the `tt::ExchangeAdapter` fn-pointer model, etc.); sub-master reshaped; DAG/MASTER/register/guard-matrix updated; doc-CI caught + I fixed 2 of my own slips (orphaned STATUS + one-way sisters).

## Arming for the next action (`E.1.0` — the surface kit)

- **Skills:** `/readiness` (re-ground + LIGHT-tier gate the E.1.0 body — it's guards/CI, lower-risk, so the full HIGH-RISK 5-agent gate is NOT needed) · `/latency-track` (the latency ratchet).
- **Tools to RUN:** `gen_code_map.sh` (regen) · the grep-CI patterns (H7 branchless / H14 no-bitfield / H20 SP-HP) · `check_determinism.sh` (the 2×run + ±`USE_NATIVE_128` gate) · `LATENCY_BENCH` (H8 budgets as a CI gate).
- **Specs:** the guard-matrix §1 enforcement-ladder + §2 verifiability-triad · `branchless-dispatch-discipline` (H20) · `avx512-byte-determinism-pattern` (H10) · the **E.1.0 scaffold**.
- **Anti-patterns not to reintroduce:** Class 28 (branchy SP/HP). **Invariants:** H7/H10/H14/H20/H8 (the HOLE cluster E.1.0 closes).
- **E.1.0 OUTBOUND seam** (what E.1.1 relies on): a STANDING green CI net that RED-builds any later hot-path branch / bitfield / non-byte-identical SIMD / latency regression / determinism break — the rename then runs UNDER it.

## Homed-deferred (do NOT silently drop)

- **Build-tier (D-226 — flagged, not built):** `/verify-implementation` V-class skill (task #16) · `emit_decisions_to_honor.py` (PLANNED) · extract the tabula-rasa template to standalone + enforce in `/precoding-audit-gate`'s spawn.
- **Homed-open (resolve at the named dive — in the sub-master § Homed-open):** TD-162 homing (E.1.4 vs E.1.6) · `MAX_EXECUTION_NODES` vs `MAX_NODES_PER_CLUSTER` naming · TECH_DEBT-189 third-storage convergence.
- **TD-203** (Phase-D gate removal) stays → v5.16.

## Operator collaboration reminders (every-turn)

- Address **Caramel as Caramel / she / her**. No AskUserQuestion modal boxes — inline options.
- Evaluate options on **robustness + latency + design philosophy, NOT time**.
- **Consult before coding** — E.1.0 is lower-risk but still capital-foundation; present the re-grounded body + the gate verdict before coding.
- **The scaffolds are SCAFFOLDS** — each leaf gets a C-class re-ground to a full body at its dive (the file:line WILL have drifted; the sub-master § "Re-grounding corrections" lists the known ones). Verify against current code — don't inherit the scaffold's anchors.
