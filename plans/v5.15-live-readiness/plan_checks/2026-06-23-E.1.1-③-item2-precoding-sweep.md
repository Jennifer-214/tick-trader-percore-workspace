---
type: plan-check
subtype: precoding-sweep
ship_tag: v5.15.5.F.4d.1.E.1.1
workstream: "③ config-compiler item 2 (MIGRATE the 4 capital globals) — I→A pre-coding sweep + orchestrator cross-check"
date: 2026-06-23
head_at_sweep: c6967be
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md  # D-255 governs; D-256 (this sweep's resolution) PENDING
design_ssot: plans/v5.15-live-readiness/plan_checks/2026-06-22-E.1.1-config-compiler-IA-cascade-synthesis.md
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-06-22-E.1.1-③-item2-migrate-handoff.md
agents: [i-class (map+measure), a-class (behavior-equivalence refuter), a-class (determinism/registry-fit refuter)]
status: LIVING — D1+B1 BLESSED + scope-correction CONFIRMED (operator 2026-06-23, D-256); forward-seam pass + per-node-parser re-vet BOTH COMPLETE; item-2 GREEN-to-code with 7 pre-coding pins (§per-node-parser re-vet); loss-cap + barrier-units HOMED to item-4
---

# ③ item 2 — pre-coding I→A sweep (the MIGRATE shape, before the diff)

A 1-I → 2-A independent cascade (default-refuted a-class, blind to the i-class for independence) on item 2 of the D-255 converged scope: MIGRATE the 4 non-registry capital globals into `FOREACH_GLOBAL_CFG_FIELD`. Orchestrator cross-checked all three (AR-11) — **no factual agent-vs-agent disagreement**; the `sizeof` claim was MEASURED independently by 2 agents (identical offsets).

## The 4 fields (re-grounded @ c6967be; defaults VERIFIED from code)

| Field | decl | parse | default site | current default | target row |
|---|---|---|---|---|---|
| `starting_balance` | `ControllerConfig.hpp:366` | `CFG_PARSE_MONEY` `:2267` | `:1552` | **1000000.0** (the `:366` comment "10000" is STALE) | `X(Money, KIND_DOUBLE, …, DBL(1000000.0, 0.0, hi))` |
| `min_sl_tp_ratio` | `:428` | `CFG_PARSE_MONEY` `:2278` | `:1591` | 0.5 | `X(Money, KIND_DOUBLE, …, DBL(0.5, 0.0, hi))` |
| `min_kill_loss` | `:1030` | `CFG_PARSE_MONEY_POS` `:2332` | `:1894` | 5.0 | `X(Money, KIND_DOUBLE, …, DBL(5.0, 0.0, hi))` |
| `min_hold_gain_pct` | `:450` | `CFG_PARSE_PCT` `:2460` | `:1609` | 0.001 **fraction** | `X(Money, KIND_DOUBLE_PCT, …, DBL(0.1, 0.0, 100.0))` ← percent-space (walker ÷100s → 0.001) |

Precedent correction: the handoff cites `fee_floor_mult:492` but that row is **PER_NODE**. The genuine GLOBAL decimal precedents are **`lazy_rebuild_price_threshold_pct:470`** (`KIND_DOUBLE_PCT`) + **`risk_pct:491`** (`KIND_DOUBLE_PCT` WITH `CAPITAL_BOUND_LOSS`, and its old manual `CFG_PARSE_PCT(risk_pct):2326` is already dead — the walker shadows it; proves the migration mechanism).

## Cross-checked findings (ranked)

### 🔴 F1 — the size-pin does NOT trip; the handoff's central HEADS-UP is backwards (MEASURED ×2)
All 4 are 16B `Money`; deleting 64B from the struct middle and appending 64B to the `EMIT_GLOBAL_CFG_STRUCT_FIELD` tail (which had ≥64B slack to the `alignas(64)` boundary) is **size-neutral → `sizeof(ControllerConfig<64>)` stays 53056 → `static_assert` at `:1331` does NOT fire.** But field OFFSETS shift (80/528/688/4544 → 52992/53008/53024/53040) → `Fingerprint_Compute` (raw-struct SHA, `BacktestPanels.hpp:3157` / `Fingerprint.hpp:180`) silently moves. The pin comment (`:1324-1334`) claims it guards layout-change → fingerprint-shift, but it only watches SIZE → it is **vacuous for size-preserving relocation**. A coder following the handoff's "let it RED-build the pin → regen" sees a GREEN build and ships a silent fingerprint divergence.
→ **Amendment:** regen the backtest fingerprint golden **EXPLICITLY**; do not rely on the pin. Coder re-measures `sizeof` post-actual-edit (the 2 sims appended all 4 at once; the −64/+64 arithmetic holds regardless of interleave, but confirm). **Only the backtest fingerprint golden is owed** — `fp_determinism_golden.txt` does NOT hash the cfg, and no field is `STAMP_BOUND`, so neither is owed. → D2.

### 🔴 F2 — bit-free rows do NOT close the founding bug; naive bit-tagging false-trips the future sweep
FATAL malformed-refuse (`CfgFieldDispatch.hpp:100-112`) is gated on `(CAPITAL_BOUND_LOSS|CAPITAL_BOUND_GAIN)`. The 4 fields aren't loss/gain %, and `CAPITAL_BOUND_DOLLAR` is deferred to E.1.6 (bit 15 is the last uint16 slot → adding one IS the widening). So migrating with bit-free `KIND_DOUBLE` rows gives the walker's WARN (`:90-94`) but **not** the boot-refuse.
- The 2 **dollar** fields' malformed close rides **D-255 item-3** (`>0`/sane): malformed→0, `starting_balance>0`/`min_kill_loss>0` faults on 0. Covered by an already-decided item.
- The 2 **ratio/gate** fields (`min_sl_tp_ratio`, `min_hold_gain_pct`) have no item-3 check → malformed→0 = feature silently disabled. **This is D1.**
→ **D1 (operator-decided: catch in ③, no tech debt).** Mechanism **B1** (recommended): decouple the FATAL malformed-refuse from the cap bit — lift `:100-112` to fire for any decimal-`Money` field (matching the WARN scope at `:90-94`); `CAPITAL_BOUND_LOSS/GAIN` then gates ONLY the out-of-range fraction-sweep. Restores **D-242**'s config-compiler intent (un-narrows D-247 for the malformed subset). Owes 1 verify (over-fire: does any current config tolerate a malformed `Money` value today). Alternatives rejected: hand-roll-the-2 (tech-debt, anti-prefer-migrate); pull DOLLAR-bit+widening forward (anti-defer).

### 🟠 F3 — the default literals are the binding silent-change risk (byte-equality PROVEN)
The 4 manual defaults + 4 manual parse branches must be **deleted in the same atomic commit** as each row-add (the walker default-fill `:1527` runs first; a leftover manual line silently wins; the walker parser-case `:2186` shadows the manual branch). `KIND_DOUBLE_PCT` ÷100 is structurally CORRECT (byte-identical to `CFG_PARSE_PCT`) — only the default literal is the trap (`min_hold_gain_pct` → `DBL(0.1)` percent-space, NOT `DBL(0.001)`).

### 🟠 F4 — no binding clamps on the open-ended dollar fields
A `DBL(lo,hi)` on `starting_balance`/`min_kill_loss` makes the GUI `SliderFloat` lie + pre-empts item-3's FAULT-not-clamp with a silent cap. Migrate for malformed-capture only; defer range validation to item-3 + E.1.6. `min_kill_loss` `clamp_min=0.0` reproduces the old `_POS` `<0→0`; the real guard is item-3's `>0`. CAVEAT: a wide `clamp_max` literal itself routes through `money_from_double_payload` (`Dispatch:117-118`) → verify it saturates harmlessly (`llround(1e15·1e8)=1e23` > Money ~9.2e18; the saturation guard at `FixedPointN.hpp:1897` should catch it — confirm).

### 🟢 F5 — cleanup + confirmed-safe (no surprises)
- After migrate, `CFG_PARSE_MONEY_POS` becomes fully dead (`min_kill_loss` is its sole user) → DELETE the macro (H21/TD-215). The other macros keep users.
- All consumers are **by-name** (relocation-safe), incl. the `&field` persist at `PortfolioController.hpp:2075` (`&`-of-field, not offset). Whole-struct copies are value-copies (order-irrelevant).
- `min_hold_gain_pct` GUI is **NOT** a duplicate: `SettingsPanel.hpp:645` is the per-NODE override grid (`per_node_fields[]`), orthogonal to the new GLOBAL registry render (i-class resolved a-class#1's flag). 3 of 4 gain a new (desirable) global Settings render.
- H15/H16/H18/H19 registry discipline SAFE (`FOREACH_GLOBAL_CFG_FIELD` already enrolled `MetaRegistry.hpp:39`); STAMP_BOUND correctly omitted; in-range parse byte-identical (both `Money_FromString`).

## Conformance to the disciplines (the migration direction)
H17 ✅ (auto-gen from the registry) · H4 ✅ (stays Money) · H9/H12 ✅ (golden regen for the offset shift) · H21 ✅ (cfg key names unchanged = no wire-slot recycle; dead-macro removal is H21-clean) · H22 ✅ (globals; per-node purity + override path untouched) · structural-fix-preferred ✅ (migrate > hand-roll-then-delete, D-255 hooks-open) · merit-based-defer ✅ (CAPITAL_BOUND_DOLLAR→E.1.6 is consumer-driven). The only discipline-brush points are D1 (scope re-bless) + D2 (guard strengthening) — both operator calls.

## Dispositions
- **D1 — DECIDED (operator 2026-06-23): catch the 2 ratio/gate fields in ③, no tech debt.** Mechanism B1 RECOMMENDED, pending operator scope-nod (all-Money malformed-refuse = un-narrow D-247) + the over-fire verify.
- **D2 — PENDING operator.** (i) regen backtest fingerprint golden = mandatory/mechanical. (ii) strengthen the size-pin → layout/offset hash = optional structural hardening (matches the struct-guard family); recommend do-now per no-tech-debt stance, but the determinism gate is the real net.

## Forward-seam coverage pass (2026-06-23; 3 a-class FS-1/2/3 — the anti-treadmill input-space enumeration)

Operator-directed (the "+1 every sweep = we forgot to account through E.X" insight). Enumerated ALL forward consumers of ③'s validation design in ONE exhaustive pass to STOP the +1 treadmill (per `feedback_iteration_spiral_signals_audit_meta_gap` + `feedback_dont_generalize_substrate_before_input_space_known`). Cross-checked (AR-11): NO inter-agent factual disagreement; FS-1+FS-2 CONVERGE on the decisive finding.

### Coverage matrix
| Consumer | Verdict | Note |
|---|---|---|
| E.1.2 (SoA/Money relayout) | SERVES | size-pin vacuity + fingerprint-regen homed to E.1.2; layout clean (E.1.2 SoAs `Portfolio`/`Position`, a DIFFERENT struct). PROSE-FIX owed (below). |
| E.1.3 (aggregator / single_core cleanup) | SERVES | all 4 globals stay live on the sharded path (no wasted validation); dead `kill_switch_*` (TD-220) orthogonal (value-validation vs consumer-rewire). |
| E.1.5 (per-cluster purity) | **HOLE → SERVES iff item-2 restored** | THE decisive finding (below). |
| E.1.6 (multi-exchange / ENUM_STRICT) | SERVES | bit-free migrate = clean 1-token DOLLAR-add; B1 composes with ENUM_STRICT; **B1 RELIEVES widening pressure** (consumes 0 bits → ENUM_STRICT is correctly the first widen-forcer). Class-51 widening trap (uint16 reductions + `cfg_compute_mask<uint16_t Bit>` must widen SAME-commit) = E.1.6's, homed. |
| E.2 (decoupled compiler) | SERVES + 1 hole | the FINDING-7 orphan-homings are ALREADY WRITTEN (D-255 folds landed at c6967be — AR-12 CLOSED). New hole = the headless re-read gate (below). |
| E.5 (cluster Σ) | SERVES | exact per-field ≤100%/≤1000% + `starting_balance>0` denominator E.5's Σ builds on; H22 split correct; boot-Σ fully homed (not submit-only). E.5-internal: paste-not-assert its "5 dollar-floor sites" count (Class-33). |
| D-206 (exit ratchet) | SERVES | MULT-loss hole real (`sl_trail_mult` stddev-multiplier) but ALREADY homed (D-206:52 runtime guard) + the "by-construction" over-claim explicitly disclaimed. |

### 🔴 DECISIVE FINDING (FS-1 + FS-2 convergent; confirmed vs the decision log) — item-2 was UNDER-SCOPED vs D-255
The handoff framed item-2 = "MIGRATE the 4 GLOBALS." But **D-255 item-2** (`decision-log:1654`) = *"MIGRATE the 4 globals **+ route the per-node overrides through the walker** (malformed-capture by construction)"* and **D-255 D1** (`:1630`) = the legacy-array branches (`node_N_risk_pct:2893`/`node_N_max_drawdown_pct:2904`) BYPASS the walker = loss-side **H22 break** → *"hand-roll their loss-cap in ③ NOW (don't wait on WIP2g)."* The handoff DROPPED the per-node-override clause + re-deferred the legacy arrays to WIP2g.
- The per-node MALFORMED case (`core_0_stop_loss_pct=banana`→0→SL off) falls BETWEEN the handoff's item-2 (globals only) and item-4 (≤100% caps — which `0` PASSES) → **uncaught**. At HEAD the `node_N_*` indexed keys fall through the walkers (`strcmp` matches the FLAT key `"risk_pct"`, not `"node_0_risk_pct"`) into the flag-discarding hand-rolled branches (`ControllerConfig.hpp:2893-2910` + `_PARSE_OV_PCT:2985`).
- **E.1.5's purity premise UNMET** under the dropped scope: its A34 governor + the HARD live-enable gate (E.1.3 ∧ E.1.5) would compute on unvalidated per-node capital (`node_0_risk_pct=999`→999% alloc, `Run.hpp:924`, code-verified). Shipping item-2 as 4-globals-only = a **false-completeness ship on the per-node surface** (the founding bug class ③ exists to kill, surviving into HARD-live-enable).
- **CORRECTED item-2 scope (= D-255's actual item-2):** (a) migrate the 4 globals; (b) route the per-node OVERRIDES (`_PARSE_OV_PCT`, idiom-3) through the walker → malformed-capture by construction; (c) hand-roll the 2 legacy-ARRAY branches' (`node_N_risk_pct`/`_max_drawdown_pct`, idiom-2) flag-capture + loss-cap NOW (D-255 D1 — not WIP2g; less work + not blocked by it); (d) fold the 7 `strstr` branches into the exact-suffix dispatch at `:2975` (closes the `node_0_risk_pct_typo`→slot-0 substring under-fire + de-vacuous-es E.1.5's H22 CI row). B1 (operator-blessed 2026-06-23) makes (b)/(c)'s malformed-capture unit-agnostic.

### Forward-write obligations (FOLD into destination BODIES — AR-12: write, don't point)
- **E.2** (🟠): the headless SIGUSR1/UDS config re-read is a 2nd cfg-poisoning re-entry; ③ gates only the GUI `Async.hpp:321` path (item-5). WRITE into E.2's body (Phase E.2 `:764` + INBOUND `:1038`): the re-read MUST gate on `cfg_compile_ok(new_cfg)` + keep-old (the capital fault bitmap, not just cross-ref resolvability). NOT ③'s job — E.2's construct.
- **E.1.2** (🟢 prose-fix): the E.1.2 fold `:87` + E.1.5 fold `:80` say `cfg_load_fault_flags` "fingerprint-EXCLUDED OR guaranteed-0" — EXCLUSION is IMPOSSIBLE (`Fingerprint_Compute` is `void*`+sizeof; `ControllerConfig.hpp:1328` admits it). It's "guaranteed-0" ONLY, and FRAGILE (every fingerprint call site must be downstream of the abort gate). Correct both folds: drop "excluded"; add `assert(cfg.cfg_load_fault_flags==0)` at `BacktestPanels.hpp:3157`. (= the D2 strengthen-the-guard surface; folds with E.1.2's F-I `static_assert`.)
- **E.5** (🟢): paste-not-assert the "5 dollar-floor sites" count when E.5 codes (Class-33 on E.5's own enumeration).

### Inflection: HIT
The pass enumerated the ENTIRE forward roadmap (E.1.2/1.3/1.5/1.6/2/5 + D-206) — no un-enumerated forward consumer remains. Findings are BOUNDED (1 item-2 scope-restoration + 3 destination-fold obligations + confirmations); a re-run finds nothing new because the forward input space is now fully enumerated. The +1 treadmill stops here. The design is SOUND; the only correction is restoring item-2 to its DECIDED (D-255) scope.

### Implementation-state (HEAD c6967be — all forward, consistent with mid-build)
B1 NOT yet decoupled (the malformed-FATAL at `CfgFieldDispatch.hpp:100-103` still cap-bit-gated); the 4 globals still hand-rolled (`:2267/2278/2332/2460`); the item-4 out-of-range sweep uncoded (`CFG_FAULT_CAPITAL_OUT_OF_RANGE` zero setters). All = the forward item-2..4 work.

## Per-node-parser re-vet (2026-06-23; 1-I→2-A on the corrected/expanded shape — recascade-on-corrected-shape)

The forward-seam pass EXPANDED item-2 into the per-node PARSER (route the overrides + hand-roll the legacy arrays + the 7-strstr→exact-suffix fold) — a surface no prior gate vetted as it'll be coded. Fired a focused 1-I→2-A (i-class map + sentinel/Class-48 refuter + behavior-change refuter). CONVERGENT — no AR-11 conflict. **Verdict: GREEN-to-code with 7 pins.** It caught 3 things coding-from-D-256(b)-literally would have shipped (the winsor clamp import, a vacuous sentinel test, the item-2/item-4 loss-cap boundary).

**The reconciliation:** D-255/D-256(b)'s "route the per-node overrides through the walker" was LOOSE SHORTHAND. Coded literally (calling `cfg_parse_field`) it imports the walker's UNCONDITIONAL clamp (`CfgFieldDispatch.hpp:117-120`; the malformed-capture `:100-112` and the clamp are sequential with NO early-exit → both or neither) → floors `winsor_pct_high`'s `0`=inherit→`0.5` (`clamp_min=0.5`, `CfgFieldRegistry.hpp:611`; RAW override `ControllerConfig.hpp:161`; STAMP_BOUND) = a live Class-48. PCT fields are inert (all `clamp_min=0.0`); ~10 RAW fields have no descriptor (route-infeasible). → the **in-macro parse-point flag-capture** seam (the D-254-ADDENDUM literal) is the ONLY whole-cohort-correct shape, and it preserves `0`=inherit for free (capture flags, never clamp).

**The 7 pre-coding pins:**
1. **In-macro flag-capture** (`Money_FromString().flags`→`cfg.cfg_load_fault_flags` inside `_PARSE_OV_PCT`/`_PARSE_OV_RAW`); **NEVER call `cfg_parse_field` for overrides** (its clamp bites `winsor_pct_high`).
2. **item-2 = MALFORMED-capture ONLY**; the ≤100% loss-cap is item-4 (no `barrier_is_corrupt`/`100.0`/`1.0` literal in item-2's parser — that's the Class-43). Resolves D-255 D1's "hand-roll the loss-cap now" = the malformed-half now.
3. **One shared `CFG_FAULT_CAPITAL_MALFORMED` bit, same `cfg_load_fault_flags`** (no parallel-mirror → avoids Class-18 drift; matches the walker). The `_PARSE_OV_PCT` override macro is the LARGER hole (covers `take_profit_pct`/`stop_loss_pct`/`ml_tp_pct`/`ml_sl_pct`/…); the 2 legacy arrays are the smaller hand-roll.
4. **List-C non-vacuous sentinel test:** `core_N_winsor_pct_high=0`→inherit (NOT 0.5) — the planned `stop_loss_pct=0` test is VACUOUS (`clamp_min=0.0` passes even under buggy routing). + strstr-fold contract tests: `node_0_risk_pct_typo`→FAULT, `node_0_my_strategy`→fall-through.
5. **`parse_double_fast_checked` is net-new** (RAW channel: `atof` + `parse_double_fast` both swallow malformed→0 = silent `0`=inherit collision). Empty-string = clean inherit (not malformed — avoid over-fire).
6. **Preserve `node_strategies_explicit_set |= 1<<idx`** (`ControllerConfig.hpp:2962`) in the moved `_strategy` exact-suffix branch + **delete the 7 strstr branches in the SAME commit** as the fold (a leftover shadows via substring).
7. **Determinism: parse-path-only** — byte-identical stored value for valid input (no golden shift beyond item-2's migrate-relocation); the fault sink is untouched on clean parse.

**HOMED to item-4:** A2 refuted "loss-cap via `tt::barrier_is_corrupt`" (domain/units). Part is the decided design (use its CONSTANTS on the resolved fraction, NOT the pair-predicate — synthesis `:167`); the "wrong units" sub-claim appears to conflate the stored fraction (`0.20`) with the registry percent `clamp_max` (`100.0`) — the decided FRACTION-space validation vs `BARRIER_SANE_MAX_SL=1.0` (also a fraction) IS consistent. Settle by code-read at item-4's pre-coding; does NOT block item-2.

**Alternative noted (NOT taken):** A2's "give the 2 legacy arrays real descriptors + route through `cfg_parse_field`" (D-254-step-3 pattern; safe since `clamp_min=0.0`) is the cleaner end-state but pulls WIP2g forward — D-255 D1 decided hand-roll-now (respects the WIP2g deferral). The hand-roll's Class-18-drift risk is mitigated by writing the SAME shared fault bit (pin 3).

## Verifies owed before coding item 2
1. **B1 over-fire** — ✅ SATISFIED (FS-3 inline check: `engine.cfg`'s `Money` values all well-formed; no current config newly-aborts under B1).
2. **Post-edit `sizeof` re-measure** — `clang -Xclang -fdump-record-layouts` after the actual interleaved row placement, confirm 53056 (and that the pin therefore stays green → golden regen is manual, not pin-forced).
3. **Per-node parser** — after wiring (b)/(c)/(d): confirm `node_0_stop_loss_pct=banana`→ABORT + `node_0_risk_pct_typo`→ABORT (the exact-suffix fix) + `core_0_stop_loss_pct=0`→clean (sentinel preserved).

## Tech-debt closures / folds (this surface)
- **TD-212** (vacuous-percent-clamp — the ③ founding finding): the CLASS ③ closes. Closes structurally at ③ completion (malformed + out-of-range + recurrence guard; B1 extends the malformed half to all numeric cfg).
- **TD-215** (dead capital parse-branches, `in-flight`): item-2 SUBSUMES the 4-field slice (migrate → walker-shadowed-dead → delete + the sole-user-dead `CFG_PARSE_MONEY_POS` macro, same atomic commits); the ~24 registry-field branches stay the bulk ③/D-253 delete. **RE-PARTITION captured in the ledger 2026-06-23.**
- **TD-216** (Check-K blind to ControllerConfig fingerprint / the sizeof pin): D2 FOLDED here (NOT a new TD) — the pin is vacuous for size-preserving relocation; item-2 = first instance. **Ledger sharpening captured 2026-06-23.** Strengthen-now vs accept-net = pending operator (D2).
- ADJACENT (tracked, homed elsewhere — NOT subsumed): TD-186 (Σ→E.5) · TD-217 (serve-time fingerprint→live-readiness) · TD-220 (dead `kill_switch_*` Class-47→E.1.3).

## Capture manifest (remaining, once D1-scope + D2 locked)
- **D-256** in the decision log — the item-2 sweep resolution (B1 malformed-decouple + the size-pin-vacuity correction of the handoff's workflow + D2). Supersedes the handoff's "trips the pin" framing.
- **Synthesis SSoT REMAINING-③ update** + the stale findings-doc CONVERGED-PLAN item-2 prose (still says "hand-roll"; D-255 = MIGRATE; now also the corrected workflow).
