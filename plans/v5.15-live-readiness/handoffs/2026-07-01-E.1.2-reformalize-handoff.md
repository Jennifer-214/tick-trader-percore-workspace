---
type: handoff
status: active   # active = THE one live handoff (no-arg /accept-handoff resolves here). Supersedes 2026-06-30-E.1.2-pickup-handoff.md (the design+validation work landed + captured this session).
ship_tag: E.1.2-nodestate-soa-layout
plan_type: refactor
sprint_end_goal: v5.15 live-readiness — make the codebase live-ready + more maintainable
ship_end_goal: "E.1.2 — open Position/NodeState to the successor SoA layout ONCE (the determinism-gated wire/layout freeze): the D-206 peak + owner_node_id reserve, registry-drive the NodeContext persist-serializer via COMPOSE-SUB-REGISTRIES, delete the dead serializers, Money-ize the residual feature-typed money fields. The successor-foundation leaf the rest of .E builds on."
predecessor_handoff: 2026-06-30-E.1.2-pickup-handoff.md
engine_head: 0ee227a
workspace_head: "at or after 2d27e29 (this close advances it via /sync-workspace)"
branch: feat/v5.15-live-readiness
deletion_scope: major   # E.1.2 deletes the dead Portfolio_Save/Load + PortfolioController_*Snapshot serializers (D-289; capital-persistence). /accept-handoff Stage 4 auto-fires /blindspot-scan B14. Mechanics = ledger/SOURCES lockstep (check_identifier_retirement.py:57-59 + tools/identifier_ledger.txt:55/57) + PRESERVE the "TICK" refuse-magic (ShardedSnapshot_Load:329-333).
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, MEMORY_EXTENDED.md, "plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md", "plans/v5.15-live-readiness/plan_checks/2026-06-30-E.1.2-per-field-declaration-table.md", "plans/v5.15-live-readiness/plan_checks/2026-06-30-E-umbrella-hotpath-forward-audit-synthesis.md", "plans/v5.15-live-readiness/subplans/2026-06-15-v5.15.5.F.4d.1.E.1.3-aggregator-coherence.md", "plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md", "plans/v5.15-live-readiness/MASTER.md", "DESIGN_SPECS/data-disciplines/per-node-purity-scale-invariance.md", "DESIGN_SPECS/refactor-patterns/decision-time-data-binding-pattern.md"]
coding_status: planning-incomplete   # the E.1.2 subplan is a SCAFFOLD (status:scaffold). FIRST action = REFORMALIZE it into a coding plan. Do NOT code against the scaffold body.
---

# E.1.2 reformalize handoff — NodeState SoA / layout freeze (successor foundation)

**Created:** 2026-07-01
**Target ship:** E.1.2 — `subplans/2026-06-15-v5.15.5.F.4d.1.E.1.2-nodestate-soa-layout.md` (the layout/wire freeze leaf; successor-foundation #6)
**Branch:** feat/v5.15-live-readiness
**Pre-tag rollback anchor:** `v5.15.5.F.4d.1.E.1.1` (E.1.1 shipped @ engine `0ee227a`, pushed)
**Engine HEAD at write:** `0ee227a` · **Version.hpp:** `5.15.5.F.4d.1.E.1.1` · **Workspace HEAD:** `2d27e29` (+ this session's edits, `/sync-workspace` pending)
**Working tree:** engine CLEAN @`0ee227a` (untracked-only, all benign: `?? --help`, `?? build_clangd/`, `?? .clang-format` [the task-#8 `DisableFormat` config — stops nvim clang-format-on-save reflowing the hand-laid style; not committed, operator's call]); workspace edits committed this close (`/sync-workspace`).
**Verified at write:** `check_session_docs.sh` SWEEP CLEAN (all HARD) + an INDEPENDENT Stage-5.5 deliverable review returned CLEAN (all 12 artifacts landed + coherent + neighbors intact + private/public boundary uncrossed).

> **This is a CLEAN close after a DESIGN + CODIFICATION session, not a mid-coding checkpoint.** No E.1.2 CODE was written (engine unchanged `@0ee227a`). The session's product: the cycle-1 codification landed, Fight #4 fully investigated (3-wide I→A) + homed to E.1.3, the deletion + tools-over-grep decided, and the E.1.2 reformalize INPUTS assembled + verified. The pickup's job is to REFORMALIZE the scaffold → gate → code.

---

## What landed this session (the design/codification that de-risks E.1.2)

All doc-CI CLEAN + Stage-5.5 independent-review CLEAN. Decisions **D-287..D-290** (2026-07-01) in `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`.

- **D-287 — cycle-1 codification LANDED** (closes D-282's slate; the 3:3:3 sweep refined it to the MIDDLE-PATH): H22 spec (`per-node-purity-scale-invariance.md`) new **"Cross-tier ownership + field categories"** section — forward-framed to the E.1.3 aggregator, anchored to the live `realized_pnl` co-write (`ControllerEventLoop.hpp:1948-1959`), the 4 facets (replica/derived/pipeline-staged/capability-vs-config) as POINTERS to Class 24/27/43 + H6 + decision-time-binding (NOT restatements — the Class-47 avoidance); + `decision-time-data-binding-pattern.md` **v1.4** replica-coherence amendment (torn-read-safe "same authoritative input via the same resolver"; Fight #4 canonical); + memory `feedback_synthesis_needs_its_own_adversarial_pass` (in MEMORY_EXTENDED); + bidirectional sisters. NO H-row, NO CLAUDE.md edit.
- **D-288 — Fight #4 (`live_tp`/`live_sl` decision-anchor) dispositioned** (a 3-wide I→A, 6 agents): a REAL bug (confirmed at the E.1.3 seam per AR-11), config-scoped, INERT at `slippage_pct=0` / bites at configured-slippage + live. FIX = a **branchless seqlock-mask-select re-arm** of the hot fields (NOT `sg_*`-reuse — the a-class PROVED that BREAKS: drifts the band every rebuild + re-opens the v5.5.5 postmortem) + the **A25-mirrored `per_fill_sl`** owner leg (reuse `ResolvePerFillSlPct`; grows `OrderPreResolved` 48→64B but ZERO wire coupling — SPSC-only, not persisted, tool-verified). HOME = folded INTO the E.1.3 subplan §"Known folds it owns" (the AR-12 gap A-γ found — it was only a pointer). Interim backtest caveat: run `slippage_pct=0` until E.1.3.
- **D-289 — delete both dead Position serializers** (`Portfolio_Save/Load` + `PortfolioController_*Snapshot`/CONTROLLER=14) — operator: DELETE, not tombstone-in-place. Lands at E.1.2. Mechanics in the frontmatter `deletion_scope`.
- **D-290 — tools-over-grep discipline**: SUBAGENT_ARMING §3 GENERAL "SEARCH `TOOLS.md` before you grep, prefer the tool" rule (categorical, not a curated list) + accept-handoff Stage 3.6 + the sister memory.
- **TECH_DEBT-227 opened** (`partner_pending_pnl` not persisted → mid-partial-exit W/L miscount; → E.1.4 OR the E.1.2 serializer registry-drive closes it). **TD-168 moved open→closed** (it was CLOSED-in-`.E.0.10` but stale-open; it is the A1 SIBLING of Fight #4 — DIFFERENT root, don't conflate).

---

## RESUME PATH (ordered — the pickup's actual work)

1. **`/accept-handoff <this doc>`** — loads everything explicitly (the arming was hardened D-290; the pickup loads the specs/nav-infra/tools without being asked). Regens CODE_MAP at pickup.
2. **REFORMALIZE the E.1.2 scaffold** (`subplans/2026-06-15-…E.1.2-nodestate-soa-layout.md`, `status:scaffold`) into a coding plan, from the **amended spec (D-283)** + the **per-field declaration table** (`plan_checks/2026-06-30-E.1.2-per-field-declaration-table.md`), using the future-oriented plan template. **CARRY the session-verified inputs (§ below).** Do NOT code against the scaffold body.
3. **RUN THE CASCADE TOOLS FIRST** — `./tools/gen_code_map.sh --composition "Position<64>"` + `--byte-context "Position<64>"` (mandatory per CoreFrameworks surface rule + the just-codified SUBAGENT_ARMING §3). This session ran them once: `--composition` = ZERO transitive containers (the 192B freeze is CONTAINED); `--byte-context` = the 2 size-pins (`Portfolio.hpp:117/143`) + the live `ShardedSnapshot` dump + the dead `Portfolio_Save` (`:560-564`, being deleted). Re-run at the dive to re-ground.
4. **`/precoding-audit-gate`** against the reformalized plan (HIGH-RISK: determinism-gated wire/layout freeze on capital state → 5-agent tier + `/blindspot-scan` B14 for the deletion). Then **consult Caramel** (standing rule: present findings, do NOT auto-proceed).
5. **Code E.1.2.** Open Position ONCE. Golden-master determinism guard (`±USE_NATIVE_128` byte-compare — NOT a sizeof-only assert). Then `/ship`.

---

## The reformalize INPUTS (session-verified — carry ALL into the plan body)

**Currency fixes (the scaffold body is STALE on these — re-ground at HEAD `0ee227a`):**
- The NodeContext hand-`fwrite` loop is **`ShardedSnapshotPersist.hpp:172-261` save / `:436-499` load**, NOT the scaffold's `:176-233` (that under-cites — building against it drops `:234-260` = a silent-zero-on-restore / D-110).
- The F-096 leg-split path is **`CoreFrameworks/EngineSharded/Async.hpp:842-852`** (add the `EngineSharded/`), and it is **STILL `double`** — the scaffold's "already Money-ized" is FALSE. Money-ize it in the relayout (TD-167).
- Snapshot versions at HEAD: `PORTFOLIO_SNAPSHOT_VERSION 7` (→8), `SHARDED_SNAPSHOT_VERSION 10u` (→11), `CONTROLLER_SNAPSHOT_VERSION 14` (delete, D-289). The Portfolio.hpp inline comments still say "=6" (Ship-A vintage, stale) — sweep them at the bump; and the `ShardedSnapshotPersist.hpp:9-11/25/330-331` "legacy v11" comments too.

**The serializer SHAPE (load-bearing — per I3/A3):** registry-drive the NodeContext persist-serializer as a **COMPOSE-SUB-REGISTRIES** driver — a thin loop that delegates to `regime_state` + `pnl_feeder` + `confidence` sub-walkers (confidence ALREADY is one: `FOREACH_CONFIDENCE_PERSIST_FIELD`) — **NOT** a flat `FOREACH_NODE_PERSIST_FIELD` "mirroring OMS" (OMS is flat `{DIRECT,BIT}` only; NodeContext needs nested/array/delegate). This ALSO structurally closes **TECH_DEBT-196** (the field-count-lock gap). Do NOT introduce a `SERIALIZER_GENERATED` meta-registry flag (grep-zero; adding it triggers the TD-057 schema cascade) — keep it Stage-2.

**The deletion (D-289):** ONE commit each, or the guard `sys.exit`-crashes — delete `#define` + dead fns + the ledger row (`identifier_ledger.txt:55/57`) + the tool SOURCES row (`check_identifier_retirement.py:57-59`) + `--update`; PRESERVE the `"TICK"` refuse-magic (`ShardedSnapshot_Load:329-333`, the one live consumer). Position `--composition` = zero transitive containers → the freeze is contained.

**Reserve, don't solve (per D-283 + the sweep):** the #3 Fight-#4 fix needs at most a FREE `NodeState.hot` reserve-note (a fill-confirm flag + re-sync channel — but `ExecutionCore`/`GateParameters` aren't wire-serialized, so ZERO hard E.1.2 coupling; it lands in E.1.3, not here). RESERVE the E.1.3 slots (AggregatorState `Money` fields, the `intended_*` seqlock pack, the fill-record owner) + RESERVE-NOT-DELETE `NodeState.hot.mode` (D-284). The E.1.3/E.1.4 **seam is SETTLED**: E.1.3 owns the aggregator/torn-read/kill-hierarchy (D-225 lock; the synthesis `:64` "open decision" was corrected to RESOLVED this session).

**Additional sweep sub-findings to FOLD at the reformalize (exhaustive-capture 2026-07-01 — the sub-findings UNDER the D-287..D-290 headlines):**
- **Delete the dead `PositionExitGate`** (`Portfolio.hpp:477-514`, "HOT PATH"-bannered but ZERO live callers per I3; H21 remove-dead-code) while relaying out Portfolio — its deadness is what makes the `active_bitmap` single-writer validator clean (a re-wired PositionExitGate = a multi-writer trap).
- **Drop `node_dd_pct`'s redundant persist** (I2): recomputed each rebuild (`ControllerEventLoop.hpp:2909`) AND raw-persisted (`ShardedSnapshotPersist.hpp:213`) AND raw-loaded not-re-derived (`:556`), while its inputs (`node_peak_balance` + realized/alloc) are ALSO persisted → dead weight. The compose-sub-registries serializer marks it derived/recompute-on-load, NOT a persist row (same for every `ShardedSnapshot` per_node double, `:490-496`).
- **Drop `drawdown_max` (0 consumers) + `drawdown_current` (derivable)** from the relayout (synthesis frozen-layout item 6).
- **Snapshot-version-bump guard (TD-180):** E.1.2's version bumps (PORTFOLIO 7→8 / SHARDED 10→11) + Position 128→192B are EXACTLY where TD-180's "size-pin trips but nothing forces the VERSION bump" gap bites — wire the guard (or let the golden-master byte-compare cover it) so a field-add can't ship green without the version bump.
- **GUI-publish cadence comment drift (I2):** `ControllerEventLoop.hpp:195,630` (30Hz) vs `ShardedSnapshot.hpp:171,377`/`OrderManager.hpp:545,751` (60Hz) — reconcile in the stale-comment sweep alongside the `=6`/`v11` version comments. LOW.
- **`OrderEvent<F>` size-pin** → homed separately as **TECH_DEBT-228** (E.1.4).

---

## TaskList state at handoff write (recreate via TaskCreate at pickup)

| ID | Status | Subject |
|---|---|---|
| #6 | completed | Cycle-1 codification — 3:3:3 sweep → consult → fold (D-287) |
| #7 | completed | Fight #4 live_tp/live_sl decision-anchor — 3-wide I→A → E.1.3 (D-288) |
| #1 | **pending (NEXT)** | E.1.2 — reformalize the scaffold into a coding plan |
| #2 | pending | E.1.2 — code the NodeState SoA / Money relayout + Position 192B freeze (blocked by #1) |
| #5 | pending | #15 GAP-2 strategy-state OOB → folds into the E.1.2 union (blocked by #1) |
| #3 | pending | #11 boot-surface GUI config-error warn-banner (doable half; refuse-half BLOCKED on TD-223) |
| #4 | pending | #14 OQ1 — WIRE the inert sharded kill/exposure controls (own capital ship, D-273) |
| #8 | pending | fox-symdeps.nvim / `.clang-format` coexistence fix (post-close-out, cheap — root cause: no repo `.clang-format`) |

`#1` is the live next action. `#6`/`#7` are done. `#3`/`#4` remain queued (ORDERING: E.1.2 next — no #3 coupling verified; #14 is its own capital ship; #11's refuse-half is blocked).

---

## Arming for the next action (the reformalize → gate)

The next action is **reformalize E.1.2 (a determinism-gated capital-persistence wire/layout freeze) → `/precoding-audit-gate`**. Surface = persistence + layout + per-node state + a major deletion. Arm it:
- **Route to:** `/dod-audit` (DESIGN_SPECS layout-pattern + the compose-sub-registries serializer) + `/parity-check` (wire-format byte-preservation, serializer round-trip) + `/trace-deps` (the dead-serializer consumer enumeration) + `/blindspot-scan` B14 (deletion ordering) + `/accounting-audit` (the F-096 FPN→Money leg). HIGH-RISK → 5-agent tier. ARM each agent per M8 (docs + toolchain + scout-first; adversarial agent independent on the verdict).
- **Run (not just read) — SEARCH `TOOLS.md` first (D-290):** `gen_code_map.sh --composition/--byte-context "Position<64>"` (the mandatory cascade) · `check_struct_alignment.py` (Check-K size-pins) · `check_identifier_retirement.py` (the deletion ledger/SOURCES) · `./build.sh test` + golden-master byte-compare · `check_session_docs.sh`.
- **Read first:** `STRATEGY_AND_CODING_RULES.md` (H4/H9/H12/H21) + `wire-format-byte-preservation-discipline.md` + the H22 (`per-node-purity-scale-invariance.md`) + `decision-time-data-binding-pattern.md` v1.4 specs. Invariants in play: **H4 H6 H9 H12 H17 H21 H22**.

---

## Critical pickup-time reads (nav-infra + durable artifacts)

- **`plan_checks/2026-06-30-E.1.2-per-field-declaration-table.md`** — THE E.1.2 constraint-declaration (per-field owner/tier/thread/wire; the 9 "fights"). Build the reformalized plan from this.
- **`plan_checks/2026-06-30-E-umbrella-hotpath-forward-audit-synthesis.md`** — the synthesis (the WHY): the amended E.1.2 spec + the A-class refutations + the 3:3 invariant-validation verdict. Its `:64` seam-drift is now RESOLVED.
- **The E.1.2 subplan** (`subplans/2026-06-15-…E.1.2-nodestate-soa-layout.md`) — SCAFFOLD; supersede at reformalize.
- **The E.1.3 subplan** (`subplans/2026-06-15-…E.1.3-aggregator-coherence.md`) §"Known folds it owns" — the **Fight #4 fold-block** (D-288; the E.1.2 fix's home is HERE, not E.1.2).
- **`decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`** — D-282..D-290 (the .E decision SSoT).
- **`MASTER.md`** — sprint pipeline + the .E successor-foundation ordering.
- **The H22 + decision-time-binding DESIGN_SPECS** — the codification landed this session (the ownership/replica-coherence model E.1.2's serializer + reserve-set conform to).
- Regen `tools/gen_code_map.sh` at pickup; consult `DOCS/TOOLS.md` + `DOCS/RECURRING_BUG_PATTERNS.md` (Class 25/26/27 cfg-scope; Class 47 split-brain).

---

## Capture-completeness (this session — nothing lost)

- **Decisions → log:** D-287 (codification), D-288 (Fight #4), D-289 (deletion), D-290 (tools-over-grep) — all WRITTEN with paired `<!-- D/C/F -->` + `<!-- STATUS -->` sentinels in `v5.15.5.F.4d.1.E-architecture-v2.md`. No un-logged decision (Stage-5.5 independent review confirmed all propagated to their homes).
- **Findings → ledger:** `partner_pending_pnl` → **TECH_DEBT-227**; the Fight #4 stale fill-anchor comments → homed in the E.1.3 fold-block (correct-at-the-fix); the adjacent TD-196/180/A29 cross-linked; TD-168 moved open→closed. No un-homed debt.
- **Memories/skills → indexed:** ONE new memory `feedback_synthesis_needs_its_own_adversarial_pass` (in MEMORY_EXTENDED, correct tier — MEMORY.md is at its byte cap); `feedback_run_dedicated_audit_skills_not_just_armed_prompts` EXTENDED (the mechanical-tool analog); SUBAGENT_ARMING §3 + accept-handoff Stage 3.6 amended. No new skills.
- **Verified:** `check_session_docs.sh` SWEEP CLEAN (multiple passes) + the independent Stage-5.5 deliverable-completeness review CLEAN + engine confirmed clean `@0ee227a` (a mid-session clang-format artifact on `FixedPointN.hpp` from the operator's nvim plugin was reverted; queued as task #8).

---

## Operator collaboration norms (every-turn Tier-0)

Address Caramel as Caramel / she / her · no AskUserQuestion modals (inline text) · evaluate on robustness + latency + design, NOT time · **between agent rounds AND after pre-coding checks: present findings + CONSULT, never auto-proceed** · commit/push only when asked (the workspace push needs her — non-interactive push hangs) · auto-write TECH_DEBT/PARITY on deferral · SEARCH `TOOLS.md` before grep (D-290).

---

## Closing value statement

E.1.2 is the **successor-foundation leaf** — the one determinism-gated cycle where Position/NodeState open to their final shape. Everything downstream in .E (the E.1.3 aggregator + the Fight #4 coherence fix, the exit-redesign D-206, the multi-exchange .E.1) builds on this freeze, so the bar is "open it ONCE, correctly" (avionics-grade). The heavy design + validation is DONE + on disk + independently verified; the reformalize INPUTS are assembled + currency-corrected. Good luck.
