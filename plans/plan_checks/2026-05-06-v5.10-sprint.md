# /plan-check Report — v5.10 Sprint B (Pending Ships)

**Date:** 2026-05-06
**Master plan:** `plans/2026-05-02-MASTER-v5.9-to-v5.10.md`
**Branch:** `feat/v5.10-foundation` @ commit `4413197`
**Tests:** 1517/0 passing (post-recovery build verified)
**Verdict at audit time:** YELLOW (4 must-fix plan amendments before
v5.10.0b.1 coding); fully GREEN after amendments landed in same
session (commits forthcoming).

---

## Plan Summary

- **Master plan:** v5.9 sprint exit + v5.10 epic (Sprint B — 6 ships)
- **Sub-plans audited:** 4 pending ships (0b, 0c, 0d, 0e); 2 shipped (0, 0a)
- **Total remaining effort:** 10-14 days pre-amendment; 12-16 days post-amendment (added FPN_Exp + FPN_Sqrt to 0b)
- **Critical path:** v5.10.0 → v5.10.0a → v5.10.0b → v5.10.0c → v5.10.0e (0d independent)
- **Status entering audit:** v5.10.0 foundation + v5.10.0a-final SHIPPED; v5.10.0b-e PENDING

---

## Per-Sub-Plan Readiness Verdicts

| Plan | Verdict | PASS | GAP | DRIFT-RISK | DEFERRED | Key Findings |
|---|---|---|---|---|---|---|
| **v5.10.0b (FPN-e2e)** | YELLOW | 13 | 3 | 2 | 1 | Scope drift: exp/sqrt not locked in; Part 11 Newton-Raphson IS byte-level change; design gate only covers sin/cos |
| **v5.10.0c (hot-swap)** | GREEN | 15 | 0 | 0 | 0 | Clean RCU mirror; existing strategy pattern applicable |
| **v5.10.0d (FOREACH_TARGET)** | YELLOW | 12 | 2 | 0 | 1 | Stamp position MISMATCH with master plan; 0a already shipped position 19 |
| **v5.10.0e (drift detection)** | GREEN | 14 | 0 | 0 | 0 | Clean cfg + rolling IC + existing kill_switch mechanism |

---

## Cross-Plan Integration Findings

### A. CRITICAL — Stamp Body Position Order Mismatch [FIXED]

Master plan claimed position 19 = label_registry_hash (0d), 20 = grid_member_count (0a). Actual shipped code in `ML_Headers/ModelInference.hpp:1420` puts grid_member_count at 19 (0a-final SHIPPED) and reserves 20 for label_registry_hash.

**Fix applied:** master plan Integration Matrix updated to lock-positions reflecting shipped reality. v5.10.0d plan amended to specify position 20 unconditionally.

### B. v5.10.0b Scope Drift: FPN_Exp + FPN_Sqrt Required [FIXED — Option B]

`FlowFeatures.hpp` uses `exp()` 3× per push (EWMA decay at lines 167-169) and `sqrt()` in `LargeTradeState_ZScore` and `SpreadState_ZScore` (lines 231 + 302). Plan only locked FPN_Trig (sin/cos) via the design gate. Cross-build determinism for z-score outputs requires FPN-native exp + sqrt.

**Fix applied:** Operator chose Option B (full conversion). 0b plan extended with new Phase B.2.5 (FPN_Exp + FPN_Sqrt primitives + FlowFeatures conversion); effort estimate 3-5 days → 5-7 days. v5.11 master plan annotated: vectorization of these primitives can fold into v5.11.X if profiling shows wins, but isn't on critical path.

### C. Part 11 Newton-Raphson Divide Byte-Level Change [VERIFIED SOUND]

Newton-Raphson 128-bit divide replaces the long-double-based FP64_DivNoAssert. IS a byte-level change. Plan claims B.4's retrain absorbs it.

**Verified:** divide appears in slow-path feature compute + training math, not on hot path. Retrain post-B.4 is the canonical reference; old models rejected by MODEL_FORMAT_VERSION 6 bump. No cross-build determinism hazard within v5.10. Effort estimate increase justified.

### D. Files-Touched Conflicts (Multi-Plan Coordination) [PASS]

- ControllerConfig.hpp (0c + 0e cfg fields): different fields, sequential edits OK
- ModelInference.hpp (0b version bump + 0d field add): orthogonal, version bump first
- controller_test.cpp (0c + 0d + 0e tests): independent, cumulative
- MLStatusPanel.hpp (0c + 0e display rows): side-by-side, no collision
- ControllerEventLoop.hpp (0c swap consumer + 0e drift check): same slow-path loop. Sequencing requirement: drift check runs AFTER swap consumer (so new model's IC is tracked from swap point forward). Tracked as ship-time gate, not plan blocker.

### E. Cfg Field Additions [PASS — No Collisions]

- `acknowledge_hot_swap_with_open_positions` (0c): unique, default 0
- `confidence_ic_floor` (0e): unique, default 0.02
- `confidence_ic_floor_window` (0e): unique, default 86400 sec
- `auto_kill_on_drift` (0e): unique, default 0

Propagation gates (parser + engine.cfg.example + GUI tooltip + CHANGELOG) verified at ship time, not plan time.

### F. Architectural Invariants [PASS]

| Invariant | Status |
|---|---|
| Hot path UNTOUCHED | PASS (MODEL_FORMAT_VERSION bump in metadata only) |
| `MODEL_FORMAT_VERSION` only deliberate | PASS (0b 5→6 documented as deliberate breach in master) |
| `SHARDED_SNAPSHOT_VERSION` stable | PASS |
| `FEATURE_REGISTRY_HASH` stable | PASS (FOREACH_FEATURE untouched) |
| `LABEL_REGISTRY_HASH` introduced cleanly | PASS (new constant in 0d) |
| Train-serve parity | PASS (0b explicitly verifies replay-determinism) |
| Stamp body forward-compat | PASS (0b bumps version; 0d uses has_label_hash=0 fallback) |
| 3-tier strict mode | PASS (all follow v5.9.3a pattern) |

### G. Dependency Edge Validation [PASS post-fix]

```
v5.10.0 (shipped) → v5.10.0a (shipped) → v5.10.0b (PENDING)
                                              ├──→ v5.10.0c (PENDING)
                                              │      └──→ v5.10.0e auto-swap enhancement (optional)
                                              ├──→ v5.10.0e (PENDING; only requires 0b)
                                              └──→ v5.10.0d (independent; can ship anytime)
```

**Fix applied:** 0e plan re-stated to make 0c optional (auto-swap-to-fallback enhancement) instead of required (kill_switch trip works standalone via v5.5.x mechanism).

### H. Test Count Progression [PLAUSIBLE]

| Ship | Claimed | Estimated post-amendment |
|---|---|---|
| v5.10.0a-final (SHIPPED) | 1517 | 1517 ✅ |
| v5.10.0b | +8-12 | +14-18 (with FPN_Exp/Sqrt + Part 11 tests) |
| v5.10.0c | +6-8 | +6-8 |
| v5.10.0d | +6-8 | +6-8 |
| v5.10.0e | +6-8 | +6-8 |
| **Sprint B close** | ~1389 | ~1551-1559 |

### I. Codebase Drift Check [PASS]

All file:line references in pending plans resolve in current codebase. No stale function names, no removed cfg fields, no relocated headers.

---

## Recommendations (Action State)

### Must Fix Before Coding [DONE — all 4 amendments landed in same session]

1. ✅ Master plan Integration Matrix: stamp positions corrected (19 = grid_member_count, 20 = label_registry_hash)
2. ✅ v5.10.0b plan: option B chosen — Phase B.2.5 added with FPN_Exp + FPN_Sqrt primitives + FlowFeatures conversion. Effort 3-5 → 5-7 days.
3. ✅ v5.10.0d plan: position-number conditional stripped; states position 20 unconditionally
4. ✅ v5.10.0e plan: predecessor restated; 0c is optional enhancement, not required

### Worth Fixing During Coding

5. **Event loop sequencing (0c + 0e):** drift check must run AFTER swap consumer. Verify via integration test at 0e ship time.
6. **Cfg propagation completeness:** mechanical verification at each ship — parser + engine.cfg.example + GUI tooltip + CHANGELOG.

### Acceptable Risk

7. Newton-Raphson divide byte-level change: verified sound (B.4 retrain absorbs).
8. Test count claims aspirational (±2 per ship is normal); reconcile with actual at ship time.
9. FPN_Trig polynomial fallback to lookup table: reasonable hedge, no blocker.

---

## Final Verdict

### Pre-amendment: YELLOW (1 critical + 3 mechanical)
### Post-amendment: GREEN — sprint cleared to start v5.10.0b.1 coding

**What changed since the audit:**
- 4 plan files amended (master, 0b, 0d, 0e) + v5.11 master cross-reference updated
- All amendments preserve dependency DAG acyclic
- All cfg field collisions remain none
- All architectural invariants preserved or deliberate-and-documented
- Sprint B remaining effort: 12-16 days (revised up from 10-14 due to Phase B.2.5 fold-in)

**Operator next step:** Begin v5.10.0b.1 (RollingStats primitives + ROR_regressor index sites — trivial 5-site `FPN_FromDouble<F>((double)int)` removal as the confidence-building first step before B.2 / B.2.5 / B.3 / B.4).

---

## Audit lifecycle metadata

- **Generated by:** /plan-check skill (skill spec: `.claude/skills/plan-check/SKILL.md`)
- **Skill composition:** by-reference with /readiness (per-sub-plan checklist applied inline)
- **Audit scope:** 1 master + 4 sub-plans + 4 codebase reference checks + 9 cross-plan integration matrix dimensions
- **Following convention** (set this session): plan-check reports are private artifacts saved to `plans/plan_checks/<date>-<scope>.md`. Workspace-symlinked, gitignored from public repo.
