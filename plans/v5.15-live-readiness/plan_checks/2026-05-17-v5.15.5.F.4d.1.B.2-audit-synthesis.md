# v5.15.5.F.4d.1.B.2 — Pre-coding audit synthesis (Batch 1)

**Date:** 2026-05-17
**Ship:** `v5.15.5.F.4d.1.B.2` (cohort migration; 2 of 3 split in `.F.4d.1.B`)
**Branch:** `feat/v5.15-live-readiness`
**HEAD:** `725fe46` (`.B.1` shipped; not yet pushed to engine remote)
**Plan body audited:** `subplans/2026-05-17-v5.15.5.F.4d.1.B.2-cohort-migration.md` v1.0 DRAFT (first canonical NEW plan body drafted from inception via `future-oriented-plan-template.md`)
**Batch status:** Batch 1 of 1 (6 audits parallel; Batch 2-3 deferred per per-sub-ship cycle discipline — escalate only if Batch 1 surfaces wider-scope structural critique)

---

## Per-audit verdict table

| Audit | Verdict | Severity counts | Report path |
|---|---|---|---|
| `/parity-check` | YELLOW | 1 CRIT + 3 HIGH + 1 MED + 1 DOC + 3 CC | `plan_checks/parity-check-2026-05-17-v5.15.5.F.4d.1.B.2.md` |
| `/trace-deps` | YELLOW | 1 CRIT + 2 HIGH + 4 MED + 1 LOW | `plan_checks/trace-deps-2026-05-17-v5.15.5.F.4d.1.B.2.md` |
| `/readiness` | YELLOW | 1 CRIT + 4 HIGH + 5 MED + 4 LOW + 2 DOC | `plan_checks/readiness-2026-05-17-v5.15.5.F.4d.1.B.2.md` |
| `/dod-audit` | YELLOW | 2 MED + 3 LOW | `plan_checks/dod-audit-2026-05-17-v5.15.5.F.4d.1.B.2.md` |
| `/merge-scan` | YELLOW | 1 CRIT + 1 HIGH + 2 MED + 4 LOW | `plan_checks/merge-scan-2026-05-17-v5.15.5.F.4d.1.B.2.md` |
| `/accounting-audit` | YELLOW | 2 CRIT + 3 HIGH + 4 MED + 2 LOW + 1 DOC | `plan_checks/accounting-audit-2026-05-17-v5.15.5.F.4d.1.B.2.md` |

**Combined Batch 1 verdict: YELLOW.** No RED-flagged surfaces; framework's structural shape (`.B.1`) is sound; cohort migration direction is sound. **5 convergent CRIT findings + 7 convergent HIGH findings need plan body v1.0 → v1.1 amendment before pre-coding tag.** Net effort: ~90-120 min planning revision + 1 new Step (HIGH-CONV-D).

---

## CRITICAL convergent findings (verified at HEAD `725fe46`)

### CRIT-CONV-1 — `FOREACH_CFG_DERIVED_INFERENCE_CFG` NOT actually deleted at `.B.1`

**Source:** trace-deps HIGH-β + readiness HIGH-3 + accounting-audit CRIT-1
**Verified at HEAD:**
- `MemHeaders/CfgDerivedInferenceCfgRegistry.hpp:101` — macro still exists with 16 rows
- `CoreFrameworks/MetaRegistry.hpp:99` — annotation: "deletion deferred to `.B.3`"
- `ML_Headers/StampHelper.hpp:183` — production call `INFERENCE_CFG_AUTOPOPULATE(inf, cfg)` (legacy walker)
- `tests/controller_test.cpp:24981` — count test PASSING at 16

**Issue:** Plan body line 111 in "Canonical sister registries considered" table claims `FOREACH_CFG_DERIVED_INFERENCE_CFG` was "DELETED at `.B.1`". Reality: SUPERSEDED structurally; deletion deferred to `.B.3` per `.B.1` postmortem Discovery 1 (PARITY-020 regression risk at 0-row walker).

**Plan amendment (mechanical):** change line 111 verdict to `**NO-CHANGE (SUPERSEDED at .B.1; deletion at .B.3)**` + rationale.

### CRIT-CONV-2 — Struct field naming mismatch (framework expects prefixed; legacy uses unprefixed)

**Source:** parity-check CRIT-P1 + trace-deps CRIT-α
**Verified at HEAD:**
- `MemHeaders/CfgGateRegistry.hpp:212-213, 223-224, 290` — framework template fns access `inf.inference_cfg_<name>` + `inf.has_inference_cfg_<name>` + `handle.inference_cfg_<name>`
- `ML_Headers/ModelInference.hpp:1640-1643` (and 1196-1199) — `StampInferenceCfgInputs` + `ModelStampResult` declare unprefixed `int has_<name>; type <name>;`
- At HEAD, `inf.ridge_lambda` exists (from FOREACH_STAMP_BOUND_CFG); `inf.inference_cfg_ridge_lambda` does NOT exist for the cohort fields (only 8 `.A.7`/`.F.4d` fields have the prefixed form in POST_CFG)

**Issue:** When `.B.2` Step 1 populates rows + `if constexpr` filter goes non-empty → framework walker tries to access non-existent prefixed field names → BUILD FAILS at Step 1.

**Resolution options (3 options; resolve at triage):**
- **(A) Extend StampInferenceCfgInputs + ModelStampResult** — add `inference_cfg_<name>` + `has_inference_cfg_<name>` fields for each cohort row alongside existing unprefixed fields. ~24 struct fields × 2 structs = duplication; ~3KB struct cost; matches existing 8 POST_CFG fields' shape
- **(B) Drop `inference_cfg_` prefix in framework template fns** — align with legacy unprefixed names. Prefix was holdover from deleted `FOREACH_CFG_DERIVED_INFERENCE_CFG` registry (where rows ARE prefixed `inference_cfg_*`); has no remaining purpose in cfg-derived framework. `/parity-check` auto-pick
- **(C) Defer Step 7 walker site migration to `.B.3`** — keep legacy walker alive at `.B.2`; framework walker stays 0-row through `.B.2`; `.B.3` legacy deletion forces the walker swap (with prefix decision made at `.B.3`)

**Auto-pick recommendation: (B)** per `/parity-check` — simplest mechanical fix; aligns naming convention; preserves Step 7 walker migration scope at `.B.2`. Operator override to (A) if prefix preservation matters for POST_CFG sister registry's `inference_cfg_*` namespace.

### CRIT-CONV-3 — TECH_DEBT-082 already CLOSED at `.F.4d`; absorption is FALSE-NEW

**Source:** readiness CRIT-1 + merge-scan M-6 + parity-check MED-P1
**Verified at HEAD:**
- `tick-trader-percore-workspace/DOCS/TECH_DEBT.md:1555` — TECH_DEBT-082 already shows **CLOSED at v5.15.5.F.4d**
- `CoreFrameworks/CfgFieldRegistry.hpp:641` — `lazy_rebuild_price_threshold_pct` exists in master registry with `WARN_ON_CLAMP` only (NO STAMP_BOUND)
- `:644` — `exit_threshold` exists; same metadata
- `:647` — `confidence_ic_floor` exists; same metadata
- All carry "`.F.4d` TECH_DEBT-082 close" comments at HEAD

**Issue:** Plan body Step 1 line 364-370 auto-picks "ABSORB TECH_DEBT-082 (3 .F.5 residual fields)". The fields are NOT residual; they're already in master registry. The real open question is **STAMP_BOUND_CFG_DERIVED bit-add eligibility** for the 3 fields (different scope — only 2 are stamp-bound-eligible: `confidence_ic_floor` + `exit_threshold`; `lazy_rebuild_price_threshold_pct` is performance config NOT stamp-bound).

**Resolution options:**
- **(A) Defer to `.F.4f`** — 3 fields have no cohort gate predicate in common; full migration is `.F.4f` cleanup-ship territory. `/merge-scan` auto-pick.
- **(B) Partial absorb** — `confidence_ic_floor` only (always-emit; ~30-45 min); reframe Step 1 absorption to single field with explicit stamp-bound-eligibility rationale
- **(C) Reframe + absorb 2** — `confidence_ic_floor` + `exit_threshold` get STAMP_BOUND + STAMP_BOUND_CFG_DERIVED; `lazy_rebuild_price_threshold_pct` stays performance-only

**Auto-pick recommendation: (A)** per `/merge-scan` + per `feedback_no_defer_for_effort` (defer for scope-clarity reasons here, not effort-avoidance). Operator override to (B) or (C) if including in `.B.2` cycle preferred to fold-in completeness now.

### CRIT-CONV-4 — `stamp_format_version` is hardcoded literal, NOT a constant

**Source:** parity-check HIGH-P1 + readiness HIGH-2 + merge-scan M-2 + accounting-audit CRIT-2
**Verified at HEAD:**
- `ML_Headers/ModelInference.hpp:1747` — emit site writes `"stamp_format_version=1\n"` as string literal (no `STAMP_FORMAT_VERSION` constant exists)
- Parser at `:1346-1351` — has NO upper-bound validation of stamp_format_version on load

**Issue:** Plan body Step 8 framing "bump the constant by 1" is incorrect. Bumping `1`→`2` at literal alone produces SILENT HMAC mismatch on old fixtures (not explicit version-mismatch error which Step 9.4 test expects).

**Plan amendment (mechanical; expand Step 8):**
- Step 8.0: Extract literal → `static constexpr uint32_t STAMP_FORMAT_VERSION_CURRENT = 1;` named constant (sister to existing `MODEL_FORMAT_VERSION` precedent)
- Step 8.1: Add `MAX_SUPPORTED_STAMP_FORMAT_VERSION = N;` constant + parser bounds check at `:1346-1351` (refuse load if `version > MAX_SUPPORTED`)
- Step 8.2: Bump `STAMP_FORMAT_VERSION_CURRENT` 1 → 2 per option (a)
- Step 8.3: Test fixture failure-mode: synthesize v1 stamp; load on `.B.2` engine; verify operator-visible error references `stamp_format_version` mismatch + actionable regeneration message
- Step 8.4: DESIGN_SPEC amendment to `wire-format-byte-preservation-discipline.md` (explicit ship deliverable; HIGH-CONV-G below)

### CRIT-CONV-5 — Held-over Path γ #3 from `.B.1` self-audit (cohort gate predicates duplicated across 3 registries)

**Source:** merge-scan M-1 (CRITICAL — held-over)
**Verified at HEAD:**
- `ML_Headers/StampBoundCfgRegistry.hpp:99-179` — `FOREACH_STAMP_BOUND_CFG` col 5 `emit_when` (25 entries; ALL 6 cohort predicates inline)
- `ML_Headers/CfgDriftCheckRegistry.hpp:194-322` — `FOREACH_CFG_DRIFT_CHECK` col 8 `gate_when` (18 entries; same predicates: Bandit/Thompson + BLENDED-4 + PARITY-024 cohorts)
- Plan Step 5 ADDS the SAME predicates as ~20 entries in `FOREACH_CFG_GATE_PER_CORE`

**Issue:** The 5th BANDIT_THOMPSON field `thompson_exp3_blend_alpha` is in BOTH sister registries already with identical cohort gate. The `.B.1` self-audit Finding F1 flagged this triplet drift surface; ship proceeded with intent to revisit at `.B.2`/`.B.3`. **`.B.2` is the revisit moment.**

**Resolution options:**
- **(A) Accept duplication** — keep ~20 inline sidecar entries; live with parallel encoding until `.B.3` registry deletion eliminates 2 of the 3 sources. `.B.3` close = single source. Risk: 3-way drift surface alive throughout `.B.2`
- **(B) Extract 6 named `COHORT_GATE_*` macros** — `COHORT_GATE_BANDIT_ENABLED`, `COHORT_GATE_RIDGE_ANY`, `COHORT_GATE_COMPOSITE_CONFIDENCE`, `COHORT_GATE_SOFTRISK_ENABLED`, `COHORT_GATE_BLENDED_STATE_4`, `COHORT_GATE_PER_HORIZON_BARRIER` — both registries (StampBoundCfgRegistry + CfgDriftCheckRegistry) reference. ~+15 LOC + ~25 mechanical edits in the 2 legacy registries to use COHORT_GATE_* macros. `/merge-scan` recommendation. Closes the 3-way drift surface mid-`.B.2` regardless of legacy registry alive-or-deleted state.
- **(C) Defer to `.B.3` legacy empty-out** — let `.B.3` collapse the drift surface naturally via registry deletion. No `.B.2` action needed.

**Auto-pick recommendation: SURFACING — genuinely sharp trade-off.** Option (B) is the "structural fix preferred when bug class can recur" approach (CLAUDE.md item 19) — closes Path γ #3 NOW vs banking on `.B.3` cleanup. Option (C) accepts known drift surface for ~1 sub-ship. Operator preference: structural-fix-preference (B) vs scope-discipline (C).

---

## HIGH convergent findings

### HIGH-CONV-A — `ml_buy_threshold` already wire-bound through legacy registry; risk of duplicate emit

**Source:** parity-check HIGH-P2 + readiness HIGH-4 + trace-deps MED-2
**Verified at HEAD:**
- `ML_Headers/StampBoundCfgRegistry.hpp:157-158` — `ml_buy_threshold` already in `FOREACH_STAMP_BOUND_CFG` (always-emit; legacy walker emits to stamp body)
- `CoreFrameworks/CfgFieldRegistry.hpp:524` — exists in master registry without STAMP_BOUND bit
- Parallel manual decl at `ControllerConfig.hpp:822/1691/2507` — Path γ-class sibling

**Issue:** Plan Step 3 frames as "pre-canonical parity gap" needing STAMP_BOUND add. Reality: already wire-bound via DIFFERENT registry. Adding STAMP_BOUND + STAMP_BOUND_CFG_DERIVED bits → both legacy AND new framework walkers emit `ml_buy_threshold=X` → DUPLICATE wire line.

**Plan amendment:** at Step 3.1 (`ml_buy_threshold` migration), simultaneously DELETE legacy entry at `StampBoundCfgRegistry.hpp:157-158`. Also DELETE parallel manual decl at `ControllerConfig.hpp:822/1691/2507` (Path γ-class) — verify cohort migration covers.

### HIGH-CONV-B — `bandit_blend_ratio` already in FOREACH_STAMP_BOUND_MODEL_CONST with prefixed key

**Source:** parity-check HIGH-P2
**Verified at HEAD:**
- `ML_Headers/StampBoundModelConstRegistry.hpp:296-297` — `bandit_blend_ratio` already in `FOREACH_STAMP_BOUND_MODEL_CONST` with wire key `inference_cfg_bandit_blend_ratio`

**Issue:** Without DELETE simultaneously with ADD at Step 3.2, `.B.2` emits BOTH `inference_cfg_bandit_blend_ratio=X` (legacy POST_CFG) AND `bandit_blend_ratio=X` (new framework) → duplicate emit + wire-key rename.

**Plan amendment:** at Step 3.2, simultaneously DELETE POST_CFG entry at `StampBoundModelConstRegistry.hpp:296-297`. Verify wire-key transition is documented (legacy `inference_cfg_bandit_blend_ratio` → new `bandit_blend_ratio`); this is part of CRIT-CONV-2 prefix decision.

### HIGH-CONV-C — HMAC LOCKED hash constant re-introduces Class 18 mirror at hash-constant layer

**Source:** parity-check HIGH-P3
**Verified at HEAD:**
- `wire-format-byte-preservation-discipline.md:202-274` — Layer 5b Option F is canonical: structural invariant runner `STAMP_BOUND_CFG_DERIVED_run_generic_invariants()` (I1-I5)
- `.A` planning explicitly rejected LOCKED-hash approach per `feedback_principle_beats_registry_for_eliminating`

**Issue:** Plan Step 9.4 uses `LOCKED_STAMP_BOUND_CFG_DERIVED_HASH_V5_15_5_F4D_1_B_2` constant — the rejected v1.0 LOCKED-hash approach. Reintroduces Class 18 mirror.

**Plan amendment:** at Step 9.4, replace LOCKED hash constant approach with `STAMP_BOUND_CFG_DERIVED_run_generic_invariants()` invariant runner (I1-I5 sections per spec). Test fires invariant runner; verifies invariants hold (not byte-equivalence against frozen hash).

### HIGH-CONV-D — FOREACH_ML_CFG_FLAG NOT walked by `.B.1` framework consumers

**Source:** readiness HIGH-1
**Verified at HEAD:**
- `MemHeaders/CfgGateRegistry.hpp:204-228` — `cfg_derived::populate_inference_cfg_from_derived` walks `FOREACH_PER_CORE_CFG_FIELD` + `FOREACH_GLOBAL_CFG_FIELD` only
- Same for `populate_stamp_cfg_from_derived` + `drift_check_from_derived`

**Issue:** Plan Step 4 adds `STAMP_BOUND_CFG_DERIVED` bit to 5 ML_CFG_FLAG rows (`CONFIDENCE_COMPOSITE_ENABLED` + `RIDGE_WITHIN_HORIZON` + `RIDGE_ACROSS_HORIZONS` + `EXIT_BLENDER_MODE` + `PER_HORIZON_BARRIER_BLEND`). Without framework extension to walk ML_CFG_FLAG, the bit-add produces NO observable behavior.

**Plan amendment (substantive — new Step 0.5):**
- Step 0.5: Extend framework consumer template fns at `MemHeaders/CfgGateRegistry.hpp:204-228 + 234-269 + 277-309` to walk `FOREACH_ML_CFG_FLAG` (filtered by `metadata_flags & STAMP_BOUND_CFG_DERIVED`) AFTER walking the master per-core + global registries
- New X-macro filter pattern: `if constexpr (((metadata_flags) & STAMP_BOUND_CFG_DERIVED) != 0) { ... }` with bitmap-bool emit shape (read `BITMAP_IS_SET(cfg.ml_cfg_flags, MASK_ML_CFG_<NAME>)` → emit `0` or `1`)
- Wire byte preservation: same locale-pin + `%.17g` for double or `%d` for int; ternary normalization per Y3 dispatch precedent
- Effort: ~30-45 min focused

### HIGH-CONV-E — Per-core gate semantics scope flatten (Class 27 latent)

**Source:** accounting-audit HIGH-1 + HIGH-2
**Verified at HEAD:**
- `cfg.risk_degradation_curve` exists at BOTH `ControllerConfig.hpp:710` (flat global) AND `FOREACH_PER_CORE_CFG_FIELD` at `CfgFieldRegistry.hpp:608` (per-core)
- Plan Step 5 sidecar gate `cfg.risk_degradation_curve != 0` reads GLOBAL flat → silently flattens per-core distinction if cores diverge
- Same shape for `cfg.bandit_algorithm == 4` (per-core in FOREACH_PER_CORE_CFG_FIELD at `:599`; sidecar gate reads global)

**Issue:** If core-1 has `bandit_algorithm=4` (BLENDED) but global=0, stamp won't emit `thompson_exp3_blend_alpha` for core-1 → silent training/serving drift on core-1. Latent Class 27 surface.

**Resolution options:**
- **(A) Per-core gate evaluation in sidecar lookups** — `cfg_gate::lookup_populate` + `lookup_drift` take a `core_id` param; switch dispatch reads `cfg.per_core[core_id].risk_degradation_curve != 0` etc. when applicable. Changes `.B.1` framework signature (HIGH impact)
- **(B) Migrate flat fields to single-source per-core only** — DELETE flat `cfg.risk_degradation_curve` + `cfg.bandit_algorithm` at `ControllerConfig.hpp:710/...`; verify all read sites use per-core access. Per `cfg-scope-discipline.md`. Scope expansion (~30-45 min)
- **(C) Accept flat-read in sidecar gate; document deviation** — when stamp emits canonical body, it reads the GLOBAL value as a "canonical" representation; per-core divergence handled at training-time fixture choice. Documents the simplification.

**Auto-pick recommendation: SURFACING — genuinely sharp trade-off.** Option (A) is structurally correct but invasive (modifies `.B.1`-shipped framework). Option (B) eliminates the duality (cfg scope-discipline). Option (C) accepts the flatten with documentation. Operator preference: correctness-via-invasive-change (A) vs scope-discipline (B) vs document-and-move-on (C).

### HIGH-CONV-F — Step 3 DELETE manual POST_CFG entries is INCORRECT at `.B.2`

**Source:** readiness HIGH-4 + trace-deps MED-3
**Verified at HEAD:**
- `ML_Headers/StampBoundModelConstRegistry.hpp:454-465` — POST_CFG entries with `inference_cfg_*`-prefixed wire keys for `ml_tp_pct`/`ml_sl_pct`/`barrier_blend_mode`/`per_horizon_barrier_blend`

**Issue:** Plan Step 3 says "DELETE manual POST_CFG entries" for the 4 retroactive `.A.7` fields. The POST_CFG entries are EMIT-side rows with `inference_cfg_*` wire keys; deleting at `.B.2` breaks wire format mid-flight.

**Plan amendment:** at Step 3, REMOVE "DELETE corresponding manual POST_CFG entries" line. Defer POST_CFG entry deletions to `.B.3` (sister to `.B.3` skeleton's legacy registry empty-out scope). `.B.2` only adds STAMP_BOUND_CFG_DERIVED bit; bytes still flow through legacy POST_CFG via existing INFERENCE_CFG_AUTOPOPULATE walker until `.B.3` legacy deletion forces the swap.

### HIGH-CONV-G — Step 8 spec amendment must be ship-deliverable

**Source:** accounting-audit HIGH-3
**Verified at HEAD:**
- `wire-format-byte-preservation-discipline.md` does NOT yet have "Framework refactor wire-format changes" section

**Issue:** Plan Step 8 claims `wire-format-byte-preservation-discipline.md` is amended with procedure. But the amendment hasn't been written. Stamp_format_version bump is FIRST-OF-KIND (no prior bump in engine history). Procedure documentation IS the ship-deliverable for future ships to follow.

**Plan amendment (mechanical):** at Step 8, add sub-step 8.4: WRITE new section in `wire-format-byte-preservation-discipline.md` titled "Procedure for wire-format changes during framework refactoring" with canonical mechanism: (1) bump `STAMP_FORMAT_VERSION_CURRENT`; (2) add `MAX_SUPPORTED_STAMP_FORMAT_VERSION` parser bounds check; (3) test fixture failure-mode at v1→v2 boundary; (4) postmortem documents the bump rationale. `.B.2` is first canonical reference.

---

## MED + LOW findings (smaller; coding-time amendments)

| Finding | Source | Mechanism |
|---|---|---|
| **MED-1** — Step 7.1/7.2/7.3 struct-gen approach unresolved (Approach A/B/C) | dod-audit | Pre-decide at v1.1 amendment: Path 1 unconditional struct-gen (~1.9KB .bss cost) OR Path 2 defer struct-gen to `.B.3` |
| **MED-2** — Step 2 NEW row template uses non-existent `CategoryApplicability::` namespace | dod-audit | Fix template at v1.1 lines 397-400: use bare enum names per neighbor rows (`STRAT_CAT_ALL`, `OP_MODE_CAT_ALL`, etc.) |
| **MED-3** — Class 27 latent for `bandit_blend_ratio` (stamp-only at HEAD; no accounting-path read) | accounting-audit | Add Class-27-prevention note for future cohorts |
| **MED-4** — Step 6 Winsor predicate uses FPN_ToDouble chain on comparison | accounting-audit | Prefer FPN-native compare `cfg.winsor_pct_low >= cfg.winsor_pct_high` |
| **MED-5** — `BacktestPanels.hpp:651, 854, 1666, ...` has display-side `r->gap_acceptable_threshold` (training summary mirror) | trace-deps + dod-audit | Step 2 verification note clarifying display-only mirror NOT touched |
| **MED-6** — Step 5 gates assume `cfg.ml_cfg_flags` is global single-bitmap (correct at HEAD) | accounting-audit | Flag for future per-core ml_cfg_flags variants |
| **MED-7** — Step 1 `risk_min_size_pct` line ref "search via grep" placeholder | accounting-audit + trace-deps | Resolve to `CfgFieldRegistry.hpp:617` (verified) |
| **LOW-1** — DESIGN_SPECs landed/amended section uses `v?` placeholder | dod-audit | Replace with concrete versions at coding-time Step 0.5 |
| **LOW-2** — FOREACH_DRIFT_OVERRIDE row in sister table (future at `.C`) lacks annotation | dod-audit | Add "pre-noted for axis disambiguation" annotation |
| **LOW-3** — Display-side `float gap_acceptable_threshold` at BacktestPanels.hpp:651 (training summary metadata mirror) — H4 display-only OK | dod-audit + accounting-audit | Defensive footnote at Step 2 |
| **LOW-4** — Plan body uses 18/24+/25/29-row count variously | accounting-audit | Single canonical footnote near line 21 ("25 cohort rows / 18 master + 5 ML_CFG_FLAG + 1 gap_acceptable_threshold + 1 trading_mode") |
| **LOW-5** — Off-by-1 in FOREACH_ML_CFG_FLAG line refs (53 vs 54, 55 vs 56) | trace-deps | Mechanical fix |
| **LOW-6** — Plan body Step 3 + Step 7.5 subtly inconsistent re: inf struct field population | merge-scan | Step 7.5 (keep STAMP_CFG_AUTOPOPULATE alive at `.B.2`) is correct; Step 3 alignment note |
| **LOW-7** — Sister table missing 2 rows | merge-scan | Add `FOREACH_STAMP_BOUND_MODEL_CONST` (EXTEND verdict) + `FOREACH_LIVES_IN_STRUCT` (NO-CHANGE) |
| **LOW-8** — Sidecar entry order is cosmetic | merge-scan | Add ~5-line comment that canonical emit order is master registry declaration order, not sidecar order |
| **DOC-1** — Step 9.4/9.6 reference `wire_format_invariants.hpp` helper but don't enumerate I1-I5 invariant sections | parity-check | Add explicit invariant section names to test descriptions |
| **DOC-2** — readiness's 2 DOC findings (per-spec status banner finalization + sample table) | readiness | Cosmetic |

---

## Plan amendment list (ordered; resolves CRIT + HIGH findings)

### Mechanical amendments (no operator decision needed; auto-fix at v1.1)

1. **Line 111 stale-claim fix:** `FOREACH_CFG_DERIVED_INFERENCE_CFG` → "NO-CHANGE (SUPERSEDED at `.B.1`; deletion at `.B.3`)" [CRIT-CONV-1]
2. **Step 3 amendment:** REMOVE "DELETE manual POST_CFG entries" line; defer to `.B.3` [HIGH-CONV-F]
3. **Step 9.4 LOCKED hash → invariant runner:** replace `LOCKED_STAMP_BOUND_CFG_DERIVED_HASH_V5_15_5_F4D_1_B_2` with `STAMP_BOUND_CFG_DERIVED_run_generic_invariants()` (I1-I5) per `wire-format-byte-preservation-discipline.md:202-274` [HIGH-CONV-C]
4. **Step 8 expansion:** sub-steps 8.0-8.4 (extract constant + bounds check + bump + fixture test + spec amendment) [CRIT-CONV-4 + HIGH-CONV-G]
5. **Step 2 template fix:** use bare enum names (`STRAT_CAT_ALL`, etc.) instead of `CategoryApplicability::` namespace [MED-2]
6. **Step 3.1/3.2 simultaneous DELETE additions:**
   - 3.1: also DELETE `StampBoundCfgRegistry.hpp:157-158` entry + parallel `ControllerConfig.hpp:822/1691/2507` for ml_buy_threshold [HIGH-CONV-A]
   - 3.2: also DELETE `StampBoundModelConstRegistry.hpp:296-297` entry for bandit_blend_ratio [HIGH-CONV-B]
7. **Line ref corrections:** off-by-1 in FOREACH_ML_CFG_FLAG (53/55 vs 54/56); `risk_min_size_pct:617` [LOW-5 + MED-7]
8. **Row count canonical footnote:** clarify 25-row breakdown near plan body line 21 [LOW-4]
9. **Step 6 Winsor FPN-native compare:** replace FPN_ToDouble chain with native compare [MED-4]

### Substantive amendments (operator decision required at triage)

**Decision A — CRIT-CONV-2 struct field naming (3 options):** (A) extend struct + (B) drop prefix in framework + (C) defer Step 7 to `.B.3`. Auto-pick: **(B)**. Operator override: ?

**Decision B — CRIT-CONV-3 TECH_DEBT-082 reframe (3 options):** (A) defer to `.F.4f` + (B) partial absorb `confidence_ic_floor` + (C) reframe + absorb 2. Auto-pick: **(A)**. Operator override: ?

**Decision C — CRIT-CONV-5 Path γ #3 cohort gate macros (3 options):** (A) accept duplication + (B) extract 6 COHORT_GATE_* macros + (C) defer to `.B.3`. Auto-pick: **GENUINELY UNCERTAIN — surfacing**. Operator preference: ?

**Decision D — HIGH-CONV-D framework extension to walk FOREACH_ML_CFG_FLAG:**
- ACCEPT (new Step 0.5; ~30-45 min effort) — required for FOREACH_ML_CFG_FLAG bit-adds to have observable behavior
- ALTERNATIVE: DEFER Step 4 (FOREACH_ML_CFG_FLAG sig migration) to `.B.3` — saves the framework extension cost at `.B.2`; sig migration done at `.B.3` when legacy registry deletion forces FOREACH_ML_CFG_FLAG framework wiring anyway

Auto-pick: **ACCEPT new Step 0.5** — keeping FOREACH_ML_CFG_FLAG sig migration at `.B.2` is structurally clean (cohort migration ship; covers all cohort registries). Operator override: ?

**Decision E — HIGH-CONV-E per-core gate semantics (3 options):** (A) per-core gate eval in sidecar + (B) migrate flat fields to per-core only + (C) accept flat-read + document. Auto-pick: **GENUINELY UNCERTAIN — surfacing**. Operator preference: ?

**Decision F — CRIT-6 byte order (3 options from plan body Design Decision 1):** (a) accept reorder + version bump + (b) walker enrichment + (c) reorder source rows. Plan body auto-pick: **(a)**. Already surfaced inline; reconfirm at triage given CRIT-CONV-4 expansion of Step 8.

---

## Per-sub-ship cycle effort revision

| Phase | v1.0 estimate | v1.1 revised |
|---|---|---|
| Planning (this synthesis + plan body v1.0 → v1.1) | ~30 min | **+ ~90 min for triage + amendment** |
| Pre-coding tag | — | unchanged |
| Implementation Steps 0-10 | ~6-8h | **+ ~30-45 min for new Step 0.5 (if Decision D = ACCEPT)** |
| Build verify + ship close | ~30 min | unchanged |
| Postmortem + auto-writes + /sync-workspace | ~30 min | unchanged |
| **Total** | **~6-8h focused** | **~7-9h focused** |

Net delta: ~1-1.5 hours total — well within `.B.2` sub-ship boundary. No re-split needed.

---

## Cross-references

- Plan body audited: `subplans/2026-05-17-v5.15.5.F.4d.1.B.2-cohort-migration.md` v1.0 DRAFT
- 6 audit reports: `plan_checks/{parity-check,trace-deps,readiness,dod-audit,merge-scan,accounting-audit}-2026-05-17-v5.15.5.F.4d.1.B.2.md`
- `.B.1` postmortem: `postmortems/2026-05-17-v5.15.5.F.4d.1.B.1-postmortem.md`
- `.B.1` audit synthesis (predecessor): `plan_checks/2026-05-17-v5.15.5.F.4d.1.B-audit-synthesis.md`
- Sub-master: `subplans/2026-05-16-v5.15.5.F.4d.1-thread-a-framework-full.md` v1.3
- Sprint roadmap: `plans/v5.15-live-readiness/ROADMAP-2026-05-17-to-paper-test.md`

---

**End of Batch 1 synthesis.** Operator triage required for 6 decision points (A/B/C/D/E/F) before plan body v1.0 → v1.1 amendment + pre-coding tag.
