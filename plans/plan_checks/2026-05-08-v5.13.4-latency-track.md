# /latency-track audit — v5.13.4 (sell-side bandit) — 2026-05-08

**Commits scanned:** 937acf1..c03749e (v5.13.4.A/B/C/D)
**Scope:** Sell-side bandit reward attribution (drainer-only feature)

## Summary

- Hot-path additions: **0**
- Slow-path additions: 2 int8_t writes per exit-fire (inside
  existing branch)
- Drainer additions: ~1–50 ns per fill (cfg-dependent)
- Cold-path additions: init + save/load helpers (boot/shutdown only)

**Verdict: PASS on all claimed latency tiers.** Zero hot-path
impact, minimal slow-path cost, ~50 ns counterfactual + Bandit_Update
in drainer post-fill (rare event).

---

## Detailed Findings

### 1. Hot-path (`ExecutionCore.hpp`) — UNCHANGED ✓

- No modifications to `ExecutionCore_Tick`, `ExecutionCore_Tick_Impl`,
  `BG_Evaluate`, `SG_Evaluate`.
- `exit_bandits` not accessed in hot path.
- `last_exit_predicted_arm[]` / `last_exit_predicted_regime[]` not
  touched by hot path.

**PASS.**

### 2. Slow-path (`EngineSharded.hpp:2961-2964`) — +2 writes ✓

Two int8_t writes per exit-fire:
```cpp
oms.last_exit_predicted_arm[pidx] = (int8_t)
    state.cores[c].last_exit_dominant_horizon;
oms.last_exit_predicted_regime[pidx] = (int8_t)
    state.cores[c].regime_state.current_regime;
```

Inside nested branches: outer cfg gate + middle `if (bm)` + inner
`while (bm)` — already entered on cache-warm fill loop.

Cost: ~3 ns total when branch fires (rare event). **PASS.**

### 3. `OrderManagerState` size growth: 32 bytes ✓

- `last_exit_predicted_arm[16]` = 16 B
- `last_exit_predicted_regime[16]` = 16 B
- Padding = 0 B

Cache adjacency: lands next to v5.13.0.B `last_exit_was_predicted[]`
+ `last_exit_predicted_p[]` — same cache line for slow-path /
drainer access. Drainer is sole reader; no false-sharing.

**PASS.**

### 4. `EnsembleModelZoo` exit_bandits size — DOC NIT ⚠

**HOT_PATH_CHANGELOG claim:** "~96 B × 5 = 480 B"

**Actual:** `BanditState` is ~456 B (not 96 B):
- `weights[8]` = 64 B
- `cum_reward[8]` = 64 B
- `pulls[8]` = 32 B
- `arm_names[8][32]` = 256 B
- `n_arms` + `total_steps` + `gamma` + `eta_max` + `blend_ratio` +
  `min_samples` + `ramp_up_samples` = 40 B

Total: ~456 B/BanditState × 5 regimes = **~2,280 B**

**Impact:** None on latency — `exit_bandits` is drainer-only, not
read by hot path. Pure documentation accuracy fix.

**Action:** Update HOT_PATH_CHANGELOG.md v5.13.4 entry size figure
(480 B → ~2,280 B / "~2.3 KB").

### 5. Drainer post-fill (`ControllerEventLoop.hpp:1379-1412`) — ~50 ns ✓

Operations when 5-condition gate fires:
1. Pointer cast: ~1 ns
2. Bounds checks: ~2 ns
3. Counterfactual math (FPN→double + arithmetic): ~25 ns
4. `Bandit_Update` (counters + adaptive eta + weight update): ~20 ns

Total: ~50 ns when triggered.

Default cfg (`exit_bandit_enabled=0`): ~1 ns predicted-not-taken.
cfg=1 + no predicted exit on slot: ~2 ns early return.

**PASS — matches claim.**

### 6. Hot-path atomic load check ✓

`flatten_pending.load(memory_order_acquire)` at line 1382 is on
the DRAINER, not hot path. No new hot-path atomic operations.

**PASS.**

---

## Cross-checks

| Check | Result |
|---|---|
| New atomic ops on hot path? | ✗ None |
| New clock calls? | ✗ None |
| New branches on hot path? | ✗ None |
| New struct fields on hot-path-accessed types? | ✗ None |
| Data-dependent slow-path predicate? | ✓ 1 (cfg-gated, predicted-not-taken default) |
| Cache-line straddles? | ✗ None (adjacent to v5.13.0.B fields) |
| Reuse existing computation? | ✓ entry_price/original_tp/notional all cache-warm from fill loop |

---

## Optimization Opportunity (deferred)

**Precompute counterfactual `(tp_pct - 2*fee_taker)` at entry-submit
time** → store on `Position` struct → save ~10 ns per attribution.

Marginal — drainer p99 sits in microseconds for buy-side bandit
work; +50 ns is 0.5% of tail. Defer until drainer p99 becomes a
bottleneck.

---

## Verdict: **GREEN** — ship as-is

- Zero hot-path impact
- Minimal slow-path cost (+2 int8_t writes inside existing branch)
- ~50 ns drainer cost per attribution (rare event)
- Correct cache locality
- Cfg-opt-in default (~1 ns gate check)

**Minor doc nit:** HOT_PATH_CHANGELOG size estimate (480 B → ~2,280 B).
Not load-bearing, low priority. Worth fixing for future-reader
accuracy.
