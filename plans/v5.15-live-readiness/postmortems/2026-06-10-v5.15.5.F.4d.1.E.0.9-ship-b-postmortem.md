---
type: postmortem
ship: v5.15.5.F.4d.1.E.0.9 (Ship B — decimal money)
date: 2026-06-10
status: SHIPPED
---

# Ship B postmortem — the decimal money epoch

## What shipped
The full P0–P5 sequence in two session-blocks: the certified decimal core (P0/P1, oracle-gated,
1,759 frozen rows), the teeth-proofed epoch net (P2), 8+1 dispatcher decimal branches (P2a),
**the atomic flip** (P2b — ~48 compiler-driven red-build cycles, 53 files), fee booking +
flags drain (P3), the #6 quantizer + epoch-reject tests (P4), and the close (P5: FP64 absorb,
golden refreeze, **Check-F un-bypassed**, H4 rewrite, Class-41 codification). Suite 3246 → 3280/0.

## What worked (keep doing)
- **The epoch net carried the flip.** Every guard fired exactly as designed and the red build
  PRESCRIBED the version/OMSEL/stamp cohort — zero reliance on memory at a 53-file atomic change.
  Guards-compound validated at full scale.
- **Compiler-as-enumerator + the work-order.** The probe → work-order → flip pipeline meant the
  indivisible red period was bounded and lossless across a session boundary.
- **Oracle-first (D-100).** Every decimal kernel was green against frozen Python-decimal fixtures
  BEFORE any production caller existed; zero kernel bugs surfaced during the flip.

## What bit us (codified)
- **Class 41** — raw `.v` compares are encoding-blind: BuyGate silently produced zero entries
  while 3,268 unit tests passed; the integration smoke caught it. Encoding migrations must
  enumerate `.v`-compare sites; integration smokes are mandatory epoch gate criteria.
- **Domain guards for bulk transforms** (memory codified): the compiler adjudicates types, not
  domains — the test-file MQ promotion was compile-clean but runtime-wrong for binary math-kernel
  tests; the auto-pass oscillation (17/17 swap↔revert) was the signal.
- **Regex hygiene** (B-19 family): a `\1` literal mangled a registry row and broke the FIELD_IDX
  chain 2 cycles before detection — verify each scripted registry transform by grep IMMEDIATELY.

## Deliberate residue (tracked)
- TECH_DEBT-149 PARTIALLY closed (WS money parse exact; TUI/depth/recorder doubles remain).
- OrderResult ring vehicle stays double + payload-bridge (S-8 scaled-i64 rides .E.3 with the
  REST/WS rework); commission-asset string-direct parse rides .E.3.
- The fold (`Portfolio_FromEventLog`) stays gross-of-fee for boot recovery; the S-3 fee slot
  makes a net-aware fold possible whenever wanted.
- TECH_DEBT-159 UNGATED (16B re-pack /dod-audit eligible any sprint).

## Close-review addendum (2026-06-10, post-ship)

The independent deliverable-review (D-79) + the operator's "are we forgetting anything"
sweep caught SEVEN post-ship items, all closed same-session:
- 2 always-loaded doc gaps (MEMORY.md index splice; stale H4 gradient bullet) — reviewer finds.
- 5 promised-at-close items the self-close missed: **Class 42** authored (D-172a), the **B10
  taxonomy encoding-flip note** (D-172b), the **D-172c sweep-clause memory extension**, the
  **lot_max_qty clamp** at both submit paths (D-175 ratified pick; was parsed-never-consumed),
  and **D-173 commission-branch tests** (all 3 branches verified; suite 3280 → 3285/0).
- Bonus catch: the USE_NATIVE_128 `sed`-line-delete had orphaned FIVE CMake `endif()`s —
  configure failed SILENTLY (the build-error grep keyed on "error:" with a colon; CMake says
  "errors occurred!") and the suite ran a STALE binary for two commits. Same B-19 regex-hygiene
  family: line-delete seds need an if/endif balance check + build-verify greps must match
  configure-stage failures too. Comment-only commits were the only ones affected; the fresh
  full rebuild re-verified everything at 3285/0.

Lesson reinforced: the close isn't clean until an INDEPENDENT pass + the decision-log
promise-sweep both come back empty — the builder's own checklist missed 7 items.
