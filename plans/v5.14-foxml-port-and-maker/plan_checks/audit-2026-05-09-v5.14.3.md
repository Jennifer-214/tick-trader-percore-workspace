# /trace-deps + /readiness audit — v5.14.3 (3-layer registry fingerprinting)

**Plan:** `plans/2026-05-08-v5.14.3-three-layer-fingerprint.md`
**Date:** 2026-05-09
**Auditor:** Manual /trace-deps + /readiness all 25 checks (incl. v5.14.2.E.3 additions: Check 24/25 + Step 6 strengthen)

---

## Verdict: YELLOW (2 actionable gaps before coding)

Plan quality 9/10 architecturally; 7/10 completeness due to call-site enumeration gap that exactly matches Class 18 (PARITY-009/010/011/012 pattern just closed by v5.14.2.E.1).

---

## Section A — /trace-deps (Steps 1-5 + strengthened Step 6)

### Steps 1-5: PASS

| Check | Result |
|---|---|
| FEATURE_REGISTRY_HASH at FeatureRegistry.hpp:400 (FNV-1a, v5.8.6) | ✅ exists |
| Surface G `has_*` flag pattern (17 uses post-v5.11.18a) | ✅ established |
| Scaler sidecar binding (FeatureStandardizer.hpp v5.9.3a) | ✅ analog precedent |
| Stamp body 4 new fields claim | ✅ design clear |
| Sidecar JSON canonical-order discipline | ✅ specified |

### Step 6 STRENGTHENED (call-sequence enumeration): RED

**The gap:** Plan Step 4 says "Boot-time loader integration in EngineSharded.hpp" but doesn't enumerate ALL the mirror sites that need to call the new `FeatureOverlay_Load + FeatureOverlay_VerifyAgainstStamp`.

**Required call sites (per existing model-load discipline post-v5.14.2.E.1):**

| Site | File:line | Plan mentions? |
|---|---|---|
| Boot single-zoo | `EngineSharded.hpp:~1108-1131` | ✅ |
| Boot ensemble | `EngineSharded.hpp:~1153-1207` | ✅ |
| Backtest single-zoo | `BacktestSharded.hpp:~291-307` | ❌ |
| Backtest ensemble | `BacktestSharded.hpp:~312-368` | ❌ |
| Single-zoo hot-swap | `EngineSharded.hpp:~2772-2820` | ❌ |
| Ensemble hot-swap | `EnsembleHotSwap.hpp` | ❌ |

**Same Class 18 footprint that just bit us 4× (PARITY-009/010/011/012).** v5.14.2.E.1's PostLoadSetup helpers extinguished it for model-load setup; v5.14.3 risks re-introducing it for overlay verification.

**Recommended fix per CLAUDE.local.md structural-fix rule + CLAUDE.md item 13:**
Extract `FeatureOverlay_PostLoadVerify<F>(zoo, ezoo, cfg, ...)` helper. Boot, backtest, hot-swap (×2) all call it. Same shape as `CoreModelZoo_PostLoadSetup` + `EnsembleModelZoo_PostLoadSetup` from v5.14.2.E.1.

---

## Section B — /readiness Checks 1-25

| # | Check | Verdict | Notes |
|---|---|---|---|
| 1 | Hot path purity | PASS | Boot-time only (one-time per ML model load); per-cycle = 0 |
| 2 | Train-serve parity | PASS | New parity surface — overlay sidecar on training side, stamp body verify on serving side |
| 3 | Surface area | YELLOW | 3 sub-tags (~450 LOC). Adds 1 new C++ header + 1 new Python file. Manageable. |
| 4 | Pointer init + heap | PASS | FeatureOverlay struct simple; no malloc |
| 5 | Backward compat | PASS | Surface G `has_overlay_hash`/`has_effective_hash` flags; legacy stamps skip silently |
| 6 | Multi-threading | PASS | Boot-time only; no concurrent access |
| 7 | Test coverage | PASS | 7 scenarios specified |
| 8 | Docs + invariants | PASS | Plan documents Surface G usage |
| 9 | Forward maintenance | YELLOW | If overlay field count grows, no X-macro registry today (manual additions) |
| 10 | Rollback story | PASS | pre-v5.14.3 = v5.14.2.E close |
| 11 | Architectural sprint | PASS | Additive only |
| 12 | Display ↔ execution | N/A | No display logic |
| 13 | Strategy lifecycle | N/A | No strategy changes |
| 14 | X-macro dispatch | YELLOW | 4 new stamp body fields — should they fit a future FOREACH_STAMP_BOUND_SIDECAR registry, or stay manual? Plan should declare. |
| 15 | ML feature parity | PASS | No FOREACH_FEATURE changes |
| 16 | Cfg field + stamp | PASS | Reuses existing held_out_gate_strict; no new cfg |
| 17 | Model-load path → strict-mode | PASS | Plan claims 3-tier strict mode (refuse/warn/silent) |
| 18 | Reuse-audit | PASS | 6 primitives reused (SHA256, JSON, stamp body, has_* flags, 3-tier strict, sidecar pattern) |
| 19 | Pre-existing-work | PASS | Plan documents reuse claims thoroughly |
| 20 | Future-proofness | YELLOW | Per Check 14: 4 new fields manually emitted/parsed — clarify pattern choice |
| 21 | Test count assertion | PASS | No `==` assertions cited |
| 22 | Auto-trigger downstream re-audit | YELLOW | v5.14.3 touches stamp body schema; v5.14.4-12 plans depending on stamp shape need re-audit AFTER .3 ships |
| 23 | Latency accountability | PASS | Boot-only path; documented as N/A |
| 24 | Mirror-function call-sequence (NEW) | **RED** | Step 4 doesn't enumerate backtest + hot-swap call sites for FeatureOverlay verify. EXACT Class 18 pattern. |
| 25 | TECH_DEBT.md surface scan (NEW) | PASS | Verified: TECH_DEBT-001 (bash CLI) — orthogonal; TECH_DEBT-003 (parser refactor) — 32 keys < 40 trigger; TECH_DEBT-006 (FOREACH_STAMP_BOUND_MODEL_CONST) — overlay fields are sidecar-derived (different class), don't fit |

**Summary:** 18 PASS, 5 YELLOW, 1 N/A, 1 RED.

---

## Critical blockers (must fix before coding)

### Blocker #1 (RED): Step 4 missing call-site enumeration

Plan must enumerate all 6 call sites for FeatureOverlay verify:
- EngineSharded.hpp single-zoo boot (~1108-1131)
- EngineSharded.hpp ensemble boot (~1153-1207)
- BacktestSharded.hpp single-zoo boot (~291-307)
- BacktestSharded.hpp ensemble boot (~312-368)
- EngineSharded.hpp single-zoo hot-swap (~2772-2820)
- EnsembleHotSwap.hpp hot-swap

**Recommended structural fix:** extract `FeatureOverlay_PostLoadVerify<F>(zoo, ezoo, cfg, ...)` helper following v5.14.2.E.1's PostLoadSetup pattern. Each call site = 1 helper invocation. Adding overlay-related steps later = 1-line edit to helper.

### Blocker #2 (YELLOW): Step 3 stamp body pattern explicit

Plan must declare which stamp body field-emission pattern the 4 new fields use:
- Manual emit + parse (like v5.14.2.E.2.B architectural fields) — RECOMMENDED for current scope
- New FOREACH_STAMP_BOUND_SIDECAR registry — overkill for 4 fields, defer to TECH_DEBT-006-style refactor when count > 5

**Recommendation:** Manual; document discipline comment near field declarations (mirror v5.14.2.E.2.B pattern). Add TECH_DEBT entry when count grows.

---

## Recommendations

1. Amend Step 4 with call-site enumeration + helper extraction recommendation (PostLoadVerify shape).
2. Amend Step 3 with explicit pattern choice (manual; TECH_DEBT trigger documented).
3. Optional Step 0.5: read `CoreFrameworks/EnsembleHotSwap.hpp` + `Backtest/BacktestSharded.hpp` post-load sites before coding to confirm the helper's exact integration points.

After amendments: PROCEED TO CODING. Class 18 prevention applied; structural symmetry preserved.
