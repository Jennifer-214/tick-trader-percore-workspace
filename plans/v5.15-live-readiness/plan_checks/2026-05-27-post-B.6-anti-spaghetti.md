---
type: audit-report
audit_tier: WIDE
established: 2026-05-27
ship: post-v5.15.5.F.4d.1.B.6
scope: codebase-wide (quarterly cadence + post-B.6 subfolder-pattern viability sweep)
focus: parallel-infrastructure (Class 21 family) + subfolder-pattern viability for next-ship candidates + Class 34/35 detection
status: complete
tags: anti-spaghetti, structural-audit, framework-discipline, post-B.6, quarterly-cadence
sister_specs:
  - canonical-sister-extension-discipline.md
  - sidecar-override-pattern-for-registry-auto-flows.md
  - meta-registry-pattern-for-codebase-registry-discipline.md
  - file-size-split-discipline.md
  - feedback_count_code_loc_not_total_lines
---

# /anti-spaghetti report — 2026-05-27 — post-v5.15.5.F.4d.1.B.6 quarterly + .B.7 viability check

## Phase 1 summary — registry enumeration

**Registries enumerated: 65 FOREACH_* macro definitions** across `CoreFrameworks/` (15) + `ML_Headers/` (20) + `MemHeaders/` (16) + `Strategies/` (4) + `Backtest/` (1) + `DataStream/` (1) + `GUI/` (1) + `tools/check_per_core_registry_integrity.py` (1, infra). Meta-registry at `CoreFrameworks/MetaRegistry.hpp` enrolls 54 of them at Level-1/2 per H15/H19 topology discipline (codified `.F.4d`).

**Meta-registry health (H15+H19):**
- ROOT_NONE seed at Level 0 (FOREACH_REGISTRY itself).
- Cohort: 5 Level-2 children (FOREACH_<DOMAIN>_CFG_FLAG) parented to FOREACH_PER_CORE_DOMAIN_BITMAP.
- 1 unenrolled-but-tracked: `FOREACH_LEGACY_PREFIXED_KEY` (ModelInference.hpp:1307) — INTENTIONAL per TECH_DEBT-101 (deletion-tracked back-compat layer; absence from MetaRegistry signals "temporary infrastructure"). GREEN — discipline intact.
- `tools/check_per_core_registry_integrity.py` row is the CI tool helper (not a true registry); GREEN.

**EngineSharded subfolder split state (post-.B.6 first-canonical baseline):**
| File | Total lines | Status |
|---|---|---|
| `CoreFrameworks/EngineSharded.hpp` (INDEX shim) | 96 | conforming to subfolder INDEX pattern |
| `CoreFrameworks/EngineSharded/Boot.hpp` | 67 | well under 1500 threshold |
| `CoreFrameworks/EngineSharded/SlowPath.hpp` | 188 | well under |
| `CoreFrameworks/EngineSharded/Async.hpp` | 905 | well under |
| `CoreFrameworks/EngineSharded/Run.hpp` | 2,436 total / 1,406 code-LOC | UNDER threshold by code-LOC (per `feedback_count_code_loc_not_total_lines` discipline; matches `.B.6` B.4.1 revert decision) |

All sub-files have `// parent_index: CoreFrameworks/EngineSharded.hpp` cross-ref headers. Subfolder pattern discipline VALIDATED at first canonical.

---

## CRITICAL findings (0)

**NONE.** Codebase remains structurally clean post-`.B.6`.

The discipline-bearing infrastructure (cfg-derived consumer framework, sidecar override per H18, FOREACH_BANDIT_SIDE auto-mirror, FOREACH_STAMP_BOUND_DERIVED_COHORT meta-walker, FOREACH_LEGACY_PREFIXED_KEY back-compat layer, EngineSharded subfolder INDEX shim) is all behaving per its codified pattern. No parallel infrastructure detected at framework boundaries.

---

## HIGH findings (0)

**NONE.**

Spot-checked for canonical Class 21 patterns:
- `FOREACH_STAMP_BOUND_MODEL_CONST_PRE_CFG` vs `_POST_CFG` vs `_GROUPS` vs `_STANDALONE` vs `_UNION` — INTENTIONAL emission-order split per wire-format byte preservation discipline (header doc at line 230, 249, 279, 384, 483 explicitly explains).  GREEN — semantic distinction documented.
- `FOREACH_CFG_GATE_PER_CORE` + `FOREACH_CFG_GATE_GLOBAL` (CfgGateRegistry.hpp:77, 103) — sidecar override per H18 + canonical to PER_CORE vs GLOBAL cfg field split (intentional asymmetry; consumer pattern requires both). GREEN.
- `buy_thompson_bandits[]` vs `exit_thompson_bandits[]` (CoreModelZoo.hpp:1021, 1030) — DEFERRED hand-mirror per TECH_DEBT-084 (FOREACH_BANDIT_SIDE meta-X-macro exists; cascade rename deferred for collision-safe ordering). KEEP. Tracked, not drift.
- `FOREACH_CORE_CTX_INIT_FIELD` vs `_RESET_FIELD` — STRICT SUBSET relationship documented at line 22-25 (reset = "per-session state that should reset between operator-initiated session boundaries"). Wire format orthogonal; semantic distinction load-bearing.  GREEN.

---

## MEDIUM findings (1)

### MED-1 — Class 35 block-scope statics in EngineSharded sub-files (Run.hpp:825 + Async.hpp:425/506/507/534) — informational for `.B.7+` split candidates

**File:line citations:**
- `CoreFrameworks/EngineSharded/Run.hpp:825` — `static pthread_t g_depth_tid = 0;` (function-body scope inside `EngineSharded_Run`)
- `CoreFrameworks/EngineSharded/Async.hpp:425` — `static int save_counter = 0;` (inside async cycle body)
- `CoreFrameworks/EngineSharded/Async.hpp:506, 507, 534` — `static uint64_t prev_ring_full / prev_realloc_failed / prev_log_drops = 0;` (one-shot WARN sentinels)

**Severity rationale:** These are well-encapsulated within their containing function bodies; no current hoist hazard. Documenting per Class 35 / B18 pillar discipline as advisory for FUTURE sub-splits.

**Bearing on `.B.7`-`.B.11` umbrella:** When future ships hoist lambda bodies from container fns into sub-headers (e.g., a hypothetical Run.hpp orchestrator extraction), the M6 + B18 discipline (`feedback_enumerate_block_scope_statics_before_hoist`) applies. Pre-coding enumeration via B-Plus v0.4 `--block-scope-statics` mode mechanizes detection.

**Structural fix proposal (proportionate-response menu per `feedback_proportionate_response_to_audit_findings`):**
- (A) INLINE MERGE: N/A — these are localized cadence sentinels.
- **(B) ACCEPT WITH RATIONALE — RECOMMENDED.** Function-scope `static` for one-shot WARN sentinels + save_counter rate-limiters is idiomatic + correct. No drift surface. Documenting for B18 audit awareness only.
- (C) FOLD: N/A.
- (D) ARCHITECT NEW: would over-engineer.

**Action:** No code edit needed. Re-evaluate at any future hoist of containing function.

---

## LOW findings (summary; not enumerated)

- ~196 `namespace tt` declaration sites (codebase-wide). Spot-check confirms all match true type homes (EventLoopState, CoreContext, CoreSlowState all live in `tt::` per `ControllerEventLoop.hpp:78`; OrderManagerState lives in `tt::` per `OrderManager.hpp:223`; CoreContext used in `tt::` per CoreCtxSummaryFieldRegistry forward-decl). **NO Class 34 shadows detected** codebase-wide.
- `reinterpret_cast<>` remnants in `MemHeaders/BuddyAllocator.hpp` (6 sites) — INTENTIONAL per `void*+offset` allocator-internal arithmetic; CfgFieldDispatch.hpp:15 explicit comment confirms H13 doesn't apply here (allocator is exempt). GREEN.
- `Async.hpp:80` global-scope `struct CandleAccumulator;` forward-decl with explicit comment citing `.B.6 Phase B.3 lesson` — CORRECTLY APPLIES Class 34 closure. Pattern adoption verified.

---

## Findings bearing on `.B.7` BacktestPanels split

`.B.7` target: `Backtest/BacktestPanels.hpp` @ 6,376 total lines / >2,000 code-LOC (above hard threshold per `file-size-split-discipline.md`).

### Subfolder shape applies CLEANLY — natural panel boundaries

The file is **organized by Panel from the start**. Section banners + struct/fn boundaries:

| Panel | Lines | Structs + GUI_Panel_ fn |
|---|---|---|
| DataPanel | 36-117 | DataPanelState, DataPanel_Init, DataPanel_Scan, GUI_Panel_DataBrowser |
| RunControl | 119-516 | RunControlState, BacktestWorkerArgs, RunControl_Init, RunControl_Start, GUI_Panel_RunControl |
| Results | 517-617 | ResultsPnlColor, GUI_Panel_Results |
| PastRuns | 619-1898 | ComparisonState, PastRun, PastRunsState, PastRuns_Init, _Scan, _Load, _Delete, GUI_Panel_PastRuns |
| Comparison | 1900-2085 | Comparison_Init, _Free, _SaveRun, GUI_Panel_Comparison |
| Optimizer | 2086-2356 | OptimizerPanelState, OptWorkerArgs, optimizer_worker_fn, GUI_Panel_Optimizer |
| Training | 2357-6376 | TrainingPanelState (massive), multiple worker fns (walkforward / hp_sweep / fullvalidation / train_model / multihorizon), GUI_Panel_Training (line 4164+) |

**Recommended subfolder split shape (proportionate-response per `feedback_proportionate_response_to_audit_findings`):**
- `Backtest/BacktestPanels.hpp` (INDEX shim ~80 lines; #include sub-files in correct order)
- `Backtest/BacktestPanels/Data.hpp` (~120 lines)
- `Backtest/BacktestPanels/RunControl.hpp` (~400 lines)
- `Backtest/BacktestPanels/Results.hpp` (~100 lines)
- `Backtest/BacktestPanels/PastRuns.hpp` (~1,280 lines)
- `Backtest/BacktestPanels/Comparison.hpp` (~190 lines)
- `Backtest/BacktestPanels/Optimizer.hpp` (~270 lines)
- `Backtest/BacktestPanels/Training.hpp` (~4,020 lines — above 1500 threshold; may need a second-level split into Training_Single.hpp / Training_Multi.hpp / Training_HpSweep.hpp / Training_WalkForward.hpp)

### Differences from EngineSharded split (and how to handle)

| Concern | EngineSharded (`.B.6`) | BacktestPanels (`.B.7`) | Mitigation |
|---|---|---|---|
| Block-scope statics in hoisted functions | 6 missed → 25-arg signature post-fix (Phase B.2 fan_out) | **LOWER RISK** — panels are mostly straight ImGui render bodies; less state hoisting. Worker fns own private state via `*Args` structs (already extracted). | Run B-Plus v0.4 `--block-scope-statics` against each panel sub-file pre-split to verify. |
| Lambda-to-named-fn hoist | YES (fan_out, slow_path) | NO — panels don't have hoisted lambdas; GUI_Panel_* are already named. | N/A — simpler split. |
| Forward-decl namespace shadow risk (Class 34) | YES — encountered steady_clock + CandleAccumulator | LOWER — panels don't typically need std:: forward-decls. Possible: `ImVec4`, `ImGuiWindowFlags`, etc. — already included transitively via `imgui.h` at file top. | Include `imgui.h` at each sub-file top; NO forward-decl shortcuts. |
| Cross-include cycles | YES — Boot, Run, Async, SlowPath ordering | LOWER — each panel is mostly self-contained; deps are `imgui.h` + `BacktestEngine.hpp` + `Fingerprint.hpp` + (Training only) `BacktestSharded.hpp` | Each sub-file includes its own dep set; INDEX shim includes in panel-order. |
| Sub-file size threshold (1500 LOC) | Run.hpp 1,406 code-LOC under | **Training panel alone is ~4,020 lines** → second-level split required | Plan body must allocate a `.B.7-1` (RunControl/Data/Results/Comparison/Optimizer/PastRuns), `.B.7-2` (Training sub-split) or single-ship 2-level split. |
| C++17 inline-variable for shared state | Applied at `.B.6` (g_engine_sharded_shutdown) | UNLIKELY needed — panels own their state via *PanelState struct passed by pointer. | Audit during plan-draft: any file-static state that needs cross-TU access → C++17 `inline` per `feedback_cpp17_inline_variable_for_shared_state_across_tus`. |

### Subfolder shape VERDICT for `.B.7`

**CLEAN APPLICATION** — naturally panel-aligned; lower risk profile than EngineSharded; Training panel ALONE warrants second-level split (Run.hpp's 1,406 code-LOC was the largest sub-file at `.B.6`; Training panel's ~4,020 lines is >2.5× that threshold).

**Pre-coding recommendations for `.B.7` plan body:**
1. Plan body must enumerate Training panel sub-split shape (4 sub-files of ~1,000 lines each: `Training_Single.hpp` / `Training_Multi.hpp` / `Training_HpSweep.hpp` / `Training_WalkForward.hpp`) OR justify why ~4k LOC sub-file accepted.
2. Apply B17 pillar discipline at every sub-file: forward-decls at GLOBAL scope only (NOT inside namespace tt); prefer `#include` over forward-decl for std:: + GUI types.
3. Apply B18 pillar (B-Plus v0.4 `--block-scope-statics` enumeration) pre-coding.
4. Apply M6 parent discipline if any panel body content is hoisted (body-content arg enumeration CSV).
5. Apply C++17 inline-variable discipline for any file-shared globals (likely NONE in panels — state owned by *PanelState struct).
6. Tier classification: HIGH-RISK (multi-file split with compile-time interdependencies) → fire `/precoding-audit-gate` with 5-agent set per `feedback_tiered_audit_discipline_per_plan_scope`.

---

## Findings bearing on other queued split candidates

| Target | Total / Code-LOC est | Split-shape viability | Notes |
|---|---|---|---|
| `CoreFrameworks/ControllerEventLoop.hpp` | 3,836 total / ~1,748 code-LOC | NATURAL — at line 78 opens `namespace tt`; structs at 107, 211, 254, 604, 654, 731 = clear cluster boundaries (CoreSlowState / SlowPathTelemetry / CoreContext / WsHeartbeatTelemetry / CoreContextDisplayMeta / EventLoopState). Subfolder shape: `Types.hpp` (struct defs) + `Init.hpp` + `Tick.hpp` + `Drain.hpp` | Higher complexity than BacktestPanels (cross-include cycles with Strategies/* + ML_Headers/*). HIGH-RISK ship. |
| `ML_Headers/CoreModelZoo.hpp` | 3,092 total / ~1,852 code-LOC | NATURAL — clear sections: CoreModelZoo struct (72) / EnsembleModelZoo struct (948) / reward + drift + post-load + bandit init helpers + Thompson init helpers (separated by section banners). Subfolder shape: `Types.hpp` + `Init.hpp` + `Reward.hpp` + `Drift.hpp` + `BanditInit.hpp` + `ThompsonInit.hpp` | Cross-include with bandit registries; HIGH-RISK. |
| `Backtest/BacktestEngine.hpp` | 2,578 total / ~1,725 code-LOC | NATURAL — Data load (60-185) / Run config (187-330) / Backtest runners (335-580) / Walk-forward + HeldOutSplit (882-1370) / Optimizer + ConfigField + Sweep (2237+). Subfolder shape: `DataLoad.hpp` + `Run.hpp` + `WalkForward.hpp` + `Optimizer.hpp` | Lower complexity than ControllerEventLoop (less cross-include depth). MED-RISK. |
| `GUI/DashboardPanels.hpp` | 2,377 total / ~1,850 code-LOC | NATURAL — multiple panel boundaries at section banners (lines 68, 193, 230, 436, 888, 1127, 1175, 1449, 1554, 1643, 1697-1702). Same shape as BacktestPanels — panel-aligned. | Lower risk than BacktestPanels (smaller; less cross-build divergence). |
| `CoreFrameworks/PortfolioController.hpp` | 2,303 total / ~1,610 code-LOC | NATURAL — Controller struct (65-128) / Init (282-523) / State mutations (524-745) / Strategy dispatch (768-920) / Tick orchestrator (921-1946) / Shared functions (1947-2043) / Snapshot save/load (2044-2300). Subfolder shape: `Types.hpp` + `Init.hpp` + `Mutations.hpp` + `StrategyDispatch.hpp` + `Tick.hpp` + `Snapshot.hpp` | Cross-include with Portfolio + Strategy framework; MED-RISK. |

**Aggregate `.B.7`-`.B.11` umbrella sequencing recommendation:**

Per `feedback_no_defer_for_effort` + `feedback_motivated_collaborator_for_caramel` (best-software path) + risk-graduation:

1. `.B.7` — **BacktestPanels** (HIGHEST LOC; lowest cross-cycle complexity; natural panel boundaries) — proven pattern (`.B.6` first canonical applied identical shape to EngineSharded subfolder)
2. `.B.8` — **DashboardPanels** (sister to BacktestPanels; panel-aligned; mechanically straightforward after `.B.7` validates pattern)
3. `.B.9` — **CoreModelZoo** (MED-HIGH complexity; ML cross-include; benefits from extra discipline cycles)
4. `.B.10` — **BacktestEngine** + **PortfolioController** (paired ship if scope permits; both ~1,700 code-LOC; MED complexity)
5. `.B.11` — **ControllerEventLoop** (HIGHEST cross-include complexity; SAVE FOR LAST after panel + zoo + engine patterns validated)

---

## Pattern-codification candidates

**NONE NEW** at this audit. Existing patterns codified at `.B.6` (forward-decl global scope + block-scope statics enumeration + C++17 inline-variable + single-source-of-truth + count-code-LOC-not-total-lines) cover the relevant discipline surface for `.B.7-.B.11` umbrella.

Possible future emergence: if 2nd canonical of "Training panel sub-split with worker-fn cluster" shape arises (e.g., DashboardPanels Phase-N has a comparable mega-cluster), Stage 2 DRAFT codification of "multi-worker-fn panel sub-split discipline" warranted. Defer to 2nd instance per `feedback_proactive_novel_alternative_consideration`.

---

## Top-line verdict: **GREEN**

**Codebase is structurally clean at quarterly cadence.** No parallel infrastructure detected at framework boundaries. The 2 NEW Classes codified at `.B.6` (Class 34: forward-decl namespace shadow / Class 35: block-scope statics in hoist candidates) have ALREADY been applied at sub-file headers via documented comments at instance sites (`Async.hpp:74-80` CandleAccumulator forward-decl comment cites the lesson).

`.B.6` subfolder pattern is the first-canonical of source-code-level subfolder discipline. The `.B.7-.B.11` umbrella inherits a validated pattern + clean ground state from this audit.

Discipline-bearing infrastructure investments through `.B.4` (cfg-derived consumer framework + FOREACH_STAMP_BOUND_DERIVED_COHORT meta-walker + sidecar override per H18 + FOREACH_BANDIT_SIDE auto-mirror + FOREACH_LEGACY_PREFIXED_KEY tracked back-compat) are all behaving per codified pattern. Framework-layer payoff inflection point (per `feedback_framework_layer_payoff_diminishing_returns`) appears intact — no new framework infrastructure is warranted at this quarterly cadence.

---

## Recommendations

**For `.B.7` BacktestPanels split (next ship):**
- (1) Plan body MUST allocate 2-level split for Training panel (~4,020 lines) → either second-level subfolder OR justify single mega-sub-file at plan-draft time.
- (2) Apply B17 (forward-decl global-scope) + B18 (block-scope statics enumeration) audit pillars pre-coding via B-Plus v0.4.
- (3) HIGH-RISK ship: fire `/precoding-audit-gate` with 5-agent set per `feedback_tiered_audit_discipline_per_plan_scope`.
- (4) Bench validation: confirm `bin/foxml_suite` build still links (TrainingPanel cluster has libgomp + XGBoost dependencies that may need careful sub-file include ordering).

**For codification + memory:**
- (none) — quarterly cadence GREEN; no new disciplines emerging.

**For tracking + ledgers:**
- (none) — no findings warrant TECH_DEBT entries.

**For workspace sync:**
- This report itself → workspace via `/sync-workspace` at session close (per auto-write contract).

---

## Methodology cross-references

- Skill spec: `/anti-spaghetti` SKILL.md (Stage 3 ACTIVE v1.1; pattern lifecycle Stage 3 first canonical was `.B` audit Batch 2 2026-05-17).
- Sister registries' discipline: `DESIGN_SPECS/framework-patterns/canonical-sister-extension-discipline.md`.
- Sidecar override discipline: `DESIGN_SPECS/framework-patterns/sidecar-override-pattern-for-registry-auto-flows.md` (H18).
- Meta-registry discipline: `DESIGN_SPECS/framework-patterns/meta-registry-pattern-for-codebase-registry-discipline.md` (H15 + H19).
- Subfolder split discipline: `DESIGN_SPECS/doc-disciplines/file-size-split-discipline.md`.
- Count-code-LOC discipline: `DESIGN_SPECS/doc-disciplines/file-size-split-discipline.md` § "Threshold counting methodology" + `feedback_count_code_loc_not_total_lines`.
- B17/B18 pillars: `DESIGN_SPECS/meta-disciplines/implementation-layer-blindspot-taxonomy.md` (Stage 2 DRAFT at `.B.6` ship).
- Proportionate-response: `feedback_proportionate_response_to_audit_findings`.
- Audit-canonical-sister: `feedback_audit_canonical_sister_before_new_infra`.

**End of audit report.**
