# /dod-audit re-sweep report — v5.15.5.F.4d.1.B.3-legacy-empty-out v1.9 — 2026-05-17

**Scope:** plan-mode re-sweep (`/dod-audit plan <plan_path>`)
**Target plan:** `plans/v5.15-live-readiness/subplans/2026-05-17-v5.15.5.F.4d.1.B.3-legacy-empty-out.md` **v1.9 FULL**
**Engine HEAD:** `9b62a72` (v5.15.5.F.4d.1.B.2 ship close; Version.hpp 5.15.5.F.4d.1.B.2)
**Ship:** `v5.15.5.F.4d.1.B.3`
**Predecessor audit:** `dod-audit-2026-05-17-v5.15.5.F.4d.1.B.3.md` (against v1.2; YELLOW; 2 HIGH + 3 MED + 1 LOW)
**Reviewer:** Explore subagent for /dod-audit (Layer 2 per SKILLS_HIERARCHY)
**Type:** RE-SWEEP — v1.3-v1.9 amendments substantially expanded scope; verify v1.9 amendments follow established DESIGN_SPECS patterns + don't introduce DoD anti-patterns

---

## Catalog ingested

- **80 patterns** in `DESIGN_SPECS/*.md` (catalog walked dynamically; +4 since prior audit run)
- Patterns deeply consulted for this re-sweep (per focus area list):
  - **Focus 1 (Step 0.5d FOREACH_GATE_CFG_FLAG walker):** x-macro-registry-with-presence-dispatch, cfg-derived-consumer-framework, metadata-bit-driven-derived-filter-framework, heterogeneous-registry-pattern, canonical-sister-extension-discipline
  - **Focus 2 (Decision A Path α 12-col cascade):** universal-cfg-field-registry-pattern, registry-tuple-as-single-source-of-truth, x-macro-registry-with-presence-dispatch
  - **Focus 3 (Decision B failure-attribution-buffer-pattern Stage 3):** failure-attribution-buffer-pattern (Stage 2 DRAFT on disk verified at /home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/refactor-patterns/failure-attribution-buffer-pattern.md; 15229 bytes; v1.0 DRAFT), pattern-codification-lifecycle
  - **Focus 4 (Decision C Approach A struct-gen):** metadata-bit-driven-derived-filter-framework (Option E body verified at v1.2; lines 85-120), bitmap-overflow-protection-discipline
  - **Focus 5 (Step 1.6.6.b 15-row STAMP-side rename):** wire-format-byte-preservation-discipline Layer 4, registry-coverage-ci-check-pattern
  - **Focus 6 (Class 31/32 codification placement):** pattern-codification-lifecycle, structural-fix-preferred-decision-framework
  - **Focus 7 (Step 1.6.4.a byte-identity assertion):** wire-format-byte-preservation-discipline Layer 2 + 4, wire-format-canonical-body-invariants-helper
  - **Focus 8 (CI Check 9 compile-time static_assert):** registry-coverage-ci-check-pattern (lines 83-148; decision matrix verified)
  - **Focus 9 (149-site consumer enumeration procedure):** canonical-sister-extension-discipline, structural-fix-preferred-decision-framework
- Doc-debt findings: NONE — catalog complete for surfaces this v1.9 plan body touches

---

## Summary — v1.9 vs v1.2 delta

| Pattern | Prior (v1.2) | v1.9 Status |
|---|---|---|
| metadata-bit-driven-derived-filter-framework (Option E) | APPLIED-2; HIGH-1 risk if scope mis-counted | APPLIED-4; HIGH-1 RESOLVED (Decision D scope now 15 entries; 10 + 5 Class 32 closure) |
| failure-attribution-buffer-pattern (Stage 2 DRAFT) | MISSED — Stage 2 spec NOT on disk; HIGH-2 blocker | **APPLIED — on disk at /home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/refactor-patterns/failure-attribution-buffer-pattern.md; v1.0 DRAFT 277 lines; 3 existing canonical applications documented; Stage 3 first reference at .B.3 sequenced correctly** |
| metadata-bit-driven-derived-filter-framework Option E adherence | applied conceptually; Decision C correct path under risk if reactive flip | **APPLIED — Decision C Approach A REVERTED from reactive Approach B per Caramel pushback #1; explicit cite of Option E + § Option A anti-pattern rejection lines 34-44** |
| x-macro-registry-with-presence-dispatch (Step 0.5d new walker) | N/A — Step 0.5d didn't exist at v1.2 | **APPLIED — sister-extension to FOREACH_ML_CFG_FLAG walker at CfgGateRegistry.hpp:296-305; same arg shape; same dispatch idiom** |
| universal-cfg-field-registry-pattern (Path α global cascade) | considered at v1.2 in Decision A | APPLIED-1 (Decision A (a) chosen; Path α 12-col STORAGE_T column to 47 FOREACH_GLOBAL_CFG_FIELD rows; sister to PerCoreCfg<F> auto-gen mechanism) |
| registry-coverage-ci-check-pattern (CI Check 9 mechanism) | MED-3 — Python tool proposed; should be compile-time | **APPLIED — Step 4 reframed to compile-time static_assert per registry-coverage-ci-check-pattern.md line 144 preference order; co-located with mask declarations at CfgFieldRegistry.hpp:1079-1088; sister to H16 first canonical at :1143** |
| wire-format-byte-preservation-discipline (SOFT bump + back-compat) | MED-2 — procedure not codified | **APPLIED — Step 1.6.7.0 promotes DESIGN_SPEC amendment to Step 0 (BEFORE coding bumps apply procedure); Layer 6 Surface G sister pattern explicitly cited for parser back-compat; closed-set X-macro FOREACH_LEGACY_PREFIXED_KEY** |
| pattern-codification-lifecycle Stage 2 before Stage 3 | HIGH-2 risk | **APPLIED — Step 0 explicitly verifies Stage 2 DRAFT specs on disk before pre-coding tag; failure-attribution-buffer-pattern.md DONE at synthesis time per plan body line 284-285** |
| structural-fix-preferred-decision-framework (Class 31/32 codification) | N/A — codification not in v1.2 scope | **APPLIED — Class 31 + Class 32 NEW codified in RECURRING_BUG_PATTERNS.md at Step 9; structural fix (delete 15 prefixed entries) closes all instances + pattern codified for future detection** |
| canonical-sister-extension-discipline (12 candidates considered) | strong adherence at v1.2 | **STRENGTHENED — v1.9 expands to 12 candidates with explicit per-candidate verdict; INLINE MERGE verdict added for parser back-compat closed-set (Decision F (F.2)) per `canonical-sister-extension-discipline.md` § expanded menu** |
| bitmap-overflow-protection-discipline (struct size budgets) | MED-1 — verification not auto-asserted | **PARTIALLY APPLIED — Step 9 explicitly mentions build verify but no static_assert on sizeof(StampInferenceCfgInputs<64>) / sizeof(ModelStampResult<64>) added at v1.9; see NEW finding NF-2** |

**Tally for v1.9:** 12 APPLIED-cohort findings (vs 5 at v1.2). 0 prior HIGH findings unresolved. 1 prior MED finding (MED-1 struct-size verification) partially addressed but not fully closed. 3 NEW findings surface at v1.9-specific surfaces (procedural opportunities; not blockers).

**v1.9 is GREEN-able with 1 MED follow-through + 3 NEW low-priority findings.**

---

## Findings (severity-ordered)

### CLOSED FROM PRIOR AUDIT — v1.2 HIGH/MED resolved at v1.9

| Prior finding | v1.9 resolution | Verification |
|---|---|---|
| **HIGH-1 (Decision D scope)** | RESOLVED — scope expanded 5 → 9 → 10 → 15 entries per Caramel "reduce future headache" directive; v1.6/v1.9 explicitly enumerates 15 prefixed POST_CFG entries to delete; Class 32 FULL closure | Plan body lines 80-95 (Decision D); 317-333 (Step 1.6.2 expanded entries 11-15); verification gate line 661 |
| **HIGH-2 (Stage 2 DRAFT DESIGN_SPEC missing)** | RESOLVED — `failure-attribution-buffer-pattern.md` v1.0 DRAFT on disk at `/home/caramel/code/tick-trader-percore-workspace/DESIGN_SPECS/refactor-patterns/failure-attribution-buffer-pattern.md` (verified ls -la output; 15229 bytes); 3 existing canonical applications documented at lines 174-180; Stage 3 first reference sequenced at .B.3 Step 0.5a; Step 0 explicitly verifies DRAFT on disk before pre-coding tag | Plan body line 178; spec file existence + content (Reference implementations § lines 174-180) |
| **MED-2 (stamp_format_version procedure ordering)** | RESOLVED — Step 1.6.7.0 NEW promoted from 1.6.7.4 (codify procedure BEFORE first canonical bump); explicitly references Layer 6 Surface G + Layer 4/5b in DESIGN_SPECS amendment scope at plan body line 184; sequencing constraint at line 636-637 enforces 1.6.7.0 lands at Step 0 | Plan body line 460; sequencing constraint line 636-637 |
| **MED-3 (CI Check 9 mechanism)** | RESOLVED — Step 4 reframed to compile-time static_assert co-located with mask declarations at CfgFieldRegistry.hpp:1079-1088; sister to H16 first canonical at :1143; explicit NO Python tool clause per /dod-audit MED-3 | Plan body line 557-561; Step 9 verification gate line 663 |
| **MED-1 (struct-gen .bss bound verification)** | **PARTIALLY ADDRESSED** — Step 9 build verify CLEAN but NO explicit static_assert(sizeof(StampInferenceCfgInputs<F>) < N, ...) added; v1.9 cites .bss cost as "bounded (~5KB across both structs; .bss only)" but doesn't codify a static_assert bound | Plan body line 74; Step 9 line 583-606 (verification gate doesn't explicitly mention struct-size static_assert) |
| **LOW-1 (Decision E rationale)** | RESOLVED — Decision E rationale at line 117 cites `categorical-tag-applicability-pattern.md` for orthogonal axes; "applicability vs runtime gating" framing applied explicitly | Plan body line 117 |

**Prior audit closure rate at v1.9: 5/6 fully resolved + 1/6 partially.** MED-1 carries forward as NF-2 below.

---

### Focus Area Verdicts

#### Focus 1 — Step 0.5d FOREACH_GATE_CFG_FLAG walker pattern adherence — **GREEN**

- **Pattern sisters consulted:** `x-macro-registry-with-presence-dispatch.md` + `cfg-derived-consumer-framework.md` + `metadata-bit-driven-derived-filter-framework.md` § Option E
- **Verification at HEAD:** `CfgGateRegistry.hpp:296-305` `X_STAMP_CFG_POPULATE_ML_CFG_FLAG` macro reads `cfg.ml_cfg_flags` via BITMAP_IS_SET + emits `%s=%d\n` line. Sister walker for FOREACH_GATE_CFG_FLAG (which exists at `GateCfgFlagRegistry.hpp:46`; verified) would read `cfg.gate_cfg_flags` with MASK_GATE_CFG_* prefix. Same 6-arg shape (NAME, legacy_field, display_label, section, metadata_flags, doc).
- **Class 18 risk:** **CLEAN** — Step 0.5d.a explicitly says "copy-pattern + adapt MASK_GATE_CFG_* prefix" (line 294); same dispatch shape, different bitmap source. NOT mirror state — sister-extension that composes cleanly. Per `canonical-sister-extension-discipline.md` EXTEND verdict shape.
- **Class 21 risk:** **CLEAN** — Step 0.5d walker is a PARALLEL EMIT PASS over a sister registry (FOREACH_GATE_CFG_FLAG) that DOES NOT MIRROR FOREACH_ML_CFG_FLAG; they have distinct domains per heterogeneous-registry-pattern.md DOMAIN SPLIT (Form 2). Not the Class 21 anti-pattern (parallel-DESCRIPTOR registry mirroring SAME content).
- **Class 27 risk:** **CLEAN** — Step 0.5d.c says discrete field `int barrier_gate_enabled` on StampInferenceCfgInputs + ModelStampResult. This is NOT scalar cfg-mirror cache on subsystem state (Class 27 surface). Per `decision-time-data-binding-pattern.md`: Class 27 concerns subsystem-state caching of cfg values at decision-time without registration. The struct field here is an AUTO-GEN consumer-cfg-derived output (stamp/inference cfg struct), not a cache. Per Decision C Approach A (line 74) the struct holds all 104 master rows' fields uniformly. Pattern-application clean.
- **Pattern application adherence:** **CONSISTENT** with FOREACH_ML_CFG_FLAG walker idiom at CfgGateRegistry.hpp:296-305 + drift-check at :352-360. Step 0.5d.d explicitly says "Same dispatch extensions for `populate_inference_cfg_from_derived` + `drift_check_from_derived` template fns. Sister-pattern."

**Verdict: GREEN** — sister-extension done correctly; no parallel-mirror introduction.

#### Focus 2 — Decision A Path α 12-col cascade — **GREEN**

- **Pattern sisters consulted:** `universal-cfg-field-registry-pattern.md` + H17 PerCoreCfg<F> auto-gen
- **Cascade scope:** add STORAGE_T column to all 47 FOREACH_GLOBAL_CFG_FIELD rows (line 289 + line 51). Per Decision A (a) auto-pick rationale: "Closes H17 violation; eliminates 3-site manual storage; column symmetry with per-core" (line 51).
- **STORAGE_T column derivation:** **VERIFIED DETERMINISTIC** — per universal-cfg-field-registry-pattern.md mechanism, STORAGE_T per row maps from KIND_TOKEN (KIND_DOUBLE → FPN<F>; KIND_INT → int32_t; KIND_BOOL → uint8_t/bitmap-resident; KIND_PCT → FPN<F>). Derivable from existing row content. v1.9 plan body line 51 implies but doesn't explicitly state derivation; per universal-cfg-field-registry-pattern.md § "Adding a new cfg field requires choosing KIND_TOKEN; STORAGE_T derives".
- **Class 21 risk:** **CLEAN** — Path α is a CASCADE on existing parent registry (FOREACH_GLOBAL_CFG_FIELD), not creation of parallel-mirror. Adding column to existing tuple is sister-extension per `canonical-sister-extension-discipline.md` EXTEND verdict. NOT parallel-descriptor introduction.
- **Pattern application adherence:** **CONSISTENT** with ControllerConfig<F>'s PerCoreCfg<F> auto-gen sister mechanism. v1.9 plan body line 289 cites "sister to per-core PerCoreCfg<F> auto-gen per H17"; sequencing constraint at line 631 enforces Step 0.5b lands FIRST (before Step 1.6.1 cleanup which depends on it).

**Verdict: GREEN** — Path α cascade adheres to universal-cfg-field-registry-pattern.md sister mechanism + H17 discipline.

#### Focus 3 — Decision B failure-attribution-buffer-pattern Stage 3 first reference — **GREEN**

- **Pattern sister consulted:** `failure-attribution-buffer-pattern.md` Stage 2 DRAFT (15229 bytes on disk; v1.0; 3 canonical applications + Stage 3 first reference at .B.3)
- **Step 0.5a signature shape (verified at plan body line 288):** `cfg_derived::drift_check_from_derived` extended with `char* reason_buf, size_t reason_cap` args. **MATCHES** Stage 2 DRAFT § "Framework-extension shape (canonical at .B.3)" lines 117-139 exactly.
- **First-failure-wins idiom + nullable opt-in:** **CORRECT** — Stage 2 DRAFT § "The pattern (concrete shape)" lines 98-103 codifies `if (reason_buf && reason_buf[0] == '\0') snprintf(...);`. Plan body line 288 ("Decision B (a)") + Step 1.6.6 (line 435) reference this pattern explicitly. Caller (CoreModelZoo.hpp:225-247 per spec line 142) provides `sr.reason + sizeof(sr.reason)`.
- **Buffer size convention:** **CORRECT** — spec § "Buffer sizing convention" line 107: "256 bytes for long-form attribution... matches ModelStampResult.reason + Reconcile.refusal_reason". Plan body line 288 references the existing sr.reason buffer which is already 256 bytes at ModelInference.hpp:1166.
- **Pre-coding tag blocker:** **CORRECTLY ENFORCED** — Step 0 line 282-285 explicitly says "Verify failure-attribution-buffer-pattern.md Stage 2 DRAFT on disk (DONE at synthesis time)". Pre-coding triggers item 3 line 712 verifies "✓ Stage 2 DRAFT failure-attribution-buffer-pattern.md on disk".

**Verdict: GREEN** — Stage 2 DRAFT verified on disk; signature shape matches spec; first-failure-wins idiom correct; buffer size convention honored.

#### Focus 4 — Decision C Approach A unconditional struct-gen Option E adherence — **GREEN**

- **Pattern sister consulted:** `metadata-bit-driven-derived-filter-framework.md` § Option E (lines 85-120) + § Option A explicit rejection (lines 34-44)
- **Step 1.6.3 mechanism (line 418-422):** "replace `FOREACH_STAMP_BOUND_CFG(X)` walker with unconditional master-cfg struct-gen" — generates `uint8_t has_<name>; T <name>;` for ALL master per-core + global rows (line 74).
- **Option E adherence:** **VERIFIED CORRECT** — Option E mechanism is `cfg_compute_mask + CFG_FIELD_FOR_EACH_SET_BIT` at consumer surface. Decision C Approach A (line 74) explicitly states "matches H17 PerCoreCfg<F> precedent; aligned with `metadata-bit-driven-derived-filter-framework.md` § Option E". Verified at CfgGateRegistry.hpp:325-333 drift_check walker — uses `if constexpr (((meta) & CfgFieldDescriptor::STAMP_BOUND_CFG_DERIVED) != 0)` reduce per row, branchless mask-select per H20. Pattern-application clean.
- **Per-entry has_##name preservation (Surface G):** **EXPLICITLY HONORED** — plan body line 396, 398 explicitly states "all 15 fields retain per-entry `has_<name>` Surface G semantic via Approach A unconditional struct-gen". Plan body line 422 ("Step 1.6.3: Preserve per-entry has_<name> Surface G semantic per HIGH-CONV-I"). Surface G sister discipline preserved.
- **Decision C reactive flip recovery:** **HONORED** — Decision C is documented as REVERTED from reactive Approach B per Caramel pushback #1 (line 78). Caramel-pushback-aware recovery per `feedback_recheck_designspecs_on_pushback.md` — re-checked both options + Approach A is structurally clean per Option E; Approach B WOULD HAVE INTRODUCED Class 21 anti-pattern (line 75). Reactive flip avoided.

**Verdict: GREEN** — Approach A correctly applies Option E mechanism; Surface G preserved; reactive flip recovery codified explicitly.

#### Focus 5 — Step 1.6.6.b 15-row STAMP-side rename — **GREEN (mechanical; no DoD concern)**

- **DoD pattern relevance:** **MINIMAL** — Step 1.6.6.b is a per-row STAMP-side field-access rename (`h->inference_cfg_<name>` → `h-><name>`) across 15 rows at `CfgDriftCheckRegistry.hpp:236-311`. Mechanical replace_all-safe per `feedback_avoid_substring_replace_all_on_member_access` (full-token matches; no substring conflict; line 457).
- **Wire-format byte preservation:** **NOT IMPACTED** — STAMP-side reads happen at drift-check time (slow-path/load-time), not at canonical body emit time. Wire-format bytes are unchanged by this rename — only the C++ field access path. Per `wire-format-byte-preservation-discipline.md` Layer 4: wire format depends on EMIT semantics, not READ semantics.
- **New DESIGN_SPEC opportunity:** **NO** — this is a one-off mechanical rename driven by Decision D scope expansion (10 → 15 entries with column-1 alignment). Not a recurring pattern needing its own spec. The general procedure (rename consumer-side field access when source registry column-1 changes) is covered by existing `feedback_enumerate_consumers_before_registry_row_deletion.md` + comprehensive cross-grep procedure (Focus 9 below). NO new DESIGN_SPEC warranted.
- **Pattern application adherence:** **CONSISTENT** — Step 1.6.6.b honors `feedback_avoid_substring_replace_all_on_member_access` (full-token matches verified; ~30-60 min mechanical per line 457).

**Verdict: GREEN** — mechanical rename; correctly characterized in plan body; no new DESIGN_SPEC opportunity.

#### Focus 6 — Class 31 + Class 32 NEW codification — pattern catalog placement — **GREEN**

- **Class 31** (Wire-format duplicate-key emit from sister registries): **PLACEMENT CORRECT** — Class 31 codified in `DOCS/RECURRING_BUG_PATTERNS.md` at ship close (line 595). Per `structural-fix-preferred-decision-framework.md`: bug classes catalogued in RECURRING_BUG_PATTERNS.md once recurrence is demonstrated. 10 instances closed at this ship + ≥2 recurrence history (PARITY-026 + this ship) qualifies for codification. Detection mechanism tracked via TECH_DEBT-102 (closed at ship close per line 696 — `/parity-check` skill spec amendment batches with other skill amendments).
- **Class 32** (Prefixed/unprefixed struct field mirror in X-macro auto-gen registry; column-1 wire-key name ≠ get_value source name): **PLACEMENT CORRECT** — Class 32 codified in `DOCS/RECURRING_BUG_PATTERNS.md` at ship close (line 595). 15 instances closed at this ship FULL closure (per v1.6 scope expansion). Existing X-macro auto-gen discipline (column-1 should equal source name) is sufficient — detection happens via column-1 vs source name asymmetry check at registry-author time. No separate `prefix-asymmetry-prevention.md` DESIGN_SPEC needed because the discipline IS X-macro registry hygiene already.
- **DESIGN_SPEC vs RECURRING_BUG_PATTERNS placement:** **CORRECT** per `pattern-codification-lifecycle.md`:
  - **DESIGN_SPECS** = positive patterns + canonical shapes (what to DO)
  - **RECURRING_BUG_PATTERNS** = bug classes + detection signatures (what NOT to do)
  - Class 31 + Class 32 are BUG CLASSES (anti-patterns) → RECURRING_BUG_PATTERNS placement correct
- **`prefix-asymmetry-prevention.md` DESIGN_SPEC opportunity:** **NOT NEEDED** — the discipline is already embedded in canonical X-macro registry hygiene (column-1 == source name == display label tag). Plus `feedback_enumerate_consumers_before_registry_row_deletion.md` covers consumer-side detection. Plus pre-coding `/readiness` Check 18 (sibling X-macro coverage cross-check) catches column-1 vs source asymmetry pre-coding. Adding a separate DESIGN_SPEC would duplicate discipline already covered.

**Verdict: GREEN** — both classes placed correctly in RECURRING_BUG_PATTERNS.md; no new DESIGN_SPEC needed.

#### Focus 7 — Step 1.6.4.a explicit byte-identity assertion — **GREEN**

- **Pattern sister consulted:** `wire-format-byte-preservation-discipline.md` Layer 2 (locale-pin) + Layer 4 (round-trip HMAC) + `wire-format-canonical-body-invariants-helper.md`
- **Byte-identity assertion shape (line 428):** "Add explicit byte-identity assertion at Step 1.6.4 test fixture that verifies canonical body output bytes EXACTLY match expected (compile-time string literal of canonical body emitted by framework walker for known cfg state)". **MATCHES** existing test discipline at HEAD (per `wire_format_invariants.hpp` discipline).
- **Layer 2 locale-pin alignment:** **CORRECT** — Step 1.6.4.a explicitly states "outer + inner are FUNCTIONALLY CORRECT (newlocale creates separate locale_t; uselocale(prev) restores outer's pin) but redundant. NOT a correctness hazard." (line 426). Verifies Layer 2 discipline + tracks LOW-priority optimization via TECH_DEBT-103 (line 697).
- **Sister to wire-format-canonical-body-invariants-helper (I1-I5):** **APPLIED** — Step 1.7 NEW (line 539-543) invokes `run_wire_format_canonical_body_invariants(ctx)` at first non-zero cohort context — verifies framework walker output satisfies I1-I5 invariants per the helper. Sister to I3 (no comma decimals; locale pin) directly cited at line 428.

**Verdict: GREEN** — byte-identity assertion shape matches existing test discipline; Layer 2/4 cited explicitly; sister to invariants helper applied at Step 1.7.

#### Focus 8 — CI Check 9 (compile-time static_assert) — **GREEN**

- **Pattern sister consulted:** `registry-coverage-ci-check-pattern.md` § "Mechanism choice: compile-time vs runtime" (lines 83-148; decision matrix)
- **Step 4 mechanism (line 557-561):** "compile-time static_assert per `/dod-audit` MED-3"; concrete shape: `static_assert(cfg_field_count(g_per_core_cfg_stamp_bound_cfg_derived_mask.words) + cfg_field_count(g_global_cfg_stamp_bound_cfg_derived_mask.words) >= 24, "STAMP_BOUND_CFG_DERIVED coverage regression");` co-located with mask declarations at CfgFieldRegistry.hpp:1079-1088.
- **Co-location with mask declarations:** **CORRECT per H16 discipline** — H16 first canonical at CfgFieldRegistry.hpp:1143 co-locates `FOREACH_DERIVED_FILTER` reduction with metadata bit declarations. CI Check 9 sister placement at :1079-1088 (where the masks are declared via X_GEN_*_MASK X-macro) follows the same pattern. Per `registry-coverage-ci-check-pattern.md` line 116 "Single file — no separate tools/ script to maintain or invoke" (preference compile-time).
- **Decision matrix verification:** **CORRECT** — Step 4 source data IS X-macro-driven (metadata_flags column in FOREACH_PER_CORE_CFG_FIELD + FOREACH_GLOBAL_CFG_FIELD + FOREACH_ML_CFG_FLAG); predicate IS expressible as bitmap reduction; result IS evaluable at compile time. Per decision matrix line 141 "compile-time static_assert (preferred)".
- **Should CI Check 9 be in cfg-derived-consumer-framework.md DESIGN_SPEC?:** **NO** — already covered by `registry-coverage-ci-check-pattern.md` Shape A (positive coverage) discipline. Cross-reference in cfg-derived-consumer-framework.md v1.3 amendment (per line 186) "Production walker migration discipline" section would suffice if amendment needs it; standalone DESIGN_SPEC not warranted.

**Verdict: GREEN** — CI Check 9 mechanism correct; co-location placement aligned with H16; no new DESIGN_SPEC needed.

#### Focus 9 — 149-site consumer enumeration procedure — DESIGN_SPEC opportunity? — **YELLOW → see NF-1**

- **Procedure characterization (plan body lines 363-388):** comprehensive `rg` for ALL access patterns (dot/arrow/STAMP_HAS/token-paste) codebase-wide; per-field full-token replace_all sweep; build verify each. 149 total sites enumerated across 8 files. Per `feedback_enumerate_consumers_before_registry_row_deletion.md` (codified 2026-05-17 at v1.8).
- **DESIGN_SPEC vs skill/procedural amendment:** **MIXED** — the procedure is partly codified in:
  - `feedback_enumerate_consumers_before_registry_row_deletion.md` (memory)
  - `/trace-deps` skill amendment (per plan body line 599-601: "ALWAYS produce comprehensive consumer enumeration as standard trace output when scope includes registry-row deletion / rename / column-1-vs-source-name change")
  - `/precoding-audit-gate` skill amendment (per line 601: "orchestrator synthesis step verifies per-deletion-scope-field consumer enumeration done")
  - `/readiness` Check 34 NEW (per line 598-599: "comprehensive consumer enumeration for registry-row-deletion scope")
- **DESIGN_SPEC opportunity:** **YES — but NOT urgent at this ship** — see NF-1 below. The procedure is a recurring pattern (Class 32 demonstrates it; future column-1-rename cohorts will need it). A dedicated DESIGN_SPEC `consumer-enumeration-discipline-for-registry-row-deletion.md` would consolidate the discipline + cross-reference the 4 skill amendments + the feedback memory. **NOT a v1.9 blocker** — skill amendments + memory + plan body procedure capture sufficient at this ship; DESIGN_SPEC codification deferrable to .C ship or post-paper-test ship.

**Verdict: YELLOW (minor)** — procedure currently captured in skill amendments + feedback memory + plan body; DESIGN_SPEC codification opportunity exists but deferrable.

---

### NEW Findings (vs prior v1.3 audit)

#### NF-1 (LOW) — `consumer-enumeration-discipline-for-registry-row-deletion.md` DESIGN_SPEC opportunity

- **Pattern:** `pattern-codification-lifecycle.md` Stage 1→2 promotion when pattern has 2+ recurrence demonstrated
- **Plan section:** Step 1.6.2 lines 363-388 (149-site procedure) + Step 9 line 599-601 (/trace-deps skill amendment to ALWAYS produce comprehensive consumer enumeration)
- **Symptom:** the procedure (one comprehensive `rg` covering ALL access patterns codebase-wide; per-field full-token replace_all + build verify each) is mature enough to codify as DESIGN_SPEC. Currently distributed across `feedback_enumerate_consumers_before_registry_row_deletion.md` memory + 4 skill amendments. A unified DESIGN_SPEC would (a) provide single canonical reference for future operator + agent consultation, (b) be discoverable via DESIGN_SPECS catalog walker (which `/dod-audit` uses), (c) reduce skill-amendment surface area (skills reference the spec; spec is single source of truth).
- **Why LOW:** the skill amendments + feedback memory are functionally sufficient at this ship. DESIGN_SPEC codification adds polish + discoverability; not load-bearing for `.B.3` correctness.
- **Suggested fix:** Defer to `.C` ship or post-paper-test ship per `feedback_framework_layer_payoff_diminishing_returns.md` (framework discipline payoff curve at this ship is at consolidation phase; new DESIGN_SPEC at this ship would be light-touch but not high-yield).
- **Effort estimate:** ~1-2h (Stage 2 DRAFT writing; cross-reference 4 skill amendments + feedback memory)
- **Cross-ref:** `feedback_enumerate_consumers_before_registry_row_deletion.md`; `pattern-codification-lifecycle.md` Stage 1→2

#### NF-2 (LOW carry-over from prior MED-1) — Decision C Approach A struct-gen .bss budget verification

- **Pattern:** `bitmap-overflow-protection-discipline.md` (co-located static_assert pattern for compile-time bounds) + `structural-fix-preferred-decision-framework.md` (verify cleanly)
- **Plan section:** Decision C Approach A row (line 74) + Step 9 verification gate (line 583-606)
- **Symptom:** v1.9 cites .bss cost "bounded" but no explicit `static_assert(sizeof(StampInferenceCfgInputs<64>) < N, ...)` + `static_assert(sizeof(ModelStampResult<64>) < N, ...)` at Step 9 verification. Future cohort growth (24 → 104 over ships) extends struct uniformly; static_assert at first-canonical bounds the budget mechanically.
- **Why LOW (downgrade from prior MED-1):** v1.9 .bss cost narrative is more honest (line 74 "bounded .bss cost (~5KB across both structs; .bss only)"). Build-time verification catches regressions; explicit static_assert is polish vs blocker.
- **Suggested fix:** Optional during Step 9; add `static_assert(sizeof(StampInferenceCfgInputs<64>) < 8192, "struct-gen .bss budget exceeded; reconsider Approach B");` + `static_assert(sizeof(ModelStampResult<64>) < 8192, "...");` co-located with struct definition at `ModelInference.hpp` after the X-macro struct-gen.
- **Effort estimate:** ~10 min static_assert addition
- **Cross-ref:** `DESIGN_SPECS/refactor-patterns/bitmap-overflow-protection-discipline.md` (co-located static_assert pattern; sister application to struct-size budgets)

#### NF-3 (LOW) — Plan body Step 0.5d.c discrete-field vs bitmap-read consistency note could cite `decision-time-data-binding-pattern.md` § "Subsystem reads cfg via single decision path"

- **Pattern:** `decision-time-data-binding-pattern.md` (subsystem cfg reading discipline)
- **Plan section:** Step 0.5d.c (line 296)
- **Symptom:** Step 0.5d.c chooses "Discrete field preferred for consistency with current `CoreModelZoo.hpp:402-403` access pattern + Decision D mechanism 1 sister fields". This is correct — it preserves the consumer-side read pattern (one place reads `inf.barrier_gate_enabled` rather than dispatching across two paths). Adding an explicit cite to `decision-time-data-binding-pattern.md` § "Subsystem reads cfg via single decision path" would tie the choice to the canonical decision-time discipline.
- **Why LOW:** the choice is correct; missing cite is documentation polish. Not load-bearing for `.B.3` correctness.
- **Suggested fix:** Optional v1.10 plan body amendment: "Per `decision-time-data-binding-pattern.md` § subsystem-cfg-read-via-single-decision-path: discrete struct field preferred over conditional bitmap-vs-scalar dispatch — single read path per consumer."
- **Effort estimate:** ~3 min plan body amendment
- **Cross-ref:** `DESIGN_SPECS/refactor-patterns/decision-time-data-binding-pattern.md`

---

## Anti-pattern instance verification at v1.9

Per Caramel pushback discipline. Each focus area cross-checked against Class 18/21/27/32:

| Class | v1.9 risk | Verdict |
|---|---|---|
| Class 18 (Mirror state/code) | Step 0.5d new walker + Decision D 15-entry deletion + Decision C Approach A struct-gen | **CLEAN** — sister-extension (not mirror); deletion eliminates 15 mirror instances; unconditional struct-gen is Option E mechanism (not parallel-mirror walker) |
| Class 21 (Multiple parallel descriptors) | Decision C Approach A unconditional struct-gen; Decision D 15 prefixed POST_CFG deletion | **CLEAN** — Approach A REVERTED from reactive Approach B which WOULD HAVE introduced Class 21 (parallel-FILTER-macro); Decision D eliminates 15 mirror-descriptor instances |
| Class 27 (Scalar cfg-mirror cache on subsystem state) | Step 0.5d.c discrete `int barrier_gate_enabled` on inf struct | **CLEAN** — inf struct is consumer-cfg-derived OUTPUT (auto-gen from master registry filtered by metadata bit), NOT subsystem-state cache. Not the Class 27 surface. Per `decision-time-data-binding-pattern.md` § Subsystem-state-cache anti-pattern definition. |
| Class 32 (Prefix asymmetry in X-macro auto-gen registry) | 10 → 15 instance closure at this ship | **CLEAN** — full closure at v1.6 scope expansion; ZERO surviving instances at cfg-derived stamp surface; pattern CODIFIED at ship close (Step 9 line 595) |
| Class 8 (User-configurable feature silently inactive in sharded) | per_horizon_barrier_blend bit-add | **CLEAN** — plan body line 409 explicitly verifies "StrategyParameters.hpp:1348-1349 reads BITMAP_IS_SET unconditionally in sharded per-core decision path; bit-add doesn't change evaluation" |

**Verdict: NO anti-pattern instances introduced at v1.9.**

---

## Inflection assessment — DoD pattern application clean at v1.9 OR opportunities remaining?

Per `feedback_framework_layer_payoff_diminishing_returns.md`:

- **Sites added vs eliminated at this ship:**
  - Added: 1 new framework walker (Step 0.5d for FOREACH_GATE_CFG_FLAG)
  - Eliminated: ~149 consumer sites + 15 prefixed POST_CFG entries + 2 legacy registry bodies (FOREACH_STAMP_BOUND_CFG + FOREACH_CFG_DERIVED_INFERENCE_CFG) + 1 STAMP_CFG_AUTOPOPULATE macro body + 1 FOREACH_STAMP_BOUND_CFG_COUNT constant + 2 production walker + ~12 test consumer migrations
  - **Net: -180:+1 ratio** → consolidation phase; pattern-application discipline at peak yield
- **Iteration spiral status:** plan body went v1.2 → v1.9 (7 iterations). v1.8 codified consumer-enumeration meta-gap per `feedback_iteration_spiral_signals_audit_meta_gap.md`. v1.9 closed `tools/stamp_model.sh` final hit (line 524-533) — comprehensive consumer enumeration applied + inflection reached.
- **DoD pattern-application opportunities remaining at v1.9:** **NONE BLOCKING.** 3 NEW findings (NF-1 NF-2 NF-3) are all LOW polish + deferrable.

**Inflection: REACHED at v1.9.** v1.9 represents the consolidation-phase end for `.F.4d.1.B` umbrella. `.C` ship (sidecar override + bit-packed inventory) per `subplans/2026-05-16-v5.15.5.F.4d.1.C-sidecar-bitpacked.md` continues consolidation at sidecar surface. Post-`.F.4d.1` umbrella close, framework-layer payoff curve flattens per `feedback_framework_layer_payoff_diminishing_returns.md`; shift to code-moving maintainability wins.

---

## Verdict: GREEN

**v1.9 is GREEN-able with no blockers.**

- **0 HIGH findings** (vs 2 at v1.2 — both resolved)
- **0 MED findings as blockers** (vs 3 at v1.2 — 2 resolved + 1 partially resolved + downgraded to LOW NF-2)
- **3 LOW findings** (NF-1 DESIGN_SPEC codification opportunity; NF-2 carry-over struct-size static_assert polish; NF-3 documentation cite polish) — all deferrable

GREEN because:
- All prior HIGH findings (Decision D scope; Stage 2 DRAFT DESIGN_SPEC) are RESOLVED at v1.9
- All v1.9 NEW scope (Step 0.5d FOREACH_GATE_CFG_FLAG walker; Decision A Path α cascade; Decision B Stage 3 first reference; Decision C Approach A struct-gen; Decision F SOFT compat; Step 1.6.6.b 15-row STAMP-side rename; Step 1.6.8 stamp_model.sh) follows established DESIGN_SPECS patterns
- NO new Class 18/21/27/32 anti-pattern instances introduced (verified at Anti-pattern instance verification section above)
- Inflection reached at v1.9 per iteration-spiral discipline

**Recommendation: PROCEED to pre-coding tag.** NF-1/NF-2/NF-3 are deferrable — capture as TECH_DEBT entries or fold into `.C` ship as polish items, NOT blockers for `.B.3` pre-coding tag.

---

## Notes for /precoding-audit-gate synthesis

v1.9 re-sweep should compose with parallel re-sweeps of /merge-scan + /parity-check + /readiness + /trace-deps + /anti-spaghetti + /accounting-audit. Cross-skill correlation hypotheses:

- NF-1 (consumer-enumeration DESIGN_SPEC opportunity) likely surfaces in /readiness Check 34 verification + /trace-deps skill amendment item
- NF-2 (struct-size budget static_assert) likely surfaces in /hft-audit (cache-line / .bss budget concern)
- NF-3 (decision-time-data-binding cite) is documentation-only; unlikely to surface elsewhere

If 2+ skills converge on same NF finding, escalate severity per `/precoding-audit-gate` synthesis convention. Otherwise NF-1/NF-2/NF-3 remain LOW deferrable polish.

---

**End of /dod-audit re-sweep report v1.9.** v1.9 GREEN; READY for operator final greenlight + pre-coding tag per Pre-coding triggers item 11.
