---
type: plan-check
audit_kind: multi-plan-cohesion
target_set: v5.15.5.F.4d.1.B.4 + .B.5 through .B.11 maintenance umbrella
audit_date: 2026-05-25
engine_head: 64e7101
sprint: v5.15-live-readiness
verdict: YELLOW
---

# /plan-check report — v5.15-live-readiness maintenance umbrella — 2026-05-25

## Plan summary

8 sub-ship planning artifact set audited:

- **`.B.4`** train-serve execution-layer parity (NEW `CoreFrameworks/EngineCommon.hpp` shared helpers; closes 4 CRIT + 3 HIGH PARITY entries structurally; M5 meta-discipline first canonical) — MED-HIGH risk; ~2-3 days
- **`.B.5`** `controller_test.cpp` (26,169 lines / 3,208 tests) → 5 domain-aligned sub-files via function-exposed pattern + thin main wrappers — HIGH risk; ~1-2 days
- **`.B.6`** `CoreFrameworks/EngineSharded.hpp` → subfolder split (first canonical of subfolder pattern; codifies `file-size-split-discipline.md` v1.1) — LOW-MED risk; ~1 day
- **`.B.7`** `Backtest/BacktestPanels.hpp` (6,376 lines) panel-per-file subfolder — LOW-MED; ~1-1.5 days
- **`.B.8`** `ControllerEventLoop.hpp` + `ControllerConfig.hpp` bundle subfolder — MED; ~1.5-2 days
- **`.B.9`** `CoreModelZoo.hpp` + `BacktestEngine.hpp` bundle subfolder — LOW-MED; ~1.5-2 days
- **`.B.10`** `DashboardPanels.hpp` + `PortfolioController.hpp` + `EngineTUI.hpp` bundle subfolder + marginal-4 TRIM evaluation → TECH_DEBT-029 FULL CLOSE — LOW-MED; ~1.5-2 days
- **`.B.11`** ledger re-split + Class 34 codification + umbrella postmortem — LOW; ~0.5 day

**Umbrella total:** ~10-13 days (`.B.4`-`.B.11` sum); roadmap-amended sprint runway ~22-36 days to paper-test.

**Integration matrix highlights:**
- Strict serial sequencing; zero parallel ships; per-sub-ship audit→consult→implement→ship cycle preserved
- Hot path UNTOUCHED across all 8 ships (declared per ship + mechanical reorg by construction)
- 3,208 baseline tests preserved across all 8 ships (`.B.4` may add 5-20 regression tests)
- `file-size-split-discipline.md` v1.0 DRAFT → v1.1 AMENDED at `.B.6` ship close; `.B.7-.B.10` consume v1.1 mechanically
- TECH_DEBT closures: -119 at `.B.4`; -114 at `.B.5`; -029 PARTIAL @`.B.6`-`.B.9` → FULL CLOSE @`.B.10`; -118 verify-close @`.B.11`; NEW Class 34 codified @`.B.11`

---

## Per-sub-plan readiness verdicts

| Plan | Verdict | PASS | GAP | DRIFT-RISK | DEFERRED | Must-fix |
|---|---|---|---|---|---|---|
| `.B.4` train-serve | GREEN-with-amendments | 23 | 2 | 1 | 3 | 1 |
| `.B.5` test split | YELLOW | 22 | 3 | 1 | 0 | 2 |
| `.B.6` EngineSharded subfolder (first canonical) | YELLOW | 24 | 2 | 2 | 0 | 2 |
| `.B.7` BacktestPanels subfolder | GREEN | 26 | 1 | 1 | 0 | 0 |
| `.B.8` CoreFrameworks bundle | YELLOW | 23 | 2 | 2 | 0 | 1 |
| `.B.9` ML+Backtest bundle | GREEN | 26 | 1 | 1 | 0 | 0 |
| `.B.10` GUI+Frameworks bundle | YELLOW | 22 | 2 | 2 | 1 | 1 |
| `.B.11` ledger re-split + Class 34 | GREEN | 27 | 0 | 1 | 0 | 0 |

### Per-plan must-fix detail

**`.B.4` must-fix (1):**
- M-1 (MEDIUM): The "15+ explicit Mirrors EngineSharded_Run lines X-Y citations" claim is exaggerated. Actual `rg "Mirrors" Backtest/BacktestSharded.hpp` returns **12 hits** (4 are non-EngineSharded-Run references). True count of "Mirrors EngineSharded_Run lines" specific citations is ~5-8. Plan body should correct to "12 Mirror citations total in BacktestSharded.hpp; ~5-8 specifically cite EngineSharded_Run line ranges" — keeps argument intact (Class 18 mirror still demonstrated by accumulated PARITY evidence) but removes exaggeration. NON-BLOCKING — argument doesn't require inflated count.

**`.B.5` must-fix (2):**
- M-2 (HIGH): Decision E "section assignment ambiguity" tiebreaker is too vague. With 3,208 tests at risk, ambiguity-by-judgment-during-extract introduces drift risk. PROPOSED FIX: Before Phase B starts, fire `rg "TEST_SECTION\("` against the current monolith + produce one-time CSV of (section_name, line_range, primary_domain_guess) → operator triages ambiguous ones AHEAD of extract → Phase B has deterministic mapping. ~30 min Phase A work prevents per-section judgment at extract time.
- M-3 (MED): Per-binary `tests_failed` counter discipline in Decision C is unclear. The plan says "shared global counter in test_common.hpp" but with 5 sub-binaries each `#include`ing test_common.hpp, each binary has its OWN copy of the global (TU-local). Need either: (a) `extern int tests_failed;` declaration in test_common.hpp + ONE definition in one .cpp file; OR (b) `inline int tests_failed = 0;` (C++17 inline variable). Phase A should resolve this; Decision C amendment recommended.

**`.B.6` must-fix (2):**
- M-4 (HIGH): Direct-include enforcement `#error` guard pattern in Decision C is good but ENGINESHARDED_VIA_SHIM define discipline has a subtle gap: the shim does `#define ENGINESHARDED_VIA_SHIM 1` then `#include`s sub-files then `#undef ENGINESHARDED_VIA_SHIM`. **But sub-files use `#pragma once`** — second include of the shim in same TU would NOT re-trigger `#define` (shim is `#pragma once`'d via implicit pragma — actually shim has explicit `#pragma once` per Decision C). However, when shim itself is included multiple times via transitive deps (`A.hpp #include shim; B.hpp #include shim; C.hpp #include A + B`), `#pragma once` means shim is included ONCE → `#define ENGINESHARDED_VIA_SHIM` happens once → sub-files compile cleanly via #include from shim → guard works. **VERIFY: are sub-files ever included transitively WITHOUT going through the shim?** If yes, the guard fires (correct) but the compile error message is the user-visible safety net. Phase A `/blindspot-scan` (B7 include topology check) catches this.
- M-5 (MED): Decision A "Helpers.hpp threshold" rule is judgment-call. Suggest: if Helpers post-`.B.4` extract < 200 LOC, fold into Run.hpp; >= 200, separate sub-file. Make threshold explicit.

**`.B.7` must-fix:** None. Plan body is thorough; ImGui state coupling concerns surfaced as pre-coding concerns; pattern application after `.B.6` first canonical.

**`.B.8` must-fix (1):**
- M-6 (HIGH): Decision D "ControllerConfig<F> struct layout preservation" cites `static_assert(sizeof(ControllerConfig<int64_t>) == EXPECTED_SIZE);` discipline but does NOT actually require capturing the BASELINE size at Phase A pre-coding. **Without baseline capture, the static_assert has no value** — there's nothing to compare against. ADD to Phase A Step A.6 (or B.0): capture `sizeof(ControllerConfig<int64_t>)` + `offsetof()` for critical cross-thread fields BEFORE split + record as BASELINE constants in Struct.hpp (or sister test helper). Then post-split static_asserts validate against captured baseline. NON-NEGOTIABLE per H6 cross-thread invariant.

**`.B.9` must-fix:** None. Plan body has explicit HMAC byte preservation verification at StampEmit.hpp split (Phase C Step C.6 round-trip test). Strong.

**`.B.10` must-fix (1):**
- M-7 (MED): Decision G CLEANUP-001 fold-in evaluation criterion ("if parser refactor surfaces cleanly during TRIM inspection AND scope manageable ~2-3h work") is fuzzy. The decision could be deferred indefinitely behind "doesn't surface cleanly". Strengthen: **Set hard cutoff** — if `verify_model_stamp` parser is the bulk of ModelInference.hpp's over-threshold content (likely yes; current 1,998 LOC; parser is ~700 LOC of if-else chain per CLEANUP-001 backlog), CLEANUP-001 fold-in IS the right TRIM. Reframe Decision G: default = fold-in; defer ONLY if specific operational reason surfaces during Phase A.

**`.B.11` must-fix:** None. Good closure ship; lighter audit gate appropriate; honest postmortem framing.

---

## Cross-plan integration findings

### 3.a — Files touched conflicts

**Multi-plan touches with conflicting scopes:** ZERO. Each file is owned by exactly one ship:
- `EngineSharded.hpp` → `.B.4` REDUCES via EngineCommon extract; `.B.6` SPLITS what's left. Sequencing strictly preserved (`.B.4` predecessor of `.B.6` in roadmap + umbrella).
- `ControllerConfig.hpp` → `.B.8` only; no other ship touches.
- `controller_test.cpp` → `.B.5` only.
- `BacktestPanels.hpp` → `.B.7` only.

**NEW finding — file ownership ambiguity:** `Backtest/BacktestSharded.hpp` is implicitly touched by `.B.4` (Phase C.2 step calls EngineCommon helper + deletes mirror comments). It's at 1,009 lines (under threshold). NOT in any maintenance ship's file-touch list. **Acceptable** — `.B.4` is a separate train-serve concern, not file-size; touch is contained.

### 3.b — Pattern codification timing

**`file-size-split-discipline.md` v1.0 → v1.1 timing:**
- v1.0 DRAFT lives at workspace/DESIGN_SPECS/doc-disciplines/ today (verified)
- v1.1 amendment lands at `.B.6` ship close per `.B.6` Phase D Step D.1
- `.B.7-.B.10` cite v1.1 mechanically (verified per plan body cross-refs)

**OK with one caveat:** `.B.5` plan body cites `file-size-split-discipline.md (Stage 2 DRAFT v1.0; applied here)` — `.B.5` ships BEFORE `.B.6` so v1.0 application is correct. But `.B.5` test split shape (flat-sibling + thin main wrappers) is NOT subfolder. `.B.5` applies the FLAT-SIBLING pattern (already codified in v1.0); subfolder is `.B.6` first canonical for SOURCE headers. Plan body is correct; pattern timing is correct.

**Class 34 codification timing:**
- `.B.5`-`.B.10` plan bodies say "NEW Class 34 candidate" / "umbrella's NEW class"
- `.B.11` actually CODIFIES Class 34 (per Decision D + Phase C.1)
- **OK** — candidate-vs-codified framing preserved correctly

### 3.c — Cfg field / X-macro registry conflicts

- **ZERO new cfg fields** added across the entire 8-ship umbrella (mechanical reorg only). Verified.
- **`.B.8` ControllerConfig split touches X-macro registries** — preserves macro definition→consumption order discipline via shim include order (Decision F + Decision B Struct.hpp first in include sequence). Direct-include `#error` enforcement per `.B.6` codification prevents downstream consumers from bypassing shim → correct macro expansion preserved.
- **`.B.9` `FOREACH_ENSEMBLE_POST_LOAD` walker** — PostLoad.hpp consumes; Ensemble.hpp (or Types.hpp) defines. Plan body Pre-coding concern #6 explicitly addresses include order. OK.

### 3.d — Architectural invariant preservation

**Hot path UNTOUCHED:** Declared in every plan body + verified by `tools/calls_graph_diff.sh` in each ship's verification gate. GREEN.

**H6 cluster alignment preservation:**
- `.B.8` ControllerConfig<F> — Decision D requires `sizeof` + `offsetof` static_asserts; **GAP per M-6 above** — must capture baseline at Phase A
- `.B.10` `EngineTUI::PerCoreSnap` — Decision C requires struct stays whole in Snapshot.hpp + static_asserts; correctly framed
- `.B.6` EngineSharded — struct definitions stay in dedicated sub-file per R5 mitigation; sufficient

**HMAC byte preservation (H9):**
- `.B.9` BacktestEngine/StampEmit.hpp — explicit Phase C.6 round-trip test required. GREEN.
- `.B.4` EngineCommon helper extract — Phase A pre-coding concern #3 explicitly notes; verified at Phase C.5 `/parity-check` post-migration. GREEN.

**H8 latency budgets:** Mechanical reorg; preserved by construction. GREEN.

**3,208 tests preserved:**
- `.B.4` says "3229 → 3229+" — implies pre-`.B.4` baseline is 3229. Engine HEAD at `64e7101` is `.B.3` ship close with **3208 tests** per CLAUDE.local.md sprint state. `.B.4` plan body has stale baseline (3229 likely a typo or reflects expected post-`.B.4` additions). **DRIFT-RISK** — non-blocking but should reconcile: actual baseline is 3208; `.B.4` should target 3208 → 3208+ (with new regression tests).
- `.B.5` says "3,208 tests" — correct baseline.
- `.B.6`-`.B.11` say "3,208" — correct.

### 3.e — Dependency edge validation

**Verified dependency edges:**

```
.B.4 (predecessor) ── .B.5 ── .B.6 ── .B.7 ── .B.8 ── .B.9 ── .B.10 ── .B.11
       │                       │
       │                       └─ codifies subfolder pattern v1.1
       │
       └─ extracts EngineCommon (reduces EngineSharded ~700-800 LOC)
          → .B.6 splits what's left (sequencing CRITICAL)
```

- **`.B.4` predecessor of `.B.5`** — declared; `.B.5` doesn't structurally depend on `.B.4`'s helpers but per-sub-ship cycle requires ordering
- **`.B.4` predecessor of `.B.6`** — `.B.4` REDUCES EngineSharded.hpp (~3,793 → ~3,000 LOC); `.B.6` plan body verifies "post-`.B.4` size ~3,000 LOC" — sequencing correct
- **`.B.6` predecessor of `.B.7`-`.B.10`** — codifies subfolder pattern Stage 3 first canonical; viability gate per umbrella Decision G provides HALT path if pattern fails at first canonical — STRONG
- **`.B.10` predecessor of `.B.11`** — TECH_DEBT-029 FULL CLOSE feeds into `.B.11` umbrella postmortem
- **Zero cycles**, zero backwards-dependencies. GREEN.

### 3.f — Test count progression

| Ship | Baseline | Expected delta | Expected post |
|---|---|---|---|
| Pre-`.B.4` (current HEAD 64e7101) | 3,208 | — | 3,208 |
| `.B.4` | 3,208 | +5-20 regression | 3,213-3,228 |
| `.B.5` | 3,208 (or +.B.4 delta) | 0 (test reorg only) | 3,208-3,228 |
| `.B.6`-`.B.11` | preserved | 0 (mechanical reorg) | preserved |

**Plan body claim drift:** `.B.4` plan body says "3229 → 3229+" — this assumes baseline is already 3229 (not 3208). **DRIFT-RISK** — should be reconciled at `.B.4` Phase A audit gate fire. Likely typo / forward-looking estimate; non-blocking.

### 3.g — Effort estimate sanity

| Ship | Plan estimate | 1.4× adjusted | Notes |
|---|---|---|---|
| `.B.4` | ~2-3 days | ~3-4 days | Pre-coding audit gate adds overhead; M5 codification scope substantial |
| `.B.5` | ~1-2 days | ~1.5-3 days | HIGH risk (3,208 tests); per-domain incremental discipline is right |
| `.B.6` | ~1 day | ~1.5 days | First canonical; `/blindspot-scan` adds ~1h |
| `.B.7` | ~1-1.5 days | ~1.5-2 days | Biggest header; 8 panel extracts |
| `.B.8` | ~1.5-2 days | ~2-3 days | Bundle compounds risk; H6 baseline capture work |
| `.B.9` | ~1.5-2 days | ~2-3 days | HMAC round-trip verification work |
| `.B.10` | ~1.5-2 days | ~2-3 days | 3 splits + 4 marginal TRIM judgment |
| `.B.11` | ~0.5 day | ~0.7 day | Light closure |
| **Total** | **~10-13 days** | **~14-18 days** | Maintenance umbrella alone |

**Plus existing pipeline:**
- `.C` (sidecar) ~3-5h
- `.D` (CI) ~2-4h
- `.F.4e/.F.4f` ~3-4 days
- `.F.5.A/B/C` ~5-8 days
- `v5.15.6.A/B` ~2-3 days

**Adjusted total to paper-test:** ~25-35 days (~5-7 weeks) per Caramel's `feedback_motivated_collaborator_for_caramel` mandate. Reasonable runway given professionalization-phase context. NOT flagging as RED.

### 3.h — Codebase drift (file:line + function name claims)

**File sizes verified against engine HEAD `64e7101`:**

| Plan claim | File | Actual lines | Match? |
|---|---|---|---|
| 26,169 | tests/controller_test.cpp | 26,169 | ✅ EXACT |
| 6,376 | Backtest/BacktestPanels.hpp | 6,376 | ✅ EXACT |
| 3,881 | CoreFrameworks/ControllerEventLoop.hpp | 3,881 | ✅ EXACT |
| 3,793 | CoreFrameworks/EngineSharded.hpp | 3,793 | ✅ EXACT |
| 3,205 | CoreFrameworks/ControllerConfig.hpp | 3,205 | ✅ EXACT |
| 3,092 | ML_Headers/CoreModelZoo.hpp | 3,092 | ✅ EXACT |
| 2,578 | Backtest/BacktestEngine.hpp | 2,578 | ✅ EXACT |
| 2,409 | GUI/DashboardPanels.hpp | 2,409 | ✅ EXACT |
| 2,301 | CoreFrameworks/PortfolioController.hpp | 2,301 | ✅ EXACT |
| 2,210 | DataStream/EngineTUI.hpp | 2,210 | ✅ EXACT |
| 1,998 | ML_Headers/ModelInference.hpp | 1,998 | ✅ EXACT |
| 1,861 | Strategies/StrategyParameters.hpp | 1,861 | ✅ EXACT |
| 1,774 | GUI/SettingsPanel.hpp | 1,774 | ✅ EXACT |
| 1,543 | CoreFrameworks/OrderManager.hpp | 1,543 | ✅ EXACT |
| 2,577 | DOCS/tech-debt/open.md | 2,577 | ✅ EXACT |

**ALL file-size claims VERIFIED EXACT.** Strong planning hygiene.

**Function/symbol existence verified:**
- `EventLoopState_ConfigureKillSwitch` at `CoreFrameworks/ControllerEventLoop.hpp:3243` ✅
- `EngineSharded.hpp:750` consumer of ConfigureKillSwitch ✅
- `EngineSharded.hpp:748` comment "closing at v5.15.5.F.4d.1.B.4 via EngineCommon_BootPerCore extract" ✅ (planning pre-amendment marker present)
- `Strategy_InitPerCore` exists in multiple files (Strategies/StrategyLifecycle.hpp confirmed) ✅
- `tt::stamp_write_for_model` at `ML_Headers/StampHelper.hpp` ✅
- `Backtest_RunFullValidation` at `Backtest/BacktestEngine.hpp` ✅
- `BacktestSharded_Run` exists at BOTH `Backtest/BacktestSharded.hpp:104` AND `Backtest/BacktestEngine.hpp:534` — likely wrapper pattern; **`.B.4` plan body should clarify** which is the helper-extract caller-side OR both
- `EngineCommon.hpp` does NOT exist yet (correct; `.B.4` creates it)
- `FOREACH_ENSEMBLE_POST_LOAD` at `ML_Headers/CoreModelZoo.hpp` ✅

**TECH_DEBT entries verified:**
- TECH_DEBT-002, -029, -107, -114, -118, -119, -123 all OPEN in `DOCS/tech-debt/open.md` ✅
- CLEANUP-001 reference: plan cites `plans/_future/2026-05-09-v5.15-plus-cleanup-backlog.md` — verify exists at coding time

**PARITY entries verified:**
- PARITY-026/027/028/029/030/031 all present in `DOCS/PARITY_ISSUES.md` ✅
- PARITY-026 already CLOSED standalone per .B.4 plan body claim — verify ledger status

**DESIGN_SPECS verified:**
- `file-size-split-discipline.md` exists at `DESIGN_SPECS/doc-disciplines/` ✅ (Stage 2 DRAFT v1.0; awaits v1.1 amendment at `.B.6`)
- `canonical-sister-extension-discipline.md` referenced by `.B.4` + `.B.9` — verify exists

**Mirror comment count:**
- `.B.4` plan body claims "15+ explicit `Mirrors EngineSharded_Run lines X-Y` comment citations" — actual: **12 total Mirror citations** in BacktestSharded.hpp; ~5-8 specifically cite EngineSharded_Run line ranges. Plan body should correct (per M-1 above).

**`/readiness` SKILL.md status:**
- Plan body `.B.11` Decision F says "L1 verified at planning: `wc -l workspace/claude-skills/readiness/SKILL.md` = 625 lines"
- Verified: **625 lines** ✅
- Sidecar checks/ directory EXISTS with multiple check-*.md files (verified via `find claude-skills/readiness -name "*.md"`)
- **TECH_DEBT-118 STATUS DISCREPANCY:** TECH_DEBT-118 is **STILL OPEN** in `DOCS/tech-debt/open.md` per `rg "TECH_DEBT-118"`. The split has effectively been DONE (SKILL.md 625 lines + checks/ sidecars exist), but the ledger entry was never marked CLOSED. `.B.11` Phase B Step B.5 correctly handles this (verify + move to closed.md). NON-BLOCKING; .B.11 plan is correct.

### 3.i — Specific concerns from operator self-audit (Phase 5 verification)

Per the prompt's Phase 5 amendments-applied check:

**H1 ODR risk amendment at `.B.6` Decision C:** ✅ APPLIED
- Direct-include `#error` guard pattern present (lines 140-156 of `.B.6` plan body)
- `ENGINESHARDED_VIA_SHIM` define discipline present in shim convention (lines 117-137)
- Both shim define + sub-file `#error` guard codified

**H2 build system amendment at `.B.5` Decision C:** ✅ APPLIED
- Function-exposed sub-files (`void RunTests_<domain>()`) + thin main wrappers (5-LOC each) explicitly codified
- File structure with `tests/wrappers/` clearly diagrammed
- One-main-per-TU linker conflict explicitly addressed
- **PARTIAL GAP:** `tests_failed` counter discipline mechanism (extern vs C++17 inline variable) not fully specified — flagged as M-3 above

**H3 viability gate amendment at umbrella Decision G:** ✅ APPLIED
- Explicit gate criteria listed (5 bullets: tests pass / build clean / external callers unchanged / blindspot-scan GREEN / pattern works without amendment)
- Explicit HALT policy (3 options: amend + retry / fall back to flat-sibling / accept exemption)
- Strong amendment; clear gate

**M5 SPDX format spec at `.B.6` Decision C:** ✅ APPLIED
- EXACT shim convention codified with full SPDX comment block format
- EXACT sub-file convention codified with PART_OF + Section identifier + direct-include reason
- Both formats embedded as code blocks in plan body (cannot be misinterpreted)

**M6 marginal-4 fallback at `.B.10` Decision F:** ✅ APPLIED
- 4-level hierarchy explicit: TRIM → SPLIT → EXEMPT → DEFER
- Exemption criteria + expected-level-per-header table both present
- Hard rule: "No marginal header gets Level 4 (defer) unless ALL 1-3 fail with documented reasoning"

**M8 novel alternatives at umbrella Decision F:** ✅ APPLIED
- C++20 modules: REJECTED with rationale (C++17 codebase; needs upgrade)
- Header/impl split: REJECTED with rationale (template-heavy codebase; explicit instantiation overhead)
- Auto-generation from registry: REJECTED with rationale (post-inflection per `feedback_framework_layer_payoff_diminishing_returns`)

**All 6 operator-self-audit amendments verified applied.** GREEN on amendment hygiene.

---

## Recommendations

### Must fix before coding

1. **M-2 [.B.5 HIGH]**: Pre-extract section-to-domain CSV at Phase A (~30 min) to deterministically map all 3,208 tests; eliminates per-section judgment during extract. **BLOCKING for `.B.5` Phase B unlock.**

2. **M-6 [.B.8 HIGH]**: Add Phase A baseline-capture step for `sizeof(ControllerConfig<int64_t>)` + `offsetof()` of critical cross-thread fields. Static_assert post-split is meaningless without captured baseline. **BLOCKING for `.B.8` Phase A audit gate completion.**

3. **M-3 [.B.5 MED]**: Specify exact mechanism for `tests_failed` counter sharing across 6 binaries (extern vs `inline` C++17 variable). Decision C needs this resolved before `.B.5` Phase B.

### Worth fixing during coding (non-blocking)

4. **M-1 [.B.4]**: Correct "15+ Mirrors" claim to actual count (12 Mirror citations; ~5-8 EngineSharded_Run-specific). Strengthen plan body's evidence accuracy.

5. **M-7 [.B.10]**: Reframe Decision G CLEANUP-001 fold-in as default-yes; defer only if specific operational reason surfaces.

6. **M-4 [.B.6]**: Phase A `/blindspot-scan` should explicitly verify transitive-include topology (sub-files reached only via shim). Pillar B7 of taxonomy.

7. **M-5 [.B.6]**: Make Helpers.hpp threshold explicit (e.g., <200 LOC → fold; >=200 → separate sub-file).

8. **Test count reconciliation [.B.4]**: Plan body says "3229 → 3229+"; actual baseline is 3208. Reconcile before Phase A.

9. **BacktestSharded_Run dual-definition [.B.4 + .B.9]**: `Backtest/BacktestSharded.hpp:104` AND `Backtest/BacktestEngine.hpp:534` both define `BacktestSharded_Run`. `.B.4` Phase A should clarify which is the helper-extract caller; `.B.9` should clarify the relationship (wrapper-vs-impl pattern) since both files are touched at this stage.

### Acceptable risk (don't block)

10. CLEANUP-001 file path `plans/_future/2026-05-09-v5.15-plus-cleanup-backlog.md` cited in `.B.10` Decision G — verify at coding time.

11. `canonical-sister-extension-discipline.md` cited by `.B.4` + `.B.9` — verify exists in DESIGN_SPECS at coding time.

12. PARITY-026 closure status: plan claims standalone hotfix already shipped via commit `6fd0ba3`. CLAUDE.local.md sprint state confirms hotfix tag `v5.15.5.F.4d.1.B.2.h1-killswitch-fix`. Verify PARITY-026 entry status in ledger reflects CLOSED.

13. Umbrella + parent-sub-master cross-references coherent. `MASTER.md` v5.15.5 amendments section + `2026-05-16-v5.15.5.F.4d.1-thread-a-framework-full.md` "Sibling-umbrella additions" section + `ROADMAP-2026-05-17-to-paper-test.md` `.B.5`-`.B.11` rows all reference each other correctly. NO orphaned references found.

---

## Verdict: YELLOW

**Rationale:**

The planning artifact set is **strong overall** — 8 plan bodies with high cohesion + 100% file-size claim accuracy against current engine HEAD + complete cross-reference integrity + per-sub-ship discipline preserved + all 6 operator-self-audit amendments verified applied. Dependency edges correct; zero cycles; sequencing strict per per-sub-ship cycle.

**YELLOW (not GREEN) because 3 must-fix items (M-2, M-6, M-3) are HIGH/MED severity gaps that should land in plan body amendments BEFORE coding starts:**

1. `.B.5` section-to-domain pre-mapping discipline (~30 min Phase A work) — without this, 3,208-test reorg has per-section judgment risk during extract
2. `.B.8` `sizeof(ControllerConfig<F>)` baseline capture — H6 cross-thread invariant verification needs baseline to compare against
3. `.B.5` `tests_failed` counter sharing mechanism — TU-local global vs C++17 inline variable affects 6-binary correctness

These are NOT scope-shifting changes — they're plan body precision improvements within existing scope. ~1-2 hours of plan body amendments resolve all three.

**YELLOW (not RED) because:**
- Hot path UNTOUCHED preservation is robust across all 8 ships
- HMAC byte preservation discipline applied at `.B.9` (the byte-equivalence-critical surface)
- H6 cross-thread alignment discipline applied (with M-6 amendment) at `.B.8` (the cross-thread-critical surface)
- 3,208 tests preservation hard-required at each ship verification gate
- Subfolder pattern viability gate at `.B.6` (Decision G umbrella) provides HALT path if first canonical fails
- Effort estimate sanity check passes even with 1.4× multiplier (~14-18 days umbrella; ~25-35 days total to paper-test acceptable per Caramel's runway tolerance)
- Per-sub-ship audit→consult→implement→ship cycle preserved → drift catch points at every ship

**RECOMMENDED NEXT ACTION:**

1. Operator triages 3 must-fix items (M-2, M-3, M-6) — accept or modify
2. Apply plan body amendments to `.B.5` (add Phase A pre-mapping step + Decision C counter mechanism) + `.B.8` (add Phase A baseline-capture step)
3. Fix non-blocking items M-1 (`.B.4` mirror count correction) + test count reconciliation during plan body cleanup pass
4. THEN proceed to `.B.4` Phase A pre-coding audit gate fire (the next planned task)

The maintenance umbrella is **structurally ready** for execution; precision improvements bring YELLOW → GREEN.

---

**End of plan-check report.** Audit duration: ~45 min focused. Engine HEAD verified at `64e7101`. Cross-references between plan bodies, sprint MASTER, sub-master, roadmap, and current codebase all verified.
