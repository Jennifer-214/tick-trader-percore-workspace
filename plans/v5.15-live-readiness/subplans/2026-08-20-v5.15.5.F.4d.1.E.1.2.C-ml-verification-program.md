---
type: plan
ship_tag: v5.15.5.F.4d.1.E.1.2.C
title: ML verification program — make the train→serve→learn loop real, verified end-to-end
status: executing — gate CLOSED same-day (SOUND-WITH-AMENDMENTS-12; see § AMENDED); legs 0/2/3 + the deferred tables + polish + 3G-i/3G-ii SHIPPED (D-429); REMAINING = leg 4 empirical (operator) + D6/D7 forks + the #16/A-12 flag + /ship close
opened: 2026-08-20
audit_tier: HIGH
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
decision_ref: D-428
predecessor: the E.1.2 D-426 arc (closed 2026-08-17; its queue item 1 = leg 2 here)
owning_findings: [PARITY-042, PARITY-043, PARITY-044, "the fan-shift defect (FIXED leg 0; no ledger entry minted — the register line + D-429 (per its item 1 disposition) are the record)"]
sister_specs:
  - meta-disciplines/train-serve-execution-layer-parity.md
  - meta-disciplines/advertised-capability-never-exercised.md
  - meta-disciplines/dead-code-and-identifier-retirement-discipline.md
  - framework-patterns/x-macro-registry-with-presence-dispatch.md
reports_dir: plans/v5.15-live-readiness/reports/2026-08-20-ml-verification-program/
---

# E.1.2.C — ML verification program

**Operator directive (2026-08-20, Caramel):** *"i wanna test out the ml stuff so that all of this is working … proceed with the exit and buy model training as well as the bandit and ML integration within live to confirm it actually works, then proceed with the rest of E … its core we verify this stuff works and we have a solid base to work off of."* Tacked onto the current E phase per the same directive. Investigation: 7 i-class agents fanned 2026-08-20, all reports saved verbatim in `reports_dir` — **the reports are the depth; this plan is the index + the decisions.** Engine anchor at drafting: `417e524`.

## Problem statement

Every layer of the ML loop is *advertised* and *test-green*, and almost none of it has ever run end-to-end in production shape. The investigation measured, at HEAD:

1. **The exit-bandit reward update is DEAD on every production path — live and backtest — and has been since v5.14.1.F.** The `EventLoop_DrainPostFill` fan passes 10 positional args into `EventLoop_DrainPostFillOneCore`'s 12-param signature; a v5.14.1.F mid-signature insertion (`confidence_ic_variant` at position 9) silently shifted every later binding: the cfg exit flag lands in the IC-variant param, the fee constant `0.001` truncates into `int exit_bandit_enabled` (= 0, always), and `node_cfg` stays nullptr (Thompson forced to EXP3 on the drainer). **Compiled-probe-proven** (bandit report H-1) and **independently re-derived** by the backtest agent (Defect 2). Worse than dead: flipping `exit_bandit_enabled=1` in LIVE would set the IC variant out-of-range → constant 0.0 IC → guaranteed drift-floor breach → with `auto_kill_on_drift=1`, **spurious per-node auto-kill** — the flag poisons a capital safety control instead of enabling learning. D-423's "reward half works" premise was false when written.
2. **PARITY-043:** the cfg-derived stamp cohort is **36 fields** (measured; not "~30"); all 36 declared on ModelHandle, **33 written by nothing** — the family has 4 of its 5 walkers; the sr→handle copy walker was never built. With `bandit_enabled` now defaulting ON, two REFUSE_STRICT rows fire falsely on **every** model load at a default cfg.
3. **PARITY-044:** exit-side training routes to a tree nothing loads, with buy-role filenames, side-blind labels, a parsed-never-read cfg pointer, and no side marker anywhere — while the serve side already walks co-located `exit.json` slots that nothing produces. The exit-bandit loop has **no production model source**.
4. **The live train path emits NO scaler and no stamp scaler binding** (both sides): the only scaler producer is `train_model_worker_fn`, which is **dead code** (zero callers since v5.11.44; the Train Model button routes through the multi-horizon worker). The v5.9.3 scaler verification chain is unreachable from the GUI. The Save Run block is dead UI (gated on a flag only the dead worker sets).
5. **At HEAD there is NO functioning train↔serve cfg-parity gate at all** (PARITY-042 vacuous + PARITY-043 false-firing + ensemble loads skip the drift walk entirely + single-zoo boot hardcodes `secret=nullptr` so Shape-B loads never HMAC-verify). cfg-parity currently rests entirely on keeping `backtest.cfg ≡ engine.cfg`.

## Ship end goal + acceptance criteria

**End goal:** the complete loop — train (buy AND exit) → stamp → load → predict → trade → attribute → learn → persist → reload — runs end-to-end, in backtest and paper, with every gate that guards it non-vacuous, and with the false-noise layers removed. The E series resumes on that verified base.

**Acceptance (per leg, all re-derivable):**
- **Leg 0:** a seam-level regression test drives the production wrapper with `exit_bandit_enabled=1` through a synthetic fill and asserts `exit_bandits[r].pulls` moved (the exact blind spot the leaf-level test C.3c leaves); `confidence_ic_variant` reaches OneCore un-perturbed; backtest completion writes all four bandit-state files.
- **Leg 1:** operator completes the buy-side protocol (report: `e2e-verification-protocol.md`) with Shape A (HMAC-verified) — trained model loads with `[model] … ok (signature verified…)`.
- **Leg 2:** a default-cfg model load emits ZERO `INFERENCE_CFG` drift lines whose stamp carried the key (round-trip test against PRODUCTION emit, never the `:15582` fixture); the trained-without-feature catch still fires (that gate is intentional).
- **Leg 3:** `training_side=1` in the suite produces `exit.json` + `.stamp` co-located per-horizon; the engine loads it into `exit_predictor[]`; a buy model hand-placed in an exit slot REFUSES in strict / WARNs+flags in non-strict; the retired cfg key + tree are burned per H21.
- **Leg 4:** paper/backtest run shows `SHALT_EXIT_PREDICTED` fires → `exit_bandit_state.json` lands with `pulls>0` and non-uniform weights → reload proves persistence; then the A/B matrix (bandit vs uniform vs no-exit) runs on fixed seeds with controlled state files.

## Design space + future-oriented choice (the settled and the open)

**Settled by investigation (operator endorsement carried from the 2026-08-20 session):**
- **Leg-3 convention (a):** side=1 → role file `exit.json`, CO-LOCATED in `<base>_horizon_<H>/` — zero serve-side changes (both loaders already walk role "exit"); the trainer diff is ~6 line-regions. Wiring `exit_signal_model_dir` as a second discovery root was REFUTED (builds a parallel mechanism beside a production-called walk; contradicts the loader's own design comment).
- **Leg-3 semantics (b):** the side combo's on-change defaults the label kind to `WILL_PEAK` (binary P(peak) — verified correct through `buy_class_idx`/`Model_Predict` for the `exit_threshold` consumer). PEAK_VALLEY_STABLE stays legal on either side (the zoo was designed for its peak class).
- **Retirement shape T2:** full delete + `RETIRED_NAMES` burn + tombstone comments, same commit (both agents converge; zero live cfgs carry the key; the burn is the only mechanized never-reuse available — cfg keys are not ledger-enrolled yet).
- **Fan fix shape B+C:** ONE shared binder (generalize `EngineSharded_SlowPath_DrainPostFill`) called by live + both backtest sites; per-node flags read from `node_cfg->ml_cfg_flags` at point of use instead of mirrored boolean args; de-default the tail so the next mid-signature insertion is a compile error. Plus the three missing backtest end-of-run savers. (Second recurrence on one signature = the structural threshold; M5's last hand-mirrored execution-layer hop.)
- **PARITY-043 fix:** the 5th walker — `cfg_derived::copy_stamp_result_to_handle` + one call in `NodeModelZoo_TryLoadRole` after Model_Load success; per-field `has_` gating; do NOT re-gate the drift rows (the trained-without-feature catch is a settled decision).

**RESOLVED BY THE GATE (2026-08-20 a-class passes — see the three verdict reports in `reports_dir` + the AMENDED section below):**
- **D2 (guard) — DECIDED: O1-ONLY (S-C-with-fixes). NO new `training_side` key.** Enforce the existing `expected_role` key at the TryLoadRole chokepoint, with three mandatory legs: **F1** (FV re-stamp derives `req_role` from the model basename — closes the one emit hole, ~10 LOC), **F2** (the enforcement + the fully pinned role×slot×strict decision table, failure bit named `ml_role_mismatch`), **F3** (trainer-side side×label gate: side=1 refuses {WIN_LOSS, FORWARD_PNL, REGIME, CS_*}, allows {WILL_PEAK, PVS}, WARNs {WILL_VALLEY, VOL_BARRIER} pending operator triage). Zero wire change, zero ledger event, zero bless. Full table + catch matrix: `a-class-D2-guard-fork-verdict.md`. Re-open trigger: side≠role ever becomes a real convention (tail-append the key THEN — mechanics stay mapped).
- **R1 (reachability) — CONFIRMED (compiled probe + deployment-reality check): the ML dispatch gate is single-zoo-blind, and every real multi-horizon deployment on disk is the dead shape** (silent SimpleDip in paper/backtest; loud REFUSE in live). **ADOPTED: the ensemble-aware dispatch fix as leg 3's serve-side prerequisite** — `StrategyParameters.hpp:913` gains the ensemble OR, plus the three same-class blind sisters in the same commit (strategy-swap refusal `Run.hpp:1799-1803`; live-readiness model check + the ensemble-blind drift aggregation `LiveReadiness.hpp:113-149`), plus the two actively-misleading comments (`StrategyParameters.hpp:169-174`, `backtest.cfg:376`). The copy-to-base stopgap is documented for an early paper run only ("exit-on-top-of-single-model-entry"); the R2 scaler seam (ensemble-only cycles must standardize from an ensemble handle's scaler) rides this same commit. Full chain: `a-class-R1-reachability-verdict.md`.
- **D4:** summary.txt collision under co-location (buy + exit runs sharing run_name+horizon overwrite one summary) — accept+document / distinct run_name convention / role-suffixed summary (breaks PastRuns_LoadOne).
- **D6 (scaler):** the dead `train_model_worker_fn` is the only holder of the v5.9.3 scaler emit. Deleting the dead code (owed per H21-family discipline) must be COUPLED to a decision: restore scaler compute+persist+stamp-binding in the multi-horizon path, or explicitly retire the scaler apparatus with its advertisements. Training currently fits on raw features and serves identity — consistent-by-accident; the decision is whether standardization returns as a real feature.
- **D7 (exit observability):** exit-bandit weights/pulls are published NOWHERE — Leg 4's Stage-2 confirmation is JSON-forensics without a PerNodeSnap mirror of the buy-side heatmap block. Add the fields (layout-gate re-derive owed) or accept forensics for this program.
- **R1 (reachability, VERIFY before Leg 3 codes):** the serve agent's highest-value refute — the exit block appears unreachable unless the SINGLE-zoo loaded ≥1 role (`ML_BuildParameters` early-returns on `!NodeModelZoo_HasAny(zoo)`), so a pure `_horizon_*` ensemble deployment may leave the exit path dead. Needs a compile/deployment-layout probe; if it holds, the plan needs a base-dir model requirement in the protocol or a dispatch change.
- **R2 (scaler seam):** exit predictions consume features standardized in-place by the SINGLE-zoo primary handle's scaler — never the exit handle's own. Interacts with D6.

**Entry-aware exits: considered and DEFERRED** — recorded privately in `plans/_future/FUTURE_ML.md` (2026-08-20 entry: the MoE framing; three rungs; revisit triggers). Rung 2 (position-bucket bandit context) is the sanctioned future direction; nothing in this program precludes it.

## Canonical sisters (extend, don't invent)

`cfg_derived` walker family (the 5th walker completes it) · `expected_role` (existing stamp key — enforce, don't duplicate blindly: D2) · `ml_scaler_load_failed` 3-tier site set (the refusal-surface template; copy the DUAL-walk aggregation, not the single-zoo-only defect) · `EngineCommon_*` shared boot/cycle helpers (the fan fix relocates into this family) · `FOREACH_FAILURE_MODE` (the warn-arm bit rides the fixed dual-walk for free — no PerNodeSnap layout change) · `STAMP_PUT` + the D-426 guard (leg-2's residual: opt ModelHandle in; the `scaler` name-collision is the one design decision there).

## Legs (dependency-ordered)

| Leg | What | Gate before code | Report(s) |
|---|---|---|---|
| **0. Fan-shift fix + backtest forwarding + savers** | Shared DrainPostFill binder (shape B+C); forward flag/node_cfg/fee; 3 backtest end-of-run savers; seam regression test; `-Werror=float-conversion` or targeted CI probe as the class guard | a-class confirms archaeology framing (regression at v5.14.1.F vs fan-authored-wrong); operator OK on drift-arg semantics in backtest | bandit-loop-state.md · backtest-exit-exercisability.md |
| **1. Buy-side e2e (operator, no code)** | Run the protocol: Collect → Train → Verify Stamp → Shape-A engine load → ML Status | none — read the protocol's known-false-noise list first | e2e-verification-protocol.md |
| **2. PARITY-043: the 5th walker** | `copy_stamp_result_to_handle` + TryLoadRole call + stale-comment sweep + round-trip tests vs production emit; then (separable) the ModelHandle guard opt-in + STAMP_PUT conversion (scaler-collision decision) | a-class chokepoint check (Model_LoadAOT / direct Model_Load bypasses); the `:15582` fixture MUST-TOUCH honored | parse-handle-seam-parity-043.md |
| **3. PARITY-044: exit seam + guard + retirement** | Trainer side arm (role="exit", co-located); WILL_PEAK default on side-switch; T2 retirement of exit tree + `exit_signal_model_dir` + advertisements + 3 test sites; exit-blindness role_files cohort; guard per D2; grid_member_count consistency | **D2 + D4 + side-check table pinned; R1 resolved** | trainer-side-exit-seam.md · serve-side-exit-load-and-side-key-guard.md · retirement-and-test-blast-radius.md |
| **3G. Model-selection UX (operator ask 2026-08-20: "make model selection easier in settings + GUI")** | Bundle PICKER replacing the typed path: `Settings_RescanModels` extended to enumerate single-zoo dirs AND horizon-sibling FAMILIES as one selectable ensemble entry each; selection writes the correct `node_N_model_dir` form. RESOLUTION PREVIEW on select: shape (single/ensemble), roles per horizon, primary role, exit predictors present, stamp-verify vs this build's registry hash, and a SimpleDip-fallback warning line (post-R1-fix truth). Same picker feeds the hot-swap request path (`pending_model_path`), with the cached-horizon swap constraint surfaced in the UI instead of discovered at failure. Pairs with the PARKED panel-prune leaf (tier B/C dispositions still owed by operator) — one suite-GUI session can land prune + picker together. | AFTER the R1 dispatch fix (the preview must show the FIXED engine's resolution, not the broken gate's); GUI-plane = light process per blast-radius rule | serve-side-exit-load-and-side-key-guard.md §Q2/Q3 (role_files cohort + base_run_path sources) · a-class-R1-reachability-verdict.md (the Shape A/B trap the preview kills) |
| **4. Bandit empirical (backtest → paper)** | Stage 1 artifacts (≥2 exit models from leg 3) → Stage 2 loop-closure in vivo → Stage 3 A/B matrix (`exit_blender_mode=0` both arms; explicit `bandit_enabled`; fixed seeds; state files controlled — today's default is ACCIDENTAL transfer of whatever JSON is on disk) | D7 decided; leg 0 + leg 3 landed | bandit-loop-state.md § "What the verification program must run" |
| **5. Rest of E** | E.1.2 close-gate + queue per the 2026-08-17 handoff | this program's close | — |

## Folded findings register (homed here; disposition at triage — no unhomed debt)

Each traced in the reports; severity as assessed there:
1. Fan-shift defect (leg 0) — HIGH, capital-adjacent (drift-kill poisoning). Ledger entry to mint at triage.
2. Dead `train_model_worker_fn` (:3657-4125) + dead Save Run block (:6171-6468) + the false "back-compat callers" comment — dead-code removal leaf, COUPLED to D6.
3. No-scaler-on-live-path — D6 (couples with #2).
4. Buy-side Thompson trade-close rewards forced EXP3 via nullptr node_cfg (fixed by leg 0's shape C).
5. `MBS_OrderSetBanditContext` setter never called + `bandit_reward_bps[]` zero writers → two calibration-log columns permanently zero with a false producer comment (C-424 re-confirmed + widened).
6. Ensemble loads pass no cfg_ptr → cfg-drift walk + model-age dead for ALL ensemble-loaded models (PARITY-042/043-adjacent; NOT fixed by leg 2 — needs its own disposition).
7. Single-zoo boot hardcodes `secret=nullptr` → Shape-B loads never HMAC-verify (live boot gate does catch it via the hmac-not-verified failure mask in live mode).
8. Boot discards `ValidateAgainstCfg`'s strict REFUSE (engine continues; TODO v5.10) — enforcement gap distinct from signal truth.
9. "Backtest inherits live-learned bandit state from the model dir by default" — un-numbered parity surface; `bandit_state_prior_path` advertised but operator-unreachable (no cfg key, no GUI writer).
10. 9 dead prefixed `inference_cfg_*` POST_CFG rows + the never-set group bit (H21 tombstone decision; PARITY-042's fix path).
11. Exit Ridge IC hardcoded 0.0; `exit_bandit_display` zero readers; `MASK_BANDIT_SHADOW_LEARNING` zero production readers; `ensemble_bandit_save_interval=0` unreachable; `bandit_blend_ratio` behavioral consumer only on the dead legacy path.
12. Stale-comment batch (all named with file:line across the seven reports; fix in the leg that touches each file, same-commit).
13. Doc-cite drift: `CLAUDE_ML_INVARIANTS.md:478` contract-table row (3 stale names) + the `~:1818-1840` populator anchor; `FEATURE_LOOKUP.md` exit-tree advertisements (5 sites).
14. `grep -r` does not follow file symlinks (session-measured; engine `DOCS/` are symlinks) — LANDMINES.md + SUBAGENT_ARMING candidates.
15. Suite `role_files[]` exit-blindness ×3 (leg 3 closes).
16. Mixed-output exit arms (binary WILL_PEAK next to 3-class PVS in `exit_predictor[]`) — consumer math check flagged as a possible sixth leg (retirement report risk 8).

**Register dispositions (2026-08-20 evening currency pass — post legs 0/2/3 + the deferred tests + GUI polish + 3G-i/3G-ii):**
- **CLOSED:** #1 fan-shift (leg 0 `56deb39`; fixed same-program — no ledger entry ever minted, this line is the record) · #4 nullptr-node_cfg forced-EXP3 (leg 0 shape C, same commit) · #14 symlink grep/compile family (Landmines 19 + NEW 21; workspace `18a21cd`) · #15 suite `role_files[]` exit-blindness ×3 (leg 3-role `22433b0`) · #17(c) `Settings_RescanModels` exit/layout blindness (3G-ii `bed04f1` — the scanner now sees families AND the `models/<class>/` depth).
- **SURFACED-AS-DESIGNED:** #17(b) cached-horizon swap constraint now stated in the Apply-(live) tooltip (3G-ii); #7's Shape-B never-HMAC got an operator-visible note in the picker preview — the defect itself stays OPEN.
- **PARTIAL:** #12 stale-comment batch — per-leg same-commit sweeps landed (commit-recorded); totality check owed at `/post-ship-audit`. #13 doc-cite drift — the 5 FEATURE_LOOKUP `exit_signal_model_dir`/`models/exit` ad sites + the `CLAUDE_ML_INVARIANTS` contract-table names FIXED this pass; the register's own "~:1818-1840 populator anchor" cite does NOT resolve (the file is 546 lines) — that cite was stale as written; nothing found behind it to fix.
- **CLOSED (2026-08-21):** #16 / A-12 — verified AND the verification found the real defect underneath it. The mixed-arm question itself is a NON-issue: `exit_predictor[]` aliases per handle from that handle's own `num_outputs`, so a binary WILL_PEAK arm (idx 0 = raw P(peak)) and a 3-class PVS arm (idx 1 = peak) both yield a peak probability and blend coherently. But tracing it surfaced **the buy-side inversion**: the SAME `(num_outputs >= 2) ? 1 : 0` rule was hand-copied THREE times (`NodeModelZoo` single-zoo `:793`, `EnsurePrimary` `:1587`, `LoadFromCfg` `:2280`) and is *correct for exit* (sell at a peak) but *inverted for buy* (you enter at a VALLEY, class 2). A barrier-primary ensemble therefore fed `P(peak) = P(bad entry)` to the buy threshold while `BarrierGate` received `1-P(peak)` and was least likely to block exactly when a peak was most likely. **Latent since v5.11.62; made LIVE by leg 3** giving the ensemble branch precedence at `StrategyParameters.hpp:1050`. **Not a corner case:** zero `buy_signal.json` exist anywhere on disk — every model the trainer emits for a buy side is `barrier.json` (PVS), so barrier-primary is the ONLY ensemble shape it can produce. **FIXED 2026-08-21:** the rule is now two NAMED functions whose asymmetry is the point — `Model_PrimaryBuyClassIdx` (valley) / `Model_ExitClassIdx` (peak), `ModelInference.hpp`; all six call sites route through them; C.3i pins both plus the invariant *"if these ever agree on a multiclass model, one is reading the other's class"* and drives the real `EnsurePrimary`. Found via `/decision-check` on an unrelated question — reports at `reports/2026-08-21-f3-tier-flip-decision-check/`.
- **OPEN — NEW, homed here (2026-08-21, from the same gate):**
  - **#18 `p_peak` charges the stable mass. TRIGGER-HOMED 2026-08-21: fix BEFORE `barrier_gate_enabled` is ever turned on.** Measured at HEAD: the flag defaults to 0 (`ControllerConfig.hpp:2227`) and is set in NEITHER `engine.cfg` nor `backtest.cfg`, so `BarrierGate_Compute` (`StrategyParameters.hpp:1823`) never runs and `p_peak` is consumed by nothing today. Not MOOT-UNREACHABLE — it is one cfg flag away, so it stays open with an explicit trigger rather than being closed. **Corollary worth recording:** the barrier gate being off means nothing mitigated the #16 buy-side inversion for its whole life; the safety control that would have hard-blocked "buy at the top" was simply not enabled. At `StrategyParameters.hpp:1439-1441` `p_peak = 1 - prediction`, which on a 3-class model is `P(peak)+P(stable)`, not `P(peak)`. Right sign post-fix, wrong magnitude: at P(stable)=.5/P(peak)=.2/P(valley)=.3 the true p_peak is .2 (gate ~.61) but this yields .7 — a hard block (`BarrierGate.hpp:81`). Conservative, never unsafe, but it discards the third class PVS exists to break out. Exact fix = one `Model_PredictMulti` per handle (same inference count) blended per-class in both the weighted and selection branches. Documented in-place at the site.
  - **#19 reward/IC attribution — CONFIRMED inverted, and CLOSED by the same commit (traced 2026-08-21).** Both buy-side reward paths score correctness as `((p > 0.5f) == <bullish outcome>)`: the slow-path lookback against `price_delta > 0` (`NodeModelZoo.hpp:1760`) and the trade-close against `realized_pnl_bps > 0` (`:1837`). That convention requires `p` to be a BULLISH probability. Pre-fix `p` was P(peak) — bearish — so **a model that correctly called a top scored `(true == false)` and was paid −50 bps for being right**, and `EnsembleModelZoo_UpdateDrift(ezoo, a, correct, ic_floor)` (`:1772`) fed the same inverted `correct` into the IC watchdog, so the most accurate arms drifted toward demotion. Post-fix `p` = P(valley) = bullish, and the convention holds. So `97f12c9` fixed not just the entry signal but the whole buy-side learning loop — the reward, the Thompson posterior, and the drift watchdog all read the same corrected quantity.
    **Exit bandit NOT affected:** its reward is counterfactual P&L, `reward_bps = actual_pnl_bps - hypothetical_pnl_bps` (`ControllerEventLoop.hpp:2046`, "did exiting early beat holding to TP"). It never reads `p > 0.5`, so it carries no directional-convention dependency.
    **Leg-4 consequence:** none — zero `*bandit*state*.json` / `*thompson*state*.json` exist on disk (measured), so there is no anti-learned state to purge. Any weights produced from here are learned under the corrected reward. Had state existed it would have needed deleting, not transferring.
  - **#20 RE-DISPOSITIONED 2026-08-21 — it is a DEAD DUPLICATE wire slot, not a missing emit. Correct answer is TOMBSTONE, not wire.** The original framing ("`req_num_outputs` has no writer ⇒ `expected_num_classes` never emitted") is factually right but leads to the wrong fix. Measured at HEAD: (1) `req_num_outputs` has **no writer** — 7 sites tree-wide, all decl/default-init/read; (2) the parsed stamp field has **zero consumers** — the row is `SKIP_HANDLE` and no `sr.expected_num_classes` read exists anywhere; (3) **`model_num_outputs` already does this job** — emitted UNCONDITIONALLY (`StampHelper.hpp:334`, `K = req_num_outputs>0 ? req_num_outputs : LabelType_NumClasses(label_kind)`), copied to the handle, and **REFUSING on mismatch** at `NodeModelZoo.hpp:505-508`; (4) both keys carry the SAME documented semantics ("model output dimension at training time"); (5) the working class-count enforcement is the `expected.cfg` sidecar at `NodeModelZoo.hpp:1025-1029`. So emitting it would stand up a second mechanism beside a working one (Class 21) and add a third copy of one declared value (SSoT). **Disposition: delete the row + bit + MASK, burn the name in `RETIRED_NAMES`, re-bless — the exact D-426 (ii) arc.** BLOCKED ON OPERATOR: ledger-enrolled as `stamp-key|expected_num_classes|21`, so this is a wire change on an HMAC-signed body and the D-394 bless is a human-eyes TTY gate. Bless review property (per D-426): exactly 1 REMOVED / 0 ADDED, survivors 22→21 and 23→22 in a uniform −1 bucket, relative order preserved, all other categories byte-unchanged.
    **ATTEMPTED + REVERTED 2026-08-21 — the arc CANNOT be completed as specified, and the blocker is a real tooling gap, not this key.** The full deletion was built (row + `STAMP_BIT_` + `MASK_` + emit block + `req_num_outputs` decl/assign/StampArgs member + the `RETIRED_NAMES` burn) and `check_identifier_retirement.py` went RED with **7 RETIRED-NAME-REUSE hits** — because **`expected_num_classes` names TWO independent formats**: the dead STAMP key, and the **LIVE `expected.cfg` sidecar key** written at `BacktestPanels.hpp:6347/6350/6353/6360/6436/6439/6492` and enforced at `NodeModelZoo.hpp:1025-1029`. `RETIRED_NAMES` is **name-GLOBAL** (whole-word scan over comment-stripped code) while the codebase has per-FORMAT namespaces, so a name shared between a retired wire key and a live different-format key is unburnable. Deleting without the burn is exactly the "narrated rather than enforced" failure that file's own `fees` correction paragraph documents, so the work was REVERTED to HEAD rather than forced past the gate. **Two dispositions, operator's call — this is now a DESIGN question, not a chore:** (i) teach `RETIRED_NAMES` a NAMESPACE (`stamp-key:expected_num_classes`) so burns scope to the format that retired them — the general fix, since this collision class will recur as cfg-name-key enrollment proceeds; or (ii) leave the dead stamp row in place, documented, and accept an inert slot rather than weaken the burn. **Recommend (i)** — it extends the tool at the gap rather than working around it, and it is the same discipline that made the `fees` burn real. Until then #20 stays OPEN and the row stays live-but-inert.
  - **#21(a) declared label != trained label — FIXED 2026-08-21.** `mh_run_one_horizon_fv` set `label_forward_ticks`/`tp_pct`/`sl_pct` on the per-horizon `local_run_cfg` but never `label_type`, while `Backtest_ComputeLabelsFromSamples` picks the label leaf from exactly that field (`BacktestEngine.hpp:904`). So every horizon recomputed labels from whatever the last COLLECT click left, while the `label_type` PARAMETER drove num_classes / the XGB objective / the role file / the run_subdir / the stamp — one run, two different labels. **This silently voided the "Label Kind CSV" feature outright** (Class 12, advertised-capability-never-exercised): `:4885` writes the per-horizon kind into `job->label_type`, so it reached the objective and the stamp but NEVER the labels the model trained on. One-line fix — `local_run_cfg->label_type = label_type;` — in the same per-horizon mutation set, which `:4284` documents as exactly what that copy is for. **Test gap acknowledged:** `mh_run_one_horizon_fv` lives in the ImGui-only TU and is not reachable from the ANSI test TU, so this is inspection- and build-verified, not pinned. Same untestable-in-place shape as #21(b).
  - **#21(b) F3 gate reaches only the COLLECT buttons**, not Train (`can_train` `:5802` / `mh_can_train` `:6031` carry no `side_gate` term) — its own comment at `:5045` says "buttons disabled" and is false. The gate also reads `state->label_type` alone, so the Label-Kind-CSV per-horizon set bypasses it. STILL OPEN.
  - **#22 `expected_label_type` is written (`BacktestPanels.hpp:6416`) and read by nothing** — the one on-disk label-identity artifact is inert; cheaper enforcement seam than a wire key if a load-side label guard is ever wanted.
- **OPERATIONAL (leg 4 pre-flight):** every exit/barrier `.stamp` on disk lacks `expected_role` — traced to a suite process image predating the F1 fix, NOT a code defect (F1's basename derive covers `exit.json`). Consequence stands: existing models hit D2's F2 absent-key cells on load (strict REFUSE / non-strict WARN+flag), and `rehab_6_horizon_15000/exit.json` has no `.stamp` at all. **Retrain from a freshly-launched suite before leg 4.**
- **UNCHANGED-OPEN:** #2/#3 (D6-coupled) · #5 · #6 · #7 · #8 · #9 (A-11 widening noted: `buy_thompson_state.json` lands by default post-leg-0) · #10 (PARITY-042's path) · #11 · #17(a).

## Tests (members, not tallies)

OUT: the 2 `exit_signal_model_dir` pins + the 4-check side-routing replica (a reimplemented oracle that "tests" `(0==1)`/`(1==1)` — replace, don't mourn). IN: fan-seam regression (leg 0) · walker round-trip vs production emit + REFUSE-no-false-fire + legacy-skip (leg 2) · side=1 role/path/`expected_role` pins via an extracted pure helper + side-check strict/non-strict arms + retired-key-still-boots (leg 3). Suite count re-derives via `./build.sh test`.

## Bookkeeping (from the retirement report's ordered checklist)

## AMENDED 2026-08-20 (same day) — pre-coding gate CLOSED: 3 a-class verdicts folded + operator re-scopes

**Gate outcome:** plan-level verdict **SOUND-WITH-AMENDMENTS-12** (`a-class-plan-level-verdict.md`) · **D2 DECIDED O1-only** (`a-class-D2-guard-fork-verdict.md`) · **R1 CONFIRMED, dispatch fix adopted** (`a-class-R1-reachability-verdict.md`). H-1 triple-verified (regression at `f973b5c`, commit-message-confirmed); no include cycle; no circular leg deps; leg-0 byte-inert at default cfg; plan facts verified (36 cohort · 45 keys · 33 test sites, not "~40").

**All twelve amendments ADOPTED, dispositions:**
- **A-1 (leg-0 shape PINNED):** signatures SHRINK then de-default — `exit_bandit_enabled` and `fee_rate_taker_for_cf` cease to be parameters at all (read from `node_cfg->ml_cfg_flags` / `node_cfg->fee_rate_taker` at point of use — shape C + A-3); the remaining tail (`confidence_ic_variant`, drift trio, `node_cfg`) goes NON-defaulted on BOTH fan and OneCore; the EngineCommon binder owns cfg→args; the fan survives as the per-core loop. The **33 test sites** (measured; 30 wrapper 3-arg + 3 OneCore 4-arg) get one mechanical +args sweep. Arity-mismatch = compile error ⇒ the real class closure; `-Werror=float-conversion` (engine TU: measured 1 pre-fix warning = the defect) is the PARTIAL belt on top, engine target now, others measured in-build first.
- **A-2:** registry-derived clamp for `confidence_ic_variant` (dispatcher clamp-to-0 + max from `FOREACH_IC_VARIANT_COUNT-1`) rides the leg-0 commit — closes the cfg-range poisoning path, not just relocates it.
- **A-3:** the fee param DIES; point-of-use per-node read completes the recorded WIP2d-1.B.1 decision; `SlowPath.hpp:92-97` becomes TRUE rather than deleted.
- **A-4:** leg-3's `TryLoadRole` edit is APPEND-ONLY with all-8-call-sites explicitly audited in the commit; the 10-function defaulted-tail cohort → TECH_DEBT entry at close; the -Werror guard is documented PARTIAL (type-crossing only).
- **A-5:** leg-0 acceptance re-scoped — real-backtest asserts buy-Exp3 + buy-Thompson files; the four-file assertion is a synthetic-zoo test now + real in leg 4.
- **A-6:** leg-2 acceptance pins the load shape (single-zoo dir, trained-without-feature positive control on the SAME shape). **`NodeModelZoo_LoadLegacy` stamp-less bypass** (`EngineCommon.hpp:327`) disposition: document-as-intended — on `node_N_model_path` deployments the drift lines are TRUE "cannot verify an unstamped model" signals, not false noise; added to the e2e protocol's noise-list caveats; walker-in-LoadLegacy question folds into the drift-layer follow-up (register #6 family).
- **A-7:** fixed (single pinned order in § Bookkeeping below).
- **A-8:** register grows #17 (hot-swap triple: primary-keyed bundle-id vs swapped exit state · cached-horizon swap constraint · `Settings_RescanModels` exit-blindness) + the `EnsembleHotSwap.hpp` tests-only correction (H21 dead-code candidate).
- **A-9:** the seam test asserts ALL four re-bound positions (flag, ic_variant, fee-consumer, node_cfg-consumer); leg-4 arms echo state-file digests at run start.
- **A-10:** tests-TU directory-symlink compile blindness → LANDMINES line at close (sister of register #14).
- **A-11:** register #9 notes `buy_thompson_state.json` now lands by default post-leg-0 (accidental-transfer surface widens; the delete-between-arms instruction covers it).
- **A-12:** K=1 mixed-output exit arms promoted to a leg-3 gate item (assert the blend loop accepts binary arms beside 3-class).

**Operator re-scopes (2026-08-20, mid-gate):**
- **GUI panel PRUNE = PARKED** (operator: *"lets not prune gui yet, focus on fixing + this stuff"*). Tier B/C dispositions no longer owed for this program; the prune leaf keeps its earlier tier-A/B/C analysis for whenever it resumes.
- **Leg 3G reshaped, split by dependency:** **(3G-i, TODAY-eligible)** the Settings **category-grouping fix** — operator screenshots show the Global tab (and per-node tabs) emitting a CollapsingHeader per category-CHANGE while walking registry row order, so interleaved categories render as dozens of duplicate headers ("Trading" ×3, "Kill Switch" ×3, "Toggles" ×4, …). Fix = one-time category→field-index grouping built at init + curated category display order + grouped render on Global AND per-node tabs; a display-string vocabulary pass dedupes ambiguous category names. **NO registry reorder** — `FOREACH_CFG_FIELD` row order is wire-load-bearing (cfg-derived stamp-key ordinals; H21 positional ledger). **(3G-ii, AFTER the R1 dispatch fix)** the model bundle picker + resolution preview + hot-swap integration, as specified in the leg row.

**Bookkeeping (pinned):** baselines before code (identifier guard GREEN/93; Class-44 oracle PASS/5-pending; cache-layout strict-new). **Commit order (per A-7, D2 folded — the leg order IS the commit order):** leg 0 (fan/binder + A-2 clamp + A-3 fee-dies + backtest forwarding + savers + seam test) → leg 2 (walker + comments + tests) → leg 3 in three commits: 3-pre (R1 dispatch fix + blind sisters + R2 scaler seam) → 3-retire (T2 delete + burn + tests) → 3-role (side arm + WILL_PEAK default + F1/F2/F3 guard + role_files cohort + pure-helper tests). **NO TTY bless this ship** (D2 = O1-only ⇒ no new stamp key; T2 burn is tool-source, not ledger). FEATURE_LOOKUP + PARITY closures + CHANGELOG + `/post-ship-audit` at ship close. PARITY-042 queue-order coordination simplifies to nothing (no ledger movement from this ship).
