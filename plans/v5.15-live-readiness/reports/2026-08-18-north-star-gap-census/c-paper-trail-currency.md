# C-CLASS CURRENCY REPORT — in-code doc-system north star + fox-symdeps plugin paper trail vs HEAD

> Saved verbatim at receipt by the orchestrator (c-class agent report, 2026-08-18 north-star gap census, agent C of 3).

**Census date:** 2026-08-18. **Ground:** workspace `/home/caramel/code/tick-trader-percore-workspace/` (plans/DOCS/DESIGN_SPECS/tools), plugin repo `tools/plugins/fox-symdeps.nvim` at HEAD `77ab06e` (2026-08-16), engine `/home/caramel/code/FoxML_Trader_v2` (build.sh/asm sidecars/symlink verified on disk). Method: every claim below was verified by Read/grep of the current file or by `git log` of the plugin repo — never by recall, never by token-count. `reports/2026-08-18-north-star-gap-census/` exists and is **empty** — this report is that census.

**The one-paragraph re-ground:** the E.1.2.B toolchain/plugin arc **closed 2026-08-14** (operator call; `handoffs/2026-08-10-E.1.2.B-derived-facts-integrity-arc-handoff.md:3,13` — status `superseded`, superseded_by the engine-resume handoff; active handoff singleton = `2026-08-17-E.1.2-D426-closed-guard-armed-next-B3-parse-handle-handoff.md`, engine-scoped). Between 2026-08-10 and 08-16 the plugin landed the entire 0.4 tag-native wave, the parity-fleet fixes, TD-257/TD-258 closes, and the 0.5 shipped-asm + regfit cards. Almost none of that was written back into the plan body, the north-star, or several ledger status fields. The authoritative remainder statement is the arc-close line in the handoff coding_status (`:13`, TENTH boundary): *"Remainder HOMED, not dropped: R5 = already delivered by the board; registry [ROW]/[COLUMN] cards + `.toolbus` stay at the plan's 0.5 line as the post-engine tail; 0.6 (AST/TD-256) / 0.7 (verdicts) / 0.8 (planes) are later increments."*

---

## (1) STILL-OPEN — the genuine finish-list (ranked by operator value)

| # | Item | Home doc (where a finisher flips it) | Evidence it is still open |
|---|---|---|---|
| 1 | **Registry `[ROW]/[COLUMN]` cards** (the polymorphic REGISTRY card facet) | plan body `subplans/2026-07-19-E.1.2.B-toolchain-v1-plugin-first.md:186` (0.5 line) + north-star §6/§7.5 | No render module in the plugin (`rg "\[ROW\]|\[COLUMN\]"` over `lua/fox-symdeps/` = 0 code hits); grammar substrate IS ready (`in-code-documentation-schema.md:234-238` — D-339 landed the sub-schema). Named the post-engine tail at arc close (handoff `:13`). |
| 2 | **Register-pressure checker** (spill counting; ideas §11.5, §12 rung 4) | `plans/_ideas/2026-08-07-operator-ideas-luts-asm-compile-piping.md:303-320, 399-400` | No spill counting anywhere in the plugin (only cache-residency "spills a line" wording at `dashboard.lua:20` / `ambient.lua:14` — exactly the confusable family §11.5 warns about). **Its HARD dep is now SATISFIED**: TD-257 closed 2026-08-14 and the sidecars exist (`build/asm/*.asm` verified on disk) — §12 rung 4 says "now UNBLOCKED". Nuance: `check_latency_path_conformance` has a stack-spill axis, but it is manifest-kernel gate-side, not the per-function card/HUD-chip fact §11.5 specifies. |
| 3 | **`.toolbus/`** (volatile-fact rendezvous) | plan body `:185` (0.4 line — STALE, says "lands HERE") + ideas §11(v.5) (re-sequenced to 0.5) | No `.toolbus` dir in workspace tools/ or engine root (verified). Ideas §11(v.5) records the re-sequence AND an owed edit — *"Annotate the plan's 0.4 line at the next planning touch"* — never made. Note: 0.5's volatile consumers landed via the `build*/asm/` sidecars instead, so the finisher should re-derive whether `.toolbus/` still has a first genuine consumer. |
| 4 | **Ideas §12 remaining rungs**: rung 2 per-line SHIPPED-cost chips (explorer per-line costs are editor-flags; shipped card shows per-function counts only), rung 5 cross-binary same-fn DIFF (the "also in" sweep exists, `be03cbc`; a diff view does not), rung 6 perf-annotate ceiling | ideas §12 `:390-403` | Plugin log: rung 1 `--inlines` LANDED (`bdac9e3`), rung 3 BUDGET+SIMD chips LANDED (`ce60547`); no commits for rungs 2-shipped/5-diff/6. |
| 5 | **TD-252 — plugin repo runs NO git hooks** | `DOCS/tech-debt/open.md:3591-3598` | Verified live at HEAD: `git -C <plugin> config --get core.hooksPath` exit 1, no `.githooks/` dir. Trigger is ARMED: the `_RETIRED_UNIT` soak-then-delete (item 6) is exactly what TD-252 says wants a gate first. |
| 6 | **0.3 soak-then-delete of the `_RETIRED_UNIT` Lua mirrors** | sidecar `subplans/2026-07-19-E.1.2.B-sidecar-0.3-plugin-cutover.md` + D-349 PASS≠done | Both copies still present, neutered not deleted: `tag_grammar_adapter.lua:31-33`, `tagcontext.lua:14-16` (`local _ = _RETIRED_UNIT`). |
| 7 | **0.6 — TD-256 AST fact-producers + the AI-plane surface** | `open.md:3610-3618` (TD-256, med-high) + plan `:188` | Open in ledger. **Currency note a finisher should write:** TD-256's "⚠️ Depends on TECH_DEBT-257" (`open.md:3616`) is now satisfied — TD-256 is unblocked and the entry doesn't say so. TD-246 (`open.md:3491`) remains blocked behind it. |
| 8 | **0.7 — pre-push gate + PRODUCER/GATE `[TAG]` role + gate-verdicts as `vim.diagnostic`** | plan `:189` | Both repos have only `pre-commit` in `.githooks/` (verified); no verdict-diagnostic surface in the plugin; north-star §8.6 "pre-push is the clean add" still true. |
| 9 | **0.8 — `FOREACH_PLANE` registry + multi-comment-syntax parser + toolchain self-hosting (D-367)** + ideas §5 plane-index (its first consumer) | plan `:190` + decision log D-367 STATUS (`:2195` region) | `FOREACH_PLANE` exists only as prose in `tools/CLAUDE.md`; D-367 STATUS correctly says execution is a future increment. Current as stated — genuinely open. |
| 10 | **1.0.0 — the D-415 v1 C++ conversion program** (per-surface, against locked contracts + accumulated validity teeth; foxtag re-arms then) | decision log D-415 (`:2809`) + plan `:191` | foxtag is frozen-kept, Check T PARKED (`FOXTAG_REARM=1`), layout `auto` = Lua (handoff WILL-BITE `:79`). Nothing to build now; it is the far anchor of the finish-list. |
| 11 | **TOOLCHAIN_VERSION discipline drift**: `tools/TOOLCHAIN_VERSION` = `0.1.0` while 0.2 is COMPLETE (D-418) and the 0.4/0.5 arcs landed | `tools/TOOLCHAIN_VERSION` + D-373 (MINOR = new surface/capability) | Verified `0.1.0` on disk. Either bump per D-373 or record why increments don't map to file bumps — as-is the version SSoT misreports the product. |
| 12 | **Ideas §4 declarative tag-driven codegen ("the big thing") — PLACEMENT STILL OPEN** | ideas `:99-129` ("Operator places it at the next planning touch"); pointer at plan `:185` | No placement decision in the log; the planning touch never happened. Highest-weight operator idea still unhomed-to-an-increment. Also open, dive-gated: §3 template autogen, §8 chain badges/data-flow graph, §10 tag-scoped mass-ops + `foxtag arm` context packer, §7 tools-dir structure (with §6/v1), §1 LUTs (ENGINE plane, deliberately parked behind the toolchain gate). |
| 13 | **(g) operator-paced triage queues**: 4 WARN fired-triggers (TD-119 x2 / 120 / 126) + 58 INFO contract-stale re-homes | handoff TaskList #1 (`:56`) | Recorded open at arc close; no closing evidence since. |
| 14 | **Task #5/#6 backlog**: TD-246 (`open.md:3491`) · TD-259 (`open.md:3622`) · the ledger **id-line format split + stragglers** | handoff `:59` | All verified open. The id-line item has two live instances found by this census (see list 2, items 5-6). Note: the same line lists TD-254(c), which HAS since closed — strike it. |
| 15 | **TD-243 remainder** (C5 codegen-write regression guard · call-graph coverage · shared `calibration_fixtures.py` · foxtag code-units cutover — the cutover now v1-gated by D-415) + **TD-236** (latency-budget coverage gate, unblocked since conversion complete) | `open.md:3474-3481` (TD-243) · `:3432-3436` (TD-236) | Both open; TD-243's own 2026-07-18 currency note already marks two sub-items landed. |
| 16 | **North-star §10 residuals that are GENUINELY open**: (a) the DERIVED-set *enforcement* half (the spec half landed — D-339 axes table + `N/A_FOLDED`/`[LAT_EXEMPT]`, schema `:273-277`; enforcement = TD-243/TD-236); (b) the tag-system doc home (no `tag-system/` subdir exists — co-location in `doc-disciplines/` won by inertia; decide-or-close, cosmetic) | north-star `:307-315` | See list 3 for the rows in §10 that are NOT open anymore. |
| 17 | **Candidate stale-open, needs one verification at finish-off — TD-231** (shared clang flag-filter HUD+CI) | `open.md:3395-3399` | Its close condition ("the gate lands with the shared filter wired") likely fired long ago — `check_cache_layout` drives the plugin's own Lua emitters headlessly, and TD-257's `flags_for` rework re-cut the flag path. Verify, then flip or re-scope. |
| 18 | **Residual overlay honesty check — `branchtag.M.verdicts`** (`branchtag.lua:61-77`) greens "branchless" whenever the branch-detail set is empty — on a vacuous/empty parse this is the RC-E shape at the overlay layer. The producer-side guard landed (D-351) and the shipped-asm card is honest-by-design; verify whether an upstream caller gates the empty-parse case before painting, else this is the last RC-E residue. | north-star §5 RC-E + plugin `branchtag.lua` | Code-read this census; upstream gating not established either way. |

---

## (2) CLOSED-SINCE-CLAIMED — the doc edits a finish-off session owes

| # | Item | Closing evidence | Doc still wrongly claiming it open |
|---|---|---|---|
| 1 | **TD-257** (compile_commands 1:1 / real-flags facts) | Ledger CLOSED 2026-08-14, 4-leg disposition (`closed.md:1580-1592`); plugin `9209258`; engine `2654ad4`/`dc6affc`; ws `a328418`; `build/asm/` sidecars + `emit_asm_for_dir` chained in `build.sh:108-321` (verified on disk) | Plan body 0.5 line `:186` still opens with "⚠️ PRECONDITION (TECH_DEBT-257 … HIGH) … BEFORE rendering any asm/layout/register-fit card" — the precondition is met and the cards shipped. Also TD-256's dependency line (`open.md:3616`) un-annotated; north-star §5 RC-B + §8's "RC-B compile-DB story" (see list 3). |
| 2 | **TD-258** (registry-driven plugin consumer surface) | Ledger CLOSED 2026-08-12, all three fix-shape legs (`closed.md:1567-1578`); plugin `9d6c98b`+`3566412`; floor gate `check_toolio_kind_parity.py` HARD | Plan body 0.4 line `:185` still reads "⚠️ DESIGN CONSTRAINT (TECH_DEBT-258 …): the plugin's consumer surface must be REGISTRY-DRIVEN before these land" — it is, and it fired live twice (TD-257 closure text). Fleet register roadmap line also lists it (point-in-time honest, now stale). |
| 3 | **TAG ADD row** | Plugin `7da124e` (2026-08-11, `tagadd.lua`, 8/8 teeth); recorded shipped at ideas §11(ii) `:349-352` | Fleet register closing roadmap (`plan_checks/2026-08-10-plugin-parity-fleet-register.md:212-216`) lists it among out-of-arc roadmap features. Of that 5-item list only **register-pressure §11.5** and **TD-252** remain open — 3 of 5 landed within days. |
| 4 | **0.5 asm cards** | Plugin `be03cbc` (card) + `730cee1` (sync) + `df66414` (follow) + `5c3a092` (branch marks/call-follow) + `bdac9e3` (--inlines) + `ce60547` (budget/SIMD chips) + `c0ecbbe` (regfit RC-F card) | Same fleet-register roadmap line; north-star §7.5/§6 markers (list 3). |
| 5 | **TD-247** (plugin suite's normalized red — hardcoded engine line pins) | The prescribed fix ("re-anchor to a stable locator") landed 2026-08-10: `tests/test_byte_layout_cascade.lua:44-69` now SELF-DERIVES expected lines by scanning for the definition ("the old integer pins (130/144/72) went stale" — the file says so itself); suite green 46/46 at arc close (handoff `:13`) | `open.md:3506-3511` still `status: open`. Flip via `check_tech_debt.py --close` after one suite re-run. |
| 6 | **TD-254** (index silent-drop; all three legs incl. the stage-6 promotion) | `closed.md:1557-1565` — "Status: CLOSED (2026-08-10)", all legs terminal | Two residues: (a) its own id-line at `closed.md:1551` still reads `**status:** open` — greppable-status drift inside the closed ledger (an instance of the very "id-line format split" item in handoff task #5); (b) handoff TaskList #5 (`:59`) still lists "TD-254(c)" as backlog. |
| 7 | **0.2 completion + the whole 0.4/0.5 arc vs the plan body Status header** | D-416/D-417/D-418 (log `:2812-2822`): (g) COMPLETE, (d)+(a) LANDED, "0.2 IS COMPLETE" 2026-08-10; then the fleet + 0.4 wave + 0.5 arc through 2026-08-14 | Plan body Status header frozen at **v1.5 (2026-08-07)**: "0.2 IN FLIGHT … Remaining (g) → (d)" (`subplans/…-toolchain-v1-plugin-first.md:22`). This violates the plan's own stale-prose guard (`:47`) across ~6 landed boundaries and is the **primary repair target** for a finish-off session — the plan body is the roadmap SSoT a finisher would otherwise mis-trust. |
| 8 | **Decision-log STATUS sentinels for the closed arc**: D-413 ("fix sequenced BEHIND the sweep"), D-414 ("sweep in-flight; step-(3) contract pull-forward PROPOSED pending operator"), D-372 ("Executing: … 0.2 is next") | The sweep closed (leaves 1-4 all shipped; handoff TaskList `:56-60`); the (a) contracts LANDED with (d) at D-418 (`DOCS/TOOLCHAIN_CONTRACTS.md`); 0.2/0.4/0.5 landed | Log `:2802` (D-413), `:2806` (D-414), `:2427` region (D-372 STATUS). All three sentinels describe in-flight states for work that finished 2026-08-10..14 — decided-vs-open drift inside the log itself. D-415's sentinel ("operator may flip at the C1 session") is acceptable — the park held through C1, but a LANDED note would close it cleanly. |

---

## (3) NORTH-STAR STALE-PROSE (`DESIGN_SPECS/doc-disciplines/in-code-doc-system-north-star.md`) — both directions

**§ 8 status paragraph (`:228-247`) — frozen at 2026-07-15 (D-348), now ~8 boundaries behind. Specific false claims:**
- `:244-246` "Remaining increment 2c = `foxtag update` (the D-327 STRUCT writer) + the RC-B compile-DB story + the generalized drift-gate — then the P6 CONVERSION's hard toolchain dependencies are met" — **dead on all three**: P6 conversion completed 2026-07-18 *without* 2c (§8's own phased-path item 5 says so — internal contradiction); `foxtag update` was never built and foxtag is now **FROZEN-KEPT with the parity gate PARKED** (D-415, log `:2809`; handoff WILL-BITE `:79`); RC-B closed by a different mechanism (TD-257: `CMAKE_EXPORT_COMPILE_COMMANDS=ON` every build dir + shipping-first `flags_for` + objdump sidecars).
- `:247` "Plugin major-features work, overlays/asm half-broken (phase 5, operator's session)" — **false now**: the 0.4 wave (cursor-tracking `0b08c1e`, board role-swap `699d1c2`, COMPARE `28fc247`, docview `e380a5a`/`c15a479`, filterable trees `1b95f7a`/`46f4dbb`, TAG ADD `7da124e`, one-registry/runner/renderer `30f48f7..f0b1149`) + the 0.5 shipped-asm card family + regfit; suite 46/46, 127+ teeth at arc close.
- `:249-254` phased path: item 5 carries a DONE mark, item 4 (plugin rework) carries none though its V1 cycle closed 2026-08-14.

**§ 5 toolchain-gaps list (`:100-120`) — the RC statuses have mostly flipped:**
- RC-B — CLOSED (above). RC-C — CLOSED (D-351 real width-class incl. scalar-xmm; `ce60547` SIMD chips). RC-E — CLOSED producer-side (D-351) and card-side (honest INLINED-AWAY/STALE/refusal states, `be03cbc`); overlay residue = list-1 item 18. RC-A — superseded-in-approach (sidecars read the linked binary; `--isolate` probe-TUs; dependent-template probes `0b0f3a8`). RC-D — flags half closed (shipping-first); RC-D′ — subsumed by RC-F(a) as §5 itself predicts. **RC-F — SHIPPED** (`tools/check_register_fit.py` + `foxtag fields` + the `<leader>dR` card `c0ecbbe`) — the long §5 RC-F paragraph still reads as operator-envisioned future.

**§ 4 format layer (`:77-90`):**
- `:88-90` "PENDING the comment-shape survey … Do NOT freeze the schema before then" — the survey landed (D-333; `format-input-space-taxonomy.md`), all 14 shapes verified (D-345), and the schema **FROZE at `[SCHEMA]_[v1.0]` 2026-07-14** (`in-code-documentation-schema.md:17`).
- The gap bullets are mostly resolved at the lock: multi-section discipline blocks → labeled `[COMMENT]_[<label>]` (D-348, schema `:244`); the `[SWAR]` axis → **DECIDED** as `[BIT_PACKED]_[SWAR]`, "the first canonical" sub-tag, auto-detected from the idiom (schema `:229`); registry sub-schema → `[ROW]`/`[COLUMN]` landed (schema `:234-238`); per-type `[DERIVED]` set → specified by the D-339 axes table + `N/A_FOLDED`/`[LAT_EXEMPT]` (schema `:273-277`), enforcement remainder tracked at TD-243/TD-236.

**§ 10 open decisions (`:307-315`) — decided-vs-open drift, 3 of 4 rows:**
- "D-SWAR … OPEN" → **DECIDED** (schema `:229`). — "Complete `[DERIVED]` set … gates the fact-producer completion" → spec half DECIDED (D-339); only enforcement remains (TD-243). — "`:FoxTagUpdate` naming + surface" → **superseded in substance**: the CI/write side is `tools/update_toolchain.py` (D-418) + `check_cache_layout --fix`; the editor side is per-card `r`-refresh + `:FoxSymdepsDerived!`; nothing named `FoxTagUpdate` exists in the plugin (grep = 0) and nothing needs the name. — "Doc home for tag-system docs" → the only genuinely-open row (no `tag-system/` subdir; inertia chose co-location), cosmetic.

**§ 6 / § 7.5 (target UX + inventory) — the OPEN-but-reads-as-done direction is also here:**
- `:183-184` "all the UX targets above are unbuilt" — false (above). But conversely §6/§7.5 correctly still-open items a finisher should NOT mark done: REGISTRY-SPECIAL card facet (list-1 #1), graph-browser re-root + back/forward over the tag graph (only the asm call-follow back-stack exists, `5c3a092`), tag-query with fact predicates ("SLOW_PATH fns > 500 instr" — only browse/filter-by-TAG landed), module grouping, scaffold generator (🟡 stands — templates exist, no generator; = ideas §4's substrate).
- §7.5 markers now wrong: tag-filterable trees / dual-panel compare / doc-viewer / `[REFERENCE]` resolver ⬜ → **built**; unit CARD ⬜ → largely built minus the registry facet; "derived-write 🟡 (no struct size, partial DERIVED)" → the layout quartet was corpus-filled (~900 tags, engine `9939ed6`; TD-243 note); "the complete schema ⬜ (blocked on the 7 decisions)" → LOCKED v1.0; "code-tag index ⬜" contradicts §8's own "code-tag index landed" (D-347) — `DOCS/CODE_TAG_INDEX.md` exists; "generalized drift-gate ⬜" — landed in substance per-axis (layout HARD `--strict-new` over a shrink-only baseline; call-graph `--callgraph-check` ADVISORY; codegen ratchet on real flags), not as one gate.

**§ 3 diagram (`:56-59`)** still names `:FoxTagUpdate` as the CI-gate verb — same supersession as §10.

**Frontmatter** `status: draft-v0.1` — a living-draft label, defensible, but the doc's self-description as "resolved *against this doc*" (`:16-18`) is currently unsafe: §5/§8/§10 would mis-resolve a fresh session. The finish-off session's north-star edit is a §4/§5/§8/§10 re-ground plus a dated §8 boundary.

---

## (4) ORPHANS (git work no register/plan tracks) — effectively none

- The 2026-08-10..14 plugin wave is fully homed: fleet register (all phases + dispositions), the derived-facts handoff boundaries 3-10, ideas §11/§12 ⏩ markers, TD-257/258 closure texts, D-419. FEATURE_LOOKUP enrollment is claimed at the handoff (`:13`).
- Two small post-close plugin fixes are tracked only by their own commits: `77ab06e` (2026-08-16, asmshipped header_base returned a template argument — INLINED-INTO named `true`) and `93a04d2` (bless terminals TermClose). Both are completed self-recording defect fixes, not open debt — no unhomed-debt violation. If the fleet-register convention is "every dogfood defect gets a register row," `77ab06e` is the only candidate row missing.
- Inverse-orphan worth naming: the **empty `reports/2026-08-18-north-star-gap-census/` directory** — a staged census with no artifact; this report is its intended content.

---

## Coverage statement

Corpus swept: the north-star doc (full); decision log v2 — the named entries D-320/321/326-337/346-351/367/372 plus every later plugin/toolchain-scoped entry found (D-373-375, 380, 384-385, 388, 390-399, 400-419 sentinel region read in full; D-400..412 located despite non-line-start formatting); fleet register (full); ideas file (full, §1-§12); `DOCS/tech-debt/open.md` + `closed.md` (grep-swept for plugin/foxtag/tag/nvim/docview/toolio + direct reads of TD-231/243/246/247/252/254/256/257/258/259/269); both E.1.2.B subplans; the 2026-08-09/08-10 toolchain handoffs + supersession chain; plugin git log (80 commits) + targeted code reads (`branchtag`, `tag_grammar_adapter`, `tagcontext`, `browse`, cascade test); on-disk existence checks (`.toolbus`, `TOOLCHAIN_VERSION`, hooks, `FoxTagUpdate`, `FOREACH_PLANE`, sidecars, `compile_commands.json` symlink, `update_toolchain.py`/`compile_command.py`/`check_register_fit.py`). NOT covered: running the plugin Lua suite (nvim-headless execution not attempted — suite-green claims cite the handoff's 46/46), MASTER.md sprint framing, and the engine-GUI reports (2026-08-14/15 ui-* — verified out-of-corpus). Refute-spots self-flagged inline: list-1 #17 (TD-231), #18 (branchtag upstream gating), and the `.toolbus` first-consumer question.
