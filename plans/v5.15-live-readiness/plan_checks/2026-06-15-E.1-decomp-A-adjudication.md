---
type: decomposition-adjudication
agent_class: A (Adversarial adjudicator)
lens: ADJUDICATION (by-layer N=9 vs by-spine N=6 hybrid)
inputs:
  - plan_checks/2026-06-15-E.1-decomp-D-by-layer.md (N=9)
  - plan_checks/2026-06-15-E.1-decomp-D-by-spine.md (N=6 hybrid)
  - meta-disciplines/plan-decomposition-and-future-aware-agent-arming.md (cut-line SSoT)
  - plan_checks/2026-06-15-E.1-precoding-gate-SYNTHESIS.md (cycle-1)
date: 2026-06-15
engine_head: 572c978 (.E.0.10 SHIPPED; 3635/0)
verdict: LOCKED at N=7 — by-spine's hybrid SKELETON (mechanical foundation → spine superstructure → framework) ADOPTED, with two by-layer amendments folded in (split rename↔relayout into 2 anchors; lift the cluster-CONTAINER shape out of the rename ship into its own early data-layout ship). REFUTE: sound, one residual HOLE the scaffolding MUST carry (the SPINE-2→SPINE-1 stub boundary).
---

# `.E.1` decomposition — A-class adjudication

## 0. Method + the code-reads that settle the inter-D disagreements (AR-11)

I did NOT resolve any factual disagreement by fiat. Five load-bearing claims differed or were unverified across the two D-proposals; I settled each against HEAD 572c978:

| Claim under dispute | Verdict (file:line) |
|---|---|
| `MAX_EXECUTION_CORES` is the real symbol; `MAX_CORES` is a phantom | **CONFIRMED.** `MAX_CORES` = 0 hits; `MAX_EXECUTION_CORES` spans ≥16 files (Limits.hpp + 15 consumers). `CoreContext`/`CoreContextDisplayMeta` inline at ControllerEventLoop.hpp:651/734 (no `CoreContext.hpp`). Both D-agents correct; v0.1 grep `MAX_CORES\b` is FALSE-CLEAN. |
| `FillEvent` / `ClusterState` / `AggregatorState` / `binding.cluster_id` / `owner_node_id` / `subaccount_id` are NEW (0 hits) | **CONFIRMED — all six are 0-hit NEW symbols.** Decisive for resolution #3: there is NO existing `binding.cluster_id` being indexed. By-layer's E.1.4 self-critique ("the aggregator already indexes binding.cluster_id") describes FUTURE code → the placement is a free DESIGN choice, not a code constraint. |
| The drainer-absorption (SPINE-2) and fill-handler (SPINE-1) share the same `ProcessFillCommand` body | **CONFIRMED + REFINED.** `OrderManager_ProcessFillCommand` (OrderManager.hpp:1379) splits cleanly at line 1450: lines 1393-1444 = slot-lookup/state-transition/dedup (the PLUMBING); line 1450 `OrderManager_HandleFill` → `g_fill_dispatch[]` (OrderManager.hpp:1306) → `handle_sell_fill`/`handle_buy_fill` (the MATH). The drain SHELL is `OrderManager_Tick` (:1573) + `OmsPhasedDrain` buckets (:259/275). **The seam is the `OrderManager_HandleFill` call at :1450 — a real function boundary.** This is the crux of resolution #3 below. |
| The Class-46 SELL whole-close bug is real | **CONFIRMED.** `handle_sell_fill` (OrderManager.hpp:1214) calls `Portfolio_CloseSlot(&oms->portfolio, pslot, fill_price)` at :1243 — ignores `fill_qty` → whole-close on a partial. The SPINE-1 root. |
| An assert-only / interim fill form exists from `.E.0.10` | **CONFIRMED (as INERT-kept).** OrderManager.hpp:1508 — "INERT but kept as the correct .E.1 form, CO-GATED with the A2/A16 cumulative-fill landing." The interim composes; both D-agents' premise holds. |
| The torn-read is `KillSwitchEvaluate` reading `oms->balance` off the producer thread while the drainer writes it | **CONFIRMED.** Async.hpp:435-449 — the live KNOWN-RACE comment; fix-note = "move kill switch eval to drainer thread OR atomic balance snapshot." Genuinely coupled to the single-writer/seqlock work → correctly placed in the coherence ship by both. |

---

## 1. The three resolutions

### Resolution #1 — the capital-superstructure cut: **BY-SPINE wins; by-layer's per-spine-acceptance-thread is SUBSUMED, not adopted as the cut.**

By-layer scatters the three D-214 spines across E.1.3 (purity/absorption) + E.1.4 (coherence/aggregator) + E.1.5 (fill). By-spine makes each spine one ship. **The cut-line SSoT principle 4 ("capital-critical clusters isolated + gated; bisectable under its own anchor") decides this for by-spine**, and the code-read sharpens *why*:

- A `tsan` failure or aggregator-drift assert in by-layer's E.1.4 could be the aggregator OR conc-5 OR the A22 pack — but in by-spine's coherence ship, with the rename (foundation) and the fill-MATH (later spine) in OTHER ships, **the same failure is unambiguously a coherence bug.** That is the entire point of a rollback anchor.
- A conservation-invariant break in by-layer's E.1.5 could trace to a torn ledger from E.1.4 OR a mis-book in E.1.5. In by-spine, coherence is upstream-CLOSED (the published-snapshot ledger), so a conservation break is unambiguously fill-math. Code confirms the dependency direction: `handle_sell_fill` mutates `oms->balance`/`oms->realized_pnl` (OrderManager.hpp:1248-1249) — the fill handler WRITES the ledger the kill-switch READS, so the ledger must be coherent (single-writer/seqlock) BEFORE the fill math is reworked. SPINE-2-before-SPINE-1 is a hard data dependency, not a preference.

By-layer's own §-Weakest-cut concedes by-spine wins on "verification locality of the safety case" and proposes keeping by-layer's ORDER + adding a per-spine acceptance thread. **That hybrid is strictly dominated:** the spines already HAVE a hard order (purity→coherence→fill is forced by the data dependency above + H22), so by-spine delivers by-layer's ordering AND the clean anchors — adding an acceptance-thread on top of fused layer-ships only re-documents what by-spine structurally enforces. **The HARD live-enable gate is therefore NOT one ship and NOT split across coherence+purity — it is the CONJUNCTION of two preconditions on two spine ships: coherence (the torn-read class CLOSED) is the D-168 BLOCKER; purity (per-cluster authority + A34) is the second gate; live stays OFF until BOTH land (+ all of `.E`/v5.16).** This matches by-spine's §2 `.E.1.2` ★ + `.E.1.4` framing exactly.

### Resolution #2 — rename↔relayout: **SPLIT into 2 anchors (by-layer wins over by-spine's 2-phase-1-anchor).**

By-spine fuses them under one anchor with Phase-A/Phase-B revert. By-layer splits into two ships. **By-layer wins on cut-line principle 2 (one rollback anchor per concern) read strictly:** a rename is a pure identifier substitution; a relayout is a byte-offset + snapshot-VERSION change. These are *different concerns with different failure modes* (a rename typo vs a wire/determinism break), and the SSoT principle is "one anchor per concern," not "one anchor per file-set." By-spine's defense ("they touch the identical file set → phase granularity suffices") conflates file-overlap with concern-identity — the file set is the same, but the *invariant each must preserve* is different (rename preserves byte-behavior; relayout preserves conservation + determinism under a NEW snapshot version). A `git bisect` lands on a ship anchor, not a phase, so two anchors is the bisectable choice. D-101 (numeric-final-before-rename) is already satisfied (Ship-A/B), so the relayout is SoA-only — confirming there is no type-churn forcing the two together. **Two ships: rename (name-only, frozen layout) then relayout (bytes-only, frozen names).**

### Resolution #3 — the two flagged boundaries + `binding.cluster_id` placement.

**(a) by-layer's E.1.4/E.1.6 boundary (aggregator indexes cluster_id before ClusterState exists).** Code-read: `binding.cluster_id` is a 0-hit NEW symbol → no constraint, free choice. **Resolution: lift the cluster-CONTAINER SHAPE (the `ClusterState` struct skeleton + `NodeState.binding{cluster_id, subaccount_id, client_order_id_prefix}` + the flat `per_cluster[]` aggregate slot array) into the data-layout ship (the relayout ship), NOT a late ship.** This is by-layer's own predicted amendment ("move binding.cluster_id + flat per-cluster slots into the layout ship") AND by-spine's resolution ("the container STRUCT is layout → lands in rename+layout; the FOREACH_EXCHANGE MACHINERY is late"). Both D-agents independently converged here — I adopt it. The cut-line caveat-5 ("distinguish FOUNDATIONAL layout that only LOOKS like framework") is the governing principle: the *container shape* is foundational data-layout (sequences with the relayout); the *adapter/WS/registry machinery* is additive framework (sequences last). The aggregator ship then indexes a `cluster_id` that already exists in the frozen layout — no HOLE.

**(b) by-spine's E.1.2/E.1.3 boundary (SPINE-2 + SPINE-1 share the relocated `ProcessFillCommand`).** Code-read settles it: the seam is `OrderManager_HandleFill` at OrderManager.hpp:1450 — a real function boundary. **Resolution: the coherence ship lands the COMPLETE drain plumbing — relocated `OrderManager_Tick` + `OmsPhasedDrain` buckets + single-producer ring + published-snapshot ledger + the INERT/assert fill leaf (the OrderManager.hpp:1508 form, unchanged); the fill ship rewrites ONLY the `handle_*_fill` leaf math (A16 accumulate + the Class-46 `Portfolio_CloseSlot`-by-`fill_qty` fix) inside the already-proven plumbing.** The M6 body-content enumeration (the 4 omitted drainer responsibilities) is the de-risk: if the absorption list is complete, the stub boundary at :1450 is clean. This is by-spine's own §5 mitigation, now code-anchored to a specific call site. **The scaffolding MUST carry this as an explicit OUTBOUND-seam contract on the coherence ship** (see REFUTE §3).

---

## 2. The LOCKED seam map (N = 7, ordered)

Anchor = `pre-v5.15.5.F.4d.1.E.1.N` per ship. The skeleton is by-spine's hybrid (mechanical foundation → 3 spine ships → framework) with resolution #2 splitting the foundation's rename+layout into two, and resolution #3a placing the cluster-container shape in the layout ship.

### E.1.0 — §4d guard HOLE-cluster closure (guards-first) · FOUNDATION (no spine)
- **Scope:** H7 (hot-path branchless grep-CI) · H10 (SIMD scalar-fallback → determinism gate: 2× run + ±`USE_NATIVE_128` byte-compare) · H14 (no-bitfield grep-CI) · H20 (SP/HP branchless grep-CI) · H8 (`LATENCY_BENCH` budgets → CI ratchet). Pure enforcement scaffold; ZERO behavior change.
- **TD-closed:** none (opens the net the rest ride).
- **Decisions honored:** D-82/D-83 (guards-first) · D-72 (this ship's gate IS the rolling-window net).
- **§4d/guard rows closed:** §4a H7, H10, H14, H20, H8 (HOLE→ENFORCED); §3 latency-ratchet, determinism-gate, CI-merge-gate.
- **INBOUND:** `.E.0.10` net (§4d complete; 3635/0). **OUTBOUND:** a STANDING green CI that RED-builds any later hot-path branch / bitfield / non-byte-identical SIMD / latency regression / determinism break. *This is the net, not the audits.*
- **Bisectable:** zero runtime change → any break is a CI-config break or a surfaced pre-existing violation. The cleanest first cut.

### E.1.1 — Core→Node mechanical rename + cfg-key CLEAN-BREAK · FOUNDATION (no spine)
- **Scope:** Mechanical `Core→Node` across the 77 files (RE-DERIVED grep on real symbols: `MAX_EXECUTION_CORES`→`MAX_NODES`, `CoreContext`/`CoreContextDisplayMeta`→`NodeContext`/`NodeContextDisplayMeta`, `state.cores[i]`→`state.nodes[i]`, `core_id`→`node_id`, `FOREACH_PER_CORE_CFG_FIELD`→`FOREACH_PER_NODE_CFG_FIELD`, per-core dirs→per-node). Layout UNCHANGED (names only). Delete `single_core` (D-61). cfg-keys: `core_N_*`→`node_N_*` + parser **HARD-REFUSE** unrecognized legacy keys (D-219/D-223 clean-break, the 8 `strncmp(key,"core_",5)` branches at ControllerConfig.hpp:2878-2968). Build+use the cascade tool (TD-175).
- **TD-closed:** TD-197 (cfg-flag-orphan retirement — hard-refuse subsumes) · TD-175 (build-and-use the cascade tool) · TD-002 (single_core LIVE full removal) · TD-129/135 (rename-adjacent stale-naming — verify subsumed; TD-135 regime_names confirm via `/registry-fit-audit`).
- **Decisions honored:** D-27 · D-61 · D-219/D-223 (clean-break + hard-refuse, NOT alias) · H21 (snapshot VERSION NOT touched here — identifiers only).
- **§4d/guard rows closed:** none new; RUNS UNDER E.1.0's net (proves it catches a careless-rename hot-path regression).
- **INBOUND:** E.1.0 CI green + armed. **OUTBOUND:** NO `Core*`/`MAX_EXECUTION_CORES` residue (RE-DERIVED grep empty); parser hard-refuses legacy keys; 3635/0 byte-behavior-neutral; cascade tool exists for E.1.2.
- **Bisectable:** pure identifier substitution on FROZEN logic → a test break is a rename typo, never a layout race or aggregator change.

### E.1.2 — NodeState SoA/Money relayout + `owner_node_id` + ClusterState CONTAINER shape + snapshot VERSION bump · FOUNDATION (no spine)
- **Scope:** AoS→SoA + AVX-512 (D-55) on Portfolio/Position (already Money-typed/16B — SoA-only, no type churn); add `owner_node_id` (D-209 venue-net foundation). Re-lay NodeState clusters (`hot` / `slow_account` / `drainer_state` / `binding` / `kill_flags`) `alignas(64)`. **Land the `ClusterState` struct SKELETON + `NodeState.binding{cluster_id, subaccount_id, client_order_id_prefix}` + the flat `per_cluster[]` aggregate slot array** (resolution #3a — the CONTAINER shape is foundational layout; machinery is E.1.6). Bump PORTFOLIO_SNAPSHOT_VERSION 6→7 + SHARDED_SNAPSHOT_VERSION + H21 tombstone + `check_identifier_retirement.py --update` + `check_struct_alignment.py` size-pins. F-096 double-split conservation assert (`legA.qty+legB.qty==intended_qty`). NO consumer-logic rework.
- **TD-closed:** none directly; LANDS `owner_node_id` (D-209) + SoA (D-55) + container shape (D-15/D-28) foundations.
- **Decisions honored:** D-55 · D-209 (sub-pool DEFERRED) · D-99/104/107/125/176 (USE Money) · D-101 (numeric-final already satisfied; layout-final-after-rename) · D-131 (epoch break FREE — regen fixtures) · D-15/D-28 (cluster hierarchy SHAPE is foundational layout) · H10/H12/H21.
- **§4d/guard rows closed:** §4a H10 (first real SoA AVX kernel w/ byte-identical scalar, validated vs E.1.0 gate) · H12 (explicit-padding static_asserts on new clusters) · H6 (alignas cross-thread on the seqlock-bound clusters).
- **INBOUND:** E.1.1 delivered `NodeContext`/`NodeState` by NAME + the cascade tool. **OUTBOUND:** SoA + `owner_node_id` + `ClusterState` skeleton + `binding.cluster_id` + `per_cluster[]` all PRESENT; snapshot VERSION bumped+tombstoned; size-pins green; determinism gate green ±`USE_NATIVE_128`. **The struct is FINAL — every spine ship reads/writes this frozen layout, no re-touch.**
- **Bisectable:** layout-only → a determinism/size-pin break bisects here, orthogonal-and-ordered vs the rename (E.1.1 = NAME, E.1.2 = BYTES, separate anchors).

### E.1.3 — Aggregator + cross-thread torn-read class + drainer absorption + conc-5 · SPINE-2 (coherence) · ★ HARD LIVE-GATE precondition 1
- **Scope:** GLOBAL O(1) push aggregator (D-34, NOT per-cluster; D-54 push) on native 16B `__int128` `cmpxchg16b` (24B/split-slot premise DEAD per D-214-F1). Aggregator = SOLE writer of global+cluster kill flags; nodes mirror via atomic read. **Closes the cross-thread torn-read CLASS** (9 sites, 3 LIVE incl. the Knight-shaped `KillSwitchEvaluate`-off-torn-`balance` at Async.hpp:435-449) via single-writer + seqlock-published snapshot (reader-side per `cross-thread-multiword-read-consistency-discipline.md`). Per-node slow-path ABSORBS the central drainer (Run.hpp lambda goes; relocate `OrderManager_Tick` + `OmsPhasedDrain`) — M6 enumeration MUST capture the 4 omitted (phased-drain ordering, `Order_BindPreResolved`/fee-rate binding, `slot>>partial_on` mapping, drainer H8 histogram). **LAND the COMPLETE fill PLUMBING with the INERT leaf (OrderManager.hpp:1508 form unchanged) — the math is E.1.4** (resolution #3b; seam = `OrderManager_HandleFill` :1450). conc-5 single-producer (assert + tsan-clean). 3-kill-flag bit-pack (F3). A22 `intended_*` gets its OWN seqlock pack (the money-aggregator does NOT reach it). H1 boot-reconcile `std::vector<std::future>`→`std::array<pthread_t,MAX_NODES>` + manual join. A3 half-flatten retry/back-pressure design (TD-170).
- **TD-closed:** TD-002 (central drainer goes — ControllerEventLoop removal) · TD-129 (per-node drainer arch) · TD-170 (FlattenAll retry) · TD-171 (conc-5 / capital guards) · TD-196 (seqlock snapshot supersedes count-lock) · TD-197 (torn-read class — the structural close). Closes Class 26/27 STRUCTURALLY + the torn-read class.
- **Decisions honored:** D-34 · D-54 · D-193/H22 (single-writer = purity mechanism) · D-168 (this is the GATE not the enable) · cut-line-principle-4.
- **§4d/guard rows closed:** §4b conc-5 (tsan-clean ENFORCED) + the torn-read class · §3 kill-switch-hierarchy + tested-flatten-all + watchdog/liveness · §4a H6 (seqlock packs) · the `FPN_AtomicAdd`/`USE_NATIVE_128` determinism row (aggregator 128-bit atomic = the gate's reference consumer) · Class-50 single-owner-`disk_file` detector (PARTIAL→ENFORCED).
- **INBOUND:** E.1.2's frozen NodeState (`slow_account`/`drainer_state`/`kill_flags`/`binding` clusters present+aligned) + grep-CI guards the absorption stays branchless. **OUTBOUND:** torn-read class `/bug-check` CLEAN; aggregator always-exact (drift=0); conc-5 single-producer tsan-clean; the published snapshot is the SOURCE E.1.4 (reconciler) AND E.1.5 (A34) read; **the fill PLUMBING is complete + tsan-clean with the INERT leaf — E.1.4 adds ONLY the leaf math.** ★ live BLOCKED until this lands (D-168).
- **Bisectable:** a break here is a CONCURRENCY break (race/torn/drift) — rename (E.1.1) + fill-math (E.1.4) are in OTHER ships → a tsan/drift failure is unambiguously coherence. The single highest-risk ship gets the cleanest isolation.

### E.1.4 — Fill-path completeness + single-owner-`disk_file` event-log · SPINE-1 (fill-completeness)
- **Scope:** Define `FillEvent<F>` (POD/H1, explicit `_padding`/H12, Money fields, persists→H9). REWRITE the `handle_*_fill` LEAF MATH inside E.1.3's proven plumbing: A16 intra-session partial-accumulate (weighted-avg re-entry + keep-slot-open, `order_complete`-gated) + Class-46 SELL-partial fix (`handle_sell_fill`:1214 → `Portfolio_CloseSlot`-by-`fill_qty` at :1243, currently whole-closes). A20 warm-tail replay (WS-fill watermark + persist + venue-exact commission through reconcile + `GetMyTrades` pagination). A21 venue-net qty leg (Σ venue-asset-qty reconcile + ledger-booked correction, NEVER raw `balance=exchange_usdt` clobber at Reconcile.hpp:608 + `ks_peak` source-purity). Single-owner-`disk_file` event-log (writer owns open/close/rotate via polled atomic; Reset/Init SIGNAL never touch `FILE*`; `LoadFromDisk` integrity gate; `OrderEvent` H21 bump IF per-fill data persists).
- **TD-closed:** TD-169 (venue fill-parser data-loss / venue-exact commission) · TD-202 (class-level single-owner-`disk_file`, beyond `.E.0.10` instance) · TD-195 (warm-restart exit-disarm — verify subsumed). Closes RBP Class 46.
- **Decisions honored:** D-209 (venue-net reconstruction; sub-pool deferred) · D-212/213/214 (fill real-handler; SPINE-1 reference app) · D-216 (A20/A21 legs) · D-215 (A20).
- **§4d/guard rows closed:** §4c conservation invariants (`balance==start+Σrealized` through fill+reconcile) · fill-path normalization (Class 46 ENFORCED) · `ks_peak` source-purity (CI/audit candidate).
- **INBOUND:** E.1.3's coherent published snapshot (the reconciler torn-read leg + A21 read the snapshot, not racily off the drainer) + the COMPLETE plumbing with the INERT leaf at the `OrderManager_HandleFill` :1450 seam + `owner_node_id` (E.1.2, attribution). **OUTBOUND:** partials accumulate (char-tests); SELL-partials reduce by `fill_qty`; warm-tail reachable + venue-exact-fee + idempotent; reconcile ledger-booked + replay-reproducible; event-log single-owner (tsan + recovery test); conservation holds. **The capital-accounting path is COMPLETE.**
- **Bisectable:** coherence (E.1.3) + rename (E.1.1) upstream-CLOSED → a conservation-invariant or fill-accounting break is unambiguously fill-math, isolated from the aggregator race surface.

### E.1.5 — Per-cluster authority relocation + A34 concentration governor · SPINE-3 (purity) · ★ HARD LIVE-GATE precondition 2
- **Scope:** NEW-1 per-cluster relocation: `ControllerConfig_IsLiveCapital` body `cfg.trading_mode`→`cluster.trading_mode` (ControllerConfig.hpp:2012; one-cluster=one-exchange) as a SINGLE body edit, ZERO authorizer-site edits (the H22 scale-out seam — a Node never independently authorizes; it INHERITS its cluster's decision). A34 per-cluster concentration governor on the aggregator (N nodes long the same symbol = N× concentration owned by NOTHING) — per-cluster-PURE. DISTINCT from `.E.5` `max_per_node_pct`.
- **TD-closed:** TD-190 (A34 cluster-aggregate concentration governor) · TD-168 (live-OFF-till-`.E` per-cluster authority — verify subsumed).
- **Decisions honored:** D-193/H22 (purity) · D-217 (NEW-1 per-cluster relocation) · D-218 (NEW-1 HARD-REFUSE, reconciled `.E.0.10`) · D-168.
- **§4d/guard rows closed:** §4a H22-candidate (per-shard read of a global cfg field with a `core_N_*` override → CI-candidate; the relocation makes it UNREPRESENTABLE) · the A34 per-cluster-purity row.
- **INBOUND:** E.1.3's GLOBAL aggregator (A34 governs on the per-cluster running aggregate) + E.1.2's `cluster.trading_mode` field (relocation TARGET in ClusterState) + the UNCHANGED authorizer call-sites. **OUTBOUND:** capital authority per-cluster-PURE ((N+1)th-cluster test passes); concentration owned by the governor. **★ With E.1.3's coherence, the HARD-live-gate's BOTH preconditions are now satisfied** (live still OFF till all `.E`/v5.16 per D-168).
- **Bisectable:** a break is a PURITY break (a node reading global / a concentration mis-aggregation) — isolated from fill-math + coherence. Smallest ship (NEW-1 trivial; A34 the substance).

### E.1.6 — Multi-exchange FOREACH_EXCHANGE machinery + 4-state mode + dev/prod topology · FRAMEWORK (no spine)
- **Scope:** `FOREACH_EXCHANGE` + `FOREACH_SUBACCOUNT` meta-registries (Binance first canonical row; H15/H18/H19 enrolled) + `tt::submit_order<ExchangeT>` adapter dispatch (H13). Per-cluster adapter/WS/rate-budget MACHINERY wiring into the E.1.2 container shape (the SHAPE was early; the MACHINERY is here). 4-state per-node mode enum (backtest|paper|live|shadow; MBS_* per H14) + dev/prod ThreadTopology (OS-sched vs isolcpus/nohz_full).
- **TD-closed:** TD-162 (multi-exchange substrate readiness — verify subsumed). **TD-203 STAYS v5.16 — do NOT close here.**
- **Decisions honored:** D-3/D-58 (FOREACH_EXCHANGE day-one, generic-framework) · D-22/D-32/D-33 (4-state mode + dev/prod topology) · D-15/D-28 (machinery late, shape was early) · H15/H18/H19/H13/H14.
- **§4d/guard rows closed:** §4a H15 (registries enrolled) · H18 (sidecar) · H19 (topology) · H13 (tt:: adapter dispatch).
- **INBOUND:** E.1.2's ClusterState container shape + E.1.5's per-cluster authority. **OUTBOUND (= `.E.1` close):** adding the (N+1)th exchange = 1 FOREACH_EXCHANGE row; 4-state mode live; `.E.2-.E.8` substrate established; the DAG's `.E.1`→`.E.2..8` edges satisfied.
- **Bisectable:** additive framework on a complete trading core → a registry mis-wire is a compile error (H15/H19 CI) or a Binance-row regression; a rollback drops ONLY multi-exchange, leaving a fully-working single-exchange engine. Safest last cut.

### Cross-ship invariant chain (D-72 rolling-window)
```
E.1.0 ─[grep-CI+determinism+latency armed]→ E.1.1 ─[tree Node-renamed, hard-refuse]→ E.1.2 ─[NodeState SoA/Money + owner_node_id + ClusterState SHAPE + binding.cluster_id + per_cluster[] FROZEN, snap v7/v11]→
E.1.3 ─[torn-read class CLOSED + coherent snapshot + conc-5 single-producer + fill PLUMBING complete w/ INERT leaf @:1450]→ E.1.4 (leaf math) AND E.1.5 (A34 reads per-cluster agg)
E.1.4 ─[fill path COMPLETE + conservation holds]→ E.1.5 (governs on fill-fed aggregate) ─[capital authority per-cluster-PURE]→ E.1.6 (machinery wires into the pure container)
```
**HARD live-enable = E.1.3 (coherence) ∧ E.1.5 (purity), both required (D-168).**

---

## 3. REFUTE verdict

**The convergent foundation+superstructure hybrid is correctness-SOUND and ready to scaffold — with ONE residual HOLE the scaffolding MUST carry explicitly.**

I attacked it on the six failure modes the A-class is charged to find:

- **Severed cross-ship invariant?** NO. The chain is a strict topological order forced by real data dependencies (the fill handler WRITES `oms->balance` at OrderManager.hpp:1248 → the kill-switch READS it → coherence must precede fill-math; the relayout defines `cluster.trading_mode` → the NEW-1 relocation targets it → layout precedes purity). No ship relies on an invariant a successor (not a predecessor) provides.
- **Orphaned fold?** NO. All 13 named-only folds map (see §4 VERIFY). The interim fill leaf (OrderManager.hpp:1508) composes as the E.1.3 stub.
- **Guards-not-actually-first?** NO. E.1.0 is pure enforcement, zero behavior, and every later ship runs under it.
- **A sub-ship that can't bisect under its own anchor?** NO for E.1.0/1/2/3/5/6. **PARTIAL for E.1.4** — see the HOLE.
- **Re-litigated locked decision?** NO. D-219 (clean-break, NOT alias) is honored in E.1.1 (no `core_N_*` aliasing re-proposed — the v0.1 tombstone slip is NOT reintroduced); D-34 (GLOBAL not per-cluster) honored; D-131 (epoch-free) honored; D-168 (live-OFF) honored; D-214-F1 (16B not 24B) honored. Clean.
- **A HOLE where the assert-only interim (D-212) doesn't compose?** This is THE residual HOLE → ⬇.

**RESIDUAL HOLE (the scaffolding MUST carry it): the E.1.3→E.1.4 stub boundary at `OrderManager_HandleFill` (OrderManager.hpp:1450).** E.1.3 lands the drain plumbing + the INERT leaf; E.1.4 rewrites the leaf math. The risk (by-spine §5, now code-anchored): if E.1.3's absorption drops one of the 4 M6 responsibilities, a fill-accounting break in E.1.4 traces back to a *plumbing* gap in E.1.3 — the bisection muddies across the seam. **Mitigation the scaffold MUST encode as a HARD outbound-seam contract on E.1.3:** (1) the M6 body-content CSV enumerates ALL drainer responsibilities (the 4 named + any the enumeration surfaces) and E.1.3 does not close until each has a relocated home; (2) E.1.3's plumbing is tsan-clean + the INERT leaf is reached by a test that asserts "a fill COMMAND drains, dispatches, and hits the leaf" WITHOUT asserting leaf math; (3) E.1.4's inbound check is "the :1450 seam is reached + the plumbing is frozen" — E.1.4 touches ONLY the `handle_*_fill` bodies + `FillEvent`, never the drain shell. If the scaffold writes this contract, the seam is clean; if it leaves "the absorption is complete" implicit, the HOLE is live. **This is a scaffolding requirement, not a re-decomposition** — the N=7 map stands.

One lower note: E.1.4 internally spans 6 sub-concerns (FillEvent + A16 + Class-46 + A20 + A21 + event-log) under one anchor — by-spine §5 second-suboptimality. I do NOT pre-split it (they share one inbound contract = the coherent snapshot, and one outbound = conservation; you cannot assert "fills half-work" at an intermediate seam). If E.1.4 proves oversized at runtime, the natural further-split is {FillEvent+A16+Class46} | {A20+A21 reconcile} | {event-log} — a runtime-sizing call (principle 7), homed here as a note, NOT a defer.

---

## 4. VERIFY (the four mandated checks)

**(a) 13 TD + TD-175/TD-203 — each maps to exactly one ship:**

| TD | Ship | TD | Ship |
|---|---|---|---|
| TD-002 (ControllerEventLoop/single_core) | E.1.1 (single_core delete) + E.1.3 (central drainer) — SPLIT, see note | TD-171 (conc-5/capital guards) | E.1.3 |
| TD-129 (per-node drainer) | E.1.3 | TD-190 (A34 cluster-aggregate) | E.1.5 |
| TD-135 (regime_names) | E.1.1 (verify via /registry-fit-audit) | TD-195 (warm-restart exit-disarm) | E.1.4 (verify subsumed) |
| TD-162 (multi-exchange substrate) | E.1.6 | TD-196 (snapshot count-lock) | E.1.3 |
| TD-168 (live-OFF authority) | E.1.5 | TD-197 (torn-read class) | E.1.3 |
| TD-169 (venue fill data-loss) | E.1.4 | TD-202 (single-owner-disk_file) | E.1.4 |
| TD-170 (FlattenAll retry) | E.1.3 | TD-175 (build-and-use cascade tool) | E.1.1 |
| | | TD-203 | STAYS v5.16 (NOT closed) |

**Note on TD-002:** it is the ONLY TD spanning two ships — `single_core` LIVE removal (D-61) naturally lands with the rename (E.1.1); the ControllerEventLoop *central drainer* removal lands with the absorption (E.1.3). These are two distinct deletions under one TD id. **Resolution: TD-002 closes in E.1.3 (the drainer removal is the substantive close); E.1.1 lands the `single_core` sub-deletion and cross-links.** This is the one place a TD is not atomic-to-one-ship — flagged, not hidden. All other 12 are 1:1.

**(b) §4d guard rows + torn-read/kill rows each have an owning ship:** H7/H10/H14/H20/H8 → E.1.0 (with H10 re-validated in E.1.2's first SoA AVX kernel). H6 → E.1.2 + E.1.3. H12 → E.1.2. H21 → E.1.2 (snapshot) + E.1.1 (identifiers). H13/H15/H18/H19 → E.1.6. **The torn-read class → E.1.3.** kill-switch-hierarchy + tested-flatten-all + watchdog/liveness → E.1.3. conservation-invariants → E.1.4. H22-candidate → E.1.5. Every row owned.

**(c) `.E.0`-shape-match — NOTHING already-landed scheduled as to-do:** Verified against the currency floor + my code-reads. A1 (warm-restart predicate), A3-happy (honest-count), A6 (ingress/egress chokepoints), A8 (`slot>>partial_on` mapping — HARDENED `.E.0.10` F-046, now ABSORBED not re-built in E.1.3), A17 (`order_complete`), A20/A21-predicate, NEW-1-predicate (`ControllerConfig_IsLiveCapital` EXISTS at ControllerConfig.hpp:2012 — E.1.5 RELOCATES it, does not build it), GateParameters_FinalizeEmit, MODEL_CORRUPT/SHALT_MODEL_CORRUPT, Reconcile_SeedWatermark, quiesce-first OrderEventLog_Init — NONE scheduled as net-new. E.1.3 ABSORBS A8's hardened mapping (re-home, not re-do); E.1.4 builds on A20/A21 *predicates* (the legs, not the predicate). Clean — no already-landed work re-scheduled.

**(d) NO locked decision re-litigated:** Verified in REFUTE §3 (D-219/D-34/D-131/D-168/D-214-F1 all honored; the v0.1 `core_N_*`-alias tombstone slip is NOT reintroduced — E.1.1 is clean-break+hard-refuse).

---

## 5. Inter-D factual disagreements settled by code-read

1. **by-spine claimed SPINE-2/SPINE-1 "share the same relocated `ProcessFillCommand` body" (a fragility); by-layer treated absorption (E.1.3) and fill (E.1.5) as cleanly separate layers.** SETTLED: BOTH partially right. They share the body (by-spine correct) BUT it splits at a real function boundary `OrderManager_HandleFill` (OrderManager.hpp:1450) into plumbing (SPINE-2) + leaf math (SPINE-1) (by-layer's separability instinct correct). The locked map encodes the seam at :1450 explicitly (resolution #3b + the residual HOLE).
2. **by-layer's E.1.4 self-critique assumed "the aggregator already indexes `binding.cluster_id`."** SETTLED: FALSE at HEAD — `binding.cluster_id` is 0-hit NEW. The coupling is a FUTURE-code design choice, resolved by placing the container SHAPE in E.1.2 (resolution #3a). Both D-agents independently converged on this fix; it is adopted.
3. **Both cited `MAX_EXECUTION_CORES` ×52/×75/×78 with varying counts.** SETTLED: the count is enumeration-tool-output (RE-DERIVE at E.1.1 scaffold per `feedback_paste_tool_output_dont_summarize`); the SYMBOL is confirmed (`MAX_CORES`=0 hits, `MAX_EXECUTION_CORES` is real). Exact ref-count is not load-bearing for the cut.

---

**VERDICT: LOCKED at N=7. Correctness-sound, ready to scaffold.** The single residual HOLE (the E.1.3→E.1.4 stub boundary at OrderManager.hpp:1450) is a SCAFFOLDING contract requirement, not a re-decomposition — the scaffold must write E.1.3's outbound-seam contract as "complete tsan-clean plumbing + INERT leaf reached, math untouched" and E.1.4's inbound as "touch ONLY the leaf." With that contract carried, the map has no open HOLE across any outbound seam.
