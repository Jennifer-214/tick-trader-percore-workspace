---
type: handoff
ship_tag: v5.15.5.F.4d.1.B.3
plan_type: refactor
sprint_end_goal: make the codebase more maintainable for future development — frame engine for paper-test live readiness while consolidating framework discipline
ship_end_goal: close cfg-derived consumer drift + delete legacy registries + Phase K cleanups + ship close
predecessor_handoff: 2026-05-18-v5.15.5.F.4d.1.B.3-phase-l-coding-handoff.md (subsumed; Phase L REVERTED per Path C)
required_reading: [CLAUDE.md, CLAUDE.local.md, MEMORY.md, plan-body-v1.17, this-handoff]
coding_status: phase-f-landed
established: 2026-05-24
---

# v5.15.5.F.4d.1.B.3 post-Phase-F — remaining-work handoff

**Target ship:** `v5.15.5.F.4d.1.B.3` (Legacy empty-out + Path C bash CLI deletion)
**Branch:** `feat/v5.15-live-readiness`
**Plan body:** `plans/v5.15-live-readiness/subplans/2026-05-17-v5.15.5.F.4d.1.B.3-legacy-empty-out.md` v1.17
**Sprint MASTER:** `plans/v5.15-live-readiness/MASTER.md`

---

## § 0. What landed at Phase F (WIP-checkpoint 8) — context recap

Phase F same-commit trio (originally Step 1.6.4 + Step 1.6.7 + Phase L; **Phase L reverted to spec-only per Path C 2026-05-24**) plus HIGH-1 (b) cascade closure + production bug fix landed atomically.

**Engine-side closures:**
- ✅ Step 1.6.4 — production emit walker (`ML_Headers/ModelInference.hpp:1810-1824`) → `cfg_derived::populate_stamp_cfg_from_derived<F>` framework call (source-of-truth shift caller-inf → cfg at emit time)
- ✅ Step 1.6.7 — SOFT version bump 1→2 + `FOREACH_LEGACY_PREFIXED_KEY` parser back-compat for 15 wire keys + Layer 6b SOFT-bump procedure DESIGN_SPEC codified at `wire-format-byte-preservation-discipline.md`
- ✅ HIGH-1 (b) — `held_out_fraction` migrated to `FOREACH_GLOBAL_CFG_FIELD` with `STAMP_BOUND_CFG_DERIVED` bit; 4 orphan MC PRE_CFG `inference_cfg_*` rows DELETED; **ModelHandle gained cfg-derived auto-gen** via `STAMP_RESULT_DERIVED_FIELDS_AUTO_GEN()` (sister to ModelStampResult + StampInferenceCfgInputs); manual `held_out_fraction` decl/default/parser DELETED (Path α auto-gen covers)
- ✅ Production bug fix — `tt::cfg_parse_field` added `wire_context` bool param; cfg-FILE parser scales KIND_DOUBLE_PCT /100 (operator-`%`), wire parser does NOT scale (raw fraction). Closes silent 100× round-trip drift in stamp body for fee_rate_* fields. `parse_stamp_cfg_to_derived` passes `wire_context=true`. NEW lesson at `feedback_wire_context_vs_cfg_file_parser_separation`.

**Path C adoption (Phase L reverted):**
- ✅ `tools/stamp_model.sh` DELETED (48-line shim)
- ✅ `tools/stamp_model_cli.cpp` DELETED (477-LOC C++ binary)
- ✅ `CMakeLists.txt` stamp_model_cli target REMOVED
- ✅ 13 v5.9.5c bash CLI parity tests auto-skipped (file deleted; tests fall through to skip branch)
- ✅ `framework-driven-cli-binary-pattern.md` DESIGN_SPEC retained as Stage 2 DRAFT (first canonical queued for v5.16+ cmdline-invocable training)
- ✅ Operator workflow continuity: foxml_suite GUI auto-stamp via `Backtest_RunFullValidation → tt::Stamp_AssembleAndEmit<BACKTEST_FP>(...)` at `Backtest/BacktestEngine.hpp:1202`

**Test migration:**
- ✅ Class A (26 tests; v5.14.1.B.3.E + v5.9.2b + v5.9.5b + v5.14.8.A.7 + v5.14.9.C + v5.15.0.C) — migrated to cfg-driven flow (construct cfg, set fields, pass `&cfg` to `stamp_write_for_model<F>`)
- ✅ Class B (5 tests; v5.14.1.E.E.B + v5.14.9.F.2) — semantic shift acceptance documented (ml_cfg_flag walker bit-IS-value contract; presence-recorded unconditional has_*=1; VALUE follows bit)
- ✅ Group-bit assertions (`STAMP_HAS(v, inference_cfg)`) replaced with per-field has_<name> checks (cfg-derived auto-gen sets per-field; group bits semantically obsolete)

**Test state:** 3230 pass / 0 fail. Build: 6 binaries clean. All 6 CI checks PASS.

---

## § 1. Engine state at handoff write (VERIFY ON COLD-PICKUP)

| Field | Value |
|---|---|
| Engine HEAD | TBD (WIP-checkpoint 8 commit; verify via `git rev-parse HEAD` post-commit) |
| Branch | `feat/v5.15-live-readiness` |
| Version.hpp | `5.15.5.F.4d.1.B.2` (UNBUMPED — bump deferred to Step 9 ship close) |
| Build errors | 0 (all 6 binaries clean) |
| Tests | 3230 pass / 0 fail |
| 6 CI tools | All PASS |
| Engine binaries | `engine` / `engine_gui` / `foxml_suite` / `parity_harness` / `depth_recorder_test` / `controller_test` / `compare_scalers` — ALL CLEAN |
| WIP-checkpoint commits | 8 (`a406120` → `c13c127` → `3298ab2` → `9e99e91` → `d2931f3` → `3d27512` → `9f17ecb` → Phase F commit) |

**Cold-pickup verification (run first in fresh session):**

```bash
cd /home/caramel/code/FoxML_Trader_v2
git rev-parse HEAD                          # expect Phase F WIP-checkpoint 8 SHA
head -10 Version.hpp | grep STRING          # expect 5.15.5.F.4d.1.B.2 (unbumped)
git status --short                          # expect clean
git tag --list 'pre-v5.15.5.F.4d.1.B.3'     # expect: tag exists

# 6 CI checks (all expected PASS):
python3 tools/check_meta_registry.py 2>&1 | tail -2
python3 tools/check_per_core_registry_integrity.py 2>&1 | tail -2
python3 tools/check_field_name_uniqueness.py 2>&1 | tail -2
python3 tools/check_storage_t_coverage.py 2>&1 | tail -2
python3 tools/check_struct_field_uniqueness.py 2>&1 | tail -2
python3 tools/check_doc_metadata.py 2>&1 | tail -3

# Build + test state (expect CLEAN + 3230/0):
./build.sh test 2>&1 | grep -c 'error:'     # expect 0
./build/controller_test 2>&1 | tail -4      # expect "3230 passed, 0 failed"
```

Any divergence → investigate via `git log` since Phase F commit before proceeding.

---

## § 2. Plan body v1.17 — Path C transition note

Plan body Status header at line 8 now reflects v1.17. Key transitions:
- Phase L scope DELETED (bash CLI gone; spec retained as v5.16+ DRAFT)
- HIGH-1 (b) cascade close LANDED (held_out_fraction + ModelHandle auto-gen + 4 MC PRE_CFG row deletion)
- Production bug fix LANDED (cfg_parse_field wire_context)
- Test migration COMPLETE (Class A + B)

REMAINING per plan body for ship close: Step 1.6.6 + Step 2 + Phase K Steps 3-8.6 + Step 9.

---

## § 3. Remaining work — coding sequence

Per BUILD-FORCED sequencing in plan body.

### Step 1.6.6 — Drift walker migration (~1h)

Per plan body line 763-786.

- **1.6.6 main**: `CoreModelZoo.hpp:225-247` drift walker migration ALREADY LANDED at WIP-checkpoint 3 (commit `3298ab2`) — verify via `git show 3298ab2 -- ML_Headers/CoreModelZoo.hpp`. May be partially / fully complete; pickup needs verification.
- **1.6.6.a** — 4 CfgDriftCheck row substitutions to `COHORT_GATE_*` macros at `ML_Headers/CfgDriftCheckRegistry.hpp` lines 272 (`thompson_exp3_blend_alpha`), 300 (`ml_tp_pct`), 304 (`ml_sl_pct`), 308 (`barrier_blend_mode`). 4-line edit.
- **1.6.6.b** — CfgDriftCheckRegistry.hpp STAMP-side field-access rename — **15 row-edits** for `h->inference_cfg_<name>` → `h->name` for 15 names (4 ALREADY DONE in Phase F cascade for confidence_threshold_scale + barrier_gate_enabled + confidence_hard_block_threshold; **remaining 11**: bandit_blend_ratio + bandit_algorithm + thompson_mu_prior + thompson_precision_prior + thompson_precision_obs + thompson_exp3_blend_alpha + fee_rate_maker + fee_rate_taker + ml_tp_pct + ml_sl_pct + barrier_blend_mode + per_horizon_barrier_blend).

ModelHandle cfg-derived auto-gen (landed Phase F) means `h->bandit_blend_ratio` etc. exist as fields → renames just work via mechanical replace_all.

### Step 2 — DELETE legacy registries (FORCED LAST in Phase F; ~30 min)

Per plan body line 1045-1050.

- DELETE `MemHeaders/CfgDerivedInferenceCfgRegistry.hpp` — full file (orphaned post-Step-1.5; macro INFERENCE_CFG_AUTOPOPULATE is unused; tests already migrated). Remove the `#include` from `ML_Headers/StampHelper.hpp`.
- DELETE `ML_Headers/StampBoundCfgRegistry.hpp:99-179` — `FOREACH_STAMP_BOUND_CFG(X)` macro body (replace with `// SUPERSEDED — see DESIGN_SPECS/framework-patterns/cfg-derived-consumer-framework.md`)
- DELETE `ML_Headers/StampBoundCfgRegistry.hpp:230` — `STAMP_CFG_AUTOPOPULATE` macro body
- DELETE `ML_Headers/StampBoundCfgRegistry.hpp:264` — `FOREACH_STAMP_BOUND_CFG_COUNT`
- **Build BREAKS HERE if Steps 1.5 + 1.6.3 + 1.6.4 + 1.6.5 + 1.6.6 not addressed** — verifies migrations LANDED before deletion (all confirmed at WIP-7 + Phase F WIP-8).

### Phase K cleanup (Steps 3-8.6; ~3-5h)

- **Step 3** — Remove `FOREACH_REGISTRY` enrollment rows for deleted registries at `CoreFrameworks/MetaRegistry.hpp:52,99`. Per H15/H19 (`check_meta_registry.py` verifies).
- **Step 4** — Add CI Check 9 (compile-time `static_assert(cfg_field_count(g_per_core_cfg_stamp_bound_cfg_derived_mask) + cfg_field_count(g_global_cfg_stamp_bound_cfg_derived_mask) >= 24, "STAMP_BOUND_CFG_DERIVED coverage regression");` co-located with mask declarations at `CfgFieldRegistry.hpp:1079-1088`). NO Python tool.
- **Step 5** — v5.14 stamp fixture regression test (forward-compat per Decision F SOFT; verify v1 stamps LOAD CLEANLY on `.B.3` engine via parser back-compat; verify drift check fires correctly).
- **Step 6** — DESIGN_SPECS spec-body cleanup (mostly landed pre-coding; verify final state per plan body line 1069-1076).
- **Step 7** — Plan body residual SUPERSEDED ref cleanup if not done.
- **Step 8** — `/test-strength-audit` final pass over `.B.1`+`.B.2`+`.B.3` cumulative diff.
- **Step 8.5** — Extend `universal-cfg-field-registry-pattern.md` v1.0 → v1.1 with NEW § "Registry default precedence over manual defaults" (~30 min; lands BEFORE Step 8.6 sweep).
- **Step 8.6** — TECH_DEBT-107 47-globals registry-default sweep (~3-4h; per-row audit registry-vs-manual default; delete redundant manuals OR update registry to match). MUST land in its OWN commit (NOT bundled with Phase F or Step 2).

### Step 9 — Ship close (~2h)

Per plan body line 1104-1149 (v1.10/v1.12/v1.16 ship-close auto-writes preserved).

- `./build.sh all` — 6 binaries clean
- Tests GREEN
- Hot path UNTOUCHED — `tools/calls_graph_diff.sh verify`
- 6 CI gates PASS + NEW Check 9 PASS
- Version.hpp bump `5.15.5.F.4d.1.B.2` → `5.15.5.F.4d.1.B.3`
- TECH_DEBT bookkeeping (close: 001 / 018 / 093-100 / 102 / 104 / 107 / 110 / 098 / 099 / 096 / 094 / 095 / 097; open: 101 / 103 + any new from Phase F bug fix; defer: foxml_suite refactor per project memory)
- NEW Class codifications in `DOCS/recurring-bug-patterns/` (Class 31 wire-format duplicate-key + Class 32 prefix asymmetry + Class 18 cross-tool surface extinguished annotation)
- Commit; `git tag -s -a v5.15.5.F.4d.1.B.3`
- Postmortem at `postmortems/2026-05-24-v5.15.5.F.4d.1.B.3-postmortem.md` — must capture Path C transition narrative + wire-context bug discovery
- CHANGELOG.md `.B.3` row
- CLAUDE.local.md sprint state row update (Most recent → `.B.3`; Previous → `.B.2`)
- `/sync-workspace`; push only after operator authorization

---

## § 4. Required reading at cold-pickup (parallel)

**Auto-loaded:**
- `CLAUDE.md` (orientation)
- `CLAUDE.local.md` (operator overlay + sprint state index — UPDATED post-Phase-F to reflect v1.17)
- `~/.claude/projects/-home-caramel-code-FoxML-Trader-v2/memory/MEMORY.md` — UPDATED with `feedback_wire_context_vs_cfg_file_parser_separation` (Phase F lesson)

**Primary:**
- THIS HANDOFF (entire doc; § 3 is coding sequence)
- Plan body v1.17: `plans/v5.15-live-readiness/subplans/2026-05-17-v5.15.5.F.4d.1.B.3-legacy-empty-out.md`

**DESIGN_SPECS for remaining work (post-folder-subdivision paths):**
- `DESIGN_SPECS/framework-patterns/cfg-derived-consumer-framework.md` v1.3 (cohort framework)
- `DESIGN_SPECS/wire-format-patterns/wire-format-byte-preservation-discipline.md` v1.1 (Layer 6b SOFT bump + Layer 7 cross-tool obviation)
- `DESIGN_SPECS/refactor-patterns/framework-driven-cli-binary-pattern.md` v1.1 Stage 2 DRAFT (Phase L PATTERN; first canonical at v5.16+)
- `DESIGN_SPECS/framework-patterns/universal-cfg-field-registry-pattern.md` v1.0 (Step 8.5 amends v1.0 → v1.1)
- `DESIGN_SPECS/meta-disciplines/structural-fix-preferred-decision-framework.md`

**Source files (load before substantive coding):**

| File | Lines | Relevance |
|---|---|---|
| `ML_Headers/CfgDriftCheckRegistry.hpp` | 230-330 | 11 remaining STAMP-side h→ field renames + 4 COHORT_GATE_* substitutions at lines 272/300/304/308 |
| `MemHeaders/CfgDerivedInferenceCfgRegistry.hpp` | full | DELETE at Step 2 (orphan post-Step-1.5) |
| `ML_Headers/StampBoundCfgRegistry.hpp` | 99-264 | Step 2 deletions (FOREACH_STAMP_BOUND_CFG body + STAMP_CFG_AUTOPOPULATE + count macro) |
| `CoreFrameworks/MetaRegistry.hpp` | 52, 99 | Step 3 FOREACH_REGISTRY row deletions for deleted registries |
| `CoreFrameworks/CfgFieldRegistry.hpp` | 870-1050 | Step 8.6 47-globals registry-default sweep target |

**Memory rules (load if not already in context):**
- `feedback_wire_context_vs_cfg_file_parser_separation` (NEW Phase F lesson)
- `project_foxml_suite_refactor_queued` (post-`.F.4d.1.D` sub-sprint)
- `feedback_no_defer_for_effort` (5th-recurrence pattern)
- `feedback_motivated_collaborator_for_caramel`
- `feedback_proportionate_response_to_audit_findings` (Path C adoption was proportionate response to Caramel's pushback)
- `feedback_consult_on_audit_findings`
- `feedback_audit_canonical_sister_before_new_infra`
- `feedback_iteration_spiral_signals_audit_meta_gap`

---

## § 5. Pre-coding concerns (informational; not blocking)

### 5.1 Step 1.6.6 main — verify what's landed

Step 1.6.6 MAIN drift walker migration at `CoreModelZoo.hpp:225-247` per WIP-checkpoint 3 (commit `3298ab2`). Phase F cascade did NOT touch this; sequence assumed it landed earlier. Verify before starting Step 1.6.6.a:

```bash
git show 3298ab2 -- ML_Headers/CoreModelZoo.hpp | grep -E '(DRIFT_CHECK_FROM_DERIVED|FOREACH_STAMP_BOUND_CFG)'
grep -n 'DRIFT_CHECK_FROM_DERIVED\|FAILURE_MASK_cfg_binding_drift' ML_Headers/CoreModelZoo.hpp | head -5
```

If `DRIFT_CHECK_FROM_DERIVED(...)` is present at CoreModelZoo.hpp:~238 → Step 1.6.6 main DONE; proceed to 1.6.6.a.
If not → start with main migration.

### 5.2 Step 2 build break verification

Step 2 deletion will BREAK BUILD if any consumer still references `FOREACH_STAMP_BOUND_CFG` macro or `STAMP_CFG_AUTOPOPULATE`. Run pre-deletion:

```bash
rg 'FOREACH_STAMP_BOUND_CFG\b|STAMP_CFG_AUTOPOPULATE\b' --type cpp --type hpp 2>&1 | head -10
```

If only the registry definition + DESIGN_SPEC docs reference → safe to delete. If production code references → migrate FIRST.

### 5.3 Phase K Step 8.6 — 47-globals sweep operator decisions

Each row is case-by-case: MATCH (delete manual) / DIFFER-RECONCILE (update registry to match manual) / DIFFER-EXEMPT (keep manual + memory file). Surface each row's verdict in postmortem. `warmup_ticks` example: registry=0 vs manual=128 likely DIFFER-EXEMPT (historical operational reason).

### 5.4 TECH_DEBT-110 auto-close

TECH_DEBT-110 (bash shim deletion target) was originally trigger-deferred ("when operator scripts migrated"). Path C made it auto-closed (bash shim DELETED unconditionally). Update ledger entry to reflect this.

### 5.5 Wire-context bug fix coverage

Phase F added `wire_context` bool to `tt::cfg_parse_field`. Verify NO OTHER callers reuse it without thinking about context. Grep all callers + audit:

```bash
rg 'tt::cfg_parse_field\b' --type cpp --type hpp 2>&1 | head -10
```

cfg-FILE parser walkers at `ControllerConfig.hpp:2110/2125+` should KEEP default `wire_context=false` (operator-`%` convention applies). Wire parsers (only `parse_stamp_cfg_to_derived` at `CfgGateRegistry.hpp:534/544`) MUST pass `wire_context=true`. Any future wire-context dispatch site MUST follow the same discipline.

---

## § 6. Cold-pickup prompt (paste into fresh Claude Code session)

```
I'm picking up v5.15.5.F.4d.1.B.3 (Legacy empty-out + Path C bash CLI deletion + Phase K) for the v5.15-live-readiness sprint. Phase F same-commit + HIGH-1 cascade closure + Path C deletion LANDED at WIP-checkpoint 8 (2026-05-24). Remaining: Step 1.6.6 drift walker migration + Step 2 legacy registry deletion + Phase K Steps 3-8.6 + Step 9 ship close.

## Sprint end goal
Make the codebase more maintainable for future development.

## Ship end goal
Close cfg-derived consumer drift + DELETE legacy registries + Phase K cleanups + ship close.

## Step 0 — orient + verify state (MANDATORY)

1. SHA-diff trigger check:
   cd /home/caramel/code/FoxML_Trader_v2
   git rev-parse HEAD                    # expect Phase F WIP-checkpoint 8 SHA
   head -10 Version.hpp | grep STRING    # expect 5.15.5.F.4d.1.B.2 (unbumped)
   git status --short                    # expect clean
   git tag --list 'pre-v5.15.5.F.4d.1.B.3'   # expect tag exists

2. CI checks (6 expected PASS):
   python3 tools/check_meta_registry.py 2>&1 | tail -2
   python3 tools/check_per_core_registry_integrity.py 2>&1 | tail -2
   python3 tools/check_field_name_uniqueness.py 2>&1 | tail -2
   python3 tools/check_storage_t_coverage.py 2>&1 | tail -2
   python3 tools/check_struct_field_uniqueness.py 2>&1 | tail -2
   python3 tools/check_doc_metadata.py 2>&1 | tail -3

3. Build state — EXPECT CLEAN + 3230 / 0:
   ./build.sh test 2>&1 | grep -c 'error:'     # expect 0
   ./build/controller_test 2>&1 | tail -4      # expect 3230 passed, 0 failed

Any divergence → investigate via `git log` since the most recent commit before proceeding.

## Required reading (parallel; post-Phase-F state):
- CLAUDE.md
- CLAUDE.local.md + MEMORY.md (auto-loaded; both UPDATED post-Phase-F)
- THIS HANDOFF: plans/v5.15-live-readiness/handoffs/2026-05-24-v5.15.5.F.4d.1.B.3-post-phase-f-handoff.md
- Plan body v1.17: plans/v5.15-live-readiness/subplans/2026-05-17-v5.15.5.F.4d.1.B.3-legacy-empty-out.md (Status header line 8)
- DESIGN_SPECS:
  - framework-patterns/cfg-derived-consumer-framework.md v1.3
  - wire-format-patterns/wire-format-byte-preservation-discipline.md v1.1 (Layer 6b SOFT bump)
  - framework-patterns/universal-cfg-field-registry-pattern.md v1.0 (Step 8.5 amends to v1.1)
- Source files per handoff § 4
- Memory rules: feedback_wire_context_vs_cfg_file_parser_separation (NEW Phase F lesson) + project_foxml_suite_refactor_queued + feedback_no_defer_for_effort + feedback_motivated_collaborator_for_caramel + feedback_proportionate_response_to_audit_findings

## Coding sequence per handoff § 3:
1. Step 1.6.6 — drift walker migration (verify 1.6.6 main landed at WIP-3 first; then 1.6.6.a + 1.6.6.b 11-row rename; ~1h)
2. Step 2 — DELETE legacy registries (CfgDerivedInferenceCfgRegistry.hpp full file + StampBoundCfgRegistry.hpp body; ~30 min)
3. Phase K Steps 3-8.6 — MetaRegistry cleanup + CI Check 9 + v5.14 fixture regression + DESIGN_SPEC cleanup + /test-strength-audit + Step 8.5 spec extension + Step 8.6 47-globals sweep (~3-5h)
4. Step 9 — ship close (build verify + version bump + commit + tag + postmortem + CHANGELOG + sync-workspace + push; ~2h)

Total: ~6-8.5h focused.

## Pre-coding concerns (handoff § 5):
- Verify Step 1.6.6 main landed at WIP-3 before starting 1.6.6.a
- Pre-deletion grep verification at Step 2 (build break verifier)
- Phase K Step 8.6 case-by-case operator decisions
- TECH_DEBT-110 auto-close at ship close
- Wire-context discipline verification per § 5.5

## Per consult-before-coding discipline:
Caramel will iterate on substantive findings. Per feedback_consult_on_audit_findings — present findings inline; don't auto-proceed past substantive discoveries. Per feedback_plan_right_not_fast — sit with options long enough to honestly evaluate.

Phase F landed clean (3230 / 0). Time to finish Phase F's remaining work + ship close. Start with Step 1.6.6 verification.
```

---

## § 7. Notes for future-Claude

- **Phase F landed CLEAN.** No half-finished state. cfg-derived emit migration + SOFT bump + held_out_fraction Class 21 closure + production bug fix all atomic.

- **Path C was the right call.** Phase L bash CLI was edge-case-only infrastructure; foxml_suite GUI auto-stamp covers operator workflow. ~500 LOC + 13 parity tests DELETED net positive. Spec retained for v5.16+ first canonical when decoupling endgoal needs headless CLI.

- **Wire-context bug was a real production catch.** `cfg_parse_field` KIND_DOUBLE_PCT scaling reused across cfg-file + wire contexts — silent 100× drift in fee_rate stamps. ONE failing test caught it (`v5.9.2b: parsed fee_rate_taker matches stamp`). Without that test, would have shipped. Lesson: dual-context functions need explicit context awareness per `feedback_wire_context_vs_cfg_file_parser_separation`.

- **ModelHandle cfg-derived auto-gen is the deepest structural win at Phase F.** Now ALL three structs (ModelStampResult + StampInferenceCfgInputs + ModelHandle) auto-gen cfg-derived cohort fields via single `STAMP_RESULT_DERIVED_FIELDS_AUTO_GEN()` macro. Future cfg-derived field add = 1 row in master registry → all three structs auto-extend → consumers work without code edits. Drift impossible by construction at the struct-gen layer.

- **Caramel's Week 2 mindset.** She wants this DONE. Path C adoption was driven by maintainability over feature-completeness. Continue that discipline through Step 1.6.6 → Step 2 → Phase K → Step 9.

- **TECH_DEBT bookkeeping at ship close.** Phase F WIP-8 didn't update TECH_DEBT ledger; do that at Step 9 ship close (consolidates closures into single commit per plan body Step 9 process).

- **foxml_suite refactor IS NEXT MAJOR scope** (post-`.F.4d.1.D` close). Memory `project_foxml_suite_refactor_queued` captures the framing — Option A confirmed (consolidation BEFORE v5.16+ decoupling). Will need its own MASTER plan.

- **Per `feedback_compaction_degrades`:** verify HEAD + tests + CI before acting on any handoff claim. Even this handoff can drift after context compaction.

---

**Engine state at handoff write: WIP-checkpoint 8 just committed. Pre-tag rollback anchor: `pre-v5.15.5.F.4d.1.B.3` at `9b62a72`.**

Estimated ~6-8.5h focused coding to ship close.
