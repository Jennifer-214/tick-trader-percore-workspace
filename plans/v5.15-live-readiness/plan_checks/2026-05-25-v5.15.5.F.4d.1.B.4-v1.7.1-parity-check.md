---
type: audit-report
audit: parity-check
scope: targeted-re-fire-against-plan-body-v1.7.1
ship_tag: v5.15.5.F.4d.1.B.4
plan_body: subplans/2026-05-24-v5.15.5.F.4d.1.B.4-train-serve-execution-layer-parity.md
plan_body_version: v1.7.1
engine_head: 09204b3 (skeleton signature update at EngineCommon.hpp UNCOMMITTED)
date: 2026-05-25
predecessor_audits:
  - plan_checks/2026-05-25-v5.15.5.F.4d.1.B.4-v1.7-parity-check.md
  - plan_checks/2026-05-25-v5.15.5.F.4d.1.B.4-v1.7-blindspot-scan.md
  - plan_checks/2026-05-25-v5.15.5.F.4d.1.B.4-v1.7-bug-check.md
---

# /parity-check report — v1.7.1 targeted re-fire (2026-05-25)

## Executive verdict

**GREEN-WITH-3-MINOR-DRIFT** — Phase B Step B.2 unlock RECOMMENDED PROCEED.

All 13 v1.7.1 targeted fixes verified at HEAD `09204b3`. CRITICAL D1-B scope/state correction structurally sound and aligned with canonical sister consumer pattern at `ControllerEventLoop.hpp:2344` + `:3558`. All v1.7 verifications still PASS. No NEW substantive findings.

3 MINOR cosmetic drift items surfaced during v1.7.1 fixes-validation pass — these are amendment-internal inconsistencies (NOT engine-side gaps); do NOT block Phase B Step B.2 unlock. Operator may decide to amend a hypothetical v1.7.2 OR address inline at coding time OR accept.

Inflection per `feedback_iteration_spiral_signals_audit_meta_gap`: v1.7 → v1.7.1 = single-class targeted correction cycle (CRITICAL fix + sister mechanical sweep). NOT iteration spiral — each cycle addresses semantically distinct concerns. v1.7.1 cycle scope is implementation-of-v1.7-audit-validated D1-B path. Further audit cycle NOT recommended; coding can unlock with the 3 cosmetic drifts triaged.

---

## Plan summary

- HEAD `09204b3` (skeleton signature update at EngineCommon.hpp UNCOMMITTED)
- Tests: 3208 / 0 baseline (per CLAUDE.local.md)
- Audit scope: TARGETED RE-FIRE against plan body v1.7.1 (13 targeted fixes validation per operator spec)
- Cross-check baseline: 3-agent v1.7 audit synthesis (parity-check + blindspot-scan + bug-check converged 3-of-3 on CRITICAL D1-B scope/state mismatch)
- Engine surface: `CoreFrameworks/SlowPathGateRegistry.hpp` + `CoreFrameworks/ControllerEventLoop.hpp` + `CoreFrameworks/EngineSharded.hpp` + `CoreFrameworks/ShardedBacktestDriver.hpp` + `CoreFrameworks/EngineCommon.hpp` (skeleton; ~228 lines)

---

## Stage 0 DESIGN_SPECS preload (loaded for this re-fire)

- `DESIGN_SPECS/wire-format-patterns/wire-format-byte-preservation-discipline.md` — Layer 5b/7 still applies (HMAC byte preservation)
- `DESIGN_SPECS/framework-patterns/autopopulate-pattern-for-production-caller-class.md` — D1-B reuses canonical pattern
- `DESIGN_SPECS/framework-patterns/x-macro-registry-with-presence-dispatch.md` — FOREACH_SLOW_PATH_GATE row addition follows pattern
- `DESIGN_SPECS/wire-format-patterns/struct-padding-determinism-pattern.md` — N/A this surface
- `DOCS/PARITY_ISSUES.md` (workspace-symlinked) — PARITY-026..032 lifecycle tracked
- `DOCS/RECURRING_BUG_PATTERNS.md` Classes 14 + 18 + 21 + 24 + 28 — closures verified

---

## v1.7.1 13-fix verification — point-by-point (per operator spec)

### CRITICAL re-verification (D1-B scope/state alignment)

**STATUS: VERIFIED — STRUCTURALLY CORRECT**

| Verification Item | Status | Evidence |
|---|---|---|
| Body call now reads `state.global_gate_state.flags` matching `scope=ENGINE_WIDE` row declaration | PASS | Plan body line 689 + 690 explicitly cite `state.global_gate_state.flags` correctly |
| Step B.3 + Pre-coding concern #12 + v1.7.1 amendment summary all reference ENGINE_WIDE corrected access | PASS | Lines 46, 689, 690, 691, 707, 1029 all consistent |
| `state.global_gate_state` member EXISTS in EventLoopState | PASS | `ControllerEventLoop.hpp:784` — `GlobalGateState global_gate_state;` |
| `GlobalGateState` struct EXISTS with `uint16_t flags` field | PASS | `SlowPathGateRegistry.hpp:205-207` — `struct GlobalGateState { uint16_t flags; };` |
| AUTOPOPULATE_ENGINE_WIDE macro EXISTS | PASS | `SlowPathGateRegistry.hpp:236` — `#define SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE(state, _cfg_arg)` |
| ENGINE_WIDE AUTOPOPULATE targets `state.global_gate_state.flags` | PASS | Sister call sites at `ControllerEventLoop.hpp:2335` + `:3555` confirm targeting `state->global_gate_state` |
| Sister consumer pattern at `:2344` (MASK_LAZY_REBUILD_ACTIVE) | PASS | Verified at `ControllerEventLoop.hpp:2344` — exact match `BITMAP_IS_SET(state->global_gate_state.flags, MASK_LAZY_REBUILD_ACTIVE)` |
| Sister consumer pattern at `:3558` (MASK_WS_FLATTEN_ACTIVE) | PASS | Verified at `ControllerEventLoop.hpp:3558` — exact match `BITMAP_IS_SET(state->global_gate_state.flags, MASK_WS_FLATTEN_ACTIVE)` |
| No residual `state.cores[c].gate_state.flags` references in plan body for breakeven gating | PASS | `grep "state.cores\[c\].gate_state"` returns only audit-history references (lines 5 + 46 + 957 + 1029 + 1092) where v1.7 → v1.7.1 contrast cited; current PARITY-032 closure description correctly uses ENGINE_WIDE |

**CRITICAL fix verdict: D1-B body call alignment STRUCTURALLY SOUND.**

### v1.7.1 Verification Item 1: D1-B scope alignment

Per CRITICAL section above. PASS.

### v1.7.1 Verification Item 2: AUTOPOPULATE_ENGINE_WIDE caller-site refresh

**STATUS: SPEC-CORRECT — symbol-name MINOR DRIFT (Finding M-1)**

- v1.7.1 M2.B at plan body line 690 requires SlowPathCycleOneCore body call AUTOPOPULATE before BITMAP_IS_SET read. Spec is structurally correct (existing AUTOPOPULATE sites at `:2335` + `:3555` don't fire at slow-path-cycle entry; cached bit would be stale across cycles without refresh).
- HOWEVER: Plan body line 690 names `SlowPathGateState_PopulateEngineWide(&state, cfg)` as the lead symbol — this function does NOT EXIST in the codebase. The actual symbol is the macro `SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE` (ALL CAPS macro, not CamelCase function). The plan body hedges with "(or equivalent AUTOPOPULATE_ENGINE_WIDE macro)" which saves it from being a critical drift, but the lead symbol is a fabrication.
- **FINDING M-1 (MEDIUM):** Symbol-existence discipline regression at v1.7.1 M2.B — `SlowPathGateState_PopulateEngineWide` is fabricated. The actual API is `SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE(state->global_gate_state, cfg)`.
- **Triage:** Recommend coding-time correction in plan body or accept as audit-trail drift caught at this re-fire; will not block Phase B Step B.2 compile (macro is the actual mechanism per sister call sites).

### v1.7.1 Verification Item 3: EventLoop_BreakevenOnProfit wrapper deletion in Step C.4

**STATUS: VERIFIED — site exists, deletion sound, no test break risk per available cross-refs**

- `EventLoop_BreakevenOnProfit` wrapper EXISTS at `ControllerEventLoop.hpp:3796-3804` — EXACTLY matches plan body M1.B citation
- Wrapper body at `:3799-3803`: `if (!BITMAP_IS_SET(cfg.lifecycle_cfg_flags, MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT)) return; ... for (c) EventLoop_BreakevenOnProfitOneCore(state, cfg, current_price, c);`
- Post-D1-B + Step C.3 migration, wrapper is no longer called by backtest centralized path (replaced by `SlowPathCycleAllCores` → `SlowPathCycleOneCore` flow); LIVE per_core_slow arch never called wrapper (PARITY-032)
- Plan body M1.B says "DELETE wrapper OR annotate as kept-for-tests if any test invokes directly. Verify via `grep "EventLoop_BreakevenOnProfit\b"` post-C.4 returns ZERO non-test sites." — appropriately defensive; will catch test consumers at Phase B coding time
- Sister `EventLoop_BreakevenOnProfitOneCore` at `:3754` (per-core variant; 4-arg signature: state + cfg + current_price + core_id) MIGRATES with the body to SlowPathCycleOneCore extract — correctly preserved per M2

### v1.7.1 Verification Item 4: Mask rename reconsidered → MASK_BREAKEVEN_ON_PROFIT (kept convention)

**STATUS: STRUCTURALLY CORRECT — but FINDING (M-2) internal-amendment-summary drift**

- Verification of naming convention claim: existing FOREACH_SLOW_PATH_GATE rows produce these MASK_ constants:
  - `MASK_LADDER_ACTIVE`, `MASK_CONFIDENCE_ENABLED`, `MASK_COMPOSITE_ENABLED`, `MASK_RIDGE_WITHIN_ACTIVE`, `MASK_EXIT_BLENDER_ACTIVE`, `MASK_RIDGE_ONLINE_CORR_ACTIVE`, `MASK_THOMPSON_ACTIVE`, `MASK_BANDIT_SHADOW_LEARNING` (PER_CORE)
  - `MASK_LAZY_REBUILD_ACTIVE`, `MASK_WS_FLATTEN_ACTIVE` (ENGINE_WIDE)
- **Convention precedent verified:** `MASK_LAZY_REBUILD_ACTIVE` (ENGINE_WIDE gate) coexists with `MASK_ML_CFG_LAZY_REBUILD_ENABLED` (cfg flag) — same shape as proposed `MASK_BREAKEVEN_ON_PROFIT` (gate) vs `MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT` (cfg flag). Sister precedent SUPPORTS H1.B reconsideration.
- No collision with `MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT` (defined only at `LifecycleCfgFlagRegistry.hpp:96`) — full-token comparison distinguishes them; prefix discipline separates namespaces. CORRECT.
- **FINDING M-2 (MEDIUM):** Plan body INTERNAL DRIFT between v1.7.1 amendment summary line 47 (H1.B says "Rename `MASK_BREAKEVEN_ON_PROFIT` → `MASK_GATE_BREAKEVEN_ON_PROFIT`") + plan body status line 5 (also says "rename to MASK_GATE_BREAKEVEN_ON_PROFIT") + actual body fixes at lines 691, 707, 709 (reconsider and KEEP `MASK_BREAKEVEN_ON_PROFIT` per convention). The summary table H1.B mis-describes what actually landed. ALSO: line 693 still uses `MASK_GATE_BREAKEVEN_ON_PROFIT` framing inconsistently.
- **Triage:** Recommend coding-time correction OR amend H1.B + status line to reflect "rename reconsidered — kept MASK_BREAKEVEN_ON_PROFIT per convention". Substantive fix is correct.

### v1.7.1 Verification Item 5: Decision H arg count math

**STATUS: VERIFIED — 4/2/2 = 8 args**

- Plan body Decision H at line 438: "4 non-null refs + 2 nullable ptrs + 2 PODs"
- Breakdown: cfg + state + tick_ring + core = 4 non-null refs; zoo_ptr + ezoo_ptr = 2 nullable * ptrs; c + core_balance = 2 PODs
- Sum: 4 + 2 + 2 = 8 ✓
- Matches v1.7 O1 8-arg signature ✓
- EngineCommon.hpp:220-228 skeleton signature has exactly 8 args ✓

### v1.7.1 Verification Item 6: Step B.1 LOC estimate alignment

**STATUS: VERIFIED — ~19 LOC body in scope (within ~20-30 LOC range)**

- Plan body line 632 says "~20-30 LOC (v1.7.1 MH-1 correction; v1.6 incorrectly said "~50-90 LOC" assuming DepthRecorder/Notify/trade_log INCLUDED; landed body correctly EXCLUDES per M5)"
- Actual landed body at `EngineCommon.hpp:169-187` is 19 LOC in function scope (line 169 fn opening to line 187 closing brace)
- EXCLUDES DepthRecorder/Notify/trade_log per M5 LIVE-only persistence sink discipline (confirmed)
- INCLUDES EventLoopState_Init + ConfigureKillSwitch (gated MASK_RISK_CFG_KILL_SWITCH_ENABLED) + Regime_Init loop (confirmed)
- M5 LIVE-only persistence sinks correctly EXCLUDED per landed body skeleton header at `EngineCommon.hpp:158-160`

### v1.7.1 Verification Item 7: 3 prose-body sites fixed (lines 112/216/434)

**STATUS: SUBSTANTIVE FIX VERIFIED — line-cite drift (Finding L-1)**

- v1.7.1 M6.B says fixes landed at "lines 112/216/434"
- Actual fixes verified at lines 147 (v1.6 4-pillar Operator-impact row), 251 (Sprint goal tie-back), 469 (Class 24 closure framing) — all 3 NOW use `BITMAP_IS_SET(cfg.lifecycle_cfg_flags, MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT)` framing OR `MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT` lifecycle bitmap framing with M6.B parenthetical correction notes
- **FINDING L-1 (LOW):** Plan body M6.B amendment summary cites stale line numbers (112/216/434) but actual fix landed at 147/251/469 (offset ~+35). Substantive fix is correct; cite drift is cosmetic.
- **Triage:** Cosmetic; do not block. Operator may correct in hypothetical v1.7.2 or accept as audit-trail drift.

### v1.7.1 Verification Item 8: No NEW fabricated symbols

**STATUS: 1 NEW FABRICATION CAUGHT (Finding M-1 above; also Finding L-2 below)**

- Comprehensive grep against v1.7.1 plan body for newly-cited symbols:
  - `AUTOPOPULATE_ENGINE_WIDE` — actual is `SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE` (full-prefixed macro name); plan body uses shorthand consistently — acceptable since longer form is unambiguous
  - `SlowPathGateState_PopulateEngineWide` — **FABRICATED**; does not exist (Finding M-1)
  - `GlobalGateState` — EXISTS at `SlowPathGateRegistry.hpp:205`
  - `global_gate_state.flags` — EXISTS at `ControllerEventLoop.hpp:784`
  - `state.global_gate_state` — EXISTS via `EventLoopState.global_gate_state` member
  - `MASK_BREAKEVEN_ON_PROFIT` — will be auto-generated by AUTOPOPULATE row addition at Phase B Step B.3a (correctly framed as NEW row addition)
  - `MASK_GATE_BREAKEVEN_ON_PROFIT` — appears at lines 5/47/693 as v1.7 → v1.7.1 reconsideration framing; this rename was REJECTED per convention. Cleanup needed (Finding M-2)
  - `MASK_WS_DEAD_TIME_FLATTEN_ENABLED` — at plan body line 707: cited as sister mask for ENGINE_WIDE row at SlowPathGateRegistry.hpp. **PARTIAL FABRICATION (Finding L-2):** actual constant is `MASK_RISK_CFG_WS_DEAD_TIME_FLATTEN_ENABLED` (RISK_CFG_ prefix; the corresponding gate is `MASK_WS_FLATTEN_ACTIVE` at line 115). Pattern claim is structurally correct (WS_FLATTEN_ACTIVE IS the sister gate); mask constant as cited is mis-named.
- **FINDING L-2 (LOW):** Plan body Step B.3a line 707 cites `MASK_WS_DEAD_TIME_FLATTEN_ENABLED` — actual constant is `MASK_RISK_CFG_WS_DEAD_TIME_FLATTEN_ENABLED`. Pattern-correctness intact (sister to `WS_FLATTEN_ACTIVE` row); mask name as cited is mis-spelled.
- **Triage:** Plan body coding-time correction OR accept as audit-trail noise. Doesn't block compilation since the new gate row at Step B.3a body uses correct `MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT` per `cfg.lifecycle_cfg_flags`.

### v1.7.1 Verification Item 9: All v1.7 verifications still PASS

**STATUS: VERIFIED — all PARITY closures intact**

| v1.7 Verification | v1.7.1 Re-verify Status |
|---|---|
| PARITY-026 closure intact (kill_switch sister in BootGlobal) | PASS — landed body at EngineCommon.hpp:176-180 gates on MASK_RISK_CFG_KILL_SWITCH_ENABLED |
| PARITY-027 closure intact (per-core boot via BootPerCore) | PASS — skeleton signature aligned |
| PARITY-028 closure intact (ConfidenceScorer + RollingTurnover in BootPerCore body Phase B) | PASS — body spec at lines 658-670 includes both calls |
| PARITY-029 closure intact (Strategy_InitPerCore in BootPerCore body Phase B) | PASS — body spec at line 671 |
| PARITY-030 closure intact (ApplyBnbDiscount sister called by both LIVE + BACKTEST) | PASS — landed body at EngineCommon.hpp:140-151 |
| PARITY-031 closure intact (BACKTEST_REGIME_SAMPLE_CORE named constant in EngineCommon.hpp) | PASS — landed at EngineCommon.hpp:119 |
| PARITY-032 closure intact (SlowPathCycleOneCore fold-in via D1-B FOREACH_SLOW_PATH_GATE row addition) | PASS — v1.7.1 CRITICAL fix applied (scope/state alignment) |
| Bytewise-identical math intact (v1.6 O2 caller-precompute discipline) | PASS — plan body Step C.1 + C.2 explicit 12-line precompute blocks (LIVE :742-749 + BACKTEST :803-810) |
| Double-fire prevention intact (with H1 expansion) | PASS — Step C.4 enumerates 7 deletion sites + 7-grep verification gate at lines 871-878 |
| HMAC byte preservation intact | PASS — landed body does not touch cfg state read by stamp emit per pre-coding concern #3 |
| AC consistency | PASS — Ship end goal acceptance criteria at lines 236-247 consistent with PARITY-032 NEW closure + 5-helper count + Step C.4 double-fire elimination |

### v1.7.1 Verification Item 10: Step C.4 line range correction (:1925-1955 → :1928-1954)

**STATUS: VERIFIED — exact match**

- Plan body line 868: cites `EngineSharded.hpp:1928-1954`
- Verified at HEAD `09204b3`: line 1928 = `if (cfg.engine_arch != ENGINE_ARCH_PER_CORE_SLOW)` (opening of centralized arch block); line 1954 = closing `}` of the block. EXACTLY MATCHES.
- v1.7 incorrectly cited `:1925-1955`; v1.7.1 corrected to `:1928-1954`. Plan body fix is precise.

---

## Findings summary (per severity)

### CRITICAL

(none — D1-B scope/state alignment FIX VERIFIED structurally sound)

### HIGH

(none)

### MEDIUM

**M-1 — Symbol-existence drift at v1.7.1 M2.B** (NEW; not in 3-agent v1.7 audit)
- Plan body line 690 cites `SlowPathGateState_PopulateEngineWide(&state, cfg)` as the AUTOPOPULATE invocation
- ACTUAL symbol: `SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE(state->global_gate_state, cfg)` (macro, not function)
- Hedge "(or equivalent AUTOPOPULATE_ENGINE_WIDE macro)" present but lead symbol is fabrication
- META-discipline regression: `feedback_verify_symbol_existence_at_plan_drafting_time` (codified v1.5) applies at EVERY amendment cycle per v1.7 D3 promotion; this v1.7.1 amendment introduced a NEW fabrication despite the discipline being Stage 6 cadence-locked candidate
- **Recommended fix:** Plan body coding-time correction at line 690 — replace `SlowPathGateState_PopulateEngineWide(&state, cfg)` with `SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE(state->global_gate_state, cfg)` (matches sister call sites at :2335 + :3555).
- **Effort:** ~5 min single-line edit OR accept as audit-trail drift caught at this re-fire.
- **Severity rationale:** Doesn't block Phase B Step B.2 compilation (the actual macro IS the mechanism); hedge "(or equivalent...)" saves the spec; tagged MEDIUM because META-discipline regression deserves visibility.

**M-2 — Internal amendment-summary drift on H1.B mask rename** (NEW)
- Plan body status line 5 + amendment summary table H1.B (line 47) say "rename MASK_BREAKEVEN_ON_PROFIT → MASK_GATE_BREAKEVEN_ON_PROFIT"
- Plan body actual body fixes at lines 691 + 707 + 709 RECONSIDER the rename — keep `MASK_BREAKEVEN_ON_PROFIT` per convention (sister precedent: `MASK_LAZY_REBUILD_ACTIVE` coexists with `MASK_ML_CFG_LAZY_REBUILD_ENABLED`)
- Line 693 still inconsistently references `MASK_GATE_BREAKEVEN_ON_PROFIT`
- Substantive verdict: CORRECT (keep convention `MASK_BREAKEVEN_ON_PROFIT`); summary table H1.B mis-describes outcome
- **Recommended fix:** Amend H1.B summary row + status line 5 to "H1.B — mask rename RECONSIDERED; kept `MASK_BREAKEVEN_ON_PROFIT` per convention (sister precedent: MASK_LAZY_REBUILD_ACTIVE ENGINE_WIDE coexists with MASK_ML_CFG_LAZY_REBUILD_ENABLED cfg)". Also clean up line 693.
- **Effort:** ~10 min summary table edit + line 693 wording correction OR accept as audit-trail drift.

### LOW

**L-1 — Plan body M6.B cites stale line numbers (112/216/434)** (NEW)
- v1.7.1 M6.B says "3 prose-body sites missed in v1.7 14a sweep: line 112 (4-pillar Operator-impact row) + line 216 (Sprint goal tie-back) + line 434 (Class 24 closure framing)"
- ACTUAL fix locations verified at lines 147 (Operator-impact) + 251 (Sprint goal) + 469 (Class 24)
- Likely cause: line offset shifted during cumulative amendment cycle; cite was made at earlier amendment state
- Substantive fix is correct (all 3 sites NOW use lifecycle bitmap framing); cite drift is cosmetic audit-trail noise
- **Recommended fix:** Optional cite correction OR accept

**L-2 — Step B.3a sister mask cite drift** (NEW)
- Plan body line 707 cites sister ENGINE_WIDE mask as `MASK_WS_DEAD_TIME_FLATTEN_ENABLED`
- ACTUAL constant name: `MASK_RISK_CFG_WS_DEAD_TIME_FLATTEN_ENABLED` (RISK_CFG_ prefix per `RiskCfgFlagRegistry.hpp` convention)
- The corresponding GATE constant (per FOREACH_SLOW_PATH_GATE row WS_FLATTEN_ACTIVE) is `MASK_WS_FLATTEN_ACTIVE`
- Pattern claim ("sister ENGINE_WIDE row at SlowPathGateRegistry.hpp") is STRUCTURALLY CORRECT; mask constant as cited mis-names
- **Recommended fix:** Replace `MASK_WS_DEAD_TIME_FLATTEN_ENABLED` with either `MASK_RISK_CFG_WS_DEAD_TIME_FLATTEN_ENABLED` (cfg-side bit) or `MASK_WS_FLATTEN_ACTIVE` (gate-side bit; sister to proposed `MASK_BREAKEVEN_ON_PROFIT`). The gate-side bit is the more apt sister.
- **Effort:** ~5 min single-line edit

### DOCUMENT-ONLY / NOT-A-BUG

(none)

---

## Cross-cutting concerns

### Meta-discipline regression caught
v1.7.1 introduced ONE NEW fabricated symbol (Finding M-1) despite `feedback_verify_symbol_existence_at_plan_drafting_time` being Stage 6 cadence-locked candidate per v1.7 D3. This validates v1.7 D3's structural-fix decision to land CI tool `tools/check_plan_body_symbol_existence.py` + /readiness Check 31+32 at Phase D Step D.6-D.8 — the CI tool would have caught M-1 + L-2 at amendment-time.

**No new META-discipline needed** — existing one is correct; need to LAND its enforcement mechanism faster. Phase D Step D.6 CI tool is the structural fix; this audit catch demonstrates urgency. Recommend prioritizing D.6 build during Phase D rather than treating as ship-close-end work.

### v1.7.1 META-discipline candidates fold correctly
- /readiness Check 31 Part D (registry-scope-vs-state-struct consistency) — folded correctly at plan body lines 954-961; CORRECT
- /readiness Check 31 Part E (prose-body comprehensive grep at amendment time) — folded at lines 963-967; CORRECT
- Both folded as Part D/Part E NEW additions to Check 31 vs creating new Check 33/34 — matches `feedback_proportionate_response_to_audit_findings` Option C FOLD discipline

---

## Behavior matrix — D1-B scope/state alignment (verify train and serve agree)

| Scenario | LIVE per_core_slow (post-`.B.4`) | BACKTEST centralized (post-`.B.4`) | Identical behavior? |
|---|---|---|---|
| Cfg `MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT` UNSET (default) | AUTOPOPULATE_ENGINE_WIDE walks → predicate false → cached bit 0 → BITMAP_IS_SET returns 0 → `EventLoop_BreakevenOnProfitOneCore` SKIPPED | Same path via SlowPathCycleAllCores → OneCore → same cached bit 0 → same SKIP | YES (bytewise-identical to current per_core_slow + current centralized) |
| Cfg `MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT` SET | AUTOPOPULATE → predicate true → cached bit 1 → BITMAP_IS_SET returns 1 → `EventLoop_BreakevenOnProfitOneCore(state, cfg, current_price, c)` FIRES per-core | Same path → same FIRE per-core | YES (BOTH paths now correctly fire breakeven for cohorts with cfg SET; previously only centralized fired, per_core_slow silently SKIPPED for 5+ years — this IS the PARITY-032 closure) |
| Stale cached bit between cycles | M2.B caller-wiring at SlowPathCycleOneCore entry refreshes cached bit per cycle | Same | YES (assuming M2.B AUTOPOPULATE call lands at body entry per spec) |

**Verdict:** Train↔serve identity preserved by-construction post-D1-B for the breakeven gate.

---

## Suggested ship sequence (v1.7.1 → coding unlock)

**No new ship sequence amendments.** Proceed per v1.7.1 plan body:

1. Phase B Step B.2 (BootPerCore body) — UNLOCK per /parity-check verdict GREEN-WITH-3-MINOR-DRIFT
2. Phase B Step B.3 (SlowPathCycleOneCore body) — UNLOCK with D1-B fix structurally sound; recommend coding-time correction of M-1 + L-2 inline at body authoring
3. Phase B Step B.3a (FOREACH_SLOW_PATH_GATE row addition) — UNLOCK; recommend coding-time correction of L-2 sister mask cite inline
4. Phase B Step B.4-B.6 — proceed per spec

**Triaged items for coding-time correction (NOT blocking):**
- M-1 — Replace `SlowPathGateState_PopulateEngineWide` with `SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE` at plan body line 690 AND at SlowPathCycleOneCore body when authored
- M-2 — Amend H1.B summary table + status line + line 693 to reflect rename-reconsidered (keep `MASK_BREAKEVEN_ON_PROFIT`)
- L-1 — Update M6.B cite line numbers from 112/216/434 to 147/251/469 (cosmetic)
- L-2 — Update Step B.3a sister mask cite from `MASK_WS_DEAD_TIME_FLATTEN_ENABLED` to `MASK_WS_FLATTEN_ACTIVE` or `MASK_RISK_CFG_WS_DEAD_TIME_FLATTEN_ENABLED` per intent

---

## NOT a bug (verified-safe items)

- v1.7.1 D1-B "(or equivalent AUTOPOPULATE_ENGINE_WIDE macro)" hedge at line 690 saves M-1 from being CRITICAL — the actual mechanism is correctly described
- v1.7.1 H1.B rename reconsideration is STRUCTURALLY CORRECT per sister precedent (MASK_LAZY_REBUILD_ACTIVE coexists with MASK_ML_CFG_LAZY_REBUILD_ENABLED) — convention prefix discipline separates namespaces
- All v1.7 verifications hold post-v1.7.1 amendment cycle — no regression introduced
- v1.7.1 amendment cycle stays inside the 7th iteration cycle of the v1.0→v1.7 spiral; per `feedback_iteration_spiral_signals_audit_meta_gap`, the cycle distinction (each cycle addresses semantically distinct concerns: v1.5 mechanical drift / v1.6 body-extract refinement / v1.7 full-audit triage / v1.7.1 implementation-of-v1.7-audit-validated D1-B path) AVOIDS spiral classification — INFLECTION REACHED
- 6 build dirs claim (v1.7.1 L2.B) matches CLAUDE.md doc (filesystem has 8 directories including 2 stale/manual builds — non-blocking)

---

## PARITY_ISSUES.md auto-write contract

**No new PARITY-NNN entries required this re-fire.** v1.7.1 fixes-validation against engine HEAD `09204b3` did not surface new train-serve drift surfaces. The 3 MINOR drift findings (M-1, M-2, L-1, L-2) are PLAN-BODY amendment-internal issues, not engine-side parity gaps.

PARITY-026..032 lifecycle status (verified intact post-v1.7.1):
- PARITY-026 — FIXED at hotfix 6fd0ba3; preserved in landed BootGlobal body
- PARITY-027 — OPEN; closes by-construction at Phase B Step B.2 (BootPerCore body extract)
- PARITY-028 — OPEN; closes by-construction at Phase B Step B.2 ML branch
- PARITY-029 — OPEN; closes by-construction at Phase B Step B.2 Strategy_InitPerCore
- PARITY-030 — OPEN; closes by-construction at Phase B Step B.0 (ApplyBnbDiscount; LANDED)
- PARITY-031 — OPEN; closes at Phase C Step C.4.5 (BACKTEST_REGIME_SAMPLE_CORE deletion)
- PARITY-032 — OPEN (NEW); closes at Phase B Step B.3 + B.3a (D1-B FOREACH_SLOW_PATH_GATE row + cached gate state body read)

---

## Phase B Step B.2 unlock recommendation

**VERDICT: PROCEED**

CRITICAL D1-B scope/state alignment fix structurally sound and aligned with canonical sister consumer pattern. All 13 v1.7.1 targeted fixes substantively correct (3 minor cosmetic drifts surfaced for coding-time triage; none blocking).

Step B.2 (BootPerCore body) explicitly UNLOCKABLE per this re-fire — the CRITICAL was in Step B.3 only (SlowPathCycleOneCore + B.3a FOREACH_SLOW_PATH_GATE row). B.2 boot-path body extract has zero dependency on D1-B fix.

Recommend triaging M-1 + M-2 + L-1 + L-2 inline at Phase B coding time rather than spinning v1.7.2 — discipline regression at M-1 is the structurally relevant catch (validates v1.7 D3 D.6 CI tool urgency), not an architectural issue.

Per consult-before-coding discipline: this synthesis returned to operator for review; will not auto-proceed to Phase B Step B.2 body coding without explicit greenlight.

---

## v1.7.1 iteration-spiral verdict

Per `feedback_iteration_spiral_signals_audit_meta_gap`: v1.7 → v1.7.1 = single-class targeted correction cycle. NOT iteration spiral.

Each preceding cycle addressed semantically distinct concerns:
- v1.5 → v1.6 = body-extract signature refinement (OneCore signature divergence; bytewise-identical math)
- v1.6 → v1.7 = full-audit triage (architectural D1-B/C/D + mechanical 13 fixes + D3 structural)
- v1.7 → v1.7.1 = implementation-of-audit-validated D1-B path (CRITICAL scope/state alignment + sister mechanical sweep)

v1.7.1 cycle scope is fix-the-CRITICAL + clean-up-mechanical = legitimate v1.7-finding implementation cycle. INFLECTION REACHED — further audit cycle NOT recommended; Phase B Step B.2 coding can unlock with the 3 cosmetic drifts triaged.

---

## File:line citation appendix

Source-of-truth evidence (per Section M claim → evidence chain discipline; meta-discipline M4 / Pillar B9):

| Claim | Evidence |
|---|---|
| `state.global_gate_state` member exists in EventLoopState | `CoreFrameworks/ControllerEventLoop.hpp:784` — `GlobalGateState global_gate_state;` |
| `GlobalGateState` struct exists with `uint16_t flags` field | `CoreFrameworks/SlowPathGateRegistry.hpp:205-207` — `struct GlobalGateState { uint16_t flags; };` |
| `SLOW_PATH_GATE_AUTOPOPULATE_ENGINE_WIDE` macro exists | `CoreFrameworks/SlowPathGateRegistry.hpp:236-243` |
| Sister consumer pattern at :2344 (MASK_LAZY_REBUILD_ACTIVE) | `CoreFrameworks/ControllerEventLoop.hpp:2344` — `if (state && BITMAP_IS_SET(state->global_gate_state.flags, MASK_LAZY_REBUILD_ACTIVE)` |
| Sister consumer pattern at :3558 (MASK_WS_FLATTEN_ACTIVE) | `CoreFrameworks/ControllerEventLoop.hpp:3558` — `? BITMAP_IS_SET(state->global_gate_state.flags, MASK_WS_FLATTEN_ACTIVE)` |
| Sister AUTOPOPULATE_ENGINE_WIDE call sites | `CoreFrameworks/ControllerEventLoop.hpp:2335` + `:3555` |
| `EventLoop_BreakevenOnProfit` wrapper exists | `CoreFrameworks/ControllerEventLoop.hpp:3796-3804` |
| `EventLoop_BreakevenOnProfitOneCore` per-core variant exists | `CoreFrameworks/ControllerEventLoop.hpp:3754-3792` |
| Centralized arch trio fires at EngineSharded :1928-1954 | `CoreFrameworks/EngineSharded.hpp:1928-1954` (centralized arch branch open + body + close) |
| ShardedBacktestDriver deletion sites :346/:353-354/:356-364/:366/:367/:378-383 | `CoreFrameworks/ShardedBacktestDriver.hpp` — all verified exact match |
| `MASK_LIFECYCLE_CFG_BREAKEVEN_ON_PROFIT` exists | `CoreFrameworks/LifecycleCfgFlagRegistry.hpp:96` |
| `cfg.lifecycle_cfg_flags` exists | `CoreFrameworks/ControllerConfig.hpp:567` |
| `MASK_RISK_CFG_KILL_SWITCH_ENABLED` exists + used in landed body | `CoreFrameworks/EngineCommon.hpp:176` + `CoreFrameworks/EngineSharded.hpp:749` |
| `cfg.num_execution_cores` exists | `CoreFrameworks/ControllerConfig.hpp:1847` |
| 10 existing FOREACH_SLOW_PATH_GATE rows + 11 with NEW row = ≤16 bit budget | `CoreFrameworks/SlowPathGateRegistry.hpp:69-117` + static_assert at :170-171 |
| Engine HEAD | `09204b3` (skeleton signature update at EngineCommon.hpp UNCOMMITTED) |

---

**End of /parity-check v1.7.1 re-fire report.** Verdict GREEN-WITH-3-MINOR-DRIFT; Phase B Step B.2 unlock RECOMMENDED PROCEED; 3 MINOR cosmetic drifts (M-1 + M-2 + L-1 + L-2) for coding-time triage. CRITICAL D1-B scope/state alignment STRUCTURALLY SOUND per canonical sister consumer pattern verified at `ControllerEventLoop.hpp:2344` + `:3558`. Per consult-before-coding: synthesis returned to operator for review; no auto-proceed.
