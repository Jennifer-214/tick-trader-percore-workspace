# R1 reachability — a-class REFUTE verdict

> Saved verbatim at receipt 2026-08-20 (orchestrator write per `feedback_save_agent_reports_verbatim`).

**Agent:** A-class adversarial · target = R1 of `plans/v5.15-live-readiness/reports/2026-08-20-ml-verification-program/serve-side-exit-load-and-side-key-guard.md` · engine HEAD `417e524` + docs commit (`feat/v5.15-live-readiness`). Default-refuted posture applied; the claim survived every refute avenue.

## Verdict line

**R1: CONFIRMED** — with one **NARROWING**: in `trading_mode=live` the failure is a **loud boot REFUSE**, not silent SimpleDip trading (paper/shadow = WARN + silent SimpleDip; backtest = silent SimpleDip, gate never runs). The reachability claim itself (exit block + ensemble buy path dead without a single-zoo role) is true in **all** modes, source-verified at every link and demonstrated by a compiled runtime probe with a non-vacuous positive control.

## Evidence chain

**Boot leg — `model_handle` is written nowhere else.** Uncapped grep of `model_handle\s*=` across `CoreFrameworks/ Strategies/ ML_Headers/ Backtest/ DataStream/ GUI/ MemHeaders/ FixedPoint/` (roots named per Landmine 19): exactly two production writes — `CoreFrameworks/EngineCommon.hpp:343` (gated on single-zoo `loaded`) and `:350` (strict-verify unload → NULL). Init default is `nullptr` via the X-macro row `MemHeaders/NodeCtxInitRegistry.hpp:101`. `EngineCommon_BootPerCore` is the only boot path for **both** LIVE (`CoreFrameworks/EngineSharded/Run.hpp:1031`) and BACKTEST (`Backtest/BacktestSharded.hpp:306`). Hot-swap never writes it (grep; `HotSwap.hpp` installs into the ezoo only).

**No fallback/auto-detect rescues the single zoo.** `NodeModelZoo_LoadFromDir` (`ML_Headers/NodeModelZoo.hpp:684-714`) is four `stat()` probes of `<dir>/{barrier,regime,exit,buy_signal}.{json,xgb}` (`:233-254`) — absent file = `return 0`, no sibling scan, no `_horizon_*` awareness. `EnsembleModelZoo_AutoDetectFromDir` (`:2501-2534`) scans the **parent** dir for `<base>_horizon_<digits>` names — the base dir itself need not exist. The boot comment at `EngineCommon.hpp:356-358` states this shape is a **designed-for deployment** (v5.11.60: "multi-horizon-only deployments have no base dir"), and `ensemble_handle` is set at `:394` independently of `loaded`. So: pure-siblings layout ⇒ `loaded=0`, ensemble ACTIVE, `model_handle=NULL` — by design on one side of the seam, fatal on the other.

**Dispatch leg — one chokepoint, ensemble-blind.** `EventLoop_RebuildOneCore` wires `ml_ctx.model_handle` / `ml_ctx.ensemble_zoo` as direct copies (`CoreFrameworks/ControllerEventLoop.hpp:2957-2958`) and calls the dispatcher regardless of NULL. `ML_BuildParameters` reads `zoo = mctx->model_handle` (`Strategies/StrategyParameters.hpp:903`) and early-returns SimpleDip at `:913-933` on `!zoo || !NodeModelZoo_HasAny(zoo)` (`NodeModelZoo.hpp:793-795`) — **the guard never consults the ensemble**. The exit block (`StrategyParameters.hpp:1423-1429`) and every `ezoo` buy consult (`:1089-1396`, all nested inside the `:1069` `NODE_MODEL_BUY_SIGNAL` arm) sit downstream of that return. The only other production ensemble buy dispatch, `MLStrategy_BuySignal` (`Strategies/MLStrategy.hpp:204`), is legacy-PortfolioController-only (`CoreFrameworks/PortfolioController.hpp:922`); the sharded registry binds STRATEGY_ML build → `ML_BuildParameters` (`Strategies/StrategyInterface.hpp:141-142`).

**Compiled runtime probe (per directive; `~/.cache/r1probe/r1_probe.cpp`, g++ -std=c++17, no XGBoost — `Model_Predict` stub returns 0.0f at `ModelInference.hpp:955` without touching the fake handle; `Model_IsLoaded` = backend+handle at `:1309-1311`):**

```text
ARM1 zoo=NULL ezoo=populated+flag : exit_predict_call_count=0 sink=-777.0
ARM2 zoo=BUY_SIGNAL same ezoo    : exit_predict_call_count=1 sink=0.000
VERDICT: arm1_exit_block_dead=1 arm2_positive_control_reached=1 => R1 CONFIRMED
```

ARM1 = exact claimed boot state (NULL zoo, ACTIVE ezoo with one fake-loaded `exit_predictor`, flag + sink wired): block never executed. ARM2 = identical except `loaded_mask=NODE_MODEL_BUY_SIGNAL`: block executed, sink written. The positive control defeats the vacuous-probe failure mode.

**Deployment-reality leg.** Every real multi-horizon fixture on disk is the dead shape: `models/classification/{5.15.4_test,5.15_testing,multi06,multi_2year_01}_horizon_*` each contain `barrier.json` + `.stamp`, and **no base dir exists for any family**. Only May-era single-zoo dirs (`test_case_01/` has `barrier.json`) are base-shaped. The trainer **cannot** produce a base-dir model for a multi-horizon run: the save path writes only `models/<side_prefix><subdir>/<run>_horizon_<H>/` (`Backtest/BacktestPanels.hpp:4304-4319`). `models/exit/` doesn't exist (consistent with PARITY-044). Current cfgs set no model dir at all (`engine_sharded.cfg:247` empty), so exposure is prospective — it fires exactly when E.1.2.C deploys Shape A, which the e2e report **recommends** (`e2e-verification-protocol.md` Shape A).

**The narrowing.** `check_all_ml_cores_have_model` (`CoreFrameworks/LiveReadiness.hpp:113-122`) fails on `model_handle == nullptr` for ML nodes — ensemble-blind, same class — and is enrolled `LR_SEV_REFUSE` (`:248`; live blocks boot, paper/shadow WARN per `:42-43`), fired at `Run.hpp:1248`. Backtest never runs the gate (no LiveReadiness call in `BacktestSharded.hpp`). So Shape-A-live = refused loudly (with a misleading "no model" hint while a verified ensemble is attached); Shape-A-paper/backtest = the i-class's silent-SimpleDip story, rate-limited health CRITICAL only (`StrategyParameters.hpp:917-928`, and its text "no model configured (zoo=null)" is wrong — MODEL_LOAD_FAILED isn't set because 5f at `EngineCommon.hpp:405-407` requires BOTH loads failed).

## Refute avenues tried that failed

(1) hidden `model_handle` writer — none (write-set enumeration); (2) LoadFromDir sibling fallback — none (`:684-714`); (3) ensemble-first dispatch elsewhere — `MLStrategy.hpp:204` is legacy-only; (4) mctx carrying the ensemble past the guard — it does (`:2958`), but the guard fires first; (5) backtest divergence — same boot helper, same dispatcher; (6) hot-swap installing a single zoo — never writes `model_handle`, and swap-to-ML is itself REFUSED on NULL (`Run.hpp:1799-1803`).

## Cascade findings (beyond R1, found while there)

1. **Layer-b: the ensemble buy blend requires the single zoo to have a BUY_SIGNAL role specifically.** The entire ensemble buy dispatch is nested inside `:1069`'s `loaded_mask & NODE_MODEL_BUY_SIGNAL` arm. A base-dir **barrier** model (all current fixtures are barrier-trained, trainer role table `BacktestPanels.hpp:4291-4293`) opens the `:913` gate but routes to the `:1029` barrier arm, which never consults `ezoo` → bandits/Thompson/Ridge buy blend **still dead**. The copy-to-base stopgap under-delivers structurally.
2. **Two more single-zoo-blind gate sisters** any fix must co-update: strategy-swap refusal `Run.hpp:1799-1803`; live-readiness `LiveReadiness.hpp:113-122` (and `check_model_max_age_set`/`check_no_feature_hash_drift` at `:125-149` aggregate drift from `model_handle` only → **vacuously pass** for ensemble-only nodes — the ensemble's drift bits never feed the live gate).
3. **Stale comment (arming §2.5, actively misleading on this exact question):** `StrategyParameters.hpp:169-174` claims "ML_BuildParameters checks ensemble_zoo **first**; if active, dispatches through Model_Predict_Ensemble; else falls through to single-zoo path." All three clauses false at HEAD for the function-level flow (single-zoo checked first at `:913`; ensemble consulted only inside the buy arm; `Model_Predict_Ensemble` is the `:1360` selection-mode leaf). This comment is how R1 stays invisible. Suggested wording: "single-zoo gate at :913 runs FIRST — a NULL/empty single zoo SimpleDips even when this pointer is active (see R1/E.1.2.C); ensemble dispatch happens inside the buy_signal arm only."
4. **Sister stale comment in the operator cfg:** `backtest.cfg:376` instructs pointing `core_N_model_dir` at "the BASE path (without _horizon_<H> suffix)" — the documented instruction produces the dead shape, and uses the boot-REFUSED `core_` prefix (same family as the i-class's `NodeModelZoo.hpp:2349` find).
5. **The e2e protocol would catch this if run as written:** its Shape-A runtime expectations (live `pred:`/`thr:`/`conf:` once warm) are unachievable; `staged_prediction` is never written on the fallback path. Its R6 ("if LoadFromDir has a fallback…") is now settled: no fallback. Also its Shape-A expected `primary=buy_signal` would read `primary=barrier` for every current fixture (LoadFromCfg priority `:2204-2224` with barrier-only dirs).
6. Exit gate at `:1425-1429` does **not** require `MASK_EZOO_ACTIVE` — only `exit_predictor_count > 0`. Harmless today (count>0 implies a LoadFromCfg pass) but worth pinning when the gate is restructured.

## Dependent question — minimal change, both branches

**Minimal protocol change (stopgap):** "Shape A deployments must also place a single-zoo role file at the base dir." The trainer does **not** produce one (`:4304-4319`) — it means hand-`mkdir models/classification/<run>/` + copying one horizon's `barrier.json` + `.stamp`. Mechanically sound: single-zoo loads pass `expected_horizon_ticks=0` (`NodeModelZoo.hpp:690-711`) and the horizon-mismatch refusal is gated `expected_horizon_ticks > 0` (`:511-512`), so the copied stamped model loads; scaler identity is preserved by the G.3 shared-scaler invariant. It opens the `:913` gate, makes the **exit block reachable**, and passes the live gate. But (a) it's a hand-managed artifact the ledger/trainer knows nothing about, (b) entry signals come from the single copied barrier model, **not** the ensemble blend (cascade #1), and (c) all three blind sisters stay latent. Acceptable ONLY as the E.1.2.C paper-run stopgap, documented as "exit-on-top-of-single-model-entry."

**Minimal dispatch change (the real fix, recommended):** make the ML gate ensemble-aware — `:913` becomes `(!zoo || !HasAny(zoo)) && !(ezoo ACTIVE && primary_count>0)` — plus the forced companions: (i) null-safe `loaded_mask` reads / an ensemble-reachable arm in the `:1029/:1069` chain, (ii) the scaler seam — ensemble-only cycles must standardize `features[]` from an ensemble handle's scaler (today it happens as a side effect of the single-zoo arms; this is R2 and it bites the exit block's inputs too), (iii) the two gate sisters (`Run.hpp:1799-1803`, `LiveReadiness.hpp:113-122`) get the same OR. That's ~4 sites + one dispatch restructure — a small sub-ship, not a hotfix. The decisive argument for it: the exit predictor **exists only on the ensemble** (`ezoo->exit_predictor`; single-zoo `zoo->exit` has no predict-time consumer, i-class Q2), the trainer **only emits horizon dirs**, and the boot code **already declares ensemble-only deployments supported** (v5.11.60). The `:913` gate is the bug; the deployment shape is the design. Patching the protocol to feed the gate inverts that.

**For the E.1.2.C plan body:** adopt the dispatch fix as the plan's serve-side prerequisite (it is upstream of everything the verification program wants to measure), keep the copy-to-base stopgap only if a paper run must happen before that lands, and fold cascade items 2-4 (gate sisters + both stale comments) into the same commit — they are the same single-zoo-centrism instance, and fixing the gate without the sisters ships a deployment that trades in paper but still refuses live boot.

**Probe artifacts:** `~/.cache/r1probe/{r1_probe.cpp,r1_probe,compile.log,run.log}` — rerun: `cd ~/.cache/r1probe && g++ -std=c++17 -O1 -I/home/caramel/code/FoxML_Trader_v2 -o r1_probe r1_probe.cpp && ./r1_probe` (exit 0 = R1 confirmed with live control).
