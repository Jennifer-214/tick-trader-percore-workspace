---
type: handoff
ship_tag: v5.15.5.F.4d.1.E.0.1 (the FP+replay+locale determinism NET — ~98% coded, NOT yet ship-closed)
predecessor: 2026-05-30-v5.15.5.F.4d.1.E-session4-close-handoff.md (picked up this session; D-97 numeric-foundation was its headline-next)
successor: numeric-foundation unification ship (#11; gated behind .E.0.1 shipping)
engine_head: 1edd50e (12 WIP commits this session 2c8830a..1edd50e, all pushed; Version still 5.15.5.F.4d.1.E.0.5 — NOT bumped, ship-close pending)
workspace_head: 9ebf5f9 (pushed)
branch: feat/v5.15-live-readiness
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (D-90..D-115; NO new D's this session — implementation, not decisions)
in_flight_plan: plans/v5.15-live-readiness/subplans/2026-05-29-v5.15.5.F.4d.1.E.0.1-pre-E1-foundational-fix-net.md (v0.3)
rollback_anchor: v5.15.5.F.4d.1.E.0.5 @ 0b841b3 (last GPG-signed tag)
---

# Session close — .E.0.1 determinism net ~98% coded (all clusters DONE + green + pushed); remaining = finish (D) + codify + ship-close

**Created 2026-05-31** (context full at 98%). Branch `feat/v5.15-live-readiness`. **Pickup: `/accept-handoff <this doc>`.**

## 1. State — .E.0.1 is the NET that gates #11 (decimal) + .E.1 (rename). ~98% coded, NOT ship-closed.

All determinism/correctness clusters are **done, committed, pushed, green** (controller_test 3241/0; engine_gui+foxml_suite+compare_scalers+parity_harness+depth_recorder_test build; hot path untouched):

| Commit | Cluster |
|---|---|
| 2c8830a | FP cluster F-056/057/058 (+F-078) + FP-determinism GOLDEN gate (tools/fp_determinism_golden.* + check_fp_determinism.sh) |
| 69f295e | Phase C replay: F-054/055 (tt::parse_double_fast_advance) + recorder to_chars emit + tools/replay_locale_gate.cpp |
| ed3137e | Locale-determinism class close: boot pin (4 mains, binary-specific placement) + de-race + tools/check_locale_determinism.sh |
| 1e67e32 | STANDING NET: tools/check_determinism.sh umbrella + pre-commit Check F (proven on a real commit) |
| 58060c4 | **HOOK CONSOLIDATION** — see §3 (a real hidden bug fixed) |
| 4f6a4ec + 5d95b35 | F-076 (PARITY-035): zero-init ControllerConfig ctor + StampT has_unique_object_representations static_assert + characterization test |
| 7d6de56 | F-057 completeness: depth_recorder_test native (was 2 of 4 enumerated targets) |
| 1edd50e | verify_ticks.sh col-6→col-1 fix |

## 2. REMAINING .E.0.1 (well-defined, mostly close-out)
- **(D) Adversarial net-verification — IN PROGRESS, finish it.** Inject each regression, confirm each gate goes RED (not just GREEN-on-clean — the lesson from §3): corrupt golden→check_determinism.sh RED; stray setlocale→locale guard RED; remove F-076 ctor→characterization test fails; stage a drift→pre-commit BLOCKS. (Tests 1-2 were mid-run at close; re-run cleanly. A scratch `CoreFrameworks/_adversarial_scratch.hpp` + golden corruption are self-restoring — verify tree clean first.)
- **F-107 + recorder round-trip test: ACCEPTED (documented, no code)** — F-107 locale-closed by boot pin (output/display log); round-trip is to_chars↔from_chars standard-guaranteed + covered by depth_recorder_test + replay_locale_gate.
- **CODIFY (ship-close):** Classes 37+ (raw-struct-byte-equivalence [F-076] + phantom-invariant + global-state-mutation [locale]) + `locale-determinism-discipline.md` DESIGN_SPEC + meta-anti-pattern rows (incl. **"CI verified by direct-invocation ≠ verified by the real trigger"** — the §3 meta-lesson) + the `/post-ship-audit`-as-standing-pre-close-step + PARITY-035/036 status + TECH_DEBT-144 + **determinism-gate concurrency hardening** (check_fp_determinism.sh / check_determinism.sh use FIXED .fpdet_*/.fpg_* temp paths → race on concurrent runs; PID-suffix them).
- **SHIP-CLOSE:** Version.hpp → 5.15.5.F.4d.1.E.0.1, CHANGELOG, GPG tag, postmortem, /sync-workspace. THEN #11 (numeric-foundation, gated behind this).

## 3. The big find this session — the entire pre-commit enforcement was DEAD
`core.hooksPath=.githooks` meant git ran a stale **34-line `.githooks/pre-commit`** (doc-metadata only); the full A-F suite in `tools/hooks/pre-commit` installed to `.git/hooks/` — which git **ignored**. So Checks A-E (incl. .E.0.4/D-115 doc-floor, "proven to block an orphan" — proven by *direct invocation*, never a real commit) had NOT fired on real commits. **Fixed (58060c4):** `.githooks/pre-commit` is now THE canonical hook (A-G suite, zero hardcoded paths, Check E pipe-mask bug fixed); retired tools/hooks + install-git-hooks.sh. **Meta-lesson (codify): verify CI by the REAL trigger, not direct invocation.**

## 4. NEW this session (workspace, pushed)
- Memory `feedback_verify_every_enumerated_site_at_close` (the F-057 2-of-4 gap → enumerate the set + verify EACH done before close; design was fine, it was checklist-completeness). Auto-persists at ~/.claude/.
- `/accept-handoff` **Stage 4.6** (decision-log status reconciliation — surfaces decided-vs-open so a `decided` item isn't re-flagged as a blocker; the canonical-failure that prompted it was D-105 mis-flagged).
- F-076 design-audit doc: `plans/.../E.0.1-audit-reports/2026-05-30-F-076-design-audit.md`.

## 5. Operator collaboration notes (this session's texture)
Heavy rigor was warranted + paid off (caught the dead hook, F-076-as-CRITICAL, the F-057 gap, the cache-line-padding subtlety). Operator repeatedly + correctly pushed "more work now less later?", "is it breaking?", "are we forgetting anything?" — each tightened the work. Proportionality held: F-107/round-trip ACCEPTED, verify_ticks FIXED (not heavy-everything). Operator validated the refactor-under-a-net approach over rewrite-fresh.

## Required reading (pickup)
- This handoff + the .E.0.1 v0.3 plan (`subplans/2026-05-29-...E.0.1-pre-E1-foundational-fix-net.md`) + the F-076 design doc + the new memory. Decision-log D-90..D-115 (unchanged). Baseline CLAUDE.md/local/MEMORY auto-load.

## TaskList (recreate)
Engine: #3 .E.0.1 (in_progress, ~98% — finish D + codify + ship-close) · #4 #11 numeric-foundation (pending, gated behind .E.0.1) · #5 codify · #6 D-98 backlog map · #8 .E.1 rename · #9 SWAR parse · #10 concern-tags. (#1/#2/#7/#11-golden completed.)

## First action
`/accept-handoff <this doc>` → then finish **(D) adversarial net-verification** (re-run cleanly, confirm each gate RED) → **codify** → **ship-close .E.0.1** (Version bump + CHANGELOG + GPG tag + postmortem) → then the numeric-foundation ship (#11) under the now-locked net.

**End of session-close handoff.**
