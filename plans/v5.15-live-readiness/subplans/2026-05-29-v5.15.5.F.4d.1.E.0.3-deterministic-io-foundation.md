---
type: subplan
ship_tag: v5.15.5.F.4d.1.E.0.3
ship_kind: deterministic-IO foundation — modular tt:: parse/format primitive family + string→FPN-exact + H12 byte-equivalence enforcement + cfg locale migration (engine-code + CI tooling; pre-.E.1 foundation)
plan_version: v0.1-STUB
status: DRAFT-STUB (2026-05-29; decisions captured D-84/D-85/D-87; DEEP DESIGN PASS PENDING — focused session, NOT designed at half-attention since string→FPN is a money-path algorithm). ⚠ 2026-05-30: D-97 REOPENED D-85 (binary FPN vs DECIMAL money representation) — decimal leans the correctness winner; the "binary FPN-direct" framing below is SUPERSEDED-PENDING that foundational decision, which is UPSTREAM of this ship's design pass.
predecessor: v5.15.5.F.4d.1.E.0.1 (FP/replay determinism net)
successor: v5.15.5.F.4d.1.E.1 (Foundation; gated behind .E.0.1 + this + Net-1)
parent_umbrella: v5.15.5.F.4d.1.E sub-sprint
sprint: v5.15-live-readiness
risk: HIGH (string→FPN is a NEW algorithm on the ACCOUNTING path; the rest is mechanical migration + CI tooling)
audit_tier: HIGH-RISK
hot_path: UNTOUCHED (parse/format/fingerprint are boot/replay/slow-path; the 500ns hot path parses no strings)
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md (D-84/D-85/D-87)
implements_decision: D-84 (.E pre-rework shape + close-the-class) + D-85 (string↔FPN primitive direction) + D-87 (codification batch)
findings_sidecar: plans/v5.15-live-readiness/plan_checks/E.0.1-audit-reports/2026-05-29-E.0.1-fresh-audits-synthesis.md
sister_specs:
  - wire-format-patterns/struct-padding-determinism-pattern.md (H12 — EXTEND for enforcement, don't duplicate)
  - wire-format-patterns/wire-format-byte-preservation-discipline.md (locale-IO relates; round-trip stability)
  - meta-disciplines/single-source-of-truth-discipline.md (one parser/formatter)
# forward-ref (to be created at .E.0.1/task-#3, NOT yet existing — kept out of sister_specs to avoid a dangling pointer):
#   data-disciplines/fp-determinism-canonical-path-discipline.md (the .E.0.1 sister)
---

# v5.15.5.F.4d.1.E.0.3 — deterministic-IO foundation (STUB — deep design pending)

> ### ⚠⚠ SUPERSEDED-PENDING by the unified numeric-core foundation (D-99/D-101, 2026-05-30 Session-4)
> This STUB's scope (the `tt::` parse/format family + `string→FPN`-exact + H12 byte-equivalence enforcement) is **SUBSUMED into** the **money-numeric-core foundation ship** (`subplans/2026-05-30-v5.15.5.F.4d.1.E-money-numeric-core-foundation.md`) — because D-97 (decimal money) + D-99 (ONE unified `FixedPoint<RADIX,FRAC>` type) make the parse family, the string→decimal primitive, and the H12 stamp enforcement all part of that single unified-core ship's parse + stamp surfaces. **O-2 SETTLED (D-108, 2026-05-30): this `.E.0.3` STUB is RETIRED/FOLDED — NOT a standalone ship.** Scope split: the type + parse-primitive (string→decimal `FromString`, error-detecting `tt::` family, H12 `Fingerprint_Canonicalize`, the guard) → the numeric-foundation ship; the **paced NON-money site migration** (62 float + 132 int → checked primitive, silent-0→handled) → GUARD-TRACKED KNOWN-PENDING (TECH_DEBT-144; `.F`/incremental). The D-85 "binary FPN-direct" framing here is FULLY SUPERSEDED by decimal (D-97). **Do not pick this up standalone — see the foundation doc.**

**Status:** v0.1 STUB (2026-05-29; SUPERSEDED-PENDING 2026-05-30 per D-99/D-101 — see banner above). Decisions captured (D-84/D-85/D-87 — D-85 now superseded by D-97 decimal). The **DEEP DESIGN PASS** is now the **money-numeric-core foundation ship**'s design pass (the `string→FPN` algorithm became `string→decimal`, exact AND simpler). This file TRACKS the original scope so it is not lost; the live plan is the foundation doc.

> ### ⚠ D-97 (2026-05-30) — the binary-vs-decimal direction is REOPENED (UPSTREAM of this ship)
> This STUB's **"binary FPN-direct (D-85)"** framing below is **SUPERSEDED-PENDING** by **D-97**: the money-representation foundational decision (binary FPN vs **DECIMAL** fixed-point) was reopened on operator pushback — **decimal wins on correctness** (exact-by-construction + source-exact vs binary's `1e-19` + correct-by-discipline). **If decimal, the `string→FPN` algorithm here CHANGES fundamentally** — a decimal parse is *digit-accumulate-into-a-scaled-int* (exact AND simpler; **no** `POW10_RECIP` / binary-conversion / rounding — that apparatus existed only to reach binary), and the money type is decimal (features stay binary FPN — they need *determinism*, not decimal-exactness). **The deep design pass MUST resolve D-97 FIRST** — it picks the TARGET TYPE before the parser can be designed. The A/B/C foundational decision doc precedes this ship.

## TL;DR

The deterministic-IO foundation that closes the locale-fragile + lossy float-I/O bug class **structurally** (the primitive + an enforcing CI guard) before `.E.1` builds on it. Per D-84: closing the class (primitive + guard) ≠ migrating every site — the guard de-risks paced cleanup. Per D-85: **binary FPN-direct** (NOT true-decimal); raw-string-record as the recorder end-goal; SWAR-where-clean (correctness leads, off-hot-path).

## Scope (decisions captured; algorithm design pending)

- **Modular `tt::` parse/format family** — locale-immune + correctly-rounded; covers `string→double` (extend the existing `parse_double_fast`/`from_chars`), `string→int` (SWAR), and the emit mirror (extend the existing `FPN_ToString` + `to_chars`).
- **`string→FPN`-exact (binary FPN-direct)** — the HIGH-RISK piece: parse decimal digits as integers + exact integer-scale into the FPN words (no `double` intermediate; ~1e-19 precise; "decimal-exact" is binary-bounded per D-85). Built on the existing `FPN_FromInt` + the `FP64_FromDouble` construction template.
- **Class-closing CI guard** — manifest-scoped grep (determinism/wire/replay files) forbidding `atof`/`strtod`/bare-`%f` except the locale-immune primitives; **DROP** global-`setlocale` policing (GUI save/restore is legit). cfg-`atof` + the 111 integer sites seeded KNOWN-PENDING.
- **H12 byte-equivalence enforcement** — EXTEND `struct-padding-determinism-pattern.md`; close F-076 (a `Fingerprint_Canonicalize`-style helper [NEW] — field-wise into a zeroed buffer; preserve the `fee_rate`-not-maker/taker bundle invariant at `ControllerConfig.hpp:369-370`) + the `CfgFieldDispatch.hpp:471` `StampT` `memcmp`; a `static_assert(no-padding)` / canonicalize-wrapper pattern + a RECURRING_BUG_PATTERNS Class.
- **cfg read + write locale** — migrate the ~35 cfg `atof` sites (PARITY-034) + the GUI cfg-write emit (`SettingsPanel.hpp:1048`).
- **DESIGN_SPECs** — the parse/format primitive (NEW); the H12-enforcement extension; promote the D-70 enforcement-ladder/verifiability-triad.

## Design-pass agenda (resolve BEFORE coding — the focused session)

1. **Input shape:** Binance variable-width decimal — max precision/range? (bounds the SWAR masking + scaling).
2. **Exact-scaling contract:** `frac_int × 2⁶⁴ / 10^k` in 128-bit — exact for what `k` / overflow bounds?
3. **Verification (the HIGH-RISK gate):** prove decimal-exact — differential vs a bignum/decimal reference + property tests.
4. **Edge cases:** negatives, scientific notation (does Binance send `1e-8`?), overflow, malformed — branchless core + `__builtin_expect`-rare guards (H20-compliant).
5. **Round-trip stability:** `parse(s)→FPN→emit→s'` must round-trip (H9 + golden-master); parse + emit are a matched pair (built on `FPN_ToString`).
6. **The boundary:** target-type — FPN/accounting → custom; inherent-`double` → `from_chars`.
7. **Raw-string-record end-goal:** plumb the original Binance string to the recorder (store bytes, defer parse → zero drift); architectural change — scope it.
8. **Build in-engine** (FoxLIB occasional-sync, not primary).
9. **Canonical-sister survey:** extend `parse_double_fast` + `uselocale` + `FromInt` + `FPN_ToString` + `struct-padding-determinism-pattern`; don't reinvent.
10. **Symbol precision:** does the parser use `qty_decimals` (TECH_DEBT-146)?

## End goal + acceptance (draft — finalize at design pass)

**End goal:** the locale-fragile + lossy float-I/O class is CLOSED structurally (primitive + enforcing CI guard) and the accounting `string→FPN` path is decimal-precise + deterministic, so `.E.1` builds on a solid byte-deterministic foundation.

**Acceptance (draft):** the `tt::` primitive family + DESIGN_SPEC; the class-closing CI guard standing (cfg + int sites KNOWN-PENDING); F-076 + the `StampT` `memcmp` closed + H12 enforcement; the ~35 cfg + GUI-write sites migrated; round-trip + decimal-exactness CI gates GREEN; hot path UNTOUCHED (`tools/calls_graph_diff.sh verify`).

## Forward promises / routing

- 111 integer-parse sites → guard-tracked sweep / `.F` (TECH_DEBT-144).
- F-107 → PRE-PAPER-TEST (TECH_DEBT-145). True-decimal representation → deferred (TECH_DEBT-147).
- **Net-1 golden-regen:** if `string→FPN`-exact changes accounting values, the `.E.0.1` golden regenerates deliberately (per D-82 two-foundations — correctness fix, not a determinism regression).

## Cross-references

- Decisions: D-84 / D-85 / D-87 (decision-log v2 addendum).
- Audit synthesis: `plan_checks/E.0.1-audit-reports/2026-05-29-E.0.1-fresh-audits-synthesis.md` + the 13 per-lens + 3 fixaudit reports.
- Ledgers: PARITY-034 / 035 / 036; TECH_DEBT-144..147.
- Memories: [[feedback_close_the_class_vs_migrate_every_site]] · [[feedback_two_foundations_determinism_vs_correctness]] · [[feedback_guard_matrix_bounds_foundation_hardening]].

**End of `.E.0.3` STUB — deep design pass + HIGH-RISK gate pending (focused session).**
