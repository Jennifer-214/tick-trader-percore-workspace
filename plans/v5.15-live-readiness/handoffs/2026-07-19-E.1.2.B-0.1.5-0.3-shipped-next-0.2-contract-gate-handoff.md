---
type: handoff
status: active   # THE one live handoff. Supersedes 2026-07-19-E.1.2.B-handoff.md (0.1.5 AND 0.3 both shipped; 0.2 reframed). ≤1-active enforced by check_handoff_active_singleton.py.
ship_tag: E.1.2.B
plan_type: mixed
sprint_end_goal: make the codebase more maintainable for future development (v5.15 live-readiness professionalization phase)
ship_end_goal: make every toolchain surface (CLI · local-CI · plugin · doc-viewer · AI) a thin consumer of the ONE core, versioned 0.x→1.0.0 — "the code becomes the IDE", felt in the editor
predecessor_handoff: plans/v5.15-live-readiness/handoffs/2026-07-19-E.1.2.B-handoff.md
decision_log: plans/v5.15-live-readiness/decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md
sister_specs: [DESIGN_SPECS/framework-patterns/standardized-tool-io-envelope-and-payload.md, DESIGN_SPECS/doc-disciplines/toolchain-semantic-versioning.md, DESIGN_SPECS/framework-patterns/one-action-toolchain-update-orchestrator.md]
coding_status: 0.1.5 COMPLETE · 0.3 COMPLETE (soak-pending-delete) · 0.2 reframed + NEXT
---

# E.1.2.B — 0.1.5 + 0.3 SHIPPED; next = 0.2 (the toolchain CONTRACT + GATE layer)

**Created:** 2026-07-19 · **Branch:** `feat/v5.15-live-readiness`

## State at handoff write — THREE repos

| Repo | HEAD | Note |
|---|---|---|
| Engine `FoxML_Trader_v2` | **`4c076ed` UNCHANGED** | **ZERO engine code touched all session** (dev-plane only) |
| Workspace | **`62a3f5b`** | plans/specs/decisions/tools (`tools/` is a SYMLINK into here) |
| Plugin `fox-symdeps.nvim` | **`6dcbbd7`** on `feat/hud-robustness` | its OWN repo — 0.3 lives here |

Untracked scratch (not ours; leave): `build_clangd/`, `resume`, `tmpo0ujx6rw/`, `note`.

## What SHIPPED

### `0.1.5` — the tool-I/O substrate — COMPLETE
The envelope substrate everything downstream consumes, **proven byte-identical across C++ and Python**:
- `tools/lib/toolio_schemas.json` — the language-neutral schema registry (payload `{tables}` per kind + the cross-cutting envelope-level `findings/1`), read by BOTH languages.
- `tools/toolio.py` + `toolio_selftest.sh` — the Python emit/read/validate SSoT (`--selftest` 4/4: 1 positive + 3 negative controls).
- foxtag C++ twin: `json_parse` cluster promoted core-ward; `grammar --json` emits the O3 envelope (`categories/ref_subcats/concern/surface/unit_types{name,closable}`); `schema_version` **DERIVED** from the schema doc (never hardcoded); `producer.version` from `tools/TOOLCHAIN_VERSION` (RC-E loud-fail if missing — **verified**: exit 2); `git_head` from `.git/HEAD`. Bare `grammar` byte-identical.
- `parity_check.sh` §3b — by-members grammar parity (191 members), closing a previously-ungated node-model hole.
- **VERIFIED:** `foxtag grammar --json` == `toolio.emit(same data)` byte-for-byte incl. key order.

### `0.3` — plugin cutover — COMPLETE (soak-pending-delete)
- NEW `nodemodel.lua` — the plugin's node-model SSoT, derived from `foxtag grammar --json`. Discovery `opts.foxtag_bin → PATH → script-anchor LAST`. Runs foxtag from the ENGINE ROOT (marker-based, Landmine 5). Negative-probe **throttle** (200 calls in 0.1 ms vs 1.2 ms probe — prevents a per-token subprocess storm on a broken binary). Staleness via the envelope's `git_head`. One-keypress heal (build→refresh→retry); **no bundled fallback**.
- `enclosing_block` now filters openers to the **CLOSABLE** set — fixes a LIVE `FILE`/`MACRO` bug and pre-empts inside-block `[ASSERT]`.
- `tagadapter.install()` **wired live** (was dormant). `health.lua` node-model section.
- NEW `tests/test_tagcontext.lua` (12) — proven discriminating (9/3 against pre-cutover code).
- `parity_check.sh` §3c — plugin node-model parity, **non-vacuous** (count>0 guard + ran-vs-skipped verdict).
- **Both hardcoded `_RETIRED_UNIT` copies RETAINED** (soak-then-delete, D-349).

## ▶ NEXT ACTION — `0.2`, reframed as the TOOLCHAIN CONTRACT + GATE LAYER (D-387)

The toolchain became load-bearing infrastructure the engine work rests on, **without** acquiring the contract+enforcement layer the engine has. Three coupled deliverables:
1. **CONTRACT docs** — per-tool consumes/emits/guarantees/failure-modes. **Seeded already**: `tools/CLAUDE.md` § "Tool invariants + gotchas" (harvested from this session's misfires). Machine-readable half already exists (`toolio_schemas.json` + the envelope).
2. **OUTPUT GOLDENS (D-386)** — pin fact-producer output for a fixed input; deviation REDs; `--bless` re-blessing SHOWS the diff; normalize the volatile frame (`git_head`/`producer.version`/`status`) out. **Distinct from `tools/lib/*_baseline.txt`, which are EXCEPTION lists, not goldens.**
3. **ONE WIRED tools-gate** — **EXTEND `parity_check.sh`** (selftests + parity + goldens) and wire it into commit/session-close. It is **MANUAL-only today** and pre-commit never runs it. **NOT a parallel `gate_check.sh`** — `check_session_docs.sh` gates docs, `parity_check.sh` gates tools.
4. D-374's update-orchestrator stays here as the WRITE side (the gate is the VERIFY side).

**Land it EARLY: it is a verification substrate** — `0.4`–`0.7` and the resumed engine SoA work all inherit it, and per D-385 it converts PARTIAL oracles into TOTAL ones (which is what makes delegation safe).

## Sequence (dependency-ordered, D-387 amended)
`0.2` (contract+gate) → `0.4` (tag-native + `.toolbus/`, layout = the 2nd envelope kind) → `0.5` (UX + RC-F) → `0.6` (AI-plane) → `0.7` (pre-push + verdict producer) → `0.8`→`1.0.0` (0.8 likely floats to 1.1 per D-381).

## Decisions this session (D-380 → D-387)
- **D-380** 0.1.5 typing lock: single-doc JSON · record-set TABLE · schema-as-DATA registry · comments-only-never-code.
- **D-381** V1 = "felt in the editor" (~through 0.5); 0.8 floats to 1.1.
- **D-382** 0.1.5 resolved by a 3-I→3-A cascade (foxtag emits the full envelope; O3 payload; no `.toolbus/` at 0.1.5).
- **D-383** armed I→A sweep precedes IMPLEMENTATION of any toolchain/tag-system change.
- **D-384** re-sweep amendments: `findings/1` restructure · `schema_version` single-source · toolio.py placement · parity by-members.
- **D-385 (M10)** delegate on a TOTAL oracle; hand-review on a PARTIAL one. **Contains a verbatim CORRECTION of its own false first evidence** — read it, it is the sharpest lesson here.
- **D-386** output goldens for the fact-producers.
- **D-387** `0.2` reframed into the contract+gate layer.
- **D-388** scope calls: the knowledge base stays ONE repo (**split PARKED**, trigger = a surface gaining external contributors; even then publish-from-a-subtree); the toolchain gets NUMBERED invariants (T1–T12) but **deliberately NOT** a forked anti-pattern catalog or memory namespace.

## Ledgers + memories
- **TECH_DEBT-245** — the completeness gate is blind to **gitignored** source (`rg` without `--no-ignore`).
- **TECH_DEBT-246** — the gate **never checks FUNCTIONS** for a missing block: **501 anchored `[FUNCTION]` vs 195 `[STRUCT]`** corpus-wide, i.e. the dominant unit type is unguarded. *(Both are holes in the SAME gate — fix the shared enumerator once, at 0.2.)*
- NEW memories: `feedback_structural_fix_over_belt_and_suspenders`, `feedback_delegate_on_total_oracle_handreview_on_partial`.
- **`tools/CLAUDE.md` now carries a numbered T1–T12 invariant index** (the dev-plane's H1–H22). **Cite them by ID.**

## PENDING / residual
1. **`0.3` soak-then-delete** — the two `_RETIRED_UNIT` copies are still in-tree by design. Delete ONLY after the plugin parity section passes **NON-SKIPPED** (nvim present) plus a dogfood soak. A SKIP prints an explicit "does NOT authorize deleting" note.
2. **M10 codification residual** — the memory + T12 landed; §11.5 also wants a DESIGN_SPEC + a skill amendment + a `/readiness` check. Not done.
3. **`format_derived`** remains a residual mirror (the envelope carries no plane→axis payload). Homed, not fixed.
4. **Repo split** — considered and **PARKED** (operator: current is fine; a defined split may eventually be needed but is not the concern now). Trigger if revisited: a surface gaining external contributors on an independent release cycle.
5. Pre-existing, NOT ours: `test_byte_layout_cascade` fails (engine line-number drift — proven pre-existing by stashing the cutover); the decision log carries ~146 stale line-anchors (long-lived doc; TECH_DEBT-089 covers drift cadence).

## HARD rules
1. **Dev-plane** — `tools/CLAUDE.md` governs; engine H1–H22 do NOT apply here. Cite **T1–T12** for toolchain rules.
2. **`[SCHEMA]_[v1.0]` is LOCKED** — the tool-I/O JSON is a SEPARATE serialization riding the same vocab.
3. **T12/D-385** — classify the oracle before delegating. PARTIAL ⇒ read the diff yourself before commit. Green is not done.
4. **T11/D-383** — run the armed I→A sweep BEFORE implementing a toolchain/tag-system change.
5. Never touch engine code on this ship (it has stayed at `4c076ed` all session).

## Arming for the next action
- **Read:** `tools/CLAUDE.md` (T1–T12 + the gotchas section — the harvested invariants) · D-385/386/387 · the 0.1.5 + 0.3 sidecars.
- **Run (not just read):** `tools/foxtag/parity_check.sh` (the gate you're extending) · `tools/toolio_selftest.sh` · `./tools/check_session_docs.sh` · `foxtag grammar --json | head` (the golden's first target).
- **Skills:** the 0.2 dive fires its own armed I→A sweep (T11). `/registry-fit-audit` + `/merge-scan` for the gate-extension (EXTEND `parity_check.sh`, do not add a parallel gate).
- **Anti-patterns:** Class-18 (mirror) · **Class-51** (vacuously-green — the golden's own teeth must be proven by perturbation).

## SECOND CLOSE-OUT PHASE — the meta-layer sweep (operator: *"finish the M9 codification residual … and anything else similar"* + *"be sure we actually captured and harvested everything"*)

The first close-out was committed (`3ba0fe8`/`4aecc95`) and then reopened on operator instruction. **The reopen was correct** — it found six real defects the first pass missed, including one the first pass itself created.

**M10 codification COMPLETE (was the stated residual).** All 8 § 11.5 steps: spec `DESIGN_SPECS/meta-disciplines/acceptance-oracle-totality-before-delegation.md` · `/decision-check` **Stage 0** (classify the oracle before ANY dispatch) · `/readiness` **Check 47** · § 11.5 table row · memory updated to name the spec · step-5 CI **deliberately omitted and labelled as such** (oracle totality is a judgment about coverage, not a parseable property — an honest ⛔ beats a vacuous guard).

**The M9→M10 renumber.** The discipline was written as `M9`; that slot was already earmarked for enumerate-set by the `.E.0.10` close (2026-06-12, decision log `~:1293` *"earmark M8→M9 for enumerate-set"*) and by `DESIGN_PHILOSOPHY.md:819` (2026-05-29). Older claim wins → **M10**, with a numbering tombstone in D-385 so commits `3ba0fe8`/`4aecc95` (which say M9) still resolve. **M9 promoted** to enumerate-set with an explicit ⚠️ PARTIAL-codification marker (TECH_DEBT-248) — a table jumping M8→M10 would have been the same silent-gap defect.

**§ 11.5 was 4-of-8 defective** (each hand-verified, not agent-relayed): M4 cited Checks 36-39 (36 is M1's; M4 owns 37-40) · M7 cited `.git/hooks/pre-commit` while the repo sets `core.hooksPath=.githooks` · M6 cited only its memory, leaving its canonical spec unreachable from the registry · `check-36` cited a memory that **has never existed** · the "aggregating index" claimed M1-M7 while § 11.5 claimed M1-M8, with M2/M4/M5/M6 appearing in it **zero** times. All repaired.

**M5's gate gap — closed now, not deferred (capital-facing).** `/precoding-audit-gate` had **zero** train-serve trigger and the M5 spec was cited by **zero** skills — the highest-capital meta-discipline was unreachable from any gate, though § 11.5 had claimed the trigger since codification. Fire-condition added. Its worked example is 4 CRITs that 6 audits missed, incl. a kill_switch dead 14+ months.

**The citable-ID class (D-389) — the real find.** IDs are a knowledge-base primitive here (`H21`, `Class 28`, `M7`, `D-385`) and **nothing enforced that one ID means one thing.** Verified corpus-wide: `M9` double-defined · **`D-1`..`D-13` double-defined** across two decision logs with genuinely diverging content (`D-7` = *opposite* ImGui dispositions) while the older log's status still read `pre-plan-body` · `WH-7` cited canonically by 5 live docs with no SSoT row · **8 dangling `TECH_DEBT` citations** · **5 split-brain entries** in both `open.md` and `closed.md` (`-107`: 49-globals-OPEN vs 47-globals-CLOSED) · `D-245`/`D-246` minted-then-dropped. Fixed in-flight: the renumber, the v1 supersede banner (**one frontmatter line retiring 13 ambiguous IDs**), WH-7 enrollment.

**Why CI never saw it:** `check_code_tag_blocks.py:170` builds its membership set by **MENTION** (`re.findall` over raw ledger text), so `TECH_DEBT-101` resolves because some other entry's prose mentions it. **A membership set that admits anything it has seen cannot go red** — Class-51, and the direct cause. Prerequisite fix, and it will newly-RED existing by-mention refs.

**Prevention (D-389, → `0.2`):** ONE **extended** tool, not a parallel checker — `check_code_tag_blocks.py` already indexes 5 of 8 namespaces and already resolves dangling refs; it just never reads prose. Lift into `tools/lib/citable_ids.py`, harden the by-mention sets, add 3 namespaces, scan prose. Merged with the § 11.5 coverage checker (**TECH_DEBT-249**) since they share their whole substrate. Namespace-*scope* overlap (`M1`-`M9` = meta-disciplines globally but MEDIUM finding IDs in audit reports; `T1`-`T13` collided on arrival) is **deliberately NOT mechanized** — fix is prefixing finding IDs (`F-M1`) at template level.

**THIRD PASS (operator: *"did we fix and update everything?"*) — the honest answer was NO, and the re-verify found it.** Everything previously CLAIMED had landed (verified in the committed tree), but three first-agent findings had been neither fixed NOR homed — **unhomed debt**: **M1**'s `/trace-deps` cohort-parity amendment, **M2**'s `/parity-check` cross-tool emit-site section AND its plan-template wire-format section, **M3**'s "False-positive surface" template requirement. All four had been claimed by their § 11.5 rows *since codification* and none had ever existed. **All now written**; the rows were corrected to say so (incl. M2's amendment being Section **F**, not E — E is stamp-body schema parity). TECH_DEBT-**251** opened for the classes 1-24 FP-surface backfill — created immediately because citing it first would have been a dangling reference, i.e. the exact class this session closed. **M3 still has no DESIGN_SPEC and no memory, so its codification stays PARTIAL.** 

**Still open — needs YOUR triage, not mechanical work:** the **8 dangling** + **5 split-brain** TECH_DEBT IDs. Each needs re-home-vs-tombstone and HISTORICAL (a frozen postmortem citing a then-real ID) vs LIVE. Only `-125` is confidently LIVE (cited in the CURRENT decision log as a deferral target). This corpus is also what will prove the new guard's teeth.

**The meta-lesson worth carrying:** `-101` was found once, patched once (`closed.md:1300` is literally titled *"cited TECH_DEBT-101 exists in NO ledger file — re-homed here"*), and **never swept** — so 8 siblings survived. Patching an instance without sweeping its class is the M7 signal.

## Capture-completeness (this session — verified by sweep, nothing lost)

A capture-completeness sweep ran at close (operator: *"be thorough on the capture and harvest since there is a lot of critical decisions here"*). It found **2 genuinely floating items — both now captured** — and flagged 3 others that turned out to be **my own bad grep patterns** (the items were properly homed; verified by READING, per `feedback_verify_by_context_not_count`).

- **Decisions → LOGGED:** D-380 … **D-388**, each with paired `<!-- D/C/F -->` + `<!-- STATUS -->` sentinels. **D-385 contains a verbatim CORRECTION of its own original evidence, which was false** — kept deliberately: a partial check misleading its own auditor IS the thesis.
- **Ledgers:** TECH_DEBT-**245** (gate blind to gitignored source) · **246** (gate never checks FUNCTIONS — 501 unguarded units) · **247** (the plugin suite's normalized red). 245+246 are holes in the SAME gate → fix the shared enumerator once at `0.2`.
- **Memories → 2 NEW, indexed:** `feedback_structural_fix_over_belt_and_suspenders` · `feedback_delegate_on_total_oracle_handreview_on_partial` (the M10/T12 behavioural rule).
- **Docs:** `tools/CLAUDE.md` gains the **T1–T12 invariant index** + a "Tool invariants + gotchas" section harvested from this session's actual misfires (foxtag is cwd-sensitive · the converted-file selector is ANCHORED · gate covers structs/registries only · gate skips gitignored · exception-baselines ≠ goldens · `[DERIVED]` is struct-required). Spec `standardized-tool-io-envelope-and-payload.md` corrected (`:43` shape, `:44`/`:46` verdict model). Plan `0.1.5`/`0.2`/`0.4`/`0.5` lines updated.
- **Code:** `0.1.5` (registry + `toolio.py` + selftest + foxtag C++ twin + parity §3b) and `0.3` (nodemodel + closable filter + `tagadapter.install` + tests + parity §3c) — all committed across workspace + plugin repos.
- **Residual, explicitly homed:** the M10 §11.5 codification (DESIGN_SPEC + skill amendment + `/readiness` check) — the memory and T12 landed, the rest has NOT.

## TaskList state at handoff write (recreate via /accept-handoff Stage 7)

| ID | Status | Subject |
|---|---|---|
| #1 | completed | DESIGN D-372 open questions RESOLVED |
| #2 | completed | `0.1` Foundation (foxroots + migrations + import-lint) |
| #3 | completed | Step 0 — rollback tag + sync-workspace |
| #4 | completed | `0.1.5` tool-I/O envelope + O3 payload + schema registry |
| #6 | completed | `0.3` plugin cutover (soak-pending-delete; copies retained) |
| #5 | pending | **`0.2` TOOLCHAIN CONTRACT + GATE layer (▶ NEXT; D-387 reframe)** |
| #7 | pending | `0.4` tag-native surfaces + `.toolbus/` (layout = the 2nd envelope kind) |
| #8 | pending | `0.5` UX + RC-F register-fit + registry cards (the dogfood UX notes land here) |
| #9 | pending | `0.6` AI-plane fact-consumption surface |
| #10 | pending | `0.7` pre-push gate + PRODUCER/GATE `[TAG]` role + verdict producer |
| #11 | pending | `0.8` north-star substrate (likely floats to `1.1`, D-381) |
| #12 | pending | `1.0.0` cohesion close |

*(#4 and #6 are DONE this session. #5 is the live NEXT — reframed, not the original "update skill".)*

## Paste into a fresh session
```
Picking up E.1.2.B. 0.1.5 (tool-I/O substrate, byte-identical C++/Python) and 0.3 (plugin cutover —
node model now DERIVED from foxtag; Lua copies retained pending soak) are both SHIPPED.

Run: /accept-handoff plans/v5.15-live-readiness/handoffs/2026-07-19-E.1.2.B-0.1.5-0.3-shipped-next-0.2-contract-gate-handoff.md

NEXT: 0.2 — the toolchain CONTRACT + GATE layer (D-387): contract docs (seeded in tools/CLAUDE.md)
+ OUTPUT GOLDENS (D-386, distinct from the exception-baselines) + ONE wired tools-gate (EXTEND
parity_check.sh — it is manual-only today — NOT a parallel gate_check.sh). Fix TECH_DEBT-245/246
together (same gate, shared enumerator). Land early: it is the verification substrate 0.4-0.7 inherit.

HARD: dev-plane (cite T1-T12, not H1-H22); [SCHEMA]_[v1.0] LOCKED; T11 = armed I→A sweep before
implementing; T12 = classify the oracle before delegating, PARTIAL ⇒ hand-review before commit.
Engine has stayed at 4c076ed all session — keep it that way.
```
