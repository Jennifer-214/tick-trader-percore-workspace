---
type: handoff
ship_tag: v5.15.5.F.4d.1.D.1
plan_type: doc-system end-goal-language sweep (META; pre-.E gate) — MID-SHIP CHECKPOINT
sprint_end_goal: make codebase more maintainable for future development (v5.15-live-readiness)
ship_end_goal: doc system reflects post-.E architecture (per-node) as canonical context; conceptual per-core→per-node swept + glossary anchor + Class 36 logged; remaining F-H = mechanical landing + ship close
predecessor: v5.15.5.F.4d.1.D (Forward-promise verification CI; SHIPPED 2026-05-28 AM)
successor: v5.15.5.F.4d.1.E.0 per-plan audit phase (OR correctness-first mini-ship — see OPEN DECISION below)
coding_status: mid-ship checkpoint — Phases A+B + timeless-doc sweep DONE+committed; manual cases + Phases F-H remaining
audit_tier: MED-RISK (3-agent /precoding-audit-gate; verified GREEN through 3 cycles)
engine_head: ce2173b
workspace_head: 5151ae5
pre_tag_rollback_anchor: pre-v5.15.5.F.4d.1.D.1 (GPG-signed)
findings_sidecar: plans/v5.15-live-readiness/plan_checks/E.0-audit-reports/pre-implementation-findings/_SESSION-CONTEXT.md
---

# v5.15.5.F.4d.1.D.1 — doc-sweep mid-ship checkpoint handoff

**Created:** 2026-05-28 (end of a long session; `/close-session` ritual).
**Branch:** `feat/v5.15-live-readiness`
**State:** `.D.1` is MID-SHIP. The hard/risky part (the institutional-doc conceptual sweep + 2 bugs caught/fixed/logged) is DONE + committed. Phases F-H are mechanical landing + ship close.

**Engine state:** HEAD `ce2173b`; clean of code changes (untracked `.E.2`-staged DOCS skeletons only). Pre-tag `pre-v5.15.5.F.4d.1.D.1` GPG-signed.
**Workspace state:** HEAD `5151ae5`. NOTE: `CLAUDE.local.md` (gitignored) has uncommitted edits — the 5 new going-forward rules; backed up via `/sync-workspace` at this session's close.

---

## ⚠️ TWO things this handoff carries — read both

1. **`.D.1` doc-sweep ship** — mid-ship; finish F-H + ship (this doc, §A-§D below).
2. **A SEPARATE read-only audit** dropped ~141 findings (2 provisional CRITICALs) on the current engine — durable record + full pickup context at `plan_checks/E.0-audit-reports/pre-implementation-findings/_SESSION-CONTEXT.md` (READ THAT for the audit; it's self-contained). A parallel handoff is being written by the audit agent; this section (§E) is the `.D.1`-side cross-link + the routing strategy so the findings actually get USED. (§F is the OPEN STRATEGIC DECISION the operator raised.)

---

## §A — What `.D.1` is + why

Per operator directive: reshape the doc system to reflect the post-`.E` architecture (per-node sharding + Cluster/Node/Deployment + multi-exchange + headless) BEFORE any `.E` coding, so every subsequent ship plans against current canonical context. META work; NO engine code touched.

Scope decision (operator option A): **conceptual** `per-core`→`per-node` swept in forward-looking timeless docs NOW; **code symbols** (`CoreContext`/`MAX_CORES`/`state.cores`/`FOREACH_PER_CORE_CFG_FIELD`/cfg-fields) **defer to `.E.1`** (rename with the code, citations together); **historical-record docs** (postmortems/handoffs/changelogs/bug-catalog/shipped-plans) **preserved + bridged** via glossary, NOT rewritten (per `feedback_terminology_evolution_bridge_not_history_rewrite`).

## §B — What's DONE + committed

- **2 CI tools** (`tools/check_doc_rename_classification.py` w/ `--apply` mode + `tools/check_plan_body_tests_section.py` = Check 45); 22 unit tests green. Engine commits `5b00ed4`, `c2a8044`, `ce2173b`.
- **Canonical pre-commit hook** — rewrote `tools/hooks/pre-commit` as single-source-of-truth (Check A B-Plus + B Check-11 + C doc-rename + D tests-section); closed a `.D`-era drift (Check 11 was in the installed hook but not the versioned source); `cmp`-parity verified.
- **~525 conceptual renames** across timeless docs (engine DOCS + workspace DOCS + DESIGN_SPECS + CLAUDE.md/local + DESIGN_PHILOSOPHY). Verified: no broken file-path links, no corruption, code symbols correctly deferred, bug-catalog preserved. Workspace commits `c95921f`, `5151ae5`.
- **Phase B glossary** — DESIGN_PHILOSOPHY `§ 15 Glossary` (canonical SSoT: Deployment/Cluster/Node/ExecutionCore/CPU-core/threading/economic/artifacts/mode-version) + terminology-evolution bridge note.
- **Class 36** logged (RECURRING_BUG_PATTERNS row + sub-file, 2 sub-shapes: overlapping-span corruption + file-path-rename broken-links) — caught DURING this ship's tool dev; forward-relevant to `.E.1`'s 5,000-site code rename. Operator's "never reintroduce" ask.
- **5 NEW memories** + MEMORY.md index + CLAUDE.local.md going-forward rules (this session's close): `feedback_sequential_audit_for_granular_operator_triage`, `feedback_test_change_enumeration_per_plan_body`, `feedback_proactive_rename_candidate_surfacing`, `feedback_plan_body_length_no_target_loc`, `feedback_terminology_evolution_bridge_not_history_rewrite`.
- **rename-candidates running list** seeded (11 entries) at `plans/v5.15-live-readiness/rename-candidates-running-list.md`.

## §C — What REMAINS in `.D.1` (mechanical landing)

Run the tools — they're built + tested. Each `--apply` defaults to PREVIEW; add `--write`; ALWAYS verify with the broken-link + corruption greps (see commit history for the exact grep) + re-scan after.

1. **Manual cases (~16)** — from the classifier TSV, hand-decide: `per-CoreContext` (→ leave for `.E.1`; CoreContext is a code symbol), `Two-Position-per-Core` heading in `DOCS/CLAUDE_INVARIANTS.md` (conceptual → rename to per-Node by hand), `legacy single_core` refs in STRATEGY_INTERFACE/REPO_CLEANUP_GUIDE (leave as historical descriptions), code-fence-target cfg-registry examples (leave — code), sister-`§`-xrefs (verify).
2. **Phase F** — (a) active `.E` plan-body sweep: `python3 tools/check_doc_rename_classification.py --apply --write --exclude NONE --scope <each .E.* plan body + E-MASTER-REFERENCE + MASTER + running-list + decoupling-roadmap>` (~14 conceptual renames; transition-context auto-LEFT). (b) decision log D-65 + D-66 (PRE-DRAFTED in `.D.1` plan body Phase F.2 — copy verbatim into `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md`). (c) plan-template "Tests changed" section (PRE-DRAFTED Phase F.3 → `DESIGN_SPECS/plan-templates/future-oriented-plan-template.md`). (d) `.E.1` gets a Tests-changed section (Check 45 flagged it modifies `controller_test.cpp`). (e) `/readiness` SKILL.md Check 45 row (PRE-DRAFTED Phase F.4).
3. **Phase G** — decoupling-roadmap reframe (PRE-DRAFTED Phase G.1: status field + "lands at `.E.2`" Status section + preserve historical body) + dir-banner bridge READMEs in `postmortems/` + `handoffs/` (point to DESIGN_PHILOSOPHY § 15 evolution note).
4. **Phase H — ship close** — CLAUDE.md glossary cross-ref (How-to-find) + verify (tool final run 0-unintentional + comprehensive `rg` + tests baseline) + Version.hpp bump `5.15.5.F.4d.1.D` → `5.15.5.F.4d.1.D.1` + CHANGELOG row + GPG-signed tag + postmortem + TECH_DEBT-NEW entries (`engine_mode` vestigial cfg; `BacktestSharded_Run` unification; `check_per_core_registry_integrity.py` rename — all → `.E.1`; SKILL.md Check 44 table-row sister-gap → `.F`).

All Phase B/F/G/H pre-drafted content lives in the `.D.1` plan body (`subplans/2026-05-28-v5.15.5.F.4d.1.D.1-doc-system-end-goal-language-sweep.md`, v0.5).

## §D — `.D.1` plan body + verification record

Plan body v0.5 (GREEN through 3 audit cycles: cycle1 12 findings → cycle2 8 → cycle3 5, all addressed; convergent, not META-gap). All cited tools/files exist. The Class 36 + file-path + conceptual-scope corrections are baked into the plan body's A.4/A.5 execution notes.

---

## §E — SEPARATE AUDIT carry-forward (ensure findings get USED in `.E`)

**Durable record + full pickup:** `plan_checks/E.0-audit-reports/pre-implementation-findings/_SESSION-CONTEXT.md` (self-contained; read it first for the audit). Per-ship sidecars there: PRE-PAPER-TEST (55), E.1 (30), E.2 (14), E.3 (10), E.5 (6), E.6 (2), BACKLOG-STANDALONE (24); + `codebase-wide-ml-bandit-audit.md` (F1-F22).

**2 provisional CRITICALs (do not lose):**
- `conc-5` — multi-producer race on per-core `submit_queue` SPSC ring (drainer thread + per-core slow thread both push → single-producer invariant violation). **LIKELY closed by `.E.1`'s drainer absorption** — the `.E.1` plan confirms drainer→per-node-slow-path absorption + TECH_DEBT-129 closure, so the slow-path SHOULD become the sole submit_queue producer. BUT "second-producer removed" is an INFERENCE, not an explicit `.E.1` statement — **VERIFY at `.E.1` design + runtime-confirm via tsan; do NOT skip the fix on assumption** (it's a CRITICAL).
- `live-bc-1` — user-data WS at Binance GLOBAL while orders/listen-key use Binance US (endpoint split breaks live fills). Confirm via config trace.

**Routing strategy (operator-decided 2026-05-28) — fold INTO `.E`, don't run a parallel fix-stream:**
1. **Order:** `.D.1` ships FIRST (clears the rename collision — codification touches RECURRING_BUG_PATTERNS / DESIGN_SPECS / TECH_DEBT / PARITY / `.E` plan bodies / E-MASTER-REFERENCE, all rename-owned until `.D.1` lands).
2. **Then codify** (per `_SESSION-CONTEXT` §7.1): Classes **starting at 37** (36 = `.D.1`'s) + ~97 CI-tool specs + ~122 test specs + NEW DESIGN_SPECS (`/doc-create`) + TECH_DEBT/PARITY rows + **`findings_sidecar:` backlink in each `.E.X` plan body frontmatter** + E-MASTER-REFERENCE index rows. Run `/capture-audit` after.
3. **Then runtime-confirm** the 49 provisionals (esp. the 2 CRITICALs + fixedpoint cluster: build with/without `USE_NATIVE_128`, diff `FPN_Sqrt`) on an **ISOLATED throwaway clone** — never the canonical tree.
4. **Fixes land at `.E` coding time**, per each sidecar's `fix_ship` + `dont_carry_forward` — because `.E` rewrites those surfaces anyway (fixing `conc-5` in current code = throwaway IF `.E.1`'s drainer absorption closes it as expected — verify that it actually does).
5. **PRE-PAPER-TEST 55** gate the paper-test milestone — focused correctness ship (after runtime-confirm).

**PRE-`.E` CHARACTERIZATION + CI SAFETY NET (operator-proposed 2026-05-28; the "ensure nothing breaks" gate — expand `.E.0` to include this):**

The right way to do a 5,000-site rename + concurrency-model change safely = lock current correct behavior in tests BEFORE the rework, so a regression breaks the build the instant `.E` introduces it. Make this part of `.E.0` (already the "audit all `.E` plans before coding" gate):

1. **Audit each `.E` plan against its findings sidecar; lay out a detailed per-finding fix-design IN the plan body** before that ship codes (the "extremely detailed solutions" — every routed finding gets a concrete fix spec + which `.E` ship lands it).
2. **Split the 122 `test_gap` + 97 `ci_tool_candidate` findings into PERSIST vs CHANGES-BY-DESIGN:**
   - **PERSIST** (behavior `.E` KEEPS — `FPN_Sqrt` determinism, fee/accounting correctness, regime-given-input, parser byte-equivalence): write as **characterization tests BEFORE `.E`** = the regression safety net. The `core→node` rename updates their symbol names at `.E.1`; the **assertions stay** → they catch behavioral drift through the rename. Set up the PERSIST CI invariant checks (H1-H20 compliance etc.) here too.
   - **CHANGES-BY-DESIGN** (behavior `.E` intentionally alters — e.g. `conc-5` drainer-SPSC: `.E.1` removes the drainer): write the NEW-invariant test ("per-node submit_queue single-producer") AS PART OF the `.E` ship, NOT before.
3. **Runtime-confirm provisionals FIRST** — never write a test/CI for an unconfirmed bug (49 provisional incl. both CRITICALs).
4. `live-bc-1` (config endpoint split) → fix in current code (`.E` won't auto-fix it). `conc-5` → EXPECTED closed by `.E.1` drainer absorption (verify at `.E.1` it truly removes the 2nd submit_queue producer; don't write a throwaway current-code fix, but don't skip it either if `.E.1` turns out not to close it).

This DISSOLVES the §F Path-1-vs-Path-2 tension: the correctness-first work BECOMES the safety net that makes the `.E` rework safe. The CI/test setup touches engine `tests/` + `tools/` (NOT rename-owned by `.D.1`), so it can begin once findings are codified (post-`.D.1`-ship).

### CONCRETE `.E.0` UPDATE SPEC (the "update .E.0 with the needed stuff" the operator asked for)

**File to edit:** `plans/v5.15-live-readiness/subplans/2026-05-28-v5.15.5.F.4d.1.E.0-precoding-plan-audit-verification.md` (the `.E.0` plan body — currently a pure-audit gate; EXPAND it to also ingest findings + build the safety net). Do this AFTER `.D.1` ships (plan bodies are rename-owned until then).

**ADD to the `.E.0` plan body:**
1. **A new phase — "Findings ingestion + pre-`.E` safety net"** (sits in `.E.0`, before any `.E.1` coding). Its steps = the 4 in the subsection above (runtime-confirm provisionals → split PERSIST vs CHANGES-BY-DESIGN → write PERSIST characterization tests + persist CI invariant checks → per-finding fix-designs).
2. **Per-`.E`-ship findings audit** — for each ship, audit its plan body against its sidecar + lay out a detailed per-finding fix-design IN that ship's plan body. The sidecar→ship mapping (already `fix_ship`-routed):
   - `E.1-findings.md` (30) → `.E.1` plan body · `E.2-findings.md` (14) → `.E.2` · `E.3-findings.md` (10) → `.E.3` · `E.5-findings.md` (6) → `.E.5` · `E.6-findings.md` (2) → `.E.6` · `PRE-PAPER-TEST-findings.md` (55) → its own correctness gate · `BACKLOG-STANDALONE-findings.md` (24) → TECH_DEBT/`.F`.
3. **Per-`.E`-ship plan body edits (the enforcement wiring, per `_SESSION-CONTEXT` §7.5):** add `findings_sidecar: <path>` to each `.E.X` plan body frontmatter + cite the sidecar in that plan's D-52 "Required reading + reference docs cross-check" section (the section already exists in every `.E` plan per D-52). `/capture-audit` then verifies the link holds.
4. **`.E.0` acceptance-criteria additions:** "all PERSIST characterization tests written + green on current engine" + "all CI invariant checks live" + "each `.E.X` plan body has `findings_sidecar:` + per-finding fix-designs" + "49 provisionals runtime-confirmed (or explicitly accepted/dismissed)" — i.e., `.E.0` GREEN now ALSO means the safety net is up.

**Everything needed to do that edit is referenced in §G** (audit sidecars + `_SESSION-CONTEXT` §7.5 enforcement + the `.E` plan bodies + codification-format docs + `/doc-create`). The `.E.0` plan body's existing 6-audit-dimension structure stays; this is additive.

**Enforcement (so findings don't rot — per `_SESSION-CONTEXT` §7.5):** the GUARANTEE is (1) CI checks + regression tests (machine-enforced; reintroduced bug breaks the build) > (2) plan-body required-reading link (`findings_sidecar:` + D-52 section — `/capture-audit` verifies) > (3) per-ship `/precoding-audit-gate` input. Don't mistake "findings filed" for "findings handled."

**Discipline (do not violate):** no engine code changes; no codification into rename-owned files until `.D.1` ships; run-required steps on disposable clones only; finding line-numbers are pre-rename (cite by symbol + structure).

---

## §F — OPEN STRATEGIC DECISION (operator raised 2026-05-28; not yet decided)

Operator voiced doubt ("reworking the architecture was a bad idea... but idk... it feels more useable"). Framed for next-session decision — **decide with a clear head, not at session-end fatigue:**

- **Path 1 — proceed with `.E`.** Merits: it's genuinely MORE USABLE (headless service + multi-exchange-1-row + per-node sub-account isolation + viewer split + TUI/Grafana); it's year-held vision-convergence (`project_e_series_is_vision_convergence_not_scope_balloon`), not a whim; it is EXPECTED to close `conc-5` (a CRITICAL — verify at `.E.1`) + Class 26/27/KS-race structurally. The 141 findings are about the CURRENT engine + exist regardless of `.E`.
- **Path 2 — correctness-first de-risk.** Pause `.E`; runtime-confirm + fix the 2 CRITICALs + 55 PRE-PAPER-TEST findings; actually paper-test the CURRENT per-core engine (the literal sprint goal "live-readiness"); THEN decide `.E` from a working baseline. Loses nothing — `.D.1` doc work + audit findings are valuable either way; no engine code committed.

Honest lean (claude): not dumb; "more usable" is the sober read. Either path is defensible; Path 2 earns the `.E` commitment with evidence instead of fatigue. Nothing forces the choice now.

**SYNTHESIS (operator-proposed 2026-05-28; resolves Path-1-vs-Path-2):** they're not exclusive. Do the **PRE-`.E` characterization + CI safety net** (§E) — runtime-confirm CRITICALs + write PERSIST characterization tests + set up CI invariant checks + lay out per-finding fix-designs in the `.E` plans — as an expansion of `.E.0`. That correctness work IS the safety net that makes the `.E` rework safe (a regression breaks the build instantly). So: proceed with `.E` (Path 1's usability win) BUT gated behind the correctness net (Path 2's de-risk). Recommended route.

---

## §G — References to bring docs current before `.E`

- Audit pickup: `pre-implementation-findings/_SESSION-CONTEXT.md` (+ MASTER-BACKLOG + sidecars)
- `.E` context: `E-MASTER-REFERENCE.md`, `decision-logs/v5.15.5.F.4d.1.E-architecture-v2.md` (D-1..D-64; add D-65/66 at `.D.1` Phase F), `subplans/2026-05-28-v5.15.5.F.4d.1.E-dependency-graph.md`, `subplans/2026-05-28-v5.15.5.F.4d.1.E.{0,1,2,3,5,6,X}-*.md`
- Invariants/judging: CLAUDE.md (H1-H20), `DOCS/DESIGN_PHILOSOPHY.md` (now incl. § 15 Glossary), `DOCS/STRATEGY_AND_CODING_RULES.md`, `DOCS/CLAUDE_INVARIANTS.md`, `DOCS/CLAUDE_ML_INVARIANTS.md`
- Codification format: `DOCS/RECURRING_BUG_PATTERNS.md` (Classes 1-36; NEW from 37) + `DOCS/recurring-bug-patterns/`, `DESIGN_SPECS/README.md`, `DESIGN_SPECS/meta-disciplines/{doc-frontmatter-convention,doc-tag-vocabulary,pattern-codification-lifecycle}.md`, `/doc-create`
- Ledgers: `DOCS/TECH_DEBT.md` + `DOCS/tech-debt/{open,in-flight,closed}.md`, `DOCS/PARITY_ISSUES.md`

---

## §H — Pickup (first actions next session)

1. `/accept-handoff plans/v5.15-live-readiness/handoffs/2026-05-28-v5.15.5.F.4d.1.D.1-doc-sweep-checkpoint-handoff.md`
2. **Decide §F** (proceed-`.E` vs correctness-first) — fresh head.
3. **Finish `.D.1`** (§C: manual cases → Phase F → G → H → ship). Tools are built; pre-drafted content is in the plan body. This unblocks the audit codification (collision clears when `.D.1` ships).
4. **Then** the audit codification per `_SESSION-CONTEXT` §7 (Classes 37+, `findings_sidecar:` backlinks, CI/test specs) + runtime-confirm CRITICALs on a disposable clone.
5. TaskList (recreate from this handoff): #15 `.D.1` in-progress; #16 audit codification (deferred until `.D.1` ships); #17 runtime-confirm provisionals; #2-13 `.E` pipeline; the §F decision gates #2+.

---

**End of handoff.** Committed checkpoint is a clean resume point — every option (proceed-`.E`, correctness-first, scope-down) is preserved. The doc sweep + audit findings are valuable in all of them.
